# Copy file này thành `.env` rồi điền giá trị thật. `.env` đã được gitignore.
# Claude Code sẽ expand ${VAR} trong .mcp.json từ env shell — nên cần load .env
# vào shell trước khi mở Claude Code (xem docs/REDMINE-SETUP.md §Set env vars).

# Redmine MCP (xem docs/REDMINE-SETUP.md)
# URL = BASE URL của Redmine (không kèm /projects/.../issues path).
REDMINE_URL=https://redmine.example.com
REDMINE_API_KEY=your-api-key-here
