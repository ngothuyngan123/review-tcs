# 05 — Review Report

> Draft cho Leader verify.

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | `(1) MCP LME TEST STUDIO task #74 (ticket 39641, round 16, branch feature/docs-api)` |
| Tổng số TC review | `613` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `chiều (a) KHÔNG chạy được · chiều (b) 4 vùng thiếu — 2 GAP · 2 RISK · 1 dòng Input thiếu`

> **Input thiếu (chiều a)**: `03-dev-impact.md` **trống hoàn toàn** — Redmine #39641 không có section "Đánh giá ảnh hưởng phía dev", nên không có `F*` / `D*` / `T*` nào để đối chiếu. Mọi kết luận coverage dưới đây chỉ dựa chiều (b).
> **Nguồn chiều (b)**: `task_get_context(74, ["dev_impact","spec_delta"])`. ⚠️ `diffAvailable = false` — **không có diff thật**; `dev_impact` của Studio là phân tích từ Technical Spec đã duyệt, không phải từ code.

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | Toàn bộ task — **không có code để đối chiếu** | `diff code` | 613 TC, 358 báo `pass` | `GAP quy trình` — Studio xác thực trực tiếp remote Bitbucket (2026-08-20): branch `feature/docs-api` **KHÔNG tồn tại** ở cả 3 repo (sns-line, linect-service, backend-mcp-line); base ref `release-product-202606` cũng không tồn tại. Nhưng 613 TC vẫn được chạy 2026-09-15 trên `staging` và báo pass. **Phải chốt: TC chạy trên code nào?** Nếu Studio đúng thì toàn bộ kết quả `pass` không gắn được với code nào | `[BLOCKER]` |
| G2 | 2 middleware mới `api_key_auth` + `api_key_throttle` | `diff code` | `không có` | `GAP` — middleware chạy trước MỌI request `/api/v1/*`; không TC nào kiểm nó **không rò sang route web hiện hữu** `/basic/*`, và `/api/v1/*` không đi nhờ session auth web. Đây là **code dùng chung**, Dev chưa kê danh sách nơi bị ảnh hưởng (BƯỚC 2 câu hỏi 3) | `[BLOCKER]` |
| G3 | `conversation.id_status / is_blocked / is_hide / is_bookmark` bị API ngoài ghi trực tiếp → màn chat 1:1 (FA-001) | `diff code` | `NEW-97` `NEW-118` `NEW-154` `NEW-155` `NEW-472` `NEW-681`…`NEW-685` `NEW-691` | `RISK` — có TC nhưng **13/14 TC `skip`**; chưa TC nào verify tầng 3 (RULE-07): sau khi API ngoài đổi trạng thái thì **màn chat 1:1 của staff hiển thị đúng ngay**. `NEW-691` còn ghi thẳng "cần chốt allow hay reject" → expected chưa chốt | `[MAJOR]` |
| G4 | 2 job nền mới `ApiKeyExpireTask` (quét 60s) + `ApiIdempotencyCleanupTask` (00:05) | `diff code` | 10 TC `tc_group=job` | `RISK` — toàn bộ mang mã `JOB-002`, **không có trong `checklist-lme.md`** → không tính cover; và **0 TC chạy production** dù job nền thuộc RULE-08 | `[MAJOR]` |

- Mỗi dòng ở đây có TC tương ứng ở §5, trừ **G1** (vấn đề quy trình/base ref, không giải được bằng TC — cần Dev cung cấp branch + base ref đúng rồi Studio tính lại diff) và **G4** phần "mã quan điểm lạ" (đã gộp vào I5 §4).

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `28 quan điểm Trigger khớp task · 13 chưa cover đủ` (Q11–Q13 bổ sung 2026-09-18 theo yêu cầu Leader)

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `INTG-LINE-001` | `Cao` (BẮT BUỘC khi gọi LINE API) | `GAP — 0 TC cover`. EP-09/EP-10 gọi LINE Messaging API nhưng **không TC nào** cho LINE trả lỗi / timeout / 429 / 500. Toàn bộ 220 TC gửi tin đều giả định LINE luôn OK | `[BLOCKER]` |
| Q2 | `MSG-005` | `Cao` (BẮT BUỘC khi tiêu thụ quota tin nhắn) | `GAP` — chỉ `NEW-661` (ngưỡng 1000 gói free) + `NEW-662` (`free_send_count`), cả 2 **`skip`** và mang mã lạ `TOOL-NEGCTRL-001` / `DATA-001` → không tính cover | `[BLOCKER]` |
| Q3 | `STATE-CLEAN-001` | `Cao` (BẮT BUỘC khi hủy hợp đồng / ngắt kết nối bot-OA / gỡ tích hợp) | `GAP — 0 TC cover`. Không TC nào trả lời: ngắt kết nối LINE OA / hủy hợp đồng / **đổi bot (FA-039)** thì `api_keys` của bot đó còn gọi được API không | `[BLOCKER]` |
| Q4 | `ENV-003` | `Cao` (BẮT BUỘC khi chạm media / URL-domain / job nền) | `GAP — 0 TC mang mã này` **và 0/613 TC chạy production**. Task chạm cả 3 hạng mục: media URL (ảnh 10MB, video/audio 200MB), domain công bố `api.lme.jp`, 2 job nền. RULE-08 cấm kết luận từ staging | `[BLOCKER]` |
| Q5 | `DATA-AUDIT-001` | `Cao` (BẮT BUỘC khi sửa/xóa dữ liệu nhạy cảm) | `GAP` — 1 TC duy nhất, và chỉ về màn `操作履歴` của **chính API key**. Không TC nào kiểm audit khi **API ngoài ghi dữ liệu nghiệp vụ** (do_action đổi tag / friend info / trạng thái hội thoại) → không phân biệt được thao tác do staff hay do hệ thống ngoài | `[MAJOR]` |
| Q6 | `SEC-001` | `Cao` (BẮT BUỘC khi chạm PII) | `GAP` — `GET /conversations` (trả 100 message chat thật) **không có TC cách ly bot**; chỉ có `NEW-366` (scope read) + `NEW-570` (không lộ field nội bộ). Và **0 TC IDOR**: không TC nào thử khai `bot_id` trong body/query để ép đọc sang tenant khác | `[MAJOR]` |
| Q7 | `FUNC-004` | `Cao` | `RISK — test sai ngưỡng`. `NEW-437` "Ảnh preview vượt 10MB bị từ chối" dùng ngưỡng **10MB**, nhưng spec (`SESSION-216-E644027`) + `NEW-436` xác nhận preview giới hạn **1MB**. Biên thật (1MB + ε) và vùng 1–10MB **không được test** → preview 5MB sẽ lọt | `[MAJOR]` |
| Q8 | `PERM-003` | `Cao` (BẮT BUỘC khi vận hành nhiều LINE OA) | `RISK` — 12 TC cách ly bot nhưng **6 `skip`**, gồm **toàn bộ nhánh ghi/gửi**: `NEW-217` (gửi tin friend bot khác), `NEW-222` (qua `line_user_id`), `NEW-225` (template bot khác), `NEW-243` (do_action chạm friend bot khác), `NEW-66` (danh sách key tách biệt khi đổi bot) | `[MAJOR]` |
| Q9 | `MSG-004` | `Cao` (RULE-06 output cuối) | `RISK` — chỉ `NEW-695` verify tin nhắn hiển thị trên ứng dụng LINE thật, và đang **`skip`**. 220 TC gửi tin còn lại dừng ở response API | `[MAJOR]` |
| Q10 | `COMPAT-LEGACY-001` | `Cao` (RULE-09) | `RISK` — `NEW-668` (template group gửi đủ sub-template) + `NEW-669` (ma trận template legacy) đều **`skip`** và mang mã `REG-SHARED-001` | `[MAJOR]` |
| Q11 | `PERM-003` | `Cao` (BẮT BUỘC khi vận hành nhiều LINE OA) | `GAP — tầng ĐỌC của 4 endpoint`. Cách ly bot chỉ được đặt bẫy ở EP-01/02/03 (`NEW-81`) và nhánh **ghi** của EP-04/05/08 (`NEW-240`/`241`/`242`). **Không TC nào** kiểm `GET /friend-info-fields`, `GET /scenarios`, `GET /templates`, `GET /compliant-statuses` của bot A có lẫn dữ liệu bot B. Riêng EP-06 `/templates` là **0 TC** ở cả 2 nhánh. Chính ghi chú của `NEW-81` dẫn Technical Spec §2.8: 3 bảng liên kết **tag, scenario, friend info** dùng ID người dùng LINE toàn hệ thống — thiếu lọc bot ở bảng cha là rò chéo bot; 2/3 bảng đó (scenario, friend info) lại không có TC đọc | `[BLOCKER]` |
| Q12 | `STATE-CLEAN-001` | `Cao` (BẮT BUỘC khi hủy hợp đồng / ngắt kết nối bot-OA) | `GAP — 0 TC cover`. **Bot hết hạn hợp đồng vẫn gọi được API hay không** — không TC nào trả lời. Quét 613 TC: 80 lần xuất hiện chữ "hợp đồng" đều là **"hợp đồng dữ liệu"** (data contract của response); 40 TC chứa "hết hạn" đều là hạn của **API key** (`expires_at` / 「有効期限」), không phải hạn hợp đồng bot. Khác `Q3` ở chỗ `Q3` là hủy chủ động / ngắt OA, còn đây là hợp đồng tự hết hạn theo thời gian | `[BLOCKER]` |
| Q13 | `MSG-005` | `Cao` (BẮT BUỘC khi tiêu thụ quota tin nhắn) | `GAP — EP-10 không có TC quota nào`. Cả 4 TC quota gói free (`NEW-660`…`NEW-663`) đều **chỉ gắn EP-09**; 37 TC của EP-10 không TC nào chạm `free_send_count` / ngưỡng 1000. Nặng hơn: EP-10 gửi được **5 template/request** và 1 template có thể là **group nở ≥ 3 sub-template** (`NEW-668`) → quy tắc đếm quota của EP-10 chưa được định nghĩa. Kèm theo, nhánh **gửi một phần thất bại phía LINE** → ghi bản ghi lỗi vào màn 「配信エラー」 (FA-028) là **0/613 TC** | `[BLOCKER]` |

**Đã loại khỏi phạm vi** (Trigger khớp nhưng không tìm được bằng chứng có ảnh hưởng — BƯỚC 5a):
- `DATA-BACKUP-001` — Trigger khớp (task **thêm 3 bảng DB mới**). Nhưng không tìm được câu khẳng định nào trong `kho-tcs/fa033-backup-データコピー.md` hay spec cho thấy `api_keys` / `api_key_logs` / `api_idempotency_keys` nằm trong phạm vi dữ liệu được copy khi copy bot. → **Không đề xuất TC**; nếu Dev xác nhận 3 bảng này có trong luồng copy bot thì mở lại.

---

## 3. TC trùng lặp nội dung

**Đã rà 613 TC — không phát hiện trùng lặp thật.**

Quét tự động cho ra **287 cặp giống nhau ở bề mặt** (tiêu đề ≥ 0.72, expected ≥ 0.70, tiền đề ≥ 0.60). Áp đủ **4 yếu tố** — thêm điều kiện *cùng đối tượng + cùng tham số* — thì còn **25 cặp**, và cả 25 đều **không phải trùng**: chúng khác nhau ở giá trị/đối tượng test và là **chủ ý tách** đã ghi trong requirement (`E644014` tách 6 giá trị limit, `E644016` tách biên số message, `E644017` tách 5 phép kiểm template, `E644019` tách 7 nhánh friend info).

Ví dụ các cặp bị loại vì không phải trùng: `NEW-290` /friends vs `NEW-515` /friends/detail (khác endpoint) · `NEW-420/421/422` private class A/B/C (khác dải IP) · `NEW-345` hạn 1 ngày vs `NEW-346` hạn 1 tuần (khác mức hạn dùng) · `NEW-324` block/block vs `NEW-590` block/unblock (khác action).

| Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| — | — | — | — | Không có | — |

- **Gate đã chạy**: không đề nghị xóa TC nào → coverage §1 + §2 không đổi.
- `DUP-INFLATE`: không có.
- `DUP-CONFLICT`: không có cặp TC nào mâu thuẫn expected với nhau. ⚠️ Nhưng cặp `NEW-435` / `NEW-437` tuy **không phải trùng** lại mâu thuẫn với **spec** về ngưỡng preview → đã đưa vào **Q7 §2** + **§6**.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | 24 TC (toàn bộ nguồn) | **TC `fail`/`error` chưa raise ticket bug**: 31 TC fail+error, chỉ 7 TC có `bug_tickets`. 24 TC còn lại không ticket — gồm `NEW-553` (phát hành key HTTP 500 **báo thành công giả**), `NEW-554` (xoá key HTTP 500 làm dòng biến mất giả), `NEW-612` (`/do_action` thiếu `actions`), `NEW-338`, `NEW-329`, `NEW-552`, `NEW-556`, và 8 TC `error` ở nhóm `do_action point_*` | Raise ticket cho từng TC, hoặc triage rõ TC nào là **fail oan do expected sai** (tiền lệ đã có: `NEW-546` từng fail oan vì đòi cột `expiry` không tồn tại) |
| I2 | `[MAJOR]` | Toàn bộ 613 TC | **Pass rate 58.4%** (358 pass / 613). 222 `skip` + 21 `error` + 10 `fail` + 2 chưa chạy = **255 TC không có kết luận test**. Nặng nhất là 2 endpoint ghi: EP-09 `POST /messages/push` 31/121 pass (89 skip), EP-10 `POST /messages/template` 28/99 pass (71 skip) | Chạy lại đủ EP-09/EP-10 trước khi kết luận Phase 1 đạt. Mọi kết luận "đủ TC" ở §1/§2 giữ ở mức `RISK` cho tới lúc đó |
| I3 | `[MAJOR]` | Môi trường thực thi | **0/613 TC chạy production** (596 staging · 10 dev · 5 local). Task chạm đủ 3 hạng mục RULE-08: media/file, URL-domain công bố `api.lme.jp`, job nền | Chạy bộ TC ở Q4 §5 trên production trước release |
| I4 | `[MAJOR]` | Toàn bộ 613 TC | **Không verify được evidence (RULE-02)** — `testcase_list` không trả về trường evidence, nên không xác nhận được 358 TC `pass` có đính kèm đúng loại bằng chứng hay chỉ tự khai. 607/613 TC do **pipeline AI chạy** (`source=ai`, by `phapbt`), chỉ 4 TC chạy manual | Leader mở Studio kiểm evidence của ít nhất nhóm TC ưu tiên Cao; hoặc yêu cầu Studio trả evidence qua MCP |
| I5 | `[MAJOR]` | 316 TC / 21 mã | **21 mã quan điểm không có trong `checklist-lme.md`** → `/review-tc` không map được coverage: `API-CONTRACT-001` (121 TC), `API-001` (43), `TOOL-VAL2-001` (36), `TOOL-NEGCTRL-001` (23), `TOOL-ERRHYG-001` (16), `SEARCH-001` (11), `SEC-INJECT-001` (10), `UI-TABLE-001` (9), `A11Y-001` (8), `DATA-HIST-001` (7), `PERF-LATENCY-001` (7), `AUTH-SESSION-001` (5), `I18N-001` (5), `JOB-002` (2), … | Chốt với Leader: bổ sung các mã này vào `framework/checklist-lme.md` (RULE-10), hoặc map lại về mã chuẩn. Trong lúc chờ, quan điểm nào **chỉ** được cover bởi mã lạ đã bị tính là chưa cover ở §2 |
| I6 | `[MAJOR]` | Spec nguồn | **Không có spec chuẩn trong repo**: `spec-features/` không có feature nào cho public API (chỉ có `payment-system-integration`); `LME-SYSTEM-SPEC.md` không cấp mã `FA-xxx` cho tính năng này. Spec thực tế là **file đính kèm Redmine** — và có **5 bản HTML cùng tên khác size**, description lại trỏ bản **cũ nhất** (`api-detail-spec-proposal_vi (7).html`) trong khi bản mới nhất là attachment `30325` | Chốt bản spec chuẩn với PM/Dev, rồi bổ sung `spec-features/admin/public-api/feature-spec.md` + cấp mã `FA-xxx` |
| I7 | `[MAJOR]` | `03-dev-impact.md` | **Input thiếu toàn bộ** — Redmine không có section "Đánh giá ảnh hưởng phía dev"; checkbox "Tester verify auto-fill chính xác" chưa tick và **không tick được** vì không có gì để verify. Riêng mục 3 (caller đã check) là rủi ro cao nhất: `do_action` ghi vào `tag_line_user` / `friend_information_value` / `scenario_lineuser` / `conversation` — đều là đường ghi của tính năng hiện hữu | Yêu cầu Dev điền 4 mục chuẩn; tối thiểu là **danh sách nơi bị ảnh hưởng của 2 middleware dùng chung** (xem G2) |
| I8 | `[MAJOR]` | 3 mâu thuẫn spec chưa chốt | (a) **Rate limit tính theo `bot`** (Journal #134147, 2026-09-04) vs **theo `api_key_id`** (requirement `SESSION-216-E644012`) — và TC hiện có **cả 2 chiều**: `NEW-706` "hai key cùng bot dùng chung bộ đếm" (pass) vs `NEW-77` "ngưỡng tính theo từng khoá" (skip). (b) Tên bảng `access_key_api` (description Redmine) vs `api_keys` (spec + TC). (c) Cơ chế xác thực **HMAC 3-header** (`E644036`, 2026-08-25) vs **1 header `X-LME-API-KEY`** — ghi chú `NEW-551` nói 3-header "đã bị đảo ngược ngày 04/09" | Chốt từng mục với Dev; TC của phương án bị loại phải archive trên Studio, không để lẫn |
| I9 | `[MINOR]` | `NEW-691` | **Expected chưa chốt** — tiêu đề TC ghi thẳng "Handling status trên hội thoại đang ẩn — **cần chốt allow hay reject**". TC không đo được ở trạng thái này | Chốt hành vi rồi sửa expected (`testcase_update`) — đã đưa vào §6 |
| I10 | `[NIT]` | `NEW-153` | `Ghi chú` của requirement `E644027` nói rõ: không đo được `Content-Length` thì **vẫn cho gửi kèm cảnh báo — đánh đổi có chủ đích**. TC ghi đúng, chỉ cần đảm bảo người chạy không tick Không đạt vì hiểu nhầm là lỗi | Giữ nguyên, thêm dòng cảnh báo cho người thực thi |

---

## 5. TCs đề xuất bổ sung (42)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa001-chat11-11チャット.md` · `kho-tcs/fa012-quanlythe-タグ管理.md` · `kho-tcs/fa015-quanlythongtinbanbe-友だち情報管理.md` · `kho-tcs/fa033-backup-データコピー.md` — **kho chưa có tính năng public API**, không đối chiếu trực tiếp được |
| Vùng regression phát hiện từ kho | `TC-CHT-80`…`TC-CHT-87` (modal クイックアクション: ブックマーク / 対応ステータス変更 / 確認状況を変更 / 非表示・ブロック) — ghi vào **đúng các cột** `conversation.is_bookmark / id_status / is_hide / is_blocked` mà `do_action` của EP-11 cũng ghi → vùng regression thật, đã sinh `TC-DATAAUDIT001-*` + `TC-REGSHARED001-01` |
| Conflict expected vs kho | `Không` |
| GAP dùng lại TC kho (không viết mới) | `Không` — kho chưa có TC nào cover đường ghi từ API ngoài |
| Xác nhận chống trùng | Đã đối chiếu **613 TC ở BƯỚC 0** + 4 file kho — **không TC đề xuất nào trùng** |

**Các GAP/RISK không đề xuất TC mới** (đã có TC, chỉ thiếu việc chạy — theo BƯỚC 5b không đẻ TC trùng):
- `Q8` cách ly bot → chạy `NEW-217`, `NEW-222`, `NEW-225`, `NEW-243`, `NEW-66`.
- `Q9` output cuối trên LINE app → chạy `NEW-695`.
- `Q10` template legacy → chạy `NEW-668`, `NEW-669`.
- `G3` (phần đã có TC) → chạy `NEW-97`, `NEW-154`, `NEW-155`, `NEW-681`…`NEW-685`; riêng tầng "màn chat 1:1 hiển thị đúng" là GAP thật → có TC mới bên dưới.
- `G1` → không giải được bằng TC; cần Dev cung cấp branch + base ref đúng để Studio tính lại diff.

**Ma trận cách ly bot — phủ đủ 11/11 endpoint** (yêu cầu Leader 2026-09-16). Cột "TC" ghi TC chịu trách nhiệm cho từng endpoint; TC có sẵn trên Studio thì **chạy lại**, không viết mới:

| Endpoint | TC cách ly bot | Nguồn |
|---|---|---|
| EP-01 `GET /friends` | `NEW-81` (bước 1) · `NEW-185` | Studio — `Đạt` |
| EP-02 `GET /friends/detail` | `NEW-81` (bước 2) · `NEW-189` | Studio — `Đạt` |
| EP-03 `GET /tags` | `NEW-81` (bước 3) | Studio — `Đạt` |
| EP-04 `GET /friend-info-fields` | **`TC-PERM003-01`** | mới — trước đó chỉ có `NEW-240` (nhánh ghi) |
| EP-05 `GET /scenarios` | **`TC-PERM003-02`** | mới — trước đó chỉ có `NEW-241` (nhánh ghi) |
| EP-06 `GET /templates` | **`TC-PERM003-03`** | mới — trước đó **0 TC** |
| EP-07 `GET /conversations` | **`TC-SEC001-01`** · **`TC-SEC001-02`** | mới — trước đó **0 TC** |
| EP-08 `GET /compliant-statuses` | **`TC-PERM003-04`** | mới — trước đó chỉ có `NEW-242` (nhánh ghi) |
| EP-09 `POST /messages/push` | `NEW-217` · `NEW-222` | Studio — **`Chưa test`**, phải chạy |
| EP-10 `POST /messages/template` | `NEW-225` · **`TC-PERM003-03`** (bước 4–5) | Studio `Chưa test` + mới |
| EP-11 `POST /do_action` | `NEW-239`…`NEW-242` `Đạt` · `NEW-243` `Chưa test` | Studio — phải chạy `NEW-243` |
| Chung (chống giả mạo tenant) | **`TC-SEC001-03`** | mới — 0 TC IDOR trước đó |

**Bot hết hạn hợp đồng** phủ toàn bộ API bằng 2 TC gộp endpoint: `TC-STATECLEAN001-04` (đủ 8 endpoint đọc) + `TC-STATECLEAN001-05` (đủ 3 endpoint ghi), thêm `TC-STATECLEAN001-06` (biên thời điểm) và `TC-STATECLEAN001-07` (gia hạn). Gộp endpoint vì expected giống hệt nhau ở mọi endpoint — tách 11 TC sẽ trùng lặp theo BƯỚC 4b.

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-INTGLINE001-01 | API | INTG-LINE-001 | Public API — EP-09 gửi tin | Normal | manual | Tất cả | LINE Messaging API trả 200 — API báo thành công và tin tới đúng friend | Bot A đã kết nối LINE OA thật; có API key quyền 全権限; friend F là bạn của bot A và chưa chặn OA; có điện thoại thật đăng nhập tài khoản LINE của F | 1. Gọi `POST /v1/messages/push` với 1 message text tới F<br>2. Đọc HTTP status + body response<br>3. Mở LINE trên điện thoại của F, xem hội thoại với OA của bot A<br>4. Mở màn chat 1:1 của admin, xem hội thoại với F | 1 message `type=text`, nội dung `LINE連携テスト01` | Response 200, `success=true`, có `request_id`. Tin `LINE連携テスト01` xuất hiện trong LINE app của F đúng 1 lần, đúng nội dung, gắn đúng profile OA của bot A. Màn chat 1:1 của admin hiện đúng tin đó ở phía OA gửi | | Lấp `Q1` · RULE-06 output cuối · Đánh giá spec: Spec ghi rõ · Evidence: ảnh chụp LINE app + response JSON |
| TC-INTGLINE001-02 | API | INTG-LINE-001 | Public API — EP-09 gửi tin | Abnormal | manual | Tất cả | LINE Messaging API trả 500 hoặc timeout — không báo thành công giả | Bot A như trên; có cách ép LINE Messaging API lỗi (stub/mock gateway hoặc dùng channel token đã bị thu hồi) | 1. Ép LINE API trả 500 (hoặc timeout > ngưỡng)<br>2. Gọi `POST /v1/messages/push` 1 message text tới F<br>3. Đọc HTTP status + `error.code` + body<br>4. Mở LINE app của F kiểm tra có tin nào tới không<br>5. Kiểm màn chat 1:1 của admin | 1 message `type=text`, nội dung `LINE連携テスト02` | API **KHÔNG** trả `success=true`. Trả mã lỗi phân biệt được là lỗi phía LINE (không phải `invalid_request`), thông điệp không lộ channel token. LINE app của F **không nhận** tin nào. Màn chat 1:1 **không** hiện tin giả đã gửi | | Lấp `Q1` · Đánh giá spec: Spec không ghi — cần hỏi Dev mã lỗi cho nhánh LINE lỗi · Evidence: response JSON + ảnh LINE app + log |
| TC-INTGLINE001-03 | API | INTG-LINE-001 | Public API — EP-09 gửi tin | Boundary | manual | product | LINE trả 429 — phân biệt được với 429 của chính LME | Bot A trên **production**; API key còn dưới ngưỡng 60 req/phút của nhóm ghi; có cách đẩy LINE API vào trạng thái 429 | 1. Giữ số request dưới ngưỡng rate limit của LME<br>2. Ép LINE API trả 429<br>3. Gọi `POST /v1/messages/push`<br>4. Đọc `error.code`, header `Retry-After`, header `X-RateLimit-Remaining` | 1 message `type=text` | Response phân biệt rõ 429 **của LINE** với 429 `rate_limited` **của LME**: `X-RateLimit-Remaining` của LME vẫn còn > 0, `Retry-After` (nếu có) phản ánh đúng nguồn LINE. Hệ thống ngoài đọc response biết được nên retry theo nguồn nào | | Lấp `Q1` · RULE-08 (gọi LINE thật) · Đánh giá spec: Spec không ghi · Evidence: response header đầy đủ |
| TC-MSG005-01 | API | MSG-005 | Public API — quota tin nhắn | Normal | manual | product | Gửi tin qua API trừ đúng quota tin nhắn của bot | Bot A trên production, gói có quota tin nhắn; ghi lại số tin đã dùng trước khi test (màn thống kê của LME + LINE Official Account Manager) | 1. Ghi số tin đã dùng trước<br>2. Gọi `POST /v1/messages/push` gửi 3 message text trong 1 request tới 1 friend<br>3. Đợi số liệu cập nhật, đọc lại số tin đã dùng ở cả 2 nơi | 3 message `type=text` trong 1 request | Ghi rõ mức tăng thực tế. ⚠️ **Đơn vị đếm chưa chốt** — Leader chốt 2026-09-16 rằng 1 lần gửi = 1 tin (xem `TC-MSG005-04`), trong khi `NEW-662` ghi expected là tăng theo số message. Chạy TC này để lấy số thật rồi chốt theo §6 mục 10, **không** tự kết luận đúng/sai. Con số ở LME phải khớp LINE Official Account Manager | | Lấp `Q2` · RULE-08 bill tiền · ⚠️ expected phụ thuộc §6 mục 10 · Đánh giá spec: Spec không ghi · Evidence: ảnh chụp 2 màn thống kê trước–sau |
| TC-MSG005-02 | API | MSG-005 | Public API — quota tin nhắn | Abnormal | manual | product | Bot hết quota tin nhắn — API từ chối gửi, không báo thành công | Bot A trên production đã dùng hết quota tin nhắn của kỳ | 1. Gọi `POST /v1/messages/push` 1 message text tới friend F<br>2. Đọc HTTP status + `error.code`<br>3. Kiểm LINE app của F<br>4. Kiểm số tin đã dùng có bị tăng thêm không | 1 message `type=text` | API trả lỗi nêu rõ nguyên nhân hết quota, **không** `success=true`. F **không** nhận tin. Số tin đã dùng không tăng | | Lấp `Q2` · RULE-08 bill tiền · Đánh giá spec: Spec không ghi — cần chốt mã lỗi · Evidence: response JSON + ảnh màn quota |
| TC-MSG005-03 | API | MSG-005 | Public API — quota tin nhắn | Boundary | manual | product | Gửi tin ở đúng ngưỡng quota cuối cùng | Bot A gói free trên production, còn **đúng 1 tin** trong quota (ngưỡng 1000 của gói free) | 1. Gọi `POST /v1/messages/push` 1 message text → ghi kết quả<br>2. Gọi tiếp lần 2 với 1 message text → ghi kết quả<br>3. Kiểm LINE app của friend nhận được mấy tin | 2 lần gọi, mỗi lần 1 message `type=text` | Lần 1 thành công, friend nhận đúng 1 tin, quota về 0. Lần 2 bị từ chối đúng lỗi hết quota, friend **không** nhận thêm tin. Không có tin nào bị gửi mà không trừ quota | | Lấp `Q2` · RULE-08 bill tiền · liên quan `NEW-661` đang skip · Đánh giá spec: Spec không ghi · Evidence: ảnh LINE app + 2 response |
| TC-STATECLEAN001-01 | API | STATE-CLEAN-001 | Public API — vòng đời key theo bot | Normal | manual | Tất cả | Ngắt kết nối LINE OA — key của bot đó mất hiệu lực ngay | Bot A đang kết nối OA, có 1 API key `status=有効` đang gọi API thành công | 1. Gọi `GET /v1/friends` bằng key của bot A → xác nhận 200<br>2. Vào màn cấu hình, ngắt kết nối LINE OA của bot A<br>3. Gọi lại `GET /v1/friends` bằng đúng key đó<br>4. Mở màn 「API連携」 của bot A xem trạng thái key | Key `lme_live_sk_...` của bot A | Sau khi ngắt kết nối, lời gọi API bị từ chối (không trả dữ liệu friend nào). Màn 「API連携」 thể hiện trạng thái nhất quán với hành vi API — không còn key nào hiển thị `有効` mà thực tế gọi được | | Lấp `Q3` · Đánh giá spec: Spec không ghi — cần Dev chốt hành vi · Evidence: 2 response trước–sau + ảnh màn API連携 |
| TC-STATECLEAN001-02 | API | STATE-CLEAN-001 | Public API — vòng đời key theo bot | Abnormal | manual | Tất cả | Hủy hợp đồng / hạ gói — key còn gọi được API hay không | Bot A có API key đang hoạt động; chuẩn bị được thao tác hủy hợp đồng hoặc hạ xuống gói không có quyền dùng API | 1. Gọi `GET /v1/friends` + `POST /v1/do_action` bằng key bot A → xác nhận chạy được<br>2. Thực hiện hủy hợp đồng (hoặc hạ gói) cho bot A<br>3. Gọi lại đúng 2 endpoint trên<br>4. Kiểm dữ liệu friend của bot A có bị thay đổi bởi bước 3 không | Key bot A; `do_action` gắn 1 tag cho 1 friend | Sau khi hủy hợp đồng/hạ gói, cả endpoint đọc lẫn endpoint ghi đều bị từ chối. **Đặc biệt: không friend nào bị gắn tag** bởi lời gọi ở bước 3 | | Lấp `Q3` · RULE-08 bill tiền · Đánh giá spec: Spec không ghi · Evidence: response + đối chiếu dữ liệu friend trước–sau |
| TC-STATECLEAN001-03 | API | STATE-CLEAN-001 | Public API — vòng đời key theo bot | Boundary | manual | Tất cả | Đổi LINE公式アカウント (FA-039) — key trỏ bot cũ hay bot mới | Bot A có API key đang hoạt động và có friend; chuẩn bị luồng 入れ替え đổi sang OA khác | 1. Ghi lại danh sách friend bot A qua `GET /v1/friends`<br>2. Thực hiện đổi LINE公式アカウント cho bot A<br>3. Gọi lại `GET /v1/friends` bằng đúng key cũ<br>4. Đối chiếu danh sách trả về với dữ liệu của OA cũ và OA mới | Key bot A (phát hành trước khi đổi OA) | Hành vi phải **xác định và nhất quán**: hoặc key bị vô hiệu, hoặc key tiếp tục trỏ đúng bot A sau khi đổi OA. Tuyệt đối không được trả lẫn dữ liệu của OA cũ và OA mới mà không phân biệt được | | Lấp `Q3` · regression FA-039 · Đánh giá spec: Spec không ghi — cần Dev chốt · Evidence: 2 danh sách friend trước–sau |
| TC-ENV003-01 | API | ENV-003 | Public API — media trên production | Normal | manual | product | Gửi ảnh/video/audio bằng URL thật trên production | Bot A trên production, key 全権限; có sẵn URL HTTPS public thật cho ảnh JPEG, video MP4, audio M4A; có điện thoại thật của friend F | 1. Gọi `POST /v1/messages/push` gửi lần lượt 3 message media (image, video, audio) tới F<br>2. Mở LINE app của F, mở từng media<br>3. Đối chiếu nội dung media nhận được với file gốc | image JPEG ~9MB · video MP4 ~190MB · audio M4A ~190MB, đều HTTPS public | Cả 3 media tới đúng LINE app của F, **mở và phát được**, đúng nội dung file gốc, không lỗi tải, không bị nén hỏng. Thời gian tới nằm trong ngưỡng chấp nhận được | | Lấp `Q4` + `G4` · RULE-08 media (cấm kết luận từ staging) · Đánh giá spec: Spec ghi rõ · Evidence: ảnh chụp LINE app mở từng media |
| TC-ENV003-02 | API | ENV-003 | Public API — domain công bố | Normal | manual | product | Base URL production `api.lme.jp/v1` hoạt động đúng qua hạ tầng thật | Có API key production hợp lệ; DNS/TLS của `api.lme.jp` đã cấu hình | 1. Gọi `GET https://api.lme.jp/v1/friends` với key hợp lệ<br>2. Kiểm chứng chỉ TLS và redirect<br>3. Gọi lại bằng `http://` (không TLS)<br>4. Kiểm 3 header `X-RateLimit-*` có mặt | Key production; 1 request GET | Endpoint `https://api.lme.jp/v1/friends` trả 200 với envelope chuẩn. Chứng chỉ TLS hợp lệ, đúng domain. Request `http://` không được phục vụ nội dung. 3 header `X-RateLimit-*` có mặt | | Lấp `Q4` · RULE-08 domain · Đánh giá spec: Spec ghi rõ (§2 Base URL) · Evidence: response header đầy đủ + thông tin chứng chỉ |
| TC-ENV003-03 | API | ENV-003 | Public API — job nền trên production | Normal | manual | product | `ApiKeyExpireTask` chạy đúng trên production | Trên production có 1 API key sắp hết hạn (đặt `expires_at` cách hiện tại ~2 phút) | 1. Ghi trạng thái key trên màn 「API連携」 trước thời điểm hết hạn<br>2. Gọi API bằng key đó → xác nhận còn dùng được<br>3. Đợi qua mốc hết hạn + hơn 60 giây<br>4. Xem lại màn 「API連携」 và gọi lại API<br>5. Mở drawer 「操作履歴」 của key | 1 key với `expires_at` = now + 2 phút | Sau mốc hết hạn ≤ 60 giây, màn 「API連携」 hiển thị key là `無効（期限切れ）`, lời gọi API bằng key đó bị từ chối, và 「操作履歴」 có đúng **1** bản ghi loại 無効 (job chạy nhiều vòng không sinh bản ghi trùng) | | Lấp `Q4` + `G4` · RULE-08 job nền · Đánh giá spec: Spec ghi rõ (§4.1) · Evidence: ảnh màn trước–sau + drawer 操作履歴 |
| TC-DATAAUDIT001-01 | Data | DATA-AUDIT-001 | Public API — audit thao tác từ hệ thống ngoài | Normal | manual | Tất cả | Thao tác ghi qua API ngoài truy nguyên được về API key nào | Bot A có 2 API key tên phân biệt (`KEY-EXT-01`, `KEY-EXT-02`); friend F chưa có tag T | 1. Dùng `KEY-EXT-01` gọi `POST /v1/do_action` gắn tag T cho F<br>2. Dùng `KEY-EXT-02` gọi `POST /v1/do_action` gỡ tag T khỏi F<br>3. Mở các màn có lịch sử liên quan (「操作履歴」 của key, lịch sử của friend F, màn quản lý tag)<br>4. Đối chiếu xem có truy ra được thao tác nào do key nào thực hiện không | 2 lời gọi `do_action` từ 2 key khác nhau | Truy nguyên được **thao tác nào do key nào** thực hiện. Nếu hệ thống không ghi được điều này ở bất kỳ màn nào → ghi nhận là **GAP thiết kế**, báo Leader để chốt có bổ sung audit hay không | | Lấp `Q5` · Đánh giá spec: Spec không ghi · Evidence: ảnh các màn lịch sử sau 2 thao tác |
| TC-DATAAUDIT001-02 | UI | DATA-AUDIT-001 | Chat 1:1 — phân biệt nguồn thao tác | Normal | manual | Tất cả | Thao tác của API ngoài không bị hiển thị nhầm thành thao tác của staff | Bot A có staff S đang đăng nhập màn chat 1:1; friend F có hội thoại; API key `KEY-EXT-01` | 1. Staff S đổi 対応ステータス của F trên màn chat 1:1 (modal クイックアクション)<br>2. Dùng `KEY-EXT-01` gọi `POST /v1/do_action` đổi trạng thái xử lý của F sang giá trị khác<br>3. Mở lại màn chat 1:1 và mọi nơi hiển thị người thao tác<br>4. Đối chiếu 2 thao tác | 2 thao tác liên tiếp lên cùng friend F, 1 từ staff 1 từ API | Hệ thống phân biệt được thao tác của staff S và thao tác của hệ thống ngoài; **không** gán thao tác của API ngoài thành tên staff S. Không đủ thông tin phân biệt → ghi nhận GAP và báo Leader | | Lấp `Q5` · regression — dẫn từ `TC-CHT-80`…`TC-CHT-87` (kho FA-001) · Đánh giá spec: Spec không ghi · Evidence: ảnh màn chat 1:1 |
| TC-SEC001-01 | API | SEC-001 | Public API — EP-07 cách ly bot | Abnormal | auto | Tất cả | Đọc hội thoại bằng ID friend của bot khác bị từ chối | Bot A và bot B là 2 bot khác nhau; key `KEY-A` thuộc bot A; friend FB thuộc **bot B** và đã có ≥ 5 message trong hội thoại | 1. Lấy `lmessage_friend_id` của FB (friend bot B)<br>2. Gọi `GET /v1/conversations` bằng `KEY-A` với ID đó<br>3. Đọc HTTP status, `error.code` và toàn bộ body | `KEY-A` (bot A) + `lmessage_friend_id` của friend thuộc bot B | Bị từ chối (403 `forbidden` hoặc 404 `not_found` theo spec đã chốt). Body **không chứa** bất kỳ message, tên, avatar hay metadata nào của FB. Thông điệp lỗi không tiết lộ FB có tồn tại hay không | | Lấp `Q6` · Đánh giá spec: Spec không ghi — cần chốt 403 hay 404 · Evidence: response JSON đầy đủ |
| TC-SEC001-02 | API | SEC-001 | Public API — EP-07 cách ly bot | Abnormal | auto | Tất cả | Đọc hội thoại bằng LINE user ID của friend bot khác bị từ chối | Như trên; lấy được `line_friend_id` (LINE user ID) của FB | 1. Gọi `GET /v1/conversations` bằng `KEY-A`, truyền `line_friend_id` của FB kèm khai đúng loại định danh<br>2. Đọc status + body<br>3. Lặp lại với một LINE user ID vừa là friend của bot A vừa là friend của bot B (nếu dựng được) | `KEY-A` + `line_friend_id` của FB | Bị từ chối như TC trên, không lộ message. Với LINE user ID thuộc cả 2 bot: **chỉ** trả hội thoại thuộc bot A, tuyệt đối không trộn message của bot B | | Lấp `Q6` · Đánh giá spec: Spec không ghi · Evidence: response JSON |
| TC-SEC001-03 | API | SEC-001 | Public API — chống giả mạo tenant | Abnormal | auto | Tất cả | Khai `bot_id` trong request không ép được API đọc sang bot khác | `KEY-A` thuộc bot A; biết `bot_id` của bot B; bot B có ≥ 1 friend và ≥ 1 tag | 1. Gọi `GET /v1/friends` bằng `KEY-A` kèm query `bot_id=<id bot B>`<br>2. Gọi lại kèm `bot_id` của bot B trong JSON body<br>3. Gọi `GET /v1/tags` tương tự<br>4. Gọi `POST /v1/do_action` kèm `bot_id` bot B + 1 friend của bot B<br>5. Đối chiếu dữ liệu bot B trước–sau | `KEY-A` + tham số `bot_id` của bot B ở query và body | Tham số `bot_id` bị **bỏ qua hoàn toàn** — mọi response chỉ chứa dữ liệu bot A. `do_action` không chạm được friend nào của bot B; dữ liệu bot B không đổi. Không có nhánh nào cho phép key tự khai tenant | | Lấp `Q6` · Đánh giá spec: Spec không ghi · Evidence: response JSON + đối chiếu dữ liệu bot B trước–sau |
| TC-FUNC004-01 | API | FUNC-004 | Public API — EP-09 biên dung lượng preview | Boundary | auto | Tất cả | Ảnh preview vượt 1MB một chút bị từ chối | Bot A, key 全権限, friend F; có URL HTTPS public tới ảnh JPEG dung lượng 1MB + vài KB | 1. Gọi `POST /v1/messages/push` với 1 message `type=image`, `image.url` hợp lệ ≤ 10MB, `preview_url` trỏ ảnh 1MB + vài KB<br>2. Đọc status + `error.code`<br>3. Kiểm LINE app của F | preview JPEG = 1MB + vài KB; ảnh gốc JPEG 2MB | Request bị từ chối đúng vì **preview vượt giới hạn 1MB**, thông điệp nêu đúng ngưỡng 1MB (không phải 10MB). F không nhận tin nào | | Lấp `Q7` · sửa sai ngưỡng của `NEW-437` · Đánh giá spec: Spec ghi rõ (`E644027`) · Evidence: response JSON + ảnh LINE app |
| TC-FUNC004-02 | API | FUNC-004 | Public API — EP-09 biên dung lượng preview | Boundary | auto | Tất cả | Ảnh preview 5MB (giữa 1MB và 10MB) vẫn phải bị từ chối | Như trên; có URL HTTPS public tới ảnh JPEG ~5MB | 1. Gọi `POST /v1/messages/push` với `preview_url` trỏ ảnh JPEG ~5MB, `image.url` hợp lệ<br>2. Đọc status + `error.code`<br>3. Kiểm LINE app của F | preview JPEG ~5MB; ảnh gốc JPEG 2MB | Bị từ chối vì vượt ngưỡng preview 1MB. Đây là vùng mà `NEW-437` (chỉ chặn từ 10MB) **không** bắt được — nếu request thành công thì xác nhận bug ngưỡng preview | | Lấp `Q7` · Đánh giá spec: Spec ghi rõ (`E644027`) · Evidence: response JSON |
| TC-PERM002-01 | API | PERM-002 | Middleware — cách ly route web và route API | Abnormal | manual | Tất cả | Route quản trị `/basic/*` không bị middleware API key chặn | Tài khoản admin đăng nhập web bình thường; có sẵn 1 API key của bot A | 1. Đăng nhập web, mở lần lượt `/basic/chat-v3`, `/basic/tag`, `/basic/friend`, `/basic/api-integration`<br>2. Lặp lại các bước trên nhưng gắn thêm header `X-LME-API-KEY` với key hợp lệ<br>3. Lặp lại với header `X-LME-API-KEY` chứa giá trị rác | Session web hợp lệ; header API key hợp lệ / rác | Cả 3 lần các màn `/basic/*` đều mở bình thường bằng session web. Header `X-LME-API-KEY` (hợp lệ hay rác) **không** làm màn web bị 401/403, cũng không nâng quyền cho session | | Lấp `G2` · code dùng chung — 2 middleware mới · Đánh giá spec: Spec không ghi · Evidence: ảnh các màn + response status |
| TC-PERM002-02 | API | PERM-002 | Middleware — cách ly route web và route API | Abnormal | manual | Tất cả | Endpoint `/v1/*` không đi nhờ được session đăng nhập web | Trình duyệt đang đăng nhập web với tài khoản admin của bot A (có cookie session hợp lệ) | 1. Trong chính trình duyệt đó, gọi `GET /v1/friends` **không** gắn header `X-LME-API-KEY`<br>2. Gọi `POST /v1/do_action` gắn tag cho 1 friend, cũng không gắn header key<br>3. Đọc status + body<br>4. Kiểm friend đó có bị gắn tag không | Cookie session web hợp lệ, không có header API key | Cả 2 lời gọi bị từ chối 401. Cookie session web **không** thay được API key. Không friend nào bị gắn tag | | Lấp `G2` · Đánh giá spec: Spec ghi rõ (xác thực bằng API key) · Evidence: response JSON + dữ liệu tag trước–sau |
| TC-REGSHARED001-01 | UI | REG-SHARED-001 | Chat 1:1 — hiển thị sau khi API ngoài đổi trạng thái | Normal | manual | Tất cả | Màn chat 1:1 phản ánh ngay trạng thái do API ngoài đổi | Bot A, staff đang mở màn `/basic/chat-v3`; friend F đang ở trạng thái: chưa bookmark, 対応ステータス mặc định, không ẩn, không chặn; key `KEY-EXT-01` | 1. Ghi lại dòng của F trên danh sách chat<br>2. Gọi `POST /v1/do_action` cho F với 4 thay đổi: bookmark, đổi 対応ステータス, ẩn hội thoại, đánh dấu đã xác nhận<br>3. Tải lại màn chat 1:1<br>4. Đối chiếu từng dấu hiệu trên dòng của F và vị trí của F trong danh sách<br>5. Dùng bộ lọc 対応ステータス và bộ lọc 確認状況 để kiểm số đếm | 1 request `do_action` chứa 4 action lên friend F | Dòng của F hiển thị đúng cả 4 thay đổi (icon bookmark, trạng thái xử lý mới, hội thoại vào nhóm ẩn, hết dấu chưa xác nhận). F được đẩy lên đầu danh sách theo quy tắc bookmark. Số đếm trên bộ lọc cập nhật khớp | | Lấp `G3` · regression — dẫn từ `TC-CHT-80`…`TC-CHT-87` (kho FA-001) · RULE-07 verify tầng màn hình · Đánh giá spec: Spec không ghi · Evidence: ảnh màn chat trước–sau + số đếm bộ lọc |
| TC-PERM003-01 | API | PERM-003 | Public API — EP-04 cách ly bot | Abnormal | auto | Tất cả | Danh sách friend info field của bot A không lẫn field của bot B | Bot A và bot B là 2 bot khác nhau, mỗi bot có ≥ 3 friend info field custom với tên phân biệt rõ (`FIELD-A-01`…, `FIELD-B-01`…); key `KEY-A` thuộc bot A quyền 全権限 | 1. Mở màn quản lý friend info của bot A và bot B, ghi lại đầy đủ tên + số lượng field của từng bot<br>2. Gọi `GET /v1/friend-info-fields` bằng `KEY-A`, lấy hết các trang<br>3. Đối chiếu danh sách trả về với 2 danh sách đã ghi<br>4. Gọi tiếp với `limit=500` để chắc chắn không sót trang | `KEY-A` (bot A); `page=1..n`, `limit=100` rồi `limit=500` | Danh sách trả về chứa **đúng** các field của bot A, đủ số lượng. **Không** có bất kỳ field nào của bot B (kể cả field bị ẩn hay đã ngừng dùng). `total_items` bằng đúng số field của bot A trên màn quản lý | | Lấp GAP cách ly bot EP-04 — Technical Spec §2.8 cảnh báo bảng liên kết friend info dùng ID người dùng LINE toàn hệ thống · bổ sung cho `NEW-240` (chỉ chặn nhánh ghi) · Đánh giá spec: Spec không ghi · Evidence: ảnh 2 màn quản lý field + response JSON đủ trang |
| TC-PERM003-02 | API | PERM-003 | Public API — EP-05 cách ly bot | Abnormal | auto | Tất cả | Danh sách scenario của bot A không lẫn scenario của bot B | Bot A và bot B mỗi bot có ≥ 2 scenario tên phân biệt (`SCN-A-01`…, `SCN-B-01`…), mỗi scenario có ít nhất 1 friend đang follow; key `KEY-A` thuộc bot A | 1. Mở màn quản lý scenario của bot A và bot B, ghi lại tên + `followers_count` của từng scenario<br>2. Gọi `GET /v1/scenarios` bằng `KEY-A`, lấy hết các trang<br>3. Đối chiếu tên scenario trả về với 2 danh sách<br>4. Đối chiếu `followers_count` / `completed_count` / `dropped_count` với số trên màn quản lý của bot A | `KEY-A` (bot A); `page=1..n`, `limit=100` | Chỉ trả scenario của bot A. **Không** có scenario nào của bot B. 3 con số đếm của từng scenario khớp đúng màn quản lý bot A — không bị cộng dồn friend của bot B đang follow scenario cùng tên | | Lấp GAP cách ly bot EP-05 — Technical Spec §2.8 cảnh báo bảng liên kết scenario dùng ID người dùng LINE toàn hệ thống · bổ sung cho `NEW-241` (chỉ chặn nhánh ghi) · Đánh giá spec: Spec không ghi · Evidence: ảnh 2 màn scenario + response JSON |
| TC-PERM003-03 | API | PERM-003 | Public API — EP-06 cách ly bot | Abnormal | auto | Tất cả | Danh sách template của bot A không lẫn template của bot B | Bot A và bot B mỗi bot có ≥ 3 template tên phân biệt (`TPL-A-01`…, `TPL-B-01`…), gồm cả template thường và template group; key `KEY-A` thuộc bot A | 1. Mở màn quản lý template của bot A và bot B, ghi lại tên + số lượng<br>2. Gọi `GET /v1/templates` bằng `KEY-A`, lấy hết các trang<br>3. Đối chiếu danh sách trả về<br>4. Lấy 1 `template_id` của bot B từ màn bot B, gọi `POST /v1/messages/template` bằng `KEY-A` với ID đó tới 1 friend của bot A<br>5. Kiểm hội thoại của friend bot A sau bước 4 | `KEY-A` (bot A); sau đó `template_id` thuộc bot B | Bước 3: chỉ có template của bot A, không lẫn template bot B. Bước 4: bị từ chối, **không** gửi được nội dung template của bot B cho friend bot A — hội thoại không phát sinh tin nào | | Lấp GAP cách ly bot EP-06 (0 TC trước đó) · gộp luôn nhánh ghi vì `NEW-225` đang `Chưa test` · Đánh giá spec: Spec không ghi — cần chốt 403 hay 404 · Evidence: ảnh 2 màn template + response JSON + ảnh hội thoại |
| TC-PERM003-04 | API | PERM-003 | Public API — EP-08 cách ly bot | Abnormal | auto | Tất cả | Danh sách trạng thái xử lý của bot A không lẫn trạng thái của bot B | Bot A và bot B mỗi bot có ≥ 2 trạng thái xử lý chat tên phân biệt (`ST-A-01`…, `ST-B-01`…); key `KEY-A` thuộc bot A | 1. Mở màn cài đặt trạng thái xử lý của bot A và bot B, ghi lại tên từng trạng thái<br>2. Gọi `GET /v1/compliant-statuses` bằng `KEY-A`<br>3. Đối chiếu danh sách trả về với 2 danh sách đã ghi<br>4. Lấy ID 1 trạng thái của bot B, gọi `GET /v1/conversations` của 1 friend bot A rồi kiểm `compliant_statuses` trả về | `KEY-A` (bot A) | Chỉ trả trạng thái của bot A, đúng đủ số lượng, **không** có trạng thái nào của bot B. Trường `compliant_statuses` trong response `/conversations` cũng chỉ mang trạng thái thuộc bot A | | Lấp GAP cách ly bot EP-08 · bổ sung cho `NEW-242` (chỉ chặn nhánh ghi) · Đánh giá spec: Spec không ghi · Evidence: ảnh 2 màn cài đặt + response JSON |
| TC-STATECLEAN001-04 | API | STATE-CLEAN-001 | Public API — hợp đồng hết hạn, 8 endpoint đọc | Abnormal | manual | Tất cả | Bot hết hạn hợp đồng — toàn bộ endpoint đọc bị từ chối | Bot A có API key `KEY-A` quyền 全権限 còn hạn (key **chưa** hết hạn); hợp đồng của bot A sắp hết hạn hoặc dựng được trạng thái hợp đồng đã hết hạn trên màn 「契約情報」; bot A có sẵn friend, tag, scenario, template, trạng thái xử lý, hội thoại | 1. Khi hợp đồng còn hiệu lực: gọi lần lượt đủ 8 endpoint đọc `GET /v1/friends` · `/friends/detail` · `/tags` · `/friend-info-fields` · `/scenarios` · `/templates` · `/conversations` · `/compliant-statuses` → ghi lại 8 response làm đối chứng<br>2. Đưa bot A về trạng thái hợp đồng đã hết hạn, xác nhận trên màn 「契約情報」<br>3. Gọi lại **đúng 8 endpoint** đó bằng đúng `KEY-A`<br>4. Ghi lại HTTP status + `error.code` của từng endpoint<br>5. Mở màn 「API連携」 của bot A xem trạng thái key hiển thị thế nào | `KEY-A` (key còn hạn, quyền 全権限); 8 request GET | Bước 1: cả 8 endpoint trả 200 bình thường. Bước 3: **cả 8 endpoint đều bị từ chối**, không endpoint nào lọt. Body **không chứa** dữ liệu nghiệp vụ nào (friend, tag, scenario, template, message). Mã lỗi **giống nhau ở cả 8 endpoint** và phân biệt được với `unauthorized` (key sai) — hệ thống ngoài đọc lỗi phải biết nguyên nhân là hợp đồng, không phải key hỏng. Màn 「API連携」 không hiển thị key là 有効 trong khi API thực tế đã chặn | | Lấp GAP hợp đồng hết hạn — **0/613 TC** trước đó (80 lần chữ "hợp đồng" trong bộ TC đều là "hợp đồng dữ liệu") · khác `TC-STATECLEAN001-02` (hủy hợp đồng chủ động, 2 endpoint) · màn hợp đồng = 「契約情報」 · Đánh giá spec: Spec không ghi — cần Dev chốt mã lỗi · Evidence: bảng 8 response trước–sau + ảnh màn 契約情報 + màn API連携 |
| TC-STATECLEAN001-05 | API | STATE-CLEAN-001 | Public API — hợp đồng hết hạn, 3 endpoint ghi | Abnormal | manual | Tất cả | Bot hết hạn hợp đồng — toàn bộ endpoint ghi bị từ chối và không có side effect | Như TC trên; thêm: friend F của bot A chưa gắn tag T, chưa follow scenario S, hội thoại của F ở trạng thái mặc định; có sẵn 1 `template_id` hợp lệ của bot A | 1. Ghi lại snapshot của F: danh sách tag, friend info, scenario đang follow, trạng thái xử lý, số tin trong hội thoại<br>2. Đưa bot A về trạng thái hợp đồng đã hết hạn<br>3. Gọi `POST /v1/messages/push` (1 text) tới F<br>4. Gọi `POST /v1/messages/template` (1 template) tới F<br>5. Gọi `POST /v1/do_action` cho F với 4 action: gắn tag T, cộng point, start scenario S, đổi trạng thái xử lý<br>6. Mở LINE app của F kiểm có tin nào tới không<br>7. Mở màn 友だち詳細 của F và màn chat 1:1, đối chiếu với snapshot bước 1 | `KEY-A`; 3 request POST như mô tả | Cả 3 endpoint ghi đều bị từ chối, cùng mã lỗi hợp đồng như TC-STATECLEAN001-04. **F không nhận tin nào** trên LINE app. Snapshot của F ở bước 7 **giống hệt** bước 1 — không tag mới, point không đổi, không scenario nào được start, trạng thái xử lý không đổi. `do_action` phải từ chối **trước khi chạm dữ liệu**, không được ghi một phần rồi mới báo lỗi | | Lấp GAP hợp đồng hết hạn (nhánh ghi) · RULE-08 bill tiền · Đánh giá spec: Spec không ghi · Evidence: snapshot friend trước–sau + 3 response + ảnh LINE app |
| TC-STATECLEAN001-06 | API | STATE-CLEAN-001 | Public API — biên thời điểm hết hạn hợp đồng | Boundary | manual | product | Biên thời điểm hợp đồng hết hạn — chặn đúng mốc, không sớm không muộn | Bot A trên **production**, hợp đồng có ngày hết hạn xác định; biết chính xác mốc hết hạn theo giờ Nhật; có thể gọi API liên tục quanh mốc đó | 1. Trước mốc hết hạn ~2 phút: gọi `GET /v1/friends` → ghi kết quả + thời điểm<br>2. Gọi lặp lại mỗi 30 giây, ghi lại thời điểm đầu tiên response đổi từ thành công sang bị từ chối<br>3. Sau mốc hết hạn ~5 phút: gọi lại lần nữa xác nhận vẫn bị chặn<br>4. Đối chiếu thời điểm chuyển trạng thái với mốc hết hạn ghi trên màn 「契約情報」 | `KEY-A`; chuỗi request GET quanh mốc hết hạn | Trước mốc hết hạn: vẫn gọi được bình thường — **không chặn sớm**. Sau mốc hết hạn: bị chặn, độ trễ chấp nhận được phải ≤ 1 phút (cùng chu kỳ quét 60 giây của job hết hạn key). Ghi rõ kết quả mốc **đúng giây hết hạn** để chốt biên đóng hay mở — spec chưa nói rõ ngày hết hạn có còn dùng được hết ngày hay không | | Lấp GAP hợp đồng hết hạn (biên) · RULE-08 bill tiền + job nền · liên quan `NEW-714` (biên 23:59:59 của hạn **key**, khác hạn hợp đồng) · Đánh giá spec: Spec không ghi — cần chốt biên · Evidence: bảng thời điểm + response từng lần gọi |
| TC-STATECLEAN001-07 | API | STATE-CLEAN-001 | Public API — gia hạn hợp đồng | Normal | manual | Tất cả | Gia hạn hợp đồng — API dùng lại được ngay bằng key cũ | Bot A đang ở trạng thái hợp đồng đã hết hạn và API đang bị chặn (kết quả của TC-STATECLEAN001-04); `KEY-A` vẫn còn hạn và chưa bị xoá | 1. Xác nhận `GET /v1/friends` bằng `KEY-A` đang bị từ chối<br>2. Gia hạn hợp đồng cho bot A, xác nhận trên màn 「契約情報」<br>3. Gọi lại `GET /v1/friends` bằng **đúng `KEY-A` cũ**, không phát hành key mới<br>4. Gọi thêm `POST /v1/do_action` gắn 1 tag cho friend F<br>5. Mở màn 友だち詳細 của F kiểm tag đã gắn | `KEY-A` cũ (không phát hành lại) | Sau khi gia hạn, `KEY-A` **cũ vẫn dùng được ngay**, không bắt phát hành key mới. `GET /v1/friends` trả 200 với dữ liệu đầy đủ. `do_action` gắn tag thành công và tag hiện đúng trên màn 友だち詳細. Nếu key cũ bị vô hiệu vĩnh viễn thì phải được nêu rõ trong tài liệu API cho hệ thống ngoài | | Lấp GAP hợp đồng hết hạn (phục hồi) · Đánh giá spec: Spec không ghi — cần Dev chốt key cũ sống lại hay phải phát hành lại · Evidence: response trước–sau gia hạn + ảnh màn 友だち詳細 |
| TC-MSG005-04 | API | MSG-005 | Public API — EP-10 đếm quota gói free | Normal | manual | product | Gửi 5 template trong 1 request chỉ trừ 1 tin của gói free | Bot A trên **production** dùng gói free, còn quota ≥ 10 tin trong tháng; có ≥ 5 template thường tên phân biệt; friend F là bạn của bot A, chưa chặn OA; có điện thoại thật đăng nhập LINE của F | 1. Mở màn thống kê số tin đã dùng của bot A, ghi lại con số trước khi test<br>2. Gọi `POST /v1/messages/template` với **5 template** trong 1 request tới F<br>3. Mở LINE app của F, đếm số tin thực nhận<br>4. Đợi số liệu cập nhật, mở lại màn thống kê và ghi con số sau | 1 request, `templates[]` = 5 template thường | Response 200. F nhận **đủ 5 template** đúng thứ tự khai báo. Số tin đã dùng tăng **đúng 1** — tính theo lần gửi, **không** tăng 5 theo số template | | Lấp GAP quota EP-10 — 4 TC quota hiện có (`NEW-660`…`NEW-663`) đều chỉ gắn EP-09 · quy tắc đếm do Leader chốt 2026-09-16 · RULE-08 bill tiền · Đánh giá spec: Spec không ghi — xem §6 mục 10 · Evidence: ảnh màn thống kê trước–sau + ảnh LINE app |
| TC-MSG005-05 | API | MSG-005 | Public API — EP-10 đếm quota gói free | Boundary | manual | product | Template group nở nhiều sub-template vẫn chỉ trừ 1 tin | Bot A trên production gói free, còn quota ≥ 10 tin; có 1 template `type=group` gồm **3 sub-template khác loại** (text + image + form) với thứ tự đã biết; friend F như trên | 1. Ghi lại cấu trúc + thứ tự 3 sub-template của group trên màn quản lý template<br>2. Ghi lại số tin đã dùng trước khi test<br>3. Gọi `POST /v1/messages/template` với **đúng 1 template group** đó tới F<br>4. Mở LINE app của F, đếm số tin thực nhận và đối chiếu thứ tự<br>5. Đọc lại số tin đã dùng | 1 request, `templates[]` = 1 template group (3 sub-template) | F nhận **đủ 3 sub-template** đúng thứ tự cấu hình, trong cùng một đợt. Số tin đã dùng tăng **đúng 1**, **không** tăng 3 theo số sub-template thực gửi tới LINE | | Lấp GAP quota EP-10 — nhánh riêng của EP-10, EP-09 không có · quy tắc đếm do Leader chốt 2026-09-16 · liên quan `NEW-668` (đang `Chưa test`) · RULE-08 bill tiền · Đánh giá spec: Spec không ghi — xem §6 mục 10 · Evidence: ảnh màn thống kê trước–sau + ảnh LINE app đủ 3 sub-template |
| TC-MSG005-06 | API | MSG-005 | Public API — EP-10 biên quota gói free | Boundary | manual | product | Còn đúng 1 tin trong quota vẫn gửi được 5 template | Bot A trên production gói free, đã dựng trạng thái còn **đúng 1 tin** trong quota tháng (đã dùng 999/1000); có ≥ 5 template; friend F như trên | 1. Xác nhận trên màn thống kê: còn đúng 1 tin<br>2. Gọi `POST /v1/messages/template` với **5 template** trong 1 request tới F<br>3. Đọc HTTP status + body<br>4. Mở LINE app của F đếm số tin nhận được<br>5. Đọc lại số tin đã dùng<br>6. Gọi thêm 1 request nữa với 1 template và ghi kết quả | Lần 1: 5 template · Lần 2: 1 template | Lần 1 **thành công** — không bị chặn với lý do "cần 5 slot". F nhận đủ 5 template. Quota về 0 (đã dùng 1000/1000). Lần 2 bị từ chối đúng lỗi hết quota, F **không** nhận thêm tin nào | | Lấp GAP quota EP-10 (biên) · trực tiếp từ quy tắc "1 lần gửi = 1 tin" do Leader chốt · RULE-08 bill tiền · Đánh giá spec: Spec không ghi — xem §6 mục 10 · Evidence: ảnh màn thống kê 3 mốc + ảnh LINE app + 2 response |
| TC-MSG005-07 | API | MSG-005 | Public API — EP-10 chặn khi hết quota | Abnormal | manual | product | Gói free đạt 1000 tin/tháng — gửi template bị chặn | Bot A trên production gói free, đã dùng **đúng 1000/1000** tin của tháng hiện tại; có template hợp lệ; friend F như trên | 1. Xác nhận trên màn thống kê: đã dùng 1000/1000<br>2. Ghi lại số tin trong hội thoại của F<br>3. Gọi `POST /v1/messages/template` với 1 template tới F<br>4. Đọc HTTP status + `error.code` + thông điệp<br>5. Mở LINE app của F và màn chat 1:1 kiểm tra<br>6. Đọc lại số tin đã dùng | 1 request, 1 template | Bị từ chối với lỗi nêu rõ nguyên nhân **hết quota gói cước** (không phải `invalid_request` hay `forbidden` chung chung). F **không** nhận tin. Hội thoại không phát sinh tin mới. Số tin đã dùng vẫn đúng 1000, không tăng | | Mirror `NEW-660` sang EP-10 · RULE-08 bill tiền · Đánh giá spec: Spec không ghi — cần chốt mã lỗi dùng chung với EP-09 · Evidence: response JSON + ảnh màn thống kê + ảnh chat 1:1 |
| TC-MSG005-08 | API | MSG-005 | Public API — EP-10 gói trả phí | Normal | manual | product | Gói trả phí không bị áp ngưỡng 1000 của gói free | Bot B trên production dùng **gói trả phí**, đã gửi vượt 1000 tin trong tháng hiện tại; có template hợp lệ; friend G là bạn của bot B | 1. Xác nhận trên màn thống kê của bot B: số tin đã dùng > 1000<br>2. Gọi `POST /v1/messages/template` với 1 template tới G<br>3. Đọc HTTP status + body<br>4. Mở LINE app của G kiểm tra tin đã tới chưa<br>5. Đọc lại số tin đã dùng | 1 request, 1 template | Gửi **thành công**, G nhận đúng template. Ngưỡng 1000 của gói free **không** được áp lên bot gói trả phí. Số tin đã dùng tăng đúng 1 | | Mirror `NEW-661` sang EP-10 · RULE-08 bill tiền · Đánh giá spec: Spec không ghi — xem §6 mục 10 · Evidence: ảnh màn thống kê + ảnh LINE app |
| TC-MSG005-09 | API | MSG-005 | Public API — EP-10 gửi lỗi không trừ quota | Abnormal | manual | product | Gửi template thất bại không trừ quota gói free | Bot A trên production gói free còn quota; friend F **đã chặn OA** (hoặc dựng được điều kiện chắc chắn LINE từ chối); có template hợp lệ | 1. Ghi lại số tin đã dùng và số tin trong hội thoại của F<br>2. Gọi `POST /v1/messages/template` với 1 template tới F<br>3. Đọc HTTP status + `error.code`<br>4. Kiểm hội thoại của F không có tin mới<br>5. Đọc lại số tin đã dùng | 1 request, 1 template, tới friend đã chặn OA | Response là lỗi, **không** `success=true`. Không có tin mới trong hội thoại. Số tin đã dùng **không thay đổi** — lần gửi thất bại không bị trừ quota | | Mirror `NEW-663` sang EP-10 (`NEW-663` là TC quota duy nhất đang `Đạt`, nhưng chỉ EP-09) · RULE-08 bill tiền · Đánh giá spec: Spec không ghi · Evidence: ảnh màn thống kê trước–sau + response JSON |
| TC-OUTTRUTH001-01 | API | OUT-TRUTH-001 | Public API — EP-10 gửi một phần thất bại | Abnormal | manual | product | Gửi 5 template, LINE lỗi 2 template cuối — 2 template đó được lưu lại và hiện ở màn 配信エラー | Bot A trên production, key 全権限; 5 template tên phân biệt `TPL-01`…`TPL-05`, trong đó `TPL-04` và `TPL-05` chắc chắn bị LINE từ chối (ví dụ chứa media URL hỏng); friend F là bạn của bot A; ghi lại trạng thái màn 「配信エラー」 (`/basic/error-list-v2`) trước khi test | 1. Ghi lại số dòng hiện có trên màn 「配信エラー」 tab 未対応<br>2. Gọi `POST /v1/messages/template` với 5 template `TPL-01`…`TPL-05` tới F<br>3. Đọc HTTP status + body response<br>4. Mở LINE app của F, đếm và ghi tên các template thực nhận<br>5. Mở màn 「配信エラー」 của bot A, tìm các dòng mới phát sinh<br>6. Mở modal chi tiết từng dòng lỗi mới, đối chiếu tên template và friend | 1 request, `templates[]` = `TPL-01`…`TPL-05` | F nhận **đúng 3 template đầu** (`TPL-01`…`TPL-03`). Màn 「配信エラー」 phát sinh **đúng 2 dòng mới** ứng với `TPL-04` và `TPL-05`, mỗi dòng ghi đúng tên friend F và nội dung template tương ứng — **không được im lặng bỏ qua**. Response API không báo `success=true` cho toàn bộ request | | Lấp GAP — **0/613 TC** chạm màn 「配信エラー」 · ⚠️ **mâu thuẫn expected với `NEW-104`** (EP-09, ghi "dừng ngay tại message lỗi", không gửi tiếp) → xem §6 mục 11 · FA-028 `/basic/error-list-v2` · RULE-08 bill tiền + job nền · Đánh giá spec: Spec không ghi · Evidence: ảnh LINE app + ảnh màn 配信エラー trước–sau + modal chi tiết |
| TC-OUTTRUTH001-02 | API | OUT-TRUTH-001 | Public API — EP-09 gửi một phần thất bại | Abnormal | manual | product | Gửi 4 message, LINE lỗi message thứ 3 — phần lỗi được lưu lại và hiện ở màn 配信エラー | Bot A trên production như trên; chuẩn bị 4 message: text1, text2, 1 media chắc chắn lỗi phía LINE, text4; friend F; ghi lại màn 「配信エラー」 trước khi test | 1. Ghi lại số dòng trên màn 「配信エラー」 tab 未対応<br>2. Gọi `POST /v1/messages/push` với 4 message theo thứ tự trên tới F<br>3. Đọc HTTP status + body<br>4. Mở LINE app của F, ghi lại đúng những tin đã tới<br>5. Mở màn 「配信エラー」 tìm dòng mới phát sinh<br>6. Mở modal chi tiết dòng lỗi, đối chiếu nội dung | 1 request, `messages[]` = text1, text2, media lỗi, text4 | Phần message thất bại **có bản ghi trên màn 「配信エラー」** với đúng tên friend F và nội dung tương ứng. Response **không** báo `success=true`. Ghi rõ kết quả thực tế của message thứ 4 (text4) để chốt: dừng tại message lỗi hay vẫn gửi tiếp — spec hiện chưa nói rõ | | Bổ sung tầng còn thiếu của `NEW-104` (`Chưa test`) — `NEW-104` chỉ yêu cầu "log tra được theo mã truy vết", không chạm màn 「配信エラー」 · FA-028 · RULE-08 · Đánh giá spec: Spec không ghi — xem §6 mục 11 · Evidence: ảnh LINE app + ảnh màn 配信エラー trước–sau |
| TC-MSG005-10 | API | MSG-005 | Public API — EP-10 quota khi gửi một phần thất bại | Boundary | manual | product | Gửi 5 template lỗi 2 cái cuối — quota bị trừ bao nhiêu | Bot A trên production gói free còn quota ≥ 10 tin; 5 template `TPL-01`…`TPL-05` trong đó `TPL-04`, `TPL-05` chắc chắn bị LINE từ chối; friend F | 1. Ghi lại số tin đã dùng trước khi test<br>2. Gọi `POST /v1/messages/template` với 5 template tới F<br>3. Xác nhận trên LINE app: F nhận 3 template đầu<br>4. Xác nhận trên màn 「配信エラー」: có 2 dòng lỗi mới<br>5. Đợi số liệu cập nhật, đọc lại số tin đã dùng | 1 request, 5 template, 2 cái cuối lỗi | Ghi rõ mức tăng thực tế của số tin đã dùng. Theo quy tắc "1 lần gửi = 1 tin", kỳ vọng tăng **đúng 1**. Tuyệt đối **không** tăng 5 (đếm cả phần chưa gửi được) và **không** tăng 3 (đếm theo số template thành công) nếu quy tắc request-based được giữ. Nếu kết quả khác 1 → báo Leader để chốt lại quy tắc | | Giao điểm quota × partial-fail — chưa quy tắc nào phủ · RULE-08 bill tiền · Đánh giá spec: Spec không ghi — xem §6 mục 10 · Evidence: ảnh màn thống kê trước–sau + ảnh LINE app + ảnh màn 配信エラー |
| TC-REGSHARED001-02 | UI | REG-SHARED-001 | FA-028 「配信エラー」 — nguồn phát sinh của lỗi từ public API | Normal | manual | Tất cả | Bản ghi lỗi từ public API hiển thị đúng ở bộ lọc 4 nguồn | Bot A đã có ≥ 1 bản ghi lỗi phát sinh từ `POST /v1/messages/push` hoặc `/messages/template` (kết quả của TC-OUTTRUTH001-01 / -02); màn 「配信エラー」 (`/basic/error-list-v2`) mở được | 1. Mở màn 「配信エラー」 tab 未対応<br>2. Bấm lần lượt cả 4 nguồn ở bộ lọc dọc: 「1:1チャット」 · 「メッセージ配信」 · 「ステップ配信」 · 「その他メッセージ」<br>3. Ở mỗi nguồn, tìm bản ghi lỗi sinh từ public API<br>4. Ghi lại nguồn nào chứa bản ghi đó và badge số lượng của từng nguồn<br>5. Mở modal chi tiết bản ghi, ghi lại các thông tin hiển thị | Không nhập gì — chỉ thao tác lọc | Bản ghi lỗi từ public API xuất hiện ở **đúng một** nguồn xác định và **không** biến mất khỏi cả 4 nguồn. Badge số lượng của nguồn đó tăng đúng. Modal chi tiết cho biết được tin này phát sinh từ đâu — nếu không phân biệt được với tin gửi tay thì ghi nhận **GAP thiết kế** và báo Leader để chốt có thêm nguồn riêng cho public API hay không | | Lấp GAP — FA-028 hiện chỉ có 4 nguồn (`chat11` / `send_all` / `step` / `other`), **không nguồn nào dành cho public API** · Đánh giá spec: Spec không ghi — xem §6 mục 12 · Evidence: ảnh 4 tab lọc + modal chi tiết |
| TC-REGSHARED001-03 | UI | REG-SHARED-001 | FA-028 「配信エラー」 — nguyên nhân lỗi từ public API | Normal | manual | product | Bản ghi lỗi từ public API tra được nguyên nhân, không kẹt ở 「上記以外の場合」 | Bot A trên **production** gói free đã dùng hết quota; gọi `POST /v1/messages/template` để sinh 1 bản ghi lỗi do chạm trần hạn mức; màn 「配信エラー」 mở được | 1. Gọi `POST /v1/messages/template` khi đã hết quota → sinh bản ghi lỗi<br>2. Mở màn 「配信エラー」, tìm bản ghi vừa sinh<br>3. Đọc cột nguyên nhân lỗi hiển thị trên dòng và trong modal chi tiết<br>4. Mở 「エラー原因一覧」 đối chiếu với bảng tra cứu 8 dòng<br>5. Đợi ≥ 5 phút rồi tải lại màn, xem nguyên nhân có được cập nhật lên mã cụ thể không | Không nhập gì sau bước 1 | Bản ghi hiển thị nguyên nhân **cụ thể** ứng với chạm trần hạn mức (nhóm mã `001`/`002`/`003`/`004` trong bảng tra cứu), **không** kẹt vĩnh viễn ở dòng fallback 「上記以外の場合」. Hướng dẫn khắc phục hiển thị đúng nhóm nguyên nhân | | Lấp GAP — FA-028 BR-26 cảnh báo: bản ghi thiếu mã lỗi thì job dò hạn mức **không bao giờ nhặt được** để nâng lên mã `001`/`002`/`003` · RULE-08 job nền + bill tiền · Đánh giá spec: Spec không ghi · Evidence: ảnh dòng lỗi + modal + màn エラー原因一覧, chụp 2 mốc thời gian |
| TC-REGSHARED001-05 | UI | REG-SHARED-001 | FA-028 「配信エラー」 — gửi lại tin lỗi của public API | Normal | manual | product | Gửi lại từ màn 配信エラー cho tin lỗi phát sinh từ public API | Bot A trên production có ≥ 1 bản ghi lỗi sinh từ public API và **nguyên nhân đã khắc phục** (ví dụ đã sửa media URL hoặc đã nâng gói); friend F nhận được tin; API key `KEY-A` đang ở gần ngưỡng rate limit nhóm ghi (đã dùng ~55/60 request trong phút hiện tại) | 1. Ghi lại số request còn lại của `KEY-A` trong phút hiện tại (header `X-RateLimit-Remaining`)<br>2. Mở màn 「配信エラー」, chọn bản ghi lỗi từ public API<br>3. Bấm 「すぐに再送する」<br>4. Trong vòng 10 giây, mở LINE app của F kiểm tra tin đã tới chưa; nếu chưa thì đợi thêm và ghi lại độ trễ<br>5. Kiểm bản ghi đã chuyển sang tab 再送済み履歴 chưa<br>6. Gọi ngay 1 request `GET /v1/friends` bằng `KEY-A` và đọc lại `X-RateLimit-Remaining` | Thao tác 「すぐに再送する」 trên UI, không gọi API | Tin được gửi lại thành công tới F trong vòng vài giây. Bản ghi chuyển sang tab 再送済み履歴. **Lần gửi lại này không trừ vào rate limit của `KEY-A`** — `X-RateLimit-Remaining` ở bước 6 chỉ giảm đúng 1 do request GET của chính bước đó. Ghi rõ lần gửi lại có bị trừ quota tin nhắn của gói cước hay không để chốt với Dev | | Lấp GAP — 再送 đi qua job nền, **thoát khỏi rate limit của API key**; chưa TC nào chạm · FA-028 §1.2 (Laravel chỉ ghi hàng đợi, Spring Boot gửi thật) · RULE-08 job nền + bill tiền · Đánh giá spec: Spec không ghi · Evidence: ảnh màn 配信エラー 2 tab + ảnh LINE app + 2 response header |

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | §2 Quy định chung — Rate limit | Chốt quota tính theo **bot** hay theo **`api_key_id`**. Hai cách cho kết quả test khác nhau khi 1 bot có nhiều key | Journal Redmine #134147 ("tính theo bot") vs requirement `SESSION-216-E644012` ("theo `api_key_id`"); TC `NEW-706` (pass, dùng chung bộ đếm) vs `NEW-77` (skip, theo từng khoá) | Dev + PM |
| 2 | §2 Authentication | Chốt cơ chế cuối: **HMAC 3 header** (`X-LME-Channel-Id` / `X-LME-Timestamp` / `X-LME-Signature`) hay **1 header `X-LME-API-KEY`**. Chốt xong phải archive TC của phương án bị loại trên Studio | requirement `SESSION-216-E644036` (2026-08-25, 3-header) vs ghi chú `NEW-551` ("đã bị đảo ngược ngày 04/09") | Dev + PM |
| 3 | Technical Spec §3.2 — schema | Thống nhất tên bảng/cột: `access_key_api` + `secret_key` + `role_access` + `expired_date` (description Redmine) vs `api_keys` + `key_lookup_hash` + `expires_at` + `is_deleted` (spec + toàn bộ TC) | Description Redmine #39641 vs spec đính kèm + 613 TC | Dev |
| 4 | §7.3 / Technical Spec §2.8 EP-09 — media | Ghi rõ **4 ngưỡng riêng** trong contract API để không bị hiểu nhầm là 1 ngưỡng chung: ảnh gốc 10MB · **preview 1MB** · video 200MB · audio 200MB | `NEW-437` test preview ở ngưỡng 10MB vs `NEW-436` + requirement `E644027` (preview 1MB) | Dev + Leader |
| 5 | Technical Spec §2.8 EP-11 — handling status | Chốt hành vi khi đổi 対応ステータス trên **hội thoại đang ẩn**: cho phép hay từ chối | TC `NEW-691` ghi thẳng "cần chốt allow hay reject" | Leader |
| 6 | EP-07 — `block_status` | Chốt tên 3 giá trị trạng thái chặn trước khi công bố tài liệu (requirement ghi có đề xuất đổi tên) | requirement `SESSION-216-E644033` / QA-020 | PM |
| 7 | EP-01 — phạm vi danh sách friend | Chốt friend **đã chặn OA** và **đã hủy kết bạn** có xuất hiện trong `GET /friends` không — hiện 2 nguồn mâu thuẫn, TC đang ghi nhận hành vi thực tế thay vì có expected | requirement `SESSION-216-E644034` / QA-007 | PM + Dev |
| 8 | §7.5 — `do_action` point | Bổ sung contract cho **point random** (`is_random` / `from` / `to`) hoặc xác nhận nằm ngoài Phase 1. Contract hiện chỉ có point cố định trong khi `t_actions_detail` hỗ trợ cả 2 | requirement `SESSION-216-E644038` (feedback `phapbt`) | PM + Dev |
| 9 | `spec-features/` | Tạo `spec-features/admin/public-api/feature-spec.md` + cấp mã `FA-xxx`, sau khi chốt bản spec chuẩn trong 5 file HTML đính kèm Redmine (description trỏ bản cũ nhất, bản mới nhất là attachment `30325`) | `spec-features/` không có feature này; `LME-SYSTEM-SPEC.md` không có mã màn hình tương ứng | Leader + PM |
| 10 | §2 Quy định chung — quota gói cước | Chốt **đơn vị đếm** `free_send_count`: theo **request** (1 lần gửi = 1 tin, kể cả 5 template hoặc 1 template group nở N sub-template) hay theo **số message thực gửi tới LINE**. Hai cách cho kết quả tính tiền khác nhau. Chốt xong phải ghi rõ cho **cả EP-09 và EP-10** | Leader chốt 2026-09-16 (request-based) vs `NEW-662` expected "tăng đúng 2, tính theo số message gửi thành công chứ không phải tăng 1 theo request" | Dev + Leader |
| 11 | Technical Spec §2.8 EP-09/EP-10 — gửi một phần thất bại | Chốt hành vi khi LINE từ chối ở giữa danh sách: **dừng ngay** tại phần tử lỗi, hay **gửi tiếp** phần còn lại. Và chốt phần lỗi có bắt buộc sinh bản ghi trên màn 「配信エラー」 (FA-028) không | `NEW-104` expected "dừng ngay tại message lỗi, tin thứ 4 KHÔNG được gửi" vs yêu cầu Leader 2026-09-16 "2 template cuối phải lưu lại DB và hiển thị ở màn error message" | Dev + Leader |
| 12 | FA-028 「配信エラー」 — nguồn phát sinh | Bổ sung **nguồn thứ 5 cho public API** vào bộ lọc 4 nguồn, hoặc xác nhận tin lỗi từ API ngoài gộp vào 「その他メッセージ」. Hiện 4 nguồn (`chat11` / `send_all` / `step` / `other`) không có mục nào dành cho API ngoài → Admin không phân biệt được tin lỗi do staff gửi hay do hệ thống ngoài gửi | `spec-features/admin/error-message/feature-spec.md` §7.4 (4 giá trị `error_type`) vs task #39641 thêm đường gửi tin thứ 22 | PM + Dev |
