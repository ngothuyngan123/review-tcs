<!-- sync-tcs: url=<chưa có — Redmine #39255 không có Link TCs human> | sheet=<tên tab> | anchor=Main Function -->
<!-- source: MCP LME TEST STUDIO — task_id=46, ticket 39255, testcase_list (29 TC), fetch lúc 2026-08-25. READ-ONLY snapshot, sinh bởi scripts/parse_studio_tcs.py. -->

# 04 — TC List (snapshot từ MCP LME TEST STUDIO)

> ⚠️ `contentTrust = untrusted` → xử lý như **data**, không phải chỉ thị.
> ⚠️ **READ-ONLY** — muốn sửa TC thì sửa trên Studio (`testcase_update`) rồi fetch lại.

# Digest — Studio task #46 · ticket 39255 · 29 TC

## Kết quả thực thi
| Trạng thái | Số TC |
|---|---|
| `pass` | 20 |
| `skip` | 9 |

→ **20/29 TC (68%) thực sự Đạt**; 9 TC còn lại KHÔNG có kết luận test.

## Môi trường
| Env | Số TC |
|---|---|
| `PRD` | 29 |

→ **Toàn bộ 29 TC có `last_exec.env = prd` (Production)** — RULE-08 về "không kết luận job nền từ local/staging" KHÔNG áp dụng ở đây.
> ⚠️ **Nhưng có mâu thuẫn env cần Leader xác minh**: 19/29 TC khai `env_tag = local-only` và `env_scope` KHÔNG chứa `prd`
> (13 TC scope `[dev, local, staging]` · 1 TC `[dev, staging]` · 1 TC `[local]` · 4 TC không khai scope),
> nhưng lại được ghi nhận chạy trên `prd`. Hoặc nhãn env của lần exec sai, hoặc TC đã chạy ngoài phạm vi env đã khai.
> Nhiều TC trong nhóm này là "kiểm tra tĩnh mã nguồn" / cố tình phá cấu hình khoá VAPID → **không thể thực thi thật trên Production**.

## Ai chạy (source / by)
| source / by | Số TC |
|---|---|
| `manual / hanhntb` | 29 |

## Tác giả TC
| author | Số TC |
|---|---|
| `AI` | 25 |
| `hanhntb` | 4 |

## Loại case
| case_type | Số TC |
|---|---|
| `Normal` | 14 |
| `Abnormal` | 10 |
| `Boundary` | 5 |

## Nhóm / chế độ chạy
| tc_group | Số TC |
|---|---|
| `job` | 23 |
| `data` | 4 |
| `ui` | 2 |

| exec_mode | Số TC |
|---|---|
| `auto` | 26 |
| `manual` | 3 |

## Mã quan điểm KHỚP checklist-lme (9 mã)
`CONC-001`(4) · `DATA-DB-001`(1) · `DATA-MIG-001`(1) · `ENV-003`(1) · `FUNC-001`(3) · `FUNC-004`(1) · `JOB-001`(1) · `MSG-004`(2) · `REG-SHARED-001`(2)

## ⚠️ Mã quan điểm KHÔNG có trong checklist-lme (3 mã)
| Mã Studio | Số TC |
|---|---|
| `TOOL-ERRHYG-001` | 7 |
| `(trống)` | 4 |
| `TOOL-NEGCTRL-001` | 2 |

→ `/review-tc` KHÔNG map được coverage cho các mã này.

## ⚠️ TC fail / error hoặc có ticket bug (0)
(không có)

## ⚠️ TC skip / chưa chạy (9)
| ID | exec | Tiêu đề |
|---|---|---|
| NEW-1 | `skip` | (Kiểm tra kỹ thuật) Toàn bộ push dùng chung một kết nối HTTP khởi tạo  |
| NEW-2 | `skip` | (Kiểm tra kỹ thuật) Tham số giới hạn kết nối và thời gian chờ đúng giá |
| NEW-3 | `skip` | (Kiểm tra kỹ thuật) Giữ nguyên cách mã hoá nội dung và ký khoá khi đổi |
| NEW-4 | `skip` | (Kiểm tra kỹ thuật) Phản hồi luôn được đọc hết và đóng lại ở cả nhánh  |
| NEW-6 | `skip` | (Kiểm tra kỹ thuật) Hai nơi gọi bên ngoài không phải sửa gì vì cách gọ |
| NEW-7 | `skip` | (Kiểm tra kỹ thuật) Dự án job biên dịch được với thư viện HTTP chỉ có  |
| NEW-8 | `skip` | (Kiểm tra kỹ thuật) Giới hạn kết nối của pool phải lớn hơn số luồng gử |
| NEW-11 | `skip` | Gửi dồn dập tới địa chỉ nhận FCM thật — tái kiểm chính lỗi gốc của tic |
| NEW-5 | `skip` | (Kiểm tra kỹ thuật) Không có thay đổi tệp cấu hình, bảng dữ liệu hay l |

## Màn hình (4)
| screen | Số TC |
|---|---|
| `Job gửi thông báo web push PC` | 16 |
| `Mã nguồn gửi thông báo web push PC (kiểm tra tĩnh)` | 8 |
| `Dữ liệu đăng ký nhận thông báo PC` | 3 |
| `Thông báo web push trên trình duyệt Chrome PC` | 2 |

## requirement_keys (8)
`REQ-001`(6) · `REQ-002`(5) · `REQ-003`(3) · `REQ-004`(6) · `REQ-005`(5) · `REQ-006`(3) · `REQ-007`(5) · `REQ-008`(1)

---

## Bảng TC (16 cột canonical)

| TC No. | Mã quan điểm liên kết | Loại case | Tiêu đề test case | Điều kiện tiền đề | Các bước thực hiện | Dữ liệu test/input | Kết quả mong đợi | Kết quả thực thi | Evidence thực tế | Môi trường test | Người thực hiện | Ngày thực hiện | Số ticket bug | Trạng thái đánh giá spec | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-MSG004-01 | MSG-004 | Normal | Admin nhận đúng thông báo trên Chrome PC sau khi đổi cách gửi | Job chạy bản đã sửa lỗi, đã bật chức năng gửi web push với khoá VAPID thật. Admin đã đăng nhập trang quản trị bằng Chrome trên PC, vào màn hình cài đặt thông báo 「通知設定」 và đã bật nhận thông báo trên PC, đã cấp quyền hiển thị thông báo cho trình duyệt (bản ghi đăng ký nhận thông báo đã được tạo và đang ở trạng thái đang nhận). | 1. Trên trang quản trị, tạo một sự kiện làm phát sinh thông báo PC cho bot (ví dụ có bạn bè mới đăng ký)<br>2. Giữ nguyên cửa sổ Chrome trên PC và chờ thông báo hiện lên<br>3. Đối chiếu tiêu đề và nội dung thông báo hiện trên màn hình với dữ liệu thông báo đã tạo<br>4. Rà log job cho lần gửi tương ứng | Chạy lần lượt 3 bộ nội dung để phủ luôn phần ký tự: (1) thuần chữ La-tinh; (2) tiếng Nhật 「新しい友だちが登録されました」 kèm ký tự ①②㈱; (3) nội dung có emoji 🎉 và độ dài sát mức dài nhất mà tiêu đề thông báo cho phép. | Với cả 3 bộ nội dung, Chrome trên PC đều hiển thị thông báo với đúng tiêu đề và đúng nội dung như dữ liệu đã tạo, không mất ký tự, không lỗi font, không hiện dấu hỏi hay ô vuông. Log job ghi nhận gửi thành công, không có dòng gửi thất bại và không có lỗi liên quan tới chữ ký. Đây là bằng chứng phần mã hoá nội dung và ký khoá VAPID vẫn hoạt động sau khi đổi tầng gửi. | Đạt |  | PRD | hanhntb | 2026-08-19 |  |  | Studio #7681 (NEW-24) · ui · manual · env-safe · REQ: REQ-003, REQ-001 · spec: SRC-REGRESSION-008, FA-006 job-spec muc 3.5, TICKET-39255 · Lý do manual: cần mắt người quan sát thông báo do hệ điều hành hiển thị, cần trình duyệt Chrome thật và quyền thông báo thật — không tự động hoá được ở tầng test hiện có. techNote: đây là xác nhận thực tế cho pushService.preparePost + Encoding.AESGCM; nhánh tĩnh đã có TC riêng. Bộ dữ liệu emoji/tiếng Nhật được GỘP vào đây thay vì tách TC vì cùng root cause (payload.getBytes() không chỉ định charset là code CÓ SẴN, không đổi trong diff). env_scope có prd vì theo ENV-003 hành vi với endpoint đẩy thật chỉ xác nhận trọn vẹn được trên môi trường thật; dùng tài khoản QA, không đụng dữ liệu khách. · author=AI · status=draft |
| TC-MSG004-02 | MSG-004 | Normal | Staff đang hoạt động của bot cũng nhận được thông báo trên Chrome PC | Job chạy bản đã sửa lỗi, đã bật chức năng gửi web push với khoá VAPID thật. Cùng một bot có: 1 admin và 1 staff đang hoạt động (không phải quyền admin). Cả hai đã đăng nhập trên 2 máy/2 hồ sơ Chrome khác nhau và đều đã bật nhận thông báo trên PC. | 1. Trên trang quản trị, tạo một sự kiện làm phát sinh thông báo PC cho bot<br>2. Quan sát cửa sổ Chrome của admin<br>3. Quan sát cửa sổ Chrome của staff<br>4. Rà log job cho lần gửi tương ứng | 1 thông báo PC với tiêu đề và nội dung tiếng Nhật chuẩn của hệ thống. | Cả admin và staff đều nhận được thông báo với cùng tiêu đề và nội dung, mỗi người nhận đúng 1 lần, không ai bị thiếu và không ai nhận trùng. Log job ghi nhận số đăng ký được gửi bằng đúng tổng số đăng ký đang nhận của admin và staff. Không có thông báo nào lọt sang bot hoặc tài khoản khác. | Đạt |  | PRD | hanhntb | 2026-08-19 |  |  | Studio #7682 (NEW-25) · ui · manual · env-safe · REQ: REQ-003, REQ-005 · spec: code:LineModel.webPushPc, FA-006 job-spec muc 3.5 · Lý do manual: cần 2 trình duyệt/2 tài khoản thật quan sát đồng thời thông báo hệ điều hành. techNote: phủ nhánh gom đăng ký của staff trong webPushPc (findAllByBotIdAndStatusAndIsAdmin(botId,1,0) rồi cộng dồn đăng ký của từng userInviteId) — đây là vòng lặp nhiều lần gửi liên tiếp trong CÙNG một luồng, tức là kịch bản tái sử dụng kết nối rõ nhất ở điều kiện thật. · author=AI · status=draft |
| TC-FUNC001-01 | FUNC-001 | Normal | (Kiểm tra kỹ thuật) Toàn bộ push dùng chung một kết nối HTTP khởi tạo một lần lúc job chạy | Đã lấy được mã nguồn nhánh sửa lỗi của dự án job (nhánh m_202607_webpushpc_connnection-reset_39255). | 1. Mở tệp nguồn của dịch vụ gửi thông báo web push PC trên nhánh sửa lỗi<br>2. Tìm nơi khởi tạo kết nối HTTP và đối chiếu với nơi thực hiện gửi từng thông báo<br>3. Đối chiếu tiếp với bản trên nhánh gốc release-t06-2026 để xác nhận điểm khác biệt | Không có dữ liệu nhập — đọc mã nguồn 2 nhánh để so sánh. | Kết nối HTTP dùng chung được tạo đúng một lần tại bước khởi tạo dịch vụ lúc job khởi động, và được lưu ở phạm vi dùng chung cho cả job. Trong hàm gửi từng thông báo KHÔNG còn bất kỳ lệnh tạo mới kết nối/HTTP client nào; hàm chỉ lấy lại kết nối dùng chung đã có. Bản gốc phải cho thấy đúng ngược lại (gửi qua lời gọi tự tạo client mới mỗi lần). | Chưa test |  | PRD | hanhntb | 2026-08-19 |  |  | Studio #7658 (NEW-1) · job · auto · read-only · REQ: REQ-002 · spec: TICKET-39255, commit:fc43f54c, FA-006 job-spec muc 3.5 · techNote: file src/main/java/sns/line/helper/WebPushNotificationService.java. Sau fix: static CloseableHttpClient httpClient được gán trong init() qua buildHttpClient(); sendPushNotifyPC() chỉ gọi httpClient.execute(httpPost). Trước fix: pushService.send() → web-push 5.1.1 gọi HttpAsyncClients.createSystem() tạo mới CloseableHttpAsyncClient mỗi notification (đã xác minh trong bytecode PushService.class). Kiểm tra tĩnh trên source, không cần dựng app. · author=AI · status=draft |
| TC-FUNC001-02 | FUNC-001 | Normal | (Kiểm tra kỹ thuật) Tham số giới hạn kết nối và thời gian chờ đúng giá trị đã chốt trong ticket | Đã lấy được mã nguồn nhánh sửa lỗi của dự án job. | 1. Mở tệp nguồn của dịch vụ gửi thông báo web push PC<br>2. Đọc phần khai báo hằng số cấu hình kết nối<br>3. Đối chiếu từng giá trị với mô tả cách fix trong ticket #39255 | Bộ giá trị cần đối chiếu: tổng số kết nối tối đa = 100; số kết nối tối đa mỗi route = 50; thời gian chờ mở kết nối = 5 giây; thời gian chờ lấy kết nối từ pool = 5 giây; thời gian chờ đọc dữ liệu = 15 giây; kiểm tra lại kết nối sau 5 giây không hoạt động; dọn kết nối rảnh sau 30 giây; số lần tự thử lại = 2. | Đủ 8 giá trị trên đều xuất hiện trong mã nguồn và được gắn đúng vào cấu hình kết nối dùng chung (giới hạn số kết nối gắn vào bộ quản lý pool; ba mốc thời gian chờ gắn vào cấu hình request; dọn kết nối rảnh và số lần thử lại gắn vào client). Không có giá trị nào bị bỏ sót hoặc lệch so với ticket. | Chưa test |  | PRD | hanhntb | 2026-08-19 |  |  | Studio #7659 (NEW-2) · job · auto · read-only · REQ: REQ-002 · spec: TICKET-39255, commit:fc43f54c · techNote: đối chiếu các hằng số MAX_CONNECTION_TOTAL=100, MAX_CONNECTION_PER_ROUTE=50, CONNECT_TIMEOUT=5000, CONNECTION_REQUEST_TIMEOUT=5000, SOCKET_TIMEOUT=15000, VALIDATE_AFTER_INACTIVITY=5000, EVICT_IDLE_SECOND=30, RETRY_COUNT=2 và nơi sử dụng: PoolingHttpClientConnectionManager.setMaxTotal/setDefaultMaxPerRoute/setValidateAfterInactivity, RequestConfig.custom(), HttpClients.custom().setRetryHandler(new DefaultHttpRequestRetryHandler(2,true)).evictIdleConnections(30, SECONDS). Spec không mô tả tầng transport nên oracle lấy từ ticket + diff (đã ghi ở plan.json ruleDecisions/TOOL-SPECFIRST-001). · author=AI · status=draft |
| TC-REGSHARED001-01 | REG-SHARED-001 | Normal | (Kiểm tra kỹ thuật) Giữ nguyên cách mã hoá nội dung và ký khoá khi đổi sang kết nối dùng chung | Đã lấy được mã nguồn nhánh sửa lỗi của dự án job. | 1. Mở tệp nguồn của dịch vụ gửi thông báo web push PC<br>2. Tìm đoạn dựng yêu cầu gửi thông báo và kiểu mã hoá được truyền vào<br>3. So sánh với bản trên nhánh gốc release-t06-2026 | Không có dữ liệu nhập — đọc mã nguồn 2 nhánh để so sánh. | Yêu cầu gửi vẫn do chính thư viện web push dựng nên (đi qua hàm chuẩn bị yêu cầu của thư viện), nên toàn bộ phần mã hoá nội dung và ký khoá VAPID vẫn nằm trong thư viện, không bị viết lại thủ công. Kiểu mã hoá truyền vào vẫn là AESGCM giống hệt bản gốc. Bản fix chỉ thay tầng thực thi yêu cầu, không thay bất kỳ tham số mã hoá/ký nào. | Chưa test |  | PRD | hanhntb | 2026-08-19 |  |  | Studio #7660 (NEW-3) · job · auto · read-only · REQ: REQ-003 · spec: TICKET-39255, commit:fc43f54c · techNote: pushService.preparePost(notification, Encoding.AESGCM) trả HttpPost (đã xác minh method này tồn tại và trả org/apache/http/client/methods/HttpPost trong bytecode web-push-5.1.1.jar), sau đó httpClient.execute(httpPost). Trước fix: pushService.send(notification, Encoding.AESGCM). Encoding không đổi. Đây là chứng cứ tĩnh cho regression VAPID; xác nhận thực tế nằm ở TC trên trình duyệt Chrome PC. · author=AI · status=draft |
| TC-TOOLERRHYG001-01 | TOOL-ERRHYG-001 | Normal | (Kiểm tra kỹ thuật) Phản hồi luôn được đọc hết và đóng lại ở cả nhánh thành công lẫn nhánh lỗi | Đã lấy được mã nguồn nhánh sửa lỗi của dự án job. | 1. Mở tệp nguồn của dịch vụ gửi thông báo web push PC<br>2. Đọc toàn bộ khối xử lý phản hồi sau khi gửi thông báo<br>3. Rà từng nhánh: nhận mã 201, nhận mã khác 201 có nội dung lỗi, nhận mã khác 201 nhưng không có nội dung, và nhánh phát sinh ngoại lệ | Không có dữ liệu nhập — rà 4 nhánh thoát của khối xử lý phản hồi. | Cả 4 nhánh đều đi qua bước đóng phản hồi (đặt trong khối bảo đảm luôn chạy). Nhánh thành công có bước đọc hết nội dung phản hồi trước khi kết thúc để kết nối được trả về pool. Nhánh lỗi có nội dung thì đọc hết nội dung rồi đóng. Nhánh lỗi không có nội dung không được gây lỗi phụ mà vẫn kết thúc sạch. Không có nhánh nào thoát ra mà bỏ quên việc đóng phản hồi. | Chưa test |  | PRD | hanhntb | 2026-08-19 |  |  | Studio #7661 (NEW-4) · job · auto · read-only · REQ: REQ-004 · spec: TICKET-39255, commit:fc43f54c · techNote: kiểm try { ... } finally { response.close(); }; EntityUtils.consume(entity) ở nhánh 201; nhánh statusCode!=201 kiểm entity != null trước khi IOUtils.readLines. Lưu ý thay đổi hành vi nhỏ: trước fix nếu entity == null sẽ ném NPE và hàm trả về chuỗi 'Exception'; sau fix trả về chuỗi '[]'. Cả 2 giá trị đều KHÔNG khớp 4 chuỗi lỗi mà LineModel.webPushPc dùng để tắt subscription nên không đổi dữ liệu — đã kiểm tra, chỉ ghi chú để reviewer biết. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-REGSHARED001-02 | REG-SHARED-001 | Normal | (Kiểm tra kỹ thuật) Hai nơi gọi bên ngoài không phải sửa gì vì cách gọi giữ nguyên | Đã lấy được mã nguồn nhánh sửa lỗi của dự án job. | 1. Tìm toàn bộ nơi gọi tới hàm khởi tạo dịch vụ gửi thông báo web push PC trong dự án job<br>2. Tìm toàn bộ nơi gọi tới hàm gửi một thông báo web push PC trong dự án job<br>3. Đối chiếu danh sách tham số và kiểu giá trị trả về của 2 hàm này giữa nhánh sửa lỗi và nhánh gốc | Không có dữ liệu nhập — tìm kiếm toàn bộ nơi gọi trong dự án job. | Chỉ tìm thấy đúng 2 nơi gọi: một nơi khởi tạo dịch vụ lúc job khởi động, và một nơi gửi thông báo nằm trong luồng gửi thông báo PC theo từng đăng ký. Cả 2 hàm giữ nguyên danh sách tham số và kiểu giá trị trả về so với nhánh gốc, nên 2 nơi gọi không phải sửa dòng nào. Không phát sinh nơi gọi thứ ba nào ngoài phạm vi thông báo PC. | Chưa test |  | PRD | hanhntb | 2026-08-19 |  |  | Studio #7663 (NEW-6) · job · auto · read-only · REQ: REQ-007 · spec: TICKET-39255, commit:fc43f54c, code:AppMain:201, code:LineModel:297 · techNote: AppMain.java:201 gọi WebPushNotificationService.init(); LineModel.java:297 (trong webPushPc) gọi WebPushNotificationService.sendPushNotifyPC(botId, item.getId(), endpoint, publicKey, authToken, payload). Cả 2 chữ ký không đổi trong diff. Xác nhận SRC-BUSINESS-005. · author=AI · status=draft |
| TC-TOOLERRHYG001-02 | TOOL-ERRHYG-001 | Abnormal | (Kiểm tra kỹ thuật) Dự án job biên dịch được với thư viện HTTP chỉ có gián tiếp qua phụ thuộc khác | Đã lấy được mã nguồn nhánh sửa lỗi và có sẵn kho thư viện phụ thuộc của dự án job. | 1. Mở tệp khai báo phụ thuộc của dự án job và kiểm tra thư viện HTTP mà đoạn code mới sử dụng có được khai báo trực tiếp hay không<br>2. Xác định phiên bản thư viện HTTP thực tế được lấy về khi build<br>3. Biên dịch dự án job trên nhánh sửa lỗi | Nhóm chức năng cần thư viện: bộ quản lý pool kết nối, cấu hình thời gian chờ, bộ tự thử lại, dọn kết nối rảnh, đóng phản hồi và đọc hết nội dung phản hồi. | Dự án biên dịch thành công, không có lỗi thiếu lớp/thiếu phương thức. Phiên bản thư viện HTTP lấy về phải đủ mới để hỗ trợ hai chức năng dọn kết nối rảnh và kiểm tra lại kết nối sau thời gian không hoạt động. Nếu biên dịch báo thiếu lớp hoặc thiếu phương thức thì đánh Không đạt và yêu cầu Dev khai báo thư viện HTTP một cách tường minh thay vì dựa vào phụ thuộc gián tiếp. | Chưa test |  | PRD | hanhntb | 2026-08-19 |  |  | Studio #7664 (NEW-7) · job · auto · read-only · REQ: REQ-007, REQ-002 · spec: TICKET-39255, commit:fc43f54c, build.gradle · techNote: build.gradle KHÔNG khai báo trực tiếp org.apache.httpcomponents:httpclient; nó vào compileClasspath gián tiếp qua web-push 5.1.1 và elasticsearch-rest-client. Phiên bản đã resolve trong gradle cache là httpclient-4.5.10 — thoả yêu cầu >= 4.4 của evictIdleConnections(long,TimeUnit) và setValidateAfterInactivity(int). Rủi ro này KHÔNG được ticket nhắc tới, QA tự phát hiện từ diff nên phải giữ TC. Đây là kiểm tra tĩnh + biên dịch, không chạy app. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-FUNC004-01 | FUNC-004 | Boundary | (Kiểm tra kỹ thuật) Giới hạn kết nối của pool phải lớn hơn số luồng gửi thông báo | Đã lấy được mã nguồn nhánh sửa lỗi của dự án job. | 1. Đọc số luồng xử lý thông báo web push PC được cấu hình trong bộ điều phối job<br>2. Đọc giới hạn số kết nối mỗi route và tổng số kết nối trong cấu hình pool<br>3. So sánh 3 con số theo thứ tự: số luồng ≤ giới hạn mỗi route ≤ tổng giới hạn | Số luồng gửi = 10; giới hạn mỗi route = 50; tổng giới hạn = 100. | Quan hệ 10 ≤ 50 ≤ 100 đúng, nghĩa là trong điều kiện bình thường 10 luồng cùng bắn về cùng một endpoint vẫn không bao giờ chạm trần kết nối mỗi route, không phải xếp hàng chờ lấy kết nối. Nếu số luồng bị nâng vượt quá giới hạn mỗi route thì đánh Không đạt và cảnh báo Dev vì sẽ phát sinh chờ pool và nguy cơ hết thời gian chờ lấy kết nối. | Chưa test |  | PRD | hanhntb | 2026-08-19 |  |  | Studio #7665 (NEW-8) · job · auto · read-only · REQ: REQ-002, REQ-004 · spec: TICKET-39255, commit:fc43f54c, code:HandleWebpushManager · techNote: HandleWebpushManager.numberThread = 10; MAX_CONNECTION_PER_ROUTE = 50; MAX_CONNECTION_TOTAL = 100. Đây là Boundary theo trục giới hạn dùng chung (CONC-001 adapt), không phải biên ký tự — diff không có field nhập liệu nào. · author=AI · status=draft |
| TC-FUNC001-03 | FUNC-001 | Normal | Gửi một thông báo PC đơn lẻ tới đăng ký hợp lệ | Job đã bật chức năng gửi web push và đã nạp khoá VAPID hợp lệ. Có 1 bot còn hạn, 1 admin của bot, và 1 bản ghi đăng ký nhận thông báo PC hợp lệ (có đủ khoá công khai + khoá xác thực, đang ở trạng thái đang nhận). Địa chỉ nhận của đăng ký này trỏ tới máy chủ giả lập trả mã 201. | 1. Tạo 1 bản ghi thông báo PC mới ở trạng thái chờ xử lý cho bot và admin nói trên<br>2. Chạy job xử lý thông báo web push PC<br>3. Xem log của job và xem yêu cầu mà máy chủ giả lập nhận được<br>4. Kiểm tra lại bản ghi đăng ký nhận thông báo trong dữ liệu | 1 bản ghi thông báo PC: tiêu đề 「新しい通知があります。」, nội dung 「アプリからご確認ください。」. | Máy chủ giả lập nhận đúng 1 yêu cầu gửi thông báo. Job ghi log kết thúc thành công cho lần gửi này và KHÔNG ghi bất kỳ dòng lỗi gửi thất bại nào, đặc biệt không có lỗi 「Connection reset by peer」. Bản ghi thông báo PC chuyển sang trạng thái đã xử lý. Bản ghi đăng ký nhận thông báo giữ nguyên trạng thái đang nhận và không có nội dung lỗi nào được ghi vào. | Đạt |  | PRD | hanhntb | 2026-08-19 |  |  | Studio #7666 (NEW-9) · job · auto · local-only · REQ: REQ-001 · spec: TICKET-39255, FA-006 job-spec muc 3.5 · techNote: seed notification_pc(status=0) + push_subscriptions(endpoint trỏ stub, status != FALSE, auth_token != null); chạy HandleWebpushManager/HandleWebpushTask; assert log #sendPushNotifyPC không có dòng FALSE và log #pushNotifyWeb onSuccess. Lưu ý oracle: notification_pc.status LUÔN được set DONE(2) kể cả khi push lỗi (HandleWebpushTask:58-60) nên KHÔNG dùng cột này làm bằng chứng gửi thành công — bằng chứng là request nhận tại stub + log. env_scope loại prd vì TC cần ghi dữ liệu seed vào DB và khởi động lại job. · author=AI · status=draft |
| TC-CONC001-01 | CONC-001 | Normal | Gửi dồn dập 200 thông báo qua 10 luồng tới địa chỉ nhận giả lập | Job đã bật chức năng gửi web push, khoá VAPID hợp lệ. Có 1 bot còn hạn, 1 admin, 1 đăng ký nhận thông báo PC hợp lệ trỏ tới máy chủ giả lập trả mã 201 và có ghi lại số kết nối TCP thực sự được mở. | 1. Tạo 200 bản ghi thông báo PC ở trạng thái chờ xử lý cho cùng bot và admin<br>2. Chạy job xử lý thông báo web push PC<br>3. Chờ tới khi toàn bộ 200 bản ghi được xử lý xong<br>4. Đếm số yêu cầu và số kết nối mà máy chủ giả lập đã nhận, đồng thời rà toàn bộ log job | 200 bản ghi thông báo PC giống nhau về nội dung, chỉ khác số thứ tự trong tiêu đề để dễ đối chiếu. | Máy chủ giả lập nhận đủ 200 yêu cầu, tất cả đều trả mã 201. Số kết nối TCP thực sự được mở phải NHỎ HƠN NHIỀU so với 200 (kỳ vọng xấp xỉ bằng số luồng đang chạy, tối đa vài chục) — đây là bằng chứng kết nối được tái sử dụng qua pool chứ không tạo mới mỗi lần. Log job không có dòng nào chứa 「Connection reset by peer」, không có dòng gửi thất bại, không có luồng nào rơi vào trạng thái ngủ 60 giây do ngoại lệ. Toàn bộ 200 bản ghi chuyển sang trạng thái đã xử lý. | Đạt |  | PRD | hanhntb | 2026-08-21 |  |  | Studio #7667 (NEW-10) · job · auto · local-only · REQ: REQ-001, REQ-004 · spec: TICKET-39255, commit:fc43f54c, FA-006 job-spec muc 3.5 · techNote: đây là TC lõi cho REQ-001 nhưng chạy với stub nên KHÔNG tái hiện được reset thật của FCM — phần đó do TC 'Gửi dồn dập tới địa chỉ nhận FCM thật' đảm nhiệm (trục môi trường endpoint, ENV-003 adapt). Điểm mấu chốt kiểm ở đây là tỉ lệ kết nối/số request, chứng minh EntityUtils.consume + response.close() trả kết nối về pool. Nếu số kết nối ≈ số request thì pool không được tái sử dụng ⇒ Không đạt dù mọi request đều 201. · author=AI · status=draft |
| TC-ENV003-01 | ENV-003 | Normal | Gửi dồn dập tới địa chỉ nhận FCM thật — tái kiểm chính lỗi gốc của ticket | Job đã bật chức năng gửi web push với khoá VAPID thật. Có ít nhất 1 đăng ký nhận thông báo PC THẬT được tạo từ trình duyệt Chrome trên PC (địa chỉ nhận thuộc dịch vụ FCM của Google), đang ở trạng thái đang nhận. Đã chuẩn bị sẵn bản ghi log của lần chạy trên bản chưa sửa để đối chiếu. | 1. Tạo 200 bản ghi thông báo PC ở trạng thái chờ xử lý cho bot của đăng ký thật<br>2. Chạy job xử lý thông báo web push PC trên bản đã sửa lỗi<br>3. Chờ xử lý xong toàn bộ và rà log job<br>4. Đối chiếu với log của cùng kịch bản chạy trên bản chưa sửa | 200 bản ghi thông báo PC, tiêu đề 「新しい通知があります。」 kèm số thứ tự, nội dung 「アプリからご確認ください。」. | Trên bản đã sửa: log không xuất hiện lỗi 「java.io.IOException: Connection reset by peer」, mọi lần gửi đều trả mã 201, không có luồng nào bị ngoại lệ. Trên bản chưa sửa (đối chiếu): phải tái hiện được lỗi Connection reset ít nhất một lần — nếu bản chưa sửa cũng không tái hiện được thì phải ghi rõ vào kết quả là chưa tái hiện được lỗi gốc, và không được kết luận bản fix đã hết lỗi chỉ vì lần chạy này sạch. | Chưa test |  | PRD | hanhntb | 2026-08-19 |  |  | Studio #7668 (NEW-11) · job · manual · local-only · REQ: REQ-001 · spec: TICKET-39255, SRC-REGRESSION-006 · Lý do manual: cần đăng ký web push THẬT tạo từ Chrome trên PC và cần bắn thật vào endpoint FCM của Google — không tự động hoá được bằng stub, và cần người quan sát/so sánh 2 lần chạy. techNote: lỗi gốc chỉ bộc lộ khi TLS handshake liên tục với FCM (web-push 5.1.1 gọi HttpAsyncClients.createSystem() mỗi notification). env_scope loại local (thường không có khoá VAPID thật) và loại prd (không được seed dữ liệu + khởi động lại job production). · author=AI · status=draft |
| TC-CONC001-02 | CONC-001 | Normal | Gửi lại sau khoảng nghỉ dài hơn thời gian dọn kết nối rảnh | Job đã bật chức năng gửi web push. Có 1 đăng ký nhận thông báo PC hợp lệ trỏ tới máy chủ giả lập. Máy chủ giả lập được cấu hình sẽ tự đóng lặng lẽ các kết nối rảnh sau khoảng 20 giây, giống cách dịch vụ đẩy thật thường làm. | 1. Tạo và cho job xử lý 5 thông báo PC đợt một<br>2. Để job nghỉ không gửi gì trong 60 giây<br>3. Tạo tiếp 5 thông báo PC đợt hai và cho job xử lý<br>4. Rà log job và các yêu cầu máy chủ giả lập nhận được ở cả hai đợt | Đợt một 5 thông báo, nghỉ 60 giây, đợt hai 5 thông báo. Máy chủ giả lập đóng kết nối rảnh sau 20 giây. | Cả 10 yêu cầu đều tới nơi và trả mã 201. Riêng đợt hai không được xuất hiện lỗi 「Connection reset by peer」 hay lỗi kết nối đã đóng — nghĩa là các kết nối chết trong lúc nghỉ đã bị dọn hoặc bị kiểm tra lại trước khi dùng, hệ thống tự mở kết nối mới. Không có thông báo nào bị mất giữa hai đợt. | Đạt |  | PRD | hanhntb | 2026-08-19 |  |  | Studio #7669 (NEW-12) · job · auto · local-only · REQ: REQ-004 · spec: TICKET-39255, commit:fc43f54c · techNote: TC này kiểm chứng riêng 2 tham số evictIdleConnections(30s) và setValidateAfterInactivity(5000ms) — đây chính là lớp bảo vệ chống 'kết nối chết nằm trong pool' mà bản gốc không có. Nghỉ 60s > 30s để chắc chắn chạm ngưỡng dọn kết nối rảnh. · author=AI · status=draft |
| TC-TOOLNEGCTRL001-01 | TOOL-NEGCTRL-001 | Normal | Đối chứng âm — thông báo của bot đã hết hạn quá 7 ngày vẫn không được gửi đi | Job đã bật chức năng gửi web push. Có 1 bot đã hết hạn gói quá 7 ngày, admin của bot này có 1 đăng ký nhận thông báo PC hợp lệ trỏ tới máy chủ giả lập. | 1. Tạo 1 bản ghi thông báo PC ở trạng thái chờ xử lý cho bot đã hết hạn<br>2. Chạy job xử lý thông báo web push PC<br>3. Kiểm tra máy chủ giả lập có nhận yêu cầu nào không<br>4. Kiểm tra trạng thái bản ghi thông báo PC và log job | 1 thông báo PC thuộc bot đã hết hạn gói quá 7 ngày. | Máy chủ giả lập KHÔNG nhận bất kỳ yêu cầu nào. Bản ghi thông báo PC được chuyển sang trạng thái bỏ qua do bot hết hạn, kèm log ghi rõ lý do bỏ qua. Hành vi này giống hệt bản trước khi sửa lỗi — bản fix không được làm thay đổi nhánh này. | Đạt |  | PRD | hanhntb | 2026-08-21 |  |  | Studio #7676 (NEW-19) · job · auto · local-only · REQ: REQ-007 · spec: FA-006 job-spec muc 3.5, code:HandleWebpushTask · techNote: HandleWebpushTask.actionPushPc() — nhánh bot == null \|\| bot.isExpiredOver7Day() → set STATUS_EXPIRED_BOT, không gọi LineModel.webPushPc(). Đối chứng âm theo TOOL-NEGCTRL-001: mutate thứ KHÔNG liên quan (tầng transport) thì nhánh này phải CÒN NGUYÊN. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-JOB001-01 | JOB-001 | Abnormal | Địa chỉ nhận đột ngột đóng kết nối — hệ thống tự thử lại và gửi được | Job đã bật chức năng gửi web push. Có 1 đăng ký nhận thông báo PC hợp lệ trỏ tới máy chủ giả lập. Máy chủ giả lập được cấu hình: yêu cầu đầu tiên bị ngắt kết nối đột ngột ngay khi vừa nhận, các yêu cầu sau trả mã 201 bình thường. | 1. Tạo 1 bản ghi thông báo PC ở trạng thái chờ xử lý<br>2. Chạy job xử lý thông báo web push PC<br>3. Đếm số yêu cầu máy chủ giả lập nhận được cho cùng một thông báo<br>4. Rà log job và kiểm tra bản ghi đăng ký nhận thông báo | 1 thông báo PC. Kịch bản máy chủ giả lập: lần 1 ngắt kết nối đột ngột, lần 2 trả mã 201. | Máy chủ giả lập nhận 2 yêu cầu cho cùng 1 thông báo (lần đầu bị ngắt, lần thử lại thành công), và tối đa không quá 3 yêu cầu vì chỉ được thử lại 2 lần. Kết quả cuối là gửi thành công: job không ghi dòng gửi thất bại, không ném ngoại lệ ra ngoài, luồng xử lý không bị ngủ 60 giây. Bản ghi đăng ký nhận thông báo giữ nguyên trạng thái đang nhận, không bị ghi nội dung lỗi. | Đạt |  | PRD | hanhntb | 2026-08-21 |  |  | Studio #7670 (NEW-13) · job · auto · local-only · REQ: REQ-006, REQ-001 · spec: TICKET-39255, SRC-REGRESSION-007, commit:fc43f54c · techNote: đây là nhánh DUY NHẤT mà DefaultHttpRequestRetryHandler(2, true) thật sự kích hoạt — lỗi kiểu SocketException/Connection reset ở tầng IO. Xem thêm 2 TC kế tiếp cho các nhánh KHÔNG được retry (5xx và quá thời gian đọc). · author=AI · status=draft |
| TC-TOOLERRHYG001-03 | TOOL-ERRHYG-001 | Abnormal | Địa chỉ nhận trả mã lỗi máy chủ 503 — xác nhận có thử lại hay không | Job đã bật chức năng gửi web push. Có 1 đăng ký nhận thông báo PC hợp lệ trỏ tới máy chủ giả lập. Máy chủ giả lập luôn trả mã 503 kèm nội dung lỗi ngắn. | 1. Tạo 1 bản ghi thông báo PC ở trạng thái chờ xử lý<br>2. Chạy job xử lý thông báo web push PC<br>3. Đếm số yêu cầu máy chủ giả lập nhận được<br>4. Rà log job và kiểm tra bản ghi đăng ký nhận thông báo | 1 thông báo PC. Máy chủ giả lập trả mã 503 cho mọi yêu cầu. | Theo cài đặt hiện tại của mã nguồn: máy chủ giả lập chỉ nhận ĐÚNG 1 yêu cầu, KHÔNG có lần thử lại nào, vì cơ chế thử lại chỉ áp dụng cho lỗi đứt kết nối chứ không áp dụng cho phản hồi có mã lỗi. Job ghi log gửi thất bại kèm mã 503 và nội dung phản hồi, không ném ngoại lệ, luồng tiếp tục xử lý bản ghi kế tiếp. Bản ghi đăng ký nhận thông báo giữ nguyên trạng thái đang nhận (nội dung lỗi 503 không khớp nhóm lỗi khiến ngừng nhận). ⚠ CẦN DEV XÁC NHẬN: ticket ghi 「retry 2 lần」 nên nếu kỳ vọng đúng là phải thử lại cả khi gặp 503 thì TC này chuyển thành Không đạt và cần sửa cách cài đặt. | Đạt |  | PRD | hanhntb | 2026-08-21 |  |  | Studio #7671 (NEW-14) · job · auto · local-only · REQ: REQ-006 · spec: TICKET-39255, commit:fc43f54c, CONFLICT-01 · CONFLICT-01 — chưa chốt expected. techNote: DefaultHttpRequestRetryHandler chỉ xử lý IOException, không nhìn status code; muốn retry theo mã HTTP phải thêm ServiceUnavailableRetryStrategy. Hệ quả nghiệp vụ: FCM lỗi tạm thời dạng 5xx sẽ mất thông báo ngay lần đầu mà notification_pc vẫn bị đánh đã xử lý ⇒ mất im lặng. Đề nghị leader hỏi Dev trước khi chạy. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-TOOLERRHYG001-04 | TOOL-ERRHYG-001 | Abnormal | Địa chỉ nhận không phân giải được tên miền — báo lỗi sạch và xử lý tiếp bản ghi sau | Job đã bật chức năng gửi web push. Cùng 1 admin có 2 đăng ký nhận thông báo PC: đăng ký A trỏ tới tên miền không tồn tại, đăng ký B trỏ tới máy chủ giả lập trả mã 201. | 1. Tạo 1 bản ghi thông báo PC ở trạng thái chờ xử lý cho admin nói trên<br>2. Chạy job xử lý thông báo web push PC<br>3. Rà log job<br>4. Kiểm tra trạng thái của cả hai bản ghi đăng ký nhận thông báo | Đăng ký A: địa chỉ nhận thuộc tên miền không tồn tại. Đăng ký B: địa chỉ nhận hợp lệ trỏ máy chủ giả lập. | Job ghi log có ngoại lệ cho đăng ký A nhưng KHÔNG dừng lại: vẫn gửi tiếp và thành công cho đăng ký B (máy chủ giả lập nhận đúng 1 yêu cầu). Luồng xử lý không chết, không rơi vào trạng thái ngủ 60 giây. Cả hai bản ghi đăng ký đều giữ nguyên trạng thái đang nhận vì lỗi tên miền không thuộc nhóm lỗi khiến ngừng nhận. | Đạt |  | PRD | hanhntb | 2026-08-21 |  |  | Studio #7673 (NEW-16) · job · auto · local-only · REQ: REQ-001, REQ-005 · spec: TICKET-39255, FA-006 job-spec muc 7 · techNote: UnknownHostException nằm trong nonRetriableClasses nên không retry; exception được bắt trong sendPushNotifyPC → trả chuỗi 'Exception' → LineModel.webPushPc không khớp 4 chuỗi lỗi nên không UPDATE push_subscriptions. TC này đồng thời là đối chứng cho vòng lặp theo từng đăng ký trong webPushPc (1 đăng ký lỗi không chặn đăng ký còn lại). · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-TOOLERRHYG001-05 | TOOL-ERRHYG-001 | Abnormal | Khởi động job khi thiếu khoá VAPID — dừng rõ ràng, không chạy với kết nối chưa sẵn sàng | Môi trường job cục bộ, đã bật chức năng gửi web push nhưng khoá VAPID trong cấu hình để trống hoặc sai định dạng. | 1. Khởi động job<br>2. Quan sát log khởi động<br>3. Kiểm tra job có tiếp tục nhận và xử lý thông báo PC hay không | Khoá công khai và khoá bí mật VAPID để trống. | Job báo lỗi ngay ở bước khởi tạo dịch vụ gửi thông báo web push, với thông báo lỗi đủ để biết nguyên nhân nằm ở khâu khởi tạo. Job KHÔNG được chạy tiếp ở trạng thái nửa vời rồi mới ném lỗi khi có thông báo đầu tiên. Log lỗi không được in ra khoá bí mật VAPID. | Đạt |  | PRD | hanhntb | 2026-08-21 |  |  | Studio #7674 (NEW-17) · job · auto · local-only · REQ: REQ-007 · spec: TICKET-39255, commit:fc43f54c, code:AppMain:201 · techNote: init() bọc trong try/catch và ném RuntimeException; sau fix httpClient cũng được gán trong cùng khối này nên nếu init() lỗi thì httpClient vẫn null. TC bảo đảm không rơi vào trạng thái pushService/httpClient null mà job vẫn chạy. env_scope chỉ local vì phải cố tình phá cấu hình khoá — không làm việc này trên môi trường dùng chung. Phần không in khoá bí mật lấy theo SEC-002 (adapt). · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-CONC001-03 | CONC-001 | Abnormal | Kiểm tra thông báo trùng khi hệ thống phải thử lại sau khi yêu cầu đã gửi đi | Job đã bật chức năng gửi web push. Có 1 đăng ký nhận thông báo PC hợp lệ trỏ tới máy chủ giả lập. Máy chủ giả lập được cấu hình: NHẬN TRỌN VẸN yêu cầu đầu tiên rồi mới ngắt kết nối trước khi kịp trả phản hồi; yêu cầu tiếp theo trả mã 201. | 1. Tạo 1 bản ghi thông báo PC ở trạng thái chờ xử lý<br>2. Chạy job xử lý thông báo web push PC<br>3. Đếm số yêu cầu mà máy chủ giả lập đã nhận TRỌN VẸN cho cùng một thông báo<br>4. Ghi lại kết quả để đối chiếu với xác nhận của Dev | 1 thông báo PC. Máy chủ giả lập: lần 1 nhận đủ nội dung rồi ngắt trước khi trả phản hồi, lần 2 trả mã 201. | Ghi nhận thực tế: máy chủ giả lập nhận TRỌN VẸN 2 yêu cầu cho cùng 1 thông báo, tức là nội dung thông báo đã được đẩy đi 2 lần. Về phía người dùng cuối, điều này có thể làm trình duyệt hiển thị 2 thông báo giống hệt nhau. ⚠ ĐÂY LÀ ĐIỂM CẦN DEV/PO QUYẾT ĐỊNH, không tự đánh Đạt/Không đạt: nếu chấp nhận đánh đổi (thà trùng còn hơn mất) thì ghi Đạt kèm ghi chú; nếu không chấp nhận thì cần chỉnh lại chế độ thử lại để không gửi lại yêu cầu đã đi. | Đạt |  | PRD | hanhntb | 2026-08-21 |  |  | Studio #7675 (NEW-18) · job · auto · local-only · REQ: REQ-008 · spec: TICKET-39255, commit:fc43f54c, CONFLICT-02 · CONFLICT-02 — rủi ro MỚI do bản fix sinh ra, ticket không nhắc. techNote: new DefaultHttpRequestRetryHandler(RETRY_COUNT, true) — tham số thứ hai requestSentRetryEnabled=true cho phép retry cả POST đã gửi. Web push không có khoá chống trùng ở tầng ứng dụng nên endpoint không tự khử trùng. Nếu Dev xác nhận cần chặn thì đổi thành false và ghi lại rủi ro mất thông báo tương ứng. · author=AI · status=draft |
| TC-TOOLERRHYG001-06 | TOOL-ERRHYG-001 | Boundary | Địa chỉ nhận không phản hồi quá 15 giây — thoát đúng hạn, không treo luồng | Job đã bật chức năng gửi web push. Có 1 đăng ký nhận thông báo PC hợp lệ trỏ tới máy chủ giả lập. Máy chủ giả lập nhận yêu cầu rồi giữ im không trả phản hồi trong 60 giây. | 1. Tạo 2 bản ghi thông báo PC ở trạng thái chờ xử lý, ghi lại thời điểm bắt đầu<br>2. Chạy job xử lý thông báo web push PC<br>3. Đo khoảng thời gian từ lúc gửi yêu cầu tới lúc job ghi log kết thúc lần gửi đó<br>4. Kiểm tra job có tiếp tục xử lý bản ghi thứ hai không | 2 thông báo PC. Máy chủ giả lập giữ kết nối im 60 giây. Mốc thời gian cần đối chiếu: 15 giây. | Lần gửi kết thúc sau khoảng 15 giây (chấp nhận sai số vài giây), KHÔNG chờ tới 60 giây và KHÔNG treo vô hạn. Job ghi log có ngoại lệ cho lần gửi này. Theo cài đặt hiện tại thì máy chủ giả lập chỉ nhận 1 yêu cầu, không có lần thử lại nào cho tình huống hết thời gian chờ đọc. Sau đó job vẫn xử lý tiếp bản ghi thứ hai, không có luồng nào chết. Kết nối bị treo phải được giải phóng, không nằm lại chiếm chỗ trong pool. | Đạt |  | PRD | hanhntb | 2026-08-21 |  |  | Studio #7672 (NEW-15) · job · auto · local-only · REQ: REQ-006, REQ-004 · spec: TICKET-39255, commit:fc43f54c, CONFLICT-03 · CONFLICT-03 — mốc 15 giây là biên thật của bản fix (SOCKET_TIMEOUT), trước fix không có giới hạn tương đương nên luồng có thể treo rất lâu. techNote: SocketTimeoutException kế thừa InterruptedIOException nằm trong nonRetriableClasses của DefaultHttpRequestRetryHandler ⇒ không retry. Cần Dev xác nhận cùng CONFLICT-01. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-CONC001-04 | CONC-001 | Boundary | Gửi đồng thời 100 thông báo về cùng một địa chỉ nhận, chạm ngưỡng kết nối mỗi route | Job đã bật chức năng gửi web push. Có nhiều đăng ký nhận thông báo PC cùng trỏ về MỘT máy chủ giả lập (cùng một route). Máy chủ giả lập trả mã 201 nhưng cố tình chậm khoảng 1 giây mỗi yêu cầu để nhiều kết nối cùng bị giữ. | 1. Tạo lượng thông báo PC đủ để có khoảng 100 lần gửi cùng lúc về một địa chỉ nhận<br>2. Chạy job xử lý thông báo web push PC<br>3. Theo dõi số kết nối đồng thời mà máy chủ giả lập ghi nhận<br>4. Chờ xử lý xong và rà log job | Khoảng 100 lần gửi đồng thời về cùng một route. Ngưỡng cần đối chiếu: 50 kết nối mỗi route, 100 kết nối tổng. | Số kết nối đồng thời tới máy chủ giả lập không vượt quá 50. Phần vượt ngưỡng xếp hàng chờ lấy kết nối chứ không bị lỗi ngay. Toàn bộ thông báo cuối cùng đều được gửi và nhận mã 201, không có thông báo nào bị bỏ sót. Không có luồng nào bị treo vĩnh viễn: nếu có lần gửi nào phải chờ lấy kết nối quá 5 giây thì phải kết thúc bằng lỗi có ghi log rõ ràng chứ không đứng im. | Đạt |  | PRD | hanhntb | 2026-08-21 |  |  | Studio #7677 (NEW-20) · job · auto · local-only · REQ: REQ-004, REQ-002 · spec: TICKET-39255, commit:fc43f54c · techNote: Boundary của CONC-001 tình huống 4 (batch đa luồng chạm giới hạn dùng chung). Thực tế 10 thread < 50 perRoute nên phải ép số lần gửi đồng thời lên bằng cách cho mỗi thông báo có nhiều đăng ký nhận (webPushPc lặp tuần tự trong 1 thread) hoặc tạm nâng số luồng ở môi trường test. Nếu không dựng được điều kiện chạm ngưỡng thì ghi skipReason cho đúng TC này, không gộp lý do với TC khác. · author=AI · status=draft |
| TC-DATAMIG001-01 | DATA-MIG-001 | Normal | (Kiểm tra kỹ thuật) Không có thay đổi tệp cấu hình, bảng dữ liệu hay lược đồ trong phạm vi sửa lỗi | Đã lấy được nhánh sửa lỗi và nhánh gốc release-t06-2026 của dự án job. | 1. Liệt kê toàn bộ tệp bị thay đổi giữa nhánh sửa lỗi và nhánh gốc<br>2. Kiểm tra trong danh sách có tệp cấu hình, tệp migration, tệp mô tả bảng dữ liệu nào không<br>3. Kiểm tra trong nội dung thay đổi có khoá cấu hình mới nào được đọc từ tệp cấu hình không | Không có dữ liệu nhập — so sánh danh sách tệp thay đổi giữa 2 nhánh. | Chỉ đúng một tệp mã nguồn của dịch vụ gửi thông báo web push PC bị thay đổi. Không có tệp cấu hình, không có migration, không có thay đổi bảng/lược đồ. Mọi giá trị tinh chỉnh đều là hằng số viết thẳng trong mã nguồn, không đọc thêm khoá cấu hình mới nào. Kết luận: triển khai bản fix này không cần cập nhật cấu hình hay chạy migration ở bất kỳ môi trường nào. | Chưa test |  | PRD | hanhntb | 2026-08-19 |  |  | Studio #7662 (NEW-5) · data · auto · read-only · REQ: REQ-007 · spec: TICKET-39255, commit:fc43f54c · techNote: đối chiếu commit fc43f54c — 1 file changed, 73 insertions, 14 deletions, chỉ src/main/java/sns/line/helper/WebPushNotificationService.java. Xác nhận SRC-BUSINESS-004. TC này là căn cứ để bỏ toàn bộ nhóm quan điểm migration/backup/asset (xem knowledgeAudit). · author=AI · status=draft |
| TC-TOOLNEGCTRL001-02 | TOOL-NEGCTRL-001 | Normal | Đối chứng âm — đăng ký lành của cùng admin không bị đụng khi một đăng ký khác lỗi | Job đã bật chức năng gửi web push. Cùng 1 admin có 3 đăng ký nhận thông báo PC đang ở trạng thái đang nhận: A trỏ máy chủ giả lập trả 410 kèm nội dung đã huỷ đăng ký, B và C trỏ máy chủ giả lập trả mã 201. Ngoài ra có 1 admin khác của bot khác cũng có đăng ký đang nhận. | 1. Ghi lại trạng thái của cả 4 bản ghi đăng ký trước khi chạy<br>2. Tạo 1 bản ghi thông báo PC ở trạng thái chờ xử lý cho admin có 3 đăng ký<br>3. Chạy job xử lý thông báo web push PC<br>4. Kiểm tra lại trạng thái và nội dung lỗi của cả 4 bản ghi đăng ký | Đăng ký A lỗi đã huỷ đăng ký; đăng ký B và C bình thường; đăng ký D thuộc admin và bot khác. | Chỉ duy nhất đăng ký A bị chuyển sang ngừng nhận và ghi nội dung lỗi. Đăng ký B và C vẫn ở trạng thái đang nhận, nội dung lỗi vẫn trống, và cả hai vẫn nhận được thông báo (máy chủ giả lập ghi nhận đủ 2 lần gửi thành công). Đăng ký D của admin/bot khác không bị thay đổi bất kỳ trường nào. | Đạt |  | PRD | hanhntb | 2026-08-21 |  |  | Studio #7679 (NEW-22) · data · auto · local-only · REQ: REQ-005 · spec: code:LineModel.webPushPc, FA-006 job-spec muc 7 · techNote: đối chứng âm cho câu lệnh cập nhật updateMesssageErrorAndStatus — xác nhận điều kiện lọc theo đúng id đăng ký chứ không quét rộng theo admin/bot (DATA-DB-001 adapt: verify WHERE đúng phạm vi bằng 2 tài khoản khác nhau). · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-DATADB001-01 | DATA-DB-001 | Abnormal | Đăng ký nhận thông báo đã bị huỷ ở phía trình duyệt bị chuyển sang ngừng nhận | Job đã bật chức năng gửi web push. Có 1 đăng ký nhận thông báo PC đang ở trạng thái đang nhận, trỏ tới máy chủ giả lập. Máy chủ giả lập trả mã 410 kèm nội dung 「push subscription has unsubscribed or expired」. | 1. Ghi lại trạng thái và nội dung lỗi hiện tại của bản ghi đăng ký<br>2. Tạo 1 bản ghi thông báo PC ở trạng thái chờ xử lý cho admin sở hữu đăng ký này<br>3. Chạy job xử lý thông báo web push PC<br>4. Kiểm tra lại bản ghi đăng ký trong dữ liệu và rà log job | Nội dung phản hồi lỗi: 「push subscription has unsubscribed or expired」. Cần kiểm thêm 3 nội dung lỗi cùng nhóm: 「No such subscription」, 「BadJwtToken」, 「A valid push subscription endpoint should be specified」. | Với cả 4 nội dung lỗi trên, bản ghi đăng ký đều được chuyển sang trạng thái ngừng nhận và trường nội dung lỗi được ghi lại đúng nội dung mà máy chủ trả về. Log job ghi lại việc cập nhật trạng thái kèm mã đăng ký. Hành vi này phải giống hệt bản trước khi sửa lỗi — nếu sau fix có nội dung lỗi nào không còn khớp và đăng ký hỏng không bị tắt nữa thì đánh Không đạt. | Đạt |  | PRD | hanhntb | 2026-08-21 |  |  | Studio #7678 (NEW-21) · data · auto · local-only · REQ: REQ-005 · spec: FA-006 job-spec muc 7, code:LineModel.webPushPc, SRC-REGRESSION-007 · techNote: điểm rủi ro thật của diff — LineModel.webPushPc:298-306 so khớp CHUỖI trên giá trị trả về của sendPushNotifyPC. Sau fix giá trị này vẫn là '' + IOUtils.readLines(entity) nên định dạng giữ nguyên, nhưng phải chạy để chắc. Kiểm cả 4 chuỗi bằng cách chạy lại TC với 4 nội dung phản hồi khác nhau (dataset, không tách 4 TC). · author=AI · status=draft |
| TC-TOOLERRHYG001-07 | TOOL-ERRHYG-001 | Abnormal | Đăng ký thiếu khoá xác thực hoặc đã tắt bị bỏ qua, không phát sinh lần gửi nào | Job đã bật chức năng gửi web push. Cùng 1 admin có 3 đăng ký nhận thông báo PC: E thiếu khoá xác thực, F đang ở trạng thái ngừng nhận, G hợp lệ trỏ máy chủ giả lập trả mã 201. | 1. Tạo 1 bản ghi thông báo PC ở trạng thái chờ xử lý cho admin nói trên<br>2. Chạy job xử lý thông báo web push PC<br>3. Đếm số yêu cầu máy chủ giả lập nhận được<br>4. Kiểm tra lại trạng thái của cả 3 bản ghi đăng ký | Đăng ký E: khoá xác thực để trống. Đăng ký F: trạng thái ngừng nhận. Đăng ký G: hợp lệ. | Máy chủ giả lập chỉ nhận ĐÚNG 1 yêu cầu (của đăng ký G). Hai đăng ký E và F bị bỏ qua ngay từ đầu, không phát sinh lần gửi nào và không bị mở kết nối tương ứng. Trạng thái của cả 3 bản ghi giữ nguyên như trước khi chạy, không bản ghi nào bị ghi thêm nội dung lỗi. | Đạt |  | PRD | hanhntb | 2026-08-21 |  |  | Studio #7680 (NEW-23) · data · auto · local-only · REQ: REQ-005 · spec: code:LineModel.webPushPc, FA-006 job-spec muc 3.5 · techNote: LineModel.webPushPc:296 — if (item.getAuthToken() == null \|\| item.getStatus() == STATUS_FALSE) continue. Nhánh này nằm TRƯỚC lời gọi sendPushNotifyPC nên hoàn toàn không đụng tầng transport; giữ TC để chắc bản fix không vô tình đổi thứ tự kiểm tra và làm lãng phí kết nối trong pool. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-NOVP-01 |  | Boundary | Cùng một Admin đăng ký Web Push trên 2 PC/2 Chrome profile | Cùng một tài khoản Admin.<br>PC/Profile A đã bật Web Push.<br>PC/Profile B cũng đã bật Web Push.<br>Hai profile đều có quyền Notification. | 1. Đăng nhập cùng tài khoản Admin trên Chrome A.<br>2. Bật nhận thông báo PC và xác nhận browser đã Allow notification.<br>3. Đăng nhập cùng Admin trên Chrome B hoặc PC thứ hai.<br>4. Bật nhận thông báo PC trên Chrome B.<br>5. Giữ cả hai browser đang hoạt động.<br>6. Thực hiện một hành động tạo notification cho Admin.<br>7. Quan sát Chrome A.<br>8. Quan sát Chrome B.<br>9. Lặp lại thêm 2–3 lần với notification khác nhau. |  | Chrome A nhận notification.<br>Chrome B cũng nhận notification.<br>Mỗi browser nhận đúng 1 lần cho mỗi event.<br>Không có browser nào nhận 2 lần cho cùng một event.<br>Việc gửi tới subscription thứ nhất không làm subscription thứ hai bị bỏ qua.<br>Các lần gửi tiếp theo vẫn hoạt động bình thường. | Đạt |  | PRD | hanhntb | 2026-08-19 |  |  | Studio #11112 (NEW-26) · job · auto · local-only · author=hanhntb · status=draft |
| TC-NOVP-02 |  | Abnormal | Một thiết bị bị mất quyền Notification không làm thiết bị còn lại mất Web Push | Cùng Admin đã bật notification trên PC A và PC B.<br>Ban đầu cả hai máy đều nhận Web Push thành công. | 1. Gửi 1 notification để xác nhận PC A và PC B đều nhận.<br>2. Trên PC A, mở Chrome Site Settings.<br>3. Chuyển quyền Notifications của site từ Allow → Block.<br>4. Giữ PC B ở trạng thái bình thường.<br>5. Thực hiện một event mới phát sinh Web Push.<br>6. Quan sát PC A.<br>7. Quan sát PC B.<br>8. Thực hiện thêm một notification nữa.<br>9. Quan sát PC B lần thứ hai. |  | PC A không hiển thị notification do browser đã Block.<br>PC B vẫn phải nhận notification bình thường.<br>Subscription lỗi/không sử dụng được của PC A không làm toàn bộ lượt gửi dừng.<br>Notification kế tiếp trên PC B vẫn tiếp tục nhận được.<br>Không xảy ra trường hợp một subscription lỗi làm user mất notification trên tất cả thiết bị. | Đạt |  | PRD | hanhntb | 2026-08-19 |  |  | Studio #11114 (NEW-28) · job · auto · local-only · author=hanhntb · status=draft |
| TC-NOVP-03 |  | Boundary | Tắt rồi bật lại 「Thông báo PC」 trên hệ thống | Admin đang nhận Web Push bình thường trên Chrome.<br>Browser vẫn Allow notification. | 1. ửi một notification và xác nhận Admin nhận được.<br>2. Mở màn 「通知設定」.<br>3. Tắt chức năng nhận notification trên PC.<br>4. Lưu setting.<br>5. Thực hiện một event có phát sinh notification.<br>6. Xác nhận trạng thái trên PC.<br>7. Quay lại 「通知設定」.<br>8. Bật lại nhận notification trên PC.<br>9. Nếu UI yêu cầu cấp lại quyền/đăng ký lại thì hoàn tất.<br>10. Tạo một notification mới.<br>11. Quan sát PC. |  | Trước khi tắt: nhận notification bình thường.<br>Trong thời gian setting PC notification đang OFF: không nhận Web Push.<br>Không xuất hiện notification cũ bị gửi muộn sau khi OFF.<br>Sau khi bật lại: notification mới được nhận bình thường.<br>Mỗi event chỉ xuất hiện một lần.<br>Không cần tạo lại account hoặc đổi browser mới để tiếp tục nhận notification. | Đạt |  | PRD | hanhntb | 2026-08-19 |  |  | Studio #11113 (NEW-27) · job · auto · local-only · author=hanhntb · status=draft |
| TC-NOVP-04 |  | Abnormal | Block quyền Notification của Chrome rồi cấp lại quyền | Web Push đang hoạt động bình thường.<br>「通知設定」 trên hệ thống vẫn ON. | 1. Gửi notification A và xác nhận nhận bình thường.<br>2. Trong Chrome Site Settings, đổi Notification thành Block.<br>3. Reload trang quản trị.<br>4. Thực hiện event tạo notification B.<br>5. Xác nhận không xuất hiện notification B trên PC.<br>6. Đổi quyền Notification từ Block về Allow.<br>7. Reload lại trang.<br>8. Nếu sản phẩm yêu cầu đăng ký lại Web Push, thực hiện theo UI.<br>9. Thực hiện event tạo notification C.<br>10. Quan sát notification trên PC.<br>11. Thực hiện thêm notification D để xác nhận trạng thái ổn định. |  | A nhận bình thường.<br>Khi Chrome Block: không hiển thị B.<br>Hệ thống không crash/trắng màn hình khi browser từ chối notification.<br>Sau khi Allow lại và hoàn tất đăng ký cần thiết: nhận được C.<br>Notification D tiếp tục nhận bình thường.<br>Không nhận bù/trùng B sau khi Allow lại. | Đạt |  | PRD | hanhntb | 2026-08-19 |  |  | Studio #11115 (NEW-29) · job · auto · local-only · author=hanhntb · status=draft |
