# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | MCP LME TEST STUDIO — task #281 (ticket 40056, round 1, branch `ai_fixbug_40056`) |
| Tổng số TC review | 87 |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `12/40 vùng ảnh hưởng đủ TC · 20 GAP · 7 RISK · 1 orphan`

> ⚠️ **Trả lời trực tiếp câu hỏi "TC đã cover đủ đánh giá ảnh hưởng phía Dev chưa": CHƯA — và bản thân mục 4.3 của Dev cũng thiếu.**
> Bảng đối chiếu từng mục Dev kê nằm ngay dưới bảng GAP. Tóm tắt: **T1** thiếu nhánh thành công của xoá thư mục + sao chép nhóm · **T2** thiếu 5/6 loại message · **T3 T4** mới có nhánh thành công, nhánh lỗi chưa chạy · **T5 trống hoàn toàn** · và **5 tính năng dùng chung mà spec §8 kê thì Dev không kê** (`G24`).

> **Đối chiếu endpoint** (bổ sung sau khi rà [feature-spec.md §6](../../spec-features/admin/message-template/feature-spec.md) — 40+ endpoint / 10 nhóm):
> - **Trong phạm vi fix (24 endpoint)**: ~20 có TC, 4 mỏng → xem `G21` `G22`.
> - **Ngoài danh sách fix nhưng cùng màn / dùng chung JS đã sửa**: **4 nhóm endpoint hoàn toàn 0 TC** → `G13` `G14` `G15` `G18`.

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | Nhánh **LỖI** của màn dùng chung (broadcast / scenario / reminder) — Studio `dev_impact` rủi ro hồi quy (2): "FE nhánh success cũ dựa cờ body 200 nay không kích hoạt trên lỗi → caller chưa sửa sẽ **im lặng/sai**" | `diff code` | `NEW-85` `NEW-86` `NEW-84` `NEW-87` | **GAP thực tế — cả 4 TC đều `Chưa test`**. Nhánh thành công đã pass (`NEW-72/73/74/76/78`) nhưng đúng rủi ro số 1 của fix thì chưa có kết luận nào | `[BLOCKER]` |
| G2 | `T5 — Reminder Delivery (FA-022)` | `dev-impact` | `NEW-83` `NEW-84` | **GAP thực tế — cả 2 TC `Chưa test`**. 1/5 tính năng bị ảnh hưởng hiện không có bằng chứng test nào | `[BLOCKER]` |
| G3 | `F4 — copyFile / cloneActionButton`: helper chặn `action_id`, nhánh **"action chưa có detail nào thì CHO QUA"** (bypass có chủ ý của Dev, vì `t_actions` không có `bot_id`) | `diff code` | `NEW-11` `NEW-12` (chỉ nhánh **từ chối**) | **GAP — 0 TC cover nhánh cho-qua**. Điều kiện mới thêm chỉ test 1/2 nhánh (BƯỚC 2 câu hỏi #2). Đây là lỗ hổng bảo mật **có chủ ý**, phải chứng minh nó không bị lợi dụng | `[BLOCKER]` |
| G4 | 5/6 loại message con có file JS bị sửa trong diff — `media.js` `stamp.js` `location.js` `introduction.js` `button.js` (25 dòng) | `diff code` | `NEW-39` (chỉ **mở** khối), `NEW-62` `NEW-70` `NEW-61` (nhánh lỗi) | **GAP — 0 TC lưu thành công** cho ảnh/video/âm thanh, stamp, vị trí, thẻ giới thiệu, panel/button. Duy nhất `NEW-38` lưu được message **văn bản**. BƯỚC 2 câu hỏi #1 | `[MAJOR]` |
| G5 | `BUG-8 / F6` — nút bật/tắt gửi giãn cách khôi phục trạng thái ở nhánh lỗi (lỗi **do vòng fix 1 gây ra**, đã refix) | `dev-impact` | `NEW-48` (`Chưa test`), `NEW-49` (happy path) | **GAP thực tế**. `NEW-48` test tình huống "nhóm chưa lưu", KHÔNG phải kịch bản Dev mô tả: *mở modal, **không đổi gì**, bấm lưu, request thất bại → không được lật ngược trạng thái* | `[MAJOR]` |
| G6 | `getDataIntroduction` / `getDataLocation` — 2 bảng `tmp_introduction` / `tmp_location` **không có cột `bot_id`**, Dev ràng buộc **gián tiếp** qua template cha | `diff code` | `không có` | **GAP — 0 TC cross-tenant** chạm 2 endpoint này. `NEW-2` là `getDataPreviewTemplateChild`, endpoint khác. Ràng buộc gián tiếp là chỗ dễ hở nhất trong cả fix | `[MAJOR]` |
| G7 | `BUG-7 / F3` — endpoint path-param `GET .../park-template/list-template/{id}`; Dev ghi **"3 nơi gọi đều hỏng"** nhưng không kê ra 3 nơi đó | `dev-impact` | `NEW-80` `NEW-26` | **RISK — không xác nhận đủ 3 nơi gọi**. BƯỚC 2 câu hỏi #3: fix chạm hàm dùng chung mà Dev không kê danh sách caller | `[MAJOR]` |
| G8 | Xoá hàng loạt mẫu tin — câu SQL **nhận thẳng danh sách id từ client** (Dev tự đánh giá là điểm nguy hiểm) | `diff code` | `NEW-5` | **RISK — chỉ test danh sách TOÀN id bot khác.** Thiếu case **trộn** id bot mình + id bot khác: hệ thống xoá phần hợp lệ (partial) hay từ chối cả lô (all-or-nothing)? | `[MAJOR]` |
| G9 | `ajaxDelTemplate` — cleanup phụ khi xoá template (spec BR-06: `ActionDetail` + `SendRandomMessage` + `TemplateMappingTable`; BR-15: `step_message.update_timestamp`) | `dev-impact` | `NEW-22` | **RISK — chỉ verify UI + reload** (RULE-07). Endpoint này vừa được thêm validate + scope `bot_id`; nếu scope sai thì template biến mất khỏi danh sách nhưng step_message vẫn trỏ tới. `NEW-16` chỉ kiểm **negative control** trên bot B | `[MAJOR]` |
| G10 | Thao tác **GHI trên màn tạo/sửa** khi bot đang sao lưu (spec BR-02: `store` / `save` cũng check `BackupHistory`) | `diff code` | `NEW-37` (chỉ màn **danh sách**) | **RISK** — `NEW-37` cover tốt 4 thao tác màn danh sách, nhưng màn tạo mẫu tin / tạo nhóm không có TC nào | `[MAJOR]` |
| G11 | `app/Services/Templates/TemplateV2Service.php` — **chứa chính fix bảo mật cross-tenant** (`createTemplateGroup` đổi `find($id)` → `where id + bot_id`; `getDataPreviewTemplateChild` thêm `where bot_id`) | `orphan` | `NEW-1` `NEW-2` (TC **có** cover) | **File nằm trong diff nhưng KHÔNG có trong mục 4.1 của `03-dev-impact.md`** — Dev kê 20 file, commit 21 file. Coverage may mắn đủ, nhưng chiều `dev-impact` đang thiếu điểm sửa quan trọng nhất | `[MAJOR]` |

| G12 | **Xoá thư mục ĐANG CHỨA mẫu tin** — spec BR-05: cascade **hard delete** toàn bộ template bên trong, đệ quy xuống message con, **mất dữ liệu không phục hồi**. Nhánh này nằm trong `ajaxInitTemplate` (EP-20) — endpoint Dev vừa sửa (`deleteGroup id=0` 200→400 + scope `bot_id`) | `dev-impact` | `NEW-21` (chỉ nhánh **từ chối** xoá `未分類`) | **GAP — 0 TC cover thao tác xoá thư mục thành công.** Thao tác phá huỷ nặng nhất của màn, vừa được thêm validate + đổi mã HTTP, không có TC nào chạy | `[BLOCKER]` |
| G13 | Nhóm **6.7 Media Library** (EP-60~64: thư viện ảnh/video/âm thanh/PDF, đổi thumbnail) + nhóm **6.6 Image Map** (EP-55, EP-56) | `diff code` | `không có` | **GAP — 0/7 endpoint có TC.** `media.js` **nằm trong diff**; thư viện media và image map mở trực tiếp từ màn soạn message → không phải "ngoài phạm vi" mà là vùng ảnh hưởng chưa test | `[MAJOR]` |
| G14 | **EP-21** `POST /basic/message-template/add` + **EP-22** `POST /basic/message-template/save/{id}` — đường ghi template **đời cũ**, spec BR-14 (reset + recreate **TẤT CẢ** relationships) và BR-03 (`botIdCurrent == getBotId()`) | `diff code` | `không có` | **GAP — 0 TC.** Đây là 2 endpoint ghi dữ liệu của chính màn template; BR-14 xoá hết relationship cũ trước khi tạo lại → rủi ro mất dữ liệu nếu bước tạo lại thất bại | `[MAJOR]` |
| G15 | **EP-26** `GET /ajax/message-template/{id}` — lấy chi tiết 1 template, **nhận id qua đường dẫn** | `diff code` | `không có` | **GAP — 0 TC.** Cùng **shape** với `BUG-7` (endpoint path-param làm rule required luôn trượt → 422). Dev khẳng định "chỉ endpoint park-template lấy tham số từ đường dẫn" nhưng spec §6.2 liệt kê endpoint này cũng dùng path param | `[MAJOR]` |
| G16 | **Sửa nội dung một message đã có rồi lưu lại** (không phải tạo mới) | `dev-impact` | `NEW-40` (chỉ **MỞ** lại, không sửa + lưu) | **GAP — 0 TC.** `saveTemplate` là endpoint được sửa nhiều nhất; luồng sửa-rồi-lưu là thao tác phổ biến nhất của màn và đi qua BR-14 (reset + recreate relationships) | `[MAJOR]` |
| G17 | Các action **folder** còn lại của EP-20 (endpoint đa năng 11 action): **sắp xếp folder** (kéo-thả / 一番上の移動 / 一番下の移動) và **ẩn folder** (フォルダを非表示) | `diff code` | `không có` | **GAP — 0 TC.** EP-20 là endpoint Dev thêm "validate theo action"; chỉ 4/11 action có TC (thêm, đổi tên, xoá, di chuyển/tìm kiếm) | `[MAJOR]` |
| G18 | Nhóm **6.8 Test & LINE User** (EP-70~73: gửi test, gửi test V3, danh sách tester, tìm kiếm tester) | `diff code` | `NEW-78` (chỉ bật/tắt trả lời nhanh) | **GAP — 0 TC thực sự gửi test.** 2 file JS `modal-preview-send-test.js` + `modal-send-test-all.js` nằm trong diff | `[MAJOR]` |
| G19 | **Nhân bản NHÓM mẫu tin** — spec BR-07: deep clone **đệ quy** toàn bộ message con + relationships, `position = min - 1` | `dev-impact` | `NEW-24` (nhân bản **1 mẫu tin đơn**) `NEW-3` (cross-tenant) | **GAP — 0 TC nhân bản nhóm.** `copyTemplate` được sửa (200→404, 500→409, exception→500, + scope bot); nhánh đệ quy cho group là nhánh phức tạp nhất và không được chạy | `[MAJOR]` |
| G20 | **Thao tác hàng loạt của chính mình** (Normal): xoá hàng loạt + 一括フォルダ変更 di chuyển hàng loạt | `dev-impact` | `NEW-5` (Abnormal, cross-tenant) `NEW-28` (1 bước trong chuỗi) | **GAP — 0 TC Normal.** Câu xoá hàng loạt nhận thẳng danh sách id từ client và vừa được thêm scope `bot_id` → phải chứng minh nó vẫn xoá đúng phần hợp lệ của chính mình | `[MAJOR]` |
| G21 | **EP-53 / EP-54** Metadata URL (OGP fetch: title, image, description) | `diff code` | `NEW-60` (chỉ Abnormal — thiếu dữ liệu bắt buộc) | **RISK — 0 TC Normal.** Endpoint gọi **ra domain ngoài**; `exception → 500` vừa được sửa. Không TC nào lấy metadata thành công từ một URL thật (→ RULE-08 / ENV-003) | `[MAJOR]` |
| G22 | 6 endpoint **lấy dữ liệu editor** (EP-40 text, EP-41 stamp, EP-42 location, EP-46 button, EP-47 media, + `getDataIntroduction`) | `diff code` | `NEW-39` (mở 6 loại, mã **rỗng**) `NEW-62` (mã sai kiểu / rỗng) `NEW-40` (2/6 loại) | **RISK — không TC nào nạp dữ liệu THẬT của template đã có cho 4/6 loại** (stamp, button, media, introduction). Rule mới `template_id nullable\|integer` chỉ được test ở nhánh rỗng và nhánh sai kiểu | `[MAJOR]` |

| G23 | **Xoá THƯ MỤC của bot khác** — Dev kê rõ ở mục 2 vòng 2: *"endpoint điều phối (**xoá thư mục**, xoá 1 mẫu, xoá nhiều mẫu)"* nằm trong danh sách được siết `bot_id` | `dev-impact` | `không có` | **GAP — 0/15 TC cross-tenant chạm xoá thư mục.** `NEW-4` là xoá mẫu tin, `NEW-5` là xoá hàng loạt mẫu tin. Kết hợp BR-05 (cascade **hard delete** toàn bộ template + message con bên trong) → đây là đường **phá huỷ dữ liệu hàng loạt của bot khác**, nặng hơn mọi case cross-tenant đã test | `[BLOCKER]` |
| G24 | **Mục 4.3 của Dev thiếu 5 tính năng dùng chung.** [Spec §8](../../spec-features/admin/message-template/feature-spec.md) kê **8 tính năng** tham chiếu SC-001/SC-005: FA-001 · **FA-003 tự động trả lời** · **FA-004 rich menu** · **FA-007 tin nhắn chào mừng** · FA-008 · FA-009 · **FA-013 danh sách bạn bè** · **FA-015 thông tin bạn bè**. Dev chỉ kê FA-008, FA-009, FA-022 | `dev-impact` | `NEW-74` `NEW-76` (chat 1:1 — TC đi **xa hơn** danh sách Dev) | **GAP — 0 TC cho 5 tính năng.** Bằng chứng cụ thể đây không phải suy diễn: `NEW-52` coi `richmenu` là giá trị `action_type` **KHÔNG hợp lệ** → nếu màn rich menu thật sự gọi `saveTemplate` với ngữ cảnh đó thì fix này làm hỏng FA-004 | `[BLOCKER]` |
| G25 | Rule mới **`action_type in-list`** ở `saveTemplate` (Studio `dev_impact` ghi rõ) | `diff code` | `NEW-52` (chỉ 2 giá trị **không** hợp lệ: rỗng và `richmenu`) | **GAP — 0 TC liệt kê và verify ĐỦ danh sách giá trị hợp lệ.** Rule `in-list` là dạng chặn: giá trị nào thiếu trong danh sách thì màn đó chết. Đây chính là cơ chế đe doạ FA-003 / FA-004 / FA-007 / FA-013 / FA-015 (`G24`) và Reminder (`G2`, dùng ngữ cảnh `event`) | `[BLOCKER]` |
| G26 | **Carve-out ticket #40979** — Dev **CỐ Ý** không lọc `bot_id` cho 4 câu truy vấn ở nhánh **di chuyển / sắp xếp** mẫu tin | `dev-impact` | `không có` | **GAP — 0 TC ghi nhận lỗ hổng còn mở.** Hệ quả kép: (1) tester tình cờ test sẽ raise ticket trùng #40979; (2) khi #40979 merge thì không có điểm neo để so sánh trước/sau. Cần 1 TC đánh dấu phạm vi, không phải để "đạt" mà để ghi nhận trạng thái | `[MAJOR]` |
| G27 | `saveTemplate` — **đổi tên / đổi thư mục mẫu tin của bot khác**. Dev kê ở mục 2 vòng 2: *"Màn tạo mẫu tin nhắn/nhóm: **lưu mẫu tin nhắn (đổi tên/đổi thư mục)**"* thuộc danh sách siết bot | `dev-impact` | `NEW-1` (chỉ `ajaxCreateGroup`) | **GAP — 0 TC.** `saveTemplate` và `ajaxCreateGroup` là 2 endpoint khác nhau; `NEW-1` không cover đường này | `[MAJOR]` |
| G28 | Helper chặn `action_id` đặt **tại nơi gọi** thay vì sửa hàm chung — Dev nêu hàm nhân bản action *"đang dùng chung **~110 nơi**"*; spec §8 xác nhận `t_actions` + `t_actions_detail` dùng bởi FA-001, FA-003, FA-004, FA-010, FA-012, FA-013, FA-015 | `diff code` | `NEW-11` `NEW-12` (chỉ 2 nơi gọi **trong màn template**) | **RISK — 108/110 nơi gọi còn lại không được chặn và cũng không có TC.** Dev chọn cách vá cục bộ; report này không kết luận cách đó sai, nhưng phải có TC chứng minh các nơi gọi khác **không bị đổi hành vi** | `[MAJOR]` |

> Ghi chú chiều `diff code`: lấy từ Studio `task_get_context(281, ["dev_impact","spec_delta"])` — `diffAvailable = true`, 21 file / 833 thêm / 125 bớt.
>
> **Đối chiếu từng mục Dev kê ở `03-dev-impact.md` mục 4.3** (câu hỏi trực tiếp của Leader):
>
> | Dev kê | Thao tác Dev liệt kê | Nhánh **thất bại báo đúng lý do** | Nhánh **thành công** | Kết luận |
> |---|---|---|---|---|
> | **T1** Message Template (FA-010/SC-001) — màn danh sách | thêm thư mục | `NEW-18` `NEW-19` ✔ | `NEW-17` ✔ | Đủ |
> | | đổi tên thư mục | `NEW-33` ✔ | `NEW-20` ✔ | Đủ (nhưng xem `DUP-3` §3) |
> | | **xoá thư mục** | `NEW-21` `NEW-31` `NEW-37` ✔ | **✗ `G12`** | **Thiếu** — và thiếu cả cross-tenant (`G23`) |
> | | xoá mẫu tin | `NEW-23` `NEW-31` `NEW-37` `NEW-4` ✔ | `NEW-22` ✔ | Đủ |
> | | **sao chép mẫu tin** | `NEW-25` `NEW-37` `NEW-3` ✔ | `NEW-24` (đơn) — **✗ nhóm `G19`** | Thiếu nhánh deep clone |
> | | **di chuyển mẫu tin** | `NEW-32` ✔ | `NEW-28` (1 bước trong chuỗi) | Mỏng (`G20`) + carve-out `G26` |
> | | **sắp xếp mẫu tin** | `NEW-34` ✔ | `NEW-28` (1 bước trong chuỗi) | Mỏng (`G17`) + carve-out `G26` |
> | **T2** Template Message / Editor (SC-001/SC-005) | **soạn text/ảnh/video/stamp/vị trí/nút** | `NEW-51` `NEW-62` `NEW-70` ✔ | **chỉ text `NEW-38`** — ✗ 5 loại (`G4`) | **Thiếu** |
> | | lưu mẫu tin nhắn | `NEW-41` `NEW-42` `NEW-51` `NEW-53` ✔ | `NEW-38` ✔ | Đủ (thiếu sửa-rồi-lưu `G16`) |
> | | tạo nhóm | `NEW-44` `NEW-45` `NEW-54` `NEW-1` ✔ | `NEW-43` ✔ | Đủ |
> | | sắp xếp message con | `NEW-56` `NEW-9` ✔ | `NEW-46` ✔ | Đủ |
> | | xoá message con | `NEW-57` `NEW-8` ✔ | `NEW-47` ✔ | Đủ |
> | | **bật/tắt gửi giãn cách** | `NEW-48` **`Chưa test`** `NEW-58` `NEW-10` | `NEW-49` ✔ | **Thiếu nhánh lỗi** (`G5`) |
> | **T3** Broadcast (FA-008) | nhánh hiển thị lỗi tại nơi gọi | `NEW-85` **`Chưa test`** ✗ | `NEW-72` `NEW-76` `NEW-78` ✔ | **Một nửa** (`G1`) |
> | **T4** Step Delivery / Scenario (FA-009) | nhánh hiển thị lỗi tại nơi gọi | `NEW-86` **`Chưa test`** ✗ | `NEW-73` `NEW-76` `NEW-78` ✔ | **Một nửa** (`G1`) |
> | **T5** Reminder Delivery (FA-022) | nhánh lỗi + ngữ cảnh `event` | `NEW-84` **`Chưa test`** ✗ | `NEW-83` **`Chưa test`** ✗ | **Trống hoàn toàn** (`G2`) |
> | **4.2 Data** | chặn xoá theo id `null` | `NEW-31` ✔ | — | Đủ |
> | | chặn ghi đè danh sách message con thành rỗng | `NEW-56` `NEW-36` ✔ | — | Đủ |
> | **Mục 1 — root cause** | `BUG-1` lỗi nghiệp vụ trả 200 | `NEW-23` `NEW-25` `NEW-51` ✔ | — | Đủ |
> | | `BUG-2` backup trả 500 | `NEW-37` ✔ (expected ghi rõ "không trả 500") | — | Đủ ở màn danh sách; thiếu màn tạo (`G10`) |
> | | `BUG-3` thiếu validate required | 11 TC ✔ | — | Đủ (nhưng mang mã lạ — `Q8`) |
> | | `BUG-4` catch bắt nhầm lớp ngoại lệ | `NEW-64` — **đọc source code, không phải hành vi** ✗ | — | **Không có bằng chứng runtime** (`I1` `I8`) |
> | | `BUG-5` lấy mã ngoại lệ PHP làm HTTP code | `NEW-63` ✔ | — | Đủ |
>
> **Bảng đối chiếu 10 nhóm endpoint của [spec §6](../../spec-features/admin/message-template/feature-spec.md)** ↔ TC:
>
> | Nhóm spec §6 | Endpoint | Trong phạm vi fix? | TC cover | Kết luận |
> |---|---|---|---|---|
> | 6.1 Page Routes | EP-01~12 | Một phần (4 blade) | `NEW-65` `NEW-71` `NEW-80` | Đủ ở mức nạp trang · **EP-04 set-cookie folder (BR-13) 0 TC** |
> | 6.2 CRUD & danh sách | EP-20~26 | EP-20/23/24/25 có | `NEW-17…37` (12 TC) | EP-20/23/24/25 tốt · **EP-21 EP-22 EP-26 = 0 TC** (`G14` `G15`) |
> | 6.3 TemplateV2 Editor | EP-30~36 | **Có, toàn bộ** | `NEW-38…61` (18 TC) | Đủ |
> | 6.4 Lấy dữ liệu editor | EP-40/41/42/46/47 | **Có** | `NEW-39` `NEW-62` `NEW-40` | Mỏng (`G22`) |
> | 6.5 URL Redirect & Metadata | EP-50~54 | **Có** | `NEW-13` `NEW-60` `NEW-63` | EP-50/51 đủ · **EP-52 0 TC** · EP-53/54 thiếu Normal (`G21`) |
> | 6.6 Image Map | EP-55, EP-56 | Không (nhưng `media.js` trong diff) | `không có` | **0 TC** (`G13`) |
> | 6.7 Media Library | EP-60~64 | Không (nhưng `media.js` trong diff) | `không có` | **0 TC** (`G13`) |
> | 6.8 Test & LINE User | EP-70~73 | Không (nhưng 2 JS gửi thử trong diff) | `NEW-78` (một phần) | **0 TC gửi thử** (`G18`) |
> | 6.9 Park Template | EP-80~84 | **Có** | `NEW-26` `NEW-27` `NEW-35` `NEW-36` `NEW-69` `NEW-80` | Đủ |
> | 6.10 Liên quan khác | EP-90 | Không | `NEW-74` (một phần) | Chấp nhận được |

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `24 quan điểm Trigger khớp task · 11 chưa cover đủ`

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `PERM-003` | Cao ★ | **GAP — 0 TC cover.** Fix siết `bot_id` ở 24 endpoint nhưng không TC nào verify kịch bản **đổi bot giữa chừng** (spec BR-03: `store()` kiểm `botIdCurrent == getBotId()` → `別のアカウントに切り替えたので...`). Bot context theo session, đổi bot ở 1 tab đổi cả trình duyệt → đây là đường sinh ra chính cái lỗi mà ticket đang siết | `[BLOCKER]` |
| Q2 | `PERM-001` | Cao | **GAP — 0 TC chạy bằng account staff.** Toàn bộ 87 TC chạy bằng account chủ. Siết `bot_id` ở 24 endpoint có thể chặn nhầm staff được phân quyền, hoặc để hở ở tầng API | `[BLOCKER]` |
| Q3 | `DATA-TEXT-001` | Trung bình → **Cao** (text được gửi LINE) | **GAP — 0 TC cover.** `saveTemplate` thêm rule `data required + json`; không TC nào lưu text chứa emoji / dấu ngoặc kép / backslash / xuống dòng nhiều lần để chứng minh rule `json` không chặn nhầm nội dung hợp lệ. `NEW-51` chỉ test nội dung **không đọc được** (Abnormal) | `[MAJOR]` |
| Q4 | `OUT-PREVIEW-001` | Cao ★ | **RISK — RULE-06.** Task sửa 4 file JS của preview / gửi thử (`modal-preview-send-test`, `modal-send-test-all`, `modal-preview-draff-delivered`, `modal-preview-template-child`) nhưng mọi TC dừng ở màn admin. Không TC nào verify tin thật đến LINE app | `[MAJOR]` |
| Q5 | `MEDIA-001` | Trung bình → **Cao** (output hiển thị cho khách cuối) | **GAP — 0 TC cover.** `media.js` + `copyFile` (copy tệp ảnh) nằm trong diff; `NEW-12` `NEW-61` chỉ test nhánh từ chối / thiếu dữ liệu | `[MAJOR]` |
| Q6 | `ENV-003` | Cao ★ | **RISK — RULE-08.** Task chạm **media / file** và **URL + metadata domain** (`ajaxMetadataUrl`, `ajaxGetListUrlRedirect`); 82/87 TC chạy ở `local`, **0 TC** ở staging / production | `[MAJOR]` |
| Q7 | `REG-SHARED-001` | Cao ★ | **RISK — 3/4 TC mang mã này `Chưa test`** (`NEW-83` `NEW-85` `NEW-86`); chỉ `NEW-72` đã chạy. Fix chạm `showTemplateAjaxError()` dùng chung **21 nơi gọi** mà Dev không kê danh sách 21 nơi | `[MAJOR]` |
| Q8 | `FUNC-002` | Cao | **Chưa map được** — "validate required đầu vào" là **1 trong 3 trục chính** của ticket, nhưng **0 TC** mang mã `FUNC-002`. Nội dung thực tế **đã cover tốt** bởi `NEW-30/31/32/41/44/51/55/56/57/60/61`, chỉ là các TC này mang **mã nội bộ Studio** (`TOOL-VAL2-001`, `TOOL-ERRHYG-001`, `TOOL-KNOW-002`) không có trong `checklist-lme.md` → coverage không map được | `[MAJOR]` |
| Q10 | `FUNC-001` | Cao | **RISK — bộ TC lệch hẳn về nhánh lỗi.** Chỉ **17/87 TC** là thao tác GUI chạy bình thường của màn template (`NEW-14` `NEW-17` `NEW-20` `NEW-22` `NEW-24` `NEW-26` `NEW-28` `NEW-29` `NEW-38` `NEW-39` `NEW-40` `NEW-43` `NEW-46` `NEW-47` `NEW-49` `NEW-80` `NEW-81`), trong khi **52/87 là `Abnormal`**. 8 thao tác chính của màn **không có TC happy-path nào**: xoá thư mục có template · sửa message rồi lưu · nhân bản nhóm · xoá / di chuyển hàng loạt · sắp xếp + ẩn folder · sort theo cột 作成日 / 管理名 · lưu 5/6 loại message · gửi thử. Ticket siết `bot_id` trên **24 endpoint** → rủi ro lớn nhất là **chặn nhầm thao tác hợp lệ**, mà đó đúng là phần mỏng nhất | `[BLOCKER]` |
| Q11 | `FUNC-SEQ-001` | Trung bình ★ **BẮT BUỘC** (màn có ≥2 thao tác trên cùng danh sách + có **Sort**) | **RISK.** `NEW-28` cover chuỗi thêm thư mục → di chuyển → sắp xếp, nhưng **sắp xếp folder** và **ẩn folder** (2 action của EP-20) hoàn toàn không có TC. Màn danh sách có tới 3 cơ chế sắp xếp độc lập: sort folder, sort template (popup 並べ替え), sort theo cột 作成日/管理名 — chỉ 1 được chạm | `[MAJOR]` |
| Q9 | `FUNC-001` · `CONC-001` · `SEC-001` · `OUT-TRUTH-001` · `PERM-002` · `DATA-DB-001` | Cao | **RULE-01 — thiếu loại case, không ghi lý do**: `FUNC-001` 8 TC toàn `Normal` · `CONC-001` 1 TC `Abnormal` · `OUT-TRUTH-001` 1 TC `Abnormal` · `SEC-001` 4 TC toàn `Abnormal` · `PERM-002` 3 TC toàn `Abnormal` · `DATA-DB-001` thiếu `Boundary` | `[MINOR]` |

> Đã loại khỏi phạm vi sau khi kiểm chứng: `DATA-BACKUP-001` (nhánh backup đã có `NEW-37` cover 4 thao tác, chỉ thiếu màn tạo → đã ghi ở G10) · `COMPAT-LEGACY-001` (`NEW-80` `NEW-81` `NEW-69` đủ 2 nhánh cũ/mới) · `LIST-001` (`NEW-29`) · `DEPLOY-ASSET-001` (`NEW-65` `NEW-71`) · `FUNC-004` (`NEW-19/33/42/53/54` đủ Boundary) · `INTG-LINE-001` (task không sửa `sendTestTemplate`; phần output LINE đã gom vào Q4).

---

## 3. TC trùng lặp nội dung

Đã rà **87 TC**, **không có TC nào cần xóa**. Có **1 cặp mâu thuẫn expected** (`DUP-3`, phát hiện khi đối chiếu spec) và 2 cặp chồng lấn một phần nhưng khác tầng kiểm chứng:

| Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| **DUP-3** | `NEW-19` + `NEW-33` (**KHÔNG xóa** — 1 trong 2 sai chuẩn) | — | **`DUP-CONFLICT`** | Cùng quan điểm `FUNC-004`, cùng `Boundary`, cùng đối tượng **tên thư mục** (`category.name`), cùng tiền đề. Nhưng expected **mâu thuẫn**: `NEW-19` (thêm mới) chốt **20 ký tự** + `「フォルダ名は20文字以内で入力してください。」` · `NEW-33` (đổi tên) chốt **50 ký tự** + `「フォルダ名は50文字以内で入力してください。」`. **Cả hai đều `Đạt`** → nghĩa là cùng 1 field đang có 2 giới hạn khác nhau tuỳ đường ghi. Thêm nữa: [spec SCR-TMT-03](../../spec-features/admin/message-template/feature-spec.md) ghi **max 20**, còn kho `TC-TMT-07` `TC-TMT-08` chốt **15** (`フォルダ名は15文字以内`) → **3 nguồn, 3 con số** | `[MAJOR]` |
| DUP-1 | `NEW-42` + `NEW-53` (**giữ cả hai**) | — | Chồng lấn một phần | Cùng quan điểm giới hạn 20 ký tự `管理名` của mẫu tin độc lập, cùng `Boundary`. **Khác tầng**: `NEW-42` qua UI, `NEW-53` gọi thẳng endpoint. Cả hai đều `Không đạt` với **2 ticket bug riêng** (41190 / 41188) → xác nhận lỗi ở cả 2 tầng, không phải trùng | — |
| DUP-2 | `NEW-41` + `NEW-53` (**giữ cả hai**) | — | Chồng lấn một phần | Cùng "lưu mẫu tin độc lập thiếu `管理名`". **Khác tầng** (UI vs endpoint) và `NEW-53` còn cover thêm nhánh quá dài | — |

- **Gate đã chạy**: giả định xóa các TC trên → coverage §1 + §2 **mất cover** tầng UI hoặc tầng API → **không đề nghị xóa**, giữ nguyên.
- `DUP-INFLATE` che GAP: không có.
- `DUP-CONFLICT`: **`DUP-3` (`NEW-19` vs `NEW-33`)** — không xóa TC nào; đã đưa vào **§6 mục 6** để Dev/Leader chốt giới hạn đúng của tên thư mục.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | Toàn bộ nhánh `catch (\Exception) → 500` (AP-1) | **Generic-fix chỉ test 1 trigger runtime.** Fix đổi `catch` ở nhiều endpoint (`ajaxInitTemplate`, `copyTemplate`, `saveTemplate`, `getDataIntroduction`) cùng trả 500 chung, nhưng chỉ `NEW-63` trigger thật 1 lỗi hệ thống (danh sách đường dẫn). `NEW-64` **không phải TC hành vi** — nó đọc source code | Bổ sung TC trigger **≥ 3 lỗi hệ thống khác nhau** ở 3 endpoint khác nhau + **1 trigger chưa biết** (lỗi ngoài danh sách dự kiến) → tất cả phải trả 500 sạch, thân phản hồi chỉ `「エラーが発生しました。」`, không lộ nội dung ngoại lệ. Xem `TC-OUTTRUTH001-01` §5 |
| I2 | `[MAJOR]` | Nguồn TC — 87/87 | **Cột `Evidence thực tế` rỗng toàn bộ** dù 82 TC đã có `Kết quả thực thi` → vi phạm **RULE-02**, kết quả là tự khai | Yêu cầu đính kèm evidence đúng loại (ảnh response + HTTP code trên devtools, ảnh danh sách sau reload, kết quả truy vấn dữ liệu) trước khi Leader duyệt |
| I3 | `[MAJOR]` | Nguồn TC — 87/87 | **Cột `Trạng thái đánh giá spec` rỗng toàn bộ** — không TC nào ghi `Spec ghi rõ` / `Spec không ghi` / `Đã hỏi leader`. Nguy cơ tự suy diễn Expected rồi cho Đạt, đặc biệt với các mã HTTP mới (409 / 422 / 400 / 404) vốn **không có trong spec gốc** | Điền cột này cho toàn bộ TC. Các TC chốt mã HTTP mới phải ghi `Đã hỏi leader` hoặc dẫn nguồn — xem §6 |
| I4 | `[MAJOR]` | Nguồn TC — 42/87 (48%) | **11 mã quan điểm không có trong `framework/checklist-lme.md`**: `TOOL-KNOW-002` (12 TC) · `TOOL-ERRHYG-001` (9) · `TOOL-VAL2-001` (6) · `TOOL-NEGCTRL-001` (4) · `RULE-TOOL-028` (4) · `RULE-TOOL-029` (2) · `TOOL-OLDREC-001` · `AUTH-SESSION-001` · `RULE-09` · `TOOL-AXIS-001` · `TOOL-PAIRWISE-001`. Gần **một nửa bộ TC** không map được coverage → làm quan điểm chuẩn (`FUNC-002`, `OUT-TRUTH-001`…) trông như GAP | Map lại sang mã chuẩn của `checklist-lme.md` (`FUNC-002` cho nhóm validate, `OUT-TRUTH-001` cho nhóm thông báo đúng sự thật, `REG-SHARED-001` cho `RULE-TOOL-028/029`), hoặc bổ sung các mã này vào checklist nếu Leader công nhận |
| I5 | `[MAJOR]` | Nguồn TC — 82/87 chạy `local`, 0 production | **RULE-08 / ENV-003.** Task chạm media/file và URL + metadata domain → không được kết luận từ `local` | Chạy lại tối thiểu nhóm media + URL redirect trên production (xem `TC-FUNC001-01`, và TC kho `TC-TMT-394`) |
| I6 | `[MAJOR]` | `03-dev-impact.md` | **Checkbox "Tester verify auto-fill chính xác" CHƯA tick** (file `Auto-filled: 2026-09-21 by /new-task`) → F/D/T có thể thiếu hoặc map sai. Thực tế **đã phát hiện thiếu** 1 file (G11) | Tester đọc lại Redmine #40056 journal #136618 và tick checkbox trước khi review này có giá trị chốt |
| I6b | `[BLOCKER]` | `03-dev-impact.md` mục 4.3 (AP-6) | **Đánh giá ảnh hưởng của Dev tự nó đã thiếu 5 tính năng.** [Spec §8](../../spec-features/admin/message-template/feature-spec.md) kê 8 tính năng tham chiếu SC-001/SC-005 (FA-001, **FA-003**, **FA-004**, **FA-007**, FA-008, FA-009, **FA-013**, **FA-015**); mục 4.3 chỉ kê FA-008, FA-009, FA-022. Bộ TC bám theo danh sách của Dev nên **kế thừa nguyên lỗ hổng**. `NEW-52` (coi `richmenu` là ngữ cảnh không hợp lệ) là bằng chứng cụ thể rủi ro này có thật | Yêu cầu Dev rà lại 4.3 theo spec §8 + cung cấp **danh sách đầy đủ giá trị `action_type` hợp lệ** của rule `in-list`. Chạy `TC-REGSHARED001-01` + `TC-FUNC003-02` §5 |
| I7 | `[MAJOR]` | `03-dev-impact.md` mục 4.1 (AP-6) | **Thiếu `app/Services/Templates/TemplateV2Service.php`** — Dev kê 20 file, commit 21 file, và file thiếu chính là nơi đặt fix bảo quyền cross-tenant. Ngoài ra Dev nêu "**3 nơi gọi**" (BUG-7), "**21/21 nơi gọi**" (`showTemplateAjaxError`) và "**~110 nơi**" dùng chung hàm nhân bản action nhưng **không kê danh sách nào** | Yêu cầu Dev bổ sung file còn thiếu + 3 danh sách caller, để chốt coverage `G7` `G28` `Q7` |
| I8 | `[MAJOR]` | `NEW-64` (AP-4) | **Không phải test case hành vi** — steps là "mở tệp điều khiển trong mã nguồn", "xác nhận khối bắt lớp ngoại lệ gốc". Đây là code review, tester không dựng được env để chạy, và **không chứng minh** ngoại lệ thật sự bị bắt lúc runtime | Giữ `NEW-64` như ghi chú kỹ thuật, nhưng **không tính là coverage** cho nhánh `catch`. Thay bằng `TC-OUTTRUTH001-01` (I1) |
| I9 | `[MAJOR]` | Bối cảnh fix | **Dev verify chỉ ở mức `lint`** — "chưa chạy được trên dev stack (không có phiên đăng nhập)". Toàn bộ hành vi runtime (HTTP code thật, message tiếng Nhật thật, lọc bot thật) chưa từng chạy trước khi chuyển test | Không phải lỗi của bộ TC, nhưng Leader cần biết: mọi kết luận "đã fix" hiện chỉ dựa vào 82 TC chạy ở `local` |
| I9b | `[MAJOR]` | `NEW-19` `NEW-33` (SPEC-CONFLICT) | **Giới hạn tên thư mục có 3 con số khác nhau ở 3 nguồn** — `NEW-19` chốt 20 (thêm mới), `NEW-33` chốt 50 (đổi tên), [spec SCR-TMT-03](../../spec-features/admin/message-template/feature-spec.md) ghi max 20, kho `TC-TMT-08` chốt 15. Cả `NEW-19` và `NEW-33` đều `Đạt` → hai đường ghi cùng 1 field đang validate khác nhau. Ticket này **vừa thêm validate required cho chính EP-20**, nên đây có thể là hệ quả của fix | Hỏi Dev: rule `max` thực tế đang đặt bao nhiêu ở nhánh `addFolder` và nhánh `renameFolder` của `ajaxInitTemplate`. Chốt 1 con số → sửa TC sai + cập nhật spec (§6 mục 6) |
| I10 | `[MINOR]` | `NEW-22` `NEW-49` | **Expected dừng ở UI, không verify tầng dữ liệu** (RULE-07). `NEW-49` verify công tắc gửi giãn cách bằng "mở lại hộp thoại", không đối chiếu cờ `is_delay_message` đã lưu và cũng không kiểm hiệu lực thật khi gửi thử | Bổ sung bước đối chiếu dữ liệu đã lưu; tham chiếu TC kho `TC-TMT-346` (`is_delay_message = 1`) |
| I11 | `[NIT]` | `NEW-15` | Tiêu đề "Lưu nhóm mẫu tin với mã nhóm **không tồn tại** — tạo nhóm mới thay vì báo lỗi" mô tả hành vi mới sau khi đổi `find($id)` → `where id + bot_id`. Hành vi này **hợp lý về kỹ thuật** nhưng chưa có nguồn spec xác nhận là mong muốn | Xem §6 |

---

## 5. TCs đề xuất bổ sung (26)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | [kho-tcs/fa010-mautinnhan-テンプレート.md](../../kho-tcs/fa010-mautinnhan-テンプレート.md) — 394 TC, 40 nhóm chức năng |
| Vùng regression phát hiện từ kho | `Delay message` (TC-TMT-346→368, 23 TC — `updateDelayMessage` bị sửa) · `Quick reply` (TC-TMT-263→273 — `updateUserQuickReply` bị sửa) · `URL redirect` (TC-TMT-*, 34 TC — `ajaxMetadataUrl` / `ajaxGetListUrlRedirect` bị sửa) · `Media ảnh/video/imagemap` (43 TC — `media.js` + `copyFile`) · `Panel/Button` (88 TC — `button.js` + `cloneActionButton`) · `Preview ở các màn khác` (TC-TMT-106→113) · `Màn list template con` (TC-TMT-73→84) · `Chuyển folder & xóa hàng loạt` (TC-TMT-65→71) · `Template legacy` (TC-TMT-337→345) |
| Conflict expected vs kho | **Không** — không TC đề xuất nào mâu thuẫn expected của kho |
| GAP dùng lại TC kho (không viết mới) | `Q1 → TC-TMT-391` "Bot ID verification: đã chuyển sang account khác rồi submit form tạo template → bị chặn" (cần chỉnh: mở rộng sang **24 endpoint** trong phạm vi ticket, không chỉ form tạo) · `Q2 → TC-TMT-389` + `TC-TMT-390` "Staff KHÔNG được phân quyền template → chặn cả ở UI và ở tầng API" (cần chỉnh: chạy lại **sau** khi siết `bot_id` để chắc staff hợp lệ không bị chặn nhầm) · `Q6 → TC-TMT-394` "Regression trên PRODUCTION: media, domain và job gửi tin của template chạy đúng" |
| Xác nhận chống trùng | Đã đối chiếu **87 TC** ở BƯỚC 0 + **394 TC** kho FA-010 — **không TC đề xuất nào trùng** |

**Không đề xuất TC mới cho**: `G1` · `G2` · `Q7` — TC **đã tồn tại** trên Studio (`NEW-83` `NEW-84` `NEW-85` `NEW-86` `NEW-87`), chỉ là **chưa được chạy**. Việc cần làm là **chạy 5 TC này**, không phải đẻ TC mới. `Q8` cũng không cần TC mới — nội dung đã cover, chỉ cần map lại mã quan điểm (xem I4).

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-PERM002-01 | UI | PERM-002 | Panel/Button — nút & action | Normal | auto | Tất cả | Copy khối nội dung có gắn hành động **chưa cấu hình chi tiết** — vẫn copy được, không bị chặn nhầm | Bot A có 1 mẫu tin loại panel/button; trên đó có 1 nút vừa được thêm nhưng **chưa cấu hình hành động nào** (nút mới tạo, chưa chọn loại hành động). Đang đăng nhập bot A | 1. Mở mẫu tin panel/button của bot A ở màn soạn message<br>2. Bấm copy khối panel chứa nút chưa cấu hình hành động<br>3. Quan sát kết quả và thông báo<br>4. Lưu mẫu tin rồi tải lại trang, mở lại mẫu tin | Nút chưa gắn hành động (chưa chọn loại action) | Khối panel được nhân bản thành công, **không** hiện thông báo không có quyền. Bản sao giữ đủ ảnh, tiêu đề, nội dung và nút. Sau khi lưu và tải lại, bản sao vẫn còn đủ dữ liệu | | Lấp `G3` · Nhánh **cho-qua** của helper chặn `action_id` (Dev cố ý cho qua khi action chưa có detail) · Cặp đối chứng với `NEW-11` `NEW-12` · Đánh giá spec: Spec không ghi — cần hỏi Dev · Evidence: ảnh màn hình trước/sau copy + ảnh mẫu tin sau khi tải lại |
| TC-FUNC001-01 | UI | FUNC-001 | Media — ảnh | Normal | manual | product | Tạo và lưu message loại ảnh / video / âm thanh sau khi siết đầu vào — upload và lưu vẫn chạy đúng | Đang đăng nhập bot test trên production. Có sẵn 1 file ảnh png ≤ 10MB, 1 file video và 1 file âm thanh hợp lệ. Bot không ở trạng thái đang sao lưu | 1. Mở màn soạn message, chọn loại 画像・動画・音声<br>2. Upload ảnh, đặt kích thước hiển thị, lưu<br>3. Lặp lại với video và với âm thanh<br>4. Tải lại trang, mở lại từng message vừa lưu<br>5. Gửi thử mẫu tin tới tài khoản tester và xem trên LINE app | Ảnh png ≤10MB · video mp4 hợp lệ · file âm thanh hợp lệ | Cả 3 loại lưu thành công, không có thông báo lỗi. Sau khi tải lại, mỗi message hiển thị đúng file đã upload và đúng kích thước đã đặt. Tin gửi thử đến LINE app hiển thị đúng ảnh/video/âm thanh | | Lấp `G4` `Q5` `Q6` · RULE-08 (media không kết luận từ local) · RULE-06 (verify trên LINE app thật) · manual vì môi trường production + thiết bị thật · regression — dẫn từ `TC-TMT-276` `TC-TMT-281` · Đánh giá spec: Spec ghi rõ (BR-08, BR-11) · Evidence: ảnh màn soạn sau khi tải lại + ảnh chụp LINE app |
| TC-FUNC001-02 | UI | FUNC-001 | Panel/Button — panel & ảnh | Normal | auto | Tất cả | Tạo và lưu message loại panel/button nhiều panel nhiều nút — lưu và mở lại đúng | Đang đăng nhập bot test. Bot không ở trạng thái đang sao lưu | 1. Mở màn soạn message, chọn loại パネル・ボタン<br>2. Tạo 2 panel, mỗi panel gắn ảnh, tiêu đề, nội dung và 3 nút có hành động<br>3. Bấm lưu<br>4. Tải lại trang và mở lại message vừa lưu<br>5. Đối chiếu số panel, số nút, ảnh, tiêu đề, nội dung, hành động của từng nút | 2 panel × 3 nút (BR-08: từ 2 panel trở lên thì tối đa 3 nút/panel) | Lưu thành công, không có thông báo lỗi và không có lỗi JavaScript. Sau khi tải lại: đúng 2 panel, mỗi panel đúng 3 nút, ảnh/tiêu đề/nội dung và hành động của từng nút khớp với lúc nhập | | Lấp `G4` · `button.js` là file JS sửa nhiều thứ 2 trong diff (25 dòng) · regression — dẫn từ `TC-TMT-288` nhóm `Panel/Button — panel & ảnh` · Đánh giá spec: Spec ghi rõ (BR-08) · Evidence: ảnh màn soạn sau khi tải lại |
| TC-FUNC001-03 | UI | FUNC-001 | Sticker | Normal | auto | Tất cả | Tạo và lưu message loại stamp, vị trí và thẻ giới thiệu — cả 3 loại lưu và mở lại đúng | Đang đăng nhập bot test. Bot không ở trạng thái đang sao lưu | 1. Mở màn soạn message, chọn loại スタンプ, chọn 1 sticker rồi lưu<br>2. Chọn loại 位置情報, chọn vị trí trên bản đồ rồi lưu<br>3. Chọn loại 紹介文, nhập LINE ID và nội dung giới thiệu rồi lưu<br>4. Tải lại trang và mở lại cả 3 message | Sticker bất kỳ · 1 vị trí trên bản đồ có tên địa điểm · LINE ID + nội dung giới thiệu | Cả 3 loại lưu thành công, không có thông báo lỗi. Sau khi tải lại, mỗi message hiển thị đúng sticker / đúng vị trí và tên địa điểm / đúng LINE ID và nội dung giới thiệu đã nhập | | Lấp `G4` · 3 file JS `stamp.js` `location.js` `introduction.js` đều nằm trong diff · regression — dẫn từ `TC-TMT-317` `TC-TMT-322` `TC-TMT-337` · Đánh giá spec: Spec ghi rõ (SCR-TMT-09, SCR-TMT-10) · Evidence: ảnh màn soạn sau khi tải lại của cả 3 loại |
| TC-UI003-01 | UI | UI-003 | Delay message | Abnormal | auto | Tất cả | Mở hộp thoại gửi giãn cách, **không đổi gì**, bấm lưu và máy chủ thất bại — công tắc giữ nguyên trạng thái cũ | Đang đăng nhập bot test; có 1 nhóm mẫu tin **đã lưu**, tuỳ chọn gửi giãn cách đang **BẬT**. Mở sẵn công cụ dành cho nhà phát triển để chặn yêu cầu | 1. Mở màn sửa nhóm mẫu tin, ghi lại trạng thái công tắc gửi giãn cách (đang BẬT)<br>2. Mở hộp thoại tuỳ chọn gửi, **không thay đổi gì cả**<br>3. Trong công cụ dành cho nhà phát triển, chặn để yêu cầu lưu thất bại<br>4. Bấm lưu trong hộp thoại<br>5. Quan sát công tắc trong hộp thoại và công tắc ngoài màn<br>6. Đóng hộp thoại, tải lại trang và mở lại hộp thoại | Không nhập gì — giữ nguyên trạng thái BẬT | Hiện thông báo lỗi. Công tắc trong hộp thoại **và** công tắc ngoài màn đều **vẫn ở trạng thái BẬT** (đúng giá trị trước khi bấm lưu), không bị lật sang TẮT và không hiện giá trị lạ. Sau khi tải lại trang, trạng thái vẫn là BẬT — khớp với máy chủ | | Lấp `G5` · Đúng kịch bản `BUG-8` Dev mô tả ở journal #134899 (lỗi do vòng fix 1 gây ra, khôi phục trạng thái bằng phép đảo) · Khác `NEW-48` ở chỗ nhóm **đã lưu** và **không đổi gì** · Đánh giá spec: Spec không ghi — hành vi do fix quy định · Evidence: ảnh công tắc ở bước 5 và bước 6 + ảnh yêu cầu bị chặn |
| TC-SEC001-01 | API | SEC-001 | Màn list template con | Abnormal | auto | Tất cả | Lấy dữ liệu thẻ giới thiệu và vị trí của message thuộc bot khác — bị từ chối, không lộ nội dung | Bot B có 1 message loại 紹介文 và 1 message loại 位置情報, đã ghi lại mã của cả hai. Đang đăng nhập và chọn bot A | 1. Chọn bot A, mở màn soạn message loại 紹介文 để nắm hình dạng yêu cầu nạp dữ liệu<br>2. Trong công cụ dành cho nhà phát triển, gửi lại yêu cầu nhưng thay mã bằng mã message 紹介文 của bot B<br>3. Ghi lại mã trạng thái và thân phản hồi<br>4. Lặp lại bước 1-3 với message 位置情報 của bot B<br>5. Đăng nhập lại bằng bot B, mở 2 message đó và đối chiếu nội dung | Mã message 紹介文 và 位置情報 của bot B | Cả 2 yêu cầu đều bị từ chối: **không** trả về LINE ID, nội dung giới thiệu, toạ độ hay tên địa điểm của bot B. Dữ liệu 2 message của bot B khi mở bằng chính bot B vẫn nguyên vẹn | | Lấp `G6` · 2 bảng `tmp_introduction` / `tmp_location` **không có cột `bot_id`**, Dev ràng buộc gián tiếp qua template cha — đây là mắt xích yếu nhất của fix bảo mật · Đánh giá spec: Đã hỏi leader — cần Dev xác nhận hành vi mong đợi (404 hay 403) · Evidence: ảnh phản hồi + mã trạng thái của cả 2 yêu cầu |
| TC-COMPATLEGACY001-01 | UI | COMPAT-LEGACY-001 | Màn list template con | Normal | auto | Tất cả | Danh sách message con của nhóm nạp được ở **cả 3 nơi gọi** endpoint đời cũ | Đang đăng nhập bot test; bot có 1 nhóm mẫu tin đời cũ (park-template) chứa ít nhất 2 message con | 1. Mở màn **tạo** nhóm mẫu tin và gắn nhóm đã chuẩn bị, quan sát danh sách message con<br>2. Mở màn **sửa** nhóm mẫu tin đã chuẩn bị, quan sát danh sách message con<br>3. Mở màn **quản lý mẫu tin đời cũ** (park-template) của nhóm đó, quan sát danh sách message con<br>4. Ở mỗi nơi, ghi lại mã trạng thái của yêu cầu nạp danh sách | Nhóm mẫu tin đời cũ có 2 message con | Cả 3 nơi đều nạp được đủ 2 message con, mã trạng thái là 200, **không nơi nào trả 422**. Nội dung message con hiển thị giống nhau ở cả 3 nơi | | Lấp `G7` · `BUG-7` — vòng fix 1 làm mọi yêu cầu trả 422 do tham số nhận qua đường dẫn; Dev ghi "3 nơi gọi đều hỏng" nhưng không kê ra · Nếu Dev xác nhận 3 nơi khác với 3 nơi ở đây thì phải chỉnh lại TC · regression — dẫn từ `TC-TMT-73` · Đánh giá spec: Spec ghi rõ (SCR-TMT-02) · Evidence: ảnh danh sách message con + mã trạng thái ở cả 3 nơi |
| TC-DATADB001-01 | API | DATA-DB-001 | Chuyển folder & xóa hàng loạt | Abnormal | auto | Tất cả | Xoá hàng loạt với danh sách **trộn** mẫu tin của bot mình và bot khác — dữ liệu bot khác không bị chạm | Bot A có 2 mẫu tin bỏ đi được `「A_混在1_<mã phiên chạy>」` `「A_混在2_<mã phiên chạy>」`. Bot B có 2 mẫu tin `「B_混在1_<mã phiên chạy>」` `「B_混在2_<mã phiên chạy>」`, đã ghi lại mã và ảnh chụp dữ liệu của bot B. Đang đăng nhập bot A | 1. Chọn bot A, tích chọn 2 mẫu tin của bot A rồi bấm xoá để nắm hình dạng yêu cầu xoá nhiều<br>2. Trong công cụ dành cho nhà phát triển, gửi lại yêu cầu xoá nhiều với danh sách **trộn**: 2 mã của bot A + 2 mã của bot B<br>3. Ghi lại mã trạng thái và thông báo<br>4. Tải lại danh sách mẫu tin của bot A<br>5. Đăng nhập bot B, tải lại danh sách và đối chiếu với ảnh chụp ban đầu | Danh sách mã trộn: 2 mã bot A + 2 mã bot B | 2 mẫu tin của **bot B vẫn còn nguyên** trong danh sách bot B, không bản ghi nào mất hay đổi chủ sở hữu. Hệ thống xử lý nhất quán với phần của bot A: hoặc xoá đúng 2 mẫu của bot A, hoặc từ chối cả lô — **không được** báo thành công rồi xoá nhầm sang bot B | | Lấp `G8` · Câu xoá hàng loạt **nhận thẳng danh sách id từ client** — Dev tự đánh giá là điểm nguy hiểm · Khác `NEW-5` ở chỗ danh sách **trộn** 2 bot, không phải toàn id bot khác · Đánh giá spec: Đã hỏi leader — cần chốt partial hay all-or-nothing · Evidence: ảnh phản hồi + kết quả truy vấn dữ liệu bot B trước/sau |
| TC-DATAREF001-01 | Data | DATA-REF-001 | Màn list template | Normal | auto | Tất cả | Xoá mẫu tin đang được tham chiếu — các nơi tham chiếu được dọn đúng | Đang đăng nhập bot test. Đã chuẩn bị 1 mẫu tin `「削除連鎖_<mã phiên chạy>」` và gắn nó vào: 1 bước kịch bản, 1 hành động của nút panel, và 1 bản tin gửi toàn bộ chưa gửi. Đã ghi lại ảnh chụp dữ liệu tham chiếu trước khi xoá | 1. Mở màn danh sách mẫu tin, xoá mẫu tin `「削除連鎖_<mã phiên chạy>」` và xác nhận<br>2. Tải lại danh sách, xác nhận mẫu tin đã mất<br>3. Mở màn kịch bản có bước gắn mẫu tin đó, quan sát bước đó<br>4. Mở mẫu tin panel có nút gắn hành động đó, quan sát nút<br>5. Mở bản tin gửi toàn bộ chưa gửi, quan sát nội dung<br>6. Đối chiếu dữ liệu tham chiếu với ảnh chụp trước khi xoá | Mẫu tin được tham chiếu ở 3 nơi | Mẫu tin mất khỏi danh sách. Ở cả 3 nơi tham chiếu, hệ thống thể hiện mẫu tin đã bị xoá một cách nhất quán (không còn trỏ tới bản ghi cũ, không hiện khung trắng, không lỗi JavaScript). Bước kịch bản nhận biết được mẫu tin đã xoá. Không nơi nào còn giữ tham chiếu treo | | Lấp `G9` · Spec BR-06 (cleanup `ActionDetail` + `SendRandomMessage` + `TemplateMappingTable`) + BR-15 (`step_message` nhận biết mẫu tin đã xoá) · `ajaxDelTemplate` vừa được thêm validate + scope bot · RULE-07 — `NEW-22` chỉ verify UI · regression — dẫn từ `TC-TMT-82` `TC-TMT-320` · Đánh giá spec: Spec ghi rõ (BR-06, BR-15) · Evidence: ảnh 3 nơi tham chiếu sau khi xoá + kết quả truy vấn dữ liệu |
| TC-DATABACKUP001-01 | UI | DATA-BACKUP-001 | Tạo group template | Abnormal | auto | Tất cả | Thao tác ghi trên màn **tạo mẫu tin / tạo nhóm** khi bot đang sao lưu — báo đúng trạng thái xung đột | Bot test thuộc gói có chức năng sao lưu dữ liệu. Đã dựng trạng thái đang sao lưu cho bot test. Đang đăng nhập và chọn bot test | 1. Mở màn tạo nhóm mẫu tin, nhập `管理名` hợp lệ rồi bấm 「登録」, ghi lại mã trạng thái và thông báo<br>2. Mở màn soạn message, nhập nội dung văn bản rồi bấm lưu, ghi lại kết quả<br>3. Mở nhóm mẫu tin đã có, kéo thả đổi thứ tự message con, ghi lại kết quả<br>4. Tải lại trang và xác nhận dữ liệu không đổi<br>5. Trả bot về trạng thái không sao lưu sau khi chạy xong | `管理名` hợp lệ · nội dung văn bản hợp lệ | Cả 3 thao tác ghi đều bị chặn với mã xung đột trạng thái (409) kèm thông báo `「データコピー中は、データ不備を回避するために、編集不可能です。少し待ってから操作し直してください。」`. Không thao tác nào được lưu. **Không thao tác nào trả mã lỗi hệ thống (500)** | | Lấp `G10` · Spec BR-02 (`store` / `save` cũng check `BackupHistory`) · Bổ sung cho `NEW-37` vốn chỉ cover màn danh sách · regression — dẫn từ `TC-TMT-392` · Đánh giá spec: Spec ghi rõ (BR-02) · Evidence: ảnh thông báo + mã trạng thái của cả 3 thao tác |
| TC-OUTPREVIEW001-01 | UI | OUT-PREVIEW-001 | Quick test — gửi & preview | Normal | manual | product | Gửi thử nhóm mẫu tin có bật gửi giãn cách — tin đến LINE app đúng nội dung và đúng nhịp | Đang đăng nhập bot test trên production. Có 1 nhóm mẫu tin gồm 3 message con (văn bản, ảnh, panel/button), đã **BẬT** tuỳ chọn gửi giãn cách. Có tài khoản tester với LINE app thật trên thiết bị | 1. Mở nhóm mẫu tin, xem trước từng message con trong hộp thoại xem trước<br>2. Bấm gửi thử tới tài khoản tester<br>3. Quan sát trên LINE app thật: thứ tự tin, khoảng cách thời gian giữa các tin, nội dung từng tin<br>4. Đối chiếu nội dung trên LINE app với nội dung ở hộp thoại xem trước | Nhóm 3 message con · gửi giãn cách BẬT | Cả 3 tin đến LINE app đúng thứ tự, nội dung **khớp với hộp thoại xem trước** (văn bản, ảnh, panel đủ nút). Tin đầu đến ngay, 2 tin sau cách nhau vài giây theo cơ chế giãn cách. Không tin nào bị trùng, bị thiếu hay sai nội dung | | Lấp `Q4` `Q6` · RULE-06 — 4 file JS preview/gửi thử nằm trong diff nhưng mọi TC hiện có dừng ở màn admin · manual vì môi trường production + phải nhìn trên LINE app thật · regression — dẫn từ `TC-TMT-348` `TC-TMT-360` · Đánh giá spec: Spec ghi rõ (BR-10) · Evidence: ảnh chụp LINE app có dấu thời gian + ảnh hộp thoại xem trước |
| TC-DATATEXT001-01 | UI | DATA-TEXT-001 | Text — soạn nội dung | Normal | auto | Tất cả | Lưu message văn bản chứa ký tự đặc biệt và emoji — rule kiểm tra định dạng không chặn nhầm | Đang đăng nhập bot test. Bot không ở trạng thái đang sao lưu | 1. Mở màn soạn message loại テキスト<br>2. Nhập nội dung gồm: emoji, dấu ngoặc kép, dấu gạch chéo ngược, dấu ngoặc nhọn, và 3 lần xuống dòng liên tiếp<br>3. Bấm lưu và ghi lại mã trạng thái<br>4. Tải lại trang, mở lại message và đối chiếu từng ký tự<br>5. Gửi thử tới tài khoản tester và đối chiếu nội dung nhận được | `テスト😀 "引用" \ {token} 【全角】` + 3 lần xuống dòng liên tiếp | Lưu thành công (mã 200), **không** hiện thông báo `「メッセージ内容の形式が正しくありません。」`. Sau khi tải lại, nội dung hiển thị **nguyên vẹn từng ký tự**, giữ đủ 3 lần xuống dòng, không bị escape thành ký tự lạ và không bị cắt | | Lấp `Q3` · `saveTemplate` thêm rule `data required + json` — cần chứng minh rule không chặn nhầm nội dung hợp lệ · `NEW-51` chỉ test nội dung không đọc được (Abnormal) · regression — dẫn từ `TC-TMT-264` `TC-TMT-269` · Đánh giá spec: Spec không ghi — cần Dev xác nhận phạm vi rule `json` · Evidence: ảnh nội dung sau khi tải lại + ảnh tin nhận được |
| TC-DATAREF001-02 | UI | DATA-REF-001 | Folder template | Normal | auto | Tất cả | Xoá thư mục **đang chứa mẫu tin** — cascade xoá đúng phạm vi, thư mục khác không bị chạm | Đang đăng nhập bot test. Đã chuẩn bị thư mục `「削除フォルダ_<mã phiên chạy>」` chứa 1 mẫu tin đơn và 1 **nhóm** mẫu tin (nhóm có 2 message con). Thư mục `「残すフォルダ_<mã phiên chạy>」` chứa 1 mẫu tin khác. Đã ghi lại ảnh chụp dữ liệu của cả 2 thư mục. Bot không ở trạng thái đang sao lưu | 1. Mở màn danh sách mẫu tin, chọn thư mục `「削除フォルダ_<mã phiên chạy>」`<br>2. Bấm xoá thư mục từ menu và xác nhận trên hộp thoại<br>3. Ghi lại mã trạng thái và thông báo<br>4. Tải lại trang, đối chiếu danh sách thư mục và danh sách mẫu tin<br>5. Mở thư mục `「残すフォルダ_<mã phiên chạy>」` và đối chiếu với ảnh chụp ban đầu<br>6. Kiểm tra nhóm mẫu tin đã bị xoá không còn xuất hiện ở màn chọn mẫu tin của kịch bản và bản tin gửi toàn bộ | Thư mục chứa 1 mẫu tin đơn + 1 nhóm (2 message con) | Thư mục biến mất khỏi danh sách. Mẫu tin đơn **và** nhóm bên trong đều bị xoá, **kể cả 2 message con của nhóm** (cascade). Thư mục `「残すフォルダ」` và mẫu tin bên trong **còn nguyên vẹn**, khớp ảnh chụp ban đầu. Nhóm đã xoá không còn xuất hiện ở màn chọn mẫu tin của kịch bản / gửi toàn bộ | | Lấp `G12` · Spec BR-05 — cascade **hard delete**, mất dữ liệu không phục hồi · `NEW-21` chỉ cover nhánh từ chối xoá `未分類` · regression — dẫn từ `TC-TMT-17` `TC-TMT-18` · Đánh giá spec: Spec ghi rõ (BR-05) · Evidence: ảnh danh sách trước/sau + kết quả truy vấn dữ liệu 2 thư mục |
| TC-FUNC001-04 | UI | FUNC-001 | Màn list template con | Normal | auto | Tất cả | Sửa nội dung message đã có rồi lưu — nội dung mới được ghi, liên kết cũ không bị mất | Đang đăng nhập bot test. Có 1 message văn bản đã lưu, bên trong có chèn 1 đường dẫn và 1 biến thông tin bạn bè. Message này đang được gắn vào 1 bước kịch bản. Đã ghi lại nội dung và liên kết hiện tại | 1. Mở màn sửa message văn bản đã chuẩn bị<br>2. Đổi nội dung văn bản, đổi đường dẫn sang một URL khác<br>3. Bấm lưu, ghi lại mã trạng thái và thông báo<br>4. Tải lại trang, mở lại message và đối chiếu nội dung<br>5. Mở bước kịch bản đang gắn message đó và đối chiếu nội dung hiển thị<br>6. Gửi thử message tới tài khoản tester | Nội dung văn bản mới + URL mới | Lưu thành công (mã 200). Sau khi tải lại: nội dung và đường dẫn là **giá trị mới**, biến thông tin bạn bè vẫn còn và vẫn thay thế đúng. Bước kịch bản hiển thị nội dung mới. Tin gửi thử mang nội dung mới. **Không** mất liên kết nào so với trước khi sửa | | Lấp `G16` `G14` · Spec BR-14 — update template xoá **TẤT CẢ** relationships cũ rồi tạo lại; nếu bước tạo lại hỏng thì mất dữ liệu âm thầm · `NEW-40` chỉ MỞ lại, không sửa + lưu · regression — dẫn từ `TC-TMT-77` `TC-TMT-105` · Đánh giá spec: Spec ghi rõ (BR-09, BR-14) · Evidence: ảnh nội dung sau khi tải lại + ảnh bước kịch bản + ảnh tin nhận được |
| TC-FUNC001-05 | UI | FUNC-001 | Sửa & xóa group template | Normal | auto | Tất cả | Nhân bản **nhóm** mẫu tin — bản sao có đủ message con và đứng đầu danh sách | Đang đăng nhập bot test. Có 1 nhóm mẫu tin `「コピー元_<mã phiên chạy>」` gồm 3 message con khác loại (văn bản, ảnh, panel/button), đã BẬT tuỳ chọn gửi giãn cách. Đã ghi lại thứ tự và nội dung 3 message con | 1. Mở màn danh sách mẫu tin, bấm 「コピー」 trên nhóm `「コピー元_<mã phiên chạy>」`<br>2. Ghi lại mã trạng thái và thông báo<br>3. Tải lại trang, quan sát vị trí bản sao trong danh sách<br>4. Mở bản sao và đối chiếu số lượng, thứ tự, nội dung 3 message con với bản gốc<br>5. Mở hộp thoại tuỳ chọn gửi của bản sao, đối chiếu trạng thái gửi giãn cách<br>6. Sửa `管理名` của bản sao rồi mở lại bản gốc để đối chiếu | Nhóm 3 message con khác loại, gửi giãn cách BẬT | Bản sao được tạo, nằm ở **đầu** danh sách. Bản sao có **đủ 3 message con, đúng thứ tự, đúng nội dung** từng loại, và giữ trạng thái gửi giãn cách BẬT. Sửa bản sao **không** làm đổi bản gốc | | Lấp `G19` · Spec BR-07 — deep clone **đệ quy** cho group, `position = min - 1` · `NEW-24` chỉ nhân bản mẫu tin đơn · regression — dẫn từ `TC-TMT-47` `TC-TMT-48` `TC-TMT-368` · Đánh giá spec: Spec ghi rõ (BR-07, BR-12) · Evidence: ảnh danh sách sau khi copy + ảnh 3 message con của bản sao |
| TC-BULK001-01 | UI | BULK-001 | Chuyển folder & xóa hàng loạt | Normal | auto | Tất cả | Xoá hàng loạt và di chuyển hàng loạt mẫu tin của chính mình — chạy đúng sau khi siết phạm vi bot | Đang đăng nhập bot test. Thư mục `「一括元_<mã phiên chạy>」` có 4 mẫu tin bỏ đi được. Có sẵn thư mục đích `「一括先_<mã phiên chạy>」` đang rỗng | 1. Mở màn danh sách, chọn thư mục `「一括元_<mã phiên chạy>」`<br>2. Tích chọn 2 mẫu tin, bấm 一括フォルダ変更, chọn thư mục đích rồi xác nhận<br>3. Tải lại trang, mở thư mục đích và đối chiếu<br>4. Quay lại thư mục nguồn, tích chọn 2 mẫu tin còn lại và bấm xoá hàng loạt, xác nhận<br>5. Tải lại trang và đối chiếu danh sách<br>6. Ghi lại mã trạng thái của cả 2 thao tác | 4 mẫu tin: 2 để di chuyển, 2 để xoá hàng loạt | Cả 2 thao tác đều thành công (mã 200), **không** bị chặn nhầm. 2 mẫu tin đã di chuyển nằm trong thư mục đích và không còn ở thư mục nguồn; số đếm của cả 2 thư mục cập nhật đúng. 2 mẫu tin đã xoá hàng loạt biến mất và vẫn mất sau khi tải lại | | Lấp `G20` · Câu xoá hàng loạt nhận thẳng danh sách id từ client + vừa thêm scope `bot_id` → phải chứng minh không chặn nhầm chính mình · Cặp đối chứng với `NEW-5` và `TC-DATADB001-01` · regression — dẫn từ `TC-TMT-66` `TC-TMT-71` · Đánh giá spec: Spec ghi rõ (Luồng 5, Luồng 6) · Evidence: ảnh 2 thư mục trước/sau + mã trạng thái 2 thao tác |
| TC-FUNCSEQ001-01 | UI | FUNC-SEQ-001 | Sắp xếp & ẩn folder | Normal | auto | Tất cả | Sắp xếp thư mục, ẩn thư mục và sắp xếp danh sách theo cột — cả 3 cơ chế giữ đúng sau khi tải lại | Đang đăng nhập bot test. Bot có ít nhất 4 thư mục và 1 thư mục chứa ≥ 3 mẫu tin với ngày tạo và `管理名` khác nhau. Đã ghi lại thứ tự thư mục hiện tại | 1. Mở hộp thoại 並べ替え của thư mục, kéo-thả đổi vị trí 1 thư mục rồi bấm lưu<br>2. Tải lại trang, đối chiếu thứ tự thư mục<br>3. Bật tuỳ chọn ẩn thư mục (フォルダを非表示), tải lại trang và quan sát khung thư mục<br>4. Mở 1 thư mục có ≥ 3 mẫu tin, sắp xếp theo cột 作成日 tăng rồi giảm<br>5. Sắp xếp theo cột 管理名 tăng rồi giảm<br>6. Ghi lại mã trạng thái của từng thao tác | 4 thư mục · 3 mẫu tin khác ngày tạo và khác tên | Đổi thứ tự thư mục lưu thành công và **giữ nguyên sau khi tải lại**. Ẩn thư mục hoạt động đúng, thư mục mặc định vẫn hiện. Sắp xếp theo 作成日 và 管理名 cho thứ tự đúng ở cả 2 chiều. **Không thao tác nào trả 422** dù đây là các action của cùng endpoint đa năng vừa được thêm kiểm tra | | Lấp `G17` `Q11` · EP-20 có 11 action, hiện chỉ 4 action có TC; Dev thêm "validate theo action" nên action nào cũng có thể bị chặn nhầm · regression — dẫn từ `TC-TMT-26` `TC-TMT-29` `TC-TMT-55` `TC-TMT-56` · Đánh giá spec: Spec ghi rõ (Luồng 3, SCR-TMT-01) · Evidence: ảnh khung thư mục và danh sách sau mỗi lần tải lại |
| TC-MEDIA001-01 | UI | MEDIA-001 | Media — image map | Normal | manual | product | Thư viện media và cài đặt image map mở được và lưu đúng sau khi sửa mã hiển thị lỗi | Đang đăng nhập bot test trên production. Thư viện ảnh của bot đã có sẵn ≥ 3 ảnh và ≥ 1 video. Bot không ở trạng thái đang sao lưu | 1. Mở màn soạn message loại 画像・動画・音声<br>2. Mở thư viện ảnh, chuyển trang và chọn 1 ảnh có sẵn<br>3. Mở thư viện video, chọn 1 video và đổi ảnh đại diện<br>4. Bật tuỳ chọn dùng image map, chọn một kiểu bố cục có sẵn và gắn hành động cho 1 vùng<br>5. Lưu, tải lại trang và mở lại message<br>6. Gửi thử tới tài khoản tester, bấm vào vùng đã gắn hành động trên LINE app | Ảnh và video có sẵn trong thư viện · 1 kiểu bố cục image map | Thư viện ảnh/video mở được, phân trang chạy đúng, chọn được file. Đổi ảnh đại diện video thành công. Image map lưu đúng số vùng và hành động đã gắn; sau khi tải lại vẫn đủ. Trên LINE app, bấm đúng vùng đã gắn thì hành động chạy | | Lấp `G13` `Q5` `Q6` · 7 endpoint nhóm 6.6 + 6.7 hiện **0 TC**, dù `media.js` nằm trong diff · RULE-08 + RULE-06 · manual vì môi trường production + thiết bị thật · regression — dẫn từ `TC-TMT-289` `TC-TMT-292` `TC-TMT-298` · Đánh giá spec: Spec ghi rõ (SCR-TMT-08, BR-11) · Evidence: ảnh thư viện + ảnh cài đặt image map sau khi tải lại + ảnh LINE app |
| TC-FUNC003-01 | UI | FUNC-003 | URL redirect — preview & metadata | Normal | manual | product | Lấy thông tin xem trước của một đường dẫn thật — hiển thị đủ tiêu đề, ảnh, mô tả | Đang đăng nhập bot test trên production. Có 2 URL công khai thật: 1 URL có đủ thẻ xem trước, 1 URL **không có** thẻ xem trước | 1. Mở màn soạn message văn bản, chèn URL thứ nhất vào nội dung<br>2. Bấm lấy thông tin xem trước, ghi lại mã trạng thái và nội dung hiển thị<br>3. Lặp lại với URL thứ hai (không có thẻ xem trước)<br>4. Lưu message và tải lại trang, đối chiếu thông tin xem trước còn giữ không<br>5. Gửi thử tới tài khoản tester và quan sát tin trên LINE app | URL có thẻ OGP đầy đủ · URL không có thẻ OGP | URL thứ nhất: trả mã 200, hiển thị đúng tiêu đề, ảnh và mô tả của trang. URL thứ hai: **không** báo lỗi hệ thống, hiển thị trạng thái không có thông tin xem trước một cách rõ ràng. Sau khi lưu và tải lại, thông tin xem trước vẫn đúng. Tin trên LINE app hiển thị khớp | | Lấp `G21` `Q6` · EP-53/54 gọi **ra domain ngoài** → RULE-08 · `NEW-60` chỉ test thiếu dữ liệu bắt buộc, 0 TC Normal · manual vì môi trường production + thiết bị thật · regression — dẫn từ nhóm `URL redirect — preview & metadata` của kho · Đánh giá spec: Spec ghi rõ (BR-09, EP-53/54) · Evidence: ảnh khối xem trước + ảnh LINE app |
| TC-COMPATLEGACY001-02 | API | COMPAT-LEGACY-001 | Template legacy & tương thích | Normal | auto | Tất cả | Đường ghi template đời cũ và endpoint lấy chi tiết theo đường dẫn vẫn chạy đúng | Đang đăng nhập bot test. Có 1 template đời cũ (tạo từ màn legacy, không thuộc nhóm) đã ghi lại mã và nội dung | 1. Mở màn tạo template đời cũ, nhập nội dung hợp lệ và lưu, ghi lại mã trạng thái<br>2. Mở màn sửa template đời cũ vừa tạo, đổi nội dung rồi lưu, ghi lại mã trạng thái<br>3. Gọi endpoint lấy chi tiết template theo đường dẫn với mã template hợp lệ, ghi lại mã trạng thái<br>4. Gọi lại endpoint đó với mã template của bot khác, ghi lại mã trạng thái<br>5. Tải lại màn danh sách và đối chiếu nội dung | Mã template hợp lệ của bot mình · mã template của bot khác | Bước 1-2: lưu thành công (mã 200), nội dung mới hiển thị đúng sau khi tải lại, liên kết cũ không mất. Bước 3: trả mã 200 kèm đúng dữ liệu template — **không trả 422** (đây là endpoint nhận mã qua đường dẫn, cùng dạng với lỗi đã xảy ra ở vòng fix 1). Bước 4: **không** trả về dữ liệu của bot khác | | Lấp `G14` `G15` · EP-21 / EP-22 / EP-26 hiện **0 TC**; EP-26 cùng shape path-param với `BUG-7` · Spec BR-03 + BR-14 · regression — dẫn từ `TC-TMT-340` `TC-TMT-345` · Đánh giá spec: Spec ghi rõ (§6.2) · Evidence: mã trạng thái 4 bước + ảnh nội dung sau khi tải lại |
| TC-DATADB001-02 | API | DATA-DB-001 | Folder template | Abnormal | auto | Tất cả | Xoá **thư mục của bot khác** — bị từ chối, toàn bộ mẫu tin bên trong của bot kia còn nguyên | Bot B có thư mục `「B_削除フォルダ_<mã phiên chạy>」` chứa 1 mẫu tin đơn và 1 nhóm (2 message con); đã ghi lại mã thư mục và ảnh chụp toàn bộ dữ liệu bên trong. Bot A có 1 thư mục bỏ đi được. Đang đăng nhập bot A | 1. Chọn bot A, xoá thư mục bỏ đi được của bot A để nắm hình dạng yêu cầu xoá thư mục<br>2. Trong công cụ dành cho nhà phát triển, gửi lại yêu cầu xoá thư mục nhưng thay mã thư mục bằng mã thư mục của bot B<br>3. Ghi lại mã trạng thái và thông báo<br>4. Đăng nhập lại bằng bot B, mở màn danh sách mẫu tin<br>5. Đối chiếu thư mục, mẫu tin đơn, nhóm và 2 message con với ảnh chụp ban đầu | Mã thư mục thật của bot B | Yêu cầu bị từ chối. Thư mục `「B_削除フォルダ_<mã phiên chạy>」` của bot B **vẫn còn**, mẫu tin đơn còn nguyên, nhóm còn nguyên và **vẫn đủ 2 message con**. Không bản ghi nào của bot B bị xoá hay đổi chủ sở hữu | | Lấp `G23` · Dev kê rõ "endpoint điều phối (**xoá thư mục**...)" thuộc danh sách siết `bot_id` nhưng 0/15 TC cross-tenant chạm đường này · Kết hợp BR-05 cascade hard delete → đường phá huỷ dữ liệu hàng loạt của bot khác · Đánh giá spec: Spec ghi rõ (BR-05) · Evidence: mã trạng thái + kết quả truy vấn dữ liệu bot B trước/sau |
| TC-REGSHARED001-01 | UI | REG-SHARED-001 | Gửi template từ màn khác | Normal | auto | Tất cả | Soạn và lưu message từ **5 màn dùng chung mà Dev KHÔNG kê** — luồng thành công không đổi | Đang đăng nhập bot test. Đã chuẩn bị sẵn: 1 kịch bản tự động trả lời, 1 rich menu có vùng gắn hành động, 1 tin nhắn chào mừng, 1 bạn bè để thao tác từ danh sách bạn bè, 1 trường thông tin bạn bè có cài đặt hành động | 1. Mở màn **tự động trả lời** (FA-003), thêm/sửa message qua màn soạn dùng chung rồi lưu, ghi lại mã trạng thái<br>2. Mở màn **rich menu** (FA-004), gắn hành động gửi mẫu tin và lưu, ghi lại mã trạng thái<br>3. Mở màn **tin nhắn chào mừng** (FA-007), chọn/sửa mẫu tin rồi lưu<br>4. Từ **danh sách bạn bè** (FA-013), dùng thao tác gửi mẫu tin<br>5. Mở **cài đặt hành động của thông tin bạn bè** (FA-015), chọn mẫu tin rồi lưu<br>6. Tải lại từng màn và đối chiếu nội dung đã lưu | Nội dung message hợp lệ ở mỗi màn | Cả 5 màn đều lưu thành công (mã 200), **không màn nào trả 422**, nội dung hiển thị đúng sau khi tải lại. Hành động gắn mẫu tin ở rich menu và thông tin bạn bè vẫn chạy đúng | | Lấp `G24` `G28` · [Spec §8](../../spec-features/admin/message-template/feature-spec.md) kê 8 tính năng tham chiếu SC-001/SC-005, Dev chỉ kê 3 · `t_actions` dùng chung bởi đúng nhóm màn này, mà helper chặn `action_id` chỉ vá 2 nơi gọi trong màn template · Đánh giá spec: Spec ghi rõ (§8) · Evidence: mã trạng thái + ảnh nội dung sau khi tải lại của cả 5 màn |
| TC-FUNC003-02 | API | FUNC-003 | Màn list template con | Normal | auto | Tất cả | Rule loại ngữ cảnh — **mọi giá trị hợp lệ** đều lưu được, không màn nào bị chặn nhầm | Đang đăng nhập bot test. Đã lấy được **danh sách đầy đủ giá trị `action_type` cho phép** từ Dev (yêu cầu Dev cung cấp trước khi chạy — xem `I7`) | 1. Với **từng giá trị** trong danh sách hợp lệ Dev cung cấp, gửi yêu cầu lưu message với nội dung hợp lệ và ghi lại mã trạng thái<br>2. Với mỗi giá trị, đối chiếu message có thật sự được tạo trong đúng ngữ cảnh đó không<br>3. Đối chiếu danh sách Dev cung cấp với các màn thực tế đang gọi màn soạn dùng chung (gửi toàn bộ, kịch bản, nhắc lịch, chat, tự động trả lời, rich menu, tin nhắn chào mừng, danh sách bạn bè, thông tin bạn bè)<br>4. Ghi lại mọi màn có trong danh sách màn nhưng **không** có giá trị tương ứng trong danh sách hợp lệ | Từng giá trị `action_type` trong danh sách hợp lệ | **Mọi** giá trị hợp lệ đều trả mã 200 và tạo được message đúng ngữ cảnh. **Không** giá trị nào trả 422. Mỗi màn đang gọi màn soạn dùng chung đều có một giá trị tương ứng trong danh sách hợp lệ — không màn nào bị bỏ sót | | Lấp `G25` `G24` · Rule `action_type in-list` là cơ chế **chặn**: giá trị thiếu trong danh sách thì màn đó chết · `NEW-52` chỉ test 2 giá trị KHÔNG hợp lệ, trong đó có `richmenu` — cần Dev xác nhận rich menu có thật sự dùng ngữ cảnh khác không · Đánh giá spec: Đã hỏi leader — phụ thuộc danh sách Dev cung cấp · Evidence: bảng giá trị × mã trạng thái |
| TC-SEC001-02 | API | SEC-001 | Sửa & xóa group template | Abnormal | auto | Tất cả | Đổi tên / đổi thư mục mẫu tin của bot khác qua endpoint lưu mẫu tin — bị từ chối | Bot B có 1 mẫu tin `「B_改名対象_<mã phiên chạy>」` nằm ở thư mục xác định; đã ghi lại mã, tên và thư mục. Đang đăng nhập bot A | 1. Chọn bot A, đổi tên một mẫu tin của bot A và đổi sang thư mục khác để nắm hình dạng yêu cầu lưu<br>2. Trong công cụ dành cho nhà phát triển, gửi lại yêu cầu lưu nhưng thay mã mẫu tin bằng mã của bot B, kèm tên mới và thư mục của bot A<br>3. Ghi lại mã trạng thái và thông báo<br>4. Đăng nhập lại bằng bot B và mở màn danh sách mẫu tin | Mã mẫu tin của bot B + tên mới + mã thư mục của bot A | Yêu cầu bị từ chối. Mẫu tin của bot B **giữ nguyên tên cũ**, vẫn nằm ở **thư mục cũ của bot B**, không bị kéo sang thư mục của bot A và không đổi chủ sở hữu | | Lấp `G27` · Dev kê "lưu mẫu tin nhắn (đổi tên/đổi thư mục)" thuộc danh sách siết bot, nhưng `NEW-1` chỉ cover `ajaxCreateGroup` — đây là endpoint khác · Đánh giá spec: Đã hỏi leader — cần Dev xác nhận mã trả về (404 hay 403) · Evidence: mã trạng thái + ảnh danh sách bot B sau khi chạy |
| TC-PERM003-02 | API | PERM-003 | Chuyển folder & xóa hàng loạt | Abnormal | auto | Tất cả | Di chuyển và sắp xếp mẫu tin của bot khác — **ghi nhận phạm vi carve-out của ticket #40979** | Bot B có 2 mẫu tin đã ghi lại mã, tên và thư mục. Bot A có ít nhất 2 thư mục. Đang đăng nhập bot A. **Đã đọc ghi chú**: ticket #40056 CỐ Ý không lọc bot ở 4 câu truy vấn này, phần đó thuộc ticket #40979 | 1. Chọn bot A, di chuyển một mẫu tin của bot A sang thư mục khác để nắm hình dạng yêu cầu<br>2. Gửi lại yêu cầu di chuyển với mã mẫu tin của bot B, ghi lại mã trạng thái<br>3. Làm tương tự với yêu cầu sắp xếp mẫu tin, ghi lại mã trạng thái<br>4. Đăng nhập bot B, đối chiếu thư mục và thứ tự 2 mẫu tin với ghi nhận ban đầu<br>5. Ghi lại kết quả quan sát được vào ticket #40979, **không raise ticket mới** | Mã mẫu tin của bot B | **TC này ghi nhận trạng thái, không phải cổng duyệt ticket #40056.** Kết quả quan sát được (bot B có bị đổi thư mục / đổi thứ tự hay không) phải được ghi vào ticket **#40979** làm mốc trước khi fix. Nếu dữ liệu bot B **bị đổi** → đúng như phạm vi #40979 đã biết, **KHÔNG** tính là bug mới của #40056 | | Lấp `G26` · Dev CỐ Ý không đụng 4 câu truy vấn này để tránh xung đột merge với #40979 · Mục đích: tránh raise ticket trùng và tạo điểm neo trước/sau cho #40979 · Đánh giá spec: Đã hỏi leader — phạm vi do Dev chốt ở journal #136618 · Evidence: mã trạng thái + ảnh dữ liệu bot B, đính kèm vào #40979 |
| TC-OUTTRUTH001-01 | API | OUT-TRUTH-001 | Màn list template | Abnormal | auto | Tất cả | Lỗi hệ thống ở **nhiều endpoint khác nhau** đều trả mã 500 sạch, không lộ nội dung ngoại lệ | Đang đăng nhập bot test. Có quyền dựng lỗi hệ thống (ngắt kết nối dữ liệu tạm thời hoặc dựng dữ liệu gây lỗi) cho từng endpoint | 1. Dựng lỗi hệ thống rồi gọi thao tác điều phối trên màn danh sách (xoá thư mục), ghi lại mã trạng thái và thân phản hồi<br>2. Dựng lỗi hệ thống rồi gọi nhân bản mẫu tin, ghi lại kết quả<br>3. Dựng lỗi hệ thống rồi gọi lưu message ở màn soạn, ghi lại kết quả<br>4. Gây một lỗi **không nằm trong danh sách dự kiến** (ví dụ dữ liệu vượt giới hạn tầng dưới) ở một trong ba endpoint trên, ghi lại kết quả<br>5. Đối chiếu thân phản hồi của cả 4 lần | 3 endpoint khác nhau + 1 lỗi ngoài dự kiến | Cả 4 lần đều trả mã lỗi hệ thống (500), **không lần nào trả 200**. Thân phản hồi chỉ chứa câu thông báo chung `「エラーが発生しました。」`, **không** chứa nội dung ngoại lệ, tên tệp, số dòng hay câu truy vấn. Màn hình hiện thông báo lỗi, không im lặng | | Lấp `I1` (AP-1 generic-fix) · Thay cho `NEW-64` vốn chỉ đọc source code, không chứng minh hành vi lúc chạy · Bắt buộc ≥3 trigger khác nhau + 1 trigger chưa biết · Đánh giá spec: Spec không ghi — hành vi do fix quy định · Evidence: ảnh mã trạng thái + thân phản hồi của cả 4 lần |

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | [message-template/feature-spec.md](../../spec-features/admin/message-template/feature-spec.md) §5 Business Rules — **BR-02** | Spec ghi nhánh đang sao lưu trả `{ status: false, message: MESSAGE_NOTIFY_BACKUP }` (tức HTTP 200). Fix đổi thành **409**. Spec phải ghi lại mã HTTP mới, nếu không mọi TC verify 409 đều rơi vào `Spec không ghi` | Fix #40056 vs spec BR-02 | Dev + PM |
| 2 | [message-template/feature-spec.md](../../spec-features/admin/message-template/feature-spec.md) §6 API Endpoints | Spec không mô tả mã HTTP của 40+ endpoint. Fix đã chuẩn hoá **409 / 422 / 400 / 404 / 500** cho 24 endpoint. Cần bổ sung bảng mã HTTP theo endpoint làm chuẩn đánh giá Expected | Fix #40056 vs spec §6 | Dev + Leader |
| 3 | [message-template/feature-spec.md](../../spec-features/admin/message-template/feature-spec.md) §Xác thực & phân quyền | Spec ghi "`getBotId()` → **tất cả** queries filter theo `bot_id`" — thực tế **trước fix thì không**, và **sau fix vẫn còn 4 câu truy vấn** ở nhánh di chuyển / sắp xếp mẫu tin chưa lọc (Dev cố ý để cho ticket **#40979**). Spec đang mô tả trạng thái lý tưởng, không phải trạng thái thật | Spec vs journal #136618 | Dev + Leader |
| 4 | Hành vi mới — lưu nhóm với mã nhóm không tồn tại | `NEW-15` chốt expected "tạo nhóm mới thay vì báo lỗi" (hệ quả của việc đổi `find($id)` → `where id + bot_id`). Không nguồn spec nào xác nhận đây là hành vi mong muốn — nếu người dùng gõ nhầm mã thì tạo ra nhóm rác thay vì báo lỗi | `NEW-15` vs spec (không có mục nào) | Dev + Leader |
| 6 | [message-template/feature-spec.md](../../spec-features/admin/message-template/feature-spec.md) SCR-TMT-03 — **giới hạn tên thư mục** | **3 nguồn cho 3 con số**: spec ghi **max 20** · `NEW-19` (thêm mới) chốt **20** · `NEW-33` (đổi tên) chốt **50** · kho `TC-TMT-07` `TC-TMT-08` chốt **15**. Cả `NEW-19` và `NEW-33` đều `Đạt` → cùng 1 field `category.name` đang validate khác nhau tuỳ đường ghi. Cần chốt 1 con số, sửa TC sai và cập nhật spec + kho | `DUP-CONFLICT §3 (NEW-19 vs NEW-33)` + `spec vs kho-tcs` | Dev + Leader |
| 5 | Hành vi có chủ ý — copy hành động chưa có chi tiết | Helper chặn `action_id` **cho qua** khi action chưa có `t_actions_detail` nào (vì `t_actions` không có `bot_id`). Đây là lỗ hổng cross-tenant còn lại **có chủ ý**. Cần ghi vào spec như một giới hạn đã biết, kèm điều kiện khai thác và mức rủi ro chấp nhận được | Journal #136618 vs spec §Xác thực | Dev + Leader |
