# 05 — Review Report

## Thông tin

| Trường | Giá trị |
|---|---|
| Bug ID | `#39372 — [05-08-2026] [TY-11730] [Error message] Badge lỗi gửi tin hiển thị dù không có nội dung lỗi` |
| Reviewer (Leader) | `<Leader điền>` (draft do `/review-tc` sinh) |
| Tester được review | TC hiện có trên Sheet `[AI]TCs_UI` (QA team) + TC `RV-xx` do AI Review bổ sung vòng trước |
| Ngày review | `2026-08-08` |
| Version TCs | Bản fetch từ Sheet, range `A285:Z312` (28 dòng) |
| Vòng review | `Round 1` |

**Input đã đọc**: `01-bug-task.md` ✔ · `02-spec-reference.md` ✘ (không có) · `03-dev-impact.md` ✔ · `04-tc-list.md` ✔

> **Spec reference**: dùng [templates/LME-SYSTEM-SPEC.md](../../templates/LME-SYSTEM-SPEC.md) tổng, không có spec riêng cho task này. Feature liên quan Dev ghi là `FA-028 Delivery Error List` và `FA-001 Chat 1:1`.

---

## 1. Verdict

- [ ] **APPROVED**
- [ ] **APPROVED WITH CHANGES**
- [x] **REJECTED** — có issue BLOCKER, cần bổ sung TC và review lại

**Lý do ngắn gọn**: Fix vòng 2 chạm **4 file / 6 điểm render badge / 2 sidebar**, nhưng bộ TC hiện có **chỉ phủ sidebar người dùng (`layout/v2/basic`)**. Ba vùng Dev sửa trực tiếp — **sidebar quản trị `admin_v2`**, **badge chat ghim ở お気に入り**, và **màn テンプレート (`addGroup`)** — **không có TC nào**. Ngoài ra fix là thay đổi **JS đã cache ở browser user** nhưng không có TC `DEPLOY-ASSET-001` (quan điểm ưu tiên Cao, trigger bắt buộc).

---

## 2. Tóm tắt cho member

Bộ TC bám rất sát **trục chính của bug**: khối dòng 304–312 kiểm đúng thứ cần kiểm — số tin chưa đọc của chat 1:1 và số lỗi gửi tin phải **độc lập nhau**, kể cả khi sidebar thu gọn. Khối 285–303 (gồm cả TC `RV-xx` từ vòng review trước) cho bộ regression tốt cho màn `送信エラー`, có cả đối chứng âm và cách ly đa-bot — đây là phần làm tốt.

Vấn đề chính: bộ TC được viết cho **phạm vi fix vòng 1** (chỉ sidebar người dùng). Fix vòng 2 (2026-08-07, commit `4a2c301858`) đã **mở rộng thêm 1 file và 2 vùng mới** mà TC chưa đuổi kịp — sidebar quản trị, badge ở mục お気に入り, và màn テンプレート. Cần bổ sung 4 nhóm TC ở §5 rồi review lại vòng 2.

Một điểm nữa cần chốt với Leader/PM trước khi test: sau fix, **badge 送信エラー không còn cập nhật realtime** (chỉ đúng khi tải lại trang). Dev nói đây không phải mất tính năng vì trước đó số realtime là số SAI — nhưng đây là **thay đổi hành vi user nhìn thấy được**, nên phải có người duyệt, không tự suy diễn rồi cho Đạt.

---

## 3. Coverage Matrix

> File 04 là bản fetch nguyên trạng từ Sheet human (không có cột `Mã quan điểm liên kết`) → map impact **suy luận 100%** từ nội dung cột `Sub Function / Tiêu đề`, `Nội dung check / Steps`, `Điều kiện / Data`, `Kết quả mong đợi`. TC dưới đây gọi theo **số dòng Sheet**.

| Impact | Loại | Priority | TCs map (suy luận) | # TC | Status |
|---|---|---|---|---|---|
| **BUG** — badge 送信エラー bị `setBadge` ghi đè bằng số hội thoại chưa đọc | Fix | — | 304, 305, 306, 307, 308, 309 | 6 | **RISK** — mọi TC đều ngầm giả định badge lỗi = **0**; không có TC nào chạy với **badge lỗi ≠ 0** (baseline thật) |
| **F1** — `setBadge()` `public/js/chats/chat-v2.js:5835` (đổi bộ chọn + cập nhật `badge-item`) | Function | Direct | 298, 303, 304–312 | 12 | **RISK** — thiếu nhánh favourite + sidebar admin (xem F2, F3) |
| **F2** — `layout/v2/basic/sidebar.blade.php` — 4 điểm render badge (`:146` favourite · `:172` chat · `:185` submenu · `:214` 送信エラー) | Function | Direct | `:214` → 285–293, 298, 301, 302, 303 · `:172` → 304–312 · `:146` → 294–297 (**chỉ badge 送信エラー**) | 20 | **RISK** — `:146` chỉ được test với badge **送信エラー**; **badge chat 1:1 ghim vào お気に入り không có TC** (đúng chỗ Dev bind `:data-badge-route="fmenu.route"`) |
| **F3** — `layout/admin_v2/sidebar.blade.php` — 2 badge chat 1:1 (`:424`, `:772`) | Function | Direct | — | **0** | **GAP** |
| **F4** — `public/js/msg_template/index.js` `addGroup()` (lấy `group_id` từ state Vue) | Function | Direct | — | **0** | **GAP** |
| **D1** — không có data bị chạm (Dev xác nhận 4.2 = "Không có") | Data | — | N/A | — | **N/A** — không cần TC data; đúng với `feedback_root_cause_layer_focus` (không đụng `message_error`) |
| **T1** — Delivery Error List (FA-028) | Feature | High | 285–303 | 19 | **OK** (4 dòng đang `NG` thuộc bug #38699 / #38701 — xem §4.2) |
| **T2** — Chat 1:1 (FA-001), gồm sidebar người dùng **và sidebar quản trị**, gồm cả khi ghim お気に入り | Feature | High | 304–312 (chỉ sidebar người dùng) | 9 | **RISK** — thiếu hoàn toàn nhánh **sidebar quản trị** và nhánh **ghim favourite** mà chính Dev nêu ở 4.3 |
| **T3** — Sidebar / menu お気に入り | Feature | Medium | 294, 295, 296, 297 | 4 | **RISK** — chỉ có badge 送信エラー; chưa có badge chat |
| **T4** — Sidebar thu gọn (chấm đỏ `badge-item`) | Feature | Medium | 302, 310, 311, 312 | 4 | **OK** — có cả chiều hiện (310, 312) và chiều tắt (311) |
| **T5** — Message Template (テンプレート) thêm / đổi tên thư mục | Feature | Medium | — | **0** | **GAP** |

### ORPHAN TCs

Không có TC nào **lạc chủ đề cần remove**. Nhưng có 2 nhóm cần **gắn nhãn lại** cho đúng phạm vi ticket:

| Dòng | Tiêu đề | Lý do | Hành động đề xuất |
|---|---|---|---|
| 285 | Check hiển thị các tab `未確認エラー / 再送登録済み / 再送済み履歴 / エラー原因一覧` | Không chạm code fix (chỉ mở màn) | **Giữ** — smoke của T1. Ghi `regression` ở Ghi chú |
| 286–290 | Badge hiện đúng 1 / 10 / 99 / 99+ theo type lỗi | Kiểm **số do server render** (`totalErrorMessage`) — Dev đã **revert**, không chạm code này (AP-5 nhẹ) | **Giữ** — vẫn cần vì fix đổi *phần tử nào* nhận số. Ghi `regression` ở Ghi chú |
| 292, 293, 296, 297 | Xóa / resend msg lỗi → giảm count | Thuộc bug **#38699**, đang `NG`, **không** do ticket này fix | **Giữ** — RULE-12 (case đã từng Không đạt). Ghi rõ "theo dõi ở #38699, không phải tiêu chí nghiệm thu #39372" |

---

## 3.5 Fix-shape analysis (adversarial)

| Mục | Giá trị |
|---|---|
| **Fix shape** (đọc mục 2 dev-impact) | **Kết hợp 3 shape**: (1) **Sửa hàm dùng chung** — `setBadge` đổi từ bộ chọn class dùng chung `.badge-span` sang bộ chọn theo tên riêng `[data-badge-route="chatBasicV3"]`; (2) **JS / asset** — sửa 2 file `public/js/**` + 2 blade; (3) **Dọn nguồn dữ liệu** — `addGroup()` bỏ truy vấn DOM toàn trang `$('.active')`. **KHÔNG** phải generic catch-all / validation / race-condition / migration / soft-delete |
| **Trigger space cần cover** | **6 điểm render badge** Dev đã đặt tên + **1 điểm ghi** (`setBadge`) + **1 điểm dọn DOM query**: `v2/basic:146` (favourite, bind động) · `v2/basic:172` (chat) · `v2/basic:185` (submenu — hiện không menu nào khai báo badge) · `v2/basic:214` (送信エラー) · `admin_v2:424` (chat) · `admin_v2:772` (chat) · `setBadge` (chấm đỏ `badge-item` khi thu gọn) · `msg_template addGroup` |
| **Số trigger TCs hiện cover** | **4 / 8** — có: `v2/basic:172`, `v2/basic:214`, `v2/basic:146` (**chỉ nhánh badge 送信エラー**, chưa nhánh chat), `badge-item` thu gọn. **Thiếu**: `admin_v2:424`, `admin_v2:772`, `v2/basic:146` nhánh chat, `msg_template addGroup`. (`v2/basic:185` — chấp nhận × vì hiện không menu nào khai báo badge, Dev đã ghi rõ) |
| **KH report dạng** | **Có root cause khá cụ thể** — KH nêu đúng dấu hiệu quyết định: *"số lượng lỗi tăng theo tỷ lệ với số tin nhắn chưa đọc"* + *"mở ra không có nội dung lỗi"*. Đủ để chốt root cause, KHÔNG phải symptom-only thuần |
| **Alternative root causes cần verify** | **1 cái — chưa được fix này giải thích**: tin nhắn KH gửi ở lần trao đổi **thứ 1** nói *"dù tin nhắn đã gửi thành công nhưng vẫn bị phân vào mục tin nhắn lỗi"*. Nếu account KH **thực sự có bản ghi** trong `未確認エラー`, đó là root cause **thứ hai** hoàn toàn khác (ghi nhầm bản ghi lỗi), fix này không chạm. Dev khẳng định *"dữ liệu `message_error` vốn không có bản ghi nào"* nhưng **KHÔNG verify được** (DB dev Connection refused) → phải xác nhận trên production account KH. Xem `[MAJOR] SYMPTOM-ONLY` ở §4.2 |
| **Anti-patterns dính** | **AP-3** (happy-path-only regression: T3 chỉ 1 chiều, T5 = 0 TC) · **AP-5 mức nhẹ** (dòng 286–290 test lớp server-render Dev đã revert — giữ làm regression, không remove). **KHÔNG dính**: AP-1 (fix không phải generic catch), AP-2 (KH có nêu dấu hiệu root cause), AP-4 (có branch + commit `4a2c301858`), AP-6 (mục 3 điền đủ 13 dòng + có quét ngang toàn repo) |

> **Kết luận fix-shape**: trigger space cover **4/8** → flag `[BLOCKER] FIX-SHAPE` ở §4.1.
>
> Điểm cộng cho Dev: mục 3 có **quét ngang toàn repo cùng họ lỗi** (84 chỗ `querySelectorAll` theo class, xác định duy nhất `setBadge` ghi ở phạm vi `document`) — đây là đúng tinh thần `REG-SHARED-001`, giúp review khoanh vùng chắc chắn. Vấn đề còn lại **không phải Dev thiếu danh sách**, mà là **TC chưa test hết danh sách Dev đã đưa**.

---

## 4. Issues phát hiện

### 4.1 Blocker (phải fix trước khi merge)

- **[BLOCKER] FIX-SHAPE / GAP-1** — `F3` (`layout/admin_v2/sidebar.blade.php` :424, :772) là impact **Direct** nhưng **không có TC nào** trong range 285–312 chạm sidebar **màn quản trị**. Dev đã sửa trực tiếp 2 badge chat 1:1 ở đây và 4.3-T2 ghi rõ *"trên CẢ sidebar người dùng lẫn sidebar quản trị"*. Nếu `data-badge-route` đặt sai/thiếu ở đây thì badge chat 1:1 của admin **im lặng không cập nhật** — đúng rủi ro Dev tự nêu. → Bổ sung **TC-REGSHARED001-01/02/03** (§5).
- **[BLOCKER] FIX-SHAPE / GAP-2** — `F4` (`public/js/msg_template/index.js` `addGroup()`) + `T5` (màn テンプレート) là impact **Direct** nhưng **0 TC**. Đây là thay đổi **thứ 4** chỉ xuất hiện ở báo cáo vòng 2 (2026-08-07) — bộ TC hiện có được viết trước đó nên chưa biết tới. Thao tác **Thêm / Đổi tên thư mục template** là chức năng user dùng hằng ngày. → Bổ sung **TC-FUNC001-01/02/03** (§5).
- **[BLOCKER] GAP-3** — `F2` điểm `:146` + `T2` (High): **không có TC nào cho badge chat 1:1 khi menu 1:1チャット được ghim vào お気に入り**. Bốn dòng 294–297 chỉ ghim **送信エラー** — mà badge đó do server render, **không** đi qua `setBadge`. Nhánh Dev thực sự sửa (`:data-badge-route="fmenu.route"`, bind động theo route) **chưa được chạm tới lần nào**. Dev còn tự nêu mục favourite có `v-if="favBadge(...) > 0"` là điểm yếu sẵn có. → Bổ sung **TC-DATA001-01/02/03** (§5).
- **[BLOCKER] GAP-4 / `DEPLOY-ASSET-001`** (quan điểm ưu tiên **Cao**, trigger **BẮT BUỘC** khi release sửa file JS/CSS) — fix sửa **2 file JS trong `public/js/`** mà user đang giữ bản cache. **Không có TC nào** kiểm luồng **F5 thường** (KHÔNG Ctrl+F5) trên browser còn cache bản cũ, cũng không có TC kiểm Network tab. Rủi ro cụ thể: blade mới đã render `data-badge-route` nhưng browser vẫn chạy `chat-v2.js` cũ quét `.badge-span` → **bug tái diễn y hệt với chính KH đã khiếu nại**, trong khi QA test trên máy sạch lại thấy Đạt. → Bổ sung **TC-DEPLOYASSET001-01/02** (§5).

### 4.2 Major (nên fix)

- **[MAJOR] SYMPTOM-ONLY** — Lần trao đổi **thứ 1** của KH mô tả một triệu chứng **khác** mà root cause này **không giải thích được**: *"dù tin nhắn đã gửi thành công nhưng vẫn bị phân vào mục tin nhắn lỗi"*. KH có đính chính ở lần 2, nhưng Dev **chưa verify được** khẳng định *"`message_error` không có bản ghi nào"* (DB dev không kết nối được). Nếu tab `未確認エラー` của account KH thực tế **có** bản ghi → là root cause thứ hai, fix này không chạm. → Bổ sung **TC-ENV003-01/02** (§5) chạy trên **PRODUCTION** account KH; nếu tab có bản ghi → escalate ticket riêng, **không** đóng #39372 bằng kết quả staging.
- **[MAJOR] `01-bug-task.md`** — Auto-filled `2026-08-08 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" CHƯA tick**. Yêu cầu tester đọc lại detail Redmine #39372 (11 journal, 1 attachment, custom field `Commit Date`) và tick trước khi review này có giá trị nghiệm thu.
- **[MAJOR] `03-dev-impact.md`** — Auto-filled `2026-08-08 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" CHƯA tick**. Đặc biệt quan trọng ở task này vì Redmine có **2 vòng báo cáo Auto-fixbug** (`data-badge-menu` ở vòng 1 → `data-badge-route` ở vòng 2, 3 file → 4 file). Tester phải xác nhận đang review đúng **vòng 2** và branch QA checkout đúng commit `4a2c301858`.
- **[MAJOR] BUG root cause thiếu chiều — baseline ≠ 0** — Toàn bộ TC 304–309 ngầm chạy với **badge lỗi = 0** (dòng 305 ghi rõ *"Check màn error không có số msg nào"*). Case nguy hiểm hơn **chưa có**: bot **đang có lỗi gửi tin thật** (badge = 5) rồi friend gửi tin đến — badge phải **giữ nguyên 5**, không bị đè thành số chưa đọc và cũng không bị **reset về 0**. Cover "giữ nguyên khi = 0" chưa chứng minh được "giữ nguyên khi ≠ 0". → **TC-OUTTRUTH001-02** (§5).
- **[MAJOR] `OUT-TRUTH-001` + thay đổi hành vi chưa được duyệt** — Dev ghi rõ: *"Badge 送信エラー nay chỉ cập nhật khi tải lại trang"*. Đây là **thay đổi user nhìn thấy được** so với trước fix. Không TC nào ghi nhận / xác nhận hành vi này, và không TC nào có cột `Trạng thái đánh giá spec`. Nguy cơ đúng cảnh báo của RULE: tester tự suy diễn rồi cho Đạt. → **TC-OUTTRUTH001-01** (§5) với `Trạng thái đánh giá spec = Spec không ghi` + bắt buộc ghi **đã hỏi ai**. Xem thêm §6.
- **[MAJOR] `DATA-COUNT-001`** (Cao — lỗi lặp nhiều nhất lịch sử: 12 ticket Closed) — Dòng 286–290 có đối chiếu badge với **số lỗi seed theo type**, nhưng **không dòng nào** đối chiếu badge với **số dòng thật hiển thị trên tab `未確認エラー`**. Mà đây **chính là tiêu chí nghiệm thu của KH**: "badge hiện số nhưng mở ra rỗng". → **TC-DATACOUNT001-01/02/03** (§5).
- **[MAJOR] `PERM-001`** (Cao) — Dòng 300 (RV-05, staff không được cấp quyền) đang **`NG`** kèm Bug #38701. Nhánh render **no-permission** nằm **cùng vòng lặp render badge** mà fix vừa sửa (`v2/basic/sidebar.blade.php`) → bắt buộc **chạy lại sau fix**, không được coi là "đã biết, bỏ qua". → **TC-PERM001-01/02/03** (§5).
- **[MAJOR] Format file 04 không phải TC list canonical** — File 04 là **bản fetch nguyên trạng** từ sheet human `[AI]TCs_UI`, thiếu toàn bộ `TC No.` · `Mã quan điểm liên kết` · `Loại case` · `Kết quả thực thi` · `Môi trường test` · `Trạng thái đánh giá spec`. Hệ quả thực tế cho review này: **toàn bộ map impact và map quan điểm phải suy luận từ văn bản tự do**, độ tin cậy thấp hơn hẳn. Ngoài ra khối 304–312 để **trống cột `Status` (J)**, chỉ có cột `Bug KH #39372` (K) = `OK` → không rõ đó là kết quả của lần chạy nào. → Đề nghị: chạy `/write-tc` sinh bộ canonical cho vòng 2, hoặc bổ sung tối thiểu `Mã quan điểm liên kết` + `Loại case` + `Trạng thái đánh giá spec` cho 28 dòng hiện có.
- **[MAJOR] `AP-3` — T3 chỉ có chiều thuận** — `T3` (menu お気に入り) có 4 TC nhưng đều là badge 送信エラー ở trạng thái sạch. Không có TC ghim/bỏ ghim menu **trong lúc** có badge, không có TC ghim **2 menu cùng có badge**. → gộp vào **TC-DATA001-03** (§5).

### 4.3 Minor (có thể fix sau)

- **[MINOR] RULE-02** — Không dòng nào ghi **loại evidence bắt buộc**. Với task này evidence tối thiểu phải là: **video/GIF** badge thay đổi realtime (ảnh tĩnh không chứng minh được realtime) + **screenshot toàn sidebar** (chứng minh đối chứng âm) + **screenshot DevTools Network** cho nhóm `DEPLOY-ASSET-001`.
- **[MINOR] Môi trường test** — Mọi dòng đều trỏ `staging.lme.jp`, không dòng nào ghi `PRODUCTION`. Task chạm **asset JS đã cache** và bug xảy ra **trên account thật của KH** → theo `ENV-003` / Catalog D khối `ENV-ASSET` phải có ít nhất 1 TC production (đã đưa vào §5).
- **[MINOR] Lỗi chính tả làm giảm độ rõ của TC** — dòng 306 `"tư động"`, 308 `"màn erro"`, 309 `"chec"`, 310 `"confiirm"`, 311 `"chec"`. Hai người đọc có thể hiểu khác nhau ở dòng 309 (`"chec ở chat 1:1 line user đang có nhiều số msg chưa confirm"` + `"Xóa line user"`) — không rõ xóa friend hay xóa hội thoại.
- **[MINOR] Điều kiện tiền đề trống** ở phần lớn dòng 291–312 — thiếu account, số lượng data seed, và **quan trọng nhất: đang đứng ở màn nào**. Bug này **chỉ xảy ra khi đang ở màn chat 1:1** (nơi `chat-v2.js` được nạp) — không dòng nào ghi điều kiện đó.

### 4.4 Nit (gợi ý)

- **[NIT]** Dòng 286–290 kiểm lớp `totalErrorMessage` mà Dev đã **revert** (không chạm code). Giữ lại là đúng (fix đổi *phần tử nào* nhận số), nhưng nên ghi `regression` ở Ghi chú để vòng review sau không nhầm là TC verify fix.
- **[NIT]** `CONC-003` (race tầng giao diện) — có thể thêm 1 TC: friend gửi **5 tin trong 2 giây** trong lúc user chuyển nhanh giữa các menu → badge cuối cùng khớp số thật. Ưu tiên thấp vì fix làm bộ chọn **hẹp hơn**, không mở rộng bề mặt race.
- **[NIT] RULE-11** — Không dùng §4 `checklist-lme.md` (FORM-01, CHAT-01, ADM-01/03/04, TPL-01) để chặn task này; `CHAT-01` (đồng bộ tin loại đặc biệt) có vẻ liên quan chat 1:1 nhưng **chưa đủ bằng chứng** → chỉ ghi nhận, không flag.
- **[NIT]** Nếu muốn badge 送信エラー **thật sự realtime**, Dev đã chỉ sẵn đường: trường `totalErrorMessage` có trong `InfoEvent` nhưng **chưa có JS nào lắng nghe** (code chết). Đề nghị Leader mở **ticket riêng**, không nhét vào #39372.

---

## 5. TCs đề xuất bổ sung

> Member copy thẳng vào `04-tc-list.md` ở round tiếp theo. Bảng dùng **đúng 16 cột canonical**.
> Trước khi chạy: checkout branch `ai_fixbug_39372` commit `4a2c301858` (**4 file** — nếu thấy `data-badge-menu` thay vì `data-badge-route` là đang ở commit cũ vòng 1).

| TC No. | Mã quan điểm liên kết | Loại case | Tiêu đề test case | Điều kiện tiền đề | Các bước thực hiện | Dữ liệu test/input | Kết quả mong đợi | Kết quả thực thi | Evidence thực tế | Môi trường test | Người thực hiện | Ngày thực hiện | Số ticket bug | Trạng thái đánh giá spec | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-REGSHARED001-01 | REG-SHARED-001 | Normal | Sidebar QUẢN TRỊ (admin_v2): badge menu 1:1チャット cập nhật realtime khi friend gửi tin | - Bot test có ≥ 1 friend thật trên LINE app.<br>- Đăng nhập account owner, đang mở **màn quản trị** (sidebar admin).<br>- Tất cả hội thoại đã confirm hết → badge chat = 0 (không hiện).<br>- Đã deploy branch `ai_fixbug_39372`. | 1. Mở màn quản trị, để nguyên trang, **KHÔNG reload**.<br>2. Từ LINE app thật, friend gửi 1 tin cho OA.<br>3. Nhìn badge cạnh menu 1:1チャット trên sidebar admin.<br>4. Friend gửi thêm 2 tin nữa.<br>5. Nhìn lại badge. | 1 friend · gửi 1 tin, sau đó gửi 2 tin.<br>Phép tính tay: badge kỳ vọng `1` sau bước 2, `3` sau bước 4. | Sau bước 3 badge menu 1:1チャット hiện **`1`**; sau bước 5 hiện **`3`** — cập nhật ngay, **không cần reload**. Mọi menu khác trên sidebar admin **không mọc badge**. | Chưa test | | STAGING | | | | Spec ghi rõ | Lấp **GAP-1** — impact **F3** (`layout/admin_v2/sidebar.blade.php` :424, :772) hiện 0 TC. Evidence bắt buộc: **video/GIF** badge tăng realtime + screenshot **toàn sidebar admin**. `regression` |
| TC-REGSHARED001-02 | REG-SHARED-001 | Abnormal | Sidebar QUẢN TRỊ: tin đến dồn dập KHÔNG làm menu khác trên sidebar admin mọc badge (đối chứng âm) | - Như TC-REGSHARED001-01.<br>- Ghi lại ảnh sidebar admin ở trạng thái badge = 0 để đối chiếu. | 1. Mở màn quản trị, không reload.<br>2. Friend gửi **5 tin liên tiếp trong ~10 giây**.<br>3. Rà **từng mục** menu trên sidebar admin từ trên xuống, so với ảnh chụp ban đầu.<br>4. Thu gọn sidebar admin, rà lại các chấm đỏ. | 5 tin liên tiếp từ 1 friend. | Chỉ menu 1:1チャット có badge (`5`). **Tất cả mục còn lại không có badge, không có chấm đỏ mới.** Không lỗi đỏ trong Console. | Chưa test | | STAGING | | | | Spec ghi rõ | Lấp **GAP-1** — đối chứng âm cho sidebar admin (tương đương dòng 298/303 nhưng ở sidebar chưa được test). Evidence: screenshot sidebar trước/sau + Console. |
| TC-REGSHARED001-03 | REG-SHARED-001 | Boundary | Sidebar QUẢN TRỊ: badge chat về 0 sau khi confirm hết → badge biến mất, không hiện số `0` | - Sidebar admin đang hiện badge chat = 3. | 1. Sang màn chat 1:1, confirm **hết** các hội thoại chưa đọc.<br>2. Quay lại màn quản trị, **không reload**, nhìn badge.<br>3. Thu gọn sidebar, kiểm chấm đỏ.<br>4. F5 rồi kiểm lại. | Từ 3 → 0 hội thoại chưa đọc. | Badge menu 1:1チャット **ẩn hoàn toàn** (không hiển thị chữ `0`). Chấm đỏ khi thu gọn cũng mất. Sau F5 vẫn giữ trạng thái ẩn. | Chưa test | | STAGING | | | | Spec ghi rõ | Lấp **GAP-1** + kiểm nhánh `badge-item` (thay đổi số 3 của fix) trên sidebar admin. Evidence: screenshot mở + thu gọn, trước và sau F5. |
| TC-FUNC001-01 | FUNC-001 | Normal | テンプレート: thêm thư mục mới lưu vào **đúng thư mục đang chọn** sau khi `addGroup` bỏ truy vấn DOM | - Đăng nhập account có quyền テンプレート.<br>- Màn テンプレート đang có ≥ 2 thư mục, đang chọn thư mục thứ 2. | 1. Mở màn テンプレート (`/basic/message-template`).<br>2. Chọn thư mục thứ 2 trong danh sách.<br>3. Bấm thêm thư mục mới, nhập tên, Lưu.<br>4. Quan sát danh sách thư mục.<br>5. F5 rồi kiểm lại. | Tên thư mục: `TC39372_フォルダ_01` | Thư mục mới được tạo và nằm **đúng vị trí ứng với thư mục đang chọn**, tên hiển thị đúng nguyên văn. Sau F5 vẫn đúng, không nhảy sang nhánh khác, không lỗi đỏ trong Console. | Chưa test | | STAGING | | | | Spec ghi rõ | Lấp **GAP-2** — impact **F4** + **T5** hiện 0 TC. Evidence: screenshot danh sách thư mục trước / sau / sau F5. |
| TC-FUNC001-02 | FUNC-001 | Abnormal | テンプレート: đổi tên thư mục khi header có popup chứa tab `利用規約` → vẫn lưu đúng thư mục, không văng lỗi | - Như trên.<br>- **Điều kiện then chốt**: đây đúng là phần tử gây lỗi cũ (`$('.active')` quét trúng tab `利用規約` của popup header và trả `undefined`). | 1. Mở màn テンプレート.<br>2. Mở popup ở header có tab `利用規約` rồi đóng lại (để popup đã render trong DOM).<br>3. Chọn 1 thư mục bất kỳ trong danh sách.<br>4. Đổi tên thư mục đó, Lưu.<br>5. F5 kiểm lại tên thư mục và danh sách template bên trong. | Tên cũ → `TC39372_リネーム後` | Tên **đúng thư mục vừa chọn** được đổi. **Không** tạo nhầm thư mục mới, **không** đổi nhầm thư mục khác, **không** báo lỗi. Danh sách template bên trong giữ nguyên. Sau F5 vẫn đúng. | Chưa test | | STAGING | | | | Spec ghi rõ | Lấp **GAP-2** — TC nhắm **thẳng** điều kiện gây lỗi của thay đổi số 4 trong mục 2 dev-impact. Evidence: video thao tác + screenshot sau F5. |
| TC-FUNC001-03 | FUNC-001 | Boundary | テンプレート: đổi tên thư mục **đầu** và **cuối** danh sách, tên dài sát maxlength | - Màn テンプレート có ≥ 3 thư mục.<br>- Tester đọc **maxlength thực tế** trên ô nhập tên và ghi lại vào cột Dữ liệu test. | 1. Đổi tên thư mục **đầu tiên** thành chuỗi dài **đúng maxlength**.<br>2. Lưu, F5, kiểm hiển thị.<br>3. Lặp lại với thư mục **cuối cùng**.<br>4. Thử nhập **maxlength + 1** ký tự. | Chuỗi full-width JP dài đúng maxlength (tester ghi số thực tế) và maxlength+1. | Cả thư mục đầu và cuối đều đổi **đúng cái được chọn**. Tên dài đúng maxlength **không vỡ layout** danh sách. Nhập maxlength+1 → bị chặn theo đúng hành vi hiện có của field. | Chưa test | | STAGING | | | | Spec không ghi | Lấp **GAP-2**. `Spec không ghi` maxlength → tester **ghi rõ đã hỏi ai** trước khi kết luận Đạt (RULE-01/RULE-03). Evidence: screenshot 2 đầu danh sách + số ký tự đã nhập. |
| TC-DATA001-01 | DATA-001 | Normal | Ghim 1:1チャット vào お気に入り: badge chưa đọc cập nhật realtime ở **cả** mục favourite lẫn mục menu chính | - Sidebar người dùng (`v2/basic`), đã ghim menu 1:1チャット vào お気に入り.<br>- **Đang có sẵn ≥ 1 hội thoại chưa đọc lúc tải trang** (để phần tử badge favourite tồn tại).<br>- Đang mở màn chat 1:1. | 1. Tải lại trang, xác nhận badge hiện ở **cả 2 chỗ**: mục お気に入り và mục menu chính.<br>2. **Không reload**, cho friend gửi thêm 2 tin.<br>3. So sánh số ở 2 chỗ.<br>4. Confirm 1 hội thoại, so lại. | Bắt đầu 1 hội thoại chưa đọc → +2 → confirm 1.<br>Phép tính tay: `1 → 3 → 2`. | Badge ở **mục favourite** và **mục menu chính** luôn hiện **cùng một số** và cùng cập nhật realtime (`1 → 3 → 2`). Badge 送信エラー **không đổi** trong suốt quá trình. | Chưa test | | STAGING | | | | Spec ghi rõ | Lấp **GAP-3** — impact **F2 `:146`** (`:data-badge-route="fmenu.route"`) + **T2/T3**. Evidence: **video** quay cả 2 vị trí badge trong 1 khung hình. |
| TC-DATA001-02 | DATA-001 | Abnormal | Ghim 1:1チャット vào favourite khi số chưa đọc = 0 lúc tải trang → có tin mới đến: xác nhận hành vi badge favourite | - Đã ghim 1:1チャット vào お気に入り.<br>- **Confirm hết** hội thoại trước khi tải trang → badge favourite = 0 lúc render.<br>- Dev đã nêu đây là hạn chế **có sẵn từ trước** (`v-if="favBadge(...) > 0"`), không phải hồi quy của fix. | 1. Confirm hết, F5.<br>2. Không reload, cho friend gửi 1 tin.<br>3. Quan sát badge ở mục favourite **và** mục menu chính.<br>4. F5, quan sát lại. | 0 → 1 hội thoại chưa đọc. | **Mục menu chính**: badge hiện `1` ngay, không cần reload. **Mục favourite**: ghi nhận hành vi thực tế (theo Dev là chưa hiện cho tới khi reload); sau F5 **bắt buộc** hiện `1`. Không được hiện **số sai**. | Chưa test | | STAGING | | | | Spec không ghi | Lấp **GAP-3** + AP-3. Đây là hành vi Dev **chủ động khai báo là hạn chế cũ** → tester **không tự kết luận Đạt/Không đạt**, ghi rõ **đã hỏi ai** và đối chiếu nhánh `ai_small_38691`. Evidence: video + screenshot sau F5. |
| TC-DATA001-03 | DATA-001 | Boundary | Ghim **đồng thời** 1:1チャット và 送信エラー vào favourite, bot có **cả** tin chưa đọc **và** lỗi gửi tin → 2 badge độc lập | - Ghim cả 2 menu vào お気に入り.<br>- Bot có **3 lỗi gửi tin thật** (badge 送信エラー = 3).<br>- Có **2 hội thoại chưa đọc**. | 1. F5, xác nhận mục favourite hiện 2 badge: chat = `2`, 送信エラー = `3`.<br>2. Không reload, friend gửi thêm 4 tin.<br>3. Đọc lại **cả 2** badge ở favourite và ở menu chính.<br>4. Thu gọn sidebar, kiểm chấm đỏ của **từng** mục. | 3 lỗi gửi tin (seed sẵn) + 2 → 6 hội thoại chưa đọc.<br>Phép tính tay: chat `2 → 6`; 送信エラー giữ **`3`**. | Badge chat lên `6` ở cả 2 vị trí. Badge 送信エラー **giữ nguyên `3`** ở cả 2 vị trí — **không** nhảy theo `6`, **không** về `0`. Khi thu gọn, chấm đỏ xuất hiện đúng ở **cả 2** mục, không lẫn. | Chưa test | | STAGING | | | | Spec ghi rõ | Lấp **GAP-3** + là **case đối chứng mạnh nhất** của root cause (2 badge cùng sống cạnh nhau ở cùng vùng render). Evidence: video 1 khung hình chứa cả 2 badge, mở + thu gọn. |
| TC-DEPLOYASSET001-01 | DEPLOY-ASSET-001 | Normal | Sau deploy, **F5 thường** trên browser còn cache bản cũ → nạp `chat-v2.js` mới, badge 送信エラー không bị ghi đè | - Browser **đã mở màn chat 1:1 TRƯỚC khi deploy** (đã cache `chat-v2.js` bản cũ).<br>- Bot có **2 lỗi gửi tin thật**.<br>- Deploy branch fix xong. | 1. Trên chính browser đó, bấm **F5 thường** (**KHÔNG** Ctrl+F5).<br>2. Mở DevTools > Network, lọc `chat-v2.js` và `msg_template/index.js`.<br>3. Kiểm status + query version của 2 file.<br>4. Cho friend gửi 3 tin, quan sát badge 送信エラー và badge chat. | 2 lỗi gửi tin có sẵn + 3 tin đến. | Network: cả 2 file JS trả **200 kèm query version mới**, **không 404**, không lấy từ cache bản cũ. Badge chat lên `3`; badge 送信エラー **giữ nguyên `2`**. | Chưa test | | PRODUCTION | | | | Spec ghi rõ | Lấp **GAP-4** — `DEPLOY-ASSET-001` (Cao, trigger bắt buộc khi sửa JS). Ghi `PRODUCTION` theo **RULE-08 / Catalog D `ENV-ASSET`** (user production giữ cache lâu). **Không có Boundary** cho quan điểm này — cache chỉ có 2 trạng thái cũ/mới (**RULE-01**, đã ghi lý do). Evidence: **screenshot DevTools Network** (query version + 200) + video badge. |
| TC-DEPLOYASSET001-02 | DEPLOY-ASSET-001 | Abnormal | Tab mở sẵn từ TRƯỚC deploy, **không reload** → ghi nhận hành vi badge với JS cũ + xác nhận không lỗi Console | - Mở màn chat 1:1 trước khi deploy và **giữ nguyên tab**, không đụng vào.<br>- Bot có 2 lỗi gửi tin thật. | 1. Deploy branch fix (blade mới đã có `data-badge-route`).<br>2. Trên tab cũ **không reload**, cho friend gửi 3 tin.<br>3. Quan sát badge 送信エラー.<br>4. Mở Console kiểm lỗi đỏ.<br>5. F5 thường rồi kiểm lại badge. | 2 lỗi có sẵn + 3 tin đến. | Ghi nhận hành vi thực tế của tab cũ (JS cũ quét `.badge-span` nên badge lỗi **có thể vẫn bị đè** — đây là giới hạn đã biết của cache). Bắt buộc: **không exception đỏ** trong Console, và **sau F5 badge trở về đúng `2`**. Nếu sau F5 vẫn sai → **Không đạt**. | Chưa test | | PRODUCTION | | | | Spec không ghi | Lấp **GAP-4** — case "client cũ + server mới" (liên quan `DEPLOY-LIVE-001`). Hành vi tab-chưa-reload **chưa có spec** → tester ghi rõ **đã hỏi ai**, và Leader quyết có cần thông báo KH F5 sau release không. Evidence: video tab cũ + Console + screenshot sau F5. |
| TC-OUTTRUTH001-01 | OUT-TRUTH-001 | Normal | Lỗi gửi tin phát sinh khi user đang ở màn chat → badge 送信エラー chỉ đúng **sau khi tải lại trang** (xác nhận thay đổi hành vi) | - Bot đang có **0 lỗi** gửi tin.<br>- User đang mở màn chat 1:1, **không reload**.<br>- Có cách tạo 1 lỗi gửi tin thật (theo cách dòng 308 đang dùng). | 1. Ở màn chat 1:1, tạo **1 lỗi gửi tin thật**.<br>2. **Không reload** — quan sát badge 送信エラー.<br>3. F5 — quan sát lại badge.<br>4. Mở tab `未確認エラー`, đếm số dòng. | 1 lỗi gửi tin. | Bước 2: badge **không tăng** (hành vi mới sau fix — Dev xác nhận badge lỗi chỉ render từ server). Bước 3: sau F5 badge hiện **`1`**. Bước 4: tab `未確認エラー` có **đúng 1 dòng**, khớp badge. | Chưa test | | STAGING | | | | Spec không ghi | Lấp **MAJOR `OUT-TRUTH-001`** — thay đổi hành vi user nhìn thấy được. `Spec không ghi` → **bắt buộc ghi đã hỏi ai (Leader/PM)** trước khi cho Đạt; xem §6. Evidence: video từ lúc tạo lỗi → F5 + screenshot tab `未確認エラー`. |
| TC-OUTTRUTH001-02 | OUT-TRUTH-001 | Abnormal | **Root cause với baseline ≠ 0**: bot đang có 5 lỗi thật, friend gửi 10 tin liên tiếp → badge 送信エラー vẫn đúng `5` | - Bot có **đúng 5 lỗi gửi tin chưa xác nhận** (seed trước, xác nhận badge = 5 sau khi F5).<br>- User đang mở **màn chat 1:1** (điều kiện then chốt — JS chat chỉ chạy ở đây).<br>- Không reload trong suốt bài test. | 1. F5, ghi nhận badge 送信エラー = `5`, badge chat = `0`.<br>2. Cho friend gửi **10 tin liên tiếp**.<br>3. Sau mỗi ~3 tin, đọc **cả 2** badge.<br>4. Mở tab `未確認エラー`, đếm số dòng.<br>5. F5, đọc lại badge 送信エラー. | 5 lỗi seed + 10 tin đến.<br>Phép tính tay: chat `0 → 10`; 送信エラー **giữ `5`** xuyên suốt. | Badge chat tăng dần tới `10`. Badge 送信エラー **giữ nguyên `5`** ở mọi thời điểm — **không** nhảy thành `10`, **không** về `0`, **không** thành `15`. Tab `未確認エラー` có **đúng 5 dòng**. Sau F5 badge vẫn `5`. | Chưa test | | STAGING | | | | Spec ghi rõ | Lấp **MAJOR baseline ≠ 0** — cover **BUG** ở chiều mà dòng 304–309 bỏ sót (chúng chỉ chạy với badge lỗi = 0). Đây là **TC quan trọng nhất của cả bộ**. Evidence: **video liên tục** quay cả 2 badge từ đầu tới cuối. |
| TC-OUTTRUTH001-03 | OUT-TRUTH-001 | Boundary | Bot có đúng **1** lỗi và đúng **1** tin chưa đọc → 2 badge cùng hiện `1` nhưng độc lập; confirm tin → chat về 0, lỗi vẫn `1` | - Bot có **đúng 1 lỗi** gửi tin chưa xác nhận.<br>- Có **đúng 1 hội thoại chưa đọc**.<br>- Đang ở màn chat 1:1. | 1. F5, xác nhận cả 2 badge cùng hiện `1`.<br>2. Confirm hội thoại đó.<br>3. Đọc lại 2 badge (không reload).<br>4. F5, đọc lại. | 1 lỗi + 1 hội thoại chưa đọc. | Sau bước 1: 2 badge cùng `1`. Sau bước 3: badge chat **ẩn hẳn** (không hiện `0`), badge 送信エラー **vẫn `1`**. Sau F5 giữ nguyên trạng thái đó. | Chưa test | | STAGING | | | | Spec ghi rõ | Biên nguy hiểm nhất: 2 badge **trùng giá trị** nên lỗi ghi đè sẽ **không nhìn thấy được** nếu chỉ nhìn số → phải kiểm ở bước confirm. Evidence: screenshot 3 mốc (trước / sau confirm / sau F5). |
| TC-DATACOUNT001-01 | DATA-COUNT-001 | Normal | Badge 送信エラー khớp **số dòng thật** trên tab `未確認エラー` (tiêu chí nghiệm thu của KH) | - Bot có lỗi gửi tin ở **nhiều tính năng khác nhau** (chat 1:1, broadcast, ステップ配信).<br>- Đã F5 để badge lấy số mới nhất. | 1. F5, đọc số trên badge 送信エラー, ghi lại.<br>2. Click vào menu 送信エラー.<br>3. Ở tab `未確認エラー`, **đếm tay** số dòng (qua hết các trang nếu có phân trang).<br>4. So 2 số. | 7 lỗi: 2 chat 1:1 + 3 broadcast + 2 ステップ配信.<br>Phép tính tay: `2+3+2 = 7`. | Badge hiện **`7`** và tab `未確認エラー` có **đúng 7 dòng**. Hai số khớp tuyệt đối. | Chưa test | | STAGING | | | | Spec ghi rõ | Lấp **MAJOR `DATA-COUNT-001`** — dòng 286–290 đối chiếu badge với **số seed**, chưa dòng nào đối chiếu badge với **số dòng hiển thị**. Đây đúng là điều KH khiếu nại. Evidence: screenshot badge + screenshot danh sách đã đếm. |
| TC-DATACOUNT001-02 | DATA-COUNT-001 | Abnormal | **Tái hiện đúng case của KH**: bot 0 lỗi + đang có nhiều tin chưa đọc → badge 送信エラー ẩn hẳn và tab `未確認エラー` rỗng | - Bot **không có bản ghi lỗi gửi tin nào**.<br>- Có **8 hội thoại chưa đọc**.<br>- Đang mở màn chat 1:1, không reload. | 1. Cho friend gửi tin tới khi có 8 hội thoại chưa đọc.<br>2. Không reload — đọc badge 送信エラー.<br>3. Click menu 送信エラー.<br>4. Kiểm tab `未確認エラー`. | 0 lỗi + 8 hội thoại chưa đọc. | Badge 送信エラー **không hiển thị** (không hiện `8`, không hiện `0`). Tab `未確認エラー` hiển thị **trạng thái rỗng đúng**, không màn trắng. **Đây chính là hiện tượng KH báo — phải hết sau fix.** | Chưa test | | STAGING | | | | Spec ghi rõ | Lấp **MAJOR `DATA-COUNT-001`** + là TC **reproduce trực tiếp** khiếu nại của KH. Mạnh hơn dòng 305 vì ép số chưa đọc lên cao (KH nói số lỗi *tăng tỷ lệ* với tin chưa đọc). Evidence: video + screenshot tab rỗng. |
| TC-DATACOUNT001-03 | DATA-COUNT-001 | Boundary | Badge tại mốc **99 / 100** khớp số dòng thật trên tab `未確認エラー` | - Seed được **99** rồi **100** lỗi gửi tin chưa xác nhận. | 1. Seed 99 lỗi, F5, đọc badge.<br>2. Vào tab `未確認エラー`, đếm số dòng (qua hết phân trang).<br>3. Seed thêm 1 lỗi (tổng 100), F5, đọc badge.<br>4. Đếm lại số dòng. | 99 → 100 lỗi. | Ở 99: badge hiện **`99`**, danh sách có **99 dòng**. Ở 100: badge hiện **`99+`**, danh sách có **đúng 100 dòng** (badge rút gọn nhưng danh sách **không được cắt bớt**). | Chưa test | | STAGING | | | | Spec ghi rõ | Lấp **MAJOR `DATA-COUNT-001`**. Chồng một phần với dòng 288/289 (chỉ kiểm badge) — TC này thêm chiều **đối chiếu danh sách**, chống lỗi "giới hạn ngầm số bản ghi" (Catalog B `UIC-08`). Evidence: screenshot badge + trang cuối danh sách. |
| TC-PERM001-01 | PERM-001 | Normal | Staff **CÓ** quyền 送信エラー: mục menu hiện badge số đúng sau khi thêm `data-badge-route` | - Bot có **4 lỗi gửi tin thật**.<br>- Có account staff **được cấp quyền** vào màn 送信エラー.<br>- Đăng nhập bằng account staff đó. | 1. Đăng nhập staff có quyền, F5.<br>2. Đọc badge mục 送信エラー trên sidebar.<br>3. Click vào menu → đếm số dòng tab `未確認エラー`.<br>4. Cho friend gửi 3 tin, kiểm badge 送信エラー không đổi. | 4 lỗi + 3 tin đến. | Badge mục 送信エラー hiện **`4`**, click vào được, danh sách có 4 dòng. Sau khi có 3 tin đến, badge 送信エラー **vẫn `4`**. | Chưa test | | STAGING | | | | Spec ghi rõ | Lấp **MAJOR `PERM-001`** — nhánh **có quyền** trong cùng vòng lặp render badge vừa sửa. Evidence: screenshot sidebar + danh sách, chụp bằng account staff. |
| TC-PERM001-02 | PERM-001 | Abnormal | Staff **KHÔNG** được cấp quyền 送信エラー: mục menu render nhánh no-permission, **không lộ số lỗi** (chạy lại dòng 300 / #38701) | - Bot có **4 lỗi gửi tin thật**.<br>- Account staff **KHÔNG** được cấp quyền màn 送信エラー.<br>- Đã deploy branch fix. | 1. Đăng nhập staff không quyền, F5.<br>2. Xem mục 送信エラー trên sidebar.<br>3. Thử click vào mục đó.<br>4. Ghim mục đó vào お気に入り (nếu ghim được) rồi xem lại.<br>5. Thu gọn sidebar, kiểm chấm đỏ. | 4 lỗi (staff không được biết con số này). | Mục 送信エラー hiển thị **trạng thái no-permission**, **KHÔNG hiện badge số `4`** ở bất kỳ đâu (menu chính, favourite, chấm đỏ khi thu gọn) và **không click vào được**. | Chưa test | | STAGING | | | | Spec ghi rõ | Lấp **MAJOR `PERM-001`** — **chạy lại dòng 300 (RV-05) đang `NG` / Bug #38701**, bắt buộc theo **RULE-12**. Nhánh này nằm cùng vòng lặp render badge vừa sửa. Evidence: screenshot sidebar mở + thu gọn + favourite, bằng account staff. `regression` |
| TC-PERM001-03 | PERM-001 | Boundary | Staff bị **thu hồi quyền** 送信エラー khi đang mở sẵn trang → sau F5 badge và menu biến mất | - Staff đang đăng nhập, **đang có quyền**, badge 送信エラー hiện `4`.<br>- Owner ở máy khác thu hồi quyền của staff đó. | 1. Owner thu hồi quyền màn 送信エラー của staff.<br>2. Ở phiên staff, **không reload**, cho friend gửi 2 tin, quan sát 2 badge.<br>3. F5 ở phiên staff.<br>4. Xem lại mục 送信エラー và badge. | 4 lỗi + 2 tin đến, thu hồi quyền giữa chừng. | Sau F5: mục 送信エラー về **trạng thái no-permission**, badge số `4` **biến mất**. Badge chat 1:1 vẫn hoạt động bình thường (`2`). | Chưa test | | STAGING | | | | Spec không ghi | Lấp **MAJOR `PERM-001`** ở chiều biên (đổi quyền giữa phiên — liên kết `PERM-004`). `Spec không ghi` hành vi phiên chưa reload → ghi rõ **đã hỏi ai**. Evidence: screenshot phiên staff trước/sau F5. |
| TC-ENV003-01 | ENV-003 | Normal | **PRODUCTION — account KH**: sau deploy, friend gửi tin → badge 送信エラー không tăng, tab `未確認エラー` rỗng | - Account KH thật (LOA `@196kuwhr`).<br>- ⚠️ **Đã báo trước tên account test cho KH** — yêu cầu riêng của KH ghi trong Redmine #39372.<br>- Fix đã lên production. | 1. Đăng nhập production account KH, mở màn chat 1:1.<br>2. Từ account test đã báo trước, add friend và gửi 3 tin.<br>3. **Không reload**, quan sát badge 送信エラー và badge chat.<br>4. Click menu 送信エラー, kiểm tab `未確認エラー`. | 3 tin từ 1 friend test đã được KH duyệt. | Badge chat lên `3`. Badge 送信エラー **không xuất hiện / không tăng**. Tab `未確認エラー` **rỗng**. Hiện tượng KH báo **không còn tái hiện**. | Chưa test | | PRODUCTION | | | | Spec ghi rõ | Lấp **MAJOR SYMPTOM-ONLY** + **RULE-08** (không kết luận từ staging cho asset/production). **Bắt buộc báo trước tên account cho KH.** Evidence: video trên production + screenshot tab rỗng. |
| TC-ENV003-02 | ENV-003 | Abnormal | **PRODUCTION — đối soát root cause thứ 2**: nếu tab `未確認エラー` của KH thực sự **có** bản ghi → không thuộc fix này, escalate | - Như TC-ENV003-01.<br>- Bối cảnh: KH ở lần trao đổi **thứ 1** nói *"tin nhắn gửi thành công vẫn bị phân vào mục tin nhắn lỗi"* — Dev **chưa verify được** vì DB dev không kết nối. | 1. Trên production account KH, F5 sạch.<br>2. Mở menu 送信エラー, rà **đủ 4 tab**: `未確認エラー` / `再送登録済み` / `再送済み履歴` / `エラー原因一覧`.<br>3. Ghi lại số dòng của **từng tab**.<br>4. Nếu `未確認エラー` có dòng → mở chi tiết, đối chiếu với lịch sử gửi tin xem tin đó **thực tế đã gửi thành công hay chưa**. | Không seed data — đọc dữ liệu thật của KH. | Kỳ vọng: `未確認エラー` **rỗng** → khẳng định của Dev đúng, đóng được #39372. **Nếu có dòng và tin đó thực tế đã gửi thành công** → root cause **thứ hai**, **KHÔNG** đóng #39372 bằng fix này, raise ticket riêng. | Chưa test | | PRODUCTION | | | | Đã hỏi leader | Lấp **MAJOR SYMPTOM-ONLY** (AP-2 biến thể). **Không** đề xuất TC đào sâu logic `totalErrorMessage` / `MessageErrorController` vì Dev **đã revert**, không chạm code đó — TC này chỉ **quan sát dữ liệu thật** để quyết định có escalate hay không. Evidence: screenshot đủ 4 tab trên production. |

---

## 6. Spec update needed

- [ ] Không cần update spec
- [x] **Cần chốt spec** — chi tiết:
  - **Section**: `LME-SYSTEM-SPEC` — `FA-028 Delivery Error List` (hành vi badge trên sidebar).
  - **Nội dung cần chốt**: Sau fix, badge `送信エラー` **không còn cập nhật realtime**, chỉ đúng ở thời điểm **tải lại trang** (server render từ `totalErrorMessage`). Trước fix nó "có realtime" nhưng bằng **con số sai**. Cần văn bản hóa: *badge 送信エラー là số tại thời điểm tải trang, không phải realtime* — nếu không, vòng review sau hoặc KH khác sẽ báo lại đúng chỗ này như một bug mới.
  - **Hệ quả vận hành cần Leader quyết**: lỗi gửi tin phát sinh trong lúc user đang mở màn chat sẽ **không được thông báo** cho tới khi user chuyển màn / F5. Có cần thông báo cho CS/KH không?
  - **Người chịu trách nhiệm update**: `<Leader / PM điền>`
  - **Liên quan**: Dev đề xuất nếu muốn realtime thật thì dùng trường `totalErrorMessage` sẵn có của `InfoEvent` (hiện **không có JS nào lắng nghe** — code chết) → **tách ticket riêng**, không gộp vào #39372.

---

## 7. Checklist đã chạy

- [x] **A. Coverage** — A.1 `RISK` (thiếu baseline ≠ 0) · A.2 **FAIL** (F3, F4 = GAP) · A.3 N/A (không có data impact) · A.4 **FAIL** (T5 = GAP, T2/T3 = RISK) · A.5 pass (không có orphan cần remove) · A.6 **FAIL** (trigger space 4/8)
- [x] **B. Chất lượng từng TC** — B.1 **FAIL** (điều kiện tiền đề trống ở phần lớn dòng; thiếu điều kiện then chốt "đang ở màn chat 1:1") · B.2 pass · B.3 pass · B.4 pass
- [x] **C. Chất lượng bộ TC** — không phân loại được `Normal/Abnormal/Boundary` vì file 04 **không có cột `Loại case`**; phân bố quan điểm lệch hẳn về `T1` (19/28 dòng)
- [x] **D. Spec alignment** — không có `02-spec-reference.md`; phát hiện **1 thay đổi hành vi cần chốt spec** (§6)
- [x] **E. Hành chính** — **FAIL**: không có `TC No.`, file 04 chưa điền `Tester viết TCs` / `Ngày submit` / `Version TCs`
- [x] **F. Base quan điểm test LME** — [checklist-lme.md](../../framework/checklist-lme.md) + [catalog-lme.md](../../framework/catalog-lme.md)
  - [x] F.1 Quan điểm (tầng 1) — bảng dưới
  - [x] F.2 Catalog (tầng 2) — **B** `UIC-02` (badge/nút), `UIC-13` (độ phân giải), `UIC-15` (asset/icon) · **C** `MAP-PERM-01/02` (ma trận staff, đường truy cập favourite) · **D** `ENV-ASSET` (cache asset production), `ENV-LB` (2 server) · A/E **không áp dụng** (không có ô nhập mới, không chạm media)
  - [x] F.3 RULE — **RULE-01** vi phạm (không phân loại được loại case) · **RULE-02** vi phạm (không ghi loại evidence) · **RULE-08** vi phạm (mọi TC staging, task chạm asset production) · **RULE-12** một phần (dòng 300 `NG` chưa được đưa vào phạm vi chạy lại)

### F.1 — Bảng quan điểm đối chiếu

| Mã quan điểm | Ưu tiên | Trigger khớp task? | TC cover (suy luận) | Kết luận |
|---|---|---|---|---|
| `FUNC-001` — luồng chính đúng đặc tả | Cao | ◯ — mọi chức năng | 286–291, 304–312 | **RISK** — không phân loại được 3 loại case (thiếu cột `Loại case`); màn テンプレート (T5) không có TC → xem `[BLOCKER] GAP-2` |
| `FUNC-SEQ-001` — thao tác liên tiếp & reload (F5) | Trung bình | ◯ — badge 送信エラー nay **phụ thuộc hẳn vào reload** | Chỉ dòng 299 nhắc *"điều hướng lại để sidebar render lại"* | **RISK** — không dòng nào ghi bước **F5** tường minh. Đã đưa F5 vào hầu hết TC ở §5 |
| `CONC-003` — race tầng giao diện | Trung bình | ◯ nhẹ — `setBadge` gọi từ socket + 3 điểm ajax | — | **GAP** → chỉ nêu `[NIT]`: fix làm bộ chọn **hẹp hơn**, không mở rộng bề mặt race |
| `DATA-001` — cập nhật phản ánh đủ ở mọi màn liên quan | Cao | ◯ — 1 con số phải đúng ở 6 điểm render × 2 sidebar | 304–312 (1 sidebar) | **GAP** ở nhánh favourite-chat và sidebar admin → `[BLOCKER] GAP-1`, `[BLOCKER] GAP-3` |
| `DATA-COUNT-001` — số đếm & tổng hợp | Cao | ◯ **BẮT BUỘC** — badge là con số đếm | 286–290 (badge vs số seed) | **RISK** — thiếu đối chiếu badge ↔ **số dòng thật** trên danh sách → `[MAJOR]` |
| `DATA-DB-001` — WHERE scope, khóa mồ côi | Cao | **×** | — | **×** — 4.2 xác nhận **không có data bị chạm**; fix thuần thuộc tính HTML + bộ chọn JS. **Lý do đã ghi (RULE-03)** |
| `DATA-AUDIT-001` — lịch sử thao tác | Cao | **×** | — | **×** — không có thao tác xóa/sửa dữ liệu nhạy cảm |
| `PERM-001` — menu/nút hiện đúng theo role | Cao | ◯ — badge nằm trong vòng lặp render menu có phân quyền | 300 (**đang `NG`**) | **RISK** → `[MAJOR]`, bắt buộc chạy lại (RULE-12) |
| `PERM-002` — không bypass quyền qua API/URL | Cao | ◯ một phần — `MAP-PERM-02` (favourite, URL trực tiếp) | 300 (chỉ tầng UI) | **RISK** → gộp vào `TC-PERM001-02`. Không mở rộng rà API sibling vì bug này **không phải bug phân quyền** |
| `PERM-003` — cách ly đa tài khoản LINE OA | Cao | ◯ — badge đếm theo bot đang chọn | 301 (RV-06, `OK`) | **OK** |
| `OUT-TRUTH-001` — UI khớp trạng thái THẬT | Cao | ◯ **BẮT BUỘC** — đây đúng là bản chất bug (badge hiện số không có thật) | 304–309 | **RISK** — chỉ chạy với baseline = 0; thay đổi hành vi (mất realtime) chưa được duyệt → `[MAJOR]` ×2 |
| `UI-001` — responsive theo surface | Trung bình | ◯ nhẹ | 302 (thu gọn) | **RISK** — thiếu `1366×768` (`UIC-13`). Ưu tiên thấp: fix **không đổi CSS**, chỉ thêm thuộc tính HTML |
| `UI-002` — khác biệt trình duyệt | Trung bình | **×** | — | **×** — bộ chọn `[data-badge-route=...]` là CSS selector chuẩn, hỗ trợ phổ quát; **không** có khai báo CSS mới (Dev đã grep xác nhận `.badge-span` không có style nào). **Lý do đã ghi (RULE-03)** |
| `REG-SHARED-001` — shared code / logic | Cao | ◯ **BẮT BUỘC** — sửa đúng 1 hook dùng chung cho mọi badge | 298, 303 (đối chứng âm, 1 sidebar) | **GAP một phần** — Dev **đã cung cấp danh sách** đầy đủ (mục 3), nhưng TC **chưa test hết** danh sách đó → `[BLOCKER] GAP-1`, `[BLOCKER] GAP-2` |
| `REG-RUN-001` — job/dữ liệu chạy dở khi release | Cao | **×** | — | **×** — fix thuần frontend (blade + JS), không chạm job hay dữ liệu đang chạy dở |
| `DEPLOY-ASSET-001` — version asset JS/CSS | Cao | ◯ **BẮT BUỘC** — release sửa **2 file JS** trong `public/js/` | — | **GAP** → `[BLOCKER] GAP-4` |
| `DEPLOY-LIVE-001` — release không lock maintain | Cao | ◯ một phần — tab cũ + blade mới | — | **GAP** → gộp vào `TC-DEPLOYASSET001-02` (không đổi payload API nên rủi ro thấp hơn) |
| `COMPAT-LEGACY-001` — dữ liệu/định dạng đời cũ | Cao | **×** | — | **×** — 2 sidebar đời cũ (`layout/basic`, `layout/admin`) **không được `@include` ở đâu** (màn chết), Dev đã xác nhận và **cố ý không đụng**. **Lý do đã ghi (RULE-03)** |
| `ENV-003` — khác biệt dev/staging/production | Cao | ◯ **BẮT BUỘC** — cache asset (`ENV-ASSET`) + bug xảy ra trên account production của KH | — | **GAP** → `[BLOCKER] GAP-4` + `[MAJOR] SYMPTOM-ONLY` |
| `SYNC-APP-001` — đồng bộ web ⇔ app | Trung bình | **×** | — | **×** — fix chỉ chạm blade + JS của **web**; không có thay đổi phía mobile app. **Lý do đã ghi (RULE-03)** |
| `MSG-001` / `MSG-004` — đối tượng nhận tin, preview vs thật | Cao | **×** | — | **×** — không chạm logic gửi tin, chỉ chạm hiển thị badge. **Lý do đã ghi (RULE-03)** |
| `JOB-001` — job nền / batch | Cao | **×** | — | **×** — không thêm/sửa job. Dòng 308 (job send lỗi) giữ làm **regression** cho T1 |
| `LIST-001` — search/filter/pagination | Trung bình | ◯ nhẹ — `UIC-08` giới hạn ngầm số bản ghi | — | **RISK** → đã gộp vào `TC-DATACOUNT001-03` (đếm hết phân trang) |
| `CHAT-01` (§4 — chưa đủ bằng chứng) | — | — | — | **Chỉ `[NIT]`** — theo **RULE-11** không dùng để flag BLOCKER/MAJOR |

**Tổng kết F.1**: 4 quan điểm **GAP** (`DATA-001` nhánh favourite/admin · `REG-SHARED-001` một phần · `DEPLOY-ASSET-001` · `ENV-003`) — cả 4 đều **ưu tiên Cao** → **4 BLOCKER**. 6 quan điểm **RISK** → **MAJOR**. 8 quan điểm đánh **×** đều đã ghi lý do (RULE-03).

---

## 8. Ký duyệt

| Người | Tên | Ngày |
|---|---|---|
| Reviewer (Leader) | | |
| Tester | (đã đọc & hiểu feedback) | |
