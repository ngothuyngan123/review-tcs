# 05 — Review Report

## 0. Nguồn TC

- **Nguồn đã dùng**: MCP LME TEST STUDIO — task #179 (ticket 37645, round 1, branch `ai_fixbug_37645`, `reviewState = leader`, `reviewed = false`)
- **Tổng số TC review**: 16

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: 6/15 vùng ảnh hưởng đủ TC · **4 GAP** · **5 RISK**

| # | Chiều | Vùng ảnh hưởng | TC hiện có | Status | Vấn đề |
|---|---|---|---|---|---|
| **G1** | dev-impact | `BUG` root cause — gỡ quyền thật trên trang tài khoản Google rồi mở lại màn | TC-TOOLKNOW002-01 | **RISK** | TC **duy nhất** tái hiện đúng bug khách báo, nhưng `last_exec = null` (**chưa chạy**). 12 TC "Đạt" còn lại đều dựng bằng **sửa token trong DB**, không phải gỡ quyền thật → chưa có bằng chứng nào xác nhận bug gốc đã hết. |
| **G2** | diff code | `LandingGoogleSheetService::isConnectionRevoked` — nhánh bắt `ClientException` để kết luận "đã gỡ quyền" | TC-OUTTRUTH001-01, TC-TOOLOLDREC001-01 | **GAP** `[BLOCKER]` | Fix là **generic catch theo class exception** (`ClientException` = mọi lỗi 4xx từ Guzzle), nhưng TC chỉ trigger **1 điều kiện** duy nhất: refresh token bị thu hồi (Google trả 400 `invalid_grant`). Các lỗi 4xx khác (403 vượt hạn mức/quota, 401, 400 `invalid_client` khi app bị xoá hoặc secret đổi) **sẽ bị báo nhầm là "đã gỡ quyền"**. Xem `[AP-1]` §4. |
| **G3** | diff code | Bản ghi liên kết ở **`status = 4`** — nhánh vừa hiển thị modal, vừa hủy liên kết | *(không TC nào)* | **GAP** | Spec [FA-017 BR-21 / R-17](../../spec-features/admin/qr-landing/feature-spec.md): `landing_connect_google.status = 4` chiếm **8/20 dòng = 40% dữ liệu thật**, **không** nằm trong hằng số model và **không** bị chặn hủy liên kết. TC-STATECLEAN001-02 chỉ test `status` 0/1/3. Đây là trạng thái **phổ biến nhất ngoài production** mà bộ TC bỏ trắng. |
| **G4** | dev-impact | `cancelGoogleSheet` — xóa `google_sheet_id` của **toàn bộ** landing thuộc bot, **kể cả bản xóa mềm** | TC-STATECLEAN001-01 | **GAP** | Spec **BR-22** ghi rõ `withTrashed`. TC-STATECLEAN001-01 chỉ yêu cầu "ít nhất 1 QR đang gắn khóa spreadsheet" — **không** kiểm QR đã xóa mềm (trong thùng rác). Sót → QR khôi phục lại sẽ mang `google_sheet_id` trỏ sheet của tài khoản Google cũ. |
| **G5** | diff code | Nhánh **liên kết lại** (`google_sheet_auth_url_reconnect` → EP-52) | TC-OUTTRUTH001-03 *(skip)*, TC-FUNC001-01 *(chưa chạy)* | **RISK** | REQ-003 risk **High** nhưng **cả 2 TC đều không có kết luận**. Đây chính là phần fix thêm mới (biến `google_sheet_auth_url_reconnect` tách khỏi `google_sheet_auth_url`) → đang là vùng **hoàn toàn chưa được kiểm chứng**. |
| **G6** | diff code | Nhánh **lỗi mạng** khi gọi Google (fail-safe "coi như vẫn kết nối") | TC-ENV001-01 | **RISK** | TC đang **Không đạt**, đã raise bug **#40936**. Chưa có TC xác nhận lại sau khi #40936 được fix, và chưa rõ #40936 ảnh hưởng ngược tới nhánh phát hiện revoked hay không. |
| **G7** | diff code | URL liên kết lại trỏ vào **EP-52** — callback nhận `state` = `bot_id` | TC-OUTTRUTH001-03 *(skip)* | **GAP** | Spec **R-13**: EP-52 nhận `state` từ query mà **không đối chiếu bot đang đăng nhập**. Fix tạo thêm **một entry point mới** vào chính luồng này. TC-OUTTRUTH001-03 chỉ kiểm URL mang **đúng** `bot_id`, không TC nào thử **sửa** `state` sang bot khác. |
| **G8** | dev-impact | Output cuối chuỗi — dữ liệu quay lại chảy vào spreadsheet sau khi liên kết lại | *(không TC nào)* | **RISK** `RULE-06` | Mục đích cuối của việc liên kết lại là sheet chạy tiếp. TC-FUNC001-01 dừng ở "token cập nhật + hết cảnh báo" (Studio ghi lý do: job 3–5 phút, ngoài phạm vi fix). Hợp lý cho phạm vi diff, nhưng **chưa ai xác nhận giá trị cuối cho khách**. |
| **G9** | diff code | Toàn bộ vùng chạm **URL / domain** (redirect URI dựng từ domain request vs domain trong biến môi trường) | 16/16 TC | **RISK** `RULE-08` | **100% TC chỉ chạy ở `local`** (1 run, by `pipeline`). `dev` / `staging` / `prd` = **0 run**. Chính note của TC-OUTTRUTH001-03 cảnh báo hai domain có thể lệch nhau — đây đúng là thứ local không phát hiện được. |

> **Vùng đã đủ TC (không ghi chi tiết)**: modal hiển thị đúng mẫu · nền màn giữ nguyên khi bị gỡ quyền · 2 nhánh đối chứng âm (liên kết còn hiệu lực / chưa liên kết) · cách ly hiển thị giữa 2 bot · chỉ-đọc không ghi đè token · biên hạn token với bản ghi thiếu refresh token.
>
> **Không tính GAP** — `LandingGoogleSheetService::insertDataToGoogleSheet` (luồng ghi dữ liệu vào sheet khi quét QR): `dev_impact` của Studio xác nhận **method cũ giữ nguyên, không sửa**, `spec_delta` chỉ có 3 file. Lưu ý đã nêu ở cuối `03-dev-impact.md` được **gỡ bỏ** sau khi đối chiếu diff. Không đề xuất TC cho layer không bị chạm code (AP-5).
>
> **Orphan**: không có — 16/16 TC đều đi qua code path đã sửa.

---

## 2. Thiếu so với quan điểm test

**Kết luận**: 6/12 quan điểm Trigger khớp đã cover đủ · **6 quan điểm thiếu**

| # | Mã quan điểm | Ưu tiên | Trigger khớp vì | Thiếu gì | Severity |
|---|---|---|---|---|---|
| **Q1** | `SEC-002` | Cao | Chức năng xử lý **credential / token** — `google_access_token` lưu JSON thô, **không mã hoá** (spec LUỒNG 5) | Không TC nào kiểm token **không lộ ra HTML nguồn / URL `再連携する` / query string**. Chỉ TC-ENV001-01 (đang fail) và TC-COMPATLEGACY001-01 có assert "log không chứa token", và cả 2 đều ở nhánh lỗi — **nhánh revoked chính không có** TC nào. | `[BLOCKER]` |
| **Q2** | `PERM-003` | Cao | Tổ chức vận hành **nhiều LINE OA**; màn + OAuth `state` định danh theo `bot_id` | TC-SECISO001-01 chỉ so sánh **hiển thị** của 2 bot ở tầng UI. Không TC nào chạm tầng URL/callback — tức là chính chỗ spec cảnh báo R-13. → xem G7. | `[BLOCKER]` |
| **Q3** | `STATE-CLEAN-001` | Cao | Thao tác **gỡ tích hợp** (hủy liên kết Google) | Có 2 TC nhưng **cả 2 đều `Abnormal`** (TC-10, TC-11). **Thiếu `Normal`** (hủy liên kết ở trạng thái bình thường — regression hành vi trước fix) và **thiếu `Boundary`** (bot nhiều QR / QR xóa mềm). `RULE-01`. | `[MAJOR]` |
| **Q4** | `COMPAT-LEGACY-001` | Cao | Tính năng chạm **google spread** + bản ghi liên kết tạo trước fix | Chỉ **1 TC `Abnormal`** (TC-13) mang mã này, và TC-13 có `spec_status = needs-human-review` → expected **chưa chốt**. TC-TOOLOLDREC001-01 (bản ghi cũ thiếu refresh token) mang **mã lạ** → theo 0.6 **không tính cover**. Thiếu `Normal` + `Boundary`. `RULE-01` + `RULE-09`. | `[MAJOR]` |
| **Q5** | `FUNC-001` | Cao | Mọi chức năng — luôn bắt buộc | TC duy nhất mang mã này (TC-FUNC001-01, luồng liên kết lại) **chưa chạy**. Thiếu `Abnormal` (từ chối cấp quyền / đổi tài khoản Google khác) + `Boundary`. `RULE-01`. | `[MAJOR]` |
| **Q6** | `OUT-EXPORT-001` | Trung bình → **nâng Cao** | "với mọi chức năng **ghi Google Spreadsheet**" | Không TC nào chạm spreadsheet thật ở output cuối. → xem G8. | `[MAJOR]` |

> **Quan điểm đã cover đủ (chỉ đếm)**: `OUT-TRUTH-001` (modal đúng sự thật — 3 TC Normal, biên đã cover ở `FUNC-004`) · `SEC-ISO-001` · `DATA-DB-001` (verify DB + đối chứng 2 bot ở TC-10) · `ENV-001` · `FUNC-004` · `FUNC-SEQ-001`.
>
> ⚠️ **5/16 TC mang mã quan điểm không có trong `framework/checklist-lme.md`** (`TOOL-KNOW-002`, `TOOL-NEGCTRL-001` ×3, `TOOL-OLDREC-001`) → theo BƯỚC 0.6 **không được tính là cover**. Đây là lý do Q4 bị hụt dù nội dung TC-TOOLOLDREC001-01 thực chất rất khớp `COMPAT-LEGACY-001`. **Cách xử lý rẻ nhất là gán lại mã chuẩn cho 5 TC này trên Studio, không phải viết TC mới** — xem §4 #9.

---

## 3. TC trùng lặp nội dung

Đã rà **16/16** TC theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`). Phát hiện **2 nhóm chồng lấn**, **không có `DUP-EXACT`**, **không đề nghị xóa TC nào**.

| Nhóm | TC giữ lại | TC đề nghị gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| N1 | **TC-FUNC004-01** (TC-15, Boundary, 2 lượt còn hạn / quá hạn) | **TC-TOOLOLDREC001-01** (TC-14, Abnormal) | `DUP-SUBSET` | Cùng tiền đề (bản ghi cũ **không có refresh token**) × cùng thao tác (đặt `created` quá hạn → mở màn) × cùng expected (modal cảnh báo hiện). **Lượt 2 của TC-15 ≡ toàn bộ TC-14.** Khác: mã quan điểm + loại case. | `[MINOR]` |
| N2 | **TC-TOOLNEGCTRL001-03** (TC-16, mở màn 3 lần) | *(giữ cả 2 — không gộp)* | Chồng lấn **một phần** | Cùng assert "modal hiện lại sau khi tải lại màn" + "bản ghi liên kết không đổi" với TC-FUNCSEQ001-01 (TC-07). | `[NIT]` |

**Xử lý đề nghị**:
- **N1** — **gộp, KHÔNG xóa**. Gate đã chạy: xóa TC-14 không làm mất cover REQ-006 (còn TC-13, TC-15) nhưng **sẽ mất** 2 assert riêng của TC-14 (nền màn giữ khối đang kết nối + nút `再連携する` có URL hợp lệ). → Cách đúng: bổ sung 2 assert đó vào TC-15 rồi mới bỏ TC-14; hoặc **giữ TC-14 và đổi mã quan điểm sang `COMPAT-LEGACY-001`** — cách này vừa khử trùng vừa lấp Q4, **là lựa chọn khuyến nghị**.
- **N2** — **giữ cả 2**. TC-07 có thao tác riêng (bấm 「再連携せずにこの画面を閉じる」), TC-16 đo tính ổn định qua 3 lần mở. Không phải trùng thật.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | **[AP-1]** Toàn bộ nhánh bắt `ClientException` | **Single-trigger generic-fix**. Mục 2 dev-impact: *"đã bắt riêng `ClientException` = đã gỡ quyền"* — generic catch theo class. Đếm trigger trong 16 TC: **1** (refresh token thu hồi → 400 `invalid_grant`). Ngưỡng bắt buộc ≥ 3 trigger + ≥ 1 lỗi chưa biết trước. | Bổ sung 3 TC ở §5 (`TC-OUTTRUTH001-04/05/06`). Hỏi Dev: 403 quota / 401 / `invalid_client` có bị gom vào "đã gỡ quyền" không? |
| 2 | `[MAJOR]` | **[AP-4]** Nguồn diff | Không verify được **fix shape thật**: mục "Commit / Pull Request" chỉ có commit hash `f50f62c6e5`, **không có link PR**; `spec_delta` của Studio có `diffStrategy = "stat"` và `hasCode = false` → chỉ biết **3 file, +90/−3 dòng**, không đọc được nội dung. | Yêu cầu Dev cung cấp link PR / diff đầy đủ. Câu hỏi adversarial: *fix là `catch (ClientException)` thuần hay có `if (code === 'invalid_grant')` bên trong?* Câu trả lời quyết định #1 là BLOCKER hay đã đủ. |
| 3 | `[MAJOR]` | Nguồn TC — tỷ lệ pass | `pass` = **12/16 = 75% < 80%**. Trong đó **2 TC chưa chạy chính là 2 TC `manual` verify bug gốc + luồng recover** (TC-TOOLKNOW002-01, TC-FUNC001-01), **1 skip** (TC-OUTTRUTH001-03), **1 fail** (TC-ENV001-01). | Chạy 2 TC manual trước khi kết luận ticket. TC `skip`/chưa chạy **không** được tính là coverage OK. |
| 4 | `[MAJOR]` | TC-OUTTRUTH001-03 (`skip`) | TC `skip` **chưa raise ticket bug và cũng chưa có lý do skip ghi lại** — trong khi nó cover REQ-003 risk **High**. | Chạy lại trên `staging`/`prd`; nếu skip vì môi trường thì ghi rõ lý do vào kết quả (note của TC đã yêu cầu điều này). |
| 5 | `[MAJOR]` | Nguồn TC — môi trường | `RULE-08` / `ENV-003`: task chạm **URL / domain** (redirect URI) nhưng **0 TC chạy production**, 16/16 chỉ chạy `local`. | Chạy lại tối thiểu TC-TOOLKNOW002-01 · TC-OUTTRUTH001-03 · TC-FUNC001-01 trên `staging` **và** `prd`. |
| 6 | `[MAJOR]` | Nguồn TC — evidence | `RULE-02`: **16/16 TC có `Kết quả thực thi` nhưng cột `Evidence thực tế` rỗng hoàn toàn**, và người chạy là `pipeline` (AI tự chạy, `last_exec.source = ai`). Kết quả đang là **tự khai, không có bằng chứng**. | Bổ sung evidence đúng loại (ảnh màn có modal, ảnh bản ghi DB trước/sau, log) cho ít nhất các TC ở nhánh revoked. |
| 7 | `[MAJOR]` | 15/16 TC | Cột `Trạng thái đánh giá spec` **để trống** ở 15 TC (chỉ TC-COMPATLEGACY001-01 có `needs-human-review`). Task này có spec thật ([feature-spec.md](../../spec-features/admin/qr-landing/feature-spec.md) BR-21/BR-22/R-13/R-17) nhưng **không TC nào dẫn chiếu** — toàn bộ `spec_ids` chỉ trỏ ticket + diff + source code. | Gán `Spec ghi rõ` + dẫn BR tương ứng cho các TC chạm hủy liên kết (BR-21, BR-22) và OAuth (R-13). Đây cũng là lý do G3/G4/G7 bị bỏ sót. |
| 8 | `[MAJOR]` | `03-dev-impact.md` | File có `Auto-filled: 2026-09-14 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" CHƯA tick** → F/D/T có thể thiếu hoặc map sai. Thực tế mục 4.1 của Dev là **danh sách file**, không phải danh sách function; mục 4.3 chỉ có **1 dòng**. | Tester đọc lại Redmine #37645 journal #131642 và tick checkbox trước khi report này có giá trị chốt. |
| 9 | `[MAJOR]` | TC-TOOLKNOW002-01, TC-TOOLNEGCTRL001-01/02/03, TC-TOOLOLDREC001-01 | **5 TC mang mã quan điểm nội bộ Studio** (`TOOL-*`) không có trong `framework/checklist-lme.md` → không map được coverage, kéo theo Q4 (và một phần Q3) bị tính là thiếu dù nội dung TC đã đúng. | Gán lại mã chuẩn trên Studio (`testcase_update`), gợi ý: TC-14 → `COMPAT-LEGACY-001` · TC-02/TC-03 → `OUT-TRUTH-001` hoặc `FUNC-001` (đối chứng âm) · TC-16 → `FUNC-SEQ-001` · TC-01 → `FUNC-001`. **Rẻ hơn nhiều so với viết TC mới.** |
| 10 | `[MINOR]` | TC-COMPATLEGACY001-01 (TC-13) | Expected ghi rõ *"chỉ ghi nhận actual, **không dùng trạng thái này để chấm Pass/Fail**"*, nhưng `last_exec.status = pass` → TC **đã bị chấm Đạt** dù chính nó tuyên bố không chấm được. Kết quả "Đạt" này đang che phần chưa có quyết định nghiệp vụ. | Đổi kết quả về `Chưa test`/`needs-human-review` cho tới khi PO chốt (xem §6). |
| 11 | `[NIT]` | TC-STATECLEAN001-02 (TC-11) | Note nêu đúng một vấn đề trải nghiệm thật (vừa báo mất liên kết, vừa không hủy được) nhưng **kết luận sẵn** là "không tính fail của ticket". Đây là quyết định của PO, không phải của TC. | Đẩy lên §6 để PO chốt, giữ TC nguyên trạng. |

> Chất lượng **từng TC** (5 mục BƯỚC 4a) nhìn chung **tốt**: expected đo lường được (nêu rõ wording JP, tên khối UI, 5 trường DB), tiền đề đủ dựng lại env, tiêu đề có keyword, dữ liệu test thật (token JSON có cấu trúc). **Không có issue nào ở 5 mục này** — không đẻ thêm issue cho đủ bảng.

---

## 5. TCs đề xuất bổ sung (15)

**Xác nhận đối chiếu** (bắt buộc):

| Mục | Kết quả |
|---|---|
| Kho TCs | ⚠️ [kho-tcs/](../../kho-tcs/) **chưa có FA-017** (QR Code Action) — chỉ có FA-001/003/004/007/008/009/010/011/012/013/015/019/020/021/026/031/033/039. **Không đối chiếu được**, không phát hiện được conflict expected với TC cũ của tính năng này. |
| Chống trùng | Đã đối chiếu **16 TC ở BƯỚC 0** + nội bộ §5 — **không TC đề xuất nào trùng**. **3 TC đã bị loại** vì trùng ý định với TC sẵn có, chuyển thành issue §4 thay vì đẻ TC mới: (a) "bản ghi cũ còn hiệu lực không cảnh báo" ≡ TC-TOOLNEGCTRL001-01 → §4 #9 gán mã · (b) "log không chứa token ở nhánh lỗi" ≡ TC-ENV001-01 + TC-COMPATLEGACY001-01 → gộp vào `TC-SEC002-01` ở nhánh revoked · (c) "redirect URI đúng trên môi trường thật" ≡ TC-OUTTRUTH001-03 → §4 #5 yêu cầu chạy lại, không viết mới. |
| GAP không đẻ TC | **G9** (`RULE-08`) — không viết TC mới, vì TC đã có sẵn đủ; vấn đề là **môi trường chạy**. Xử lý ở §4 #5. |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-OUTTRUTH001-04 | UI | OUT-TRUTH-001 | Màn liên kết Google Spreadsheet của QR code action (`/basic/landing-qr/link-google`) | Abnormal | manual | Tất cả | Google trả lỗi vượt hạn mức thì màn KHÔNG được báo "đã gỡ quyền" | Bot test riêng đang liên kết Google Spreadsheet **hợp lệ** (quyền chưa hề bị gỡ). Chuẩn bị cách làm cho lời gọi làm mới token trả về lỗi vượt hạn mức của Google (nhờ Dev bật cờ mô phỏng, hoặc dùng tài khoản Google đã chạm trần hạn mức trong ngày). | 1. Đặt điều kiện để lời gọi làm mới token trả lỗi vượt hạn mức<br>2. Đăng nhập màn quản trị, chọn bot test<br>3. Mở màn liên kết Google Spreadsheet<br>4. Quan sát toàn màn + kiểm tra log ứng dụng<br>5. Gỡ điều kiện mô phỏng, mở lại màn | Lỗi Google trả về: HTTP 403, nội dung báo vượt hạn mức (`rateLimitExceeded` / `quotaExceeded`) | Màn **KHÔNG** hiện hộp thoại 「Googleスプレッドシートの連携が解除されました」 — vì liên kết vẫn còn hiệu lực, chỉ là Google đang chặn tạm. Màn vẫn mở được, giữ khối 「接続されたGoogleアカウント」. Nếu hệ thống có báo gì cho người dùng thì nội dung phải nói đúng bản chất (tạm thời không kiểm tra được), tuyệt đối không khẳng định đã bị gỡ quyền. Log ghi lại mã lỗi thật. Sau khi gỡ mô phỏng, mở lại màn hiển thị bình thường. | | Lấp G2 · `[BLOCKER]` AP-1 — trigger thứ 2 của nhánh `ClientException` · Evidence: ảnh toàn màn + log có mã lỗi 403 · Spec: TICKET-37645 · Rủi ro nếu sai: khách bị dọa "mất liên kết" rồi bấm cấp quyền lại vô ích |
| TC-OUTTRUTH001-05 | UI | OUT-TRUTH-001 | Màn liên kết Google Spreadsheet của QR code action | Abnormal | manual | Tất cả | Ứng dụng Google bị đổi/xóa cấu hình thì màn báo đúng bản chất, không quy cho khách gỡ quyền | Bot test riêng đang liên kết hợp lệ. Chuẩn bị cách làm cho lời gọi làm mới token trả lỗi sai thông tin ứng dụng (nhờ Dev mô phỏng, hoặc dùng cấu hình ứng dụng Google sai chủ đích trên môi trường test). | 1. Đặt điều kiện để lời gọi làm mới token trả lỗi sai thông tin ứng dụng<br>2. Mở màn liên kết Google Spreadsheet của bot test<br>3. Quan sát màn + đọc log<br>4. Khôi phục cấu hình đúng rồi mở lại màn | Lỗi Google trả về: HTTP 401 hoặc 400 kèm nội dung `invalid_client` / `unauthorized_client` | Màn không được kết luận nhầm là khách đã gỡ quyền. Ghi nhận **actual thực tế** của bản fix hiện tại (nhiều khả năng vẫn hiện modal "đã gỡ quyền") và đối chiếu với quyết định của Dev/PO ở §6. Log phải ghi đủ mã lỗi gốc để vận hành phân biệt được lỗi cấu hình hệ thống với lỗi của khách. Khôi phục cấu hình → màn trở lại bình thường. | | Lấp G2 · trigger thứ 3 của nhánh `ClientException` · Đây là lỗi **phía hệ thống**, báo nhầm sẽ đẩy việc sang khách · Evidence: ảnh màn + log |
| TC-OUTTRUTH001-06 | UI | OUT-TRUTH-001 | Màn liên kết Google Spreadsheet của QR code action | Abnormal | manual | Tất cả | Lỗi lạ chưa biết trước từ Google không làm hỏng màn và không bị quy thành "đã gỡ quyền" | Bot test riêng đang liên kết hợp lệ. Có cách làm cho lời gọi tới Google trả về một lỗi **không nằm trong danh sách Dev đã lường trước**. | 1. Đặt điều kiện để lời gọi tới Google trả lỗi máy chủ (5xx) hoặc phản hồi sai định dạng<br>2. Mở màn liên kết Google Spreadsheet<br>3. Quan sát màn, bấm giờ thời gian tải<br>4. Kiểm tra log<br>5. Gỡ mô phỏng, mở lại màn | Lần 1: HTTP 500 từ Google. Lần 2: phản hồi không phải JSON hợp lệ | Cả 2 lần: màn **vẫn mở được**, không trắng màn, không lỗi hệ thống, không treo quá lâu (ghi lại thời gian tải thực tế). Không hiện cảnh báo "đã gỡ quyền" (đúng thiết kế fail-safe mà Dev tự nêu). Log có bản ghi đủ truy vết và **không chứa nguyên văn token**. | | Lấp G2 · TC kiểm **fallback lỗi chưa biết** theo yêu cầu AP-1 · Bổ trợ cho TC-ENV001-01 đang fail (#40936) nhưng khác nhánh: đây là lỗi **có phản hồi**, TC-12 là **không có phản hồi** |
| TC-STATECLEAN001-03 | UI | STATE-CLEAN-001 | Màn liên kết Google Spreadsheet + hủy liên kết (`SCR-QRL-33`) | Abnormal | auto | Tất cả | Bản ghi ở trạng thái 4 và đã bị gỡ quyền: hủy liên kết KHÔNG bị chặn, dữ liệu phải được dọn sạch | Bot test A có bản ghi liên kết: token với refresh token đã bị thu hồi, **trạng thái = 4**, có ≥ 1 QR đang gắn khóa spreadsheet. Bot test B đối chứng đang liên kết hợp lệ. Ghi lại dữ liệu 2 bot trước thao tác. | 1. Mở màn liên kết Google Spreadsheet của bot A<br>2. Ghi nhận modal cảnh báo có hiện hay không<br>3. Đóng modal, bấm 「Googleアカウントの接続を解除する」 rồi xác nhận 「接続を解除する」<br>4. Quan sát màn sau khi quay về<br>5. Đối chiếu dữ liệu liên kết + khóa spreadsheet của bot A và bot B | Trạng thái bản ghi liên kết = **4** (giá trị không có trong hằng số model — spec R-17) | Hủy liên kết **thực hiện được** (khác trạng thái 0/1/3 bị chặn — BR-21): màn chuyển về khối 「Googleアカウント連携が完了していません」, không còn dòng hủy liên kết, không còn modal cảnh báo. Dữ liệu bot A: token / tên tài khoản / ảnh đại diện / thời điểm kết nối rỗng, khóa spreadsheet của mọi QR thuộc bot A bị xóa. Bot B giữ nguyên. | | Lấp G3 · Spec **BR-21 + R-17**: trạng thái 4 chiếm **40% dữ liệu thật** nhưng không TC nào chạm · Evidence: ảnh màn trước/sau + ảnh bản ghi DB 2 bot · `regression` |
| TC-STATECLEAN001-04 | UI | STATE-CLEAN-001 | Màn liên kết Google Spreadsheet của QR code action | Normal | auto | Tất cả | Bản ghi ở trạng thái 4 và đã bị gỡ quyền vẫn phải hiện cảnh báo liên kết lại | Bot test riêng có bản ghi liên kết: token với refresh token đã bị thu hồi, **trạng thái = 4**, còn đủ tên tài khoản + ảnh đại diện. | 1. Đăng nhập màn quản trị, chọn bot test<br>2. Mở màn liên kết Google Spreadsheet<br>3. Đóng modal, quan sát nền màn, kéo xuống hết chân màn<br>4. Lặp lại với trạng thái = 2 (đã hoàn tất) để đối chiếu | Trạng thái bản ghi lần lượt: **4**, rồi 2 | Với trạng thái 4: modal cảnh báo 「Googleスプレッドシートの連携が解除されました」 **vẫn tự bật** như trạng thái 2 — việc phát hiện gỡ quyền không được phụ thuộc vào trạng thái bản ghi. Nền màn giữ khối 「接続されたGoogleアカウント」 và dòng 「Googleアカウントの接続を解除する」. Hai lượt cho kết quả hiển thị giống nhau. | | Lấp G3 · Lấp Q3 (`Normal` còn thiếu của STATE-CLEAN-001) · Nếu trạng thái 4 **không** hiện cảnh báo → 40% bot ngoài thật vẫn dính đúng bug gốc #37645 |
| TC-STATECLEAN001-05 | UI | STATE-CLEAN-001 | Hủy liên kết Google khi liên kết vẫn bình thường | Normal | auto | Tất cả | Hủy liên kết ở trạng thái bình thường giữ nguyên hành vi như trước fix | Bot test A đang liên kết Google Spreadsheet **hợp lệ** (quyền chưa bị gỡ), trạng thái = 2, có ≥ 1 QR gắn khóa spreadsheet. Bot test B đối chứng cũng đang liên kết hợp lệ. Ghi lại dữ liệu 2 bot. | 1. Mở màn liên kết Google Spreadsheet của bot A<br>2. Xác nhận **không** có modal cảnh báo nào bật lên<br>3. Bấm 「Googleアカウントの接続を解除する」<br>4. Đối chiếu hộp thoại xác nhận hiển thị đúng tài khoản Google của bot A<br>5. Bấm 「接続を解除する」, quan sát màn và đối chiếu dữ liệu 2 bot | Không nhập liệu | Toàn bộ luồng hủy chạy đúng **y như trước khi fix**: không modal cảnh báo xen vào, hộp thoại xác nhận đúng tài khoản, sau khi hủy màn về khối 「Googleアカウント連携が完了していません」, dữ liệu bot A được dọn sạch và khóa spreadsheet của QR thuộc bot A bị xóa; bot B không đổi. | | Lấp Q3 (`RULE-01` — `Normal` còn thiếu) · `regression` — đường hủy liên kết là thứ Dev khẳng định "giữ nguyên hành vi" khi tách `google_sheet_auth_url_reconnect`, cần 1 TC chứng minh ở nhánh bình thường · Evidence: ảnh màn + bản ghi DB 2 bot |
| TC-STATECLEAN001-06 | Data | STATE-CLEAN-001 | Hủy liên kết Google — phạm vi xóa khóa spreadsheet | Boundary | auto | Tất cả | Bot có nhiều QR: hủy liên kết phải xóa khóa spreadsheet của TẤT CẢ, không sót bản nào | Bot test A đang liên kết hợp lệ và có **≥ 30 QR code action** đều đang gắn khóa spreadsheet (gồm QR nằm trong nhiều thư mục khác nhau). Ghi lại tổng số QR có khóa spreadsheet trước thao tác. | 1. Đếm và ghi lại số QR của bot A đang có khóa spreadsheet<br>2. Mở màn liên kết Google Spreadsheet của bot A<br>3. Bấm hủy liên kết và xác nhận<br>4. Đếm lại số QR còn khóa spreadsheet của bot A<br>5. Kiểm tra ngẫu nhiên 5 QR ở các thư mục khác nhau trên màn danh sách | Bot A với ≥ 30 QR gắn khóa spreadsheet, trải trên ≥ 3 thư mục | Sau khi hủy: số QR còn khóa spreadsheet của bot A = **0** (không sót bản nào, không phụ thuộc thư mục). Icon Google Sheet biến mất khỏi mọi hàng QR trên màn danh sách. Thao tác hoàn tất trong thời gian dùng được, không timeout — ghi lại thời gian thực tế. | | Lấp Q3 (`RULE-01` — `Boundary` còn thiếu) · Spec **BR-22** (phạm vi xóa là **toàn bộ** landing thuộc bot) · Cặp đôi với `TC-DATADB001-01` (bản xóa mềm) |
| TC-DATADB001-01 | Data | DATA-DB-001 | Hủy liên kết Google — QR đã xóa mềm (thùng rác) | Normal | auto | Tất cả | Hủy liên kết phải xóa khóa spreadsheet của cả QR đang nằm trong thùng rác | Bot test A đang liên kết Google Spreadsheet hợp lệ, có: (a) ≥ 1 QR đang hiển thị bình thường có khóa spreadsheet, (b) ≥ 1 QR **đã xóa mềm** (đang ở màn thùng rác) cũng có khóa spreadsheet. Ghi lại khóa spreadsheet của cả 2 QR. | 1. Vào màn thùng rác QR, xác nhận QR đã xóa mềm vẫn đang tồn tại<br>2. Mở màn liên kết Google Spreadsheet của bot A, bấm hủy liên kết và xác nhận<br>3. Đối chiếu khóa spreadsheet của QR đang hiển thị và QR trong thùng rác<br>4. Vào thùng rác, bấm 「復元する」 khôi phục QR đã xóa mềm<br>5. Mở màn sửa QR vừa khôi phục, kiểm tra trạng thái liên kết spreadsheet | Bot A có 1 QR hiển thị + 1 QR trong thùng rác, cả 2 đều có khóa spreadsheet | Sau khi hủy liên kết: khóa spreadsheet bị xóa ở **cả QR đang hiển thị lẫn QR trong thùng rác**. QR khôi phục từ thùng rác **không** mang theo khóa spreadsheet cũ, không trỏ tới sheet của tài khoản Google đã ngắt. | | Lấp G4 · Spec **BR-22** ghi rõ xóa `withTrashed` — bộ TC hiện tại bỏ trắng nhánh xóa mềm · `RULE-07` verify cả DB + màn hình · Rủi ro nếu sai: QR khôi phục ghi dữ liệu vào sheet của tài khoản Google **không còn quyền** |
| TC-FUNC001-02 | UI | FUNC-001 | Liên kết lại từ modal cảnh báo bằng tài khoản Google khác | Abnormal | manual | product | Liên kết lại bằng tài khoản Google KHÁC tài khoản cũ thì dữ liệu cũ phải được thay đúng | Bot test riêng đang ở trạng thái bị gỡ quyền thật (tiếp nối TC-TOOLKNOW002-01). Có **2 tài khoản Google test** của QA: tài khoản cũ (đã gỡ quyền) và tài khoản mới. Ghi lại tên tài khoản, ảnh đại diện, khóa spreadsheet của các QR trước thao tác. | 1. Mở màn liên kết Google Spreadsheet của bot test, modal cảnh báo hiện<br>2. Bấm 「再連携する」<br>3. Ở màn Google chọn **tài khoản mới**, đồng ý cấp quyền<br>4. Chờ quay về màn quản trị, tải lại màn<br>5. Đối chiếu tên tài khoản + ảnh đại diện hiển thị và khóa spreadsheet của các QR | Tài khoản Google test **thứ hai** của QA | Màn hiển thị đúng **tài khoản mới** (tên + ảnh đại diện), không còn modal cảnh báo. Khóa spreadsheet cũ (thuộc tài khoản cũ) **không được giữ lại trỏ sang tài khoản mới** — nếu hệ thống yêu cầu hủy liên kết trước rồi mới đổi tài khoản thì phải báo rõ cho người dùng, không im lặng ghi đè. Ghi nhận actual để đối chiếu §6. | | Lấp G5 + Q5 (`RULE-01` — `Abnormal` còn thiếu của FUNC-001) · Mục 2 dev-impact nói fix giữ được nút hủy **để khách xóa mã bảng tính cũ trước khi liên kết tài khoản Google khác** → đây chính là kịch bản Dev viện dẫn nhưng **không TC nào kiểm** · `RULE-08` domain → chạy product |
| TC-FUNC001-03 | UI | FUNC-001 | Từ chối cấp quyền ở màn Google sau khi bấm liên kết lại | Abnormal | manual | product | Người dùng bấm liên kết lại rồi từ chối cấp quyền thì hệ thống không hỏng và không mất dữ liệu | Bot test riêng đang ở trạng thái bị gỡ quyền. Ghi lại đầy đủ bản ghi liên kết trước thao tác (token, tên tài khoản, ảnh đại diện, thời điểm kết nối, trạng thái). | 1. Mở màn liên kết Google Spreadsheet, modal cảnh báo hiện<br>2. Bấm 「再連携する」<br>3. Ở màn Google, bấm **Hủy / quay lại** (không đồng ý cấp quyền)<br>4. Quan sát nơi hệ thống đưa người dùng về<br>5. Đối chiếu bản ghi liên kết trong dữ liệu | Không nhập liệu — từ chối ở màn đồng ý của Google | Hệ thống đưa người dùng về màn quản trị (không trắng màn, không lỗi hệ thống, không lỗi 500). Nếu có thông báo thì nội dung dễ hiểu, không hiện thông báo kỹ thuật thô. Bản ghi liên kết giữ **nguyên vẹn 5 trường** như trước thao tác — đặc biệt token cũ không bị xóa hay ghi đè rỗng. Mở lại màn thì modal cảnh báo vẫn hiện (vì quyền vẫn đang bị gỡ). | | Lấp G5 + Q5 · Nhánh người dùng bỏ giữa chừng của luồng OAuth — hoàn toàn trống trong bộ TC hiện tại · `RULE-08` domain → product |
| TC-FUNC001-04 | UI | FUNC-001 | Bấm liên kết lại ở 2 tab song song | Boundary | manual | product | Hai luồng cấp quyền chạy song song không làm hỏng bản ghi liên kết | Bot test riêng đang ở trạng thái bị gỡ quyền. Có 1 tài khoản Google test. Ghi lại bản ghi liên kết trước thao tác. | 1. Mở màn liên kết Google Spreadsheet ở **tab A**, bấm 「再連携する」, dừng ở màn chọn tài khoản Google (chưa đồng ý)<br>2. Mở cùng màn ở **tab B**, bấm 「再連携する」, hoàn tất cấp quyền tới khi quay về màn quản trị<br>3. Quay lại tab A, bấm đồng ý cấp quyền để hoàn tất nốt<br>4. Tải lại màn liên kết, đối chiếu bản ghi liên kết | Cùng 1 tài khoản Google test, 2 luồng cấp quyền chồng nhau | Bản ghi liên kết của bot test ở trạng thái **nhất quán**: đúng 1 tài khoản, token dùng được, thời điểm kết nối hợp lệ, trạng thái không kẹt ở giá trị dở dang. Màn không còn modal cảnh báo. Không phát sinh bản ghi liên kết thứ hai cho cùng bot (spec: `landing_connect_google` UNIQUE theo `bot_id`). | | Lấp Q5 (`RULE-01` — `Boundary` còn thiếu của FUNC-001) · Fix thêm **đường vào thứ hai** của luồng cấp quyền (`google_sheet_auth_url_reconnect` song song `google_sheet_auth_url`) → cần 1 TC cho tình huống 2 luồng chồng nhau · `RULE-08` domain → product |
| TC-PERM003-01 | API | PERM-003 | Callback cấp quyền Google (`/basic/landing-qr/redirect-google-sheet`) | Abnormal | manual | Tất cả | Sửa mã bot trên đường dẫn cấp quyền không được ghi token sang bot khác | Tài khoản quản trị đang đăng nhập có quyền trên **bot A**. Tồn tại **bot B** (có thể là bot của tài khoản khác hoặc bot mà tài khoản đang đăng nhập không được phép thao tác). Bot A đang bị gỡ quyền để modal cảnh báo hiện. Ghi lại bản ghi liên kết của **cả bot A và bot B** trước thao tác. | 1. Mở màn liên kết Google Spreadsheet của bot A, lấy đường dẫn của nút 「再連携する」<br>2. Sửa tham số trạng thái trên đường dẫn từ mã bot A thành **mã bot B**<br>3. Mở đường dẫn đã sửa, đồng ý cấp quyền bằng tài khoản Google test<br>4. Quan sát kết quả trả về<br>5. Đối chiếu bản ghi liên kết của bot A và bot B | Tham số trạng thái trên đường dẫn bị thay bằng mã của bot B | Hệ thống **từ chối** (báo lỗi quyền / quay về màn quản trị mà không ghi gì) hoặc chỉ ghi cho đúng bot mà tài khoản đang đăng nhập được phép. Bản ghi liên kết của **bot B KHÔNG bị thay đổi** — token, tên tài khoản, ảnh đại diện, thời điểm kết nối, trạng thái giữ nguyên. Ghi nhận actual đầy đủ để Leader/PO chốt ở §6. | | Lấp G7 + Q2 · Spec **R-13**: callback nhận `state` = `bot_id` mà **không đối chiếu bot đang đăng nhập** · Đây là rủi ro **có sẵn**, nhưng fix tạo thêm entry point mới vào đúng luồng này → cần biết mức độ trước khi release · Chạy `staging` trước, chỉ lặp lại trên `prd` khi Leader duyệt |
| TC-SEC002-01 | UI | SEC-002 | Màn liên kết Google Spreadsheet — trạng thái bị gỡ quyền | Normal | auto | Tất cả | Trạng thái bị gỡ quyền không làm lộ token ra HTML, đường dẫn hay log | Bot test riêng ở trạng thái bị gỡ quyền (modal cảnh báo đang hiện). Có quyền xem log ứng dụng của môi trường test. | 1. Mở màn liên kết Google Spreadsheet của bot test<br>2. Xem mã nguồn trang (view-source) và tìm các chuỗi `access_token`, `refresh_token`, `ya29.`, `client_secret`<br>3. Kiểm tra đường dẫn của nút 「再連携する」 và thanh địa chỉ trình duyệt<br>4. Đóng modal, tải lại màn, đọc log ứng dụng sinh ra trong 2 lần mở màn | Không nhập liệu — chỉ quan sát ở trạng thái revoked | Mã nguồn trang **không chứa** giá trị token nào (cả access token lẫn refresh token) và không chứa khóa bí mật của ứng dụng. Đường dẫn nút 「再連携する」 chỉ mang tham số cần cho việc cấp quyền, không mang token cũ. Log ứng dụng của nhánh phát hiện gỡ quyền ghi đủ để truy vết nhưng **không chứa nguyên văn token**. | | Lấp Q1 · Spec LUỒNG 5 ghi rõ `google_access_token` lưu **JSON thô, không mã hoá** → nhánh mới đọc và xử lý token này mỗi lần mở màn · Nhánh revoked chính hiện **chưa có TC nào** assert điều này (TC-ENV001-01 đang fail, TC-COMPATLEGACY001-01 ở nhánh token hỏng) |
| TC-OUTEXPORT001-01 | UI | OUT-EXPORT-001 | Google Spreadsheet thật sau khi liên kết lại | Normal | manual | product | Sau khi liên kết lại, dữ liệu quay lại chảy vào spreadsheet thật | Bot test riêng vừa liên kết lại thành công (tiếp nối TC-FUNC001-01), trạng thái bản ghi đã về đã hoàn tất. Có QR code action đang hoạt động và quét được. Biết lịch chạy job đẩy dữ liệu (`landing:insert_google_sheet`, 02:10). | 1. Sau khi liên kết lại thành công, ghi lại thời điểm và khóa spreadsheet của QR test<br>2. Quét QR test bằng máy thật để phát sinh dữ liệu trong ngày<br>3. Chờ qua mốc job chạy của ngày hôm sau<br>4. Mở chính spreadsheet trên tài khoản Google vừa cấp quyền lại<br>5. Đối chiếu dòng dữ liệu mới với số liệu ở màn 「データ詳細」 | QR test được quét thật ≥ 1 lần trong ngày | Spreadsheet trên tài khoản Google **nhận được dòng dữ liệu của ngày hôm trước** với đúng 4 cột 「日時 / URL読み込み / 友だち追加・ブロック解除 / アクション稼働」. Số liệu khớp với màn 「データ詳細」. Không phát sinh spreadsheet trùng lặp cho cùng QR. | | Lấp G8 + Q6 · `RULE-06` output cuối chuỗi — cả 16 TC hiện dừng ở màn admin · `RULE-08` job nền + domain → **bắt buộc product** · Đây là giá trị cuối mà khách thực sự cần sau khi bấm 再連携する · Studio đã cố ý để ngoài phạm vi diff → cần Leader xác nhận ai chạy TC này |
| TC-COMPATLEGACY001-02 | Data | COMPAT-LEGACY-001 | Màn liên kết Google Spreadsheet — bản ghi liên kết tạo trước fix | Boundary | auto | Tất cả | Bản ghi cũ có phạm vi quyền hẹp hơn hiện tại vẫn được xử lý đúng | Bot test riêng có bản ghi liên kết kiểu cũ: token **có** refresh token và còn hạn, nhưng phạm vi quyền chỉ gồm bảng tính, **thiếu** phần thông tin hồ sơ người dùng mà bản mới yêu cầu. | 1. Đặt token của bản ghi liên kết bot test theo dữ liệu nhập<br>2. Mở màn liên kết Google Spreadsheet<br>3. Quan sát màn: có modal cảnh báo không, khối tài khoản hiển thị thế nào (đặc biệt tên + ảnh đại diện)<br>4. Kiểm tra log<br>5. Lặp lại lần 2 với token có đủ cả 2 phạm vi quyền để đối chiếu | Lượt 1: phạm vi quyền chỉ `spreadsheets`. Lượt 2: đủ `spreadsheets` + thông tin hồ sơ | Lượt 1: màn **mở được bình thường**, không lỗi hệ thống, không trắng màn. Việc có hiện cảnh báo hay không phải **nhất quán** với trạng thái quyền thật của token (quyền còn hiệu lực thì không được báo đã gỡ). Thiếu thông tin hồ sơ chỉ được phép ảnh hưởng phần hiển thị tên/ảnh đại diện, không làm hỏng việc phát hiện trạng thái liên kết. Lượt 2 hiển thị đầy đủ như bình thường. | | Lấp Q4 (`RULE-01` — `Boundary` còn thiếu của COMPAT-LEGACY-001) · `RULE-09` cũ & mới song song · Bản ghi cũ có phạm vi quyền hẹp là dữ liệu thật tồn tại trước fix (TC-TOOLOLDREC001-01 đã dùng đúng dạng token này) |

---

## 6. Spec update needed

| # | Vấn đề | Nguồn | Cần ai chốt |
|---|---|---|---|
| 1 | **Lỗi nào từ Google mới được coi là "đã gỡ quyền"?** Hiện fix gom mọi `ClientException` (4xx) thành một kết luận duy nhất. Cần chốt danh sách mã lỗi được phép hiện modal 「連携が解除されました」 và mã lỗi phải báo khác (vượt hạn mức, sai cấu hình ứng dụng, lỗi máy chủ Google). | Mục 2 + mục 6 `03-dev-impact.md`; `[AP-1]` §4 #1 | **Dev + PO** |
| 2 | **Trạng thái `4` của bản ghi liên kết là gì?** Spec [FA-017 R-17](../../spec-features/admin/qr-landing/feature-spec.md) ghi giá trị này chiếm **40% dữ liệu thật** nhưng **không có trong hằng số model**, khiến cron bỏ qua và BR-21 không chặn hủy. Cần định nghĩa chính thức + hành vi mong đợi của màn ở trạng thái này. | Spec BR-21 / R-17; G3 | **Dev + PO** |
| 3 | **Bị gỡ quyền + đang tạo sheet = người dùng kẹt.** Ở tổ hợp trạng thái 0/1/3, khách vừa bị báo mất liên kết vừa không hủy được liên kết, chỉ còn đường bấm liên kết lại. Note của TC-STATECLEAN001-02 tự kết luận "không tính là fail của ticket" — quyết định này thuộc về PO. | TC-STATECLEAN001-02 (TC-11); §4 #11 | **PO** |
| 4 | **Token lưu sai định dạng thì UI nên hiện gì?** TC-COMPATLEGACY001-01 đang mang `spec_status = needs-human-review`: chưa chốt là tiếp tục hiển thị 「đang kết nối」 hay chuyển sang cảnh báo liên kết lại. TC này hiện đã bị chấm `pass` dù expected tuyên bố không chấm được. | TC-COMPATLEGACY001-01 (TC-13); §4 #10 | **PO + Leader** |
| 5 | **Callback cấp quyền không đối chiếu bot đang đăng nhập (R-13).** Rủi ro có sẵn trước fix, nhưng fix mở thêm một entry point vào đúng luồng đó. Cần chốt: xử lý trong ticket này, hay tách ticket bảo mật riêng. | Spec R-13; G7 + TC-PERM003-01 | **Leader + Dev** |
| 6 | **Liên kết lại bằng tài khoản Google khác thì khóa spreadsheet cũ xử lý ra sao?** Mục 2 dev-impact viện dẫn kịch bản này để biện minh cho việc giữ nút hủy liên kết, nhưng không nêu hành vi mong đợi khi khách bấm thẳng 再連携する với tài khoản khác. | Mục 2 `03-dev-impact.md`; TC-FUNC001-02 | **PO + Dev** |
| 7 | **Kho TCs chưa có FA-017.** Không đối chiếu được TC cũ của QR Code Action → review này không phát hiện được conflict expected với bộ TC lịch sử của tính năng. | [kho-tcs/](../../kho-tcs/); §5 | **Leader** (xếp lịch `/collect-tcs` cho FA-017) |
