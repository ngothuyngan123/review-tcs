# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | **MCP LME TEST STUDIO** — task #233 (ticket #33762), round 2 |
| Tổng số TC review | **9** |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận: 4/15 vùng ảnh hưởng đủ TC · 7 GAP · 4 RISK.**

Chiều `diff code` lấy từ Studio `task_get_context` (`diffAvailable = true`, `diffStat` = 2 file / 2 dòng đổi) — không suy từ snippet.

| # | Vùng ảnh hưởng | Chiều | TC cover | Status | Severity |
|---|---|---|---|---|---|
| G1 | `T2 — Reminder Delivery (FA-022)`: job `NewEventRemindTask` đọc `event_step_time` → gửi tin LINE | `dev-impact` | không có | `GAP` — 9/9 TC dừng ở bản ghi `event_step_time`, không TC nào chờ tới giờ gửi để xác nhận khách **có / không** nhận tin. Triệu chứng gốc của bug là "khách nhận remind sớm" → chưa TC nào chứng minh triệu chứng đã hết (RULE-06) | `[BLOCKER]` |
| G2 | `EP-73 saveSettingSendMessageEventStep` — luồng **CẬP NHẬT** mốc gửi remind | `diff code` | không có | `GAP` — Studio tự chấm `level = none` (0 TC) và ghi *"cùng bug, Dev để NGOÀI scope"*; `dev_impact` ghi *"cập nhật remind EP-73 thiếu check giờ kết thúc — cần rà regression"*. Sau fix, luồng THÊM chặn mốc < giờ kết thúc còn luồng SỬA thì không → 2 luồng cùng rule, 2 hành vi | `[BLOCKER]` |
| G3 | Khung tiếp nhận **kết thúc lúc nửa đêm** (`end_time = 00:00`, hiển thị `22:00~24:00`) — REQ-009 | `diff code` | **TC đã bị gỡ khỏi bộ** (`tc-13766`) | `GAP` — chính bộ TC này đã phát hiện **bug #712 → Redmine #40253** ở run #582: bản vá so với `received_booking_date + end_time`, `end_time = 00:00` bị quy về ĐẦU ngày ⇒ remind lập lịch **trước cả giờ lớp bắt đầu**. TC phát hiện không còn trong bộ 9 TC hiện tại ⇒ mất khả năng bắt lại | `[BLOCKER]` |
| G4 | Số ngày gửi sau **N > 0** (REQ-007) | `diff code` | không có | `GAP` — `dev_impact` liệt kê rõ *"vùng cần test kỹ: … day=0 **và day=N**"*. Chỉ có day=0 được test ⇒ chưa chứng minh bản vá không chặn nhầm nhóm N>0 | `[MAJOR]` |
| G5 | Mốc gửi đã **trôi qua so với `NOW()`** (REQ-006 / BR-40) | `diff code` | không có | `GAP` — BR-40: chỉ INSERT khi `sent_date_time >= NOW()`. Điều kiện cũ này nằm cạnh dòng vừa sửa; không TC nào xác nhận nó còn nguyên | `[MAJOR]` |
| G6 | Lối **APP mobile** (`EP-M15`) và `EP-38` cùng gọi `CalendarCourseBookingService::addActionRemind` | `diff code` | không có | `GAP` — spec §4.5 liệt kê điểm kích hoạt: `EP-P12 / EP-42 / EP-38 / EP-M15 / webhook`. TC chỉ phủ EP-69, EP-41, EP-42, EP-P12(web). Kho `TC-LSN-407` test đủ **5 lối** gồm *"Admin book ở APP"* + *"Admin approve ở APP"* | `[MAJOR]` |
| G7 | **Salon** (`CalendarSalonLineBookingService` / `CalendarSalonController`) — luồng Dev dùng làm **chuẩn đối chứng** cho tính đúng của fix | `diff code` | không có | `GAP` — mục 6 file 03 dựa vào "salon đã so đúng giờ kết thúc" để khẳng định quy tắc, nhưng không TC nào xác nhận salon thật sự còn đúng. Đối chứng chưa được kiểm chứng thì không dùng làm bằng chứng được (RULE-12 mục 2) | `[MAJOR]` |
| G8 | `type_remind = 2` (**đếm ngược**) không đổi hành vi — nửa sau của REQ-005 | `diff code` | `TC-REGSHARED001-01` (chỉ nhánh gửi-trước, `type_remind = 1`) | `RISK` — REQ-005 gồm 2 nhánh: gửi-trước **và** kiểu thời gian trôi qua. TC chỉ phủ nhánh 1. BR-39 quy định nhánh đếm ngược "sau" cũng cộng từ **giờ kết thúc** — đúng vùng biến `$time_end_booking` vừa được dùng lại | `[MAJOR]` |
| G9 | `BUG` — root cause (so mốc gửi với giờ BẮT ĐẦU thay vì KẾT THÚC) | `dev-impact` | `TC-TOOLKNOW002-01/02/03` | `RISK` — 3 TC phủ đúng nội dung và đã pass ở run #1099, **nhưng mang mã quan điểm `TOOL-KNOW-002` không có trong `framework/checklist-lme.md`** ⇒ theo BƯỚC 0.6 không tính là cover. TC tái hiện lõi của phiếu đang nằm ngoài lưới coverage | `[MAJOR]` |
| G10 | `D1 — event_step_time`: bản ghi sai tồn từ **trước** bản vá | `dev-impact` | `TC-TOOLOLDREC001-01` | `RISK` — TC chỉ chạy ở **local sạch**, kết quả `0 bản ghi` là hiển nhiên, không chứng minh gì về staging/production. Ngoài ra mã `TOOL-OLDREC-001` cũng không có trong checklist. Xem thêm §6 mục 4 (đã có sẵn command `recover:remindLesson`) | `[MAJOR]` |
| G11 | Môi trường thực thi của cả bộ TC | `diff code` | — | `RISK` — 9/9 TC chỉ pass ở `env = local` (run #1099). Run staging #1108 **đang `running`, chưa có kết quả**; `dev` 0 run; `prd` 0 run. Task chạm **job nền** ⇒ RULE-08 không cho kết luận từ local | `[MAJOR]` |

**Vùng đủ TC (không ghi chi tiết):** `F1` addActionRemindNew (EP-69) · `F2` addActionRemind (EP-41/42/P12) · `D1` chặn ghi mới · `T1` Lesson/Calendar Booking ở nhánh day=0.

---

## 2. Thiếu so với quan điểm test

**Kết luận: 12 quan điểm Trigger khớp task · 0 đủ chuẩn · 9 GAP · 3 RISK.**

| # | Mã quan điểm | Ưu tiên | Tình trạng | Severity |
|---|---|---|---|---|
| Q1 | `FUNC-DATE-001` — Ngày giờ sát ranh giới | Cao | `GAP` — **không TC nào mang mã này**, dù Trigger ghi thẳng *"tính toán / so sánh ngày giờ (hạn, N ngày sau, **remind**)"* — đây là quan điểm trung tâm của phiếu. Checklist đòi 00:00 / 24:00 · cuối tháng · 31/12→01/01 · timezone · năm khác nhau. Đúng chiều **00:00** là chỗ bug #712 chui ra | `[BLOCKER]` |
| Q2 | `MSG-002` — Tin đặt lịch / step gửi đúng thời điểm | Cao | `GAP` — checklist ghi rõ *"không bấm rồi kết luận ngay — phải chờ thời gian thật trôi qua hoặc chỉnh giờ server"*. 9/9 TC kết luận ngay tại bản ghi DB | `[BLOCKER]` |
| Q3 | `JOB-001` — Job nền / batch | Cao | `GAP` — `NewEventRemindTask` là job Spring Boot tiêu thụ đúng bảng vừa đổi hành vi ghi; 0 TC chạm job | `[BLOCKER]` |
| Q4 | `STATE-DEP-001` — Hành động phụ thuộc khi gốc thay đổi | Cao | `GAP` — Trigger khớp tuyệt đối: *"hành động phụ thuộc **đã lên lịch** (remind) và gốc có thể bị đổi"*. Không TC nào đổi mốc gửi / đổi khung giờ rồi kiểm `event_step_time` được tính lại. Trùng vùng G2 (EP-73) | `[BLOCKER]` |
| Q5 | `ENV-003` — Khác biệt dev / staging / production | Cao | `GAP` — 0 TC production, staging chưa có kết quả. Trigger: *"chạm … **job nền**"*. Không được đánh × với lý do "local đã pass" (RULE-08) | `[MAJOR]` |
| Q6 | `COMPAT-LEGACY-001` — Dữ liệu đời cũ chạy song song | Cao | `GAP` — Trigger liệt kê đích danh **`remind`**. Đòi test **cặp** cũ ⇄ mới. `TC-TOOLOLDREC001-01` chỉ đếm bản ghi cũ trên môi trường sạch, không dựng 2 thế hệ dữ liệu chạy song song (kho đã có `TC-LSN-415` làm đúng việc này) | `[MAJOR]` |
| Q7 | `REG-RUN-001` — Dữ liệu / job đang chạy dở khi release | Cao | `GAP` — deploy trong khi `event_step_time` còn bản ghi `status = 0` (chờ gửi) và `status = 1` (job vừa nhặt). BR-41 nói rõ bản ghi `status = 1` **vẫn sẽ được gửi**. Kho có `TC-LSN-413` | `[MAJOR]` |
| Q8 | `DATA-DB-001` ★ — WHERE scope + khóa mồ côi | Cao | `GAP` — Trigger: mọi chức năng có UPDATE/DELETE; mục 5 file 03 đề xuất **xoá** bản ghi sai. Checklist đòi dựng bản ghi trùng ở **2 bot** rồi xác nhận bot B không đổi. `TC-TOOLOLDREC001-01` chỉ SELECT trên 1 môi trường, không kiểm scope | `[MAJOR]` |
| Q9 | `UI-003` — Loading / rỗng / lỗi (→ Cao khi có rủi ro **false success**) | Cao | `GAP` — sau fix, admin thêm mốc 「コース終了 0日後の 15時00分」 vẫn thấy mốc hiện **bình thường** trong danh sách (TC-TOOLKNOW002-01 xác nhận đúng điều này) nhưng **không booking nào được lên lịch gửi** và **không có cảnh báo nào**. Đây đúng định nghĩa false success. Xem §6 mục 2 | `[MAJOR]` |
| Q10 | `REG-SHARED-001` — Shared code / logic | Cao | `RISK` — có 2 TC nhưng thiếu 3 nhánh trong chính danh sách 9 function Dev cung cấp: salon (G7), app mobile (G6), `type_remind = 2` (G8). Checklist ghi *"cặp Salon / Lesson / Booking Event là điểm lặp lại"*. Thiếu `Normal` (chỉ có Boundary + Abnormal) → RULE-01 | `[MAJOR]` |
| Q11 | `FUNC-001` — Luồng chính hoàn tất đúng đặc tả | Cao | `RISK` — chỉ **1 TC Normal**, thiếu Abnormal + Boundary, không ghi lý do (RULE-01). Checklist còn đòi khớp **3 điểm** DB / màn hình / output — TC dừng ở DB + màn hình | `[MAJOR]` |
| Q12 | `FUNC-004` — Giới hạn trên/dưới số ký tự, số lượng | Cao | `RISK` — **gắn nhầm mã**: Trigger là *"giới hạn số lượng / ký tự / dung lượng"*, còn 2 TC thực chất test **biên thời gian** (mốc gửi = giờ kết thúc) → thuộc `FUNC-DATE-001`. Ngoài ra chỉ có 2 `Boundary`, thiếu Normal + Abnormal (RULE-01) | `[MINOR]` |

---

## 3. TC trùng lặp nội dung

Đã rà **9/9 TC** theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`) — **không phát hiện trùng lặp cần xóa hoặc gộp**.

3 cụm dưới đây **giống nhau về oracle nhưng KHÁC nơi gọi**, nên giữ nguyên cả 3:

| Cụm | TC | Vì sao KHÔNG phải trùng |
|---|---|---|
| Mốc 15:00 < giờ kết thúc 17:00 → không lập lịch | `TC-TOOLKNOW002-02` (thêm booking thủ công, EP-41) · `TC-TOOLKNOW002-03` (duyệt yêu cầu, EP-42) · `TC-REGSHARED001-02` (khách LINE tự đặt, EP-P12) | 3 **caller khác nhau** của cùng `addActionRemind`. `REG-SHARED-001` yêu cầu *"test lại **từng mục**"* trong danh sách nơi ảnh hưởng. Xóa bất kỳ TC nào là mất cover 1 caller → **gate chống xóa bật** |
| Mốc = đúng giờ kết thúc 17:00 → vẫn lập lịch | `TC-FUNC004-01` (màn cấu hình, EP-69) · `TC-FUNC004-02` (thêm booking, EP-41) | Cùng biên nhưng khác điểm sửa (điểm 1 vs điểm 2 của diff). Giữ cả 2 |
| Backfill hàng loạt | `TC-TOOLKNOW002-01` (2 booking khác giờ kết thúc) · `TC-FUNC004-01` | Khác điều kiện: 1 TC so 2 booking, 1 TC so biên bằng nhau. Không phải tập con của nhau |

⚠️ **Không có DUP-INFLATE**, nhưng lưu ý ngược lại: 3 cụm trên khiến bộ TC **trông dày** ở nhánh day=0 trong khi 4 requirement (REQ-006/007/008/009) hoàn toàn trống — xem §1.

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | Bộ 9 TC / bug #712 | Bug **#712 → Redmine #40253** *"Khung tiếp nhận kết thúc lúc nửa đêm (22:00~24:00): remind sau khi kết thúc bị lập lịch trước cả giờ học"* do chính bộ TC này phát hiện ở run #582 (`tc-13766`). Bug đã `closed` trên Studio, nhưng **TC phát hiện nó không còn trong bộ 9 TC**, và REQ-009 có 0 TC. Journal Dev chỉ ghi *"không đổi so với hiện trạng"* — tức **chưa fix**, chỉ chấp nhận | Xác nhận với Leader/Dev #40253 đã xử lý chưa. Chưa xử lý → **giữ lại TC regression** cho nhánh nửa đêm (đã viết lại ở §5 `TC-FUNCDATE001-01`). Không được đóng #33762 khi TC phát hiện lỗi phụ đã biến mất khỏi bộ |
| 2 | `[MAJOR]` | 8/9 TC ở run #1099 | **RULE-02** — `artifacts: []`, chỉ `TC-TOOLOLDREC001-01` có 1 `audit.log`. Tất cả đều `Đạt` nhưng không có screenshot / log / bản ghi DB đính kèm. Nghịch lý: 2 run **thất bại** (#582, #671) lại có đủ screenshot + video + trace | Yêu cầu run lại có bật thu artifact cho các TC pass, tối thiểu: ảnh danh sách mốc gửi + kết quả query `event_step_time` |
| 3 | `[MAJOR]` | Toàn bộ task | **RULE-08 / ENV-003** — kết luận `aiResult = pass` đứng hoàn toàn trên `env = local`. Run staging #1108 vẫn `running` (bắt đầu 2026-09-09 03:42), `prd` 0 run. Task chạm job nền | Chờ #1108 xong rồi đánh giá lại; bổ sung `TC-ENV003-01` (§5) chạy trên production |
| 4 | `[MAJOR]` | `TC-TOOLKNOW002-01/02/03`, `TC-TOOLOLDREC001-01` | 4/9 TC mang mã quan điểm **không có trong `framework/checklist-lme.md`** (`TOOL-KNOW-002` ×3, `TOOL-OLDREC-001` ×1) — trong đó có TC **tái hiện lõi** của phiếu. Coverage không map được | Map lại sang mã chuẩn: 3 TC `TOOL-KNOW-002` → `FUNC-DATE-001`; `TOOL-OLDREC-001` → `COMPAT-LEGACY-001`. Hoặc bổ sung 2 mã này vào checklist kèm ngày + nguồn (RULE-10) |
| 5 | `[MAJOR]` | Coverage spec của Studio | Studio tự chấm **0/11 ref `covered`** — 10 `partial`, 1 `none`. Ref `none` là `EP-73`, đúng vùng bug còn tồn | Không đóng phiếu khi còn ref `none` thuộc cùng root cause; xem §1 G2 và §6 mục 3 |
| 6 | `[MAJOR]` | REQ-006 / 007 / 008 / 009 | **4/10 requirement của chính Studio có 0 TC**. Cả 4 đều `risk = Medium` và đều nằm trong vùng `dev_impact` khuyến nghị test kỹ | Đã đề xuất TC lấp ở §5 (`TC-FUNCDATE001-01/02/03`, `TC-STATEDEP001-01`) |
| 7 | `[MINOR]` | Toàn bộ 9 TC | 9/9 TC ở `status = draft`, task `reviewed = false`, `reviewState = leader` — bộ TC chưa qua duyệt nào | Duyệt trên Studio sau khi xử lý §1/§2 |
| 8 | `[MINOR]` | `redmineWriteback` | Nội dung ghi ngược về Redmine gộp **cả 5 run**: *"pass 19 · fail 0 · error 6"*. Người đọc Redmine dễ hiểu là bộ TC có 25 case, hoặc còn 6 case lỗi | Ghi rõ số liệu của **run mới nhất** (#1099: 9/9 pass, local) + trạng thái run staging đang chạy |

### Chất lượng từng TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 9 | `[MAJOR]` | `TC-TOOLKNOW002-03` | **Tiền đề không dựng lại được** (BƯỚC 4a #2): ghi *"Như testcase duyệt yêu cầu với mốc gửi 18:00"* — **TC đó không tồn tại** trong bộ 9 TC. Người test mới đọc không biết dựng gì | Viết lại tiền đề đầy đủ, tự đứng độc lập (giống `TC-TOOLKNOW002-02`) |
| 10 | `[MAJOR]` | `FUNC-001`, `FUNC-004`, `REG-SHARED-001` | **RULE-01** — cả 3 quan điểm ưu tiên Cao đều thiếu loại case và **không TC nào ghi lý do**: `FUNC-001` chỉ Normal · `FUNC-004` chỉ 2 Boundary · `REG-SHARED-001` chỉ Boundary + Abnormal. Phân bố toàn bộ: 4 Abnormal / 3 Boundary / **2 Normal** | Bổ sung theo §5, hoặc ghi lý do vào `Ghi chú` của TC |
| 11 | `[MAJOR]` | 9/9 TC | **RULE-06 + RULE-07** — mọi `Kết quả mong đợi` dừng ở bản ghi `event_step_time` + danh sách mốc gửi trên màn admin. Không TC nào đi tới output cuối chuỗi (tin LINE trên máy thật). Với bug mà triệu chứng là "khách nhận tin sai giờ", đây là chỗ hụt lớn nhất | `TC-MSG002-01/02/03` ở §5 |
| 12 | `[MAJOR]` | `[AP-3]` Happy-path-only regression | `T2` (Reminder Delivery) có **0 TC**; `T1` có TC nhưng mọi tiền đề đều là data sạch, booking mới, 1 bot, không edge state (booking đã hủy, friend block bot, bot hết hạn, khung qua nửa đêm) | Bổ sung edge state — xem `TC-MSG002-03` (§5) và dùng lại `TC-LSN-399` của kho |
| 13 | `[MINOR]` | `TC-FUNC004-01/02` | Gắn `FUNC-004` (giới hạn số lượng/ký tự) cho case **biên thời gian** → sai lưới quan điểm, làm `FUNC-DATE-001` trông như không cần | Đổi `Mã quan điểm liên kết` sang `FUNC-DATE-001` |
| 14 | `[NIT]` | `TC-TOOLOLDREC001-01` | Chạy ở local sạch, `expected` chấp nhận `0 bản ghi` — TC luôn pass ở môi trường mới dựng, không phân biệt được "đã dọn sạch" với "chưa từng có dữ liệu". Ghi chú của TC còn tự nêu *"RULE-04 … KHÔNG có trong danh mục tri thức của lượt này"* trong khi RULE-04 là rule bắt buộc của repo | Chạy trên môi trường **có dữ liệu thật** (staging/production) và tách rõ 2 kết luận. RULE-04 đòi thống kê **toàn hệ thống** trước khi đóng ticket |

> **Không dính:** AP-1 (fix không phải generic catch — đúng 2 dòng đổi điều kiện so sánh) · AP-2 (phiếu có đủ expected/actual cụ thể) · AP-4 (có `diffStat` từ Studio, verify được fix shape) · AP-5 (không TC nào over-test layer không bị chạm) · AP-6 (mục 3 file 03 có đủ 9 function).
> **Spec:** đã tìm được — `spec-features/admin/lesson-booking/feature-spec.md` §4.5 + §8.7 (BR-37…BR-43, BR-P30). Không phát sinh dòng `[MAJOR]` thiếu spec.

---

## 5. TCs đề xuất bổ sung (10)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa019-datlichbaihoc-レッスン予約.md` (632 TC) — nhóm 「リマインド — cài đặt」 (`TC-LSN-381`…`396`) + 「リマインド — job gửi & recover」 (`TC-LSN-397`…`415`) |
| Vùng regression phát hiện từ kho | `TC-LSN-407` (SpecChange #32367 — remind SAU theo END time, **5 lối vào** gồm APP) · `TC-LSN-402` (backfill theo 6 trạng thái booking) · `TC-LSN-403` (sửa timing → tính lại `event_step_time`) · `TC-LSN-413` (restart job khi còn bản ghi `status = 1`) · `TC-LSN-399` (friend block bot) |
| Conflict expected vs kho | **Không** — `TC-LSN-407` / `TC-LSN-410` khẳng định cùng quy tắc "remind SAU tính theo END time", thống nhất với bản vá |
| GAP dùng lại TC kho (không viết mới) | `G10` → **`TC-LSN-410`** *"recover step gửi SAU của booking CŨ"* + **`TC-LSN-411`** *"booking MỚI → command recover KHÔNG đụng tới"* + **`TC-LSN-415`** *"dữ liệu trước/sau #32367 cùng tồn tại"* · `Q7` → **`TC-LSN-413`** · `G6` mở rộng từ **`TC-LSN-407`** (2 lối APP) |
| Xác nhận chống trùng | Đã đối chiếu **9 TC** ở BƯỚC 0 + kho FA-019 — **không TC đề xuất nào trùng** |

> ⚠️ **Phát hiện quan trọng khi đối chiếu kho**: đã tồn tại command **`recover:remindLesson`** (Support #33648, kho `TC-LSN-409`…`412`). `TC-LSN-410` tổ hợp 3 mô tả đúng loại dữ liệu sai mà bug #33762 sinh ra (`day=0`, mốc gửi > giờ bắt đầu nhưng < giờ kết thúc) và quy định *"nếu send time < end time thì **XÓA** bản ghi remind"*. Mục 5 file 03 lại đề xuất SELECT + xoá tay như thể chưa có công cụ. → xem §6 mục 4.

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-FUNCDATE001-01 | UI | FUNC-DATE-001 | リマインド — cài đặt | Boundary | manual | Tất cả | Khung tiếp nhận kết thúc lúc nửa đêm (22:00~24:00) — mốc gửi sau khi kết thúc không được lập lịch trước giờ học | - Admin bot test, lịch đặt bài học đang ON<br>- Khung tiếp nhận NGÀY MAI **22:00 - 00:00** (danh sách 受付枠 hiển thị 「22:00~24:00」)<br>- 1 đơn đặt lịch đang hiệu lực của LINE friend test trên khung đó<br>- Chưa có mốc gửi nào của khối 「コース終了後」 | 1. Mở chi tiết lịch đặt → tab 「全体設定」 → 「予約前後に送るリマインドメッセージ」<br>2. Ở khối 「コース終了後のメッセージ・アクション」 bấm 「送信タイミングを追加する」<br>3. Chọn 「日時で指定」, số ngày = 0, giờ **15:00**, bấm 「決定」<br>4. Xem danh sách mốc gửi vừa tạo<br>5. Mở dữ liệu lịch gửi của đơn đặt lịch đó, ghi lại thời điểm gửi | Khung: ngày mai 22:00 - 00:00. Mốc gửi: sau khi kết thúc, kiểu ngày, day = 0, 15:00 | Mốc gửi 15:00 **KHÔNG** được lập lịch cho đơn này. Đặc biệt KHÔNG được sinh bản ghi có thời điểm gửi **15:00 cùng ngày đặt lịch** — tức trước cả lúc lớp bắt đầu (22:00). Nếu vẫn sinh bản ghi ⇒ tái hiện lại đúng Redmine **#40253** |  | Lấp `G3` · `Q1` · Đánh giá spec: Đã hỏi leader (REQ-009 chưa chốt) · Evidence: ảnh danh sách mốc gửi + kết quả tra lịch gửi · **regression — thay cho TC `tc-13766` đã bị gỡ khỏi bộ** |
| TC-FUNCDATE001-02 | UI | FUNC-DATE-001 | リマインド — cài đặt | Normal | manual | Tất cả | Mốc gửi sau khi kết thúc N ngày (N > 0) vẫn được lập lịch bình thường | - Khung tiếp nhận NGÀY MAI 12:00 - 17:00, có 1 đơn đặt lịch đang hiệu lực<br>- Chưa có mốc gửi nào của khối 「コース終了後」 | 1. Vào 「予約前後に送るリマインドメッセージ」 → khối 「コース終了後」 → 「送信タイミングを追加する」<br>2. Chọn 「日時で指定」, số ngày = **1**, giờ **09:00**, bấm 「決定」<br>3. Tra lịch gửi của đơn đặt lịch<br>4. Lặp lại bước 1-3 với số ngày = **3**, giờ **08:00** | Khung: ngày mai 12:00 - 17:00. Mốc gửi 1: day = 1, 09:00. Mốc gửi 2: day = 3, 08:00 | Cả 2 mốc đều tạo đúng 1 bản ghi lịch gửi, trạng thái chưa gửi, thời điểm gửi = ngày đặt lịch **+1 ngày 09:00** và **+3 ngày 08:00**. Bản vá **không** chặn nhầm nhóm N > 0 dù giờ trong ngày (09:00 / 08:00) nhỏ hơn giờ kết thúc 17:00 |  | Lấp `G4` · `Q1` · Đánh giá spec: Spec ghi rõ (BR-38) · Evidence: kết quả tra lịch gửi của cả 2 mốc |
| TC-FUNCDATE001-03 | UI | FUNC-DATE-001 | リマインド — cài đặt | Abnormal | manual | Tất cả | Mốc gửi muộn hơn giờ kết thúc nhưng đã trôi qua so với hiện tại — vẫn không được lập lịch | - Khung tiếp nhận **HÔM NAY** với giờ kết thúc đã qua, ví dụ 08:00 - 09:00 (thực hiện lúc quá 12:00)<br>- 1 đơn đặt lịch đang hiệu lực trên khung đó | 1. Vào 「予約前後に送るリマインドメッセージ」 → khối 「コース終了後」 → 「送信タイミングを追加する」<br>2. Chọn 「日時で指定」, số ngày = 0, giờ **10:00** (muộn hơn giờ kết thúc 09:00 nhưng đã trôi qua)<br>3. Bấm 「決定」, xem danh sách mốc gửi<br>4. Tra lịch gửi của đơn đặt lịch | Khung: hôm nay 08:00 - 09:00. Mốc gửi: day = 0, 10:00. Thời điểm thao tác: sau 12:00 cùng ngày | **KHÔNG** có bản ghi lịch gửi nào được tạo. Điều kiện cũ "chỉ lập lịch khi thời điểm gửi còn ở tương lai" vẫn còn nguyên sau bản vá — không bị dòng sửa làm mất |  | Lấp `G5` · `Q1` · Đánh giá spec: Spec ghi rõ (BR-40) · Evidence: kết quả tra lịch gửi + ảnh giờ hệ thống |
| TC-MSG002-01 | UI | MSG-002 | リマインド — job gửi & recover | Normal | manual | product | Mốc gửi sau giờ kết thúc — chờ tới giờ thật, khách LINE nhận đúng tin remind | - Bot production đang hoạt động, có LINE friend test đã kết bạn và **không** block bot<br>- Khung tiếp nhận hôm nay, giờ kết thúc cách hiện tại khoảng 15 phút<br>- Mốc gửi khối 「コース終了後」, kiểu ngày, day = 0, giờ = **giờ kết thúc + 10 phút**<br>- 1 đơn đặt lịch đang hiệu lực của friend test<br>- Chuẩn bị điện thoại thật đã đăng nhập LINE bằng friend test | 1. Xác nhận mốc gửi đã hiển thị trong danh sách 「コース終了後」<br>2. Ghi lại giờ hệ thống và giờ dự kiến gửi<br>3. **Chờ trôi qua thời điểm gửi**, không thao tác gì thêm<br>4. Mở LINE trên điện thoại thật của friend test, xem tin nhận được<br>5. Ghi lại giờ nhận tin hiển thị trên LINE<br>6. Đối chiếu giờ nhận tin với giờ cấu hình, tính sai số | Giờ kết thúc khung: `<T>`. Mốc gửi: day = 0, `<T + 10 phút>` | Khách nhận **đúng 1** tin remind trên LINE app thật. Giờ nhận tin lệch so với mốc cấu hình trong khoảng chấp nhận của job (ghi rõ sai số đo được). Nội dung tin là mẫu remind sau khi kết thúc, không phải mẫu remind trước |  | Lấp `G1` · `Q2` · `Q3` · RULE-06 · RULE-08 (job nền → bắt buộc production) · Đánh giá spec: Spec ghi rõ (spec §4.5) · Evidence: ảnh chụp LINE app thật kèm giờ nhận + log giờ gửi |
| TC-MSG002-02 | UI | MSG-002 | リマインド — job gửi & recover | Boundary | manual | product | Mốc gửi trùng đúng giờ kết thúc — tin vẫn được gửi tới khách tại đúng thời điểm đó | - Như TC-MSG002-01 nhưng mốc gửi đặt **trùng đúng giờ kết thúc** của khung tiếp nhận<br>- Giờ kết thúc cách thời điểm bắt đầu test khoảng 15 phút | 1. Xác nhận mốc gửi 「コース終了 0日後の `<giờ kết thúc>`」 hiển thị trong danh sách<br>2. Ghi lại giờ dự kiến gửi<br>3. Chờ trôi qua giờ kết thúc<br>4. Mở LINE trên máy thật của friend test, ghi lại giờ nhận tin | Giờ kết thúc khung = mốc gửi = `<T>`. day = 0 | Khách **có** nhận tin remind — trường hợp bằng nhau không bị bản vá loại bỏ, và đi được tới tận LINE chứ không chỉ dừng ở bản ghi lịch gửi. Đây là biên thật của điều kiện trong bản vá |  | Lấp `G1` · `Q2` · RULE-06 · RULE-08 · Đánh giá spec: Spec ghi rõ · Evidence: ảnh LINE app thật kèm giờ nhận |
| TC-MSG002-03 | UI | MSG-002 | リマインド — job gửi & recover | Abnormal | manual | product | Đơn đặt lịch bị hủy sau khi remind đã lên lịch — khách không được nhận tin | - Như TC-MSG002-01, mốc gửi đã lên lịch thành công cho đơn đặt lịch của friend test<br>- Thời điểm gửi còn cách hiện tại vài phút | 1. Xác nhận lịch gửi đã được tạo cho đơn đặt lịch<br>2. Vào danh sách booking, admin **hủy** đơn đặt lịch đó<br>3. Chờ trôi qua thời điểm gửi đã cấu hình<br>4. Mở LINE trên máy thật của friend test, kiểm có tin remind nào tới không<br>5. Tra lại dữ liệu lịch gửi của đơn vừa hủy | Đơn đặt lịch bị admin hủy trước thời điểm gửi vài phút | Khách **KHÔNG** nhận tin remind nào. Bản ghi lịch gửi chưa gửi của đơn đã bị dọn khi hủy. Nếu khách vẫn nhận tin ⇒ raise ticket riêng: remind của booking đã hủy vẫn gửi |  | Lấp `G1` · `Q2` · `[AP-3]` edge state · RULE-06 · RULE-08 · Đánh giá spec: Spec ghi rõ (spec §4.5 bảng dọn hàng đợi) · Evidence: ảnh LINE app không có tin + kết quả tra lịch gửi |
| TC-STATEDEP001-01 | UI | STATE-DEP-001 | リマインド — cài đặt | Abnormal | manual | Tất cả | Sửa mốc gửi đã có sang giờ sớm hơn giờ kết thúc — lịch gửi cũ phải được tính lại theo cùng quy tắc | - Khung tiếp nhận NGÀY MAI 12:00 - 17:00, có 1 đơn đặt lịch đang hiệu lực<br>- Đã có sẵn mốc gửi khối 「コース終了後」, kiểu ngày, day = 0, giờ **18:00**, và đơn đã có 1 bản ghi lịch gửi lúc 18:00 | 1. Vào 「予約前後に送るリマインドメッセージ」, tìm mốc gửi 18:00 đang có<br>2. Mở chỉnh sửa mốc gửi, đổi giờ từ 18:00 sang **15:00** (sớm hơn giờ kết thúc 17:00), lưu lại<br>3. Xem danh sách mốc gửi đã cập nhật<br>4. Tra lại dữ liệu lịch gửi của đơn đặt lịch<br>5. Đổi ngược mốc gửi về **18:00**, lưu, rồi tra lại lần nữa | Mốc gửi ban đầu: day = 0, 18:00 → sửa thành 15:00 → sửa lại 18:00 | Sau bước 2-4: bản ghi lịch gửi lúc 18:00 phải bị **xóa hoặc không còn hiệu lực**, vì mốc mới 15:00 sớm hơn giờ kết thúc 17:00 — cùng quy tắc với luồng THÊM. Sau bước 5: lịch gửi được tạo lại đúng 18:00. ⚠️ Nếu bản ghi 18:00 vẫn còn hoặc bản ghi 15:00 được tạo ⇒ **đúng lỗ hổng EP-73 Studio đã cảnh báo**, raise ticket riêng |  | Lấp `G2` · `Q4` · Đánh giá spec: Đã hỏi leader (Dev để EP-73 ngoài scope) · Evidence: kết quả tra lịch gửi trước/sau mỗi lần sửa · dẫn từ `TC-LSN-403` (kho chỉ test nhánh "quá hạn", chưa có nhánh giờ kết thúc) |
| TC-REGSHARED001-03 | UI | REG-SHARED-001 | リマインド — job gửi & recover | Normal | manual | Tất cả | Thêm booking thủ công trên **APP mobile** — mốc gửi sớm hơn giờ kết thúc thì không lập lịch | - Đã cài app quản trị mobile và đăng nhập đúng bot test<br>- Khung tiếp nhận NGÀY MAI 12:00 - 17:00, còn chỗ trống<br>- Mốc gửi khối 「コース終了後」 đã có sẵn: kiểu ngày, day = 0, giờ **15:00**<br>- 1 LINE friend test đang hoạt động, hiển thị được trong màn chọn khách của app<br>- Chưa có đơn đặt lịch nào của friend này trên khung | 1. Mở **app mobile**, chọn đúng bot kiểm thử<br>2. Vào lịch đặt bài học → tìm khung tiếp nhận ngày mai 12:00 - 17:00, mở chi tiết khung<br>3. Thêm booking thủ công, ở mục chọn khách chọn **LINE friend test** — không dùng phương án nhập tên khách trực tiếp<br>4. Xác nhận đăng ký, chờ màn đóng lại<br>5. Xác nhận đơn vừa thêm hiện trong danh sách booking của khung, ghi lại mã đơn<br>6. Tra dữ liệu lịch gửi của đơn vừa tạo | Khung: ngày mai 12:00 - 17:00. Mốc gửi có sẵn: sau khi kết thúc, kiểu ngày, day = 0, 15:00. Thao tác **thêm booking trên app mobile**, không phải web | Đơn đặt lịch được thêm thành công và hiển thị bình thường trong danh sách booking của khung. **KHÔNG** có bản ghi lịch gửi nào được tạo cho đơn này, vì mốc gửi 15:00 sớm hơn giờ kết thúc 17:00. Kết quả phải trùng khớp với lối thêm booking trên web (`TC-TOOLKNOW002-02`) — lối app gọi chung hàm đã sửa nên không được lệch hành vi |  | Lấp `G6` · `Q10` · regression · Đánh giá spec: Spec ghi rõ (spec §4.5 `EP-M15`) · Evidence: ảnh màn app + kết quả tra lịch gửi · dẫn từ `TC-LSN-407` (kho có lối 「Admin book ở APP」) |
| TC-REGSHARED001-04 | UI | REG-SHARED-001 | リマインド — cài đặt | Normal | manual | Tất cả | Mốc gửi kiểu **đếm ngược** (thời gian trôi qua) không bị bản vá làm đổi hành vi | - Khung tiếp nhận NGÀY MAI 12:00 - 17:00, có 1 đơn đặt lịch đang hiệu lực<br>- Chưa có mốc gửi nào | 1. Vào 「予約前後に送るリマインドメッセージ」 → khối 「コース終了後」 → 「送信タイミングを追加する」<br>2. Chọn kiểu **đếm ngược / thời gian trôi qua** (không phải 「日時で指定」), đặt **1 giờ** sau khi kết thúc, bấm 「決定」<br>3. Tra lịch gửi của đơn đặt lịch<br>4. Sang khối 「コース開始前」, thêm mốc kiểu đếm ngược **1 giờ** trước khi bắt đầu, bấm 「決定」<br>5. Tra lịch gửi của mốc thứ hai | Khung: ngày mai 12:00 - 17:00. Mốc 1: đếm ngược, 1 giờ **sau khi kết thúc**. Mốc 2: đếm ngược, 1 giờ **trước khi bắt đầu** | Mốc 1 tạo lịch gửi lúc **18:00** (giờ kết thúc 17:00 + 1 giờ). Mốc 2 tạo lịch gửi lúc **11:00** (giờ bắt đầu 12:00 − 1 giờ). Hành vi nhánh đếm ngược giữ nguyên như trước bản vá |  | Lấp `G8` · `Q10` · regression · Đánh giá spec: Spec ghi rõ (BR-39) · Evidence: kết quả tra lịch gửi của cả 2 mốc |
| TC-DATADB001-01 | Data | DATA-DB-001 | リマインド — job gửi & recover | Abnormal | manual | product | Rà và dọn lịch gửi sai của bot A không được chạm dữ liệu bot B | - Có ít nhất **2 bot** cùng dùng lịch đặt bài học, mỗi bot có mốc gửi sau khi kết thúc kiểu ngày và có booking đang hiệu lực<br>- Ghi nhận trước số lượng lịch gửi chưa gửi của **cả 2 bot** | 1. Chạy rà soát lịch gửi sai (mốc gửi sớm hơn giờ kết thúc, chưa gửi) trên **toàn hệ thống**, ghi lại số bản ghi theo từng bot<br>2. Xử lý dọn dữ liệu sai **chỉ cho bot A** theo phương án Leader chốt (xem §6 mục 4)<br>3. Đếm lại lịch gửi chưa gửi của bot A và bot B<br>4. Mở màn cấu hình remind + danh sách booking của **bot B**, kiểm hiển thị bình thường<br>5. Chờ tới mốc gửi hợp lệ của bot B, kiểm khách của bot B vẫn nhận tin | 2 bot, mỗi bot ≥ 1 booking có lịch gửi chưa gửi | Bước 1 cho ra thống kê **toàn hệ thống** theo bot (RULE-04). Sau bước 2: chỉ bản ghi sai của **bot A** bị xóa; số lịch gửi của **bot B không đổi**. Bot B mở màn bình thường, không lỗi, và khách bot B vẫn nhận remind đúng giờ ⇒ điều kiện lọc khi dọn dữ liệu có đủ phạm vi theo bot |  | Lấp `G10` · `Q8` · RULE-04 (thống kê toàn hệ thống trước khi đóng ticket) · RULE-07 · Đánh giá spec: Đã hỏi leader · Evidence: bảng đếm trước/sau của cả 2 bot + ảnh màn bot B |

**Dùng lại TC kho — không viết mới:**

| GAP | TC kho dùng lại | Cần chỉnh gì cho bug này |
|---|---|---|
| `G10` (dữ liệu sai tồn từ trước) | `TC-LSN-410` *"Support #33648: recover step gửi SAU của booking CŨ"* · `TC-LSN-411` *"booking MỚI → command recover KHÔNG đụng tới"* | Chạy nguyên như kho. Tổ hợp 3 của `TC-LSN-410` chính là dữ liệu sai mà #33762 sinh ra ⇒ dùng để chốt xem `recover:remindLesson` có dọn được không, thay cho SELECT + xoá tay ở mục 5 file 03 |
| `Q6` (COMPAT-LEGACY-001) | `TC-LSN-415` *"Dữ liệu tạo TRƯỚC và SAU spec change #32367 cùng tồn tại"* | Đổi mốc so sánh từ release #32367 sang **ngày deploy `ai_fixbug_33762`**; giữ nguyên các bước và expected |
| `Q7` (REG-RUN-001) | `TC-LSN-413` *"Restart job gửi remind trong lúc đang có bản ghi status = 1"* | Chạy trước/sau khi deploy bản vá lên môi trường đích |
| `[AP-3]` edge state | `TC-LSN-399` *"Friend bị block / block bot → vẫn insert event_step_time nhưng KHÔNG gửi"* | Ghép chung buổi test với `TC-MSG002-03` |

**GAP KHÔNG đề xuất TC — Leader đã quyết loại (2026-09-09):**

| GAP / quan điểm | TC đã bỏ | Hệ quả còn lại |
|---|---|---|
| `G7` — Salon là chuẩn đối chứng của bản vá | ~~`TC-REGSHARED001-05`~~ (đặt lịch salon) | **GAP vẫn mở.** Mục 6 file 03 dùng "salon đã so đúng giờ kết thúc" làm bằng chứng cho tính đúng của fix, nhưng không còn TC nào kiểm chứng salon. Bằng chứng này giữ nguyên trạng thái **chưa được verify** khi đóng phiếu |
| `G11` + `Q5` — ENV-003 / RULE-08 | ~~`TC-ENV003-01`~~ (chạy lại bộ case lõi trên production) | **GAP vẫn mở.** Kết luận `pass` của phiếu tiếp tục chỉ đứng trên `env = local`; `prd` 0 run. Task chạm job nền nên RULE-08 chưa được thỏa. Cần Leader ghi phiếu xác nhận không verify được trên production + phương án giám sát sau release (đúng yêu cầu Evidence của `ENV-003`) |
| `G6` — lối APP mobile | ~~`TC-REGSHARED001-04`~~ (duyệt yêu cầu trên app) | **GAP thu hẹp, chưa đóng.** Còn `TC-REGSHARED001-03` phủ lối *thêm booking* trên app; lối *duyệt yêu cầu* trên app (`EP-M15`) không còn TC — trong khi kho `TC-LSN-407` tách riêng đúng 2 lối này |

---

## 6. Spec update needed

| # | Vấn đề | Cần chốt |
|---|---|---|
| 1 | **REQ-009 — khung tiếp nhận kết thúc lúc nửa đêm.** Requirement của Studio tự ghi *"Cần xác định đây là hạn chế chấp nhận được hay hồi quy phải sửa"*; bug #712 → Redmine **#40253** đã được mở và `closed` trên Studio; journal Dev chỉ ghi *"không đổi so với hiện trạng"*. Với `end_time = 00:00`, bản vá lập lịch remind **trước cả giờ lớp bắt đầu** — hành vi này tệ hơn cả bug gốc | Leader/Dev chốt: #40253 là **chấp nhận** hay **phải sửa**? Chấp nhận → ghi vào spec §8.7 làm hạn chế đã biết. Phải sửa → không đóng #33762 trước khi có bản vá cho nhánh nửa đêm |
| 2 | **False success ở màn cấu hình remind.** Sau fix, admin thêm mốc 「コース終了 0日後の 15時00分」 vẫn thấy mốc hiển thị bình thường trong danh sách, nhưng **không booking nào được lên lịch gửi** và **không có cảnh báo nào**. Spec BR-40 có cờ `is_send_remind_real_time` phản ánh trạng thái này nhưng UI không dùng | Spec chưa định nghĩa UI phải làm gì. Chốt: hiển thị cảnh báo khi mốc gửi rơi vào giữa khung tiếp nhận, hay chấp nhận im lặng? Nếu chấp nhận → ghi rõ vào spec để tester không coi là bug |
| 3 | **EP-73 — luồng CẬP NHẬT mốc gửi thiếu cùng kiểm tra.** Studio chấm `EP-73 level = none` và ghi *"cùng bug, Dev để NGOÀI scope"*. Sau bản vá: THÊM mốc thì chặn `mốc < giờ kết thúc`, SỬA mốc thì không ⇒ cùng một rule, 2 hành vi khác nhau tùy đường vào | Chốt: sửa luôn trong #33762, hay tách ticket riêng? Nếu tách → tạo ticket ngay và ghi liên kết vào #33762, đừng để rơi. Spec §8.7 BR-42 cũng cần bổ sung chiều "giờ kết thúc" |
| 4 | **Phương án recover mâu thuẫn với công cụ đã có.** Mục 5 file 03 đề xuất SELECT thủ công rồi *"xoá nếu xác nhận sai"*. Nhưng kho `TC-LSN-409`…`412` cho thấy đã tồn tại command **`recover:remindLesson`** (Support #33648) và `TC-LSN-410` tổ hợp 3 quy định *"nếu send time < end time thì XÓA bản ghi remind"* — đúng loại dữ liệu #33762 sinh ra | Chốt: dùng `recover:remindLesson` hay dọn tay? Nếu command đã đủ → bỏ phương án xoá tay khỏi file 03 (xoá tay trên production rủi ro hơn hẳn). Nếu command **không** phủ được case này → ghi rõ vì sao, và bổ sung vào spec phạm vi của command |
| 5 | **`type_remind = 2` sau khi kết thúc dùng chung biến `$time_end_booking` vừa được dùng lại.** BR-39 quy định nhánh đếm ngược "sau" = giờ kết thúc + HH:mm. Dev khẳng định nhánh này "vốn đã dùng end_time" nên không đổi, nhưng không có TC nào chứng minh | Không phải sửa spec — chỉ cần xác nhận bằng `TC-REGSHARED001-05` (§5). Nếu kết quả lệch BR-39 thì mới đưa lên spec |

---

> Report này là **draft cho Leader verify**, không phải kết luận cuối. Nguồn phân tích: Studio task #233 (`testcase_list` 9 TC · `task_get_context` dev_impact + spec_delta · `task_get_report` 5 run + bug #712) · `01-bug-task.md` · `03-dev-impact.md` · `spec-features/admin/lesson-booking/feature-spec.md` §4.5 + §8.7 · `kho-tcs/fa019-datlichbaihoc-レッスン予約.md`.
