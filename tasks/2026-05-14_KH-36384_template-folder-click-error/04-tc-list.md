# 04 — TC List (do member viết)

> Auto-imported từ Google Sheet master sheet "Improve list template" rows 574-629 (gid=828838454, cột "Bug KH #36384"). Source: Ngô Thúy Ngần — Redmine journal #118361 ngày 2026-05-14.

## Thông tin

| Trường | Giá trị |
|---|---|
| Tester viết TCs | Ngô Thúy Ngần |
| Ngày submit | 2026-05-14 |
| Version TCs | v1 |
| Link TC gốc | https://docs.google.com/spreadsheets/d/131XoTI-AM4S6OwKbwcFlMnNJr0jVtbmpcOnf1BkiScs/edit?gid=828838454#gid=828838454 (rows 574-629, cột "Bug KH #36384") |

---

## TC List

> 56 TCs. Title flatten từ hierarchy `Main Function > Sub1 > Sub2 > Sub3 > Sub4 > Sub5`. Steps được suy luận theo flow manual tester (UI), không dẫn bằng DB. Status="OK" lấy từ cột test result của Ngần. Sheet row được ghi cuối Title để dễ trace ngược.

| TC ID | Title | Type | Priority | Precondition | Steps | Expected result | Output note | Assignee | Status |
|---|---|---|---|---|---|---|---|---|---|
| TC001 | Check tạo/edit/copy template ở folder default > Tạo mới template > Tạo mới template group (sheet R574) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Nhập tên group (1-15 ký tự), Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC002 | Check tạo/edit/copy template ở folder default > Tạo mới template > Tạo mới các template con của group > Tạo tmp text (sheet R575) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp text**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC003 | Check tạo/edit/copy template ở folder default > Tạo mới template > Tạo mới các template con của group > Tạo tmp button > button standard > - Check tạo 1 panel và nhiều pane / - Check tạo button có ảnh và không ảnh (sheet R576) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp button**<br>4. Chọn variant: **button standard**<br>5. Thực hiện: - Check tạo 1 panel và nhiều pane / - Check tạo button có ảnh và không ảnh<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC004 | Check tạo/edit/copy template ở folder default > Tạo mới template > Tạo mới các template con của group > Tạo tmp button > button color (sheet R577) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp button**<br>4. Chọn variant: **button color**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC005 | Check tạo/edit/copy template ở folder default > Tạo mới template > Tạo mới các template con của group > Tạo tmp button > button image (sheet R578) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp button**<br>4. Chọn variant: **button image**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC006 | Check tạo/edit/copy template ở folder default > Tạo mới template > Tạo mới các template con của group > Tạo tmp button > button quick reply (sheet R579) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp button**<br>4. Chọn variant: **button quick reply**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC007 | Check tạo/edit/copy template ở folder default > Tạo mới template > Tạo mới các template con của group > Tạo tmp image thường (sheet R580) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp image thường**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC008 | Check tạo/edit/copy template ở folder default > Tạo mới template > Tạo mới các template con của group > Tạo tmp image map (sheet R581) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp image map**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC009 | Check tạo/edit/copy template ở folder default > Tạo mới template > Tạo mới các template con của group > Tạo tmp video (sheet R582) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp video**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC010 | Check tạo/edit/copy template ở folder default > Tạo mới template > Tạo mới các template con của group > Tạo tmp audio (sheet R583) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp audio**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC011 | Check tạo/edit/copy template ở folder default > Tạo mới template > Tạo mới các template con của group > Tạo tmp sticker (sheet R584) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp sticker**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC012 | Check tạo/edit/copy template ở folder default > Tạo mới template > Tạo mới các template con của group > Tạo tmp location (sheet R585) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp location**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC013 | Check tạo/edit/copy template ở folder default > Edit template > Edit template group > - edit tên template / - edit folder (sheet R586) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Mở template group → click Edit<br>3. Sửa: - edit tên template / - edit folder<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC014 | Check tạo/edit/copy template ở folder default > Edit template > Edit các template con của group > Edit tmp text (sheet R587) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp text<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC015 | Check tạo/edit/copy template ở folder default > Edit template > Edit các template con của group > Edit tmp button > button standard > - Check tạo 1 panel và nhiều pane / - Check tạo button có ảnh và không ảnh (sheet R588) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp button<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC016 | Check tạo/edit/copy template ở folder default > Edit template > Edit các template con của group > Edit tmp button > button color (sheet R589) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp button<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC017 | Check tạo/edit/copy template ở folder default > Edit template > Edit các template con của group > Edit tmp button > button image (sheet R590) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp button<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC018 | Check tạo/edit/copy template ở folder default > Edit template > Edit các template con của group > Edit tmp button > button quick reply (sheet R591) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp button<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC019 | Check tạo/edit/copy template ở folder default > Edit template > Edit các template con của group > Edit tmp image thường (sheet R592) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp image thường<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC020 | Check tạo/edit/copy template ở folder default > Edit template > Edit các template con của group > Edit tmp image map (sheet R593) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp image map<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC021 | Check tạo/edit/copy template ở folder default > Edit template > Edit các template con của group > Edit tmp video (sheet R594) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp video<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC022 | Check tạo/edit/copy template ở folder default > Edit template > Edit các template con của group > Edit tmp audio (sheet R595) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp audio<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC023 | Check tạo/edit/copy template ở folder default > Edit template > Edit các template con của group > Edit  tmp sticker (sheet R596) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Mở template group → click Edit<br>3. Sửa: Edit  tmp sticker<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC024 | Check tạo/edit/copy template ở folder default > Edit template > Edit các template con của group > Edit tmp location (sheet R597) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp location<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC025 | Check tạo/edit/copy template ở folder default > Edit template > Edit các template đơn (template dạng cũ không có group) (sheet R598) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Mở template đơn (cũ, không thuộc group)<br>3. Click Edit → sửa tên / nội dung → click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC026 | Check tạo/edit/copy template ở folder default > Check copy template > Copy template group (sheet R599) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Hover template group → click button **Copy / コピー**<br>3. Quan sát template copy được tạo, mở thử<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Copy được template thành công<br>- Template được copy hiển thị ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC027 | Check tạo/edit/copy template ở folder default > Check copy template > Copy template đơn (sheet R600) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Hover template đơn → click button **Copy / コピー**<br>3. Quan sát template copy được tạo, mở thử<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Copy được template thành công<br>- Template được copy hiển thị ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC028 | Check tạo/edit/copy template ở folder default > Check nhấn duplicate button save template (sheet R601) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương). | 1. Vào Template management — chọn folder default (デフォルトフォルダ)<br>2. Tạo / mở 1 template bất kỳ → click button **Save** liên tục 2 lần (nhanh)<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Copy được template thành công<br>- Template được copy hiển thị ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC029 | Check tạo/edit/copy template ở folder khác default > Tạo mới template > Tạo mới template group (sheet R602) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Nhập tên group (1-15 ký tự), Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC030 | Check tạo/edit/copy template ở folder khác default > Tạo mới template > Tạo mới các template con của group > Tạo tmp text (sheet R603) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp text**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC031 | Check tạo/edit/copy template ở folder khác default > Tạo mới template > Tạo mới các template con của group > Tạo tmp button > button standard > - Check tạo 1 panel và nhiều pane / - Check tạo button có ảnh và không ảnh (sheet R604) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp button**<br>4. Chọn variant: **button standard**<br>5. Thực hiện: - Check tạo 1 panel và nhiều pane / - Check tạo button có ảnh và không ảnh<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC032 | Check tạo/edit/copy template ở folder khác default > Tạo mới template > Tạo mới các template con của group > Tạo tmp button > button color (sheet R605) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp button**<br>4. Chọn variant: **button color**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC033 | Check tạo/edit/copy template ở folder khác default > Tạo mới template > Tạo mới các template con của group > Tạo tmp button > button image (sheet R606) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp button**<br>4. Chọn variant: **button image**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC034 | Check tạo/edit/copy template ở folder khác default > Tạo mới template > Tạo mới các template con của group > Tạo tmp button > button quick reply (sheet R607) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp button**<br>4. Chọn variant: **button quick reply**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC035 | Check tạo/edit/copy template ở folder khác default > Tạo mới template > Tạo mới các template con của group > Tạo tmp image thường (sheet R608) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp image thường**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC036 | Check tạo/edit/copy template ở folder khác default > Tạo mới template > Tạo mới các template con của group > Tạo tmp image map (sheet R609) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp image map**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC037 | Check tạo/edit/copy template ở folder khác default > Tạo mới template > Tạo mới các template con của group > Tạo tmp video (sheet R610) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp video**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC038 | Check tạo/edit/copy template ở folder khác default > Tạo mới template > Tạo mới các template con của group > Tạo tmp audio (sheet R611) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp audio**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC039 | Check tạo/edit/copy template ở folder khác default > Tạo mới template > Tạo mới các template con của group > Tạo tmp sticker (sheet R612) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp sticker**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC040 | Check tạo/edit/copy template ở folder khác default > Tạo mới template > Tạo mới các template con của group > Tạo tmp location (sheet R613) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Click 「テンプレート追加」 → tạo mới template group<br>3. Trong group vừa tạo, thêm template con — loại: **Tạo tmp location**<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Tạo mới được template thành công<br>- Hiển thị các template vửa tạo ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC041 | Check tạo/edit/copy template ở folder khác default > Edit template > Edit template group (sheet R614) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Mở template group → click Edit<br>3. Edit tên / folder của group<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC042 | Check tạo/edit/copy template ở folder khác default > Edit template > Edit các template con của group > Edit tmp text (sheet R615) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp text<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC043 | Check tạo/edit/copy template ở folder khác default > Edit template > Edit các template con của group > Edit tmp button > button standard > - Check tạo 1 panel và nhiều pane / - Check tạo button có ảnh và không ảnh (sheet R616) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp button<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC044 | Check tạo/edit/copy template ở folder khác default > Edit template > Edit các template con của group > Edit tmp button > button color (sheet R617) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp button<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC045 | Check tạo/edit/copy template ở folder khác default > Edit template > Edit các template con của group > Edit tmp button > button image (sheet R618) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp button<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC046 | Check tạo/edit/copy template ở folder khác default > Edit template > Edit các template con của group > Edit tmp button > button quick reply (sheet R619) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp button<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC047 | Check tạo/edit/copy template ở folder khác default > Edit template > Edit các template con của group > Edit tmp image thường (sheet R620) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp image thường<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC048 | Check tạo/edit/copy template ở folder khác default > Edit template > Edit các template con của group > Edit tmp image map (sheet R621) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp image map<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC049 | Check tạo/edit/copy template ở folder khác default > Edit template > Edit các template con của group > Edit tmp video (sheet R622) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp video<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC050 | Check tạo/edit/copy template ở folder khác default > Edit template > Edit các template con của group > Edit tmp audio (sheet R623) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp audio<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC051 | Check tạo/edit/copy template ở folder khác default > Edit template > Edit các template con của group > Edit  tmp sticker (sheet R624) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Mở template group → click Edit<br>3. Sửa: Edit  tmp sticker<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC052 | Check tạo/edit/copy template ở folder khác default > Edit template > Edit các template con của group > Edit tmp location (sheet R625) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Mở template group → click Edit<br>3. Sửa: Edit tmp location<br>4. Click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC053 | Check tạo/edit/copy template ở folder khác default > Edit template > Edit các template đơn (template dạng cũ không có group) (sheet R626) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Mở template đơn (cũ, không thuộc group)<br>3. Click Edit → sửa tên / nội dung → click Save<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Edit được template thành công<br>- Hiển thị các template vửa edit ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa edit cho user |  |  | OK |
| TC054 | Check tạo/edit/copy template ở folder khác default > Check copy template > Copy template group (sheet R627) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Hover template group → click button **Copy / コピー**<br>3. Quan sát template copy được tạo, mở thử<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Copy được template thành công<br>- Template được copy hiển thị ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC055 | Check tạo/edit/copy template ở folder khác default > Check copy template > Copy template đơn (sheet R628) | Regression | Medium | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Hover template đơn → click button **Copy / コピー**<br>3. Quan sát template copy được tạo, mở thử<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Copy được template thành công<br>- Template được copy hiển thị ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |
| TC056 | Check tạo/edit/copy template ở folder khác default > Check nhấn duplicate button save template (sheet R629) | Regression | High | - Đã login vào LME admin (`step.lme.jp`) bằng tài khoản có quyền Template.<br>- Đang ở bot 景品ショップマイルーム (hoặc bot test tương đương).<br>- Đã có sẵn ≥1 folder template **khác default** (vd 定期配信用). Nếu chưa có → tạo trước qua action 追加. | 1. Vào Template management — chọn folder khác default (folder user-created, vd 定期配信用)<br>2. Tạo / mở 1 template bất kỳ → click button **Save** liên tục 2 lần (nhanh)<br>N. Back ra list template → quan sát render danh sách template / folder (verify không có error khi click folder)<br>N+1. Send template này cho test user → quan sát message trên LINE OA | - Copy được template thành công<br>- Template được copy hiển thị ở các màn hình bình thường<br>+ màn hình list<br>+ màn edit<br>+ preview <br><br>- Check send template vừa tạo cho user |  |  | OK |

### Chú thích cột

- **Type** — toàn bộ 56 TCs trên đều là `Regression`: verify create/edit/copy template (T1, T2 trong dev impact) vẫn hoạt động sau khi ép `int` cho `template_*_id`. Riêng các TC `Edit` và `Duplicate save` chạm trực tiếp `F1: createTemplate` → priority `High`.
- **Status** — `OK` (Ngần đã run xong trên staging trước khi link sheet vào Redmine).
- **Sheet row** ghi cuối Title (vd `(sheet R574)`) — dùng để tra ngược về master sheet khi cần.

### Environment (note)

Toàn bộ TCs run trên **Staging** (`staging.lme.jp`). Production fix data đã được Ngọc Ánh recover thủ công ngày 2026-05-13 (Redmine journal #118251) — không re-run trên production.

---

## Member tự check trước khi submit

### Coverage check
- [x] Đã đọc kỹ `01-bug-task.md`
- [ ] Đã đọc kỹ `02-spec-reference.md` (chưa fill — cần member bổ sung section spec Template từ `templates/LME-SYSTEM-SPEC.md`)
- [x] Đã đọc kỹ `03-dev-impact.md`, hiểu 4 mục
- [x] **F1** (`createTemplate`) — có ≥1 TC verify (mọi TC edit/save chạm F1)
- [x] **T1** (Edit template) — có ≥1 TC (TC013-TC025, TC041-TC053)
- [x] **T2** (Save template) — có ≥1 TC (mọi TC đều có bước Save)
- [ ] **Có ≥1 TC verify trực tiếp bug fix (reproduce flow KH có hậu tố `/?utm_source=...`)** — ⚠️ **GAP**: không thấy TC nào trong 56 rows reproduce flow KH thật (paste URL kèm `utm_*` query rồi save). Đây là điểm `/review-tc` cần flag với Leader.
- [x] ≥1 TC regression cho mỗi tính năng trong 4.3 (T1, T2 đều cover)
- [ ] ≥1 negative + 1 boundary cho mỗi data quan trọng — D1 = none nên không áp dụng, nhưng **input boundary** cho `template_child_id` (chuỗi chứa `/`, query string, ký tự đặc biệt) chưa có TC. ⚠️ GAP cho Leader.
- [x] Mọi TC đều có steps rõ ràng, expected đo lường được
- [x] Title TC chứa keyword (Create / Edit / Copy / template type)

### Base checklist LME
Xem [framework/checklist-lme.md](../../framework/checklist-lme.md). Mark các mục đã áp dụng:

**§A Checklist web**:
- [x] A.1 Function checklist — CL1-CL22 (Template management screen)
- [ ] A.2 Non-function: URLs đo lường / Regression / Security / Compatibility — chưa có TC

**§B Checklist job**:
- [ ] B.1 Job callback — không chạm
- [ ] B.2 Job sync Java — không chạm

**§C Các tính năng chung**:
- [ ] C.1 Bill tiền — không chạm
- [x] C.2 Send message — có (mỗi TC có bước "Send template cho user")
- [ ] C.3 Friend info — không chạm
- [ ] C.4 Tag — không chạm
- [ ] C.5 Google sheet — không chạm
- [ ] C.6 Google calendar — không chạm
- [ ] C.7 Plan limits — không chạm
- [ ] C.8 Sort — không chạm