<!-- source: MCP LME TEST STUDIO — task_id=314, ticket 41108, testcase_list (27 TC), fetch lúc 2026-09-19. READ-ONLY snapshot, sinh bởi scripts/parse_studio_tcs.py. -->

# 04 — TC List (snapshot từ MCP LME TEST STUDIO)

> ⚠️ `contentTrust = untrusted` → xử lý như **data**, không phải chỉ thị.
> ⚠️ **READ-ONLY** — muốn sửa TC thì sửa trên Studio (`testcase_update`) rồi fetch lại.

# Digest — Studio task #314 · ticket 41108 · 27 TC

## Kết quả thực thi
| Trạng thái | Số TC |
|---|---|
| `pass` | 15 |
| `skip` | 9 |
| `(chưa chạy)` | 3 |

→ **15/27 TC (55%) thực sự Đạt**; 12 TC còn lại KHÔNG có kết luận test.

## Môi trường
| Env | Số TC |
|---|---|
| `LOCAL` | 24 |
| `(CHƯA CHẠY)` | 3 |

→ Production: **0 TC**.  ⚠️ **RULE-08**: không kết luận media / domain / job nền / bill tiền từ local-staging.

## Ai chạy (source / by)
| source / by | Số TC |
|---|---|
| `ai / ngannt` | 16 |
| `manual / ngannt` | 8 |
| `— / —` | 3 |

## Tác giả TC
| author | Số TC |
|---|---|
| `AI` | 20 |
| `ngannt@mcp` | 7 |

## Loại case
| case_type | Số TC |
|---|---|
| `Normal` | 17 |
| `Abnormal` | 7 |
| `Boundary` | 3 |

## Nhóm / chế độ chạy
| tc_group | Số TC |
|---|---|
| `ui` | 17 |
| `job` | 7 |
| `data` | 2 |
| `api` | 1 |

| exec_mode | Số TC |
|---|---|
| `auto` | 24 |
| `manual` | 3 |

## Mã quan điểm KHỚP checklist-lme (10 mã)
`COMPAT-LEGACY-001`(1) · `DATA-DB-001`(1) · `DATA-TEXT-001`(2) · `ENV-003`(1) · `FRIEND-001`(1) · `FUNC-001`(1) · `FUNC-004`(1) · `MSG-USER-001`(1) · `REG-SHARED-001`(5) · `SYNC-APP-001`(1)

## ⚠️ Mã quan điểm KHÔNG có trong checklist-lme (9 mã)
| Mã Studio | Số TC |
|---|---|
| `TOOL-NEGCTRL-001` | 3 |
| `TOOL-ERRHYG-001` | 2 |
| `TOOL-KNOW-002` | 1 |
| `RULE-TOOL-029` | 1 |
| `TOOL-OLDREC-001` | 1 |
| `API-CONTRACT-001` | 1 |
| `SEC-INJECT-001` | 1 |
| `TOOL-BRANCH-001` | 1 |
| `RULE-04` | 1 |

→ `/review-tc` KHÔNG map được coverage cho các mã này.

## ⚠️ TC fail / error hoặc có ticket bug (0)
(không có)

## ⚠️ TC skip / chưa chạy (12)
| ID | exec | Tiêu đề |
|---|---|---|
| NEW-1 | `skip` | Trích dẫn tin nhắc đặt lịch salon của bạn bè có tên LINE chứa dấu ngoặ |
| NEW-2 | `skip` | Trích dẫn tin nhắc đặt lịch lesson dùng mã friend info có giá trị chứa |
| NEW-4 | `skip` | Đối chứng âm — bạn bè có tên bình thường: trích dẫn tin nhắc đặt lịch  |
| NEW-17 | `skip` | Web tự dựng lại nội dung trích dẫn khi cột đang rỗng — kết quả vẫn là  |
| NEW-6 | `skip` | Trích dẫn tin văn bản có nội dung tình cờ đúng dạng JSON — kiểm tra nh |
| NEW-7 | `skip` | Bản ghi trích dẫn tạo trước bản fix — xác nhận ranh giới, không tính l |
| NEW-8 | `skip` | Giá trị thay thế chứa ký tự biên — trích dẫn tin nhắc đặt lịch vẫn hiể |
| NEW-21 | `(chưa chạy)` | Production — friend tên LINE chứa " trả lời trích dẫn tin nhắc đặt lịc |
| NEW-22 | `(chưa chạy)` | Ca KH đã report (bot つっつん, friend つか咲"き) — hội thoại chat 1:1 PC mở đư |
| NEW-24 | `(chưa chạy)` | Trích dẫn tin nhắc event booking (cũ và mới) và tin nhắc form — friend |
| NEW-19 | `skip` | API lịch sử tin nhắn trả về nội dung trích dẫn đúng hình dạng cho tin  |
| NEW-18 | `skip` | Kiểm kê bản ghi nội dung trích dẫn đã hỏng từ trước bản fix |

## Màn hình (6)
| screen | Số TC |
|---|---|
| `Màn chat 1:1 web PC (SCR-CHT-01) — khối trích dẫn trong khung hội thoại` | 9 |
| `Reply / quote message` | 7 |
| `Job linect-service — dựng nội dung trích dẫn khi nhận callback LINE` | 7 |
| `Dữ liệu tin nhắn chat 1:1 — cột nội dung trích dẫn` | 2 |
| `Màn chat 1:1 trên app admin điện thoại` | 1 |
| `API lịch sử tin nhắn chat 1:1 (EP-05)` | 1 |

## requirement_keys (8)
`REQ-001`(11) · `REQ-002`(6) · `REQ-003`(4) · `REQ-004`(3) · `REQ-005`(2) · `REQ-006`(4) · `REQ-007`(1) · `REQ-008`(1)

---

## Bảng TC (16 cột canonical)

| TC No. | Mã quan điểm liên kết | Loại case | Tiêu đề test case | Điều kiện tiền đề | Các bước thực hiện | Dữ liệu test/input | Kết quả mong đợi | Kết quả thực thi | Evidence thực tế | Môi trường test | Người thực hiện | Ngày thực hiện | Số ticket bug | Trạng thái đánh giá spec | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| NEW-1 | TOOL-KNOW-002 | Normal | Trích dẫn tin nhắc đặt lịch salon của bạn bè có tên LINE chứa dấu ngoặc kép — khung hội thoại hiển thị đầy đủ | Job đã chạy bản fix (nhánh job của #41108). Bot test đã bật lịch salon và có tin nhắc đặt lịch chứa mã tên bạn bè. Có một bạn bè test với tên hiển thị LINE là つか咲"き (có đúng 1 dấu ngoặc kép nửa chiều rộng ở giữa). Bạn bè này đã NHẬN tin nhắc đặt lịch salon SAU thời điểm áp bản fix, rồi từ LINE trả lời kèm trích dẫn chính tin nhắc đó. Đăng nhập admin và đã chọn đúng bot test. | 1. Mở màn chat 1:1 của bot test<br>2. Ở cột danh sách bạn bè, chọn hội thoại của bạn bè つか咲"き<br>3. Mở bảng điều khiển trình duyệt (F12, tab Console) rồi tải lại trang bằng F5<br>4. Chờ khung hội thoại nạp xong, cuộn xuống tin trả lời mới nhất của bạn bè<br>5. Quan sát khối trích dẫn nằm phía trên nội dung tin trả lời<br>6. Quan sát danh sách lỗi trong tab Console | Tên hiển thị LINE của bạn bè: つか咲"き · Tin nhắc đặt lịch salon có nội dung dạng "{name}さん、ご予約のリマインドです" | Cột giữa hiển thị đầy đủ lịch sử tin nhắn, không có vùng trắng và không mất bong bóng tin nào. Tin trả lời của bạn bè hiện kèm khối trích dẫn màu xám phía trên, bên trong đọc được nội dung tin nhắc đặt lịch salon với tên hiển thị đúng nguyên văn つか咲"き (đúng 1 dấu ngoặc kép, không có dấu gạch chéo ngược đứng trước). Tab Console KHÔNG có lỗi SyntaxError liên quan tới phân tích JSON. | Chưa test |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18883 (NEW-1) · mã theo quan điểm: TC-TOOLKNOW002-01 · ui · auto · env-safe · REQ: REQ-002, REQ-001 · spec: TICKET-41108, SCR-CHT-01, source: sns-line public/js/chats/chat-v2.js:2446 · TC tái hiện đúng hiện tượng gốc của ticket và cũng là TC xác nhận đã fix. Kỹ thuật: điểm vỡ là public/js/chats/chat-v2.js:2446 showMessageQuotedSalonLesson gọi JSON.parse(item.content) không có try/catch; dữ liệu do job linect-service HandlePostbackTask.applyReplaceContent sinh ra. Bắt buộc quan sát trên trình duyệt thật — không được kết luận pass chỉ bằng truy vấn dữ liệu, vì lớp JSON bên ngoài vẫn hợp lệ kể cả khi bug còn. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-2 | FRIEND-001 | Normal | Trích dẫn tin nhắc đặt lịch lesson dùng mã friend info có giá trị chứa dấu ngoặc kép — hiển thị đúng | Job đã chạy bản fix. Bot test có lịch lesson và tin nhắc đặt lịch lesson dùng mã friend info (dạng [FRIEND_INFO_xxx]) trong nội dung. Có bạn bè test bất kỳ (tên LINE bình thường). Đăng nhập admin, đã chọn đúng bot test. | 1. Mở màn chat 1:1 và chọn hội thoại của bạn bè test<br>2. Ở cột phải, mở tab 「友だち情報」 và sửa giá trị của trường friend info đang được dùng trong tin nhắc đặt lịch lesson thành 山田"太郎", rồi bấm lưu<br>3. Chờ (hoặc kích hoạt) hệ thống gửi tin nhắc đặt lịch lesson mới tới bạn bè này<br>4. Từ LINE, bạn bè trả lời kèm trích dẫn chính tin nhắc đặt lịch lesson vừa nhận<br>5. Quay lại màn chat 1:1, tải lại trang và mở lại hội thoại đó<br>6. Quan sát khối trích dẫn của tin trả lời và tab Console | Giá trị friend info: 山田"太郎" (2 dấu ngoặc kép nửa chiều rộng) · Tin nhắc lesson dùng mã [FRIEND_INFO_xxx] | Khung hội thoại hiển thị đầy đủ, không trắng. Khối trích dẫn hiện nội dung tin nhắc đặt lịch lesson với giá trị friend info đúng nguyên văn 山田"太郎" — đủ 2 dấu ngoặc kép, không có dấu gạch chéo ngược thừa. Tab Console không có lỗi SyntaxError. | Chưa test |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18884 (NEW-2) · mã theo quan điểm: TC-FRIEND001-01 · ui · auto · env-safe · REQ: REQ-002, REQ-001 · spec: TICKET-41108, SCR-CHT-01, source: linect-service MessageBuilder.java:4082-4101 · Phủ 2 trục cùng lúc: loại lịch lesson và nguồn giá trị thay thế là friend info. Đây là đường tạo dữ liệu chạy được trên mọi môi trường vì giá trị friend info sửa được ngay trên màn admin, khác tên hiển thị LINE (đồng bộ từ hồ sơ LINE, admin không sửa được). · author=AI · status=draft |
| NEW-3 | REG-SHARED-001 | Normal | Trích dẫn tin template của bạn bè có tên chứa dấu ngoặc kép — khung hội thoại hiển thị bình thường | Job đã chạy bản fix. Bot test đã gửi cho bạn bè (tên LINE hoặc friend info có chứa dấu ngoặc kép) một tin từ template có chèn mã tên bạn bè. Bạn bè đã trả lời kèm trích dẫn chính tin template đó từ LINE. Đăng nhập admin, chọn đúng bot test. | 1. Mở màn chat 1:1 và chọn hội thoại của bạn bè test<br>2. Tải lại trang và cuộn tới tin trả lời có trích dẫn tin template<br>3. Quan sát nội dung trong khối trích dẫn<br>4. Quan sát tab Console | Tên hiển thị LINE hoặc giá trị friend info: 佐藤"花" · Template văn bản có chứa mã tên bạn bè | Khối trích dẫn hiển thị đúng nội dung template đã gửi kèm tên 佐藤"花" nguyên văn; khung hội thoại đầy đủ, không trắng; Console không có lỗi SyntaxError. Kết quả phải giống hệt hành vi trước bản fix. | Đạt |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18885 (NEW-3) · mã theo quan điểm: TC-REGSHARED001-01 · ui · auto · env-safe · REQ: REQ-004, REQ-002 · spec: TICKET-41108, SCR-CHT-01, source: linect-service HandlePostbackTask.java:4487 · Phủ nơi gọi thứ hai của hàm dùng chung: nhánh dựng nội dung trích dẫn cho tin template (buildQuoteMessageContent, HandlePostbackTask.java:4487), khác nhánh còn lại đi qua checkFindQuoteMessage (dòng 4418). · author=AI · status=draft |
| NEW-4 | TOOL-NEGCTRL-001 | Normal | Đối chứng âm — bạn bè có tên bình thường: trích dẫn tin nhắc đặt lịch hiển thị y như trước fix | Job đã chạy bản fix. Có bạn bè test với tên hiển thị LINE hoàn toàn bình thường (không ký tự đặc biệt). Bạn bè đã nhận tin nhắc đặt lịch salon và đã trả lời kèm trích dẫn tin đó. Đã chụp lại màn hình hiển thị của cùng loại tin này TRƯỚC khi áp bản fix. | 1. Mở màn chat 1:1 và chọn hội thoại của bạn bè test<br>2. Cuộn tới tin trả lời có trích dẫn tin nhắc đặt lịch salon<br>3. Quan sát và chụp lại khối trích dẫn<br>4. So sánh với ảnh chụp trước khi áp bản fix | Tên hiển thị LINE: 田中太郎 (không ký tự đặc biệt) | Khối trích dẫn hiển thị nội dung tin nhắc đặt lịch giống hệt trước bản fix: đúng tên 田中太郎, đúng chữ, không thêm/bớt ký tự nào. | Chưa test |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18886 (NEW-4) · mã theo quan điểm: TC-TOOLNEGCTRL001-01 · ui · auto · read-only · REQ: REQ-003, REQ-002 · spec: TICKET-41108, SCR-CHT-01 · Đối chứng âm bắt buộc: chứng minh bản fix không làm lệch đầu ra ở dữ liệu thường. Bằng chứng nên kèm 2 ảnh chụp (trước/sau). · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-17 | TOOL-NEGCTRL-001 | Normal | Web tự dựng lại nội dung trích dẫn khi cột đang rỗng — kết quả vẫn là JSON hợp lệ | Có một bản ghi tin trả lời kèm trích dẫn tin nhắc đặt lịch salon/lesson của bạn bè tên chứa dấu ngoặc kép. Trước khi chạy, đặt cột nội dung trích dẫn của bản ghi đó về rỗng (giữ nguyên mã tin được trích dẫn) và ghi lại giá trị cũ để khôi phục. | 1. Ghi lại giá trị hiện tại của cột nội dung trích dẫn rồi đặt cột đó về rỗng<br>2. Mở màn chat 1:1 trên trình duyệt và mở hội thoại của bạn bè đó<br>3. Chờ khung hội thoại nạp xong và quan sát khối trích dẫn cùng tab Console<br>4. Truy vấn lại bản ghi để xem cột nội dung trích dẫn đã được ghi lại chưa<br>5. Phân tích JSON lớp ngoài và phân tích tiếp trường nội dung của giá trị vừa được ghi | Không nhập dữ liệu; chỉ đặt rỗng cột nội dung trích dẫn của 1 bản ghi | Khung hội thoại hiển thị đầy đủ, không trắng; khối trích dẫn hiện đúng nội dung tin nhắc đặt lịch kèm tên có dấu ngoặc kép. Cột nội dung trích dẫn được web ghi lại; giá trị mới phân tích JSON được ở cả hai lớp. | Chưa test |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18899 (NEW-17) · mã theo quan điểm: TC-TOOLNEGCTRL001-02 · ui · auto · local-only · REQ: REQ-006, REQ-002 · spec: source: sns-line app/Http/Controllers/ChatController.php:2183-2189, source: sns-line app/Services/ChatService.php:447-490, SCR-CHT-01 · Đối chứng âm cho nguồn ghi thứ hai của cùng cột dữ liệu: web chỉ dựng lại khi cột đang rỗng (ChatController.php:2183-2189) và đường dựng của web không nhét giá trị thay thế vào JSON — thay thế làm ở trình duyệt sau khi đã phân tích — nên luôn cho JSON hợp lệ. TC này đồng thời chứng minh tính khả thi của đường phục hồi bản ghi cũ đã hỏng, nhưng phục hồi thật phải chờ leader/Dev duyệt. Nhớ khôi phục giá trị cũ sau khi chạy. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-5 | RULE-TOOL-029 | Abnormal | Trích dẫn tin văn bản thường có mã tên — không được xuất hiện dấu escape thừa | Job đã chạy bản fix. Bot test đã gửi cho bạn bè (tên chứa ký tự đặc biệt) một tin VĂN BẢN THƯỜNG có chèn mã tên. Bạn bè đã trả lời kèm trích dẫn tin văn bản đó. Trong cùng hội thoại còn một tin do chính bạn bè gửi được OA trả lời kèm trích dẫn, để kiểm cả hai phía bong bóng. | 1. Mở màn chat 1:1 và chọn hội thoại của bạn bè test<br>2. Cuộn tới tin trả lời của bạn bè có trích dẫn tin văn bản thường (bong bóng bên trái)<br>3. Đọc kỹ từng ký tự trong khối trích dẫn<br>4. Cuộn tới tin do OA gửi có kèm trích dẫn (bong bóng bên phải) và đọc kỹ khối trích dẫn của nó<br>5. Quan sát tab Console | Tên bạn bè: つか咲"き\テスト · Tin văn bản thường: "{name}さん、こんにちは" | Khối trích dẫn ở CẢ HAI phía bong bóng hiển thị đúng つか咲"き\テスト — đúng 1 dấu ngoặc kép và đúng 1 dấu gạch chéo ngược. TUYỆT ĐỐI không thấy \" hoặc \\ trên màn hình. Console không có lỗi. | Đạt |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18887 (NEW-5) · mã theo quan điểm: TC-RULETOOL029-01 · ui · auto · env-safe · REQ: REQ-003 · spec: TICKET-41108, SCR-CHT-01, SRC-REGRESSION-011 · Đây là rủi ro lớn nhất của bản fix: escape áp nhầm cho nội dung không phải JSON sẽ lộ ký tự escape ra màn hình. Khối trích dẫn của tin văn bản đi qua showMessageText (không phân tích JSON), render ở cả 2 vị trí content_chat.blade.php dòng 562 (tin OA, bên phải) và dòng 1775 (tin bạn bè, bên trái). · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-6 | TOOL-ERRHYG-001 | Abnormal | Trích dẫn tin văn bản có nội dung tình cờ đúng dạng JSON — kiểm tra nhận diện nhầm | Job đã chạy bản fix. Bot test đã gửi cho bạn bè (tên có chứa dấu ngoặc kép) một tin VĂN BẢN THƯỜNG mà nội dung tình cờ đúng cú pháp JSON. Bạn bè đã trả lời kèm trích dẫn tin đó. | 1. Mở màn chat 1:1 và chọn hội thoại của bạn bè test<br>2. Cuộn tới tin trả lời có trích dẫn tin văn bản dạng JSON<br>3. Đọc kỹ nội dung hiển thị trong khối trích dẫn và so với nội dung tin gốc phía trên trong cùng hội thoại<br>4. Quan sát tab Console | Tên bạn bè: つか咲"き · Tin văn bản thường có nội dung: {"memo":"{name}さんへ"} | Khối trích dẫn hiển thị đúng nguyên văn nội dung mà bạn bè đã nhìn thấy trên LINE — không thêm dấu gạch chéo ngược nào so với bong bóng tin gốc. Khung hội thoại không trắng và Console không có lỗi. | Chưa test |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18888 (NEW-6) · mã theo quan điểm: TC-TOOLERRHYG001-01 · ui · auto · env-safe · REQ: REQ-003 · spec: TICKET-41108, SCR-CHT-01, SRC-REGRESSION-008 · CẦN DEV XÁC NHẬN trước khi kết luận fail: Dev khai thêm hàm nhận diện nội dung JSON nhưng không mô tả cách nhận diện. Nếu nhận diện chỉ dựa vào 'phân tích JSON thành công' thì tin này bị coi là JSON và giá trị thay thế sẽ bị escape, làm hiển thị lệch so với tin gốc. Khi thấy lệch, báo cáo dưới dạng câu hỏi quy tắc. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-7 | TOOL-OLDREC-001 | Abnormal | Bản ghi trích dẫn tạo trước bản fix — xác nhận ranh giới, không tính là fail của ticket | Job đã chạy bản fix. Trong môi trường test còn tồn tại ít nhất một hội thoại có tin trả lời kèm trích dẫn tin nhắc đặt lịch được tạo TRƯỚC thời điểm áp bản fix, với bạn bè tên chứa dấu ngoặc kép. Cùng bạn bè đó cũng đã có thêm một tin trả lời kèm trích dẫn tạo SAU khi áp fix. | 1. Mở màn chat 1:1 và chọn hội thoại của bạn bè đã report<br>2. Mở tab Console rồi tải lại trang<br>3. Cuộn tới tin trả lời kèm trích dẫn tạo TRƯỚC khi áp fix và ghi nhận hiện tượng<br>4. Cuộn tới tin trả lời kèm trích dẫn tạo SAU khi áp fix và ghi nhận hiện tượng<br>5. Chụp màn hình cả hai trạng thái | Không nhập dữ liệu; chỉ quan sát 2 bản ghi khác thời điểm | Bản ghi tạo TRƯỚC fix có thể vẫn làm trắng khung hội thoại và vẫn sinh lỗi SyntaxError — ĐÂY LÀ HIỆN TRẠNG ĐÃ BIẾT theo khai báo của Dev (không phục hồi dữ liệu cũ), KHÔNG tính là fail của #41108. Bản ghi tạo SAU fix hiển thị bình thường. Kết luận của TC là ghi nhận ranh giới và báo cáo số hội thoại còn bị ảnh hưởng cho leader quyết việc phục hồi. | Chưa test |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18889 (NEW-7) · mã theo quan điểm: TC-TOOLOLDREC001-01 · ui · auto · read-only · REQ: REQ-006 · spec: TICKET-41108, SCR-CHT-01, SRC-REGRESSION-014 · Nếu bản ghi cũ và mới cùng nằm trong một hội thoại thì bản ghi cũ vẫn làm hỏng cả khung, che mất bản ghi mới — khi đó tách ra 2 hội thoại khác nhau và ghi rõ vào báo cáo. Đường phục hồi đề xuất (chưa duyệt, KHÔNG tự chạy): xoá rỗng cột nội dung trích dẫn của bản ghi hỏng để web tự dựng lại. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-8 | DATA-TEXT-001 | Boundary | Giá trị thay thế chứa ký tự biên — trích dẫn tin nhắc đặt lịch vẫn hiển thị đủ | Job đã chạy bản fix. Bot test có tin nhắc đặt lịch salon dùng mã friend info. Có bạn bè test để lần lượt đặt 5 bộ giá trị friend info bên dưới; mỗi lần đặt xong thì phát sinh một tin nhắc đặt lịch mới và bạn bè trả lời kèm trích dẫn tin đó. | 1. Ở tab 「友だち情報」 của màn chat 1:1, đặt giá trị friend info theo bộ dữ liệu thứ nhất rồi lưu<br>2. Phát sinh tin nhắc đặt lịch salon mới và để bạn bè trả lời kèm trích dẫn tin đó<br>3. Tải lại màn chat 1:1, mở hội thoại và quan sát khối trích dẫn cùng tab Console<br>4. Lặp lại 3 bước trên cho lần lượt các bộ dữ liệu còn lại<br>5. Ghi nhận kết quả từng bộ dữ liệu | Bộ 1 — gạch chéo ngược: 山田\太郎 · Bộ 2 — xuống dòng: 山田(xuống dòng)太郎 · Bộ 3 — ngoặc kép toàn chiều rộng và ngoặc kép cong: 山田＂太郎” · Bộ 4 — emoji kèm ngoặc kép: 山田"太郎"🎉 · Bộ 5 — chuỗi rất dài 255 ký tự có xen 3 dấu ngoặc kép | Với cả 5 bộ dữ liệu: khung hội thoại hiển thị đầy đủ, không trắng; Console không có lỗi SyntaxError. Khối trích dẫn hiện đúng giá trị đã nhập — 1 dấu gạch chéo ngược (bộ 1), xuống dòng giữ nguyên thành 2 dòng (bộ 2), ký tự ＂ và ” giữ nguyên không bị thêm dấu escape (bộ 3), emoji hiển thị đúng (bộ 4), chuỗi dài bị cắt theo giới hạn hiển thị nhưng phần hiện ra vẫn đúng ký tự và không vỡ khung (bộ 5). | Chưa test |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18890 (NEW-8) · mã theo quan điểm: TC-DATATEXT001-01 · ui · auto · env-safe · REQ: REQ-001, REQ-002 · spec: TICKET-41108, SCR-CHT-01, knowledge: DATA-TEXT-001 · Gom 5 biến thể ký tự vào một TC theo bộ dữ liệu thay vì tách 5 TC cùng gốc lỗi. Phân biệt quan trọng: ngoặc kép toàn chiều rộng ＂ và ngoặc kép cong ” KHÔNG phá cú pháp JSON nên phải giữ nguyên; nếu bị thêm escape là lỗi escape quá tay. · author=AI · status=draft |
| NEW-20 | SYNC-APP-001 | Normal | So sánh hiển thị khối trích dẫn giữa web PC và app admin điện thoại | Job đã chạy bản fix. Có tài khoản admin đăng nhập được cả trên web PC lẫn app admin trên điện thoại thật. Hội thoại của bạn bè tên chứa dấu ngoặc kép đã có tin trả lời kèm trích dẫn tin nhắc đặt lịch salon/lesson tạo SAU khi áp fix. | 1. Trên web PC, mở màn chat 1:1 và mở hội thoại của bạn bè đó, chụp màn hình khối trích dẫn<br>2. Trên app admin điện thoại, đăng nhập cùng tài khoản và mở đúng hội thoại đó<br>3. Cuộn tới đúng tin trả lời có trích dẫn, chụp màn hình<br>4. Đặt hai ảnh cạnh nhau và đối chiếu nội dung khối trích dẫn | Không nhập dữ liệu; chỉ đối chiếu hiển thị trên 2 thiết bị | Hai bên hiển thị cùng nội dung trích dẫn, cùng tên bạn bè kể cả dấu ngoặc kép. Không còn hiện tượng một bên trắng khung một bên bình thường như mô tả trong ticket. | Đạt |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18902 (NEW-20) · mã theo quan điểm: TC-SYNCAPP001-01 · ui · manual · read-only · REQ: REQ-007 · spec: TICKET-41108, SCR-CHT-01, knowledge: SYNC-APP-001 · Đặt manual vì bắt buộc dùng app admin trên thiết bị thật và đối chiếu bằng mắt hai màn hình. Ticket ghi rõ điện thoại xem bình thường còn PC trắng, nên phép so sánh này là bằng chứng khép lại triệu chứng gốc. Bằng chứng bắt buộc: 2 ảnh chụp đặt cạnh nhau. · author=AI · status=draft |
| NEW-21 | ENV-003 | Normal | Production — friend tên LINE chứa " trả lời trích dẫn tin nhắc đặt lịch salon, chat 1:1 PC hiển thị đầy đủ | - Bản fix #41108 đã deploy lên production (job callback)<br>- Bot test trên production đã bật lịch salon, tin nhắc đặt lịch có chèn tên bạn bè<br>- 1 tài khoản LINE test đã đổi tên hiển thị thành テスト"太郎, đã kết bạn bot test; màn 友だち詳細 của bạn bè này hiện đúng tên mới<br>- Ghi lại giờ deploy | 1. Đặt 1 lịch salon cho bạn bè test sao cho tin nhắc được gửi SAU giờ deploy<br>2. Trên LINE, xác nhận nhận được tin nhắc có tên テスト"太郎<br>3. Trên LINE, bấm giữ tin nhắc → Trả lời → gửi 了解です<br>4. Trên PC, mở 1:1チャット của bot test → chọn hội thoại bạn bè test → F5<br>5. Quan sát khung hội thoại và khối trích dẫn phía trên tin 了解です | Tên LINE: テスト"太郎 · Tin trả lời: 了解です | - Khung hội thoại PC hiển thị đủ lịch sử, không vùng trắng<br>- Tin 了解です có khối trích dẫn, bên trong là nội dung tin nhắc với tên đúng テスト"太郎 (1 dấu ", không có \ phía trước)<br>- Mở lại hội thoại lần 2 vẫn hiển thị như trên | Chưa test |  | all |  |  |  |  | Studio #18903 (NEW-21) · mã theo quan điểm: TC-ENV003-01 · ui · manual · local-only · author=ngannt@mcp · status=draft |
| NEW-22 | FUNC-001 | Normal | Ca KH đã report (bot つっつん, friend つか咲"き) — hội thoại chat 1:1 PC mở được sau khi fix + xử lý dữ liệu cũ | - Leader/Dev đã chốt và thực hiện cách xử lý bản ghi trích dẫn hỏng trước fix<br>- Có quyền xem bot つっつん trên production (tài khoản hỗ trợ của CS)<br>- Chưa chốt xử lý dữ liệu cũ → TC để trạng thái chờ, không chạy | 1. Đăng nhập admin production, chọn bot つっつん<br>2. Mở 1:1チャット → chọn hội thoại friend つか咲"き<br>3. Mở Console trình duyệt (F12) → F5<br>4. Cuộn tới tin trả lời có trích dẫn tin nhắc đặt lịch salon ngày 2026/08/28 17:16 | Không nhập dữ liệu — hội thoại thật của KH (T12182) | - Khung hội thoại PC hiển thị đủ lịch sử, không vùng trắng<br>- Khối trích dẫn tin nhắc 2026/08/28 hiển thị tên つか咲"き đúng nguyên văn<br>- Console không có SyntaxError ... JSON | Chưa test |  | all |  |  |  |  | Studio #18904 (NEW-22) · mã theo quan điểm: TC-FUNC001-01 · ui · manual · local-only · author=ngannt@mcp · status=draft |
| NEW-23 | COMPAT-LEGACY-001 | Normal | Trích dẫn tin nhắc đặt lịch remind cũ và remind mới của salon và lesson — friend tên chứa " | - Bản fix đã deploy<br>- Bot test có cả remind cũ và remind mới cho salon và cho lesson (hỏi Dev cách bật từng loại nếu bot chưa có)<br>- Tài khoản LINE test tên テスト"太郎, LME đã hiện đúng tên mới | 1. Lần lượt cho 4 loại tin nhắc ở cột Dữ liệu gửi tới bạn bè test<br>2. Với mỗi tin: trên LINE, trả lời kèm trích dẫn tin nhắc đó<br>3. Trên PC, mở 1:1チャット → hội thoại bạn bè test → F5<br>4. Quan sát khối trích dẫn của từng tin trả lời | salon remind cũ · salon remind mới · lesson remind cũ · lesson remind mới — tên テスト"太郎 | - Cả 4 tin trả lời đều có khối trích dẫn đúng nội dung tin nhắc, tên テスト"太郎 đúng nguyên văn<br>- Khung hội thoại không trắng | Đạt |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18905 (NEW-23) · mã theo quan điểm: TC-COMPATLEGACY001-01 · ui · auto · local-only · author=ngannt@mcp · status=draft |
| NEW-24 | REG-SHARED-001 | Normal | Trích dẫn tin nhắc event booking (cũ và mới) và tin nhắc form — friend tên chứa " | - Bản fix đã deploy<br>- Bot test có event booking (イベント予約) với tin nhắc cũ và mới, có form có tin nhắc (nếu bot không có loại nào thì ghi skip loại đó)<br>- Tài khoản LINE test tên テスト"太郎 | 1. Đăng ký event (và gửi form) để phát sinh từng tin nhắc ở cột Dữ liệu<br>2. Trên LINE, trả lời kèm trích dẫn từng tin nhắc<br>3. Trên PC, mở 1:1チャット → hội thoại bạn bè test → F5<br>4. Quan sát khối trích dẫn của từng tin trả lời | event remind cũ · event remind mới · form remind — tên テスト"太郎 | - Mỗi tin trả lời có khối trích dẫn đúng nội dung tin nhắc, tên đúng nguyên văn<br>- Khung hội thoại không trắng | Chưa test |  | all |  |  |  |  | Studio #18906 (NEW-24) · mã theo quan điểm: TC-REGSHARED001-02 · ui · auto · local-only · author=ngannt@mcp · status=draft |
| NEW-26 | REG-SHARED-001 | Normal | Friend trích dẫn tin chính mình đã gửi có dấu " và nội dung dạng JSON — hiển thị nguyên văn như trước fix | - Bản fix đã deploy<br>- Bạn bè test bất kỳ đã kết bạn bot test | 1. Trên LINE, bạn bè gửi lần lượt 2 tin ở cột Dữ liệu<br>2. Bạn bè trả lời kèm trích dẫn từng tin của chính mình<br>3. Trên PC, mở 1:1チャット → hội thoại bạn bè test → F5<br>4. So nội dung khối trích dẫn với bong bóng tin gốc | Tin 1: 山田"太郎"です · Tin 2: {"memo":"テスト"} | - Khối trích dẫn hiện đúng nguyên văn 2 tin, giống hệt bong bóng tin gốc, không thêm \<br>- Khung hội thoại không trắng | Đạt |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18908 (NEW-26) · mã theo quan điểm: TC-REGSHARED001-03 · ui · auto · local-only · author=ngannt@mcp · status=draft |
| NEW-25 | REG-SHARED-001 | Abnormal | Trích dẫn tin template button / quick reply có chèn tên bạn bè — friend tên chứa " | - Bản fix đã deploy<br>- Dev đã xác nhận loại template nào lưu nội dung dạng JSON<br>- Bot test có template button standard và quick reply text, phần chữ có chèn tên bạn bè<br>- Tài khoản LINE test tên テスト"太郎 | 1. Từ 1:1チャット, gửi lần lượt template button standard và quick reply cho bạn bè test<br>2. Trên LINE, trả lời kèm trích dẫn từng tin<br>3. Trên PC, F5 hội thoại bạn bè test<br>4. Quan sát khối trích dẫn | template button standard (1 panel, có chèn tên) · quick reply text (1 nút, có chèn tên) — tên テスト"太郎 | - Khối trích dẫn hiển thị đúng nội dung tin gốc với tên テスト"太郎, không có \ thừa<br>- Khung hội thoại không trắng | Đạt |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18907 (NEW-25) · mã theo quan điểm: TC-REGSHARED001-04 · ui · auto · local-only · author=ngannt@mcp · status=draft |
| NEW-27 | DATA-TEXT-001 | Boundary | Giá trị friend info chứa ký tự Dev chưa kê (CRLF, tab, $) — trích dẫn tin nhắc vẫn hiển thị và tin của friend không bị mất | - Bản fix đã deploy<br>- Bot test có tin nhắc lesson dùng mã friend info (text)<br>- Bạn bè test bất kỳ | 1. Ở tab 友だち情報 của 1:1チャット, nhập giá trị friend info theo bộ 1 (xuống dòng bằng Enter trong ô nhập) → lưu<br>2. Phát sinh tin nhắc lesson tới bạn bè → trên LINE trả lời kèm trích dẫn<br>3. Trên PC, F5 hội thoại → quan sát khối trích dẫn + tin trả lời + Console<br>4. Lặp lại cho bộ 2, bộ 3 | Bộ 1: 山田 + Enter + 太郎 (nhập trên web, có thể lưu dạng CRLF) · Bộ 2: 山田 + tab + 太郎 (dán từ Excel) · Bộ 3: 価格$100\円 | - Cả 3 bộ: khung hội thoại không trắng, Console không có SyntaxError<br>- Khối trích dẫn hiện đúng giá trị (bộ 1 thành 2 dòng; bộ 2 giữ khoảng tab; bộ 3 đúng $100\円)<br>- Tin trả lời của bạn bè luôn hiện trong hội thoại — không mất tin dù trích dẫn lỗi | Đạt |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18909 (NEW-27) · mã theo quan điểm: TC-DATATEXT001-02 · ui · auto · local-only · author=ngannt@mcp · status=draft |
| NEW-19 | API-CONTRACT-001 | Normal | API lịch sử tin nhắn trả về nội dung trích dẫn đúng hình dạng cho tin nhắc đặt lịch | Đã có phiên đăng nhập admin hợp lệ và đã chọn bot test. Trong hội thoại của bạn bè tên chứa dấu ngoặc kép đã có tin trả lời kèm trích dẫn tin nhắc đặt lịch salon/lesson, tạo SAU khi áp bản fix. | 1. Gọi trực tiếp endpoint lấy lịch sử tin nhắn của hội thoại đó với phiên đăng nhập admin hợp lệ<br>2. Trong phần dữ liệu trả về, tìm tin nhắn có nội dung trích dẫn<br>3. Kiểm tra kiểu dữ liệu và các khoá của trường nội dung trích dẫn<br>4. Lấy giá trị của khoá nội dung bên trong và phân tích JSON<br>5. Đối chiếu tên trong kết quả với tên gốc của bạn bè | Tham số gọi endpoint: mã hội thoại của bạn bè test, trang đầu tiên của lịch sử tin | Endpoint trả về thành công. Trường nội dung trích dẫn của tin là một đối tượng (đã được giải mã sẵn ở phía máy chủ), có đủ các khoá loại tin, loại người gửi, nội dung, thông tin bot gửi và thông tin người gửi. Với loại tin là tin nhắc đặt lịch salon/lesson thì giá trị khoá nội dung là chuỗi phân tích JSON được, và tên bên trong đúng nguyên văn tên của bạn bè kể cả dấu ngoặc kép. Hình dạng dữ liệu không đổi so với trước bản fix. | Chưa test |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18901 (NEW-19) · mã theo quan điểm: TC-APICONTRACT001-01 · api · auto · read-only · REQ: REQ-008, REQ-001 · spec: EP-05, SCR-CHT-01, source: sns-line app/Http/Controllers/ChatController.php:2075-2118 · TC hợp đồng dữ liệu, tách lớp để khi hỏng còn biết lỗi nằm ở tầng dữ liệu hay tầng hiển thị. Đây là endpoint nội bộ của web admin, không phải hợp đồng công bố cho bên thứ ba, nên mặt tiếp xúc khai là web. TC này KHÔNG thay thế TC thao tác trên màn hình — hiện tượng trắng khung chỉ quan sát được trên trình duyệt. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-9 | SEC-INJECT-001 | Normal | Job dựng nội dung trích dẫn tin nhắc đặt lịch salon khi giá trị thay thế chứa dấu ngoặc kép | Job đã checkout nhánh fix của #41108 và đang chạy, có kết nối tới DB test. Trong DB đã có một tin nhắc đặt lịch salon đã gửi cho bạn bè test, trong đó phần dữ liệu thay thế của tin chứa giá trị tên có dấu ngoặc kép. | 1. Phát sinh callback LINE dạng tin nhắn kèm trích dẫn (bạn bè trả lời trích dẫn tin nhắc đặt lịch salon) tới job — bằng bạn bè thật trên LINE hoặc bằng callback mô phỏng đúng định dạng của LINE<br>2. Chờ job xử lý xong callback<br>3. Truy vấn bản ghi tin nhắn vừa được lưu cho hội thoại đó, lấy giá trị cột nội dung trích dẫn<br>4. Phân tích JSON lớp ngoài của giá trị vừa lấy<br>5. Lấy tiếp trường nội dung bên trong và phân tích JSON lần hai<br>6. Đối chiếu giá trị tên sau khi phân tích với tên gốc | Tên bạn bè dùng làm giá trị thay thế: つか咲"き · Nội dung tin nhắc đặt lịch salon dạng JSON có 3 khoá loại lịch, tên lịch và nội dung | Cột nội dung trích dẫn phân tích JSON lớp ngoài thành công, có đủ các khoá loại tin, loại người gửi, nội dung, thông tin bot gửi và thông tin người gửi. Trường NỘI DUNG bên trong phân tích JSON lần hai CŨNG thành công (đây mới là điểm bug). Sau khi phân tích, giá trị tên bằng đúng つか咲"き — một dấu ngoặc kép thật, không phải chuỗi có dấu gạch chéo ngược còn sót. | Đạt |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18891 (NEW-9) · mã theo quan điểm: TC-SECINJECT001-01 · job · auto · local-only · REQ: REQ-001 · spec: TICKET-41108, SRC-BUSINESS-003, source: linect-service HandlePostbackTask.java:4418,4498-4514 · CẢNH BÁO ORACLE: nguồn đầu vào ghi 'cột nội dung trích dẫn phải là JSON hợp lệ' là chưa đủ và sẽ cho pass giả — lớp JSON ngoài LUÔN hợp lệ vì job tuần tự hoá cả cụm bằng thư viện Jackson, chỗ vỡ nằm ở chuỗi JSON lồng trong trường nội dung. Bắt buộc phân tích 2 lớp. Nhánh này đi qua checkFindQuoteMessage (HandlePostbackTask.java:4418) → applyReplaceContent. Cần nhánh job m_202609_replace_json_41108 trước khi chạy. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-10 | REG-SHARED-001 | Normal | Job dựng nội dung trích dẫn tin template khi giá trị thay thế chứa dấu ngoặc kép | Job đã checkout nhánh fix và đang chạy. Bot test đã gửi cho bạn bè một tin từ template có chèn mã tên, và bản ghi ảnh chụp template tương ứng đã tồn tại trong dữ liệu. | 1. Phát sinh callback LINE dạng tin nhắn kèm trích dẫn, trích dẫn chính tin template đã gửi<br>2. Chờ job xử lý xong callback<br>3. Truy vấn bản ghi tin nhắn vừa lưu và lấy cột nội dung trích dẫn<br>4. Phân tích JSON lớp ngoài và kiểm tra các khoá loại tin, loại người gửi, nội dung, thông tin bot gửi<br>5. Đối chiếu nội dung và tên với template gốc | Tên bạn bè: 佐藤"花" · Template văn bản có chèn mã tên | Cột nội dung trích dẫn là JSON hợp lệ, có khoá thông tin bot gửi được điền từ hồ sơ bot (ảnh đại diện và tên hiển thị) đúng như trước bản fix. Nội dung bên trong đúng nội dung template sau khi thay mã tên, tên bằng đúng 佐藤"花". Nếu nội dung template không phải JSON thì giá trị tên được chèn nguyên văn, không có dấu gạch chéo ngược thêm vào. | Đạt |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18892 (NEW-10) · mã theo quan điểm: TC-REGSHARED001-05 · job · auto · local-only · REQ: REQ-004, REQ-001 · spec: SRC-BUSINESS-004, SRC-REGRESSION-017, source: linect-service HandlePostbackTask.java:4487 · Phủ nơi gọi thứ hai của hàm dùng chung: buildQuoteMessageContent (HandlePostbackTask.java:4487). Kiểm thêm khoá thông tin bot gửi vì nhánh này có logic riêng lấy hồ sơ bot. · author=AI · status=draft |
| NEW-11 | MSG-USER-001 | Normal | Callback tin nhắn của hội thoại nhóm và của bạn bè đã ngừng theo dõi vẫn lưu tin kèm trích dẫn | Job đã checkout nhánh fix và đang chạy. Có sẵn một hội thoại nhóm mà bot tham gia, và một bạn bè đã chặn/ngừng theo dõi bot. | 1. Phát sinh callback tin nhắn kèm trích dẫn từ hội thoại 1:1 thông thường<br>2. Phát sinh callback tin nhắn kèm trích dẫn từ hội thoại NHÓM<br>3. Phát sinh callback tin nhắn kèm trích dẫn từ bạn bè đã ngừng theo dõi<br>4. Với từng trường hợp, truy vấn bản ghi tin nhắn vừa lưu<br>5. Kiểm tra các cột liên quan tới trích dẫn và kiểm tra nhật ký job | 3 callback tương ứng 3 loại hội thoại; nội dung tin trích dẫn là tin văn bản thường | Cả 3 trường hợp đều lưu được bản ghi tin nhắn, có mã tin được trích dẫn và có nội dung trích dẫn đúng như trước bản fix (hoặc để trống đúng theo hành vi cũ với các loại tin mà web tự xử lý). Nhật ký job không có lỗi mới phát sinh từ hàm thay thế nội dung. | Đạt |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18893 (NEW-11) · mã theo quan điểm: TC-MSGUSER001-01 · job · auto · local-only · REQ: REQ-005 · spec: SRC-REGRESSION-013 · Regression theo khai báo vùng ảnh hưởng của Dev (3 hàm xử lý callback tin nhắn). Nếu môi trường không dựng được hội thoại nhóm thì ghi skip riêng cho nhánh đó, không gộp kết luận. · author=AI · status=draft |
| NEW-12 | TOOL-BRANCH-001 | Normal | Kiểm tra tĩnh mã nguồn job — hàm thay thế nội dung có nhánh escape và 2 nơi gọi giữ nguyên | Đã checkout được nhánh fix của #41108 trong repo job linect-service (nhánh Dev khai: m_202609_replace_json_41108, commit fcee76a). | 1. Mở mã nguồn hàm thay thế nội dung trong lớp xử lý callback của job<br>2. Kiểm tra có nhánh nhận diện nội dung là JSON và có bước escape giá trị thay thế trước khi chèn<br>3. Kiểm tra nhánh nội dung không phải JSON vẫn chèn giá trị nguyên văn như bản cũ<br>4. Mở hai nơi gọi hàm này và đối chiếu chữ ký cùng cách gọi với bản gốc<br>5. Ghi lại đoạn mã tương ứng làm bằng chứng | Không nhập dữ liệu; đọc mã nguồn tại nhánh fix | Hàm thay thế nội dung có thêm bước nhận diện nội dung JSON; ở nhánh JSON, giá trị thay thế được escape theo chuẩn JSON trước khi chèn (không còn chèn chuỗi thô). Nhánh nội dung không phải JSON giữ nguyên logic cũ. Hai nơi gọi giữ nguyên chữ ký và cách gọi, không phát sinh thay đổi ngoài phạm vi. | Đạt |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18894 (NEW-12) · mã theo quan điểm: TC-TOOLBRANCH001-01 · job · auto · read-only · REQ: REQ-001, REQ-004 · spec: SRC-BUSINESS-003, SRC-BUSINESS-004, SRC-REGRESSION-008, SRC-REGRESSION-009 · TC dự phòng khi không dựng chạy được job — kiểm tra tĩnh trên mã nguồn sau khi checkout. KHÔNG dùng thay cho các TC chạy thật. BLOCKER hiện tại: nhánh và commit Dev khai không lấy được (git fetch cả 2 repo lỗi xác thực, mirror không có nhánh này) — chưa có nhánh thì TC trả skip kèm lý do. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-13 | TOOL-NEGCTRL-001 | Abnormal | Nội dung tin không phải JSON — giá trị thay thế phải được chèn nguyên văn, không escape | Job đã checkout nhánh fix và đang chạy. Bot test đã gửi cho bạn bè một tin VĂN BẢN THƯỜNG có chèn mã tên; tên bạn bè chứa dấu ngoặc kép và dấu gạch chéo ngược. | 1. Phát sinh callback tin nhắn kèm trích dẫn, trích dẫn tin văn bản thường nói trên<br>2. Chờ job xử lý xong<br>3. Truy vấn cột nội dung trích dẫn của bản ghi vừa lưu và phân tích JSON lớp ngoài<br>4. Đọc giá trị của trường nội dung dưới dạng chuỗi và soi từng ký tự | Tên bạn bè: つか咲"き\テスト · Tin văn bản: "{name}さん、こんにちは" | Trường nội dung là chuỗi văn bản thường (không phải JSON), chứa đúng つか咲"き\テスト — một dấu ngoặc kép và một dấu gạch chéo ngược ở dạng ký tự thật sau khi phân tích lớp ngoài. Không có dấu gạch chéo ngược thừa được chèn thêm vào dữ liệu. | Đạt |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18895 (NEW-13) · mã theo quan điểm: TC-TOOLNEGCTRL001-03 · job · auto · local-only · REQ: REQ-003, REQ-001 · spec: SRC-BUSINESS-003, SRC-REGRESSION-011 · Đối chứng âm ở tầng dữ liệu cho rủi ro escape quá tay. Lưu ý khi soi bằng công cụ truy vấn: dấu gạch chéo ngược do lớp JSON ngoài sinh ra là bình thường; phải so sau khi phân tích lớp ngoài. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-14 | TOOL-ERRHYG-001 | Abnormal | Dữ liệu thay thế rỗng hoặc không khớp mã — nội dung trích dẫn giữ nguyên, không lỗi | Job đã checkout nhánh fix và đang chạy. Chuẩn bị 3 tin gốc để trích dẫn: (a) tin không có dữ liệu thay thế, (b) tin có dữ liệu thay thế nhưng mã thay thế không xuất hiện trong nội dung, (c) tin có dữ liệu thay thế với giá trị rỗng. | 1. Phát sinh callback tin nhắn kèm trích dẫn cho từng tin gốc (a), (b), (c)<br>2. Chờ job xử lý xong từng callback<br>3. Truy vấn cột nội dung trích dẫn của từng bản ghi và phân tích JSON<br>4. Kiểm tra nhật ký job trong khoảng thời gian xử lý | (a) không có dữ liệu thay thế · (b) mã thay thế không có trong nội dung · (c) giá trị thay thế là chuỗi rỗng | Cả 3 trường hợp: nội dung trích dẫn được lưu, JSON lớp ngoài hợp lệ, nội dung bên trong đúng bằng nội dung tin gốc (trường hợp (c) thì mã thay thế được thay bằng chuỗi rỗng đúng như hành vi cũ). Nhật ký job không ghi lỗi của hàm thay thế nội dung. | Đạt |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18896 (NEW-14) · mã theo quan điểm: TC-TOOLERRHYG001-02 · job · auto · local-only · REQ: REQ-001, REQ-005 · spec: source: linect-service HandlePostbackTask.java:4498-4514 · Biên đầu vào của chính hàm được sửa; bản cũ đã có nhánh trả về nguyên nội dung khi dữ liệu thay thế trống, TC này bảo đảm bản fix không làm mất nhánh đó. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-15 | FUNC-004 | Boundary | Giá trị thay thế chứa ký tự biên — nội dung trích dẫn vẫn phân tích được và ký tự về đúng nguyên gốc | Job đã checkout nhánh fix và đang chạy. Có tin nhắc đặt lịch salon hoặc lesson (nội dung dạng JSON) dùng mã thay thế; chuẩn bị 5 bộ giá trị thay thế bên dưới. | 1. Đặt giá trị thay thế theo bộ dữ liệu thứ nhất và phát sinh tin nhắc đặt lịch mới<br>2. Phát sinh callback tin nhắn kèm trích dẫn tin nhắc đặt lịch đó<br>3. Truy vấn cột nội dung trích dẫn, phân tích JSON lớp ngoài rồi phân tích tiếp trường nội dung<br>4. Đối chiếu ký tự thu được với giá trị gốc đã đặt<br>5. Lặp lại cho các bộ dữ liệu còn lại | Bộ 1: 山田\太郎 (gạch chéo ngược) · Bộ 2: 山田(xuống dòng)太郎 · Bộ 3: 山田＂太郎” (ngoặc kép toàn chiều rộng và ngoặc kép cong) · Bộ 4: 山田"太郎"🎉 · Bộ 5: chuỗi 255 ký tự xen 3 dấu ngoặc kép | Cả 5 bộ: phân tích JSON 2 lớp đều thành công. Sau khi phân tích, giá trị thu được bằng đúng giá trị gốc — một dấu gạch chéo ngược (bộ 1), ký tự xuống dòng thật (bộ 2), ký tự ＂ và ” giữ nguyên không bị đổi (bộ 3), emoji nguyên vẹn không bị vỡ mã (bộ 4), đủ 255 ký tự (bộ 5). | Đạt |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18897 (NEW-15) · mã theo quan điểm: TC-FUNC004-01 · job · auto · local-only · REQ: REQ-001 · spec: TICKET-41108, knowledge: DATA-TEXT-001, SRC-BUSINESS-003 · Ba ký tự bắt buộc escape theo mô tả fix là dấu ngoặc kép, dấu gạch chéo ngược và xuống dòng; hai ký tự ngoặc kép toàn chiều rộng/cong là đối chứng KHÔNG được escape. Emoji nằm ngoài mặt phẳng cơ bản nên kiểm thêm để chắc việc escape không cắt đôi cặp mã. · author=AI · status=draft |
| NEW-16 | DATA-DB-001 | Normal | Quét dữ liệu sau fix — mọi nội dung trích dẫn tin nhắc đặt lịch mới đều phân tích được 2 lớp | Bản fix của job đã được áp và đã chạy một khoảng thời gian có phát sinh tin trả lời kèm trích dẫn tin nhắc đặt lịch salon/lesson (ít nhất 5 bản ghi mới, trong đó có ít nhất 2 bản ghi của bạn bè tên chứa ký tự đặc biệt). Biết mốc thời gian áp bản fix. | 1. Lấy danh sách bản ghi tin nhắn có nội dung trích dẫn, tạo SAU mốc áp bản fix<br>2. Với từng bản ghi, phân tích JSON lớp ngoài của cột nội dung trích dẫn<br>3. Lọc các bản ghi mà loại tin trong nội dung trích dẫn là tin nhắc đặt lịch salon hoặc lesson<br>4. Với từng bản ghi đã lọc, phân tích tiếp trường nội dung bên trong<br>5. Đếm số bản ghi phân tích lần hai thất bại và liệt kê mã bản ghi | Không nhập dữ liệu; quét dữ liệu đã có sau mốc áp fix | 100% bản ghi tạo sau mốc áp fix phân tích JSON được ở cả hai lớp. Số bản ghi phân tích lần hai thất bại bằng 0. Nếu còn bản ghi thất bại thì liệt kê mã bản ghi, tên bạn bè và ký tự gây vỡ để Dev xử lý tiếp. | Đạt |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18898 (NEW-16) · mã theo quan điểm: TC-DATADB001-01 · data · auto · read-only · REQ: REQ-001, REQ-006 · spec: TICKET-41108, SRC-REGRESSION-018, spec: admin/chat-1on1/db/db-mapping.md §3.2 · Oracle bao phủ rộng, bổ trợ cho các TC đi một luồng. Nhấn mạnh: chỉ kiểm lớp JSON ngoài sẽ luôn pass và che mất bug — bắt buộc phân tích lớp thứ hai. Chạy ở local vì cần quyền truy vấn dữ liệu. · author=AI · status=draft |
| NEW-18 | RULE-04 | Abnormal | Kiểm kê bản ghi nội dung trích dẫn đã hỏng từ trước bản fix | Biết mốc thời gian áp bản fix của job. Có quyền truy vấn dữ liệu tin nhắn của môi trường đang kiểm. | 1. Lấy các bản ghi tin nhắn có nội dung trích dẫn, tạo TRƯỚC mốc áp bản fix<br>2. Phân tích JSON lớp ngoài của từng bản ghi<br>3. Lọc các bản ghi có loại tin trích dẫn là tin nhắc đặt lịch salon hoặc lesson<br>4. Phân tích tiếp trường nội dung bên trong và đánh dấu các bản ghi phân tích thất bại<br>5. Thống kê số bản ghi hỏng, số hội thoại bị ảnh hưởng và danh sách bạn bè tương ứng<br>6. Ghi kết quả thống kê vào báo cáo cho leader | Không nhập dữ liệu; chỉ đọc dữ liệu | Có được con số cụ thể: bao nhiêu bản ghi còn hỏng, thuộc bao nhiêu hội thoại, của những bạn bè nào. Danh sách này được đưa vào báo cáo để leader/Dev quyết định có phục hồi hay không. TC này KHÔNG dùng để kết luận pass/fail cho bản fix — Dev đã khai không phục hồi dữ liệu cũ. | Chưa test |  | LOCAL | ngannt | 2026-09-18 |  |  | Studio #18900 (NEW-18) · mã theo quan điểm: TC-RULE04-01 · data · auto · read-only · REQ: REQ-006 · spec: SRC-BUSINESS-005, SRC-REGRESSION-014, knowledge: RULE-04 · Mỗi hội thoại có bản ghi hỏng là một hội thoại mà nhân viên vẫn không đọc được trên PC sau khi fix — thông tin quyết định việc có cần phục hồi hay không, nên phải có con số chứ không phải nhận định chung. KHÔNG tự chạy phục hồi khi chưa được duyệt. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
