# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | **MCP LME TEST STUDIO** — task #301 (ticket 40806), round 1, branch `ai_fixbug_40806`, `reviewState = leader` |
| Tổng số TC review | **11** |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận: 9/22 vùng ảnh hưởng đủ TC · 8 GAP · 5 RISK.**

Chiều `diff code` lấy từ Studio tab Thông tin (`dev_impact` + `spec_delta`, `diffAvailable = true`): 5 file, +239/−28.

| Mã | Chiều | Vùng ảnh hưởng | TC cover | Status | Vấn đề |
|---|---|---|---|---|---|
| **G1** | diff code | `resources/views/.../order/index.blade.php` — inline script bắt lỗi JS: `window.onerror` + `unhandledrejection` + `Vue.config.errorHandler` (3 kênh) | **0 TC** | **GAP** | **[BLOCKER] AP-1** — fix dạng **generic catch** (bắt mọi lỗi JS không biết trước) nhưng **không TC nào trigger dù chỉ 1 kênh**. Cần ≥ 3 trigger khác nhau + 1 lỗi **chưa biết trước** để chứng minh nhánh fallback chạy. |
| **G2** | diff code + dev-impact (F7, F9, D2) | `MobileEventBookingController::reportJsError` (+49) + route `POST /ajax/booking-event/report-js-error` (`routes/web.php` +2, **CSRF-exempt**) + ghi app log | **0 TC** | **GAP** | Endpoint HTTP **mới, public, miễn CSRF** hoàn toàn không được test. REQ-010 của chính Studio (miễn CSRF · message rỗng → `success:false` · cắt 500/500/300/1000 · throttle 10 phút → `throttled:true` nhưng vẫn ghi log) **không có TC nào**. |
| **G3** | diff code + dev-impact (F8, D3) | `notifyChatworkJsError` — `app/Helpers/functions.php` (+64): Guzzle POST **đồng bộ** tới Chatwork phòng `100533944`, timeout 5s, **retry 3 lần** cho 5xx/429/timeout | **0 TC** | **GAP** | File có diff lớn nhất về số hàm mới mà **0 TC**. REQ-011 (kênh báo lỗi hỏng/chậm không được phá luồng đăng ký) không có TC. Worst case ~15s treo request nền. |
| **G4** | dev-impact (F6) | `Vue.config.errorHandler` — trước fix Vue **nuốt lỗi vào console** (đây là cơ chế làm bug gốc trở nên vô hình) | **0 TC** | **GAP** | Không TC nào chứng minh lỗi Vue giờ đã **nổi lên** thay vì bị nuốt. Đây là cơ chế phòng thủ chính cho lần sau — không verify = không biết nó có chạy. |
| **G5** | diff code | **Sibling cùng kiến trúc**: Lesson booking (FA-019) LIFF là **SPA Vue 2 một trang, chuyển bước wizard bằng biến** (`spec-features/admin/lesson-booking/feature-spec.md:225`) — cùng shape với Event Booking. Salon booking (FA-020) cùng cặp. | **0 TC** | **GAP** | **REG-SHARED-001 ghi thẳng: "cặp Salon / Lesson / Booking Event là điểm lặp lại"**. Dev không kê 2 màn này trong caller đã check. Nếu 2 trang đó cũng cuộn bằng anchor `#top` thì **cùng bug vẫn còn sống** ở đó. Cần Dev `grep '#top'` toàn repo và trả lời. |
| **G6** | dev-impact (BUG) + symptom-only | **Root cause thứ 2 chưa được điều tra** — Journal #135615/#135618: friend 「平野れみ」 **đã đăng ký thành công** sau khi bấm 「もどる」, nhưng vẫn nằm trong nhóm **"không ai nhận được tin xác nhận"** | **0 TC** | **GAP** | **[BLOCKER] AP-2.** Giả thuyết của Dev (request chưa tới server) giải thích được 4 người không có đơn, **nhưng KHÔNG giải thích được vì sao người CÓ đơn cũng không nhận tin**. Dev dẫn "6/6 đơn có thật đều gửi action 21212607" mà **không xác nhận đơn của 平野れみ có nằm trong 6 đơn đó không**. Nếu không → còn 1 lỗi độc lập ở khâu gửi action. |
| **G7** | dev-impact (T1) + spec BR-18 | **Nhánh `リクエスト制`** (`approval_system = 1` → `status = 3` → action **8002** 「予約リクエスト申請時」) — chính là luồng khách gặp (「リクエストお待ちください」) | **0 TC** | **GAP** | Cả 11 TC đều ngầm định nhánh `全承認` (`status = 5`, action 8001). Màn hình khách mô tả là màn **リクエスト制**, nhưng không TC nào cấu hình nhánh này. |
| **G8** | dev-impact (F1) | Danh sách **15 điểm** đổi bước đã thay `#top` → `scrollToTop()` (6 JS + **9 nút blade**) | TC-REGSHARED001-02 (5 biến thể A–E) | **RISK** | Dev **không liệt kê cụ thể 9 nút blade** là nút nào. TC-REGSHARED001-02 gộp 5 biến thể, **không chứng minh phủ hết 15 điểm**. Không có danh sách → không kiểm được đủ hay thiếu. |
| **G9** | dev-impact (T2, T3) | Regression **nghiệp vụ** đổi lịch (BR-16: tạo 2 bản ghi liên kết `update_to`) + hủy (`status = 4/7`) | TC-REGSHARED001-02 biến thể A, B | **RISK** | **AP-3 happy-path-only** — chỉ verify **vị trí cuộn**, không verify đổi lịch/hủy còn ghi dữ liệu đúng. Đây là 2 luồng `High` do Dev tự kê. |
| **G10** | dev-impact (T4) | Regression **luồng thanh toán** (`loadFormPayment`, màn thẻ Stripe/UnivaPay, 特定商取引法に基づく表記) | TC-REGSHARED001-02 biến thể C | **RISK** | Chỉ verify cuộn, **không chạy giao dịch thật**; TC gắn `env_tag = local-only`. **RULE-08**: bill tiền không được kết luận từ local/staging. |
| **G11** | dev-impact (T5) | Regression màn lịch sử đặt chỗ / chi tiết 開催情報 | TC-REGSHARED001-02 biến thể D | **RISK** | Gộp chung 1 biến thể, expected không nêu dữ liệu lịch sử phải đúng. |
| **G12** | dev-impact (BUG) | Root cause chính (anchor `#top` trùng hash từ lần 2) | TC-TOOLKNOW002-01 | **RISK** | TC **duy nhất** reproduce root cause lại mang mã quan điểm **`TOOL-KNOW-002` không có trong `framework/checklist-lme.md`** → theo BƯỚC 0.6 **không tính là cover**. Cần gán lại mã hợp lệ. |
| **G13** | diff code (hành vi đổi) | **Back của LINE giờ thoát LIFF** thay vì cuộn trang (hệ quả bỏ `#top`) | TC-ENV003-02 | **RISK** | Chỉ verify **phía mới** (thoát LIFF, không tạo đơn). Không TC nào kiểm phía cũ: user đang nhập dở ở bước 1 bấm Back → **mất toàn bộ dữ liệu đã nhập**, đây là UX xấu đi sau fix. |
| **G14** | orphan | — | — | — | Không có TC orphan: cả 11 TC đều map được về ít nhất 1 vùng ảnh hưởng. |

---

## 2. Thiếu so với quan điểm test

**Kết luận: 9/17 quan điểm Trigger khớp đã cover đủ · 8 quan điểm thiếu.**

| Mã | Quan điểm | Ưu tiên | Vì sao Trigger khớp | Thiếu gì | Severity |
|---|---|---|---|---|---|
| **Q1** | `DEPLOY-ASSET-001` ★ | **Cao tuyệt đối** | Trigger: "release có sửa file **JS**/CSS". Release sửa `order-item.js` + blade. **"Cao tuyệt đối nếu file bị sửa nằm trong luồng thanh toán"** — `order-item.js` chứa `loadFormPayment` → đúng điều kiện. | **0 TC.** REQ-013 của Studio nêu đúng rủi ro này (khách phải nhận bản JS đã vá) nhưng bị hạ xuống thành 1 dòng precondition và note *"không cần testcase deploy riêng"*. **Precondition không phải bằng chứng** — không có TC nào mở màn bằng cache bản cũ rồi **F5 thường** (không Ctrl+F5) để chứng minh khách thật nhận được bản mới. Cả 11 TC sẽ pass với JS cũ lẫn mới nếu tester tự Ctrl+F5. | **[BLOCKER]** |
| **Q2** | `MSG-USER-001` | Cao | Trigger: BẮT BUỘC khi chức năng tương tác với friend LINE. Đặt chỗ gửi action 8001/8002 tới friend. | **0 TC verify tin nhắn thật trên LINE.** TC-FUNC001-01 (core E2E) dừng ở `予約履歴` + admin list + số chỗ. **Triệu chứng gốc khách báo chính là "không nhận được tin xác nhận"** — mà không TC nào kiểm tin đó. **RULE-06** (output cuối chuỗi). REQ-004 của Studio có yêu cầu này, TC lại bỏ. | **[BLOCKER]** |
| **Q3** | `OUT-TRUTH-001` | Cao | Trigger: BẮT BUỘC với mọi thao tác **có thông báo kết quả**. Bản chất bug là **false success** — UI nói 「ありがとうございます」 khi đơn chưa được ghi. | Studio **tự chọn** OUT-TRUTH-001 trong `test_viewpoint_selection` nhưng **không TC nào mang mã này**. Thiếu TC đối chiếu thông báo UI với trạng thái thật trong DB (`status = 5` vs `3`), và TC "đăng ký thất bại thì KHÔNG được hiện màn thành công". | **[BLOCKER]** |
| **Q4** | `CONC-001` | Cao | Trigger: BẮT BUỘC khi có nút thực thi hành động quan trọng. | Quan điểm yêu cầu **4 kịch bản**; TC-CONC001-01 chỉ làm **1** (戻る rồi sửa rồi bấm **đúng một lần**). **Thiếu kịch bản (1) double-click** — mà REQ-005 của Studio ghi rõ *"hoặc bấm 「申し込む」 nhiều lần liên tiếp"*. Thiếu cả (2) 2 tab/2 thiết bị cùng user. Khách bối rối bấm lại nhiều lần chính là bối cảnh của ticket. | **[MAJOR]** |
| **Q5** | `SEC-001` | Cao | Trigger: BẮT BUỘC khi chức năng chạm dữ liệu cá nhân. Beacon gửi `message` / `user_agent` / `stack` / `page_url` (URL chứa `u_code` định danh friend) ra **Chatwork phòng chung**. | **0 TC.** REQ-012 (chuỗi chứa thẻ script / ký tự điều khiển / xuống dòng giả mạo định dạng tin) không có TC. Studio **tự loại** `SEC-INJECT-001` khỏi phạm vi. Endpoint **miễn CSRF + public** → bất kỳ ai cũng POST được vào kênh cảnh báo nội bộ. | **[MAJOR]** |
| **Q6** | `DATA-CACHE-001` | Trung bình → **Cao** (output user-facing) | Trigger: "release có đổi JS/asset". Output là trang đăng ký của khách cuối → nâng Cao. | **0 TC.** Thiếu kịch bản bắt buộc của quan điểm: mở sẵn 1 tab **trước** deploy, **không refresh**, thao tác tiếp sau deploy (JS cũ + backend mới). | **[MAJOR]** |
| **Q7** | `LIFF-ENTRY-001` ★ | Cao | Trigger: BẮT BUỘC khi tính năng phát sinh URL cho LINE user access (**event**). Fix **đổi URL** (bỏ `#top`). | Thiếu **ma trận điểm vào** bắt buộc: user **chưa kết bạn** OA → redirect màn kết bạn; mở bằng **trình duyệt ngoài** / **PC** (không phải in-app LINE). Sau khi đổi cơ chế điều hướng, các điểm vào này chưa được xác nhận lại. | **[MAJOR]** |
| **Q8** | `RULE-01` — pattern tối thiểu | — | 9/11 TC ở `priority = High`. | **Toàn bộ 11 TC: Normal 5 · Abnormal 6 · Boundary 0.** Không TC nào ghi lý do miễn Boundary. Áp cho 4 quan điểm Cao đang có TC: `FUNC-001`, `CONC-001`, `REG-SHARED-001`, `ENV-003`. Thiếu biên rõ nhất: số lượng đặt chỗ ở mức 残数, giới hạn 5 báo lỗi/lượt, ngưỡng cắt chuỗi 500/300/1000 của endpoint. | **[MAJOR]** |

---

## 3. TC trùng lặp nội dung

**Đã rà toàn bộ 11 TC theo 4 yếu tố (mã quan điểm × loại case × đối tượng+thao tác × tiền đề tương đương) — không phát hiện cặp nào đủ điều kiện đề nghị xóa hoặc gộp.**

Ghi nhận 2 cặp **chồng lấn một phần**, đã chạy gate "giả định xóa rồi tính lại coverage" và **giữ nguyên cả hai**:

| Nhóm chồng lấn | TC | Yếu tố trùng | Yếu tố KHÁC nhau (lý do giữ) | Kết luận |
|---|---|---|---|---|
| Lặp đổi bước nhiều lần vẫn cuộn đúng | TC-TOOLKNOW002-01 · TC-ENV003-01 · TC-SYNCAPP001-01 | Đối tượng + thao tác (lặp bấm 「予約確認にすすむ」 → kiểm vị trí cuộn) | **Môi trường thực thi khác nhau**: local/auto (4 lần lặp) vs iPhone thật (3 lần) vs Android thật (3 lần). `ENV-003` + `SYNC-APP-001` **bắt buộc** thiết bị thật vì bug chưa tái hiện được trên desktop. Xóa bất kỳ TC nào đều mất 1 môi trường bắt buộc. | **Giữ cả 3** |
| E2E tạo đơn | TC-FUNC001-01 · TC-CONC001-01 | Cùng có oracle "chưa bấm 申し込む thì chưa có booking" | TC-FUNC001-01 verify sâu số chỗ + admin participant list + `予約履歴`; TC-CONC001-01 thêm chiều **sửa dữ liệu sau khi 戻る phải dùng giá trị mới**. Xóa TC-FUNC001-01 làm `FUNC-001` mất cover. | **Giữ cả 2** |

Không có `DUP-EXACT` / `DUP-INFLATE` / `DUP-CONFLICT` giữa các TC. (Mâu thuẫn expected **giữa TC và requirement REQ-003** → xem §6.)

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | **[BLOCKER]** | Toàn bộ task #301 | **0/11 TC đã chạy** (`pass = 0`, `untested = 11`, mọi `last_exec = null`) → **tỷ lệ pass 0%**. Bộ TC này **chưa cho ra kết luận nào** về bản fix. `envAuto` ghi `local` có `runs = 1` nhưng `automated = 0` → đã có 1 lượt pipeline mà **không TC nào ghi được kết quả** — cần xác định lượt chạy đó hỏng ở đâu. | Chạy bộ TC trước khi Leader duyệt. Truy vấn `task_get_report(301)` sau khi chạy để đối chiếu lịch sử run/env. |
| 2 | **[MAJOR]** | 8/11 TC (`env_tag = local-only`) | **RULE-08 / ENV-003**: task chạm **asset JS**, **URL/domain** (bỏ `#top`), **API bên thứ 3** (Chatwork), và **luồng thanh toán** (`loadFormPayment`) — nhưng **0 TC chạy production**. Bug gốc xảy ra **trên production** và chưa từng tái hiện ở env nào khác. | Nâng `env_scope` lên `prd` cho nhóm TC asset/deploy, Chatwork, thanh toán. Không kết luận "fix OK" từ local. |
| 3 | **[MAJOR]** | `03-dev-impact.md` | File có `Auto-filled: 2026-09-15 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" chưa tick**. Dev **không kê theo format `F*`/`D*`/`T*`** — bảng 4.1/4.2/4.3 hiện tại do `/new-task` **map thủ công** từ mục "File thay đổi" + mục 3 của Dev. | Tester đọc lại Journal #136321 trên Redmine, xác nhận không sót impact rồi tick. Trước khi tick, mọi kết luận coverage ở §1 chỉ là tạm. |
| 4 | **[MAJOR]** | Dev mục 3 + 4.1 | **Danh sách nơi ảnh hưởng không đầy đủ** (`REG-SHARED-001` yêu cầu dev cung cấp danh sách). Dev nói "15 chỗ (6 JS + 9 nút blade)" nhưng **chỉ nêu tên 6 hàm JS**, **9 nút blade không được liệt kê**. Sibling Lesson/Salon booking không được nhắc tới (xem G5). | Yêu cầu Dev dán danh sách đầy đủ 15 điểm (file:line) + kết quả `grep '#top'` toàn repo. |
| 5 | **[MINOR]** | TC-TOOLKNOW002-01 | Mã quan điểm **`TOOL-KNOW-002` không tồn tại** trong `framework/checklist-lme.md` (mã nội bộ Studio) → `/review-tc` không map được coverage; TC reproduce root cause chính vì thế **không được tính cover** (G12). | Đổi sang mã hợp lệ — phù hợp nhất là `REG-SHARED-001` hoặc `UI-003`. |
| 6 | **[MINOR]** | 11/11 TC | Cột `Trạng thái đánh giá spec` trống toàn bộ (`spec_status = null`), dù `spec-features/admin/event-booking/feature-spec.md` có đủ BR-04/BR-18/BR-22 để đối chiếu. | Điền `Spec ghi rõ` cho TC bám BR, `Đã hỏi leader` cho phần suy diễn. |

### Chất lượng từng TC

| # | Severity | TC | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 7 | **[MAJOR]** | TC-FUNC004-01 | **Mã quan điểm gán sai**: `FUNC-004` = "chức năng có **giới hạn số lượng / ký tự / dung lượng**", nhưng nội dung TC là matrix **bật/tắt field** tên & email (guard undefined). Map coverage sẽ báo `FUNC-004` đã cover trong khi giới hạn số lượng thật (残数, 1回の予約上限) **không có TC nào**. | Đổi sang `FUNC-002` (form nhập liệu) hoặc `UI-FIELD-001`. Giữ `FUNC-004` cho TC giới hạn số lượng thật. |
| 8 | **[MAJOR]** | TC-REGSHARED001-02 | **Không atomic** (4a #4): 1 TC gộp **5 biến thể A–E** trải trên 5 luồng nghiệp vụ khác nhau (đổi lịch / hủy / thanh toán / lịch sử / điều khoản), với ≥ 5 expected không liên quan nhau. Fail 1 biến thể thì cả TC fail, không biết luồng nào hỏng. | Tách tối thiểu theo 3 luồng có rủi ro riêng: đổi lịch · hủy · thanh toán. |
| 9 | **[MAJOR]** | TC-ENV003-01 · TC-SYNCAPP001-01 · TC-TOOLKNOW002-01 | **Expected tự nới lỏng tiêu chí** — cả 3 TC đều ghi biến thể của câu *"không fail riêng nếu 「まだ予約は完了していません」/「申し込む」 nằm dưới fold"*. Nhưng **đó chính xác là điều đã gây ra bug**: khách không thấy nút nên tưởng xong. TC pass mà khách vẫn gặp lại triệu chứng cũ. Mâu thuẫn trực tiếp với REQ-003 → xem §6. | Chốt lại tiêu chí pass với Leader/Dev trước khi chạy (§6), rồi sửa expected của cả 3 TC. |
| 10 | **[MAJOR]** | AP-3 — TC-REGSHARED001-02 | **Happy-path-only regression**: 4 tính năng `High` do Dev kê (T2 đổi lịch, T3 hủy, T4 thanh toán, T5 lịch sử) chỉ được cover bởi **1 TC gộp**, precondition toàn trạng thái sạch. Không có edge state (booking đã quá hạn đổi, slot đầy, `status_webhook` đang xử lý → BR-10). | Thêm TC regression nghiệp vụ cho đổi lịch (BR-16) — xem §5. |
| 11 | **[MINOR]** | TC-UI003-01 | Tiền đề để ngỏ *"chặn đường gọi hoặc ép trả về 500 — ghi rõ cách mô phỏng đã dùng vào kết quả chạy"* → 2 tester chạy ra 2 cách khác nhau, kết quả không so sánh được giữa các đợt. | Chốt 1 cách mô phỏng chuẩn (DevTools request blocking) ghi thẳng vào tiền đề. |

> Không dùng §4 của `checklist-lme.md` (FORM-01/CHAT-01/ADM-01/03/04/TPL-01) để flag — theo **RULE-11**.

---

## 5. TCs đề xuất bổ sung (19)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | [kho-tcs/fa021-eventbooking-イベント予約.md](../../kho-tcs/fa021-eventbooking-イベント予約.md) — 375 TC; đọc nhóm 14–20 (LINE user LIFF, `TC-EBK-150` → `TC-EBK-206`) |
| Vùng regression phát hiện từ kho | `TC-EBK-165` → `TC-EBK-183` (đặt chỗ & giới hạn số lần) · `TC-EBK-184` → `TC-EBK-194` (đổi lịch) · `TC-EBK-195` → `TC-EBK-199` (hủy) · `TC-EBK-200` → `TC-EBK-206` (lịch sử booking) — toàn bộ nằm trên trang LIFF vừa bị đổi cơ chế điều hướng ở 15 điểm |
| Conflict expected vs kho | **Không** conflict với kho. (Conflict thật nằm giữa TC Studio và REQ-003 của chính Studio → §6.) |
| GAP dùng lại TC kho (không viết mới) | **Q2 (nhánh 全承認)** → dùng lại **`TC-EBK-170`** "Đặt chỗ không bill tiền → tạo booking, **gửi action**, hiện ở màn quản lý phía admin" (expected đã có *"LINE user nhận đúng tin action「予約完了」"*). Chỉ cần thêm tiền đề fixture của ticket này (≥10 friend info + trang dài ≥2 viewport). TC mới bên dưới chỉ viết cho nhánh **リクエスト制** mà kho chưa có. |
| Xác nhận chống trùng | Đã đối chiếu **11 TC** ở BƯỚC 0 + kho FA-021 — **không TC đề xuất nào trùng**. |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-DEPLOYASSET001-01 | UI | DEPLOY-ASSET-001 | LINE user — mở link & entry | Normal | manual | product | Khách đang có bản JS cũ trong bộ nhớ đệm, chỉ F5 thường vẫn nhận được bản đã vá | Máy thật đã mở trang đăng ký sự kiện **trước khi** phát hành bản vá (để trình duyệt lưu lại file JS cũ). Bản vá đã phát hành lên production. Mở sẵn DevTools (hoặc dùng máy tính có kết nối USB debug với điện thoại). | 1. Trước khi phát hành: mở trang đăng ký, để yên tab đó.<br>2. Sau khi phát hành: quay lại tab cũ, **bấm F5 thường — tuyệt đối KHÔNG Ctrl+F5**.<br>3. Mở DevTools > Network, lọc file `order-item.js`, đọc mã trạng thái và tham số phiên bản trên đường dẫn.<br>4. Nhập đủ thông tin, bấm 「予約確認にすすむ」 2 lần liên tiếp (có bấm 「戻る」 ở giữa), quan sát vị trí cuộn. | Trang đã cache bản JS trước khi vá | Sau F5 thường: `order-item.js` trả **200** kèm **tham số phiên bản MỚI**, khác giá trị trước khi phát hành; không có lỗi 404.<br>Cả 2 lần đổi bước đều cuộn về đầu trang → chứng minh máy khách đang chạy bản đã vá chứ không phải bản cũ.<br>Không phải dùng Ctrl+F5 mới ra kết quả đúng. | | Lấp `Q1` · **RULE-08** (asset/domain, bắt buộc production) · `order-item.js` nằm trong luồng thanh toán → DEPLOY-ASSET-001 ở mức Cao tuyệt đối · Evidence: ảnh chụp DevTools Network (tham số phiên bản + 200) · Đánh giá spec: Spec không ghi — đã hỏi leader |
| TC-DEPLOYASSET001-02 | UI | DATA-CACHE-001 | LINE user — mở link & entry | Abnormal | manual | product | Tab mở sẵn từ trước khi phát hành, không tải lại trang, vẫn đăng ký được hoặc báo lỗi rõ ràng | Có 1 tab trang đăng ký mở sẵn **trước** thời điểm phát hành bản vá, đã nhập dở thông tin, **không** đóng, **không** tải lại. | 1. Trước khi phát hành: mở trang, nhập đủ thông tin, dừng ở bước nhập.<br>2. Phát hành bản vá lên production.<br>3. Quay lại tab cũ (JS cũ + máy chủ mới), bấm 「予約確認にすすむ」 rồi 「申し込む」.<br>4. Kiểm 「予約履歴」, danh sách người tham gia phía quản trị và số chỗ đã dùng. | Tab giữ bản JS trước khi vá, máy chủ đã chạy bản mới | Hoặc đăng ký thành công bình thường và ghi đúng 1 đơn, hoặc báo lỗi rõ ràng yêu cầu tải lại trang.<br>**Tuyệt đối không** được im lặng thất bại, không được tạo đơn hỏng, không được hiện thông báo thành công mà không có đơn.<br>Số chỗ đã dùng khớp số đơn thật. | | Lấp `Q6` · kịch bản (1) bắt buộc của DATA-CACHE-001 · rủi ro thật vì endpoint `report-js-error` là route mới — JS cũ không biết route này · Evidence: ảnh chụp tab cũ sau phát hành + bản ghi trong DB |
| TC-MSGUSER001-01 | UI | MSG-USER-001 | LINE user — đặt chỗ & giới hạn số lần | Normal | manual | Tất cả | Sự kiện chế độ リクエスト制 — đăng ký xong nhận đúng tin 予約リクエスト申請時 trên LINE thật | Sự kiện có khung giờ đặt **`リクエスト制`** (`approval_system = 1`). Khung giờ đó đã gán action nhóm 「予約リクエスト申請時」 với nội dung nhận dạng được. Sự kiện dựng đúng fixture của ticket: **≥ 10 mục thông tin khách hàng** + nội dung đầu trang/mô tả dài để trang cuộn được ≥ 2 viewport. Tài khoản LINE test đã kết bạn, **chưa bị chặn**. | 1. Ghi lại số tin nhắn hiện có trong đoạn chat giữa tài khoản test và bot.<br>2. Mở trang đăng ký từ LINE, nhập đủ thông tin, bấm 「予約確認にすすむ」.<br>3. Bấm 「申し込む」 **một lần**.<br>4. Quan sát màn hình kết quả hiển thị ngay sau đó.<br>5. Mở đoạn chat với bot trên **điện thoại thật**, đọc tin mới nhận.<br>6. Mở màn quản trị danh sách người tham gia, đọc trạng thái của đơn vừa tạo. | Sự kiện リクエスト制, 1 người, action 「予約リクエスト申請時」 gắn nội dung riêng | Màn hình hiện đúng thông báo của chế độ リクエスト制 (「リクエストお待ちください」 — không phải màn hoàn tất của chế độ tự động duyệt).<br>Tài khoản LINE **nhận đúng 1 tin** đúng nội dung action 「予約リクエスト申請時」, không phải action của chế độ tự động duyệt, không im lặng.<br>Màn quản trị hiện đơn ở trạng thái **承認待ち**.<br>Đây là triệu chứng gốc khách báo — nếu không nhận được tin thì **dừng và mở ticket riêng**. | | Lấp `Q2` + `G7` + `G6` · **RULE-06** output cuối trên thiết bị thật · spec BR-18 + BR-04 (`status = 3`) · nhánh 全承認 dùng lại `TC-EBK-170` của kho · Evidence: ảnh chụp màn LINE thật + ảnh màn quản trị |
| TC-MSGUSER001-02 | UI | MSG-USER-001 | LINE user — đặt chỗ & giới hạn số lần | Abnormal | manual | Tất cả | Friend đã chặn bot rồi đăng ký — đơn vẫn phải được ghi, hệ thống không treo vì không gửi được tin | Tài khoản LINE test **đã chặn** bot (blocked). Vẫn còn giữ đường dẫn đăng ký kèm mã người dùng. Khung giờ còn chỗ. | 1. Chặn bot từ phía tài khoản LINE test.<br>2. Mở đường dẫn đăng ký đã lưu, nhập đủ thông tin, bấm 「予約確認にすすむ」 rồi 「申し込む」.<br>3. Quan sát màn hình kết quả.<br>4. Kiểm màn quản trị danh sách người tham gia và số chỗ đã dùng.<br>5. Bỏ chặn bot, kiểm xem có tin nào được gửi bù hay không. | Tài khoản đã chặn bot, đăng ký 1 chỗ | Đơn **vẫn được tạo** đầy đủ, hiện ở màn quản trị, số chỗ tăng đúng.<br>Trang không treo, không lỗi trắng màn dù không gửi được tin LINE.<br>Ghi nhận rõ hành vi remind: theo spec **BR-22** chỉ đăng ký lịch nhắc khi tài khoản chưa bị chặn — xác nhận đúng như vậy. | | Lấp `Q2` (nhánh lifecycle) · spec BR-22 · MSG-USER-001 yêu cầu kiểm từng trạng thái vòng đời friend · Evidence: ảnh màn quản trị + trạng thái chặn |
| TC-OUTTRUTH001-01 | UI | OUT-TRUTH-001 | LINE user — đặt chỗ & giới hạn số lần | Normal | auto | Tất cả | Màn kết quả hiển thị đúng trạng thái thật của đơn ở cả 2 chế độ duyệt | Có 2 khung giờ trong cùng sự kiện: S1 để **全承認**, S2 để **リクエスト制**. Cả 2 dùng fixture trang dài của ticket. Đã ghi lại số đơn hiện có của tài khoản test. | 1. Đặt chỗ vào S1 (全承認), bấm 「申し込む」, **chụp lại nguyên văn** màn kết quả.<br>2. Đọc trạng thái đơn vừa tạo ở màn quản trị.<br>3. Đặt chỗ vào S2 (リクエスト制), bấm 「申し込む」, chụp lại màn kết quả.<br>4. Đọc trạng thái đơn thứ hai ở màn quản trị.<br>5. Đối chiếu từng cặp: nội dung màn hình ↔ trạng thái thật. | S1 全承認 · S2 リクエスト制 · mỗi bên 1 chỗ | S1: màn kết quả báo đã đặt xong **và** đơn ở trạng thái **予約済み**; hai bên khớp nhau.<br>S2: màn kết quả báo đang chờ duyệt **và** đơn ở trạng thái **承認待ち**; hai bên khớp nhau.<br>**Không** trường hợp nào màn hình báo đã xong trong khi đơn chưa được duyệt, hoặc ngược lại. | | Lấp `Q3` + `G7` · spec BR-04 + BR-18 · đây là quan điểm Studio tự chọn nhưng chưa có TC · Evidence: 2 cặp ảnh màn hình ↔ trạng thái quản trị |
| TC-OUTTRUTH001-02 | UI | OUT-TRUTH-001 | LINE user — đặt chỗ & giới hạn số lần | Abnormal | auto | Tất cả | Yêu cầu đăng ký thất bại giữa chừng thì không được hiện màn thành công | Có cách làm cho yêu cầu đăng ký thất bại từ phía trình duyệt: ngắt mạng ngay sau khi bấm nút, hoặc chặn đường gọi tới địa chỉ xử lý đăng ký. Ghi rõ cách đã dùng vào kết quả chạy. Đã ghi lại số đơn và số chỗ trước khi thao tác. | 1. Nhập đủ thông tin, sang màn xác nhận.<br>2. Bật cách chặn đường gọi xử lý đăng ký.<br>3. Bấm 「申し込む」 một lần, chờ xử lý kết thúc.<br>4. Đọc nguyên văn thông báo hiện ra.<br>5. Kiểm 「予約履歴」, màn quản trị và số chỗ đã dùng.<br>6. Bỏ chặn, thử đăng ký lại bình thường. | Đường gọi xử lý đăng ký bị chặn hoặc trả về lỗi máy chủ | **Không** hiện màn hoàn tất, **không** hiện 「ありがとうございます」.<br>Hiện thông báo lỗi nêu đúng nguyên nhân và cho phép thử lại.<br>**Không có** đơn nào được tạo; số chỗ đã dùng **không đổi**.<br>Sau khi bỏ chặn, đăng ký lại thành công và chỉ tạo đúng 1 đơn. | | Lấp `Q3` · đây là chiều "báo thành công thì dữ liệu phải thật sự được lưu" của OUT-TRUTH-001 · Evidence: ảnh thông báo lỗi + số đơn trước/sau |
| TC-CONC001-02 | UI | CONC-001 | LINE user — đặt chỗ & giới hạn số lần | Abnormal | auto | Tất cả | Bấm nhanh nút đăng ký nhiều lần liên tiếp chỉ tạo đúng một đơn và một tin nhắn | Khung giờ còn **≥ 3 chỗ**. Fixture trang dài của ticket. Đã ghi lại số chỗ đã dùng, số đơn của tài khoản test và số tin trong đoạn chat. | 1. Nhập đủ thông tin, sang màn xác nhận.<br>2. Bấm 「申し込む」 **3 lần liên tiếp thật nhanh** (dưới 1 giây).<br>3. Chờ xử lý kết thúc hoàn toàn.<br>4. Đếm số đơn mới ở 「予約履歴」 và ở màn quản trị danh sách người tham gia.<br>5. Đọc số chỗ đã dùng của khung giờ.<br>6. Đếm số tin nhận được trong đoạn chat LINE. | Bấm nút đăng ký 3 lần liên tiếp | Chỉ tạo **đúng 1 đơn**; 「予約履歴」 và màn quản trị đều chỉ có **1** dòng mới.<br>Số chỗ đã dùng tăng **đúng 1 lần** theo số lượng đã chọn, không cộng dồn 3 lần.<br>Tài khoản LINE nhận **đúng 1 tin**, không nhận 3 tin trùng.<br>Nút phải bị khóa lại sau lần bấm đầu, hoặc hệ thống chặn xử lý trùng. | | Lấp `Q4` · kịch bản (1) double-click của CONC-001 · đúng bối cảnh ticket: khách bối rối bấm lại nhiều lần · REQ-005 yêu cầu nhưng TC-CONC001-01 không làm · Evidence: số đơn + số chỗ + số tin sau khi cố tình bấm trùng |
| TC-CONC001-03 | UI | CONC-001 | LINE user — đặt chỗ & giới hạn số lần | Abnormal | auto | Tất cả | Cùng một người mở 2 phiên song song và bấm đăng ký gần như cùng lúc | Khung giờ **định viên đúng 1 chỗ** còn trống. Mở trang đăng ký trên **2 thiết bị / 2 phiên khác nhau** cùng một tài khoản LINE test, cả hai đều đã ở màn xác nhận. | 1. Đưa cả 2 phiên tới màn xác nhận với cùng khung giờ.<br>2. Bấm 「申し込む」 ở cả 2 phiên gần như cùng lúc (lệch dưới 1 giây).<br>3. Chờ cả 2 xử lý xong, đọc thông báo của từng phiên.<br>4. Đếm số đơn tạo ra và số chỗ đã dùng. | 2 phiên song song, khung giờ còn đúng 1 chỗ | Chỉ **1 phiên** đặt thành công; phiên còn lại nhận thông báo lỗi rõ ràng (hết chỗ hoặc xung đột), **không bị treo màn**.<br>Tổng cộng chỉ **1 đơn** mới; số chỗ đã dùng **không vượt định viên**.<br>Không có đơn dở dang nào được để lại. | | Lấp `Q4` · kịch bản (2) của CONC-001 · phân biệt với `TC-EBK-183` của kho (2 **người khác nhau** đặt cùng lúc) — case này là **cùng một người, 2 phiên** · lưu ý: đổi bot theo phiên nên phải dùng 2 phiên đăng nhập riêng · Evidence: thông báo của cả 2 phiên + số chỗ cuối |
| TC-SEC001-01 | API | SEC-001 | LINE user — mở link & entry | Abnormal | auto | product | Nội dung lỗi do khách gửi lên không bị thực thi và không làm vỡ định dạng tin cảnh báo | Bản vá đã lên production. Có công cụ gửi được yêu cầu HTTP tới địa chỉ nhận báo lỗi mới (`/ajax/booking-event/report-js-error`). Có quyền đọc phòng Chatwork nhận cảnh báo và đọc được log ứng dụng. | 1. Gửi yêu cầu với nội dung lỗi chứa **thẻ script**.<br>2. Gửi yêu cầu với nội dung chứa **ký tự điều khiển** và ký tự xuống dòng giả mạo định dạng tin.<br>3. Gửi yêu cầu với nội dung chứa **emoji và ký tự full-width**.<br>4. Gửi yêu cầu với nội dung **rỗng**.<br>5. Đọc tin nhận được trong phòng Chatwork và bản ghi trong log ứng dụng. | 4 mẫu nội dung: thẻ script · ký tự điều khiển + xuống dòng giả · emoji/full-width · rỗng | Tin Chatwork hiển thị nội dung ở dạng **văn bản thuần**, thẻ script **không được thực thi** và không làm hỏng bố cục tin.<br>Ký tự xuống dòng giả **không** tạo ra được trường giả mạo trong tin cảnh báo.<br>Emoji và ký tự full-width hiển thị đúng, không thành dấu hỏi.<br>Nội dung rỗng: trả về kết quả **không thành công**, không đẩy tin rác.<br>Log ứng dụng ghi đủ, không lộ thông tin định danh khách ngoài phạm vi cần thiết. | | Lấp `Q5` · REQ-012 (Studio tự loại `SEC-INJECT-001` khỏi phạm vi) · **RULE-08** (gọi dịch vụ ngoài → production) · địa chỉ nhận báo lỗi **miễn CSRF và công khai** · Nhóm API vì phải tự dựng yêu cầu ngoài trang · Evidence: ảnh tin Chatwork + dòng log |
| TC-SEC001-02 | API | SEC-001 | LINE user — mở link & entry | Abnormal | auto | product | Chặn lặp 10 phút giữ cho kênh cảnh báo không bị bơm rác từ bên ngoài | Như trên. Biết trước phòng Chatwork nhận cảnh báo đang trống hoặc đã ghi lại mốc thời gian. | 1. Gửi **10 yêu cầu giống hệt nhau** (cùng nội dung lỗi, cùng địa chỉ trang) trong vòng 1 phút.<br>2. Đếm số tin thực tế xuất hiện trong phòng Chatwork.<br>3. Đếm số dòng ghi trong log ứng dụng.<br>4. Đợi quá 10 phút, gửi lại đúng nội dung đó 1 lần nữa.<br>5. Gửi yêu cầu với nội dung **dài vượt ngưỡng** để kiểm việc cắt chuỗi. | 10 yêu cầu trùng trong 1 phút · 1 yêu cầu sau 10 phút · 1 yêu cầu nội dung quá dài | Chatwork chỉ nhận **1 tin** cho cả 10 yêu cầu trùng; các lần sau trả về trạng thái **đã bị chặn lặp**.<br>Log ứng dụng **vẫn ghi đủ** cả 10 lần (chặn lặp chỉ áp cho việc gửi tin, không được nuốt log).<br>Sau 10 phút, yêu cầu lại được gửi tin bình thường.<br>Nội dung quá dài bị **cắt đúng ngưỡng**, không làm lỗi máy chủ. | | Lấp `Q5` + `G2` · REQ-010 · cũng là case **Boundary** cho `RULE-01` (ngưỡng cắt + ngưỡng chặn lặp) · Evidence: số tin Chatwork ↔ số dòng log |
| TC-INTGHOOK002-01 | API | INTG-HOOK-002 | LINE user — mở link & entry | Abnormal | manual | product | Chatwork lỗi hoặc chậm thì trang đăng ký vẫn hoạt động bình thường | Bản vá trên production. Có cách làm cho lời gọi Chatwork thất bại hoặc chậm (sai thông tin phòng, hoặc chặn đường ra tới dịch vụ đó). Ghi rõ cách đã dùng. | 1. Bật điều kiện để lời gọi Chatwork thất bại.<br>2. Gây ra 1 lỗi JavaScript trên trang đăng ký để kích hoạt việc báo lỗi.<br>3. Đo thời gian phản hồi của yêu cầu báo lỗi.<br>4. **Ngay sau đó**, thực hiện trọn luồng đăng ký bình thường và bấm 「申し込む」.<br>5. Kiểm đơn được tạo, 「予約履歴」 và số chỗ.<br>6. Đọc log xem có ghi lại việc gửi Chatwork thất bại không. | Dịch vụ Chatwork lỗi / chậm | Yêu cầu báo lỗi **vẫn trả về thành công** và **không treo vô hạn** (thời gian phản hồi hữu hạn, không kéo dài bất thường).<br>Luồng đăng ký hoạt động **hoàn toàn bình thường**: đơn được tạo đúng, số chỗ tăng đúng, tin xác nhận vẫn gửi.<br>Log ghi rõ việc gửi cảnh báo thất bại để còn truy vết. | | Lấp `Q5` + `G3` · REQ-011 · lời gọi Chatwork là **đồng bộ, timeout 5s, thử lại 3 lần** → xấu nhất khoảng 15s, phải đo thật · **RULE-08** · Evidence: thời gian phản hồi đo được + đơn tạo thành công |
| TC-UI003-02 | UI | UI-003 | LINE user — mở link & entry | Abnormal | auto | product | Ba kênh bắt lỗi JavaScript và một lỗi chưa biết trước đều được báo về đúng kênh cảnh báo | Bản vá trên production. Mở được bảng điều khiển của trình duyệt trên trang đăng ký. Đọc được phòng Chatwork nhận cảnh báo. | 1. Gây lỗi kiểu **ném lỗi đồng bộ** trực tiếp trên trang → quan sát.<br>2. Gây lỗi kiểu **promise bị bỏ rơi** (lỗi bất đồng bộ không ai bắt) → quan sát.<br>3. Gây lỗi **bên trong thành phần giao diện Vue** (để kênh bắt lỗi của Vue xử lý) → quan sát.<br>4. Gây **một lỗi kiểu khác chưa nằm trong 3 loại trên** → quan sát.<br>5. Sau mỗi lần, kiểm phòng Chatwork và log; đồng thời kiểm trang đăng ký còn dùng được không. | 4 kiểu lỗi khác nhau, trong đó 1 kiểu chưa biết trước | **Cả 4 trường hợp** đều tạo ra cảnh báo về Chatwork và bản ghi trong log — kể cả loại lỗi chưa biết trước (chứng minh nhánh bắt lỗi chung thật sự chạy, không phải chỉ bắt vài loại định sẵn).<br>Sau mỗi lần gây lỗi, trang đăng ký **vẫn dùng được**, không trắng màn, không kẹt nút.<br>Nội dung cảnh báo nêu đủ loại lỗi và địa chỉ trang để truy vết. | | Lấp `G1` (**AP-1** — hiện 0 TC cho cơ chế bắt lỗi chung) + `G4` · yêu cầu ≥ 3 kênh + 1 lỗi chưa biết theo quy tắc generic-fix · **RULE-08** vì cần Chatwork thật · Evidence: 4 ảnh tin Chatwork tương ứng 4 kiểu lỗi |
| TC-UI003-03 | UI | UI-003 | LINE user — mở link & entry | Boundary | auto | product | Giới hạn 5 báo lỗi mỗi lượt vào trang và loại bỏ báo trùng hoạt động đúng ở đúng ngưỡng | Bản vá trên production. Đọc được phòng Chatwork và log. | 1. Vào trang đăng ký (1 lượt).<br>2. Gây **6 lỗi JavaScript khác nhau** liên tiếp trong cùng lượt đó.<br>3. Đếm số báo lỗi thật sự được gửi đi.<br>4. Gây **cùng một lỗi** 3 lần liên tiếp, đếm số báo gửi đi.<br>5. **Tải lại trang** (lượt mới), gây thêm 1 lỗi, kiểm bộ đếm đã được đặt lại chưa. | 6 lỗi khác nhau trong 1 lượt · 3 lỗi trùng · 1 lỗi sau khi tải lại | 5 lỗi đầu được gửi đi, lỗi **thứ 6 bị chặn** (đúng ngưỡng 5).<br>3 lỗi trùng chỉ tính là **1** báo.<br>Sau khi tải lại trang, bộ đếm đặt lại và lỗi mới lại được gửi.<br>Việc chặn không gây lỗi phụ nào cho trang đăng ký. | | Lấp `G1` + `Q8` (**RULE-01** — bộ TC gốc có **0 Boundary**) · ngưỡng 5 báo/lượt + loại trùng lấy từ mô tả cách fix · **RULE-08** · Evidence: số tin Chatwork ↔ số lỗi đã gây |
| TC-REGSHARED001-03 | UI | REG-SHARED-001 | LINE user — mở link & entry | Normal | auto | Tất cả | Trang đặt lịch bài học cũng nhiều bước trên một trang — kiểm có dính cùng lỗi cuộn không | **Trước khi chạy: yêu cầu Dev xác nhận** trang đặt lịch bài học (FA-019) có dùng cùng cơ chế cuộn bằng neo trang như sự kiện hay không. Dựng 1 lịch bài học có nội dung đủ dài để trang cuộn được ≥ 2 viewport và có nhiều bước chọn. | 1. Mở trang đặt lịch bài học từ LINE.<br>2. Đi qua các bước chọn khoá học → ngày giờ → nhập thông tin, mỗi lần đều cuộn xuống đáy trước khi bấm nút sang bước kế.<br>3. Quay lại bước trước rồi lặp lại việc chuyển bước **ít nhất 3 lần** trong cùng một lần mở trang.<br>4. Ghi lại vị trí cuộn sau **mỗi** lần đổi bước.<br>5. Hoàn tất đặt chỗ và kiểm đơn được ghi. | Lặp đổi bước ≥ 3 lần, không tải lại trang | Mọi lần đổi bước, kể cả **từ lần thứ 2 trở đi**, trang đều hiển thị từ đầu bước mới, không giữ vị trí đáy của bước cũ.<br>Nếu **tái hiện được** triệu chứng giống ticket 40806 → **mở ticket riêng cho FA-019**, không gộp vào ticket này.<br>Đặt chỗ hoàn tất ghi đúng 1 đơn. | | Lấp `G5` · `REG-SHARED-001` ghi thẳng *"cặp Salon / Lesson / Booking Event là điểm lặp lại"* · kiến trúc xác nhận ở `spec-features/admin/lesson-booking/feature-spec.md:225` (SPA Vue 2 một trang, chuyển bước bằng biến) · `regression` · Evidence: ảnh vị trí cuộn từng lần đổi bước |
| TC-REGSHARED001-04 | UI | REG-SHARED-001 | LINE user — mở link & entry | Normal | auto | Tất cả | Trang đặt lịch salon — kiểm có dính cùng lỗi cuộn không | **Trước khi chạy: yêu cầu Dev xác nhận** trang đặt lịch salon (FA-020) có dùng cùng cơ chế cuộn bằng neo trang hay không. Dựng 1 salon có trang đủ dài để cuộn được ≥ 2 viewport. | 1. Mở trang đặt lịch salon từ LINE.<br>2. Đi qua các bước chọn tới màn xác nhận, mỗi lần đều cuộn xuống đáy trước khi bấm nút chuyển bước.<br>3. Quay lại rồi lặp lại việc chuyển bước **ít nhất 3 lần**.<br>4. Ghi lại vị trí cuộn sau mỗi lần đổi bước.<br>5. Hoàn tất đặt chỗ và kiểm đơn được ghi. | Lặp đổi bước ≥ 3 lần, không tải lại trang | Mọi lần đổi bước đều hiển thị từ đầu bước mới.<br>Nếu tái hiện triệu chứng giống ticket 40806 → **mở ticket riêng cho FA-020**.<br>Đặt chỗ hoàn tất ghi đúng 1 đơn. | | Lấp `G5` · cùng lý do với TC trên · `regression` · Evidence: ảnh vị trí cuộn từng lần đổi bước |
| TC-REGSHARED001-05 | UI | REG-SHARED-001 | LINE user — đổi lịch | Normal | auto | Tất cả | Đổi lịch hoàn tất sau khi đổi cơ chế cuộn — dữ liệu vẫn ghi đúng, không chỉ đúng vị trí cuộn | Tài khoản LINE test đã có **1 đơn hợp lệ** còn trong hạn được đổi. Có khung giờ đích còn chỗ. Các màn đủ dài để cuộn được. Đã ghi lại số chỗ đã dùng của **cả khung giờ nguồn và khung giờ đích**. | 1. Từ 「予約履歴」 mở chi tiết đơn, bấm 「予約内容変更」.<br>2. Cuộn xuống đáy, chọn khung giờ đích khác, bấm sang màn xác nhận thay đổi.<br>3. Bấm 「変更内容を修正」 quay lại, đổi tiếp số lượng, rồi sang xác nhận **lần nữa** (lặp chuyển bước ≥ 2 lần).<br>4. Hoàn tất việc đổi lịch.<br>5. Kiểm 「予約履歴」, màn quản trị, **số chỗ của cả 2 khung giờ**, và tin nhận trên LINE. | Đổi khung giờ + đổi số lượng, lặp chuyển bước ≥ 2 lần | Mọi lần chuyển bước đều cuộn về đầu **và** dữ liệu đã chọn không bị mất.<br>Sau khi hoàn tất: hệ thống ghi nhận đổi lịch đúng theo cơ chế cặp bản ghi liên kết của spec (**BR-16**), không tạo đơn thừa, không mất đơn cũ.<br>Số chỗ khung giờ nguồn **giảm đúng**, khung giờ đích **tăng đúng**.<br>Tài khoản LINE nhận đúng tin của nhóm thay đổi đặt chỗ. | | Lấp `G9` + `Q8` · **AP-3** — T2 (đổi lịch, rủi ro `High`) hiện chỉ được verify vị trí cuộn · spec BR-16 · đối chiếu kho `TC-EBK-185` → `TC-EBK-194` · `regression` · Evidence: bản ghi đổi lịch + số chỗ 2 đầu + ảnh tin LINE |
| TC-PAYSTATE001-01 | UI | PAY-STATE-001 | Thanh toán — Stripe | Normal | manual | product | Sự kiện có thu phí — đi trọn luồng thanh toán sau khi đổi cơ chế cuộn | Sự kiện **bật thu phí**, cấu hình cổng thanh toán hoạt động trên production. Có thẻ test hợp lệ theo quy định của cổng. Trang đủ dài để cuộn được. Đã ghi lại số chỗ và trạng thái thanh toán trước khi thao tác. | 1. Đặt chỗ vào khung giờ có thu phí, nhập đủ thông tin, sang màn nhập thẻ.<br>2. Ở màn thẻ, cuộn xuống đáy rồi bấm quay lại; sau đó sang lại màn thẻ (lặp chuyển bước ≥ 2 lần).<br>3. Mở link 「特定商取引法に基づく表記」, cuộn xuống, bấm quay lại.<br>4. Nhập thẻ, sang màn xác nhận cuối, bấm nút thanh toán **một lần**.<br>5. Kiểm màn kết quả, 「予約履歴」, trạng thái thanh toán ở màn quản trị và tin nhận trên LINE. | Sự kiện có thu phí, thẻ test hợp lệ, lặp chuyển bước ≥ 2 lần | Mọi lần chuyển bước trong luồng thanh toán đều cuộn về đầu và **không mất thông tin thẻ đã nhập dở** (hoặc báo rõ phải nhập lại).<br>Giao dịch hoàn tất: tạo đúng **1** đơn, trạng thái thanh toán chuyển đúng sang đã thanh toán, số chỗ tăng đúng 1 lần.<br>Không phát sinh giao dịch trùng.<br>Tài khoản LINE nhận đúng tin xác nhận. | | Lấp `G10` + `Q1` · **RULE-08 bắt buộc production** (bill tiền) · `order-item.js` chứa hàm nạp màn thanh toán → chính là lý do `DEPLOY-ASSET-001` ở mức Cao tuyệt đối · Evidence: ảnh giao dịch trên cổng thanh toán + trạng thái đơn |
| TC-ENV003-03 | UI | ENV-003 | LINE user — mở link & entry | Abnormal | manual | Tất cả | Thoát LIFF giữa chừng ở bước nhập rồi mở lại — không mất dữ liệu ngoài dự kiến, không tạo đơn rác | Máy thật (iPhone hoặc Android) chạy LINE, mở được trang đăng ký bản đã vá. Đã ghi lại số đơn và số chỗ trước khi thao tác. | 1. Mở trang đăng ký từ LINE, nhập **dở dang** thông tin ở bước nhập (chưa sang màn xác nhận).<br>2. Dùng nút quay lại của LINE để thoát.<br>3. Quan sát: LIFF đóng lại hay chỉ đổi vị trí cuộn.<br>4. Mở lại đường dẫn đăng ký từ LINE.<br>5. Quan sát dữ liệu đã nhập trước đó còn hay mất.<br>6. Kiểm 「予約履歴」, màn quản trị và số chỗ. | Thoát giữa chừng ở bước nhập, chưa bấm nút đăng ký | Ghi nhận rõ hành vi mới: LIFF **thoát** thay vì chỉ cuộn trang (đúng cảnh báo của Dev sau khi bỏ neo trang).<br>**Không** tạo đơn nào, số chỗ **không đổi**.<br>Ghi nhận rõ dữ liệu đã nhập còn hay mất — nếu mất toàn bộ thì **báo Leader**: đây là UX xấu đi so với trước fix, cần cân nhắc cảnh báo trước khi thoát. | | Lấp `G13` · TC-ENV003-02 chỉ test phía **mới** ở màn xác nhận; case này test **phía cũ** (đang nhập dở) mà người dùng hay gặp hơn · Evidence: ảnh quay màn hình trước/sau khi thoát |
| TC-LIFFENTRY001-01 | UI | LIFF-ENTRY-001 | LINE user — mở link & entry | Abnormal | manual | Tất cả | Ma trận điểm vào sau khi bỏ neo trang — chưa kết bạn, trình duyệt ngoài, máy tính | Có 1 tài khoản LINE **chưa kết bạn** với bot. Có đường dẫn đăng ký của sự kiện (bản đã vá). Có máy tính để mở cùng đường dẫn. | 1. Tài khoản **chưa kết bạn** mở đường dẫn từ LINE → quan sát có được đưa sang màn kết bạn không.<br>2. Kết bạn xong, mở lại đường dẫn → đi trọn luồng tới khi đặt xong.<br>3. Mở cùng đường dẫn bằng **trình duyệt ngoài** trên điện thoại → lặp chuyển bước ≥ 2 lần, quan sát vị trí cuộn.<br>4. Mở cùng đường dẫn trên **máy tính** → lặp chuyển bước ≥ 2 lần, quan sát.<br>5. Với mỗi điểm vào, ghi lại có đặt chỗ hoàn tất được không. | 3 điểm vào: trong LINE (chưa kết bạn → đã kết bạn) · trình duyệt ngoài · máy tính | Chưa kết bạn: được đưa sang màn kết bạn đúng như trước, việc bỏ neo trang **không** làm hỏng bước chuyển hướng này.<br>Cả 3 điểm vào đều **cuộn về đầu ở mọi lần đổi bước** và **đặt chỗ hoàn tất được**.<br>Không điểm vào nào bị trắng màn hoặc kẹt ở bước xác nhận. | | Lấp `Q7` · fix **đổi URL** (bỏ neo trang) → phải xác nhận lại toàn bộ ma trận điểm vào bắt buộc của LIFF-ENTRY-001 · Evidence: 3 ảnh tương ứng 3 điểm vào + 1 ảnh case chưa kết bạn |

**Dòng §1/§2 không đề xuất TC mới** (có lý do):

| Mã | Lý do không viết TC mới |
|---|---|
| `G8` (15 điểm scroll chưa liệt kê) | Không phải thiếu TC mà thiếu **đầu vào**: chưa có danh sách 15 điểm thì không viết được TC phủ đủ. Đã ghi yêu cầu Dev ở §4 #4. Sau khi có danh sách mới bổ sung TC. |
| `G11` (lịch sử / chi tiết 開催情報) | Kho đã có `TC-EBK-200` → `TC-EBK-206` (lịch sử booking) phủ nội dung nghiệp vụ; chỉ cần thêm chiều "lặp chuyển bước ≥ 2 lần" vào tiền đề của các TC đó thay vì viết mới. |
| `G12` (mã quan điểm lạ) | Là lỗi gán nhãn, không phải thiếu TC — sửa mã của TC-TOOLKNOW002-01 (§4 #5). |
| `Q2` nhánh `全承認` | Dùng lại `TC-EBK-170` của kho (đã có expected *"LINE user nhận đúng tin action「予約完了」"*), chỉ bổ sung tiền đề fixture của ticket. |

---

## 6. Spec update needed

| # | Vấn đề | Chi tiết | Cần ai chốt |
|---|---|---|---|
| 1 | **Mâu thuẫn tiêu chí pass giữa REQ-003 và 3 TC của chính Studio** | **REQ-003** (`risk = High`) ghi: *"Ngay khi vào màn xác nhận, dòng 「まだ予約は完了していません」 và nút 「申し込む」 phải nằm trong khung nhìn đầu tiên mà không cần cuộn, để khách không hiểu nhầm là đã đăng ký thành công."*<br>Nhưng **TC-ENV003-01**, **TC-SYNCAPP001-01**, **TC-TOOLKNOW002-01** đều ghi ngược lại: *"không fail riêng nếu nằm dưới fold vì fix không cam kết element này nằm trong viewport đầu tiên"*.<br>→ Nếu nội dung đầu màn xác nhận đủ dài, trang cuộn về đầu **nhưng nút 「申し込む」 vẫn dưới fold** → TC **pass** trong khi **khách vẫn gặp đúng triệu chứng cũ**. | **Leader + Dev**: chốt tiêu chí pass là "cuộn về đầu trang" (như TC đang ghi) hay "nút đăng ký phải trong khung nhìn đầu" (như REQ-003). Nếu chọn REQ-003 → bản fix hiện tại **chưa đủ**, cần thêm thay đổi bố cục màn xác nhận. |
| 2 | **Root cause thứ 2 chưa được loại trừ** | Journal #135615/#135618: friend 「平野れみ」 **đã đăng ký thành công** sau khi bấm 「もどる」, nhưng khách khẳng định **không ai** trong nhóm nhận được tin xác nhận. Dev dẫn chứng *"6/6 đơn có thật đều tạo thành công và gửi action 21212607"* nhưng **không nói rõ đơn của 平野れみ có nằm trong 6 đơn đó không**. Nếu không → tồn tại một lỗi **độc lập** ở khâu gửi action mà bản fix hiện tại không chạm tới. | **Dev**: đối chiếu `line_user_id 13073264` (平野れみ) với 6 đơn đã gửi action; nếu đơn có tồn tại mà action không gửi → tách ticket riêng. **Đây là điều kiện tiên quyết trước khi đóng ticket** (RULE-04). |
| 3 | **Chưa xác định phạm vi sibling dùng chung pattern** | Spec ghi Lesson booking (FA-019) LIFF là *"SPA Vue 2 một trang — chuyển bước wizard bằng `step_booking`"* (`lesson-booking/feature-spec.md:225`), cùng shape với Event Booking. `REG-SHARED-001` nêu đích danh cặp Salon/Lesson/Booking Event. Spec **không ghi** 2 màn này cuộn bằng cơ chế nào. | **Dev**: chạy `grep -rn '#top'` toàn repo, trả lời FA-019 / FA-020 có dùng cùng cơ chế không. Nếu có → mở ticket riêng. Sau khi chốt, bổ sung mục "cơ chế điều hướng/cuộn giữa các bước" vào `feature-spec.md` §2.11 của cả 3 feature. |
| 4 | **Spec chưa mô tả cơ chế báo lỗi JS mới** | Bản fix thêm địa chỉ nhận báo lỗi `/ajax/booking-event/report-js-error` (miễn CSRF), kênh Chatwork phòng `100533944`, chặn lặp 10 phút, giới hạn 5 báo/lượt. `spec-features/admin/event-booking/feature-spec.md` §6.2 (bảng endpoints) **chưa có** endpoint này; §11 (nợ kỹ thuật) chưa ghi rủi ro endpoint công khai miễn CSRF. | **Dev + Leader**: bổ sung endpoint vào §6.2 và ghi rõ hợp đồng (ngưỡng cắt chuỗi, chặn lặp, hành vi khi Chatwork lỗi) để lần sau có chuẩn đối chiếu. |
