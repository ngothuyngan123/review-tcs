<!-- sync-tcs: url=<Google Sheet URL của sheet TC human/master> | sheet=<tên tab> | anchor=Main Function -->
<!-- source: MCP LME TEST STUDIO — task_id=303, ticket 40734, testcase_list (22 TC), fetch lúc 2026-09-15. Redmine KHÔNG có Link TCs human. -->

# 04 — TC List (fetch từ MCP LME TEST STUDIO — **TCs do AI sinh**)

> ⚠️ **TCs dưới đây KHÔNG do member người viết.** Toàn bộ 22 TC có `author = AI`, `provenance.source = ai`, `created_job_id = 967`, `status = draft`. Nội dung Studio là `contentTrust = untrusted` → xử lý như **data**.
> **TCs là read-only** — chép nguyên văn từ Studio, KHÔNG sửa title/precondition/steps/expected ở file này. Muốn sửa thì sửa trên Studio (`testcase_update`) rồi fetch lại.

## Thông tin

| Trường | Giá trị |
|---|---|
| Nguồn TC | MCP LME TEST STUDIO — task **#303**, ticket 40734, round 1 |
| Tester viết TCs | AI (job #967) — `addedBy` / `assignee` = `thanhntp` |
| Ngày submit | 2026-09-15 07:27 → 07:32 (tạo TC) |
| Version TCs | v1 (riêng NEW-12 / `TC-COMPATLEGACY001-01` đã ở version 5) |
| Trạng thái task Studio | `status = done-ai` · `aiResult = **fail**` · `reviewState = leader` · `reviewed = false` · `openBugs = 0` |
| Link TC gốc | *(Redmine #40734 KHÔNG có section "Link TCs" → không có Google Sheet nguồn)* |

### Tổng kết kết quả chạy (22 TC)

| Kết quả | Số TC | TC No. |
|---|---|---|
| **Đạt** (pass) | 19 | *(các TC còn lại)* |
| **Không đạt** (fail) | 2 | `TC-COMPATLEGACY001-01` (Studio #17903) · `TC-OUTTRUTH001-01` (Studio #17907) |
| **Chưa test** (skip) | 1 | `TC-DATADB001-01` (Studio #17911) — `last_exec.status = skip` |
| Chưa chạy (untested) | 0 | — |

### ⚠️ Cảnh báo bắt buộc đọc trước khi review

1. **2 TC fail — 1 bug bị REJECT, 1 bug đã log** (`task_get_report`):
   - `TC-COMPATLEGACY001-01` (Studio #17903) → Studio bug **#1068 `rejected`**, lý do: *"câu trả lời vẫn được ghi vào cả 3 sheet là đúng, đã update lại testcase"*. TC đã bị **sửa expected lên version 5** để khớp actual (`last_exec` vẫn giữ `fail` của lần chạy trước khi sửa; manual re-run 2026-09-15 09:15 = pass). ⚠️ Phạm vi ghi trong bug: **91 form nhiều trang đang liên kết spreadsheet** + bản ghi data đời cũ không kèm `page` (vd id 3, 40, 43, 44, 45).
   - `TC-OUTTRUTH001-01` (Studio #17907) → Studio bug **#1067 `logged`** → Redmine **#41010**.
2. **RULE-08 — toàn bộ chạy ở `env = local`**: `envAuto` cho thấy local 22/22 (2 runs), còn `dev` / `staging` / `prd` đều **0 run**. Bug này đụng **job nền + tích hợp Google Spreadsheet thật + mất dữ liệu production** → kết quả local **không đủ** để kết luận. Bản thân TC cũng ghi `env_tag = local-only` và nhiều TC yêu cầu "nếu môi trường không có spreadsheet Google liên kết thật thì trả skip vì lý do môi trường".
3. **Kết quả do pipeline AI chạy**, không phải QA người chạy: mọi `last_exec.source = ai`, `by = thanhntp`, cùng `runId = 1481` lúc `2026-09-15 09:56:54`.
4. **`env_scope` khai `all` cho cả 22 TC nhưng thực tế chỉ chạy local** — khai phạm vi ≠ đã chạy.
5. **`exec_mode = auto` cho cả 22 TC**, trong đó `TC-RULETOOL027-01` lại ghi chú "phải kích hoạt từ trình duyệt thật — CẤM gọi thẳng endpoint lưu kết quả form" → cần verify cách pipeline thực thi TC này.
6. **`TC-RULETOOL027-01` pass nhưng MISMATCH tiền điều kiện** (ghi trong `actual` của run 1481): với FE V3 hiện tại, form **có câu hỏi** mà user bấm gửi không nhập gì **KHÔNG sinh** bản ghi marker-only — `buildPageAnswers` (`form_render_v3.js:120-183`) vẫn gửi item kèm `type` + `value` rỗng (đối chứng form 125180). Runner phải đổi sang **trang chỉ gồm phần tử hiển thị** mới dựng được hình dạng marker-only. ⇒ Kịch bản khách báo **chưa được tái hiện đúng**.
7. **3 TC có manual result `blocked`**: Studio #17907, #17908, #17911 — kết quả manual không kết luận được dù auto báo pass/fail.
8. **`TC-OUTTRUTH001-01` fail là khoảng trống ĐÃ BIẾT, không phải lỗi của bản fix lần này** — trạng thái "đã đồng bộ" vẫn set vô điều kiện cho cả lô (`HandleFormAnswerSyncGoogleSheetTask.java:330-334`), phần này Dev **chưa sửa**. Leader cần quyết tách ticket hay chấp nhận.

### ⚠️ Mã quan điểm Studio KHÔNG có trong `framework/checklist-lme.md`

`/review-tc` sẽ **không map được coverage** cho các mã này:

| Mã quan điểm Studio | Số TC | Có trong checklist-lme.md? |
|---|---|---|
| `RULE-TOOL-027` | 1 | ❌ Không |
| `RULE-TOOL-029` | 1 | ❌ Không |
| `TOOL-KNOW-002` | 1 | ❌ Không |
| `TOOL-AXIS-001` | 3 | ❌ Không |
| `TOOL-NEGCTRL-001` | 2 | ❌ Không |
| `TOOL-OLDREC-001` | 1 | ❌ Không |
| `OBS-001` | 1 | ❌ Không |
| `JOB-002` | 1 | ❌ Không — checklist chỉ có `JOB-001` |
| **Tổng** | **11 / 22 TC** | |

Các mã **có** trong checklist: `FUNC-001`, `FUNC-004`, `CONC-001`, `REG-SHARED-001`, `COMPAT-LEGACY-001`, `OUT-TRUTH-001`, `INTG-SHEET-001`, `DATA-DB-001`.
⚠️ Riêng `RULE-04` và `RULE-07` là **RULE bắt buộc** (mục 12 RULE), không phải mã quan điểm — Studio đang dùng làm mã quan điểm cho 2 TC.

### Phân bố

| Trục | Phân bố |
|---|---|
| `case_type` | Normal 9 · Abnormal 7 · Boundary 6 |
| `tc_group` | job 19 · ui 2 · data 1 |
| `exec_mode` | auto 22 |
| `env_tag` | local-only 21 · read-only 1 |
| `screen` | Job đồng bộ câu trả lời form lên Google Spreadsheet 19 · Màn trả lời form của LINE user 1 · Màn kết quả trả lời form (quản trị viên) 1 · Dữ liệu bản ghi trả lời form (form_answer_result) 1 |
| `requirement_keys` | REQ-001 (9) · REQ-002 (4) · REQ-003 (3) · REQ-004 (2) · REQ-005 (1) · REQ-006 (2) · REQ-007 (2) · REQ-008 (3) · REQ-009 (2) · REQ-010 (2) |

---

## TC List

| TC No. | Mã quan điểm liên kết | Loại case | Tiêu đề test case | Điều kiện tiền đề | Các bước thực hiện | Dữ liệu test/input | Kết quả mong đợi | Kết quả thực thi | Evidence thực tế | Môi trường test | Người thực hiện | Ngày thực hiện | Số ticket bug | Trạng thái đánh giá spec | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-RULETOOL027-01 | RULE-TOOL-027 | Normal | LINE user mở form rồi bấm gửi mà không nhập gì — dữ liệu vẫn lên spreadsheet | Form 1 trang gồm toàn câu hỏi KHÔNG bắt buộc, đã liên kết spreadsheet và đã đồng bộ ít nhất 1 lần nên sheet của trang đã có hàng tiêu đề và dữ liệu. Có LINE user đang là bạn của bot và có link trả lời form riêng của người đó. Job đồng bộ đang chạy. | 1. Mở spreadsheet của form và ghi lại số dòng hiện có trên sheet của trang<br>2. Mở link trả lời form của LINE user bằng trình duyệt<br>3. Không nhập và không chọn bất kỳ câu trả lời nào<br>4. Bấm nút gửi ở cuối form (nhãn mặc định 「回答する」, có thể đã được đổi trong cài đặt form)<br>5. Xác nhận màn hình chuyển sang trang hoàn tất trả lời<br>6. Kiểm tra bản ghi trả lời vừa tạo trong cơ sở dữ liệu (nội dung cột dữ liệu và trạng thái đồng bộ)<br>7. Chờ job đồng bộ chạy xong rồi mở lại spreadsheet, tìm dòng theo mã câu trả lời vừa tạo | Không nhập gì vào bất kỳ câu hỏi nào; chỉ bấm nút gửi | Màn hình chuyển sang trang hoàn tất trả lời. Trong cơ sở dữ liệu xuất hiện bản ghi trả lời mới với cột dữ liệu chỉ chứa dấu mốc trang (mã trang + tên trang) và trạng thái chờ đồng bộ. Sau khi job chạy: sheet tăng đúng 1 dòng gồm mã câu trả lời, thời gian trả lời, mã và tên LINE user, các cột câu hỏi để trống; bản ghi chuyển sang trạng thái đã đồng bộ. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17912 (NEW-21) · tc_group=ui · exec_mode=auto · env_tag=local-only · REQ-001, REQ-009 · spec: TICKET-40734, redmine journal 2026-09-12 (Nguyên nhân + Fix), FormAnswerService.php:978-990, HandleFormAnswerSyncGoogleSheetTask.java:1175-1237 · note: Kịch bản đúng như khách báo, phải kích hoạt từ trình duyệt thật — CẤM gọi thẳng endpoint lưu kết quả form rồi tự dựng cột dữ liệu. Form phải toàn câu hỏi không bắt buộc. Không có spreadsheet Google thật → trả skip vì lý do môi trường, KHÔNG dùng đọc DB/mã nguồn để kết luận đạt. |
| TC-RULE07-01 | RULE-07 | Normal | Màn kết quả trả lời form của quản trị viên hiển thị bản ghi không nhập nội dung và khớp với dòng trên spreadsheet | Đã có bản ghi trả lời form không nhập nội dung (tạo ở kịch bản trên) và job đã đồng bộ xong. Tài khoản quản trị viên đã đăng nhập và đã chọn đúng bot của form. | 1. Đăng nhập trang quản trị và chọn bot tương ứng<br>2. Vào mục quản lý form và mở màn kết quả trả lời của form đang kiểm tra<br>3. Tìm bản ghi trả lời của LINE user đã thao tác, mở chi tiết bản ghi<br>4. Mở spreadsheet của form và tìm dòng có cùng mã câu trả lời | Không nhập liệu, chỉ tra cứu theo LINE user và thời điểm trả lời | Màn kết quả trả lời form hiển thị bản ghi của LINE user đó với thời điểm trả lời đúng và phần câu trả lời để trống (không bị mất bản ghi khỏi danh sách). Mã câu trả lời trên màn hình khớp với mã ở dòng tương ứng trên spreadsheet, và các cột thời gian, tên LINE user hiển thị cùng giá trị ở cả hai nơi. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17913 (NEW-22) · tc_group=ui · exec_mode=auto · env_tag=local-only · REQ-009 · spec: TICKET-40734, sns-line/routes/web.php:1287 · note: Đối chiếu đủ 3 nơi (DB, màn quản trị, spreadsheet). Màn quản trị không nằm trong phạm vi sửa — TC dùng để phân biệt lỗi đồng bộ với lỗi mất dữ liệu ở bước trả lời form. ⚠️ RULE-07 là RULE, không phải mã quan điểm |
| TC-TOOLKNOW002-01 | TOOL-KNOW-002 | Normal | Sheet đã có dữ liệu — bản ghi trả lời chỉ có dấu mốc trang vẫn được ghi 1 dòng lên spreadsheet | Form 1 trang đã liên kết spreadsheet và đồng bộ được ít nhất 1 lần trước đó, nên sheet của trang đã có hàng tiêu đề chứa cột 「回答ID」 và ít nhất 1 dòng dữ liệu. Có một LINE user đang là bạn của bot, có tên hiển thị tiếng Nhật. Job đồng bộ đang chạy, token Google của bot còn hạn. | 1. Mở spreadsheet của form, ghi lại số dòng hiện có trên sheet của trang đang kiểm tra<br>2. Tạo mới 1 bản ghi trả lời form cho form này với LINE user đã chuẩn bị, phần dữ liệu chỉ gồm đúng một dấu mốc trang (mã trang + tên trang), trạng thái đồng bộ để ở mức chờ đồng bộ<br>3. Chờ job đồng bộ nhặt bản ghi và chạy xong lô của form này<br>4. Mở lại spreadsheet, tìm dòng theo mã câu trả lời vừa tạo<br>5. Đối chiếu trạng thái đồng bộ của bản ghi trong cơ sở dữ liệu và các dòng log của job | Cột dữ liệu của bản ghi trả lời = `[{"page":"<mã trang>","pageName":"<tên trang>"}]` (không có khoá type, không có khoá value) — đúng hình dạng của bản ghi mẫu 15373677 nêu trong báo cáo phân tích; trạng thái đồng bộ = chờ đồng bộ | Sheet của trang tăng đúng 1 dòng. Dòng mới có: cột 「回答ID」 = mã câu trả lời vừa tạo, cột thời gian = thời điểm tạo bản ghi theo định dạng thời gian đã cấu hình cho bot, cột mã LINE user và cột tên LINE user đúng với người trả lời; toàn bộ các cột câu hỏi để trống. Trong cơ sở dữ liệu, bản ghi chuyển sang trạng thái đã đồng bộ. Log của job có dòng bắt đầu ghi dữ liệu lên spreadsheet kèm mã câu trả lời — KHÔNG được chỉ có dòng bỏ qua kiểu dữ liệu rồi kết thúc lô mà không ghi dòng nào. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17892 (NEW-1) · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-001 · spec: TICKET-40734, redmine journal 2026-09-12 (Fix), attachment mục 2 Path A, HandleFormAnswerSyncGoogleSheetTask.java:1175-1237 · note: TC tái hiện đúng kịch bản báo cáo. Trước khi sửa, handleNonEmptySheet lọc needIgnoreType (type=null) trước khi xét bản ghi có thuộc trang hay không nên pageIn luôn false và hàm trả null (java:1182-1196, :1236). Bắt buộc chạy trên nhánh có bản sửa (m_202609_form_googlesheet_fix_empty_data_40734 / b84b175) |
| TC-FUNC001-01 | FUNC-001 | Normal | Sheet đã có dữ liệu — trả lời form đầy đủ vẫn lên đúng cột và không trùng dòng | Form 1 trang có nhiều loại câu hỏi (văn bản một dòng, văn bản nhiều dòng, chọn một, chọn nhiều, ngày) đã liên kết spreadsheet; sheet đã có hàng tiêu đề và ít nhất 1 dòng dữ liệu cũ. Job đồng bộ đang chạy. | 1. Ghi lại số dòng và thứ tự cột tiêu đề hiện có trên sheet<br>2. Tạo mới 1 bản ghi trả lời form có đáp án cho tất cả câu hỏi của trang, trạng thái đồng bộ để ở mức chờ đồng bộ<br>3. Chờ job chạy xong lô của form<br>4. Mở spreadsheet, tìm dòng theo mã câu trả lời vừa tạo và đối chiếu từng cột với đáp án đã nhập | Mỗi câu hỏi một đáp án phân biệt được với nhau, trong đó có 1 đáp án tiếng Nhật có dấu, 1 đáp án nhiều dòng và 1 câu chọn nhiều lựa chọn (2 lựa chọn) | Sheet tăng đúng 1 dòng. Giá trị của từng câu hỏi nằm đúng cột có tiêu đề tương ứng, không lệch sang cột bên cạnh; câu chọn nhiều hiển thị đủ các nhãn đã chọn; cột thời gian và tên LINE user đúng. Trên sheet chỉ tồn tại duy nhất một dòng mang mã câu trả lời này. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17893 (NEW-2) · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-002 · spec: TICKET-40734, redmine journal 2026-09-12 (vùng ảnh hưởng), HandleFormAnswerSyncGoogleSheetTask.java:288-308 · note: Regression bắt buộc vì bản sửa nằm ngay trong hàm dựng dòng cho nhánh sheet đã có dữ liệu. Đối chiếu dòng theo mã câu trả lời, không theo tên LINE user (tên có thể trùng) |
| TC-REGSHARED001-01 | REG-SHARED-001 | Normal | Sheet còn rỗng — lần đồng bộ đầu vẫn tạo đủ hàng tiêu đề cột và các câu trả lời cũ | Form đã có sẵn vài bản ghi trả lời cũ nhưng spreadsheet vừa được liên kết và sheet của trang còn hoàn toàn trống (chưa có hàng tiêu đề). Job đồng bộ đang chạy. | 1. Xác nhận sheet của trang đang trống hoàn toàn<br>2. Tạo mới 1 bản ghi trả lời form có đáp án đầy đủ để kích hoạt lần đồng bộ đầu<br>3. Chờ job chạy xong<br>4. Mở spreadsheet, kiểm tra hàng tiêu đề và các dòng dữ liệu được ghi | Form có sẵn 3 bản ghi trả lời cũ với các đáp án khác nhau; bản ghi mới có đáp án đầy đủ | Sheet có hàng tiêu đề đủ các cột thông tin đầu dòng và cột câu hỏi theo đúng thứ tự câu hỏi của trang; các bản ghi trả lời cũ được ghi lại đầy đủ, mỗi bản ghi một dòng, giá trị đúng cột. Hành vi giống hệt trước khi sửa. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17899 (NEW-8) · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-004 · spec: TICKET-40734, redmine journal 2026-09-12 (không đụng phần sheet rỗng), HandleFormAnswerSyncGoogleSheetTask.java:720-793 · note: Regression nhánh bootstrap. Dev khẳng định không đụng phần xử lý sheet rỗng nhưng nhánh này gọi chung hàm bị sửa ở bước ghi bản ghi hiện tại nên vẫn phải kiểm |
| TC-REGSHARED001-02 | REG-SHARED-001 | Normal | Sheet còn rỗng — bản ghi chỉ có dấu mốc trang vẫn được ghi 1 dòng như trước khi sửa | Form vừa liên kết spreadsheet, sheet của trang còn trống hoàn toàn. Trong dữ liệu form đã có sẵn 1 bản ghi trả lời chỉ chứa dấu mốc trang. Job đồng bộ đang chạy. | 1. Xác nhận sheet của trang đang trống hoàn toàn<br>2. Kích hoạt lần đồng bộ đầu (tạo 1 bản ghi trả lời mới hoặc đưa bản ghi hiện có về trạng thái chờ đồng bộ)<br>3. Chờ job chạy xong<br>4. Mở spreadsheet, tìm dòng theo mã của bản ghi chỉ có dấu mốc trang | Bản ghi cũ có cột dữ liệu = `[{"page":"<mã trang>","pageName":"<tên trang>"}]` | Bản ghi chỉ có dấu mốc trang vẫn xuất hiện 1 dòng trên sheet với đủ thông tin đầu dòng và các cột câu hỏi để trống — đúng như hành vi trước khi sửa, không bị bản sửa làm mất hay nhân đôi. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17900 (NEW-9) · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-004, REQ-001 · spec: TICKET-40734, attachment mục 2 Path B + mục 4, HandleFormAnswerSyncGoogleSheetTask.java:741-779 · note: Đây là hành vi 'đúng' mà khách từng thấy và là cơ sở để thống nhất hành vi cho nhánh sheet đã có dữ liệu. Nếu TC này fail nghĩa là bản sửa đã đụng nhầm nhánh bootstrap |
| TC-RULETOOL029-01 | RULE-TOOL-029 | Normal | Đường xử lý từng bản ghi sau lỗi — bản ghi chỉ có dấu mốc trang vẫn được ghi 1 dòng | Sheet của trang đã có tiêu đề và dữ liệu. Có sẵn một bản ghi ghi nhận lỗi Google gắn với bản ghi trả lời cần đồng bộ, để job phải chuyển bản ghi này sang đường xử lý từng bản ghi thay vì đồng bộ theo lô. Job đồng bộ đang chạy. | 1. Ghi lại số dòng hiện có trên sheet của trang<br>2. Chuẩn bị 1 bản ghi trả lời chỉ có dấu mốc trang, có gắn tham chiếu tới bản ghi lỗi Google trước đó, trạng thái đồng bộ để ở mức chờ đồng bộ<br>3. Chờ job chạy xong<br>4. Mở spreadsheet, tìm dòng theo mã câu trả lời; kiểm tra log xem bản ghi đã đi qua đường xử lý từng bản ghi | Cột dữ liệu của bản ghi = `[{"page":"<mã trang>","pageName":"<tên trang>"}]`; bản ghi có tham chiếu tới bản ghi lỗi Google (khác 0) | Sheet tăng đúng 1 dòng với đầy đủ thông tin đầu dòng và các cột câu hỏi để trống — kết quả giống hệt đường đồng bộ theo lô. Log cho thấy bản ghi được xử lý theo đường từng bản ghi. Không phát sinh dòng trùng. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17901 (NEW-10) · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-005 · spec: TICKET-40734, redmine journal 2026-09-12 (2 caller giữ nguyên), HandleFormAnswerSyncGoogleSheetTask.java:236-243 + :815 · note: Đường vào thứ hai của hàm được sửa (handle gọi handleNonEmptySheet tại dòng 815), kích hoạt qua nhánh khôi phục sau lỗi tại 236-243. Không dựng được bản ghi lỗi Google thì làm hỏng tạm thời một bước gọi Google để lô rơi vào xử lý dự phòng, ghi rõ cách đã dùng |
| TC-OBS001-01 | OBS-001 | Normal | Log của job ghi kèm mã câu trả lời để truy vết bản ghi bị bỏ qua | Sheet của trang đã có tiêu đề và dữ liệu. Job đồng bộ đang chạy và ghi log ở mức đủ chi tiết. | 1. Tạo mới 1 bản ghi trả lời chỉ có dấu mốc trang và 1 bản ghi có chứa phần tử thuộc loại bị bỏ qua<br>2. Chờ job chạy xong lô của form<br>3. Mở log của job, tìm các dòng liên quan tới form và tới hai bản ghi vừa tạo | Hai bản ghi như mô tả, thuộc cùng một form và cùng một trang | Mỗi dòng log liên quan tới việc bỏ qua phần tử hoặc bỏ qua bản ghi đều kèm mã câu trả lời tương ứng, đủ để lần ngược ra bản ghi trong cơ sở dữ liệu; dòng log bắt đầu ghi dữ liệu lên spreadsheet cũng kèm mã câu trả lời. Không còn dòng log chỉ ghi chung chung loại dữ liệu bị bỏ qua mà không cho biết là của bản ghi nào. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17908 (NEW-17) · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-007 · spec: TICKET-40734, redmine journal 2026-09-12 (thêm mã câu trả lời vào log), HandleFormAnswerSyncGoogleSheetTask.java:1187 + :1233 · note: Bằng chứng trong báo cáo lỗi là log chỉ có dòng bỏ qua kiểu dữ liệu null, không cho biết bản ghi nào nên rất khó điều tra. Dev nói đã bổ sung mã câu trả lời vào log nên phải kiểm tra thật |
| TC-RULE04-01 | RULE-04 | Normal | Bản ghi cũ sau khi được đưa về trạng thái chờ đồng bộ thì được ghi lại đúng và không trùng | Tiếp nối kịch bản bản ghi cũ bị mất: có bản ghi cũ chỉ có dấu mốc trang, đang ở trạng thái đã đồng bộ và chưa có dòng trên sheet. Sheet của trang đã có tiêu đề và dữ liệu. Job đồng bộ đang chạy trên nhánh đã có bản sửa. | 1. Ghi lại số dòng hiện có trên sheet và danh sách mã câu trả lời đang có<br>2. Đưa bản ghi cũ về trạng thái chờ đồng bộ<br>3. Chờ job chạy xong lô của form<br>4. Kiểm tra sheet: tìm dòng theo mã câu trả lời của bản ghi cũ và đếm số dòng mang mã đó<br>5. Kiểm tra các bản ghi khác của form vẫn giữ nguyên | Bản ghi cũ như trên; bổ sung 1 bản ghi cũ khác ĐÃ có dòng trên sheet để kiểm tra không bị ghi trùng khi cũng đưa về trạng thái chờ | Bản ghi cũ bị mất được ghi thành đúng 1 dòng mới với mã câu trả lời, thời gian trả lời gốc (không phải thời điểm chạy lại), mã và tên LINE user, các cột câu hỏi để trống. Bản ghi cũ đã có dòng trước đó không bị ghi thêm dòng thứ hai. Các dòng sẵn có trên sheet không bị thay đổi. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17910 (NEW-19) · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-008, REQ-001 · spec: TICKET-40734, attachment mục 3, HandleFormAnswerSyncGoogleSheetTask.java:288-308 · note: TC chứng minh cách khôi phục dữ liệu khách đã mất là khả thi và an toàn (nhờ cơ chế chống trùng theo mã câu trả lời). Cần chốt với leader/Dev việc có thực hiện khôi phục hàng loạt hay không trước khi đóng ticket. ⚠️ RULE-04 là RULE, không phải mã quan điểm |
| TC-TOOLAXIS001-01 | TOOL-AXIS-001 | Abnormal | Sheet đã có dữ liệu — bản ghi chỉ chứa các phần tử không phải câu hỏi vẫn được ghi 1 dòng để trống | Form 1 trang có chèn các phần tử trang trí không phải câu hỏi (đoạn mô tả, video, HTML tự do) xen giữa các câu hỏi; sheet đã có tiêu đề và dữ liệu cũ. Job đồng bộ đang chạy. | 1. Ghi lại số dòng hiện có trên sheet của trang<br>2. Tạo mới 1 bản ghi trả lời form mà phần dữ liệu chỉ gồm các phần tử thuộc loại bị job bỏ qua, tất cả đều mang đúng mã trang của trang đang kiểm tra<br>3. Chờ job chạy xong lô của form<br>4. Mở spreadsheet, tìm dòng theo mã câu trả lời vừa tạo và kiểm tra vị trí các cột metadata | Phần dữ liệu của bản ghi gồm các phần tử có loại nằm trong nhóm bị bỏ qua: đoạn mô tả (description_1, description_2), video, HTML tự do (custom_html), văn bản hiển thị (text_new) — tất cả kèm mã trang hợp lệ | Sheet tăng đúng 1 dòng gồm mã câu trả lời, thời gian, mã và tên LINE user, các cột câu hỏi để trống; các phần tử không phải câu hỏi không sinh thêm cột và không đẩy lệch các cột metadata. Bản ghi được đánh dấu đã đồng bộ. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17894 (NEW-3) · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-001 · spec: TICKET-40734, attachment mục 2, HandleFormAnswerSyncGoogleSheetTask.java:1239-1247 · note: Gộp nhiều loại phần tử bị bỏ qua vào một TC vì cùng một gốc lỗi (danh sách needIgnoreType tại java:1239-1247) — tách theo từng loại chỉ nhân bản TC mà không kiểm chứng thêm rủi ro |
| TC-TOOLNEGCTRL001-01 | TOOL-NEGCTRL-001 | Abnormal | Form nhiều trang — trả lời một trang thì sheet của các trang khác không phát sinh dòng thừa | Form nhiều trang (mỗi trang có sheet riêng theo tên trang) đã liên kết spreadsheet; tất cả sheet của các trang đều đã có tiêu đề và dữ liệu. Job đồng bộ đang chạy. | 1. Ghi lại số dòng hiện có trên sheet của TỪNG trang<br>2. Tạo mới 1 bản ghi trả lời chỉ chứa dấu mốc của trang thứ nhất (không có đáp án nào)<br>3. Chờ job chạy xong lô của form<br>4. Kiểm tra lần lượt sheet của tất cả các trang | Cột dữ liệu của bản ghi = `[{"page":"<mã trang 1>","pageName":"<tên trang 1>"}]` | Chỉ sheet của trang thứ nhất tăng đúng 1 dòng mang mã câu trả lời đó. Sheet của các trang còn lại giữ nguyên số dòng, không xuất hiện dòng nào mang cùng mã câu trả lời. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17902 (NEW-11) · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-003 · spec: TICKET-40734, redmine journal 2026-09-12 (vùng ảnh hưởng), HandleFormAnswerSyncGoogleSheetTask.java:254-308 · note: Đối chứng âm cho trục nhiều trang, kiểm chính rủi ro của bản sửa: nếu điều kiện thuộc trang bị nới quá tay thì bản ghi sẽ được ghi lên mọi sheet |
| TC-COMPATLEGACY001-01 | COMPAT-LEGACY-001 | Abnormal | Form nhiều trang — bản ghi dữ liệu cũ không kèm mã trang vẫn được ghi vào toàn bộ các sheet | Form nhiều trang đã liên kết spreadsheet, tất cả sheet của các trang đều đã có tiêu đề và dữ liệu. Job đồng bộ đang chạy. | 1. Ghi lại số dòng hiện có trên sheet của từng trang<br>2. Chuẩn bị 1 bản ghi trả lời theo định dạng dữ liệu cũ: các phần tử không có khoá mã trang<br>3. Đưa bản ghi về trạng thái chờ đồng bộ và chờ job chạy xong<br>4. Kiểm tra lần lượt sheet của tất cả các trang, tìm theo mã câu trả lời | Cột dữ liệu của bản ghi gồm các phần tử không có khoá page (định dạng dữ liệu đời cũ), trong đó có phần tử loại bị bỏ qua và phần tử là câu trả lời thật | Câu trả lời được sync vào toàn bộ các sheet | **Không đạt** | | LOCAL | thanhntp | 2026-09-15 | Studio bug #1068 — **rejected** | | Studio #17903 (NEW-12) · **version 5** · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-003 · spec: TICKET-40734, DataFormAnswerResult.java:73, HandleFormAnswerSyncGoogleSheetTask.java:1190 · note: **Rủi ro tác dụng phụ chưa được Dev nói tới** — hàm lấy mã trang trả về chính trang đang xét khi phần tử không có mã trang (DataFormAnswerResult.java:73), nên nếu điều kiện thuộc trang được xét độc lập với bộ lọc loại phần tử thì bản ghi kiểu cũ có thể 'thuộc' mọi trang. Nếu thấy dòng lặp trên nhiều sheet, báo Dev kèm mã câu trả lời; cần Dev xác nhận hành vi mong đợi cho dữ liệu cũ không kèm mã trang. ⚠️ **FAIL nhưng bug_tickets = []** |
| TC-JOB002-01 | JOB-002 | Abnormal | Lỗi tạm thời khi ghi lên Google — bản ghi không bị mất và lên đủ sau khi khôi phục | Sheet của trang đã có tiêu đề và dữ liệu. Có cách gây lỗi tạm thời cho thao tác gọi Google (ví dụ tạm thời làm token không hợp lệ hoặc ngắt kết nối mạng ra ngoài). Job đồng bộ đang chạy. | 1. Tạo mới vài bản ghi trả lời cho cùng form, trong đó có bản ghi chỉ có dấu mốc trang<br>2. Gây lỗi tạm thời cho thao tác gọi Google ngay trước khi job chạy lô này<br>3. Chờ job chạy và ghi nhận trạng thái các bản ghi cùng log<br>4. Khôi phục lại kết nối/token và chờ job chạy lại<br>5. Đối chiếu danh sách mã câu trả lời trên sheet với trong cơ sở dữ liệu | 3 bản ghi: 2 bản ghi có đáp án đầy đủ, 1 bản ghi chỉ có dấu mốc trang | Ở lần lỗi: không bản ghi nào bị đánh dấu đã đồng bộ sai sự thật — bản ghi hoặc quay lại trạng thái chờ đồng bộ, hoặc được ghi nhận lỗi để chờ thử lại; log ghi rõ lý do và mã câu trả lời. Sau khi khôi phục: cả 3 bản ghi đều có đúng 1 dòng trên sheet, không thiếu và không trùng. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17906 (NEW-15) · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-010 · spec: TICKET-40734, HandleFormAnswerSyncGoogleSheetTask.java:316-327 + :346-351, RetryErrorGoogleSheetTask.java:42-60 · note: Nhóm thử lại và thất bại của quan điểm job. Không kiểm backoff theo thời gian vì job này không có cơ chế backoff theo cấu hình; chỉ kiểm hệ quả quan sát được là không mất và không trùng bản ghi. ⚠️ checklist-lme.md chỉ có JOB-001 |
| TC-OUTTRUTH001-01 | OUT-TRUTH-001 | Abnormal | Trạng thái đã đồng bộ và log phải khớp với số dòng thực sự ghi lên sheet | Sheet của trang đã có tiêu đề và dữ liệu. Job đồng bộ đang chạy. Chuẩn bị được hai bản ghi: một bản ghi chỉ có dấu mốc trang và một bản ghi có phần dữ liệu rỗng hoàn toàn. | 1. Ghi lại số dòng hiện có trên sheet<br>2. Tạo hai bản ghi trả lời nói trên cho cùng form, cùng để ở trạng thái chờ đồng bộ<br>3. Chờ job chạy xong lô của form<br>4. Đếm số dòng mới trên sheet và ghi lại mã câu trả lời của từng dòng<br>5. Đối chiếu với trạng thái đồng bộ của hai bản ghi trong cơ sở dữ liệu và với dòng log tổng kết lô của job | Bản ghi A: cột dữ liệu = `[{"page":"<mã trang>","pageName":"<tên trang>"}]`. Bản ghi B: cột dữ liệu = `[]` | Bản ghi A có đúng 1 dòng trên sheet và ở trạng thái đã đồng bộ — trạng thái khớp thực tế. Với bản ghi B (không ghi được dòng nào): số bản ghi mà log tổng kết báo đã đồng bộ phải khớp số dòng thực sự được ghi, và bản ghi không ghi được dòng không được báo là đã đồng bộ. Nếu quan sát thấy bản ghi B vẫn bị đánh dấu đã đồng bộ và log vẫn báo đã đồng bộ đủ số bản ghi trong khi không có dòng nào cho nó, ghi nhận Không đạt cho điểm này và dẫn chiếu đúng phần chưa được sửa. | **Không đạt** | | LOCAL | thanhntp | 2026-09-15 | **41010** | | Studio #17907 (NEW-16) · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-007 · spec: TICKET-40734, attachment mục 3 + mục 5 phần bổ sung, HandleFormAnswerSyncGoogleSheetTask.java:330-334 · note: TC chạm vào phần báo cáo phân tích đề xuất nhưng **Dev CHƯA sửa** — trạng thái đồng bộ vẫn được set vô điều kiện cho cả lô (java:330-334). Không đạt ở đây **KHÔNG phải lỗi của bản sửa lần này** mà là khoảng trống đã biết — leader quyết định tách ticket hay chấp nhận. Đây chính là cơ chế khiến bản ghi bị bỏ không bao giờ được thử lại |
| TC-TOOLOLDREC001-01 | TOOL-OLDREC-001 | Abnormal | Bản ghi cũ đã bị đánh dấu đã đồng bộ trước khi sửa không tự động được ghi lại | Sheet của trang đã có tiêu đề và dữ liệu. Trong cơ sở dữ liệu có bản ghi trả lời chỉ có dấu mốc trang, đang ở trạng thái đã đồng bộ, và không có dòng tương ứng trên sheet (mô phỏng bản ghi đã bị mất trước khi sửa). Job đồng bộ đang chạy trên nhánh đã có bản sửa. | 1. Xác nhận trên sheet không có dòng nào mang mã câu trả lời của bản ghi cũ đó<br>2. Để job chạy qua vài vòng đối với form này (có thể tạo thêm bản ghi mới để job xử lý form)<br>3. Kiểm tra lại sheet và trạng thái của bản ghi cũ | Bản ghi cũ: cột dữ liệu = `[{"page":"<mã trang>","pageName":"<tên trang>"}]`, trạng thái đồng bộ = đã đồng bộ, ngày tạo trước ngày triển khai bản sửa | Bản ghi cũ vẫn không xuất hiện trên sheet: bản sửa chỉ có tác dụng với bản ghi còn ở trạng thái chờ đồng bộ. Đây là giới hạn đã biết, cần một bước khôi phục riêng chứ không phải hệ thống tự chữa. Bản ghi mới tạo trong cùng lần chạy vẫn phải lên sheet bình thường. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17909 (NEW-18) · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-008 · spec: TICKET-40734, attachment mục 3, HandleFormAnswerSyncGoogleSheetTask.java:166-175 · note: Trục thời điểm bản ghi, giá trị 'bản ghi cũ đã bị đánh dấu đồng bộ'. Kết quả này KHÔNG phải lỗi mới mà là hệ quả của việc điểm chưa sửa. Cặp đôi với TC khôi phục sau khi đưa bản ghi về trạng thái chờ |
| TC-TOOLNEGCTRL001-02 | TOOL-NEGCTRL-001 | Boundary | Sheet đã có dữ liệu — bản ghi trả lời không có dữ liệu nào thì không phát sinh dòng | Form 1 trang đã liên kết spreadsheet, sheet đã có tiêu đề và dữ liệu cũ. Job đồng bộ đang chạy. | 1. Ghi lại số dòng hiện có trên sheet của trang<br>2. Tạo mới 1 bản ghi trả lời form có phần dữ liệu rỗng hoàn toàn, trạng thái đồng bộ để ở mức chờ đồng bộ<br>3. Chờ job chạy xong lô của form<br>4. Kiểm tra số dòng trên sheet và trạng thái của bản ghi trong cơ sở dữ liệu | Cột dữ liệu của bản ghi = `[]` (mảng rỗng); lần chạy thứ hai dùng giá trị rỗng/null để kiểm tra cùng ranh giới | Sheet không tăng dòng nào, không xuất hiện dòng chỉ có mã câu trả lời. Hành vi này giữ đúng như nhánh sheet còn rỗng (bản ghi không thuộc trang nào thì không ghi), nghĩa là bản sửa KHÔNG được nới rộng tới mức ghi dòng cho bản ghi không hề có thông tin trang. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17895 (NEW-4) · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-001, REQ-003 · spec: TICKET-40734, redmine journal 2026-09-12 (điều kiện thuộc trang đang ghi), HandleFormAnswerSyncGoogleSheetTask.java:1197-1199 · note: Đối chứng âm cho ranh giới của bản sửa. Căn cứ: Dev chốt điều kiện ghi dòng là 'câu trả lời thuộc trang đang ghi'; nhánh sheet rỗng cũng không ghi khi không gom được trang nào (java:741-753). Phần trạng thái đồng bộ của bản ghi này xem TC-OUTTRUTH001-01 |
| TC-FUNC004-01 | FUNC-004 | Boundary | Sheet đã có dữ liệu — bản ghi trả lời bỏ trống một phần câu hỏi vẫn ghi đúng cột | Form 1 trang có ít nhất 3 câu hỏi không bắt buộc, đã liên kết spreadsheet; sheet đã có tiêu đề và dữ liệu cũ. Job đồng bộ đang chạy. | 1. Ghi lại thứ tự cột tiêu đề trên sheet<br>2. Tạo mới 1 bản ghi trả lời form chỉ có đáp án cho câu hỏi ở giữa, hai câu còn lại để trống<br>3. Chờ job chạy xong lô của form<br>4. Mở spreadsheet, tìm dòng theo mã câu trả lời và kiểm tra giá trị nằm ở cột nào | Câu hỏi 1 để trống, câu hỏi 2 nhập giá trị dễ nhận biết, câu hỏi 3 để trống | Sheet tăng đúng 1 dòng; giá trị của câu hỏi 2 nằm đúng cột có tiêu đề của câu hỏi 2, hai cột còn lại để trống. Không có hiện tượng giá trị bị dồn lên cột đầu tiên của vùng câu hỏi. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17896 (NEW-5) · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-002 · spec: TICKET-40734, redmine journal 2026-09-12 (không lệch cột), HandleFormAnswerSyncGoogleSheetTask.java:1183-1196 · note: Ranh giới giữa 'không có đáp án nào' và 'có đủ đáp án'; cũng là chỗ dễ lệch cột nhất vì nhánh sheet đã có dữ liệu ghép giá trị theo thứ tự phần tử trong cột dữ liệu, khác với nhánh sheet rỗng ghép theo tiêu đề cột |
| TC-TOOLAXIS001-02 | TOOL-AXIS-001 | Boundary | Sheet có cột mã câu trả lời ở cột đầu tiên — dòng ghi thêm ghép đúng 4 ô thông tin đầu dòng | Sheet của trang có hàng tiêu đề mà ô đầu tiên là 「回答ID」 (kiểu bố cục cột thứ nhất) và đã có ít nhất 1 dòng dữ liệu. Job đồng bộ đang chạy. | 1. Xác nhận ô đầu tiên của hàng tiêu đề trên sheet là 「回答ID」<br>2. Tạo mới 1 bản ghi trả lời form chỉ có dấu mốc trang cho trang đang kiểm tra<br>3. Chờ job chạy xong lô của form<br>4. Mở spreadsheet và kiểm tra vị trí từng ô của dòng mới | Cột dữ liệu của bản ghi = `[{"page":"<mã trang>","pageName":"<tên trang>"}]` | Dòng mới bắt đầu bằng đúng 4 ô: mã câu trả lời, thời gian trả lời, mã LINE user, tên LINE user; từ ô thứ 5 trở đi là các cột câu hỏi và đều để trống. Không có ô thừa đẩy dữ liệu lệch sang phải. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17897 (NEW-6) · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-006, REQ-001 · spec: TICKET-40734, HandleFormAnswerSyncGoogleSheetTask.java:268-276 + :1211-1212 · note: Trục kiểu bố cục cột, giá trị thứ nhất (vị trí cột mã câu trả lời = cột A). Sinh đôi với TC kiểu bố cục cột thứ hai; tách riêng vì hai giá trị của cùng một trục |
| TC-TOOLAXIS001-03 | TOOL-AXIS-001 | Boundary | Sheet có cột mã câu trả lời ở cột thứ hai — dòng ghi thêm ghép đúng 6 ô thông tin đầu dòng | Sheet của trang có hàng tiêu đề mà ô thứ hai là 「回答ID」 (kiểu bố cục cột thứ hai, có thêm cột mã LINE và cột tên hiển thị) và đã có ít nhất 1 dòng dữ liệu. Job đồng bộ đang chạy. | 1. Xác nhận ô thứ hai của hàng tiêu đề trên sheet là 「回答ID」<br>2. Tạo mới 1 bản ghi trả lời form chỉ có dấu mốc trang cho trang đang kiểm tra, với LINE user có đặt tên hiển thị riêng<br>3. Chờ job chạy xong lô của form<br>4. Mở spreadsheet và kiểm tra vị trí từng ô của dòng mới | Cột dữ liệu của bản ghi = `[{"page":"<mã trang>","pageName":"<tên trang>"}]`; LINE user có tên hiển thị khác tên LINE | Dòng mới bắt đầu bằng đúng 6 ô: mã LINE, mã câu trả lời, thời gian trả lời, mã LINE user, tên LINE user, tên hiển thị; từ ô thứ 7 trở đi là các cột câu hỏi và đều để trống. Nếu LINE user không có tên hiển thị thì ô tên hiển thị để trống chứ không bị bỏ ô làm lệch cột. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17898 (NEW-7) · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-006, REQ-001 · spec: TICKET-40734, HandleFormAnswerSyncGoogleSheetTask.java:273-275 + :1213-1216 · note: Trục kiểu bố cục cột, giá trị thứ hai. Đây là kiểu header hay gặp với form nhiều trang; sai ở đây làm lệch toàn bộ vùng câu hỏi |
| TC-CONC001-01 | CONC-001 | Boundary | Chạy lại job với cùng bản ghi — không sinh dòng trùng trên sheet | Sheet của trang đã có tiêu đề và dữ liệu. Job đồng bộ đang chạy. | 1. Tạo mới 1 bản ghi trả lời chỉ có dấu mốc trang và chờ job ghi xong dòng đầu tiên<br>2. Đưa chính bản ghi đó về trạng thái chờ đồng bộ một lần nữa<br>3. Chờ job chạy lại lần thứ hai<br>4. Đếm số dòng trên sheet mang mã câu trả lời đó | Cột dữ liệu của bản ghi = `[{"page":"<mã trang>","pageName":"<tên trang>"}]` | Trên sheet chỉ có đúng 1 dòng mang mã câu trả lời đó sau cả hai lần chạy; lần chạy thứ hai không thêm dòng. Bản ghi vẫn ở trạng thái đã đồng bộ. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17904 (NEW-13) · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-002, REQ-010 · spec: TICKET-40734, redmine journal 2026-09-12 (không ghi trùng dòng), HandleFormAnswerSyncGoogleSheetTask.java:288-302 · note: Sau khi sửa, bản ghi loại này mới thật sự có mặt trên sheet nên cơ chế chống trùng theo mã câu trả lời mới được kích hoạt cho nó — trước khi sửa không có dòng nào để so. Cũng là bước chuẩn bị cho kịch bản khôi phục dữ liệu cũ |
| TC-INTGSHEET001-01 | INTG-SHEET-001 | Boundary | Nhiều bản ghi trả lời cùng form trong một lô — mỗi bản ghi đúng một dòng, không thiếu bản ghi nào | Sheet của trang đã có tiêu đề và dữ liệu. Job đồng bộ đang chạy. | 1. Ghi lại số dòng hiện có trên sheet<br>2. Tạo liên tiếp nhiều bản ghi trả lời cho cùng form trong thời gian ngắn, trộn lẫn bản ghi có đáp án đầy đủ và bản ghi chỉ có dấu mốc trang<br>3. Chờ job chạy xong lô của form<br>4. Đối chiếu danh sách mã câu trả lời trong cơ sở dữ liệu với danh sách mã câu trả lời trên sheet | 10 bản ghi: 5 bản ghi có đáp án đầy đủ, 5 bản ghi chỉ có dấu mốc trang, tạo cách nhau vài giây | Sheet tăng đúng 10 dòng; mỗi mã câu trả lời xuất hiện đúng 1 lần; các bản ghi có đáp án hiển thị đúng cột, các bản ghi chỉ có dấu mốc trang hiển thị dòng để trống phần câu hỏi. Tất cả 10 bản ghi ở trạng thái đã đồng bộ. | Đạt | | LOCAL | thanhntp | 2026-09-15 | | | Studio #17905 (NEW-14) · tc_group=job · exec_mode=auto · env_tag=local-only · REQ-001, REQ-002 · spec: TICKET-40734, HandleFormAnswerSyncGoogleSheetTask.java:298-327 · note: Kiểm phần ghi gộp theo lô của job (gom nhiều bản ghi rồi append theo từng khối). Trước khi sửa, đúng 5 bản ghi chỉ có dấu mốc trang sẽ biến mất trong khi log vẫn báo đã đồng bộ đủ 10 |
| TC-DATADB001-01 | DATA-DB-001 | Abnormal | Rà soát phạm vi bản ghi trả lời đã bị mất trên spreadsheet trước khi đóng ticket | Có quyền truy vấn cơ sở dữ liệu và đọc được spreadsheet của các form đang liên kết. Không cần chạy job. | 1. Liệt kê các form đang liên kết spreadsheet<br>2. Với mỗi form, truy vấn các bản ghi trả lời ở trạng thái đã đồng bộ, chưa bị xoá, mà phần dữ liệu chỉ chứa dấu mốc trang hoặc chỉ chứa các phần tử thuộc loại bị bỏ qua<br>3. Đọc cột mã câu trả lời trên từng sheet tương ứng của spreadsheet<br>4. Đối chiếu hai danh sách theo mã câu trả lời và lập danh sách bản ghi có trong cơ sở dữ liệu nhưng không có trên sheet | Phạm vi rà soát: toàn bộ bản ghi tạo từ ngày bắt đầu dùng cơ chế đồng bộ theo lô tới ngày triển khai bản sửa; đối chiếu theo mã câu trả lời, không theo tên LINE user | Lập được danh sách đầy đủ các bản ghi bị mất kèm mã form, mã câu trả lời và thời điểm trả lời; trong đó có bản ghi mẫu nêu trong báo cáo (form 228620, bản ghi tạo ngày 2026-08-19 10:47). Danh sách này là đầu vào cho bước khôi phục và cần được đính kèm vào ticket trước khi đóng. | Chưa test | | LOCAL *(dự kiến: Tất cả)* | thanhntp | 2026-09-15 | | | Studio #17911 (NEW-20) · **`last_exec.status = skip`** · tc_group=data · exec_mode=auto · env_tag=read-only · REQ-008 · spec: TICKET-40734, attachment mục 3 + mục 4, HandleFormAnswerSyncGoogleSheetTask.java:330-334 · note: TC thuần dữ liệu, chỉ truy vấn và đối chiếu. **Bắt buộc theo RULE-04**: bug gây mất dữ liệu thì phải xác định phạm vi đã mất trước khi đóng. Khách báo hiện tượng lặp ở nhiều form nên phạm vi có thể lớn hơn một form |

---

## Member tự check

`<member điền sau khi review>`

<!-- Source: fetched từ MCP LME TEST STUDIO — task_id=303 (ticket 40734, round 1), testcase_list 22/22 TC (nextCursor=null), lúc 2026-09-15.
     Redmine #40734 KHÔNG có section "Link TCs" → không có Google Sheet nguồn.
     TCs là read-only, KHÔNG sửa title/precondition/steps/expected ở file này. Sửa trên Studio (testcase_update) rồi fetch lại. -->
