# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | (1) MCP LME TEST STUDIO — task **#303** (ticket 40734, round 1, `aiResult=fail`, `reviewState=leader`) |
| Tổng số TC review | **22** |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `7/13 vùng ảnh hưởng đủ TC · 3 GAP · 3 RISK`

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | `BUG` — root cause "user bấm gửi không nhập gì → bản ghi marker-only" | `dev-impact` | `TC-RULETOOL027-01` (Studio #17912), `TC-TOOLKNOW002-01` (#17892) | **RISK — chưa tái hiện được đúng ca khách.** `actual` của run 1481 ghi rõ: với FE V3, form **có câu hỏi** mà user bấm gửi không nhập gì **KHÔNG sinh** marker-only, mà sinh item kèm `type` + `value` rỗng (`form_render_v3.js:120-183`, đối chứng form 125180). Runner phải đổi sang **trang chỉ gồm phần tử hiển thị** mới dựng được hình dạng bug. ⇒ Chưa có TC nào chứng minh bản ghi của khách (form 228620, 2026-08-19 10:47) thuộc đúng hình dạng mà bản fix xử lý | `[BLOCKER]` |
| G2 | `D — form_answer_result.status_sync_sheet` | `dev-impact` | `TC-OUTTRUTH001-01` (#17907, **fail**) | **RISK — mục 4.2 của Dev ghi "Không có" data bị update, nhưng job có UPDATE trường này.** #17907 chứng minh trạng thái bị set `1` cho cả bản ghi không sinh dòng nào (`java:330-334`). Dev kê thiếu D-impact ⇒ toàn bộ nhánh recovery bị bỏ ra ngoài phạm vi fix | `[BLOCKER]` |
| G3 | `D — phạm vi dữ liệu đã mất trên production (RULE-04)` | `diff code` | `TC-DATADB001-01` (#17911) — **`skip`** | **RISK — TC duy nhất cover RULE-04 nhưng không chạy được.** `actual`: DB test không có form 228620, không có bản ghi mốc 2026-08-19 10:47, 0 spreadsheet khách đọc được ⇒ chưa biết bao nhiêu bản ghi / bao nhiêu form đã mất dữ liệu. Không được đóng ticket khi chưa có con số này | `[BLOCKER]` |
| G4 | **Xóa dòng trên sheet** (luồng DELETE dùng chung `handleNonEmptySheet`) | `diff code` | `không có` | **GAP — 0 TC cover.** Studio `dev_impact` ghi: *"Không đụng logic xóa dòng (DELETE) và nhánh khởi tạo sheet rỗng — **nhưng vì `handleNonEmptySheet` dùng chung, vẫn cần smoke lại 2 vùng này**"*. Nhánh sheet rỗng đã có TC (#17899/#17900); nhánh **xóa dòng thì không** | `[BLOCKER]` |
| G5 | `T3 — form nhiều trang` (bản ghi data đời cũ không kèm `page`) | `dev-impact` | `TC-COMPATLEGACY001-01` (#17903, **fail** → bug #1068 **rejected**) | **GAP — TC bị sửa expected để khớp actual thay vì chốt hành vi đúng.** Expected version 5 nay ghi *"sync vào toàn bộ các sheet"*; bug bị reject với lý do *"ghi vào cả 3 sheet là đúng"* nhưng **không dẫn được spec nào** xác nhận. Phạm vi ghi trong chính bug: **91 form nhiều trang đang liên kết spreadsheet**. Chưa chốt spec ⇒ coi như chưa có coverage hợp lệ cho `T3` ở nhánh legacy | `[BLOCKER]` |
| G6 | Edge state của sheet đích (ô cột A trống giữa vùng data · sheet hàng nghìn dòng · header bị sửa) | `diff code` | `không có` | **GAP — 0 TC cover.** Bản fix **làm tăng số dòng ghi thêm** vào sheet đã có dữ liệu, đúng vùng mà kho TCs đã chốt quy tắc (MT-16: *"tuyệt đối không ghi đè"*). Đây là AP-3 — regression chỉ chạy trên sheet "sạch" | `[MAJOR]` |

> **Input thiếu**: Studio `spec_delta.diffAvailable = false`, `branch = null`, `mode = redmine-only` — **không đọc được diff thật**. Chiều (b) chỉ suy được từ `dev_impact` Studio + mô tả cách fix ở file 03. Redmine có ghi commit `b84b175` + branch `m_202609_form_googlesheet_fix_empty_data_40734` nhưng chưa được nạp vào Studio ⇒ không xác thực được **bộ build nào đã chạy 22 TC** (run 1480 trước đó skip toàn bộ vì `SOURCE_BRANCH_MISSING`).

**4 câu hỏi adversarial (BƯỚC 2 chiều b)** — kết quả:

| # | Hỏi | Trả lời |
|---|---|---|
| 1 | Mỗi điểm sửa có ≥ 1 TC đi qua? | `handleNonEmptySheet` ✅ (12 TC) · thêm mã câu trả lời vào log ✅ (#17908). **Không verify được đủ** vì thiếu diff → xem *Input thiếu* |
| 2 | Điều kiện mới có TC cả 2 nhánh? | ✅ true → #17892 (ghi dòng) · false → #17895 (`data=[]` không ghi dòng). Fix **không** phải generic catch → không áp AP-1 |
| 3 | Hàm dùng chung — Dev kê đủ nơi bị ảnh hưởng và TC test từng nơi? | Dev kê **2 caller** (batch + retry) → #17905, #17901 ✅. Studio `dev_impact` kê thêm **2 vùng** (sheet rỗng ✅ #17899/#17900, **xóa dòng ❌**) → **G4** |
| 4 | Hành vi thay đổi (nới ra) có TC cả hành vi MỚI và chỗ cũ không hỏng? | ✅ mới → #17892 · cũ không hỏng → #17893, #17895, #17896, #17899, #17900 |

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `12 quan điểm Trigger khớp task · 10 chưa cover đủ`

> ⚠️ **11/22 TC mang mã quan điểm KHÔNG có trong `framework/checklist-lme.md`** (`RULE-TOOL-027`, `RULE-TOOL-029`, `TOOL-KNOW-002`, `TOOL-AXIS-001` ×3, `TOOL-NEGCTRL-001` ×2, `TOOL-OLDREC-001`, `OBS-001`, `JOB-002`) → theo BƯỚC 0.6 #5 **không tính là cover**. Thêm 2 TC dùng `RULE-04` / `RULE-07` làm mã quan điểm — đó là **RULE**, không phải mã quan điểm.

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `JOB-001` | Cao | **GAP — 0 TC mang mã hợp lệ.** Bộ TC chỉ có `JOB-002` (mã lạ). Job sync có batch 500 record, retry backoff `1p→5p→10p→30p→60p`, reset `PROCESSING` khi restart app, lock theo `form_id`, `MAX_SYNC_GOOGLE_SHEET_THREAD` mặc định 5 (kho MT-22) — **không TC nào chạm các cơ chế này với bản ghi marker-only** | `[BLOCKER]` |
| Q2 | `OUT-EXPORT-001` | Trung bình → **BẮT BUỘC (nâng Cao)** — trigger *"ghi Google Spreadsheet"* | **GAP — 0 TC cover.** Không có TC đối chiếu **nội dung** ghi ra spreadsheet với nguồn (màn kết quả + export CSV). Kho có mâu thuẫn chưa đóng về item 規約 (MT-07) | `[BLOCKER]` |
| Q3 | `DATA-DB-001` | Cao | **RISK — TC duy nhất (#17911) `skip`**, và **thiếu TC kiểm `WHERE` scope**: job sửa `status_sync_sheet`, chạy đa luồng nhiều form / nhiều bot, nhưng không TC nào chứng minh job **không đụng** form / bot khác (kho `TC-FORM-402`) | `[BLOCKER]` |
| Q4 | `REG-RUN-001` | Cao | **GAP — 0 TC cover.** Release này deploy trong lúc job đang chạy dở và đang có bản ghi ở trạng thái chờ / PROCESSING. Không TC nào kiểm bản ghi đang xử lý lúc restart (kho `TC-FORM-398`) | `[BLOCKER]` |
| Q5 | `ENV-003` | Cao | **GAP — 0/22 TC chạy production.** `envAuto`: local 22/22 (2 run), dev/staging/prd **0 run**. Task chạm **job nền + tích hợp Google thật + mất dữ liệu production** → RULE-08 cấm kết luận từ local. Chính #17911 đã `skip` vì *"thiếu đúng một thứ: dữ liệu production"* | `[MAJOR]` |
| Q6 | `COMPAT-LEGACY-001` | Cao | **RISK — chỉ cover 1 nhánh legacy** (data không kèm `page`, và nhánh này đang conflict → G5). **Không** cover form đời cũ `using_old_version = 1` (kho `TC-FORM-401`) → RULE-09 | `[MAJOR]` |
| Q7 | `CONC-001` | Cao | **RISK — có #17904 (chạy lại job, idempotency) nhưng thiếu đồng thời thật**: nhiều LINE user submit cùng lúc trên cùng form và trên nhiều form song song với `MAX_SYNC_GOOGLE_SHEET_THREAD = 5` (kho `TC-FORM-397`) | `[MAJOR]` |
| Q8 | `INTG-SHEET-001` | Cao | **RISK — chỉ 1 TC (#17905, Boundary).** Thiếu `Normal` + `Abnormal` mang mã hợp lệ → RULE-01 | `[MAJOR]` |
| Q9 | `PERF-LARGE-001` | Trung bình → **Cao** (trigger *"sync"*) | **GAP — 0 TC cover.** Sheet đã có hàng nghìn dòng → vị trí append của dòng marker-only (kho `TC-FORM-404`) | `[MAJOR]` |
| Q10 | `REG-SPEC-001` | Cao | **GAP — 0 TC cover.** `TC-COMPATLEGACY001-01` bị **đổi expected sang version 5 SAU khi đã chạy** và bug bị reject → đúng trigger "thay đổi spec/requirement sau khi đã có TC", phải có TC chạy lại theo hành vi đã chốt. Hiện chưa chốt → xem §6 | `[MAJOR]` |

*(Đã cover đủ, không ghi dòng: `OUT-TRUTH-001` — có #17907, fail đã raise #41010 · `FUNC-001` / `FUNC-004` — #17893 / #17896.)*

---

## 3. TC trùng lặp nội dung

Đã rà **22 TC** — phát hiện **1 nhóm** trùng.

| Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| DUP-1 | `TC-OUTTRUTH001-01` (#17907) | `TC-TOOLNEGCTRL001-02` (#17895) → **GỘP, không xóa** | `DUP-SUBSET` | Loại case khác nhau (Boundary vs Abnormal) nhưng **cùng đối tượng + thao tác** (bản ghi `data = []` trên sheet đã có dữ liệu) · **cùng tiền đề** · expected phần "không sinh dòng nào" **tương đương** — #17907 đã bao trùm và kiểm thêm trạng thái sync + log | `[MINOR]` |

- **Gate đã chạy**: giả định xóa #17895 → mất phần riêng *"lần chạy thứ hai dùng giá trị rỗng/null"* (#17907 chỉ có `[]`) ⇒ **đổi từ XÓA sang GỘP**: giữ #17907, bổ sung nhánh `null` vào steps của nó.
- `DUP-INFLATE` che GAP: **không có**.
- `DUP-CONFLICT`: **không có theo đúng 4 yếu tố** — #17902 (`chỉ sheet trang tương ứng`) vs #17903 (`toàn bộ các sheet`) mâu thuẫn **về nguyên tắc** nhưng **khác tiền đề** (có / không có khoá `page`), nên không xếp DUP-CONFLICT. Vẫn phải chốt hành vi đúng → đã đưa vào **§6**.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | Toàn bộ 22 TC | **Không xác thực được bản build đã test.** Studio `branch = null`, `diffAvailable = false`, `mode = redmine-only`; run 1480 skip toàn bộ 22 TC vì `SOURCE_BRANCH_MISSING`. Run 1481 pass nhưng không truy được là chạy trên commit nào | Gắn branch `m_202609_form_googlesheet_fix_empty_data_40734` / commit `b84b175` vào task Studio rồi **chạy lại**; chỉ nhận kết quả khi Studio ghi nhận đúng branch |
| I2 | `[BLOCKER]` | `TC-COMPATLEGACY001-01` (#17903) · bug #1068 | **Bug bị reject và TC bị sửa expected để khớp actual** — AP kinh điển "đổi expected theo hiện trạng". Lý do reject *"ghi vào cả 3 sheet là đúng"* **không dẫn được spec** nào; runner chỉ chứng minh *"hành vi có sẵn từ trước"* (`git show b84b175d^`), mà **cũ ≠ đúng**. Ảnh hưởng **91 form nhiều trang** | Dừng việc coi TC này là pass. Yêu cầu PM/Dev chốt hành vi đúng bằng văn bản (§6 #1), sau đó mới sửa expected và chạy lại |
| I3 | `[BLOCKER]` | `TC-RULETOOL027-01` (#17912) — **AP-2** | **Symptom-only KH report.** Khách chỉ mô tả hiện tượng ("dữ liệu không phản ánh vào spreadsheet"), không có log / error. Dev đoán **1 root cause** (marker-only). Chính `actual` của TC chứng minh kịch bản khách mô tả **không sinh marker-only** ⇒ có thể còn root cause khác cho cùng triệu chứng | Hỏi Dev: bản ghi thật của khách (form 228620, 2026-08-19 10:47) có đúng hình dạng marker-only không? Nếu không → fix chưa chạm ca của khách. TC cover ≥ 2 root cause hợp lý (xem §5 `TC-INTGSHEET001-02/03`) |
| I4 | `[MAJOR]` | 22/22 TC — RULE-08 / ENV-003 | **0 TC chạy production/staging.** Task chạm job nền + Google Spreadsheet thật + mất dữ liệu production, nhưng mọi run đều `env = local`, `env_tag = local-only` | Chạy lại bộ TC lõi trên môi trường có spreadsheet Google thật (§5 `TC-ENV003-01`) |
| I5 | `[MAJOR]` | 22/22 TC — RULE-02 | **Cột `Evidence thực tế` rỗng toàn bộ** trong `04-tc-list.md` (`testcase_list` không trả về). Evidence thật có tồn tại trong `task_get_report` (screenshot / log / sheet json) nhưng **không gắn vào TC** → người đọc file 04 không kiểm chứng được kết quả | Khi sync kết quả về, đính `artifacts` path vào cột Evidence của từng TC |
| I6 | `[MAJOR]` | Studio #17907, #17908, #17911 | **3 TC có manual result `blocked`** trong khi auto báo pass/fail. Trạng thái manual và auto không khớp → không rõ kết luận cuối là gì | Thống nhất 1 kết luận cho mỗi TC trước khi trình Leader |
| I7 | `[MAJOR]` | `03-dev-impact.md` | **Checkbox "Tester verify auto-fill chính xác" chưa tick** (file auto-fill `2026-09-15 by /new-task`) — F/D/T có thể thiếu hoặc map sai. Thực tế **đã phát hiện sai**: mục 4.2 ghi "Không có" data update nhưng job có UPDATE `status_sync_sheet` (xem G2) | Tester đọc lại Journal #136168, bổ sung `D1 = form_answer_result.status_sync_sheet (UPDATE)` rồi tick checkbox |
| I8 | `[MAJOR]` | Mục 4.3 file 03 — **AP-3** | **Happy-path-only regression.** T1–T4 đều có TC nhưng precondition toàn "sheet sạch". Không TC nào chạy regression với sheet ở **edge state** (ô cột A trống giữa vùng data, hàng nghìn dòng, header đã bị sửa) — đúng vùng bản fix ghi thêm dòng vào | Bổ sung TC edge state (§5 `TC-OUTEXPORT001-03`, `TC-PERFLARGE001-01`) |
| I9 | `[MAJOR]` | `TC-COMPATLEGACY001-01` (#17903) | **Expected không đo lường được**: version 5 ghi đúng 1 câu *"Câu trả lời được sync vào toàn bộ các sheet "* (thừa khoảng trắng cuối) — không nêu số sheet, không nêu mỗi sheet bao nhiêu dòng, không nêu nội dung dòng | Viết lại expected dạng đếm được: *"mỗi sheet của N trang có đúng 1 dòng mang mã câu trả lời, nội dung cột ... "* |
| I10 | `[MINOR]` | `TC-RULE04-01` (#17910), `TC-RULE07-01` (#17913) | Dùng **mã RULE** (`RULE-04`, `RULE-07`) làm `Mã quan điểm liên kết`. RULE là quy tắc bắt buộc, không phải mã quan điểm → cột map coverage sai | Đổi sang mã quan điểm thật: #17910 → `DATA-DB-001`; #17913 → `OUT-TRUTH-001` |
| I11 | `[MINOR]` | 11/22 TC | **Mã quan điểm nội bộ Studio** (`TOOL-*`, `RULE-TOOL-*`, `OBS-001`, `JOB-002`) không có trong `framework/checklist-lme.md` → không map được coverage, và làm quan điểm chuẩn **trông như đã cover** | Map lại sang mã chuẩn, hoặc bổ sung các mã này vào `checklist-lme.md` (RULE-10) |
| I12 | `[NIT]` | Spec `FA-011` | `spec-features/admin/form-answer/feature-spec.md` **BR-08 + Gap #11** tự nhận không biết cơ chế sync thực tế (*"job `AddResultFormAnswerToGoogleSpreadSheet` hiện bị comment out"*), trong khi kho TCs đã mô tả đầy đủ cơ chế batch hiện tại (MT-22) | Đã có đề xuất sẵn ở kho MT-22 — xem §6 #2 |

---

## 5. TCs đề xuất bổ sung (16)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa011-taobieumau-フォーム作成.md` — nhóm **[Sync Google Sheet & job]** (32 TC) + **[Liên kết Google Sheet]** (14 TC) |
| Vùng regression phát hiện từ kho | `TC-FORM-394` sheet có ô cột A trống · `TC-FORM-395` xóa form-result → xóa dòng sheet · `TC-FORM-396` batch 500 · `TC-FORM-397` submit đồng thời · `TC-FORM-398` restart app khi job đang chạy · `TC-FORM-400` retry chỉ record lỗi · `TC-FORM-401` form `using_old_version=1` · `TC-FORM-402` job không đụng form khác · `TC-FORM-403` config số luồng · `TC-FORM-404` sheet hàng nghìn dòng · `TC-FORM-382` nội dung sync (item hiển thị / item 規約) |
| Conflict expected vs kho | **Có** — `TC-COMPATLEGACY001-01` (#17903, *"sync vào toàn bộ các sheet"*) vs kho `TC-FORM-383` (*"sync đúng câu trả lời của từng page vào sheet của page đó, page không đi qua thì không ghi"*) → `[MAJOR] SPEC-CONFLICT`, **không tự chọn bên**, đã đưa §6 #1 |
| GAP dùng lại TC kho (không viết mới) | `Q1 → TC-FORM-391, TC-FORM-396, TC-FORM-400, TC-FORM-403` · `Q4 → TC-FORM-398` · `Q7 → TC-FORM-397` · `Q9 → TC-FORM-404` · `Q3 (WHERE scope) → TC-FORM-402` · `Q6 → TC-FORM-401` · `G4 → TC-FORM-395` · `G6 → TC-FORM-394` — **tất cả cần thêm nhánh "bản ghi marker-only"**, nên bên dưới viết biến thể riêng thay vì dẫn chiếu suông |
| Xác nhận chống trùng | Đã đối chiếu **22 TC** ở BƯỚC 0 + kho FA-011 — **không TC đề xuất nào trùng** |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-INTGSHEET001-02 | UI | INTG-SHEET-001 | Sync Google Sheet & job | Normal | manual | product | Form CÓ câu hỏi không bắt buộc — LINE user bấm gửi mà không nhập gì thì dòng vẫn lên spreadsheet | Form 1 trang gồm toàn câu hỏi KHÔNG bắt buộc (có ít nhất 1 câu chọn một, 1 câu text), đã liên kết spreadsheet, sheet của trang đã có hàng tiêu đề và ít nhất 1 dòng dữ liệu. Có LINE user là bạn của bot và link trả lời riêng | 1. Mở spreadsheet của form, ghi lại số dòng hiện có<br>2. Mở link trả lời form trên trình duyệt điện thoại thật<br>3. Không nhập, không chọn gì<br>4. Bấm nút gửi ở cuối form<br>5. Xác nhận chuyển sang trang hoàn tất<br>6. Mở màn kết quả trả lời form trên trang quản trị, mở chi tiết bản ghi vừa tạo và ghi lại mã câu trả lời<br>7. Chờ job chạy xong, mở lại spreadsheet và tìm dòng theo mã câu trả lời đó | Không nhập gì vào bất kỳ câu hỏi nào; chỉ bấm nút gửi | Chuyển sang trang hoàn tất trả lời. Màn kết quả có bản ghi mới, phần câu trả lời để trống. Sheet tăng đúng 1 dòng, mang đúng mã câu trả lời đó, có thời gian trả lời + tên LINE user, các cột câu hỏi để trống. Không có dòng nào bị mất và không có dòng trùng | | Lấp `G1` · `Q8` · Đây là kịch bản **đúng như khách báo** mà bộ TC hiện tại chưa dựng được (Studio #17912 phải đổi sang trang chỉ gồm phần tử hiển thị) · Đánh giá spec: Đã hỏi leader · Evidence: ảnh chụp màn hoàn tất + ảnh chụp dòng trên spreadsheet + mã câu trả lời |
| TC-INTGSHEET001-03 | UI | INTG-SHEET-001 | Sync Google Sheet & job | Abnormal | manual | product | Tái hiện đúng ca lỗi của khách trên form nhiều trang đang dùng thật | Dùng lại đúng form khách báo (`BW申し込み＞9/17(木)BW19申込`, form 228620) hoặc form nhiều trang có cấu hình tương đương, đã liên kết spreadsheet và sheet đã có dữ liệu. Có quyền xem bản ghi trả lời ngày 2026-08-19 10:47 | 1. Mở màn kết quả trả lời form, tìm bản ghi của friend 中村 和之 lúc 2026-08-19 10:47, ghi lại mã câu trả lời và nội dung câu trả lời hiển thị<br>2. Mở spreadsheet của form, tìm dòng mang mã câu trả lời đó<br>3. Lặp lại thao tác của khách trên chính form đó: mở form, bấm gửi theo đúng cách khách mô tả<br>4. Mở lại màn kết quả và spreadsheet, đối chiếu bản ghi mới với bản ghi cũ bị mất | Thao tác đúng như khách mô tả trên form thật | Xác định được bản ghi 2026-08-19 10:47 hiện có trên màn kết quả nhưng **thiếu dòng** trên spreadsheet. Bản ghi mới tạo sau khi đã deploy bản sửa **có dòng** trên spreadsheet. Ghi rõ hình dạng dữ liệu của bản ghi cũ để đối chiếu với giả thiết root cause của Dev | | Lấp `G1` · `Q8` · **Bắt buộc trước khi đóng ticket** — nếu bản ghi cũ KHÔNG phải hình dạng marker-only thì root cause chưa đúng, phải trả lại Dev · Đánh giá spec: Đã hỏi leader · Evidence: ảnh chụp 2 nơi + mã câu trả lời + nội dung cột dữ liệu của bản ghi cũ |
| TC-DATADB001-02 | Data | DATA-DB-001 | Sync Google Sheet & job | Abnormal | manual | product | Rà soát phạm vi bản ghi đã mất dòng trên spreadsheet — chạy trên dữ liệu thật | Có quyền truy vấn dữ liệu production và quyền đọc spreadsheet của các form đang liên kết (đã xin khách cấp cho `css.wss.2025@gmail.com`). Không cần chạy job | 1. Liệt kê toàn bộ form đang liên kết spreadsheet<br>2. Với mỗi form, lọc các bản ghi trả lời đang ở trạng thái đã đồng bộ, chưa xóa, mà phần dữ liệu không chứa câu trả lời thật<br>3. Đọc cột mã câu trả lời trên từng sheet tương ứng<br>4. Đối chiếu 2 danh sách theo mã câu trả lời<br>5. Lập bảng: mã form · tên form · mã câu trả lời · thời điểm trả lời · tên LINE user | Phạm vi: từ ngày bật cơ chế đồng bộ theo lô tới ngày deploy bản sửa; đối chiếu theo mã câu trả lời, không theo tên | Có danh sách đầy đủ bản ghi bị mất kèm số lượng theo từng form, trong đó **phải có** bản ghi của khách (form 228620, 2026-08-19 10:47). Danh sách được đính vào ticket trước khi đóng | | Lấp `G3` · `Q3` · **RULE-04 — chưa có con số này thì không đóng ticket**. Studio #17911 đã `skip` đúng vì thiếu dữ liệu production · Đánh giá spec: Spec ghi rõ · Evidence: file danh sách + ảnh chụp truy vấn |
| TC-DATADB001-03 | Data | DATA-DB-001 | Sync Google Sheet & job | Normal | manual | product | Khôi phục lô bản ghi đã mất — mỗi bản ghi đúng 1 dòng, giữ thời gian gốc, không ghi trùng | Đã có danh sách bản ghi mất dòng từ TC rà soát phạm vi. Bản sửa đã deploy. Đã backup spreadsheet của các form trong danh sách | 1. Chọn 1 form trong danh sách, ghi lại số dòng và danh sách mã câu trả lời hiện có trên sheet<br>2. Đưa các bản ghi bị mất của form đó về trạng thái chờ đồng bộ<br>3. Chờ job chạy xong<br>4. Đếm số dòng tăng thêm và đối chiếu từng mã câu trả lời<br>5. Kiểm tra các dòng đã có sẵn trước đó không bị đổi<br>6. Lặp lại cho form thứ hai trong danh sách | Lô bản ghi bị mất của 1 form; kèm 2 bản ghi ĐÃ có dòng để kiểm tra không bị ghi trùng | Số dòng tăng thêm đúng bằng số bản ghi bị mất. Mỗi mã câu trả lời xuất hiện đúng 1 lần. Cột thời gian hiển thị **thời điểm trả lời gốc**, không phải thời điểm chạy khôi phục. Các dòng cũ giữ nguyên. Bản ghi đã có dòng không bị thêm dòng thứ hai | | Lấp `G3` · `Q3` · RULE-04 · Chạy sau khi leader duyệt phạm vi khôi phục · Đánh giá spec: Đã hỏi leader · Evidence: ảnh chụp sheet trước/sau + danh sách mã câu trả lời |
| TC-DATADB001-04 | Data | DATA-DB-001 | Sync Google Sheet & job | Boundary | manual | Tất cả | Job chỉ đụng form đang xử lý — form khác và bot khác giữ nguyên dữ liệu lẫn trạng thái đồng bộ | Có 2 bot khác nhau, mỗi bot có ít nhất 1 form đã liên kết spreadsheet và sheet đã có dữ liệu. Ở cả 2 bot đều đặt sẵn bản ghi trả lời tên LINE user giống nhau để kiểm chứng phạm vi truy vấn | 1. Ghi lại số dòng + danh sách mã câu trả lời của sheet ở cả 2 bot<br>2. Ghi lại trạng thái đồng bộ của các bản ghi đang chờ ở form của bot B<br>3. Chỉ tạo bản ghi mới (gồm 1 bản ghi không nhập nội dung) cho form của bot A<br>4. Chờ job chạy xong lô của form bot A<br>5. Kiểm tra lại sheet và trạng thái bản ghi của **cả 2 bot** | 2 bot · mỗi bot 1 form · tên LINE user trùng nhau ở cả 2 bot | Sheet của bot A tăng đúng số dòng mới. Sheet của bot B **giữ nguyên số dòng**, không xuất hiện mã câu trả lời lạ. Trạng thái đồng bộ các bản ghi của bot B **không đổi**. Không có dòng nào của bot A lọt sang sheet bot B | | Lấp `Q3` · RULE-07 (kiểm chứng phạm vi truy vấn với dữ liệu trùng tên ở 2 tài khoản) · dẫn từ `TC-FORM-402` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh chụp 2 sheet + bảng trạng thái |
| TC-DATADB001-05 | UI | DATA-DB-001 | Sync Google Sheet & job | Normal | manual | Tất cả | Xóa bản ghi trả lời không nhập nội dung trên màn quản trị → đúng dòng đó bị xóa khỏi spreadsheet | Sheet của trang đã có dữ liệu và đã có sẵn dòng của bản ghi không nhập nội dung (tạo sau khi deploy bản sửa), cùng ít nhất 2 bản ghi bình thường nằm ngay trên và ngay dưới dòng đó | 1. Mở spreadsheet, ghi lại thứ tự và mã câu trả lời của các dòng<br>2. Vào màn kết quả trả lời form, xóa đúng bản ghi không nhập nội dung<br>3. Chờ job chạy xong<br>4. Mở lại spreadsheet, đối chiếu từng dòng còn lại với danh sách ban đầu | Xóa 1 bản ghi không nhập nội dung nằm giữa 2 bản ghi bình thường | Chỉ dòng mang mã câu trả lời vừa xóa biến mất. Hai dòng liền kề **giữ nguyên nội dung và không bị dịch sai**. Tổng số dòng giảm đúng 1 | | Lấp `G4` · **Vùng Studio `dev_impact` yêu cầu smoke lại nhưng chưa có TC nào** · dẫn từ `TC-FORM-395` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh chụp sheet trước/sau |
| TC-JOB001-01 | API | JOB-001 | Sync Google Sheet & job | Normal | manual | product | Job ghi theo lô 500 bản ghi — lô trộn bản ghi không nhập nội dung vẫn đủ, không sót | Form đã liên kết spreadsheet, sheet đã có tiêu đề và dữ liệu. Job đồng bộ đang chạy với cấu hình lô mặc định | 1. Ghi lại số dòng hiện có trên sheet<br>2. Tạo 501 bản ghi trả lời cho cùng form, trong đó có 100 bản ghi không nhập nội dung xen kẽ<br>3. Chờ job chạy xong toàn bộ<br>4. Đếm số dòng tăng thêm và đối chiếu danh sách mã câu trả lời trên sheet với trong hệ thống<br>5. Đọc log để xác nhận job chia thành 2 lần ghi | 501 bản ghi: 401 có đáp án đầy đủ, 100 không nhập nội dung | Sheet tăng đúng 501 dòng, mỗi mã câu trả lời xuất hiện đúng 1 lần, 100 bản ghi không nhập nội dung đều có dòng với các cột câu hỏi để trống. Log cho thấy job chia 2 lần ghi (500 + 1). Không bản ghi nào còn ở trạng thái chờ | | Lấp `Q1` · RULE-08 (job nền) · dẫn từ `TC-FORM-396` · Đánh giá spec: Spec không ghi — theo kho MT-22 · Evidence: log job + ảnh chụp sheet + số dòng |
| TC-JOB001-02 | API | JOB-001 | Sync Google Sheet & job | Abnormal | manual | product | Restart ứng dụng khi job đang xử lý bản ghi không nhập nội dung — không mất, không trùng | Sheet đã có tiêu đề và dữ liệu. Có quyền restart ứng dụng chạy job. Đã chuẩn bị lô bản ghi đủ lớn để job còn đang chạy khi restart | 1. Ghi lại số dòng hiện có trên sheet<br>2. Tạo lô bản ghi trộn bản ghi không nhập nội dung và bản ghi có đáp án<br>3. Khi job bắt đầu chạy lô này thì restart ứng dụng<br>4. Sau khi ứng dụng khởi động lại, ghi nhận trạng thái các bản ghi<br>5. Chờ job chạy lại xong<br>6. Đối chiếu danh sách mã câu trả lời trên sheet với trong hệ thống | Lô ~50 bản ghi, trong đó 20 bản ghi không nhập nội dung | Sau restart, các bản ghi đang xử lý dở quay về trạng thái chờ đồng bộ (không kẹt). Sau khi job chạy lại: mỗi mã câu trả lời có đúng 1 dòng, không bản ghi nào bị mất, không dòng nào bị ghi 2 lần | | Lấp `Q1` · `Q4` (REG-RUN-001) · RULE-08 · dẫn từ `TC-FORM-398` · Đánh giá spec: Spec không ghi — theo kho MT-22 · Evidence: log job trước/sau restart + danh sách mã câu trả lời |
| TC-JOB001-03 | API | JOB-001 | Sync Google Sheet & job | Boundary | manual | product | Google lỗi liên tục — job thử lại theo lịch tăng dần rồi đánh dấu lỗi hẳn, bản ghi không nhập nội dung không bị mất | Sheet đã có tiêu đề và dữ liệu. Có cách gây lỗi kéo dài cho thao tác gọi Google (thu hồi quyền / chặn mạng ra ngoài) | 1. Tạo 3 bản ghi, trong đó 1 bản ghi không nhập nội dung<br>2. Gây lỗi gọi Google trước khi job chạy<br>3. Ghi lại thời điểm từng lần job thử lại và nội dung log<br>4. Để lỗi kéo dài qua đủ số lần thử lại<br>5. Khôi phục kết nối, đưa các bản ghi bị đánh dấu lỗi về trạng thái chờ, chờ job chạy lại<br>6. Đối chiếu sheet với danh sách bản ghi | 3 bản ghi (1 không nhập nội dung); lỗi Google kéo dài | Khoảng cách giữa các lần thử lại tăng dần theo đúng cấu hình. Hết số lần thử lại, bản ghi bị đánh dấu lỗi hẳn chứ **không** bị đánh dấu đã đồng bộ. Log mỗi lần đều kèm mã câu trả lời. Sau khi khôi phục, cả 3 bản ghi lên sheet đủ, mỗi bản ghi 1 dòng | | Lấp `Q1` · RULE-08 · dẫn từ `TC-FORM-391` + `TC-FORM-400` · Đánh giá spec: Spec không ghi — theo kho MT-22 · Evidence: log job kèm dấu thời gian từng lần thử lại |
| TC-OUTEXPORT001-01 | UI | OUT-EXPORT-001 | Sync Google Sheet & job | Normal | manual | Tất cả | Nội dung bản ghi không nhập nội dung khớp nhau ở cả 3 nơi: màn kết quả · file CSV · spreadsheet | Đã có bản ghi trả lời không nhập nội dung đã đồng bộ. Tài khoản quản trị đã đăng nhập đúng bot | 1. Mở màn kết quả trả lời form, mở chi tiết bản ghi, ghi lại mã câu trả lời · thời điểm trả lời · tên LINE user<br>2. Bấm xuất file CSV của form, mở file và tìm dòng theo mã câu trả lời<br>3. Mở spreadsheet, tìm dòng theo mã câu trả lời<br>4. So từng ô giữa 3 nơi | Không nhập liệu, chỉ tra cứu | Cả 3 nơi đều có bản ghi đó. Mã câu trả lời · thời điểm trả lời · tên LINE user **giống hệt nhau** ở cả 3 nơi. Phần câu trả lời để trống ở cả 3 nơi, không nơi nào hiển thị giá trị lạ hay bị thiếu bản ghi | | Lấp `Q2` · RULE-06 (output cuối) + RULE-07 (khớp 3 nơi) · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh chụp 3 nơi |
| TC-OUTEXPORT001-02 | UI | OUT-EXPORT-001 | Sync Google Sheet & job | Abnormal | manual | Tất cả | Item đồng ý quy chế ĐƯỢC ghi ra spreadsheet; đoạn mô tả · video · HTML tự do thì KHÔNG | Form 1 trang có đủ 3 nhóm phần tử: ít nhất 2 câu hỏi thường · 1 item đồng ý quy chế đã nhập nội dung hướng dẫn checkbox 「チェックボックスの案内文」 · 3 phần tử hiển thị (đoạn mô tả, video, HTML tự do) xen giữa các câu hỏi. Đã liên kết spreadsheet, sheet của trang đã có hàng tiêu đề và dữ liệu | 1. Mở cài đặt item quy chế, ghi lại nguyên văn nội dung 「チェックボックスの案内文」 đang cài<br>2. Ghi lại hàng tiêu đề hiện có trên sheet và số cột<br>3. Cho LINE user trả lời form: tick đồng ý quy chế, nhập đáp án cho 1 câu hỏi, để trống câu còn lại<br>4. Chờ job chạy xong<br>5. Mở spreadsheet: đối chiếu hàng tiêu đề và dòng mới, xác định cột nào ứng với item nào<br>6. Xuất file CSV của form và đối chiếu cùng nội dung | Nội dung 「チェックボックスの案内文」 đặt một chuỗi dễ nhận biết; tick đồng ý quy chế; 1 câu hỏi có đáp án, 1 câu để trống | Item đồng ý quy chế **CÓ cột riêng** trên spreadsheet, giá trị ô = **đúng nguyên văn nội dung 「チェックボックスの案内文」** đã cài trong form; file CSV cũng có cột này với cùng giá trị. Ba phần tử hiển thị (đoạn mô tả, video, HTML tự do) **KHÔNG sinh cột nào** và không ghi giá trị nào lên sheet, không đẩy lệch các cột câu hỏi. Số cột của dòng mới khớp đúng hàng tiêu đề | | Lấp `Q2` · Hành vi đúng do **Leader chốt 2026-09-16**, khớp kho MT-07 · ⚠️ Kho ghi hiện trạng cũ là spreadsheet KHÔNG ghi item quy chế (r800) → nếu thiếu cột hoặc ô rỗng thì ghi Không đạt và **raise bug** · dẫn từ `TC-FORM-382` · Đánh giá spec: Đã hỏi leader · Evidence: ảnh chụp cài đặt 案内文 + ảnh chụp cột quy chế trên sheet + file CSV |
| TC-OUTEXPORT001-03 | API | OUT-EXPORT-001 | Sync Google Sheet & job | Boundary | manual | Tất cả | Sheet có ô cột A trống ở giữa vùng data — dòng mới không nhập nội dung vẫn ghi xuống cuối, không đè dòng cũ | Sheet của trang có 5 dòng dữ liệu, trong đó **xóa trống ô cột A của dòng thứ 3 và dòng thứ 5** (giữ nguyên các ô còn lại). Job đồng bộ đang chạy | 1. Chụp lại toàn bộ 5 dòng hiện có kèm nội dung từng ô<br>2. Tạo 1 bản ghi trả lời không nhập nội dung cho trang đó<br>3. Chờ job chạy xong<br>4. Mở sheet, xác định dòng mới nằm ở vị trí nào<br>5. Đối chiếu lại nội dung 5 dòng cũ | 1 bản ghi không nhập nội dung; sheet có ô cột A trống ở dòng 3 và 5 | Dòng mới được ghi vào **dòng thứ 6**. Toàn bộ 5 dòng cũ **giữ nguyên nội dung**, tuyệt đối không bị ghi đè. Dòng mới bắt đầu từ cột A, không lệch cột | | Lấp `G6` · `Q2` · **Quy tắc đã chốt ở kho MT-16 — thấy ghi đè thì RAISE BUG** · dẫn từ `TC-FORM-394` · Đánh giá spec: Đã hỏi leader (MT-16) · Evidence: ảnh chụp sheet trước/sau đủ 6 dòng |
| TC-ENV003-01 | API | ENV-003 | Sync Google Sheet & job | Normal | manual | product | Chạy lại bộ TC lõi trên môi trường có spreadsheet Google thật | Môi trường staging/production có bot đã liên kết Google account thật, form đã có spreadsheet thật và sheet đã có dữ liệu. Job đồng bộ chạy bằng cấu hình thật | 1. Chọn lại các TC lõi đã pass ở local: bản ghi không nhập nội dung trên sheet đã có dữ liệu · trả lời đầy đủ không lệch cột · 2 kiểu bố cục cột · form nhiều trang · chạy lại job không trùng dòng<br>2. Chạy lần lượt từng TC trên môi trường thật, dùng spreadsheet Google thật<br>3. Ghi nhận kết quả từng TC kèm mã câu trả lời<br>4. Đối chiếu với kết quả đã chạy ở local | Bộ TC lõi, dữ liệu thật trên môi trường thật | Mọi TC lõi cho kết quả **giống hệt** khi chạy ở local. Nếu có TC lệch kết quả → ghi Không đạt kèm mã câu trả lời và raise bug ngay, không kết luận theo kết quả local | | Lấp `Q5` · **RULE-08 — job nền + tích hợp Google không được kết luận từ local** · Đánh giá spec: Spec ghi rõ · Evidence: ảnh chụp spreadsheet thật + bảng đối chiếu kết quả local vs thật |
| TC-COMPATLEGACY001-02 | Data | COMPAT-LEGACY-001 | Sync Google Sheet & job | Normal | manual | Tất cả | Form đời cũ vẫn ghi được dòng cho bản ghi không nhập nội dung | Có form được tạo bằng phiên bản giao diện cũ (form cũ chưa chuyển sang bản dựng mới), đã liên kết spreadsheet và sheet đã có dữ liệu. Job đồng bộ đang chạy | 1. Xác nhận form đang ở phiên bản cũ<br>2. Ghi lại số dòng hiện có trên sheet<br>3. Cho LINE user mở form cũ và bấm gửi mà không nhập gì<br>4. Chờ job chạy xong<br>5. Tìm dòng theo mã câu trả lời và kiểm tra vị trí từng ô<br>6. Lặp lại thao tác tương tự trên 1 form bản mới để đối chiếu | 1 form đời cũ + 1 form bản mới để đối chiếu | Cả form cũ và form mới đều sinh đúng 1 dòng cho bản ghi không nhập nội dung, cùng cấu trúc ô đầu dòng, các cột câu hỏi để trống, không lệch cột ở cả hai | | Lấp `Q6` · **RULE-09 — cũ & mới song song** · dẫn từ `TC-FORM-401` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh chụp sheet của cả 2 form |
| TC-CONC001-02 | API | CONC-001 | Sync Google Sheet & job | Abnormal | manual | product | Nhiều người trả lời đồng thời trên nhiều form — không mất, không trùng, không ghi nhầm sheet | Có ít nhất 3 form đã liên kết spreadsheet, sheet của mỗi form đã có dữ liệu; trong đó 1 form nhiều trang. Job chạy với cấu hình nhiều luồng mặc định | 1. Ghi lại số dòng hiện có trên sheet của từng form<br>2. Cho nhiều LINE user cùng lúc gửi trả lời: trên cùng 1 form và trên 3 form khác nhau, trộn bản ghi không nhập nội dung với bản ghi có đáp án<br>3. Chờ job chạy xong toàn bộ<br>4. Với từng form, đối chiếu danh sách mã câu trả lời trên sheet với trong hệ thống<br>5. Kiểm tra không có mã câu trả lời của form này nằm trên sheet form khác | ~30 lượt gửi trong vòng 1 phút, trải trên 3 form, 1/3 số lượt là không nhập nội dung | Mỗi form nhận đúng số dòng tương ứng số lượt gửi của mình. Mỗi mã câu trả lời xuất hiện đúng 1 lần và chỉ trên sheet của đúng form/đúng trang. Không bản ghi nào còn ở trạng thái chờ | | Lấp `Q7` · RULE-08 (đa luồng) · dẫn từ `TC-FORM-397` · Đánh giá spec: Spec không ghi — theo kho MT-22 · Evidence: bảng đối chiếu mã câu trả lời từng form + log job |
| TC-PERFLARGE001-01 | API | PERF-LARGE-001 | Sync Google Sheet & job | Boundary | manual | product | Sheet đã có hàng nghìn dòng — dòng mới không nhập nội dung vẫn ghi đúng cuối, không lệch cột | Sheet của trang đã có ít nhất 5.000 dòng dữ liệu và hàng tiêu đề đầy đủ. Job đồng bộ đang chạy | 1. Ghi lại tổng số dòng và nội dung 3 dòng cuối<br>2. Tạo 1 bản ghi không nhập nội dung và 1 bản ghi có đáp án đầy đủ<br>3. Chờ job chạy xong, ghi lại thời gian job xử lý<br>4. Mở sheet, kiểm tra vị trí 2 dòng mới và nội dung từng ô<br>5. Kiểm tra 3 dòng cuối cũ không bị thay đổi | Sheet 5.000+ dòng; 2 bản ghi mới | Hai dòng mới nằm ngay sau dòng cuối cùng, đúng thứ tự tạo. Dòng của bản ghi không nhập nội dung có đủ ô đầu dòng và các cột câu hỏi để trống, không lệch cột. Ba dòng cũ giữ nguyên. Job không lỗi và không bỏ sót bản ghi nào | | Lấp `Q9` · RULE-08 (hiệu năng) · dẫn từ `TC-FORM-404` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh chụp vùng cuối sheet + thời gian xử lý của job |

**Dòng `GAP` / `RISK` không đề xuất TC:**

- **G2** (`status_sync_sheet` bị set sai) — đã có `TC-OUTTRUTH001-01` (#17907) mô tả đúng vấn đề và đã raise Redmine **#41010**. Không viết TC mới; việc cần làm là **sửa mục 4.2 của file 03** (bổ sung `D1`) và theo dõi #41010, xem I7.
- **G5 / Q10** (form nhiều trang với data đời cũ) — **không đề xuất TC** vì `Kết quả mong đợi` đang mâu thuẫn giữa #17903 (*"toàn bộ các sheet"*) và kho `TC-FORM-383` (*"chỉ sheet của trang đó"*). Theo BƯỚC 5a, **không tự chọn bên** → đẩy lên §6 #1. Chốt xong hành vi đúng mới viết TC.

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | `spec-features/admin/form-answer/feature-spec.md` — **BR-08** (Google Sheets integration) | **Chốt hành vi đúng cho bản ghi trả lời đời cũ (phần tử `data` không có khoá `page`) trên form nhiều trang**: ghi vào **mọi** sheet, hay chỉ sheet của trang bản ghi thực sự thuộc về? Hiện `DataFormAnswerResult.getPageCustom()` trả về chính trang đang xét khi phần tử không có khoá `page` ⇒ bản ghi "thuộc" mọi trang. Phạm vi: **91 form nhiều trang đang liên kết spreadsheet** | `TC-COMPATLEGACY001-01` (#17903, expected v5 = *"toàn bộ các sheet"*, Studio bug **#1068 rejected**) **vs** kho `TC-FORM-383` (*"sync đúng câu trả lời của từng page vào sheet của page đó, page không đi qua thì không ghi"*) **vs** `TC-TOOLNEGCTRL001-01` (#17902, *"chỉ sheet trang tương ứng"*) | **PM + Dev** — quyết định này đang được dùng làm lý do reject bug, nên phải có văn bản chốt trước khi đóng ticket |
| 2 | `spec-features/admin/form-answer/feature-spec.md` — **BR-08 + §9 + Gap #11** | Spec đang ghi *"job `AddResultFormAnswerToGoogleSpreadSheet` hiện bị comment out — sync có thể đang dùng cơ chế khác"* và tự nhận **Gap #11**. Cơ chế thật (đã được kho TCs mô tả đầy đủ ở MT-22): ghi theo **lô 500 bản ghi** · hàng đợi thử lại với chuỗi `1p→5p→10p→30p→60p`, quá 5 lần đánh dấu lỗi hẳn · bản ghi `PROCESSING` reset khi restart app · khóa theo `form_id` · `MAX_SYNC_GOOGLE_SHEET_THREAD` mặc định 5 luồng. **Bổ sung thêm hành vi mới của lần fix này**: bản ghi thuộc trang đang ghi **luôn** sinh 1 dòng kể cả khi không có câu trả lời nào | Kho `kho-tcs/fa011` **MT-22** (đã CHỐT) + Journal #136168 mục 2 | **Dev** (viết lại BR-08, đóng Gap #11) |
| 3 | `spec-features/admin/form-answer/feature-spec.md` — **BR-08** | Chốt bằng spec quy tắc **"tuyệt đối không ghi đè dòng cũ"** khi sheet có ô cột A trống giữa vùng data (kho MT-16 đã chốt, quyết định cũ của PM *"chấp nhận ghi đè, lỗi KH"* đã bị hủy). Lần fix này **làm tăng số dòng ghi thêm** vào sheet đã có dữ liệu nên rủi ro ghi đè tăng theo | Kho `kho-tcs/fa011` **MT-16** (đã CHỐT) vs hiện trạng chưa có TC nào kiểm ở task này | **Dev + Leader** |
| 4 | `spec-features/admin/form-answer/feature-spec.md` — **BR-08** + danh sách loại câu hỏi | **Chốt danh sách phần tử nào được ghi ra spreadsheet** (Leader đã quyết 2026-09-16): item **đồng ý quy chế (規約)** **CÓ** ghi, giá trị = nội dung 「チェックボックスの案内文」 cài trong form; **đoạn mô tả · video · HTML tự do KHÔNG** ghi. Spec hiện **không mô tả item 規約** trong danh sách loại câu hỏi và **không mô tả nội dung sync** ra spreadsheet | Quyết định của Leader 2026-09-16 · kho `kho-tcs/fa011` **MT-07** (đã CHỐT, cùng kết luận) · hiện trạng cũ ở corpus r800 ghi ngược lại (*"item quy chế không ghi vào file google"*) | **Dev** — bổ sung vào BR-08 + danh sách loại câu hỏi; nếu code đang không ghi thì raise bug theo `TC-OUTEXPORT001-02` |
| 5 | `spec-features/admin/form-answer/feature-spec.md` — **BR-08** | Ghi rõ **điều kiện sinh bản ghi chỉ có dấu mốc trang**: theo `actual` của #17912, giao diện form V3 chỉ sinh hình dạng này khi trang **không có phần tử nhập liệu nào**; form có câu hỏi mà bỏ trống vẫn gửi item kèm `type` + `value` rỗng. Spec cần mô tả đúng 2 hình dạng dữ liệu này để TC và Dev không hiểu khác nhau | `TC-RULETOOL027-01` (#17912) `actual` vs mô tả root cause ở Journal #136168 mục 1 | **Dev** — đồng thời trả lời câu hỏi ở I3 (bản ghi thật của khách thuộc hình dạng nào) |
