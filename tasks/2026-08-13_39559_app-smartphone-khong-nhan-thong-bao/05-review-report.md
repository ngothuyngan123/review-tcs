# 05 — Review Report

## Thông tin

| Trường | Giá trị |
|---|---|
| Bug ID | `#39559` — [T11862][Notify Setting] App smartphone không nhận được thông báo (PC vẫn nhận) |
| Reviewer (Leader) | `<Leader ký tên>` — draft do `/review-tc` sinh 2026-08-13 |
| Tester được review | **AI** (27 TC, `created_job_id=326`) + **haodtb** (18 TC) — bộ TC trên MCP LME TEST STUDIO task #67 |
| Ngày review | `2026-08-13` |
| Version TCs | Studio round 1 · toàn bộ 45 TC `version=1`, `status=draft` |
| Vòng review | `Round 1` |
| Input đã đọc | `01-bug-task.md` · `03-dev-impact.md` · `04-tc-list.md` (45 TC) · **KHÔNG có `02-spec-reference.md`** |

> **Spec reference**: không có `02-spec-reference.md` riêng. Đã fallback tham chiếu [templates/LME-SYSTEM-SPEC.md](../../templates/LME-SYSTEM-SPEC.md) (FA-006 通知設定, FS-014) **và** spec sub-repo [spec-features/admin/notify-setting/](../../spec-features/admin/notify-setting/) (`feature-spec.md`, `job/job-spec.md`, `web/api-spec.md`, `db/db-mapping.md`). Kết quả đối chiếu spec → **§6 (có vấn đề, phải update spec)**.

---

## 1. Verdict

- [ ] **APPROVED** — TCs đạt, không cần chỉnh sửa
- [ ] **APPROVED WITH CHANGES** — Approve sau khi fix các issue MINOR (không cần review lại)
- [x] **REJECTED** — Có issue BLOCKER/MAJOR, cần fix và review lại

**Lý do ngắn gọn**: **Toàn bộ 45 TC được chạy lúc `2026-08-13 10:03:36`, TRƯỚC khi commit fix cuối `3233088f19` được push (journal Redmine `11:21:51Z`)** → 32 kết quả `Đạt` không phải bằng chứng cho code sẽ release, và ít nhất 1 TC (`TC-RULE09-01`) đang `Đạt` với **kết quả mong đợi trái ngược** hành vi sau fix cuối. Cộng thêm 3 TC `fail` chưa raise ticket (1 trong đó là **rò rỉ dữ liệu chéo tài khoản**), API xoá token được sửa nhưng **0 TC**, và không có TC nào chạy trên production dù task chạm **job nền + push Firebase** (RULE-08).

---

## 2. Tóm tắt cho member

Bộ TC ở tầng API rất chắc: 45 TC phủ được cả 4 điểm sửa chính (tra bản ghi kèm user, dọn trùng giữ bản mới nhất, logout đúng người, getFirebaseToken theo chủ), có cả TC tái hiện đúng lỗi khách (`NEW-3`) và TC chứng minh hậu quả gửi sai đối tượng (`NEW-22`) — phần này viết tốt hơn mức trung bình. Ba TC `fail` cũng được viết rất có ý thức: `NEW-13` và `NEW-25` chủ động ghi "nếu thực tế sai thì đây là khoảng trống, chờ dev/PO quyết" thay vì tự đóng Đạt — đúng tinh thần RULE-03.

Vấn đề lớn nhất **không nằm ở chất lượng TC mà ở thời điểm chạy**: ticket đã qua 3 vòng auto-fixbug, và cả bộ TC chạy xong lúc 10:03 trong khi commit cuối lên lúc 11:21 cùng ngày. Nghĩa là bộ kết quả hiện tại đang mô tả **code của vòng 2**, không phải code sẽ release — bằng chứng rõ nhất là `NEW-11` vẫn `Đạt` với kỳ vọng "bản ghi được bổ sung mã định danh", trong khi vòng 3 đã **xoá hẳn** đoạn ghi cột đó. Cần **chạy lại toàn bộ trên commit `3233088f19`**, viết lại `NEW-11`/`NEW-12`/`NEW-13` theo hành vi mới, raise ticket cho 3 TC fail, bổ sung TC cho API xoá token, và đưa TC end-to-end lên **production** (job nền + Firebase không kết luận được từ local — RULE-08). 18 TC do `haodtb` viết (NEW-28→NEW-45) cần thêm `Dữ liệu test/input` và `Kết quả mong đợi` đo lường được — hiện đa số chỉ ghi "User nhận được thông báo".

---

## 3. Coverage Matrix

> Impact lấy từ `03-dev-impact.md`. Map TC → impact **suy luận** từ `Tiêu đề` / `Điều kiện tiền đề` / `Các bước thực hiện` / `Kết quả mong đợi` (file 04 không có cột Map to Impact). TC ghi theo `temp_id` Studio cho ngắn — đối chiếu `TC No.` ở file 04.

| Impact | Loại | Priority | TCs map | # TC | Status |
|---|---|---|---|---|---|
| **BUG-1** — RC1: `device_id` trùng (ROM build ID) → `updateFirebaseToken` tra không kèm user → chiếm bản ghi | Fix | — | NEW-3 (tái hiện lỗi KH), NEW-5, NEW-22, NEW-26, NEW-27 (`blocked`) | 5 | **RISK** — TC E2E trên máy thật (NEW-27) **blocked**; 4 TC còn lại chạy pre-final-commit |
| **BUG-2** — RC2: app iOS **không bao giờ gọi** API cập nhật token (19 máy) — Dev ghi rõ **KHÔNG fix ở web** | Fix | — | — | **0** | **GAP** (chấp nhận được ở tầng web) — nhưng **chặn việc đóng ticket**, xem `[MAJOR] SYMPTOM-ONLY` §4.2 |
| **F1a** — `MobileController::updateFirebaseToken` | Function | Direct | NEW-1, NEW-2, NEW-3, NEW-4 (`fail`), NEW-5, NEW-6, NEW-7, NEW-8, NEW-9 | 9 | **RISK** — đủ chiều (Normal/Abnormal/Boundary) nhưng NEW-4 fail chưa kết luận + chạy pre-final-commit |
| **F1b** — `MobileController::getFirebaseToken` | Function | Direct | NEW-10 (`skip`), NEW-11 (**expected sai**), NEW-12 (`skip`), NEW-13 (`fail`), NEW-14, NEW-15 | 6 | **RISK nghiêm trọng** — 6 TC nhưng chỉ 2 chạy được và Đạt; đây là hàm bị sửa **nhiều nhất** (3 vòng liên tiếp) |
| **F1c** — API **xoá** token (`deleteFirebaseToken`) — vòng 3 thêm chốt chặn `user_token` rỗng | Function | Direct | — | **0** | **GAP** → `[BLOCKER] GAP-1` |
| **F2** — `AuthMobileController::logout` | Function | Direct | NEW-16, NEW-17, NEW-18, NEW-19, NEW-21, NEW-42 | 6 | **OK** (Normal + Abnormal + Boundary + regression) |
| **F3** — `HandleSubscribeTopic` (job `firebase:subscribe_topic`) | Function | Indirect (Dev không ghi ở 4.1) | NEW-20, NEW-21, NEW-22, NEW-41 | 4 | **RISK** — chỉ verify "đăng ký đúng chủ"; **không** có rate limit / retry / backoff / vào=ra (`JOB-001`) → `[BLOCKER] GAP-3` |
| **F4** — 2 API popup bảo trì cùng file (**chưa fix**, cùng root cause) | Function | Chưa fix | NEW-25 (`fail` — chủ động ghi nhận để leader quyết) | 1 | **RISK** — TC làm đúng việc, nhưng **chưa có quyết định scope** của leader → §4.2 |
| **F5** — lệnh `recover:user_firebase_token` (Dev **cố ý không fix**, còn lỗi cùng họ) | Function | Chưa fix | — | **0** | **GAP** → `[BLOCKER] GAP-9` (lệnh này sẽ **xoá bản ghi của người vừa đăng nhập**) |
| **D1** — `user_firebase_token` đổi mô hình: mỗi (user, device_id, os) = 1 bản ghi | Data | CREATE/DELETE | NEW-6, NEW-7, NEW-14, NEW-23 | 4 | **OK** |
| **D2** — `user_firebase_token.user_id` không còn bị ghi đè | Data | UPDATE | NEW-2, NEW-3, NEW-5 | 3 | **OK** |
| **D3** — `user_firebase_token.status_subscribe_topic` (0 / 2) | Data | UPDATE | NEW-2, NEW-6, NEW-7, NEW-16, NEW-17, NEW-20, NEW-21 | 7 | **OK** |
| **D4** — dữ liệu cũ đã bị chiếm → **tự lành** ở lần gọi kế tiếp | Data | — | NEW-5, NEW-22, NEW-24 | 3 | **RISK** — NEW-24 (RULE-04) chỉ query trên **local**; RULE-04 đòi thống kê **toàn hệ thống** trên production |
| **D5** — `user_firebase_token.user_token` sau vòng 3 **không còn chỗ nào đọc/ghi** | Data | — | NEW-11 (kỳ vọng **NGƯỢC LẠI**: "được bổ sung mã định danh") | 1 | **GAP / SAI** → `[BLOCKER] GAP-2` |
| **T1** — Notification Settings (FA-006) push lên app Elme | Feature | High | NEW-26, NEW-27 (`blocked`), NEW-28→NEW-40, NEW-41 | 16 | **RISK** — 9/16 TC `skip`, gồm **chat 1:1** (NEW-28) là kênh khách báo lỗi |
| **T2** — System Notifications (FS-014) — thông báo **hệ thống** đẩy lên app | Feature | High | — | **0** | **GAP** → `[BLOCKER] GAP-8` (Dev ghi High risk ở 4.3) |
| **T3** — Đăng nhập / đăng xuất app | Feature | High | NEW-16, NEW-17, NEW-18, NEW-19, NEW-42 | 5 | **OK** |
| **T4** — Job `firebase:subscribe_topic` | Feature | High (job nền → RULE-08) | NEW-20, NEW-21, NEW-22 | 3 | **RISK** — 100% chạy `local`, **0 TC production** → `[BLOCKER] GAP-4` |
| **T5** — Badge / đếm chưa đọc trên app | Feature | Medium (Dev) — nhưng `DATA-COUNT-001` là **Cao** | NEW-10 (`skip`), NEW-12 (`skip`), NEW-13 (`fail`), NEW-15 | 4 | **GAP thực tế** — 0 TC chạy được Đạt, **không TC nào đối chiếu phép tính tay** → `[BLOCKER] GAP-5` |

### ORPHAN TCs

| TC ID | Title | Lý do orphan | Hành động đề xuất |
|---|---|---|---|
| NEW-23 (`TC-DATADB001-02`) | Cấu trúc dữ liệu token cho phép nhiều tài khoản trên cùng mã thiết bị | Verify **schema không đổi** — REQ-007 tự ghi "bản sửa lần này không thêm ràng buộc / index nào". Không có code change để verify | **Giữ**, re-label là *tiền đề dữ liệu* (precondition check) chứ không phải TC. Gộp làm precondition của NEW-7 |
| NEW-24 (`TC-RULE04-01`) | Xác nhận phạm vi dữ liệu sau khi chạy bộ test — không phát sinh bản ghi bị đổi chủ mới | Đây là **thủ tục đóng ticket (RULE-04)**, không phải TC verify 1 impact | **Giữ**, nhưng chuyển env sang **PRODUCTION** — RULE-04 đòi query **toàn hệ thống**, query trên `local` không có giá trị |
| NEW-43, NEW-44 (`TC-FUNC001-17/18`) | Tạo thông báo khi app **đóng hẳn** / **chạy ngầm** | Hành vi thuộc **tầng app + OS**, không thuộc code web đã sửa (AP-5) | **Giữ ở mức smoke** — hữu ích cho RC2, nhưng ghi rõ ở `Ghi chú` là *app-side*, không phải verify diff này |

---

## 3.5 Fix-shape analysis (adversarial)

| Mục | Giá trị |
|---|---|
| **Fix shape** (đọc mục 2 dev-impact) | **Specific code check** — đổi **khoá tra cứu** của query (thêm `user_id` vào WHERE), thêm `ORDER BY id DESC`, đảo chiều đoạn dọn trùng, + **guard validation** (chặn `user_token` rỗng). **KHÔNG** phải generic catch-all. Kèm **rollback** 1 commit của lần fix trước (`4dd0eb47`) |
| **Trigger space cần cover** | **8 điểm**: (1) `updateFirebaseToken` lookup theo device+os+user; (2) khối dọn trùng giữ max id trong scope user+device+os; (3) `logout` scope theo user + bản mới nhất; (4) `getFirebaseToken` theo device+user, bản mới nhất, **bỏ hẳn `os`**; (5) guard `user_token` rỗng ở `getFirebaseToken`; (6) guard ở `updateFirebaseToken`; (7) guard ở **API xoá token**; (8) **xoá hẳn** nhánh dự phòng tra qua `user_firebase_token.user_token` + xoá đoạn ghi cột đó |
| **Số trigger TCs hiện cover** | **6/8 (và không điểm nào có kết quả hợp lệ trên commit cuối)** — (1) ✅ NEW-1/2/3/7 · (2) ✅ NEW-6/8 · (3) ✅ NEW-16/17/18/19/21 · (4) ⚠️ NEW-10 `skip`, NEW-14 ✅, NEW-15 ✅ — **nhưng nhánh "bỏ `os`" không TC nào cover** · (5) ⚠️ NEW-13 `fail` · (6) ⚠️ NEW-4 `fail`, NEW-9 ✅ · (7) ❌ **0 TC** · (8) ❌ NEW-11 kỳ vọng **hành vi cũ đã bị xoá** |
| **KH report dạng** | **Symptom-only** — KH chỉ ghi "không nhận được thông báo trên app, PC vẫn nhận", không có error code / message / log. Dev tự tìm ra **2 root cause** từ log production |
| **Alternative root causes cần verify** | **RC2 (Dev đã xác định, KHÔNG fix)**: 19 máy — chủ yếu **iOS**, `device_id` là UUID hợp lệ không trùng — app **không bao giờ gọi** API cập nhật token (user từ chối quyền thông báo, hoặc lấy mã Firebase thất bại). **Không TC nào cover.** ⚠️ Journal QA tái hiện lại ghi *iOS nhận được / Android KHÔNG nhận* — **ngược chiều** phân bố RC2 → chưa ai xác định khách `rintaro@ssks.work` thuộc nhóm nào |
| **Anti-patterns dính** | **AP-2** (symptom-only) · **AP-3** (happy-path-only regression) · **AP-4** (không có PR link để verify fix shape) · **AP-5** nhẹ (NEW-43/44 app-side) · **AP-6 biến thể** (mục 3 dev-impact **không trống nhưng lạc hậu** — là bản vòng 1, thiếu `getFirebaseToken`, API xoá token, 2 API popup) |

> Trigger space cover **6/8** và **0/8 có kết quả hợp lệ trên commit cuối** → flag `[BLOCKER] FIX-SHAPE` ở §4.1.

---

## 4. Issues phát hiện

### 4.1 Blocker (phải fix trước khi merge)

**Nhóm A — kết quả test không hợp lệ (nghiêm trọng nhất, đọc trước)**

- `[BLOCKER] FIX-SHAPE-1` **Toàn bộ 45 TC chạy TRƯỚC commit fix cuối**: Studio `ranAt = 2026-08-13 10:03:36`, nhưng journal AI AUTO-FIXBUG cuối (commit **`3233088f19`**) push lúc **`2026-08-13T11:21:51Z`** — sau ~1h18′. Mốc thời gian Studio khớp hệ UTC của Redmine (`addedAt 03:48:45` nằm ngay sau journal vòng 1 `03:39:10Z`), nên lần chạy này ứng với **code vòng 2 (`caa3c277b7`)**. ⇒ **32 kết quả `Đạt` không phải bằng chứng cho code sẽ release**; 3 `fail` cũng có thể đã tự hết sau vòng 3. — **Fix**: chạy lại **toàn bộ 45 TC** trên đúng commit `3233088f19` (branch `ai_small_39559`), ghi `Ngày thực hiện` + commit hash vào `Ghi chú` từng TC. **Không** dùng lại kết quả cũ. Đề nghị Studio bổ sung field commit hash vào `last_exec` để lần sau tự phát hiện.

- `[BLOCKER] GAP-2` **`NEW-11` (`TC-RULE09-01`) có `Kết quả mong đợi` TRÁI NGƯỢC fix cuối nhưng đang `Đạt`**: TC kỳ vọng *"bản ghi được **bổ sung mã định danh** vừa hỏi vào cột `user_token` để lần sau tra đúng chủ"*. Vòng 3 ghi rõ: *"**bỏ luôn đoạn ghi mã người dùng vào bản ghi chưa gắn** (chính là cơ chế chiếm bản ghi, nay không còn tới được)"* và *"BỎ HẲN nhánh dự phòng ... không xác định được thì API **trả rỗng**"*. Đây chính là bằng chứng độc lập xác nhận FIX-SHAPE-1. Cùng vấn đề: **REQ-004 của Studio đã lạc hậu** (còn mô tả "nhận bản ghi có `user_token` rỗng **hoặc** khớp") → kéo theo `NEW-12`, `NEW-13`, `NEW-15`. — **Fix**: viết lại `NEW-11` theo hành vi mới (**trả rỗng, KHÔNG ghi cột `user_token`**) — xem `TC-COMPATLEGACY001-01/02` §5; cập nhật REQ-004 trên Studio; rà lại `NEW-12`/`NEW-13`/`NEW-15` theo `REG-SPEC-001` (gán `[Giữ nguyên]` / `[Cần sửa]` / `[Hết hiệu lực]`).

- `[BLOCKER]` **3 TC `fail` chưa raise ticket bug, chưa có kết luận** (`bug_tickets` rỗng cả 3):
  - `NEW-13` (`TC-SECISO001-02`, `SEC-ISO-001` **Cao**) — *"mã định danh không tra được + mã thiết bị dùng chung → không được trả dữ liệu của tài khoản khác"*. **Đây là rò rỉ dữ liệu chéo tài khoản** (đúng nội dung #39636). TC tự ghi *"test case quan trọng nhất cần leader/dev xem trước khi đóng ticket"*.
  - `NEW-4` (`TC-OBS001-01`) — không tra được tài khoản → không ghi dữ liệu + **không in token ra log** (`SEC-002` **Cao**).
  - `NEW-25` (`TC-REGSHARED001-01`) — 2 API popup bảo trì vẫn xử lý theo `device_id` không phân biệt tài khoản (**F4, cùng root cause, chưa fix**).
  — **Fix**: chạy lại trên commit cuối. Nếu còn `Không đạt` → **raise ticket Redmine ngay** và điền `Số ticket bug`. `NEW-25` cần **quyết định scope của Leader** (mở rộng ticket #39559 hay tách ticket mới) theo đúng đề nghị ở REQ-011 — không để trạng thái "fail không ticket, không quyết định".

**Nhóm B — GAP coverage**

- `[BLOCKER] GAP-1` **API xoá token (`deleteFirebaseToken`) bị sửa ở vòng 3 nhưng có 0 TC**: vòng 3 thêm chốt chặn `user_token` rỗng cho **cả 3 API** (`getFirebaseToken`, `updateFirebaseToken`, **xoá token**) với lý do *"không xoá mất bản ghi đẩy của tài khoản khớp nhầm"*. Hai API đầu có TC, API xoá **không có TC nào** trong 45 TC. Đây là **F1c Direct impact** và hành vi sai của nó = **mất thông báo của người khác** (đúng triệu chứng ticket). — **Fix**: thêm `TC-DATADB001-04/05/06` (§5) — Normal + Abnormal + Boundary, có kiểm `WHERE` scope trên **2 tài khoản** (RULE-07 / `DATA-DB-001`).

- `[BLOCKER] GAP-3` **`JOB-001` (Cao) — job nền gọi Firebase API: 0 TC về rate limit / retry / backoff / vào=ra**: task sửa dữ liệu đầu vào của job `firebase:subscribe_topic`, và job này gọi **Firebase API bên thứ 3 theo lô**. 4 TC hiện có (NEW-20/21/22/41) chỉ verify "đăng ký đúng chủ". `JOB-001` bắt buộc: tra rate limit hiện hành của FCM (**RULE-05**), tạo lô vượt ngưỡng, job **không dừng**, có retry + backoff, và **số bản ghi vào = số thành công + số vào hàng đợi lỗi**. Rủi ro thật: sau fix **số bản ghi tăng theo số tài khoản dùng chung `device_id`** (Dev ghi 135 tài khoản / 1 ROM build ID) → tải job tăng. — **Fix**: thêm `TC-JOB001-01/02/03` (§5).

- `[BLOCKER] GAP-4` **`ENV-003` + RULE-08 — 0 TC chạy trên production dù task chạm job nền + push Firebase**: 23/45 TC chạy `env=local`, 22 `staging`, `env_tag=local-only` cho 41/45. Chỉ 2 TC có `env_scope` gồm `prd` (NEW-26, NEW-27) và **NEW-27 đang `blocked`** → TC end-to-end 2 tài khoản trên 2 máy thật **chưa từng chạy được**. Catalog D: production **tách 3 job độc lập** (callback / broadcast / scenario) + có **2 server loadbalance**, khác hẳn dev/staging chạy 1 job all. — **Fix**: unblock NEW-27 (ghi rõ lý do blocked là gì: thiếu 2 máy thật? thiếu account? thiếu quyền notify?) + thêm `TC-ENV003-01/02` (§5) chạy **PRODUCTION**.

- `[BLOCKER] GAP-5` **`DATA-COUNT-001` (Cao) — badge/số chưa đọc: 0 TC đối chiếu phép tính tay**: `getFirebaseToken` trả **token + số badge**, và #39636 chính là *"trả token + **số thông báo chưa xem** của NGƯỜI KHÁC"*. 4 TC chạm badge (NEW-10 `skip`, NEW-12 `skip`, NEW-13 `fail`, NEW-15) → **không TC nào chạy được và Đạt**, và không TC nào chuẩn bị bộ dữ liệu biết trước kết quả rồi đối chiếu 4 nguồn (app / API / DB / màn hình). `DATA-COUNT-001` là **lỗi lặp nhiều nhất lịch sử bug — 12 ticket Closed**. — **Fix**: thêm `TC-DATACOUNT001-01/02/03` (§5).

- `[BLOCKER] GAP-8` **T2 — System Notifications (FS-014) High risk: 0 TC**: Dev ghi ở 4.3 *"thông báo hệ thống đẩy lên ứng dụng đi cùng cơ chế mã đẩy/chủ đề bot nên **cùng được khôi phục**"*. 16 TC của T1 đều là thông báo **nghiệp vụ** (chat 1:1, add friend, broadcast, QR, form, booking, lesson, event, item, schedule, conversion, affiliate) — **không TC nào** cho thông báo **hệ thống** (`mobile_notify` `TYPE_SYSTEM` = type 6, sinh khi có `message_error` mới — xem `job/job-spec.md:213-238`). — **Fix**: thêm `TC-FUNC001-19` (§5).

- `[BLOCKER] GAP-9` **F5 — lệnh `recover:user_firebase_token` còn lỗi cùng họ, 0 TC, chưa có quyết định**: Dev tự ghi *"sau fix này, hai tài khoản trên CÙNG một máy vật lý có chung mã đẩy sẽ thành một nhóm trùng và lệnh sẽ **xoá bản ghi của người vừa đăng nhập**"* và *"cố ý KHÔNG sửa … ĐỀ NGHỊ dev/PM quyết"*. Fix này **làm lệnh vận hành trở nên nguy hiểm hơn trước** (trước fix mỗi thiết bị 1 bản ghi nên không thành nhóm trùng). Không TC, không quyết định → rủi ro vận hành xoá mất thông báo hàng loạt. — **Fix**: Leader/PM ra quyết định bằng văn bản (**ngừng dùng lệnh** + chặn ở tài liệu vận hành, hoặc tách ticket viết lại theo `(user, device, os)`), và thêm `TC-REGSHARED001-03` (§5) chứng minh hành vi hiện tại để làm căn cứ quyết định.

- `[BLOCKER] GAP-10` **`getFirebaseToken` bỏ hẳn điều kiện `os` — 0 TC cho trục hệ điều hành sau khi bỏ**: vòng 3 chốt *"BỎ HẲN điều kiện hệ điều hành … truy vấn chỉ còn device_id + user_id, lấy bản mới nhất"*, và tự cảnh báo *"nếu cùng một người dùng có bản ghi trên nhiều hệ điều hành mà device_id lại trùng nhau, truy vấn sẽ **lấy bản mới nhất bất kể hệ điều hành**"*. Trong khi đó `NEW-7` (`TC-TOOLAXIS001-01`) **tạo ra đúng trạng thái đó** (1 user, 1 `device_id`, 2 bản ghi android + ios) — nhưng chỉ verify ở `updateFirebaseToken`, **không** gọi tiếp `getFirebaseToken` để xem trả token của OS nào. `NEW-14` ghi note *"nếu 2 bản ghi khác hệ điều hành thì thứ tự trả về có thể không phải bản mới nhất — ghi nhận là **điểm ngoài phạm vi diff**"* — nhưng vòng 3 đã đưa nó **vào trong** phạm vi diff, nên note này đã lạc hậu. ⚠️ Đúng cấu hình khách: journal QA tái hiện *"đăng nhập account trên **iOS + Android**"*. — **Fix**: thêm `TC-FUNC004-04` + `TC-SYNCAPP001-03` (§5).

### 4.2 Major (nên fix)

- `[MAJOR] SYMPTOM-ONLY` **KH report symptom-only + RC2 chưa được verify → chưa đủ căn cứ đóng ticket cho khách**: KH chỉ nêu hiện tượng; Dev tìm 2 root cause và **chỉ fix RC1** (96% ca). RC2 (19 máy, chủ yếu iOS, app không gọi API) *"KHÔNG sửa được ở phía web"*. Nhưng journal QA tái hiện lại cho **iOS nhận được / Android không nhận** — ngược chiều RC2. — **Fix**: (1) hỏi Dev truy log production cho **đúng `user_id` của `rintaro@ssks.work` / bot 170162** để xác định khách thuộc RC1 hay RC2; (2) nếu RC2 → mở ticket cho **đội app** (kiểm quyền thông báo + bước lấy mã Firebase) và **không đóng #39559** như đã giải quyết triệt để; (3) ghi kết luận vào `01-bug-task.md`.

- `[MAJOR] AP-4` **Không có PR/diff link → không verify được fix shape thực tế**: mục "Commit / Pull Request" của `03-dev-impact.md` chỉ có repo + branch + commit hash (`3233088f19`), **không có URL diff**. Fix shape "specific code check" chỉ được suy ra từ **văn bản Dev tự mô tả**, qua 3 vòng có đảo quyết định. — **Fix**: yêu cầu Dev cung cấp link diff (hoặc `git show 3233088f19`) để Leader xác nhận: `ORDER BY id DESC` có mặt ở đủ 4 query site; guard rỗng có mặt ở đủ 3 API; nhánh dự phòng đã xoá thật.

- `[MAJOR] AP-6 biến thể` **Mục 3 + 4.1/4.2/4.3 của `03-dev-impact.md` là bản vòng 1, không cập nhật sau 2 vòng sửa** (đã diff xác nhận nội dung 3 journal): mục 3 không có dòng nào cho `getFirebaseToken`, **API xoá token**, hay **2 API popup bảo trì**; 4.1 **chỉ list 2 file, không list function nào**. Caller list lạc hậu là nguồn GAP-1 và GAP-10 ở trên. — **Fix**: yêu cầu Dev cập nhật mục 3 + 4.1 theo commit cuối, list **tới mức function**.

- `[MAJOR] AP-3` **Happy-path-only regression cho T1**: 16 TC của T1 (NEW-26→NEW-41) đều có precondition dạng "bật 3 kênh, account chuẩn" — **không TC nào** chạy regression ở **edge state**: friend đã block, bot hết hợp đồng, quota tin đã đầy, `notification_schedule != 0` (gửi gộp theo lịch thay vì realtime — chỉ NEW-41 chạm nhưng expected chỉ 61 ký tự). — **Fix**: thêm ≥1 TC edge state cho T1; ưu tiên **gửi gộp theo lịch** vì `HandlePushMessageNotifyService` batch-mark `status=0 → 1` cho **toàn bộ** `bot_id` (job-spec.md:213-238).

- `[MAJOR] RULE-01` **Quan điểm Cao thiếu loại case, không ghi lý do**: `SEC-ISO-001` (Cao) chỉ có 2 TC **cùng `Abnormal`**, thiếu Normal + Boundary. `CONC-001` (Cao) chỉ có **1 TC Abnormal** (NEW-8) — thiếu Normal + Boundary. `REG-SHARED-001` (Cao) chỉ Abnormal. `JOB-002`/`AUTH-SESSION-001` chỉ Normal + Abnormal. Không TC nào ghi lý do thiếu ở `Ghi chú`. — **Fix**: bổ sung theo §5 hoặc ghi lý do rõ ràng.

- `[MAJOR] CONC-001` **(Cao) — chỉ 1/4 kịch bản bắt buộc**: NEW-8 cover kịch bản (1) "gửi token 2 lần liên tiếp". Thiếu (2) cùng user trên 2 thiết bị song song, (3) 2 user sửa cùng bản ghi gần đồng thời, (4) batch đa luồng chạm giới hạn chung. Đặc biệt: Dev tự nêu *"giữ nguyên cách dọn trùng bằng id lớn nhất … vì **an toàn hơn với 2 request tạo mới song song** — cả hai cùng giữ lại một bản"* → đây là **khẳng định chưa được test**. — **Fix**: `TC-CONC001-02/03` (§5), evidence phải **đếm số bản ghi thực tế** trong DB.

- `[MAJOR] RULE-06 + RULE-07` **Đa số TC dừng ở tầng DB/API, không tới output cuối trên thiết bị thật**: 40/45 TC là `tc_group=api|job|data` với expected dạng "bản ghi có token X, trạng thái topic = 0". RULE-06 đòi verify tới **output cuối trên thiết bị thật**; RULE-07 đòi khớp **3 tầng** DB + màn hình + output. Chỉ NEW-26 (pass, staging) và NEW-27 (**blocked**) đi tới máy thật. Nghĩa là: "bản ghi đúng chủ" đã được chứng minh, nhưng **"máy đúng người nhận được thông báo"** thì gần như chưa. — **Fix**: mỗi impact High (T1/T2/T4) cần ≥1 TC kết thúc bằng **thông báo hiện trên điện thoại thật** (screenshot LINE/Elme app iOS + Android).

- `[MAJOR] RULE-02` **0/45 TC ghi loại evidence bắt buộc; 32 TC `Đạt` không có evidence**: cột `Evidence thực tế` trống toàn bộ (Studio `testcase_list` không trả về), và `Ghi chú` không TC nào ghi *loại* evidence cần thu. RULE-02: chỉ tick Đạt khi đã đính kèm **đúng loại** bằng chứng. — **Fix**: bổ sung loại evidence vào `Ghi chú` từng TC (query DB trước/sau + câu query cho TC data; log job cho TC job; screenshot máy thật cho TC E2E) và đính kèm evidence khi chạy lại.

- `[MAJOR]` **0/45 TC có `Trạng thái đánh giá spec`** (`spec_status=null` toàn bộ): với task này rất nhiều hành vi **spec không ghi** (xem §6) — đặc biệt việc `getFirebaseToken` **trả `success=true` dù không ghi gì** (NEW-4 tự nêu *"điểm cần leader/dev xác nhận"*). Bỏ trống cột này = nguy cơ tự suy diễn rồi cho Đạt. — **Fix**: điền `Spec ghi rõ` / `Spec không ghi` / `Đã hỏi leader` cho từng TC, case `Spec không ghi` phải ghi **đã hỏi ai**.

- `[MAJOR]` **`01-bug-task.md` và `03-dev-impact.md` auto-fill từ Redmine nhưng checkbox "Tester verify auto-fill chính xác" CHƯA tick** (cả 2 file): review chỉ có giá trị sau khi tester đọc lại Redmine #39559 và xác nhận. Với ticket 3 vòng fix + 6 journal, rủi ro sót thông tin cao — và `03` đang cố ý chỉ lấy journal cuối. — **Fix**: tester đọc lại 6 journal + tick 2 checkbox trước khi Leader ký.

- `[MAJOR] PERF-LARGE-001` **Không TC nào test quy mô sau khi mô hình dữ liệu đổi**: D1 ghi *"số bản ghi của bảng sẽ **tăng theo số tài khoản dùng chung** một chuỗi `device_id`"* — Dev dẫn 135 tài khoản dùng chung `BP4A.251205.006`. Dev cũng tự nêu *"chưa kiểm tra được bảng có chỉ mục nào cho `(device_id, os, user_id)` … vì DB dev không chạy"*. Query mới lọc theo `device_id + user_id` + `ORDER BY id DESC` trên bảng sẽ phình. — **Fix**: `TC-PERFLARGE001-01` (§5) + yêu cầu Dev chạy `SHOW INDEX` trên production.

- `[MAJOR]` **9 TC `skip` không ghi lý do skip, gồm chat 1:1 — chính kênh khách báo lỗi**: NEW-10, NEW-12, NEW-28, NEW-32→NEW-37 đều `skip` ở `staging` mà `Ghi chú` không nêu vì sao. NEW-28 (chat 1:1) đặc biệt quan trọng: spec `feature-spec.md:756` (G-04) ghi *"Chat 1:1 realtime: code bỏ qua Firebase push trong `checkAddNotify()`. Cơ chế gửi realtime cho chat 1:1 **chưa trace hết** (có thể nằm ở … Firebase **topic subscription**)"* — tức chat 1:1 realtime đi qua đúng cơ chế topic mà fix này chạm. — **Fix**: chạy NEW-28 bắt buộc; mọi TC `skip` phải ghi lý do + ai duyệt skip.

- `[MAJOR]` **18 TC do `haodtb` viết thiếu `Dữ liệu test/input` và `Kết quả mong đợi` đo lường được**: NEW-28→NEW-45 — 17/18 TC có `data_input = None`, `Kết quả mong đợi` dài ~24-61 ký tự (*"User nhận được thông báo"*, *"Cả 3 kênh đều nhận được thông báo"*), và **0/18 gắn `requirement_keys`** nên không trace được về REQ nào. Nặng nhất là **NEW-45**: tiêu đề *"Xóa xong, close app, open lại app"* nhưng steps chỉ có 3 bước (*"1. Vào lại app / 2. Có sự kiện thông báo notify / 3. Quan sát hiển thị"*) — **không có bước "xóa"**, precondition *"Gỉa lập trường hợp bị ghi đè khi login do trùng device_id"* không nói cách giả lập. 2 người chạy sẽ ra 2 kết quả khác nhau. — **Fix**: bổ sung `Dữ liệu test/input` cụ thể (tên bot, `device_id`, nội dung tin, mốc thời gian), `Kết quả mong đợi` đo lường được (nội dung push `「新しい通知があります。アプリからご確認ください。」` + badge tăng đúng bao nhiêu), gắn `requirement_keys`, và viết lại steps NEW-45.

### 4.3 Minor (có thể fix sau)

- `[MINOR]` **11 mã quan điểm / 14 TC không map được về `framework/checklist-lme.md`** — `/review-tc` không map được coverage: `TOOL-KNOW-002`, `TOOL-OLDREC-001` (×2), `TOOL-ERRHYG-001`, `TOOL-AXIS-001`, `TOOL-NEGCTRL-001`, `OBS-001`, `API-CONTRACT-001`, `AUTH-SESSION-001` (×2), `JOB-002` (×2) là **mã Studio tự đặt**; `RULE-04` và `RULE-09` là **RULE quy trình, không phải quan điểm** (§0.2 checklist-lme: RULE / quan điểm / test case là 3 khái niệm không được lẫn). Gợi ý map: `OBS-001`→`SEC-002`+`OUT-TRUTH-001` · `AUTH-SESSION-001`→`PERM-004` · `JOB-002`→`JOB-001` · `TOOL-AXIS-001`→`DATA-DB-001` · `TOOL-NEGCTRL-001`/`TOOL-KNOW-002`→`SEC-ISO-001`/`DATA-DB-001` · `TOOL-OLDREC-001`/`RULE-09`→`COMPAT-LEGACY-001` · `API-CONTRACT-001`→`FUNC-001` · `RULE-04`→`DATA-DB-001`. — **Fix**: đổi `Mã quan điểm liên kết` sang mã tầng 1, hoặc bổ sung mã mới vào `checklist-lme.md` theo **RULE-10** (có ID + ngày thêm + nguồn bug).

- `[MINOR]` **`catalog = null` ở toàn bộ 45 TC** → không TC nào ghi đã tra **tầng 2**. Task này bắt buộc mở **Catalog D** (khác biệt dev/staging/production + job nền) và **Catalog C** (bản đồ đường gửi tin). Bỏ tầng 2 là nguyên nhân trực tiếp của GAP-4. — **Fix**: điền catalog đã tra cho từng quan điểm ◯.

- `[MINOR]` **`Môi trường test` của 23 TC là `LOCAL` — ngoài enum canonical** (`STAGING` / `DEV` / `PRODUCTION`). — **Fix**: quy ước lại (map `local` → `DEV`) hoặc bổ sung `LOCAL` vào enum ở template; và theo RULE-08 các TC này **không được** dùng để kết luận.

- `[MINOR]` **Studio `test_viewpoint_selection = null`** → không có bảng duyệt quan điểm ◯/× + lý do (**RULE-03**). Không chứng minh được đã duyệt hết tầng 1 từ trên xuống. — **Fix**: dựng bảng §7 F.1 dưới đây trên Studio.

- `[MINOR]` **`NEW-29` có tiêu đề kèm dòng trống thừa** và `NEW-43` có khoảng trắng cuối tiêu đề → lỗi hình thức khi export sang sheet.

### 4.4 Nit (gợi ý)

- `[NIT]` `NEW-3` là TC tái hiện lỗi khách nhưng `Loại case = Abnormal`; xét theo mục đích "reproduce bug KH" có thể tách thêm 1 TC `Normal` mô tả **luồng đúng sau fix** để member dễ thấy trạng thái mong đợi.
- `[NIT]` Gộp `NEW-32`→`NEW-40` (9 TC cùng khuôn "sinh sự kiện X → 3 kênh nhận") thành 1 TC có bảng `Dữ liệu test/input` liệt kê 9 loại sự kiện sẽ gọn hơn và dễ bảo trì; hoặc giữ tách nhưng thêm cột dữ liệu cụ thể cho từng loại.
- `[NIT]` `NEW-24` (RULE-04) nên chạy **sau cùng** trong bộ và ghi rõ câu query dùng để thống kê toàn hệ thống, để dùng lại ở các ticket cùng họ.
- `[NIT]` Theo **RULE-11**, các mục §4 "Quan điểm chưa đủ bằng chứng" của `checklist-lme.md` (FORM-01, CHAT-01, ADM-01/03/04, TPL-01) **không** được dùng để flag BLOCKER/MAJOR. Ở task này `CHAT-01` (tin loại đặc biệt phải đồng bộ từ LINE OA lên tool) có liên quan xa tới NEW-28 — chỉ nêu ở mức gợi ý, **không** tính là GAP.

---

## 5. TCs đề xuất bổ sung

> Member copy thẳng vào `04-tc-list.md` ở round tiếp theo — **16 cột canonical**. `TC No.` đã kiểm **không trùng** TC No. nào đang có ở file 04.
> ⚠️ Bắt buộc chạy trên **commit `3233088f19`** (branch `ai_small_39559`). TC có `Môi trường test = PRODUCTION` là theo **RULE-08** (job nền + push Firebase + bill/domain không kết luận từ staging) — thao tác trên production phải **tránh tạo/xoá data thật**, dùng account test của team.

| TC No. | Mã quan điểm liên kết | Loại case | Tiêu đề test case | Điều kiện tiền đề | Các bước thực hiện | Dữ liệu test/input | Kết quả mong đợi | Kết quả thực thi | Evidence thực tế | Môi trường test | Người thực hiện | Ngày thực hiện | Số ticket bug | Trạng thái đánh giá spec | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-DATADB001-04 | DATA-DB-001 | Normal | deleteFirebaseToken: xoá token của đúng chủ trên máy có device_id dùng chung — bản ghi tài khoản khác nguyên vẹn | - 2 tài khoản A và B đều đã đăng nhập app, cùng gửi lên `device_id = TC39559-BP4A.251205.006`, os = android<br>- Bảng `user_firebase_token` có đúng 2 bản ghi: A (token TC39559-FCM-A-DEL) và B (token TC39559-FCM-B-DEL)<br>- Đã ghi lại `id` + `user_id` + `firebase_token` + `status_subscribe_topic` của cả 2 bản ghi | 1. Query DB ghi lại 2 bản ghi trước khi thao tác (kèm câu query)<br>2. Gọi API xoá token với `user_token` của A + `device_id` trên + os = android<br>3. Query lại toàn bộ bản ghi của `device_id` đó<br>4. Mở app của B trên máy thật, tạo 1 sự kiện sinh thông báo cho bot của B<br>5. Quan sát thông báo trên máy của B | `user_token` = user_token thật của A (60 ký tự); `device_id` = TC39559-BP4A.251205.006; os = android; bản ghi A: TC39559-FCM-A-DEL, bản ghi B: TC39559-FCM-B-DEL | Chỉ bản ghi của A bị xoá. Bản ghi của B **còn nguyên** đúng `id` cũ, `user_id` = B, `firebase_token` = TC39559-FCM-B-DEL, `status_subscribe_topic` không đổi. Máy của B **vẫn nhận được** thông báo đẩy `「新しい通知があります。アプリからご確認ください。」` và badge tăng 1 | Chưa test | | PRODUCTION | | | | Spec không ghi | **Lấp GAP-1** (F1c — API xoá token, 0 TC). Evidence bắt buộc: ảnh query DB trước/sau kèm câu query (2 tài khoản) + screenshot thông báo trên máy thật của B. RULE-07 3 tầng + RULE-06 output cuối. `Spec không ghi`: `web/api-spec.md` không mô tả API xoá token → **đã hỏi**: `<ghi tên Dev/Leader đã confirm>` |
| TC-DATADB001-05 | DATA-DB-001 | Abnormal | deleteFirebaseToken: gọi KHÔNG kèm mã người dùng — không được xoá bản ghi nào (chốt chặn user_token rỗng) | - Trên `device_id` dùng chung có 2 bản ghi của A và B như TC-DATADB001-04<br>- Có ≥1 tài khoản C **chưa từng đăng nhập app** nên cột `user_token` của bảng `users` còn rỗng | 1. Query DB ghi lại 2 bản ghi + đếm tổng số bản ghi của bảng<br>2. Gọi API xoá token **không gửi** `user_token` (thiếu param), kèm `device_id` dùng chung + os<br>3. Kiểm tra phản hồi API<br>4. Query lại: đếm tổng số bản ghi + đối chiếu 2 bản ghi A/B<br>5. Lặp lại bước 2 với `user_token` = chuỗi rỗng `""` | Lần 1: bỏ hẳn param `user_token`; Lần 2: `user_token = ""`; `device_id` = TC39559-BP4A.251205.006; os = android | Cả 2 lần: **không bản ghi nào bị xoá** (tổng số bản ghi không đổi, bản ghi A và B giữ nguyên `id`/`user_id`/token). API trả về theo hợp đồng cũ, **không** 500. Đặc biệt **không** khớp nhầm tài khoản C có `user_token` rỗng (Eloquent dịch `where('user_token', null)` thành `IS NULL`) | Chưa test | | STAGING | | | | Spec không ghi | **Lấp GAP-1** — verify chốt chặn `user_token` rỗng ở API thứ 3 (vòng 3 thêm cho cả get/update/delete, hiện chỉ get+update có TC). Evidence: response API + ảnh query đếm bản ghi trước/sau. Cùng họ với NEW-9 (update) và NEW-13 (get) |
| TC-DATADB001-06 | DATA-DB-001 | Boundary | deleteFirebaseToken: xoá khi chủ có NHIỀU bản ghi trùng trên cùng device_id — dọn hết đúng phạm vi chủ đó | - Tự dựng dữ liệu tồn đọng: tài khoản A có **3** bản ghi trên cùng `device_id` (id nhỏ→lớn: TC39559-FCM-A-1 / -2 / -3), tài khoản B có **1** bản ghi trên cùng `device_id`<br>- Ghi lại `id` của cả 4 bản ghi | 1. Query DB ghi lại 4 bản ghi kèm `id`<br>2. Gọi API xoá token với `user_token` của A + `device_id` + os<br>3. Query lại toàn bộ bản ghi của `device_id` đó<br>4. Gọi API lấy token với `user_token` của A → đối chiếu phản hồi<br>5. Gọi API lấy token với `user_token` của B → đối chiếu phản hồi | 4 bản ghi: A id nhỏ (TC39559-FCM-A-1), A id giữa (TC39559-FCM-A-2), A id lớn (TC39559-FCM-A-3), B (TC39559-FCM-B-1); `device_id` = TC39559-BP4A.251205.006 | **Cả 3** bản ghi của A bị xoá (không còn bản mồ côi nào của A trên `device_id` đó). Bản ghi của B còn nguyên `id` + token TC39559-FCM-B-1. Bước 4 API lấy token của A trả **rỗng**; bước 5 trả đúng TC39559-FCM-B-1 | Chưa test | | STAGING | | | | Spec không ghi | **Lấp GAP-1** — biên số lượng bản ghi (1 / nhiều) cho API xoá, đối xứng với NEW-6 (update) và NEW-14 (get). Evidence: ảnh query 4 bản ghi trước/sau kèm `id` |
| TC-FUNC004-04 | FUNC-004 | Boundary | getFirebaseToken sau khi BỎ điều kiện os: cùng chủ + cùng device_id nhưng 2 hệ điều hành — xác định trả token của bản ghi nào | - Dựng đúng trạng thái mà NEW-7 tạo ra: tài khoản A có **2 bản ghi** trên **cùng** `device_id`, 1 bản os = android (token TC39559-FCM-A-AND, id nhỏ hơn), 1 bản os = ios (token TC39559-FCM-A-IOS, id lớn hơn)<br>- Ghi lại `id` + os + token của cả 2 bản | 1. Query DB ghi lại 2 bản ghi kèm `id`, os, token<br>2. Gọi API lấy token với `user_token` của A + `device_id` đó, **không gửi** os (đúng như app Flutter hiện tại gửi)<br>3. Ghi lại token + badge API trả về, đối chiếu xem thuộc bản android hay ios<br>4. Gọi lại bước 2 nhưng **có gửi** os = android<br>5. Đối chiếu 2 lần gọi | `user_token` của A; `device_id` = TC39559-BP4A.251205.006; bản android = TC39559-FCM-A-AND (id nhỏ), bản ios = TC39559-FCM-A-IOS (id lớn) | Ghi nhận và chốt với Dev **hành vi chuẩn**: theo mô tả vòng 3, API trả token của **bản ghi mới nhất bất kể hệ điều hành** ⇒ trả TC39559-FCM-A-IOS ở **cả** bước 2 và bước 4 (điều kiện os đã bỏ hẳn nên gửi os cũng không lọc). Nếu quan sát thấy khác ⇒ ghi nhận kèm bằng chứng và **chờ Dev xác nhận**, không tự đóng Đạt. ⚠️ Nếu máy android của A đang dùng token TC39559-FCM-A-AND mà API trả token ios ⇒ **máy android không nhận được thông báo** = đúng triệu chứng ticket | Chưa test | | STAGING | | | | Spec không ghi | **Lấp GAP-10** — vòng 3 bỏ hẳn `where os` ở `getFirebaseToken` nhưng 0 TC. Note của NEW-14 ghi đây là "ngoài phạm vi diff" — **đã lạc hậu**, vòng 3 đưa vào trong diff. Evidence: ảnh query 2 bản ghi + response 2 lần gọi. Bắt buộc hỏi Dev trước khi kết luận Đạt/Không đạt |
| TC-SYNCAPP001-03 | SYNC-APP-001 | Normal | Một tài khoản đăng nhập đồng thời trên iOS và Android — cả 2 máy đều nhận được thông báo (đúng cấu hình khách báo lỗi) | - Tài khoản A là chủ bot 170162, đã bật kênh スマートフォンアプリ với tần suất realtime trên màn 通知設定<br>- A đăng nhập app trên **1 máy iOS thật và 1 máy Android thật**, cả 2 đã cho phép quyền nhận thông báo<br>- Bot 170162 có ≥1 friend LINE để tạo sự kiện<br>- Đã ghi lại `device_id` + os + token mà **từng máy** gửi lên (query DB) | 1. Mở app trên cả 2 máy bằng tài khoản A, chọn bot 170162 để app gửi token<br>2. Query DB liệt kê toàn bộ bản ghi token của A, ghi lại `device_id`/os/token/`status_subscribe_topic` từng bản<br>3. Chờ job `firebase:subscribe_topic` xử lý xong, query lại `status_subscribe_topic`<br>4. Dùng friend LINE gửi 1 tin nhắn tới bot 170162<br>5. Quan sát thông báo trên **cả 2 máy** và trên PC | Bot 170162; tần suất realtime; sự kiện chat 1:1; nội dung tin `TC39559-2OS-<mốc thời gian>` | Tồn tại bản ghi **riêng** cho từng máy (khác `device_id` hoặc khác os), không bản nào bị ghi đè. Sau job, `status_subscribe_topic` của cả 2 bản rời khỏi 0. **CẢ 2 máy** (iOS và Android) đều nhận push `「新しい通知があります。アプリからご確認ください。」` và badge tăng 1; PC cũng nhận. Ghi rõ `device_id` thực tế mà máy Android gửi lên (build ID của ROM hay mã máy thật) | Chưa test | | PRODUCTION | | | | Spec ghi rõ | **Lấp GAP-10 + BUG-1 + RULE-06**. Đây là **đúng cấu hình tái hiện của QA trong journal Redmine** ("đăng nhập account trên ios + android", iOS nhận / Android không nhận). Evidence: screenshot thông báo trên **cả 2 máy thật** + ảnh query DB. Ghi lại `device_id` Android để xác nhận RC1 còn xảy ra hay không |
| TC-COMPATLEGACY001-01 | COMPAT-LEGACY-001 | Normal | Bản ghi token đời cũ (user_token rỗng) sau fix cuối — API lấy token trả RỖNG và KHÔNG ghi mã người dùng vào bản ghi | - Tự dựng dữ liệu đời cũ: trên `device_id` riêng cho TC này có đúng 1 bản ghi thuộc A, cột `user_token` của **bảng token** để RỖNG (dữ liệu tạo trước 2022), token TC39559-FCM-A-LEGACY<br>- Chuẩn bị 1 `user_token` **không tra được** trong bảng `users` | 1. Query DB ghi lại bản ghi seed (id, `user_id`, `user_token`, `firebase_token`)<br>2. Gọi API lấy token với `user_token` KHÔNG tra được + `device_id` trên<br>3. Đối chiếu phản hồi API<br>4. Query lại chính bản ghi đó, so từng cột với bước 1 | `device_id` = TC39559-LEGACY-DEVICE-01; `user_token` = TC39559-USERTOKEN-CU-01 (không có trong bảng `users`); bản ghi seed: chủ = A, cột `user_token` rỗng, `firebase_token` = TC39559-FCM-A-LEGACY | API trả **rỗng** (không trả TC39559-FCM-A-LEGACY, không trả badge của A) vì vòng 3 đã **bỏ hẳn** nhánh dự phòng tra qua cột `user_token` của bảng token. Bản ghi seed **KHÔNG thay đổi bất kỳ cột nào** — đặc biệt cột `user_token` **vẫn RỖNG** (đoạn ghi cột này đã bị xoá). App sẽ đăng ký lại token ở lần gọi update kế tiếp | Chưa test | | STAGING | | | | Đã hỏi leader | **THAY THẾ `NEW-11` (`TC-RULE09-01`) — TC cũ kỳ vọng NGƯỢC LẠI ("được bổ sung mã định danh") và đang Đạt sai.** Lấp GAP-2. Theo `REG-SPEC-001`, NEW-11 đánh dấu `[Hết hiệu lực]`, **KHÔNG xoá** (giữ vết). Evidence: response API + ảnh query bản ghi trước/sau chứng minh cột `user_token` vẫn rỗng |
| TC-COMPATLEGACY001-02 | COMPAT-LEGACY-001 | Abnormal | Máy đời cũ không mở lại app — bản ghi trùng cũ vẫn tồn tại nhưng không gây trả sai dữ liệu | - Dựng dữ liệu tồn đọng từ trước fix: trên `device_id` dùng chung có 3 bản ghi — 2 bản đã bị chiếm sai chủ (cột `user_id` đã bị ghi đè sang B) và 1 bản của B<br>- **KHÔNG** gọi API cập nhật token cho các bản này (giả lập máy không mở lại app)<br>- Ghi lại toàn bộ 3 bản ghi | 1. Query DB ghi lại 3 bản ghi kèm `id`, `user_id`, token<br>2. Gọi API lấy token với `user_token` của A (chủ thật đã mất bản ghi)<br>3. Gọi API lấy token với `user_token` của B<br>4. Query lại 3 bản ghi, đối chiếu với bước 1<br>5. Chạy job `firebase:subscribe_topic` trong khoảng giới hạn, đọc log | 3 bản ghi trên `device_id` = TC39559-BP4A.251205.006; A = chủ thật đã bị mất bản ghi; B = tài khoản đang chiếm | Bước 2: API trả **rỗng** cho A (A không còn bản ghi nào của mình) — **không** trả token/badge của B. Bước 3: trả đúng bản ghi mới nhất của B. Bước 4: dữ liệu trùng cũ **vẫn nằm im, không bản nào bị xoá hay đổi chủ** (Dev ghi rõ chỉ dọn khi app gọi lại API đúng cặp user+device+os). Bước 5: job **không** gắn token của bản ghi bị chiếm vào bot của A | Chưa test | | STAGING | | | | Spec không ghi | **Lấp D4 + RULE-09** — Dev nêu ở mục rủi ro số 6 nhưng NEW-5/NEW-22 chỉ test nhánh "chủ thật mở lại app". Đây là nhánh "**không** mở lại app". Evidence: ảnh query 3 bản ghi trước/sau + log job |
| TC-JOB001-01 | JOB-001 | Normal | Job firebase:subscribe_topic — số bản ghi vào = số xử lý thành công + số vào hàng đợi lỗi | - Tạo N = 50 bản ghi token có `status_subscribe_topic = 0`, thuộc ≥5 tài khoản khác nhau, trong đó ≥10 bản dùng chung 1 `device_id`<br>- Ghi lại danh sách `id` của toàn bộ 50 bản ghi<br>- Đã tra tài liệu chính thức mới nhất của Firebase Cloud Messaging để lấy rate limit hiện hành của API subscribe topic (RULE-05) | 1. Query DB đếm và liệt kê 50 bản ghi có cờ = 0 kèm `id`<br>2. Chạy job `firebase:subscribe_topic`, đo thời gian chạy<br>3. Đọc log job trong khoảng thời gian đó<br>4. Query lại `status_subscribe_topic` của cả 50 bản ghi<br>5. Đối chiếu: số bản ghi vào = số rời khỏi cờ 0 + số còn cờ 0 kèm log lỗi | N = 50 bản ghi, ≥5 tài khoản, ≥10 bản dùng chung `device_id` = TC39559-BP4A.251205.006; rate limit FCM: `<ghi số tra được từ tài liệu chính thức + ngày tra>` | **Không bản ghi nào biến mất**: 50 = số bản ghi đăng ký thành công (cờ rời khỏi 0) + số bản ghi thất bại còn cờ 0 **và có dòng log lỗi tương ứng**. Log ghi rõ từng token được đăng ký vào **danh sách bot của đúng chủ bản ghi đó**. Job chạy hết 50 bản, **không dừng giữa chừng** | Chưa test | | PRODUCTION | | | | Spec không ghi | **Lấp GAP-3** (`JOB-001` Cao — 0 TC). RULE-08: job nền phải verify trên production (production tách 3 job độc lập + 2 server loadbalance, khác dev/staging chạy 1 job all — Catalog D). Evidence: log job + bảng đối chiếu số bản ghi vào/ra. RULE-05: ghi rõ nguồn + ngày tra rate limit FCM |
| TC-JOB001-02 | JOB-001 | Abnormal | Job firebase:subscribe_topic khi Firebase API lỗi hoặc vượt rate limit — có retry, backoff, không mất bản ghi | - Có ≥20 bản ghi token cờ `status_subscribe_topic = 0`<br>- Có cách giả lập lỗi từ Firebase: chặn network tới FCM, hoặc dùng token Firebase không hợp lệ cho một phần bản ghi, hoặc đẩy lô vượt rate limit đã tra được<br>- Ghi lại danh sách `id` | 1. Query DB liệt kê bản ghi cờ = 0 kèm `id`<br>2. Giả lập lỗi FCM cho một phần bản ghi (ghi rõ cách giả lập đã dùng)<br>3. Chạy job, đo thời gian<br>4. Đọc log: tìm dấu vết retry và khoảng giãn giữa các lần retry (backoff)<br>5. Query lại cờ của toàn bộ bản ghi<br>6. Bỏ giả lập lỗi, chạy lại job, query lại cờ | 20 bản ghi; cách giả lập lỗi: `<chặn network FCM / token không hợp lệ / vượt rate limit>`; số bản ghi cố tình lỗi = 5 | Job **không dừng và không crash**: 15 bản thành công rời cờ 0, 5 bản lỗi **giữ cờ 0** và có log lỗi riêng từng bản. Log thấy rõ **retry + backoff** (khoảng giãn tăng dần), không retry vô hạn. Sau khi bỏ giả lập lỗi và chạy lại, 5 bản còn lại đăng ký thành công — **không bản nào biến mất** | Chưa test | | STAGING | | | | Spec không ghi | **Lấp GAP-3** — `JOB-001` mục (3)(4). Evidence: log job thấy retry + backoff + bảng đối chiếu vào/ra. Ghi rõ cách giả lập lỗi để người sau lặp lại được |
| TC-JOB001-03 | JOB-001 | Boundary | Job firebase:subscribe_topic ở quy mô device_id dùng chung lớn nhất thực tế (135 tài khoản/1 build ID) | - Tra số liệu thực tế: `device_id` bị dùng chung nhiều nhất hiện có bao nhiêu tài khoản (Dev dẫn `BP4A.251205.006` = 135 tài khoản)<br>- Tạo test data **bằng hoặc lớn hơn** con số đó trên cùng 1 `device_id`, tất cả cờ = 0<br>- Ghi lại tổng số bản ghi + `id` | 1. Query DB xác nhận số bản ghi cờ = 0 trên `device_id` test<br>2. Chạy job, đo thời gian chạy<br>3. Đọc log job<br>4. Query lại cờ của toàn bộ bản ghi<br>5. Với 3 tài khoản mẫu trong lô, gọi API lấy token và đối chiếu trả về đúng token của chính họ | Số tài khoản dùng chung 1 `device_id` = **135** (hoặc số thực tế lớn hơn nếu tra được); `device_id` = TC39559-BP4A.251205.006 | Toàn bộ 135 bản ghi được xử lý, cờ rời khỏi 0, **không bản nào bị bỏ sót hoặc bị xoá**. Thời gian chạy ghi lại cụ thể và không vượt chu kỳ job. 3 tài khoản mẫu đều lấy được **đúng token của mình**, không lẫn của người khác | Chưa test | | STAGING | | | | Spec không ghi | **Lấp GAP-3 + `PERF-LARGE-001`** — sau fix, số bản ghi tăng theo số tài khoản dùng chung `device_id` (D1). Evidence: số liệu quy mô + thời gian chạy + bảng đối chiếu số kết quả. Ghi rõ **nguồn** của con số 135 |
| TC-ENV003-01 | ENV-003 | Normal | Production: push thông báo tới máy thật qua đủ 3 job độc lập + 2 server loadbalance | - Đối chiếu Catalog D trước khi chạy: production tách **3 job độc lập** (callback / broadcast / scenario) + job download media, và có **2 server** loadbalance<br>- Tài khoản test của team trên production (**không** dùng account khách thật), đã bật kênh app realtime<br>- 1 máy thật đã đăng nhập app và cho phép quyền thông báo<br>- Ghi lại router id của server đang phục vụ | 1. Ghi lại router id + `device_id`/os/token mà máy thật gửi lên (query DB production)<br>2. Tạo sự kiện sinh thông báo qua **từng** đường: chat 1:1, broadcast, scenario<br>3. Quan sát thông báo trên máy thật sau **mỗi** đường<br>4. Lặp lại bộ case cơ bản theo **router còn lại** (server thứ 2)<br>5. Đối chiếu log của từng job tương ứng | Account test production của team; bot test; 3 đường sinh thông báo: chat 1:1 / broadcast / scenario; ghi router id từng lần | Máy thật nhận được push `「新しい通知があります。アプリからご確認ください。」` từ **cả 3 đường**, badge tăng đúng số sự kiện. Kết quả **giống nhau trên cả 2 router**. Log của từng job ghi nhận đúng lượt gửi tương ứng | Chưa test | | PRODUCTION | | | | Spec ghi rõ | **Lấp GAP-4** (RULE-08 + `ENV-003` Cao — 41/45 TC `local-only`, 0 TC production). Evidence: screenshot máy thật cho từng đường + log 3 job + ghi router id. ⚠️ Tránh tạo/xoá data thật trên production |
| TC-ENV003-02 | ENV-003 | Abnormal | Production: 2 tài khoản trên 2 máy thật có device_id trùng — mỗi người chỉ nhận thông báo bot của mình (unblock NEW-27) | - **Tiền đề đang chặn NEW-27, phải giải quyết trước**: cần 2 máy Android thật có cùng ROM build ID (cùng model + cùng bản ROM) để `device_id` trùng tự nhiên; nếu không có, ghi rõ lý do và dùng phương án giả lập ở tầng API kèm phiếu xác nhận của Leader<br>- Tài khoản A và B là chủ 2 bot khác nhau, mỗi người đăng nhập app trên 1 máy, đều bật kênh app realtime | 1. Query DB production ghi lại `device_id` mà 2 máy gửi lên, xác nhận **trùng nhau**<br>2. Xác nhận tồn tại 2 bản ghi riêng cho A và B trên cùng `device_id`<br>3. Tạo sự kiện sinh thông báo cho bot của A<br>4. Quan sát **cả 2 máy**<br>5. Tạo sự kiện sinh thông báo cho bot của B<br>6. Quan sát **cả 2 máy** | 2 máy Android cùng ROM build ID; A = chủ bot X, B = chủ bot Y; nội dung sự kiện `TC39559-PRD-A-<mốc>` và `TC39559-PRD-B-<mốc>` | Bước 3-4: **chỉ máy của A** nhận thông báo bot X; máy của B **không** nhận. Bước 5-6: **chỉ máy của B** nhận thông báo bot Y; máy của A **không** nhận. Không máy nào nhận thông báo bot của người kia (đây là hệ quả nặng nhất Dev nêu ở mục 1: token bị gắn vào topic bot của người chiếm) | Chưa test | | PRODUCTION | | | | Spec ghi rõ | **Lấp GAP-4 + BUG-1 end-to-end** — thay thế/unblock NEW-27 (`blocked`, chưa từng chạy). Đây là TC quan trọng nhất chứng minh fix đóng được bug. Evidence: screenshot **cả 2 máy** sau mỗi lượt + ảnh query 2 bản ghi. Nếu vẫn không dựng được env → **phải ghi phiếu xác nhận + phương án giám sát production** thay vì bỏ trống |
| TC-DATACOUNT001-01 | DATA-COUNT-001 | Normal | Badge số thông báo chưa xem — đối chiếu phép tính tay với 4 nguồn (app, API, DB, màn hình) | - Tài khoản A chủ bot 170162, đã đăng nhập app trên máy thật, badge đang ở **0** (đã đọc hết thông báo)<br>- Bảng `mobile_notify` không còn bản ghi chưa xem của A cho bot 170162<br>- Đã ghi lại số badge ban đầu ở cả 4 nguồn | 1. Ghi lại badge ở 4 nguồn: app (số trên icon), API lấy token (trường badge), DB (`mobile_notify` đếm bản ghi chưa xem), màn hình danh sách thông báo trong app<br>2. Tạo **đúng 5** sự kiện sinh thông báo cho bot 170162 (ghi rõ từng sự kiện + mốc thời gian)<br>3. Chờ theo tần suất đã cấu hình<br>4. Đối chiếu lại badge ở **cả 4 nguồn**<br>5. Mở app đọc **2** thông báo<br>6. Đối chiếu lại badge ở cả 4 nguồn | Bot 170162; **5** sự kiện: 1 chat 1:1 + 1 add friend + 1 trả lời form + 1 booking + 1 conversion; **Phép tính tay**: 0 + 5 = **5**; sau khi đọc 2 → 5 − 2 = **3** | Sau bước 4: **cả 4 nguồn** đều bằng **5** (khớp phép tính tay). Sau bước 6: **cả 4 nguồn** đều bằng **3**. Không nguồn nào lệch; không có độ trễ cache ngoài mức spec ghi | Chưa test | | STAGING | | | | Spec không ghi | **Lấp GAP-5** (`DATA-COUNT-001` **Cao**, là lỗi lặp nhiều nhất lịch sử bug — 12 ticket Closed). T5 badge có 4 TC nhưng **0 TC chạy được và Đạt**, **0 TC** đối chiếu phép tính tay. Evidence: bảng đối chiếu 4 nguồn + phép tính tay + screenshot badge trên máy thật |
| TC-DATACOUNT001-02 | DATA-COUNT-001 | Abnormal | Badge trên device_id dùng chung — API KHÔNG được trả số badge của tài khoản khác (đóng #39636) | - Trên `device_id` dùng chung có 2 bản ghi: A và B<br>- A có **5** thông báo chưa xem, B có **0**<br>- Ghi lại số chưa xem của cả 2 từ DB | 1. Query DB ghi lại số thông báo chưa xem của A và của B<br>2. Gọi API lấy token với `user_token` của **B** + `device_id` dùng chung<br>3. Đối chiếu badge trả về với số của B và số của A<br>4. Gọi API lấy token với `user_token` **rác/không tồn tại** + cùng `device_id`<br>5. Đối chiếu phản hồi | `device_id` = TC39559-BP4A.251205.006; A: 5 thông báo chưa xem; B: 0; `user_token` rác = TC39559-USERTOKEN-KHONG-TON-TAI | Bước 3: badge trả về = **0** (số của chính B), **KHÔNG** phải 5 của A. Bước 5: trả **rỗng**, **không** trả badge = 5 và **không** trả token của A. Đây chính là nội dung #39636 (API trả token + số thông báo chưa xem của NGƯỜI KHÁC) | Chưa test | | STAGING | | | | Đã hỏi leader | **Lấp GAP-5 + liên kết `SEC-001`/`SEC-ISO-001`** — bổ sung chiều **badge** cho NEW-13 (hiện `fail`, chỉ nói chung "dữ liệu"). Evidence: ảnh query số chưa xem của 2 tài khoản + response API 2 lần gọi. Nếu badge trả về = 5 ⇒ **rò rỉ dữ liệu, raise ticket ngay** |
| TC-DATACOUNT001-03 | DATA-COUNT-001 | Boundary | Badge tại biên 0 và tại số thông báo lớn — không âm, không tràn, khớp DB | - Tài khoản A chủ bot 170162, badge đang 0<br>- Có thể tạo số lượng lớn thông báo chưa xem (ghi rõ cách tạo)<br>- Ghi lại số ban đầu ở DB và app | 1. Với badge = 0: gọi API lấy token, đối chiếu badge trả về<br>2. Đọc thêm 1 lần nữa khi đã hết thông báo chưa xem, đối chiếu badge<br>3. Tạo **100** thông báo chưa xem cho bot 170162<br>4. Đối chiếu badge ở app + API + DB<br>5. Đọc hết 100 thông báo, đối chiếu lại cả 3 nguồn | Biên dưới: **0** thông báo; biên trên: **100** thông báo; **Phép tính tay**: 0 → 100 → 0 | Bước 1-2: badge = **0**, **không âm**, không lỗi. Bước 4: cả 3 nguồn = **100**, hiển thị trên icon app đúng theo quy ước của OS (nếu OS cắt hiển thị ở 99+ thì API và DB vẫn phải = 100 — ghi rõ điểm khác biệt này). Bước 5: cả 3 nguồn về **0** | Chưa test | | STAGING | | | | Spec không ghi | **Lấp GAP-5 + RULE-01** (`DATA-COUNT-001` Cao cần đủ 3 loại case). Evidence: bảng đối chiếu 3 nguồn tại biên 0 và 100 + screenshot badge. Ghi rõ nguồn của giới hạn nếu spec có quy định số hiển thị tối đa |
| TC-CONC001-02 | CONC-001 | Abnormal | Hai request TẠO MỚI song song cho cùng chủ + cùng thiết bị — chỉ còn 1 bản ghi, không xoá lẫn nhau | - Tài khoản A **chưa có** bản ghi token nào trên `device_id` test (xoá sạch trước khi chạy)<br>- Có công cụ gửi 2 request đồng thời (ghi rõ công cụ đã dùng)<br>- Ghi lại tổng số bản ghi trước khi chạy | 1. Query DB xác nhận A không có bản ghi nào trên `device_id` test<br>2. Gửi **đồng thời** 2 request cập nhật token với cùng `user_token` của A, cùng `device_id`, cùng os, **2 token khác nhau**<br>3. Query DB liệt kê toàn bộ bản ghi của A trên `device_id` đó kèm `id`<br>4. Gọi API lấy token của A, đối chiếu token trả về<br>5. Chạy job đăng ký topic, kiểm `status_subscribe_topic` | `user_token` của A; `device_id` = TC39559-CONC-DEVICE-01; os = android; token 1 = TC39559-FCM-CONC-1, token 2 = TC39559-FCM-CONC-2; công cụ gửi song song: `<ghi rõ>` | Sau 2 request song song: **còn đúng 1 bản ghi** của A trên `device_id` đó (không 0 bản do xoá lẫn nhau, không 2 bản trùng). Token của bản còn lại là **1 trong 2** token vừa gửi và `status_subscribe_topic = 0`. API lấy token trả về **đúng token của bản ghi còn lại**. Job đăng ký topic xử lý được bản ghi đó | Chưa test | | STAGING | | | | Đã hỏi leader | **Lấp `[MAJOR] CONC-001`** — verify **trực tiếp khẳng định của Dev**: *"giữ nguyên cách dọn trùng bằng id lớn nhất vì an toàn hơn với 2 request tạo mới song song — cả hai cùng giữ lại một bản, không xoá lẫn nhau"*. Khẳng định này **chưa từng được test** (NEW-8 chỉ gửi 2 lần liên tiếp, không song song). Evidence bắt buộc: **đếm số bản ghi thực tế trong DB** sau khi cố tình gửi trùng (RULE-02 / `CONC-001`) |
| TC-CONC001-03 | CONC-001 | Abnormal | Đăng xuất và cập nhật token xảy ra gần đồng thời trên cùng bản ghi — không để bản ghi ở trạng thái nửa vời | - Tài khoản A đã có 1 bản ghi token trên `device_id` test, `status_subscribe_topic = 1`<br>- Có công cụ gửi 2 request đồng thời<br>- Ghi lại bản ghi trước khi chạy | 1. Query DB ghi lại bản ghi của A (id, token, cờ)<br>2. Gửi **đồng thời**: 1 request đăng xuất của A và 1 request cập nhật token mới của A (cùng `device_id` + os)<br>3. Query lại bản ghi của A<br>4. Chạy job đăng ký topic, đọc log<br>5. Tạo 1 sự kiện sinh thông báo cho bot của A, quan sát máy thật | `user_token`/session của A; `device_id` = TC39559-CONC-DEVICE-02; os = android; token mới = TC39559-FCM-CONC-3 | Bản ghi kết thúc ở **một trong hai trạng thái nhất quán**, không nửa vời: (a) đã đăng xuất → cờ = 2 và job **hủy** đăng ký topic, máy **không** nhận thông báo; hoặc (b) đã cập nhật token → cờ = 0, token mới, job **đăng ký lại**, máy **nhận** thông báo. **Không** được rơi vào trạng thái cờ = 2 nhưng mang token mới (máy đăng nhập lại mà vẫn bị hủy đăng ký = mất thông báo im lặng) | Chưa test | | STAGING | | | | Spec không ghi | **Lấp `[MAJOR] CONC-001`** kịch bản (2)/(3). Rủi ro thật: logout đặt cờ 2, update đặt cờ 0 — chạy song song có thể để bản ghi mang token mới nhưng cờ 2 ⇒ job không đăng ký lại ⇒ **đúng triệu chứng ticket**. Evidence: ảnh query bản ghi sau race + log job + screenshot máy thật |
| TC-FUNC001-19 | FUNC-001 | Normal | Thông báo HỆ THỐNG (FS-014) đẩy lên app sau fix — máy đúng chủ nhận được | - Tài khoản A chủ bot 170162, bật kênh app realtime, đã đăng nhập app trên máy thật<br>- Chuẩn bị điều kiện sinh thông báo hệ thống: tạo `message_error` mới cho bot 170162 (theo `job/job-spec.md` mục 3.1: khi `countMsgError > appTotalMsgError` thì INSERT `mobile_notify` TYPE_SYSTEM type = 6)<br>- Ghi lại `appTotalMsgError` hiện tại của bot | 1. Query DB ghi lại `appTotalMsgError` và số bản ghi `mobile_notify` type = 6 của bot 170162<br>2. Tạo 1 `message_error` mới cho bot 170162 (ghi rõ cách tạo)<br>3. Chờ job `HandlePushMessageNotifyService` chạy<br>4. Query DB xác nhận có bản ghi `mobile_notify` type = 6 mới<br>5. Quan sát thông báo trên máy thật của A và màn danh sách thông báo trong app | Bot 170162; loại thông báo = **hệ thống** (`TYPE_SYSTEM`, type = 6); cách tạo `message_error`: `<ghi rõ>` | Có **đúng 1** bản ghi `mobile_notify` type = 6 mới cho bot 170162. Máy thật của A nhận push `「新しい通知があります。アプリからご確認ください。」`, badge tăng 1, và thông báo hệ thống hiện trong danh sách thông báo của app. Máy của tài khoản khác trên cùng `device_id` (nếu có) **không** nhận | Chưa test | | STAGING | | | | Spec ghi rõ | **Lấp GAP-8** (T2 — System Notifications FS-014, Dev đánh **High risk** ở 4.3 nhưng **0 TC**). 16 TC của T1 đều là thông báo nghiệp vụ. Nguồn spec: `spec-features/admin/notify-setting/job/job-spec.md` mục 3.1 (dòng 213-238). Evidence: ảnh query `mobile_notify` + screenshot máy thật |
| TC-REGSHARED001-03 | REG-SHARED-001 | Abnormal | Lệnh recover:user_firebase_token sau fix — chứng minh xoá mất bản ghi của người vừa đăng nhập | - **CHỈ chạy trên môi trường test, TUYỆT ĐỐI không chạy lệnh này trên production**<br>- Trên **cùng 1 máy vật lý** (cùng `device_id`, cùng `firebase_token`) có 2 bản ghi: A (đăng nhập trước, id nhỏ) và B (**vừa đăng nhập**, id lớn)<br>- Ghi lại `id` + `user_id` + token của cả 2 bản ghi | 1. Query DB ghi lại 2 bản ghi kèm `id` (xác nhận **cùng** `device_id` và **cùng** `firebase_token`)<br>2. Chạy lệnh `recover:user_firebase_token`<br>3. Query lại toàn bộ bản ghi của `device_id` đó<br>4. Gọi API lấy token với `user_token` của B<br>5. Tạo sự kiện sinh thông báo cho bot của B, quan sát máy thật | 2 bản ghi cùng `device_id` = TC39559-RECOVER-01 và cùng `firebase_token` = TC39559-FCM-SHARED; A id nhỏ, B id lớn (vừa đăng nhập) | **GHI NHẬN kết quả thực tế làm căn cứ quyết định** (không tự tạo bug trước khi Leader/PM quyết): theo mô tả của Dev, lệnh gom theo `(device_id, token)` rồi **xoá bản MỚI nhất** ⇒ bản ghi của **B (người vừa đăng nhập) bị xoá**, bước 4 trả rỗng, máy của B **không nhận** được thông báo. Nếu quan sát đúng như vậy ⇒ đính kèm bằng chứng vào ticket và **chờ quyết định**: ngừng dùng lệnh hoặc tách ticket viết lại theo `(user, device, os)` | Chưa test | | DEV | | | | Đã hỏi leader | **Lấp GAP-9** (F5 — Dev **cố ý không fix**, tự ghi *"ĐỀ NGHỊ dev/PM quyết"*, 0 TC). Fix này khiến lệnh **nguy hiểm hơn trước** (trước fix mỗi thiết bị 1 bản ghi nên không thành nhóm trùng). Evidence: ảnh query 2 bản ghi trước/sau khi chạy lệnh + response API của B. ⚠️ Ghi rõ ở đầu TC: **không chạy trên production** |
| TC-PERFLARGE001-01 | PERF-LARGE-001 | Boundary | Truy vấn token ở quy mô bảng phình sau fix — thời gian phản hồi và index | - Tra số liệu thực tế: tổng số bản ghi `user_firebase_token` hiện tại trên production (Dev ghi bảng ~79KB) và số tài khoản dùng chung `device_id` nhiều nhất (135)<br>- Tạo test data **bằng hoặc lớn hơn** quy mô dự phóng sau fix (mỗi tài khoản 1 bản ghi/thiết bị thay vì dùng chung)<br>- Đã yêu cầu Dev chạy `SHOW INDEX` trên bảng ở môi trường thật và ghi lại kết quả | 1. Ghi lại kết quả `SHOW INDEX` của bảng `user_firebase_token` (do Dev cung cấp)<br>2. Tạo test data ở quy mô dự phóng, ghi lại tổng số bản ghi<br>3. Gọi API lấy token 10 lần, đo thời gian phản hồi từng lần<br>4. Gọi API cập nhật token 10 lần, đo thời gian<br>5. Chạy job đăng ký topic, đo thời gian toàn lô<br>6. Đối chiếu với thời gian đo ở quy mô hiện tại | Quy mô test: `<số bản ghi thực tế tra được, tối thiểu bằng production>`; số tài khoản dùng chung 1 `device_id` = 135; số lần đo = 10/API | Có kết quả `SHOW INDEX` bằng văn bản (xác nhận có/không index cho `(device_id, os, user_id)`). Thời gian phản hồi API lấy/cập nhật token ở quy mô dự phóng **không xấu đi đáng kể** so với quy mô hiện tại (ghi số cụ thể từng lần đo). Job xử lý hết lô trong thời gian **không vượt chu kỳ job** | Chưa test | | STAGING | | | | Spec không ghi | **Lấp `[MAJOR] PERF-LARGE-001`** — Dev tự nêu *"chưa kiểm tra được bảng có chỉ mục nào cho (device_id, os, user_id) vì DB dev không chạy … nếu muốn chắc thì dev kiểm SHOW INDEX trên môi trường thật"*. Query mới lọc theo `device_id + user_id` + `ORDER BY id DESC` trên bảng sẽ phình theo D1. Evidence: kết quả `SHOW INDEX` + bảng số liệu quy mô/thời gian |

---

## 6. Spec update needed

- [ ] Không cần update spec
- [x] **Cần update spec** — chi tiết:

**6.1 — Spec sub-repo thiếu hẳn cơ chế bị sửa (nghiêm trọng nhất)**

- **Section**: [spec-features/admin/notify-setting/](../../spec-features/admin/notify-setting/) — `db/db-mapping.md`, `web/api-spec.md`, `job/job-spec.md`
- **Nội dung cần update**: đã grep toàn bộ 2.262 dòng của 4 file spec notify-setting — **không có bất kỳ dòng nào** về bảng **`user_firebase_token`**, cột `status_subscribe_topic`, job **`firebase:subscribe_topic` / `HandleSubscribeTopic`**, hay 3 API `getFirebaseToken` / `updateFirebaseToken` / xoá token. Spec chỉ mô tả bảng `push_subscriptions` (đó là **Web Push cho PC**, cơ chế khác hẳn). ⇒ **Toàn bộ cơ chế token + topic mà fix này sửa đang KHÔNG có spec** → TC nào cũng phải để `Trạng thái đánh giá spec = Spec không ghi`, và đây là lý do gốc khiến bộ TC phải suy diễn hành vi mong đợi từ mô tả của Dev.
- **Người chịu trách nhiệm**: `<Dev + PM>` — bổ sung mục "Firebase token & topic subscription" vào `db-mapping.md` + `api-spec.md` + `job-spec.md`.

**6.2 — Hành vi mới của `getFirebaseToken` phải được ghi thành spec**

- **Section**: `web/api-spec.md` (mới) + `web/logic-spec.md`
- **Nội dung cần update**: chốt bằng văn bản 3 điểm vòng 3 quyết định, vì cả 3 đều **đảo lại** hành vi trước đó: (1) xác định người dùng **chỉ** từ `users.user_token`, không đoán theo thiết bị; (2) **bỏ hẳn** điều kiện `os` khi tra bản ghi token (kèm hệ quả Dev đã cảnh báo: cùng user + cùng `device_id` + khác OS → trả bản mới nhất bất kể OS); (3) cột `user_firebase_token.user_token` **không còn chỗ nào đọc/ghi** (giữ cột, không đụng schema). Không có spec thì TC không có chuẩn để kết luận Đạt/Không đạt — đúng vấn đề của `NEW-11`.
- **Người chịu trách nhiệm**: `<Dev>`

**6.3 — Hợp đồng phản hồi khi không tra được người dùng (cần quyết định, không chỉ ghi spec)**

- **Section**: `web/api-spec.md`
- **Nội dung cần update**: `NEW-4` chỉ ra API cập nhật token **trả `success = true` dù không ghi gì** khi `user_token` không tra được, và TC tự ghi *"điểm cần leader/dev xác nhận: có nên đổi phản hồi để app biết token chưa được đăng ký hay không"*. Đây là rủi ro `OUT-TRUTH-001` (báo thành công giả) và liên quan trực tiếp RC2 (app không biết mình chưa đăng ký được nên không thử lại). — **Cần quyết định**: giữ hợp đồng cũ (và ghi rõ vào spec) hay đổi để app retry.
- **Người chịu trách nhiệm**: `<Leader + Dev + đội app>`

**6.4 — Sai lệch citation trong Studio requirements (sửa trên Studio)**

| Studio ref | Vấn đề (đã verify trên repo) |
|---|---|
| `spec: admin/notify-setting/feature-spec.md SCR-NS-01` (REQ-010, NEW-26) | **`SCR-NS-01` không tồn tại** ở bất kỳ file nào trong spec sub-repo. ID thật của màn 通知設定 là **`SCR-NTF-01`** |
| `spec: admin/notify-setting/job/job-spec.md:215-237` (REQ-006 — job đăng ký topic) | Dòng 215-237 là **`HandlePushMessageNotifyService`** (job **gửi** push theo lịch), **không phải** `HandleSubscribeTopic` (job **đăng ký** topic). Citation gắn sai job |
| `spec: admin/notify-setting/job/job-spec.md:664` (REQ-010) | Dòng 664 là bảng External API Calls — có nhắc FCM topic push (`topic = TOPIC + botId`), **tạm chấp nhận được** |

**6.5 — Spec gap G-04 đã biết, trùng vùng ảnh hưởng của fix**

- **Section**: `feature-spec.md:756` (bảng gap, mục **G-04**, mức Trung bình)
- **Nội dung**: spec tự ghi *"Chat 1:1 realtime: code bỏ qua Firebase push trong `checkAddNotify()`. Cơ chế gửi realtime cho chat 1:1 chưa trace hết (**có thể nằm ở HandlePostbackTask hoặc Firebase topic subscription**)"*. Fix này sửa đúng vùng **topic subscription**, và TC chat 1:1 (`NEW-28`) đang **`skip`** — trong khi chat 1:1 chính là kênh khách báo lỗi. ⇒ Phải trace xong G-04 **trước khi** đóng ticket.
- **Người chịu trách nhiệm**: `<Dev>` trace + `<QA>` chạy NEW-28

---

## 7. Checklist đã chạy

- [x] **A. Coverage** — chạy đủ A.1→A.6. Fail: A.2 (F1c không TC), A.4 (T2 không TC), A.5 (3 ORPHAN), A.6 (fix-shape 6/8, 0/8 hợp lệ trên commit cuối)
- [x] **B. Chất lượng từng TC** — 27 TC `author=AI` đạt B.1→B.4 (precondition/steps/expected chi tiết, data cụ thể). **18 TC `author=haodtb` fail B.1** (expected không đo lường được, thiếu `Dữ liệu test/input`) và **NEW-45 fail B.1+B.4** (steps không khớp tiêu đề, precondition không dựng được)
- [x] **C. Chất lượng bộ TC** — tỷ lệ `Normal 26 : Abnormal 15 : Boundary 4` = **58/33/9**, lệch khỏi gợi ý 40/35/25; **Boundary quá mỏng (4/45)** cho task đổi mô hình dữ liệu. Phân bố quan điểm dồn: `FUNC-001` chiếm **18/45**. Không có TC trùng lặp thật sự
- [x] **D. Spec alignment** — **FAIL**: xem §6 (spec thiếu hẳn cơ chế bị sửa + 2 citation sai + G-04 chưa trace)
- [x] **E. Hành chính** — file 04 đúng folder review. **FAIL**: `TC No.` do `/new-task` sinh theo mã Studio (14 TC mang mã không thuộc tầng 1); `Người thực hiện`/`Ngày thực hiện` có nhưng **commit hash không được ghi** → đây là gốc của FIX-SHAPE-1; 2 checkbox "Tester verify auto-fill" chưa tick
- [x] **F. Base quan điểm test LME** — bảng F.1 dưới đây
  - [x] F.1 Quan điểm (tầng 1) — **7/18 mã Studio map được**, 11 mã không map (§4.3)
  - [ ] **F.2 Catalog (tầng 2) — FAIL: `catalog = null` ở 45/45 TC.** Task bắt buộc mở **Catalog D** (dev/staging/production + job nền) và **Catalog C** (bản đồ đường gửi tin) — bỏ tầng 2 là nguyên nhân trực tiếp của GAP-4
  - [ ] **F.3 RULE quy trình — FAIL**: RULE-01 (4 quan điểm Cao thiếu loại case) · RULE-02 (0/45 ghi loại evidence) · RULE-03 (không có bảng ◯/×) · RULE-06 (40/45 dừng ở DB/API) · RULE-07 (thiếu tầng "output") · **RULE-08 (0 TC production)** · RULE-09 (NEW-11 expected sai)

### F.1 — Bảng quan điểm đối chiếu

| Mã quan điểm | Ưu tiên | Trigger khớp task? | TC cover (suy luận) | Kết luận |
|---|---|---|---|---|
| `FUNC-001` | Cao | ◯ mọi chức năng | NEW-1, NEW-16, NEW-28→NEW-40, NEW-43, NEW-44 | **RISK** — 9/16 TC nhóm sự kiện đang `skip`; expected không đo lường được |
| `FUNC-004` | Cao | ◯ biên số lượng bản ghi trên 1 thiết bị | NEW-6, NEW-14, NEW-18 | **RISK** — thiếu biên cho **API xoá** (GAP-1) và biên **trục os** (GAP-10) |
| `CONC-001` | Cao | ◯ Dev tự nêu "2 request tạo mới song song" | NEW-8 (1/4 kịch bản) | **RISK** → `[MAJOR]`, bổ sung `TC-CONC001-02/03` |
| `DATA-DB-001` | Cao | ◯ **BẮT BUỘC** — task có UPDATE + DELETE | NEW-2, NEW-3, NEW-17, NEW-21, NEW-23 | **OK** ở update/logout (có kiểm 2 tài khoản), **GAP** ở API xoá |
| `DATA-COUNT-001` | Cao | ◯ **BẮT BUỘC** — API trả số badge | NEW-10 (`skip`), NEW-12 (`skip`), NEW-13 (`fail`), NEW-15 | **GAP** — 0 TC chạy được Đạt, 0 TC phép tính tay → `[BLOCKER] GAP-5` |
| `SEC-ISO-001` | Cao | ◯ nhiều tài khoản trên cùng thiết bị | NEW-12 (`skip`), NEW-13 (`fail`) | **RISK nghiêm trọng** — 2 TC cùng `Abnormal`, cả 2 không có kết quả hợp lệ |
| `SEC-001` | Cao | ◯ PII chéo tài khoản (#39636) | NEW-13 (`fail`) | **RISK** — chỉ 1 TC và đang fail; bổ sung `TC-DATACOUNT001-02` |
| `SEC-002` | Cao | ◯ log không được chứa token | NEW-4 (`fail`) | **RISK** — TC đúng hướng nhưng fail chưa kết luận |
| `OUT-TRUTH-001` | Cao | ◯ API trả `success=true` dù không ghi gì | NEW-4 (`fail`, tự nêu điểm lệch) | **RISK** — cần quyết định spec §6.3 |
| `REG-SHARED-001` | Cao | ◯ **BẮT BUỘC** — sửa file dùng chung, Dev có list caller | NEW-21, NEW-25 (`fail`) | **RISK** — list caller của Dev **lạc hậu** (thiếu getFirebaseToken, API xoá, 2 API popup); lệnh recover chưa có TC |
| `JOB-001` | Cao | ◯ **BẮT BUỘC** — job nền gọi Firebase API theo lô | — (NEW-20/21/22 chỉ verify đúng chủ) | **GAP** → `[BLOCKER] GAP-3` |
| `ENV-003` | Cao | ◯ **BẮT BUỘC** — job nền + push ra thiết bị thật; **không được × vì "staging đã pass"** | — (NEW-26 staging, NEW-27 `blocked`) | **GAP** → `[BLOCKER] GAP-4` |
| `COMPAT-LEGACY-001` | Cao | ◯ bản ghi đời cũ `user_token` rỗng (thêm 2022) | NEW-11 (**expected sai**), NEW-5, NEW-22 | **GAP/SAI** → `[BLOCKER] GAP-2` |
| `SYNC-APP-001` | Trung bình → **Cao** (flow critical user-facing) | ◯ tính năng có mặt cả web và app | NEW-26, NEW-27 (`blocked`), NEW-29 | **RISK** — TC E2E quan trọng nhất đang blocked |
| `PERF-LARGE-001` | Trung bình → **Cao** | ◯ bảng phình theo số tài khoản dùng chung `device_id`; Dev chưa kiểm index | — | **GAP** → `[MAJOR]`, bổ sung `TC-PERFLARGE001-01` |
| `PERM-004` | Trung bình → Cao | ◯ session bị thu hồi (logout) còn thao tác được không | NEW-19, NEW-42 | **OK** (Studio dùng mã `AUTH-SESSION-001`) |
| `PERM-003` | Cao | ◯ topic gắn theo **bot**, badge theo (user, bot) → change bot ảnh hưởng | — | **GAP** → `[MAJOR]` gợi ý: thêm TC đổi bot rồi kiểm topic/badge (chưa đưa vào §5 vì cần Dev confirm phạm vi) |
| `DATA-001` | Cao | ◯ dữ liệu token phản ánh ở app + màn hình + job | NEW-2, NEW-20, NEW-26 | **RISK** — thiếu chiều "màn hình admin" (màn 通知設定 hiển thị gì khi token lỗi?) |
| `UI-001` / `UI-002` / `UI-INPUT-001` | Trung bình | **×** — fix thuần tầng API/job, không đổi UI màn 通知設定 | — | × có lý do (RULE-03) |
| `MSG-001` / `MSG-004` / `BULK-001` | Cao | **×** — không phải chức năng gửi tin cho LINE user, không có bộ lọc đối tượng | — | × có lý do. ⚠️ Lưu ý: `NEW-22` chứng minh **push sai người** — nếu Leader coi đây là "gửi sai đối tượng" thì nâng `MSG-001` lên ◯ |
| `PAY-*` | Cao | **×** — không chạm tiền/gói cước | — | × có lý do |
| `DEPLOY-ASSET-001` / `DEPLOY-LIVE-001` | Cao | **×** — không sửa JS/CSS/asset, không đổi payload API (Dev ghi "không đổi cấu trúc phản hồi") | — | × có lý do. ⚠️ Nếu Dev có đổi response contract theo §6.3 thì `DEPLOY-LIVE-001` thành ◯ |
| `STATE-CLEAN-001` | Cao | **×** — không phải luồng hủy hợp đồng / ngắt kết nối | — | × có lý do |
| `DATA-AUDIT-001` | Cao | **×** — bảng token không có màn lịch sử thao tác | — | × có lý do |
| `INTG-HOOK-001` / `INTG-LINE-001` | Cao | **×** — không nhận webhook trong phạm vi fix (FCM là chiều gửi ra, đã gộp vào `JOB-001`) | — | × có lý do |
| `REG-RUN-001` | Cao | ◯ nhẹ — có bản ghi cờ 0/2 **đang chờ job** lúc release | — | **GAP** → `[NIT]`/`[MAJOR]` tùy Leader: sau khi deploy, các bản ghi cờ 0 tồn từ trước phải được job xử lý đúng, không bỏ sót |
| §4 checklist-lme (FORM-01, CHAT-01, ADM-01/03/04, TPL-01) | — | `CHAT-01` liên quan xa tới NEW-28 | — | **Chỉ `[NIT]`** theo **RULE-11** — không dùng để flag BLOCKER/MAJOR |

---

## 8. Ký duyệt

| Người | Tên | Ngày |
|---|---|---|
| Reviewer (Leader) | | |
| Tester | (đã đọc & hiểu feedback) | |

---

### Điều kiện để chuyển sang Round 2

1. **Chạy lại toàn bộ 45 TC trên commit `3233088f19`**, ghi commit hash vào `Ghi chú` từng TC (FIX-SHAPE-1).
2. Viết lại `NEW-11` theo `TC-COMPATLEGACY001-01`; cập nhật **REQ-004** trên Studio; rà `NEW-12`/`NEW-13`/`NEW-15` theo `REG-SPEC-001` (gán 4 trạng thái).
3. **Raise ticket** cho mọi TC còn `Không đạt` sau khi chạy lại và điền `Số ticket bug`.
4. Bổ sung **20 TC** ở §5 (ưu tiên GAP-1 → GAP-5, GAP-8 → GAP-10).
5. **Quyết định của Leader/PM** cho 2 việc treo: phạm vi 2 API popup bảo trì (`NEW-25` / REQ-011) và lệnh `recover:user_firebase_token` (F5).
6. Unblock `NEW-27` hoặc thay bằng `TC-ENV003-02`, và có **≥1 TC chạy PRODUCTION** (RULE-08).
7. Tester tick 2 checkbox "Tester verify auto-fill chính xác" ở `01-bug-task.md` + `03-dev-impact.md`.
8. Dev cập nhật **mục 3 + 4.1** của `03-dev-impact.md` theo commit cuối (list tới mức function) + cung cấp **link diff**.
