# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | MCP LME TEST STUDIO — task #295 (round 1, branch `m_202609_changebot_fix_old_accesstoken_40563`) |
| Tổng số TC review | 32 |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `11/17 vùng ảnh hưởng đủ TC · 4 GAP · 2 RISK` — ⚠️ nhưng **26/32 TC chưa chạy**, nên 11 vùng "đủ TC" mới chỉ đủ **trên giấy**, chưa có bằng chứng thực thi (xem I1–I3 §4).

> `Input thiếu`: Studio `spec_delta.diffAvailable = false` — không dựng được `git diff` (không tìm thấy ref branch `m_202609_changebot_fix_old_accesstoken_40563`). Chiều (b) suy từ `dev_impact` Studio + mục 1/2/3 của `03-dev-impact.md` + danh sách **9 file** trong phạm vi sửa (lấy từ kết quả chạy TC `NEW-26`, không phải từ diff).

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | `GoogleCalendarStopWatchClient.java` — 1 trong 9 file bị sửa (thêm `readBody` + log response body); bước **dừng theo dõi lịch Google** của channel cũ khi change bot | `diff code` | `NEW-27` (chỉ **đọc mã nguồn**) | `GAP` — 0 TC hành vi. File nằm trong diff nhưng không TC nào thực sự chạy qua bước dừng watch Google Calendar | `[BLOCKER]` |
| G2 | `T3` — Dev tự kê: *"bot mới vẫn nhận webhook (task check connect **không set `is_connected = 2`**)"* | `dev-impact` | `không có` | `GAP` — 0 TC kiểm trạng thái kết nối sau change bot. `NEW-19` chỉ verify địa chỉ webhook + nhận được tin, không bấm 「接続チェック」 và không đọc `is_connected` | `[BLOCKER]` |
| G3 | `F3 — isSameChannelId` (nhánh điều kiện MỚI): guard chỉ so channel **gửi tin**. Case giữ nguyên channel gửi tin nhưng **đổi channel LINE Login** → toàn bộ bước dọn bị bỏ qua, 2 LIFF app trên Login channel cũ còn sót | `diff code` | `NEW-20` (chỉ ghi *"nếu dựng được dữ liệu đó thì quan sát"* ở `note`, không phải bước test) | `GAP` — điều kiện mới chỉ được test 1 nhánh (trùng hoàn toàn), nhánh trùng-một-nửa không có TC | `[BLOCKER]` |
| G4 | `D2` — hành vi production của ảnh richmenu: ảnh **đã sync lên B2 và đã xóa khỏi local** (~1 ngày sau upload) rồi mới change bot → `uploadImage` lên OA mới phải fallback lấy ảnh từ B2 | `dev-impact` | `NEW-5` (chỉ test ảnh **mất hẳn** ở cả local lẫn B2) | `GAP` — không TC nào test nhánh ảnh-chỉ-còn-trên-B2. Kho TCs đã có vùng này (`TC-RM-479` / `TC-RM-480`, regression bug KH #33471) | `[BLOCKER]` |
| G5 | `D3` — hành vi **MỚI** do fix tạo ra: LIFF app cũ của bot bị **xóa thật** (trước fix không xóa) → mọi link LIFF đã phát hành cho friend **trước** change bot (form, 商品, calendar, event, 流入アクション) | `diff code` | `NEW-11` / `NEW-12` / `NEW-13` (chỉ bấm ô richmenu → LIFF **mới**) | `RISK` — chỉ verify phía LIFF mới, không verify hệ quả phía user cuối với link cũ. Spec `SCR-BE-04` đã cảnh báo hệ quả này khi LIFF ID đổi | `[MAJOR]` |
| G6 | `T4` — nhánh job abort: user nhìn thấy gì trên màn 「LOA入れ替え」 khi `getBotInfo` fail 3 lần | `dev-impact` | `NEW-14` (verify DB + log + LIFF, **không** verify UI) | `RISK` — rủi ro *false success*: không TC nào chứng minh màn hình không báo hoàn tất 100% khi job đã abort | `[MAJOR]` |

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `21 quan điểm Trigger khớp task · 9 chưa cover đủ`.

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `INTG-CAL-001` | Cao | `GAP` — 0 TC. `GoogleCalendarStopWatchClient` nằm trong phạm vi sửa mà không TC nào chạy qua 3 hướng đồng bộ lịch (LME→Google / Google→LME / đồng thời) | `[BLOCKER]` |
| Q2 | `LIFF-ENTRY-001` | Cao | `RISK` — thiếu gần hết ma trận bắt buộc: chưa test **user chưa kết bạn OA mới**, chưa test **link cũ đã phát hành**, chưa test điểm vào in-app LINE vs trình duyệt ngoài vs PC | `[MAJOR]` |
| Q3 | `ENV-003` ★ | Cao | `GAP` — **0/32 TC chạy production**; task chạm đủ cả 3 nhóm bắt buộc của RULE-08: **job nền** (ChangeBotJob) · **media** (upload lại ảnh richmenu, server `p.lmes.jp` + B2) · **domain/URL** (ghi đè webhook endpoint). `NEW-25` khai `env_scope = prd` nhưng chưa chạy | `[MAJOR]` |
| Q4 | `STATE-DEP-001` | Cao | `GAP` — quan điểm này chỉ được gán cho `NEW-7`, mà `NEW-7` là TC **đọc mã nguồn** về token, không liên quan hành động đã lên lịch. Không TC nào kiểm broadcast / step / remind **đã đặt lịch trước** change bot rồi đến giờ gửi **sau** change bot | `[BLOCKER]` |
| Q5 | `DATA-REF-001` | Cao | `GAP` — richmenu bị xóa rồi tạo lại với `rich_menu_id` MỚI, nhưng không TC nào kiểm **nơi tham chiếu**: action 「リッチメニュー変更」 trong scenario / autoreply, và thao tác gán richmenu hàng loạt từ màn friend list. `NEW-30` chỉ cover hàng đợi đồng bộ tự động | `[MAJOR]` |
| Q6 | `PERF-LARGE-001` (→ Cao với sync) + `JOB-001` ★ | Cao | `GAP` — chỉ test trần **gói Free = 2 menu** (`NEW-6`). Fix làm mỗi menu phát sinh thêm **1 lệnh DELETE thật** (trước fix chỉ 404 ngay) → bot gói trả phí nhiều richmenu chưa được đo thời gian job và chưa test rate limit LINE API (RULE-05 + JOB-001 mục 1–4) | `[MAJOR]` |
| Q7 | `COMPAT-LEGACY-001` ★ | Cao | `GAP` — richmenu **legacy** (tạo/sửa trước 09/2025) đi qua luồng change bot đã sửa: không TC nào. Kho đã có `TC-RM-482` → **dùng lại**, không viết mới | `[MAJOR]` |
| Q8 | `SEC-002` | Cao | `RISK` — chỉ được cover bởi `NEW-27`, vừa mang **mã lạ** (`OBS-001`) vừa là TC **tĩnh**. Không TC hành vi nào quét log/màn lỗi thật để xác nhận token không lọt ra khi chạy end-to-end | `[MAJOR]` |
| Q9 | `PAY-LIMIT-001` | Cao | `GAP` — spec `EP-08` ghi rõ *"plan_type = 2 (free) → redirect về /admin/home"*, nhưng không TC nào verify bot gói Free bị chặn. Đây đúng là điểm ticket #40563 nghi vấn (khách gói Free vẫn đổi được LOA) | `[MAJOR]` |

> **Đã đủ, không ghi dòng**: `FUNC-001` · `FUNC-004` · `CONC-001` · `DATA-DB-001` · `DATA-MIG-001` · `INTG-LINE-001` · `PERM-003` · `MSG-USER-001` · `OUT-TRUTH-001` · `STATE-001` · `STATE-CLEAN-001` · `REG-SHARED-001` · `REG-RUN-001`.

---

## 3. TC trùng lặp nội dung

**Đã rà 32/32 TC — không phát hiện `DUP-EXACT`, `DUP-SUBSET`, `DUP-INFLATE` hay `DUP-CONFLICT` cần xóa.**

3 cặp có **chồng lấn một phần** đã kiểm theo đủ 4 yếu tố và **đề nghị GIỮ CẢ HAI**:

| Nhóm | Cặp TC | Vì sao KHÔNG phải trùng |
|---|---|---|
| OV-1 | `NEW-1` ↔ `NEW-29` | Cùng kiểm "OA cũ không còn richmenu" nhưng **tiền đề khác**: `NEW-1` bắt buộc friend U **đã được gán** menu R trước change bot (oracle là trạng thái thật của chính friend đó); `NEW-29` chỉ cần 1 máy đã kết bạn OA cũ, trọng tâm là đối chiếu **màn admin vs hiển thị thật**. Xóa bên nào cũng mất 1 oracle. |
| OV-2 | `NEW-8` ↔ `NEW-31` | Cùng kiểm webhook OA cũ đã ngắt, nhưng `NEW-8` dùng friend **mới kết bạn**, `NEW-31` dùng friend **đã có lịch sử chat** + có đối chứng dương trên OA mới. Đây là 2 nhánh lifecycle khác nhau. |
| OV-3 | `NEW-31` ↔ `NEW-32` | Cùng quan điểm `MSG-USER-001` + cùng Normal, nhưng oracle khác hẳn: `NEW-32` kiểm **badge tự cập nhật không reload**, `NEW-31` kiểm nội dung + `last_message` + unread **sau reload**. |

- **Gate đã chạy**: giả định xóa từng TC trong 3 cặp trên → coverage §1 + §2 **mất oracle**, nên không đề nghị xóa TC nào.
- `DUP-INFLATE` che GAP: **không có**.
- `DUP-CONFLICT`: **không có giữa các TC**; các điểm "chờ Dev chốt" nằm trong `note` của TC (`NEW-4`, `NEW-5`, `NEW-20`, `NEW-21`, `NEW-30`) đã được đưa xuống §6.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | Toàn bộ nguồn — 32 TC | **Tỷ lệ pass 6/32 = 19% (< 50%)**. 26 TC chưa chạy, trong đó có `NEW-1` — TC **tái hiện chính bug #40563**. | Chạy hết nhóm TC end-to-end trước khi kết luận fix đạt. Ưu tiên `NEW-1` · `NEW-2` · `NEW-8` · `NEW-11` · `NEW-18`. |
| I2 | `[BLOCKER]` | `NEW-7` · `NEW-17` · `NEW-23` · `NEW-26` · `NEW-27` | **5/6 TC "pass" là đối chiếu mã nguồn tĩnh**, không phải test hành vi. Chính phần `actual` của cả 5 TC đều tự ghi: *"đây là phép kiểm TĨNH trên mã nguồn… không thay thế TC đầu-cuối cần 2 LINE OA thật để xem rich menu/LIFF biến mất trên tài khoản cũ"*. → **Chưa có một bằng chứng thực thi nào chứng minh richmenu bị xóa thật trên OA cũ.** | Không tính 5 TC này vào coverage kết luận. Gắn nhãn rõ nhóm "static gate" khi báo cáo tiến độ. |
| I3 | `[MAJOR]` | `NEW-28` · `NEW-29` | **RULE-02 — kết quả tự khai**: 2 TC manual tick `pass` ở env `local` nhưng `evidence = []` **rỗng** và `actual = null`. Riêng `NEW-29` là TC kết luận *"trên điện thoại kết bạn tài khoản CŨ không còn rich menu nào"* — tức đang kết luận **đúng nội dung bug** mà không có ảnh chụp nào. | Bắt buộc đính screenshot LINE app của cả OA cũ và OA mới rồi mới giữ trạng thái `pass`; nếu không có thì trả về chưa chạy. |
| I4 | `[MAJOR]` | Toàn bộ nguồn | **RULE-08 / ENV-003**: 0 TC chạy production (`envAuto[prd].runs = 0`), trong khi task chạm job nền + media + domain. Riêng nhánh ảnh B2 (G4) **chỉ tồn tại trên production** — local/staging không tái hiện được. | Chốt danh sách TC bắt buộc chạy production (tối thiểu G4, Q6) + phương án bot test thật trên production, hoặc phiếu xác nhận không verify được kèm phương án giám sát. |
| I5 | `[MAJOR]` | 7 TC mang 6 mã quan điểm lạ | `TOOL-KNOW-002` · `TOOL-NEGCTRL-001` (×2) · `TOOL-ERRHYG-001` · `TOOL-OLDREC-001` · `JOB-002` · `OBS-001` **không có trong `framework/checklist-lme.md`** → không map được coverage. Nghiêm trọng nhất: `NEW-1` — TC trọng tâm tái hiện bug — mang mã `TOOL-KNOW-002`. | Gán lại mã chuẩn trên Studio (`NEW-1` → `INTG-LINE-001` hoặc `STATE-CLEAN-001`; `NEW-27` → `SEC-002`; `NEW-15` → `JOB-001`; `NEW-9`/`NEW-20` → `PERM-003`), hoặc bổ sung các mã này vào checklist theo RULE-10. |
| I6 | `[MAJOR]` | `03-dev-impact.md` | File auto-fill `2026-09-11 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" chưa tick** → F/D/T chưa có người xác nhận là đủ. | Tester đọc lại Journal #135941 trên Redmine rồi tick, trước khi report này được dùng để giao việc. |
| I7 | `[MAJOR]` | Studio tab Thông tin | `spec_delta.diffAvailable = false` — *"git diff không chạy được: không tìm thấy ref m_202609_changebot_fix_old_accesstoken_40563"*. Chiều `diff code` của BƯỚC 2 phải suy gián tiếp, **không đối chiếu được diff thật**. | Push/đồng bộ branch lên remote mà Studio đọc được rồi refresh context, để vòng review sau có diff thật. |
| I8 | `[MAJOR]` | `NEW-27` + toàn bộ TC end-to-end | `SEC-002` chỉ được kiểm ở mức tĩnh. `note` của `NEW-27` nói *"khi chạy các TC đầu-cuối, tester nên tìm thêm trong log xem có chuỗi chìa khóa nào lọt ra không"* — nhưng **không TC nào có bước đó trong `steps`**, nên thực tế sẽ không ai làm. | Thêm 1 bước cố định vào `NEW-1` / `NEW-2` / `NEW-18`: sau khi job xong, `grep` log job tìm chuỗi access token / channel secret → expected: không có kết quả. |
| I9 | `[MINOR]` | `NEW-6` | Không atomic: 1 TC gộp 3 giá trị biên (0 menu · `rich_menu_id` rỗng · trần gói Free = 2) **và** chạy trên **2 bot khác nhau** (bot A + bot B). Fail ở bất kỳ nhánh nào cũng cho ra 1 kết quả `Không đạt` chung, khó truy vết. | Tách nhánh "bot B không có richmenu nào" thành TC riêng; 2 biên còn lại cùng vòng lặp thì giữ chung được. |
| I10 | `[NIT]` | `NEW-4` · `NEW-5` · `NEW-20` · `NEW-21` · `NEW-30` | `Kết quả mong đợi` trộn tiêu chí pass với phần "ghi nhận bắt buộc, hỏi Dev". Cách viết này **đúng** (không tự kết luận là bug), nhưng nếu Dev chưa trả lời trước khi chạy thì tester sẽ phải dừng giữa chừng. | Chốt 5 câu hỏi ở §6 **trước** khi giao TC, rồi cập nhật lại `expected` trên Studio. |

> Anti-pattern AP-1 → AP-6: đã rà, **không dính cái nào** (fix có ≥ 3 trigger được test ở `NEW-3`/`NEW-10`/`NEW-12`; bug symptom-only đã có root cause thứ 2 ở `NEW-5`; mục 3 dev-impact có danh sách caller đầy đủ).

---

## 5. TCs đề xuất bổ sung (16)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa004-richmenu-リッチメニュー.md` — nhóm **25. Backup & đổi bot** (18 TC) + **27. Richmenu cũ & recover** (21 TC)<br>`kho-tcs/fa039-changebot-LINE公式アカウント入れ替え機能.md` — **224 TC**, đối chiếu **121 TC vùng logic job** (nhóm 8→14; bỏ qua nhóm UI wizard 1→7 theo yêu cầu) — xem §5b |
| Vùng regression phát hiện từ kho | `TC-RM-479` / `TC-RM-480` (change bot khi ảnh richmenu đã sync B2 / đã xóa local — regression bug KH #33471) · `TC-RM-481` (sau change bot: user OA cũ bị gỡ richmenu) · `TC-RM-482` (change bot với richmenu legacy trước 09/2025) |
| Conflict expected vs kho | **Không** có mâu thuẫn expected. ⚠️ Nhưng phát hiện đáng báo động: `TC-RM-481` đã ghi sẵn expected *"User của bot cũ: KHÔNG còn hiển thị rich menu"* từ trước, **mà bug #40563 vẫn lọt ra production** → đã đưa vào §6 để rà vì sao TC kho không bắt được. |
| GAP dùng lại TC kho (không viết mới) | `Q7` → `TC-RM-482` — *"change bot với rich menu CŨ (tạo trước 09/2025)"*; chỉ cần bổ sung 1 bước: kiểm richmenu legacy trên **OA cũ** đã bị xóa thật chưa. |
| Xác nhận chống trùng | Đã đối chiếu **32 TC** ở BƯỚC 0 + kho `fa004` — **không TC đề xuất nào trùng** theo 4 yếu tố. |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-INTGCAL001-01 | UI | `INTG-CAL-001` | Đổi LOA — Dừng theo dõi lịch Google | Normal | manual | product | Đổi LOA xong thì việc theo dõi lịch Google của tài khoản cũ được dừng, đồng bộ lịch chạy lại bình thường trên tài khoản mới | Bot A đang kết nối OA cũ, có 1 レッスン予約 hoặc サロン予約 đã liên kết Google Calendar và đang đồng bộ 2 chiều (tạo booking trên LME thấy trên Google, sửa trên Google thấy về LME). Có quyền mở Google Calendar của tài khoản đã liên kết. Có sẵn OA mới chưa dùng cho bot nào. | 1. Trước khi đổi, tạo 1 booking trên LME → xác nhận sự kiện xuất hiện trên Google Calendar<br>2. Sửa giờ sự kiện đó trên Google Calendar → xác nhận LME cập nhật theo<br>3. Chạy đổi LOA cho bot A từ màn 「LOA入れ替え」 tới 100%<br>4. Mở log job, tìm dòng gọi dừng theo dõi lịch Google, đọc mã trạng thái và nội dung phản hồi<br>5. Sửa tiếp 1 sự kiện trên Google Calendar và quan sát màn đặt lịch của bot A<br>6. Tạo 1 booking mới trên LME sau khi đổi → kiểm tra Google Calendar | 1 lesson/salon calendar đã liên kết Google Calendar, 1 booking trước khi đổi + 1 booking sau khi đổi | Log có dòng dừng theo dõi lịch Google kèm mã trạng thái thành công và nội dung phản hồi (không còn dòng trống không rõ nguyên nhân). Sau khi đổi LOA, job chạy hết 6 bước, kết thúc hoàn tất 100%. Sự kiện sửa trên Google Calendar không còn đẩy ngược về bot theo kênh theo dõi cũ. Booking tạo mới sau khi đổi vẫn đồng bộ đúng sang Google Calendar. |  | Lấp `G1` · `Q1` · Đánh giá spec: Spec không ghi — cần Dev xác nhận hành vi mong đợi của bước dừng theo dõi · RULE-08 job + API bên thứ 3 → product · Evidence: log job + ảnh Google Calendar + ảnh màn đặt lịch |
| TC-INTGCAL001-02 | UI | `INTG-CAL-001` | Đổi LOA — Dừng theo dõi lịch Google | Abnormal | manual | Tất cả | Google trả lỗi khi dừng theo dõi lịch: chỉ ghi cảnh báo, việc đổi LOA vẫn hoàn tất | Bot A có yêu cầu đổi LOA đang chờ và có calendar liên kết Google. Có thể đưa kết nối tới Google vào trạng thái lỗi trước khi job chạy (thu hồi quyền của tài khoản Google đã liên kết, hoặc chặn đường ra tới máy chủ Google trên máy chạy job). | 1. Đưa kết nối Google vào trạng thái lỗi<br>2. Cho job xử lý yêu cầu đổi LOA<br>3. Mở log, tìm dòng gọi dừng theo dõi lịch Google<br>4. Kiểm tra trạng thái kết thúc của yêu cầu đổi<br>5. Mở màn 「リッチメニュー」 và dùng điện thoại kết bạn OA mới kiểm tra menu | Thu hồi quyền tài khoản Google đã liên kết trước khi job chạy | Log ghi cảnh báo dừng theo dõi lịch Google thất bại, **có kèm mã trạng thái và nội dung phản hồi** (đây chính là phần log vừa được bổ sung). Job **không dừng**, vẫn chạy hết 6 bước và kết thúc hoàn tất 100%. Richmenu vẫn được dựng lại đủ trên OA mới và friend OA mới vẫn thấy menu. |  | Lấp `G1` · `Q1` · Đánh giá spec: Spec không ghi · Không có khái niệm biên cho nhóm này → không viết case Boundary (RULE-01) · Evidence: log job |
| TC-REGURL001-01 | UI | `REG-URL-001` | Đổi LOA — Trạng thái kết nối sau khi đổi | Normal | manual | Tất cả | Sau khi đổi LOA, 「接続チェック」 báo kết nối bình thường và trạng thái kết nối không bị hạ về lỗi webhook | Bot A vừa đổi LOA xong, tiến trình 100%. Biết cách xem trạng thái kết nối của bot trên màn 「接続設定」. Job kiểm tra kết nối định kỳ đang bật. | 1. Mở màn 「接続設定」 của bot A sau khi đổi<br>2. Bấm nút 「接続チェック」 và đọc nguyên văn thông báo hiện ra<br>3. Ghi lại trạng thái kết nối đang hiển thị của bot A<br>4. Chờ qua ít nhất 1 chu kỳ của job kiểm tra kết nối định kỳ<br>5. Tải lại màn danh sách tài khoản và màn 「接続設定」, đọc lại trạng thái kết nối<br>6. Từ 1 friend của OA mới gửi 1 tin nhắn, kiểm tra tin vào đúng màn chat 1:1 của bot A | Tin nhắn thử: `test connect sau doi LOA 40563` | Bấm 「接続チェック」 hiện thông báo kết nối bình thường 「正常に接続しています。」. Trạng thái kết nối của bot A là **đã kết nối**, KHÔNG rơi vào trạng thái báo sai webhook. Sau khi job kiểm tra kết nối định kỳ chạy, trạng thái vẫn giữ nguyên là đã kết nối. Tin nhắn từ friend OA mới vào đúng chat 1:1 của bot A. |  | Lấp `G2` · Đánh giá spec: Spec ghi rõ — `spec-features/admin/bot-edit` SCR-BE-01 (4 giá trị trạng thái kết nối) · Evidence: ảnh thông báo 接続チェック + ảnh trạng thái trước/sau chu kỳ job |
| TC-STATECLEAN001-01 | UI | `STATE-CLEAN-001` | Đổi LOA — Guard trùng tài khoản | Abnormal | manual | Tất cả | Giữ nguyên tài khoản gửi tin nhưng đổi tài khoản đăng nhập: ghi nhận LIFF app trên tài khoản đăng nhập cũ có được dọn không | Bot A đang chạy trên tài khoản gửi tin X và tài khoản đăng nhập L1, có đủ 2 LIFF app trên L1. Chuẩn bị tài khoản đăng nhập L2 khác L1. Tạo yêu cầu đổi LOA với tài khoản gửi tin mới **vẫn là X** nhưng tài khoản đăng nhập mới là **L2**. Có quyền xem danh sách LIFF app của cả L1 và L2 trên LINE Developers. | 1. Ghi lại mã 2 LIFF app của bot A trên L1 và số app hiện có trên L2<br>2. Tạo yêu cầu đổi LOA giữ nguyên tài khoản gửi tin X, đổi tài khoản đăng nhập sang L2<br>3. Cho job xử lý và theo dõi log, tìm dòng báo trùng tài khoản và các dòng xóa LIFF app<br>4. Đếm số LIFF app của bot A còn trên L1 và trên L2<br>5. Kiểm tra webhook của tài khoản X trên LINE Developers<br>6. Dùng điện thoại đã kết bạn X, bấm ô richmenu mở form và ô mở đặt lịch | Tài khoản gửi tin mới = tài khoản gửi tin cũ (X); tài khoản đăng nhập mới L2 ≠ L1 | Yêu cầu đổi kết thúc hoàn tất 100%. Webhook của tài khoản X **không bị ghi đè** (vẫn là địa chỉ kèm mã bot). Richmenu vẫn xóa và tạo lại được bằng chìa khóa của chính X, friend vẫn thấy menu. Ô mở form và ô mở đặt lịch vẫn mở được bằng LIFF app mới trên L2. **Ghi nhận bắt buộc**: số LIFF app của bot A còn sót trên tài khoản đăng nhập cũ L1 — tester chụp màn hình danh sách app của L1 và gửi Dev xác nhận đây có phải hành vi mong đợi không, KHÔNG tự kết luận là lỗi. |  | Lấp `G3` · Đánh giá spec: Đã hỏi leader — chờ Dev chốt ở §6 mục 3 · Mở rộng đúng điểm `NEW-20` chỉ ghi trong note mà không có bước test · Evidence: ảnh danh sách LIFF app của L1 + L2 trước/sau + log job |
| TC-LIFFENTRY001-01 | UI | `LIFF-ENTRY-001` | Đổi LOA — Link LIFF đã phát hành trước khi đổi | Normal | manual | Tất cả | Link form / đặt lịch / sản phẩm đã gửi cho friend TRƯỚC khi đổi LOA: xác định còn mở được hay không sau khi đổi | Bot A đang chạy trên OA cũ. Trước khi đổi, đã tạo và gửi cho friend U (qua tin nhắn 1:1) đủ 3 loại link: 1 link フォーム, 1 link レッスン予約 hoặc サロン予約, 1 link 商品. Friend U đã kết bạn OA cũ và **cũng sẽ kết bạn OA mới**. Chuẩn bị OA mới chưa dùng. | 1. Trước khi đổi, friend U bấm lần lượt cả 3 link trong lịch sử chat → xác nhận cả 3 mở được bình thường<br>2. Chụp lại URL của cả 3 link<br>3. Chạy đổi LOA cho bot A tới 100%<br>4. Friend U bấm lại đúng 3 link cũ trong lịch sử chat (không dùng link mới)<br>5. Ghi lại chính xác màn hình hiện ra cho từng link (mở được / báo lỗi / trắng màn / lỗi LIFF)<br>6. Mở màn quản trị lấy link mới của cùng 3 đối tượng đó rồi gửi lại cho friend U, bấm thử | 3 link đã phát hành trước khi đổi: フォーム · 予約 · 商品 | Link **mới** lấy từ màn quản trị sau khi đổi: cả 3 đều mở được bình thường trên app LINE và hoàn tất được luồng (gửi được câu trả lời form / đặt được lịch / xem được sản phẩm). Với 3 link **cũ**: tester ghi lại chính xác hành vi thực tế của từng link và đối chiếu với cảnh báo trong spec SCR-BE-04 (LIFF ID đổi thì các form, sản phẩm, calendar, event, 流入アクション đã tạo có thể không dùng được) — không màn nào được trắng trang hoặc treo vô hạn. |  | Lấp `G5` · `Q2` · Đánh giá spec: Spec ghi rõ — `spec-features/admin/bot-edit` SCR-BE-04 (cảnh báo LIFF ID thay đổi) · Đây là hành vi MỚI do fix xóa thật LIFF app cũ · Evidence: ảnh in-app LINE của từng link trước và sau khi đổi |
| TC-LIFFENTRY001-02 | UI | `LIFF-ENTRY-001` | Đổi LOA — Link LIFF đã phát hành trước khi đổi | Abnormal | manual | Tất cả | Sau khi đổi LOA, user CHƯA kết bạn OA mới bấm link form thì được đưa sang màn kết bạn, không rơi vào lỗi LIFF | Bot A vừa đổi LOA xong. Có 1 điện thoại LINE **chưa kết bạn** với OA mới. Đã lấy link form mới của bot A từ màn quản trị. | 1. Trên điện thoại chưa kết bạn OA mới, mở link form vừa lấy trong app LINE<br>2. Quan sát màn hình hiện ra<br>3. Thực hiện kết bạn OA mới rồi mở lại đúng link đó<br>4. Điền và gửi form<br>5. Mở link đó trên trình duyệt ngoài (không phải in-app LINE) và quan sát<br>6. Kiểm tra câu trả lời trên màn quản trị của bot A | 1 tài khoản LINE chưa kết bạn OA mới | Khi chưa kết bạn: được chuyển sang màn kết bạn OA mới, KHÔNG hiện lỗi LIFF và không trắng màn. Sau khi kết bạn, mở lại link thì vào thẳng form, gửi được câu trả lời và câu trả lời hiển thị đúng trên màn quản trị, gắn đúng friend vừa kết bạn. Mở trên trình duyệt ngoài: hiển thị đúng theo hành vi chuẩn của link, không lỗi. |  | Lấp `Q2` · Đánh giá spec: Spec ghi rõ — LIFF-ENTRY-001 ma trận trạng thái user + điểm vào · Evidence: ảnh màn kết bạn + ảnh in-app + ảnh trình duyệt ngoài |
| TC-MEDIAIMG001-01 | UI | `MEDIA-IMG-001` | Đổi LOA — Ảnh richmenu trên production | Normal | manual | product | Đổi LOA khi ảnh richmenu đã sync lên B2 và đã xóa khỏi local: ảnh vẫn được đẩy lại đúng lên tài khoản mới | Trên production, bot A có richmenu R1 với ảnh **đã sync lên B2 và đã xóa khỏi server local** (xác minh: link ảnh local trả về không tồn tại, link B2 truy cập được) và richmenu R2 có ảnh còn ở local. Có điện thoại kết bạn OA cũ và 1 điện thoại sẽ kết bạn OA mới. | 1. Xác nhận link ảnh local của R1 trả về not found, link B2 của R1 truy cập được<br>2. Ghi lại ảnh hiện tại của R1 và R2 trên màn 「リッチメニュー」<br>3. Chạy đổi LOA cho bot A tới 100%<br>4. Đọc log, đếm số dòng tải ảnh thành công và tìm dòng cảnh báo không tìm thấy ảnh (nếu có)<br>5. Mở màn 「リッチメニュー」 đối chiếu ảnh của R1 và R2<br>6. Kết bạn OA mới trên điện thoại, mở phòng chat và đối chiếu ảnh richmenu hiển thị<br>7. Trên điện thoại kết bạn OA cũ, kiểm tra vùng richmenu | R1: ảnh chỉ còn trên B2 · R2: ảnh còn ở local | Cả R1 và R2 đều có dòng tải ảnh thành công trong log; **không** có dòng cảnh báo bỏ qua richmenu vì không tìm thấy ảnh. Màn 「リッチメニュー」 hiển thị đúng ảnh của cả R1 và R2, vùng tap giữ nguyên. Trên điện thoại kết bạn OA mới, richmenu hiển thị đúng ảnh và đúng bố cục, bấm các ô chạy đúng action. Trên điện thoại kết bạn OA cũ, không còn richmenu nào. |  | Lấp `G4` · regression — dẫn từ `TC-RM-479` / `TC-RM-480` (bug KH #33471) · Đánh giá spec: Spec ghi rõ — Catalog D (production lưu media tạm ~1 ngày rồi sync B2) · RULE-08 media → product · Evidence: ảnh màn richmenu + ảnh LINE app + log tải ảnh |
| TC-UI003-01 | UI | `UI-003` | Đổi LOA — Màn hình khi job dừng giữa chừng | Abnormal | manual | Tất cả | Job dừng vì không lấy được thông tin tài khoản mới: màn hình báo lỗi rõ, KHÔNG báo hoàn tất | Bot A có yêu cầu đổi LOA vừa được đặt từ màn 「LOA入れ替え」. Tài khoản gửi tin mới ở trạng thái không trả được thông tin tài khoản (tắt Messaging API của tài khoản mới). Ghi lại tên hiển thị, ảnh và mã LINE hiện tại của bot A. | 1. Đặt lệnh đổi LOA và **ở lại màn hình** theo dõi thanh tiến trình<br>2. Để job chạy tới khi dừng vì không lấy được thông tin tài khoản mới<br>3. Đọc và chụp lại: thanh tiến trình dừng ở mốc nào, thông báo hiện ra là gì<br>4. Tải lại màn 「LOA入れ替え」 và đọc trạng thái hiển thị<br>5. Mở màn danh sách tài khoản, đối chiếu tên, ảnh, mã LINE của bot A<br>6. Thử đặt lại lệnh đổi LOA lần nữa và quan sát màn hình có cho thao tác không | Tài khoản gửi tin mới bị tắt Messaging API | Thanh tiến trình **KHÔNG** chạy tới 100% và màn hình **KHÔNG** hiện thông báo hoàn tất. Màn hình hiển thị trạng thái lỗi rõ ràng cho người dùng biết việc đổi đã thất bại (không trắng màn, không quay vô hạn). Sau khi tải lại, màn hình vẫn phản ánh đúng trạng thái lỗi chứ không hiện như đang xử lý bình thường. Tên hiển thị, ảnh và mã LINE của bot A giữ nguyên giá trị cũ. |  | Lấp `G6` · Đánh giá spec: Spec không ghi — cần Dev/PM chốt thông điệp hiển thị khi job abort (§6 mục 6) · Bổ sung đúng phần `NEW-14` bỏ trống (chỉ verify DB + log) · Evidence: ảnh màn hình lúc job dừng + sau khi reload |
| TC-STATEDEP001-01 | UI | `STATE-DEP-001` | Đổi LOA — Tin đã đặt lịch trước khi đổi | Normal | manual | Tất cả | Tin nhắn đã đặt lịch trước khi đổi LOA, đến giờ gửi sau khi đổi thì gửi qua tài khoản mới | Bot A đang chạy trên OA cũ. Đặt trước 1 メッセージ配信 hẹn giờ gửi sau khoảng 30 phút cho toàn bộ friend, nội dung nhận biết được. Friend U đã kết bạn OA cũ, friend V sẽ kết bạn OA mới sau khi đổi. Chuẩn bị OA mới. | 1. Đặt lịch gửi tin với nội dung 「SCHEDULED_BEFORE_CHANGE_40563」, ghi lại giờ gửi dự kiến<br>2. Chạy đổi LOA cho bot A và chờ hoàn tất 100% **trước** giờ gửi<br>3. Kết bạn OA mới bằng điện thoại của friend V<br>4. Chờ tới giờ gửi đã đặt<br>5. Kiểm tra điện thoại friend V (OA mới) xem có nhận được tin không<br>6. Kiểm tra điện thoại friend U ở phòng chat OA cũ xem có nhận được tin không<br>7. Mở màn lịch sử gửi tin của bot A, đọc trạng thái và số lượng đã gửi | 1 tin đặt lịch nội dung 「SCHEDULED_BEFORE_CHANGE_40563」 | Đến giờ đã đặt, tin được gửi đi bình thường **qua tài khoản LINE MỚI**: friend V của OA mới nhận được tin 「SCHEDULED_BEFORE_CHANGE_40563」. Friend U **không** nhận được tin này ở phòng chat OA cũ. Màn lịch sử gửi tin hiển thị trạng thái đã gửi xong với số lượng khớp số friend của OA mới, không có bản ghi lỗi gửi. |  | Lấp `Q4` · regression — vùng gửi tin sau khi đổi channel · Đánh giá spec: Spec không ghi hành vi của tin đã đặt lịch khi đổi LOA → đưa §6 mục 7 · Evidence: ảnh LINE app của cả 2 friend + ảnh màn lịch sử gửi tin |
| TC-DATAREF001-01 | UI | `DATA-REF-001` | Đổi LOA — Nơi tham chiếu richmenu | Normal | manual | Tất cả | Action 「リッチメニュー変更」 trong kịch bản / trả lời tự động vẫn đổi đúng richmenu sau khi đổi LOA | Bot A có richmenu R1 và R2. Có 1 シナリオ hoặc 自動応答 chứa action đổi richmenu sang R2, kích hoạt được bằng 1 từ khóa. Trước khi đổi, đã xác nhận friend gõ từ khóa thì richmenu đổi sang R2. Chuẩn bị OA mới và 1 điện thoại sẽ kết bạn OA mới. | 1. Trước khi đổi, friend gõ từ khóa trên OA cũ → xác nhận richmenu đổi sang R2<br>2. Ghi lại mã richmenu của R1 và R2 đang lưu<br>3. Chạy đổi LOA cho bot A tới 100%<br>4. Ghi lại mã richmenu MỚI của R1 và R2<br>5. Kết bạn OA mới bằng điện thoại thử, xác nhận đang hiển thị richmenu mặc định<br>6. Gõ đúng từ khóa đó trên OA mới<br>7. Quan sát richmenu hiển thị trên điện thoại và kiểm tra màn 「リッチメニュー」 | Từ khóa kích hoạt action đổi richmenu sang R2 | Sau khi đổi LOA, mã richmenu của R1 và R2 đều đã đổi sang giá trị mới. Friend của OA mới gõ từ khóa thì richmenu trên điện thoại **đổi đúng sang R2** (đúng ảnh, đúng bố cục), không bị giữ nguyên menu cũ và không mất richmenu. Action không báo lỗi ở lịch sử trả lời tự động / lịch sử kịch bản. |  | Lấp `Q5` · regression — nơi tham chiếu `rich_menu_id` ngoài luồng đổi LOA · Đánh giá spec: Spec không ghi · Evidence: ảnh LINE app trước/sau khi gõ từ khóa + mã richmenu ghi lại 2 thời điểm |
| TC-BULK001-01 | UI | `BULK-001` | Đổi LOA — Gán richmenu hàng loạt sau khi đổi | Normal | manual | Tất cả | Lọc friend rồi gán richmenu hàng loạt sau khi đổi LOA: friend nhận đúng richmenu mới | Bot A vừa đổi LOA xong, tiến trình 100%. OA mới đã có ít nhất 3 friend, trong đó 2 friend gắn cùng 1 tag để lọc. Bot A có richmenu R2 đang không hiển thị cho ai. | 1. Mở màn 友だちリスト, lọc theo tag đã chuẩn bị → xác nhận đúng 2 friend<br>2. Chọn toàn bộ kết quả lọc và thực hiện thao tác gán richmenu R2 cho nhóm<br>3. Đọc thông báo kết quả hiển thị trên màn hình<br>4. Chờ job đồng bộ richmenu xử lý xong, đọc log các dòng gán richmenu<br>5. Trên điện thoại của 2 friend thuộc tag, mở phòng chat và xem richmenu<br>6. Trên điện thoại của friend KHÔNG thuộc tag, kiểm tra richmenu đang hiển thị | Tag lọc ra đúng 2 friend · richmenu R2 | Thông báo trên màn hình báo gán thành công đúng 2 friend. Log job đồng bộ dùng **mã richmenu mới** của R2 (không dùng mã cũ do OA cũ cấp) và không có dòng lỗi 404. Trên điện thoại của 2 friend thuộc tag, richmenu hiển thị đúng là R2 với đúng ảnh và bấm được các ô. Friend không thuộc tag vẫn giữ richmenu cũ của mình, không bị đổi theo. |  | Lấp `Q5` · regression — thao tác hàng loạt sau khi `rich_menu_id` đổi · Đánh giá spec: Spec không ghi · Evidence: ảnh màn friend list sau thao tác + ảnh LINE app của 3 friend + log job đồng bộ |
| TC-PERFLARGE001-01 | UI | `PERF-LARGE-001` | Đổi LOA — Bot nhiều richmenu | Boundary | manual | product | Bot gói trả phí có nhiều richmenu: đổi LOA xử lý đủ tất cả menu, không lỗi giới hạn tần suất gọi LINE | Hỏi vận hành số richmenu của khách hàng đang có nhiều richmenu nhất, dựng bot A với số richmenu **bằng hoặc lớn hơn** số đó (tối thiểu 10), mỗi menu đều đã đẩy lên LINE và có ảnh. Có điện thoại kết bạn OA cũ và điện thoại sẽ kết bạn OA mới. Ghi lại số richmenu N và mã richmenu của từng menu. | 1. Ghi lại N và toàn bộ mã richmenu hiện tại<br>2. Chạy đổi LOA cho bot A, ghi lại thời điểm bắt đầu<br>3. Theo dõi tới khi tiến trình đạt 100%, ghi lại thời điểm kết thúc và tính tổng thời gian<br>4. Đếm trong log: số dòng xóa richmenu, số dòng tạo richmenu, số dòng tải ảnh, số dòng đặt alias<br>5. Tìm trong log các dòng báo lỗi vượt giới hạn tần suất gọi LINE hoặc lỗi tạm thời của LINE<br>6. Mở màn 「リッチメニュー」 đếm số menu và kiểm tra tất cả đều có mã mới<br>7. Trên điện thoại kết bạn OA mới, kiểm tra richmenu hiển thị; trên điện thoại kết bạn OA cũ, kiểm tra vùng richmenu | N richmenu (N ≥ 10, lấy theo khách lớn nhất) | Log có đúng N dòng xóa richmenu thành công, N dòng tạo richmenu thành công, N dòng tải ảnh thành công. **Không** có dòng lỗi vượt giới hạn tần suất gọi LINE; nếu có thì phải thấy job thử lại và cuối cùng vẫn thành công, không menu nào bị bỏ sót. Yêu cầu đổi kết thúc hoàn tất 100%. Màn 「リッチメニュー」 hiển thị đủ N menu và tất cả đều mang mã richmenu mới. Friend OA mới thấy đúng richmenu; friend OA cũ không còn richmenu nào. Ghi lại tổng thời gian chạy job để đối chiếu ngưỡng vận hành. |  | Lấp `Q6` · Đánh giá spec: Spec không ghi ngưỡng thời gian — cần vận hành cung cấp số richmenu của khách lớn nhất · RULE-05 tra rate limit LINE mới nhất trước khi chạy · RULE-08 performance + job → product · Evidence: số N + tổng thời gian chạy + bảng đếm số dòng log từng loại |
| TC-PAYLIMIT001-01 | UI | `PAY-LIMIT-001` | Đổi LOA — Giới hạn theo gói | Abnormal | manual | Tất cả | Bot gói Free không vào được màn 「LOA入れ替え」 | Có bot F đang ở **gói Free** và bot P đang ở **gói trả phí**, cùng 1 tài khoản admin quản lý. Biết đường dẫn màn 「LOA入れ替え」 của cả 2 bot. | 1. Chọn bot F (gói Free), mở màn 「接続設定」 và quan sát nút 「LINE公式アカウント入れ替え」 có hiện không<br>2. Nhập thẳng đường dẫn màn 「LOA入れ替え」 của bot F vào thanh địa chỉ trình duyệt<br>3. Ghi lại màn hình đích và đường dẫn cuối cùng<br>4. Lặp lại bước 2 với bot P (gói trả phí)<br>5. So sánh hành vi của 2 bot | Bot F: gói Free · Bot P: gói trả phí | Với bot F (gói Free): truy cập thẳng đường dẫn màn 「LOA入れ替え」 bị **chuyển hướng về màn chủ của admin**, không vào được màn đổi LOA và không đặt được lệnh đổi. Với bot P (gói trả phí): vào được màn 「LOA入れ替え」 bình thường. |  | Lấp `Q9` · Đánh giá spec: Spec ghi rõ — `spec-features/admin/bot-edit` EP-08 (`plan_type = 2` → redirect `/admin/home`) · Liên quan trực tiếp nghi vấn ở ticket: khách gói Free vẫn đổi được LOA → §6 mục 8 · Evidence: ảnh màn hình + đường dẫn cuối cùng của cả 2 bot |

| TC-ENV003-01 | UI | `ENV-003` | Đổi LOA — Domain webhook theo môi trường | Normal | manual | product | Webhook ghi đè lên tài khoản cũ trỏ đúng domain của môi trường đang chạy | Có quyền chạy đổi LOA trên staging và production. Biết domain nhận sự kiện mong đợi của từng môi trường. Bot A trên mỗi môi trường đang có webhook trỏ về hệ thống LME kèm mã bot. Có quyền xem cấu hình webhook của tài khoản cũ và mới trên LINE Developers. | 1. Trên staging: ghi lại webhook hiện tại của tài khoản cũ, chạy đổi LOA tới 100%<br>2. Đọc lại webhook của tài khoản CŨ và của tài khoản MỚI trên LINE Developers, ghi cả 2 giá trị<br>3. Lặp lại toàn bộ trên production với bot khác<br>4. Đối chiếu 4 giá trị thu được với domain mong đợi của từng môi trường<br>5. Kiểm tra endpoint của 2 LIFF app mới trên tài khoản đăng nhập mới ở production<br>6. Từ 1 friend gửi tin vào tài khoản MỚI trên production, xác nhận tin vào đúng chat 1:1 | Bot test trên staging + bot test trên production | Webhook của tài khoản **CŨ** sau khi bị ngắt trỏ về địa chỉ nhận sự kiện chung **đúng domain của chính môi trường đang chạy**, không kèm mã bot và **không lẫn domain của môi trường khác**. Webhook của tài khoản **MỚI** trỏ về địa chỉ kèm mã bot, cũng đúng domain môi trường đó. Endpoint của 2 LIFF app mới trên production dùng domain production. Không giá trị nào còn chứa domain cũ sau khi đổi. Tin nhắn vào tài khoản mới trên production vào đúng chat 1:1. |  | Lấp `Q3` · dẫn từ `TC-CB-221` (kho chỉ kiểm domain của webhook/LIFF **mới**; TC này thêm nhánh webhook **tài khoản cũ bị ghi đè** — code mới của fix) · Đánh giá spec: Spec không ghi · RULE-08 domain + job → product · Evidence: 4 ảnh cấu hình webhook trên LINE Developers (cũ/mới × staging/production) |
| TC-DATAID001-01 | Data | `DATA-ID-001` | Đổi LOA — Bản sao lưu thông tin tài khoản cũ | Normal | manual | Tất cả | Thông tin tài khoản cũ trong bản ghi yêu cầu đổi được chụp lại lúc đặt lệnh và không đổi sau khi dữ liệu bot bị ghi đè | Bot A đang chạy trên tài khoản gửi tin X và tài khoản đăng nhập L1. Có quyền xem bản ghi yêu cầu đổi LOA. Chuẩn bị tài khoản gửi tin Y và tài khoản đăng nhập L2 để đổi sang. | 1. Trước khi đặt lệnh, ghi lại thông tin kết nối hiện tại của bot A (tài khoản gửi tin X và tài khoản đăng nhập L1)<br>2. Đi hết wizard 「LOA入れ替え」 nhưng **chưa** thực thi, mở bản ghi yêu cầu đổi vừa sinh<br>3. Đối chiếu 4 thông tin tài khoản CŨ trong bản ghi yêu cầu đổi với giá trị ghi ở bước 1<br>4. Thực thi đổi LOA và chờ hoàn tất 100%<br>5. Mở lại bản ghi yêu cầu đổi, đọc lại đúng 4 thông tin tài khoản cũ đó<br>6. Mở màn 「接続設定」 của bot A, xác nhận thông tin kết nối hiện tại đã là tài khoản MỚI | Tài khoản cũ X + L1, tài khoản mới Y + L2 | Ở bước 3, bản ghi yêu cầu đổi chứa **đúng** 4 thông tin của tài khoản CŨ (tài khoản gửi tin X và tài khoản đăng nhập L1), khớp giá trị ghi lại trước khi đặt lệnh. Sau khi job chạy xong, 4 thông tin tài khoản cũ trong bản ghi yêu cầu đổi **giữ nguyên không đổi**, trong khi màn 「接続設定」 của bot A đã hiển thị tài khoản MỚI. Đây là bản chụp bất biến để lần chạy lại vẫn lấy đúng chìa khóa tài khoản cũ. |  | Lấp `G3` · bổ sung hành vi thật cho `NEW-23` (hiện chỉ đối chiếu mã nguồn) · dẫn từ `TC-CB-137` (kho chỉ kiểm có sinh bản ghi hàng đợi; TC này kiểm **nội dung 4 cột snapshot** — bất biến cốt lõi của bản fix) · Đánh giá spec: Spec không ghi · Evidence: ảnh bản ghi yêu cầu đổi tại 2 thời điểm + ảnh màn 接続設定 |
| TC-JOB001-01 | UI | `JOB-001` | Đổi LOA — Nhiều yêu cầu xếp hàng cùng lúc | Boundary | manual | product | Ba bot cùng đổi LOA trong một đợt: mỗi bot dọn đúng tài nguyên của mình, không lẫn chìa khóa giữa các bot | Có 3 bot A, B, C thuộc cùng 1 tài khoản admin, mỗi bot đang chạy trên 1 tài khoản LINE riêng và mỗi bot có ít nhất 1 richmenu đã đẩy lên LINE + 2 LIFF app. Chuẩn bị 3 tài khoản LINE mới tương ứng. Có quyền xem log job và hàng đợi yêu cầu đổi. | 1. Ghi lại mã richmenu, mã LIFF app và webhook của cả 3 bot<br>2. Đặt lệnh đổi LOA cho cả 3 bot trong vòng 5 phút<br>3. Đếm số bản ghi đang chờ trong hàng đợi yêu cầu đổi<br>4. Theo dõi log job cho tới khi cả 3 yêu cầu xử lý xong, ghi lại thời gian xử lý từng bản ghi<br>5. Với từng bot, đối chiếu các dòng xóa richmenu và xóa LIFF app trong log có đúng mã của chính bot đó không<br>6. Kiểm tra webhook của 3 tài khoản cũ và 3 tài khoản mới trên LINE Developers<br>7. Trên điện thoại đã kết bạn từng tài khoản cũ, kiểm tra vùng richmenu | 3 bot đổi LOA gần như đồng thời | Cả 3 bản ghi trong hàng đợi đều được xử lý xong, **không bản ghi nào bị bỏ sót** và không bản ghi nào kẹt ở trạng thái đang xử lý. Trong log, mã richmenu và mã LIFF app bị xóa của mỗi bot **chỉ thuộc về chính bot đó** — không có mã của bot khác xuất hiện trong lượt xử lý của một bot. Webhook của cả 3 tài khoản cũ đều bị ngắt, webhook của cả 3 tài khoản mới đều kèm đúng mã bot tương ứng. Friend của cả 3 tài khoản cũ đều không còn thấy richmenu. Job không treo, log không có lỗi chưa xử lý. |  | Lấp `Q6` · dẫn từ `TC-CB-222` (kho kiểm hàng đợi không sót bản ghi; TC này thêm chiều **không lẫn chìa khóa/tài nguyên giữa các bot** — rủi ro riêng của bản fix vì mỗi lượt xử lý 2 token) · Đánh giá spec: Spec không ghi · RULE-08 job → product · Evidence: log job tách theo từng bot + bảng đối chiếu mã tài nguyên trước/sau |

**Dòng §1 / §2 KHÔNG đề xuất TC mới — kèm lý do:**

| Mã | Lý do không viết TC mới |
|---|---|
| `Q7` `COMPAT-LEGACY-001` | Kho đã có `TC-RM-482` (*"change bot với rich menu CŨ tạo trước 09/2025"*) cover đúng vùng này. Chỉ cần **bổ sung 1 bước** vào TC kho: sau change bot, kiểm richmenu legacy trên **OA cũ** đã bị xóa thật chưa (TC kho hiện chỉ kiểm phía OA mới). |
| `Q8` `SEC-002` | Không đẻ TC riêng — đúng hơn là **bổ sung 1 bước quét log** vào 3 TC end-to-end đã có (`NEW-1`, `NEW-2`, `NEW-18`), đã ghi thành issue `I8` ở §4. |
| `Q3` `ENV-003` | Không phải thiếu TC mà là thiếu **lần chạy trên production** của chính bộ TC hiện có. Việc phải làm là chốt danh sách TC chạy production (`I4`), không phải viết thêm TC. |

---

## 5b. Đối chiếu kho `fa039-changebot` — vùng LOGIC JOB

> Phạm vi so: **121/224 TC** của kho thuộc nhóm 8→14 (tiến trình & hoàn tất · đặt lịch · xóa data bot cũ web + app · setting giữ nguyên · phân quyền · hồi quy). **Bỏ qua** nhóm 1→7 (wizard UI: menu, campaign, chọn phương thức, nhập thông tin, webhook setting, xác nhận, QR) theo yêu cầu.
> Đối chiếu với **36 TC Studio task #295** + **16 TC §5**. Theo BƯỚC 5a: TC kho đã đủ → **dẫn chiếu ID**, KHÔNG viết lại.

### A. Kho đã có TC, vùng fix CHẠM → đưa vào phạm vi test ticket này (6 TC)

| ID kho | Quan điểm | Tên case | Vì sao liên quan fix #40563 | Cần chỉnh gì cho ticket này |
|---|---|---|---|---|
| `TC-CB-137` | `DATA-DB-001` | Sinh bản ghi hàng đợi cho job dọn dữ liệu ngay khi bấm thực hiện | Bản ghi này chính là nơi fix lấy **snapshot chìa khóa tài khoản cũ**. Không có bản ghi đúng thì toàn bộ logic 2 token sụp | Thêm bước đọc 4 cột thông tin tài khoản cũ → đã tách thành `TC-DATAID001-01` ở §5 |
| `TC-CB-122` | `STATE-001` | Bot mới chỉ-lưu-credentials — chưa kết nối thật, bot cũ vẫn chạy | Xác lập tiền đề: trước khi job chạy, chìa khóa mới **chưa** ghi vào bản ghi bot → giải thích vì sao phải đọc snapshot chứ không đọc bản ghi bot | Dùng nguyên |
| `TC-CB-132` | `INTG-LINE-001` | Thực hiện từ đặt lịch cũ mà LOA mới đã bị xóa / đổi secret → báo lỗi, không thực hiện | Fix chạm `generateAccessToken`. `NEW-3` chỉ test **secret CŨ sai**; đây là nhánh **secret MỚI sai** — chưa TC nào trong task #295 chạm | Dùng nguyên. ⏳ kho ghi `QA-dev-008` chưa chốt re-validate có bắt buộc không |
| `TC-CB-106` | `CONC-001` | Bot bị khóa trong lúc đổi LOA — thao tác ghi khác bị chặn | `NEW-22` chỉ test 2 luồng **job** tranh nhau; đây là tranh chấp giữa **job và người dùng** đang thao tác | Dùng nguyên. ⏳ kho ghi `QA-dev-006` chưa chốt danh sách thao tác bị chặn |
| `TC-CB-108` | `STATE-001` | Đóng tab / trình duyệt khi đang đổi LOA → tiến trình vẫn hoàn tất | Chứng minh job chạy độc lập với trình duyệt — điều kiện để mọi TC job của task #295 có giá trị | Dùng nguyên |
| `TC-CB-117` | `PERM-001` | Tài khoản Staff giữ nguyên quyền truy cập sau khi đổi LOA | Fix đổi cách ghi bản ghi bot (`getBotInfo` abort, projection mới) → rủi ro chạm quyền Staff gắn theo bot | Dùng nguyên |

### B. Kho đã có TC, vùng fix chạm GIÁN TIẾP (dữ liệu do job dọn) → nên chạy kèm (8 TC)

| ID kho | Quan điểm | Tên case | Ghi chú |
|---|---|---|---|
| `TC-CB-146` | `DATA-COUNT-001` | Rich Menu — lịch sử click và bộ đếm friend đang theo richmenu về 0 | `NEW-30` chỉ cover hàng đợi đồng bộ, không cover bộ đếm/lịch sử |
| `TC-CB-164` | `LIFF-ENTRY-001` | Link QR và ảnh QR cũ sau khi đổi không còn dẫn về LOA cũ | `NEW-34` (đã push) cover form/đặt lịch/商品 nhưng **thiếu QR** |
| `TC-CB-155` | `INTG-SHEET-001` | Liên kết Google bị mất, phải liên kết lại rồi sync vào spreadsheet MỚI | Cùng vùng với `TC-INTGCAL001-01` (§5). ⚠️ kho ghi **MT-09 chưa chốt** — 2 nguồn nói khác nhau |
| `TC-CB-152` | `STATE-DEP-001` | Biểu mẫu — remind đã đặt cho friend LOA cũ bị hủy | ⚠️ **MT-07 chưa chốt**: `event_step_time` xóa hay không xóa |
| `TC-CB-168` | `STATE-DEP-001` | Đặt lịch bài học — nhắc lịch của friend LOA cũ bị hủy | Cùng MT-07 |
| `TC-CB-171` | `STATE-DEP-001` | Gửi nhắc lịch — friend LOA cũ bị xóa khỏi danh sách đang phát hành | Cùng MT-07. Bổ trợ `TC-STATEDEP001-01` (§5) ở chiều ngược lại (hủy vs vẫn gửi) |
| `TC-CB-173` | `PAY-BATCH-001` | Sản phẩm định kỳ — friend LOA cũ KHÔNG bị trừ tiền ở chu kỳ sau | **Bill tiền** — RULE-08 bắt buộc production. Rủi ro cao nhất trong nhóm dọn dữ liệu |
| `TC-CB-150` | `STATE-DEP-001` | Scenario đang chạy dở của friend LOA cũ dừng hẳn, không gửi tiếp | Bổ trợ `TC-STATEDEP001-01` |

### C. Kho có TC nhưng fix KHÔNG chạm code → Leader quyết có chạy regression rộng không (~35 TC)

Toàn bộ phần còn lại của nhóm **10. Xóa data bot cũ — web** và **11. app mobile** — dọn dữ liệu của các tính năng không nằm trong 9 file bị sửa:
`TC-CB-138` `139` `141` `142` `143` `144` `145` `148` `149` `151` `154` `156` `157` `158` `159` `160` `161` `162` `163` `165` `166` `167` `169` `170` `172` `174` `175` `176` `177` `178` `179` · app mobile `TC-CB-180`→`186`.

Theo nguyên tắc *chỉ test layer bị fix chạm*, nhóm này **không** được đưa vào phạm vi ticket #40563. Nêu ra để Leader biết kho đã cover sẵn — nếu muốn chạy full regression đổi LOA thì lấy thẳng từ kho, không cần viết mới.

### D. Vùng kho đã được bộ TC hiện có phủ — không cần bổ sung

| ID kho | Được phủ bởi |
|---|---|
| `TC-CB-147` (richmenu trên LINE của friend LOA cũ bị clear) | `NEW-1` — TC trọng tâm tái hiện bug |
| `TC-CB-153` (link form cũ LIFF ID cũ không mở được) | `NEW-34` = `TC-LIFFENTRY001-01` (đã push Studio) |
| `TC-CB-189` (richmenu cũ gán cho friend mới) | `NEW-36` = `TC-DATAREF001-01` (đã push) + `NEW-30` |
| `TC-CB-191` (danh sách ảnh richmenu giữ nguyên) | `TC-MEDIAIMG001-01` (§5) |
| `TC-CB-194` `195` (biểu mẫu cũ/mới sau khi đổi) | `NEW-34` + `NEW-35` (đã push) |
| `TC-CB-197` `198` (salon/lesson + đồng bộ Google) | `TC-INTGCAL001-01` (§5) |
| `TC-CB-110` (tiến trình fail → không báo thành công giả) | `TC-UI003-01` (§5) |
| `TC-CB-219` (dữ liệu tạo trước release đọc đúng sau khi thêm cột) | `NEW-26` |
| `TC-CB-205` (đổi LOA ở cả 3 gói) + `TC-CB-207` (Staff không quyền bị chặn URL) | `TC-PAYLIMIT001-01` (§5) — một phần |

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | `spec-features/admin/rich-menu` — job đồng bộ richmenu | `UpdateRichMenuThread.java:248` gọi xóa richmenu bằng token của **bot hiện tại**, trong khi mã richmenu cũ do OA cũ cấp — **cùng đúng pattern lỗi của #40563 nhưng nằm ngoài phạm vi fix**. Cần chốt: có fix nốt không, hay chấp nhận lỗi 404 vô hại vì luồng đổi LOA đã xóa thật từ trước. | Kết quả chạy `NEW-7` (phần "Ghi nhận thêm") + `note` của `NEW-30` | Dev |
| 2 | `spec-features/admin/bot-edit` — luồng đổi LOA | Sau fix, nếu **xóa richmenu cũ thành công nhưng tạo lại trên OA mới thất bại** thì `rich_menu_id` trong dữ liệu vẫn trỏ tới menu **đã bị xóa khỏi LINE**: màn admin còn hiển thị menu mà friend không thấy. Rủi ro **MỚI do chính bản fix tạo ra**, chưa có trong đánh giá của Dev. Cần chốt hành vi mong đợi (rollback? đánh dấu lỗi? xóa bản ghi?). | `note` của `NEW-4` | Dev + PM |
| 3 | `spec-features/admin/bot-edit` — guard trùng tài khoản | `isSameChannelId` chỉ so channel **gửi tin**. Nếu khách giữ nguyên channel gửi tin nhưng đổi channel **LINE Login** thì toàn bộ bước dọn bị bỏ qua → 2 LIFF app còn sót trên Login channel cũ. Cần chốt đây là hành vi mong đợi hay thiếu sót. | `note` của `NEW-20` + TC đề xuất `TC-STATECLEAN001-01` | Dev |
| 4 | `spec-features/admin/rich-menu` — richmenu thiếu ảnh | Hàm giải quyết file ảnh chạy **trước** bước xóa, không tìm thấy ảnh thì bỏ qua cả vòng lặp → lệnh xóa không bao giờ được gọi, richmenu đó **còn sót trên OA cũ**. Đây là root cause thứ 2 của cùng triệu chứng #40563 nhưng chưa được fix. Cần chốt có nằm trong phạm vi ticket không. | `note` của `NEW-5` | Dev + Leader |
| 5 | Tài liệu vận hành job đổi LOA | Yêu cầu đổi bị kẹt ở trạng thái **đang xử lý** khi job dừng giữa chừng, **không có cơ chế tự phục hồi** — phải admin can thiệp tay đổi trạng thái về chờ. Cần ghi thành quy trình khôi phục chính thức. | `note` của `NEW-21` | Dev + vận hành |
| 6 | `spec-features/admin/bot-edit` — SCR-BE-03 | Spec chưa mô tả màn 「LOA入れ替え」 hiển thị gì khi job **abort** (`getBotInfo` fail 3 lần): thông điệp lỗi, trạng thái thanh tiến trình, có cho đặt lại lệnh không. | TC đề xuất `TC-UI003-01` (`G6`) | PM + Dev |
| 7 | `spec-features/admin/message-send-all` + SCR-BE-03 | Spec chưa mô tả điều gì xảy ra với **tin nhắn đã đặt lịch / step / remind** đang chờ gửi khi bot đổi LOA giữa chừng. | TC đề xuất `TC-STATEDEP001-01` (`Q4`) | PM + Dev |
| 8 | `spec-features/admin/bot-edit` — EP-08 | Spec ghi `plan_type = 2 (free)` → redirect `/admin/home`, **nhưng khách hàng ở ticket #40563 đang ở gói Free mà vẫn đổi được LOA** (Journal #135030 + #135032: CS xác nhận gói Free, khách khẳng định đã dùng 「LINE公式アカウント入れ替え機能」 thành công ngày 03/09). Cần làm rõ: từng có giai đoạn mở miễn phí? hay tồn tại đường vòng qua được chốt chặn? | Journal Redmine #135030 / #135032 vs spec EP-08 | Dev + CS + PM |
| 9 | Quy trình regression richmenu (RULE-10 / RULE-12) | Kho TCs **đã có sẵn** `TC-RM-481` với expected đúng *"User của bot cũ: KHÔNG còn hiển thị rich menu"* (môi trường PRODUCTION), vậy mà bug #40563 vẫn lọt ra khách hàng. Cần rà: TC này chưa từng được chạy, hay đã chạy mà chỉ kiểm phía OA mới. | `kho-tcs/fa004-richmenu-リッチメニュー.md` `TC-RM-481` vs bug #40563 | Leader |
| 10 | Phạm vi dữ liệu tồn đọng (RULE-04) | Bot đã đổi LOA **trước** ngày triển khai fix vẫn còn richmenu mồ côi trên OA cũ; fix không tự dọn. `NEW-25` mới chỉ yêu cầu lập bảng thống kê — cần chốt quy trình dọn tay và thời hạn. | `NEW-25` / `REQ-014` + log production 404 (Journal #135570) | Dev + vận hành |
| 11 | **CONFLICT expected — friend của LOA cũ còn hay bị xóa** | `NEW-31` (Studio) giả định hội thoại của friend U **vẫn còn** sau change bot: expected là *"không tạo hội thoại mới, không cập nhật last message/thời gian và không tăng unread"* — tức vẫn tìm thấy friend U để đối chiếu. Kho `TC-CB-140` lại ghi *"DB `conversation` + `bot_line_user`: bản ghi của friend cũ **bị xóa**; màn chat 1:1 KHÔNG hiển thị friend nào của LOA cũ"*. Hai expected **không thể cùng đúng** — nếu friend cũ bị xóa sạch thì bước 6 của `NEW-31` vô nghĩa. | `NEW-31` (Studio #17476) vs `TC-CB-140` (kho, nguồn Change bot r9-r11 TR=OK + stg=OK) | Dev + Leader |
| 12 | **CONFLICT expected — job kẹt có tự phục hồi không** | `NEW-21` (Studio) expected: yêu cầu kẹt ở trạng thái đang xử lý, **KHÔNG tự chạy lại**, phải admin can thiệp tay. Kho `TC-CB-111` expected: quá ngưỡng timeout thì **tự** chuyển sang thất bại, **tự mở khóa bot** và thông báo cho Admin. Kho tự ghi ⏳ `QA-dev-001` (P0): ngưỡng chưa chốt (spec 1 giờ vs TA đề xuất 15 phút). | `NEW-21` (Studio #17397) vs `TC-CB-111` (kho) | Dev + Leader |
| 13 | Đồng bộ với 16 điểm mâu thuẫn sẵn có của kho `fa039` | 4 mục của kho trùng vùng với report này, nên chốt **cùng một lượt** để không ra 2 kết luận khác nhau: **MT-04** (bot gói Free có được đổi LOA — trùng mục 8 ở trên) · **MT-16** (cảnh báo LIFF ID đổi ảnh hưởng 5 tính năng — trùng mục 2 và `TC-LIFFENTRY001-01`) · **MT-09** (liên kết Google mất hay giữ sau khi đổi — trùng `TC-INTGCAL001-01`) · **MT-07** (`event_step_time` xóa hay không — chi phối `TC-CB-152/168/171` ở §5b nhóm B). | `kho-tcs/fa039-changebot-...md` §"Điểm mâu thuẫn với spec" | Leader + Dev |
