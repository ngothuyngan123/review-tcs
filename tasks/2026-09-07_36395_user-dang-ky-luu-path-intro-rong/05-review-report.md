# 05 — Review Report

> Draft cho Leader verify. Chỉ ghi phần **THIẾU** + việc phải làm.

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | Studio task #259 (ticket 36395, branch `ai_fixbug_36395`, round 1, `reviewState=leader`) |
| Tổng số TC review | 30 |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `13/20 vùng ảnh hưởng đủ TC · 4 GAP · 3 RISK` — ⚠️ nhưng **0/30 TC đã chạy** (xem I1), nên mọi ô "đủ TC" ở đây là **đủ trên giấy**, chưa có kết luận test nào; toàn bộ hạ xuống `RISK` cho tới khi có kết quả thực thi.

> `G5`–`G7` phát hiện thêm sau khi đối chiếu **bộ TC cũ** của chính lần fix trước (`TCsLine_Đăng ký user - Login` → tab `đăng ký từ PC/ mobile`, rows 15–37) — xem bảng đối chiếu ở §5.

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | `app/UserTemporary.php` — `$fillable` thêm `path_intro` trên **model dùng chung** | `diff code` | `NEW-1`, `NEW-8` (chỉ đi qua luồng đăng ký) | `GAP` — Studio `dev_impact` ghi rõ *"là shared model, cần rà mọi nơi mass-assign `UserTemporary::create/fill`"*, nhưng `03-dev-impact.md` mục 3 **không có danh sách caller** và 0 TC chạm consumer nào khác của model này. File này cũng **không nằm trong mục 4.1** của Dev (4.1 chỉ kê 2 file, `diffStat` có 3 file). | `[BLOCKER]` |
| G2 | `D2 — user_temporary.path_intro` (bản ghi tạm **tồn dư**) | `dev-impact` | `NEW-2` (khẳng định KHÔNG ghi đè giá trị cũ), `NEW-24` (referer > temp) | `GAP` — spec FA-039 **BR-21 / GAP-02**: bản ghi tạm **không bao giờ bị DELETE** sau khi đăng ký xong và không có UNIQUE INDEX. `sendMail` dùng lại bản ghi theo email + `NEW-2` xác nhận không ghi đè khi lần mới không có nguồn ⇒ một lần đăng ký bỏ dở **rất lâu trước đó** vẫn để lại `path_intro` cũ và sẽ được kế thừa cho lần đăng ký sau. 0 TC. Hệ quả: quy kết sai nguồn + `count_user` cộng nhầm cho referrer cũ. | `[BLOCKER]` |
| G3 | `D1 — users.path_intro` **dữ liệu cũ còn rỗng** → `T2` Account List + `T3` Payment History | `dev-impact` | `NEW-28`, `NEW-29` (chỉ dùng tài khoản **mới tạo sau fix**) | `GAP` — bản fix không đụng dữ liệu cũ (mục 5 Recover Data: script bù **chưa viết**). `resolveRegistrationRoute` coi rỗng **hoặc** `APP_URL` là `通常登録`, nhưng 0 TC mở 2 màn này với tài khoản **cũ đang có `path_intro=''`** để xác nhận không vỡ/không để trống. | `[MAJOR]` |
| G4 | `F8 — countPathIntro` / `D3 — path_intro_data.count_user`: nhánh **khớp một phần** | `diff code` | `NEW-9`, `NEW-10`, `NEW-25`, `NEW-26`, `NEW-27`, `NEW-14` | `RISK` — theo spec **BR-10**, `countPathIntro` loop **toàn bộ** `path_intro_data` và `increment` **mọi** bản ghi khớp (`Str::contains` cho partial). Fix đổi giá trị truyền vào từ **thô/mã hoá** sang **đã decode** ⇒ partial-match nay khớp rộng hơn hẳn trước. Mọi TC đếm hiện có đều dựng **bản ghi cô lập** nên không phát hiện được trường hợp 1 lượt đăng ký cộng cho **nhiều** bản ghi cùng lúc. | `[MAJOR]` |

| G5 | `F9 / register-v3.blade.php` — nhánh **referer** vừa được khôi phục | `diff code` | `NEW-6`, `NEW-24` | `RISK` — cả 2 TC đều dựng trang trước **cùng origin** (*"trang đăng nhập của hệ thống hoặc một trang giới thiệu nội bộ"*) nên referer trả **URL đầy đủ**. Nguồn thật ngoài đời đều **khác origin** (Google, YouTube, landing page đối tác) — theo `Referrer-Policy` mặc định của Chrome, trình duyệt chỉ gửi **origin**. Bộ TC cũ chứng minh: rows 17–19 nhận `https://www.youtube.com/`, rows 24–25 nhận `https://buppan-dx.com/` (mất hẳn path). ⇒ Expected *"bằng đúng địa chỉ trang trước đã bấm"* của `NEW-6` **chỉ đúng khi same-origin**, sẽ fail sai khi chạy với nguồn thật. | `[MAJOR]` |
| G6 | `D1 / F3 resolvePathIntro` — **nguồn cũ đọng lại trong cookie** thắng nguồn mới thật | `dev-impact` | `NEW-7`, `NEW-23`, `NEW-24` (đều dựng 3–4 nguồn trong **cùng 1 phiên**) | `GAP` — 0 TC cho trường hợp **đổi nguồn giữa 2 lần truy cập cách nhau**: lần 1 vào từ nguồn X nhưng **không đăng ký**, lần 2 vào từ nguồn Y rồi mới đăng ký. Bộ TC cũ row 28 đã test và **NG** (kỳ vọng lưu Google, thực tế lưu `https://lme.jp/` — cookie lần trước thắng). Đây là **cùng họ với G2** nhưng carrier là **cookie** thay vì `user_temporary`. ⚠️ Row 37 của bộ cũ lại kỳ vọng **NGƯỢC LẠI** (giữ nguồn aff cũ = OK) → mâu thuẫn nghiệp vụ, đã đưa §6 #7. | `[BLOCKER]` |
| G7 | `F3 resolvePathIntro` — URL-decode giá trị chứa `&` | `diff code` | `NEW-9` (thô, **không có** `&`), `NEW-10` / `NEW-27` (đã mã hoá) | `RISK` — 0 TC dùng giá trị **thô có chứa `&`**. Bộ TC cũ row 17 chứng minh giá trị bị **cắt ở `&` đầu tiên** (kỳ vọng `...&gad_source=1&gclid=...`, thực tế chỉ tới `https://lme.jp/?google_ad_search_branded`, ghi chú *"CHỈ LẤY ĐƯỢC ĐẾN ĐÂY THÔI"*). Mọi URL quảng cáo Google thật đều có `&gclid=`/`&gad_source=` ⇒ ảnh hưởng trực tiếp chất lượng dữ liệu nguồn giới thiệu. Fix **đổi thời điểm decode** nên phải đo lại hành vi này. | `[MAJOR]` |

**Không có TC orphan** — cả 30 TC đều trace được về `BUG` / `F*` / `D*` / `T*`.

**Truy vết 3 dòng NG lịch sử của bộ TC cũ** (chạy 27/09/2025 trên `step` = production) — theo **RULE-12** phần (3) *"mọi case đã từng Không đạt"* bắt buộc nằm trong bộ chạy lại:

| Row TC cũ | Nội dung | Kết quả cũ | TC Studio tương ứng |
|---|---|---|---|
| Row 16 | Vào thẳng `https://step.lme.jp/register_user` → kỳ vọng `path_intro = https://step.lme.jp/`, thực tế `''` | **NG** (27/09) | `NEW-3` ✅ đã cover |
| Row 34 | Mở thẳng link đăng ký aff `/register_user/<ucode>` → thực tế `path_intro = ''` | **NG** (27/09) | `NEW-19` ✅ đã cover |
| Row 28 | Đổi nguồn giữa 2 lần truy cập → lưu nguồn CŨ thay vì nguồn mới | **NG** (27/09) | ❌ **không TC nào cover** → `G6` |

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `16 quan điểm Trigger khớp task · 8 chưa cover đủ`

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `NOTI-MAIL-001` | `Trung bình → BẮT BUỘC (nâng Cao)` — mail chứa link xác thực | `GAP` — **0/30 TC mở hộp thư thật**. Mọi TC lấy token bằng cách đọc thẳng cột `code` trong DB rồi tự ghép URL. Chính root cause của ticket là *"link trong mail thiếu tham số"*, nhưng **chưa ai kiểm link trong mail thật chứa gì**. Nếu mail thật vẫn kèm `?pathIntro=` thì nhánh dự phòng `user_temporary` (NEW-8/NEW-25) không bao giờ chạy trong thực tế; nếu không kèm thì đó mới là đường đi chính. Vi phạm **RULE-06** (output cuối chuỗi). | `[BLOCKER]` |
| Q2 | `ENV-003` | `Cao` (BẮT BUỘC — chạm URL/domain) | `GAP` — **0 TC chạy `prd`**. Toàn bộ expected phụ thuộc `APP_URL` (khác nhau từng env: dev `form.watermeru.com` · staging `go.lmes.jp` · prd `step.lme.jp`) và nhánh mobile thay chuỗi cứng `https://lme.jp`. Nặng nhất: `NEW-22` chạy đúng cohort của ticket (`admin_id=1`, tháng 2026-05 = **dữ liệu production**) nhưng `env_scope` lại giới hạn `dev`/`staging` → không trả lời được câu hỏi của ticket. Vi phạm **RULE-08**. | `[BLOCKER]` |
| Q3 | `DATA-COUNT-001` | `Cao` (BẮT BUỘC — có con số đếm) | `RISK` — 3 TC gắn mã này (`NEW-25/26/27`) **đều là Normal**. Chiều Abnormal chỉ được cover bởi `NEW-14`, `NEW-21` mang **mã lạ** `TOOL-NEGCTRL-001` (không có trong `checklist-lme.md`) → theo BƯỚC 3 **tính là chưa cover**. Thiếu Boundary: nhiều bản ghi thống kê cùng khớp 1 lượt đăng ký (xem G4). **RULE-01**. | `[MAJOR]` |
| Q4 | `DATA-001` | `Cao` | `RISK` — `NEW-28`, `NEW-29` đều Normal, đều chỉ dùng tài khoản mới. Thiếu Abnormal (tài khoản cũ `path_intro=''` — trùng G3). **RULE-01**. | `[MAJOR]` |
| Q5 | `REG-SHARED-001` | `Cao` (BẮT BUỘC — sửa code dùng chung) | `GAP` — 2 TC gắn mã này (`NEW-8`, `NEW-18`) đều nằm trong luồng đăng ký; không TC nào chạm consumer khác của model dùng chung `UserTemporary` (trùng G1). | `[BLOCKER]` |
| Q6 | `DATA-DB-001` | `Cao` (BẮT BUỘC — có UPDATE) | `RISK` — `sendMail` UPDATE `user_temporary` theo `where('email')`. Đã có kiểm `WHERE` scope cho bảng đếm (`NEW-26` dựng 2 bản ghi, khẳng định chỉ 1 bản tăng), nhưng **chưa có TC dựng 2 email** để chứng minh thao tác trên email E1 không chạm bản ghi của E2. **RULE-07**. | `[MAJOR]` |

| Q7 | `FUNC-003` | `Trung bình → Cao` (field liên quan **URL**) | `RISK` — có `NEW-12` (toàn khoảng trắng), `NEW-30` (trim 2 đầu), `NEW-13` (giá trị quy ước `'1'`), nhưng **thiếu 2 dạng URL thật hay gặp nhất**: (a) giá trị **thô chứa `&`** — bộ TC cũ row 17 chứng minh bị cắt ở `&` đầu tiên (G7); (b) URL quảng cáo dài có `?...&gclid=&gad_source=&gbraid=` (rows 22–23 bộ cũ). | `[MAJOR]` |
| Q8 | `REG-URL-001` | `Trung bình → BẮT BUỘC (nâng Cao)` — CR chạm màn đăng ký | `RISK` — 5 TC gắn mã này (`NEW-5/9/10/23/30`) đều dựng nguồn **nhân tạo trong cùng 1 phiên**, cùng origin. **Không TC nào dùng nguồn thật ngoài đời** (Google organic / Google ads / YouTube / landing page đối tác / OEM LP `lme.jp/?manual_header`) — trong khi bộ TC cũ dành hẳn rows 17–27 cho đúng các nguồn đó và cho thấy hành vi khác hẳn (chỉ lấy origin, cắt ở `&`). Trùng `G5` + `G7`. | `[MAJOR]` |

Các quan điểm còn lại đã cover đủ: `FUNC-001` · `FUNC-004` · `FUNC-SEQ-001` · `DATA-MIG-001` · `COMPAT-LEGACY-001` · `UI-001` · `DEPLOY-ASSET-001` · `OUT-TRUTH-001` (xem I10 về nhãn mã quan điểm).

---

## 3. TC trùng lặp nội dung

Đã rà **toàn bộ 30 TC** theo 4 yếu tố. Phát hiện **2 cặp chồng lấn dạng SUBSET** — cả 2 đều do 8 TC bổ sung sau (`NEW-23`…`NEW-30`, tác giả `anhptn@mcp`) mở rộng assertion của TC AI viết trước đó.

| Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| DUP-1 | `NEW-25` (assert rộng hơn: `users.path_intro` **+** `count_user`) | `NEW-8` — **đề nghị GỘP, KHÔNG xóa** | `DUP-SUBSET` | Cùng loại case `Normal`; cùng đối tượng + thao tác (mở `/redirect-regist/<token>` **không kèm** `pathIntro` → hoàn tất đăng ký); cùng tiền đề (bản ghi tạm đã có `path_intro`, cookie sạch); expected của `NEW-8` là tập con của `NEW-25`. Khác mã quan điểm (`REG-SHARED-001` vs `DATA-COUNT-001`). | `[MINOR]` |
| DUP-2 | `NEW-26` (assert rộng hơn: `users.path_intro` **+** count theo bản mobile, không theo bản gốc) | `NEW-11` — **đề nghị GỘP, KHÔNG xóa** | `DUP-SUBSET` | Cùng loại case `Normal`; cùng tiền đề (bản ghi tạm `is_mobile=1`, `path_intro` thật, token còn hạn); cùng steps; expected `NEW-11` (`users.path_intro` = bản `sp.html`) là tập con của `NEW-26`. Khác mã quan điểm (`UI-001` vs `DATA-COUNT-001`). | `[MINOR]` |

- **Gate đã chạy**: giả định xóa `NEW-8` + `NEW-11` → **coverage KHÔNG còn nguyên**. `NEW-11` là **TC duy nhất** mang `UI-001`; `NEW-8` là TC duy nhất chứng minh riêng nhánh dự phòng `user_temporary` (note của nó nêu rõ giá trị chẩn đoán: fail thì phân biệt được lỗi do `$fillable` hay do hàm phân giải). → **Đổi đề xuất từ XÓA sang GỘP/GIỮ.**
- **Khuyến nghị thực tế**: giữ cả 4 TC nhưng khi đánh giá coverage **không tính `NEW-8`/`NEW-25` và `NEW-11`/`NEW-26` là 2 chiều độc lập** — chúng là cùng 1 kịch bản với 2 mức assertion. Nếu cần tiết kiệm thời gian chạy, gộp assertion của `NEW-8` vào `NEW-25` và bổ sung mã `REG-SHARED-001` vào `NEW-25` (tương tự `UI-001` cho `NEW-26`), rồi mới xóa bản hẹp.
- `DUP-INFLATE`: **không có** — 2 cặp trên không làm quan điểm nào trông như đủ 3 loại case.
- `DUP-CONFLICT` **trong 30 TC Studio**: không có. Nhưng phát hiện **1 conflict giữa 2 TC CŨ** khi đối chiếu bộ TC tham chiếu (§5): **row 28 vs row 37** của tab `đăng ký từ PC/ mobile` — cùng kịch bản *"nguồn cũ đọng lại từ lần truy cập trước, lần này vào từ nguồn khác rồi mới đăng ký"* nhưng expected ngược nhau (row 28 kỳ vọng **lấy nguồn MỚI**, đánh NG khi hệ thống giữ nguồn cũ; row 37 kỳ vọng **GIỮ nguồn aff cũ**, đánh OK). **Không tự chọn bên** → đã đưa §6 #7 để PM/Leader chốt; `G6` + `TC-REGURL001-02`/`-03` viết cho cả 2 phía.
- Cặp `NEW-9` / `NEW-10` (cùng `REG-URL-001`, Normal, cùng bản ghi thống kê exact-match) trông rất giống nhau nhưng **KHÔNG phải trùng**: input khác nhau về bản chất (URL thô vs URL đã mã hoá) và đó chính là điểm hành vi bị fix thay đổi. **Giữ cả hai.**
- Xóa thật do human thực hiện trên Studio (`testcase_delete`) — report này không tự xóa.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | Toàn bộ 30 TC (nguồn Studio #259) | **0/30 TC đã chạy** — `exec: total 30 / pass 0 / fail 0 / untested 30`, `envAuto` mọi env `runs=0` (trừ `local` `runs=1` nhưng `automated=0`). Pass rate `0% < 50%`. Task đang ở `status=tc-ready`, chưa vào giai đoạn chạy. | Đây là review **thiết kế TC**, không phải review kết quả. Không được kết luận "fix OK" từ report này. Chạy bộ TC rồi review lại vòng 2. |
| I2 | `[BLOCKER]` | `03-dev-impact.md` mục 3 + 4.1 | `[AP-6]` Mục 3 có 8 dòng nhưng **thiếu hẳn nhánh model dùng chung**: `app/UserTemporary.php` bị sửa (`diffStat` xác nhận 3 file) nhưng **không xuất hiện ở mục 4.1** và không có caller nào của `UserTemporary::create/fill` được kê — trong khi Studio `dev_impact` yêu cầu đích danh phải rà. Kèm theo: kỳ vọng của ticket là *"mọi user đăng ký phải có path_intro"* nhưng Dev chưa liệt kê **tất cả đường tạo bản ghi `users`** (TC mới chỉ tự dò được 3 đường: register-v3, link giới thiệu v1/v2, `/register` đời cũ). | Yêu cầu Dev bổ sung: (a) danh sách mọi nơi mass-assign `UserTemporary`; (b) danh sách mọi đường tạo bản ghi `users`. Chưa có 2 danh sách này thì G1/Q5 không đóng được. |
| I3 | `[MAJOR]` | `03-dev-impact.md` mục 2 vs mục 3 + 4.2 | **Báo cáo Dev tự mâu thuẫn**: mục 2 viết *"Nay truyền chính biến đã xử lý vào lệnh đếm"*, nhưng mục 3 ghi `countPathIntro` *"giữ nguyên"* và mục 4.2 ghi `path_intro_data.count_user` *"không đổi"*. Studio `dev_impact` xác nhận phía **CÓ đổi**: *"countPathIntro nay nhận `$pathIntro` đã resolve ⇒ thay đổi thống kê `PathIntroData.count_user`; rủi ro hồi quy ở đếm nguồn giới thiệu"*. | Yêu cầu Dev sửa lại mục 3 + 4.2 trên Redmine. Trong lúc chờ, coi `D3` là **CÓ đổi** (đã xử lý như vậy ở §1 G4). |
| I4 | `[MAJOR]` | `03-dev-impact.md` | Có `Auto-filled: 2026-09-07 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" chưa tick** → F/D/T có thể thiếu hoặc map sai (thực tế đã thiếu — xem I2). | Tester đọc lại Journal #133361 trên Redmine, đối chiếu đủ 4 mục rồi tick. |
| I5 | `[MAJOR]` | 8 TC: `NEW-3`, `NEW-7`, `NEW-13`, `NEW-14`, `NEW-15`, `NEW-16`, `NEW-19`, `NEW-21`, `NEW-22` | **6 mã quan điểm không có trong `framework/checklist-lme.md`**: `TOOL-NEGCTRL-001` (4 TC) · `TOOL-KNOW-002` · `TOOL-SCOPE-001` · `TOOL-VAL2-001` · `TOOL-OLDREC-001` · `RULE-12`. Riêng `RULE-12` là **mã RULE, không phải mã quan điểm** → gắn sai trên `NEW-16`. Hệ quả: coverage của các TC này không map được, và `DATA-COUNT-001` bị tính là thiếu chiều Abnormal (Q3) dù thực tế `NEW-14`/`NEW-21` đã cover. | Gắn lại mã theo `checklist-lme.md` — gợi ý: `NEW-14`/`NEW-21` → `DATA-COUNT-001`; `NEW-16` → `DEPLOY-ASSET-001`; `NEW-13` → `FUNC-003`; `NEW-3` → `FUNC-001`; `NEW-19` → `REG-SHARED-001`; `NEW-22` → `DATA-MIG-001`. |
| I6 | `[MAJOR]` | `NEW-22` | `env_scope = ['dev','staging']` với lý do ghi trong note: *"Không dùng prd vì env_list hiện tại không có prd"* — **sai thực tế**. `env_list` trả về `prd` **enabled**, `baseUrl=https://step.lme.jp`, `capabilities=["read-only"]`, `requireApproval=true`. TC này thuần **đọc** nên hoàn toàn hợp với env read-only, và cohort của nó (`admin_id=1`, tháng 2026-05) là **dữ liệu production** — chạy trên dev/staging thì con số vô nghĩa với ticket. | Thêm `prd` vào `env_scope` của `NEW-22` và chạy trên prd (xin approval). Sửa luôn note sai. |
| I7 | `[MAJOR]` | `NEW-17` | **Không atomic + expected không đo lường được**: 1 TC chạy 3 độ dài (254 / 255 / 300 ký tự) với 3 email khác nhau; riêng nhánh 300 ký tự expected là *"ghi nhận hành vi thực tế (thành công và bị cắt, hoặc bị từ chối)"* → không pass/fail được, đây là **điều tra** chứ không phải test. | Tách làm 2: (a) Boundary 254 + 255 — expected chốt cứng "lưu đủ, không 5xx"; (b) case điều tra 300 ký tự — chạy trước, lấy kết quả chốt spec (đã đưa vào §6 #3), rồi mới viết TC có expected chốt. |
| I8 | `[MINOR]` | `NEW-16` | Không atomic — 1 TC kiểm 3 tình huống token độc lập (a: email đã có tài khoản · b: quá 24h · c: token không tồn tại). Fail 1 tình huống thì cả TC fail, khó khoanh vùng. | Tách thành 3 TC, hoặc giữ 1 TC nhưng ghi rõ 3 kết quả riêng ở evidence. |
| I9 | `[MINOR]` | `NEW-20` | Steps chứa nhánh **tự quyết định Skip** (*"nếu trang không còn truy cập được thì dừng và trả kết quả bỏ qua"*) → TC không fail/pass tất định, phụ thuộc phán đoán người chạy. | Đưa "route `/register` còn mở hay không" thành **tiền đề** (Leader xác nhận trước), TC chỉ chạy khi tiền đề đúng. |
| I10 | `[MINOR]` | Phân bố mã quan điểm toàn bộ bộ TC | Nhãn mã quan điểm lệch so với nội dung: `FUNC-001` (Cao) chỉ có 2 TC **đều Normal**; `REG-URL-001` (nâng Cao) có 4 Normal + 1 Boundary, **0 Abnormal**. Chiều Abnormal/Boundary thực chất **đã có** (`NEW-12/13/14/15/17/19`) nhưng bị gắn mã khác — phần lớn là mã lạ `TOOL-*` (I5). | Không nâng lên `[MAJOR]` vì **độ sâu test thực tế đã đủ** — đây là vấn đề nhãn, sẽ tự hết sau khi sửa I5. Rà lại RULE-01 sau khi gắn lại mã. |
| I12 | `[MAJOR]` | `NEW-11`, `NEW-15`, `NEW-26` (nhánh mobile) | Note của `NEW-11` bỏ ngỏ một câu hỏi quan trọng: *"hoặc nhánh di động đã chết trên màn mới, hoặc màn mới còn thiếu bước đọc cờ thiết bị"* — và cả 3 TC đều **né** bằng cách dựng thẳng `is_mobile=1` vào bản ghi tạm thay vì đi qua giao diện. Bộ TC cũ **trả lời được câu này**: rows 5–11 (ngoài range 14–37 human đưa) test `is_mobile` qua **link aff** trên PC / iOS Safari / iOS Chrome / Android và **đều OK trên `staging` + `step`**; row 30 cho thấy link aff mang cờ ngay trên URL (`...?path_intro=...&is_mobile=0`). Tức cờ thiết bị **đi qua đường link aff**, không qua màn đăng ký thường. | Chạy 1 lượt đăng ký **thật trên điện thoại** qua link aff (không dựng dữ liệu tay) để xác nhận `is_mobile` còn được set trên `register-v3`. Nếu không set được → nhánh `sp.html` đã chết trên màn mới, `NEW-11`/`NEW-26` đang test một nhánh không tồn tại → báo Dev. Bổ sung rows 5–11 vào bộ regression. |
| I11 | `[MINOR]` | `NEW-22` | Thiếu input để chạy: TC cần *"thời điểm triển khai bản sửa lên môi trường đang kiểm tra"* nhưng giá trị này **không có** ở `01-bug-task.md`, `03-dev-impact.md` hay Redmine (ticket mới ở trạng thái `Fix done - Đợi test`, chưa có mốc deploy). | Bổ sung mốc deploy vào tiền đề trước khi giao TC; nếu chưa deploy thì `NEW-22` chưa chạy được. |

---

## 5. TCs đề xuất bổ sung

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/` **chưa có FA-039 (Đăng ký tài khoản)** — kho hiện chỉ có FA-001…FA-033, không đối chiếu được |
| **Nguồn TC cũ human cung cấp** | Sheet **`TCsLine_Đăng ký user - Login`** → tab **`đăng ký từ PC/ mobile`** (`gid=767570298`), **rows 15–37** — đọc bằng `scripts/fetch_grid.py` (bung 32 ô gộp). Đây là bộ TC của **chính lần fix trước**: *"Thêm referer url khi đăng ký user"* (khối spec ở row 14), tức nhánh referer mà `register-v3` đã làm mất và bản fix này khôi phục. Có cột kết quả chạy trên `staging` + **`step` (production)** ngày 22/09/2025 và 27/09/2025. |
| Vùng regression phát hiện từ nguồn cũ | **4 vùng** chưa có TC ở bộ Studio: (1) referer **cross-origin** từ nguồn thật — rows 17–27 → `G5`/`Q8`; (2) **đổi nguồn giữa 2 lần truy cập** (cookie đọng) — rows 28–29, row 28 **NG** → `G6`; (3) giá trị **thô chứa `&`** bị cắt — row 17 → `G7`/`Q7`; (4) giữ nguồn **aff** khi user đóng link rồi đăng ký từ màn login — row 37 → `TC-REGURL001-03`. |
| Conflict expected vs nguồn cũ | **CÓ** — row 28 vs row 37 kỳ vọng ngược nhau về "nguồn cũ đọng lại" → `[MAJOR] SPEC-CONFLICT`, đã ghi §3 + **§6 #7**. Không tự chọn bên. |
| GAP dùng lại TC cũ (không viết mới) | Rows 16 + 34 (2 dòng **NG** lịch sử) **đã được** `NEW-3` / `NEW-19` cover — không viết TC mới, chỉ bắt buộc đưa vào bộ chạy lại theo RULE-12. |
| Xác nhận chống trùng | Đã đối chiếu **30 TC** ở BƯỚC 0 **+ 23 dòng TC cũ (rows 15–37)** — **không TC đề xuất nào trùng** |

**Nguồn spec đã dùng**: `spec-features/admin/register-user/feature-spec.md` (FA-039) — BR-10, BR-11, BR-21, GAP-02; `db/db-mapping.md` §2.2, §2.8, §4.10; `web/logic-spec.md` §`countPathIntro`.

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-DATA001-01 | UI | DATA-001 | Đăng ký tài khoản (register-v3) — nguồn giới thiệu từ bản ghi tạm tồn dư | Abnormal | manual | Tất cả | Bản ghi đăng ký tạm còn sót từ lần bỏ dở trước đó vẫn được dùng làm nguồn giới thiệu cho lần đăng ký mới | Ứng dụng chạy nhánh `ai_fixbug_36395`. Đã biết `APP_URL` của môi trường. Có mã mời hợp lệ. Chuẩn bị email E chưa có tài khoản. Trình duyệt sạch cookie `path_intro_lme` / `path_intro`. | 1. Mở `/register_user?path_intro=<A đã mã hoá URL>`, nhập email E + mã mời, tích đồng ý, bấm 「認証メールを送信」 → tạo bản ghi tạm của E có `path_intro = A`. 2. **Dừng lại, KHÔNG hoàn tất đăng ký** (mô phỏng bỏ dở). 3. Chỉnh `updated_at` của bản ghi tạm của E lùi lại hơn 1 phút. 4. Mở trình duyệt phiên sạch, **gõ thẳng** `/register_user` (không tham số, không bấm từ trang khác, không cookie). 5. Nhập lại đúng email E + mã mời, tích đồng ý, bấm 「認証メールを送信」. 6. Lấy mã thông báo mới của bản ghi tạm E, mở `/redirect-regist/<mã thông báo>` không kèm `pathIntro`. 7. Nhập đủ 「名前・担当者名」「会社名・屋号」「電話番号」「パスワード」「パスワード(確認用)」, bấm 「この内容で登録」. 8. Tra `users.path_intro` của tài khoản E và `count_user` của bản ghi thống kê ứng với A. | A = `https://lme.jp/lp-qa36395-stale` ; email E = `qa36395-stale-<hậu tố duy nhất>@example.com` ; điện thoại `0901234567` ; mật khẩu `Abc123!@` ; bản ghi thống kê: url = A, so khớp chính xác, ghi lại `count_user` trước test | Đăng ký thành công. `users.path_intro` của tài khoản E **bằng A** (kế thừa từ bản ghi tạm cũ) chứ không phải `APP_URL`, và `count_user` của bản ghi thống kê A **tăng đúng 1** — dù lần đăng ký này hoàn toàn không có nguồn giới thiệu nào. Ghi lại đúng 2 giá trị này làm bằng chứng. ⚠️ Nếu nghiệp vụ **không** chấp nhận kế thừa giá trị từ lần bỏ dở (đặc biệt khi cách nhau nhiều tháng — bản ghi tạm không bao giờ bị xoá theo BR-21) thì đây là **bug mới**, mở ticket riêng. | | Lấp `G2` · Đánh giá spec: Spec không ghi — đã đưa §6 #4 để PM chốt · Evidence: ảnh chụp 2 dòng DB (`user_temporary`, `users`) + số `count_user` trước/sau · regression |
| TC-DATA001-02 | UI | DATA-001 | Danh sách tài khoản (FS-003) + Lịch sử thanh toán (FS-009) — tài khoản cũ còn `path_intro` rỗng | Abnormal | manual | Tất cả | Tài khoản tạo TRƯỚC bản fix (đang có `path_intro` rỗng) vẫn hiển thị đúng nguồn đăng ký ở 2 màn quản trị | Có ít nhất 1 tài khoản **tạo trước bản fix** đang có `users.path_intro = ''` hoặc `NULL` (dùng cohort của ticket, hoặc dựng thủ công trên dev/staging). Có thêm 1 tài khoản mới sau fix `path_intro = APP_URL` và 1 tài khoản mới `path_intro = <URL giới thiệu>` để đối chứng. Tài khoản đăng nhập được có quyền xem 2 màn. | 1. Đăng nhập màn quản trị, mở **Danh sách tài khoản**. 2. Tìm tài khoản cũ (`path_intro` rỗng), ghi nhận cột/nhãn nguồn đăng ký hiển thị. 3. Tìm 2 tài khoản đối chứng, ghi nhận nhãn của chúng. 4. Mở **Payment History**, tìm dòng của cả 3 tài khoản, ghi nhận cột phân loại nguồn đăng ký. 5. Đối chiếu 3 nhãn với `users.path_intro` tương ứng. | Tài khoản cũ: `path_intro = ''` ; đối chứng D: `path_intro = APP_URL` ; đối chứng R: `path_intro = https://lme.jp/lp-qa36395-oldview` | Cả 2 màn đều hiển thị bình thường, **không lỗi, không ô trống bất thường, không 5xx**. Tài khoản cũ (`path_intro` rỗng) và đối chứng D (`APP_URL`) **cùng hiển thị 「通常登録」** (đúng quy ước `resolveRegistrationRoute`: rỗng hoặc bằng `APP_URL` đều là đăng ký thông thường). Đối chứng R hiển thị đúng nguồn giới thiệu, không bị gộp thành 「通常登録」. | | Lấp `G3` + `Q4` · Đánh giá spec: Spec ghi rõ (`resolveRegistrationRoute`) · Evidence: ảnh chụp 2 màn + 3 dòng `users.path_intro` · regression — dữ liệu cũ ngoài phạm vi fix nhưng không được vỡ |
| TC-DATACOUNT001-01 | UI | DATA-COUNT-001 | Thống kê nguồn giới thiệu (`path_intro_data`) — 1 lượt đăng ký khớp nhiều bản ghi | Abnormal | manual | Tất cả | Một lượt đăng ký khớp đồng thời nhiều bản ghi thống kê thì số lượt được cộng cho những bản ghi nào | Ứng dụng chạy nhánh `ai_fixbug_36395`. Tạo sẵn **3** bản ghi thống kê dùng riêng cho lượt test và ghi lại `count_user` của cả 3: (a) url = `https://lme.jp`, so khớp **một phần**; (b) url = `https://lme.jp/campaign`, so khớp **một phần**; (c) url = `https://lme.jp/campaign/qa36395-multi`, so khớp **chính xác**. Trình duyệt sạch cookie. Email đăng ký là email mới. | 1. Mở `/register_user?path_intro=<C đã mã hoá URL>`. 2. Nhập email + mã mời, tích đồng ý, bấm 「認証メールを送信」. 3. Lấy mã thông báo, mở `/redirect-regist/<mã thông báo>?pathIntro=<C đã mã hoá URL>`. 4. Nhập đủ thông tin, bấm 「この内容で登録」. 5. Đối chiếu `count_user` của **cả 3** bản ghi (a)(b)(c) so với giá trị trước test. 6. Đối chiếu `users.path_intro` của tài khoản mới. | C = `https://lme.jp/campaign/qa36395-multi` (dạng mã hoá: `https%3A%2F%2Flme.jp%2Fcampaign%2Fqa36395-multi`) ; email `qa36395-multi-<hậu tố duy nhất>@example.com` ; điện thoại `0901234567` ; mật khẩu `Abc123!@` | Đăng ký thành công, `users.path_intro = C`. Theo **BR-10** (loop toàn bảng, tăng **mọi** bản ghi khớp), `count_user` của **cả ba** bản ghi (a), (b), (c) đều **tăng đúng 1** — tức 1 lượt đăng ký được đếm 3 lần ở 3 dòng thống kê khác nhau. Ghi lại chính xác 3 con số trước/sau. ⚠️ Trước bản fix, giá trị truyền vào bộ đếm còn **mã hoá** nên (a)(b)(c) đều không khớp; sau fix giá trị đã decode nên khớp cả 3 → **số liệu thống kê thay đổi hẳn về cách tính**. Nếu nghiệp vụ chỉ muốn cộng cho 1 bản ghi thì đây là bug. | | Lấp `G4` + `Q3` · Đánh giá spec: Spec không ghi — đã đưa §6 #5 để PM chốt · Evidence: bảng `count_user` 3 dòng trước/sau + `users.path_intro` |
| TC-NOTIMAIL001-01 | UI | NOTI-MAIL-001 | Mail xác thực đăng ký — nội dung link trong hộp thư thật | Normal | manual | Tất cả | Link xác thực trong mail thật có kèm tham số đường dẫn giới thiệu hay không, và bấm đúng link đó thì lưu ra giá trị nào | Ứng dụng chạy nhánh `ai_fixbug_36395`. Có **hộp thư thật nhận được mail** (không dùng log, không lấy token từ DB). Trình duyệt sạch cookie `path_intro_lme` / `path_intro`. Có mã mời hợp lệ. Email đăng ký là email mới thuộc hộp thư đó. | 1. Mở `/register_user?path_intro=<A đã mã hoá URL>`. 2. Nhập email + mã mời, tích đồng ý, bấm 「認証メールを送信」. 3. **Mở hộp thư thật**, tìm mail xác thực vừa nhận. 4. Sao chép **nguyên văn URL** trong mail và ghi lại: URL đó **có** hay **không có** tham số `pathIntro`. 5. **Bấm chính link trong mail** (không tự sửa, không tự thêm tham số). 6. Nhập đủ thông tin, bấm 「この内容で登録」. 7. Tra `users.path_intro` của tài khoản mới và `user_temporary.path_intro` của email đó. | A = `https://lme.jp/lp-qa36395-mail` ; email: địa chỉ thật nhận được mail ; điện thoại `0901234567` ; mật khẩu `Abc123!@` | Mail xác thực đến hộp thư. **Ghi rõ trong evidence URL trong mail có kèm `pathIntro` hay không** — đây là kết quả chính của TC. Sau khi bấm đúng link trong mail và hoàn tất, `users.path_intro` **bằng A**, không rỗng và không phải `APP_URL`. Nếu URL trong mail **không** kèm `pathIntro` thì giá trị phải đến từ `user_temporary` (xác nhận nhánh dự phòng thực sự chạy trong đời thật); nếu **có** kèm thì phải báo lại: kịch bản gốc của ticket (*link trong mail thiếu tham số*) chưa được chứng minh tồn tại. | | Lấp `Q1` · **RULE-06** — bắt buộc verify tại output cuối là hộp thư thật, cấm lấy token từ DB · Đánh giá spec: Spec không ghi · Evidence: ảnh chụp mail + URL nguyên văn + dòng DB |
| TC-DATADB001-01 | UI | DATA-DB-001 | Bản ghi đăng ký tạm (`user_temporary`) — phạm vi `WHERE` khi gửi lại mail | Abnormal | manual | Tất cả | Gửi lại mail xác thực cho email E1 không được chạm bản ghi đăng ký tạm của email E2 | Ứng dụng chạy nhánh `ai_fixbug_36395`. Đã dựng **2** bản ghi đăng ký tạm: E1 có `path_intro = A`, E2 có `path_intro = B` (A ≠ B), cả hai đều chưa hoàn tất đăng ký. Đã chỉnh `updated_at` của **cả hai** lùi hơn 1 phút. Đã ghi lại `code`, `path_intro`, `updated_at` của cả 2 bản ghi. Trình duyệt sạch cookie. | 1. Mở `/register_user` **không** kèm tham số đường dẫn. 2. Nhập đúng email **E1** + mã mời, tích đồng ý, bấm 「認証メールを送信」. 3. Xác nhận màn hình hiện 「認証メールが送信されました」. 4. Tra lại **cả hai** bản ghi đăng ký tạm E1 và E2, đối chiếu `code` · `path_intro` · `updated_at` với giá trị đã ghi ở tiền điều kiện. | E1 = `qa36395-scope1-<hậu tố duy nhất>@example.com`, A = `https://lme.jp/lp-scope-e1` ; E2 = `qa36395-scope2-<hậu tố duy nhất>@example.com`, B = `https://lme.jp/lp-scope-e2` | Bản ghi **E1**: `code` được cấp mới, `path_intro` **vẫn giữ A** (không bị ghi đè thành rỗng). Bản ghi **E2**: `code`, `path_intro` (= B) và `updated_at` **giữ nguyên hoàn toàn**, không bị chạm. Không có bản ghi đăng ký tạm nào khác bị thay đổi. | | Lấp `Q6` · **RULE-07** — kiểm `WHERE` scope trên 2 tài khoản, bắt buộc verify DB không thay bằng UI · Đánh giá spec: Spec ghi rõ (`sendMail` dùng `where('email')`) · Evidence: ảnh chụp 2 dòng `user_temporary` trước/sau |

| TC-REGURL001-01 | UI | REG-URL-001 | Đăng ký tài khoản (register-v3) — referer từ **nguồn thật ngoài hệ thống** | Normal | manual | product | Referer cross-origin từ Google / YouTube / landing page đối tác chỉ trả về origin, nhưng vẫn phải lưu ra giá trị khác rỗng | Ứng dụng chạy nhánh `ai_fixbug_36395` đã deploy lên môi trường có tên miền công khai (nguồn ngoài không trỏ được vào `localhost`). Trình duyệt sạch cookie `path_intro_lme` / `path_intro` **trước mỗi lượt**. Mỗi lượt dùng 1 email mới. Đã biết `APP_URL`. ⚠️ Cần Leader duyệt vì tạo tài khoản thật. | Chạy **4 lượt độc lập**, mỗi lượt xoá cookie trước: 1. **Google organic** — search `エルメ 登録`, bấm kết quả trỏ tới trang đăng ký, đăng ký. 2. **Google Ads** — search `エルメ`, bấm quảng cáo vào `lme.jp` (URL đích có `?google_ad_search_branded&gad_source=...&gclid=...`), bấm 「無料で使ってみる」, đăng ký. 3. **YouTube** — mở video OEM, bấm link 「新規アカウント作成はこちら」 dưới mô tả, đăng ký. 4. **Landing page đối tác** — mở LP ngoài có nhúng link đăng ký (vd `https://buppan-dx.com/<trang test>`), bấm link, đăng ký. Sau mỗi lượt tra `users.path_intro` và **ghi lại nguyên văn** giá trị lưu được. | 4 email `qa36395-ref-google/ads/youtube/lp-<hậu tố duy nhất>@example.com` ; điện thoại `0901234567` ; mật khẩu `Abc123!@` | Cả 4 lượt **đăng ký thành công** và `users.path_intro` **khác rỗng, khác `NULL`**. Ghi lại chính xác giá trị từng lượt và đối chiếu với bộ TC cũ: lượt YouTube kỳ vọng `https://www.youtube.com/`, lượt LP đối tác kỳ vọng `https://buppan-dx.com/` — tức **chỉ có origin, KHÔNG có path đầy đủ** (đúng `Referrer-Policy` mặc định); lượt Google organic kỳ vọng `https://www.google.com/`; lượt Google Ads kỳ vọng URL `lme.jp` kèm tham số quảng cáo. **Không lượt nào được ra `''` hay `APP_URL`.** Nếu có lượt ra `APP_URL` nghĩa là referer không tới được server → nhánh vừa khôi phục vẫn hỏng với nguồn thật. | | Lấp `G5` + `Q8` · **RULE-08** — nguồn thật chỉ trỏ về tên miền production, không dựng được ở local · Đánh giá spec: Spec không ghi — đã đưa §6 #8 · Evidence: ảnh chụp thanh địa chỉ nguồn + dòng `users.path_intro` từng lượt · regression — dẫn từ rows 17–27 bộ TC cũ (đã OK trên `step` 27/09/2025) |
| TC-REGURL001-02 | UI | REG-URL-001 | Đăng ký tài khoản (register-v3) — đổi nguồn giữa 2 lần truy cập | Abnormal | manual | product | Vào từ nguồn X nhưng không đăng ký, lần sau vào từ nguồn Y mới đăng ký thì lưu nguồn nào | Ứng dụng chạy nhánh `ai_fixbug_36395` trên môi trường có tên miền công khai. Trình duyệt **sạch hoàn toàn** cookie `path_intro_lme` / `path_intro` trước khi bắt đầu. Email đăng ký là email mới. ⚠️ Cần Leader duyệt vì tạo tài khoản thật. | 1. **Lượt 1** — mở landing page `https://lme.jp/`, bấm vào trang đăng ký LME, **DỪNG LẠI, KHÔNG nhập gì, KHÔNG đăng ký**. 2. Đóng tab. **KHÔNG xoá cookie** (mô phỏng người dùng thật). 3. **Lượt 2** — search Google `エルメ 登録`, bấm kết quả organic vào trang đăng ký. 4. Nhập email + mã mời, tích đồng ý, bấm 「認証メールを送信」. 5. Lấy mã thông báo, mở `/redirect-regist/<mã thông báo>`, nhập đủ thông tin, bấm 「この内容で登録」. 6. Tra `users.path_intro` và `user_temporary.path_intro` của email đó; kiểm tra cookie `path_intro_lme` đang mang giá trị nào. | Nguồn lượt 1 = `https://lme.jp/` ; nguồn lượt 2 = `https://www.google.com/` ; email `qa36395-switch-<hậu tố duy nhất>@example.com` | Đăng ký thành công. **Ghi lại chính xác `users.path_intro`** — chỉ có 2 khả năng: (a) `https://www.google.com/` = lấy nguồn MỚI, (b) `https://lme.jp/` = cookie lần trước thắng. Bộ TC cũ row 28 kỳ vọng (a) và **đánh NG** vì hệ thống cho ra (b). ⚠️ **Không tự kết luận đúng/sai** — ghi kết quả thực tế rồi đối chiếu quyết định của PM ở §6 #7, vì row 37 của chính bộ TC cũ lại kỳ vọng ngược lại. | | Lấp `G6` · **cùng họ với `TC-DATA001-01`** (carrier `user_temporary`), TC này carrier là **cookie** · **RULE-12(3)** — case đã từng NG (row 28, 27/09/2025) bắt buộc chạy lại · Đánh giá spec: Spec không ghi — §6 #7 · Evidence: ảnh chụp cookie + `users.path_intro` |
| TC-REGURL001-03 | UI | REG-URL-001 | Đăng ký tài khoản (register-v3) — giữ nguồn giới thiệu aff sau khi đóng link | Normal | manual | product | Mở link aff rồi đóng, sau đó đăng ký từ màn login thường thì vẫn giữ được nguồn aff | Ứng dụng chạy nhánh `ai_fixbug_36395` trên môi trường có tên miền công khai. Có tài khoản đóng vai người giới thiệu và biết `ucode` của tài khoản đó. Trình duyệt sạch cookie trước khi bắt đầu. Email đăng ký là email mới. ⚠️ Cần Leader duyệt vì tạo tài khoản thật. | 1. Mở link giới thiệu `https://lme.jp?ucode=<ucode>`. 2. **Đóng tab, KHÔNG đăng ký, KHÔNG xoá cookie.** 3. Mở màn login LME theo cách thông thường, bấm 「新規登録」. 4. Nhập email + mã mời, tích đồng ý, bấm 「認証メールを送信」. 5. Lấy mã thông báo, mở `/redirect-regist/<mã thông báo>`, nhập đủ thông tin, bấm 「この内容で登録」. 6. Tra `users.path_intro` và 2 cột quan hệ người giới thiệu (`user_introduce` / `user_introduce_2`) của tài khoản mới. | `ucode` của tài khoản người giới thiệu ; email `qa36395-affkeep-<hậu tố duy nhất>@example.com` ; điện thoại `0901234567` ; mật khẩu `Abc123!@` | Đăng ký thành công. `users.path_intro` **giữ nguyên nguồn aff** `https://lme.jp/?ucode=<ucode>` (không bị thay bằng URL màn login hay `APP_URL`), và quan hệ người giới thiệu vẫn được ghi đúng vào `user_introduce` hoặc `user_introduce_2` theo cờ cho phép nhận hoa hồng. | | Lấp `G6` (phía đối lập) · regression — dẫn từ row 37 bộ TC cũ (OK trên `step` 22/09 + 27/09/2025), **kết quả phải giữ nguyên sau fix** · TC này và `TC-REGURL001-02` cùng kịch bản nhưng expected ngược nhau → chạy **cả hai** rồi đưa kết quả lên §6 #7 · Đánh giá spec: Spec không ghi · Evidence: dòng `users` 3 cột |
| TC-FUNC003-01 | UI | FUNC-003 | Đăng ký tài khoản (register-v3) — đường dẫn giới thiệu chứa ký tự `&` | Boundary | manual | Tất cả | Đường dẫn giới thiệu có nhiều tham số nối bằng `&` phải lưu đủ, không bị cắt | Ứng dụng chạy nhánh `ai_fixbug_36395`. Trình duyệt sạch cookie `path_intro_lme` / `path_intro`. Có mã mời hợp lệ. Chuẩn bị 2 email mới. Tạo sẵn 1 bản ghi thống kê so khớp **chính xác** với url = giá trị đầy đủ ở cột dữ liệu, ghi lại `count_user` trước test. | **Lượt A (thô)**: 1. Mở `/register_user?path_intro=<giá trị ĐẦY ĐỦ để nguyên, KHÔNG mã hoá>`. 2. Hoàn tất gửi mail xác thực, mở link bước 2 với cùng giá trị thô. 3. Đăng ký xong, tra `users.path_intro` — **đếm xem có bị cắt ở `&` đầu tiên không**. **Lượt B (đã mã hoá)**: 4. Lặp lại toàn bộ với chính giá trị đó nhưng **đã mã hoá URL** và email thứ hai. 5. Tra `users.path_intro` lượt B và `count_user` của bản ghi thống kê. | Giá trị đầy đủ: `https://lme.jp/?google_ad_search_branded&gad_source=1&gclid=EAIaIQobChMI-qa36395-amp` ; bản mã hoá: `https%3A%2F%2Flme.jp%2F%3Fgoogle_ad_search_branded%26gad_source%3D1%26gclid%3DEAIaIQobChMI-qa36395-amp` ; email `qa36395-amp-raw@example.com` và `qa36395-amp-enc@example.com` | **Lượt B (mã hoá)**: `users.path_intro` bằng **đúng giá trị đầy đủ** sau decode, còn nguyên cả 3 tham số, và `count_user` của bản ghi thống kê so khớp chính xác **tăng đúng 1**. **Lượt A (thô)**: ghi lại giá trị thực tế — nếu chỉ lưu tới `https://lme.jp/?google_ad_search_branded` (cắt ở `&` đầu tiên, đúng như row 17 bộ TC cũ) thì **ghi rõ đây là hành vi còn tồn tại** và `count_user` **không** tăng vì không khớp. So sánh chênh lệch A vs B là kết quả chính của TC. | | Lấp `G7` + `Q7` · Đánh giá spec: Spec không ghi — đã đưa §6 #9 để chốt có cần mã hoá phía nguồn hay không · Evidence: ảnh chụp URL nguồn + `users.path_intro` 2 lượt + `count_user` · regression — dẫn từ row 17 bộ TC cũ |

**Các dòng §1/§2 KHÔNG đề xuất TC mới** (kèm lý do):

- **`G1` + `Q5` (`app/UserTemporary.php` — model dùng chung)**: **chưa viết được TC** vì chưa biết có consumer nào khác. Không đoán bừa tên hàm/màn hình. Việc phải làm trước: Dev cung cấp danh sách mọi nơi mass-assign `UserTemporary::create/fill` (I2). Có danh sách rồi mới viết 1 TC cho mỗi nơi.
- **`Q2` (`ENV-003` / RULE-08)**: **không đẻ TC mới cho phần thống kê** — chỉ cần sửa `env_scope` của `NEW-22` để thêm `prd` (I6); `prd` là env `read-only` nên đúng loại cho TC thuần đọc này. ⚠️ **Đính chính giả định ban đầu**: bộ TC cũ cho thấy đội test **đã từng đăng ký tài khoản thật trên `step` (production)** cho đúng tính năng này — cột `step` và `22/09 OK step` / `27/09 OK step` ở rows 15–37. Vậy "không test được production" là **không đúng**; 3 TC mới `TC-REGURL001-01/02/03` đặt `Phạm vi ENV = product` theo tiền lệ đó, nhưng **cần Leader duyệt** vì có tạo tài khoản thật.

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | FA-039 §5 Business Rules — thêm BR mới | Quy ước **"đăng ký không có nguồn giới thiệu thì lưu `APP_URL`"** hiện chỉ suy từ code + báo cáo Dev, chưa có BR nào ghi. Đây là expected của hơn 1/3 số TC. | `NEW-3` note tự ghi *"cần PO/QA xác nhận là kỳ vọng nghiệp vụ chính thức"*; spec FA-039 chưa có rule tương ứng | PM / PO |
| 2 | FA-039 BR-10 (`countPathIntro`) | Sau fix, bộ đếm nhận giá trị **đã decode + trim** thay vì giá trị thô ⇒ **số liệu thống kê nguồn giới thiệu trước và sau fix không cùng cách tính**: các bản ghi so khớp **chính xác** trước đây gần như không bao giờ khớp (vì giá trị còn mã hoá), từ nay sẽ khớp. Cần ghi vào spec và báo bộ phận dùng số liệu. | Studio `dev_impact` + `NEW-10` note | PM + Dev |
| 3 | FA-039 db-mapping §2.1 / §2.2 — `path_intro` | Chưa có quy định hành vi khi đường dẫn giới thiệu **dài hơn 255 ký tự**: `users.path_intro` là `varchar(255)` còn `user_temporary.path_intro` là `varchar(500)` → giá trị lấy lại từ bản ghi tạm có thể bị cắt hoặc gây lỗi khi ghi vào `users`. Cần chốt: cắt cụt, từ chối, hay validate ở đầu vào. | `NEW-17` không chốt được expected cho case 300 ký tự (xem I7) | Dev + PM |
| 4 | FA-039 BR-21 / GAP-02 + BR mới về nguồn dự phòng | Bản fix dùng `user_temporary.path_intro` làm nguồn dự phòng, nhưng bản ghi tạm **không bao giờ bị DELETE** sau khi đăng ký xong. Cần chốt: có được kế thừa nguồn giới thiệu từ lần đăng ký bỏ dở **cũ bao lâu cũng được** hay phải có hạn (vd cùng 24h với token)? | §1 `G2` + `TC-DATA001-01` | PM |
| 5 | FA-039 BR-10 — hành vi đa khớp | `countPathIntro` loop toàn bảng và `increment` **mọi** bản ghi khớp ⇒ 1 lượt đăng ký có thể cộng cho nhiều dòng thống kê (rõ nhất với `url_type=1` PARTIAL). Cần chốt: có chấp nhận đếm trùng nhiều dòng không, hay chỉ được cộng cho 1 dòng khớp cụ thể nhất? | §1 `G4` + `TC-DATACOUNT001-01` | PM |
| 6 | FA-039 `feature-spec.md` dòng 75 + `web/logic-spec.md` §`App\UserTemporary` + NIT-09 | **Spec đã lệch code**: spec ghi bước 2 render view `auth.register_user_v2` và `$fillable = ['email','code','is_mobile']` với NIT-09 *"`path_intro` không nằm trong `$fillable`"*. Code hiện tại dùng `auth-v3/register-v3.blade.php` và bản fix **đã thêm `path_intro` vào `$fillable`** → NIT-09 đã được xử lý, cần cập nhật cả 3 chỗ. | `spec_delta.files[]` của Studio vs spec-features | Dev |
| 7 | FA-039 — BR mới về **nguồn giới thiệu đọng lại từ lần truy cập trước** | ⚠️ **Mâu thuẫn kỳ vọng trong chính bộ TC cũ**, chưa ai chốt: **row 28** (vào từ LP không đăng ký → sau đó vào từ Google mới đăng ký) kỳ vọng lấy **nguồn MỚI** và **đánh NG** vì hệ thống giữ nguồn cũ; **row 37** (mở link aff rồi đóng → đăng ký từ màn login) kỳ vọng **GIỮ nguồn aff cũ** và đánh OK. Cùng một cơ chế (cookie/`user_temporary` đọng lại) nhưng 2 kết luận ngược nhau. Cần 1 rule thống nhất: nguồn cũ có được kế thừa không, có phân biệt nguồn aff với nguồn thường không, và có hạn thời gian bao lâu (liên quan §6 #4). | Sheet `đăng ký từ PC/ mobile` row 28 vs row 37 · §1 `G6` · `TC-REGURL001-02` vs `TC-REGURL001-03` | PM (bắt buộc) + Leader |
| 8 | FA-039 §5 — BR về nhánh **referer** vừa khôi phục | Chưa có rule ghi rõ referer **cross-origin chỉ trả về origin** (`Referrer-Policy` mặc định của trình duyệt) nên `path_intro` từ nguồn ngoài **không bao giờ có path đầy đủ** — bộ TC cũ đã xác nhận (`https://www.youtube.com/`, `https://buppan-dx.com/` thay vì URL đầy đủ). Cần ghi vào spec để không ai viết expected sai, và để bên nghiệp vụ biết dữ liệu nguồn giới thiệu từ referer chỉ ở mức tên miền. | Rows 17–19, 24–25 bộ TC cũ · §1 `G5` · expected của `NEW-6` hiện chỉ đúng same-origin | PM + Dev |
| 9 | FA-039 BR-10 / db-mapping §2.1 — `path_intro` chứa `&` | Chưa có rule cho URL nhiều tham số: bộ TC cũ row 17 cho thấy giá trị truyền **thô** bị **cắt ở `&` đầu tiên** (mất `gad_source`, `gclid`), trong khi rows 22–23 (đi qua referer) lại giữ đủ. Mọi URL quảng cáo Google thật đều có `&` ⇒ dữ liệu nguồn giới thiệu đang không nhất quán. Cần chốt: bắt buộc mã hoá URL ở phía nguồn, hay server phải tự xử lý. | Row 17 vs rows 22–23 bộ TC cũ · §1 `G7` · `TC-FUNC003-01` | Dev + PM |
