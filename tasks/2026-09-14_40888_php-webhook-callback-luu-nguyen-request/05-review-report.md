# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | `(1) MCP LME TEST STUDIO — task #299` (ticket 40888, round 1, branch `ai_small_40888`), `testcase_list` fetch 2026-09-14 |
| Tổng số TC review | `35` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: chiều **(a) dev-impact** `11/21 vùng Dev kê đủ TC · 2 GAP · 8 RISK`, **cộng 1 vùng Dev KHÔNG kê ra** (notify mobile/PC/Chatwork) cũng `0 TC`; chiều **(b) diff code** phát hiện thêm `7 GAP` **không nằm trong danh sách Dev tự kê**.

> Chiều (b) lấy từ Studio `task_get_context(dev_impact + spec_delta)` — `diffAvailable = true`, 7 file / +337 −49 dòng.
> Nhận định bao trùm: bộ TC hiện tại chứng minh **tầng GHI** (PHP lưu nguyên văn) là đúng, nhưng **chưa chứng minh tầng ĐỌC** ở phiên bản sẽ thực sự chạy trên **staging** — môi trường đích duy nhất của luồng này (job `linect-service` bản release + 3 tiến trình PHP cũ).

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | `T4 — Auto Reply (FA-003)`, tính năng tiêu thụ chính của `callback_event` | `dev-impact` | `NEW-25` (manual, local, oracle không kết luận được) | `GAP` — không TC nào verify auto reply vẫn chạy đúng khi 1 bản ghi gộp **nhiều event CÙNG loại**; TC duy nhất chạm auto reply lại tự cho phép "không kết luận đạt/không đạt tại chỗ" | `[BLOCKER]` |
| G2 | `T1 — Chat 1:1 (FA-002)`: tin nhắn hiển thị + bộ đếm chưa xác nhận | `dev-impact` | `NEW-16` (chỉ đọc log tiến trình) | `GAP` — 0 TC đi tới **output cuối** (màn chat 1:1 / LINE app) theo RULE-06; 0 TC kiểm `conversation.confirm_count` khi 1 bản ghi gộp 3 tin | `[BLOCKER]` |
| G3 | **Thứ tự deploy PHP ↔ job `linect-service`** (cross-repo) | `diff code` | `không có` | `GAP` — mọi TC phía job chạy trên **branch dev** `m_202609_forward_webhook_40475_*`. Spec bản release [`auto-reply/job/job-spec.md:100`](../../spec-features/admin/auto-reply/job/job-spec.md) ghi job dispatch `switch callbackEvent.getType()` — **theo cột `type` của bản ghi**, chưa có `handleCallbackEventByType()`. PHP lên trước job → bản ghi gộp gặp job cũ | `[BLOCKER]` |
| G4 | `callback_event.status` là **1 giá trị cho cả bản ghi** đã gộp | `diff code` | `không có` | `GAP` — bản ghi gộp `[tin nhóm, tin 1:1]` → `status=10` (bỏ qua tin nhóm) đóng luôn cả bản ghi; bot hết hạn → `status=8`. Kho đã có TC-RPL-224 / TC-RPL-225 cho 2 trạng thái này ở thời 1 event/bản ghi | `[BLOCKER]` |
| G5 | Khoá đồng thời theo `line_id` **của bản ghi** (BR-22) | `diff code` | `không có` | `GAP` — 1 bản ghi nay chứa event của **nhiều `line_id`**; khoá theo `line_id` của bản ghi không còn đúng hạt. Kho TC-RPL-226 kiểm khoá này ở thời 1 event/bản ghi | `[BLOCKER]` |
| G6 | Xác thực chữ ký `X-Line-Signature` trong `callbackWebHook` | `diff code` | `không có` | `GAP` — `BotController.php` sửa **114 dòng**, Dev khai "giữ nguyên xác thực chữ ký trước khi lưu" nhưng 0 TC verify chữ ký sai / thiếu header → không tạo bản ghi | `[BLOCKER]` |
| G7 | `F3 — HandleCallback::handle` (kết bạn / hủy kết bạn) + `T3 — Friend Information` | `dev-impact` | `NEW-17` `fail`, `NEW-20` `fail` | `RISK` — **cả 2 TC của F3 đều fail** (#40912), chưa có kết luận Đạt nào cho F3. Nhánh **follow (kết bạn)** 0 TC — NEW-17 chọn nhánh unfollow làm oracle vì nhánh này chỉ ghi log | `[BLOCKER]` |
| G8 | `T2 — Webhook転送 / Forward webhook (#40475)` | `dev-impact` | `NEW-28`…`NEW-38` (11 TC manual) | `RISK` — 100% chạy `local`; 10/13 TC manual không evidence; TC `NEW-28` ghi rõ **DB emoji chưa decode** nhưng vẫn đánh Đạt → mục tiêu cốt lõi ticket chưa được chứng minh | `[BLOCKER]` |
| G9 | `REG-RUN` — bản ghi `status=0/1` **tồn đọng lúc deploy** | `diff code` | `không có` | `GAP` — bản ghi định dạng cũ đang chờ xử lý + bản ghi mới sinh sau deploy nằm **cùng một lượt poll** của job | `[MAJOR]` |
| G10 | `F8 — UserController::storeFakeCallbackEvent` → `F5 — HandleCallbackPostback` | `diff code` | `NEW-21` (chỉ đi qua `F6` API) | `RISK` — bản ghi định dạng cũ do postback giả lập **vẫn đang được sinh mới**, nhưng chưa test qua tiến trình dòng lệnh `F5` | `[MAJOR]` |
| G11 | `T5 — Rich Menu (FA-004)` | `dev-impact` | `NEW-33` (chỉ kiểm forward) | `RISK` — 0 TC verify **action richmenu vẫn thực thi** sau fix | `[MAJOR]` |
| G12 | `D1` — body vượt sức chứa cột `TEXT` → phản hồi trả về LINE | `diff code` | `NEW-14` (chỉ kiểm log cảnh báo) | `RISK` — 0 TC kiểm LINE nhận phản hồi gì. Trả 200 khi đã cắt cụt → LINE **không gửi lại** → event mất vĩnh viễn | `[MAJOR]` |
| G13 | **Ma trận loại event LINE** ở tầng lưu + tầng đọc PHP | `diff code` | xem bảng đối chiếu bên dưới | `GAP` — quét toàn văn 35 TC: **0 TC** cho `unsend` · `sticker` · `audio` · `location` · **tin nhắn trong group** · `memberJoined` · `memberLeft` · `accountLink`. `HandleCallback` chỉ khai `[follow, unfollow, join, leave]`, `HandleCallbackMessage` khai `[message]` → bản ghi có `type` **nằm ngoài các danh sách này** rơi vào nhánh fallback của `callbackEventsMainFirst()` mà chưa TC nào chạm | `[BLOCKER]` |
| G14 | **Notify mobile / PC / Chatwork** — hệ quả của 5/8 loại callback | `dev-impact` (**Dev KHÔNG kê ở mục 4.3**) | `không có` | `GAP` — **0 TC ở mọi tầng** (quét `notify` / `chatwork` / `通知` trên 35 TC = 0). Một bản ghi gộp nhiều event mà chỉ 1 event được xử lý ⇒ **mất notify của các event còn lại, im lặng, không log** | `[BLOCKER]` |
| G15 | `conversation.last_message` / `last_time_message` + auto confirm message | `diff code` | `không có` | `GAP` — **0 TC**. Đây là 3 hệ quả trực tiếp của **mỗi** event `message` / `unsend`; `last_time_message` còn quyết định thứ tự sắp xếp danh sách bạn bè (BR-02 của FA-001) | `[MAJOR]` |

### 1b. Đối chiếu ma trận loại callback LINE (bằng chứng cho G13 · G14 · G15)

Quét toàn văn 35 TC (`name` + `precondition` + `steps` + `data_input` + `expected` + `note`) theo từng loại event.
**3 tầng**: `Lưu` = webhook EP-24 ghi `callback_event` · `Đọc PHP` = 3 tiến trình dòng lệnh + endpoint postback nội bộ · `Forward` = job chuyển tiếp ra hệ thống bên thứ 3.

| Loại callback LINE | Lưu | Đọc PHP | Forward | Hệ quả nghiệp vụ đã verify | Kết luận |
|---|---|---|---|---|---|
| `message` — text | ✔ `NEW-1` `NEW-2` | ✔ `NEW-16` | ✔ `NEW-28` | ✘ không TC nào tới chat 1:1 / last_message / auto confirm / notify | `RISK` |
| `message` — ảnh · video · file | ✔ gián tiếp | ✘ | ✔ `NEW-29/30/31` | ✘ | `RISK` |
| `message` — **sticker** | ✘ | ✘ | ✘ | ✘ | **`GAP`** |
| `message` — **location** | ✘ | ✘ | ✘ | ✘ | **`GAP`** |
| `message` — **audio** | ✘ | ✘ | ✘ | ✘ | **`GAP`** |
| **`unsend`** (thu hồi tin) | ✘ | ✘ | ✘ | ✘ | **`GAP`** |
| `follow` (kết bạn thường / qua landing / unblock) | ✔ gián tiếp | ✘ (`NEW-17` chọn nhánh unfollow làm oracle) | ✔ `NEW-35` | ✘ action kết bạn + notify chưa verify | **`GAP`** ở tầng đọc |
| `unfollow` (block OA) | ✔ | ⚠ `NEW-17` `fail` · `NEW-20` `fail` | ✔ `NEW-36` | ✘ cập nhật "đã block" trên chat 1:1 chưa verify | `RISK` |
| `join` / `leave` (OA vào/rời nhóm) | ✔ gián tiếp `NEW-9` | ✘ | ✔ `NEW-37` `NEW-38` | ✘ cập nhật thông tin nhóm trên chat 1:1 chưa verify | `RISK` |
| **`message` trong group** + **unsend trong group** | ✘ | ✘ | ✘ | ✘ — kho `TC-RPL-224` cho thấy tin nhóm dùng trạng thái riêng (bỏ qua), nay gộp chung bản ghi với tin 1:1 | **`GAP`** (đã có `TC-JOB001-01`) |
| `postback` — rich menu · button template | ✔ `NEW-3` | ✔ `NEW-18` `NEW-19` `NEW-21` | ✔ `NEW-33` `NEW-34` | ✘ action chỉ verify ở tầng log, không tới output cuối | `RISK` |
| `postback` — **image map** | ✘ | ✘ | ✘ | ✘ | **`GAP`** |
| `videoPlayComplete` | ✘ | ✘ | ✔ `NEW-32` | ✘ action chưa verify | **`GAP`** ở tầng lưu + đọc |
| **`memberJoined` · `memberLeft` · `accountLink`** | ✘ | ✘ | ✘ | ✘ — **không nằm trong 8 nhánh dispatch của job** ([`job-spec.md:100-112`](../../spec-features/admin/auto-reply/job/job-spec.md)) lẫn 3 danh sách `findFirstEvent` của PHP | **`GAP`** |
| Callback của job auto reply (từ khóa thỏa mãn) | — | — | — | ⚠ chỉ `NEW-25` (manual, local, oracle không kết luận) | `RISK` (G1) |
| `delivery` (thông báo đã gửi) | ✔ `NEW-6` `NEW-40` | ✔ `NEW-16` `NEW-3` | — | — | `OK` |

**Hệ quả downstream — quét riêng, kết quả `0 TC` cho toàn bộ:**

| Hệ quả | Số TC | Thuộc loại event nào |
|---|---|---|
| `last_message` / `last_time_message` | **0** | `message`, `unsend` |
| Hiển thị tin trên màn chat 1:1 | **0** | `message`, `unsend`, `unfollow`, `join`/`leave` |
| Auto confirm message (bộ đếm chưa xác nhận) | **0** | `message` |
| **Notify mobile / PC / Chatwork** | **0** | `message`, `follow`, `unfollow`, `group`, auto reply — **5/8 dòng của ma trận** |

> ⚠️ Mục **4.3 của `03-dev-impact.md`** chỉ kê 5 tính năng (Chat 1:1 · Forward webhook · Friend Information · Auto Reply · Rich Menu) — **không kê notify, không kê `unsend`, không kê luồng group**. Đây là thiếu sót của chính bản đánh giá ảnh hưởng, không phải chỉ của bộ TC.

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `23 quan điểm Trigger khớp task · 17 chưa cover đủ`.

> ⚠️ 18/35 TC (51%) mang **mã quan điểm không có trong `framework/checklist-lme.md`** (`API-CONTRACT-001` · `TOOL-ERRHYG-001` · `TOOL-KNOW-002` · `API-001` · `TOOL-OLDREC-001`) → theo quy tắc BƯỚC 3 các TC này **không được tính là cover** cho quan điểm nào. Chi tiết ở §4 I8.

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `DEPLOY-LIVE-001` | `Cao` | `GAP` — release đổi **cấu trúc dữ liệu dùng chung giữa 2 repo**, không maintain, nhưng 0 TC cho kịch bản deploy lệch pha (G3) | `[BLOCKER]` |
| Q2 | `MSG-004` | `Cao` | `GAP` — output cuối trên LINE app (auto reply / richmenu action) hoàn toàn không được verify sau fix (G1, G11) | `[BLOCKER]` |
| Q3 | `CONC-001` | `Cao` | `GAP` — LINE gộp tin dồn dập là **điều kiện kích hoạt gốc** của bug; 0 TC cho khoá đồng thời khi 1 bản ghi chứa nhiều `line_id` / nhiều event cùng loại (G5) | `[BLOCKER]` |
| Q4 | `SEC-002` | `Cao` | `GAP` — 0 TC cho chữ ký `X-Line-Signature`; đồng thời `replyToken` / `markAsReadToken` / `quoteToken` nay được lưu nguyên văn vào DB **và** chuyển tiếp ra hệ thống bên thứ 3, chưa có TC/đánh giá nào (G6) | `[BLOCKER]` |
| Q5 | `REG-RUN-001` | `Cao` | `GAP` — 0 TC cho dữ liệu đang chạy dở tại thời điểm release (G9) | `[BLOCKER]` |
| Q6 | `JOB-001` | `Cao` | `RISK` — chỉ `NEW-25` (1 TC manual, `local`, chạy trên branch dev của job, oracle không kết luận được). State machine `callback_event` (0→1→2, 8, 10) chưa test lại với bản ghi gộp | `[MAJOR]` |
| Q7 | `ENV-003` | `Cao` | `RISK` — tại snapshot review `35/35` TC chạy `local`, **0 TC trên staging** — mà staging là **môi trường đích duy nhất** của luồng này (production không có callback do PHP insert). Domain callback `go.lmes.jp` và domain forward chỉ tồn tại ở staging nên mọi kết luận từ `local` đều không thay cho staging được. ⚠️ **Không** flag theo hướng "phải test production" | `[MAJOR]` |
| Q8 | `DATA-MIG-001` | `Cao` | `RISK` — có `NEW-20`/`NEW-21` cho định dạng cũ nhưng `NEW-20` **fail**; thiếu case **xen kẽ bản ghi cũ + mới trong cùng một lượt xử lý**; thiếu `Abnormal` + `Boundary` (RULE-01) | `[MAJOR]` |
| Q9 | `COMPAT-LEGACY-001` | `Cao` | `RISK` — chỉ 1 TC (`NEW-20`) và đang `fail`; nhánh cũ qua tiến trình dòng lệnh postback chưa có TC (G10) | `[MAJOR]` |
| Q10 | `MSG-USER-001` | `Cao` | `RISK` — 2 TC (`NEW-16` pass / `NEW-17` fail); nhánh **kết bạn (follow)** 0 TC dù đây đúng là rủi ro "xử lý SAI người" Dev nêu (G7) | `[MAJOR]` |
| Q11 | `DATA-COUNT-001` | `Cao` | `RISK` — 1 lần LINE gọi nay gộp nhiều tin vào 1 bản ghi; 0 TC kiểm `conversation.confirm_count` / badge chưa đọc còn đếm đúng (G2) | `[MAJOR]` |
| Q12 | `OUT-TRUTH-001` | `Cao` | `RISK` — 0 TC kiểm phản hồi trả về LINE khi lưu thất bại / bị cắt cụt (G12) | `[MAJOR]` |
| Q13 | `DATA-TEXT-001` | `Trung bình → Cao` (text gửi qua LINE) | `RISK` — `NEW-1` kiểm chuỗi trong DB, `NEW-28` kiểm bên thứ 3, nhưng note của `NEW-28` ghi **DB chưa decode emoji** → 2 TC cho kết luận ngược nhau về cùng một cột dữ liệu (G8, §6) | `[MAJOR]` |
| Q14 | `FUNC-004` | `Cao` | `RISK` — 2 TC (`NEW-13`, `NEW-14`) **đều là `Boundary`**; thiếu `Normal` + `Abnormal` riêng cho giới hạn sức chứa cột (RULE-01) | `[MAJOR]` |
| Q15 | `INTG-LINE-001` | `Cao` | `RISK` → thực chất `GAP` theo **ma trận loại event** (§1b): 8 loại callback LINE không TC nào chạm ở tầng lưu lẫn tầng đọc. Quan điểm này bắt buộc "phủ đủ loại event mà LINE có thể gửi", không phải chỉ loại phổ biến (G13) | `[BLOCKER]` |
| Q16 | `SYNC-APP-001` | `Trung bình → Cao` (flow critical user-facing) | `GAP` — **notify mobile / PC / Chatwork 0 TC**. Đây là hệ quả người dùng cuối nhìn thấy của 5/8 loại callback; mất notify là mất im lặng, không có log để lần ra (G14) | `[BLOCKER]` |
| Q17 | `DATA-001` | `Cao` | `GAP` — `conversation.last_message` / `last_time_message` được **nơi khác tham chiếu** (BR-02 FA-001: sắp xếp danh sách bạn bè theo `last_time_message DESC`) nhưng 0 TC (G15) | `[MAJOR]` |

> ⚠️ **RULE-10** — checklist hiện **không có quan điểm riêng** cho "thông báo đẩy mobile / PC / Chatwork"; `NOTI-MAIL-001` chỉ dành cho email. Q16 đang phải mượn `SYNC-APP-001`. Đề nghị bổ sung 1 dòng quan điểm mới (`NOTI-PUSH-001`) vào [framework/checklist-lme.md](../../framework/checklist-lme.md) sau khi Leader chốt.

---

## 3. TC trùng lặp nội dung

Đã rà **35/35 TC**. Phát hiện **3 nhóm trùng**.

| Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| DUP-1 | `NEW-2` (Studio 17664) | **xóa** `NEW-39` (17701) | `DUP-SUBSET` | quan điểm (cùng tầng ghi webhook) × `Normal` × POST webhook body ≥3 event → 1 bản ghi × tiền đề "web nhánh fix, đã ghi mốc id" → expected tương đương: đúng 1 bản ghi, đủ 3 event, event thứ 3 không mất. Phần "giữ `destination` + `events`" đã có ở `NEW-1` | `[MINOR]` |
| DUP-2 | `NEW-3` (Studio 17665) | **xóa** `NEW-40` (17702) | `DUP-SUBSET` | quan điểm × `Normal` × POST webhook body mở đầu `delivery` + event nghiệp vụ × cùng tiền đề → expected tương đương: `type`/`line_id` lấy event khác `delivery`, `delivery` vẫn còn trong `request` | `[MINOR]` |
| DUP-3 | `NEW-28` (forward text) | **gộp** `NEW-29` (file) · `NEW-30` (ảnh) · `NEW-31` (video) | `DUP-SUBSET` | `API-CONTRACT-001` × `Normal` × "friend gửi media → forward nguyên request" × cùng tiền đề → expected chỉ khác ở `message.type`. Tiền lệ kho: `TC-RPL-220` gộp 8 loại message vào 1 TC | `[MINOR]` |

- **Gate đã chạy**: giả định xóa `NEW-39` + `NEW-40` → coverage §1 và quan điểm §2 **còn nguyên** (`NEW-1`/`NEW-2`/`NEW-3` giữ đủ; 2 TC này mang mã lạ `API-CONTRACT-001` nên vốn không được tính cover). ✔ Giữ nguyên đề xuất **xóa**.
  Riêng DUP-3 → đề xuất **GỘP, không xóa**: catalog E (ma trận media) cho thấy mỗi loại media có field LINE gửi kèm khác nhau; gộp thì phải liệt kê đủ 4 loại ở `Dữ liệu nhập` và ghi điểm khác biệt ở `Kết quả mong đợi`.
- `DUP-INFLATE` — **`NEW-39` và `NEW-40` vốn là 2 TC kiểm chứng forward webhook**, bị `skip` ở run #1383 và #1396 với lý do "thiếu source + tiến trình job `linect-service` và endpoint bên thứ 3". Sau đó 2 TC được **viết lại thành TC kiểm tầng lưu trữ** (trùng `NEW-2`/`NEW-3`) rồi `pass` ở run #1400/#1408. Kết quả: rủi ro *không test được* bị thay bằng 2 TC pass trùng lặp, làm vùng `T2` trông đủ hơn thực tế → đã thêm dòng **G8** vào §1. `[MAJOR]`
- `DUP-CONFLICT` — không có trong nội bộ bộ TC; nhưng có **conflict với kho** (`TC-RPL-223` vs `NEW-23`) → đã đưa §6.
- Xóa thật do human thực hiện trên Studio (`testcase_delete`) — report **không tự xóa**.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | `NEW-28` (Studio 17690) | TC đánh **Đạt** nhưng chính `actual` của tester ghi: *"bên thứ 3 đang hiển thị đúng text nhật + emoji ⇒ **nhưng db thì emoji đang hiển thị dưới dạng chưa decode** ⇒ job java báo tạm thời để như này sau lên stg rồi config thêm domain và sửa"*. Đây **chính là mục tiêu cốt lõi của ticket** (REQ-001: `request` không được chứa escape `\uXXXX`) — chưa đạt mà vẫn tick Đạt | Đổi kết quả về **Không đạt**, hoặc tách điểm kiểm DB thành TC riêng và raise ticket. Không được đóng #40888 dựa trên TC này |
| I2 | `[BLOCKER]` | `NEW-20` (Studio 17682) | **Kết quả mâu thuẫn**: auto run #1408 (2026-09-14 08:31) = `fail`; nhưng có 1 manual result (06:53) đánh `pass` với `actual` = *"hiện tại check ok"*, không evidence. Đây là TC **duy nhất** cover `COMPAT-LEGACY-001` | Chốt lại trạng thái thật của TC. Nếu pass là đúng thì phải kèm evidence; nếu fail là đúng thì Q9 mất toàn bộ cover |
| I3 | `[MAJOR]` | Toàn bộ 35 TC (tại snapshot 2026-09-14) | **`35/35` TC chỉ chạy `env=local`** (`env_tag = local-only`). ⚠️ **RULE-08 KHÔNG áp dụng theo hướng "phải test production"** — luồng callback do PHP insert **chỉ tồn tại trên staging**, production không có (Leader xác nhận 2026-09-15). Nhưng **staging mới là môi trường đích thật** và tại thời điểm review chưa có run nào ở đó; tester manual còn ghi domain forward **chưa được config** | Chạy trên **staging** với domain callback `https://go.lmes.jp/line/callback/add/<bot_id>` đặt trên LINE console. **Đã bắt đầu từ 2026-09-15** (6 run staging) — cần triage 2 run `ai` trả `error` |
| I4 | `[MAJOR]` | 13 TC manual (`NEW-25`, `NEW-28`…`NEW-38`) | `10/13` TC có `actual = null` **và** `evidence = []` → kết quả tự khai (RULE-02). TC duy nhất có evidence dùng link `prnt.sc` ngoài Studio, không lưu trong hệ thống | Yêu cầu đính kèm raw body bên thứ 3 + ảnh chụp DB cho từng TC forward |
| I5 | `[MAJOR]` | `NEW-25` (Studio 17687) | **Oracle không đo lường được**: expected ghi *"Nếu quan sát thấy chỉ event đầu được xử lý còn cú bấm nút của khách B bị bỏ, thì KHÔNG kết luận đạt/không đạt tại chỗ mà ghi lại bằng chứng và chuyển leader"* — TC vẫn được đánh `pass`. Đây là TC duy nhất cover `REQ-011` và duy nhất chạm auto reply | Viết lại expected thành điều kiện nhị phân (cả 2 event đều được xử lý = Đạt), bỏ nhánh thoát |
| I6 | `[MAJOR]` | `NEW-16` (17678), `NEW-22` (17684) | Log của cả 3 run đều ghi `ErrorException` *"Undefined index: timestamp"* tại `HandleCallbackMessage.php:302`, nhưng 2 TC vẫn `pass` và **chưa raise ticket nào** (Studio chỉ có 1 bug #40912 cho nhánh `handle:callback`) | Triage: lỗi có sẵn từ trước hay do fix? Nếu có sẵn → raise ticket riêng như đã làm với #40912, đừng để pass âm thầm |
| I7 | `[MAJOR]` | `NEW-23` (Studio 17685) | **Oracle bị nới lỏng giữa các vòng**: `fail` ở run #1383/#1396/#1400 (bản ghi kết thúc `status=3` + `ErrorException`), rồi `pass` ở #1408 sau khi ghi *"evidence downstream (KHÔNG dùng làm oracle theo bước 5)"*. TC không còn verify bản ghi kết thúc `status=2` | Ghi rõ trong TC rằng điểm kiểm `status` đã được **cố ý loại khỏi oracle** vì #40912, và gắn lại khi #40912 fix xong |
| I8 | `[MAJOR]` | 18/35 TC (51%) | Mã quan điểm **không có trong `framework/checklist-lme.md`**: `API-CONTRACT-001` (13 TC) · `TOOL-ERRHYG-001` (3) · `TOOL-KNOW-002` (1) · `API-001` (1) · `TOOL-OLDREC-001` (1) → không map được coverage, không tính cover ở §2 | Map lại sang mã chuẩn (`INTG-HOOK-001` / `INTG-LINE-001` / `DATA-TEXT-001` / `COMPAT-LEGACY-001` / `SEC-002`…), hoặc bổ sung mã mới vào checklist theo RULE-10 |
| I9 | `[MAJOR]` | Nguồn spec | **Không có spec cho Webhook転送 / forward webhook** trong [spec-features/](../../spec-features/). Studio tham chiếu `spec-webhook-forward.md:219 BR-01` — file này **không tồn tại** trong repo spec. 11 TC forward do đó không có chuẩn để đánh giá Expected | Bổ sung spec feature forward webhook (#40475) hoặc đính kèm link spec gốc vào task |
| I10 | `[MAJOR]` | `03-dev-impact.md` | Có `Auto-filled: 2026-09-14 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" chưa tick** → `F*`/`D*`/`T*` chưa ai xác nhận là đủ | Tester đọc lại Journal #136271 trên Redmine rồi tick |
| I11 | `[MAJOR]` | `F2` — 2 hàm dùng chung mới | Danh sách nơi bị ảnh hưởng chỉ gồm **4 chỗ Dev tự liệt kê**. Bằng chứng Dev đưa là grep **bên trong đúng 4 file đó** (`không có foreach ($body`, `không có $body[1]`), **không phải** grep toàn repo tìm mọi nơi đọc `callback_event.request` | Yêu cầu Dev đưa kết quả grep toàn repo cho `callback_event` / `->request` để chứng minh danh sách 4 caller là đủ |
| I12 | `[MINOR]` | `NEW-20` (17682) | `bug_tickets` **trống** dù bug #40912 ghi rõ *"Các testcase khác fail cùng nguyên nhân gốc này: 17682, 17685"* | Gắn #40912 vào `bug_tickets` của 17682 và 17685 để trace được |
| I13 | `[MINOR]` | Toàn bộ 35 TC | `spec_status = null` ở tất cả TC → không TC nào khai `Spec ghi rõ` / `Spec không ghi` / `Đã hỏi leader`. Kết hợp với I9 (thiếu spec forward) → rủi ro tự suy diễn Expected | Điền `Trạng thái đánh giá spec` cho ít nhất nhóm forward và nhóm job |
| I14 | `[NIT]` | Studio coverage | `0/2 covered` — cả `EP-24` lẫn `TICKET-40888` đều ở mức `partial` | Sau khi bổ sung TC §5, chạy lại đánh giá coverage trên Studio |

---

## 5. TCs đề xuất bổ sung (20)

> ⚠️ **Phạm vi môi trường của toàn bộ TC trong §5** (Leader xác nhận 2026-09-15):
> Task này **chỉ apply cho staging** — **production KHÔNG có** luồng này, vì chỉ staging mới nhận callback do PHP insert.
> → Cột `Phạm vi ENV` ghi `staging`; khi `/sync-review-tc` push lên Studio thì `env_scope = ["local", "dev", "staging"]` (**không** gồm `prd`) — khớp đúng `env_scope` mà 35 TC gốc đang dùng.
> → **RULE-08 không được áp theo hướng "phải test production"** cho task này; ràng buộc thật là **phải rời khỏi `local` sang `staging`**.
>
> **Điều kiện tiên quyết dùng chung cho mọi TC ở §5**: trên **LINE console** của bot thử nghiệm phải đặt webhook URL dạng
> `https://go.lmes.jp/line/callback/add/<bot_id>` — không đặt đúng domain này thì callback không về được hệ thống và mọi TC dưới đây không chạy được.

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | [`kho-tcs/fa003-tudongtraloi-自動応答.md`](../../kho-tcs/fa003-tudongtraloi-自動応答.md) (nhóm **"Runtime — nhận message & callback"**, 7 TC) · [`kho-tcs/fa001-chat11-11チャット.md`](../../kho-tcs/fa001-chat11-11チャット.md) · [`kho-tcs/fa004-richmenu-リッチメニュー.md`](../../kho-tcs/fa004-richmenu-リッチメニュー.md) · [`kho-tcs/fa015-quanlythongtinbanbe-友だち情報管理.md`](../../kho-tcs/fa015-quanlythongtinbanbe-友だち情報管理.md) |
| Vùng regression phát hiện từ kho | `TC-RPL-220` → `TC-RPL-226` — toàn bộ vòng đời `callback_event` runtime: 8 loại message hiển thị ở chat 1:1 · auto reply 全てのメッセージ · đổi domain callback `cb.lmes.jp` → `cb-2.lmes.jp` · state machine `0→1→2` · `status=10` tin nhóm · `status=8` bot hết hạn · khoá đồng thời theo `line_id`. **Tất cả 7 TC này viết cho thời "1 event / 1 bản ghi" → phải chạy lại toàn bộ với bản ghi gộp.** |
| Conflict expected vs kho | `TC-RPL-223` (kho) kỳ vọng `callback_event` kết thúc `status = 2` ⟷ `NEW-23` (Studio 17685) đã **cố ý loại điểm kiểm `status` khỏi oracle**, và 3 TC (17679/17682/17685) thực tế kết thúc `status = 3`. → `[MAJOR] SPEC-CONFLICT`, đã đưa **§6 mục 3** để Dev/Leader chốt. Không tự chọn bên |
| GAP dùng lại TC kho (không viết mới) | `G1` → chạy lại `TC-RPL-221` với bản ghi gộp · `G2` → `TC-RPL-220` · `G4` → `TC-RPL-224` + `TC-RPL-225` · `G5` → `TC-RPL-226` · `Q7` → `TC-RPL-222` (đổi domain callback — chạy trên **staging** với `go.lmes.jp`, KHÔNG phải production như kho ghi). TC dưới đây chỉ viết **phần kho chưa có**: kịch bản bản ghi gộp nhiều event |
| Xác nhận chống trùng | Đã đối chiếu **35 TC** ở BƯỚC 0 + 4 file kho — **không TC đề xuất nào trùng** |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-DEPLOYLIVE001-01 | API | DEPLOY-LIVE-001 | Runtime — nhận message & callback | Normal | manual | staging | Job bản RELEASE đọc được bản ghi định dạng mới do PHP nhánh fix ghi | - Web chạy nhánh fix `ai_small_40888`<br>- Job `linect-service` chạy **bản RELEASE hiện hành** (CHƯA merge 2 nhánh `m_202609_forward_webhook_40475_*`)<br>- Bot A liên kết LINE OA thật, 1 friend F đã kết bạn<br>- Bot A có 1 quy tắc auto reply 全てのメッセージ đang ON, action gửi text「OK」 | 1. Friend F gửi 1 tin text từ LINE app<br>2. Mở màn chat 1:1 xem tin của F<br>3. Quan sát LINE app của F<br>4. Xem trạng thái bản ghi vừa sinh ở màn theo dõi / nhờ Dev query `callback_event` | 1 tin text「テスト」 | - Tin của F hiển thị ở màn chat 1:1<br>- F nhận được「OK」trên LINE app<br>- Bản ghi kết thúc ở trạng thái đã xử lý, không kẹt ở trạng thái mới | | Lấp `G3` · `Q1` · Đánh giá spec: Spec không ghi — `job-spec.md:100` mô tả job dispatch theo cột `type` của bản ghi · Evidence: ảnh chat 1:1 + ảnh LINE app + kết quả query trạng thái · **Nếu TC này Không đạt ⇒ PHP KHÔNG được lên trước job** |
| TC-DEPLOYLIVE001-03 | API | DEPLOY-LIVE-001 | Runtime — nhận message & callback | Boundary | manual | staging | Thời điểm chuyển giao: bản ghi định dạng cũ và mới xen kẽ trong cùng một lượt xử lý | - Web đã lên nhánh fix, job đã lên nhánh hỗ trợ 2 định dạng<br>- Trong bảng còn bản ghi định dạng cũ **chưa xử lý** (nhờ Dev giữ lại vài bản ghi đang chờ trước khi deploy) | 1. Trước deploy: tạo 2-3 bản ghi định dạng cũ đang chờ xử lý<br>2. Deploy web + job<br>3. Ngay sau deploy cho friend gửi thêm 2 tin mới (sinh bản ghi định dạng mới)<br>4. Chờ job chạy hết, đối chiếu từng bản ghi | 2-3 bản ghi cũ đang chờ + 2 bản ghi mới | - **Cả bản ghi cũ lẫn mới** đều được xử lý hết, không bản ghi nào kẹt hoặc bị bỏ<br>- Friend nhận đủ phản hồi tương ứng của cả 2 nhóm | | Lấp `G3` · `G9` · `Q5` · `Q8` · Đánh giá spec: Spec không ghi · Evidence: danh sách trạng thái bản ghi trước/sau + log job · regression |
| TC-MSG004-01 | UI | MSG-004 | Runtime — nhận message & callback | Normal | manual | staging | Friend gửi 3 tin dồn dập trong 1 lần LINE gọi — nhận đủ 3 lượt trả lời tự động | - Bot A liên kết LINE OA thật, friend F đã kết bạn<br>- Quy tắc auto reply 全てのメッセージ đang ON, action gửi text「OK」, chế độ **何度でも**<br>- Job chạy nhánh đã merge | 1. Friend F gửi liên tiếp 3 tin text trong khoảng 1 giây (gõ sẵn rồi gửi nhanh)<br>2. Đếm số tin「OK」F nhận được trên LINE app<br>3. Mở màn chat 1:1 đếm số tin của F hiển thị | 3 tin text「あ」「い」「う」gửi trong ~1 giây | - F nhận **đúng 3 tin「OK」** trên LINE app<br>- Màn chat 1:1 hiển thị **đủ 3 tin** của F, đúng thứ tự<br>- Không tin nào bị mất | | Lấp `G1` · `Q2` · Đánh giá spec: Đã hỏi leader · Evidence: ảnh LINE app đếm được 3 tin trả lời + ảnh chat 1:1 · RULE-06 đi tới output cuối · dẫn từ `TC-RPL-221` |
| TC-MSG004-02 | UI | MSG-004 | Runtime — nhận message & callback | Abnormal | manual | staging | Bản ghi mở đầu bằng thông báo đã gửi (delivery) — trả lời tự động vẫn chạy đúng người | - Như TC-MSG004-01<br>- Dựng được lần gọi mà event đầu tiên là `delivery` của friend khác (nhờ Dev bắn body có chữ ký hợp lệ nếu LINE không tự sinh) | 1. Tạo lần gọi gộp: `delivery` của friend A → tin text của friend B<br>2. Quan sát LINE app của B<br>3. Quan sát LINE app của A<br>4. Mở màn chat 1:1 của cả A và B | Gói 2 event: `delivery` (A) → `message` (B) | - **B** nhận được「OK」<br>- **A KHÔNG** nhận tin nào và dữ liệu của A không đổi<br>- Chat 1:1 của B có tin mới, của A không có | | Lấp `G1` · `Q2` · Đánh giá spec: Đã hỏi leader · Evidence: ảnh LINE app của cả A và B (đối chứng âm) · RULE-06 |
| TC-MSG004-03 | UI | MSG-004 | Rich Menu — action khi bấm nút | Normal | manual | staging | Bấm Rich Menu trong lần gọi gộp nhiều event — action richmenu vẫn được thực thi | - Bot A có Rich Menu đang áp dụng, 1 area có action エルメ (gửi text + gắn tag)<br>- Friend F đã kết bạn<br>- Job chạy nhánh đã merge | 1. Friend F gửi 1 tin text rồi bấm ngay nút Rich Menu (trong ~1 giây)<br>2. Quan sát LINE app của F<br>3. Mở tab タグ管理 của F ở màn friend<br>4. Mở màn chat 1:1 của F | 1 tin text + 1 lượt bấm Rich Menu liền nhau | - F nhận đủ tin của action richmenu<br>- Tag của action được gắn cho F<br>- Chat 1:1 hiện cả tin của F lẫn tin action trả về | | Lấp `G11` · `Q2` · Đánh giá spec: Spec không ghi · Evidence: ảnh LINE app + ảnh tab タグ管理 · RULE-06 · dẫn từ `TC-RM-147` |
| TC-MSGUSER001-01 | UI | MSG-USER-001 | Chat 1:1 — nhận tin từ bạn bè | Normal | manual | staging | 3 tin trong 1 lần gọi hiển thị đủ ở chat 1:1 và bộ đếm chưa xác nhận tăng đúng 3 | - Bot A, friend F đã kết bạn, hội thoại F đang ở trạng thái **đã xác nhận hết** (bộ đếm = 0)<br>- Bot A **KHÔNG** có quy tắc auto reply nào ON (để cô lập biến)<br>- Ghi lại số ở badge chưa xác nhận trước khi test | 1. Ghi lại bộ đếm chưa xác nhận của F và tổng của bot<br>2. Friend F gửi 3 tin text dồn dập trong ~1 giây<br>3. Mở màn chat 1:1, đếm số bong bóng tin của F<br>4. Đọc lại badge chưa xác nhận của F và tổng của bot | 3 tin text gửi trong ~1 giây | - Chat 1:1 hiển thị **đủ 3 tin**, đúng thứ tự, đúng nội dung<br>- Badge chưa xác nhận của F **tăng đúng 3**<br>- Tổng số bạn bè chưa xác nhận của bot tăng đúng 1 (F chuyển từ đã xác nhận sang chưa) | | Lấp `G2` · `Q2` · `Q11` · Đánh giá spec: Đã hỏi leader · Evidence: ảnh chat 1:1 đếm được 3 bong bóng + ảnh badge trước/sau · RULE-06 + RULE-07 · dẫn từ `TC-RPL-220` |
| TC-MSGUSER001-02 | UI | MSG-USER-001 | Friend info — kết bạn / hủy kết bạn | Normal | manual | staging | Bản ghi gộp [thông báo đã gửi, kết bạn] — đúng người được ghi nhận kết bạn và chạy action kết bạn | - Bot A có cấu hình あいさつメッセージ + action kết bạn (gắn tag T1)<br>- Friend mới N **chưa từng** kết bạn với bot A<br>- Ghi lại danh sách bạn bè và tag của các friend hiện có | 1. Dựng lần gọi gộp: `delivery` của friend A → `follow` của friend mới N<br>2. Mở màn danh sách bạn bè tìm N<br>3. Mở tab タグ管理 của N<br>4. Đối chiếu lại friend A: tag và trạng thái kết bạn có đổi không | Gói 2 event: `delivery` (A) → `follow` (N) | - **N** xuất hiện trong danh sách bạn bè, nhận được あいさつメッセージ, được gắn tag T1<br>- **A** giữ nguyên hoàn toàn: không bị gắn T1, không bị đổi trạng thái kết bạn | | Lấp `G7` · `Q10` · Đánh giá spec: Đã hỏi leader · Evidence: ảnh danh sách bạn bè + ảnh tab タグ管理 của cả N và A · **Nhánh follow hiện 0 TC** — `NEW-17` chỉ chọn nhánh unfollow làm oracle |
| TC-CONC001-01 | API | CONC-001 | Runtime — nhận message & callback | Abnormal | manual | staging | Bản ghi chứa event của 2 bạn bè khác nhau — người thứ 2 không bị bỏ sót do khoá theo bạn bè | - Bot A, 2 friend B và C đã kết bạn<br>- Quy tắc auto reply 全てのメッセージ ON, action gửi text「OK」<br>- Job chạy nhánh đã merge | 1. Dựng lần gọi gộp: tin của B → tin của C<br>2. Đồng thời cho C gửi thêm 1 tin riêng ở lần gọi khác, cách ~0.2 giây<br>3. Đếm số tin「OK」mỗi người nhận được<br>4. Mở chat 1:1 của B và C đếm tin | Gói 2 event (B, C) + 1 event riêng của C gần như đồng thời | - B nhận đúng 1「OK」, C nhận đúng 2「OK」<br>- Chat 1:1 của B có 1 tin, của C có 2 tin<br>- Không tin nào của C bị nuốt do bản ghi kia đang giữ khoá | | Lấp `G5` · `Q3` · Đánh giá spec: Spec không ghi — kho `TC-RPL-226` (BR-22 khoá theo `line_id`) viết cho thời 1 event/bản ghi · Evidence: ảnh LINE app của B và C + log job · dẫn từ `TC-RPL-226` |
| TC-JOB001-01 | API | JOB-001 | Runtime — nhận message & callback | Abnormal | manual | staging | Bản ghi gộp [tin trong nhóm, tin 1:1] — tin 1:1 không bị bỏ theo trạng thái bỏ qua tin nhóm | - Bot A đã được thêm vào 1 group LINE<br>- Friend F đã kết bạn 1:1 với bot A<br>- Quy tắc auto reply 全てのメッセージ ON | 1. Dựng lần gọi gộp: tin trong group → tin 1:1 của F<br>2. Quan sát group trên LINE app<br>3. Quan sát LINE app của F<br>4. Mở chat 1:1 của F | Gói 2 event: `message` (nguồn group) → `message` (nguồn user F) | - Bot **không** trả lời vào group<br>- F **vẫn** nhận được「OK」và tin của F **vẫn** hiển thị ở chat 1:1<br>- Tin 1:1 không bị bỏ cùng với tin nhóm | | Lấp `G4` · `Q6` · Đánh giá spec: Spec không ghi · Evidence: ảnh group + ảnh LINE app của F + ảnh chat 1:1 · dẫn từ `TC-RPL-224` |
| TC-JOB001-02 | API | JOB-001 | Runtime — nhận message & callback | Abnormal | manual | staging | Bot hết hạn hợp đồng với bản ghi gộp — toàn bộ event dừng đúng, không xử lý một phần | - Bot B có hợp đồng đã hết hạn > 7 ngày<br>- Bot B còn quy tắc auto reply ON, còn friend đã kết bạn | 1. Dựng lần gọi gộp 2 event cho bot B: tin của friend D → bấm nút của friend E<br>2. Quan sát LINE app của D và E<br>3. Mở chat 1:1 của D và E | Gói 2 event cho bot đã hết hạn | - **Không** friend nào nhận tin trả về<br>- Cả 2 event dừng **cùng một cách**, không có chuyện 1 event chạy 1 event dừng | | Lấp `G4` · `Q6` · Đánh giá spec: Spec không ghi · Evidence: ảnh LINE app của D và E + trạng thái bản ghi · dẫn từ `TC-RPL-225` |
| TC-DATATEXT001-01 | Data | DATA-TEXT-001 | Webhook転送 — chuyển tiếp ra hệ thống ngoài | Normal | manual | staging | Emoji và tiếng Nhật nguyên dạng ở CẢ cột lưu nội dung LẪN body bên thứ 3 | - Đã cấu hình chuyển tiếp webhook sang 1 endpoint bên thứ 3 đọc được raw body<br>- **Trên staging**: webhook URL của bot đặt `https://go.lmes.jp/line/callback/add/&lt;bot_id&gt;` và domain forward đã được config đầy đủ (điều kiện tester ghi thiếu ở lần chạy local)<br>- Friend F đã kết bạn | 1. Friend F gửi tin chứa tiếng Nhật + emoji + ký tự đặc biệt<br>2. Đọc raw body bên thứ 3 nhận được<br>3. Nhờ Dev đọc nội dung cột lưu của bản ghi tương ứng<br>4. So sánh từng ký tự giữa 2 nơi và với tin gốc trên LINE | Tin「テスト🎉メッセージをご確認ください」+ 1 tin chỉ emoji | - Bên thứ 3 nhận **đúng nguyên văn**, không escape<br>- Cột lưu nội dung cũng **đúng nguyên văn, KHÔNG ở dạng `\uXXXX`**<br>- 2 nơi khớp nhau từng ký tự | | Lấp `G8` · `Q13` · Đánh giá spec: Đã hỏi leader · Evidence: raw body bên thứ 3 + ảnh nội dung cột lưu · **TC gốc `NEW-28` đã pass dù chính note ghi DB chưa decode emoji** — xem §4 I1 |
| TC-INTGLINE001-01 | UI | INTG-LINE-001 | Runtime — nhận message & callback | Normal | manual | staging | Ma trận 7 loại tin nhắn — lưu nguyên văn và hiển thị đúng dạng ở chat 1:1 | - Bot A liên kết LINE OA thật, friend F đã kết bạn<br>- Bot A **KHÔNG** có quy tắc auto reply nào ON (cô lập biến)<br>- Job chạy nhánh đã merge | 1. Friend F lần lượt gửi 7 loại: text · **sticker** · **vị trí** · ảnh · video · **âm thanh** · file<br>2. Sau mỗi lần mở màn chat 1:1 kiểm tra tin hiển thị đúng dạng<br>3. Nhờ Dev đọc nội dung đã lưu của 7 bản ghi tương ứng | 7 loại tin nhắn LINE | - Cả 7 loại hiển thị đúng dạng ở chat 1:1 (ảnh xem được, video phát được, âm thanh nghe được, file tải được, vị trí hiện bản đồ, sticker hiện đúng hình)<br>- Nội dung lưu giữ nguyên văn body LINE gửi cho cả 7 loại | | Lấp `G13` · `Q15` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh chat 1:1 của cả 7 loại · **sticker · vị trí · âm thanh hiện 0 TC** · dẫn từ `TC-RPL-220` |
| TC-INTGLINE001-02 | UI | INTG-LINE-001 | Chat 1:1 — nhận tin từ bạn bè | Abnormal | manual | staging | Friend thu hồi tin nhắn (unsend) — chat 1:1 và tin nhắn cuối cập nhật đúng | - Bot A, friend F đã kết bạn<br>- F vừa gửi 2 tin text, cả 2 đã hiển thị ở chat 1:1<br>- Ghi lại nội dung tin nhắn cuối của F ở danh sách bạn bè | 1. Trên LINE app, F thu hồi tin vừa gửi<br>2. Mở màn chat 1:1 của F<br>3. Đọc dòng tin nhắn cuối của F ở danh sách bạn bè<br>4. Thử tiếp: F gửi 1 tin rồi thu hồi **ngay** (trong ~1 giây, để LINE gộp `message` + `unsend` vào cùng 1 lần gọi) | 2 tin text + 1 lượt thu hồi · sau đó 1 cặp gửi-rồi-thu-hồi liền nhau | - Chat 1:1 hiển thị tin **đã bị thu hồi** đúng theo hành vi hiện có<br>- Tin nhắn cuối ở danh sách bạn bè cập nhật đúng<br>- Ở bước 4: **cả 2 event `message` và `unsend` đều được xử lý**, không rơi event nào | | Lấp `G13` · `G15` · `Q15` · `Q17` · Đánh giá spec: Spec không ghi · Evidence: ảnh chat 1:1 trước/sau + ảnh dòng tin nhắn cuối · **`unsend` hiện 0 TC**; `HandleCallback` không khai `unsend` trong danh sách loại |
| TC-INTGLINE001-03 | UI | INTG-LINE-001 | Runtime — nhận message & callback | Abnormal | manual | staging | OA trong nhóm — nhận tin và thu hồi tin trong nhóm, thông tin nhóm cập nhật ở chat 1:1 | - Bot A đã được thêm vào 1 group LINE có ≥2 thành viên<br>- Friend F cũng kết bạn 1:1 với bot A | 1. Một thành viên gửi tin trong nhóm → mở chat 1:1 xem dòng của nhóm<br>2. Thành viên đó thu hồi tin trong nhóm → xem lại<br>3. Thêm 1 thành viên mới vào nhóm, rồi cho 1 thành viên rời nhóm<br>4. Quan sát dòng của nhóm ở danh sách | Tin trong nhóm · thu hồi trong nhóm · thành viên vào/rời nhóm | - Dòng của nhóm ở chat 1:1 cập nhật đúng sau mỗi thao tác<br>- Thao tác thành viên vào/rời nhóm **không gây lỗi và không làm kẹt** bản ghi<br>- Tin 1:1 của F trong cùng khoảng thời gian không bị ảnh hưởng | | Lấp `G13` · `Q15` · Đánh giá spec: Spec không ghi · Evidence: ảnh dòng nhóm ở chat 1:1 qua từng bước · `memberJoined` / `memberLeft` **không nằm trong 8 nhánh dispatch của job** |
| TC-INTGLINE001-04 | API | INTG-LINE-001 | Webhook LINE EP-24 | Abnormal | manual | staging | Loại callback ngoài danh sách xử lý — lưu được, không lỗi máy chủ, không kẹt bản ghi | - Web chạy nhánh fix<br>- Dựng được lần gọi có chữ ký hợp lệ mang loại event lạ (nhờ Dev bắn body theo tài liệu LINE) | 1. Gửi lần lượt 3 lần gọi mang loại `memberJoined` · `memberLeft` · `accountLink`<br>2. Đọc mã phản hồi mỗi lần<br>3. Nhờ Dev đọc loại và định danh của bản ghi sinh ra<br>4. Chạy tiến trình xử lý, xem bản ghi kết thúc ở trạng thái nào<br>5. Đọc nhật ký tìm lỗi | 3 loại event ngoài danh sách `findFirstEvent` của PHP | - Không lần nào gây lỗi máy chủ<br>- Bản ghi được lưu nguyên văn<br>- Bản ghi **không kẹt vĩnh viễn** ở trạng thái chờ — kết thúc ở một trạng thái xác định và ghi lại trạng thái đó để Leader chốt | | Lấp `G13` · `Q15` · Đánh giá spec: Đã hỏi leader — cần Dev xác nhận hành vi mong muốn · Evidence: mã phản hồi + loại/trạng thái bản ghi + trích nhật ký |
| TC-INTGLINE001-05 | UI | INTG-LINE-001 | Rich Menu — action khi bấm nút | Normal | manual | staging | 3 nguồn postback — rich menu · button template · **image map** đều thực thi action | - Bot A có Rich Menu đang áp dụng (1 area có action), 1 template có button action, 1 image map có vùng action<br>- Friend F đã kết bạn | 1. F bấm nút Rich Menu → quan sát LINE app + chat 1:1<br>2. F bấm button trên template đã nhận → quan sát<br>3. F bấm vùng action trên image map đã nhận → quan sát | 3 nguồn postback khác nhau | - Cả 3 nguồn: action được thực thi đúng, F nhận đủ kết quả trên LINE app<br>- Chat 1:1 hiện tin trả về tương ứng | | Lấp `G13` · `Q2` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh LINE app của cả 3 lượt bấm · **image map hiện 0 TC** · dẫn từ `TC-RM-147` |
| TC-INTGLINE001-06 | UI | INTG-LINE-001 | Runtime — nhận message & callback | Normal | manual | staging | Xem hết video có gắn theo dõi — action được thực thi | - Bot A đã gửi cho friend F 1 tin video **có gắn theo dõi lượt xem**, video đó có cấu hình action khi xem hết<br>- Job chạy nhánh đã merge | 1. F mở và xem **hết** video trên LINE app<br>2. Quan sát LINE app của F<br>3. Mở chat 1:1 của F | 1 video có gắn theo dõi, xem đến hết | - Action cấu hình cho "xem hết video" được thực thi, F nhận đủ kết quả<br>- Chat 1:1 hiện tin trả về tương ứng | | Lấp `G13` · `Q2` · Đánh giá spec: Spec không ghi · Evidence: ảnh LINE app + ảnh chat 1:1 · `NEW-32` mới chỉ kiểm forward, **chưa kiểm action** |
| TC-SYNCAPP001-01 | UI | SYNC-APP-001 | Runtime — nhận message & callback | Normal | manual | staging | Thông báo mobile / PC / Chatwork không bị mất khi một lần gọi gộp nhiều event | - Bot A đã bật thông báo cho **cả 3 kênh**: app mobile LME · trình duyệt PC · Chatwork<br>- Tài khoản quản trị đang đăng nhập app mobile và mở trình duyệt PC<br>- 2 friend B và C đã kết bạn | 1. Ghi lại số thông báo hiện có ở cả 3 kênh<br>2. Dựng lần gọi gộp: tin của B → kết bạn mới của C<br>3. Đếm thông báo nhận được ở app mobile<br>4. Đếm thông báo trên trình duyệt PC<br>5. Mở phòng Chatwork đếm tin thông báo | Gói 2 event: `message` (B) → `follow` (C) | - **Cả 3 kênh** đều nhận **đủ 2 thông báo** (1 cho tin của B, 1 cho việc C kết bạn)<br>- Không kênh nào chỉ nhận 1 thông báo | | Lấp `G14` · `Q16` · Đánh giá spec: Đã hỏi leader · Evidence: ảnh màn thông báo của cả 3 kênh · RULE-06 đi tới output cuối · **Dev KHÔNG kê notify ở mục 4.3 file 03** |
| TC-DATA001-01 | UI | DATA-001 | Chat 1:1 — danh sách bạn bè | Normal | manual | staging | Một lần gọi gộp 3 tin — tin nhắn cuối, thời điểm và thứ tự danh sách bạn bè đều đúng | - Bot A có ≥3 friend đang có hội thoại<br>- Friend F hiện **KHÔNG** ở đầu danh sách bạn bè<br>- Ghi lại thứ tự danh sách và nội dung tin nhắn cuối của F | 1. Friend F gửi 3 tin text dồn dập trong ~1 giây, tin cuối có nội dung riêng biệt dễ nhận<br>2. Mở lại danh sách bạn bè<br>3. Đọc dòng tin nhắn cuối và thời điểm của F<br>4. Kiểm vị trí của F trong danh sách | 3 tin, tin thứ 3 là「最後のメッセージ」 | - Tin nhắn cuối của F = **nội dung tin thứ 3**, không phải tin thứ 1<br>- Thời điểm tin nhắn cuối cập nhật theo tin thứ 3<br>- F **nhảy lên đầu** danh sách bạn bè | | Lấp `G15` · `Q17` · Đánh giá spec: Đã hỏi leader — liên quan `MT-01` đang mở của kho FA-001 · Evidence: ảnh danh sách bạn bè trước/sau · dẫn từ `TC-CHT-164` |
| TC-COMPATLEGACY001-01 | API | COMPAT-LEGACY-001 | Tiến trình PHP xử lý callback_event | Normal | manual | staging | Bản ghi postback giả lập định dạng cũ chạy qua tiến trình dòng lệnh postback | - Web chạy nhánh fix<br>- Khách bấm nút trên giao diện di động để hệ thống tự sinh bản ghi định dạng cũ qua `storeFakeCallbackEvent` (KHÔNG tạo tay) | 1. Khách bấm 1 nút có action trên giao diện di động<br>2. Xác nhận bản ghi vừa sinh ở **định dạng cũ**<br>3. Chạy tiến trình dòng lệnh xử lý postback (không phải endpoint API)<br>4. Quan sát action có được thực thi không | 1 lượt bấm nút thật trên giao diện di động | - Action của nút được thực thi đúng<br>- Bản ghi kết thúc ở trạng thái đã xử lý<br>- Kết quả **giống hệt** trước khi có thay đổi | | Lấp `G10` · `Q9` · Đánh giá spec: Spec không ghi · Evidence: ảnh kết quả action + trạng thái bản ghi · regression — `NEW-21` chỉ đi qua endpoint API, chưa qua tiến trình dòng lệnh |

**Dòng §1/§2 không đề xuất TC mới:**

> 📌 **Leader đã quyết bỏ 7 TC** khỏi bản đề xuất ban đầu (2026-09-15): `TC-DEPLOYLIVE001-02` · `TC-REGRUN001-01` · `TC-MSGUSER001-03` · `TC-CONC001-02` · `TC-SEC002-01` · `TC-SEC002-02` · `TC-OUTTRUTH001-01`. Hệ quả ghi lại bên dưới để trace được, **không tự thêm lại**.

- `G6` + `Q4 — SEC-002` (xác thực chữ ký `X-Line-Signature`): **Leader quyết KHÔNG bổ sung TC**. Lý do chấp nhận được: xác thực chữ ký là **hành vi có sẵn trước ticket**, không nằm trong phần logic mà #40888 thay đổi (fix chỉ đổi cách **ghi** và cách **chọn event đại diện**). ⚠️ Ghi nhận đánh đổi: sau quyết định này **`G6` và `Q4` không còn TC nào**, nên nếu 114 dòng sửa ở `BotController.php` có vô tình chạm nhánh chữ ký thì bộ TC hiện tại không bắt được.
- `G12` + `Q12 — OUT-TRUTH-001` (body vượt sức chứa cột → phản hồi trả về LINE): **Leader quyết KHÔNG bổ sung TC**. `NEW-14` đã kiểm được phần quan trọng nhất (có log cảnh báo, không lộ nội dung tin nhắn). ⚠️ Phần chưa ai kiểm: LINE nhận mã phản hồi gì khi nội dung đã bị cắt cụt — nếu trả thành công thì LINE không gửi lại và event mất vĩnh viễn. Rủi ro này đã được chuyển thành **mục 4 của §6** (chốt đổi cột sang `MEDIUMTEXT`) thay vì test.
- `Q14 — FUNC-004 (RULE-01)`: không viết TC mới. `NEW-13`/`NEW-14` đã là 2 `Boundary` tốt; chỉ cần **bổ sung điểm kiểm `Normal`** (body kích thước thường lưu đủ) vào `NEW-13`.
- `G7` / `Q9` / `Q10` — **chạy lại `NEW-17` · `NEW-20` · `NEW-23` sau khi #40912 fix** (kèm gắn lại điểm kiểm trạng thái đã bị gỡ khỏi oracle của `NEW-23`): đây là **việc phải làm khi retest**, không phải TC mới → theo dõi ở §4 `I2` / `I7`, không nằm trong bảng §5.
- `G5` / `Q3` — **xác nhận 3 tiến trình `handle:callback` / `handle:message` / `handle:postback` đã dừng hẳn** (rủi ro R2 Dev tự nêu): đây là **câu hỏi xác nhận với Dev/vận hành**, không phải TC → Leader hỏi trực tiếp. Phần test được của `Q3` vẫn còn `TC-CONC001-01`.
- `G9` / `Q5 — REG-RUN-001`: không còn TC riêng; kịch bản bản ghi tồn đọng lúc deploy đã nằm trong **`TC-DEPLOYLIVE001-03`** (bước 1 giữ lại bản ghi đang chờ trước khi deploy).
- `G3` / `Q1 — DEPLOY-LIVE-001`: còn **`TC-DEPLOYLIVE001-01`** (job bản release đọc bản ghi định dạng mới) và **`TC-DEPLOYLIVE001-03`** (cũ + mới xen kẽ) — vẫn đủ chặn rủi ro deploy lệch pha.

- `G8 / Q7 (ENV-003)`: không viết TC mới cho từng TC forward — **chạy lại nguyên bộ 11 TC forward (`NEW-28`…`NEW-38`) trên staging** sau khi domain callback `go.lmes.jp` và domain forward được config, cộng `TC-DATATEXT001-01` cho điểm kiểm DB đang bị bỏ. **Không** cần bản production.
- `Q6 (JOB-001) state machine`: dùng lại `TC-RPL-223` của kho (`0→1→2`), chạy lại với bản ghi gộp — không viết trùng.

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | [`spec-features/admin/auto-reply/job/job-spec.md:95-120`](../../spec-features/admin/auto-reply/job/job-spec.md) | Spec ghi job dispatch `switch callbackEvent.getType()` — **theo cột `type` của bản ghi**, và "lock theo `line_id`" (BR-22). Sau #40888 một bản ghi chứa **nhiều event nhiều `line_id`**, còn 2 nhánh dev đã đổi sang dispatch **theo từng event** (`handleCallbackEventByType`). Cần cập nhật spec + ghi rõ **từ phiên bản job nào** hành vi mới có hiệu lực | `TC vs spec` — Journal #136271 (bằng chứng dòng 416-470 branch `b47e086`) vs `job-spec.md:100` | `Dev` + `Leader` |
| 2 | Thiếu hẳn — không có feature spec cho **Webhook転送 / Forward webhook (#40475)** | Studio tham chiếu `spec-webhook-forward.md:219 BR-01` nhưng file **không tồn tại** trong [spec-features/](../../spec-features/). 11 TC forward đang không có chuẩn Expected. Cần bổ sung spec: định dạng body gửi ra, có bọc envelope không, xử lý retry, cấu hình domain | `Input thiếu spec` — §4 I9 | `PM` + `Dev` |
| 3 | `callback_event` state machine — giá trị trạng thái kết thúc | Kho `TC-RPL-223` kỳ vọng bản ghi kết thúc **trạng thái đã xử lý**; TC Studio `NEW-23` đã **cố ý loại điểm kiểm này khỏi oracle** và 3 TC thực tế kết thúc ở **trạng thái lỗi** do #40912. Cần chốt: trạng thái kết thúc đúng là gì, và khi nào gắn lại điểm kiểm | `TC vs kho-tcs` — `TC-RPL-223` vs `NEW-23` (Studio 17685) | `Leader` |
| 4 | `callback_event.request` — kiểu cột | Cột kiểu `TEXT` (~65.535 byte), body lớn hơn bị **cắt cụt âm thầm**. Dev chỉ thêm log cảnh báo. Cần chốt: đổi sang `MEDIUMTEXT` hay chấp nhận rủi ro, và phản hồi trả về LINE trong trường hợp đó | `Dev tự nêu` (mục R3 file 03) + TC 17676 đề xuất nâng kiểu cột | `Dev` + `Leader` |
| 5 | `callback_event.webhook_event_id` — chống trùng khi LINE gửi lại | PHP **vẫn không ghi** cột này → cơ chế chống trùng redelivery của job chưa kích hoạt cho bản ghi do PHP tạo. `NEW-11` chỉ "ghi nhận hiện trạng". 1 bản ghi nay chứa nhiều event với nhiều mã khác nhau → cần chốt cách định danh | `Dev tự nêu` (mục R4 file 03) — khai là ngoài phạm vi ticket | `Leader` — quyết định có tách ticket riêng |
| 6 | `bots.is_verify` — điều kiện đánh dấu đã xác thực | TC 17670 ghi nhận: `is_verify` **bị đặt = 1 khi body không đọc được**. Tester ghi "hành vi có từ trước ticket này, để leader quyết". Chưa có spec mô tả | `TC vs spec` — TC 17670 actual | `Leader` |
| 7 | Chat 1:1 — bộ đếm chưa xác nhận khi 1 lần gọi gộp nhiều tin | `conversation.confirm_count` tăng theo **mỗi tin friend gửi**. Nay nhiều tin nằm chung 1 bản ghi → cần chốt bộ đếm phải tăng theo **số event** chứ không phải số bản ghi. Liên quan `MT-15` đang mở của kho FA-001 | `Suy từ diff code` — chưa TC nào phủ (G2 / Q11) | `Dev` + `Leader` |
