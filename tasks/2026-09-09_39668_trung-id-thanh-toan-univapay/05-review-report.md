# 05 — Review Report

## 0. Nguồn TC

- **Nguồn đã dùng**: MCP LME TEST STUDIO — task #93 (`ticket_id=39668`, round 1, branch `ai_small_39668`, `aiResult=fail`, `reviewState=leader`, chưa reviewed).
- **Tổng số TC review**: 29.

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: 9/16 vùng ảnh hưởng đủ TC · **3 GAP** · **4 RISK**.

| # | Chiều | Vùng ảnh hưởng | Tình trạng TC hiện có | Status | Severity |
|---|---|---|---|---|---|
| **G1** | dev-impact | `D2` `payment_detail_aff` + `T3` Affiliate Reward (FA-027) / `T4` Affiliate Payment Mgmt (FS-013) — webhook trùng nhân đôi bản ghi hoa hồng | Chỉ **NEW-29** (`id=11960`) — TC stub, **chưa chạy**, không có `viewpoint`/`case_type`/`screen`/`requirement_keys` (xem §4 #8) | **GAP** | `[BLOCKER]` |
| **G2** | diff code | `F3` `isUnivaChargeRecorded` — **lớp chốt thứ hai** (chốt claim thắng nhưng lịch sử đã có → vẫn phải bỏ qua) | **NEW-27** (`11482`) là TC **duy nhất** đi qua đường này và **FAIL**. Bug #664 → Redmine #40087 đã bị **rejected** không giải thích, bằng chứng trong bug tự mâu thuẫn (xem §4 #3) | **GAP** | `[BLOCKER]` |
| **G3** | dev-impact + diff code | `D3` `bot_contracts.expired_date_contract` — **cộng dồn hạn thêm 1 kỳ** ở nhánh gia hạn tự động (`handleCallbackBillJob` / `handleExtendContractSuccess`) | NEW-13 (`11468`) chỉ test **lần gửi ĐẦU**. Kiểm "hạn không đổi khi gửi lại" chỉ có ở nhánh **chuyển khoản** (NEW-9) — không phải nhánh Dev chỉ đích danh là rủi ro nhất | **GAP** | `[BLOCKER]` |
| **G4** | diff code | 5 nhóm `actionBill` dùng chung chốt — Dev + Studio `dev_impact` đều nêu `REG-SHARED-001` | Lần gửi ĐẦU: đủ 5/5 (NEW-13→17). Lần gửi **TRÙNG**: chỉ 2/5 (`bill_max_friend` NEW-18, `transfer` NEW-9). Thiếu `bill_job`, `new/upgrade_pro`, `change_card/extend_contract/recontractV2`, `bill_again` | **RISK** | `[MAJOR]` |
| **G5** | diff code | Deploy thiếu migration → theo Studio `dev_impact`, hệ quả là **chặn TOÀN BỘ thanh toán UnivaPay**, không chỉ lọt bug cũ | NEW-26 (`11481`) có cover, nhưng `env_scope = local` và chỉ chạy ở `local` — không kết luận được cho production (**RULE-08**) | **RISK** | `[MAJOR]` |
| **G6** | dev-impact | `T2` Payment History (FS-009) — màn quản trị + `EP-11` `POST /ajax/point-settings/payment-history` (API cũ song song `EP-04`, Studio coverage `level=none`) | 0 TC. **KHÔNG đề xuất TC bổ sung**: đây là tầng ĐỌC, diff không chạm (`spec_delta.files` chỉ có `BotController.php` + migration). Fix chỉ làm **giảm** số dòng trùng, không đổi shape dữ liệu → không có đường sinh regression. Ghi lại để Leader biết là quyết định có chủ ý, không phải bỏ sót | **RISK** | `[MINOR]` |
| **G7** | dev-impact | Dữ liệu trùng **phát sinh trước fix** (mục 5 RECOVER DATA) — NEW-28 (`11483`) đã định lượng: **65 mã giao dịch trùng · 202 bản ghi thừa · 65 tài khoản** | Có TC rà soát (pass) nhưng **chưa có quyết định dọn**, và mâu thuẫn với thông báo đã gửi khách (xem §6) | **RISK** | `[MAJOR]` |

> **Chiều (b) `diff code`** lấy từ Studio tab Thông tin: `diffAvailable = true`, `diffStat` = `BotController.php +148` · `2026_08_18_000000_create_univapay_webhook_processed_table.php +41` (2 file, 189 dòng thêm). Cả 2 file đều có ≥ 1 TC đi qua → câu hỏi adversarial #1 **đạt**. Điều kiện mới đều có TC cho cả 2 nhánh (claim thắng/thua NEW-1/2 · reclaim >900s/<900s NEW-4/5 · release gỡ/giữ NEW-10/11 · `statusCharge` khác `successful` NEW-7 · thiếu mã giao dịch NEW-6) → câu hỏi #2 **đạt**, fix **không** thuộc dạng generic-catch nên không áp rule ≥ 3 trigger. Câu hỏi #3 (hàm dùng chung) → Dev **có** kê đủ 5 nhánh nhưng TC chỉ test trùng ở 2/5 → **G4**. Câu hỏi #4 (hành vi đổi) → có cả TC hành vi mới lẫn TC chỗ cũ không hỏng (NEW-3 hai mã khác nhau · NEW-8 chuyển khoản · NEW-21 hợp đồng năm 13 bản ghi) → **đạt**.
>
> ⚠️ **Sai lệch phạm vi diff**: mục 6 VERIFY của Dev ghi `1 file changed, 40 insertions(+)` nhưng diff thật là **2 file / 189 dòng**. Xem §4 #7.

---

## 2. Thiếu so với quan điểm test

**Kết luận**: 9/17 quan điểm Trigger khớp đã cover đủ · **8 quan điểm thiếu**.

| # | Mã quan điểm | Ưu tiên | Trigger khớp vì | Thiếu gì | Severity |
|---|---|---|---|---|---|
| **Q1** | `DATA-DB-001` | Cao | Fix có **DELETE** (`releaseUnivaChargeClaim` xoá dòng chốt) và **UPDATE** (reclaim quá 900 giây) | Không TC nào kiểm **phạm vi `WHERE` trên 2 tài khoản**. NEW-3 có 2 mã giao dịch nhưng **cùng 1 hợp đồng** → chưa chứng minh gỡ/nhận-lại chốt của giao dịch A không đụng dòng của tài khoản B | `[BLOCKER]` |
| **Q2** | `ENV-003` | Cao | Task chạm **bill tiền** + **webhook cổng ngoài** + **job nền** | **0 TC chạy production**; 27/29 chạy `local`, 2 TC chưa chạy. TC duy nhất khai `env_scope` có `prd` là NEW-12 nhưng **chưa ai chạy**. Theo **RULE-08** không được kết luận từ local | `[MAJOR]` |
| **Q3** | `SEC-002` | Cao | Endpoint webhook công khai xử lý **payment**; fix mới **giành chốt theo `univa_charge_id` do bên ngoài gửi** | Không TC nào verify **xác thực nguồn webhook**. Nếu endpoint không xác thực, bên thứ ba gửi trước một `charge_id` sẽ **chiếm chốt**, khiến webhook thật sau đó bị bỏ qua → mất bản ghi thanh toán thật. NEW-6 cho thấy payload tuỳ ý vẫn nhận HTTP 200 | `[MAJOR]` |
| **Q4** | `JOB-001` + `PAY-BATCH-001` | Cao | `AutoPaymentJobUnivapay` (06:30 hằng ngày, 5 phase) là **nguồn phát sinh webhook hàng loạt**; `univapay:check_status_webhook` (01:00) kiểm webhook | 0 TC chạy job thật. Toàn bộ TC gửi webhook thủ công từng cái — chưa chứng minh chốt chịu được **lô nhiều hợp đồng cùng lúc** | `[MAJOR]` |
| **Q5** | `CONC-001` | Cao | Fix chính là chốt chống race trên hành động quan trọng | Chỉ **1 TC** (NEW-2, Abnormal, 2 request). **RULE-01** thiếu Normal + Boundary; thiếu ngưỡng **N > 2 request đồng thời** | `[MAJOR]` |
| **Q6** | `INTG-HOOK-001` | Cao | Nhận webhook từ cổng thanh toán ngoài | Chỉ **1 TC** (NEW-9, Abnormal). Thiếu: sự kiện đến **không đúng thứ tự**, event khác `charge_finished` mang cùng mã giao dịch, cổng gửi lại sau nhiều giờ | `[MAJOR]` |
| **Q7** | `REG-SHARED-001` | Cao | Studio `dev_impact` nêu đích danh: lỗi ở `claimUnivaChargeEvent`/`isUnivaChargeRecorded` **chặn nhầm TẤT CẢ** loại thanh toán UnivaPay | 5 TC (NEW-13→17) **đều `Normal`** — **RULE-01** thiếu Abnormal/Boundary. Chiều Abnormal chính là **G4** | `[MAJOR]` |
| **Q8** | `DATA-BACKUP-001` | Cao | Release **thêm bảng DB mới** `univapay_webhook_processed` | 0 TC. Chưa xác nhận bảng mới có nằm trong phạm vi backup / copy bot hay không — nếu copy bot mang theo dòng chốt cũ thì bot mới bị bỏ qua giao dịch thật | `[MAJOR]` |

> **Quan điểm đã cover đủ** (không ghi chi tiết): `FUNC-004` (ngưỡng 900 giây — NEW-4/5 đủ 2 phía) · `STATE-001` (NEW-10/11) · `PAY-STATE-001` (NEW-7/8) · `DATA-MIG-001` (NEW-24, riêng NEW-27 đang fail → G2) · `DEPLOY-LIVE-001` (NEW-26, hạn chế env → G5) · `COMPAT-LEGACY-001` (NEW-21 hợp đồng năm 13 bản ghi) · `DATA-COUNT-001` (NEW-19 đối chiếu số dòng màn ↔ DB) · `OUT-TRUTH-001` (log bỏ qua sự kiện trùng) · `FUNC-001`.
>
> ⚠️ **10/29 TC mang mã quan điểm KHÔNG có trong `framework/checklist-lme.md`** → theo quy tắc **không tính là cover**. Xem §4 #5. Các quan điểm ở bảng trên đã trừ phần cover bởi mã lạ.

---

## 3. TC trùng lặp nội dung

**Đã rà 29/29 TC theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`) — không phát hiện trùng lặp cần xoá/gộp.**

2 cặp gần giống đã soi kỹ và **kết luận giữ nguyên cả hai**:

| Cặp | Vì sao KHÔNG phải trùng |
|---|---|
| NEW-19 (`11474`) vs NEW-20 (`11475`) | NEW-20 là kịch bản gửi trùng; NEW-19 là **negative control** (1 lần thanh toán) và kiểm thêm ngày · số tiền · 決済方法 khớp DB — NEW-20 không kiểm 3 mục này. Xoá NEW-19 sẽ **mất cover** phần đối chiếu giá trị hiển thị. |
| NEW-9 (`11464`, chuyển khoản gửi lại) vs NEW-18 (`11473`, số bạn bè gửi lại) | Cùng ý định "gửi lại bị chặn" nhưng **khác nhánh nghiệp vụ** và khác đối tượng dữ liệu bị bảo vệ (`status_transfer` + hạn hợp đồng vs bản ghi tiến trình + `max_friend_plan`). Cả hai đều là TC duy nhất của nhánh mình — thuộc diện **không được đề nghị xoá**. |

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | NEW-27 (`11482`) · bug #664 → Redmine #40087 | TC fail **đã** raise ticket nhưng ticket bị **rejected** với lý do trống ("Đồng bộ từ Redmine: Reject"). Bằng chứng trong chính bug **tự mâu thuẫn**: mệnh đề fail ghi `thực tế=2 / mong đợi=1`, nhưng phần kiểm chứng độc lập ngay dưới ghi `payment_histories tổng=1, bản ghi cha=1`. Đây là TC duy nhất chứng minh **lớp chốt thứ hai** hoạt động (G2) | Chạy lại NEW-27 trên môi trường sạch, chốt xem `isUnivaChargeRecorded` có chặn được hay không. Nếu bug reject là đúng thì phải sửa **assertion của TC**, không để TC fail treo |
| 2 | `[MAJOR]` | Toàn bộ 29 TC | **RULE-08 / ENV-003**: 27 TC chạy `env=local`, 0 TC ở `dev`/`staging`/`prd`. Task là **bill tiền qua cổng thanh toán ngoài** — theo Catalog D không kết luận được từ local | Chạy lại tối thiểu bộ TC gửi trùng + 5 nhánh nghiệp vụ trên `product` sau khi deploy (xem §5) |
| 3 | `[MAJOR]` | Run 392 (27 TC `skip`) | Lần chạy đầu **skip toàn bộ** do lỗi hạ tầng runner (`SOURCE_CHECKOUT_ERROR: unable to create file app/Helpers/PostBackActionBuilder.php: File exists`) → chỉ còn **1 lần chạy hợp lệ duy nhất** (run 394). Không có lần chạy lặp để loại flaky, trong khi NEW-27 fail chỉ được quan sát đúng 1 lần | Chạy lại ≥ 1 lượt nữa trước khi kết luận pass/fail |
| 4 | `[MAJOR]` | 24/29 TC | **RULE-02**: chỉ 5 TC (NEW-19→23) có artifact ảnh chụp màn hình; 24 TC còn lại cột `Evidence thực tế` rỗng. *Giảm nhẹ*: trường `actual` của runner có ghi số liệu DB + trích log nên không phải "đã xem, OK" — nhưng không có file lưu lại để Leader tự kiểm | Đính kèm dump DB / file log cho nhóm TC API + Data |
| 5 | `[MAJOR]` | 10/29 TC (34%) | Mã quan điểm không có trong `framework/checklist-lme.md`: `TOOL-NEGCTRL-001`(2) · `TOOL-KNOW-002`(2) · `TOOL-KNOW-001` · `TOOL-OLDREC-001` · `TOOL-SCOPE-001` · `TOOL-ERRHYG-001` · `TOOL-PAIRWISE-001` · `DATA-HIST-001` · 1 TC **trống mã** (NEW-29). Trong đó có **2 TC quan trọng nhất của ticket**: NEW-1 (gửi trùng tuần tự) và NEW-20 (màn hình hết trùng) đều mang `TOOL-KNOW-002` | Map lại sang mã chuẩn: NEW-1/NEW-20 → `CONC-001`/`INTG-HOOK-001`; NEW-3/NEW-21 → `COMPAT-LEGACY-001`; NEW-22 → `DATA-MIG-001`; NEW-25 → `FUNC-004` |
| 6 | `[MAJOR]` | `03-dev-impact.md` | Có `Auto-filled: 2026-09-09 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" chưa tick** → F/D/T có thể thiếu hoặc map sai, review này dựa trên input chưa được người xác nhận | Tester đọc lại journal #129182 trên Redmine rồi tick |
| 7 | `[MAJOR]` | Mục 6 VERIFY của Dev | Dev tự khai `git diff --stat: 1 file changed, 40 insertions(+) — đúng phạm vi`, nhưng diff thật theo Studio là **2 file / 189 dòng thêm**. Mức verify chỉ `lint`, và Dev tự ghi **không kết nối được MySQL dev** nên chưa chạy thử lần nào | Yêu cầu Dev đính chính con số phạm vi trước khi release — đang chênh 4.7 lần |
| 8 | `[MAJOR]` | Spec | Studio trích nguồn `spec: admin/point/feature-spec.md#SCR-PT-01` — đường dẫn này **không tồn tại** trong `spec-features/`. Spec thật của màn là `spec-features/admin/billing-plan/feature-spec.md` §SCR-BLP-03 (+ `admin/detail-contract/feature-spec.md` §2.9 SCR-DC-09) | Sửa `source_refs` trên Studio về mã màn `SCR-BLP-03` để lần review sau truy được |

### Chất lượng từng TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 9 | `[MAJOR]` | NEW-29 (`11960`) | **TC stub**: `viewpoint` · `case_type` · `tc_group` · `screen` · `requirement_keys` đều rỗng; tiền đề 1 dòng viết thường ("chuẩn bị giao dịch hợp lệ có phát sinh payment_detail_aff"), không có bước thực hiện dựng lại được; **chưa chạy**. Đây lại là TC **duy nhất** cover impact `D2` affiliate → §1 G1 | Viết lại đủ 16 cột hoặc thay bằng 2 TC ở §5 |
| 10 | `[MAJOR]` | `[AP-3]` NEW-13 → NEW-17 | **Happy-path-only regression**: cả 5 TC regression của 5 nhánh nghiệp vụ đều là `Normal` (lần gửi đầu). Nhánh Abnormal — đúng kịch bản bug của ticket — chỉ tồn tại ở 2/5 nhánh | Bổ sung 4 TC Abnormal ở §5 (`TC-REGSHARED001-01→04`) |
| 11 | `[MINOR]` | `[AP-5]` NEW-23 (`11478`) | **Over-coverage tầng dưới**: TC kiểm nút 「領収書の発行」 điều hướng sang `/basic/payment-history` và bảng có đủ 8 cột. Tầng này **không bị diff chạm** — trùng nội dung với `kho-tcs` `TC-BLP-03` / `TC-BLP-235` | Giữ được (rẻ) nhưng đừng tính vào coverage của fix; nếu cần cắt thời gian chạy thì bỏ TC này trước |
| 12 | `[MINOR]` | NEW-12 (`11467`) | TC duy nhất chạm `prd`, `exec_mode=manual` nhưng chưa gán người chạy và chưa có ngày dự kiến → nguy cơ treo mãi | Giao đích danh + chạy ngay sau khi release lên production |
| 13 | `[MINOR]` | NEW-26 / NEW-27 | Ghi chú môi trường của cả 2 TC nêu: `php artisan migrate:rollback` **bất khả dụng** trên nhánh này vì 2 migration từ 2021 trùng tên class `AddBotsToUsersTable`. TC phải gọi thẳng `down()`/`up()` để lách | Nợ kỹ thuật ngoài phạm vi #39668 nhưng **chặn khả năng rollback thật khi release** — nên mở ticket riêng |

---

## 5. TCs đề xuất bổ sung (14)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa031-billtientool-契約プラン・決済情報.md` (398 TC) — nhóm 『Lịch sử thanh toán — theo năm』(7) ·『— theo tháng & lọc』(17) ·『Tải lãnh thụ thư』(17) ·『Job bill định kỳ — thẻ』(12) ·『Môi trường & regression』(7) |
| Vùng regression phát hiện từ kho | `TC-BLP-294` (job bill năm: 1 bản ghi cha + 12 bản ghi phân bổ) — **đã** được NEW-21 cover, không viết lại · `TC-BLP-287` (job bill thẻ thành công → cập nhật hợp đồng + **3 loại lịch sử**) → vùng regression của G3/Q4, đã dẫn chiếu trong `TC-JOB001-01` · `TC-BLP-392/393` (mọi TC tiền phải chạy PRODUCTION) → củng cố Q2 |
| Conflict expected vs kho | **Không** có conflict expected. Nhưng kho có `MT-13` (⏳ CHỜ QUYẾT ĐỊNH) đúng chủ đề ticket này — đã đưa §6 |
| GAP dùng lại TC kho (không viết mới) | `G6` → không viết TC mới, dùng `TC-BLP-235`/`TC-BLP-252` của kho nếu cần smoke tầng đọc |
| Xác nhận chống trùng | Đã đối chiếu **29 TC ở BƯỚC 0** + kho `fa031` — **không TC đề xuất nào trùng** |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-PAYCONFIRM001-01 | API | PAY-CONFIRM-001 | Hoa hồng giới thiệu — webhook UnivaPay | Normal | manual | product | Giao dịch có giới thiệu: lần webhook đầu ghi đúng 1 bản ghi hoa hồng | Đã chạy migration. Có tài khoản A được giới thiệu bởi tài khoản B (có quan hệ affiliate). A vừa thanh toán gói trả phí bằng thẻ, đang chờ callback. Ghi lại số bản ghi hoa hồng hiện có của B | 1. Gửi webhook `charge_finished` trạng thái thành công với mã giao dịch mới<br>2. Mở màn quản lý hoa hồng giới thiệu của B, đếm số bản ghi mới<br>3. Đối chiếu số tiền hoa hồng với số tiền giao dịch của A | Mã giao dịch mới chưa dùng; hợp đồng của A gói Standard tháng | Phát sinh **đúng 1** bản ghi hoa hồng cho B, số tiền tính đúng theo giao dịch của A. Màn hoa hồng của B hiện 1 dòng mới | | Lấp `G1` (negative control) · Đánh giá spec: Spec không ghi — **cần hỏi Dev** mức hoa hồng · Evidence: ảnh màn hoa hồng + dump bản ghi trước/sau · RULE-08 |
| TC-PAYCONFIRM001-02 | API | PAY-CONFIRM-001 | Hoa hồng giới thiệu — webhook UnivaPay | Abnormal | manual | product | Webhook gửi trùng KHÔNG nhân đôi bản ghi hoa hồng giới thiệu | Tiếp nối trạng thái sau `TC-PAYCONFIRM001-01`: đã có 1 bản ghi hoa hồng cho B với mã giao dịch đó | 1. Gửi lại **đúng** webhook `charge_finished` cũ (cùng mã giao dịch) lần thứ 2<br>2. Đợi phản hồi, đọc log<br>3. Mở lại màn hoa hồng của B, đếm bản ghi<br>4. Kiểm tổng hoa hồng luỹ kế của B | Lặp lại nguyên payload của lượt 1 | Lượt 2 trả HTTP 200 kèm thông điệp giao dịch trùng. Số bản ghi hoa hồng của B **không tăng** (vẫn 1). Tổng hoa hồng luỹ kế **không đổi**. Log ghi nhận đã bỏ qua sự kiện trùng | | Lấp `G1` — đây là impact `D2` Dev tự kê mà bộ TC hiện có **chưa hề chạy** · Evidence: ảnh màn + log · RULE-08 |
| TC-REGSHARED001-01 | API | REG-SHARED-001 | Gia hạn tự động (`bill_job`) — webhook UnivaPay | Abnormal | manual | product | Webhook gia hạn tự động gửi trùng không cộng dồn hạn hợp đồng thêm một kỳ | Đã chạy migration. Có hợp đồng bot đến kỳ gia hạn, thanh toán thẻ, đã lưu token thẻ. **Ghi lại `expired_date_contract` trước khi test** | 1. Gửi webhook `charge_finished` thành công nhánh gia hạn tự động, mã giao dịch mới<br>2. Ghi lại hạn hợp đồng sau lượt 1<br>3. Gửi lại **cùng** webhook đó lần 2<br>4. So hạn hợp đồng sau lượt 2 với sau lượt 1<br>5. Đếm bản ghi lịch sử thanh toán mang mã đó | Hợp đồng Standard kỳ tháng, hạn hiện tại `<ghi lại>` | Sau lượt 1: hạn hợp đồng cộng đúng **1 kỳ**. Sau lượt 2: hạn hợp đồng **giữ nguyên y hệt sau lượt 1** (không cộng thêm kỳ thứ 2), `status_payment` không đổi thêm, chỉ có **1** bản ghi lịch sử thanh toán | | Lấp `G3` + `Q7` · Đây là nhánh Dev chỉ đích danh là **rủi ro nhất** (mail + richmenu + vòng đời hợp đồng → dễ quá thời gian chờ) · Evidence: giá trị hạn hợp đồng 3 mốc + log · RULE-08 |
| TC-REGSHARED001-02 | API | REG-SHARED-001 | Đăng ký mới / nâng gói (`new`,`upgrade_pro`) — webhook | Abnormal | manual | Tất cả | Webhook đăng ký mới gửi trùng không kích hoạt hợp đồng hai lần | Đã chạy migration. Có hợp đồng bot vừa đăng ký gói trả phí bằng thẻ, đang chờ callback. Ghi lại `plan_type` và trạng thái hợp đồng trước test | 1. Gửi webhook thành công `actionBill=new` (hoặc `upgrade_pro`), mã giao dịch mới<br>2. Ghi lại trạng thái hợp đồng + gói của bot<br>3. Gửi lại cùng webhook lần 2<br>4. So sánh trạng thái, gói, số bản ghi lịch sử | Gói Standard → Pro nếu test `upgrade_pro` | Lượt 2 bị bỏ qua với thông điệp giao dịch trùng. Hợp đồng **không** bị kích hoạt lại, gói của bot **không** bị cập nhật lần 2, chỉ **1** bản ghi lịch sử thanh toán | | Lấp `G4` + `Q7` · regression · Evidence: dump `bot_contracts` + `bots.plan_type` trước/sau |
| TC-REGSHARED001-03 | API | REG-SHARED-001 | Đổi thẻ / gia hạn / tái ký (`change_card`,`extend_contract`,`recontractV2`) — webhook | Abnormal | manual | Tất cả | Webhook đổi thẻ và tái ký gửi trùng không ghi đè thông tin thẻ lần hai | Đã chạy migration. Có hợp đồng đang thanh toán thẻ và vừa thao tác đổi thẻ, đang chờ callback. Ghi lại 4 số cuối thẻ + hạn hợp đồng | 1. Gửi webhook thành công nhánh `change_card`, mã giao dịch mới<br>2. Ghi lại 4 số cuối thẻ và hạn hợp đồng<br>3. Gửi lại cùng webhook lần 2<br>4. Lặp lại toàn bộ với nhánh `extend_contract` bằng mã giao dịch khác | Thẻ mới 4 số cuối `4242` | Lượt 2 của cả 2 nhánh đều bị bỏ qua. Thông tin thẻ và hạn hợp đồng **giữ nguyên như sau lượt 1**, mỗi mã giao dịch chỉ **1** bản ghi lịch sử | | Lấp `G4` + `Q7` · regression · Evidence: dump `bot_contracts.univa_last_four_card` + `expired_date_contract` |
| TC-REGSHARED001-04 | API | REG-SHARED-001 | Thanh toán lại sau lỗi (`bill_again`) — webhook | Abnormal | manual | Tất cả | Webhook thanh toán lại gửi trùng không đặt lại bộ đếm lỗi hai lần | Đã chạy migration. Có hợp đồng đang ở trạng thái thanh toán lỗi (`status_payment_fail > 0`) và vừa được thao tác thanh toán lại | 1. Gửi webhook thành công `actionBill=bill_again`, mã giao dịch mới<br>2. Ghi lại `status_payment` + số lần lỗi<br>3. Gửi lại cùng webhook lần 2<br>4. So sánh 2 giá trị trên và đếm bản ghi lịch sử | Hợp đồng có `status_payment_fail = 2` trước test | Lượt 2 bị bỏ qua. Hợp đồng đã thoát trạng thái lỗi ở lượt 1 và **không** bị xử lý lại; số lần lỗi giữ nguyên `0`; chỉ **1** bản ghi lịch sử thanh toán | | Lấp `G4` + `Q7` · regression · Evidence: dump `bot_contracts.status_payment` + `status_payment_fail` |
| TC-DATADB001-01 | Data | DATA-DB-001 | Bảng chốt `univapay_webhook_processed` | Abnormal | manual | Tất cả | Gỡ chốt khi lỗi chỉ xoá dòng của giao dịch đó, không đụng dòng của tài khoản khác | Đã chạy migration. Dựng **2 tài khoản khác nhau** A và B, mỗi tài khoản 1 hợp đồng bot trả phí thẻ. Chuẩn bị 2 mã giao dịch khác nhau. Có cách ép webhook văng lỗi trước khi ghi lịch sử | 1. Gửi webhook thành công cho B (mã `MB`) → hoàn tất bình thường, bảng chốt có dòng `MB`<br>2. Gửi webhook cho A (mã `MA`) trong điều kiện ép lỗi trước khi ghi lịch sử<br>3. Đọc toàn bộ bảng chốt: đếm dòng, đối chiếu mã<br>4. Gửi lại webhook của B lần 2 | `MA`, `MB` là 2 UUID khác nhau, 2 `bot_id` khác nhau | Dòng chốt `MA` bị xoá (vì A chưa ghi được lịch sử). Dòng chốt `MB` **vẫn còn nguyên**, thời điểm cập nhật không đổi. Lượt gửi lại của B vẫn bị chặn với thông điệp giao dịch trùng và không sinh thêm bản ghi lịch sử của B | | Lấp `Q1` · **RULE-07** — bắt buộc kiểm ở tầng DB, không thay bằng UI · Evidence: dump toàn bảng chốt trước/sau |
| TC-CONC001-01 | API | CONC-001 | Webhook UnivaPay — chốt đồng thời | Boundary | manual | Tất cả | Năm request đồng thời cùng mã giao dịch chỉ một lượt được xử lý | Đã chạy migration. Có hợp đồng bot đang hoạt động thanh toán thẻ. Chuẩn bị công cụ gửi được **5 request song song** trong cùng cửa sổ mili-giây. Bảng lịch sử chưa có bản ghi nào mang mã này | 1. Bắn đồng thời 5 request `charge_finished` cùng mã giao dịch<br>2. Thu toàn bộ 5 phản hồi<br>3. Đếm bản ghi lịch sử thanh toán mang mã đó<br>4. Đếm dòng trong bảng chốt<br>5. Rà log tìm lỗi trùng khoá bị đẩy ra ngoài | 5 request giống hệt nhau, cùng 1 mã giao dịch | Cả 5 trả HTTP 200. **Đúng 1** lượt được xử lý tiếp, **4** lượt trả thông điệp giao dịch trùng. Có đúng **1** bản ghi lịch sử và **1** dòng chốt. Không lỗi ràng buộc trùng khoá nào lọt ra thành lỗi máy chủ | | Lấp `Q5` · **RULE-01** — bổ sung chiều Boundary cho `CONC-001` (hiện chỉ có 1 TC Abnormal 2 request) · Evidence: 5 phản hồi + log |
| TC-SEC002-01 | API | SEC-002 | Webhook UnivaPay — xác thực nguồn | Abnormal | manual | product | Request giả mạo không được chiếm chốt của mã giao dịch chưa phát sinh | Đã chạy migration. Biết trước một mã giao dịch **sắp** được cổng thanh toán tạo (hoặc dùng mã tự sinh rồi tạo giao dịch thật mang đúng mã đó). Có công cụ gửi HTTP tuỳ ý tới endpoint webhook từ **ngoài** hệ thống | 1. Từ ngoài, gửi `charge_finished` trạng thái thành công mang mã giao dịch chưa phát sinh, **không kèm** chữ ký / thông tin xác thực hợp lệ<br>2. Đọc phản hồi và kiểm bảng chốt có sinh dòng mới không<br>3. Cho phát sinh giao dịch **thật** mang đúng mã đó qua cổng thanh toán<br>4. Kiểm xem webhook thật có được xử lý và có ghi lịch sử thanh toán không | Payload sao chép cấu trúc thật, đổi mã giao dịch | Request giả **bị từ chối** (không phải HTTP 200 báo thành công) và **không tạo dòng chốt**. Giao dịch thật sau đó được xử lý đầy đủ, ghi đúng 1 bản ghi lịch sử. ⚠️ Nếu request giả được chấp nhận và chiếm chốt → giao dịch thật bị bỏ qua, **mất bản ghi thanh toán** → raise ticket ngay | | Lấp `Q3` · Đánh giá spec: **Spec không ghi** — chưa hỏi ai; cần Dev xác nhận endpoint có xác thực nguồn không · Evidence: phản hồi + dump bảng chốt + log · RULE-08 |
| TC-INTGHOOK001-01 | API | INTG-HOOK-001 | Webhook UnivaPay — thứ tự & loại sự kiện | Abnormal | manual | Tất cả | Sự kiện khác loại mang cùng mã giao dịch không chiếm chốt của sự kiện thành công | Đã chạy migration. Có hợp đồng bot đang chờ callback thanh toán thẻ. Chuẩn bị 1 mã giao dịch mới | 1. Gửi sự kiện **không phải** `charge_finished` (ví dụ `charge_updated` / trạng thái `pending`) mang mã giao dịch đó<br>2. Kiểm bảng chốt có sinh dòng cho mã đó không<br>3. Gửi tiếp `charge_finished` trạng thái thành công cùng mã<br>4. Đếm bản ghi lịch sử và kiểm trạng thái hợp đồng | 2 sự kiện cùng mã, khác `event` / khác trạng thái | Bước 1 **không** tạo dòng chốt. Bước 3 được xử lý đầy đủ như bình thường, tạo đúng **1** bản ghi lịch sử thanh toán và cập nhật hợp đồng đúng | | Lấp `Q6` · Bổ sung chiều "sự kiện đến không đúng thứ tự" mà bộ TC hiện có bỏ trống · Evidence: dump bảng chốt sau mỗi bước + log |
| TC-JOB001-01 | Data | JOB-001 | Job `AutoPaymentJobUnivapay` (06:30) | Normal | manual | product | Job bill định kỳ chạy lô nhiều hợp đồng, webhook gửi lại vẫn chỉ một bản ghi mỗi hợp đồng | Đã chạy migration. Chuẩn bị **≥ 5 hợp đồng** cùng đến kỳ gia hạn trong một lượt job, thanh toán thẻ. Ghi lại số bản ghi lịch sử của từng hợp đồng trước khi chạy | 1. Chạy job bill định kỳ UnivaPay<br>2. Trong lúc webhook đang đổ về, cho gửi lại sự kiện của **ít nhất 2** hợp đồng bất kỳ<br>3. Sau khi job xong, đếm bản ghi lịch sử của **từng** hợp đồng<br>4. Đếm dòng chốt tương ứng<br>5. Đối chiếu hạn hợp đồng của cả 5 | 5 hợp đồng Standard kỳ tháng cùng ngày bill | **Mỗi** hợp đồng có đúng **1** bản ghi lịch sử thanh toán và **1** dòng chốt; hạn hợp đồng của cả 5 cộng đúng 1 kỳ. Không hợp đồng nào bị bỏ sót vì chốt của hợp đồng khác | | Lấp `Q4` · Vùng regression dẫn từ kho `TC-BLP-287` (job bill thẻ → cập nhật hợp đồng + 3 loại lịch sử) · Evidence: dump theo từng hợp đồng + log job · RULE-08 |
| TC-DEPLOYLIVE001-01 | Data | DEPLOY-LIVE-001 | Release — thứ tự deploy & migration | Abnormal | manual | product | Deploy code trước khi chạy migration phải quan sát được ngay, không âm thầm chặn thanh toán | Môi trường có traffic thanh toán thật (hoặc bản sao production). Đã triển khai mã nguồn có fix nhưng **chưa** chạy migration. Có quyền đọc kênh cảnh báo Chatwork | 1. Cho phát sinh 1 giao dịch thanh toán thẻ thật<br>2. Quan sát phản hồi webhook và kênh cảnh báo<br>3. Kiểm bản ghi lịch sử thanh toán có được tạo không<br>4. Chạy migration<br>5. Cho cổng gửi lại sự kiện của chính giao dịch ở bước 1<br>6. Kiểm lại bản ghi lịch sử và trạng thái hợp đồng | 1 giao dịch thật gói Standard | Bước 2: cảnh báo **nổi ngay** trên kênh Chatwork, không im lặng. Bước 3: chưa có bản ghi lịch sử. Bước 6: sau khi chạy migration, sự kiện gửi lại được xử lý và tạo **đúng 1** bản ghi lịch sử — giao dịch thật **không bị mất** | | Lấp `G5` · Kiểm chính rủi ro Studio `dev_impact` nêu: thiếu migration có thể **chặn TOÀN BỘ** thanh toán UnivaPay · Evidence: ảnh kênh cảnh báo + log + dump `payment_histories` · RULE-08 |
| TC-DATABACKUP001-01 | Data | DATA-BACKUP-001 | Backup & copy bot với bảng mới | Normal | manual | Tất cả | Bảng chốt webhook không bị mang theo sai khi copy bot hoặc khôi phục backup | Đã chạy migration. Có bot nguồn đã phát sinh ≥ 2 dòng chốt trong bảng đánh dấu. Biết quy trình copy bot / khôi phục backup của hệ thống | 1. Ghi lại danh sách dòng chốt của bot nguồn<br>2. Thực hiện copy bot sang bot mới<br>3. Kiểm bảng chốt: bot mới có mang theo dòng chốt cũ không<br>4. Cho bot mới phát sinh giao dịch thanh toán thật mang mã giao dịch mới<br>5. Kiểm bản ghi lịch sử của bot mới | Bot nguồn có 2 mã giao dịch đã xử lý | Bot mới **không** thừa hưởng dòng chốt của bot nguồn (chốt gắn theo mã giao dịch, không theo bot). Giao dịch mới của bot mới được xử lý bình thường, ghi đúng 1 bản ghi lịch sử — không bị bỏ qua vì trùng chốt cũ | | Lấp `Q8` · Đánh giá spec: **Spec không ghi** — cần Dev xác nhận bảng mới có nằm trong phạm vi backup/copy không · Evidence: dump bảng chốt trước/sau copy |
| TC-DATACOUNT001-01 | UI | DATA-COUNT-001 | 年間決済一覧 — tổng tiền tháng/năm | Abnormal | manual | product | Tài khoản còn bản ghi trùng cũ hiển thị tổng tiền tháng bị cộng đôi | Chọn 1 trong **65 tài khoản** mà NEW-28 đã liệt kê là còn mã giao dịch trùng (trước 2026-08-18). Có quyền đăng nhập tài khoản đó hoặc bản sao dữ liệu. Biết số tiền thật của giao dịch từ cổng thanh toán | 1. Mở màn 支払履歴, panel trái 年間決済一覧 của năm chứa giao dịch trùng<br>2. Đọc ご請求額 của tháng đó<br>3. Mở 詳細を確認 xem bảng chi tiết, đếm dòng cùng 課金ID<br>4. Đối chiếu tổng tháng với tổng số tiền thật trên cổng thanh toán<br>5. Lặp cho 2-3 tài khoản khác trong danh sách 65 | Danh sách 65 mã trùng / 202 bản ghi thừa từ NEW-28 | Xác định **rõ bằng số**: tổng tiền tháng hiển thị **cao hơn** số tiền thật đúng bằng phần bản ghi thừa, và tổng năm cũng lệch tương ứng. Ghi lại mức lệch của từng tài khoản để Leader quyết định phạm vi dọn dữ liệu và nội dung đính chính với khách | | Lấp `G7` + `Q10` · **RULE-04** — xác nhận phạm vi trước khi đóng ticket; đây là **cơ sở số liệu cho quyết định ở §6** · Evidence: ảnh panel 年間決済一覧 + bảng chi tiết + export số liệu 65 tài khoản · RULE-08 |

**GAP/RISK không đề xuất TC mới** (giải thích bắt buộc):

| Mã | Vì sao không viết TC mới |
|---|---|
| `G2` | Đã có **NEW-27** đúng kịch bản này — theo BƯỚC 5b, trùng với bộ TC gốc thì **không đẻ TC mới**. Việc cần làm là **chạy lại + chốt lại assertion** của NEW-27 và mở lại bug #40087 (§4 #1), không phải viết TC thứ hai. |
| `G6` | Tầng đọc (màn FS-009 + `EP-11` API cũ) **không bị diff chạm**; fix chỉ làm giảm số dòng trùng chứ không đổi cấu trúc dữ liệu → không có đường sinh regression. Nếu Leader vẫn muốn smoke thì dùng lại `TC-BLP-235` / `TC-BLP-252` của kho, không viết mới. |
| `Q2` | `ENV-003` được lấp bằng **cột `Phạm vi ENV = product`** của 8 TC ở trên + chạy lại **NEW-12** (`11467`) trên `prd` — TC đó đã tồn tại và đang chờ người chạy, không viết TC trùng. |

---

## 6. Spec update needed

| # | Vấn đề | Nguồn | Cần ai chốt |
|---|---|---|---|
| 1 | **Thông báo đã gửi khách mâu thuẫn với thực tế.** Journal #129911 (2026-08-19) trả lời khách: *"hệ thống đã tự động thực hiện xử lý xóa dữ liệu trùng lặp. Hiện tại hiển thị trùng lặp cũng đã được khắc phục."* Nhưng mục 5 RECOVER DATA của Dev nói dữ liệu trùng cũ **vẫn còn**, TC **NEW-22** (pass) chứng minh bản ghi trùng cũ **VẪN hiển thị 2 dòng và VẪN tải được 2 hoá đơn**, và **NEW-28** đếm được **65 mã trùng / 202 bản ghi thừa / 65 tài khoản** chưa dọn | Redmine journal #129911 · TC NEW-22 · NEW-28 | **CS + Leader** — cần chốt có đính chính với khách TY-11918 (và 64 tài khoản còn lại) hay không |
| 2 | **Đánh đổi thiết kế do Dev tự nêu, chưa ai duyệt.** Khi lượt đầu **đã ghi lịch sử rồi mới lỗi giữa chừng**, chốt được GIỮ nên lần gửi lại bị bỏ qua — đổi lại **phần việc sau điểm ghi lịch sử không được chạy lại** (gửi mail, cập nhật richmenu, ghi vòng đời hợp đồng). Dev ghi rõ *"cần người review xác nhận"*. TC **NEW-11** chỉ verify không ghi trùng, **không** verify hệ quả bỏ sót các bước sau | `03-dev-impact.md` §TỰ REVIEW · TC NEW-11 | **Dev + Leader** — chấp nhận bỏ sót mail/richmenu để đổi lấy không ghi trùng? Nếu không chấp nhận thì cần cơ chế bù |
| 3 | **`kho-tcs` `MT-13` đang ⏳ CHỜ QUYẾT ĐỊNH đúng chủ đề ticket này**: *"Callback UnivaPay đến MUỘN hoặc có 2 charge transfer song song: xử lý ra sao?"* — corpus liệt kê 6 kịch bản nhưng cột Expect Result **để trống**; kho tự viết expected theo nguyên tắc idempotent và ghi "cần Dev xác nhận". Fix #39668 nay đã **chốt chính thức nguyên tắc idempotent ở tầng webhook** | `kho-tcs/fa031-billtientool-契約プラン・決済情報.md` MT-13 | **Dev** — xác nhận để **đóng MT-13** và điền expected cho 6 kịch bản trong kho |
| 4 | **Spec màn thiếu mục idempotency.** `spec-features/admin/billing-plan/feature-spec.md` §SCR-BLP-03 mô tả đủ 8 cột + điều kiện lọc bản ghi hợp lệ (`type_bill`, `status_refund=0`, `parent_month=1`) nhưng **không có dòng nào** nói webhook `charge_finished` gửi lại thì xử lý ra sao — đúng vùng mù đã gây ra bug này | `spec-features/admin/billing-plan/feature-spec.md` §SCR-BLP-03 · §8 | **Leader** — bổ sung mục "webhook trùng" vào spec (sub-repo `spec-features/`) |
| 5 | **Mã màn hình không thống nhất.** Studio trích `spec: admin/point/feature-spec.md#SCR-PT-01` — đường dẫn **không tồn tại**. Spec thật: `admin/billing-plan/feature-spec.md` §SCR-BLP-03 và `admin/detail-contract/feature-spec.md` §2.9 SCR-DC-09 (cùng URL `/basic/payment-history`, **2 spec mô tả 1 màn**) | Studio `requirements[].source_refs` · `spec-features/` | **Leader** — chốt spec nào là nguồn chính cho `/basic/payment-history`, rồi sửa `source_refs` trên Studio |

---

> Report là **draft cho Leader verify**. Nguồn spec đã dùng: [`spec-features/admin/billing-plan/feature-spec.md`](../../spec-features/admin/billing-plan/feature-spec.md) §SCR-BLP-03 + [`spec-features/admin/detail-contract/feature-spec.md`](../../spec-features/admin/detail-contract/feature-spec.md) §2.9. Kho TCs đối chiếu: [`kho-tcs/fa031-billtientool-契約プラン・決済情報.md`](../../kho-tcs/fa031-billtientool-契約プラン・決済情報.md).
