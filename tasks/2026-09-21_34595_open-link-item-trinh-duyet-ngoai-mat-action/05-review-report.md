# 05 — Review Report

## 0. Nguồn TC

- **Nguồn đã dùng**: MCP LME TEST STUDIO — task **#248** (round 1, `tc-ready`, branch `ai_fixbug_34595`, `aiResult=fail`, `reviewState=leader`)
- **Tổng số TC review**: **31**

---

## 1. Coverage — `dev-impact` + `diff code`

**Kết luận: 0/21 vùng ảnh hưởng đủ TC · 14 GAP · 5 RISK · 2 orphan.**

> ⚠️ **Nền tảng của toàn bộ §1**: `spec_delta` của Studio task #248 có `computedAt = 2026-08-27`, `diffStat = app/Services/TemplateService.php | +12/-2`, `journalCount = 2`. Đây là diff của **bản fix v1 ĐÃ BỊ REVERT**, chưa hề thấy 3 journal ngày 2026-09-19. Chiều `diff code` vì vậy **không lấy được từ Studio** → fallback nguồn 2 (`03-dev-impact.md` mục 1/2/3). Diff thật có hiệu lực: **`app/Http/Controllers/LiffController.php`, +54/-0, commit `d552e8292b`**.

| # | Vùng ảnh hưởng | Chiều | TC cover | Status |
|---|---|---|---|---|
| **G1** | `BUG` root cause — mở link sản phẩm **ngoài LINE** → `GET /liff-callback/{unique_id}` case `product_id` → nhận diện khách → `u_code` → action「商品ページ表示時」được gửi | dev-impact + diff code | **0 TC**. NEW-21/22/23 gần nhất nhưng steps là "mua hàng", không đi qua điểm vào ngoài LINE; cả 3 đều `skip` | **GAP** `[BLOCKER]` |
| **G2** | `F1` helper MỚI `resolveProductUrlByLineLogin` (cookie → API hồ sơ LINE → `u_code` → URL) | diff code | **0 TC** | **GAP** `[BLOCKER]` |
| **G3** | `F2` nhánh **PC** (`!isMobileDevice()`) — trước: trang QR `qr_code_open_pc`; sau: redirect thẳng kèm `u_code` | diff code | **0 TC**. Toàn bộ 31 TC thao tác trên app LINE / điện thoại | **GAP** `[BLOCKER]` |
| **G4** | `F3` nhánh **điện thoại** — trước: route `open.mobile`; sau: redirect thẳng kèm `u_code` | diff code | **0 TC** | **GAP** `[BLOCKER]` |
| **G5** | Nhánh **fallback** "không nhận diện được khách" → giữ nguyên hành vi cũ (cookie rỗng / quá TTL 6h / bot chưa khai channel LINE Login) | diff code | **0 TC**. Điều kiện mới thêm có 2 nhánh true/false, **không nhánh nào có TC** | **GAP** `[BLOCKER]` |
| **G6** | `D8` giữ `lp_id` · `aff_id` · `aff_setting_id` trên URL redirect mới (`T3` FA-017, `T4` FA-027/FS-013) — chính là lỗi bản fix gốc làm rơi, v3 mới sửa | dev-impact + diff code | **0 TC** | **GAP** `[BLOCKER]` |
| **G7** | `T1` **3 loại link sản phẩm**: `detail` / **`change`** / **`cancel`** (EP-60 / EP-63 / EP-62) — Dev tự nêu "cần tester xác nhận cả 3" | dev-impact | **0 TC** cho `change` và `cancel` | **GAP** `[BLOCKER]` |
| **G8** | `D5` INSERT `bot_line_user_items` (status_contract=0) khi khách chưa có bản ghi — hệ quả của đường đi mới | dev-impact | **0 TC** | **GAP** `[MAJOR]` |
| **G9** | `D6` `count_action_view_page` **+1** mỗi lần gửi action; read-modify-write, fix làm **tăng lưu lượng** | dev-impact | **0 TC** | **GAP** `[MAJOR]` |
| **G10** | `D7` **số lần gửi action** ra ngoài (`number_action_show_page`, `flag_page_start != 2`) — Dev nêu "QA cần canh số lần gửi" | dev-impact | **0 TC pass**. NEW-21/22/23 chạm ý này nhưng `skip` | **GAP** `[MAJOR]` |
| **G11** | `T6`/`F7` regression **luồng trong LINE** (`QRCodeController::checkFriend`) phải KHÔNG đổi — fix dựng URL "giống hệt" nên phải đối chứng | dev-impact | **0 TC** | **GAP** `[MAJOR]` |
| **G12** | `T5`/`F6` phụ thuộc vận hành: cookie access token **chỉ do luồng BIỂU MẪU ghi ra** (`LiffController:1028`, TTL 360 phút) | dev-impact + diff code | **0 TC** | **GAP** `[MAJOR]` |
| **G13** | `F4` `SalesManagementV2Controller::orderDetail` — nhánh có `u_code` nay thực sự được chạy từ đường ngoài LINE (chặn bot hết hạn / friend block / đơn chờ webhook vẫn phải đúng) | dev-impact | **0 TC** trên đường mới | **GAP** `[MAJOR]` |
| **G14** | Helper mới có **2 caller** (PC + mobile) — Dev CÓ kê danh sách, nhưng TC phải test **từng nơi** | diff code (câu hỏi adversarial #3) | **0/2 caller** có TC | **GAP** `[MAJOR]` |
| **G15** | `T2` SC-004 — hành động gắn cho sản phẩm không còn bị mất trên đường đi mới | dev-impact | NEW-21/22/23 (`skip`), NEW-28/29 (chưa chạy) — có TC nhưng **0 TC pass** | **RISK** |
| **G16** | Hành vi **đổi so với trước** (PC: trang QR → redirect thẳng; mobile: `open.mobile` → redirect thẳng) — cần TC hành vi MỚI **và** TC chỗ cũ không hỏng | diff code (câu hỏi adversarial #4) | Chỉ có TC bám hành vi **bản v1 đã revert**, không phía nào đúng | **RISK** |
| **G17** | `D1`/`D2`/`D3` đọc `bot_line_users` (`is_blocked=0`) · `line_users.line_id` · `s_items.item_code` | dev-impact | Chỉ chạm ngầm qua TC `skip` | **RISK** |
| **G18** | **Symptom-only** (AP-2) — file 01 chỉ có triệu chứng, Dev đã đổi root cause 2 lần (v1 `TemplateService` → v4 `LiffController`). Bản v1 bị revert ⇒ root cause v1 **sai** | diff code | Không TC nào cover root cause thứ 2 hợp lý (vd nhánh booking cùng switch) | **RISK** |
| **G19** | `liffAppCallback` là hàm dùng chung cho **nhiều case** trong switch (`unique_key` form, `product_id`, booking event/calendar…). Dev chỉ kê `unique_key` làm mẫu đối chiếu, **không kê danh sách đầy đủ case còn lại** | diff code (câu hỏi adversarial #3) | Không có danh sách ⇒ không rà được | **RISK** |
| **G20** | **NEW-1 → NEW-20** (20 TC) neo vào `TemplateService::getTemplate` / `makeImageMapVer2` và tham số `openExternalBrowser=1` — code này **đã revert về nguyên trạng**, không còn trong diff | orphan | 20 TC không thuộc vùng ảnh hưởng nào của bản fix v4 | **ORPHAN** `[BLOCKER]` |
| **G21** | **NEW-26 / NEW-27 / NEW-30 / NEW-31** (4 TC job) — Dev khai rõ gửi tin qua job (`linect-service MessageBuilder`) **NGOÀI PHẠM VI, chưa fix** (`03` mục X1) | orphan | 4 TC đang test vùng cố ý không sửa; NEW-27 `fail` + đã raise bug **#41140** | **ORPHAN** `[MAJOR]` |

---

## 2. Quan điểm test còn thiếu

**Kết luận: 0/12 quan điểm Trigger khớp được cover đủ · 12 GAP.**

> 8 TC mang mã **không có trong `framework/checklist-lme.md`** (`TOOL-OLDREC-001` ×3, `TOOL-KNOW-002` ×2, `RULE-09` ×2, `TOOL-NEGCTRL-001` ×1) → **không tính là cover** (BƯỚC 0.6 #5).
> Các mã hợp lệ có xuất hiện (`OUT-TRUTH-001` ×8, `REG-SHARED-001` ×8, `COMPAT-LEGACY-001` ×2, `FUNC-DATE-001` ×2, `UI-FIELD-001` ×2, `REG-URL-001` ×1) **đều gắn vào TC vùng `TemplateService` đã revert hoặc vùng job ngoài phạm vi** ⇒ cũng không cover được bản fix v4.

| # | Mã quan điểm | Ưu tiên | Trigger khớp vì | Tình trạng | Severity |
|---|---|---|---|---|---|
| **Q1** | `LIFF-ENTRY-001` ★ | Cao | Tính năng phát sinh URL cho LINE user access — **item** nằm đúng danh sách trigger | **GAP**. Ma trận bắt buộc (trạng thái user × điểm vào in-app/ngoài/PC/button × loại link) — chiều **"trình duyệt ngoài"** và **"từ PC"** là đúng trọng tâm bug mà **0 TC** nào chạy | `[BLOCKER]` |
| **Q2** | `INTG-LINE-001` | Cao | Fix **gọi LINE API** lấy hồ sơ (`api.line.me`) | **GAP**. 0 TC giả lập token thu hồi / 401 / timeout / rate limit. Lỗi kinh điển của quan điểm này ("báo thành công dù chưa gửi") đúng là hình dạng bug gốc | `[BLOCKER]` |
| **Q3** | `SEC-002` | Cao | Fix **xử lý credential/token** — đọc cookie access token LINE Login, gửi tới `api.line.me` | **GAP**. 0 TC quét màn lỗi / console / server log xem có lộ access token · `u_code` · LINE user ID. Dev tự khai "token không bị ghi log" nhưng **chưa ai kiểm chứng** | `[BLOCKER]` |
| **Q4** | `DATA-COUNT-001` | Cao | `count_action_view_page` là con số đếm; spec `bill-item` R16 còn ghi nhận `$sItem` chưa khởi tạo ở case `view_page` → luôn ghi `13001` | **GAP**. 0 TC đối chiếu phép tính tay với 稼働回数「1度のみ」/「何度でも」 | `[BLOCKER]` |
| **Q5** | `CONC-001` | Cao | Dev tự nêu `count_action_view_page` dùng **read-modify-write** → "nguy cơ đếm thiếu khi trùng request", và fix **làm tăng lưu lượng** vào đó (`03` mục X4) | **GAP**. 0 TC mở trùng / 2 tab / double request | `[BLOCKER]` |
| **Q6** | `ENV-003` ★ + **RULE-08** | Cao | Tính năng chạm **URL/domain** (LIFF, redirect) và **thanh toán** | **GAP**. 24/31 TC chạy env `local`, **0 TC production** ⇒ theo RULE-08 không được kết luận | `[BLOCKER]` |
| **Q7** | `REG-SHARED-001` | Cao | Fix tạo **helper dùng chung** `resolveProductUrlByLineLogin` cho 2 nhánh; `liffAppCallback` cũng là hàm dùng chung nhiều case | **GAP**. 8 TC đang mang mã này nhưng toàn bộ nằm ở vùng `TemplateService` / job — không chạm helper mới | `[BLOCKER]` |
| **Q8** | `PERM-002` | Cao | Thao tác nhạy cảm (thanh toán / PII); đường mới đi thẳng vào `orderDetail` nơi có các chặn bot hết hạn (route 410) và friend block | **GAP**. 0 TC verify các chặn này **trên đường đi mới** | `[MAJOR]` |
| **Q9** | `FRIEND-001` | Cao | Fix đọc `bot_line_users` lọc `bot_id` + `is_blocked = 0` để suy `u_code` | **GAP**. 0 TC cho chưa kết bạn / old friend / đã block **qua đường ngoài LINE** | `[MAJOR]` |
| **Q10** | `STATE-001` | Cao | Đường mới dẫn thẳng vào `orderDetail` — nơi có ≥2 bước ghi tuần tự (tạo `bot_line_user_item` → gửi action → tăng counter) và chặn đơn định kỳ chờ webhook (`status_webhook ∈ {0,3,4}`) | **GAP**. 0 TC | `[MAJOR]` |
| **Q11** | `COMPAT-LEGACY-001` ★ | Cao | `SalesManagementController::orderDetail` (bản cũ) tồn tại song song với `V2`; link item đã từng chuyển từ LIFF app QR landing → LIFF app form (kho `TC-BIL-373`) | **GAP**. 2 TC mang mã này (NEW-3, NEW-13) thuộc vùng `TemplateService` đã revert | `[MAJOR]` |
| **Q12** | `SEC-001` | Cao | Fix chạm dữ liệu cá nhân (hồ sơ LINE, `u_code`); spec `bill-item` §2.4 ghi rõ **toàn bộ prefix `/v2` KHÔNG có middleware xác thực** — định danh chỉ dựa `u_code` trên URL | **GAP**. 0 TC thử `u_code` của người khác trên URL | `[MAJOR]` |

> **Đã loại khỏi phạm vi** (trigger khớp nhưng không tìm được bằng chứng ảnh hưởng): `JOB-*` — 4 TC job hiện có (NEW-26/27/30/31) thuộc vùng Dev khai **chưa fix**; không đề xuất thêm TC job cho ticket này, xem §6.

---

## 3. TC trùng lặp nội dung

Đã rà **31/31** TC theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`). Phát hiện **4 nhóm trùng**:

| # | Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|---|
| 1 | Product mua 1 lần qua link có action, gửi qua web | **NEW-21** | **NEW-28** | `DUP-SUBSET` | `OUT-TRUTH-001` × `Normal` × (product item mua 1 lần + action open link + trình duyệt ngoài) × tiền đề tương đương → expected "action không mất" | `[MINOR]` |
| 2 | Product mua chu kỳ qua link có action, gửi qua web | **NEW-22** | **NEW-29** | `DUP-SUBSET` | `OUT-TRUTH-001` × `Normal` × (product item chu kỳ + action open link + trình duyệt ngoài) × tiền đề tương đương | `[MINOR]` |
| 3 | Job **send all** gửi template có link Product | **NEW-26** | **NEW-30** | `DUP-SUBSET` | `REG-SHARED-001` × `Normal` × (broadcast/send all + template link product + action) × tiền đề tương đương | `[MINOR]` |
| 4 | Job **auto reply** gửi template có link Product | **NEW-27** (giữ — có `last_exec=fail` + ticket #41140) | **NEW-31** | `DUP-SUBSET` | `REG-SHARED-001` × `Normal` × (auto reply + template link product + action) × tiền đề tương đương | `[MINOR]` |

**Gate xóa đã chạy**: giả định xóa NEW-28/29/30/31 → chạy lại BƯỚC 2 + 3 trên tập còn lại ⇒ **không mất cover** vùng nào (NEW-21/22/26/27 giữ nguyên vùng đó). Vì vậy giữ đề xuất **xóa**, không cần gộp.

⚠️ **`DUP-INFLATE` — trùng đang che GAP**: cụm **NEW-21 → NEW-31 (11 TC)** làm quan điểm `OUT-TRUTH-001` + `REG-SHARED-001` và impact `T1`/`T2` **trông như đã có coverage**, nhưng thực tế **6 `skip` + 4 chưa chạy + 1 `fail` = 0 TC `pass`**. Đã mở lại ở §1 (`G10`, `G15`) và §2 (`Q7`) dưới dạng `GAP` / `RISK`. `[MAJOR]`

**KHÔNG tự xóa** — TC Studio read-only. Human quyết định rồi chạy `testcase_delete` trên Studio.

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | Toàn bộ task Studio #248 | `spec_delta.computedAt = 2026-08-27`, `diffStat = TemplateService.php +12/-2`, `journalCount = 2/6`. **Snapshot diff của Studio đứng ở bản fix v1 ĐÃ REVERT**, chưa thấy 3 journal 2026-09-19 (v2/v3/v4) | Chạy lại đồng bộ Redmine + recompute diff cho task #248 **trước** khi dùng lại bất kỳ kết luận nào của tool. Branch phải trỏ commit `d552e8292b` |
| 2 | `[BLOCKER]` | NEW-1 → NEW-20 (20 TC) | TC được tạo `2026-08-27` theo bản fix v1. `Kết quả mong đợi` của chúng ("link gửi đi **không kèm** tham số trình duyệt ngoài", "trang mở **ngay trong LINE**") **mâu thuẫn trực tiếp** với fix v4 — v4 giữ nguyên việc mở ngoài LINE, chỉ thêm bước nhận diện khách | Đánh dấu 20 TC này là **của round cũ**; mở round 2 cho phạm vi `LiffController`. Không chạy lại 20 TC này để kết luận ticket |
| 3 | `[BLOCKER]` | Toàn bộ 31 TC | Chỉ **14/31 `pass` = 45% < 50%** (7 chưa chạy · 6 `skip` · 3 `error` · 1 `fail`) | Không kết luận "đã test xong" cho đến khi có tỷ lệ `pass` thực trên bộ TC **đúng phạm vi** |
| 4 | `[BLOCKER]` | NEW-13 · NEW-14 · NEW-15 | 3 TC `error` **chưa raise ticket bug** (cột `bug_tickets` rỗng) | Raise ticket hoặc ghi rõ lý do `error` là do môi trường; `error` không được để trôi |
| 5 | `[BLOCKER]` | Môi trường thực thi | 24/31 TC chạy `env = local`, **0 TC** trên `staging` / `prd`. Task chạm **URL/domain + thanh toán** → **RULE-08 / ENV-003** cấm kết luận từ local | Chạy lại bộ TC trọng tâm trên `product` (xem cột `Phạm vi ENV` §5) |
| 6 | `[MAJOR]` | `03-dev-impact.md` | `Auto-filled: 2026-09-21 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" CHƯA tick** → F/D/T có thể thiếu hoặc map sai | Tester đọc lại journal #137231 trên Redmine, đối chiếu mục 4.1/4.2/4.3 rồi tick |
| 7 | `[MAJOR]` | 8 TC | 4 mã quan điểm **không có trong `framework/checklist-lme.md`**: `TOOL-OLDREC-001` (NEW-9/17/18), `TOOL-KNOW-002` (NEW-1/11), `RULE-09` (NEW-6/14), `TOOL-NEGCTRL-001` (NEW-5). `RULE-09` là mã **RULE**, không phải mã quan điểm | Map lại sang mã hợp lệ, hoặc bổ sung mã mới vào checklist theo RULE-10 |
| 8 | `[MAJOR]` | `Evidence thực tế` | Cột rỗng toàn bộ trong snapshot (`testcase_list` không trả field này) → **không kiểm chứng được RULE-02** từ payload | Leader mở Studio kiểm trực tiếp 14 TC `pass` có đính đúng loại evidence chưa; không có → hạ `pass` xuống chưa kết luận |
| 9 | `[MAJOR]` | NEW-27 / ticket #41140 | NEW-27 `fail` và đã raise **#41140**, nhưng TC này test **vùng job mà Dev khai cố ý KHÔNG fix** (`03` mục X1) | Xác nhận #41140 đang track đúng phạm vi (task riêng cho job), không phải bug của ticket 34595 |
| 10 | `[MAJOR]` | `[AP-2]` Symptom-only | File 01 chỉ có triệu chứng (description Redmine **trống**); Dev đã đổi root cause 2 lần qua 4 lượt fix. Root cause v1 bị revert ⇒ đã từng sai 1 lần | Hỏi Dev: ngoài nhánh `product_id`, các case khác trong cùng switch (`booking event` / `lesson calendar`) có cùng lỗi không — không tự đoán |
| 11 | `[MAJOR]` | `[AP-3]` Happy-path-only | Phân bố loại case toàn bộ: 25 `Normal` / 4 `Abnormal` / 2 `Boundary`. **Cả 4 `Abnormal` + 2 `Boundary` đều thuộc vùng `TemplateService` đã revert** ⇒ vùng fix v4 có **0 Abnormal, 0 Boundary** | Bổ sung theo §5 (RULE-01 cho quan điểm ưu tiên Cao) |
| 12 | `[MAJOR]` | Mức verify của Dev | Dev verify **chỉ tới `lint`** — không có DB dev, không gửi được tin LINE trong container, **chưa tái hiện lần nào**. Toàn bộ gánh nặng xác minh dồn cho tester | Coi như fix **chưa có bằng chứng chạy**; ưu tiên chạy nhóm `[BLOCKER]` §5 trước |

### Chất lượng từng TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 13 | `[MAJOR]` | NEW-20 | Tiêu đề "**Khảo sát** gửi thử ở màn kịch bản — **ghi nhận** ... có còn kèm tham số hay không" → `Kết quả mong đợi` **không đo lường được**; đây là hoạt động khảo sát, không phải TC verify | Chốt hành vi đúng với Dev/BA rồi viết lại expected thành giá trị cụ thể |
| 14 | `[MAJOR]` | NEW-24 · NEW-25 | Expected chỉ dừng ở "**cấu hình template được lưu đúng**" — không verify điều gì liên quan bug (action có được gửi hay không). Vi phạm **RULE-06** (dừng trước output cuối) | Nối tiếp tới output cuối: sau khi lưu → gửi → mở link → kiểm action trên LINE. Hoặc gộp vào TC dữ liệu tiền đề |
| 15 | `[MAJOR]` | NEW-21 · NEW-22 · NEW-23 · NEW-28 · NEW-29 | Là 5 TC duy nhất chạm vùng sản phẩm, nhưng `Các bước thực hiện` **không nêu điểm vào "mở bằng trình duyệt ngoài"** và **không phân biệt PC / điện thoại** — đúng 2 chiều quyết định của fix v4 | Bổ sung bước chọn điểm vào + tách 2 môi trường; hoặc dùng TC mới ở §5 |
| 16 | `[MINOR]` | NEW-17 · NEW-18 | "Xác minh dữ liệu tiền điều kiện của nút / vùng ảnh" — bản chất là **bước setup**, không phải TC verify hành vi | Chuyển thành `Điều kiện tiền đề` của các TC dùng nó |

---

## 5. TCs đề xuất bổ sung (30)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | [kho-tcs/fa026-billtienitem-商品販売.md](../../kho-tcs/fa026-billtienitem-商品販売.md) — nhóm 11 「LINE user — mở trang mua」(16 TC) · nhóm 24 「Action & notify theo sự kiện」(13 TC) · nhóm 33 「Domain LIFF & redirect」(8 TC) · nhóm 8 「アクション設定」(9 TC) |
| Vùng regression phát hiện từ kho | `TC-BIL-116` (mở 商品ページ trên trình duyệt ngoài LINE app) · `TC-BIL-107`/`TC-BIL-111` (mở từ button / image map / rich menu / QR) · `TC-BIL-112`/`113` (chưa kết bạn) · `TC-BIL-114`/`115` (old friend) · `TC-BIL-121` (friend đã block) · `TC-BIL-117`/`118`/`119` (trạng thái đơn) · `TC-BIL-120` (bot hết hạn 410) · `TC-BIL-84`/`85` (稼働回数 1度のみ / 何度でも) · `TC-BIL-371`/`373` (domain LIFF, trong vs ngoài LINE) |
| Conflict expected vs kho | **Không** conflict với kho. ⚠️ Conflict thật nằm giữa **NEW-1→NEW-20 và bản fix v4** — đã đưa §4 #2 và §6 |
| GAP dùng lại TC kho (không viết mới) | `G11` → **`TC-BIL-107` + `TC-BIL-111`** (regression luồng **trong** LINE, chạy nguyên trạng làm đối chứng) · `G13` một phần → **`TC-BIL-120`** (bot hết hạn 410) và **`TC-BIL-117`** (đơn chờ webhook) — **cần chỉnh**: đổi điểm vào từ "trong LINE" sang "ngoài LINE (PC + điện thoại)" |
| Xác nhận chống trùng | Đã đối chiếu **31 TC** ở BƯỚC 0 + kho FA-026 — **không TC đề xuất nào trùng**. 31 TC cũ không có TC nào đi qua `/liff-callback` nhánh `product_id` ngoài LINE |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-LIFFENTRY001-01 | UI | LIFF-ENTRY-001 | LINE user — mở link sản phẩm ngoài LINE | Normal | auto | Tất cả | Mở link sản phẩm trên **PC** bằng trình duyệt ngoài, phiên LINE Login còn hạn → vào thẳng trang chi tiết và nhận được action | - Bot A **đã khai channel LINE Login**<br>- Friend F1 đã kết bạn bot A<br>- SP-A là 単品商品, slot action「商品ページ表示時」gắn action gắn tag `TC34595`<br>- Trên **cùng trình duyệt PC**, F1 vừa mở 1 biểu mẫu của bot A qua LINE Login (trong vòng 6 giờ) | 1. Copy link 商品ページ của SP-A từ màn quản lý sản phẩm<br>2. Dán link vào thanh địa chỉ trình duyệt PC (Chrome/Safari), Enter<br>3. Quan sát trang hiện ra và URL trên thanh địa chỉ<br>4. Mở chat 1:1 của F1 trên app LINE, quan sát tin nhận được<br>5. Mở màn 友だちリスト → chi tiết F1, xem tag | Link 商品ページ của SP-A | - **KHÔNG** hiện trang mã QR「LINEアプリで開く」<br>- Vào thẳng màn 商品詳細 của SP-A<br>- URL có dạng `/v2/order-item/detail/{item_code}/{u_code}`, `u_code` **khớp** F1<br>- F1 nhận được action đã gắn (tag `TC34595` xuất hiện ở chi tiết F1) |  | Lấp G1 · G3 · Q1 — chiều "từ PC" của ma trận LIFF-ENTRY-001 · Evidence: ảnh chụp URL + ảnh chat 1:1 + ảnh tag |
| TC-LIFFENTRY001-02 | UI | LIFF-ENTRY-001 | LINE user — mở link sản phẩm ngoài LINE | Abnormal | auto | Tất cả | Mở link sản phẩm trên **PC** bằng trình duyệt **chưa từng đăng nhập LINE Login** → giữ nguyên trang mã QR, không gửi action | - Như TC-LIFFENTRY001-01<br>- Dùng **cửa sổ ẩn danh mới** (chưa từng mở biểu mẫu nào của bot A) | 1. Mở cửa sổ ẩn danh trên PC<br>2. Dán link 商品ページ của SP-A, Enter<br>3. Quan sát trang hiện ra<br>4. Kiểm chat 1:1 của F1 và tag của F1 | Link 商品ページ của SP-A | - Hiện **trang mã QR「LINEアプリで開く」** như trước fix<br>- **KHÔNG** redirect sang trang chi tiết<br>- F1 **KHÔNG** nhận action, tag không đổi<br>- Trang không trắng, không lỗi 500 |  | Lấp G5 · Q1 — nhánh false của điều kiện mới; đây là **giới hạn đã biết** của fix, không phải bug |
| TC-LIFFENTRY001-03 | UI | LIFF-ENTRY-001 | LINE user — mở link sản phẩm ngoài LINE | Boundary | auto | Tất cả | Phiên LINE Login ở mốc **TTL 6 giờ** — còn hạn sát mốc vs vừa quá hạn | - Như TC-LIFFENTRY001-01<br>- Ghi lại chính xác thời điểm F1 mở biểu mẫu (mốc `T0`) | 1. Tại `T0 + 5 giờ 55 phút`: mở link 商品ページ trên PC → ghi kết quả<br>2. Tại `T0 + 6 giờ 05 phút`: mở lại cùng link trên **cùng trình duyệt** → ghi kết quả<br>3. So sánh 2 lần | Mốc thời gian `T0+5h55m` và `T0+6h05m` | - Lần 1 (còn hạn): vào thẳng trang chi tiết kèm `u_code`, action được gửi<br>- Lần 2 (quá hạn): rơi về trang mã QR, **không** gửi action<br>- Không lần nào lỗi 500 / trắng trang |  | Lấp G5 · G12 · Q1 — biên TTL 360 phút của cookie ghi ở `LiffController:1028`. RULE-01 (Boundary của quan điểm Cao) |
| TC-LIFFENTRY001-04 | UI | LIFF-ENTRY-001 | LINE user — mở link sản phẩm ngoài LINE | Normal | auto | Tất cả | Mở link sản phẩm trên **điện thoại** bằng trình duyệt ngoài (Safari/Chrome), phiên còn hạn → vào thẳng trang chi tiết và nhận action | - Như TC-LIFFENTRY001-01 nhưng thao tác trên **điện thoại**, trình duyệt ngoài app LINE<br>- Trên chính trình duyệt đó, F1 vừa mở biểu mẫu của bot A trong 6 giờ | 1. Trên điện thoại, mở Safari/Chrome (KHÔNG mở trong app LINE)<br>2. Dán link 商品ページ của SP-A, Enter<br>3. Quan sát trang hiện ra và URL<br>4. Kiểm chat 1:1 của F1 và tag của F1 | Link 商品ページ của SP-A | - **KHÔNG** rơi vào trang trung gian「LINEアプリで開く」<br>- Vào thẳng 商品詳細, URL kèm `u_code` đúng F1<br>- F1 nhận được action (tag `TC34595`) |  | Lấp G1 · **G4** — nhánh điện thoại là lỗi LATERAL phát hiện sau review, chỉ được sửa ở v4; **bắt buộc test tách khỏi PC** |
| TC-LIFFENTRY001-05 | UI | LIFF-ENTRY-001 | LINE user — mở link sản phẩm ngoài LINE | Abnormal | auto | Tất cả | Mở link sản phẩm trên **điện thoại** ở trình duyệt chưa đăng nhập → giữ nguyên trang trung gian cũ | - Như TC-LIFFENTRY001-04 nhưng dùng **tab riêng tư mới** trên điện thoại | 1. Mở tab riêng tư trên trình duyệt điện thoại<br>2. Dán link 商品ページ của SP-A, Enter<br>3. Quan sát trang hiện ra<br>4. Kiểm tag của F1 | Link 商品ページ của SP-A | - Hiện **trang trung gian「LINEアプリで開く」** như trước fix<br>- **KHÔNG** gửi action, tag không đổi<br>- Không trắng trang |  | Lấp G4 · G5 — nhánh false phía mobile |
| TC-LIFFENTRY001-06 | UI | LIFF-ENTRY-001 | LINE user — mở link sản phẩm ngoài LINE | Abnormal | auto | Tất cả | Bot **CHƯA khai channel LINE Login** → cả PC lẫn điện thoại đều giữ hành vi cũ | - Bot **B** chưa khai channel LINE Login<br>- SP-B thuộc bot B, có gắn action「商品ページ表示時」<br>- Friend F5 đã kết bạn bot B | 1. Trên PC, mở link 商品ページ của SP-B → ghi kết quả<br>2. Trên điện thoại (trình duyệt ngoài), mở cùng link → ghi kết quả<br>3. Kiểm tag của F5 sau cả 2 lần | Link 商品ページ của SP-B | - Cả 2 lần đều hiện trang「LINEアプリで開く」 (PC = mã QR, điện thoại = trang trung gian)<br>- **KHÔNG** lỗi, **KHÔNG** gửi action<br>- Tag của F5 không đổi |  | Lấp G2 · G5 — điều kiện tiên quyết Dev tự nêu ("bot chưa khai thì vẫn như cũ") |
| TC-LIFFENTRY001-07 | UI | LIFF-ENTRY-001 | LINE user — đổi thẻ | Normal | auto | Tất cả | Link **カード情報変更** (đổi thẻ) mở ngoài LINE → vào thẳng trang đổi thẻ kèm `u_code` | - Bot A đã khai channel LINE Login<br>- Friend F1 **đang có hợp đồng 継続 đang chạy** với SP-C<br>- Trình duyệt đang còn phiên LINE Login của F1 | 1. Lấy link 変更 của SP-C (cấu hình ở wizard 各種ページ)<br>2. Mở link trên trình duyệt ngoài, lần lượt PC rồi điện thoại<br>3. Quan sát URL và trang hiện ra ở cả 2 lần | Link 変更 của SP-C | - Cả PC lẫn điện thoại: vào thẳng trang đổi thẻ<br>- URL dạng `/v2/order-item/change/{item_code}/{u_code}`, `u_code` đúng F1<br>- Form nhập thẻ UnivaPay/Stripe hiện bình thường |  | Lấp **G7** — Dev tự nêu "cần tester xác nhận cả 3 loại link"; đây là loại thứ 2 |
| TC-LIFFENTRY001-08 | UI | LIFF-ENTRY-001 | LINE user — hủy hợp đồng | Normal | auto | Tất cả | Link **解約** (huỷ hợp đồng) mở ngoài LINE → vào thẳng trang huỷ kèm `u_code` | - Như TC-LIFFENTRY001-07<br>- SP-C đã bật 解約用ページ | 1. Lấy link 解約 của SP-C<br>2. Mở link trên trình duyệt ngoài, lần lượt PC rồi điện thoại<br>3. Quan sát URL và trang hiện ra<br>4. **KHÔNG** bấm xác nhận huỷ | Link 解約 của SP-C | - Cả PC lẫn điện thoại: vào thẳng trang huỷ hợp đồng<br>- URL dạng `/v2/order-item/cancel/{item_code}/{u_code}`, `u_code` đúng F1<br>- Hiển thị đúng thông tin hợp đồng của F1 |  | Lấp **G7** — loại link thứ 3 |
| TC-LIFFENTRY001-09 | UI | LIFF-ENTRY-001 | LINE user — mở link sản phẩm ngoài LINE | Abnormal | auto | Tất cả | Trình duyệt sạch **chưa từng mở biểu mẫu** của bot → xác nhận fix KHÔNG phủ được trường hợp này | - Bot A đã khai channel LINE Login<br>- Máy/trình duyệt **chưa từng** mở bất kỳ biểu mẫu nào của bot A | 1. Xoá toàn bộ cookie của domain LME trên trình duyệt<br>2. Mở link 商品ページ của SP-A<br>3. Quan sát trang và kiểm tag của F1<br>4. Sau đó mở 1 biểu mẫu của bot A qua LINE Login, rồi lặp lại bước 2-3 | Link 商品ページ của SP-A | - Lượt 1: ra trang「LINEアプリで開く」, **không** gửi action<br>- Lượt 2 (sau khi đã mở biểu mẫu): vào thẳng trang chi tiết, **có** gửi action<br>- Chênh lệch này tái lập được |  | Lấp **G12** · Q1 — chứng minh fix phụ thuộc cookie do luồng biểu mẫu ghi; là **giới hạn thiết kế**, cần ghi vào spec (§6) |
| TC-REGURL001-01 | UI | REG-URL-001 | LINE user — mở link sản phẩm ngoài LINE | Normal | manual | product | Link mang **đủ** `aff_id` + `aff_setting_id` + `lp_id` → URL đích giữ đủ 3 và nối tiếp tới bước mua | - Bot A đã khai channel LINE Login, trình duyệt còn phiên<br>- Có 1 landing page (`lp_id`) và 1 affiliater (`aff_id` + `aff_setting_id`) đang hoạt động<br>- SP-A cho phép mua | 1. Dựng link 商品ページ của SP-A kèm đủ 3 tham số<br>2. Mở link trên trình duyệt ngoài (PC)<br>3. Ghi lại URL sau khi redirect<br>4. Bấm「お客様情報入力にすすむ」→ ghi URL<br>5. Đi tiếp tới màn xác nhận đơn → ghi URL<br>6. Hoàn tất mua, mở màn 販売履歴 xem quy công | Link + `aff_id`, `aff_setting_id`, `lp_id` có thật | - URL sau redirect **còn đủ cả 3** tham số<br>- 3 tham số được mang tiếp qua `enter-friend-info` và `confirm-order`<br>- Đơn trong 販売履歴 quy công đúng landing page và đúng affiliater |  | Lấp **G6** · T3 · T4 — chính là lỗi bản fix gốc (rơi `lp_id`), v3 mới sửa. `manual vì bắt buộc production` — RULE-08 (bill tiền + quy công hoa hồng) |
| TC-REGURL001-02 | UI | REG-URL-001 | LINE user — mở link sản phẩm ngoài LINE | Abnormal | manual | product | Link có `aff_id` nhưng **THIẾU** `aff_setting_id` → `aff_id` vẫn không bị rơi | - Như TC-REGURL001-01 | 1. Dựng link 商品ページ của SP-A kèm `aff_id` và `lp_id`, **bỏ** `aff_setting_id`<br>2. Mở link trên trình duyệt ngoài<br>3. Ghi lại URL sau redirect<br>4. Hoàn tất mua, kiểm quy công ở 販売履歴 | Link có `aff_id` + `lp_id`, không có `aff_setting_id` | - URL sau redirect **vẫn còn `aff_id` và `lp_id`**<br>- Đơn vẫn quy công đúng affiliater |  | Lấp **G6** — đây đúng là kịch bản bản fix gốc làm rơi `aff_id`. `manual vì bắt buộc production` |
| TC-REGURL001-03 | UI | REG-URL-001 | LINE user — mở link sản phẩm ngoài LINE | Boundary | auto | Tất cả | Link **không có tham số theo dõi nào** → URL đích không thừa dấu `?` hay `&` | - Bot A đã khai channel LINE Login, trình duyệt còn phiên | 1. Dựng link 商品ページ của SP-A **không kèm** `aff_id` / `aff_setting_id` / `lp_id`<br>2. Mở link trên trình duyệt ngoài<br>3. Copy nguyên URL sau redirect, kiểm ký tự cuối chuỗi<br>4. Bấm sang bước tiếp theo, kiểm URL | Link trần không tham số | - URL sau redirect kết thúc ngay sau `u_code`, **không** có `?` hoặc `&` thừa<br>- Bước tiếp theo vẫn đi đúng, không lỗi |  | Lấp **G6** — v3 đổi sang `array_filter` + `http_build_query`; RULE-01 (Boundary) |
| TC-DATACOUNT001-01 | Data | DATA-COUNT-001 | Action & notify theo sự kiện | Normal | auto | Tất cả | Lần đầu mở link ngoài LINE → tạo bản ghi hợp đồng nháp và số đếm mở trang = 1 | - Bot A đã khai channel LINE Login, trình duyệt còn phiên<br>- Friend F6 **chưa từng** mở SP-A<br>- SP-A gắn action「商品ページ表示時」, 稼働回数 =「何度でも」 | 1. Mở màn 販売履歴 lọc theo SP-A, ghi lại trạng thái hiện tại của F6 (chưa có dòng nào)<br>2. Mở link 商品ページ của SP-A bằng trình duyệt ngoài (PC)<br>3. Quay lại màn 販売履歴, tìm dòng của F6<br>4. Mở chat 1:1 của F6 đếm số action nhận được | Link 商品ページ của SP-A | - Xuất hiện dòng của F6 với trạng thái chưa đăng ký<br>- F6 nhận **đúng 1** action<br>- Số lần mở trang của F6 với SP-A = **1** |  | Lấp **G8** · **G9** · Q4 — quan sát qua màn 販売履歴 + chat, không dùng query DB |
| TC-DATACOUNT001-02 | Data | DATA-COUNT-001 | Action & notify theo sự kiện | Normal | auto | Tất cả | 稼働回数「何度でも」— mở 5 lần qua đường ngoài LINE → nhận đúng 5 action, số đếm = 5 | - Như TC-DATACOUNT001-01, F6 vừa chạy xong TC-DATACOUNT001-01 (đang ở mức 1) | 1. Mở lại link 商品ページ của SP-A **4 lần**, mỗi lần cách nhau ≥ 10 giây, đóng tab giữa các lần<br>2. Đếm số action F6 nhận trong chat 1:1<br>3. Kiểm số lần mở trang của F6 ở màn 販売履歴 | Lặp 4 lần | - F6 nhận **tổng cộng 5** action (1 + 4)<br>- Số đếm mở trang = **5**, khớp phép tính tay<br>- Không lần nào bị bỏ sót |  | Lấp **G9** · Q4 — đối chiếu phép tính tay theo `DATA-COUNT-001`. Đối chứng kho `TC-BIL-85` |
| TC-DATACOUNT001-03 | Data | DATA-COUNT-001 | Action & notify theo sự kiện | Boundary | auto | Tất cả | 稼働回数「1度のみ」— mở lần 2 qua đường ngoài LINE → **không** gửi thêm action, số đếm không tăng | - Bot A đã khai channel LINE Login, trình duyệt còn phiên<br>- SP-D gắn action「商品ページ表示時」với 稼働回数 =「**1度のみ**」<br>- Friend F7 chưa từng mở SP-D | 1. Mở link 商品ページ của SP-D bằng trình duyệt ngoài → ghi số action F7 nhận<br>2. Mở lại đúng link đó lần thứ 2 → ghi số action F7 nhận<br>3. Kiểm số lần mở trang của F7 ở màn 販売履歴 | Link 商品ページ của SP-D, mở 2 lần | - Lần 1: F7 nhận 1 action<br>- Lần 2: F7 **KHÔNG** nhận thêm action<br>- Tổng số action = 1<br>- Trang vẫn mở bình thường ở lần 2 |  | Lấp **G9** · **G10** · Q4 — Dev nêu rõ điều kiện `flag_page_start != 2`. RULE-01 (Boundary). Đối chứng kho `TC-BIL-84` |
| TC-CONC001-01 | Data | CONC-001 | Action & notify theo sự kiện | Abnormal | auto | Tất cả | Mở **2 tab đồng thời** cùng link sản phẩm → số đếm mở trang không bị thiếu | - Bot A đã khai channel LINE Login, trình duyệt còn phiên<br>- SP-A gắn action「商品ページ表示時」, 稼働回数 =「何度でも」<br>- Friend F8 chưa từng mở SP-A | 1. Ghi lại số lần mở trang hiện tại của F8 (= 0)<br>2. Chuẩn bị 2 tab cùng dán sẵn link 商品ページ của SP-A<br>3. Enter **gần như cùng lúc** ở cả 2 tab<br>4. Đếm số action F8 nhận trong chat 1:1<br>5. Kiểm số lần mở trang của F8 ở màn 販売履歴<br>6. Lặp lại toàn bộ 3 lượt | 2 request gần đồng thời × 3 lượt | - Số action F8 nhận **khớp** số lần mở trang ghi nhận, ở cả 3 lượt<br>- Không có lượt nào action gửi 2 lần mà số đếm chỉ tăng 1 |  | Lấp **G9** · Q5 — Dev tự khai `count_action_view_page` dùng read-modify-write, nguy cơ đếm thiếu (`03` mục X4). Tái hiện được → raise ticket kỹ thuật riêng |
| TC-INTGLINE001-01 | API | INTG-LINE-001 | LINE user — mở link sản phẩm ngoài LINE | Normal | auto | Tất cả | Gọi API hồ sơ LINE thành công → suy đúng khách và redirect đúng | - Bot A đã khai channel LINE Login<br>- Trình duyệt PC còn phiên LINE Login của **F1** (không phải friend khác) | 1. Mở link 商品ページ của SP-A trên trình duyệt đó<br>2. Đọc `u_code` trên URL sau redirect<br>3. Đối chiếu với `u_code` của F1 ở màn chi tiết friend<br>4. Kiểm tag/action mà F1 nhận | Link 商品ページ của SP-A | - `u_code` trên URL **đúng của F1**, không phải friend khác<br>- Action gửi đúng cho F1 |  | Lấp Q2 — nhánh happy path của lời gọi `api.line.me` |
| TC-INTGLINE001-02 | API | INTG-LINE-001 | LINE user — mở link sản phẩm ngoài LINE | Abnormal | auto | Tất cả | Phiên LINE Login **bị thu hồi / không còn hợp lệ** → rơi về hành vi cũ, không trắng trang | - Bot A đã khai channel LINE Login<br>- Trình duyệt còn cookie phiên nhưng token đã bị vô hiệu (đăng xuất LINE Login ở nơi khác, hoặc sửa giá trị cookie thành chuỗi sai) | 1. Làm cookie phiên LINE Login không còn hợp lệ<br>2. Mở link 商品ページ của SP-A trên trình duyệt đó<br>3. Quan sát trang hiện ra và thời gian phản hồi<br>4. Kiểm tag của F1 | Cookie token sai/thu hồi | - Hiện trang「LINEアプリで開く」 (hành vi cũ)<br>- **KHÔNG** trắng trang, **KHÔNG** lỗi 500<br>- Trang trả về trong thời gian chấp nhận được (không treo)<br>- Không gửi action |  | Lấp Q2 — `INTG-LINE-001` yêu cầu giả lập từng loại lỗi LINE |
| TC-INTGLINE001-03 | API | INTG-LINE-001 | LINE user — mở link sản phẩm ngoài LINE | Boundary | auto | Tất cả | API hồ sơ LINE **phản hồi chậm / timeout** → trang không treo vô hạn | - Như TC-INTGLINE001-02 nhưng phiên hợp lệ<br>- Có cách làm chậm kết nối tới `api.line.me` (giới hạn băng thông ở DevTools hoặc chặn tạm domain) | 1. Bật giả lập mạng rất chậm / chặn `api.line.me`<br>2. Mở link 商品ページ của SP-A<br>3. Bấm giờ tới khi trang trả kết quả<br>4. Ghi lại trang cuối cùng hiện ra | Giả lập timeout | - Trang **trả về trong thời gian hữu hạn**, không treo vô hạn<br>- Rơi về trang「LINEアプリで開く」 chứ không lỗi 500<br>- Không gửi action |  | Lấp Q2 — RULE-01 (Boundary của quan điểm Cao) |
| TC-SEC002-01 | API | SEC-002 | LINE user — mở link sản phẩm ngoài LINE | Abnormal | auto | Tất cả | Ép lỗi ở bước nhận diện khách → quét màn lỗi, console, log server: không lộ token / `u_code` / LINE user ID | - Bot A đã khai channel LINE Login<br>- Có quyền đọc log ứng dụng của môi trường test | 1. Làm cookie token sai để ép vào nhánh lỗi<br>2. Mở link 商品ページ của SP-A, mở sẵn DevTools tab Console + Network<br>3. Đọc toàn bộ nội dung hiển thị trên màn hình<br>4. Đọc Console và nội dung response<br>5. Mở log ứng dụng, tìm chuỗi access token, `u_code`, LINE user ID | Cookie token sai | - Màn hình **không** hiện access token / `u_code` / LINE user ID<br>- Console và response **không** chứa access token<br>- Log server **không** chứa access token (Dev khai "chỉ gửi tới `api.line.me`, không ghi log" — phải kiểm chứng) |  | Lấp Q3 — `SEC-002` yêu cầu chủ động tạo lỗi rồi quét 4 nơi. Evidence: ảnh Console + trích đoạn log |
| TC-SEC002-02 | API | SEC-002 | LINE user — mở link sản phẩm ngoài LINE | Normal | auto | Tất cả | Luồng thành công → log server không chứa access token | - Như TC-SEC002-01 nhưng phiên hợp lệ | 1. Ghi lại mốc thời gian bắt đầu<br>2. Mở link 商品ページ của SP-A, vào được trang chi tiết<br>3. Mở log ứng dụng từ mốc đó tới hiện tại<br>4. Tìm chuỗi access token và LINE user ID | — | - Log **không** chứa access token<br>- Nếu có ghi LINE user ID thì phải ở dạng che bớt hoặc có lý do nghiệp vụ rõ ràng |  | Lấp Q3 |
| TC-SEC001-01 | API | SEC-001 | LINE user — mở link sản phẩm ngoài LINE | Abnormal | auto | Tất cả | Thay `u_code` trên URL bằng `u_code` của **friend khác** → không xem được dữ liệu người khác | - Bot A có 2 friend F1 và F9, cả hai đã kết bạn<br>- F9 **đang có** hợp đồng 継続 với SP-C; F1 thì không<br>- Trình duyệt đang ở phiên của F1 | 1. Mở link 商品ページ SP-A qua đường ngoài LINE → lấy `u_code` của F1 trên URL<br>2. Lấy `u_code` của F9 ở màn chi tiết friend<br>3. Sửa URL thành `/v2/order-item/change/{item_code}/{u_code_của_F9}` và mở<br>4. Quan sát nội dung trang | `u_code` của F9 | - Không hiển thị thông tin hợp đồng / thẻ của F9 cho phiên đang là F1<br>- Nếu hệ thống cho phép (do `/v2` không có middleware xác thực theo spec §2.4) → **raise ticket bảo mật riêng**, ghi rõ đây là hành vi sẵn có chứ không do fix gây ra |  | Lấp Q12 — spec `bill-item` §2.4 ghi rõ prefix `/v2` không có middleware. Fix làm đường vào dễ tiếp cận hơn nên cần chốt lại |
| TC-PERM002-01 | UI | PERM-002 | LINE user — mở link sản phẩm ngoài LINE | Abnormal | auto | Tất cả | Friend **đã BLOCK bot** mở link ngoài LINE → không nhận diện được, không gửi action | - Bot A đã khai channel LINE Login<br>- Friend F4 **đã block** bot A<br>- Trình duyệt còn phiên LINE Login của F4<br>- SP-A gắn action「商品ページ表示時」 | 1. Mở link 商品ページ của SP-A trên trình duyệt đó (PC)<br>2. Quan sát trang hiện ra<br>3. Lặp lại trên điện thoại<br>4. Kiểm xem có action nào được gửi không | Link 商品ページ của SP-A | - Không redirect thẳng vào trang chi tiết kèm `u_code` của F4<br>- Rơi về trang「LINEアプリで開く」 hoặc trang kết bạn, đúng như luồng trong LINE<br>- **KHÔNG** gửi action cho friend đã block |  | Lấp G13 · Q8 · Q9 — fix lọc `is_blocked = 0`. Đối chứng kho `TC-BIL-121` (cùng ý định, khác điểm vào) |
| TC-PERM002-02 | UI | PERM-002 | LINE user — mở link sản phẩm ngoài LINE | Abnormal | manual | product | **Bot hết hạn hợp đồng LME quá 7 ngày** → chặn trang mua kể cả qua đường mới | - Bot B có `plan_type = 1`, đã quá hạn > 7 ngày<br>- SP-B thuộc bot B<br>- Bot B đã khai channel LINE Login, trình duyệt còn phiên của friend bot B | 1. Mở link 商品ページ của SP-B qua trình duyệt ngoài (PC)<br>2. Quan sát trang hiện ra<br>3. Lặp lại trên điện thoại<br>4. Kiểm có action nào được gửi không | Link 商品ページ của SP-B | - Cả 2 lần đều bị chặn, hiện trang lỗi 410 đúng như luồng trong LINE<br>- **KHÔNG** vào được trang mua<br>- **KHÔNG** gửi action |  | Lấp G13 · Q8. `manual vì bắt buộc production` — RULE-08 (trạng thái hợp đồng/bill tiền). Đối chứng kho `TC-BIL-120` |
| TC-FRIEND001-01 | UI | FRIEND-001 | LINE user — mở link sản phẩm ngoài LINE | Abnormal | auto | Tất cả | Tài khoản LINE **CHƯA kết bạn** mở link ngoài LINE → ra trang kết bạn, không nhảy thẳng vào trang mua | - Bot A đã khai channel LINE Login<br>- Tài khoản LINE F2 **chưa kết bạn** bot A<br>- Trình duyệt còn phiên LINE Login của F2 | 1. Mở link 商品ページ của SP-A trên trình duyệt đó (PC)<br>2. Quan sát trang hiện ra<br>3. Lặp lại trên điện thoại<br>4. Kết bạn rồi mở lại link, quan sát | Link 商品ページ của SP-A | - Cả 2 lần đầu: ra **trang kết bạn**, không vào thẳng trang mua<br>- Sau khi kết bạn: vào được trang chi tiết kèm `u_code` và nhận action |  | Lấp G13 · Q1 · Q9 — chiều "trạng thái user" của ma trận `LIFF-ENTRY-001`. Đối chứng kho `TC-BIL-112` |
| TC-FRIEND001-02 | UI | FRIEND-001 | LINE user — mở link sản phẩm ngoài LINE | Normal | auto | Tất cả | **OLD FRIEND** (kết bạn trước khi bot vào tool) mở link ngoài LINE → tự thêm vào tool và nhận action | - Bot A đã khai channel LINE Login<br>- Tài khoản LINE F3 là old friend, **chưa có** bản ghi trong màn 友だちリスト<br>- Trình duyệt còn phiên LINE Login của F3 | 1. Xác nhận F3 chưa xuất hiện ở màn 友だちリスト<br>2. Mở link 商品ページ của SP-A qua trình duyệt ngoài<br>3. Quan sát trang hiện ra<br>4. Kiểm lại màn 友だちリスト và chat 1:1 của F3 | Link 商品ページ của SP-A | - F3 được thêm vào màn 友だちリスト<br>- Vào được trang chi tiết kèm `u_code`<br>- F3 nhận được action |  | Lấp G13 · Q9. Đối chứng kho `TC-BIL-114` |
| TC-STATE001-01 | UI | STATE-001 | LINE user — mở link sản phẩm ngoài LINE | Abnormal | manual | product | Đang có **đơn định kỳ chờ webhook** → chặn đúng thông báo kể cả qua đường mới | - Bot A đã khai channel LINE Login<br>- Friend F1 có đơn 継続 của SP-C đang ở trạng thái chờ webhook<br>- Trình duyệt còn phiên LINE Login của F1 | 1. Mở link 商品ページ của SP-C qua trình duyệt ngoài (PC)<br>2. Đọc thông báo hiện ra<br>3. Lặp lại trên điện thoại<br>4. So sánh với thông báo khi mở cùng link **trong** app LINE | Link 商品ページ của SP-C | - Cả 3 đường (PC ngoài / điện thoại ngoài / trong LINE) hiện **cùng một** thông báo chặn<br>- Không cho tạo đơn mới<br>- Không gửi action trùng |  | Lấp G13 · Q10. `manual vì bắt buộc production` — RULE-08 (trạng thái bill tiền). Đối chứng kho `TC-BIL-117` |
| TC-REGSHARED001-01 | UI | REG-SHARED-001 | Domain LIFF & redirect | Normal | auto | Tất cả | Regression — link **BIỂU MẪU** mở ngoài LINE vẫn chạy đúng sau khi thêm helper dùng chung | - Bot A đã khai channel LINE Login<br>- Có 1 biểu mẫu của bot A đang hoạt động, gắn action khi gửi<br>- Friend F1 đã kết bạn | 1. Mở link biểu mẫu qua trình duyệt ngoài trên PC → điền và gửi<br>2. Lặp lại trên điện thoại<br>3. Kiểm câu trả lời ở màn 回答一覧 và action F1 nhận | Link biểu mẫu | - Cả 2 lần: mở được, nhận diện đúng F1, gửi được câu trả lời<br>- Câu trả lời gắn đúng F1 ở màn 回答一覧<br>- Action của biểu mẫu vẫn chạy<br>- **Không** thay đổi so với trước fix |  | Lấp **G11** · **G19** · Q7 — nhánh `unique_key` nằm cùng hàm `liffAppCallback`, phải chứng minh không bị helper mới phá |
| TC-REGSHARED001-02 | UI | REG-SHARED-001 | Domain LIFF & redirect | Normal | auto | Tất cả | Regression — mở link sản phẩm **TRONG** app LINE (đường cũ) không đổi hành vi | - Bot A đã khai channel LINE Login<br>- Friend F1 đã kết bạn, SP-A gắn action「商品ページ表示時」 | 1. Gửi template có button mở SP-A cho F1<br>2. F1 tap button **trong app LINE**<br>3. Quan sát trang, URL, và action nhận được<br>4. Hoàn tất tới bước nhập thông tin khách | Template button mở SP-A | - Mở đúng trang chi tiết kèm `u_code` như trước fix<br>- Action được gửi đúng 1 lần<br>- Đi tiếp được sang bước nhập thông tin |  | Lấp **G11** · Q7 — đối chứng đường cũ `QRCodeController::checkFriend`. Chạy kèm kho `TC-BIL-107` + `TC-BIL-111` nguyên trạng |
| TC-REGSHARED001-03 | UI | REG-SHARED-001 | LINE user — mở link sản phẩm ngoài LINE | Abnormal | auto | Tất cả | Helper dùng chung — **2 caller PC và điện thoại** cho cùng kết quả với cùng phiên | - Bot A đã khai channel LINE Login<br>- Cùng 1 tài khoản LINE F1, chuẩn bị phiên LINE Login còn hạn trên **cả** trình duyệt PC lẫn trình duyệt điện thoại | 1. Mở link 商品ページ của SP-A trên PC → ghi lại URL đích đầy đủ<br>2. Mở **cùng link** trên điện thoại → ghi lại URL đích đầy đủ<br>3. So sánh 2 URL từng tham số<br>4. So sánh action F1 nhận ở 2 lần | Cùng link, 2 thiết bị | - 2 URL đích **giống nhau** ở `item_code`, `u_code` và các tham số theo dõi<br>- Cả 2 lần đều gửi action<br>- Không nhánh nào thiếu tham số so với nhánh kia |  | Lấp **G14** · Q7 — helper có 2 caller, bắt buộc test từng nơi (câu hỏi adversarial #3) |

**Không đề xuất TC cho:**
- **G20** (20 TC orphan vùng `TemplateService`) — không viết TC mới; xử lý bằng cách đóng round cũ, xem §4 #2.
- **G21** (4 TC vùng job) — Dev khai rõ **chưa fix**, cần task riêng; viết TC ở ticket này sẽ tạo kỳ vọng sai. Xem §6.
- **G18** (root cause thứ 2 hợp lý) — cần Dev trả lời trước phạm vi switch `liffAppCallback`, không tự đoán. Xem §6.
- **Q6** (`ENV-003` / RULE-08) — không phải GAP nội dung mà là **GAP môi trường**; đã xử lý bằng cột `Phạm vi ENV = product` ở 4 TC bên trên và §4 #5.
- **Q11** (`COMPAT-LEGACY-001`) — dùng lại kho **`TC-BIL-373`** (link item chuyển từ LIFF app QR landing sang LIFF app form), chạy thêm chiều "mở ngoài LINE" thay vì viết TC mới.

---

## 6. Spec update needed

| # | Nội dung cần chốt | Căn cứ | Người chốt |
|---|---|---|---|
| 1 | **Đường lấy `u_code` thứ 2 chưa có trong spec.** Spec `bill-item` §2.4 chỉ mô tả 1 đường: `EP-88 POST /ajax/get-ucode-by-line-user-id` qua LIFF (retry 15 lần). Fix v4 thêm đường **cookie access token LINE Login → API hồ sơ LINE → `u_code`** cho trường hợp mở ngoài LINE | [spec-features/admin/bill-item/feature-spec.md](../../spec-features/admin/bill-item/feature-spec.md) §2.4 vs `03-dev-impact.md` mục 2 | BA + Dev |
| 2 | **Giới hạn thiết kế phải ghi vào spec**: fix chỉ có hiệu lực khi trình duyệt còn cookie LINE Login, mà cookie đó **chỉ do luồng BIỂU MẪU ghi ra** (`LiffController:1028`, TTL 360 phút). Khách mở link sản phẩm trên trình duyệt chưa từng dùng biểu mẫu trong 6h thì **action vẫn không gửi** — tức bug **chưa được fix hoàn toàn** | `03-dev-impact.md` mục 2 "ĐIỀU KIỆN HIỆU LỰC" + mục X3 | PM/BA |
| 3 | **Hành vi mong muốn khi chọn「外部ブラウザで開く」cho link nội bộ.** Bản v1 (đã revert) ép link mở **trong** LINE. Bản v4 giữ nguyên mở **ngoài** LINE. Hai bản kỳ vọng ngược nhau — cần chốt bản nào là chuẩn, và có nên ẩn/khoá lựa chọn này cho link LIFF không | Journal #133240 (v1) vs #137231 (v4); đây cũng là gốc của §4 #2 | BA |
| 4 | **Phạm vi các case còn lại trong `liffAppCallback`.** Dev khai "các nhánh đặt lịch (booking event FA-021 / lesson calendar FA-019) trong cùng hàm **có thể thiếu y hệt**, cố ý không gộp". Cần chốt: có cùng lỗi không, và mở ticket riêng hay không | `03-dev-impact.md` mục X2 + §1 `G19` | Dev + Leader |
| 5 | **Vùng job chưa fix.** Gửi tin qua job (broadcast / step / remind — `linect-service MessageBuilder`) còn nguyên lỗi cũ. NEW-27 đang `fail` với ticket **#41140** — cần xác nhận #41140 chính là task riêng cho job, không phải bug của ticket 34595 | `03-dev-impact.md` mục X1 + §1 `G21` | Dev + Leader |
| 6 | **Rủi ro đếm sai đã biết.** `count_action_view_page` dùng read-modify-write → đếm thiếu khi trùng request; fix này **làm tăng lưu lượng** vào đó. Ngoài ra spec ghi nhận **R16**: `$sItem` chưa khởi tạo trong case `view_page` → luôn ghi `13001` kể cả 単品 ⇒ sai thống kê theo loại trigger | `03-dev-impact.md` mục X4 + [feature-spec.md](../../spec-features/admin/bill-item/feature-spec.md) R16 (`:1177`) | Dev |
| 7 | **`/v2` không có middleware xác thực.** Spec ghi rõ toàn bộ prefix `/v2` định danh chỉ bằng `u_code` trên URL. Fix làm đường vào này dễ tiếp cận hơn từ ngoài LINE ⇒ cần chốt có siết lại không (liên quan `TC-SEC001-01`) | [feature-spec.md](../../spec-features/admin/bill-item/feature-spec.md) §2.4 (`:347`) | Leader + Dev |
