# 05 — Review Report (round 2)

## 0. Nguồn TC

| Mục | Giá trị |
|---|---|
| Nguồn đã dùng | **MCP LME TEST STUDIO** — task `#229` (ticket 38944, round 10, `tc-ready`, branch `origin/ai_fixbug_38944`, `aiResult=fail`, `openBugs=2`) |
| Tổng số TC review | **47** — `pass 39` · `fail 2` · `skip 6` → **39/47 (83%)**, nhưng **3 trong 39 `pass` không có bằng chứng** (xem §4 #2) ⇒ chỉ **36/47 (77%) là Đạt có cơ sở** |
| Cập nhật giữa kỳ | Bản đầu của report này soát trên 48 TC. Sau đó: **TC `NEW-58` đã bị xóa** theo chỉ định Leader (tính năng `deleteMessageGroup` không còn trên tool), và QA chạy tay ngày 2026-09-10 06:19→06:54 làm **NEW-53 · NEW-54 · NEW-57** đổi `skip → pass`. Toàn bộ §1/§2/§4/§5 bên dưới đã cập nhật theo số mới. |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận: 11/23 vùng ảnh hưởng đủ TC · 7 GAP · 5 RISK.**

> ⚠️ **Bổ sung 2026-09-10 (lần soát thứ 2)** — G9 → G11 tìm được khi rà **từng mục** của note Dev (mục "4.2 Data", "KHÔNG BỊ ẢNH HƯỞNG", "PHẠM VI TEST ĐỀ XUẤT") thay vì chỉ rà theo file/hàm. Lần rà đầu bỏ sót vì các mục này không xuất hiện trong `diffStat`.

> Chiều `dev-impact` = 8 điểm Dev tự kê trên Redmine. ⚠️ Redmine có **2 note**: `#135367` (2026-09-09 06:58) và **`#135413` (07:40) là ĐÍNH CHÍNH** — đã gỡ phần sửa `saveSettingDisplayInfo` (V1) khỏi branch (commit `21116c9b1b`) vì V1 là code chết ở phía UI, và bỏ bảng `line_user` khỏi 4.2. Báo cáo này lấy theo **note đính chính**.
> Chiều `diff code` = `task_get_context(dev_impact + spec_delta)` của Studio: `diffAvailable=true`, `diffStat` = **5 files, 207 insertions, 50 deletions**.
> Vùng đủ TC (không liệt kê): `settingFriendDisplayModal` null-check · `saveSettingDisplayInfoItem` chống dòng trùng · 4 chỗ kiểm rỗng khi setting đã xóa · `saveSettingDisplayInfoV2` thiếu `type_data` / thiếu `sortList` · cờ chặn lưu nhanh `chat-v2.js` · `setting_display_info_friend_chat11` giảm dòng trùng sinh mới · `friend_information_value` giữ nguyên logic · `total_user_has_value` chỉ +/- khi setting còn tồn tại · REQ-001→REQ-012 (trừ các dòng dưới) · BUG root cause `#38944`.

| # | Chiều | Vùng ảnh hưởng | TC hiện có | Status | Vì sao thiếu |
|---|---|---|---|---|---|
| **G1** | diff code | **`#40689` — `FriendDetailFriendInfoService::normalizeDateValue`** (40 dòng): chặn giá trị không phải ngày ở màn 「友だち詳細」 + trường 生年月日, abort 422 「日付のフォーマットが無効です。」 | `REQ-014`: NEW-54 `pass`* · NEW-57 `pass`* · NEW-55 `fail` · NEW-56 `fail` | **RISK** *(hạ từ GAP)* | Tiến triển: 2 TC tầng UI đã được **chạy tay** ngày 2026-09-10 và chấm Đạt — nhưng **cả hai có `actual` TRỐNG, không evidence, không `by`** (§4 #2) ⇒ theo RULE-02 chưa đủ cơ sở. 2 TC tầng API vẫn `fail` ở đúng một vế: **HTTP 404 thay vì 422** — nay đã raise **Redmine #40805** (`#991`) và **#40808** (`#992`), cần quyết định phạm vi ở §6 #1. |
| **G2** | dev-impact | **`blockUserGroup()` — khoá thông báo lỗi `#40556`** (「設定の変更に失敗しました。」) | NEW-59 `skip` | **GAP** | Nhánh `catch` **không kích hoạt được từ bên ngoài**: runner đã đọc code tại chỗ — `line_id` không tồn tại chỉ làm câu UPDATE khớp 0 dòng, KHÔNG ném ngoại lệ, nên luôn rơi vào nhánh thành công (`HTTP 200 {"success":true}`). Câu tiếng Nhật tồn tại trong mã nhưng chưa ai đọc được trên UI. ⚠️ **`deleteMessageGroup()` đã được Leader xác nhận KHÔNG còn tồn tại trên tool** → TC `NEW-58` đã xóa khỏi Studio (2026-09-10). Đây là **lần thứ 3** Dev kê một thứ không còn dùng được vào `4.1` (sau `saveSettingDisplayInfo` V1 đã tự đính chính, và màn chat 1:1 bản cũ) — xem §6 #9. |
| **G3** | dev-impact | **4.3 FA-016 Lịch hẹn hành động** — mục 「年月日」 đặt/huỷ lịch nhắc, "nay không chạy với mục đã bị xoá" | NEW-20 `skip` | **GAP** | TC duy nhất cho chiều lịch nhắc. `last_exec` = manual `skip` từ **2026-09-07**, **KHÔNG có `reason_code`, không ghi lý do**, và **không được chạy lại trong run 980 / 1170 / 1183** — tức 3 vòng gần nhất đều bỏ qua. TC này khai `env_scope=prd` nhưng chưa từng chạy ở production. |
| **G4** | dev-impact | **4.2 "Dòng cấu hình TRÙNG đã sinh ra từ trước vẫn nằm nguyên trong DB — đợt này chỉ ngăn sinh thêm, không dọn dữ liệu cũ"** | Không có | **GAP** | 5 TC kiểm *không sinh thêm* dòng trùng (NEW-34/42/48/61 pass), nhưng **0 TC** kiểm hệ thống cư xử thế nào khi bot **đã có sẵn dòng trùng** — mục hiện 2 lần ở cột phải? sắp xếp / bỏ mục / lưu nhanh có đúng? Dev tự tuyên bố dữ liệu bẩn còn tồn tại ⇒ phải có TC cho trạng thái đó. |
| **G5** | diff code | **`spec_delta` / `dev_impact` của Studio là SNAPSHOT CŨ** | — | **RISK** | `computedAt = 2026-09-09T07:40:23Z`, tức **11 giây TRƯỚC** journal đính chính `#135413` (07:40:34Z). `journalCount=4` và `lastNotes` **không chứa** đính chính. Vì vậy `dev_impact` Studio vẫn kê `saveSettingDisplayInfo(V1)` và bảng `line_user` — **2 thứ đã bị gỡ khỏi branch**. Chiều (b) của vòng review này phải đọc kèm đính chính; cần **recompute snapshot** trước vòng sau. |
| **G6** | dev-impact | **4.1 của Dev KHÔNG khớp `diffStat` — 3/5 file không được kê** | TC có cover (REQ-009 / 013 / 014) | **RISK** | `diffStat` có 5 file; note đính chính chỉ kê 2 (`ChatController.php`, `chat-v2.js`). Thiếu: **`Basic/FriendInformationController.php`** (10 dòng, `#40661`) · **`FriendDetailFriendInfoService.php`** (40 dòng, `#40689`) · **`Basic/ChatController.php`** (6 dòng) — file này note còn ghi rõ "**KHÔNG sửa**" nhưng diff có 6 dòng thay đổi. Coverage thì có, nhưng tài liệu dev sai ⇒ vòng sau dễ bỏ sót. |
| **G7** | diff code | **`#40661` — `total_user_has_value` trừ theo NGƯỜI, sàn `GREATEST(...,0)`** | NEW-52 `pass` (Normal) · NEW-53 `pass`* (Boundary) | **RISK** | Nhánh thường đã chứng minh (xóa 2 bạn → bộ đếm giảm đúng 2). Vế **sàn 0** vừa được chạy tay ngày 2026-09-10 06:32 và chấm Đạt, **nhưng `actual` TRỐNG** — trong khi lần chạy AI trước đó đã **đo và ghi lại từng đường** để kết luận tiền đề "bộ đếm < số người có giá trị" **không dựng được qua app**. Hai kết luận ngược nhau mà bản chấm Đạt lại là bản không có số liệu ⇒ chưa chốt được (§4 #2). |
| **G8** | diff code | **Điểm Dev tự nhận CHƯA BỊT: race tạo dòng cấu hình trùng** (không unique DB, không transaction) | NEW-61 `pass` | **RISK** | Chỉ 1 lần thăm dò 2 request song song và Đạt. Race không tái hiện ở 1 lần chạy **không chứng minh đã an toàn**; Dev nói rõ "nếu QA còn bắt được nhân đôi thì đây là chỗ duy nhất chưa bịt". Cần chạy lặp nhiều vòng mới có số liệu cho Leader quyết. |
| **G9** | dev-impact | **4.2 — "đồng bộ Elasticsearch: các lời gọi KHÔNG bị sửa dòng nào, chỉ nằm SAU các bước kiểm tra mới"** | **0 TC** (quét cả 48 TC, không TC nào nhắc Elasticsearch / hàng đợi đồng bộ) | **GAP** | Đây là rủi ro trực tiếp của kiểu sửa "thêm guard rồi return sớm": nếu một guard mới thoát trước khi tới lời gọi đồng bộ thì **chỉ mục tìm kiếm bị cũ trong im lặng** — không lỗi, không log, chỉ sai kết quả tìm kiếm bạn bè. Spec `chat-11` xác nhận luồng chat có `INSERT sync_elasticsearch (type=2)` (:136, :151). Kho FA-013 §15 có 6 TC đồng bộ ES nhưng **chỉ cho thao tác ở màn friendlist** (xóa/block/ẩn friend), **không có** cho đường ghi giá trị friend info. |
| **G10** | dev-impact | **4.2 — `bot_line_user.is_blocked`: "chỉ đổi nội dung JSON trả về ở nhánh lỗi, **logic ghi không đổi**"** + **P7 "cả nhánh thành công lẫn nhánh lỗi"** | NEW-59 `skip` — chỉ nhắm nhánh LỖI | **GAP** | **Nhánh THÀNH CÔNG của chặn/bỏ chặn thành viên nhóm: 0 TC.** Dev tự khẳng định "logic ghi không đổi" — đúng ra đây là chỗ cần đối chứng âm, vì hàm có bị sửa (thêm khoá `msg`) và bọc `try/catch` cả hàm. Kho FA-001 §39 có 15 TC Group chat nhưng **không TC nào** cho thao tác này. *(Vế xóa tin nhắn nhóm đã bỏ khỏi phạm vi — tính năng không còn trên tool.)* |
| **G11** | dev-impact | **"KHÔNG BỊ ẢNH HƯỞNG" — "Không đổi số hiệu phiên bản tài nguyên tĩnh trong config"** | **0 TC** | **GAP** | Mâu thuẫn nội tại: `public/js/chats/chat-v2.js` **bị sửa** (cờ chặn lưu nhanh, `saveItemInfo`) nhưng **asset version không bump** ⇒ trình duyệt của người dùng đang mở sẵn sẽ **tiếp tục dùng bản JS cũ không có cờ chặn**. Khi đó hành vi B3 mất tác dụng và chỉ còn chốt phía máy chủ — tức rơi đúng về tình huống NEW-60 (màn cũ, chỉ có chốt BE) mà TC đó đang `skip`. Trigger `DEPLOY-ASSET-001` khớp tuyệt đối. |
| **G12** | dev-impact | **P1 — "sửa từng mục đủ các kiểu — chữ / số / 選択肢 / 年月日 / ảnh / PDF; có nhập, **xoá trắng**, chọn 「-」"** | Vế *xoá trắng bằng giao diện* hiện **chỉ có cho 選択肢** (NEW-12 `pass`, qua lựa chọn 「-」) | **GAP** | 3 kiểu **記述 · ポイント · 年月日 không có TC nào xóa trắng bằng thao tác người dùng**. Chỉ có đường **API thiếu khóa giá trị** (NEW-22 `pass`) — mà chính NEW-41 đã chứng minh **giao diện KHÔNG bao giờ sinh ra payload thiếu khóa**, nên NEW-22 không thay thế được đường UI: hai đường đi vào cùng nhánh xóa nhưng khác điểm vào. Vế 画像/PDF đã có kho `TC-CHT-353` phụ trách. |

---

## 2. Thiếu so với quan điểm test

**Kết luận: 8 quan điểm Trigger khớp task nhưng TC chưa cover đủ (7 Cao · 1 nâng-Cao).** Các quan điểm đã đủ (`CONC-001`, `CONC-003`, `DATA-DB-001`, `DATA-HIST-001`, `FUNC-SEQ-001`, `MSG-USER-001`, `REG-SHARED-001`, `STATE-MATRIX-001`, `TOOL-*`) không liệt kê.

| # | Mã quan điểm | Ưu tiên | Trigger khớp vì | TC hiện có | Kết luận |
|---|---|---|---|---|---|
| **Q1** | `STATE-DEP-001` | Cao | Bản vá đổi hành vi với mục 「年月日」 có action lịch (`event_step_time`). Spec `detail-friend` xác nhận `birthday` → `settingEventTimeFriendInfo` → INSERT `event_step_time`. | 3 TC, **0 pass** | **[BLOCKER]** — và 2/3 TC **gán sai mã**: NEW-55 / NEW-56 là *validate định dạng ngày*, không phải *phụ thuộc lịch đã đặt*. Gỡ 2 TC sai nhãn ra thì quan điểm này chỉ còn **NEW-20 (skip)** ⇒ chiều lịch nhắc **thực chất 0 TC**. Nhãn sai đang làm quan điểm trông như có 3 TC (`DUP-INFLATE` dạng nhãn). |
| **Q2** | `FUNC-001` + `FUNC-004` (tầng UI màn 「友だち詳細」) | Cao | `#40689` sửa đúng màn này; RULE-07 đòi khớp cả 3 tầng DB + màn hình + output. | NEW-54 `pass`* · NEW-57 `pass`* | **[MAJOR]** *(hạ từ BLOCKER)* — QA đã chạy tay ngày 2026-09-10 (06:19 và 06:40) và chấm Đạt cả 2, gỡ được chỗ automation bế tắc (「友だち情報」 nằm trong dropdown của tab 「基本情報」, ô ngày là component `lme-date-picker`). **Nhưng cả 2 bản chấm Đạt đều `actual` TRỐNG, không evidence** ⇒ theo RULE-02 chưa nghiệm thu được. Chỉ cần QA đính bằng chứng là quan điểm này đóng. |
| **Q3** | `OUT-TRUTH-001` | Cao | "BẮT BUỘC với mọi thao tác lưu/gửi **có thông báo kết quả**" — triệu chứng gốc của ticket là hộp thoại 「undefined」. | 3 TC, **1 pass** (NEW-44); NEW-18 · NEW-59 `skip` | **[BLOCKER]** — `REQ-012` chỉ được chứng minh ở **tầng endpoint** (NEW-50 pass: response trả đúng 「保存に失敗しました。入力内容をご確認ください。」). **Chưa ai đọc được câu đó trên hộp thoại thật**: NEW-18 skip vì cả 3 đường làm luồng lưu thất bại đều đã bị guard mới đóng lại. Phần chat nhóm còn lại (`blockUserGroup`, G2) cũng chưa kiểm. *(Trước có 4 TC; NEW-58 đã xóa cùng tính năng `deleteMessageGroup`.)* |
| **Q4** | `SEC-ISO-001` | Cao | Đường XÓA giá trị mới tác động dữ liệu khách ⇒ phải chứng minh không vượt ranh giới **bot**. | NEW-23 `skip` | **[MAJOR]** — skip vì **thiếu dữ liệu test**, không phải vì sản phẩm: runner duyệt 17 hội thoại bot 323 + 10 hội thoại bot 69, **không có LINE user nào là bạn của cả hai bot**, và không tạo được bạn LINE mới từ admin. Kho đã có oracle `TC-FRI-192` / `TC-FRI-197`. |
| **Q5** | `DATA-COUNT-001` + `FUNC-004` | Cao | Bộ đếm 「回答人数」 bị 2 đường mới chạm (`#40661` + đường xóa do thiếu `value`). | NEW-53 `pass`* · NEW-24 `skip` | **[MAJOR]** — xem G7. NEW-53 (đường xóa hàng loạt) vừa được chấm Đạt tay nhưng **không có `actual`/evidence**, ngược với kết luận đo được của lần chạy AI. NEW-24 (đường thiếu khóa giá trị) **vẫn `skip`** và là TC duy nhất còn mang bug `#988` đang `open`. |
| **Q6** | `COMPAT-LEGACY-001` | Cao | RULE-09 — `4.1` của Dev kê `resources/views/chats/chat_basic.blade.php` (chat 1:1 **bản cũ**) là màn đi qua; chốt chặn trùng ở FE chỉ có ở bản mới. | NEW-60 `skip` | **[MAJOR]** — **2 nguồn nói ngược nhau.** Lần chạy AI: thử 3 đường vào (`/chat-v2`, `/basic/chat-v2`, `/basic/chat`), tất cả HTTP 200 nhưng **redirect về `/basic/overview`, không nạp `chat.js`**; đối chiếu mã nguồn thì route duy nhất trỏ tới view `chats.chat_basic` **đang bị comment trong `routes/web.php`**. Lần chạy tay 2026-09-10 06:24 lại ghi `actual` = **"Truy cập được màn chat 1:1 BẢN CŨ"** nhưng **vẫn chấm `skip`**. ⇒ Nếu màn cũ truy cập được thì RULE-09 **bắt buộc** phải test, và TC này không được để `skip`; nếu không thì phải gỡ khỏi `4.1`. Cần chốt ở §6 #3. |
| **Q7** | `ENV-003` | Cao | "BẮT BUỘC khi tính năng chạm job nền… **Không được đánh × với lý do staging đã pass**" (RULE-08). Bug gốc phát sinh ở **PRODUCTION**. | **0/48 TC chạy `prd`** (`envAuto`: local runs=11 · staging runs=9 · **prd runs=0**) | **[MAJOR]** — tiến bộ rõ so với round 1 (trước 100% local, nay staging 48/48), nhưng production vẫn trắng. Riêng NEW-20 khai `env_scope=prd` mà `last_exec.env=staging` ⇒ **khai env ≠ đã chạy env**. |

| **Q8** | `DEPLOY-ASSET-001` | Cao | "**BẮT BUỘC** khi release có sửa file JS/CSS/font/icon" — `chat-v2.js` bị sửa. Dev lại khẳng định **không đổi số hiệu phiên bản tài nguyên tĩnh**. | **0 TC** | **[MAJOR]** — xem G11. Không TC nào kiểm người dùng đang mở sẵn trang (JS cũ trong bộ nhớ đệm) có nhận được cờ chặn mới hay không. Đây là quan điểm Cao, trigger khớp tuyệt đối, **chưa ai duyệt**. |

> **RULE-01 (gộp 1 dòng, chi tiết ở §4 #10):** 9 quan điểm ưu tiên Cao thiếu loại case, đáng chú ý nhất: `OUT-TRUTH-001` + `CONC-001` + `STATE-DEP-001` (chỉ có `Abnormal`, **0 Normal / 0 Boundary**) và `FUNC-001` (chỉ có `Normal`, 0 Abnormal / 0 Boundary).

---

## 3. TC trùng lặp nội dung

**Đã rà toàn bộ 48 TC theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`). Phát hiện 3 nhóm.**

> Nguyên nhân hệ thống: job `695` (2026-08-26) và job `893` (2026-09-09) sinh TC **độc lập**, không đối chiếu bộ cũ ⇒ job 893 viết lại một số TC của job 695.

| Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| Kiểm payload UI luôn kèm khóa `value` | **NEW-41** (#16921, job 893) | **NEW-14** (#13636, job 695) — đề nghị **XÓA** | `DUP-SUBSET` | Trùng **cả 4**: `TOOL-VAL2-001` × `Abnormal` × `ui` × cùng tiền đề, và **tiêu đề giống hệt từng chữ**. NEW-41 là tập cha thực sự: giữ nguyên 2 quan sát của NEW-14 (chọn `オプションA`, chọn `-`) và **thêm lần 3 là lưu hàng loạt**. Cả hai `pass`. | `[MINOR]` |
| Thêm mục kiểu chọn vào danh sách hiển thị qua modal | **NEW-45** (#16925, job 893) | **NEW-16** (#13642, job 695) — đề nghị **XÓA** | `DUP-SUBSET` | `Normal` × `ui` × cùng thao tác (kéo `TC38944_選択2` từ nhóm ẩn sang hiện rồi 「内容を保存」) × tiền đề tương đương, **tiêu đề giống hệt**. NEW-45 mạnh hơn: thêm vế "**chỉ có đúng một dòng cấu hình hiển thị cho mục này**". Mã quan điểm khác nhau (`REG-SHARED-001` vs `FUNC-001`) nhưng nội dung test là một. Cả hai `pass`. | `[MINOR]` |
| V2 nhận payload thiếu khóa `value` | **NEW-50** (#16930, job 893) | **NEW-17** (#16394, `vinth@mcp`) — **KHÔNG xóa**, phải **SIẾT expected** | `DUP-CONFLICT` | Cùng `api` × `Abnormal` × cùng thao tác (POST lưu danh sách hiển thị với 1 mục bỏ hẳn khóa `value`) × tiền đề tương đương. **Expected mâu thuẫn**: NEW-17 chấp nhận **cả hai** kết cục ("nếu được chấp nhận thì lưu hoàn chỉnh; nếu bị từ chối thì có thông báo rõ"), còn NEW-50 chốt **phải TỪ CHỐI SẠCH** kèm đúng nguyên văn 「保存に失敗しました。入力内容をご確認ください。」 và danh sách cấu hình không đổi một chút nào. | `[MAJOR]` |

**Gate trước khi đề nghị xóa — đã chạy lại BƯỚC 2 + 3 trên tập giả định đã xóa:**
- Xóa **NEW-14**: `REQ-010` còn NEW-42/48/34 `pass`; `TOOL-VAL2-001` còn NEW-41 `pass`. **An toàn** — nhưng phải **gắn thêm `REQ-010` vào NEW-41** trước khi xóa, vì NEW-41 hiện chỉ mang `REQ-015`+`REQ-001`.
- Xóa **NEW-16**: `REQ-008` còn NEW-44/17/19/32 `pass`; `REG-SHARED-001` còn 5 TC, toàn `pass`. **An toàn.**
- **NEW-17**: không xóa. `REQ-011` (bản 2026-09-09) đã chốt "từ chối sạch, có thông báo tiếng Nhật" ⇒ expected permissive của NEW-17 **đã lỗi thời** và sẽ vẫn `pass` dù hành vi có tụt về "chấp nhận" — tức nó **che mất** một hồi quy. Siết expected theo REQ-011; giữ `tech_note` mapping bug `#694`/`#697`/`#40213` (hoặc chuyển mapping đó sang NEW-50 rồi mới xóa NEW-17).

**Ghi chú không phải trùng:** NEW-24 và NEW-53 cùng bị chặn bởi một tiền đề "bộ đếm lệch", nhưng khác trigger (thiếu `value` ở 1 mục vs xóa hàng loạt ở 「回答一覧」) ⇒ **giữ cả hai** (xem Q5).

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[MAJOR]` *(hạ từ BLOCKER)* | Bug tab Studio | Đã xử lý 2/3: **`#991` → Redmine #40805** và **`#992` → Redmine #40808** (đều đã `logged`). **Còn đúng 1 bug `open` KHÔNG có ticket Redmine: `#988`** (Medium, tc 16530 = NEW-24, "Lưu mục 友だち情報 kiểu 選択肢 từ khung chat 1:1 không khớp mong đợi") — và NEW-24 đang `skip` nên bug này không có TC nào đang kiểm chứng nó. | Raise ticket Redmine cho `#988` hoặc đóng nếu đã hết hiệu lực, trước khi chuyển trạng thái ticket. |
| 2 | `[BLOCKER]` | NEW-53 · NEW-54 · NEW-57 | **3 bản chấm Đạt tay KHÔNG có bằng chứng, ghi đè lên kết luận `skip` đã có số liệu của lần chạy AI.** Cả 3 đều `source=manual`, `at` = 2026-09-10 06:19→06:40, **`actual` TRỐNG**, `by = null`, không `reason_code`, không evidence. Trong khi lần chạy AI trước đó **từ chối chấm pass** và ghi rõ số đo: NEW-54/57 — màn 「友だち詳細」 không mở được bằng automation (đo: 595 ký tự nội dung, 0 thẻ select, 20 ô nhập, không thấy mục); NEW-53 — mọi đường ghi của app đều giữ bất biến "bộ đếm ≥ số người có giá trị". **Vi phạm RULE-02** ("không chấp nhận đã xem, OK"). 3 TC này đang là chỗ duy nhất phủ `REQ-013` biên sàn và `REQ-014` tầng UI. | Yêu cầu người chạy đính **evidence** (ảnh màn hình từng bước + giá trị sau F5 + số 「回答人数」 trước/sau) và ghi `actual` đo được; đặc biệt với NEW-53 phải nói rõ **đã dựng tiền đề lệch bằng cách nào**. Chưa có thì không tính vào coverage. |
| 3 | `[BLOCKER]` | `REQ-014` | Requirement `risk = High` **không có một TC nào Đạt** (2 fail + 2 skip) — xem G1 / Q2. Đây là requirement mới thêm 2026-09-09 cho `#40689`. | Không được kết luận "fix done" cho `#40689` khi chưa có TC nào Đạt. |
| 4 | `[MAJOR]` | Toàn bộ 47 TC | **Không có một run xanh toàn bộ nào.** Run tự động gần nhất (1183, staging, 2026-09-10) = `fail`; 3 kết quả Đạt mới nhất là **chấm tay rời rạc**, không thuộc một lần chạy chung. Cộng dồn: tự động `pass 203 · fail 3 · skip 54 · error 8`; tay `pass 6 · fail 1 · skip 6`. **8/47 TC (17%) vẫn không có kết luận** (6 skip + 2 fail), cộng 3 TC Đạt-không-bằng-chứng ⇒ **11/47 chưa nghiệm thu được**. | Gỡ chặn nhóm skip (xem §5) rồi chạy lại **toàn bộ 48 TC trong 1 run xanh** mới kết luận. |
| 5 | `[MAJOR]` | NEW-20 (#16526) | TC duy nhất cho chiều **lịch nhắc/job** (G3): `skip` từ **2026-09-07**, `reason_code = null`, `by = null` — **không ghi lý do**, và **không được đưa vào 3 run gần nhất** (980 / 1170 / 1183). TC im lặng trôi qua 3 vòng. | Bắt buộc ghi `reason_code` cho mọi skip; đưa NEW-20 vào run kế tiếp. |
| 6 | `[MAJOR]` | NEW-18 · NEW-24 · NEW-23 (#16524 / #16530 / #16529) | 3 TC có **manual `pass` ngày 2026-09-07→08** nhưng `last_exec` là auto `skip` ngày 2026-09-10. Các bản manual pass đó chạy **TRƯỚC** 2 commit ngày 09/09 (`037725a61b`, `21116c9b1b`) ⇒ **bằng chứng trên code đã cũ**, không dùng được để kết luận cho bản đem test hiện tại. | Không tính manual pass cũ vào coverage của round này. |
| 7 | `[MAJOR]` | `03-dev-impact.md` trong folder | File 03 vẫn là bản **round 1** (2026-09-07, chỉ có 1 điểm sửa `isset()` ở dòng 4471, mục 4.2 ghi "Không có"). Thực tế phạm vi nay là **8 điểm / 5 file / 9 ticket** (`#38944` `#40538` `#40540` `#40555` `#40556` `#40558` `#40557` `#40661` `#40689`). Checkbox "Tester verify auto-fill chính xác" vẫn **chưa tick**. | Chạy lại `/new-task` hoặc cập nhật tay file 03 theo journal `#135367` **+ đính chính `#135413`**, rồi tick verify. |
| 8 | `[MAJOR]` | `[AP-3]` NEW-52 / REQ-013 | Regression cho `#40661` chỉ có **1 TC Normal** trên tiền đề sạch (NEW-52); vế edge state (bộ đếm lệch sẵn, NEW-53) `skip`. Đúng mẫu happy-path-only. | Xem `TC-DATACOUNT001-02` ở §5. |
| 9 | `[MAJOR]` | `[AP-1]` `REQ-012` / nhánh `catch` | `#40556` là **generic catch** (bọc cả hàm, trả `msg` tiếng Nhật). Số trigger thật sự kiểm được: **1** (NEW-50, thiếu khóa `value` ở V2). Trigger còn lại (`blockUserGroup`) **không kích hoạt được từ ngoài**, và **không có trigger lỗi chưa biết** nào. *(Trigger `deleteMessageGroup` đã bỏ khỏi phạm vi — tính năng không còn trên tool.)* | Cần đường tiêm lỗi hoặc nghiệm thu bằng code-review — §6 #2. |
| 10 | `[MINOR]` | NEW-55 · NEW-56 | Gán `viewpoint = STATE-DEP-001` nhưng nội dung là **validate định dạng ngày**, không liên quan hành động đã lên lịch. Nhãn sai làm `STATE-DEP-001` trông như có 3 TC (xem Q1). | Đổi sang `FUNC-003` (định dạng field) hoặc `DATA-001`. |
| 11 | `[MINOR]` | RULE-01 | 9 quan điểm Cao thiếu loại case: `OUT-TRUTH-001` (4 Abnormal, 0 Normal, 0 Boundary) · `CONC-001` (4 Abnormal) · `STATE-DEP-001` (3 Abnormal) · `FUNC-001` (4 Normal) · `FUNC-004` (3 Boundary) · `DATA-001` (1 Boundary) · `SEC-ISO-001` (1 Abnormal) · `DATA-REF-001` (1 Abnormal) · `COMPAT-LEGACY-001` (1 Abnormal). Không TC nào ghi lý do thiếu. | Bổ sung hoặc ghi lý do ở `Ghi chú`. Ưu tiên `OUT-TRUTH-001` (thiếu vế Normal: nhánh lưu **thành công** có hiện đúng thông báo thành công không). |
| 12 | `[MINOR]` | 6 mã quan điểm | **Không có trong `framework/checklist-lme.md`**: `TOOL-ERRHYG-001`(4) · `STATE-MATRIX-001`(3) · `TOOL-VAL2-001`(2) · `DATA-HIST-001`(1) · `TOOL-NEGCTRL-001`(1) · `TOOL-KNOW-002`(1) — 12 TC. Tăng 1 mã so với round 1 (`TOOL-NEGCTRL-001` mới). | Map về mã chuẩn: `TOOL-ERRHYG-001`→`OUT-TRUTH-001` · `STATE-MATRIX-001`→`STATE-001` · `DATA-HIST-001`→`DATA-AUDIT-001` · `TOOL-NEGCTRL-001`→`DATA-001`. |
| 13 | `[MINOR]` | NEW-33 · NEW-43 / `REQ-009` | Hành vi mới: lưu mục **đã bị xóa** ở tab khác → **API trả THÀNH CÔNG** dù không ghi được gì (Dev liệt kê đây là "thay đổi hành vi QA sẽ nhìn thấy" #5). 2 TC kiểm "không lỗi, không mồ côi" nhưng **0 TC kiểm người dùng có được biết mục đã biến mất** — đúng dạng *false success* mà `OUT-TRUTH-001` nhắm tới. | Bổ sung vế quan sát UI sau khi lưu (mục mất khỏi cột phải / có thông báo) vào NEW-43. |
| 14 | `[MAJOR]` | NEW-10 · NEW-13 · NEW-8 | **Tiền đề thiếu `稼働設定` ⇒ expected không xác định.** Spec `friend-information` (:146, :234, :394) định nghĩa `稼働設定` = `action_mode` trên `friend_information_setting`: `一度のみ` (=1) hoặc `何度でも稼働` (=2). NEW-10 khẳng định "lưu lại đúng giá trị đang có → **không bắn lại hành động**" — điều này **chỉ đúng khi `稼働設定 = 一度のみ`**. Nếu bot test đang ở `何度でも稼働` thì hành vi đúng là **CÓ** bắn lại, và NEW-10 sẽ báo fail oan (hoặc pass oan nếu action bị chặn sai). Cả 3 TC đều **không khai `稼働設定`** trong tiền đề. Dev lại nêu đích danh ở P6: "action vẫn chạy đúng theo chế độ `稼働設定` như #40492 đã sửa". | Khai rõ `稼働設定` trong tiền đề của NEW-8/10/13, và chạy lại kho `TC-FRI-68` + `TC-FRI-69` (xem §5) để phủ cả 2 chế độ. |
| 15 | `[MINOR]` | NEW-33 | Phủ được vế "không sinh dòng cấu hình mồ côi" + "hộp thoại vẫn mở được" + "có ghi log", nhưng **không assert `回答人数`**. Mục 4.2 của Dev nói `total_user_has_value` "**chỉ cộng/trừ khi bản ghi cài đặt còn tồn tại**" — nửa sau của khẳng định đó (setting đã xóa ⇒ KHÔNG cộng/trừ) hiện không có TC nào kiểm. | Bổ sung 1 vế vào expected của NEW-33: sau request, `回答人数` của mọi mục **giữ nguyên**. |
| 16 | `[NIT]` | `coverage` Studio | 63 spec ref, **`covered = 0`, `partial = 63`** — không ref nào được phủ đủ theo chính thước đo của Studio. | Dùng làm tham chiếu khi chốt round, không dùng để chặn. |

**Anti-pattern đã rà**: `[AP-1]` dính (#8) · `[AP-2]` **không dính** (file 01 có log + error message + stack trace cụ thể) · `[AP-3]` dính (#7) · `[AP-4]` **không còn dính** (Dev đã cung cấp 4 commit hash + nêu rõ đã `php -l` / `node --check`; vẫn chưa có link PR nhưng đã trace được về commit) · `[AP-5]` **không dính** · `[AP-6]` **không còn dính** (note mới kê đủ 8 điểm + danh sách hàm/đường HTTP/màn hình — cải thiện lớn so với round 1).

---

## 5. TCs đề xuất bổ sung (11)

**Đã đối chiếu trước khi viết (BƯỚC 5a/5b):**

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | [kho-tcs/fa001-chat11-11チャット.md](../../kho-tcs/fa001-chat11-11チャット.md) §35 "Rightbar — 友だち情報" (8 TC) + §39 "Group chat" (15 TC, `TC-CHT-377`→`391`) · [kho-tcs/fa015-quanlythongtinbanbe-友だち情報管理.md](../../kho-tcs/fa015-quanlythongtinbanbe-友だち情報管理.md) §19 "Bộ đếm 回答人数" (30 TC) · §20 "Ghi giá trị từ màn admin" (17 TC) · §23 "Job action ngày tháng" (22 TC) |
| Vùng regression phát hiện từ kho | `TC-FRI-187` (bộ đếm không âm) · `TC-FRI-192`/`TC-FRI-197` (không vượt ranh giới bot) · `TC-FRI-199`/`TC-FRI-200` (điểm = 0 hợp lệ) · `TC-FRI-214` (xóa giá trị 年月日 → dọn lịch) · `TC-CHT-352`/`353`/`354` (Rightbar 友だち情報) |
| **Vùng kho CHƯA CÓ** (ghi để `/collect-tcs` bổ sung sau) | (a) **Xóa tin nhắn nhóm / chặn thành viên nhóm — nhánh LỖI**: kho §39 có 15 TC group chat nhưng **không TC nào** cho 2 thao tác này. (b) **Validate định dạng ngày ở màn 「友だち詳細」**: kho FA-015 chỉ có `TC-FRI-89` (offset ở màn *setting*), **không có TC cho giá trị của bạn bè**. |
| Conflict expected vs kho | **Không** với kho. **CÓ 1 conflict vs spec** → §6 #5 (`REQ-014` nói "nhận mọi định dạng đọc được", spec `detail-friend:525` nói `birthday` chỉ nhận regex `Y-m-d`/`Y/m/d`). |
| GAP dùng lại TC kho (không viết mới) | **Không** — các GAP round này là *TC đã có nhưng bị chặn*, không phải thiếu TC. |
| **Kho ĐÃ CÓ TC — dùng lại, KHÔNG viết mới** | (1) **`稼働設定`** (Dev nêu ở P6 / N2): kho FA-015 đã có **`TC-FRI-68`** (`稼働設定 = 一度のみ` → gán lại cùng lựa chọn lần 2 **KHÔNG** chạy action lần nữa) và **`TC-FRI-69`** (đổi sang `何度でも稼働` → bạn đã từng chạy **được chạy lại**) ⇒ chỉ cần **chạy lại 2 TC này trên bản vá**, cùng với `TC-FRI-67`. (2) **Upload/thay/xóa file mục `画像`/`PDF` ở cột phải chat 1:1** (Dev nêu ở P1 "có nhập"): kho đã có **`TC-CHT-353`** (FA-001 §35) + **`TC-FRI-112`**/**`TC-FRI-113`** (gán giá trị ảnh / PDF) + **`TC-FRI-114`** (upload sai định dạng) ⇒ chạy lại, không viết mới. |
| Xác nhận chống trùng | Đã đối chiếu **48 TC** ở BƯỚC 0 + 2 file kho — **không TC đề xuất nào trùng**. 2 gap đã có TC kho (`稼働設定`, upload ảnh/PDF) được chuyển sang dùng lại thay vì viết mới. |

**Trạng thái push lên Studio** (cập nhật 2026-09-10, `client_ref` = `TC No.` nên chạy lại `testcase_create` KHÔNG tạo trùng):

| TC | Đã push? | Studio |
|---|---|---|
| `TC-DATA001-02` | ✅ đã push | `#17033` · `NEW-62` |
| `TC-REGSHARED001-07` | ✅ đã push | `#17034` · `NEW-63` |
| `TC-DATADB001-01` | ✅ đã push | `#17035` · `NEW-64` |
| `TC-DATADB001-02` | ✅ đã push | `#17036` · `NEW-65` |
| `TC-DATADB001-03` | ✅ đã push | `#17037` · `NEW-66` |
| `TC-OUTTRUTH001-02` · `TC-DATACOUNT001-02` · `TC-DATAREF001-01` · `TC-CONC001-01` | ⏸ Leader chưa duyệt push | — |
| `TC-ENV003-01` · `TC-DEPLOYASSET001-01` | ⏸ Leader hoãn — `env_scope = prd` | — |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-OUTTRUTH001-02 | UI | OUT-TRUTH-001 | Rightbar — 友だち情報 | Abnormal | manual | Tất cả | Hộp thoại của luồng lưu hàng loạt hiện đúng câu tiếng Nhật khi máy chủ trả thất bại | - Đăng nhập admin, đang ở Chat 1:1 bản mới, tab 「友だち情報」 mở được<br>- Bot có ≥ 2 mục trong danh sách hiển thị<br>- Đã bật công cụ phát triển của trình duyệt và biết cách chặn/sửa yêu cầu trước khi gửi | 1. Mở hộp thoại 「表示内容を変更」 từ biểu tượng ba chấm<br>2. Bật chặn yêu cầu trong công cụ phát triển<br>3. Bấm 「内容を保存」, chặn yêu cầu tới luồng lưu danh sách hiển thị<br>4. Sửa dữ liệu gửi: **bỏ hẳn khóa giá trị** của một mục, rồi cho yêu cầu đi tiếp<br>5. **Đọc nguyên văn nội dung hộp thoại hiện lên, chụp màn hình**<br>6. Đóng hộp thoại, tải lại trang và đối chiếu danh sách mục hiển thị so với trước bước 3 | Dữ liệu gửi của luồng lưu hàng loạt, một mục bị bỏ hẳn khóa giá trị | - Hộp thoại hiện **đúng nguyên văn** 「保存に失敗しました。入力内容をご確認ください。」<br>- **KHÔNG** hiện chữ `undefined`, không hiện hộp thoại trống<br>- Danh sách mục hiển thị **giữ nguyên y như trước** thao tác lưu | | Lấp `Q3` (vế UI) · Thay cho NEW-18 — TC đó skip vì mọi đường làm luồng lưu thất bại **từ thao tác bình thường** đã bị guard mới đóng; sửa dữ liệu gửi bằng công cụ phát triển là đường **còn mở** và vẫn là thao tác của người thật trên trình duyệt · NEW-50 đã chứng minh máy chủ trả đúng câu này ⇒ TC này chỉ kiểm vế hiển thị · Đánh giá spec: Spec ghi rõ — `REQ-012` · Evidence: ảnh hộp thoại + ảnh danh sách mục trước/sau |
| TC-DATACOUNT001-02 | Data | DATA-COUNT-001 | Bộ đếm 回答人数 | Boundary | manual | Tất cả | Bộ đếm lệch sẵn trong dữ liệu — xóa hàng loạt dừng ở 0, không xuống âm | - Bot thử nghiệm, mục 「TC38944_選択」 có đúng 2 bạn đang giữ giá trị<br>- **Tiền đề lệch được seed trực tiếp ở tầng dữ liệu** (không dựng được qua giao diện — đã đo ở NEW-24 / NEW-53): đặt 「回答人数」 của mục về `1` trong khi vẫn còn 2 bạn có giá trị<br>- Đã ghi lại số bạn có giá trị và số bộ đếm trước khi thao tác | 1. Xác nhận trạng thái lệch: 「回答人数」 = 1 nhưng đếm được 2 bạn còn giá trị<br>2. Mở màn 「回答一覧」 của mục, chọn **cả 2 bạn**, bấm xóa hàng loạt<br>3. Đọc mã trạng thái và nội dung phản hồi<br>4. Đọc lại 「回答人数」 của mục trên màn 「友だち情報管理」<br>5. Kiểm tra dữ liệu giá trị của mục cho cả 2 bạn<br>6. Mở lại màn 「回答一覧」 và đối chiếu số bạn hiển thị<br>7. Đặt lại giá trị cho 1 bạn qua khung chat 1:1, đọc lại bộ đếm | Xóa hàng loạt 2 bạn trong khi bộ đếm chỉ là 1 | - Phản hồi thành công, không lỗi trong nhật ký<br>- Giá trị của cả 2 bạn bị xóa<br>- 「回答人数」 **dừng ở `0`, KHÔNG xuống số âm**<br>- Màn 「回答一覧」 không còn bạn nào, nhất quán với bộ đếm<br>- Sau khi đặt lại giá trị cho 1 bạn: bộ đếm lên đúng `1` (không bị lệch tiếp) | | Lấp `Q5` · `G7` · **dẫn từ `TC-FRI-187`** (kho FA-015 §19) · ⚠️ **Cập nhật**: NEW-53 vừa được chấm Đạt tay ngày 2026-09-10 06:32 **nhưng `actual` trống** nên không biết tiền đề lệch đã được dựng bằng cách nào (§4 #2). Giữ TC này để có **bằng chứng đo được từng bậc**; nếu người chạy NEW-53 bổ sung được `actual` + cách dựng tiền đề thì **gộp vào NEW-53 và bỏ TC này** · Đánh giá spec: Spec không ghi — đã hỏi Leader (§6 #6) · Evidence: ảnh bộ đếm trước/sau + kết quả truy vấn dữ liệu giá trị |
| TC-DATAREF001-01 | UI | DATA-REF-001 | Rightbar — 友だち情報 | Abnormal | manual | Tất cả | Bot đã có sẵn dòng cấu hình hiển thị TRÙNG từ trước — màn chat và các thao tác lưu vẫn đúng | - Bot thử nghiệm có **2 dòng cấu hình hiển thị cho cùng một mục** 「TC38944_選択」 (cùng bot, cùng loại, cùng mã mục) — seed ở tầng dữ liệu, mô phỏng dữ liệu bẩn Dev tuyên bố **vẫn còn trong DB**<br>- Bạn bè 「TC38944_FRIEND_A」 đang có giá trị 「オプションA」 cho mục đó<br>- Đã ghi lại toàn bộ danh sách dòng cấu hình hiển thị của bot | 1. Mở Chat 1:1, chọn bot, mở hội thoại của bạn bè; quan sát cột phải tab 「友だち情報」 — **đếm số lần mục 「TC38944_選択」 xuất hiện**<br>2. Mở hộp thoại 「表示内容を変更」, đếm số lần mục đó xuất hiện trong nhóm đang hiển thị<br>3. Đổi giá trị của mục bằng lưu nhanh ở cột phải, tải lại trang và đọc lại giá trị<br>4. Đổi thứ tự các mục trong hộp thoại rồi 「内容を保存」; tải lại trang và đối chiếu thứ tự<br>5. Bỏ mục 「TC38944_選択」 khỏi danh sách hiển thị rồi lưu; đếm lại số dòng cấu hình còn lại của mục đó<br>6. Mở nhật ký ứng dụng tìm lỗi trong khoảng vừa thao tác | Bot có sẵn 2 dòng cấu hình trùng cho 1 mục | - Ghi nhận rõ mục hiện **1 lần hay 2 lần** ở cột phải và trong hộp thoại<br>- Lưu nhanh: giá trị đổi đúng và **chỉ có 1 bản ghi giá trị** cho cặp bạn bè × mục<br>- Đổi thứ tự: lưu được, thứ tự sau khi tải lại đúng như vừa đặt, không sinh thêm dòng cấu hình<br>- Bỏ mục khỏi danh sách: **cả hai** dòng cấu hình trùng đều bị gỡ, mục không còn ở cột phải<br>- Nhật ký không có lỗi<br>- Nếu mục hiện 2 lần hoặc thao tác nào lỗi ⇒ raise ticket riêng về dữ liệu bẩn tồn đọng | | Lấp `G4` · Dev ghi rõ ở 4.2: "dòng cấu hình TRÙNG đã sinh ra từ trước **vẫn nằm nguyên trong DB** — đợt này chỉ ngăn sinh thêm, không dọn dữ liệu cũ" ⇒ phải có TC cho chính trạng thái đó · Đánh giá spec: Spec không ghi — đã hỏi Leader (§6 #4) · Evidence: ảnh cột phải + ảnh hộp thoại + kết quả truy vấn bảng cấu hình trước/sau |
| TC-CONC001-01 | API | CONC-001 | Rightbar — 友だち情報 | Abnormal | auto | Tất cả | Lặp nhiều vòng hai yêu cầu lưu song song — đo tần suất sinh dòng cấu hình trùng ở điểm Dev tự nhận chưa bịt | - Đăng nhập admin, lấy phiên hợp lệ cho bot thử nghiệm<br>- Bot có mục 「TC38944_選択3」 kiểu 「選択肢」 **chưa** nằm trong danh sách hiển thị<br>- Đã ghi lại số dòng cấu hình hiển thị của mục đó (mong đợi ban đầu = 0)<br>- Có sẵn cách dọn dữ liệu giữa các vòng | 1. Xóa hết dòng cấu hình hiển thị của mục 「TC38944_選択3」 để về trạng thái 0 dòng<br>2. Gửi **2 yêu cầu lưu song song** (cùng lúc, không chờ nhau) cho mục đó với mã dòng hiển thị để rỗng<br>3. Đếm số dòng cấu hình hiển thị của mục sau 2 yêu cầu<br>4. Dọn về 0 dòng<br>5. **Lặp bước 2→4 đủ 50 vòng**, ghi lại số vòng sinh ra nhiều hơn 1 dòng<br>6. Mở nhật ký ứng dụng, đếm lỗi phát sinh trong toàn bộ quá trình | 50 vòng × 2 yêu cầu lưu song song cho cùng một mục | - Ghi nhận **số liệu cụ thể**: `<n>/50` vòng sinh ra nhiều hơn 1 dòng cấu hình<br>- Mong đợi để Đạt: `0/50` vòng bị nhân đôi và nhật ký không có lỗi<br>- Nếu `n > 0` ⇒ raise ticket và **đính số liệu** để Leader quyết có bổ sung ràng buộc duy nhất ở tầng dữ liệu hay không | | Lấp `G8` · `REQ-016` — NEW-61 mới thăm dò **1 lần** và Đạt; race không tái hiện 1 lần không chứng minh đã an toàn · Dev chốt KHÔNG thêm ràng buộc duy nhất và KHÔNG dùng giao dịch ⇒ TC này là nguồn số liệu duy nhất cho quyết định đó · Đánh giá spec: Spec ghi rõ — `REQ-016` là "ghi nhận thực nghiệm", không phải căn cứ chấm bản vá hỏng · Evidence: bảng kết quả 50 vòng + trích nhật ký |
| TC-ENV003-01 | UI | ENV-003 | Phân quyền & môi trường | Normal | manual | product | Smoke bộ TC lõi trên production sau khi deploy | - Bản vá đã deploy lên **production** (`step.lme.jp`)<br>- Dùng **bot thử nghiệm nội bộ**, tuyệt đối không đụng dữ liệu khách<br>- Đã dựng mục 「選択肢」, 「年月日」 (có action nhắc trước 1 ngày) và bạn bè thử nghiệm trên bot đó<br>- Có điện thoại đăng nhập LINE đã kết bạn với bot thử nghiệm | 1. Chat 1:1: chọn một lựa chọn cho mục 「選択肢」, tải lại trang và đối chiếu giá trị<br>2. Chọn dòng trống 「-」 để xóa giá trị; đọc lại 「回答人数」<br>3. Mở hộp thoại 「表示内容を変更」, thêm 1 mục rồi 「内容を保存」; bấm lưu **2 lần liên tiếp** và đếm số lần mục xuất hiện<br>4. Nhập ngày sai định dạng cho mục 「年月日」 ở màn 「友だち詳細」; đọc thông báo<br>5. Đặt giá trị ngày hợp lệ cho mục có action nhắc, rồi **xóa giá trị đó**; kiểm tra lịch nhắc đang chờ của bạn<br>6. Chờ qua mốc nhắc cũ, kiểm tra **LINE app trên điện thoại thật**<br>7. Dọn sạch dữ liệu thử nghiệm đã tạo | Bộ dữ liệu tối thiểu của 3 kiểu mục: 選択肢 · 年月日 có action · một mục bất kỳ để test hộp thoại | - Bước 1-2: giá trị lưu/xóa đúng, 「回答人数」 tăng/giảm đúng 1<br>- Bước 3: mục **chỉ xuất hiện 1 lần**, không nhân đôi<br>- Bước 4: hiện 「日付のフォーマットが無効です。」, không ghi giá trị rác<br>- Bước 5: lịch nhắc đang chờ **bị dọn sạch**<br>- Bước 6: LINE app **KHÔNG** nhận tin nhắn nhắc ở mốc cũ<br>- Không phát sinh lỗi trong nhật ký production | | Lấp `Q7` · **RULE-08** — 0/48 TC chạy `prd`, bug gốc phát sinh ở production; bước 5-6 chạm **job nền** nên bắt buộc `product` · **RULE-06** — bước 6 đi tới output cuối trên thiết bị thật · Đã đề xuất ở round 1 nhưng Leader **loại khỏi đợt push**; nêu lại vì `ENV-003` vẫn là quan điểm Cao chưa cover · Evidence: bảng kết quả từng bước + ảnh màn hình LINE app |
| TC-DATA001-02 | UI | DATA-001 | Rightbar — 友だち情報 | Normal | manual | Tất cả | Lưu và xóa giá trị mục thông tin bạn bè — chỉ mục tìm kiếm bạn bè được cập nhật theo | - Đăng nhập admin, đã chọn bot thử nghiệm<br>- Bot có mục 「TC38944_記述」 kiểu 「記述」 nằm trong danh sách hiển thị của khung chat<br>- Bạn bè 「TC38944_FRIEND_A」 **chưa** có giá trị ở mục đó<br>- Màn 「友だちリスト」 có bộ lọc / tìm kiếm theo giá trị thông tin bạn bè<br>- Đã ghi lại mốc dòng của hàng đợi đồng bộ chỉ mục tìm kiếm | 1. Ở màn 「友だちリスト」, tìm bạn bè theo giá trị `ES_SYNC_MARKER` — xác nhận **không ra kết quả nào**<br>2. Mở Chat 1:1, tab 「友だち情報」, nhập giá trị `ES_SYNC_MARKER` cho mục 「TC38944_記述」 và lưu<br>3. Chờ hàng đợi đồng bộ chạy xong (hoặc theo chu kỳ đã biết), quay lại 「友だちリスト」 và **tìm lại** bằng `ES_SYNC_MARKER`<br>4. Kiểm tra hàng đợi đồng bộ: có bản ghi mới cho bạn bè này sau mốc đã ghi ở tiền điều kiện<br>5. Quay lại Chat 1:1, **xóa trắng** giá trị của mục đó và lưu<br>6. Chờ đồng bộ, tìm lại bằng `ES_SYNC_MARKER` ở 「友だちリスト」<br>7. Lặp bước 2→6 nhưng lần này lưu cho **một mục vừa bị xóa ở màn 「友だち情報管理」** (trường hợp guard mới bỏ qua mục) và quan sát hàng đợi đồng bộ | Giá trị `ES_SYNC_MARKER` cho mục 「TC38944_記述」; sau đó xóa trắng | - Bước 1: tìm không ra bạn nào<br>- Bước 3: **tìm ra đúng** 「TC38944_FRIEND_A」<br>- Bước 4: hàng đợi đồng bộ **có** bản ghi mới cho bạn bè này<br>- Bước 6: tìm **không còn ra** bạn đó nữa<br>- Bước 7: ghi nhận rõ hàng đợi đồng bộ **có hay không** phát sinh bản ghi khi request bị guard bỏ qua — nếu dữ liệu thật sự không đổi thì không phát sinh là đúng; nếu dữ liệu đổi mà hàng đợi trống ⇒ **raise ticket: chỉ mục tìm kiếm bị cũ trong im lặng** | | Lấp `G9` · regression · **dẫn từ `TC-FRL-143`/`TC-FRL-144`/`TC-FRL-145`** (kho FA-013 §15 "Đồng bộ Elasticsearch" — kho chỉ có cho thao tác ở màn friendlist, **chưa có** cho đường ghi giá trị friend info) · Lý do: Dev ghi rõ lời gọi đồng bộ **nằm SAU các bước kiểm tra mới** ⇒ guard return sớm sẽ làm chỉ mục cũ mà không báo lỗi · Đánh giá spec: Spec ghi rõ — `chat-11 feature-spec.md:136, :151` (`INSERT sync_elasticsearch type=2`) · Evidence: ảnh kết quả tìm kiếm trước/sau + kết quả truy vấn hàng đợi đồng bộ |
| TC-REGSHARED001-07 | UI | REG-SHARED-001 | Group chat | Normal | manual | Tất cả | Chat nhóm — nhánh THÀNH CÔNG của chặn và bỏ chặn thành viên vẫn ghi dữ liệu đúng | - Đăng nhập admin, đã chọn bot thử nghiệm<br>- **Dùng một nhóm LINE chỉ gồm thành viên nội bộ của đội test** — cờ chặn ảnh hưởng trực tiếp việc nhận tin của người thật<br>- Nhóm có ≥ 1 thành viên đang **chưa** bị chặn<br>- Thành viên đó có điện thoại đăng nhập LINE để gửi tin kiểm chứng<br>- Đã ghi lại trạng thái chặn của thành viên và bộ đếm tin chưa xác nhận của bot | 1. Mở hội thoại nhóm ở Chat 1:1 và mở danh sách thành viên<br>2. **Chặn** thành viên thử nghiệm; đọc thông báo/kết quả hiện ra và trạng thái chặn hiển thị<br>3. Tải lại trang, xác nhận trạng thái chặn vẫn là đã chặn<br>4. Từ điện thoại của thành viên đó, **gửi tin vào nhóm** và quan sát phía admin<br>5. **Bỏ chặn** thành viên đó; đọc lại trạng thái chặn và tải lại trang để đối chiếu<br>6. Gửi lại tin từ thành viên đó và quan sát phía admin<br>7. Đọc lại bộ đếm tin chưa xác nhận của bot<br>8. Mở nhật ký ứng dụng, tìm lỗi trong khoảng vừa thao tác | 1 thành viên nhóm có thật: chặn rồi bỏ chặn | - Bước 2-3: trạng thái chặn chuyển sang **đã chặn** và giữ nguyên sau khi tải lại trang; **không** hiện chữ `undefined`<br>- Bước 4: tin thành viên gửi sau khi bị chặn **KHÔNG** vào hội thoại<br>- Bước 5-6: bỏ chặn thành công, tin gửi sau đó **vào lại** hội thoại bình thường<br>- Bước 7: bộ đếm tin chưa xác nhận cập nhật đúng, không bị âm<br>- Nhật ký không có lỗi | | Lấp `G10` · regression — Dev khẳng định ở 4.2 "logic ghi không đổi", đây là **đối chứng âm** cho khẳng định đó · NEW-59 chỉ nhắm nhánh LỖI và đang `skip` ⇒ `blockUserGroup` hiện **không có một kết luận nào** · **Kho FA-001 §39 có 15 TC Group chat nhưng KHÔNG có TC cho thao tác này** — ghi vào danh sách bổ sung kho · ⚠️ Đã bỏ vế **xóa tin nhắn nhóm** khỏi TC: Leader xác nhận `deleteMessageGroup` không còn trên tool · RULE-06 — bước 4/6 verify trên LINE app thật · Đánh giá spec: Spec không ghi — đã hỏi Leader · Evidence: ảnh trạng thái chặn trước/sau + ảnh màn hình LINE của thành viên |
| TC-DATADB001-01 | UI | DATA-DB-001 | Rightbar — 友だち情報 | Normal | manual | Tất cả | Xóa trắng giá trị mục thông tin bạn bè kiểu 記述 trên khung chat 1:1 | - Đăng nhập admin, đã chọn bot thử nghiệm<br>- Bot có mục 「TC38944_記述」 kiểu 「記述」 nằm trong danh sách hiển thị của khung chat<br>- Bạn bè 「TC38944_FRIEND_A」 **đang có giá trị** 「テスト記述」 ở mục đó<br>- Đã ghi lại số 「回答人数」 của mục và số dòng lịch sử của bạn ở mục đó | 1. Mở Chat 1:1, chọn bot thử nghiệm, mở hội thoại của bạn 「TC38944_FRIEND_A」<br>2. Ở tab 「友だち情報」, bấm vào ô giá trị mục 「TC38944_記述」, **xóa hết nội dung**, bấm ra ngoài ô để lưu<br>3. Quan sát ô giá trị ngay sau khi lưu<br>4. Tải lại trang bằng F5, mở lại hội thoại và đọc lại giá trị của mục<br>5. Mở màn 「友だち情報管理」, đọc lại số 「回答人数」 của mục<br>6. Mở lịch sử thay đổi thông tin bạn bè của bạn ở mục đó, đọc dòng mới nhất<br>7. Kiểm tra bảng lưu giá trị cho cặp bạn bè × mục này<br>8. Mở nhật ký ứng dụng, tìm lỗi trong khoảng vừa thao tác | Giá trị trước khi xóa: 「テスト記述」. Thao tác: xóa hết nội dung trong ô | - Ô giá trị **trống** ngay sau khi lưu và **vẫn trống sau khi tải lại trang**<br>- Bản ghi giá trị của cặp bạn bè × mục **không còn tồn tại** (không để lại dòng rỗng)<br>- 「回答人数」 **giảm đúng 1**<br>- Lịch sử thêm **đúng 1 dòng** loại 'xóa giá trị', giá trị cũ 「テスト記述」, giá trị mới trống, người thao tác là tài khoản đang đăng nhập<br>- Nhật ký không có lỗi | | Lấp `G12` · **Tách từ TC gộp theo chỉ định Leader (2026-09-10): 1 kiểu info = 1 case**, đúng tiêu chí atomic ở BƯỚC 4a #4 · Kiểu 記述 theo spec `friend-information:417 (BR-02)` **KHÔNG hỗ trợ action** ⇒ TC này thuần tầng dữ liệu, không có vế action (khác 2 TC cùng họ bên dưới) · dẫn từ kho `TC-CHT-352` (FA-001 §35) · RULE-07 verify 3 tầng DB + màn hình + bộ đếm · Biên "chuỗi chỉ gồm khoảng trắng" nếu cần thì tách case riêng, không gộp vào đây · Đánh giá spec: Spec ghi rõ — BR-02 · Evidence: ảnh ô giá trị sau F5 + ảnh 回答人数 + ảnh dòng lịch sử |
| TC-DATADB001-02 | UI | DATA-DB-001 | Rightbar — 友だち情報 | Normal | manual | Tất cả | Xóa trắng giá trị mục thông tin bạn bè kiểu ポイント trên khung chat 1:1 — phân biệt với nhập 0 | - Đăng nhập admin, đã chọn bot thử nghiệm<br>- Bot có mục 「TC38944_ポイント」 kiểu 「ポイント」 nằm trong danh sách hiển thị, **CÓ cấu hình action theo ngưỡng** gửi tin nhắn nội dung 「TC38944_POINT_ACTION」<br>- Bạn bè 「TC38944_FRIEND_A」 **đang có điểm = 50**<br>- Đã ghi lại 「回答人数」 của mục, số dòng lịch sử của bạn, mốc thời gian và lịch sử tin nhắn hiện tại của hội thoại | 1. Mở Chat 1:1 và mở hội thoại của bạn 「TC38944_FRIEND_A」<br>2. Ở tab 「友だち情報」, bấm ô giá trị mục 「TC38944_ポイント」, **xóa hết số**, bấm ra ngoài ô để lưu<br>3. Quan sát ô giá trị ngay sau khi lưu<br>4. Tải lại trang, mở lại hội thoại và đọc lại giá trị<br>5. Đọc lại 「回答人数」 trên màn 「友だち情報管理」<br>6. Đọc dòng lịch sử mới nhất của bạn ở mục đó<br>7. Kiểm tra bảng lưu giá trị cho cặp bạn bè × mục<br>8. Xem lịch sử tin nhắn của hội thoại sau mốc đã ghi; mở nhật ký xem có dòng ghi nhận kích hoạt action cho mục này không<br>9. **Đối chứng**: nhập `0` vào đúng ô đó rồi lưu, tải lại trang, kiểm lại bảng giá trị và 「回答人数」 | Giá trị trước khi xóa: `50`. Thao tác 1: xóa hết số. Thao tác 2 (đối chứng): nhập `0` | - **Sau khi xóa trắng**: ô trống sau khi tải lại trang; bản ghi giá trị **KHÔNG còn tồn tại**; 「回答人数」 giảm đúng 1; lịch sử thêm đúng 1 dòng 'xóa giá trị' với giá trị cũ `50`<br>- Hội thoại **KHÔNG** xuất hiện 「TC38944_POINT_ACTION」 và nhật ký **KHÔNG** ghi nhận kích hoạt action<br>- **Đối chứng nhập `0`**: bản ghi giá trị **TỒN TẠI** với giá trị `0`; 「回答人数」 **tăng lại đúng 1**; lịch sử ghi loại 'ghi mới' ⇒ chứng minh **xóa trắng ≠ nhập 0** | | Lấp `G12` · Tách từ TC gộp theo chỉ định Leader (2026-09-10) · **Đây là kiểu info duy nhất mà "xóa trắng" dễ bị lẫn với "giá trị 0"** (PHP coi chuỗi `"0"` là falsy) ⇒ vế đối chứng nằm trong cùng TC là cần thiết; bổ trợ cho `TC-DATA001-01` (chiều nhập 0) và kho `TC-FRI-199`/`TC-FRI-200` · Vế action: **47 TC hiện có KHÔNG có TC nào cho ポイント + action**; dẫn từ kho `TC-FRI-213` ("Xóa trắng giá trị trường Lựa chọn / **Điểm** CÓ gắn action → KHÔNG kích hoạt action ngoài ý") · Đánh giá spec: **Spec không ghi** — kho `MT-11` ("kiểu ポイント: vượt ngưỡng có kích hoạt action không?") đang ⏳ CHỜ QUYẾT ĐỊNH ⇒ vế action cần Leader chốt trước khi chấm · Evidence: ảnh ô giá trị 2 lượt + ảnh 回答人数 + ảnh lịch sử + ảnh hội thoại |
| TC-DATADB001-03 | UI | DATA-DB-001 | Rightbar — 友だち情報 | Normal | manual | Tất cả | Xóa trắng giá trị mục thông tin bạn bè kiểu 年月日 trên khung chat 1:1 | - Đăng nhập admin, đã chọn bot thử nghiệm<br>- Bot có mục 「TC38944_日付」 kiểu 「年月日」 nằm trong danh sách hiển thị và **KHÔNG gắn action lịch** (cố ý, để TC giữ đúng 1 mục đích — chiều lịch nhắc do NEW-20 phụ trách)<br>- Bạn bè 「TC38944_FRIEND_A」 **đang có giá trị** `2026-09-15`<br>- Đã ghi lại 「回答人数」 của mục và số dòng lịch sử của bạn ở mục đó | 1. Mở Chat 1:1 và mở hội thoại của bạn 「TC38944_FRIEND_A」<br>2. Ở tab 「友だち情報」, bấm ô giá trị mục 「TC38944_日付」, **xóa trắng ô ngày** (hoặc dùng thao tác xóa của bộ chọn ngày), bấm ra ngoài ô để lưu<br>3. Quan sát ô giá trị và **đọc nguyên văn thông báo nếu có** hiện lên<br>4. Tải lại trang, mở lại hội thoại và đọc lại giá trị<br>5. Đọc lại 「回答人数」 trên màn 「友だち情報管理」<br>6. Đọc dòng lịch sử mới nhất của bạn ở mục đó<br>7. Kiểm tra bảng lưu giá trị cho cặp bạn bè × mục<br>8. Kiểm tra **không có lịch nhắc nào** được tạo hoặc còn lại cho cặp bạn bè × mục này<br>9. Mở nhật ký ứng dụng, tìm lỗi trong khoảng vừa thao tác | Giá trị trước khi xóa: `2026-09-15`. Thao tác: xóa trắng ô ngày | - Ô giá trị trống sau khi tải lại trang<br>- **KHÔNG** hiện thông báo 「日付のフォーマットが無効です。」 — giá trị trống là **hợp lệ**, bộ kiểm ngày mới không được chặn<br>- Bản ghi giá trị **không còn tồn tại**<br>- 「回答人数」 giảm đúng 1<br>- Lịch sử thêm đúng 1 dòng 'xóa giá trị' với giá trị cũ `2026-09-15`<br>- **Không có lịch nhắc mồ côi** nào cho cặp bạn bè × mục<br>- Nhật ký không có lỗi | | Lấp `G12` · Tách từ TC gộp theo chỉ định Leader (2026-09-10) · **Vế đặc thù của kiểu này**: xóa trắng phải được bộ kiểm ngày của `#40689` coi là **HỢP LỆ** — `REQ-014` ghi rõ "Giá trị trống vẫn hợp lệ"; nếu bị báo 「日付のフォーマットが無効です。」 thì bản vá **chặn quá tay** · Cố ý chọn mục KHÔNG gắn action để giữ TC atomic; chiều lịch nhắc do NEW-20 (`TC-STATEDEP001-01`) + kho `TC-FRI-214` phụ trách · Là **cặp trên khung chat 1:1** của NEW-57 (cùng thao tác nhưng ở màn 「友だち詳細」) · Đánh giá spec: Spec ghi rõ — `REQ-014` · Evidence: ảnh ô ngày sau F5 + ảnh 回答人数 + ảnh dòng lịch sử + kết quả truy vấn lịch nhắc |
| TC-DEPLOYASSET001-01 | UI | DEPLOY-ASSET-001 | Phân quyền & môi trường | Abnormal | manual | product | Trình duyệt giữ bản JS cũ sau deploy — cờ chặn lưu nhanh có còn tác dụng không | - Xác định trước **số hiệu phiên bản tài nguyên tĩnh** mà trang đang gắn vào `chat-v2.js` (đọc trong mã nguồn trang)<br>- Có 2 trình duyệt / 2 profile: **A** mở trang Chat 1:1 **TRƯỚC** khi deploy và giữ nguyên, **không** tải lại trang · **B** mở sau khi deploy<br>- Bot thử nghiệm nội bộ có mục 「TC38944_選択2」 ở nhóm mục ẩn<br>- Đã ghi lại danh sách dòng cấu hình hiển thị của mục đó | 1. Trước deploy: trên trình duyệt **A**, mở Chat 1:1 và mở tab 「友だち情報」; ghi lại số hiệu phiên bản tài nguyên của `chat-v2.js` đang nạp<br>2. Deploy bản vá lên production<br>3. Trên **B** (mở mới): ghi lại số hiệu phiên bản tài nguyên của `chat-v2.js` — **so sánh với bước 1**<br>4. Trên **A** (vẫn giữ JS cũ): thêm mục 「TC38944_選択2」 rồi **bấm lưu nhanh 2 lần liên tiếp**; đếm số yêu cầu lưu gửi đi trong công cụ phát triển<br>5. Đếm số dòng cấu hình hiển thị của mục đó sau bước 4<br>6. Trên **B**: làm đúng bước 4 và đếm lại số yêu cầu gửi đi<br>7. Dọn sạch dữ liệu thử nghiệm | Bấm lưu nhanh 2 lần liên tiếp, thực hiện trên cả 2 trình duyệt A (JS cũ) và B (JS mới) | - Bước 3: ghi nhận rõ số hiệu phiên bản tài nguyên **có đổi hay không** sau deploy<br>- Trên **B**: **chỉ 1** yêu cầu lưu được gửi (cờ chặn ở giao diện hoạt động)<br>- Trên **A**: ghi nhận số yêu cầu gửi đi. Nếu là **2** ⇒ khẳng định người dùng chưa tải lại trang **không** có cờ chặn<br>- **Trong MỌI trường hợp**: chỉ có **đúng 1 dòng cấu hình hiển thị** cho mục đó — chốt phía máy chủ phải đỡ được cả khi cờ giao diện vắng mặt<br>- Không hiện hộp thoại lỗi ở cả 2 trình duyệt | | Lấp `G11` · `Q8` · `chat-v2.js` bị sửa nhưng Dev khẳng định **không đổi số hiệu phiên bản tài nguyên tĩnh** ⇒ đây là đối chứng cho chính khẳng định đó · **RULE-08 → `product`**: chỉ đo được hành vi cache thật khi deploy thật, không bật maintain · Liên quan `TC-DEPLOYLIVE001-01` của round 1 (Leader đã hoãn) — nếu vẫn hoãn vế deploy thì **tối thiểu giữ vế bước 4-5**: giả lập JS cũ bằng cách chặn cache-busting trên staging · Đánh giá spec: Spec không ghi — đã hỏi Leader · Evidence: ảnh số hiệu phiên bản tài nguyên ở A và B + ảnh tab Network đếm số yêu cầu + kết quả truy vấn bảng cấu hình |


**Dòng `GAP`/`RISK` KHÔNG đề xuất TC mới — lý do:**
- **`G2` (`blockUserGroup` — câu 「設定の変更に失敗しました。」)** — runner đã chứng minh từ mã nguồn rằng nhánh `catch` **chỉ chạy khi có ngoại lệ hạ tầng thật**, không dựng được bằng bất kỳ dữ liệu gửi nào. Viết thêm TC chạy từ ngoài sẽ lại `skip`. Cần Dev mở đường tiêm lỗi hoặc Leader chốt cách nghiệm thu → §6 #2. *(Vế `deleteMessageGroup` đã bỏ — tính năng không còn trên tool, TC NEW-58 đã xóa.)*
- **`G3` (lịch nhắc `年月日`)** — NEW-20 đã đúng nội dung cần kiểm, không cần TC mới; vấn đề là **TC bị bỏ khỏi 3 run gần nhất và skip không ghi lý do** (§4 #4). Việc phải làm là đưa vào run kế tiếp; vế production đã nằm trong `TC-ENV003-01` bước 5-6.
- **`Q4` (`SEC-ISO-001`)** — NEW-23 đúng nội dung, `skip` vì **thiếu dữ liệu test**: cần 1 LINE user kết bạn với **cả 2 bot** thử nghiệm. Đây là việc chuẩn bị môi trường, không phải thiếu TC.
- **`Q6` (`COMPAT-LEGACY-001`)** — không viết TC mới khi surface không tồn tại. Cần quyết định phạm vi trước → §6 #3.
- **`G5` / `G6` (tài liệu)** — không phải thiếu TC: cần **recompute snapshot Studio** (G5) và **Dev cập nhật mục 4.1** (G6).

---

## 6. Spec update needed

| # | Chủ đề | Hiện trạng | Cần ai chốt |
|---|---|---|---|
| 1 | **`HTTP 404` thay vì `422` ở nhánh validate ngày** | 2 TC `fail` (NEW-55, NEW-56) **chỉ vướng đúng một vế: mã trạng thái**. Runner đã xác minh vế nội dung ĐẠT: thông báo đúng nguyên văn 「日付のフォーマットが無効です。」 và giá trị rác không được ghi. Nguyên nhân: **`app/Exceptions/Handler.php` ép MỌI lỗi JSON thành `HTTP 404`** (`401` nếu không HTTPS) và đặt mã thật vào `error.code` — **hành vi chung toàn ứng dụng, có từ trước, không do bản vá này sinh ra**. Bug `#991` đang `open`, **chưa có ticket Redmine**. | **Leader + Dev** — chốt 1 trong 2: (a) expected của TC sai theo quy ước của ứng dụng ⇒ sửa expected thành "mã thật 422 nằm trong `error.code`" và 2 TC chuyển Đạt; (b) `Handler.php` là lỗi thật ⇒ raise ticket **riêng** (ảnh hưởng toàn ứng dụng, **ngoài phạm vi** #38944) và 2 TC này không chặn ticket hiện tại. **Không tự chọn bên.** |
| 2 | **Cách nghiệm thu `REQ-012` cho nhánh `catch` không kích hoạt được từ ngoài** | `blockUserGroup`: khoá `msg` 「設定の変更に失敗しました。」 nằm trong nhánh `catch` **chỉ chạy khi có ngoại lệ CSDL/hạ tầng**. Runner đã đọc mã và kết luận không dựng được từ ngoài; đã **từ chối chấm pass** (đúng). *(Vế `deleteMessageGroup` đã bỏ khỏi phạm vi — tính năng không còn trên tool.)* | **Dev + Leader** — chọn: (a) Dev mở đường tiêm lỗi tạm (cờ cấu hình / hook test) để QA chạy được; (b) Leader chấp nhận nghiệm thu bằng **code-review có ký** cho nhánh này và ghi vào `Ghi chú` của NEW-59 lý do không có evidence runtime. |
| 3 | **Màn chat 1:1 bản cũ là surface chết nhưng vẫn nằm trong `4.1`** | Route duy nhất trỏ tới view `chats.chat_basic` **đang bị comment trong `routes/web.php`**; 3 đường vào đều redirect về `/basic/overview`. Dev đã tự gỡ phần sửa V1 vì "code chết" nhưng **vẫn kê `chat_basic.blade.php` là màn đi qua** — cùng một loại sai. | **Dev** — xác nhận màn cũ còn được dùng ở đâu không. Nếu chết ⇒ **gỡ khỏi 4.1** và đóng `COMPAT-LEGACY-001` với lý do chính đáng (RULE-03). Nếu còn dùng ⇒ mở lại route trên môi trường test để chạy NEW-60. |
| 4 | **Dòng cấu hình hiển thị TRÙNG tồn đọng trong DB** | Dev tuyên bố chỉ ngăn sinh thêm, **không dọn dữ liệu cũ**, và không thêm ràng buộc duy nhất ở tầng DB. Spec `chat-11` BR-17 không nói gì về trường hợp 1 mục có nhiều dòng cấu hình. | **Leader + PO** — chốt: có chạy script dọn dữ liệu bẩn không; và hành vi đúng khi đã tồn tại dòng trùng (hiện 1 lần hay 2 lần ở cột phải). Chốt xong ghi vào BR-17. |
| 5 | **`REQ-014` nói "nhận mọi định dạng đọc được" nhưng spec nói regex cố định** | `REQ-014`: "nhận mọi định dạng đọc được và chuẩn hóa". Spec [detail-friend/feature-spec.md:525](../../spec-features/admin/detail-friend/feature-spec.md) ghi rõ **EP-11: `birthday` regex `Y-m-d` hoặc `Y/m/d`** và dòng 114 "Validate `birthday` regex". Hai phát biểu **không khớp** — không có chuẩn để chấm `TC-FUNC001-01` bước 4 (`2026/09/20`). | **Dev + Leader** — chốt danh sách định dạng được chấp nhận cho mục 「年月日」 và cho 「生年月日」 (có giống nhau không), rồi cập nhật spec `detail-friend` EP-11. |
| 6 | **Tiền đề "bộ đếm lệch" không dựng được qua ứng dụng** | NEW-24 và NEW-53 cùng `skip` vì mọi luồng ghi của app đều giữ bất biến "bộ đếm ≥ số người có giá trị" (runner đã đo từng đường). Nhưng dữ liệu lệch **có thật ở production** (chính bug `#40661`/`#918` đã tạo ra: xóa 2 bạn mà bộ đếm chỉ giảm 1). | **Leader** — cho phép seed tiền đề ở **tầng dữ liệu** cho nhóm TC `Data` (như `TC-DATACOUNT001-02` đề xuất), hay đóng 2 TC này với lý do "bất biến của app đã chặn"? Nếu đóng thì phải ghi rõ rằng sàn `GREATEST(...,0)` của `#40661` **chưa từng được kiểm chứng bằng chạy thật**. |
| 7 | **Chính sách file ảnh/PDF mồ côi khi xóa giá trị** | Bug `#919` (từ `TC-REGSHARED001-06` của round 1) → Redmine `#40660`: "xóa giá trị mục 画像/PDF **không xóa file trên kho lưu trữ**" — trạng thái **`rejected`**, và `dev_impact` ghi "`#40660` dọn file đã **revert**". NEW-22 hiện `pass` chỉ vì expected cho phép cả hai kết cục. `MEDIA-CLEAN-001` là quan điểm nâng-Cao. | **Leader + PO** — chốt: file mồ côi là chấp nhận được (ghi vào spec + siết expected NEW-22 thành "file vẫn còn là đúng") hay phải dọn (mở lại `#40660`). Hiện TC đang không phán được điều gì. |
| 8 | **`4.3` ghi "FA-036 Trang cá nhân (マイページ): KHÔNG ảnh hưởng" — cần làm rõ đang nói màn nào** | Trong repo có **2 thứ khác nhau cùng tên マイページ**: (a) `spec-features/admin/my-page/` = **màn tài khoản của chính admin** (`/admin/my-page`: tên, công ty, điện thoại, email, mật khẩu, ảnh) — không liên quan friend info; (b) endpoint **`POST /ajax/friendlist/my_page/{line_id}/friend-info-value`** — đường ghi giá trị friend info, Studio đặt tên màn là 「友だち詳細」, và **chính là nơi `#40689` sửa**: NEW-55/NEW-56 đang `fail` ở đây, bug `#991` mở trên đúng endpoint này. Nếu (b) là thứ Dev gọi là マイページ thì câu "KHÔNG ảnh hưởng" **sai**; nếu (a) thì câu đó đúng nhưng đang gây nhầm. | **Dev** — xác nhận FA-036 「マイページ」 trong `4.3` là màn nào, và có màn **friend-facing** riêng cho bạn bè tự sửa thông tin không (nếu có thì `FriendDetailFriendInfoService` vừa sửa có phục vụ màn đó ⇒ phải bổ sung TC). **Chưa viết TC cho mục này** vì chưa xác nhận được surface — không đoán. |
| 9 | **`4.1` của Dev đã 3 lần kê thứ KHÔNG còn dùng được — cần một bước tự rà trước khi submit** | Ba lần trong cùng một ticket: (1) hàm `saveSettingDisplayInfo` **V1** — Dev tự phát hiện là code chết và **tự đính chính** (`#135413`, commit `21116c9b1b`); (2) màn **chat 1:1 bản cũ** `chats/chat_basic.blade.php` — route bị comment trong `routes/web.php` (lần chạy AI đo được), **vẫn nằm trong 4.1**; (3) **`deleteMessageGroup()`** — Leader xác nhận tính năng **không còn trên tool**, nhưng vẫn nằm trong 4.1 và đã làm sinh ra 1 TC vô ích (`NEW-58`, đã xóa 2026-09-10). Lưu ý: endpoint `POST /basic/delete-message-group` **vẫn đăng ký và trả `HTTP 200`** (lần chạy AI đo được) — tức code + route còn sống trong khi UI đã bỏ, nên chỉ đọc code sẽ không phát hiện được. | **Dev + Leader** — thêm 1 bước vào quy trình đánh giá ảnh hưởng: với mỗi màn / đường HTTP kê ở `4.1`, xác nhận **còn được UI gọi tới** (route không bị comment, có blade/JS gắn sự kiện) trước khi submit. Hiện 3/13 mục ở `4.1` là surface chết ⇒ QA mất công viết và chạy TC cho thứ không tồn tại. |

---

### Phụ lục — thay đổi so với round 1 (2026-09-07)

| Mục | Round 1 | Round 2 | Nhận xét |
|---|---|---|---|
| Phạm vi fix | 1 file, 2 dòng, 1 ticket | **5 file, 207 insertions, 8 điểm, 9 ticket** | Dev đã chủ động rà và dọn cả họ bug — cải thiện lớn |
| Chất lượng note dev-impact | Mục 3 chỉ 1 hàm, không có caller; 4.2 ghi "Không có" | Kê đủ 8 điểm + hàm + đường HTTP + màn hình + data + "điểm chưa bịt" + **tự đính chính** khi phát hiện sửa code chết | `[AP-6]` và `[AP-4]` đã hết; còn sai ở 3/5 file (G6) |
| Số TC | 24 | **47** (48 rồi xóa NEW-58 theo chỉ định Leader) | Cả 16 REQ đều có ≥ 1 TC |
| Môi trường chạy | **100% local**, 0 staging, 0 prd | **staging 48/48** (9 run), 0 prd | RULE-08 còn thiếu vế production |
| Kết luận test | "17/17 pass" nhưng thực tế là số hợp nhất | `39 pass / 2 fail / 6 skip` — minh bạch hơn, skip của lần chạy AI có ghi lý do đo được | Chất lượng báo cáo runner tăng rõ; nhưng 3 bản chấm tay mới nhất lại **trống `actual`** (§4 #2) |
| 7 TC đề xuất round 1 đã push | — | **3 bug thật**: `#40689` (closed) · `#40661` (closed) · `#40660` (rejected); 1 bug còn open (`#988`) | Bộ TC bổ sung có giá trị thực tế |
