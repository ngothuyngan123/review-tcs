# 05 — Review Report

## 0. Nguồn TC

| Mục | Giá trị |
|---|---|
| Nguồn đã dùng | **MCP LME TEST STUDIO** — task `#229` (ticket 38944, round 6, branch `origin/ai_fixbug_38944`, `reviewState=leader`) |
| Tổng số TC review | **17** |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận: 5/10 vùng ảnh hưởng đủ TC · 3 GAP · 2 RISK.**

> Chiều `dev-impact` = 4 vùng Dev tự kê ở `03-dev-impact.md`. Chiều `diff code` = 6 điểm sửa lấy từ **tab Thông tin Studio** (`dev_impact` + `spec_delta`), `diffAvailable = true`, `diffStat` = `app/Http/Controllers/ChatController.php | 4 ++-- (1 file, 2 insertions, 2 deletions)`.
> ⚠️ Chỉ ghi dòng `GAP`/`RISK`. Các vùng đủ TC (BUG root cause · F1 `saveSettingDisplayInfoItem` · T1 Friend Information FA-015 · b4 nhánh DELETE + `ACTION_CLEAR` · b5 `sendAction` không trigger khi rỗng) không liệt kê.

| # | Chiều | Vùng ảnh hưởng | TC hiện có | Status | Vì sao thiếu |
|---|---|---|---|---|---|
| **G1** | diff code | **b2 — commit `#40213` (`97033fb2`) LẪN trong cùng branch**: `saveSettingDisplayInfoV2` catch (~4741) thêm `'msg' => '保存に失敗しました。入力内容をご確認ください。'` | `TC-REGSHARED001-03` (chỉ kiểm "response không phải chữ `undefined`" ở tầng endpoint) | **GAP** | Không TC nào verify **đúng chuỗi JP** `保存に失敗しました。入力内容をご確認ください。`, và không TC nào kiểm **hộp thoại trên UI** — mà chính "hộp thoại hiện `undefined`" là triệu chứng gốc. `REQ-011` có **0 TC**. |
| **G2** | diff code | **b3 — `saveSettingDisplayInfoV2` (~line 4717) VẪN đọc thẳng `$value->value` cho nhánh calendar; `#40213` KHÔNG guard dòng này** (nguyên văn `dev_impact` Studio) | Không có | **GAP** | `TC-REGSHARED001-03` chỉ gửi payload **`選択肢`** tới V2. Payload custom field kiểu **`年月日`** thiếu `value` gửi tới V2 → vẫn còn nguy cơ crash cùng root cause. Đây là điểm Studio **tự nêu là cần verify** nhưng không sinh TC. |
| **G3** | diff code | **b6 — nền so sánh đổi từ `release_step_20260623` sang `release_step_20260827` (2 nền rẽ nhánh nhau)** | Không có | **GAP** | `REQ-012` có **0 TC**. Bug Studio `#753` → Redmine **#40343** ("Xuất CSV lịch sử trò chuyện: 181 ngày KHÔNG bị chặn") chính là bằng chứng bản đem test **thiếu tính năng của nền mới**, nhưng bug bị đánh **`rejected`** và không có TC verify lại. |
| **G4** | diff code | **b1 — `isset()` guard tại 4471 nằm ở nhánh `type==1` (custom field), dùng chung cho MỌI `type_data`** (BR-02: 1 選択肢 · 2 記述 · 3 年月日 · 4 画像 · 5 PDF · ポイント) | 14 TC nhưng **100% chỉ dùng `type_data=1` (選択肢)** | **RISK** | Chỉ 1/6 kiểu dữ liệu đi qua nhánh vừa sửa được test với payload thiếu `value`. `TC-REGSHARED001-01` có chạm 記述/年月日/ポイント nhưng **chỉ ở luồng lưu bình thường (có `value`)**, không phải luồng thiếu `value`. Studio tự loại 画像/PDF với lý do "fix không chạm nhánh media" — lý do này **không đứng vững** vì guard nằm ở nhánh dùng chung trước khi rẽ theo `type_data`. |
| **G5** | dev-impact | **D1 — `03-dev-impact.md` mục 4.2 khai "Không có data impact"** | `TC-DATADB001-01` (có TC) | **RISK** | Có TC nhưng **Dev kê sai impact**: thực tế bản vá biến request-crash thành **`DELETE friend_information_value` + giảm `total_user_has_value` + ghi `recordFriendInfoHistory ACTION_CLEAR`**. Mục 4.2 phải được Dev sửa lại, nếu không vòng review sau sẽ tiếp tục bỏ qua chiều data. |

---

## 2. Thiếu so với quan điểm test

**Kết luận: 8 quan điểm Trigger khớp task nhưng TC chưa cover đủ (6 Cao · 2 nâng-Cao).** Các quan điểm đã đủ (`FUNC-001`, `DATA-DB-001`, `DATA-AUDIT-001`, `FUNC-SEQ-001`, `MSG-USER-001` phần trigger action, `REG-SHARED-001` phần V1) không liệt kê.

| # | Mã quan điểm | Ưu tiên | Trigger khớp vì | TC hiện có | Kết luận |
|---|---|---|---|---|---|
| **Q1** | `STATE-DEP-001` + `JOB-001` | Cao | Bản vá tạo **đường XÓA giá trị mới**. Custom field kiểu `年月日` có action lịch → mỗi friend có bản ghi `event_step_time` đã lên lịch. Xóa giá trị thì lịch phải được dọn. | **0 TC** | **[BLOCKER]** — không TC nào kiểm lịch gửi sau khi giá trị bị xóa qua đường mới. Kho đã có oracle: `TC-FRI-214` "Xóa trắng giá trị trường 年月日 CÓ gắn action lịch → lịch gửi được dọn, không còn lịch mồ côi". |
| **Q2** | `OUT-TRUTH-001` | Cao | "BẮT BUỘC với mọi thao tác lưu/gửi **có thông báo kết quả**". Triệu chứng gốc của ticket là **hộp thoại hiện `undefined`**; commit `#40213` trong cùng branch sửa đúng chỗ này. | 0 TC ở tầng UI | **[BLOCKER]** — xem G1. Fix thông báo lỗi được ship kèm nhưng không ai verify người dùng thật nhìn thấy gì. |
| **Q3** | `REG-SPEC-001` | Cao | "BẮT BUỘC với mọi thay đổi spec/requirement xảy ra **SAU** khi đã có test case". `REQ-011` + `REQ-012` được thêm **2026-08-27**; bộ TC sinh **2026-08-26** (job 695). | **0 TC cho cả 2 REQ** | **[BLOCKER]** — 2/12 requirement không có TC nào. Bộ TC chưa được cập nhật sau khi requirement đổi. |
| **Q4** | `DATA-001` (+ `FUNC-004`) | Cao | Custom field kiểu **`ポイント` nhận giá trị `0` như một giá trị hợp lệ** (kho `TC-FRI-199`/`TC-FRI-200` xác nhận: "đặt = 0 và ≠ 0", "0 → khác 0, khác 0 → 0"). Bản vá tính `valueNew` rồi các điểm dùng phía sau kiểm **truthy** (chính Dev ghi: "dòng calendar 4474 chỉ chạy khi `valueNew` truthy"). Trong PHP `"0"` là **falsy**. | **0 TC dùng `value = "0"`** | **[BLOCKER]** — nguy cơ `ポイント = 0` bị xử lý như "bỏ trống" → **xóa mất giá trị hợp lệ**. `TC-FUNC004-01` chỉ test `value = ""`, không test `"0"`. |
| **Q5** | `ENV-003` | Cao | "BẮT BUỘC khi tính năng chạm job nền... **Không được đánh × với lý do staging đã pass**" (RULE-08). Bug gốc phát sinh ở **PRODUCTION**. | **17/17 TC chỉ chạy `env = local`** (`envAuto`: local runs=9 · dev 0 · staging 0 · prd 0) | **[MAJOR]** — không có bất kỳ lần chạy nào ngoài local. 6 TC còn bị gắn `env_tag = local-only`. |
| **Q6** | `DEPLOY-LIVE-001` | Cao | "BẮT BUỘC với release lên production không bật maintain, **đặc biệt khi đổi payload API / cấu trúc request**". `#40213` **thêm field `msg` vào response JSON** của V2. `REQ-011` nêu rõ phải "xác nhận việc thêm trường thông báo không phá client đang chạy". | **0 TC** | **[MAJOR]** — không TC nào kiểm client JS bản cũ (đang chạy trên production trong lúc deploy) xử lý response có thêm field `msg` mà không vỡ. |
| **Q7** | `SEC-ISO-001` | Cao | Đường XÓA mới tác động dữ liệu khách. Phải chứng minh phạm vi xóa không vượt ranh giới **bot**. | `TC-DATADB001-01` kiểm **friend khác** + **mục thông tin khác**, **KHÔNG kiểm bot khác** | **[MAJOR]** — kho có `TC-FRI-192` "Xóa giá trị của bạn A / trường X / **bot này** KHÔNG đụng bạn B, trường Y, **bot khác**". Chiều bot đang thiếu. |
| **Q8** | `DATA-COUNT-001` | Cao | "BẮT BUỘC khi màn hình có bất kỳ con số đếm nào" — `回答人数` = `friend_information_setting.total_user_has_value`, bị **giảm** bởi đường xóa mới. | 8 TC kiểm tăng/giảm/không đổi, **0 TC kiểm biên counter** | **[MAJOR]** — kho có `TC-FRI-187` "`回答人数` đang = 0 nhưng bạn vẫn còn giá trị sót → xóa giá trị KHÔNG làm count âm". Đường xóa mới có thể chạy trên dữ liệu lệch sẵn có ở production. |

---

## 3. TC trùng lặp nội dung

**Đã rà toàn bộ 17 TC theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`). Không có `DUP-EXACT`. Phát hiện 1 `DUP-SUBSET`.**

| Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| Payload thiếu `value`, friend đang có `オプションA` | `TC-DATADB001-01` (#13621) | `TC-TOOLKNOW002-01` (#13620) — **đề nghị GỘP, KHÔNG xóa** | `DUP-SUBSET` | Đối tượng + thao tác: **trùng hoàn toàn** (POST `saveSettingDisplayInfoItem`, `infos` bỏ khóa `value`, `FRIEND_A` đang có `オプションA`) · Tiền đề: **tương đương** (#13621 chỉ thêm dữ liệu đối chứng) · Loại case: **trùng** (`Abnormal`) · Mã quan điểm: **khác** (`TOOL-KNOW-002` vs `DATA-DB-001`) | `[MINOR]` |

**Gate trước khi đề nghị xóa — đã chạy lại BƯỚC 2 + 3 trên tập giả định đã xóa `#13620`:**
- `#13620` là **TC tái hiện bug duy nhất** được gắn nhãn "tái hiện lỗi #38944" và là TC duy nhất map tới điểm sửa `ChatController.php:4471 (release)`. Xóa nó làm **mất traceability về BUG root cause**.
- → Đổi đề xuất từ **xóa** sang **gộp**: giữ `#13620` làm TC tái hiện, **chuyển assertion đọc nhật ký** (`không có dòng 「Undefined property: stdClass::$value」`) sang `#13621` để `#13621` tự đủ, rồi thu hẹp `#13620` về đúng phần tái hiện + log. **Không xóa TC nào.**

**Cụm KHÔNG phải trùng (ghi rõ để Leader không xóa nhầm):**
`TC-STATEMATRIX001-01` (`value="clear_data_option"`) · `TC-FUNC004-01` (`value=""`) · `TC-REGSHARED001-04` (`value=null`) có steps + expected gần như y hệt, nhưng là **3 lớp tương đương input khác nhau** của chính biểu thức bị sửa → **giữ đủ cả 3**. Chỉ thiếu lớp thứ 4 là `value="0"` (xem Q4).

---

## 4. Issues khác

> Không chứa GAP coverage (đã ở §1 / §2). Nhóm **chất lượng nguồn TC** nằm trên, **chất lượng từng TC** nằm dưới.

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | Bug tab Studio | **2 bug mức High chưa đóng, cùng root cause với ticket đang test**: `#694` **`stale`** → Redmine **#40213**; `#697` **`suppressed`** (không có Redmine). Cả hai đều là "Payload thiếu trường giá trị làm hàm lưu crash: phản hồi thất bại TRỐNG + dữ liệu ghi dở dang". Commit fix của `#40213` nằm **ngay trong branch đang test** nhưng bug vẫn ở trạng thái `stale`. | Chốt với Dev/Leader: `#40213` đã fix xong trong branch này chưa → cập nhật trạng thái bug, và **bổ sung TC verify** (xem Q2/G1). Không tạo bug duplicate. |
| 2 | `[BLOCKER]` | `[AP-1]` Single-trigger generic-fix — `TC-REGSHARED001-03` | Commit `#40213` trong cùng branch là **generic catch** (`catch { ... 'msg' => '保存に失敗しました。入力内容をご確認ください。' }`). Toàn bộ bộ TC chỉ trigger khối catch này bằng **đúng 1 điều kiện** (payload thiếu `value`). Không có TC nào trigger bằng điều kiện lỗi **khác**, và không có TC nào trigger bằng lỗi **chưa biết trước** để verify fallback. | Bổ sung ≥ 3 trigger khác nhau vào khối catch V2 (thiếu `value` · `id_setting` không tồn tại · `lineId` không thuộc bot) + 1 trigger unknown, tất cả phải trả cùng message JP, không rơi về `undefined`. |
| 3 | `[MAJOR]` | Toàn bộ 17 TC | **Con số `pass 17/17` là hợp nhất `last_exec`, KHÔNG phải một lần chạy xanh toàn bộ.** Lần chạy cuối (`run 946`, 2026-09-05) **chỉ chạy 1/17 TC** (`#16394`). 16 TC còn lại lấy kết quả từ `run 728` (2026-08-28) — mà **`run 728` có `status = fail`**. | Chạy lại **toàn bộ 17 TC trong một run duy nhất** trên đúng commit đem test, rồi mới kết luận. |
| 4 | `[MAJOR]` | Lịch sử 9 run | **`skip 32` + `error 8`** trên tổng 129 kết quả. `run 603` và `run 636` **skip toàn bộ 16 TC** vì `SOURCE_BLOCKED` (`SOURCE_CHECKOUT_ERROR`: checkout `ai_fixbug_38944` thất bại). `run 572` **error 6 TC** vì popup `#topModalOverview` chặn thao tác. Skip/error **không phải kết quả test**. | Hạ tầng runner cần fix trước (worktree lock, popup QR chặn UI). Không tính skip/error vào coverage. |
| 5 | `[MAJOR]` | 13/17 TC — RULE-02 | **Chỉ 4/17 TC có artifact ở lần chạy cuối** (`#13620`, `#13635`, `#13642`, `#16394`). 13 TC còn lại `pass` nhưng **không có bằng chứng nào** — cột `Evidence thực tế` rỗng toàn bộ. Vi phạm RULE-02 ("không chấp nhận đã xem, OK"). | Yêu cầu runner đính artifact (log request/response + screenshot + kết quả truy vấn dữ liệu) cho mọi TC trước khi tick Đạt. |
| 6 | `[MAJOR]` | Bug `#753` → Redmine **#40343** | Bug "Xuất CSV lịch sử trò chuyện: 181 ngày KHÔNG bị chặn" bị đánh **`rejected`** nhưng đây chính là **bằng chứng nền test lệch** (`REQ-012`: nhánh fix tách từ `release_step_20260623`, chưa rebase lên `release_step_20260827`). Reject mà không có TC verify lại. | Xác nhận với Dev lý do reject. Nếu do nền lệch → phải rebase rồi chạy lại, không được reject. |
| 7 | `[MAJOR]` | `[AP-6]` `03-dev-impact.md` mục 3 | Dev **chỉ kê đúng 1 function** (`saveSettingDisplayInfoItem`), **không có danh sách caller / điểm dùng chung nào**. Trong khi `dev_impact` của Studio chỉ ra ngay `saveSettingDisplayInfoV2` cùng vùng code, cùng pattern lỗi, **chưa được vá** ở nhánh calendar. | Yêu cầu Dev list đầy đủ nơi đọc thẳng `->value` trong `ChatController` và nói rõ chỗ nào cố ý không vá. |
| 8 | `[MAJOR]` | `[AP-4]` `03-dev-impact.md` mục Thông tin | **Không có link Pull Request** (chỉ có commit hash `0aa5427f22`). Mức verify của Dev **chỉ là `lint`** (`php -l`) — không unit test, không chạy thật. | Yêu cầu Dev cung cấp PR link để review verify được hình dạng fix thực tế. |
| 9 | `[MAJOR]` | `[AP-3]` `TC-REGSHARED001-01` | TC regression duy nhất cho `T1` dùng precondition **data sạch** ("bạn bè chưa có giá trị ở cả ba mục"). Không có TC regression nào chạy trên **edge state** (friend đã có giá trị, giá trị `0`, mục đã bị xóa cấu hình, lịch gửi đang chờ). | Bổ sung regression trên trạng thái đã có dữ liệu — xem `TC-DATA001-01` và `TC-STATEDEP001-01` ở §5. |
| 10 | `[MAJOR]` | `03-dev-impact.md` | File có `Auto-filled: 2026-09-07 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" CHƯA tick** → F/D/T có thể thiếu hoặc map sai (đã thấy sai ở mục 4.2, xem G5). | Tester đọc lại journal #133032 trên Redmine, sửa mục 4.2, rồi tick checkbox. |
| 11 | `[MAJOR]` | `TC-DATADB001-01` · `TC-STATEMATRIX001-02` · `TC-MSGUSER001-02` | 3 TC còn `spec_status = needs-human-review`, đang chặn kết luận. **2/3 đã có tiền lệ trong kho, có thể chốt ngay**: `TC-STATEMATRIX001-02` ↔ kho `TC-FRI-189` ("bạn CHƯA có giá trị mà lưu rỗng → không tạo dòng, count giữ 0") — **khớp**; `TC-MSGUSER001-02` ↔ kho `TC-FRI-213` ("xóa trắng giá trị trường Lựa chọn/Điểm CÓ gắn action → KHÔNG kích hoạt action ngoài ý") — **khớp**. Còn lại `TC-DATADB001-01` trùng đúng câu hỏi mở `MT-03` của kho. | Bỏ `needs-human-review` cho 2 TC có tiền lệ; đẩy câu hỏi còn lại lên §6. |
| 12 | `[MINOR]` | 6 TC | **5 mã quan điểm Studio không tồn tại trong `framework/checklist-lme.md`**: `DATA-HIST-001`, `TOOL-VAL2-001`, `TOOL-KNOW-002`, `STATE-MATRIX-001`, `TOOL-ERRHYG-001`. Các mã `TOOL-*` là mã nội bộ Studio → **không tính là cover** khi map coverage. | Map lại về mã chuẩn: `DATA-HIST-001` → `DATA-AUDIT-001`; `STATE-MATRIX-001` → `STATE-001`; `TOOL-ERRHYG-001` → `OUT-TRUTH-001`; `TOOL-VAL2-001` / `TOOL-KNOW-002` → giữ nhưng ghi rõ là mã nội bộ. |
| 13 | `[MINOR]` | `TC-FUNC004-01` | Gán mã `FUNC-004` (trigger = "chức năng có **giới hạn số lượng / ký tự / dung lượng**") nhưng nội dung test là `value = ""` — biên **giá trị rỗng**, không phải biên giới hạn. Mã không khớp trigger. | Đổi sang `DATA-001` hoặc `FUNC-001` (Boundary). |
| 14 | `[MINOR]` | `TC-REGSHARED001-03` (#16394) | **Tiêu đề mô tả hành vi LỖI** ("...crash: phản hồi thất bại TRỐNG (hộp thoại hiện `undefined`) và dữ liệu bị ghi dở dang") nhưng **`Kết quả mong đợi` lại mô tả hành vi ĐÚNG**. Đọc bảng TC sẽ hiểu ngược. | Đổi tiêu đề sang dạng khẳng định hành vi mong đợi, VD "Payload thiếu trường giá trị ở luồng lưu danh sách hiển thị — xử lý xác định, không ghi dở dang". |
| 15 | `[NIT]` | `TC-MSGUSER001-01` — RULE-06 | `Kết quả mong đợi` cho phép kết luận Đạt **mà không cần tin nhắn tới LINE thật** ("nếu outbound bị chặn thì ghi lỗi môi trường"). Hợp lý cho local nhưng chưa đi tới output cuối chuỗi. | Khi chạy ở `product` phải verify tin `TC38944_ACTION_FIRED` trên **LINE app thật** (RULE-06). |

**Anti-pattern đã rà**: `[AP-1]` dính (#2) · `[AP-2]` **không dính** (file 01 có log + error message + stack trace cụ thể, không phải symptom-only) · `[AP-3]` dính (#9) · `[AP-4]` dính (#8) · `[AP-5]` **không dính** (mọi TC đều đứng ở tầng controller/UI, không lan xuống layer không bị chạm) · `[AP-6]` dính (#7).

---

## 5. TCs đề xuất bổ sung

**Đã đối chiếu trước khi viết (BƯỚC 5a/5b):**

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | [kho-tcs/fa001-chat11-11チャット.md](../../kho-tcs/fa001-chat11-11チャット.md) §35 "Rightbar — 友だち情報" (8 TC) · [kho-tcs/fa015-quanlythongtinbanbe-友だち情報管理.md](../../kho-tcs/fa015-quanlythongtinbanbe-友だち情報管理.md) §19 "Bộ đếm 回答人数" (30 TC) · §20 "Ghi giá trị từ màn admin" (17 TC) · §23 "Job action ngày tháng" (22 TC) |
| Vùng regression phát hiện từ kho | `TC-FRI-214` (xóa trắng 年月日 có action → dọn lịch) · `TC-FRI-213` (xóa trắng 選択肢/ポイント có action → không kích hoạt) · `TC-FRI-187` (count không âm) · `TC-FRI-192` (không đụng bot khác) · `TC-FRI-199`/`TC-FRI-200` (điểm = 0 là giá trị hợp lệ) · `TC-CHT-352`/`TC-CHT-353`/`TC-CHT-354` (Rightbar 友だち情報: kiểu chọn/chữ/ngày, ảnh/PDF, điểm âm-dương) |
| Conflict expected vs kho | **Không** — mọi expected của TC đề xuất khớp với kho. Ngược lại kho còn **giải quyết 2/3 `needs-human-review`** của Studio (xem §4 #11). |
| GAP dùng lại TC kho (không viết mới) | **Không** — kho cover đường trigger cũ ("xóa trắng từ UI"), còn bản vá tạo trigger **mới** ("payload thiếu khóa `value`") tới cùng đoạn code. Phải viết TC mới nhưng **mượn nguyên oracle của kho**, ghi `dẫn từ <ID kho>`. |
| Xác nhận chống trùng | Đã đối chiếu **17 TC** ở BƯỚC 0 + 2 file kho — **không TC đề xuất nào trùng**. |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-OUTTRUTH001-01 | UI | OUT-TRUTH-001 | Rightbar — 友だち情報 | Abnormal | manual | Tất cả | Lưu danh sách hiển thị thất bại — hộp thoại hiện thông báo tiếng Nhật đọc được, không còn chữ undefined | - Đăng nhập admin, đang chọn bot thử nghiệm<br>- Màn Chat 1:1, đang mở hội thoại 1 bạn bè<br>- Tab 「友だち情報」 ở cột phải mở được<br>- Đã chuẩn bị 1 mục thông tin sẽ bị hệ thống từ chối khi lưu (nhờ Dev chỉ điều kiện từ chối, VD mục đã bị xóa ở tab khác) | 1. Mở modal 「表示内容を変更」 từ biểu tượng ba chấm của tab 「友だち情報」<br>2. Ở tab trình duyệt thứ 2, xóa mục thông tin đang được chọn trong modal ở tab 1<br>3. Quay lại tab 1, bấm nút lưu của modal<br>4. Đọc **nguyên văn** nội dung hộp thoại hiện lên, chụp màn hình<br>5. Đóng hộp thoại, tải lại trang và đối chiếu danh sách mục hiển thị so với trước bước 3 | Thao tác lưu rơi vào nhánh thất bại của modal 「表示内容を変更」 | - Hộp thoại hiện **đúng chuỗi** 「保存に失敗しました。入力内容をご確認ください。」<br>- Hộp thoại **KHÔNG** hiện chữ `undefined`, không hiện hộp thoại trống<br>- Sau khi tải lại trang, danh sách mục hiển thị **giữ nguyên như trước** thao tác lưu (không ghi dở dang) | | Lấp `G1` · `Q2` · Đánh giá spec: Spec không ghi — chuỗi lấy từ commit `97033fb2cb` của Redmine #40213, **cần Dev xác nhận điều kiện tái hiện nhánh thất bại** · Evidence: ảnh chụp hộp thoại + ảnh danh sách mục trước/sau |
| TC-REGSHARED001-05 | API | REG-SHARED-001 | Rightbar — 友だち情報 | Abnormal | auto | Tất cả | Luồng lưu danh sách hiển thị nhận payload mục kiểu 年月日 thiếu trường giá trị — không crash, không ghi dở dang | - Đăng nhập admin bằng màn đăng nhập thật, đã chọn bot thử nghiệm để có phiên hợp lệ<br>- Bot có mục 「TC38944_日付2」 kiểu 「年月日」, hiện CHƯA nằm trong danh sách hiển thị<br>- Đã ghi lại danh sách mục đang hiển thị và mốc thời gian trước khi gửi | 1. Lấy cookie phiên + mã chống giả mạo biểu mẫu từ phiên đăng nhập thật<br>2. Ghi lại danh sách mục đang hiển thị ở cột phải tab 「友だち情報」<br>3. Gửi POST tới luồng lưu **danh sách hiển thị** (đường V2 của modal 「表示内容を変更」) với `infos` chứa mục kiểu 年月日 và **bỏ hẳn khóa `value`**<br>4. Đọc mã trạng thái HTTP và toàn bộ nội dung phản hồi<br>5. Mở nhật ký ứng dụng, tìm chuỗi `Undefined property: stdClass::$value` trong khoảng thời gian vừa gọi<br>6. Mở lại Chat 1:1, đối chiếu danh sách mục hiển thị trước/sau<br>7. Dọn cấu hình hiển thị về trạng thái ban đầu | `infos` = 1 mục kiểu 年月日 (`type_data` ngày tháng), có `id_setting` và `title`, **KHÔNG có khóa `value`** | - Nhật ký **KHÔNG** xuất hiện `Undefined property: stdClass::$value`<br>- Phản hồi xác định: hoặc thành công hoàn chỉnh, hoặc từ chối **kèm thông báo lỗi rõ ràng** (không rỗng, không phải `undefined`)<br>- Danh sách mục hiển thị hoặc lưu hoàn chỉnh, hoặc **giữ nguyên** như trước request — không tạo dòng rồi mới lỗi | | Lấp `G2` · Đánh giá spec: Đã hỏi leader — `dev_impact` Studio ghi rõ dòng ~4717 của V2 **vẫn đọc thẳng `$value->value` cho calendar và #40213 KHÔNG guard** · Evidence: log request/response + trích nhật ký ứng dụng + ảnh danh sách mục trước/sau |
| TC-STATEDEP001-01 | Data | STATE-DEP-001 | Job action ngày tháng | Abnormal | manual | product | Payload thiếu trường giá trị cho mục 年月日 có action lịch — lịch gửi được dọn, không còn lịch mồ côi | - Bot thử nghiệm có mục 「TC38944_日付」 kiểu 「年月日」 **có gắn action** gửi tin nhắn nhắc trước 1 ngày<br>- Bạn bè 「TC38944_FRIEND_A」 **đang có giá trị** ngày ở mục này và **đã được sinh lịch gửi đang chờ**<br>- Đã ghi lại mốc lịch gửi hiện có của bạn này và số 「回答人数」 của mục | 1. Mở màn 「友だち情報管理」, ghi lại số 「回答人数」 của mục 「TC38944_日付」<br>2. Mở danh sách lịch gửi đang chờ của bạn 「TC38944_FRIEND_A」 ứng với mục này, ghi lại mốc gửi<br>3. Gửi POST tới luồng lưu một mục thông tin bạn bè cho bạn này, với `infos` của mục 年月日 **bỏ hẳn khóa `value`**<br>4. Đọc mã trạng thái HTTP và nội dung phản hồi<br>5. Kiểm tra giá trị của bạn ở mục 「TC38944_日付」<br>6. **Mở lại danh sách lịch gửi đang chờ** của bạn này cho mục đó<br>7. Chờ qua mốc gửi đã ghi ở bước 2, kiểm tra hội thoại LINE của bạn trên **điện thoại thật** | `infos` = mục 「TC38944_日付」 kiểu 年月日 có `action_id`, **KHÔNG có khóa `value`** | - Phản hồi thành công, không sinh lỗi trong nhật ký<br>- Giá trị ngày của bạn ở mục này **bị xóa**, 「回答人数」 **giảm đúng 1**<br>- **Lịch gửi đang chờ của bạn cho mục này bị dọn sạch** — không còn lịch mồ côi trỏ tới giá trị đã xóa<br>- Qua mốc gửi cũ, LINE app của bạn **KHÔNG** nhận được tin nhắn nhắc | | Lấp `Q1` · regression · **dẫn từ `TC-FRI-214`** (kho FA-015 §23 "Xóa trắng giá trị trường 年月日 CÓ gắn action lịch → lịch gửi được dọn, không còn lịch mồ côi") · RULE-08 → `product` vì chạm **job nền** · RULE-06 → verify trên LINE app thật · Đánh giá spec: Spec ghi rõ (feature-spec friend-information §Luồng xoá dữ liệu bạn bè: xoá `event_step_time` type=3) · Evidence: ảnh danh sách lịch gửi trước/sau + ảnh màn hình LINE tại mốc gửi |
| TC-DATA001-01 | UI | DATA-001 | Ghi giá trị từ màn admin | Boundary | manual | Tất cả | Mục kiểu ポイント nhận giá trị 0 từ khung chat 1:1 — lưu thành 0, KHÔNG bị hiểu là bỏ trống | - Bot thử nghiệm có mục 「TC38944_ポイント」 kiểu 「ポイント」 nằm trong danh sách hiển thị của khung chat<br>- Bạn 「TC38944_FRIEND_A」 đang có điểm = 50<br>- Bạn 「TC38944_FRIEND_C」 chưa có giá trị ở mục này<br>- Đã ghi lại số 「回答人数」 của mục | 1. Mở Chat 1:1, chọn bot thử nghiệm, mở hội thoại 「TC38944_FRIEND_A」<br>2. Ở tab 「友だち情報」, bấm ô giá trị mục 「TC38944_ポイント」, nhập `0`, bấm ra ngoài ô để lưu<br>3. Tải lại trang, mở lại hội thoại, đọc giá trị hiển thị của mục<br>4. Mở màn 「友だち情報管理」 đọc lại số 「回答人数」<br>5. Mở lịch sử thay đổi thông tin bạn bè của bạn này ở mục đó, đọc dòng mới nhất<br>6. Lặp bước 2-5 với bạn 「TC38944_FRIEND_C」 (chưa có giá trị) — nhập `0`<br>7. Kiểm tra bảng lưu giá trị cho cả hai bạn ở mục này | Điểm = `0` (bạn đã có giá trị 50 → đổi thành 0; bạn chưa có giá trị → nhập 0) | - Với `FRIEND_A`: giá trị hiển thị là **`0`**, không phải ô trống. Bản ghi giá trị **vẫn tồn tại** với giá trị `0`. 「回答人数」 **KHÔNG đổi**. Lịch sử ghi loại **'cập nhật'** (giá trị cũ 50 → mới 0), **KHÔNG** phải 'xóa giá trị'<br>- Với `FRIEND_C`: bản ghi giá trị **được TẠO** với giá trị `0`, 「回答人数」 **tăng đúng 1**, lịch sử ghi loại 'ghi mới' | | Lấp `Q4` · **dẫn từ `TC-FRI-199` + `TC-FRI-200`** (kho FA-015 §20: điểm = 0 là giá trị hợp lệ, khác thao tác xóa) và `TC-CHT-354` (kho FA-001 §35: ポイント nhận âm và dương) · Rủi ro: `"0"` là falsy trong PHP, bản vá tính `valueNew` rồi các điểm dùng sau kiểm truthy · Đánh giá spec: Spec không ghi — đã hỏi Leader · Evidence: ảnh giá trị hiển thị sau F5 + ảnh 回答人数 + ảnh dòng lịch sử |
| TC-REGSHARED001-06 | API | REG-SHARED-001 | Rightbar — 友だち情報 | Abnormal | auto | Tất cả | Payload thiếu trường giá trị cho các kiểu dữ liệu còn lại đi qua cùng nhánh vừa vá | - Bot thử nghiệm có 4 mục trong danh sách hiển thị của khung chat: 「TC38944_記述」 kiểu 記述, 「TC38944_ポイント」 kiểu ポイント, 「TC38944_画像」 kiểu 画像, 「TC38944_PDF」 kiểu PDF<br>- Bạn 「TC38944_FRIEND_A」 **đang có giá trị ở cả 4 mục** (ảnh và PDF đã tải file lên)<br>- Đã ghi lại 「回答人数」 của 4 mục và đường dẫn file đã tải lên | 1. Đăng nhập admin, chọn bot thử nghiệm để lấy phiên hợp lệ<br>2. Gửi POST tới luồng lưu một mục thông tin bạn bè cho mục 「TC38944_記述」 với `infos` **bỏ hẳn khóa `value`**; đọc mã trạng thái và phản hồi<br>3. Lặp lại bước 2 cho 「TC38944_ポイント」, 「TC38944_画像」, 「TC38944_PDF」<br>4. Sau mỗi lần: kiểm tra bảng lưu giá trị của bạn ở mục đó và số 「回答人数」<br>5. Với mục ảnh và PDF: kiểm tra **file trên kho lưu trữ** còn hay đã bị xóa<br>6. Mở nhật ký ứng dụng tìm lỗi thuộc tính không tồn tại trong khoảng thời gian vừa gọi | 4 request, mỗi request 1 mục thuộc kiểu 記述 / ポイント / 画像 / PDF, tất cả **KHÔNG có khóa `value`** | - Cả 4 request: nhật ký **KHÔNG** có `Undefined property: stdClass::$value`, phản hồi xác định<br>- Hành vi xóa giá trị **nhất quán với kiểu 選択肢**: bản ghi bị xóa, 「回答人数」 giảm đúng 1 cho từng mục<br>- Với 画像 / PDF: ghi nhận rõ **file trên kho lưu trữ bị xóa theo hay bị bỏ lại mồ côi** — nếu bỏ lại thì raise ticket riêng | | Lấp `G4` · **dẫn từ `TC-CHT-353`** (kho FA-001 §35: mục kiểu ảnh và PDF — tải lên, thay, xóa) · Lý do: guard tại 4471 nằm ở nhánh `type==1` (custom field) **dùng chung mọi `type_data`** theo BR-17, không chỉ 選択肢 — Studio loại 画像/PDF với lý do "fix không chạm nhánh media" là **chưa đủ căn cứ** · Đánh giá spec: Đã hỏi leader — **cần Dev xác nhận vị trí guard trước khi chạy** · Evidence: log 4 request/response + ảnh 回答人数 + kết quả kiểm tra file trên kho lưu trữ |
| TC-REGSPEC001-01 | UI | REG-SPEC-001 | Phân quyền & môi trường | Normal | manual | Tất cả | Bản đem test giữ đủ tính năng của nền phát hành đích release_step_20260827 | - Bản đem test được dựng từ **đúng nhánh sẽ deploy**<br>- Đã có danh sách khác biệt giữa `release_step_20260623` và `release_step_20260827` do Dev cung cấp<br>- Có hội thoại chat 1:1 với lịch sử tin nhắn trải dài > 181 ngày | 1. Vào màn Chat 1:1, mở chức năng xuất CSV lịch sử trò chuyện<br>2. Chọn khoảng thời gian **181 ngày**, bấm xuất<br>3. Đọc nguyên văn thông báo hiện lên<br>4. Chọn khoảng **180 ngày**, bấm xuất, ghi nhận kết quả<br>5. Đối chiếu 2 thông báo lỗi của rich menu và phần lưu danh sách trạng thái theo danh sách khác biệt Dev cung cấp | Khoảng xuất CSV = 181 ngày (ngoài giới hạn) và 180 ngày (trong giới hạn) | - 181 ngày: bị chặn bằng thông báo 「エクスポート対象期間は180日以内で指定してください。」<br>- 180 ngày: xuất được bình thường<br>- 2 thông báo lỗi rich menu và phần lưu danh sách trạng thái của nền mới **còn nguyên** trên bản đem test | | Lấp `G3` · `Q3` · regression · Đây chính là bug Studio `#753` → Redmine **#40343** đang bị đánh `rejected` — TC này để **chốt lại nền test đã đúng chưa** trước khi kết luận bộ TC · Đánh giá spec: Spec ghi rõ — `REQ-012` · Evidence: ảnh thông báo 181 ngày + file CSV 180 ngày + ảnh 2 màn rich menu |
| TC-DEPLOYLIVE001-01 | API | DEPLOY-LIVE-001 | Rightbar — 友だち情報 | Abnormal | manual | product | Response luồng lưu danh sách hiển thị thêm trường thông báo — client bản cũ đang chạy không vỡ | - Bản đem test đã deploy (có commit `97033fb2cb` thêm trường `msg`)<br>- Chuẩn bị được **trình duyệt giữ bản JS cũ** (mở tab trước khi deploy, KHÔNG hard-reload; hoặc chặn cache-busting) | 1. Trước khi deploy: mở Chat 1:1, mở modal 「表示内容を変更」, **giữ nguyên tab, không tải lại trang**<br>2. Deploy bản đem test<br>3. Ở tab đã mở từ bước 1 (đang chạy JS cũ), thêm 1 mục vào danh sách hiển thị rồi bấm lưu<br>4. Quan sát modal đóng/không đóng, hộp thoại hiện ra (nếu có), và console lỗi của trình duyệt<br>5. Tải lại trang, đối chiếu danh sách mục hiển thị<br>6. Lặp bước 3-5 cho trường hợp lưu **thất bại** | Thao tác lưu thành công và thao tác lưu thất bại, thực hiện từ tab giữ JS bản cũ | - Cả 2 trường hợp: client bản cũ **không văng lỗi JavaScript**, modal xử lý đúng<br>- Trường hợp thành công: mục được thêm, danh sách đúng sau khi tải lại<br>- Trường hợp thất bại: có thông báo cho người dùng (dù client cũ chưa biết đọc trường `msg` thì cũng **không được hiện `undefined`**) | | Lấp `Q6` · RULE-08 → `product` vì kiểm hành vi **trong lúc deploy không bật maintain** · `REQ-011` yêu cầu rõ "xác nhận việc thêm trường thông báo không phá client đang chạy" · Đánh giá spec: Spec ghi rõ — `REQ-011` · Evidence: video thao tác trên tab JS cũ + ảnh console trình duyệt |
| TC-SECISO001-01 | Data | SEC-ISO-001 | Bộ đếm 回答人数 | Abnormal | auto | Tất cả | Xóa do payload thiếu trường giá trị không vượt ranh giới bot | - Cùng 1 LINE user là bạn của **cả bot A và bot B**<br>- Cả hai bot đều có mục thông tin kiểu 選択肢 **trùng tên** 「TC38944_選択」<br>- Người dùng này **đang có giá trị ở cả 2 bot**<br>- Đã ghi lại 「回答人数」 của mục ở cả 2 bot | 1. Đăng nhập admin, chọn **bot A**, lấy phiên hợp lệ<br>2. Gửi POST tới luồng lưu một mục thông tin bạn bè cho người dùng này ở **bot A**, `infos` **bỏ hẳn khóa `value`**<br>3. Kiểm tra bảng lưu giá trị của người dùng ở mục 「TC38944_選択」 của **bot A**<br>4. Kiểm tra bảng lưu giá trị của người dùng ở mục 「TC38944_選択」 của **bot B**<br>5. Đọc lại 「回答人数」 của mục ở cả 2 bot<br>6. Đổi sang bot B trên giao diện, mở Chat 1:1 của người dùng này, xem tab 「友だち情報」 | `infos` cho mục 選択肢 của **bot A**, không có khóa `value` | - Giá trị ở **bot A** bị xóa, 「回答人数」 của bot A giảm đúng 1<br>- Giá trị ở **bot B** **VẪN NGUYÊN**, 「回答人数」 của bot B **KHÔNG đổi**<br>- Tab 「友だち情報」 của bot B vẫn hiển thị đúng giá trị cũ | | Lấp `Q7` · **dẫn từ `TC-FRI-192`** (kho FA-015 §19: xóa giá trị của bạn A / trường X / bot này KHÔNG đụng bạn B, trường Y, bot khác) và `TC-FRI-197` · Bổ sung chiều **bot** mà `TC-DATADB001-01` còn thiếu · RULE-07 · Đánh giá spec: Spec ghi rõ · Evidence: kết quả truy vấn dữ liệu 2 bot trước/sau + ảnh 回答人数 2 bot |
| TC-DATACOUNT001-01 | Data | DATA-COUNT-001 | Bộ đếm 回答人数 | Boundary | auto | Tất cả | Xóa do payload thiếu trường giá trị khi bộ đếm đang lệch — 回答人数 không bị âm | - Mục 「TC38944_選択」 có 「回答人数」 hiển thị **`0人`** nhưng **vẫn còn bạn có giá trị sót** (dựng dữ liệu lệch: đặt bộ đếm về 0 trong khi bạn `TC38944_FRIEND_A` vẫn giữ giá trị `オプションA`)<br>- Đã ghi lại trạng thái bộ đếm và danh sách bạn còn giá trị | 1. Xác nhận 「回答人数」 của mục đang hiển thị `0人` và bạn `TC38944_FRIEND_A` vẫn có giá trị<br>2. Đăng nhập admin, chọn bot thử nghiệm để lấy phiên hợp lệ<br>3. Gửi POST tới luồng lưu một mục thông tin bạn bè cho `TC38944_FRIEND_A`, `infos` **bỏ hẳn khóa `value`**<br>4. Đọc mã trạng thái và nội dung phản hồi<br>5. Mở màn 「友だち情報管理」 đọc lại 「回答人数」 của mục<br>6. Kiểm tra bảng lưu giá trị của bạn ở mục đó<br>7. Mở màn danh sách câu trả lời của mục, đối chiếu số bạn hiển thị | `infos` cho mục 選択肢, không có khóa `value`, thực hiện khi bộ đếm đang = 0 | - Phản hồi thành công, không lỗi trong nhật ký<br>- Bản ghi giá trị của bạn bị xóa<br>- 「回答人数」 **giữ ở `0人`, KHÔNG xuống số âm**<br>- Màn danh sách câu trả lời hiển thị nhất quán với bộ đếm | | Lấp `Q8` · **dẫn từ `TC-FRI-187`** (kho FA-015 §19: 回答人数 đang = 0 nhưng bạn vẫn còn giá trị sót → xóa giá trị KHÔNG làm count âm) · Lý do cần TC: bản vá mở thêm một đường giảm bộ đếm, chạy được trên dữ liệu lệch sẵn có ở production · Đánh giá spec: Spec không ghi — đã hỏi Leader · Evidence: ảnh 回答人数 trước/sau + kết quả truy vấn bảng giá trị |
| TC-ENV003-01 | UI | ENV-003 | Phân quyền & môi trường | Normal | manual | product | Bộ TC lõi của bản vá chạy lại trên staging và smoke trên production | - Bản vá đã deploy lên **staging** (`staging.lme.jp`) và **production** (`step.lme.jp`)<br>- Đã dựng dữ liệu thử nghiệm tương đương môi trường local trên cả 2 nơi<br>- Trên production dùng **bot thử nghiệm nội bộ**, tuyệt đối không đụng dữ liệu khách | 1. Trên **staging**: chạy lại nhóm TC lõi — `TC-FUNC001-01` (chọn giá trị từ khung chat), `TC-DATAHIST001-01` (bỏ chọn 「-」), `TC-TOOLKNOW002-01` (payload thiếu `value`), `TC-DATADB001-01` (phạm vi xóa), `TC-MSGUSER001-01` (action chạy 1 lần)<br>2. Ghi nhận kết quả từng TC kèm bằng chứng<br>3. Trên **production**: chạy smoke `TC-FUNC001-01` + `TC-DATAHIST001-01` + `TC-MSGUSER001-01` trên bot thử nghiệm nội bộ<br>4. Với `TC-MSGUSER001-01`: kiểm tra tin nhắn action trên **LINE app thật của điện thoại**<br>5. Dọn sạch dữ liệu thử nghiệm đã tạo trên production | Cùng bộ dữ liệu đã dùng ở local, dựng lại trên staging và production | - Toàn bộ TC lõi cho kết quả **giống local** trên staging<br>- Smoke trên production: giá trị lưu/xóa đúng, 「回答人数」 đúng<br>- Tin nhắn action **thật sự đến LINE app trên điện thoại**, đúng 1 lần<br>- Không phát sinh lỗi trong nhật ký của cả 2 môi trường | | Lấp `Q5` · **RULE-08** — 17/17 TC hiện chỉ chạy `local`, bug gốc phát sinh ở production · **RULE-06** — bước 4 đi tới output cuối chuỗi trên thiết bị thật · Đánh giá spec: Spec ghi rõ · Evidence: bảng kết quả từng TC theo môi trường + ảnh màn hình LINE app |

**Dòng `GAP`/`RISK` không đề xuất TC mới:**
- **`G5`** (Dev khai sai data impact ở mục 4.2) — **không cần TC mới**: `TC-DATADB001-01` đã cover hành vi. Việc phải làm là **sửa `03-dev-impact.md` mục 4.2** cho đúng và tick checkbox verify (§4 #10), không phải viết thêm TC.

---

## 6. Spec update needed

| # | Chủ đề | Hiện trạng | Cần ai chốt |
|---|---|---|---|
| 1 | **Payload thiếu hẳn khóa `value` có tương đương thao tác bỏ chọn / xóa giá trị không?** | Sau bản vá, hành vi là **XÓA bản ghi**. Nhưng expected này mới chỉ chứng minh được từ implementation + kết quả run — **`BR-17` không phát biểu rõ**. Đây **trùng đúng** câu hỏi mở `MT-03` của kho FA-015: *"Phạm vi hành vi 'xóa trắng → xóa hẳn dòng, không để dòng rỗng': chỉ màn 友だち詳細 hay **mọi lối ghi giá trị**?"* — đang ở trạng thái ⏳ CHỜ QUYẾT ĐỊNH. Đang chặn `TC-DATADB001-01` (`spec_status = needs-human-review`). | **Leader + PO** — chốt xong thì cập nhật `BR-17` trong `spec-features/admin/chat-11/feature-spec.md` **và** đóng `MT-03` của kho FA-015 cùng lúc. |
| 2 | **Thông báo lỗi của luồng lưu danh sách hiển thị** | Chuỗi 「保存に失敗しました。入力内容をご確認ください。」 mới được thêm bởi commit `97033fb2cb` (Redmine #40213) nhưng **chưa có trong spec nào**. Không có nguồn để tester đối chiếu đúng/sai. | **Dev + Leader** — bổ sung vào spec `chat-11` phần thông báo của modal 「表示内容を変更」. |
| 3 | **Ranh giới `saveSettingDisplayInfoItem` (V1) vs `saveSettingDisplayInfoV2`** | Hai hàm cùng vùng code, cùng pattern đọc `->value`, nhưng **chỉ V1 được vá**. Spec không mô tả khi nào client gọi hàm nào, cũng không nói V2 có phải chấp nhận cùng dạng payload không. Thiếu cái này thì không kết luận được `TC-REGSHARED001-05` đạt hay không. | **Dev** — mô tả rõ hợp đồng của 2 endpoint, rồi Leader bổ sung vào spec. |
| 4 | **Giá trị `0` của mục kiểu `ポイント`** | Kho khẳng định `0` là giá trị hợp lệ khác "xóa" (`TC-FRI-199`, `TC-FRI-200`), nhưng spec `friend-information` **không ghi** quy tắc này, và `MT-11` của kho ("kiểu ポイント: ngưỡng là số nguyên hay thập phân?") vẫn ⏳ CHỜ QUYẾT ĐỊNH. | **Leader + PO** — chốt và ghi vào `BR-02` của `spec-features/admin/friend-information/feature-spec.md`. |
| 5 | **2 mục có thể ĐÓNG ngay (không cần chốt mới)** | `TC-STATEMATRIX001-02` ↔ kho `TC-FRI-189` và `TC-MSGUSER001-02` ↔ kho `TC-FRI-213` — expected của Studio **khớp hoàn toàn** với TC đã có trong kho. Không phải câu hỏi mở. | **Leader** — bỏ cờ `needs-human-review` của 2 TC này trên Studio, ghi nguồn là 2 ID kho tương ứng. |
