# 05 — Review Report

> Draft cho Leader verify. Bug ID + ngày nằm ở tên folder; vòng review nằm ở tên file.

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | **(1) MCP LME TEST STUDIO — task #245** |
| Vì sao không dùng nguồn ưu tiên cao hơn | N.A. — đã dùng nguồn 1 (ưu tiên cao nhất) |
| Ticket · task_id · round · branch | `33107` · `#245` · round `5` · `ai_fixbug_33107` |
| Thời điểm fetch | `2026-09-04` (`testcase_list` 38 TC + `task_get_context` + `task_get_report` + `review_list_comments`) |
| Tổng số TC review | **38** |
| Snapshot đã ghi | `04-tc-list.md` — **giữ nguyên** bản `/new-task` cùng phiên (header `<!-- source: MCP LME TEST STUDIO — task_id=245 ... -->`, cùng task #245 + cùng run #849, nội dung fetch trùng khớp) → không ghi đè |
| Đối chiếu chéo nguồn | **KHÔNG** — đã dừng ở nguồn 1 (Google Sheet / file 04 không được fetch, không đối chiếu) |
| Tester được review | **AI pipeline** — TC do `provenance.source = ai` (job #801) sinh; run #849 do `trangnq` bấm chạy nhưng `last_exec.source = ai` |
| Vòng review | Round 1 (Studio `reviewState = leader`, `reviewed = false`, `review_list_comments` = rỗng → chưa có vòng review nào trước) |
| **Nguồn spec đã dùng** | `spec-features/admin/message-template/feature-spec.md` (§5 Business Rules BR-01…BR-15, §6 Middleware/phân quyền dòng 777-786, §8 Cross-references dòng 813-836) · `spec-features/admin/bill-item/feature-spec.md` (§1.5 V1/V2 legacy, §1.6 本番/テスト, §5.1 BR-01…BR-15, §5.2 rule chỉ enforce ở client, §6 Middleware dòng 798-812 + EP-05/25/33/41/42/45, §10 R5/R6/R7/R12 dòng 1161-1168) · `kho-tcs/fa010-mautinnhan-テンプレート.md` (MT-48, MT-49) · `kho-tcs/fa026-billtienitem-商品販売.md` (MT-39) |

**Cảnh báo bắt buộc về chất lượng nguồn** — *Nguồn 1 (Studio)*:

| # | Chiều | Kết quả | Flag |
|---|---|---|---|
| 1 | Kết quả thực thi thật | **32/38 pass (84,2%)** · 2 `fail` · 4 `error` · 0 `skip` · 0 chưa chạy | `OK` (≥80%) — nhưng **6 TC không có kết luận test** |
| 2 | TC `fail`/`error` + TC gắn ticket bug | `fail`: **NEW-36**, **NEW-31** — **cả 2 KHÔNG có ticket bug đang mở**<br>`error`: NEW-32 · NEW-45 · NEW-46 · NEW-38<br>TC có ticket: NEW-2 (#40283) · NEW-4 (#40349) · NEW-22 (#40290, #40350) — cả 3 nay `pass` | **`[BLOCKER]`** — xem B-1 |
| 3 | Môi trường đã chạy — RULE-08 / ENV-003 | `PROD 0` · `STAGING 0` · `LOCAL 38` (8/8 run đều `env=local`).<br>`env_scope` khai báo: **0/38 TC** có `prd`; 22 TC `["local","dev","staging"]`, 16 TC `["all"]` | **`[MAJOR]`** — xem M-1 |
| 4 | Ai chạy | `ai / trangnq` = 38/38. `submittedWithoutMcp = false` | **`[MAJOR]`** — xem M-2 |
| 5 | Tác giả TC | `author = AI` 38/38 · `provenance = {source: ai, actor: AI, createdJobId: 801}` · `status = draft` 38/38 · `reviewed = false` | **`[MAJOR]`** — xem M-2 |
| 6 | Mã quan điểm KHÔNG có trong `checklist-lme.md` | **7 mã / 11 lượt TC (29%)**: `TOOL-ERRHYG-001`(3) · `AUTH-SESSION-001`(2) · `API-001`(2) · `TOOL-AXIS-001`(1) · `RULE-09`(1) · `API-CONTRACT-001`(1) · `TOOL-OLDREC-001`(1) | **`[MAJOR]`** — xem M-3 |

Bổ sung ngoài 6 mục bắt buộc:
- `spec_status = null` ở **38/38 TC** → không TC nào ghi "Spec ghi rõ / Spec không ghi / Đã hỏi leader" (xem M-4).
- **12 bug Studio** đã raise: 8 closed (#40283, #40284, #40285, #40287, #40288, #40290, #40349, #40350) · **4 rejected** — trong đó **2 bug severity High**: #40286 (remind lỗi 500 cho **cả chính chủ**) và **#40289 (chiếm quyền sở hữu item bản cũ — chính là mục 2 của ticket gốc)**, cả 2 reject với lý do "Đồng bộ từ Redmine: Reject" (xem M-5).
- Độ phủ spec Studio tự chấm: **0/55 covered · 10 partial · 45 none** (feature `template-v2`).

---

## 1. Verdict

- [ ] **APPROVED**
- [ ] **APPROVED WITH CHANGES**
- [x] **REJECTED**

**Lý do ngắn gọn**: 4 `[BLOCKER]` — (1) TC `NEW-36` **fail = rò rỉ nội dung cross-bot có thật, chưa raise ticket**; (2) mục **yokoten của Dev bị thiếu** trong khi spec liệt kê ≥5 endpoint cùng lớp lỗi chưa sửa; (3) **chưa rà dữ liệu lệch đã phát sinh trước fix** trên môi trường thật (RULE-04); (4) **0/38 TC được phép chạy production** trong khi nhánh chạm bill tiền (RULE-08).

---

## 2. Tóm tắt cho member

Bộ 38 TC này **chất lượng cao hơn mặt bằng chung**: phủ đủ 5 biến thể link tạo tin con (nhóm / kịch bản / remind / gửi một lần / đặt lịch trong chat) ở **cả 2 tầng màn và tầng ghi**, có đối chứng dương cho từng guard (chính chủ không bị chặn nhầm), và tự phát hiện thêm 2 trục tấn công mới mà ticket gốc không nêu (`template_child_id`, id mục thông tin khách hàng).

Ba điểm phải sửa trước khi chạy lại: (1) **TC `NEW-36` đang fail và đó là lỗ hổng thật** — API xem trước tin con vẫn trả nội dung của bot khác — phải raise ticket ngay, đừng để trong trạng thái fail không ticket; (2) **toàn bộ kết quả chỉ chạy trên `local`** nên chưa kết luận được gì cho nhánh item bán hàng (bill tiền) theo RULE-08; (3) **29% TC dùng mã quan điểm ngoài `checklist-lme.md`** khiến coverage không map được — chỉ cần đổi lại mã là 6 quan điểm ưu tiên Cao đủ 3 loại case ngay.

---

## 3. Coverage Matrix

| Impact | Loại | TC cover (suy luận) | # TC | Exec | Status |
|---|---|---|---|---|---|
| `BUG` — thiếu ownership check `bot_id` ở 2 tầng, 2 luồng | Root cause | NEW-2 (tái hiện bug mục 1) · NEW-59 (tái hiện bug mục 2) | 2 | 2/2 | `OK` |
| `F1` — `TemplateV2Controller::createTemplate` + `isTemplateGroupOwnedByBot` | Direct | NEW-39, NEW-2, NEW-32, NEW-43, NEW-4, NEW-44, NEW-45, NEW-46, NEW-47, NEW-48, NEW-49, NEW-50, NEW-51, NEW-52 | 14 | 11/14 | `RISK` — 3 TC không kết luận (NEW-32/45/46) |
| `F2` — `TemplateV2Service::save*` (5 hàm) + guard `isGroupOwnedByCurrentBot` | Direct | NEW-41, NEW-53, NEW-33, NEW-34, NEW-35, NEW-54, NEW-55, NEW-56, NEW-57 | 9 | 9/9 | `RISK` — **không có Boundary ở tầng ghi** (id `0` / `-11` / `-1111` / không tồn tại chỉ test ở tầng màn) |
| `F3` — `SalesManagementV2Controller::saveItem` + `ajaxGetItemDetail` | Direct | NEW-58, NEW-59, NEW-61, NEW-22, NEW-24, NEW-37, NEW-63, NEW-64 | 8 | 8/8 | `OK` |
| `F4` — `SalesManagementController` (V1 legacy) `saveItem` + `editItem` | Direct | NEW-65, NEW-66, NEW-29 | 3 | 3/3 | `RISK` — không có Boundary; **các endpoint V1 cùng lớp lỗi chưa test** (xem GAP-7) |
| `F5` — luồng tạo mới hợp lệ (id rỗng / âm / không tồn tại) mà guard **cố ý bỏ qua** | Indirect | NEW-4, NEW-43, NEW-22, NEW-46, NEW-49 (`-11`), NEW-52 (`-1111`), NEW-51 | 7 | 6/7 | `RISK` — NEW-46 `error` do **expected mâu thuẫn NEW-4** (xem §4.5 CONF-1) |
| `D1` — `template.bot_id` / `template.content` | Data | NEW-53, NEW-54, NEW-35, NEW-68 | 4 | 4/4 | `RISK` — **thiếu kiểm `WHERE` scope bằng bản ghi TRÙNG TÊN ở 2 bot** (DATA-DB-001 mục 1) |
| `D2` — `step_message.template_ids` (bảng **không có** `bot_id`) | Data | NEW-44, NEW-45, NEW-55 | 3 | 2/3 | `RISK` — chưa test **lệch cặp cha–con** (step của B + scenario của A) |
| `D3` — `event_step.templates_id` | Data | NEW-47, NEW-48, NEW-56 | 3 | 3/3 | `RISK` — chưa test lệch cặp `event_step` ↔ `event_id` |
| `D4` — `broadcast.template_ids` | Data | NEW-49, NEW-50, NEW-51, NEW-33, NEW-57 | 5 | 5/5 | `OK` |
| `D5` — `s_items.bot_id` (chiếm quyền sở hữu) | Data | NEW-29, NEW-63, NEW-58, NEW-24 | 4 | 4/4 | `OK` |
| `T1` — Message Template (FA-010) | Feature | 20 TC nhánh template | 20 | 16/20 | `RISK` |
| `T2` — Step Delivery / Scenario (FA-009) | Feature | NEW-44, NEW-45, NEW-46, NEW-55 | 4 | 2/4 | `RISK` |
| `T3` — Broadcast (FA-008) | Feature | NEW-49, NEW-50, NEW-51, NEW-57, NEW-33 | 5 | 5/5 | `RISK` — **không TC nào verify tin thật gửi tới LINE user** (RULE-06) |
| `T4` — Event Booking / remind (FA-021) | Feature | NEW-47, NEW-48, NEW-56 | 3 | 3/3 | `RISK` — bug #40286 (remind 500 cho **chính chủ**) bị reject, chưa có TC hồi quy riêng |
| `T5` — Single Product / Sales (FA-026) | Feature | NEW-58, NEW-59, NEW-61, NEW-22, NEW-24, NEW-37, NEW-63, NEW-64, NEW-65, NEW-66, NEW-29, NEW-31 | 12 | 11/12 | `RISK` — 0 TC production dù là nhánh **bill tiền** (RULE-08); 販売履歴 / hoàn tiền / hủy hợp đồng chưa rà (GAP-7) |
| **Dữ liệu lệch phát sinh TRƯỚC fix** (mục ■5 RECOVER DATA) | Data / recovery | NEW-68 | 1 | 1/1 | **`RISK`** — chỉ chạy `local`; RULE-04 đòi thống kê **toàn hệ thống** |
| **Ma trận tài khoản** (owner / staff 1 bot / staff nhiều bot / vừa owner vừa staff) | Phân quyền | — | 0 | — | **`GAP`** — xem GAP-1 |
| **Output cuối chuỗi** (tin lệch bot được job gửi tới LINE user của bot khác) | Output | — | 0 | — | **`GAP`** — xem GAP-6 |
| **Endpoint cùng lớp lỗi chưa sửa** (yokoten) | Regression | — | 0 | — | **`GAP`** — xem GAP-7 |

### ORPHAN TCs

| TC | Vấn đề | Đề xuất |
|---|---|---|
| `NEW-31` — *Smoke màn bán hàng sau khi **nhánh sửa mã dùng chung*** | **Tiền đề sai**: nhánh `ai_fixbug_33107` chỉ đổi **4 file PHP** (Dev đã verify bằng `git diff --stat`), KHÔNG đổi `public/js/common.js` / `public/js/sales/v2/list-order-history.js`. Note của TC ghi "mã dùng chung được nạp toàn site" — tiền đề kế thừa từ nhánh cũ `fix-bug-27083`. Lỗi console `Unexpected identifier 'label'` nhiều khả năng là nhiễu môi trường local, không do nhánh. | **Không xóa** — đổi lại tiền đề + phạm vi (smoke 2 màn đích của guard: danh sách mẫu tin + danh sách item), hoặc đổi mã sang `REG-RUN-001`. Đang là 1/3 TC gánh `REG-SHARED-001` → xóa sẽ làm hở quan điểm. |
| `NEW-38` — *Smoke màn danh sách mẫu tin và màn chat* | Cùng vấn đề tiền đề (note ghi rõ "Thay cho TC smoke của lượt trước — nhắm vào mã dùng chung của nhánh `fix-bug-27083`"). `error` do WebSocket `ws://127.0.0.1:3442` không chạy ở slot local. | Giữ, nhưng **loại bỏ tiêu chí "console không có lỗi script"** khỏi expected khi chạy local, hoặc chuyển sang `env_scope = staging`. |

> Không có TC nào nằm ngoài scope `BUG / F* / D* / T*` — tất cả 38 TC đều trace được về nhánh fix. `AP-5` (over-coverage tầng dưới) **không dính**.

---

## 3.5 Fix-shape analysis (adversarial)

**Fix shape nhận diện từ mục 2 `03-dev-impact.md`**: `"Thêm kiểm tra quyền sở hữu"` / `"tìm kèm điều kiện mã bot"` / `"câu lệnh cập nhật cũng thêm điều kiện mã bot"` → **shape "validate input / add check"**, kèm shape phụ **"sửa hàm dùng chung"** (`isGroupOwnedByCurrentBot` + `saveTemplateByType` dùng chung cho 5 luồng ghi; màn `createTemplate` dùng chung cho 5 biến thể link).

| Shape | Câu hỏi adversarial | Trả lời từ bộ TC | Kết luận |
|---|---|---|---|
| **validate input / add check** | Cover bao nhiêu input variant? | 5 biến thể link × 2 tầng = đủ 10 tổ hợp (NEW-39…NEW-57) | ✅ Đạt |
| | Test **server-side** hay chỉ frontend? | 14 TC gọi thẳng tầng ghi (`tc_group = api`) — NEW-53/54/55/56/57/63/64/24/33/34/35/37 | ✅ Đạt |
| | Đủ 5 pattern biên (biên / biên±1 / 0 / rỗng)? | Tầng **màn**: `999999999`, rỗng, `abc`, `1 or 1=1`, `あ`, `-11`, `-1111`, `zzzzzz`, mã item đã xóa → **đủ**.<br>Tầng **ghi**: **0 TC biên** — guard tầng service cũng "cố ý bỏ qua id rỗng/âm" nhưng chưa ai thử | ⚠️ **`[MAJOR]`** → GAP-9 |
| | Đủ luồng vào: create / edit / **copy** / import CSV / API? | create ✅ · edit ✅ (NEW-35) · **copy ❌** — `cloneTemplate` (BR-07 deep clone đệ quy) và copy item (kho `TC-BIL` mục 9) **không có TC nào**; kho `TC-TMT-383` đã từng đòi kiểm `cloneTemplate` | ⚠️ **`[MAJOR]`** → GAP-11 |
| **sửa hàm dùng chung (REG-SHARED-001)** | Có **danh sách nơi ảnh hưởng do DEV cung cấp**? | Có mục 3 (13 function) cho phần **đã sửa**. Nhưng mục 2 ghi *"Quét ngang còn **2 chỗ cùng kiểu chưa sửa** vì ngoài phạm vi ticket (**xem mục yokoten**)"* — **journal KHÔNG có mục yokoten**, 2 chỗ đó không được nêu tên | ❌ **`[BLOCKER]`** → B-2 |
| | TC test **từng nơi** trong danh sách? | 13/13 function của mục 3 đều có ≥1 TC | ✅ Đạt |
| | Chức năng **tương tự** đã rà? | Spec FA-010 §8 liệt kê 8 tính năng tham chiếu template (FA-001/003/004/007/008/009/013/015) — TC chỉ phủ 008/009/021 + chat 送信予約. Spec FA-026 §6 + §10 R5/R12 liệt kê **≥5 endpoint cùng lớp lỗi** chưa sửa | ❌ **`[BLOCKER]`** → B-2, GAP-7 |

**Symptom-only KH report check**: KH mô tả **cơ chế cụ thể** (dán link của bot B → template lưu sai `bot_id`), kèm 4 URL tái hiện — **KHÔNG phải symptom-only**. `AP-2` không dính.

**Anti-pattern**:

| AP | Dính? | Ghi chú |
|---|---|---|
| `AP-1` Single-trigger generic-fix | ❌ | Fix là specific ownership check, TC phủ 5 trigger khác nhau |
| `AP-2` Symptom-only KH report | ❌ | KH đã nêu cơ chế + 4 URL |
| `AP-3` Happy-path-only regression | ✅ **dính** | `T2` (FA-009) chỉ 4 TC, `T4` (FA-021) chỉ 3 TC, tất cả precondition đều là *"data sạch, bot A/bot B mới dựng"*. Không TC nào chạy trên **bản ghi đời cũ** (template không group, item `is_product_new=0` tạo trước version-up, remind loại cũ). → `[MAJOR] [AP-3]`, xem M-8 |
| `AP-4` Specific code-check disguised as generic catch | ⚠️ một phần | Mục "Commit / Pull Request" **không có PR link** (chỉ có branch + commit hash `822b9abcd9`) → reviewer không đọc được diff để xác nhận guard thực sự nằm trong query hay chỉ là `if` trước query. → `[MAJOR]`, xem M-9 |
| `AP-5` Layer-downstream over-coverage | ❌ | Mọi TC đều trace về 4 file đã sửa |
| `AP-6` Mục 3 dev-impact trống | ❌ | Mục 3 có đủ 13 function |

---

## 3.6 Bảng quan điểm đối chiếu

> Nguồn: `framework/checklist-lme.index.md` (tầng 1) + `framework/catalog-lme.index.md` §2 (tầng 2).
> ⚠️ Theo mục 0.6 #6: TC mang mã **ngoài** `checklist-lme.md` **không được tính là cover**. Cột "TC cover" ghi cả TC mang mã lạ trong ngoặc `(…)` để Leader thấy nội dung có hay không.

| Mã quan điểm | Ưu tiên | Trigger khớp task? | TC cover (suy luận) | Exec | Kết luận |
|---|---|---|---|---|---|
| `PERM-002` — không bypass quyền bằng API / URL | Cao | ◯ — bypass bằng URL + gọi thẳng API là **đúng bản chất bug** (catalog `MAP-PERM-02`, `MAP-PERM-03`) | NEW-32, NEW-34, NEW-54, NEW-55, NEW-56, NEW-57, NEW-63 — **7 TC, 100% Abnormal** | 6/7 | ⚠️ `[MAJOR] RULE-01` — thiếu Normal + Boundary mang mã này (Normal thực chất có ở NEW-53/24/33 nhưng mang mã `API-001`/`API-CONTRACT-001`) |
| `PERM-003` — cách ly dữ liệu đa tài khoản LINE OA / **change bot** | Cao | ◯ **BẮT BUỘC** — tổ chức vận hành nhiều OA + có chức năng change bot (catalog `MAP-PERM-01`, `MAP-PERM-03`) | *(NEW-41, NEW-61 — mã `AUTH-SESSION-001`, ngoài checklist)* | 2/2 | ⚠️ **Chưa tính cover.** Nội dung "đổi bot giữa chừng rồi ghi" đã có; **thiếu hẳn** chiều "sau change bot, **danh sách/dữ liệu** của 2 bot độc lập" và **ma trận tài khoản** `MAP-PERM-01` → xem M-3 + GAP-1 |
| `PERM-001` — menu/nút hiện đúng theo role | Cao | ◯ — spec FA-010:807 ghi rõ *"Staff permission: kiểm tra ở middleware level (**không thấy trong controller**) [Trung bình]"*; guard mới dựa `getBotId()` từ session | **không có TC nào** | — | ❌ **GAP** → GAP-1 (kho đã có `TC-TMT-390`, `TC-BIL-374/375` — dẫn chiếu) |
| `SEC-001` — chặn truy cập trái phép PII | Cao | ◯ **BẮT BUỘC** — đọc chéo nội dung tin / item / mục thông tin khách hàng | NEW-2, NEW-45, NEW-48, NEW-50, NEW-59, NEW-64, NEW-66, NEW-36 — **8 TC, 100% Abnormal** | 5/8 | ⚠️ `[MAJOR] RULE-01` (thiếu Normal/Boundary) + **NEW-36 fail = lỗ hổng thật** → B-1 |
| `DATA-DB-001` ★ — xác minh tầng DB (`WHERE` scope + khóa mồ côi) | Cao | ◯ **BẮT BUỘC** — fix có UPDATE (`s_items`, `template`, danh sách `template_ids`) | NEW-35, NEW-37, NEW-29 — **3 TC, 100% Abnormal** | 3/3 | ⚠️ `[MAJOR]` — **thiếu mục (1) của quan điểm**: chưa tạo bản ghi **TRÙNG TÊN ở 2 bot**; **thiếu mục (3)**: khóa mồ côi khi xóa hết bản ghi con → GAP-2 |
| `DATA-REF-001` — reference integrity (xóa / copy / rename / move) | Cao | ◯ **BẮT BUỘC** — tin con được `step_message` / `broadcast` / `event_step` tham chiếu; BR-05/BR-06/BR-15 (FA-010) có cascade delete + propagation | **không có TC nào** | — | ❌ **GAP** → GAP-3 |
| `REG-SHARED-001` — shared code / logic | Cao | ◯ **BẮT BUỘC** — guard dùng chung 5 luồng ghi + màn dùng chung 5 biến thể | NEW-52, NEW-31, NEW-38 — **3 TC, 100% Normal**, 2/3 không kết luận, 2/3 sai tiền đề (xem ORPHAN) | 1/3 | ❌ **`[BLOCKER]`** — thiếu danh sách yokoten → B-2 |
| `REG-RUN-001` — dữ liệu / job đang chạy dở khi release | Cao | ◯ **BẮT BUỘC** — có broadcast đặt lịch + step delivery đang chạy | **không có TC nào** | — | ❌ **GAP** → GAP-6 |
| `COMPAT-LEGACY-001` ★ — dữ liệu / định dạng đời cũ chạy song song | Cao | ◯ **BẮT BUỘC** — chạm **template** + **remind** + màn item **bản cũ** (`is_product_new = 0`); RULE-09 | *(NEW-65 — mã `RULE-09`, không phải mã quan điểm)*, NEW-66, NEW-29 phủ **item** V1 | 3/3 | ⚠️ Chưa tính cover đúng mã; **thiếu hẳn** cặp *template không group ⇄ có group* và *bản ghi tạo trước version-up* → M-3 + GAP-8 |
| `ENV-003` ★ — khác biệt dev / staging / production | Cao | ◯ **BẮT BUỘC** — chạm **bill tiền** (`s_items`) + job gửi tin; catalog `ENV-LB` (production 2 server), `ENV-DOMAIN` | **0 TC chạy production**, `0/38` TC có `env_scope` chứa `prd` | 0 | ❌ **`[BLOCKER]`** RULE-08 → B-4 |
| `FUNC-001` — luồng chính hoàn tất đúng đặc tả | Cao | ◯ | NEW-39, NEW-44, NEW-49, NEW-58 — **4 TC, 100% Normal** | 4/4 | ⚠️ `[MAJOR] RULE-01` (thiếu Abnormal + Boundary mang mã này) |
| `FUNC-004` — giới hạn trên/dưới, số lượng | Cao | ◯ — id âm/`0`/`999999999`/rỗng là vùng biên guard **cố ý bỏ qua** | NEW-4, NEW-46 — **2 TC, 100% Boundary** | 1/2 | ⚠️ `[MAJOR] RULE-01` + expected 2 TC **mâu thuẫn nhau** → §4.5 CONF-1 |
| `SEC-ISO-001` — cách ly dữ liệu đa phiên | Cao | ◯ — 2 tab cùng user, đổi bot ở tab khác | *(NEW-41, NEW-61 — mã `AUTH-SESSION-001`)* | 2/2 | ⚠️ Chưa tính cover đúng mã; thiếu chiều "mở nhiều đối tượng, **tải nhanh liên tiếp**" → M-3 |
| `CONC-001` — đồng thời / idempotency | Cao | △ — fix không thêm nút thực thi mới, không có limit dùng chung | — | — | Không trigger. Lý do: guard là read-check trước ghi, không đổi cơ chế khoá |
| `DATA-AUDIT-001` — audit log / lịch sử thao tác | Cao | △ | — | — | Không trigger: FA-010 và FA-026 **không có màn `操作履歴`** trong spec → không có chuẩn để đối chiếu. *(Không nâng flag — tránh bịa)* |
| `DEPLOY-LIVE-001` ★ — release không lock maintain | Cao | △ | *(NEW-41, NEW-61 gần đúng)* | 2/2 | Không nâng flag: fix **không đổi payload API / field form / cấu trúc request**, chỉ thêm điều kiện phía server |
| `MSG-004` / `RULE-06` — output cuối chuỗi trên LINE app | Cao | ◯ — tin con lệch bot nằm trong broadcast/step **sẽ được job gửi thật** (chính note của NEW-57 nêu hậu quả này) | **không có TC nào** — mọi Expected dừng ở màn admin + DB | — | ❌ **GAP** → GAP-6, M-6 |
| `LIST-001` — màn danh sách có search/filter/pagination | Trung bình | ◯ — NEW-31 có kiểm phân trang lịch sử đơn hàng | NEW-31 | 0/1 | ⚠️ TC duy nhất đang `fail` |
| `DATA-MIG-001` · `DATA-BACKUP-001` · `PAY-*` · `INTG-LINE-001` · `JOB-001` · `BULK-001` · `MEDIA-*` | — | ✕ | — | — | Không trigger: fix không đổi schema, không thêm bảng, không chạm thanh toán / LINE API / job / media |

**Tóm tắt quan điểm thiếu**: `PERM-001` (GAP) · `DATA-REF-001` (GAP) · `REG-RUN-001` (GAP) · `MSG-004`/RULE-06 (GAP) · `ENV-003` (BLOCKER) · `REG-SHARED-001` (BLOCKER — thiếu yokoten) · `PERM-003`, `COMPAT-LEGACY-001`, `SEC-ISO-001` (có nội dung nhưng mang mã ngoài checklist → chưa tính cover).

> ⚠️ **Ghi chú minh bạch về mức severity — Leader cân nhắc lại nếu không đồng ý.**
> Theo đúng chữ nghĩa BƯỚC 3 (*"Trigger khớp nhưng không TC nào cover → `[BLOCKER]` nếu ưu tiên Cao"*), **7 quan điểm** dưới đây đáng lẽ đều là `[BLOCKER]`: `PERM-001`, `PERM-003`, `DATA-REF-001`, `REG-RUN-001`, `MSG-004`, `COMPAT-LEGACY-001`, `SEC-ISO-001`. Tôi **hạ 5 trong số đó xuống `[MAJOR]`** vì lý do cụ thể sau, chứ không phải vì đánh giá rủi ro thấp:
> - `PERM-003`, `SEC-ISO-001`, `COMPAT-LEGACY-001` — **nội dung đã có TC** (`NEW-41`, `NEW-61`, `NEW-65`) và đã `pass`; cái thiếu là **mã quan điểm gán sai**, sửa bằng remap ở `M-3` là gần đủ.
> - `PERM-001`, `DATA-REF-001` — GAP thật nhưng **kho TCs đã có TC dùng lại được** (`TC-TMT-390`) / đã có business rule rõ trong spec (BR-05/06/15) nên chi phí lấp thấp, không chặn merge nếu Leader chấp nhận chạy bổ sung ở vòng sau.
>
> **2 quan điểm tôi giữ nguyên ở mức chặn** là `ENV-003` (B-4) và `REG-SHARED-001` (B-2), cộng với `REG-RUN-001` + `MSG-004` được gộp vào `B-3` (dữ liệu lệch tồn đọng + job gửi thật) — đây là 3 nhóm mà **không có cách nào biết kết quả nếu không chạy**, nên không hạ mức.
> Nếu Leader muốn giữ đúng chữ nghĩa quy tắc → nâng `M-6`, `M-8`, `M-12` và 2 GAP `DATA-REF-001` / `PERM-003` lên `[BLOCKER]`; danh sách TC bổ sung ở §5 **không đổi** trong cả 2 trường hợp.

---

## 4. Issues phát hiện

### 4.1 Blocker (phải fix trước khi merge)

**`[BLOCKER] B-1` — `NEW-36` fail = rò rỉ nội dung cross-bot CÓ THẬT, chưa raise ticket**
Run #849 ghi actual: phiên **Bot A** gọi chức năng xem trước tin con `id=9901059` của **Bot B** → **HTTP 200**, body trả nguyên `{"success":true,"item":{"id":9901059,"bot_id":990434679,"name":"TC33107_B_グループ_849_15019_子1","content":"TC33107 tin con goc"…}}`. `bug_tickets` của TC = **rỗng**, không có bug Studio nào cho `tc_id = 15019`.
Đây khớp **`REQ-016`** của Studio (*"Chức năng xem trước tin nhắn con cũng phải giới hạn theo bot (**yokoten RK-01**)"*) và khớp `MT-48` trong `kho-tcs/fa010` (*"kiểm tra chủ sở hữu ở **TẤT CẢ** endpoint đọc/ghi template, không chỉ `store()`"*).
→ **Đề xuất fix**: raise ticket Redmine severity **High** ngay (cùng lớp #40283/#40287/#40288 đã closed), gắn vào `bug_tickets` của `NEW-36`; **không được** đóng #33107 khi TC này còn fail. Đồng thời hỏi Dev: đây có phải 1 trong "2 chỗ yokoten chưa sửa" không.
*Kèm theo*: `NEW-31` cũng `fail` mà không có ticket đang mở (bug Studio #772 đã **rejected**, không có `redmine_id`) — nhưng tiền đề TC sai (xem ORPHAN) nên xử lý bằng sửa TC, không raise bug.

**`[BLOCKER] B-2` FIX-SHAPE — mục **yokoten** của Dev bị thiếu, trong khi spec liệt kê ≥5 endpoint cùng lớp lỗi chưa sửa**
Mục 2 `03-dev-impact.md` viết *"Quét ngang còn **2 chỗ cùng kiểu chưa sửa** vì ngoài phạm vi ticket (**xem mục yokoten**)"* nhưng journal Redmine **không có mục yokoten** → `REG-SHARED-001` không có danh sách nơi ảnh hưởng đầy đủ.
Đối chiếu `spec-features/admin/bill-item/feature-spec.md`, các endpoint **cùng đúng lớp lỗi** (thao tác theo id trần, không lọc `bot_id`) mà nhánh fix **không chạm**:

| Endpoint | Vị trí spec | Rủi ro |
|---|---|---|
| `EP-05 GET /basic/sales/order-history-detail/{hashId}` — `@orderHistoryDetail:1099` | `:296`, `:824`, R12 (`:1168`) | **IDOR** — không lọc `bot_id` |
| `EP-25 POST /ajax/cancel-order-v2` — `@cancelOrderV2:3192` | `:279`, `:844`, R5 (`:1161`) | **KHÔNG có middleware auth** → huỷ/hoàn tiền đơn của bot khác |
| `EP-33 GET /basic/list-item/change-valid-item` — `@changeValidItem:562` | `:857`, R12 | không lọc `bot_id` (V1) |
| `EP-45 POST /ajax/sort-items` — `@sortItems:523` | `:866`, R12 | không lọc `bot_id` (V1) |
| `EP-41/EP-42 .../edit/{id}` — `@editOrder:1187`, `@editCycleOrder:1229` | `:864`, R12 | **IDOR** |
| V1 `saveOrder`/`saveCycleOrder` (`:992`, `:1212`, `:1260`), `cancelOrder` (`:1389`) | R12 (`:1168`) | update không lọc `bot_id` |

Cộng thêm `cloneTemplate` (BR-07 deep clone đệ quy — kho `TC-TMT-383` đã từng đòi kiểm) và `previewTemplate` (đã fail ở B-1).
→ **Đề xuất fix**: (1) yêu cầu Dev bổ sung **mục yokoten đầy đủ** vào Redmine, nêu đích danh 2 chỗ; (2) đối chiếu với bảng trên, chốt chỗ nào trong/ngoài phạm vi #33107 và mở ticket riêng cho phần ngoài; (3) bổ sung TC ở **GAP-7**. Theo quy tắc dự án: bug quyền ở tầng API phải rà **tất cả màn sibling cùng lưới phân quyền ở TẦNG API**, không chỉ 2 màn ticket nêu.

**`[BLOCKER] B-3` RULE-04 — dữ liệu lệch phát sinh TRƯỚC fix chưa được rà trên môi trường thật**
Mục ■5 của Dev ghi rõ *"Fix chỉ chặn phát sinh mới… Chưa dump được vì dev DB không kết nối được"*. TC duy nhất phủ việc này là **`NEW-68`** (`REQ-012`) nhưng `env_scope = ["local","dev","staging"]` và đã chạy trên **`local`** — tức là rà trên DB test do chính runner dựng, **không phải phạm vi thật**.
RULE-04: *"Bug làm sai dữ liệu → trước khi đóng ticket phải có query/thống kê **toàn hệ thống** xác nhận phạm vi ảnh hưởng"*. Bug tồn tại từ **2025-12-12** → dữ liệu lệch (`template.bot_id` ≠ bot của nhóm cha; `s_items.bot_id` bị đổi qua màn V1) rất có thể **đã có trên production**.
→ **Đề xuất fix**: nâng `NEW-68` lên `env_scope` gồm `prd`, chạy trên production/staging thật, xuất bảng bản ghi lệch kèm `id` + thời điểm; **bản ghi lệch phát sinh SAU khi deploy phải = 0**. Bổ sung `GAP-5`. Nếu tìm thấy bản ghi lệch → cần ticket recover data riêng trước khi đóng #33107.

**`[BLOCKER] B-4` RULE-08 / `ENV-003` — 0/38 TC được phép chạy production, trong khi nhánh chạm bill tiền**
8/8 run đều `env = local`; `env_scope` của 38/38 TC **không TC nào** chứa `prd` (22 TC `["local","dev","staging"]`, 16 TC `["all"]` — mà `all` trong Studio là `["dev","local","prd","staging"]`, nhưng thực tế **chưa chạy lần nào** trên prd).
Nhánh sửa `SalesManagementV2Controller` + `SalesManagementController` = **商品販売 / bill tiền** (`s_items.bot_id`, `s_items` liên quan `flag_environment` 本番/テスト, khóa Stripe/UnivaPay tách theo môi trường — spec FA-026 §1.6). Catalog `ENV-LB`: production có **2 server**, *"lỗi chỉ xuất hiện ở một server; session không đồng bộ"* — mà guard mới đọc **bot đang chọn từ session**.
→ **Đề xuất fix**: bổ sung `GAP-4` — chạy bộ cross-bot cốt lõi trên **production** (tối thiểu: mở màn sửa item bot khác, lưu đè item bot khác, lưu tin con vào nhóm bot khác), ghi lại **router id** và lặp trên **từng server** theo `ENV-LB`.

### 4.2 Major (nên fix)

**`[MAJOR] M-1`** — Kết quả "Đạt" của **toàn bộ nhóm rủi ro cao chỉ đến từ `local`**: `PROD 0 / STAGING 0 / LOCAL 38`. Ngay cả nhánh template (job gửi tin) và nhánh item (bill tiền) đều chưa có 1 lượt chạy nào ngoài local. → Ít nhất phải chạy lại trên **staging** cho nhánh template và **production** cho nhánh item (B-4).

**`[MAJOR] M-2`** — TC **do AI sinh 100%** (`author = AI`, `provenance.source = ai`, job #801), **`status = draft` 38/38**, `reviewed = false`, `reviewState = leader`; kết quả cũng **do AI runner chấm** (`last_exec.source = ai` 38/38, `by = trangnq` chỉ là người bấm chạy). Với task **phân quyền / bảo mật / tiền**, kết luận "Đạt" hoàn toàn tự động là chưa đủ. → Yêu cầu QA người **chạy tay lại** tối thiểu: NEW-2, NEW-29, NEW-36, NEW-54, NEW-63 và đính evidence thật (RULE-02).

**`[MAJOR] M-3`** — **11/38 TC (29%) mang 7 mã quan điểm ngoài `framework/checklist-lme.md`** → coverage không map được, và hệ quả là **6 quan điểm ưu tiên Cao đều "thiếu loại case"** dù nội dung thực tế đã có. Đề nghị remap:

| Mã Studio hiện tại | TC | Đề nghị remap sang | Lý do |
|---|---|---|---|
| `AUTH-SESSION-001` | NEW-41, NEW-61 | **`PERM-003`** (+ phụ `SEC-ISO-001`) | Đúng nội dung "change bot / đa phiên" của PERM-003 |
| `API-001` | NEW-53, NEW-24 | **`FUNC-001`** | Đối chứng dương luồng chính ở tầng ghi |
| `API-CONTRACT-001` | NEW-33 | **`FUNC-001`** | Như trên |
| `TOOL-ERRHYG-001` | NEW-43, NEW-47, NEW-22 | **`FUNC-004`** (NEW-43, NEW-22) · **`FUNC-001`** (NEW-47) | NEW-43/22 là biên input; NEW-47 là luồng chính chính chủ |
| `TOOL-AXIS-001` | NEW-51 | **`PERM-002`** | Bypass bằng URL thiếu tham số |
| `RULE-09` | NEW-65 | **`COMPAT-LEGACY-001`** | `RULE-09` là **rule**, không phải mã quan điểm |
| `TOOL-OLDREC-001` | NEW-68 | **`DATA-DB-001`** | Rà `WHERE` scope ở tầng DB |

Sau remap, `RULE-01` của `PERM-002` / `SEC-001` / `FUNC-001` / `FUNC-004` / `COMPAT-LEGACY-001` / `DATA-DB-001` gần như đủ 3 loại case — chỉ còn thiếu Boundary ở tầng ghi (GAP-9).

**`[MAJOR] M-4`** — **`spec_status = null` ở 38/38 TC**. Không TC nào ghi `Spec ghi rõ` / `Spec không ghi (đã hỏi ai)`. Nguy hiểm nhất ở các TC mà expected **được TC tự suy diễn**: NEW-4 và NEW-46 (id không tồn tại), NEW-2/NEW-32/NEW-43 (chặn bằng 404 hay redirect). Trong khi đó spec **có** rule liên quan: FA-010 `BR-03` (`botIdCurrent == getBotId()` → thông báo 「別のアカウントに切り替えたので...」) áp cho NEW-41; FA-026 `BR-15`/`R6` (`saveItem` không có validator server, `/ajax/*` miễn CSRF) áp cho NEW-24/NEW-63. → Điền `Trạng thái đánh giá spec` cho từng TC, và với các TC "Spec không ghi" phải ghi **đã hỏi ai**.

**`[MAJOR] M-5`** — **2 bug severity High bị reject không rõ căn cứ kỹ thuật**: **#40289** *"Lưu item bản cũ của bot khác làm ĐỔI cột bot (chiếm quyền sở hữu)"* — đây **chính là mục 2 của ticket gốc #33107**; và **#40286** *"Màn soạn tin biến thể remind trả lỗi 500 `Unknown column templates_ids` cho **cả chính chủ**"*. Cả 2 chỉ có lý do `"Đồng bộ từ Redmine: Reject"`. → Leader xác nhận lại: reject vì đã fix trong nhánh này (thì TC `NEW-29`/`NEW-47` pass là bằng chứng, nên ghi vào lý do), hay reject nhầm.

**`[MAJOR] M-6` RULE-06** — **không TC nào đi tới output cuối chuỗi**. Mọi Expected dừng ở màn admin + DB. Chính note của `NEW-57` viết *"hậu quả nếu lọt: tin của bot A được gửi tới **đúng khách hàng của bot B**"* — nhưng không TC nào verify tin thật trên LINE app. Kết hợp với B-3 (dữ liệu lệch cũ vẫn còn) thì rủi ro là **có thật, đang sống**: broadcast/step của bot B đã chứa tin con của bot A từ trước fix → job vẫn gửi. → GAP-6.

**`[MAJOR] M-7` chất lượng TC #4 — Expected dạng "A **hoặc** B", không đo lường được**: `NEW-2` (*"trả trang 404 **(hoặc điều hướng về danh sách)**"*), `NEW-4` (*"trả trang 404 **hoặc** điều hướng"*), `NEW-32` (*"bị chặn… **hoặc** mở màn soạn tin RỖNG"*), `NEW-43` (*"màn mở theo luồng tạo tin mới **hoặc** điều hướng về danh sách"*). Bug Studio **#775 bị reject với đúng lý do này** (*"redirect thay cho 404"*) → chứng minh sự mơ hồ đang gây tranh cãi thật. → Chốt **một** hành vi chuẩn (xem §6 SPEC-1) rồi sửa Expected thành giá trị đơn.

**`[MAJOR] M-8` `[AP-3]` Happy-path-only regression** — `T2` (FA-009) và `T4` (FA-021) mỗi cái chỉ 3–4 TC, precondition đều là dữ liệu mới dựng (`TC33107_A_*`, `TC33107_B_*`). Không TC nào chạy trên **bản ghi đời cũ**: template **không có group**, item `is_product_new = 0` tạo trước version-up, remind **loại cũ** (catalog `MAP-SEND-03`/`MAP-SEND-04` — *"cả hai loại remind cùng tồn tại, phải test cả hai"*). → GAP-8.

**`[MAJOR] M-9` `[AP-4]`** — **Không có PR link** (mục "Commit / Pull Request" = `<chưa có PR link>`, chỉ có `ai_fixbug_33107` + commit `822b9abcd9`). Reviewer không xác nhận được guard nằm **trong query** (`->where('bot_id', …)`) hay chỉ là `if` đứng trước query — khác biệt này quyết định có race giữa check và ghi hay không. Mục ■6 của Dev ghi mức verify mới là **`lint`**, *"chưa chạy được runtime/dev DB"*. → Yêu cầu Dev cung cấp PR/diff link + tự chạy lại ở mức ≥ smoke runtime.

**`[MAJOR] M-10` chất lượng TC #3 — 14 TC tầng API không nêu cách gọi trong cột `Các bước thực hiện`**. Steps chỉ ghi *"Gọi chức năng lưu tin nhắn con với…"*; endpoint thật (`POST /basic/ajax/sales/save-item`, `POST /ajax/template-v2/save-template`) chỉ nằm ở trường `note`. `exec_mode = auto` nên runner chạy được, nhưng **QA người chạy tay lại (M-2) sẽ không dựng được**. → Đưa endpoint + cách lấy payload/cookie vào Steps.

**`[MAJOR] M-11` chất lượng TC #8 — tỷ lệ Boundary quá thấp**: Normal 13 : Abnormal 22 : **Boundary 3** (8%). Với task mà guard **cố ý không chặn** id rỗng / âm / không tồn tại, vùng biên chính là chỗ dễ sai nhất — và thực tế 2/3 TC Boundary đang không kết luận được (NEW-4 có ticket, NEW-46 `error`). → GAP-9.

**`[MAJOR] M-12` chất lượng TC #10 — không có TC cho role staff**. Task là bài toán phân quyền nhưng toàn bộ 38 TC chạy bằng **1 tài khoản admin quản lý 2 bot**. Catalog `MAP-PERM-01` đòi 4 tổ hợp: chỉ owner · staff 1 bot · **staff nhiều bot** · **vừa owner vừa staff của bot khác**. Kho `fa010` `MT-49` và `fa026` `MT-39` đều đang **⏳ CHỜ QUYẾT ĐỊNH** đúng vùng này. → GAP-1.

### 4.3 Minor (có thể fix sau)

- **`[MINOR] N-1`** `NEW-35` không atomic: 1 TC gộp 2 lời gọi khác mục đích (ghi đè **nội dung** tin con + đổi **tên/thư mục**). Tách thành 2 TC để khi fail biết ngay nhánh nào hở.
- **`[MINOR] N-2`** `NEW-22` gộp 3 input với **3 expected khác nhau** (mã rác → 404 · mã item đã xóa → 404 · mã rỗng → mở form thêm mới). Chấp nhận được cho bộ biên nhưng nên tách case "mã rỗng" ra vì nó là **luồng Normal**, không phải Boundary.
- **`[MINOR] N-3`** `NEW-64` gộp 3 lời gọi (2 endpoint × id của B, rồi 1 lần đối chứng id của A). Giữ nguyên được, nhưng nên ghi rõ tiêu chí Đạt cho **từng** lời gọi.
- **`[MINOR] N-4` RULE-02** — cột `Evidence thực tế` trống ở 38/38 TC (Studio `testcase_list` không trả trường này; report có `artifacts` screenshot nhưng chưa gắn vào TC). Khi QA chạy tay lại phải đính đúng loại evidence: `SEC-001` → *kết quả thử truy cập chéo (URL/API/export) đều bị chặn*; `DATA-DB-001` → *ảnh chụp query trước/sau kèm câu query trên **2 tài khoản***; `PERM-003` → *bằng chứng dữ liệu tách biệt sau change bot*.
- **`[MINOR] N-5`** `priority = null` ở 38/38 TC → không phân biệt được TC nào bắt buộc chạy mỗi vòng regression (RULE-12 phần 3).

### 4.4 Nit (gợi ý)

- **`[NIT] G-1`** `TC No.` của Studio dùng `temp_id` dạng `NEW-2`, `NEW-68` — không theo quy ước repo `TC-<mã quan điểm bỏ gạch>-<nn>`. Snapshot `04-tc-list.md` đã tự sinh đúng quy ước (`TC-SEC001-01`…), chỉ cần đồng bộ ngược lên Studio nếu Leader muốn.
- **`[NIT] G-2`** `NEW-4` note ghi *"ghi nhận đúng hành vi quan sát được để leader đối chiếu với bản base"* — TC dạng "quan sát rồi báo cáo" không có tiêu chí Đạt/Không đạt rõ. Sau khi chốt SPEC-1 thì viết lại thành expected cứng.
- **`[NIT] G-3`** Theo `RULE-11`, các mục `⏳ CHỜ QUYẾT ĐỊNH` trong kho (`MT-48`, `MT-49`, `MT-39`) chưa phải bằng chứng ticket Closed → chỉ nêu ở mức gợi ý; ticket này là cơ hội tốt để **đóng `MT-39`** vì đã có TC tầng API cho bill-item (NEW-24/63/64/37) — xem §6.

---

## 4.5 TC trùng lặp nội dung

Đã rà **38/38 TC** theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`).

| Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| Chặn mở màn soạn tin biến thể **gửi một lần** với id bản tin của bot khác | **`NEW-51`** (có đối chứng dương: lặp lại với id bản tin của chính Bot A) | `NEW-50` → **GỘP**, không xóa | `DUP-SUBSET` | Quan điểm: `SEC-001` ⇄ `TOOL-AXIS-001` (sau remap M-3 cả 2 thành `PERM-002`/`SEC-001`) · Loại case: **Abnormal** cả 2 · Đối tượng+thao tác: **mở màn soạn tin, biến thể sendAll, id bản tin của Bot B** · Tiền đề: **tương đương** (đã ghi id bản tin của B, phiên đang chọn A) · Expected: **tương đương** (chặn + điều hướng về danh sách mẫu tin, không nạp nội dung).<br>Khác biệt duy nhất: `NEW-50` **giữ** `template_group_id=-11`, `NEW-51` **bỏ** tham số đó. | `[MINOR]` |
| **Kiểm tra gate trước khi đề nghị**: bỏ `NEW-50` khỏi tập → `REQ-004` vẫn còn `NEW-51` + `NEW-57`; `SEC-001` vẫn còn 7 TC; `D4` vẫn còn 4 TC → **không mất cover**. Nhưng biến thể URL *có* `-11` là hằng số quy ước thật do UI sinh, khác về mặt kỹ thuật → **đề xuất GỘP** (thêm 1 bước "lặp lại với URL giữ nguyên `-11`" vào `NEW-51`), **không xóa**. | | | | | |

**Trùng dạng mâu thuẫn (không xóa — cần Leader chốt spec):**

| Nhóm | TC A | TC B | Loại | Mâu thuẫn |
|---|---|---|---|---|
| `CONF-1` — id **không tồn tại** truyền qua URL màn soạn tin | **`NEW-4`** (`FUNC-004`/Boundary): id nhóm `999999999` → *"trả trang **404 hoặc điều hướng** về danh sách mẫu tin"* | **`NEW-46`** (`FUNC-004`/Boundary): id bước kịch bản `999999999` → *"màn soạn tin **mở ở trạng thái tạo mới**, **không bị điều hướng** đi nơi khác"* | `DUP-CONFLICT` | Cùng loại input (id không tồn tại), cùng màn, cùng loại case — **expected ngược nhau**. Runner tự triage: *"KHÔNG CHẤM ĐƯỢC — mong đợi của testcase xung đột với nguồn khác"*; app thực tế **chặn**. Bug #40349 (closed) đứng về phía "phải chặn"; bug #775 (rejected) lại tranh cãi 404 ⇄ redirect. → **`[MAJOR]`**, đưa lên §6 `SPEC-1`. |

**Không phát hiện `DUP-EXACT` và `DUP-INFLATE`.** Các cặp trông giống nhau đều tách đúng **2 tầng** (tầng màn `createTemplate` ⇄ tầng ghi `save*`) hoặc **5 biến thể entry** khác nhau — đây là thiết kế đúng, không phải trùng:
`NEW-45` ⇄ `NEW-55` (kịch bản) · `NEW-48` ⇄ `NEW-56` (remind) · `NEW-50` ⇄ `NEW-57` (gửi một lần) · `NEW-2` ⇄ `NEW-54` (nhóm mẫu tin) · `NEW-59` ⇄ `NEW-63` (item bản mới) · `NEW-66` ⇄ `NEW-29` (item bản cũ) · `NEW-39`/`NEW-44`/`NEW-47`/`NEW-49`/`NEW-52` (5 đối chứng dương, 5 entry khác nhau).

---

## 5. TCs đề xuất bổ sung

**Đã đối chiếu kho TCs trước khi viết** (BƯỚC 5a):
- `kho-tcs/fa010-mautinnhan-テンプレート.md` — vùng regression: nhóm 「Phân quyền & môi trường」(6 TC), 「Recover dữ liệu lỗi」(8 TC), 「Template legacy & tương thích」(9 TC). Mục mở liên quan: **`MT-48`** (kiểm chủ sở hữu ở TẤT CẢ endpoint đọc/ghi template — trùng B-1/B-2), **`MT-49`** (phân quyền staff có enforce ở tầng API không — trùng GAP-1).
- `kho-tcs/fa026-billtienitem-商品販売.md` — vùng regression: nhóm 「Phân quyền & môi trường」(8 TC). Mục mở: **`MT-39`** (*"Endpoint thanh toán và bill-item không có lớp xác thực — rủi ro thao tác chéo bot; corpus **KHÔNG có TC** nào ở tầng API"*) — trùng B-2/GAP-7.

**TC kho dùng lại, KHÔNG viết mới:**

| GAP | Dùng lại TC kho | Cần chỉnh gì cho bug hiện tại |
|---|---|---|
| Staff **không** quyền màn template gọi thẳng API | `TC-TMT-390` — *"Staff KHÔNG được phân quyền template → chặn cả ở UI và ở tầng API"* | Thêm 1 bước: staff của **bot A** gọi `save-template` với `template_group_id` của **bot B** (trục cross-bot, kho chỉ có trục cross-role) |
| Truy cập trực tiếp API bill-item không quyền | `TC-BIL-375` — *"Truy cập TRỰC TIẾP API bill-item bằng tài khoản không có quyền / không đăng nhập"* | Kho phủ trục "không quyền/không đăng nhập"; GAP-7 bổ sung trục **"có đăng nhập, sai bot"** |
| Regression production của FA-010 | `TC-TMT-394` — *"Regression trên PRODUCTION: media, domain và job gửi tin của template"* | Chạy kèm GAP-4, không viết lại |
| V1/V2 item không lẫn danh sách | `TC-BIL-381` — *"Sản phẩm phiên bản CŨ không lẫn vào danh sách phiên bản mới"* | Dùng làm đối chứng cho GAP-8 |
| `template_child_id` phải ép int | `TC-TMT-382`, `TC-TMT-383` | Nhánh fix chạm đúng tham số này (`NEW-35`) → chạy lại 2 TC kho làm regression, không viết mới |
| Cross-bot mở màn edit template | `TC-TMT-384` | Đã được `NEW-2` phủ → **không thêm** |

**Chống trùng (BƯỚC 5b)**: đã đối chiếu **38 TC ở BƯỚC 0** + `kho-tcs/fa010` + `kho-tcs/fa026` — **không TC đề xuất nào trùng** theo 4 yếu tố; các TC dùng lại kho đã tách sang bảng trên thay vì viết mới.

> **Lịch sử phạm vi §5 (2026-09-04)**
> 1. Đợt 1 — 15 TC. Leader **loại 6 TC**: `TC-SEC001-01` · `TC-SEC001-02` · `TC-COMPATLEGACY001-01` · `TC-COMPATLEGACY001-02` · `TC-MSG004-01` · `TC-REGRUN001-01` → còn **9 TC** (đã push Studio task #245).
> 2. Đợt 2 — Leader yêu cầu **quét toàn bộ route có tham số id** để regression cùng lớp bug → bổ sung **12 TC sweep** (`TC-SEC001-03` → `TC-FUNC004-02`). §5 hiện có **21 TC**.
>
> **Trạng thái GAP sau đợt 2:**
>
> | GAP | Trạng thái | Ghi chú |
> |---|---|---|
> | `GAP-7` — endpoint cùng lớp lỗi (yokoten) → `B-2` | ✅ **đã lấp** | 12 TC sweep phủ 151 route cross-bot + 18 route cross-user |
> | `GAP-4` — cross-bot trên production → `B-4` | ✅ **đã lấp** | `TC-SEC001-07` (tiền) và `TC-SEC001-08` (domain công khai) đều `Phạm vi ENV = product` |
> | `GAP-8` — bản ghi đời cũ | ✅ **đã lấp** | `TC-COMPATLEGACY001-03` (12 route bản cũ) |
> | `GAP-9` — biên id | ✅ **đã lấp** | `TC-FUNC004-01` (tầng ghi) + `TC-FUNC004-02` (20 route × 7 giá trị biên) |
> | `GAP-6` — output cuối chuỗi trên LINE app → `M-6`, một phần `B-3` | ❌ **vẫn hở** | `TC-MSG004-01` đã bị loại đợt 1 và không được thay thế. Rủi ro "tin con lệch bot được job gửi tới khách hàng bot khác" vẫn chưa có TC nào chứng minh |
> | `GAP-10` — job / dữ liệu đang chạy dở khi release → `REG-RUN-001` | ❌ **vẫn hở** | `TC-REGRUN001-01` đã bị loại đợt 1 |
>
> Verdict §1 **giữ nguyên REJECTED** — 4 blocker chưa đổi trạng thái. Đợt 2 lấp được phương án test cho `B-2` và `B-4`, nhưng blocker chỉ đóng khi **chạy xong và không phát hiện route nào lọt**.

### Inventory route có tham số id (nguồn: `lme-route-access.md`, 362 màn hình)

**212 route** mang tham số id. Phân trục và ánh xạ sang TC:

| Trục | Nhóm | Số route | TC phủ |
|---|---|---|---|
| **A — cross-bot, `/basic/*`** | A1 thông tin bạn bè & PII | 18 | `TC-SEC001-03` |
| | A2 nội dung & thiết lập (template, rich menu, tag, URL, form, QR, popup, CSV, blog, backup) | 32 | `TC-SEC001-04` |
| | A3 luồng gửi tin (broadcast, kịch bản, remind, trả lời tự động, action schedule) | 26 | `TC-SEC001-05` |
| | A4 đặt lịch & đặt chỗ (event mới, calendar, lesson, salon) | 32 | `TC-SEC001-06` |
| | A5 sản phẩm · đơn hàng · hợp đồng · hoá đơn | 11 | `TC-SEC001-07` |
| **B — `bot_id` truyền thẳng trên URL** | `/basic/*`, `/admin/*`, affiliate, short link | 20 | `TC-PERM003-04` |
| **E — nhánh đời cũ** | booking_event cũ, nhân viên cũ, trả lời tự động cũ, sản phẩm V1, template cũ | 12 | `TC-COMPATLEGACY001-03` |
| | **Tổng trong phạm vi cross-bot (A + B + E)** | **151** | |
| **C — cross-user cổng quản trị** | `/admin/*`, affiliate, xác thực 2 lớp | 18 | `TC-PERM001-01` |
| **D — công khai / LIFF theo hash · `u_code`** | order-item, mobile booking, form render, landing, liff-callback, short link | 31 | `TC-SEC001-08` |
| **F — auth / token — LOẠI khỏi phạm vi** | `{token}`, `{email}`, `{user_id}` lúc đăng ký, `{conversation_id}` app mobile | 12 | — |
| | **TỔNG** | **212** | |

Ba nhóm bổ sung không nằm trong bảng route nhưng cùng lớp lỗi, đã phủ riêng: **tầng GHI** (lưu · xoá · sắp xếp · sao chép · đổi trạng thái hiển thị) → `TC-PERM002-03`; **giá trị biên** → `TC-FUNC004-02`; **đối chứng dương** (guard không chặn nhầm chính chủ) → `TC-SEC001-09`.

**Lý do loại trục F**: các route này định danh bằng **token dùng một lần / email / id phiên đăng ký**, không phải id bản ghi thuộc sở hữu của bot → không cùng lớp lỗi với #33107. Rủi ro của chúng là đoán/hết hạn token, thuộc `SEC-002`, cần task riêng chứ không gộp vào regression này.

⚠️ **4 route xuất hiện ở 2 trục** (`/basic/message-template/edit/{id}`, `/basic/reply/edit/{item_id}`, `/basic/employees-management/edit/{id}`, `/basic/list-items`) — cố ý lặp: một lần theo trục chức năng, một lần theo trục nhánh đời cũ (RULE-09 đòi test **cả hai nhánh**).

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-PERM003-01 | API | PERM-003 | Phân quyền & cách ly bot | Normal | manual | staging | Tài khoản **staff của 2 bot** chuyển qua lại → danh sách mẫu tin và danh sách item của 2 bot độc lập | - Tài khoản S1 là **staff được phân quyền template + 商品販売 ở CẢ bot A và bot B**<br>- Bot A có nhóm mẫu tin 「G_A」 và item 「I_A」<br>- Bot B có nhóm mẫu tin 「G_B」 và item 「I_B」<br>- Đăng nhập bằng S1 | 1. Đăng nhập S1, chọn Bot A<br>2. Mở 「テンプレート」 → ghi lại toàn bộ tên nhóm hiển thị<br>3. Mở 「商品管理」 → ghi lại toàn bộ tên item hiển thị<br>4. Chuyển bot đang chọn sang Bot B (không đăng xuất)<br>5. Lặp lại bước 2 và 3<br>6. Quay lại Bot A, lặp lại bước 2 và 3 lần nữa | Tài khoản staff S1 thuộc 2 bot | - Ở Bot A: danh sách chỉ có 「G_A」/「I_A」, **không** xuất hiện 「G_B」/「I_B」<br>- Ở Bot B: ngược lại<br>- Bước 6 cho kết quả **giống hệt** bước 2-3 (không dính dữ liệu bot vừa rời) |  | Lấp GAP-1 · cover impact F1, F3 · Đánh giá spec: Spec không ghi (cần hỏi Dev — kho `MT-49` đang ⏳ CHỜ QUYẾT ĐỊNH) · Evidence: screenshot 2 danh sách × 2 bot + bằng chứng dữ liệu tách biệt sau change bot · dẫn từ catalog `MAP-PERM-01` |
| TC-PERM003-02 | API | PERM-003 | Phân quyền & cách ly bot | Abnormal | manual | staging | Staff **chỉ thuộc bot A** dán link tạo tin con của bot B → bị chặn ở cả màn và tầng ghi | - Tài khoản S2 là staff **chỉ của bot A**, được phân quyền template<br>- Bot B có nhóm mẫu tin 「G_B」 (đã ghi lại id nhóm)<br>- Đăng nhập bằng S2 | 1. Đăng nhập S2 (chỉ thấy Bot A)<br>2. Dán link màn soạn tin nhắn con với id nhóm của Bot B<br>3. Quan sát màn hình đích + xem mã nguồn trang<br>4. Nếu màn mở: soạn 1 tin 「TC33107_staff_xam」 rồi bấm 保存<br>5. Đăng nhập lại bằng tài khoản của Bot B, mở 「G_B」 đếm số tin con | id nhóm 「G_B」 của Bot B; tài khoản staff chỉ thuộc bot A | - Bước 3: bị chặn (điều hướng về danh sách mẫu tin của Bot A hoặc trang lỗi), **không** render nội dung của 「G_B」 và **không** nhúng dữ liệu 「G_B」 vào mã nguồn trang<br>- Bước 5: số tin con của 「G_B」 **không đổi**, không có 「TC33107_staff_xam」 |  | Lấp GAP-1 · cover impact F1, F2, D1 · Đánh giá spec: Spec không ghi (`feature-spec.md:807` ghi "Staff permission: kiểm tra ở middleware level — không thấy trong controller") · Evidence: kết quả thử truy cập chéo + ảnh mã nguồn trang · dẫn từ `TC-TMT-390` (kho) |
| TC-PERM003-03 | API | PERM-003 | Phân quyền & cách ly bot | Boundary | manual | staging | Tài khoản **vừa là owner bot A vừa là staff bot B** → thao tác đúng quyền ở từng bot, không lẫn dữ liệu | - Tài khoản O1 là **owner (主管理者) của bot A** đồng thời là **staff của bot B** (quyền hạn chế hơn)<br>- Bot A và bot B đều có nhóm mẫu tin + item | 1. Đăng nhập O1, chọn Bot A → thực hiện tạo tin con + sửa item (thao tác đầy đủ của owner)<br>2. Chuyển sang Bot B → quan sát menu/nút hiển thị<br>3. Ở Bot B, thực hiện đúng các thao tác được phân quyền<br>4. Ở Bot B, dán link màn sửa item của **bot A** → quan sát<br>5. Kiểm tra item của bot A trong DB | Tài khoản O1 = owner bot A + staff bot B | - Bước 1: thao tác đầy đủ, thành công<br>- Bước 2: menu/nút ở Bot B đúng theo lưới phân quyền staff (không hiện thao tác chỉ dành owner)<br>- Bước 4: **bị chặn** — không mở form sửa item của bot A khi phiên đang chọn Bot B<br>- Bước 5: item của bot A **không đổi**, cột bot không đổi |  | Lấp GAP-1 · cover impact F1, F3, F4, D5 · Đánh giá spec: Spec không ghi (đã ghi vào §6 SPEC-2) · Evidence: ma trận role đã test + screenshot từng bot · dẫn từ catalog `MAP-PERM-01` (biên của ma trận tài khoản) |
| TC-DATADB001-01 | Data | DATA-DB-001 | Cách ly dữ liệu tầng DB | Normal | manual | staging | Bản ghi **TRÙNG TÊN** ở 2 bot → sửa/xóa ở bot A không chạm bot B (kiểm `WHERE` scope) | - Bot A và bot B **cùng có** nhóm mẫu tin tên **giống hệt** 「TC33107_同名グループ」, mỗi nhóm 2 tin con **cùng tên** 「TC33107_同名子1」/「TC33107_同名子2」<br>- Bot A và bot B **cùng có** item tên giống hệt 「TC33107_同名商品」<br>- Đã chụp lại bản ghi của **cả 2 bot** trước khi thao tác | 1. Chọn Bot A, sửa nội dung 「TC33107_同名子1」 thành 「A_v2」 rồi 保存<br>2. Chọn Bot A, xóa 「TC33107_同名子2」<br>3. Chọn Bot A, đổi tên item 「TC33107_同名商品」 thành 「A_商品_v2」<br>4. Chuyển sang Bot B, mở nhóm 「TC33107_同名グループ」 và màn 商品管理<br>5. So sánh bản ghi của **cả 2 bot** với ảnh chụp ở tiền điều kiện | 2 bot, dữ liệu trùng tên hoàn toàn | - Bot A: 3 thay đổi áp dụng đúng<br>- Bot B: **cả 3 bản ghi giữ nguyên 100%** — tin con vẫn 2 cái, tên và nội dung không đổi, item không đổi tên và **không đổi cột bot**<br>- Không có bản ghi nào của Bot B bị xóa theo |  | Lấp GAP-2 · cover impact D1, D5 · Đánh giá spec: Spec ghi rõ (`checklist-lme.md` DATA-DB-001 mục 1-2 — RULE-07) · Evidence: **ảnh chụp kết quả query trước/sau kèm câu query, trên CẢ 2 tài khoản** |
| TC-DATAREF001-01 | Data | DATA-REF-001 | Toàn vẹn tham chiếu tin nhắn con | Abnormal | manual | staging | Xóa nhóm mẫu tin của bot B **đang chứa tin con lệch bot** (dữ liệu cũ) → không để lại tham chiếu mồ côi ở bước kịch bản / bản tin | - Dựng dữ liệu lệch mô phỏng **trạng thái trước fix**: nhóm 「G_B」 của Bot B chứa 1 tin con mang `bot_id` của Bot A<br>- Tin con đó **đang được tham chiếu** bởi 1 bước kịch bản của Bot B và 1 bản tin gửi một lần của Bot B<br>- Đã ghi lại id của tin con và của 2 bản ghi tham chiếu | 1. Chọn Bot B, xóa nhóm 「G_B」 (cascade theo BR-05)<br>2. Mở kịch bản của Bot B ở bước có tham chiếu → quan sát danh sách tin<br>3. Mở bản tin gửi một lần của Bot B → quan sát danh sách tin<br>4. Mở lại danh sách mẫu tin của **Bot A** → quan sát<br>5. Kiểm tra bản ghi tin con và 2 bản ghi tham chiếu trong DB | Tin con lệch bot nằm trong nhóm của bot khác | - Bước 2, 3: 2 màn **mở được, không lỗi**; tin con đã xóa không còn hiển thị và **không để lại id mồ côi** trong danh sách tham chiếu<br>- Bước 4: danh sách mẫu tin của Bot A **không bị hỏng** và không mất tin nào khác<br>- Bước 5: không còn id trỏ tới bản ghi đã xóa |  | Lấp GAP-3 · cover impact D1, D2, D4 · Đánh giá spec: Spec ghi rõ (FA-010 `BR-05` cascade delete, `BR-06` hard delete + cleanup, `BR-15` propagation `step_message.update_timestamp`) · Evidence: bằng chứng kiểm tra **từng nơi tham chiếu** sau thao tác · regression |
| TC-PERM002-01 | API | PERM-002 | Chặn bypass bằng lệch cặp cha–con | Abnormal | manual | staging | Truyền **id bước kịch bản của bot B kèm id kịch bản của bot A** → vẫn phải bị chặn | - Bot A có kịch bản 「S_A」 (đã ghi id kịch bản)<br>- Bot B có kịch bản 「S_B」 với 1 bước đã có tin (đã ghi id bước)<br>- Phiên đang chọn Bot A | 1. Chọn Bot A<br>2. Mở màn soạn tin biến thể kịch bản với **id bước = của Bot B** nhưng **id kịch bản = của Bot A**<br>3. Quan sát màn hình đích và danh sách tin được nạp<br>4. Nếu màn mở: soạn 1 tin 「TC33107_lech_cap」 rồi 保存<br>5. Chọn Bot B, mở bước của 「S_B」 đếm số tin<br>6. Lặp lại bước 2-5 với **id kịch bản = của Bot B, id bước = của Bot A** | id bước của B + id kịch bản của A (và hoán đổi) | - Bước 3: bị chặn, **không** nạp danh sách tin của bước thuộc Bot B<br>- Bước 5: số tin của bước 「S_B」 **không đổi**, không có 「TC33107_lech_cap」<br>- Bước 6: cũng bị chặn, bước của Bot A không phát sinh tin |  | Lấp GAP-3 · cover impact F1, F2, D2 · Đánh giá spec: Spec không ghi (bảng `step_message` **không có** cột `bot_id` → chủ sở hữu tra qua `scenario.bot_id`; cần hỏi Dev guard tra theo cha nào — §6 SPEC-3) · Evidence: kết quả gọi trực tiếp + ảnh DB trước/sau |
| TC-PERM002-02 | API | PERM-002 | Chặn bypass bằng lệch cặp cha–con | Abnormal | manual | staging | Truyền **id bước remind của bot B kèm id sự kiện của bot A** → vẫn phải bị chặn | - Bot A có sự kiện remind 「E_A」 (đã ghi id sự kiện)<br>- Bot B có sự kiện remind 「E_B」 với 1 bước đã có tin (đã ghi id bước)<br>- Phiên đang chọn Bot A | 1. Chọn Bot A<br>2. Mở màn soạn tin biến thể remind với **id bước = của Bot B**, **id sự kiện = của Bot A**<br>3. Quan sát màn hình đích<br>4. Gọi thẳng chức năng lưu tin nhắn con với đúng cặp id đó, nội dung 「TC33107_lech_event」<br>5. Chọn Bot B, mở bước của 「E_B」 đếm số tin | id bước remind của B + id sự kiện của A | - Bước 3: bị chặn, không nạp nội dung của Bot B<br>- Bước 4: phản hồi báo **thất bại**<br>- Bước 5: danh sách tin của bước 「E_B」 **không đổi** |  | Lấp GAP-3 · cover impact F1, F2, D3 · Đánh giá spec: Spec không ghi (đã đưa vào §6 SPEC-3) · Evidence: mã lỗi phản hồi + ảnh DB trước/sau |
| TC-FUNC004-01 | UI | FUNC-004 | Biên id ở **tầng ghi** | Boundary | manual | staging | Gọi thẳng chức năng lưu tin nhắn con với id nhóm/bước ở các giá trị biên → không lỗi hệ thống, không ghi nhầm | - Phiên đang chọn Bot A, đã lấy được phiên hợp lệ và payload thật bắt từ giao diện<br>- Ghi lại số tin con hiện có của nhóm 「G_A」 và tổng số bản ghi tin con của Bot A | 1. Gọi chức năng lưu tin nhắn con với `id nhóm = 0`<br>2. Lặp lại với `-1`<br>3. Lặp lại với `-11` (hằng số quy ước của gửi một lần) nhưng **không** kèm id bản tin<br>4. Lặp lại với `-1111` (hằng số của đặt lịch gửi trong chat) nhưng **không** kèm id bản ghi hẹn giờ<br>5. Lặp lại với `999999999`<br>6. Lặp lại với chuỗi `abc`<br>7. Sau mỗi lần: kiểm tra phản hồi + đếm bản ghi tin con của Bot A và của Bot B | id nhóm = `0` · `-1` · `-11` · `-1111` · `999999999` · `abc` | - Cả 6 lần: **không lỗi hệ thống 500**, phản hồi không lộ câu truy vấn / tên bảng / đường dẫn file<br>- **Không** phát sinh tin con **mồ côi** (tin được tạo nhưng không gắn vào nhóm/bước/bản tin nào)<br>- Số bản ghi của **Bot B** không đổi ở cả 6 lần<br>- Hành vi với `-11`/`-1111` thiếu id đi kèm phải **thống nhất** với hành vi ở tầng màn (NEW-49, NEW-52) |  | Lấp GAP-9 · cover impact F2, F5 · Đánh giá spec: Spec không ghi — Dev nêu ở mục 2 *"id rỗng/âm… giữ nguyên luồng cũ"* nhưng chưa nói tầng ghi (§6 SPEC-1) · Evidence: ghi rõ nguồn limit + kết quả 6 pattern + đếm bản ghi mồ côi |
| TC-DATADB001-02 | Data | DATA-DB-001 | Rà dữ liệu lệch toàn hệ thống | Normal | manual | product | RULE-04 — rà **toàn hệ thống** bản ghi lệch chủ sở hữu trước khi đóng ticket | - Quyền truy vấn **chỉ đọc** trên DB **production** (và staging)<br>- Đã áp nhánh fix và ghi lại **thời điểm deploy chính xác**<br>- ⚠️ Tuyệt đối không chạy lệnh ghi/sửa | 1. Truy vấn tin con có `bot_id` khác `bot_id` của nhóm mẫu tin cha chứa nó<br>2. Truy vấn tương tự cho tin gắn vào bước kịch bản (qua bot của kịch bản cha), bước remind, bản tin gửi một lần, tin hẹn giờ trong chat<br>3. Truy vấn item bán hàng có dấu hiệu đổi chủ sở hữu (đối chiếu lịch sử đơn hàng với cột bot hiện tại)<br>4. Chia kết quả thành 2 nhóm theo **thời điểm deploy**: phát sinh **trước** và **sau**<br>5. Lập bảng kết quả kèm id + thời điểm cho từng nhóm | Không có dữ liệu nhập — chỉ truy vấn chỉ đọc trên **production** | - Có bảng kết quả đủ 6 nhóm dữ liệu ở bước 1-3<br>- **Số bản ghi lệch phát sinh SAU thời điểm deploy = 0** — nếu > 0 thì còn đường ghi chưa được chặn, phải điều tra tiếp và **không được đóng ticket**<br>- Bản ghi lệch phát sinh **trước** deploy được liệt kê đủ kèm id + thời điểm, bàn giao cho Dev lập kế hoạch phục hồi |  | Lấp GAP-5 · cover impact D1, D2, D3, D4, D5 · Đánh giá spec: Spec ghi rõ (RULE-04; journal Dev mục ■5 RECOVER DATA) · Evidence: ảnh chụp kết quả query kèm câu query, tách 2 nhóm trước/sau deploy · `product` theo RULE-08 · **nâng cấp của `NEW-68`** (bản Studio mới chỉ chạy `local`) |
| TC-SEC001-03 | API | SEC-001 | Quét route id — thông tin bạn bè & PII | Abnormal | manual | staging | Mở 18 route chi tiết bạn bè / câu trả lời / đặt chỗ bằng id của bot khác → không route nào lộ PII | - Tài khoản admin quản lý Bot A và Bot B<br>- Bot B có sẵn: ≥1 friend, ≥1 câu trả lời form, ≥1 đặt chỗ sự kiện, ≥1 mục thông tin bạn bè, ≥1 conversion đã có người phản hồi, ≥1 rich menu đã áp dụng, ≥1 broadcast đã gửi, ≥1 remind đã gửi, ≥1 action schedule đã chạy<br>- Đăng nhập Bot B, đi qua từng màn và ghi lại id thật trên thanh địa chỉ vào bảng theo dõi 18 dòng<br>- Chụp lại dữ liệu Bot B trước khi quét | 1. Chuyển bot đang chọn sang Bot A<br>2. Lần lượt mở từng route trong danh sách, thay id bằng id của Bot B đã ghi<br>3. Với mỗi route ghi lại: mã HTTP · màn đích · có hiển thị dữ liệu Bot B không<br>4. Xem mã nguồn trang / nội dung phản hồi để chắc dữ liệu Bot B không bị nhúng dù màn không hiển thị<br>5. Quét xong, chuyển sang Bot B đối chiếu dữ liệu với ảnh chụp ban đầu<br>6. Lập bảng kết quả 18 dòng, đánh dấu route KHÔNG bị chặn | /basic/friendlist/my_page/{line_id} · /basic/frienslist/mypage/answer_form/{line_user_id} · /basic/frienslist/mypage/conversion/{line_user_id} · /basic/frienslist/mypage/analyst_history/{line_user_id} · /basic/frienslist/mypage/site_access_history/{line_user_id} · /basic/friend-information/{id} · /basic/friend-information/copy/{id} · /basic/friend-information/item/{id} · /basic/friend_confirmed/status/{status} · /basic/friendlist/get-friend-follow/{date} · /basic/form-answer/detail-result/{form_id}/{form_result_id} · /basic/booking-event-day/detail-booking/{id} · /basic/conversion/visited/{id} · /basic/rich-menu/filter-friend/{id} · /basic/message-send-all/broadcast-histories/{broadcast_id} · /basic/events/list-user/{event_time_id} · /basic/action-schedules/custom/getUsersActionScheduleHistory/{actionScheduleId} · /basic/cross-analysis/show-account | - Cả 18 route đều bị chặn theo hành vi chuẩn chốt ở SPEC-1 (404 hoặc điều hướng về màn danh sách của Bot A), không mở màn dữ liệu<br>- KHÔNG route nào hiển thị hoặc nhúng vào trang/phản hồi: tên LINE, ảnh đại diện, LINE user id, số điện thoại, email, nội dung câu trả lời form, thông tin đặt chỗ của bạn bè thuộc Bot B<br>- Dữ liệu Bot B không đổi sau khi quét<br>- Có bảng kết quả đủ 18 dòng; route nào KHÔNG bị chặn → raise ticket riêng ngay, ghi rõ route + trường dữ liệu bị lộ | | Lấp GAP-7 (yokoten) · cover impact F1, T1, T2, T3, T4 · nhóm rủi ro cao nhất vì lộ PII khách hàng của bot khác · Đánh giá spec: Spec không ghi (chờ SPEC-1 chốt hành vi chặn) · Evidence: bảng 18 dòng kèm ảnh màn đích + ảnh mã nguồn trang cho từng route · regression |
| TC-SEC001-04 | API | SEC-001 | Quét route id — nội dung & thiết lập | Abnormal | manual | staging | Mở 32 route soạn thảo / xem trước nội dung bằng id của bot khác → không route nào lộ hoặc cho sửa | - Tài khoản admin quản lý Bot A và Bot B<br>- Bot B có sẵn mỗi loại ≥1 bản ghi đặt tên tiền tố TC33107_B_: template, park template, rich menu, ảnh rich menu, tag, URL rút gọn, conversion, cross analysis, form, QR action, popup, CSV export, blog, bản backup<br>- Đăng nhập Bot B, ghi lại id thật của từng bản ghi vào bảng theo dõi 32 dòng<br>- Chụp lại dữ liệu Bot B trước khi quét | 1. Chuyển bot đang chọn sang Bot A<br>2. Lần lượt mở từng route, thay id bằng id của Bot B<br>3. Ghi lại mã HTTP · màn đích · dữ liệu hiển thị<br>4. Với route nào màn vẫn mở: thử sửa 1 trường rồi bấm lưu, ghi lại phản hồi<br>5. Xem mã nguồn trang để chắc nội dung Bot B không bị nhúng<br>6. Chuyển sang Bot B đối chiếu toàn bộ bản ghi với ảnh chụp ban đầu<br>7. Lập bảng kết quả 32 dòng | /basic/message-template/edit/{id} · /basic/message_setting/{line_id}/template/{template_id} · /basic/park-template/edit/{id} · /basic/park-template/show/{id} · /basic/templates/detail/{template_id} · /basic/template-v2/add-template · /basic/rich-menu/edit/{id} · /basic/rich-menu/detail-click/{id} · /basic/rich-menu/setting-display/{id} · /basic/image-richmenu/{id} · /basic/tag/edit-tag/{id} · /basic/tag/copy-tag/{id} · /basic/url/detail/{id} · /basic/url/detail-click/{id}/{type_data} · /basic/conversion/edit/{id} · /basic/cross-analysis/edit/{id} · /basic/cross-analysis/result-cross/{id} · /basic/form-answer/edit/{id} · /basic/form-answer/v3/result/{id} · /basic/form-answer/common-design-setting/{id} · /basic/form-answer/setting/{id} · /basic/landing/v2/edit/{id} · /basic/landing/v2/edit/{id}/poster · /basic/landing/show/{id} · /basic/landing/edit/{id} · /basic/landing/copy/{id} · /basic/popup/{category_id}/create-or-update · /basic/popup/detail-data/{popup_id} · /basic/csv-management/edit-download-file/{id} · /basic/csv-management/copy-download-file/{id} · /basic/backup/processing/{id} · /basic/push-notifications/edit-push-notification/{pushId} | - Cả 32 route đều bị chặn, không render form sửa và không nhúng nội dung Bot B vào trang<br>- Bước 4: mọi lần lưu đều THẤT BẠI, không bản ghi nào của Bot B bị đổi nội dung, đổi tên hay đổi cột bot<br>- Route copy (`copy-tag`, `landing/copy`, `copy-download-file`) KHÔNG tạo được bản sao của bản ghi Bot B sang Bot A<br>- Bảng kết quả đủ 32 dòng; route nào lọt → raise ticket riêng | | Lấp GAP-7 · cover impact F1, F2, D1, T1 · ⚠️ `/basic/templates/detail/{template_id}` là route của chức năng xem trước — chính là chỗ `NEW-36` đang FAIL, dự kiến route này KHÔNG chặn · route copy nguy hiểm riêng vì tạo bản sao dữ liệu bot khác sang bot mình · Đánh giá spec: Spec không ghi (SPEC-1) · Evidence: bảng 32 dòng + ảnh mã nguồn trang · regression |
| TC-SEC001-05 | API | SEC-001 | Quét route id — luồng gửi tin | Abnormal | manual | staging | Mở 26 route broadcast / kịch bản / remind / trả lời tự động / action bằng id của bot khác → không route nào lộ hoặc cho ghi | - Tài khoản admin quản lý Bot A và Bot B<br>- Bot B có sẵn: ≥1 broadcast chưa gửi, ≥1 kịch bản nhiều bước, ≥1 remind có bước, ≥1 trả lời tự động, ≥1 action schedule, ≥1 tin đặt lịch trong chat, ≥1 cài đặt tin kết bạn<br>- Đăng nhập Bot B, ghi lại id thật của từng bản ghi vào bảng theo dõi 26 dòng<br>- Ghi lại số tin / số bước hiện có của từng bản ghi | 1. Chuyển bot đang chọn sang Bot A<br>2. Lần lượt mở từng route, thay id bằng id của Bot B<br>3. Ghi lại mã HTTP · màn đích · danh sách tin được nạp<br>4. Với route nào màn vẫn mở: thử thêm 1 tin nội dung TC33107_sweep rồi lưu, ghi lại phản hồi<br>5. Chuyển sang Bot B, đếm lại số tin / số bước của từng bản ghi<br>6. Lập bảng kết quả 26 dòng | /basic/schedule-send/{conversation_id} · /basic/schedule-send/pack-message/show/{send_id}/{pack_id} · /basic/message-send-all/create/{id} · /basic/message-send-all/copy/{id} · /basic/message-send-all/use-templates/{id} · /basic/message-send-all/pack-message/show/{broadcast_id}/{pack_id} · /basic/scenario/pack-template/show/{scenario_id}/{pack_id} · /basic/step-message/list-message/{scenario} · /basic/step-message/create/{scenario}/{scenario_step_id} · /basic/step-message/edit/{step_message}/{template_id} · /basic/step-message/scenario-message/{scenario} · /basic/step-message/preview-scenario-message/{scenario} · /basic/step-message-template-child/{scenarioId}/{stepId}/{templateId} · /basic/add-friend/setting-message/{typeSetting} · /basic/add-friend/edit-setting-message/{typeSetting} · /basic/events/{category_id}/create-or-update · /basic/events/edit/{id} · /basic/event/step/{event_id}/create-or-update · /basic/events/edit-step/{event_id} · /basic/events/create-template/{event_step_id} · /basic/events/edit-template/{event_step_id}/{template_id} · /basic/events/open-new-tab/{event_step_id} · /basic/events/open-new-tab-all/{event_id} · /basic/reply/edit/{item_id} · /basic/action-schedules/{id}/edit · /basic/action-schedules/{id} | - Cả 26 route đều bị chặn, không nạp danh sách tin của Bot B<br>- 2 route xem trước (`open-new-tab`, `open-new-tab-all`) KHÔNG trả nội dung tin của Bot B<br>- Bước 4: mọi lần lưu đều THẤT BẠI; số tin / số bước của mọi bản ghi Bot B giữ nguyên; KHÔNG phát sinh tin con mồ côi<br>- Bảng kết quả đủ 26 dòng | | Lấp GAP-7 · cover impact F1, F2, D2, D3, D4, T2, T3, T4 · ⚠️ nhóm này nguy hiểm nhất về hậu quả: nội dung ghi lệch sẽ được job gửi THẬT tới khách hàng của bot khác · `/basic/step-message-template-child/...` là biến thể cùng họ với 5 biến thể đã test ở bộ TC gốc · Đánh giá spec: Spec không ghi (SPEC-1) · Evidence: bảng 26 dòng + đếm tin trước/sau · regression |
| TC-SEC001-06 | API | SEC-001 | Quét route id — đặt lịch & đặt chỗ | Abnormal | manual | staging | Mở 32 route đặt lịch sự kiện / calendar / lesson / salon bằng id của bot khác → không route nào lộ hoặc cho sửa | - Tài khoản admin quản lý Bot A và Bot B<br>- Bot B có sẵn: ≥1 sự kiện đặt chỗ bản mới (kèm ngày, khung giờ, khoá, người đặt), ≥1 calendar đặt hẹn (kèm khoá + nhân viên), ≥1 lịch lesson (kèm khoá), ≥1 lịch salon (kèm khoá + nhân viên)<br>- Đăng nhập Bot B, ghi lại id thật của từng bản ghi vào bảng theo dõi 32 dòng<br>- Chụp lại cấu hình + danh sách người đặt của Bot B | 1. Chuyển bot đang chọn sang Bot A<br>2. Lần lượt mở từng route, thay id bằng id của Bot B<br>3. Ghi lại mã HTTP · màn đích · dữ liệu hiển thị<br>4. Với 6 route xem trước (`preview*`): kiểm tra nội dung trả về có phải của Bot B không<br>5. Với route nào màn vẫn mở: thử sửa và lưu, ghi lại phản hồi<br>6. Chuyển sang Bot B, đối chiếu cấu hình và danh sách người đặt với ảnh chụp ban đầu<br>7. Lập bảng kết quả 32 dòng | /basic/booking-event-day/{id}/edit · /event/{id}/slots · /detail-slot/{date_id} · /edit-slot/{date_id}/{slot_id} · /add-slot-date/{date_id} · /add-plan/{slot_id} · /edit-plan/{plan_id} · /booking-all-slot/{id} · /booking-slot/{slot_id} · /add-booking/{id} · /preview-event-info/{id} · /preview-term-bill/{id} · /basic/manager-booking/setting/{calendar_id} · /{booking_calendar_id}/course/{course_id} · /{booking_calendar_id}/staff/{staff_id} · /basic/calendar-management/{id} · /{id}/edit-course/{courseId} · /{id}/preview · /{id}/preview-top · /{id}/preview-form · /basic/calendar-salon/{calendar_id} · /calendar-create-course/{id} · /{calendar_id}/edit-course/{id} · /calendar-create-staff/{id} · /{calendar_id}/edit-staff/{id} · /calendar-staff-type/{id} · /calendar-setting-event/{id} · /calendar-display-event/{id} · /calendar-display-event-many-staff/{id} · /{id}/preview · /{id}/preview-top · /{id}/preview-form | - Cả 32 route đều bị chặn, không hiển thị cấu hình cửa hàng, khoá dịch vụ, nhân viên, khung giờ hay danh sách người đặt của Bot B<br>- 6 route xem trước KHÔNG render trang thông tin cửa hàng / câu hỏi / đặc thương pháp của Bot B<br>- Bước 5: mọi lần lưu THẤT BẠI; cấu hình và danh sách người đặt của Bot B giữ nguyên<br>- Route `add-booking` KHÔNG tạo được đặt chỗ vào sự kiện của Bot B<br>- Bảng kết quả đủ 32 dòng | | Lấp GAP-7 · cover impact F1, T4 · nhóm Salon ⇄ Lesson ⇄ Booking Event là cặp lặp lại được nêu đích danh ở `REG-SHARED-001` — bug ở 1 nhánh thường có ở 2 nhánh còn lại · route `preview*` là dạng đã fail ở `NEW-36` nên khả năng lọt cao · Đánh giá spec: Spec không ghi (SPEC-1) · Evidence: bảng 32 dòng + ảnh cấu hình trước/sau · regression |
| TC-SEC001-07 | API | SEC-001 | Quét route id — sản phẩm, đơn hàng, hợp đồng | Abnormal | manual | product | Mở 11 route bán hàng / hợp đồng / hoá đơn bằng id của bot khác → không lộ tiền, không sửa, không huỷ được | - Tài khoản admin quản lý Bot A và Bot B<br>- Bot B có sẵn: ≥1 sản phẩm đơn lẻ, ≥1 đơn hàng đơn lẻ đã thanh toán, ≥1 hợp đồng định kỳ đang hiệu lực, ≥1 hợp đồng LME, ≥1 hoá đơn<br>- Đăng nhập Bot B, ghi lại id và chuỗi hash thật của từng bản ghi vào bảng theo dõi 11 dòng<br>- Chụp lại tên, giá, trạng thái đơn, trạng thái hợp đồng của Bot B<br>- ⚠️ Chỉ dùng bot test của công ty, KHÔNG dùng bot khách hàng thật | 1. Chuyển bot đang chọn sang Bot A<br>2. Lần lượt mở từng route, thay id/hash bằng của Bot B<br>3. Ghi lại mã HTTP · màn đích · các trường tiền hiển thị<br>4. Với route sửa đơn và huỷ hợp đồng: thử submit và ghi lại phản hồi<br>5. Chuyển sang Bot B, đối chiếu tên, giá, trạng thái đơn, trạng thái hợp đồng với ảnh chụp ban đầu<br>6. Kiểm tra lịch sử đơn hàng của Bot B xem có phát sinh thao tác lạ không<br>7. Lập bảng kết quả 11 dòng | /basic/edit-item/{id} · /basic/sales/get-single-item-detail/{id} · /basic/sales/order-history-detail/{hashId} · /basic/sales/cycle-order-history-detail/{hashId} · /basic/order-history/edit/{id} · /basic/cycle-order-history/edit/{id} · /basic/detail-contract/{id} · /basic/detail-contract-bill-max-fail/{id} · /basic/detail-contract/{id}/cancel · /basic/point-settings/cancel/{id} · /basic/sales/preview-invoice/{hashId} | - Cả 11 route đều bị chặn, KHÔNG hiển thị tên sản phẩm, giá, số tiền đã thanh toán, thông tin người mua hay nội dung hoá đơn của Bot B<br>- Bước 4: sửa đơn và huỷ hợp đồng đều THẤT BẠI<br>- Bước 5: tên, giá, trạng thái đơn, trạng thái hợp đồng của Bot B giữ nguyên 100%<br>- Bước 6: lịch sử đơn hàng Bot B không phát sinh dòng nào từ thao tác của Bot A<br>- Bảng kết quả đủ 11 dòng | | Lấp GAP-7 + GAP-4 · cover impact F3, F4, D5, T5 · ⚠️ `order-history-detail` được spec FA-026 ghi đích danh là **IDOR** (`feature-spec.md:296`, R12 `:1168` — `@orderHistoryDetail:1099` không lọc `bot_id`) → khả năng lọt rất cao · `product` bắt buộc theo RULE-08 (bill tiền) · Đánh giá spec: Spec ghi rõ rủi ro, chưa ghi hành vi đúng (SPEC-4) · Evidence: bảng 11 dòng + ảnh DB trước/sau · regression |
| TC-SEC001-08 | API | SEC-001 | Quét route id — trang công khai & LIFF | Abnormal | manual | product | 31 route công khai theo hash / u_code → không suy ra được dữ liệu bot khác và không lộ thông tin người dùng khác | - Bot A và Bot B đều có: ≥1 sản phẩm đang bán, ≥1 sự kiện đặt chỗ, ≥1 lịch lesson, ≥1 lịch salon, ≥1 form, ≥1 QR action, ≥1 URL rút gọn<br>- Có 2 friend test F_A (của Bot A) và F_B (của Bot B), mỗi người đã có ≥1 đơn/đặt chỗ để sinh `u_code` thật<br>- Đã thu thập hash/`u_code` thật của cả 2 bot vào bảng theo dõi 31 dòng<br>- Mở bằng trình duyệt ẩn danh, KHÔNG đăng nhập admin | 1. Mở từng route công khai của Bot B bằng trình duyệt ẩn danh → ghi lại có mở được không và hiển thị gì<br>2. Lặp lại từng route nhưng GIỮ hash của Bot B và thay `u_code` bằng `u_code` của F_A (người dùng thuộc bot khác) → quan sát<br>3. Lặp lại nhưng thay `u_code` bằng chuỗi rác và bằng `u_code` đã hết hạn → quan sát<br>4. Với route đơn hàng và huỷ đơn: thử submit bằng `u_code` không khớp chủ đơn → ghi lại phản hồi<br>5. Kiểm tra đơn hàng và đặt chỗ của Bot B xem có bị đổi trạng thái không<br>6. Lập bảng kết quả 31 dòng | /order-item/index/{item_code}/{u_code} · /order-item/detail/... · /order-item/change/... · /order-item/cancel/... · /v2/order-item/detail/{item_code}/{u_code} · /v2/order-item/cancel/... · /v2/order-item/change/... · /v2/order-item/enter-payment-info/... · /v2/order-item/enter-friend-info/... · /mobile/event-booking/index/{hash_event_id}/{u_code} · /mobile/booking-event-day/preview-term-bill/{id} · /mobile/calendar/{calendarHashId}/{uCode} · /mobile/calendar-lesson/{calendarHashId}/close-booking · /mobile/calendar-salon/{calendarHashId}/{uCode} · /app-booking/{liff_id}/event_id/{event_id} · /app-booking-calendar/{booking_calendar_id}/{tab}/{booking_id} · /haitest/{booking_calendar_id}/{tab}/{booking_id} · /basic/form-answer/form-render/{unique_key}/{line_id} · /form-answer/count-down-success/{id} · /landing/page-intro/{id}/{u_code} · /landing/preview-page-intro/{type}/{id} · /landing-qr/{liffId} · /liff-callback/{unique_id} · /open-external-browser/{type}/{id} · /open-mobile/{type}/{id} · /open-action-mobile/{liff_app_id}/{type}/{id} · /open-action-mobile/v2/{liff_app_id}/{type}/{id} · /l/{unique_key} · /template-v2-redirect-url-expired/{type}/{buttonId} · /customer/payments/{lp_id}/{admin_id} · /customer/contracts/confirm/{payment_id} | - Trang bán hàng / đặt chỗ công khai của Bot B mở được (đúng thiết kế) nhưng CHỈ hiển thị thông tin công khai của sản phẩm/sự kiện<br>- Bước 2: ghép hash của Bot B với `u_code` của người dùng thuộc bot khác KHÔNG được hiển thị tên, số điện thoại, email, lịch sử đơn hay đặt chỗ của bất kỳ ai — phải báo lỗi hoặc quay về trạng thái chưa xác định người dùng<br>- Bước 3: `u_code` rác / hết hạn không gây lỗi hệ thống 500 và không lộ thông tin kỹ thuật<br>- Bước 4: submit bằng `u_code` không khớp chủ đơn phải THẤT BẠI<br>- Bước 5: đơn hàng và đặt chỗ của Bot B không đổi trạng thái<br>- Bảng kết quả đủ 31 dòng | | Lấp GAP-7 · cover impact T3, T4, T5 · trục khác 5 TC trên: các route này CỐ Ý mở công khai, rủi ro nằm ở việc ghép sai `u_code`/hash chứ không phải ở phiên đăng nhập · `product` bắt buộc theo RULE-08 (domain — catalog `ENV-DOMAIN`, `ENV-MEDIA-DOMAIN`: link công khai chạy trên `s.lmes.jp` / `p.lmes.jp` chỉ có ở production) · Đánh giá spec: Spec không ghi · Evidence: bảng 31 dòng + ảnh trang công khai từng trường hợp |
| TC-SEC001-09 | API | SEC-001 | Quét route id — đối chứng dương | Normal | manual | staging | Chính chủ mở toàn bộ route trong 5 nhóm quét → mở bình thường, guard không chặn nhầm | - Dùng lại đúng bảng theo dõi route của TC-SEC001-03 → TC-SEC001-07<br>- Đăng nhập tài khoản sở hữu Bot B<br>- Phiên đang chọn ĐÚNG Bot B | 1. Chọn Bot B<br>2. Mở lần lượt toàn bộ route trong 5 bảng theo dõi, dùng ĐÚNG id của Bot B<br>3. Ghi lại mã HTTP và màn đích của từng route<br>4. Với các màn có form: sửa 1 trường rồi lưu, xác nhận lưu thành công<br>5. Đối chiếu số route mở được với tổng số route trong 5 bảng<br>6. Lập bảng kết quả | Toàn bộ id của chính Bot B, cho 119 route thuộc nhóm A1 + A2 + A3 + A4 + A5 | - 100% route mở bình thường, KHÔNG route nào bị điều hướng về danh sách hoặc trả trang lỗi<br>- Toàn bộ thao tác lưu ở bước 4 đều thành công và dữ liệu cập nhật đúng<br>- Không có route nào bị guard chặn nhầm chính chủ<br>- Bất kỳ route nào chặn nhầm → BLOCKER, báo Dev ngay vì guard đang chặn luồng hợp lệ | | Lấp GAP-7 (đối chứng dương bắt buộc theo RULE-01 cho quan điểm ưu tiên Cao) · cover impact F5 · ⚠️ TC này QUAN TRỌNG ngang các TC chặn: Dev đã tự cảnh báo ở mục TỰ REVIEW rằng guard có thể chặn nhầm luồng hợp lệ · phải chạy SAU cùng đợt với 5 TC quét để so cặp · Evidence: bảng đối chiếu số route mở được / tổng số route |
| TC-PERM003-04 | API | PERM-003 | Quét route id — bot_id truyền thẳng trên URL | Abnormal | manual | staging | 20 route nhận thẳng `bot_id` / id bot trên đường dẫn → đổi sang bot khác phải bị từ chối | - Tài khoản admin quản lý Bot A và Bot B<br>- Đã ghi lại `bot_id` thật của cả 2 bot<br>- Bot B có: hợp đồng LME, gói giới hạn bạn bè, ≥1 nhân viên, ≥1 cài đặt kết nối bot, ≥1 URL rút gọn đã phát hành<br>- Chụp lại cấu hình và hợp đồng của Bot B trước khi quét | 1. Đăng nhập, chọn Bot A<br>2. Mở lần lượt từng route, thay `bot_id` (hoặc id bot) bằng của Bot B<br>3. Ghi lại mã HTTP · màn đích · dữ liệu hiển thị<br>4. Đặc biệt chú ý 4 route chạm hợp đồng và gói cước: kiểm xem có hiển thị hoặc cho thao tác trên hợp đồng của Bot B không<br>5. Với route nhân viên: kiểm có xem/sửa được nhân viên của Bot B không<br>6. Chuyển sang Bot B đối chiếu cấu hình, hợp đồng, danh sách nhân viên với ảnh chụp ban đầu<br>7. Lập bảng kết quả 20 dòng | /basic/home/{bot_id} · /basic/tutorial/{bot_id} · /admin/plan-max-friend/{bot_id} · /admin/confirm-contract-standard/{contract_type}/{bill_type}/{bot_id} · /admin/setting-bot/{id} · /admin/change-bot-sub/{id} · /admin/change-bots-new/{id} · /admin/payment-max-friend/{id}/{type} · /admin/bots/list-bot-enterprise/{contract_id} · /admin/edit-staff/{staffBotId} · /admin/employees-management/edit/{id} · /admin/affiliate-management/preview-add-friend/{bot_id}/{id} · /v2/affiliate/detail-item/{bot_id}/{id} · /v2/affiliate/aff-bot/{bot_id} · /v2/line/optin/regist/{aff_id}/{bot_id}/follow/{landing_page_id} · /open-action-mobile-normal/{bot_id}/{type}/{id}/{user_id} · /open-success-action-mobile-normal/{bot_id} · /st/{bot_id}/{url_id}/{unique_key} · /s/{bot_id}/{unique_key}/{msg_id} · /sl/{bot_id}/{unique_key}/{msg_id} | - Với `bot_id` của bot mà tài khoản KHÔNG sở hữu: mọi route bị từ chối, không hiển thị dữ liệu bot đó<br>- Với `bot_id` của Bot B mà tài khoản CÓ sở hữu: hoặc bị từ chối vì không khớp bot đang chọn, hoặc chuyển hẳn phiên sang Bot B — KHÔNG được rơi vào trạng thái lẫn lộn (hiển thị dữ liệu Bot B trong khi phiên vẫn ghi Bot A)<br>- 4 route hợp đồng / gói cước KHÔNG cho xem hay thao tác trên hợp đồng của bot khác<br>- Cấu hình, hợp đồng, danh sách nhân viên của Bot B không đổi<br>- Bảng kết quả đủ 20 dòng | | Lấp GAP-7 · cover impact F1, F3, T5 · trục riêng: `bot_id` nằm THẲNG trên URL nên không cần đoán id bản ghi — dễ khai thác nhất · catalog `MAP-PERM-03` (đổi param ID trên URL sang bot khác) · trạng thái "lẫn lộn" ở expected là biến thể nguy hiểm mà `NEW-41`/`NEW-61` chưa phủ · Đánh giá spec: Spec không ghi (SPEC-2) · Evidence: bằng chứng dữ liệu tách biệt sau đổi `bot_id` trên URL |
| TC-PERM002-03 | API | PERM-002 | Quét tầng GHI — gọi thẳng chức năng lưu / xoá / sắp xếp | Abnormal | manual | staging | Gọi thẳng chức năng ghi của 6 module bằng id bản ghi của bot khác → mọi lời gọi phải bị từ chối | - Tài khoản admin quản lý Bot A và Bot B<br>- Đã bắt được payload thật của các thao tác ghi từ giao diện Bot A: lưu · xoá · sắp xếp · sao chép · đổi trạng thái hiển thị · chuyển thư mục<br>- Bot B có bản ghi tương ứng cho từng module (dùng lại dữ liệu của TC-SEC001-04 → TC-SEC001-07)<br>- Đã chụp lại toàn bộ bản ghi Bot B trước khi quét | 1. Chọn Bot A, giữ phiên đăng nhập hợp lệ<br>2. Với từng module: gọi thẳng chức năng LƯU, thay id bản ghi bằng id của Bot B → ghi lại phản hồi<br>3. Lặp lại với chức năng XOÁ<br>4. Lặp lại với chức năng SẮP XẾP (truyền danh sách id của Bot B)<br>5. Lặp lại với chức năng SAO CHÉP và ĐỔI TRẠNG THÁI HIỂN THỊ<br>6. Chuyển sang Bot B, đối chiếu từng bản ghi với ảnh chụp ban đầu: nội dung · tên · thứ tự · trạng thái hiển thị · cột bot<br>7. Kiểm tra danh sách của Bot A xem có bản ghi lạ được sao chép sang không<br>8. Lập bảng kết quả theo module × thao tác | 6 module: tag · template · rich menu · form · QR action · sản phẩm.<br>5 thao tác mỗi module: lưu · xoá · sắp xếp · sao chép · đổi trạng thái hiển thị.<br>Tham số: id bản ghi = của Bot B, phiên = Bot A | - Toàn bộ lời gọi đều trả kết quả THẤT BẠI, không lời gọi nào báo thành công giả<br>- Bản ghi của Bot B giữ nguyên: nội dung · tên · thứ tự sắp xếp · trạng thái hiển thị · cột bot<br>- KHÔNG bản ghi nào của Bot B bị xoá<br>- Danh sách của Bot A KHÔNG xuất hiện bản sao của bản ghi thuộc Bot B<br>- Bảng kết quả đủ 6 × 5 = 30 ô, ô nào lọt → raise ticket riêng | | Lấp GAP-7 · cover impact F2, F3, F4, D1, D5 · ⚠️ **xoá và sắp xếp là 2 thao tác bộ TC gốc CHƯA phủ** — spec FA-026 ghi đích danh `/ajax/sort-items` (`:866`) và `change-valid-item` (`:857`) không lọc `bot_id`; spec FA-010 `BR-05`/`BR-06` cho biết xoá là **hard delete có cascade**, lọt là mất dữ liệu vĩnh viễn · Đánh giá spec: Spec ghi rõ rủi ro (SPEC-4), chưa ghi hành vi đúng · Evidence: kết quả gọi trực tiếp (mã lỗi) + ảnh DB trước/sau cho từng ô |
| TC-PERM001-01 | API | PERM-001 | Quét route id — trục cross-user cổng quản trị | Abnormal | manual | staging | 18 route `/admin/*` và affiliate nhận id → tài khoản này không được mở bản ghi của tài khoản khác | - Có 2 tài khoản quản trị độc lập U1 và U2 (KHÔNG chung bot, không phải quan hệ owner–staff)<br>- U2 có: tài khoản quản trị, thông báo, LP, email ASP, cộng tác viên, thanh toán CTV<br>- Đăng nhập U2, ghi lại id thật của từng bản ghi vào bảng theo dõi 18 dòng<br>- Chụp lại dữ liệu của U2 | 1. Đăng nhập bằng U1<br>2. Mở lần lượt từng route, thay id bằng id của U2<br>3. Ghi lại mã HTTP · màn đích · dữ liệu hiển thị<br>4. Với route sửa: thử lưu và ghi lại phản hồi<br>5. Với 2 route xác thực 2 lớp: kiểm xem có gửi/nhận được mã của tài khoản khác không<br>6. Đăng nhập lại U2, đối chiếu dữ liệu với ảnh chụp ban đầu<br>7. Lập bảng kết quả 18 dòng | /admin/edit-account/{id} · /admin/edit-notifies/{id} · /admin/bot-management/{userId} · /admin/bot/{hashId} · /admin/user/{id} · /admin/add_new_lp/{lp_id} · /admin/affiliate-management/confirm-email/{emailId} · /admin/manage-payment/{aff_id} · /affiliate/{admin_id}/regist · /aff/v2/login/{admin_id} · /aff/v2/register-success/{admin_id} · /affiliate/reset-password/{admin_id} · /aff/v2/get_change_password/{admin_id} · /affiliate/contact/{id} · /affiliate/contact-confirm/{id} · /affiliate/detail-mail-send/{id} · /enter-verify-code/{id} · /view-send-code-two-factor-verify/{id} | - Cả 18 route đều bị từ chối, không hiển thị email, tên, thông tin ngân hàng, số tiền hoa hồng hay thông tin thanh toán của U2<br>- Bước 4: mọi lần lưu THẤT BẠI, dữ liệu U2 không đổi<br>- Bước 5: KHÔNG gửi lại được mã xác thực 2 lớp cho tài khoản khác và không đọc được mã đó<br>- 2 route `/admin/bot/{hashId}` và `/admin/user/{id}` chỉ mở được bằng tài khoản có quyền vận hành, tài khoản thường bị từ chối<br>- Bảng kết quả đủ 18 dòng | | Lấp GAP-1 + GAP-7 · **trục KHÁC các TC trên**: đây là cross-user ở cổng quản trị, không phải cross-bot — nhưng cùng lớp lỗi "dùng id trần không kèm điều kiện chủ sở hữu" · 2 route xác thực 2 lớp là rủi ro chiếm tài khoản, ưu tiên chạy trước · catalog `MAP-PERM-01` · Đánh giá spec: Spec không ghi (SPEC-2) · Evidence: ma trận tài khoản đã test + ảnh từng route |
| TC-COMPATLEGACY001-03 | Data | COMPAT-LEGACY-001 | Quét route id — nhánh đời cũ | Abnormal | manual | Tất cả | 12 route bản cũ vẫn còn truy cập được → cũng phải chặn cross-bot như bản mới | - Tài khoản admin quản lý Bot A và Bot B<br>- Bot B có bản ghi ĐỜI CŨ: sự kiện đặt chỗ bản cũ, nhân viên bản cũ, trả lời tự động bản cũ, sản phẩm `is_product_new = 0`, template không thuộc group<br>- Đăng nhập Bot B, ghi lại id thật của từng bản ghi bản cũ<br>- Chụp lại dữ liệu bản cũ của Bot B | 1. Chuyển bot đang chọn sang Bot A<br>2. Mở lần lượt 12 route bản cũ, thay id bằng id của Bot B<br>3. Ghi lại mã HTTP · màn đích · dữ liệu hiển thị<br>4. Với route nào màn vẫn mở: thử sửa và lưu, ghi lại phản hồi<br>5. Chuyển sang Bot B, đối chiếu dữ liệu bản cũ với ảnh chụp ban đầu, đặc biệt kiểm cột bot<br>6. Đối chiếu kết quả 12 route bản cũ với kết quả route bản mới tương ứng ở TC-SEC001-04/05/06/07 | /basic/booking_event/{id}/edit · /basic/booking_event/{id}/copy · /basic/booking_event/event/{id}/slots · /basic/booking_event/detail-slot/{slot_id} · /basic/booking_event/list-plan/{slot_id} · /basic/booking_event/event/booking-all-slot/{id} · /basic/booking_event/event/booking-slot/{eventId}/slots/{slotId} · /basic/employees-management/edit/{id} · /basic/reply/edit/{item_id} · /basic/edit · /basic/list-items · /basic/message-template/edit/{id} | - Cả 12 route bản cũ đều bị chặn giống hệt route bản mới tương ứng<br>- KHÔNG route bản cũ nào hiển thị dữ liệu của Bot B<br>- Bước 4: lưu THẤT BẠI; đặc biệt **cột bot của mọi bản ghi Bot B không đổi** (bản cũ là chỗ đã xác nhận có lỗi chiếm quyền sở hữu ở `NEW-29`)<br>- Bước 6: hành vi chặn của nhánh cũ và nhánh mới **thống nhất** — nếu lệch nhau thì ghi rõ route nào lệch và lệch thế nào | | Lấp GAP-7 + GAP-8 · cover impact F4, D5, T4, T5 · RULE-09: không được kết luận từ "nhánh mới chạy ổn" · lỗi chiếm quyền sở hữu ở ticket gốc nằm ĐÚNG ở màn bản cũ (`NEW-29`) nên nhánh cũ là vùng rủi ro cao nhất · route `/basic/booking_event/{id}/copy` nguy hiểm riêng: sao chép sự kiện của bot khác về bot mình · Đánh giá spec: Spec ghi rõ (bill-item §1.5 `is_product_new`) · Evidence: screenshot CẢ 2 nhánh cũ/mới · regression |
| TC-FUNC004-02 | UI | FUNC-004 | Quét route id — giá trị biên | Boundary | manual | staging | Truyền id biên / id rác vào 20 route đại diện → không lỗi hệ thống, không lộ thông tin kỹ thuật | - Chọn 20 route đại diện, mỗi nhóm A1–A5 lấy 4 route (ưu tiên route có nhiều tham số id và route dùng chuỗi hash)<br>- Phiên đăng nhập hợp lệ, đang chọn Bot A<br>- Đã ghi lại 1 id thật của Bot A cho mỗi route để làm mốc | 1. Với mỗi route, lần lượt thay id bằng: `0`<br>2. Thay bằng `-1`<br>3. Thay bằng `999999999`<br>4. Thay bằng chuỗi `abc`<br>5. Thay bằng chuỗi `1 or 1=1`<br>6. Thay bằng ký tự tiếng Nhật 「あ」<br>7. Bỏ hẳn tham số id<br>8. Với route dùng hash: thêm 1 lần với chuỗi hash sai định dạng và 1 lần với hash của bản ghi đã xoá<br>9. Ghi lại mã HTTP và toàn bộ nội dung màn lỗi cho từng ô | 20 route × 7 giá trị biên (+2 giá trị hash cho route dùng hash) | - KHÔNG ô nào trả lỗi hệ thống 500<br>- KHÔNG màn lỗi nào lộ câu truy vấn, tên bảng, đường dẫn file, tên biến hay dấu vết framework<br>- Hành vi với id không tồn tại **thống nhất trên cả 20 route** — không route thì chặn, route thì mở màn tạo mới (đây chính là điểm mâu thuẫn `CONF-1` đang chờ chốt ở SPEC-1)<br>- Bỏ tham số id: mở luồng tạo mới hoặc trả lỗi sạch, không lỗi hệ thống<br>- Bảng kết quả ghi rõ ô nào lệch chuẩn | | Lấp GAP-7 + GAP-9 · cover impact F5 · guard mới **cố ý bỏ qua** id rỗng/âm/không tồn tại nên đây là vùng dễ sai nhất khi áp ra diện rộng · TC này cũng là bằng chứng để chốt `SPEC-1` — chạy xong sẽ biết app đang xử lý id không tồn tại theo kiểu nào trên toàn hệ thống · Đánh giá spec: Spec không ghi (SPEC-1) · Evidence: ghi rõ nguồn limit + bảng 20 × 7 ô |

---

## 6. Spec update needed

| # | Vấn đề | Bằng chứng | Cần ai chốt |
|---|---|---|---|
| `SPEC-1` | **Hành vi chuẩn khi id trên URL không hợp lệ / không tồn tại là gì?** Ba khả năng đang cùng tồn tại trong bộ TC và trong bug list: (a) trả **404**; (b) **điều hướng** về danh sách mẫu tin; (c) **mở màn tạo mới**. | `NEW-4` đòi (a) hoặc (b); `NEW-46` đòi (c) — mâu thuẫn trực tiếp (§4.5 `CONF-1`). Bug **#40349** (closed) đứng về phía "phải chặn"; bug Studio **#775 rejected** với lý do *"redirect thay cho 404"*. Dev mục 2 ghi *"id rỗng/âm hoặc bản ghi không tồn tại thì **giữ nguyên luồng cũ**"* → nghiêng về (c), ngược với hành vi quan sát được ở run #849 (app **chặn**). | **Dev + Leader** — chốt 1 hành vi, ghi vào `spec-features/admin/message-template/feature-spec.md` §5 (BR mới), rồi sửa Expected của `NEW-4`, `NEW-46`, `NEW-2`, `NEW-32`, `NEW-43`, `NEW-22` thành giá trị đơn |
| `SPEC-2` | **Ma trận phân quyền của màn template và màn 商品販売 chưa có trong spec.** `PERM-001` quy định *"phải có role matrix trong spec trước khi test — không có thì không bắt đầu test được"*. | `spec-features/admin/message-template/feature-spec.md:807` tự ghi *"Staff permission: kiểm tra ở middleware level (**không thấy trong controller**) [Trung bình]"*; danh sách middleware không có middleware phân quyền theo màn. Kho `fa010` `MT-49` và `fa026` `MT-39` đều đang ⏳ CHỜ QUYẾT ĐỊNH. | **Dev** cung cấp cơ chế enforce + **Leader** bổ sung ma trận `MAP-PERM-01` vào spec |
| `SPEC-3` | **Chủ sở hữu của bước kịch bản / bước remind được tra qua đâu?** `step_message` **không có** cột `bot_id` → guard tra qua `scenario.bot_id`. Nếu guard đọc `scenario_id` **từ URL** thay vì từ bản ghi cha thật của bước thì có thể bypass bằng cách truyền lệch cặp. | Dev mục ■6 VERIFY: *"`step_message` KHÔNG có `bot_id` nên phải tra chủ sở hữu qua `scenario.bot_id`"*. Không TC nào hiện có thử cặp lệch (TC-PERM002-01/02 đề xuất bổ sung). | **Dev** xác nhận nguồn tra chủ sở hữu; ghi vào spec FA-009 |
| `SPEC-4` | **Hành vi đúng của các endpoint bán hàng khi bị gọi chéo bot chưa được định nghĩa** — spec mới chỉ ghi *rủi ro*, không ghi *hành vi mong đợi*. | `spec-features/admin/bill-item/feature-spec.md` R5 (`:1161`), R12 (`:1168`), EP-05 (`:824`), EP-25 (`:844` — *"⚠ không auth"*), EP-33 (`:857`), EP-41/42 (`:864`), EP-45 (`:866`). Kho `fa026` `MT-39` đánh giá: *"bất kỳ ai biết id đơn hàng đều có thể HOÀN TIỀN hoặc HUỶ HỢP ĐỒNG của bot khác"*. | **Dev + Leader** — chốt phạm vi #33107 vs ticket mới, bổ sung BR vào spec FA-026 §5 |
| `SPEC-5` | **Hành vi với dữ liệu lệch chủ sở hữu đã tồn tại (trước fix) chưa được định nghĩa**: job gửi tin gặp tin con lệch bot thì gửi, bỏ qua, hay báo lỗi? | Dev mục ■5: *"Fix chỉ chặn phát sinh mới"*. Note của `NEW-57`: *"hậu quả nếu lọt: tin của bot A được gửi tới đúng khách hàng của bot B"*. Không TC nào phủ (GAP-6). | **Dev + Leader** — quyết định: chặn ở tầng job, hay xoá dữ liệu lệch trước khi đóng ticket |
| `SPEC-6` | **`MT-39` (kho `fa026`) có thể chuyển từ ⏳ sang ĐÃ CÓ TC** — ticket này đã sinh 4 TC tầng API cho bill-item (`NEW-24`, `NEW-63`, `NEW-64`, `NEW-37`), lấp đúng ghi chú *"corpus KHÔNG có TC nào ở tầng API cho bill item"*. | Bug #40287, #40288 (closed) chứng minh lỗ hổng có thật và đã fix. | **Leader** — cập nhật `MT-39` sau khi #33107 đóng; theo `RULE-10` sinh thêm 1 dòng quan điểm/catalog mới cho lớp bug "id trần không kèm `bot_id`" |
