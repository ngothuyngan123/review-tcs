# 05 — Review Report

## 0. Nguồn TC

| Mục | Giá trị |
|---|---|
| Nguồn đã dùng | **MCP LME TEST STUDIO** — task #189 (ticket 38871), `testcase_list`, fetch 2026-09-08 |
| Tổng số TC review | **10** (9 `pass` · 1 chưa chạy · 0 `fail`) |

---

## 1. Coverage — `dev-impact` + `diff code`

**Kết luận: 8/17 vùng ảnh hưởng đủ TC · 6 GAP · 3 RISK.**

| Mã | Chiều | Vùng ảnh hưởng | TC hiện có | Status | Vấn đề |
|---|---|---|---|---|---|
| **G1** | `diff code` | **Điểm sửa 2 — chuẩn hóa `tags_search` về mảng id** | NEW-3 (`{id,name}`), NEW-1 (mảng id), NEW-7 (trùng chuỗi con) | **GAP** `[BLOCKER]` | Dev tự test **6 dạng** dữ liệu trong script PHP (mảng id / 1 tag / **id đơn lẻ scalar** / `{id,name}` cũ / **thiếu trường `tags_search`** / trùng chuỗi con) nhưng TC chỉ phủ **3/6**. Đây là **generic-fix** (normalize + try/catch bao mọi shape) mà chỉ test **1 trigger lệch dạng thật** (`{id,name}`) → vi phạm rule generic-fix ≥ 3 trigger + 1 trigger chưa biết. Dạng **"thiếu trường `tags_search`"** đặc biệt nguy hiểm: chính Dev ghi code cũ *"thậm chí bị xóa nhầm khi thiếu trường tags_search"* → là case **mất dữ liệu**, hiện **0 TC**. |
| **G2** | `dev-impact` F3 | **Entry point `manageTag` case `deleteGroup`** — xóa **nhóm tag** (cascade nhiều tag) | Không có | **GAP** `[MAJOR]` | `REQ-010` của Studio ghi rõ shared path gồm *"path đơn (menu ⋮ deleteTag) / **xóa nhóm cascade**"*, nhưng NEW-5 chỉ test menu ⋮. Xóa nhóm = gọi `deletedDataTag` **nhiều lần liên tiếp** → đúng kịch bản dễ bung lỗi loop nhất, lại là path **duy nhất chưa chạm**. |
| **G3** | `dev-impact` F4 / D3 | **Bước cuối chuỗi cleanup — `sync_elasticsearch` (insert)** | NEW-8 (dừng ở `ActionDetail` / `tag_line_user` / `ActionLimitTag`) | **GAP** `[MAJOR]` | Spec [tag-management BR-10](../../spec-features/admin/tag-management/feature-spec.md) định nghĩa `deletedDataTag` gồm **6 bước a→f**, `f = Insert sync_elasticsearch records` là **bước CUỐI**. Trước fix, exception ở bước a làm abort cả hàm → f bị bỏ. NEW-8 verify tới bước e rồi **dừng**, đúng bước dễ sót nhất lại không kiểm. ES lệch → tag đã xóa vẫn ra trong tìm kiếm/lọc bạn bè. |
| **G4** | `diff code` D4 | **`Log::error('...deletedDataTag filter_id ... error')` — log mới thêm** | Không có | **GAP** `[MAJOR]` | Studio khai `REQ-009` nhưng **0 TC map**. Đây là **cơ chế quan sát DUY NHẤT** còn lại: sau fix, bản ghi filter lỗi bị `continue` **âm thầm**. Dev cũng nói *"cần log lỗi ... mới thêm để chẩn đoán tiếp"* nếu KH vẫn lỗi — log không chạy thì mất luôn đường điều tra. |
| **G5** | `dev-impact` T2 / T4 / T6 | **`parent_type` ≠ broadcast** — Friend Filter (SC-003), Scenario (FA-009), Auto Reply (FA-003), `modal_action`, `action_schedule`, `filter_manager` | NEW-9 (1 TC, **chỉ truy vấn DB**, không mở màn nào) | **RISK** `[MAJOR]` | Fix `WHERE` **không giới hạn `parent_type`** → chạm 6 `parent_type`. NEW-9 chỉ lấy 1 đại diện và **không verify tầng UI** (RULE-07 đòi 3 tầng). Studio tự ghi trong note NEW-9: *"cần leader xác nhận có mở rộng đủ 5 parent_type không"* — chưa ai trả lời. |
| **G6** | `diff code` | **Hành vi mới: bản ghi filter lỗi bị bỏ qua âm thầm** | Không có | **GAP** `[MAJOR]` | try/catch **per-record** làm 1 filter lỗi không còn chặn các filter khác — nhưng cũng nghĩa là filter đó **vẫn bẩn** trong khi UI báo xóa tag **thành công**. Không TC nào dựng ca "1 filter không dọn được" để xem người dùng thấy gì. Đây là mặt trái trực tiếp của cách fix, không phải suy diễn. |
| **G7** | `diff code` | **Broadcast / scenario đang ĐẶT LỊCH chờ gửi mà filter bị sửa/xóa** | Không có | **GAP** `[MAJOR]` | NEW-2 xác nhận: tag duy nhất bị xóa → **bản ghi `filters_v2` bị XÓA**. Nếu bản ghi đó thuộc broadcast `wait_to_send` đã hẹn giờ thì **tới giờ gửi, đối tượng nhận là ai?** (gửi toàn bộ friend / không gửi / lỗi). Đây là hậu quả **tiền bạc + gửi nhầm** của chính hành vi TC đang cho là Đạt. Kho FA-008 cũng chưa có TC nào ghép "đặt lịch + xóa tag". |
| **G8** | `diff code` | **Phạm vi dữ liệu đã hỏng trước fix (`RULE-04`)** | Không có | **GAP** `[MAJOR]` | Dev xác nhận dữ liệu cũ **không tự dọn** và **không còn sự kiện nào kích hoạt dọn lại**. Studio cũng liệt `RULE-04` trong knowledge pack. Chưa có TC/thao tác nào **thống kê toàn hệ thống** xem bao nhiêu bot × bao nhiêu `filters_v2` còn trỏ tag không tồn tại → **không đủ căn cứ đóng ticket**, và cũng không biết có cần script recover hay không. |
| **G9** | `dev-impact` F2 | **Bulk xóa nhiều tag trong 1 thao tác** | NEW-11 | **RISK** `[MINOR]` | TC có nhưng **chưa chạy lần nào** (`last_exec = null`, tạo 2026-09-08 sau run 2026-08-24) → chưa có bằng chứng. Không cần TC mới, chỉ cần **chạy**. |
| **G10** | `dev-impact` F1 / D1 / D2 / T1 / T3 + `BUG` | Root cause · `filters_v2.data`/`text_preview` · false-positive LIKE · Tag Management · Broadcast | NEW-1, NEW-2, NEW-3, NEW-6, NEW-7, NEW-10 | **OK** | Đủ TC, có `pass`. Không ghi chi tiết. |

> `dev-impact` **F5 / T5** (`TagMobileController` chưa fix) — Dev khai **ngoài phạm vi ticket**, code **không bị chạm** → **không tính GAP**, không đề xuất TC. Xử lý ở §4 + §6.
> **Không có TC orphan** — cả 10 TC đều map được về ít nhất 1 vùng ảnh hưởng.

---

## 2. Quan điểm test còn thiếu

**Kết luận: 6/13 quan điểm khớp trigger đã đủ TC · 7 chưa đủ.**

| Mã | Quan điểm | Ưu tiên | Trigger khớp vì | TC hiện có | Kết luận |
|---|---|---|---|---|---|
| **Q1** | `DATA-DB-001` ★ | **Cao** | "BẮT BUỘC với mọi chức năng có UPDATE hoặc DELETE" — fix là DB-level cleanup `UPDATE`/`DELETE` trên `filters_v2` | Không có | **GAP** `[BLOCKER]` — RULE-07 đòi *"tạo bản ghi trùng tên ở **2 tài khoản** để kiểm chứng `WHERE`"*. **0 TC dựng 2 bot.** NEW-2 có ghi chữ *"đúng scope bot_id + type='tag'"* ở Expected nhưng **tiền đề chỉ có 1 bot** → câu đó không thể verify được. Hàm quét bằng `LIKE` trên toàn bộ filter → sai scope `bot_id` = **xóa filter của khách hàng khác**. Studio cũng liệt `DATA-DB-001` trong knowledge pack nhưng không TC nào mang mã này. |
| **Q2** | `COMPAT-LEGACY-001` ★ | **Cao** | "BẮT BUỘC khi ... bất kỳ đối tượng nào đã từng version-up" — `tags_search` có ≥ 2 thế hệ định dạng | NEW-3 (Abnormal) | **RISK** `[MAJOR]` — chỉ **1 TC / 1 dạng cũ**. Thiếu Normal + Boundary (RULE-01). Xem G1. |
| **Q3** | `ENV-003` ★ | **Cao** | "BẮT BUỘC khi tính năng chạm ... **job nền**" — bước `f` sinh bản ghi cho job đồng bộ Elasticsearch | Không có (9/9 TC chạy `local`) | **GAP** `[MAJOR]` — **RULE-08**: *"Không được đánh × với lý do staging đã pass"*. Ở đây còn nhẹ hơn staging: **toàn bộ chạy `local`**, `dev`/`staging`/`prd` đều `runs=0`. Riêng bug này còn một lý do nữa: dữ liệu lệch dạng `{id,name}` là **dữ liệu lịch sử của production**, DB local dựng mới **không có sẵn** — mọi TC phải seed tay, nên local chỉ chứng minh *code xử lý được shape mình tự bịa*, không chứng minh *shape thật ngoài production là gì*. |
| **Q4** | `REG-RUN-001` | **Cao** | "BẮT BUỘC với mọi release khi hệ thống có **job/dữ liệu đang chạy dở**" — broadcast/scenario hẹn giờ đang chờ gửi | Không có | **GAP** `[MAJOR]` — xem G7. |
| **Q5** | `DATA-COUNT-001` | **Cao** | "BẮT BUỘC khi màn hình có bất kỳ con số đếm nào" — `count_user_tag`, số bạn bè theo tag, số đối tượng nhận của broadcast | Không có | **GAP** `[MAJOR]` — bước `d` (hard delete `tag_line_user`) trước fix bị abort → số đếm sai. Nay được khôi phục nhưng **không TC nào kiểm con số trên màn hình**; NEW-8 chỉ đếm ở DB. Số **đối tượng nhận dự kiến** của broadcast sau khi filter bị thu hẹp cũng chưa ai kiểm. |
| **Q6** | `BULK-001` | **Cao** | "BẮT BUỘC mỗi khi có tổ hợp **bộ lọc + thao tác hàng loạt**" | NEW-11 | **RISK** `[MINOR]` — chưa chạy. Xem G9. Boundary quy mô lớn đã có sẵn ở kho (`TC-TAG-106`). |
| **Q7** | `SYNC-APP-001` | Trung bình | "tính năng có mặt trên cả web và app" — `TagMobileController::deletedDataTag` cùng pattern lỗi | Không có | `[MAJOR]` **nhưng KHÔNG đề xuất TC** — code mobile **không bị chạm** trong branch này, Dev khai ngoài phạm vi. Việc cần làm là **tách ticket**, không phải viết TC vào bộ này. Xem §4 + §6. |
| — | `DATA-REF-001` · `REG-SHARED-001` · `STATE-DEP-001` · `OUT-TRUTH-001` · `FUNC-004` · `RULE-09` | Cao | — | NEW-1/2/11 · NEW-5/9 · NEW-8 · NEW-6 · NEW-10 · NEW-3 | Có TC. **Nhưng xem `[MAJOR] RULE-01` ở §4-#5**: cả 4 quan điểm Cao đầu đều **chỉ có Normal**, không TC nào Abnormal/Boundary. |

> **2 TC không tính vào coverage quan điểm**: NEW-1 (`TOOL-KNOW-002`) và NEW-7 (`TOOL-NEGCTRL-001`) — mã nội bộ Studio, **không có trong `framework/checklist-lme.md`**. Nội dung 2 TC vẫn hợp lệ và vẫn được tính ở §1 (G10), chỉ là không map được sang quan điểm chuẩn.

---

## 3. TC trùng lặp

Đã rà **10/10 TC** theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`).

| Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| Entry point "menu ⋮ `deleteTag`" | **NEW-2** (`DATA-REF-001`, nhánh xóa bản ghi) | **NEW-5** (`REG-SHARED-001`) — **KHÔNG xóa, đề nghị ĐỔI MỤC ĐÍCH** sang path `deleteGroup` | `DUP-INFLATE` | Cùng `loại case` Normal · cùng thao tác **xóa tag qua menu ⋮ trên SCR-TAG-01** · tiền đề tương đương (bot Standard/Pro + broadcast có filter tag) · phần assert của NEW-5 (*bớt 1 tag, giữ tag còn lại, rebuild `text_preview`*) **trùng khít NEW-1** | `[MAJOR]` |

**Vì sao là `DUP-INFLATE` chứ không phải trùng vô hại:** NEW-5 được đặt ra để chứng minh *"`deletedDataTag` dùng chung chạy đúng qua **entry point khác**"*. Nhưng entry point nó dùng (menu ⋮) **đã bị NEW-2 đi qua rồi**, còn assert thì trùng NEW-1. Kết quả là `REG-SHARED-001` **trông như có 2 TC** (NEW-5 + NEW-9) trong khi entry point thứ 3 — **`deleteGroup` xóa nhóm tag cascade** — **chưa TC nào chạm**. Trùng lặp này đang **che GAP G2**.

→ **Đã mở lại coverage**: `REG-SHARED-001` hạ từ `OK` xuống `RISK` ở §2; `G2` được thêm vào §1.
→ **Đề xuất**: giữ nguyên NEW-5 (đang `pass`, không mất mát gì) nhưng **sửa steps sang path `deleteGroup`** trên Studio, **hoặc** giữ NEW-5 và thêm `TC-REGSHARED001-03` ở §5. Báo cáo này chọn **cách 2** để không đụng TC đã chạy.

**Không phát hiện `DUP-EXACT` / `DUP-SUBSET` / `DUP-CONFLICT`.**
Riêng cặp **NEW-1 vs NEW-11** có nhìn giống nhau (cùng bulk EP-11, cùng Normal, cùng nhánh "giữ bản ghi") nhưng **không tính trùng**: NEW-11 xóa **2 tag trong 1 thao tác** → `deletedDataTag` chạy **tuần tự 2 lần cộng dồn trên cùng bản ghi filter**, là cơ chế khác hẳn xóa 1 tag. Giữ cả hai.

⚠️ **Không tự xóa/sửa TC nào** — TC Studio read-only. Human quyết định qua `testcase_update` / `testcase_delete` trên Studio.

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[MAJOR]` | **Toàn bộ 10 TC** | **RULE-02** — cột `Evidence thực tế` **rỗng 10/10**, kể cả 9 TC đã `pass`. Kết quả Đạt hiện là **tự khai**, không có ảnh chụp DB/màn hình nào đính kèm. Bug này verify chủ yếu ở tầng DB → không có evidence thì không tái kiểm được. | Yêu cầu đính kèm đúng loại evidence của từng quan điểm trước khi chốt Đạt. |
| 2 | `[MAJOR]` | **Toàn bộ 10 TC** | **RULE-08 / ENV-003** — `envAuto`: `local runs=2`, `dev`/`staging`/`prd` đều `runs=0`. 9/9 kết quả `pass` đều sinh ở **`local`**. Task chạm job nền (`sync_elasticsearch`) và phụ thuộc **dữ liệu lịch sử production**. | Chạy lại bộ core trên staging + production (`TC-ENV003-01` ở §5). |
| 3 | `[MAJOR]` | Kết quả thực thi | **9/9 kết quả do `pipeline` AI tự chạy** (`source=ai`, `by=pipeline`, cùng `runId=445`, cùng 1 mốc thời gian `2026-08-24 11:31:25`). **0 TC do QA người chạy.** Kết hợp với #1 (không evidence) → chưa có xác nhận độc lập nào. | QA chạy tay tối thiểu các TC ưu tiên Cao + TC mới ở §5. |
| 4 | `[MAJOR]` | `03-dev-impact.md` | File có `Auto-filled: 2026-09-08 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" CHƯA tick**. Riêng file này mục 4.1 **Dev chỉ kê "File thay đổi" (1 file), không kê function** — bảng `F1–F5` là do `/new-task` **suy ra**, chưa ai xác nhận. | Tester đọc lại journal Redmine #132539, xác nhận F/D/T rồi tick. |
| 5 | `[MINOR]` | NEW-1, NEW-7 | 2 mã quan điểm (`TOOL-KNOW-002`, `TOOL-NEGCTRL-001`) không có trong `framework/checklist-lme.md` → không map được coverage chuẩn (đã xử lý ở §2). | Bổ sung 2 mã vào checklist, hoặc gắn lại mã chuẩn khi sửa trên Studio. |
| 6 | `[MINOR]` | Studio `specRefs` | Studio trích **"tag/BR-06: `deletedDataTag` phải dọn FilterV2"** — **sai mã**. Trong [spec tag-management](../../spec-features/admin/tag-management/feature-spec.md), `BR-06` = *"Limit action mode"*; chuỗi cleanup khi xóa tag là **`BR-10`** (`TagController.php:947-1041`, ưu tiên **Cao**). 4/10 TC đang dẫn `BR-06` sai. | Sửa `spec_ids` trên Studio thành `BR-10`. |
| 7 | `[NIT]` | Toàn bộ 10 TC | Cả 10 TC còn `status=draft`, `reviewed=false`, `reviewState=leader` — chưa TC nào được duyệt. | — |

### Chất lượng từng TC + anti-pattern

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 8 | `[BLOCKER]` | **`[AP-1]` Single-trigger generic-fix** | Fix là **generic**: `try/catch` bao mọi shape + `normalize` chấp nhận mọi dạng. Trigger lệch dạng được test thật chỉ có **1** (`{id,name}` ở NEW-3), trong khi Dev tự liệt kê **3 dạng gây lỗi** và tự test 6 dạng bằng script PHP. **Không có TC nào cho shape "chưa biết"** để chứng minh nhánh fallback (bỏ qua bản ghi + ghi log + chạy tiếp) hoạt động. | 3 TC `TC-COMPATLEGACY001-02/03/04` ở §5. |
| 9 | `[MAJOR]` | **`[AP-3]` Happy-path-only regression** | **8/10 TC là `Normal`**, chỉ 2 `Abnormal` + 1 `Boundary`. Nhìn theo quan điểm còn lệch hơn: `DATA-REF-001` (3 TC) · `REG-SHARED-001` (2 TC) · `STATE-DEP-001` · `OUT-TRUTH-001` — **cả 4 đều ưu tiên Cao và 100% chỉ có `Normal`**. Vi phạm **RULE-01** (Cao ⇒ tối thiểu Normal + Abnormal + Boundary), **không TC nào ghi lý do miễn trừ**. | TC bổ sung ở §5 đã cân lại: 6 `Abnormal` + 1 `Boundary` / 14 TC. |
| 10 | `[MAJOR]` | NEW-9 | Note của chính Studio ghi *"1 TC đại diện, **cần leader xác nhận có mở rộng đủ 5 parent_type không**"* — câu hỏi mở này **chưa ai trả lời** mà TC vẫn được đánh `pass`. Ngoài ra TC **chỉ truy vấn DB**, không mở màn hình nào → vi phạm RULE-07 (3 tầng). | Trả lời câu hỏi scope (§6) + `TC-REGSHARED001-04` ở §5. |
| 11 | `[MAJOR]` | NEW-3, NEW-7, NEW-8, NEW-9, NEW-10 | **5/10 TC yêu cầu "Seed TRỰC TIẾP" bản ghi `filters_v2` vào DB** nhưng **không TC nào ghi cách seed** (script? SQL? qua UI nào?). Người khác cầm TC này **không dựng lại được env** → không tái chạy được, và kết quả `pass` không kiểm chứng lại được. | Bổ sung câu lệnh/thao tác seed cụ thể vào `Điều kiện tiền đề` của 5 TC trên (sửa trên Studio). |
| 12 | `[MAJOR]` | NEW-1, NEW-2, NEW-3, NEW-5, NEW-9, NEW-10 | `Trạng thái đánh giá spec` **rỗng ở cả 10 TC** (`spec_status = null`). Bộ TC này có ≥ 2 điểm hành vi **không nằm trong spec** (giữ nguyên bản ghi false-positive LIKE; `text_preview` của broadcast **đã gửi** có bị viết lại không) mà không TC nào đánh dấu `Spec không ghi` / `Đã hỏi leader`. | Điền `Trạng thái đánh giá spec`; 2 điểm trên đưa vào §6. |
| 13 | `[MINOR]` | NEW-10 | Gắn mã `FUNC-004` — quan điểm này định nghĩa là *"chức năng có **giới hạn số lượng / ký tự / dung lượng**"*, trong khi TC thực tế kiểm **4 nhánh `tag_condition` 0/1/2/3** (rẽ nhánh logic, không phải giới hạn). Mã sai làm lệch bản đồ coverage. | Đổi sang mã phản ánh đúng nhánh điều kiện. |
| 14 | `[MINOR]` | NEW-1 … NEW-10 | Dữ liệu test đặt tên `TagA`…`TagQ` — dữ liệu giả cho field nghiệp vụ. Riêng bug này còn **ảnh hưởng kết quả**: tên tag ngắn/thuần ASCII **không chạm được** nhánh so khớp `LIKE` với tên tag tiếng Nhật, ký tự đặc biệt hay tên dài. | Dùng ≥ 1 bộ tên tag thật (JP + ký tự đặc biệt). |
| 15 | `[NIT]` | `TagMobileController` | Yokoten **chưa làm** — path xóa tag từ app mobile còn nguyên bug. **Không phải GAP của ticket này** (code không bị chạm) nhưng nếu QA lỡ test qua app sẽ báo "fix không ăn". | Ghi rõ trong kết quả test: *chỉ verify path web*. Tách ticket riêng → §6. |

> **AP-2 / AP-4 / AP-5 / AP-6: không dính.** Root cause đã xác định rõ (không phải symptom-only); fix đúng là generic chứ không phải check cụ thể ngụy trang; không có over-coverage tầng downstream; mục 3 dev-impact đã liệt kê đủ **7 caller**.
> **Spec đã tìm được** — không phát sinh flag thiếu spec.

---

## 5. TCs đề xuất bổ sung (14)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | [kho-tcs/fa012-quanlythe-タグ管理.md](../../kho-tcs/fa012-quanlythe-タグ管理.md) (nhóm "Xóa tag", 25 TC) + [kho-tcs/fa008-broadcast-メッセージ配信.md](../../kho-tcs/fa008-broadcast-メッセージ配信.md) |
| Vùng regression phát hiện từ kho | `TC-TAG-107` (chuỗi cleanup **8 điểm** gồm `sync_elasticsearch`) · `TC-TAG-108…111` (gỡ tag ở đầu/giữa/cuối/hết list) · `TC-TAG-106` (Boundary 100 tag) · `TC-TAG-116` (race 2 tab) · `TC-TAG-119` (tag đã xóa trên app mobile) · `TC-TAG-120` (cross analysis modal filter) — **không TC nào trong bộ 10 TC ở BƯỚC 0 chạm tới `sync_elasticsearch`, quy mô lớn, race, hay app mobile** |
| Conflict expected vs kho | **Không.** `TC-TAG-108/109/110` ("chỉ gỡ id, KHÔNG xóa bản ghi") khớp NEW-1; `TC-TAG-111` ("xóa hết tag → `filters_v2` bị XÓA") khớp NEW-2. Không mâu thuẫn. |
| GAP dùng lại TC kho (không viết mới) | **G9/Q6 Boundary quy mô** → dùng lại `TC-TAG-106` *"Xóa NHIỀU TAG: 100 tag cùng lúc, mỗi tag có friend + action + filter"* — chỉ cần **thêm điều kiện: trong 100 tag có ≥ 3 tag nằm ở bản ghi filter lệch dạng**. · **Q7 mobile** → dùng lại `TC-TAG-119`, chạy **sau khi** có ticket fix `TagMobileController`, không thuộc vòng test này. · **Race** → dùng lại `TC-TAG-116`. |
| Xác nhận chống trùng | Đã đối chiếu **10 TC** ở BƯỚC 0 + 2 file kho — **không TC đề xuất nào trùng** theo 4 yếu tố. |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-COMPATLEGACY001-02 | UI | COMPAT-LEGACY-001 | Xóa tag → dọn bộ lọc | Abnormal | manual | Tất cả | Bộ lọc lưu tag dạng **id đơn lẻ (không phải mảng)** — xóa tag vẫn dọn đúng, không văng | Bot Standard/Pro. Có TagR (sẽ xóa) + TagS (giữ). Seed 2 broadcast: (1) `bc-scalar` có bộ lọc tag mà `tags_search` lưu **1 id trần** (không bọc mảng) là TagR; (2) `bc-chuan` có bộ lọc `[TagR, TagS]` dạng chuẩn. Ghi lại nội dung 2 bản ghi trước khi xóa. | 1. Mở 「タグ管理」, xóa TagR bằng menu ⋮ → 「削除」, xác nhận<br>2. Quan sát màn báo kết quả xóa<br>3. Mở `bc-scalar` → 「絞り込み」→「設定」, quan sát điều kiện lọc theo tag<br>4. Mở `bc-chuan` → 「絞り込み」→「設定」, quan sát điều kiện lọc theo tag<br>5. Đối chiếu 2 bản ghi `filters_v2` tương ứng | `tags_search` bản ghi 1 = id đơn lẻ TagR (scalar); bản ghi 2 = `[TagR, TagS]` | Màn xóa tag báo thành công, không lỗi 500. `bc-scalar`: điều kiện lọc theo tag **không còn TagR** (bản ghi bị xóa vì hết tag). `bc-chuan`: modal chỉ còn TagS, `text_preview` dựng lại theo TagS. **Quan trọng — bản ghi scalar KHÔNG được làm chặn việc dọn `bc-chuan`**: nếu `bc-chuan` vẫn còn TagR thì fix chưa đạt. | | Lấp `G1` · Đánh giá spec: Spec không ghi — dạng scalar là dữ liệu lịch sử, cần leader chốt · Evidence: ảnh modal 2 broadcast + ảnh bản ghi `filters_v2` trước/sau · 1 trong 3 dạng lệch Dev tự test nhưng TC chưa phủ |
| TC-COMPATLEGACY001-03 | UI | COMPAT-LEGACY-001 | Xóa tag → dọn bộ lọc | Abnormal | manual | Tất cả | Bộ lọc **thiếu hẳn trường `tags_search`** — không bị xóa nhầm (case mất dữ liệu) | Bot Standard/Pro. Có TagT (sẽ xóa). Seed 1 broadcast `bc-thieu-truong` có bộ lọc tag nhưng phần dữ liệu **không có trường `tags_search`** (mô phỏng bản ghi hỏng/cũ), và tên/id của nó vô tình khớp chuỗi con với id TagT. Chụp lại toàn bộ nội dung bản ghi + `text_preview` trước khi xóa. Seed thêm 1 broadcast `bc-hople` có bộ lọc `[TagT, TagU]`. | 1. Mở 「タグ管理」, xóa TagT, xác nhận<br>2. Mở `bc-thieu-truong` → 「絞り込み」→「設定」, quan sát điều kiện lọc còn nguyên không<br>3. Đối chiếu bản ghi `filters_v2` của `bc-thieu-truong` với ảnh chụp trước khi xóa<br>4. Mở `bc-hople` → 「絞り込み」→「設定」, quan sát | Bản ghi thiếu trường `tags_search`; TagT bị xóa | **`bc-thieu-truong` phải còn NGUYÊN VẸN** — bản ghi không bị xóa, `text_preview` không bị ghi đè, điều kiện lọc trên modal hiển thị y như trước. (Code cũ **xóa nhầm** đúng ca này — Dev ghi rõ trong mục 2.) `bc-hople` được dọn bình thường còn TagU. Không lỗi 500. | | Lấp `G1` · **Case mất dữ liệu** — Dev tự nêu "thậm chí bị xóa nhầm khi thiếu trường tags_search" · Đánh giá spec: Spec không ghi — cần leader chốt · Evidence: ảnh bản ghi trước/sau + ảnh modal |
| TC-COMPATLEGACY001-04 | UI | COMPAT-LEGACY-001 | Xóa tag → dọn bộ lọc | Boundary | manual | Tất cả | **Trộn đủ 5 dạng dữ liệu lệch + 1 bản ghi hợp lệ đứng CUỐI** — vòng lặp vẫn chạy hết | Bot Standard/Pro. Có TagV (sẽ xóa) + TagW (giữ). Seed **6 bản ghi** `filters_v2` cùng chứa TagV, xếp theo id tăng dần, bản ghi **hợp lệ đứng cuối cùng**: (1) dạng `{id,name}` cũ; (2) id đơn lẻ scalar; (3) thiếu trường `tags_search`; (4) `tags_search` là chuỗi rỗng/`null`; (5) dạng lạ chưa từng gặp (vd mảng lồng mảng); (6) dạng chuẩn `[TagV, TagW]` — gắn vào broadcast `bc-cuoi-hang`. | 1. Mở 「タグ管理」, xóa TagV, xác nhận<br>2. Quan sát màn hình có báo lỗi gì không<br>3. Mở `bc-cuoi-hang` → 「絞り込み」→「設定」, quan sát điều kiện lọc theo tag<br>4. Rà lần lượt 6 bản ghi `filters_v2`<br>5. Mở log ứng dụng, tìm dòng `TagController::deletedDataTag filter_id ... error` | 5 dạng lệch + 1 dạng chuẩn, bản chuẩn xếp cuối | **`bc-cuoi-hang` (bản ghi cuối) PHẢI được dọn thành `[TagW]`** — đây là phép thử vòng lặp không abort. Bản ghi dạng (1)(2) được chuẩn hóa và dọn đúng. Bản ghi (3)(4)(5) không xử lý được thì **giữ nguyên, không bị xóa nhầm**, và **mỗi bản ghi lỗi sinh 1 dòng `Log::error` kèm `filter_id`**. Màn xóa tag không lỗi 500. | | Lấp `G1` + `G4` · **TC chốt của generic-fix** — phủ luôn dạng "chưa biết" (5) để verify nhánh fallback · Đánh giá spec: Spec không ghi · Evidence: ảnh 6 bản ghi trước/sau + trích log |
| TC-DATADB001-01 | UI | DATA-DB-001 | Xóa tag → phạm vi `WHERE` | Normal | manual | Tất cả | **2 bot cùng tên tag** — xóa tag ở bot A không đụng bộ lọc của bot B | 2 bot A và B cùng do 1 tài khoản quản lý (hoặc 2 tài khoản khác nhau), cả 2 đều Standard/Pro. Mỗi bot tạo tag **trùng tên** 「共通タグ」. Mỗi bot tạo 1 broadcast có bộ lọc chứa tag 「共通タグ」 của chính bot mình. Chụp lại bộ lọc của bot B trước khi thao tác. | 1. Chọn bot A, mở 「タグ管理」, xóa tag 「共通タグ」 của bot A, xác nhận<br>2. Ở bot A, mở broadcast → 「絞り込み」→「設定」, quan sát<br>3. **Chuyển sang bot B**, mở broadcast của bot B → 「絞り込み」→「設定」, quan sát<br>4. Ở bot B mở 「タグ管理」 kiểm tag 「共通タグ」 còn không<br>5. Đối chiếu bản ghi `filters_v2` của cả 2 bot | 2 bot, tag trùng tên 「共通タグ」 | Bot A: tag bị xóa, bộ lọc của broadcast bot A được dọn. **Bot B: tag 「共通タグ」 còn nguyên trong 「タグ管理」, bộ lọc broadcast bot B KHÔNG đổi một chút nào** (`text_preview` giữ nguyên, danh sách tag giữ nguyên). Không bản ghi `filters_v2` nào của bot B bị chạm. | | Lấp `Q1` · **RULE-07** — kiểm `WHERE bot_id` bằng 2 tài khoản, TC bắt buộc mà bộ 10 TC đang thiếu hoàn toàn · Đánh giá spec: Spec ghi rõ · Evidence: ảnh 2 bot trước/sau + ảnh bản ghi bot B |
| TC-DATADB001-02 | UI | DATA-DB-001 | Xóa tag → phạm vi `WHERE` | Abnormal | manual | Tất cả | **Id tag trùng chuỗi con GIỮA 2 BOT** — quét `LIKE` không được lan sang bot khác | 2 bot A, B. Bot A có tag id = **N** (vd `id=7`). Bot B có bộ lọc broadcast chứa tag id **chứa chuỗi con của N** (vd `id=17` và `id=70`). Ghi lại bộ lọc bot B (danh sách tag + `text_preview`) trước khi thao tác. | 1. Chọn bot A, mở 「タグ管理」, xóa tag id=N, xác nhận<br>2. Chuyển sang bot B, mở broadcast → 「絞り込み」→「設定」, quan sát danh sách tag<br>3. Đối chiếu bản ghi `filters_v2` của bot B với ảnh chụp trước<br>4. Ở bot B thử gửi test/xem số đối tượng nhận dự kiến của broadcast đó | Bot A xóa tag `id=7`; bot B có filter chứa `[17, 70]` | Bộ lọc bot B **giữ nguyên tuyệt đối**: vẫn đủ tag 17 và 70, `text_preview` không đổi, số đối tượng nhận dự kiến không đổi. Đây là phép thử kép: vừa kiểm `WHERE bot_id`, vừa kiểm guard `hasTagDeleted` chống false-positive `LIKE` **qua ranh giới bot**. | | Lấp `Q1` · Mở rộng NEW-7 (mới chỉ test trong 1 bot) sang **2 bot** — rủi ro thật là dọn nhầm dữ liệu KH khác · Đánh giá spec: Spec ghi rõ · Evidence: ảnh bộ lọc bot B trước/sau |
| TC-DATADB001-03 | Data | DATA-DB-001 | Khảo sát phạm vi dữ liệu hỏng | Normal | manual | product | **Thống kê toàn hệ thống** số bộ lọc còn trỏ tới tag không tồn tại (trước khi đóng ticket) | Có quyền truy vấn dữ liệu **production** (hoặc bản sao production gần nhất). Đã nắm danh sách bot đang hoạt động. | 1. Rà toàn bộ bản ghi bộ lọc loại tag trên production, đếm số bản ghi còn tham chiếu tag **đã bị xóa hoặc không tồn tại**<br>2. Nhóm kết quả theo bot và theo nơi dùng bộ lọc (broadcast / scenario / auto reply / action schedule / filter manager)<br>3. Với mỗi nhóm, mở thử 1–2 màn tương ứng trên production, xem điều kiện lọc hiển thị ra sao<br>4. Kiểm riêng broadcast **đang hẹn giờ chờ gửi** có nằm trong nhóm bị ảnh hưởng không<br>5. Lập bảng: số bot ảnh hưởng · số bản ghi · có broadcast chờ gửi bị ảnh hưởng không | Dữ liệu production thật | Có **con số cụ thể** về phạm vi dữ liệu hỏng trước fix, chia theo bot và theo nơi dùng. Kết luận rõ ràng: **có cần script dọn 1 lần hay không**, và nếu có thì phạm vi bao nhiêu bản ghi. Nếu tồn tại broadcast đang chờ gửi bị ảnh hưởng → **phải xử lý trước khi tới giờ gửi**. | | Lấp `G8` · **RULE-04** — bug làm sai dữ liệu thì phải có thống kê toàn hệ thống trước khi đóng ticket; Dev đã hoãn script recover và chờ human quyết · RULE-08 → product · Evidence: bảng thống kê + ảnh màn production |
| TC-REGSHARED001-03 | UI | REG-SHARED-001 | Xóa tag → entry point | Normal | manual | Tất cả | **Xóa cả NHÓM tag (cascade)** — bộ lọc của mọi tag trong nhóm đều được dọn | Bot Standard/Pro. Tạo 1 nhóm/thư mục tag chứa **≥ 4 tag**: TagX1…TagX4. Tạo 3 broadcast có bộ lọc lần lượt chứa TagX1, TagX2, TagX4 (mỗi bộ lọc còn thêm 1 tag ngoài nhóm để không bị xóa cả bản ghi). **Seed thêm 1 bản ghi bộ lọc lệch dạng** chứa TagX2 để mô phỏng dữ liệu thật. | 1. Mở 「タグ管理」, chọn nhóm tag vừa tạo<br>2. Xóa **cả nhóm** (menu của nhóm → 「削除」), xác nhận hộp thoại<br>3. Mở lần lượt 3 broadcast → 「絞り込み」→「設定」, quan sát điều kiện lọc theo tag<br>4. Kiểm 「タグ管理」 xem 4 tag đã biến mất chưa<br>5. Rà các bản ghi `filters_v2` liên quan | Nhóm 4 tag, 3 broadcast, 1 bản ghi lệch dạng | Cả 4 tag bị xóa. **Cả 3 broadcast đều được dọn**, mỗi bộ lọc chỉ còn tag ngoài nhóm, `text_preview` dựng lại đúng. Bản ghi lệch dạng **không làm hỏng việc dọn của các bộ lọc còn lại**. Không bản ghi nào còn trỏ tới TagX1…TagX4. | | Lấp `G2` · Entry point **thứ 3** (`manageTag` case `deleteGroup`) — path duy nhất chưa TC nào chạm, lại gọi `deletedDataTag` nhiều lần liên tiếp nên rủi ro cao nhất · Đánh giá spec: Spec ghi rõ (BR-10) · Evidence: ảnh 3 modal filter + ảnh màn tag |
| TC-STATEDEP001-02 | UI | STATE-DEP-001 | Xóa tag → chuỗi cleanup | Normal | manual | product | **Chuỗi cleanup chạy đủ tới bước CUỐI (đồng bộ tìm kiếm)** dù có bộ lọc lỗi chặn đường | Bot Standard/Pro. Tag 「CleanAll2」 đồng thời: gắn cho **5 friend**; nằm trong **1 bộ lọc lệch dạng** (dạng lạ, sẽ gây lỗi); nằm trong 1 action; có bản ghi giới hạn action. Ghi nhận trạng thái tìm kiếm/lọc bạn bè theo tag này trước khi xóa. | 1. Mở 「タグ管理」, xóa tag 「CleanAll2」, xác nhận<br>2. Mở màn 友だちリスト, lọc/tìm bạn bè theo tag vừa xóa — quan sát tag còn xuất hiện trong ô chọn tag không<br>3. Mở chi tiết 5 friend, xem tag đã biến mất chưa<br>4. Mở màn action liên quan, xem action còn trỏ tag đã xóa không<br>5. Kiểm các bảng của chuỗi cleanup, **đặc biệt bảng hàng đợi đồng bộ tìm kiếm** (`sync_elasticsearch`) có sinh bản ghi cho đủ 5 friend không<br>6. Chờ job đồng bộ chạy xong rồi lặp lại bước 2 | 1 tag × 5 friend × 1 bộ lọc lỗi × 1 action × 1 giới hạn action | Dù bộ lọc lệch dạng không dọn được, **toàn bộ chuỗi vẫn chạy tới bước cuối**: tag gỡ khỏi 5 friend, action được dọn, bản ghi giới hạn action bị xóa, và **hàng đợi đồng bộ tìm kiếm có đủ bản ghi cho 5 friend**. Sau khi job chạy, màn 友だちリスト **không còn tìm ra tag đã xóa**. | | Lấp `G3` · **dẫn từ `TC-TAG-107`** (kho FA-012, chuỗi 8 điểm) — bổ sung điều kiện "có 1 bộ lọc lỗi chặn đường" mà kho chưa có · **RULE-08 → product** vì phụ thuộc job nền đồng bộ · Đánh giá spec: Spec ghi rõ (BR-10 bước f) · Evidence: ảnh màn tìm kiếm trước/sau + ảnh hàng đợi đồng bộ |
| TC-OUTTRUTH001-02 | UI | OUT-TRUTH-001 | Xóa tag → thông báo kết quả | Abnormal | manual | Tất cả | **Có bộ lọc KHÔNG dọn được** — hệ thống báo gì cho người dùng, có ghi log không | Bot Standard/Pro. Tag TagY nằm trong 2 bộ lọc: 1 bản ghi **cố tình hỏng để không dọn được** (dạng lạ), 1 bản ghi hợp lệ của broadcast `bc-ok`. Chuẩn bị sẵn quyền xem log ứng dụng. | 1. Mở 「タグ管理」, xóa TagY, xác nhận<br>2. **Đọc kỹ thông báo hiện trên màn hình** — ghi lại nguyên văn<br>3. Mở `bc-ok` → 「絞り込み」→「設定」, xác nhận đã dọn đúng<br>4. Mở bản ghi bộ lọc hỏng, xác nhận nó **vẫn còn** TagY<br>5. Mở log ứng dụng tìm dòng `TagController::deletedDataTag filter_id ... error`<br>6. Đánh giá: người vận hành nhìn màn hình có biết được là còn dữ liệu chưa dọn không | 1 bộ lọc hỏng + 1 bộ lọc hợp lệ | `bc-ok` được dọn đúng (đây là mục tiêu của fix). Bộ lọc hỏng còn TagY. **Log ghi đúng 1 dòng `Log::error` kèm `filter_id` của bản ghi hỏng** — không được nuốt lỗi im lặng. Về thông báo: ghi lại **thực tế** màn hình hiển thị gì; nếu chỉ báo "thành công" mà không có dấu hiệu nào cho biết còn bản ghi chưa dọn → **ghi nhận là điểm cần quyết định spec** (§6), không tự kết luận Đạt/Không đạt. | | Lấp `G4` + `G6` · Mặt trái trực tiếp của `try/catch` per-record: dọn sót nay **im lặng** · Đánh giá spec: Spec không ghi — cần leader chốt có cần cảnh báo người dùng không · Evidence: ảnh thông báo + trích log + ảnh 2 bản ghi |
| TC-REGRUN001-01 | UI | REG-RUN-001 | Xóa tag ↔ broadcast đang hẹn giờ | Abnormal | manual | product | **Xóa tag đang được dùng bởi broadcast ĐẶT LỊCH chờ gửi** — tới giờ gửi ai nhận? | Bot Standard/Pro trên môi trường gửi được tin thật, có ≥ 3 friend test. Tạo 2 broadcast **đặt lịch gửi sau ~15 phút**, trạng thái chờ gửi: (1) `bc-1tag` lọc **chỉ theo TagZ** (tag duy nhất); (2) `bc-2tag` lọc theo `[TagZ, TagZZ]`. Ghi lại **số đối tượng nhận dự kiến** của cả 2 trước khi xóa. | 1. Mở 「タグ管理」, xóa TagZ, xác nhận<br>2. Mở tab 配信予約, mở `bc-1tag` → 「絞り込み」→「設定」, quan sát điều kiện lọc và **số đối tượng nhận dự kiến**<br>3. Làm tương tự với `bc-2tag`<br>4. **Chờ tới giờ gửi**<br>5. Kiểm 配信履歴 và **hộp thoại LINE của friend test**: ai thực sự nhận được tin<br>6. Đối chiếu số người nhận thực tế với số dự kiến ghi ở bước 2 | 2 broadcast hẹn giờ, 1 dùng tag duy nhất, 1 dùng 2 tag | `bc-2tag`: bộ lọc còn TagZZ, gửi đúng nhóm bạn bè có TagZZ. **`bc-1tag`: bản ghi bộ lọc đã bị xóa (theo hành vi NEW-2) → phải xác định rõ hệ thống làm gì** — gửi cho **TOÀN BỘ** friend (rủi ro gửi nhầm hàng loạt + tốn phí), không gửi, hay báo lỗi. Ghi lại hành vi **thực tế** ở màn 配信履歴 và trên LINE. Bất kể kết quả nào, nếu là "gửi toàn bộ" thì đây là **lỗi nghiêm trọng cần raise ticket riêng**. | | Lấp `G7` / `Q4` · **RULE-06** — verify tại LINE thật, không dừng ở màn admin · **RULE-08 → product** (gửi tin thật, tốn lượt gửi) · Đánh giá spec: Spec không ghi — hành vi khi bộ lọc của broadcast chờ gửi bị xóa · Evidence: ảnh 配信履歴 + ảnh màn hình LINE của friend test |
| TC-REGSHARED001-04 | UI | REG-SHARED-001 | Xóa tag → bộ lọc dùng chung | Normal | manual | Tất cả | Bộ lọc của **Scenario / Auto Reply / Friend List** được dọn đúng — kiểm **trên màn hình**, không chỉ DB | Bot Standard/Pro. Tag TagAA nằm trong bộ lọc của **4 nơi khác nhau**: 1 scenario (ステップ配信), 1 auto reply (自動応答), 1 bộ lọc lưu ở màn 友だちリスト, 1 action schedule. Mỗi bộ lọc còn thêm TagBB để bản ghi không bị xóa hẳn. Chụp màn 4 nơi trước khi xóa. | 1. Mở 「タグ管理」, xóa TagAA, xác nhận<br>2. Mở scenario → mở phần điều kiện lọc đối tượng, quan sát<br>3. Mở auto reply → mở phần điều kiện lọc, quan sát<br>4. Mở 友だちリスト → mở bộ lọc đã lưu, quan sát<br>5. Mở action schedule → mở điều kiện lọc, quan sát<br>6. Ở mỗi màn, kiểm cả **dòng mô tả điều kiện lọc** (text tóm tắt) lẫn danh sách tag trong modal | 1 tag × 4 nơi dùng bộ lọc | **Cả 4 màn** đều không còn TagAA trong modal lọc, và **dòng mô tả điều kiện lọc cũng không còn tên TagAA**. TagBB vẫn còn ở cả 4. Số đối tượng dự kiến của scenario/auto reply cập nhật theo. Không màn nào lỗi khi mở. | | Lấp `G5` · NEW-9 mới chỉ truy vấn DB 1 `parent_type` — TC này phủ **tầng UI** cho 4 `parent_type` còn lại (RULE-07) · Đánh giá spec: Spec ghi rõ · Evidence: ảnh 4 màn trước/sau |
| TC-DATACOUNT001-01 | UI | DATA-COUNT-001 | Xóa tag → các con số đếm | Normal | manual | Tất cả | Số bạn bè gắn tag + số đối tượng nhận dự kiến cập nhật đúng sau khi xóa tag | Bot Standard/Pro. TagCC gắn cho **7 friend**, TagDD gắn cho 3 friend (2 friend trùng cả 2 tag). 1 broadcast lọc theo `[TagCC, TagDD]` với điều kiện "chứa ít nhất 1 tag". **Ghi lại**: số bạn bè hiển thị ở màn 「タグ管理」 cho TagCC/TagDD, số kết quả khi lọc theo tag ở 友だちリスト, số đối tượng nhận dự kiến của broadcast. Seed thêm 1 bộ lọc lệch dạng chứa TagCC. | 1. Mở 「タグ管理」, xóa TagCC, xác nhận<br>2. Quan sát số bạn bè hiển thị cho TagDD ở màn 「タグ管理」<br>3. Mở 友だちリスト, lọc theo TagDD, đếm số kết quả<br>4. Mở chi tiết 2 friend từng có cả 2 tag, xem còn tag nào<br>5. Mở broadcast → 「絞り込み」→「設定」 và xem **số đối tượng nhận dự kiến** | 7 friend TagCC, 3 friend TagDD, 2 friend trùng | TagDD vẫn hiển thị **đúng 3** bạn bè. 友だちリスト lọc theo TagDD trả **đúng 3** kết quả. 2 friend trùng chỉ còn TagDD. Broadcast: bộ lọc còn TagDD, **số đối tượng nhận dự kiến giảm về đúng 3** (không còn tính 7 friend của TagCC). Không con số nào còn tính bạn bè của tag đã xóa. Bộ lọc lệch dạng không làm sai các con số này. | | Lấp `Q5` · Bước hard delete `tag_line_user` trước fix bị abort → mọi con số dựa trên nó đều sai; NEW-8 chỉ đếm ở DB, chưa ai kiểm trên màn hình · Đánh giá spec: Spec ghi rõ · Evidence: ảnh 3 màn có số đếm, trước/sau |
| TC-DATAREF001-03 | UI | DATA-REF-001 | Xóa tag → broadcast đã gửi | Abnormal | manual | Tất cả | **Broadcast ĐÃ GỬI XONG** — điều kiện lọc trong lịch sử có bị viết lại không? | Bot Standard/Pro. TagEE + TagFF. Tạo 1 broadcast lọc theo `[TagEE, TagFF]` và **gửi xong** (nằm ở 配信履歴, trạng thái đã gửi). **Chụp lại dòng mô tả điều kiện lọc trong lịch sử** + số người đã nhận. Tạo thêm 1 broadcast nháp cũng lọc `[TagEE, TagFF]` để đối chứng. | 1. Mở 配信履歴, ghi lại nguyên văn dòng mô tả điều kiện lọc của broadcast đã gửi<br>2. Mở 「タグ管理」, xóa TagEE, xác nhận<br>3. Quay lại 配信履歴, mở lại broadcast đã gửi, quan sát dòng mô tả điều kiện lọc và chi tiết bộ lọc<br>4. So sánh với ảnh chụp ở bước 1<br>5. Kiểm số người đã nhận có đổi không<br>6. Mở broadcast nháp để đối chứng (bản này **được phép** đổi) | 1 broadcast đã gửi + 1 broadcast nháp, cùng bộ lọc | Broadcast **nháp**: bộ lọc dọn còn TagFF (đúng như NEW-1). Broadcast **đã gửi**: cần chốt hành vi đúng — theo cách fix hiện tại thì bản ghi bộ lọc của nó **cũng bị viết lại**, làm **lịch sử gửi tin không còn phản ánh đúng điều kiện lúc gửi**. Ghi lại hành vi thực tế; số người đã nhận **không được đổi**. Nếu lịch sử bị sửa → đưa lên leader quyết định (§6), **không tự kết luận Đạt**. | | Lấp `Q2` (bổ sung `Abnormal` cho `DATA-REF-001` — RULE-01) · Fix quét theo `bot_id + type='tag'`, **không phân biệt bộ lọc thuộc bản ghi đã gửi hay chưa** · Đánh giá spec: **Spec không ghi** — đã đưa §6 · Evidence: ảnh 配信履歴 trước/sau |
| TC-ENV003-01 | UI | ENV-003 | Xóa tag → môi trường thật | Normal | manual | product | Chạy lại bộ kịch bản lõi trên **staging + production** với dữ liệu bộ lọc có sẵn | Tài khoản test trên staging và production. **Không seed dữ liệu lệch dạng** — dùng đúng bot có sẵn bộ lọc tag đã tồn tại từ lâu (dữ liệu lịch sử thật). Chọn 1 tag đang nằm trong ≥ 2 bộ lọc. Chụp lại trạng thái các bộ lọc trước khi xóa. | 1. Trên staging: xóa tag đã chọn, kiểm các bộ lọc liên quan trên màn hình + dòng mô tả điều kiện<br>2. Kiểm log có dòng lỗi dọn bộ lọc nào không — **nếu có, ghi lại `filter_id` và dạng dữ liệu của bản ghi đó**<br>3. Lặp lại toàn bộ trên production với 1 bot ít rủi ro (không có broadcast đang hẹn giờ)<br>4. Đối chiếu kết quả 2 môi trường với kết quả đã chạy ở local | Dữ liệu bộ lọc **có sẵn** trên staging/production, không seed | Kết quả trên staging và production **khớp với local**: bộ lọc được dọn đúng, không lỗi. **Giá trị chính của TC này**: nếu log phát sinh dòng lỗi dọn bộ lọc thì đó là **bằng chứng dạng dữ liệu lệch có thật ngoài môi trường thật** — thu thập lại để bổ sung vào `TC-COMPATLEGACY001-04`. Không có lỗi nào cũng là kết luận có giá trị. | | Lấp `Q3` · **RULE-08** — 9/9 TC hiện chạy `local`, mà dữ liệu lệch dạng là **dữ liệu lịch sử production**, local dựng mới không có → local chỉ chứng minh code xử lý được shape tự bịa · Đánh giá spec: Spec ghi rõ · Evidence: ảnh 2 môi trường + trích log |

**Dòng `GAP`/`RISK` không đề xuất TC mới:**

| Mã | Lý do |
|---|---|
| `G9` / `Q6` | TC đã tồn tại (**NEW-11**), chỉ **chưa chạy** → yêu cầu chạy, không viết TC mới. Boundary quy mô lớn dùng lại `TC-TAG-106` của kho (thêm điều kiện: trong 100 tag có ≥ 3 tag nằm ở bản ghi bộ lọc lệch dạng). |
| `Q7` (`SYNC-APP-001`) | `TagMobileController` **không bị chạm code** trong branch này, Dev khai ngoài phạm vi → viết TC vào bộ này là sai chỗ. Cần **tách ticket riêng**; khi có ticket thì dùng lại `TC-TAG-119` của kho. Xem §6. |
| `G10` | Đã đủ TC và có `pass` — không cần bổ sung. |

---

## 6. Spec update needed

| # | Điểm cần chốt | Vì sao phát sinh | Ai chốt |
|---|---|---|---|
| 1 | **Bộ lọc của broadcast ĐÃ GỬI XONG có được phép bị viết lại khi xóa tag không?** | Fix quét theo `bot_id + type='tag'`, **không phân biệt bản ghi thuộc broadcast đã gửi hay chưa gửi** → lịch sử gửi tin có thể không còn phản ánh đúng điều kiện lọc tại thời điểm gửi. Spec [BR-10](../../spec-features/admin/tag-management/feature-spec.md) không nói tới trạng thái của `parent`. Phát hiện khi viết `TC-DATAREF001-03`. | Leader + Dev |
| 2 | **Broadcast đang HẸN GIỜ mà bộ lọc bị xóa (do tag duy nhất bị xóa) thì tới giờ gửi cho ai?** | NEW-2 xác nhận bản ghi bộ lọc **bị xóa** khi hết tag. Không spec nào mô tả broadcast `wait_to_send` mất bộ lọc thì gửi toàn bộ, không gửi, hay lỗi. Rủi ro **gửi nhầm hàng loạt + tốn phí**. Phát hiện khi viết `TC-REGRUN001-01`. | Leader + Dev |
| 3 | **Dọn sót bộ lọc có cần báo cho người vận hành không?** | `try/catch` per-record khiến bản ghi lỗi bị bỏ qua **âm thầm** trong khi UI báo xóa tag thành công. Hiện chỉ có `Log::error` — người vận hành không có cách nào biết. Cần chốt: đủ log, hay phải hiện cảnh báo trên màn hình. | Leader |
| 4 | **Bản ghi chỉ khớp `LIKE` do trùng chuỗi con nay được GIỮ NGUYÊN** — xác nhận là hành vi mong muốn, không phải bug | Dev tự nêu đây là **thay đổi hành vi so với code cũ** (chủ ý, tránh mất dữ liệu). NEW-7 đang assert theo hành vi mới nhưng `Trạng thái đánh giá spec` để trống → chưa ai chính thức duyệt. | Leader |
| 5 | **Phạm vi test: có mở rộng đủ 6 `parent_type` không?** | Câu hỏi do **chính Studio nêu** trong note của NEW-9 và trong `assumptions` của tab Thông tin, **chưa ai trả lời** mà TC vẫn được đánh `pass`. Ticket chỉ report broadcast nhưng fix chạm cả `step_message` / `auto_reply` / `modal_action` / `action_schedule` / `filter_manager`. | Leader |
| 6 | **`TagMobileController::deletedDataTag` — tách ticket riêng hay gộp?** | Bản sao **cùng pattern lỗi**, Dev khai yokoten *"chưa sửa, không thuộc phạm vi ticket web này"*. Nếu không tách ticket thì lỗi này **không có ai theo dõi**: xóa tag từ app mobile vẫn để lại bộ lọc bẩn. | Leader + Dev |
| 7 | **Có chạy script dọn dữ liệu cũ không, và khi nào?** | Dev xác nhận dữ liệu bị bỏ sót trước fix **không tự dọn** và **không còn sự kiện nào kích hoạt dọn lại** — gồm chính broadcast trong ticket. Đây cũng là lý do **không thể verify fix bằng data cũ của ticket**. Kết quả `TC-DATADB001-03` là đầu vào cho quyết định này. | Leader + Dev |
| 8 | **Chuẩn hóa dữ liệu `tags_search` về 1 định dạng duy nhất?** | Fix chỉ **chấp nhận** mọi dạng lúc dọn, **không migrate** dữ liệu về dạng chuẩn → hệ thống tiếp tục tồn tại ≥ 4 dạng lưu trữ song song, mỗi chỗ đọc đều phải tự ép kiểu (đã có **10 chỗ** như vậy). Mỗi chỗ đọc mới trong tương lai là 1 chỗ có thể lặp lại đúng bug này. | Leader + Dev |
