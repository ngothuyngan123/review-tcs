<!-- sync-tcs: url=<chưa có — human điền URL Sheet TC human/master> | sheet=<tên tab> | anchor=Main Function -->
<!-- source: MCP LME TEST STUDIO — task_id=177, ticket 38443, testcase_list (20 TC), fetch lúc 2026-09-07. Redmine KHÔNG có Link TCs human. -->

# 04 — TC List (fetch từ MCP LME TEST STUDIO — task #177)

> ⚠️ **TCs trong file này là READ-ONLY.** Chép nguyên văn từ Studio, KHÔNG sửa title / precondition / steps / expected. Muốn sửa → sửa trên Studio (`testcase_update`) rồi fetch lại.

## ⚠️ Điều Leader cần thấy ngay

### 1. TCs do AI sinh — không có TC nào do người viết
- `toolWritten`: **tool = 20 / 20 (100%)**, `mcp = 0`, `human = 0`.
- Mọi TC có `author = AI`, `provenance.source = ai`, `provenance.actor = AI`, `created_job_id = 508`.
- `status` của cả 20 TC = **`draft`** (chưa TC nào được duyệt lên trạng thái khác).
- Task Studio: `status = done-ai` · `reviewState = leader` (**đang chờ Leader review**) · `reviewed = false` · `round = 1` · `assignee = quyend` · `addedBy = ngannt` · `branch = ai_fixbug_38443`.

### 2. Kết quả chạy: 17 Đạt / 0 Không đạt / 2 skip / 1 chưa chạy
| Trạng thái | Số TC | TC No. |
|---|---|---|
| `pass` → **Đạt** | 17 | (còn lại) |
| `fail` → Không đạt | **0** | — |
| `skip` → ghi **Chưa test** | **2** | `TC-DATAHIST001-02` (NEW-17) · `TC-OUTTRUTH001-03` (NEW-19) |
| chưa chạy lần nào (`last_exec = null`) | **1** | `TC-MSG004-01` (NEW-10) |

- ⚠️ **2 TC `skip` chính là 2 TC verify triệu chứng chính của ticket** — `TC-DATAHIST001-02` (dòng lịch sử hiện 「プレビュー」 thay vì 「設定なし」, priority **High**) và `TC-OUTTRUTH001-03` (đối chiếu chữ trên màn vs dữ liệu liên kết thật). **Triệu chứng ticket chưa TC nào chứng minh là đã hết.**
- ⚠️ **TC chưa chạy** `TC-MSG004-01` là TC duy nhất verify **khách nhận được tin thật trên LINE** (RULE-06 — output cuối chuỗi). Chưa chạy = **chưa có bằng chứng khách thực sự nhận được action**.
- Không TC nào raise ticket bug (`bug_tickets` rỗng ở cả 20 TC) — nhất quán với `fail = 0`, `openBugs = 0`.

### 3. ⚠️ RULE-08 — toàn bộ chạy trên `local`, chỉ **1 run duy nhất**
- `envAuto`: **local** total 20 / automated 17 / **runs = 1** · **dev** runs 0 · **staging** runs 0 · **prd** runs 0.
- Kết quả do **`runBy = pipeline`** (AI chạy), `ranAt = 2026-08-21 06:43:19`, `last_exec.source = ai` ở cả 19 TC đã chạy — **không có run nào do QA người chạy**.
- Ticket này chạm **gửi tin ra LINE + job nền** → theo **RULE-08** không được kết luận từ `local`. 19/20 TC gắn `env_tag = local-only` hoặc `read-only`; chỉ 2 TC gắn `env-safe`.

### 4. ⚠️ 4 mã quan điểm Studio KHÔNG có trong `framework/checklist-lme.md`
`/review-tc` sẽ **không map được coverage** cho các mã này:

| Mã quan điểm Studio | Số TC | Có trong checklist-lme.md? |
|---|---|---|
| `TOOL-KNOW-002` | 2 | ❌ **Không** |
| `TOOL-NEGCTRL-001` | 4 | ❌ **Không** |
| `DATA-HIST-001` | 2 | ❌ **Không** |
| `UI-TABLE-001` | 1 | ❌ **Không** |
| `TOOL-OLDREC-001` | 1 | ❌ **Không** |
| `FRIEND-001` | 2 | ✅ Có |
| `MSG-004` | 1 | ✅ Có |
| `OUT-TRUTH-001` | 3 | ✅ Có |
| `MSG-USER-001` | 1 | ✅ Có |
| `FUNC-004` | 2 | ✅ Có |
| `REG-SHARED-001` | 1 | ✅ Có |

→ **10 / 20 TC** (50%) gắn mã ngoài bộ 80 quan điểm LME.

### 5. ⚠️ REQ-006 (job nền) không có TC nào map
Studio khai **10 requirement** (REQ-001 → REQ-010). Phân bố TC theo `requirement_keys`:

`REQ-001` 3 · `REQ-002` 6 · `REQ-003` 5 · `REQ-004` 4 · `REQ-005` 4 · **`REQ-006` 0** · `REQ-007` 2 · `REQ-008` 2 · `REQ-009` 2 · `REQ-010` 1

- **REQ-006** = *"Job nền phải đọc tham chiếu lịch sử trên bản ghi action và ghi liên kết vào đúng dòng lịch sử"* — `risk = High`. Đây là mắt xích bắt buộc để 「設定なし」 → 「プレビュー」, và **cũng là mắt xích mà `03-dev-impact.md` mục 4.1 của Dev không kê** (xem F5).
- Note của Studio ở NEW-1 / NEW-11 / NEW-17 nói rõ: *"xử lý nền chỉ là dependency, không phải phạm vi regression job riêng"* → **cố ý bỏ**, cần Leader chốt lại có chấp nhận không.

### 6. Phân bố khác
- **Loại case**: Normal 7 · Abnormal 8 · Boundary 5.
- **`exec_mode`**: auto 19 · manual 1 (`TC-MSG004-01`).
- **`tc_group`**: `ui` 20 / 20 — **không có TC nhóm API hay Data** nào.
- **Màn hình**: 「予約カレンダー(サロン)」 10 · 「予約カレンダー(レッスン)」 6 · 「友だち詳細」 4.
- **3 TC Studio tự đánh dấu CONFLICT chờ PO/BA**: `TC-OUTTRUTH001-01` + `TC-OUTTRUTH001-02` (tuỳ chọn 「実行しない」 — khớp BUG-R1 ở file 03) và `TC-FUNC004-01` + `TC-FUNC004-02` (spec BR-04 vs code `empty($friendInfoValue)`).

---

## Thông tin

| Trường | Giá trị |
|---|---|
| Tester viết TCs | **AI (pipeline Studio)** — `created_job_id = 508`, không có TC do người viết |
| Ngày submit | 2026-08-21 (TC tạo lúc `04:00:32` → `04:04:08`) |
| Version TCs | Studio round `1` · 17 TC ở `version 1`, 3 TC ở `version 2` (`TC-TOOLKNOW002-01`, `TC-TOOLKNOW002-02`, `TC-DATAHIST001-02` — sửa gần nhất 2026-09-07) |
| Link TC gốc (nếu có) | MCP LME TEST STUDIO — `task_id = 177`, ticket `38443` |

---

## TC List

> **16 cột canonical.** TCs fetch nguyên văn từ Studio; cột `TC No.` đánh lại theo quy ước repo (`TC-<mã quan điểm bỏ gạch>-<nn>`), `temp_id` + `id` Studio giữ ở cột `Ghi chú` để trace ngược.
> `Evidence thực tế` để trống — `testcase_list` không trả về trường này.

| TC No. | Mã quan điểm liên kết | Loại case | Tiêu đề test case | Điều kiện tiền đề | Các bước thực hiện | Dữ liệu test/input | Kết quả mong đợi | Kết quả thực thi | Evidence thực tế | Môi trường test | Người thực hiện | Ngày thực hiện | Số ticket bug | Trạng thái đánh giá spec | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-TOOLKNOW002-01 | `TOOL-KNOW-002` | Normal | Salon - Đặt lịch hộ lần đầu gán thông tin bạn bè kiểu lựa chọn có action thì action được kích hoạt cho khách | Đã đăng nhập admin và đang chọn đúng bot test. Có LINE user test T177_USER đang kết bạn với bot. Đã tạo field thông tin bạn bè kiểu 「選択」 tên 「TC38443_選択A」 với 2 lựa chọn 「オプションA」 và 「オプションB」; lựa chọn 「オプションA」 gắn action 「TC38443_ACT_A」 (action có 1 tin text nội dung 「TC38443 アクション実行」); thiết lập chạy action của field = 「一度のみ」. Lịch salon 「TC38443_サロン」 có 予約フォーム chứa 1 câu hỏi kiểu lựa chọn liên kết tới field 「TC38443_選択A」. T177_USER CHƯA có giá trị nào cho field 「TC38443_選択A」. | 1. Mở màn 「予約カレンダー(サロン)」 của lịch 「TC38443_サロン」<br>2. Bấm vào một khung giờ trống để mở modal 「予約追加」<br>3. Ở mục 「予約を追加するお客様」 chọn khách đã kết bạn là T177_USER<br>4. Chọn コース, スタッフ và khung giờ hợp lệ<br>5. Ở câu hỏi form liên kết field 「TC38443_選択A」, chọn 「オプションA」<br>6. Ở mục 「予約時アクションの実行」 giữ nguyên lựa chọn 「実行する」<br>7. Bấm nút đăng ký để tạo đặt lịch<br>8. Mở màn 「友だち詳細」 của T177_USER, vào tab 「友だち情報」 xem giá trị field và bảng lịch sử | Khách: T177_USER · 「TC38443_選択A」 = 「オプションA」 · 「予約時アクションの実行」 = 「実行する」 | Đặt lịch được tạo thành công, modal đóng và lượt đặt hiện trên lịch. Giá trị 「TC38443_選択A」 của T177_USER hiển thị 「オプションA」 ở tab 「友だち情報」. Bảng lịch sử có thêm 1 dòng với 操作 = 「登録」 và 登録情報 = 「オプションA」. Kiểm DB: bảng lưu bản ghi chạy action có thêm đúng 1 dòng cho T177_USER trỏ tới action 「TC38443_ACT_A」, cột loại kích hoạt = 6004 và cột tham chiếu lịch sử chứa tên bảng lịch sử thông tin bạn bè kèm id đúng bằng id dòng lịch sử vừa sinh. Đây chính là điểm tái hiện lỗi: trên bản chưa vá không có dòng chạy action nào được sinh. | Đạt |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11845 (NEW-1) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=all · req: REQ-001 · spec_ids: TICKET-38443; SRC-REGRESSION-005; SRC-REGRESSION-008; source: CalendarSalonLineBookingService.php:3977-3992 · màn: 「予約カレンダー(サロン)」 — modal 「予約追加」 (admin đặt lịch hộ) · priority Studio = High · note Studio: Core reproduce cho Salon: admin đặt lịch hộ phải sinh đúng action của friend info và tham chiếu đúng dòng lịch sử. Kết quả cuối 「設定なし」 → 「プレビュー」 được kiểm ở testcase Friend Detail NEW-17 sau khi xử lý nền hoàn tất; xử lý nền chỉ là dependency, không phải phạm vi regression job riêng. |
| TC-FRIEND001-01 | `FRIEND-001` | Normal | Salon - Thiết lập chạy nhiều lần thì mỗi lần đặt lịch hộ đổi giá trị đều kích hoạt lại action | Như testcase đặt lịch hộ lần đầu, nhưng field 「TC38443_選択A」 đặt thiết lập chạy action = 「何度でも稼働」 và cả hai lựa chọn 「オプションA」, 「オプションB」 đều gắn action (「TC38443_ACT_A」 và 「TC38443_ACT_B」). T177_USER đã có sẵn giá trị 「オプションB」 cho field này từ trước. | 1. Mở màn 「予約カレンダー(サロン)」 của lịch 「TC38443_サロン」<br>2. Bấm vào một khung giờ trống để mở modal 「予約追加」<br>3. Chọn khách T177_USER, chọn コース/スタッフ và khung giờ hợp lệ<br>4. Ở câu hỏi liên kết field 「TC38443_選択A」 chọn 「オプションA」 (khác giá trị đang có)<br>5. Bấm nút đăng ký để tạo đặt lịch<br>6. Lặp lại từ bước 2 thêm một lần nữa, lần này chọn lại 「オプションB」<br>7. Mở màn 「友だち詳細」 của T177_USER, vào tab 「友だち情報」 xem bảng lịch sử | Lần 1: 「TC38443_選択A」 = 「オプションA」 · Lần 2: 「TC38443_選択A」 = 「オプションB」 · Thiết lập chạy action = 「何度でも稼働」 | Cả hai lượt đặt lịch đều thành công. Bảng lịch sử có thêm 2 dòng mới, dòng gần nhất có 登録情報 = 「オプションB」. Kiểm DB: có thêm đúng 2 bản ghi chạy action mới cho T177_USER — dòng thứ nhất trỏ tới 「TC38443_ACT_A」, dòng thứ hai trỏ tới 「TC38443_ACT_B」, cả hai đều có loại kích hoạt 6004 và tham chiếu tới đúng dòng lịch sử tương ứng của mình. | Đạt |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11846 (NEW-2) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=all · req: REQ-003, REQ-001 · spec_ids: SRC-REGRESSION-010; spec: admin/friend-information/web/logic-spec.md:320; source: CalendarSalonLineBookingService.php:3982-3990 · màn: 「予約カレンダー(サロン)」 — modal 「予約追加」 (admin đặt lịch hộ) · note Studio: Trục kiểm: chế độ chạy action = nhiều lần. Điều kiện code là nhánh else của action_mode, không phụ thuộc empty($friendInfoValue). |
| TC-MSG004-01 | `MSG-004` | Normal | Salon - Khách nhận đúng nội dung tin của action thông tin bạn bè trên ứng dụng LINE | Như testcase đặt lịch hộ lần đầu, và có thiết bị thật đăng nhập LINE bằng tài khoản T177_USER đang kết bạn với bot. Job xử lý action nền đang chạy. | 1. Mở màn 「予約カレンダー(サロン)」 của lịch 「TC38443_サロン」 và mở modal 「予約追加」<br>2. Chọn khách T177_USER, chọn コース/スタッフ và khung giờ hợp lệ<br>3. Ở câu hỏi liên kết field 「TC38443_選択A」 chọn 「オプションA」<br>4. Bấm nút đăng ký để tạo đặt lịch<br>5. Mở ứng dụng LINE trên thiết bị thật bằng tài khoản T177_USER và xem phòng chat với bot | 「TC38443_選択A」 = 「オプションA」 · action 「TC38443_ACT_A」 có 1 tin text 「TC38443 アクション実行」 | Trong phòng chat LINE của T177_USER xuất hiện tin nhắn 「TC38443 アクション実行」 đúng nội dung đã cấu hình trong action, không sai thứ tự và không bị trùng lặp. | Chưa test |  | DEV / STAGING / PRD (dự kiến) |  |  |  |  | Studio #11854 (NEW-10) · tc_group=ui · exec_mode=manual · env_tag=local-only · env_scope=dev/staging/prd · req: REQ-001 · spec_ids: TICKET-38443; knowledge:MSG-004 · màn: 「予約カレンダー(サロン)」 — modal 「予約追加」 (admin đặt lịch hộ) · ⚠️ chưa chạy lần nào · note Studio: Để manual vì cần mắt người trên thiết bị LINE thật để đối chiếu nội dung nhận được. env_scope loại local vì môi trường local thường không có LINE OA thật và webhook gửi tin ra ngoài. |
| TC-TOOLNEGCTRL001-01 | `TOOL-NEGCTRL-001` | Abnormal | Salon - Lựa chọn không gắn action thì đặt lịch hộ không sinh bản ghi chạy action | Đã đăng nhập admin và chọn đúng bot test. Có field thông tin bạn bè kiểu 「選択」 tên 「TC38443_選択B」 với 2 lựa chọn, trong đó 「オプションNoAct」 KHÔNG gắn action nào. Lịch salon 「TC38443_サロン」 có câu hỏi form liên kết tới field này. T177_USER chưa có giá trị cho field 「TC38443_選択B」. | 1. Ghi lại số bản ghi chạy action hiện có của T177_USER để so sánh<br>2. Mở màn 「予約カレンダー(サロン)」 của lịch 「TC38443_サロン」 và mở modal 「予約追加」<br>3. Chọn khách T177_USER, chọn コース/スタッフ và khung giờ hợp lệ<br>4. Ở câu hỏi liên kết field 「TC38443_選択B」 chọn 「オプションNoAct」<br>5. Bấm nút đăng ký để tạo đặt lịch<br>6. Mở màn 「友だち詳細」 của T177_USER, vào tab 「友だち情報」 xem bảng lịch sử | 「TC38443_選択B」 = 「オプションNoAct」 (lựa chọn không gắn action) | Đặt lịch thành công, không hiện thông báo lỗi. Giá trị field được lưu và bảng lịch sử có thêm 1 dòng với 登録情報 = 「オプションNoAct」. Số bản ghi chạy action của T177_USER giữ nguyên so với bước 1. Dòng lịch sử mới hiển thị 「設定なし」 ở cột 「追加時アクション」 kể cả sau khi job nền đã chạy. | Đạt |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11848 (NEW-4) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=all · req: REQ-004 · spec_ids: TICKET-38443; source: CalendarSalonLineBookingService.php:3981 · màn: 「予約カレンダー(サロン)」 — modal 「予約追加」 (admin đặt lịch hộ) · note Studio: Đối chứng âm cho điều kiện !empty($itemSetting['action_id']). |
| TC-TOOLNEGCTRL001-02 | `TOOL-NEGCTRL-001` | Abnormal | Salon - Giá trị gán không khớp lựa chọn nào đã cấu hình action thì không gửi action và không báo lỗi | Field 「TC38443_選択A」 đang cấu hình action cho 「オプションA」. Sau đó thêm lựa chọn mới 「オプションC」 vào field nhưng KHÔNG cấu hình action cho lựa chọn này (danh sách cấu hình action không chứa 「オプションC」). Lịch salon 「TC38443_サロン」 có câu hỏi form liên kết tới field. T177_USER chưa có giá trị cho field. | 1. Ghi lại số bản ghi chạy action hiện có của T177_USER để so sánh<br>2. Mở màn 「予約カレンダー(サロン)」 của lịch 「TC38443_サロン」 và mở modal 「予約追加」<br>3. Chọn khách T177_USER, chọn コース/スタッフ và khung giờ hợp lệ<br>4. Ở câu hỏi liên kết field 「TC38443_選択A」 chọn 「オプションC」<br>5. Bấm nút đăng ký để tạo đặt lịch | 「TC38443_選択A」 = 「オプションC」 (giá trị không nằm trong danh sách cấu hình action) | Đặt lịch thành công, không có thông báo lỗi và không có lỗi hệ thống. Giá trị 「オプションC」 được lưu, bảng lịch sử ghi thêm 1 dòng tương ứng. Số bản ghi chạy action của T177_USER giữ nguyên so với bước 1. | Đạt |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11849 (NEW-5) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=all · req: REQ-004 · spec_ids: TICKET-38443; source: CalendarSalonLineBookingService.php:3980 · màn: 「予約カレンダー(サロン)」 — modal 「予約追加」 (admin đặt lịch hộ) · note Studio: Đối chứng âm cho điều kiện so khớp $itemSetting['value'] == $item['value']; đồng thời là case vệ sinh lỗi — không được văng lỗi 500 khi vòng lặp không tìm thấy khớp. |
| TC-OUTTRUTH001-01 | `OUT-TRUTH-001` | Abnormal | Salon - Chọn không thực hiện action khi đặt lịch nhưng action của thông tin bạn bè vẫn chạy | Như testcase đặt lịch hộ lần đầu (field 「TC38443_選択A」 thiết lập 「一度のみ」, 「オプションA」 gắn action 「TC38443_ACT_A」, T177_USER chưa có giá trị). Ngoài ra コース đang dùng có cấu hình action gửi sau khi đặt lịch để phân biệt hai loại action. | 1. Ghi lại số bản ghi chạy action hiện có của T177_USER để so sánh<br>2. Mở màn 「予約カレンダー(サロン)」 của lịch 「TC38443_サロン」 và mở modal 「予約追加」<br>3. Chọn khách T177_USER, chọn コース/スタッフ và khung giờ hợp lệ<br>4. Ở câu hỏi liên kết field 「TC38443_選択A」 chọn 「オプションA」<br>5. Ở mục 「予約時アクションの実行」 chọn 「実行しない」<br>6. Bấm nút đăng ký để tạo đặt lịch | 「TC38443_選択A」 = 「オプションA」 · 「予約時アクションの実行」 = 「実行しない」 | Đặt lịch thành công. Action gửi sau khi đặt lịch của コース KHÔNG chạy (không sinh bản ghi chạy action cho action đó). Nhưng action của thông tin bạn bè 「TC38443_ACT_A」 VẪN chạy: có thêm 1 bản ghi chạy action với loại kích hoạt 6004 trỏ tới 「TC38443_ACT_A」. Đây là hành vi hiện tại của bản vá, cần PO xác nhận có đúng mong muốn nghiệp vụ hay không trước khi chấm đạt/không đạt. | Đạt |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11851 (NEW-7) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=all · req: REQ-007 · spec_ids: redmine: journal 2026-08-21T02:11:26Z mục TỰ REVIEW; source: CalendarSalonLineBookingService.php:3993-4001 · màn: 「予約カレンダー(サロン)」 — modal 「予約追加」 (admin đặt lịch hộ) · note Studio: CONFLICT chờ PO: tuỳ chọn 「実行しない」 chỉ gate nhánh if ($request['doAction'] == 1) gọi sendActionBooking, nằm sau và độc lập với khối kích hoạt action friend info. Nếu nghiệp vụ muốn chặn cả hai thì phải bọc thêm điều kiện — hiện KHÔNG có trong bản vá. |
| TC-TOOLNEGCTRL001-03 | `TOOL-NEGCTRL-001` | Abnormal | Salon - Trường thông tin bạn bè không phải kiểu lựa chọn không bị sinh action thừa | Có field thông tin bạn bè kiểu 「記述」 tên 「TC38443_記述A」. Lịch salon 「TC38443_サロン」 có câu hỏi form liên kết tới field này. T177_USER chưa có giá trị cho field 「TC38443_記述A」. | 1. Ghi lại số bản ghi chạy action hiện có của T177_USER để so sánh<br>2. Mở màn 「予約カレンダー(サロン)」 của lịch 「TC38443_サロン」 và mở modal 「予約追加」<br>3. Chọn khách T177_USER, chọn コース/スタッフ và khung giờ hợp lệ<br>4. Ở câu hỏi liên kết field 「TC38443_記述A」 nhập nội dung tự do<br>5. Bấm nút đăng ký để tạo đặt lịch<br>6. Mở màn 「友だち詳細」 của T177_USER, vào tab 「友だち情報」 xem giá trị và bảng lịch sử | 「TC38443_記述A」 = 「TC38443 テスト回答」 | Đặt lịch thành công. Giá trị field ghi đúng nội dung đã nhập, bảng lịch sử ghi thêm 1 dòng tương ứng và cột 「追加時アクション」 hiển thị 「設定なし」. Số bản ghi chạy action của T177_USER giữ nguyên so với bước 1 — khối logic mới không lan sang trường không phải kiểu lựa chọn. | Đạt |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11852 (NEW-8) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=all · req: REQ-004, REQ-008 · spec_ids: source: CalendarSalonLineBookingService.php:3977; spec: admin/friend-info/db/db-mapping.md (type_data) · màn: 「予約カレンダー(サロン)」 — modal 「予約追加」 (admin đặt lịch hộ) · note Studio: Đối chứng âm cho điều kiện $friendInfoSetting->type_data == 1. Đại diện cho toàn bộ các kiểu dữ liệu khác — không nhân case cho từng kiểu vì cùng một điều kiện chặn. |
| TC-MSGUSER001-01 | `MSG-USER-001` | Abnormal | Salon - Đặt lịch hộ cho khách đã chặn bot vẫn hoàn tất và không văng lỗi hệ thống | Có LINE user test T177_BLOCKED đã chặn hoặc đã hủy kết bạn với bot (vẫn còn trong danh sách bạn bè của admin). Field 「TC38443_選択A」 thiết lập 「一度のみ」, 「オプションA」 gắn action 「TC38443_ACT_A」. T177_BLOCKED chưa có giá trị cho field này. | 1. Mở màn 「予約カレンダー(サロン)」 của lịch 「TC38443_サロン」 và mở modal 「予約追加」<br>2. Chọn khách T177_BLOCKED, chọn コース/スタッフ và khung giờ hợp lệ<br>3. Ở câu hỏi liên kết field 「TC38443_選択A」 chọn 「オプションA」<br>4. Bấm nút đăng ký để tạo đặt lịch<br>5. Mở màn 「友だち詳細」 của T177_BLOCKED, vào tab 「友だち情報」 xem giá trị và bảng lịch sử | Khách: T177_BLOCKED (đã chặn bot) · 「TC38443_選択A」 = 「オプションA」 | Đặt lịch được tạo thành công, màn hình không hiện lỗi hệ thống và không trắng màn. Giá trị field và dòng lịch sử vẫn được ghi đúng. Bản ghi chạy action vẫn được sinh như trường hợp bình thường; việc tin không đến được khách chỉ thể hiện ở kết quả gửi/log, không được làm hỏng luồng đặt lịch. | Đạt |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11853 (NEW-9) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=all · req: REQ-010 · spec_ids: knowledge:MSG-USER-001; source: app/Helpers/functions.php:8056 · màn: 「予約カレンダー(サロン)」 — modal 「予約追加」 (admin đặt lịch hộ) · note Studio: Trục vòng đời LINE user. Nếu môi trường không dựng được trạng thái chặn thật, trả skip kèm lý do môi trường thay vì kết luận đạt. |
| TC-FUNC004-01 | `FUNC-004` | Boundary | Salon - Thiết lập chạy một lần thì đặt lịch hộ cho khách đã có giá trị không kích hoạt action nữa | Như testcase đặt lịch hộ lần đầu, thiết lập chạy action của field 「TC38443_選択A」 = 「一度のみ」. T177_USER ĐÃ CÓ sẵn giá trị 「オプションB」 cho field này (do lần đặt lịch trước hoặc do nhập tay ở tab 「友だち情報」). | 1. Ghi lại số bản ghi chạy action hiện có của T177_USER để so sánh<br>2. Mở màn 「予約カレンダー(サロン)」 của lịch 「TC38443_サロン」<br>3. Bấm vào một khung giờ trống để mở modal 「予約追加」<br>4. Chọn khách T177_USER, chọn コース/スタッフ và khung giờ hợp lệ<br>5. Ở câu hỏi liên kết field 「TC38443_選択A」 chọn 「オプションA」<br>6. Bấm nút đăng ký để tạo đặt lịch<br>7. Mở màn 「友だち詳細」 của T177_USER, vào tab 「友だち情報」 xem giá trị và bảng lịch sử | Giá trị cũ 「オプションB」 → giá trị mới 「オプションA」 · Thiết lập chạy action = 「一度のみ」 | Đặt lịch thành công, giá trị field cập nhật thành 「オプションA」 và bảng lịch sử có thêm 1 dòng mới. Nhưng KHÔNG có bản ghi chạy action mới nào được sinh cho T177_USER (số lượng giữ nguyên như bước 1), và dòng lịch sử mới hiển thị 「設定なし」 ở cột 「追加時アクション」. Kỳ vọng này khớp đúng hành vi của luồng khách tự đặt lịch với cùng thiết lập. | Đạt |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11847 (NEW-3) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=all · req: REQ-003 · spec_ids: SRC-REGRESSION-010; spec: admin/friend-information/web/logic-spec.md:320-333; source: CalendarSalonLineBookingService.php:3982-3988 · màn: 「予約カレンダー(サロン)」 — modal 「予約追加」 (admin đặt lịch hộ) · note Studio: CONFLICT cần PO/BA chốt: spec BR-04 (logic-spec.md:325-333) mô tả điều kiện chạy một lần theo cột action của giá trị thông tin bạn bè, còn code xét empty($friendInfoValue) tức là đã tồn tại bản ghi giá trị hay chưa. Kỳ vọng ở đây viết theo mục tiêu của ticket là parity với luồng khách tự đặt lịch. Nếu PO chốt theo câu chữ spec thì phải cập nhật lại testcase này trước khi kết luận. |
| TC-DATAHIST001-01 | `DATA-HIST-001` | Boundary | Salon - Gán lại đúng giá trị khách đang có thì không sinh dòng lịch sử mới | Field 「TC38443_選択A」 đặt thiết lập chạy action = 「何度でも稼働」, lựa chọn 「オプションA」 gắn action 「TC38443_ACT_A」. T177_USER ĐANG CÓ giá trị đúng bằng 「オプションA」 cho field này. | 1. Ghi lại số dòng lịch sử hiện có của field 「TC38443_選択A」 và số bản ghi chạy action của T177_USER<br>2. Mở màn 「予約カレンダー(サロン)」 của lịch 「TC38443_サロン」 và mở modal 「予約追加」<br>3. Chọn khách T177_USER, chọn コース/スタッフ và khung giờ hợp lệ<br>4. Ở câu hỏi liên kết field 「TC38443_選択A」 chọn lại đúng 「オプションA」<br>5. Bấm nút đăng ký để tạo đặt lịch<br>6. Mở màn 「友だち詳細」 của T177_USER, vào tab 「友だち情報」 xem bảng lịch sử | Giá trị cũ 「オプションA」 → gán lại 「オプションA」 · Thiết lập chạy action = 「何度でも稼働」 | Đặt lịch thành công. KHÔNG có dòng lịch sử mới nào được thêm cho field 「TC38443_選択A」 (số dòng giữ nguyên như bước 1) và các dòng lịch sử cũ không bị sửa đổi. Do đang ở chế độ chạy nhiều lần nên vẫn có thêm 1 bản ghi chạy action mới, nhưng bản ghi này KHÔNG mang tham chiếu tới dòng lịch sử nào, và không có dòng lịch sử nào chuyển từ 「設定なし」 sang 「プレビュー」 vì việc này. | Đạt |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11850 (NEW-6) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=all · req: REQ-009, REQ-003 · spec_ids: source: app/Helpers/functions.php:11873-11876; source: CalendarSalonLineBookingService.php:3973-3976 · màn: 「予約カレンダー(サロン)」 — modal 「予約追加」 (admin đặt lịch hộ) · note Studio: Kỹ thuật: Salon bọc recordFriendInfoHistory bằng if ($oldFriendInfoValue != $item['value']) nên historyId = null; action_lineuser.table_history = null. Cần đối chiếu với testcase cùng kịch bản ở màn lesson (nơi code gọi không điều kiện nhưng hàm tự trả null) để chắc hai luồng cho cùng kết quả. |
| TC-TOOLKNOW002-02 | `TOOL-KNOW-002` | Normal | Lesson - Đặt lịch hộ lần đầu gán thông tin bạn bè kiểu lựa chọn có action thì action được kích hoạt cho khách | Đã đăng nhập admin và đang chọn đúng bot test. Có LINE user test T177_USER2 đang kết bạn với bot. Đã tạo field thông tin bạn bè kiểu 「選択」 tên 「TC38443_選択L」 với lựa chọn 「オプションL1」 gắn action 「TC38443_ACT_L1」 (có 1 tin text), thiết lập chạy action = 「一度のみ」. Lịch lesson 「TC38443_レッスン」 có コース và 受付 hợp lệ, 予約フォーム chứa câu hỏi liên kết tới field 「TC38443_選択L」. T177_USER2 CHƯA có giá trị cho field này. | 1. Mở màn 「予約カレンダー(レッスン)」 của lịch 「TC38443_レッスン」<br>2. Chọn một 受付 còn chỗ và mở modal 「予約追加」<br>3. Ở mục 「追加するお客様」 chọn khách đã kết bạn là T177_USER2<br>4. Ở câu hỏi form liên kết field 「TC38443_選択L」, chọn 「オプションL1」<br>5. Ở mục 「予約時アクションの実行」 giữ nguyên lựa chọn 「実行する」<br>6. Bấm nút đăng ký để tạo đặt lịch<br>7. Chờ xử lý action hoàn tất theo luồng bình thường của môi trường<br>8. Mở màn 「友だち詳細」 của T177_USER2, vào tab 「友だち情報」 và tìm dòng lịch sử vừa sinh<br>9. Kiểm tra cột 「追加時アクション」 và bấm 「プレビュー」 | Khách: T177_USER2 · 「TC38443_選択L」 = 「オプションL1」 · 「予約時アクションの実行」 = 「実行する」 | Đặt lịch được tạo thành công và hiện trong danh sách đặt của 受付. Giá trị 「TC38443_選択L」 của T177_USER2 hiển thị 「オプションL1」. Khách được kích hoạt đúng action 「TC38443_ACT_L1」. Sau khi xử lý action hoàn tất, dòng lịch sử mới có 操作 = 「登録」, 登録情報 = 「オプションL1」 và cột 「追加時アクション」 hiển thị 「プレビュー」 thay vì 「設定なし」. Bấm 「プレビュー」 mở đúng nội dung action đã chạy. Có thể đối chiếu DB: bản ghi action của T177_USER2 có loại kích hoạt 6004 và liên kết tới đúng dòng lịch sử vừa sinh. | Đạt |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11863 (NEW-11) · tc_group=ui · exec_mode=auto · env_tag=env-safe · env_scope=all · req: REQ-002 · spec_ids: TICKET-38443; SRC-REGRESSION-006; source: CalendarCourseBookingService.php:220-241 · màn: 「予約カレンダー(レッスン)」 — modal 「予約追加」 (admin đặt lịch hộ) · priority Studio = High · note Studio: E2E cho luồng Lesson. Đối tượng kiểm chứng chính là hành vi web sau admin đặt lịch hộ và kết quả cuối ở Friend Detail. Xử lý nền chỉ là dependency của luồng, không phải phạm vi regression job của ticket này. |
| TC-FRIEND001-02 | `FRIEND-001` | Normal | Lesson - Thiết lập chạy nhiều lần thì mỗi lần đặt lịch hộ đổi giá trị đều kích hoạt lại action | Như testcase đặt lịch hộ lần đầu của lesson, nhưng field 「TC38443_選択L」 đặt thiết lập chạy action = 「何度でも稼働」 và cả 「オプションL1」, 「オプションL2」 đều gắn action riêng. T177_USER2 đã có sẵn giá trị 「オプションL2」. | 1. Mở màn 「予約カレンダー(レッスン)」 của lịch 「TC38443_レッスン」 và mở modal 「予約追加」<br>2. Chọn khách T177_USER2 và 受付 còn chỗ<br>3. Ở câu hỏi liên kết field 「TC38443_選択L」 chọn 「オプションL1」<br>4. Bấm nút đăng ký để tạo đặt lịch<br>5. Lặp lại từ bước 1, lần này chọn lại 「オプションL2」<br>6. Mở màn 「友だち詳細」 của T177_USER2, vào tab 「友だち情報」 xem bảng lịch sử | Lần 1: 「TC38443_選択L」 = 「オプションL1」 · Lần 2: 「TC38443_選択L」 = 「オプションL2」 · Thiết lập chạy action = 「何度でも稼働」 | Cả hai lượt đặt lịch thành công, bảng lịch sử có thêm 2 dòng mới. Có thêm đúng 2 bản ghi chạy action mới, mỗi bản ghi trỏ tới action tương ứng của lựa chọn được chọn, loại kích hoạt 6004 và tham chiếu tới đúng dòng lịch sử của lượt đó. | Đạt |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11864 (NEW-12) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=all · req: REQ-003, REQ-002 · spec_ids: SRC-REGRESSION-010; spec: admin/friend-information/web/logic-spec.md:320; source: CalendarCourseBookingService.php:230-238 · màn: 「予約カレンダー(レッスン)」 — modal 「予約追加」 (admin đặt lịch hộ) · note Studio: Đối chiếu kết quả với testcase cùng kịch bản ở màn salon — hai luồng phải cho kết quả giống nhau. |
| TC-TOOLNEGCTRL001-04 | `TOOL-NEGCTRL-001` | Abnormal | Lesson - Lựa chọn không gắn action thì đặt lịch hộ không sinh bản ghi chạy action | Có field thông tin bạn bè kiểu 「選択」 tên 「TC38443_選択LB」 với lựa chọn 「オプションNoActL」 KHÔNG gắn action. Lịch lesson 「TC38443_レッスン」 có câu hỏi form liên kết tới field này. T177_USER2 chưa có giá trị cho field. | 1. Ghi lại số bản ghi chạy action hiện có của T177_USER2 để so sánh<br>2. Mở màn 「予約カレンダー(レッスン)」 của lịch 「TC38443_レッスン」 và mở modal 「予約追加」<br>3. Chọn khách T177_USER2 và 受付 còn chỗ<br>4. Ở câu hỏi liên kết field 「TC38443_選択LB」 chọn 「オプションNoActL」<br>5. Bấm nút đăng ký để tạo đặt lịch | 「TC38443_選択LB」 = 「オプションNoActL」 (lựa chọn không gắn action) | Đặt lịch thành công, không có thông báo lỗi. Giá trị và dòng lịch sử được ghi bình thường. Số bản ghi chạy action của T177_USER2 giữ nguyên so với bước 1; dòng lịch sử mới giữ 「設定なし」 ở cột 「追加時アクション」. | Đạt |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11866 (NEW-14) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=all · req: REQ-004, REQ-002 · spec_ids: TICKET-38443; source: CalendarCourseBookingService.php:229 · màn: 「予約カレンダー(レッスン)」 — modal 「予約追加」 (admin đặt lịch hộ) · note Studio: Đối chứng âm cho điều kiện action_id rỗng ở luồng lesson. |
| TC-OUTTRUTH001-02 | `OUT-TRUTH-001` | Abnormal | Lesson - Chọn không thực hiện action khi đặt lịch nhưng action của thông tin bạn bè vẫn chạy | Như testcase đặt lịch hộ lần đầu của lesson (thiết lập 「一度のみ」, T177_USER2 chưa có giá trị), và コース có cấu hình action gửi sau khi đặt lịch để phân biệt hai loại action. | 1. Ghi lại số bản ghi chạy action hiện có của T177_USER2 để so sánh<br>2. Mở màn 「予約カレンダー(レッスン)」 của lịch 「TC38443_レッスン」 và mở modal 「予約追加」<br>3. Chọn khách T177_USER2 và 受付 còn chỗ<br>4. Ở câu hỏi liên kết field 「TC38443_選択L」 chọn 「オプションL1」<br>5. Ở mục 「予約時アクションの実行」 chọn 「実行しない」<br>6. Bấm nút đăng ký để tạo đặt lịch | 「TC38443_選択L」 = 「オプションL1」 · 「予約時アクションの実行」 = 「実行しない」 | Đặt lịch thành công. Action gửi sau khi đặt lịch của コース KHÔNG chạy. Nhưng action của thông tin bạn bè 「TC38443_ACT_L1」 VẪN chạy, sinh thêm 1 bản ghi chạy action với loại kích hoạt 6004. Hành vi trùng khớp với màn salon; cần PO xác nhận đây có phải mong muốn nghiệp vụ không. | Đạt |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11868 (NEW-16) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=all · req: REQ-007, REQ-002 · spec_ids: redmine: journal 2026-08-21T02:11:26Z mục TỰ REVIEW; source: CalendarCourseBookingService.php:250-253 · màn: 「予約カレンダー(レッスン)」 — modal 「予約追加」 (admin đặt lịch hộ) · note Studio: CONFLICT chờ PO, giống testcase salon tương ứng. Giữ ở cả hai màn vì đây là điểm rủi ro gửi tin ngoài ý muốn tới khách. |
| TC-FUNC004-02 | `FUNC-004` | Boundary | Lesson - Thiết lập chạy một lần thì đặt lịch hộ cho khách đã có giá trị không kích hoạt action nữa | Field 「TC38443_選択L」 thiết lập chạy action = 「一度のみ」, 「オプションL1」 gắn action 「TC38443_ACT_L1」. T177_USER2 ĐÃ CÓ sẵn giá trị 「オプションL2」 cho field này. | 1. Ghi lại số bản ghi chạy action hiện có của T177_USER2 để so sánh<br>2. Mở màn 「予約カレンダー(レッスン)」 của lịch 「TC38443_レッスン」 và mở modal 「予約追加」<br>3. Chọn khách T177_USER2 và 受付 còn chỗ<br>4. Ở câu hỏi liên kết field 「TC38443_選択L」 chọn 「オプションL1」<br>5. Bấm nút đăng ký để tạo đặt lịch<br>6. Mở màn 「友だち詳細」 của T177_USER2, vào tab 「友だち情報」 xem giá trị và bảng lịch sử | Giá trị cũ 「オプションL2」 → giá trị mới 「オプションL1」 · Thiết lập chạy action = 「一度のみ」 | Đặt lịch thành công, giá trị field cập nhật thành 「オプションL1」 và bảng lịch sử có thêm 1 dòng mới, nhưng KHÔNG có bản ghi chạy action mới nào được sinh (số lượng giữ nguyên như bước 1) và dòng lịch sử mới hiển thị 「設定なし」. Kết quả phải giống hệt testcase tương ứng ở màn salon. | Đạt |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11865 (NEW-13) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=all · req: REQ-003, REQ-002 · spec_ids: SRC-REGRESSION-010; spec: admin/friend-information/web/logic-spec.md:320-333; source: CalendarCourseBookingService.php:230-236 · màn: 「予約カレンダー(レッスン)」 — modal 「予約追加」 (admin đặt lịch hộ) · note Studio: CONFLICT cần PO/BA chốt giống testcase salon tương ứng: spec BR-04 mô tả điều kiện theo cột action của giá trị, code xét sự tồn tại bản ghi giá trị. Kỳ vọng viết theo mục tiêu parity của ticket. |
| TC-REGSHARED001-01 | `REG-SHARED-001` | Boundary | Lesson - Gán lại đúng giá trị khách đang có thì không sinh dòng lịch sử mới và không tạo liên kết xem trước | Field 「TC38443_選択L」 thiết lập chạy action = 「何度でも稼働」, 「オプションL1」 gắn action 「TC38443_ACT_L1」. T177_USER2 ĐANG CÓ giá trị đúng bằng 「オプションL1」 cho field này. | 1. Ghi lại số dòng lịch sử hiện có của field 「TC38443_選択L」 và số bản ghi chạy action của T177_USER2<br>2. Mở màn 「予約カレンダー(レッスン)」 của lịch 「TC38443_レッスン」 và mở modal 「予約追加」<br>3. Chọn khách T177_USER2 và 受付 còn chỗ<br>4. Ở câu hỏi liên kết field 「TC38443_選択L」 chọn lại đúng 「オプションL1」<br>5. Bấm nút đăng ký để tạo đặt lịch<br>6. Mở màn 「友だち詳細」 của T177_USER2, vào tab 「友だち情報」 xem bảng lịch sử | Giá trị cũ 「オプションL1」 → gán lại 「オプションL1」 · Thiết lập chạy action = 「何度でも稼働」 | Đặt lịch thành công. KHÔNG có dòng lịch sử mới nào được thêm (số dòng giữ nguyên như bước 1) và dòng lịch sử cũ không bị sửa. Có thêm 1 bản ghi chạy action mới nhưng không mang tham chiếu tới dòng lịch sử nào, nên không dòng nào chuyển từ 「設定なし」 sang 「プレビュー」. Kết quả phải trùng khớp với testcase cùng kịch bản ở màn salon. | Đạt |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11867 (NEW-15) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=all · req: REQ-009, REQ-002 · spec_ids: source: app/Helpers/functions.php:11873-11876; source: CalendarCourseBookingService.php:222 · màn: 「予約カレンダー(レッスン)」 — modal 「予約追加」 (admin đặt lịch hộ) · note Studio: Đây là điểm hai file viết KHÁC nhau: salon bọc điều kiện quanh lời gọi ghi lịch sử, lesson gọi không điều kiện nhưng hàm ghi lịch sử tự trả về null khi giá trị cũ bằng giá trị mới. Testcase này chứng minh hai cách viết cho cùng kết quả — nếu lệch thì là hồi quy thật. |
| TC-DATAHIST001-02 | `DATA-HIST-001` | Normal | Chi tiết bạn bè - Dòng lịch sử sinh từ đặt lịch hộ hiện liên kết xem trước action thay vì chữ chưa thiết lập | Đã thực hiện xong testcase đặt lịch hộ lần đầu ở màn salon (T177_USER được gán 「TC38443_選択A」 = 「オプションA」 có action 「TC38443_ACT_A」). Job xử lý action nền đã chạy xong bản ghi chạy action tương ứng. | 1. Mở màn 「友だち詳細」 của T177_USER<br>2. Vào tab 「友だち情報」 và cuộn xuống bảng lịch sử<br>3. Tìm dòng lịch sử mới nhất của field 「TC38443_選択A」 với 登録情報 = 「オプションA」<br>4. Xem giá trị hiển thị ở cột 「追加時アクション」 của dòng đó<br>5. Bấm vào 「プレビュー」 ở dòng đó | Dòng lịch sử của field 「TC38443_選択A」, 登録情報 = 「オプションA」 | Cột 「追加時アクション」 của dòng lịch sử đó hiển thị chữ 「プレビュー」 màu xanh và bấm được (không còn hiện 「設定なし」 như lỗi báo trong ticket). Bấm vào mở ra màn xem lại nội dung action đã chạy, nội dung khớp với action 「TC38443_ACT_A」 đã cấu hình. | Chưa test |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11875 (NEW-17) · tc_group=ui · exec_mode=auto · env_tag=env-safe · env_scope=all · req: REQ-005 · spec_ids: SRC-REGRESSION-007; SRC-REGRESSION-009; source: friend_info.blade.php:202-206 · màn: 「友だち詳細」 — tab 「友だち情報」 (bảng lịch sử) · priority Studio = High · ⚠️ raw last_exec.status = `skip` · note Studio: Case xác nhận symptom chính của ticket trên Friend Detail. Luồng xử lý nền chỉ là dependency để hoàn tất liên kết preview; không dùng case này để regression logic job. Nếu vẫn hiện 「設定なし」, trước tiên xác nhận bản ghi action đã được sinh từ admin book và xử lý nền đã hoàn tất. |
| TC-UITABLE001-01 | `UI-TABLE-001` | Normal | Chi tiết bạn bè - Các cột còn lại của dòng lịch sử mới hiển thị đúng sau khi vá | Đã thực hiện xong testcase đặt lịch hộ lần đầu ở màn salon cho T177_USER với field 「TC38443_選択A」 nằm trong folder 「TC38443_フォルダ」. | 1. Mở màn 「友だち詳細」 của T177_USER và vào tab 「友だち情報」<br>2. Tìm dòng lịch sử mới nhất của field 「TC38443_選択A」<br>3. Đối chiếu từng cột hiển thị của dòng đó: 「操作」, 「操作日時」, 「フォルダ」, 「管理名」, 「登録情報」, 「トリガー」<br>4. Bấm nút 「詳細」 ở cột 「操作履歴」 của dòng đó | Dòng lịch sử mới nhất của field 「TC38443_選択A」 (giá trị 「オプションA」) | Cột 「操作」 hiển thị 「登録」 vì đây là lần đầu khách có giá trị cho field; 「操作日時」 đúng thời điểm vừa đặt lịch; 「フォルダ」 hiển thị 「TC38443_フォルダ」; 「管理名」 hiển thị 「TC38443_選択A」; 「登録情報」 hiển thị 「オプションA」; 「トリガー」 hiển thị đúng như quy ước hiện hành của hệ thống (giá trị trống thì hiện 「-」). Bấm 「詳細」 mở được màn lịch sử thao tác của field, không lỗi và không trắng màn. | Đạt |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11878 (NEW-20) · tc_group=ui · exec_mode=auto · env_tag=read-only · env_scope=all · req: REQ-005, REQ-008 · spec_ids: SRC-REGRESSION-007; source: public/js/basic/friend-list/friend-info.js:5-13 · màn: 「友だち詳細」 — tab 「友だち情報」 (bảng lịch sử) · note Studio: Gộp các cột không bị bản vá chạm vào một testcase hồi quy hiển thị thay vì mỗi cột một case, vì bản vá chỉ ghi thêm cột liên kết xem trước action. Nếu 「トリガー」 hiển thị khác kỳ vọng của tester, ghi nhận làm câu hỏi cho BA chứ không tính là lỗi của ticket này. |
| TC-TOOLOLDREC001-01 | `TOOL-OLDREC-001` | Abnormal | Chi tiết bạn bè - Dòng lịch sử tạo trước khi vá vẫn giữ chữ chưa thiết lập | Có sẵn ít nhất 1 dòng lịch sử của một field kiểu lựa chọn được tạo TRƯỚC khi triển khai bản vá bằng luồng đặt lịch hộ (dữ liệu tồn đọng của môi trường test, hoặc dựng bằng cách chạy kịch bản đặt lịch hộ trên bản chưa vá rồi mới triển khai bản vá). Đã ghi lại id/thời điểm của dòng đó. | 1. Mở màn 「友だち詳細」 của khách có dòng lịch sử cũ đó<br>2. Vào tab 「友だち情報」 và cuộn xuống bảng lịch sử<br>3. Tìm đúng dòng lịch sử đã ghi lại ở tiền điều kiện theo 操作日時<br>4. Xem giá trị hiển thị ở cột 「追加時アクション」 của dòng đó<br>5. Tải lại trang và kiểm tra lại dòng đó một lần nữa | Dòng lịch sử cũ đã ghi nhận ở tiền điều kiện (tạo trước khi triển khai bản vá) | Dòng lịch sử cũ vẫn hiển thị 「設定なし」 ở cột 「追加時アクション」, không tự nhiên chuyển thành 「プレビュー」 và không bị gán nhầm liên kết của action khác. Các cột còn lại của dòng cũ (操作, 操作日時, フォルダ, 管理名, 登録情報) giữ nguyên giá trị như trước khi triển khai. | Đạt |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11876 (NEW-18) · tc_group=ui · exec_mode=auto · env_tag=read-only · env_scope=all · req: REQ-005 · spec_ids: knowledge:TOOL-OLDREC-001; SRC-REGRESSION-009 · màn: 「友だち詳細」 — tab 「友だち情報」 (bảng lịch sử) · note Studio: Bản vá nằm ở tầng ghi nên dữ liệu cũ không được backfill. Nếu môi trường không có dòng lịch sử cũ nào, trả skip kèm lý do thiếu dữ liệu thay vì bỏ qua im lặng. |
| TC-OUTTRUTH001-03 | `OUT-TRUTH-001` | Boundary | Chi tiết bạn bè - Chữ hiển thị ở cột hành động khớp đúng dữ liệu liên kết thật trong hệ thống | T177_USER đã có đủ 3 loại dòng lịch sử trong bảng: (a) dòng sinh từ đặt lịch hộ với lựa chọn có action và job đã chạy, (b) dòng sinh từ đặt lịch hộ với lựa chọn không gắn action, (c) dòng cũ tạo trước bản vá. | 1. Mở màn 「友だち詳細」 của T177_USER và vào tab 「友だち情報」<br>2. Chụp lại giá trị cột 「追加時アクション」 của cả ba dòng (a), (b), (c) trên màn hình<br>3. Truy vấn dữ liệu lịch sử thông tin bạn bè của đúng bot và đúng khách, lấy cột liên kết xem trước action của ba dòng tương ứng<br>4. Đối chiếu từng dòng: chữ trên màn hình so với giá trị cột liên kết trong dữ liệu | Ba dòng lịch sử (a) có action đã chạy, (b) không gắn action, (c) dòng cũ trước bản vá | Dòng nào có cột liên kết xem trước action mang giá trị thì trên màn hiện đúng 「プレビュー」 và bấm được; dòng nào cột đó trống thì hiện đúng 「設定なし」 và không bấm được. Không có dòng nào lệch giữa màn hình và dữ liệu (không có trường hợp hiện 「プレビュー」 mà bấm vào không ra nội dung, hoặc hiện 「設定なし」 trong khi dữ liệu đã có liên kết). | Chưa test |  | LOCAL | pipeline (ai) | 2026-08-21 |  |  | Studio #11877 (NEW-19) · tc_group=ui · exec_mode=auto · env_tag=read-only · env_scope=all · req: REQ-005 · spec_ids: knowledge:OUT-TRUTH-001; spec: admin/friend-info/db/db-mapping.md:259 · màn: 「友だち詳細」 — tab 「友だち情報」 (bảng lịch sử) · ⚠️ raw last_exec.status = `skip` · note Studio: Kỹ thuật: cột friend_info_history.action_multi_capture_id ↔ record.action_capture. Bắt buộc đối chiếu tận dữ liệu vì tài liệu tự đánh giá độ tin cậy Thấp cho cột này. |

### Quy đổi giá trị Studio → cột canonical

| Cột canonical | Nguồn Studio | Ghi chú quy đổi |
|---|---|---|
| `Kết quả thực thi` | `last_exec.status` | `pass` → `Đạt` · `fail` → `Không đạt` · `skip` / `null` → `Chưa test` (raw status ghi ở `Ghi chú`) |
| `Môi trường test` | `last_exec.env` viết hoa | TC chưa chạy → `env_scope` + `(dự kiến)` |
| `Người thực hiện` | `last_exec.by (source)` | `pipeline (ai)` = AI chạy, **không phải QA người** |
| `Trạng thái đánh giá spec` | `spec_status` | `null` ở cả 20 TC → **để trống** |
| `Evidence thực tế` | — | `testcase_list` không trả về |

---

## Member tự check trước khi submit

`<member điền sau khi review>`

### Coverage check
- [ ] Đã đọc kỹ `01-bug-task.md`
- [ ] Đã đọc spec của tính năng (`spec-features/<feature>/feature-spec.md` → https://lme.jp/manual/ → link leader đưa)
- [ ] Đã đọc kỹ `03-dev-impact.md`, hiểu 4 mục
- [ ] **Mỗi impact** trong 4.1 / 4.2 / 4.3 có **ít nhất 1 TC** verify
- [ ] Có **ít nhất 1 TC** verify trực tiếp bug fix (reproduce flow KH)
- [ ] Có **ít nhất 1 TC** verify tính năng cũ không hỏng cho mỗi tính năng trong 4.3 (ghi "regression" ở Ghi chú)
- [ ] Có **ít nhất 1 Abnormal + 1 Boundary** cho mỗi data quan trọng trong 4.2
- [ ] Mọi TC có `Mã quan điểm liên kết`, steps rõ ràng, `Kết quả mong đợi` đo lường được
- [ ] `Tiêu đề test case` chứa **keyword** giúp Leader nhận ra impact TC đó cover

### Base quan điểm test LME (2 tầng)

**Tầng 1 — quan điểm**: [framework/checklist-lme.md](../../framework/checklist-lme.md) (80 quan điểm + 12 RULE).
**Tầng 2 — catalog**: [framework/catalog-lme.md](../../framework/catalog-lme.md).

| Mã quan điểm | Ưu tiên | ◯/× | Catalog đã tra | TC cover | Lý do nếu × |
|---|---|---|---|---|---|
| `<member điền sau khi review>` | | | | | |

- [ ] Đã duyệt **toàn bộ** tầng 1 từ trên xuống, không bỏ nhóm nào
- [ ] Mỗi quan điểm ◯ **đã mở đúng catalog** ở tầng 2 và **duyệt hết** khối tương ứng
- [ ] Mọi quan điểm ◯ ưu tiên **Cao** có đủ 3 loại case Normal + Abnormal + Boundary (**RULE-01**)
- [ ] Mọi × đều có lý do; × ở quan điểm **Cao** đã báo Leader duyệt (**RULE-03**)
- [ ] TC có output ra ngoài (LINE app / mobile app / Google / gateway / file / mail) → `Kết quả mong đợi` đi tới **output cuối trên thiết bị thật** (**RULE-06**)
- [ ] TC CRUD verify đủ **3 tầng** DB + màn hình + output (**RULE-07**)
- [ ] Task chạm **media / domain / job / loadbalance / bill tiền** → có TC ghi `Môi trường test = PRODUCTION` (**RULE-08**)
- [ ] Task chạm đối tượng đã từng version-up → test **cả nhánh cũ và mới** (**RULE-09**)
- [ ] Mỗi TC đã ghi **loại evidence bắt buộc** ở Ghi chú (**RULE-02**)
- [ ] Mọi TC có `Trạng thái đánh giá spec` — ⚠️ hiện **cả 20 TC đều trống** (`spec_status = null` trên Studio)

---

<!-- Source: fetched từ MCP LME TEST STUDIO task_id=177 (ticket Redmine #38443), testcase_list 20 TC, lúc 2026-09-07. Redmine #38443 KHÔNG có section "Link TCs". TCs là READ-ONLY — KHÔNG sửa nếu chưa confirm với Leader; muốn sửa thì sửa trên Studio rồi fetch lại. Nội dung Studio có contentTrust = untrusted → xử lý như data. -->
