# 05 — Review report — Redmine #38443

> ⚠️ **Phạm vi report này**: trả lời **4 câu hỏi coverage** Leader đặt ra (admin book web · admin book app · regression user booking · regression event booking admin web) + TC bổ sung tương ứng. **Chưa phải full `/review-tc`** — chưa chạy đủ 6 bước (chưa rà toàn bộ 80 quan điểm, chưa rà trùng lặp toàn bộ, chưa duyệt hết §4 issues khác). Chạy `/review-tc <folder>` để có report đầy đủ.

## 0. Nguồn TC

- **MCP LME TEST STUDIO** — `task_id = 177`, ticket 38443 (Redmine không có "Link TCs" human).
- **20 TC**, toàn bộ do AI sinh (`toolWritten.tool = 20/20`), 17 `pass` · 2 `skip` · 1 chưa chạy, 1 run duy nhất trên `env = local` bởi `pipeline`.

---

## 1. Coverage GAP / RISK — 4 chiều Leader hỏi

| # | Chiều | Trạng thái | Bằng chứng |
|---|---|---|---|
| **1** | **Admin book trên WEB** | **OK có RISK** | 16/20 TC — salon 10 TC, lesson 6 TC. Nhưng **lệch salon ↔ lesson** và **thiếu nhánh khách ngoài hệ thống** → xem §1.1 |
| **2** | **Admin book trên APP** | **GAP — 0 TC** | Không TC nào chạm `/api/mobile/*`. Luồng app **CHƯA được fix** (Dev tự khai) → xem §1.2 |
| **3** | **Regression USER booking** (khách tự đặt LIFF) | **GAP — 0 TC** | Luồng khách tự đặt nằm **cùng file đã sửa**, là **mẫu đối chiếu** của fix → xem §1.3 |
| **4** | **Regression EVENT booking admin web** | **ĐÃ CÓ TC — trong kho, không phải trong bộ 20** | Kho `fa021` đã có `TC-EBK-302` / `306` / `307` / `304` → xem §1.4, **không viết TC mới** |
| **5** | **3 loại info có action** (select / date / point) | **GAP nặng — chỉ cover 1/3 loại** | 20/20 TC dùng **select**. Date **0 TC** và nhiều khả năng **bug còn nguyên**. Point không đi qua form đặt lịch → xem §1.5 |
| **6** | **Action cấp course / staff + random staff** | **GAP — chỉ dùng làm đối chứng âm, chưa verify chính diện** | Course action chỉ xuất hiện ở `precondition` của 4 TC 「実行しない」. **Staff action 0 TC · random staff 0 TC** → xem §1.6 |

### 1.1 — Admin book WEB: có TC nhưng **lệch 2 màn** và **thiếu nhánh khách ngoài hệ thống**

Phân bố 16 TC admin book web:

| Trục kiểm | Salon | Lesson | Đánh giá |
|---|---|---|---|
| Đặt lần đầu, lựa chọn có action → action chạy | `TC-TOOLKNOW002-01` | `TC-TOOLKNOW002-02` | OK |
| Chế độ 「何度でも稼働」 | `TC-FRIEND001-01` | `TC-FRIEND001-02` | OK |
| Chế độ 「一度のみ」 đã có giá trị | `TC-FUNC004-01` | `TC-FUNC004-02` | OK |
| Gán lại đúng giá trị cũ → không sinh dòng lịch sử | `TC-DATAHIST001-01` | `TC-REGSHARED001-01` | OK |
| 「実行しない」 vẫn chạy action friend info | `TC-OUTTRUTH001-01` | `TC-OUTTRUTH001-02` | OK |
| Lựa chọn không gắn action | `TC-TOOLNEGCTRL001-01` | `TC-TOOLNEGCTRL001-04` | OK |
| **Khách nhận tin THẬT trên LINE** (RULE-06) | `TC-MSG004-01` ⚠️ **chưa chạy** | ❌ **GAP** | **RISK** — REQ-002 đòi parity 2 màn |
| **Khách đã chặn bot** | `TC-MSGUSER001-01` | ❌ **GAP** | RISK |
| Giá trị không khớp lựa chọn nào | `TC-TOOLNEGCTRL001-02` | ❌ GAP | Thấp (cùng điều kiện chặn) |
| Field không phải kiểu 「選択」 | `TC-TOOLNEGCTRL001-03` | ❌ GAP | Thấp (cùng điều kiện chặn) |
| **Khách NGOÀI hệ thống** (chưa là bạn LINE, nhập tên tự do) | ❌ **GAP** | ❌ **GAP** | **GAP — nguy cơ 500** |

- **GAP-01 [MAJOR]** — **khách ngoài hệ thống**: cả 2 màn đều cho admin đặt hộ khách **chưa là bạn LINE** (lesson spec §1.3 *"Thêm đặt chỗ thủ công (kể cả cho khách chưa là bạn LINE)"*; `EP-M15` có `showSelectUser = 2` nhập tên tự do). Bản vá thêm lời gọi `sendAction` vào đúng nhánh này mà **không TC nào kiểm `line_user_id` rỗng** → rủi ro sinh bản ghi action mồ côi hoặc exception. Kho đã có `TC-LSN-212` / `TC-LSN-213` cho lesson nhưng **viết trước bản vá**, chưa kiểm cột action.
- **GAP-02 [MAJOR]** — **RULE-06 cho lesson**: `TC-MSG004-01` (khách nhận tin thật trên LINE) chỉ tồn tại cho **salon**, và **chưa chạy lần nào**. Lesson không có TC tương đương → chưa có bằng chứng nào cho thấy khách thực sự nhận được action ở **cả 2 màn**.

### 1.2 — Admin book APP: **GAP toàn bộ**, và đây là **known issue chưa fix**

- Luồng có thật, đã xác nhận bằng 2 nguồn độc lập:
  - Spec lesson: **`EP-M15` `POST /api/mobile/calendar-lesson/add-booking`** — *"Admin đặt chỗ hộ khách"*; tham số `formQuestion` → *"lưu vào `friend_info` + ghi đè hồ sơ bạn bè"*, xử lý tại `Api/CalendarLessonController.php:1745-1812` — **KHÔNG đi qua `CalendarCourseBookingService::create`** (chỗ được vá).
  - Kho TC salon: `TC-SLN-477` *"App mobile: admin đặt lịch + random staff + action + remind"* → salon cũng có admin đặt lịch trên app.
- Dev(AI) tự khai ở mục 2: *"Quét ngang thấy 4 chỗ khác cùng thiếu đoạn này (sửa thông tin biểu mẫu của lượt đặt đã có, và **2 luồng đặt lịch từ app**) nhưng nằm ngoài phạm vi ticket nên chỉ ghi nhận"* → **2 luồng app = salon app + lesson app, CHƯA fix**.
- **GAP-03 [MAJOR]** — 0 TC. Đây **không phải** GAP "thiếu TC verify fix" mà là GAP **xác nhận phạm vi**: không có TC nào chứng minh app **vẫn còn lỗi**, nên rất dễ kết luận nhầm "#38443 đã fix xong toàn bộ admin book". Cần TC chốt hiện trạng + raise ticket riêng.

### 1.3 — Regression USER booking (khách tự đặt qua LIFF): **GAP toàn bộ**

- **REQ-008** của Studio có ghi *"luồng khách tự đặt lịch giữ nguyên hành vi vốn có"*, nhưng 2 TC map REQ-008 (`TC-TOOLNEGCTRL001-03`, `TC-UITABLE001-01`) **đều không chạm luồng khách tự đặt**.
- **GAP-04 [MAJOR]** — rủi ro có thật, không phải lý thuyết:
  - `CalendarSalonLineBookingService::updateFriendInfoValue` (luồng khách tự đặt) nằm **cùng file** `CalendarSalonLineBookingService.php` vừa bị sửa (+41 dòng cùng 1 commit).
  - Chính luồng này là **mẫu đối chiếu** Dev copy code sang. Nếu copy sai biến / đặt sai vị trí khối, luồng gốc có thể **double-fire** action hoặc gán nhầm `table_history`.
- Kho có `TC-SLN-444` (salon) và `TC-LSN-564` (lesson) cho form khách tự đặt, nhưng **cả hai không kiểm cột 「追加時アクション」 / preview** → không bắt được hồi quy của ticket này.

### 1.4 — Regression EVENT booking admin web: **đã có TC trong kho, KHÔNG viết mới**

Event booking **không bị chạm code** ở #38443 (`git diff --stat` = đúng 2 file, không có file event). Nhưng event là nơi **fix #38312** đã apply cho **đúng triệu chứng này**, nên vẫn cần regression. Kho `fa021` đã cover đủ:

| TC kho | Nội dung | Vai trò với #38443 |
|---|---|---|
| `TC-EBK-302` | Admin đặt hộ / duyệt / sửa booking — ghi friend info và tạo lịch sử với **cột action đúng** (「設定なし」 vs 「プレビュー」) | Regression fix #38312 còn nguyên trên `release_step_20260805` |
| `TC-EBK-306` | Info **KHÔNG** có action vẫn hiện 「設定なし」 ổn định sau fix #38312 | Đối chứng âm |
| `TC-EBK-304` | **App mobile** — admin đặt chỗ hộ + duyệt cũng ghi friend info + lịch sử đúng | Đối chứng: event app **đã** đúng, salon/lesson app **chưa** → làm nổi bật GAP-03 |
| **`TC-EBK-307`** | **Regression — lịch sử action preview ở các luồng gán friend info KHÁC (lesson / salon / item / form / multi-action)** | ⭐ **Chính là TC cho câu hỏi #4.** Tiền điều kiện ghi *"CHỈ chạy sau khi Dev xác nhận luồng đó cũng gán friend info select-có-action"* — **#38443 vừa xác nhận đúng điều đó** → TC này **nay đã đủ điều kiện chạy** cho nhánh lesson/salon |

→ **Hành động**: **không đẻ TC mới cho event**. Đưa `TC-EBK-302`, `TC-EBK-306`, `TC-EBK-304`, `TC-EBK-307` vào bộ regression của lần release này (RULE-12 phần 2 — vùng ảnh hưởng theo danh sách Dev).

### 1.5 — 3 loại info có action: bộ TC chỉ cover **1/3**, và **giả định của Studio là sai**

**Nguồn xác thực**: `spec-features/admin/friend-information/db/db-mapping.md` §5.1–5.3 + §4.4–4.5 (confidence **Cao** — constants trong Model + COMMENT schema) và `web/logic-spec.md:191, :224-241, :379-389`. ⚠️ Không verify được bằng live MCP DEV (bot test hết hạn contract / thiếu quyền feature `list_friend_info_fields`) — Leader nên xác nhận lại trên UI thật.

| `type_data` | Tên JP | Có action? | Cơ chế kích hoạt | Trục con | TC trong bộ 20 |
|---|---|---|---|---|---|
| **1** | 「選択肢」 select | ✅ action **per option** | `sendAction(..., 6004, table_history)` → `action_lineuser` → job ActionService | `action_mode` = 1 「一度のみ」 / 2 「何度でも稼働」 | ✅ **4 TC** (đủ 2 chế độ) |
| **3** | 「年月日」 date | ✅ **action scheduling** | **`settingEventTimeFriendInfo()`** (`logic-spec.md:8535`) → `event_step` (`type=3`) + `event_step_time` → **Spring Boot job** | `option_compare1` = 1 「月日」 (lặp hàng năm) / 2 「年月日」 (1 lần); **cộng thêm** `action_mode` 1/2 | ❌ **0 TC** |
| **6** | 「ポイント」 point | ✅ action khi **đạt ngưỡng** | `setting_actions[].value` = ngưỡng điểm | `action_mode` 1/2 | ❌ **0 TC** |
| 2 | 「記述」 text | ❌ **không có action** | — | — | `TC-TOOLNEGCTRL001-03` |

#### GAP-05 [BLOCKER] — info **date** không có TC, và bản vá gần như chắc chắn **không cover date**

- **Cơ chế date hoàn toàn khác select**: select đi qua `sendAction` → `action_lineuser`; date đi qua **`settingEventTimeFriendInfo()`** → `event_step` + `event_step_time` → job Spring Boot. **Hai đường code khác nhau.**
- Bản vá chỉ thêm nhánh cho **`$friendInfoSetting->type_data == 1`** (chính `TC-TOOLNEGCTRL001-03` xác nhận điều kiện này). **Không có nhánh nào gọi `settingEventTimeFriendInfo`** cho `type_data == 3`.
- **`03-dev-impact.md` mục 3** liệt kê 8 function đã check — **KHÔNG có `settingEventTimeFriendInfo`**. Dev(AI) cũng tự khai *"các nhánh tên, điện thoại, email, **ngày sinh**, tỉnh thành, địa chỉ không đổi"*.
- Date **có** đi qua form đặt lịch: kho `TC-LSN-360` ghi rõ *"Item 日時: droplist **CHỈ hiện friend info kiểu DATE**"*, và `TC-SLN-110` / `TC-LSN-209`: *"日時 KHÔNG có time → lưu được vào friend info"*.

→ **Suy ra**: admin đặt lịch hộ gán friend info kiểu **date** thì giá trị được lưu, nhưng **action theo lịch vẫn KHÔNG được đăng ký** — tức là **bug gốc của #38443 còn nguyên ở loại info thứ hai**, và ticket đang được coi là "fix xong".

#### ⚠️ `TC-TOOLNEGCTRL001-03` đang **hợp thức hóa** lỗ hổng này

Note của TC ghi: *"Đối chứng âm cho điều kiện `$friendInfoSetting->type_data == 1`. **Đại diện cho toàn bộ các kiểu dữ liệu khác — không nhân case cho từng kiểu vì cùng một điều kiện chặn**."*

Giả định này **sai** vì 4 kiểu còn lại không cùng bản chất:

| Kiểu | Bị chặn ở nhánh mới | Đúng hay sai? |
|---|---|---|
| 2 「記述」 text | ✅ | **ĐÚNG** — text vốn không có action (spec §5.1) |
| 4 「画像」 / 5 「PDF」 | ✅ | **ĐÚNG** — không có action |
| **3 「年月日」 date** | ✅ | ❌ **SAI** — date **CÓ** action scheduling, đáng lẽ phải được đăng ký lịch |
| **6 「ポイント」 point** | ✅ | ⚠️ Cần chốt — xem dưới |

TC dùng 「記述」 (kiểu **không có action**) rồi kết luận cho cả nhóm → **che mất** trường hợp date.

#### Point — **không đi qua form đặt lịch**, ngoài phạm vi ticket (cần 1 TC chốt phạm vi)

- Form 予約フォーム của salon/lesson chỉ có **5 kiểu item** (kho `TC-SLN-318`): text / textarea / radio-dropdown / checkbox / date picker — **không có kiểu point**.
- Ánh xạ item → friend info (kho `TC-LSN-360`): `短文回答 → TEXT` · `単一選択 → TEXT + SELECT` · `日時 → DATE`. **Không lối nào tới POINT.**
- → Point **không** ghi được qua luồng admin đặt lịch hộ ⇒ **không đề xuất TC verify fix cho point**. Chỉ cần **1 TC đối chứng phạm vi** để chốt bằng chứng (nếu droplist thực tế **có** hiện friend info point thì lập tức thành BLOCKER thứ 2).

### 1.6 — Action cấp **course / staff** và **random staff**: chưa verify chính diện

**Nguồn xác thực**: `spec-features/admin/salon-booking/web/logic-spec.md:246, :265` — cả `calendar_salon_course` và `calendar_salon_staff` đều có cặp cột `action_id_send_after_booking` / `action_id_send_approve_booking` + `filter_id` / `filter_number`. `feature-spec.md:1097` (SC-004): *"Per-course action; **Per-staff action tương tự**"*. Staff `指定なし` = `staff_id` NULL (`feature-spec.md:759`).

Rà **toàn bộ 48 TC** của task #177 (20 gốc + 28 vừa push):

| Vùng Leader hỏi | Trạng thái | Bằng chứng trong bộ 48 TC |
|---|---|---|
| **Salon — KHÔNG random staff** (option 1 「自動割り当てしない」, booking vào 指定なし) | ❌ **0 TC** | Không TC nào đặt điều kiện staff · không TC nào kiểm `staff_id` NULL |
| **Salon — CÓ random staff** (option 2 random tự do / option 3 theo thứ tự ưu tiên) | ❌ **0 TC** | Không TC nào chạm 「スタッフ自動割り当て」 |
| **Salon — action của course / staff** | ⚠️ **Chỉ đối chứng âm, chưa verify chính diện** | `course` xuất hiện ở `precondition` của **4 TC** (`TC-OUTTRUTH001-01/02/04/05`) chỉ để *"phân biệt hai loại action"*, và `expected` chỉ khẳng định action course **KHÔNG** chạy. **Action của staff: 0 TC.** |
| **Lesson — action của course** | ⚠️ **Chỉ đối chứng âm** | Cùng 4 TC trên; không TC nào verify action course **CÓ** chạy đúng khi admin book |

#### GAP-06 [MAJOR] — vì sao đây là vùng regression thật, không phải mở rộng phạm vi

Bản vá chèn khối kích hoạt action friend info vào **đúng cùng hàm** với khối gửi action của lượt đặt, và nằm **ngay trước** nó:

| Màn | Khối mới (friend info action) | Khối `sendActionBooking` (course/staff action) |
|---|---|---|
| Salon | `CalendarSalonLineBookingService.php:3977-3992` | `:3993-4001` |
| Lesson | `CalendarCourseBookingService.php:220-241` | `:250-253` |

*(Số dòng lấy từ `spec_ids` của chính các TC Studio — `TC-TOOLKNOW002-01`, `TC-OUTTRUTH001-01/02`.)*

Ba rủi ro cụ thể:

1. **Thứ tự gửi đổi** — action friend info nay chạy **trước** action course/staff. Khách nhận tin theo thứ tự mới so với trước bản vá.
2. **Tương tác tag × filter** — action friend info có thể **gán tag**, còn action của course/staff **có filter** (`filter_id` / `filter_number`; kho `TC-LSN-101`: *"Filter course chỉ send multi action cho user thỏa filter"*). Chèn khối mới lên trước làm **đổi trạng thái tag của khách tại thời điểm filter được đánh giá** ⇒ khách trước đây không thỏa filter nay có thể thỏa (hoặc ngược lại). Đây là thay đổi hành vi **không ai kê** trong `03-dev-impact.md`.
3. **Random staff** — staff được gán tự động; action của staff phụ thuộc `staff_id`. Cần chắc khối mới không chạy trước khi `staff_id` được quyết định, và nhánh `指定なし` (staff NULL) không bị khối mới làm hỏng.

Kho đã có TC cho **bản thân** các chức năng này (`TC-SLN-263/264/265/267/268` random staff · `TC-SLN-446/447`, `TC-LSN-102..107` ưu tiên action course/staff · `TC-SLN-113` 5 cấu hình action khi admin book) — nhưng **tất cả viết trước bản vá** và **không kiểm cột 「追加時アクション」 / action friend info**, nên không bắt được tương tác mới. → Bổ sung 5 TC ở §5.2 (nhóm **S8**), không viết lại phần kho đã cover.

---

## 2. Quan điểm test còn thiếu

| Mã quan điểm | Ưu tiên | Vì sao thiếu |
|---|---|---|
| `SYNC-APP-001` | Trung bình → **Cao** (flow gửi tin cho khách) | 0 TC cho app di động, dù tính năng có mặt trên cả web và app (GAP-03) |
| `LIFF-ENTRY-001` ★ | **Cao** — BẮT BUỘC khi tính năng phát sinh URL cho LINE user (salon, lesson) | 0 TC cho luồng khách tự đặt qua LIFF (GAP-04) |
| `MSG-004` | **Cao** — BẮT BUỘC khi output là nội dung user cuối thấy trên LINE | Chỉ 1 TC (salon), **chưa chạy**; lesson không có (GAP-02) |
| `MSG-USER-001` | **Cao** — BẮT BUỘC khi tương tác friend LINE | Chỉ salon; thiếu nhánh **khách ngoài hệ thống** (GAP-01) |
| `REG-SHARED-001` | **Cao** — BẮT BUỘC khi fix bug **có thể tồn tại ở chức năng tương tự** | Bộ 20 TC chỉ có 1 TC mã này, và chỉ so salon ↔ lesson. Không phủ event / app / luồng sửa booking |
| `DATA-AUDIT-001` | **Cao** — BẮT BUỘC khi thao tác trên dữ liệu nhạy cảm có màn lịch sử | Không TC nào kiểm dòng lịch sử friend info **sau khi hủy / sửa booking** |
| `FRIEND-001` | **Cao** — BẮT BUỘC khi chức năng đọc/ghi friend info | Có 2 TC nhưng **chỉ trên kiểu select**. Bỏ trắng **date** (2 trục 月日/年月日) và **point** — 2/3 loại info có action (GAP-05) |
| `JOB-*` / `REG-RUN-001` | **Cao** — BẮT BUỘC khi hệ thống có job đang chạy dở | Action của info **date** chạy qua `event_step_time` + **Spring Boot job**, khác hẳn đường `action_lineuser` của select. Không TC nào chạm đường này (liên quan REQ-006 cũng đang trắng TC) |

---

## 3. TC trùng lặp

Không có DUP-EXACT trong bộ 20. Cặp salon ↔ lesson là **song sinh có chủ đích** (REQ-002 đòi parity, và Dev ghi rõ 2 file viết khác nhau ở chỗ `recordFriendInfoHistory`) → **giữ nguyên, không đề nghị gộp**.

---

## 4. Issues khác

- **[MAJOR]** Cả 20 TC chạy **1 run duy nhất trên `local`** bởi `pipeline` (AI), **0 run** trên dev/staging/prd. Ticket chạm **gửi tin ra LINE + job nền** → **RULE-08** không cho kết luận từ local.
- **[MAJOR]** 2 TC verify **triệu chứng chính** của ticket đang ở trạng thái `skip` (`TC-DATAHIST001-02`, `TC-OUTTRUTH001-03`), và TC verify khách nhận tin thật (`TC-MSG004-01`) **chưa chạy**. → Hiện **chưa TC nào chứng minh bug đã hết**.
- **[MAJOR]** **REQ-006** (job nền đọc `table_history` rồi ghi liên kết vào dòng lịch sử, `risk = High`) **không có TC nào map**. Đây cũng là mắt xích Dev **không kê** ở mục 4.1 của `03-dev-impact.md`.
- **[BLOCKER]** `TC-TOOLNEGCTRL001-03` dùng field 「記述」 (kiểu **không có action**) làm *"đại diện cho toàn bộ các kiểu dữ liệu khác"* và ghi *"không nhân case cho từng kiểu vì cùng một điều kiện chặn"* → **che mất** trường hợp info **date** (kiểu **CÓ** action nhưng cũng bị nhánh mới chặn). Kỳ vọng của TC này cần được **thu hẹp lại** chỉ áp cho kiểu text/画像/PDF, không được suy rộng (xem GAP-05).
- **[MINOR]** 5 mã quan điểm Studio (`TOOL-KNOW-002`, `TOOL-NEGCTRL-001`, `DATA-HIST-001`, `UI-TABLE-001`, `TOOL-OLDREC-001`) không có trong `framework/checklist-lme.md` → 10/20 TC không map được coverage.
- **[MINOR]** `spec_status` trống ở cả 20 TC, trong khi có **2 điểm CONFLICT spec** Studio tự đánh dấu (BUG-R1 「実行しない」 và spec BR-04 vs code `empty($friendInfoValue)`) → xem §6.

---

## 5. TCs đề xuất bổ sung

> **14 cột** = 12 cột format kho + `Chạy` + `Phạm vi ENV`. `Kết quả thực thi` để trống.
> Quy đổi env code Studio: `product` → `prd` · `Tất cả` → `["dev","local","prd","staging"]`.

### 5.1 — Ma trận bắt buộc: **mỗi kịch bản admin book × đủ 3 loại info**

Theo yêu cầu Leader: **tách TC riêng cho từng loại info**, không dùng TC "đại diện". Chỉ tính trường hợp **admin chọn giá trị CÓ gắn action**.

| # | Kịch bản admin đặt lịch hộ | **select** 「選択肢」 | **date** 「年月日」 | **point** 「ポイント」 |
|---|---|---|---|---|
| **S1** | Đặt lần đầu — khách CHƯA có giá trị → action phải chạy | ✅ `TC-TOOLKNOW002-01` (salon)<br>✅ `TC-TOOLKNOW002-02` (lesson) | 🆕 `TC-FRIEND001-04` (月日)<br>🆕 `TC-FRIEND001-05` (年月日) | 🆕 `TC-FRIEND001-09` |
| **S2** | Chế độ **何度でも稼働** — đổi giá trị nhiều lần → chạy lại mỗi lần | ✅ `TC-FRIEND001-01/02` | 🆕 `TC-FRIEND001-08` | 🆕 `TC-FRIEND001-10` |
| **S3** | Chế độ **一度のみ** — khách ĐÃ có giá trị → không chạy lại | ✅ `TC-FUNC004-01/02` | 🆕 `TC-FUNC004-03` | 🆕 `TC-FUNC004-04` |
| **S4** | Gán lại **đúng giá trị cũ** → không sinh dòng lịch sử mới | ✅ `TC-DATAHIST001-01`<br>✅ `TC-REGSHARED001-01` | 🆕 `TC-DATAAUDIT001-02` | 🆕 `TC-DATAAUDIT001-04` |
| **S5** | 「予約時アクションの実行」 = **「実行しない」** → action info vẫn chạy | ✅ `TC-OUTTRUTH001-01/02` | 🆕 `TC-OUTTRUTH001-04` | 🆕 `TC-OUTTRUTH001-05` |
| **S6** | Lịch sử friend info hiện **「プレビュー」** (kết quả cuối của ticket) | ✅ `TC-DATAHIST001-02` ⚠️ *đang skip* | 🆕 `TC-DATAAUDIT001-03` | 🆕 `TC-DATAAUDIT001-05` |
| **S7** | Khách nhận **tin thật trên LINE** (RULE-06) | ✅ `TC-MSG004-01` ⚠️ *chưa chạy*<br>🆕 `TC-MSG004-02` (lesson) | 🆕 `TC-MSG004-03` | 🆕 `TC-MSG004-04` |

✅ = đã có trong bộ 20 TC Studio · 🆕 = TC bổ sung ở §5.2 · **21/21 ô** đã có TC sau khi bổ sung.

**S8 — action cấp course / staff (trục riêng, không nhân theo loại info)**: các TC dưới đây kiểm **tương tác** giữa action friend info mới thêm và action của lượt đặt, nên chỉ cần chạy với loại info **select** làm đại diện (cơ chế `sendAction` giống nhau; date/point đã có TC riêng ở S1–S7).

| # | Kịch bản | Salon | Lesson |
|---|---|---|---|
| **S8a** | Staff 「指定なし」 + **KHÔNG** tự phân công → action course chạy, không có action staff | 🆕 `TC-FUNC001-01` | — (lesson không có staff) |
| **S8b** | Staff 「指定なし」 + **CÓ random** → action của **đúng staff được random** chạy | 🆕 `TC-FUNC001-02` | — |
| **S8c** | Chọn **staff cụ thể** + course có action → đủ **3 loại action**, đúng số lượng và thứ tự | 🆕 `TC-MSG002-01` | — |
| **S8d** | Ma trận **ưu tiên action của course** khi admin book không đổi sau bản vá | (kho `TC-SLN-113`) | 🆕 `TC-MSG002-02` |
| **S8e** | **Tag × filter**: action friend info gán tag → filter của action course/staff đánh giá theo trạng thái nào | 🆕 `TC-REGSHARED001-07` | 🆕 (cùng TC, chạy 2 màn) |

**Case biên riêng của từng loại** (không nằm trong ma trận 7×3):

| Loại | TC | Nội dung |
|---|---|---|
| date | 🆕 `TC-FRIEND001-07` | Mốc gửi tính ra **đã ở quá khứ** → không đăng ký lịch, không gửi tin muộn |
| point | 🆕 `TC-FUNC004-04` (gộp) | Biên **ngưỡng điểm**: 99 (dưới) / 100 (đúng ngưỡng) |
| point | 🆕 `TC-FRIEND001-06` | Đối chứng phạm vi: form đặt lịch có liên kết được tới info point không |

⚠️ **Điều kiện tiên quyết của 7 TC point**: phân tích spec hiện có (`TC-SLN-318` — form chỉ có 5 kiểu item; `TC-LSN-360` — ánh xạ item → friend info chỉ có TEXT / SELECT / DATE) cho thấy point **có thể không** đi qua form đặt lịch. `TC-FRIEND001-06` chạy **trước** để chốt lối gán; nếu form không hỗ trợ thì dùng lối gán Dev/BA xác nhận và ghi rõ vào Evidence. Không verify được bằng live MCP DEV (bot test hết hạn contract / thiếu quyền feature).

### 5.2 — Bảng TC

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-MSG004-02 | UI | MSG-004 | Lesson — admin đặt lịch hộ (web) | Normal | manual | product | Lesson: khách nhận đúng nội dung tin của action friend info trên LINE thật | - Đăng nhập admin bot A, lịch lesson 「TC38443_レッスン」 đang ON, có コース + 受付 còn chỗ<br>- Field friend info kiểu 「選択」 「TC38443_選択L」, lựa chọn 「オプションL1」 gắn action 「TC38443_ACT_L1」 (1 tin text 「TC38443 アクション実行」), chế độ 「一度のみ」<br>- 予約フォーム của lịch có câu hỏi liên kết field này<br>- Khách T177_USER2 đã kết bạn bot A, CHƯA có giá trị field, có thiết bị thật đăng nhập LINE bằng tài khoản này | 1. Mở 「予約カレンダー(レッスン)」 → chọn 受付 còn chỗ → mở modal 「予約追加」<br>2. Chọn khách T177_USER2<br>3. Ở câu hỏi liên kết 「TC38443_選択L」 chọn 「オプションL1」<br>4. Giữ 「予約時アクションの実行」 = 「実行する」 → bấm đăng ký<br>5. Mở LINE trên thiết bị thật bằng T177_USER2, xem phòng chat với bot A<br>6. Quay lại 「友だち詳細」 → tab 「友だち情報」 → xem dòng lịch sử vừa sinh | 「TC38443_選択L」 = 「オプションL1」 · action 「TC38443_ACT_L1」 = 1 tin text 「TC38443 アクション実行」 | - Bước 5: phòng chat LINE của T177_USER2 xuất hiện **đúng 1** tin 「TC38443 アクション実行」, đúng nội dung, **không trùng lặp**, không kèm tin lạ<br>- Bước 6: dòng lịch sử mới có 操作 = 「登録」, 登録情報 = 「オプションL1」, cột 「追加時アクション」 hiện 「プレビュー」 bấm được<br>- Nội dung preview khớp tin khách nhận được | | Lấp **GAP-02** · Evidence: ảnh chụp phòng chat LINE trên thiết bị thật + ảnh dòng lịch sử · **RULE-06** (output cuối chuỗi) · **RULE-08** (gửi tin LINE → không kết luận từ local/staging) · Đối xứng với `TC-MSG004-01` của salon (REQ-002 parity) · Đánh giá spec: Spec ghi rõ |
| TC-MSGUSER001-02 | UI | MSG-USER-001 | Salon + Lesson — admin đặt lịch hộ (web) | Abnormal | manual | Tất cả | Admin đặt lịch hộ khách NGOÀI hệ thống (chưa là bạn LINE) không sinh action mồ côi và không văng lỗi | - Đăng nhập admin bot A<br>- Lịch salon 「TC38443_サロン」 và lịch lesson 「TC38443_レッスン」 đều có câu hỏi form liên kết field 「選択」 có action ở lựa chọn<br>- Modal 「予約追加」 cho phép nhập tên khách tự do (không chọn từ danh sách bạn bè)<br>- Ghi lại số bản ghi chạy action hiện có của bot A để so sánh | 1. Mở modal 「予約追加」 của lịch salon<br>2. Ở mục chọn khách, **nhập tên tự do** thay vì chọn bạn bè LINE (VD 「テスト外部客」)<br>3. Chọn コース/スタッフ/khung giờ hợp lệ<br>4. Ở câu hỏi liên kết field 「選択」 chọn lựa chọn **CÓ gắn action** → bấm đăng ký<br>5. Lặp bước 1-4 trên lịch lesson<br>6. Đối chiếu số bản ghi chạy action của bot A với bước chuẩn bị<br>7. Kiểm tra màn hình và log ứng dụng | Tên khách tự do 「テスト外部客」 (không có `line_user_id`) · lựa chọn có gắn action | - Cả 2 màn: đặt lịch **tạo thành công**, modal đóng, lượt đặt hiện trên lịch<br>- **KHÔNG** văng lỗi 500 / không trắng màn / không hiện thông báo lỗi<br>- Số bản ghi chạy action của bot A **giữ nguyên** — không sinh bản ghi action nào trỏ tới khách không có `line_user_id`<br>- Không có dòng lịch sử friend info nào được tạo cho khách ngoài hệ thống<br>- Log ứng dụng không có exception | | Lấp **GAP-01** · Bản vá thêm `sendAction` vào đúng nhánh này, chưa TC nào kiểm nhánh `line_user_id` rỗng · Đối chiếu kho `TC-LSN-212` / `TC-LSN-213` (viết TRƯỚC bản vá, chưa kiểm cột action) · Evidence: ảnh màn hình + số đếm bản ghi action trước/sau + log · Đánh giá spec: Spec không ghi → **hỏi PO/BA** hành vi mong muốn |
| TC-SYNCAPP001-01 | UI | SYNC-APP-001 | Salon — admin đặt lịch hộ trên **app di động** | Abnormal | manual | Tất cả | Salon app: admin đặt lịch hộ gán friend info có action → xác nhận VẪN chưa gửi action (known issue ngoài phạm vi #38443) | - Đã cài và đăng nhập app di động Admin bằng tài khoản bot A<br>- Lịch salon 「TC38443_サロン」 có câu hỏi form liên kết field 「TC38443_選択A」, lựa chọn 「オプションA」 gắn action 「TC38443_ACT_A」, chế độ 「一度のみ」<br>- Khách T177_USER đã kết bạn bot A, CHƯA có giá trị field<br>- Ghi lại số bản ghi chạy action hiện có của T177_USER<br>- Đã deploy branch `ai_fixbug_38443` | 1. Trên **app di động**, mở lịch salon 「TC38443_サロン」 → chọn khung giờ trống → thêm đặt lịch<br>2. Chọn khách T177_USER, chọn コース/スタッフ, khung giờ hợp lệ<br>3. Ở câu hỏi liên kết 「TC38443_選択A」 chọn 「オプションA」 → đăng ký<br>4. Trên **web**, mở 「友だち詳細」 của T177_USER → tab 「友だち情報」<br>5. Đọc giá trị field và cột 「追加時アクション」 của dòng lịch sử mới<br>6. Đối chiếu số bản ghi chạy action với bước chuẩn bị<br>7. Kiểm phòng chat LINE của T177_USER | 「TC38443_選択A」 = 「オプションA」 (lựa chọn CÓ gắn action), đặt qua app di động | - Đặt lịch tạo thành công trên app và hiện đúng trên web<br>- Giá trị field được lưu, bảng lịch sử có thêm 1 dòng<br>- ⚠️ **Hiện trạng dự kiến (chưa fix)**: số bản ghi chạy action **giữ nguyên**, cột 「追加時アクション」 hiện **「設定なし」**, khách **KHÔNG** nhận tin trên LINE<br>- Nếu kết quả đúng như trên → **KHÔNG tính là Không đạt của #38443**, mà **raise ticket mới** cho luồng app<br>- Nếu action **CÓ** chạy → phạm vi fix rộng hơn Dev khai, phải cập nhật lại `03-dev-impact.md` | | Lấp **GAP-03** · Dev(AI) khai rõ 「2 luồng đặt lịch từ app」 **ngoài phạm vi ticket, chưa fix** · TC này **chốt hiện trạng để không kết luận nhầm "đã fix toàn bộ admin book"** · Đối chiếu kho `TC-SLN-477` (app mobile: admin đặt lịch + action) · Evidence: ảnh app + ảnh cột 「追加時アクション」 trên web + số đếm action · Đánh giá spec: Spec không ghi → đã hỏi Dev |
| TC-SYNCAPP001-02 | UI | SYNC-APP-001 | Lesson — admin đặt lịch hộ trên **app di động** (`EP-M15`) | Abnormal | manual | Tất cả | Lesson app: admin đặt lịch hộ gán friend info có action → xác nhận VẪN chưa gửi action (known issue ngoài phạm vi #38443) | - Đã cài và đăng nhập app di động Admin bằng tài khoản bot A<br>- Lịch lesson 「TC38443_レッスン」 có 受付 còn chỗ và câu hỏi form liên kết field 「TC38443_選択L」, lựa chọn 「オプションL1」 gắn action 「TC38443_ACT_L1」, chế độ 「一度のみ」<br>- Khách T177_USER2 đã kết bạn bot A, CHƯA có giá trị field<br>- Ghi lại số bản ghi chạy action hiện có của T177_USER2<br>- Đã deploy branch `ai_fixbug_38443` | 1. Trên **app di động**, mở lịch lesson 「TC38443_レッスン」 → chọn 受付 còn chỗ → thêm đặt chỗ<br>2. Chọn khách T177_USER2<br>3. Ở câu hỏi liên kết 「TC38443_選択L」 chọn 「オプションL1」 → đăng ký<br>4. Trên **web**, mở 「友だち詳細」 của T177_USER2 → tab 「友だち情報」<br>5. Đọc giá trị field và cột 「追加時アクション」 của dòng lịch sử mới<br>6. Đối chiếu số bản ghi chạy action với bước chuẩn bị<br>7. Kiểm phòng chat LINE của T177_USER2 | 「TC38443_選択L」 = 「オプションL1」 (lựa chọn CÓ gắn action), đặt qua app di động | - Đặt chỗ tạo thành công trên app và hiện đúng trên web<br>- Giá trị field được lưu, bảng lịch sử có thêm 1 dòng<br>- ⚠️ **Hiện trạng dự kiến (chưa fix)**: số bản ghi chạy action **giữ nguyên**, cột 「追加時アクション」 hiện **「設定なし」**, khách **KHÔNG** nhận tin trên LINE<br>- Kết quả đúng như trên → **raise ticket mới** cho `EP-M15`, không tính Không đạt của #38443 | | Lấp **GAP-03** · Spec `api-spec-public.md` §EP-M15: `formQuestion` → *"lưu vào friend_info + ghi đè hồ sơ bạn bè"*, xử lý tại `Api/CalendarLessonController.php:1745-1812` — **không đi qua `CalendarCourseBookingService::create`** (chỗ được vá) · Nếu QA không có app: gọi thẳng `POST /api/mobile/calendar-lesson/add-booking` (khi đó `Nhóm` = API) · Evidence: ảnh app hoặc request/response + ảnh cột 「追加時アクション」 · Đánh giá spec: Spec không ghi → đã hỏi Dev |
| TC-SYNCAPP001-03 | UI | SYNC-APP-001 | Salon + Lesson — đối chiếu Web ↔ App | Boundary | manual | Tất cả | Cùng một kịch bản đặt lịch hộ chạy song song Web và App → chênh lệch hành vi được ghi nhận đầy đủ | - Đã chạy xong `TC-TOOLKNOW002-01` (salon web) và `TC-SYNCAPP001-01` (salon app) trên **cùng bản deploy**<br>- Đã chạy xong `TC-TOOLKNOW002-02` (lesson web) và `TC-SYNCAPP001-02` (lesson app)<br>- Dùng 2 khách khác nhau cho web và app để không lẫn dữ liệu | 1. Lập bảng đối chiếu 4 lượt chạy theo 4 tiêu chí: (a) bản ghi chạy action có sinh không, (b) cột 「追加時アクション」 hiện gì, (c) khách có nhận tin trên LINE không, (d) dòng lịch sử có được tạo không<br>2. Ghi rõ từng ô Web vs App cho cả salon và lesson<br>3. Với mỗi ô lệch, ghi lại `booking_id` và thời điểm để Dev trace | 4 lượt chạy: salon-web · salon-app · lesson-web · lesson-app | - Cột Web (salon + lesson): (a) CÓ sinh · (b) 「プレビュー」 · (c) CÓ nhận tin · (d) CÓ dòng lịch sử<br>- Cột App (salon + lesson): (a) KHÔNG sinh · (b) 「設定なし」 · (c) KHÔNG nhận tin · (d) CÓ dòng lịch sử<br>- Bảng đối chiếu này là **đầu vào bắt buộc** của ticket mới cho luồng app<br>- Nếu App khớp Web ở mọi ô → luồng app đã được fix ở đâu đó, phải cập nhật `03-dev-impact.md` | | Lấp **GAP-03** · RULE-01: quan điểm `SYNC-APP-001` nâng **Cao** cần đủ 3 loại case — **thiếu Normal** vì luồng app hiện **chưa được fix**, không tồn tại case Normal cho tới khi có ticket fix riêng (lý do theo RULE-01) · Đối chiếu kho `TC-SLN-475` (đối chiếu song song Web/App) · Evidence: bảng đối chiếu 4×4 + ảnh 2 kênh |
| TC-REGSHARED001-02 | UI | REG-SHARED-001 | Salon — LINE user tự đặt lịch (LIFF) | Normal | manual | product | Regression salon: khách TỰ đặt lịch gán friend info có action vẫn chạy action và hiện 「プレビュー」 như trước bản vá | - Bot A đã đăng ký LIFF ID cho tính năng đặt lịch salon<br>- Lịch salon 「TC38443_サロン」 đang ON, có コース + スタッフ có ca<br>- Field 「TC38443_選択A」 kiểu 「選択」, 「オプションA」 gắn action 「TC38443_ACT_A」, chế độ 「一度のみ」<br>- 予約フォーム có câu hỏi liên kết field này<br>- Khách T177_USER3 đã kết bạn bot A, **CHƯA** có giá trị field, có thiết bị thật đăng nhập LINE<br>- Đã deploy branch `ai_fixbug_38443` | 1. Trên thiết bị thật, T177_USER3 mở URL đặt lịch salon từ LINE<br>2. Chọn コース / スタッフ / khung giờ hợp lệ<br>3. Ở câu hỏi liên kết 「TC38443_選択A」 chọn 「オプションA」 → xác nhận đặt lịch<br>4. Xem phòng chat LINE của T177_USER3<br>5. Trên web admin, mở 「友だち詳細」 của T177_USER3 → tab 「友だち情報」<br>6. Đọc cột 「追加時アクション」 của dòng lịch sử mới, bấm 「プレビュー」<br>7. Đối chiếu số bản ghi chạy action sinh ra (phải đúng **1**, không phải 2) | 「TC38443_選択A」 = 「オプションA」, khách TỰ đặt qua LIFF | - Đặt lịch thành công, khách nhận tin hoàn tất đặt lịch<br>- Khách nhận **đúng 1** tin của action 「TC38443_ACT_A」 — **không bị nhân đôi**<br>- Dòng lịch sử mới hiện **「プレビュー」** bấm được, nội dung khớp action<br>- **Đúng 1** bản ghi chạy action được sinh, trỏ đúng dòng lịch sử vừa tạo<br>- Hành vi **không đổi** so với trước bản vá | | Lấp **GAP-04** · Luồng này là **mẫu đối chiếu** Dev copy code sang, nằm **cùng file** `CalendarSalonLineBookingService.php` vừa +41 dòng → rủi ro double-fire / gán nhầm `table_history` · Kho `TC-SLN-444` có test form khách tự đặt nhưng **không kiểm cột 「追加時アクション」** · Evidence: ảnh phòng chat LINE + ảnh dòng lịch sử + số đếm action · **RULE-06** · **RULE-08** · Đánh giá spec: Spec ghi rõ |
| TC-REGSHARED001-03 | UI | REG-SHARED-001 | Lesson — LINE user tự đặt lịch (LIFF) | Normal | manual | product | Regression lesson: khách TỰ đặt lịch gán friend info có action vẫn chạy action và hiện 「プレビュー」 như trước bản vá | - Bot A đã đăng ký LIFF ID cho tính năng đặt lịch lesson<br>- Lịch lesson 「TC38443_レッスン」 đang ON, có コース + 受付 còn chỗ<br>- Field 「TC38443_選択L」 kiểu 「選択」, 「オプションL1」 gắn action 「TC38443_ACT_L1」, chế độ 「一度のみ」<br>- 予約フォーム có câu hỏi liên kết field này<br>- Khách T177_USER4 đã kết bạn bot A, **CHƯA** có giá trị field, có thiết bị thật đăng nhập LINE<br>- Đã deploy branch `ai_fixbug_38443` | 1. Trên thiết bị thật, T177_USER4 mở URL đặt lịch lesson từ LINE<br>2. Chọn コース và 受付 còn chỗ<br>3. Ở câu hỏi liên kết 「TC38443_選択L」 chọn 「オプションL1」 → xác nhận đặt chỗ<br>4. Xem phòng chat LINE của T177_USER4<br>5. Trên web admin, mở 「友だち詳細」 của T177_USER4 → tab 「友だち情報」<br>6. Đọc cột 「追加時アクション」 của dòng lịch sử mới, bấm 「プレビュー」<br>7. Đối chiếu số bản ghi chạy action sinh ra (phải đúng **1**) | 「TC38443_選択L」 = 「オプションL1」, khách TỰ đặt qua LIFF | - Đặt chỗ thành công, khách nhận tin hoàn tất đặt chỗ<br>- Khách nhận **đúng 1** tin của action 「TC38443_ACT_L1」 — **không bị nhân đôi**<br>- Dòng lịch sử mới hiện **「プレビュー」** bấm được<br>- **Đúng 1** bản ghi chạy action, trỏ đúng dòng lịch sử vừa tạo<br>- Hành vi **không đổi** so với trước bản vá | | Lấp **GAP-04** · `CalendarCourseBookingService.php` cũng bị sửa trong cùng commit · Kho `TC-LSN-564` có test form khách tự đặt nhưng **không kiểm cột 「追加時アクション」** · Evidence: ảnh phòng chat LINE + ảnh dòng lịch sử + số đếm action · **RULE-06** · **RULE-08** · Đánh giá spec: Spec ghi rõ |
| TC-REGSHARED001-04 | UI | REG-SHARED-001 | Salon + Lesson — admin book và khách tự đặt xen kẽ | Boundary | manual | Tất cả | Chế độ 「一度のみ」: admin đặt hộ rồi khách tự đặt lại cùng field → action chỉ chạy ĐÚNG MỘT LẦN, không double-fire | - Field 「TC38443_選択A」 (salon) và 「TC38443_選択L」 (lesson) đều đặt chế độ **「一度のみ」**, lựa chọn có gắn action<br>- Khách T177_USER5 đã kết bạn bot A, **CHƯA** có giá trị field nào<br>- Ghi lại số bản ghi chạy action hiện có của T177_USER5<br>- Đã deploy branch `ai_fixbug_38443` | 1. Admin đặt lịch hộ T177_USER5 trên web salon, chọn lựa chọn CÓ action → đăng ký<br>2. Đối chiếu: số bản ghi chạy action **+1**, dòng lịch sử hiện 「プレビュー」<br>3. Ngay sau đó, T177_USER5 **tự đặt lịch** qua LIFF salon, chọn **cùng** lựa chọn đó<br>4. Đối chiếu lại số bản ghi chạy action và bảng lịch sử<br>5. Lặp toàn bộ bước 1-4 cho lesson<br>6. Kiểm phòng chat LINE của T177_USER5 đếm số tin nhận được | Lần 1 admin book · Lần 2 khách tự đặt · cùng field, cùng lựa chọn, chế độ 「一度のみ」 | - Bước 2: **+1** bản ghi chạy action, dòng lịch sử hiện 「プレビュー」<br>- Bước 4: **KHÔNG** sinh thêm bản ghi chạy action (chế độ 「一度のみ」 và khách đã có giá trị) — tổng vẫn là **+1**<br>- Bước 6: khách nhận **đúng 1** tin của action, **không nhận 2 tin**<br>- Kết quả **giống hệt** giữa salon và lesson | | Lấp **GAP-04** + đối chứng chéo web ↔ LIFF · Bắt lỗi double-fire do bản vá thêm lời gọi `sendAction` mới vào cùng file với luồng khách tự đặt · Liên kết `TC-FUNC004-01` / `TC-FUNC004-02` (cùng trục 「一度のみ」 nhưng chỉ trong 1 luồng) · Evidence: số đếm action trước/sau từng bước + ảnh phòng chat LINE · **RULE-06** |
| TC-REGSHARED001-05 | UI | REG-SHARED-001 | Salon + Lesson — sửa thông tin form của lượt đặt đã có | Abnormal | manual | Tất cả | Sửa đáp án form của booking đã tạo → xác nhận VẪN chưa gửi action friend info (known issue ngoài phạm vi #38443) | - Đã có 1 booking salon và 1 booking lesson của khách T177_USER6, đáp án form ban đầu là lựa chọn **KHÔNG** gắn action<br>- Field 「選択」 tương ứng đặt chế độ 「何度でも稼働」, lựa chọn 「オプションA」 / 「オプションL1」 CÓ gắn action<br>- Ghi lại số bản ghi chạy action hiện có của T177_USER6<br>- Đã deploy branch `ai_fixbug_38443` | 1. Mở detail booking salon của T177_USER6 trên web admin<br>2. **Sửa** đáp án câu hỏi form sang lựa chọn **CÓ** gắn action → lưu<br>3. Mở 「友だち詳細」 của T177_USER6 → tab 「友だち情報」, đọc cột 「追加時アクション」 của dòng lịch sử mới<br>4. Đối chiếu số bản ghi chạy action với bước chuẩn bị<br>5. Lặp bước 1-4 cho booking lesson<br>6. Kiểm phòng chat LINE của T177_USER6 | Đáp án cũ = lựa chọn không action → đáp án mới = lựa chọn **CÓ** action · chế độ 「何度でも稼働」 | - Sửa booking thành công, giá trị friend info cập nhật, bảng lịch sử có thêm dòng mới<br>- ⚠️ **Hiện trạng dự kiến (chưa fix)**: số bản ghi chạy action **giữ nguyên**, dòng lịch sử mới hiện **「設定なし」**, khách **KHÔNG** nhận tin<br>- Kết quả đúng như trên → **raise ticket mới**, không tính Không đạt của #38443 | | Đây là **chỗ thứ 4** Dev(AI) tự khai còn thiếu: *「sửa thông tin biểu mẫu của lượt đặt đã có」* — **ngoài phạm vi ticket, chưa fix** · TC chốt hiện trạng để không kết luận nhầm phạm vi · Evidence: ảnh dòng lịch sử + số đếm action · Đánh giá spec: Spec không ghi → đã hỏi Dev |
| TC-DATAAUDIT001-01 | UI | DATA-AUDIT-001 | Salon + Lesson — hủy booking sau khi admin đặt hộ | Boundary | manual | Tất cả | Hủy booking sau khi action friend info đã chạy → không gửi lại action, dòng lịch sử giữ nguyên 「プレビュー」 | - Đã chạy xong `TC-TOOLKNOW002-01` (salon) và `TC-TOOLKNOW002-02` (lesson): khách đã được gán giá trị, action đã chạy, dòng lịch sử đang hiện 「プレビュー」<br>- ⚠️ **Action lúc hủy cấu hình ở CẤP CALENDAR** — tab 「予約・キャンセル設定」 → sub-tab 「アクション」 (`setting_action_id` / `setting_action_request` / `setting_action_approve` / `setting_action_reject`). **KHÔNG cấu hình ở cấp course/staff**: course và staff chỉ có `action_id_send_after_booking` (lúc đặt lịch) và `action_id_send_approve_booking` (lúc duyệt request), **không có action hủy**<br>- Đặt action hủy ở cấp calendar với nội dung riêng phân biệt được<br>- Ghi lại số bản ghi chạy action hiện có và ảnh chụp bảng lịch sử friend info | 1. Trên web admin, mở booking salon vừa tạo → **hủy booking**<br>2. Mở 「友だち詳細」 → tab 「友だち情報」, đối chiếu bảng lịch sử với ảnh chụp ở bước chuẩn bị<br>3. Đối chiếu số bản ghi chạy action<br>4. Kiểm phòng chat LINE của khách<br>5. Lặp bước 1-4 cho booking lesson | Hủy booking đã tạo bằng luồng admin đặt hộ có action friend info | - Hủy booking thành công, trạng thái booking đổi đúng<br>- Dòng lịch sử friend info **giữ nguyên**, **không bị xóa**, cột 「追加時アクション」 **vẫn là 「プレビュー」** và bấm ra đúng nội dung cũ<br>- Giá trị friend info của khách **không bị rollback**<br>- Action **của friend info KHÔNG** chạy lại (không sinh thêm bản ghi loại 6004)<br>- Chỉ action **hủy ở cấp calendar** chạy (nếu có cấu hình) — đúng 1 lần<br>- Action của **course/staff KHÔNG** chạy — đúng thiết kế, vì chúng chỉ apply lúc **đặt lịch** và lúc **duyệt request**, không có action hủy | | Trả lời trục 「Setting action lúc booking/**cancel booking** (multi action)」 Leader nêu · ⚠️ **Đã sửa 2026-09-07** theo phản hồi Leader: bản trước ghi nhầm "action hủy của コース" — course/staff **không có** action hủy (`salon-booking/web/logic-spec.md:246, :265` chỉ có `action_id_send_after_booking` + `action_id_send_approve_booking`); action hủy nằm ở cấp calendar (`feature-spec.md:430`) · Bản vá chỉ chạm luồng tạo booking, nhưng cần chốt action mới **không xung đột** với multi action lúc hủy · Đối chiếu kho `TC-SLN-448` (ma trận action ở 4 mốc của luồng HỦY) · Evidence: ảnh bảng lịch sử trước/sau + số đếm action + ảnh phòng chat LINE · Đánh giá spec: Spec ghi rõ |

| TC-FRIEND001-04 | UI | FRIEND-001 | Salon — admin đặt lịch hộ (web), info kiểu **年月日** chế độ **月日** | Normal | manual | Tất cả | Info date chế độ 月日 (lặp hàng năm): admin đặt lịch hộ phải đăng ký được lịch chạy action | - Đăng nhập admin bot A, lịch salon 「TC38443_サロン」 đang ON<br>- Tạo field friend info kiểu 「年月日」 tên 「TC38443_日付A」, cấu hình action: 「登録」 = **「月日」**, offset 「から 0 日」「後」, giờ gửi 「10:00」, action 「TC38443_ACT_D1」 (1 tin text 「TC38443 日付アクション」), 「稼働設定」 = 「一度のみ」<br>- 予約フォーム của lịch có 1 câu hỏi kiểu 「日時」 **KHÔNG kèm giờ**, liên kết tới field 「TC38443_日付A」<br>- Khách T177_USER7 đã kết bạn bot A, **CHƯA** có giá trị field này<br>- Ghi lại số bản ghi lịch chạy action theo ngày hiện có của T177_USER7 | 1. Mở 「予約カレンダー(サロン)」 → khung giờ trống → modal 「予約追加」<br>2. Chọn khách T177_USER7, chọn コース/スタッフ/khung giờ hợp lệ<br>3. Ở câu hỏi 「日時」 nhập một ngày trong **tương lai** (VD ngày mai)<br>4. Bấm đăng ký<br>5. Mở 「友だち詳細」 của T177_USER7 → tab 「友だち情報」: đọc giá trị field + dòng lịch sử + cột 「追加時アクション」<br>6. Kiểm tra **lịch chạy action theo ngày** của T177_USER7 đã được đăng ký chưa<br>7. Chờ tới mốc đã đăng ký (hoặc nhờ Dev đẩy mốc về hiện tại) → kiểm phòng chat LINE | 「TC38443_日付A」 = ngày mai · 「登録」 = 「月日」 · offset 0 ngày sau · 10:00 | - Đặt lịch thành công, giá trị 「TC38443_日付A」 lưu đúng ngày đã nhập, bảng lịch sử có thêm 1 dòng<br>- **Lịch chạy action theo ngày được đăng ký** đúng 1 bản ghi cho T177_USER7, mốc gửi = ngày đã nhập ± offset lúc 10:00<br>- Bước 7: khách nhận đúng tin 「TC38443 日付アクション」<br>- ⚠️ **Nếu KHÔNG có bản ghi lịch nào được đăng ký → GAP-05 được xác nhận là bug thật, raise ngay** | | Lấp **GAP-05** · Cơ chế date **khác select**: đi qua `settingEventTimeFriendInfo()` → `event_step`(type=3) + `event_step_time` → Spring Boot job, **không** qua `action_lineuser` · `03-dev-impact.md` mục 3 **không** liệt kê hàm này · Đối chứng: làm **cùng kịch bản** ở màn 「友だち詳細」 (nhập tay giá trị date) — nơi đó CÓ đăng ký lịch → chênh lệch = bug · Evidence: ảnh lịch sử + bản ghi lịch chạy action + ảnh phòng chat LINE · **RULE-06** · Đánh giá spec: Spec ghi rõ (db-mapping §4.4) |
| TC-FRIEND001-05 | UI | FRIEND-001 | Lesson — admin đặt lịch hộ (web), info kiểu **年月日** chế độ **年月日** | Normal | manual | Tất cả | Info date chế độ 年月日 (1 lần duy nhất): admin đặt lịch hộ phải đăng ký được lịch chạy action | - Đăng nhập admin bot A, lịch lesson 「TC38443_レッスン」 đang ON, có 受付 còn chỗ<br>- Tạo field friend info kiểu 「年月日」 tên 「TC38443_日付L」, cấu hình action: 「登録」 = **「年月日」**, offset 「から 1 日」「前」, giờ gửi 「09:00」, action 「TC38443_ACT_D2」, 「稼働設定」 = 「一度のみ」<br>- 予約フォーム của lịch có câu hỏi 「日時」 **KHÔNG kèm giờ** liên kết field này<br>- Khách T177_USER8 đã kết bạn bot A, **CHƯA** có giá trị field<br>- Ghi lại số bản ghi lịch chạy action hiện có của T177_USER8 | 1. Mở 「予約カレンダー(レッスン)」 → chọn 受付 còn chỗ → modal 「予約追加」<br>2. Chọn khách T177_USER8<br>3. Ở câu hỏi 「日時」 nhập một ngày trong **tương lai** cách hiện tại ≥ 3 ngày<br>4. Bấm đăng ký<br>5. Mở 「友だち詳細」 của T177_USER8 → tab 「友だち情報」: đọc giá trị + dòng lịch sử + cột 「追加時アクション」<br>6. Kiểm tra lịch chạy action theo ngày của T177_USER8<br>7. So sánh kết quả với `TC-FRIEND001-04` (salon, chế độ 月日) | 「TC38443_日付L」 = ngày trong tương lai ≥ 3 ngày · 「登録」 = 「年月日」 · offset 1 ngày **trước** · 09:00 | - Đặt chỗ thành công, giá trị lưu đúng, bảng lịch sử có thêm 1 dòng<br>- **Lịch chạy action được đăng ký** đúng 1 bản ghi, mốc gửi = ngày đã nhập **− 1 ngày** lúc 09:00<br>- Chế độ 「年月日」 chỉ khớp **đúng 1 lần** (không lặp sang năm sau) — khác 「月日」 của `TC-FRIEND001-04`<br>- Kết quả salon và lesson phải **giống nhau** về việc CÓ / KHÔNG đăng ký lịch<br>- ⚠️ Không có bản ghi lịch → xác nhận GAP-05 | | Lấp **GAP-05** · Trục `option_compare1`: 1 = 「月日」 lặp hàng năm · 2 = 「年月日」 1 lần (spec §5.3) · Cặp với `TC-FRIEND001-04` để phủ **cả 2 loại action của info date** đúng như Leader nêu · Evidence: bản ghi lịch chạy action (mốc gửi cụ thể) + ảnh lịch sử · Đánh giá spec: Spec ghi rõ |
| TC-FRIEND001-07 | UI | FRIEND-001 | Salon + Lesson — admin đặt lịch hộ (web), info date **mốc đã qua** | Boundary | manual | Tất cả | Info date: mốc gửi tính ra đã ở quá khứ → không đăng ký lịch, không lỗi, không gửi tin muộn | - Như `TC-FRIEND001-04`, nhưng field 「TC38443_日付A」 cấu hình offset 「から 3 日」「前」<br>- Khách T177_USER9 đã kết bạn bot A, chưa có giá trị field<br>- Ghi lại số bản ghi lịch chạy action hiện có của T177_USER9 | 1. Admin đặt lịch hộ T177_USER9 trên salon, ở câu hỏi 「日時」 nhập **ngày hôm nay** (mốc gửi = hôm nay − 3 ngày ⇒ đã ở quá khứ)<br>2. Kiểm giá trị field, dòng lịch sử, số bản ghi lịch chạy action<br>3. Lặp bước 1-2 với ngày **trong quá khứ** (VD tháng trước)<br>4. Lặp toàn bộ trên lesson<br>5. Theo dõi phòng chat LINE của T177_USER9 trong 30 phút | Ngày nhập = hôm nay và một ngày quá khứ · offset 3 ngày **trước** ⇒ mốc gửi < hiện tại | - Đặt lịch **thành công**, không thông báo lỗi, không lỗi hệ thống<br>- Giá trị field và dòng lịch sử **vẫn được ghi đúng**<br>- **KHÔNG** đăng ký bản ghi lịch chạy action nào (mốc gửi đã qua)<br>- Khách **KHÔNG** nhận tin muộn / tin dồn<br>- Hành vi giống hệt giữa salon và lesson | | Lấp **GAP-05** (loại case Boundary theo **RULE-01** cho quan điểm `FRIEND-001` ưu tiên Cao) · Spec `friend-information/web/logic-spec.md:386`: *"Nếu `sent_date_time` > now → insert `event_step_time`"* ⇒ mốc quá khứ phải bị bỏ qua · Evidence: số đếm bản ghi lịch trước/sau + ảnh phòng chat LINE sau 30 phút · Đánh giá spec: Spec ghi rõ |
| TC-FRIEND001-06 | UI | FRIEND-001 | Salon + Lesson — đối chiếu 2 lối ghi friend info (form item vs multi action) | Normal | manual | Tất cả | Bản đồ 2 lối ghi friend info ở salon/lesson — lối nào ghi được loại info nào | - Đăng nhập admin bot A<br>- Đã tạo 3 field friend info CÓ action: 1 kiểu 「選択肢」 「TC38443_選択A」, 1 kiểu 「年月日」 「TC38443_日付A」, 1 kiểu 「ポイント」 「TC38443_ポイントA」 (action theo ngưỡng 100)<br>- Lịch salon 「TC38443_サロン」 (loại スタッフ) và lịch lesson 「TC38443_レッスン」 đều đang ON<br>- Mở được: tab 「予約時のお客様への質問項目」 (lối ghi 1) và các màn cấu hình multi action (lối ghi 2) | 1. LỐI GHI 1 — form item: mở tab 「予約時のお客様への質問項目」 của salon, tạo lần lượt item 「短文回答」/「長文回答」/「単一選択」/「複数選択」/「日時」, mỗi item mở droplist 「紐つけ友だち情報」 và ghi lại field nào xuất hiện<br>2. LỐI GHI 2 — multi action: mở 「予約・キャンセル設定」 → sub-tab 「アクション」, thêm action loại 「友だち情報」 vào multi action lúc đặt lịch; ghi lại field nào chọn được và kiểu thao tác của từng loại field<br>3. Lặp bước 2 ở multi action lúc HỦY đặt lịch (4 mốc hủy)<br>4. Lặp bước 2 ở tab 「アクション・詳細設定」 của một コース<br>5. Lặp bước 2 ở cấu hình action của một スタッフ (chỉ salon)<br>6. Lặp toàn bộ bước 1-4 trên lịch lesson (lesson không có staff)<br>7. Lập bảng đối chiếu: lối ghi × loại info → ghi được / không ghi được | 3 field friend info có action (select / date / point) × 2 lối ghi × 4 vị trí gắn multi action | - LỐI GHI 1 (form item): 短文回答 → chỉ hiện field kiểu TEXT · 単一選択 → hiện TEXT + SELECT · 日時 → chỉ hiện DATE. KHÔNG kiểu item nào chọn được field kiểu 「ポイント」 — khớp quyết định Leader đã chốt 2026-09-05 (point là N/A với form nhập)<br>- LỐI GHI 2 (multi action): action 「友だち情報」 chọn được CẢ 3 loại field (select / date / point), mỗi loại có kiểu thao tác riêng — select: ghi option · date: 指定の日付を登録 / 当日日付を登録 / 登録情報を削除 · point: ghi đè / cộng / trừ / xóa / ngẫu nhiên<br>- Bảng đối chiếu cuối cùng phải khớp: point CHỈ ghi được qua lối 2; select và date ghi được qua CẢ 2 lối<br>- Kết quả giống nhau giữa salon và lesson (trừ vị trí staff chỉ có ở salon) |  | TC bản đồ — chốt lối ghi nào áp dụng cho loại info nào, làm tiền đề cho toàn bộ TC lối ghi 2. Sửa 2026-09-07 theo giải thích Leader: bản trước chỉ hỏi 'form có liên kết point không' (câu hỏi Leader đã chốt 2026-09-05), nay mở rộng thành bản đồ đủ 2 lối ghi. Đối chiếu kho TC-SLN-318 (5 kiểu item), TC-LSN-360 (ánh xạ item to friend info), TC-FRI-227 (dialog action chọn trường + kiểu thao tác theo loại). |
| TC-REGSHARED001-06 | UI | REG-SHARED-001 | Salon + Lesson — LINE user tự đặt lịch (LIFF), info kiểu **年月日** | Normal | manual | product | Đối chứng date: khách TỰ đặt lịch có đăng ký lịch chạy action không → so với admin đặt hộ | - Như `TC-FRIEND001-04` (salon) và `TC-FRIEND001-05` (lesson), field date đã cấu hình action<br>- Bot A đã đăng ký LIFF ID cho tính năng đặt lịch<br>- Khách T177_USER10 đã kết bạn bot A, **CHƯA** có giá trị field date, có thiết bị thật đăng nhập LINE<br>- Đã chạy xong `TC-FRIEND001-04` / `TC-FRIEND001-05` và ghi lại kết quả | 1. T177_USER10 mở URL đặt lịch salon từ LINE trên thiết bị thật<br>2. Chọn コース/スタッフ/khung giờ, ở câu hỏi 「日時」 nhập ngày tương lai → xác nhận<br>3. Kiểm giá trị field, dòng lịch sử, **bản ghi lịch chạy action** của T177_USER10<br>4. Lặp bước 1-3 trên lesson<br>5. Lập bảng đối chiếu 4 ô: {salon, lesson} × {admin đặt hộ, khách tự đặt} — ô nào CÓ đăng ký lịch, ô nào KHÔNG | Khách TỰ đặt qua LIFF · câu hỏi 「日時」 = ngày tương lai · field date có action | - Khách tự đặt: giá trị lưu đúng, dòng lịch sử được ghi<br>- **Lịch chạy action được đăng ký** (đây là hành vi đúng, không bị bản vá làm hỏng)<br>- Bảng đối chiếu 4 ô là **bằng chứng quyết định**: nếu khách tự đặt **CÓ** đăng ký lịch mà admin đặt hộ **KHÔNG** ⇒ **GAP-05 là bug thật, cùng bản chất với #38443 nhưng ở loại info date** ⇒ raise ticket<br>- Nếu **cả hai đều KHÔNG** đăng ký ⇒ date vốn chưa bao giờ chạy qua luồng booking ⇒ ghi nhận là hạn chế thiết kế, hỏi PO | | Vừa lấp **GAP-04** (regression user booking) vừa là **đối chứng quyết định** cho **GAP-05** · Cùng mẫu lập luận Dev(AI) đã dùng cho select (*"luồng khách tự đặt lịch vẫn chạy đúng vì có sẵn đoạn kích hoạt action này"*) — áp lại cho date · Evidence: bảng đối chiếu 4 ô + bản ghi lịch chạy action từng ô · **RULE-06** · **RULE-08** |
| TC-FRIEND001-08 | UI | FRIEND-001 | Salon + Lesson — admin đặt lịch hộ, info **date**, chế độ chạy **nhiều lần** | Normal | manual | Tất cả | S2-date: chế độ 何度でも稼働 — mỗi lần admin đặt lịch hộ đổi ngày đều đăng ký lại lịch chạy action | - Field 「TC38443_日付A」 kiểu 「年月日」, action 「TC38443_ACT_D1」, 「登録」 = 「月日」, offset 0 ngày sau, 10:00, **「稼働設定」 = 「何度でも稼働」**<br>- 予約フォーム salon và lesson đều có câu hỏi 「日時」 KHÔNG kèm giờ liên kết field này<br>- Khách T177_D2 đã kết bạn bot A, đã có sẵn giá trị date cũ<br>- Ghi lại số bản ghi lịch chạy action hiện có của T177_D2 | 1. Admin đặt lịch hộ T177_D2 trên salon, câu hỏi 「日時」 nhập **ngày X** (tương lai) → đăng ký<br>2. Kiểm bản ghi lịch chạy action của T177_D2<br>3. Admin đặt lịch hộ lần 2 cho T177_D2, nhập **ngày Y ≠ X** → đăng ký<br>4. Kiểm lại bản ghi lịch: mốc cũ còn hay bị thay?<br>5. Lặp bước 1-4 trên lesson<br>6. Đọc bảng lịch sử friend info: phải có 2 dòng mới | Lần 1 = ngày X · Lần 2 = ngày Y (khác X) · 「稼働設定」 = 「何度でも稼働」 | - Cả 2 lượt đặt lịch thành công, bảng lịch sử có thêm **2 dòng** (登録情報 = X rồi Y)<br>- Sau bước 3: lịch chạy action **được đăng ký lại theo ngày Y** (mốc cũ theo ngày X bị xoá hoặc thay thế — không tồn tại song song 2 mốc mồ côi)<br>- Kết quả **giống hệt** giữa salon và lesson<br>- ⚠️ Nếu KHÔNG bản ghi lịch nào được đăng ký ở cả 2 lượt → **GAP-05 xác nhận** | | Ô **S2 × date** của ma trận · Đối xứng `TC-FRIEND001-01/02` (cùng kịch bản nhưng loại **select**) · Cơ chế date đi qua `settingEventTimeFriendInfo()` → `event_step_time`, khác `sendAction` của select · Evidence: bản ghi lịch chạy action sau mỗi lượt + ảnh 2 dòng lịch sử · Đánh giá spec: Spec ghi rõ (db-mapping §4.4, §5.2) |
| TC-FUNC004-03 | UI | FUNC-004 | Salon + Lesson — admin đặt lịch hộ, info **date**, chế độ chạy **một lần** | Boundary | manual | Tất cả | S3-date: chế độ 一度のみ — khách đã có giá trị date thì đặt lịch hộ lần sau không đăng ký lại lịch action | - Như `TC-FRIEND001-08` nhưng field 「TC38443_日付A」 đặt **「稼働設定」 = 「一度のみ」**<br>- Khách T177_D3 **ĐÃ CÓ** sẵn giá trị date (do lần đặt trước hoặc nhập tay ở tab 「友だち情報」), lịch action của lần đó đã được đăng ký<br>- Ghi lại số bản ghi lịch chạy action hiện có của T177_D3 | 1. Admin đặt lịch hộ T177_D3 trên salon, câu hỏi 「日時」 nhập ngày **khác** giá trị đang có → đăng ký<br>2. Kiểm giá trị field (phải cập nhật) và bảng lịch sử (phải có dòng mới)<br>3. Đối chiếu số bản ghi lịch chạy action với bước chuẩn bị<br>4. Lặp bước 1-3 trên lesson<br>5. So sánh với hành vi của cùng kịch bản ở loại **select** (`TC-FUNC004-01/02`) | Giá trị date cũ → giá trị date mới · 「稼働設定」 = 「一度のみ」 | - Đặt lịch thành công, giá trị date **cập nhật** thành ngày mới, bảng lịch sử có thêm 1 dòng<br>- **KHÔNG** đăng ký thêm bản ghi lịch chạy action mới (số lượng giữ nguyên như bước chuẩn bị)<br>- Cột 「追加時アクション」 của dòng lịch sử mới hiện 「設定なし」<br>- Hành vi **nhất quán** với loại select ở cùng chế độ 一度のみ<br>- ⚠️ Nếu mốc lịch cũ bị **xoá mà không đăng ký mốc mới** → action đã hứa gửi bị mất, raise bug | | Ô **S3 × date** · Đối xứng `TC-FUNC004-01/02` (select) · ⚠️ Điểm rủi ro riêng của date: đổi giá trị date làm mốc gửi lệch — cần chắc mốc cũ không bị xoá oan · Evidence: số đếm bản ghi lịch trước/sau + mốc gửi cụ thể · Đánh giá spec: Spec ghi rõ — nhưng liên quan **CONFLICT spec BR-04** ở §6 mục 2, chốt PO trước khi kết luận |
| TC-DATAAUDIT001-02 | UI | DATA-AUDIT-001 | Salon + Lesson — admin đặt lịch hộ, info **date**, gán lại đúng giá trị cũ | Boundary | manual | Tất cả | S4-date: gán lại đúng ngày khách đang có → không sinh dòng lịch sử mới, không nhân đôi mốc lịch action | - Field 「TC38443_日付A」 kiểu 「年月日」 có action, 「稼働設定」 = 「何度でも稼働」<br>- Khách T177_D4 **ĐANG CÓ** giá trị date đúng bằng ngày Z, mốc lịch action của ngày Z đã đăng ký<br>- Ghi lại số dòng lịch sử của field và số bản ghi lịch chạy action của T177_D4 | 1. Admin đặt lịch hộ T177_D4 trên salon, ở câu hỏi 「日時」 nhập **lại đúng ngày Z**<br>2. Bấm đăng ký<br>3. Đối chiếu số dòng lịch sử của field 「TC38443_日付A」 với bước chuẩn bị<br>4. Đối chiếu số bản ghi lịch chạy action — có bị nhân đôi mốc không?<br>5. Lặp bước 1-4 trên lesson | Giá trị cũ = ngày Z → gán lại đúng ngày Z · 「稼働設定」 = 「何度でも稼働」 | - Đặt lịch thành công<br>- **KHÔNG** thêm dòng lịch sử mới cho field (số dòng giữ nguyên), dòng cũ không bị sửa<br>- Mốc lịch chạy action **KHÔNG bị nhân đôi** — khách không nhận 2 tin cùng nội dung cùng thời điểm<br>- Kết quả giống nhau giữa salon và lesson | | Ô **S4 × date** · Đối xứng `TC-DATAHIST001-01` (salon-select) và `TC-REGSHARED001-01` (lesson-select) · ⚠️ Salon bọc `recordFriendInfoHistory` bằng `if ($oldValue != $newValue)`, lesson gọi không điều kiện → 2 cách viết phải cho cùng kết quả, kiểm luôn cho loại date · Evidence: số đếm dòng lịch sử + mốc lịch trước/sau |
| TC-OUTTRUTH001-04 | UI | OUT-TRUTH-001 | Salon + Lesson — admin đặt lịch hộ, info **date**, chọn 「実行しない」 | Abnormal | manual | Tất cả | S5-date: chọn không thực hiện action khi đặt lịch → action của info date vẫn được đăng ký lịch | - Field 「TC38443_日付A」 kiểu 「年月日」 có action, 「稼働設定」 = 「一度のみ」<br>- コース đang dùng có cấu hình action gửi sau khi đặt lịch (để phân biệt 2 loại action)<br>- Khách T177_D5 chưa có giá trị date<br>- Ghi lại số bản ghi lịch chạy action và số bản ghi action của lượt đặt | 1. Admin đặt lịch hộ T177_D5 trên salon<br>2. Ở câu hỏi 「日時」 nhập ngày tương lai<br>3. Ở mục 「予約時アクションの実行」 chọn **「実行しない」**<br>4. Bấm đăng ký<br>5. Kiểm: action của **lượt đặt** có chạy không? lịch action của **info date** có được đăng ký không?<br>6. Lặp bước 1-5 trên lesson | 「TC38443_日付A」 = ngày tương lai · 「予約時アクションの実行」 = 「実行しない」 | - Đặt lịch thành công<br>- Action gửi sau khi đặt lịch của コース **KHÔNG** chạy<br>- Lịch chạy action của **info date** **VẪN** được đăng ký (cùng nguyên tắc với loại select ở `TC-OUTTRUTH001-01/02`)<br>- ⚠️ Hành vi này thuộc **CONFLICT BUG-R1 chờ PO** — ghi nhận, **không tự chấm Đạt/Không đạt** | | Ô **S5 × date** · Đối xứng `TC-OUTTRUTH001-01/02` (select) · Rủi ro cao hơn select: action date gửi **sau nhiều ngày**, khách nhận tin lúc admin đã quên mình chọn 「実行しない」 · Evidence: 2 số đếm (action lượt đặt / lịch action date) + ảnh cấu hình · Đánh giá spec: Spec không ghi → **hỏi PO** (§6 mục 1) |
| TC-DATAAUDIT001-03 | UI | DATA-AUDIT-001 | Chi tiết bạn bè — lịch sử info **date** sinh từ admin đặt lịch hộ | Normal | manual | Tất cả | S6-date: dòng lịch sử của info date sinh từ đặt lịch hộ hiển thị đúng cột 「追加時アクション」 | - Đã chạy xong `TC-FRIEND001-04` (salon, date 月日 có action) — giá trị đã gán, lịch action đã đăng ký<br>- Đã chờ tới mốc gửi và job nền đã chạy xong action đó (hoặc nhờ Dev đẩy mốc về hiện tại)<br>- Có sẵn 1 dòng lịch sử của info date **không** gắn action để đối chứng | 1. Mở 「友だち詳細」 của khách → tab 「友だち情報」<br>2. Tìm dòng lịch sử của field 「TC38443_日付A」 sinh từ lượt đặt lịch hộ<br>3. Đọc cột 「追加時アクション」<br>4. Bấm 「プレビュー」 nếu có<br>5. Đối chiếu với dòng lịch sử của info date **không** gắn action | Dòng lịch sử info date có action (đã chạy) vs dòng info date không action | - Dòng có action **đã chạy**: cột 「追加時アクション」 hiện **「プレビュー」** bấm được, nội dung khớp action 「TC38443_ACT_D1」<br>- Dòng không gắn action: hiện **「設定なし」**, không bấm được<br>- ⚠️ Nếu dòng có action vẫn hiện 「設定なし」 sau khi job đã chạy → **cùng triệu chứng ticket #38443 nhưng ở loại date** ⇒ raise bug<br>- ⚠️ Nếu **chưa từng** có mốc lịch nào được đăng ký (GAP-05) thì TC này **không chạy được** — ghi skip kèm lý do, trỏ về `TC-FRIEND001-04` | | Ô **S6 × date** · Đối xứng `TC-DATAHIST001-02` (select — đang **skip** trên Studio) · ⚠️ Với date, liên kết preview chỉ xuất hiện **sau khi job gửi thật**, không phải ngay lúc đặt lịch như select → thời điểm kiểm khác hẳn · Evidence: ảnh cột 「追加時アクション」 2 dòng đối chứng + ảnh preview · Liên quan **REQ-006** (job nền) đang trắng TC |
| TC-MSG004-03 | UI | MSG-004 | Salon + Lesson — khách nhận tin action info **date** trên LINE | Normal | manual | product | S7-date: khách nhận đúng tin của action info date vào đúng mốc thời gian đã cấu hình | - Đã chạy `TC-FRIEND001-04` / `TC-FRIEND001-05`, lịch action đã được đăng ký<br>- Có thiết bị thật đăng nhập LINE bằng tài khoản khách test<br>- Job nền xử lý lịch gửi đang chạy<br>- Đã ghi lại **mốc gửi dự kiến** (ngày ± offset, giờ HH:mm) của từng khách | 1. Chờ tới **mốc gửi dự kiến** của khách trên salon (hoặc nhờ Dev đẩy mốc về hiện tại)<br>2. Mở LINE trên thiết bị thật, xem phòng chat với bot A<br>3. Ghi lại **thời điểm thực tế** nhận tin và nội dung<br>4. Lặp bước 1-3 cho khách trên lesson (chế độ 年月日, offset 1 ngày trước)<br>5. Đối chiếu thời điểm thực tế với mốc dự kiến | Mốc gửi = ngày đã nhập ± offset lúc HH:mm đã cấu hình | - Khách nhận **đúng 1** tin 「TC38443 日付アクション」, nội dung khớp action đã cấu hình<br>- Thời điểm nhận **khớp mốc dự kiến** (sai số trong dung sai của job), **không sớm/muộn cả ngày**<br>- Không trùng lặp, không nhận trước khi tới mốc<br>- Chế độ 「月日」: mốc lặp lại năm sau · chế độ 「年月日」: chỉ gửi 1 lần duy nhất | | Ô **S7 × date** · Đối xứng `TC-MSG004-01` (salon-select, **chưa chạy**) và `TC-MSG004-02` (lesson-select) · **RULE-06** output cuối chuỗi trên thiết bị thật · **RULE-08** — gửi tin LINE + **job nền** ⇒ **không** kết luận từ local/staging · Evidence: ảnh phòng chat kèm timestamp + bảng đối chiếu mốc dự kiến vs thực tế |
| TC-FRIEND001-09 | UI | FRIEND-001 | Salon + Lesson — multi action lúc đặt lịch, action 「友だち情報」 ghi info point | Normal | manual | Tất cả | Lối ghi 2 - multi action lúc đặt lịch ghi point đạt ngưỡng thì action của info point được kích hoạt | - Field friend info kiểu 「ポイント」 「TC38443_ポイントA」, action 「TC38443_ACT_P1」 (1 tin text 「TC38443 ポイントアクション」) tại ngưỡng 100 điểm, 「稼働設定」 = 「一度のみ」<br>- Point KHÔNG ghi được qua form item (Leader chốt 2026-09-05) → dùng LỐI GHI 2: ở 「予約・キャンセル設定」 → sub-tab 「アクション」, multi action lúc đặt lịch có thêm action loại 「友だち情報」 ghi 「TC38443_ポイントA」 = 100<br>- Khách T177_P1 đã kết bạn bot A, điểm hiện tại = 0, có thiết bị thật đăng nhập LINE<br>- Ghi lại số bản ghi chạy action hiện có của T177_P1 | 1. Admin đặt lịch hộ T177_P1 trên salon (multi action lúc đặt lịch sẽ tự ghi point = 100)<br>2. Bấm đăng ký<br>3. Mở 「友だち詳細」 → tab 「友だち情報」: đọc giá trị point của khách<br>4. Đọc bảng lịch sử: có dòng mới cho field 「TC38443_ポイントA」 không, cột 「追加時アクション」 hiện gì<br>5. Đối chiếu số bản ghi chạy action của T177_P1<br>6. Kiểm phòng chat LINE của T177_P1<br>7. Lặp bước 1-6 trên lesson với khách khác | Multi action lúc đặt lịch ghi 「TC38443_ポイントA」 = 100 (= ngưỡng cấu hình) · 「稼働設定」 = 「一度のみ」 | - Đặt lịch thành công; giá trị point của khách được cập nhật = 100 (kiểm chứng 1: update được friend info)<br>- Bảng lịch sử có thêm 1 dòng cho field point (kiểm chứng 3: ghi lịch sử trên trang detail)<br>- Action 「TC38443_ACT_P1」 được kích hoạt vì đạt ngưỡng — sinh 1 bản ghi chạy action (kiểm chứng 2: chạy action nếu thỏa điều kiện)<br>- Khách nhận tin 「TC38443 ポイントアクション」 trên LINE<br>- Cột 「追加時アクション」 của dòng lịch sử hiện 「プレビュー」 và bấm ra đúng nội dung<br>- Kết quả giống nhau giữa salon và lesson |  | LỐI GHI 2 - vị trí: multi action lúc đặt lịch (cấp calendar). Sửa 2026-09-07 theo giải thích Leader: point chỉ ghi được qua multi action, không qua form item. 3 kiểm chứng bắt buộc: update giá trị + chạy action nếu thỏa + ghi lịch sử. Đối chiếu kho TC-FRI-202/203 (multi action set/cộng/trừ/xóa/random điểm). |
| TC-FRIEND001-10 | UI | FRIEND-001 | Salon + Lesson — multi action lúc đặt lịch, info point, chế độ chạy nhiều lần | Normal | manual | Tất cả | Lối ghi 2 - chế độ 何度でも稼働: mỗi lần multi action ghi point vượt ngưỡng đều kích hoạt lại action | - Như TC-FRIEND001-09 nhưng field 「TC38443_ポイントA」 đặt 「稼働設定」 = 「何度でも稼働」<br>- Multi action lúc đặt lịch cấu hình action 「友だち情報」 kiểu CỘNG điểm (+120 rồi +30) để mỗi lượt đặt lịch đều đẩy điểm vượt ngưỡng<br>- Khách T177_P2 điểm hiện tại = 0<br>- Ghi lại số bản ghi chạy action hiện có của T177_P2 | 1. Admin đặt lịch hộ T177_P2 trên salon lần 1 → multi action cộng điểm lên 120 (vượt ngưỡng 100)<br>2. Kiểm giá trị point, bảng lịch sử và số bản ghi chạy action (kỳ vọng +1)<br>3. Admin đặt lịch hộ T177_P2 lần 2 → multi action cộng tiếp lên 150<br>4. Kiểm lại giá trị point, bảng lịch sử và số bản ghi chạy action (kỳ vọng +1 nữa)<br>5. Kiểm phòng chat LINE: khách nhận mấy tin?<br>6. Lặp trên lesson | Lần 1: point 0 → 120 · Lần 2: point 120 → 150 · ngưỡng = 100 · 「稼働設定」 = 「何度でも稼働」 | - Cả 2 lượt thành công; giá trị point cập nhật đúng sau mỗi lượt (0 → 120 → 150)<br>- Bảng lịch sử có thêm 2 dòng cho field point<br>- 2 bản ghi chạy action mới được sinh (mỗi lượt 1) vì chế độ 何度でも稼働<br>- Khách nhận 2 tin 「TC38443 ポイントアクション」<br>- Kết quả giống nhau giữa salon và lesson |  | LỐI GHI 2 - trục chế độ chạy action nhiều lần. Sửa 2026-09-07: lối gán đổi từ form item sang multi action. |
| TC-FUNC004-04 | UI | FUNC-004 | Salon + Lesson — multi action lúc đặt lịch, info point, chế độ chạy một lần + biên ngưỡng | Boundary | manual | Tất cả | Lối ghi 2 - chế độ 一度のみ + biên ngưỡng điểm: khách đã đạt ngưỡng thì lần sau không kích hoạt lại | - Như TC-FRIEND001-09 với 「稼働設定」 = 「一度のみ」<br>- Multi action lúc đặt lịch cấu hình action 「友だち情報」 kiểu GHI ĐÈ điểm, giá trị đổi theo từng lượt test<br>- Khách T177_P3 ĐÃ đạt ngưỡng 100 trước đó và action đã chạy 1 lần<br>- Ghi lại số bản ghi chạy action hiện có của T177_P3 | 1. Admin đặt lịch hộ T177_P3 trên salon, multi action ghi đè point = 200 (vẫn vượt ngưỡng)<br>2. Kiểm giá trị point (phải cập nhật = 200) và bảng lịch sử (phải có dòng mới)<br>3. Đối chiếu số bản ghi chạy action với bước chuẩn bị<br>4. Kiểm phòng chat LINE — khách có nhận thêm tin không?<br>5. Thử biên: đổi multi action ghi đè point = 99 (dưới ngưỡng) → đặt lịch → kiểm; rồi = 100 (đúng ngưỡng) → đặt lịch → kiểm<br>6. Lặp trên lesson | Điểm cũ ≥ 100 (đã chạy action) → ghi đè 200 · biên 99 và 100 · 「稼働設定」 = 「一度のみ」 | - Giá trị point cập nhật đúng ở mọi lượt, bảng lịch sử có dòng mới ở mọi lượt<br>- KHÔNG sinh bản ghi chạy action mới (số lượng giữ nguyên), khách không nhận thêm tin<br>- Biên: point = 99 → không kích hoạt (dưới ngưỡng) · point = 100 → đúng ngưỡng nhưng do 一度のみ và đã chạy nên vẫn không kích hoạt lại<br>- Hành vi nhất quán với loại select ở cùng chế độ 一度のみ |  | LỐI GHI 2 - trục chế độ chạy một lần + biên ngưỡng điểm. Loại case Boundary theo RULE-01 cho FUNC-004 ưu tiên Cao. Sửa 2026-09-07: lối gán đổi từ form item sang multi action. |
| TC-DATAAUDIT001-04 | UI | DATA-AUDIT-001 | Salon + Lesson — multi action lúc đặt lịch, info point, ghi lại đúng giá trị cũ | Boundary | manual | Tất cả | Lối ghi 2 - multi action ghi lại đúng số điểm khách đang có thì không sinh dòng lịch sử mới | - Field 「TC38443_ポイントA」 có action tại ngưỡng 100, 「稼働設定」 = 「何度でも稼働」<br>- Multi action lúc đặt lịch cấu hình action 「友だち情報」 kiểu GHI ĐÈ point = 120<br>- Khách T177_P4 ĐANG CÓ point đúng bằng 120<br>- Ghi lại số dòng lịch sử của field và số bản ghi chạy action của T177_P4 | 1. Admin đặt lịch hộ T177_P4 trên salon → multi action ghi đè lại đúng 120<br>2. Bấm đăng ký<br>3. Đối chiếu số dòng lịch sử của field 「TC38443_ポイントA」 với bước chuẩn bị<br>4. Đối chiếu số bản ghi chạy action<br>5. Lặp trên lesson | Điểm cũ = 120 → multi action ghi đè lại đúng 120 · 「稼働設定」 = 「何度でも稼働」 | - Đặt lịch thành công<br>- KHÔNG thêm dòng lịch sử mới cho field (giá trị không đổi), dòng cũ không bị sửa<br>- Nếu chế độ 何度でも稼働 vẫn khiến action chạy thì bản ghi action đó không mang tham chiếu tới dòng lịch sử nào, và không dòng nào chuyển từ 「設定なし」 sang 「プレビュー」<br>- Kết quả giống nhau giữa salon và lesson |  | LỐI GHI 2 - trục giá trị không đổi. Đối chiếu kho TC-FRI-236 (action gán giá trị nhưng giá trị không đổi → không cập nhật lại lịch). Sửa 2026-09-07: lối gán đổi từ form item sang multi action. |
| TC-OUTTRUTH001-05 | UI | OUT-TRUTH-001 | Salon + Lesson — multi action lúc đặt lịch, info point, chọn 実行しない | Abnormal | manual | Tất cả | Lối ghi 2 - chọn 実行しない khi đặt lịch thì multi action KHÔNG chạy nên info point cũng không được ghi | - Field 「TC38443_ポイントA」 có action tại ngưỡng 100, 「稼働設定」 = 「一度のみ」<br>- Multi action lúc đặt lịch có action 「友だち情報」 ghi point = 100 và 1 action text để phân biệt<br>- Khách T177_P5 điểm hiện tại = 0<br>- Ghi lại số bản ghi chạy action và giá trị point hiện tại của T177_P5 | 1. Admin đặt lịch hộ T177_P5 trên salon<br>2. Ở mục 「予約時アクションの実行」 chọn 「実行しない」<br>3. Bấm đăng ký<br>4. Kiểm giá trị point của khách — có bị ghi = 100 không?<br>5. Kiểm bảng lịch sử — có dòng mới cho field point không?<br>6. Kiểm số bản ghi chạy action và phòng chat LINE<br>7. Lặp trên lesson<br>8. Đối chứng: lặp lại toàn bộ nhưng chọn 「実行する」 để thấy khác biệt | Multi action lúc đặt lịch ghi point = 100 · 「予約時アクションの実行」 = 「実行しない」 | - Đặt lịch thành công<br>- Vì point được ghi QUA multi action, mà 「実行しない」 chặn cả multi action → giá trị point KHÔNG bị ghi, KHÔNG có dòng lịch sử mới, action của info point KHÔNG chạy, khách KHÔNG nhận tin<br>- Đây là điểm KHÁC BẢN CHẤT với lối ghi 1: ở lối ghi 1 (form item) action friend info VẪN chạy dù chọn 「実行しない」 (xem TC-OUTTRUTH001-01/02) vì nó nằm ngoài nhánh doAction<br>- Bước 8 đối chứng 「実行する」: point ghi = 100, có dòng lịch sử, action chạy, khách nhận tin<br>- Chênh lệch hành vi giữa 2 lối ghi phải được ghi rõ vào ticket để PO xác nhận |  | LỐI GHI 2 - trục 実行しない. Điểm mấu chốt: lối 2 đi qua nhánh doAction nên BỊ chặn, lối 1 KHÔNG bị chặn (CONFLICT BUG-R1 chờ PO). Cặp TC-OUTTRUTH001-01/02 (lối 1) và TC này (lối 2) dùng để chứng minh chênh lệch đó. Sửa 2026-09-07 theo giải thích Leader. |
| TC-DATAAUDIT001-05 | UI | DATA-AUDIT-001 | Chi tiết bạn bè — lịch sử info point sinh từ multi action của luồng đặt lịch | Normal | manual | Tất cả | Lối ghi 2 - dòng lịch sử info point sinh từ multi action hiển thị đúng cột 追加時アクション | - Đã chạy xong TC-FRIEND001-09 (multi action ghi point đạt ngưỡng, action đã chạy)<br>- Job nền đã xử lý xong bản ghi chạy action tương ứng<br>- Có sẵn 1 dòng lịch sử info point do multi action ghi nhưng KHÔNG đạt ngưỡng (action không chạy) để đối chứng | 1. Mở 「友だち詳細」 của khách → tab 「友だち情報」<br>2. Tìm dòng lịch sử của field 「TC38443_ポイントA」 sinh từ multi action của lượt đặt lịch đạt ngưỡng<br>3. Đọc cột 「追加時アクション」 → bấm 「プレビュー」<br>4. Đối chiếu với dòng lịch sử của lượt KHÔNG đạt ngưỡng<br>5. Đối chiếu cột 「トリガー」 của 2 dòng — phải chỉ đúng nguồn ghi là multi action của luồng đặt lịch<br>6. Đối chiếu tiếp với 1 dòng lịch sử sinh từ LỐI GHI 1 (form item, field select) để thấy 2 lối ghi đều ghi lịch sử đúng | Dòng lịch sử point đạt ngưỡng (action đã chạy) vs dòng point dưới ngưỡng vs dòng select từ form item | - Dòng đạt ngưỡng có action đã chạy: cột 「追加時アクション」 hiện 「プレビュー」 bấm được, nội dung khớp 「TC38443_ACT_P1」<br>- Dòng dưới ngưỡng: hiện 「設定なし」, không bấm được<br>- Cột 「トリガー」 phản ánh đúng nguồn ghi của từng dòng<br>- Dòng sinh từ lối ghi 1 cũng hiện 「プレビュー」 đúng — cả 2 lối ghi đều ghi lịch sử đầy đủ trên trang detail line user<br>- Không dòng nào lệch giữa màn hình và dữ liệu liên kết thật |  | LỐI GHI 2 - kiểm chứng 3 (ghi lịch sử trên trang detail line user) mà Leader yêu cầu, đồng thời đối chiếu chéo với lối ghi 1. Sửa 2026-09-07 theo giải thích Leader. |
| TC-MSG004-04 | UI | MSG-004 | Salon + Lesson — khách nhận tin action info point trên LINE (lối ghi multi action) | Normal | manual | product | Lối ghi 2 - khách nhận đúng tin của action info point khi multi action đẩy điểm đạt ngưỡng | - Đã chạy TC-FRIEND001-09, multi action đã ghi point đạt ngưỡng và action point đã được kích hoạt<br>- Có thiết bị thật đăng nhập LINE bằng tài khoản khách test<br>- Job nền xử lý action đang chạy<br>- Multi action lúc đặt lịch còn có 1 action text riêng để đối chiếu thứ tự | 1. Sau khi admin đặt lịch hộ và multi action ghi point đạt ngưỡng, mở LINE trên thiết bị thật<br>2. Xem phòng chat với bot A, đếm số tin nhận được và đọc nội dung<br>3. Đối chiếu nội dung với action 「TC38443_ACT_P1」 đã cấu hình<br>4. Ghi lại thứ tự tin của multi action và tin của action info point<br>5. Lặp cho khách trên lesson | Multi action ghi point = 100 (đạt ngưỡng) · action 「TC38443_ACT_P1」 = 1 tin text 「TC38443 ポイントアクション」 | - Khách nhận đúng 1 tin 「TC38443 ポイントアクション」, nội dung khớp cấu hình<br>- Không trùng lặp, không sai thứ tự<br>- Tin của action info point đến SAU tin của multi action đã ghi giá trị (vì action point là hệ quả của việc ghi giá trị)<br>- Kết quả giống nhau giữa salon và lesson<br>- Khách ở lượt dưới ngưỡng không nhận tin nào |  | LỐI GHI 2 - RULE-06 output cuối chuỗi trên thiết bị thật. RULE-08 gửi tin LINE nên không kết luận từ local/staging. Sửa 2026-09-07 theo giải thích Leader. |
| TC-FUNC001-01 | UI | FUNC-001 | Salon — admin đặt lịch hộ, staff 「指定なし」 + KHÔNG tự phân công | Normal | manual | product | S8a: staff 指定なし không random — action course chạy, KHÔNG có action staff, action friend info vẫn chạy | - Đăng nhập admin bot A, lịch salon 「TC38443_サロン」 loại 「スタッフ」, có staff S1/S2 (+ 指定なし bật)<br>- 「スタッフ自動割り当て」 đặt **option 1 — không tự phân công**<br>- コース 「TC38443_コース」 CÓ cấu hình action gửi sau khi đặt lịch 「TC38443_ACT_COURSE」 (1 tin text riêng)<br>- Staff S1 và S2 mỗi người CÓ action riêng 「TC38443_ACT_S1」 / 「TC38443_ACT_S2」<br>- Field 「TC38443_選択A」, 「オプションA」 gắn action 「TC38443_ACT_A」, chế độ 「一度のみ」; 予約フォーム có câu hỏi liên kết<br>- Khách T177_ST1 đã kết bạn bot A, CHƯA có giá trị field, có thiết bị thật đăng nhập LINE<br>- Ghi lại số bản ghi chạy action hiện có của T177_ST1 | 1. Mở 「予約カレンダー(サロン)」 → khung giờ trống → modal 「予約追加」<br>2. Chọn khách T177_ST1, chọn コース 「TC38443_コース」<br>3. Ở ô chọn スタッフ giữ nguyên 「指定なし」<br>4. Ở câu hỏi liên kết 「TC38443_選択A」 chọn 「オプションA」<br>5. Giữ 「予約時アクションの実行」 = 「実行する」 → bấm đăng ký<br>6. Kiểm booking vừa tạo: cột スタッフ hiển thị gì, dữ liệu staff của booking để trống hay có giá trị<br>7. Đối chiếu số bản ghi chạy action của T177_ST1 và xác định từng bản ghi thuộc action nào<br>8. Mở LINE trên thiết bị thật, đếm và đọc từng tin nhận được | Khách T177_ST1 · コース 「TC38443_コース」 (có action) · スタッフ = 「指定なし」 · 「TC38443_選択A」 = 「オプションA」 · tự phân công = option 1 | - Đặt lịch thành công, booking hiển thị スタッフ = 「指定なし」, dữ liệu staff của booking để trống (không bị gán staff nào)<br>- Sinh **đúng 2** bản ghi chạy action: 1 của 「TC38443_ACT_A」 (friend info, loại kích hoạt 6004) + 1 của 「TC38443_ACT_COURSE」<br>- **KHÔNG** sinh bản ghi nào của 「TC38443_ACT_S1」 / 「TC38443_ACT_S2」<br>- Khách nhận đúng 2 tin tương ứng, không thừa không thiếu<br>- Dòng lịch sử friend info hiện 「プレビュー」 | | Ô **S8a** · Lấp **GAP-06** · Nguồn: `salon-booking/web/logic-spec.md:265` (staff có `action_id_send_after_booking`), `feature-spec.md:759` (指定なし = staff NULL) · Kho `TC-SLN-263` cover random option 1 nhưng **viết trước bản vá**, không kiểm action friend info · Evidence: ảnh booking + liệt kê từng bản ghi chạy action + ảnh phòng chat LINE · **RULE-06** · **RULE-07** (khớp DB + màn + output) · Đánh giá spec: Spec ghi rõ |
| TC-FUNC001-02 | UI | FUNC-001 | Salon — admin đặt lịch hộ, staff 「指定なし」 + CÓ random tự phân công | Boundary | manual | product | S8b: random staff — action của ĐÚNG staff được phân công chạy, không lấy nhầm staff khác, không mất action | - Như TC-FUNC001-01, nhưng 「スタッフ自動割り当て」 đặt **option 2 (random tự do)**<br>- Chỉ S1 và S2 thỏa điều kiện khung giờ; mỗi staff có action riêng phân biệt được nội dung<br>- Khách T177_ST2 chưa có giá trị field 「TC38443_選択A」<br>- Ghi lại số bản ghi chạy action hiện có của T177_ST2 | 1. Mở modal 「予約追加」 của lịch salon<br>2. Chọn khách T177_ST2, chọn コース, giữ スタッフ = 「指定なし」<br>3. Ở câu hỏi liên kết 「TC38443_選択A」 chọn 「オプションA」 → bấm đăng ký<br>4. Mở booking vừa tạo, **ghi lại staff thực tế được phân công** (S1 hay S2)<br>5. Đối chiếu số bản ghi chạy action và xác định từng bản ghi thuộc action nào<br>6. Mở LINE trên thiết bị thật, đối chiếu tin action staff nhận được với staff ghi ở bước 4<br>7. Lặp bước 1-6 thêm 3 lần với khách khác nhau để thấy random rơi vào cả S1 và S2<br>8. Đổi 「スタッフ自動割り当て」 sang **option 3 (theo thứ tự ưu tiên)** rồi lặp bước 1-6 | スタッフ = 「指定なし」 + random bật (option 2, rồi option 3) · コース có action · 「TC38443_選択A」 = 「オプションA」 | - Mỗi lượt đặt lịch đều được gán 1 staff cụ thể (S1 hoặc S2), không để trống<br>- Sinh **đúng 3** bản ghi chạy action mỗi lượt: friend info 「TC38443_ACT_A」 + course 「TC38443_ACT_COURSE」 + action của **đúng staff đã được phân công**<br>- Tin action staff khách nhận được **khớp đúng** staff ghi ở bước 4 — không phải staff còn lại, không phải cả hai<br>- Option 3: staff được chọn là người **đầu tiên thỏa mãn** theo danh sách ưu tiên, action tương ứng chạy đúng<br>- Không lượt nào mất action staff hoặc sinh action staff trùng | | Ô **S8b** · Lấp **GAP-06** · ⚠️ Rủi ro kiểm: khối friend info action mới chèn **ngay trước** `sendActionBooking` — nếu chạy trước khi `staff_id` được quyết định thì action staff có thể lấy sai hoặc rỗng · Kho `TC-SLN-264/265/267/268` cover random nhưng không kiểm action friend info · Loại case Boundary theo **RULE-01** cho `FUNC-001` ưu tiên Cao · Evidence: bảng 4 lượt {staff phân công ↔ action staff nhận được} + ảnh phòng chat |
| TC-MSG002-01 | UI | MSG-002 | Salon — admin đặt lịch hộ, chọn staff cụ thể + course có action | Normal | manual | product | S8c: đủ 3 loại action (friend info + course + staff) chạy đúng số lượng, đúng nội dung, đúng thứ tự | - Như TC-FUNC001-01 nhưng chọn **staff cụ thể S1** thay vì 「指定なし」<br>- コース 「TC38443_コース」 có action 「TC38443_ACT_COURSE」; staff S1 có action 「TC38443_ACT_S1」; field 「TC38443_選択A」 có action 「TC38443_ACT_A」 — **3 action nội dung khác nhau, phân biệt được**<br>- Khách T177_ST3 chưa có giá trị field, có thiết bị thật đăng nhập LINE<br>- Ghi lại số bản ghi chạy action hiện có của T177_ST3 và **ảnh chụp phòng chat trước khi test** | 1. Mở modal 「予約追加」, chọn khách T177_ST3<br>2. Chọn コース 「TC38443_コース」 và **staff S1** cụ thể<br>3. Ở câu hỏi liên kết 「TC38443_選択A」 chọn 「オプションA」<br>4. Giữ 「予約時アクションの実行」 = 「実行する」 → bấm đăng ký<br>5. Đối chiếu số bản ghi chạy action, liệt kê từng bản ghi thuộc action nào<br>6. Mở LINE trên thiết bị thật, **ghi lại thứ tự** các tin nhận được kèm timestamp<br>7. So sánh thứ tự này với kết quả chạy cùng kịch bản trên bản **chưa vá** (hoặc với mô tả hành vi cũ mà Dev/QA xác nhận) | Khách T177_ST3 · コース có action · staff S1 có action · 「TC38443_選択A」 = 「オプションA」 (có action) | - Sinh **đúng 3** bản ghi chạy action: friend info (loại kích hoạt 6004) + course + staff S1 — không thiếu, không trùng<br>- Khách nhận **đúng 3** tin, nội dung khớp từng action đã cấu hình<br>- Dòng lịch sử friend info hiện 「プレビュー」 và bấm ra đúng nội dung 「TC38443_ACT_A」<br>- ⚠️ **Ghi nhận thứ tự nhận tin**: sau bản vá action friend info chạy **trước** action course/staff. Nếu thứ tự khác trước bản vá → là thay đổi hành vi thấy được, **ghi vào ticket để PO xác nhận**, không tự chấm Không đạt | | Ô **S8c** · Lấp **GAP-06** · Bản vá chèn khối mới ở `CalendarSalonLineBookingService.php:3977-3992`, ngay trước `sendActionBooking` (`:3993-4001`) ⇒ thứ tự gửi đổi · Kho `TC-SLN-113` (5 cấu hình action khi admin book) và `TC-SLN-446` (ưu tiên action course/staff) viết trước bản vá, không có action friend info · Evidence: ảnh phòng chat LINE kèm timestamp từng tin + liệt kê 3 bản ghi action · **RULE-06** · **RULE-08** |
| TC-MSG002-02 | UI | MSG-002 | Lesson — admin đặt lịch hộ, ma trận ưu tiên action của course | Normal | manual | product | S8d: ma trận ưu tiên action của course khi admin book KHÔNG bị bản vá làm đổi | - Đăng nhập admin bot A, lịch lesson 「TC38443_レッスン」 có 受付 còn chỗ<br>- コース 「TC38443_コースL」 cấu hình được cả **message text** và **multi action** ở tab 「アクション・詳細設定」<br>- Field 「TC38443_選択L」 có action 「TC38443_ACT_L1」 ở 「オプションL1」, chế độ 「一度のみ」; 予約フォーム có câu hỏi liên kết<br>- Khách test riêng cho từng tổ hợp, đều chưa có giá trị field, có thiết bị thật đăng nhập LINE<br>- Đã ghi lại kết quả ma trận ưu tiên action **trước bản vá** (hoặc dẫn chiếu kho `TC-LSN-102`/`103`/`104`/`107`) | 1. Tổ hợp A — course **CÓ** message text: admin đặt lịch hộ gán 「オプションL1」 → kiểm tin khách nhận<br>2. Tổ hợp B — course **KHÔNG** có message text + multi action **KHÔNG** có action detail → lặp bước 1<br>3. Tổ hợp C — course **KHÔNG** có message text + multi action **CÓ** action detail → lặp bước 1<br>4. Tổ hợp D — course không cấu hình gì, chỉ có action chung của lịch → lặp bước 1<br>5. Với mỗi tổ hợp: liệt kê bản ghi chạy action, đọc tin trên LINE, ghi rõ tin nào của course, tin nào của friend info<br>6. Đối chiếu 4 tổ hợp với kết quả trước bản vá | 4 tổ hợp {course có/không message text} × {multi action có/không detail} · 「TC38443_選択L」 = 「オプションL1」 (có action) | - Ở **cả 4 tổ hợp**: quy tắc ưu tiên action của course giữ **nguyên như trước bản vá** — bản vá không được làm đổi tổ hợp nào<br>- Ngoài action của course, mỗi lượt sinh **thêm đúng 1** bản ghi action của friend info (loại kích hoạt 6004), độc lập với ma trận ưu tiên<br>- Khách nhận đủ tin của cả 2 nguồn, không tin nào bị nuốt<br>- Dòng lịch sử friend info hiện 「プレビュー」 ở cả 4 tổ hợp | | Ô **S8d** · Lấp **GAP-06** cho lesson · Lesson **không có** khái niệm staff nên chỉ kiểm course · Dẫn chiếu kho `TC-LSN-102` / `103` / `104` / `107` (ma trận ưu tiên action course) — TC này **không viết lại** ma trận mà chỉ kiểm ma trận đó không đổi sau bản vá · Evidence: bảng 4 tổ hợp {tin course nhận được / tin friend info nhận được} + ảnh phòng chat · **RULE-06** · Đánh giá spec: Spec ghi rõ |
| TC-REGSHARED001-07 | UI | REG-SHARED-001 | Salon + Lesson — tương tác tag của action friend info với filter của action course/staff | Abnormal | manual | product | S8e: action friend info gán TAG → filter của action course/staff đánh giá theo trạng thái tag nào | - Tạo tag 「TC38443_タグ」<br>- Action 「TC38443_ACT_A」 (gắn ở 「オプションA」 của field 「TC38443_選択A」) cấu hình **gán tag 「TC38443_タグ」** cho khách<br>- コース 「TC38443_コース」 có action riêng, và action đó đặt **filter chỉ gửi cho khách CÓ tag 「TC38443_タグ」**<br>- Khách T177_TAG1 đã kết bạn bot A, **CHƯA có tag** 「TC38443_タグ」 và **chưa có giá trị** field 「TC38443_選択A」<br>- Có thiết bị thật đăng nhập LINE bằng T177_TAG1<br>- Ghi lại số bản ghi chạy action hiện có của T177_TAG1 | 1. Admin đặt lịch hộ T177_TAG1 trên salon, chọn コース 「TC38443_コース」<br>2. Ở câu hỏi liên kết 「TC38443_選択A」 chọn 「オプションA」 (action này sẽ gán tag)<br>3. Giữ 「予約時アクションの実行」 = 「実行する」 → bấm đăng ký<br>4. Kiểm khách T177_TAG1 đã được gán tag 「TC38443_タグ」 chưa<br>5. Kiểm action của course **có chạy hay không** — liệt kê bản ghi chạy action<br>6. Mở LINE trên thiết bị thật, đếm tin nhận được<br>7. Lặp toàn bộ trên lesson với コース 「TC38443_コースL」<br>8. Lặp lần nữa với khách **ĐÃ CÓ SẴN** tag 「TC38443_タグ」 từ trước để so sánh | Khách chưa có tag · action friend info gán tag 「TC38443_タグ」 · action course có filter yêu cầu tag đó | - Khách được gán tag 「TC38443_タグ」 do action friend info<br>- **Điểm cần chốt**: action của course có chạy cho khách này không?<br>  · Nếu **CÓ** chạy ⇒ filter đánh giá **SAU** khi action friend info gán tag ⇒ **hành vi đổi so với trước bản vá** (trước đây khách chưa có tag nên không thỏa filter) ⇒ ghi nhận là thay đổi hành vi, báo PO<br>  · Nếu **KHÔNG** chạy ⇒ filter đánh giá **TRƯỚC** ⇒ giữ nguyên hành vi cũ<br>- Lượt với khách đã có sẵn tag: action course **phải chạy** ở cả trước và sau bản vá (đối chứng)<br>- Kết quả phải **giống nhau** giữa salon và lesson; lệch nhau là bất nhất, raise ngay<br>- Dù nhánh nào, phải ghi rõ kết luận vào ticket vì `03-dev-impact.md` **không kê** ảnh hưởng này | | Ô **S8e** · Lấp **GAP-06** · ⚠️ Đây là rủi ro **không ai kê**: khối friend info action chèn ngay trước `sendActionBooking` làm đổi trạng thái tag tại thời điểm filter được đánh giá · Nguồn: `salon-booking/web/logic-spec.md:246, :265` (course và staff đều có `filter_id`/`filter_number`); kho `TC-LSN-101` (*"Filter course chỉ send multi action cho user thỏa filter"*) · Evidence: ảnh tag của khách trước/sau + liệt kê bản ghi action + ảnh phòng chat · Đánh giá spec: **Spec không ghi** → bắt buộc hỏi PO/BA |

### TC dẫn chiếu từ kho — KHÔNG viết mới, đưa vào bộ regression lần này

| ID kho | Tính năng | Lý do đưa vào |
|---|---|---|
| `TC-EBK-307` | Event booking | ⭐ Tiền điều kiện *"CHỈ chạy sau khi Dev xác nhận luồng đó cũng gán friend info select-có-action"* — **#38443 vừa xác nhận cho lesson/salon** → TC nay đủ điều kiện chạy |
| `TC-EBK-302` | Event booking | Regression fix #38312 (cùng triệu chứng 「設定なし」/「プレビュー」) còn nguyên trên `release_step_20260805` |
| `TC-EBK-306` | Event booking | Đối chứng âm: info không có action vẫn hiện 「設定なし」 |
| `TC-EBK-304` | Event booking — app mobile | Đối chứng: event **app** đã đúng, salon/lesson **app** chưa → làm nổi bật GAP-03 |
| `TC-SLN-113` | Salon | Action khi admin thêm booking theo 5 cấu hình — kiểm action friend info mới không phá 5 tổ hợp cũ |
| `TC-LSN-211` | Lesson | 実行する / 実行しない × 全承認 / リクエスト制 — liên quan trực tiếp CONFLICT BUG-R1 |

---

## 6. Spec update needed

| # | Điểm cần chốt | Ai chốt | Vì sao |
|---|---|---|---|
| 1 | **BUG-R1** — tuỳ chọn 「予約時アクションの実行」 = 「実行しない」 **có chặn cả action của friend info** hay không. Bản vá **cố ý KHÔNG** chặn. | **PO** | 4 TC đang chờ (`TC-OUTTRUTH001-01`, `TC-OUTTRUTH001-02` + 2 TC lesson). **Rủi ro gửi tin ngoài ý muốn tới khách** khi admin chủ động chọn "không thực hiện" |
| 2 | **Spec BR-04 vs code** — `admin/friend-information/web/logic-spec.md:325-333` mô tả điều kiện 「一度のみ」 theo **cột action của giá trị friend info**, còn code xét `empty($friendInfoValue)` (đã tồn tại bản ghi giá trị hay chưa). | **PO / BA** | `TC-FUNC004-01`, `TC-FUNC004-02` đang viết kỳ vọng theo **mục tiêu parity của ticket**, không theo câu chữ spec. Chốt theo spec thì phải sửa lại TC **trước khi** kết luận |
| 3 | **Phạm vi 4 chỗ chưa fix** — 2 luồng đặt lịch từ app + sửa form của lượt đặt đã có. Cần quyết định: fix trong ticket này, hay tách ticket riêng? | **PO / Dev lead** | Quyết định này đổi ý nghĩa của `TC-SYNCAPP001-01/02/03` và `TC-REGSHARED001-05` từ "xác nhận known issue" thành "verify fix" |
| 5 | **Info kiểu 「年月日」 (date) có thuộc phạm vi #38443 không?** Ticket nói "không send action của friend info" — date **cũng là** friend info có action, nhưng bản vá chỉ xử lý `type_data == 1` (select). | **PO / Dev lead** | **GAP-05 [BLOCKER]**. Nếu date thuộc phạm vi → bản vá **chưa đủ**, phải bổ sung nhánh gọi `settingEventTimeFriendInfo`. Nếu không → phải tách ticket riêng và **sửa lại kỳ vọng của `TC-TOOLNEGCTRL001-03`** để không suy rộng cho date |
| 6 | **Info date có 「稼働設定」 (一度のみ / 何度でも稼働) không?** Spec `db-mapping.md` §4.4 mục 6 ghi date **CÓ** `action_mode`, nhưng UI thực tế chưa verify được. | **BA** | Nếu có, ma trận date là **2 trục** (月日/年月日 × 一度のみ/何度でも稼働) = 4 tổ hợp, phải nhân đôi `TC-FRIEND001-04/05`. Chưa verify được bằng live MCP DEV |
| 4 | **REQ-006 (job nền)** — có nằm trong phạm vi test của ticket không? Studio note ghi *"xử lý nền chỉ là dependency, không phải phạm vi regression job riêng"* nhưng REQ-006 `risk = High` và **không có TC nào map**. | **Leader** | Nếu job nền không chạy thì 「プレビュー」 **không bao giờ** xuất hiện — triệu chứng chính của ticket phụ thuộc mắt xích này |
