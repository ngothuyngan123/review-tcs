# 05 — Review Report — #38674 [CSV][Cross-analysis] ô giá trị 0 bị bỏ trống trong file CSV

## 0. Nguồn TC

- **Nguồn đã dùng**: MCP LME TEST STUDIO — task #222 (ticket 38674, round 1, branch `ai_fixbug_38674`, `status = done-ai`, `reviewState = leader`, `reviewed = false`)
- **Tổng số TC review**: 13

---

## 1. Coverage — `dev-impact` + `diff code`

**Kết luận: 3/7 vùng ảnh hưởng đủ TC · 2 GAP · 4 RISK.**

| # | Chiều | Vùng ảnh hưởng | TC hiện có | Status | Vấn đề |
|---|---|---|---|---|---|
| **G1** | `diff code` | Nhánh so sánh strict `!==` — giá trị **chuỗi rỗng `""`** | — | **GAP** | `!= null` (cũ) bỏ qua cả `0` **và** `""`; `!== null` (mới) chỉ bỏ qua `null` → **ô `""` nay được tạo thay vì bị bỏ qua**. Đây là hành vi đổi thứ hai của commit, ngoài ô `0`. Chính `note` của TC-OUTEXPORT001-01 thừa nhận ("thư viện tạo ô cho cả chuỗi rỗng chứ không bỏ qua như trước") nhưng **không TC nào verify**. TC-OUTEXPORT001-03 chỉ test `0` / `"0"` / `null` — **thiếu đúng `""`**. Rủi ro: ô bị bỏ qua ở **cuối dòng** trước đây làm dòng ngắn hơn header, nay đủ cột → **đổi cấu trúc file** với hệ thống downstream đọc file |
| **G2** | `dev-impact` | `F3 CrossAnalysisController::downloadCsv` — đường tải file thật | TC-TOOLKNOW002-01 (một phần) | **RISK** | `note` của TC-TOOLKNOW002-01 cho phép fallback *"đọc file trực tiếp tại `FOLDER_MEDIA + media/csv/<admin_id>/<bot_id>/<file_name>`"* khi môi trường không ra được CDN. Đọc file trên đĩa **không phải đường người dùng thật** — vi phạm **RULE-06** (output cuối). Và thực tế cả 13 TC chạy ở `local` → nhánh fallback gần như chắc chắn đã được dùng. `F3` không có TC nào đi qua |
| **G3** | `diff code` | `REG-SHARED` — 3 lớp export CSV khác cùng kiểu lỗi | — | **GAP** | Dev tự khai trong mục 2: *"cùng kiểu lỗi còn ở vài lớp xuất CSV khác (thống kê click landing, lịch sử đơn hàng, đặt chỗ) nhưng ngoài phạm vi ticket, chỉ ghi nhận"*. Không TC nào **đối chứng** rằng commit này KHÔNG đổi hành vi của các export đó. Mức rủi ro thật thấp (diff chỉ 1 file, `ExportCrossAnalysis` 1 caller) nhưng `config/excel.php:45` là cấu hình **global** → cần 1 TC đối chứng để chốt |
| **G4** | `dev-impact` + `diff code` | `BUG` root cause · `F1 ExportCrossAnalysis` · `F2 exportCsv (EP-23)` · `T1 Cross Analysis CSV` | 13 TC | **RISK** | Cả 4 vùng **đều có TC**, nhưng **0 TC nào `pass`** — 12 TC trạng thái `skip`, 1 TC chưa chạy lần nào. Không vùng nào được **chứng minh**. Theo BƯỚC 0.6 #1, mọi kết luận "đủ TC" ở đây phải hạ xuống `RISK` |
| **G5** | `diff code` | Nhãn dòng trục cơ sở kiểu **ngày giờ** (trục 「友だち情報」 field date) | — | **RISK** | `OUT-EXPORT-001` điểm kiểm (2) yêu cầu verify **format datetime** trong file export. TC-TOOLAXIS001-01 chỉ test nhãn **số** và nhãn **rỗng**, không test nhãn ngày giờ — trong khi trục cơ sở 「友だち情報」 nhận được field kiểu ngày |

**Không GAP** (đủ TC, chỉ thiếu lần chạy — đã gộp vào G4): `D1` không có data impact (Dev khẳng định fix không đọc/ghi DB — nhất quán với diff 1 file `app/Exports/`).

**Chiều `diff code` — nguồn**: Studio tab Thông tin, `diffAvailable = true` · `diffStat` = `app/Exports/ExportCrossAnalysis.php | 5 ++++- · 1 file changed, 4 insertions(+), 1 deletion(-)`.

**Anti-patterns (AP-1 → AP-6)**: không AP nào dính. AP-1 không áp dụng (fix không phải generic catch) · AP-2 không dính (Dev đã chứng minh cơ chế bằng repro trong vendor, không phải đoán root cause từ triệu chứng) · AP-3 không dính (có TC-TOOLNEGCTRL001-01 làm đối chứng âm) · AP-6 không dính (mục 3 liệt kê đủ 5 caller).

---

## 2. Thiếu so với quan điểm test LME

**Kết luận: 8 quan điểm Trigger khớp task · 1 đủ · 4 GAP · 3 RISK.**

| # | Mã quan điểm | Ưu tiên | Trigger khớp vì | TC hiện có | Kết luận |
|---|---|---|---|---|---|
| **Q1** | `ENV-003` | **Cao** | Export **sinh file** + trả `file_url` trên **CDN/domain riêng** → chạm cả *media/file* lẫn *URL/domain* của Catalog D | **0 TC** | **GAP — `[BLOCKER]` RULE-08.** Toàn bộ 13 TC chạy `env = local`; `envAuto` cho thấy `staging 0 run · prd 0 run`. Production dùng **server media riêng qua `p.lmes.jp`** + sync B2, và link phải **redirect đúng** — không kết luận được từ local. Quan điểm ghi rõ *"không được đánh × với lý do staging đã pass"*, ở đây thậm chí staging cũng chưa chạy |
| **Q2** | `REG-SHARED-001` | **Cao** | Trigger *"fix bug **có thể tồn tại ở chức năng tương tự**"* — Dev tự khai 3 export CSV khác cùng lỗi | **0 TC** | **GAP — `[MAJOR]`** (hạ từ `[BLOCKER]`: diff chỉ 1 file và Dev đã grep xác nhận `ExportCrossAnalysis` chỉ 1 caller). Quan điểm yêu cầu *"dev cung cấp danh sách nơi ảnh hưởng → test lại từng mục"* — Dev **đã có danh sách** nhưng không TC nào chạm. → TC ở §5 |
| **Q3** | `DATA-TEXT-001` | **Cao** (nâng từ Trung bình vì *"text được export CSV"*) | Tên 「分析項目」 và tên tag là **text tự do** đi thẳng vào dòng tiêu đề + cột đầu của file | 1 phần — TC-OUTEXPORT001-01 (chỉ dấu phẩy + nháy kép) | **GAP — `[MAJOR]`.** Quan điểm yêu cầu test **emoji, ①②③, ㈱, ký tự kiểu cũ**; không TC nào có. Sau khi bật strict comparison, thư viện tạo ô cho cả chuỗi rỗng → càng cần chắc phần text không xô lệch cấu trúc file |
| **Q4** | `OUT-EXPORT-001` | **Cao** (BẮT BUỘC — export CSV) | Chính là chức năng bị fix | 3 TC: 1 Abnormal + 2 Boundary | **RISK — `[MAJOR]`.** (a) **Thiếu `Normal`** ở chính mã này, không ghi lý do → **RULE-01**. (b) Điểm kiểm **(4) encoding UTF-8 / Shift-JIS → mở Excel không mojibake** bị TC-OUTEXPORT001-01 **chủ động loại khỏi phán quyết** (`note`: *"phán quyết pass/fail dựa trên nội dung văn bản đọc được từ file"*) — mojibake chỉ lộ khi mở bằng Excel, nên trục này coi như chưa test |
| **Q5** | `DATA-COUNT-001` | **Cao** | Bảng phân tích chéo **toàn số đếm / tổng hợp** | 1 TC Normal (TC-DATACOUNT001-01) | **RISK — `[MAJOR]` RULE-01.** Thiếu `Abnormal` + `Boundary`, không ghi lý do. Quan điểm còn yêu cầu đối chiếu **cùng số liệu giữa 4 nguồn** (màn tóm tắt / chi tiết / CSV / API) và chú ý **cách xử lý friend đã block** — chưa TC nào đối chiếu chéo màn ↔ CSV ↔ response API, cũng chưa TC nào đụng friend đã block. ⚠️ Các TC Boundary về ô 0 hiện mang mã `TOOL-AXIS-001` (mã lạ, xem §4 #3) nên theo BƯỚC 0.6 **không tính là cover** cho mã này |
| **Q6** | `COMPAT-LEGACY-001` | **Cao** | Nhánh **bản ghi kiểu cũ** (`isOldItems`) = đối tượng đã version-up | 1 TC Boundary (TC-COMPATLEGACY001-01), chưa chạy | **RISK — `[MAJOR]` RULE-09 + RULE-01.** Thiếu `Normal` + `Abnormal`. Nghiêm trọng hơn: chính `note` của TC ghi nhận nhánh legacy **không kích hoạt được từ nút CSV trên màn hình** (FE gửi `isOldItem`, server đọc `isOldItems`) → nhánh cũ hiện **chỉ test được ở tầng API**, không có bằng chứng nào cho đường người dùng thật. Xem §6 |
| **Q7** | `PERF-LARGE-001` | **Cao** (nâng — *export / phân tích*) | Strict comparison làm thư viện **tạo thêm ô trong bộ nhớ** cho mọi giá trị trước đây bị bỏ qua | 1 TC (TC-OUTEXPORT001-02, 10 分析項目 / 42 cột) | **RISK — `[MINOR]`.** Nội dung **đã đủ** nhưng TC mang mã `OUT-EXPORT-001`, không mang mã này → không map coverage tự động. Ngoài ra TC chỉ boundary theo **số cột** (10 分析項目), không boundary theo **số dòng** (trục 「友だち情報」 free text có thể sinh hàng nghìn dòng) |
| **Q8** | `FUNC-001` | Cao | Mọi chức năng | TC-TOOLKNOW002-01 + TC-DATACOUNT001-01 | **Đủ** — không ghi issue |

> **Không flag** `PERM-002` (export = thao tác nhạy cảm) dù Trigger khớp: diff không chạm tầng auth/controller, fix nằm hoàn toàn ở tầng ghi ô của lớp export. Theo nguyên tắc không phủ layer downstream không bị chạm code.

---

## 3. TC trùng lặp nội dung

Đã rà **13 TC**, phát hiện **2 nhóm chồng lấn**, **không nhóm nào đề nghị xóa**.

| Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| N1 | **TC-TOOLAXIS001-03** (Studio #14064) — dòng toàn 0, verify đủ **5 cột số** | TC-TOOLKNOW002-01 (Studio #14057) — **GỘP, không xóa** | `DUP-SUBSET` | ① quan điểm: cùng trục "ô 0 phải ra 0" · ② loại case: Normal ⊂ Boundary · ③ đối tượng + thao tác: cùng bấm nút 「CSV」 trên màn kết quả 分析リスト rồi đọc file · ④ tiền đề tương đương: trục cơ sở là tag chưa gắn cho ai + 「表示する友だち」 tick đủ 3 loại. Expected của #14057 (2 cột 対象人数 + 合計 ra 0) là **tập con** của #14064 (cả 5 cột số ra 0) | `[MINOR]` |
| N2 | **TC-DATACOUNT001-01** (Studio #14058) — bảng hỗn hợp 0 và dương, đối chiếu **từng ô** màn ↔ file | TC-TOOLNEGCTRL001-01 (Studio #14059) — **GỘP, không xóa** | `DUP-SUBSET` | ① cùng trục "giá trị dương không bị đổi" (cùng map REQ-003) · ② cùng Normal · ③ cùng thao tác export rồi đối chiếu từng ô · ④ tiền đề của #14059 (bảng toàn dương) là **lát cắt** của tiền đề #14058 (bảng có cả dương lẫn 0) | `[MINOR]` |

**Gate đã chạy** — giả định xóa rồi chạy lại BƯỚC 2 + 3:
- Xóa **TC-TOOLKNOW002-01** → mất TC **tái hiện đúng step-to-reproduce của ticket** (yêu cầu bắt buộc của quy trình fix-bug) → **đổi sang đề nghị gộp**: nếu Leader muốn giảm số TC thì giữ #14064 và bổ sung vào đó ghi chú "đồng thời là TC tái hiện bug #38674", không xóa trắng.
- Xóa **TC-TOOLNEGCTRL001-01** → mất **đối chứng âm thuần** (bảng không có ô 0 nào, dùng để chứng minh commit chỉ tác động ô 0). Giá trị phương pháp luận riêng → **đổi sang đề nghị gộp**, và chỉ gộp nếu Leader chấp nhận mất lát cắt đối chứng.
- **Không nhóm nào là `DUP-INFLATE`** — 2 nhóm trên không làm quan điểm nào trông như đủ 3 loại case, nên **không mở lại** coverage ở §1/§2 vì lý do trùng.
- **Không có `DUP-CONFLICT`** — không cặp TC nào cùng tiền đề + cùng steps mà `Kết quả mong đợi` mâu thuẫn.

⚠️ **Không tự xóa** — TC Studio là read-only. Leader quyết định → sửa/xóa trên Studio (`testcase_update` / `testcase_delete`) rồi fetch lại.

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | Toàn bộ 13 TC | **0% pass.** `pass = 0 · fail = 0 · error = 0`; 12 TC `last_exec.status = skip` (pipeline, runId 586, 2026-08-26 10:18), 1 TC `last_exec = null`. Bản fix `18bee01948` **chưa được chứng minh bằng bất kỳ lần chạy nào**. Theo BƯỚC 0.6 #1 (`pass < 50%`) | Chạy thật bộ TC trước khi review có giá trị kết luận. Với TC `skip` phải ghi **lý do skip** vào kết quả, không để trống |
| 2 | `[MAJOR]` | Toàn bộ 13 TC | **Không TC nào có Evidence** — `Evidence thực tế` rỗng 13/13. RULE-02 yêu cầu evidence đúng loại; `OUT-EXPORT-001` chỉ định rõ: **file export thật (đính kèm) + screenshot màn hình tương ứng** | Bắt buộc đính kèm cặp *file CSV thật + screenshot bảng trên web* cho mỗi TC trước khi tick Đạt |
| 3 | `[MAJOR]` | 8/13 TC | **Mã quan điểm không map được framework.** 7 TC mang mã không có trong `framework/checklist-lme.md` — `TOOL-KNOW-002` (1), `TOOL-NEGCTRL-001` (1), `TOOL-OLDREC-001` (1), `TOOL-AXIS-001` (3), `API-CONTRACT-001` (1) — cộng **TC-NOVP-01 (Studio #14070) không có mã quan điểm nào** (`viewpoint = null`). Hệ quả: quan điểm framework mà các TC này cover (`DATA-COUNT-001`, `PERF-LARGE-001`, `OUT-EXPORT-001`) **không tính là cover** theo BƯỚC 0.6 #5 → đẩy Q4/Q5/Q7 lên RISK | Hoặc map lại mã Studio → mã framework, hoặc bổ sung nhóm `TOOL-*` vào `framework/checklist-lme.md` để 2 hệ thống nói cùng ngôn ngữ. Riêng TC-NOVP-01 **bắt buộc** gán mã (gợi ý `REG-SHARED-001`) |
| 4 | `[MAJOR]` | Toàn bộ | **Không có spec cho tính năng.** `spec-features/admin/index.md:34` ghi FA-024 「クロス分析」 ở trạng thái **CHƯA** (chưa reverse-engineer) → không có `feature-spec.md`. Không có chuẩn độc lập để đánh giá `Kết quả mong đợi`; mọi Expected hiện dựa 100% vào mô tả của chính Dev đã fix | Ghi nhận rủi ro tự suy diễn. Nhiều `note` TC dẫn *"feature-spec §3.4 / §6 / §7"* nhưng file đó **không tồn tại trong repo** — cần Leader xác minh nguồn các trích dẫn này trước khi tin |
| 5 | `[MAJOR]` | `03-dev-impact.md` | Auto-fill `2026-09-14 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" CHƯA tick** → `F*`/`D*`/`T*` có thể thiếu hoặc map sai | Tester đọc lại Redmine #38674 rồi tick checkbox trước khi review được coi là có giá trị |
| 6 | `[MAJOR]` | Bản fix | **Dev verify mức `lint`** — chỉ `php -l` + tái hiện cơ chế bằng PhpSpreadsheet trong vendor. **Không có lần chạy end-to-end nào** từ phía Dev, và bộ TC cũng 0% pass → không tầng nào đã verify hành vi thật | Yêu cầu ít nhất 1 lần chạy end-to-end có evidence trước khi chuyển trạng thái ticket |
| 7 | `[NIT]` | Ngoài phạm vi ticket | Dev ghi nhận **3 lớp export CSV khác cùng kiểu lỗi** (thống kê click landing, lịch sử đơn hàng, đặt chỗ) nhưng **chưa mở ticket** nào | Mở ticket riêng để bug không rơi vào quên lãng (không thuộc phạm vi #38674) |

### Chất lượng từng TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 8 | `[MAJOR]` | TC-TOOLKNOW002-01 (Studio #14057) | **RULE-06 — Expected dừng trước output cuối.** `note` cho phép fallback *"đọc file trực tiếp tại `FOLDER_MEDIA + media/csv/<admin_id>/<bot_id>/<file_name>`"* thay cho việc tải qua `file_url`. Đọc file trên đĩa bỏ qua toàn bộ đường CDN/domain mà khách thật đi qua. Kết hợp với việc 13/13 TC chạy `local`, nhánh fallback gần như chắc chắn đã được dùng | Bỏ fallback khỏi tiêu chí pass, hoặc tách riêng thành TC phụ ghi rõ *"không thay thế TC tải qua đường thật"*. Đường thật phải được verify trên `product` (→ TC-OUTEXPORT001-05 ở §5) |
| 9 | `[MINOR]` | TC-NOVP-01 (Studio #14070) | **TC kiểm tra tĩnh lặp lại chính diff** — steps là "mở file, kiểm tra khai báo `implements`, tìm nơi khởi tạo". Nội dung này đã được đọc thẳng từ `spec_delta` / commit; giá trị phát hiện bug gần bằng 0 vì nó không thể fail khi diff đã được merge | Giữ như implementation guard thì đổi thành **assert trong CI** thay vì TC thủ công; hoặc mở rộng thành TC đối chứng `REG-SHARED-001` (kiểm các lớp export khác **không** khai báo interface này) — hữu ích hơn nhiều |
| 10 | `[MINOR]` | TC-OUTEXPORT001-03 (Studio #17707) | `requirement_keys` **rỗng** — TC duy nhất do người viết lại không map REQ nào, trong khi nó khóa trực tiếp root cause. Không xuất hiện trong thống kê coverage theo requirement | Gán `REQ-001`, `REQ-002`, `REQ-007` |
| 11 | `[MINOR]` | TC-TOOLAXIS001-01 (Studio #14062) | **TC dựa trên giả định chưa xác minh** — `note` tự nhận *"chưa xác minh được engine trả nhãn dạng số hay dạng chuỗi"*, và điều kiện tiền đề ("bảng xuất hiện dòng có nhãn là số 0 hoặc nhãn để trống") có thể **không dựng được** → TC nhiều khả năng luôn kết thúc ở `skip` | Xác minh kiểu dữ liệu nhãn dòng với Dev trước, rồi viết lại tiền đề bằng dữ liệu dựng chắc chắn được |
| 12 | `[MINOR]` | Toàn bộ 13 TC | `Trạng thái đánh giá spec` **rỗng 13/13** (`spec_status = null`). Kết hợp với #4 (không có spec) → không TC nào ghi nhận Expected của mình đến từ đâu và đã hỏi ai | Điền `Spec không ghi` + tên người đã xác nhận cho các Expected suy từ mô tả của Dev |

**Anti-pattern**: không AP nào dính (đã rà AP-1 → AP-6, chi tiết ở cuối §1).

---

## 5. TCs đề xuất bổ sung (9)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs` **chưa có FA-024** 「クロス分析」 — không đối chiếu được (kho hiện có 18 tính năng, gần nhất là FA-013 友だちリスト / FA-015 友だち情報管理, không chạm luồng export của phân tích chéo) |
| Vùng regression phát hiện từ kho | Không có — kho không chứa TC nào của FA-024 |
| Conflict expected vs kho | Không |
| GAP dùng lại TC kho (không viết mới) | Không |
| Xác nhận chống trùng | Đã đối chiếu **13 TC** ở BƯỚC 0 + kho-tcs — **không TC đề xuất nào trùng** |
| Đã push lên test tool | **4/9 TC** đã được Leader chọn và push lên **MCP LME TEST STUDIO task #222** ngày 2026-09-15: `TC-OUTEXPORT001-06` → Studio #17780 · `TC-DATATEXT001-02` → #17781 · `TC-DATACOUNT001-02` → #17782 · `TC-DATACOUNT001-03` → #17783. `client_ref` = `ID` nên push lại không tạo trùng. **5 TC còn lại chưa push**: `TC-OUTEXPORT001-04` (G1) · `TC-OUTEXPORT001-05` (G2) · `TC-ENV003-01` (Q1) · `TC-REGSHARED001-01` (G3/Q2) — đây là 4 TC lấp **GAP nặng nhất** (ô chuỗi rỗng, đưồng tải thật, RULE-08 production, đối chứng REG-SHARED) (đã soi theo 4 yếu tố: mã quan điểm × loại case × đối tượng+thao tác × tiền đề) |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-OUTEXPORT001-04 | Data | OUT-EXPORT-001 | クロス分析 — xuất CSV (tầng ghi ô) | Boundary | auto | Tất cả | Ô chuỗi rỗng vẫn giữ đúng vị trí cột sau khi bật so sánh nghiêm ngặt | Đã checkout nhánh chứa fix #38674. Chạy được lớp `ExportCrossAnalysis` ở local/dev không cần dữ liệu production | 1. Dựng một hàng dữ liệu đi qua đúng lớp `ExportCrossAnalysis` với 4 ô liên tiếp lần lượt là: chuỗi rỗng `""`, số nguyên `0`, `null`, rồi một chuỗi có nội dung<br>2. Thêm 1 hàng thứ hai có đủ giá trị ở mọi cột để làm mốc so cột<br>3. Sinh file CSV bằng chính lớp export sau fix<br>4. Mở file ở dạng text, đếm số ô của từng dòng và so với dòng tiêu đề<br>5. Lặp lại với ô chuỗi rỗng đặt ở **cuối dòng** (cột cuối cùng) | Hàng 1: `""` · `0` · `null` · `"abc"`. Hàng 2: `"x"` · `5` · `"y"` · `"z"`. Biến thể: hàng 3 có `""` ở đúng cột cuối | Ô chuỗi rỗng được ghi thành ô trống nhưng **vẫn chiếm đúng một vị trí cột** — các cột phía sau không bị dồn lệch. Số ô của mọi dòng bằng số cột của dòng tiêu đề, kể cả dòng có `""` ở cột cuối. Ô `0` ghi ra `0`, ô `null` để trống. Đối chiếu với bản trước fix: nếu dòng trước fix bị **ngắn hơn** header do ô bị bỏ qua thì ghi rõ đây là **thay đổi cấu trúc file**, cần báo cho bên đọc file downstream | | Lấp `G1` · Đánh giá spec: Spec không ghi — hành vi suy từ cơ chế `!==` của PhpSpreadsheet, cần Dev xác nhận · Evidence: file CSV thật trước/sau fix + số cột từng dòng |
| TC-OUTEXPORT001-05 | UI | OUT-EXPORT-001 | クロス分析 — tải file CSV qua đường thật | Normal | manual | product | Tải file CSV qua đúng đường người dùng thật trên production, không đọc file trên đĩa | Đã đăng nhập admin trên **production**, chọn đúng bot. Có 分析リスト đã tính xong, bảng có ít nhất 1 ô 「対象人数」 = 0 và 1 ô 「合計」 = 0 | 1. Mở màn kết quả của 分析リスト, chụp màn hình toàn bảng<br>2. Bấm nút 「CSV」 và **để trình duyệt tự tải** file theo đường dẫn máy chủ trả về — không copy đường dẫn, không mở thư mục trên máy chủ<br>3. Ghi lại đường dẫn file thực tế mà trình duyệt đã tải (domain nào)<br>4. Mở file vừa tải và đối chiếu các ô đang là 0 trên màn hình<br>5. Thử mở lại đúng đường dẫn đó ở tab mới để xác nhận file còn tải được | 分析リスト bất kỳ trên production có ô giá trị 0 | File tải về thành công qua đúng đường dẫn máy chủ trả về, **không cần thao tác thủ công nào trên máy chủ**. Đường dẫn tải nằm trên domain media của production và mở lại được ở tab mới. Các ô 0 trên màn hình xuất hiện là `0` trong file, không ô nào trống | | Lấp `G2` + `Q4` (bổ sung loại case Normal còn thiếu của `OUT-EXPORT-001`) · RULE-06 + RULE-08 · Đánh giá spec: Spec không ghi · Evidence: file CSV tải về + screenshot bảng + ảnh chụp đường dẫn tải |
| TC-ENV003-01 | UI | ENV-003 | クロス分析 — xuất CSV trên production | Normal | manual | product | Bộ lõi "ô 0 ra 0" chạy lại trên production, đối chiếu với kết quả local | Đã chạy xong TC-TOOLKNOW002-01 và TC-TOOLAXIS001-03 ở môi trường thấp hơn và có kết quả. Đã đăng nhập admin trên **production** với account thật, chọn đúng bot | 1. Dựng trên production một 分析リスト có trục cơ sở gồm 1 tag chưa gắn cho bạn bè nào<br>2. Chờ bảng tính xong, chụp màn hình dòng có toàn bộ ô bằng 0<br>3. Bấm 「CSV」 và tải file về<br>4. Mở file, đọc toàn bộ ô của dòng đó<br>5. So kết quả này với kết quả đã ghi nhận khi chạy cùng kịch bản ở môi trường thấp hơn | 分析リスト trên production · trục cơ sở: 1 tag chưa gắn cho ai · 「表示する友だち」 tick đủ 3 loại · 1 分析項目 | Dòng tương ứng trong file có đủ giá trị `0` ở tất cả cột số 「対象人数」「…　合計」「…　有効」「…　ブロック」「…　被ブロック」. Kết quả **trùng khớp** với lần chạy ở môi trường thấp hơn. Nếu khác → ghi rõ khác ở đâu và dừng, không kết luận đã fix | | Lấp `Q1` · RULE-08 — media/file + domain, không kết luận từ local/staging · Đánh giá spec: Spec không ghi · Evidence: bảng đối chiếu môi trường + file CSV lấy trên production |
| TC-REGSHARED001-01 | UI | REG-SHARED-001 | Các màn xuất CSV khác — đối chứng phạm vi fix | Normal | manual | Tất cả | Ba lớp export CSV khác giữ nguyên hành vi cũ, không bị commit này chạm tới | Đã deploy nhánh chứa fix #38674. Có dữ liệu để export ở 3 màn Dev đã liệt kê: thống kê click landing, lịch sử đơn hàng, đặt chỗ — mỗi màn có ít nhất 1 ô đang hiển thị giá trị 0 trên web | 1. Vào màn thống kê click landing, chụp màn hình ô đang hiển thị 0, bấm export CSV và tải file<br>2. Mở file, ghi lại ô tương ứng là trống hay là 0<br>3. Lặp lại bước 1-2 với màn lịch sử đơn hàng<br>4. Lặp lại bước 1-2 với màn đặt chỗ<br>5. Đối chiếu 3 kết quả với hành vi trước khi deploy fix | 1 bản ghi có giá trị 0 ở mỗi màn trong 3 màn trên | Hành vi của cả 3 file export **không đổi so với trước khi deploy** — ô nào trước đây trống thì vẫn trống, ô nào có giá trị thì giữ nguyên. Xác nhận commit chỉ tác động đúng file CSV của 分析リスト. ⚠️ Nếu 3 màn này vẫn để trống ô 0 → đó là bug **có sẵn, ngoài phạm vi ticket**, ghi nhận để mở ticket riêng chứ **không** đánh Không đạt cho #38674 | | Lấp `G3` + `Q2` · regression · Đánh giá spec: Spec không ghi — danh sách nơi ảnh hưởng do Dev cung cấp trong mục 2 file 03 · Evidence: 3 file CSV + screenshot màn tương ứng |
| TC-OUTEXPORT001-06 | UI | OUT-EXPORT-001 | クロス分析 — mã hoá file CSV | Abnormal | manual | Tất cả | Mở file CSV bằng Excel: tiếng Nhật không lỗi phông và ô 0 vẫn hiện 0 | Đã đăng nhập admin, chọn đúng bot. Có 分析リスト mà tên tag trục cơ sở và tên 分析項目 đều là tiếng Nhật, bảng đã tính xong và có ít nhất 1 ô = 0 | 1. Mở màn kết quả của 分析リスト, chụp màn hình bảng<br>2. Bấm 「CSV」 và tải file về<br>3. Mở file **bằng Excel** (nhấp đúp, không dùng chức năng nhập dữ liệu có chọn mã hoá)<br>4. Đọc dòng tiêu đề và cột đầu tiên: chữ tiếng Nhật có hiển thị đúng không<br>5. Đọc các ô đang là 0 trên màn hình xem Excel hiển thị 0 hay ô trống | 分析リスト có tên tag và tên 分析項目 bằng tiếng Nhật, trong đó có ký tự kanji và katakana | Excel hiển thị toàn bộ chữ tiếng Nhật đúng, **không mojibake**. Các ô đang là 0 trên web hiển thị `0` trong Excel, không bị Excel hiểu thành ô trống. Nếu mojibake → ghi rõ mã hoá thực tế của file và đối chiếu với mã hoá trước fix để xác định có phải commit này gây ra không | | Lấp `Q4` (điểm kiểm (4) của `OUT-EXPORT-001` bị TC-OUTEXPORT001-01 loại khỏi phán quyết) · Đánh giá spec: Spec không ghi · Evidence: screenshot cửa sổ Excel + file CSV gốc · **Đã push Studio #17780** |
| TC-DATATEXT001-01 | UI | DATA-TEXT-001 | クロス分析 — text tự do trong tiêu đề file CSV | Abnormal | manual | Tất cả | Emoji và ký tự đặc biệt trong tên 分析項目 không làm hỏng file CSV | Đã đăng nhập admin, chọn đúng bot. Đã tạo 分析リスト với tên các 「分析項目」 chứa emoji và ký tự đặc biệt, bảng đã tính xong và có ít nhất 1 ô = 0 | 1. Mở màn chỉnh sửa 分析リスト, đặt tên 3 「分析項目」 theo phần dữ liệu nhập, lưu lại<br>2. Vào màn kết quả, chờ bảng tính xong, chụp màn hình dòng tiêu đề<br>3. Bấm 「CSV」 và tải file về<br>4. Mở file ở dạng text, đối chiếu từng tên cột với tên đang hiển thị trên màn hình<br>5. Đếm số ô của từng dòng dữ liệu và so với dòng tiêu đề | 分析項目 1: `テスト😀絵文字` · 分析項目 2: `①②③丸数字` · 分析項目 3: `㈱旧字体テスト` · tên tag trục cơ sở chứa emoji | Tên cột trong file khớp **từng ký tự** với tên trên màn hình — emoji, ①②③ và ㈱ không bị mất, không bị thay bằng dấu hỏi. Số ô của mọi dòng bằng số cột của dòng tiêu đề. Các ô 0 vẫn được ghi là 0 | | Lấp `Q3` · Đánh giá spec: Spec không ghi — không có `feature-spec` cho FA-024 · Evidence: file CSV thật + screenshot dòng tiêu đề trên web |
| TC-DATATEXT001-02 | UI | DATA-TEXT-001 | クロス分析 — text tự do trong tiêu đề file CSV | Boundary | manual | Tất cả | Tên 分析項目 dài sát giới hạn và chứa xuống dòng: file CSV không lệch cột | Đã đăng nhập admin, chọn đúng bot. Đã xác nhận với Dev/màn hình giới hạn độ dài của ô nhập tên 「分析項目」. Có 分析リスト với bảng đã tính xong và ít nhất 1 ô = 0 | 1. Đặt tên 分析項目 thứ nhất dài **đúng bằng** giới hạn tối đa mà ô nhập cho phép<br>2. Thử nhập ký tự xuống dòng vào tên 分析項目 thứ hai; nếu ô nhập chặn thì ghi lại là chặn và bỏ qua nhánh này<br>3. Lưu, vào màn kết quả, chờ bảng tính xong<br>4. Bấm 「CSV」 và tải file về<br>5. Mở file ở dạng text, đếm số dòng của file và số ô của từng dòng | Tên 分析項目 1: chuỗi dài đúng giới hạn tối đa · Tên 分析項目 2: chuỗi có ký tự xuống dòng (nếu nhập được) | Tên dài được ghi đầy đủ vào dòng tiêu đề, không bị cắt cụt. Nếu tên chứa xuống dòng thì ô đó phải được bọc trong dấu nháy kép sao cho **tổng số dòng của file không tăng** và số ô mỗi dòng vẫn bằng số cột tiêu đề. Các ô 0 vẫn ghi là 0 | | Lấp `Q3` · Đánh giá spec: Spec không ghi — giới hạn độ dài tên 分析項目 chưa có tài liệu, cần hỏi Dev trước khi chạy · Evidence: file CSV thật + screenshot ô nhập tên · **Đã push Studio #17781** |
| TC-DATACOUNT001-02 | UI | DATA-COUNT-001 | クロス分析 — đối chiếu số liệu màn hình ↔ file CSV | Abnormal | manual | Tất cả | Bạn bè đã block được đếm nhất quán giữa màn hình và file CSV | Đã đăng nhập admin, chọn đúng bot. Có ít nhất 1 bạn bè ở trạng thái 「ブロック」 và 1 bạn bè 「被ブロック」 đang mang tag dùng làm trục cơ sở. Có 分析リスト đã tính xong với 「表示する友だち」 tick đủ 3 loại | 1. Mở màn kết quả 分析リスト, ghi lại giá trị 「対象人数」 và các cột 「有効」「ブロック」「被ブロック」 của dòng chứa tag đó<br>2. Tự cộng tay 3 cột trạng thái và so với cột 「合計」 đang hiển thị<br>3. Bấm 「CSV」 và tải file về<br>4. Đối chiếu đúng các ô đó trong file với số trên màn hình<br>5. Bỏ tick 「ブロック」 trong 「表示する友だち」, lưu, chờ tính lại rồi lặp bước 1-4 | 1 tag gắn cho: 1 bạn bè 有効, 1 bạn bè ブロック, 1 bạn bè 被ブロック | Cột 「合計」 bằng đúng tổng 3 cột trạng thái đang hiển thị (kiểm bằng phép cộng tay). Giá trị trong file khớp **từng ô** với màn hình ở cả 2 lần chạy. Khi bỏ tick 「ブロック」, cả màn hình lẫn file đều bỏ cột đó và 「合計」 được tính lại theo đúng các loại còn được chọn — không cột nào giữ số cũ | | Lấp `Q5` · Đánh giá spec: Spec không ghi — cách tính 合計 khi bỏ bớt loại bạn bè cần Dev xác nhận · Evidence: bảng đối chiếu màn hình ↔ file + phép cộng tay · **Đã push Studio #17782** |
| TC-DATACOUNT001-03 | UI | DATA-COUNT-001 | クロス分析 — biên dữ liệu rỗng | Boundary | manual | Tất cả | Bảng không có dòng dữ liệu nào: file CSV vẫn có tiêu đề và không lỗi | Đã đăng nhập admin, chọn đúng bot. Có 分析リスト mà trục cơ sở chọn tiêu chí không sinh ra dòng nào (ví dụ trục 「友だち情報」 của một field chưa bạn bè nào nhập giá trị). Bảng đã tính xong | 1. Mở màn kết quả của 分析リスト đó, xác nhận bảng chỉ có dòng tiêu đề, không có dòng dữ liệu<br>2. Chụp màn hình trạng thái bảng rỗng<br>3. Bấm 「CSV」 và chờ file được sinh<br>4. Mở file và kiểm tra nội dung<br>5. Ghi lại có thông báo lỗi nào hiện trên màn hình không | 分析リスト với trục cơ sở không sinh dòng nào · 1 分析項目 bất kỳ | Quá trình xuất hoàn tất, không báo lỗi trên màn hình và không trả lỗi máy chủ. File được sinh và tải về được, chứa **đúng dòng tiêu đề** với đủ số cột như khi có dữ liệu, không có dòng dữ liệu nào. File không rỗng hoàn toàn và không bị hỏng khi mở | | Lấp `Q5` · Đánh giá spec: Spec không ghi · Evidence: file CSV thật + screenshot bảng rỗng trên web · **Đã push Studio #17783** |

**Dòng `GAP`/`RISK` không đề xuất TC mới:**

- **`G4`** (mọi vùng đều có TC nhưng 0% pass) — không phải thiếu TC mà là **thiếu lần chạy**. Việc cần làm là chạy thật bộ 13 TC hiện có kèm evidence, không viết thêm TC.
- **`G5`** (nhãn dòng kiểu ngày giờ) — phụ thuộc câu hỏi chưa có lời đáp ở `[MINOR] #11`: engine trả nhãn dòng dạng số hay chuỗi. Xác minh với Dev trước, rồi mở rộng **TC-TOOLAXIS001-01 sẵn có** thêm một nhánh nhãn ngày giờ, thay vì đẻ TC mới trùng tiền đề.
- **`Q6`** (`COMPAT-LEGACY-001` thiếu Normal + Abnormal) — nhánh legacy **hiện không kích hoạt được từ giao diện** do lệch tên tham số `isOldItem` / `isOldItems`. Viết TC UI cho nhánh này lúc này là viết TC không chạy được. Chờ Leader chốt ở §6 rồi mới bổ sung.
- **`Q7`** (`PERF-LARGE-001`) — nội dung đã được TC-OUTEXPORT001-02 cover đủ; chỉ cần **đổi/thêm mã quan điểm** trên Studio, không cần TC mới.

---

## 6. Spec update needed

| # | Vấn đề | Cần chốt |
|---|---|---|
| 1 | **Lệch tên tham số nhánh bản ghi kiểu cũ** — FE gửi `isOldItem` (`public/js/cross_analysis/result.js:302`), server đọc `isOldItems` (`CrossAnalysisController.php:2185`) → nhánh legacy **không bao giờ bật được** từ nút CSV trên màn hình. Khiếm khuyết **có trước**, không thuộc diff #38674; AI đã ghi vào phần conflicts của plan nhưng **chưa có ticket** | Leader/Dev chốt: (a) nhánh legacy còn cần dùng không — nếu không thì **xoá code chết**; (b) nếu còn cần thì mở **ticket riêng** sửa tên tham số, sau đó mới bổ sung TC UI cho `COMPAT-LEGACY-001` (Q6) |
| 2 | **Không có spec cho FA-024** 「クロス分析」 — `spec-features/admin/index.md:34` đánh dấu **CHƯA**. Nhiều `note` của TC Studio lại dẫn *"feature-spec §2.2 / §3.4 / §6 / §7 (retention 8 ngày)"* và *"BR-03 (tối đa 10 分析項目)"* — **các trích dẫn này không trỏ tới file nào tồn tại trong repo** | Xác minh nguồn thật của các trích dẫn đó (Studio có spec riêng? hay AI tự suy từ source?). Trước khi có câu trả lời, mọi `Kết quả mong đợi` suy từ chúng chỉ là giả định — không dùng làm căn cứ đánh Đạt |
| 3 | **Hành vi ô chuỗi rỗng sau fix chưa được định nghĩa** (`G1`) — commit đổi đồng thời 2 thứ: ô `0` được ghi **và** ô `""` được tạo. Tài liệu fix của Dev chỉ nói về ô `0`, hoàn toàn không nhắc ô `""` | Dev xác nhận: việc ô `""` nay chiếm vị trí cột (thay vì bị bỏ qua) là **có chủ đích** hay tác dụng phụ; nếu file có bên thứ ba đọc tự động thì cần thông báo thay đổi cấu trúc |
| 4 | **Quy ước mã quan điểm giữa Studio và framework** (`[MAJOR] #3`) — 7/13 TC mang mã `TOOL-*` / `API-CONTRACT-*` không tồn tại trong `framework/checklist-lme.md`, 1 TC không mã | Chốt một trong hai: bổ sung nhóm mã Studio vào `framework/checklist-lme.md`, hoặc bắt buộc Studio map sang mã framework khi sinh TC. Hiện tại coverage không map tự động được |

> **Không phải mâu thuẫn spec**: nhãn 「ブロック」/「被ブロック」 bị hoán đổi so với tên field nội bộ — web và CSV đọc **giống nhau** nên không ảnh hưởng ticket này (đã ghi trong `note` TC-DATACOUNT001-01). Không đưa vào bảng trên.
