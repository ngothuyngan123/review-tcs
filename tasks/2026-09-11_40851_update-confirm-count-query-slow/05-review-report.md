# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | `(1) MCP LME TEST STUDIO — task #294 (round 1, branch ai_small_40851, status tc-ready)` |
| Tổng số TC review | `21` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `6/14 vùng ảnh hưởng đủ TC · 5 GAP · 3 RISK`

> Chiều (b) đọc từ Studio `task_get_context(294, ["dev_impact","spec_delta"])` — `diffAvailable = true`, diff 1 file `app/Http/Controllers/Api/ChatController.php` (+55 / −19).

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | `F4 — 3 hàm sibling cùng tính năng đánh dấu đã đọc: makeReadMessageAll (Api/ChatController), makeReadMessageAll (Mobile/ChatMobileController), makeReadMessageByIds (Api/ChatController)` | `dev-impact` + `diff code` | `không có` | `GAP — 0 TC cover.` Dev tự khai out-of-scope nhưng chính `dev_impact` của Studio yêu cầu **"smoke-check tránh hồi quy"**, và `REQ-010` (Studio) đòi "2 chức năng đánh dấu đã đọc còn lại không bị hồi quy, **giữ cả phần đồng bộ tìm kiếm**". Studio coverage xác nhận ref `EP-03` (họ endpoint mark-read-all) = `level: none`, `tcIds: []` | `[BLOCKER]` |
| G2 | `D4 — bots.count_user_unconfirm khi bot CÒN hội thoại bị chặn chưa đọc` | `diff code` | `TC-TOOLNEGCTRL001-02` (17340) verify hội thoại bị chặn giữ nguyên, **nhưng không verify counter**; `TC-FUNC001-03` / `TC-FUNC004-01` chỉ verify counter `→ 0` khi **không có** hội thoại bị chặn | `GAP — 0 TC.` Endpoint reset `count_user_unconfirm = 0` nhưng **cố ý bỏ qua hội thoại bị chặn** (vẫn còn `unconfirm_message`). Badge có thể về 0 trong khi vẫn còn hội thoại chưa đọc. Kho TCs ghi nhận **bug lịch sử #35144**: "không count lại mà update `count_user_unconfirm = 0` luôn ⇒ sai" | `[BLOCKER]` |
| G3 | `D2 — conversation.updated_at, chiều NGƯỢC LẠI: hội thoại CHƯA đọc phải ĐƯỢC cập nhật` | `diff code` | `TC-DATADB001-02` (17342) chỉ verify nửa "hội thoại đã đọc **không** bị bump" | `RISK — chỉ test 1 phía của hành vi đổi.` Câu hỏi adversarial #4: fix siết điều kiện lọc → phải có TC verify chỗ cũ **không hỏng**. Kho `TC-CHT-40` (bộ TC của #39257 — chính khuôn mẫu fix này tái dùng) yêu cầu đủ 2 chiều | `[MAJOR]` |
| G4 | `T2 — FA-002 badge / số hội thoại chưa đọc sau khi app mark-read-all` | `dev-impact` | `TC-FUNC001-01/02`, `TC-SYNCAPP001-02/03` (17373-17376) | `RISK — 4/4 TC đều "Chưa test"` (`status=draft`, `exec_mode=manual`). Có TC ≠ đã test | `[MAJOR]` |
| G5 | `T3 — thứ tự / sort danh sách hội thoại theo updated_at sau fix` | `diff code` | `không có` | `GAP — 0 TC.` `updated_at` không còn bị đẩy mới ở hội thoại vốn đã đọc (REQ-005, chủ đích) → nếu danh sách hội thoại web/app sort theo cột này thì thứ tự hiển thị đổi so với trước | `[MAJOR]` |
| G6 | `REQ-008 — hợp đồng response + request bất thường: chưa đăng nhập → 401; bot_id không tồn tại / trống / sai kiểu` | `dev-impact` | `TC-CONC001-02` (17350) có gắn nhãn `REQ-008` nhưng nội dung là **2 request đồng thời**, không chạm auth/tham số | `GAP — 0 TC thật cover.` Nhãn `requirement_keys` làm requirement này **trông như đã cover** | `[MAJOR]` |
| G7 | `BUG root cause — query 95.533359s trên dataset production (Rows_examined 50.135.150)` | `dev-impact` + `diff code` | `TC-TOOLKNOW002-01` (17348), `TC-TOOLKNOW002-02` (17372), `TC-FUNC004-02/03` | `RISK — oracle bị hạ xuống "HÌNH DẠNG câu lệnh".` Chính actual của TC ghi: *"env test KHÔNG tái hiện được 95.533359s của production (conversation prod ~50M dòng, DB test nhỏ hơn nhiều bậc) … KHÔNG kết luận endpoint nhanh hơn X giây"*. `TC-TOOLKNOW002-02` chạy EXPLAIN trên bảng **232.287 dòng** — không đại diện cho 50 triệu dòng, và chính Dev đề nghị *"xác nhận chỉ mục trên môi trường thật"* | `[MAJOR]` |
| G8 | `T1+T2 — ma trận 5 màn có thao tác confirm/unconfirm cùng ghi vào bộ data này` | `dev-impact` + `diff code` | xem bảng bên dưới | `GAP — 0/5 màn được test THAO TÁC confirm/unconfirm; 2/5 màn chỉ được QUAN SÁT kết quả sau khi API mobile chạy` | `[BLOCKER]` |

**G8 — chi tiết ma trận màn hình** (đối chiếu cột `screen` của 21 TC):

| # | Màn hình | TC hiện có | Đánh giá |
|---|---|---|---|
| 1 | **List chat 1:1 — app mobile** | `TC-FUNC001-01/02`, `TC-SYNCAPP001-02/03` (17373-17376, screen `App Mobile — Chat 1:1 / Badge thông báo`) | ⚠️ Chỉ ở góc **badge/chấm đỏ**, không có TC nào thao tác confirm/unconfirm ngay trên danh sách (đánh dấu từng item, filter 未確認/確認済み trên app). **Cả 4 TC đều chưa chạy** |
| 2 | **Confirm/unconfirm trong màn chat từng friend — app mobile** | `TC-SYNCAPP001-02` (17375) — "đọc một LINE user chỉ giảm badge 1" | ⚠️ Chỉ chiều **confirm** (mở hội thoại = tự đọc). **0 TC chiều unconfirm** (đánh dấu lại chưa đọc). Chưa chạy |
| 3 | **`/basic/talk-list` — web (FA-002)** | `không có` | ❌ **GAP hoàn toàn.** Đây là màn dùng `EP-05 /ajax/change-status-message` (`status=1` 確認済 / `status=2` 未確認) — đường ghi confirm/unconfirm **thứ hai** vào cùng `unconfirm_message` + `conversation.confirm_count` |
| 4 | **List chat 1:1 — web `/basic/chat-v3`** | `TC-SYNCAPP001-01` (17357), `TC-TOOLNEGCTRL001-01` (17358) | ⚠️ Chỉ **quan sát filter 未確認/確認済み sau khi API mobile chạy**. 0 TC thao tác `全て確認済みに変更` hay quick action `確認状況を変更` tại chính màn này |
| 5 | **Confirm/unconfirm trong màn chat từng friend — web** | `không có` | ❌ **GAP.** Kho TCs cũng không có (nhóm "Header hội thoại" `TC-CHT-143`→`150` không có TC confirm/unconfirm) |

> **Vì sao đây là vấn đề của chính fix này, không phải mở rộng phạm vi**: cả 5 màn ghi vào **cùng** `unconfirm_message` · `conversation.confirm_count` · `has_status_*` · `bots.count_user_unconfirm`. Fix **thêm điều kiện lọc** "chỉ lấy hội thoại còn lệch trạng thái **hoặc** còn `unconfirm_message`" → hành vi của nó **phụ thuộc vào shape dữ liệu do 4 màn kia tạo ra**. `TC-DATADB001-01` (17337) có test shape "đã đọc nhưng còn tin chưa xác nhận", nhưng bằng **data tự dựng**, chưa lần nào bằng **thao tác thật** từ màn `/basic/talk-list` hay quick action web.
>
> **Bằng chứng mâu thuẫn cụ thể**: spec `chat-management` §EP-05 bước 7 ghi màn `/basic/talk-list` **"Đếm lại `bots.count_user_unconfirm`"**, trong khi endpoint được fix **set thẳng = 0** (actual TC 17336: `5→0`, TC 17345: `7→0`). Hai đường ghi **khác thuật toán** trên cùng một counter → củng cố `G2`.

**Đã đủ TC (không ghi dòng)**: `BUG` (hình dạng câu lệnh) · `F1 makeReadAllMessageByBot` · `D1` 10 cột read-state · `D3 unconfirm_message` · `T1` FA-001 mark-read từ app · phạm vi WHERE theo bot/blocked · tính hội tụ chunkById · bản ghi đời cũ (`has_status_*` NULL) · lỗi giữa chừng nhiều lô.

**Không có ORPHAN** — 21/21 TC đều map được về `BUG` / `F*` / `D*` / `T*`.

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `11 quan điểm Trigger khớp task · 6 chưa cover đủ`

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `REG-SHARED-001` | `Cao` | `GAP — 0 TC.` Trigger khớp tuyệt đối: "fix bug **có thể tồn tại ở chức năng tương tự**". 3 hàm sibling có **cùng pattern chậm** (Dev mô tả "vòng lặp từng hội thoại — kiểu chậm khác"), Dev **đã cung cấp danh sách** nơi ảnh hưởng (mục 3 file 03) nhưng **không test lại mục nào**. Evidence bắt buộc của quan điểm = "danh sách nơi ảnh hưởng + kết quả test từng nơi" → mới có nửa đầu | `[BLOCKER]` |
| Q2 | `PERM-003` | `Cao` | `GAP — 0 TC.` Endpoint nhận `bot_id` từ request (TC 17341 gọi bot A = 991279199, bot B = 991279200 là 2 bot **của cùng tài khoản**). Chưa có TC nào gọi endpoint với `bot_id` **không thuộc tài khoản đăng nhập** ở **tầng API**. Kho TCs `MT-17` (mức CAO, chờ quyết định) ghi: spec Chat 1:1 tự nhận *"không phát hiện kiểm tra quyền Staff trong code Chat 1:1"*, *"mọi query filter theo bot_id **từ session**"* — độ tin cậy Trung bình. Fix **viết lại toàn bộ mệnh đề lọc** → đúng lúc phải kiểm lại nguồn của `bot_id` | `[BLOCKER]` |
| Q3 | `DATA-COUNT-001` | `Cao` | `GAP — 0 TC đối chiếu đủ nguồn.` "BẮT BUỘC khi màn hình có bất kỳ con số đếm nào" — badge chưa đọc là con số đếm, và quan điểm này được đánh dấu *"lỗi lặp nhiều nhất lịch sử bug"*. TC hiện có chỉ đọc `bots.count_user_unconfirm` ở DB, **chưa đối chiếu 4 nguồn** (badge app · badge menu web · DB · API), chưa có case **phép tính tay** khi còn hội thoại bị chặn (xem G2) | `[BLOCKER]` |
| Q4 | `PERF-LARGE-001` | `Trung bình → Cao` (chức năng xử lý khối lượng lớn — đây là **mục đích của ticket**) | `RISK — chưa test theo ngưỡng THỰC TẾ.` Kiểm tra bắt buộc: *"hỏi/tra số liệu khách hàng lớn nhất hiện tại → tạo test data **bằng hoặc lớn hơn** → đo thời gian"*. TC lớn nhất mới 2.500 hội thoại / bảng 232.287 dòng, trong khi ca lỗi thật quét 50.135.150 dòng | `[MAJOR]` |
| Q5 | `ENV-003` ★ | `Cao` | `RISK — 17/17 TC đã chạy đều ở env local; production 0 TC.` Evidence bắt buộc: *"screenshot/log lấy **trên production** (hoặc phiếu xác nhận không thể verify + phương án giám sát)"* — hiện chưa có cả hai. RULE-08: không kết luận từ local/staging với hạng mục loadbalance/hiệu năng | `[MAJOR]` |
| Q6 | `SYNC-APP-001` | `Trung bình → Cao` (flow critical user-facing: badge chưa đọc) | `RISK — 3/4 TC chưa chạy.` Chỉ `TC-SYNCAPP001-01` (17357, web) đã pass; 2 TC app (17375, 17376) còn draft. Evidence bắt buộc = *"screenshot cùng thao tác trên **cả** web và app"* — mới có phía web | `[MAJOR]` |

**Đã cover đủ (không ghi dòng)**: `CONC-001` (3 TC: 17349 idempotency · 17350 2 request đồng thời · 17371 lỗi giữa chừng) · `FUNC-001` / `FUNC-004` (biên lô 0 / 1000 / 1001 / 2500) · `DATA-MIG-001`-`COMPAT-LEGACY-001` (17338 bản ghi đời cũ `has_status_*` NULL) · `REG-RUN-001` (17371).

> ⚠️ `DATA-DB-001` (Cao) chỉ có **2 TC Normal** mang đúng mã (17337, 17342). Các chiều Abnormal (WHERE scope bot khác / hội thoại bị chặn) tuy **có nội dung** ở 17340, 17341 nhưng mang mã nội bộ Studio `TOOL-NEGCTRL-001` — không có trong `framework/checklist-lme.md`. Nội dung thực tế đã đủ chiều nên **không tính GAP**, nhưng xem `I4` ở §4 về việc mã lạ làm hỏng map coverage.

---

## 3. TC trùng lặp nội dung

Đã rà **21/21** TC theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`) — **không phát hiện cặp trùng cần xóa hoặc gộp.**

| Nhóm | TC | Kết luận |
|---|---|---|
| Cặp đã soi kỹ 1 | `TC-TOOLNEGCTRL001-02` (17340, DB) vs `TC-TOOLNEGCTRL001-01` (17358, UI web) | **Không trùng** — cùng đối tượng (hội thoại bị chặn) nhưng **khác tầng kiểm chứng**; RULE-07 yêu cầu verify cả DB lẫn màn hình. Giữ cả hai |
| Cặp đã soi kỹ 2 | `TC-CONC001-01` (17349, gọi 2 lần liên tiếp) vs `TC-DATADB001-02` (17342, hội thoại đã đọc không bị ghi đè) | **Không trùng** — khác tiền đề (vừa chạy xong lần 1 vs dựng sẵn hội thoại đã đọc) và khác ý định (idempotency vs điều kiện lọc bỏ qua) |
| Cặp đã soi kỹ 3 | `TC-FUNC004-02` (1000) · `TC-FUNC004-03` (1001) · `TC-TOOLKNOW002-01` (2500) | **Không trùng** — 3 giá trị biên khác nhau của cùng trục kích thước lô, mỗi TC có oracle riêng về số câu lệnh / số id mỗi câu |

- **Gate đã chạy**: không đề nghị xóa TC nào → coverage §1 + §2 không đổi.
- `DUP-INFLATE`: không có.
- `DUP-CONFLICT`: không có giữa các TC trong bộ. (Mâu thuẫn **TC ↔ spec** có, đã đưa §6.)

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[MAJOR]` | Toàn bộ nguồn — run #1280 | **0/21 TC chạy trên production**, 17 TC đã chạy đều ở `env = local`; bảng `conversation` local chỉ 232.287 dòng so với ~50 triệu dòng production. Ticket này bản chất là **hiệu năng trên dataset production** → RULE-08 / ENV-003 | Chạy lại tối thiểu bộ TC hiệu năng + index trên production (hoặc DB có volume tương đương). Không kết luận "fix xong" từ local |
| I2 | `[MAJOR]` | Run #1290 (staging) | Run staging đang ở trạng thái **`running`**, `startedAt 2026-09-11 07:26:24`, **`finishedAt: null`**, 0 kết quả. `envAuto` hiển thị `staging runs: 1` dễ bị đọc nhầm thành "đã test staging" | Chờ run kết thúc rồi mới tổng kết. Không tính staging vào coverage ở vòng review này |
| I3 | `[MAJOR]` | `03-dev-impact.md` | File có `Auto-filled: 2026-09-11 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" CHƯA tick** — F/D/T có thể thiếu hoặc map sai | Tester đọc lại Journal #135819 trên Redmine, đối chiếu mục 4.1/4.2/4.3 rồi tick checkbox trước khi review có giá trị |
| I4 | `[MAJOR]` | `TC-DATADB001-02` (17342) | **Oracle bị nới lỏng đúng ở điểm khác biệt hành vi của fix.** Actual ghi: *"BẰNG CHỨNG updated_at của P: trước=2026-09-08 05:31:01 → sau=2026-09-08 05:31:01 (giữ nguyên) — **chưa dùng làm oracle pass/fail theo đúng testcase**"*. TC pass mà không dùng `updated_at` làm điều kiện đánh giá, trong khi đây chính là REQ-005 | Sửa TC trên Studio (`testcase_update`) để `updated_at` giữ nguyên trở thành **oracle pass/fail**, không chỉ là ghi chú |
| I5 | `[MAJOR]` | `TC-CONC001-02` (17350) | Gắn `requirement_keys = REQ-006, REQ-008` nhưng nội dung chỉ test 2 request đồng thời — **không chạm** phần auth 401 / `bot_id` bất thường của REQ-008. Nhãn sai làm requirement trông như đã cover (xem G6) | Gỡ `REQ-008` khỏi TC này, hoặc bổ sung TC thật cho REQ-008 (đã đề xuất ở §5) |
| I6 | `[MINOR]` | 7/21 TC | Dùng **mã quan điểm nội bộ Studio không có trong `framework/checklist-lme.md`**: `TOOL-NEGCTRL-001` (3 TC) · `TOOL-KNOW-002` (2) · `TOOL-AXIS-001` (1) · `TOOL-OLDREC-001` (1) → không map được coverage theo bộ 80 quan điểm LME | Map lại sang mã chuẩn khi sửa trên Studio: `TOOL-NEGCTRL-001` → `DATA-DB-001` / `PERM-003`; `TOOL-KNOW-002` → `PERF-LARGE-001`; `TOOL-OLDREC-001` → `COMPAT-LEGACY-001` |
| I7 | `[MINOR]` | 4 TC badge (17373-17376) | `status = draft`, `author = haodtb@mcp`, `exec_mode = manual`, `env_scope = dev, staging`, chưa chạy lần nào. Nguồn spec ghi là `human: badge Chat 1:1 tính theo số LINE user...` — **business rule do người nhập trực tiếp vào Studio**, không có trong `spec-features/` | Chuyển 4 TC sang `active` và chạy; đồng thời đưa rule badge vào spec chính thức (xem §6) |
| I8 | `[NIT]` | Studio spec refs | Nhiều TC trích nguồn `spec: admin/chat/feature-spec.md` — **thư mục này không tồn tại** trong `spec-features/` (chỉ có `admin/chat-11/` và `admin/chat-management/`). Mã `BR-07` / `EP-03` Studio dùng cũng mang nghĩa **khác hẳn** mã cùng tên trong 2 spec đó | Sửa đường dẫn spec ref trên Studio để trace được về file thật |

**Chất lượng từng TC (5 mục rà)**: Expected đo lường được ✔ · Tiền đề + steps dựng lại được ✔ · Tiêu đề có keyword ✔ · Atomic ✔ · Dữ liệu test thật ✔ (dùng ID bot/hội thoại cụ thể). **Evidence** đầy đủ: 17/17 TC đã chạy đều có artifact `log`, 2 TC UI có kèm screenshot — đạt RULE-02.

---

## 5. TCs đề xuất bổ sung (17)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa001-chat11-11チャット.md` (FA-001) — 14 nhóm chức năng. **FA-002 (`/basic/talk-list`) chưa có trong kho** → màn đó không đối chiếu được |
| Vùng regression phát hiện từ kho | `TC-CHT-34` → `TC-CHT-40` nhóm **「全て確認済みに変更」** (11 TC, BULK-001/DATA-001) — chức năng **tương đương bên web** của endpoint đang fix. `TC-CHT-40` ghi rõ nguồn *"Bug tự detect **#39257** — performance confirmReadMessage"*, đúng ticket mà Dev nói đã tái dùng khuôn mẫu. `TC-CHT-35` mang bug lịch sử **#35144**: *"bug cũ là set `count_user_unconfirm = 0` luôn ⇒ sai"* |
| Conflict expected vs kho | **Có** — `TC-CHT-40` (kho) yêu cầu *"updated_at của hội thoại **CHƯA** xác nhận **được cập nhật**"*, còn bộ TC Studio chỉ verify phía "đã xác nhận thì giữ nguyên". Không mâu thuẫn trực tiếp nhưng là **nửa còn thiếu** → đã ra `TC-DATA001-01` thay vì bỏ qua. Mâu thuẫn thật sự (BR-06 sync ES) đã đưa **§6** |
| GAP dùng lại TC kho (không viết mới) | `G5` → tham chiếu `TC-CHT-40` cho phần `updated_at`; phần **thứ tự danh sách** kho chưa có → vẫn viết mới `TC-LIST001-01` |
| Vùng regression bổ sung (ma trận màn hình `G8`) | Kho có sẵn `TC-CHT-94`→`98` (quick action 確認状況を変更, cả 2 chiều 確認済/未確認) và `TC-CHT-52` (chấm xanh chưa đọc) → dùng làm nền cho `TC-REGSHARED001-06`. Kho **không có** TC confirm/unconfirm trong màn hội thoại từng friend (nhóm "Header hội thoại" `TC-CHT-143`→`150`) → phải viết mới |
| Xác nhận chống trùng | Đã đối chiếu **21** TC ở BƯỚC 0 + **kho FA-001** — **không TC đề xuất nào trùng**. Cụ thể: `TC-PERM003-01` khác `17341` (17341 gọi 2 bot **cùng tài khoản**, đây là bot **khác tài khoản**); `TC-ENV003-01` khác `17372` (17372 EXPLAIN trên local 232K dòng, đây là production ~50M dòng); `TC-DATA001-01` là **nửa ngược lại** của `17342` |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-REGSHARED001-01 | UI | REG-SHARED-001 | 全て確認済みに変更 (web) | Normal | manual | Tất cả | Thao tác 「全て確認済みに変更」 trên web vẫn đúng sau khi deploy branch fix | - Đã deploy branch `ai_small_40851`<br>- Đăng nhập admin, chọn bot A ở màn `/basic/chat-v3`<br>- Bot A có ≥3 friend chưa xác nhận (1 tin, 2 tin, 5 tin) và ≥3 friend đã xác nhận | 1. Ghi lại số badge chưa đọc ở menu 「1:1チャット」<br>2. Click icon filter → 「全て確認済みに変更」 → 決定<br>3. Quan sát danh sách + badge<br>4. Mở lại màn, đổi filter 未確認 / 確認済み | 3 friend chưa xác nhận (1 / 2 / 5 tin chưa đọc) | - Toàn bộ friend chuyển sang đã xác nhận, không dòng nào còn chấm xanh<br>- Badge menu 「1:1チャット」 về 0<br>- Filter 未確認 trả danh sách rỗng, filter 確認済み hiện đủ friend | | Lấp `G1` `Q1` · regression — dẫn từ `TC-CHT-34` · Đánh giá spec: Spec ghi rõ · Evidence: screenshot màn + ảnh badge |
| TC-REGSHARED001-02 | UI | REG-SHARED-001 | Đánh dấu đã đọc theo danh sách hội thoại | Normal | manual | Tất cả | Đánh dấu đã đọc từng hội thoại đã chọn vẫn đúng sau khi deploy branch fix | - Đã deploy branch `ai_small_40851`<br>- Bot A có ≥5 hội thoại chưa đọc<br>- Đang ở màn chat 1:1 của bot A | 1. Chọn 2 hội thoại bất kỳ trong danh sách chưa đọc<br>2. Thực hiện đánh dấu đã đọc cho đúng 2 hội thoại đó<br>3. Quan sát 2 hội thoại vừa chọn và 3 hội thoại còn lại<br>4. Ghi lại badge chưa đọc | 2 hội thoại được chọn / 3 hội thoại không chọn | - Đúng 2 hội thoại đã chọn chuyển sang đã đọc<br>- 3 hội thoại còn lại **giữ nguyên** trạng thái chưa đọc và số tin chưa đọc<br>- Badge giảm đúng 2, không về 0 | | Lấp `G1` `Q1` · regression — hàm `makeReadMessageByIds` Dev khai out-of-scope · Đánh giá spec: Spec ghi rõ · Evidence: screenshot trước/sau + ảnh badge |
| TC-REGSHARED001-03 | UI | REG-SHARED-001 | Đánh dấu đã đọc toàn bộ (bản cũ, app) | Normal | manual | Tất cả | Chức năng đánh dấu đã đọc toàn bộ bản cũ trên app vẫn chạy đúng, không bị ảnh hưởng bởi fix | - Đã deploy branch `ai_small_40851`<br>- Đăng nhập app di động bằng tài khoản có bot A<br>- Bot A có ≥5 hội thoại chưa đọc, trong đó ≥1 hội thoại bị chặn | 1. Trên app, dùng chức năng đánh dấu đã đọc toàn bộ **bản cũ** (đường không phải `by-bot`)<br>2. Đợi màn hình phản hồi<br>3. Kiểm tra danh sách hội thoại + badge trên app<br>4. Mở web cùng bot, đối chiếu danh sách | Bot A: 5 hội thoại chưa đọc (1 bị chặn) | - Chức năng bản cũ chạy xong, không lỗi, không treo<br>- Các hội thoại không bị chặn chuyển sang đã đọc<br>- Kết quả hiển thị **khớp nhau giữa app và web** | | Lấp `G1` `Q1` · regression — hàm `makeReadMessageAll` (Api + Mobile) · Đánh giá spec: Spec không ghi — cần Dev xác nhận điểm vào trên app · Evidence: screenshot app + web |
| TC-DATACOUNT001-01 | UI | DATA-COUNT-001 | Bộ đếm chưa xác nhận (badge) | Normal | manual | Tất cả | Badge chưa đọc sau khi app đánh dấu đã đọc toàn bộ — đối chiếu 4 nguồn | - Bot A có đúng 5 hội thoại: 3 chưa đọc không bị chặn, 2 chưa đọc **bị chặn**<br>- Đăng nhập app di động + mở web cùng bot A | 1. Ghi lại badge trên app, badge menu 「1:1チャット」 trên web<br>2. Trên app, chạy 「đánh dấu đã đọc toàn bộ theo bot」<br>3. Sau khi xong, đọc lại badge trên **app**, badge menu trên **web**<br>4. Đối chiếu với số hội thoại còn chưa đọc thực tế đếm tay trên danh sách | Bot A: 3 hội thoại chưa đọc thường + 2 hội thoại chưa đọc bị chặn | - Số badge ở **app** và ở **web** bằng nhau<br>- Số badge **khớp với số đếm tay** số hội thoại còn chưa đọc trên danh sách<br>- Chênh lệch giữa 2 nguồn bất kỳ = **fail** | | Lấp `G2` `Q3` · Đánh giá spec: Đã hỏi leader — liên quan bug lịch sử #35144 · Evidence: bảng đối chiếu 4 nguồn + phép tính tay + screenshot |
| TC-DATACOUNT001-02 | UI | DATA-COUNT-001 | Bộ đếm chưa xác nhận (badge) | Abnormal | manual | Tất cả | Bot chỉ còn hội thoại **bị chặn** chưa đọc — badge không được về 0 sai | - Bot B có đúng 2 hội thoại, **cả 2 đều bị chặn** và đều còn tin chưa đọc<br>- Không có hội thoại không-bị-chặn nào chưa đọc<br>- Đăng nhập app + web cùng bot B | 1. Ghi lại badge trên app và trên web<br>2. Trên app, chạy 「đánh dấu đã đọc toàn bộ theo bot」<br>3. Đọc lại badge trên app và web<br>4. Mở filter 未確認 trên web, đếm số hội thoại còn hiện | Bot B: 2 hội thoại bị chặn, mỗi hội thoại 2 tin chưa đọc | - 2 hội thoại bị chặn **vẫn ở trạng thái chưa đọc** (đúng thiết kế)<br>- Badge **phản ánh đúng** số hội thoại chưa đọc còn lại — **KHÔNG được hiển thị 0** trong khi filter 未確認 vẫn còn 2 hội thoại<br>- Nếu badge = 0 mà danh sách 未確認 còn hội thoại → **fail**, raise bug | | Lấp `G2` `Q3` · Đánh giá spec: Đã hỏi leader — tái lặp pattern bug #35144 (set counter = 0 thay vì đếm lại) · Evidence: screenshot badge + danh sách 未確認 cùng thời điểm |
| TC-DATACOUNT001-03 | UI | DATA-COUNT-001 | Bộ đếm chưa xác nhận (badge) | Boundary | manual | Tất cả | Bot không còn hội thoại chưa đọc nào — gọi lại lần nữa badge vẫn đúng 0 | - Bot C vừa được đánh dấu đã đọc toàn bộ, hiện 0 hội thoại chưa đọc<br>- Đăng nhập app + web cùng bot C | 1. Ghi lại badge (kỳ vọng không hiển thị / bằng 0)<br>2. Trên app, chạy lại 「đánh dấu đã đọc toàn bộ theo bot」 lần nữa<br>3. Đọc lại badge trên app và web<br>4. Kiểm tra danh sách hội thoại có bị thay đổi thứ tự không | Bot C: 0 hội thoại chưa đọc | - Thao tác trả về thành công, không lỗi<br>- Badge vẫn là 0 ở cả app và web<br>- Danh sách hội thoại **không đổi thứ tự**, không có hội thoại nào nhảy lên đầu | | Lấp `G2` `G5` `Q3` · Đánh giá spec: Spec ghi rõ · Evidence: screenshot badge + danh sách trước/sau |
| TC-DATA001-01 | Data | DATA-001 | 全て確認済みに変更 / đánh dấu đã đọc toàn bộ | Normal | manual | staging | Hội thoại CHƯA đọc phải được cập nhật dấu thời gian, hội thoại đã đọc thì không | - Bot A có 3 hội thoại **chưa đọc** và 3 hội thoại **đã đọc**<br>- Ghi lại được `updated_at` của cả 6 hội thoại trước khi thao tác<br>- Đăng nhập app di động | 1. Ghi lại `updated_at` của 3 hội thoại chưa đọc và 3 hội thoại đã đọc<br>2. Trên app, chạy 「đánh dấu đã đọc toàn bộ theo bot」<br>3. So sánh lại `updated_at` của cả 6 hội thoại<br>4. Kiểm tra bảng tin chưa xác nhận của 6 hội thoại | 3 hội thoại chưa đọc + 3 hội thoại đã đọc | - `updated_at` của 3 hội thoại **CHƯA** đọc **ĐƯỢC cập nhật** (đây là nửa hiện chưa ai test)<br>- `updated_at` của 3 hội thoại **ĐÃ** đọc **KHÔNG đổi**<br>- Bản ghi tin chưa xác nhận chỉ bị xoá ở 3 hội thoại chưa đọc, **không xoá thừa** của hội thoại khác | | Lấp `G3` · regression — dẫn từ `TC-CHT-40` (kho, bug #39257) · Đánh giá spec: Spec ghi rõ · Evidence: ảnh chụp kết quả query trước/sau kèm câu query |
| TC-LIST001-01 | UI | LIST-001 | Danh sách hội thoại — thứ tự hiển thị | Normal | manual | Tất cả | Thứ tự danh sách hội thoại không bị đảo sau khi đánh dấu đã đọc toàn bộ | - Bot A có ≥10 hội thoại với thời điểm tin cuối khác nhau rõ ràng<br>- Trong đó có cả hội thoại đã đọc lẫn chưa đọc xen kẽ<br>- Mở song song app và web cùng bot A | 1. Chụp lại thứ tự 10 hội thoại đầu trên **app** và trên **web**<br>2. Trên app, chạy 「đánh dấu đã đọc toàn bộ theo bot」<br>3. Tải lại danh sách trên cả app và web<br>4. So sánh thứ tự 10 hội thoại đầu với ảnh ở bước 1 | 10 hội thoại, tin cuối cách nhau ≥1 ngày | - Thứ tự hội thoại **giữ nguyên** theo thời điểm tin cuối ở cả app và web<br>- Không có hội thoại cũ nào nhảy lên đầu danh sách chỉ vì vừa được đánh dấu đã đọc | | Lấp `G5` · Đánh giá spec: Spec không ghi — hành vi `updated_at` đổi theo REQ-005, cần Dev xác nhận cột dùng để sort · Evidence: screenshot danh sách trước/sau trên cả app và web |
| TC-PERM003-01 | API | PERM-003 | API đánh dấu đã đọc toàn bộ theo bot | Abnormal | manual | staging | Gọi endpoint với mã bot KHÔNG thuộc tài khoản đăng nhập — không được đụng dữ liệu bot đó | - Có 2 tài khoản khác nhau: tài khoản 1 sở hữu bot A, tài khoản 2 sở hữu bot Z<br>- Bot Z có ≥3 hội thoại chưa đọc, ghi lại trạng thái + badge trước khi test<br>- Lấy được token/phiên đăng nhập app của **tài khoản 1** | 1. Ghi lại trạng thái chưa đọc + badge của bot Z (tài khoản 2)<br>2. Đăng nhập app bằng **tài khoản 1**<br>3. Gọi endpoint đánh dấu đã đọc toàn bộ nhưng truyền mã bot của **bot Z**<br>4. Đọc phản hồi<br>5. Đăng nhập lại tài khoản 2, kiểm tra trạng thái + badge của bot Z | Phiên đăng nhập tài khoản 1 + mã bot của bot Z | - Yêu cầu bị **từ chối** (hoặc kết thúc êm mà không thay đổi gì)<br>- Bot Z **giữ nguyên** đủ 3 hội thoại chưa đọc và badge như trước<br>- Nếu bot Z bị đánh dấu đã đọc → **fail, raise bug bảo mật ngay** | | Lấp `G6` `Q2` · Đánh giá spec: Đã hỏi leader — kho `MT-17` ghi spec tự nhận chưa xác định kiểm quyền ở Chat 1:1 · Evidence: phản hồi API + ảnh trạng thái bot Z trước/sau |
| TC-PERM003-02 | API | PERM-003 | API đánh dấu đã đọc toàn bộ theo bot | Abnormal | manual | staging | Gọi endpoint khi chưa đăng nhập — bị chặn, không đổi dữ liệu | - Bot A có ≥3 hội thoại chưa đọc<br>- Ghi lại trạng thái trước khi test<br>- Không có phiên đăng nhập hợp lệ (đã đăng xuất / token rỗng / token sai) | 1. Ghi lại trạng thái chưa đọc của bot A<br>2. Gọi endpoint đánh dấu đã đọc toàn bộ **không kèm thông tin đăng nhập**<br>3. Lặp lại với thông tin đăng nhập sai<br>4. Kiểm tra lại trạng thái bot A | Không token · token sai | - Cả 2 lần đều bị chặn với mã **401**<br>- Bot A giữ nguyên đủ 3 hội thoại chưa đọc, badge không đổi | | Lấp `G6` `Q2` · Đánh giá spec: Spec ghi rõ — REQ-008 Studio · Evidence: mã phản hồi HTTP + ảnh trạng thái trước/sau |
| TC-PERM003-03 | API | PERM-003 | API đánh dấu đã đọc toàn bộ theo bot | Abnormal | manual | staging | Mã bot không tồn tại / bỏ trống / sai kiểu — kết thúc êm, không lỗi máy chủ | - Đăng nhập hợp lệ bằng tài khoản có bot A<br>- Bot A có ≥3 hội thoại chưa đọc, ghi lại trạng thái trước khi test | 1. Gọi endpoint với mã bot **không tồn tại** (VD 999999999)<br>2. Gọi lại với mã bot **để trống**<br>3. Gọi lại với mã bot **sai kiểu** (chuỗi chữ)<br>4. Sau mỗi lần: đọc phản hồi và kiểm tra trạng thái bot A | `999999999` · rỗng · `abc` | - Cả 3 lần: **không** trả lỗi máy chủ (5xx), không treo<br>- Bot A và mọi bot khác **không bị thay đổi** dữ liệu nào<br>- Phản hồi giữ đúng cấu trúc body mà app đang dùng | | Lấp `G6` · Đánh giá spec: Spec ghi rõ — REQ-008 Studio · Evidence: 3 phản hồi API + ảnh trạng thái bot A trước/sau |
| TC-REGSHARED001-05 | UI | REG-SHARED-001 | `/basic/talk-list` — đổi trạng thái xác nhận | Normal | manual | Tất cả | Đổi 「確認済」/「未確認」 hàng loạt ở màn danh sách tin nhắn web vẫn đúng sau khi deploy | - Đã deploy branch `ai_small_40851`<br>- Đăng nhập admin, bot A, mở màn `/basic/talk-list`<br>- Có ≥4 dòng tin: 2 đang 未確認, 2 đang 確認済<br>- Ghi lại badge menu trước khi thao tác | 1. Tick chọn 2 dòng đang 未確認 → chọn radio 「確認済」 → bấm 「変更」<br>2. Quan sát danh sách + badge menu + tab 「未確認のみ」<br>3. Tick chọn 2 dòng vừa đổi → chọn radio 「未確認」 → bấm 「変更」<br>4. Quan sát lại danh sách, badge và tab 「未確認のみ」 | 2 dòng 未確認 + 2 dòng 確認済 | - Bước 2: 2 dòng chuyển sang 確認済, biến mất khỏi tab 「未確認のみ」, badge menu **giảm đúng 2**<br>- Bước 4: 2 dòng quay lại 未確認, xuất hiện lại ở tab 「未確認のみ」, badge menu **tăng lại đúng 2**<br>- Không lỗi, không màn trắng | | Lấp `G8` `Q1` · regression — đường ghi `EP-05 /ajax/change-status-message`, màn hiện có **0 TC** · Đánh giá spec: Spec ghi rõ — `chat-management` §EP-05 · Evidence: screenshot danh sách + badge trước/sau mỗi bước |
| TC-REGSHARED001-06 | UI | REG-SHARED-001 | Quick action 確認状況を変更 (web `/basic/chat-v3`) | Normal | manual | Tất cả | Đổi trạng thái xác nhận từng bạn bè bằng quick action trên web vẫn đúng sau khi deploy | - Đã deploy branch `ai_small_40851`<br>- Đăng nhập admin, bot A, màn `/basic/chat-v3`<br>- Bạn bè X đang 未確認 (có tin chưa đọc), bạn bè Y đang 確認済<br>- Ghi lại badge menu 「1:1チャット」 | 1. Click avatar bạn bè X → mở modal クイックアクション<br>2. Ở nhóm 「確認状況を変更」 chọn 「確認済」 → thực thi<br>3. Quan sát chấm xanh của X, badge menu, filter 未確認<br>4. Lặp lại với bạn bè Y nhưng chọn 「未確認」<br>5. Quan sát chấm xanh của Y, badge menu, filter 未確認 | X: 2 tin chưa đọc · Y: đã xác nhận | - X: mất chấm xanh, rời khỏi filter 未確認, badge **giảm 1**<br>- Y: hiện chấm xanh trở lại, vào filter 未確認, badge **tăng 1**<br>- Cả 2 chiều đều phản ánh ngay, không cần tải lại màn | | Lấp `G8` `Q1` · regression — dẫn từ `TC-CHT-95` / `TC-CHT-96` (kho) · Đánh giá spec: Spec ghi rõ · Evidence: screenshot chấm xanh + badge trước/sau cả 2 chiều |
| TC-REGSHARED001-07 | UI | REG-SHARED-001 | Màn hội thoại từng friend (web) | Normal | manual | Tất cả | Confirm/unconfirm ngay trong cửa sổ hội thoại của 1 bạn bè trên web vẫn đúng | - Đã deploy branch `ai_small_40851`<br>- Đăng nhập admin, bot A, màn `/basic/chat-v3`<br>- Bạn bè X có 3 tin chưa đọc<br>- Ghi lại badge menu trước khi mở | 1. Click mở hội thoại của X<br>2. Quan sát trạng thái xác nhận của X + badge menu ngay sau khi mở<br>3. Dùng thao tác đổi trạng thái xác nhận có trong màn hội thoại để chuyển X về 「未確認」<br>4. Quay lại danh sách, quan sát chấm xanh của X + badge + filter 未確認 | X: 3 tin chưa đọc | - Bước 2: ghi nhận rõ mở hội thoại **có** hay **không** tự động chuyển sang đã xác nhận (đây là điểm chưa ai chốt — ghi lại hành vi thực tế)<br>- Bước 4: X quay về 未確認, có chấm xanh, nằm trong filter 未確認, badge khớp số thực tế | | Lấp `G8` `Q1` · regression · Đánh giá spec: **Spec không ghi** — kho TCs nhóm "Header hội thoại" (`TC-CHT-143`→`150`) cũng không có TC confirm/unconfirm → cần Leader chốt hành vi kỳ vọng · Evidence: screenshot hội thoại + danh sách + badge |
| TC-REGSHARED001-08 | UI | REG-SHARED-001 | Màn hội thoại từng friend (app mobile) | Normal | manual | Tất cả | Confirm/unconfirm trong màn chat của 1 bạn bè trên app mobile vẫn đúng | - Đã deploy branch `ai_small_40851`<br>- Đăng nhập app di động, bot A<br>- Bạn bè X có 3 tin chưa đọc, bạn bè Y có 2 tin chưa đọc<br>- Ghi lại badge Chat 1:1 trên app | 1. Mở màn chat của X trên app<br>2. Quan sát badge app + chấm đỏ của X sau khi mở<br>3. Dùng thao tác đánh dấu lại chưa đọc (nếu app có) cho X<br>4. Thoát về danh sách, quan sát chấm đỏ của X, của Y và badge<br>5. Mở web cùng bot, đối chiếu trạng thái của X | X: 3 tin chưa đọc · Y: 2 tin chưa đọc | - Bước 2: badge giảm đúng **1** (theo LINE user, không theo số tin), chấm đỏ của X mất, của Y còn nguyên<br>- Bước 4: nếu app có chức năng đánh dấu chưa đọc → X hiện lại chấm đỏ và badge tăng lại 1; nếu **không có** → ghi rõ "app không hỗ trợ unconfirm" vào kết quả<br>- Bước 5: trạng thái X trên web **khớp** với app | | Lấp `G8` `Q1` `Q6` · regression — hàm `makeReadMessageByIds` Dev khai out-of-scope · Đánh giá spec: Spec không ghi — cần Dev xác nhận app có chức năng unconfirm không · Evidence: screenshot app + web cùng thời điểm |
| TC-DATA001-02 | Data | DATA-001 | Liên thông `/basic/talk-list` ⇄ app mark-read-all | Abnormal | manual | staging | Hội thoại vừa bị đánh dấu 「未確認」 từ web phải được app đánh dấu đã đọc toàn bộ dọn sạch | - Bot A có 3 hội thoại **đang ở trạng thái đã xác nhận**<br>- Đăng nhập được cả web `/basic/talk-list` lẫn app di động cùng bot A<br>- Ghi lại được trạng thái + badge trước khi thao tác | 1. Trên web `/basic/talk-list`: tick 3 dòng tin của 3 hội thoại đó → chọn 「未確認」 → 「変更」<br>2. Ghi lại badge menu web + badge app sau bước 1<br>3. Trên app, chạy 「đánh dấu đã đọc toàn bộ theo bot」<br>4. Kiểm tra lại 3 hội thoại đó ở cả web (tab 「未確認のみ」, filter 未確認) và app<br>5. Kiểm tra badge ở web + app | 3 hội thoại đã xác nhận → bị đổi về 未確認 từ web | - Sau bước 3: **cả 3 hội thoại** được dọn sạch, không còn ở tab 「未確認のみ」 và filter 未確認<br>- Badge web và badge app đều về **0**, khớp nhau<br>- Nếu còn sót hội thoại nào ⇒ điều kiện lọc mới của fix **không bắt được** shape dữ liệu do web tạo ra → **fail, raise bug** | | Lấp `G8` `G2` · **interop — đây là TC quan trọng nhất của nhóm này**: `TC-DATADB001-01` (17337) mới test shape này bằng data tự dựng, chưa lần nào bằng thao tác thật từ web · Đánh giá spec: Spec ghi rõ — `chat-management` §EP-05 bước 5b/6 · Evidence: ảnh trạng thái + badge ở cả 2 màn qua từng bước |
| TC-DATA001-03 | Data | DATA-001 | Liên thông quick action web ⇄ app mark-read-all | Abnormal | manual | staging | Bạn bè bị đánh dấu 「未確認」 bằng quick action trên web phải được app đánh dấu đã đọc toàn bộ dọn sạch | - Bot A có 2 bạn bè đang ở trạng thái **đã xác nhận**<br>- Đăng nhập được cả web `/basic/chat-v3` lẫn app di động cùng bot A<br>- Ghi lại badge trước khi thao tác | 1. Trên web `/basic/chat-v3`: quick action đổi 2 bạn bè đó sang 「未確認」<br>2. Ghi lại badge web + app<br>3. Trên app, chạy 「đánh dấu đã đọc toàn bộ theo bot」<br>4. Quay lại web, xem filter 未確認 và chấm xanh của 2 bạn bè<br>5. Đối chiếu badge web + app | 2 bạn bè đã xác nhận → bị đổi về 未確認 bằng quick action | - Sau bước 3: cả 2 bạn bè hết chấm xanh, không còn trong filter 未確認<br>- Badge web = badge app = **0**<br>- Còn sót ⇒ **fail, raise bug** (đường ghi quick action tạo shape mà fix không nhận ra) | | Lấp `G8` `G2` · interop — đường ghi thứ 3 vào cùng bộ data · Đánh giá spec: Spec ghi rõ · Evidence: ảnh filter + chấm xanh + badge ở cả 2 màn |

**GAP không đề xuất TC mới**:
- `G4` (badge FA-002 chưa test) — **đã có đủ 4 TC** trên Studio (17373-17376), vấn đề là **chưa chạy**, không phải thiếu TC. Việc cần làm: chuyển `status` sang active và chạy, không đẻ TC mới. Nếu chạy xong vẫn hở thì mở lại ở vòng review sau.
- `Q6` (SYNC-APP-001) — cùng lý do với `G4`; phần web đã có `TC-SYNCAPP001-01` pass, phần app nằm ở 17375/17376.
- **`G7` · `Q4` (PERF-LARGE-001) · `Q5` (ENV-003) — Leader quyết định LOẠI khỏi vòng này (2026-09-11).** 3 TC đã soạn cho các mục này (`TC-PERFLARGE001-01` đo thời gian thật trên bot lớn nhất production · `TC-ENV003-01` xác nhận chỉ mục trên production · `TC-REGSHARED001-04` chạy hàm sibling trên bot ≥5.000 hội thoại) **đều bắt buộc `Phạm vi ENV = product`** nên đã được gỡ khỏi bảng trên và **KHÔNG sync lên test tool**.
  ⚠️ **Hệ quả phải ghi nhận**: 3 dòng `G7` / `Q4` / `Q5` **vẫn đang mở**, và đây chính là **mục đích gốc của ticket** — sau vòng test này vẫn **chưa có bằng chứng** rằng câu lệnh 95.533359s đã hết trên production, cũng chưa xác nhận bảng `conversation` production có chỉ mục như fix giả định (chính Dev đề nghị reviewer kiểm). Cần một phương án thay thế do Leader chốt: **giám sát slow query log production sau release** (theo dõi endpoint `make-read-all-message-by-bot` trong N ngày), hoặc phiếu xác nhận "không thể verify trên production" theo Evidence của `ENV-003`.
- **RULE-01 cho `Q1` (REG-SHARED-001, ưu tiên Cao)**: sau khi loại `TC-REGSHARED001-04`, nhóm này chỉ còn **7 TC Normal**, **không còn Boundary**. Lý do ghi theo RULE-01: TC Boundary duy nhất của nhóm (bot ≥5.000 hội thoại) cần dữ liệu quy mô production nên nằm ngoài phạm vi vòng này. Chiều Abnormal của trục phân quyền/tham số đã được `TC-PERM003-01/02/03` gánh.

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | `spec-features/admin/chat-management/feature-spec.md:450` — **BR-06** (ưu tiên **Cao**) | BR-06 ghi: *"Khi `confirm_count` của conversation thay đổi → ghi record vào `sync_elasticsearch` để đồng bộ dữ liệu tìm kiếm"*. Nhưng endpoint `make-read-all-message-by-bot` **không sync Elasticsearch** — Studio `dev_impact` xác nhận: *"Không sync ElasticSearch trong hàm này (**cả cũ lẫn mới**) — không phải hồi quy mới nhưng lưu ý khi so mobile ⇔ web"*. Cần ghi rõ BR-06 **không áp dụng** cho đường mobile, hoặc thừa nhận đây là lỗ hổng đồng bộ dữ liệu tìm kiếm | `TC vs spec` — Studio `dev_impact` vs `chat-management` BR-06 | `Dev + Leader` |
| 2 | `spec-features/admin/chat-management/feature-spec.md:451` — **BR-07** (ưu tiên **Cao**) | BR-07 ghi: sau khi đổi trạng thái phải *"cập nhật `bots.count_user_unconfirm`, gửi **FCM push** cập nhật badge mobile, broadcast **InfoEvent** qua WebSocket cập nhật realtime web"*. Không tài liệu nào nói endpoint mobile `by-bot` có làm 2 việc cuối hay không → không có chuẩn để chấm 4 TC badge (17373-17376). Cần bổ sung: đường mobile có bắn FCM/WebSocket không | `TC vs spec` — 4 TC badge Studio lấy rule từ `human:` nhập tay, không có trong spec | `Dev + PM` |
| 3 | `spec-features/admin/chat-management/feature-spec.md` §5 + `chat-11/feature-spec.md:394` | Cần 1 Business Rule mô tả **vòng đời `bots.count_user_unconfirm`**: khi nào set 0, khi nào đếm lại, và xử lý thế nào khi bot **còn hội thoại bị chặn chưa đọc**. Kho TCs `MT-15` (chờ quyết định) đã nêu: bộ đếm được duy trì bởi ≥5 trường ở 3 bảng nhưng spec chỉ mô tả 1 trường; bug **#35144** và **#39257** đều xuất phát từ vùng này | `TC vs kho-tcs` — `TC-CHT-35` (kho) vs hành vi reset 0 của endpoint | `Dev + Leader` |
| 4 | Studio task #294 — spec refs | Nhiều TC trích `spec: admin/chat/feature-spec.md (SCR-CHAT-01 / BR-ENV)` — **thư mục không tồn tại**; mã `BR-07` / `EP-03` mà Studio dùng mang nghĩa khác hẳn `BR-07` / `EP-03` trong `chat-11` và `chat-management`. Cần thống nhất hệ mã trích dẫn để trace được | `TC vs spec` | `Leader` |
