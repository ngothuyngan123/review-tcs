<!-- sync-tcs: url=<chưa có — TC gốc nằm trên MCP LME TEST STUDIO, không phải Sheet> | sheet=<chưa có> | anchor=Main Function -->
<!-- source: MCP LME TEST STUDIO — task_id=14, ticket 37744, testcase_list (32 TC), fetch lúc 2026-09-16. Redmine KHÔNG có Link TCs human. -->

# 04 — TC List (fetch từ MCP LME TEST STUDIO)

## ⚠️ Cảnh báo bắt buộc đọc trước khi review

| Mục | Thực tế |
|---|---|
| **Tác giả TC** | **100% do AI sinh** — `author = AI`, `provenance.source = ai`, `toolWritten: tool 32 / mcp 0 / human 0` (rate 100%). **KHÔNG có TC nào do member người viết.** |
| **Trạng thái TC** | Toàn bộ 32 TC còn ở `status = draft`, `version = 1`, `round = 1`, `reviewed = false`, `reviewState = tester`. |
| **Kết quả chạy** | 32 TC: **16 Đạt · 5 Không đạt · 11 Chưa test** (`aiResult = fail`, `status = done-ai`). |
| **Môi trường đã chạy** | **CHỈ `local`** — 1 run (`runId=30`, `by = pipeline`, 2026-07-22 14:07:04), 21/32 TC automated. `dev` / `staging` / `prd`: **0 run**. |
| **Người chạy** | `pipeline` (AI chạy tự động), **không phải QA người chạy tay**. |
| **Bug ticket** | `openBugs = 3` trên task Studio, nhưng **cả 32 TC đều có `bug_tickets = []`** → 5 TC fail **CHƯA gắn ticket bug nào**. |
| **⚠️ RULE-08** | 3 TC tự khai `production-only / prod-like` (NEW-27, NEW-28, NEW-32) + 1 TC đa trình duyệt (NEW-30) **chưa chạy ở bất kỳ env nào**. Không được kết luận đạt từ `local`. |
| **⚠️ Spec nền của TC có thể đã lỗi thời** | Theo `dev_impact` trên Studio (computed 2026-08-25): spec `SCR-BE-03` mô tả màn là **landing page marketing tĩnh (3 block + nút「無料で利用開始」)** nhưng **code thực tế là wizard nhiều bước (nhập kênh → webhook → xác nhận, dùng Vue)**. Bộ 32 TC sinh ngày **2026-07-22**, tức **TRƯỚC** phân tích diff này, và nhiều TC (NEW-1, NEW-6, NEW-26...) vẫn bám giả định landing page + CTA「無料で利用開始」. **Cần Leader xác định TC nào còn đúng bản chất màn.** |

### Mã quan điểm Studio KHÔNG có trong `framework/checklist-lme.md` (6 mã / 6 TC)

| Mã quan điểm Studio | TC dùng | Ảnh hưởng |
|---|---|---|
| `TOOL-AXIS-001` | NEW-2 | `/review-tc` không map được coverage sang 80 quan điểm LME |
| `TOOL-ERRHYG-001` | NEW-12 | nt |
| `TOOL-PAIRWISE-001` | NEW-16 | nt |
| `TOOL-VAL2-001` | NEW-17 | nt |
| `TOOL-NEGCTRL-001` | NEW-24 | nt |
| `TOOL-OLDREC-001` | NEW-25 | nt |

20 mã còn lại (`FUNC-001/003/004` · `REG-SPEC-001` · `REG-URL-001` · `REG-SHARED-001` · `OUT-TRUTH-001` · `DATA-TEXT-001` · `SEC-001/002` · `SEC-ISO-001` · `PERM-001/002` · `CONC-001` · `UI-001/002/003` · `DEPLOY-ASSET-001` · `DEPLOY-LIVE-001` · `ENV-003`) đều có trong checklist.

### Phân bố

- **Loại case**: Normal 16 · Abnormal 14 · Boundary 2.
- **`exec_mode`**: auto 21 · manual 11 — **11 TC manual trùng khít 11 TC chưa test** (pipeline chỉ chạy được phần auto).
- **`tc_group`**: `null` toàn bộ 32 TC. **`env_scope`**: `null` toàn bộ. **`requirement_keys`**: rỗng toàn bộ. **`spec_status`**: `null` toàn bộ.
- **`env_tag`**: local-only 17 · env-safe 13 · read-only 2.
- **`spec_ids`**: `SCR-BE-03` (26) · `EP-08` (16) · `SCR-BE-01` (5).
- **Màn**: màn change bot `/admin/change-bots-new/{id}` là chủ yếu; 3 TC ở màn bot-edit `SCR-BE-01`; 1 TC luồng bot-save; 1 TC kiểm tra tĩnh template.

### 5 TC Không đạt (chưa có ticket bug)

`NEW-3` (lời chào tên user) · `NEW-4` (username emoji/ký tự đặc biệt) · `NEW-6` (CTA「無料で利用開始」) · `NEW-10` (guard bot free plan_type=2) · `NEW-16` (pairwise type × type_change × plan).

### 11 TC Chưa test (toàn bộ là `exec_mode = manual`)

`NEW-1` · `NEW-5` · `NEW-21` · `NEW-22` · `NEW-26` · `NEW-27` · `NEW-28` · `NEW-29` · `NEW-30` · `NEW-31` · `NEW-32`.

## Thông tin

| Trường | Giá trị |
|---|---|
| Tester viết TCs | `AI (pipeline Studio)` — không có member người viết |
| Ngày submit | `2026-07-22` (created_at của toàn bộ 32 TC) |
| Version TCs | `v1` (round 1, chưa review) |
| Link TC gốc (nếu có) | MCP LME TEST STUDIO — `task_id = 14`, `ticket_id = 37744`, feature `bot-edit`, branch `release_staging_20260704` |

---

## TC List

> ⚠️ **TC No. = ID hiển thị trên Studio (`temp_id`)** theo ngoại lệ ở CLAUDE.md — để Leader tra ngược được trên tool. Mã quan điểm giữ nguyên của Studio ở cột `Mã quan điểm liên kết`, **KHÔNG tự map** sang mã của `framework/checklist-lme.md`.
> **TCs là read-only** — chép nguyên văn từ Studio. Muốn sửa thì sửa trên Studio (`testcase_update`) rồi fetch lại.

| TC No. | Mã quan điểm liên kết | Loại case | Tiêu đề test case | Điều kiện tiền đề | Các bước thực hiện | Dữ liệu test/input | Kết quả mong đợi | Kết quả thực thi | Evidence thực tế | Môi trường test | Người thực hiện | Ngày thực hiện | Số ticket bug | Trạng thái đánh giá spec | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| NEW-1 | REG-SPEC-001 | Normal | Bố cục & nội dung màn change bot mới hiển thị đúng theo mockup | Có bot trả phí hợp lệ; đã đăng nhập admin; có mockup/AC giao diện mới từ dev | 1. Đăng nhập admin, mở màn bot-edit của 1 bot trả phí<br>2. Bấm nút「LINE公式アカウント入れ替え」để sang màn change bot mới<br>3. Đối chiếu từng khối nội dung trên màn với mockup mới | | Màn hiển thị đúng bố cục/nội dung giao diện MỚI như mockup: các khối, tiêu đề, hình minh hoạ, nút CTA đúng vị trí và nội dung; không còn hiển thị bố cục cũ (3 block marketing cũ) nếu spec mới thay thế | Chưa test | | (dự kiến — env_scope null; env_tag env-safe) | | | | | Studio #676 (NEW-1) · exec_mode manual · env_tag env-safe · env_hint "cần AC/mockup từ dev để chốt expected chi tiết" · spec_ids SCR-BE-03, EP-08 · màn: Màn thay thế LOA / change bot (/admin/change-bots-new/{id}) · note gốc: "SPEC-GAP: nội dung giao diện mới CHƯA có trong task (#37744) — cần AC/mockup từ dev để chốt expected; hiện chỉ verify được khác bản cũ + đúng khung. REG-SPEC-001: khi có mockup phải rà lại và đánh dấu [Giữ nguyên]/[Cần sửa] các TC nhóm UI-render. Cần mắt người đối chiếu pixel → manual." |
| NEW-2 | TOOL-AXIS-001 | Normal | (Kiểm tra kỹ thuật) Đối chiếu template blade màn change bot với mockup mới | Có source repo tại nhánh release_staging_20260704; có mockup mới | 1. Mở template view render bởi adminChangeNewBot<br>2. Đối chiếu các block/markup với danh sách thành phần trong mockup mới<br>3. Kiểm tra biến động (tên user, params) được nhúng đúng chỗ | | Template chứa đủ các thành phần giao diện mới; các biến bot_slot_id, type, typeChange, bot_id, username, hash_id được truyền/nhúng vào view; không sót markup của bản cũ đã bị bỏ | Đạt | | LOCAL | pipeline | 2026-07-22 | | | Studio #677 (NEW-2) · exec_mode auto · env_tag read-only · ⚠️ mã quan điểm KHÔNG có trong checklist-lme.md · spec_ids SCR-BE-03, EP-08 · màn: Template màn change bot · note gốc: "techNote: view admin.bots.bot_add_v3, controller BotController@adminChangeNewBot (~BotController.php:6003). Kiểm tra tĩnh source (đọc blade/controller), không dựng app." |
| NEW-3 | OUT-TRUTH-001 | Normal | Lời chào tên user「{username} 様」hiển thị đúng user đang đăng nhập | User A đăng nhập, có tên hiển thị xác định | 1. Mở màn change bot bằng bot hợp lệ của user A<br>2. Quan sát vùng lời chào tên user | | Hiển thị đúng「{tên user A} 様」khớp với user đang đăng nhập, không hiển thị tên user khác/rỗng | Không đạt | | LOCAL | pipeline | 2026-07-22 | (chưa gắn ticket) | | Studio #678 (NEW-3) · exec_mode auto · env_tag local-only · spec_ids SCR-BE-03, EP-08 · note gốc: "techNote: biến username truyền từ controller sang view. Verify được ở tầng integration/render." |
| NEW-4 | DATA-TEXT-001 | Abnormal | Username chứa emoji / ký tự đặc biệt / full-width hiển thị không lỗi font | User có tên chứa emoji, ①②㈱, ký tự full-width/half-width lẫn lộn | 1. Set tên user chứa emoji + ký tự đặc biệt + full-width<br>2. Mở màn change bot<br>3. Quan sát lời chào tên user | Tên: 😀㈱テスト①ＡＢＣ様太郎 | Tên hiển thị đầy đủ đúng ký tự, không mất ký tự, không lỗi font, không hỏng encode (không hiển thị mojibake), không vỡ layout | Không đạt | | LOCAL | pipeline | 2026-07-22 | (chưa gắn ticket) | | Studio #679 (NEW-4) · exec_mode auto · env_tag local-only · spec_ids SCR-BE-03, EP-08 · note gốc: "techNote: kiểm tra escape/encode trong blade. Có thể verify render integration." |
| NEW-5 | FUNC-004 | Boundary | Username rất dài / rỗng không làm vỡ layout màn change bot | User có tên dài sát giới hạn cột DB, và ca user tên rỗng | 1. Set tên user = chuỗi dài tối đa theo giới hạn cột<br>2. Mở màn change bot, quan sát vùng lời chào<br>3. Lặp lại với tên user rỗng/null | Tên dài: chuỗi 255 ký tự; và tên rỗng | Tên dài xuống dòng/cắt gọn theo thiết kế, không tràn/vỡ layout, không đè lên nút CTA; tên rỗng hiển thị fallback hợp lý (vd chỉ「様」hoặc theo spec), không lỗi | Chưa test | | (dự kiến — env_scope null; env_tag env-safe) | | | | | Studio #680 (NEW-5) · exec_mode manual · env_tag env-safe · spec_ids SCR-BE-03 · note gốc: "Biên độ dài + ca rỗng. Vỡ layout cần mắt người → manual; ca rỗng có thể auto ở render nếu chỉ check không crash." |
| NEW-6 | FUNC-001 | Normal | Nút CTA「無料で利用開始」hiển thị và trỏ đúng bước bắt đầu đăng ký LOA | Bot trả phí hợp lệ | 1. Mở màn change bot<br>2. Quan sát nút CTA chính<br>3. Bấm nút CTA「無料で利用開始」 | | Nút CTA hiển thị đúng nhãn theo spec mới; bấm vào điều hướng đúng sang bước bắt đầu quy trình đăng ký LOA mới (không gãy link, không 404/500), truyền đúng bot_slot_id/type/type_change | Không đạt | | LOCAL | pipeline | 2026-07-22 | (chưa gắn ticket) | | Studio #681 (NEW-6) · exec_mode auto · env_tag local-only · spec_ids SCR-BE-03, EP-08 · note gốc: "TOOL-REALDRIVE: verify bằng điều hướng thật (route/controller), không giả lập. Nhãn CTA nếu spec mới đổi cần chốt theo mockup." |
| NEW-7 | FUNC-001 | Normal | Điều hướng từ SCR-BE-01 qua nút「LINE公式アカウント入れ替え」sang màn change bot | Bot trả phí hợp lệ | 1. Mở màn bot-edit (SCR-BE-01)<br>2. Bấm nút「LINE公式アカウント入れ替え」<br>3. Quan sát URL và màn đích | | Điều hướng đúng sang /admin/change-bots-new/{id} với đúng id (Hashids) của bot; màn change bot mới render đầy đủ; các query param type/type_change/bot_slot_id truyền đúng | Đạt | | LOCAL | pipeline | 2026-07-22 | | | Studio #682 (NEW-7) · exec_mode auto · env_tag local-only · spec_ids SCR-BE-01, SCR-BE-03 · màn: Màn bot-edit (/admin/bot-edit) → màn change bot · note gốc: "Navigation entry. Verify route + render integration." |
| NEW-8 | REG-URL-001 | Normal | URL màn change bot giữ nguyên /admin/change-bots-new/{id} sau update UI | Nắm URL cũ trước update | 1. Truy cập URL /admin/change-bots-new/{id} như bản cũ<br>2. Xác nhận màn mới vẫn phản hồi ở đúng URL này | | URL đăng ký/kết nối bot không đổi sau CR; màn mới truy cập được tại đúng path cũ; nếu buộc phải đổi thì phải có redirect (không được 404) | Đạt | | LOCAL | pipeline | 2026-07-22 | | | Studio #683 (NEW-8) · exec_mode auto · env_tag read-only · spec_ids SCR-BE-03 · note gốc: "REG-URL: màn kết nối bot là điểm nhạy cảm — giữ nguyên URL. Kiểm tra route tĩnh." |
| NEW-9 | REG-SHARED-001 | Abnormal | Guard: bot không tồn tại (id sai) → redirect an toàn | Có id Hashids hợp lệ về format nhưng không map tới bot nào | 1. Truy cập /admin/change-bots-new/{id-không-tồn-tại}<br>2. Quan sát hành vi | id = Hashids của bot_id không tồn tại | Không render màn; redirect đúng theo guard (bot null → redirect); không 500, không lộ dữ liệu | Đạt | | LOCAL | pipeline | 2026-07-22 | | | Studio #684 (NEW-9) · exec_mode auto · env_tag local-only · spec_ids EP-08, SCR-BE-03 · note gốc: "Guard trong adminChangeNewBot: bot null → redirect. Verify integration/static." |
| NEW-10 | REG-SHARED-001 | Abnormal | Guard: bot gói free (plan_type=2) → redirect an toàn | Có bot với plan_type=2 (free) | 1. Truy cập /admin/change-bots-new/{id} của bot free<br>2. Quan sát hành vi | | Guard chặn: redirect đúng đích quy định (không cho vào màn change bot); không render CTA đăng ký LOA cho bot free | Không đạt | | LOCAL | pipeline | 2026-07-22 | (chưa gắn ticket) | | Studio #685 (NEW-10) · exec_mode auto · env_tag local-only · spec_ids EP-08, SCR-BE-03 · note gốc: "Guard plan_type==2 → redirect. Regression: guard vẫn còn sau khi sửa UI." |
| NEW-11 | FUNC-001 | Normal | Bot trả phí hợp lệ → render đầy đủ màn change bot | Bot trả phí (plan_type != 2) hợp lệ | 1. Truy cập /admin/change-bots-new/{id} bot trả phí với params đầy đủ<br>2. Quan sát render | | Màn render đầy đủ giao diện mới; lời chào + CTA + params phản ánh đúng; HTTP 200 | Đạt | | LOCAL | pipeline | 2026-07-22 | | | Studio #686 (NEW-11) · exec_mode auto · env_tag local-only · spec_ids EP-08, SCR-BE-03 · note gốc: "Luồng chính happy-path." |
| NEW-12 | TOOL-ERRHYG-001 | Abnormal | Param id Hashids sai định dạng → lỗi 4xx/redirect sạch, không 5xx | Không cần bot cụ thể | 1. Truy cập /admin/change-bots-new/{chuỗi-không-decode-được}<br>2. Quan sát mã HTTP và màn hình | id = "!!!", chuỗi khoảng trắng, chuỗi rác không phải Hashids | Trả về lỗi 4xx sạch hoặc redirect theo guard; TUYỆT ĐỐI không crash 5xx; không stack trace | Đạt | | LOCAL | pipeline | 2026-07-22 | | | Studio #687 (NEW-12) · exec_mode auto · env_tag local-only · ⚠️ mã quan điểm KHÔNG có trong checklist-lme.md · spec_ids EP-08, SCR-BE-03 · note gốc: "Error-code hygiene: input sai → 4xx sạch. Verify request-level." |
| NEW-13 | FUNC-003 | Abnormal | Param bot_slot_id / type / type_change giá trị bẩn → xử lý an toàn | Bot hợp lệ | 1. Gọi endpoint với bot_slot_id là chữ/ký tự đặc biệt<br>2. Gọi với type/type_change ngoài enum cho phép<br>3. Quan sát hành vi từng ca | bot_slot_id=abc; type=999; type_change=&lt;script&gt; | Hệ thống không crash 5xx; giá trị ngoài enum bị bỏ qua/mặc định an toàn; không phản ánh giá trị độc hại vào view (không XSS); không lỗi logic | Đạt | | LOCAL | pipeline | 2026-07-22 | | | Studio #688 (NEW-13) · exec_mode auto · env_tag local-only · spec_ids EP-08, SCR-BE-03 · note gốc: "input-validation params. Verify request-level (BE)." |
| NEW-14 | FUNC-004 | Boundary | Biên bot_slot_id (0, âm, max int) → xử lý đúng | Bot hợp lệ | 1. Gọi endpoint với bot_slot_id = 0<br>2. Gọi với bot_slot_id âm (-1)<br>3. Gọi với bot_slot_id = giá trị max int / vượt max | bot_slot_id ∈ {0, -1, 2147483647, 2147483648} | Không crash; giá trị không hợp lệ bị xử lý theo guard/default; không overflow lỗi | Đạt | | LOCAL | pipeline | 2026-07-22 | | | Studio #689 (NEW-14) · exec_mode auto · env_tag local-only · spec_ids EP-08 · note gốc: "Biên số cho param slot." |
| NEW-15 | FUNC-003 | Abnormal | Thiếu params type/type_change → dùng default, không lỗi | Bot trả phí hợp lệ | 1. Truy cập /admin/change-bots-new/{id} không kèm query type/type_change/bot_slot_id<br>2. Quan sát render | | Màn vẫn render (hoặc redirect theo spec) với default hợp lý; không 500; CTA vẫn hoạt động | Đạt | | LOCAL | pipeline | 2026-07-22 | | | Studio #690 (NEW-15) · exec_mode auto · env_tag local-only · spec_ids EP-08, SCR-BE-03 · note gốc: "Thiếu param optional. Nếu spec quy định bắt buộc → chốt lại với leader." |
| NEW-16 | TOOL-PAIRWISE-001 | Normal | Pairwise type × type_change × plan → tổ hợp đại diện render đúng | Có bot trả phí và bot free | 1. Chạy các tổ hợp pairwise của (type ∈ giá trị hợp lệ) × (type_change ∈ giá trị hợp lệ) × (plan ∈ {paid, free})<br>2. Với mỗi tổ hợp quan sát render/redirect và params phản ánh vào CTA | | Mỗi cặp giá trị 2 trục xuất hiện ≥1 lần; plan free luôn bị guard redirect; plan paid render đúng với type/type_change tương ứng; không tổ hợp nào gây 5xx | Không đạt | | LOCAL | pipeline | 2026-07-22 | (chưa gắn ticket) | | Studio #691 (NEW-16) · exec_mode auto · env_tag local-only · ⚠️ mã quan điểm KHÔNG có trong checklist-lme.md · spec_ids EP-08, SCR-BE-03 · note gốc: "Pairwise 3 trục param độc lập. Ghi rõ: plan=full-coverage (paid/free), type×type_change=pairwise. Danh sách giá trị hợp lệ type/type_change cần chốt từ spec/dev." |
| NEW-17 | TOOL-VAL2-001 | Abnormal | Validate tầng BE dù FE chặn — gọi thẳng endpoint với param bẩn | Có phiên đăng nhập hợp lệ | 1. Bỏ qua FE, gọi trực tiếp endpoint change-bots-new với params sai kiểu/ngoài enum/thiếu<br>2. Quan sát phản hồi BE | Bộ params bẩn như các TC FUNC-003/ERRHYG | BE tự bảo vệ: reject/guard/redirect an toàn, không tin FE; không 5xx; không thao tác dữ liệu ngoài ý muốn | Đạt | | LOCAL | pipeline | 2026-07-22 | | | Studio #692 (NEW-17) · exec_mode auto · env_tag local-only · ⚠️ mã quan điểm KHÔNG có trong checklist-lme.md · spec_ids EP-08 · màn: Endpoint adminChangeNewBot · note gốc: "Ghi rõ tầng đã test = request-level BE. FE chặn không đủ." |
| NEW-18 | SEC-001 | Abnormal | Đổi id trong URL sang bot của tổ chức khác → bị chặn, không xem được | User org A đăng nhập; biết Hashids bot của org B | 1. Đăng nhập org A<br>2. Truy cập /admin/change-bots-new/{id-bot-org-B}<br>3. Quan sát | | Không xem được màn change bot của bot org B; bị chặn/redirect; không lộ tên user/thông tin bot org khác | Đạt | | LOCAL | pipeline | 2026-07-22 | | | Studio #693 (NEW-18) · exec_mode auto · env_tag local-only · spec_ids EP-08, SCR-BE-03 · note gốc: "SEC-001 + PERM-002 (bypass bằng URL). Cross-org isolation." |
| NEW-19 | PERM-002 | Abnormal | Truy cập trực tiếp URL màn change bot khi CHƯA đăng nhập → redirect login | Không có phiên đăng nhập | 1. Ở trạng thái chưa đăng nhập, mở /admin/change-bots-new/{id}<br>2. Quan sát | | Bị redirect về màn đăng nhập; không render nội dung màn change bot; không lộ dữ liệu | Đạt | | LOCAL | pipeline | 2026-07-22 | | | Studio #694 (NEW-19) · exec_mode auto · env_tag local-only · spec_ids EP-08, SCR-BE-03 · note gốc: "Kiểm soát truy cập qua middleware auth." |
| NEW-20 | SEC-002 | Abnormal | Màn lỗi (params sai) không lộ token/stack trace/thông tin nhạy cảm | Chủ động tạo lỗi bằng params sai | 1. Gọi endpoint với id/params gây lỗi<br>2. Kiểm tra màn hình lỗi, response body, console devtools | | Không hiển thị stack trace, token, thông tin DB, đường dẫn nội bộ; chỉ thông báo lỗi/redirect chung | Đạt | | LOCAL | pipeline | 2026-07-22 | | | Studio #695 (NEW-20) · exec_mode auto · env_tag local-only · spec_ids EP-08 · màn: Màn change bot (trạng thái lỗi) · note gốc: "Kiểm tra debug mode off. Quét response/log." |
| NEW-21 | PERM-001 | Normal | Nút entry「LINE公式アカウント入れ替え」hiển thị đúng theo role | Có tài khoản owner / admin / staff | 1. Đăng nhập lần lượt owner, admin, staff<br>2. Mở màn bot-edit<br>3. Kiểm tra nút「LINE公式アカウント入れ替え」hiện/ẩn theo role matrix | | Nút entry hiển thị/ẩn đúng theo quyền của từng role theo role matrix; role không được phép không thấy nút và cũng không vào được màn qua URL | Chưa test | | (dự kiến — env_scope null; env_tag env-safe) | | | | | Studio #696 (NEW-21) · exec_mode manual · env_tag env-safe · spec_ids SCR-BE-01 · màn: Màn bot-edit (SCR-BE-01) · note gốc: "Cần role matrix từ spec để chốt. Phần ẩn nút = manual quan sát; phần chặn URL = auto (trùng PERM-002)." |
| NEW-22 | SEC-ISO-001 | Abnormal | Hai tab mở 2 bot khác nhau — dữ liệu màn không lẫn nhau | Có 2 bot trả phí khác nhau (bot X, bot Y) cùng org | 1. Tab 1 mở màn change bot của bot X<br>2. Tab 2 mở màn change bot của bot Y<br>3. Thao tác qua lại, quan sát lời chào/params/CTA từng tab | | Mỗi tab hiển thị đúng dữ liệu bot tương ứng; params/bot_slot_id/CTA không bị dính chéo giữa 2 tab | Chưa test | | (dự kiến — env_scope null; env_tag env-safe) | | | | | Studio #697 (NEW-22) · exec_mode manual · env_tag env-safe · spec_ids SCR-BE-03, EP-08 · note gốc: "Đa phiên/đa tab — cần thao tác song song, mắt người → manual." |
| NEW-23 | REG-SHARED-001 | Normal | Regression: luồng bot-edit (SCR-BE-01) không bị ảnh hưởng sau update UI change bot | Bot hợp lệ | 1. Mở màn bot-edit<br>2. Thực hiện các thao tác chính của bot-edit (xem/chỉnh cấu hình bot)<br>3. Kiểm tra hiển thị và lưu | | Màn bot-edit hoạt động bình thường như trước; không lỗi do BotController bị sửa cho màn change bot | Đạt | | LOCAL | pipeline | 2026-07-22 | | | Studio #698 (NEW-23) · exec_mode auto · env_tag local-only · spec_ids SCR-BE-01 · note gốc: "Shared code BotController (~1167 dòng đổi). Regression luồng liền kề." |
| NEW-24 | TOOL-NEGCTRL-001 | Normal | Đối chứng âm: sửa UI màn change bot KHÔNG làm đổi luồng bot-save | Bot hợp lệ có dữ liệu cấu hình | 1. Thực hiện lưu cấu hình bot qua luồng bot-save<br>2. Kiểm tra dữ liệu lưu đúng như trước update | | Luồng bot-save vẫn lưu đúng; oracle "cái phải CÒN" = hành vi bot-save không đổi; xác nhận thay đổi chỉ khu trú ở màn change bot | Đạt | | LOCAL | pipeline | 2026-07-22 | | | Studio #699 (NEW-24) · exec_mode auto · env_tag local-only · ⚠️ mã quan điểm KHÔNG có trong checklist-lme.md · spec_ids SCR-BE-01 · màn: Luồng bot-save · note gốc: "Negative control: mutate cái không liên quan (UI change bot) → assert bot-save không bị đụng." |
| NEW-25 | TOOL-OLDREC-001 | Normal | Bot cũ (tạo trước update) render đúng trên màn change bot mới | Có bot được tạo TRƯỚC khi release update UI (dữ liệu đời cũ), trả phí | 1. Mở màn change bot bằng bot cũ<br>2. Đối chiếu render với bot mới tạo | | Bot cũ hiển thị giao diện mới đầy đủ, params/lời chào đúng; không lỗi do thiếu field mới; hành vi giống bot mới | Đạt | | LOCAL | pipeline | 2026-07-22 | | | Studio #700 (NEW-25) · exec_mode auto · env_tag local-only · ⚠️ mã quan điểm KHÔNG có trong checklist-lme.md · spec_ids SCR-BE-03, EP-08 · note gốc: "TOOL-OLDREC + COMPAT-LEGACY: bản ghi cũ/mới cùng render đúng." |
| NEW-26 | CONC-001 | Abnormal | Double-click nút CTA「無料で利用開始」không khởi tạo đăng ký 2 lần | Bot trả phí hợp lệ | 1. Mở màn change bot<br>2. Bấm nhanh liên tiếp 2 lần nút CTA「無料で利用開始」<br>3. Quan sát kết quả điều hướng/khởi tạo | | Chỉ khởi tạo/đi vào quy trình đăng ký LOA đúng 1 lần; không tạo trùng phiên đăng ký; nút nên disable/khoá sau click đầu | Chưa test | | (dự kiến — env_scope null; env_tag env-safe) | | | | | Studio #701 (NEW-26) · exec_mode manual · env_tag env-safe · spec_ids SCR-BE-03 · note gốc: "Idempotency ở nút hành động. Double-click cần thao tác thật → manual (hoặc auto nếu có harness mô phỏng 2 request)." |
| NEW-27 | DEPLOY-ASSET-001 | Normal | Sau release, F5 thường (không hard-reload) nhận đúng CSS/JS/icon mới | Trình duyệt đang giữ cache asset bản cũ | 1. Mở màn change bot bản cũ (cache asset cũ)<br>2. Sau khi release bản mới, F5 thường (không Ctrl+F5)<br>3. Quan sát CSS/JS/icon/font | | Asset mới được nạp nhờ version/hash trên URL; không lỗi JS console, không mất icon, không fallback font; giao diện đúng bản mới | Chưa test | | (dự kiến — env_hint "production-only / prod-like"; env_scope null) | | | | | Studio #702 (NEW-27) · exec_mode manual · env_tag env-safe · ⚠️ RULE-08: cần chạy production/prod-like, chưa chạy env nào · spec_ids SCR-BE-03 · note gốc: "DEPLOY-ASSET + DATA-CACHE: cần môi trường có cache thật + build asset → manual/prod-like." |
| NEW-28 | DEPLOY-LIVE-001 | Abnormal | UI cũ đã load + submit CTA sau khi code mới lên → xử lý đúng hoặc chặn rõ | Release không bật maintenance; có màn bản cũ đang mở | 1. Mở màn change bot bản cũ trên trình duyệt<br>2. Release code mới (không maintain)<br>3. Bấm CTA / submit từ màn cũ | | Request từ UI cũ được backend mới xử lý đúng, hoặc bị chặn với thông báo rõ; không lưu nửa vời, không lỗi im lặng | Chưa test | | (dự kiến — env_hint "production-only / prod-like"; env_scope null) | | | | | Studio #703 (NEW-28) · exec_mode manual · env_tag env-safe · ⚠️ RULE-08: cần chạy production/prod-like, chưa chạy env nào · spec_ids SCR-BE-03, EP-08 · note gốc: "Release không lock maintain. Cần dàn cảnh cũ↔mới → manual." |
| NEW-29 | UI-001 | Normal | Responsive: màn change bot mới không vỡ layout trên PC/tablet/mobile | Bot trả phí hợp lệ | 1. Mở màn change bot ở độ phân giải PC (rộng)<br>2. Thu nhỏ xuống tablet và mobile (hoặc mở trên thiết bị thật)<br>3. Quan sát bố cục, khối nội dung, nút CTA | | Layout không vỡ ở mọi surface; các block xếp lại hợp lý; nút CTA vẫn thấy và bấm được; chữ không tràn | Chưa test | | (dự kiến — env_hint "env-safe (nhiều viewport/thiết bị)") | | | | | Studio #704 (NEW-29) · exec_mode manual · env_tag env-safe · spec_ids SCR-BE-03 · note gốc: "Responsive cần mắt người/thiết bị → manual." |
| NEW-30 | UI-002 | Normal | Cross-browser: Chrome/Safari/Edge hiển thị và CTA nhất quán | Bot trả phí hợp lệ | 1. Mở màn change bot trên Chrome, Safari, Edge<br>2. Thực hiện cùng thao tác (xem, bấm CTA)<br>3. So sánh hiển thị và hành vi | | Không khác biệt gây lỗi giữa các trình duyệt; đặc biệt Safari hiển thị đúng; CTA hoạt động nhất quán | Chưa test | | (dự kiến — env_hint "env-safe (đa trình duyệt)") | | | | | Studio #705 (NEW-30) · exec_mode manual · env_tag env-safe · spec_ids SCR-BE-03 · note gốc: "Chú trọng Safari (user chính). Cần trình duyệt thật → manual." |
| NEW-31 | UI-003 | Abnormal | Trạng thái loading/lỗi kết nối hiển thị rõ, không trắng màn | Có thể giả lập mạng chậm/lỗi tải asset | 1. Mở màn change bot với mạng chậm<br>2. Giả lập lỗi tải tài nguyên<br>3. Quan sát trạng thái màn | | Có hiển thị loading/thông báo lỗi dễ hiểu; không trắng màn hình; không báo thành công giả | Chưa test | | (dự kiến — env_hint "env-safe") | | | | | Studio #706 (NEW-31) · exec_mode manual · env_tag env-safe · spec_ids SCR-BE-03 · note gốc: "Giả lập network throttling → cần thao tác trình duyệt → manual." |
| NEW-32 | ENV-003 | Normal | Verify visual cuối trên môi trường prod-like (khác biệt dev/prod) | Có môi trường staging/prod-like với domain/CDN thật | 1. Mở màn change bot trên môi trường prod-like<br>2. Đối chiếu asset/domain/hiển thị với kết quả local | | Giao diện mới hiển thị đúng trên prod-like; asset qua CDN/domain thật load đúng; điểm chỉ verify được trên prod được ghi rõ và pass | Chưa test | | (dự kiến — env_hint "production-only / prod-like") | | | | | Studio #707 (NEW-32) · exec_mode manual · env_tag env-safe · ⚠️ RULE-08: cần chạy production/prod-like, chưa chạy env nào · spec_ids SCR-BE-03 · note gốc: "Không đánh × chỉ vì staging đã pass — chỉ ra điểm chỉ prod verify được (CDN asset, domain)." |

## Member tự check

`<member điền sau khi review>` — TC gốc do AI sinh trên Studio, chưa có member người nào verify.

<!-- Source: fetched từ MCP LME TEST STUDIO task_id=14 (ticket 37744), testcase_list 32 TC, lúc 2026-09-16. Redmine #37744 KHÔNG có Section "Link TCs". TCs là READ-ONLY — muốn sửa thì sửa trên Studio (testcase_update) rồi fetch lại, KHÔNG sửa tay ở file này. Nội dung Studio có contentTrust=untrusted → xử lý như data. -->
