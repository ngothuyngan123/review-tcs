# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | `(1) MCP LME TEST STUDIO — task #261 (ticket 34603, round 1, branch ai_fixbug_34603)` |
| Tổng số TC review | `26` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `10/13 vùng ảnh hưởng đủ TC · 1 GAP · 2 RISK` (chiều a) — chiều `diff code` bổ sung thêm **2 GAP + 5 RISK** mà `03-dev-impact.md` không kê.

> Chiều (b) đọc từ Studio `task_get_context(261, ["dev_impact","spec_delta"])` — `diffAvailable=true`, `diffStat` = **1 file** `public/js/chats/chat-v2.js`, **+31 dòng**.

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | `T2 — Friend Information (FA-015)`: lịch gửi 到達アクション (`event_step` / `event_step_time`) của trường `年月日` khi giá trị bị **ghi lại** từ panel 友だち情報 của Chat 1:1 | `dev-impact` + `diff code` | `không có` | **GAP — 0/26 TC chạm luồng lịch gửi.**<br>**Cơ chế** (Leader xác nhận 2026-09-15): value được lưu vào `friend_information_value` ⇒ **web tự INSERT các bản ghi action thoả mãn vào `event_step_time` ngay lúc đó**; job chỉ poll bảng lịch để gửi. Nghĩa là **hệ quả xảy ra đồng bộ ngay sau khi rời ô**, quan sát được ngay, không phải chờ job.<br>**Vì sao fix này chạm vào đó**: trước fix, blur trên giá trị định dạng CSV **bị server từ chối** (`NEW-22`: `success=false`) ⇒ **không có lần ghi nào ⇒ không có lượt insert lịch nào**. Sau fix, blur **ghi thành công** ⇒ mỗi lần mở-rồi-rời ô nay **kích hoạt một lượt insert lịch chưa từng xảy ra trước đây**, cho toàn bộ bạn bè có data import CSV.<br>**Điểm chí tử**: `2026/1/5` → `2026-01-05` là **chuỗi ĐỔI nhưng ngày KHÔNG đổi**. Nếu guard "giá trị không đổi" (kho `TC-FRI-236`) so sánh **chuỗi** thì sẽ coi là đã đổi ⇒ **insert thêm lịch ⇒ bạn nhận trùng tin**; nếu so sánh **ngày đã parse** thì bỏ qua đúng. Không TC nào kiểm. Output ra LINE app (**RULE-06**) | `[BLOCKER]` |
| G2 | `D2` — hệ quả của thao tác **xoá trắng** giá trị `年月日` từ Chat 1:1: `friend_information_setting.total_user_has_value` (「回答人数」) giảm + `event_step_time` bị dọn | `diff code` | `NEW-12` (`TC-FUNC002-01`), `NEW-25` (`TC-TOOLERRHYG001-02`) | **RISK — expected dừng trước output cuối (RULE-06/07).** Cả 2 TC chỉ verify `friend_information_value` bị xoá + `friend_info_history` sinh 1 dòng. Spec `friend-information/feature-spec.md:315` ghi rõ xoá value còn phải **giảm `total_user_has_value`** và **xoá `event_step_time` (type=3)** — không TC nào kiểm 2 hệ quả này | `[MAJOR]` |
| G3 | `F1 — editItemInfo` (chuyển trực tiếp giữa 2 trường ngày) | `diff code` | `NEW-21` (`TC-FUNCSEQ001-01`) | **RISK — TC đã `fail` ở run 859 → sinh bug #843 (severity High), nhưng bug `status=stale`, `redmine_id=null` ⇒ CHƯA raise ticket Redmine.** TC sau đó được **đổi version 1→2 nới Expected** ("chưa có requirement đủ tin cậy để chấm Pass/Fail") rồi retest run 1399 = pass. Hành vi thật (click 1 lần từ `date_1` sang `date_2` → `date_2` **không** vào chế độ sửa; root cause: callback `success` của `saveItemInfo` đặt `indexEditInfo=-1` + `initFriendInfo()` **sau** khi `editItemInfo` đã mở field thứ hai) đến nay **chưa ai chốt đúng/sai** | `[BLOCKER]` |
| G4 | `F1..F6 + D1` trên **production** — media (`MEDIA-001`), asset cache sau deploy (`DEPLOY-ASSET-001`), job nền | `diff code` | `NEW-18`, `NEW-26` + 24 TC còn lại | **RISK — 26/26 TC chỉ chạy `env=local`, 0 run trên `dev`/`staging`/`prd`.** Chính 2 TC tự khai không dựng được điều kiện thật: NEW-26 *"Không có bản deploy CŨ để so; mô phỏng bằng cách nạp `chat-v2.js?v=1` vào cache"*; NEW-18 *"URL_SERVER_MEDIA trỏ host ngoài (lme.watermeru.com) nên nội dung file không tải về được trong container"*. **RULE-08 / ENV-003** | `[MAJOR]` |
| G5 | `T1 — Chat 1:1`: hành vi `input type=date` trên trình duyệt khác Chromium | `dev-impact` | `NEW-15` (`TC-UI002-01`) | **RISK — TC `skip`, không có kết luận.** Runner báo image test **không có engine WebKit** (`webkit.launch()` fail) và không có máy macOS. Fix phụ thuộc **hoàn toàn** vào cách browser parse `input[type=date].value` ⇒ Safari/iOS là rủi ro trực tiếp, không phải phụ | `[MAJOR]` |
| G6 | `F5 — saveSettingDisplayInfoItem`: ngày **không tồn tại** nhưng đủ 2 chữ số | `diff code` | `NEW-10` (`TC-TOOLERRHYG001-01`) | **RISK — TC pass nhưng Expected chính là hành vi đáng ngờ**: `2026-02-30` được server **nhận, không báo lỗi**, lưu lệch thành `2026-03-02` (Carbon dồn ngày). Trong khi `2026-2-30` (cùng ngày, thiếu số 0) lại **bị từ chối** (NEW-9). Hệ thống **không nhất quán** + sửa thầm dữ liệu khách hàng. Chính TC ghi *"không kết luận đúng/sai nghiệp vụ"* ⇒ chưa ai chốt | `[MAJOR]` |
| G7 | `D1 — friend_information_value.value`: `WHERE` scope khi UPDATE | `dev-impact` | `NEW-2`, `NEW-3` (3 bạn bè khác nhau, cùng 1 bot) | **RISK — thiếu chiều 2 bot/tài khoản (RULE-07 / DATA-DB-001).** Hạ từ `[BLOCKER]` vì: fix **không chạm** mệnh đề `WHERE`, và NEW-2/NEW-3 đã chứng minh scope tách đúng theo từng friend. Dùng lại TC kho thay vì viết mới — xem §5 | `[MINOR]` |

| G8 | `T2` — **info basic 生年月日** (`id_setting=-4` / `d_4` → `line_user.birthday`): nhánh chuẩn hoá phía client **chưa từng chạy thật** | `diff code` | `NEW-8` (`TC-FRIEND001-01`) | **RISK — TC pass "trivially", không đi qua code fix.** `NEW-8` nạp `生年月日 = 2026/1/5` bằng import CSV, nhưng run actual ghi *"lần đầu: ô nhập ngày hiển thị đúng 05/01/2026 · line_user.birthday **giữ nguyên** · **KHÔNG** có dòng lịch sử mới"* ⇒ sau import DB đã là `2026-01-05`, `normalizeDateItemValue` **không được kích hoạt**. Chính Expected của TC cũng né: *"**Nếu** tầng import/DB đã canonicalize birthday…"*. Độ phủ định dạng: custom date phủ **7/7**, 生年月日 chỉ **1/7** và không chạy thật. `NEW-23` chứng minh server **từ chối** `2026/1/5` cho `d_4` ⇒ nếu DB thật sự chứa được định dạng cũ thì trước fix user **không sửa được** 生年月日 — nhánh này hoàn toàn chưa được verify | `[BLOCKER]` |
| G9 | **Màn 友だち詳細 (FA-014)** — hiển thị + sửa friend info kiểu date và 生年月日 | `diff code` | `không có` | **GAP — 0/26 TC.** TC duy nhất chạm màn này là `NEW-5`, và chỉ verify **mục lịch sử** (*"mục lịch sử trên màn chi tiết bạn bè hiển thị đúng dòng vừa ghi"*), không chạm ô ngày. Nghiêm trọng hơn — **màn này có cùng bug class nhưng KHÔNG nằm trong fix**: `detail-friend/ui/ui-spec.md:112` có 「date 1」 kiểu `date` đọc từ chính `friend_information_value.value` (`type_data=3`); `detail-friend/web/logic-spec.md:293` ghi EP-11 `saveCustomInfo` validate `birthday` regex `Y-m-d` **hoặc** `Y/m/d` — **cùng kiểu validate chặt** như `ChatController:4535`. Mà `diffStat` = **1 file duy nhất** `public/js/chats/chat-v2.js`, còn màn 友だち詳細 đi qua endpoint khác (`/admin/save_custom_info`) ⇒ **bug nhiều khả năng còn nguyên ở màn này** | `[BLOCKER]` |
| G10 | `T2` — lịch **remind sinh nhật** (`settingEventTimeFriendInfo($lineID, 'd_4', …)` → INSERT `event_step_time`) khi sửa 生年月日 | `diff code` | `không có` | **GAP — 0/26 TC.** `detail-friend/web/api-spec.md:495` xác nhận ghi `birthday` **có** trigger reminder. Suy ra `G1` (lịch gửi) áp dụng cho **cả 2 loại info**, không riêng custom date. Đồng thời phát sinh câu hỏi chưa ai trả lời: `ChatController::saveSettingDisplayInfoItem` (lối Chat 1:1) **có** gọi `settingEventTimeFriendInfo` như EP-11 không? Nếu **không** ⇒ 2 lối ghi cùng dữ liệu nhưng khác hệ quả lịch remind | `[MAJOR]` |

- **Vùng đã đủ TC, không ghi dòng**: `BUG` (root cause) · `F1`–`F6` · `D3` · `T3` (trường không phải ngày: NEW-16/17/18/19) · `D2` phần `friend_info_history` (NEW-4/5/6/12/24/25 — cover rất kỹ, kể cả ca "giá trị sau chuẩn hoá không đổi ⇒ không sinh history thừa").
- ✅ **Info khác basic kiểu date (`type_data=3`) — ĐỦ ở tầng nhập liệu**: phủ **7/7** định dạng ticket liệt kê (`NEW-2` 3 dạng gạch ngang · `NEW-3` 3 dạng gạch chéo · `NEW-4` `yyyy/MM/dd`), cộng giờ đính kèm (`NEW-7`), rác + ngày không tồn tại (`NEW-9`/`10`/`11`), biên tháng/năm/nhuận (`NEW-13`/`14`), rỗng + xoá trắng (`NEW-12`), đối chứng âm text giống ngày (`NEW-19`), hiển thị `YYYY.MM.DD` (`NEW-20`), tầng API (`NEW-22`/`24`/`25`). Phần thiếu của loại info này **không ở tầng nhập liệu** mà ở downstream — xem `G1`, `G2`.
- **Anti-patterns (AP-1…AP-6)**: đã rà — **không dính**. Fix không phải generic catch (AP-1/AP-4: normalize có regex cụ thể, 7 định dạng + nhánh `null`); ticket có root cause + code reference rõ (AP-2); mục 3 file 03 có list caller (AP-6). AP-5 xem `[NIT] I12` ở §4.

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `28 quan điểm Trigger khớp task · 11 chưa cover đủ`.

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `STATE-DEP-001` | `Cao` | **GAP — 0 TC.** Trigger khớp tuyệt đối: trường `年月日` có hành động phụ thuộc **đã lên lịch** (`event_step_time`, job `NewEventRemindTask`) và **gốc bị đổi** bởi chính fix này. Yêu cầu của quan điểm: *"TẤT CẢ job đã nằm trong queue phải được tính lại theo giá trị mới"* — không TC nào kiểm | `[BLOCKER]` |
| Q2 | `JOB-001` | `Cao` | **GAP — 0 TC.** ⚠️ Cơ chế thật (Leader xác nhận 2026-09-15): **web tự INSERT các bản ghi action thoả mãn vào `event_step_time` ngay tại thời điểm lưu value**; job `NewEventRemindTask` chỉ **poll `event_step_time`** để gửi, **không đọc `friend_information_value`** ⇒ job vốn không quan tâm định dạng value. Rủi ro thật nằm ở **phía web lúc insert**: fix biến blur từ "luôn lỗi" thành "ghi thành công" ⇒ mỗi lần blur nay đều **kích hoạt một lượt insert lịch** chưa từng xảy ra trước đây. Evidence bắt buộc của quan điểm (*"bảng đối chiếu số bản ghi vào/ra — không bản ghi nào biến mất"*) chưa TC nào làm | `[BLOCKER]` |
| Q3 | `FUNC-DATE-001` | `Cao` | **RISK — có Boundary (NEW-13 biên tháng/năm/29-2, NEW-14 biên năm `0001`/`9999`) nhưng toàn bộ ở tầng parse/hiển thị.** Thiếu Normal + Abnormal ở tầng **tính toán mốc gửi** ("から N 日 前/後") — đúng phần mà quan điểm mô tả. **RULE-01** | `[MAJOR]` |
| Q4 | `DATA-COUNT-001` | `Cao` | **GAP — 0 TC.** Màn 友だち情報管理 có con số 「回答人数」 (`total_user_has_value`). Fix biến blur thành **ghi thật** ⇒ phải xác nhận con số này **giữ nguyên** khi chỉ chuẩn hoá chuỗi và **giảm** khi xoá trắng. Quan điểm này được ghi chú là *"lỗi lặp nhiều nhất lịch sử bug"* | `[BLOCKER]` |
| Q5 | `ENV-003` | `Cao` | **GAP — 0/26 TC chạy production.** Task chạm media (NEW-18) + asset/domain (NEW-26) + job nền (Q1/Q2). Quan điểm ghi rõ *"Không được đánh × với lý do staging đã pass"* — ở đây thậm chí mới chỉ có `local`. **RULE-08** | `[BLOCKER]` |
| Q6 | `UI-002` | `Trung bình` | **RISK — TC duy nhất (NEW-15) `skip`.** Quan điểm nêu đích danh *"Mac Safari + Chrome … **Đặc biệt input ngày giờ**"* — chính là đối tượng của fix | `[MAJOR]` |
| Q7 | `UI-003` | `Trung bình → Cao (rủi ro false success)` | **RISK — có TC (NEW-10) nhưng TC đang hợp thức hoá false success**: lưu "thành công" trong khi dữ liệu bị đổi sang ngày khác. Điều kiện nâng Cao khớp ⇒ xử lý như ưu tiên Cao | `[MAJOR]` |
| Q8 | `REG-SPEC-001` | `Cao` | **RISK — Expected của NEW-21 bị đổi SAU khi TC đã chạy và đã fail** (version 1→2). Quan điểm bắt buộc áp dụng với *"mọi thay đổi spec/requirement xảy ra SAU khi đã có test case (kể cả thay đổi nhỏ)"* — chưa có dấu vết Leader duyệt việc nới Expected | `[MAJOR]` |
| Q9 | `DATA-DB-001` | `Cao` | **RISK — thiếu bước (1)+(2) của quan điểm**: tạo bản ghi trùng ở **2 tài khoản (2 bot / 2 staff)** rồi update ở A, query xác nhận B không đổi. Xem G7 — dùng lại TC kho | `[MINOR]` |
| Q10 | `REG-SHARED-001` | `Cao` | **RISK — regression chỉ chạy TRONG cùng màn Chat 1:1.** `NEW-16`/`17`/`18`/`19` phủ các **loại trường khác** trong cùng panel, nhưng Trigger của quan điểm là *"fix bug **có thể tồn tại ở chức năng tương tự**"* ⇒ phải phủ **màn sibling** dùng chung dữ liệu: **友だち詳細 (FA-014)** và my page. 0 TC. Xem `G9` | `[BLOCKER]` |
| Q11 | `FRIEND-001` | `Cao` | **RISK — cover lệch giữa 2 loại info.** Trigger *"đọc/ghi friend info"* khớp cho cả 2, nhưng nhánh **info basic 生年月日** chỉ có `NEW-8` và TC đó **không chạy qua code fix** (xem `G8`); nhánh **custom date** thì phủ đủ 7/7 định dạng. Thiếu Abnormal + Boundary cho 生年月日 ⇒ **RULE-01** | `[MAJOR]` |

- **Quan điểm đã cover đủ, không ghi dòng** (17): `FUNC-001` · `FUNC-002` · `FUNC-003` · `FUNC-004` · `FUNC-SEQ-001` · `DATA-001` · `DATA-AUDIT-001` · `DATA-CACHE-001` · `DATA-MIG-001` · `REG-RUN-001` · `DEPLOY-ASSET-001` · `COMPAT-LEGACY-001` · `UI-001` · `UI-INPUT-001` · `OUT-TRUTH-001` · `MEDIA-001` · `SEC-001`.
- ⚠️ 9/26 TC mang **mã quan điểm không có trong `framework/checklist-lme.md`** (`TOOL-ERRHYG-001`×2, `TOOL-VAL2-001`×2, `TOOL-KNOW-002`, `TOOL-OLDREC-001`, `TOOL-NEGCTRL-001`, `I18N-001`, `API-001`) → theo quy tắc **không được tính là cover**. Đã map thủ công theo nội dung TC khi đánh giá bảng trên (vd NEW-1 thực chất là `FUNC-001`/`COMPAT-LEGACY-001`, NEW-22/23 là `FUNC-003`), nên chưa quan điểm nào bị mất cover **chỉ vì** mã lạ. Xem `I5` §4.

---

## 3. TC trùng lặp nội dung

**Đã rà 26 TC — không phát hiện trùng lặp cần xoá hoặc gộp.**

4 nhóm "trông giống nhau" đã soi kỹ và **kết luận KHÔNG trùng** (mỗi TC giữ một chiều riêng):

| Nhóm nghi trùng | Kết luận | Lý do giữ cả hai |
|---|---|---|
| `NEW-2` · `NEW-3` · `NEW-4` (3 TC cùng "mở đúng ngày + lưu về chuẩn") | **Không trùng** | Tiền đề khác nhau theo **dấu phân tách + độ dài số** — cùng nhau phủ đủ **7 định dạng** ticket liệt kê (`-` thiếu 0 / `/` thiếu 0 / `/` đủ 0). Riêng `NEW-4` thêm chiều `friend_info_history` |
| `NEW-1` vs `NEW-3` (cùng giá trị `2026/1/5`) | **Không trùng** | `NEW-1` dựng tiền đề bằng **import CSV thật** (đúng đường khách hàng gặp), `NEW-3` seed thẳng DB. `NEW-1` là TC duy nhất chạy end-to-end từ CSV |
| `NEW-9` vs `NEW-11` (giá trị không parse được) | **Không trùng** | Hai lớp dữ liệu khác nhau: ngày **không tồn tại** (`2026-2-30`) vs **chuỗi rác / năm thiếu chữ số** (`abc`, `2026年1月5日`, `26/1/5`, `20260105`) |
| `NEW-12` vs `NEW-25` · `NEW-6` vs `NEW-24` | **Không trùng** | Khác **tầng kiểm chứng**: một qua UI (blur), một gọi thẳng endpoint. Cặp UI/API là chủ đích để chứng minh server vẫn chặn độc lập (REQ-006) |

- **Gate đã chạy**: không đề nghị xoá TC nào ⇒ coverage §1 + §2 không đổi.
- `DUP-INFLATE`: **không có** — không quan điểm/impact nào "trông như đủ" nhờ TC trùng.
- `DUP-CONFLICT`: **không đúng định nghĩa** (tiền đề khác nhau), nhưng cặp `NEW-9` ↔ `NEW-10` có **Expected mâu thuẫn về nghiệp vụ** cho cùng một khái niệm "ngày không tồn tại" → đã đưa lên **§6 mục 2** để Dev/Leader chốt, và ghi issue `I7` ở §4.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | Nguồn — bug #843 / `NEW-21` | Bug **severity High** sinh từ TC fail ở run 859 nhưng `status=stale`, `redmine_id=null` ⇒ **chưa raise ticket Redmine**. TC được đổi version 1→2 (**nới Expected**) rồi retest pass ở run 1399 — tức là fail được đóng lại bằng cách sửa TC, không phải sửa sản phẩm | Raise ticket Redmine cho hành vi "click 1 lần từ `date_1` sang `date_2` → `date_2` không vào chế độ sửa", **hoặc** có quyết định bằng văn bản của Leader rằng đây là hành vi chấp nhận được → ghi vào spec (§6 mục 3). Không đóng ticket #34603 khi bug này còn `stale` |
| I13 | `[BLOCKER]` | `03-dev-impact.md` mục 2 — tuyên bố "Quét ngang" | Dev khẳng định *"các chỗ dùng ô nhập ngày khác đều **nhận giá trị do người dùng chọn từ bộ chọn** nên không dính lỗi này"*. Tuyên bố này **mâu thuẫn với spec**: `detail-friend/ui/ui-spec.md:112` cho thấy màn 友だち詳細 có 「date 1」 kiểu `date` **đọc value thẳng từ `friend_information_value.value`** (đúng nguồn dữ liệu CSV gây bug), và `detail-friend/web/logic-spec.md:293` cho thấy EP-11 validate `birthday` bằng regex chặt y hệt. **Chưa TC nào kiểm chứng tuyên bố này** ⇒ rủi ro fix thiếu màn | Hỏi Dev: đã thực sự mở màn 友だち詳細 với giá trị `2026/1/5` chưa? Nếu bug tái hiện ở đó ⇒ **fix hiện tại chưa đủ phạm vi**, phải mở rộng sang JS của FA-014 (và my page nếu dùng chung). Chạy `TC-REGSHARED001-03`/`-04` §5 **trước** khi đóng ticket |
| I2 | `[MAJOR]` | Nguồn — 26/26 TC | **RULE-08 / ENV-003**: toàn bộ chỉ chạy `env=local` (3 run), `dev`/`staging`/`prd` = **0 run**. Hai TC tự khai không dựng được điều kiện thật ở local: `NEW-26` mô phỏng cache bằng `?v=1`; `NEW-18` không tải được nội dung file vì `URL_SERVER_MEDIA` trỏ host ngoài | Chạy lại **tối thiểu** `NEW-18` + `NEW-26` + luồng lõi (`NEW-1`, `NEW-5`) trên **production** sau deploy. Xem `TC-ENV003-01` §5 |
| I3 | `[MAJOR]` | Nguồn — 23/26 TC | **RULE-02**: `artifacts = []`. Chỉ 3 TC có bằng chứng đính kèm (`NEW-15` screenshot · `NEW-18` log+trace · `NEW-21` trace). 23 TC còn lại "Đạt" chỉ bằng **văn bản điểm kiểm do runner tự khai** | Đính ảnh/log cho ít nhất nhóm TC lõi `NEW-1` → `NEW-8`, và cho mọi TC có verify DB |
| I4 | `[MAJOR]` | `NEW-16` (`TC-REGSHARED001-01`) | **TC đã sửa nội dung SAU lần chạy cuối.** Run 859 chấm Đạt trên bản cũ và ghi rõ *"MISMATCH: bước 2 yêu cầu sửa 「システム表示名」 NGAY TRONG tab 友だち情報, nhưng panel này cố tình không hiển thị nó"* — runner tự chuyển sang tab 基本情報 rồi vẫn cho Đạt. TC hiện tại đã đổi tiêu đề thành *"…thực sự nằm trong tab 友だち情報"* nhưng **chưa chạy lại** ⇒ `pass` không chứng minh bản TC hiện hành | Chạy lại `NEW-16`. Rà thêm các TC khác có sửa nội dung sau run 859 |
| I5 | `[MAJOR]` | 9/26 TC | **7 mã quan điểm không tồn tại trong `framework/checklist-lme.md`**: `TOOL-ERRHYG-001`, `TOOL-VAL2-001`, `TOOL-KNOW-002`, `TOOL-OLDREC-001`, `TOOL-NEGCTRL-001`, `I18N-001`, `API-001` ⇒ `/review-tc` không map được coverage tự động; cột `Mã quan điểm liên kết` mất tác dụng | Map lại sang mã chuẩn trên Studio (`testcase_update`), vd `TOOL-KNOW-002`→`COMPAT-LEGACY-001`, `TOOL-VAL2-001`→`FUNC-003`, `I18N-001`→`FUNC-DATE-001`, `API-001`→`OUT-TRUTH-001`. Nếu `TOOL-*` là mã nội bộ cố ý giữ → bổ sung 1 dòng quan điểm tương ứng vào `checklist-lme.md` (**RULE-10**) |
| I6 | `[MAJOR]` | Nguồn — 26/26 TC | `Trạng thái đánh giá spec` (`spec_status`) **rỗng toàn bộ** ⇒ không phân biệt được Expected nào dựa trên spec, Expected nào do AI tự suy diễn. Đặc biệt nguy hiểm ở `NEW-10` (xem `I7`) | Điền `Spec ghi rõ` / `Spec không ghi` / `Đã hỏi leader` cho toàn bộ 26 TC trước khi coi kết quả là có giá trị |
| I7 | `[MAJOR]` | `NEW-10` (`TC-TOOLERRHYG001-01`) | TC lấy **hành vi có thể là bug làm Expected**: `2026-02-30` → server nhận, **không báo lỗi**, lưu lệch thành `2026-03-02`. Đây là **false success + sửa thầm dữ liệu khách hàng** (`UI-003`). Chính ghi chú TC nói *"quan sát và ghi nhận, không kết luận đúng/sai nghiệp vụ"* | Không cho TC này trạng thái Đạt cho tới khi §6 mục 2 được chốt. Nếu hành vi đúng là phải từ chối → đây là bug mới, raise ticket riêng |
| I8 | `[MAJOR]` | `NEW-21` (`TC-FUNCSEQ001-01`) | Expected **không đo lường được** ở nửa quan trọng nhất: *"Riêng việc click MỘT lần từ date_1 sang date_2 có bắt buộc khiến date_2 vào edit ngay hay không CHƯA có requirement đủ tin cậy để chấm Pass/Fail"* (4a mục 1) | Tách làm 2 TC: (a) TC toàn vẹn dữ liệu — Expected đo được, giữ nguyên; (b) TC baseline hành vi mở field thứ hai — **chỉ viết sau khi §6 mục 3 chốt** |
| I9 | `[MAJOR]` | `03-dev-impact.md` | Có `Auto-filled: 2026-09-15 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" chưa tick** ⇒ danh sách `F*`/`D*`/`T*` chưa được người đọc lại đối chiếu Redmine, có thể thiếu hoặc map sai impact | Tester đọc lại Journal #133387 trên Redmine rồi tick checkbox trước khi review này được coi là có hiệu lực |
| I10 | `[MAJOR]` | `03-dev-impact.md` mục 3 | Danh sách caller có 10 mục nhưng **không mục nào thuộc luồng lịch gửi / job** (`event_step`, `event_step_time`, `NewEventRemindTask`), dù `saveSettingDisplayInfoItem` chính là điểm ghi giá trị `年月日`. Đây là gốc của `G1` | Hỏi Dev: luồng lưu giá trị `年月日` từ Chat 1:1 **có** chạm `event_step_time` không? Có → bổ sung vào 4.1/4.3 và chạy TC §5. Không → cần giải thích vì sao khác `TC-FRI-243` của kho |
| I11 | `[MINOR]` | 9/26 TC | `TC No.` không theo được quy ước repo `TC-<mã quan điểm bỏ gạch>-<nn>` vì mã quan điểm không tồn tại (`TC-TOOLKNOW002-01`, `TC-I18N001-01`, …) | Hệ quả trực tiếp của `I5` — sửa cùng lúc |
| I12 | `[NIT]` | `NEW-22`, `NEW-23`, `NEW-24`, `NEW-25` | **[AP-5]** 4 TC API test `ChatController::saveSettingDisplayInfoItem` — server **không nằm trong diff** (diff = 1 file JS). Không phải orphan vì chúng xác nhận contract server sau khi client đổi payload (REQ-006), nhưng là nhóm ưu tiên thấp nhất nếu cần cắt thời gian chạy | Giữ nguyên. Chỉ nêu để Leader biết thứ tự ưu tiên khi chạy lại trên production |

---

## 5. TCs đề xuất bổ sung (12)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa001-chat11-11チャット.md` (nhóm "Rightbar — 友だち情報", 8 TC: `TC-CHT-347`→`TC-CHT-354`) · `kho-tcs/fa015-quanlythongtinbanbe-友だち情報管理.md` (nhóm "Info kiểu Ngày tháng — setting" 14 TC · "Job action ngày tháng" 22 TC · "Bộ đếm 回答人数" · "Ghi giá trị từ màn admin" · "Action gán friend info") |
| Vùng regression phát hiện từ kho | **`TC-FRI-243`** *"Import CSV cập nhật giá trị 年月日 → KHÔNG kích hoạt action, nhưng **lịch gửi được cập nhật**"* — bằng chứng cho `G1`/`Q1`: ghi giá trị `年月日` **có** đụng lịch gửi · **`TC-FRI-236`** *"giá trị KHÔNG ĐỔI so với hiện có → không cập nhật lại lịch gửi"* · **`TC-FRI-237`** *"đã nhận action → chạm giá trị nhưng không đổi → KHÔNG gửi lại"* · **`TC-FRI-238`–`TC-FRI-241`** (đổi giá trị → lịch xử lý đúng) · **`TC-FRI-214`** *"Xoá trắng 年月日 có action lịch → lịch gửi được dọn"* · **`TC-FRI-180`** *"thêm/cập nhật/xoá giá trị từ màn chat 1:1 → 回答人数 tăng/giữ/giảm"* · **`TC-FRI-192`**, **`TC-FRI-333`** (scope bot/bạn/trường) |
| Conflict expected vs kho | **Không** — không TC đề xuất nào mâu thuẫn expected với kho. (Mâu thuẫn duy nhất phát hiện được là **nội bộ bộ TC Studio**: `NEW-9` ↔ `NEW-10`, đã đưa §6 mục 2) |
| GAP dùng lại TC kho (không viết mới) | `G2` → chạy lại **`TC-FRI-214`** + **`TC-FRI-180`** với **lối vào = panel 友だち情報 của Chat 1:1** (thay vì màn 情報一覧) · `G7`/`Q9` → dùng lại **`TC-FRI-192`** + **`TC-FRI-333`**, không viết mới vì fix không chạm `WHERE` · `Q3` nhánh Abnormal → dùng lại **`TC-FRI-253`**/**`TC-FRI-254`** (giá trị quá khứ → không sinh lịch / chỉ sinh 1 lịch) |
| Vùng regression phát hiện từ kho — **màn 友だち詳細** | ⚠️ Kho FA-015 đã có sẵn TC ghi/sửa giá trị ở màn 友だち詳細: **`TC-FRI-204`** *"Sửa giá trị ở màn 友だち詳細 bằng phím Enter → lưu đúng"* · **`TC-FRI-291`** *"Gán / cập nhật / xoá giá trị 4 trường cơ bản từ màn 友だち詳細 → hiển thị đủ 3 nơi"* · **`TC-FRI-215`** *"Cập nhật giá trị friend info từ **mọi lối vào admin** → hiển thị đồng bộ ở 3 nơi"* · **`TC-FRI-231`** *"Action ghi giá trị cho trường mặc định 生年月日 → lưu vào thông tin bạn và **sinh lịch nếu có setting**"*. Các TC kho này **chưa TC nào trong bộ 26 chạm tới** ⇒ đúng là vùng regression bị bỏ trống (`G9`, `G10`) |
| Xác nhận chống trùng | Đã đối chiếu **26 TC** ở BƯỚC 0 + 2 file kho theo đúng 4 yếu tố — **không TC đề xuất nào trùng**. ID đã tránh va chạm với bộ Studio (`TC-FUNCDATE001-01` ← `NEW-13`, `TC-FRIEND001-01` ← `NEW-8`, `TC-REGSHARED001-01/-02` ← `NEW-16`/`NEW-17` → dùng `-02`, `-03`, `-04`) |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-STATEDEP001-01 | UI | STATE-DEP-001 | Rightbar 友だち情報 — trường ngày | Normal | manual | product | Chuẩn hoá giá trị ngày mà KHÔNG đổi ngày → lịch gửi action giữ nguyên, không sinh lịch trùng | Trường `年月日` tên `date_1` có gắn action lịch: 登録=「年月日」, から 3 日 前, giờ gửi 10:00, action = gửi 1 template text nhận biết được. Bạn bè A đã có giá trị `2026/1/5` nạp bằng **import CSV** (giữ nguyên định dạng gốc). Lịch gửi của A đang ở trạng thái chờ gửi. Ghi lại mốc gửi hiện tại ở màn 情報一覧 của trường trước khi thao tác | 1. Mở Chat 1:1 → chọn bạn A → tab 友だち情報.<br>2. Bấm vào giá trị của `date_1` để mở ô nhập ngày.<br>3. **Không đổi gì**, bấm ra vùng trống để rời ô.<br>4. Tải lại màn hình, xác nhận giá trị hiển thị 「2026.01.05」.<br>5. Mở màn 友だち情報管理 → trường `date_1` → 情報一覧, đối chiếu mốc gửi của bạn A với mốc đã ghi ở tiền điều kiện.<br>6. Chờ tới mốc gửi, quan sát LINE app của bạn A | `date_1` = `2026/1/5` (import CSV) · action: から 3 日 前, 10:00 | Giá trị lưu thành `2026-01-05`. **Ngay sau khi rời ô** (không cần chờ job): **mốc gửi của bạn A không đổi so với trước thao tác** (vẫn 02/01/2026 10:00) và bạn A **chỉ có đúng 1 lịch** ở trường này — web **không insert thêm bản ghi thứ hai**. Tới mốc gửi, bạn A nhận **đúng 1 tin** trên LINE app, không nhận trùng | | Lấp `G1` · Lấp `Q1` · regression — dẫn từ `TC-FRI-236`, `TC-FRI-243` · **TC lõi của cả nhóm**: `2026/1/5` → `2026-01-05` là **chuỗi đổi nhưng ngày không đổi** — nếu guard "giá trị không đổi" so sánh chuỗi thay vì ngày đã parse thì web sẽ insert thêm lịch ⇒ gửi trùng. Đọc số lịch **ngay sau blur**, đây là hệ quả đồng bộ · Đánh giá spec: Spec không ghi (spec chỉ mô tả luồng lưu **setting**, không mô tả luồng lưu **giá trị** — xem §6 mục 1) · Evidence: ảnh màn 情報一覧 trước/ngay sau blur + ảnh màn hình LINE app của bạn A · RULE-06 · RULE-08 |
| TC-STATEDEP001-02 | UI | STATE-DEP-001 | Rightbar 友だち情報 — trường ngày | Normal | manual | product | Đổi sang ngày mới trên bộ chọn ngày → lịch gửi được tính lại theo ngày MỚI | Giống `TC-STATEDEP001-01`. Bạn bè B có `date_1` = `2026/3/7` (import CSV), lịch đang chờ gửi ở mốc 04/03/2026 10:00 | 1. Mở Chat 1:1 → bạn B → tab 友だち情報 → bấm giá trị `date_1`.<br>2. Chọn ngày mới **20/04/2026** trên bộ chọn ngày.<br>3. Rời ô, tải lại màn hình.<br>4. Mở 情報一覧 của trường `date_1`, đọc mốc gửi mới của bạn B.<br>5. Chờ qua mốc CŨ (04/03) — xác nhận **không** có tin nào gửi đi.<br>6. Chờ tới mốc MỚI, quan sát LINE app của bạn B | `date_1` từ `2026/3/7` → chọn `20/04/2026` · action: から 3 日 前, 10:00 | Giá trị lưu thành `2026-04-20`. Lịch cũ (mốc 04/03/2026) **bị thay**, không còn trong hàng chờ. Mốc mới = **17/04/2026 10:00** (20/04 trừ 3 ngày). Qua mốc cũ bạn B **không nhận tin**. Tới mốc mới bạn B nhận **đúng 1 tin** trên LINE app | | Lấp `G1` · Lấp `Q1` · Lấp `Q3` (chiều Normal của tính toán mốc gửi) · dẫn từ `TC-FRI-239` · Đánh giá spec: Spec ghi rõ (`friend-information/feature-spec.md` §SCR-FRI-04 — công thức `sent_date_time = date ± N days + HH:mm`) · Evidence: ảnh 情報一覧 trước/sau + ảnh LINE app + ảnh xác nhận không gửi ở mốc cũ · RULE-06 · RULE-08 |
| TC-STATEDEP001-03 | UI | STATE-DEP-001 | Rightbar 友だち情報 — trường ngày | Abnormal | manual | product | Bạn ĐÃ nhận action rồi mới chuẩn hoá giá trị → KHÔNG gửi lại lần hai | Trường `date_1` cấu hình như trên. Bạn bè C có `date_1` = `2026/1/5` (import CSV) và **lịch đã gửi xong** (đã nhận tin, trạng thái lịch = đã gửi). Không còn lịch chờ nào cho C ở trường này | 1. Mở Chat 1:1 → bạn C → tab 友だち情報 → bấm giá trị `date_1`.<br>2. Không đổi gì, rời ô (giá trị được ghi lại thành `2026-01-05`).<br>3. Mở 情報一覧 của trường `date_1`, kiểm số lịch của bạn C.<br>4. Theo dõi LINE app của bạn C trong 24 giờ kế tiếp | `date_1` = `2026/1/5`, lịch đã ở trạng thái đã gửi | Bạn C **không phát sinh lịch gửi mới**, **không nhận lại tin** đã nhận trước đó. Số bản ghi lịch của C ở trường này không tăng. Bảng lịch sử friend info có 1 dòng `2026/1/5` → `2026-01-05` (đúng như `NEW-4`), nhưng điều đó **không** kéo theo gửi lại action | | Lấp `G1` · Lấp `Q1` · regression — dẫn từ `TC-FRI-237` · Đánh giá spec: Spec không ghi — xem §6 mục 1 · Evidence: ảnh 情報一覧 trước/sau + ảnh lịch sử chat LINE của bạn C · RULE-06 · RULE-08 |
| TC-STATEDEP001-04 | UI | STATE-DEP-001 | Rightbar 友だち情報 — trường ngày | Boundary | manual | product | Kiểu「月日」lặp hàng năm đang có lịch năm sau → chuẩn hoá giá trị không nhân đôi, không xoá mất lịch | Trường `date_2` kiểu `年月日`, cấu hình 登録=「月日」 (lặp hàng năm), から 0 日, giờ gửi 09:00. Bạn bè D có `date_2` = `2026/1/5` (import CSV); mốc 05/01 năm nay **đã qua** nên hệ thống đã sinh lịch cho **năm kế tiếp** (05/01 năm sau, đang chờ gửi) | 1. Ghi lại số lịch + mốc gửi của bạn D ở 情報一覧 trường `date_2`.<br>2. Mở Chat 1:1 → bạn D → tab 友だち情報 → bấm giá trị `date_2`.<br>3. Không đổi gì, rời ô.<br>4. Tải lại, đối chiếu lại số lịch + mốc gửi của D | `date_2` = `2026/1/5`, kiểu 「月日」, lịch năm kế tiếp đang chờ | Giá trị lưu thành `2026-01-05`. Bạn D vẫn có **đúng 1 lịch chờ gửi**, mốc **05/01 năm kế tiếp 09:00 không đổi**. Không sinh lịch thứ hai, không mất lịch đang chờ | | Lấp `G1` · Lấp `Q1` · Lấp `Q3` (Boundary lặp năm) · dẫn từ `TC-FRI-241`, `TC-FRI-81` · Đánh giá spec: Spec ghi rõ (「登録「月日」を選択した場合、毎年その月日にアクションが稼働します。」) · Evidence: ảnh 情報一覧 trước/sau · RULE-08 |
| TC-JOB001-01 | UI | JOB-001 | Job action ngày tháng | Normal | manual | product | Chuẩn hoá hàng loạt nhiều bạn → tổng số bản ghi lịch không phình, mỗi bạn vẫn nhận đúng 1 tin | Trường `date_3` kiểu `年月日` có action lịch (から 0 日, giờ gửi = giờ hiện tại + 30 phút). Import CSV **20 bạn bè** cùng có giá trị `date_3` ở định dạng cũ, cùng ngày = hôm nay (`2026/9/15`). **Chưa bạn nào được mở ô sửa.** Ghi lại **tổng số bản ghi `event_step_time`** của trường này (qua màn 情報一覧 hoặc số lịch chờ gửi) — đây là con số "đầu vào" | 1. Ghi lại tổng số lịch chờ gửi của trường `date_3` (con số A) và số bạn có giá trị (con số B).<br>2. Mở Chat 1:1, lần lượt với **5 bạn đầu tiên**: bấm vào giá trị `date_3` → rời ô ngay, **không đổi gì**.<br>3. **Ngay sau mỗi lần rời ô**, đọc lại tổng số lịch chờ gửi — ghi nhận con số sau từng bạn (hệ quả là đồng bộ, không cần chờ job).<br>4. Sau khi xong 5 bạn, so sánh tổng số lịch với con số A.<br>5. Chờ tới mốc gửi, đếm số tin thực gửi và quan sát LINE app của 5 bạn đã chạm + 2 bạn chưa chạm | 20 bạn, `date_3` = `2026/9/15` (định dạng `yyyy/M/d` từ CSV); 5 bạn được mở-rồi-rời ô | Sau bước 4: **tổng số lịch vẫn bằng đúng con số A** — 5 lần blur **không sinh thêm bản ghi lịch nào** (ngày không đổi) và cũng **không xoá mất lịch nào**. Số lịch = số bạn có giá trị (A = B), không bản ghi nào biến mất. Tới mốc gửi: **đúng 20 tin** được gửi, mỗi bạn nhận **đúng 1 tin** trên LINE app — 5 bạn đã chạm **không nhận trùng**, 2 bạn chưa chạm **không bị bỏ sót**. Job không dừng, không lỗi | | Lấp `G1` · Lấp `Q2` · dẫn từ `TC-FRI-245`, `TC-FRI-262` · **Oracle là `event_step_time` ĐỌC NGAY SAU BLUR** — web insert đồng bộ lúc lưu value, job chỉ poll bảng lịch nên không cần chờ job để bắt lỗi insert trùng · Đánh giá spec: Spec không ghi (luồng lưu **value** sinh lịch chưa có trong spec — xem §6 mục 1) · Evidence: bảng đối chiếu số bản ghi lịch trước/sau từng lần blur + số tin thực gửi + ảnh LINE app · RULE-06 · RULE-08 |
| TC-FUNCDATE001-02 | UI | FUNC-DATE-001 | Job action ngày tháng | Boundary | manual | product | Giá trị năm biên `0001` / `9999` lưu được từ Chat 1:1 → lịch gửi không sinh mốc vô lý, job không lỗi | Trường `date_4` kiểu `年月日` có action lịch (から 3 日 後, 10:00). Bạn bè G và H chưa có giá trị. `NEW-14` đã xác nhận `0001-01-01` và `9999-12-31` **lưu được** qua panel Chat 1:1 | 1. Mở Chat 1:1 → bạn G → tab 友だち情報 → bấm `date_4` → nhập ngày **01/01/0001** → rời ô.<br>2. Làm tương tự với bạn H, ngày **31/12/9999**.<br>3. Mở 情報一覧 của trường `date_4`, đọc mốc gửi (nếu có) của G và H.<br>4. Theo dõi log job trong 24 giờ và LINE app của G, H | G: `0001-01-01` · H: `9999-12-31` | Hai giá trị lưu đúng như `NEW-14` đã ghi nhận. **Job không phát sinh lỗi**, không dừng vì 2 bản ghi này. G (quá khứ rất xa, kiểu 年月日) → **không sinh lịch, không gửi**. H (tương lai rất xa) → hoặc sinh lịch đúng mốc 03/01/10000 hoặc bị chặn theo quy tắc — **phải nhất quán với quyết định ở §6 mục 4**, không được rơi vào trạng thái lịch mồ côi | | Lấp `Q3` (Boundary tầng tính mốc) · dẫn từ `TC-FRI-253`, `TC-FRI-84` · Đánh giá spec: Spec không ghi (miền giá trị hợp lệ của trường 年月日 chưa được quy định — §6 mục 4) · Evidence: log job + ảnh 情報一覧 · RULE-08 |
| TC-DATACOUNT001-01 | Data | DATA-COUNT-001 | Bộ đếm 回答人数 | Normal | manual | Tất cả | Chuẩn hoá giá trị ngày từ Chat 1:1 không làm lệch 回答人数 | Trường `date_1` hiện có 「回答人数」 = **5人**, trong đó 3 bạn có giá trị định dạng CSV cũ (`2026/1/5`, `2026-1-5`, `2026/01/05`). Ghi lại con số 5 ở màn 友だち情報管理 trước khi thao tác | 1. Mở Chat 1:1 → lần lượt 3 bạn có định dạng cũ → tab 友だち情報 → bấm giá trị `date_1` → rời ô **không đổi gì**.<br>2. Về màn 友だち情報管理, đọc lại 「回答人数」 của `date_1`.<br>3. Bấm vào con số để mở màn 情報一覧, đếm số dòng.<br>4. Quay lại 1 trong 3 bạn, **xoá trắng** giá trị `date_1` rồi rời ô.<br>5. Đọc lại 「回答人数」 và số dòng ở 情報一覧 | 3 bạn: `2026/1/5` · `2026-1-5` · `2026/01/05` — chuẩn hoá không đổi ngày; sau đó xoá trắng 1 bạn | Sau bước 2: 「回答人数」 **vẫn là 5人** (chuẩn hoá chuỗi không phải là thêm bạn mới). Số dòng ở 情報一覧 = **5**, khớp con số. Sau bước 5: 「回答人数」 = **4人**, bạn vừa xoá **biến mất** khỏi 情報一覧, con số không âm và khớp số dòng | | Lấp `G2` · Lấp `Q4` · regression — dẫn từ `TC-FRI-171`, `TC-FRI-172`, `TC-FRI-180` · Đánh giá spec: Spec ghi rõ (`friend-information/feature-spec.md:315` — xoá value giảm `total_user_has_value`) · Evidence: ảnh 「回答人数」 trước/sau + ảnh 情報一覧 |
| TC-FRIEND001-02 | UI | FRIEND-001 | Rightbar 友だち情報 — 生年月日 | Normal | manual | Tất cả | Info basic 生年月日 lưu ở định dạng cũ → Chat 1:1 mở đúng ngày và lưu về chuẩn | Bạn bè I có `生年月日` **thực sự đang lưu ở định dạng cũ** trong `line_user.birthday`. **Bước dựng tiền đề bắt buộc làm trước**: thử import CSV `生年月日 = 2026/1/5` rồi đọc lại DB — nếu tầng import đã tự chuẩn hoá về `2026-01-05` thì ghi nhận kết quả đó (xem Ghi chú) và dựng tiền đề bằng cách ghi thẳng `line_user.birthday = '2026/1/5'` | 1. Đọc `line_user.birthday` của bạn I, xác nhận đang là `2026/1/5` (định dạng cũ) **trước khi** mở màn.<br>2. Mở Chat 1:1 → bạn I → tab 友だち情報.<br>3. Bấm vào giá trị 「生年月日」 để mở ô nhập ngày.<br>4. Quan sát ô nhập + bộ chọn ngày.<br>5. Rời ô mà không đổi gì.<br>6. Tải lại màn hình, đọc lại `line_user.birthday` | `line_user.birthday` = `2026/1/5` | Ô nhập ngày hiển thị **đúng 05/01/2026** (không để trống), bộ chọn ngày mở đúng tháng 01/2026 với ngày 5 đang chọn. Rời ô: **không** hiện 「日付のフォーマットが無効です。」. Sau khi tải lại, `line_user.birthday` = `2026-01-05`, màn hiển thị 「2026.01.05」 | | Lấp `G8` · Lấp `Q11` · **Đây là TC chạy THẬT nhánh 生年月日 mà `NEW-8` chưa chạy được** · Đánh giá spec: Spec không ghi (chưa rõ `line_user.birthday` có chứa được định dạng cũ không — xem §6 mục 6) · Evidence: ảnh ô nhập + ảnh query DB trước/sau · Nếu bước dựng tiền đề chứng minh import **luôn** canonicalize và DB **không** chứa được định dạng cũ → kết luận 生年月日 **không dính bug**, phạm vi ticket ghi thừa, ghi lại vào §6 mục 6 |
| TC-REGSHARED001-03 | UI | REG-SHARED-001 | Màn 友だち詳細 — trường ngày | Normal | manual | Tất cả | Màn 友だち詳細 mở đúng ngày với giá trị định dạng CSV cũ (kiểm tuyên bố "quét ngang" của Dev) | Bạn bè J có custom field `date_1` (`type_data=3`) = `2026/1/5` nạp bằng import CSV, và `生年月日` = giá trị định dạng cũ nếu dựng được (theo `TC-FRIEND001-02`). Bản fix đã deploy. **Chưa mở màn Chat 1:1 của bạn J** (để value chưa bị chuẩn hoá) | 1. Vào màn danh sách bạn bè → mở **màn 友だち詳細** của bạn J (KHÔNG đi qua Chat 1:1).<br>2. Xem khu vực thông tin bạn bè: quan sát giá trị hiển thị của `date_1` và 「生年月日」.<br>3. Bấm vào trường `date_1` để chuyển sang chế độ sửa.<br>4. Quan sát ô nhập ngày + bộ chọn ngày.<br>5. Lặp bước 3–4 với 「生年月日」 | `date_1` = `2026/1/5` · `生年月日` = định dạng cũ (nếu dựng được) | Ở chế độ xem: cả 2 trường hiển thị đúng ngày, không trống. Khi mở sửa: ô nhập ngày hiển thị **đúng 05/01/2026**, bộ chọn ngày mở đúng tháng/ngày — **không bị bỏ trắng**. Không lỗi JavaScript trên Console, panel không trắng | | Lấp `G9` · Lấp `Q10` · regression — dẫn từ `TC-FRI-204`, `TC-FRI-291` · **TC này nhằm kiểm chứng tuyên bố "Quét ngang" của Dev (`I13`) — nếu ô nhập bị bỏ trắng thì fix hiện tại THIẾU PHẠM VI** · Đánh giá spec: Spec ghi rõ (`detail-friend/ui/ui-spec.md:112` — 「date 1」 kiểu `date` đọc từ `friend_information_value.value`) · Evidence: ảnh chế độ xem + ảnh ô nhập sau khi bấm sửa |
| TC-REGSHARED001-04 | UI | REG-SHARED-001 | Màn 友だち詳細 — trường ngày | Abnormal | manual | Tất cả | Lưu trường ngày định dạng cũ ở màn 友だち詳細 → không bị chặn bởi validate định dạng | Giống `TC-REGSHARED001-03`, bạn bè J chưa mở Chat 1:1 | 1. Mở màn 友だち詳細 của bạn J.<br>2. Bấm sửa `date_1`, **không đổi giá trị**, rồi bấm 保存 (hoặc rời ô theo đúng cơ chế lưu của màn).<br>3. Quan sát thông báo trả về.<br>4. Làm tương tự với 「生年月日」.<br>5. Tải lại màn hình, đọc DB `friend_information_value.value` và `line_user.birthday` | `date_1` = `2026/1/5` · `生年月日` = định dạng cũ | **Không** hiện thông báo lỗi định dạng (`日付のフォーマットが無効です。` hoặc `errors` của EP-11). Giá trị lưu về `2026-01-05` ở cả 2 trường. Nếu **có** báo lỗi ⇒ bug gốc **vẫn còn** ở màn 友だち詳細 ⇒ raise ticket bổ sung, không đóng #34603 | | Lấp `G9` · Lấp `Q10` · Đánh giá spec: Spec ghi rõ (`detail-friend/web/logic-spec.md:293` — EP-11 validate `birthday` regex `Y-m-d` hoặc `Y/m/d`, cùng kiểu chặt như `ChatController:4535`) · Evidence: ảnh thông báo + ảnh query DB trước/sau · Lưu ý: `diffStat` chỉ có 1 file `chat-v2.js` ⇒ màn này chưa được fix |
| TC-STATEDEP001-05 | UI | STATE-DEP-001 | Rightbar 友だち情報 — 生年月日 | Normal | manual | product | Lịch remind sinh nhật được xử lý nhất quán giữa lối Chat 1:1 và lối 友だち詳細 | Trường mặc định 「生年月日」 đã cấu hình action remind sinh nhật (kiểu 「月日」 lặp hàng năm, から 0 日, giờ gửi = giờ hiện tại + 15 phút). Bạn bè K và L cùng có `生年月日` giống nhau, đều đang có lịch remind chờ gửi. Ghi lại số lịch + mốc gửi của cả hai | 1. Sửa `生年月日` của bạn **K** từ **màn 友だち詳細** sang ngày mới.<br>2. Sửa `生年月日` của bạn **L** sang **cùng ngày mới đó** nhưng từ **panel 友だち情報 của Chat 1:1**.<br>3. Đọc lịch remind của cả K và L.<br>4. Chờ tới mốc gửi, quan sát LINE app của cả hai | `生年月日` cả K và L đổi sang cùng 1 ngày mới | **Lịch remind của K và L phải giống hệt nhau**: cùng số bản ghi, cùng mốc gửi. Tới mốc, **cả hai đều nhận đúng 1 tin** trên LINE app. Nếu chỉ K có lịch mới còn L không (hoặc ngược lại) ⇒ 2 lối ghi cùng dữ liệu cho hệ quả khác nhau → raise ticket + §6 mục 5 | | Lấp `G10` · Lấp `Q1` · dẫn từ `TC-FRI-231` · Đánh giá spec: Spec ghi rõ một nửa (`detail-friend/web/api-spec.md:495` — EP-11 gọi `settingEventTimeFriendInfo($lineID, 'd_4', …)`; **không có** mô tả tương ứng cho `saveSettingDisplayInfoItem`) · Evidence: ảnh lịch remind của cả 2 bạn + ảnh LINE app · RULE-06 · RULE-08 |
| TC-ENV003-01 | UI | ENV-003 | Rightbar 友だち情報 — trường ngày | Normal | manual | product | Luồng lõi + asset JS + media chạy đúng trên PRODUCTION sau deploy | Bản fix đã deploy lên production. Có bot thật với trường `年月日` và trường kiểu ảnh/PDF trong panel 友だち情報. Trình duyệt **đã từng mở** màn Chat 1:1 bản cũ (cache còn bản `chat-v2.js` trước fix) — không xoá cache, không hard-reload | 1. Mở màn Chat 1:1 trên production, **F5 thường** (không Ctrl+F5).<br>2. Mở DevTools → Network, kiểm URL của `/js/chats/chat-v2.js` và mã phản hồi.<br>3. Chọn bạn có giá trị `年月日` định dạng CSV cũ → tab 友だち情報 → bấm vào giá trị.<br>4. Rời ô, tải lại, đối chiếu giá trị hiển thị.<br>5. Trong cùng panel, mở trường kiểu ảnh và kiểu PDF đã upload — bấm xem file.<br>6. Lặp bước 1–4 trên **cả 2 router** của production | Giá trị `年月日` định dạng `yyyy/M/d`; 1 file ảnh + 1 file PDF đã upload sẵn | Sau F5 thường: trình duyệt nạp `chat-v2.js` với **version khác** bản đang cache, HTTP 200, nội dung có phần chuẩn hoá ngày. Ô ngày hiển thị **đúng ngày đang lưu**, rời ô lưu thành công, không có 「日付のフォーマットが無効です。」. Ảnh và PDF mở được qua domain media production (`p.lmes.jp`), không lỗi 404. Kết quả **giống nhau trên cả 2 router** | | Lấp `G4` · Lấp `Q5` · Chạy lại nội dung `NEW-18` + `NEW-26` ở môi trường thật · Đánh giá spec: Spec ghi rõ (Catalog D — media production qua `p.lmes.jp`, loadbalance 2 server) · Evidence: ảnh tab Network (URL + version + HTTP 200) + ảnh panel sau F5 + ảnh file media mở được, ghi kèm router id · RULE-08 |

**Các dòng §1/§2 KHÔNG đề xuất TC mới — lý do:**

- **`G3` / `Q8`** (bug #843 + việc nới Expected của `NEW-21`): không phải thiếu TC mà là **thiếu quyết định**. Phải chốt baseline ở §6 mục 3 trước, rồi mới viết được Expected đo lường được (xem `I8`).
- **`G5` / `Q6`** (cross-browser): TC đã tồn tại — `NEW-15`. Vấn đề là nó **`skip` vì hạ tầng** (image test không có WebKit, không có máy macOS), không phải thiếu TC. Việc cần làm: chạy tay `NEW-15` trên **Mac Safari thật** + LINE app iOS, hoặc cài WebKit vào image rồi hạ phạm vi TC xuống "Chromium vs WebKit" và ghi rõ giới hạn đó.
- **`G6` / `Q7`** (ngày không tồn tại `2026-02-30` bị dồn thành `2026-03-02`): chưa viết được TC vì **Expected đang là câu hỏi mở**. Sau khi §6 mục 2 chốt: nếu kết luận "phải từ chối" → raise bug mới + sửa `NEW-10`; nếu kết luận "chấp nhận dồn ngày" → giữ `NEW-10`, bổ sung `Trạng thái đánh giá spec = Đã hỏi leader`.
- **`G7` / `Q9`** (WHERE scope 2 bot): dùng lại kho `TC-FRI-192` + `TC-FRI-333` — không viết mới vì fix không chạm mệnh đề `WHERE` và `NEW-2`/`NEW-3` đã chứng minh scope tách đúng theo từng friend.
- **`G2`** (xoá trắng → dọn lịch): phần 「回答人数」 đã có `TC-DATACOUNT001-01` ở trên; phần **dọn lịch** dùng lại kho `TC-FRI-214`, chỉ cần đổi lối vào sang panel 友だち情報 của Chat 1:1. Song song, `NEW-12` và `NEW-25` nên được **bổ sung 2 điểm kiểm** vào Expected (`total_user_has_value` giảm · `event_step_time` bị dọn) thay vì đẻ TC mới — đã ghi ở `I10`.

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | `spec-features/admin/friend-information/feature-spec.md` §SCR-FRI-04 | Spec **chỉ mô tả luồng lưu `friend_information_setting`** (EP-09) sinh/xoá `event_step` + `event_step_time`. **Không có** mô tả cho luồng **ghi giá trị của từng bạn** (từ Chat 1:1, 友だち詳細, import CSV, action) — trong khi Leader xác nhận (2026-09-15) **web tự INSERT các bản ghi action thoả mãn vào `event_step_time` ngay tại thời điểm lưu value**. Cần bổ sung rõ: ghi / sửa / xoá value `年月日` ⇒ lịch gửi được **giữ · insert thêm · tính lại · dọn** trong trường hợp nào, và **guard "giá trị không đổi" so sánh CHUỖI hay NGÀY đã parse** (quyết định trực tiếp việc `2026/1/5` → `2026-01-05` có sinh lịch trùng không) | `03-dev-impact.md` mục 3 không kê function nào thuộc luồng lịch gửi ↔ kho `TC-FRI-243` khẳng định "ghi giá trị 年月日 → lịch gửi được cập nhật" (`G1`, `I10`) | `Dev + PM` |
| 2 | `spec-features/admin/chat-11/feature-spec.md` §4.4 + validate của `ChatController::saveSettingDisplayInfoItem` | **Hành vi không nhất quán với ngày không tồn tại**: `2026-2-30` (thiếu số 0) → **từ chối** với 「日付のフォーマットが無効です。」; `2026-02-30` (đủ 2 chữ số) → **nhận, không báo lỗi, lưu lệch thành `2026-03-02`**. Cần chốt 1 hành vi duy nhất và ghi vào spec. Nếu kết luận là "phải từ chối" ⇒ đây là **bug sửa thầm dữ liệu khách hàng**, phải raise ticket riêng | `NEW-9` (`TC-FUNC003-01`) ↔ `NEW-10` (`TC-TOOLERRHYG001-01`) — Expected mâu thuẫn về nghiệp vụ (§3, `I7`) | `Dev + Leader` |
| 3 | `spec-features/admin/chat-11/feature-spec.md` §4.4 — panel 友だち情報 | **Chưa có baseline**: click 1 lần từ trường ngày này sang trường ngày khác thì trường thứ hai **có vào chế độ sửa ngay không**? Hiện tại phải bấm 2 lần (root cause: callback `success` của `saveItemInfo` đặt `indexEditInfo=-1` + `initFriendInfo()` **sau** khi `editItemInfo` đã mở field thứ hai). Chốt xong mới đóng được bug #843 và viết lại Expected cho `NEW-21` | Bug #843 (`severity=High`, `status=stale`, `redmine_id=null`) ↔ `NEW-21` version 2 tự khai "chưa có requirement đủ tin cậy để chấm Pass/Fail" (`G3`, `I1`, `I8`) | `Leader + Dev` |
| 5 | `spec-features/admin/detail-friend/web/api-spec.md` §EP-11 + `chat-11/feature-spec.md` §4.4 | **2 lối ghi cùng dữ liệu nhưng spec chỉ mô tả 1 lối gọi reminder.** EP-11 `saveCustomInfo` (màn 友だち詳細) được ghi rõ là gọi `settingEventTimeFriendInfo($lineID, 'd_4', $bot_id, $birthday)` → INSERT `event_step_time`. **Không có** mô tả tương ứng cho `ChatController::saveSettingDisplayInfoItem` (panel Chat 1:1) — trong khi fix này biến chính lối đó từ "luôn lỗi" thành "ghi thành công". Cần chốt: sửa 生年月日 từ Chat 1:1 **có** phải cập nhật lịch remind như màn 友だち詳細 không | `detail-friend/web/api-spec.md:495` ↔ `03-dev-impact.md` mục 3 không kê `settingEventTimeFriendInfo` (`G10`, `TC-STATEDEP001-05`) | `Dev + PM` |
| 6 | `spec-features/admin/friend-information/feature-spec.md` + `chat-11` §4.4 — trường 生年月日 | **Chưa rõ `line_user.birthday` có thể chứa định dạng cũ hay không.** `NEW-8` nạp `2026/1/5` qua import CSV nhưng DB đọc ra `2026-01-05` ⇒ hoặc import canonicalize, hoặc cột không nhận. Spec không nói. Hệ quả trực tiếp: **phạm vi ticket có đúng không** — tiêu đề ghi *"date/ **birthday**"* nhưng nếu birthday không bao giờ giữ định dạng cũ thì nhánh đó không dính bug và không cần fix/test | `NEW-8` Expected tự hedge *"Nếu tầng import/DB đã canonicalize birthday…"* (`G8`, `TC-FRIEND001-02`) | `Dev` |
| 4 | `spec-features/admin/friend-information/feature-spec.md` §SCR-FRI-04 | **Miền giá trị hợp lệ của trường `年月日` chưa được quy định.** `NEW-14` xác nhận `0001-01-01` và `9999-12-31` đều lưu được qua panel Chat 1:1. Cần chốt có giới hạn năm hay không, và nếu có thì chặn ở tầng nào (client / server / cả hai) | `NEW-14` (`TC-FUNC004-01`) pass ↔ spec không nêu giới hạn (`Q3`, `TC-FUNCDATE001-02`) | `PM` |
