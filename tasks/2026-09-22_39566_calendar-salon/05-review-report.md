# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | `(1) MCP LME TEST STUDIO — task #149` (ticket 39566, branch `ai_fixbug_39566`, round 1, `reviewState=leader`) |
| Tổng số TC review | `81` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Trả lời** (2 dòng bắt buộc):

| Chiều | Kết quả |
|---|---|
| **(a) dev-impact** — Dev tự kê ở `03-dev-impact.md` mục 4 | `25/38 mục có TC` — **CHƯA ĐỦ** (5 GAP · 8 RISK; 4 mục N/A: `F29` đã revert · `D1` Dev khai không có · `T2`/`T3` Dev khai KHÔNG sửa) |
| **(b) diff code** — suy từ diff thật (Studio `dev_impact` + `spec_delta`, `diffAvailable=true`, 32 file / +786 −180) | `5/15 điểm có TC` — **CHƯA ĐỦ** (2 GAP · 8 RISK) |

**Kết luận**: `30/53 vùng ảnh hưởng đủ TC · 7 GAP · 16 RISK`

**2 câu hỏi Leader đặt thêm** (chi tiết ở bảng **1b** và **1c** cuối section):

| Câu hỏi | Trả lời |
|---|---|
| TCs đã check **toàn bộ API** của tính năng salon chưa? | **CHƯA** — `44/63` endpoint có ≥1 TC chạm, **19 endpoint có 0 TC**. Và chỉ `27/81` TC thật sự gọi ở tầng API |
| TCs đã check **thao tác GUI** còn chạy bình thường chưa (web + app mobile)? | **CHƯA** — web: chỉ `22/81` TC là `ui` + `Normal`, trong đó 4 fail + 1 skip → **17 thao tác GUI được xác nhận bình thường**; **7 màn/tab có 0 TC Normal**. App mobile admin: **0 TC** thao tác trên thiết bị thật |

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | `F15 — public/js/calendar_salon/setting_reservation/setting-payment.js` (Tab 「決済連携」) — ⚠️ **lệch tầng**: JS **có** trong diff nhưng controller phục vụ EP-17/EP-18 là `Basic\SettingPaymentCalendarSalonController` (`web/api-spec.md:36-37`) **KHÔNG** có trong `spec_delta.files[]` | `dev-impact` | `không có` | `GAP — 0 TC. JS đã đổi cách xử lý lỗi (đi nhánh error, đọc error_message) trong khi controller payment vẫn trả mã cũ → phải kiểm JS mới + controller cũ còn khớp nhau. BR-05 (điều kiện bật thanh toán) + BR-06 (ghi lịch sử) không TC nào verify` | `[BLOCKER]` |
| G2 | `F14 — setting-message.js` + `F16 — setting-remind.js` (「予約・キャンセルのメッセージ」 / 「リマインドメッセージ」) · hàm `saveSettingMessage` Dev kê là 1 trong 25 hàm không guard | `dev-impact` | `không có` | `GAP — NEW-29 chỉ cover 4 mục cài đặt (thông báo kín lịch · giới hạn số đặt · thời gian đệm · điều khoản + từ ngữ hệ thống), KHÔNG cover tin nhắn đặt/huỷ và remind` | `[BLOCKER]` |
| G3 | `F8 — booking.js` + `F13 — add-new-booking.js` (tab 「本日／新着の予約」 · admin thêm booking · huỷ đơn) · hàm `checkBookingCancel` / `checkBookingCancelStaff` | `dev-impact` | `NEW-46 (skip)` | `GAP chéo bot cho 2 hàm huỷ đơn + RISK: TC duy nhất đi qua luồng thêm/huỷ booking (NEW-46) đang skip → 0 kết luận` | `[BLOCKER]` |
| G4 | `D4 — calendar_salon_history_change_setting_payment` (bảng KHÔNG có `bot_id`, Dev ràng buộc gián tiếp qua `calendar_id`) | `dev-impact` | `không có` | `GAP — 0 TC verify section 「決済利用 変更履歴」 còn hiện đủ bản ghi của lịch đang xem (BR-06)` | `[MAJOR]` |
| G5 | `D7 — 16 câu xoá cascade khi xoá lịch salon` (BR-07) | `dev-impact` | `NEW-41`, `NEW-65`, `NEW-72` | `RISK — cả 3 TC đều FAIL (bug 1125 · 1170 · NEW-65 chưa raise bug) → 0 TC có kết luận Đạt cho vùng Dev tự đánh giá là rủi ro cao nhất` | `[BLOCKER]` |
| G6 | `D3 — 4 bảng bot_id DEFAULT NULL` (`calendar_salon_course` · `calendar_salon_course_menu` · `sync_booking_google_calendar_histories` · `download_csv_sync_google_calendar`), ràng buộc bằng subquery qua lịch cha | `dev-impact` | `NEW-39 (fail)`, `NEW-64 (pass)`, `NEW-45 (pass)`, `NEW-65 (fail)` | `RISK — 2/4 FAIL (bug 1119: khoá học bị giấu mất khỏi danh sách). Dev khai CHƯA verify được trên DB thật ("MySQL dev Connection refused") → đúng vùng Dev yêu cầu human chạy regression` | `[BLOCKER]` |
| G7 | `T5 — Cron / Job đồng bộ Google Calendar + webhook thanh toán` (3 service dùng chung với **8 Console Command + 7 Job** + Mobile + Api, CỐ Ý không siết bot) | `dev-impact` + `diff code` | `NEW-73 (skip)`, `NEW-46 (skip)`, `NEW-52 (fail)` | `RISK — 2 skip + 1 fail = 0 kết luận. 8 Console Command không có TC nào chạm. Đây là rủi ro Dev tự nêu là sẽ "CHẾT toàn bộ cron/webhook/màn đặt chỗ khách" nếu siết sai` | `[BLOCKER]` |
| G8 | `T4 — App mobile admin` (`Api\CalendarSalonController`, 4 hàm `addWorking`/`updateWorking`/`deleteWorking`/`createBooking`) | `dev-impact` | `NEW-61 (pass)` | `RISK — RULE-06: expected khẳng định "app không bị treo vòng quay tải" nhưng steps chỉ gọi API, exec_mode=auto, không có thiết bị thật. Dev tự nêu reservation.dart:906 gọi addWorking() KHÔNG có catchError` | `[MAJOR]` |
| G9 | `F7 — calendar_detail.js` (lưới lịch ngày/tuần/tháng) · hàm `getDetailEventStep` | `dev-impact` | `không có TC chéo bot` | `RISK — getDetailEventStep nằm trong 25 hàm Dev sửa mà không có guard nào trước fix; 0 TC lấy chi tiết khung giờ 「予約枠」 bằng mã của bot khác` | `[MAJOR]` |
| G10 | `D6 — line_booking_history_actions` (INSERT trong `orderRefund`, bảng không có `bot_id`, CỐ Ý không lọc) | `dev-impact` | `NEW-48 (pass)` | `RISK — chỉ 1 TC Normal ghi lịch sử; 0 TC ĐỌC LẠI 操作履歴 của đơn sau khi siết bot (ràng buộc qua booking_id)` | `[MINOR]` |
| G11 | **6 hàm trong 25 hàm Dev sửa chưa có TC chéo bot**: `getDetailEventStep` · `deleteCalendarStaff` · `checkAuthorGoogleSync`/`sendMailCodeAuthGoogleSync` · `saveSettingMessage` · `checkBookingCancel`/`checkBookingCancelStaff` · `getDetailHistorySyncForBooking` | `diff code` | `không có` | `GAP — 16 TC REQ-004 chỉ chạm ~10/25 hàm. 6 hàm trên Dev khai "id đến thẳng từ request rồi dùng luôn — lỗ hổng THẬT" nhưng không TC nào verify` | `[BLOCKER]` |
| G12 | **4 file JS nằm trong diff nhưng KHÔNG có trong mục 4.1 của Dev**: `setting-free-time-before-after.js` · `setting-limit-booking.js` · `setting-notify-ful-slot.js` · `setting-season.js` | `diff code` | `NEW-29 (pass)` | `RISK — NEW-29 chỉ cover nhánh chéo bot (404). 0 TC lưu hợp lệ + 0 TC thiếu required → 422 trên 4 màn này. 「シーズン設定」 (setting-season.js) không TC nào chạm ở bất kỳ nhánh nào` | `[MAJOR]` |
| G13 | `Controller — hằng VALIDATION_ATTRIBUTES_JA (22 nhãn field)` | `diff code` | `8 TC (nhóm I18N-001)` + `NEW-66` | `RISK — chỉ rà được ~8/22 nhãn. Bug 1120 đã chứng minh có nhãn SAI so với chữ trên màn (nhận 「従業員名を入力してください」 thay vì 「スタッフ名」) → 14 nhãn còn lại chưa ai kiểm` | `[MAJOR]` |
| G14 | `Controller — helper MỚI validateInput, dùng chung cho 22 lời gọi` | `diff code` | `13 TC validate` | `RISK — BƯỚC 2 câu 3: Dev KHÔNG kê danh sách 22 lời gọi cụ thể (chỉ ghi số "22"), nên không đối chiếu được lời gọi nào chưa có TC. Hàm dùng chung không có danh sách caller` | `[BLOCKER]` |
| G15 | `Hành vi đổi — 12 file JS CŨ bị sửa nội dung nhưng bump config/sns-line.php ĐÃ revert (commit 1b32a31c0a)` | `diff code` | `NEW-12 (pass)` | `RISK — NEW-12 chỉ verify file được nạp (200) + có tham số ?v=. 0 TC cho nhánh browser còn cache bản JS cũ (JS cũ không đọc khoá error_message → mất thông báo lỗi sau khi server chuyển 4xx/5xx)` | `[MAJOR]` |
| G16 | `Hành vi đổi — UI tải TRƯỚC release, submit SAU release` (đổi mã HTTP ~22 endpoint + thêm required validate) | `diff code` | `không có` | `GAP — 0 TC. Form đang mở từ trước release gửi lên server mới → thiếu field required mới / nhận 4xx mà nhánh error cũ không xử lý` | `[BLOCKER]` |
| G17 | `Rủi ro hồi quy (iv) — hoàn tiền` (3/4 nhánh HTTP được sửa nằm trong `orderRefund`) | `diff code` | `NEW-48/49/50/51/68 (pass)`, `NEW-52 (fail)` | `RISK — RULE-08: hoàn tiền là bill tiền, 0/81 TC chạy production (toàn bộ ở env local). Không được kết luận từ local` | `[MAJOR]` |
| G18 | `T1 — 17/61 endpoint admin của Basic\CalendarSalonController KHÔNG có TC nào chạm` (xem bảng 1b dưới) | `dev-impact` | `không có` | `GAP — fix áp 22 lời gọi validateInput + 78 query bot-scope lên TOÀN BỘ controller (4629 dòng), nên mọi endpoint admin của tính năng đều nằm trong phạm vi ảnh hưởng; 17 endpoint chưa TC nào gọi tới` | `[BLOCKER]` |
| G19 | `T1 — 7 màn/tab admin không có TC Normal nào xác nhận GUI chạy bình thường` (xem bảng 1c dưới) | `dev-impact` | `không có TC Normal` | `GAP — 「本日／新着の予約」 · 「決済連携」 · 「予約・キャンセルのメッセージ」 · 「リマインド」 · 「予約ページの非表示」 · 「Googleスプレッドシート連携」 · 「削除済み予約」` | `[BLOCKER]` |
| G20 | `T4 — App mobile admin: 0 TC có thao tác GUI trên thiết bị thật` | `dev-impact` | `NEW-61 (grp=api, mode=auto)` | `RISK — TC duy nhất nhắc tới app chỉ GỌI API, không bấm gì trên app. RULE-06: output cuối phải verify trên thiết bị thật` | `[MAJOR]` |

> ⚠️ ID tham chiếu = `temp_id` hiển thị trên Studio (`NEW-xx`); TC gốc từ kho dùng `#<studio id>`.

### 1b. Endpoint của tính năng salon chưa có TC nào (trả lời câu hỏi "đã check toàn bộ API chưa")

Đối chiếu **63 endpoint** ở `feature-spec.md` §6 (EP-01→EP-63) với 81 TC → **44/63 endpoint có ≥1 TC chạm · 19 endpoint có 0 TC**:

| EP | Endpoint | Nhóm màn | Controller | Vì sao vẫn trong phạm vi task |
|---|---|---|---|---|
| EP-07 | `get-list-booking` — danh sách booking có filter + phân trang | SCR-SLN-02a | `Basic\CalendarSalonController` | Query danh sách booking nằm trong 78 query bị thêm ràng buộc bot |
| EP-08 | `get-staffs-active` | SCR-SLN-02 | nt | `getListStaffByCalendar` — Dev kê là hàm không có guard |
| EP-09 | `get-courses-active` | SCR-SLN-02 | nt | `getListCourseByCalendar` — Dev kê là hàm không có guard |
| EP-15 | `setting-message` (GET) | SCR-SLN-04 | nt | `saveSettingMessage` Dev kê là hàm không có guard |
| EP-16 | `save-setting-message` (POST) | SCR-SLN-04 | nt | nt — đồng thời là 1 trong 22 lời gọi `validateInput` |
| EP-29 | `api/course/{id}/delete` | SCR-SLN-03 | nt | `find()` → `where(id)->where(bot_id)` (1 trong 15 chỗ đổi) |
| EP-30 | `api/staff/{id}/delete` | SCR-SLN-03 | nt | `deleteCalendarStaff` — Dev kê là hàm không có guard |
| EP-32 | `booking/delete` | SCR-SLN-02a | nt | Query booking bị thêm ràng buộc bot |
| EP-33 | `export-csv-staff` | SCR-SLN-02 | nt | ⚠️ `NEW-45` **không** cover EP này — NEW-45 là CSV lịch sử đồng bộ Google, khác hẳn |
| EP-39 | `get-list-step-remind` | SCR-SLN-04 | nt | Dev nêu đích danh: 1 trong 2 chỗ deref `$calendar` không guard |
| EP-40 | `save-setting/remind` | SCR-SLN-04 | nt | Dev nêu đích danh (`saveCreateSettingRemind`) |
| EP-41 | `get-list-courses-by-calendar` | SCR-SLN-04 | nt | Dev kê là hàm không có guard |
| EP-46 | `save-staff-assignment` | SCR-SLN-02b | nt | Đổi nhân viên phụ trách booking — query booking + staff |
| EP-51 | `save/info` — thông tin cửa hàng | SCR-SLN-04 | nt | Cùng nhóm `getDataSettingPolicy` mà Dev kê |
| EP-53 | `get-list-booking-delete` | SCR-SLN-02a | nt | Query booking đã xoá |
| EP-54 | `change-display-line-name` | SCR-SLN-02a | nt | Query booking |
| EP-58 | `staff/update-time-working` | SCR-SLN-02d | nt | Cùng nhóm ca làm với `addWorkScheduleForStaff` (endpoint ĐÃ đổi 200→400) |
| EP-17 | `init-data-setting-payment` | SCR-SLN-05 | **`Basic\SettingPaymentCalendarSalonController`** | ⚠️ Controller **KHÔNG** trong diff, nhưng `setting-payment.js` **CÓ** → lệch tầng, xem `G1` |
| EP-18 | `save-setting-payment` | SCR-SLN-05 | nt | nt |

> **Cảnh báo cách đọc con số 44/63**: "có TC chạm" ≠ "đã kiểm ở tầng API". Chỉ **27/81 TC** thuộc nhóm `api` (thật sự tự dựng request); 51 TC còn lại chạm endpoint gián tiếp qua thao tác UI. Lớp `Api\CalendarSalonController` (LIFF khách + app mobile) **không** nằm trong 63 endpoint này và chỉ có `NEW-61` kiểm 3 lời gọi.

### 1c. Màn/tab admin chưa có TC Normal (trả lời câu hỏi "GUI còn chạy bình thường chưa")

Trong 81 TC: **51 TC nhóm `ui`**, nhưng chỉ **22 TC là `ui` + `Normal`** — tức chỉ 22 TC thật sự trả lời "thao tác trên GUI chạy bình thường". Trong 22 TC đó: **17 pass · 4 fail (`NEW-39` · `NEW-41` · `NEW-57` · `#18308`/`#18310`) · 1 skip (`NEW-46`)**.

| Màn / tab (theo `feature-spec.md` §2) | TC `ui` + `Normal` | Trạng thái |
|---|---|---|
| SCR-SLN-01 Danh sách lịch salon | `NEW-1` · `NEW-7` · `NEW-10` · `NEW-38` | ✅ có |
| SCR-SLN-02b 「予約カレンダー」 lưới lịch | `NEW-40` (mới xem lịch theo tuần) | ⚠️ mỏng — không TC nào cho lưới **ngày** và **tháng** |
| SCR-SLN-02d 「シフト」 ca làm | `NEW-53` · `NEW-57 (fail)` | ⚠️ 1/2 fail |
| SCR-SLN-03 「コース・スタッフ」 | `NEW-13` · `NEW-39 (fail)` · `#18308/#18309/#18310` | ⚠️ 3/5 fail |
| SCR-SLN-04 「Googleカレンダー連携」 | `NEW-43` · `NEW-45` · `NEW-46 (skip)` · `NEW-64` · `#18344` · `#18345` | ✅ có |
| 予約システムの削除 | `NEW-41 (fail)` | ⚠️ fail |
| 予約カレンダー — chi tiết đơn & hoàn tiền | `NEW-48` | ✅ có |
| **SCR-SLN-02a 「本日／新着の予約」** (danh sách booking · filter · phân trang · リクエスト一括操作 · đơn đã xoá) | **không có** | ❌ **0 TC** |
| **SCR-SLN-05 「決済連携」** | **không có** | ❌ **0 TC** |
| **SCR-SLN-04 「予約・キャンセルのメッセージ」** | **không có** | ❌ **0 TC** |
| **SCR-SLN-04 「リマインドメッセージ」** | **không có** | ❌ **0 TC** |
| **SCR-SLN-04 「店舗情報・利用規約」** | **không có** | ❌ **0 TC Normal** (chỉ `NEW-29` nhánh chéo bot) |
| **「予約ページの非表示」 (filter)** | **không có** | ❌ **0 TC** |
| **「Googleスプレッドシート連携」** | **không có** | ❌ **0 TC** (xem `Q19` §2) |
| **Admin thêm booking thủ công** | `NEW-46` **skip** | ❌ không có kết luận |
| Trang đặt lịch LIFF của khách | `#18369` | ✅ có (1 TC) |
| **App mobile admin** | **không có** | ❌ **0 TC** — `NEW-61` chỉ gọi API, `exec_mode=auto`, không thao tác trên app |

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `41 quan điểm Trigger khớp task · 23 chưa cover đủ`

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `JOB-001` | `Cao` | `GAP thực chất — TC duy nhất NEW-73 bị SKIP; 8 Console Command + 7 Job dùng chung 3 service không TC nào chạm. Trigger: "× chỉ khi hoàn toàn không có xử lý nền" — task có job đồng bộ Google` | `[BLOCKER]` |
| Q2 | `DEPLOY-LIVE-001` | `Cao` | `GAP — 0 TC. Trigger: release không bật maintain + ĐỔI payload API (mã HTTP ~22 endpoint) + thêm field required` | `[BLOCKER]` |
| Q3 | `PERM-003` | `Cao` | `GAP — 0 TC mang mã này. Chỉ được cover bởi mã lạ AUTH-SESSION-001 (NEW-38, NEW-63) → không tính cover. Trigger: tổ chức vận hành nhiều LINE OA — chính là nội dung fix` | `[BLOCKER]` |
| Q4 | `FUNC-002` | `Cao` | `GAP — 0 TC mang mã này. 13 TC validate đều mang mã lạ (I18N-001 ×8, TOOL-VAL2-001, API-001) → không tính cover. Trigger: mọi chức năng có form nhập liệu` | `[BLOCKER]` |
| Q5 | `ENV-003` | `Cao` | `GAP — 0/81 TC chạy staging hoặc production (4 run đều env local). Task chạm thanh toán (hoàn tiền Stripe/Univapay) + job nền + file CSV. Trigger ghi rõ: KHÔNG được đánh × với lý do "staging đã pass" (RULE-08)` | `[BLOCKER]` |
| Q6 | `INTG-CAL-001` | `Cao` | `RISK — 3 TC nhưng 2 SKIP (NEW-46, NEW-73), chỉ NEW-43 pass. Toàn Normal: thiếu Abnormal + Boundary (RULE-01), không ghi lý do` | `[BLOCKER]` |
| Q7 | `DATA-AUDIT-001` | `Cao` | `RISK — 0 TC mang mã này. NEW-48 cover nhánh GHI lịch sử; thiếu nhánh ĐỌC 操作履歴 của đơn + 「決済利用 変更履歴」 sau khi siết bot (2/3 bảng nhóm không có bot_id)` | `[MAJOR]` |
| Q8 | `NOTI-MAIL-001` | `Trung bình → BẮT BUỘC (nâng Cao — mail chứa mã xác thực)` | `RISK — 0 TC mang mã này. NEW-42 (nhập sai mã) + NEW-47 (lỗi gửi mail → 500, FAIL) không verify mail THẬT trong hộp thư (RULE-06). BR-07 xoá lịch = 2 bước xác thực qua email` | `[MAJOR]` |
| Q9 | `CONC-001` | `Cao` | `GAP — 0 TC. Trigger: nút thực thi hành động quan trọng (nút 返金, nút xoá lịch). Fix thêm guard 404 vào orderRefund → lần bấm thứ 2 nay đổi hành vi` | `[MAJOR]` |
| Q10 | `DATA-DB-001` | `Cao` | `RISK — 5 TC, 3 FAIL, 0 Boundary (RULE-01). Thiếu case bắt buộc của RULE-07: "tạo bản ghi TRÙNG TÊN ở 2 tài khoản để kiểm chứng WHERE"` | `[MAJOR]` |
| Q11 | `DATA-REF-001` | `Cao` | `RISK — 0 TC mang mã này. Cascade delete cover bởi DATA-DB-001 nhưng 3/3 FAIL. Trigger: đối tượng có xoá/copy VÀ được nơi khác tham chiếu. Kho đã có TC-SLN-356` | `[MAJOR]` |
| Q12 | `SEC-001` | `Cao` | `RISK — 7 TC nhưng 3 FAIL + 2 chưa chạy (NEW-74, NEW-75); toàn Abnormal, thiếu Normal + Boundary (RULE-01)` | `[MAJOR]` |
| Q13 | `SEC-002` | `Cao` | `RISK — 1 TC (NEW-33). Trigger: xử lý credential/token. 0 TC verify token Google OAuth trong b_c_salon_google_calendar không bị lộ qua response chéo bot` | `[MAJOR]` |
| Q14 | `REG-RUN-001` | `Cao` | `GAP — 0 TC. Trigger: mọi release khi hệ thống có job/dữ liệu đang chạy dở. Task có job sync Google + đơn đang chờ webhook Univapay (NEW-52 FAIL)` | `[MAJOR]` |
| Q15 | `DEPLOY-ASSET-001` | `Cao` (Cao tuyệt đối — file sửa nằm trong luồng thanh toán) | `RISK — 1 TC Normal (NEW-12). File bị sửa gồm setting-payment.js + calendar-management.js (luồng hoàn tiền) → trigger nâng "Cao tuyệt đối". Thiếu Abnormal (browser cache bản JS cũ) + Boundary` | `[MAJOR]` |
| Q16 | `PAY-STATE-001` | `Cao` | `RISK — 1 TC Normal (NEW-48). Thiếu Abnormal + Boundary bằng mã này; 0 TC production (RULE-08). 0 TC hoàn tiền đơn 現地決済 (không qua cổng)` | `[MAJOR]` |
| Q17 | `INTG-HOOK-001` | `Cao` | `RISK — 1 TC (NEW-52) FAIL, thiếu Normal + Boundary (RULE-01). Trigger BẮT BUỘC: nhận webhook thanh toán` | `[MAJOR]` |
| Q18 | `COMPAT-LEGACY-001` | `Cao` | `RISK — 3 TC, 2 FAIL (NEW-9, NEW-39), thiếu Boundary (RULE-01). RULE-09: dữ liệu cũ bot_id NULL + bản ghi cũ vượt giới hạn độ dài mới là 2 nhánh legacy chính, chưa có case ở đúng BIÊN` | `[MAJOR]` |
| Q19 | `INTG-SHEET-001` | `Cao` | `GAP — 0 TC. calendar_salon là 1 trong 11 bảng bị lọc thẳng where(bot_id) và là nơi giữ google_sheet_id + google_sheet_access_token (feature-spec.md:469 · :747) → trạng thái liên kết Spreadsheet đọc qua query đã bị đổi` | `[MAJOR]` |
| Q20 | `STATE-DEP-001` | `Cao` | `RISK — 1 TC (NEW-55). 0 TC verify remind / action ĐÃ LÊN LỊCH bị dọn đúng khi xoá lịch (BR-07 cascade gồm Events type=5 + MobileNotify) hoặc khi xoá ca` | `[MAJOR]` |
| Q21 | **Gộp — RULE-01 thiếu loại case (không ghi lý do)** ở 6 quan điểm Cao còn lại: `FUNC-001` (0 Boundary) · `FUNC-004` (0 Normal) · `OUT-TRUTH-001` (0 Normal, 0 Boundary) · `PERM-002` (0 Normal, 0 Boundary) · `REG-SHARED-001` (0 Abnormal, 0 Boundary) · `DATA-001` (0 Abnormal, 0 Boundary) | `Cao` | `RISK hình thức — nội dung đã được cover bởi TC mang mã khác trong cùng bộ; không đề xuất TC riêng cho từng mã. Yêu cầu member ghi lý do thiếu loại case vào Ghi chú (RULE-01)` | `[MINOR]` |

| Q22 | `LIST-001` | `Trung bình` (→ Cao: màn có filter + phân trang) | `GAP — 0 TC. Trigger BẮT BUỘC: mọi màn danh sách có search/filter/pagination. Màn 「本日／新着の予約」 (danh sách booking + lọc + phân trang + thao tác hàng loạt + đơn đã xoá) không TC nào chạm, trong khi query danh sách booking nằm trong 78 query bị thêm ràng buộc bot` | `[MAJOR]` |
| Q23 | `PERM-001` | `Cao` | `GAP — 0 TC. Trigger: chức năng có phân biệt quyền. Màn 「予約ページの非表示」 (chặn khách theo điều kiện lọc) không TC nào chạm; kho đã có TC-SLN-350/TC-SLN-351` | `[MAJOR]` |
| Q24 | `BULK-001` | `Cao` | `GAP — 0 TC. Trigger BẮT BUỘC: tổ hợp bộ lọc + thao tác hàng loạt. Màn 「本日／新着の予約」 có 「リクエスト一括操作」 (đổi trạng thái hàng loạt theo bộ lọc) — endpoint changeStatusBooking còn là 1 trong 2 chỗ CỐ Ý giữ mã 200` | `[MAJOR]` |

> Quan điểm **× (không áp dụng)**: `DATA-MIG-001` (không đổi cấu trúc bảng) · `MEDIA-*` (không chạm upload) · `MSG-003`/`MSG-004`/`MSG-005` (không phải chức năng gửi tin hàng loạt).
> ⚠️ `PERM-001` **không còn** ở nhóm × — lý do loại trước đó ("lớp phân quyền theo role không bị chạm code") chỉ đúng cho phân quyền **tài khoản staff**, không đúng cho màn 「予約ページの非表示」 (phân biệt quyền của **khách**). Giới hạn về phân quyền theo role vẫn giữ nguyên ở `I10` §5 và `TC-PERM003-01` §7.

---

## 3. TC trùng lặp nội dung

| Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| DUP-1 | `NEW-71` | `NEW-75` → **gộp** | `DUP-SUBSET` | `SEC-001` × `Abnormal` × `cập nhật khoá học của bot khác qua API` × tiền đề tương đương (đăng nhập bot A, bot B có course dữ liệu đặc trưng) → expected tương đương ("không thay đổi được dữ liệu bot B"). Yếu tố phân biệt mà NEW-75 nêu ("ID có cùng format với course bot A") không phải yếu tố thật — ID ở cả 2 bot đều là số nguyên | `[MINOR]` |
| DUP-2 | `#18310` | `#18308` → **gộp** | `DUP-SUBSET` | Cùng defect (`copy khoá học → đồng bộ vào staff có `is_all_course=1``) × `Normal` × cùng thao tác copy khoá × tiền đề tương đương. Khác nhau chỉ ở màn quan sát: `#18308` xem ở 「コース」 list, `#18310` xem ở màn quản lý nhân viên → đề nghị **gộp 2 bước quan sát vào 1 TC** thay vì xoá | `[MINOR]` |

- **Gate đã chạy**: giả định xoá `NEW-75` + `#18308` → coverage §1 + quan điểm §2 còn nguyên? `Có` — `NEW-71` vẫn cover SEC-001/REQ-004, `#18310` vẫn cover DATA-001/REQ-005. `#18308` mang mã lạ `TOOL-KNOW-002` (không tính cover quan điểm nào). **Tuy vậy vẫn đề nghị GỘP thay vì XOÁ** vì mỗi TC đang gắn 1 bug riêng (1116 / 1117) — xoá sẽ mất đường trace về bug.
- `DUP-INFLATE` che GAP: `Không có` — không nhóm trùng nào làm một quan điểm trông như đủ 3 loại case.
- Đã rà `81` TC theo 4 yếu tố. Các cặp **KHÔNG** tính là trùng (giữ cả 2, có lý do): `NEW-27` (UI mở chi tiết lịch bot khác) vs `NEW-28` (API lấy lịch theo mã) — khác **tầng** UI/API, đúng bài học "UI ẩn nhưng API không enforce" · `NEW-34`/`NEW-35` (ĐỌC màn sửa chéo bot) vs `NEW-71`/`NEW-74` (GHI chéo bot) — khác thao tác đọc/ghi · `NEW-49` vs `NEW-50` (Stripe vs Univapay) — 2 trigger khác nhau, cần giữ đủ ≥3 trigger cho nhánh generic catch · `NEW-56` (xoá ca thiếu danh sách → 422) vs `NEW-57` (lưu khi không còn dòng ca → phải thành công) — 2 endpoint khác nhau, REQ-008 phân định rõ.
- Xoá / gộp thật do human thực hiện trên Studio (`testcase_delete` / `testcase_update`).

---

## 4. Mâu thuẫn trong TCs

| # | Loại | TC liên quan | Nội dung check trùng nhau | Expected A | Expected B / nguồn đối chiếu | Khả năng sai | Severity | Ai chốt |
|---|---|---|---|---|---|---|---|---|
| C1 | `CONF-SPEC` | `NEW-4`, `NEW-8`, `NEW-9` | Giới hạn độ dài 店舗名 (`calendar_salon.calendar_name`) khi tạo / sửa / lưu lại lịch salon | `31 ký tự bị chặn, hiện 「店舗名は30文字以内で入力してください。」` (NEW-4 bộ 2) — tức giới hạn **30** | `spec-features/admin/salon-booking/feature-spec.md:747` §4 Field Traceability Matrix dòng 1: `calendar_salon.calendar_name` — **varchar(100)**, bắt buộc | **(1) TC sai** — fix đặt `max:30` chặt hơn cả cột DB và spec, là thay đổi hành vi ngoài phạm vi "validate required" mà ticket yêu cầu · **(2) spec cũ** — UI vốn đã `maxlength=30`, fix chỉ đồng bộ server-side, spec chưa cập nhật. Bug 1112 (High) ghi "màn hình tự cắt chuỗi ở giới hạn mới: ô 店舗名 chỉ giữ 30 ký tự" | `[BLOCKER]` — trái đúng rule thuộc root cause `BUG` mục 2 (validate đầu vào) | `Dev / PM` |
| C2 | `CONF-SPEC` | `NEW-8`, `NEW-15` | Giới hạn độ dài カレンダー管理名 (**10**) và メニュー名 (**50**) | `11 ký tự 管理名 bị chặn` · `51 ký tự メニュー名 bị chặn` | `feature-spec.md` §4 Field Traceability Matrix **KHÔNG có** 2 field này; field cùng họ `calendar_salon_course.course_name` được ghi là **varchar(100)** (dòng 32) | **(1) TC sai** — 2 giới hạn này không có nguồn spec nào, đang tự suy từ implementation · **(2) spec thiếu field** — cần bổ sung 2 dòng vào Field Traceability Matrix | `[MAJOR]` | `Dev / PM` |
| C3 | `CONF-KHO` | `NEW-4`, `NEW-8`, `NEW-9`, `NEW-15` | Chính 2 giới hạn ở C1 + C2 | Expected của 4 TC coi giới hạn 30/10/50 là **chuẩn đã chốt** | `kho-tcs/fa020-datlichsalon-サロン・面談予約.md` — **MT-32** 「店舗名: 30 hay 100 ký tự?」 **⏳ CHỜ QUYẾT ĐỊNH** · **MT-05** 「Tên course vượt độ dài: báo lỗi, tự cắt, hay cho 100 ký tự?」 **⏳ CHỜ QUYẾT ĐỊNH` | **(1) TC sai** — đang phán Đạt/Không đạt trên một chuẩn kho ghi là chưa chốt · **(2) kho cần update** — nếu fix này chính thức hoá 30/10/50 thì MT-32 + MT-05 được đóng theo giá trị đó. Hệ quả nặng hơn: `NEW-9` cho thấy bản ghi CŨ (店舗名 40 ký tự · 管理名 15 ký tự) **không còn lưu lại được** — cần chốt hành vi với dữ liệu cũ: chặn / tự cắt / cho qua | `[MAJOR]` | `Leader / PM` |

**Đã rà**: `81` TC × `spec-features/admin/salon-booking/feature-spec.md` (§1.4 Hạn chế theo plan · §4 Field Traceability Matrix · §5 BR-01→BR-12) + `kho-tcs/fa020-datlichsalon-サロン・面談予約.md` (498 TC, 42 mục MT) → phát hiện **C1 · C2 · C3**.

Các điểm đã kiểm và **KHÔNG** mâu thuẫn: `NEW-32` (chéo bot KHÔNG được xoá liên kết Google) vs `BR-10` + kho `TC-SLN-250` (ẩn staff cùng bot THÌ xoá liên kết) — khác phạm vi bot, nhất quán · `NEW-55` vs `BR-12` (không xoá ca còn 予約確定) — khớp · `NEW-6`/`NEW-18`/`NEW-19`/`NEW-25` vs `BR-01`/`BR-02` + §1.4 — khớp, và tiền đề NEW-6 dùng đúng cách đọc của BR-01 ("free: 1 lịch cá nhân + 1 lịch nhiều nhân viên") nên **không** vướng mâu thuẫn nội tại của spec ở MT-01 · `NEW-61`/`NEW-62` (lớp Api giữ 200) vs `REQ-001` ("không nhánh lỗi nào trả 200") — được phân định bằng phạm vi controller (`Basic` vs `Api`), REQ-010/REQ-011 carve-out rõ.

- Mỗi dòng trên có 1 dòng tương ứng ở **§8**.
- C1 + C3 làm vùng `BUG` (validate đầu vào) và quan điểm `FUNC-004` / `COMPAT-LEGACY-001` **mất chuẩn đánh giá Đạt–Không đạt** → đã thêm `Q18` ở §2 và `TC-COMPATLEGACY001-01` ở §7.

---

## 5. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | Nguồn — toàn bộ 81 TC | Chỉ **53/81 = 65% pass**. 24 fail + 3 skip + 3 error + 2 chưa chạy → **28 TC không có kết luận Đạt**. Ngưỡng 0.6#1: pass < 80% = `[MAJOR]`, < 50% = `[BLOCKER]`; 65% nhưng **23 bug đang mở** (5 mức High) trong đó 4 bug High đúng vùng cách ly chéo bot | Không ký duyệt ticket ở tỷ lệ này. Xử 23 bug → chạy lại toàn bộ → review lại §1 |
| I2 | `[BLOCKER]` | `NEW-65` | TC **FAIL nhưng CHƯA raise bug** trên Studio (23 bug cho 24 TC fail; đã đối chiếu `testcase_ref` của cả 23 bug). NEW-65 = "Xoá lịch salon có dữ liệu con cũ `bot_id` NULL phải dọn sạch đúng" — đúng giao điểm 2 rủi ro Dev tự nêu (bảng nullable + cascade 16 câu) | Raise bug cho NEW-65 trước khi chuyển vòng review tiếp |
| I3 | `[BLOCKER]` | Nguồn — môi trường chạy | **81/81 TC chỉ chạy env `local`** (4 run: #1752, #1791, #1809, #1834; `envAuto[]` cho dev/staging/prd đều `runs=0`). Task chạm **bill tiền** (hoàn tiền Stripe/Univapay), **job nền** (đồng bộ Google Calendar), **file** (export/import CSV ca làm) → RULE-08 / ENV-003 cấm kết luận từ local–staging | Bổ sung run staging + production cho nhóm hoàn tiền · job đồng bộ · CSV. Kho đã có sẵn `TC-SLN-150` (hoàn tiền production) + `TC-SLN-412` (thanh toán production) để dùng lại |
| I4 | `[MAJOR]` | Nguồn — 23 bug đang mở | **23/23 bug có `redmine_id = null`** → chưa đẩy về Redmine. Dev đang thấy ticket ở trạng thái "Fix done - Đợi test" mà không thấy 23 bug này | Đẩy bug về Redmine (hoặc gộp vào #39566) trước khi giao lại Dev |
| I5 | `[MAJOR]` | Nguồn — run đang chạy | Run **#1834** (env local) bắt đầu `2026-09-22 02:55:15`, `status = running`, chưa có `finishedAt` → **snapshot 53/24/3/3 có thể đổi ngay sau khi run xong** | Chờ #1834 kết thúc rồi chốt lại số ở §1 trước khi gửi report cho Dev |
| I6 | `[MAJOR]` | 81/81 TC | Cột `Trạng thái đánh giá spec` (`spec_status`) **rỗng toàn bộ** — không TC nào ghi `Spec ghi rõ` / `Spec không ghi` / `Đã hỏi leader`. Trùng khớp với §4: có 3 mâu thuẫn spec chưa ai chốt mà TC vẫn phán Đạt/Không đạt | Fill `spec_status` cho toàn bộ, **ưu tiên nhóm TC giới hạn độ dài** (NEW-4/8/9/15) → phải là `Spec không ghi` hoặc `Đã hỏi leader`, không được để trống |
| I7 | `[MAJOR]` | `#18308`, `#18310` | 2 TC gốc kho (`kho_id` CS-045 / CS-048) FAIL với bug **1116** + **1117** mô tả lỗi đồng bộ `staff.is_all_course=1` khi **copy khoá học** — nội dung này **không nằm trong diff** của #39566 (`spec_delta.files[]` không có file nào của luồng copy khoá) | Triage với Dev: nếu là bug cũ sẵn có → **tách ticket riêng**, không chặn #39566. Nếu là regression của fix → bổ sung vào mục 4 file 03 |
| I8 | `[MAJOR]` | `NEW-61` | Expected khẳng định *"Ứng dụng di động vẫn đọc được thông báo lỗi và **không bị treo vòng quay tải**"* nhưng steps chỉ gọi 3 endpoint API, `exec_mode = auto`, không có thiết bị thật → vi phạm **RULE-06** (output cuối trên thiết bị thật). Đây đúng là rủi ro Dev tự nêu ở mục 6 VERIFY (`reservation.dart:906` gọi `addWorking()` không có `catchError`) | Tách phần khẳng định về app sang TC manual trên app admin thật — đã đề xuất `TC-SYNCAPP001-01` ở §7. Giữ NEW-61 chỉ khẳng định phần hợp đồng API |
| I9 | `[MAJOR]` | 12 TC khai `exec_mode = manual` | Kênh manual của Studio có **0 lượt chạy** (`manual.totals` = pass 0 / fail 0 / blocked 0 / pending 0) — 12 TC khai manual thực tế đều có `last_exec` do runner ghi ở env `local`. Riêng `NEW-48`/`NEW-49`/`NEW-50` (hoàn tiền qua cổng thật) và `NEW-46`/`NEW-73` (Google Calendar + job) **không thể có kết luận hợp lệ từ local** | Xác nhận lại 12 TC manual có người chạy thật chưa; nếu chưa → đổi `last_exec` về chưa test thay vì để `pass` |
| I10 | `[MINOR]` | 16 TC chéo bot (REQ-004) | Tiền đề đều là **"1 tài khoản admin quản lý cả bot A và bot B"** (`NEW-38`: *"Một tài khoản admin quản lý cả bot A và bot B"*). Chứng minh được filter `where(bot_id, getBotId())` hoạt động, **KHÔNG** chứng minh biên giới giữa 2 **TÀI KHOẢN KHÁCH** khác nhau | **Không đề xuất TC mới** — lớp chọn bot / authorize account→bot không bị chạm code trong diff. Chỉ ghi giới hạn phạm vi kết luận này vào report test để Leader không hiểu sai là "đã test IDOR chéo khách hàng" |
| I11 | `[MINOR]` | `NEW-70` | Steps dùng **mock response** (*"Mock response lỗi chứa đồng thời message và error_message"*) → chứng minh được thứ tự ưu tiên của `ajax-error.js` nhưng **không** chứng minh server thật trả đúng khoá `error_message` ở 2 endpoint đã đổi mã | Ghi liên kết sang `NEW-54` (thêm ca làm, server thật, đang FAIL — bug 1122 ghi "màn hiện \"\"") để cặp TC này bổ trợ nhau; hoặc thêm 1 bước gọi endpoint `orderRefund` thật vào NEW-70 |
| I12 | `[MINOR]` | 79/81 TC | `env_scope = ['all']` nhưng `envAuto[]` cho thấy **0 lượt chạy** ở dev/staging/prd → khai phạm vi env **không đồng nghĩa đã chạy** ở env đó | Khi đọc coverage môi trường phải dựa `envAuto[].runs`, không dựa `env_scope`. Liên quan I3 |
| I13 | `[NIT]` | Nguồn — độ phủ spec Studio | `task_get_report.coverage` ghi **0/83 covered · 82 partial · 1 none** (`source = investigate`) trong khi 12/12 REQ đều có ≥1 TC. Con số 0 covered đến từ tầng spec item, không phải REQ | Hỏi owner Studio vì sao 0 covered — nếu là lỗi tính thì bỏ qua, nếu thật thì 82 spec item đang ở trạng thái phủ một phần |

---

## 6. TCs thừa / ngoài phạm vi task

**Đã rà `81` TC — không có TC nào ngoài phạm vi task.**

Chi tiết các TC **ban đầu nghi thừa** nhưng đã qua gate và **được giữ**:

| TC nghi thừa | Vì sao GIỮ (gate) |
|---|---|
| `#18308`, `#18310` (copy khoá → staff `is_all_course`) | Map được `REQ-005` (ràng buộc bot không giấu mất dữ liệu) + là TC regression **dẫn được từ kho** (`kho_id` CS-045/CS-048) → gate "TC regression dẫn từ kho-tcs KHÔNG phải TC thừa". Nghi vấn về nguồn gốc defect đã ghi ở **I7 §5** thay vì flag thừa |
| `#18369` (luồng friend thật đặt lịch salon) | Map `REQ-010` — 3 service bị sửa dùng chung với `Mobile\CalendarSalonController` (màn đặt chỗ của KHÁCH). Chính là rủi ro Dev tự nêu ("làm CHẾT màn đặt chỗ của khách") |
| `#18344`, `#18345` (lịch sử đồng bộ theo ngày / theo phút) | Map `D3`/`D4` — `sync_booking_google_calendar_histories` (bot_id NULL) + `b_c_salon_google_calendar_histories` (không có bot_id) đều bị đổi cách truy vấn |
| `NEW-62` (2 endpoint cố ý giữ 200) | Map `REQ-011` — Dev yêu cầu tester ghi nhận hiện trạng để không báo bug nhầm |
| `NEW-63` (session không có bot) | Kiểm đúng nhánh `getBotId()` trả null mà Dev bàn dài ở mục 2 (lý do chọn `getBotId()` thay `getBotIdInScope()`) |
| `NEW-67` (field chỉ chứa khoảng trắng) | Kiểm đúng rule `required` mới thêm (Laravel trim → whitespace thành rỗng) |
| `NEW-12` (asset nạp trên mọi màn) | 12 blade trong `spec_delta.files[]` đều chỉ đổi dòng `?v=` → đúng điểm sửa trong diff |

**Không** có TC nào test layer không bị chạm code: đã kiểm `T2` Lesson Booking (FA-019) và `T3` Event Booking (FA-021) — Dev khai KHÔNG sửa, và **0/81 TC** chạm 2 tính năng này → bộ TC không mắc lỗi over-coverage layer downstream.

---

## 7. TCs đề xuất bổ sung (47)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa020-datlichsalon-サロン・面談予約.md` (498 TC · 42 mục MT · coverage 51 nhóm chức năng) |
| Vùng regression phát hiện từ kho | `TC-SLN-356` (cascade xoá calendar) · `TC-SLN-354/355` (2 bước xác thực mã email) · `TC-SLN-250` (ẩn staff → xoá liên kết Google, BR-10) · `TC-SLN-213` (multi-tenant sửa ca bot A không ảnh hưởng bot B) · `TC-SLN-248/255` (URL detail course/staff bằng bot khác) · `TC-SLN-464` (đổi bot khi đang mở màn calendar, kể cả 2 tab) · `TC-SLN-479/480/481` (phân quyền staff trên TẤT CẢ màn con salon) · `TC-SLN-148` (hoàn tiền "chỉ đổi trạng thái" — 現地決済) · `TC-SLN-150`/`TC-SLN-412` (hoàn tiền / thanh toán trên PRODUCTION) · `TC-SLN-377→388` (Googleスプレッドシート連携, 15 TC) · `TC-SLN-488` (UI load trước release, submit sau release) · `TC-SLN-498` (version asset JS/CSS màn salon sau release) · `TC-SLN-214→216` (export CSV ca làm) |
| Conflict expected vs kho | `NEW-4`/`NEW-8`/`NEW-9`/`NEW-15` vs `MT-32` + `MT-05` → **`C3` CONF-KHO**, đã đưa §4 + §8 |
| GAP dùng lại TC kho (không viết mới) | `G17` (RULE-08 hoàn tiền production) → dùng lại **`TC-SLN-150`** "Hoàn tiền trên môi trường PRODUCTION với cả Stripe và UnivaPay" — cần **bổ sung 2 bước**: ghi lại mã HTTP thực nhận ở nhánh cổng từ chối (phải 400, không 200) và ở nhánh lỗi hệ thống (phải 500) · `Q5` phần thanh toán → dùng lại **`TC-SLN-412`** "Thanh toán trên môi trường PRODUCTION với Stripe và UnivaPay" |
| Căn cứ TC regression `R<x>` | `R1` ← `dev_impact`: *"thêm ràng buộc bot cho 78 query + 15 chỗ find()→where(id)->where(bot_id)->first()"* (rủi ro hiệu năng subquery) · `R2` ← `dev_impact`: *"Dùng getBotId() chứ không phải getBotIdInScope()"* + kho `MT-35` "Phân quyền tài khoản Staff: CHƯA từng được test" + `TC-SLN-479/480/481` · `R3` ← kho `TC-SLN-464` (đổi bot khi đang mở màn calendar, 2 tab) · `R4` ← kho `TC-SLN-148` + `MT-12` (現地決済 thiếu hoàn toàn trong spec) · `R5` ← kho `TC-SLN-214→216` (export CSV ca làm) · `R6` ← `dev_impact`: *"12+ file JS/blade còn lại chủ yếu thêm ?v= cho asset mới"* + kho `TC-SLN-498` |
| Xác nhận chống trùng | Đã đối chiếu `81` TC ở BƯỚC 0 + `498` TC kho theo 4 yếu tố — **không TC đề xuất nào trùng** |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-PAYSTATE001-01 | API | PAY-STATE-001 | 決済連携 — cài đặt | Normal | auto | Tất cả | Lưu cài đặt thanh toán hợp lệ vẫn thành công và ghi lịch sử | Bot A có lịch salon đã liên kết Stripe (hoặc UnivaPay); form đặt lịch đã bật + bắt buộc cả trường tên và email | 1. Mở lịch salon của bot A → tab 「決済連携」<br>2. Bật thanh toán, chọn môi trường テスト, bấm lưu<br>3. Quan sát thông báo trên màn<br>4. Mở lại tab 「決済連携」 kiểm tra trạng thái đã lưu<br>5. Cuộn xuống section 「決済利用 変更履歴」 kiểm tra bản ghi mới | Thanh toán: bật · Môi trường: テスト | Lưu thành công, thông báo thành công hiện ra, overlay tải tắt. Trạng thái thanh toán sau khi tải lại đúng là đang bật ở môi trường テスト. Section 「決済利用 変更履歴」 có thêm đúng 1 dòng từ 停止 sang テスト利用 | | Lấp `G1` · Đánh giá spec: Spec ghi rõ (BR-05 · BR-06) · Evidence: ảnh màn 決済連携 sau khi tải lại + ảnh section 変更履歴 |
| TC-PAYSTATE001-02 | API | PAY-STATE-001 | 決済連携 — cài đặt | Abnormal | auto | Tất cả | Bật thanh toán khi chưa đủ điều kiện bị chặn và màn vẫn hiện lý do | Bot A có lịch salon **chưa** liên kết cổng thanh toán nào; lịch thứ hai đã liên kết cổng nhưng form đặt lịch **tắt** trường email | 1. Ở lịch chưa liên kết cổng: bật thanh toán rồi bấm lưu, ghi lại mã HTTP và thông báo trên màn<br>2. Ở lịch có cổng nhưng thiếu trường email: bật thanh toán rồi bấm lưu, ghi lại mã HTTP và thông báo<br>3. Bỏ trống môi trường thanh toán rồi bấm lưu, ghi lại mã HTTP và thông báo<br>4. Đối chiếu trạng thái thanh toán trong cơ sở dữ liệu của cả 2 lịch<br>5. Kiểm tra 「決済利用 変更履歴」 không phát sinh dòng mới | Bước 1–2: bật thanh toán · Bước 3: để trống môi trường | Bước 1 và 2 bị chặn với mã **400** kèm thông báo lý do hiện rõ trên màn (không phải 500, không phải 200 im lặng). Bước 3 bị chặn với mã **422** kèm thông báo tiếng Nhật đúng nhãn trên màn. Trạng thái thanh toán của cả 2 lịch trong cơ sở dữ liệu giữ nguyên 停止 và 「決済利用 変更履歴」 không có dòng mới | | Lấp `G1` · Đánh giá spec: Spec ghi rõ (BR-05) · Evidence: ảnh thông báo lỗi + log mã HTTP của 3 lần gọi |
| TC-PAYSTATE001-03 | API | PAY-STATE-001 | Hoàn tiền 返金 | Abnormal | auto | Tất cả | Hoàn tiền đơn thanh toán tại chỗ 現地決済 chỉ đổi trạng thái, không gọi cổng | Bot A có 1 đơn đặt lịch đã thanh toán theo hình thức 現地決済 (không qua cổng). Ghi lại trạng thái thanh toán và số bản ghi lịch sử thao tác trước khi test | 1. Mở tab 「予約カレンダー」 → chi tiết đơn 現地決済<br>2. Bấm thao tác hoàn tiền, chọn kiểu 「chỉ đổi trạng thái」<br>3. Xác nhận trong hộp thoại, ghi lại mã HTTP và thông báo trên màn<br>4. Đối chiếu trạng thái thanh toán của đơn trên màn và trong cơ sở dữ liệu<br>5. Kiểm tra lịch sử giao dịch trên Stripe và UnivaPay: KHÔNG có giao dịch hoàn tiền nào phát sinh<br>6. Kiểm tra bản ghi lịch sử thao tác của đơn | Đơn 現地決済 · kiểu hoàn tiền: chỉ đổi trạng thái | Thao tác trả về mã 200 và thành công. Trạng thái thanh toán chuyển 返金 trên cả màn và cơ sở dữ liệu. **Không** có giao dịch nào được gửi tới Stripe/UnivaPay. Có thêm bản ghi lịch sử thao tác cho đơn. Không rơi vào nhánh catch trả 500 | | Lấp `R4` · regression · dẫn từ `TC-SLN-148` + kho `MT-12` (現地決済 thiếu hoàn toàn trong spec) · Đánh giá spec: Spec không ghi — cần hỏi leader · Evidence: ảnh chi tiết đơn + ảnh dashboard cổng chứng minh không có refund |
| TC-DATAAUDIT001-01 | Data | DATA-AUDIT-001 | 決済連携 — 決済利用 変更履歴 | Normal | auto | Tất cả | Lịch sử đổi cài đặt thanh toán hiện đủ bản ghi của lịch đang xem sau khi siết bot | Bot A có lịch salon với ít nhất 3 bản ghi lịch sử đổi cài đặt thanh toán (trong đó ≥1 bản ghi tạo từ trước bản fix). Bot B cũng có lịch salon có lịch sử riêng. Ghi lại số bản ghi của từng lịch | 1. Đăng nhập bot A, mở tab 「決済連携」 của lịch bot A<br>2. Đếm số dòng ở section 「決済利用 変更履歴」, đối chiếu với số đã ghi<br>3. Kiểm tra nội dung từng dòng (from → to, thời điểm, người thao tác)<br>4. Đổi sang bot B, mở lịch bot B, đếm lại số dòng lịch sử<br>5. Xác nhận lịch sử của 2 bot không lẫn vào nhau | — | Lịch sử của lịch bot A hiện **đủ** số bản ghi đã ghi trước test, kể cả bản ghi cũ (bảng `calendar_salon_history_change_setting_payment` không có cột `bot_id`, ràng buộc gián tiếp qua `calendar_id` → không được giấu mất dòng nào). Nội dung từng dòng đúng. Lịch sử bot B không xuất hiện ở bot A và ngược lại | | Lấp `G4` + `Q7` · Đánh giá spec: Spec ghi rõ (BR-06) · Evidence: ảnh section 変更履歴 của cả 2 bot + số dòng đếm được |
| TC-DATAAUDIT001-02 | Data | DATA-AUDIT-001 | 予約カレンダー — chi tiết đơn & lịch sử | Normal | auto | Tất cả | Lịch sử thao tác của đơn đặt lịch hiện đủ sau khi siết bot | Bot A có 1 đơn đặt lịch đã qua nhiều thao tác (đổi trạng thái, hoàn tiền) với ≥3 bản ghi lịch sử thao tác, trong đó ≥1 bản ghi tạo trước bản fix | 1. Mở chi tiết đơn của bot A<br>2. Mở khu vực lịch sử thao tác, đếm số bản ghi<br>3. Đối chiếu từng bản ghi với dữ liệu đã ghi trước test<br>4. Thực hiện thêm 1 thao tác đổi trạng thái, kiểm tra bản ghi mới được thêm | — | Lịch sử thao tác hiện **đủ** tất cả bản ghi của đơn, kể cả bản ghi cũ (bảng `line_booking_history_actions` chỉ có `booking_id`, ràng buộc qua bảng cha) — không dòng nào bị giấu mất. Thao tác mới tạo thêm đúng 1 bản ghi | | Lấp `G10` + `Q7` · Đánh giá spec: Spec không ghi (BR không mô tả màn lịch sử thao tác đơn) — cần hỏi leader · Evidence: ảnh lịch sử thao tác trước/sau |
| TC-MSG002-01 | API | MSG-002 | 予約・キャンセル メッセージ | Abnormal | auto | Tất cả | Lưu tin nhắn đặt/huỷ lịch thiếu nội dung bắt buộc bị chặn kèm thông báo tiếng Nhật | Bot A có lịch salon đã khởi tạo cài đặt tin nhắn mặc định. Ghi lại nội dung tin nhắn hiện tại | 1. Mở tab 「予約設定」 → mục 「予約・キャンセルのメッセージ・リクエストと締切」<br>2. Xoá trống nội dung tin nhắn đặt lịch, bấm lưu → ghi mã HTTP + thông báo trên màn<br>3. Nhập nội dung vượt giới hạn ký tự, bấm lưu → ghi mã HTTP + thông báo<br>4. Nhập nội dung hợp lệ, bấm lưu → ghi mã HTTP + thông báo<br>5. Tải lại trang, đối chiếu nội dung đã lưu và đối chiếu trong cơ sở dữ liệu | Bước 2: để trống · Bước 3: nội dung vượt giới hạn · Bước 4: nội dung hợp lệ | Bước 2 bị chặn mã **422** kèm thông báo tiếng Nhật dùng đúng nhãn trên màn, nội dung cũ trong cơ sở dữ liệu **không** bị ghi đè. Bước 3 bị chặn 422 kèm thông báo giới hạn ký tự. Bước 4 lưu thành công mã 200 và nội dung sau khi tải lại đúng như đã nhập | | Lấp `G2` + `Q4` · Đánh giá spec: Spec không ghi giới hạn ký tự — cần hỏi leader · Evidence: log mã HTTP 3 lần gọi + ảnh thông báo |
| TC-MSG002-02 | API | MSG-002 | リマインド — cài đặt | Abnormal | auto | Tất cả | Lưu và xoá cài đặt nhắc lịch bằng mã lịch của bot khác bị từ chối | Phiên đăng nhập admin đang chọn bot A. Bot B có lịch salon đã cấu hình ≥2 bước nhắc lịch (1 bước kiểu ngày trước/sau, 1 bước kiểu đếm ngược). Ghi lại toàn bộ cấu hình nhắc lịch của bot B | 1. Gửi yêu cầu lấy danh sách bước nhắc lịch với mã lịch của bot B, ghi mã HTTP và nội dung trả về<br>2. Gửi yêu cầu lưu bước nhắc lịch mới với mã lịch của bot B, ghi mã HTTP<br>3. Gửi yêu cầu xoá 1 bước nhắc lịch của bot B, ghi mã HTTP<br>4. Đổi sang bot B, mở mục 「予約前後に送るリマインドメッセージ」 đối chiếu lại toàn bộ cấu hình<br>5. Kiểm tra không phát sinh bản ghi nhắc lịch mới nào cho lịch bot B | Mã lịch salon của bot B | Cả 3 yêu cầu bị từ chối với mã **404** kèm thông báo lịch không tồn tại, và **không** trả về nội dung cấu hình của bot B. Cấu hình nhắc lịch của bot B giữ nguyên nguyên vẹn, không bước nào bị thêm/xoá/ghi đè | | Lấp `G2` + `G11` (`saveSettingMessage` · `getListStepRemind` · `saveCreateSettingRemind`) · Đánh giá spec: Spec ghi rõ (BR-08) · Evidence: log 3 lần gọi + ảnh cấu hình bot B trước/sau |
| TC-FUNC002-01 | UI | FUNC-002 | 予約設定 — 4 màn cài đặt phụ | Abnormal | auto | Tất cả | Bốn màn cài đặt đặt chỗ thiếu field bắt buộc bị chặn 422 kèm thông báo tiếng Nhật | Bot A có lịch salon đã khởi tạo đủ cài đặt mặc định. Ghi lại giá trị hiện tại của 4 mục: 前後の空き時間 · 店舗とスタッフの受付上限 · 空き枠通知受け取り設定 · シーズン設定 | 1. Vào 「前後の空き時間」: để trống ô số phút rồi lưu → ghi mã HTTP + thông báo<br>2. Vào 「店舗とスタッフの受付上限」: để trống ô giới hạn rồi lưu → ghi mã HTTP + thông báo<br>3. Vào 「空き枠通知受け取り設定」: để trống field bắt buộc rồi lưu → ghi mã HTTP + thông báo<br>4. Vào 「シーズン設定」: để trống ngày bắt đầu/kết thúc rồi lưu → ghi mã HTTP + thông báo<br>5. Nhập chữ vào ô số ở cả 4 màn rồi lưu → ghi mã HTTP + thông báo<br>6. Đối chiếu lại 4 nhóm cài đặt trong cơ sở dữ liệu | Bước 1–4: để trống field bắt buộc · Bước 5: nhập chữ vào ô số | Cả 4 màn đều bị chặn mã **422** kèm thông báo **tiếng Nhật** dùng đúng nhãn hiển thị trên chính màn đó (không lẫn tên field tiếng Anh kiểu `limit_booking`). Bước 5 trả 422 với thông báo dạng "...は数値で入力してください。". Giá trị cũ của cả 4 nhóm cài đặt trong cơ sở dữ liệu **không** bị ghi đè, không phát sinh bản ghi mới | | Lấp `G12` + `Q4` · 4 file JS này nằm trong `spec_delta.files[]` nhưng KHÔNG có trong mục 4.1 của Dev · Đánh giá spec: Spec không ghi — cần hỏi leader · Evidence: log mã HTTP 5 lần gọi + ảnh 4 thông báo |
| TC-FUNC002-02 | UI | FUNC-002 | 予約設定 — 4 màn cài đặt phụ | Normal | auto | Tất cả | Bốn màn cài đặt đặt chỗ lưu giá trị hợp lệ vẫn thành công | Bot A có lịch salon đã khởi tạo đủ cài đặt mặc định | 1. 「前後の空き時間」: nhập số phút hợp lệ rồi lưu<br>2. 「店舗とスタッフの受付上限」: nhập giới hạn hợp lệ cho cả cửa hàng và nhân viên rồi lưu<br>3. 「空き枠通知受け取り設定」: bật nhận thông báo, nhập đủ field rồi lưu<br>4. 「シーズン設定」: nhập khoảng ngày hợp lệ rồi lưu<br>5. Tải lại từng màn, đối chiếu giá trị hiển thị<br>6. Đối chiếu giá trị trong cơ sở dữ liệu của 4 bảng cài đặt | Giá trị hợp lệ cho từng màn | Cả 4 màn lưu thành công mã 200, thông báo thành công hiện ra, overlay tải tắt. Giá trị sau khi tải lại và giá trị trong cơ sở dữ liệu khớp đúng cái đã nhập — rule `required` mới thêm **không** chặn nhầm luồng lưu hợp lệ | | Lấp `G12` · Đánh giá spec: Spec ghi rõ (§2.4.3 · §2.4.4 · §2.4.5) · Evidence: ảnh 4 màn sau khi tải lại |
| TC-FUNC002-03 | UI | FUNC-002 | Toàn bộ màn lịch salon — thông báo validate | Boundary | auto | Tất cả | Rà đủ 22 nhãn field tiếng Nhật của thông báo validate khớp chữ trên màn hình | Bot A có lịch salon đủ dữ liệu để mở được hết các màn con. Chuẩn bị bảng 22 nhãn Dev khai ở file 03 mục 2 (店舗名 · カレンダー管理名 · メニュー名 · コース名 · スタッフ名 · シフト · 並び順 · カレンダー · 予約枠 · アクション · 予約ページでの表示 · 連携するスタッフ · Googleカレンダー) | 1. Với **từng** field trong bảng 22 nhãn: mở màn tương ứng, để trống field đó rồi lưu<br>2. Ghi lại nguyên văn thông báo hiện trên màn<br>3. Mở cùng màn đó, đọc nguyên văn **nhãn hiển thị** của field<br>4. So từng cặp: chữ trong thông báo phải trùng khít chữ trên nhãn màn hình<br>5. Lập bảng 22 dòng: nhãn màn hình / thông báo nhận được / khớp hay không | Để trống lần lượt 22 field | Cả **22/22** thông báo là tiếng Nhật hoàn chỉnh và dùng **đúng chữ trên màn hình**, không có tên field tiếng Anh (kiểu `course menu id`) và không dùng từ khác nghĩa. Đặc biệt field tên nhân viên phải hiện 「スタッフ名」 đúng như nhãn màn hình, **không** phải 「従業員名」 (bug 1120) | | Lấp `G13` + `G14` · 8 TC nhóm I18N hiện chỉ rà ~8/22 nhãn · Đánh giá spec: Spec không ghi (nhãn lấy từ blade, không có trong feature-spec) — cần hỏi leader · Evidence: bảng 22 dòng + ảnh từng thông báo |
| TC-SEC001-01 | API | SEC-001 | 本日／新着の予約 — huỷ đơn | Abnormal | auto | Tất cả | Kiểm tra trước khi huỷ đơn bằng mã đơn của bot khác bị từ chối và không lộ dữ liệu | Phiên đăng nhập admin đang chọn bot A. Bot B có 1 đơn đặt lịch trạng thái 予約確定 với tên khách và số điện thoại đặc trưng dễ nhận. Ghi lại trạng thái đơn và số bản ghi lịch sử của đơn đó | 1. Gửi yêu cầu kiểm tra trước khi huỷ đơn (`checkBookingCancel`) với mã đơn của bot B, ghi mã HTTP và toàn bộ nội dung trả về<br>2. Gửi yêu cầu kiểm tra huỷ theo nhân viên (`checkBookingCancelStaff`) với mã nhân viên của bot B, ghi mã HTTP và nội dung<br>3. Gửi tiếp yêu cầu huỷ đơn thật của bot B, ghi mã HTTP<br>4. Đổi sang bot B, mở chi tiết đơn đó đối chiếu trạng thái và lịch sử<br>5. Kiểm tra nội dung trả về ở bước 1–2 có chứa tên khách / số điện thoại / mã khoá học của bot B hay không | Mã đơn và mã nhân viên của bot B | Cả 3 yêu cầu bị từ chối mã **404** kèm thông báo không tìm thấy. Nội dung trả về **không chứa bất kỳ thông tin cá nhân nào** của khách bot B (không tên, không số điện thoại, không mã khoá học). Đơn của bot B giữ nguyên trạng thái 予約確定, không phát sinh bản ghi lịch sử huỷ | | Lấp `G3` + `G11` + `Q12` · 2 hàm này nằm trong 25 hàm Dev khai "id đến thẳng từ request rồi dùng luôn" · Đánh giá spec: Spec ghi rõ (REQ-004) · Evidence: log nguyên văn response 3 lần gọi + ảnh chi tiết đơn bot B sau test |
| TC-SEC001-02 | API | SEC-001 | スタッフ — xoá nhân viên | Abnormal | auto | Tất cả | Xoá nhân viên của bot khác bị từ chối và bản ghi bot đó còn nguyên | Phiên đăng nhập admin đang chọn bot A. Bot B có ≥2 nhân viên, 1 trong đó đã liên kết Google Calendar và có ca làm + đơn đặt lịch. Ghi lại số nhân viên, số ca làm, số bản ghi liên kết Google của bot B | 1. Gửi yêu cầu xoá nhân viên (`deleteCalendarStaff`) với mã nhân viên của bot B, ghi mã HTTP và thông báo<br>2. Lặp lại với mã nhân viên đã liên kết Google Calendar của bot B<br>3. Đổi sang bot B, mở tab 「コース・スタッフ」 đếm lại số nhân viên<br>4. Kiểm tra ca làm của các nhân viên đó còn đủ<br>5. Kiểm tra bản ghi liên kết Google Calendar của bot B còn nguyên | Mã nhân viên của bot B | Cả 2 yêu cầu bị từ chối mã **404**. Số nhân viên của bot B **không đổi**, ca làm còn đủ, bản ghi liên kết Google Calendar của bot B **không** bị xoá. Không phát sinh thao tác xoá dữ liệu con nào của bot B | | Lấp `G11` + `Q12` · Đánh giá spec: Spec ghi rõ (REQ-004 · BR-09) · Evidence: log 2 lần gọi + ảnh danh sách nhân viên bot B trước/sau |
| TC-SEC001-03 | API | SEC-001 | 予約カレンダー — khung giờ 予約枠 | Abnormal | auto | Tất cả | Lấy chi tiết bước khung giờ bằng mã của bot khác bị từ chối | Phiên đăng nhập admin đang chọn bot A. Bot B có lịch salon đã cấu hình khung giờ 予約枠 với giá trị đặc trưng dễ nhận. Ghi lại cấu hình khung giờ của bot B | 1. Gửi yêu cầu lấy chi tiết bước khung giờ (`getDetailEventStep`) với mã của bot B, ghi mã HTTP và toàn bộ nội dung trả về<br>2. Gửi yêu cầu cập nhật cấu hình khung giờ đó với mã của bot B, ghi mã HTTP<br>3. Gửi yêu cầu xoá khung giờ đó với mã của bot B, ghi mã HTTP<br>4. Đổi sang bot B, mở lại lưới lịch và đối chiếu cấu hình khung giờ | Mã bước / mã khung giờ của bot B | Cả 3 yêu cầu bị từ chối mã **404** và nội dung trả về không chứa cấu hình khung giờ của bot B. Cấu hình khung giờ của bot B giữ nguyên, không bị đổi hay xoá | | Lấp `G9` + `G11` + `Q12` · Đánh giá spec: Spec ghi rõ (REQ-004) · Evidence: log nguyên văn response + ảnh lưới lịch bot B |
| TC-SEC001-04 | API | SEC-001 | Googleカレンダー連携 — xác thực mã email | Abnormal | auto | Tất cả | Gửi mã xác thực liên kết Google bằng mã lịch của bot khác bị từ chối và không gửi mail | Phiên đăng nhập admin đang chọn bot A. Bot B có lịch salon với email quản trị khác email của bot A. Chuẩn bị truy cập được hộp thư của bot B | 1. Gửi yêu cầu gửi mã xác thực (`sendMailCodeAuthGoogleSync`) với mã lịch của bot B, ghi mã HTTP<br>2. Gửi yêu cầu xác thực mã (`checkAuthorGoogleSync`) với mã lịch của bot B kèm mã bất kỳ, ghi mã HTTP<br>3. Kiểm tra hộp thư của bot B: có mail mã xác thực mới nào không<br>4. Đổi sang bot B, kiểm tra trạng thái liên kết Google Calendar còn nguyên | Mã lịch salon của bot B | Cả 2 yêu cầu bị từ chối mã **404**. Hộp thư của bot B **không** nhận được mail mã xác thực nào (không bị dùng để spam hoặc dò lịch). Trạng thái liên kết Google Calendar của bot B không đổi | | Lấp `G11` + `Q8` + `Q12` · Đánh giá spec: Spec ghi rõ (BR-07 · BR-12 quy trình xác thực mã email) · Evidence: log 2 lần gọi + ảnh hộp thư bot B |
| TC-SEC001-05 | API | SEC-001 | データ同期履歴 | Abnormal | auto | Tất cả | Lấy chi tiết một bản ghi lịch sử đồng bộ bằng mã của bot khác bị từ chối | Phiên đăng nhập admin đang chọn bot A. Bot B có ≥1 bản ghi lịch sử đồng bộ Google Calendar chi tiết theo phút với dữ liệu đặc trưng. Ghi lại mã bản ghi đó | 1. Gửi yêu cầu lấy chi tiết lịch sử đồng bộ theo đơn (`getDetailHistorySyncForBooking`) với mã của bot B, ghi mã HTTP và nội dung trả về<br>2. Gửi yêu cầu tải tệp CSV lịch sử đồng bộ với mã lịch của bot B, ghi mã HTTP<br>3. Nếu có tệp tải về thì mở ra kiểm tra nội dung<br>4. Đối chiếu nội dung trả về ở bước 1 với dữ liệu đặc trưng của bot B | Mã bản ghi lịch sử / mã lịch của bot B | Cả 2 yêu cầu bị từ chối mã **404**. Không có tệp CSV nào được tải về. Nội dung trả về không chứa dữ liệu đồng bộ của bot B | | Lấp `G11` + `Q12` · Bổ trợ NEW-36 (đang FAIL — bug 1115 "truy cập chéo bot trả 500") ở mức bản ghi chi tiết · Đánh giá spec: Spec ghi rõ (REQ-004 · REQ-012) · Evidence: log nguyên văn response |
| TC-SEC002-01 | API | SEC-002 | Googleカレンダー連携 | Abnormal | auto | Tất cả | Response chéo bot không để lộ token hay credential Google của bot khác | Phiên đăng nhập admin đang chọn bot A. Bot B có lịch salon và nhân viên đã liên kết Google Calendar (tồn tại bản ghi liên kết kèm token). Bot B cũng đã liên kết Google Spreadsheet | 1. Gửi lần lượt các yêu cầu đọc trạng thái liên kết Google của bot B: kiểm tra đã liên kết, lấy danh sách nhân viên đã liên kết, lấy chi tiết lịch<br>2. Ghi lại **nguyên văn** toàn bộ nội dung trả về của từng lời gọi<br>3. Tìm trong nội dung trả về các chuỗi nhạy cảm: mã lịch Google, access token, refresh token, mã Spreadsheet, email tài khoản Google<br>4. Lặp lại bước 1–3 với phiên không có bot trong session | Mã lịch salon và mã nhân viên của bot B | Mọi lời gọi bị từ chối mã **404** hoặc trả về rỗng. Nội dung trả về **không chứa** bất kỳ chuỗi nào trong nhóm nhạy cảm (mã lịch Google, access token, refresh token, mã Spreadsheet, email tài khoản Google) của bot B — kể cả trong trường thông báo lỗi | | Lấp `Q13` · Bảng `b_c_salon_google_calendar` và cột `calendar_salon.google_sheet_access_token` đều nằm trong phạm vi siết bot · Đánh giá spec: Spec ghi rõ (REQ-004) · Evidence: log nguyên văn response của toàn bộ lời gọi |
| TC-SECISO001-01 | API | SEC-ISO-001 | Cách ly dữ liệu theo bot của lịch salon | Abnormal | auto | Tất cả | Mở 2 tab 2 bot rồi đổi bot ở một tab không làm ghi chéo dữ liệu ở tab kia | Một tài khoản admin quản lý cả bot A và bot B, mỗi bot có lịch salon với tên cửa hàng khác nhau rõ rệt. Ghi lại dữ liệu của cả 2 lịch | 1. Tab 1: mở màn quản lý lịch salon của bot A<br>2. Tab 2: mở màn quản lý lịch salon của bot A, vào form sửa khoá học nhưng CHƯA lưu<br>3. Ở tab 1: đổi bot sang bot B<br>4. Quay lại tab 2 (vẫn đang hiển thị form của bot A): bấm lưu, ghi mã HTTP và thông báo<br>5. Đối chiếu dữ liệu khoá học của bot A và của bot B trong cơ sở dữ liệu<br>6. Tải lại tab 2, kiểm tra màn hiển thị theo bot nào | Mã khoá học của bot A, gửi trong lúc session đã là bot B | Thao tác ở tab 2 bị từ chối mã **404** (session hiện tại là bot B, mã khoá thuộc bot A) kèm thông báo rõ trên màn, overlay tải tắt, màn không treo. Dữ liệu khoá học của **cả bot A và bot B** giữ nguyên, không bản ghi nào bị ghi chéo. Sau khi tải lại, tab 2 hiển thị theo bot B | | Lấp `R3` · regression · dẫn từ kho `TC-SLN-464` "Đổi bot khi đang mở màn quản lý calendar (kể cả 2 tab)" · Bối cảnh bot là theo session nên đổi bot ở 1 tab đổi cả trình duyệt · Đánh giá spec: Spec không ghi — cần hỏi leader · Evidence: ảnh 2 tab + log mã HTTP lần lưu |
| TC-PERM003-01 | API | PERM-003 | Phân quyền & môi trường | Normal | auto | Tất cả | Tài khoản スタッフ có quyền màn 予約管理 vẫn thấy đủ dữ liệu sau khi controller siết bot | Bot A có lịch salon đủ dữ liệu (khoá học, nhân viên, ca làm, đơn đặt, lịch sử đồng bộ). Có 1 tài khoản スタッフ được cấp quyền màn 予約管理 của bot A. Ghi lại số lượng từng loại dữ liệu khi xem bằng tài khoản admin | 1. Đăng nhập bằng tài khoản admin, mở lần lượt: danh sách lịch salon, tab 「コース・スタッフ」, tab 「予約カレンダー」, tab 「データ同期履歴」 — ghi số bản ghi từng màn<br>2. Đăng xuất, đăng nhập bằng tài khoản スタッフ có quyền<br>3. Mở lại đúng 4 màn trên, đếm lại số bản ghi<br>4. So từng cặp số<br>5. Thử 1 thao tác lưu (sửa tên khoá học) bằng tài khoản スタッフ, ghi mã HTTP | — | Số bản ghi ở cả 4 màn khi xem bằng tài khoản スタッフ **bằng đúng** số khi xem bằng admin — không màn nào trống hoặc thiếu bản ghi. Thao tác lưu bằng tài khoản スタッフ thành công mã 200 | | Lấp `Q3` + `R2` · regression · căn cứ: Dev khai *"Dùng getBotId() chứ không phải getBotIdInScope()"* — nếu session của tài khoản スタッフ không set `current_bot_id` cùng cách thì mọi query lọc thành rỗng · dẫn từ kho `MT-35` "Phân quyền tài khoản Staff: CHƯA từng được test" + `TC-SLN-479`/`TC-SLN-480`/`TC-SLN-481` · Đánh giá spec: Spec không ghi — cần hỏi leader · Evidence: bảng đối chiếu số bản ghi admin vs スタッフ ở 4 màn |
| TC-PERM003-02 | API | PERM-003 | Cách ly dữ liệu theo bot của lịch salon | Boundary | auto | Tất cả | Bot mới tạo chưa có lịch salon nào không hiện dữ liệu của bot khác | Một tài khoản admin quản lý bot A (đã có ≥2 lịch salon đủ dữ liệu) và bot C **mới tạo, chưa có lịch salon nào** | 1. Đăng nhập, chọn bot C<br>2. Mở màn danh sách lịch salon → quan sát danh sách và bộ đếm 「登録カレンダー数」<br>3. Gọi trực tiếp endpoint lấy danh sách khoá học và danh sách nhân viên (không truyền mã lịch), ghi nội dung trả về<br>4. Mở màn 「データ同期履歴」 bằng cách gõ URL trực tiếp, ghi mã HTTP<br>5. Đổi sang bot A, xác nhận dữ liệu bot A vẫn hiện đủ | — | Với bot C: danh sách lịch salon **rỗng**, bộ đếm bằng 0, các lời gọi trả về rỗng hoặc **404** — **không** hiện bất kỳ bản ghi nào của bot A (đặc biệt với 4 bảng có `bot_id` DEFAULT NULL, subquery qua lịch cha phải cho ra tập rỗng chứ không phải toàn bộ). Sau khi đổi lại bot A, dữ liệu bot A hiện đủ | | Lấp `Q3` + `Q10` (bù Boundary cho RULE-01) · Đây là biên đối nghịch của NEW-39 (danh sách không được giấu mất dữ liệu) · Đánh giá spec: Spec ghi rõ (REQ-004 · REQ-005) · Evidence: ảnh màn bot C + log response |
| TC-DATADB001-01 | Data | DATA-DB-001 | Cách ly dữ liệu theo bot của lịch salon | Boundary | auto | Tất cả | Hai bot có bản ghi trùng tên hoàn toàn, thao tác ở bot A không đụng bot B | Bot A và bot B mỗi bot có 1 lịch salon **trùng tên cửa hàng**, trong đó có khoá học **trùng tên** và nhân viên **trùng tên** (đặt cùng chuỗi ký tự, chỉ khác mã). Ghi lại mã và toàn bộ giá trị của 6 bản ghi | 1. Đăng nhập bot A, sửa tên + giá khoá học trùng tên đó rồi lưu<br>2. Sửa tên + phụ phí nhân viên trùng tên đó rồi lưu<br>3. Đổi thứ tự hiển thị khoá học và nhân viên của bot A<br>4. Xoá 1 nhân viên của bot A<br>5. Đối chiếu trong cơ sở dữ liệu: bản ghi của bot A đã đổi đúng, bản ghi của bot B còn **nguyên vẹn** từng trường<br>6. Đổi sang bot B, mở màn đối chiếu bằng mắt | Cùng một chuỗi tên cho bản ghi của 2 bot | Toàn bộ 4 thao tác chỉ tác động **đúng** bản ghi của bot A. Bản ghi trùng tên của bot B giữ nguyên từng trường (tên, giá, phụ phí, thứ tự, trạng thái tồn tại) — chứng minh điều kiện `WHERE` lọc theo mã + bot chứ không theo tên. Không bản ghi nào của bot B bị ghi đè hay xoá | | Lấp `Q10` · RULE-07 bắt buộc "tạo bản ghi trùng tên ở 2 tài khoản để kiểm chứng WHERE" — bộ TC hiện tại toàn dùng tên đặc trưng khác nhau nên không kiểm được điều này · Đánh giá spec: Spec ghi rõ (REQ-004) · Evidence: bảng đối chiếu giá trị 6 bản ghi trước/sau |
| TC-DATAREF001-01 | Data | DATA-REF-001 | 予約システムの削除 | Boundary | auto | Tất cả | Xoá một lịch của bot khi bot còn lịch khác thì chỉ dọn dữ liệu con của lịch bị xoá | Bot A có **2** lịch salon (lịch X và lịch Y), mỗi lịch có đủ dữ liệu con ở 18 bảng liên quan (khoá học, nhóm menu, nhân viên, ca làm, đơn đặt, lịch sử thao tác, 4 nhóm cài đặt, liên kết + lịch sử Google, yêu cầu CSV). Đếm và ghi lại số bản ghi từng bảng cho **cả 2** lịch | 1. Thực hiện xoá lịch X qua đủ 2 bước xác thực mã email<br>2. Sau khi xoá xong, đếm lại số bản ghi từng bảng thuộc lịch X<br>3. Đếm lại số bản ghi từng bảng thuộc lịch Y<br>4. Mở màn danh sách lịch salon của bot A, xác nhận chỉ còn lịch Y và lịch Y mở được bình thường<br>5. Mở từng tab của lịch Y đối chiếu dữ liệu còn đủ<br>6. Kiểm tra không còn bản ghi mồ côi nào trỏ tới lịch X | — | Toàn bộ dữ liệu con của **lịch X** bị dọn sạch ở cả 18 bảng, không còn bản ghi mồ côi. Dữ liệu của **lịch Y** còn **nguyên vẹn từng bảng** (số bản ghi bằng đúng số đã đếm trước khi xoá) và mở được bình thường ở mọi tab — điều kiện `WHERE` của 16 câu xoá cascade không được lan sang lịch khác cùng bot | | Lấp `G5` + `Q11` · Bù Boundary cho cascade delete: NEW-41/65/72 đều chỉ có 1 lịch nên không phát hiện được lan xoá sang lịch khác cùng bot · dẫn từ kho `TC-SLN-356` · Đánh giá spec: Spec ghi rõ (BR-07) · Evidence: bảng đếm 18 bảng × 2 lịch trước/sau |
| TC-STATEDEP001-01 | UI | STATE-DEP-001 | 予約システムの削除 | Normal | auto | Tất cả | Xoá lịch salon dọn cả hành động và nhắc lịch đã lên lịch của đúng lịch đó | Bot A có lịch salon với: ≥2 bước nhắc lịch đã lên lịch gửi cho đơn trong tương lai, ≥1 action đặt/huỷ đã cấu hình, ≥1 thông báo cho app đã xếp hàng. Ghi lại các bản ghi đã lên lịch này. Bot A còn 1 lịch khác cũng có nhắc lịch đã lên lịch | 1. Ghi lại danh sách bản ghi đã lên lịch (nhắc lịch, action, thông báo app) của cả 2 lịch<br>2. Xoá lịch thứ nhất qua đủ 2 bước xác thực<br>3. Kiểm tra các bản ghi đã lên lịch thuộc lịch vừa xoá: phải không còn<br>4. Kiểm tra các bản ghi đã lên lịch của lịch còn lại: phải còn đủ<br>5. Chờ tới thời điểm nhắc lịch đã lên lịch của lịch ĐÃ XOÁ: xác nhận không có tin nào được gửi ra<br>6. Chờ tới thời điểm nhắc lịch của lịch CÒN LẠI: xác nhận tin vẫn gửi đúng | — | Bản ghi nhắc lịch, action và thông báo app của lịch bị xoá **không còn** và **không có tin nào được gửi ra** tại thời điểm đã lên lịch. Toàn bộ bản ghi đã lên lịch của lịch còn lại vẫn nguyên và **vẫn gửi đúng** thời điểm | | Lấp `Q20` · BR-07 kê cascade gồm `Events type=5` + `MobileNotify` nhưng 0 TC nào verify nhánh đã-lên-lịch · Đánh giá spec: Spec ghi rõ (BR-07 · BR-08) · Evidence: ảnh log gửi tin tại 2 thời điểm + bảng bản ghi đã lên lịch trước/sau |
| TC-JOB001-01 | API | JOB-001 | Job & Console Command dùng chung service | Normal | auto | Tất cả | Toàn bộ job và console command của lịch salon vẫn chạy khi không có session bot | Môi trường test không có session admin. Bot A có lịch salon đã liên kết Google Calendar, có đơn đặt lịch chờ nhắc, có yêu cầu xuất CSV đồng bộ. Lập danh sách **8 console command + 7 job** của lịch salon (lấy từ file 03 mục 2) | 1. Chạy lần lượt **từng** console command trong danh sách 8 cái, ghi lại mã thoát và log<br>2. Đưa lần lượt **từng** job trong danh sách 7 cái vào hàng đợi rồi chạy, ghi lại trạng thái và log<br>3. Với mỗi lần chạy: đếm số bản ghi mà job/command xử lý được<br>4. Đối chiếu số bản ghi xử lý được với số bản ghi đủ điều kiện trong cơ sở dữ liệu<br>5. Soát log tìm lỗi liên quan tới thiếu bot trong session hoặc lọc ra 0 bản ghi | Không có session bot (`current_bot_id` không tồn tại) | Cả **8/8 command và 7/7 job** chạy thành công, không lỗi do thiếu `current_bot_id`. Số bản ghi mỗi job/command xử lý được **bằng đúng** số bản ghi đủ điều kiện trong cơ sở dữ liệu — **không** job nào lọc ra 0 bản ghi. Log không có lỗi SQL và không có cảnh báo về bot rỗng | | Lấp `G7` + `Q1` · Đây là rủi ro Dev tự nêu ("làm CHẾT toàn bộ cron đồng bộ Google Calendar, webhook thanh toán và màn đặt chỗ của khách"); NEW-73 là TC duy nhất chạm vùng này và đang SKIP · Đánh giá spec: Spec ghi rõ (REQ-010) · Evidence: log của 15 lần chạy + bảng đối chiếu số bản ghi xử lý |
| TC-JOB001-02 | API | JOB-001 | Job đồng bộ Google Calendar | Normal | manual | product | Job đồng bộ Google Calendar và job nhắc lịch chạy đúng trên production sau deploy | Đã deploy bản fix lên production. Trên production có lịch salon thật đã liên kết Google Calendar và có đơn đặt lịch sắp tới hạn nhắc | 1. Ghi lại thời điểm deploy và lịch chạy của job đồng bộ + job nhắc lịch<br>2. Chờ tới chu kỳ chạy đầu tiên sau deploy, theo dõi log job trên production<br>3. Kiểm tra lịch Google thật: sự kiện mới có được đẩy lên không<br>4. Kiểm tra tab 「データ同期履歴」 có bản ghi mới của đúng lịch đó<br>5. Kiểm tra tin nhắc lịch có gửi tới LINE của khách đúng thời điểm<br>6. So số bản ghi job xử lý ở chu kỳ sau deploy với chu kỳ trước deploy | — | Job chạy đúng chu kỳ, log không lỗi. Sự kiện xuất hiện trên lịch Google thật, lịch sử đồng bộ có bản ghi mới đúng lịch. Tin nhắc lịch tới LINE của khách đúng thời điểm. Số bản ghi xử lý ở chu kỳ sau deploy **không sụt** so với chu kỳ trước deploy | | Lấp `Q1` + `Q5` · RULE-08 (job nền) → bắt buộc `product` · manual vì môi trường production + phải nhìn kết quả trên LINE thật và lịch Google thật · Đánh giá spec: Spec ghi rõ (§7 Background Jobs) · Evidence: log job 2 chu kỳ + ảnh lịch Google + ảnh tin trên LINE |
| TC-REGRUN001-01 | UI | REG-RUN-001 | Dữ liệu cũ & hồi quy | Abnormal | auto | Tất cả | Dữ liệu đang xử lý dở lúc deploy không bị kẹt trạng thái | Chuẩn bị 3 khối dữ liệu đang dở trước khi deploy: (1) job đồng bộ Google đang chạy giữa chừng, (2) đơn UnivaPay đang chờ phản hồi webhook, (3) yêu cầu xuất CSV đồng bộ đang xếp hàng. Ghi lại trạng thái từng khối | 1. Tạo đủ 3 khối dữ liệu dở ở trên<br>2. Thực hiện deploy bản fix trong lúc 3 khối vẫn đang dở<br>3. Sau deploy, kiểm tra trạng thái từng khối<br>4. Với job đồng bộ: kiểm tra nó chạy tiếp được hoặc được chạy lại sạch, không tạo bản ghi trùng<br>5. Với đơn chờ webhook: gửi webhook về và kiểm tra trạng thái thanh toán cập nhật đúng, không kẹt<br>6. Với yêu cầu CSV: kiểm tra tệp được sinh ra và tải được | — | Không khối nào kẹt trạng thái. Job đồng bộ hoàn tất hoặc chạy lại sạch, không tạo bản ghi đồng bộ trùng. Đơn chờ webhook nhận được phản hồi và cập nhật trạng thái thanh toán đúng (không kẹt ở trạng thái chờ). Yêu cầu CSV sinh được tệp và tải được | | Lấp `Q14` · Bổ trợ NEW-52 (đang FAIL — bug 1121 "trạng thái đơn bị đổi") ở góc độ deploy · Đánh giá spec: Spec không ghi — cần hỏi leader · Evidence: bảng trạng thái 3 khối trước/sau deploy + log job |
| TC-DEPLOYLIVE001-01 | API | DEPLOY-LIVE-001 | Dữ liệu cũ & hồi quy | Abnormal | auto | Tất cả | Form mở trước release submit sau release vẫn hiện được thông báo lỗi | Có 2 phiên bản mã nguồn: bản **trước** fix và bản **sau** fix. Bot A có lịch salon đủ dữ liệu | 1. Ở bản **trước** fix: mở đồng thời 4 form (tạo lịch salon, tạo khoá học, tạo nhân viên, cài ca làm) và **để nguyên không submit**<br>2. Deploy bản **sau** fix<br>3. Lần lượt submit 4 form đang mở đó với dữ liệu thiếu field bắt buộc mới → ghi mã HTTP + quan sát màn<br>4. Lần lượt submit 4 form với dữ liệu hợp lệ → ghi mã HTTP + quan sát màn<br>5. Với mỗi lần submit: kiểm tra overlay tải có tắt, modal có treo, thông báo có hiện<br>6. Kiểm tra cơ sở dữ liệu không có bản ghi nửa vời | Bước 3: thiếu field bắt buộc mới · Bước 4: dữ liệu hợp lệ | Ở bước 3: server trả **422/400** và màn hình **vẫn hiện được thông báo lỗi** (không im lặng, không treo overlay, không treo modal), cơ sở dữ liệu không có bản ghi nửa vời. Ở bước 4: lưu thành công bình thường. Không form nào rơi vào trạng thái người dùng bấm lưu mà không biết đã xảy ra gì | | Lấp `G16` + `Q2` · dẫn từ kho `TC-SLN-488` "UI load TRƯỚC release, submit SAU release" · Đánh giá spec: Spec không ghi — cần hỏi leader · Evidence: video 4 lần submit + log mã HTTP |
| TC-DEPLOYASSET001-01 | API | DEPLOY-ASSET-001 | Dữ liệu cũ & hồi quy | Abnormal | auto | Tất cả | Browser còn cache bản JS cũ vẫn hiển thị được thông báo lỗi mới | Đã truy cập màn lịch salon ở bản **trước** fix để browser cache đủ 12 tệp JS bản cũ (index.js · calendar_detail.js · booking.js · calendar-management.js · step_create.js · edit_course.js · edit_staff.js · add-new-booking.js · 4 tệp setting_reservation). Sau đó deploy bản fix nhưng **KHÔNG** xoá cache browser | 1. Ghi lại giá trị tham số `?v=` của 12 tệp JS trước và sau deploy<br>2. Mở lại màn lịch salon **không** xoá cache, kiểm tra trong bảng theo dõi mạng: mỗi tệp JS nạp từ cache hay tải mới<br>3. Thực hiện thao tác thêm ca làm bị từ chối (server trả 400 kèm khoá `error_message`) → quan sát màn<br>4. Thực hiện thao tác hoàn tiền bị cổng từ chối (server trả 400) → quan sát màn<br>5. Ghi lại: có thông báo lỗi hiện ra không, overlay tải có tắt không, modal có đóng không<br>6. Xoá cache, tải lại và làm lại bước 3–4 để đối chiếu | — | **Hoặc** giá trị `?v=` đã đổi khiến 12 tệp JS được tải mới → hành vi ở bước 3–4 giống hệt sau khi xoá cache; **hoặc** nếu tệp vẫn nạp từ cache thì vẫn phải hiện được thông báo lỗi và tắt overlay. Tuyệt đối **không** có trường hợp người dùng bấm thao tác, server trả 400 mà màn không hiện gì và overlay quay mãi | | Lấp `G15` + `Q15` · Bump `config/sns-line.php` đã bị revert (commit 1b32a31c0a) nên 12 tệp JS CŨ bị sửa nội dung có thể giữ nguyên `?v=`; tệp MỚI (ajax-error.js) có URL mới nên không dính cache · Trigger nâng "Cao tuyệt đối" vì tệp sửa gồm setting-payment.js + calendar-management.js (luồng hoàn tiền) · dẫn từ kho `TC-SLN-498` · Đánh giá spec: Spec không ghi — cần hỏi leader · Evidence: ảnh bảng theo dõi mạng 12 tệp + video 2 thao tác trước/sau xoá cache |
| TC-INTGSHEET001-01 | API | INTG-SHEET-001 | Googleスプレッドシート連携 | Normal | auto | Tất cả | Liên kết Google Spreadsheet vẫn hiển thị và ghi đúng sau khi lọc lịch theo bot | Bot A có lịch salon **đã liên kết** Google Spreadsheet (tồn tại `google_sheet_id`). Bot B cũng có lịch đã liên kết Spreadsheet khác. Ghi lại mã Spreadsheet của từng bot | 1. Đăng nhập bot A, mở màn danh sách lịch salon: kiểm tra biểu tượng Google Spreadsheet có hiện trên thẻ lịch<br>2. Mở tab 「予約設定」 mục 「Googleスプレッドシート連携」: kiểm tra trạng thái đã liên kết và mã Spreadsheet hiển thị đúng<br>3. Tạo 1 đơn đặt lịch mới, kiểm tra dòng mới được ghi vào **đúng** Spreadsheet của bot A<br>4. Đổi sang bot B, lặp bước 1–3, kiểm tra ghi vào đúng Spreadsheet của bot B<br>5. Xác nhận không có dòng nào của bot A bị ghi vào Spreadsheet của bot B và ngược lại | — | Biểu tượng và trạng thái liên kết Spreadsheet hiện **đúng** cho từng bot (không bị mất do lọc `where(bot_id)` trên `calendar_salon`). Đơn mới của mỗi bot được ghi vào **đúng** Spreadsheet của bot đó, không ghi chéo | | Lấp `Q19` · Căn cứ: `google_sheet_id` + `google_sheet_access_token` là cột của `calendar_salon` (`feature-spec.md:469` · `:747`) — bảng này nay bị lọc thẳng `where(bot_id, getBotId())`; kho có 15 TC nhóm `TC-SLN-377→388` nhưng 0 TC Studio chạm · Đánh giá spec: Spec ghi rõ (§2.4 · §4 dòng 469) · Evidence: ảnh 2 Spreadsheet + ảnh thẻ lịch của 2 bot |
| TC-COMPATLEGACY001-01 | Data | COMPAT-LEGACY-001 | Dữ liệu cũ & hồi quy | Boundary | auto | Tất cả | Bản ghi cũ có độ dài đúng bằng giới hạn mới vẫn lưu lại được | Dựng trong cơ sở dữ liệu các bản ghi cũ ở **đúng biên** của giới hạn mới: lịch có 店舗名 đúng 30 ký tự và カレンダー管理名 đúng 10 ký tự; nhóm menu khoá học có tên đúng 50 ký tự; cùng bộ bản ghi **vượt biên 1 ký tự** (31 · 11 · 51) mô phỏng dữ liệu tạo trước khi có giới hạn. Ghi lại giá trị gốc từng bản ghi | 1. Mở từng bản ghi **đúng biên** (30 · 10 · 50), bấm lưu lại **nguyên trạng** không sửa gì → ghi mã HTTP + thông báo<br>2. Mở từng bản ghi **vượt biên** (31 · 11 · 51), bấm lưu lại nguyên trạng → ghi mã HTTP + thông báo<br>3. Với các bản ghi vượt biên: kiểm tra giá trị gốc trong cơ sở dữ liệu có bị cắt hoặc ghi đè một phần không<br>4. Kiểm tra cả 6 bản ghi vẫn hiển thị **đầy đủ** ở màn danh sách (dữ liệu cũ không bị ẩn)<br>5. Ghi lại nguyên văn hành vi của từng trường hợp để Leader chốt theo `C3` §4 | Bản ghi đúng biên 30/10/50 và vượt biên 31/11/51 | Bản ghi **đúng biên** lưu lại thành công mã 200, giá trị không bị cắt. Bản ghi **vượt biên** cho ra hành vi **nhất quán và có thông báo rõ** (không im lặng, không cắt ngầm), và giá trị gốc trong cơ sở dữ liệu **không bị ghi đè một phần**. Cả 6 bản ghi vẫn hiện đầy đủ ở màn danh sách | | Lấp `Q18` + kiểm chứng `C3` §4 · ⚠️ Kết luận Đạt/Không đạt của nhánh vượt biên **phụ thuộc quyết định của Leader ở §8 dòng 3** (chặn / tự cắt / cho qua) — chưa có quyết định thì ghi nguyên văn hành vi, **không** tự phán · Đánh giá spec: Spec không ghi (kho `MT-32` + `MT-05` đang chờ quyết định) — đã hỏi leader · Evidence: bảng 6 bản ghi × giá trị trước/sau + ảnh thông báo |
| TC-INTGCAL001-01 | API | INTG-CAL-001 | Googleカレンダー連携 | Abnormal | auto | Tất cả | Google trả lỗi khi đồng bộ thì ghi lịch sử lỗi và không mất đơn đặt lịch | Bot A có lịch salon đã liên kết Google Calendar. Có thể làm cho lời gọi Google API thất bại (thu hồi quyền, hoặc mã lịch Google không còn tồn tại). Ghi lại số đơn và số bản ghi lịch sử đồng bộ | 1. Làm cho lời gọi Google API thất bại<br>2. Tạo 1 đơn đặt lịch mới cho nhân viên đã liên kết Google<br>3. Chờ tác vụ đồng bộ chạy, ghi lại mã HTTP và thông báo trên màn<br>4. Kiểm tra đơn đặt lịch vừa tạo vẫn tồn tại và đúng dữ liệu<br>5. Mở tab 「データ同期履歴」 kiểm tra có bản ghi ghi nhận lỗi đồng bộ<br>6. Khôi phục quyền Google, chạy lại đồng bộ và kiểm tra đơn được đẩy lên | Google API trả lỗi | Đơn đặt lịch **vẫn được tạo** và không bị mất/hỏng dữ liệu dù đồng bộ Google thất bại. Lỗi đồng bộ được ghi nhận ở 「データ同期履歴」 (không im lặng). Màn hình hiện thông báo phù hợp, không trả 500 cho lỗi nghiệp vụ của bên thứ ba. Sau khi khôi phục quyền, đồng bộ lại thành công và không tạo sự kiện trùng | | Lấp `Q6` (bù Abnormal cho RULE-01 — 3 TC INTG-CAL hiện toàn Normal, 2 trong đó SKIP) · Đánh giá spec: Spec ghi rõ (BR-09) · Evidence: ảnh tab データ同期履歴 + ảnh lịch Google trước/sau khôi phục |
| TC-INTGHOOK001-01 | API | INTG-HOOK-001 | Hoàn tiền 返金 | Normal | auto | Tất cả | Webhook về sau khi đã hoàn tiền không ghi ngược trạng thái đơn | Bot A có 1 đơn UnivaPay đã thanh toán thành công. Có thể gửi lại webhook của cổng về hệ thống. Ghi lại trạng thái thanh toán và số bản ghi lịch sử của đơn | 1. Thực hiện hoàn tiền đơn đó thành công qua màn quản lý<br>2. Xác nhận trạng thái thanh toán đã chuyển 返金 trên màn và trong cơ sở dữ liệu<br>3. Gửi lại webhook **thanh toán thành công** của cổng cho đơn đó<br>4. Kiểm tra trạng thái thanh toán của đơn sau khi nhận webhook<br>5. Gửi tiếp webhook **hoàn tiền** cho đơn đó và kiểm tra lại<br>6. Đối chiếu số bản ghi lịch sử thao tác của đơn | Webhook thanh toán thành công + webhook hoàn tiền, gửi sau khi đã 返金 | Trạng thái thanh toán của đơn **giữ nguyên 返金**, không bị webhook ghi ngược về đã thanh toán. Webhook trùng lặp không tạo thêm bản ghi lịch sử sai. Hệ thống trả mã phù hợp cho cổng (không 500) | | Lấp `Q17` (bù Normal cho RULE-01) · Bổ trợ NEW-52 (đang FAIL — bug 1121 "trạng thái đơn bị đổi: payment_status / status_webhook") · Đánh giá spec: Spec không ghi thứ tự ưu tiên webhook vs thao tác admin — cần hỏi leader · Evidence: log webhook + bảng trạng thái đơn qua từng bước |
| TC-CONC001-01 | API | CONC-001 | Hoàn tiền 返金 | Abnormal | auto | Tất cả | Bấm hoàn tiền hai lần liên tiếp không hoàn tiền hai lần | Bot A có 1 đơn đã thanh toán thành công qua cổng. Ghi lại số dư giao dịch trên cổng và số bản ghi lịch sử của đơn | 1. Mở chi tiết đơn, bấm thao tác hoàn tiền và xác nhận<br>2. **Trong lúc** yêu cầu thứ nhất chưa xong, bấm xác nhận hoàn tiền lần thứ hai (hoặc gửi song song 2 yêu cầu hoàn tiền cùng mã đơn)<br>3. Ghi lại mã HTTP và thông báo của **cả hai** lần<br>4. Đối chiếu lịch sử giao dịch trên cổng thanh toán: có mấy giao dịch hoàn tiền<br>5. Đối chiếu trạng thái thanh toán và số bản ghi lịch sử thao tác của đơn | 2 yêu cầu hoàn tiền song song cùng mã đơn | Chỉ **1** giao dịch hoàn tiền được thực hiện trên cổng. Yêu cầu thứ hai bị từ chối với mã **400** hoặc **404** kèm thông báo rõ trên màn (không phải 200 im lặng, không phải 500). Trạng thái thanh toán là 返金 và chỉ có đúng 1 bản ghi lịch sử hoàn tiền | | Lấp `Q9` · Fix thêm guard 404 vào `orderRefund` nên hành vi lần bấm thứ 2 đã đổi so với trước · Đánh giá spec: Spec không ghi — cần hỏi leader · Evidence: ảnh dashboard cổng (đếm giao dịch refund) + log 2 lần gọi |
| TC-CONC001-02 | API | CONC-001 | 予約システムの削除 | Abnormal | auto | Tất cả | Hai yêu cầu xoá lịch salon song song không để lại dữ liệu mồ côi | Bot A có lịch salon đủ dữ liệu con ở 18 bảng. Đã lấy được mã xác thực qua email. Đếm và ghi lại số bản ghi từng bảng | 1. Gửi **song song** 2 yêu cầu xoá cùng mã lịch với cùng mã xác thực<br>2. Ghi lại mã HTTP và thông báo của cả 2 yêu cầu<br>3. Đếm lại số bản ghi từng bảng con của lịch đó<br>4. Kiểm tra không còn bản ghi mồ côi nào trỏ tới lịch đã xoá<br>5. Mở màn danh sách lịch salon xác nhận lịch đã mất và màn không lỗi<br>6. Soát log tìm lỗi SQL | 2 yêu cầu xoá song song cùng mã lịch | Đúng **1** yêu cầu thành công, yêu cầu còn lại bị từ chối mã **404** kèm thông báo rõ (không 500, không 200 im lặng). Toàn bộ dữ liệu con được dọn sạch, **không** còn bản ghi mồ côi, **không** có lỗi SQL trong log. Màn danh sách hiển thị bình thường | | Lấp `Q9` · Bổ trợ NEW-72 (đang FAIL — bug 1170 "chuỗi xoá lịch KHÔNG có transaction: lỗi ở một bảng con để lại trạng thái xoá một phần") ở góc độ đồng thời · Đánh giá spec: Spec ghi rõ (BR-07) · Evidence: bảng đếm 18 bảng sau test + log 2 lần gọi + log SQL |
| TC-NOTIMAIL001-01 | API | NOTI-MAIL-001 | 予約システムの削除 | Normal | manual | Tất cả | Mail mã xác thực xoá lịch nhận được trong hộp thư thật và mã dùng được | Bot A có lịch salon cần xoá. Truy cập được hộp thư **thật** của email quản trị bot A. Ghi lại thời điểm bắt đầu | 1. Vào mục 「予約システムの削除」, bấm gửi mã xác thực<br>2. Mở **hộp thư thật**, xác nhận nhận được mail: kiểm tra người gửi, tiêu đề, nội dung, mã xác thực, thời gian nhận<br>3. Kiểm tra mail không vào thư rác<br>4. Nhập mã vừa nhận vào màn, xác nhận đi tiếp được bước 2<br>5. Nhập mã **đã hết hạn / đã dùng** để đối chiếu, ghi mã HTTP + thông báo<br>6. Hoàn tất xoá và xác nhận lịch đã mất | Mã xác thực từ mail thật | Mail tới hộp thư thật (không vào thư rác) với người gửi, tiêu đề, nội dung tiếng Nhật đúng và chứa mã xác thực đọc được. Mã đó nhập vào màn thì đi tiếp được. Mã sai/hết hạn bị từ chối mã **400** kèm thông báo rõ trên màn. Xoá hoàn tất bình thường | | Lấp `Q8` · manual vì phải mở mail thật trong hộp thư (RULE-06) · dẫn từ kho `TC-SLN-354`/`TC-SLN-355` · Đánh giá spec: Spec ghi rõ (BR-07) · Evidence: ảnh mail trong hộp thư (có timestamp) + ảnh màn nhập mã |
| TC-SYNCAPP001-01 | UI | SYNC-APP-001 | App admin mobile | Normal | manual | Tất cả | Thao tác ca làm và đặt lịch trên app admin thật vẫn hiện được thông báo lỗi | Có **thiết bị thật** đã cài app admin mobile, đăng nhập bot A có lịch salon. Chuẩn bị các tình huống lỗi: thêm ca liên tục bị từ chối, xoá ca còn đơn 予約確定, đặt lịch vào khung giờ đã bị chặn, hoàn tiền đơn không hợp lệ | 1. Trên app admin thật: thêm ca làm trùng/liên tục để bị từ chối → quan sát app<br>2. Xoá ca còn đơn 予約確定 → quan sát app<br>3. Tạo đơn đặt lịch vào khung giờ đã bị chặn → quan sát app<br>4. Hoàn tiền 1 đơn không hợp lệ → quan sát app<br>5. Với **mỗi** thao tác ghi lại: có hiện toast/thông báo lý do không, vòng quay tải có tắt không, app có đứng hay crash không<br>6. Đối chiếu dữ liệu trên app với dữ liệu trên web admin | — | Cả 4 thao tác đều hiện **toast/thông báo lý do** trên app, vòng quay tải **tắt** hoàn toàn, app không đứng và không crash. Dữ liệu trên app khớp dữ liệu trên web admin. Hợp đồng lớp Api giữ nguyên (app không nhận 4xx gây ném ngoại lệ) | | Lấp `G8` + `Q3` (phần app) · manual vì cần thiết bị thật, kết quả phải nhìn trên app (RULE-06) · Tách phần khẳng định về app ra khỏi NEW-61 theo `I8` §5; Dev tự nêu `reservation.dart:906` gọi `addWorking()` không có `catchError` · Đánh giá spec: Spec ghi rõ (REQ-010) · Evidence: video quay màn app cho cả 4 thao tác |
| TC-PERFLARGE001-01 | API | PERF-LARGE-001 | Hiệu năng & dữ liệu lớn | Normal | auto | Tất cả | Danh sách và tệp CSV không chậm rõ rệt sau khi thêm subquery ràng buộc bot | Bot A có dữ liệu lớn: ≥5 lịch salon, ≥200 khoá học và ≥200 nhân viên trên 1 lịch, ≥1000 đơn đặt lịch, ≥1000 bản ghi lịch sử đồng bộ Google. Đo và ghi lại thời gian phản hồi của các màn này ở bản **trước** fix | 1. Ở bản **trước** fix: đo thời gian tải danh sách lịch salon, danh sách khoá học, danh sách nhân viên, lưới lịch theo tháng, tab 「データ同期履歴」, và thời gian sinh tệp CSV — ghi lại từng số<br>2. Deploy bản **sau** fix<br>3. Đo lại đúng 6 phép đo trên với cùng bộ dữ liệu<br>4. So từng cặp số, tính mức tăng<br>5. Bật log truy vấn chậm, soát xem có truy vấn nào vượt ngưỡng vì subquery mới<br>6. Kiểm tra số bản ghi trả về ở mỗi màn giữa 2 bản là **bằng nhau** | Bot có ≥200 khoá học / ≥200 nhân viên / ≥1000 đơn | Thời gian phản hồi của cả 6 phép đo **không tăng quá 20%** so với bản trước fix. Không truy vấn nào vào log truy vấn chậm vì subquery `IN (select id from calendar_salon where bot_id = ?)` mới thêm. Số bản ghi trả về ở mỗi màn giữa 2 bản bằng nhau (siết bot không làm mất bản ghi) | | Lấp `R1` · regression · căn cứ `dev_impact`: *"thêm ràng buộc bot cho 78 query + 15 chỗ find()->where(id)->where(bot_id)->first()"* — subquery thêm vào 78 truy vấn nhưng Dev chỉ verify mức lint · RULE-08 (performance) nhưng cần so sánh 2 bản mã nguồn nên chạy ở env có dựng được cả 2 bản · Đánh giá spec: Spec không ghi ngưỡng — cần hỏi leader · Evidence: bảng 6 phép đo × 2 bản + log truy vấn chậm |
| TC-OUTEXPORT001-01 | API | OUT-EXPORT-001 | Ca làm việc — CSV | Normal | auto | Tất cả | Tệp CSV ca làm và CSV đồng bộ của bot A không lẫn dữ liệu bot B | Bot A và bot B mỗi bot có lịch salon với nhân viên **trùng tên** và ca làm ở cùng khoảng ngày. Bot A còn có ≥1 bản ghi lịch sử đồng bộ Google cũ với `bot_id` để trống. Ghi lại số dòng ca làm của từng bot | 1. Đăng nhập bot A, xuất CSV ca làm cho nhân viên của bot A<br>2. Mở tệp, đếm số dòng và đối chiếu từng dòng với ca làm của bot A<br>3. Tìm trong tệp có dòng nào thuộc ca làm của bot B không<br>4. Xuất CSV lịch sử đồng bộ Google của lịch bot A, kiểm tra có đủ cả bản ghi cũ `bot_id` để trống<br>5. Đổi sang bot B, xuất CSV ca làm và lặp bước 2–3<br>6. Nhập lại tệp CSV ca làm của bot A vào lịch của bot A, xác nhận nhập được và đúng dữ liệu | Nhân viên trùng tên ở 2 bot, cùng khoảng ngày | Tệp CSV của mỗi bot chứa **đúng** và **đủ** dữ liệu của bot đó — không thiếu dòng nào (kể cả bản ghi cũ `bot_id` để trống) và **không** lẫn dòng nào của bot kia dù nhân viên trùng tên. Nhập lại tệp vào đúng bot thì thành công và dữ liệu khớp | | Lấp `R5` · regression · dẫn từ kho `TC-SLN-214`/`TC-SLN-215`/`TC-SLN-216` (export CSV ca làm) · `download_csv_sync_google_calendar` là 1 trong 4 bảng `bot_id` DEFAULT NULL dùng subquery qua lịch cha · Đánh giá spec: Spec ghi rõ (§2.4) · Evidence: 2 tệp CSV tải về + bảng đối chiếu số dòng |
| TC-LIST001-01 | UI | LIST-001 | 本日／新着の予約 — danh sách booking | Normal | auto | Tất cả | Danh sách đơn đặt lịch hiện đủ và lọc phân trang chạy đúng sau khi siết bot | Bot A có lịch salon với ≥60 đơn đặt lịch đủ mọi trạng thái (chờ duyệt, 予約確定, đã huỷ, đã xoá), trải nhiều ngày và nhiều nhân viên. Bot B cũng có đơn riêng. Đếm và ghi lại số đơn từng trạng thái của bot A | 1. Mở tab 「本日／新着の予約」, đếm tổng số đơn và đối chiếu với số đã ghi<br>2. Lọc theo từng trạng thái, đối chiếu số đơn từng nhóm<br>3. Lọc theo nhân viên và theo khoảng ngày, đối chiếu kết quả<br>4. Chuyển sang trang 2, 3 của phân trang, kiểm tra không lặp và không sót đơn<br>5. Mở mục danh sách đơn **đã xoá**, đối chiếu số đơn<br>6. Kiểm tra không đơn nào của bot B lọt vào danh sách của bot A | Bot A ≥60 đơn đủ trạng thái | Tổng số đơn và số đơn từng trạng thái **khớp đúng** số đã đếm trước test — không đơn nào bị giấu mất do ràng buộc bot mới. Bộ lọc và phân trang cho kết quả đúng, không lặp không sót. Danh sách đơn đã xoá hiện đủ. Không đơn nào của bot B xuất hiện | | Lấp `G18` (EP-07 · EP-53) + `G19` · Màn 「本日／新着の予約」 hiện có **0 TC** · Đánh giá spec: Spec ghi rõ (§2.2 SCR-SLN-02a) · Evidence: bảng đếm số đơn từng trạng thái trước/sau + ảnh màn từng bộ lọc |
| TC-LIST001-02 | UI | LIST-001 | 本日／新着の予約 — thao tác hàng loạt | Normal | auto | Tất cả | Đổi trạng thái hàng loạt và xoá đơn từ danh sách chạy đúng | Bot A có ≥5 đơn ở trạng thái chờ duyệt. Ghi lại mã và trạng thái từng đơn. Bot B có đơn riêng để đối chứng | 1. Ở tab 「本日／新着の予約」 chọn 3 đơn rồi đổi trạng thái hàng loạt sang 予約確定<br>2. Ghi lại mã HTTP và thông báo trên màn<br>3. Đối chiếu trạng thái 3 đơn đó trên màn và trong cơ sở dữ liệu<br>4. Xoá 1 đơn từ danh sách, ghi mã HTTP và thông báo<br>5. Kiểm tra đơn đã chuyển sang danh sách đơn đã xoá<br>6. Đổi tên hiển thị khách giữa tên LINE và tên hệ thống, kiểm tra danh sách cập nhật đúng<br>7. Đối chiếu đơn của bot B không bị đụng | 3 đơn chờ duyệt | Đổi trạng thái hàng loạt thành công, đúng 3 đơn được đổi, 2 đơn còn lại giữ nguyên. Xoá đơn thành công và đơn chuyển sang danh sách đã xoá. Đổi tên hiển thị áp dụng đúng. Mọi thao tác có thông báo kết quả rõ trên màn, overlay tải tắt. Đơn của bot B không đổi | | Lấp `G18` (EP-31 · EP-32 · EP-54) + `G19` · `NEW-62` mới chỉ **ghi nhận hiện trạng** 2 endpoint giữ mã 200, không kiểm chức năng · Đánh giá spec: Spec ghi rõ (§2.2 SCR-SLN-02a) · Evidence: ảnh danh sách trước/sau + log mã HTTP |
| TC-FUNC001-01 | UI | FUNC-001 | 予約カレンダー — lưới lịch & phân công | Normal | auto | Tất cả | Lưới lịch ngày, tuần, tháng và đổi nhân viên phụ trách chạy đúng | Bot A có lịch salon nhiều nhân viên, có ca làm và đơn đặt trải trong tháng hiện tại. Ghi lại số đơn và ca làm theo từng ngày | 1. Mở tab 「予約カレンダー」 chế độ **ngày**, đối chiếu đơn và ca làm hiển thị với dữ liệu đã ghi<br>2. Chuyển sang chế độ **tuần**, đối chiếu<br>3. Chuyển sang chế độ **tháng**, đối chiếu<br>4. Chuyển sang chế độ hiển thị theo danh sách và tab 「シフト」, đối chiếu<br>5. Mở 1 đơn, đổi nhân viên phụ trách sang nhân viên khác, ghi mã HTTP và thông báo<br>6. Đối chiếu nhân viên phụ trách mới trên màn và trong cơ sở dữ liệu<br>7. Đổi sang bot B kiểm tra lưới lịch bot B không lẫn đơn của bot A | — | Cả **3 chế độ ngày/tuần/tháng** + chế độ danh sách + tab シフト đều hiển thị **đủ và đúng** số đơn, ca làm theo dữ liệu đã ghi. Đổi nhân viên phụ trách thành công, dữ liệu cập nhật đúng cả trên màn và trong cơ sở dữ liệu. Lưới lịch 2 bot không lẫn nhau | | Lấp `G18` (EP-45 · EP-46) + `G19` · `NEW-40` mới chỉ xem lưới **tuần** · Đánh giá spec: Spec ghi rõ (§2.2 SCR-SLN-02b) · Evidence: ảnh 3 chế độ lưới + bảng đối chiếu số đơn |
| TC-FUNC001-02 | UI | FUNC-001 | コース・スタッフ — xoá khoá học & nhân viên | Normal | auto | Tất cả | Xoá khoá học và nhân viên của chính bot chạy đúng và chặn đúng khi còn ràng buộc | Bot A có lịch salon với: 1 khoá học **không** còn đơn, 1 khoá học **còn** đơn chưa huỷ, 1 nhân viên **không** còn đơn, 1 nhân viên **còn** đơn chưa huỷ. Ghi lại số khoá và số nhân viên | 1. Xoá khoá học không còn đơn → ghi mã HTTP + thông báo, đối chiếu danh sách và cơ sở dữ liệu<br>2. Xoá khoá học còn đơn chưa huỷ → ghi mã HTTP + thông báo<br>3. Xoá nhân viên không còn đơn → ghi mã HTTP + thông báo<br>4. Xoá nhân viên còn đơn chưa huỷ → ghi mã HTTP + thông báo<br>5. Với nhân viên đã liên kết Google Calendar: kiểm tra bản ghi liên kết được dọn đúng<br>6. Đếm lại số khoá và số nhân viên | — | Bước 1 và 3 xoá thành công mã 200, danh sách và cơ sở dữ liệu giảm đúng 1. Bước 2 và 4 bị chặn mã **400** kèm thông báo lý do hiện rõ trên màn (không phải 500, không phải 200 im lặng). Bản ghi liên kết Google của nhân viên bị xoá được dọn đúng. Không bản ghi nào của lịch khác bị đụng | | Lấp `G18` (EP-29 · EP-30) + `G19` · 2 endpoint xoá này nằm trong 15 chỗ đổi `find()` → `where(id)->where(bot_id)->first()` · Đánh giá spec: Spec ghi rõ (BR-11 · BR-09) · Evidence: log 4 lần gọi + bảng đếm trước/sau |
| TC-OUTEXPORT001-02 | API | OUT-EXPORT-001 | 予約カレンダー — export CSV booking | Normal | auto | Tất cả | Export CSV danh sách đơn đặt lịch xuất đủ và đúng đơn của bot đang xem | Bot A có ≥30 đơn đặt lịch trải nhiều trạng thái và nhiều nhân viên. Bot B có đơn với khách **trùng tên** khách của bot A. Ghi lại số đơn và nội dung mẫu của bot A | 1. Ở tab 「予約カレンダー」 chọn phạm vi ngày rồi xuất CSV đơn đặt lịch<br>2. Mở tệp, đếm số dòng và đối chiếu với số đơn trong phạm vi đã chọn<br>3. Đối chiếu nội dung từng cột với dữ liệu trên màn<br>4. Tìm trong tệp có dòng nào thuộc đơn của bot B không<br>5. Đổi bộ lọc (theo nhân viên, theo trạng thái) rồi xuất lại, đối chiếu<br>6. Đổi sang bot B xuất CSV và lặp bước 2–4 | Phạm vi ngày có ≥30 đơn | Tệp CSV chứa **đúng và đủ** số đơn trong phạm vi đã chọn, nội dung từng cột khớp dữ liệu trên màn. **Không** lẫn dòng nào của bot B dù khách trùng tên. Xuất theo bộ lọc cho kết quả khớp bộ lọc | | Lấp `G18` (EP-33) · ⚠️ `NEW-45` **không** cover EP này (NEW-45 là CSV lịch sử đồng bộ Google) · Đánh giá spec: Spec ghi rõ (§2.2) · Evidence: tệp CSV + bảng đối chiếu số dòng |
| TC-FUNC001-03 | UI | FUNC-001 | シフト — sửa giờ ca làm | Normal | auto | Tất cả | Sửa giờ ca làm đã có chạy đúng và chặn đúng khi còn đơn xác nhận | Bot A có nhân viên với ≥2 ca làm, trong đó 1 ca **có** đơn 予約確定 và 1 ca **không** có đơn. Ghi lại giờ bắt đầu/kết thúc từng ca | 1. Mở modal sửa ca của ca **không** có đơn, đổi giờ bắt đầu và kết thúc rồi lưu → ghi mã HTTP + thông báo<br>2. Đối chiếu giờ mới trên lưới lịch và trong cơ sở dữ liệu<br>3. Mở modal sửa ca **có** đơn 予約確定, thu hẹp giờ khiến đơn rơi ra ngoài ca rồi lưu → ghi mã HTTP + thông báo<br>4. Sửa giờ ca sang giá trị không hợp lệ (giờ kết thúc trước giờ bắt đầu) rồi lưu → ghi mã HTTP + thông báo<br>5. Đối chiếu giờ ca trong cơ sở dữ liệu sau mỗi bước | Bước 1: giờ hợp lệ · Bước 3: thu hẹp giờ · Bước 4: giờ kết thúc < giờ bắt đầu | Bước 1 lưu thành công mã 200, giờ ca cập nhật đúng trên lưới lịch và cơ sở dữ liệu. Bước 3 bị chặn mã **400** kèm thông báo còn đơn 予約確定 hiện rõ trên màn. Bước 4 bị chặn mã **422** kèm thông báo tiếng Nhật. Giờ ca gốc không bị ghi đè một phần ở bước 3 và 4 | | Lấp `G18` (EP-58) + `G19` · Cùng nhóm ca làm với `addWorkScheduleForStaff` — endpoint đã đổi 200→400 · Đánh giá spec: Spec ghi rõ (BR-12) · Evidence: log 4 lần gọi + ảnh lưới lịch trước/sau |
| TC-FUNC001-04 | UI | FUNC-001 | 予約設定 — 店舗情報・利用規約 | Normal | auto | Tất cả | Lưu thông tin cửa hàng và điều khoản sử dụng chạy đúng | Bot A có lịch salon đã khởi tạo cài đặt mặc định. Ghi lại nội dung thông tin cửa hàng và điều khoản hiện tại | 1. Mở tab 「予約設定」 mục thông tin cửa hàng, sửa tên cửa hàng, địa chỉ, điện thoại rồi lưu → ghi mã HTTP + thông báo<br>2. Tải lại trang, đối chiếu nội dung đã lưu<br>3. Sửa điều khoản sử dụng rồi lưu → ghi mã HTTP + thông báo, tải lại đối chiếu<br>4. Bỏ trống field bắt buộc rồi lưu → ghi mã HTTP + thông báo<br>5. Mở trang đặt lịch phía khách, kiểm tra thông tin cửa hàng và điều khoản hiển thị đúng nội dung mới | Nội dung hợp lệ + trường hợp bỏ trống | Bước 1 và 3 lưu thành công mã 200, nội dung sau khi tải lại khớp đúng. Bước 4 bị chặn mã **422** kèm thông báo tiếng Nhật đúng nhãn, nội dung cũ không bị ghi đè. Trang đặt lịch phía khách hiển thị đúng nội dung mới (RULE-06 — output cuối) | | Lấp `G18` (EP-51) + `G19` · `NEW-29` mới chỉ kiểm nhánh chéo bot của mục này · Đánh giá spec: Spec ghi rõ (§2.4.5) · Evidence: ảnh màn admin + ảnh trang đặt lịch phía khách |
| TC-PERM001-01 | API | PERM-001 | 予約ページの非表示 (filter) | Normal | auto | Tất cả | Cài đặt ẩn trang đặt lịch theo điều kiện lọc vẫn lưu và áp dụng đúng | Bot A có lịch salon và ≥2 friend LINE: 1 friend **thoả** điều kiện lọc, 1 friend **không thoả**. Ghi lại cấu hình lọc hiện tại | 1. Mở mục 「予約ページの非表示」, cấu hình điều kiện lọc rồi lưu → ghi mã HTTP + thông báo<br>2. Tải lại trang, đối chiếu cấu hình đã lưu và trong cơ sở dữ liệu<br>3. Dùng friend **thoả** điều kiện mở link đặt lịch → quan sát trang<br>4. Dùng friend **không thoả** mở link đặt lịch → quan sát trang<br>5. Bỏ trống điều kiện bắt buộc rồi lưu → ghi mã HTTP + thông báo | Điều kiện lọc hợp lệ + trường hợp bỏ trống | Cấu hình lọc lưu thành công mã 200 và sau khi tải lại khớp đúng. Friend thoả và không thoả điều kiện nhận đúng hành vi theo cấu hình. Bỏ trống điều kiện bắt buộc bị chặn **422** kèm thông báo tiếng Nhật, cấu hình cũ không bị ghi đè | | Lấp `G19` · Màn này có **0 TC**; cấu hình lọc lưu vào bảng thuộc nhóm bị siết bot · dẫn từ kho `TC-SLN-350`/`TC-SLN-351` · Đánh giá spec: Spec ghi rõ (§2.4) · Evidence: ảnh cấu hình + ảnh trang đặt lịch của 2 friend |
| TC-SYNCAPP001-02 | UI | SYNC-APP-001 | App admin mobile — xem dữ liệu | Normal | manual | Tất cả | Các màn lịch salon trên app admin thật hiển thị đủ dữ liệu sau khi siết bot | Thiết bị thật đã cài app admin mobile, đăng nhập bot A có lịch salon đủ dữ liệu. Đã ghi lại số lịch, số khoá học, số nhân viên, số ca làm, số đơn khi xem trên **web** admin | 1. Trên app admin thật: mở danh sách lịch salon, đếm số lịch<br>2. Mở chi tiết 1 lịch, đếm số khoá học và số nhân viên<br>3. Mở lịch đặt chỗ, đếm số đơn và số ca làm theo ngày<br>4. So từng cặp số với số đã ghi trên web admin<br>5. Đổi sang bot B trên app, lặp bước 1–3 và xác nhận dữ liệu đổi theo bot<br>6. Quan sát app không có màn trống bất thường, không vòng quay tải kẹt | — | Số liệu trên app **bằng đúng** số liệu trên web admin ở cả 5 phép đếm — không màn nào trống hoặc thiếu bản ghi do ràng buộc bot mới. Đổi bot trên app thì dữ liệu đổi đúng theo bot. Không màn nào kẹt vòng quay tải | | Lấp `G20` + `G8` · manual vì cần thiết bị thật (RULE-06) · 3 service bị sửa dùng chung với `Api\CalendarSalonController` mà app gọi · Đánh giá spec: Spec ghi rõ (REQ-010) · Evidence: ảnh chụp app từng màn + bảng đối chiếu số liệu app vs web |
| TC-LIFFENTRY001-01 | UI | LIFF-ENTRY-001 | LINE user — mở link & entry | Abnormal | auto | Tất cả | Khách mở link đặt lịch của lịch đã xoá hoặc thuộc bot khác được xử lý đúng | Bot A có lịch salon đã bị xoá (lấy lại link đặt lịch cũ của nó). Bot B có lịch salon đang hoạt động (lấy link đặt lịch). Có friend LINE thật để mở link | 1. Dùng friend LINE thật mở link đặt lịch của lịch **đã xoá** của bot A → quan sát trang<br>2. Ghi lại nội dung hiển thị và mã HTTP<br>3. Mở link đặt lịch **đang hoạt động** của bot B → xác nhận vào được và chọn được khoá học/nhân viên<br>4. Thử đặt 1 đơn hoàn chỉnh qua link bot B, xác nhận đơn vào đúng lịch của bot B<br>5. Kiểm tra trang không hiện lỗi hệ thống thô (không hiện 500, không hiện thông tin nội bộ) | Link đặt lịch của lịch đã xoá + link của bot B | Link của lịch đã xoá hiện **thông báo thân thiện** cho khách (không 500, không trang trắng, không lộ thông tin nội bộ). Link của bot B vào được bình thường và đơn đặt thành công được ghi vào **đúng** lịch của bot B — việc siết bot ở lớp admin **không** ảnh hưởng luồng đặt lịch của khách | | Lấp `Q3` (phần LIFF khách) · bù Abnormal cho LIFF-ENTRY-001 (hiện chỉ có `#18369` Normal) · dẫn từ kho `TC-SLN-357` "LINE user mở link của calendar đã bị xóa" · Đánh giá spec: Spec ghi rõ (§2.6 · REQ-010) · Evidence: ảnh trang LIFF 2 trường hợp trên LINE thật |

---

## 8. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | `spec-features/admin/salon-booking/feature-spec.md` §4 Field Traceability Matrix — dòng 1 (`calendar_salon.calendar_name`) | Hiện ghi **varchar(100), bắt buộc**. Bản fix áp `max:30` ở server (thông báo 「店舗名は30文字以内で入力してください。」). Cần chốt: **(a)** spec đúng → bỏ `max:30` khỏi fix vì vượt phạm vi ticket; **(b)** implementation đúng → sửa spec thành "giới hạn nhập 30 ký tự (cột DB varchar(100))" và giải thích khoảng cách giữa 2 con số | `C1` (CONF-SPEC) | `Dev / PM` |
| 2 | `spec-features/admin/salon-booking/feature-spec.md` §4 Field Traceability Matrix | **Thiếu hẳn 2 field** mà bản fix đang validate độ dài: `calendar_salon.manager_name` (fix áp **10** ký tự, nhãn カレンダー管理名) và `calendar_salon_course_menu.course_menu_name` (fix áp **50** ký tự, nhãn メニュー名). Bổ sung 2 dòng kèm giới hạn chính thức | `C2` (CONF-SPEC) | `Dev / PM` |
| 3 | `kho-tcs/fa020-datlichsalon-サロン・面談予約.md` — **MT-32** + **MT-05** (đang ⏳ CHỜ QUYẾT ĐỊNH) | Chốt 2 việc: **(a)** giá trị giới hạn chính thức của 店舗名 / メニュー名; **(b)** hành vi với **bản ghi CŨ vượt giới hạn mới** — chặn không cho lưu lại (hiện trạng theo `NEW-9`) / tự cắt / cho qua. Chưa chốt thì `NEW-4` · `NEW-8` · `NEW-9` · `NEW-15` và `TC-COMPATLEGACY001-01` (§7) **không có chuẩn** để phán Đạt–Không đạt | `C3` (CONF-KHO) | `Leader / PM` |
| 4 | `spec-features/admin/salon-booking/feature-spec.md` §5 Business Rules — **thêm BR mới** | Bổ sung quy ước mã HTTP thành Business rule của tính năng: lớp admin `Basic\CalendarSalonController` = **422** validate · **400** nghiệp vụ · **404** không tìm thấy · **500** lỗi hệ thống; lớp `Api\CalendarSalonController` (LIFF khách + app mobile admin) **giữ 200 + khoá `error_message`**; và 2 endpoint cố ý giữ 200 với cờ thành công động (`changeStatusBooking` · `createBooking`). Hiện quy ước này **chỉ** nằm trong `.claude/docs/implement-logic-spec-conventions.md` của repo code, không có trong spec tính năng → tester không có nguồn để phán mã HTTP nào là đúng | `REQ-001` + `REQ-010` + `REQ-011`; liên quan `I6` §5 (81/81 TC để trống `Trạng thái đánh giá spec`) | `Dev / PM` |
| 5 | `spec-features/admin/salon-booking/feature-spec.md` §3 Data Model | Ghi rõ mô hình cách ly theo bot của 18 bảng: **11 bảng** có `bot_id` NOT NULL (lọc thẳng) · **4 bảng** có `bot_id` DEFAULT NULL (`calendar_salon_course` · `calendar_salon_course_menu` · `sync_booking_google_calendar_histories` · `download_csv_sync_google_calendar` — ràng buộc qua lịch cha) · **3 bảng** không có `bot_id` (`line_booking_history_actions` · `history_change_setting_payment` · `b_c_salon_google_calendar_histories` — ràng buộc qua bảng cha). Đây là thông tin bắt buộc để viết TC cho `REQ-004`/`REQ-005` mà hiện chỉ tồn tại trong journal Redmine | `G6` · `G11` §1; `REQ-004` + `REQ-005` | `Dev` |
| 6 | `spec-features/admin/salon-booking/feature-spec.md` §5 Business Rules — phạm vi tầng service | Ghi rõ **quyết định kiến trúc**: 14 service trong `app/Services/CalendarSalon` **CỐ Ý không** ràng buộc bot vì dùng chung với 8 Console Command + 7 Job + `Mobile\CalendarSalonController` + `Api\CalendarSalonController` (không có session). Muốn siết tầng service phải truyền `$botId` tường minh → **ticket riêng**. Ghi vào spec để vòng review sau không raise lại như lỗ hổng | `03-dev-impact.md` mục 2 ("⚠ PHẠM VI CÓ CHỦ Ý"); liên quan `G7` §1 + `Q1` §2 | `Dev / PM` |
