# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | `(1) MCP LME TEST STUDIO — task #231, ticket 39176, round 1` |
| Tổng số TC review | `24` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `0/7 vùng ảnh hưởng đủ TC · 2 GAP · 6 RISK` — bộ TC **thiết kế đủ vùng** nhưng **không vùng nào có bằng chứng thực thi đạt**.

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | `BUG` — root cause: notify broadcast phải ra 「配信人数 1,234人」 (kịch bản tái hiện lỗi gốc trong ticket) | `dev-impact` | `NEW-1` (Studio 13628) | `RISK` — exec = `error` qua cả 3 run (555 skip / 876 skip / 1317 error). Chưa một lần nào quan sát được chuỗi có dấu phẩy trên bản ghi notify thật | `[BLOCKER]` |
| G2 | `F2` — `ActionScheduleBotTask.run` (notify 「対象人数」) | `dev-impact` | `NEW-6,7,8,9` (13638-13641) · `NEW-11` (13645) · `NEW-14` (13648) · `NEW-24` (17513) | `GAP thực thi` — 0/7 TC chạy được lần nào qua 3 run. Run 1317 dừng ở "không thấy ô nhập tên lịch trên màn tạo mới アクションスケジュール実行"; run 555/876 dừng ở "không vào được ngữ cảnh bot". **Một nửa bản fix chưa có bất kỳ bằng chứng runtime nào** | `[BLOCKER]` |
| G3 | `T1/T2` — 4 kênh tiêu thụ notify (chuông web · PC push · Chatwork · app mobile) | `dev-impact` | `NEW-10` (13644) · `NEW-11` (13645) · `NEW-12` (13646) · `NEW-13` (13647) · `NEW-14` (13648) · `NEW-15` (13649) · `NEW-21` (17510) · `NEW-20` (13674) | `GAP thực thi` — 8/8 skip. Web bell + PC push = `SOURCE_BLOCKED` "project web chưa có branch run"; Chatwork = thiếu room + token; app mobile = thiếu thiết bị thật + FCM. Dev kê 4 nơi tiêu thụ ở mục 3 file 03 → không nơi nào được verify runtime | `[BLOCKER]` |
| G4 | `T3` — số < 1.000 không dính dấu phẩy thừa | `dev-impact` | `NEW-2` (13629, 999) · `NEW-7` (13639, 999) · `NEW-22` (17511, 0) · `NEW-9` (13641, 0) | `RISK` — 0/4 TC pass (1 error + 3 skip). Mốc 999 / 1.000 / 0 chỉ được chứng minh bằng TC tĩnh `NEW-19` (13673) áp giá trị vào khuôn chuỗi, không qua đường ghi thật | `[MAJOR]` |
| G5 | `D` — cột `send_number` của bản ghi notify giữ nguyên số nguyên thô | `dev-impact` | `NEW-4` (13631) | `RISK` — exec = `error`. Ngoài ra chỉ kiểm ở luồng broadcast; bản ghi notify của action schedule (type 14) không có TC nào kiểm cột số | `[MAJOR]` |
| G6 | Không có diff chính thức để đối chiếu | `diff code` | — | `Input thiếu: không có diff` — Studio `spec_delta.diffAvailable = false`, note "git diff không chạy được: không tìm thấy ref release-t07-2026". Chiều (b) chỉ suy được từ `dev_impact` dạng text + 2 TC đọc source tĩnh (13670, 13671) | `[MAJOR]` |
| G7 | Ký tự phân cách phụ thuộc **Locale JVM** của tiến trình job — rủi ro biên Studio `dev_impact` nêu, file 03 **không** kê | `diff code` | `NEW-18` (13672) | `RISK` — pass nhưng chỉ chứng minh locale của container **local** (`-Duser.language=ja -Duser.country=JP`). Không có bằng chứng JVM job production khởi chạy cùng tham số. Evidence của chính TC ghi: chuỗi notify thật gần nhất là 「配信人数 0人」 → không quan sát được dấu phẩy nào | `[MAJOR]` |
| G8 | Hành vi đổi so với trước: bản ghi notify tạo **TRƯỚC** fix giữ dạng liền | `diff code` | `NEW-20` (13674) | `RISK` — exec = skip (`SOURCE_BLOCKED`), và TC mang mã Studio lạ `TOOL-OLDREC-001` → không map được coverage | `[MAJOR]` |

> **Không có dòng `orphan`** — cả 24 TC đều map được về `BUG` / `F1` / `F2` / `D` / `T1` / `T2` / `T3`.
>
> **Symptom-only**: không áp dụng — ticket mô tả đúng root cause ở tầng format specifier, Dev fix đúng tầng đó.
>
> **4 câu hỏi adversarial (BƯỚC 2 chiều b)**: (1) mỗi điểm sửa đều có TC ✔; (2) không phải fix dạng generic catch → không áp dụng; (3) fix không đổi hàm dùng chung, Dev **có** kê đủ 4 nơi tiêu thụ và TC **có** viết cho từng nơi, nhưng 0/4 chạy được → G3; (4) hành vi đổi so với trước (record cũ vs mới) chỉ có 1 TC và đang skip → G8.

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `9 quan điểm Trigger khớp task · 9 chưa cover đủ` — không quan điểm nào đạt.

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `ENV-003` ★ | `Cao` (BẮT BUỘC — job nền) | `GAP` — 0 TC mang mã này. Cả 24 TC chạy ở `env = local`, production 0 TC. Catalog D: production **tách 3 job độc lập** (callback / broadcast / scenario) trong khi dev/staging chạy 1 job all → đường sinh notify trên production chưa từng được chạm. RULE-08 cấm kết luận job nền từ local | `[BLOCKER]` |
| Q2 | `JOB-001` ★ | `Cao` (BẮT BUỘC — sửa code trong job nền) | `GAP` — 0 TC mang mã này. Thiếu hẳn: job không dừng khi có friend gửi lỗi · số bản ghi vào = xử lý thành công + hàng đợi lỗi · nhiều bot chạy broadcast đồng thời không lẫn số giữa các bot | `[BLOCKER]` |
| Q3 | `REG-RUN-001` | `Cao` | `GAP` — 0 TC. Không có TC nào cho: deploy bản fix **khi đang có broadcast ở trạng thái delivering**, hoặc action schedule đã lên lịch trước deploy tới giờ chạy sau deploy. Đây là nhánh duy nhất mà "fix ở tầng ghi" có thể sinh notify lai cũ/mới | `[BLOCKER]` |
| Q4 | `COMPAT-LEGACY-001` ★ | `Cao` | `RISK` — chỉ được cover bởi `NEW-20` (13674) mang mã Studio lạ `TOOL-OLDREC-001` → theo quy tắc tính là **chưa cover**; TC đó lại đang skip. Thiếu evidence **cả hai** nhánh cũ/mới hiển thị song song (RULE-09) | `[BLOCKER]` |
| Q5 | `FUNC-004` | `Cao` | `RISK` — 7 TC nhưng **cả 7 đều Boundary** (13629, 13630, 13639, 13640, 13641, 13673, 17511). Thiếu Normal + Abnormal (RULE-01). Nhánh Normal thực chất đã được viết ở 13628 / 13638 nhưng 2 TC đó mang mã lạ `TOOL-KNOW-002` | `[MAJOR]` |
| Q6 | `DATA-COUNT-001` | `Cao` | `RISK` — đúng 1 TC (13631, Normal, exec = error). Thiếu Abnormal + Boundary (RULE-01). Chiều thật sự chưa có TC: đối chiếu 3 tầng (cột `send_number` ↔ chuỗi notify ↔ số hiển thị trên chuông) cho bản ghi **action schedule** | `[MAJOR]` |
| Q7 | `OUT-TRUTH-001` | `Cao` | `RISK` — 3 TC (13632 Abnormal, 17512 + 17513 Normal), thiếu Boundary; 3/3 skip nên không có kết luận nào về "số báo = số thật" | `[MAJOR]` |
| Q8 | `REG-SHARED-001` | `Cao` | `RISK` — 3 TC (13644, 13645, 13670) **đều Normal**, thiếu Abnormal + Boundary. TC duy nhất pass (13670) là rà source tĩnh. Chưa có TC runtime chứng minh **các loại notify khác** (chat 1:1 type 1, kết bạn type 2, 配信数アラート type 15) không dính dấu phẩy lạ sau deploy — REQ-008 hiện chỉ được kiểm bằng đọc code (13671) | `[MAJOR]` |
| Q9 | `SYNC-APP-001` | `Trung bình` | `RISK` — 4 TC (17510, 13646, 13648, 13649), **4/4 skip qua cả 3 run**. Tính năng có mặt trên cả web và app nhưng không nhánh nào được verify | `[MAJOR]` |

> `MSG-003` / `MSG-005` / `PERF-LARGE-001` **không** flag: bản fix không chạm logic gửi tin hay quota — broadcast chỉ là bối cảnh dựng dữ liệu. Không đề xuất TC cho tầng gửi tin.

---

## 3. TC trùng lặp nội dung

Đã rà **24/24 TC** theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`) — **không phát hiện nhóm trùng đủ 4 yếu tố**.

| Nhóm | TC liên quan | Kết luận | Loại trùng | 4 yếu tố | Severity |
|---|---|---|---|---|---|
| N-1 | `NEW-21` (17510) vs `NEW-12` (13646) + `NEW-15` (13649) | **Không phải trùng** — chồng lấn một phần ở ý "broadcast → PC/app hiển thị có dấu phẩy", nhưng khác `loại case` (Boundary 12.345 vs Normal 1.234) và khác `tiền đề` (số người khác nhau) → chỉ 2/4 yếu tố | — | `mã quan điểm ✔ · thao tác ✔ · loại case ✘ · tiền đề ✘` | `[NIT]` |
| N-2 | `NEW-16` (13670) vs `NEW-17` (13671) | **Không phải trùng** — cùng REQ-007 và cùng là kiểm tĩnh trên source, nhưng khác ý định: 13670 tìm **chỗ sót thứ ba**, 13671 kiểm **phạm vi thay đổi đúng 2 dòng** | — | `mã quan điểm ✘ (REG-SHARED-001 vs TOOL-NEGCTRL-001) · thao tác ✘` | `[NIT]` |
| N-3 | `NEW-5` (13632) vs `NEW-23` (17512) | **Không phải trùng** — cùng ý "số báo = số gửi thành công, không phải số đối tượng", nhưng cơ chế tạo chênh lệch khác nhau (friend chặn bot vs friend ngoài điều kiện lọc) và khác loại case | — | `mã quan điểm ✔ · thao tác ✔ · loại case ✘ · tiền đề ✘` | `[NIT]` |

- **Gate đã chạy**: không đề nghị xóa TC nào → coverage §1 + §2 giữ nguyên.
- `DUP-INFLATE`: không có. Lưu ý ngược lại — bộ TC **không** bị thổi phồng bằng TC trùng; con số 24 TC là thật, vấn đề nằm ở **khâu thực thi**, không ở khâu thiết kế.
- `DUP-CONFLICT`: không có.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | Toàn bộ task #231 | **0/24 TC chứng minh được hành vi fix end-to-end.** 4 TC pass đều là kiểm tra tĩnh: 13670 grep source · 13671 so 2 nhánh source · 13672 đọc locale JVM · 13673 **áp giá trị vào khuôn chuỗi trong một JVM riêng**. Không TC nào có bằng chứng: chạy job thật → bản ghi notify thật chứa 「1,234人」. `NEW-19` (13673) là **tautology** — nó chứng minh `String.format("%,d")` hoạt động, không chứng minh sản phẩm đúng | Chạy lại bộ TC sau khi gỡ 3 blocker hạ tầng (I4 / I5 / I6). **Không đóng ticket** trên cơ sở 4 TC tĩnh hiện tại |
| I2 | `[BLOCKER]` | `NEW-1, NEW-2, NEW-3, NEW-4` (13628-13631) | 4 TC `error` nhưng `bugs.total = 0` — **chưa raise ticket bug nào**. Triage ghi nguyên nhân là harness (stub LINE API trả body rỗng `{}` → `LineModel.sendPushMessageV2` coi là "Unknown Error." → `successSendCount` không tăng → notify ra 「配信人数 0人」), nhưng lỗi hạ tầng test vẫn phải có ticket theo dõi, nếu không sẽ lặp lại ở round sau | Raise 1 ticket hạ tầng test cho stub LINE API; gắn vào 4 TC này |
| I3 | `[BLOCKER]` | Kết quả thực thi task | `pass 4/24 = 16.7%` — dưới ngưỡng 50%. Phân bố: 4 pass · 4 error · 16 skip · 0 fail. Trong khi đó `aiResult = "pass"` và `status = done-ai`, còn run mới nhất (#1317) lại có `status = fail` | Sửa nhãn task hoặc chặn chuyển `done-ai` khi pass rate dưới ngưỡng; Leader không duyệt round 1 |
| I4 | `[BLOCKER]` | `NEW-6, NEW-7, NEW-8, NEW-9` (13638-13641) | Nhánh **action schedule** bị chặn ở bước UI qua cả 3 run: run 1317 "không thấy ô nhập tên lịch trên màn tạo mới アクションスケジュール実行"; run 555 / 876 "không vào được ngữ cảnh bot". Đây là 1 trong 2 điểm sửa của ticket | Xác minh màn tạo mới action schedule trên env test (UI đổi? thiếu quyền?). Gỡ xong mới chạy lại |
| I5 | `[MAJOR]` | `NEW-10, NEW-11, NEW-13, NEW-20, NEW-21..24` (13644, 13645, 13647, 13674, 17510-17513) | 9 TC skip với lý do `[SOURCE_BLOCKED] web: SOURCE_BRANCH_MISSING: project web chưa có branch run` — project web chưa được cấp nhánh chạy, nên **toàn bộ tầng hiển thị** (chuông LME-Studio, web push PC) không kiểm được | Cấp branch run cho project web, hoặc chuyển các TC này sang chạy manual |
| I6 | `[MAJOR]` | `NEW-14` (13648) · `NEW-15` (13649) | Thiếu **tài nguyên test thật**: không có phòng Chatwork + token (`notify_setting.notification_room_url` rỗng, `ENABLE_NOTIFY_CHATWORK=0`, `TOKEN_API_BOT_CHATWORK` trống); không có thiết bị di động thật + FCM credential. Runner đã tự chặn không chấm pass bằng cách đọc code — **đúng nguyên tắc** — nhưng 2 kênh output cuối vẫn trống bằng chứng (RULE-06) | Leader cấp phòng Chatwork + token và 1 thiết bị thật đã cài app, hoặc chấp nhận 2 TC này chạy manual trên production |
| I7 | `[MAJOR]` | Môi trường thực thi | **0 TC chạy production**, 24/24 chạy `env = local`. Task chạm **job nền** → RULE-08 / ENV-003 cấm kết luận từ local. Đặc biệt: locale JVM và việc production tách 3 job độc lập đều không tồn tại ở local | Bắt buộc 1 vòng verify trên production — xem TC đề xuất §5 (`Phạm vi ENV = product`) |
| I8 | `[MAJOR]` | `NEW-4` (13631) | Fixture sai gói cước: bot kiểm thử ở `plan_type = 2` (gói FREE, trần 1.000通) trong khi TC cần 1.234 người nhận → 802 dòng `error_code = 'reach_limit_free'`. Kể cả khi stub LINE được sửa, TC này vẫn không thể ra 1.234 | Đổi bot test sang gói không giới hạn trước khi chạy lại |
| I9 | `[MAJOR]` | 6 TC mang mã quan điểm lạ | `TOOL-KNOW-002` (13628, 13638) · `TOOL-NEGCTRL-001` (13647, 13671) · `I18N-001` (13672) · `TOOL-OLDREC-001` (13674) — không có trong `framework/checklist-lme.md` nên **không tính là cover**. Nghiêm trọng nhất: 13628 và 13638 chính là 2 TC tái hiện bug gốc của 2 luồng, lại không map được vào quan điểm nào | Gán lại mã trên Studio (`testcase_update`): 13628 → `FUNC-004` (Normal) · 13638 → `FUNC-004` (Normal) · 13674 → `COMPAT-LEGACY-001` · 13672 → `ENV-003` · 13647 + 13671 → `REG-SHARED-001`. Việc này **tự nó lấp Q5 và một phần Q4** |
| I10 | `[MAJOR]` | `spec_delta` của task #231 | `diffAvailable = false` — "không tìm thấy ref release-t07-2026 (đã thử release-t07-2026, origin/release-t07-2026)". Studio còn khai `repo = /workspace/source/sns-line` trong khi 2 file sửa nằm ở `linect-service`. Chiều (b) vì vậy không có diff thật để đối chiếu | Sửa mapping repo / branch của task trên Studio rồi tính lại `spec_delta` |
| I11 | `[MAJOR]` | `03-dev-impact.md` | File có `Auto-filled: 2026-09-12 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" chưa tick** — nội dung 4 mục đang là bản máy parse từ Journal #133006, chưa ai đối chiếu lại với Redmine. `F*` / `D*` / `T*` có thể thiếu hoặc map sai, mà đây là input chính của coverage §1 | Tester đọc lại Journal #133006 trên Redmine, đối chiếu mục 4.1 / 4.2 / 4.3 rồi tick checkbox. Đặc biệt xác nhận mục 4.2 "Không có data bị update" — spec `notify-setting/db/db-mapping.md` cho thấy mỗi lượt notify vẫn **INSERT** một bản ghi `mobile_notify` mới |
| I12 | `[MINOR]` | `NEW-2` (13629) | Oracle không cô lập: cửa sổ quan sát bắt luôn notify của TC trước (bản ghi #908068612 「TC39176_配信E」 của 13632 lẫn vào phép đếm) → TC mong đợi đúng 1 bản ghi mới nhưng thấy 2 | Lọc oracle theo `title_message` / `notify_time` của chính lượt gửi, không lấy theo bot |
| I13 | `[MINOR]` | `NEW-21` (17510) | Không atomic — 1 TC verify đồng thời 3 kênh (App + Chatwork + PC). Một kênh hỏng là cả TC fail, khó triage; TC lại cần đủ thiết bị thật + Chatwork room nên khó chạy trọn vẹn | Tách 3 TC theo kênh, hoặc giữ nguyên nhưng ghi rõ tiêu chí pass từng phần |
| I14 | `[MINOR]` | Cả 24 TC | `spec_status` (cột canonical `Trạng thái đánh giá spec`) = null toàn bộ. Studio không có field tương đương nên đây là khác biệt format nguồn, không phải lỗi member | Không bắt buộc sửa; nếu push về Sheet thì điền `Spec ghi rõ` — spec cho cả 2 chuỗi đều có (xem §6) |
| I15 | `[NIT]` | `NEW-18` (13672) | TC tên "vùng ngôn ngữ cho ra dấu phẩy" nhưng evidence thực tế chỉ quan sát được chuỗi 「配信人数 0人」 (số < 1.000, không có dấu phân cách) — tức phần oracle quan trọng nhất không được quan sát mà vẫn chấm pass | Ràng buộc TC: chỉ pass khi quan sát được ít nhất 1 chuỗi notify thật có số ≥ 1.000 |

> **Chất lượng thiết kế TC**: bộ 24 TC **đạt** cả 5 mục rà ở BƯỚC 4a — expected đo lường được tới từng ký tự (kể cả hai khoảng trắng trước 「タイトル：」), tiền đề đủ dựng env, tiêu đề chứa keyword, dữ liệu test thật. Vấn đề của task này nằm **hoàn toàn ở khâu thực thi**, không ở khâu viết TC.

---

## 5. TCs đề xuất bổ sung (14)

**Đã đối chiếu trước khi viết** (BƯỚC 5a / 5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa008-broadcast-メッセージ配信.md` (nhóm 「配信数 & danh sách friend đã gửi」, 「Job — quá hạn, lỗi & resume」, 「Phân quyền & môi trường」). Kho **chưa có** tính năng action schedule / 通知設定 → phần 「対象人数」 không đối chiếu được |
| Vùng regression phát hiện từ kho | `TC-BC-344` (NOTI-MAIL-001 — "Thông báo mobile khi broadcast gửi xong", môi trường PRODUCTION) · `TC-BC-323` (REG-SHARED-001 — hồi quy luồng broadcast sau deploy) · `TC-BC-279` (JOB-001 — job broadcast quá hạn) |
| Conflict expected vs kho | `Không` — `TC-BC-344` chỉ yêu cầu "nội dung nêu đúng tên broadcast và trạng thái hoàn tất", không nói gì về định dạng số → không mâu thuẫn, chỉ thiếu chiều |
| GAP dùng lại TC kho (không viết mới) | `Không dùng lại nguyên` — `TC-BC-344` là TC gần nhất nhưng expected chưa kiểm dấu phân cách; `TC-SYNCAPP001-01` dưới đây viết mở rộng từ nó (đã ghi `dẫn từ TC-BC-344`) |
| Xác nhận chống trùng | Đã đối chiếu **24 TC** ở BƯỚC 0 + `kho-tcs/fa008` — **không TC đề xuất nào trùng**. `Q5` (FUNC-004 thiếu Normal) và `Q6` (DATA-COUNT-001 thiếu loại case) **không đẻ TC mới** vì TC đã tồn tại, chỉ sai mã quan điểm → xử lý bằng `I9` (gán lại mã trên Studio) |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-ENV003-01 | UI | ENV-003 | Notify broadcast — 「メッセージ配信」 | Normal | manual | product | Trên production, broadcast tới 1.234 friend sinh notify 「配信人数 1,234人」 | Bot production có đúng 1.234 friend còn hoạt động gắn thẻ riêng, gói cước không giới hạn số tin. Màn 「通知設定」 bật 「スマートフォンアプリ」 ở chế độ thời gian thực, tick 「一斉配信」 | 1. Tạo broadcast tên 「TC39176_PRD_A」, chọn đối tượng là tệp 1.234 friend<br>2. Gửi và chờ job broadcast production chạy xong<br>3. Mở chuông thông báo trên LME-Studio, đọc nguyên văn dòng mới nhất<br>4. Chụp màn hình dòng thông báo | Tệp 1.234 friend · tên broadcast 「TC39176_PRD_A」 | Dòng thông báo hiển thị nguyên văn 「配信人数 1,234人  タイトル：TC39176_PRD_A」 — có dấu phẩy, giữ đúng hai khoảng trắng trước 「タイトル：」, không hiển thị 「1234人」 | | Lấp `Q1` `G1` · RULE-08: job nền → production · Đánh giá spec: Spec ghi rõ (`message-send-all/job/job-spec.md:440`) · Evidence: screenshot chuông |
| TC-ENV003-02 | UI | ENV-003 | Notify broadcast — ký tự phân cách theo môi trường | Abnormal | manual | product | Ký tự phân cách trên notify sinh ở production là dấu phẩy, không phải dấu chấm hay khoảng trắng | Đã có notify sinh trên production ở `TC-ENV003-01`. Biết cấu hình khởi chạy JVM của job production | 1. Đọc dòng thông báo vừa sinh trên production, ghi lại đúng ký tự nằm giữa 「1」 và 「234」<br>2. Đối chiếu với ký tự quan sát được trên môi trường local<br>3. Nếu 2 môi trường khác nhau, ghi lại tham số locale khởi chạy của job ở từng nơi | Chuỗi notify thật có số ≥ 1.000 sinh trên production | Ký tự phân cách trên production đúng là dấu phẩy 「,」 và giống hệt môi trường local. Không ra dấu chấm 「.」, khoảng trắng hay ký tự khác | | Lấp `Q1` `G7` · RULE-08 · TC chỉ được chấm đạt khi quan sát chuỗi có số ≥ 1.000, không chấm bằng chuỗi 「0人」 · Evidence: screenshot 2 môi trường |
| TC-ENV003-03 | UI | ENV-003 | Notify action schedule — 「アクションスケジュール実行」 | Boundary | manual | product | Trên production (3 job tách độc lập), cả 2 luồng notify đều có dấu phân cách | Bot production có 1.234 friend gắn thẻ riêng. Đã tạo 1 action schedule tên 「TC39176_PRD_AS」 lọc đúng tệp đó. Màn 「通知設定」 bật 「アクションスケジュール実行」 ở chế độ thời gian thực | 1. Đặt action schedule chạy sau 5 phút, chờ tới giờ<br>2. Mở chuông thông báo, đọc dòng 「アクションスケジュール実行」<br>3. Trong cùng ngày, gửi thêm 1 broadcast tới cùng tệp<br>4. Đọc dòng 「メッセージ配信」<br>5. So sánh định dạng số của 2 dòng | Tệp 1.234 friend · lịch 「TC39176_PRD_AS」 | Dòng action schedule hiển thị 「TC39176_PRD_ASが稼働しました（対象人数 1,234人）」 giữ nguyên cặp ngoặc tròn kiểu Nhật; dòng broadcast hiển thị 「配信人数 1,234人  タイトル：...」. Cả hai cùng dùng dấu phẩy, không dòng nào ra dạng liền | | Lấp `Q1` `G2` · RULE-08 · Đánh giá spec: Spec ghi rõ (`friend-list/job/job-spec.md:202`) · Evidence: screenshot 2 dòng thông báo |
| TC-JOB001-01 | UI | JOB-001 | Job broadcast — số gửi thành công khi có lỗi gửi | Normal | manual | product | Có friend gửi lỗi — job không dừng, notify sinh đúng một lần với số thành công thật | Bot production có 1.200 friend gắn thẻ riêng, trong đó đúng 100 friend đã chặn bot. Bật notify 「一斉配信」 ở chế độ thời gian thực | 1. Gửi broadcast 「TC39176_PRD_ERR」 tới tệp 1.200 friend<br>2. Chờ job chạy xong, không can thiệp<br>3. Đọc dòng thông báo trên chuông<br>4. Đếm số bản ghi thông báo mới sinh cho bot trong khoảng thời gian đó<br>5. Đối chiếu con số trong chuỗi với số friend nhận thành công trên màn lịch sử gửi | Tệp 1.200 friend, 100 friend đã chặn bot | Job chạy tới cùng, không treo. Sinh **đúng một** bản ghi thông báo với nội dung 「配信人数 1,100人  タイトル：TC39176_PRD_ERR」 — số là 1.100 người thành công chứ không phải 1.200 người đối tượng, và vẫn có dấu phẩy | | Lấp `Q2` · RULE-08 · Evidence: screenshot chuông + màn lịch sử gửi |
| TC-JOB001-02 | UI | JOB-001 | Job broadcast — nhiều bot chạy đồng thời | Abnormal | manual | product | Hai bot gửi broadcast cùng lúc — mỗi bot nhận đúng notify số của mình, không lẫn số | Hai bot production khác nhau: bot A có 1.234 friend, bot B có 2.345 friend. Cả hai đều bật notify 「一斉配信」 thời gian thực. Có tài khoản quản trị xem được chuông của từng bot | 1. Hẹn giờ để 2 broadcast của 2 bot gửi trong cùng một phút<br>2. Chờ cả 2 job chạy xong<br>3. Mở chuông của bot A, đọc dòng mới nhất<br>4. Chuyển sang bot B, đọc dòng mới nhất<br>5. Kiểm tra không bot nào nhận thừa dòng thông báo của bot kia | Bot A 1.234 friend / 「TC39176_PRD_BOTA」 · bot B 2.345 friend / 「TC39176_PRD_BOTB」 | Chuông bot A chỉ có 「配信人数 1,234人  タイトル：TC39176_PRD_BOTA」; chuông bot B chỉ có 「配信人数 2,345人  タイトル：TC39176_PRD_BOTB」. Không bot nào thấy số hoặc tiêu đề của bot còn lại, cả hai số đều có dấu phẩy | | Lấp `Q2` · RULE-08 · Đây cũng là chiều mà `I11` (oracle lẫn notify giữa các lượt) làm lộ ra · Evidence: screenshot chuông 2 bot |
| TC-JOB001-03 | UI | JOB-001 | Job — broadcast và action schedule trùng thời điểm | Boundary | manual | product | Cùng bot, broadcast và action schedule chạy trùng giờ — sinh 2 notify riêng, đúng tiêu đề và đúng số | Bot production có 2 tệp friend riêng: tệp X 1.234 friend, tệp Y 999 friend. Bật cả 「一斉配信」 và 「アクションスケジュール実行」 ở chế độ thời gian thực | 1. Đặt action schedule lọc tệp Y chạy lúc HH:MM<br>2. Đặt broadcast gửi tệp X cũng lúc HH:MM<br>3. Chờ qua thời điểm đó<br>4. Đọc toàn bộ dòng thông báo mới trên chuông | Tệp X 1.234 friend · tệp Y 999 friend | Sinh đúng 2 bản ghi riêng biệt: một dòng 「メッセージ配信」 với 「配信人数 1,234人」 (có dấu phẩy) và một dòng 「アクションスケジュール実行」 với 「対象人数 999人」 (dạng liền, không dấu phẩy). Hai dòng không lẫn số của nhau | | Lấp `Q2` `G4` · RULE-08 · Evidence: screenshot chuông |
| TC-REGRUN001-01 | UI | REG-RUN-001 | Release — broadcast đang chạy dở khi deploy | Normal | manual | product | Deploy bản fix khi broadcast đang delivering — lượt đó vẫn hoàn tất, sinh đúng một notify | Có broadcast tệp ≥ 1.234 friend đang ở trạng thái delivering trên production. Đã hẹn được thời điểm deploy job service. Bật notify 「一斉配信」 | 1. Gửi broadcast tệp 1.234 friend, chờ tới khi trạng thái chuyển delivering<br>2. Trong lúc đang gửi dở, deploy bản fix cho job service<br>3. Chờ lượt gửi kết thúc<br>4. Đọc dòng thông báo sinh ra và ghi lại định dạng số<br>5. Kiểm tra không friend nào nhận tin trùng và không friend nào bị bỏ sót | Broadcast 1.234 friend, deploy giữa chừng lượt gửi | Lượt gửi hoàn tất, không gửi lại từ đầu, không mất friend. Sinh **đúng một** bản ghi thông báo. Ghi lại định dạng số thực tế của dòng này (dạng liền hay có dấu phẩy tuỳ thời điểm tiến trình nạp code) và báo Leader ngay nếu sinh ra hai bản ghi | | Lấp `Q3` · RULE-08 · regression · Evidence: log job + screenshot chuông + số friend đã nhận |
| TC-REGRUN001-02 | UI | REG-RUN-001 | Release — action schedule lên lịch trước deploy | Abnormal | manual | product | Lịch đặt trước deploy, tới giờ chạy sau deploy — chạy đúng một lần, notify theo định dạng mới | Có action schedule tạo TRƯỚC thời điểm deploy, đặt chạy vào thời điểm SAU deploy, lọc tệp 1.234 friend. Bật notify 「アクションスケジュール実行」 | 1. Tạo action schedule 「TC39176_PRD_PRE」 trước khi deploy, đặt giờ chạy sau deploy 30 phút<br>2. Deploy bản fix<br>3. Chờ tới giờ lịch chạy<br>4. Đọc dòng thông báo và màn lịch sử thực thi của lịch đó | Lịch tạo trước deploy, chạy sau deploy, tệp 1.234 friend | Lịch chạy đúng một lần, không bị bỏ qua và không chạy lại. Dòng thông báo hiển thị 「TC39176_PRD_PREが稼働しました（対象人数 1,234人）」 có dấu phẩy theo bản đã sửa | | Lấp `Q3` `G2` · RULE-08 · regression · Evidence: screenshot chuông + lịch sử thực thi |
| TC-COMPATLEGACY001-01 | UI | COMPAT-LEGACY-001 | Chuông thông báo — bản ghi cũ và mới song song | Normal | manual | Tất cả | Chuông hiển thị đồng thời bản ghi trước fix (dạng liền) và sau fix (có dấu phẩy) | Môi trường có ít nhất 1 bản ghi notify loại 「メッセージ配信」 hoặc 「アクションスケジュール実行」 tạo TRƯỚC ngày deploy bản fix, với số người ≥ 4 chữ số. Đã ghi lại mốc thời điểm deploy | 1. Sinh thêm 1 notify MỚI sau deploy với số ≥ 1.000<br>2. Mở chuông thông báo, cuộn tới vùng có cả bản ghi cũ và bản ghi mới<br>3. Chụp màn hình có đồng thời 2 dòng<br>4. Tải lại trang, đọc lại cả 2 dòng | 1 bản ghi trước deploy (vd 「配信人数 12345人」) · 1 bản ghi sau deploy | Dòng cũ giữ NGUYÊN dạng liền 「配信人数 12345人」, không tự sinh dấu phẩy và không bị cập nhật lại. Dòng mới hiển thị 「配信人数 12,345人」. Hai dòng khác định dạng nhau là đúng thiết kế. Sau khi tải lại trang, cả hai không đổi | | Lấp `Q4` `G8` · RULE-09: evidence phải có CẢ hai nhánh cũ/mới trong cùng một ảnh · Đánh giá spec: Spec ghi rõ (fix ở tầng ghi, không có migration) · Evidence: screenshot 2 dòng |
| TC-COMPATLEGACY001-02 | UI | COMPAT-LEGACY-001 | App mobile / Chatwork / PC push — bản ghi cũ | Abnormal | manual | Tất cả | Bản ghi cũ khi hiển thị lại trên app và các kênh không bị định dạng lại, không lỗi | Có thiết bị di động thật đã cài app LME-Studio, đăng nhập tài khoản quản trị của bot. Bot có bản ghi notify cũ dạng liền số ≥ 4 chữ số | 1. Mở danh sách thông báo trong app mobile<br>2. Tìm dòng của bản ghi cũ, đọc nguyên văn<br>3. Đối chiếu với dòng tương ứng trên chuông web<br>4. Nếu có phòng Chatwork đã liên kết, kiểm tra tin nhắn cũ trong phòng vẫn giữ nguyên văn | Bản ghi notify tạo trước deploy | Bản ghi cũ hiển thị dạng liền y như trước ở mọi kênh, không bị thêm dấu phẩy, không mất nội dung và không gây lỗi hiển thị. Nội dung trên app khớp từng ký tự với chuông web | | Lấp `Q4` `G3` · RULE-06: đi tới output cuối trên thiết bị thật · regression · Evidence: screenshot app + chuông web |
| TC-DATACOUNT001-01 | Data | DATA-COUNT-001 | Notify action schedule — đối chiếu 3 tầng | Boundary | manual | product | Bản ghi notify của action schedule: số trong chuỗi, cột số người và số hiển thị khớp nhau | Bot production có đúng 1.234 friend gắn thẻ riêng. Action schedule lọc đúng tệp đó. Có quyền xem dữ liệu bản ghi thông báo trên môi trường test | 1. Chạy action schedule tới khi sinh notify<br>2. Đọc chuỗi thông báo trên chuông, ghi lại con số hiển thị<br>3. Mở màn lịch sử thực thi của lịch, ghi lại số đối tượng<br>4. Đối chiếu con số ở 3 nơi: chuỗi thông báo, số trên màn lịch sử, và giá trị cột số người của bản ghi thông báo | Tệp 1.234 friend | Chuỗi hiển thị 「対象人数 1,234人」 (có dấu phẩy). Bỏ dấu phẩy đi thì bằng đúng 1.234 — khớp số trên màn lịch sử thực thi. Cột lưu số người vẫn là số nguyên 1234 dạng thuần số, KHÔNG chứa dấu phẩy và không bị đổi thành chuỗi | | Lấp `Q6` `G5` · RULE-07: verify 3 tầng · Chiều này hiện chỉ có ở luồng broadcast (13631), chưa có ở action schedule · Evidence: screenshot chuông + lịch sử + giá trị cột số |
| TC-REGSHARED001-01 | UI | REG-SHARED-001 | Notify các loại sự kiện khác — đối chứng âm runtime | Abnormal | manual | Tất cả | Sau deploy, notify của chat 1:1 và kết bạn giữ nguyên nội dung, không dính dấu phẩy lạ | Bot đã bật notify cho 「1:1チャット」 và 「友だち登録情報」 ở chế độ thời gian thực. Có friend test gửi được tin vào chat và 1 tài khoản LINE chưa kết bạn để thêm mới | 1. Dùng friend test gửi 1 tin nhắn vào chat 1:1 với bot<br>2. Đọc dòng thông báo 「1:1チャット」 vừa sinh trên chuông<br>3. Dùng tài khoản LINE chưa kết bạn, kết bạn với bot<br>4. Đọc dòng thông báo 「友だち登録情報」 vừa sinh<br>5. So sánh nguyên văn 2 dòng với ảnh chụp cùng loại thông báo trước deploy | 1 tin nhắn chat 1:1 · 1 lượt kết bạn mới | Cả 2 dòng thông báo giữ nguyên nội dung như trước deploy, không xuất hiện dấu phẩy lạ ở bất kỳ con số nào và không mất nội dung | | Lấp `Q8` · regression · REQ-008 hiện mới chỉ được kiểm bằng đọc code (13671), chưa có bằng chứng runtime · Evidence: screenshot trước/sau deploy |
| TC-REGSHARED001-02 | UI | REG-SHARED-001 | Notify 「配信数アラート」 — số ngưỡng không đổi định dạng | Boundary | manual | product | Thông báo cảnh báo số lượng tin nhắn hiển thị số theo đúng định dạng cũ | Bot production đã bật 「配信数アラート」 với một ngưỡng cụ thể. Có thể đẩy số tin đã gửi của bot chạm ngưỡng đó | 1. Gửi tin cho tới khi số tin của bot chạm ngưỡng đã đặt<br>2. Chờ thông báo 「配信数アラート」 sinh ra<br>3. Đọc nguyên văn dòng thông báo, ghi lại cách hiển thị con số ngưỡng<br>4. So sánh với ảnh chụp cùng loại thông báo trước deploy | Ngưỡng cảnh báo đã đặt trên bot | Dòng 「配信数アラート」 hiển thị con số theo đúng định dạng như trước deploy — bản sửa chỉ chạm 2 chuỗi của broadcast và action schedule, loại thông báo này không được đổi | | Lấp `Q8` · RULE-08 · regression · Loại notify này cũng chứa số nên là đối chứng âm sát nhất · Evidence: screenshot trước/sau deploy |
| TC-SYNCAPP001-01 | UI | SYNC-APP-001 | Notify broadcast — app mobile thật | Normal | manual | product | App mobile, chuông web và Chatwork cùng hiển thị một con số một định dạng | Thiết bị di động thật đã cài app LME-Studio, đăng nhập tài khoản quản trị của bot production, đã bật quyền nhận thông báo đẩy. Đã liên kết một phòng Chatwork test. Bật cả 3 kênh 「スマートフォンアプリ」, 「PCデスクトップ」, 「ChatWork」 ở chế độ thời gian thực cho 「一斉配信」 | 1. Gửi broadcast 「TC39176_PRD_APP」 tới tệp 1.234 friend<br>2. Chờ job chạy xong<br>3. Đọc thông báo đẩy hiện trên màn hình thiết bị<br>4. Mở danh sách thông báo trong app, đọc dòng tương ứng<br>5. Mở phòng Chatwork, đọc tin nhắn vừa nhận<br>6. Mở chuông trên trình duyệt, đọc dòng tương ứng | Tệp 1.234 friend · tên 「TC39176_PRD_APP」 | Cả bốn chỗ (đẩy trên thiết bị, danh sách trong app, phòng Chatwork, chuông web) đều hiển thị 「1,234人」 — cùng một con số, cùng một định dạng, không chỗ nào ra 「1234人」. Các kênh chỉ được nối thêm tiền tố loại sự kiện, không định dạng lại con số | | Lấp `Q9` `G3` · RULE-06 + RULE-08 · dẫn từ `TC-BC-344` (kho fa008 — TC đó chỉ kiểm tên broadcast + trạng thái, chưa kiểm định dạng số) · Evidence: screenshot thiết bị thật + Chatwork + chuông |

**Các dòng §1 / §2 không đề xuất TC mới** (có lý do):

- `G6` — thiếu diff là vấn đề cấu hình Studio, không phải thiếu TC → xử lý ở `I10`.
- `Q5` (FUNC-004 thiếu Normal) và `Q7` (OUT-TRUTH-001 thiếu Boundary) — TC đã tồn tại trong bộ 24, chỉ sai mã quan điểm hoặc chưa chạy; đẻ TC mới sẽ tạo trùng. Xử lý bằng `I9` (gán lại mã) + chạy lại sau khi gỡ blocker `I4` / `I5` / `I6`.

---

## 6. Spec update needed

`Không cần update spec.`

- Spec mô tả đúng khuôn chuỗi của cả 2 luồng và không mâu thuẫn với cách fix: `spec-features/admin/message-send-all/job/job-spec.md:440` ghi `配信人数 {count}人  タイトル：{broadcastName}`; `spec-features/admin/friend-list/job/job-spec.md:202` ghi 「{name}が稼働しました（対象人数 {N}人）」. Cả hai để `{count}` / `{N}` dưới dạng placeholder nên không ràng buộc định dạng — bản fix chỉ làm rõ cách render, không đổi khuôn.
- `spec-features/admin/notify-setting/job/job-spec.md` xác nhận cột `send_number` là "Số lượng gửi (dùng cho broadcast)" kiểu Integer → yêu cầu "cột số giữ nguyên số nguyên thô" ở `TC-DATACOUNT001-01` là đúng spec, không cần chốt thêm.
- Không phát hiện `DUP-CONFLICT` ở §3 và không có conflict expected với `kho-tcs/fa008` ở §5.
