# 05 — Review Report

## 0. Nguồn TC

- **Nguồn đã dùng**: MCP LME TEST STUDIO — task #292 (ticket 40835), round 1, `status = tc-ready`, branch `ai_fixbug_40835`.
- **Tổng số TC review**: 27.

---

## 1. Coverage — `dev-impact` + `diff code`

**Kết luận**: 10/15 vùng ảnh hưởng đủ TC · **4 GAP** · **4 RISK**.

| Mã | Chiều | Vùng ảnh hưởng | TC hiện có | Status | Vấn đề |
|---|---|---|---|---|---|
| **G1** | `dev-impact` | **F4 `CalendarGoogleSheetService` + T3 Lesson / Calendar Booking (FA-019)** | **0 TC** | **GAP** | Dev kê T3 ở mục 4.3 (nguy cơ High) và `CalendarGoogleSheetService` ở mục 3, nhưng **không TC nào chạm luồng đặt lịch bài học**. Lesson booking chỉ xuất hiện trong danh sách call-site tĩnh của `NEW-2` và trong đối chứng âm `NEW-3` (đó là Google **カレンダー**, không phải スプレッドシート). 3/4 tính năng T1/T2/T4 có TC riêng, riêng T3 bị bỏ trắng. |
| **G2** | `diff code` | **Đổi tên bảng tính (rename sheet)** | **0 TC** | **GAP** | Mục 2 "Cách fix" liệt kê `đổi tên sheet` là một trong các luồng bị bound 5 giây; `NEW-2` cũng nêu trong danh sách thừa hưởng. Nhưng **không TC nào thực thi** luồng rename. Kho có `TC-FORM-378` cho luồng này ở mức chức năng. |
| **G3** | `diff code` | **Refill toàn bộ dữ liệu khi sheet bị đổi tên / xóa trên Google** | **0 TC** | **GAP** | Đây là **thao tác ghi lớn nhất** của cả hệ: kho `TC-FORM-378` ghi rõ "tạo sheet mới và fill lại **TOÀN BỘ** câu trả lời (cũ + mới)", `TC-LSN-471` ghi "các booking TRƯỚC ĐÂY cũng được get ra sheet mới". Với form/calendar nhiều nghìn bản ghi, refill gần như chắc chắn vượt 5 giây → nay **bị cắt**, trước đây thành công. `NEW-21` chỉ đo ghi thường trên QR/salon, **không đi qua đường refill**. Đúng "rủi ro hồi quy chính" mà `dev_impact` Studio nêu nhưng chưa có TC. |
| **G4** | `dev-impact` | **T1 Form Builder — đồng bộ câu trả lời biểu mẫu** | `NEW-23` | **RISK** | `NEW-23` đã kiểm chứng: lệnh đẩy job phía PHP **đã bị chú thích** (`FormAnswerService:1659`) và lệnh xử lý lỗi bỏ qua loại câu trả lời form → luồng thật do **job Java** chạy, **không đi qua phần mã được sửa**. Tức mục 4.3 T1 của Dev ("đồng bộ câu trả lời biểu mẫu nay dừng sau 5 giây") **mô tả sai thực tế** → xem §6. |
| **G5** | `diff code` | **Hệ quả trạng thái sau khi timeout** | `NEW-5`, `NEW-16` (một phần) | **GAP** | `NEW-5` chốt job → `FormAnswerConnectGoogle.status = 3 (lỗi)`; `NEW-16` chốt màn UI → `bots.google_sheet_status = 0 (tắt liên kết)`. Nhưng **không TC nào kiểm hệ quả tiếp theo**: kho `TC-FORM-365` ghi "chưa chạy, **đang chạy và lỗi**: báo lỗi 「スプレッドシートを作成しているため、接続を解除できません。」" → sau một lần Google treo, bot rơi vào trạng thái lỗi và **không hủy liên kết được**; còn `google_sheet_status = 0` thì ảnh hưởng **mọi form khác cùng bot** (kho `TC-FORM-364`: 1 account Google dùng cho nhiều bot). Không TC nào đo bán kính ảnh hưởng và đường phục hồi. |
| **G6** | `dev-impact` | **F5 Salon Booking — trang đặt lịch của LINE user** | `NEW-20` | **RISK** | `NEW-20` tự cho phép trả `skip` nếu "môi trường không mở được trang này ngoài ứng dụng LINE" → hành vi timeout **trên LINE in-app browser thật** (thứ khách cuối thực sự gặp) có thể không bao giờ được verify. Vi phạm RULE-06 nếu skip. |
| **G7** | `diff code` | **Client singleton dùng chung nhiều bot / nhiều tài khoản Google** | `NEW-2` (chỉ tĩnh) | **RISK** | `dev_impact` Studio nêu rõ ảnh hưởng lan toả qua `singleton getClient()->setHttpClient(guzzleClient)`. `NEW-2` chỉ **đọc mã** để khẳng định mọi nơi dùng chung client, **không có TC chạy thật** 2 bot / 2 tài khoản Google trong cùng một tiến trình queue để chứng minh token và dữ liệu không lẫn sang nhau sau khi client được dùng lại. Kho `TC-FORM-364` có kịch bản đa bot ở mức chức năng. |

| **G8** | `diff code` | **Ứng dụng di động Admin — thao tác booking / đổi trạng thái booking (FA-019)** | **0 TC** | **GAP** | `spec-features/admin/lesson-booking/feature-spec.md:78` xác nhận đây là **portal thứ 4** có thật: `POST /api/mobile/calendar-lesson/*` (guard `api-mobile`), **15 endpoint** duyệt / huỷ / hoàn tiền / thêm đặt chỗ / sửa khung giờ, chạy qua controller **riêng** `Mobile\CalendarController` (`:139`) — **không** phải `Basic\CalendarManagementController`. Các luồng này gọi thẳng `Google Sheet updateStatusBooking()` (`:461`, `:573`), `updateStatusPaymentBooking()` (`:672`) và `updateFriendInfo()` (`:1716`) qua **cùng `CalendarGoogleSheetService` → cùng singleton `GoogleSheetService`** ⇒ chịu đúng timeout 5 giây vừa thêm. Nghiêm trọng hơn: `:415` + `:1613` (RA-09 / RP-13) ghi rõ **"Gọi API ngoài ĐỒNG BỘ trong request"** → timeout 5 giây tác động **trực tiếp lên thời gian phản hồi API mobile**. Trong 27 TC Studio **không có một lần nào** nhắc app mobile / アプリ / điện thoại; `NEW-24`/`NEW-25`/`NEW-27` đều chỉ thao tác trên **màn quản trị web**. Đây là nơi bản fix có tác dụng rõ rệt nhất mà không ai kiểm. |

> Không có TC `orphan` — 27/27 TC đều trace được về một vùng ảnh hưởng hoặc một quan điểm cross-cutting (log/bảo mật, đối chứng âm).

---

## 2. Thiếu so với quan điểm test

**Kết luận**: 8 quan điểm Trigger khớp đã cover đủ · **10 quan điểm chưa cover đủ** (4 GAP, 6 RISK).

| Mã | Quan điểm | Ưu tiên | Tình trạng | Vấn đề | Severity |
|---|---|---|---|---|---|
| **Q1** | `ENV-003` ★ | Cao (BẮT BUỘC — job nền) | **GAP** | 27/27 TC khai `env_scope` = `local` (17) / `dev+staging` (4) / `staging` (3) / `all` (3) — **0 TC ở production**. Task chạm **job nền + domain Google + luồng đồng bộ dữ liệu khách hàng** → RULE-08 cấm kết luận từ local/staging. Kho cũng đánh dấu các TC Google Sheet cốt lõi (`TC-FORM-365`, `TC-FORM-378`) là "Môi trường: PRODUCTION". | `[MAJOR]` |
| **Q2** | `COMPAT-LEGACY-001` ★ | Cao (BẮT BUỘC — chạm `google spread`) | **GAP** | Trigger nêu đích danh "google spread". Không TC nào test **bot/sheet đã liên kết từ bản cũ**: kho `TC-FORM-372` ("Bot liên kết Google TRƯỚC bản sửa, không có email lưu") và `TC-FORM-370/371` (liên kết lại cùng/khác email) là dữ liệu cũ có thật trên production, đi qua đúng client vừa bị đổi cấu hình. RULE-09 cấm đánh × chỉ vì "dữ liệu mới chạy ổn". | `[BLOCKER]` |
| **Q3** | `REG-RUN-001` | Cao (BẮT BUỘC — có job/dữ liệu đang chạy dở) | **GAP** | Không TC nào xét tình huống **deploy trong khi hàng đợi còn job sheet dở**: job cũ chạy với client không timeout, job mới chạy với client timeout 5 giây, bản ghi `status = 1 (đang xử lý)` kẹt lại. | `[BLOCKER]` |
| **Q4** | `PERF-LARGE-001` | Trung bình → **Cao** (sync/export) | **RISK** | Nội dung đã có ở `NEW-21` nhưng TC mang mã `PERF-LATENCY-001` — **mã không tồn tại** trong `framework/checklist-lme.md` → không tính là cover. Ngoài ra `NEW-21` khai `staging`, trong khi đo độ trễ Google thật thuộc RULE-08 → phải chạy `product`. Không cần TC mới, cần **đổi mã + nâng env** (§4). | `[MAJOR]` |
| **Q5** | `OUT-TRUTH-001` | Cao (BẮT BUỘC — thao tác lưu/đồng bộ có thông báo kết quả) | **GAP** | Không TC nào mang mã này. Chính `NEW-16` đã tự ghi nhận một **false success**: "màn hình vẫn báo lưu thành công dù bảng tính chưa được tạo". Fix làm tình huống này **xảy ra thường xuyên hơn** (dừng ở 5 giây thay vì treo), nên độ đúng của thông báo cần là một quan điểm được kiểm riêng, không phải một dòng ghi chú. | `[BLOCKER]` |
| **Q6** | `INTG-HOOK-001` | Cao (BẮT BUỘC — nhận webhook/callback từ Google) | **RISK** | Nội dung đã có ở `NEW-17` (đường dẫn nhận kết quả liên kết OAuth khi Google chậm) nhưng mang mã `TOOL-ERRHYG-001` — mã nội bộ Studio, không map được coverage. Cần **đổi mã**, không cần TC mới. | `[MAJOR]` |
| **Q7** | `UI-003` | Trung bình → **Cao** (có rủi ro false success — điều kiện nâng đã khớp qua `NEW-16`) | **RISK** | Chỉ có `NEW-16` + `NEW-19`, **cả hai đều `Abnormal`**. Thiếu `Normal` và `Boundary` dưới mã này, không TC nào ghi lý do → RULE-01. | `[MAJOR]` |
| **Q8** | `JOB-001` ★ | Cao (BẮT BUỘC — sửa job nền / gọi API bên thứ 3 theo lô) | **RISK** | Chỉ `NEW-11` mang mã hợp lệ. 3 TC job quan trọng nhất (`NEW-4` tạo sheet form, `NEW-13`/`NEW-14` thử lại đặt lịch salon) mang mã `JOB-002` — **không tồn tại** trong checklist. Cần **đổi mã sang `JOB-001`**. | `[MAJOR]` |
| **Q9** | `LIFF-ENTRY-001` ★ | Cao (BẮT BUỘC — URL cho LINE user access: salon, lesson, form) | **RISK** | Chỉ `NEW-20` chạm trang đặt lịch salon và **cho phép skip** nếu không mở được ngoài LINE. Không TC nào mở form / trang đặt lịch **từ trong ứng dụng LINE thật** khi Google treo — đây là điểm khách cuối thực sự nhìn thấy (RULE-06). | `[MAJOR]` |

| **Q10** | `SYNC-APP-001` | Trung bình → **Cao** (flow critical: luồng có **hoàn tiền** và gọi Google Sheet **đồng bộ trong request**) | **GAP** | Trigger "tính năng có mặt trên cả web và app" **khớp rõ**: FA-019 có portal **Ứng dụng di động Admin** với 15 endpoint `/api/mobile/calendar-lesson/*`, controller riêng, gọi cùng `CalendarGoogleSheetService`. **0/27 TC** chạm app mobile — cả bộ chỉ test màn quản trị web. Đúng "bug kinh điển" mà quan điểm này cảnh báo: *"sửa Web nhưng quên triển khai sang app"*, ở đây là **test Web nhưng quên test app** dù cả hai dùng chung đúng một client vừa bị đổi cấu hình. Kho `fa019` đã có sẵn vùng này (`TC-LSN-615/616/617` mục "App mobile", `TC-LSN-522` admin approve ở APP MOBILE, `TC-LSN-527` khoá thao tác trên APP MOBILE) → không phải vùng lạ, chỉ là bị bỏ sót. | `[BLOCKER]` |

> Quan điểm đã cover đủ (không ghi chi tiết): `FUNC-001` · `FUNC-004` · `INTG-SHEET-001` · `REG-SHARED-001` · `ENV-001` · `SEC-002` · `DATA-001` · `STATE-001`.

---

## 3. TC trùng lặp

**Đã rà toàn bộ 27 TC theo 4 yếu tố** (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`) — **không phát hiện TC trùng ý định cần xóa**. Bộ TC phân tách consumer khá kỷ luật.

| Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| Kiểm tra tĩnh cấu hình 2 nhánh | `NEW-1` | **Không xóa TC nào** — chỉ gộp 1 assertion | `DUP-SUBSET` (một phần) | Cả `NEW-1` và `NEW-3` cùng khẳng định *"danh sách tệp thay đổi giữa hai nhánh chỉ có đúng một tệp"*. Trùng **duy nhất** ở assertion này; mục đích chính khác nhau (`NEW-1` = có timeout; `NEW-3` = Gmail/カレンダー **không** có timeout). | `[MINOR]` |

**3 cặp đã soi và kết luận KHÔNG trùng** (ghi lại để lần review sau không rà lại):

| Cặp | Vì sao không trùng |
|---|---|
| `NEW-5` (job form, blackhole) ↔ `NEW-6` (tái hiện trước/sau fix) | `NEW-6` lần 2 mô tả sơ hơn; `NEW-5` khẳng định thêm `status = 3`, nội dung lỗi `cURL 28`, dữ liệu form còn nguyên. Giá trị riêng của `NEW-6` nằm ở **nhánh "trước khi sửa"**. Xóa bên nào cũng mất cover. |
| `NEW-8` (token hết hạn, thư viện **tự** làm mới) ↔ `NEW-12` (token hết hạn, mã ứng dụng **tự gọi** làm mới) | Hai nhánh dùng **hai client khác nhau** — đúng điểm nghi ngờ trọng tâm của ticket (`REQ-006` vs `REQ-007`). Bắt buộc tách. |
| `NEW-16` (màn form, Google treo) ↔ `NEW-19` (màn QR action, Google treo) | Cùng `UI-003`/`Abnormal`/blackhole nhưng **khác consumer và khác cách xử lý lỗi** — `NEW-16` hạ `google_sheet_status = 0`, `NEW-19` chỉ để trống mã bảng tính. Expected khác nhau ⇒ không phải trùng. |

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | Toàn bộ 27 TC | **0/27 TC đã chạy** (`untested = 27`, `pass = 0%`, `ranAt = null`). Không có một kết luận test nào. Mọi đánh giá "đủ TC" ở §1/§2 chỉ là đủ **trên giấy**. | Chạy bộ TC trước khi Leader duyệt. Không ký kết quả dựa trên bộ chưa chạy. |
| 2 | `[MAJOR]` | Toàn bộ 27 TC | **0 TC khai `env_scope` production** trong khi task chạm job nền + domain Google → RULE-08 / `ENV-003`. | Nâng `env_scope` cho nhóm TC bắt buộc product (xem Q1 + TC đề xuất `TC-ENV003-01`, `TC-PERFLARGE001-01`). |
| 3 | `[MAJOR]` | 9/27 TC | **6 mã quan điểm không có trong `framework/checklist-lme.md`** → `/review-tc` không map được coverage. Bảng đổi mã ở dưới. | Đổi mã trên Studio (`testcase_update`) theo bảng, rồi fetch lại. |
| 4 | `[MAJOR]` | Toàn bộ 27 TC | `spec_status` = `null` ở **tất cả** TC → không TC nào khai đã đối chiếu spec hay chưa, cũng không nêu đã hỏi ai. Nguy cơ tự suy diễn expected rồi cho Đạt. | Điền `Trạng thái đánh giá spec` (`Spec ghi rõ` / `Spec không ghi` / `Đã hỏi leader`) cho từng TC. |
| 5 | `[MAJOR]` | `03-dev-impact.md` | Checkbox **"Tester verify auto-fill chính xác" chưa tick**; ngoài ra Redmine mục 4.1 **chỉ ghi "File thay đổi"**, Dev **không kê bảng function `F1..Fn`** — bảng `F1–F8` trong file 03 là do `/new-task` suy từ mục 3. Coverage chiều `dev-impact` đang dựa trên suy luận chưa được người xác nhận. | Tester đọc lại Redmine #40835, xác nhận/hiệu chỉnh bảng F, rồi tick checkbox. |

**Bảng đổi mã quan điểm** (mã Studio → mã hợp lệ trong `checklist-lme.md`):

| TC | Mã Studio hiện tại | Mã đề nghị | Lý do |
|---|---|---|---|
| `NEW-4`, `NEW-13`, `NEW-14` | `JOB-002` | `JOB-001` | Không tồn tại mã `JOB-002`; nội dung đúng là job nền / gọi API bên thứ 3 theo lô. |
| `NEW-1`, `NEW-6` | `TOOL-KNOW-002` | `NEW-1` → `REG-SHARED-001` · `NEW-6` → `FUNC-001` | Mã nội bộ Studio. `NEW-1` kiểm cấu hình dùng chung; `NEW-6` là cặp tái hiện/đã fix của chức năng. |
| `NEW-3` | `TOOL-NEGCTRL-001` | `REG-SHARED-001` | Đối chứng âm cho phạm vi code dùng chung. |
| `NEW-17` | `TOOL-ERRHYG-001` | `INTG-HOOK-001` | Đường dẫn nhận callback OAuth từ Google → đúng trigger `INTG-HOOK-001` (Q6). |
| `NEW-21` | `PERF-LATENCY-001` | `PERF-LARGE-001` | Mã đúng trong checklist là `PERF-LARGE-001` (Q4). Kèm nâng `env_scope` → `product`. |
| `NEW-18` | `RULE-12` | `OUT-EXPORT-001` | `RULE-12` là **RULE**, không phải mã quan điểm. Nội dung = luồng ghi Google Spreadsheet với tài khoản thật → `OUT-EXPORT-001` (nâng Cao). |

### Chất lượng từng TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 6 | `[MAJOR]` | `NEW-24` | **Không atomic** — 1 TC gộp **9 action** (create / approve / deny / approve cancel / deny cancel / cancel / change staff / random staff / delete) với 10 bước. Fail ở action thứ 3 thì 6 action sau không có kết luận, và không thể ghi kết quả từng nhánh. | Tách theo nhóm action (tối thiểu: nhóm duyệt/từ chối · nhóm hủy · nhóm đổi staff · delete). |
| 7 | `[MAJOR]` | `NEW-25`, `NEW-26` | Cùng vấn đề ở mức nhẹ hơn — `NEW-25` gộp 5 biến thể, `NEW-26` gộp 2 biến thể trong 1 TC. | Tách thành TC riêng cho mỗi biến thể, hoặc ghi rõ cách báo cáo kết quả từng biến thể. |
| 8 | `[MAJOR]` | `NEW-17` | `Kết quả mong đợi` **bỏ ngỏ**: "nếu quay về màn liên kết kèm thông báo lỗi thì Đạt; nếu hiện trang lỗi 500 thì Không đạt". TC không có chuẩn cố định → kết quả phụ thuộc người chạy. Nguyên nhân đã được TC tự nêu: mã chỉ bắt `\Google_Exception`, không bắt ngoại lệ tầng HTTP. | Chốt hành vi đúng với Dev/PO **trước khi chạy** (→ §6), rồi cố định expected. Giữ nguyên mốc 5 giây làm phần bắt buộc. |
| 9 | `[NIT]` `[AP-5]` | `NEW-24`, `NEW-25`, `NEW-26`, `NEW-27` | **Layer-downstream over-coverage** — diff chỉ đổi **cấu hình timeout của client HTTP** (15 dòng, 1 file), nhưng 4 TC thủ công nặng này kiểm **toàn bộ vòng đời booking + mapping cột Sheet + trạng thái thanh toán + friend info**, là phần mã **không bị chạm**. Ghi chú TC xác nhận "theo yêu cầu QA". | Không xóa, nhưng **re-label là `regression` diện rộng** và hạ ưu tiên chạy so với G1/G3 — nguồn lực thủ công nên dồn cho lesson booking (G1) và refill dữ liệu lớn (G3) vốn đang trắng TC. |
| 10 | `[MINOR]` | `NEW-1`, `NEW-3` | Trùng assertion "diff chỉ có đúng 1 tệp" (xem §3). | Giữ assertion ở `NEW-1`, bỏ khỏi `NEW-3` để `NEW-3` tập trung vào đối chứng âm Gmail/カレンダー. |

---

## 5. TCs đề xuất bổ sung (21)

### 5a — Đối chiếu kho TCs trước khi viết

**Đã đối chiếu 27 TC ở BƯỚC 0 + `kho-tcs/fa011-taobieumau-フォーム作成.md` + `kho-tcs/fa019-datlichbaihoc-レッスン予約.md` + `kho-tcs/fa020-datlichsalon-サロン・面談予約.md` — không TC đề xuất nào trùng.** (Kho **chưa có** FA-017 QR Code Action → không đối chiếu được phần QR/landing.)

**TC kho dùng lại được, KHÔNG viết mới** — chỉ cần chạy lại trên nhánh `ai_fixbug_40835`:

| GAP | TC kho dùng lại | Cần chỉnh gì cho bug hiện tại |
|---|---|---|
| **G1** (nhánh `Normal`) | `TC-LSN-461` — *Dữ liệu sync lên Google Sheet — booking trước và sau khi liên kết* | Chạy nguyên trạng trên nhánh fix để chứng minh không timeout oan. Không cần sửa steps. |
| **G2** (nhánh chức năng) | `TC-FORM-378` — *Đổi tên sheet hoặc xóa sheet trên Google → lần sync tiếp theo tạo sheet mới và fill lại TOÀN BỘ câu trả lời* | Chạy nguyên trạng trên nhánh fix; **bổ sung đo thời gian** thao tác refill (phần đo tách sang `TC-PERFLARGE001-01`). |
| **G5** (nhánh hiện trạng) | `TC-FORM-365` — *Hủy liên kết Google: đang tạo hoặc lỗi thì bị chặn* | Dùng làm **mốc đối chứng**; kịch bản "trạng thái lỗi **do timeout**" là phần mới, tách sang `TC-STATECLEAN001-01`. |
| **Q2** | `TC-FORM-372`, `TC-FORM-370`, `TC-FORM-371` — *bot liên kết Google trước bản sửa / liên kết lại cùng & khác email* | Chạy trên nhánh fix (xem `TC-COMPATLEGACY001-01` để gói thành 1 lượt). |
| **G8 · Q10** | `TC-LSN-615` — *App mobile admin: xem và thao tác booking lesson* · `TC-LSN-522` — *UnivaPay: admin approve ở APP MOBILE* · `TC-LSN-527` — *Khoá thao tác trên APP MOBILE khi đang chờ webhook* | Chạy nguyên trạng trên nhánh fix làm **mốc luồng bình thường**. Phần mới là hành vi khi Google treo / ở biên 5 giây → tách sang `TC-SYNCAPP001-01/02/03`. |

### 5b — TC mới

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-INTGSHEET001-01 | UI | INTG-SHEET-001 | Đặt lịch bài học (レッスン予約) — đồng bộ Google スプレッドシート | Abnormal | auto | Tất cả | Google treo khi đồng bộ đặt lịch bài học — lượt đặt lịch vẫn ghi nhận, màn không đứng chờ | App chạy trên nhánh `ai_fixbug_40835`.<br>Có lesson calendar thử nghiệm 「TC40835-lesson-<thời điểm>」 đã liên kết Google スプレッドシート, token còn hạn, có ít nhất 1 course và khung giờ còn chỗ.<br>Chặn tên miền Google về địa chỉ không định tuyến được trước khi thao tác. | 1. Mở trang đặt lịch bài học của calendar thử nghiệm.<br>2. Chọn course + khung giờ còn chỗ, nhập thông tin tối thiểu, bấm xác nhận đặt lịch, bấm giờ tới lúc trang trả kết quả.<br>3. Mở màn quản lý đặt lịch của calendar, kiểm lượt đặt lịch vừa tạo.<br>4. Mở Google スプレッドシート đích, kiểm có dòng mới không.<br>5. Mở log ứng dụng, tìm dòng lỗi đồng bộ bảng tính.<br>6. Kiểm bảng lưu lỗi đồng bộ Google có phát sinh bản ghi cho lượt này không. | 1 lượt đặt lịch mới trên calendar 「TC40835-lesson-<thời điểm>」 | Trang trả kết quả trong khoảng 5–10 giây, không đứng chờ vô hạn.<br>Lượt đặt lịch được ghi nhận đầy đủ và hiện ở màn quản lý đặt lịch.<br>Bảng tính KHÔNG có dòng mới.<br>Log ghi lỗi hết thời gian chờ kèm định danh lượt đặt lịch.<br>Ghi rõ vào kết quả: có hay không có bản ghi để tự đồng bộ lại — nếu không có thì lượt này mất đồng bộ vĩnh viễn. |  | Lấp G1 · Môi trường: local/dev · Đánh giá spec: Spec ghi rõ (`spec-features/admin/lesson-booking/feature-spec.md`) · Evidence: video bấm giờ + ảnh màn quản lý đặt lịch + ảnh sheet + trích log · regression — dẫn từ `TC-LSN-461` · **T3 hiện đang trắng TC, đây là TC bắt buộc của task** |
| TC-INTGSHEET001-02 | API | INTG-SHEET-001 | Job đồng bộ đặt lịch bài học lên Google スプレッドシート | Boundary | auto | Tất cả | Biên 5 giây cho đồng bộ đặt lịch bài học — 4,5 giây vẫn lên sheet, 5,5 giây bị cắt | App chạy trên nhánh `ai_fixbug_40835`.<br>Máy chủ Google giả chỉnh được độ trễ, chứng chỉ tự ký khớp tên miền Google, trỏ tên miền Google về máy chủ giả.<br>Mỗi lượt đo dùng một lượt đặt lịch riêng trên calendar 「TC40835-lesson-bien-<độ trễ>」. | 1. Đặt máy chủ giả trả lời sau 4,5 giây, tạo 1 lượt đặt lịch, chờ luồng đồng bộ chạy, ghi thời gian.<br>2. Mở bảng tính đích, kiểm dòng đã lên đủ chưa.<br>3. Đặt máy chủ giả trả lời sau 5,5 giây, tạo lượt đặt lịch mới, chờ đồng bộ, ghi thời gian.<br>4. Mở bảng tính và log của lượt thứ hai. | Lượt 1: độ trễ 4,5 giây.<br>Lượt 2: độ trễ 5,5 giây. | Lượt 4,5 giây: đồng bộ thành công, dòng đặt lịch lên bảng tính đầy đủ, log không có lỗi hết thời gian chờ.<br>Lượt 5,5 giây: bị cắt sát mốc 5 giây, log ghi lỗi hết thời gian chờ với mốc 5000 mili giây, bảng tính không có dòng mới, lượt đặt lịch trong hệ thống còn nguyên.<br>Mốc cắt đúng 5 giây, không sớm hơn, không dài hơn. |  | Lấp G1 · Môi trường: local · Đánh giá spec: Spec không ghi — ngưỡng 5 giây lấy từ mục 2 Redmine #40835 · Evidence: bảng 2 số đo + trích log · Không dựng được máy chủ giả TLS khớp tên miền thì trả skip kèm lý do, KHÔNG đọc mã nguồn để kết luận Đạt |
| TC-PERFLARGE001-01 | API | PERF-LARGE-001 | Refill toàn bộ dữ liệu khi sheet bị đổi tên / xóa trên Google | Boundary | manual | product | Refill toàn bộ câu trả lời lên sheet mới — đo xem ngưỡng 5 giây có cắt thao tác hợp lệ không | Bản sửa lỗi đã triển khai lên production với Internet thật và tài khoản Google thật.<br>Chuẩn bị **3 mức dữ liệu**: form A ~50 câu trả lời, form B ~1.000, form C ~5.000 (hoặc lesson calendar tương đương), tất cả đã liên kết Google スプレッドシート.<br>Ghi lại số dòng/cột hiện có của từng sheet đích. | 1. Với form A: mở Google Drive, ĐỔI TÊN sheet của form → cho phát sinh 1 câu trả lời mới → chờ lần sync kế tiếp.<br>2. Mở log, đo thời gian của lần gọi ghi (mốc bắt đầu → mốc kết thúc) và đếm số dòng đã fill lên sheet mới.<br>3. Lặp bước 1–2 cho form B rồi form C.<br>4. Với mức bị cắt (nếu có), kiểm sheet mới: fill dở dang bao nhiêu dòng, có dòng nào ghi trùng/thiếu không.<br>5. Lặp phép đo 3 lần ở các khung giờ khác nhau, ghi cả 9 số đo.<br>6. XÓA hẳn sheet (thay vì đổi tên) và lặp lại ở mức dữ liệu lớn nhất. | 3 mức dữ liệu: ~50 · ~1.000 · ~5.000 bản ghi;<br>2 thao tác: đổi tên sheet · xóa sheet;<br>lặp 3 lần/mức | Kết quả TC là **bảng số đo** kèm mốc khối lượng bắt đầu vượt 5 giây, không chỉ Đạt/Không đạt.<br>Mức dưới 5 giây: sheet mới được tạo và fill lại **TOÀN BỘ** bản ghi cũ + mới, đúng như hành vi trước fix.<br>Mức vượt 5 giây: thao tác hợp lệ bị cắt bằng lỗi hết thời gian chờ; phải ghi rõ sheet mới ở trạng thái nào (trống / fill dở dang / trùng dòng) và có tự chạy lại được không.<br>Bất kỳ trường hợp fill dở dang nào đều phải báo cáo ngay cho PO để quyết định giữ hay nới ngưỡng 5 giây. |  | Lấp G2 + G3 · **RULE-08 → bắt buộc product** (dữ liệu lớn thật + độ trễ Google thật) · Đánh giá spec: Spec ghi rõ hành vi refill (kho `TC-FORM-378`, `TC-LSN-471`) nhưng **không ghi** ngưỡng thời gian → cần PO chốt, xem §6 · Evidence: bảng 9 số đo + ảnh sheet mới + trích log · **Đây là rủi ro hồi quy lớn nhất của ticket, hiện trắng TC** |
| TC-OUTTRUTH001-01 | UI | OUT-TRUTH-001 | Màn tạo biểu mẫu / QR action — thông báo kết quả khi Google treo | Abnormal | auto | Tất cả | Thông báo trên màn hình phải phản ánh đúng việc bảng tính chưa được tạo | App chạy trên nhánh `ai_fixbug_40835`.<br>Đăng nhập trang quản trị, chọn bot đã liên kết Google スプレッドシート (token còn hạn).<br>Chặn tên miền Google về địa chỉ không định tuyến được. | 1. Tạo biểu mẫu mới 「TC40835-truth-form-<thời điểm>」, bấm lưu, **ghi lại nguyên văn thông báo hiển thị**.<br>2. Mở lại biểu mẫu, kiểm màn có dấu hiệu nào cho biết bảng tính chưa được tạo không (icon, cảnh báo, trạng thái liên kết).<br>3. Đối chiếu dữ liệu: biểu mẫu đã có mã bảng tính chưa, trạng thái liên kết Google của bot là gì.<br>4. Lặp bước 1–3 cho QR action 「TC40835-truth-qr-<thời điểm>」.<br>5. Ghi lại: thông báo và trạng thái hiển thị có khớp trạng thái thật không. | 1 biểu mẫu + 1 QR action tạo trong lúc Google bị chặn | Ghi nhận **nguyên văn** thông báo của cả 2 màn và so với trạng thái thật.<br>Nếu màn báo lưu thành công trong khi bảng tính chưa được tạo → ghi **Không đạt** và raise bug kèm ảnh chụp, vì người dùng không có cách nào biết đồng bộ đã hỏng.<br>Màn hình phải có ít nhất một dấu hiệu (cảnh báo / trạng thái liên kết) cho biết bảng tính chưa sẵn sàng. |  | Lấp G5 + Q5 · Môi trường: local/dev · Đánh giá spec: **Spec không ghi** — cần hỏi leader, xem §6 · Evidence: ảnh chụp thông báo 2 màn + ảnh màn sau khi mở lại + trích dữ liệu · `NEW-16` đã ghi nhận false success này ở mức ghi chú; TC này nâng thành quan điểm kiểm riêng |
| TC-STATECLEAN001-01 | UI | STATE-CLEAN-001 | Hủy liên kết Google スプレッドシート sau khi tạo sheet lỗi vì timeout | Abnormal | manual | Tất cả | Sau timeout, bot có thoát được trạng thái lỗi để liên kết lại không | App chạy trên nhánh `ai_fixbug_40835`.<br>Đã chạy xong một lượt làm bản ghi liên kết rơi vào trạng thái lỗi do hết thời gian chờ (chạy `TC-OUTTRUTH001-01` hoặc `NEW-5` trước).<br>Đã bỏ chặn tên miền Google, Google phản hồi bình thường trở lại. | 1. Mở màn liên kết Google スプレッドシート của bot vừa bị timeout, ghi lại trạng thái hiển thị.<br>2. Bấm hủy liên kết Google, ghi lại nguyên văn thông báo.<br>3. Nếu bị chặn: chờ 5 phút rồi thử lại, ghi lại kết quả và tổng thời gian bị kẹt.<br>4. Thử liên kết lại bằng **cùng email** đã dùng trước đó, quan sát kết quả.<br>5. Kiểm các biểu mẫu / QR action khác **cùng bot** còn đồng bộ được không.<br>6. Đối chiếu dữ liệu: trạng thái bản ghi liên kết và trạng thái liên kết Google của bot. | 1 bot đang ở trạng thái lỗi do timeout;<br>cùng email Google đã liên kết trước đó | Phải có **đường thoát**: hoặc hủy liên kết được ngay, hoặc bị chặn kèm thông báo nêu rõ thời gian chờ và sau khoảng thời gian đó thì hủy được.<br>Nếu bot kẹt vĩnh viễn ở trạng thái lỗi, không hủy và không liên kết lại được → ghi **Không đạt** và raise bug: một lần Google chậm sẽ khoá vĩnh viễn tính năng đồng bộ của bot.<br>Ghi rõ các biểu mẫu / QR action khác cùng bot có bị ảnh hưởng theo không. |  | Lấp G5 · Môi trường: dev/staging · Đánh giá spec: Spec có mô tả chặn hủy khi đang tạo/lỗi (kho `TC-FORM-365`) nhưng **không ghi** trường hợp lỗi do timeout → cần chốt, xem §6 · Evidence: ảnh thông báo + mốc thời gian + trích dữ liệu · regression — dẫn từ `TC-FORM-365`, `TC-FORM-364` |
| TC-COMPATLEGACY001-01 | UI | COMPAT-LEGACY-001 | Bot / sheet đã liên kết Google từ trước bản sửa | Normal | manual | Tất cả | Dữ liệu liên kết cũ vẫn đồng bộ bình thường sau khi thêm giới hạn 5 giây | Bản sửa lỗi đã triển khai.<br>Chuẩn bị **3 loại dữ liệu cũ**: (a) bot liên kết Google **trước bản lưu email** (không có email lưu), (b) bot đã liên kết lại bằng **cùng email**, (c) bot đã liên kết lại bằng **email khác**.<br>Mỗi bot có ít nhất 1 biểu mẫu và 1 lesson/salon calendar đang đồng bộ. | 1. Với từng loại (a)(b)(c): mở màn liên kết Google, ghi lại trạng thái và thông tin tài khoản hiển thị.<br>2. Cho phát sinh 1 bản ghi mới (câu trả lời biểu mẫu hoặc lượt đặt lịch), chờ đồng bộ.<br>3. Mở bảng tính đích, kiểm dòng mới đã lên đủ cột và đúng nội dung chưa.<br>4. Kiểm log không có lỗi hết thời gian chờ ở luồng bình thường.<br>5. Ghi lại thời gian của thao tác gọi Google cho từng loại. | 3 bot đại diện 3 loại dữ liệu liên kết cũ | Cả 3 loại đều đồng bộ thành công như trước fix, dòng mới lên bảng tính đủ cột đúng nội dung.<br>Không có lỗi hết thời gian chờ ở luồng bình thường.<br>Không loại dữ liệu cũ nào bị mất liên kết hay bị đẩy sang trạng thái lỗi chỉ vì thêm giới hạn thời gian. |  | Lấp Q2 · Môi trường: staging/product · Đánh giá spec: Spec ghi rõ (kho `TC-FORM-370/371/372`) · Evidence: ảnh màn liên kết 3 bot + ảnh sheet + trích log · regression — dẫn từ `TC-FORM-372`, `TC-FORM-370`, `TC-FORM-371` · **RULE-09: không được đánh × với lý do "dữ liệu mới chạy ổn"** |
| TC-REGRUN001-01 | API | REG-RUN-001 | Deploy bản fix khi hàng đợi còn job đồng bộ Google dở | Abnormal | manual | product | Job đang chạy dở lúc deploy không bị kẹt trạng thái "đang xử lý" | Môi trường có tiến trình xử lý hàng đợi đang bật và có thể deploy được.<br>Dựng sẵn hàng đợi có ít nhất 10 job đồng bộ Google スプレッドシート đang chờ / đang chạy (biểu mẫu + đặt lịch salon).<br>Ghi lại trạng thái từng bản ghi liên quan trước khi deploy. | 1. Cho hàng đợi bắt đầu chạy, xác nhận có bản ghi ở trạng thái đang xử lý.<br>2. Trong lúc đó, deploy nhánh `ai_fixbug_40835` theo đúng quy trình release thật (không bật maintain).<br>3. Theo dõi tiến trình xử lý hàng đợi được khởi động lại, ghi thời gian.<br>4. Sau khi hàng đợi chạy hết, đối chiếu **từng** bản ghi: đã hoàn tất, đang chờ thử lại, hay kẹt ở trạng thái đang xử lý.<br>5. Kiểm bảng tính đích: có dòng nào ghi trùng do job chạy lại không.<br>6. Mở log quanh mốc deploy, tìm lỗi bất thường. | 10+ job đồng bộ Google đang chờ/đang chạy tại thời điểm deploy | Không bản ghi nào kẹt vĩnh viễn ở trạng thái đang xử lý — mỗi bản ghi kết thúc ở hoàn tất, chờ thử lại, hoặc lỗi có thể xử lý lại.<br>Bảng tính không có dòng ghi trùng do job bị chạy lại sau deploy.<br>Job chạy bằng client cũ (không timeout) và job chạy bằng client mới (timeout 5 giây) đều kết thúc, không giữ tiến trình. |  | Lấp Q3 · **RULE-08 → product** (job nền + quy trình deploy thật) · Đánh giá spec: **Spec không ghi** hành vi job dở khi deploy → cần hỏi leader · Evidence: bảng đối chiếu trạng thái trước/sau + ảnh sheet + trích log quanh mốc deploy |
| TC-ENV003-01 | UI | ENV-003 | Smoke 4 luồng Google スプレッドシート trên production | Normal | manual | product | 4 tính năng đồng bộ Google vẫn chạy đúng trên production sau khi lên bản fix | Bản fix đã lên production.<br>Có bot thật (hoặc bot nội bộ trên production) đã liên kết Google スプレッドシート cho: biểu mẫu, QR action, lesson calendar, salon calendar.<br>Ghi lại số dòng hiện có của từng bảng tính đích trước khi test. | 1. Biểu mẫu: cho 1 người dùng LINE thật trả lời biểu mẫu → chờ đồng bộ → mở bảng tính kiểm dòng mới.<br>2. QR action: cho phát sinh lượt quét/truy cập → chờ job đồng bộ thống kê → kiểm bảng tính.<br>3. Lesson calendar: tạo 1 lượt đặt lịch bài học → kiểm bảng tính.<br>4. Salon calendar: tạo 1 lượt đặt lịch salon → kiểm bảng tính.<br>5. Với mỗi luồng, đo thời gian từ lúc phát sinh tới lúc dòng lên bảng tính.<br>6. Mở log production, đếm số lỗi hết thời gian chờ phát sinh trong 24 giờ sau khi lên bản fix. | 1 bản ghi mới cho mỗi luồng trong 4 luồng;<br>cửa sổ quan sát log 24 giờ | Cả 4 luồng đều có dòng mới lên bảng tính đúng nội dung, đủ cột.<br>Không luồng nào phát sinh lỗi hết thời gian chờ ở điều kiện mạng bình thường.<br>Số lỗi hết thời gian chờ trong 24 giờ đầu được ghi lại thành con số cụ thể — khác 0 thì phải liệt kê bản ghi nào bị ảnh hưởng và báo PO (đây chính là cảnh báo "theo dõi log vài ngày sau khi lên" mà Dev nêu). |  | Lấp Q1 · **RULE-08 / ENV-003 → bắt buộc product**, không kết luận từ staging · Đánh giá spec: Spec ghi rõ · Evidence: ảnh 4 bảng tính + bảng 4 số đo + thống kê log 24 giờ · Chạy sau khi release, trước khi đóng ticket |
| TC-LIFFENTRY001-01 | UI | LIFF-ENTRY-001 | Trang đặt lịch salon / biểu mẫu mở từ trong ứng dụng LINE khi Google treo | Abnormal | manual | product | Khách cuối trong ứng dụng LINE không bị treo màn khi Google không phản hồi | Bản fix đã lên môi trường có domain thật truy cập được từ ứng dụng LINE.<br>Có salon calendar và biểu mẫu đã liên kết Google スプレッドシート, đã cấu hình khung giờ còn chỗ.<br>Có cách làm Google không phản hồi ở môi trường đó (phối hợp hạ tầng), hoặc chọn thời điểm Google thật đang chậm đã ghi nhận. | 1. Mở trang đặt lịch salon **từ trong ứng dụng LINE** trên điện thoại thật (không phải trình duyệt máy tính).<br>2. Chọn khung giờ, nhập thông tin, bấm xác nhận, bấm giờ tới lúc trang trả kết quả, **quay màn hình**.<br>3. Quan sát: có chỉ báo đang xử lý không, có trắng màn không, có văng ra khỏi in-app browser không.<br>4. Kiểm lượt đặt lịch đã vào màn quản lý chưa.<br>5. Lặp bước 1–4 với biểu mẫu mở từ trong ứng dụng LINE.<br>6. Kiểm tin nhắn LINE mà khách nhận được sau khi đặt lịch có đúng không. | 1 lượt đặt lịch salon + 1 lượt trả lời biểu mẫu, cả hai thực hiện trong ứng dụng LINE trên điện thoại thật | Trang trả kết quả trong khoảng 5–10 giây, không trắng màn, không văng khỏi in-app browser.<br>Lượt đặt lịch / câu trả lời được ghi nhận đầy đủ trong hệ thống.<br>Tin nhắn LINE gửi cho khách vẫn đúng nội dung như luồng bình thường.<br>Khách không nhìn thấy lỗi hệ thống hay trang lỗi kỹ thuật. |  | Lấp G6 + Q9 · **RULE-06 → phải verify trên thiết bị thật trong ứng dụng LINE**, không thay bằng trình duyệt máy tính · **RULE-08 → product** (domain thật) · Đánh giá spec: Spec ghi rõ · Evidence: video quay màn điện thoại + ảnh màn quản lý đặt lịch + ảnh tin nhắn LINE · `NEW-20` cho phép skip case này — TC này đóng lại đường skip |
| TC-REGSHARED001-01 | API | REG-SHARED-001 | Client dùng chung phục vụ nhiều bot / nhiều tài khoản Google trong cùng tiến trình | Normal | auto | Tất cả | Hai bot với hai tài khoản Google khác nhau không ghi lẫn dữ liệu sang nhau | App chạy trên nhánh `ai_fixbug_40835`, tiến trình xử lý hàng đợi đang bật và **không khởi động lại giữa chừng**.<br>Bot A liên kết tài khoản Google GA, bot B liên kết tài khoản Google GB (hai tài khoản khác nhau, hai bảng tính đích khác nhau).<br>Mỗi bot có 1 biểu mẫu đang đồng bộ. | 1. Ghi lại số dòng hiện có của bảng tính GA và GB.<br>2. Cho phát sinh liên tiếp trong cùng một lượt chạy hàng đợi: 1 bản ghi của bot A, 1 của bot B, rồi lại 1 của bot A.<br>3. Chờ hàng đợi xử lý hết mà không khởi động lại tiến trình.<br>4. Mở bảng tính GA: kiểm đủ 2 dòng của bot A và **không có** dòng nào của bot B.<br>5. Mở bảng tính GB: kiểm đủ 1 dòng của bot B và **không có** dòng nào của bot A.<br>6. Lặp lại kịch bản với 1 bot bị chặn mạng Google (timeout) xen giữa, kiểm bot còn lại vẫn ghi đúng bảng tính của mình. | Bot A + tài khoản Google GA;<br>bot B + tài khoản Google GB;<br>thứ tự phát sinh A → B → A | Mỗi dòng vào đúng bảng tính của bot sở hữu, không dòng nào ghi nhầm sang bảng tính của bot kia.<br>Bot A timeout không làm bot B ghi sai hoặc ghi hỏng — bot B vẫn đồng bộ bình thường trong cùng lượt chạy.<br>Token của hai bot không bị dùng lẫn: log không cho thấy bot B gọi bằng thông tin xác thực của bot A. |  | Lấp G7 · Môi trường: local/dev · Đánh giá spec: **Spec không ghi** hành vi dùng chung client giữa nhiều bot → cần hỏi leader · Evidence: ảnh 2 bảng tính + trích log theo bot · `dev_impact` Studio nêu đích danh ảnh hưởng lan toả qua singleton nhưng `NEW-2` chỉ kiểm bằng đọc mã · regression — dẫn từ `TC-FORM-364` |
| TC-SYNCAPP001-01 | API | SYNC-APP-001 | Ứng dụng di động Admin — duyệt / huỷ đặt lịch bài học (`/api/mobile/calendar-lesson/*`) | Abnormal | manual | dev/local | Google treo khi admin thao tác booking **từ app mobile** — API trả kết quả sau khoảng 5 giây, không treo app | App chạy trên nhánh `ai_fixbug_40835`.<br>Có tài khoản admin đăng nhập được **ứng dụng di động Admin** trên điện thoại thật.<br>Lesson calendar 「TC40835-app-<thời điểm>」 đã liên kết Google スプレッドシート (token còn hạn), có sẵn 1 booking ở trạng thái chờ duyệt và 1 booking đã xác nhận.<br>Chặn tên miền Google về địa chỉ không định tuyến được. | 1. Mở ứng dụng di động Admin, vào danh sách đặt lịch của calendar thử nghiệm.<br>2. Bấm **duyệt** booking đang chờ, bấm giờ từ lúc bấm tới lúc app trả kết quả, quay màn hình.<br>3. Quan sát: app có chỉ báo đang xử lý không, có đứng chờ vô hạn không, có văng lỗi mạng không.<br>4. Bấm **huỷ** booking đã xác nhận, lặp phép đo ở bước 2–3.<br>5. Mở màn quản trị **web** của cùng calendar, kiểm trạng thái 2 booking đã đổi đúng chưa.<br>6. Mở Google スプレッドシート đích, kiểm 2 row tương ứng.<br>7. Mở log ứng dụng, tìm dòng lỗi hết thời gian chờ kèm định danh booking. | 1 booking duyệt + 1 booking huỷ, thao tác từ ứng dụng di động Admin | App trả kết quả trong khoảng 5–10 giây cho mỗi thao tác, **không đứng chờ vô hạn**, không văng lỗi kỹ thuật ra màn người dùng.<br>Trạng thái booking trong hệ thống (xem trên web) đã đổi đúng — thao tác nghiệp vụ **không bị mất** vì Google treo.<br>Google スプレッドシート **chưa** cập nhật trạng thái mới; log ghi lỗi hết thời gian chờ kèm định danh booking.<br>Ghi rõ vào kết quả: có bản ghi để tự đồng bộ lại không — nếu không thì row trên sheet lệch trạng thái vĩnh viễn. |  | Lấp G8 + Q10 · Môi trường: dev/staging · Đánh giá spec: Spec ghi rõ (`spec-features/admin/lesson-booking/feature-spec.md:78, :461, :573`) · Evidence: video quay màn điện thoại có bấm giờ + ảnh màn web + ảnh sheet + trích log · **Đường `Mobile\CalendarController` gọi Google Sheet ĐỒNG BỘ trong request (RA-09/RP-13) → đây là nơi timeout tác động trực tiếp nhất lên trải nghiệm admin** · regression — dẫn từ `TC-LSN-615`, `TC-LSN-522` |
| TC-SYNCAPP001-02 | API | SYNC-APP-001 | Ứng dụng di động Admin ⇔ màn quản trị web — cùng một booking trên Google スプレッドシート | Normal | manual | Tất cả | Thao tác từ app và từ web cập nhật đúng **cùng một row**, không sinh row trùng | App chạy trên nhánh `ai_fixbug_40835`, Google phản hồi bình thường dưới 5 giây.<br>Lesson calendar đã liên kết Google スプレッドシート.<br>Đăng nhập song song: ứng dụng di động Admin trên điện thoại **và** màn quản trị web trên máy tính, cùng một bot.<br>Ghi lại số row hiện có của sheet đích. | 1. Tạo booking B1, **duyệt từ app mobile** → mở sheet, ghi lại row và trạng thái.<br>2. Ngay sau đó **huỷ B1 từ màn web** → mở sheet, kiểm đúng row đó đổi trạng thái.<br>3. Tạo booking B2, **duyệt từ web** → rồi **huỷ từ app mobile** → kiểm sheet.<br>4. Tạo booking B3, thực hiện **hoàn tiền từ app mobile** → kiểm cột trạng thái thanh toán trên sheet.<br>5. Đếm số row theo từng booking ID và đối chiếu trạng thái cuối giữa app, web và sheet. | 3 booking B1/B2/B3;<br>đan xen thao tác app ⇔ web;<br>1 lượt hoàn tiền từ app | Mỗi booking chỉ có **đúng một row logic** trên sheet dù thao tác đến từ app hay web — không sinh row trùng.<br>Trạng thái cuối khớp nhau ở cả 3 nơi: ứng dụng di động, màn quản trị web, Google スプレッドシート.<br>Hoàn tiền từ app cập nhật đúng cột trạng thái thanh toán trên sheet (`updateStatusPaymentBooking()`).<br>Không thao tác nào bị mất hoặc ghi đè nhầm booking khác. |  | Lấp G8 + Q10 · Môi trường: dev/staging · Đánh giá spec: Spec ghi rõ (`:672`, `:1716`) · Evidence: screenshot **cùng thao tác trên cả web và app** (đúng yêu cầu Evidence của `SYNC-APP-001`) + ảnh sheet từng bước · regression — dẫn từ `TC-LSN-615` |
| TC-SYNCAPP001-03 | API | SYNC-APP-001 | Ứng dụng di động Admin — biên 5 giây của API `/api/mobile/calendar-lesson/*` | Boundary | auto | dev/local | API mobile xử lý đúng hai phía biên — 4,5 giây vẫn lên sheet, 5,5 giây bị cắt nhưng booking vẫn đổi trạng thái | App chạy trên nhánh `ai_fixbug_40835`.<br>Máy chủ Google giả chỉnh được độ trễ, chứng chỉ tự ký khớp tên miền Google, trỏ tên miền Google về máy chủ giả.<br>Lesson calendar đã liên kết Google スプレッドシート, mỗi lượt đo dựng 1 booking chờ duyệt riêng. | 1. Đặt máy chủ giả trả lời sau 4,5 giây; gọi endpoint duyệt booking từ ứng dụng di động Admin; đo thời gian API trả về.<br>2. Kiểm sheet: row đã cập nhật trạng thái chưa; kiểm log không có lỗi hết thời gian chờ.<br>3. Đặt máy chủ giả trả lời sau 5,5 giây; dựng booking mới; lặp bước 1–2.<br>4. Với lượt 5,5 giây, kiểm thêm: trạng thái booking trong hệ thống có đổi không, app hiển thị gì cho admin.<br>5. Ghi lại 2 số đo và nguyên văn phản hồi app ở cả 2 lượt. | Lượt 1: độ trễ 4,5 giây.<br>Lượt 2: độ trễ 5,5 giây. | Lượt 4,5 giây: API trả về bình thường, row trên sheet cập nhật đúng trạng thái, log không có lỗi hết thời gian chờ.<br>Lượt 5,5 giây: API trả về trong khoảng 5–10 giây (bị cắt sát mốc 5 giây), **trạng thái booking trong hệ thống vẫn đổi đúng**, sheet chưa cập nhật, log ghi lỗi hết thời gian chờ.<br>App phải hiển thị kết quả nghiệp vụ đúng cho admin, không hiện lỗi kỹ thuật và không rollback thao tác duyệt.<br>Mốc cắt đúng 5 giây, không sớm hơn. |  | Lấp Q10 (RULE-01 — `SYNC-APP-001` nâng Cao, đủ Normal + Abnormal + Boundary cùng `TC-SYNCAPP001-01/02`) · Môi trường: local · Đánh giá spec: Spec không ghi ngưỡng cho API mobile — suy từ mục 2 Redmine #40835 · Evidence: bảng 2 số đo + ảnh phản hồi app + ảnh sheet + trích log · Không dựng được máy chủ giả TLS khớp tên miền thì trả skip kèm lý do |
| TC-SYNCAPP001-04 | UI | SYNC-APP-001 | Ứng dụng di động Admin — duyệt / từ chối yêu cầu đặt chỗ (T-09, T-10) | Normal | manual | Tất cả | Duyệt và từ chối yêu cầu đặt chỗ từ app mobile — Google スプレッドシート cập nhật đúng row | Bản fix đã triển khai, Google phản hồi bình thường dưới 5 giây.<br>Đăng nhập được **ứng dụng di động Admin** trên điện thoại thật.<br>Lesson calendar 「TC40835-app-<thời điểm>」 đã liên kết Google スプレッドシート, khoá học để chế độ **承認制** (`approve_type = 2`).<br>Chuẩn bị 2 yêu cầu đặt chỗ đang chờ duyệt: B1 và B2, mỗi booking có mã dễ truy vết.<br>Ghi lại số row hiện có của sheet đích. | 1. Mở ứng dụng di động Admin → danh sách đặt chỗ → tab yêu cầu mới.<br>2. Mở B1, bấm **duyệt** (承認). Chờ app báo xong.<br>3. Mở Google スプレッドシート, tìm row của B1: kiểm cột trạng thái đã đổi sang 「予約確定」 chưa, các cột khác (ngày giờ, khoá học, khách) có đúng không.<br>4. Mở B2, bấm **từ chối** (否認). Chờ app báo xong.<br>5. Mở sheet, tìm row của B2: kiểm cột trạng thái đã đổi sang 「否認済」 chưa.<br>6. Đếm số row theo từng booking ID — xác nhận mỗi booking vẫn chỉ có **1 row**.<br>7. Mở màn quản trị **web** của cùng calendar, đối chiếu trạng thái 2 booking với app và với sheet. | B1: yêu cầu đặt chỗ chờ duyệt → duyệt.<br>B2: yêu cầu đặt chỗ chờ duyệt → từ chối. | Sheet cập nhật **đúng row** của từng booking, không sinh row mới, không đụng booking khác.<br>B1: cột trạng thái trên sheet = 「予約確定」 (`status = 1`).<br>B2: cột trạng thái trên sheet = 「否認済」 (`status = 6`).<br>Các cột dữ liệu còn lại của row (ngày giờ, khoá học, thông tin khách) giữ nguyên, không bị ghi đè sai.<br>Trạng thái khớp nhau ở cả 3 nơi: app · màn quản trị web · Google スプレッドシート. |  | Lấp G8 + Q10 · Môi trường: dev/staging · Đánh giá spec: Spec ghi rõ — `feature-spec.md` §6.3 T-09 (`0 → 1`, `approveBooking`) và T-10 (`0 → 6`, `denyBooking`), endpoint `EP-M01 /change-status-booking` (`:1389`) · Evidence: screenshot **cùng thao tác trên cả app và web** + ảnh sheet trước/sau mỗi bước · ⚠️ T-09 còn **trừ tiền** khi `payment_status = 0` (BR-24) — ghi lại trạng thái thanh toán trước/sau |
| TC-SYNCAPP001-05 | UI | SYNC-APP-001 | Ứng dụng di động Admin — yêu cầu huỷ & huỷ thẳng booking đã xác nhận (T-14, T-15) | Normal | manual | Tất cả | Yêu cầu huỷ và huỷ thẳng booking từ app mobile — sheet đổi đúng trạng thái, số chỗ tính lại đúng | Như `TC-SYNCAPP001-04`.<br>Chuẩn bị 2 booking **đã xác nhận**: B3 (`status = 1`, khách tự đặt đã duyệt) và B4 (`status = 2`, admin đặt hộ) — để kiểm cả hai trạng thái nguồn của guard.<br>Ghi lại row và nhãn trạng thái hiện tại của B3, B4 trên sheet. | 1. Trên app, mở B3 → bấm **yêu cầu huỷ** (キャンセルリクエスト).<br>2. Mở sheet, kiểm row B3: trạng thái đổi sang 「リクエスト」 (`status = 5`) chưa.<br>3. Trên app, mở B4 → bấm **huỷ** (手動予約キャンセル).<br>4. Mở sheet, kiểm row B4: trạng thái đổi sang 「キャンセル」 (`status = 7`) chưa.<br>5. Kiểm màn quản lý khung giờ: số chỗ còn lại đã tính lại đúng chưa (B3 vẫn **chiếm chỗ** ở `status = 5`, B4 **nhả chỗ** ở `status = 7`).<br>6. Đếm row theo booking ID, đối chiếu app ⇔ web ⇔ sheet. | B3: `status = 1` → yêu cầu huỷ.<br>B4: `status = 2` → huỷ thẳng. | Sheet cập nhật đúng row từng booking, không sinh row trùng.<br>B3 → 「リクエスト」 · B4 → 「キャンセル」.<br>Số chỗ còn lại của khung giờ tính lại đúng: `status = 5` **vẫn chiếm chỗ**, `status = 7` **không chiếm chỗ**.<br>Cả hai trạng thái nguồn (`1` và `2`) đều đi qua guard thành công — không trạng thái nào bị bỏ qua im lặng.<br>Trạng thái khớp ở app · web · sheet. |  | Lấp G8 + Q10 · Môi trường: dev/staging · Đánh giá spec: Spec ghi rõ — T-14 (`1/2 → 5`, `requestCancel`), T-15 (`1/2 → 7`, `adminCancel`); nhóm chiếm chỗ `{1,2,5}` ở §6.2 · Evidence: screenshot app + web + ảnh sheet + ảnh màn khung giờ · ⚠️ Nhãn `1` và `2` **cùng hiển thị** 「予約確定」 → dùng booking ID để phân biệt, không dựa vào nhãn |
| TC-SYNCAPP001-06 | UI | SYNC-APP-001 | Ứng dụng di động Admin — duyệt / từ chối yêu cầu huỷ (T-16, T-17) | Normal | manual | Tất cả | Duyệt và từ chối yêu cầu huỷ từ app mobile — sheet đúng trạng thái, 空き枠通知 gửi đúng | Như `TC-SYNCAPP001-04`.<br>Chuẩn bị 2 booking đang ở trạng thái **khách xin huỷ chờ duyệt** (`status = 5`): B5 và B6.<br>Khoá học có cấu hình 空き枠通知 (thông báo khi có chỗ trống) và có ít nhất 1 người đăng ký 「キャンセル待ち」 ở cùng khung giờ với B5. | 1. Trên app, mở B5 → bấm **duyệt yêu cầu huỷ** (キャンセルリクエスト承認).<br>2. Mở sheet, kiểm row B5: trạng thái đổi sang 「キャンセル」 (`status = 4`) chưa.<br>3. Kiểm người đăng ký 「キャンセル待ち」 có nhận được 空き枠通知 trên LINE không.<br>4. Trên app, mở B6 → bấm **từ chối yêu cầu huỷ** (キャンセルリクエスト否認).<br>5. Mở sheet, kiểm row B6: trạng thái **quay lại** 「予約確定」 (`status = 1`) chưa.<br>6. Kiểm số chỗ còn lại của khung giờ sau cả 2 thao tác.<br>7. Đối chiếu app ⇔ web ⇔ sheet. | B5: `status = 5` → duyệt huỷ.<br>B6: `status = 5` → từ chối huỷ. | Sheet cập nhật đúng row từng booking.<br>B5 → 「キャンセル」 (`status = 4`), nhả chỗ; B6 → **quay lại** 「予約確定」 (`status = 1`), vẫn chiếm chỗ.<br>Người đăng ký 「キャンセル待ち」 nhận được 空き枠通知 trên LINE sau khi B5 được duyệt huỷ.<br>Không booking nào bị kẹt ở trạng thái trung gian, không row nào bị ghi đè nhầm. |  | Lấp G8 + Q10 · Môi trường: dev/staging · Đánh giá spec: Spec ghi rõ — T-16 (`5 → 4`, `approveCancel`, + DELETE `event_step_time` + gửi 空き枠通知), T-17 (`5 → 1`, `denyCancel`) · **RULE-06** — phải verify 空き枠通知 **trên LINE thật**, không dừng ở màn admin · Evidence: screenshot app + ảnh sheet + ảnh tin nhắn LINE |
| TC-SYNCAPP001-07 | UI | SYNC-APP-001 | Ứng dụng di động Admin — đặt chỗ hộ khách (T-04 / `EP-M15`) | Normal | manual | Tất cả | Đặt chỗ hộ khách từ app mobile — sheet thêm đúng 1 row mới, đủ cột đúng nội dung | Như `TC-SYNCAPP001-04`.<br>Có khung giờ còn chỗ và một người dùng LINE thử nghiệm là bạn bè của bot.<br>Ghi lại **chính xác** số row hiện có của sheet đích trước khi thao tác. | 1. Trên app, vào lịch tháng/ngày → chọn khung giờ còn chỗ → bấm **thêm đặt chỗ** (手動予約追加).<br>2. Chọn khách là người dùng LINE thử nghiệm, nhập thông tin お客様情報 bắt buộc, xác nhận.<br>3. Mở sheet: kiểm có **đúng 1 row mới** được thêm.<br>4. Đối chiếu **từng cột** của row mới với dữ liệu đã nhập: booking ID, ngày giờ, khoá học, tên khách, các cột friend info được mapping.<br>5. Kiểm cột trạng thái = 「予約確定」 (`status = 2`).<br>6. Lặp bước 1–5 thêm 1 lần nữa cho **cùng khung giờ** để xác nhận không ghi đè row trước.<br>7. Đối chiếu app ⇔ web ⇔ sheet. | 2 lượt đặt chỗ hộ khách trên cùng 1 khung giờ, khách là người dùng LINE thử nghiệm | Mỗi lượt sinh **đúng 1 row mới** trên sheet — tổng cộng tăng đúng 2 row, không ghi đè lẫn nhau.<br>Toàn bộ cột của row mới khớp dữ liệu đã nhập trên app, kể cả các cột friend info được mapping.<br>Cột trạng thái = 「予約確定」 (`status = 2`).<br>Số chỗ còn lại của khung giờ giảm đúng 2. |  | Lấp G8 + Q10 · Môi trường: dev/staging · Đánh giá spec: Spec ghi rõ — T-04 (`→ 2`, 手動予約追加, `EP-M15 /add-booking`), luồng ghi dùng `insertDataToGoogleSheet()` (`:415`) · Evidence: screenshot app + ảnh sheet trước/sau (thấy rõ số row) · ⚠️ Spec ghi `EP-M15` **không kiểm sức chứa** (S-20) → nếu đặt vượt sức chứa được thì ghi nhận riêng, **không** gán cho ticket này |
| TC-SYNCAPP001-08 | UI | SYNC-APP-001 | Ứng dụng di động Admin — hoàn tiền (`EP-M08`) | Normal | manual | product | Hoàn tiền từ app mobile — sheet cập nhật đúng cột trạng thái thanh toán của đúng booking | Bản fix đã triển khai lên môi trường có cổng thanh toán chạy được (product hoặc sandbox của product).<br>Có booking B7 **đã thanh toán thành công** (`payment_status` đã thanh toán) trên lesson calendar đã liên kết Google スプレッドシート.<br>Ghi lại row của B7 trên sheet và giá trị cột trạng thái thanh toán hiện tại. | 1. Trên app, mở chi tiết booking B7 → vào lịch sử hoá đơn.<br>2. Bấm **hoàn tiền** (返金), xác nhận.<br>3. Chờ app báo xong, ghi lại thời gian từ lúc bấm tới lúc app trả kết quả.<br>4. Mở sheet, kiểm **đúng row B7**: cột trạng thái thanh toán đã đổi sang trạng thái đã hoàn tiền chưa.<br>5. Kiểm cột trạng thái **booking** của B7 có bị đổi ngoài ý muốn không.<br>6. Đối chiếu với màn quản trị web và với lịch sử giao dịch trên cổng thanh toán.<br>7. Đếm row theo booking ID — xác nhận không sinh row mới. | 1 booking đã thanh toán → hoàn tiền từ app mobile | Sheet cập nhật **đúng row B7**, cột trạng thái thanh toán phản ánh đúng việc đã hoàn tiền, không sinh row mới.<br>Cột trạng thái booking **không** bị đổi ngoài ý muốn bởi thao tác hoàn tiền.<br>Giá trị trên sheet khớp với màn quản trị web và với lịch sử giao dịch ở cổng thanh toán.<br>Thao tác hoàn tiền hoàn tất, không bị treo. |  | Lấp G8 + Q10 · **RULE-08 → bắt buộc product** (bill tiền) · Đánh giá spec: Spec ghi rõ — `EP-M08 /order-refund` (`:1391`), đồng bộ qua `CalendarGoogleSheetService::updateStatusPaymentBooking()` (`:672`) · Evidence: screenshot app + ảnh sheet trước/sau + ảnh lịch sử giao dịch cổng thanh toán · ⚠️ Spec ghi `EP-M08` **không kiểm `payment_status` hiện tại** (S-21) — nếu hoàn tiền được 2 lần thì ghi nhận riêng, **không** gán cho ticket này |
| TC-SYNCAPP001-09 | UI | SYNC-APP-001 | Ứng dụng di động Admin — thao tác không khớp guard trạng thái (BR-22 / BR-P18) | Abnormal | manual | Tất cả | Thao tác không khớp guard từ app mobile — sheet KHÔNG bị ghi đè dù API vẫn báo thành công | Như `TC-SYNCAPP001-04`.<br>Chuẩn bị booking B8 ở trạng thái **đã huỷ** (`status = 4` hoặc `7`) và booking B9 ở trạng thái **đã từ chối** (`status = 6`) — đều là trạng thái **không khớp** bất kỳ cặp guard nào của app.<br>Ghi lại nguyên trạng row của B8, B9 trên sheet (chụp ảnh từng cột). | 1. Trên app, mở B8 (đã huỷ) và thử bấm một hành động đổi trạng thái bất kỳ còn hiển thị (duyệt / yêu cầu huỷ).<br>2. Ghi lại **nguyên văn** phản hồi app: báo thành công hay báo lỗi.<br>3. Mở sheet, so **từng cột** của row B8 với ảnh chụp trước đó.<br>4. Lặp bước 1–3 cho B9 (đã từ chối).<br>5. Mở màn quản trị web, kiểm trạng thái B8, B9 có bị đổi không.<br>6. Mở log ứng dụng, tìm dấu vết lần gọi và lần ghi Google Sheet tương ứng. | B8: `status = 4`/`7` (đã huỷ).<br>B9: `status = 6` (đã từ chối).<br>Mỗi booking thử 1 hành động không hợp lệ. | **Trạng thái booking trong hệ thống KHÔNG đổi** — đúng thiết kế guard.<br>**Row trên sheet giữ nguyên toàn bộ cột**, không bị ghi đè, không bị xoá nhãn trạng thái, không sinh row mới.<br>Ghi lại chính xác app hiển thị gì: spec ghi rõ cặp không khớp guard thì **không nhánh nào chạy nhưng `updateStatusBooking()` VẪN chạy và API vẫn trả thành công** (BR-22 / BR-P18) — nếu app báo thành công trong khi không có gì đổi thì ghi nhận là **báo thành công sai sự thật** và raise riêng.<br>Nếu row trên sheet bị ghi đè thành giá trị rỗng hoặc sai nhãn → **Không đạt**, raise bug ngay. |  | Lấp G8 + Q10 · Môi trường: dev/staging · Đánh giá spec: Spec ghi rõ — BR-22 / BR-P18 (`:1038`, `:1188`): cặp không khớp guard ⇒ im lặng nhưng `updateStatusBooking()` vẫn chạy, API vẫn `success: true` · Evidence: ảnh chụp từng cột row trước/sau + nguyên văn phản hồi app + trích log · **Đây là đường ghi Google Sheet chạy cả khi nghiệp vụ không đổi — đúng vùng bản fix chạm tới** |
| TC-SYNCAPP001-10 | UI | SYNC-APP-001 | Ứng dụng di động Admin — phân biệt nhãn trạng thái gộp trên Google スプレッドシート | Boundary | manual | Tất cả | Nhãn trạng thái trên sheet sau thao tác từ app mobile — phân biệt được 3 cặp nhãn gộp | Như `TC-SYNCAPP001-04`.<br>Chuẩn bị 6 booking phủ đủ 6 trạng thái có nhãn gộp: B10 (`status = 1`), B11 (`status = 2`), B12 (`status = 4`), B13 (`status = 7`), B14 (`status = 0`), B15 (`status = 5`) — đưa về đúng trạng thái **bằng thao tác từ app mobile**.<br>Có ít nhất 1 đăng ký 「キャンセル待ち」 (`status = 3`) ở cùng calendar. | 1. Dùng app mobile đưa 6 booking về đúng 6 trạng thái nêu trên.<br>2. Mở sheet, ghi lại giá trị **nguyên văn** của cột trạng thái cho từng booking.<br>3. Đối chiếu từng cặp nhãn gộp: `1` vs `2` (cùng 「予約確定」), `4` vs `7` (cùng 「キャンセル」), `0` vs `5` (cùng 「リクエスト」) — ghi rõ sheet có phân biệt được không.<br>4. Tìm row của đăng ký 「キャンセル待ち」 (`status = 3`) trên sheet.<br>5. Đối chiếu nhãn trên sheet với nhãn hiển thị trên app và trên màn quản trị web. | 6 booking phủ 6 trạng thái có nhãn gộp + 1 đăng ký 「キャンセル待ち」 | Mỗi booking có đúng 1 row, nhãn trạng thái trên sheet **khớp với nhãn app hiển thị**.<br>Ghi rõ vào kết quả 3 cặp nhãn gộp có phân biệt được trên sheet hay không — đây là dữ liệu cho Leader, không phải Đạt/Không đạt.<br>Với `status = 3` (「キャンセル待ち」): spec ghi Google Sheet **không đồng bộ** cho đăng ký này (BR-P35) và **ghi ô rỗng** cho `status = 3` (RA-12) — xác nhận thực tế đúng như vậy; nếu khác thì ghi nhận riêng.<br>Không trạng thái nào làm row bị mất hoặc bị nhân đôi. |  | Lấp Q10 (RULE-01 — nhánh `Boundary` của `SYNC-APP-001`) · Môi trường: dev/staging · Đánh giá spec: Spec ghi rõ — §6.1 bảng 8 trạng thái + cảnh báo nhãn gộp; BR-P35 (`:1252`); RA-12 (`:1612`) · Evidence: bảng đối chiếu 7 dòng (trạng thái · nhãn app · nhãn web · giá trị ô sheet) + ảnh sheet · **Hiện trạng nhãn gộp là vấn đề sẵn có, TC này chỉ chốt số liệu — KHÔNG gán cho bản fix** |
| TC-UI003-01 | UI | UI-003 | Màn tạo biểu mẫu / QR action — biên 5 giây ở tầng giao diện | Boundary | auto | Tất cả | Màn hình xử lý đúng ở hai phía biên 5 giây — 4,5 giây thành công, 5,5 giây báo lỗi | App chạy trên nhánh `ai_fixbug_40835`.<br>Máy chủ Google giả chỉnh được độ trễ, chứng chỉ tự ký khớp tên miền Google.<br>Đăng nhập trang quản trị, bot thử nghiệm đã liên kết Google スプレッドシート, token còn hạn. | 1. Đặt máy chủ giả trả lời sau 4,5 giây, tạo biểu mẫu mới, bấm lưu, bấm giờ tới lúc màn trả kết quả.<br>2. Kiểm biểu mẫu đã có mã bảng tính chưa và trạng thái liên kết Google của bot.<br>3. Đặt máy chủ giả trả lời sau 5,5 giây, tạo biểu mẫu mới, lặp bước 1–2.<br>4. Lặp toàn bộ bước 1–3 cho màn QR action.<br>5. Ghi lại nguyên văn thông báo hiển thị của cả 4 lượt. | Lượt 1: độ trễ 4,5 giây.<br>Lượt 2: độ trễ 5,5 giây.<br>Áp cho cả màn biểu mẫu và màn QR action. | Lượt 4,5 giây: màn trả kết quả bình thường, đối tượng được gán mã bảng tính, trạng thái liên kết Google của bot **không** bị hạ, không có lỗi trong log.<br>Lượt 5,5 giây: màn trả kết quả trong khoảng 5–10 giây, đối tượng vẫn được lưu nhưng chưa có mã bảng tính, log ghi lỗi hết thời gian chờ.<br>Chênh lệch hành vi giữa hai lượt phải đúng ở mốc 5 giây, không sớm hơn. |  | Lấp Q7 (RULE-01 — `UI-003` nâng Cao, bổ sung nhánh `Boundary`) · Môi trường: local · Đánh giá spec: Spec không ghi ngưỡng ở tầng giao diện — suy từ mục 2 Redmine #40835 · Evidence: video bấm giờ 4 lượt + ảnh thông báo + trích dữ liệu · Nhánh `Normal` của `UI-003` dùng lại `NEW-15` (đề nghị đổi mã `NEW-15` sang `UI-003` hoặc bổ sung TC Normal riêng) |

---

## 6. Spec update needed

| # | Vấn đề | Nguồn | Cần ai chốt |
|---|---|---|---|
| 1 | **Đánh giá ảnh hưởng 4.3 T1 của Dev mô tả sai thực tế.** Redmine ghi "Form Builder — đồng bộ câu trả lời biểu mẫu sang Google Sheet nay dừng sau 5 giây thay vì treo", nhưng `NEW-23` đã kiểm chứng luồng PHP là **mã không còn chạy** (`FormAnswerService:1659` đã bị chú thích) và việc đồng bộ do **job Java** thực hiện — job Java **không nằm trong phạm vi fix** nên **vẫn chưa có giới hạn thời gian**. Tức tính năng người dùng quan tâm nhất vẫn có thể treo. | `NEW-23` · Redmine #40835 mục 4.3 · Journal #135755 | **Dev** — sửa lại đánh giá ảnh hưởng; **PO** — quyết có cần ticket riêng cho job Java không |
| 2 | **Hành vi trạng thái sau timeout chưa có spec.** Hai đường xử lý khác nhau: job → `FormAnswerConnectGoogle.status = 3 (lỗi)`; màn UI → `bots.google_sheet_status = 0 (tắt liên kết)`. Kết hợp kho `TC-FORM-365` ("đang tạo **và lỗi** thì bị chặn hủy liên kết"), một lần Google chậm có thể đẩy bot vào trạng thái **không hủy liên kết được**, và `google_sheet_status = 0` còn ảnh hưởng các biểu mẫu khác cùng bot. Không tài liệu nào định nghĩa hành vi mong muốn. | `NEW-5`, `NEW-16` · kho `TC-FORM-365`, `TC-FORM-364` · `TC-STATECLEAN001-01` | **Dev + PO** |
| 3 | **`NEW-17` chưa có expected cố định** — màn nhận callback OAuth chỉ bắt `\Google_Exception`, không bắt ngoại lệ tầng HTTP, nên chưa rõ khi timeout thì hiện thông báo lỗi hay trang lỗi 500. TC đang để tester tự ghi nhận rồi leader quyết. | `NEW-17` · §4 issue #8 | **Dev** — xác nhận nhánh bắt lỗi; **Leader** — chốt expected trước khi chạy |
| 4 | **Ngưỡng 5 giây có áp cho thao tác refill / ghi hàng loạt không.** Kho `TC-FORM-378` và `TC-LSN-471` quy định khi sheet bị đổi tên/xóa thì hệ thống **fill lại TOÀN BỘ** bản ghi — thao tác này với dữ liệu lớn gần như chắc chắn vượt 5 giây và nay sẽ bị cắt. Ticket không nêu ngoại lệ cho luồng ghi hàng loạt. | kho `TC-FORM-378`, `TC-LSN-471` · `NEW-21` · `TC-PERFLARGE001-01` | **PO** — giữ 5 giây cho mọi request, hay nới riêng cho luồng ghi hàng loạt |
| 5 | **Không có cơ chế thử lại cho lần đồng bộ đầu tiên.** `NEW-11` và `NEW-20` đều xác nhận: timeout không phải lỗi JSON của Google nên **không sinh bản ghi thử lại**, chỉ ghi log → bản ghi đó **mất đồng bộ vĩnh viễn**. Dev ghi rõ "ticket không yêu cầu nên không thêm". | `NEW-11`, `NEW-20` · Journal #135755 mục tự review | **PO** — quyết có mở ticket bổ sung cơ chế thử lại không |
| 6 | **Contract đồng bộ Friend Info lên Google Sheet chưa rõ** — `NEW-27` tự đánh dấu `needs-human-review`: chưa xác định admin sửa Friend Info có tự cập nhật row booking hiện hữu ngay hay chỉ ở lần sync kế tiếp. | `NEW-27` · kho `TC-LSN-469` | **Dev + PO** |
| 7 | **Ghi token ra log ở nhánh làm mới token** — `NEW-22` ghi nhận đã thấy trong mã một số chỗ in thẳng nội dung token ra log (**không** do ticket này thêm). | `NEW-22` | **Leader** — mở ticket bảo mật riêng nếu xác nhận |
| 8 | **Phạm vi app mobile cho Salon Booking và Friend Info chưa xác nhận được.** Với **Lesson (FA-019)** spec ghi rõ có portal ứng dụng di động Admin gọi Google Sheet → đã viết TC (`TC-SYNCAPP001-01/02/03`). Nhưng: (a) `spec-features/admin/salon-booking/feature-spec.md` **không** có mục "Ứng dụng di động Admin" tương ứng — chỉ 1 dòng `CalendarSalonSyncBookingGoogle · Chưa rõ (có thể Mobile/Spring Boot)` (`:1067`); (b) `friend-information/feature-spec.md:566` có **6 Mobile API** (`/api/mobile/save-custom-info`, `save-custom-info-v2`, …) nhưng spec tự ghi *"ngoài scope Admin portal, chỉ liệt kê, chưa phân tích chi tiết"* (`:711`) → **chưa xác định** các endpoint này có kích hoạt đồng bộ Google Sheet (`updateFriendInfo()`) hay không. **Không tự suy diễn** — cần Dev xác nhận trước khi viết TC cho 2 vùng này. | `spec-features/admin/salon-booking/feature-spec.md:1067` · `spec-features/admin/friend-information/feature-spec.md:566,711` | **Dev** — xác nhận: (1) Salon Booking có endpoint app mobile Admin ghi Google Sheet không; (2) 6 Mobile API friend info có trigger `updateFriendInfo()` lên sheet không. Có → bổ sung TC tương ứng `TC-SYNCAPP001-*` |
