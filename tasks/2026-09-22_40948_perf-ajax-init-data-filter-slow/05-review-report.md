# 05 — Review Report

## 0. Nguồn TC

- **Nguồn đã dùng**: MCP LME TEST STUDIO — task #321 (ticket 40948, round 1, branch `ai_small_40948`, `reviewState = leader`)
- **Tổng số TC review**: 37

---

## 1. Coverage — dev-impact + diff code

- **dev-impact**: `13/13` mục có TC (BUG + F1–F4 + D1 + T1–T7) — **CHƯA ĐỦ** (toàn bộ 37 TC chưa chạy → không mục nào có kết luận test; thêm 2 mục còn `RISK` ở bảng dưới)
- **diff code**: `5/9` điểm có TC đủ — **CHƯA ĐỦ**

**Kết luận**: `5/9` vùng ảnh hưởng đủ TC · **4 GAP** · **4 RISK**

| # | Chiều | Vùng ảnh hưởng | TC hiện có | Status | Severity | Thiếu gì |
|---|---|---|---|---|---|---|
| **G1** | diff code | 2 `parent_type` lịch **ngoài allowlist**: `calendar-setting-show-booking-form` (màn cài đặt hiển thị form đặt lịch) và `filter-calendar-salon-booking` (lọc đặt lịch salon). Spec §7.7 + `db/db-mapping.md` ghi **trước fix cả 2 đều trả `line_user_ids`**; allowlist của bản fix chỉ có **3** màn lịch gửi sau đặt chỗ | — (NEW-3 chỉ cover 3 màn `*-setting-status-send-after-booking`) | `GAP` | `[BLOCKER]` | 0 TC nào mở 2 màn này sau fix. Nếu 2 màn thực sự đọc `line_user_ids` thì nay nhận rỗng → số người / danh sách đối tượng đặt lịch hỏng |
| **G2** | diff code | `WHERE` scope của các lệnh UPDATE do `saveFilterV2` sinh ra (broadcast cha + **gộp update broadcast con bằng `whereIn`**, entity calendar, `rich_menu_switch_items.filter_ids`) | NEW-19 (chỉ kiểm 1 bản ghi đối chứng **cùng bot**) | `GAP` | `[BLOCKER]` | Không TC nào kiểm scope trên **2 bot** (RULE-07 / DATA-DB-001). `whereIn` gộp update là chỗ dễ ghi lan sang bản ghi của bot khác nhất |
| **G3** | dev-impact ⇄ diff code | `FilterController::saveFilterV2` — `03-dev-impact.md` mục 3 kê **"không sửa ở nhánh này"**, nhưng `spec_delta` + `dev_impact` của Studio cho thấy diff **có** sửa: dùng chung `countFilterResult`, **bỏ 2 query broadcast thừa**, **gộp update broadcast con (`whereIn`)** | NEW-19 (1 nhánh broadcast có con) | `RISK` | `[MAJOR]` | Nhánh broadcast **không có tin con** + nhánh non-broadcast của `saveFilterV2` chưa có TC riêng. Chiều (a) đang kê thiếu phạm vi thật của diff → cần Dev cập nhật file 03 |
| **G4** | diff code | **Broadcast protection** (spec §7.3): chặn lưu filter khi broadcast `delivering` / `delivered` / trong **5 phút** trước giờ gửi — đi qua đúng `saveFilterV2` vừa bị sửa | — | `GAP` | `[MAJOR]` | 0 TC Abnormal cho 3 trạng thái bị chặn; #19688 chỉ dùng bản nháp "để tránh ràng buộc chặn sửa trước giờ gửi" (né đúng vùng này) |
| **G5** | diff code | **App quản trị di động** — Dev tự ghi "cùng mẫu lỗi còn ở **API lọc phía mobile** (ngoài phạm vi)"; middleware `MobileAuthenticate` cũng nằm trong nguồn cảnh báo chậm của ticket | — | `GAP` | `[MAJOR]` | 0 TC xác nhận app quản trị di động **không** gọi `EP-02` / không bị ảnh hưởng bởi `line_user_ids` rỗng. "Ngoài phạm vi" mới là lời Dev, chưa có bằng chứng test |
| **G6** | dev-impact + diff code | **Toàn bộ 37 TC ở trạng thái chưa chạy** (`untested 37/37`, `pass 0`) | 37 TC | `RISK` | `[BLOCKER]` | Mọi ô "có TC" ở cả 2 chiều đều chưa có kết luận Đạt/Không đạt → coverage trên giấy, chưa phải coverage thật |
| **G7** | diff code | **Mục tiêu của ticket**: giảm thời gian phản hồi (max 52s, p95 52s) đo trên **production `step.lme.jp`**, bot 107152 / 196392 / 41734 / 109213 | NEW-25, NEW-26, NEW-27, NEW-28 (`env_scope = local`), NEW-29 (`all`, manual) | `RISK` | `[MAJOR]` | 4/5 TC hiệu năng khai `local`; 0 TC chạy production. RULE-08: không kết luận hiệu năng / job / loadbalance từ local. Dev cũng **chưa đo được** thời gian thật (MySQL dev không kết nối được) → chưa nguồn nào chứng minh ticket đạt mục tiêu |
| **G8** | diff code | Nhánh fix **chép nguyên văn** `countFilterResult` + hằng allowlist từ ticket **#40986 chưa merge** (cùng file `FilterController.php`) | — | `RISK` | `[MAJOR]` | 0 TC / 0 bước xác nhận sau khi **cả 2 nhánh cùng lên release**: nếu #40986 bị sửa nội dung trong lúc review thì khối này xung đột và phải gộp tay. Cần 1 lượt verify lại sau merge, không phải chỉ test nhánh đơn |

---

## 2. Quan điểm test còn thiếu

**Kết luận**: đã quét 80 quan điểm (index) — `12` quan điểm Trigger khớp task đã đủ TC · `7` quan điểm dưới đây **chưa cover đủ**.

| # | Mã quan điểm | Ưu tiên | Trigger khớp vì | TC hiện có | Severity | Thiếu gì |
|---|---|---|---|---|---|---|
| **Q1** | `DEPLOY-LIVE-001` | Cao | Bản fix **đổi payload API** (`line_user_ids` rỗng cho phần lớn `parent_type`), release production không bật maintain | — | `[BLOCKER]` | 0 TC cho "client cũ gọi server mới": tab đang mở modal lọc từ TRƯỚC release, không reload, bấm 保存 / tạo CSV sau release → phải báo lỗi rõ, không exception 500, không lưu nửa vời |
| **Q2** | `REG-RUN-001` | Cao | Hệ thống có job CSV đang chạy + broadcast đặt lịch đã lưu `filter_number` từ trước release | — | `[MAJOR]` | 0 TC cho dữ liệu/job **đang chạy dở TỪ TRƯỚC** bản fix: mục CSV đã lưu điều kiện trước release, job chạy sau release; broadcast hẹn giờ đã ghi `filter_number` cũ → không chạy lại từ đầu, không gửi trùng, không mất |
| **Q3** | `SYNC-APP-001` | TB → **Cao** (flow critical user-facing) | Bộ lọc bạn bè có mặt trên **cả web và app quản trị di động**; Dev tự nêu API lọc mobile cùng mẫu lỗi | — | `[MAJOR]` | 0 TC nào chạm app. Nghiệp vụ dùng chung web↔app → phải test app hoặc ghi rõ **ai xác nhận** app không áp dụng (liên kết `G5`) |
| **Q4** | `COMPAT-LEGACY-001` | Cao | Modal lọc có **2 phiên bản song song** V1 (`filters`) ⇄ V2 (`filters_v2`) — spec §1 "Hai phiên bản song song", §7.1 | — | `[MAJOR]` | 0 TC cho bản ghi còn dùng filter **V1** (broadcast V1, màn danh sách bạn bè V1): mở → sửa → lưu lại sau fix. RULE-09 cấm đánh × chỉ vì "nhánh mới chạy ổn" |
| **Q5** | `PERM-002` | Cao | Endpoint trả **`line_user_id` = PII**; hàm đọc dữ liệu bị thay bằng `countFilterResult` dùng chung | — | `[MAJOR]` | 0 TC gọi thẳng `EP-02` với `parent_id` thuộc **bot khác** / role không đủ quyền → phải 403, tuyệt đối không trả danh sách mã bạn bè của bot khác |
| **Q6** | `BULK-001` | Cao | Tổ hợp **bộ lọc + thao tác hàng loạt** (gắn tag hàng loạt theo điều kiện) | NEW-22 (chỉ `Normal`) | `[MAJOR]` | RULE-01: thiếu `Abnormal` + `Boundary`. Quan trọng nhất là case **bộ lọc khớp 0 người** → thao tác hàng loạt phải tác động 0 người, **không rơi về toàn bộ bạn bè** (đúng rủi ro khi `line_user_ids` nay rỗng) |
| **Q7** | `MSG-001` | Cao | Mọi chức năng **gửi tin có điều kiện lọc**; `saveFilterV2` ghi `filter_number` + cascade copy điều kiện sang broadcast con | NEW-18, NEW-19 (dừng ở số hiển thị + bản ghi DB) | `[MAJOR]` | RULE-06: 0 TC verify **output cuối** — tin thật đến đúng tập người trên **LINE app** sau khi lưu filter bằng code mới, cho cả tin cha và tin con |

> Quan điểm đã đủ TC (không ghi chi tiết): `FUNC-001` · `FUNC-004` · `DATA-COUNT-001` · `DATA-DB-001` (phần không-scope) · `REG-SHARED-001` · `OUT-EXPORT-001` · `JOB-001` · `LIST-001` · `PERF-LARGE-001` · `ENV-003` (có TC nhưng env sai — xem `G7`) · `UI-003` · `CONC-001`.

---

## 3. TC trùng lặp

Đã rà **37** TC theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`) — phát hiện **2 nhóm trùng**.

| # | Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|---|
| 1 | Rich menu — đặt điều kiện hiển thị (`setting_rich_menu`) | **#19689** (chi tiết hơn: thêm expected "không hiện lẫn điều kiện của phần chuyển richmenu") | **NEW-33** → **gộp** vào #19689 | `DUP-SUBSET` | Cùng `REG-SHARED-001` · cùng `Normal` · cùng đối tượng + thao tác (lưu điều kiện hiển thị rich menu → mở lại → số người) · tiền đề tương đương (bot trả phí + rich menu đã tạo + 1 tag) | `[MINOR]` |
| 2 | Modal lọc dùng chung — nạp lại điều kiện + số người sau tối ưu | **NEW-4** (nêu rõ từng `parent_type`, có expected `line_user_ids = []`) | **NEW-30** → **gộp** (không xóa trắng: bổ sung phần "modal mở bình thường" vào NEW-4 nếu Leader thấy cần) | `DUP-SUBSET` | Cùng `API-CONTRACT-001` · cùng `Normal` · cùng thao tác (mở modal đã lưu filter → đọc số người) · NEW-30 **không nêu `parent_type` nào**, mọi bước của nó đã nằm trong NEW-4 + NEW-31/32/33 | `[MINOR]` |

> Gate đã chạy: bỏ NEW-33 và NEW-30 **không** làm mất cover vùng ảnh hưởng nào (rich menu vẫn còn #19689 + NEW-23; modal chung vẫn còn NEW-4 + NEW-20/21/31/32). Không TC nào bị đề nghị xóa khi là TC duy nhất của một impact.
> **KHÔNG tự xóa** — TC read-only; human xóa trên Studio bằng `testcase_delete`.

---

## 4. Mâu thuẫn trong TCs

Đã rà **37** TC × `spec-features/admin/friend-filter/shared-spec.md` (§7.3, §7.4, §7.7) + `db/db-mapping.md` × `kho-tcs/fa008-broadcast` · `fa041-caidatchat` · `fa013-friendlist` — phát hiện **2 mâu thuẫn**, đều là `CONF-SPEC`.

| # | Mã | TC | Expected của TC | Chuẩn đối chiếu | 2 khả năng (KHÔNG tự chọn bên) | Severity |
|---|---|---|---|---|---|---|
| 1 | `CONF-SPEC` | **NEW-5** | Với `step_message`: "số người bằng 0 và **danh sách mã bạn bè là mảng rỗng** ở cả ba lượt — **giữ nguyên hành vi trước bản sửa**" | Spec §7.7 bảng `parent_type`: `step_message` → Tính `filterNumber`: **Không** · Trả `line_user_ids`: **Có** | (a) TC sai: trước fix `step_message` **có** nhận `line_user_ids`, nên "giữ nguyên hành vi trước bản sửa" là mô tả sai — đây thực ra là **hành vi MỚI**, cần đổi expected và bổ sung TC cho màn ステップ配信 xem có đọc trường này không; (b) Spec cũ: code thực tế đã bỏ trả `line_user_ids` cho `step_message` từ trước → spec §7.7 cần update | `[MAJOR]` |
| 2 | `CONF-SPEC` | **NEW-23**, **NEW-5** (lượt richmenu switch) | "Modal mở ra bình thường, **KHÔNG hiển thị số người** (loại màn này vốn không đếm)" / "số người bằng 0" | Spec §7.7: `filter-rich-menu-toggle` → Tính `filterNumber`: **Có** · Trả `line_user_ids`: **Có** · side effect update `rich_menu_switch_items.filter_ids` | (a) TC đúng theo hiện trạng UI (modal tab 切り替え không có số đếm) → **spec §7.7 cần update**; (b) Spec đúng và bản fix/bản gốc đang bỏ đếm nhầm cho `parent_type` này → là bug tiềm ẩn, phải hỏi Dev. Cần Dev đối chiếu thẳng nhánh `parent_type` trong `initDataFilter` | `[MAJOR]` |

> `CONF-TC`: **không có** — đã đối chiếu riêng 2 cặp dễ nhầm (NEW-5 richmenu switch vs #19689 setting rich menu; NEW-12 lọc 0 người vs NEW-7) — khác `parent_type` / khác tầng, expected không loại trừ nhau.
> `CONF-KHO`: **không có** — TC-CST-91 (`対象人数` sau khi lưu điều kiện lọc), TC-CST-95, TC-CST-107 của `kho-tcs/fa041` và TC-BC-19 (`配信数` = `filter_number`) của `fa008` đều **nhất quán** với NEW-11 / NEW-18. `kho-tcs` **chưa có** file riêng cho `SC-003 Friend Filter` (kho `fa008` ghi rõ "ĐỀ XUẤT tách feature riêng SC-003 — chờ xác nhận") → phần modal lọc dùng chung chưa có chuẩn kho để đối chiếu.

---

## 5. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | Toàn bộ nguồn TC | `pass 0/37` (0%) — 37/37 TC ở trạng thái chưa chạy, `last_exec` trống, không TC nào có kết luận Đạt/Không đạt. Ticket đang ở "Fix done - Đợi test" | Chạy bộ TC trước khi kết luận; mọi dòng "đủ TC" ở §1 chỉ là coverage trên giấy |
| 2 | `[MAJOR]` | NEW-25, NEW-26, NEW-27, NEW-28 (`env_scope = local`), NEW-5, NEW-8, NEW-19 (`local`) | **RULE-08 / ENV-003**: ticket là bug hiệu năng đo trên **production `step.lme.jp`** với bot thật, nhưng toàn bộ TC hiệu năng khai `local`; `envAuto` cho thấy 0 lượt chạy ở `prd`/`staging`. Dev cũng chưa đo được thời gian thật (mức verify chỉ `lint`, MySQL dev Connection refused) | Nâng `env_scope` của 4 TC hiệu năng lên `prd` (hoặc tối thiểu môi trường có dữ liệu tương đương bot 107152 / 196392); giữ `local` chỉ cho phần đếm số lần chạy truy vấn |
| 3 | `[MAJOR]` | NEW-12 | Tiêu đề "…**không tạo file thừa**" nhưng `Kết quả mong đợi` **không verify file nào** — chỉ kiểm số người = 0 và trạng thái modal. Tiêu đề và expected lệch nhau, đồng thời bỏ lọt case kho `TC-CST-108` (lọc 0 người → job vẫn chạy xong, file chỉ có header) | Hoặc sửa tiêu đề cho khớp expected, hoặc bổ sung expected về file CSV sinh ra (xem TC đề xuất `TC-OUTEXPORT001-01`) |
| 4 | `[MAJOR]` | NEW-30 | TC quá generic: tiền đề chỉ ghi "một bản ghi sử dụng bộ lọc bạn bè đã lưu trước đó", **không nêu `parent_type` / màn hình nào** → không dựng lại được env và không map được vào vùng ảnh hưởng cụ thể | Ghi rõ màn + `parent_type`, hoặc gộp vào NEW-4 (xem §3) |
| 5 | `[MAJOR]` | NEW-29 | `Kết quả mong đợi` không đo lường được: "thời gian chờ ở **mức chấp nhận được**", "không còn **cảm giác treo**" — không có ngưỡng số | Đặt ngưỡng cụ thể đối chiếu số của ticket (p95 52s → ngưỡng mục tiêu, vd ≤ 5s để không còn bắn cảnh báo `SLOWLV1`) |
| 6 | `[MINOR]` | NEW-19 | TC verify ghi DB cho broadcast cha + **tất cả** tin con nhưng khai `env_scope = local`; đây là dữ liệu nghiệp vụ gửi tin, local không đại diện | Nâng lên `staging` trở lên |
| 7 | `[MINOR]` | #19688, #19689, #19690, #19691 | Dữ liệu test mang tiền tố của ticket khác (`39263-TAG-1`, `39263-CU-2`) trong khi 33 TC còn lại dùng `TC40948_*` → khó trace khi chạy lẫn | Đổi tên dữ liệu hoặc ghi rõ ở `Ghi chú` rằng đây là TC regression tái sử dụng từ #39263 |

---

## 6. TCs thừa / ngoài phạm vi task

Đã rà **37** TC — **không có TC nào ngoài phạm vi task**.

- NEW-20, NEW-21 (lọc tại chỗ 友だちリスト / トークリスト) tuy không map thẳng vào `F*`/`T*` nào của file 03, nhưng **dẫn được** từ `dev_impact` Studio ("rủi ro hồi quy tập trung ở màn nào đọc `line_user_ids`") và đi qua đúng `modal_filter_v2.js` / `modal_filter_message.js` mà Dev kê ở mục 3 → **giữ**.
- #19688 / #19689 / #19690 / #19691 là TC regression tái sử dụng từ ticket #39263, map được `REG-SHARED-001` + T1 → **giữ**. `[NIT]`: 4 TC này nên nằm trong **bộ regression chung của kho** (`SC-003 絞り込み` — kho đang đề xuất tách feature riêng) thay vì nhân bản vào từng task fix của modal lọc.

---

## 7. TCs đề xuất bổ sung (15)

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-REGSHARED001-01 | UI | REG-SHARED-001 | カレンダー — 予約フォーム表示設定 (`calendar-setting-show-booking-form`) | Normal | auto | Tất cả | Màn cài đặt hiển thị form đặt lịch vẫn tính đúng số người sau khi endpoint đổi hợp đồng | Bot gói Standard/Pro, đã đăng nhập admin và chọn đúng bot test. Có 6 bạn bè `TC40948_F01`–`F06`, trong đó `F01`–`F04` mang tag `TC40948_TAG_A`, `F04` đã chặn bot. Đã có 1 lịch `TC40948_CAL_FORM` chưa đặt điều kiện lọc hiển thị form | 1. Mở màn quản lý lịch → chọn `TC40948_CAL_FORM` → mục cài đặt đối tượng hiển thị form đặt lịch<br>2. Bấm mở modal 絞り込み<br>3. Thêm điều kiện tag = `TC40948_TAG_A` → bấm 保存<br>4. Đọc số người hiển thị trên màn<br>5. Lưu cấu hình lịch → thoát ra → mở lại đúng màn đó<br>6. Mở lại modal 絞り込み | tag `TC40948_TAG_A` | Bước 4: số người hiển thị bằng **3** (4 người mang tag trừ `F04` đã chặn), không phải 0 và không trống. Bước 5: sau khi mở lại, số người vẫn bằng 3. Bước 6: modal hiện đúng 1 dòng điều kiện tag đã lưu. Không có thông báo lỗi, màn không trắng | | Lấp G1 · `parent_type` này **ngoài** allowlist 5 màn của bản fix nhưng spec §7.7 + `db-mapping` ghi trước fix **có** trả `line_user_ids` · Evidence: ảnh màn + số người |
| TC-REGSHARED001-02 | UI | REG-SHARED-001 | カレンダーサロン — 予約絞り込み (`filter-calendar-salon-booking`) | Normal | auto | Tất cả | Lọc đặt lịch salon vẫn tính đúng số người sau khi endpoint đổi hợp đồng | Như TC-REGSHARED001-01 về bot và bộ bạn bè. Đã có 1 lịch salon `TC40948_SALON` với chức năng lọc đối tượng đặt lịch chưa đặt điều kiện | 1. Mở màn lịch salon → `TC40948_SALON` → mục lọc đối tượng đặt lịch<br>2. Mở modal 絞り込み → thêm điều kiện tag `TC40948_TAG_A` → 保存<br>3. Đọc số người hiển thị<br>4. Lưu, thoát, mở lại màn<br>5. Mở lại modal | tag `TC40948_TAG_A` | Bước 3: số người bằng **3**. Bước 4: số người giữ nguyên bằng 3 sau khi mở lại. Bước 5: modal hiện đúng điều kiện đã lưu. Không lỗi, không hiện lẫn điều kiện của màn khác | | Lấp G1 · cùng lý do với TC-REGSHARED001-01, `parent_type` khác |
| TC-DATADB001-01 | Data | DATA-DB-001 | 一斉配信 — lưu điều kiện lọc (`saveFilterV2`, update broadcast con bằng `whereIn`) | Abnormal | auto | Tất cả | Lưu điều kiện lọc ở bot A không chạm bản ghi trùng tên ở bot B | Có **2 bot** test `BOT_A` và `BOT_B` cùng thuộc tài khoản admin, mở bằng **2 phiên đăng nhập riêng** (đổi bot ảnh hưởng cả trình duyệt). Mỗi bot đều có 1 tin gửi hàng loạt nháp **trùng tên** `TC40948_BC_PARENT` với ≥ 3 tin con, và 1 tin `TC40948_BC_OTHER`. Đã ghi lại số người + thời điểm lọc của **tất cả** bản ghi ở cả 2 bot trước khi chạy | 1. Ở phiên `BOT_A`: mở `TC40948_BC_PARENT` → modal 絞り込み → thêm tag `TC40948_TAG_A` → 保存<br>2. Query DB các bản ghi broadcast của `BOT_A` (cha + con + `BC_OTHER`)<br>3. Query DB các bản ghi broadcast của `BOT_B` cùng tên<br>4. Ở phiên `BOT_B`: mở `TC40948_BC_PARENT` của bot B, đọc số người trên màn | tag `TC40948_TAG_A` (chỉ tồn tại ở `BOT_A`) | Bước 2: chỉ tin cha + **tất cả** tin con của `BOT_A` được ghi số người mới và cùng một thời điểm lọc; `BC_OTHER` của bot A giữ nguyên. Bước 3: **mọi** bản ghi của `BOT_B` giữ nguyên số người và thời điểm lọc **y hệt mốc trước khi chạy** — không dòng nào bị đổi. Bước 4: màn của bot B hiển thị đúng giá trị cũ. Nếu bản ghi bot B đổi → FAIL, `whereIn` thiếu điều kiện `bot_id` | | Lấp G2 · RULE-07 / DATA-DB-001 bắt buộc 2 tài khoản · Evidence: ảnh chụp query trước/sau kèm câu query trên cả 2 bot |
| TC-DATACOUNT001-01 | Data | DATA-COUNT-001 | 一斉配信 — lưu điều kiện lọc cho tin **không có tin con** | Boundary | auto | Tất cả | Lưu điều kiện lọc cho tin gửi hàng loạt không có tin con vẫn ghi đúng số người, không lỗi | Bot test, bộ bạn bè `TC40948_F01`–`F06` như trên. Có 1 tin gửi hàng loạt nháp `TC40948_BC_SOLO` **chưa từng sinh tin con** (chỉ 1 mốc gửi). Đã ghi lại số người hiện tại của bản ghi | 1. Mở `TC40948_BC_SOLO` → modal 絞り込み → thêm tag `TC40948_TAG_A` → 保存<br>2. Đọc số người nhận trên màn soạn tin<br>3. Query DB bản ghi `TC40948_BC_SOLO`<br>4. Tải lại trang, đọc lại số người | tag `TC40948_TAG_A` | Bước 2: số người nhận bằng **3**. Bước 3: bản ghi có số người = 3 và thời điểm lọc được cập nhật; **không phát sinh bản ghi con nào** và không có câu lệnh cập nhật nào chạy vào bản ghi khác. Bước 4: số người vẫn bằng 3. Không có thông báo lỗi | | Lấp G3 · nhánh "broadcast không có tin con" của `saveFilterV2` (Studio `dev_impact`: bỏ 2 query broadcast thừa + gộp update con) chưa có TC · bổ sung cho NEW-19 chỉ test tin CÓ con |
| TC-MSG002-01 | UI | MSG-002 | 一斉配信 — Broadcast protection khi lưu điều kiện lọc | Abnormal | auto | Tất cả | Lưu điều kiện lọc bị chặn đúng với tin đang phát, đã phát và tin trong 5 phút trước giờ gửi | Bot test. Chuẩn bị 3 tin gửi hàng loạt: `TC40948_BC_DELIVERING` (trạng thái đang phát), `TC40948_BC_DELIVERED` (đã phát xong), `TC40948_BC_SOON` (đã đặt lịch, giờ gửi còn **dưới 5 phút**, không phải nháp). Ghi lại số người + điều kiện lọc hiện tại của cả 3 | 1. Mở `TC40948_BC_DELIVERING` → thử mở modal 絞り込み và bấm 保存<br>2. Lặp lại với `TC40948_BC_DELIVERED`<br>3. Lặp lại với `TC40948_BC_SOON`<br>4. Query DB 3 bản ghi + bảng điều kiện lọc của chúng | tag `TC40948_TAG_A` | Cả 3 lượt: thao tác lưu **bị chặn** với thông báo lỗi rõ ràng trên màn (không im lặng, không exception 500). Bước 4: số người, thời điểm lọc và các dòng điều kiện lọc của cả 3 bản ghi **không đổi** so với mốc trước khi chạy | | Lấp G4 · Business rule spec `friend-filter/shared-spec.md` §7.3 đi qua đúng `saveFilterV2` bị sửa trong diff · Evidence: ảnh thông báo lỗi + query DB trước/sau |
| TC-SYNCAPP001-01 | UI | SYNC-APP-001 | App quản trị di động — màn danh sách bạn bè có bộ lọc | Normal | manual | Tất cả | App quản trị di động lọc bạn bè vẫn ra đúng số người và đúng danh sách sau bản fix | Bản fix đã deploy lên môi trường mà app quản trị di động đang trỏ tới. Có thiết bị thật đã cài app quản trị di động và đăng nhập đúng bot test. Bộ bạn bè `TC40948_F01`–`F06` với tag `TC40948_TAG_A` (`F04` đã chặn). Đã biết kết quả đúng trên web: 3 người | 1. Trên web: lọc danh sách bạn bè theo tag `TC40948_TAG_A`, ghi lại số người và danh sách<br>2. Trên app quản trị di động (không restart app, dùng pull-to-refresh): vào màn danh sách bạn bè → áp cùng điều kiện lọc<br>3. So sánh số người và danh sách giữa 2 bên<br>4. Ghi lại app gọi endpoint nào (nếu xem được log) hoặc xin Dev xác nhận bằng văn bản | tag `TC40948_TAG_A` | App hiển thị đúng **3** người và đúng danh sách `F01`–`F03`, khớp 100% với web. Không màn trắng, không lỗi. Nếu app không dùng chung endpoint thì phải có **xác nhận bằng văn bản của Dev** ghi vào kết quả test, không kết luận bằng suy đoán | | Lấp G5 + Q3 · `manual vì cần thiết bị thật chạy app quản trị di động` · căn cứ: Dev tự ghi "API lọc phía mobile (ngoài phạm vi)" + middleware `MobileAuthenticate` nằm trong nguồn cảnh báo chậm của ticket · Evidence: screenshot **cả** web và app |
| TC-DEPLOYLIVE001-01 | UI | DEPLOY-LIVE-001 | Release không maintain — màn đang mở gọi server đã đổi payload | Abnormal | manual | product | Màn mở từ trước release vẫn xử lý đúng khi server đã bỏ trả danh sách mã bạn bè | Đã hẹn được cửa sổ release lên production. Trước release: mở sẵn 2 tab — tab 1 ở màn tạo mục xuất file CSV管理 (đã mở modal 絞り込み), tab 2 ở màn クロス分析. **Không reload** 2 tab này trong suốt quá trình | 1. Trước release: ở tab 1 thêm điều kiện tag `TC40948_TAG_A` nhưng **chưa** bấm 保存<br>2. Thực hiện release bản fix<br>3. **Không reload**, ở tab 1 bấm 保存 rồi lưu mục xuất file<br>4. Ở tab 2 bấm lưu mục phân tích chéo<br>5. Kiểm tra bản ghi vừa lưu ở cả 2 màn + log lỗi phía server | tag `TC40948_TAG_A` | Cả 2 tab: thao tác lưu cho kết quả **đúng** (số đối tượng đúng, mục xuất file có nút tải xuống, mục phân tích chéo lưu đủ danh sách) **hoặc** báo lỗi rõ ràng yêu cầu tải lại trang. Tuyệt đối không exception 500, không lưu nửa vời (bản ghi tồn tại nhưng số đối tượng 0 / danh sách rỗng). Log server không có ngoại lệ liên quan `line_user_ids` | | Lấp Q1 · `manual vì phải canh đúng thời điểm release production` · RULE-08: release + payload đổi → `product` · Evidence: screenshot màn cũ + bản ghi DB sau submit + log request |
| TC-REGRUN001-01 | Data | REG-RUN-001 | Job CSV + broadcast đặt lịch đang chạy dở khi release | Abnormal | manual | product | Job và dữ liệu đã lưu điều kiện lọc từ trước release vẫn tiếp tục đúng sau release | Trước release: (a) đã tạo mục xuất file CSV管理 `TC40948_CSV_PRERELEASE` với điều kiện tag `TC40948_TAG_A`, job **đang chạy dở**; (b) đã có broadcast `TC40948_BC_SCHEDULED` đặt lịch gửi **sau** thời điểm release, đã lưu điều kiện lọc và ghi số người từ trước. Đã ghi lại số đối tượng của cả hai | 1. Xác nhận job CSV đang ở trạng thái 作成中 và broadcast đang chờ tới giờ gửi<br>2. Release bản fix<br>3. Chờ job CSV chạy xong → tải file về đối chiếu<br>4. Chờ tới giờ gửi của broadcast → kiểm tra số người đã gửi và người nhận thật<br>5. Query DB số người của broadcast trước/sau release | — | Bước 3: job **không chạy lại từ đầu**, file CSV chứa đúng tập người khớp điều kiện đã lưu từ trước release, không rỗng. Bước 4: broadcast gửi đúng **một lần**, đúng tập người, không gửi trùng và không bị bỏ qua. Bước 5: số người của broadcast không bị ghi đè sai sau release | | Lấp Q2 · `manual vì phải canh release production và chờ job/giờ gửi thật` · RULE-08 (job nền + gửi tin) → `product` |
| TC-COMPATLEGACY001-01 | UI | COMPAT-LEGACY-001 | Modal lọc **V1** (`filters`) chạy song song V2 | Normal | auto | Tất cả | Bản ghi còn dùng bộ lọc đời cũ V1 vẫn mở, sửa và lưu lại được sau bản fix | Bot test có ít nhất 1 bản ghi **còn dùng filter V1** (`filters`) — vd broadcast/màn danh sách bạn bè tạo từ trước khi chuyển sang V2; xác nhận bằng dữ liệu đã lưu, không tạo mới bằng UI V2. Song song có 1 bản ghi dùng filter **V2** để đối chứng | 1. Mở bản ghi V1 → mở modal lọc → đọc điều kiện đang lưu và số người<br>2. Sửa 1 điều kiện → lưu lại<br>3. Mở lại bản ghi V1, đọc số người<br>4. Lặp bước 1–3 với bản ghi V2 để đối chứng | tag `TC40948_TAG_A` | Bản ghi V1: modal mở được, hiển thị đúng điều kiện cũ, số người tính đúng theo tập bạn bè thật, sửa và lưu lại thành công, không trắng màn và không lỗi. Bản ghi V2 cho kết quả tương đương. Không nhánh nào trả số người 0 do không nhận được danh sách mã bạn bè | | Lấp Q4 · RULE-09: spec `friend-filter` §1 "Hai phiên bản song song" V1 `filters` ⇄ V2 `filters_v2` · Evidence: screenshot **cả 2 nhánh** cũ và mới |
| TC-PERM002-01 | API | PERM-002 | `EP-02 POST /ajax/init-data-filter` — kiểm soát truy cập | Abnormal | auto | Tất cả | Gọi endpoint với bản ghi cha của bot khác không trả về danh sách mã bạn bè | Có 2 bot `BOT_A`, `BOT_B` thuộc 2 tài khoản admin **khác nhau**, đăng nhập bằng 2 phiên riêng. `BOT_B` có 1 mục xuất file CSV管理 `TC40948_CSV_B` đã lưu điều kiện lọc và có số người > 0. Đang đăng nhập bằng tài khoản của `BOT_A` | 1. Đăng nhập tài khoản `BOT_A`, mở màn CSV管理 để lấy phiên làm việc hợp lệ<br>2. Gửi request tới endpoint với loại màn của mục xuất file nhưng **mã bản ghi cha là của `TC40948_CSV_B`** (thuộc `BOT_B`)<br>3. Lặp lại bước 2 với tài khoản có role bị ẩn chức năng xuất CSV trên UI<br>4. Đọc toàn bộ nội dung phản hồi | mã bản ghi cha của bot khác | Lượt 2 và 3: hệ thống từ chối — trả 403 (hoặc lỗi tương đương) **hoặc** trả số người 0 với danh sách mã bạn bè rỗng. Tuyệt đối **không** trả về mã bạn bè nào của `BOT_B`, không lộ điều kiện lọc của bot khác. Nếu trả đủ danh sách mã bạn bè của `BOT_B` → FAIL | | Lấp Q5 · `line_user_id` là PII; hàm đọc dữ liệu vừa bị thay bằng `countFilterResult` dùng chung nên phải xác nhận lại scope · Evidence: mã lỗi + body phản hồi của cả 2 lượt |
| TC-BULK001-01 | UI | BULK-001 | 友だち一括アクション — bộ lọc khớp 0 người | Abnormal | auto | Tất cả | Thao tác hàng loạt với bộ lọc không khớp ai không tác động lên bất kỳ bạn bè nào | Bot gói Standard/Pro. Bộ bạn bè `TC40948_F01`–`F06`. Đã tạo tag `TC40948_TAG_NOBODY` **chưa gắn cho ai** và tag đích rỗng `TC40948_TAG_BULK0`. Đã ghi lại số bạn bè đang mang `TC40948_TAG_BULK0` (= 0) | 1. Mở màn danh sách bạn bè → chọn thao tác hàng loạt gắn tag<br>2. Trong modal, đặt điều kiện lọc tag = `TC40948_TAG_NOBODY`<br>3. Đọc số người hiển thị<br>4. Chọn tag đích `TC40948_TAG_BULK0` → thực thi<br>5. Đếm số bạn bè đang mang `TC40948_TAG_BULK0` | tag `TC40948_TAG_NOBODY` | Bước 3: số người bằng **0**. Bước 4: hệ thống chặn hoặc chạy xong không tác động ai, có thông báo rõ ràng, không exception. Bước 5: số bạn bè mang `TC40948_TAG_BULK0` vẫn bằng **0** — **tuyệt đối không rơi về toàn bộ 6 bạn bè**. Nếu cả 6 người bị gắn tag → FAIL nghiêm trọng | | Lấp Q6 · RULE-01 bổ sung `Abnormal` cho BULK-001 (NEW-22 chỉ có `Normal`) · đúng rủi ro khi danh sách mã bạn bè nay rỗng · Evidence: số bản ghi thật bị tác động đếm từ danh sách |
| TC-BULK001-02 | UI | BULK-001 | 友だち一括アクション — bộ lọc khớp đúng 1 người | Boundary | auto | Tất cả | Thao tác hàng loạt với bộ lọc thu hẹp còn đúng 1 người chỉ tác động 1 người | Như TC-BULK001-01. Bổ sung: tag `TC40948_TAG_ONE` chỉ gắn cho **duy nhất** `TC40948_F02`; tag đích rỗng `TC40948_TAG_BULK1` | 1. Mở thao tác hàng loạt gắn tag<br>2. Đặt điều kiện lọc tag = `TC40948_TAG_ONE`<br>3. Đọc số người hiển thị<br>4. Chọn tag đích `TC40948_TAG_BULK1` → thực thi<br>5. Liệt kê bạn bè đang mang `TC40948_TAG_BULK1` | tag `TC40948_TAG_ONE` | Bước 3: số người bằng **1**. Bước 5: đúng **một** người là `TC40948_F02` mang tag đích; 5 người còn lại không bị gắn. Số bản ghi bị tác động khớp 100% số đã lọc | | Lấp Q6 · RULE-01 bổ sung `Boundary` cho BULK-001 |
| TC-MSG001-01 | UI | MSG-001 | 一斉配信 — gửi thật tin cha và tin con sau khi lưu điều kiện lọc | Normal | manual | product | Tin gửi hàng loạt đến đúng tập người trên LINE app cho cả tin cha và mọi tin con | Bot test đã liên kết LINE OA thật. Có 3 tài khoản LINE thật đóng vai `TC40948_F01`–`F03` mang tag `TC40948_TAG_A`, và 1 tài khoản `TC40948_F05` **không** mang tag (dùng làm đối chứng phải không nhận tin). Có tin gửi hàng loạt `TC40948_BC_SEND` cấu hình sinh tin con, chưa tới giờ gửi | 1. Mở `TC40948_BC_SEND` → modal 絞り込み → thêm tag `TC40948_TAG_A` → 保存<br>2. Đếm tay số người khớp điều kiện, đối chiếu số hiển thị trên màn<br>3. Cho tin cha gửi → kiểm tra trên LINE app của cả 4 tài khoản<br>4. Chờ tin con gửi → kiểm tra lại trên LINE app của cả 4 tài khoản<br>5. Đối chiếu danh sách bạn bè đã gửi trên màn 配信履歴 | tag `TC40948_TAG_A` | Bước 2: số hiển thị bằng số đếm tay (**3**). Bước 3 và 4: **đúng 3 tài khoản** `F01`–`F03` nhận được tin trên LINE app; `F05` **không** nhận được tin ở cả tin cha lẫn tin con. Bước 5: danh sách đã gửi khớp 100% điều kiện lọc, không thừa không thiếu | | Lấp Q7 · RULE-06 output cuối: NEW-18/NEW-19 dừng ở số hiển thị + DB, chưa xác nhận trên LINE app · `manual vì production + xác nhận nhận tin trên LINE app thật` · RULE-08 (gửi tin tiêu thụ quota) → `product` |
| TC-OUTEXPORT001-01 | API | OUT-EXPORT-001 | チャット管理 CSV出力 — lọc khớp 0 người | Boundary | auto | Tất cả | Xuất CSV hội thoại với điều kiện không khớp ai vẫn sinh file chỉ có header, job chạy xong | Bot test gói trả phí. Bộ bạn bè `TC40948_F01`–`F06` đều có hội thoại trong khoảng ngày sẽ chọn. Đã tạo tag `TC40948_TAG_NOBODY` chưa gắn cho ai. Đang ở màn cài đặt chat, tab CSV | 1. Chọn khoảng ngày bao trùm toàn bộ hội thoại đã có<br>2. Mở modal 絞り込み → thêm điều kiện tag `TC40948_TAG_NOBODY` → 保存<br>3. Đọc số đối tượng hiển thị<br>4. Bắt đầu tạo CSV → chờ job chạy xong<br>5. Tải file về và mở ra đọc | tag `TC40948_TAG_NOBODY`, khoảng ngày bao trùm | Bước 3: số đối tượng bằng **0**, không lỗi. Bước 4: job chạy **xong bình thường**, chuyển sang lịch sử tạo, không treo ở 作成中 và không báo lỗi. Bước 5: file chỉ có dòng metadata + dòng tiêu đề cột, **không** chứa hội thoại của bất kỳ ai — đặc biệt không rơi về toàn bộ 6 người | | Lấp R1 · regression — dẫn từ `TC-CST-108` (kho `fa041`: lọc 0 người → file chỉ header, job vẫn xong) + `dev_impact` Studio cảnh báo `csv_download_chat11` ngoài allowlist · bù chỗ NEW-12 không verify file |
| TC-REGSHARED001-03 | Data | REG-SHARED-001 | アクション予約 — thực thi theo điều kiện lọc đã lưu | Normal | auto | Tất cả | Lịch hành động tự động chạy đúng tập người theo điều kiện lọc lưu bằng code mới | Bot test, bộ bạn bè `TC40948_F01`–`F06` với tag `TC40948_TAG_A` (`F04` đã chặn). Có 1 lịch hành động `TC40948_ACTION` cấu hình gắn tag đích `TC40948_TAG_ACT` (đang rỗng), thời điểm chạy đặt gần để quan sát được | 1. Mở `TC40948_ACTION` → modal 絞り込み → thêm tag `TC40948_TAG_A` → 保存<br>2. Đọc số người hiển thị<br>3. Lưu lịch hành động<br>4. Chờ tới thời điểm chạy<br>5. Đếm số bạn bè đang mang tag đích `TC40948_TAG_ACT` | tag `TC40948_TAG_A` | Bước 2: số người bằng **3**. Bước 5: đúng **3** người `F01`–`F03` được gắn tag đích; `F04` (đã chặn), `F05`, `F06` không bị gắn. Số người thực sự bị tác động khớp số đã lọc, không rơi về toàn bộ bạn bè | | Lấp R3 · regression — căn cứ: Studio `dev_impact` ghi `saveFilterV2` **cũng** dùng chung `countFilterResult`, mà `action_schedule` lưu điều kiện qua đúng hàm này; `03-dev-impact.md` T5 xếp Action Schedule mức Medium nhưng TC hiện có (NEW-32) chỉ dừng ở số hiển thị |

**Xác nhận đối chiếu & chống trùng**

| Mục | Kết quả |
|---|---|
| Đã đối chiếu kho-tcs | `kho-tcs/fa008-broadcast` (TC-BC-19 `配信数`) · `kho-tcs/fa041-caidatchat` (TC-CST-91 / 95 / 107 / **108**) · `kho-tcs/fa013-friendlist`. **Kho chưa có file riêng cho `SC-003 絞り込み`** (kho fa008 ghi rõ đang đề xuất tách feature) → phần modal lọc dùng chung không có chuẩn kho để đối chiếu |
| TC dùng lại từ kho | `TC-CST-108` được dùng làm **căn cứ** cho `TC-OUTEXPORT001-01` (không copy nguyên dòng — kho dùng 12 cột riêng) |
| Chống trùng | Đã đối chiếu **37** TC ở BƯỚC 0 + đối chiếu 15 TC đề xuất với nhau theo 4 yếu tố — **không TC đề xuất nào trùng**. Gần nhất là `TC-DATACOUNT001-01` vs NEW-19: khác ở nhánh **không có tin con**, và `TC-BULK001-01/02` vs NEW-22: khác loại case (`Abnormal`/`Boundary` vs `Normal`) |

---

## 8. Spec update needed

| # | Nội dung cần chốt | Nguồn | Ai chốt |
|---|---|---|---|
| 1 | Spec `friend-filter/shared-spec.md` §7.7: `step_message` ghi **Trả `line_user_ids` = Có** — mâu thuẫn expected của NEW-5 ("giữ nguyên hành vi trước bản sửa: rỗng"). Chốt hành vi **trước** bản fix để biết NEW-5 sai hay spec cũ | §4 mục 1 | Dev |
| 2 | Spec §7.7: `filter-rich-menu-toggle` ghi **Tính `filterNumber` = Có** — mâu thuẫn NEW-23 / NEW-5 ("không hiển thị số người, số người = 0") | §4 mục 2 | Dev + Leader |
| 3 | Allowlist của bản fix chỉ gồm **3** `parent_type` lịch, trong khi spec §7.7 + `db/db-mapping.md` liệt kê **5** `parent_type` calendar (`calendar-setting-show-booking-form`, `filter-calendar-salon-booking` bị bỏ ra). Chốt 2 `parent_type` này có thực sự đọc `line_user_ids` không; nếu có thì phải thêm vào allowlist, nếu không thì cập nhật spec | §1 G1 | Dev |
| 4 | Spec §7.7 và `db/db-mapping.md` **chưa có** 2 `parent_type` `csv_create_download_file` và `csv_download_chat11` — đây lại chính là 2 nơi quyết định của bản fix. Bổ sung vào bảng `parent_type` | BƯỚC 1 + Studio `dev_impact` | Dev |
| 5 | `03-dev-impact.md` mục 3 kê `saveFilterV2` "không sửa ở nhánh này", nhưng diff Studio cho thấy có sửa (bỏ 2 query broadcast thừa + gộp update tin con bằng `whereIn`). Cập nhật lại file 03 cho khớp diff thật, kèm xác nhận phạm vi merge với #40986 | §1 G3 + G8 | Dev |
