# 05 — Review Report

> Draft cho Leader verify. Chỉ ghi phần THIẾU + việc phải làm.

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | (1) Studio task #309 |
| Tổng số TC review | 34 |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: 5/13 vùng ảnh hưởng đủ TC · 1 GAP · 7 RISK (gộp thành 6 dòng G1–G6)

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | `BUG` — hết chậm trên dữ liệu thật + chỉ mục mới được optimizer chọn trên dữ liệu production (Dev: verify mức `lint`, chưa EXPLAIN MySQL thật) | `dev-impact` | NEW-17, NEW-24, NEW-30 (mã lạ, chỉ local) | RISK — chỉ đo ở local, bảng 176.777 dòng (NEW-24: 1.900 ms → 72 ms). 0 TC chạy lại đúng ca chậm 36s (rich menu `709823`, tải CSV 327 ngày) sau release | `[MAJOR]` |
| G2 | `D2` — đường ghi log tap (linect-service insert) sau khi thêm chỉ mục | `dev-impact` | NEW-32 (mã lạ `RULE-TOOL-029`, insert giả lập bằng Eloquent ở local) | GAP — NEW-32 không tính cover. 0 TC tap thật từ LINE app sau migration; 0 TC tap trong lúc migration đang chạy | `[MAJOR]` |
| G3 | `F2` / `D1` — migration thêm chỉ mục `detail_click_richmenu_statistic_index` trên bảng lớn `detail_click_richmenu` | `dev-impact` | NEW-29 (mã lạ), NEW-31, NEW-33 | RISK — up/down/chạy lại chỉ kiểm ở local 176.777 dòng. Chưa kiểm thời gian ALTER TABLE + không chặn ghi trên bảng cỡ production như chính Dev cảnh báo | `[MAJOR]` |
| G4 | Chặn dưới/trên của 2 câu gom lấy theo danh sách ngày → tương tác với mốc 最終レイアウト設定日 và nút 全期間 | `diff code` | NEW-3 (rich menu chưa từng đổi layout) | RISK — 0 TC có lượt tap trước 最終レイアウト設定日 (tooltip kho TC-RM-428: ngày trước mốc không hiển thị); chưa đối chiếu với bản trước fix trên dữ liệu thật | `[MAJOR]` |
| G5 | Lọc ngày nửa mở — Studio `dev_impact` cảnh báo mốc ngày Carbon có thể cắt ngày khác MySQL `DATE()` nếu lệch timezone | `diff code` | NEW-9, NEW-25, NEW-26, NEW-28 | RISK — biên 00:00:00 / 23:59:59 chỉ kiểm trên DB docker local, chưa chạy ở env có cấu hình timezone DB giống production | `[MAJOR]` |
| G6 | Hành vi đổi: rich menu 0 vùng → mỗi phần tử `detailClick` được thêm `data: []` (bản cũ chỉ có `day`) — trái câu "giữ nguyên hình dạng dữ liệu" của Dev | `diff code` | NEW-7, NEW-22 | RISK — NEW-22 chạy ra đúng khác biệt này nhưng expected chỉ "ghi lại để đối chiếu", không chốt đạt/không đạt | `[MAJOR]` |

- G1 → TC-PERFLARGE001-01 / -02 / -03 ở §5.
- G2 → TC-DATAMIG001-01 + TC-FUNC004-01, kèm chạy lại kho TC-RM-432 / TC-RM-433 trên staging sau deploy (xem bảng đối chiếu §5).
- G3 → TC-DATAMIG001-01.
- G4 → TC-DATACOUNT001-01.
- G5 → TC-FUNCDATE001-01 (staging) + **chạy lại NEW-9, NEW-25, NEW-26 trên staging** — không viết TC mới cho phần 00:00/23:59 đã có.
- G6 → **không đề xuất TC**: Leader chốt §6 #2 rồi `testcase_update` expected của NEW-22 thành giá trị cụ thể.

---

## 2. Thiếu so với quan điểm test

**Kết luận**: 15 quan điểm Trigger khớp task · 7 chưa cover đủ

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `PERF-LARGE-001` | Cao (export / phân tích) | GAP — 3 TC chứng minh hiệu năng (NEW-17, NEW-24, NEW-30) mang mã lạ `PERF-LATENCY-001` / `TOOL-KNOW-002` nên không tính cover. Nội dung cũng chỉ đo ở local 400 ngày × 6 vùng, không theo quy mô khách lớn nhất. Thiếu cả Normal / Abnormal / Boundary | `[BLOCKER]` |
| Q2 | `OUT-EXPORT-001` | Cao (nâng — export CSV) | RISK — chỉ có Normal (NEW-6, NEW-15); Abnormal duy nhất là NEW-22 (mã lạ); thiếu Boundary "export ở quy mô khách lớn nhất" (RULE-01) | `[MAJOR]` |
| Q3 | `DATA-MIG-001` | Cao | RISK — có Normal NEW-31 + Abnormal NEW-33 (local); thiếu Boundary: migration trên bảng cỡ production và ghi log tap trong lúc ALTER đang chạy (RULE-01) | `[MAJOR]` |
| Q4 | `FUNC-DATE-001` | Cao | RISK — 4 Boundary (NEW-9, NEW-25, NEW-26, NEW-28) chỉ phủ 00:00 / 23:59 và 31/12→01/01; thiếu cuối tháng 30/31 ngày và 29/02 năm nhuận (catalog DI-08) cho helper mới `startOfNextDay`; toàn bộ chạy local | `[MAJOR]` |
| Q5 | `FUNC-004` | Cao | RISK — biên số vùng: bộ Studio dùng tối đa 6 vùng; chưa có rich menu **20 vùng** (tối đa — kho MT-27) để kiểm câu gom `rich_item_id in (...)` và ghép cột 10/11, 19/20 | `[MAJOR]` |
| Q6 | `REG-SHARED-001` | Cao | RISK — Dev xác nhận màn thống kê rich menu **bản cũ** `Basic/UserController::ajaxInitDataDetail` còn nguyên pattern chậm, chưa sửa; 0 TC, chưa có ticket yokoten. `Input thiếu`: `spec-features/admin/rich-menu` không có route nào gọi `ajaxInitDataDetail` → không xác định được màn cũ còn lối vào từ UI không | `[MAJOR]` (nâng `[BLOCKER]` nếu màn cũ còn lối vào từ UI) |
| Q7 | `COMPAT-LEGACY-001` | Cao | RISK — dữ liệu tap đời cũ chỉ thử bằng NEW-34 (mã lạ `TOOL-OLDREC-001`, bản ghi seed ở local); chưa TC nào chạy trên lượt tap thật ghi từ trước migration. Nhánh màn cũ → xem Q6 | `[MAJOR]` |

- Q1 → TC-PERFLARGE001-01 / -02 / -03. Q2 → TC-PERFLARGE001-02 (Abnormal: tải đồng thời) + TC-PERFLARGE001-03 (Boundary: quy mô lớn nhất). Q3 → TC-DATAMIG001-01. Q4 → TC-FUNCDATE001-01. Q5 → TC-FUNC004-01. Q7 → TC-PERFLARGE001-01 (dữ liệu tap thật trên production, so trước/sau release).
- Q6 → **không đề xuất TC**: fix không chạm màn cũ nên không có hồi quy để test. Việc cần làm là Leader mở ticket yokoten cho `ajaxInitDataDetail` và xác nhận với Dev màn cũ còn lối vào không.
- **Đã loại khỏi phạm vi**:
  - `CONC-003`: FE không đổi, fix không đổi thứ tự/số request.
  - `DEPLOY-LIVE-001`: payload request không đổi; response chỉ thêm `data: []` ở nhánh 0 vùng (§6 #2).
  - `DATA-DB-001`: không có UPDATE/DELETE dữ liệu.
  - `DATA-BACKUP-001`: chỉ thêm chỉ mục, không thêm/đổi bảng hay cột.
  - `ENV-003`: không chạm media / domain / job / thanh toán; phần hiệu năng đã xử lý qua Q1 + §4 I1.
  - `PERM-002` / `PERM-003`: controller + phân quyền không đổi; NEW-20 đã có ca rich menu bot khác.

---

## 3. TC trùng lặp nội dung

| Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| DUP-1 | NEW-26 | NEW-25 → **gộp** (chuyển bước tải CSV của NEW-25 sang NEW-26 rồi xóa NEW-25) | `DUP-SUBSET` | `FUNC-DATE-001` × `Boundary` × gọi API thống kê khoảng F1→F3 × cùng bộ dữ liệu biên DS-C → cùng expected 1, 1, 1 / 合計 = 3. Lời gọi 1 của NEW-26 đã bao trọn NEW-25, chỉ thiếu bước CSV | `[MINOR]` |

- **Gate đã chạy**: bỏ NEW-25 → `FUNC-DATE-001` vẫn còn NEW-9, NEW-26, NEW-28; §1 G5 / §2 Q4 không đổi. **Có** — chỉ cần chuyển bước CSV sang NEW-26.
- NEW-9 (quan sát trên màn hình) và NEW-26 (gọi API) cùng dữ liệu nhưng khác tầng quan sát → **giữ cả 2**.
- `DUP-INFLATE`: không có.
- `DUP-CONFLICT` trong bộ TC: không có. Conflict **với kho** (NEW-3 vs TC-RM-430) → §4 I4 + §6 #1.
- Xóa thật do human thực hiện qua `testcase_delete` trên Studio.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[MAJOR]` | Toàn bộ 34 TC — nguồn | **RULE-08**: 34/34 pass chỉ ở env `local` (run #1567, DB local 176.777 dòng; run #1557 trước đó `error`). `dev` / `staging` / `prd` = 0 run. Đây là bug performance chỉ lộ trên dữ liệu production, Dev verify mức `lint`, chưa EXPLAIN dữ liệu thật → chưa kết luận được "hết chậm" | Chạy lại nhóm TC số liệu (NEW-1 → NEW-16, NEW-18 → NEW-28) trên staging sau deploy; chạy TC-PERFLARGE001-01..03 + TC-DATAMIG001-01 trên production |
| I2 | `[MAJOR]` | NEW-4, 5, 11, 13, 16, 17, 18, 22, 23, 24, 29, 30, 32, 34 (14/34) — nguồn | 10 mã quan điểm không có trong `checklist-lme.md` (`TOOL-*`, `RULE-TOOL-029`, `PERF-LATENCY-001`, `API-CONTRACT-001`, `UI-TABLE-001`; `UIC-09` là mã catalog, không phải quan điểm) → không tính cover, **gồm cả 3 TC duy nhất chứng minh hiệu năng** | `testcase_update` đổi mã: NEW-17 / 24 / 30 → `PERF-LARGE-001` · NEW-29 / 32 → `DATA-MIG-001` · NEW-34 → `COMPAT-LEGACY-001` · NEW-4 / 11 / 18 → `DATA-COUNT-001` · NEW-5 / 13 → `LIST-001` · NEW-16 → `PERM-003` · NEW-22 → `UI-003` · NEW-23 → `FUNC-003` |
| I3 | `[MAJOR]` | `03-dev-impact.md` — input | Auto-fill `2026-09-17 by /new-task` nhưng checkbox "Tester verify auto-fill chính xác" **chưa tick** → F/D/T có thể thiếu hoặc map sai | Tester đọc lại Journal #136535 trên Redmine, xác nhận 4 mục rồi tick |
| I4 | `[MAJOR]` | NEW-3 vs kho TC-RM-430 | **SPEC-CONFLICT** 全期間: NEW-3 expect "liệt kê liên tục mọi ngày, kể cả ngày 0 lượt" (chạy local pass: 11 ngày liên tục). TC-RM-430 expect "ngày không có tap **KHÔNG** xuất hiện trong list", khớp tooltip 「全期間表示の場合、1度もタップされていない日はリスト上には表示されません。」. Chính NEW-3 ghi chú lệch này có từ trước fix | Không tự chọn bên → §6 #1. Chốt xong → `testcase_update` NEW-3 hoặc sửa kho TC-RM-430 |
| I5 | `[MAJOR]` | NEW-23 | **Pass che lỗi**: kết quả chạy `per_page=0` → `success:false` + message PHP thô `Division by zero`; `per_page=abc` / `page=abc` → `A non-numeric value encountered`; `per_page=-5` → `last_page=-1`; `page=999` → `from=9981`. TC vẫn pass vì expected chỉ cấm "đường dẫn / SQL / stack trace", trái REQ-012 của chính Studio ("trả lỗi sạch có thông báo") | Chốt expected (§6 #3). Chạy NEW-23 trên commit cha `248c803460` (bản trước fix NEW-24 đã dùng) để phân loại **hồi quy** hay **tồn đọng** — đoạn tính trang nằm trong hàm bị sửa |
| I6 | `[MAJOR]` | NEW-20, NEW-22 | Expected không đo lường được: NEW-20 "ghi lại mã HTTP để đối chiếu" (thực tế HTTP 200 + `success:false`); NEW-22 "ghi lại cấu trúc để đối chiếu bản chưa fix" (thực tế thêm `data: []`) → pass không chứng minh hành vi đúng | Leader chốt §6 #2 + #3 rồi `testcase_update` expected thành giá trị cụ thể |
| I7 | `[MAJOR]` | NEW-1 → NEW-34 (bộ dữ liệu DS-A … DS-H) | Tiền đề chỉ mô tả giá trị lượt tap (thời điểm đúng 00:00:00, `bot_id` rỗng, cột `count` = 5…) mà không ghi cách tạo. Các giá trị này không tạo được bằng LINE app / màn hình → member không dựng lại được khi phải chạy staging (I1) | Đính kèm script / fixture seed (hoặc tên seeder runner Studio đã dùng) vào tiền đề của từng bộ DS |
| I8 | `[MINOR]` | Toàn bộ (`spec_ids`) | Traceability sai mã spec: Studio gắn `EP-21` + `SCR-RM-06` cho endpoint / màn thống kê, NEW-6 / NEW-15 gắn `BR-14`. Theo `spec-features/admin/rich-menu`: endpoint là **`EP-24`** (EP-21 = setting-display-history), `SCR-RCM-06` là Edit Step 4, Business Rules chỉ tới `BR-11` | Sửa `spec_ids` → `EP-24` (+ `EP-36` trang HTML `/basic/rich-menu/detail-click/{id}`) |
| I9 | `[MINOR]` | NEW-1, NEW-3, NEW-14 | TC đối chiếu số đếm gắn `FUNC-001`; không TC nào mang `DATA-COUNT-001` (BẮT BUỘC khi màn có số tổng hợp) → coverage chỉ suy được từ nội dung | Đổi mã sang `DATA-COUNT-001` |
| I10 | `[MINOR]` | NEW-13, NEW-14, NEW-16, NEW-18, NEW-19, NEW-26, NEW-32 | **RULE-02**: run #1567 không có artifact, chỉ có text `actual` | Bổ sung log response JSON làm evidence |

---

## 5. TCs đề xuất bổ sung (7)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa004-richmenu-リッチメニュー.md` — nhóm **Thống kê tap** (TC-RM-424 → TC-RM-443, 20 TC) + MT-21 / MT-22 / MT-26 / MT-27 |
| Vùng regression phát hiện từ kho | TC-RM-426 (1 · 2 · 3 · 20 vùng) · TC-RM-428 (tooltip 最終レイアウト設定日) · TC-RM-429 (calendar mặc định 6 ngày) · TC-RM-430 (全期間) · TC-RM-432 → TC-RM-439 (đếm **tap thật** theo loại action) · TC-RM-441 (CSV khớp màn hình). Bộ Studio chỉ dùng dữ liệu seed — **chưa TC nào tap thật từ LINE app** |
| Conflict expected vs kho | NEW-3 (bộ TC gốc) vs TC-RM-430 → `[MAJOR] SPEC-CONFLICT` §4 I4, §6 #1. TC đề xuất: **không** — TC-PERFLARGE001-03 và TC-DATACOUNT001-01 cố ý không assert ngày 0 lượt có hiện hay không |
| GAP dùng lại TC kho (không viết mới) | G2 → dùng lại **TC-RM-432** "Cột 合計 và cột theo ngày… hiển thị đúng số tap" + **TC-RM-433** "Đếm tap CÓ cộng… mỗi lần tap chỉ +1": chạy lại trên staging **sau khi** deploy `ai_small_40943` + migration, giữ nguyên expected. TC-FUNC004-01 chỉ thêm phần biên 20 vùng |
| Xác nhận chống trùng | Đã đối chiếu 34 TC ở BƯỚC 0 + 20 TC nhóm Thống kê tap của kho — **không TC đề xuất nào trùng** (bộ Studio: tối đa 6 vùng, không đổi layout, không có 29/02 / cuối tháng, không chạy staging / production) |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-PERFLARGE001-01 | UI | PERF-LARGE-001 | Thống kê tap | Normal | manual | product | Tải CSV 327 ngày của đúng ca chậm 36s trong ticket: sau release dưới 5 giây và file giống hệt bản trước release | - Production `step.lme.jp`, tài khoản được Leader cấp quyền **xem** bot chứa rich menu id `709823` (1 trong 3 bot 102830 / 28561 / 167302 của ticket) — chỉ thao tác đọc<br>- Rich menu `709823` còn tồn tại (đã bị xóa → thay bằng rich menu có lịch sử tap dài nhất của 3 bot trên, ghi rõ id)<br>- Chrome mở DevTools tab Network | 1. **TRƯỚC release**: mở `/basic/rich-menu/detail-click/709823`, chọn khoảng 2025.10.23 → 2026.09.14<br>2. Ghi cột Time của request `data-statistic` và dòng 合計<br>3. Bấm nút tải CSV, ghi Time của request `download_csv=1`, lưu file thành `A.csv`<br>4. **SAU release** (Dev/DevOps xác nhận migration thêm chỉ mục đã chạy xong): lặp lại bước 1–3, lưu file thành `B.csv`<br>5. So `A.csv` với `B.csv` từng dòng, từng ô<br>6. Hỏi Dev: Chatwork room 417532006 có cảnh báo chậm của `/ajax/rich-menu/data-statistic/709823` trong khung giờ chạy bước 4 không | Rich menu `709823` · khoảng 2025.10.23 → 2026.09.14 (327 ngày — đúng URL chậm 36s lúc 2026-09-14 15:58 VN) · ngày kết thúc đã qua nên số liệu không đổi giữa 2 lần đo | - Sau release: request tải CSV < 5 giây (dưới mức SLOWLV1), request xem bảng < 4 giây (dưới ngưỡng `NotifyChatworkRequestTimeSlow`); ghi con số cụ thể của cả 2 lần đo<br>- `B.csv` giống `A.csv` 100%: cùng số dòng ngày, cùng dòng 合計, cùng từng ô, cùng bảng mã Shift_JIS<br>- Dòng 合計 trên màn sau release = trước release<br>- Không có cảnh báo SLOWLV1 / VERYSLOW / SUPPERSLOW cho endpoint này trong khung giờ đo | | Lấp Q1 · G1 · Q7 (lượt tap thật ghi từ trước migration) · RULE-08 · Nếu đã release trước khi kịp lấy `A.csv` → bỏ bước 1–3, oracle thay bằng: tổng từng cột các dòng ngày trong `B.csv` = dòng 合計 · Đánh giá spec: Spec không ghi ngưỡng thời gian — 5s lấy theo mức SLOWLV1 của `notifySlowRequestCommon()` (file 01), Leader chốt · Evidence: `A.csv` + `B.csv` + screenshot Network (Time) trước/sau |
| TC-PERFLARGE001-02 | UI | PERF-LARGE-001 | Thống kê tap | Abnormal | manual | product | Nhiều request thống kê khoảng dài chạy đồng thời ở 2 tab (tải CSV + 全期間) vẫn trả đúng số, không request nào vượt 5 giây | - Production, đã release + chạy xong migration<br>- Tài khoản được cấp quyền xem bot chứa 2 rich menu `132647` và `132648` (user 66811 trong ticket) — chỉ thao tác đọc<br>- Mở sẵn 2 tab: tab 1 màn thống kê `132647`, tab 2 màn thống kê `132648`; DevTools Network mở ở cả 2 tab | 1. Tab 1 và tab 2: cùng chọn khoảng (hôm nay trừ 365 ngày) → hôm qua<br>2. Trong vòng 5 giây: bấm tải CSV ở tab 1 → chuyển ngay sang tab 2 bấm tải CSV → quay lại tab 1 bấm 全期間<br>3. Ghi Time của từng request `data-statistic` ở cả 2 tab<br>4. Sau khi mọi request xong: đóng tab 2, ở tab 1 lần lượt mở lại từng rich menu, chọn đúng khoảng 1 năm ở bước 1 và tải CSV riêng lẻ | 2 rich menu cùng bot · khoảng 1 năm · 3 request dài bắn gần như đồng thời (mô phỏng user 66811 gửi 4 request trong 1 phút lúc 09:29–09:30 VN) | - Không request nào lỗi (không 500, không màn loading treo); mỗi request < 5 giây, ghi con số<br>- CSV tải đồng thời của `132647` giống hệt CSV tải riêng lẻ của `132647` (so từng ô); tương tự với `132648`<br>- CSV của tab 1 chỉ chứa số của `132647`, của tab 2 chỉ chứa số của `132648` — không lẫn rich menu (số cột = số vùng của đúng rich menu đó)<br>- Bảng 全期間 ở tab 1 hiển thị xong, không lỗi | | Lấp Q1 · Q2 (Abnormal export — tải đồng thời) · RULE-08 · Đánh giá spec: Spec không ghi · Evidence: screenshot Network 2 tab (cột Time) + 4 file CSV |
| TC-PERFLARGE001-03 | UI | PERF-LARGE-001 | Thống kê tap | Boundary | manual | product | Rich menu quy mô lớn nhất (nhiều lượt tap nhất): 全期間 và tải CSV vẫn dưới 5 giây, CSV khớp màn hình | - Production, đã release + chạy xong migration<br>- Dev/DBA cung cấp rich menu có số bản ghi `detail_click_richmenu` lớn nhất toàn hệ thống (id + số lượt tap + số vùng + ngày tạo) và cấp quyền xem bot đó cho QA — chỉ thao tác đọc | 1. Mở màn thống kê của rich menu Dev cung cấp<br>2. Bấm 全期間, ghi Time của request ở Network, đếm tổng số dòng ngày trên mọi trang, ghi dòng 合計<br>3. Bấm tải CSV, ghi Time của request<br>4. Mở CSV bằng Shift_JIS: đếm số dòng ngày, đối chiếu dòng 合計 và 5 dòng ngày bất kỳ với màn hình | Rich menu lớn nhất theo số lượt tap (Dev tra) · khoảng 全期間 | - Request 全期間 và request tải CSV đều < 5 giây, ghi con số<br>- Số dòng ngày trong CSV = tổng số dòng ngày hiển thị trên mọi trang<br>- Dòng 合計 trong CSV = dòng 合計 trên màn; 5 dòng ngày đối chiếu khớp từng ô<br>- Số cột vùng trong CSV = số vùng của rich menu; tiếng Nhật không lỗi phông | | Lấp Q1 · Q2 (Boundary export — quy mô khách lớn nhất) · RULE-08 · Không assert ngày 0 lượt có hiện hay không (đang SPEC-CONFLICT §6 #1) · Đánh giá spec: Spec không ghi · Evidence: file CSV + screenshot Network + số liệu quy mô Dev cung cấp |
| TC-DATAMIG001-01 | Data | DATA-MIG-001 | Thống kê tap | Boundary | manual | product | Migration thêm chỉ mục trên bảng `detail_click_richmenu` cỡ production: không mất lượt tap phát sinh trong lúc chạy, màn thống kê vẫn mở được | - Lịch release production có bước chạy migration `2026_09_14_100000_add_statistic_index_to_detail_click_richmenu_table` (giờ thấp điểm, DevOps chạy)<br>- Bot nội bộ trên production có rich menu R_T đang hiển thị cho 1 tài khoản LINE test đã kết bạn; mỗi vùng có action エルメ (vùng chỉ có action 友だち / LINE URL scheme không được đếm — TC-RM-434)<br>- QA ghi sẵn số của dòng hôm nay và dòng 合計 của R_T | 1. DevOps báo lúc bắt đầu migration, QA ghi thời điểm<br>2. **Trong lúc migration đang chạy**: trên LINE app của tài khoản test, tap vùng 1 của R_T 5 lần, vùng 2 3 lần<br>3. Cũng trong lúc đó: mở màn thống kê R_T, chọn khoảng hôm nay → hôm nay<br>4. DevOps báo migration kết thúc, ghi thời gian chạy, xác nhận chỉ mục `detail_click_richmenu_statistic_index` đã tồn tại<br>5. F5 màn thống kê R_T, đọc dòng hôm nay và dòng 合計 | 5 tap vùng 1 + 3 tap vùng 2, bắn ra trong lúc ALTER TABLE đang chạy | - Mọi lần tap trên LINE app đều chạy action bình thường, không lỗi<br>- Bước 3: màn thống kê mở được, không lỗi 500 (cho phép chậm hơn bình thường)<br>- Migration kết thúc thành công; chỉ mục có đúng 3 cột theo thứ tự `bot_id`, `rich_item_id`, `time_click`; DevOps ghi được thời gian chạy<br>- Bước 5: dòng hôm nay vùng 1 tăng đúng +5, vùng 2 tăng đúng +3 so với số ghi ở tiền điều kiện; dòng 合計 tăng tương ứng — không mất lượt tap nào | | Lấp Q3 · G2 · G3 · RULE-08 · regression — dẫn từ TC-RM-433 (mỗi lần tap +1) · Đánh giá spec: Spec không ghi · Evidence: log thời gian migration (DevOps) + screenshot bảng trước/sau + video tap trên LINE app |
| TC-FUNC004-01 | UI | FUNC-004 | Thống kê tap | Boundary | manual | staging | Rich menu 20 vùng (tối đa): lượt tap thật vào vùng 1, 2, 10, 11, 19, 20 hiện đúng cột trên bảng và trong CSV | - Staging đã deploy nhánh `ai_small_40943` + chạy migration<br>- Bot A (plan có phí) có rich menu R20 chỉnh vùng thủ công 手動編集 đủ 20 vùng, mỗi vùng có action エルメ (VD gửi template), đang hiển thị cho tài khoản LINE test đã kết bạn<br>- Hôm nay R20 chưa có lượt tap nào | 1. Trên LINE app của tài khoản test: tap vùng 1 ×1, vùng 2 ×2, vùng 10 ×3, vùng 11 ×4, vùng 19 ×5, vùng 20 ×6<br>2. Mở màn thống kê R20, chọn khoảng hôm nay → hôm nay<br>3. Đọc dòng ngày hôm nay và dòng 合計 (đủ 20 cột)<br>4. Bấm tải CSV, mở bằng Shift_JIS, đọc dòng 合計 và dòng hôm nay | 20 vùng · tap 1 / 2 / 3 / 4 / 5 / 6 lần vào vùng 1 / 2 / 10 / 11 / 19 / 20 · 14 vùng còn lại 0 tap · tổng 21 lượt | - Bảng có đủ 20 cột số đánh số 1 → 20<br>- Dòng hôm nay: cột 1 = 1, 2 = 2, 10 = 3, 11 = 4, 19 = 5, 20 = 6; 14 cột còn lại hiển thị số 0 (không trống)<br>- Dòng 合計 giống hệt dòng hôm nay; tổng các cột = 21<br>- CSV: tiêu đề 年月日 + 20 cột; dòng 合計 và dòng hôm nay khớp từng ô với màn hình, không lệch cột 10/11 hay 19/20 | | Lấp Q5 · G2 (ghi log tap thật sau khi có chỉ mục) · regression — dẫn từ TC-RM-426 (20 vùng), TC-RM-432 (đếm tap thật) · Đánh giá spec: Spec ghi rõ (tối đa 20 vùng — kho MT-27) · Evidence: screenshot bảng 20 cột + file CSV |
| TC-FUNCDATE001-01 | UI | FUNC-DATE-001 | Thống kê tap | Boundary | manual | staging | Lọc ngày ở cuối tháng 30/31 ngày và 29/02 năm nhuận: lượt tap 00:00:00 ngày kế tiếp không bị cộng vào ngày kết thúc | - Staging đã deploy nhánh `ai_small_40943` + chạy migration (chạy ở staging, **không** local — timezone DB cấu hình giống production)<br>- Rich menu R_D thuộc bot A có 1 vùng, 最終レイアウト設定日 trước 2024.02.01 (ngày trước mốc này không hiển thị — TC-RM-428)<br>- Dev seed lượt tap cho vùng của R_D đúng bảng ở cột Dữ liệu nhập (lượt tap quá khứ không tạo được bằng LINE app) | 1. Mở màn thống kê R_D, chọn 2026.06.30 → 2026.06.30, đọc bảng<br>2. Chọn 2026.08.31 → 2026.08.31, đọc bảng<br>3. Chọn 2024.02.29 → 2024.02.29, đọc bảng<br>4. Chọn 2024.02.28 → 2024.03.01, đọc bảng rồi tải CSV | Mỗi mốc 1 lượt tap: 2026-06-30 23:59:59 · 2026-07-01 00:00:00 · 2026-08-31 23:59:59 · 2026-09-01 00:00:00 · 2024-02-28 23:59:59 · 2024-02-29 00:00:00 · 2024-02-29 23:59:59 · 2024-03-01 00:00:00 | - Bước 1: 1 dòng 2026.06.30 = 1; 合計 = 1<br>- Bước 2: 1 dòng 2026.08.31 = 1; 合計 = 1<br>- Bước 3: 1 dòng 2024.02.29 = 2; 合計 = 2<br>- Bước 4: 3 dòng 2024.02.28 = 1 · 2024.02.29 = 2 · 2024.03.01 = 1; 合計 = 4; CSV khớp từng ô | | Lấp Q4 · G5 (biên ngày chạy ở env có timezone DB giống production — NEW-9 / 25 / 26 / 28 mới chạy local) · Đánh giá spec: Spec không ghi · Evidence: screenshot 4 lần chọn + file CSV bước 4 |
| TC-DATACOUNT001-01 | UI | DATA-COUNT-001 | Thống kê tap | Normal | manual | staging | Rich menu đã đổi layout (có 最終レイアウト設定日) và nút 全期間: bảng, 合計, CSV giống hệt trước khi deploy nhánh fix | - Staging **đang chạy bản chưa có fix** — bước 1–3 làm trước khi deploy `ai_small_40943`<br>- Rich menu R_L thuộc bot A có lượt tap thật ở cả **trước** và **sau** 最終レイアウト設定日 (đã đổi layout sau khi có số liệu); ghi lại 最終レイアウト設定日 = L và ngày tạo = C<br>- Không ai tap R_L trong ngày test | 1. Bản cũ: mở màn thống kê R_L, chọn (L trừ 10 ngày) → (L cộng 10 ngày); chụp bảng mọi trang + dòng 合計; tải CSV lưu `L-old.csv`<br>2. Bấm 全期間; chụp danh sách ngày mọi trang + 合計; ghi khoảng ngày hiện ở ô chọn ngày; tải CSV lưu `ALL-old.csv`<br>3. Deploy `ai_small_40943` + chạy migration<br>4. Lặp lại bước 1–2, lưu `L-new.csv`, `ALL-new.csv` | R_L có tap trước L và sau L · khoảng L ± 10 ngày · 全期間 | - Danh sách ngày hiển thị (kể cả việc ngày trước L có hay không hiện) giống hệt giữa bản cũ và bản fix ở cả 2 cách chọn<br>- Dòng 合計 bản fix = bản cũ ở cả 2 cách chọn<br>- `L-new.csv` = `L-old.csv` và `ALL-new.csv` = `ALL-old.csv` từng ô<br>- Khoảng ngày ở ô chọn ngày sau khi bấm 全期間 giống nhau giữa 2 bản | | Lấp G4 · regression — dẫn từ TC-RM-428 (tooltip 最終レイアウト設定日), TC-RM-430 (全期間) · Oracle = hành vi bản trước fix vì spec không ghi cách chặn khoảng theo mốc layout; không tự kết luận ngày 0 lượt có hiện hay không (§6 #1) · Đánh giá spec: Spec không ghi · Evidence: 4 file CSV + screenshot bảng của 2 bản |

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | `spec-features/admin/rich-menu/feature-spec.md` — màn thống kê tap (chưa có mục riêng) | Chốt hành vi nút 全期間: ngày **không có lượt tap** có hiện trong bảng / CSV hay không | NEW-3 (bản hiện tại liệt kê mọi ngày, kể cả 0) vs kho TC-RM-430 + tooltip 「全期間表示の場合、1度もタップされていない日はリスト上には表示されません。」 | Leader / PM (Dev xác nhận code hiện tại) |
| 2 | `spec-features/admin/rich-menu/web/api-spec.md` — EP-24 Response | Rich menu **0 vùng**: mỗi phần tử `detailClick` nay có `{"day": ..., "data": []}` (bản cũ chỉ `{"day": ...}`). Chấp nhận thay đổi này và ghi vào spec, hay yêu cầu Dev giữ nguyên | NEW-22 (kết quả chạy so trước/sau fix) vs câu "giữ nguyên toàn bộ hình dạng dữ liệu trả về" trong journal #136535 | Dev / Leader |
| 3 | `spec-features/admin/rich-menu/web/api-spec.md` — EP-24 (chưa có mục lỗi) | Bổ sung hành vi lỗi: rich menu không thuộc bot đang chọn → HTTP 200 + `success:false` "Rich menu not found!!!" (không phải 404); `per_page` / `page` = 0, âm, không phải số → hiện trả message PHP thô và `last_page = -1` / `from = 9981`. Chốt expected, tách ticket nếu là lỗi tồn đọng | NEW-20, NEW-23 (kết quả chạy) vs REQ-012 của Studio ("trả lỗi sạch có thông báo") | Dev / Leader |
| 4 | `spec-features/admin/rich-menu/feature-spec.md` §2 + §6 | Màn thống kê tap chưa có mã SCR và mô tả: lối vào EP-36 `/basic/rich-menu/detail-click/{id}`, calendar mặc định 6 ngày (kho MT-22), mặc định server 16 ngày / 10 dòng (EP-24), mốc 最終レイアウト設定日, 全期間, tải CSV Shift_JIS. Thiếu mục này nên Studio tự đặt sai mã `SCR-RM-06` / `EP-21` (§4 I8) | Kho MT-22, TC-RM-424 → TC-RM-443 · api-spec EP-24 | Leader |
