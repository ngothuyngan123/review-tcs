<!-- sync-tcs: url=https://docs.google.com/spreadsheets/d/1DNLq5YQbHYR1bS0dr2AioraZQR8Pi0p_xBFUy9aCcEw/edit?gid=1423546594#gid=1423546594 | sheet=Improve chung from 2026.07 | anchor=TC No. -->
<!-- ⚠️ Tab "Improve chung from 2026.07" dùng format CANONICAL 16 cột (anchor = "TC No." ở cột A), KHÔNG phải format cũ 10 cột (anchor "Main Function"). scripts/push_tc_anchored.py chỉ ghi được 5 cột → KHÔNG dùng /sync-ai-tc, /sync-review-tc cho tab này. Sync bằng script ghi đủ 16 cột. -->
<!-- ⚠️ gid: 1423546594 = "Improve chung from 2026.07" (target). ĐỪNG nhầm với gid=96411881 = "Improve filter web" và gid=211424101 = "Test bug KH" — cả 2 là tab TC CŨ, read-only, chỉ tham chiếu. -->

# 04 — TC List (draft AI, chờ member verify)

> **Bản chất task**: fix **thuần tối ưu query** (rewrite tương đương), **không đổi nghiệp vụ**. Vì vậy trục TC chính là **kết quả lọc trước/sau fix phải GIỐNG HỆT** + **query không còn chạy quá 300s**, KHÔNG phải "tính năng mới chạy được".
>
> ⚠️ **Spec conflict / Input thiếu — cần Leader confirm trước khi finalize**:
> 1. **Mục 4.3 của `03-dev-impact.md` hẹp hơn tiền lệ**. Dev chỉ liệt kê 3 tính năng (Friend Filter/Segment, Broadcast, Friend List). Nhưng 2 tab TC cũ của **chính builder này** — "Improve filter web" (*tối ưu query filter*) và "improve filter ngày 13/11" (*sửa câu query count số user thỏa mãn filter*) — đều test **8 màn**: `Màn friend list` · `send all` · `scenario` · `auto reply` · `action schedule` · `csv` · `cross analysic` · `modal action`. → TC-REGSHARED001-01 phủ đủ 8 màn. **Cần Dev/Leader confirm 5 màn còn thiếu có gọi `advanceFilter`/`advanceFilterPost` không.**
> 2. **Dev chưa xác nhận phía job** có dùng chung builder không. Tab TC cũ "Test bug KH" có hẳn khối `Check filter job` (broadcast · scenario · csv · cross · callback); tab "improve filter ngày 13/11" lại ghi *"phía job không sửa gì nên không cần check"*. → cần Dev trả lời rõ cho task này.
> 3. **Biên cuối ngày `23:59:59` là giả định của Dev**, spec không ghi. Dev tự lập luận "`followed_at` là DATETIME giây nên `23:59:59` bao trọn ngày". → TC-FUNCDATE001-02 verify thẳng giả định này.
> 4. **Index composite `(bot_id, followed_at)` KHÔNG nằm trong patch** (dev chỉ khuyến nghị DBA). → TC-ENV003-03 verify perf đạt **khi chưa có index**.
> 5. Bộ lọc **`qr_code` / `conversion`** dùng **cùng kiểu subquery đếm** nhưng **KHÔNG được fix** (dev ghi yokoten) → TC-REGSHARED001-02.

## Thông tin

| Trường | Giá trị |
|---|---|
| Tester viết TCs | `<member điền>` |
| Ngày submit | `<member điền>` |
| Version TCs | `v1 (draft AI 2026-07-16)` |
| Link TC gốc (nếu có) | https://docs.google.com/spreadsheets/d/1DNLq5YQbHYR1bS0dr2AioraZQR8Pi0p_xBFUy9aCcEw/edit?gid=211424101 — tab tham chiếu: `Test bug KH`, `Bug filter OR`, `improve filter 8/10/2024`, `improve filter ngày 13/11`, `Improve filter web` (**read-only**) |

---

## Data seed dùng chung — `S1`

> Dựng 1 lần trên bot test, dùng lại cho hầu hết TC. Giá trị **biết trước kết quả** để đối chiếu phép tính tay (RULE-07 / DATA-COUNT-001).

Bot **A** (bot test), 2 tag: `VIP`, `Khách cũ`. 10 friend:

| Friend | `followed_at` (ngày kết bạn) | Tag | Ghi chú trạng thái |
|---|---|---|---|
| F01 | 2026-06-01 **00:00:00** | VIP | biên đầu ngày bắt đầu |
| F02 | 2026-06-01 12:00:00 | VIP, Khách cũ | có đủ 2 tag |
| F03 | 2026-06-15 23:59:59 | Khách cũ | không có VIP |
| F04 | 2026-06-30 **23:59:59** | VIP | biên cuối ngày kết thúc |
| F05 | 2026-07-01 00:00:00 | VIP | ngoài khoảng (sau) |
| F06 | 2026-05-31 23:59:59 | — | ngoài khoảng (trước), không tag |
| F07 | 2026-06-10 09:00:00 | — | trong khoảng, không tag |
| F08 | (lấy về qua **既存友だち取得**) | VIP | old friend — `followed_at` theo ngày import / có thể rỗng |
| F09 | 2026-06-05 10:00:00 | VIP | **đã block OA** |
| F10 | 2026-06-20 15:00:00 | VIP, Khách cũ | |

**Phép tính tay mốc — Filter `Q1`** = tag ĐK1 (`có 1 trong các tag`: VIP) **AND** ngày kết bạn `2026-06-01` → `2026-06-30`:
→ khớp **F01, F02, F04, F10** = **4 friend** (F05 sau khoảng · F06 trước khoảng · F03 không VIP · F07 không tag · F08 ngoài khoảng · F09 tùy quy tắc block — xem TC-DATACOUNT001-02).

Bot **B** (bot thứ 2, dùng cho TC cách ly): tạo tag **trùng tên** `VIP` + tối thiểu 2 friend trong cùng khoảng ngày `2026-06-01`→`2026-06-30`.

---

## TC List

> **16 cột canonical.** Bộ TC này là **delta** — chỉ chứa phần TC cũ CHƯA cover (xem §Reuse TC cũ bên dưới). Regression toàn bộ điều kiện lọc × 8 màn **đã có ở tab cũ**, member re-run, KHÔNG chép lại vào đây.

| TC No. | Mã quan điểm liên kết | Loại case | Tiêu đề test case | Điều kiện tiền đề | Các bước thực hiện | Dữ liệu test/input | Kết quả mong đợi | Kết quả thực thi | Evidence thực tế | Môi trường test | Người thực hiện | Ngày thực hiện | Số ticket bug | Trạng thái đánh giá spec | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-FUNC001-01 | FUNC-001 | Normal | advanceFilterPost: modal filter màn Broadcast — tag ĐK1 + ngày kết bạn ra đúng 4 friend | - Bot A đã dựng data seed `S1`<br>- Đã deploy branch `ai_fixbug_38294`<br>- Login owner bot A | 1. Vào màn **Gửi tin hàng loạt (send all)** → tạo broadcast mới<br>2. Mở modal filter<br>3. Chọn điều kiện tag **ĐK1 — user có 1 trong số các tag được chọn** = `VIP`<br>4. Thêm điều kiện **ngày kết bạn** = `2026-06-01` → `2026-06-30`<br>5. Bấm áp dụng → đọc số friend thỏa mãn<br>6. Mở danh sách friend thỏa mãn | Filter `Q1`<br>Phép tính tay: **4** (F01, F02, F04, F10) | - Số đếm hiển thị = **4**<br>- Danh sách liệt kê **đúng 4 friend**: F01, F02, F04, F10<br>- Không có F03/F05/F06/F07<br>- Modal không văng lỗi, không loading vô hạn | Chưa test | | STAGING | | | | Spec ghi rõ | Evidence: screenshot modal (số đếm) + screenshot danh sách 4 friend. Baseline đối chiếu: chạy cùng filter trên môi trường **chưa có patch** → phải ra cùng 4 friend |
| TC-FUNC001-02 | FUNC-001 | Abnormal | Tag đang là điều kiện lọc bị XÓA giữa chừng → EXISTS không được làm sập modal | - Bot A, data seed `S1`<br>- Đã tạo sẵn 1 broadcast có filter tag ĐK1 = `VIP` | 1. Tạo broadcast, set filter tag ĐK1 = `VIP` + ngày kết bạn `2026-06-01`→`2026-06-30`, lưu<br>2. Sang màn **Quản lý tag** → xóa tag `VIP`<br>3. Quay lại broadcast → mở lại modal filter<br>4. Đọc số đếm + mở danh sách | Tag `VIP` bị xóa khi đang được filter tham chiếu | - Modal mở được, **không văng exception / không sập màn**<br>- Số đếm + danh sách phản ánh trạng thái tag đã xóa **giống hệt hành vi trước fix**<br>- Không có friend lạ lọt vào danh sách | Chưa test | | STAGING | | | | Spec không ghi | **Cần hỏi Leader/Dev** (chưa hỏi tại thời điểm viết draft): hành vi đúng khi tag bị xóa là bỏ điều kiện hay trả 0? Evidence: screenshot modal sau khi xóa tag. Liên kết `DATA-REF-001`, Catalog C `MAP-TAG-05` |
| TC-FUNC001-03 | FUNC-001 | Boundary | Filter không khớp friend nào → NOT EXISTS trả 0, không màn trắng | - Bot A, data seed `S1` | 1. Mở modal filter màn Broadcast<br>2. Set tag ĐK1 = `VIP` + ngày kết bạn `2027-01-01` → `2027-01-31` (không friend nào)<br>3. Áp dụng → đọc số đếm<br>4. Mở danh sách friend thỏa mãn | Khoảng ngày không có friend<br>Phép tính tay: **0** | - Số đếm = **0**<br>- Danh sách hiện **trạng thái rỗng có thông báo**, không màn trắng, không loading vô hạn<br>- Nút gửi bị chặn hoặc cảnh báo theo spec | Chưa test | | STAGING | | | | Spec ghi rõ | Evidence: screenshot trạng thái rỗng. Liên kết `UI-003`, Catalog B `UIC-11` |
| TC-FUNC003-01 | FUNC-003 | Normal | API filter nhận ngày đúng `Y-m-d` → kết quả trùng khớp UI | - Bot A, data seed `S1`<br>- Có token/session owner bot A<br>- Công cụ gọi API (Postman/DevTools) | 1. Trên UI chạy filter `Q1`, ghi lại số đếm<br>2. Mở DevTools > Network, copy request filter của modal<br>3. Gọi lại đúng API đó bằng Postman với ngày dạng `2026-06-01` / `2026-06-30` | `from=2026-06-01`, `to=2026-06-30`, tag ĐK1=VIP | - API trả về **4** friend, đúng F01/F02/F04/F10<br>- Số từ API **khớp 100%** số hiển thị trên UI | Chưa test | | STAGING | | | | Spec ghi rõ | Evidence: response API + screenshot UI cùng bộ filter |
| TC-FUNC003-02 | FUNC-003 | Abnormal | Gọi thẳng API với ngày SAI định dạng → phá giả định "modal luôn gửi Y-m-d" của Dev | - Như TC-FUNC003-01 | 1. Gọi thẳng API filter, thay ngày bằng các giá trị sai định dạng, mỗi lần 1 giá trị:<br>  a. `2026/06/01` (dấu `/`)<br>  b. `01-06-2026` (d-m-Y)<br>  c. `2026-13-45` (ngày không tồn tại)<br>  d. `' OR 1=1 --`<br>2. Với mỗi lần: đọc HTTP status + body + kiểm tra log server | 4 giá trị ngày sai định dạng ở trên | - **Không** trả HTTP 500 / không lộ raw SQL error<br>- Trả lỗi validate rõ ràng (4xx) hoặc bỏ qua điều kiện ngày theo spec<br>- **Không** sinh chuỗi rác kiểu `>= ' 00:00:00'`<br>- Log server không ghi SQL syntax error<br>- Case (d): không có dấu hiệu SQL injection | Chưa test | | STAGING | | | | Spec không ghi | ⚠️ **Trọng tâm task**: Dev tự ghi giả định *"Giá trị ngày từ modal luôn dạng 'Y-m-d' nên nối ' 00:00:00'/' 23:59:59' an toàn"* — TC này verify giả định đó khi **không đi qua modal**. **Cần hỏi Dev** hành vi mong đợi. Evidence: response + log server từng case. Liên kết `SEC-002` |
| TC-FUNC003-03 | FUNC-003 | Boundary | API filter với ngày RỖNG → không sinh điều kiện `>= ' 00:00:00'` | - Như TC-FUNC003-01 | 1. Gọi API filter với tag ĐK1=`VIP`, `from=""`, `to=""`<br>2. Gọi API filter với `from="2026-06-01"`, `to=""` (chỉ 1 đầu)<br>3. So sánh kết quả với filter chỉ có tag (không điều kiện ngày) trên UI | `from=""`, `to=""`<br>rồi `from=2026-06-01`, `to=""` | - Case 1: kết quả = kết quả khi **chỉ lọc tag VIP** (6 friend: F01, F02, F04, F05, F08, F10 — trừ F09 nếu block bị loại)<br>- Case 2: chỉ áp biên dưới, không lỗi<br>- **Không** trả 500, không trả toàn bộ friend khi đáng lẽ phải lọc | Chưa test | | STAGING | | | | Spec không ghi | **Cần hỏi Dev**: chuỗi rỗng nối `' 00:00:00'` → `' 00:00:00'` có bị đưa vào WHERE không. Số friend mong đợi ở case 1 member tự đếm lại theo quy tắc block đã chốt ở TC-DATACOUNT001-02. Evidence: response 2 case |
| TC-FUNCDATE001-01 | FUNC-DATE-001 | Normal | Ngày kết bạn trong cùng tháng — sargable range cho kết quả giống DATE() | - Bot A, data seed `S1` | 1. Màn **Friend list** → mở filter<br>2. Set **chỉ** điều kiện ngày kết bạn = `2026-06-01` → `2026-06-30`<br>3. Áp dụng, đọc số đếm + danh sách | Khoảng ngày trong cùng tháng<br>Phép tính tay: **6** (F01, F02, F03, F04, F07, F10) + F09 tùy quy tắc block | - Số đếm khớp phép tính tay<br>- F05 (2026-07-01) và F06 (2026-05-31) **không** có trong danh sách | Chưa test | | STAGING | | | | Spec ghi rõ | Catalog A `DI-10` Normal. Evidence: screenshot số đếm + danh sách |
| TC-FUNCDATE001-02 | FUNC-DATE-001 | Boundary | ⭐ Biên đầu/cuối ngày: friend `00:00:00` và `23:59:59` phải NẰM TRONG khoảng | - Bot A, data seed `S1`<br>- **Bắt buộc** F01 = `2026-06-01 00:00:00` và F04 = `2026-06-30 23:59:59` (set chính xác tới giây) | 1. Filter ngày kết bạn = `2026-06-01` → `2026-06-30`<br>2. Đọc danh sách, xác nhận có F01 và F04<br>3. Đổi khoảng thành `2026-06-02` → `2026-06-29` → xác nhận F01, F04 **biến mất**<br>4. Lặp lại toàn bộ ở màn **send all** | F01 `00:00:00` (biên dưới)<br>F04 `23:59:59` (biên trên) | - Bước 2: **F01 VÀ F04 đều có** trong danh sách (rewrite `>= '2026-06-01 00:00:00'` và `<= '2026-06-30 23:59:59'` bao trọn 2 biên, đúng ngữ nghĩa `DATE()` cũ)<br>- Bước 3: F01, F04 bị loại<br>- Kết quả 2 màn giống nhau | Chưa test | | STAGING | | | | Spec không ghi | ⭐ **TC quan trọng nhất của rewrite ngày** — verify thẳng giả định Dev: *"followed_at là DATETIME giây nên '23:59:59' bao trọn ngày"*. **Cần hỏi Leader/Dev** xác nhận biên đóng 2 đầu. Evidence: screenshot danh sách 2 bước + giá trị `followed_at` tới giây của F01/F04 |
| TC-FUNCDATE001-03 | FUNC-DATE-001 | Boundary | Biên xuyên năm + năm nhuận + cuối tháng 30/31 ngày | - Bot A<br>- Tạo thêm friend: G01 `2025-12-31 23:59:59`, G02 `2026-01-01 00:00:00`, G03 `2024-02-29 12:00:00`, G04 `2026-04-30 23:59:59` | 1. Filter ngày kết bạn `2025-12-31` → `2026-01-01` → đọc danh sách<br>2. Filter `2024-02-01` → `2024-02-29` → đọc danh sách<br>3. Filter `2026-04-01` → `2026-04-30` → đọc danh sách | G01, G02, G03, G04 | - Bước 1: **có cả G01 và G02** (không sai lệch khi so sánh xuyên năm)<br>- Bước 2: **có G03** (29/02 năm nhuận không bị loại)<br>- Bước 3: **có G04** (ngày cuối tháng 30 ngày) | Chưa test | | STAGING | | | | Spec ghi rõ | Catalog A `DI-08` Boundary (so sánh ngày bằng chuỗi sẽ sai khi khác năm — rewrite chuyển sang so sánh chuỗi datetime nên phải kiểm). Evidence: screenshot 3 khoảng |
| TC-FUNCDATE001-04 | FUNC-DATE-001 | Abnormal | Ngày kết thúc TRƯỚC ngày bắt đầu → không exception | - Bot A, data seed `S1` | 1. Mở filter ngày kết bạn<br>2. Set từ `2026-06-30` → đến `2026-06-01` (đảo ngược)<br>3. Áp dụng | from > to | - UI chặn với thông báo rõ ràng **HOẶC** trả 0 friend<br>- **Không** exception 500, không treo spinner | Chưa test | | STAGING | | | | Spec không ghi | Catalog A `DI-10` Abnormal. **Cần hỏi Leader** hành vi mong đợi (chặn hay 0 kết quả). Evidence: screenshot |
| TC-FUNCDATE001-05 | FUNC-DATE-001 | Boundary | Nhánh "ngày kết bạn theo SỐ NGÀY" (không phải ngày cố định) — biên ngày thứ N | - Bot A<br>- Tạo friend H01 `followed_at` = **đúng 7 ngày trước** thời điểm test (tính tới giây), H02 = **8 ngày trước** | 1. Mở filter ngày kết bạn → chọn kiểu **theo số ngày** = `7 ngày gần đây`<br>2. Áp dụng, đọc danh sách<br>3. Lặp lại vào **23:50** và **00:10** (2 mốc gần ranh giới ngày) | H01 = N-7, H02 = N-8 | - **H01 có** trong danh sách, **H02 không**<br>- Kết quả **ổn định** ở cả 2 mốc giờ chạy<br>- Không lệch 1 ngày do timezone (JST) | Chưa test | | STAGING | | | | Spec không ghi | Dev ghi rõ rewrite áp cho **cả** "ngày cố định + theo số ngày" nhưng mục 2 không mô tả cách tính biên nhánh số ngày → **cần hỏi Dev**. Evidence: screenshot 2 mốc giờ + `followed_at` của H01/H02 |
| TC-DATACOUNT001-01 | DATA-COUNT-001 | Normal | Số đếm khớp 4 nguồn: modal / danh sách / CSV export / API | - Bot A, data seed `S1` | 1. Chạy filter `Q1` ở modal → ghi số đếm<br>2. Mở danh sách friend thỏa mãn → đếm số dòng<br>3. Màn **quản lý CSV** → export danh sách với cùng filter → đếm số dòng file<br>4. Gọi API filter với cùng tham số → đếm kết quả<br>5. Đối chiếu 4 số với phép tính tay | Filter `Q1`<br>Phép tính tay: **4** | - **Cả 4 nguồn = 4**, khớp phép tính tay<br>- Không nguồn nào lệch | Chưa test | | STAGING | | | | Spec ghi rõ | ⚠️ `DATA-COUNT-001` là **lỗi lặp nhiều nhất lịch sử bug** (12 ticket Closed) và fix này chạm **thẳng** vào query đếm. Evidence: bảng đối chiếu 4 nguồn + file CSV + phép tính tay |
| TC-DATACOUNT001-02 | DATA-COUNT-001 | Abnormal | Friend đã BLOCK trong tập lọc → quy tắc đếm không đổi sau fix | - Bot A, data seed `S1` (F09 đã block OA, có tag VIP, trong khoảng ngày) | 1. Chạy filter `Q1` trên môi trường **chưa có patch** → ghi số đếm + F09 có/không trong danh sách<br>2. Chạy đúng filter đó sau khi deploy patch<br>3. So sánh 2 kết quả | F09: blocked + VIP + 2026-06-05 | - Kết quả **sau fix giống hệt trước fix** về cả số đếm lẫn việc F09 có/không xuất hiện<br>- Nếu trước fix F09 bị loại → sau fix vẫn bị loại (và ngược lại) | Chưa test | | STAGING | | | | Spec không ghi | Đây là TC **so sánh trước/sau**, không khẳng định quy tắc block đúng/sai. **Cần hỏi Dev** quy tắc block hiện hành để chốt số mong đợi cho các TC khác. Evidence: screenshot số đếm 2 môi trường. `regression`. Liên kết `MSG-003` |
| TC-DATACOUNT001-03 | DATA-COUNT-001 | Boundary | Tag ĐK2/ĐK4 (`=N` / `!=N`) — nhánh KHÔNG được rewrite phải giữ nguyên | - Bot A, data seed `S1` | 1. Filter tag **ĐK2 — user gồm TẤT CẢ các tag được chọn** = `VIP` + `Khách cũ` → đọc số đếm<br>2. Filter tag **ĐK4 — loại trừ user có ALL tag được chọn** = `VIP` + `Khách cũ` → đọc số đếm<br>3. So sánh với kết quả chạy trên môi trường chưa có patch | ĐK2: phép tính tay = **2** (F02, F10)<br>ĐK4: các friend còn lại | - ĐK2 = **2** (F02, F10)<br>- ĐK4 = tập bù đúng<br>- **Giống hệt** kết quả trước fix (dev giữ nguyên loại đếm `=N` / `!=N`, không đổi sang EXISTS) | Chưa test | | STAGING | | | | Spec ghi rõ | Dev ghi rõ *"giữ nguyên loại 'đủ tất cả tag'(=N)/'thiếu tag'(!=N)"* → TC này chứng minh 2 nhánh đó **không bị đụng nhầm**. Evidence: screenshot 2 điều kiện, 2 môi trường. `regression` |
| TC-MSG001-01 | MSG-001 | Normal | Broadcast gửi ĐÚNG 4 friend theo filter tag ĐK1 + ngày → nhận thật trên LINE | - Bot A, data seed `S1`<br>- F01, F02, F04, F10 là **tài khoản LINE thật** (có iOS + Android trong nhóm này)<br>- F03, F05, F06, F07 cũng là tài khoản thật để verify KHÔNG nhận | 1. Màn **send all** → tạo broadcast nội dung `Test #38294`<br>2. Set filter `Q1`, xác nhận số đếm = 4<br>3. Gửi ngay<br>4. Kiểm tra **LINE app thật** của F01, F02, F04, F10<br>5. Kiểm tra LINE app của F03, F05, F06, F07 | Filter `Q1`<br>Phép tính tay: đúng **4** người nhận | - **Đúng 4 friend** F01/F02/F04/F10 nhận tin `Test #38294` trên **LINE app thật**<br>- F03, F05, F06, F07 **KHÔNG nhận** tin<br>- Màn lịch sử gửi hiển thị số đã gửi = 4, khớp số modal | Chưa test | | STAGING | | | | Spec ghi rõ | ⚠️ **Gửi nhầm đối tượng = lỗi nghiêm trọng nhất** — không được dừng ở số đếm màn admin (RULE-06). Evidence: screenshot LINE app thật của **cả** người nhận VÀ người không nhận (iOS + Android). Catalog C `MAP-SEND-01` |
| TC-MSG001-02 | MSG-001 | Abnormal | Tag ĐK3 (loại trừ — nhánh NOT EXISTS) → người bị loại KHÔNG nhận tin | - Như TC-MSG001-01 | 1. Tạo broadcast mới<br>2. Set filter tag **ĐK3 — loại trừ user có 1 trong số các tag được chọn** = `VIP`, + ngày kết bạn `2026-06-01`→`2026-06-30`<br>3. Ghi số đếm, đối chiếu phép tính tay<br>4. Gửi ngay<br>5. Kiểm tra LINE app của **F01/F02/F04/F10 (có VIP)** và **F03/F07 (không VIP)** | Tag ĐK3 = VIP + ngày `Q1`<br>Phép tính tay: **2** (F03, F07) | - Số đếm = **2**<br>- **Chỉ F03 và F07** nhận tin trên LINE app thật<br>- **F01, F02, F04, F10 KHÔNG nhận** — nhánh `NOT EXISTS` loại đúng người có tag | Chưa test | | STAGING | | | | Spec ghi rõ | ⭐ Nhánh `count=0 → NOT EXISTS` là **1 trong 2 thay đổi cốt lõi** của patch. Evidence: screenshot LINE app của cả 2 nhóm. `regression` |
| TC-MSG001-03 | MSG-001 | Boundary | Filter khớp đúng 1 friend → chỉ 1 người nhận, không rơi về toàn bộ | - Như TC-MSG001-01 | 1. Tạo broadcast<br>2. Set filter thu hẹp còn **đúng 1 friend**: tag ĐK1 = `Khách cũ` + ngày kết bạn `2026-06-15` → `2026-06-15`<br>3. Xác nhận số đếm = 1<br>4. Gửi ngay<br>5. Kiểm tra LINE app của F03 và của **ít nhất 3 friend khác** | Phép tính tay: **1** (F03) | - Số đếm = **1**<br>- **Chỉ F03** nhận tin trên LINE app thật<br>- Không friend nào khác nhận (không rơi về gửi toàn bộ) | Chưa test | | STAGING | | | | Spec ghi rõ | Khoảng ngày 1 ngày (from = to) — Catalog A `DI-10` Boundary. Evidence: screenshot LINE app F03 + 3 friend khác không nhận |
| TC-MSGUSER001-01 | MSG-USER-001 | Normal | Old friend (既存友だち取得) — `followed_at` đặc thù, lọc theo ngày phải giống trước fix | - Bot A, data seed `S1` (F08 lấy về qua **既存友だち取得**)<br>- Ghi lại giá trị `followed_at` thực tế của F08 | 1. Xác định `followed_at` của F08 (qua màn chi tiết friend)<br>2. Chạy filter ngày kết bạn **bao** mốc đó, trên môi trường chưa có patch → ghi F08 có/không<br>3. Deploy patch → chạy lại đúng filter<br>4. Chạy thêm filter **không bao** mốc đó, cả 2 môi trường | F08 = old friend | - Kết quả **sau fix giống hệt trước fix** ở cả 2 filter<br>- F08 không bị lọt vào/ra sai so với baseline | Chưa test | | STAGING | | | | Spec không ghi | Old friend là nhóm `followed_at` dễ khác thường nhất (ngày import ≠ ngày kết bạn thật). **Cần hỏi Dev** `followed_at` của old friend được set thế nào. Evidence: screenshot chi tiết F08 + danh sách 2 môi trường. `regression` |
| TC-MSGUSER001-02 | MSG-USER-001 | Abnormal | Friend có `followed_at` RỖNG/NULL → không lọt vào kết quả sai | - Bot A<br>- Có ít nhất 1 friend K01 với `followed_at` rỗng/NULL (nhờ Dev seed nếu UI không tạo được) | 1. Chạy filter ngày kết bạn khoảng rộng `2020-01-01` → `2030-12-31`, môi trường chưa có patch → ghi K01 có/không<br>2. Deploy patch → chạy lại<br>3. Chạy filter **chỉ tag** (không điều kiện ngày) → K01 phải xuất hiện nếu có tag | K01: `followed_at` = NULL | - Kết quả K01 ở bước 1 và 2 **giống hệt nhau**<br>- Không exception<br>- Bước 3: K01 xuất hiện bình thường khi không lọc ngày | Chưa test | | STAGING | | | | Spec không ghi | `DATE(NULL)` và `NULL >= '...'` đều trả NULL → về lý thuyết tương đương, TC này **chứng minh bằng dữ liệu**. **Input thiếu**: cần Dev confirm có tồn tại friend `followed_at` NULL trên production không. Evidence: screenshot 2 môi trường |
| TC-MSGUSER001-03 | MSG-USER-001 | Boundary | Friend block → unblock quanh biên ngày lọc → kết quả ổn định | - Bot A, data seed `S1` (F09 blocked, 2026-06-05) | 1. Chạy filter `Q1`, ghi kết quả (F09 có/không)<br>2. Cho F09 **unblock** OA<br>3. Chạy lại filter `Q1`<br>4. Cho F09 block lại → chạy lần 3 | F09: block → unblock → block | - `followed_at` của F09 **không đổi** qua các lần block/unblock<br>- Kết quả bước 3 khớp quy tắc block đã chốt ở TC-DATACOUNT001-02<br>- Không friend nào khác bị ảnh hưởng | Chưa test | | STAGING | | | | Spec không ghi | Catalog C `MAP-SEND-20`. Evidence: screenshot 3 lần chạy + màn chi tiết F09. `regression` |
| TC-BULK001-01 | BULK-001 | Normal | Lọc còn 4 friend → gắn tag hàng loạt → đúng 4 friend bị tác động | - Bot A, data seed `S1`<br>- Tạo tag mới `BULK-38294` chưa gán cho ai | 1. Màn **Friend list** → áp filter `Q1`, xác nhận số đếm = 4<br>2. Chọn tất cả kết quả lọc → thao tác **gắn tag hàng loạt** `BULK-38294`<br>3. Xóa filter, lọc lại theo tag `BULK-38294`<br>4. Đếm số friend có tag mới | Filter `Q1` → 4 friend<br>Phép tính tay: **4** bị tác động | - **Đúng 4 friend** (F01, F02, F04, F10) được gắn `BULK-38294`<br>- Số friend có tag `BULK-38294` = **4**, khớp 100% số đã lọc<br>- **Không** rơi về gắn tag cho toàn bộ friend của bot | Chưa test | | STAGING | | | | Spec ghi rõ | Evidence: số bản ghi THẬT SỰ bị tác động (đếm từ danh sách tag mới) khớp 100% số đã lọc. Catalog C `MAP-TAG-03` |
| TC-BULK001-02 | BULK-001 | Abnormal | Lọc ra 0 friend → thao tác hàng loạt KHÔNG được rơi về toàn bộ | - Bot A, data seed `S1`<br>- Tag `BULK-EMPTY` chưa gán cho ai | 1. Áp filter khớp **0 friend** (tag ĐK1 = `VIP` + ngày `2027-01-01`→`2027-01-31`)<br>2. Thử thao tác gắn tag hàng loạt `BULK-EMPTY`<br>3. Lọc lại theo tag `BULK-EMPTY` | Filter khớp 0 friend | - Thao tác bị **chặn** hoặc tác động **0 friend**<br>- Số friend có tag `BULK-EMPTY` = **0**<br>- ⚠️ **Tuyệt đối không** gắn tag cho toàn bộ friend (rủi ro `NOT EXISTS`/empty-set rơi về full table) | Chưa test | | STAGING | | | | Spec không ghi | **Cần hỏi Leader**: hành vi đúng là disable nút hay cho chạy với 0 tác động. Evidence: screenshot + số friend có tag sau thao tác |
| TC-BULK001-03 | BULK-001 | Boundary | Lọc bằng tag ĐK3 (NOT EXISTS) + thao tác hàng loạt → đúng tập bù | - Bot A, data seed `S1`<br>- Tag `BULK-EXCL` chưa gán cho ai | 1. Áp filter tag **ĐK3 — loại trừ** = `VIP` + ngày `2026-06-01`→`2026-06-30`, xác nhận số đếm = 2<br>2. Gắn tag hàng loạt `BULK-EXCL`<br>3. Lọc theo `BULK-EXCL`, đếm | Phép tính tay: **2** (F03, F07) | - Đúng **2 friend** (F03, F07) được gắn `BULK-EXCL`<br>- F01/F02/F04/F10 **không** bị gắn<br>- Số khớp 100% số đã lọc | Chưa test | | STAGING | | | | Spec ghi rõ | Kết hợp nhánh `NOT EXISTS` + bulk — vùng rủi ro cao nhất nếu rewrite sai. Evidence: danh sách 2 friend + xác nhận 4 friend kia không bị gắn |
| TC-PERM003-01 | PERM-003 | Normal | 2 bot có tag TRÙNG TÊN `VIP` → lọc ở bot A không đếm friend bot B | - Bot A (data seed `S1`) và **bot B**<br>- Bot B có tag trùng tên `VIP` + 2 friend kết bạn trong `2026-06-01`→`2026-06-30`<br>- Cùng 1 user login có quyền cả 2 bot | 1. Login bot A → chạy filter `Q1` → ghi số đếm + danh sách<br>2. **Change bot** sang bot B → chạy đúng filter `Q1` → ghi số đếm + danh sách<br>3. Quay lại bot A → chạy lại filter `Q1` | Bot A: 4 friend khớp<br>Bot B: 2 friend khớp | - Bot A = **4** (F01, F02, F04, F10), **không** có friend nào của bot B<br>- Bot B = **2**, không có friend bot A<br>- Bước 3: bot A vẫn ra đúng 4, không dính dữ liệu bot B | Chưa test | | STAGING | | | | Spec ghi rõ | Subquery tag đổi sang `EXISTS` với điều kiện tương quan `tag_line_user.line_user_id = bot_line_user.line_user_id` → TC này chứng minh scope theo bot **không bị nới lỏng**. Evidence: screenshot danh sách 2 bot + kết quả sau change bot. Catalog C `MAP-PERM-01` |
| TC-PERM003-02 | PERM-003 | Abnormal | Cùng 1 LINE user là friend của CẢ 2 bot → không bị đếm chéo | - Bot A và bot B<br>- **Cùng 1 tài khoản LINE thật** kết bạn với cả 2 bot<br>- Ở bot A: user này có tag `VIP`; ở bot B: **không** có tag | 1. Login bot B → filter tag ĐK1 = `VIP` + ngày bao ngày kết bạn của user đó<br>2. Đọc số đếm + danh sách<br>3. Login bot A → chạy filter tương tự | User X: friend của cả 2 bot, chỉ có tag VIP ở bot A | - Bot B: user X **KHÔNG** xuất hiện (tag VIP của bot A không được EXISTS bắt nhầm sang bot B)<br>- Bot A: user X **có** xuất hiện | Chưa test | | STAGING | | | | Spec không ghi | ⭐ Rủi ro đặc thù của rewrite `EXISTS`: nếu subquery thiếu scope bot, tag của bot khác có thể match. **Cần hỏi Dev** subquery tag có điều kiện `bot_id` không. Evidence: screenshot 2 bot |
| TC-PERM003-03 | PERM-003 | Boundary | Bot B rỗng (0 friend) → lọc ở bot B trả 0, không mượn dữ liệu bot A | - Bot A (data seed `S1`), bot **C** mới tạo, **0 friend** | 1. Login bot C → chạy filter `Q1`<br>2. Đọc số đếm + danh sách | Bot C: 0 friend | - Số đếm = **0**<br>- Danh sách rỗng, không hiện friend của bot A<br>- Không exception | Chưa test | | STAGING | | | | Spec ghi rõ | Boundary tập rỗng ở tầng bot. Evidence: screenshot bot C |
| TC-REGSHARED001-01 | REG-SHARED-001 | Normal | Smoke 8 màn dùng chung builder filter → số đếm khớp nhau | - Bot A, data seed `S1`<br>- ⚠️ Đã hỏi Dev xác nhận màn nào gọi `advanceFilter`/`advanceFilterPost` | Với **từng** màn dưới đây, áp cùng filter `Q1` rồi đọc số friend thỏa mãn:<br>1. `Màn friend list`<br>2. `send all` (broadcast)<br>3. `scenario`<br>4. `auto reply`<br>5. `action schedule`<br>6. `csv` (quản lý CSV)<br>7. `cross analysic`<br>8. `modal action` | Filter `Q1` áp cho 8 màn<br>Phép tính tay: **4** | - **Cả 8 màn** cho cùng số đếm = **4** và cùng danh sách F01/F02/F04/F10<br>- Không màn nào lỗi/loading vô hạn<br>- Màn `scenario` không hiện số friend (chỉ lưu filter vào DB) → verify filter lưu đúng, theo ghi chú tab TC cũ "Improve filter web" | Chưa test | | STAGING | | | | Spec không ghi | ⚠️ **Vượt ngoài mục 4.3 của Dev** (dev chỉ nêu 3 tính năng). Danh sách 8 màn lấy từ **tiền lệ tab TC cũ** "Improve filter web" + "improve filter ngày 13/11" — cùng builder, cùng loại fix. **Bắt buộc yêu cầu Dev cung cấp danh sách nơi ảnh hưởng** trước khi chạy. Evidence: screenshot số đếm từng màn (8 ảnh). `regression` |
| TC-REGSHARED001-02 | REG-SHARED-001 | Abnormal | Bộ lọc `qr_code` / `conversion` (CÙNG kiểu subquery đếm, KHÔNG được fix) không bị regression | - Bot A<br>- Có ≥1 QR code và ≥1 conversion đã có friend access | 1. Màn **Friend list** → filter theo **qr_code** (từng điều kiện) → đọc số đếm<br>2. Filter theo **conversion** (ĐK1, ĐK2) → đọc số đếm<br>3. Kết hợp `qr_code` + tag ĐK1 + ngày kết bạn trong 1 filter<br>4. So sánh toàn bộ với môi trường chưa có patch | Filter qr_code, conversion, và tổ hợp với tag+ngày | - Kết quả **giống hệt** trước fix ở mọi điều kiện<br>- Tổ hợp bước 3 (nhánh đã rewrite + nhánh chưa rewrite trong cùng query) chạy đúng, không lỗi SQL | Chưa test | | STAGING | | | | Spec ghi rõ | ⚠️ Dev ghi rõ: *"Các bộ lọc qr_code/conversion cùng kiểu subquery đếm còn tồn (ghi yokoten)"* → 2 nhánh cũ/mới **cùng tồn tại trong 1 câu query**, phải verify không xung đột. Evidence: screenshot 2 môi trường. `regression` |
| TC-REGSHARED001-03 | REG-SHARED-001 | Normal | Tổ hợp AND + OR (cả 2 nhánh đều được rewrite) cho kết quả giống trước fix | - Bot A, data seed `S1` | 1. Set filter **AND**: tag ĐK1 `VIP` + ngày kết bạn `2026-06-01`→`2026-06-30` → ghi số<br>2. Set filter **OR**: tag ĐK1 `VIP` **hoặc** ngày kết bạn `2026-06-01`→`2026-06-30` → ghi số<br>3. Set **kết hợp**: 1 đk AND + 1 đk OR (AND: ngày kết bạn / OR: tag ĐK1 + tag ĐK3)<br>4. So sánh cả 3 với môi trường chưa có patch | AND / OR / AND+OR | - Cả 3 tổ hợp cho kết quả **giống hệt** trước fix<br>- Số đếm khớp phép tính tay member tự lập cho từng tổ hợp | Chưa test | | STAGING | | | | Spec ghi rõ | Dev ghi rõ rewrite *"áp cho cả nhóm điều kiện AND lẫn OR"* → phải test **cả 2 nhánh**. Tab TC cũ "Bug filter OR" có sẵn bộ tổ hợp AND/OR để tham chiếu. Evidence: screenshot 3 tổ hợp × 2 môi trường. `regression`.<br>⚠️ **RULE-01 — lý do nhóm `REG-SHARED-001` không có Boundary**: quan điểm này là "rà **danh sách nơi** bị ảnh hưởng", **không có khái niệm biên** (biên dữ liệu đã cover ở `FUNC-DATE-001`, biên quy mô ở `PERF-LARGE-001`). Đổi lại thêm 1 Normal (TC-REGSHARED001-03) để phủ cả 2 nhánh AND/OR. |
| TC-PERFLARGE001-01 | PERF-LARGE-001 | Normal | ⭐ Tái hiện query gốc gây alert trên quy mô production → không còn process > 300s | - **Production**, sau khi deploy patch<br>- Bot có quy mô friend **lớn nhất thực tế** (hỏi PM/DBA số liệu; bug gốc phát sinh trên bot thật)<br>- Có quyền chạy query monitor trên DB | 1. Trên bot lớn nhất, mở modal filter ở màn **send all**<br>2. Set điều kiện **giống query gốc trong ticket**: tag ĐK1 (nhiều tag) + ngày kết bạn `2025-11-18` → `2026-06-18` (khoảng ~7 tháng)<br>3. Bấm áp dụng, **bấm giờ** từ lúc bấm tới lúc số đếm hiện<br>4. Trong lúc query chạy, chạy query monitor:<br>`SELECT COUNT(*) FROM information_schema.processlist WHERE TIME > 300 AND db is not null AND command <> "Sleep" AND USER <> 'xtrabackup_lme' AND USER <> 'dump-analytics'` | Tag ĐK1 nhiều tag + khoảng ngày 7 tháng, trên bot có số friend lớn nhất (ghi rõ số friend thực tế) | - Số đếm hiển thị trong **thời gian chấp nhận được** (ghi số giây đo được)<br>- Query monitor trả **`0`** (Expect gốc của ticket = 0, trước fix = 1)<br>- Không process nào của filter chạy > 300s | Chưa test | | **PRODUCTION** | | | | Spec ghi rõ | ⭐ **TC verify trực tiếp bug** — mô phỏng đúng hiện tượng trong `01-bug-task.md`. **RULE-08**: perf **không được kết luận từ staging** (DB staging nhỏ, không tái hiện). Evidence: kết quả query monitor (ảnh) + thời gian đo + số friend của bot test. Ghi rõ nguồn số liệu quy mô |
| TC-PERFLARGE001-02 | PERF-LARGE-001 | Abnormal | Nhiều user cùng chạy filter nặng đồng thời → không dồn process > 300s | - Như TC-PERFLARGE001-01 | 1. Cho **3 user** cùng lúc chạy filter nặng (tag ĐK1 nhiều tag + khoảng ngày dài) trên bot lớn<br>2. Trong lúc đó chạy query monitor liên tục<br>3. Ghi lại thời gian phản hồi từng user | 3 phiên đồng thời | - Query monitor vẫn trả **0** trong suốt quá trình<br>- Cả 3 user đều nhận kết quả, không timeout<br>- Không process nào > 300s | Chưa test | | **PRODUCTION** | | | | Spec không ghi | **Cần hỏi Leader** ngưỡng thời gian phản hồi chấp nhận được. Alert gốc là alert hệ thống → cần verify ở mức tải thật. Evidence: log query monitor theo thời gian + thời gian phản hồi 3 phiên |
| TC-PERFLARGE001-03 | PERF-LARGE-001 | Boundary | Khoảng ngày dài nhất + số tag nhiều nhất → vẫn không chạm 300s | - Như TC-PERFLARGE001-01 | 1. Set khoảng ngày kết bạn **rộng nhất có thể** (từ ngày bot có friend đầu tiên → hôm nay)<br>2. Chọn tag ĐK1 với **số tag nhiều nhất** mà UI cho phép<br>3. Áp dụng, bấm giờ<br>4. Chạy query monitor song song | Khoảng ngày max + số tag max | - Số đếm trả về, thời gian ghi nhận cụ thể<br>- Query monitor = **0**<br>- Không timeout / không lỗi 504 | Chưa test | | **PRODUCTION** | | | | Spec không ghi | **Cần hỏi Dev/PM** giới hạn số tag tối đa của điều kiện ĐK1 (nguồn limit). Evidence: thời gian đo + query monitor + số tag/khoảng ngày đã dùng |
| TC-ENV003-01 | ENV-003 | Normal | Sau deploy production: luồng lọc + 3 job độc lập chạy đúng | - **Production**, sau deploy<br>- Bot thật có friend thật | 1. Trên production, chạy filter `Q1`-tương đương (tag ĐK1 + ngày kết bạn) ở màn **send all** → xác nhận số đếm hợp lý<br>2. Gửi 1 broadcast nhỏ theo filter cho nhóm tài khoản test nội bộ<br>3. Theo dõi log của **cả 3 job** production: `callback` / `broadcast` / `scenario`<br>4. Xác nhận người nhận thật trên LINE app | Filter tag + ngày trên bot thật | - Số đếm khớp danh sách người nhận thực tế<br>- **Cả 3 job** chạy bình thường, log không có lỗi SQL<br>- Người nhận trên LINE app khớp 100% filter | Chưa test | | **PRODUCTION** | | | | Spec ghi rõ | Catalog D `ENV-JOB`: dev/staging chạy **1 job all**, production tách **3 job độc lập** → lỗi tranh chấp chỉ lộ trên production (RULE-08). ⚠️ Tránh tạo/xóa data thật — chỉ gửi cho tài khoản test nội bộ. Evidence: log 3 job + screenshot LINE app thật |
| TC-ENV003-02 | ENV-003 | Abnormal | Đối chiếu staging vs production cùng bộ filter → staging pass KHÔNG đủ kết luận | - Cùng bộ filter chạy được ở cả 2 môi trường<br>- Ghi lại số friend của bot ở mỗi môi trường | 1. Chạy filter tag ĐK1 + ngày kết bạn trên **staging** → ghi thời gian + số đếm<br>2. Chạy filter tương đương trên **production** (bot quy mô lớn) → ghi thời gian + số đếm<br>3. Lập bảng đối chiếu: số friend của bot / thời gian / số đếm | Cùng loại filter, 2 môi trường | - Bảng đối chiếu ghi rõ **chênh lệch quy mô dữ liệu** giữa 2 môi trường<br>- Kết luận perf **chỉ** dựa trên số liệu production<br>- Kết quả nghiệp vụ (số đếm) đúng ở **cả 2** | Chưa test | | **PRODUCTION** | | | | Spec ghi rõ | RULE-08 + Catalog D. TC này là **phiếu xác nhận** không kết luận perf từ staging. Evidence: bảng đối chiếu môi trường đã tick |
| TC-ENV003-03 | ENV-003 | Boundary | ⭐ Verify perf đạt KHI CHƯA có index composite `(bot_id, followed_at)` | - **Production**, sau deploy<br>- Xác nhận với DBA rằng index composite `(bot_id, followed_at)` trên `bot_line_user` **CHƯA được thêm** | 1. Hỏi DBA xác nhận trạng thái index hiện tại trên `bot_line_user`<br>2. Chạy filter nặng (tag ĐK1 + khoảng ngày dài) trên bot lớn nhất<br>3. Bấm giờ + chạy query monitor<br>4. Nếu perf **không đạt** → báo ngay Leader/DBA, đây là điều kiện tiên quyết bị thiếu | Trạng thái index: chưa có composite index | - Query monitor = **0** (không process > 300s) **ngay cả khi chưa có index composite**<br>- Nếu không đạt → patch **chưa đủ** để đóng ticket, cần DBA thêm index trước | Chưa test | | **PRODUCTION** | | | | Spec không ghi | ⭐ Dev ghi rõ: *"Khuyến nghị DBA thêm index composite (bot_id, followed_at)... **không nằm trong patch code này**"* → nếu perf chỉ đạt khi có index thì việc đóng ticket phụ thuộc 1 việc **chưa ai làm**. **Bắt buộc hỏi Leader/DBA**. Evidence: xác nhận trạng thái index từ DBA + kết quả query monitor |

### Chú thích cột

Giữ nguyên theo [templates/04-tc-list.template.md](../../templates/04-tc-list.template.md).

### Environment (note)

- **STAGING** (`staging.lme.jp`) — toàn bộ TC nghiệp vụ (kết quả lọc trước/sau fix).
- **PRODUCTION** (`step.lme.jp`) — bắt buộc với `PERF-LARGE-001` + `ENV-003` theo **RULE-08** (perf / job nền không được kết luận từ staging). Bug gốc là **alert monitor DB trên môi trường thật** → không tái hiện được ở staging do khác quy mô dữ liệu. ⚠️ Tránh tạo/xóa data thật.
- **Baseline trước fix**: nhiều TC yêu cầu chạy cùng filter trên môi trường **chưa có patch** để lấy số đối chiếu. Member thống nhất với Dev cách dựng baseline (môi trường chưa deploy / branch cũ) **trước khi** bắt đầu test.

---

## Reuse TC cũ — KHÔNG viết trùng

> TC cũ là **read-only**. AI không sửa, không override. Bảng dưới ghi nhận phần đã cover để member **re-run tab cũ** thay vì viết lại.

| Tab TC cũ | Nội dung đã cover | Quyết định |
|---|---|---|
| **Improve filter web** — *"Improve filter: tối ưu query filter => test lại filter các màn senario, message send all, action schedule, friend list, csv, cross analysic"* | Regression **toàn bộ điều kiện lọc × 8 màn** (`Màn friend list` · `send all` · `scenario` · `auto reply` · `action schedule` · `csv` · `cross analysic` · `modal action`): tag ĐK1–ĐK4, name, ngày add friend, scenario ĐK1–5, landing, conversion, status confirm, friend info (text/select/date/point), status chat, affiliater, new/old friend, nhiều đk AND, filter OR. Toàn bộ `OK`. | ⭐ **REUSE — tiền lệ trực tiếp nhất** (cùng builder, cùng loại fix "tối ưu query"). Member **re-run nguyên tab này** làm bộ regression. TC mới **không chép lại**. Danh sách 8 màn được dùng làm nguồn cho TC-REGSHARED001-01. |
| **improve filter ngày 13/11** — *"sửa câu query count số user thỏa mãn filter"* | Cùng bộ điều kiện lọc trên `Màn friend list` · `Màn auto_reply` · `send all` · `scenario` · `csv` · `action schedule`, kèm **expected result chi tiết từng điều kiện** (VD: *"filter ngày add friend: setting 25/10/2023 - 1/11/2023 → hiển thị user kết bạn ngày 25/10 <= x <= 1/11"*). | ⭐ **REUSE — expected result là NGUỒN SPEC tốt nhất hiện có** cho ngữ nghĩa filter ngày (biên đóng 2 đầu `<=`), vì spec chính thức không ghi. Member đối chiếu TC-FUNCDATE001-02 với dòng này. Ghi chú tab: *"phía job không sửa gì nên không cần check"* — **không áp dụng mặc định cho task này**, phải hỏi lại Dev. |
| **Bug filter OR** (#31527, #31928) | Bộ tổ hợp **AND × OR** đầy đủ (1đk AND+1đk OR, 2+1, 1+2, 2+2) trên `advanceFilterPost(Conversation.php)` — **đúng function mà task này sửa**. Ghi chú quan trọng: *"Filter dùng chung, chỉ cần test 1 màn"*. | **REUSE** cho tổ hợp AND/OR. TC-REGSHARED001-03 chỉ thêm chiều **đối chiếu trước/sau fix** mà tab cũ không có. ⚠️ Lưu ý: ghi chú *"filter dùng chung, chỉ cần test 1 màn"* **mâu thuẫn** với tiền lệ 2 tab improve (test đủ 8 màn) → **cần Leader chốt phạm vi**. |
| **improve filter 8/10/2024** | Filter dùng `LIKE %%` trên `line_user.view_name` / `phone_number` / `email` / `friend_information_value.value` / `name`; ký tự đặc biệt (dấu `'`); trên `Send all` · `Step message` · `Auto reply` · `Filter ở action`. | **SKIP — out of scope**. Patch #38294 **không chạm** nhánh `LIKE`, chỉ chạm điều kiện ngày + tag. Không sinh TC trùng. |
| **Test bug KH** (#30445) | Filter friend info **ngày sinh** / **type date** với 2 định dạng value (`2025-12-01` vs `2025/12/01`), phía **web** và phía **job** (broadcast, scenario, csv, cross, callback). | **SKIP phần friend-info-date** — patch chạm `bot_line_user.followed_at` (ngày kết bạn), **không** chạm `friend_information_value` (ngày sinh / info type date). Theo nguyên tắc **chỉ test layer bị chạm code**. ⚠️ Nhưng khối `Check filter job` của tab này là **bằng chứng filter có chạy phía job** → dùng để chất vấn Dev (xem cảnh báo #2 đầu file). |

**⚠️ TC cũ nghi không còn đúng sau fix**: **không có**. Patch là rewrite tương đương (không đổi nghiệp vụ) → toàn bộ expected result của TC cũ **vẫn giữ nguyên hiệu lực**, và chính vì vậy chúng là **baseline đối chiếu tốt nhất**. Nếu re-run tab cũ mà kết quả khác trước → đó là **bug regression của patch**, không phải TC cũ sai.

---

## Member tự check trước khi submit

### Coverage check
- [x] Đã đọc kỹ `01-bug-task.md`
- [ ] Đã đọc kỹ `02-spec-reference.md` — **file không tồn tại**; tham chiếu [templates/LME-SYSTEM-SPEC.md](../../templates/LME-SYSTEM-SPEC.md) + expected result ở tab TC cũ "improve filter ngày 13/11". Member nên bổ sung file 02.
- [x] Đã đọc kỹ `03-dev-impact.md`, hiểu 4 mục
- [x] **Mỗi impact** trong 4.1 / 4.2 / 4.3 có **ít nhất 1 TC** verify
- [x] Có **ít nhất 1 TC** verify trực tiếp bug fix — TC-PERFLARGE001-01 (tái hiện đúng query monitor + khoảng ngày trong ticket)
- [x] Có **ít nhất 1 TC** verify tính năng cũ không hỏng cho mỗi tính năng trong 4.3 (`regression` ở Ghi chú)
- [x] Có **ít nhất 1 Abnormal + 1 Boundary** cho mỗi data quan trọng trong 4.2
- [x] Mọi TC có `Mã quan điểm liên kết`, steps rõ ràng, `Kết quả mong đợi` đo lường được
- [x] `Tiêu đề test case` chứa **keyword** (`advanceFilterPost`, `EXISTS`/`NOT EXISTS`, `followed_at`, tên màn)
- [ ] **Member verify lại data seed `S1`** dựng được trên môi trường thật (đặc biệt `followed_at` set chính xác tới giây cho F01/F04)
- [ ] **Member thống nhất với Dev cách dựng baseline "trước fix"** để đối chiếu

### Base quan điểm test LME (2 tầng)

| Mã quan điểm | Ưu tiên | ◯/× | Catalog đã tra | TC cover | Lý do nếu × |
|---|---|---|---|---|---|
| FUNC-001 | Cao | ◯ | C (MAP-SEND-01) | TC-FUNC001-01/02/03 | |
| FUNC-003 | Cao (nâng — field ngày giờ) | ◯ | A (DI-08, DI-10) | TC-FUNC003-01/02/03 | |
| FUNC-DATE-001 | Cao | ◯ | A (DI-08, DI-09, DI-10) | TC-FUNCDATE001-01→05 | |
| DATA-COUNT-001 | Cao | ◯ | C (MAP-SEND-01, MAP-TAG-01) | TC-DATACOUNT001-01/02/03 | |
| MSG-001 | Cao | ◯ | C (MAP-SEND-01, MAP-TAG-05) | TC-MSG001-01/02/03 | |
| MSG-USER-001 | Cao | ◯ | C (MAP-SEND-20) | TC-MSGUSER001-01/02/03 | |
| BULK-001 | Cao | ◯ | C (MAP-TAG-03) | TC-BULK001-01/02/03 | |
| PERM-003 | Cao | ◯ | C (MAP-PERM-01/03) | TC-PERM003-01/02/03 | |
| REG-SHARED-001 | Cao | ◯ | D | TC-REGSHARED001-01/02/03 (2 Normal + 1 Abnormal, **thiếu Boundary** — lý do ghi ở Ghi chú TC-REGSHARED001-03 theo RULE-01) | |
| PERF-LARGE-001 | Cao (nâng — filter/gửi tin quy mô lớn) | ◯ | D | TC-PERFLARGE001-01/02/03 | |
| ENV-003 | Cao | ◯ | D (ENV-JOB), D2 | TC-ENV003-01/02/03 | |
| FUNC-002 | Cao | × | — | — | Modal filter **không lưu dữ liệu**, không có trường bắt buộc theo nghĩa form create/edit. Case "bỏ trống / chỉ nhập 1 đầu khoảng ngày" — chỗ rewrite nối chuỗi có thể vỡ — **đã cover** ở TC-FUNC003-03 + TC-FUNCDATE001-04. |
| FUNC-004 | Cao | × | — | — | Patch **không chạm** giới hạn số ký tự / số lượng / dung lượng nào. Biên duy nhất liên quan là **biên ngày** → cover ở `FUNC-DATE-001`; biên số tag → cover ở TC-PERFLARGE001-03. |
| DATA-DB-001 | Cao | × | — | — | Trigger là "chức năng có **UPDATE hoặc DELETE**". Patch **thuần SELECT** (mục 4.2 file 03: `followed_at`, `tag_line_user` đều **chỉ ĐỌC**, không ghi). Rủi ro `WHERE` scope 2 tài khoản → **đã cover** ở TC-PERM003-01/02. |
| DATA-001 | Cao | × | — | — | Patch **không cập nhật dữ liệu** nên không có "thay đổi cần phản ánh ở màn khác". Chiều "cùng 1 số liệu khớp giữa nhiều nguồn" → cover ở TC-DATACOUNT001-01 + TC-REGSHARED001-01. |
| DATA-REF-001 | Cao | × | — | — | Patch không thêm/đổi thao tác xóa/copy/rename/move. Case **tag bị xóa khi đang được filter tham chiếu** (`MAP-TAG-05`) → đã cover ở TC-FUNC001-02. |
| MSG-003 | Cao | × | — | — | Patch **không chạm** điều kiện block. Yêu cầu "số đếm/tính phí không tính nhầm khi có user block" → đã cover ở TC-DATACOUNT001-02 (đối chiếu trước/sau fix) + TC-MSGUSER001-03. |
| JOB-001 | Cao | × | — | — | Patch **không thêm/sửa job nền**, không gọi API bên thứ 3 theo lô → rate limit / retry / backoff không liên quan. Việc **job dùng chung builder** → cover ở TC-REGSHARED001-01 + TC-ENV003-01. ⚠️ **Cần Leader duyệt lý do này** — phụ thuộc câu trả lời của Dev cho cảnh báo #2 đầu file. |
| CONC-001 | Cao | × | — | — | Patch thuần SELECT, **không có hành động ghi** cần idempotent. Nhiều phiên cùng chạy filter → cover ở TC-PERFLARGE001-02. |
| MSG-002 | Cao | × | — | — | Patch không chạm lịch gửi / thời điểm gửi. |
| MSG-004 / OUT-PREVIEW-001 | Cao | × | — | — | Patch không đổi **nội dung** tin, chỉ đổi **đối tượng nhận**. Preview nội dung không bị ảnh hưởng. Đối tượng nhận → cover ở `MSG-001`. |
| MSG-005 / PAY-* | Cao | × | — | — | Patch không chạm quota tin, gói cước, tiền. |
| PERM-001 / PERM-002 | Cao | × | — | — | Patch không đổi phân quyền / không thêm endpoint. Cách ly dữ liệu đa bot → cover ở `PERM-003`. |
| SEC-001 | Cao | × | — | — | Patch không đổi cách chặn truy cập PII. Cross-account → cover ở TC-PERM003-02. Rò rỉ qua lỗi SQL → cover ở TC-FUNC003-02 (liên kết `SEC-002`). |
| STATE-* | Cao | × | — | — | Patch không có quy trình nhiều bước ghi dữ liệu, không chạm hủy hợp đồng / hành động phụ thuộc. |
| DATA-MIG-001 / DATA-BACKUP-001 | Cao | × | — | — | Patch **không đổi cấu trúc dữ liệu**, không thêm/đổi bảng DB, không migration. |
| DATA-AUDIT-001 | Cao | × | — | — | Patch không có thao tác xóa/sửa/đổi trạng thái → không sinh bản ghi lịch sử. |
| INTG-* | Cao | × | — | — | Patch không gọi LINE API / webhook / Google Calendar / spreadsheet. |
| COMPAT-LEGACY-001 | Cao | × | — | — | Patch không chạm template / form / remind / richmenu / google spread. Nhánh **landing cũ/mới** của filter (thấy ở tab "Bug filter OR") **không bị rewrite**. Dữ liệu đời cũ liên quan duy nhất là **old friend qua 既存友だち取得** → đã cover ở TC-MSGUSER001-01. |
| DEPLOY-ASSET-001 / DEPLOY-LIVE-001 | Cao | × | — | — | Patch **chỉ sửa 1 file PHP backend** (`app/Conversation.php`), không đổi JS/CSS/font/icon, không đổi payload API / cấu trúc request. |
| REG-RUN-001 | Cao | × | — | — | Patch không đổi cấu trúc dữ liệu/job → broadcast đang chạy dở khi release không bị ảnh hưởng logic. ⚠️ Phụ thuộc câu trả lời của Dev cho cảnh báo #2 — nếu job **có** dùng builder, cần nâng lên ◯. |
| REG-SPEC-001 | Cao | × | — | — | Spec không thay đổi (fix thuần kỹ thuật, giữ nguyên ngữ nghĩa). |
| REG-URL-001 | TB→Cao | × | — | — | Patch không chạm màn đăng ký / login / overview / thanh toán / kết nối bot. |
| SEC-ISO-001 | Cao | × | — | — | Patch không đổi cách UI tải nhiều đối tượng đồng thời. |
| ENV-001 / ENV-002 | Cao/TB | × | — | — | Patch không đổi cấu hình hạ tầng, không thêm server, không có bước validate qua nhiều server. |
| MEDIA-* | TB→Cao | × | — | — | Patch không chạm media/file. |
| FRIEND-001 | Cao | × | — | — | Patch chạm `bot_line_user.followed_at` (ngày kết bạn) + `tag_line_user`, **không** chạm `friend_information_value`. Filter friend info (text/select/date/point) không nằm trong rewrite → TC cũ tab "improve filter 8/10/2024" + "Test bug KH" đã cover và vẫn hiệu lực. |
| LIFF-ENTRY-001 / OUT-EXPORT-001 / NOTI-MAIL-001 | Cao/TB | × | — | — | Patch không sinh URL cho LINE user, không đổi nội dung/format file export (chỉ đổi **tập bản ghi** được export → cover ở TC-DATACOUNT001-01), không gửi mail. |
| UI-* / LIST-001 / CONC-003 / FUNC-SEQ-001 / FUNC-MULTI-001 / FUNC-DRAFT-001 / FUNC-UNIQ-001 / DATA-TEXT-001 / DATA-ID-001 / DATA-CACHE-001 / SYNC-APP-001 / PERM-004 / CONC-002 | TB/Thấp | × | — | — | Patch **chỉ đổi câu SQL ở backend**, không đổi UI/UX, không đổi thành phần màn hình, không đổi thứ tự response, không chạm cache/app. Trạng thái rỗng của danh sách → cover ở TC-FUNC001-03. |

- [x] Đã duyệt **toàn bộ** tầng 1 từ trên xuống, không bỏ nhóm nào
- [x] Mỗi quan điểm ◯ **đã mở đúng catalog** ở tầng 2 và duyệt hết khối tương ứng
- [x] Mọi quan điểm ◯ ưu tiên **Cao** có đủ 3 loại case Normal + Abnormal + Boundary (**RULE-01**)
- [x] Mọi × đều có lý do; × ở quan điểm **Cao** đã đưa vào cảnh báo cho Leader duyệt (**RULE-03**) — ⚠️ đặc biệt `JOB-001` và `REG-RUN-001`
- [x] TC có output ra ngoài → `Kết quả mong đợi` đi tới **LINE app thật** (**RULE-06**) — TC-MSG001-01/02/03, TC-ENV003-01
- [x] TC CRUD verify 3 tầng — **không áp dụng** (patch thuần SELECT); TC lọc verify **3 nguồn** modal / danh sách / CSV export + API (TC-DATACOUNT001-01)
- [x] Task chạm **job nền / quy mô production** → có TC ghi `Môi trường test = PRODUCTION` (**RULE-08**) — TC-PERFLARGE001-01/02/03, TC-ENV003-01/02/03
- [x] Task chạm đối tượng đã từng version-up → old friend (既存友だち取得) test ở TC-MSGUSER001-01 (**RULE-09**)
- [x] Mỗi TC đã ghi **loại evidence bắt buộc** ở Ghi chú (**RULE-02**)
- [x] Mọi TC có `Trạng thái đánh giá spec`; case `Spec không ghi` đã ghi rõ **cần hỏi ai**
- [ ] ⚠️ **Chưa ai được hỏi thực tế** — mọi ghi chú "Cần hỏi Leader/Dev" trong draft này là **việc member phải làm trước khi test**, không phải đã hoàn thành

<!-- Draft sinh bởi /write-tc lúc 2026-07-16. Old TCs fetch từ Sheet 1DNLq5YQbHYR1bS0dr2AioraZQR8Pi0p_xBFUy9aCcEw (5 tab), read-only, KHÔNG sửa. -->
