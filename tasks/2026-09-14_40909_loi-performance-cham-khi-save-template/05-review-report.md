# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | MCP LME TEST STUDIO — task `#300` (ticket 40909, round 1, branch `ai_small_40909`) |
| Tổng số TC review | `33` (bộ gốc Studio) — sau review đã push thêm **16 TC** của §5 lên chính task này, tổng hiện tại `49` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code


> ⚠️ **CẬP NHẬT 2026-09-15 — kết quả thực thi đã có, đọc trước khi dùng report này.**
> Task `#300` đã chuyển `done`, `aiResult = fail`. Run `#1423` (env `local`, 2026-09-14 10:10→12:41): **30 pass · 2 fail · 1 chưa chạy**.
> **2 bug đã raise trên Redmine**: **#40968** `[High]` (bug gốc chưa hết — xem `I1`) và **#40967** `[Medium]` (rollback migration — xem `I3`).
> Một run mới `#1443` (env `local`) đang chạy tại thời điểm ghi report.
> 16 TC ở §5 đã được push lên task này (`NEW-34`…`NEW-49`) và **chưa chạy**.

**Kết luận**: `15/25 vùng ảnh hưởng đủ TC · 8 GAP · 2 RISK`

> Chiều (b) dùng diff thật từ Studio (`diffAvailable = true`, 5 file, +259/−49, `dev_impact` 7 điểm). Studio chỉ trả **diffstat**, không có nội dung diff.

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | **`url_id` đổi khi URL bị xoá khỏi bảng `url`** — dedupe mới `where(template_id, bot_id, url_id)` sẽ **trượt** và tạo bản ghi mới, trong khi logic CŨ (so chuỗi `url`) lại khớp và update đúng | `diff code` | `không có` | `GAP — 0 TC. Kho FA-010 (TC-TMT-167 / 168 / 173) ghi rõ: URL bị xoá → save lại thì hệ thống tạo url mới và ĐỔI url_id. Fix có thể gây REGRESSION ngược: case trước đây update đúng nay lại nhân dòng` | `[BLOCKER]` |
| G2 | **Null-safe `?? ''` cho metadata title/description** — nhánh site ngoài lỗi / timeout khi fetch OGP lần đầu (timeout 10s + 60s vẫn giữ nguyên) | `diff code` | `không có` | `GAP — 0 TC. dev_impact Studio nêu đích danh "kiểm case URL không lấy được metadata (site ngoài lỗi/timeout) không gây warning/lưu sai". NEW-11/12/13 đều giả định site đích trả OGP thành công` | `[BLOCKER]` |
| G3 | **Migration ALTER khoá bảng khi đang có request lưu template** | `diff code` | `NEW-30 (chạy migration trên DB tĩnh)` | `GAP — dev_impact Studio nêu "khoá bảng lúc chạy trên bảng lớn → rủi ro với request đang lưu đồng thời". Không TC nào chạy migration trong lúc có traffic lưu template` | `[BLOCKER]` |
| G4 | `F7 — detectUrlInMessageTextV2` (Dev rà soát, **không sửa**, còn so chuỗi URL) | `dev-impact` | `không có` | `GAP — 0 TC. Kho FA-010 TC-TMT-174 cho thấy hàm này chi phối preview URL ở chat 1:1 + action text. URL dài > 255 vẫn đi qua đường so chuỗi ở đây` | `[MAJOR]` |
| G5 | **Caller chưa được kê** — `MessageTemplateController::saveUrlDetect` (`:784-861`) / `saveUrlMetaData` (`:743-783`) của template **V1 legacy**, và luồng **copy / backup template** ghi `template_url_redirect` | `diff code` | `không có` | `GAP — mục 3 file 03 kê 9 function nhưng thiếu 2 nơi trên. logic-spec.md:409 ghi rõ 2 hàm V1; kho TC-TMT-170 xác nhận copy/backup có ghi bản ghi url. Cần Dev xác nhận 2 nơi này có cùng pattern dedupe không` | `[MAJOR]` |
| G6 | **Job `recover:templateUrlRedirect`** — mục 5 file 03 yêu cầu chạy lại để điền cột `url` đầy đủ cho bản ghi cũ bị cắt | `dev-impact` | `không có` | `GAP — 0 TC. Kho FA-010 TC-TMT-165 đã có case cho job này (URL còn tồn tại → điền url; URL đã xoá → url = NULL); sau fix hành vi có thể đổi` | `[MAJOR]` |
| G7 | `T4 — Broadcast (FA-008)` + output cuối trên LINE | `dev-impact` | `không có` | `GAP — 0 TC gửi tin ra LINE app thật. Toàn bộ 33 TC dừng ở màn admin / DB / endpoint. RULE-06 chưa được thoả cho một fix chạm trực tiếp link rút gọn và preview URL mà user LINE nhìn thấy` | `[MAJOR]` |
| G8 | `T3 — URL Analytics (FA-023)`, màn 「URL分析」 `/basic/url` | `dev-impact` | `NEW-28 (link rút gọn còn sống)` | `RISK — NEW-28 chỉ mở link trên trình duyệt và nói "số liệu click không mất", không mở màn thống kê đối chiếu số liệu trước/sau khi command xoá bản ghi` | `[MAJOR]` |
| G9 | **Command DELETE chạy đồng thời với request lưu template** | `diff code` | `NEW-26 / NEW-29 (chạy command trên DB tĩnh)` | `RISK — command chạy trên production trong giờ vận hành; nếu một lần lưu chen vào giữa lúc gom nhóm → có thể xoá mất bản ghi vừa tạo. Không TC nào dựng tình huống này` | `[MAJOR]` |
| G10 | **Luồng lưu template của 「送信予約」 (đặt lịch gửi tin từ chat 1:1)** — cùng endpoint `/ajax/template-v2/save-template` nhưng `action_type = schedule_send` → router `saveTemplateByType()` rẽ sang **`saveScheduleSendChat`**, nằm trong chính `TemplateV2Service.php` (1 trong 5 file của diff) | `diff code` | `không có` | `GAP — 0 TC trên cả web lẫn mobile. Bằng chứng luồng tồn tại: api-spec EP-30 khai action_type enum có schedule_send · logic-spec:197 saveTemplateByType là router gọi saveScheduleSendChat · logic-spec:115 CATEGORY_SCHEDULE_SEND_CHAT_TEXT = -1111 (template loại TEXT riêng cho schedule send chat) · get_template_detail live trả scheduleSendId + conversationId trong cùng payload data với message_text.urlSettings · FA-001 dùng SC-001 Template + SC-005 Editor + SC-007 Schedule. Template text của luồng này CÓ THỂ chứa URL → đi qua cùng vùng template_url_redirect. ⚠️ Chưa xác định được saveScheduleSendChat có gọi lại saveText (đã fix) hay là BẢN SAO DEDUPE THỨ 3 chưa được sửa — Studio chỉ có diffstat, không đọc được nội dung diff` | `[BLOCKER]` nếu là bản sao thứ 3 · `[MAJOR]` nếu dùng chung `saveText` — **cần Dev xác nhận trước** |

**Ghi chú xuyên suốt §1**: **0/33 TC đã chạy** (`untested = 33`, `ranAt = null`) → mọi vùng ghi "đủ TC" ở trên chỉ là **đủ trên giấy**, chưa có kết luận test nào. Xem `I1` §4.

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `26 quan điểm Trigger khớp task · 14 chưa cover đủ`

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `FUNC-003` | `Cao` (field liên quan **URL** → nâng Cao) | `GAP — 0 TC test biến thể định dạng URL: %-encode, IDN/ký tự Nhật, thiếu scheme, khác trailing slash, khác thứ tự query param. Dedupe mới dựa vào url_id, mà url_id lại tra bảng url theo CHUỖI → 2 biến thể cùng một trang vẫn sinh 2 url_id. Đây chính là cơ chế đã gây bug, chỉ đổi chỗ` | `[BLOCKER]` |
| Q2 | `ENV-003` ★ | `Cao` (BẮT BUỘC — chạm **URL/domain** + **job nền**) | `GAP — 0 TC chạy production (prd runs = 0). NEW-32 chốt sức chứa cột url.url khai env=all nhưng bảng url dùng connection riêng mysql_url, cấu trúc có thể KHÁC giữa các môi trường → kết quả trên staging không kết luận được cho production. RULE-08` | `[BLOCKER]` |
| Q3 | `DEPLOY-LIVE-001` ★ | `Cao` (BẮT BUỘC — release production không maintain) | `GAP — 0 TC. Migration ALTER + command DELETE đều chạy trên hệ thống đang sống. Xem G3 / G9` | `[BLOCKER]` |
| Q4 | `MSG-004` | `Cao` | `GAP — 0 TC verify preview URL + link rút gọn trên LINE app thật (Evidence bắt buộc: screenshot LINE iOS VÀ Android). Xem G7` | `[BLOCKER]` |
| Q5 | `OUT-TRUTH-001` | `Cao` | `GAP — 0 TC cho nhánh site đích lỗi/timeout: UI báo lưu thành công hay thất bại, và trạng thái thật trong DB có khớp không. Xem G2` | `[BLOCKER]` |
| Q6 | `DATA-BACKUP-001` ★ | `Cao` (BẮT BUỘC — tính năng **đổi cấu trúc bảng DB**) | `GAP — 0 TC verify có backup bảng template_url_redirect trước khi chạy ALTER và trước khi chạy command DELETE, và khôi phục được. NEW-33 chỉ ghi cảnh báo trong note, không phải TC backup/restore` | `[BLOCKER]` |
| Q7 | `DATA-REF-001` | `Cao` | `RISK — NEW-26/27/28 kiểm bản ghi bị xoá đúng nhóm và link rút gọn còn sống, nhưng chưa kiểm TỪNG NƠI THAM CHIẾU: t_actions trỏ bởi action_id / out_time_action_id của các dòng bị xoá có thành mồ côi không, url_shorten còn trỏ đúng bản ghi giữ lại không` | `[MAJOR]` |
| Q8 | `REG-SHARED-001` | `Cao` | `RISK — Evidence yêu cầu "danh sách nơi ảnh hưởng do Dev cung cấp + kết quả test từng nơi". Danh sách hiện thiếu template V1 legacy + copy/backup template. Xem G5` | `[MAJOR]` |
| Q9 | `COMPAT-LEGACY-001` ★ | `Cao` (BẮT BUỘC — chạm **template**) | `RISK — có nhánh dữ liệu cũ (NEW-6 url NULL, NEW-24 url bị cắt) nhưng thiếu các cặp bắt buộc khác: template KHÔNG group ⇄ CÓ group template, và bản ghi tạo TRƯỚC migration → mở → sửa → lưu lại ở luồng copy template` | `[MAJOR]` |
| Q10 | `JOB-001` ★ | `Cao` (BẮT BUỘC — thêm job/command nền) | `RISK — 5 TC command đều khai env dev/local/staging trong khi mục đích của command là chạy trên production. Thiếu hẳn TC cho job recover:templateUrlRedirect (G6). Evidence yêu cầu "bảng đối chiếu số bản ghi vào/ra" — NEW-25/26 có số liệu nhưng chỉ trên dataset tự dựng 2-3 nhóm` | `[MAJOR]` |
| Q11 | `PERF-LARGE-001` | `Cao` (mục tiêu chính của ticket là hiệu năng) | `RISK — NEW-12/NEW-15 là TC đo hiệu năng nhưng expected không có ngưỡng ("thấp hơn rõ rệt", "mức chấp nhận được", "để leader chốt ngưỡng") → không kết luận được Đạt/Không đạt. Và không TC nào đo trên dataset đã nhân đôi thật (bot 41216) — nơi con số 246s xuất hiện` | `[MAJOR]` |
| Q12 | `CONC-001` | `Cao` | `RISK — NEW-8 phủ double-click + 2 tab (Abnormal) nhưng RULE-01 thiếu Boundary, và thiếu tình huống đồng thời nguy hiểm nhất: command DELETE chạy song song với lưu template (G9). NEW-8 còn để expected 2 nhánh "nếu xuất hiện 2 dòng thì báo leader" → không phải oracle` | `[MAJOR]` |
| Q13 | `DATA-CACHE-001` | `Cao` (output user-facing → nâng Cao) | `RISK — NEW-14 verify stale preview nhưng chỉ quan sát trên màn soạn tin. Evidence của quan điểm yêu cầu "nội dung LINE thật sau khi đổi dữ liệu gốc". Kho FA-010 TC-TMT-145 ("xoá ảnh preview → hệ thống lấy lại ảnh default của link") là hành vi có thể đã bị fix làm đổi — xem §6` | `[MAJOR]` |
| Q14 | `SYNC-APP-001` | `Cao` (flow critical user-facing → nâng Cao) | `RISK — NEW-19/NEW-20 chỉ phủ /api/mobile/save-template ở action_type mặc định (template list). Luồng 「送信予約」 đặt lịch gửi tin từ chat 1:1 có mặt trên CẢ web lẫn app mobile nhưng 0 TC nào chạm — chưa biết bản mobile có nhánh schedule_send riêng không (repo không có spec API mobile để đối chiếu). Xem G10` | `[MAJOR]` |

---

## 3. TC trùng lặp nội dung

Đã rà **33** TC theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`) — **không phát hiện TC trùng lặp cần xoá hoặc gộp**.

| Nhóm | TC | Loại | Nhận xét | Severity |
|---|---|---|---|---|
| OV-1 | `NEW-10` ↔ `NEW-18` | overlap **có chủ đích**, không phải DUP | Nhánh 2 của `NEW-10` (log cảnh báo URL > 500) là tập con của `NEW-18`, nhưng `NEW-10` kiểm ở tầng màn soạn tin còn `NEW-18` kiểm ở tầng log/endpoint — 2 oracle khác nhau cho cùng 1 rủi ro. **Giữ cả hai** | `[NIT]` |

- Các cặp đã soi và **kết luận KHÔNG trùng**: `NEW-1` ↔ `NEW-9` (khác biên độ dài URL) · `NEW-9` ↔ `NEW-10` (255/300/500 vs 769) · `NEW-5` ↔ `NEW-22` (màn soạn tin vs endpoint EP-25) · `NEW-3` ↔ `NEW-15` (3 URL kiểm gán nhầm tiêu đề vs 10 URL kiểm hiệu năng) · `NEW-11` ↔ `NEW-13` (cache rỗng vs cache thiếu title) · `NEW-12` ↔ `NEW-14` (không ghi đè cache vs stale hiển thị) · `NEW-7` ↔ `NEW-27` (đối chứng âm khi lưu vs khi chạy command) · `NEW-25` ↔ `NEW-26` ↔ `NEW-29` (dry-run vs chạy thật vs chunk/idempotent).
- **Gate đã chạy**: không đề nghị xoá TC nào → coverage §1 + §2 không đổi.
- `DUP-INFLATE`: **không có** — không quan điểm/impact nào đang "trông như đủ" nhờ TC trùng.
- `DUP-CONFLICT`: **không có** giữa 33 TC với nhau. Conflict với **kho-tcs** có 1 điểm → §6.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | `NEW-10` → bug **#40968** `[High]` | **Bug gốc CHƯA được fix.** Run `#1423` (2026-09-14, env `local`): URL 769 ký tự lưu 3 lần vẫn tạo **2 dòng `template_url_redirect` + 4 bản ghi `url`**. Nguyên nhân đã được `NEW-32` (pass) chốt bằng bằng chứng: `url.url` khai báo **`varchar(500)`**, `strict = false`, lưu 769 ký tự → **cắt còn 500** → lần lưu sau tra `Url::where('url', …)` không khớp → sinh `url` mới → **`url_id` đổi** → dedupe theo `url_id` trượt | ⚠️ **Nới `template_url_redirect.url` lên `varchar(1024)` KHÔNG đóng được case này** — cột phải nới là **`url.url`** (DB riêng `mysql_url`). Cần ticket nới `url.url` hoặc đổi sang so khớp bằng hash trước khi đóng #40909 |
| I2 | `[BLOCKER]` | Toàn bộ bộ TC | **Chỉ chạy trên `local`, 0 TC chạy production.** Run `#1423` chạy env `local`: 30 pass / 2 fail / 1 chưa chạy (`NEW-14` là manual). `prd` / `staging` / `dev` đều `runs = 0`. Task chạm **URL/domain** (link rút gọn), **job nền** (command), **migration khoá bảng**, **đo hiệu năng** — RULE-08 cấm kết luận từ local | Số đo hiệu năng của run này **không dùng được**: trang đích là server nội bộ `127.0.0.1` trả lời tức thì (`NEW-12`: 201/99/109ms; `NEW-15`: 367/352/351ms) trong khi mốc ticket là **246 giây** trên production. Bảng `template_url_redirect` test chỉ **746 dòng** nên `NEW-31` tự ghi rõ số đo không đại diện |
| I3 | `[MAJOR]` | `NEW-33` → bug **#40967** `[Medium]` | **Không quay lui được migration bằng Laravel trên nhánh này.** `php artisan migrate:rollback` fatal: `Cannot declare class AddBotsToUsersTable` — 2 file migration từ 2021 trùng tên lớp (`2021_12_29_162551_...` và `2021_12_31_165649_...`). **Lỗi có sẵn của cây nguồn, KHÔNG do #40909**, nhưng nó **vô hiệu hoá phương án quay lui** trong chính kế hoạch phát hành của ticket này | Kế hoạch release phải ghi rõ: nếu cần gỡ migration `url→TEXT` trên môi trường thật thì **phải làm bằng SQL tay**, không dùng được lệnh Laravel. Runner đã bù bằng SQL và đo được hậu quả: URL 769 ký tự bị cắt còn 255 và **không phục hồi được** kể cả khi chạy lại migration |
| I4 | `[MAJOR]` | `NEW-4` | **TC viết sai hành vi màn hình — không chạy được tới bước cuối và expected sai.** (a) Steps yêu cầu "xoá 「プレビュータイトル」" nhưng kho `TC-TMT-143` ghi ô này **bắt buộc nhập** khi preview đang bật → lưu sẽ báo lỗi, TC dừng ở bước 3. (b) Steps yêu cầu "xoá 「プレビューイメージ」" nhưng kho `TC-TMT-145` ghi hệ thống **tự lấy lại ảnh default của link** → không xoá trắng được. (c) Expected "URL không còn dòng cấu hình nào trong tab thiết lập" **sai**: danh sách URL ở tab sinh từ **detect nội dung 本文** (`detectUrlInMessageTextV2`), không phải từ `template_url_redirect`. Đã verify live trên STAGING (`get_template_detail` bot `5a44Ea`): template `113302` có **2 URL trong content** nhưng `urlSettings: []` — URL vẫn hiện ở tab dù không có bản ghi cấu hình nào; kho `TC-TMT-141` + `TC-TMT-151` xác nhận cùng hành vi | Viết lại theo cách trigger nhánh clear DB **đúng spec**: chuyển 「URL表示プレビュー」 sang 「表示しない」 (kho `TC-TMT-147`), hoặc bỏ URL khỏi 本文 — thay vì xoá trắng từng field. Expected sửa thành: URL **vẫn hiển thị** ở tab nhưng ở trạng thái chưa cấu hình, còn bản ghi `template_url_redirect` của cặp template/URL bị xoá. Xem TC thay thế `TC-DATADB001-01` §5. ⚠️ **Ý định của TC (giữ 1 case bảo vệ nhánh clear DB) là đúng** — chỉ phần thực thi sai. ✅ **ĐÃ SỬA 2026-09-15**: `testcase_update` trên Studio, TC #17727 lên **version 2** — dùng 「表示しない」, expected ghi rõ tab vẫn hiển thị đủ 2 URL, thêm bước 6 nối lại với quy tắc dedupe mới. Không còn TC đề xuất thay thế ở §5 |
| I5 | `[MAJOR]` | `NEW-10` | **Expected rẽ 2 nhánh** ("nếu cột chứa được 769 → 1 dòng; nếu chỉ 500 → vẫn nhân đôi + ghi log"). TC luôn "Đạt" bất kể bug gốc còn hay hết → mất tác dụng làm oracle đóng ticket, dù đây **chính là case gây ra ticket** | Tách 2 TC, hoặc đặt `NEW-32` làm tiền đề bắt buộc rồi chốt **một** expected duy nhất theo kết quả `NEW-32` trên **production** |
| I6 | `[MAJOR]` | `NEW-12`, `NEW-15` | **TC hiệu năng không có ngưỡng** — "thấp hơn rõ rệt", "nằm trong mức chấp nhận được", "ghi lại số đo để leader chốt". Ticket sinh ra từ con số 246s nhưng bộ TC không có mốc nào để kết luận đã đạt | Leader chốt ngưỡng trước khi chạy (vd: lưu template 1 URL đã cache ≤ 3s; 10 URL ≤ 10s), ghi thẳng vào expected |
| I7 | `[MAJOR]` | `NEW-9`, `NEW-10`, `NEW-19` | **Dữ liệu test không thật** — dùng `example.com/...` cho cả case tái hiện bug gốc, trong khi bug đến từ URL HotPepper thật (`beauty.hotpepper.jp` + `utm/yclid/gclid`, có OGP thật, độ trễ mạng thật). `example.com` không có OGP → nhánh fetch metadata đi khác hẳn thực tế | Case tái hiện bug (`NEW-10`) dùng đúng dạng URL của ticket; giữ `example.com` cho các case thuần đếm bản ghi |
| I8 | `[MAJOR]` | 17/33 TC | **Mã quan điểm ngoài `framework/checklist-lme.md`**: `STATE-MATRIX-001`(3) · `TOOL-OLDREC-001`(3) · `PERF-LATENCY-001`(2) · `TOOL-NEGCTRL-001`(2) · `OBS-001`(2) · `TOOL-KNOW-002`(1) · `RULE-09`(1 — đây là mã RULE, không phải mã quan điểm) · `TOOL-ERRHYG-001`(1) · `API-CONTRACT-001`(1) · `RULE-TOOL-029`(1). TC quan trọng nhất (`NEW-1`, tái hiện bug gốc) mang mã `TOOL-KNOW-002` → không map được coverage | Re-map gợi ý: `TOOL-KNOW-002`→`FUNC-001` · `STATE-MATRIX-001`→`STATE-001`/`FUNC-SEQ-001` · `TOOL-OLDREC-001`→`COMPAT-LEGACY-001` · `PERF-LATENCY-001`→`PERF-LARGE-001` · `TOOL-NEGCTRL-001`→`DATA-DB-001` · `OBS-001`→`SEC-001`/`OUT-TRUTH-001` · `TOOL-ERRHYG-001`→`FUNC-002` · `API-CONTRACT-001`→`REG-SHARED-001` · `RULE-TOOL-029`→`REG-SHARED-001` · `RULE-09`→`COMPAT-LEGACY-001` |
| I9 | `[MAJOR]` | `[AP-4]` — file 03 mục "Commit / Pull Request" | **Không có link PR** để đọc nội dung diff. Studio có `diffAvailable = true` nhưng `diffStrategy = "stat"` → chỉ biết 5 file và +259/−49, **không đọc được code thay đổi**. Không ai verify được fix shape thực tế (điều kiện dedupe mới viết chính xác ra sao, `orderBy id desc` áp ở đâu) | Yêu cầu Dev gắn link PR / hoặc mở diff đầy đủ trên Studio trước khi Leader duyệt |
| I10 | `[MAJOR]` | `NEW-8` | Expected dạng "nếu xuất hiện 2 dòng thì ghi nhận rủi ro đã biết, báo leader, KHÔNG tự kết luận" → **không phải oracle**, TC không thể fail. Trong khi Dev đã cố ý lùi unique index sang ticket sau, đây là rủi ro cần số liệu | Chốt trước: race tạo 2 dòng là **Không đạt** (và chấp nhận nợ kỹ thuật ghi vào ticket sau), hay là **Đạt có điều kiện**. Không để TC tự treo |
| I11 | `[MAJOR]` | `NEW-30`, `NEW-33` | **Expected khoá cứng kiểu TEXT — sẽ sai trên production.** `NEW-30` kỳ vọng "cột url của bảng URL redirect có kiểu **TEXT**"; `NEW-33` kỳ vọng quay lui thì cột "trở về kiểu chuỗi **255** ký tự" và URL 300 ký tự bị cắt còn 255. Nhưng production dùng **`varchar(1024)`** → chạy nguyên văn 2 TC này trên production sẽ ra **Không đạt** dù hệ thống hoạt động đúng; và với 1024 thì URL 300 ký tự **không** bị cắt khi quay lui nên kịch bản cảnh báo mất dữ liệu của `NEW-33` cũng không còn đúng | Viết lại expected theo **từng môi trường**: dev/staging = TEXT (theo migration), production = `varchar(1024)`; hoặc thống nhất migration và production về cùng một kiểu rồi mới chạy. `NEW-33` đổi mốc cắt dữ liệu theo kiểu cột thật của môi trường đang chạy. Xem §6 mục 7 |
| I12 | `[MINOR]` | `NEW-5`, `NEW-6`, `NEW-13`, `NEW-22`, `NEW-24` | Tiền đề phải **INSERT/UPDATE trực tiếp DB** để mô phỏng dữ liệu lỗi. Hợp lý với task DB-level này, nhưng TC không ghi **ai** có quyền làm và làm trên môi trường nào → member manual không dựng được | Ghi rõ trong tiền đề: cần quyền DB test, script seed kèm sẵn, và tuyệt đối không áp dụng trên production |
| I13 | `[MINOR]` | `NEW-9`, `NEW-29` | **Không atomic** — `NEW-9` gộp 3 biến thể độ dài (255/300/500) trong 1 TC; `NEW-29` gộp 3 tham số (`--chunk=1` → lặp lại → `--chunk=0`) | Tách theo biến thể để khi fail biết ngay biến thể nào; hoặc giữ nguyên và ghi rõ ở `Ghi chú` cách báo kết quả từng biến thể |
| I14 | `[MINOR]` | 32/33 TC | `exec_mode = auto` nhưng `automated rate = 0%` trên mọi env → TC khai tự động hoá mà thực tế chưa chạy tự động lần nào. Nhiều TC trong số đó cần seed DB tay hoặc mắt người (preview, ảnh) | Rà lại `exec_mode`: TC cần quan sát ảnh/preview hoặc seed tay nên là `manual` |
| I15 | `[NIT]` | 24/33 TC | `env_scope` khai `['dev','local','staging']`. Theo quy ước nội bộ, định ghi staging thì ghi **Tất cả**; khai `env_scope` cũng không đồng nghĩa đã chạy | Chuẩn hoá `env_scope`, và nâng lên `prd` cho nhóm TC ở `I2` |

---

## 5. TCs đề xuất bổ sung (16)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | [kho-tcs/fa010-mautinnhan-テンプレート.md](../../kho-tcs/fa010-mautinnhan-テンプレート.md) — nhóm 16 「Text — PDF & shorten URL」(10 TC), nhóm 17 「URL redirect — preview & metadata」(12 TC), nhóm 18 「URL redirect — hết hạn & action」(22 TC) |
| Vùng regression phát hiện từ kho | `TC-TMT-167` / `TC-TMT-168` / `TC-TMT-173` (URL bị xoá → tạo url mới + **đổi url_id**) → **G1, nguy hiểm nhất** · `TC-TMT-165` (job `recover:templateUrlRedirect`) → G6 · `TC-TMT-170` (copy/backup template có URL) → G5 · `TC-TMT-174` (`detectUrlInMessageTextV2` ở chat 1:1 + action text) → G4 · `TC-TMT-146/147/149/171/172` (preview URL trên LINE ở 4 đường gửi) → G7 · `TC-TMT-152` (metadata nhiều dạng URL thật) → Q1 |
| Conflict expected vs kho | `TC-TMT-145` ("xoá ảnh preview URL rồi lưu → hệ thống **lấy lại ảnh default** của link") **vs** hành vi mới sau fix ("chỉ fetch metadata khi cache rỗng") → **[MAJOR] SPEC-CONFLICT**, đã đưa §6. **Không tự chọn bên** — không viết TC chốt hành vi cho điểm này |
| GAP dùng lại TC kho (không viết mới) | `Q1` dùng lại một phần `TC-TMT-152` "Get metadata đúng với nhiều dạng URL thực tế (SNS, short link, Google Form, YouTube...)" — cần chỉnh thêm bước **lưu lại 3 lần + đếm dòng `template_url_redirect`** thì mới phủ được rủi ro url_id |
| Kho FA-001 (chat 1:1) đã rà thêm | Nhóm 「Đặt lịch gửi — delay & job」 (`TC-CHT-320`…`TC-CHT-326`) chỉ phủ **delay + job gửi**, nhóm 「Gửi template — chọn & preview」 có `TC-CHT-216` (sửa nội dung template ngay tại preview) nhưng là luồng **gửi ngay**, không phải đặt lịch — **không nhóm nào phủ việc lưu/sửa template có URL trong luồng 送信予約** |
| Xác nhận chống trùng | Đã đối chiếu **33** TC ở BƯỚC 0 + kho FA-010 + kho FA-001 — **không TC đề xuất nào trùng** |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-DATAREF001-01 | UI | DATA-REF-001 | URL redirect — dedupe theo url_id | Abnormal | manual | Tất cả | URL bị xoá khỏi danh sách URL của bot rồi lưu lại template — kiểm có nhân thêm dòng không | Đã đăng nhập admin, chọn bot test, migration #40909 đã chạy. Có template Text TU chứa 1 URL ngoài hệ thống, đã lưu 1 lần, đã cấu hình プレビュータイトル. URL này cũng đang hiển thị ở màn 「URL分析」 | 1. Mở template TU → tab 「URL表示期限・アクション設定」, ghi lại cấu hình URL đang có<br>2. Vào màn 「URL分析」 `/basic/url`, xoá URL đó khỏi danh sách URL của bot<br>3. Quay lại template TU, mở màn soạn tin, không sửa nội dung, bấm 「登録」<br>4. Reload màn, mở lại tab 「URL表示期限・アクション設定」, đếm số dòng URL hiển thị<br>5. Lặp lại bước 3 thêm 2 lần nữa, mỗi lần reload và đếm lại số dòng | Template Text có 1 URL ngoài hệ thống (~300 ký tự); プレビュータイトル 「削除前タイトル」 | Sau mỗi lần lưu, tab 「URL表示期限・アクション設定」 chỉ hiển thị **đúng 1 dòng** cho URL đó và giữ nguyên プレビュータイトル đã cấu hình. Số dòng **không tăng theo số lần lưu** (không thành 2 rồi 3). Nếu số dòng tăng → bug regression do fix, báo lại ticket ngay | | Lấp `G1` · `Q7` · regression — dẫn từ `TC-TMT-167` · Đánh giá spec: Spec không ghi — cần Dev xác nhận hành vi khi `url_id` đổi · Evidence: screenshot tab URL sau từng lần lưu + kết quả đếm dòng |
| TC-DATAREF001-02 | UI | DATA-REF-001 | URL redirect — dedupe theo url_id | Abnormal | manual | Tất cả | Cùng 1 URL dùng ở 2 template, URL bị xoá — lưu lại template A không đụng template B | Trên cùng bot test có 2 template Text A và B dùng **chung** một URL, mỗi template đặt プレビュータイトル riêng, cả hai đã lưu thành công. Ghi lại cấu hình URL của cả A và B | 1. Ghi lại cấu hình URL hiển thị ở tab 「URL表示期限・アクション設定」 của cả A và B<br>2. Vào 「URL分析」 xoá URL dùng chung khỏi danh sách URL của bot<br>3. Mở template A, bấm 「登録」<br>4. Reload và kiểm tab URL của template A<br>5. Mở template B, reload và kiểm tab URL của template B | URL dùng chung; A: 「Aタイトル」, B: 「Bタイトル」 | Template A: đúng 1 dòng URL, giữ 「Aタイトル」. Template B: đúng 1 dòng URL, **vẫn giữ nguyên** 「Bタイトル」 và toàn bộ cấu hình cũ — không bị xoá, không bị ghi đè theo A, không sinh thêm dòng | | Lấp `G1` · regression — dẫn từ `TC-TMT-168` · Đánh giá spec: Spec không ghi · Evidence: screenshot tab URL của cả A và B trước/sau |
| TC-DATAREF001-03 | Data | DATA-REF-001 | Lệnh dọn dữ liệu trùng | Abnormal | manual | product | Sau khi dọn dữ liệu trùng — action gắn với dòng bị xoá không thành mồ côi | Trên môi trường production đã có backup bảng. Chọn 1 nhóm trùng thật mà các dòng bị xoá **có cấu hình action khi click** và **action khi hết hạn** khác nhau. Ghi lại danh sách action của từng dòng trong nhóm | 1. Ghi lại toàn bộ action đang gắn với từng dòng của nhóm trùng (action khi click, action khi hết hạn)<br>2. Chạy lệnh dọn dữ liệu trùng ở chế độ chạy thử, xác nhận nhóm này nằm trong danh sách<br>3. Chạy lệnh ở chế độ chạy thật<br>4. Mở màn soạn tin của template đó, kiểm cấu hình action của URL còn lại<br>5. Bấm thử link rút gọn và kiểm action có chạy đúng cấu hình của bản được giữ lại không<br>6. Rà các action từng thuộc dòng bị xoá xem còn bản ghi mồ côi nào không | 1 nhóm trùng thật trên production, các dòng có action khác nhau | URL còn lại giữ đúng action của bản mới nhất; bấm link rút gọn thì action đó được thực thi. Các action từng gắn với dòng bị xoá **không còn bị tham chiếu** và không gây lỗi ở bất kỳ màn nào. Không phát sinh action chạy nhầm (action của bản cũ vẫn kích hoạt) | | Lấp `Q7` · RULE-08 (dữ liệu thật) · Đánh giá spec: Spec không ghi · Evidence: bảng đối chiếu action trước/sau + log action khi click link |
| TC-OUTTRUTH001-02 | UI | OUT-TRUTH-001 | Soạn tin Text có URL — lấy dữ liệu xem trước | Abnormal | manual | Tất cả | Trang đích trả về lỗi hoặc không có thẻ xem trước — lưu đúng, không cảnh báo hệ thống | Chuẩn bị 3 URL do tester kiểm soát: (a) trang trả lỗi máy chủ, (b) trang không có thẻ OGP nào, (c) trang chỉ có tiêu đề, thiếu ảnh và mô tả. Cả 3 chưa từng dùng ở bot test | 1. Với từng URL, mở màn thêm tin nhắn 「テキスト」 và nhập 本文 chứa URL đó<br>2. Chọn 「表示する」 cho phần xem trước, bấm 「登録」<br>3. Reload và quan sát phần xem trước URL trên màn<br>4. Kiểm log ứng dụng của mỗi lần lưu<br>5. Lưu lại lần 2 và đếm số dòng URL của template | (a) trang trả lỗi máy chủ · (b) trang không có thẻ xem trước · (c) trang chỉ có tiêu đề | Cả 3 trường hợp đều lưu được, màn không báo lỗi hệ thống. Phần xem trước hiển thị trống hoặc giá trị mặc định chứ **không hiển thị chữ lỗi/ký tự lạ**. Log **không** có cảnh báo về biến rỗng/không xác định. Lưu lần 2 vẫn chỉ 1 dòng URL cho mỗi template | | Lấp `G2` · `Q5` · Đánh giá spec: Spec không ghi · Evidence: screenshot phần xem trước của cả 3 case + đoạn log |
| TC-FUNC003-02 | UI | FUNC-003 | Soạn tin Text có URL — định dạng URL | Boundary | manual | Tất cả | URL chứa ký tự mã hoá và tên miền tiếng Nhật — lưu 3 lần vẫn 1 dòng, giữ nguyên chuỗi | Đã đăng nhập, chọn bot test, migration #40909 đã chạy. Tạo template Text mới với định danh riêng | 1. Nhập 本文 chứa URL có phần tham số mã hoá phần trăm và ký tự tiếng Nhật<br>2. Bấm 「登録」, sau đó mở lại và bấm 「登録」 thêm 2 lần<br>3. Reload, mở tab 「URL表示期限・アクション設定」, so từng ký tự của URL hiển thị với URL đã nhập<br>4. Đếm số dòng URL của template<br>5. Bấm thử link rút gọn xem có mở đúng trang đích không | URL có tham số mã hoá phần trăm chứa ký tự tiếng Nhật (vd `...?q=%E3%82%AD%E3%83%A3%E3%83%B3%E3%83%9A%E3%83%BC%E3%83%B3`), tổng độ dài ~320 ký tự | Sau 3 lần lưu chỉ có **đúng 1 dòng** URL. Chuỗi URL hiển thị lại **khớp 100%** với chuỗi đã nhập, không bị đổi mã hoá, không bị cắt. Link rút gọn mở đúng trang đích với tham số nguyên vẹn | | Lấp `Q1` · dẫn từ `TC-TMT-152` · Đánh giá spec: Spec không ghi · Evidence: so chuỗi URL nhập vs hiển thị + screenshot trang đích mở từ link rút gọn |
| TC-MSG004-01 | UI | MSG-004 | Gửi tin có URL ra LINE | Normal | manual | product | Gửi template Text có URL cho friend thật — preview và link rút gọn trên LINE đúng | Bot test đã kết nối LINE OA thật, có ít nhất 1 friend test trên iOS và 1 trên Android. Template Text có 1 URL đã cấu hình プレビュー và thời hạn hiển thị, đã lưu lại ≥ 3 lần sau fix | 1. Mở template, xác nhận tab 「URL表示期限・アクション設定」 chỉ có 1 dòng URL<br>2. Gửi test cho friend trên iOS và friend trên Android<br>3. Trên LINE app, quan sát khối xem trước URL trong tin nhận được (tiêu đề, mô tả, ảnh)<br>4. Bấm vào link trong tin trên LINE, xác nhận mở đúng trang đích<br>5. Kiểm hành động sau khi click có chạy đúng cấu hình không<br>6. Mở lại màn 「URL分析」 và kiểm lượt click vừa phát sinh có được ghi nhận không | Template Text có URL đã cấu hình プレビュータイトル / ảnh / thời hạn / hành động sau click | Tin trên LINE (cả iOS và Android) hiển thị **đúng một** khối xem trước, nội dung khớp với cấu hình trên màn admin, **không lặp link**. Bấm link mở đúng trang đích, hành động sau click chạy đúng 1 lần theo cấu hình, và lượt click được ghi nhận ở màn 「URL分析」 | | Lấp `G7` · `Q4` · RULE-06 · RULE-08 · Đánh giá spec: Spec ghi rõ (`feature-spec.md` SCR-TMT-05) · Evidence: screenshot LINE app **iOS và Android** + screenshot màn URL分析 |
| TC-MSG004-02 | UI | MSG-004 | Gửi tin có URL ra LINE | Abnormal | manual | product | Trang đích đổi nội dung xem trước — tin gửi ra LINE vẫn hiện nội dung cũ | Có trang đích do tester kiểm soát thẻ xem trước. Đã lưu template Text dùng URL đó và đã gửi 1 lần cho friend test để đối chứng nội dung ban đầu | 1. Ghi lại nội dung khối xem trước trong tin đã nhận trên LINE (ảnh chụp)<br>2. Đổi tiêu đề và ảnh xem trước của trang đích, xác nhận mở trực tiếp thấy nội dung mới<br>3. Mở template trên màn admin, bấm 「登録」 lại<br>4. Gửi test lại cho cùng friend<br>5. So khối xem trước trong tin mới nhận với ảnh chụp ban đầu | Trang đích đổi tiêu đề xem trước từ 「旧タイトル」 sang 「新タイトル」 | Tin mới nhận trên LINE **vẫn hiển thị nội dung cũ** 「旧タイトル」 — đúng với hành vi đã đổi có chủ ý (chỉ lấy dữ liệu xem trước khi chưa có). Kết quả phải **nhất quán** với màn admin. Đây là thay đổi trải nghiệm người dùng cuối → cần người có thẩm quyền xác nhận chấp nhận trước khi đóng ticket | | Lấp `Q13` · RULE-06 · RULE-08 · Đánh giá spec: Đã hỏi leader — hành vi đổi có chủ ý, chờ xác nhận · Evidence: cặp screenshot LINE trước/sau |
| TC-FUNC004-02 | UI | FUNC-004 | Soạn tin Text có URL — biên sức chứa cột 1024 | Boundary | manual | product | URL ở biên 1024 ký tự — lưu đủ, và biên 1025 ký tự phải báo rõ chứ không cắt âm thầm | **Cần xác nhận trước khi chạy**: `template_url_redirect.url` trên production đã được đổi sang `varchar(1024)` (quyết định vận hành của Leader, khác với migration của ticket vốn đổi sang TEXT). Bot test riêng trên production, đã backup. Chuẩn bị 3 URL định danh riêng: 1023, đúng 1024 và 1025 ký tự | 1. Nhập 本文 chứa URL 1023 ký tự, cấu hình xem trước rồi bấm 「登録」, lưu thêm 2 lần nữa<br>2. Đọc lại giá trị URL đã lưu, so từng ký tự với chuỗi đã nhập; đếm số dòng URL redirect<br>3. Lặp lại bước 1-2 với URL đúng 1024 ký tự<br>4. Lặp lại bước 1-2 với URL 1025 ký tự<br>5. Với case 1025 ký tự: đọc log ứng dụng của lần lưu và quan sát màn hình có báo gì không<br>6. Bấm thử link rút gọn của cả 3 case xem có mở đúng trang đích không | 3 URL độ dài 1023 · 1024 · 1025 ký tự, mỗi URL một đường dẫn định danh riêng | URL 1023 và 1024 ký tự: lưu **khớp 100%** chuỗi đã nhập, sau 3 lần lưu chỉ 1 dòng URL redirect, link rút gọn mở đúng trang. URL 1025 ký tự: **không được cắt âm thầm** — hệ thống phải báo lỗi rõ ràng cho người dùng hoặc ghi log cảnh báo nêu đúng độ dài vượt sức chứa. Nếu chuỗi bị cắt còn 1024 mà màn vẫn báo lưu thành công và không có log nào → đúng lại cơ chế đã gây ra bug gốc, chỉ dời biên từ 255 lên 1024 → báo lại ticket | | Lấp `Q2` · biên mới sinh ra do quyết định dùng `varchar(1024)` thay TEXT (TEXT không có biên này) · RULE-08 · Đánh giá spec: Spec không ghi — cần chốt hành vi khi URL vượt 1024 · Evidence: so chuỗi nhập/lưu cho cả 3 độ dài + đoạn log của case 1025 + screenshot màn hình |
| TC-ENV003-02 | Data | ENV-003 | Lệnh dọn dữ liệu trùng | Normal | manual | product | Chạy thử lệnh dọn trên production — chốt phạm vi dữ liệu rác toàn hệ thống | Có quyền chạy lệnh ở chế độ chạy thử trên production. Migration #40909 đã được áp lên production | 1. Chạy lệnh dọn dữ liệu trùng ở chế độ chạy thử trên production<br>2. Ghi lại tổng số nhóm trùng, tổng số bản ghi sẽ xoá, và phân bố theo bot<br>3. Đối chiếu riêng số liệu của bot 41216 nêu trong ticket<br>4. Ước lượng thời gian chạy thật dựa trên số nhóm<br>5. Xác nhận lệnh **không** thay đổi dữ liệu nào sau khi chạy thử | Không tham số ngoài chế độ chạy thử | Có con số phạm vi ảnh hưởng **toàn hệ thống** (bao nhiêu bot, bao nhiêu template, bao nhiêu bản ghi rác) thay vì chỉ suy từ 1 bot. Bot 41216 xuất hiện trong danh sách với số bản ghi rác lớn. Sau khi chạy thử, tổng số dòng bảng **không đổi** | | Lấp `Q2` · `Q10` · RULE-04 (xác nhận phạm vi khi recovery) · RULE-08 · Đánh giá spec: Spec không ghi · Evidence: nguyên văn kết quả lệnh + bảng phân bố theo bot |
| TC-REGSHARED001-02 | UI | REG-SHARED-001 | Copy / sao chép template có URL | Normal | manual | Tất cả | Copy template Text có URL rồi lưu bản copy nhiều lần — không nhân đôi dòng | Có template Text TU chứa 1 URL đã cấu hình đầy đủ và đã lưu sau fix. Bot test có quyền copy template | 1. Copy template TU thành bản mới TU2<br>2. Mở TU2, kiểm tab 「URL表示期限・アクション設定」 và ghi lại số dòng + cấu hình<br>3. Bấm 「登録」 trên TU2 thêm 2 lần<br>4. Reload và đếm lại số dòng URL của TU2<br>5. Mở lại TU gốc và kiểm cấu hình URL của TU còn nguyên không | Template TU có 1 URL đã cấu hình プレビュー + thời hạn | TU2 có đúng 1 dòng URL cho URL đó, số dòng **không tăng** sau 2 lần lưu thêm. Cấu hình URL của TU gốc **không bị đổi, không bị xoá**. Nếu kho ghi nhận setting URL redirect không được copy thì hành vi phải giữ đúng như trước fix | | Lấp `G5` · `Q9` · regression — dẫn từ `TC-TMT-170` · Đánh giá spec: Spec không ghi · Evidence: screenshot tab URL của TU và TU2 sau từng bước |
| TC-REGSHARED001-03 | UI | REG-SHARED-001 | Chat 1:1 / Action gửi text có URL | Normal | manual | Tất cả | Gửi URL dài trực tiếp ở chat 1:1 và trong action text — khối xem trước vẫn đúng | Bot test có friend test trên LINE. Có 1 URL dài ~300 ký tự đã từng được lưu ở template sau fix (đã có dữ liệu xem trước được ghi nhớ) và 1 URL dài ~300 ký tự **chưa từng dùng** | 1. Mở chat 1:1 với friend test, gửi trực tiếp URL đã có dữ liệu xem trước<br>2. Quan sát khối xem trước trong tin nhận được trên LINE<br>3. Gửi tiếp URL chưa từng dùng và quan sát khối xem trước<br>4. Tạo 1 action gửi text chứa đúng URL dài đó, kích hoạt action cho friend test<br>5. Quan sát khối xem trước trong tin do action gửi<br>6. Đếm số bản ghi URL redirect phát sinh sau các bước trên | 2 URL dài ~300 ký tự: 1 đã có dữ liệu xem trước, 1 chưa từng dùng | Cả 2 đường gửi (chat 1:1 và action text) đều hiển thị khối xem trước đúng nội dung trang đích, **không lỗi, không mất preview**, và URL mở được đúng trang. Không phát sinh bản ghi URL redirect rác cho template nào. Hàm dò URL trong nội dung vẫn so khớp theo chuỗi (Dev không sửa) nên với URL dài cần xác nhận rõ nó tra ra đúng bản ghi | | Lấp `G4` · regression — dẫn từ `TC-TMT-174` · Đánh giá spec: Spec không ghi — `logic-spec.md` chỉ mô tả hàm, không nêu hành vi với URL dài · Evidence: screenshot LINE app cho cả 2 đường gửi |
| TC-REGSHARED001-04 | UI | REG-SHARED-001 | Đặt lịch gửi tin từ chat 1:1 (送信予約) — template có URL | Normal | manual | Tất cả | Lưu lịch gửi nhiều lần với tin có URL dài — không nhân đôi dòng URL redirect | **Cần Dev xác nhận trước**: luồng `action_type=schedule_send` (`saveScheduleSendChat`) có dùng chung `saveText` đã fix không. Đã đăng nhập trang quản trị, chọn bot test, migration #40909 đã chạy. Có friend test đang trò chuyện ở chat 1:1. Chuẩn bị 1 URL dài ~300 ký tự (đi đúng nhánh từng gây bug) | 1. Mở chat 1:1 với friend test, soạn tin có chứa URL dài đó<br>2. Bấm 「送信予約」, chọn ngày giờ gửi trong tương lai rồi xác nhận lưu<br>3. Mở lại lịch gửi vừa tạo, **sửa nội dung tin** (đổi một phần chữ, giữ nguyên URL) rồi lưu lại<br>4. Lặp lại bước 3 thêm 2 lần nữa<br>5. Đếm số dòng URL redirect ứng với template của lịch gửi này trong DB sau mỗi lần lưu<br>6. Đối chiếu giá trị URL đã lưu với URL đã nhập (có bị cắt ở ký tự thứ 255 không)<br>7. Chờ tới giờ gửi, kiểm tin friend nhận trên LINE: link mở đúng trang, khối xem trước đúng | Tin nhắn chat 1:1 chứa 1 URL dài ~300 ký tự, định danh riêng cho lần chạy | Sau 3 lần sửa và lưu lịch gửi, template của lịch chỉ có **đúng 1 dòng** URL redirect cho URL đó — số dòng **không tăng theo số lần lưu**. URL lưu lại đầy đủ, không bị cắt. Tới giờ gửi, friend nhận đúng 1 tin, link mở đúng trang đích. Nếu số dòng tăng dần → `saveScheduleSendChat` là **bản sao dedupe chưa được fix**, mở rộng phạm vi fix ngay | | Lấp `G10` · regression · Đánh giá spec: Đã hỏi leader — `logic-spec.md:197` xác nhận router có nhánh `schedule_send`, nhưng `chat-11/feature-spec.md` Luồng 4 không mô tả bước soạn/sửa template → cần Dev xác nhận, xem §6 · Evidence: bảng đếm dòng URL redirect sau từng lần lưu + screenshot tin trên LINE |
| TC-SYNCAPP001-01 | UI | SYNC-APP-001 | Đặt lịch gửi tin từ chat 1:1 (送信予約) — web ⇄ app mobile | Normal | manual | Tất cả | Đặt lịch gửi tin có URL từ app mobile và sửa xen kẽ với web — kết quả nhất quán | **Cần Dev xác nhận trước**: app mobile có màn đặt lịch gửi tin từ chat 1:1 không, và có dùng nhánh `schedule_send` riêng không. Có tài khoản đăng nhập app mobile và phiên đăng nhập trang quản trị cho **cùng** bot test. Cùng 1 friend test | 1. Từ app mobile, mở chat 1:1 với friend test, soạn tin chứa URL dài ~300 ký tự và đặt lịch gửi<br>2. Trên trang quản trị, mở đúng lịch gửi đó và kiểm nội dung tin + URL hiển thị<br>3. Trên trang quản trị, sửa nội dung tin rồi lưu lại<br>4. Quay lại app mobile, mở lại lịch gửi đó và kiểm nội dung<br>5. Từ app mobile sửa tiếp rồi lưu, sau đó đếm số dòng URL redirect của template lịch gửi trong DB<br>6. Chờ tới giờ gửi, kiểm tin friend nhận trên LINE | Tin có URL dài ~300 ký tự; sửa xen kẽ app → web → app | Nội dung và URL hiển thị **nhất quán** giữa app mobile và trang quản trị sau mỗi lần sửa (lần lưu sau cùng thắng). Template của lịch gửi chỉ có **đúng 1 dòng** URL redirect, không tách thành 2 dòng theo nguồn lưu và không nhân đôi theo số lần sửa. Friend nhận đúng 1 tin, link mở đúng trang đích | | Lấp `G10` · `Q14` · RULE-06 · Đánh giá spec: Đã hỏi leader — repo **không có spec API mobile** để đối chiếu, nếu Dev xác nhận app mobile không có chức năng đặt lịch thì đánh × kèm lý do · Evidence: screenshot app mobile + trang quản trị sau từng lần sửa + bảng đếm dòng URL redirect |
| TC-JOB001-01 | Data | JOB-001 | Lệnh khôi phục cột URL `recover:templateUrlRedirect` | Normal | manual | product | Chạy lại lệnh khôi phục cột URL sau fix — điền đủ URL cho bản ghi cũ bị cắt | Đã áp migration #40909. Trên DB còn bản ghi URL redirect có cột URL **để trống** hoặc **bị cắt 255 ký tự**. Ghi lại danh sách các bản ghi này | 1. Ghi lại số bản ghi có cột URL trống và số bản ghi có URL bị cắt<br>2. Chạy lệnh khôi phục cột URL<br>3. Đọc thông báo kết thúc: số bản ghi đã xử lý<br>4. Kiểm lại từng bản ghi trong danh sách đã ghi: cột URL có được điền đầy đủ không, có khớp URL gốc không<br>5. Mở màn soạn tin của các template đó và kiểm cấu hình URL còn nguyên<br>6. Gọi lại chức năng nạp dữ liệu xem trước cho các template đó | Các bản ghi URL redirect có cột URL trống hoặc bị cắt | Bản ghi có URL gốc còn tồn tại → cột URL được điền **đầy đủ, không bị cắt** (nhờ cột đã là kiểu dài). Bản ghi có URL gốc đã bị xoá → xử lý đúng như trước (để trống), không gây lỗi. Cấu hình URL trên màn soạn tin không bị mất. Sau khi khôi phục, chức năng nạp dữ liệu xem trước trả về đúng nội dung người dùng đã lưu | | Lấp `G6` · `Q10` · regression — dẫn từ `TC-TMT-165` · RULE-08 · Đánh giá spec: Spec không ghi · Evidence: bảng đối chiếu bản ghi trước/sau + kết quả nạp dữ liệu xem trước |
| TC-PERFLARGE001-01 | UI | PERF-LARGE-001 | Soạn tin Text có URL — hiệu năng | Boundary | manual | product | Đo thời gian lưu template trên chính dữ liệu đã nhân đôi của ticket, theo ngưỡng đã chốt | Trên production, chọn đúng template của bot 41216 nêu trong ticket (hoặc template có số dòng URL redirect trùng lớn tương đương). **Leader đã chốt ngưỡng thời gian trước khi chạy**. Đã backup | 1. Ghi lại số dòng URL redirect hiện có của template đó và số lần đã nhân đôi<br>2. Bấm 「登録」 và bấm giờ, ghi lại thời gian trước khi dọn dữ liệu rác<br>3. Chạy lệnh dọn dữ liệu trùng cho template đó<br>4. Bấm 「登録」 lại và bấm giờ, đo 3 lần liên tiếp<br>5. Đếm số lượt hệ thống gọi ra trang đích trong các lần lưu<br>6. Đối chiếu số đo với ngưỡng leader đã chốt và với mốc 246 giây của ticket | Template thật của bot 41216 có URL dài 769 ký tự | Thời gian lưu sau khi dọn **đạt ngưỡng leader đã chốt** (ghi con số cụ thể, không dùng "nhanh hơn rõ rệt"). Số dòng URL redirect **không tăng** sau 3 lần lưu. Không phát sinh lượt gọi ra trang đích khi đã có dữ liệu xem trước. Ghi lại đủ 4 số đo: trước dọn, sau dọn lần 1/2/3 | | Lấp `Q11` · `I4` — cần ngưỡng trước khi chạy · RULE-08 · Đánh giá spec: Đã hỏi leader — chờ chốt ngưỡng · Evidence: bảng 4 số đo + số lượt gọi ra ngoài + số dòng trước/sau |
| TC-REGURL001-01 | UI | REG-URL-001 | Màn 「URL分析」 sau khi dọn dữ liệu trùng | Normal | manual | product | Số liệu phân tích URL không đổi sau khi dọn bản ghi trùng | Trên production (hoặc bản sao có dữ liệu thật), chọn 1 URL thuộc nhóm trùng sắp bị dọn và đang có lượt click tích luỹ. Ghi lại số liệu ở màn 「URL分析」 | 1. Mở màn 「URL分析」 `/basic/url`, ghi lại số lượt click và các số liệu thống kê của URL đó<br>2. Chạy lệnh dọn dữ liệu trùng ở chế độ chạy thật<br>3. Mở lại màn 「URL分析」 và so số liệu với trước khi dọn<br>4. Bấm link rút gọn 1 lần và kiểm số liệu có tăng đúng 1 không<br>5. Kiểm URL vẫn xuất hiện đúng 1 lần trong danh sách, không mất, không lặp | URL thuộc nhóm trùng, đã có lượt click tích luỹ | Số liệu click tích luỹ **giữ nguyên** sau khi dọn, không bị về 0 và không bị cộng dồn sai. URL hiển thị đúng 1 lần trong danh sách. Click mới được ghi nhận bình thường | | Lấp `G8` · RULE-08 · Đánh giá spec: Spec không ghi · Evidence: screenshot màn URL分析 trước/sau + số liệu sau click thử |


**GAP / RISK không đề xuất TC** — Leader quyết định 2026-09-15 là các TC này không cần thiết; ghi lại để §1 / §2 không có dòng nào bị bỏ trống:

| Mục | Nội dung | Lý do không đề xuất TC |
|---|---|---|
| `G3` / `Q3` | Migration ALTER khoá bảng khi đang có request lưu template (`DEPLOY-LIVE-001`) | Leader đánh giá không cần TC — xử lý bằng quy trình vận hành (chạy giờ thấp điểm) thay vì test case |
| `G9` / `Q12` | Command DELETE chạy đồng thời với request lưu template (`CONC-001`) | Leader đánh giá không cần TC — cùng lý do vận hành như trên |
| `Q6` | Backup / khôi phục bảng trước ALTER và trước command DELETE (`DATA-BACKUP-001`) | Leader đánh giá không cần TC — thuộc quy trình release, không phải phạm vi test chức năng |
| `Q2` (một phần) | Chốt sức chứa 2 cột lưu URL trên production (`ENV-003`) | Leader quyết định bỏ `TC-ENV003-01` 2026-09-15. Phần còn lại của `Q2` vẫn được `TC-ENV003-02` + `TC-FUNC004-02` phủ; riêng việc đo sức chứa cột `url.url` (varchar 500?) nay **không còn TC nào** — rủi ro URL > 500 ký tự sinh mã URL mới mỗi lần lưu vẫn để ngỏ |
| `Q8` | Test luồng template V1 legacy (`MessageTemplateController::saveUrlDetect`) (`REG-SHARED-001`) | Leader đánh giá không cần TC. Phần copy/backup template của `Q8` vẫn còn `TC-REGSHARED001-02` phủ |

> ⚠️ 4 mục trên **vẫn được giữ nguyên ở §1 / §2** vì đó là kết quả rà coverage. Bỏ TC không làm GAP biến mất — chỉ ghi nhận là đã có quyết định chấp nhận rủi ro.


---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | `spec-features/admin/message-template/feature-spec.md` — SCR-TMT-05 「URL表示期限・アクション設定」 · `db-mapping.md` §4.8 | **Hành vi làm mới dữ liệu xem trước (OGP) đã đổi có chủ ý**: trước đây mỗi lần lưu template đều fetch lại và ghi đè `url.metadata`; sau fix chỉ fetch khi cache rỗng. Spec hiện chỉ ghi các cột `meta_title`/`meta_image`/`meta_description` là "OGP metadata cache", **không mô tả khi nào cache được làm mới** | `NEW-14` (Studio) đặt expected = preview giữ nội dung CŨ **vs** kho FA-010 `TC-TMT-145` "xoá ảnh preview URL rồi lưu → hệ thống **lấy lại ảnh default** của link". Hai kỳ vọng có thể mâu thuẫn khi cache đã có dữ liệu — **review không tự chọn bên** | **PM / Leader** chốt hành vi mong muốn, rồi Dev cập nhật spec + điều chỉnh `TC-TMT-145` hoặc `NEW-14` cho khớp |
| 2 | `spec-features/admin/message-template/db/db-mapping.md` §2.6 `template_url_redirect` | Cột `url` đã đổi `varchar(255)` → **TEXT** và bảng có thêm **index `(template_id, url_id)`**. Cần ghi thêm **quy tắc dedupe mới**: một cặp `(template_id, bot_id, url_id)` chỉ giữ 1 bản ghi, ưu tiên bản `id` lớn nhất | Migration `2026_09_14_100000` **vs** spec hiện tại ghi `url` là `varchar(255)` và bảng chỉ có PRIMARY | **Dev** cập nhật sau khi migration lên production |
| 3 | `spec-features/admin/message-template/db/db-mapping.md` — bảng `url` (connection `mysql_url`) | Spec **không khai báo** độ dài cột `url.url`. Đây lại là yếu tố **quyết định** bug 769 ký tự đã hết hay chưa (`NEW-32`). Cần bổ sung cấu trúc bảng `url` kèm ghi chú `strict = false` khiến chuỗi bị cắt âm thầm | ✅ **ĐÃ XÁC NHẬN bằng thực thi** — `NEW-32` (run #1423, pass): `url.url = varchar(500)`, `strict = false`, `sql_mode = NO_ENGINE_SUBSTITUTION`, lưu 769 ký tự → **thực tế lưu 500**. Không còn là lời Dev — đã có bằng chứng, và đây chính là nguyên nhân bug **#40968** | **Dev** xác nhận con số thật trên production rồi bổ sung spec |
| 4 | `spec-features/admin/message-template/web/logic-spec.md` §BR — `detectUrlInMessageTextV2`, `ajaxMetadataUrlAll` | Ghi rõ 2 hàm này **vẫn so khớp theo chuỗi URL** (không theo `url_id`) và **hệ quả đã biết** với bản ghi cũ có URL bị cắt: preview trả về dữ liệu lấy mới từ trang đích thay vì cấu hình người dùng đã lưu | `NEW-24` đặt expected cho nửa đầu là "hiển thị sai nội dung người dùng đã lưu — hạn chế đã biết". Hạn chế này **chưa có ở spec** → tester sau dễ raise bug trùng | **Dev / Leader** — ghi vào spec kèm điều kiện thoát (lưu lại 1 lần là hết) |
| 5 | Quy trình vận hành release (không phải spec màn hình) | Thứ tự bắt buộc khi lên production: (1) backup bảng → (2) migration `2026_09_14_100000` → (3) `recover:cleanDuplicateTemplateUrlRedirect --dry-run` → (4) chạy thật → (5) `recover:templateUrlRedirect` → (6) ticket sau mới thêm unique index `(template_id, url_id)`. Tất cả vào **giờ thấp điểm** | Journal Redmine #136301 mục "Rủi ro / lưu ý khi test" nêu thứ tự này nhưng **không nằm trong tài liệu nào** — nếu QA/vận hành làm sai thứ tự, migration unique index sẽ FAIL hoặc dữ liệu rác còn nguyên | **Leader / Dev** — chốt thành checklist release kèm ticket |
| 6 | `spec-features/admin/chat-11/feature-spec.md` — Luồng 4 「Hẹn gửi tin nhắn」 · `spec-features/admin/message-template/web/logic-spec.md` §7.4 | Luồng 4 của chat-11 chỉ mô tả từ bước `POST /basic/save-schedule-send` trở đi — **thiếu hẳn bước soạn/chọn/sửa template** trước đó và **không nói template của lịch gửi được lưu qua `/ajax/template-v2/save-template` với `action_type=schedule_send`**. Cần bổ sung: luồng lưu template của 送信予約 (web và app mobile), quan hệ với `CATEGORY_SCHEDULE_SEND_CHAT_TEXT = -1111`, và **`saveScheduleSendChat` có dùng chung `saveText` hay có bản sao dedupe riêng** | `logic-spec.md:197` + `api-spec.md` EP-30 (có `action_type=schedule_send`) **vs** `chat-11/feature-spec.md` Luồng 4 (không nhắc template save) — khoảng trống này làm cả Dev lẫn QA bỏ sót một luồng lưu template khi đánh giá ảnh hưởng | **Dev** xác nhận nhánh code, rồi bổ sung spec cả 2 phía |
| 7 | `database/migrations/2026_09_14_100000_change_url_text_add_index_template_url_redirect_table.php` · `spec-features/admin/message-template/db/db-mapping.md` §2.6 | **Migration của ticket đổi `template_url_redirect.url` sang `TEXT`, nhưng production quyết định dùng `varchar(1024)`.** Cần chốt: (a) sửa migration thành `varchar(1024)` cho khớp mọi môi trường, hay (b) chấp nhận dev/staging = TEXT còn production = `varchar(1024)` và ghi rõ khác biệt này vào spec + checklist release. Kèm theo phải chốt **hành vi khi URL vượt 1024 ký tự** — `varchar(1024)` + `strict = false` vẫn cắt âm thầm y như `varchar(255)` trước đây, tức là **chỉ dời biên chứ không loại bỏ cơ chế đã gây bug** | Quyết định vận hành của Leader (production `varchar(1024)`) **vs** migration trong branch `ai_small_40909` (TEXT). Lệch này làm `NEW-30` / `NEW-33` ra kết quả sai trên production (xem §4 `I10`) và sinh ra biên mới 1024 chưa TC nào phủ (đã bổ sung `TC-FUNC004-02`) | **Leader + Dev** chốt trước khi release; Dev cập nhật migration và `db-mapping.md` theo quyết định |
