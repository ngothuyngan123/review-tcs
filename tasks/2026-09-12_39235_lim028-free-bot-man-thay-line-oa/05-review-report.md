# 05 — Review Report

## 0. Nguồn TC

- **Nguồn đã dùng**: MCP LME TEST STUDIO — task #253 (ticket 39235, feature `bot-edit`, branch `ai_fixbug_39235`, round 1, `reviewState=leader`)
- **Tổng số TC review**: 14

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: 7/13 vùng ảnh hưởng đủ TC · **6 GAP** · **3 RISK**

| # | Chiều | Vùng ảnh hưởng | TC hiện có | Status | Vấn đề | Severity |
|---|---|---|---|---|---|---|
| **G1** | `diff code` | `Bots::isFreePlan()` — điều kiện **OR** (`plan_type == 2` **HOẶC** `contract_type == 'free'`) | NEW-1 (contract free), NEW-2 (plan_type=2), NEW-10 (chưa gắn slot) | **GAP** | Chưa TC nào dựng bot có **`plan_type = 2` NHƯNG hợp đồng `standard`/`pro`** (bot Free cũ đã nâng gói mà cột `plan_type` chưa đồng bộ). Vế OR sẽ trả `true` → **chặn oan bot đang trả tiền**. Studio `dev_impact` gọi OVER-BLOCK là *"rủi ro hồi quy chính"* nhưng đúng tổ hợp nguy hiểm nhất lại không có TC. | `[BLOCKER]` |
| **G2** | `dev-impact` F1/F4 + `diff code` | Guard đọc gói của **bot nào** — bot trong URL hay bot đang chọn trong session | NEW-12 (chỉ bot của **admin khác**) | **GAP** | Chưa TC nào kiểm **cross-bot cùng admin**: đang chọn bot trả phí B trong session rồi mở URL của bot Free A đã hết campaign (và chiều ngược lại). Nếu guard đọc gói theo **session** thay vì theo **bot decode từ URL** → đi thẳng lại vào đúng bug gốc mà toàn bộ 14 TC vẫn `pass`. | `[BLOCKER]` |
| **G3** | `dev-impact` F5/T4 (REQ-007) | Tầng API đổi LOA — `ChangeBotController::init` / `botCanChangeBot` / `execute` | — | **GAP** | **0 TC**. Dev tự kê ở mục "quét ngang": 3 điểm này vẫn nhận diện Free **chỉ bằng `plan_type = 2`** → bot Free **mới** hết campaign vẫn gọi thẳng API đổi LOA được. Studio cũng khai REQ-007 nhưng không sinh TC nào. Không có TC = **không có bằng chứng hiện trạng để mở ticket tách** như Dev đề nghị. | `[BLOCKER]` |
| **G4** | `diff code` | Nhánh `type_change = 'scheduled'` — luồng **đặt lịch** đổi LOA | — | **GAP** | **0 TC**. Studio `dev_impact` liệt kê đây là bất đối xứng out-of-scope còn lại; Dev cũng tự nêu rủi ro: *"bot miễn phí đang chạy dở tiến trình đổi LOA mà campaign hết hạn đúng lúc đó sẽ không mở lại được màn theo dõi tiến độ"*. Cũng chính là vế 2 của REQ-006 (*"luồng đặt lịch đổi LINE OA"*) không TC nào chạm. **Leader đã chốt hành vi đúng (2026-09-12)**: chặn màn nhưng **lịch đã đặt vẫn phải chạy đúng hẹn** → cần TC chứng minh guard mới không làm hủy/hoãn/fail lịch đang chờ. Guard nằm ở hàm dựng màn nên trên lý thuyết không chạm job, nhưng chưa có bằng chứng nào. | `[BLOCKER]` |
| **G5** | `dev-impact` T2 (FA-032) | Màn đích sau khi bị chặn — `/admin/bot-add` | NEW-4 (chỉ ghi lại URL dừng lại) | **RISK** | TC chỉ verify **địa chỉ** trình duyệt dừng ở `/admin/bot-add`, không verify màn đó **render đúng và dùng được** (danh sách slot, nút thêm tài khoản, không lỗi JS). RULE-06 — expected dừng trước output cuối chuỗi. | `[MAJOR]` |
| **G6** | `diff code` | Cửa sổ campaign **1 tháng** vs cột `has_campaign` (**7 ngày**) — 2 cơ chế khác nhau | NEW-7 (3 ngày tuổi), NEW-14 (đúng mốc 1 tháng) | **RISK** | Theo spec `bot-add-v2/db/db-mapping.md:58,353`, `has_campaign` / `expired_date_campaign` là campaign **7 ngày** (`created_at + 7 ngày 23:59`), **không phải** mốc 1 tháng của guard mới. Nhiều TC lại đưa `has_campaign = 0` vào tiền đề như thể đó là điều kiện chặn. Vùng **8–29 ngày tuổi** (`has_campaign = 0` nhưng **còn** trong cửa sổ 1 tháng) không có TC Normal nào → nếu code nhầm sang `has_campaign` thì chặn oan mà bộ TC vẫn xanh. | `[MAJOR]` |
| **G7** | `diff code` | Bảng chân trị điều kiện `isFreePlan() AND quá 1 tháng` — tổ hợp **không Free + còn trong 1 tháng** | — | **RISK** | 3/4 tổ hợp có TC; riêng "bot trả phí mới tạo < 1 tháng" không có (NEW-9 ghi rõ cả 3 bot *"đều đã tạo quá 1 tháng"*). Rủi ro thấp vì cùng lớp tương đương với NEW-9, nhưng là tổ hợp duy nhất còn trống. | `[MINOR]` |

**Chiều `diff code` — nguồn**: Studio `spec_delta` (`diffAvailable = true`, `diffStat` = `app/Http/Controllers/Admin/BotController.php | 10 ++++++++++`, 1 file changed) + `dev_impact` do Studio tính từ diff.
**4 câu hỏi adversarial**: (1) file duy nhất bị sửa có TC đi qua — **OK**; (2) điều kiện mới có TC cả 2 nhánh true/false — **OK**, không phải generic-fix nên không áp rule ≥3 trigger; (3) fix chạm hàm dùng chung `Bots::isFreePlan()` nhưng **không sửa** hàm, Dev đã kê đủ caller (`ChatController` / `ActionScheduleController` / `BackupController`) → **không cần TC cho caller downstream**; (4) hành vi đổi là **siết lại**, có TC cả hành vi mới (NEW-1/2/3/5/6/11/15) lẫn chỗ cũ không hỏng (NEW-7/9/10/17) — **OK**.

---

## 2. Thiếu so với quan điểm test

**Kết luận**: 11 quan điểm Trigger khớp task · 4 đã cover đủ · **7 chưa cover đủ**

| # | Mã quan điểm | Ưu tiên | Trigger khớp vì | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|---|
| **Q1** | `PAY-LIMIT-001` | **Cao** | *BẮT BUỘC khi chức năng bị giới hạn theo gói* — đây là **quan điểm cốt lõi** của cả ticket | **0 TC gắn mã này** | Không TC nào kiểm: (a) **nâng gói Free → Standard xong có vào được ngay không** (hay phải logout / chờ cache) — đây là đường thoát của khách bị chặn; (b) **tài khoản cũ vs mới** (checklist yêu cầu rõ: *"test cả tài khoản cũ tạo trước khi đổi giá/spec và mới"*); (c) gói **enterprise** — NEW-9 **cố ý loại** với lý do *"LIM-028 không có evidence"*, nhưng `isFreePlan()` vẫn chạy trên bot enterprise. | `[BLOCKER]` |
| **Q2** | `FUNC-DATE-001` | **Cao** | guard so sánh `created_at + 1 tháng (endOfDay)` với `now` | NEW-14, NEW-15 (cả 2 đều `Boundary`) | (a) **Ngày tạo là ngày cuối tháng** — `31/01 + 1 tháng` = 28/02 hay 03/03? REQ-003 **nêu đích danh** *"ngày tạo là ngày cuối tháng phải xác định được"* mà **0 TC**; đây là bẫy kinh điển của `addMonth()`. (b) **Timezone** JST server vs máy tester. (c) Mốc chuyển `23:59:59 → 00:00:00` (NEW-14 chỉ *ghi chú* rủi ro này, không test). (d) RULE-01: quan điểm Cao chỉ có `Boundary`, **thiếu Normal + Abnormal**, không ghi lý do. | `[BLOCKER]` |
| **Q3** | `PERM-003` | **Cao** | *BẮT BUỘC khi tổ chức vận hành nhiều LINE OA hoặc có chức năng **change bot*** — task chính là màn change bot | **0 TC gắn mã này** | Không TC nào kiểm cách ly đa LINE OA tại màn này: switch qua lại giữa bot Free và bot trả phí rồi mở màn → màn phải bám đúng bot trong URL. Xem G2. | `[BLOCKER]` |
| **Q4** | `PERM-001` | **Cao** | *BẮT BUỘC khi chức năng có phân biệt quyền* — guard quyền `userCanAccessChangeBot` đứng ngay trước guard gói | NEW-11 (1 TC, `Normal`, staff **đã chấp nhận** lời mời) | Thiếu **role matrix**: staff **chưa chấp nhận** lời mời, staff **đã bị gỡ** khỏi bot, các `user.level` khác (spec EP-09 có `roleId`). Checklist ghi rõ *"phải có role matrix trong spec trước khi test"* — chưa có. RULE-01: thiếu `Abnormal` + `Boundary`. | `[MAJOR]` |
| **Q5** | `ENV-003` | **Cao** | *BẮT BUỘC khi tính năng chạm **thanh toán*** — guard đọc `bot_contracts.contract_type`, chặn theo gói | 14/14 TC chỉ chạy `env = local` | **0 TC chạy production**. Catalog D: *"Bill tiền: dev/staging account test, production **account thật**"*. Dữ liệu gói/hợp đồng thật (Free cũ trước 2021-07-01, enterprise, hợp đồng quá hạn) **chỉ tồn tại trên production**. RULE-08 cấm kết luận từ staging cho hạng mục bill tiền. | `[MAJOR]` |
| **Q6** | `REG-URL-001` | Trung bình → **BẮT BUỘC (nâng Cao)** | *khi CR chạm màn đăng ký / login / overview / thanh toán / **kết nối bot*** — fix **đổi đích điều hướng** của màn kết nối bot | **0 TC** | Checklist liệt kê **đích danh** `https://step.lme.jp/admin/home` và `https://step.lme.jp/basic/overview` là URL đo lường conversion **không được đổi**. Bản fix chuyển đích sang `/admin/bot-add`, trong khi spec BR-29 ghi `/admin/home` và NEW-12 lại chốt `/basic/overview` — 3 URL đo lường đều bị động chạm mà không TC nào verify sau release (200, không redirect lạ, có/không dấu `/` cuối, param thừa). | `[MAJOR]` |
| **Q7** | `COMPAT-LEGACY-001` | **Cao** | Free cũ ⇄ Free mới là cặp version-up điển hình; mốc chính sách `flagChangeFreePlan` 2021-07-01 (spec BR-35) | NEW-1 (Free mới), NEW-2 (Free cũ) | NEW-2 **tự nhận là "Free cũ" nhưng dùng dataset khác bug gốc**: `ngày tạo = 2024-01-15` (**sau** 2021-07-01) và **không set `flag_contract_new`**. Bug repro bước 1 ghi rõ *"`plan_type=2`, `flag_contract_new=0`, ngày tạo **trước 2021-07-01**"*. Cặp legacy thật (trước/sau mốc 2021-07-01) chưa TC nào chạy → nhánh dữ liệu gây bug gốc **chưa được kiểm chứng**. | `[MAJOR]` |

**Đã cover đủ, không ghi ra**: `FUNC-001` · `PERM-002` · `REG-SHARED-001` · `STATE-001`.

---

## 3. TC trùng lặp nội dung

Đã rà **14** TC theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`) — phát hiện **1 nhóm**.

| Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| N1 | **NEW-4** (`TC-OUTTRUTH001-01`) — quan sát kỹ hơn (HTTP code, tiêu đề tab, network panel) | **NEW-1** (`TC-TOOLKNOW002-01`) → **GỘP, KHÔNG XÓA** | `DUP-SUBSET` | Loại case `Normal` · đối tượng + thao tác **giống hệt** (mở màn thay LINE OA bằng **URL trực tiếp** với bot `TC39235-FREE-NEW-HET`) · tiền đề tương đương (NEW-4 chỉ thêm bước bật network panel) · expected tương đương (trang chặn 200 → dialog → `/admin/bot-add`). Chỉ khác **mã quan điểm** (`TOOL-KNOW-002` vs `OUT-TRUTH-001`) — mà `TOOL-KNOW-002` là mã nội bộ Studio, không tính coverage. | `[MINOR]` |

> **Gate đã chạy**: giả định xóa NEW-1 → mất **bước kiểm dữ liệu giữ nguyên** (*"không phát sinh bản ghi lịch đổi LINE OA mới, thông tin kết nối bot không đổi"*) mà NEW-4 không có. Vì vậy đề xuất là **GỘP** (bổ sung bước kiểm dữ liệu của NEW-1 vào NEW-4), **không xóa**.
> **Không có `DUP-INFLATE`** — không nhóm trùng nào đang che GAP ở §1/§2.
> Các cặp **thoạt nhìn giống nhau nhưng KHÔNG trùng** (khác trục đường vào / khác lớp dữ liệu, giữ nguyên cả): NEW-2 (URL trực tiếp) ⇄ NEW-3 (menu trái) ⇄ NEW-6 (nút ở SCR-BE-01) ⇄ NEW-5 (URL có param `type=change_sub&change_success=1`); NEW-9 (3 gói trả phí) ⇄ NEW-10 (chưa gắn slot); NEW-14 ⇄ NEW-15 (cặp biên trong/ngoài, cố ý).

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[MAJOR]` | Toàn bộ 14 TC | **RULE-02 — `Evidence thực tế` rỗng 100%** dù cả 14 TC đều ghi `Đạt`. Kết quả là **tự khai**, không có ảnh/log/HTTP trace để Leader đối chiếu. | Yêu cầu đính evidence đúng loại cho từng quan điểm trước khi công nhận `Đạt`; tối thiểu với 4 TC lõi (NEW-1, NEW-2, NEW-7, NEW-9). |
| 2 | `[MAJOR]` | Toàn bộ 14 TC | **RULE-08 / ENV-003** — 14/14 chạy `env = local`, **1 run duy nhất** (`runId 795`, `by = pipeline-manual`, `source = ai` → **AI pipeline chạy, không phải QA người**). 0 run ở dev / staging / production. Cộng thêm: Dev **cũng không kết nối được MySQL dev** khi fix (`Connection refused`), verify chỉ ở mức `php -l` → **cả fix lẫn test đều chưa chạm dữ liệu thật**. | Chạy lại tối thiểu bộ chặn/không-chặn trên **production** với account thật (xem `TC-ENV003-01` §5). |
| 3 | `[MAJOR]` | `03-dev-impact.md` | File có `Auto-filled: 2026-09-12 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" CHƯA tick** → F/D/T có thể thiếu hoặc map sai; review coverage dựa trên nó chưa có giá trị chốt. | Tester đọc lại Redmine #39235 Journal #133261, đối chiếu 4 mục rồi tick checkbox. |
| 4 | `[MAJOR]` | Toàn bộ 14 TC | Cột **`Trạng thái đánh giá spec` rỗng** (`spec_status = null`) ở cả 14 TC — trong khi bộ TC này **đã biết có conflict expected** (chính `note` của NEW-4 ghi *"TC CHỜ LEADER CHỐT"*). Ghi `Đạt` mà không ghi trạng thái spec = tự suy diễn rồi cho qua. | Điền `Đã hỏi leader` cho NEW-4 và các TC dùng chung expected trang chặn (NEW-1, 2, 3, 5, 6, 11, 15) sau khi §6 được chốt. |
| 5 | `[MAJOR]` | **SPEC-CONFLICT** — NEW-4, NEW-12 | Đích điều hướng khi bị chặn có **4 phiên bản khác nhau** từ 4 nguồn: spec `feature-spec.md:262` + BR-29 → `/admin/home`; ticket #39235 → *"trang chủ admin"*; bản fix + NEW-4 → trang chặn HTTP 200 → `/admin/bot-add`; NEW-12 (bot không hợp lệ) → `/basic/overview`. **Không tự chọn bên** → đẩy sang §6. | Leader/PM chốt 1 đích duy nhất, rồi sửa TC **và/hoặc** code cho khớp. |
| 6 | `[MAJOR]` | `[AP-4]` — `03-dev-impact.md` mục "Commit / Pull Request" | Chỉ có commit hash `3057ac6942` + branch, **không có PR link**; Studio `spec_delta` cho `diffStat` (1 file, +10 dòng) nhưng **không cho nội dung 10 dòng**. Không verify được: guard thực sự gọi `Bots::isFreePlan()` hay lại hardcode `plan_type == 2`; `endOfDay` được tính bằng gì; guard đặt **trước hay sau** `userCanAccessChangeBot`. | Yêu cầu Dev dán PR link / 10 dòng diff để review chốt được shape của fix. |
| 7 | `[MAJOR]` | NEW-1, NEW-6, NEW-9, NEW-10, NEW-12 (5/14 TC) | Mang mã quan điểm `TOOL-KNOW-002` / `TOOL-SCOPE-001` / `TOOL-NEGCTRL-001` / `TOOL-ERRHYG-001` — **không có trong `framework/checklist-lme.md`** (mã nội bộ Studio) → **không tính là cover** ở §1/§2. | Gắn lại mã LME tương ứng trên Studio (`testcase_update`), vd NEW-9/NEW-10 → `PAY-LIMIT-001`, NEW-12 → `PERM-002`. |
| 8 | `[MINOR]` | Task #253 | `temp_id` **đứt quãng**: có NEW-1…NEW-7, NEW-9…NEW-12, NEW-14, NEW-15, NEW-17 — **thiếu NEW-8, NEW-13, NEW-16** (14 TC nhưng đánh số tới 17). | Leader xác nhận 3 TC này bị xóa có chủ đích hay mất; nếu bị xóa thì xem lại chúng cover gì. |
| 9 | `[MINOR]` | Toàn bộ 14 TC | TC đều ở `status = draft` và `reviewed = false`, trong khi task đã `status = done-ai` / `aiResult = pass`. Trạng thái TC và trạng thái task lệch nhau. | Chốt review trên Studio rồi chuyển TC sang trạng thái chính thức. |
| 10 | `[NIT]` | NEW-9, NEW-17 | Khai `env_scope = ["all"]` (chạy được cả production) nhưng thực tế **chưa chạy môi trường nào ngoài local** — khai phạm vi ≠ đã chạy. | Chạy 2 TC này trên production (đều là `read-only`, chỉ điều hướng). |

### Chất lượng từng TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 11 | `[MAJOR]` | NEW-9 (`TC-TOOLNEGCTRL001-01`) | **Tiền đề không dựng lại được** — *"Chuẩn bị 3 bot trả phí: Standard cũ, Standard mới, Pro. Mỗi bot có dữ liệu gói/hợp đồng đúng loại"* không nói **`plan_type` bằng bao nhiêu** cho từng loại, cũng không nói dựng hợp đồng thế nào. Đây lại đúng là biến quyết định của `isFreePlan()`. Người khác chạy sẽ ra dataset khác. | Ghi rõ từng bot: `plan_type` + `bot_contracts.contract_type` + ngày tạo. |
| 12 | `[MINOR]` | NEW-9 | **Không atomic** — 1 TC chạy 3 bot khác gói với 3 kết quả có thể khác nhau; 1 gói fail thì cả TC fail mà không rõ gói nào. | Tách 3 TC theo gói, hoặc giữ 1 TC nhưng bảng kết quả riêng cho từng gói. |
| 13 | `[MINOR]` | NEW-12 (`TC-TOOLERRHYG001-01`) | Gộp 2 ca lỗi khác bản chất (mã bot rác vs mã bot của admin khác) vào 1 TC với 2 expected. | Tách 2 TC, hoặc ghi expected riêng cho từng ca. |
| 14 | `[MINOR]` | NEW-9 | Expected *"màn thay LINE OA hiển thị đầy đủ"* chưa đo lường được. | Nêu phần tử cụ thể phải thấy (đã có sẵn ở NEW-7: tiêu đề 「LINE公式アカウント入れ替え機能」, nút 「アカウント入れ替えをはじめる」). |
| 15 | `[NIT]` | NEW-7 (`FUNC-001`) | `FUNC-001` là quan điểm **Cao** nhưng chỉ có 1 TC `Normal` → hình thức là vi phạm RULE-01. Thực chất `Abnormal` (NEW-5) và `Boundary` (NEW-14/15) **đã có**, chỉ bị gắn sang mã quan điểm khác. | Chỉ cần gắn lại mã cho đúng, **không cần viết thêm TC**. |

> **Anti-pattern**: đã rà AP-1 → AP-6; chỉ dính **AP-4** (issue #6). AP-1 không áp dụng (fix không phải generic catch) · AP-2 không (root cause rõ) · AP-3 không (đã có NEW-9/10/17 làm đối chứng dương) · **AP-5 không** — NEW-10 và NEW-17 tuy chạm code không bị sửa nhưng đều là đối chứng hợp lệ (`isFreePlan()` có caller mới; màn anh em chứng minh không hồi quy), **không đề nghị bỏ** · AP-6 không (Dev đã kê đủ 9 function ở mục 3).
> **Nguồn spec đã dùng**: `spec-features/admin/bot-edit/feature-spec.md` §2.3 (SCR-BE-03) + §5.8 BR-29/BR-30 + §5.11 BR-35/BR-36 · `web/api-spec.md` EP-08/EP-09 · `db/db-mapping.md` §4.4 · `spec-features/admin/bot-add-v2/db/db-mapping.md` (campaign 7 ngày).

---

## 5. TCs đề xuất bổ sung (18)

**Đã đối chiếu**: 14 TC ở BƯỚC 0 + `kho-tcs/fa031-billtientool-契約プラン・決済情報.md` — không TC đề xuất nào trùng.

> ⚠️ **kho-tcs chưa có FA-038** (Cài đặt kết nối LOA) và **chưa có FA-032** (Thêm tài khoản mới) → không đối chiếu được vùng regression của chính màn này. Đã đối chiếu file gần nhất `fa031` (契約プラン・決済情報, cùng trục gói/hợp đồng): kho có TC về **hiển thị** gói (`TC-BLP-17`, `TC-BLP-18`) và **rule slot trống & bot free**, nhưng **không có TC nào chạm màn thay LINE OA** → không có TC dùng lại được, cũng **không phát sinh conflict expected** với kho.
> Đối chiếu `fa031` có 1 điểm đáng lưu ý cho `TC-PAYLIMIT001-01`: `MT-03` (CHỜ QUYẾT ĐỊNH) ghi nhận luồng upgrade fail có thể để bot ở trạng thái `contract_type = free` — tức bot có thể **rơi lại** về Free sau khi khách đã trả tiền. Đã đưa vào tiền đề của TC.

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-COMPATLEGACY001-01 | UI | COMPAT-LEGACY-001 | Màn thay LINE OA (SCR-BE-03) | Abnormal | manual | Tất cả | Bot Free cũ đã nâng lên hợp đồng Standard nhưng cột gói còn giá trị cũ thì KHÔNG bị chặn | Có bot 「TC39235-OLD2-STD」 từng là Free cũ, nay đã nâng gói: hợp đồng của bot là Standard còn hiệu lực, nhưng cột loại gói trên bot vẫn còn giá trị của Free cũ. Bot tạo cách hiện tại hơn 1 tháng. Đăng nhập bằng chủ tài khoản, đã chọn đúng bot này. | 1. Đăng nhập màn quản trị, chọn bot 「TC39235-OLD2-STD」<br>2. Mở màn 契約情報・領収書 xác nhận bot đang hiển thị gói 「スタンダード」 và hợp đồng còn hiệu lực<br>3. Mở màn thay LINE OA của bot này<br>4. Ghi lại có hộp thoại nâng gói hay không và màn dừng ở đâu | Bot có loại gói = 2 (giá trị Free cũ còn sót) nhưng hợp đồng đang gắn là standard, còn hiệu lực | KHÔNG bị chặn: không có hộp thoại 「スタンダードプラン以上のご契約でご利用できます」, vào được màn thay LINE OA, thấy nút 「アカウント入れ替えをはじめる」 và đi tiếp được tới bước chọn phương thức đổi. Khách đang trả tiền không bị khoá chức năng. | | Lấp G1 · Env: production ưu tiên (dữ liệu lệch kiểu này chỉ có ở data thật) · Đánh giá spec: Spec không ghi — **cần hỏi Dev**: điều kiện nhận diện Free là OR của 2 cột, nếu 1 cột còn giá trị cũ thì bot trả phí bị chặn oan · Evidence: ảnh màn 契約情報 + ảnh màn thay LINE OA + giá trị 2 cột gói |
| TC-COMPATLEGACY001-02 | UI | COMPAT-LEGACY-001 | Màn thay LINE OA (SCR-BE-03) | Normal | manual | product | Bot Free cũ đúng dataset bug gốc (tạo trước 2021-07-01, cờ hợp đồng mới = 0) bị chặn | Có bot Free cũ **tạo trước 2021-07-01** và cờ hợp đồng mới = 0 — đúng dataset ghi ở bước tái hiện của ticket. Đăng nhập bằng chủ tài khoản, đã chọn bot này. | 1. Xác nhận ngày tạo bot trước 2021-07-01 và cờ hợp đồng mới = 0<br>2. Mở màn thay LINE OA của bot bằng đường dẫn trực tiếp<br>3. Ghi lại hộp thoại, mã trả về và địa chỉ dừng lại<br>4. Lặp lại bằng menu trái 「有料プラン限定」→「LINE公式アカウント入れ替え」 | Bot Free cũ: loại gói = 2, cờ hợp đồng mới = 0, ngày tạo trước 2021-07-01 | Bị chặn ở cả 2 đường vào: trang chặn hiện ra, hộp thoại 「スタンダードプラン以上のご契約でご利用できます」 bật lên rồi chuyển sang màn kết nối/thêm LOA. Không thấy bước chọn phương thức đổi. | | Lấp Q7 · TC hiện có NEW-2 khai là "Free cũ" nhưng dùng ngày tạo 2024-01-15 (sau mốc chính sách) và không set cờ hợp đồng mới → chưa kiểm chứng nhánh gây bug gốc · Đánh giá spec: Spec ghi rõ (BR-35 mốc 2021-07-01) · Evidence: ảnh màn + giá trị ngày tạo và cờ |
| TC-PERM003-01 | UI | PERM-003 | Màn thay LINE OA (SCR-BE-03) | Abnormal | manual | Tất cả | Đang chọn bot trả phí trong phiên nhưng mở URL của bot Free hết campaign thì vẫn bị chặn | Cùng 1 tài khoản chủ sở hữu **cả 2 bot**: 「TC39235-STD」 (trả phí) và 「TC39235-FREE-NEW-HET」 (Free mới, hết campaign). Đăng nhập bằng chủ tài khoản. | 1. Đăng nhập và chọn bot 「TC39235-STD」 (trả phí) trong danh sách bot — xác nhận thanh tiêu đề đang hiện tên bot này<br>2. KHÔNG đổi bot đang chọn, dán thẳng đường dẫn màn thay LINE OA **của bot 「TC39235-FREE-NEW-HET」** vào thanh địa chỉ rồi Enter<br>3. Quan sát hộp thoại cảnh báo, nội dung màn và địa chỉ dừng lại<br>4. Nếu vào được màn, ghi lại tên bot mà màn đang hiển thị | Phiên đang chọn bot trả phí; đường dẫn trỏ tới bot Free mới đã hết campaign; cả 2 bot cùng 1 tài khoản chủ | Bị chặn theo gói của **bot trên đường dẫn** (Free hết campaign), KHÔNG theo bot đang chọn trong phiên: trang chặn hiện ra, hộp thoại 「スタンダードプラン以上のご契約でご利用できます」 bật lên rồi chuyển sang màn kết nối/thêm LOA. Tuyệt đối không mở được bước chọn phương thức đổi. | | Lấp G2, Q3 · **Đây là đường bypass nguy hiểm nhất còn lại**: nếu chặn theo bot trong phiên thì bug gốc vẫn còn nguyên mà 14 TC hiện tại đều xanh · Evidence: ảnh thanh tiêu đề (bot đang chọn) + ảnh màn kết quả + URL |
| TC-PERM003-02 | UI | PERM-003 | Màn thay LINE OA (SCR-BE-03) | Normal | manual | Tất cả | Đang chọn bot Free hết campaign nhưng mở URL của bot trả phí thì vào được bình thường | Như TC-PERM003-01 (cùng tài khoản chủ có cả 2 bot). Đăng nhập bằng chủ tài khoản. | 1. Đăng nhập và chọn bot 「TC39235-FREE-NEW-HET」 (Free mới, hết campaign)<br>2. KHÔNG đổi bot đang chọn, dán đường dẫn màn thay LINE OA **của bot 「TC39235-STD」** (trả phí) rồi Enter<br>3. Quan sát có bị chặn không và màn đang hiển thị tên bot nào | Phiên đang chọn bot Free hết campaign; đường dẫn trỏ tới bot trả phí | Vào được màn thay LINE OA bình thường (không hộp thoại nâng gói) và màn hiển thị đúng thông tin của **bot trả phí trên đường dẫn**, không phải bot Free đang chọn trong phiên. | | Lấp G2, Q3 · Chiều ngược của TC-PERM003-01 — bắt lỗi chặn oan theo phiên · Evidence: ảnh màn + tên bot hiển thị trên màn |
| TC-PERM002-03 | API | PERM-002 | API đổi LINE OA (ChangeBotController) | Abnormal | manual | Tất cả | Ghi nhận hiện trạng: bot Free mới hết campaign gọi thẳng API đổi LOA có bị chặn không | Có bot 「TC39235-FREE-NEW-HET」 (Free mới, hết campaign). Đăng nhập bằng chủ tài khoản, đã chọn bot này. Mở sẵn bảng theo dõi yêu cầu mạng của trình duyệt. | 1. Đăng nhập, chọn bot 「TC39235-FREE-NEW-HET」<br>2. Mở màn thay LINE OA của **một bot trả phí** để bắt được yêu cầu khởi tạo dữ liệu màn trong bảng theo dõi mạng<br>3. Dùng chức năng gửi lại yêu cầu của trình duyệt, đổi mã bot trong yêu cầu sang mã của bot 「TC39235-FREE-NEW-HET」 rồi gửi<br>4. Ghi lại nguyên văn nội dung phản hồi<br>5. Lặp lại với yêu cầu thực thi đổi LINE OA | Mã bot đã mã hoá của bot Free mới hết campaign, đặt vào yêu cầu khởi tạo dữ liệu màn và yêu cầu thực thi đổi LOA | **TC ghi nhận hiện trạng, không chốt Đạt/Không đạt.** Ghi lại chính xác: API có chặn hay trả về dữ liệu bình thường. Dev đã kê trước là 2 điểm này chỉ nhận diện Free cũ → dự kiến **KHÔNG chặn** được bot Free mới. Kết quả dùng làm bằng chứng mở ticket tách. | | Lấp G3 (REQ-007) · Nhóm API vì phải tự gửi lại yêu cầu ngoài luồng bấm màn hình · **Không tính vào phán quyết Đạt/Không đạt của ticket #39235** — mục đích là lấy bằng chứng · Evidence: nguyên văn yêu cầu + phản hồi của cả 2 API |
| TC-STATEDEP001-01 | UI | STATE-DEP-001 | Màn thay LINE OA (SCR-BE-03) — luồng đặt lịch | Abnormal | manual | product | Bot Free hết campaign bị chặn vào màn nhưng lịch đổi LINE OA đã đặt vẫn chạy đúng hẹn | Có bot Free mới 「TC39235-FREE-SCHED」 **còn trong** cửa sổ 1 tháng, đã chọn phương thức **đặt lịch** đổi LINE OA và có 1 bản ghi lịch đổi đang ở trạng thái chờ xử lý, **giờ hẹn đặt sau thời điểm bot hết campaign**. Có sẵn LOA đích để đổi sang. Chuẩn bị được cách đẩy bot qua mốc hết campaign (đổi ngày tạo bot về quá 1 tháng). | 1. Với bot còn campaign, đặt lịch đổi LINE OA, hẹn giờ chạy **sau** thời điểm bot sẽ hết campaign — xác nhận màn theo dõi tiến độ mở được và lịch đang ở trạng thái chờ<br>2. Đưa bot qua mốc hết campaign (đổi ngày tạo bot thành quá 1 tháng)<br>3. Mở lại màn thay LINE OA của bot bằng cả đường dẫn thường và đường dẫn có tham số quay lại sau khi đổi xong → ghi lại kết quả<br>4. Chờ tới giờ hẹn của lịch đổi<br>5. Kiểm tra lịch đổi đã chạy chưa và chạy thành công không<br>6. Kiểm tra trên **LINE app thật**: bot đã đổi sang LOA đích chưa (kết bạn / gửi tin thử để xác nhận LOA mới hoạt động)<br>7. Ghi lại khách được thông báo kết quả đổi qua kênh nào (mail / thông báo trong tool) | Bot Free mới có 1 lịch đổi LINE OA trạng thái chờ, giờ hẹn nằm sau mốc hết campaign; ngày tạo bot bị đẩy qua mốc 1 tháng | **Chốt theo phương án (b) — Leader đã quyết**: (1) Mở màn thay LINE OA thì **bị chặn**: trang chặn hiện ra, hộp thoại 「スタンダードプラン以上のご契約でご利用できます」 bật lên rồi chuyển sang màn kết nối/thêm LOA — đúng như mọi bot Free hết campaign khác. (2) **Lịch đổi đã đặt VẪN chạy đúng hẹn và đổi LOA thành công** — việc bị chặn ở màn KHÔNG được hủy, hoãn hay làm fail lịch đã đặt. (3) Sau khi lịch chạy xong, bot trỏ sang LOA đích và **LOA mới hoạt động thật trên LINE app**. (4) Khách nhận được thông báo kết quả đổi qua kênh ngoài màn bị chặn. | | Lấp G4 · **Leader đã chốt phương án (b)** (2026-09-12): chặn màn nhưng lịch đã đặt vẫn chạy · Đánh giá spec: Đã hỏi leader — quyết định này **chưa có trong spec**, xem §6 mục 7 · **ENV = product**: lịch đổi chạy qua **job nền** + output cuối ra **LINE app thật** → RULE-08 + RULE-06 không kết luận được từ local/staging. Cần bot test riêng trên production và hẹn lịch trong khung giờ đã thống nhất với vận hành · Evidence: ảnh màn bị chặn (bước 3) + trạng thái lịch đổi trước/sau giờ hẹn + ảnh LOA mới hoạt động trên LINE app + ảnh thông báo gửi khách |
| TC-OUTTRUTH001-02 | UI | OUT-TRUTH-001 | Màn thêm/chọn tài khoản (FA-032 · /admin/bot-add) | Normal | manual | Tất cả | Màn đích sau khi bị chặn dùng được bình thường, không chỉ đúng địa chỉ | Có bot 「TC39235-FREE-NEW-HET」 (Free mới, hết campaign). Đăng nhập bằng chủ tài khoản, đã chọn bot này. | 1. Mở màn thay LINE OA của bot để bị chặn<br>2. Đóng hộp thoại cảnh báo, để trình duyệt tự chuyển sang màn đích<br>3. Quan sát màn đích: tiêu đề, danh sách slot/tài khoản, các nút thao tác<br>4. Bấm thử 1 thao tác chính của màn đích (vd chọn 1 slot / mở luồng thêm tài khoản)<br>5. Mở bảng theo dõi lỗi của trình duyệt, ghi lại có lỗi mã nguồn nào không | Không nhập liệu; bot Free mới đã hết campaign | Màn đích không chỉ đúng địa chỉ mà còn **render đầy đủ và thao tác được**: hiện đúng danh sách slot/tài khoản của tài khoản đang đăng nhập, nút thao tác bấm được, không có vùng trắng, bảng theo dõi lỗi của trình duyệt **không có lỗi mã nguồn**. | | Lấp G5 · RULE-06 — NEW-4 chỉ ghi lại địa chỉ dừng lại, chưa verify output cuối chuỗi · Evidence: ảnh toàn màn đích + ảnh bảng lỗi trình duyệt |
| TC-FUNCDATE001-03 | UI | FUNC-DATE-001 | Màn thay LINE OA (SCR-BE-03) | Normal | manual | Tất cả | Bot Free mới 15 ngày tuổi (đã hết campaign 7 ngày) vẫn vào được màn vì còn trong cửa sổ 1 tháng | Có bot Free mới 「TC39235-FREE-15D」 với ngày tạo **cách hiện tại 15 ngày**, đã gắn hợp đồng loại free. Xác nhận trước rằng cột đủ-điều-kiện-campaign của bot đang là 0 (vì đã quá 7 ngày). Đăng nhập bằng chủ tài khoản, đã chọn bot này. | 1. Xác nhận ngày tạo bot cách hiện tại 15 ngày và cột đủ-điều-kiện-campaign = 0<br>2. Mở màn thay LINE OA của bot này<br>3. Ghi lại có bị chặn hay không | Bot Free mới: ngày tạo = hiện tại trừ 15 ngày; cột đủ-điều-kiện-campaign = 0; hợp đồng loại free | **KHÔNG bị chặn** — vẫn vào được màn thay LINE OA vì cửa sổ của quy tắc này là **1 tháng**, không phải cột đủ-điều-kiện-campaign (vốn chỉ là campaign 7 ngày). Không có hộp thoại nâng gói. | | Lấp G6, Q2 (loại case Normal của FUNC-DATE-001) · Bắt lỗi nếu code nhầm sang cột campaign 7 ngày → chặn oan bot Free từ ngày thứ 8 · Đánh giá spec: Spec ghi rõ (campaign 7 ngày ở db-mapping bot-add-v2) · Evidence: ảnh màn + ngày tạo + giá trị cột campaign |
| TC-FUNCDATE001-04 | UI | FUNC-DATE-001 | Màn thay LINE OA (SCR-BE-03) | Boundary | manual | Tất cả | Bot Free tạo vào ngày 31 của tháng — mốc hết campaign rơi vào ngày nào | Có 2 bot Free mới: 「TC39235-D31」 ngày tạo **31/01** và 「TC39235-D30」 ngày tạo **30/01** (chọn cặp tháng sao cho tháng kế tiếp chỉ có 28 hoặc 29 ngày). Đăng nhập bằng chủ tài khoản. | 1. Đặt ngày hệ thống (hoặc chọn cặp ngày tạo) sao cho thời điểm chạy nằm **ngày 28/02** — tức đúng ngày cuối tháng kế tiếp<br>2. Mở màn thay LINE OA của bot 「TC39235-D31」, ghi lại bị chặn hay không<br>3. Lặp lại vào **ngày 01/03** với cùng bot đó<br>4. Lặp lại cả 2 mốc với bot 「TC39235-D30」<br>5. Ghi lại giờ máy chủ tại từng lần kiểm tra | Ngày tạo bot = 31/01 và 30/01; thời điểm kiểm tra = 28/02 và 01/03 | Hành vi tại mốc phải **xác định được và nhất quán** với công thức đang dùng ở màn thay tài khoản phụ, và phải ghi rõ mốc hết campaign của bot tạo ngày 31/01 là ngày nào. Không được xảy ra chênh lệch kết quả giữa 2 bot chỉ vì tháng kế tiếp thiếu ngày. | | Lấp Q2 · REQ-003 **nêu đích danh** case ngày cuối tháng mà 0 TC hiện có chạm; đây là bẫy kinh điển của phép cộng 1 tháng · Đánh giá spec: Spec không ghi — **cần hỏi Dev** công thức cộng tháng xử lý tràn ngày thế nào · Evidence: bảng 4 dòng kết quả (2 bot × 2 mốc) + giờ máy chủ từng lần |
| TC-FUNCDATE001-05 | UI | FUNC-DATE-001 | Màn thay LINE OA (SCR-BE-03) | Boundary | manual | Tất cả | Bot Free chuyển trạng thái đúng tại thời khắc 23:59:59 sang 00:00:00 | Có bot Free mới 「TC39235-BIEN-GIOPHUT」 với ngày tạo đúng bằng ngày hiện tại trừ 1 tháng. Chuẩn bị chạy ở 2 thời điểm sát nhau quanh nửa đêm. | 1. Ghi lại giờ máy chủ<br>2. Lúc **23:58** của ngày mốc, mở màn thay LINE OA của bot → ghi kết quả<br>3. Lúc **23:59:5x** cùng ngày, mở lại → ghi kết quả<br>4. Lúc **00:01** ngày hôm sau, mở lại → ghi kết quả<br>5. Đối chiếu 3 kết quả và giờ máy chủ từng lần | Bot Free mới, ngày tạo = hiện tại trừ đúng 1 tháng; 3 mốc kiểm tra: 23:58, 23:59:5x, 00:01 hôm sau | 2 lần đầu (trước 00:00) **vào được màn**; lần thứ 3 (sau 00:00) **bị chặn** với hộp thoại 「スタンダードプラン以上のご契約でご利用できます」. Thời khắc đảo trạng thái nằm đúng ranh giới cuối ngày, không sớm/muộn hơn. | | Lấp Q2 · NEW-14 chỉ *ghi chú* rủi ro chuyển ngày trong cột note chứ không test · Evidence: 3 ảnh màn kèm giờ máy chủ hiển thị rõ |
| TC-FUNCDATE001-06 | UI | FUNC-DATE-001 | Màn thay LINE OA (SCR-BE-03) | Abnormal | manual | Tất cả | Kết quả chặn bám theo giờ máy chủ, không đổi khi máy tester lệch múi giờ | Có bot Free mới 「TC39235-BIEN-TRONG」 với ngày tạo đúng bằng ngày hiện tại trừ 1 tháng (còn trong cửa sổ, chưa tới cuối ngày theo giờ máy chủ). Chuẩn bị 1 máy/trình duyệt đặt được múi giờ khác JST. | 1. Với máy đặt múi giờ JST, mở màn thay LINE OA của bot → ghi kết quả<br>2. Đổi múi giờ máy sang một múi **đã sang ngày hôm sau** so với JST, tải lại màn → ghi kết quả<br>3. Đổi sang một múi **còn ở ngày hôm trước** so với JST, tải lại → ghi kết quả<br>4. Đối chiếu 3 kết quả | Cùng 1 bot; 3 múi giờ máy khác nhau quanh mốc chuyển ngày | **Cả 3 lần cho cùng một kết quả** (vào được màn), vì mốc campaign tính theo giờ **máy chủ**. Nếu kết quả đổi theo múi giờ của máy tester thì việc chặn đang bị tính ở phía trình duyệt — sai. | | Lấp Q2 · Checklist FUNC-DATE-001 yêu cầu rõ trục timezone JST/local · Evidence: 3 ảnh màn kèm múi giờ máy đang đặt |
| TC-PAYLIMIT001-01 | UI | PAY-LIMIT-001 | Màn thay LINE OA (SCR-BE-03) | Normal | manual | product | Bot Free hết campaign sau khi nâng lên gói Standard thì vào được màn ngay, không cần đăng nhập lại | Có bot Free mới 「TC39235-FREE-UPG」 đã hết cửa sổ campaign và **đang bị chặn** ở màn thay LINE OA (xác nhận trước ở bước 1). Tài khoản có sẵn phương thức thanh toán để nâng gói. | 1. Mở màn thay LINE OA của bot → xác nhận **đang bị chặn**<br>2. Không đăng xuất, vào luồng nâng gói và hoàn tất nâng bot này lên Standard<br>3. Xác nhận ở màn 契約情報・領収書 rằng bot đã hiển thị gói 「スタンダード」<br>4. **Không đăng xuất, không xoá bộ nhớ đệm trình duyệt**, mở lại màn thay LINE OA<br>5. Ghi lại có còn bị chặn không | Bot Free mới hết campaign → nâng lên Standard bằng thanh toán thật | Sau khi nâng gói, mở lại màn thay LINE OA **vào được ngay trong cùng phiên đăng nhập**: không còn hộp thoại nâng gói, thấy nút 「アカウント入れ替えをはじめる」. Khách vừa trả tiền không phải đăng xuất/đăng nhập lại mới dùng được. | | Lấp Q1 · **RULE-08 → bắt buộc production** (thanh toán, account thật) · Đây là đường thoát duy nhất của khách bị chặn mà 0 TC hiện có kiểm · Lưu ý từ kho fa031 MT-03: nếu luồng nâng gói fail giữa chừng, hợp đồng có thể rơi lại về free → ghi lại trạng thái hợp đồng ở mỗi bước · Evidence: ảnh màn bị chặn (trước) + ảnh màn 契約情報 + ảnh màn vào được (sau), cùng 1 phiên |
| TC-PAYLIMIT001-02 | UI | PAY-LIMIT-001 | Màn thay LINE OA (SCR-BE-03) | Abnormal | manual | product | Bot gói Enterprise vào được màn thay LINE OA như các gói trả phí khác | Có bot thuộc gói **Enterprise** còn hiệu lực, tạo cách hiện tại hơn 1 tháng, thuộc tài khoản đang test. Đăng nhập bằng chủ tài khoản, đã chọn bot này. | 1. Đăng nhập, chọn bot gói Enterprise<br>2. Xác nhận ở màn 契約情報・領収書 rằng bot đang là Enterprise<br>3. Mở màn thay LINE OA của bot<br>4. Ghi lại có hộp thoại nâng gói hay không và màn dừng ở đâu | Bot gói Enterprise, hợp đồng còn hiệu lực, tạo quá 1 tháng | KHÔNG bị chặn: vào được màn thay LINE OA đầy đủ, thấy bước chọn phương thức đổi, giống hệt Standard và Pro. | | Lấp Q1 · NEW-9 **cố ý loại** Enterprise với lý do thiếu evidence, nhưng điều kiện chặn vẫn chạy trên bot Enterprise nên vẫn phải kiểm chứng · RULE-08 → production (gói Enterprise chỉ có ở data thật) · Evidence: ảnh màn 契約情報 + ảnh màn thay LINE OA |
| TC-PAYLIMIT001-03 | UI | PAY-LIMIT-001 | Màn thay LINE OA (SCR-BE-03) | Boundary | manual | product | Tài khoản cũ và tài khoản mới cho cùng kết quả ở cùng một gói | Chuẩn bị 2 tài khoản: (1) tài khoản **cũ** đăng ký trước mốc chính sách 2021-07-01, (2) tài khoản **mới** đăng ký gần đây. Mỗi tài khoản có 1 bot Free đã hết campaign và 1 bot Standard, đều tạo quá 1 tháng. | 1. Đăng nhập tài khoản cũ, mở màn thay LINE OA của bot Free hết campaign → ghi kết quả<br>2. Vẫn tài khoản cũ, mở màn của bot Standard → ghi kết quả<br>3. Đăng nhập tài khoản mới, lặp lại bước 1 và 2<br>4. Lập bảng đối chiếu 4 kết quả | 2 tài khoản (cũ/mới) × 2 bot (Free hết campaign / Standard) | Kết quả **chỉ phụ thuộc gói của bot**, không phụ thuộc tài khoản cũ hay mới: cả 2 tài khoản đều bị chặn ở bot Free hết campaign và đều vào được ở bot Standard. | | Lấp Q1 · Checklist PAY-LIMIT-001 yêu cầu rõ *"test cả tài khoản cũ (tạo trước khi đổi giá/spec) và mới"* · RULE-08 → production · Evidence: bảng 4 ô kết quả + ảnh từng ô |
| TC-PERM001-02 | UI | PERM-001 | Màn thay LINE OA (SCR-BE-03) | Abnormal | manual | Tất cả | Nhân viên được mời nhưng CHƯA chấp nhận lời mời không vào được màn thay LINE OA | Bot 「TC39235-FREE-NEW-HET」 đã gửi lời mời cho 1 nhân viên nhưng lời mời **chưa được chấp nhận**. Có tài khoản của nhân viên đó để đăng nhập. | 1. Đăng nhập bằng tài khoản nhân viên **chưa chấp nhận** lời mời<br>2. Thử chọn bot 「TC39235-FREE-NEW-HET」 trong danh sách bot<br>3. Mở màn thay LINE OA của bot bằng đường dẫn trực tiếp<br>4. Ghi lại màn kết quả và địa chỉ dừng lại<br>5. Lặp lại với 1 bot **trả phí** cùng tài khoản chủ đó | Tài khoản nhân viên, trạng thái lời mời = chưa chấp nhận | Bị chặn ở **lớp kiểm quyền** (không phải lớp gói): đưa về trang tổng quan, KHÔNG hiện trang chặn nâng gói và KHÔNG hiện hộp thoại 「スタンダードプラン以上…」. Kết quả giống nhau ở cả bot Free lẫn bot trả phí — vì lý do là thiếu quyền. | | Lấp Q4 · NEW-11 chỉ cover nhân viên **đã** chấp nhận; phân biệt 2 lớp chặn là đúng trọng tâm của REQ-005 · Evidence: ảnh màn kết quả + URL cho cả 2 bot |
| TC-PERM001-03 | UI | PERM-001 | Màn thay LINE OA (SCR-BE-03) | Abnormal | manual | Tất cả | Nhân viên bị gỡ khỏi bot thì mất quyền vào màn thay LINE OA ngay | Bot trả phí 「TC39235-STD」 có 1 nhân viên đã chấp nhận lời mời và **đang vào được** màn thay LINE OA. Có tài khoản chủ để thao tác gỡ nhân viên. | 1. Đăng nhập bằng tài khoản nhân viên, mở màn thay LINE OA của bot → xác nhận **vào được**<br>2. Ở một trình duyệt khác, đăng nhập bằng chủ tài khoản và **gỡ nhân viên** khỏi bot<br>3. Quay lại trình duyệt của nhân viên, **không đăng xuất**, tải lại màn thay LINE OA<br>4. Ghi lại màn kết quả và địa chỉ dừng lại | Nhân viên đang có quyền → bị gỡ quyền giữa phiên | Sau khi bị gỡ, nhân viên **mất quyền ngay trong phiên đang mở**: tải lại màn thì bị đưa về trang tổng quan, không còn vào được màn thay LINE OA. Không xuất hiện trang lỗi hệ thống. | | Lấp Q4 · Kiểm lớp quyền có bám dữ liệu thật hay chỉ kiểm lúc đăng nhập · Evidence: ảnh màn trước và sau khi gỡ quyền, cùng 1 phiên |
| TC-ENV003-01 | UI | ENV-003 | Màn thay LINE OA (SCR-BE-03) | Normal | manual | product | Chạy lại bộ chặn/không-chặn trên production với dữ liệu gói thật | Trên **production**, chuẩn bị (hoặc chọn sẵn từ dữ liệu thật) 4 bot của tài khoản test: Free cũ hết campaign, Free mới hết campaign, Free còn campaign, và 1 bot trả phí. Đăng nhập bằng chủ tài khoản trên production. | 1. Lần lượt chọn từng bot trong 4 bot trên<br>2. Với mỗi bot, mở màn thay LINE OA bằng đường dẫn trực tiếp<br>3. Ghi lại: có hộp thoại nâng gói không, mã trả về, địa chỉ dừng lại<br>4. **Chỉ điều hướng, KHÔNG thực hiện đổi LINE OA**<br>5. Lập bảng 4 dòng đối chiếu với kết quả đã chạy ở local | 4 bot trên production: Free cũ hết campaign / Free mới hết campaign / Free còn campaign / trả phí | Kết quả trên production **khớp hoàn toàn** với kết quả đã chạy ở local: 2 bot Free hết campaign bị chặn, 2 bot còn lại vào được. Mọi sai lệch phải được ghi nhận và điều tra trước khi đóng ticket. | | Lấp Q5 · RULE-08 / ENV-003 — task chạm gói & hợp đồng (bill tiền) nên **không kết luận được từ local/staging**; hiện 14/14 TC chỉ chạy local 1 lần bởi AI pipeline · **Chỉ điều hướng, không đổi LOA** để an toàn trên production · Evidence: bảng 4 dòng + ảnh từng bot chụp trên production |
| TC-REGURL001-01 | UI | REG-URL-001 | URL đo lường conversion sau release | Normal | manual | product | Các URL đo lường conversion vẫn trả đúng màn sau khi đổi đích điều hướng | Bản fix đã lên production. Có tài khoản đăng nhập được trên production. | 1. Lần lượt mở từng địa chỉ: trang danh sách tài khoản, trang tổng quan, trang thêm tài khoản, trang đăng ký thành công<br>2. Với mỗi địa chỉ, ghi lại mã trả về và màn hiển thị<br>3. Lặp lại từng địa chỉ ở **biến thể có dấu `/` ở cuối**<br>4. Lặp lại từng địa chỉ với **một tham số thừa** trên đường dẫn<br>5. Ghi lại có màn nào treo vòng xoay vô hạn không | Các URL: `/admin/home`, `/basic/overview`, `/admin/bot-add`, `/register/success` — mỗi URL 3 biến thể (thường / thêm `/` cuối / thêm param thừa) | Tất cả trả mã 200 và **đúng màn tương ứng**, không redirect lạ, không màn trắng, không treo vòng xoay vô hạn ở bất kỳ biến thể nào. | | Lấp Q6 · Bản fix đổi đích điều hướng của màn kết nối bot → REG-URL-001 nâng lên Cao; checklist liệt kê đích danh `/admin/home` và `/basic/overview` · Chạy **sau khi release lên production** · Evidence: ảnh hoặc log mã trả về của toàn bộ URL × 3 biến thể |

---

## 6. Spec update needed

| # | Vấn đề | Nguồn đối nghịch | Cần ai chốt |
|---|---|---|---|
| 1 | **Đích điều hướng khi bot Free bị chặn — 4 nguồn cho 4 kết quả khác nhau.** (a) `spec-features/admin/bot-edit/feature-spec.md:262` §2.3 + **BR-29** + `web/api-spec.md` EP-08: *"Nếu bot không tồn tại hoặc `plan_type == 2` (free) → **redirect về `/admin/home`**"*; (b) ticket #39235 "Kết quả mong đợi": *"chuyển hướng về **trang chủ admin**"*; (c) bản fix + TC NEW-4: **trang chặn HTTP 200** (`change_bot_plan_blocked`) → `/admin/bot-add`; (d) Dev đề xuất phương án thay thế: `redirect('/basic/overview')`. Khác nhau cả **mã HTTP** lẫn **màn đích**. | Spec BR-29 ⇄ ticket ⇄ code ⇄ TC NEW-4 | **PM + Leader** — chốt 1 đích duy nhất, rồi sửa spec **và** (TC hoặc code) cho khớp. Trước khi chốt, TC NEW-4 không được dùng làm căn cứ đóng ticket dù đang ghi `Đạt`. |
| 2 | **Spec thiếu hẳn quy tắc cửa sổ campaign 1 tháng.** LIM-028 (*Free mới chỉ được thay LINE OA trong 1 tháng kể từ ngày tạo bot*) **không có trong** `spec-features/admin/bot-edit/`: §5.8 chỉ có BR-29 (`plan_type = 2`) và BR-30, không nhắc `contract_type = 'free'` cũng không nhắc mốc 1 tháng. Đây là quy tắc quyết định của cả ticket mà không có nguồn spec để đối chiếu. | `feature-spec.md` §5.8 ⇄ LIM-028 ⇄ code | **Leader + Dev** — bổ sung BR mới vào §5.8: điều kiện nhận diện Free (OR 2 cột) + mốc `created_at + 1 tháng` tính **hết ngày**, kèm cách xử lý ngày cuối tháng (xem `TC-FUNCDATE001-04`). |
| 3 | **Spec mô tả guard đã tồn tại, nhưng bug chứng minh là không có.** Spec §2.3 bước 2 và BR-29 (Confidence **Cao**) ghi `plan_type = 2` → redirect; trong khi ticket #39235 chứng minh bot Free cũ (`plan_type = 2`) **vẫn vào được** trước fix. Spec cũng trỏ `BotController.php:6003` còn Studio `dev_impact` trỏ `:7256`. → Spec `bot-edit` đang **lỗi thời** với code thật. | `feature-spec.md` §2.3 / BR-29 ⇄ hiện trạng code trước fix | **Leader + Dev** — rà lại toàn bộ §2.3 và §5.8, xác nhận BR-29 mô tả hành vi ở đâu (có thể đã bị lẫn với `adminChangeBotSub`). |
| 4 | **Nội dung màn SCR-BE-03 trong spec không khớp màn thật.** Spec ghi màn trả view `admin.bots.bot_add_v3` — *"trang đăng ký LOA mới (landing page marketing)"*, user nhấn 「無料で利用開始」. TC Studio lại mô tả màn có tiêu đề 「LINE公式アカウント入れ替え機能」, nút 「アカウント入れ替えをはじめる」 và **2 thẻ chọn phương thức đổi** (đổi ngay / đặt lịch). | `feature-spec.md` §2.3 + BR-30 ⇄ TC NEW-1/NEW-7/NEW-9 | **Leader** — cập nhật spec màn SCR-BE-03 theo giao diện hiện tại, kể cả nhánh `type_change = 'scheduled'` (liên quan G4). |
| 5 | **Bot không hợp lệ đi về đâu — spec và TC ngược nhau.** Spec EP-08 bước 2: bot không tồn tại → `/admin/home`. TC NEW-12 chốt expected là `/basic/overview`. Cả 2 đều là URL đo lường conversion (xem Q6). | `web/api-spec.md` EP-08 ⇄ TC NEW-12 | **Leader + Dev** — chốt đích cho nhánh bot không hợp lệ, đồng bộ với mục 1. |
| 6 | **Điểm hở tầng API cần ticket riêng.** `ChangeBotController::init` / `botCanChangeBot` / `execute` vẫn nhận diện Free chỉ bằng `plan_type = 2` → bot Free **mới** hết campaign về lý thuyết gọi thẳng API đổi LOA được. Dev đã ghi nhận và đề nghị tách ticket; Studio khai REQ-007 nhưng chưa có TC lấy bằng chứng. | Dev journal "quét ngang" ⇄ REQ-007 (0 TC) | **Leader** — chạy `TC-PERM002-03` (§5) lấy bằng chứng hiện trạng rồi mở ticket riêng. Không gộp vào phán quyết của #39235. |
| 7 | **✅ ĐÃ CHỐT (2026-09-12) — cần ghi vào spec.** Bot Free hết campaign có **lịch đổi LINE OA đang chờ**: Leader chốt phương án **(b)** — **chặn** truy cập màn thay LINE OA, **nhưng lịch đã đặt vẫn chạy đúng hẹn và đổi LOA thành công**. Quyết định này hiện **không có chỗ nào trong spec**: `feature-spec.md` §2.3 không mô tả nhánh `type_change = 'scheduled'`, §5.8 không có BR nào về tương tác giữa điều kiện chặn theo gói và lịch đã đặt. Dev cũng chưa xác nhận guard mới có chạm tới job chạy lịch hay không. | Quyết định Leader ⇄ `feature-spec.md` §2.3 / §5.8 (không có nội dung) | **Leader + Dev** — bổ sung BR mới vào §5.8 (ghi kèm mục 2): *"Điều kiện chặn theo gói chỉ chặn **truy cập màn**, không tác động tới lịch đổi LOA đã đặt"*; Dev xác nhận job chạy lịch không đọc điều kiện gói. Chạy `TC-STATEDEP001-01` (§5) để lấy bằng chứng. |
