# 05 — Review Report

## 0. Nguồn TC

- **Nguồn đã dùng**: MCP LME TEST STUDIO — task #241 (ticket 39263, round 1, `reviewState=leader`, branch `ai_fixbug_39263`), snapshot ghi ở `04-tc-list.md`.
- **Tổng số TC review**: 26.

---

## 1. Coverage — chiều `dev-impact` + chiều `diff code`

**Kết luận**: 24 vùng ảnh hưởng đã rà (10 theo `dev-impact` file 03 + 4 file trong diff thật + 10 câu hỏi adversarial / luồng vào) · **11 đủ TC · 9 GAP · 4 RISK**.

> **Phủ theo luồng của tính năng** (rà bổ sung theo yêu cầu Leader): **Tạo = đủ** (18/26 TC) · **Sửa = đủ** (NEW-18, NEW-19, + NEW-20 preview từ màn danh sách) · **Copy = 0 TC** — xem G8 + G9.
>
> **Phủ theo số richmenu đích trên một vùng** (bot trả phí được tối đa 5 đích, mỗi đích một bộ lọc riêng): **2 đích = đủ** (NEW-5 kiểm cả trước lẫn sau khi lưu + DB; NEW-26 xoá 1 trong 2) · **5 đích = chỉ kiểm TRƯỚC khi lưu** (NEW-7 có 8 bước, không bước nào bấm 「保存してTOPに戻る」) — xem G12 + G13.
>
> **Phủ theo cổng lưu dữ liệu**: bộ TC chỉ lưu bằng nút 「保存してTOPに戻る」 ở bước 4 (NEW-8) — đúng và đủ, vì MT-18 đã chốt UI **chỉ có 2 nút** 「次へ」 (không lưu) và 「保存してTOPに戻る」 (lưu). Chiều còn trống là trạng thái **"đi hết 4 bước nhưng KHÔNG lưu"** — xem G11.

> Chiều `diff code` lấy từ Studio `task_get_context(dev_impact, spec_delta)`: diff thật gồm **4 file** — `app/FilterV2.php`, `app/Helpers/functions.php`, `app/Http/Controllers/Basic/FilterController.php`, `app/Http/Controllers/Basic/UserController.php` (64 insertions / 19 deletions).

| Mã | Chiều | Vùng ảnh hưởng | TC hiện có | Status | Vì sao chưa đủ |
|---|---|---|---|---|---|
| **G2** | `diff code` | `UserController::editRichMenuForm` — lệnh **DELETE** draft mới thêm, phạm vi "cùng bot + loại filter chuyển richmenu + mã vùng âm + parent_id null/0" | NEW-12 (richmenu khác **cùng bot** còn nguyên) · NEW-25 (đọc, 2 bot) | **GAP** | Fix **thêm một lệnh xoá** và **nới** phạm vi truy vấn. NEW-25 chỉ chứng minh cách ly ở nhánh **ĐỌC**; NEW-12 chỉ trong **cùng 1 bot**. Chưa TC nào chứng minh mở màn tạo mới ở **bot A không xoá draft của bot B**. RULE-07 yêu cầu dựng bản ghi ở **2 tài khoản** để kiểm `WHERE`. |
| **G3** | `dev-impact` | T2 — Friend Filter (SC-003): `initDataFilter` là hàm **dùng chung** cho nhiều `parent_type` | NEW-21 (một lần gửi) · NEW-22 (cài đặt hiển thị richmenu) | **RISK** | Studio `dev_impact` liệt kê `broadcast / step_message / modal_action / filter_manager / auto_reply...` — **danh sách kết thúc bằng "…" tức chưa đầy đủ**, và chỉ 2/≥5 ngữ cảnh có TC. Thiếu ステップ配信, 自動応答, 友だちリスト絞り込み. |
| **G4** | `diff code` | Truy vấn nạp lại điều kiện áp cho **mọi loại điều kiện lọc**, không riêng tag | **26/26 TC chỉ dùng điều kiện `tag`** | **GAP** | Modal lọc dùng chung hỗ trợ **11 loại điều kiện** (`tag`, `friend_name`, `day_add_friend`, `scenario`, `qr_code`, `friend_info`, `bot_new_friend`, `status_search`, `conversion`, `affiliate`, `status_chat` — theo `guide_filter_structure` của MCP LME). Loại điều kiện có tham số phức tạp (khoảng ngày, số) nạp lại từ bản nháp chưa được kiểm lần nào. |
| **G5** | `dev-impact` | T1 + D2 — **output cuối chuỗi**: LINE user tap vùng → đổi sang đúng richmenu theo điều kiện lọc | *(không TC nào)* | **GAP** | Cả 26 TC dừng ở màn admin + DB. `buildAreaRichmenu` remap `rich_menu_item_id` từ mã âm sang mã vùng thật và ghi `richmenu_switch_item.filter_ids` — nếu remap sai vùng thì **DB vẫn "trông đúng"** nhưng tap nhầm vùng mới lộ. Vi phạm **RULE-06**. → §5 đề nghị **dùng lại TC kho**, không viết mới. |
| **G6** | `diff code` | Lệnh dọn draft có phạm vi **toàn bot**, không phân biệt user/phiên | NEW-17 (2 tab, **cùng 1 user**) | **RISK** | NEW-17 đã ghi nhận rủi ro nhưng chỉ dựng 2 tab của cùng tài khoản. Bot do nhiều admin dùng chung → 2 người cùng tạo richmenu thì người này xoá draft người kia. |
| **G7** | `dev-impact` | Độ tin cậy của chính chiều (a) | — | **RISK** | File 03 mục 4.1 kê **2 file**, diff thật **4 file** — thiếu `app/FilterV2.php` và `app/Http/Controllers/Basic/UserController.php`. Không được dùng mục 4.1 làm căn cứ chốt phạm vi regression. |
| **G8** | `diff code` | **Copy richmenu** (`EP-14 POST /ajax/rich-menu/copy` → `RichMenuController@copyRichMenu`) — bản copy nhân bản `rich_menu_items` + `richmenu_switch_item` + điều kiện lọc | *(không TC nào — 5 màn hình của bộ TC chỉ gồm 作成 STEP3, 編集 STEP3, 一覧プレビュー, modal API, modal 他機能)* | **GAP** | Copy là **luồng thứ 3** ghi `filters_v2.parent_id` / `rich_menu_item_id` ngoài tạo mới và sửa. Bản copy phải có bộ điều kiện **riêng** trỏ về richmenu mới — nếu dùng chung bản ghi filter với bản gốc thì sửa/xoá ở bản copy sẽ phá bản gốc. Kho FA-004 có 6 TC copy (TC-RM-306…311) nhưng **không TC nào chạm điều kiện lọc**. |
| **G9** | `diff code` | **Copy action giữa vùng** (`EP-37 POST /ajax/richmenu/copy-action-rich-item`) khi đang ở **luồng tạo mới** — vùng nguồn có action chuyển richmenu **kèm điều kiện lọc** | *(không TC nào)* | **GAP** | Đây là đường đi thẳng vào đoạn code vừa sửa: ở luồng tạo mới mã vùng là **số âm** (`0-1-i`), copy action sang vùng khác làm đổi cặp vùng–điều kiện **trước khi** `buildAreaRichmenu` remap sang mã vùng dương. Kho có 16 TC copy action giữa vùng (TC-RM-256…269) nhưng đều dừng ở loại action, **không TC nào mang theo điều kiện lọc**. |
| **G10** | `diff code` | **Điều kiện lọc của multi action** (`エルメアクション` / `友だちアクション`) trong **luồng tạo mới** — cùng bảng `filters_v2` nhưng `parent_id` = id của `action_details` | *(không TC nào)* | **GAP** | Kho TC-RM-302 xác nhận điều kiện lọc của action エルメ lưu ở `filters_v2` với `parent_id = id action detail`; kho TC-RM-163 xác nhận **đặt được filter cho action ngay ở STEP3 luồng tạo mới**. Ở thời điểm đó `action_details` **chưa tồn tại** → `parent_id` rỗng — **đúng hình dạng root cause của bug #39263**. Nhưng nhánh draft mới chỉ mở cho `parent_type = filter-rich-menu-toggle`, nên ngữ cảnh này **không nằm trong phạm vi fix**. Chiều ngược lại cũng chưa ai kiểm: lệnh DELETE draft mới có vô tình quét sang điều kiện lọc của multi action không. |

| **G11** | `diff code` | **Cổng lưu dữ liệu của luồng tạo mới** — `buildAreaRichmenu` (chỗ gắn lại điều kiện lọc) chỉ chạy khi richmenu thật sự được lưu | NEW-8 (lưu bằng 「保存してTOPに戻る」) | **GAP** | Không TC nào khẳng định chiều ngược lại: đi hết 4 bước bằng 「次へ」 rồi **thoát không lưu** thì **không có richmenu nào được tạo** và bản nháp điều kiện lọc **không bị gắn vào đâu**. Chiều này quan trọng hơn bình thường vì bản fix vừa làm bản nháp filter trở nên "hồi sinh được": trước đây chúng là rác vô hình, nay chúng nạp lại được và được gắn khi lưu. NEW-13 / NEW-15 có chạm kịch bản bỏ dở nhưng bỏ dở **từ STEP3**, và không TC nào kiểm số lượng bản ghi richmenu trước/sau. |

| **G12** | `diff code` | **Biên 5 richmenu đích/vùng** đi qua `buildAreaRichmenu` — 5 đích **dùng chung một** `rich_menu_item_id` (cùng vùng), chỉ phân biệt nhau bằng `filter_ids` của từng bản ghi chuyển menu | NEW-5 (2 đích, trước + sau lưu + DB) · NEW-7 (5 đích, **chỉ trước khi lưu**) · NEW-26 (xoá 1 trong 2) | **RISK** | Đúng điểm remap rủi ro nhất lại không được kiểm: 8 bước của NEW-7 dừng ở việc mở lại modal, **không lưu, không mở lại từ màn sửa, không đối chiếu DB**. Khi 5 filter cùng được `whereIn filter_ids` remap về cùng một vùng, khả năng cross-link cao hơn hẳn trường hợp 2 đích mà NEW-5 đang phủ. |
| **G13** | `dev-impact` | **Thứ tự ưu tiên giữa nhiều đích** sau khi remap — kho TC-RM-216 chốt quy tắc: bạn thoả nhiều đích thì thắng là bản ghi `richmenu_switch_item` có **id nhỏ hơn** | *(không TC nào)* | **GAP** | `buildAreaRichmenu` ghi lại các bản ghi chuyển menu khi lưu ⇒ **id và thứ tự chèn có thể đổi**, kéo theo đích nào thắng cũng đổi — dù điều kiện lọc vẫn "đúng" trên màn hình. Kho TC-RM-216 kiểm quy tắc ưu tiên nhưng với **2 đích KHÔNG filter**; TC-RM-217/218 có filter nhưng là trường hợp bạn thoả **0 hoặc 1** đích. Trường hợp **bạn thoả nhiều đích cùng lúc, richmenu dựng qua luồng tạo mới** chưa ai kiểm. |

| **G14** | `diff code` | **Dọn bản nháp ở nhánh "có id" của `editRichMenuForm`** — nhánh dọn `parent_id = id richmenu AND mã vùng âm`, dùng cho luồng **sửa** | NEW-19 (thêm vùng mới kèm lọc rồi **lưu ngay**) | **GAP** | Bản fix tách hàm này thành 2 nhánh (`có id` / `không id`) và khai giữ nguyên nhánh cũ — nhưng **không TC nào chạy nhánh cũ ở trạng thái cần dọn**. Điều kiện lọc ghi thẳng DB ngay khi bấm 「保存」 trong modal (Leader xác nhận 2026-09-15), nên bỏ dở phiên **sửa** để lại bản ghi `parent_id` = id richmenu + mã vùng âm — đúng loại rác mà nhánh cũ phải dọn. NEW-13/14/16 chỉ phủ luồng tạo mới; NEW-19 lưu ngay nên không sinh rác. Nếu nhánh cũ hỏng, lần sau thêm vùng mới sẽ thấy lại điều kiện của phiên bỏ dở — **đúng hình dạng bug #39263, ở luồng sửa**. |

> **G1 đã RÚT (2026-09-15)** — dòng cũ cho rằng modal điều kiện lọc của tab 「リッチメニュー切り替え」 có hiển thị **số bạn khớp điều kiện**. Leader xác nhận **màn này KHÔNG có số đếm**. Tôi đã suy diễn nhầm từ 2 ngữ cảnh khác của cùng modal dùng chung (NEW-21 一斉配信, NEW-22 cài đặt hiển thị richmenu) và từ chữ "path đếm/preview" trong `dev_impact` của Studio — **không verify trên màn thật**. Đánh giá lại: `app/FilterV2.php` **đã được cover** — phần Studio mô tả ("việc dọn bản ghi đã xoá khỏi modal áp cả cho draft → mở lại không hiện điều kiện đã xoá") nằm đúng trong NEW-3 và NEW-4; nhánh đếm/preview thì thuộc các ngữ cảnh dùng chung, đã có NEW-21 + NEW-22. Còn lại 1 câu hỏi cho Dev, xem §6 mục 9.

*Không có TC orphan không map được vùng nào.*

---

## 2. Quan điểm test còn thiếu

**Kết luận**: 20 quan điểm có Trigger khớp task — **9 đã cover đủ** (FUNC-001, FUNC-004, FUNC-SEQ-001, CONC-003, SEC-ISO-001, UI-FIELD-001, PAY-LIMIT-001, PERM-003 nhánh đọc, DATA-COUNT-001 ở ngữ cảnh dùng chung) · **11 còn thiếu** (bảng dưới).

| Mã | Quan điểm | Ưu tiên | Tình trạng | Thiếu gì | Severity |
|---|---|---|---|---|---|
| **Q2** | `MSG-USER-001` + `MSG-004` | Cao | **GAP** | Không TC nào verify trên LINE app thật (RULE-06). Kho FA-004 **đã có sẵn** TC-RM-213/214/215 cho đúng kịch bản này. | `[MAJOR]` |
| **Q3** | `FUNC-002` | Cao | **GAP** | Chỉ test 1/11 loại điều kiện lọc (`tag`). Loại có tham số ngày/số/đối tượng tham chiếu chưa được nạp lại lần nào. | `[MAJOR]` |
| **Q4** | `CONC-001` | Cao | **RISK** | NEW-17 chỉ dựng 2 tab **cùng user**. Thiếu 2 admin khác nhau trên cùng bot — đúng phạm vi mà lệnh dọn draft đang quét. | `[MAJOR]` |
| **Q5** | `DATA-DB-001` | Cao | **RISK** | Có 3 TC (NEW-3, NEW-13, NEW-26) nhưng **không TC nào kiểm `WHERE` trên 2 tài khoản cho nhánh DELETE** — RULE-07 bắt buộc. Xem G2. | `[BLOCKER]` |
| **Q6** | `REG-SHARED-001` | Cao | **RISK** | 2/≥5 ngữ cảnh dùng chung có TC; danh sách caller của Dev chưa đầy đủ (kết thúc bằng "…"). Xem G3. | `[MAJOR]` |
| **Q7** | `ENV-003` | Cao | **RISK** | 26/26 TC khai `env_scope = [local, dev, staging]`, **0 TC ở product**. Nhánh chạy thật đi qua `HandlePostbackTask` của `linect-service` (xử lý nền). RULE-08. | `[MAJOR]` |
| **Q8** | `COMPAT-LEGACY-001` | Cao | **RISK** | Bảng là `filters_v2` / class `FilterV2` — hàm ý từng version-up. NEW-16 đã cover dữ liệu **mồ côi** cũ, nhưng chưa rõ hệ thống còn tồn tại **filter v1** hay không. Không đủ căn cứ để kết luận → đẩy sang §6 hỏi Dev thay vì tự đề xuất TC. | → §6 |
| **Q9** | `DATA-REF-001` | Cao | **GAP** | Trigger khớp trực tiếp: "đối tượng có thao tác **xóa / copy** / đổi tên / di chuyển VÀ được nơi khác tham chiếu". Richmenu + điều kiện lọc của nó được **copy** ở 2 nơi (copy richmenu `EP-14`, copy action giữa vùng `EP-37`) — **0/26 TC** chạm cả 2. Xem G8 + G9. | `[BLOCKER]` |
| **Q10** | `REG-SHARED-001` (ngữ cảnh multi action) | Cao | **GAP** | Điều kiện lọc của multi action dùng **cùng modal, cùng bảng `filters_v2`, cùng hàm `initDataFilter`** — và trong luồng tạo mới cũng ở trạng thái "cha chưa có id", tức **cùng hình dạng root cause**. Fix chỉ mở nhánh cho `parent_type = filter-rich-menu-toggle`. Không TC nào kiểm ngữ cảnh này ở **cả 2 chiều**: (a) có còn bug không, (b) lệnh DELETE draft mới có quét nhầm sang không. Xem G10. | `[BLOCKER]` |
| **Q11** | `STATE-001` | Cao | **GAP** | Trigger khớp: "nghiệp vụ gồm **≥2 bước ghi dữ liệu tuần tự**". Leader xác nhận (2026-09-15): modal điều kiện lọc bấm 「保存」 là **ghi DB ngay**, không chờ 「保存してTOPに戻る」 — tức hệ thống có **2 nhịp ghi lệch nhau** (filter ghi ngay · phần còn lại của wizard ghi ở bước 4), chỉ hợp nhất tại `buildAreaRichmenu`. Không TC nào kiểm trạng thái **dở dang giữa 2 nhịp** ở cả 2 luồng: tạo mới đi hết 4 bước rồi thoát không lưu (G11), và sửa richmenu thêm vùng mới kèm lọc rồi bỏ dở (G14). | `[MAJOR]` |
| **Q12** | `FUNC-MULTI-001` | Trung bình | **RISK** | Trigger khớp thẳng: "chức năng cho phép thêm **≥2 phần tử cùng loại** trong 1 đối tượng (… nhiều điều kiện lọc)" — ở đây là **nhiều richmenu đích trên một vùng, mỗi đích một bộ lọc**. Có TC nhưng hụt 2 chiều: (a) biên 5 đích không verify sau khi lưu (G12); (b) thứ tự ưu tiên giữa các đích sau remap (G13). | `[MAJOR]` |

> **Q1 đã RÚT (2026-09-15)** — `DATA-COUNT-001` được nêu dựa trên giả định modal lọc của tab 切り替え có số đếm; Leader xác nhận **không có**. Trigger của quan điểm này vẫn khớp ở các **ngữ cảnh dùng chung** (一斉配信, cài đặt hiển thị richmenu) và đã được NEW-21 + NEW-22 cover → chuyển sang nhóm "đã cover đủ". Xem thêm ghi chú G1 ở §1.

> Lưu ý mapping:

---

## 3. TC trùng lặp nội dung

Đã rà **26/26 TC** theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`). Phát hiện **1 nhóm**:

| Nhóm trùng | TC giữ lại | TC đề nghị xoá/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| Dọn draft khi bỏ dở phiên tạo mới | **NEW-13** (`DATA-DB-001`, Abnormal) — có oracle dữ liệu rõ hơn | **NEW-14** (`FUNC-SEQ-001`, Abnormal) → **GỘP**, không xoá | `DUP-SUBSET` | Cùng Abnormal · cùng đối tượng (draft filter phiên tạo mới) · cùng thao tác (bỏ dở rồi mở lại màn tạo mới) · cùng tiền đề · cùng expected (draft không hiện lại) | `[MINOR]` |

- **Đề xuất**: gộp NEW-14 thành **1 bước bổ sung của NEW-13** ("lặp lại bằng F5 thay vì điều hướng qua màn danh sách"), thay vì xoá — vì NEW-14 là TC **duy nhất** chứng minh lệnh dọn không phụ thuộc việc điều hướng qua màn danh sách. Áp gate: xoá hẳn NEW-14 sẽ làm mất chiều đó của `REQ-005` → **chuyển từ "xoá" sang "gộp"**.
- Không có `DUP-EXACT`, `DUP-INFLATE`, `DUP-CONFLICT` trong nội bộ bộ TC. (Conflict với kho TCs: xem §6.)

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | Toàn bộ 26 TC | **0/26 TC pass** — 25 `skip` + 1 chưa chạy. Không TC nào cho ra kết luận test. Mọi dòng "đủ TC" ở §1/§2 chỉ là **đủ trên giấy**. | Chạy thật toàn bộ bộ TC trước khi đưa ticket sang Closed. Không dùng `skip` hàng loạt làm trạng thái kết thúc. |
| 2 | `[BLOCKER]` | Bản fix nói chung | Dev **không verify được runtime**: mức verify chỉ `lint` (`php -l`), dev stack MySQL `Connection refused`. Cả nguyên nhân lẫn cách fix đều chỉ dựa trên đọc code. | Bắt buộc dựng được env chạy trước khi test; nếu TC đầu tiên (NEW-1) không tái hiện đúng hành vi **trước** fix thì phải nghi ngờ chẩn đoán gốc. |
| 3 | `[MAJOR]` | 13/26 TC | 8 mã quan điểm không có trong `framework/checklist-lme.md` → `/review-tc` không map được sang lưới 80 quan điểm, và bộ TC không thống kê được theo ưu tiên. | Map lại sang mã chuẩn (gợi ý: `TOOL-NEGCTRL-001`→`REG-SHARED-001`/`DATA-DB-001`; `TOOL-AXIS-001`→`FUNC-MULTI-001`; `TOOL-OLDREC-001`→`COMPAT-LEGACY-001`; `STATE-MATRIX-001`→`UI-FIELD-001`; `LIM-027`→`PAY-LIMIT-001`; `RULE-07`→`DATA-DB-001`; `API-CONTRACT-001`→`FUNC-001`), hoặc bổ sung các mã `TOOL-*` vào checklist nếu đó là quan điểm nội bộ chính thức. |
| 4 | `[MAJOR]` | 26/26 TC | `Evidence thực tế` rỗng toàn bộ. RULE-02: chỉ tick Đạt khi đã đính đúng loại bằng chứng. | Quy định trước loại evidence cho từng TC (ảnh modal sau khi mở lại · ảnh nhãn nút · bản ghi DB `filters_v2` / `richmenu_switch_item`). |
| 5 | `[MAJOR]` | `03-dev-impact.md` | Có `Auto-filled: 2026-09-14 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" chưa tick** → F/D/T có thể thiếu hoặc map sai. Thực tế đã sai: xem #6. | Tester đọc lại Redmine #39263 rồi tick. |
| 6 | `[MAJOR]` | `03-dev-impact.md` mục 4.1 | Dev kê **2 file**, diff thật **4 file** (thiếu `FilterV2.php`, `UserController.php`). Chính journal cũng tự mâu thuẫn: mục 6 ghi "chỉ 2 file đã sửa" còn dòng BRANCH/COMMIT ghi "4 file". | Yêu cầu Dev sửa mục 4.1 cho khớp diff và kê theo **function**, không theo file. |
| 7 | `[MAJOR]` | NEW-7 | Trích nguồn sai: `Ghi chú` dẫn `spec: rich-menu ui-spec.md:202` cho giới hạn 5 richmenu đích/vùng, nhưng **dòng 202 của `spec-features/admin/rich-menu/ui/ui-spec.md` là dòng bảng tab**, và **không chỗ nào trong `spec-features/admin/rich-menu/` ghi giới hạn 5**. Giới hạn này **có thật** nhưng nguồn đúng là Leader chốt **MT-09 (2026-08-22)** trong `kho-tcs/fa004`. | Sửa trích dẫn; bổ sung message chính xác vào `Kết quả mong đợi`: 「最大で5個までしか選択できません。」 (theo TC-RM-221). |
| 8 | `[MAJOR]` | Toàn bộ 26 TC | `Trạng thái đánh giá spec` rỗng toàn bộ, trong khi `feature-spec.md` §9 **tự nhận Gap #4**: "Tab「リッチメニュー切り替え」— chi tiết UI chọn Rich Menu đích" chưa có snapshot. Nghĩa là phần lớn `Kết quả mong đợi` đang suy từ code chứ không có chuẩn spec. | Điền `Spec không ghi` + ghi rõ đã hỏi ai cho các TC thuộc tab 切り替え; các điểm đã có Leader chốt (MT-09) thì ghi `Đã hỏi leader`. |

### Chất lượng từng TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 9 | `[MAJOR]` | NEW-17 | `Kết quả mong đợi` chứa **2 kết luận trong 1 ô**: phần "điều kiện bắt buộc đạt" và phần "ghi nhận thêm… tab A nhiều khả năng mất điều kiện". Tester dễ tick Đạt trong khi hành vi mất draft của tab A chưa ai chốt là đúng hay sai. | Tách: giữ NEW-17 cho phần khẳng định được (không lẫn dữ liệu, không lỗi hệ thống); đưa phần "tab A mất draft" thành mục chờ quyết định ở §6. |
| 10 | `[MINOR]` | `[AP-3]` NEW-20, NEW-21, NEW-22 | Regression của modal dùng chung **chỉ có loại case `Normal`** — không có nhánh bất thường (xoá hết điều kiện, điều kiện tham chiếu đối tượng đã xoá) ở các ngữ cảnh dùng chung. | Bổ sung ≥1 Abnormal cho ngữ cảnh dùng chung — đã đưa vào §5 (`TC-REGSHARED001-01…03`). |
| 11 | `[NIT]` | `[AP-5]` NEW-11 | Chốt chặn gói miễn phí nằm ở `create.js` — **không có file JS nào trong diff**. TC này test layer không bị chạm code. | Giữ lại nhưng gắn nhãn `regression`, không tính vào coverage của bản fix. Kho đã có TC-RM-229 cho cùng hành vi. |
| 12 | `[BLOCKER]` | Phạm vi bộ TC | **Bộ TC chỉ phủ 2/3 luồng ghi điều kiện lọc của richmenu**: tạo mới (18 TC) + sửa (2 TC), **bỏ trắng toàn bộ luồng copy** — cả `EP-14` copy richmenu lẫn `EP-37` copy action giữa vùng. Cả hai đều ghi `filters_v2.parent_id` / `rich_menu_item_id` — đúng 2 cột mà bản fix đang chạm. | Bổ sung TC — đã đưa vào §5 (`TC-DATAREF001-01/02`, `TC-FUNCSEQ001-01`). |
| 13 | `[MAJOR]` | Phạm vi bản fix | Fix mở nhánh draft **chỉ cho `parent_type = filter-rich-menu-toggle`**, trong khi ngữ cảnh **multi action** dùng chung y hệt cơ chế "cha chưa có id" ở luồng tạo mới. Nếu ngữ cảnh đó cũng lỗi thì đây là **fix hẹp cho bug dạng pattern** — khách sẽ báo lại bug tương tự ở màn khác. | Hỏi Dev: `initDataFilter` còn `parent_type` nào rơi vào trạng thái cha-chưa-có-id? Chạy trước `TC-REGSHARED001-05` như một phép dò — nếu lỗi thì raise ticket riêng, không gộp vào #39263. |
| 14 | `[MAJOR]` | NEW-7 | TC biên **5 richmenu đích** dừng ở bước mở lại modal — **không có bước lưu richmenu**, không mở lại từ màn sửa, không đối chiếu DB. Trong khi TC 2 đích (NEW-5) lại làm đủ cả 3 việc đó. Biên cao hơn nhưng kiểm nông hơn. | Bổ sung vào NEW-7 các bước của NEW-5 từ bước 9 trở đi (lưu ở STEP4 → mở lại từ màn sửa → đối chiếu `filter_ids` của cả 5 bản ghi chuyển menu). Hoặc dùng lại kho **TC-RM-223** đã có sẵn chiều này. |
| 15 | `[NIT]` | NEW-1 … NEW-26 | Dữ liệu test đặt tên nhất quán theo ticket (`39263-TAG-1`, `39263-DICH-A`) — tốt, giữ nguyên quy ước này cho TC bổ sung. | — |

---

## 5. TCs đề xuất bổ sung (16)

**Đối chiếu trước khi viết** (BƯỚC 5a + 5b):

| Mục | Kết quả |
|---|---|
| Kho TCs đã đọc | `kho-tcs/fa004-richmenu-リッチメニュー.md` (599 dòng) — nhóm "Step 3 — Đổi richmenu" (TC-RM-208 → TC-RM-229), "Step 3 — Action エルメ" (TC-RM-146 → TC-RM-165), "Step 3 — Copy action giữa area" (TC-RM-256 → TC-RM-269), "Copy richmenu" (TC-RM-306 → TC-RM-311), "Preview action" (TC-RM-334/335), "Setting hiển thị richmenu" (TC-RM-355), TC-RM-37, TC-RM-302 |
| Vùng regression rút ra từ kho | TC-RM-213/214/215/216/217/218 (tap trên LINE thật theo filter) · TC-RM-220 (mở lại modal filter của switch) · TC-RM-221 (giới hạn 5) · TC-RM-223 (4 vùng × cấu hình switch/filter) · TC-RM-37 (perf mở modal filter khi có 5 switch, PRODUCTION) · TC-RM-163 (filter cho action エルメ ngay tại STEP3 luồng tạo mới) · TC-RM-302 (`filters_v2.parent_id` = id action detail) |
| GAP đã có TC kho, **không viết mới** | **G5 / Q2** → dùng lại **TC-RM-213, TC-RM-214, TC-RM-215** (đã ghi `Môi trường: PRODUCTION`). Cần chỉnh 1 điểm: tiền đề phải nêu rõ richmenu được dựng bằng **luồng TẠO MỚI** (không phải sửa richmenu có sẵn), vì đó mới là nhánh bản fix chạm. |
| Kho có TC **gần** nhưng chưa đủ chiều → viết mới có dẫn chiếu | **Copy richmenu**: TC-RM-306 (bản copy đủ ảnh/layout/action) và TC-RM-311 (bản copy hoạt động độc lập, sửa action không ảnh hưởng bản gốc) **không chạm điều kiện lọc** → `TC-DATAREF001-01/02` bổ sung đúng chiều filter. **Copy action giữa vùng**: TC-RM-256 → TC-RM-269 chỉ xét loại action và quy tắc ghi đè, **không mang theo điều kiện lọc** → `TC-FUNCSEQ001-01`. |
| Kho đã chốt nguyên tắc, TC mới chỉ mở rộng sang chiều filter | **Cổng lưu dữ liệu**: TC-RM-289 (tạo mới — 次へ không ghi DB) · TC-RM-294 (Step 1/2/3 không có nút lưu, phải sang bước 4) · TC-RM-305 (không sửa gì vẫn lưu được ở bước 4) · TC-RM-299 (EDIT thoát không lưu ⇒ không đổi). **Không TC nào trong nhóm này mang theo điều kiện lọc** → `TC-DATADB001-02` và `TC-DATADB001-03` bổ sung đúng chiều đó, không viết lại nguyên tắc đã có. |
| GAP đã có TC kho, **không viết mới** (2) | **G12** → dùng lại **TC-RM-223** ("Rich menu 3-4 vùng với số switch và filter khác nhau ở mỗi vùng"): vùng 3 đặt **5 switch đều có filter**, có lưu bước 4, mở lại màn edit đọc từng vùng và đối chiếu **8 bản ghi `richmenu_switch_item`, `filter_ids` đúng ở 7 bản ghi có filter và NULL ở bản ghi không filter**. Đây chính là chiều NEW-7 còn thiếu. Cần chỉnh 1 điểm: bổ sung bước **mở lại modal của từng đích trong nhóm 5** để kiểm ở tầng màn hình chứ không chỉ tầng DB. |
| Ghi chú regression quan trọng | **TC-RM-220** của kho chính là TC đáng lẽ bắt được bug #39263 ("mở lại modal filter → đối chiếu điều kiện", tiền đề đang ở Step 3 luồng tạo mới). Phải cho chạy lại TC này sau fix. |
| Chống trùng | Đã đối chiếu **26 TC ở BƯỚC 0** + `kho-tcs/fa004-richmenu-リッチメニュー.md` — không TC đề xuất nào trùng. |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-DATADB001-01 | Data | DATA-DB-001 | Step 3 — Đổi richmenu / dọn bản nháp khi mở màn tạo mới | Abnormal | manual | Tất cả | Mở màn tạo mới richmenu ở bot A không được xoá điều kiện lọc bản nháp của bot B | Có **2 bot** cùng tài khoản admin: bot A và bot B, cả hai đều trả phí. Mỗi bot có sẵn 1 richmenu đích và 1 tag riêng (`39263-TAG-A`, `39263-TAG-B`). ⚠️ **Bắt buộc dùng 2 trình duyệt riêng** (vd 1 cửa sổ thường + 1 cửa sổ ẩn danh, hoặc 2 trình duyệt khác nhau) và đăng nhập riêng ở từng cửa sổ: **bot đang chọn được lưu theo phiên đăng nhập**, nên đổi bot ở một tab sẽ đổi luôn ngữ cảnh của mọi tab khác trong cùng trình duyệt — làm 2 tab cùng trình duyệt thì TC này vô hiệu. | 1. **Trình duyệt 1** — đăng nhập, chọn **bot B** → tạo mới richmenu → STEP3 → vùng 1 chọn richmenu đích → 「絞込みを設定する」 → thêm điều kiện tag `39263-TAG-B` → 「保存」 trong modal<br>2. **Không** lưu richmenu, để nguyên màn STEP3 của trình duyệt 1, **không** rời trang và **không** tải lại (tải lại sẽ tự kích hoạt lệnh dọn của chính bot B)<br>3. **Trình duyệt 2** — đăng nhập riêng, chọn **bot A** → vào màn tạo mới richmenu (`/basic/rich-menu/create`) để kích hoạt lệnh dọn bản nháp trong phạm vi bot A<br>4. Trình duyệt 2: đặt điều kiện lọc tag `39263-TAG-A` cho vùng 1 → 「保存」 trong modal<br>5. **Quay lại trình duyệt 1** (vẫn đang ở STEP3 của bot B, phiên không bị đụng): bấm lại 「絞込みを設定する」 của vùng 1 và đọc nội dung modal<br>6. Đối chiếu dữ liệu: đếm bản ghi điều kiện lọc bản nháp (loại filter chuyển richmenu, mã vùng âm, mã richmenu cha rỗng) **của bot B** trước bước 3 và sau bước 4 | bot A: tag `39263-TAG-A` · bot B: tag `39263-TAG-B` | Bước 5: modal ở trình duyệt 1 **vẫn hiện đúng** điều kiện tag `39263-TAG-B` — bản nháp của bot B không bị thao tác ở bot A xoá mất. Bước 6: số bản ghi nháp của bot B **không đổi** trước và sau khi bot A mở màn tạo mới. Lệnh dọn chỉ tác động trong phạm vi bot đang thao tác. |  | Lấp G2 · Lấp Q5 · RULE-07 dựng bản ghi ở 2 tài khoản để kiểm `WHERE` · ⚠️ Bẫy khi thực thi: **không** kiểm bằng cách mở lại màn tạo mới của bot B — chính thao tác đó kích hoạt lệnh dọn của bot B và xoá mất bằng chứng; phải quan sát trên màn STEP3 đang mở sẵn ở trình duyệt 1 · Đánh giá spec: Spec không ghi — hỏi leader · Evidence: ảnh modal ở cả 2 trình duyệt + số bản ghi nháp của bot B trước/sau |
| TC-REGSHARED001-01 | UI | REG-SHARED-001 | Modal điều kiện lọc — ステップ配信 (hồi quy) | Abnormal | manual | Tất cả | Hồi quy modal lọc của ステップ配信: nạp, xoá hết điều kiện rồi lưu vẫn như trước bản vá | Bot trả phí có sẵn 1 kịch bản ステップ配信 với 1 bước đã đặt **2 điều kiện lọc**. | 1. Mở kịch bản ステップ配信 → mở modal điều kiện lọc của bước đó<br>2. Đối chiếu số dòng điều kiện hiện ra với cấu hình đã lưu<br>3. Xoá **hết** điều kiện → 「保存」<br>4. Mở lại modal, quan sát<br>5. Thêm lại 1 điều kiện tag → 「保存」 → mở lại modal | 2 điều kiện có sẵn → xoá hết → thêm lại 1 | Bước 2: hiện đúng 2 điều kiện. Bước 4: modal rỗng, **không** hiện lại điều kiện đã xoá và **không** hiện điều kiện lạ của ngữ cảnh khác (đặc biệt không hiện điều kiện của richmenu switch). Bước 5: hiện đúng 1 điều kiện vừa thêm. |  | Lấp G3 · Lấp Q6 · regression · `initDataFilter` dùng chung, `parent_type` = step_message · Đánh giá spec: Spec không ghi — hỏi leader · Evidence: ảnh modal từng bước |
| TC-REGSHARED001-02 | UI | REG-SHARED-001 | Modal điều kiện lọc — 自動応答 (hồi quy) | Abnormal | manual | Tất cả | Hồi quy modal lọc của 自動応答: điều kiện tham chiếu đối tượng đã xoá không làm hỏng modal | Bot trả phí có sẵn 1 自動応答 đã đặt điều kiện lọc theo **1 tag**. Chuẩn bị 1 tag riêng `39263-TAG-DEL` dùng cho auto-reply này. | 1. Đặt điều kiện lọc của 自動応答 = tag `39263-TAG-DEL` → 「保存」<br>2. Vào 「タグ管理」 xoá tag `39263-TAG-DEL`<br>3. Quay lại 自動応答, mở lại modal điều kiện lọc<br>4. Quan sát modal và thử thêm 1 điều kiện mới rồi 「保存」 | tag `39263-TAG-DEL` bị xoá sau khi đã dùng làm điều kiện | Modal vẫn mở được, không trắng màn và không lỗi hệ thống; dòng điều kiện trỏ tới tag đã xoá được xử lý nhất quán (ẩn đi hoặc hiện dạng không hợp lệ — ghi lại hiện trạng thực tế). Thêm điều kiện mới và lưu vẫn thành công. |  | Lấp G3 · Lấp Q6 · regression · `parent_type` = auto_reply · Hiện trạng ở bước 4 chưa có chuẩn spec → ghi nhận rồi hỏi leader, không tự kết luận Đạt/Không đạt · Evidence: ảnh modal + ảnh sau khi lưu |
| TC-REGSHARED001-03 | UI | REG-SHARED-001 | Modal điều kiện lọc — 友だちリスト絞り込み (hồi quy) | Normal | manual | Tất cả | Hồi quy modal lọc của 友だちリスト: lưu và nạp lại bộ lọc quản lý vẫn như cũ | Bot trả phí có ≥ 10 bạn, có tag `39263-TAG-1` gắn cho một phần trong số đó. | 1. Vào 「友だちリスト」 → mở modal điều kiện lọc<br>2. Đặt 1 điều kiện tag `39263-TAG-1` và 1 điều kiện ở nhóm 「どれか1つ以上満たす」 → 「保存」<br>3. Đọc số bạn hiển thị sau khi lọc<br>4. Rời màn rồi quay lại, mở lại modal điều kiện lọc<br>5. Đối chiếu điều kiện và số bạn | tag `39263-TAG-1` + 1 điều kiện nhóm hoặc | Bước 4–5: modal nạp lại **đúng** cả 2 nhóm điều kiện như lúc lưu; danh sách bạn và số bạn giống hệt bước 3. Không hiện thêm điều kiện lạ từ ngữ cảnh richmenu switch. |  | Lấp G3 · Lấp Q6 · regression · `parent_type` = filter_manager · Đánh giá spec: Spec không ghi — hỏi leader · Evidence: ảnh modal + ảnh danh sách bạn 2 lần |
| TC-FUNC002-01 | UI | FUNC-002 | Step 3 — Đổi richmenu / modal điều kiện lọc | Normal | manual | Tất cả | Tạo mới richmenu: điều kiện lọc **không phải tag** cũng nạp lại đúng tham số khi mở lại modal | Đăng nhập admin bot trả phí, có richmenu đích `39263-DICH-A`. Bot có dữ liệu để dựng điều kiện 「友だち情報」 dạng ngày và điều kiện 「友だち追加日」. | 1. Vào 「リッチメニュー」 → tạo mới richmenu → STEP1 upload ảnh → STEP2 chọn bố cục 3 vùng → STEP3<br>2. Vùng 1 → tab 「リッチメニュー切り替え」 → chọn richmenu đích `39263-DICH-A`<br>2. Bấm 「絞込みを設定する」<br>3. Thêm điều kiện **友だち追加日** với một khoảng ngày cụ thể<br>4. Thêm điều kiện **友だち情報** kiểu ngày với một khoảng giá trị cụ thể<br>5. 「保存」 → đóng modal → mở lại modal<br>6. Đối chiếu **từng tham số** của cả 2 điều kiện với lúc nhập | 友だち追加日: 1 khoảng ngày cụ thể · 友だち情報 kiểu ngày: 1 khoảng giá trị cụ thể | Mở lại modal hiện đủ **2** dòng điều kiện, và **từng tham số** (loại điều kiện, toán tử so sánh, giá trị/khoảng ngày) khớp y hệt lúc nhập — không mất tham số, không tụt về giá trị mặc định. |  | Lấp G4 · Lấp Q3 · Modal dùng chung hỗ trợ 11 loại điều kiện (MCP `guide_filter_structure`); 26 TC hiện có chỉ dùng `tag` · Đánh giá spec: Spec không ghi — hỏi leader · Evidence: ảnh modal lúc nhập + lúc mở lại |
| TC-FUNC002-02 | UI | FUNC-002 | Step 3 — Đổi richmenu / modal điều kiện lọc | Abnormal | manual | Tất cả | Tạo mới richmenu: trộn nhiều loại điều kiện ở cả 2 nhóm rồi xoá 1 loại, các loại còn lại không bị ảnh hưởng | Như TC-FUNC002-01. Thêm 1 シナリオ (kịch bản) và 1 tag `39263-TAG-1` để dựng điều kiện. | 1. Vào 「リッチメニュー」 → tạo mới richmenu → STEP1 upload ảnh → STEP2 chọn bố cục 3 vùng → STEP3<br>2. Vùng 1 → tab 「リッチメニュー切り替え」 → chọn richmenu đích `39263-DICH-A`<br>2. Nhóm 「全て満たす」: thêm điều kiện tag `39263-TAG-1` và điều kiện 友だち追加日<br>3. Nhóm 「どれか1つ以上満たす」: thêm điều kiện シナリオ<br>4. 「保存」 → mở lại modal, đối chiếu đủ 3 điều kiện<br>5. Xoá **riêng** điều kiện 友だち追加日 → 「保存」<br>6. Mở lại modal, đối chiếu | tag + 友だち追加日 + シナリオ | Bước 4: đủ 3 điều kiện đúng nhóm, đúng tham số. Bước 6: còn đúng **2** điều kiện (tag ở nhóm và-điều-kiện, シナリオ ở nhóm hoặc-điều-kiện) với tham số nguyên vẹn; điều kiện 友だち追加日 không hiện lại; không có điều kiện nào bị nhảy sang nhóm khác. |  | Lấp G4 · Lấp Q3 · Bản fix vá 2 truy vấn tách biệt cho 2 nhóm → phải test trộn loại ở cả 2 nhóm · Đánh giá spec: Spec không ghi — hỏi leader · Evidence: ảnh modal bước 4 và bước 6 |
| TC-DATAREF001-01 | UI | DATA-REF-001 | Copy richmenu | Normal | manual | Tất cả | Copy richmenu có action chuyển richmenu kèm điều kiện lọc: bản copy giữ đủ điều kiện lọc của từng vùng | Đăng nhập admin bot trả phí. Richmenu `39263-GOC` đã hoàn thành 4 bước, có **2 vùng**: vùng 1 chuyển sang `39263-DICH-A` kèm điều kiện tag `39263-TAG-1`, vùng 2 chuyển sang `39263-DICH-B` kèm điều kiện tag `39263-TAG-2`. | 1. Ở màn danh sách richmenu, mở menu của `39263-GOC` → 「複製」<br>2. Đặt tên bản copy là `39263-COPY` → xác nhận<br>3. Mở `39263-COPY` → STEP3 → vùng 1 → bấm 「絞込みを設定する」 của richmenu đích, đọc điều kiện<br>4. Làm tương tự với vùng 2<br>5. Mở lại `39263-GOC` STEP3, đọc điều kiện của cả 2 vùng | `39263-GOC`: vùng 1 = tag `39263-TAG-1`, vùng 2 = tag `39263-TAG-2` | Bản copy `39263-COPY`: vùng 1 hiện đúng tag `39263-TAG-1`, vùng 2 hiện đúng tag `39263-TAG-2`, nhãn nút ở cả 2 vùng là 「絞込み設定済み」. Bản gốc `39263-GOC` giữ nguyên đúng điều kiện cũ ở cả 2 vùng, không mất và không đổi. |  | Lấp G8 · Lấp Q9 · dẫn từ TC-RM-306 (kho chỉ kiểm ảnh/layout/action, không kiểm filter) · Copy là luồng thứ 3 ghi `filters_v2` ngoài tạo mới và sửa · Đánh giá spec: Spec không ghi — hỏi leader · Evidence: ảnh modal của 2 vùng ở cả bản gốc và bản copy |
| TC-DATAREF001-02 | Data | DATA-REF-001 | Copy richmenu | Abnormal | manual | Tất cả | Sửa điều kiện lọc ở bản copy không được làm đổi điều kiện lọc của bản gốc | Như TC-DATAREF001-01, đã copy xong `39263-COPY` từ `39263-GOC`. Thêm tag `39263-TAG-3`. | 1. Mở `39263-COPY` → STEP3 → vùng 1 → mở modal điều kiện lọc<br>2. Xoá điều kiện tag `39263-TAG-1`, thêm điều kiện tag `39263-TAG-3` → 「保存」<br>3. Lưu `39263-COPY` ở bước 4<br>4. Mở `39263-GOC` → STEP3 → vùng 1 → mở modal điều kiện lọc, đọc điều kiện<br>5. Query `filters_v2` của cả 2 richmenu, đối chiếu `parent_id` của từng bản ghi điều kiện | Bản copy: đổi tag `39263-TAG-1` → `39263-TAG-3` | Bản gốc `39263-GOC` vùng 1 **vẫn là tag `39263-TAG-1`**, không bị đổi theo. Bản copy là tag `39263-TAG-3`. `filters_v2`: 2 richmenu có **2 bộ bản ghi điều kiện riêng biệt**, `parent_id` trỏ đúng richmenu của mình — không dùng chung cùng một bản ghi điều kiện. |  | Lấp G8 · Lấp Q9 · mở rộng TC-RM-311 sang chiều điều kiện lọc · RULE-07 verify DB · Nếu 2 richmenu dùng chung bản ghi filter thì đây là bug độc lập với #39263, raise ticket riêng · Đánh giá spec: Spec không ghi — hỏi leader · Evidence: ảnh modal 2 richmenu + kết quả query `filters_v2` |
| TC-FUNCSEQ001-01 | UI | FUNC-SEQ-001 | Step 3 — Copy action giữa vùng (luồng tạo mới) | Abnormal | manual | Tất cả | Tạo mới richmenu: copy action chuyển richmenu **kèm điều kiện lọc** từ vùng này sang vùng khác rồi lưu, điều kiện phải gắn đúng vùng đích | Đăng nhập admin bot trả phí. Bot có richmenu đích `39263-DICH-A` và tag `39263-TAG-1`. | 1. Tạo mới richmenu → STEP1 upload ảnh → STEP2 chọn bố cục 3 vùng → STEP3<br>2. Vùng 1: tab 「リッチメニュー切り替え」 → chọn đích `39263-DICH-A` → 「絞込みを設定する」 → thêm tag `39263-TAG-1` → 「保存」<br>3. Bấm nút copy action của vùng 1, giữ radio mặc định, chọn **vùng 3** làm đích → copy<br>4. Mở 「絞込みを設定する」 của vùng 3 và của vùng 1, đọc điều kiện ở từng vùng<br>5. Lưu richmenu ở bước 4<br>6. Mở lại từ màn sửa, đọc điều kiện lọc của vùng 1 và vùng 3 | Vùng 1: đích `39263-DICH-A` + tag `39263-TAG-1` → copy sang vùng 3 | Bước 4: vùng 3 có action chuyển richmenu; ghi lại **hiện trạng thực tế** điều kiện lọc của vùng 3 (có kèm theo hay rỗng) và xác nhận vùng 1 **vẫn giữ nguyên** tag `39263-TAG-1`. Bước 6: sau khi lưu, điều kiện lọc của mỗi vùng vẫn đúng như quan sát ở bước 4 — **không có vùng nào bị gắn nhầm điều kiện của vùng kia**, và không vùng nào mất điều kiện đã có. |  | Lấp G9 · Lấp Q9 · Ở luồng tạo mới mã vùng là số âm, copy action làm đổi cặp vùng–điều kiện **trước khi** `buildAreaRichmenu` remap sang mã vùng dương · Kho TC-RM-256→269 chỉ xét loại action, không mang filter · Điều kiện lọc **có** được copy theo hay không thì spec chưa quy định → ghi nhận hiện trạng, hỏi leader, không tự chọn đáp án · Evidence: ảnh modal 2 vùng trước và sau khi lưu |
| TC-REGSHARED001-04 | UI | REG-SHARED-001 | Step 3 — Action エルメ (multi action) / modal điều kiện lọc | Normal | manual | Tất cả | Tạo mới richmenu: đặt điều kiện lọc cho multi action rồi mở lại modal — dò xem có cùng lỗi với bug #39263 không | Đăng nhập admin bot **trả phí** (bot free bị chặn — xem TC-RM-163/229). Bot có tag `39263-TAG-1`. | 1. Tạo mới richmenu → STEP1 upload ảnh → STEP2 chọn bố cục 3 vùng → STEP3<br>2. Vùng 1 → tab 「エルメアクション」 → 「アクション追加・編集」 → thêm 1 action (ví dụ gửi text) → lưu modal multi action<br>3. Bấm nút đặt điều kiện lọc của action vừa thêm → thêm điều kiện tag `39263-TAG-1` → 「保存」<br>4. Đóng modal, bấm lại nút đặt điều kiện lọc của chính action đó<br>5. Đọc danh sách điều kiện và nhãn nút | tag `39263-TAG-1`, action gửi text | Mở lại modal hiện **đúng 1 dòng** điều kiện tag `39263-TAG-1`, nhãn nút khớp trạng thái đã cài — **không rỗng**. |  | Lấp G10 · Lấp Q10 · **TC dò có chủ đích**: multi action lưu filter ở `filters_v2` với `parent_id` = id action detail (kho TC-RM-302), mà ở luồng tạo mới action detail chưa tồn tại → cùng hình dạng root cause #39263, nhưng nhánh fix chỉ mở cho `parent_type = filter-rich-menu-toggle` · Nếu modal rỗng ⇒ **bug cùng pattern chưa được fix**, raise ticket RIÊNG, không gộp vào #39263 · Evidence: ảnh modal lần lưu và lần mở lại |
| TC-REGSHARED001-05 | Data | REG-SHARED-001 | Step 3 — dọn bản nháp khi mở màn tạo mới | Abnormal | manual | Tất cả | Lệnh dọn bản nháp của luồng tạo mới không được xoá lây điều kiện lọc của multi action | Đăng nhập admin bot trả phí. Bot có richmenu đích `39263-DICH-A`, tag `39263-TAG-1` (cho multi action) và tag `39263-TAG-2` (cho switch). | 1. Tạo mới richmenu → STEP3<br>2. Vùng 1: thêm multi action + đặt điều kiện lọc tag `39263-TAG-1` → 「保存」<br>3. Vùng 2: tab 「リッチメニュー切り替え」 → đích `39263-DICH-A` + điều kiện lọc tag `39263-TAG-2` → 「保存」<br>4. **Bỏ dở**: rời màn về danh sách richmenu, không lưu<br>5. Mở lại màn tạo mới richmenu (phiên thứ hai)<br>6. Query `filters_v2` của bot, tách theo loại filter: loại chuyển richmenu và loại của action | Vùng 1: tag `39263-TAG-1` (multi action) · Vùng 2: tag `39263-TAG-2` (switch) | Bản nháp **loại chuyển richmenu** (tag `39263-TAG-2`) đã bị dọn theo đúng thiết kế của bản fix. Bản ghi điều kiện lọc **của multi action** (tag `39263-TAG-1`) **không nằm trong phạm vi dọn** — lệnh xoá không được đụng tới nó. Không có bản ghi nào ngoài phạm vi "loại filter chuyển richmenu + mã vùng âm + parent_id null/0" bị xoá. |  | Lấp G10 · Lấp Q10 · Chiều ngược của TC-REGSHARED001-04: fix vừa **thêm một lệnh xoá theo phạm vi bot** → phải chứng minh nó không quét sang `parent_type` khác · RULE-07 verify DB · Đánh giá spec: Spec không ghi — hỏi leader · Evidence: kết quả query `filters_v2` trước bước 5 và sau bước 5 |
| TC-REGSHARED001-06 | UI | REG-SHARED-001 | Step 3 — Action エルメ (multi action) / modal điều kiện lọc — luồng sửa | Normal | manual | Tất cả | Hồi quy modal điều kiện lọc của multi action ở richmenu **đã lưu**: nạp, sửa, xoá vẫn như trước bản vá | Đăng nhập admin bot trả phí. Richmenu `39263-CU-3` đã hoàn thành 4 bước, vùng 1 có multi action với điều kiện lọc gồm **2 điều kiện** đã lưu từ trước. | 1. Mở `39263-CU-3` → STEP3 → vùng 1 → bấm nút đặt điều kiện lọc của action<br>2. Đối chiếu số dòng và nội dung điều kiện với cấu hình đã lưu<br>3. Xoá 1 điều kiện, thêm 1 điều kiện mới → 「保存」<br>4. Đóng modal, mở lại, đối chiếu<br>5. Lưu richmenu ở bước 4, mở lại từ màn sửa và đối chiếu lần nữa | 2 điều kiện có sẵn → xoá 1, thêm 1 | Bước 2: hiện đúng 2 điều kiện đã lưu ngay lần mở đầu tiên. Bước 4 và bước 5: hiện đúng 2 điều kiện sau khi sửa (điều kiện đã xoá không hiện lại, điều kiện mới có mặt). Không hiện lẫn điều kiện của action chuyển richmenu. |  | Lấp G10 · Lấp Q6 · Lấp Q10 · regression nhánh "cha đã có id" của multi action — nhánh mà bản fix cam kết giữ nguyên · Đánh giá spec: Spec không ghi — hỏi leader · Evidence: ảnh modal ở bước 2, 4, 5 |
| TC-DATADB001-02 | Data | DATA-DB-001 | Lưu & điều hướng 4 bước (luồng tạo mới) | Abnormal | manual | Tất cả | Đi hết 4 bước nhưng KHÔNG lưu: không được tạo richmenu nào và bản nháp điều kiện lọc không được gắn vào đâu | Đăng nhập admin bot trả phí. Bot có richmenu đích `39263-DICH-A` và tag `39263-TAG-1`. Ghi lại **số lượng richmenu hiện có** ở màn danh sách. | 1. Tạo mới richmenu, đặt tên `39263-KHONGLUU` → STEP1 upload ảnh → STEP2 chọn bố cục<br>2. STEP3: vùng 1 → đích `39263-DICH-A` → đặt điều kiện lọc tag `39263-TAG-1` → 「保存」 trong modal<br>3. Bấm 「次へ」 sang **STEP4** nhưng **KHÔNG** bấm 「保存してTOPに戻る」<br>4. Thoát khỏi màn **mà KHÔNG bấm** 「保存してTOPに戻る」 — bấm nút TOP hoặc chuyển sang tính năng khác; nếu hiện modal xác nhận thì chọn phương án **không lưu** (ghi lại nguyên văn các nút mà modal thực sự có)<br>5. Về màn danh sách richmenu, đếm lại số richmenu và tìm `39263-KHONGLUU`<br>6. Mở lại màn tạo mới richmenu, vào STEP3, mở modal điều kiện lọc của vùng 1<br>7. Query `filters_v2` phạm vi bot: bản ghi loại filter chuyển richmenu có mã vùng âm | Tên `39263-KHONGLUU`, tag `39263-TAG-1` | Bước 5: **không** có richmenu nào tên `39263-KHONGLUU`, số lượng richmenu bằng đúng lúc đầu. Bước 6: modal điều kiện lọc **rỗng**, nhãn nút là 「絞込みを設定する」. Bước 7: bản nháp của phiên bỏ dở đã bị dọn, và **không** bản ghi điều kiện lọc nào được gắn `parent_id` sang bất kỳ richmenu có thật nào. |  | Lấp G11 · Lấp Q11 · Kho TC-RM-289 / TC-RM-294 / TC-RM-305 đã chốt nguyên tắc "chỉ 保存してTOPに戻る mới ghi DB" nhưng **không TC nào mang theo điều kiện lọc**; bản fix vừa làm bản nháp filter trở nên "có thể hồi sinh" nên phải khẳng định lại nguyên tắc này · RULE-07 verify DB · Evidence: ảnh màn danh sách trước/sau + ảnh modal rỗng + kết quả query |
| TC-DATADB001-03 | Data | DATA-DB-001 | Lưu & điều hướng 4 bước (luồng sửa) | Abnormal | manual | Tất cả | Sửa richmenu: thêm vùng mới kèm điều kiện lọc rồi bỏ dở — bản nháp của vùng mới phải được dọn khi mở lại màn sửa | Đăng nhập admin bot trả phí. Richmenu `39263-CU-5` **đã lưu**, dùng bố cục **tuỳ chỉnh**, đang có 1 vùng với action chuyển richmenu kèm điều kiện lọc tag `39263-TAG-1`. Có richmenu đích `39263-DICH-B` và 2 tag `39263-TAG-2`, `39263-TAG-8`. | 1. Mở `39263-CU-5` bằng chức năng sửa → STEP2 kẻ **thêm một vùng mới** → STEP3<br>2. Chọn vùng vừa thêm → tab 「リッチメニュー切り替え」 → chọn đích `39263-DICH-B` → 「絞込みを設定する」 → thêm điều kiện tag `39263-TAG-2` → 「保存」 trong modal<br>3. **Bỏ dở**: reload trang (hoặc bấm TOP rồi rời màn), **KHÔNG** bấm 「保存してTOPに戻る」<br>4. Mở lại `39263-CU-5` bằng chức năng sửa → STEP2 kẻ thêm **một vùng mới lần nữa** → STEP3 → chọn vùng mới này → tab 「リッチメニュー切り替え」 → chọn đích `39263-DICH-B`<br>5. Mở 「絞込みを設定する」 của vùng mới lần 2 và đọc nội dung modal<br>6. Mở 「絞込みを設定する」 của vùng cũ (tag `39263-TAG-1`) để đối chiếu<br>7. Đặt điều kiện tag `39263-TAG-8` cho vùng mới lần 2 → 「保存」 → STEP4 → 「保存してTOPに戻る」 → mở lại và đối chiếu dữ liệu điều kiện lọc của cả 2 vùng | Lần 1: vùng mới + tag `39263-TAG-2` (bỏ dở) · Lần 2: vùng mới + tag `39263-TAG-8` (lưu thật) | Bước 5: modal của vùng mới lần 2 **RỖNG** — **không** hiện lại tag `39263-TAG-2` của phiên bỏ dở. Bước 6: vùng cũ vẫn đúng tag `39263-TAG-1`, không bị đụng. Bước 7: sau khi lưu, vùng mới chỉ có tag `39263-TAG-8`; **không** bản ghi nào mang tag `39263-TAG-2` còn gắn với `39263-CU-5`. |  | Lấp G14 · Lấp Q11 · Điều kiện lọc **ghi thẳng DB ngay khi bấm 「保存」 trong modal**, không chờ 「保存してTOPに戻る」 (Leader xác nhận 2026-09-15) ⇒ bỏ dở ở luồng sửa để lại bản ghi `parent_id` = id richmenu + **mã vùng âm**. Bản fix tách `editRichMenuForm` thành 2 nhánh và khai "giữ nguyên nhánh có id" — nhánh đó chính là chỗ dọn `parent_id = id AND mã vùng âm`; **không TC nào chứng minh nó còn chạy**. NEW-13/14/16 chỉ dọn ở luồng tạo mới; NEW-19 chỉ đi happy path thêm vùng rồi lưu ngay · RULE-07 verify DB · Evidence: ảnh modal rỗng ở bước 5 + dữ liệu điều kiện lọc của `39263-CU-5` sau bước 7 |
| TC-FUNCMULTI001-01 | UI | FUNC-MULTI-001 | Step 3 — Đổi richmenu / nhiều đích trên một vùng | Normal | manual | product | Tạo mới richmenu: một vùng 3 đích đều có lọc mà bạn thoả **cả 3** — đích hiển thị đúng quy tắc ưu tiên sau khi lưu | Đăng nhập admin bot **trả phí**. Bot có 3 richmenu đích `39263-DICH-A`, `39263-DICH-B`, `39263-DICH-C` đã hoàn thành 4 bước. Có **1 LINE user thật** đã kết bạn, được gắn **cả 3** tag `39263-TAG-1`, `39263-TAG-2`, `39263-TAG-3`. | 1. Tạo mới richmenu `39263-UUTIEN` → STEP1 upload ảnh → STEP2 chọn bố cục → STEP3<br>2. Vùng 1: chọn lần lượt 3 đích theo thứ tự A → B → C<br>3. Đặt điều kiện lọc: đích A = tag `39263-TAG-1`, đích B = tag `39263-TAG-2`, đích C = tag `39263-TAG-3` (bạn test thoả cả 3)<br>4. STEP4 → 「保存してTOPに戻る」<br>5. Mở lại từ màn sửa, xác nhận cả 3 đích vẫn đúng điều kiện lọc của mình<br>6. Đối chiếu các bản ghi chuyển menu của vùng 1: ghi lại **thứ tự id** của 3 bản ghi so với thứ tự chọn ở bước 2<br>7. Gán `39263-UUTIEN` cho LINE user, **tap vùng 1 trên LINE app**, quan sát richmenu hiển thị | 3 đích A/B/C, mỗi đích 1 tag; LINE user có cả 3 tag | Bước 5: cả 3 đích giữ đúng bộ lọc riêng, không lẫn. Bước 6: thứ tự id của 3 bản ghi chuyển menu **khớp thứ tự chọn** A → B → C (A nhỏ nhất) — remap lúc lưu không đảo thứ tự. Bước 7: LINE app đổi sang **`39263-DICH-A`**, đúng quy tắc "thoả nhiều đích thì thắng là bản ghi id nhỏ hơn" đã chốt ở kho TC-RM-216. |  | Lấp G13 · Lấp Q12 · mở rộng TC-RM-216 (kho kiểm quy tắc ưu tiên nhưng với 2 đích **không** filter; TC-RM-217/218 có filter nhưng bạn chỉ thoả 0–1 đích) · `buildAreaRichmenu` ghi lại bản ghi chuyển menu khi lưu ⇒ id và thứ tự có thể đổi, kéo theo đích thắng đổi dù màn hình vẫn "trông đúng" · RULE-06 + RULE-08 → chạy product · Đánh giá spec: Đã hỏi leader (MT-09 / TC-RM-216) · Evidence: ảnh modal 3 đích + ảnh richmenu trên LINE app sau khi tap |
| TC-CONC001-01 | UI | CONC-001 | Step 3 — Đổi richmenu / dọn bản nháp khi mở màn tạo mới | Abnormal | manual | Tất cả | Hai admin khác nhau cùng tạo richmenu trên cùng một bot: bản nháp của người này không được biến mất vì người kia | Cùng 1 bot trả phí, có **2 tài khoản admin khác nhau** (user X và user Y) đều có quyền vào richmenu. Bot có richmenu đích `39263-DICH-A` và 2 tag `39263-TAG-X`, `39263-TAG-Y`. Dùng 2 trình duyệt khác nhau (hoặc 2 cửa sổ ẩn danh) để 2 phiên đăng nhập không đè nhau. | 1. Trình duyệt 1 — user X: vào màn tạo mới richmenu → STEP3 → vùng 1 chọn đích `39263-DICH-A` → đặt điều kiện tag `39263-TAG-X` → 「保存」 trong modal. Để nguyên, **chưa lưu richmenu**<br>2. Trình duyệt 2 — user Y: vào màn tạo mới richmenu **trên cùng bot đó**<br>3. Trình duyệt 1 — user X: bấm lại 「絞込みを設定する」 của vùng 1, quan sát<br>4. Trình duyệt 1 — user X: hoàn tất lưu richmenu, mở lại từ màn sửa và quan sát điều kiện lọc | user X: tag `39263-TAG-X` · user Y: chỉ mở màn tạo mới, không nhập gì | Bước 3: modal của user X **vẫn hiện** điều kiện tag `39263-TAG-X`. Bước 4: richmenu user X lưu ra **có** điều kiện lọc tag `39263-TAG-X`. Trong mọi trường hợp không được xảy ra việc điều kiện của user X bị gắn sang richmenu của user Y. |  | Lấp G6 · Lấp Q4 · Lệnh dọn bản nháp có phạm vi toàn bot, không phân biệt user → đây là chiều NEW-17 chưa phủ · Nếu kết quả là user X **mất** điều kiện thì KHÔNG tự kết luận Không đạt — ghi nhận hiện trạng và đẩy sang §6 để Leader/Dev chốt · Evidence: ảnh modal ở cả 2 trình duyệt + ảnh màn sửa sau khi lưu |

---

## 6. Spec update needed

| # | Vấn đề | Nguồn | Cần ai chốt |
|---|---|---|---|
| 1 | **Dọn bản nháp phạm vi toàn bot** — lệnh dọn ở luồng tạo mới không phân biệt tab / phiên / user. Hai phiên tạo mới song song trên cùng bot có thể xoá bản nháp của nhau (NEW-17 đã ghi nhận, TC-CONC001-01 mở rộng sang 2 user). Đây là **đánh đổi cố ý hay lỗi thiết kế**? | NEW-17 note · Studio `dev_impact` · G6 | Dev + Leader — cần chốt hành vi đúng trước khi tick Đạt/Không đạt cho NEW-17 và TC-CONC001-01 |
| 2 | **Mâu thuẫn expected — bot gói miễn phí bấm nút đặt lọc**: NEW-11 kỳ vọng "**modal lọc KHÔNG mở ra**", chặn ngay tại nút. Kho `kho-tcs/fa004` TC-RM-163 (action エルメ) ghi ngược lại: "**Modal filter MỞ ĐƯỢC** (không chặn ngay ở bước mở)", chặn ở bước **lưu** với message 「アクションの絞り込み設定は、有料プランへのアップグレードが必要です。」; TC-RM-229 (đúng ngữ cảnh switch) chỉ ghi "hiện alert upgrade plan" — không nói rõ modal có mở hay không. **Không tự chọn bên.** | NEW-11 vs TC-RM-163 / TC-RM-229 | Leader + Dev — chốt hành vi cho **richmenu switch** của bot free: chặn tại nút hay tại lưu |
| 3 | **`feature-spec.md` §9 Gap #4 vẫn mở** — "Tab「リッチメニュー切り替え」— chi tiết UI chọn Rich Menu đích" chưa có snapshot, nên toàn bộ expected của bộ TC này đang suy từ code. Kho FA-004 đã có **MT-09 (Leader chốt 2026-08-22)** ghi đủ 3 giới hạn (5 switch/vùng bot trả phí · 1 switch bot free · bot free không được set filter) và đề nghị bổ sung **BR-12** + đóng Gap #4 — **vẫn chưa được đưa vào `spec-features`**. | `spec-features/admin/rich-menu/feature-spec.md:520` · `kho-tcs/fa004` MT-09 | Leader — cho phép cập nhật `spec-features`, đóng Gap #4 |
| 4 | **`filters_v2` / `FilterV2` có nhánh dữ liệu v1 còn tồn tại không?** Nếu còn, RULE-09 / `COMPAT-LEGACY-001` bắt buộc test cả nhánh cũ và bản fix phải xử lý được cả 2 định dạng. Hiện không đủ căn cứ trong input để kết luận → **không tự đề xuất TC**, hỏi trước. | Q8 · tên bảng `filters_v2` | Dev |
| 5 | **Dữ liệu mồ côi trước bản fix**: Dev ghi "dữ liệu cũ đã lưu sai vẫn còn mồ côi" nhưng mục 5 kết luận "không cần recover data". NEW-16 chỉ chứng minh dữ liệu đó **không hồi sinh sai chỗ** — không ai khôi phục filter mà khách đã mất. Theo **RULE-04**, cần query toàn hệ thống xác nhận phạm vi trước khi đóng ticket. | `03-dev-impact.md` D4 · NEW-16 | Dev + Leader |
| 6 | **Copy action giữa vùng có kéo theo điều kiện lọc không?** `EP-37 copyActionRichItem` — spec không quy định, kho FA-004 có 16 TC cho chức năng này nhưng **không TC nào mang theo điều kiện lọc**, và MT-17 ghi nhận spec "KHÔNG mô tả modal copy action". Cần chốt hành vi đúng trước khi tester chấm Đạt/Không đạt cho `TC-FUNCSEQ001-01`. | G9 · `kho-tcs/fa004` MT-17 · TC-RM-256→269 | Leader + Dev |
| 7 | **Phạm vi bug có rộng hơn một `parent_type` không?** Điều kiện lọc của **multi action** trong luồng tạo mới cũng ở trạng thái "cha chưa có id" — cùng hình dạng root cause #39263 — nhưng nhánh fix chỉ mở cho `parent_type = filter-rich-menu-toggle`. `TC-REGSHARED001-04` là phép dò; nếu dương tính thì cần ticket riêng và có thể phải mở rộng bản fix. | G10 · Q10 · kho TC-RM-163 + TC-RM-302 | Dev — trả lời trước: `initDataFilter` còn `parent_type` nào rơi vào trạng thái cha-chưa-có-id? |
| 8 | **Bổ sung vào spec: modal điều kiện lọc ghi thẳng DB, nằm NGOÀI cổng lưu của wizard.** Leader xác nhận (2026-09-15): bấm 「保存」 trong modal lọc là **đã lưu vào dữ liệu ngay**; ở màn sửa, kể cả không bấm 「保存してTOPに戻る」 rồi reload thì điều kiện lọc **vẫn còn**. Đây là ngoại lệ so với nguyên tắc chung của wizard mà kho TC-RM-289/294/299 đã chốt ("chỉ 「保存してTOPに戻る」 mới ghi DB / thoát không lưu ⇒ không đổi") — người đọc spec hoặc kho hiện nay sẽ suy ra sai. Cũng chính ngoại lệ này tạo ra bản nháp mà bug #39263 xoay quanh. | Xác nhận của Leader 2026-09-15 · G11 · G14 · kho TC-RM-299 | Leader — cho cập nhật `spec-features/admin/rich-menu/feature-spec.md` (thêm BR cho hành vi lưu của modal lọc) và ghi chú ngoại lệ vào `kho-tcs/fa004` để TC sau không suy nhầm |
| 9 | **Nhánh bản nháp thêm vào `FilterV2::initDataFilter` có đường nào chạm tới từ tab 「リッチメニュー切り替え」 không?** Studio `dev_impact` mô tả đây là "path đếm/preview", nhưng tab 切り替え **không hiển thị số đếm**. Nếu nhánh này không reachable từ ngữ cảnh chuyển richmenu thì nó chỉ phục vụ các ngữ cảnh dùng chung (đã có NEW-21/NEW-22), hoặc là **code thừa** — cần Dev xác nhận để biết có phải test thêm gì không. | `spec_delta` 4 file · `dev_impact` · ghi chú G1 | Dev |
| 10 | **`kho-tcs/fa004` TC-RM-296 và TC-RM-297 mâu thuẫn nhau — và nhiều khả năng cả hai đều sai.** TC-RM-296 (luồng **tạo mới**): bấm TOP → modal → xác nhận thoát ⇒ richmenu **không có data, không gán được** (tức KHÔNG lưu). TC-RM-297: modal đó có **3 nút**, trong đó 「編集内容を反映」 ⇒ **áp dụng sang phía LINE rồi về TOP** (tức CÓ lưu). Hai kết luận loại trừ nhau. Thêm 2 dấu hiệu: (a) nội dung modal 「編集した内容をこのリッチメニューが表示されている友だちに反映しますがよろしいですか？」 chỉ có nghĩa ở luồng **sửa** (richmenu đã có bạn đang xem), không hợp với luồng tạo mới; (b) nguồn của cả 2 TC là corpus `r1666-r1671`, **cùng block `r1650-r1665`** mà MT-18 đã 🔴 **BÁC BỎ** (khái niệm "nút Save màu xanh lá"), và MT-18 chốt UI **chỉ có 2 nút** 「次へ」 + 「保存してTOPに戻る」. **Leader xác nhận trên GUI không thấy nút 「編集内容を反映」** ⇒ TC-RM-296/297 nhiều khả năng là corpus cũ chưa ai verify. | `kho-tcs/fa004` TC-RM-296/297 · MT-18 · xác nhận của Leader 2026-09-15 | Leader — chốt modal thoát thật sự có những nút nào, rồi sửa hoặc loại TC-RM-296/297 khỏi kho (sửa ở `kho-tcs/data/`, chạy lại `kho-tcs/build.py`) |
