# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | **MCP LME TEST STUDIO** — task #263 (round 1, branch `ai_fixbug_35351`, `reviewState=leader`), snapshot ghi vào [04-tc-list.md](04-tc-list.md) |
| Tổng số TC review | **47** |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận: 8/16 vùng ảnh hưởng đủ TC · 2 GAP · 6 RISK · 1 orphan.**

> 🔴 **Nền tảng của cả §1**: `spec_delta` của Studio có `computedAt = 2026-08-28T07:27` và `diffStat` = **3 file, 99+/3-** — tức **chỉ là diff của ĐỢT 1** (`f424bda154`). Toàn bộ 47 TC được sinh lúc `2026-08-28 07:56` từ diff này. **Đợt 2** (`2b3402c7a6`, 2026-09-05, 4 file) **chưa từng vào Studio**, nên không TC nào được viết cho nó.

| # | Chiều | Vùng ảnh hưởng | TC cover | Status | Vấn đề |
|---|---|---|---|---|---|
| **G1** | `diff code` | **Hành vi mới của đợt 2** — nới bộ lọc `content NOT LIKE '【%】'` bằng nhóm điều kiện HOẶC, khiến danh sách ở **trạng thái mặc định** lần đầu hiển thị nhóm tin 16/18/19/20/21/22 | **0/47** | **GAP** `[BLOCKER]` | Grep toàn bộ file 04: 0 TC nhắc "ngoặc vuông" / "trạng thái mặc định" / "chưa bật bộ lọc". Cả **2 nhánh** của điều kiện mới đều không có TC: (a) nhánh true — loại tin được cho qua; (b) nhánh false — loại chưa hỗ trợ (gửi hàng loạt, kịch bản, sự kiện) **vẫn phải bị ẩn**, không phát sinh dòng trắng mới |
| **G2** | `dev-impact` (F4) | **File thứ 4 của đợt 2** — model tin nhắn, hằng số dùng chung danh sách loại tin | **0** | **GAP** `[MAJOR]` | Dev không kê tên file ở mục 4.1 (mục 4 là bản chép lại của đợt 1). Không định danh được file → không viết được TC nhắm đúng. Cần Dev trả lời câu hỏi 1 ở [03-dev-impact.md](03-dev-impact.md) |
| **G3** | `dev-impact` (D4) + `diff code` | **READ-SCOPE đổi** — số dòng, phân trang, số đếm của 2 tab đều đổi sau đợt 2 (chính AI ghi: *"số dòng và phân trang sẽ đổi"*) | NEW-22 `Đạt`, NEW-23 `skip` | **RISK** `[BLOCKER]` | NEW-22 chỉ chứng minh "mỗi lần tải đủ 100 dòng, không lặp tin" — **không** đối chiếu tổng số dòng trước/sau fix. NEW-23 (mốc đếm 2 tab) `skip` vì runner không được đọc DB staging → **không có mốc đếm tin cậy nào** |
| **G4** | `dev-impact` (F6/T4) + `diff code` | **EP-05 `type_action = all`** — đổi trạng thái **toàn bộ** dùng `getMsgTalkList()`, query 4 bảng messages **không có limit** (spec §9 Cảnh báo hiệu năng #1) | NEW-24 (chỉ phạm vi **trang**) | **RISK** `[BLOCKER]` | Dev mục 3 liệt kê 6 caller nhưng **không có `getMsgTalkList`/EP-05**. Nếu `whereNotIn msg_kind` chỉ được thêm vào `getListMessagesV2` mà không vào `getMsgTalkList`, thì "chọn tất cả" sẽ tác động lên **tập bản ghi khác với tập đang hiển thị** — vi phạm BULK-001 |
| **G5** | `dev-impact` (F2) | `whereNotIn msg_kind 12-18` + **`21`** — loại bản ghi sự kiện hệ thống | NEW-47 `skip`, NEW-21 `Đạt`, NEW-43 `skip` | **RISK** `[MAJOR]` | **`msg_kind = 21` không có trong bất kỳ tài liệu nào**: mục 2 file 03 chỉ kê 7 loại (12-18), `dev_impact` Studio cũng chỉ ghi "12-18". Nhưng bug **#40572** (*"Bản ghi bỏ chặn bạn bè (msg_kind = 21) vẫn hiển thị..."*) đã **closed** ⇒ có fix cho 21 nhưng **không được ghi vào đánh giá ảnh hưởng**. TC duy nhất phủ 21 (NEW-47) `skip` |
| **G6** | `dev-impact` (BUG-b mở rộng đợt 1) | **Loại tin 19 / 20 / 21 / 22** (đặt lịch, salon, khoá học, trả lời form) lấy nội dung qua mẫu tin | NEW-17 `skip`, NEW-18 `skip`, NEW-19 `skip` | **RISK** `[MAJOR]` | Cả 3 TC đều `skip` với lý do **dữ liệu**, không phải lỗi TC: bot test có 30 tin nhóm đặt lịch nhưng **tất cả đều không có `source_message_id`**; riêng **type 22 (trả lời form) KHÔNG có `msg_kind` riêng nên không nhận diện được từ ngoài** ⇒ nhánh mở rộng của đợt 1 **chưa được verify trên dữ liệu thật lần nào** |
| **G7** | `dev-impact` (D2) | 2 truy vấn `whereIn` mỗi lần tải — chi phí phân giải | NEW-39 `skip` | **RISK** `[MAJOR]` | `skip` vì không bật được query log trên staging (môi trường do người khác vận hành). Ngoài ra điều kiện kích hoạt Dev ghi là *"chỉ chạy khi trang có tin loại 16/18"* nhưng đợt 1 đã mở rộng lên 19-22 → mô tả có thể đã lỗi thời |
| **G8** | `diff code` (rủi ro Dev tự nêu) | **Mẫu chụp `image_map`** — bản ghi cũ lưu JSON sẽ hiện nhãn ảnh kèm **ảnh hỏng** | NEW-9 `skip` | **RISK** `[MAJOR]` | Lý do skip rất đáng chú ý: *"bản fix cố ý map CẢ HAI loại capture về `msg_type = image` nên payload EP-02 của hai loại giống hệt nhau"* ⇒ **không thể phân biệt qua API**, chỉ kiểm được **bằng mắt trên UI**. Đây đúng là rủi ro #2 mà Dev tự cảnh báo và cũng tự nói không kiểm chứng được |
| **G9** | `orphan` | NEW-27 `TC-SEARCH001-02` — tìm kiếm 「メッセージ検索」 trên bảng lưu trữ đời cũ | — | **ORPHAN** `[MAJOR]` | Không thuộc điểm sửa nào của diff, nhưng là **TC `fail` duy nhất** → bug **#40608** (Medium, `logged`, đang mở). **Cần triage trước khi đóng ticket**: message bỏ sót (`id=8672864`, `msg_kind` chưa rõ) là bug **có sẵn từ trước**, hay là **hồi quy do chính `whereNotIn msg_kind` của đợt 1** loại nhầm? Hai kết luận dẫn tới 2 hành động hoàn toàn khác nhau |

**Vùng đã đủ TC (không liệt kê chi tiết)**: BUG-a (modal sticker), BUG-b (cột nội dung type 16/18), F1 (blade modal), F3 (`ajaxGetTalkListData` phân giải), F5 (`displayType`), D1 (đọc `source_messages`/`capture_templates`), T1 (FA-002 hiển thị), D3.

**T2 — FA-001 Chat 1:1: KHÔNG đề xuất TC.** Dev xác nhận *"chỉ đối chiếu cách phân giải mẫu tin, không sửa"*, và `spec_delta.files[]` cũng không chứa file nào của FA-001. Fix nằm trọn ở tầng talk-list; test lan sang màn chat 1:1 là over-coverage (AP-5).

---

## 2. Thiếu so với quan điểm test

**Kết luận: 18 quan điểm Trigger khớp task · 10 chưa cover đủ.**

> ⚠️ **20/47 TC mang mã quan điểm KHÔNG có trong [checklist-lme.md](../../framework/checklist-lme.md)** (`TOOL-NEGCTRL-001`, `TOOL-ERRHYG-001`, `TOOL-AXIS-001`, `TOOL-OLDREC-001`, `SEARCH-001`, `API-CONTRACT-001`, `SEC-INJECT-001`, `TOOL-KNOW-002`, `PERF-LATENCY-001`) → theo BƯỚC 0.6 #5, **không tính là cover** cho các quan điểm dưới đây.

| # | Mã quan điểm | Ưu tiên | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| **Q1** | `REG-SPEC-001` — Spec thay đổi giữa chừng | **Cao** | **0** | Trigger khớp tuyệt đối: spec/hành vi đổi (**đợt 2, 2026-09-05**) **SAU** khi bộ TC đã tồn tại (**2026-08-28**). Chưa ai rà lại 47 TC để gán 4 trạng thái `[Giữ nguyên]` / `[Cần sửa]` / `[Cần thêm mới]` / `[Hết hiệu lực]`. Đây là việc **phải làm trước** mọi việc khác — nhiều TC hiện chốt tiền đề "bật 「返信を含める」/bật bộ lọc mới thấy tin", vốn có thể **hết hiệu lực** sau đợt 2 | `[BLOCKER]` |
| **Q2** | `BULK-001` — Phạm vi thao tác hàng loạt sau lọc | **Cao** | NEW-24 (Normal, `Đạt`) | Chỉ 1 TC, chỉ phạm vi **trang**. Thiếu **Abnormal + Boundary** (RULE-01) và thiếu hẳn kịch bản cốt lõi của quan điểm: *áp bộ lọc thu hẹp → thao tác hàng loạt → **đếm lại số đối tượng THỰC SỰ bị tác động**, phải khớp 100%, tuyệt đối không rơi về toàn bộ*. Xem G4 | `[BLOCKER]` |
| **Q3** | `DATA-COUNT-001` — Số đếm & tổng hợp | **Cao** | NEW-23 (Normal, `skip`) | Màn có số đếm 2 tab 「一覧」/「未確認のみ」 + badge `bots.count_user_unconfirm` → Trigger bắt buộc. TC duy nhất `skip`, thiếu Abnormal + Boundary (RULE-01), và **chưa có bảng đối chiếu 4 nguồn + phép tính tay** như quan điểm yêu cầu. Checklist ghi rõ đây là **lỗi lặp nhiều nhất lịch sử bug** (12 ticket Closed / 5 tính năng) | `[BLOCKER]` |
| **Q4** | `CONC-003` — Race ở tầng giao diện | **Trung bình → Cao** (điều kiện nâng: *màn có loadmore/phân trang + filter*) | **0** | Talk-list đúng là màn lazy-load 100 dòng/lần (BR-03) **có filter** ⇒ nâng lên **Cao**. Không TC nào phủ: vừa loadmore vừa đổi filter/tab, bấm filter liên tiếp, response về không đúng thứ tự. Đợt 2 làm tập dữ liệu trả về **to hơn** nên rủi ro này tăng | `[BLOCKER]` |
| **Q5** | `ENV-003` — Khác biệt dev/staging/production | **Cao** | **0 TC production** (47/47 ở `staging`; `envAuto` cho thấy 9 lượt `local` + 2 lượt `staging`, `prd` = 0) | Task hiển thị **media** (ảnh thumbnail, ảnh sticker, ảnh mẫu chụp, **tệp PDF**) ⇒ Trigger bắt buộc, và checklist ghi rõ *"Media có update **BẮT BUỘC check trên production**"* + **không được đánh × với lý do "staging đã pass"** (RULE-08). Production dùng server media riêng `p.lmes.jp` → B2, đường dẫn và cách nhận diện type **khác staging** | `[MAJOR]` |
| **Q6** | `MEDIA-IMG-001` — Resize / tỉ lệ / kích thước | **Trung bình → BẮT BUỘC** (điều kiện nâng: *ảnh dùng cho imagemap*) | NEW-9 (`skip`) | Fix chạm thẳng mẫu chụp **`image_map`** ⇒ nâng lên bắt buộc. TC duy nhất `skip` và **về nguyên tắc không thể pass qua API** (payload image_map ≡ image). Cần TC quan sát **bằng mắt** ảnh có vỡ không, trên bản ghi cũ. Xem G8 | `[MAJOR]` |
| **Q7** | `COMPAT-LEGACY-001` — Dữ liệu đời cũ chạy song song | **Cao** | NEW-36 (`Đạt`), NEW-41 (`Đạt`) — **cả 2 đều `Abnormal`** | Thiếu **Normal + Boundary** (RULE-01), không ghi lý do. Bảng messages sharding theo năm (BR-02: `messages_v2s` → `messages` → `messages2025..2020` → `messages_page_2` → `messages_old`) là đúng dạng "đối tượng đã version-up". Thiếu hẳn case **Boundary**: một lần tải 100 dòng **vắt qua ranh giới 2 bảng shard** — chỗ dễ vỡ nhất khi thêm cột `source_message_id` vào `selectRaw` | `[MAJOR]` |
| **Q8** | `SEC-ISO-001` — Cách ly dữ liệu đa phiên | **Cao** | NEW-44 (`skip`) | `skip` vì lý do hạ tầng: *"Kho tài nguyên của lượt này chỉ gán ĐÚNG MỘT bot (mã 69 — BOT_staging)"* ⇒ không dựng được kịch bản 2 bot. Quan điểm yêu cầu phân biệt 2 tầng (multi-session cùng user / cross-account) — hiện **chưa tầng nào được chạy** | `[MAJOR]` |
| **Q9** | `LIST-001` — Search / filter / pagination / drill-down | Trung bình | NEW-22, NEW-25, NEW-26 (`Đạt`), NEW-27 (`Không đạt`) | Quan điểm đòi **4 góc**; hiện thiếu góc (2) *filter **vẫn được giữ** khi chuyển trang / đổi tab* và góc (4) *click vào số đếm mở đúng danh sách*. Góc (1) search đang **`Không đạt`** (#40608) | `[MAJOR]` |
| **Q10** | `PERF-LARGE-001` — Dữ liệu lớn theo ngưỡng THỰC TẾ | Trung bình | NEW-39 (`skip`) | Quan điểm yêu cầu **tra số liệu khách hàng lớn nhất hiện tại** rồi dựng test data bằng hoặc lớn hơn. TC hiện chỉ dùng 100 vs 10 tin và `skip` vì không bật được query log. Đợt 2 làm **tăng số dòng trả về** ⇒ ngưỡng thực tế cần đo lại | `[MINOR]` |

---

## 3. TC trùng lặp nội dung

Đã rà **47/47 TC** theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương` → `kết quả mong đợi`).

| Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| Modal chi tiết — sticker gửi **trực tiếp** (type 5, không có nguồn mẫu tin) | **NEW-35** `TC-TOOLERRHYG001-03` | **NEW-28** `TC-TOOLKNOW002-01` | `DUP-SUBSET` | Loại case: cả 2 `Normal` · Đối tượng+thao tác: cùng là *tin sticker trực tiếp → bấm 「詳細」 → quan sát ảnh sticker trong modal* · Tiền đề tương đương (**cùng `stickerId = 52002734`, cùng `packageId = 11537`**) · Expected của NEW-28 (*modal hiển thị ảnh sticker 52002734*) nằm **trọn** trong Expected của NEW-35, vốn còn khẳng định thêm phần Talk-list render bình thường. Mã quan điểm khác nhau (`TOOL-KNOW-002` vs `TOOL-ERRHYG-001`) nhưng **cả hai đều là mã nội bộ Studio**, không map được vào checklist nên không tạo khác biệt coverage | `[MINOR]` |

**Đề xuất xử lý**: gộp — giữ **NEW-35**, xoá **NEW-28**, và **chuyển nhãn `TICKET-35351` + `REQ-006` của NEW-28 sang NEW-35** để không mất vết "TC tái hiện triệu chứng thứ hai của ticket".
**Gate đã chạy**: giả định xoá NEW-28 → chạy lại BƯỚC 2 + 3 trên 46 TC còn lại: triệu chứng (b) vẫn còn NEW-35 + NEW-29 phủ, `REQ-006` vẫn còn 8 TC ⇒ **không làm mất cover**, đề xuất xoá hợp lệ.

**Các cặp đã xét và KẾT LUẬN KHÔNG trùng** (ghi lại để lần review sau khỏi rà lại):

| Cặp | Vì sao không tính trùng |
|---|---|
| NEW-11 `TC-TOOLERRHYG001-02` ⇄ NEW-40 `TC-TOOLERRHYG001-04` | Cùng quan điểm, cùng `Abnormal`, tiền đề của NEW-40 là superset — **nhưng `đối tượng + thao tác` khác tầng**: NEW-11 mở màn hình quan sát UI, NEW-40 gọi thẳng `POST /ajax/get-talk-list-v2` kiểm payload. UI và API là 2 tầng kiểm chứng độc lập |
| NEW-21 ⇄ NEW-47 | Bù nhau chứ không trùng: NEW-47 chứng minh `msg_kind` trong tập loại trừ **không hiện**, NEW-21 chứng minh `msg_kind` ngoài tập **vẫn hiện** (positive/negative control) |
| NEW-28/35 ⇄ NEW-29 `TC-REGSHARED001-03` | NEW-29 là sticker **qua mẫu tin** (type 16 + `source_message`), khác đường code với sticker trực tiếp (type 5) |
| NEW-15 ⇄ NEW-45 | Hai negative control khác đối tượng: NEW-15 = tin chữ/ảnh gửi trực tiếp; NEW-45 = tin mẫu tin **không có** `source_message_id` |
| NEW-38 ⇄ NEW-42 | Khác endpoint: EP-02 (danh sách) vs EP-03 (chi tiết) |

**Không có `DUP-INFLATE`** (không có nhóm trùng nào đang che GAP) và **không có `DUP-CONFLICT`**.

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | 17/47 TC (36%) | **Toàn bộ TC `skip` có chung 1 root cause hạ tầng, không phải lỗi TC**: staging **không cho ghi DB, không cho đọc DB**, và lượt chạy **chỉ được cấp đúng 1 bot** (mã 69 `BOT_staging`, là bot dùng chung). Hệ quả: gần như **mọi nhánh `Abnormal` / `Boundary`** (dữ liệu hỏng có chủ đích, capture bị xoá, content không phải JSON, HTML injection, PDF thiếu metadata, cách ly 2 bot, mốc đếm, query log) **không thể verify được bằng thiết lập hiện tại** | Cấp **bot test riêng trên staging có quyền seed dữ liệu**, hoặc chuyển nhóm 17 TC này sang chạy **manual trên dev/local có quyền DB**. Không được để nguyên và coi là "sẽ chạy sau" — với thiết lập hiện tại chúng **vĩnh viễn không pass được** |
| 2 | `[BLOCKER]` | Toàn bộ 47 TC | **TC được sinh từ diff ĐỢT 1**. `spec_delta.computedAt = 2026-08-28T07:27`, `diffStat` = 3 file / 99+ / 3-, `files[]` = 3 file của đợt 1. Đợt 2 (`2b3402c7a6`, 4 file, 2026-09-05) **chưa được nạp vào Studio** | Cập nhật `spec_delta` của task #263 theo commit `2b3402c7a6` **rồi mới** rà lại bộ TC theo `REG-SPEC-001` (Q1). Không nên chạy thêm lượt test nào cho tới khi làm xong bước này |
| 3 | `[MAJOR]` | 29/47 `pass` = **61.7%** | Dưới ngưỡng 80% (BƯỚC 0.6 #1). Thực chất chỉ **29 TC có kết luận test**; 17 `skip` + 1 `fail` = 18 TC **không có kết luận** | Giải quyết issue #1 rồi chạy lại. Mọi kết luận "đủ TC" ở §1 hiện chỉ ở mức `RISK` |
| 4 | `[MAJOR]` | Bug **#40573** (`High`, `rejected`) | *"Một tin nhãn dán có nội dung không phải JSON làm **trắng toàn bộ danh sách** 「チャット管理」 (không còn dòng nào, không bấm được 詳細)"* — severity **High**, nhưng trạng thái là **`rejected`** với lý do trống (`"Đồng bộ từ Redmine: Reject"`). Hai TC liên quan (NEW-10, NEW-46) đều `skip` | **Leader xác nhận lý do reject.** Nếu reject vì "không dựng được ca này trên staging" thì đó là lý do **hạ tầng**, không phải bằng chứng rủi ro không tồn tại — rủi ro làm trắng cả màn danh sách vẫn còn sống |
| 5 | `[MAJOR]` | Kết quả run | **Đang có lượt chạy #957 dở dang** (`staging`, bắt đầu `2026-09-07 02:39:21`, chưa có kết quả nào). Mọi số liệu trong report này chụp tại thời điểm review | Chờ #957 xong rồi đối chiếu lại §1/§2 trước khi chốt |
| 6 | `[MAJOR]` | [03-dev-impact.md](03-dev-impact.md) | Có `Auto-filled: 2026-09-07 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" CHƯA tick** | Tester đọc lại Redmine #35351 và tick trước khi review này có giá trị pháp lý |
| 7 | `[MAJOR]` | [03-dev-impact.md](03-dev-impact.md) mục 4 | Mục 3/4/6 của journal đợt 2 là **bản chép nguyên văn của đợt 1**: 4.1 kê 3 file trong khi commit ghi 4 file; mục 6 vẫn ghi `3 file, 76 thêm / 3 xoá`; 4.3 **không nhắc** thay đổi hành vi lớn nhất mà chính AI cảnh báo | Yêu cầu Dev/AI phát hành lại mục 4 theo đúng đợt 2 (5 câu hỏi đã liệt kê sẵn cuối file 03) |
| 8 | `[MAJOR]` | `[AP-2]` Symptom-only | Tiêu đề ticket là triệu chứng thuần (*"một số message không hiển thị"*). Đã có **3 root cause** được tìm ra ở 3 thời điểm khác nhau (modal `v-if` sai · thiếu nhánh `displayType` cho 16/18 · bộ lọc `【%】` loại nhầm ở tầng SQL). Pattern này **đã tái diễn 2 lần** | Trước khi đóng: hỏi Dev còn loại tin nào khác cũng để trống cột nội dung mà chưa được kê không (spec liệt kê tới `msg_kind` 21 và nhiều loại tin chưa có nhánh hiển thị) |
| 9 | `[MAJOR]` | `[AP-3]` Happy-path-only regression | T3 (phân trang/đếm), T4 (đổi trạng thái hàng loạt), T5 (tìm kiếm/lọc) mỗi vùng chỉ có **1 TC**, tiền đề đều là dữ liệu sạch. Không có TC nào chạy các vùng này ở trạng thái **biên/lỗi/rỗng** | Xem TC đề xuất ở §5 |
| 10 | `[MINOR]` | 20/47 TC | Mang mã quan điểm ngoài `checklist-lme.md` (9 mã, chủ yếu nhóm `TOOL-*` nội bộ Studio) → `/review-tc` không map được coverage | Khi tạo TC mới, gán thêm mã của checklist ở cột `Mã quan điểm` (§5 đã làm) |

### Chất lượng từng TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 11 | `[MAJOR]` | NEW-27 `TC-SEARCH001-02` | TC `fail` duy nhất → bug **#40608** đang mở. Chưa ai kết luận đây là bug **có sẵn** hay **hồi quy do `whereNotIn msg_kind` của đợt 1**. Message bỏ sót `id=8672864` (2024-12-31, nội dung 「action request」) nằm ở **bảng lưu trữ đời cũ** — đúng vùng mà đợt 1 đã đụng vào `selectRaw`/`where` | **Việc phải làm trước khi đóng ticket**: Dev tra `msg_kind` của `id=8672864`. Nếu nằm trong `{12..18, 21}` ⇒ đây là **hồi quy do chính fix này**, phải nâng lên `[BLOCKER]` |
| 12 | `[MINOR]` | NEW-19 (`tc 15148`) | Runner ghi rõ *"**Đã kiểm được hành vi mà testcase chốt**, trên 30 tin nhóm đặt lịch không có nguồn mẫu tin (ví dụ `id=10200068`): cột 「メッセージ内容」 hiển thị đúng phần chữ bên trong"* — tức **thực chất là Đạt** nhưng vẫn bị ghi `skip` | Sửa kết quả trên Studio (`result_submit`) thành `pass` kèm evidence sẵn có, để tỷ lệ pass phản ánh đúng thực tế |
| 13 | `[MINOR]` | NEW-27 | `testcase_list` trả `bug_tickets` **rỗng** dù bug đã raise (#40608) ⇒ snapshot [04-tc-list.md](04-tc-list.md) hiển thị cột `Số ticket bug` trống, dễ bị hiểu nhầm là "fail chưa raise ticket" | Gắn `bug_tickets` cho TC trên Studio |
| 14 | `[NIT]` | NEW-9, NEW-17, NEW-46, NEW-40, NEW-11 | Một số TC có `Kết quả mong đợi` **về nguyên tắc không quan sát được** ở tầng đã chọn — rõ nhất là NEW-9 (payload EP-02 của `image_map` và `image` giống hệt nhau theo đúng thiết kế của fix) | Chuyển các TC này sang tầng UI (`Nhóm = UI`, quan sát bằng mắt) thay vì API — xem `TC-MEDIAIMG001-01` ở §5 |

> **Không có issue về nguồn spec** — đã tìm được spec chính thức: [spec-features/admin/chat-management/feature-spec.md](../../spec-features/admin/chat-management/feature-spec.md) (FA-002), dùng các business rule **BR-02** (sharding theo năm), **BR-03** (lazy load 100), **BR-04** (mặc định ẩn tin media/sticker), **BR-09/BR-10** (`msg_kind` theo tab), **BR-11** (modal chi tiết chỉ từ `messages_v2s`), **BR-01/06/07** (đổi trạng thái xác nhận).
>
> **Không flag RULE-02 (evidence)** — cột `Evidence thực tế` trống trong file 04 chỉ là giới hạn của payload `testcase_list`. Đối chiếu `task_get_report`: **121/121 kết quả `pass` đều có artifacts** (`screenshot` + `log`), evidence đầy đủ.

---

## 5. TCs đề xuất bổ sung

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | **`kho-tcs` chưa có FA-002 (Quản lý chat) — không đối chiếu được.** Đã kiểm tra 18 file kho; file duy nhất nhắc tới talk-list là `kho-tcs/fa001-chat11-11チャット.md` dòng 24, nhưng chỉ để ghi nhận **đã LOẠI khối talk list ra khỏi phạm vi FA-001** (*"thuộc FA-002 Quản lý chat (/basic/talk-list), có spec riêng"*) ⇒ kho không chứa TC nào của màn này |
| Vùng regression phát hiện từ kho | Không có (kho chưa có FA-002) |
| Conflict expected vs kho | Không |
| GAP dùng lại TC kho (không viết mới) | Không |
| Xác nhận chống trùng | Đã đối chiếu **47 TC** ở BƯỚC 0 + kho — **không TC đề xuất nào trùng** (kiểm theo đúng 4 yếu tố của BƯỚC 4b) |

> 💡 Kho chưa có FA-002 là một khoảng trống đáng chú ý — sau khi ticket này đóng, nên chạy `/collect-tcs FA-002` để lập kho cho màn Quản lý chat.

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-REGSPEC001-01 | UI | REG-SPEC-001 | SCR-TLK-01 Danh sách tin nhắn 「チャット管理」 | Normal | manual | Tất cả | Tin gửi bằng mẫu tin hiện ngay ở trạng thái mặc định của danh sách, không cần bật bộ lọc nào | - Đã đăng nhập admin, đã chọn bot test<br>- Bot có ít nhất 1 tin gửi bằng mẫu tin (loại 16) có nguồn mẫu tin trỏ tới mẫu chụp loại chữ, nội dung 「TC35351-D2-DEFAULT」<br>- **Chưa** mở modal 「絞り込み」, chưa bật tuỳ chọn nào | 1. Mở màn 「チャット管理」 (`/basic/talk-list`)<br>2. Tick 「返信を含める」, **không** bật thêm bộ lọc nào khác<br>3. Chụp toàn bộ danh sách ở trang đầu<br>4. Tìm dòng tin mẫu tin đã dựng và quan sát cột 「メッセージ内容」<br>5. Lặp lại đúng các bước trên **bản release chưa có fix** để so sánh | Tin mẫu tin loại 16, nội dung mẫu chụp 「TC35351-D2-DEFAULT」 | Ở trạng thái mặc định, dòng tin mẫu tin **có xuất hiện** trên danh sách và cột 「メッセージ内容」 hiển thị đúng chữ 「TC35351-D2-DEFAULT」 (không trắng). Trên bản release cũ, dòng này **không xuất hiện** — chênh lệch này chính là thay đổi hành vi của đợt 2 và phải được PO/Leader xác nhận là mong muốn | | Lấp `G1` `G3` `Q1` · Đánh giá spec: **Spec không ghi** — BR-04 hiện tại mô tả hành vi CŨ, cần cập nhật (xem §6) · Evidence: screenshot danh sách bản mới **và** bản release cũ, cùng bot cùng khoảng thời gian |
| TC-REGSPEC001-02 | UI | REG-SPEC-001 | SCR-TLK-01 Danh sách tin nhắn 「チャット管理」 | Abnormal | manual | Tất cả | Loại tin chưa có cách hiển thị vẫn bị ẩn ở trạng thái mặc định, không phát sinh dòng trắng mới | - Đã đăng nhập admin, đã chọn bot test<br>- Bot có tin thuộc các loại **chưa được fix hỗ trợ**: gửi hàng loạt, kịch bản, bản ghi sự kiện — tất cả đều có cột nội dung để trống | 1. Mở màn 「チャット管理」<br>2. Tick 「返信を含める」, không bật bộ lọc nào khác<br>3. Duyệt hết trang đầu, bấm 「次へ」 thêm 2 lần<br>4. Đếm số dòng có cột 「メッセージ内容」 **trống hoàn toàn** | Tin gửi hàng loạt, tin kịch bản, bản ghi sự kiện — đều không có `source_message_id` | **Không có dòng trắng nào** xuất hiện. Các loại tin chưa được hỗ trợ vẫn bị ẩn đúng như trước fix — ngoại lệ mới chỉ mở đúng cho nhóm loại tin đã được map nội dung | | Lấp `G1` (nhánh false của điều kiện mới) · Đánh giá spec: Spec không ghi · Evidence: screenshot 3 trang liên tiếp + số dòng trắng đếm được = 0 |
| TC-REGSPEC001-03 | UI | REG-SPEC-001 | SCR-TLK-01 Danh sách tin nhắn 「チャット管理」 | Boundary | manual | Tất cả | Bộ lọc ẩn tin dạng 【 】 vẫn giữ nguyên ý nghĩa cũ với tin media và sticker gửi trực tiếp | - Đã đăng nhập admin, đã chọn bot test<br>- Bot có 1 tin ảnh và 1 tin sticker **gửi trực tiếp** (cột nội dung thật sự chứa chuỗi dạng 【 】), và 1 tin mẫu tin có cột nội dung để trống | 1. Mở màn 「チャット管理」, tick 「返信を含める」, **không** bật 「スタンプ」/media trong 「絞り込み」<br>2. Kiểm tra tin ảnh và tin sticker trực tiếp có xuất hiện không<br>3. Mở 「絞り込み」, bật 「スタンプ」 và tuỳ chọn media, áp dụng<br>4. Kiểm tra lại 3 dòng nói trên | 1 tin ảnh trực tiếp, 1 tin sticker trực tiếp (`type = 5`), 1 tin mẫu tin (`type = 16`) | Bước 2: tin ảnh và tin sticker trực tiếp **vẫn bị ẩn** (BR-04 không đổi), trong khi tin mẫu tin **có hiện**. Bước 4: cả 3 dòng đều hiện. Ngoại lệ mới chỉ áp cho dòng **có cột nội dung để trống**, không nới lỏng bộ lọc với tin thật sự có nội dung 【 】 | | Lấp `G1` `Q1` · Đánh giá spec: Spec ghi rõ (BR-04) nhưng hành vi mới chưa được ghi · Evidence: screenshot trước/sau khi bật bộ lọc |
| TC-DATACOUNT001-01 | UI | DATA-COUNT-001 | SCR-TLK-01 — số đếm 2 tab | Normal | manual | Tất cả | Số dòng của 2 tab khớp phép đếm tay sau khi nhóm tin mới xuất hiện | - Bot test **riêng**, dữ liệu biết trước: đúng 12 tin hội thoại hợp lệ (7 tin nhận, 5 tin gửi), trong đó 3 tin gửi bằng mẫu tin; thêm 4 bản ghi sự kiện hệ thống<br>- Đã ghi sẵn bảng liệt kê 16 bản ghi kèm loại | 1. Mở 「チャット管理」 tab 「一覧」, đếm số dòng thực tế hiển thị<br>2. Tick 「返信を含める」, đếm lại<br>3. Chuyển tab 「未確認のみ」, đếm số dòng<br>4. Đối chiếu 3 con số với phép đếm tay từ bảng liệt kê<br>5. Đối chiếu tiếp với số hiển thị trên badge 「未確認」 ở màn tổng quan | 16 bản ghi: 7 nhận + 5 gửi (3 trong đó là mẫu tin) + 4 sự kiện hệ thống | Tab 「一覧」 = **7** dòng · có 「返信を含める」 = **12** dòng (bao gồm cả 3 tin mẫu tin, vốn trước fix không hiện) · 4 bản ghi sự kiện **không** xuất hiện ở bất kỳ tab nào · badge 「未確認」 khớp đúng số tin chưa xác nhận đếm tay | | Lấp `G3` `Q3` · Đánh giá spec: Spec ghi rõ (BR-09, BR-10) · Evidence: bảng đối chiếu phép tính tay + screenshot 3 lần đếm |
| TC-DATACOUNT001-02 | UI | DATA-COUNT-001 | SCR-TLK-01 — số đếm 2 tab | Abnormal | manual | Tất cả | Số đếm không nhảy sai khi bot chỉ có toàn bản ghi sự kiện hệ thống | - Bot test riêng chỉ chứa **bản ghi sự kiện hệ thống** (`msg_kind` 12-18 và 21), không có tin hội thoại nào | 1. Mở 「チャット管理」 tab 「一覧」<br>2. Tick 「返信を含める」<br>3. Quan sát danh sách, số đếm và badge<br>4. Bấm 「次へ」 | Chỉ có bản ghi `msg_kind` ∈ {12,13,14,15,16,17,18,21} | Danh sách hiển thị **trạng thái rỗng đúng nghĩa** (0 dòng, không có dòng trắng), số đếm = 0, badge = 0, bấm 「次へ」 không lỗi và không load thêm dòng nào | | Lấp `G3` `G5` `Q3` · Đánh giá spec: Spec không ghi — hỏi Leader về hiển thị trạng thái rỗng · Evidence: screenshot màn rỗng + giá trị badge |
| TC-DATACOUNT001-03 | UI | DATA-COUNT-001 | SCR-TLK-01 — phân trang | Boundary | manual | Tất cả | Bot có đúng 100 tin hợp lệ — lần tải đầu lấy hết, lần 「次へ」 kế tiếp không sinh dòng lặp | - Bot test riêng có **đúng 100** tin hội thoại hợp lệ (trong đó ≥ 10 tin mẫu tin), xen kẽ 20 bản ghi sự kiện hệ thống trong cùng khoảng thời gian | 1. Mở 「チャット管理」, tick 「返信を含める」<br>2. Đếm số dòng lần tải đầu<br>3. Bấm 「次へ」<br>4. Đối chiếu ID các dòng của 2 lần tải, tìm ID trùng<br>5. Kiểm tra cờ báo hết dữ liệu | 100 tin hợp lệ + 20 bản ghi sự kiện | Lần tải đầu trả đúng **100** dòng và **không** lẫn bản ghi sự kiện. Lần 「次へ」 kế tiếp trả **0** dòng mới, **không lặp lại** bất kỳ ID nào của lần trước, và báo hết dữ liệu đúng | | Lấp `G3` `Q3` · Đánh giá spec: Spec ghi rõ (BR-03) · Evidence: danh sách ID 2 lần tải + kết quả đối chiếu trùng |
| TC-BULK001-01 | UI | BULK-001 | SCR-TLK-01 — 「ステータス 一括変更」 | Normal | manual | Tất cả | Đổi trạng thái hàng loạt sau khi lọc tác động đúng 100% số dòng đã lọc | - Bot test riêng, ≥ 30 tin 「未確認」<br>- Có bộ lọc thu hẹp được kết quả về **đúng 5 dòng** đếm rõ ràng (VD lọc theo khoảng thời gian hẹp) | 1. Mở 「チャット管理」, áp bộ lọc thu hẹp còn đúng 5 dòng<br>2. Ghi lại ID của 5 dòng đó<br>3. Chọn tất cả, chọn 「確認済」, bấm 「変更」<br>4. Bỏ lọc, đếm lại số tin đã đổi sang 「確認済」<br>5. Đối chiếu tập ID bị đổi với tập 5 ID đã ghi | 5 dòng sau lọc / tổng 30 tin 「未確認」 | **Đúng 5** bản ghi đổi trạng thái, và là **đúng 5 ID** đã ghi ở bước 2. 25 tin còn lại giữ nguyên 「未確認」. Tuyệt đối không rơi về toàn bộ 30 tin | | Lấp `G4` `Q2` · Đánh giá spec: Spec ghi rõ (BR-01) · Evidence: 2 danh sách ID (đã lọc / thực sự bị đổi) khớp 100% |
| TC-BULK001-02 | UI | BULK-001 | SCR-TLK-01 — 「ステータス 一括変更」 phạm vi toàn bộ | Abnormal | manual | Tất cả | Chọn toàn bộ (không chỉ trang hiện tại) không chạm vào bản ghi sự kiện hệ thống đã bị ẩn | - Bot test riêng có **> 100** tin hợp lệ (để có nhiều hơn 1 trang) và ≥ 20 bản ghi sự kiện hệ thống `msg_kind` ∈ {12..18, 21} đang ở trạng thái chưa xác nhận<br>- Đã ghi lại ID + trạng thái của 20 bản ghi sự kiện | 1. Mở 「チャット管理」, tick 「返信を含める」<br>2. Tick ô chọn tất cả, chọn tuỳ chọn **chọn toàn bộ kết quả** (không phải chỉ trang hiện tại)<br>3. Chọn 「確認済」, bấm 「変更」<br>4. Sau khi xong, kiểm tra lại trạng thái của 20 bản ghi sự kiện đã ghi ở tiền điều kiện<br>5. Kiểm tra số tin thực sự bị đổi so với số dòng danh sách hiển thị | > 100 tin hợp lệ + 20 bản ghi sự kiện hệ thống | Số bản ghi bị đổi **khớp đúng** số dòng mà danh sách hiển thị. **20 bản ghi sự kiện hệ thống giữ nguyên trạng thái** — không bị luồng chọn-toàn-bộ chạm vào, vì chúng đã bị loại khỏi danh sách. Nếu chúng bị đổi ⇒ luồng chọn-toàn-bộ **dùng truy vấn khác** với luồng dựng danh sách, phải báo Dev ngay | | Lấp `G4` `Q2` · Đánh giá spec: **Spec không ghi** — cần Dev xác nhận `getMsgTalkList` có áp cùng điều kiện lọc không · Evidence: trạng thái trước/sau của 20 bản ghi sự kiện |
| TC-BULK001-03 | UI | BULK-001 | SCR-TLK-01 — 「ステータス 一括変更」 | Boundary | manual | Tất cả | Lọc còn đúng 1 dòng thì chỉ đúng 1 bản ghi bị đổi | - Bot test riêng, áp bộ lọc thu hẹp về **đúng 1 dòng** | 1. Áp bộ lọc cho tới khi danh sách còn đúng 1 dòng, ghi lại ID<br>2. Chọn tất cả, chọn 「確認済」, bấm 「変更」<br>3. Bỏ lọc, đếm số tin đã đổi trạng thái | 1 dòng sau lọc | Đúng **1** bản ghi đổi trạng thái, đúng ID đã ghi. Không có bản ghi nào khác bị chạm | | Lấp `Q2` (RULE-01 — bổ sung Boundary) · Đánh giá spec: Spec ghi rõ · Evidence: ID trước/sau |
| TC-CONC003-01 | UI | CONC-003 | SCR-TLK-01 — loadmore + bộ lọc | Abnormal | manual | Tất cả | Vừa tải thêm vừa đổi bộ lọc thì kết quả cuối khớp điều kiện cuối cùng, không lặp dòng | - Bot test có > 300 tin hợp lệ trải nhiều khoảng thời gian, trong đó có tin mẫu tin<br>- Mở DevTools tab Network, bật throttling **Slow 3G** | 1. Mở 「チャット管理」, tick 「返信を含める」<br>2. Bấm 「次へ」 2 lần liên tiếp **không chờ** response trả về<br>3. Trong lúc request đang bay, đổi ngay 「表示期間」 sang một khoảng hẹp rồi áp dụng<br>4. Chờ mọi request kết thúc, quan sát danh sách<br>5. Đối chiếu ID các dòng, tìm ID trùng<br>6. Lặp lại với thao tác chuyển nhanh giữa 2 tab 「一覧」 ↔ 「未確認のみ」 | > 300 tin, throttling Slow 3G | Danh sách cuối cùng **chỉ chứa** tin thuộc khoảng thời gian đã chọn ở bước 3 (response cũ bị bỏ qua, không ghi đè). **Không có ID trùng lặp**, không nhảy vị trí cuộn. Khi chuyển tab, dữ liệu hiển thị thuộc đúng tab đang chọn | | Lấp `Q4` · Đánh giá spec: Spec không ghi — hành vi race chưa được định nghĩa, đã ghi vào §6 · Evidence: video thao tác + screenshot tab Network thể hiện thứ tự response |
| TC-ENV003-01 | UI | ENV-003 | SCR-TLK-01 + SCR-TLK-02 — media trên production | Normal | manual | **product** | Ảnh thu nhỏ, ảnh nhãn dán và tệp PDF hiển thị được trên production | - Tài khoản **production** thật, bot thật có sẵn: 1 tin mẫu tin có mẫu chụp ảnh, 1 tin nhãn dán, 1 tin PDF (loại 11)<br>- **Chỉ đọc, không tạo/xoá dữ liệu thật** | 1. Mở 「チャット管理」 trên `step.lme.jp`<br>2. Quan sát cột 「メッセージ内容」 của dòng tin mẫu tin có ảnh — ảnh thu nhỏ có tải được không<br>3. Mở DevTools tab Network, kiểm tra URL ảnh có trỏ/redirect sang `p.lmes.jp` không<br>4. Bấm 「詳細」 ở tin nhãn dán, quan sát ảnh nhãn dán trong modal<br>5. Bấm 「詳細」 ở tin PDF, quan sát khối tệp (biểu tượng, tên file, dung lượng) | Bản ghi có sẵn trên production, không tạo mới | Ảnh thu nhỏ, ảnh nhãn dán và khối tệp PDF **đều hiển thị đúng trên production**, không có ảnh vỡ / biểu tượng lỗi. URL media redirect sang `p.lmes.jp` đúng như thiết kế. Với tệp không có phần mở rộng, production (qua B2) vẫn nhận diện đúng là tệp | | Lấp `Q5` · **RULE-08** — media bắt buộc kết luận trên production, `staging` đã pass không thay thế được · Đánh giá spec: Spec ghi rõ (Catalog D) · Evidence: screenshot trên production + ảnh chụp tab Network thể hiện URL media |
| TC-MEDIAIMG001-01 | UI | MEDIA-IMG-001 | SCR-TLK-01 — mẫu chụp `image_map` | Abnormal | manual | **product** | Bản ghi `image_map` đời cũ hiển thị ảnh thật, không phải ảnh vỡ | - Tài khoản production, đã nhờ Dev truy ra **ít nhất 1 tin mẫu tin có mẫu chụp loại `image_map` tạo trước ngày fix** (đây là rủi ro #2 do chính Dev nêu)<br>- Có 1 bản ghi `image_map` tạo **sau** fix để đối chứng | 1. Mở 「チャット管理」, tìm dòng tin `image_map` đời cũ<br>2. Quan sát cột 「メッセージ内容」 **bằng mắt** — có nhãn ảnh không, ảnh có tải được không hay là biểu tượng ảnh vỡ<br>3. Bấm chuột phải vào ảnh, kiểm tra URL nguồn<br>4. Lặp lại với bản ghi `image_map` tạo sau fix<br>5. So sánh 2 trường hợp | 1 bản ghi `image_map` trước fix + 1 sau fix | Cả hai dòng đều hiển thị nhãn ảnh **kèm ảnh tải được**. Nếu bản ghi đời cũ hiện **ảnh vỡ** ⇒ đúng rủi ro Dev đã cảnh báo, phải raise ticket và quyết định có cần chuyển đổi dữ liệu cũ hay không | | Lấp `G8` `Q6` · ⚠️ **Bắt buộc kiểm bằng mắt trên UI** — không kiểm được qua API vì fix cố ý map `image_map` và `image` về cùng `msg_type` · Đánh giá spec: Spec không ghi · Evidence: screenshot cả 2 dòng + URL ảnh |
| TC-COMPATLEGACY001-03 | UI | COMPAT-LEGACY-001 | SCR-TLK-01 — bảng lưu trữ đời cũ | Normal | manual | Tất cả | Tin ở bảng lưu trữ đời cũ hiển thị đúng ở trạng thái mặc định sau đợt 2 | - Bot test có tin ở bảng lưu trữ đời cũ (VD năm 2024) **và** tin ở bảng hiện hành<br>- Biết trước ID + nội dung của 3 tin đời cũ | 1. Mở 「チャット管理」, đặt 「表示期間」 phủ khoảng dữ liệu đời cũ<br>2. Tick 「返信を含める」, không bật bộ lọc nào khác<br>3. Tìm 3 tin đời cũ đã biết ID, quan sát cột 「メッセージ内容」<br>4. Đối chiếu với nội dung đã ghi ở tiền điều kiện<br>5. So sánh cách hiển thị với bản release cũ | 3 tin ở bảng lưu trữ đời cũ, ID biết trước | 3 tin đời cũ **vẫn hiển thị đủ** và nội dung **giữ nguyên như trước fix** — cột `source_message_id` thêm vào `selectRaw` không làm lệch số cột khi gộp kết quả từ các bảng shard khác nhau. Không mất dòng, không lỗi truy vấn | | Lấp `Q7` (RULE-01 — bổ sung Normal) · **RULE-09** · Đánh giá spec: Spec ghi rõ (BR-02) · Evidence: screenshot bản mới và bản release cũ, cùng khoảng thời gian |
| TC-COMPATLEGACY001-04 | UI | COMPAT-LEGACY-001 | SCR-TLK-01 — ranh giới 2 bảng shard | Boundary | manual | Tất cả | Một lần tải 100 dòng vắt qua ranh giới 2 bảng shard vẫn đủ dòng, không lặp | - Bot test có dữ liệu **liên tục vắt qua ranh giới 2 năm** (VD 2024-12 sang 2025-01), đủ để 1 lần tải 100 dòng rơi đúng vào ranh giới<br>- Có tin mẫu tin ở **cả hai** phía ranh giới | 1. Mở 「チャット管理」, đặt 「表示期間」 phủ cả 2 năm<br>2. Tick 「返信を含める」<br>3. Bấm 「次へ」 cho tới khi danh sách đi qua mốc chuyển năm<br>4. Ghi lại ID toàn bộ các dòng đã tải<br>5. Kiểm tra ID trùng lặp và ID bị thiếu quanh mốc chuyển năm<br>6. Kiểm tra cột nội dung của tin mẫu tin ở **cả hai** phía ranh giới | Dữ liệu vắt qua 2 bảng shard, có tin mẫu tin ở cả 2 phía | Mỗi lần tải vẫn đủ **100** dòng cho tới khi hết dữ liệu. **Không có ID trùng, không có ID bị nhảy cóc** quanh mốc chuyển bảng. Tin mẫu tin ở **cả hai** phía đều phân giải được nội dung (bảng đời cũ không có cột này thì trả rỗng và bị bỏ qua, không gây lỗi) | | Lấp `Q7` (RULE-01 — bổ sung Boundary) · Đánh giá spec: Spec ghi rõ (BR-02, BR-03) · Evidence: danh sách ID liên tục quanh ranh giới |
| TC-SECISO001-02 | UI | SEC-ISO-001 | SCR-TLK-01 — cách ly 2 bot | Abnormal | manual | Tất cả | Mở 2 bot trên 2 tab và tải nhanh liên tiếp không lẫn nội dung mẫu tin của nhau | - Tài khoản có **2 bot** (A và B), mỗi bot có 1 tin mẫu tin mang nội dung định danh riêng: bot A 「TC35351-ISO-A」, bot B 「TC35351-ISO-B」<br>- ⚠️ Cần cấp thêm bot cho môi trường test (hiện chỉ có 1 bot — đúng lý do NEW-44 bị skip) | 1. Tab 1: mở 「チャット管理」 của bot A<br>2. Tab 2: mở 「チャット管理」 của bot B<br>3. Ở cả 2 tab bấm 「次へ」 gần như đồng thời<br>4. Quan sát cột 「メッセージ内容」 ở từng tab<br>5. Đổi bot qua lại 3 lần liên tiếp trên cùng 1 tab, mỗi lần không chờ tải xong<br>6. Kiểm tra nội dung hiển thị sau mỗi lần đổi | Bot A có 「TC35351-ISO-A」, bot B có 「TC35351-ISO-B」 | Mỗi tab **chỉ** hiển thị tin của đúng bot mình, không xuất hiện chuỗi định danh của bot kia. Sau mỗi lần đổi bot, danh sách thuộc đúng bot đang chọn — không có nội dung mẫu tin của bot trước còn sót lại | | Lấp `Q8` · Đánh giá spec: Spec ghi rõ (phạm vi theo bot) · Evidence: screenshot 2 tab cạnh nhau + screenshot sau mỗi lần đổi bot |
| TC-REGSHARED001-07 | UI | REG-SHARED-001 | SCR-TLK-01 — loại trừ `msg_kind` | Normal | manual | Tất cả | Từng `msg_kind` trong tập loại trừ đều bị ẩn, đặc biệt `msg_kind = 21` | - Bot test riêng có **đủ 8** bản ghi sự kiện với `msg_kind` lần lượt 12, 13, 14, 15, 16, 17, 18 và **21**, mỗi bản ghi ID biết trước<br>- Có thêm 3 tin hội thoại `msg_kind` ngoài tập để đối chứng | 1. Mở 「チャット管理」, tick 「返信を含める」<br>2. Duyệt toàn bộ danh sách (bấm 「次へ」 tới hết)<br>3. Với **từng** ID trong 8 bản ghi sự kiện, kiểm tra có xuất hiện trên danh sách không<br>4. Kiểm tra 3 tin đối chứng vẫn hiện<br>5. Lặp lại ở tab 「未確認のみ」 | 8 bản ghi `msg_kind` ∈ {12,13,14,15,16,17,18,21}, ID biết trước | **Cả 8** bản ghi, đặc biệt là **`msg_kind = 21`** (bỏ chặn bạn bè — nguyên nhân bug #40572), đều **không** xuất hiện ở bất kỳ tab nào. 3 tin đối chứng vẫn hiển thị bình thường | | Lấp `G5` · **regression** — verify lại bug #40572 đã closed · Đánh giá spec: **Spec không ghi** — `msg_kind = 21` không có trong đánh giá ảnh hưởng của Dev lẫn spec, đã đưa §6 · Evidence: bảng 8 ID kèm kết quả có/không hiển thị |
| TC-OUTTRUTH001-12 | UI | OUT-TRUTH-001 | SCR-TLK-01 — tin hành động đặt lịch | Normal | manual | Tất cả | Tin đặt lịch / salon / khoá học **có** nguồn mẫu tin hiển thị nội dung của mẫu chụp | - Bot test có **3 tin** thuộc loại 19, 20, 21 mà **có** `source_message_id` trỏ tới mẫu chụp loại chữ, nội dung lần lượt 「TC35351-BK19」, 「TC35351-BK20」, 「TC35351-BK21」<br>- ⚠️ Đây chính là dữ liệu mà lượt chạy trước **không dựng được** (30 tin đặt lịch của bot test đều không có nguồn mẫu tin) | 1. Mở 「チャット管理」, tick 「返信を含める」<br>2. Tìm lần lượt 3 dòng đã dựng<br>3. Quan sát cột 「メッセージ内容」 của từng dòng<br>4. Bấm 「詳細」 từng dòng, đối chiếu nội dung modal với cột danh sách | 3 tin loại 19 / 20 / 21 có nguồn mẫu tin | Cả 3 dòng hiển thị đúng chuỗi của mẫu chụp (「TC35351-BK19」/「-BK20」/「-BK21」), **không** hiển thị chuỗi trích từ nội dung JSON gốc. Nội dung trong modal khớp với cột danh sách | | Lấp `G6` · Đánh giá spec: Spec ghi rõ (mục 2 đợt 1 file 03) · Evidence: screenshot 3 dòng + 3 modal tương ứng |
| TC-LIST001-01 | UI | LIST-001 | SCR-TLK-01 — giữ bộ lọc khi đổi tab / tải thêm | Normal | manual | Tất cả | Bộ lọc được giữ nguyên khi bấm 「次へ」 và khi chuyển tab | - Bot test có > 200 tin trải nhiều khoảng thời gian, có tin mẫu tin | 1. Mở 「チャット管理」, đặt 「表示期間」 một khoảng hẹp và bật 「スタンプ」 trong 「絞り込み」<br>2. Ghi lại số dòng và điều kiện lọc đang áp<br>3. Bấm 「次へ」 2 lần, kiểm tra các dòng mới có còn thoả điều kiện lọc không<br>4. Chuyển sang tab 「未確認のみ」 rồi quay lại tab 「一覧」<br>5. Kiểm tra bộ lọc còn được giữ không | > 200 tin, lọc theo khoảng thời gian hẹp + bật 「スタンプ」 | Các dòng tải thêm ở bước 3 **đều thoả** điều kiện lọc đang áp. Sau khi chuyển tab qua lại, bộ lọc **vẫn được giữ nguyên** (hoặc bị reset một cách nhất quán và có thông báo rõ ràng — cần Leader chốt hành vi mong muốn) | | Lấp `Q9` (góc 2 của LIST-001) · Đánh giá spec: **Spec không ghi** — spec §9 mục 9 ghi rõ *"Bộ lọc nâng cao có được lưu giữa các phiên không? Cần kiểm tra"*, đã đưa §6 · Evidence: screenshot điều kiện lọc trước/sau mỗi thao tác |

**Các dòng GAP/RISK KHÔNG đề xuất TC — kèm lý do:**

| Mã | Lý do không đề xuất TC |
|---|---|
| **G2** (file thứ 4 — model tin nhắn) | **Chưa định danh được file**. Dev chưa trả lời file thứ 4 của commit `2b3402c7a6` là file nào; viết TC lúc này sẽ là đoán. Hành động đúng là **hỏi Dev trước** (câu hỏi 1 ở cuối file 03), sau đó mới bổ sung TC. Trên thực tế 3 TC `TC-REGSPEC001-0x` đã đi qua hành vi mà hằng số này chi phối |
| **G7** (chi phí 2 truy vấn `whereIn`) | TC đã có (NEW-39) và **được viết đúng**; nó `skip` vì không bật được query log trên staging chứ không phải vì thiếu TC. Không đẻ TC mới — **chuyển NEW-39 sang chạy trên dev/local có quyền bật query log**. Xem thêm `TC-ENV003-01` cho phần đo trên production |
| **G9** / issue #11 (bug #40608) | Đây là việc **triage của Dev**, không phải việc thiếu TC: cần tra `msg_kind` của message `id=8672864` để biết đó là bug có sẵn hay hồi quy do `whereNotIn`. Viết thêm TC trước khi có câu trả lời là vô nghĩa |
| **Q10** (`PERF-LARGE-001`) | Không đề xuất TC mới ở vòng này. Quan điểm yêu cầu **tra số liệu khách hàng lớn nhất hiện tại** rồi mới dựng test data — chưa có số liệu đó thì mọi ngưỡng đặt ra đều là cảm tính. Đề nghị Leader lấy số liệu bot lớn nhất (số tin trong 1 khoảng hiển thị) rồi mới viết TC ở vòng sau |
| **T2** (FA-001 Chat 1:1) | Code không bị chạm (`spec_delta.files[]` không có file nào của FA-001, Dev xác nhận *"chỉ đối chiếu, không sửa"*). Test lan sang là over-coverage (AP-5) |

---

## 6. Spec update needed

| # | Nội dung | Nguồn phát hiện | Đề xuất |
|---|---|---|---|
| 1 | **BR-04 mô tả hành vi đã lỗi thời.** [feature-spec.md](../../spec-features/admin/chat-management/feature-spec.md) dòng 448 ghi: *"Nếu `condition_message` không chứa flag tương ứng, query tự động thêm `WHERE content NOT LIKE '【%】' AND type != 5`"*. Đợt 2 đã **bọc điều kiện này thành nhóm HOẶC** với một ngoại lệ hẹp (cột nội dung để trống **VÀ** có liên kết bản ghi gửi **VÀ** loại tin thuộc nhóm được map) | §1 G1 · BƯỚC 1 | Cập nhật BR-04, ghi rõ nhóm loại tin được miễn trừ. **Đây là thay đổi hành vi nhìn thấy được với người dùng** — cần PO/Leader xác nhận việc màn danh sách mặc định có thêm nhóm tin trước nay chưa từng hiện là **đúng mong muốn** |
| 2 | **`msg_kind = 21` không tồn tại trong bất kỳ tài liệu nào.** File 03 mục 2 chỉ kê 7 loại (12-18), `dev_impact` Studio cũng chỉ ghi "12-18", spec BR-09/BR-10 không nhắc tập loại trừ. Nhưng bug #40572 (*bản ghi bỏ chặn bạn bè `msg_kind = 21`*) đã **closed** ⇒ tồn tại một fix không được ghi lại ở đâu | §1 G5 · task_get_report | Bổ sung tập `msg_kind` bị loại trừ (đầy đủ **{12..18, 21}**) vào spec BR-09/BR-10 **và** vào mục 4 của file 03. Hỏi Dev còn `msg_kind` nào khác cùng tính chất bản ghi sự kiện mà chưa bị loại không |
| 3 | **Loại tin 22 (trả lời form) không quan sát được từ bên ngoài.** Runner ghi: *"khác với type 18 và nhóm đặt lịch, type 22 KHÔNG có `msg_kind` riêng để nhận ra từ ngoài"* ⇒ đợt 1 có thêm type 22 vào danh sách phân giải nhưng **không có cách nào kiểm chứng** | §1 G6 · lý do skip NEW-17 | Spec cần định nghĩa cách nhận diện tin trả lời form từ phía client, hoặc Dev bổ sung định danh. Nếu không, requirement `REQ-002` phần type 22 **vĩnh viễn không testable** |
| 4 | **`REQ-011` do chính Studio đánh dấu là chưa có chuẩn** — *"Nhất quán giữa bộ lọc/tìm kiếm và nội dung đã phân giải (**cần leader xác nhận**)... hai điểm này spec chưa quy định sau khi đổi cách hiển thị"*: (a) hành vi khi tuỳ chọn ẩn nhãn dán/media đang tắt; (b) tìm kiếm bằng chữ **nhìn thấy trên cột nội dung** (chữ này nay đến từ mẫu chụp, trong khi `メッセージ検索` vẫn tìm trên cột nội dung gốc vốn để trống) | `task_get_context` REQ-011 | **Leader chốt**: người dùng gõ đúng chữ đang nhìn thấy trên màn hình mà không ra kết quả — có chấp nhận được không? Đây là hệ quả trực tiếp của cách fix và sẽ thành ticket khách hàng nếu không chốt trước |
| 5 | **Hành vi giữ bộ lọc chưa được định nghĩa.** Spec §9 "Điểm chưa rõ" mục 9 tự ghi: *"Bộ lọc nâng cao có được lưu giữa các phiên không? Cần kiểm tra bằng cách áp dụng filter rồi reload trang"* | §2 Q9 · spec §9 | Chốt hành vi mong muốn (giữ / reset) rồi ghi vào spec, để `TC-LIST001-01` có chuẩn đối chiếu thay vì tester tự suy diễn |
| 6 | **Hành vi race ở tầng giao diện chưa được định nghĩa** — response cũ về sau response mới thì UI xử lý thế nào | §2 Q4 | Ghi vào spec SCR-TLK-01: response đến muộn hơn điều kiện lọc hiện tại phải bị bỏ qua |
