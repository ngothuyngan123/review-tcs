# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | `MCP LME TEST STUDIO — task #305 (ticket 40904, round 1, branch ai_fixbug_40892)` |
| Tổng số TC review | `102` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `9/23 vùng ảnh hưởng đủ TC · 3 GAP · 11 RISK`

> Chiều `diff code` lấy từ `task_get_context(305, ["dev_impact","spec_delta"])` — 12 file diff (`ai_fixbug_40892`, 526+/39-) + phần `dev_impact` Studio tự tính. Chiều này nêu **4 điểm lệch hợp đồng web↔job** mà `03-dev-impact.md` của Dev **không kê**.

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | Mục tiêu chính của ticket: request booking **không còn gọi Google Sheets API đồng bộ** (diff bỏ cờ `google_sheet_sync_mode` ⇒ luôn enqueue) | `diff code` | `NEW-60` | `RISK — TC duy nhất chứng minh mục tiêu task đang ở trạng thái skip; chưa có bằng chứng request web hết outbound Google` | `[BLOCKER]` |
| G2 | `F2 — RetryErrorGoogleSheetTask.needRetryErrorGoogleCalendarLesson` + backoff `[1,5,10,30,60]` (nửa phạm vi ticket: "Retry google sheet của lesson") | `dev-impact` + `diff code` | `NEW-26` `NEW-27` `NEW-30` `NEW-32` `NEW-36` `NEW-39` `NEW-108` (pass) · `NEW-38` (blocked) | `RISK — triage run #1585 ghi nhận môi trường chạy có ENABLE_RETRY_ERROR_GOOGLE_SHEET=0 ⇒ RetryErrorGoogleSheetTask KHÔNG bao giờ chạy; 7 TC "pass" không chứng minh nhánh retry, TC mốc backoff thì blocked` | `[BLOCKER]` |
| G3 | `D1 — migration calendar_sync_googles` chạy trên DB **đã có dữ liệu** | `dev-impact` | `NEW-69` (pass) | `RISK — bug #1072 (High) vẫn OPEN: thiếu cột error_message ⇒ job ném SQLException mỗi vòng quét, hàng đợi không bao giờ cạn, chỉ lộ khi bảng CÓ dữ liệu. Không TC nào chạy migration trên bản sao dữ liệu thật` | `[BLOCKER]` |
| G4 | 14 call site enqueue của web (`CalendarSyncGoogleQueueService`, 229 dòng mới) | `diff code` | `NEW-82` (4 thao tác) | `GAP — chỉ kiểm 4 thao tác trên 1 luồng web; thiếu hẳn: **callback thanh toán trả lỗi ⇒ forceDelete booking** (BR-P23 — đường xoá booking tự động duy nhất, Leader xác nhận 2026-09-18 không có case huỷ tự động), thao tác hàng loạt trên màn 予約カレンダー, import/CSV` | `[BLOCKER]` |
| G5 | Điều phối deploy: web luôn enqueue + Java flag mặc định off ⇒ **phải bật flag mới drain** | `diff code` | `NEW-47` | `RISK — chỉ bật/tắt cờ trong cùng 1 môi trường job; thiếu cửa sổ chuyển đổi (web mới + job chưa bật), rollback job, và drain bản ghi lỗi lesson đời cũ (NEW-93 đã ghi nhận "không còn ai thử lại")` | `[MAJOR]` |
| G6 | Lệch hợp đồng (1): web ghi `payload` + `spreadsheet_id` nhưng job Java **không đọc** | `diff code` | `NEW-49` + `NEW-90` (cả 2 pass, expected ngược nhau) · `NEW-83` (skip) | `RISK — không kết luận được job dùng snapshot hay liên kết hiện hành; xem §3 DUP-1 + §6` | `[MAJOR]` |
| G7 | `F3 — findSheetRowByBookingId` khi dòng trên sheet không còn | `dev-impact` | `NEW-20` (blocked, expected **sai chuẩn**) | `RISK — Leader chốt 2026-09-18: job phải ĐÁNH DẤU không tìm thấy, KHÔNG chèn bù. NEW-20 đang yêu cầu ngược lại nên kết quả blocked/bug #1073 không phản ánh chất lượng sản phẩm; chưa có TC nào verify trạng thái đánh dấu có đúng và có phân biệt với “đã đồng bộ” không` | `[MAJOR]` |
| G8 | `D2 — khoá + cột liên kết ngược calendar_sync_googles_id` | `dev-impact` | `NEW-72` (skip) · `NEW-92` (skip) | `RISK — bug #1076 OPEN: khoá lệch dấu (bigint unsigned vs bigint) giữa hàng đợi và calendar_management / calendar_course_bookings; cả 2 TC kiểm hợp đồng khoá đều chưa chạy` | `[MAJOR]` |
| G9 | `F4 — updateGoogleSheetStatus` + luồng gỡ liên kết Google Sheet | `dev-impact` | `NEW-61` | `RISK — TC không kiểm giá trị google_sheet_status SAU khi gỡ liên kết, trong khi job Java đọc/ghi chính cột này (spec BR-55 / Q-16: cancelGoogsheet xoá token nhưng KHÔNG reset status)` | `[MAJOR]` |
| G10 | Regression friend info: thêm / xoá / đổi loại / sort câu hỏi → cột trên sheet | `diff code` (kho `TC-LSN-462`) | `NEW-16` · `NEW-22` (blocked) | `RISK — chỉ có nhánh "ghép header khi dựng lại"; bug #1075 (header ra "undefined") đang stale vì TC đổi version 2→3 chưa chạy lại` | `[MAJOR]` |
| G11 | Regression friend info kiểu 日時 **có giờ** (Bug #31595 cũ) | `dev-impact T1` (kho `TC-LSN-467`) | `NEW-13` (số 0 / multi-select / có-không) | `GAP — 0 TC cho kiểu ngày-giờ; đây đúng là chỗ từng lọt bug mất phần giờ khi sync lên sheet` | `[MAJOR]` |
| G12 | Kịch bản phá file đích thật: đổi tên tab, ẩn dòng cuối, đổi định dạng cột, di chuyển file | `diff code` (kho `TC-LSN-461`) | `NEW-17` `NEW-18` `NEW-19` | `GAP — chỉ có nhánh "tab bị xoá"; các thao tác người dùng thật trên file Google chưa có TC (và đều chạy trên sheet giả lập)` | `[MAJOR]` |
| G13 | `T1` — callback thanh toán đổi bill chưa→đã thanh toán | `dev-impact` | `NEW-85` (skip) | `RISK — TC duy nhất cho đường bill tự động chưa chạy` | `[MAJOR]` |
| G14 | Hỏng khi ghi hàng đợi (mất im lặng 1 dòng sheet) | `diff code` | `NEW-87` (skip) | `RISK — chưa chạy; chưa có cách phát hiện/bù dữ liệu cho dòng bị mất` | `[MINOR]` |

Không có TC orphan — 102/102 TC đều map được về `BUG` / `F*` / `D*` / `T*` hoặc 4 điểm lệch hợp đồng.

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `21 quan điểm Trigger khớp task · 9 chưa cover đủ`

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `ENV-003` | `Cao` | `GAP thực chất — 102/102 TC chạy env=local; triage run #1585 ghi rõ "35 TC pass thật qua job Java + sheet GIẢ LẬP". Task là job nền + tích hợp ngoài + chạm cột tiền ⇒ RULE-08 cấm kết luận từ local` | `[BLOCKER]` |
| Q2 | `INTG-SHEET-001` | `Cao` | `RISK — 7 TC nhưng chạy qua mock Google; các kịch bản phá bắt buộc của quan điểm (đổi định dạng cột, đổi tên/di chuyển file, rate limit thật, quyền bị đổi, recovery khi đứt giữa chừng) chưa có bằng chứng trên API thật` | `[BLOCKER]` |
| Q3 | `DEPLOY-LIVE-001` | `Cao` | `GAP — 0 TC cho "client cũ gọi server mới": mở màn đặt lịch TRƯỚC release, submit sau release không reload. Release này đổi hẳn đường ghi sheet và bỏ cờ google_sheet_sync_mode` | `[BLOCKER]` |
| Q4 | `BULK-001` | `Cao` | `GAP — 0 TC cho tổ hợp lọc + thao tác hàng loạt trên danh sách booking. Spec FA-019 (RA-09) ghi nhận chính luồng 50-100 booking là chỗ timeout của bản PHP cũ ⇒ phải đếm số bản ghi enqueue khớp 100% số đã lọc` | `[BLOCKER]` |
| Q5 | `JOB-001` | `Cao` | `RISK — chỉ NEW-33 (rate limit, mock). Thiếu 2 mục bắt buộc: đối chiếu "số bản ghi vào = xử lý thành công + vào hàng đợi lỗi" trên tải thật, và tra rate limit hiện hành của Google (RULE-05)` | `[MAJOR]` |
| Q6 | `COMPAT-LEGACY-001` | `Cao` | `RISK — NEW-96 (endpoint đổi trạng thái đời cũ app bản cũ) đang skip; cặp header sheet cũ ⇄ mới chưa có TC. RULE-09 không cho kết luận từ riêng nhánh mới` | `[MAJOR]` |
| Q7 | `INTG-HOOK-001` | `Cao` | `RISK — callback thanh toán chỉ có NEW-85 (skip); chưa test webhook trùng / trễ / sai thứ tự ⇒ chưa biết có sinh bản ghi hàng đợi trùng không` | `[MAJOR]` |
| Q8 | `FUNC-DATE-001` | `Cao` | `RISK — NEW-12 có biên ngày giờ của cột lịch, nhưng thiếu friend info kiểu 日時 có giờ (G11) và mốc 23:00-00:59 / JST của thời điểm job ghi` | `[MAJOR]` |
| Q9 | `DATA-MIG-001` | `Cao` | `RISK — NEW-71 / NEW-74 chỉ chạy trên DB test sạch; 2 bug OPEN (#1072 thiếu cột, #1076 lệch kiểu khoá) đều là lỗi migration trên dữ liệu thật` | `[MAJOR]` |

**Đã loại khỏi phạm vi (có nguồn kiểm chứng, không đề xuất TC)**:
- `DATA-BACKUP-001` — trigger khớp ("thêm/đổi bảng DB") nhưng **lesson/calendar không nằm trong phạm vi copy bot FA-033**: `feature-spec` FA-033 §7.5 xếp `booking_calendar` vào nhóm `is_enable=0` (bỏ qua), kho `kho-tcs/fa033-backup-データコピー.md` MT-12 / MT-21 / MT-28 xác nhận salon + lesson không được copy ⇒ bảng mới `calendar_sync_googles` không thể xuất hiện ở bot đích. Không viết TC copy bot.
- `INTG-CAL-001` — FA-019 **không** có tích hợp Google Calendar (`feature-spec` §1.2: `calendar_management.google_calendar_id` 174/174 NULL; kho FA-019 MT-62). `NEW-101` đang mang mã này nhưng nội dung thật là sync Google Sheet — xem §4 I7.

---

## 3. TC trùng lặp nội dung

Đã rà **102** TC theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`) — **1 nhóm mâu thuẫn**, không có DUP-EXACT / DUP-SUBSET / DUP-INFLATE.

| Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| DUP-1 | `NEW-90` (giữ cả 2, **không xoá**) | `NEW-49` — gộp thành 1 TC sau khi Leader chốt hành vi | `DUP-CONFLICT` | Cùng `OUT-TRUTH-001` · cùng `Boundary` · cùng thao tác "queue tồn đọng + gỡ liên kết A rồi liên kết sang B" · cùng tiền đề (job dừng, A có dữ liệu, B trống) → **expected ngược nhau**: `NEW-49` "ghi vào B (đích tại thời điểm job chạy)" vs `NEW-90` "ghi vào A (spreadsheet_id đã snapshot)". **Cả hai đang đánh `pass`** ⇒ ít nhất 1 kết quả sai | `[MAJOR]` |

- **Gate đã chạy**: giả định xoá `NEW-49` → vùng G6 mất chiều "job dùng liên kết hiện hành", nên **đổi sang GỘP** chứ không xoá; `NEW-90` cũng không được xoá (là TC hợp đồng REQ-018 duy nhất).
- `DUP-CONFLICT` → ✅ **ĐÃ CHỐT 2026-09-18 (Leader): ghi vào sheet B**. ⇒ `NEW-49` **đúng chuẩn, giữ lại**; `NEW-90` **sai chuẩn, phải sửa expected trên Studio**. Bằng chứng thêm: bug #1083 (`NEW-90` từng fail — "cả 2 booking nằm trên A: thực tế 0 ≠ mong đợi 2", tức job **ghi sang B**) được đánh `fixed` + retest pass lúc 08:36, trong khi `NEW-49` (expected ghi sang B) vẫn giữ `pass` từ trước đó — 2 kết quả không thể cùng đúng.
- Nhóm 4 TC "sheet rỗng → dựng lại toàn bộ" (`NEW-14` job · `NEW-68` LIFF khách · `NEW-105` web admin · `NEW-106` app admin) **không tính trùng** — 4 lối vào khác nhau, xoá bất kỳ cái nào đều mất cover `SYNC-APP-001` / `INTG-SHEET-001`.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | Toàn bộ 102 TC (chất lượng nguồn) | Triage của run #1585 ghi nguyên văn: **"35 TC pass thật qua job Java + sheet giả lập"** và **"18/24 TC không đạt là do 5 khiếm khuyết trong CHÍNH gói automation"** (mock Google từ chối ghi khối vào range neo A1 · `ENABLE_RETRY_ERROR_GOOGLE_SHEET=0` · sai tên lệnh artisan · `tinker --execute` không có ở Laravel 5.5 · lệnh mysql quá dài). ⇒ Con số `89/102 pass` **không phải bằng chứng hành vi thật của tích hợp Google Sheet**; một phần TC "không đạt" cũng bị hạ xuống vì lỗi công cụ chứ không phải sản phẩm | Chạy lại toàn bộ nhóm `INTG-SHEET-001` + `JOB-002` trên **Google Sheets API thật** (staging/product) sau khi sửa 5 khiếm khuyết automation; chỉ khi đó mới tính là pass |
| I2 | `[BLOCKER]` | `NEW-69` · `NEW-20` · `NEW-72` | **3 bug đang OPEN** nhưng trạng thái TC không phản ánh: #1072 (**High** — migration thiếu cột `error_message`, job chết mỗi vòng quét, hàng đợi không bao giờ cạn) đi kèm TC `NEW-69` **pass**; #1073 đi kèm `NEW-20` blocked; #1076 đi kèm `NEW-72` skip | Không duyệt round này khi #1072 còn open. Sau khi Dev bổ sung cột, chạy lại `NEW-69` trên DB **có dữ liệu** rồi mới đổi trạng thái bug |
| I3 | `[MAJOR]` | 13 TC | **9 skip + 4 blocked = 13 TC không có kết luận**, rơi đúng vào các case trọng yếu: `NEW-60` (mục tiêu task), `NEW-87`, `NEW-85`, `NEW-96`, `NEW-38`, `NEW-20`, `NEW-22`, `NEW-72`, `NEW-75`, `NEW-83`, `NEW-92`, `NEW-7`, `NEW-8` | Mỗi TC skip/blocked phải có 1 dòng lý do + hạn chạy lại; `NEW-60` và `NEW-85` là điều kiện cần để đóng ticket |
| I4 | `[MAJOR]` | Toàn bộ 102 TC | **0 TC chạy staging/production** (`envAuto`: local 102, dev/prd/staging đều 0 run) trong khi task chạm **job nền + API bên thứ 3 + cột số tiền** → vi phạm `RULE-08` / `ENV-003` | Chốt bộ tối thiểu **phải có lượt chạy trên production**: mọi TC ở §5 đều để `Phạm vi ENV = Tất cả` (chạy được cả staging để verify sớm), nhưng TC nào ghi `RULE-08` ở `Ghi chú` thì **kết luận cuối chỉ tính khi có evidence production** |
| I5 | `[MAJOR]` | Toàn bộ TC có `Kết quả thực thi` | Cột `Evidence thực tế` **rỗng 102/102** trong snapshot; chỉ các bug mới có file evidence (`1585/tc-*/...json`) → kết quả pass là tự khai ở tầng TC (`RULE-02`) | Đính kèm evidence theo đúng loại của quan điểm: ảnh/dump sheet sau từng kịch bản + log job có retry/backoff |
| I6 | `[MAJOR]` | `NEW-10` · `NEW-49` · `NEW-87` | `Kết quả mong đợi` **không phải oracle**: cả 3 TC kết thúc bằng "ghi nhận kết quả thực tế và báo leader chốt" nhưng vẫn được đánh `pass`. TC như vậy không thể phán Đạt/Không đạt | Tách làm 2: 1 TC có expected chốt (sau khi §6 chốt hành vi) + 1 mục hỏi Leader; không để expected treo mà vẫn tick pass |
| I7 | `[MAJOR]` | 15 mã quan điểm Studio | Các mã `JOB-002`(6) · `STATE-MATRIX-001`(5) · `RULE-09`(5) · `RULE-12`(3) · `TOOL-*`(12) · `PERF-LATENCY-001` · `DATA-HIST-001` · `OBS-001` **không có trong `framework/checklist-lme.md`** → 34 TC không map được coverage. Riêng `RULE-09` / `RULE-12` là **mã RULE**, không phải mã quan điểm; `NEW-101` gán `INTG-CAL-001` trong khi FA-019 không có tích hợp Google Calendar | Map lại về mã chuẩn (`JOB-002`→`JOB-001` · `STATE-MATRIX-001`→`STATE-DEP-001` · `RULE-09`→`COMPAT-LEGACY-001` · `RULE-12`→`REG-SHARED-001` · `PERF-LATENCY-001`→`PERF-LARGE-001` · `DATA-HIST-001`→`DATA-AUDIT-001` · `OBS-001`→`SEC-002` · `NEW-101`→`INTG-SHEET-001`), hoặc bổ sung mã mới vào checklist theo `RULE-10` |
| I8 | `[MINOR]` | `NEW-22` / bug #1075 | Bug "header friend info mới ra `undefined`" đang **stale** vì TC đổi version 2→3 chưa chạy lại → điểm nghi vấn thật đang bị treo, không ai theo dõi | Chạy lại `NEW-22` bản v3 trên sheet thật rồi đóng hoặc xác nhận bug |
| I9 | `[NIT]` | `NEW-8` | TC `blocked` nhưng tiêu đề "dữ liệu thông tin khách hỏng định dạng không làm job chết" là case chống sập job — nên ưu tiên chạy cùng đợt retest | — |
| I10 | `[BLOCKER]` | `NEW-90` · bug #1083 | **Bản fix đã merge đi ngược quyết định vừa chốt**: bug #1083 (“cả 2 booking phải nằm trên spreadsheet A đã snapshot”) được đánh `fixed` + retest `pass` lúc 2026-09-17 08:36 ⇒ job **đã được sửa để ghi theo snapshot**. Leader chốt 2026-09-18 là ghi theo **liên kết hiện hành** ⇒ hành vi hiện tại của code sai chuẩn, và `NEW-49` (đang `pass`) sẽ fail nếu chạy lại trên bản code sau fix | Mở lại bug #1083 với kết luận ngược, yêu cầu Dev bỏ nhánh dùng snapshot; sửa expected `NEW-90` trên Studio; chạy lại `NEW-49` + `NEW-90` + `TC-OUTTRUTH001-01` trên cùng một bản code |
| I11 | `[MAJOR]` | `NEW-20` · bug #1073 | Expected của `NEW-20` (“cập nhật booking chưa có dòng ⇒ job tự sinh bản ghi chèn bù”) **sai chuẩn** theo quyết định Leader 2026-09-18: hành vi đúng là **đánh dấu không tìm thấy dòng**, không chèn bù. Bug #1073 (open) sinh ra từ chính expected sai này | Sửa expected `NEW-20` trên Studio; đóng bug #1073 nếu job chỉ đơn thuần không chèn bù. **Chỉ giữ bug nếu** job đang đánh bản ghi là “đã đồng bộ” — vì như vậy là báo thành công giả cho việc không ghi được gì |
| I12 | `[MAJOR]` | `NEW-6` | Expected “booking đã xoá ⇒ sheet giữ nguyên dòng cũ, bản ghi hàng đợi chuyển sang *bỏ qua*” **sai chuẩn** theo quyết định Leader 2026-09-18: kết cục cuối phải là **dòng bị xoá khỏi sheet** nhờ bản ghi xoá đi kèm. TC hiện tại đang hợp thức hoá việc để lại dòng mồ côi | Sửa expected `NEW-6`: bản ghi cập nhật của booking đã xoá được đánh dấu là hợp lệ, **và** phải có bản ghi xoá theo sau dọn dòng. Nếu chạy thật mà không có bản ghi xoá nào được sinh ⇒ raise bug ở phía web (thiếu call site enqueue) |

---

## 5. TCs đề xuất bổ sung (44)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa019-datlichbaihoc-レッスン予約.md` (nhóm 「Googleスプレッドシート連携」 25 TC) + `kho-tcs/fa033-backup-データコピー.md` (xác minh phạm vi copy bot) |
| Vùng regression phát hiện từ kho | `TC-LSN-461` (đổi tên tab / ẩn dòng cuối) · `TC-LSN-462` (thêm-xoá-đổi-sort câu hỏi friend info) · `TC-LSN-463` (ma trận 13 luồng trạng thái) · `TC-LSN-464/465` (決済ステータス theo môi trường) · `TC-LSN-466` (cột コース ưu tiên `system_name`) · `TC-LSN-467` (Bug #31595 — friend info 日時 có giờ) |
| Conflict expected vs kho | `NEW-58 / NEW-89 / NEW-107 / NEW-109` (xoá booking ⇒ **xoá dòng khỏi sheet**) vs `TC-LSN-463` luồng 13 (xoá booking ⇒ dòng **vẫn còn**, hiển thị 「キャンセル（手動）」) → `[MAJOR] SPEC-CONFLICT`, đã đưa §6 mục 1 |
| GAP dùng lại TC kho (không viết mới) | `G12 → TC-LSN-461` và `G11 → TC-LSN-467` chỉ dùng làm **nguồn kịch bản**; vẫn phải viết mới vì tiền đề đổi hẳn (nay phải kiểm qua hàng đợi + job Java, kho viết cho bản PHP ghi đồng bộ) |
| Xác nhận chống trùng | Đã đối chiếu **102** TC ở BƯỚC 0 + kho FA-019 — không TC đề xuất nào trùng 4 yếu tố với TC đã có |
| Phạm vi bảng | **43 TC** = 20 TC vòng 1 (coverage dev-impact + diff code) **+ 23 TC vòng 2** (bộ quan điểm Leader gửi 2026-09-18, phân tích chi tiết ở [05-review-report.round2.md](05-review-report.round2.md)). Gộp về đây để `/sync-review-tc` push 1 lần. |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-ENV003-02 | UI | ENV-003 | Googleスプレッドシート連携 — mất kết nối Google | Abnormal | manual | Tất cả | Google không truy cập được: đặt lịch vẫn xong, dữ liệu lên sheet đầy đủ sau khi khôi phục | - Lịch bài học đã liên kết Google Spreadsheet **thật**, cờ job đồng bộ đang bật<br>- Mở sẵn file spreadsheet trên trình duyệt để quan sát<br>- Phối hợp hạ tầng để chặn tạm outbound tới Google Sheets API từ máy chủ web trong 10 phút | 1. Yêu cầu hạ tầng chặn outbound Google từ web<br>2. Tạo liên tiếp 5 booking từ màn quản lý, bấm giờ từng lần lưu<br>3. Quan sát màn hình từng lần lưu và kiểm booking trong lưới<br>4. Bỏ chặn Google, chờ job chạy<br>5. Mở sheet, đếm số dòng và đối chiếu 5 booking | 5 booking liên tiếp trong 1 khung giờ | - Cả 5 lần lưu đều thành công, không màn nào treo chờ hay báo lỗi hệ thống<br>- 5 booking đều có mặt trong lưới quản lý<br>- Sau khi bỏ chặn và job chạy: sheet có đủ 5 dòng, không trùng, không thiếu, thứ tự theo thời điểm đặt |  | Lấp `G1` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh 5 lần lưu + ảnh sheet sau khi khôi phục · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-JOB001-01 | Data | JOB-001 | Job đồng bộ — thử lại khi ghi sheet lỗi | Boundary | manual | Tất cả | Bật đúng cờ retry: mốc thử lại 1-5-10-30-60 phút và dừng sau lần thứ 5 | - Job production bật cả `ENABLE_HANDLE_CALENDAR_SYNC_GOOGLE_SHEET` **và** `ENABLE_RETRY_ERROR_GOOGLE_SHEET` (xác nhận trong log khởi động)<br>- 1 booking đã tạo, bản ghi hàng đợi đang chờ<br>- Chuẩn bị cách làm lệnh ghi sheet thất bại liên tục (thu hồi quyền ghi của tài khoản Google trên file đích) | 1. Kiểm log khởi động job: cả 2 cờ đều bật<br>2. Thu hồi quyền ghi của tài khoản Google trên file đích<br>3. Tạo 1 thao tác sinh bản ghi hàng đợi, chờ job xử lý lần đầu<br>4. Theo dõi log + màn quản lý bản ghi lỗi trong 2 giờ, ghi lại **mốc phút** của từng lần thử lại<br>5. Sau lần thử thứ 5, kiểm trạng thái bản ghi lỗi và cảnh báo phát ra | 1 bản ghi hàng đợi + file đích bị thu hồi quyền ghi | - Log ghi nhận 5 lần thử lại tại mốc +1, +5, +10, +30, +60 phút tính từ lần lỗi đầu<br>- Sau lần thứ 5 job **không** thử lại nữa, bản ghi lỗi đóng ở trạng thái cuối và có cảnh báo cho vận hành<br>- Không bản ghi nào biến mất: số vào = số thành công + số nằm ở hàng đợi lỗi |  | Lấp `G2` · `Q5` · Đánh giá spec: Spec ghi rõ (journal #136671 mục 2) · Evidence: log job có dấu thời gian 5 lần retry + bảng đối chiếu số bản ghi vào/ra · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-DATAMIG001-02 | Data | DATA-MIG-001 | Cấu trúc dữ liệu — khoá và ràng buộc | Boundary | manual | Tất cả | Kiểu khoá của bảng hàng đợi khớp bảng gốc và cột bắt buộc có giá trị mặc định hợp lệ | - Đã chạy 2 migration trên môi trường cần kiểm<br>- Có quyền xem cấu trúc bảng | 1. Xem cấu trúc `calendar_sync_googles`: kiểu và dấu của khoá chính, `calendar_id`, `calendar_booking_id`<br>2. Đối chiếu với kiểu khoá của bảng lịch và bảng đặt lịch gốc<br>3. Xem cột liên kết ngược ở bảng bản ghi lỗi: kiểu, dấu, cho phép rỗng<br>4. Xem cột phân loại kết quả: có bắt buộc nhập không, có giá trị mặc định không<br>5. Thử ghi 1 bản ghi lỗi từ phía job không truyền cột phân loại | — | - Khoá và các cột tham chiếu **cùng kiểu, cùng dấu** với bảng gốc, đủ sức chứa<br>- Cột liên kết ngược cho phép rỗng để dữ liệu cũ vẫn hợp lệ<br>- Bước 5: hoặc ghi được nhờ giá trị mặc định, hoặc báo lỗi rõ ràng và job không chết |  | Lấp `G8` · `Q9` · Đánh giá spec: Đã hỏi leader · Evidence: ảnh cấu trúc 3 bảng + kết quả bước 5 · regression bug #1076 |
| TC-BULK001-01 | UI | BULK-001 | 予約カレンダー — thao tác hàng loạt | Normal | manual | Tất cả | Lọc rồi duyệt hàng loạt N booking: số bản ghi hàng đợi và số dòng sheet đổi đúng bằng N | - Lịch có ≥ 60 booking, trong đó lọc ra được đúng 30 booking ở trạng thái 予約リクエスト<br>- Lịch đã liên kết sheet, job đang bật | 1. Mở danh sách booking, áp bộ lọc để còn đúng 30 booking; ghi lại con số hiển thị<br>2. Chọn tất cả và thực hiện duyệt hàng loạt<br>3. Quan sát màn hình kết quả và thời gian hoàn tất<br>4. Đếm số bản ghi hàng đợi mới sinh<br>5. Chờ job chạy rồi đếm số dòng trên sheet đổi sang 予約確定 | 30 booking sau lọc trên tổng 60 | - Thao tác hàng loạt hoàn tất, không dừng giữa chừng, không timeout<br>- Số bản ghi hàng đợi mới = **đúng 30**<br>- Đúng 30 dòng trên sheet đổi trạng thái; 30 booking ngoài bộ lọc **không** bị chạm |  | Lấp `G4` · `Q4` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh số lượng sau lọc + số bản ghi hàng đợi + ảnh sheet trước/sau · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-INTGHOOK001-02 | UI | INTG-HOOK-001 | 予約 có thu tiền — callback thanh toán trả về lỗi | Abnormal | manual | Tất cả | Callback thanh toán trả **fail** ⇒ hệ thống xoá booking ⇒ dòng tương ứng bị xoá khỏi Google Sheet | - Course có thu tiền qua cổng thanh toán, lịch đã liên kết sheet thật, job đang bật<br>- Chuẩn bị được cách làm thanh toán thất bại (thẻ test bị từ chối, hoặc gửi callback kết quả lỗi từ cổng)<br>- Sheet còn ≥ 2 booking khác để đối chứng | 1. Khách đặt lịch có thu tiền; ghi lại booking ID và số chỗ còn trống của khung giờ<br>2. **Chờ job chạy trước** để dòng booking kịp lên sheet; ghi lại số dòng<br>3. Làm cho callback thanh toán trả kết quả **lỗi**<br>4. Kiểm booking trong hệ thống, số chỗ trống của khung giờ và bản ghi hàng đợi sinh ra<br>5. Chờ job chạy → kiểm dòng của booking đó trên sheet và các dòng đối chứng<br>6. Lặp lại kịch bản **callback lỗi đến TRƯỚC khi job kịp ghi dòng** → chờ job → kiểm sheet | 1 booking thất bại thanh toán (2 kịch bản: trước và sau khi job ghi dòng) | Kịch bản A (đã có dòng): booking bị xoá khỏi hệ thống, số chỗ trống được trả lại; hàng đợi có bản ghi xoá; sau khi job chạy, **dòng của booking đó không còn trên sheet**, số dòng giảm đúng 1, các booking đối chứng và header không đổi. Kịch bản B (chưa kịp ghi dòng): sheet **không bao giờ** xuất hiện dòng của booking đã bị xoá — không để lại dòng mồ côi. Cả 2 kịch bản: không bản ghi hàng đợi nào kẹt hay lặp lỗi vô hạn |  | Lấp `G4` (nhánh callback lỗi ⇒ xoá booking) · Đánh giá spec: Spec ghi rõ — feature-spec FA-019 **BR-P23**: `lỗi → deleteOrderError() forceDelete booking + đếm lại chỗ` · Leader xác nhận 2026-09-18: không có case huỷ tự động, đây là đường xoá booking tự động duy nhất · Evidence: ảnh sheet trước/sau + số chỗ trống + log callback · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-DEPLOYLIVE001-01 | UI | DEPLOY-LIVE-001 | Điều phối deploy — web mới, job chưa bật | Normal | manual | Tất cả | Cửa sổ chuyển đổi: web đã deploy nhưng job chưa bật thì dữ liệu dồn hàng đợi và drain đủ khi bật | - Deploy web bản mới lên production, **chưa** bật cờ job Java<br>- Lịch đã liên kết sheet | 1. Sau khi deploy web, thực hiện 6 thao tác (thêm, duyệt, từ chối, huỷ, hoàn tiền, sửa thông tin khách)<br>2. Kiểm các thao tác trên màn quản lý có thành công không<br>3. Mở sheet kiểm có thay đổi gì không<br>4. Đếm bản ghi hàng đợi đang chờ<br>5. Bật cờ job Java, chờ job chạy rồi đối chiếu sheet với 6 thao tác | 6 thao tác trên 3 booking | - Bước 2: cả 6 thao tác thành công bình thường<br>- Bước 3: sheet **chưa** đổi<br>- Bước 4: hàng đợi có đủ 6 bản ghi chờ<br>- Bước 5: sau khi bật job, sheet phản ánh đủ 6 thao tác theo đúng thứ tự (thêm mới trước, cập nhật sau) |  | Lấp `G5` · `Q3` · Đánh giá spec: Spec ghi rõ (Studio dev_impact — điều phối deploy) · Evidence: ảnh sheet 3 mốc + bảng đếm hàng đợi · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-OUTTRUTH001-01 | UI | OUT-TRUTH-001 | Googleスプレッドシート連携 — đổi liên kết khi còn tồn đọng | Boundary | manual | Tất cả | Đổi liên kết spreadsheet khi hàng đợi còn tồn đọng — đích ghi đúng theo hành vi Leader đã chốt | - Lịch đang liên kết spreadsheet A đã có dữ liệu, spreadsheet B trống<br>- Job tạm dừng<br>- ✅ **Leader đã chốt 2026-09-18**: bản ghi tồn đọng ghi vào **đích hiện hành lúc job chạy** (= sheet B) | 1. Khi job dừng, tạo 3 booking để sinh 3 bản ghi hàng đợi; ghi lại mã spreadsheet lưu trong từng bản ghi<br>2. Vào 全体設定 > Googleスプレッドシート連携, gỡ liên kết rồi liên kết sang B<br>3. Bật job, chờ xử lý xong<br>4. Mở cả A và B, đếm số dòng và đối chiếu 3 booking<br>5. Kiểm trạng thái 3 bản ghi hàng đợi | 3 booking tồn đọng, 2 spreadsheet | - **3 bản ghi tồn đọng được ghi vào spreadsheet B** (đích hiện hành lúc job chạy); spreadsheet A **không bị chạm thêm**<br>- Không có booking nào xuất hiện ở cả 2 file, không booking nào mất<br>- 3 bản ghi hàng đợi đều kết thúc ở trạng thái đã đồng bộ<br>- B đang trống nên job **dựng lại toàn bộ booking của lịch lên B** (hành vi đúng, đã chốt 2026-09-18): 3 bản ghi tồn đọng + các booking cũ đều có mặt, mỗi booking 1 dòng |  | Lấp `G6` · thay cặp mâu thuẫn `NEW-49` / `NEW-90` (§3 DUP-1) · Đánh giá spec: Spec ghi rõ — Leader chốt 2026-09-18: ghi vào sheet B (liên kết hiện hành), KHÔNG dùng snapshot ⇒ `NEW-49` đúng chuẩn, `NEW-90` sai chuẩn · Evidence: ảnh cả 2 file + giá trị mã spreadsheet trong 3 bản ghi |
| TC-INTGSHEET001-01 | UI | INTG-SHEET-001 | Googleスプレッドシート連携 — gỡ liên kết | Abnormal | manual | Tất cả | Gỡ liên kết Google: trạng thái liên kết của lịch về đúng và hàng đợi tồn đọng không ghi bừa | - Lịch đang liên kết sheet và có 2 bản ghi hàng đợi đang chờ<br>- Job đang bật | 1. Ghi lại trạng thái liên kết hiện tại của lịch trên màn 全体設定<br>2. Bấm 「Googleアカウント接続解除」 và xác nhận<br>3. Kiểm lại màn hình: trạng thái liên kết, nút liên kết lại, cảnh báo (nếu có)<br>4. Chờ job chạy, kiểm 2 bản ghi tồn đọng xử lý ra sao<br>5. Liên kết lại tài khoản Google và kiểm màn hình không rơi vào trạng thái cảnh báo sai | 2 bản ghi hàng đợi tồn đọng | - Sau khi gỡ: màn hình về đúng trạng thái chưa liên kết, **không** hiện modal cảnh báo lỗi liên kết sai<br>- 2 bản ghi tồn đọng được bỏ qua có kiểm soát, không sinh bản ghi lỗi lặp vô hạn<br>- Liên kết lại thành công và lịch hoạt động bình thường ngay sau đó |  | Lấp `G9` · Đánh giá spec: Spec ghi rõ (feature-spec FA-019 BR-55 / Q-16 — `cancelGoogsheet` không reset trạng thái) · Evidence: ảnh màn 全体設定 3 mốc + trạng thái 2 bản ghi |
| TC-INTGSHEET001-02 | UI | INTG-SHEET-001 | Googleスプレッドシート連携 — phá file đích | Abnormal | manual | Tất cả | Người dùng đổi tên tab, ẩn dòng, đổi định dạng cột, di chuyển file: job xử lý có kiểm soát | - Lịch đã liên kết spreadsheet **thật**, sheet đã có ≥ 5 dòng<br>- Job đang bật<br>- Phạm vi TC này = thao tác lên **TAB và FILE** (đổi tên tab, ẩn dòng, đổi định dạng ô, di chuyển file); thao tác lên **cấu trúc cột/header** thuộc `TC-INTGSHEET001-05` | 1. Đổi tên tab đích trong file Google → tạo 1 booking mới → chờ job → quan sát<br>2. Ẩn dòng cuối cùng của bảng → tạo booking mới → chờ job → quan sát<br>3. Đổi định dạng cột ngày sang dạng văn bản → đổi trạng thái 1 booking → chờ job → quan sát<br>4. Di chuyển file sang thư mục khác trên Drive → tạo booking mới → chờ job → quan sát<br>5. Sau mỗi bước, kiểm trạng thái bản ghi hàng đợi và bản ghi lỗi | 4 kịch bản phá trên file thật | - Mỗi kịch bản cho kết quả **xác định và ghi lại được**: hoặc ghi đúng dòng, hoặc dựng lại/tạo tab rồi ghi, hoặc báo lỗi có thử lại — không có trường hợp mất im lặng<br>- Không kịch bản nào làm job dừng hẳn hoặc kẹt bản ghi quá thời hạn giữ<br>- Dữ liệu booking cũ trên file không bị hỏng |  | Lấp `G12` · `Q2` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh file sau từng kịch bản + log job · dẫn từ `TC-LSN-461` · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-FRIEND001-01 | UI | FRIEND-001 | 質問項目 — cột friend info trên sheet | Normal | manual | Tất cả | Thêm, xoá, đổi loại, sắp xếp lại câu hỏi: cột friend info trên sheet đổi đúng quy ước | - Lịch đã liên kết sheet thật, đang có 3 câu hỏi Q1/Q2/Q3 và vài booking cũ đã lên sheet<br>- Job đang bật<br>- Thao tác trên màn 予約管理 > レッスン予約 > chi tiết lịch > tab 全体設定 > 「予約時のお客様への質問項目」 (SCR-LSN-16) | 1. Thêm câu hỏi Q4 → tạo booking mới → chờ job → xem cột mới trên sheet<br>2. Xoá câu hỏi Q2 → tạo booking mới → chờ job → xem vị trí cột Q2 cũ<br>3. Đổi Q3 sang loại khác (rà đủ 5 loại: 短文回答 · 長文回答 · 単一選択 · 複数選択 · 日時) → tạo booking mới → chờ job → xem cột Q3<br>4. Sắp xếp lại thứ tự câu hỏi → tạo booking mới → chờ job → xem thứ tự cột<br>5. Kiểm các dòng booking cũ có bị lệch dữ liệu sang cột khác không | 4 thao tác trên bộ câu hỏi | - Thêm: có thêm đúng 1 cột cho Q4<br>- Xoá / đổi loại: cột cũ bị đẩy ra sau, dữ liệu booking cũ vẫn nằm đúng cột của nó<br>- Sắp xếp: cột hiển thị đúng thứ tự mới với booking mới<br>- Không dòng booking cũ nào bị lệch cột hay hiện giá trị lạ (ví dụ tiêu đề rỗng) |  | Lấp `G10` · Đánh giá spec: Đã hỏi leader (liên quan `NEW-22` blocked + bug #1075) · Evidence: ảnh sheet sau từng bước · dẫn từ `TC-LSN-462` · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-FUNCDATE001-01 | UI | FUNC-DATE-001 | 質問項目 — friend info kiểu ngày giờ | Boundary | manual | Tất cả | Câu hỏi kiểu 日時 có bật ghi giờ: sheet giữ đủ ngày và giờ ở các mốc biên | - Lịch đã liên kết sheet thật, có câu hỏi kiểu 日時 bật tuỳ chọn ghi giờ<br>- Job đang bật<br>- Chuẩn bị được 3 lối đặt: admin đặt trên web, admin đặt trên app, khách tự đặt qua LINE | 1. Với mỗi lối đặt, tạo booking và điền câu hỏi ngày giờ lần lượt 00:00 · giờ bất kỳ · 23:59<br>2. Kiểm giá trị hiển thị trên màn chi tiết booking<br>3. Chờ job chạy, đọc ô tương ứng trên sheet<br>4. Lập bảng 9 tổ hợp và đối chiếu màn hình với sheet<br>5. Kiểm thêm 1 booking đặt ngày cuối tháng và 1 booking ngày 29/2 (nếu có) | 3 lối đặt × 3 giá trị giờ (+2 mốc ngày biên) | - Cả 9 tổ hợp: ô trên sheet có **đủ cả ngày và giờ**, khớp giá trị hiển thị trên màn chi tiết, không bị mất phần giờ và không lệch múi giờ<br>- 2 mốc ngày biên hiển thị đúng ngày, không nhảy sang ngày khác |  | Lấp `G11` · `Q8` · Đánh giá spec: Spec ghi rõ (Bug #31595 cũ) · Evidence: bảng 9 tổ hợp + ảnh sheet · regression — dẫn từ `TC-LSN-467` · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-INTGHOOK001-01 | API | INTG-HOOK-001 | Đặt lịch có thu tiền — callback thanh toán | Abnormal | manual | Tất cả | Callback thanh toán gửi trùng hoặc đến trễ: chỉ 1 dòng trên sheet, trạng thái khớp bill | - Course có thu tiền, cổng thanh toán gửi callback về hệ thống<br>- Lịch đã liên kết sheet thật, job đang bật<br>- Có cách gửi lại callback thủ công từ cổng thanh toán | 1. Khách tạo booking có thu tiền, dừng ở trạng thái chờ thanh toán; chờ job xử lý dòng đầu<br>2. Gửi callback thanh toán thành công lần 1, chờ job chạy<br>3. Gửi lại **đúng callback đó** lần 2, chờ job chạy<br>4. Đếm số bản ghi hàng đợi sinh ra sau mỗi lần và số dòng của booking trên sheet<br>5. Đối chiếu cột số tiền và cột trạng thái thanh toán với trạng thái bill trong hệ thống | 1 booking có thu tiền + 2 lần callback giống hệt | - Booking chỉ có **đúng 1 dòng** trên sheet sau cả 2 lần callback, không sinh dòng trùng<br>- Cột số tiền và trạng thái thanh toán khớp trạng thái bill thật<br>- Log cho thấy callback lần 2 được nhận diện là trùng, không tạo thêm bản ghi hàng đợi thừa gây ghi đè lặp |  | Lấp `G13` · `Q7` · thay cho `NEW-85` đang skip · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: log callback 2 lần + ảnh sheet + trạng thái bill · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| | | | | | | | **— dưới đây là 23 TC của vòng 2 (bộ quan điểm Leader gửi 2026-09-18) —** | | | | | | |
| TC-FUNC001-01 | UI | FUNC-001 | 予約カレンダー — admin thêm đặt lịch | Normal | manual | Tất cả | Sheet trống, admin book trên **web** cho khách ngoài hệ thống LME: dựng lại toàn bộ và ghi đúng 2 cột tên | - Lịch đã liên kết sheet thật, **tab đích đang trống**<br>- Lịch đã có sẵn 2 booking của khách là bạn LINE<br>- Job đang bật | 1. Xoá sạch nội dung tab đích<br>2. Mở modal 「予約追加」, chọn cách nhập khách **không chọn từ danh sách bạn** mà nhập tay họ tên<br>3. Điền đủ câu hỏi và lưu<br>4. Kiểm bản ghi hàng đợi sinh ra<br>5. Chờ job chạy rồi đối chiếu header, số dòng và 2 cột tên khách của cả 3 booking | 1 khách nhập tay 「山田太郎」 (không phải bạn LINE) + 2 booking cũ của bạn LINE | - Sheet được dựng lại đủ header + 3 dòng, mỗi booking 1 dòng<br>- Booking nhập tay: cột 「お客様名（LINE名）」 **để trống**, cột 「お客様名（システム表示名）」 hiện 「山田太郎」<br>- 2 booking của bạn LINE vẫn đủ cả 2 cột tên<br>- Các cột friend info của booking nhập tay không bị lệch sang cột khác |  | Lấp `G15` · Đánh giá spec: Spec ghi rõ (feature-spec FA-019 — cột お客様名) · Evidence: ảnh sheet sau khi dựng lại · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-SYNCAPP001-01 | UI | SYNC-APP-001 | App quản lý — admin thêm đặt lịch | Normal | manual | Tất cả | Sheet trống, admin book trên **app** cho khách ngoài hệ thống LME cho kết quả giống web | - Lịch đã liên kết sheet thật, **tab đích đang trống**<br>- Lịch đã có sẵn 2 booking của khách là bạn LINE<br>- Job đang bật<br>- Thao tác trên **app quản lý** | 1. Xoá sạch nội dung tab đích<br>2. Trên app, thêm đặt lịch nhập tay tên khách (không chọn bạn LINE)<br>3. Lưu và xác nhận app báo thành công<br>4. Kiểm bản ghi hàng đợi<br>5. Chờ job chạy rồi đối chiếu dòng của booking này với kết quả TC-FUNC001-01 | 1 khách nhập tay 「佐藤花子」 | - Dòng sinh ra **giống hệt** khi thao tác cùng nghiệp vụ trên web: cột tên LINE trống, cột tên hệ thống có giá trị, trạng thái 「予約確定（手動追加）」<br>- Không sinh dòng thừa, không lệch cột |  | Lấp `G15` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh app + ảnh sheet · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-FRIEND001-10 | UI | FRIEND-001 | 質問項目 — booking của khách ngoài hệ thống | Normal | manual | Tất cả | Sửa thông tin trả lời form của booking khách **ngoài hệ thống LME**: cột friend info trên sheet cập nhật đúng dòng | - Booking do admin nhập tay cho khách **không gắn LINE user**, đã có dòng trên sheet đủ cột câu trả lời<br>- Lịch có ≥ 2 câu hỏi, trong đó 1 câu đặt chế độ 「友だち情報に回答を記録」 = 既存に記録<br>- Sheet còn 1 booking của khách **là bạn LINE** để đối chứng<br>- Job đang bật | 1. Ghi lại dòng của booking khách ngoài hệ thống: 2 cột tên và các ô câu trả lời<br>2. Trên **web**, mở booking đó và sửa câu trả lời của cả 2 câu hỏi → lưu<br>3. Kiểm bản ghi hàng đợi sinh ra → chờ job chạy → đọc lại dòng đó trên sheet<br>4. Trên **app quản lý**, sửa tiếp 1 câu trả lời của chính booking đó → chờ job → đọc lại<br>5. Kiểm dòng booking đối chứng (khách là bạn LINE) và log của job | 1 booking khách nhập tay + 1 booking khách là bạn LINE | Các ô câu trả lời của **đúng dòng đó** được cập nhật theo giá trị mới ở cả 2 lượt (web và app), kết quả 2 đường giống nhau. Cột 「お客様名（LINE名）」 vẫn **để trống** và cột tên hệ thống giữ nguyên tên nhập tay. Không sinh dòng mới, không dựng lại toàn bộ sheet, booking đối chứng không đổi. Câu hỏi đặt chế độ 既存に記録 **không làm job lỗi** dù booking không có hồ sơ bạn bè để ghi vào — ghi nhận rõ hành vi (bỏ qua có kiểm soát hay báo lỗi) |  | Lấp `G38` · Đánh giá spec: Đã hỏi leader · nhánh update của `TC-FUNC001-01` / `TC-SYNCAPP001-01` (mới chỉ có nhánh tạo lần đầu) · Evidence: ảnh dòng trước/sau 2 lượt sửa + log job · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-PAYSTATE001-01 | UI | PAY-STATE-001 | 予約 có thu tiền — course tắt thanh toán | Normal | manual | Tất cả | Course **không bật thanh toán**: sheet ghi 決済なし và cột số tiền để dấu gạch | - Lịch đã liên kết sheet thật, có course **tắt** thanh toán<br>- Cổng thanh toán của bot đang ở chế độ thật<br>- Job đang bật | 1. Khách đặt lịch vào course tắt thanh toán<br>2. Admin cũng thêm 1 booking thủ công vào course đó<br>3. Chờ job chạy<br>4. Đọc cột 「決済金額」 và 「決済ステータス」 của 2 dòng<br>5. Đối chiếu với trạng thái bill trong hệ thống | 2 booking trên course tắt thanh toán | - Cả 2 dòng: 「決済ステータス」 = 「決済なし」, 「決済金額」 = 「-」<br>- Trạng thái đặt lịch ở cột 「ステータス」 vẫn đúng theo chế độ duyệt<br>- Không dòng nào hiện 「テスト決済」 dù cổng đang cấu hình chế độ thật |  | Lấp `G16` · Đánh giá spec: Spec ghi rõ (kho `TC-LSN-464`) · Evidence: ảnh sheet + cấu hình course · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-PAYSTATE001-02 | UI | PAY-STATE-001 | 予約 có thu tiền — cổng Stripe | Normal | manual | Tất cả | Thanh toán qua **Stripe**: dòng sheet có đúng số tiền và trạng thái đã thanh toán | - Bot cấu hình cổng **Stripe** ở chế độ thật (tài khoản test của Stripe)<br>- Course bật thanh toán, giá 5.000<br>- Lịch đã liên kết sheet thật, job đang bật | 1. Khách đặt lịch và thanh toán bằng thẻ test Stripe<br>2. Xác nhận màn hình báo thanh toán thành công và trạng thái bill trong hệ thống<br>3. Kiểm bản ghi hàng đợi sinh ra (thêm mới + đổi thanh toán)<br>4. Chờ job chạy rồi đọc cột số tiền và trạng thái thanh toán<br>5. Đối chiếu với giao dịch trên dashboard Stripe | 1 booking 5.000 qua Stripe | - Sheet có đúng 1 dòng: 「決済金額」 = 「5,000」, 「決済ステータス」 = 「決済済み」<br>- Số tiền khớp giao dịch trên Stripe<br>- Không sinh dòng trùng dù có 2 bản ghi hàng đợi (thêm mới + đổi thanh toán) |  | Lấp `G17` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh sheet + giao dịch Stripe · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-PAYSTATE001-03 | UI | PAY-STATE-001 | 予約 có thu tiền — UnivaPay không webhook | Normal | manual | Tất cả | Thanh toán **UnivaPay không dùng webhook**: trạng thái bill chốt ngay trong luồng, sheet khớp | - Bot cấu hình **UnivaPay**, **tắt** webhook<br>- Course bật thanh toán, giá 3.000<br>- Lịch đã liên kết sheet thật, job đang bật | 1. Khách đặt lịch và thanh toán bằng thẻ test UnivaPay<br>2. Ghi lại thời điểm hoàn tất và trạng thái bill trong hệ thống<br>3. Kiểm bản ghi hàng đợi<br>4. Chờ job chạy rồi đọc 2 cột thanh toán<br>5. Đối chiếu với giao dịch trên UnivaPay | 1 booking 3.000 qua UnivaPay, webhook tắt | - Dòng sheet có 「3,000」 + 「決済済み」 ngay ở lượt job đầu tiên sau khi đặt<br>- Không có trạng thái trung gian 「未決済」 tồn tại trên sheet quá 1 vòng job<br>- Số tiền khớp giao dịch UnivaPay |  | Lấp `G18` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh sheet + giao dịch UnivaPay · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-PAYSTATE001-04 | UI | PAY-STATE-001 | 予約 có thu tiền — UnivaPay có webhook | Boundary | manual | Tất cả | Thanh toán **UnivaPay có webhook**, job bill cập nhật sau 5 phút: sheet phản ánh đúng ở cả 2 mốc | - Bot cấu hình **UnivaPay**, **bật** webhook<br>- Course bật thanh toán, giá 3.000<br>- Lịch đã liên kết sheet thật, job đồng bộ và job cập nhật bill đều bật | 1. Khách đặt lịch và thanh toán; ghi lại thời điểm<br>2. Ngay sau khi đặt (trước mốc 5 phút), chờ 1 vòng job đồng bộ rồi đọc 2 cột thanh toán và cột trạng thái booking<br>3. Chờ qua mốc 5 phút để job cập nhật bill chạy<br>4. Chờ tiếp 1 vòng job đồng bộ rồi đọc lại 2 cột đó<br>5. Đối chiếu trạng thái cuối trên sheet với trạng thái bill và trạng thái booking trong hệ thống | 1 booking 3.000, webhook bật, mốc job bill 5 phút | - Mốc trước 5 phút: dòng trên sheet tồn tại, trạng thái phản ánh đúng trạng thái **tại thời điểm đó** (chưa xác nhận thanh toán), **không** ghi sai thành 「決済済み」 sớm<br>- Mốc sau 5 phút: chính dòng đó được cập nhật thành 「決済済み」 + số tiền đúng, **không sinh dòng thứ hai**<br>- Trạng thái booking trên sheet khớp trạng thái trong hệ thống ở cả 2 mốc |  | Lấp `G19` · thay cho `NEW-85` đang skip · Đánh giá spec: Đã hỏi leader · Evidence: ảnh sheet 2 mốc + log webhook + trạng thái bill · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-FRIEND001-04 | UI | FRIEND-001 | 質問項目 — friend info cơ bản | Normal | manual | Tất cả | Sửa friend info **cơ bản** (tên hệ thống · điện thoại · ngày sinh · email) → cột trên sheet và friend info khớp nhau | - Lịch có 4 câu hỏi **liên kết friend info cơ bản**: tên hệ thống, số điện thoại, ngày sinh, email<br>- Khách là bạn LINE, đã có booking và đã lên sheet<br>- Job đang bật | 1. Ghi lại giá trị 4 trường ở màn chi tiết người dùng LINE và 4 ô tương ứng trên sheet<br>2. Trên màn quản lý booking, sửa cả 4 giá trị<br>3. Kiểm bản ghi hàng đợi loại đổi thông tin khách<br>4. Chờ job chạy rồi đọc lại 4 ô trên sheet<br>5. Mở màn chi tiết người dùng LINE kiểm 4 trường friend info | tên 「山田太郎」→「山田次郎」 · điện thoại 「09011112222」→「08033334444」 · ngày sinh 「1990/01/01」→「1990/12/31」 · email | - 4 ô trên sheet cập nhật đúng giá trị mới, đúng cột, không lệch sang cột khác<br>- Ngày sinh giữ đúng định dạng ngày, không bị Sheets đổi thành số<br>- Giá trị friend info ở màn chi tiết người dùng LINE khớp với sheet |  | Lấp `G20` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh màn chi tiết LINE user + ảnh sheet trước/sau · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-FRIEND001-05 | UI | FRIEND-001 | 質問項目 — friend info địa chỉ | Normal | manual | Tất cả | Sửa friend info kiểu **địa chỉ (5 trường)** → 5 giá trị nằm đúng cột, không lệch | - Lịch có câu hỏi kiểu địa chỉ đủ 5 trường (mã bưu điện · tỉnh · thành phố · số nhà · toà nhà)<br>- Booking đã có dòng trên sheet<br>- Job đang bật | 1. Ghi lại các ô địa chỉ hiện có trên sheet<br>2. Sửa đủ 5 trường địa chỉ của booking trên màn quản lý<br>3. Kiểm bản ghi hàng đợi<br>4. Chờ job chạy rồi đọc lại vùng cột địa chỉ<br>5. Lặp lại với trường hợp chỉ điền **một phần** (bỏ trống toà nhà) | Địa chỉ đủ 5 trường, mã bưu điện 「100-0001」 · lần 2 bỏ trống toà nhà | - 5 giá trị nằm đúng cột theo thứ tự câu hỏi, không dồn chung 1 ô và không đẩy lệch các cột phía sau<br>- Mã bưu điện 「100-0001」 hiển thị **nguyên văn**, không bị hiểu thành ngày hay phép trừ<br>- Lần 2: trường bỏ trống ra ô trống, các trường còn lại vẫn đúng cột |  | Lấp `G21` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh vùng cột địa chỉ trước/sau · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-FRIEND001-06 | UI | FRIEND-001 | 質問項目 — tiêu đề câu hỏi nhiều dòng | Boundary | manual | Tất cả | Tiêu đề câu hỏi **có 2 dòng**: header trên sheet không vỡ và cột vẫn map đúng | - Lịch có 1 câu hỏi mà tiêu đề chứa **ngắt dòng** (2 dòng) và 2 câu hỏi tiêu đề 1 dòng<br>- Tab đích đang trống để buộc job dựng lại header<br>- Job đang bật | 1. Tạo câu hỏi có tiêu đề 2 dòng<br>2. Xoá sạch tab đích, tạo 1 booking mới trả lời cả 3 câu hỏi<br>3. Chờ job chạy rồi quan sát dòng tiêu đề trên sheet<br>4. Kiểm giá trị của 3 câu trả lời nằm dưới đúng ô tiêu đề nào<br>5. Tạo tiếp 1 booking nữa và kiểm job có ghi thêm cột trùng lặp không | Tiêu đề: 「ご要望\n（自由記入）」 + 2 câu hỏi thường | - Dòng tiêu đề chỉ chiếm **1 dòng bảng**, nội dung nhiều dòng nằm gọn trong 1 ô (không đẩy dữ liệu xuống dòng dưới)<br>- 3 câu trả lời nằm đúng dưới ô tiêu đề tương ứng<br>- Booking thứ hai không làm sinh thêm cột trùng tên cho câu hỏi 2 dòng |  | Lấp `G22` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh header + ảnh 2 dòng dữ liệu · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-DATATEXT001-04 | UI | DATA-TEXT-001 | 質問項目 — câu trả lời nhiều dòng | Boundary | manual | Tất cả | Câu trả lời **2 dòng** ở cả info cơ bản lẫn info khác: không vỡ dòng, không lệch cột | - Lịch có 1 câu hỏi tự do (info khác) và 1 câu hỏi liên kết friend info cơ bản, cả hai cho nhập nhiều dòng<br>- Booking đã có dòng trên sheet, sheet còn booking khác làm đối chứng<br>- Job đang bật | 1. Sửa câu trả lời info khác thành nội dung 2 dòng<br>2. Sửa câu trả lời info cơ bản thành nội dung 2 dòng<br>3. Chờ job chạy rồi quan sát dòng của booking trên sheet<br>4. Đếm lại tổng số dòng dữ liệu và kiểm booking đối chứng<br>5. Tạo thêm 1 booking mới cũng trả lời 2 dòng để kiểm nhánh thêm mới | 「窓側希望\n禁煙席で」 cho cả 2 câu hỏi | - Cả 2 giá trị nằm **trong 1 ô**, hiển thị xuống dòng trong ô, không tách thành 2 dòng bảng<br>- Tổng số dòng dữ liệu **không tăng** sau bước 3; booking đối chứng không bị đẩy vị trí<br>- Nhánh thêm mới cũng cho kết quả tương tự |  | Lấp `G23` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh ô nhiều dòng + số dòng trước/sau · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-FUNC004-01 | UI | FUNC-004 | 質問項目 — giá trị số 0 ở info cơ bản | Boundary | manual | Tất cả | Giá trị **0 / bắt đầu bằng 0** ở friend info cơ bản không bị mất theo quy ước "0 ⇒ ô trống" | - Lịch có câu hỏi liên kết friend info cơ bản kiểu số/điện thoại và 1 câu hỏi kiểu số của info khác<br>- Booking đã có dòng trên sheet, job đang bật | 1. Điền một trường số của info cơ bản có giá trị **đúng bằng 0**<br>2. Điền info khác kiểu số giá trị 「0」 để đối chứng<br>3. Chờ job chạy rồi đọc 3 ô tương ứng<br>4. Đối chiếu với giá trị đang lưu ở màn chi tiết người dùng LINE<br>5. Ghi nhận chênh lệch (nếu có) giữa info cơ bản và info khác | 「0」 (info cơ bản) · 「0」 (info khác) | - Giá trị 「0」 của info cơ bản: hiển thị theo hành vi Leader chốt (giữ 「0」 hay để trống) và **nhất quán** với màn chi tiết người dùng LINE<br>- Giá trị 「0」 của info khác giữ đúng quy ước hiện tại (ô trống) — nếu 2 loại khác nhau thì phải là khác biệt có chủ đích, ghi rõ vào spec |  | Lấp `G24` · Đánh giá spec: Đã hỏi leader — xem §5 mục 8 · Evidence: ảnh 3 ô + ảnh màn chi tiết LINE user · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** · ranh giới: giá trị bắt đầu bằng 0 (số điện thoại, mã bưu điện) do `TC-DATATEXT001-02` lo, TC này chỉ lo **quy ước giá trị 0** |
| TC-DATATEXT001-01 | UI | DATA-TEXT-001 | 質問項目 — giá trị bị hiểu là công thức | Abnormal | manual | Tất cả | Câu trả lời bắt đầu bằng `=` `+` `-` `@` không biến thành công thức trên sheet | - Lịch có câu hỏi tự do<br>- Booking đã có dòng trên sheet, job đang bật | 1. Lần lượt đặt câu trả lời thành `=1+1`, `+81-90-1234-5678`, `-5`, `@user`<br>2. Sau mỗi giá trị, chờ job chạy rồi đọc ô tương ứng<br>3. Bấm vào ô trên Google Sheet xem thanh công thức hiển thị gì<br>4. Kiểm cả nhánh thêm mới (booking mới) và nhánh cập nhật<br>5. Kiểm job có báo lỗi hay bỏ qua bản ghi nào không | 4 giá trị: `=1+1` · `+81-90-1234-5678` · `-5` · `@user` | - Cả 4 ô hiển thị **đúng nguyên văn** giá trị khách nhập, không ô nào thành công thức, không ô nào hiện `#NAME?` / `#ERROR!`<br>- Thanh công thức cho thấy nội dung là văn bản<br>- Job xử lý bình thường, không sinh bản ghi lỗi |  | Lấp `A1` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh ô + ảnh thanh công thức · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-DATATEXT001-02 | UI | DATA-TEXT-001 | 質問項目 — giá trị bị Sheets tự ép kiểu | Boundary | manual | Tất cả | Chuỗi trông giống số/ngày giữ nguyên văn trên sheet | - Lịch có câu hỏi tự do (loại 短文回答)<br>- Booking đã có dòng trên sheet, job đang bật | 1. Đặt câu trả lời lần lượt: 「100-0001」, 「2026/09/18」, 「1/2」, 「0120」, chuỗi 18 chữ số<br>2. Sau mỗi giá trị, chờ job chạy rồi đọc ô<br>3. So sánh giá trị hiển thị với giá trị lưu trong hệ thống<br>4. Kiểm định dạng ô mà job ghi ra (văn bản hay số/ngày)<br>5. Lặp lại trên file đặt locale khác (EN) để đối chứng | 5 giá trị dễ bị ép kiểu | - Cả 5 ô giữ **đúng nguyên văn**: mã bưu điện không thành ngày, 「0120」 không mất số 0, chuỗi 18 chữ số không bị làm tròn/ký hiệu mũ<br>- Giá trị trên sheet khớp 100% giá trị trong hệ thống<br>- Kết quả không đổi khi file dùng locale khác |  | Lấp `A2` (+ bổ trợ `G24`) · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: bảng đối chiếu 5 giá trị hệ thống vs sheet · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-DATATEXT001-03 | UI | DATA-TEXT-001 | 質問項目 — chuỗi dài và ký tự đặc biệt | Boundary | manual | Tất cả | Câu trả lời rất dài và chứa ký tự đặc biệt không làm hỏng lượt ghi | - Lịch có câu hỏi tự do (loại 短文回答)<br>- Booking đã có dòng trên sheet, job đang bật | 1. Đặt câu trả lời 1.000 ký tự → chờ job → đọc ô<br>2. Đặt câu trả lời 10.000 ký tự → chờ job → đọc ô<br>3. Đặt câu trả lời chứa `"`, `\`, tab, `'abc`, ký tự full-width và half-width lẫn lộn<br>4. Sau mỗi bước kiểm trạng thái bản ghi hàng đợi và bản ghi lỗi<br>5. Kiểm các cột khác của dòng và các dòng khác | 3 bộ giá trị: 1.000 ký tự · 10.000 ký tự · ký tự đặc biệt | - Giá trị dài được ghi đủ (hoặc bị cắt theo quy tắc **đã ghi trong spec**, không cắt âm thầm)<br>- Ký tự đặc biệt giữ nguyên, `'abc` không bị mất dấu nháy<br>- Không lượt ghi nào thất bại làm **cả dòng** không lên sheet; nếu có lỗi thì phải vào nhánh bản ghi lỗi + thử lại, không nuốt |  | Lấp `A3` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh ô + trạng thái bản ghi hàng đợi 3 bước · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-INTGSHEET001-03 | UI | INTG-SHEET-001 | Googleスプレッドシート連携 — vượt số cột của tab | Boundary | manual | Tất cả | Số câu hỏi nhiều hơn số cột hiện có của tab: job mở rộng grid, không mất dòng | - Lịch có **30 câu hỏi** thông tin khách<br>- Tab đích là tab mới tạo (số cột mặc định)<br>- Job đang bật | 1. Xoá sạch tab đích để buộc job dựng lại header<br>2. Tạo 1 booking trả lời đủ 30 câu hỏi<br>3. Chờ job chạy rồi đếm số cột tiêu đề trên sheet<br>4. Kiểm 30 giá trị có nằm đúng cột không<br>5. Kiểm trạng thái bản ghi hàng đợi và log job | 1 booking × 30 câu hỏi | - Header có đủ 11 cột cố định + 30 cột câu hỏi (job tự mở rộng số cột của tab)<br>- 30 giá trị nằm đúng cột, không giá trị nào bị cắt<br>- Không lỗi vượt giới hạn lưới; bản ghi hàng đợi kết thúc ở trạng thái đã đồng bộ |  | Lấp `A4` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh header đủ 41 cột + log job · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-INTGSHEET001-04 | UI | INTG-SHEET-001 | Googleスプレッドシート連携 — vượt số dòng của tab | Boundary | manual | Tất cả | Lịch có hơn 1.000 booking: dựng lại toàn bộ vẫn ghi đủ, không mất booking | - Lịch có **> 1.000 booking** hợp lệ (dựng dữ liệu test)<br>- Tab đích để ở số dòng mặc định và đang trống<br>- Job đang bật | 1. Đếm chính xác số booking hợp lệ của lịch<br>2. Xoá sạch tab đích, tạo 1 booking mới để kích hoạt nhánh dựng lại toàn bộ<br>3. Theo dõi log job trong suốt quá trình<br>4. Đếm số dòng dữ liệu trên sheet sau khi job xong<br>5. Đối chiếu danh sách mã booking trên sheet với danh sách trong hệ thống | > 1.000 booking | - Số dòng dữ liệu = đúng số booking hợp lệ (đã trừ booking đăng ký chờ chỗ)<br>- Không booking nào thiếu, không dòng nào trùng<br>- Job không dừng giữa chừng, không lỗi vượt giới hạn lưới; thời gian hoàn tất ghi lại được |  | Lấp `A5` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: số đếm 2 nguồn + log job · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-INTGSHEET001-05 | UI | INTG-SHEET-001 | Googleスプレッドシート連携 — sửa cấu trúc file NGOÀI LME | Abnormal | manual | Tất cả | Sửa tay TRÊN FILE GOOGLE (không qua LME): đổi tên tiêu đề, chèn cột, đổi thứ tự cột, thêm dòng trên header — job không ghi lệch âm thầm | - Lịch đã liên kết sheet thật, sheet đã có header + ≥ 3 dòng booking<br>- Job đang bật<br>- ⚠️ **Mọi thao tác ở TC này làm TRỰC TIẾP trên file Google Sheet bằng tài khoản Google**, KHÔNG đụng tới màn 質問項目 của LME (thao tác trong LME đã có `TC-FRIEND001-01` / `TC-FRIEND001-07`); phạm vi TC này = **cấu trúc cột/header**, còn tab/file thuộc `TC-INTGSHEET001-02` | 1. **Trên file Google**, sửa tay chữ ở ô tiêu đề của 1 cột câu hỏi (câu hỏi trong LME giữ nguyên) → tạo booking mới → chờ job → quan sát<br>2. **Trên file Google**, chèn thêm 1 cột trống vào giữa vùng 11 cột cố định → đổi trạng thái 1 booking → chờ job → quan sát<br>3. **Trên file Google**, kéo đổi vị trí 2 cột câu hỏi → sửa thông tin khách → chờ job → quan sát<br>4. Thêm 1 dòng trống phía trên dòng tiêu đề → tạo booking mới → chờ job → quan sát<br>5. Sau mỗi bước kiểm dữ liệu booking cũ có bị ghi đè nhầm ô không | 4 kiểu phá cấu trúc | - Mỗi bước cho kết quả **xác định**: hoặc job nhận diện và dựng lại đúng, hoặc báo lỗi vào bản ghi lỗi — **không** ghi đè nhầm ô mà vẫn báo thành công<br>- Dữ liệu booking cũ không bị mất hay lệch sang cột khác<br>- Ghi lại rõ job định vị ô theo chỉ số cột hay theo tên tiêu đề để bổ sung vào spec |  | Lấp `A6` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh sheet sau từng bước + log job · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-FRIEND001-02 | UI | FRIEND-001 | 質問項目 — câu hỏi trùng tên | Abnormal | manual | Tất cả | Hai câu hỏi trùng tiêu đề và câu hỏi xoá rồi tạo lại cùng tên: giá trị vào đúng cột | - Lịch đang có 3 câu hỏi, sheet đã có dữ liệu<br>- Job đang bật | 1. Tạo thêm câu hỏi **trùng tên** với câu hỏi đang có → tạo booking trả lời cả hai → chờ job → quan sát số cột và giá trị<br>2. Xoá 1 câu hỏi rồi tạo lại câu hỏi **đúng tên cũ** → tạo booking mới → chờ job → quan sát<br>3. Kiểm booking cũ có bị đẩy giá trị sang cột mới không<br>4. Kiểm nhánh dựng lại toàn bộ (xoá sạch tab rồi kích hoạt lại) với cùng cấu hình câu hỏi<br>5. Đối chiếu giá trị từng booking với câu trả lời trong hệ thống | 2 câu hỏi trùng tên + 1 câu hỏi tái tạo cùng tên | - Mỗi câu hỏi có cột riêng (hoặc quy tắc gộp được ghi rõ trong spec), giá trị của từng booking nằm đúng cột của câu hỏi mình trả lời<br>- Booking cũ không bị đẩy giá trị sang cột của câu hỏi mới<br>- Nhánh dựng lại toàn bộ cho kết quả giống nhánh cập nhật |  | Lấp `A7` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh header + bảng đối chiếu giá trị từng booking · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-FRIEND001-03 | UI | FRIEND-001 | 質問項目 — câu trả lời rỗng | Abnormal | manual | Tất cả | Câu hỏi không trả lời ra ô trống, không ra chuỗi undefined | - Lịch có 3 câu hỏi, trong đó 2 câu không bắt buộc<br>- Sheet đã có booking cũ được tạo **trước** khi thêm câu hỏi thứ 3<br>- Job đang bật | 1. Tạo booking mới bỏ trống 2 câu không bắt buộc → chờ job → đọc 2 ô tương ứng<br>2. Thêm câu hỏi thứ 4 → cập nhật booking cũ (đổi trạng thái) → chờ job → đọc ô câu hỏi 4 của booking cũ<br>3. Xoá sạch tab để kích hoạt dựng lại toàn bộ → chờ job → đọc lại các ô rỗng<br>4. Kiểm cả dòng tiêu đề có ô nào ra chuỗi lạ không<br>5. Đối chiếu với bug #1075 (header từng ra 「undefined」) | 2 câu bỏ trống + 1 câu hỏi thêm sau | - Mọi ô không có câu trả lời đều là **ô trống thật**, không phải chuỗi 「undefined」/「null」/「-」<br>- Dòng tiêu đề hiện đúng tên câu hỏi, không ô nào ra chuỗi lạ<br>- Kết quả giống nhau ở cả nhánh cập nhật và nhánh dựng lại toàn bộ |  | Lấp `A8` · Đánh giá spec: Đã hỏi leader · Evidence: ảnh ô trống + ảnh header · regression bug #1075 · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-FRIEND001-07 | UI | FRIEND-001 | 質問項目 — sửa tiêu đề câu hỏi | Normal | manual | Tất cả | Đổi **tiêu đề** câu hỏi (không đổi loại): header trên sheet đổi theo, không sinh cột trùng và không mất dữ liệu cũ | - Lịch đã liên kết sheet thật, có 3 câu hỏi Q1/Q2/Q3 và ≥ 2 booking cũ đã lên sheet đủ 3 cột câu trả lời<br>- Job đang bật<br>- Thao tác trên màn 予約管理 > レッスン予約 > chi tiết lịch > tab 全体設定 > 「予約時のお客様への質問項目」 (SCR-LSN-16) | 1. Ghi lại header hiện tại và giá trị 3 cột câu hỏi của 2 booking cũ<br>2. Đổi tiêu đề Q2 sang tên mới (giữ nguyên loại câu hỏi và dữ liệu đã trả lời)<br>3. Tạo 1 booking mới trả lời đủ 3 câu hỏi → chờ job chạy → đọc header và dòng mới<br>4. Cập nhật 1 booking cũ (đổi trạng thái) → chờ job → đọc lại dòng đó<br>5. Xoá sạch tab để kích hoạt dựng lại toàn bộ → chờ job → đối chiếu header và toàn bộ dòng | Đổi tiêu đề 「電話番号」 → 「連絡先電話番号」 | - Bước 3: header hiển thị tiêu đề MỚI cho Q2, **không** sinh thêm cột thứ hai cho cùng câu hỏi; giá trị booking mới nằm đúng cột đó<br>- Bước 4: giá trị của booking cũ vẫn nằm đúng cột Q2, không bị đẩy sang cột khác và không bị xoá trắng<br>- Bước 5: nhánh dựng lại toàn bộ cho cùng kết quả — đúng 3 cột câu hỏi, không cột mồ côi mang tiêu đề cũ |  | Lấp `G25` · Đánh giá spec: Spec không ghi — đã hỏi leader · Evidence: ảnh header + giá trị 3 booking trước/sau · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-FRIEND001-08 | UI | FRIEND-001 | 質問項目 — câu hỏi chọn 1 lựa chọn | Normal | manual | Tất cả | Câu hỏi **chọn 1 lựa chọn** (có liên kết friend info): ghi đúng 1 giá trị và khớp hồ sơ người dùng LINE | - Lịch có 1 câu hỏi **chọn 1 lựa chọn** (đã liên kết friend info) và 1 câu hỏi **chọn nhiều** (không liên kết được) để đối chứng<br>- Khách là bạn LINE, job đang bật | 1. Khách đặt lịch, chọn 1 phương án ở câu hỏi chọn 1 và chọn 2 phương án ở câu hỏi chọn nhiều<br>2. Chờ job chạy rồi đọc 2 ô tương ứng trên sheet<br>3. Mở màn chi tiết người dùng LINE, kiểm giá trị friend info của câu hỏi chọn 1<br>4. Trên màn quản lý booking, đổi sang phương án khác ở câu hỏi chọn 1 → chờ job → đọc lại ô và hồ sơ<br>5. Thử bỏ chọn (để trống) câu hỏi chọn 1 nếu không bắt buộc → chờ job → đọc lại ô | Chọn 1: 「Aコース」→ đổi 「Bコース」 · chọn nhiều: 「A」「B」 | - Ô câu hỏi chọn 1 hiện **đúng 1 giá trị**, không kèm dấu phẩy; ô câu hỏi chọn nhiều hiện 「A,B」<br>- Giá trị friend info ở màn chi tiết người dùng LINE **khớp** ô trên sheet; câu hỏi chọn nhiều không tạo friend info<br>- Sau khi đổi phương án: cả ô trên sheet và hồ sơ đều đổi theo, không sinh dòng mới<br>- Bỏ chọn: ô về trống, không ra chuỗi lạ |  | Lấp `G27` · Đánh giá spec: Spec ghi rõ (FA-019 — chú thích 「複数選択の場合、友だち情報に回答を紐付けすることはできません」) · Evidence: ảnh 2 ô + ảnh màn chi tiết LINE user · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-FRIEND001-09 | UI | FRIEND-001 | 質問項目 — chế độ ghi hồ sơ bạn bè | Normal | manual | Tất cả | Ba chế độ 「友だち情報に回答を記録」 (なし · 自動生成 · 既存に記録) cho ra đúng giá trị trên sheet và trên hồ sơ | - Lịch đã liên kết sheet thật, job đang bật<br>- Tạo 3 câu hỏi cùng loại 短文回答, đặt 3 chế độ ghi hồ sơ khác nhau: なし · 自動生成 · 既存に記録 (trỏ vào 1 friend info sẵn có)<br>- Khách là bạn LINE, đã có giá trị cũ ở friend info được trỏ tới<br>- Thao tác trên màn 予約管理 > レッスン予約 > chi tiết lịch > tab 全体設定 > 「予約時のお客様への質問項目」 (SCR-LSN-16) | 1. Khách đặt lịch, trả lời cả 3 câu hỏi<br>2. Chờ job chạy rồi đọc 3 ô tương ứng trên sheet<br>3. Mở màn chi tiết người dùng LINE: kiểm friend info nào được tạo mới, friend info nào bị ghi đè, cái nào không đổi<br>4. Admin sửa câu trả lời của cả 3 câu hỏi trên màn quản lý booking → chờ job → đọc lại 3 ô và hồ sơ<br>5. Đổi chế độ của 1 câu hỏi (なし → 既存に記録) rồi cho booking mới → chờ job → quan sát | 3 câu hỏi × 3 chế độ ghi hồ sơ | - Cả 3 ô trên sheet đều có giá trị khách vừa trả lời, **không** ô nào trống chỉ vì không liên kết hồ sơ<br>- 「なし」: không tạo/không sửa friend info nào<br>- 「自動生成」: sinh friend info mới đúng tên câu hỏi<br>- 「既存に記録」: ghi đè đúng friend info được trỏ tới, giá trị trên sheet và trên hồ sơ khớp nhau<br>- Bước 4: cả 3 ô và hồ sơ cùng cập nhật, không sinh dòng mới<br>- Bước 5: đổi chế độ không làm hỏng dữ liệu booking cũ trên sheet |  | Lấp `G28` · Đánh giá spec: Spec ghi rõ (`link_friend_information` 1 なし / 2 自動生成 / 3 既存に記録 — db-mapping FA-019 §5.6) · Evidence: ảnh 3 ô + ảnh màn chi tiết LINE user · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| | | | | | | | **— 9 TC dưới đây từ đối chiếu kho form FA-011 + 2 nhóm Leader gửi 2026-09-18 (booking trước deploy · disconnect & liên kết lại) —** | | | | | | |
| TC-COMPATLEGACY001-03 | Data | COMPAT-LEGACY-001 | Googleスプレッドシート連携 — dữ liệu trước deploy | Normal | manual | Tất cả | Booking cũ do bản PHP ghi lên sheet trước deploy vẫn được job Java update đúng dòng | - Sheet đang có ≥ 5 dòng booking **do bản PHP cũ ghi trước khi deploy** (không phải job Java tạo)<br>- Đã deploy web + job bản mới, cờ job đang bật | 1. Ghi lại booking ID, số dòng và nội dung header của các dòng cũ<br>2. Đổi trạng thái 1 booking cũ trên màn quản lý → chờ job → đọc lại ô trạng thái của đúng dòng đó<br>3. Hoàn tiền 1 booking cũ khác → chờ job → đọc ô thanh toán<br>4. Sửa thông tin khách của 1 booking cũ → chờ job → đọc vùng cột friend info<br>5. Xoá 1 booking cũ → chờ job → kiểm dòng đó biến mất | 5 dòng booking do PHP ghi trước deploy | Cả 4 thao tác đều tác động **đúng dòng cũ**, không sinh dòng mới và không dựng lại toàn bộ sheet. Header giữ nguyên, các dòng cũ khác không đổi. Job tìm được dòng cũ theo booking ID dù dòng đó do PHP ghi |  | Lấp `G29` · Đánh giá spec: Đã hỏi leader · dẫn từ `TC-FORM-401` (form cũ `using_old_version=1` vẫn sync bằng job mới) · Evidence: ảnh sheet trước/sau 4 thao tác · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-INTGSHEET001-07 | UI | INTG-SHEET-001 | Googleスプレッドシート連携 — mất quyền từ phía Google | Abnormal | manual | Tất cả | User thu hồi quyền cấp cho LME ở phía Google: hệ thống hiện modal cảnh báo và có lối liên kết lại | - Lịch đang liên kết sheet thật, đã có booking lên sheet<br>- Truy cập được trang quản lý quyền ứng dụng của tài khoản Google đã cấp | 1. Vào trang quản lý tài khoản Google, **thu hồi quyền** đã cấp cho LME<br>2. Tạo 1 booking mới → chờ job chạy → kiểm bản ghi hàng đợi và bản ghi lỗi<br>3. Mở màn 全体設定 > 「Googleスプレッドシート連携」 của lịch → quan sát<br>4. Mở màn danh sách lịch → quan sát icon liên kết Google của lịch<br>5. Kiểm job có tiếp tục thử lại vô hạn không | 1 lịch bị thu hồi quyền | Hệ thống nhận ra mất liên kết: màn liên kết hiện **modal/cảnh báo đã mất liên kết** kèm lối liên kết lại; lịch được đánh dấu lỗi liên kết. Job **dừng thử lại** thay vì lặp vô hạn, bản ghi hàng đợi không kẹt. Booking mới vẫn tạo được bình thường trên LME |  | Lấp `G30` · Đánh giá spec: Đã hỏi leader — kho form MT-15 đã chốt “phải có modal cảnh báo”, dự kiến FAIL thì raise bug · dẫn từ `TC-FORM-369` · Evidence: ảnh modal + trạng thái bản ghi lỗi · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-INTGSHEET001-08 | UI | INTG-SHEET-001 | Googleスプレッドシート連携 — liên kết lại | Normal | manual | Tất cả | Liên kết lại **cùng tài khoản Google cũ**: sync tiếp vào đúng sheet đã có, không tạo file mới | - Tiếp nối TC-INTGSHEET001-07: lịch đang ở trạng thái mất liên kết, sheet cũ vẫn còn dữ liệu<br>- Có 2 booking phát sinh trong lúc mất liên kết | 1. Ghi lại link file sheet cũ, số dòng và booking ID có trên đó<br>2. Bấm liên kết lại, chọn **đúng tài khoản Google cũ**, cấp đủ quyền<br>3. Quan sát màn liên kết sau khi liên kết lại (link file, email hiển thị)<br>4. Chờ job chạy → mở file sheet cũ, đếm số dòng<br>5. Tạo thêm 1 booking mới → chờ job → kiểm dòng mới nằm ở file nào | 2 booking phát sinh khi mất liên kết + 1 booking sau khi liên kết lại | Liên kết lại thành công, màn hiển thị **đúng file cũ** (không sinh file mới). 2 booking phát sinh lúc mất liên kết được ghi bù vào sheet cũ, không trùng dòng. Booking mới ghi tiếp vào chính sheet đó |  | Lấp `G31` · Đánh giá spec: Đã hỏi leader · dẫn từ `TC-FORM-370` (form: cùng email → giữ file cũ) · Evidence: link file trước/sau + ảnh sheet · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-INTGSHEET001-09 | UI | INTG-SHEET-001 | Googleスプレッドシート連携 — liên kết lại | Abnormal | manual | Tất cả | Liên kết lại bằng **tài khoản Google KHÁC**: tạo file mới, booking mới kích hoạt dựng lại toàn bộ lên file đó | - Lịch đang mất liên kết (hoặc vừa gỡ liên kết), sheet cũ của tài khoản A còn dữ liệu<br>- Có tài khoản Google B khác để liên kết | 1. Ghi lại link file sheet của tài khoản A và số dòng hiện có<br>2. Liên kết lại bằng **tài khoản B**, cấp đủ quyền<br>3. Quan sát màn liên kết: email hiển thị và link file<br>4. Tạo 1 booking mới → chờ job → mở file mới kiểm nội dung<br>5. Mở lại file cũ của tài khoản A kiểm có bị chạm không<br>6. Tạo tiếp 1 booking nữa → chờ job → kiểm job chỉ **append 1 dòng**, không dựng lại toàn bộ lần nữa | 2 tài khoản Google khác nhau | Hệ thống **tạo file/sheet mới** trên tài khoản B; màn liên kết hiển thị email B và link file mới<br>- Ngay sau khi liên kết lại, file B **còn trống** — không tự sync bù<br>- Khi có **booking mới** phát sinh: job thấy file B trống ⇒ **dựng lại TOÀN BỘ booking của lịch (cả booking cũ + booking mới)** lên file B, kèm dòng tiêu đề<br>- Mỗi booking đúng 1 dòng, không trùng, không thiếu; booking 「通知受取希望」 vẫn bị loại<br>- File cũ của tài khoản A **không bị chạm và không bị xoá**<br>- Lần sau job chỉ **append 1 dòng** cho booking mới, KHÔNG dựng lại toàn bộ lần nữa |  | Lấp `G31` + `G37` (gộp `TC-INTGSHEET001-12` vào đây — cùng tiền đề *file mới còn trống*, cùng oracle *dựng lại toàn bộ*) · Đánh giá spec: Spec ghi rõ — kho lesson `TC-LSN-461` bước 1-2: vừa liên kết thì sheet chưa có booking cũ, **sau khi có booking mới thì sheet hiển thị TOÀN BỘ booking (cũ + mới)**; Leader xác nhận 2026-09-18 · Evidence: link 2 file + ảnh file B trước/sau booking mới · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-INTGSHEET001-10 | UI | INTG-SHEET-001 | Googleスプレッドシート連携 — xoá file Google | Abnormal | manual | Tất cả | Xoá cả **file** Google (không phải tab): job báo lỗi có kiểm soát, không tự tạo lại file | - Lịch đã liên kết sheet thật, đã có booking trên sheet<br>- Job đang bật | 1. Xoá cả file Google Spreadsheet (đưa vào thùng rác Drive)<br>2. Tạo 1 booking mới → chờ job chạy<br>3. Kiểm trạng thái bản ghi hàng đợi và bản ghi lỗi<br>4. Mở màn liên kết của lịch quan sát trạng thái hiển thị<br>5. Khôi phục file từ thùng rác → chờ job → kiểm booking mới có được ghi bù không | 1 file bị xoá rồi khôi phục | Job **không tự tạo file mới**; bản ghi vào nhánh lỗi có thử lại, không báo đồng bộ thành công giả. Màn liên kết phản ánh được tình trạng lỗi. Sau khi khôi phục file, booking phát sinh trong lúc mất file được ghi bù đủ, không trùng dòng |  | Lấp `G32` · Đánh giá spec: Đã hỏi leader · dẫn từ `TC-FORM-379` · Evidence: trạng thái bản ghi lỗi + ảnh sheet sau khôi phục · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-INTGSHEET001-11 | UI | INTG-SHEET-001 | Googleスプレッドシート連携 — ô cột A trống | Boundary | manual | Tất cả | Sheet có ô cột A trống ở giữa/cuối vùng dữ liệu: dòng mới ghi xuống dưới cùng, KHÔNG đè dòng có sẵn | - Sheet có 5 dòng booking, job đang bật | 1. Xoá trắng ô cột A (mã booking) của dòng thứ 3, giữ nguyên các ô còn lại<br>2. Tạo 1 booking mới → chờ job → kiểm dòng mới nằm ở dòng số mấy<br>3. Kiểm dòng 3, 4, 5 còn nguyên nội dung không<br>4. Lặp lại với trường hợp ô cột A trống ở **dòng cuối**<br>5. Kiểm tổng số dòng dữ liệu sau mỗi lần | Sheet 5 dòng, ô cột A trống ở dòng 3 và ở dòng cuối | Dòng mới ghi vào **dòng thứ 6** (ngay sau vùng dữ liệu hiện có), tuyệt đối không đè lên dòng 3/4/5. Nội dung các dòng cũ giữ nguyên 100%. Tổng số dòng tăng đúng 1 sau mỗi booking mới |  | Lấp `G33` · Đánh giá spec: Spec ghi rõ — kho form MT-16 đã chốt expected cứng “không được ghi đè” · dẫn từ `TC-FORM-394` · Evidence: ảnh sheet trước/sau + số dòng · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-PERM003-01 | UI | PERM-003 | Googleスプレッドシート連携 — nhiều bot | Normal | manual | Tất cả | Một tài khoản Google dùng cho nhiều bot, và 2 tài khoản cho 2 bot: dữ liệu không lẫn sang nhau | - Bot A và bot B đều có lịch bài học đã bật<br>- Tài khoản Google G1 liên kết cho lịch của cả A và B; sau đó bot B đổi sang tài khoản G2<br>- Job đang bật, dùng **2 phiên đăng nhập riêng** cho 2 bot | 1. Liên kết G1 cho lịch của bot A và lịch của bot B<br>2. Tạo booking ở cả 2 bot → chờ job → mở 2 file kiểm nội dung<br>3. Ở bot B, đổi liên kết sang tài khoản G2<br>4. Tạo booking mới ở cả 2 bot → chờ job → kiểm lại<br>5. Đối chiếu không có booking của bot này nằm trong file của bot kia | 2 bot × 2 tài khoản Google | Mỗi lịch ghi vào đúng file của mình; booking của bot A **không bao giờ** xuất hiện trong file của bot B và ngược lại. Sau khi bot B đổi tài khoản, bot A vẫn ghi bình thường vào file cũ |  | Lấp `G36` · Đánh giá spec: Spec không ghi — đã hỏi leader · dẫn từ `TC-FORM-364` · Evidence: ảnh 2-3 file + booking ID từng bot · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-DATAREF001-01 | UI | DATA-REF-001 | Job đồng bộ — xoá booking khi hàng đợi còn tồn đọng | Normal | manual | Tất cả | Booking bị xoá lúc hàng đợi còn bản ghi cập nhật: bản ghi xoá đến lượt sẽ dọn đúng dòng đó | - Lịch đã liên kết sheet thật, sheet có ≥ 3 booking<br>- **Tạm dừng job** để hàng đợi tồn đọng<br>- Booking X đã có dòng trên sheet | 1. Khi job đang dừng: đổi trạng thái booking X, rồi sửa thông tin khách của X (sinh 2 bản ghi cập nhật)<br>2. Vẫn để job dừng, **xoá booking X** trên màn quản lý<br>3. Đọc hàng đợi: phải có đủ 2 bản ghi cập nhật **+ 1 bản ghi xoá**, ghi lại thứ tự id<br>4. Bật job, chờ xử lý hết<br>5. Đọc sheet: dòng của X còn không, số dòng, các booking đối chứng<br>6. Đọc trạng thái kết thúc của cả 3 bản ghi hàng đợi | 1 booking X: 2 cập nhật + 1 xoá dồn trong hàng đợi | Hàng đợi có **đủ 3 bản ghi**, bản ghi xoá nằm sau 2 bản ghi cập nhật. Sau khi job chạy hết: **dòng của booking X không còn trên sheet**, số dòng giảm đúng 1, booking đối chứng và header không đổi. Cả 3 bản ghi kết thúc có trạng thái rõ ràng (2 cập nhật xử lý xong hoặc đánh dấu hợp lệ, bản ghi xoá = đã đồng bộ), không bản ghi nào kẹt, không lặp lỗi vô hạn |  | Lấp `G4` (nhánh xoá khi còn tồn đọng) · Đánh giá spec: Spec ghi rõ — **Leader chốt 2026-09-18**: mọi thao tác xoá đều phải sinh bản ghi xoá trong hàng đợi; bản ghi cập nhật tồn đọng cứ xử lý, bản ghi xoá đến lượt sẽ dọn dòng ⇒ expected của `NEW-6` (“sheet giữ nguyên, dòng vẫn còn”) **sai chuẩn** (§4 I12) · Evidence: ảnh hàng đợi 3 bản ghi + ảnh sheet trước/sau · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |
| TC-DATAREF001-02 | UI | DATA-REF-001 | Xoá khoá học / xoá lịch — cascade booking | Abnormal | manual | Tất cả | Xoá khoá học (và xoá lịch) kéo theo xoá booking: mọi booking bị xoá đều sinh bản ghi xoá và mất khỏi sheet | - Lịch đã liên kết sheet thật<br>- Khoá học C1 có 3 booking đã lên sheet; khoá học C2 có 2 booking làm đối chứng<br>- Job đang bật | 1. Ghi lại booking ID của 3 booking thuộc C1 và 2 booking thuộc C2, cùng số dòng trên sheet<br>2. Xoá khoá học C1 trên màn quản lý<br>3. Đọc hàng đợi: có đủ **3 bản ghi xoá**, mỗi bản ghi trỏ đúng 1 booking của C1 không<br>4. Chờ job chạy → đọc sheet, đếm số dòng và đối chiếu booking ID còn lại<br>5. Lặp kịch bản với **xoá cả lịch bài học** (kiểm hàng đợi còn được sinh không khi bản ghi lịch đã biến mất) | 3 booking của C1 + 2 booking đối chứng của C2 | Bước 3: hàng đợi có đúng 3 bản ghi xoá cho 3 booking của C1. Bước 4: 3 dòng đó biến mất khỏi sheet, 2 booking của C2 và header giữ nguyên, số dòng giảm đúng 3. Bước 5: ghi nhận rõ khi xoá cả lịch thì hàng đợi có sinh bản ghi xoá không và job xử lý ra sao — **không được để dòng mồ côi** trên file của khách |  | Lấp `G4` (đường xoá cascade) · Đánh giá spec: Đã hỏi leader — spec FA-019 ghi xoá lịch dùng `forceDelete` cả booking đã soft-delete (A-14) · Evidence: ảnh hàng đợi + ảnh sheet trước/sau · RULE-08 → kết luận cuối bắt buộc có lượt chạy trên **production** |

**TC đã loại theo yêu cầu Leader 2026-09-18** (lý do: khó dựng môi trường / khó test). Ghi lại để lần review sau không đề xuất lại, và để thấy rõ phần rủi ro đang chấp nhận:

| TC đã loại | Lấp GAP nào | Sau khi loại |
|---|---|---|
| `TC-ENV003-01` | `G1` `Q1` | Còn `TC-ENV003-02` (chặn Google từ web) |
| `TC-JOB001-02` | `G2` `Q5` | Còn `TC-JOB001-01` (chuỗi backoff) |
| `TC-DATAMIG001-01` | `G3` | ⚠️ **`G3` không còn TC nào** — bug #1072 (High, open: migration thiếu cột `error_message` ⇒ job chết mỗi vòng quét khi bảng có dữ liệu) không có TC verify. Bù bằng cách: yêu cầu Dev tự chạy migration trên bản sao dữ liệu thật trước khi release |
| `TC-DEPLOYLIVE001-02` | `G5` `Q3` | Còn `TC-DEPLOYLIVE001-01` (cửa sổ web mới + job chưa bật) |
| `TC-COMPATLEGACY001-01` | `G5` | Còn `TC-DEPLOYLIVE001-01`; riêng nhánh **rút cạn bản ghi lỗi lesson đời cũ** không còn TC — cần kế hoạch vận hành thủ công (`NEW-93` đã ghi nhận “từ nay không còn ai thử lại”) |
| `TC-COMPATLEGACY001-02` | `Q6` | ⚠️ **`Q6 COMPAT-LEGACY-001` không còn TC nào** — endpoint đổi trạng thái đời cũ của app bản cũ (`NEW-96` đang skip) hoàn toàn không được phủ. Chấp nhận được **nếu** vận hành xác nhận không còn app bản cũ nào đang dùng |
| `TC-PERFLARGE001-01` | `G34` | ⚠️ **`G34` không còn TC nào** — giới hạn 1.000 bản ghi/vòng quét không được verify |
| `TC-ENV002-01` | `G35` | ⚠️ **`G35` không còn TC nào** — thiếu config số luồng thì job chạy với giá trị mặc định nào, không được verify |
| `TC-ENV001-01` | `G14` | ⚠️ **`G14` không còn TC nào** — nhánh ghi hàng đợi thất bại (booking vẫn tạo được nhưng **mất im lặng 1 dòng sheet**) không được verify; `NEW-87` cũng đang skip. Bù bằng cách: yêu cầu Dev xác nhận có log kèm mã booking để truy vết và có cách bù dữ liệu |
| `TC-INTGSHEET001-06` | `A9` | ⚠️ **`A9` không còn TC nào** — file có vùng bảo vệ / merge cell / data validation / conditional format, và file đặt locale khác (EN) đều không được verify. Rủi ro: vùng bảo vệ khiến job ghi hỏng mà vẫn báo thành công. Bù tối thiểu: khi hướng dẫn khách, ghi rõ không đặt vùng bảo vệ lên vùng dữ liệu của tab đích |
| `TC-INTGSHEET001-12` | `G37` | **Gộp** vào `TC-INTGSHEET001-09` (DUP-SUBSET: cùng tiền đề *file mới còn trống*, cùng oracle *booking mới ⇒ dựng lại toàn bộ*) — `G37` vẫn được phủ |
| `TC-PAYSTATE001-05` | `G26` | **Trùng `NEW-56` / `NEW-97`** (cùng `PAY-STATE-001`, cùng thao tác hoàn tiền web/app). Không đẻ TC mới → chuyển thành issue: chạy lại `NEW-56`/`NEW-97` trên **cổng thật (Stripe + UnivaPay)** và bổ sung expected cho cột `決済金額` sau hoàn tiền |
| `TC-DATAID001-01` | `G7` | **Trùng `NEW-20`** (cùng trạng thái *dòng không tồn tại trên sheet*), chỉ khác expected. Theo quyết định Leader → không đẻ TC mới, **sửa expected `NEW-20`** trên Studio (§4 I11) |

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | `spec-features/admin/lesson-booking/feature-spec.md` — mục Google Sheets (BR-P35 / §Đồng bộ) | ✅ **ĐÃ CHỐT 2026-09-18 (Leader)**: xoá booking ⇒ **phải xoá dòng khỏi sheet**; xoá friend ⇒ xoá dữ liệu booking liên quan và xoá luôn các dòng đó. Việc còn lại: (a) ghi hành vi này vào spec FA-019; (b) sửa kho `TC-LSN-463` luồng 13 (expected cũ "dòng vẫn còn, hiển thị 「キャンセル（手動）」" nay **sai chuẩn**); (c) xác nhận job Java thật sự xoá dòng — xem §4 round 2 mục I14 | `NEW-58` / `NEW-89` / `NEW-107` / `NEW-109` (xoá dòng — **khớp quyết định**) vs kho `TC-LSN-463` luồng 13 (giữ dòng — lỗi thời) | `Đã chốt — Leader` |
| 2 | Tài liệu hợp đồng `ai_docs/task_40194_40904/calendar-sync-googles-plan.md` + **REQ-018** | ✅ **ĐÃ CHỐT 2026-09-18 (Leader)**: job ghi theo **liên kết hiện hành lúc chạy**, KHÔNG dùng `spreadsheet_id` snapshot. Còn lại: (a) sửa REQ-018 + docblock `buildPayload` (mục 6); (b) **mở lại bug #1083** — bản fix đã ép ghi vào sheet A theo snapshot, đi ngược quyết định này (§4 I10); (c) sửa expected `NEW-90` trên Studio | `NEW-49` (đúng chuẩn) vs `NEW-90` + bug #1083 (sai chuẩn) | `Đã chốt — Leader` |
| 3 | `feature-spec.md` FA-019 — cột `ステータス` (RA-12) | **Trạng thái 3 「通知受取希望」**: nhánh đổi trạng thái ghi **chuỗi rỗng** (xoá mất nhãn cũ) trong khi nhánh dựng lại toàn bộ **loại hẳn** booking trạng thái 3 khỏi sheet — 2 hành vi không nhất quán trên cùng 1 file | `NEW-10` vs `NEW-24` (cả 2 pass) + spec RA-12 | `Leader + PM` |
| 4 | `calendar-sync-googles-plan.md` §2.2 + `feature-spec.md` FA-019 | ✅ **ĐÃ CHỐT 2026-09-18 (Leader)** cho cả 2 tình huống: **(a) dòng bị người dùng xoá thủ công trên sheet, booking vẫn còn** ⇒ job đánh dấu *không tìm thấy dòng*, KHÔNG chèn bù. **(b) booking bị xoá khỏi hệ thống khi hàng đợi còn bản ghi cập nhật** ⇒ **mọi thao tác xoá đều phải sinh bản ghi xoá trong hàng đợi**; bản ghi cập nhật tồn đọng cứ xử lý bình thường, bản ghi xoá đến lượt sẽ dọn dòng ⇒ kết cục cuối là **dòng bị xoá khỏi sheet**. Việc còn lại: (i) Dev xác nhận **đủ 100% đường xoá** đều enqueue — đặc biệt `deleteOrderError()` (callback lỗi) và cascade xoá khoá học/xoá lịch; (ii) sửa expected `NEW-6` trên Studio (§4 I12) | `NEW-20` + `TC-DATAID001-01` (a) · `NEW-6` + `TC-DATAREF001-01` + `TC-DATAREF001-02` (b) | `Đã chốt — Leader` |
| 5 | `feature-spec.md` FA-019 — BR-55 / Q-16 | `cancelGoogsheet()` xoá token nhưng **không reset `google_sheet_status`** — nay job Java đọc/ghi chính cột này, phải chốt trạng thái sau khi gỡ liên kết | Spec BR-55 + kho FA-019 MT-42 vs `NEW-61` (không kiểm cột này) | `Dev + Leader` |
| 6 | Docblock `buildPayload` (web) vs `HandleCalendarSyncGoogleSheetTask` (job) | Docblock yêu cầu job **ghi thẳng giá trị trong ảnh chụp** (ví dụ số tiền đã định dạng 「5,000」), thực tế job truy vấn lại CSDL rồi tự định dạng → phải sửa docblock hoặc sửa job | `NEW-83` (skip) + Studio `dev_impact` điểm lệch hợp đồng (1) | `Dev` |
| 11 | `feature-spec.md` FA-019 — nhánh “sheet rỗng ⇒ dựng lại toàn bộ” | ✅ **ĐÃ CHỐT 2026-09-18 (Leader)**: nhánh này áp dụng cho **mọi** trường hợp file/sheet trống, kể cả **file mới sinh ra do đổi tài khoản Google** — khi có booking mới, job dựng lại toàn bộ booking cũ + mới lên file đó. “Không sync bù” chỉ có nghĩa là **không tự sync ngay lúc liên kết lại**, không phải là loại bỏ booking cũ vĩnh viễn. Việc còn lại: ghi hành vi này vào spec FA-019 để khỏi hiểu nhầm giữa 2 nhánh | Kho lesson `TC-LSN-461` (bước 1-2) + `NEW-49` + `TC-INTGSHEET001-09` / `-12` | `Đã chốt — Leader` |
