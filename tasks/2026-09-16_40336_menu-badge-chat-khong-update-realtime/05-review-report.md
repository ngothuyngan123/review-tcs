# 05 — Review Report

## 0. Nguồn TC

- **Nguồn đã dùng**: MCP LME TEST STUDIO — task `#306` (ticket 40336, round 1, branch `ai_fixbug_40336`), `testcase_list`.
- **Tổng số TC review**: **23**.

---

## 1. Coverage — `dev-impact` + `diff code`

**Kết luận**: 8/13 vùng ảnh hưởng đủ TC · **3 GAP** · **2 RISK** · 0 orphan.

> Cập nhật 2026-09-17 theo **đánh giá ảnh hưởng phía test** của Leader → thêm **G6** (badge 「送信エラー」), vùng mà cả 23 TC Studio lẫn vòng review đầu đều bỏ sót.
> Leader đã đính chính: **sidebar chỉ có badge ở menu 「1:1チャット」, menu 「チャット管理」 KHÔNG có badge** → vùng "badge menu talk list" đã được gỡ khỏi báo cáo.

| Mã | Chiều | Vùng ảnh hưởng | TC hiện có | Status | Vấn đề |
|---|---|---|---|---|---|
| G1 | `diff code` + `dev-impact` (F4 · T2 FA-001) | `public/js/chats/chat-v2.js` — hàm `setBadge` bị **xoá hẳn**, badge màn chat 1:1 nay do helper dùng chung ghi. Ngoài 4 chỗ gọi Dev kê, màn chat 1:1 còn **4 đường khác làm đổi số hội thoại chưa xác nhận**, đều kỳ vọng badge đổi: `全て確認済みに変更` (bulk confirm, spec FA-001 + kho `TC-CHT-34`/`TC-CHT-35` expected "badge menu 1:1チャット về 0") · `非表示` ẩn bạn bè (BR-05 auto-xác nhận, kho `TC-CHT-405`) · đổi `対応ステータス` ở header (EP-13 `/basic/update-status-confirm`, response có `countUserUnConfirm`) · xoá bạn bè (kho `TC-CHT-402`, `bots.count_user_unconfirm` giảm 1) | **0 TC** | **GAP** `[BLOCKER]` | Đây là đường đổi số **lớn nhất** (bulk confirm có thể đưa badge về 0). NEW-16/17/18 chỉ phủ 4 chỗ gọi Dev kê (confirm 1 hội thoại · get-badge sau filter · socket 2 nhánh). Câu hỏi adversarial #3: Dev **có** danh sách nơi ảnh hưởng nhưng danh sách chỉ liệt kê nơi *gọi hàm cũ*, không liệt kê nơi *làm đổi số* — 4 đường trên không có TC nào chạm. |
| G2 | `diff code` (F2) | `app/Helpers/functions.php` — diff **+7/−1** nhưng `dev_impact` Studio **chỉ mô tả `countBadge`**. `totalUserConfirmMessage` (cùng file, Dev kê ở mục 3) là nguồn số cho **FCM badge mobile** (BR-07) và cho payload socket của màn chat 1:1 | **0 TC** cho 2 kênh này | **RISK** `[MAJOR]` | Không TC nào xác nhận `totalUserConfirmMessage` **không** bị đổi cùng lượt với `countBadge`. **Input thiếu**: diff chi tiết của `functions.php` (Studio chỉ trả `diffStat`, không trả hunk) → §6 #4. ℹ️ Phần `InfoEvent` của vùng này **đã đóng**: Leader chốt badge không đồng bộ cross-tab, `InfoEvent` không phải đường cập nhật badge web (§6 #1) — `TC-REGSHARED001-06` giữ lại để chốt rằng tab không thao tác **không hiển thị sai** |
| G3 | `diff code` (F6) | `resources/views/layout/v2/basic/sidebar.blade.php` — `sidebar-badge.js` nạp ở **layout dùng chung**, tức **mọi màn có sidebar** (broadcast, tag, friend list, richmenu…) đều nạp thêm script mới | NEW-21 (chỉ 2 màn: talk-list + chat 1:1) | **RISK** `[MAJOR]` | Script lỗi/404 sẽ ảnh hưởng **toàn tool**, không chỉ 2 màn chat. Dev tự nêu rủi ro "nếu màn không render layout sidebar này thì `setChatMenuBadge` undefined" nhưng chỉ kiểm 2 màn. |
| G4 | `dev-impact` (F1 · BUG) | `TalkListController::changeStatusMessage` — các side effect còn lại của chính hàm bị sửa: `sync_elasticsearch` (BR-06) · FCM `updateBadge()` (BR-07) | **0 TC** | **GAP** `[MINOR]` — *đã cân nhắc loại* | Diff `TalkListController.php` chỉ **+4/−1** (thêm field vào response), không chạm nhánh ES/FCM → theo nguyên tắc chỉ test layer bị chạm code, **không đề xuất TC** cho 2 nhánh này. Ghi ở đây để Leader biết đã xét — xem dòng giải thích ở §5. |
| G6 | `diff code` (F4 · F6) + đánh giá phía test | **Badge 「送信エラー」 của màn error message (FA-028)** — spec ghi rõ badge này **do layout chung dựng** (`error-message/feature-spec.md:38, :61`) tức cùng `sidebar.blade.php` vừa bị sửa, và dùng **cùng quy ước `99+` khi ≥ 100** với helper mới. Kênh `InfoEvent` phát kèm `totalErrorMessage` (BR-07) | **0 TC** | **GAP** `[MAJOR]` | `setBadge()` cũ trong `chat-v2.js` bị **xoá hẳn**; nếu hàm cũ từng ghi cả badge lỗi thì badge 「送信エラー」 mất đường cập nhật. Ngược lại, helper mới toggle lớp ẩn/chấm đỏ trên mục menu cha — cần chắc **không ghi đè nhầm** badge lỗi. Leader đã yêu cầu rõ: *regression số badge màn error message không đổi logic* |

---

## 2. Thiếu so với quan điểm test

**Kết luận**: 10/17 quan điểm Trigger-khớp đã cover đủ · **7 quan điểm thiếu** (2 `[BLOCKER]`, 5 `[MAJOR]`).

| Mã | Quan điểm | Ưu tiên | Trigger khớp vì | TC hiện có | Thiếu gì |
|---|---|---|---|---|---|
| Q1 | `DEPLOY-LIVE-001` | **Cao** | Release **không bật maintain** + **đổi payload API** (`/ajax/change-status-message` thêm `count_user_unconfirm`) + đổi JS — đúng nguyên văn trigger | **0 TC** | `[BLOCKER]` Không TC nào chạy kịch bản **client cũ gọi server mới**: mở sẵn màn talk-list **trước** release → release → **không reload** → bấm 「変更」. Đây là kịch bản thật của mọi user đang mở tool lúc deploy. NEW-22 là chiều ngược lại (server trả thiếu field), NEW-21 chỉ kiểm asset version. |
| Q2 | `REG-SHARED-001` | **Cao** | Fix **gộp 2 bản logic thành helper dùng chung** — dấu hiệu "dùng chung / shared" nguyên văn | #18139, NEW-16, NEW-17 (đều **Normal**) | `[BLOCKER]` Trùng G1: 4 đường đổi badge ở chat 1:1 chưa được test. Thêm `[MAJOR] RULE-01`: quan điểm Cao nhưng **chỉ có Normal**, thiếu cả Abnormal lẫn Boundary, không ghi lý do. Evidence bắt buộc của quan điểm này là "danh sách nơi ảnh hưởng (dev cung cấp) + kết quả test **từng nơi**" — hiện chưa có bảng đối chiếu từng nơi. |
| Q3 | `DEPLOY-ASSET-001` ★ | **Cao** | Release **sửa file JS** (3 file JS + 1 file JS mới) | NEW-21 | `[MAJOR]` NEW-21 chỉ làm bước (4) của quan điểm (Network trả 200 + có query version). **Thiếu bước (1)-(3)**: mở màn bằng bản cũ để browser cache asset → release bản mới → **chỉ F5 thường (KHÔNG Ctrl+F5)** → chạy lại luồng. Đây mới là chiều bắt được lỗi cache thật. |
| Q4 | `BULK-001` | **Cao** | Tổ hợp **bộ lọc + thao tác hàng loạt** ở cả 2 màn | NEW-3, #18139 (chỉ màn talk-list) | `[MAJOR]` Nhánh bulk ở **màn chat 1:1** (`全て確認済みに変更`, kho `TC-CHT-34`→`TC-CHT-39`, có cả biến thể theo filter/search) hoàn toàn không có TC — trùng G1. |
| Q5 | `CONC-001` | **Cao** | Nút thực thi hành động quan trọng + **nhiều người cùng sửa** | NEW-6 (**Abnormal**, nhấp đúp 1 người) | `[MAJOR] RULE-01` Thiếu Normal + Boundary, và thiếu chiều **2 người dùng đồng thời**: 2 tài khoản admin cùng đổi trạng thái trên cùng bot → badge cuối cùng phải là số thật, không phải số của người bấm sau. |
| Q6 | `DATA-COUNT-001` | **Cao** | Màn có con số đếm — quan điểm **lỗi lặp nhiều nhất lịch sử bug** | NEW-4 (Normal), NEW-25 (Boundary) | `[MAJOR]` Thiếu **Abnormal** (RULE-01) và thiếu chiều "cùng 1 số liệu khớp giữa nhiều nguồn": mọi TC chỉ đối chiếu badge ↔ `bots.count_user_unconfirm`. Chưa TC nào đối chiếu badge ↔ **số dòng của filter 未確認 ở màn chat 1:1** ↔ **tab 未確認のみ ở talk-list**. Kho đã đánh dấu `MT-19` (spec tự mâu thuẫn `confirm_count = 1` vs `> 0`) đúng vùng này. |
| Q7 | `FUNC-004` | **Cao** | Chức năng có **giới hạn số lượng hiển thị** (mốc 99 / `99+`) — quan điểm mà bộ TC hiện dùng nhiều nhất | NEW-9, NEW-10, NEW-11, NEW-20, NEW-24 | `[MAJOR]` Badge 「1:1チャット」 xuất hiện ở **2 vị trí** (menu thường · mục お気に入り) và cần đủ **4 mốc** 0 / 1 / 99 / >99 — hiện chỉ **vị trí menu thường** được phủ đủ 4 mốc, và phủ bằng thao tác ở **màn talk-list** (NEW-9/10/11). Thiếu: **NEW-20** (thao tác tại màn chat 1:1) chỉ chạy 120/119 — **không chạm mốc 0 và mốc 99 đúng biên**; **NEW-24** (お気に入り) chỉ có 4→3 và 0→1 — **thiếu mốc 99 và >99** |

**Quan điểm Trigger khớp nhưng ĐÃ LOẠI khỏi phạm vi** (ghi để Leader không phải hỏi lại):

- `SYNC-APP-001` (badge trên app mobile admin) — trigger khớp ("tính năng có mặt trên cả web và app", spec BR-07 có FCM `updateBadge()`, kho `TC-CHT-398` expected "hiển thị ngay ở web và app"). **Nhưng**: `updateBadge()`/FCM không nằm trong 6 file của diff, và cả Dev lẫn `dev_impact` Studio đều xác nhận `countBadge` **chỉ có 2 caller, cả hai đều là đường web** (`ChatController` socket + `/ajax/get-badge`). Không có bằng chứng đường FCM bị chạm → **không đề xuất TC**. Nếu Leader muốn chắc, việc cần làm là hỏi Dev "`updateBadge()` lấy số từ `totalUserConfirmMessage` hay từ `countBadge`", không phải viết TC.
- `ENV-003` / RULE-08 — task **không** chạm media · domain · job nền · loadbalance · bill tiền → không bắt buộc chạy production. (Vấn đề env của bộ TC này là "chưa chạy ở đâu cả", đã ghi ở §4.)
- `COMPAT-LEGACY-001` — có đối tượng legacy trong vùng (`messages.is_confirmed`, message sharding theo năm, bot cũ/bot mới — kho `TC-CHT-16`) nhưng diff không chạm nhánh legacy nào → không đề xuất TC.

---

## 3. TC trùng lặp

**Đã rà toàn bộ 23 TC theo 4 yếu tố (mã quan điểm × loại case × đối tượng+thao tác × tiền đề tương đương) — không phát hiện `DUP-EXACT` / `DUP-SUBSET` / `DUP-INFLATE` / `DUP-CONFLICT`.**

2 cặp **chồng lấn** đã xét kỹ và kết luận **KHÔNG phải trùng** (ghi lại để vòng review sau không phải rà lại):

| Cặp | Vì sao trông giống | Vì sao KHÔNG tính là trùng | Xử lý |
|---|---|---|---|
| NEW-25 vs NEW-10 + NEW-11 | Cả 3 đều xoay quanh badge = 0 → ẩn badge + chấm đỏ, rồi >0 → hiện lại | **Tiền đề khác nhau về chất**: NEW-10/11 đạt 0 **trong phiên** (phần tử badge đã tồn tại trong DOM vì trang load lúc count=1) — helper chỉ việc ẩn/hiện. NEW-25 **load trang khi count đã = 0**, lúc đó server có thể **không render phần tử badge**, helper không có gì để ghi → đây là nhánh lỗi riêng, đúng rủi ro (2) mà NEW-24 nêu cho menu お気に入り | Giữ cả 3 |
| NEW-3 vs #18139 (kho `CM-017`) | Cùng màn talk-list, cùng đổi sang 「確認済」 hàng loạt, cùng kiểm badge giảm đúng | Khác **nhánh phạm vi chọn**: NEW-3 chọn **toàn bộ kết quả lọc** (`type_action='all'`), #18139 tick chọn **toàn bộ trang** ở tab 「一覧」; #18139 còn phủ system event `msg_kind` {12…21} bị loại khỏi phạm vi + verify DB | Giữ cả 2 |

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | Toàn bộ 23 TC | **0 TC đã chạy** (`exec`: pass 0 / fail 0 / untested 23 · mọi TC `last_exec = null`, `status = draft`). Tỷ lệ pass = 0% < 50% | Coverage ở §1/§2 chỉ là coverage **trên giấy**. Không kết luận "fix đã được verify" cho tới khi có run thật. Chạy trước ít nhất bộ phủ REQ-001/002/003 (NEW-1, NEW-2, NEW-3, NEW-9, NEW-10, NEW-12) |
| 2 | `[MAJOR]` | Toàn bộ 23 TC | `Evidence` rỗng toàn bộ (hệ quả của #1) — RULE-02 | Khi chạy, bắt buộc kèm đúng loại evidence: video/GIF badge đổi **không reload** (REQ-001), screenshot DevTools Network + JSON response (REQ-002), screenshot sidebar thu gọn (REQ-003) |
| 3 | `[MAJOR]` | NEW-1, NEW-3, NEW-18, NEW-12, NEW-13, NEW-14, NEW-22 (**7 TC**) | Mang **mã quan điểm không có trong `framework/checklist-lme.md`**: `TOOL-KNOW-002`, `SELECT-SCOPE-001`, `RULE-TOOL-029`, `API-CONTRACT-001` (3 TC), `TOOL-ERRHYG-001` → **không tính là cover** khi map quan điểm. Nặng nhất: **NEW-1 là TC tái hiện đúng bug gốc** mà lại mang mã lạ | Map lại sang mã chuẩn: NEW-1 → `FUNC-001`/`DATA-COUNT-001` · NEW-3 → `BULK-001` · NEW-18 → `REG-SHARED-001`/`INTG-LINE-001` · NEW-12/13/14/22 → `DATA-COUNT-001` + `DEPLOY-LIVE-001`. Sửa trên Studio (`testcase_update`), không sửa file 04 |
| 4 | `[MAJOR]` | 22/23 TC (trừ NEW-24) | `spec_status` **trống** — không TC nào khai `Spec ghi rõ` / `Spec không ghi` / `Đã hỏi leader`, trong khi đã có ít nhất 2 điểm spec không chốt (badge お気に入り, hành vi InfoEvent) | Điền `spec_status` trước khi chạy; TC nào expected suy từ đọc code (không có spec) phải ghi `Spec không ghi` + đã hỏi ai |
| 5 | `[NIT]` | NEW-8 | Expected của NEW-8 ("tab không thao tác giữ số cũ là **giới hạn đã biết, KHÔNG tính là lỗi**") **mâu thuẫn spec** BR-07 `chat-management` + `chat-11` §Real-time ("`InfoEvent` cập nhật badge realtime"). **Leader đã chốt 2026-09-17: badge KHÔNG đồng bộ giữa 2 tab** → expected của NEW-8 là **đúng**, spec mới là chỗ sai | Giữ nguyên NEW-8, **không sửa TC**. Việc cần làm nằm ở phía spec → §6 #1. Hạ từ `[MAJOR]` (vòng review đầu) xuống `[NIT]` sau khi Leader chốt |
| 6 | `[MAJOR]` | NEW-24 | Expected tự ghi "kỳ vọng **CHƯA được PO/dev chốt**, không tự kết luận đạt/không đạt" → TC **không phán được kết quả**; đồng thời gộp **2 kịch bản A + B** trong 1 TC (không atomic) | Chốt kỳ vọng REQ-007 trước (§6), rồi tách thành 2 TC: (A) badge お気に入り sau khi menu được vẽ lại · (B) badge お気に入り khi trang load lúc count = 0 |
| 7 | `[MINOR]` | NEW-25 | `requirement_keys` **rỗng** (TC duy nhất không map REQ nào) dù nội dung thuộc REQ-003 | Gán `REQ-003` trên Studio |
| 8 | `[MINOR]` | Toàn bộ | Chưa TC nào chạy ở env nào (`local` runs=1 nhưng automated=0; dev/staging/prd runs=0). NEW-18 khai `staging/prd` + `manual` nhưng là TC **duy nhất** kiểm đường socket tin nhắn thật | Xếp NEW-18 vào đợt chạy tay trên staging; đừng để nó rơi lại vì cả bộ đang chạy local |

### Chất lượng từng TC

| # | Severity | TC | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 9 | `[MINOR]` | NEW-9, NEW-20 | Tiền đề cần **100 / 120 hội thoại chưa xác nhận, mỗi hội thoại đúng 1 tin** nhưng không nói cách dựng → người khác khó dựng lại đúng | Ghi cách seed (script/SQL seed hay import) vào `precondition`, hoặc hạ xuống mốc dễ dựng: 99 → 100 → 101 |
| 10 | `[NIT]` | NEW-21, NEW-22 | Tiêu đề mở đầu bằng "(Kiểm tra kỹ thuật)" — không phải keyword vùng ảnh hưởng | Đổi thành tên có keyword (`sidebar-badge.js`, `/ajax/change-status-message`) để suy được impact từ tiêu đề |

---

## 5. TCs đề xuất bổ sung (14)

> **Đối chiếu 4 điểm trong "đánh giá ảnh hưởng phía test" của Leader** (2026-09-17):
>
> | # | Điểm Leader yêu cầu | Tình trạng trong 23 TC Studio | Xử lý |
> |---|---|---|---|
> | 1 | Badge menu **chat 1:1** (お気に入り + menu thường): 0 / 1 / 99 / >99 | **Thiếu một phần** — menu thường đủ 4 mốc nhưng thao tác ở màn talk-list (NEW-9/10); tại **màn chat 1:1** chỉ có >99 (NEW-20, 120/119); **お気に入り** chỉ có 4→3 và 0→1 (NEW-24) | Bổ sung `TC-FUNC004-01`, `TC-FUNC004-02` |
> | 2 | Badge menu **talk list** (お気に入り + menu thường): 0 / 1 / 99 / >99 | — | **Đã loại khỏi phạm vi**: Leader đính chính 2026-09-17 — sidebar **chỉ có badge ở menu 「1:1チャット」**, menu 「チャット管理」 không có badge. Không viết TC |
> | 3 | Đổi trạng thái unconfirm ⇄ confirm từ **cả 2 màn** | **Đã phủ**: talk-list = NEW-1, NEW-2, NEW-3, #18139 · chat 1:1 = NEW-16 + 4 TC đề xuất `TC-REGSHARED001-01`→`-04` | Không viết thêm |
> | 4 | Regression badge màn **error message** không đổi logic | **0 TC** | Bổ sung `TC-REGSHARED001-07` (G6) |

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | [kho-tcs/fa001-chat11-11チャット.md](../../kho-tcs/fa001-chat11-11チャット.md) (FA-001 — nhóm `全て確認済みに変更`, `Bộ đếm chưa xác nhận`, `Filter danh sách bạn bè`). ⚠️ **FA-002 chat-management chưa có trong kho** — không đối chiếu được phía màn talk-list |
| Vùng regression phát hiện từ kho | `TC-CHT-34`→`TC-CHT-39` (`全て確認済みに変更`, expected có "badge menu 1:1チャット về 0") · `TC-CHT-398`→`TC-CHT-409` (Bộ đếm chưa xác nhận: ẩn/xoá/chặn bạn bè, auto-xác nhận, badge realtime) → **vùng regression của G1**, hiện 0 TC ở BƯỚC 0. ⚠️ Kho FA-001 **không có** TC nào cho badge 「送信エラー」 → `G6` không dẫn chiếu được TC kho, phải viết mới |
| Conflict expected vs kho | **Không** conflict trực tiếp với kho. Có 1 conflict TC ↔ **spec**: NEW-8 vs BR-07 → §4 #5 + §6 |
| GAP dùng lại TC kho (không viết mới) | `G4` (sync Elasticsearch / FCM) → **không viết TC**, xem dòng giải thích dưới bảng. Kho `TC-CHT-409` (badge realtime khi có tin đến) ≈ NEW-18 → **không viết lại** |
| Xác nhận chống trùng | Đã đối chiếu **23 TC ở BƯỚC 0** + kho FA-001 — **không TC đề xuất nào trùng**. 5 TC thêm ngày 2026-09-17 đã đối chiếu riêng với NEW-9/10/11/20/24 (mốc 99 & badge お気に入り): khác **vị trí badge** nên không trùng, xem cột `Ghi chú` từng dòng |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-REGSHARED001-01 | UI | REG-SHARED-001 | Màn chat 1:1 — `全て確認済みに変更` | Normal | auto | Tất cả | Xác nhận toàn bộ từ màn chat 1:1 — badge menu 「1:1チャット」 về 0 ngay, không tải lại trang | Đăng nhập admin, đã chọn bot test riêng. Bot có đúng 3 bạn bè đang chưa xác nhận (1 tin / 2 tin / 5 tin), badge menu đang hiển thị 3. Đang mở màn 「1:1チャット」 (/basic/chat-v3), không áp bộ lọc nào | 1. Ghi lại con số trên badge cạnh menu 「1:1チャット」 (kỳ vọng 3)<br>2. Bấm icon 絞り込み bên phải ô tìm kiếm → chọn 「全て確認済みに変更」<br>3. Bấm 「決定」 ở popup xác nhận<br>4. KHÔNG tải lại trang: quan sát badge cạnh menu 「1:1チャット」 và danh sách bạn bè<br>5. Thu gọn thanh menu bên, quan sát chấm đỏ trên biểu tượng menu 「1:1チャット」<br>6. Bấm F5 rồi quan sát lại badge | 3 bạn bè chưa xác nhận: A 1 tin · B 2 tin · C 5 tin | Badge về 0 và **ẩn hẳn** ngay sau bước 3, không cần tải lại trang; không dòng nào trong danh sách còn dấu chưa xác nhận; chấm đỏ ở menu thu gọn biến mất; sau F5 badge vẫn không xuất hiện | | Lấp `G1` · Lấp `Q4` · regression — dẫn từ `TC-CHT-34` (kho FA-001) · Đánh giá spec: Spec ghi rõ (chat-11 §`全て確認済みに変更`) · Evidence: video badge đổi không reload + screenshot menu thu gọn |
| TC-REGSHARED001-02 | UI | REG-SHARED-001 | Màn chat 1:1 — `全て確認済みに変更` khi đang lọc | Normal | auto | Tất cả | Xác nhận toàn bộ khi ĐANG lọc — badge giảm đúng phần đã lọc, không về 0 | Đăng nhập admin, đã chọn bot test riêng. Bot có 5 bạn bè chưa xác nhận, trong đó 2 người gắn tag T1. Badge menu đang hiển thị 5. Đang mở màn 「1:1チャット」 | 1. Ghi lại badge (kỳ vọng 5)<br>2. Áp bộ lọc tag T1 → danh sách còn 2 bạn bè chưa xác nhận<br>3. Bấm icon 絞り込み → 「全て確認済みに変更」 → 「決定」<br>4. KHÔNG tải lại trang, quan sát badge<br>5. Xoá bộ lọc, kiểm tra trạng thái 3 bạn bè còn lại<br>6. Đối chiếu badge với số bạn bè chưa xác nhận thực tế | 5 bạn bè chưa xác nhận; 2 trong số đó gắn tag T1 | Badge đổi **5 → 3** ngay, KHÔNG về 0; 3 bạn bè ngoài bộ lọc vẫn ở trạng thái chưa xác nhận; badge khớp số bạn bè chưa xác nhận thực tế | | Lấp `Q4` · regression — dẫn từ `TC-CHT-35` (kho ghi rõ bug cũ #35144 là "set count_user_unconfirm = 0 luôn") · Đánh giá spec: Spec ghi rõ · Evidence: video + số bản ghi thực bị tác động |
| TC-REGSHARED001-03 | UI | REG-SHARED-001 | Màn chat 1:1 — ẩn bạn bè 「非表示」 | Normal | auto | Tất cả | Ẩn bạn bè có tin chưa xác nhận — badge menu giảm ngay theo cơ chế tự động xác nhận | Đăng nhập admin, đã chọn bot test riêng có đúng 4 bạn bè chưa xác nhận (badge = 4). Bạn bè A đang có 2 tin chưa xác nhận. Đang mở màn 「1:1チャット」 | 1. Ghi lại badge (kỳ vọng 4)<br>2. Mở hội thoại của bạn bè A<br>3. Bấm 「非表示」 trên thanh công cụ và xác nhận<br>4. KHÔNG tải lại trang, quan sát badge cạnh menu 「1:1チャット」<br>5. Kiểm tra hội thoại A đã biến mất khỏi danh sách mặc định<br>6. Chuyển bộ lọc sang 「非表示中」, mở A và bỏ ẩn, quan sát badge lần nữa | 4 bạn bè chưa xác nhận; A có 2 tin chưa xác nhận | Badge giảm **4 → 3** ngay khi ẩn, không cần tải lại trang (ẩn bạn bè tự động xác nhận hết tin chưa đọc theo BR-05); A rời khỏi danh sách mặc định; badge luôn khớp số bạn bè chưa xác nhận thực tế | | Lấp `G1` · regression — dẫn từ `TC-CHT-405` (kho) + BR-05 `chat-11` · Đánh giá spec: Spec ghi rõ (BR-05) · Evidence: video badge + screenshot danh sách 非表示中 |
| TC-REGSHARED001-04 | UI | REG-SHARED-001 | Màn chat 1:1 — đổi 対応ステータス ở header | Normal | auto | Tất cả | Đổi trạng thái đối ứng ở header chat 1:1 — badge menu cập nhật ngay | Đăng nhập admin, đã chọn bot test riêng có đúng 3 bạn bè chưa xác nhận (badge = 3). Bạn bè B đang chưa xác nhận và đang được mở ở khung hội thoại | 1. Ghi lại badge (kỳ vọng 3)<br>2. Bấm badge trạng thái đối ứng trên header hội thoại của B<br>3. Chọn trạng thái 「確認済」<br>4. KHÔNG tải lại trang, quan sát badge cạnh menu 「1:1チャット」 và badge trạng thái trên header<br>5. Đổi B về lại trạng thái chưa xác nhận, quan sát badge lần nữa | 3 bạn bè chưa xác nhận; đổi trạng thái đối ứng của B qua lại | Badge menu đổi **3 → 2** ngay khi chọn 「確認済」 và trở lại **3** khi đổi ngược, không cần tải lại trang; badge trạng thái trên header đổi đúng màu/tên; không có lỗi JavaScript | | Lấp `G1` · Đường riêng EP-13 `/basic/update-status-confirm` (response có `countUserUnConfirm`), khác đường `/basic/confirm-message` của NEW-16 · Đánh giá spec: Spec ghi rõ (chat-11 Luồng 6) · Evidence: video + JSON response |
| TC-REGSHARED001-05 | UI | REG-SHARED-001 | Sidebar dùng chung — các màn ngoài 2 màn chat | Normal | manual | Tất cả | Các màn khác có thanh menu bên vẫn chạy bình thường sau khi nạp tệp badge dùng chung | Đăng nhập admin, đã chọn bot test riêng có ít nhất 2 hội thoại chưa xác nhận (badge > 0) | 1. Lần lượt mở 4 màn có thanh menu bên: 「メッセージ配信」, 「タグ管理」, 「友だちリスト」, 「リッチメニュー」<br>2. Ở mỗi màn: mở bảng điều khiển trình duyệt, kiểm tra lỗi JavaScript<br>3. Ở mỗi màn: quan sát badge cạnh menu 「1:1チャット」 và chấm đỏ khi thu gọn thanh menu bên<br>4. Ở mỗi màn: thực hiện 1 thao tác cơ bản của màn đó (mở danh sách, mở modal tạo mới) | 4 màn có sidebar, bot có badge > 0 | Cả 4 màn: badge 「1:1チャット」 hiển thị đúng con số như lúc trang được dựng; bảng điều khiển **không có lỗi JavaScript** nào liên quan tệp badge dùng chung; thao tác cơ bản của từng màn chạy bình thường | | Lấp `G3` · Tệp nạp ở layout dùng chung nên lỗi sẽ lan ra mọi màn, không chỉ 2 màn chat · Đánh giá spec: Spec không ghi — suy từ `dev_impact` Studio (rủi ro `setChatMenuBadge` undefined) · Evidence: screenshot console 4 màn |
| TC-REGSHARED001-06 | UI | REG-SHARED-001 | Badge menu — đồng bộ giữa 2 màn đang mở | Abnormal | manual | Tất cả | Mở màn chat 1:1 ở tab khác trong lúc đổi trạng thái ở màn quản lý chat — tab không thao tác giữ số cũ, không hiển thị sai | Đăng nhập admin, đã chọn bot test riêng có đúng 5 hội thoại chưa xác nhận (badge = 5). Tab 1 mở 「1:1チャット」, tab 2 mở 「チャット管理」, cả hai cùng bot | 1. Xác nhận badge ở cả 2 tab đều hiển thị 5<br>2. Ở tab 2 (chat管理): tick 2 dòng thuộc 2 hội thoại khác nhau → 「確認済」 → 「変更」<br>3. Quan sát badge ở tab 2<br>4. Chuyển sang tab 1 (chat 1:1), **KHÔNG tải lại trang**, quan sát badge trong 30 giây<br>5. Ở tab 1 đổi bộ lọc danh sách bạn bè để màn tự lấy lại badge, quan sát badge<br>6. Ở tab 1 bấm F5, quan sát badge lần nữa | 5 hội thoại chưa xác nhận; tab 2 xác nhận 2 hội thoại | Tab 2: badge về 3 ngay. Tab 1: badge **giữ nguyên 5** — badge KHÔNG đồng bộ giữa 2 tab, đây là **hành vi đúng đã được Leader chốt (2026-09-17)**, không tính là lỗi. Tab 1 không được hiển thị sai kiểu rỗng / `NaN` / số âm trong lúc chờ. Sau bước 5 (tab 1 tự lấy lại badge) và sau bước 6 (F5), badge tab 1 **bắt buộc** về đúng 3 — nhảy thẳng tới số thật, không cộng trừ dồn từ số cũ | | Lấp `G2` · Kỳ vọng đã được **Leader chốt 2026-09-17**: badge không đồng bộ giữa 2 tab · Chốt này xác nhận expected của **NEW-8** là đúng và **spec BR-07 / chat-11 §Real-time đang mô tả sai** → §6 #1 (sửa spec, không sửa TC) · Đánh giá spec: Đã hỏi leader · Evidence: video 2 tab + screenshot badge sau bước 5 và 6 |
| TC-DEPLOYLIVE001-01 | UI | DEPLOY-LIVE-001 | Release không maintain — client cũ gọi server mới | Normal | manual | product | Màn quản lý chat mở sẵn TRƯỚC khi release — đổi trạng thái mà không tải lại trang | Có 2 bản: bản cũ (chưa có fix) đang chạy, bản mới `ai_fixbug_40336` sẵn sàng release. Đăng nhập admin, đã chọn bot test riêng có đúng 6 hội thoại chưa xác nhận | 1. Trên bản CŨ: mở màn 「チャット管理」, ghi lại badge (=6), **giữ nguyên tab, không đóng, không reload**<br>2. Release bản mới lên môi trường đang test (không bật maintain)<br>3. Quay lại tab đang mở: tick 1 dòng → 「確認済」 → 「変更」<br>4. Quan sát badge, danh sách, thông báo và bảng điều khiển trình duyệt<br>5. Kiểm tra số hội thoại chưa xác nhận thực tế của bot<br>6. Bấm F5 rồi thao tác lần nữa và quan sát badge | Tab mở bằng JS bản cũ; máy chủ đã chạy bản mới | Bước 3-4: thao tác đổi trạng thái **thành công**, không lỗi 500, không exception, không lưu nửa vời; badge có thể chưa cập nhật ngay (JS cũ) nhưng **không được hiển thị sai** (không rỗng, không NaN); số hội thoại chưa xác nhận thực tế giảm đúng 1. Bước 6: sau F5 badge cập nhật ngay đúng hành vi mới | | Lấp `Q1` · Đúng nguyên văn quan điểm: **client cũ gọi server mới**, khác `REG-RUN-001` · Đánh giá spec: Spec không ghi — suy từ trigger quan điểm · Evidence: screenshot màn trước release + JSON response + log request |
| TC-DEPLOYLIVE001-02 | UI | DEPLOY-LIVE-001 | Release không maintain — gián đoạn giữa chừng | Abnormal | manual | product | Đổi trạng thái đúng lúc máy chủ đang khởi động lại — badge không hiển thị sai, không mất dữ liệu | Đăng nhập admin, đã chọn bot test riêng có đúng 5 hội thoại chưa xác nhận (badge = 5), màn 「チャット管理」 đang mở | 1. Ghi lại badge (=5)<br>2. Kích hoạt release/restart dịch vụ<br>3. Trong lúc dịch vụ đang khởi động lại: tick 1 dòng → 「確認済」 → 「変更」<br>4. Quan sát badge, thông báo lỗi và bảng điều khiển trình duyệt<br>5. Chờ dịch vụ ổn định, bấm F5 và đối chiếu badge với số hội thoại chưa xác nhận thực tế | Yêu cầu rơi vào cửa sổ dịch vụ khởi động lại (lỗi mạng / 502 / timeout) | Màn hình báo lỗi **rõ ràng** (không im lặng, không thông báo thành công giả); badge **giữ nguyên số 5 đang hiển thị**, không rỗng / không NaN / không 0; sau khi dịch vụ ổn định và F5, badge khớp đúng số thật — dữ liệu không bị đổi nửa vời | | Lấp `Q1` · RULE-01: quan điểm Cao còn thiếu **Boundary** — không đề xuất vì kịch bản release không có khái niệm giá trị biên · Đánh giá spec: Spec không ghi · Evidence: video + log request lỗi |
| TC-DEPLOYASSET001-01 | UI | DEPLOY-ASSET-001 | Release asset — cache trình duyệt | Normal | manual | Tất cả | Trình duyệt còn cache bản cũ, chỉ F5 thường — vẫn nạp được tệp badge bản mới | Trình duyệt đã mở tool ở bản CŨ ít nhất 1 lần (asset đã nằm trong cache). Bản mới `ai_fixbug_40336` đã release. Đăng nhập admin, đã chọn bot test riêng có ít nhất 3 hội thoại chưa xác nhận | 1. Mở màn 「チャット管理」 ở bản cũ để trình duyệt cache asset<br>2. Release bản mới<br>3. **Chỉ bấm F5 thường (KHÔNG Ctrl+F5, không xoá cache)**<br>4. Mở tab theo dõi tài nguyên: kiểm tra tệp badge dùng chung của thanh menu bên — mã trạng thái và tham số phiên bản trên đường dẫn<br>5. Tick 1 dòng → 「確認済」 → 「変更」, quan sát badge<br>6. Lặp lại bước 3-5 ở màn 「1:1チャット」 | Trình duyệt giữ cache asset bản cũ; chỉ F5 thường | Tệp badge dùng chung tải về với mã trạng thái **200** và tham số phiên bản **khớp bản mới** (không lấy bản cache cũ, không 404); badge cập nhật ngay sau bước 5 ở cả 2 màn; bảng điều khiển không có lỗi hàm không tồn tại | | Lấp `Q3` · Bổ sung bước (1)-(3) của quan điểm mà NEW-21 còn thiếu — NEW-21 chỉ làm bước (4) · Đánh giá spec: Spec không ghi · Evidence: screenshot DevTools Network (query version + 200) + screenshot màn sau F5 thường |
| TC-CONC001-01 | UI | CONC-001 | Màn quản lý chat — 2 admin thao tác đồng thời | Abnormal | manual | product | Hai tài khoản admin cùng đổi trạng thái trên cùng bot — badge của cả hai về đúng số thật | Bot test riêng có đúng 8 hội thoại chưa xác nhận (badge = 8). Hai tài khoản admin khác nhau (2 trình duyệt / 2 phiên đăng nhập riêng), cùng chọn bot đó, cùng mở màn 「チャット管理」 | 1. Xác nhận badge ở cả 2 phiên đều hiển thị 8<br>2. Phiên A chọn 2 dòng (hội thoại 1, 2); phiên B chọn 2 dòng KHÁC (hội thoại 3, 4)<br>3. Hai phiên bấm 「変更」 → 「確認済」 **gần như cùng lúc** (trong vòng 1 giây)<br>4. Quan sát badge ở từng phiên<br>5. Đối chiếu với số hội thoại chưa xác nhận thực tế của bot<br>6. F5 cả 2 phiên và quan sát lại | 8 hội thoại chưa xác nhận; A xác nhận 2, B xác nhận 2 (không giao nhau) | Số hội thoại chưa xác nhận thực tế về đúng **4**; sau F5 badge ở cả 2 phiên đều là 4. Badge ngay sau bước 3 có thể lệch tạm (mỗi phiên nhận số tại thời điểm máy chủ tính) nhưng **không được âm, không NaN**, và không phiên nào ghi đè số thật thành 6 sau khi đã F5 | | Lấp `Q5` · RULE-08: race → `product` · Theo memory dự án: đổi bot áp cho cả trình duyệt nên phải dùng **2 phiên đăng nhập riêng** · Đánh giá spec: Spec không ghi · Evidence: video 2 màn hình + số thực tế |
| TC-DATACOUNT001-01 | Data | DATA-COUNT-001 | Bộ đếm chưa xác nhận — đối chiếu nhiều nguồn | Abnormal | manual | Tất cả | Cùng một con số chưa xác nhận phải khớp giữa badge menu, bộ lọc 未確認 và danh sách tin | Đăng nhập admin, đã chọn bot test riêng. Dựng dữ liệu **biết trước kết quả**: 4 bạn bè chưa xác nhận với số tin lần lượt 1 / 2 / 5 / 1 (tổng 9 tin, 4 hội thoại); các bạn bè còn lại đã xác nhận hết | 1. Mở màn 「チャット管理」, ghi lại con số trên badge menu 「1:1チャット」<br>2. Chuyển tab 「未確認のみ」, đếm số **hội thoại khác nhau** trong danh sách<br>3. Mở màn 「1:1チャット」, chọn bộ lọc 「未確認」, đếm số bạn bè hiển thị<br>4. Đối chiếu 3 con số ở bước 1-2-3 với phép đếm tay (4 hội thoại / 9 tin)<br>5. Xác nhận 1 tin của bạn bè có 5 tin, lặp lại bước 1-3<br>6. Xác nhận nốt 4 tin còn lại của bạn bè đó, lặp lại bước 1-3 | 4 hội thoại chưa xác nhận: 1 tin · 2 tin · 5 tin · 1 tin | Bước 4: badge = **4** (đếm theo hội thoại, KHÔNG phải 9 tin); bộ lọc 未確認 ở màn chat 1:1 hiện đủ **4 bạn bè** — kể cả người có 2 và 5 tin chưa đọc. Bước 5: cả 3 nguồn vẫn = 4 (hội thoại còn tin chưa xác nhận). Bước 6: cả 3 nguồn cùng về 3 | | Lấp `Q6` · Bắt đúng vùng `MT-19` của kho (spec tự mâu thuẫn `confirm_count = 1` vs `> 0`) — nếu bộ lọc chỉ hiện người có đúng 1 tin thì raise lại #33731 · Đánh giá spec: Đã hỏi leader · Evidence: bảng đối chiếu 3 nguồn + phép đếm tay |
| TC-FUNC004-01 | UI | FUNC-004 | Sidebar — badge menu 「1:1チャット」 ở お気に入り | Boundary | manual | Tất cả | Badge menu chat 1:1 ở お気に入り — bổ sung 2 mốc 99 và 100 còn thiếu | Đăng nhập admin, đã chọn bot test riêng. Đã ghim 「1:1チャット」 vào mục お気に入り. Bot có đúng **100** hội thoại chưa xác nhận, đã kiểm lại con số ở tầng dữ liệu | 1. Mở màn 「チャット管理」, ghi lại badge ở mục お気に入り và ở menu thường (số thật 100)<br>2. Xác nhận 1 hội thoại → số thật **99**, KHÔNG tải lại trang, ghi lại 2 badge<br>3. Đổi ngược 1 hội thoại về 未確認 → số thật **100**, ghi lại 2 badge<br>4. Thu gọn thanh menu bên, quan sát chấm đỏ ở cả 2 vị trí<br>5. F5 và đối chiếu lại 2 badge | 100 hội thoại chưa xác nhận, đưa về 99 rồi trở lại 100 | Ở mốc 100 cả 2 vị trí hiện `99+`; ở mốc 99 cả 2 vị trí hiện `99` (không còn dấu cộng, **không** hiện `+99`); quay lại 100 thì cả 2 lại hiện `99+`. Hai vị trí **không bao giờ lệch nhau**, và khớp giá trị sau F5 | | Lấp `Q7` · Bù đúng phần NEW-24 thiếu (NEW-24 chỉ chạy mốc 4→3 và 0→1) · Ăn khớp NEW-20: NEW-20 kiểm quy ước `99+` ở **menu thường màn chat 1:1**, TC này kiểm ở **mục お気に入り** · Đánh giá spec: Spec không ghi · Evidence: screenshot 2 vị trí tại từng mốc |
| TC-FUNC004-02 | UI | FUNC-004 | Màn chat 1:1 — badge menu 「1:1チャット」 tại mốc 99 và 0 | Boundary | manual | Tất cả | Thao tác **tại màn chat 1:1** đưa số qua mốc 99 và về 0 — badge đổi đúng quy ước | Đăng nhập admin, đã chọn bot test riêng có đúng **100** hội thoại chưa xác nhận, mỗi hội thoại 1 tin. Đang mở màn 「1:1チャット」 (/basic/chat-v3) | 1. Ghi lại badge menu 「1:1チャット」 (kỳ vọng `99+`)<br>2. Đánh dấu đã đọc 1 hội thoại → số thật **99**, KHÔNG tải lại trang, quan sát badge<br>3. Dùng 「全て確認済みに変更」 đưa số thật về **0**, quan sát badge và chấm đỏ khi thu gọn thanh menu bên<br>4. Đánh dấu chưa đọc 1 hội thoại → số thật **1**, quan sát badge<br>5. F5 và đối chiếu badge với giá trị trước F5 | 100 hội thoại chưa xác nhận; đưa lần lượt về 99 → 0 → 1 | Bước 2: badge hiện `99` (không phải `+99`, không phải số thô `99` kiểu cũ của đường đánh dấu đã đọc). Bước 3: badge **ẩn hẳn**, chấm đỏ biến mất. Bước 4: badge hiện lại `1`. Mọi mốc đều đổi ngay, không cần tải lại trang, và khớp giá trị sau F5 | | Lấp `Q7` · NEW-20 chỉ chạy 120/119 (luôn ở vùng >99) — TC này bù **mốc 99 đúng biên** và **mốc 0** tại chính màn chat 1:1 · Đánh giá spec: Spec không ghi · Evidence: video badge qua 4 mốc |
| TC-REGSHARED001-07 | UI | REG-SHARED-001 | Sidebar — badge 「送信エラー」 (màn error message FA-028) | Normal | manual | Tất cả | Badge lỗi gửi tin giữ nguyên logic sau khi gộp hàm ghi badge | Đăng nhập admin, đã chọn bot test riêng đang có: **≥ 1 tin gửi lỗi** (badge 「送信エラー」 > 0) **và** ≥ 3 hội thoại chưa xác nhận (badge 「1:1チャット」 > 0). Ghi lại số của cả 2 badge trước khi thao tác | 1. Mở màn 「チャット管理」, ghi lại đồng thời badge 「送信エラー」 và badge 「1:1チャット」<br>2. Tick 2 dòng → 「確認済」 → 「変更」, KHÔNG tải lại trang<br>3. Quan sát **cả 2 badge** và bảng điều khiển trình duyệt<br>4. Mở màn 「1:1チャット」, đánh dấu đã đọc 1 hội thoại, quan sát lại **cả 2 badge**<br>5. Mở màn danh sách lỗi gửi tin (`/basic/error-list-v2`), xử lý 1 bản ghi lỗi (đăng ký lại thời gian gửi hoặc xoá), quay lại và quan sát badge 「送信エラー」<br>6. Thu gọn thanh menu bên, kiểm tra chấm đỏ của từng mục menu | Bot có đồng thời tin gửi lỗi và hội thoại chưa xác nhận | Bước 2-4: badge 「1:1チャット」 đổi đúng, còn badge 「送信エラー」 **giữ nguyên con số cũ** — không bị xoá trắng, không bị ghi đè bằng số hội thoại chưa xác nhận, không mất chấm đỏ. Bước 5: badge 「送信エラー」 cập nhật theo đúng logic sẵn có của FA-028 (`99+` khi ≥ 100). Bước 6: chấm đỏ của từng mục menu độc lập, không menu nào thêm/mất sai | | Lấp `G6` · regression — Leader yêu cầu trực tiếp ("badge màn error message không đổi logic") · Badge này **do layout chung dựng** (`error-message/feature-spec.md:38, :61`) nên nằm trong cùng `sidebar.blade.php` vừa sửa; `InfoEvent` phát kèm `totalErrorMessage` (BR-07) · Đánh giá spec: Spec ghi rõ (FA-028 `:140` quy ước `99+` khi ≥ 100) · Evidence: screenshot 2 badge trước/sau + console |

**GAP không đề xuất TC** — `G4` (`sync_elasticsearch` BR-06 + FCM `updateBadge()` BR-07): diff của `TalkListController.php` chỉ **+4/−1** (thêm field vào response), không chạm nhánh ghi Elasticsearch lẫn nhánh đẩy FCM; `countBadge` — hàm duy nhất đổi kiểu trả về — được cả Dev và `dev_impact` Studio xác nhận chỉ có 2 caller, cả hai đều là đường hiển thị web. Không có bằng chứng 2 nhánh này bị ảnh hưởng → **không viết TC**; việc cần làm là hỏi Dev nguồn số của `updateBadge()`.

**Chưa đề xuất TC cho `PERF-LARGE-001`** (bot có hàng nghìn hội thoại chưa xác nhận → `totalUserConfirmMessage` tính lại mỗi lần đổi trạng thái): quan điểm ưu tiên Trung bình, và fix **không** đổi cách tính (chỉ chuyển con số đã tính sẵn vào response). Nếu Leader muốn phủ, chạy lại NEW-3 trên bot volume lớn và đo thời gian phản hồi thay vì viết TC mới.

---

## 6. Spec update needed

| # | Điểm cần chốt | Nguồn mâu thuẫn | Ảnh hưởng tới TC | Người chốt |
|---|---|---|---|---|
| 1 | ✅ **ĐÃ CHỐT (Leader, 2026-09-17)** — badge **không đồng bộ giữa 2 tab**; `InfoEvent` **không** dùng để cập nhật badge realtime trên web. Việc còn lại: **sửa spec cho khớp hành vi thật** | Spec `chat-management` **BR-07**: "broadcast `InfoEvent` qua WebSocket **cập nhật realtime web**" + `chat-11` §Real-time: "`InfoEvent`: cập nhật badge số tin chưa đọc" — cả hai **mô tả sai so với hành vi đã chốt** | Đã áp vào: **NEW-8** giữ nguyên expected (§4 #5 hạ xuống `[NIT]`) · **TC-REGSHARED001-06** đổi expected thành "tab 1 giữ nguyên số cũ, chỉ về số thật sau khi tự lấy lại badge hoặc F5" | Dev/Leader sửa `spec-features/admin/chat-management/feature-spec.md` BR-07 + `chat-11/feature-spec.md` §Real-time (**sub-repo `spec-features/`, không commit từ repo cha**) |
| 2 | **Kỳ vọng badge ở menu お気に入り** (REQ-007) | Studio tự ghi "kỳ vọng CHƯA được PO/dev chốt" cho 2 tình huống: khi khung menu được vẽ lại, và khi trang load lúc số = 0 (phần tử badge chưa tồn tại). Dev cũng nêu cùng rủi ro ở `03-dev-impact.md` mục 7 | **NEW-24** hiện không phán được Đạt/Không đạt; cần chốt rồi tách thành 2 TC | PO + Dev |
| 3 | **Điều kiện lọc 「未確認」** — `confirm_count = 1` hay `> 0` | Spec `chat-11` tự mâu thuẫn: `feature-spec.md:121` ghi `confirm_count = 1`, `feature-spec.md:394` ghi `0 = đã xác nhận, > 0 = chưa` (kho FA-001 đã ghi nhận là `MT-19`, mức **CAO**) | Quyết định expected của **TC-DATACOUNT001-01** bước 4. Nếu điều kiện thực là `= 1` thì bạn bè có ≥ 2 tin chưa đọc bị lọt khỏi bộ lọc — đúng hiện tượng khách hàng báo ở #33731 | Dev xác nhận truy vấn thực → sửa spec |
| 4 | **`app/Helpers/functions.php` — `totalUserConfirmMessage` có bị sửa cùng lượt không** | Diff `functions.php` **+7/−1** nhưng `dev_impact` Studio chỉ mô tả `countBadge`; Dev lại liệt kê `totalUserConfirmMessage` trong mục "đã check". Studio chỉ trả `diffStat`, không có hunk | Quyết định `G2` là RISK hay GAP thật. Nếu `totalUserConfirmMessage` bị đổi → phải bổ sung TC cho FCM badge mobile + `InfoEvent` (hiện đang loại khỏi phạm vi ở §2/§5) | Dev |
