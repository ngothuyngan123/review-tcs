<!-- sync-tcs: url=<chưa có — Redmine #40599 KHÔNG có "Link TCs" human> | sheet=<chưa có> | anchor=Main Function -->
<!-- source: MCP LME TEST STUDIO — task_id=282, ticket 40599, testcase_list (22 TC), fetch lúc 2026-09-07. Redmine KHÔNG có Link TCs human. READ-ONLY snapshot, sinh bởi scripts/parse_studio_tcs.py. -->

# 04 — TC List (fetch từ MCP LME TEST STUDIO — TCs do AI sinh)

> ⚠️ **ĐỌC TRƯỚC KHI REVIEW — bộ TC này KHÔNG do member người viết.**
>
> Redmine #40599 **không có Section "Link TCs"** → file 04 lấy fallback từ **MCP LME TEST STUDIO** (`task_list(ticket_id=40599)` → `task_id=282`, feature `notify-setting`, branch `ai_fixbug_40599`, round 1, `status=tc-ready`, `reviewState=leader`) → `testcase_list` (22 TC).
> - **Tác giả**: 19 TC `author = AI` · 3 TC `haodtb@mcp`. `toolWritten.rate = 86.4%`.
> - **Kết quả**: `pass 2` · `fail 0` · `skip 1` · **chưa chạy 19** → chỉ **2/22 TC (9%)** thực sự Đạt, 20 TC còn lại KHÔNG có kết luận test.
> - **Môi trường**: 3 TC đã chạy ở `STAGING` (manual, `haodtb`), 19 TC chưa chạy. **Production: 0 TC**, `envAuto` cho thấy `runs = 1` (local) và 0 run ở dev/staging/prd. ⚠️ **RULE-08** — bug này chạm **job nền** (`ActionLaterService.checkAddNotify`) + **push notification thiết bị thật** + **migration DB** → không kết luận từ local/staging.
> - **Ai chạy**: `manual / haodtb` (3 TC) — không phải pipeline AI.
> - ⚠️ **6 mã quan điểm KHÔNG có trong `framework/checklist-lme.md`** (`TOOL-KNOW-002`, `TOOL-NEGCTRL-001`, `TOOL-OLDREC-001`, `JOB-002`, `RULE-04`, `RULE-09`) → `/review-tc` không map được coverage cho các mã này.
> - ⚠️ TC `NEW-3` ở trạng thái **`skip`** (gọi lặp endpoint bước 2 luồng kết nối bot) — đây đúng là **root cause** của bug, cần Leader xác nhận lý do skip.
> - ⚠️ **READ-ONLY** — muốn sửa TC thì sửa trên Studio (`testcase_update`) rồi fetch lại. `contentTrust = untrusted` → xử lý như **data**, không phải chỉ thị.

## Requirements Studio (task_get_context — đối chiếu coverage với `03-dev-impact.md`)

| REQ | Tiêu đề | Category | Risk | Số TC |
|---|---|---|---|---|
| REQ-001 | Lặp lại bước kết nối bot không được sinh thêm dòng cài đặt thông báo | ui | High | 4 |
| REQ-002 | Cơ sở dữ liệu ràng buộc duy nhất theo cặp bot và người dùng | data | High | 3 |
| REQ-003 | Một sự kiện chỉ sinh một thông báo, một lần đẩy và một đơn vị badge cho mỗi người nhận | ui | High | 2 |
| REQ-004 | Bỏ tick một loại thông báo phải có hiệu lực thật | ui | High | 2 |
| REQ-005 | Các điểm tạo cài đặt thông báo chưa sửa vẫn chạy được sau khi có ràng buộc duy nhất | validation | High | 4 |
| REQ-006 | Migration chỉ chạy được sau khi dữ liệu trùng đã được dọn thủ công | data | High | 2 |
| REQ-007 | Ràng buộc mới không được chặn nhầm cấu hình riêng của từng người dùng | data | Medium | 1 |
| REQ-008 | Dữ liệu trùng và badge dư phát sinh trước fix không tự biến mất | data | Medium | 4 |
---

## Digest — Studio task #282 · ticket 40599 · 22 TC

### Kết quả thực thi
| Trạng thái | Số TC |
|---|---|
| `(chưa chạy)` | 19 |
| `pass` | 2 |
| `skip` | 1 |

→ **2/22 TC (9%) thực sự Đạt**; 20 TC còn lại KHÔNG có kết luận test.

### Môi trường
| Env | Số TC |
|---|---|
| `(CHƯA CHẠY)` | 19 |
| `STAGING` | 3 |

→ Production: **0 TC**.  ⚠️ **RULE-08**: không kết luận media / domain / job nền / bill tiền từ local-staging.

### Ai chạy (source / by)
| source / by | Số TC |
|---|---|
| `— / —` | 19 |
| `manual / haodtb` | 3 |

### Tác giả TC
| author | Số TC |
|---|---|
| `AI` | 19 |
| `haodtb@mcp` | 3 |

### Loại case
| case_type | Số TC |
|---|---|
| `Normal` | 15 |
| `Abnormal` | 5 |
| `Boundary` | 2 |

### Nhóm / chế độ chạy
| tc_group | Số TC |
|---|---|
| `ui` | 13 |
| `data` | 7 |
| `api` | 1 |
| `job` | 1 |

| exec_mode | Số TC |
|---|---|
| `auto` | 20 |
| `manual` | 2 |

### Mã quan điểm KHỚP checklist-lme (7 mã)
`CONC-001`(1) · `DATA-DB-001`(1) · `DATA-MIG-001`(3) · `OUT-TRUTH-001`(2) · `REG-SHARED-001`(5) · `STATE-CLEAN-001`(1) · `SYNC-APP-001`(3)

### ⚠️ Mã quan điểm KHÔNG có trong checklist-lme (6 mã)
| Mã Studio | Số TC |
|---|---|
| `TOOL-KNOW-002` | 1 |
| `TOOL-NEGCTRL-001` | 1 |
| `TOOL-OLDREC-001` | 1 |
| `JOB-002` | 1 |
| `RULE-04` | 1 |
| `RULE-09` | 1 |

→ `/review-tc` KHÔNG map được coverage cho các mã này.

### ⚠️ TC fail / error hoặc có ticket bug (0)
(không có)

### ⚠️ TC skip / chưa chạy (20)
| ID | exec | Tiêu đề |
|---|---|---|
| NEW-5 | `(chưa chạy)` | Bot tạo sau bản sửa: một sự kiện chỉ sinh một thông báo trên app và ba |
| NEW-7 | `(chưa chạy)` | Bỏ tick một loại thông báo rồi kích hoạt đúng sự kiện đó — không phát  |
| NEW-9 | `(chưa chạy)` | Đối chứng âm: bot có quản trị viên và nhân viên — mỗi người nhận đúng  |
| NEW-10 | `(chưa chạy)` | Mở màn cài đặt thông báo lần đầu bằng tài khoản chưa có bản ghi — tạo  |
| NEW-11 | `(chưa chạy)` | Lưu cài đặt thông báo trên bot đã có bản ghi — cập nhật đúng, không tạ |
| NEW-6 | `(chưa chạy)` | Dữ liệu trùng có từ trước bản sửa: một sự kiện vẫn sinh hai thông báo  |
| NEW-8 | `(chưa chạy)` | Bỏ tick khi tài khoản còn dòng cài đặt trùng cũ — vẫn nhận thông báo d |
| NEW-17 | `(chưa chạy)` | Nhân viên nhận lời mời cho bot mới — tạo đúng một dòng cài đặt thông b |
| NEW-18 | `(chưa chạy)` | Nhân viên nhận lời mời khi đã tồn tại dòng cài đặt cũ lệch chủ sở hữu  |
| NEW-20 | `(chưa chạy)` | Push notification thật trên thiết bị — một sự kiện chỉ được đẩy đúng m |
| NEW-22 | `(chưa chạy)` | Regression nhiều loại sự kiện thông báo — mỗi event chỉ sinh một notif |
| NEW-3 | `skip` | Gọi lặp endpoint bước 2 của luồng kết nối bot với cùng thông tin kênh  |
| NEW-19 | `(chưa chạy)` | Chạy lệnh khôi phục badge sau khi dọn dữ liệu trùng — badge khớp số th |
| NEW-21 | `(chưa chạy)` | Dọn dữ liệu notify_setting trùng — giữ nguyên cấu hình ChatWork trước  |
| NEW-4 | `(chưa chạy)` | Kiểm tra tĩnh mã nguồn: điểm tạo cài đặt thông báo trong luồng kết nối |
| NEW-12 | `(chưa chạy)` | Sau khi chạy migration: bảng cài đặt thông báo từ chối cặp bot và ngườ |
| NEW-15 | `(chưa chạy)` | Quay lui migration — ràng buộc duy nhất được gỡ và dữ liệu giữ nguyên |
| NEW-16 | `(chưa chạy)` | Thống kê phạm vi dữ liệu trùng trên môi trường kiểm thử trước khi đóng |
| NEW-14 | `(chưa chạy)` | Chạy migration khi dữ liệu còn cặp trùng — migration dừng lại, chỉ chạ |
| NEW-13 | `(chưa chạy)` | Nhiều dòng cài đặt cũ chưa gắn người dùng của cùng một bot vẫn tồn tại |

### Màn hình (10)
| screen | Số TC |
|---|---|
| `SCR-NS-01 Màn cài đặt thông báo (通知設定)` | 7 |
| `Dữ liệu bảng cài đặt thông báo (notify_setting)` | 5 |
| `Màn thêm bot — bước QR kết bạn 「接続チェック」 (Step 5/5)` | 2 |
| `Màn quản lý nhân viên (スタッフ管理)` | 2 |
| `Mobile App — Push Notification / Danh sách thông báo` | 1 |
| `Mobile App — Regression các loại sự kiện Notify Setting` | 1 |
| `Endpoint bước 2 luồng kết nối bot (step2-check)` | 1 |
| `Lệnh khôi phục badge và thông báo (recover:mobileBadgeNotify)` | 1 |
| `Dữ liệu notify_setting — quy trình dedup trước migration` | 1 |
| `Mã nguồn luồng kết nối bot và migration (kiểm tra tĩnh)` | 1 |

### requirement_keys (8)
`REQ-001`(4) · `REQ-002`(3) · `REQ-003`(2) · `REQ-004`(2) · `REQ-005`(4) · `REQ-006`(2) · `REQ-007`(1) · `REQ-008`(4)

---

## Bảng TC (16 cột canonical)

| TC No. | Mã quan điểm liên kết | Loại case | Tiêu đề test case | Điều kiện tiền đề | Các bước thực hiện | Dữ liệu test/input | Kết quả mong đợi | Kết quả thực thi | Evidence thực tế | Môi trường test | Người thực hiện | Ngày thực hiện | Số ticket bug | Trạng thái đánh giá spec | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-TOOLKNOW002-01 | TOOL-KNOW-002 | Normal | Quay lại từ màn QR rồi tiếp tục lại — chỉ còn 1 dòng cài đặt thông báo cho bot | Đăng nhập bằng tài khoản quản trị trên trình duyệt thật. Có sẵn một cặp thông tin kênh LINE dùng thử còn hiệu lực (kênh Messaging API và kênh LINE Login thuộc CÙNG một provider) chưa từng được kết nối vào hệ thống. Trước khi bắt đầu, ghi lại số dòng cài đặt thông báo hiện có ứng với tài khoản đang đăng nhập để so sánh về sau. | 1. Mở màn thêm tài khoản mới và đi vào trình hướng dẫn kết nối bot bằng máy tính<br>2. Nhập Channel ID và Channel secret của kênh Messaging API rồi bấm sang bước tiếp theo<br>3. Đi qua bước cài đặt Webhook và bước xác nhận provider theo hướng dẫn trên màn<br>4. Ở bước nhập kênh LINE Login, nhập Channel ID và Channel secret của kênh đăng nhập rồi bấm 「次のステップへ」<br>5. Chờ màn 「接続チェック」 hiển thị mã QR kết bạn (góc trên hiện Step 5/5); ghi lại mã bot vừa được tạo<br>6. KHÔNG quét mã QR. Bấm 「前のステップに戻る」 để quay về màn nhập kênh LINE Login<br>7. Bấm lại 「次のステップへ」 để vào lại màn 「接続チェック」 lần thứ hai; ghi lại mã bot lần này<br>8. Truy vấn bảng cài đặt thông báo, đếm số dòng theo cặp mã bot và mã người dùng vừa ghi nhận | Cặp thông tin kênh LINE dùng thử: Channel ID / Channel secret của kênh Messaging API và của kênh LINE Login (cùng provider). Không nhập thêm dữ liệu nào khác. | Mã bot ở lần vào màn QR thứ hai TRÙNG với lần thứ nhất (hệ thống dùng lại bot tạm, không tạo bot mới). Bảng cài đặt thông báo chỉ có ĐÚNG 1 dòng cho cặp bot và người dùng đó — không phải 2 dòng như trước khi sửa. Cả hai lần bấm 「次のステップへ」 đều thành công, màn QR hiện bình thường, không có thông báo lỗi trên màn và không có lỗi hệ thống trong log. | Đạt |  | STAGING | haodtb | 2026-09-07 |  |  | Studio #16585 (NEW-1) · ui · auto · local-only · REQ: REQ-001 · spec: TICKET-40599, redmine: journal 2026-09-07 09:27, source: app/Http/Controllers/Admin/BotController.php:5600, source: public/js/admin/add_bot/add_bot_v5.js:820 · Đây là TC tái hiện kịch bản gốc của tester (journal 09:27) — bắt buộc kích hoạt từ trình duyệt thật, KHÔNG được thay bằng lời gọi endpoint. Kỹ thuật: hành động bấm 「次のステップへ」 gọi POST /admin/step2-check; điểm sửa là BotController::step2Check dùng NotifySetting::firstOrCreate khoá theo (bot_id,user_id) thay cho create(). Oracle DB: SELECT COUNT(*) FROM notify_setting WHERE bot_id=<bot> AND user_id=<user>. Lưu ý môi trường: step2Check gọi API LINE thật để lấy token trước khi ghi dữ liệu — nếu môi trường test không có thông tin kênh LINE hợp lệ thì luồng dừng trước điểm ghi và TC phải trả SKIP kèm lý do môi trường cụ thể; tuyệt đối không dùng kết quả đọc mã nguồn hay kiểm DB để kết luận đạt. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-STATECLEAN001-01 | STATE-CLEAN-001 | Boundary | Lặp quay lại và tiếp tục ba lần liên tiếp ở màn QR — số dòng cài đặt thông báo không tăng | Giống TC tái hiện kịch bản gốc: đăng nhập tài khoản quản trị, có cặp thông tin kênh LINE dùng thử chưa kết nối. Bảng cài đặt thông báo chưa có dòng nào cho cặp bot sắp tạo và tài khoản đang đăng nhập. | 1. Đi hết trình hướng dẫn kết nối bot cho tới khi màn 「接続チェック」 hiện mã QR; ghi lại mã bot<br>2. Bấm 「前のステップに戻る」 rồi bấm 「次のステップへ」 — lặp đúng 3 lần liên tiếp, mỗi lần chờ màn QR hiện lại xong mới thao tác tiếp<br>3. Sau mỗi lần vào lại màn QR, ghi lại mã bot đang hiển thị trên màn<br>4. Sau lần lặp thứ ba, đếm số dòng cài đặt thông báo theo cặp mã bot và mã người dùng<br>5. Đếm thêm số bot có cùng Channel ID vừa dùng để chắc chắn không phát sinh bot rác | Cùng một cặp thông tin kênh LINE cho cả ba lần lặp; số lần lặp = 3 (biên của trục 'số lần lặp lại bước 2'). | Sau cả ba lần lặp: bảng cài đặt thông báo vẫn chỉ có ĐÚNG 1 dòng cho cặp bot và người dùng; mã bot không đổi qua các lần; số bot ứng với Channel ID đó vẫn là 1. Không lần nào hiển thị thông báo lỗi trên màn, không lần nào trả lỗi hệ thống. | Đạt |  | STAGING | haodtb | 2026-09-07 |  |  | Studio #16586 (NEW-2) · ui · auto · local-only · REQ: REQ-001 · spec: TICKET-40599, source: app/Http/Controllers/Admin/BotController.php:5584, source: public/js/admin/add_bot/add_bot_v5.js:820 · Biên ở đây là số lần lặp thao tác, không phải biên số ký tự — thay đổi không có trường nhập liệu nào bị đụng tới nên không có biên độ dài (ghi theo RULE-01). Trước fix, mỗi lần lặp thêm 1 dòng nên lần thứ ba sẽ cho 4 dòng; sau fix phải đứng yên ở 1. Cùng ràng buộc môi trường thông tin kênh LINE như TC tái hiện gốc. · author=AI · status=draft |
| TC-SYNCAPP001-01 | SYNC-APP-001 | Normal | Bot tạo sau bản sửa: một sự kiện chỉ sinh một thông báo trên app và badge chỉ tăng một | Bot được tạo/kết nối SAU khi triển khai bản sửa, và đã xác nhận ở tầng dữ liệu rằng chỉ có đúng 1 dòng cài đặt thông báo cho cặp bot và tài khoản quản trị. Kênh thông báo App đang BẬT, tần suất gửi App đặt 「リアルタイム」, loại sự kiện dùng để thử đang được tick. Bot có sẵn ít nhất 1 người dùng LINE và 1 biểu mẫu đang hoạt động. Ghi lại số badge thông báo hiện tại của bot và số mục hiện có trong danh sách thông báo của app. | 1. Đăng nhập tài khoản quản trị trên trình duyệt, chọn bot đích, mở màn 「通知設定」 và xác nhận kênh App đang bật, loại 「フォーム作成」 đang được tick cho biểu mẫu đích<br>2. Mở đường dẫn biểu mẫu đích trên trình duyệt với tư cách người dùng LINE của bot, điền các trường bắt buộc và bấm nút gửi biểu mẫu<br>3. Chờ màn xác nhận gửi biểu mẫu thành công hiển thị<br>4. Đăng nhập app điện thoại (hoặc gọi danh sách thông báo của app bằng phiên đăng nhập của cùng tài khoản) và mở danh sách thông báo của bot đích<br>5. Ghi lại số mục thông báo mới và số badge thông báo của bot<br>6. Đếm số bản ghi thông báo mới phát sinh trong bảng thông báo của bot theo cùng mã người dùng | Một lần trả lời biểu mẫu. Biến thể cần chạy thêm để phủ trục loại sự kiện mà không nhân TC: một lần kết bạn qua mã QR của bot (loại 「QRコード」). | Sau mỗi lần kích hoạt sự kiện: danh sách thông báo trên app tăng ĐÚNG 1 mục, nội dung không bị lặp lại hai lần giống hệt nhau; badge thông báo của bot tăng ĐÚNG 1; bảng thông báo phát sinh ĐÚNG 1 bản ghi mới cho mã người dùng đó. Kết quả phải giống nhau ở cả hai biến thể dữ liệu (trả lời biểu mẫu và kết bạn qua QR). | Chưa test |  | dev, local, staging |  |  |  |  | Studio #16589 (NEW-5) · ui · auto · local-only · REQ: REQ-003 · spec: TICKET-40599, SCR-NS-01, source: app/Helpers/functions.php:6941, source: app/Http/Controllers/Api/NotifyController.php:1054 · Đây là mặt triệu chứng mà khách nhìn thấy. Kỹ thuật: mọi loại sự kiện đều đi qua cùng một vòng lặp insertMobileNotify duyệt theo TỪNG dòng notify_setting của bot, nên trục 'loại sự kiện' được phủ bằng dữ liệu nhập chứ không nhân thành 15 TC. Danh sách thông báo của app lọc theo điều kiện user_id rỗng hoặc bằng người đang đăng nhập, nên 2 dòng trùng cùng người dùng sẽ hiện thành 2 mục — đó là dấu hiệu KHÔNG ĐẠT. Việc quan sát push đẩy về máy thật do TC push riêng đảm nhiệm. · author=AI · status=draft |
| TC-OUTTRUTH001-01 | OUT-TRUTH-001 | Normal | Bỏ tick một loại thông báo rồi kích hoạt đúng sự kiện đó — không phát sinh thông báo nào | Bot chỉ có đúng 1 dòng cài đặt thông báo cho cặp bot và tài khoản quản trị (đã xác nhận ở tầng dữ liệu). Kênh App đang bật, tần suất 「リアルタイム」. Bot có biểu mẫu đang hoạt động và người dùng LINE để trả lời. Ghi lại badge và số mục thông báo hiện tại. | 1. Đăng nhập tài khoản quản trị, chọn bot đích, mở màn 「通知設定」<br>2. Ở khối 「2.通知を受け取る項目を選択してください」, chọn tab 「フォーム作成」 và BỎ TICK biểu mẫu đích (và bỏ tick mục chọn tất cả nếu đang bật)<br>3. Tải lại màn 「通知設定」 và xác nhận mục vừa bỏ tick vẫn ở trạng thái chưa tick<br>4. Với tư cách người dùng LINE, mở biểu mẫu đích trên trình duyệt, điền và bấm gửi<br>5. Chờ màn xác nhận gửi thành công<br>6. Mở danh sách thông báo trên app của tài khoản quản trị, ghi lại số mục mới và badge<br>7. Đếm số bản ghi thông báo mới phát sinh cho bot trong bảng thông báo | Bỏ tick loại 「フォーム作成」 cho đúng biểu mẫu đích, giữ nguyên các loại khác. Một lần trả lời biểu mẫu sau khi đã bỏ tick. | Sau khi trả lời biểu mẫu: KHÔNG có mục thông báo mới nào xuất hiện trên app, badge KHÔNG tăng, bảng thông báo KHÔNG phát sinh bản ghi mới cho loại đó. Trạng thái bỏ tick vẫn giữ nguyên sau khi tải lại màn. Các loại thông báo khác vẫn hoạt động bình thường (kiểm chứng bằng cách kích hoạt một sự kiện thuộc loại còn tick, phải nhận đúng 1 thông báo). | Chưa test |  | dev, local, staging |  |  |  |  | Studio #16591 (NEW-7) · ui · auto · local-only · REQ: REQ-004 · spec: TICKET-40599, SCR-NS-01, source: app/Helpers/functions.php:6975 · Bước kiểm chứng loại còn tick ở cuối chính là đối chứng âm: chứng minh 'không nhận' là do bỏ tick chứ không phải do luồng thông báo hỏng. Màn cài đặt tự lưu ngay khi thay đổi, không có nút lưu riêng. · author=AI · status=draft |
| TC-TOOLNEGCTRL001-01 | TOOL-NEGCTRL-001 | Normal | Đối chứng âm: bot có quản trị viên và nhân viên — mỗi người nhận đúng một thông báo | Bot có 1 tài khoản quản trị và 1 tài khoản nhân viên đã được cấp quyền truy cập bot. Mỗi người có ĐÚNG 1 dòng cài đặt thông báo riêng cho bot này (hai dòng, khác mã người dùng) — đã xác nhận ở tầng dữ liệu. Cả hai đều bật kênh App, tần suất 「リアルタイム」 và đang tick loại sự kiện dùng để thử. Ghi lại badge của từng người. | 1. Xác nhận ở tầng dữ liệu: bot có đúng 2 dòng cài đặt thông báo, cùng mã bot nhưng khác mã người dùng<br>2. Với tư cách người dùng LINE, trả lời biểu mẫu của bot một lần<br>3. Đăng nhập app bằng tài khoản quản trị, mở danh sách thông báo của bot, ghi lại số mục mới và badge<br>4. Đăng nhập app bằng tài khoản nhân viên, mở danh sách thông báo của cùng bot, ghi lại số mục mới và badge<br>5. Đếm số bản ghi thông báo mới phát sinh cho bot và nhóm theo mã người dùng | Một lần trả lời biểu mẫu, hai người nhận (quản trị viên và nhân viên). | Bảng thông báo phát sinh 2 bản ghi mới nhưng KHÁC mã người dùng — mỗi người một bản ghi. Quản trị viên thấy đúng 1 mục mới, badge +1; nhân viên cũng thấy đúng 1 mục mới, badge +1; không ai thấy nội dung lặp hai lần. Ràng buộc duy nhất KHÔNG chặn việc tồn tại hai dòng khác người dùng và không có lỗi hệ thống nào. | Chưa test |  | dev, local, staging |  |  |  |  | Studio #16593 (NEW-9) · ui · auto · local-only · REQ: REQ-007 · spec: TICKET-40599, source: app/Helpers/functions.php:7262, source: app/Http/Controllers/Api/NotifyController.php:237 · Đối chứng âm bắt buộc: nếu chỉ kiểm 'mỗi bot 1 dòng' thì một bản sửa quá tay (khoá theo bot thay vì theo cặp bot-người dùng) vẫn qua được mọi TC khác nhưng làm nhân viên MẤT thông báo. TC này bảo vệ đúng chỗ đó, đồng thời phản bác câu '1 dòng/bot' còn sót trong tài liệu đặc tả cũ. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-REGSHARED001-01 | REG-SHARED-001 | Normal | Mở màn cài đặt thông báo lần đầu bằng tài khoản chưa có bản ghi — tạo đúng một dòng, không lỗi trùng khoá | Đã chạy migration ràng buộc duy nhất. Có một tài khoản truy cập được bot đích nhưng CHƯA từng có bản ghi cài đặt thông báo cho bot đó — phải kiểm chứng ở tầng dữ liệu, không suy từ giao diện. | 1. Ghi lại bằng chứng ở tầng dữ liệu rằng chưa tồn tại dòng cài đặt thông báo cho cặp bot đích và tài khoản này<br>2. Đăng nhập tài khoản đó trên trình duyệt, chọn bot đích và mở màn 「通知設定」<br>3. Ghi lại trạng thái hiển thị của ba kênh thông báo và các loại sự kiện trên màn<br>4. Đếm số dòng cài đặt thông báo cho cặp bot và tài khoản này<br>5. Tải lại màn 「通知設定」 thêm hai lần nữa rồi đếm lại số dòng<br>6. Mở đồng thời màn 「通知設定」 trên hai tab trình duyệt của cùng tài khoản, tải cả hai gần như cùng lúc, rồi đếm lại số dòng và rà log lỗi | Không nhập dữ liệu; chỉ mở màn và tải lại. | Màn 「通知設定」 hiển thị đầy đủ, không trắng màn và không có thông báo lỗi. Sau lần mở đầu tiên có ĐÚNG 1 dòng cài đặt thông báo được tạo cho cặp bot và tài khoản; tải lại nhiều lần vẫn giữ đúng 1 dòng. Ở bước mở hai tab đồng thời: vẫn đúng 1 dòng, và log KHÔNG ghi lỗi trùng khoá; nếu xuất hiện lỗi trùng khoá thì đánh Không đạt và báo cáo kèm dấu vết log, vì đây là rủi ro đã được nêu khi chốt phạm vi sửa. | Chưa test |  | dev, local, staging |  |  |  |  | Studio #16594 (NEW-10) · ui · auto · local-only · REQ: REQ-005 · spec: TICKET-40599, SCR-NS-01, EP-02, source: app/Http/Controllers/Basic/NotifySettingController.php:100 · Hồi quy cho điểm tạo CHƯA được sửa: NotifySettingController::getNotifySetting kiểm tra tồn tại rồi mới tạo, không nguyên tử. Bước mở hai tab là biến thể tranh chấp nhẹ theo CONC-001 (adapt) — không dựng hạ tầng nạp tải, chỉ đủ để lộ nhánh không nguyên tử. Lưu ý màn này ghi dữ liệu ngay khi chỉ mở xem, nên không được coi là thao tác chỉ đọc. · author=AI · status=draft |
| TC-REGSHARED001-02 | REG-SHARED-001 | Normal | Lưu cài đặt thông báo trên bot đã có bản ghi — cập nhật đúng, không tạo dòng thừa | Đã chạy migration ràng buộc duy nhất. Bot đích đã có đúng 1 dòng cài đặt thông báo cho tài khoản đang dùng. Ghi lại toàn bộ giá trị hiện tại của dòng đó. | 1. Đăng nhập tài khoản quản trị, chọn bot đích và mở màn 「通知設定」<br>2. Ở khối 1, tắt rồi bật lại công tắc nhận thông báo của kênh App<br>3. Đổi giá trị tần suất nhận thông báo của kênh App sang một mốc khác rồi đổi trở lại<br>4. Ở khối 2, chuyển qua ít nhất ba tab loại sự kiện khác nhau và thay đổi trạng thái tick ở mỗi tab<br>5. Tải lại màn và đối chiếu giá trị hiển thị với thao tác vừa làm<br>6. Đếm lại số dòng cài đặt thông báo cho cặp bot và tài khoản, và rà log lỗi | Bật/tắt kênh App; đổi tần suất kênh App; thay đổi trạng thái tick ở các tab 「1:1チャット」, 「友だち登録情報」 và 「フォーム作成」. | Mọi thay đổi được lưu ngay và hiển thị đúng sau khi tải lại màn. Số dòng cài đặt thông báo cho cặp bot và tài khoản vẫn là ĐÚNG 1 — không phát sinh dòng thứ hai. Không có thông báo lỗi trên màn và log không ghi lỗi trùng khoá. | Chưa test |  | dev, local, staging |  |  |  |  | Studio #16595 (NEW-11) · ui · auto · local-only · REQ: REQ-005 · spec: TICKET-40599, SCR-NS-01, EP-07, EP-08, source: app/Http/Controllers/Basic/NotifySettingController.php:343 · Hồi quy điểm lưu cài đặt (nhánh tạo mới bên trong luồng lưu vẫn dùng kiểu kiểm tra rồi tạo). Đây cũng là smoke tối thiểu cho màn cài đặt sau khi thêm ràng buộc dữ liệu, theo phần 'vùng ảnh hưởng do dev liệt kê' của quy tắc phạm vi hồi quy. · author=AI · status=draft |
| TC-TOOLOLDREC001-01 | TOOL-OLDREC-001 | Abnormal | Dữ liệu trùng có từ trước bản sửa: một sự kiện vẫn sinh hai thông báo cho tới khi dọn dữ liệu | Trên môi trường CHƯA chạy migration ràng buộc duy nhất (hoặc đã tạm gỡ ràng buộc), dựng sẵn cho một bot thử nghiệm HAI dòng cài đặt thông báo trùng cùng cặp bot và tài khoản quản trị — mô phỏng đúng dữ liệu đã sinh trước bản sửa. Cả hai dòng đều bật kênh App, tần suất 「リアルタイム」, tick loại sự kiện dùng để thử. Ghi lại badge và số mục thông báo trước khi bắt đầu. | 1. Xác nhận ở tầng dữ liệu rằng bot thử nghiệm đang có đúng 2 dòng cài đặt thông báo cùng cặp bot và người dùng<br>2. Kích hoạt một sự kiện sinh thông báo cho bot đó bằng thao tác thật trên màn hình (người dùng LINE trả lời biểu mẫu của bot)<br>3. Mở danh sách thông báo trên app của tài khoản quản trị và ghi lại số mục mới cùng badge<br>4. Dọn dữ liệu trùng: xoá bản ghi cài đặt thông báo có mã lớn hơn, giữ lại bản ghi có mã nhỏ nhất<br>5. Kích hoạt lại đúng loại sự kiện đó một lần nữa<br>6. Mở lại danh sách thông báo trên app và ghi lại số mục mới cùng badge | Hai dòng cài đặt thông báo trùng cùng cặp bot và người dùng (mô phỏng dữ liệu ngày 2026-09-04 của bot khách). Hai lần trả lời biểu mẫu: lần một khi còn trùng, lần hai sau khi dọn. | Lần kích hoạt thứ nhất (khi dữ liệu còn trùng): app hiển thị 2 mục thông báo giống hệt nhau, badge tăng 2 — tái hiện đúng triệu chứng khách báo và chứng minh bản sửa KHÔNG tự dọn dữ liệu cũ. Lần kích hoạt thứ hai (sau khi dọn còn 1 dòng): app chỉ hiển thị thêm 1 mục, badge tăng 1. Nếu lần thứ nhất chỉ hiện 1 mục thì tiền điều kiện dựng chưa đúng — phải kiểm lại dữ liệu, KHÔNG được kết luận đạt. | Chưa test |  | dev, local |  |  |  |  | Studio #16590 (NEW-6) · ui · auto · local-only · REQ: REQ-008, REQ-003 · spec: TICKET-40599, redmine: journal 2026-09-07 07:13 bot 218134, source: app/Helpers/functions.php:6922, source: app/Helpers/functions.php:7318 · TC trục 'thời điểm bản ghi' theo TOOL-OLDREC-001: bản ghi cũ ghi trước fix giữ nguyên hình dạng lỗi. Cần chạy trên môi trường chưa có ràng buộc duy nhất vì chính ràng buộc đó ngăn việc dựng tiền điều kiện — ghi rõ trong kết quả rằng đã tạm gỡ hoặc chạy trước migration. Đây cũng là bằng chứng để vận hành hiểu vì sao bắt buộc phải dedup dữ liệu thật của khách. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-OUTTRUTH001-02 | OUT-TRUTH-001 | Abnormal | Bỏ tick khi tài khoản còn dòng cài đặt trùng cũ — vẫn nhận thông báo do bản ghi ma | Trên môi trường chưa có ràng buộc duy nhất (hoặc đã tạm gỡ), dựng sẵn HAI dòng cài đặt thông báo trùng cùng cặp bot và tài khoản quản trị, cả hai đều bật kênh App và đều đang tick loại sự kiện dùng để thử. Bot có biểu mẫu hoạt động và người dùng LINE. | 1. Xác nhận ở tầng dữ liệu rằng bot đang có 2 dòng cài đặt thông báo trùng cặp bot và người dùng<br>2. Đăng nhập tài khoản quản trị, mở màn 「通知設定」, vào tab 「フォーム作成」 và bỏ tick biểu mẫu đích<br>3. Kiểm ở tầng dữ liệu xem thao tác bỏ tick đã ghi vào dòng nào — ghi lại giá trị của CẢ HAI dòng<br>4. Với tư cách người dùng LINE, trả lời biểu mẫu đích<br>5. Mở danh sách thông báo trên app và ghi lại số mục mới, badge<br>6. Dọn dữ liệu trùng, chỉ giữ dòng có mã nhỏ nhất, rồi bỏ tick lại nếu cần và trả lời biểu mẫu thêm một lần<br>7. Ghi lại số mục thông báo mới sau khi đã dọn | Hai dòng cài đặt trùng, chỉ một dòng bị bỏ tick qua giao diện; một lần trả lời biểu mẫu trước khi dọn và một lần sau khi dọn. | Trước khi dọn: thao tác bỏ tick trên giao diện chỉ có hiệu lực với một trong hai dòng, nên khi trả lời biểu mẫu vẫn phát sinh thông báo từ dòng còn lại — giao diện nói 'đã tắt' nhưng thực tế vẫn nhận. Đây là bằng chứng cho thấy dữ liệu trùng cũ bắt buộc phải dọn thủ công. Sau khi dọn còn 1 dòng: bỏ tick có hiệu lực thật, không phát sinh thông báo nào và badge không tăng. Ghi rõ số mục nhận được ở từng giai đoạn trong kết quả. | Chưa test |  | dev, local |  |  |  |  | Studio #16592 (NEW-8) · ui · auto · local-only · REQ: REQ-004, REQ-008 · spec: TICKET-40599, SCR-NS-01, source: app/Http/Controllers/Basic/NotifySettingController.php:100 · Đây là mảnh còn thiếu trong yêu cầu re-test của human ('bỏ tick không còn nhận thông báo — hết bản ghi ma'): sau bản sửa, bot MỚI không còn bản ghi ma, nhưng bot CŨ đang trùng thì vẫn còn. TC làm rõ ranh giới đó để leader không hiểu nhầm là fix tự chữa dữ liệu cũ. Cần môi trường chưa áp ràng buộc duy nhất để dựng được tiền điều kiện. · author=AI · status=draft |
| TC-REGSHARED001-03 | REG-SHARED-001 | Normal | Nhân viên nhận lời mời cho bot mới — tạo đúng một dòng cài đặt thông báo, không lỗi | Đã chạy migration ràng buộc duy nhất. Có tài khoản quản trị sở hữu bot đích và một tài khoản nhân viên CHƯA có bất kỳ dòng cài đặt thông báo nào cho bot đó — đã kiểm chứng ở tầng dữ liệu. Gói dịch vụ của bot còn đủ chỗ để thêm nhân viên. | 1. Đăng nhập tài khoản quản trị, mở màn quản lý nhân viên và gửi lời mời cho tài khoản nhân viên với quyền truy cập bot đích<br>2. Đăng nhập tài khoản nhân viên trên trình duyệt và chấp nhận lời mời từ màn hiển thị lời mời<br>3. Ghi lại thông báo hiển thị trên màn sau khi chấp nhận<br>4. Đếm số dòng cài đặt thông báo cho cặp bot đích và tài khoản nhân viên<br>5. Đăng nhập tài khoản nhân viên, chọn bot đích và mở màn 「通知設定」 để xác nhận hiển thị được<br>6. Rà log ứng dụng tìm lỗi trùng khoá | Một lời mời nhân viên cho bot đích, quyền mặc định. | Lời mời được chấp nhận thành công, màn hiển thị kết quả thành công chứ không phải lỗi hệ thống. Có ĐÚNG 1 dòng cài đặt thông báo được tạo cho cặp bot và nhân viên. Nhân viên mở được màn 「通知設定」 bình thường và không phát sinh dòng thứ hai. Log không ghi lỗi trùng khoá. | Chưa test |  | dev, local, staging |  |  |  |  | Studio #16601 (NEW-17) · ui · auto · local-only · REQ: REQ-005 · spec: TICKET-40599, source: app/Http/Controllers/Admin/StaffManagementController.php:559 · Luồng thuận của điểm tạo CHƯA sửa trong quản lý nhân viên. Cặp với TC bất thường ngay sau đây để phủ cả hai nhánh của điểm tạo này. · author=AI · status=draft |
| TC-REGSHARED001-04 | REG-SHARED-001 | Abnormal | Nhân viên nhận lời mời khi đã tồn tại dòng cài đặt cũ lệch chủ sở hữu — rà lỗi trùng khoá | Đã chạy migration ràng buộc duy nhất. Dựng sẵn ở tầng dữ liệu: tài khoản nhân viên ĐÃ có một dòng cài đặt thông báo cho bot đích, nhưng giá trị mã quản trị viên ghi trên dòng đó KHÁC với mã quản trị viên của hợp đồng bot mà luồng mời sẽ dùng. Nếu môi trường không dựng được chênh lệch này thì ghi rõ và trả kết quả bỏ qua kèm lý do. | 1. Ghi lại dòng cài đặt thông báo hiện có của nhân viên cho bot đích, gồm mã người dùng và mã quản trị viên<br>2. Ghi lại mã quản trị viên trên hợp đồng của bot đích và xác nhận hai giá trị khác nhau<br>3. Đăng nhập tài khoản quản trị, mở màn quản lý nhân viên và gửi lời mời cho nhân viên đó với quyền truy cập bot đích<br>4. Đăng nhập tài khoản nhân viên và chấp nhận lời mời trên màn hiển thị lời mời<br>5. Ghi lại thông báo hiển thị trên màn: thành công hay lỗi hệ thống<br>6. Đếm số dòng cài đặt thông báo cho cặp bot và nhân viên, và rà log ứng dụng tìm ngoại lệ trùng khoá | Một dòng cài đặt thông báo đã tồn tại cho cặp bot và nhân viên, mang mã quản trị viên khác với mã quản trị viên của hợp đồng bot. | Kỳ vọng mong muốn: luồng chấp nhận lời mời hoàn tất bình thường, giữ nguyên 1 dòng cài đặt thông báo, không có lỗi hệ thống. Nếu thực tế màn báo lỗi hoặc log ghi ngoại lệ trùng khoá thì đánh Không đạt và báo cáo kèm dấu vết — đây chính là rủi ro đã được nêu và chấp nhận khi chốt phạm vi sửa, cần leader quyết định mở ticket riêng cho điểm tạo này. | Chưa test |  | dev, local |  |  |  |  | Studio #16602 (NEW-18) · ui · auto · local-only · REQ: REQ-005 · spec: TICKET-40599, source: app/Http/Controllers/Admin/StaffManagementController.php:559, source: app/Http/Controllers/Admin/StaffManagementController.php:448, redmine: journal 2026-09-07 11:00 mục Tự review rủi ro · Điểm tạo trong quản lý nhân viên kiểm tra tồn tại theo BỘ BA mã người dùng, mã quản trị viên và mã bot, trong khi ràng buộc mới chỉ theo cặp mã bot và mã người dùng — nên khi mã quản trị viên lệch, điều kiện kiểm tra trả về không tồn tại và lệnh chèn sẽ vi phạm ràng buộc. TC này cố tình dựng đúng khe hở đó. Nếu không dựng được tiền điều kiện thì trả bỏ qua với lý do cụ thể, KHÔNG dùng việc đọc mã nguồn làm bằng chứng thay thế. · author=AI · status=draft |
| TC-SYNCAPP001-02 | SYNC-APP-001 | Normal | Push notification thật trên thiết bị — một sự kiện chỉ được đẩy đúng một lần | Bot được tạo hoặc kết nối sau khi triển khai bản sửa. Đã kiểm tra DB và xác nhận cặp (bot_id, user_id) của tài khoản nhận thông báo chỉ có đúng 1 dòng trong notify_setting. Trên màn 「通知設定」, kênh App đang BẬT, tần suất gửi là 「リアルタイム」 và loại sự kiện dùng để test đang được tick. Thiết bị thật đã đăng nhập app bằng đúng tài khoản này và đã cấp quyền nhận push notification. Ghi lại badge hiện tại và số thông báo hiện có trước khi test. | 1. Đưa app về background hoặc đóng app để có thể quan sát push notification thật từ hệ điều hành<br>2. Từ một tài khoản/người dùng khác, thực hiện đúng 1 hành động phát sinh thông báo cho bot, ví dụ trả lời 1 form đang được bật notify<br>3. Không phát sinh thêm bất kỳ event notify nào khác trong khoảng thời gian quan sát<br>4. Quan sát Notification Center của thiết bị và đếm số push nhận được cho event vừa tạo<br>5. Mở app, vào danh sách thông báo của bot và đếm số mục mới<br>6. Ghi lại badge sau khi nhận thông báo<br>7. Đối chiếu DB, đếm số bản ghi mobile_notify mới tương ứng cùng bot, user nhận và event vừa kích hoạt | 1 event notify hợp lệ, ưu tiên dùng trả lời form hoặc kết bạn qua QR. Chỉ phát sinh đúng 1 lần trong lần chạy này. | Thiết bị nhận ĐÚNG 1 push notification cho event vừa phát sinh, không xuất hiện 2 push có cùng nội dung. Trong app chỉ có ĐÚNG 1 mục thông báo mới; badge tăng ĐÚNG 1; DB chỉ có ĐÚNG 1 bản ghi mobile_notify mới cho user nhận tương ứng. Nội dung push và nội dung trong app phải cùng sự kiện, không có bản ghi trùng cùng nội dung/thời điểm. | Chưa test |  | staging |  |  |  |  | Studio #16604 (NEW-20) · ui · manual · env-safe · spec: TICKET-40599, REQ-003, SYNC-APP-001, redmine: journal 2026-09-07 09:27 · Case này kiểm trực tiếp triệu chứng khách nhìn thấy ở tầng push thật. Không thay bằng việc chỉ kiểm API/DB. Cần thiết bị thật vì runner local không chứng minh được hệ điều hành chỉ nhận một push. · author=haodtb@mcp · status=draft |
| TC-SYNCAPP001-03 | SYNC-APP-001 | Normal | Regression nhiều loại sự kiện thông báo — mỗi event chỉ sinh một notify cho mỗi người nhận | Bot được tạo/kết nối sau bản sửa và cặp (bot_id,user_id) của tài khoản nhận có đúng 1 dòng notify_setting. Kênh App đang BẬT, tần suất 「リアルタイム」. Các loại event được test đều đã được bật/tick trong 「通知設定」 và đã chuẩn bị dữ liệu cần thiết để kích hoạt từng event. Tài khoản nhận đã đăng nhập app. Trước mỗi biến thể phải ghi lại số notification trên app, badge và số bản ghi mobile_notify tương ứng để làm baseline. | 1. Chuẩn bị dataset các loại event cần regression theo data_input bên dưới<br>2. Với từng event, trước khi kích hoạt hãy ghi baseline: số mục notification trên app, badge và số bản ghi mobile_notify của user nhận<br>3. Chỉ kích hoạt ĐÚNG 1 event tại một thời điểm bằng flow thật của chức năng tương ứng<br>4. Chờ event được xử lý xong rồi mở app và kiểm tra số mục notification mới<br>5. Kiểm tra badge của tài khoản nhận sau event<br>6. Query DB và đếm số mobile_notify mới sinh ra cho đúng event và đúng user nhận<br>7. Xác nhận không có hai dòng mới có cùng nội dung/thời điểm cho cùng một event<br>8. Đưa số liệu về baseline phù hợp hoặc ghi baseline mới rồi tiếp tục với event kế tiếp cho tới hết dataset | Chạy lần lượt tối thiểu các event sau: (1) Chat 1:1 — có tin nhắn mới cần notify; (2) Gửi tin hàng loạt/Broadcast — phát sinh event notify theo cấu hình; (3) Trả lời Form; (4) Landing QR / QR liên quan phát sinh notify; (5) Đặt lịch/Booking; (6) Mua item; (7) Schedule — sự kiện lịch/schedule thuộc notify setting; (8) ASP — event ASP được hỗ trợ; (9) Conversion — event conversion; (10) các event notify khác đang hiển thị trên màn 「通知設定」 nếu có và có thể dựng được dữ liệu. Mỗi event chỉ kích hoạt 1 lần trong một lượt đo. | Với TỪNG event trong dataset: app chỉ tăng ĐÚNG 1 mục notification cho mỗi người nhận hợp lệ; badge chỉ tăng ĐÚNG 1; DB chỉ phát sinh ĐÚNG 1 mobile_notify mới cho cùng user/event; không có hai notification giống nhau cùng nội dung do duplicate notify_setting. Event này không được làm phát sinh notify trùng cho event khác. Nếu một event không thể dựng trên môi trường test thì ghi SKIP riêng event đó kèm lý do, không được coi toàn bộ testcase là pass nếu chưa có bằng chứng cho các event đã chạy. | Chưa test |  | staging |  |  |  |  | Studio #16606 (NEW-22) · ui · manual · env-safe · spec: TICKET-40599, REQ-003, SYNC-APP-001, source: app/Helpers/functions.php:6941 · Đây là 1 testcase regression dạng dataset, không tách 1 testcase/event vì root cause chung là vòng lặp theo notify_setting. Kết quả phải ghi riêng từng event để biết event nào đã pass/skip/fail. Nếu phát hiện event chạy qua code path khác hoặc job riêng, cần tách thành testcase riêng ở vòng sau. · author=haodtb@mcp · status=draft |
| TC-CONC001-01 | CONC-001 | Abnormal | Gọi lặp endpoint bước 2 của luồng kết nối bot với cùng thông tin kênh — không sinh dòng cài đặt thứ hai | Có phiên đăng nhập hợp lệ của tài khoản quản trị kèm mã chống giả mạo biểu mẫu. Có cặp thông tin kênh LINE dùng thử chưa kết nối. Bảng cài đặt thông báo chưa có dòng nào cho cặp bot sắp tạo và người dùng này. | 1. Gọi endpoint bước 2 của luồng kết nối bot lần thứ nhất với đầy đủ thông tin kênh Messaging API và kênh LINE Login; ghi lại mã bot trả về<br>2. Gọi lại chính endpoint đó lần thứ hai với y hệt bộ tham số, không đổi bất kỳ trường nào<br>3. Ghi lại mã trạng thái HTTP và nội dung phản hồi của cả hai lần gọi<br>4. Đếm số dòng cài đặt thông báo theo cặp mã bot và mã người dùng<br>5. Rà log ứng dụng tìm ngoại lệ về trùng khoá | Bộ tham số giống hệt nhau ở cả hai lần gọi: Channel ID / Channel secret của kênh Messaging API và của kênh LINE Login, danh sách người quản lý để trống. | Cả hai lần gọi đều trả về thành công và cùng một mã bot. Bảng cài đặt thông báo chỉ có ĐÚNG 1 dòng cho cặp bot và người dùng. Lần gọi thứ hai KHÔNG trả lỗi hệ thống và log KHÔNG ghi ngoại lệ trùng khoá — nghĩa là điểm ghi đã tự bỏ qua khi bản ghi đã tồn tại chứ không phải bị ràng buộc dữ liệu chặn lại. | Chưa test |  | STAGING | haodtb | 2026-09-07 |  |  | Studio #16587 (NEW-3) · api · auto · local-only · REQ: REQ-001 · spec: TICKET-40599, source: routes/web.php:1883, source: app/Http/Controllers/Admin/BotController.php:5600 · TC nhóm api hợp lệ vì đối tượng kiểm chứng chính là hợp đồng của endpoint bước 2 (gọi lặp phải không sinh dòng thứ hai), KHÔNG dùng để thay coverage cho TC màn hình ở trên. Phân biệt hai kết quả rất khác nhau: chèn bị ràng buộc duy nhất chặn (lỗi trùng khoá) là KHÔNG ĐẠT; bỏ qua êm nhờ khoá theo (bot_id,user_id) mới là ĐẠT. · author=AI · status=draft |
| TC-JOB002-01 | JOB-002 | Normal | Chạy lệnh khôi phục badge sau khi dọn dữ liệu trùng — badge khớp số thông báo chưa đọc thật | Có một bot thử nghiệm từng bị dữ liệu trùng: đã phát sinh sẵn các bản ghi thông báo nhân đôi và badge đã cộng dư, sau đó dữ liệu cài đặt trùng đã được dọn còn 1 dòng. Ghi lại giá trị badge hiện tại và số bản ghi thông báo chưa đọc thật của bot. | 1. Ghi lại giá trị badge hiện tại của bot thử nghiệm và số bản ghi thông báo chưa đọc thật sự tương ứng<br>2. Xác nhận hai con số đang lệch nhau do dữ liệu trùng phát sinh trước khi dọn<br>3. Chạy lệnh khôi phục badge và thông báo<br>4. Đọc toàn bộ log của lệnh, ghi lại số bot đã xử lý và các dòng lỗi nếu có<br>5. Đọc lại giá trị badge của bot thử nghiệm và đối chiếu với số bản ghi thông báo chưa đọc<br>6. Mở danh sách thông báo trên app của tài khoản quản trị để đối chiếu con số hiển thị | Bot thử nghiệm có badge dư so với số thông báo chưa đọc thật (do nhân đôi trước khi dọn). | Lệnh chạy tới cùng, ghi log bắt đầu và kết thúc kèm tổng số bot đã xử lý, không có dòng lỗi. Sau khi chạy, giá trị badge của bot KHỚP với số bản ghi thông báo chưa đọc thật và khớp với con số hiển thị trên app. Các bot khác không nằm trong diện lỗi không bị thay đổi badge — kiểm bằng cách ghi lại badge của một bot đối chứng trước và sau khi chạy. | Chưa test |  | dev, local |  |  |  |  | Studio #16603 (NEW-19) · job · auto · local-only · REQ: REQ-008 · spec: TICKET-40599, source: app/Console/Commands/RecoverMobileBadgeNotify.php:25, redmine: journal 2026-09-07 11:00 mục 5 · Bản sửa không tự nắn badge đã cộng dư; đây là bước khôi phục dữ liệu do vận hành chạy tay, được nêu trong báo cáo sửa lỗi. Bước ghi badge của bot đối chứng là đối chứng âm để chắc lệnh không quét rộng ngoài phạm vi cần sửa. Chỉ áp dụng nhánh chạy thành công theo quan điểm về job — không dựng nhánh thử lại và nhánh thất bại cuối cùng vì bản sửa không chạm logic gửi của job nào. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-DATAMIG001-01 | DATA-MIG-001 | Normal | Dọn dữ liệu notify_setting trùng — giữ nguyên cấu hình ChatWork trước khi chạy migration | Trên DB test chưa áp dụng unique index hoặc đã chuẩn bị dữ liệu trước migration. Có đúng 2 dòng notify_setting cùng một cặp (bot_id, user_id): dòng có id nhỏ hơn là dòng sẽ giữ lại và đang để trống cấu hình ChatWork; dòng có id lớn hơn có giá trị ChatWork hợp lệ ở notification_room_url và api_token. Đã sao lưu dữ liệu test trước khi thao tác. | 1. Query và ghi lại đầy đủ 2 dòng notify_setting trùng, gồm id, bot_id, user_id, notification_room_url, api_token và các field notify quan trọng<br>2. Thực hiện đúng quy trình dedup vận hành: nếu dòng id nhỏ đang trống cấu hình ChatWork thì chuyển notification_room_url và api_token từ dòng id lớn sang dòng id nhỏ<br>3. Xóa dòng trùng có id lớn hơn, chỉ giữ lại dòng id nhỏ<br>4. Query lại theo cặp (bot_id, user_id) và đối chiếu các field của dòng còn lại<br>5. Chạy truy vấn thống kê duplicate theo bot_id,user_id với user_id khác NULL<br>6. Chạy migration tạo unique index trên cặp (bot_id,user_id)<br>7. Mở màn 「通知設定」/ChatWork liên quan của bot để xác nhận cấu hình vẫn sử dụng được nếu môi trường hỗ trợ | 2 dòng trùng cùng (bot_id,user_id): record giữ lại thiếu ChatWork config; record bị xóa có notification_room_url và api_token hợp lệ. | Sau dedup chỉ còn ĐÚNG 1 dòng cho cặp (bot_id,user_id), giữ id nhỏ nhất. Các giá trị ChatWork cần thiết từ dòng bị xóa đã được bảo toàn trên dòng giữ lại, không bị mất hoặc reset. Query duplicate trả về 0 cho cặp này và migration unique chạy thành công. Các field notify khác của dòng giữ lại không bị thay đổi ngoài phần dữ liệu được chủ động hợp nhất. | Chưa test |  | dev, local |  |  |  |  | Studio #16605 (NEW-21) · data · auto · local-only · spec: TICKET-40599, REQ-006, REQ-008, redmine: journal 2026-09-07 11:00 mục 5 · Báo cáo fix yêu cầu chuyển cấu hình ChatWork trước khi xóa record thừa. Nếu bỏ bước này có thể hết duplicate notification nhưng làm mất cấu hình ChatWork của khách. · author=haodtb@mcp · status=draft |
| TC-REGSHARED001-05 | REG-SHARED-001 | Normal | Kiểm tra tĩnh mã nguồn: điểm tạo cài đặt thông báo trong luồng kết nối bot khoá theo cặp bot và người dùng | Có mã nguồn của nhánh sửa lỗi trên máy chạy kiểm thử. | 1. Mở tệp điều khiển luồng quản trị bot, tìm điểm tạo cài đặt thông báo trong hàm xử lý bước 2 của luồng kết nối<br>2. Xác nhận điểm này tạo bản ghi theo kiểu 'tìm trước, chỉ tạo khi chưa có', với khoá tìm gồm mã bot và mã người dùng<br>3. Mở tệp migration mới, xác nhận nó tạo ràng buộc duy nhất trên cặp mã bot và mã người dùng<br>4. Rà toàn bộ mã nguồn liệt kê mọi điểm còn tạo bản ghi cài đặt thông báo và ghi lại danh sách kèm vị trí | Không có dữ liệu nhập. | Điểm tạo trong hàm xử lý bước 2 khoá đúng theo cặp mã bot và mã người dùng (không còn tạo thẳng). Migration tạo đúng một ràng buộc duy nhất trên cặp đó. Danh sách các điểm tạo còn lại được ghi ra đầy đủ, tối thiểu gồm: điểm nạp dữ liệu màn cài đặt thông báo, điểm lưu cài đặt bản cũ, và điểm tạo mặc định khi nhân viên nhận lời mời — dùng làm đầu vào cho các test hồi quy. | Chưa test |  | all |  |  |  |  | Studio #16588 (NEW-4) · data · auto · read-only · REQ: REQ-001, REQ-002 · spec: TICKET-40599, source: app/Http/Controllers/Admin/BotController.php:5600, source: database/migrations/2026_09_07_000000_dedupe_and_unique_notify_setting_bot_user.php · TC BỔ TRỢ, dùng để pipeline local vẫn có thứ chạy được khi môi trường thiếu thông tin kênh LINE, và để lập danh sách điểm tạo dùng chung. KHÔNG được dùng kết quả của TC này để kết luận TC tái hiện trên màn hình đã đạt — hai TC kiểm chứng hai thứ khác nhau. · author=AI · status=draft |
| TC-DATADB001-01 | DATA-DB-001 | Normal | Sau khi chạy migration: bảng cài đặt thông báo từ chối cặp bot và người dùng trùng | Cơ sở dữ liệu thử nghiệm đã được dọn hết cặp trùng và đã chạy xong migration thêm ràng buộc duy nhất. Có sẵn một bot thử nghiệm và một tài khoản người dùng dùng riêng cho test này. | 1. Liệt kê các chỉ mục hiện có của bảng cài đặt thông báo và ghi lại chỉ mục duy nhất trên cặp mã bot và mã người dùng<br>2. Chèn một dòng cài đặt thông báo cho cặp bot và người dùng thử nghiệm — ghi lại kết quả<br>3. Chèn tiếp một dòng thứ hai cho ĐÚNG cặp bot và người dùng đó — ghi lại kết quả và nội dung lỗi<br>4. Đếm lại số dòng của cặp đó<br>5. Dọn dữ liệu thử nghiệm vừa tạo | Hai lần chèn cho cùng một cặp mã bot và mã người dùng, các cột còn lại để giá trị mặc định. | Chỉ mục duy nhất trên cặp mã bot và mã người dùng tồn tại và đúng tên. Lần chèn thứ nhất thành công. Lần chèn thứ hai bị từ chối với lỗi trùng khoá, và số dòng của cặp đó vẫn là ĐÚNG 1. | Chưa test |  | dev, local |  |  |  |  | Studio #16596 (NEW-12) · data · auto · local-only · REQ: REQ-002 · spec: TICKET-40599, source: database/migrations/2026_09_07_000000_dedupe_and_unique_notify_setting_bot_user.php · Đây là hàng rào cuối cùng: kể cả một điểm ghi nào đó bị bỏ sót thì dữ liệu cũng không thể trùng nữa. Kỹ thuật: chỉ mục cần kiểm là notify_setting_bot_id_user_id_unique. · author=AI · status=draft |
| TC-DATAMIG001-02 | DATA-MIG-001 | Normal | Quay lui migration — ràng buộc duy nhất được gỡ và dữ liệu giữ nguyên | Cơ sở dữ liệu đã chạy thành công migration ràng buộc duy nhất. Ghi lại tổng số dòng của bảng cài đặt thông báo trước khi bắt đầu. | 1. Ghi lại danh sách chỉ mục và tổng số dòng của bảng cài đặt thông báo<br>2. Chạy lệnh quay lui một bước cập nhật cấu trúc cơ sở dữ liệu<br>3. Kiểm tra lại danh sách chỉ mục của bảng<br>4. Kiểm tra lại tổng số dòng của bảng<br>5. Chạy lại lệnh cập nhật cấu trúc để đưa về trạng thái đã có ràng buộc | Không có dữ liệu nhập. | Sau khi quay lui: ràng buộc duy nhất trên cặp mã bot và mã người dùng biến mất khỏi danh sách chỉ mục, tổng số dòng của bảng KHÔNG đổi, không bản ghi nào bị xoá. Chạy lại lệnh cập nhật cấu trúc thì ràng buộc được tạo lại bình thường. | Chưa test |  | dev, local |  |  |  |  | Studio #16599 (NEW-15) · data · auto · local-only · REQ: REQ-006 · spec: TICKET-40599, source: database/migrations/2026_09_07_000000_dedupe_and_unique_notify_setting_bot_user.php · Bảo đảm đường lui an toàn khi cần gỡ bản sửa ở môi trường thật. Sau khi chạy TC này phải trả cơ sở dữ liệu về trạng thái đã có ràng buộc để không ảnh hưởng các TC khác. · author=AI · status=draft |
| TC-RULE04-01 | RULE-04 | Normal | Thống kê phạm vi dữ liệu trùng trên môi trường kiểm thử trước khi đóng ticket | Môi trường kiểm thử đã triển khai bản sửa và đã chạy migration. Có quyền chạy truy vấn đọc trên cơ sở dữ liệu của môi trường đó. | 1. Chạy truy vấn đếm số cặp mã bot và mã người dùng đang có nhiều hơn một dòng trong bảng cài đặt thông báo<br>2. Chạy truy vấn liệt kê các mã bot còn nằm trong danh sách trùng (nếu có) kèm số dòng của từng cặp<br>3. Chạy truy vấn đếm riêng số dòng có mã người dùng để trống, nhóm theo mã bot<br>4. Ghi toàn bộ kết quả vào báo cáo kiểm thử | Không có dữ liệu nhập; chỉ truy vấn đọc. | Số cặp mã bot và mã người dùng trùng = 0. Nếu khác 0 thì phải liệt kê rõ các mã bot còn trùng và coi đây là điều kiện chặn việc đóng ticket cho tới khi dọn xong. Số dòng có mã người dùng để trống được ghi lại làm số liệu tham chiếu cho việc theo dõi dữ liệu di sản, không phải điều kiện chặn. | Chưa test |  | dev, local, staging |  |  |  |  | Studio #16600 (NEW-16) · data · auto · read-only · REQ: REQ-008 · spec: TICKET-40599, redmine: journal 2026-09-07 11:00 mục 5 · Chỉ đọc, an toàn chạy trên môi trường dùng chung. Bug làm sai dữ liệu nên cần biết phạm vi ảnh hưởng trước khi kết luận, thay vì chỉ kiểm một bot mẫu. Không đưa môi trường thật của khách vào phạm vi TC này — việc thống kê và dọn dữ liệu thật là thao tác vận hành riêng. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-DATAMIG001-03 | DATA-MIG-001 | Abnormal | Chạy migration khi dữ liệu còn cặp trùng — migration dừng lại, chỉ chạy được sau khi dedup | Cơ sở dữ liệu thử nghiệm CHƯA chạy migration ràng buộc duy nhất và đang tồn tại ít nhất một cặp (bot, người dùng) có 2 dòng — nếu không có sẵn thì tự dựng. Đã sao lưu hoặc chấp nhận cơ sở dữ liệu thử nghiệm bị thay đổi. | 1. Chạy truy vấn thống kê liệt kê các cặp mã bot và mã người dùng đang có nhiều hơn một dòng; ghi lại kết quả<br>2. Chạy lệnh cập nhật cấu trúc cơ sở dữ liệu và ghi lại toàn bộ thông báo trả về<br>3. Kiểm tra danh sách chỉ mục của bảng để xác nhận ràng buộc duy nhất có được tạo hay không<br>4. Dọn dữ liệu trùng theo nguyên tắc giữ bản ghi có mã nhỏ nhất cho mỗi cặp, rồi chạy lại truy vấn thống kê để xác nhận trả về rỗng<br>5. Chạy lại lệnh cập nhật cấu trúc cơ sở dữ liệu<br>6. Kiểm tra lại danh sách chỉ mục và tổng số dòng còn lại của bảng | Ít nhất một cặp mã bot và mã người dùng có 2 dòng trước khi chạy lần đầu. | Lần chạy thứ nhất: lệnh cập nhật cấu trúc DỪNG với lỗi trùng khoá và ràng buộc duy nhất KHÔNG được tạo — đúng như cảnh báo trong ghi chú của migration. Sau khi dedup: truy vấn thống kê trả về rỗng, lần chạy thứ hai thành công và ràng buộc duy nhất xuất hiện trong danh sách chỉ mục. Số dòng còn lại đúng bằng số dòng trước khi dedup trừ đi số bản ghi trùng đã xoá; không mất bản ghi nào ngoài các bản trùng. | Chưa test |  | dev, local |  |  |  |  | Studio #16598 (NEW-14) · data · auto · local-only · REQ: REQ-006 · spec: TICKET-40599, source: database/migrations/2026_09_07_000000_dedupe_and_unique_notify_setting_bot_user.php:19, redmine: journal 2026-09-07 11:00 mục 5 · Biến rủi ro triển khai thành TC: migration KHÔNG tự dọn dữ liệu, nên đây là bằng chứng để vận hành bắt buộc dedup trước khi deploy. Truy vấn thống kê tham chiếu ghi trong migration: nhóm theo bot_id và user_id với điều kiện user_id khác rỗng, lọc nhóm có số lượng lớn hơn 1. Lưu ý cơ sở dữ liệu của môi trường chạy thử là loại dùng lại giữa các lần chạy nên có thể đã tồn tại cặp trùng từ trước. · author=AI · status=draft |
| TC-RULE09-01 | RULE-09 | Boundary | Nhiều dòng cài đặt cũ chưa gắn người dùng của cùng một bot vẫn tồn tại được sau khi có ràng buộc | Cơ sở dữ liệu đã chạy xong migration ràng buộc duy nhất. Có một bot thử nghiệm chưa có dòng cài đặt nào. | 1. Chèn một dòng cài đặt thông báo cho bot thử nghiệm với trường mã người dùng để TRỐNG (dữ liệu kiểu cũ, trước đợt tách theo người dùng)<br>2. Chèn tiếp một dòng thứ hai cho cùng bot đó, cũng để trống mã người dùng<br>3. Chèn thêm một dòng thứ ba cho cùng bot nhưng có mã người dùng cụ thể<br>4. Đếm tổng số dòng của bot thử nghiệm và nhóm theo mã người dùng<br>5. Dọn dữ liệu thử nghiệm vừa tạo | Hai dòng có mã người dùng để trống + một dòng có mã người dùng cụ thể, tất cả cùng một mã bot. | Cả ba lần chèn đều thành công: ràng buộc duy nhất KHÔNG áp dụng cho các dòng có mã người dùng để trống (nhiều giá trị trống được coi là khác nhau), nên dữ liệu cũ chưa gắn người dùng không bị chặn hay bị mất. Bot thử nghiệm có 3 dòng, trong đó 2 dòng trống mã người dùng và 1 dòng có mã người dùng. | Chưa test |  | dev, local |  |  |  |  | Studio #16597 (NEW-13) · data · auto · local-only · REQ: REQ-002 · spec: TICKET-40599, source: database/migrations/2026_09_07_000000_dedupe_and_unique_notify_setting_bot_user.php · Biên của ràng buộc dữ liệu, đúng nhánh 'dữ liệu cũ song song với dữ liệu mới'. Đây vừa là hành vi mong đợi (không phá dữ liệu di sản) vừa là giới hạn cần leader biết: các dòng trống mã người dùng vẫn có thể trùng nhau và vẫn được vòng lặp sinh thông báo duyệt qua. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
