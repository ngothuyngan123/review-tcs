# 05 — Review Report

> Draft cho Leader verify.

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | `(1) MCP LME TEST STUDIO — task #282 (ticket 40599, feature notify-setting, branch ai_fixbug_40599, round 1, status tc-ready, reviewState leader)` |
| Tổng số TC review | `22` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `12/19 vùng ảnh hưởng đủ TC · 3 GAP · 4 RISK`

> Chiều (b) lấy từ Studio `task_get_context(sections=["dev_impact","spec_delta"])` — `diffAvailable = true`, `diffStat` = 2 file / 53 dòng thêm / 2 dòng bớt: `app/Http/Controllers/Admin/BotController.php` (8 dòng) + `database/migrations/2026_09_07_000000_dedupe_and_unique_notify_setting_bot_user.php` (47 dòng). Điểm sửa: `BotController::step2Check` (~5600) đổi `create()` → `firstOrCreate()` khoá `(bot_id, user_id)`.

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | `F1 — BotController::step2Check` · giá trị **mặc định** của dòng `notify_setting` mới tạo | `diff code` | `không có` | `GAP — 0 TC cover.` `dev_impact` Studio ghi rõ điểm này tạo bản ghi mặc định **~40 cột, mọi `is_notify_*` bật**. Đổi `create()` → `firstOrCreate()` tách tham số thành `$attributes` (khoá) + `$values` (phần còn lại) — nếu 38 cột default rơi nhầm vào `$attributes` thì khoá vô dụng, nếu rơi rớt khỏi `$values` thì **bot mới tạo sau fix nhận sai/không nhận thông báo**. NEW-1/2/3 chỉ **đếm số dòng**, không TC nào kiểm giá trị các cột | `[BLOCKER]` |
| G2 | `F7 / T2 / T4` — kênh **ChatWork** và **PC (Web Push)** | `dev-impact` | `TC-DATAMIG001-01 (NEW-21)` — chỉ tầng dữ liệu | `GAP — 0 TC verify output cuối (RULE-06).` Cả 3 kênh đều poll `notify_setting` (`HandlePushNotifyChatwork`, `HandlePushNotifyPc` — spec FA-006 §7) nên dòng trùng cũng nhân đôi tin ChatWork + web push PC. Dedup **XOÁ** dòng mang `notification_room_url` / `api_token`; NEW-21 chỉ kiểm giá trị còn trong DB, **không có TC gửi thật** để chứng minh ChatWork/PC vẫn nhận đúng 1 tin sau dedup. Grep 22 TC: `PC` / `Web Push` / `Firebase` = **0 lần xuất hiện** | `[MAJOR]` |
| G3 | `F2 / D1 / T5` — migration chạy trên môi trường **live** | `diff code` | `NEW-12`, `NEW-14`, `NEW-15` | `RISK — cả 3 TC chạy trên DB test, env_scope dev/local.` `ALTER TABLE` thêm unique index trên `notify_setting` cỡ production sẽ khoá bảng; không TC nào đo thời gian khoá, cũng không TC nào kiểm request lưu cài đặt / job polling **trong lúc** migration chạy | `[MAJOR]` |
| G4 | `D2 — dedup DELETE thủ công trên production` | `dev-impact` | `NEW-15` (chỉ rollback migration) | `GAP — không có đường lùi cho dữ liệu.` NEW-15 chỉ gỡ index, **không khôi phục dòng `notify_setting` đã DELETE**. Không TC nào yêu cầu backup/export trước khi dedup rồi verify khôi phục được. Journal #134973 cho thấy DELETE đã chạy thật trên staging + step; phạm vi là **toàn bộ bot có cặp trùng**, không riêng bot khách | `[BLOCKER]` |
| G5 | `F1 / T1 — luồng thêm bot hoàn chỉnh (happy path)` | `diff code` | `NEW-1`, `NEW-2`, `NEW-3` | `RISK — cả 3 TC dừng ở màn QR 「接続チェック」, KHÔNG quét QR, KHÔNG hoàn tất Step 5/5.` Hành vi đổi là "siết lại" (tạo vô điều kiện → chỉ tạo khi chưa có) nhưng chỉ có TC cho phía siết, **không có TC chứng minh luồng tạo bot bình thường vẫn chạy trọn vẹn** | `[MAJOR]` |
| G6 | `F7 — ActionLaterService.checkAddNotify (linect-service, job callback)` | `dev-impact` | `NEW-5`, `NEW-20`, `NEW-22` — chỉ quan sát qua app | `RISK — không TC nào kiểm ở tầng job.` Catalog D: **dev/staging chạy 1 job all, production tách 3 job độc lập (callback / broadcast / scenario)** → kết quả staging KHÔNG suy ra production cho đúng job callback mà Dev kê ở F7. Grep 22 TC: `polling` = 0 lần | `[MAJOR]` |
| G7 | `D3 — mobile_notify trùng đã sinh trước fix` | `dev-impact` | `NEW-6` (chứng minh còn trùng trước dedup) | `RISK — chưa có kết luận vận hành.` Dev ghi các dòng `mobile_notify` trùng "không tự mất"; sau khi dọn `notify_setting` khách **vẫn thấy các mục thông báo trùng cũ** trong app. Không TC nào xác định phải dọn hay chấp nhận để lại → xem §6 | `[MAJOR]` |

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `22 quan điểm Trigger khớp task · 12 chưa cover đủ (4 GAP · 8 RISK)`

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `DATA-BACKUP-001` ★ | `Cao` | `GAP — 0 TC cover.` Trigger: tính năng **thêm/đổi bảng DB** + có DELETE dữ liệu thật. Evidence bắt buộc của quan điểm = "query đối chiếu trước/sau + screenshot sau recover" — không TC nào có. Trùng vùng với G4 | `[BLOCKER]` |
| Q2 | `JOB-001` ★ | `Cao` | `GAP — 0 TC ở tầng job nền.` Evidence bắt buộc = "log job + bảng đối chiếu số bản ghi vào/ra". 3 job (`HandlePushMessageNotifyService`, `HandlePushNotifyChatwork`, `HandlePushNotifyPc`) đều poll `notify_setting` mà không TC nào chạm. Trùng vùng với G2 + G6 | `[BLOCKER]` |
| Q3 | `REG-RUN-001` | `Cao` | `GAP — 0 TC cover.` Dedup DELETE + migration chạy khi job đang poll và `mobile_notify` còn bản ghi `status = 0` (chưa gửi). Evidence bắt buộc = "job/dữ liệu chạy dở từ trước release vẫn tiếp tục đúng sau release" | `[BLOCKER]` |
| Q4 | `DEPLOY-LIVE-001` ★ | `Cao` | `GAP — 0 TC cover.` Release production không bật maintain + migration đổi cấu trúc bảng đang được cả web lẫn 3 job đọc/ghi | `[BLOCKER]` |
| Q5 | `STATE-001` | `Cao` | `RISK — chỉ test gián đoạn ở 1 cặp bước.` Luồng kết nối bot có 5 bước ghi dữ liệu tuần tự; NEW-1/2/3 chỉ ngắt ở Step 5 → 4. Evidence bắt buộc = "trạng thái dữ liệu sau khi cố tình ngắt giữa **từng cặp bước**" | `[MAJOR]` |
| Q6 | `CONC-001` | `Cao` | `RISK — TC gắn mã nhưng không test đồng thời.` `TC-CONC001-01 (NEW-3)` gọi endpoint bước 2 **tuần tự 2 lần**, không phải 2 request song song → không chứng minh được idempotency dưới race. Thêm nữa TC này đang ở trạng thái **`skip`** (xem §4) | `[MAJOR]` |
| Q7 | `DATA-MIG-001` | `Cao` | `RISK — RULE-01.` Có `NEW-21` (Normal) + `NEW-15` (Normal) + `NEW-14` (Abnormal), **thiếu Boundary**, không ghi lý do. Biên thật: số cặp trùng lớn / bảng cỡ production | `[MAJOR]` |
| Q8 | `OUT-TRUTH-001` | `Cao` | `RISK — RULE-01.` Có `NEW-7` (Normal) + `NEW-8` (Abnormal), **thiếu Boundary**, không ghi lý do. Biên thật: bỏ tick **tất cả** hạng mục / tick lại tất cả (liên quan `is_all_*_new` — BR-08) | `[MAJOR]` |
| Q9 | `SYNC-APP-001` | `Trung bình → Cao` (flow critical user-facing) | `RISK — RULE-01.` Cả 3 TC (`NEW-5`, `NEW-20`, `NEW-22`) đều `Normal`, **thiếu Abnormal + Boundary**, không ghi lý do | `[MAJOR]` |
| Q10 | `COMPAT-LEGACY-001` ★ | `Cao` | `RISK — chỉ được cover bởi mã lạ.` Nội dung nhánh "dữ liệu cũ `user_id` NULL song song dữ liệu mới" **đã có** ở `NEW-13`, nhưng TC ghi mã `RULE-09` (không có trong `checklist-lme.md`) → `/review-tc` không map được coverage. Sửa mã trên Studio là đủ, không cần viết TC mới | `[MAJOR]` |
| Q11 | `DATA-COUNT-001` | `Cao` | `RISK — chuẩn của badge chưa chốt.` Spec FA-006 BR-07 tính badge **theo bot** (`getTotalNotifyExceptSystemAndBillCycle(botId)` → `bots.count_app_notify`), trong khi `NEW-9` kỳ vọng "quản trị viên badge +1, nhân viên badge +1" (theo người). 2 chuẩn khác nhau → không viết TC cho tới khi Dev chốt, xem §6 | `[MAJOR]` |
| Q12 | `REG-URL-001` ★ | `Trung bình → Cao` (CR chạm **màn kết nối bot**) | `RISK — 0 TC.` Diff không đụng `routes/`, nhưng trigger khớp nguyên văn → cần ít nhất 1 lượt smoke status code các URL của luồng kết nối bot sau release | `[MINOR]` |

---

## 3. TC trùng lặp nội dung

Đã rà **22/22 TC** theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`).

| Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| DUP-1 | `TC-SYNCAPP001-03 (NEW-22)` | `TC-SYNCAPP001-01 (NEW-5)` → **GỘP, không xóa** | `DUP-SUBSET` | Cùng `SYNC-APP-001` × `Normal` × "kích hoạt sự kiện sinh thông báo → đếm mục app + badge + bản ghi" × tiền đề "bot tạo sau fix, đúng 1 dòng `notify_setting`". Dataset của NEW-22 (nhiều loại event) bao trùm 2 event của NEW-5 (form + QR); expected tương đương từng chữ | `[MINOR]` |

| DUP-2 | `TC-CONC001-04` (#16612) · `TC-CONC001-05` (#16614) | `TC-REGSHARED001-08` (#16613) · `TC-REGSHARED001-09` (#16615) → **ĐÃ XÓA trên Studio** | `DUP-SUBSET` (và trùng nhau theo cả 4 yếu tố) | Cùng `REG-SHARED-001` × `Normal` × "hoàn tất kết nối một mạch trên slot trống → chụp cấu hình mặc định" × tiền đề "gói trả phí + 1 slot trống + channel dùng thử"; expected tương đương. Chỉ khác **cách kết nối** — nhưng phép chụp cấu hình mặc định phụ thuộc cách kết nối thì đã có `TC-REGSHARED001-06` (wizard) và `TC-REGSHARED001-07` (quick), nên 2 TC này lặp lại trên nhánh slot mà `INSERT notify_setting` chứng minh được là **cùng điểm tạo** (FA-042 §3: bước #2, `bot_slot_id` rẽ ở bước #12) | `[MINOR]` |

- **Gate DUP-2 đã chạy**: sau khi xóa, luồng "slot trống + add thường" còn `TC-CONC001-04`, "slot trống + quick connect" còn `TC-CONC001-05` → **không ô nào của ma trận 4 luồng mất cover**. Trước khi xóa đã chuyển 2 assertion riêng của TC bị xóa (số slot còn trống giảm đúng 1 + chụp đủ 3 kênh và toàn bộ tab) vào `steps` + `expected` của 2 TC giữ lại (`testcase_update` → version 2).
- **Gate đã chạy**: giả định xóa `NEW-5` → `SYNC-APP-001` và vùng `F6/F8/T4` **vẫn còn cover** bởi NEW-22 + NEW-20. **Nhưng** NEW-5 là bản `exec_mode = auto` duy nhất của cụm này (NEW-22 và NEW-20 đều `manual`) → theo gate, đổi đề xuất từ **xóa** sang **gộp**: giữ NEW-5 làm bản auto tối giản (form + QR), cắt 2 event đó khỏi dataset của NEW-22 để 2 TC không chồng nhau.
- **Đã kiểm và KHÔNG phải trùng** (ghi lại để Leader khỏi rà lại): `NEW-1` vs `NEW-2` (khác mã + khác loại case Normal/Boundary, NEW-2 có trục riêng "số lần lặp = 3") · `NEW-3` vs `NEW-1` (khác tầng: endpoint vs UI) · `NEW-6` vs `NEW-8` (cùng tiền đề 2 dòng trùng nhưng khác assertion: số lượng notify vs hiệu lực bỏ tick) · `NEW-14` vs `NEW-21` (khác assertion: migration dừng khi còn trùng vs bảo toàn cấu hình ChatWork khi dedup).
- `DUP-INFLATE`: không có — không nhóm trùng nào đang che GAP ở §1 / §2.
- `DUP-CONFLICT`: không có.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | Toàn bộ nguồn TC | **Chỉ 2/22 TC (9%) thực sự `pass`** — 19 TC chưa chạy, 1 TC `skip`. Ticket đang ở `Fix done - Đợi test` nhưng bộ TC gần như chưa có kết luận nào | Chạy hết 22 TC trước khi kết luận. Không dùng "đã có 22 TC" làm bằng chứng đã test |
| 2 | `[MAJOR]` | `TC-CONC001-01 (NEW-3)` | TC **duy nhất** kiểm idempotency ở tầng endpoint của **chính điểm sửa** đang ở trạng thái `skip`, không ghi lý do | Yêu cầu người chạy ghi lý do skip trên Studio; nếu skip vì thiếu công cụ gọi API → chuyển sang chạy tay hoặc gỡ TC khỏi bộ để không tạo cảm giác đã cover |
| 3 | `[MAJOR]` | Toàn bộ nguồn TC · RULE-08 / ENV-003 | **0 TC chạy production.** 3 TC đã chạy đều ở `STAGING`; `envAuto` cho thấy `runs = 1` (local) và 0 run ở dev/staging/prd. Task chạm **job nền** (production tách 3 job độc lập vs staging 1 job all — Catalog D), **domain ngoài** (Firebase FCM, ChatWork API v2) và **migration DB** — cả 3 đều là hạng mục RULE-08 cấm kết luận từ staging | Nâng `env_scope` các TC ở §5 sang `product`; chốt lịch chạy trên production trước khi đóng ticket |
| 4 | `[MAJOR]` | 2 TC `Đạt` (`NEW-1`, `NEW-2`) · RULE-02 | Cột `Evidence thực tế` rỗng cho cả 2 TC đã `Đạt`. ⚠️ Snapshot `testcase_list` **không trả về** trường evidence → **phải mở Studio kiểm trực tiếp**; nếu trên Studio cũng rỗng thì 2 kết quả Đạt là tự khai | Leader mở Studio task #282 xác nhận evidence của NEW-1 / NEW-2 (query đếm dòng `notify_setting` trước/sau + screenshot màn QR 2 lần) |
| 5 | `[MAJOR]` | 6/22 TC | **Mã quan điểm không có trong `framework/checklist-lme.md`**: `TOOL-KNOW-002` (NEW-1), `TOOL-NEGCTRL-001` (NEW-9), `TOOL-OLDREC-001` (NEW-6), `JOB-002` (NEW-19), `RULE-04` (NEW-16), `RULE-09` (NEW-13). Trong đó **TC tái hiện bug gốc** (NEW-1) và **TC đối chứng âm** (NEW-9) — 2 TC quan trọng nhất — đều mang mã lạ nên không map được coverage | Map lại trên Studio: NEW-1 → `STATE-001` hoặc `CONC-001` · NEW-9 → `DATA-DB-001` · NEW-6 → `DATA-MIG-001` · NEW-19 → `JOB-001` · NEW-16 → `DATA-BACKUP-001` · NEW-13 → `COMPAT-LEGACY-001`. `RULE-04` / `RULE-09` là **RULE**, không phải mã quan điểm — không được dùng ở cột này |
| 6 | `[MAJOR]` | `03-dev-impact.md` | File có `Auto-filled: 2026-09-07 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" chưa tick** — F/D/T có thể thiếu hoặc map sai (mục 4.1 của Dev chỉ kê **file thay đổi**, không kê function-level; cột F1–F9 hiện là suy luận từ mục 3) | Tester đọc lại Journal #134965 trên Redmine, đối chiếu mục 4.1/4.2/4.3 rồi tick checkbox |
| 7 | `[MAJOR]` | `NEW-18`, `NEW-10` | **`Kết quả mong đợi` không phải một chuẩn duy nhất** — NEW-18 ghi *"Kỳ vọng mong muốn: … Nếu thực tế màn báo lỗi … thì đánh Không đạt … cần leader quyết định"*; NEW-10 cũng có nhánh "nếu xuất hiện lỗi trùng khoá thì đánh Không đạt". TC không được để người chạy tự chọn chuẩn | Hỏi Dev chốt trước: sau khi thêm unique index, `StaffManagementController::createNotifyDefault` (**không sửa** theo quyết định human) có được phép ném `Duplicate entry` không. Chốt xong mới viết expected dứt khoát |
| 8 | `[MINOR]` | `NEW-2` | **Mã quan điểm gán sai trigger**: NEW-2 gắn `STATE-CLEAN-001` (trigger = hủy hợp đồng / ngắt kết nối bot-OA / gỡ tích hợp / hạ gói) nhưng nội dung là lặp back→next ở wizard thêm bot | Đổi mã NEW-2 → `STATE-001` (quy trình nhiều bước bị gián đoạn) |
| 8b | `[MINOR]` | Toàn bộ TC luồng thêm bot (`NEW-1`, `NEW-2`, `NEW-3`, `NEW-4`, `TC-REGSHARED001-06`, `TC-CONC001-02`, `TC-STATE001-01`) | **Không TC nào ghi rõ đang chạy biến thể nào** của luồng thêm bot. Tiền đề chỉ ghi "cặp thông tin kênh dùng thử chưa từng kết nối" → ngầm định **free lần đầu + add thường**, nhưng người chạy khác có thể chạy trên slot trả phí hoặc 「クイック接続」 và ra kết quả khác mà không biết đã lệch biến thể | Thêm 1 câu vào tiền đề trên Studio: *"Chạy ở luồng free lần đầu, kết nối theo cách 「1ステップずつ順番に設定」"*. 3 biến thể còn lại đã có TC riêng — xem ma trận 4 luồng ở §5 |
| 8c | `[MAJOR]` | `TC-REGSHARED001-06` (Studio #16607) | **Tiền đề "biến thể B" không chạy được**: ghi *"tài khoản quản trị đã có sẵn bot khác và đã có dòng cài đặt thông báo cho bot đó"* — nếu bot cũ là bot free thì bị chặn bởi giới hạn **1 bot free/tài khoản** (FA-042 BR-004), TC dừng trước khi tới màn QR | Bỏ biến thể B khỏi TC này, để nó thuần "free lần đầu + add thường". Nhánh "tài khoản đã có bot khác" nay được `TC-REGSHARED001-08` cover đúng bằng **slot trả phí** |
| 9 | `[MINOR]` | `NEW-4` (`TC-REGSHARED001-05`) | TC là **đọc mã nguồn tĩnh** ("mở tệp điều khiển…, xác nhận điểm này tạo bản ghi theo kiểu tìm-trước"), không phải test hành vi. Chính TC tự ghi "TC BỔ TRỢ". Đang chiếm 1 slot `REG-SHARED-001` và làm quan điểm này trông đủ hơn thực tế | Giữ lại như một mục code-review, nhưng **không tính vào coverage**; `env_scope = all` hiện tại gây hiểu nhầm là đã kiểm trên mọi môi trường |
| 10 | `[MINOR]` | `T1 — Add New Account` · AP-3 | **Happy-path-only regression**: cả 3 TC của T1 đều có tiền đề "bảng cài đặt thông báo chưa có dòng nào cho cặp bot sắp tạo". Không TC nào chạy luồng kết nối bot ở **edge state** (tài khoản đã có dòng cài đặt cho bot khác, hoặc bot đã có dòng legacy `user_id` trống) | Thêm biến thể tiền đề vào `TC-REGSHARED001-06` ở §5 |
| 11 | `[NIT]` | `NEW-22` | TC dạng dataset gộp nhiều loại event vào 1 TC (không atomic — 4a #4). Chấp nhận được vì root cause chung, nhưng 1 event fail sẽ làm cả TC fail, khó trace | Giữ nguyên nhưng bắt buộc ghi kết quả **từng event** vào evidence, đúng như TC đã tự yêu cầu ("ghi SKIP riêng event đó") |

> Spec đã tìm được (`spec-features/admin/notify-setting/feature-spec.md` — FA-006, có §5 Business Rules BR-01…BR-15) → **không** có dòng `[MAJOR]` thiếu spec.

---

## 5. TCs đề xuất bổ sung

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/` **chưa có FA-006 (通知設定) và chưa có FA-032 (新規アカウント追加)** — không đối chiếu được. 18 file hiện có thuộc FA-001/003/004/007/008/009/010/011/012/013/015/019/020/021/026/031/033 |
| Vùng regression phát hiện từ kho | Không có (kho chưa cover 2 feature này) |
| Conflict expected vs kho | Không |
| GAP dùng lại TC kho (không viết mới) | Không |
| Xác nhận chống trùng | Đã đối chiếu **22 TC** ở BƯỚC 0 + kho — **không TC đề xuất nào trùng** |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-REGSHARED001-06 | UI | REG-SHARED-001 | Thêm tài khoản mới 「新規アカウント追加」 — hoàn tất kết nối bot | Normal | manual | Tất cả | Hoàn tất trọn vẹn luồng thêm bot mới — màn 通知設定 của bot mới hiện đúng cấu hình mặc định | Tài khoản quản trị đăng nhập trên trình duyệt. Có cặp Channel ID/secret dùng thử (Messaging API + LINE Login cùng provider) chưa từng kết nối. Có điện thoại đăng nhập được LINE để quét QR. Biến thể B: dùng tài khoản quản trị **đã có sẵn** bot khác và đã có dòng cài đặt thông báo cho bot đó | 1. Đi hết trình hướng dẫn kết nối bot tới màn 「接続チェック」 (Step 5/5)<br>2. Quét mã QR bằng LINE trên điện thoại, kết bạn với bot<br>3. Bấm nút xác nhận kết nối để hoàn tất trình hướng dẫn<br>4. Chờ màn chuyển về danh sách/tổng quan bot, xác nhận bot mới đã xuất hiện<br>5. Chọn bot mới, mở màn 「通知設定」<br>6. Ghi lại trạng thái của **cả 3 kênh** (App / ChatWork / PC), giá trị tần suất gửi, và trạng thái tick của **từng tab** trong khối 「2.通知を受け取る項目を選択してください」<br>7. Chạy lại toàn bộ các bước trên với biến thể B | Cặp Channel ID / Channel secret dùng thử. Không nhập thêm dữ liệu nào khác | Bot mới được tạo và hoàn tất kết nối, không có thông báo lỗi ở bất kỳ bước nào. Màn 「通知設定」 của bot mới mở được và hiển thị đúng cấu hình mặc định: kênh **App BẬT**, kênh **PC TẮT**, tần suất gửi 「リアルタイム」, và **mọi hạng mục thông báo đang ở trạng thái tick** (đúng như mô tả "~40 cột, mọi is_notify_* bật" ở đánh giá ảnh hưởng). Không hạng mục nào hiển thị trống/không tick. Biến thể B cho kết quả y hệt, và cấu hình thông báo của bot cũ **không bị thay đổi** | | Lấp `G1` + `G5` + issue #10 (AP-3) · Đánh giá spec: Spec ghi rõ (FA-006 BR-02) · Evidence: screenshot màn 「通知設定」 của bot mới, chụp đủ 3 kênh + toàn bộ tab hạng mục |
| TC-DATABACKUP001-01 | Data | DATA-BACKUP-001 | Vận hành — dedup `notify_setting` trước deploy | Abnormal | manual | product | Sao lưu trước khi dọn dữ liệu trùng và khôi phục lại được một dòng đã xoá | Môi trường đang còn ít nhất 1 cặp (bot, người dùng) trùng. Có quyền chạy truy vấn đọc/ghi và quyền export. Đã thống nhất với vận hành nơi lưu file sao lưu | 1. Chạy truy vấn liệt kê **toàn bộ** các dòng cài đặt thông báo sắp bị xoá (mọi cặp trùng, không riêng bot của khách), export ra file kèm đầy đủ các cột<br>2. Ghi lại tổng số dòng của bảng và số dòng sắp xoá<br>3. Thực hiện dedup theo đúng runbook (chuyển `notification_room_url` + `api_token` sang dòng giữ, xoá dòng id lớn hơn)<br>4. Chọn **1 dòng bất kỳ đã xoá**, khôi phục lại từ file sao lưu vào một môi trường thử<br>5. Đối chiếu từng cột của dòng khôi phục với bản ghi trong file sao lưu<br>6. Xoá lại dòng vừa khôi phục thử để trả môi trường về trạng thái sau dedup | Danh sách cặp (bot, người dùng) trùng lấy từ chính môi trường đích | File sao lưu chứa **đủ** số dòng sắp xoá, khớp con số đếm ở bước 2. Dòng khôi phục thử có **mọi cột trùng khớp** với bản ghi trong file sao lưu, đặc biệt là cấu hình ChatWork và các cột `is_notify_*`. Nếu thiếu file sao lưu hoặc không khôi phục được → **chặn deploy**, vì migration rollback chỉ gỡ được ràng buộc chứ không trả lại dòng đã xoá | | Lấp `G4` + `Q1` · RULE-08 (dữ liệu production là đối tượng bị xoá) · Đánh giá spec: Spec không ghi — đã hỏi Dev qua §6 · Evidence: file export trước dedup + bảng đối chiếu cột trước/sau khôi phục |
| TC-JOB001-01 | UI | JOB-001 | Cài đặt thông báo 「通知設定」 — kênh ChatWork | Normal | manual | product | Sau dedup, ChatWork vẫn nhận đúng một tin cho mỗi sự kiện | Một bot **đã từng có 2 dòng cài đặt trùng**, trong đó dòng bị xoá là dòng mang cấu hình ChatWork. Đã hoàn tất dedup theo runbook (chuyển cấu hình ChatWork sang dòng giữ). Kênh ChatWork đang BẬT, room ChatWork thật đang mở để quan sát | 1. Mở màn 「通知設定」 của bot, xác nhận phần cài đặt ChatWork vẫn hiển thị đúng room đã cấu hình trước dedup<br>2. Bấm nút gửi tin test ChatWork trên màn<br>3. Mở room ChatWork thật, đếm số tin nhận được<br>4. Quay lại LME, kích hoạt một sự kiện sinh thông báo bằng thao tác thật (người dùng LINE trả lời biểu mẫu của bot)<br>5. Chờ hết chu kỳ gửi ChatWork (khoảng 30 giây–1 phút theo cảnh báo trên màn) rồi đếm lại số tin trong room<br>6. Lặp lại bước 4–5 thêm 2 lần với 2 sự kiện khác nhau | 1 lần gửi tin test + 3 sự kiện sinh thông báo thuộc 3 loại khác nhau | Cấu hình room ChatWork **còn nguyên** sau dedup, không bị trống. Tin test vào room đúng 1 lần. Mỗi sự kiện ở bước 4 sinh **đúng 1 tin** trong room ChatWork, không có 2 tin cùng nội dung. Nếu room trống cấu hình hoặc không nhận được tin → dedup đã làm mất cấu hình ChatWork, đánh Không đạt | | Lấp `G2` + `Q2` · RULE-06 (verify tại output cuối, không dừng ở màn admin) · RULE-08 (job nền + ChatWork API bên ngoài) · Đánh giá spec: Spec ghi rõ (FA-006 BR-09, BR-13) · Evidence: screenshot room ChatWork kèm timestamp từng tin |
| TC-JOB001-02 | UI | JOB-001 | Cài đặt thông báo 「通知設定」 — kênh PC (Web Push) | Normal | manual | product | Sau dedup, trình duyệt nhận đúng một web push cho mỗi sự kiện | Bot đã hoàn tất dedup, chỉ còn 1 dòng cài đặt cho tài khoản đang dùng. Trình duyệt đã cấp quyền hiển thị thông báo và đã đăng ký nhận thông báo PC từ màn 「通知設定」 | 1. Mở màn 「通知設定」, BẬT kênh PC và hoàn tất đăng ký nhận thông báo trên trình duyệt<br>2. Đóng hoặc thu nhỏ tab LME để quan sát được thông báo hệ thống của trình duyệt<br>3. Kích hoạt đúng 1 sự kiện sinh thông báo bằng thao tác thật<br>4. Không kích hoạt thêm sự kiện nào trong thời gian quan sát<br>5. Đếm số thông báo trình duyệt hiện lên cho sự kiện đó<br>6. Mở lại màn 「通知設定」, TẮT kênh PC, kích hoạt thêm 1 sự kiện cùng loại và quan sát lại | 2 sự kiện cùng loại: 1 lần khi kênh PC bật, 1 lần khi kênh PC tắt | Khi kênh PC bật: trình duyệt nhận **đúng 1** thông báo cho sự kiện, không có 2 thông báo cùng nội dung. Khi kênh PC tắt: **không** nhận thông báo nào. Trạng thái bật/tắt kênh PC vẫn giữ đúng sau khi tải lại màn | | Lấp `G2` + `Q2` · RULE-06 · RULE-08 (Web Push qua domain ngoài) · Đánh giá spec: Spec ghi rõ (FA-006 §7 `HandlePushNotifyPc`, BR-15) · Evidence: quay màn hình lúc thông báo trình duyệt hiện lên |
| TC-JOB001-03 | UI | JOB-001 | Mobile App — thông báo qua job callback | Normal | manual | product | Trên production (3 job tách riêng), sự kiện đi qua job callback chỉ sinh một thông báo | Chạy **trên production**. Bot đã dedup, chỉ còn 1 dòng cài đặt cho tài khoản nhận. Kênh App bật, tần suất 「リアルタイム」, loại sự kiện dùng để thử đang được tick. Có thiết bị thật đã đăng nhập app | 1. Ghi lại số mục thông báo và badge hiện tại của bot trên app<br>2. Kích hoạt một sự kiện đi qua luồng callback bằng thao tác thật của người dùng LINE (kết bạn qua mã QR của bot)<br>3. Chờ tối đa 2 phút, quan sát thông báo đẩy trên thiết bị<br>4. Mở app, đếm số mục thông báo mới và badge<br>5. Lặp lại bước 2–4 với một sự kiện gửi tin hàng loạt để đi qua job khác<br>6. Ghi lại thời điểm từng sự kiện và thời điểm nhận thông báo | 1 sự kiện kết bạn qua QR + 1 sự kiện gửi tin hàng loạt, cách nhau ít nhất 2 phút | Mỗi sự kiện: thiết bị nhận **đúng 1** thông báo đẩy, app tăng **đúng 1** mục, badge tăng **đúng 1**. Không có mục nào trùng nội dung. Kết quả phải giống nhau ở cả 2 loại sự kiện dù chúng đi qua 2 job khác nhau trên production | | Lấp `G6` + `Q2` · RULE-08 / ENV-003 — production tách 3 job độc lập (callback / broadcast / scenario) trong khi staging chạy 1 job all, nên kết quả staging **không** thay thế được TC này · Đánh giá spec: Spec ghi rõ (FA-006 §7) · Evidence: screenshot Notification Center của thiết bị + screenshot danh sách thông báo trong app kèm timestamp |
| TC-REGRUN001-01 | Data | REG-RUN-001 | Vận hành — dedup + migration khi job đang chạy | Abnormal | manual | product | Thông báo đang chờ gửi trước khi dedup vẫn được gửi đúng một lần sau dedup và migration | Môi trường còn ít nhất 1 cặp (bot, người dùng) trùng, kênh App đang bật với tần suất **không phải realtime** để chắc chắn có thông báo đang nằm chờ. Các job nền đang chạy bình thường, **không** dừng job khi test | 1. Kích hoạt vài sự kiện sinh thông báo cho bot còn dữ liệu trùng, để chúng ở trạng thái chờ gửi<br>2. Ghi lại số thông báo đang chờ và badge hiện tại<br>3. **Không dừng job**, chạy dedup theo runbook rồi chạy migration<br>4. Trong lúc migration chạy, mở màn 「通知設定」 của một bot khác và thử lưu một thay đổi<br>5. Sau khi migration xong, chờ hết chu kỳ gửi rồi mở app đếm số thông báo nhận được<br>6. Đối chiếu với số thông báo đang chờ ghi ở bước 2 | Số thông báo đang chờ ghi nhận ở bước 2; 1 thao tác lưu cài đặt trên bot khác trong lúc migration chạy | Toàn bộ thông báo đang chờ từ trước vẫn được gửi, **không mất** và **không bị nhân đôi** — số mục nhận trên app khớp số ghi ở bước 2. Thao tác lưu cài đặt ở bước 4 hoặc thành công, hoặc báo lỗi có thông báo rõ ràng cho người dùng — **không được** trả lỗi hệ thống trắng màn. Sau migration, ràng buộc duy nhất tồn tại và không cặp nào còn trùng | | Lấp `Q3` + `G3` · RULE-08 (job nền + loadbalance) · Đánh giá spec: Spec không ghi — đã hỏi Dev qua §6 · Evidence: bảng đối chiếu số thông báo chờ trước/sau + log lỗi trong khoảng thời gian migration chạy |
| TC-DATAMIG001-04 | Data | DATA-MIG-001 | Migration `notify_setting` — ràng buộc duy nhất | Boundary | manual | product | Migration trên bảng cỡ production với số cặp trùng lớn — đo thời gian khoá bảng | Bản sao dữ liệu có kích thước tương đương production, đã dựng sẵn **số lượng lớn** cặp (bot, người dùng) trùng (tối thiểu vài trăm cặp). Ghi lại tổng số dòng của bảng trước khi bắt đầu | 1. Chạy truy vấn đếm số cặp trùng và tổng số dòng của bảng, ghi lại<br>2. Chạy dedup theo runbook, ghi lại thời gian chạy và số dòng bị xoá<br>3. Bắt đầu bấm giờ rồi chạy migration<br>4. **Trong lúc migration chạy**, ở phiên khác thử mở màn 「通知設定」 và thử lưu một thay đổi; ghi lại thời gian chờ và thông báo hiển thị<br>5. Ghi lại tổng thời gian migration hoàn tất<br>6. Chạy lại truy vấn đếm cặp trùng và tổng số dòng | Bảng cỡ production; tối thiểu vài trăm cặp trùng | Dedup xoá **đúng** số dòng thừa, tổng số dòng còn lại = tổng ban đầu trừ số dòng thừa. Migration hoàn tất, ràng buộc duy nhất xuất hiện. Thời gian khoá bảng được ghi lại thành con số cụ thể và **đủ ngắn để chấp nhận deploy không bật maintain**; nếu vượt ngưỡng vận hành chấp nhận được thì đánh Không đạt và đề xuất bật maintain. Thao tác ở bước 4 không trả lỗi hệ thống | | Lấp `Q7` (Boundary còn thiếu của DATA-MIG-001) + `Q4` + `G3` · RULE-08 (loadbalance + hiệu năng) · Đánh giá spec: Spec không ghi — đã hỏi Dev qua §6 · Evidence: log thời gian từng bước + kết quả 2 truy vấn đếm trước/sau |
| TC-CONC001-02 | API | CONC-001 | Luồng kết nối bot — endpoint bước 2 | Abnormal | manual | product | Gọi đồng thời hai request bước 2 với cùng thông tin kênh — chỉ sinh một dòng cài đặt | Phiên đăng nhập hợp lệ của tài khoản quản trị kèm mã chống giả mạo biểu mẫu. Cặp thông tin kênh dùng thử chưa kết nối. Bảng cài đặt thông báo chưa có dòng nào cho cặp bot sắp tạo. Đã chuẩn bị cách phát 2 request **song song** (không phải tuần tự) | 1. Chuẩn bị 2 request giống hệt nhau tới endpoint bước 2 của luồng kết nối bot<br>2. Phát **đồng thời** cả 2 request<br>3. Ghi lại mã trạng thái HTTP và nội dung phản hồi của từng request<br>4. Đếm số dòng cài đặt thông báo theo cặp mã bot và mã người dùng<br>5. Đếm số bot được tạo ứng với Channel ID đã dùng<br>6. Rà log ứng dụng tìm ngoại lệ trùng khoá<br>7. Xoá bot thử nghiệm sau khi test xong | Bộ tham số giống hệt nhau ở cả 2 request phát song song | Bảng cài đặt thông báo chỉ có **đúng 1** dòng cho cặp bot và người dùng; chỉ **1** bot được tạo. Cả 2 request hoặc cùng trả về thành công, hoặc 1 request trả lỗi **có thông báo rõ ràng** — **không được** trả lỗi hệ thống 500 do ngoại lệ trùng khoá. Log không có ngoại lệ trùng khoá chưa bắt | | Lấp `Q6` + issue #8 · RULE-08 (race) · Bổ sung cho `NEW-3` (đang chỉ gọi tuần tự và đang `skip`) · Đánh giá spec: Spec không ghi — đã hỏi Dev qua §6 · Evidence: 2 response body + log ứng dụng khoảng thời gian phát request |
| TC-STATE001-01 | UI | STATE-001 | Thêm tài khoản mới — gián đoạn giữa các bước | Abnormal | manual | Tất cả | Ngắt trình hướng dẫn kết nối bot ở từng cặp bước rồi quay lại làm tiếp | Tài khoản quản trị đăng nhập. Có cặp thông tin kênh dùng thử chưa kết nối. Ghi lại số dòng cài đặt thông báo hiện có của tài khoản trước khi bắt đầu | 1. Đi tới bước nhập kênh Messaging API rồi **đóng hẳn trình duyệt**; mở lại, đăng nhập và vào lại trình hướng dẫn thêm bot<br>2. Ghi lại trình hướng dẫn mở ở bước nào và có dùng lại bot tạm cũ không<br>3. Đi tiếp tới bước cài đặt Webhook rồi đóng trình duyệt lần nữa; mở lại và vào tiếp<br>4. Đi tiếp tới bước nhập kênh LINE Login rồi đóng trình duyệt lần nữa; mở lại và đi tiếp cho tới màn QR<br>5. Sau mỗi lần vào lại, ghi lại mã bot đang hiển thị<br>6. Đếm số dòng cài đặt thông báo theo cặp mã bot và mã người dùng, và đếm số bot ứng với Channel ID đã dùng | Cùng một cặp thông tin kênh cho toàn bộ các lần vào lại; ngắt ở 3 điểm khác nhau | Sau cả 3 lần ngắt: chỉ **1** bot được tạo cho Channel ID đó và chỉ **đúng 1** dòng cài đặt thông báo cho cặp bot và người dùng. Mã bot không đổi qua các lần vào lại. Không lần nào hiển thị lỗi hệ thống; trình hướng dẫn mở lại ở trạng thái dùng được, không kẹt màn trắng | | Lấp `Q5` · Bổ sung cho `NEW-1`/`NEW-2` (chỉ ngắt ở cặp bước Step 5 → 4) · Đánh giá spec: Spec không ghi — đã hỏi Dev qua §6 · Evidence: screenshot màn sau mỗi lần mở lại + kết quả đếm dòng cuối cùng |
| TC-OUTTRUTH001-03 | UI | OUT-TRUTH-001 | Cài đặt thông báo 「通知設定」 — bỏ tick / tick toàn bộ | Boundary | manual | Tất cả | Bỏ tick toàn bộ hạng mục rồi tick lại toàn bộ — trạng thái hiển thị khớp thông báo thực nhận | Bot chỉ còn 1 dòng cài đặt thông báo cho tài khoản đang dùng. Kênh App bật, tần suất 「リアルタイム」. Có sẵn dữ liệu để kích hoạt ít nhất 3 loại sự kiện khác nhau. Ghi lại badge và số mục thông báo hiện tại | 1. Mở màn 「通知設定」, **bỏ tick toàn bộ** hạng mục ở khối 2 (kể cả các ô chọn tất cả và ô 「今後以下に追加される項目に、自動でチェックをいれる」)<br>2. Tải lại màn, xác nhận không hạng mục nào còn tick<br>3. Kích hoạt lần lượt 3 loại sự kiện khác nhau bằng thao tác thật<br>4. Mở app, đếm số mục thông báo mới và badge<br>5. Quay lại màn 「通知設定」, **tick lại toàn bộ** hạng mục<br>6. Tải lại màn, xác nhận toàn bộ đã tick<br>7. Kích hoạt lại đúng 3 loại sự kiện đó và đếm lại số mục thông báo và badge | Trạng thái biên: 0 hạng mục được tick, rồi toàn bộ hạng mục được tick; 3 loại sự kiện chạy ở cả 2 trạng thái | Ở trạng thái bỏ tick toàn bộ: **không** mục thông báo mới nào, badge **không** tăng sau cả 3 sự kiện. Ở trạng thái tick toàn bộ: nhận **đúng 3** mục mới (mỗi sự kiện 1 mục), badge tăng **đúng 3**, không mục nào trùng nội dung. Trạng thái tick hiển thị trên màn luôn khớp với thông báo thực nhận ở cả 2 lượt | | Lấp `Q8` (Boundary còn thiếu của OUT-TRUTH-001) · Đánh giá spec: Spec ghi rõ (FA-006 BR-03, BR-08) · Evidence: screenshot màn 「通知設定」 ở cả 2 trạng thái + screenshot danh sách thông báo app sau mỗi lượt |
| TC-SYNCAPP001-04 | UI | SYNC-APP-001 | Mobile App — kênh App bị tắt | Abnormal | manual | product | Tắt kênh App rồi kích hoạt sự kiện — app không nhận thông báo nào | Bot chỉ còn 1 dòng cài đặt thông báo cho tài khoản đang dùng. Thiết bị thật đã đăng nhập app và đang nhận được thông báo bình thường. Ghi lại badge và số mục thông báo hiện tại | 1. Trên màn 「通知設定」, TẮT công tắc nhận thông báo của kênh App<br>2. Tải lại màn, xác nhận kênh App đang tắt<br>3. Đưa app về nền để quan sát được thông báo đẩy của hệ điều hành<br>4. Kích hoạt 1 sự kiện sinh thông báo bằng thao tác thật<br>5. Chờ tối đa 2 phút, quan sát thiết bị<br>6. Mở app, đếm số mục thông báo mới và badge<br>7. BẬT lại kênh App, kích hoạt 1 sự kiện cùng loại và quan sát lại | 2 sự kiện cùng loại: 1 lần khi kênh App tắt, 1 lần sau khi bật lại | Khi kênh App tắt: **không** có thông báo đẩy nào trên thiết bị, **không** có mục mới trong app, badge **không** tăng. Sau khi bật lại: nhận **đúng 1** thông báo đẩy cho sự kiện mới, app tăng **đúng 1** mục. Các thông báo phát sinh trong lúc kênh App tắt **không** được gửi bù lại sau khi bật (theo BR-04, thông báo cũ đang chờ bị đánh dấu đã xử lý khi bật lại kênh) | | Lấp `Q9` (Abnormal còn thiếu của SYNC-APP-001) · RULE-08 (push qua domain ngoài) · Đánh giá spec: Spec ghi rõ (FA-006 BR-04) · Evidence: screenshot Notification Center trong khoảng quan sát + screenshot màn 「通知設定」 trạng thái tắt |
| TC-SYNCAPP001-05 | UI | SYNC-APP-001 | Mobile App — nhiều sự kiện liên tiếp | Boundary | manual | product | Mười sự kiện liên tiếp trong thời gian ngắn — đúng mười thông báo, badge tăng đúng mười | Bot chỉ còn 1 dòng cài đặt thông báo cho tài khoản đang dùng. Kênh App bật, tần suất 「リアルタイム」. Thiết bị thật đã đăng nhập app. Ghi lại badge và số mục thông báo hiện tại. Chuẩn bị sẵn dữ liệu để kích hoạt 10 sự kiện cùng loại | 1. Ghi lại badge và số mục thông báo trước khi bắt đầu<br>2. Kích hoạt liên tiếp **10 sự kiện cùng loại** trong vòng khoảng 1 phút bằng thao tác thật<br>3. Chờ tối đa 5 phút cho toàn bộ thông báo được xử lý<br>4. Đếm số thông báo đẩy nhận được trên thiết bị<br>5. Mở app, đếm số mục thông báo mới và badge<br>6. Đối chiếu nội dung 10 mục, kiểm xem có mục nào trùng nội dung và trùng thời điểm không | 10 sự kiện cùng loại, phát liên tiếp trong khoảng 1 phút | App tăng **đúng 10** mục thông báo, badge tăng **đúng 10**, thiết bị nhận **đúng 10** thông báo đẩy. Không có mục nào trùng cả nội dung lẫn thời điểm. Không mục nào bị mất | | Lấp `Q9` (Boundary còn thiếu của SYNC-APP-001) · RULE-08 (push qua domain ngoài + tải đồng thời) · Đánh giá spec: Spec không ghi — đã hỏi Dev qua §6 · Evidence: screenshot danh sách thông báo app cuộn đủ 10 mục kèm timestamp + số badge |
| TC-CONC001-03 | UI | CONC-001 | Thêm tài khoản mới — 「クイック接続」 (free lần đầu) | Abnormal | manual | Tất cả | Kết nối nhanh cho bot free đầu tiên — lặp lại bước nhập thông tin kênh không làm thông báo bị nhân đôi | Tài khoản quản trị **chưa có bot free nào** (nếu đã có thì hệ thống chặn theo giới hạn 1 bot free/tài khoản — phải dùng tài khoản khác). Có cặp Channel ID/secret dùng thử (Messaging API + LINE Login cùng provider) chưa từng kết nối. Có điện thoại đăng nhập LINE để quét QR. Thao tác trên máy tính, không dùng điện thoại/tablet | 1. Mở luồng thêm tài khoản mới, ở màn chọn cách kết nối chọn 「クイック接続」 (khoảng 5–10 phút), KHÔNG chọn 「1ステップずつ順番に設定」<br>2. Nhập Channel ID và Channel secret của cả kênh Messaging API và kênh LINE Login trong cùng một màn, bấm tiếp<br>3. Chờ màn 「接続チェック」 hiển thị mã QR; ghi lại mã bot đang hiển thị. KHÔNG quét QR<br>4. Quay lại màn nhập thông tin kênh: dùng nút quay lại trên màn nếu có, nếu không có thì dùng nút Back của trình duyệt hoặc mở lại luồng 「クイック接続」<br>5. Nhập lại **đúng cặp Channel ID/secret cũ** và bấm tiếp lần thứ hai; ghi lại mã bot lần này<br>6. Quét mã QR bằng LINE trên điện thoại để hoàn tất kết nối<br>7. Mở màn 「通知設定」 của bot vừa tạo, xác nhận kênh App đang bật và hạng mục dùng để thử đang được tick<br>8. Kích hoạt đúng 1 sự kiện sinh thông báo bằng thao tác thật (người dùng LINE kết bạn qua mã QR của bot hoặc trả lời biểu mẫu)<br>9. Mở app, đếm số mục thông báo mới và mức tăng của badge<br>10. Đối chiếu số dòng cài đặt thông báo theo cặp mã bot và mã người dùng | Cùng một cặp Channel ID / Channel secret cho cả hai lần nhập; 1 sự kiện sinh thông báo | Mã bot ở lần vào màn QR thứ hai **trùng** lần thứ nhất, không tạo bot thứ hai. Cả hai lần bấm tiếp đều thành công, **không** hiện thông báo lỗi và **không** lỗi hệ thống. Sau khi hoàn tất: 1 sự kiện chỉ làm app tăng **đúng 1** mục thông báo, badge tăng **đúng 1**, không có 2 mục cùng nội dung; số dòng cài đặt thông báo cho cặp bot và người dùng là **đúng 1**. Nếu app hiện 2 mục hoặc màn báo lỗi trùng khoá ở lần bấm thứ hai → luồng 「クイック接続」 có điểm tạo cài đặt thông báo **riêng chưa được sửa**, đánh Không đạt và báo ngay | | Lấp `G1` + `G5` — trục 4 luồng add bot, ô **free lần đầu + quick connect** · Bản sửa chỉ chạm điểm tạo của luồng wizard; TC này là phép thử xem 「クイック接続」 có dùng chung điểm tạo đó không · Đánh giá spec: Spec không ghi — `spec-features/admin/bot-add-v2/` chỉ mô tả wizard 5 bước, đã đưa §6 · Evidence: screenshot màn QR 2 lần kèm mã bot + screenshot danh sách thông báo app |
| TC-REGSHARED001-07 | UI | REG-SHARED-001 | Thêm tài khoản mới — 「クイック接続」 (free lần đầu) | Normal | manual | Tất cả | Kết nối nhanh cho bot free đầu tiên — hoàn tất một mạch, màn 通知設定 hiện đúng cấu hình mặc định | Tài khoản quản trị **chưa có bot free nào**. Có cặp Channel ID/secret dùng thử chưa từng kết nối. Có điện thoại đăng nhập LINE để quét QR. Thao tác trên máy tính | 1. Mở luồng thêm tài khoản mới, chọn 「クイック接続」<br>2. Nhập Channel ID và Channel secret của cả hai kênh trong một màn, bấm tiếp — **đi thẳng, không quay lại bước nào**<br>3. Quét mã QR bằng LINE trên điện thoại<br>4. Chờ màn báo hoàn tất kết nối, xác nhận bot mới xuất hiện trong danh sách tài khoản<br>5. Chọn bot mới, mở màn 「通知設定」<br>6. Ghi lại trạng thái của cả 3 kênh (App / ChatWork / PC), giá trị tần suất gửi, và trạng thái tick của từng tab trong khối 「2.通知を受け取る項目を選択してください」<br>7. Kích hoạt 1 sự kiện sinh thông báo bằng thao tác thật rồi mở app đếm số mục mới và mức tăng badge | Cặp Channel ID / Channel secret dùng thử. Không nhập thêm dữ liệu nào khác | Kết nối hoàn tất, không có thông báo lỗi ở bất kỳ bước nào. Màn 「通知設定」 mở được và hiển thị đúng cấu hình mặc định: kênh **App BẬT**, kênh **PC TẮT**, tần suất 「リアルタイム」, **mọi hạng mục thông báo đang ở trạng thái tick**, không hạng mục nào trống. Sự kiện ở bước 7 làm app tăng **đúng 1** mục, badge tăng **đúng 1**. Cấu hình mặc định phải **giống hệt** bot tạo bằng luồng wizard (đối chiếu với `TC-REGSHARED001-06`) | | Lấp `G1` + `G5` — trục 4 luồng add bot, ô **free lần đầu + quick connect** · Đánh giá spec: Spec không ghi cho 「クイック接続」; cấu hình mặc định lấy chuẩn từ FA-006 BR-02 · Evidence: screenshot màn 「通知設定」 chụp đủ 3 kênh + toàn bộ tab hạng mục |
| TC-CONC001-04 | UI | CONC-001 | Thêm tài khoản mới — wizard 5 bước, vào từ **slot trống** | Abnormal | manual | product | Thêm bot vào slot trống bằng cách đi từng bước — quay lại rồi tiếp tục lại ở màn QR không nhân đôi thông báo | Tài khoản quản trị có **gói trả phí với ít nhất 1 slot chưa gán bot**. Vào wizard **từ đúng slot trống đó** (không phải mở thẳng màn thêm tài khoản mới), vì slot đã có bot thì hệ thống sẽ chuyển hướng về trang chủ. Có cặp Channel ID/secret dùng thử chưa từng kết nối. Có điện thoại đăng nhập LINE để quét QR | 1. Mở màn quản lý tài khoản/hợp đồng, chọn **slot còn trống** và bắt đầu kết nối từ slot đó<br>2. Chọn cách kết nối 「1ステップずつ順番に設定」 và đi hết các bước tới màn 「接続チェック」 (Step 5/5)<br>3. Ghi lại mã bot đang hiển thị. KHÔNG quét QR<br>4. Bấm 「前のステップに戻る」 để về màn nhập kênh LINE Login<br>5. Bấm 「次のステップへ」 để vào lại màn QR lần thứ hai; ghi lại mã bot lần này<br>6. Quét mã QR bằng LINE trên điện thoại để hoàn tất kết nối<br>7. Xác nhận trên màn quản lý hợp đồng: slot vừa dùng nay đã gắn đúng bot mới, và **không** slot nào khác bị chiếm<br>8. Mở màn 「通知設定」 của bot mới, xác nhận kênh App bật và hạng mục dùng để thử đang tick<br>9. Kích hoạt đúng 1 sự kiện sinh thông báo bằng thao tác thật, mở app đếm số mục mới và mức tăng badge<br>10. Đối chiếu số dòng cài đặt thông báo theo cặp mã bot và mã người dùng | Cặp Channel ID / Channel secret dùng thử; 1 slot trống của gói trả phí; 1 sự kiện sinh thông báo | Mã bot ở lần vào màn QR thứ hai **trùng** lần thứ nhất. Slot vừa dùng gắn đúng 1 bot, không slot nào khác bị chiếm và số slot còn trống giảm **đúng 1**. Sau khi hoàn tất: 1 sự kiện làm app tăng **đúng 1** mục, badge tăng **đúng 1**; số dòng cài đặt thông báo cho cặp bot và người dùng là **đúng 1**. Không lần nào hiện thông báo lỗi hay lỗi hệ thống | | Lấp `G1` + `G5` — trục 4 luồng add bot, ô **slot trống + add thường** · RULE-08 (chạm hợp đồng / slot trả phí) · Nhánh có slot đi qua đường xử lý sau khi quét QR **khác hẳn** nhánh free (gắn slot + hợp đồng sẵn có thay vì tạo hợp đồng free mới) · Đánh giá spec: Spec ghi rõ (FA-042 BR-001) · Evidence: screenshot màn QR 2 lần kèm mã bot + screenshot màn quản lý hợp đồng trước/sau |
| TC-CONC001-05 | UI | CONC-001 | Thêm tài khoản mới — 「クイック接続」, vào từ **slot trống** | Abnormal | manual | product | Kết nối nhanh vào slot trống — lặp lại bước nhập thông tin kênh không làm thông báo bị nhân đôi | Tài khoản quản trị có gói trả phí với ít nhất 1 slot chưa gán bot. Vào luồng kết nối **từ đúng slot trống đó**. Có cặp Channel ID/secret dùng thử chưa từng kết nối. Có điện thoại đăng nhập LINE để quét QR | 1. Từ màn quản lý tài khoản/hợp đồng, chọn slot còn trống và bắt đầu kết nối<br>2. Ở màn chọn cách kết nối, chọn 「クイック接続」<br>3. Nhập Channel ID và Channel secret của cả hai kênh trong cùng một màn, bấm tiếp<br>4. Chờ màn QR hiển thị; ghi lại mã bot. KHÔNG quét QR<br>5. Quay lại màn nhập thông tin kênh (nút quay lại trên màn, hoặc Back của trình duyệt, hoặc mở lại luồng 「クイック接続」 từ chính slot đó)<br>6. Nhập lại **đúng cặp Channel ID/secret cũ**, bấm tiếp lần thứ hai; ghi lại mã bot lần này<br>7. Quét mã QR để hoàn tất kết nối<br>8. Xác nhận slot vừa dùng gắn đúng 1 bot và không slot nào khác bị chiếm<br>9. Mở màn 「通知設定」, xác nhận kênh App bật và hạng mục dùng để thử đang tick<br>10. Kích hoạt đúng 1 sự kiện sinh thông báo, mở app đếm số mục mới và mức tăng badge; đối chiếu số dòng cài đặt thông báo theo cặp mã bot và mã người dùng | Cùng một cặp Channel ID / Channel secret cho cả hai lần nhập; 1 slot trống của gói trả phí; 1 sự kiện sinh thông báo | Mã bot ở lần thứ hai **trùng** lần thứ nhất, không tạo bot thứ hai và không chiếm thêm slot. Slot vừa dùng gắn đúng 1 bot, số slot trống giảm **đúng 1**. Sau khi hoàn tất: 1 sự kiện làm app tăng **đúng 1** mục, badge tăng **đúng 1**; số dòng cài đặt thông báo cho cặp bot và người dùng là **đúng 1**. Không lần nào hiện lỗi trùng khoá hay lỗi hệ thống | | Lấp `G1` + `G5` — trục 4 luồng add bot, ô **slot trống + quick connect** · RULE-08 (chạm hợp đồng / slot trả phí) · Ô rủi ro nhất: cộng cả 2 trục chưa được cover · Đánh giá spec: Spec không ghi cho 「クイック接続」, đã đưa §6 · Evidence: screenshot màn QR 2 lần kèm mã bot + screenshot màn quản lý hợp đồng trước/sau + screenshot danh sách thông báo app |

**Ma trận 4 luồng thêm bot** (`G1` + `G5` — trục biến thể do Leader chỉ định):

| | add thường (「1ステップずつ順番に設定」) | add quick connect (「クイック接続」) |
|---|---|---|
| **Free lần đầu** | `NEW-1` `NEW-2` `NEW-3` + `TC-STATE001-01` (lặp / ngắt) · `TC-REGSHARED001-06` (hoàn tất + **chụp cấu hình mặc định**) — bộ TC hiện có, **mặc định chạy ở luồng này** | `TC-CONC001-03` (lặp) · `TC-REGSHARED001-07` (hoàn tất + **chụp cấu hình mặc định**) |
| **Slot trống** | `TC-CONC001-04` — lặp + slot + chụp cấu hình mặc định (gộp) | `TC-CONC001-05` — lặp + slot + chụp cấu hình mặc định (gộp) |

> **Vì sao nhánh slot chỉ 1 TC/ô**: phép **chụp cấu hình mặc định** chỉ phụ thuộc **cách kết nối**, không phụ thuộc free/slot — theo FA-042 §3, `INSERT notify_setting` là **bước #2** trong transaction `step2Check`, còn `bot_slot_id` chỉ rẽ nhánh ở **bước #12** (campaign) và ở bước sau khi quét QR. Lặp phép chụp đó trên nhánh slot là trùng. Hai TC `TC-REGSHARED001-08` / `-09` từng được tạo (Studio #16613 / #16615) nay **đã xóa** — xem §3 DUP-2; assertion riêng của chúng (số slot trống giảm đúng 1 + chụp đủ cấu hình mặc định) đã gộp vào `TC-CONC001-04` / `-05`.

- ⚠️ **Bộ TC hiện có không ghi rõ mình đang chạy luồng nào.** `NEW-1/2/3`, `TC-CONC001-02`, `TC-STATE001-01`, `TC-REGSHARED001-06` chỉ ghi tiền đề "cặp thông tin kênh dùng thử chưa từng kết nối" → ngầm định **free lần đầu + add thường**. Cần bổ sung 1 câu vào tiền đề của các TC đó trên Studio: *"Chạy ở luồng free lần đầu, kết nối theo cách đi từng bước 「1ステップずつ順番に設定」"*, nếu không người chạy khác có thể chạy nhầm biến thể mà không biết.
- ⚠️ **`TC-REGSHARED001-06` (Studio #16607) cần sửa "biến thể B".** Tiền đề đang ghi *"tài khoản quản trị đã có sẵn bot khác"* — nếu bot cũ là bot free thì bị chặn bởi giới hạn 1 bot free/tài khoản (FA-042 BR-004), TC không chạy được. Bỏ biến thể B khỏi TC đó; nhánh "đã có bot khác" nay đã được `TC-REGSHARED001-08` cover đúng bằng slot trả phí.
- `TC-CONC001-03/04/05` dùng `CONC-001` theo đúng nghĩa *"một hành động logic chỉ được xử lý 1 lần"* (lặp thao tác tuần tự) — cùng nghĩa với `NEW-3`. Biến thể **đồng thời** đã tách riêng ở `TC-CONC001-02`.

**Các dòng §1 / §2 KHÔNG đề xuất TC mới — lý do:**

- **`G7`** (mobile_notify trùng đã sinh trước fix) — chưa viết TC vì **chưa có chuẩn**: Dev mới nói "không tự mất" mà chưa nói có dọn hay không. Cần Leader/Dev chốt trước (đã đưa §6); chốt xong mới viết TC verify.
- **`Q10`** (`COMPAT-LEGACY-001`) — nội dung **đã được cover đầy đủ** bởi `NEW-13`; chỉ cần đổi `viewpoint` trên Studio từ `RULE-09` sang `COMPAT-LEGACY-001` (xem §4 issue #5). Viết TC mới sẽ tạo trùng.
- **`Q11`** (`DATA-COUNT-001` — badge theo bot hay theo người) — **không tự chọn bên**: spec BR-07 và `NEW-9` đang mâu thuẫn. Đã đưa §6 để Dev chốt; chốt xong mới viết TC.
- **`Q12`** (`REG-URL-001`) — diff không đụng `routes/`, chỉ đụng thân hàm `BotController` + 1 migration → không đề xuất TC riêng; gộp vào bộ smoke URL cố định sau release (RULE-12 phần 1).

---

## 6. Spec update needed

| # | Nguồn spec | Nội dung spec hiện tại | Xung đột với | Đề nghị |
|---|---|---|---|---|
| 1 | `spec-features/admin/notify-setting/feature-spec.md` §5 **BR-11** + §3 ER Diagram | *"**Mỗi bot 1 record** — mỗi bot chỉ có 1 record trong `notify_setting`. Query luôn dùng `WHERE bot_id = ? LIMIT 1`"*; ER ghi `bots ||--o| notify_setting : "1 bot : 0..1 setting"` và bảng `notify_setting` **không có cột `user_id`** | Mô hình sau fix: **1 record / cặp (bot, người dùng)**, unique key `(bot_id, user_id)`, và `NEW-9` khẳng định 2 dòng khác `user_id` cho cùng bot là **dữ liệu hợp lệ** | Cập nhật BR-11 + ER + Data Model của FA-006 theo đợt tách `notify_setting` theo người dùng. Spec hiện tại **không dùng làm chuẩn đánh giá Expected được** cho ticket này |
| 2 | `spec-features/admin/notify-setting/feature-spec.md` §5 **BR-07** | *"Badge count: `getTotalNotifyExceptSystemAndBillCycle(botId)` → đếm `mobile_notify` chưa xác nhận → UPDATE `bots.count_app_notify`"* — badge tính **theo bot** | `NEW-9` kỳ vọng *"Quản trị viên thấy đúng 1 mục mới, badge +1; nhân viên cũng thấy đúng 1 mục mới, badge +1"* — badge **theo người**. Nếu badge thật sự là `bots.count_app_notify` thì 1 sự kiện với 2 người nhận sẽ làm badge của **cả hai** tăng 2 | **Dev chốt**: badge hiện được tính theo bot hay theo người? Chốt xong mới viết TC cho `Q11`; nếu badge vẫn theo bot thì expected của `NEW-9` đang sai và phải sửa trên Studio |
| 3 | `spec-features/admin/notify-setting/feature-spec.md` §5 **BR-02** | Spec chỉ mô tả 1 điểm auto-create (`NotifySettingController@getNotifySetting`) với bộ giá trị mặc định | Thực tế có **4 điểm tạo** `notify_setting` (thêm `BotController::step2Check`, `saveNotifySettings:343`, `StaffManagementController::createNotifyDefault:577`), mỗi điểm có bộ mặc định riêng — spec không mô tả bộ mặc định "~40 cột" của luồng kết nối bot | Bổ sung vào FA-006 danh sách đầy đủ các điểm tạo + bộ giá trị mặc định của từng điểm. Đây chính là chuẩn để đánh giá `TC-REGSHARED001-06` (`G1`) |
| 4 | Chưa có spec / runbook | Không có tài liệu nào định nghĩa: (a) có phải dọn các dòng `mobile_notify` trùng đã sinh trước fix không; (b) yêu cầu backup trước khi dedup; (c) ngưỡng thời gian khoá bảng chấp nhận được khi chạy migration không bật maintain | `G7`, `G4`/`Q1`, `Q4`/`G3` — cả 3 đều không có chuẩn để viết `Kết quả mong đợi` dứt khoát | **Leader + Dev chốt runbook deploy** cho ticket này trước khi chạy §5, và ghi lại thành tài liệu (3 điểm a/b/c ở trên) |
| 6 | `spec-features/admin/bot-add-v2/feature-spec.md` (FA-042) | Spec mô tả **duy nhất** wizard 5 roadStep từ `/admin/bot-add-v2`, không có màn chọn phương thức kết nối. Thực tế [manual LME](https://lme.jp/manual/register/) ghi rõ **2 cách**: 「1ステップずつ順番に設定」 (~15–20 phút) và 「クイック接続」 (~5–10 phút, *"1画面で設定が完結可能"*). LME-SYSTEM-SPEC còn liệt kê route thứ ba `/admin/bot-add` (FA-032) chưa có folder trong `spec-features/` | Ma trận 4 luồng ở §5 — không có chuẩn để biết 「クイック接続」 đi qua điểm tạo nào. `03-dev-impact.md` mục 3 liệt kê 8 điểm liên quan, **không điểm nào thuộc luồng quick connect** | **Hỏi Dev trước khi chạy §5**: 「クイック接続」 và `/admin/bot-add` (FA-032) có dùng chung `BotController::step2Check` với wizard không? Nếu **không**, điểm tạo `notify_setting` của các luồng đó nằm ở đâu và đã sửa chưa — vì bản vá chỉ chạm **một** điểm tạo. Sau đó bổ sung 「クイック接続」 vào spec FA-042 |
| 5 | `03-dev-impact.md` mục 7 (Tự review AI) | *"Điểm tạo lúc nhân viên nhận lời mời (**không sửa** theo quyết định human) sẽ báo lỗi trùng khoá nếu điều kiện tồn tại của nó trượt"* — rủi ro được **nêu và chấp nhận**, nhưng không nói hành vi nào là đúng | `NEW-18` và `NEW-10` phải để expected dạng "nếu lỗi thì đánh Không đạt và nhờ leader quyết định" (§4 issue #7) | **Dev chốt**: sau khi có unique index, `createNotifyDefault` được phép ném `Duplicate entry` hay bắt buộc phải bắt lỗi? Chốt xong mới sửa expected của `NEW-18` / `NEW-10` trên Studio |
