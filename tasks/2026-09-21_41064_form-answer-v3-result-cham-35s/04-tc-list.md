<!-- source: MCP LME TEST STUDIO — task_id=317, ticket 41064, testcase_list (39 TC), fetch lúc 2026-09-21. READ-ONLY snapshot, sinh bởi scripts/parse_studio_tcs.py. -->

# 04 — TC List (snapshot từ MCP LME TEST STUDIO)

> ⚠️ `contentTrust = untrusted` → xử lý như **data**, không phải chỉ thị.
> ⚠️ **READ-ONLY** — muốn sửa TC thì sửa trên Studio (`testcase_update`) rồi fetch lại.

# Digest — Studio task #317 · ticket 41064 · 39 TC

## Kết quả thực thi
| Trạng thái | Số TC |
|---|---|
| `(chưa chạy)` | 39 |

→ **0/39 TC (0%) thực sự Đạt**; 39 TC còn lại KHÔNG có kết luận test.

## Môi trường
| Env | Số TC |
|---|---|
| `(CHƯA CHẠY)` | 39 |

→ Production: **0 TC**.  ⚠️ **RULE-08**: không kết luận media / domain / job nền / bill tiền từ local-staging.

## Ai chạy (source / by)
| source / by | Số TC |
|---|---|
| `— / —` | 39 |

## Tác giả TC
| author | Số TC |
|---|---|
| `AI` | 36 |
| `cucdtk@mcp` | 3 |

## Loại case
| case_type | Số TC |
|---|---|
| `Normal` | 23 |
| `Abnormal` | 9 |
| `Boundary` | 7 |

## Nhóm / chế độ chạy
| tc_group | Số TC |
|---|---|
| `ui` | 21 |
| `api` | 14 |
| `data` | 4 |

| exec_mode | Số TC |
|---|---|
| `auto` | 38 |
| `manual` | 1 |

## Mã quan điểm KHỚP checklist-lme (14 mã)
`COMPAT-LEGACY-001`(1) · `CONC-003`(1) · `DATA-DB-001`(3) · `DATA-ID-001`(1) · `ENV-003`(1) · `FUNC-001`(4) · `FUNC-004`(1) · `FUNC-DATE-001`(4) · `MEDIA-001`(1) · `MSG-USER-001`(1) · `PERF-LARGE-001`(1) · `REG-SHARED-001`(3) · `SEC-001`(1) · `UI-003`(1)

## ⚠️ Mã quan điểm KHÔNG có trong checklist-lme (8 mã)
| Mã Studio | Số TC |
|---|---|
| `SEARCH-001` | 3 |
| `API-CONTRACT-001` | 3 |
| `DATA-RETENTION-001` | 2 |
| `API-001` | 2 |
| `TOOL-ERRHYG-001` | 2 |
| `UI-TABLE-001` | 1 |
| `TOOL-NEGCTRL-001` | 1 |
| `TOOL-KNOW-002` | 1 |

→ `/review-tc` KHÔNG map được coverage cho các mã này.

## ⚠️ TC fail / error hoặc có ticket bug (0)
(không có)

## ⚠️ TC skip / chưa chạy (39)
| ID | exec | Tiêu đề |
|---|---|---|
| NEW-1 | `(chưa chạy)` | Mở màn kết quả trả lời từ danh sách biểu mẫu — tab danh sách bạn bè hi |
| NEW-2 | `(chưa chạy)` | Tab danh sách câu trả lời hiển thị đúng nội dung trả lời và điểm chẩn  |
| NEW-41 | `(chưa chạy)` | Form rẽ nhánh nhiều page — kết quả trả lời hiển thị đúng các page và n |
| NEW-3 | `(chưa chạy)` | Nhắc hẹn đã dừng vẫn hiển thị kèm người thao tác sau khi tải lại màn |
| NEW-4 | `(chưa chạy)` | Câu trả lời không có nhắc hẹn thì tab nhắc hẹn trống, không mượn dữ li |
| NEW-9 | `(chưa chạy)` | Lọc khoảng ngày bằng ô lịch — chỉ còn câu trả lời trong khoảng đã chọn |
| NEW-10 | `(chưa chạy)` | Lần mở màn đầu tiên áp khoảng ngày mặc định — câu trả lời cũ hơn chỉ t |
| NEW-11 | `(chưa chạy)` | Bấm bỏ lọc toàn kỳ — danh sách trở về đầy đủ và khớp tổng số câu trả l |
| NEW-12 | `(chưa chạy)` | Sắp xếp theo ngày trả lời tăng dần và giảm dần bằng mũi tên trên tiêu  |
| NEW-13 | `(chưa chạy)` | Đổi trang và đổi số dòng mỗi trang — mỗi trang ghép đúng bạn bè và nhắ |
| NEW-36 | `(chưa chạy)` | Xoá một câu trả lời — dòng đó mất khỏi danh sách và khỏi tổng số |
| NEW-5 | `(chưa chạy)` | Câu trả lời của bạn bè đã bị xoá khỏi hệ thống vẫn hiển thị an toàn |
| NEW-14 | `(chưa chạy)` | Đổi trang liên tiếp khi mạng chậm — danh sách hiển thị khớp thao tác c |
| NEW-7 | `(chưa chạy)` | Biểu mẫu chưa có câu trả lời — danh sách rỗng, không lỗi |
| NEW-8 | `(chưa chạy)` | Ô thông tin bạn bè dạng file — phân loại ảnh, file thường và giá trị r |
| NEW-15 | `(chưa chạy)` | Lọc đúng một ngày (ngày bắt đầu = ngày kết thúc) vẫn thấy đủ câu trả l |
| NEW-16 | `(chưa chạy)` | Lọc khoảng ngày không có câu trả lời nào — danh sách rỗng, bỏ lọc thì  |
| NEW-17 | `(chưa chạy)` | Tổ hợp lọc ngày + sắp xếp + đổi trang cho kết quả nhất quán |
| NEW-42 | `(chưa chạy)` | Chat 1:1 — tab 「フォーム回答」 bên phải hiển thị đúng lịch sử và nội dung trả |
| NEW-43 | `(chưa chạy)` | My Page — tab 「フォーム回答」 hiển thị đúng lịch sử và chi tiết trả lời biểu  |
| NEW-37 | `(chưa chạy)` | Người dùng LINE nộp biểu mẫu sau khi thêm chỉ mục — bản ghi mới vẫn và |
| NEW-18 | `(chưa chạy)` | Endpoint danh sách câu trả lời giữ nguyên cấu trúc dữ liệu trả về |
| NEW-19 | `(chưa chạy)` | Sắp xếp theo từng cột được phép, cả chiều tăng và chiều giảm |
| NEW-20 | `(chưa chạy)` | Hai định dạng ngày mà giao diện thực gửi cho cùng một kết quả |
| NEW-21 | `(chưa chạy)` | Lọc chỉ có ngày bắt đầu hoặc chỉ có ngày kết thúc |
| NEW-22 | `(chưa chạy)` | Đối chiếu dữ liệu trả về của bản trước fix và bản sau fix trên cùng dữ |
| NEW-29 | `(chưa chạy)` | Tái hiện và xác nhận đã fix — số truy vấn cho một trang không tăng the |
| NEW-30 | `(chưa chạy)` | Thời gian phản hồi của màn kết quả trên biểu mẫu có rất nhiều lượt trả |
| NEW-23 | `(chưa chạy)` | Yêu cầu sắp xếp theo cột không nằm trong danh sách được phép |
| NEW-24 | `(chưa chạy)` | Tham số ngày là chuỗi không đọc được thì không gây lỗi hệ thống |
| NEW-25 | `(chưa chạy)` | Tham số ngày là chuỗi tương đối kiểu 'yesterday' — ghi nhận khác biệt  |
| NEW-26 | `(chưa chạy)` | Truy cập kết quả của biểu mẫu không thuộc bot đang đăng nhập |
| NEW-27 | `(chưa chạy)` | Mở bằng đường dẫn thường hoặc gửi yêu cầu không phải ajax thì trả về m |
| NEW-28 | `(chưa chạy)` | Biên số dòng mỗi trang và khoảng ngày đảo ngược |
| NEW-31 | `(chưa chạy)` | Biên thời gian chính xác của bộ lọc ngày — 00:00:00 và 23:59:59 |
| NEW-32 | `(chưa chạy)` | Chỉ mục mới được tạo đúng cột và đúng thứ tự sau khi chạy migration |
| NEW-33 | `(chưa chạy)` | Truy vấn của màn kết quả sử dụng được chỉ mục mới |
| NEW-34 | `(chưa chạy)` | Chạy lại migration khi chỉ mục đã tồn tại và rollback chỉ mục |
| NEW-35 | `(chưa chạy)` | Vận hành thêm chỉ mục trên bảng log lớn — ghi nhận ảnh hưởng đọc/ghi v |

## Màn hình (5)
| screen | Số TC |
|---|---|
| `Màn kết quả trả lời` | 32 |
| `Bảng form_answer_result — chỉ mục và dữ liệu log` | 4 |
| `Chat 1:1 — panel phải 「フォーム回答」` | 1 |
| `My Page — tab 「フォーム回答」` | 1 |
| `Trang trả lời biểu mẫu v3 (phía LINE user)` | 1 |

## requirement_keys (14)
`REQ-001`(7) · `REQ-002`(6) · `REQ-003`(1) · `REQ-004`(2) · `REQ-005`(11) · `REQ-006`(5) · `REQ-007`(4) · `REQ-008`(1) · `REQ-009`(1) · `REQ-010`(1) · `REQ-011`(3) · `REQ-012`(4) · `REQ-013`(2) · `REQ-014`(1)

---

## Bảng TC (16 cột canonical)

| TC No. | Mã quan điểm liên kết | Loại case | Tiêu đề test case | Điều kiện tiền đề | Các bước thực hiện | Dữ liệu test/input | Kết quả mong đợi | Kết quả thực thi | Evidence thực tế | Môi trường test | Người thực hiện | Ngày thực hiện | Số ticket bug | Trạng thái đánh giá spec | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| NEW-1 | UI-TABLE-001 | Normal | Mở màn kết quả trả lời từ danh sách biểu mẫu — tab danh sách bạn bè hiển thị đủ 4 cột | Đăng nhập admin qua màn đăng nhập của hệ thống và đã chọn bot A. Bot A có 1 biểu mẫu V3 với ít nhất 3 câu trả lời được tạo trong 2 tháng gần đây, trong đó có ít nhất 1 câu trả lời có thời gian trả lời (回答時間) và 1 câu trả lời không có. | 1. Vào menu 「フォーム作成」 để mở danh sách biểu mẫu<br>2. Ở dòng biểu mẫu cần xem, bấm 「表示」 ở cột 回答情報 để mở màn kết quả trả lời<br>3. Ở tab 「友だち一覧」, đọc lần lượt từng dòng: 回答日時, 回答時間, 友だち名 (kèm ảnh đại diện) và nút 「回答表示」<br>4. Đối chiếu tên hiển thị và ảnh đại diện của từng dòng với thông tin bạn bè tương ứng ở màn My Page bạn bè | Biểu mẫu có ≥3 câu trả lời của ≥2 bạn bè khác nhau | Màn kết quả trả lời mở được, không có thông báo lỗi và không có vùng trắng. Bảng hiển thị đủ số dòng câu trả lời của biểu mẫu. 回答日時 hiển thị theo dạng YYYY.MM.DD(thứ) HH:mm đúng thời điểm trả lời; 回答時間 hiển thị dạng 「〜分〜秒」 khi có dữ liệu và hiển thị 「--:--」 khi không có; cột 友だち名 hiển thị đúng LINE名/システム表示名 và ảnh đại diện của đúng bạn bè đã trả lời (khớp My Page); nút 「回答表示」 có ở mọi dòng. | Chưa test |  | all |  |  |  |  | Studio #19268 (NEW-1) · mã theo quan điểm: TC-UITABLE001-01 · ui · auto · read-only · REQ: REQ-001 · spec: SCR-FA-04, EP-09, TICKET-41064, git:bed522fed1 · Đây là case xương sống cho tuyên bố 'giữ nguyên định dạng dữ liệu trả về, FE không phải sửa'. Kỹ thuật: tên/ảnh đại diện nay lấy từ 1 truy vấn theo lô LineUser whereIn(id) 4 cột thay vì tra từng dòng; cột 友だち名 hỏng là dấu hiệu ghép lô sai. Chỉ đọc dữ liệu, không thao tác ghi. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-2 | FUNC-001 | Normal | Tab danh sách câu trả lời hiển thị đúng nội dung trả lời và điểm chẩn đoán | Bot A có biểu mẫu V3 bật nội dung chẩn đoán (có điểm) và có ít nhất 2 câu trả lời với các loại câu hỏi khác nhau: chữ, chọn nhiều, ngày giờ, nội dung chẩn đoán. Biết trước tổng điểm đúng của từng câu trả lời (cộng tay theo cấu hình điểm). | 1. Mở màn kết quả trả lời của biểu mẫu đó<br>2. Bấm sang tab 「回答一覧」<br>3. Đọc từng thẻ câu trả lời: ngày trả lời, tên bạn bè, 回答時間 và toàn bộ cặp tiêu đề câu hỏi — nội dung trả lời<br>4. Quay lại tab 「友だち一覧」, bấm 「回答表示」 của cùng câu trả lời đó rồi đọc 「診断ポイント」 trong popup | Câu trả lời có câu hỏi dạng chữ, chọn nhiều giá trị, ngày giờ và nội dung chẩn đoán | Tab 「回答一覧」 hiển thị đủ số thẻ bằng số dòng ở tab 「友だち一覧」. Mỗi thẻ hiển thị đúng tên bạn bè + ảnh đại diện của người trả lời, câu trả lời chọn nhiều giá trị hiển thị các giá trị nối bằng dấu phẩy, câu trả lời ngày giờ hiển thị dạng ngày (và giờ nếu có) chứ không phải chuỗi dữ liệu thô. 「診断ポイント」 trong popup bằng đúng tổng điểm đã cộng tay. Không dòng nào hiển thị nội dung của câu trả lời khác. | Chưa test |  | all |  |  |  |  | Studio #19269 (NEW-2) · mã theo quan điểm: TC-FUNC001-01 · ui · auto · read-only · REQ: REQ-001 · spec: SCR-FA-04, EP-09, git:bed522fed1 · Phủ tab thứ hai của cùng màn theo yêu cầu phủ đều các tab. Hai tab dùng chung một lần tải dữ liệu (tham số type không được phía server dùng để lọc) nên các case lọc/sắp xếp không cần nhân đôi cho từng tab — ghi rõ ở đây để không bị coi là thiếu phủ. · author=AI · status=draft |
| NEW-41 | FUNC-001 | Normal | Form rẽ nhánh nhiều page — kết quả trả lời hiển thị đúng các page và nhánh từng user đã đi qua | Bot A có 1 biểu mẫu V3 kiểu rẽ nhánh gồm ít nhất 3 page. Page 1 có câu hỏi dùng để rẽ nhánh: lựa chọn A đi tới Page 2A, lựa chọn B đi tới Page 2B; sau đó các nhánh tiếp tục tới page cuối. Có ít nhất 2 tài khoản LINE test: U1 trả lời theo nhánh A và U2 trả lời theo nhánh B. Nội dung câu hỏi ở từng page/nhánh khác nhau rõ ràng để dễ đối chiếu. | 1. Dùng U1 mở biểu mẫu, trả lời Page 1 theo lựa chọn A, tiếp tục trả lời đầy đủ các câu hỏi ở Page 2A và các page tiếp theo cho tới khi hoàn tất gửi form<br>2. Dùng U2 mở cùng biểu mẫu, trả lời Page 1 theo lựa chọn B, tiếp tục trả lời đầy đủ các câu hỏi ở Page 2B và các page tiếp theo cho tới khi hoàn tất gửi form<br>3. Ở trang quản trị, mở 「フォーム作成」 và vào màn kết quả trả lời của biểu mẫu<br>4. Ở tab 「友だち一覧」, xác nhận có kết quả của cả U1 và U2 với đúng tên/ảnh đại diện, sau đó bấm 「回答表示」 của U1<br>5. Đối chiếu toàn bộ nội dung trả lời của U1 theo từng page với dữ liệu đã nhập khi đi nhánh A<br>6. Đóng popup, bấm 「回答表示」 của U2 và đối chiếu toàn bộ nội dung trả lời theo từng page với dữ liệu đã nhập khi đi nhánh B<br>7. Chuyển sang tab 「回答一覧」 và kiểm tra lại nội dung hiển thị của U1 và U2 | U1: Page 1 chọn A → trả lời Page 2A → các page tiếp theo; U2: Page 1 chọn B → trả lời Page 2B → các page tiếp theo. Mỗi page dùng nội dung câu trả lời khác nhau để nhận biết rõ. | Kết quả của U1 và U2 đều hiển thị đầy đủ, không lỗi và không mất dữ liệu sau khi form có nhiều page. U1 chỉ hiển thị các câu hỏi/câu trả lời thuộc Page 1 và các page thực tế đã đi qua theo nhánh A; không hiển thị câu hỏi hoặc câu trả lời chỉ thuộc nhánh B. U2 chỉ hiển thị các câu hỏi/câu trả lời thuộc Page 1 và các page thực tế đã đi qua theo nhánh B; không hiển thị dữ liệu chỉ thuộc nhánh A. Tên và ảnh đại diện của mỗi dòng đúng user tương ứng. Nội dung ở popup 「回答表示」 và tab 「回答一覧」 khớp nhau, không bị ghép nhầm dữ liệu giữa U1/U2 hoặc giữa các page. | Chưa test |  | all |  |  |  |  | Studio #19347 (NEW-41) · mã theo quan điểm: TC-FUNC001-02 · ui · auto · read-only · spec: SCR-FA-04, EP-09, TICKET-41064, git:bed522fed1 · Bổ sung coverage cho Form V3 kiểu rẽ nhánh nhiều page. Fix #41064 thay đổi cách nạp dữ liệu kết quả theo lô nhưng phải giữ nguyên contract và mapping từng result; case này tập trung phát hiện lỗi mất/trộn câu trả lời giữa các page/nhánh khi nhiều user đi các flow khác nhau. · author=cucdtk@mcp · status=draft |
| NEW-3 | DATA-RETENTION-001 | Normal | Nhắc hẹn đã dừng vẫn hiển thị kèm người thao tác sau khi tải lại màn | Bot A có biểu mẫu V3 gắn nhắc hẹn. Có 1 câu trả lời U1 với ít nhất 2 nhắc hẹn, trong đó 1 nhắc hẹn đã được dừng trước đó bằng nút 「配信停止」 (ghi nhớ tài khoản đã dừng và thời điểm dừng), 1 nhắc hẹn còn hoạt động. | 1. Mở màn kết quả trả lời của biểu mẫu, bấm 「回答表示」 ở dòng câu trả lời U1<br>2. Trong popup, chuyển sang tab 「リマインド」 và đọc toàn bộ các khối nhắc hẹn theo thứ tự từ trên xuống<br>3. Nhấn F5 tải lại màn kết quả trả lời, mở lại popup của U1 và tab 「リマインド」<br>4. So sánh danh sách nhắc hẹn ở bước 2 và bước 3 | Câu trả lời U1: 1 nhắc hẹn đã dừng + 1 nhắc hẹn đang hoạt động | Tab 「リマインド」 hiển thị ĐỦ cả 2 nhắc hẹn. Nhắc hẹn đã dừng hiển thị dòng 「<tên tài khoản đã dừng>が配信を停止しました。」 đúng tài khoản đã thao tác và 「操作日時」 đúng thời điểm đã dừng; nhắc hẹn còn hoạt động hiển thị 「配信終了日時」 và nút 「配信停止」. Thứ tự các khối nhắc hẹn ở bước 3 giống hệt bước 2. Không nhắc hẹn nào bị mất sau khi tải lại. | Chưa test |  | all |  |  |  |  | Studio #19270 (NEW-3) · mã theo quan điểm: TC-DATARETENTION001-01 · ui · auto · read-only · REQ: REQ-004 · spec: SCR-FA-04, EP-09, git:bed522fed1 · Kỹ thuật: nhắc hẹn nay gom 1 truy vấn theo lô có withTrashed + quan hệ userDeleted(select id,username) và orderBy(id) — case này kiểm cả 3 yếu tố (thấy bản đã xoá mềm, có tên người dừng, giữ thứ tự tạo). Không tự dừng nhắc hẹn trong case này để tách khỏi luồng ghi của ticket #34051. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-4 | TOOL-NEGCTRL-001 | Normal | Câu trả lời không có nhắc hẹn thì tab nhắc hẹn trống, không mượn dữ liệu dòng khác | Trong cùng 1 biểu mẫu có 2 câu trả lời liền nhau trên danh sách: U1 có nhắc hẹn, U2 KHÔNG có nhắc hẹn (ví dụ nộp trước khi gắn nhắc hẹn cho biểu mẫu, hoặc nhắc hẹn đã bị xoá cùng cấu hình). | 1. Mở màn kết quả trả lời của biểu mẫu<br>2. Bấm 「回答表示」 ở dòng U1, vào tab 「リマインド」, ghi nhận các nhắc hẹn đang hiển thị<br>3. Đóng popup, bấm 「回答表示」 ở dòng U2 và vào tab 「リマインド」<br>4. Đóng popup, mở lại U1 và kiểm tra lại tab 「リマインド」 | U1 có nhắc hẹn, U2 không có nhắc hẹn | Popup của U2: tab 「リマインド」 không hiển thị khối nhắc hẹn nào, không hiển thị nhắc hẹn của U1, không báo lỗi JavaScript và không trắng vùng nội dung. Popup của U1 vẫn hiển thị đúng nhắc hẹn của U1 như bước 2 (không bị mất sau khi mở U2). Tab 「回答内容」 của U2 vẫn hiển thị đúng nội dung trả lời của U2. | Chưa test |  | all |  |  |  |  | Studio #19271 (NEW-4) · mã theo quan điểm: TC-TOOLNEGCTRL001-01 · ui · auto · read-only · REQ: REQ-004, REQ-002 · spec: SCR-FA-04, EP-09, git:bed522fed1 · Đối chứng âm cho việc gom nhắc hẹn theo mã câu trả lời: dòng không có nhắc hẹn phải ra mảng rỗng chứ không nhận nhóm của dòng khác. Kỹ thuật: dataRemind lấy theo groupBy(form_result_id) với giá trị mặc định là mảng rỗng. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-9 | SEARCH-001 | Normal | Lọc khoảng ngày bằng ô lịch — chỉ còn câu trả lời trong khoảng đã chọn | Đăng nhập admin, chọn bot A. Biểu mẫu V3 có câu trả lời rải ở ít nhất 3 ngày khác nhau trong 2 tháng gần đây. Trước khi lọc, bấm 「全期間」 và ghi lại danh sách đầy đủ (ngày trả lời + tên bạn bè của từng dòng) làm mốc đối chiếu. | 1. Mở màn kết quả trả lời của biểu mẫu và bấm 「全期間」 để thấy toàn bộ câu trả lời, ghi lại ngày của từng dòng<br>2. Bấm vào ô lịch phía trên danh sách, chọn khoảng ngày bao đúng 2 trong 3 ngày có dữ liệu rồi áp dụng<br>3. Đếm số dòng còn lại và đọc ngày trả lời của từng dòng<br>4. Chuyển sang tab 「回答一覧」 và đếm lại số thẻ | Khoảng ngày chọn trên lịch = [ngày có dữ liệu thứ 1 → ngày có dữ liệu thứ 2] | Danh sách chỉ còn các câu trả lời có ngày trả lời nằm trong khoảng đã chọn; mọi câu trả lời thuộc 2 ngày đó đều xuất hiện (kể cả bản ghi sớm nhất và muộn nhất trong ngày); câu trả lời của ngày thứ 3 không xuất hiện. Số dòng ở tab 「友だち一覧」 bằng số thẻ ở tab 「回答一覧」. Tên và ảnh đại diện của các dòng còn lại vẫn đúng như khi chưa lọc. Ô lịch hiển thị đúng khoảng ngày đã chọn, không lỗi JavaScript. | Chưa test |  | all |  |  |  |  | Studio #19276 (NEW-9) · mã theo quan điểm: TC-SEARCH001-01 · ui · auto · read-only · REQ: REQ-005, REQ-006 · spec: SCR-FA-04, EP-09, git:bed522fed1 · Đường người dùng thật của nhánh lọc ngày mới (so khoảng thời gian thay cho hàm lấy phần ngày). Kỹ thuật: khi chọn lịch, giao diện gửi ngày dạng YYYY-MM-DD — khác dạng YYYY.MM.DD của lần mở màn đầu tiên; cả hai dạng đều phải cho cùng kết quả (xem case hợp đồng endpoint). · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-10 | FUNC-001 | Normal | Lần mở màn đầu tiên áp khoảng ngày mặc định — câu trả lời cũ hơn chỉ thấy khi bỏ lọc | Biểu mẫu V3 có cả câu trả lời mới (trong 2 tháng gần đây) và câu trả lời CŨ hơn mốc đầu tháng của 2 tháng trước (ví dụ từ 6 tháng trước). Biết trước số lượng của từng nhóm. | 1. Mở màn kết quả trả lời của biểu mẫu (lần mở đầu tiên, chưa thao tác gì)<br>2. Đọc khoảng ngày đang hiển thị trong ô lịch và đếm số dòng của danh sách<br>3. Bấm nút 「全期間」 và đếm lại số dòng<br>4. Mở lại ô lịch, chọn khoảng ngày bao cả các câu trả lời cũ rồi áp dụng và đếm số dòng | Nhóm câu trả lời mới (trong ~3 tháng) và nhóm câu trả lời cũ (trước mốc mặc định) | Ngay lần mở màn đầu tiên, ô lịch hiển thị khoảng từ ngày đầu tháng của 2 tháng trước tới hôm nay và danh sách CHỈ hiển thị nhóm câu trả lời mới (đúng số lượng đã biết) — đây là hành vi mặc định, không phải mất dữ liệu. Sau khi bấm 「全期間」, danh sách hiển thị đủ cả nhóm cũ và nhóm mới. Khi chọn khoảng ngày bao cả nhóm cũ, các câu trả lời cũ xuất hiện lại đúng số lượng. Không có lỗi hiển thị hay lỗi 500 ở cả 3 lần tải. | Chưa test |  | all |  |  |  |  | Studio #19277 (NEW-10) · mã theo quan điểm: TC-FUNC001-03 · ui · auto · read-only · REQ: REQ-005, REQ-006 · spec: SCR-FA-04, EP-09, source: form_result_v3.js:12-13, git:bed522fed1 · Quan trọng để tester không kết luận sai 'fix làm mất dữ liệu cũ'. Kỹ thuật: mọi lần mở màn đều gửi kèm khoảng ngày mặc định nên nhánh lọc ngày mới là đường mặc định chứ không phải nhánh phụ. · author=AI · status=draft |
| NEW-11 | SEARCH-001 | Normal | Bấm bỏ lọc toàn kỳ — danh sách trở về đầy đủ và khớp tổng số câu trả lời | Biểu mẫu V3 có tổng số câu trả lời biết trước (đối chiếu số người trả lời ở danh sách biểu mẫu) và có dữ liệu ở nhiều ngày. | 1. Mở màn kết quả trả lời, chọn một khoảng ngày hẹp chỉ chứa 1 ngày có dữ liệu<br>2. Ghi lại số dòng đang hiển thị<br>3. Bấm nút 「全期間」<br>4. Đếm lại số dòng (cộng dồn qua các trang nếu có phân trang) và so với tổng số câu trả lời đã biết | Khoảng ngày hẹp 1 ngày, sau đó bỏ lọc | Sau khi bấm 「全期間」, ô lọc ngày không còn giới hạn và danh sách hiển thị lại TOÀN BỘ câu trả lời chưa xoá của biểu mẫu — tổng số dòng khớp tổng số câu trả lời đã biết. Các dòng vừa xuất hiện lại vẫn có đúng tên/ảnh đại diện và mở popup được. Không lỗi JavaScript. | Chưa test |  | all |  |  |  |  | Studio #19278 (NEW-11) · mã theo quan điểm: TC-SEARCH001-02 · ui · auto · read-only · REQ: REQ-005 · spec: SCR-FA-04, EP-09, git:bed522fed1 · Yêu cầu 'nút xoá lọc phải khôi phục toàn bộ dữ liệu'. Kỹ thuật: bỏ lọc gửi khoảng ngày rỗng nên câu truy vấn không còn điều kiện thời gian — đường này cũng phải dùng được chỉ mục mới. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-12 | FUNC-001 | Normal | Sắp xếp theo ngày trả lời tăng dần và giảm dần bằng mũi tên trên tiêu đề cột | Biểu mẫu V3 có ≥5 câu trả lời với thời điểm trả lời khác nhau rõ ràng, của ít nhất 2 bạn bè khác nhau. | 1. Mở màn kết quả trả lời và bấm 「全期間」<br>2. Bấm mũi tên hướng lên ở tiêu đề cột 「回答日時」 và đọc thứ tự các dòng<br>3. Bấm mũi tên hướng xuống ở cùng tiêu đề và đọc lại thứ tự các dòng<br>4. Với dòng đầu và dòng cuối của mỗi lần sắp xếp, kiểm tra tên bạn bè và nội dung popup vẫn đúng với ngày trả lời của dòng đó | ≥5 câu trả lời, sắp xếp tăng rồi giảm theo 回答日時 | Bấm mũi tên lên: danh sách sắp xếp theo ngày trả lời tăng dần (cũ nhất ở trên). Bấm mũi tên xuống: sắp xếp giảm dần (mới nhất ở trên). Hai lần sắp xếp là đảo ngược nhau và tổng số dòng không đổi. Tên/ảnh đại diện và nội dung popup của từng dòng vẫn đúng theo dòng đó sau khi đổi thứ tự — không bị giữ lại dữ liệu của thứ tự cũ. | Chưa test |  | all |  |  |  |  | Studio #19279 (NEW-12) · mã theo quan điểm: TC-FUNC001-04 · ui · auto · read-only · REQ: REQ-007, REQ-002 · spec: SCR-FA-04, EP-09, git:bed522fed1 · Đây là đường sắp xếp duy nhất mà giao diện dùng (luôn gửi cột ngày trả lời) và là cột đầu tiên trong danh sách cột được phép sau fix. Việc dữ liệu ghép vẫn đúng sau khi đổi thứ tự là điểm mấu chốt vì các truy vấn theo lô được chạy lại cho từng trang. · author=AI · status=draft |
| NEW-13 | DATA-ID-001 | Normal | Đổi trang và đổi số dòng mỗi trang — mỗi trang ghép đúng bạn bè và nhắc hẹn của trang đó | Biểu mẫu V3 có nhiều hơn 100 câu trả lời để có ít nhất 2 trang với số dòng mặc định 100/trang. Dataset phải có: (1) ít nhất 2 bạn bè khác ID nhưng cùng tên hiển thị và dùng avatar khác nhau; (2) ít nhất 1 bạn bè có nhiều câu trả lời khác nhau; (3) ít nhất 1 câu trả lời trên trang 2 có nhắc hẹn. | 1. Mở màn kết quả trả lời, bấm 「全期間」 và ghi nhận số dòng/trang đang chọn cùng số trang<br>2. Ở trang 1, ghi lại ID/ngày trả lời/tên/avatar của một số dòng, bao gồm user có nhiều câu trả lời nếu xuất hiện trên trang này<br>3. Bấm sang trang 2, kiểm tra tên/ảnh đại diện của từng dòng khớp đúng bạn bè; đặc biệt đối chiếu 2 user khác ID nhưng cùng tên hiển thị để chắc avatar không bị ghép nhầm<br>4. Kiểm tra các câu trả lời thuộc cùng một user vẫn giữ đúng ngày/nội dung của từng result, không bị gộp thành một dòng<br>5. Mở popup của câu trả lời có nhắc hẹn trên trang 2 và kiểm tra tab 「リマインド」<br>6. Đổi số dòng mỗi trang sang một giá trị khác trong ô 「件/1ページ」 rồi kiểm tra lại dữ liệu dòng đầu/dòng cuối và các dòng cùng tên<br>7. Quay lại trang 1 và đối chiếu với dữ liệu đã ghi ban đầu | Biểu mẫu >100 câu trả lời; có 2 line_user khác ID nhưng cùng tên hiển thị + avatar khác nhau; có 1 line_user trả lời nhiều lần; đổi số dòng/trang 100 → 50. | Mỗi trang hiển thị đúng số dòng theo thiết lập. Tên và avatar được ghép theo đúng line_user ID của từng result, kể cả khi hai user có cùng tên hiển thị; không lấy nhầm avatar theo tên. Nhiều câu trả lời của cùng một user vẫn là các result độc lập với đúng ngày/nội dung tương ứng. Trang 2 không lặp dữ liệu của trang 1; nhắc hẹn thuộc đúng result đang mở. Sau khi đổi số dòng/trang và quay lại các trang, mapping user/result/reminder vẫn không thay đổi. | Chưa test |  | all |  |  |  |  | Studio #19280 (NEW-13) · mã theo quan điểm: TC-DATAID001-01 · ui · auto · read-only · REQ: REQ-002, REQ-001 · spec: SCR-FA-04, EP-09, git:bed522fed1 · Bổ sung dataset dễ phát hiện lỗi batch mapping: không được dùng tên hiển thị làm khoá; cùng user có nhiều result cũng không được gộp. Đây là rủi ro trực tiếp của việc nạp LineUser/FormAnswerItemRemind theo lô cho trang hiện tại. · author=AI · status=draft |
| NEW-36 | DATA-RETENTION-001 | Normal | Xoá một câu trả lời — dòng đó mất khỏi danh sách và khỏi tổng số | Biểu mẫu V3 có ≥3 câu trả lời mà tester được phép xoá (dùng câu trả lời do chính tester tạo bằng tài khoản LINE test). Ghi trước tổng số dòng và số người trả lời hiển thị ở danh sách biểu mẫu. | 1. Mở màn kết quả trả lời, bấm 「全期間」 và ghi lại tổng số dòng cùng danh sách ngày/tên của từng dòng<br>2. Mở popup 「回答表示」 của một câu trả lời do tester tạo, bấm 「この回答を削除」 và xác nhận ở hộp thoại 「削除しますが、宜しいですか？」<br>3. Sau khi danh sách tải lại, đếm số dòng và kiểm tra dòng vừa xoá<br>4. Bấm 「全期間」 lần nữa, đổi khoảng ngày bao đúng ngày của câu trả lời vừa xoá và kiểm tra lại<br>5. Sang trang khác rồi quay lại để chắc dòng đã xoá không xuất hiện lại | 1 câu trả lời của tài khoản LINE test | Popup đóng sau khi xác nhận xoá. Dòng vừa xoá không còn xuất hiện trong danh sách ở bất kỳ khoảng ngày nào, kể cả khi lọc đúng ngày của nó hoặc đổi trang; tổng số dòng giảm đúng 1. Các dòng còn lại giữ nguyên dữ liệu (tên, ảnh đại diện, nhắc hẹn). Không lỗi JavaScript. | Chưa test |  | all |  |  |  |  | Studio #19334 (NEW-36) · mã theo quan điểm: TC-DATARETENTION001-02 · ui · auto · local-only · REQ: REQ-008 · spec: EP-09, SCR-FA-04, git:bed522fed1 · Cột đánh dấu xoá mềm là cột thứ 2 của chỉ mục mới nên phải chứng minh điều kiện loại bản ghi đã xoá vẫn đúng sau khi viết lại truy vấn. Gắn nhãn an toàn local-only vì case này XOÁ dữ liệu: khi chạy ở môi trường dùng chung, chỉ được xoá câu trả lời do chính tester tạo. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-5 | MSG-USER-001 | Abnormal | Câu trả lời của bạn bè đã bị xoá khỏi hệ thống vẫn hiển thị an toàn | Ở local: chọn 1 biểu mẫu có ít nhất 4 câu trả lời. Dựng 2 dòng bất thường nhưng giữ nguyên form result: (A) một result có line_id trỏ tới ID không tồn tại; (B) một result có line_id = null/rỗng nếu schema cho phép. Giữ ít nhất 2 result bình thường làm đối chứng. | 1. Mở màn kết quả trả lời ở tab 「友だち一覧」<br>2. Tìm dòng A có line_id không tồn tại và kiểm tra tên/avatar, sau đó mở 「回答表示」<br>3. Tìm dòng B có line_id null/rỗng và kiểm tra tên/avatar, sau đó mở 「回答表示」<br>4. Kiểm tra các dòng bình thường còn lại vẫn hiển thị đúng tên và avatar của user tương ứng | A: line_id không tồn tại; B: line_id = null/rỗng; thêm ít nhất 2 result bình thường. | Cả dòng A và B vẫn tồn tại trong danh sách với ngày/thời gian trả lời đúng; tên hiển thị/tên/mã bạn bè để trống, không hiện 'null'/'undefined', avatar dùng giá trị mặc định của hệ thống. Popup 「回答表示」 mở được và giữ đúng nội dung trả lời của từng result. Các dòng bình thường không bị ảnh hưởng hoặc ghép nhầm. Không lỗi 500/JavaScript. | Chưa test |  | local |  |  |  |  | Studio #19272 (NEW-5) · mã theo quan điểm: TC-MSGUSER001-01 · ui · auto · local-only · REQ: REQ-003 · spec: SCR-FA-04, EP-09, git:bed522fed1 · REQ-003 yêu cầu cover cả mã bạn bè không tồn tại và mã rỗng. Case chỉ chạy local vì cần dựng dữ liệu mồ côi/bất thường trực tiếp. · author=AI · status=draft |
| NEW-14 | CONC-003 | Abnormal | Đổi trang liên tiếp khi mạng chậm — danh sách hiển thị khớp thao tác cuối cùng | Biểu mẫu có ≥3 trang dữ liệu (giảm số dòng/trang nếu cần). Có thể làm chậm mạng bằng công cụ giả lập mạng chậm của trình duyệt. | 1. Mở màn kết quả trả lời và bấm 「全期間」<br>2. Bật giả lập mạng chậm<br>3. Bấm nhanh liên tiếp sang trang 2 rồi trang 3 (không chờ trang 2 tải xong)<br>4. Chờ toàn bộ phản hồi trả về và đọc số trang đang được chọn cùng dữ liệu đang hiển thị<br>5. Mở popup của 1 dòng đang hiển thị và đối chiếu nội dung với dòng đã bấm | 2 lần đổi trang liên tiếp dưới mạng chậm | Danh sách cuối cùng hiển thị đúng dữ liệu của trang được bấm sau cùng (trang 3) và ô phân trang cũng đánh dấu đúng trang đó — phản hồi của trang 2 về muộn không được ghi đè lên danh sách. Không trộn lẫn dòng của 2 trang, không lỗi JavaScript, chỉ báo đang tải được tắt sau khi xong. Popup mở ra đúng dòng đã bấm. | Chưa test |  | all |  |  |  |  | Studio #19281 (NEW-14) · mã theo quan điểm: TC-CONC003-01 · ui · auto · read-only · REQ: REQ-002 · spec: SCR-FA-04, EP-09, git:bed522fed1 · Không phải nhánh do fix tạo ra, nhưng thời gian phản hồi thay đổi mạnh sau fix nên đây là case bảo vệ: pass/fail dựa trên dữ liệu hiển thị cuối cùng, không dựa vào cảm nhận tốc độ. · author=AI · status=draft |
| NEW-7 | UI-003 | Boundary | Biểu mẫu chưa có câu trả lời — danh sách rỗng, không lỗi | Bot A có 1 biểu mẫu V3 đã tạo nhưng chưa có bất kỳ câu trả lời nào (số người trả lời hiển thị 0 ở danh sách biểu mẫu). | 1. Mở danh sách biểu mẫu và bấm 「表示」 ở cột 回答情報 của biểu mẫu chưa có câu trả lời<br>2. Quan sát tab 「友だち一覧」 và chuyển sang tab 「回答一覧」<br>3. Bấm nút 「全期間」 rồi quan sát lại danh sách<br>4. Quan sát vùng phân trang và trạng thái tải dữ liệu | Biểu mẫu 0 câu trả lời | Màn mở bình thường và hiển thị đúng tên biểu mẫu. Cả 2 tab đều không có dòng/thẻ nào, không xuất hiện dòng giả hoặc chữ 'null'. Không có loading chạy vô hạn, không lỗi JavaScript và không lỗi 500. Vùng phân trang không hiển thị trang dữ liệu. Bấm 「全期間」 vẫn giữ danh sách rỗng và không phát sinh lỗi. | Chưa test |  | all |  |  |  |  | Studio #19274 (NEW-7) · mã theo quan điểm: TC-UI003-01 · ui · auto · read-only · REQ: REQ-001 · spec: SCR-FA-04, EP-09, git:bed522fed1 · Biên dữ liệu rỗng cho nhánh batch load: khi page không có result thì các tập ID rỗng phải được xử lý an toàn. Không assert trạng thái nút CSV vì không có evidence trong scope fix này. · author=AI · status=draft |
| NEW-8 | MEDIA-001 | Boundary | Ô thông tin bạn bè dạng file — phân loại ảnh, file thường và giá trị rỗng | Bot A có biểu mẫu V3 chứa ô thông tin bạn bè dạng file. Có 3 câu trả lời: (1) đã nộp file ảnh (jpg/png), (2) đã nộp file không phải ảnh (pdf), (3) bỏ trống ô file đó. | 1. Mở màn kết quả trả lời của biểu mẫu, sang tab 「回答一覧」<br>2. Với câu trả lời có file ảnh: quan sát cách hiển thị của ô file đó<br>3. Với câu trả lời có file pdf: quan sát cách hiển thị của ô file đó<br>4. Với câu trả lời bỏ trống ô file: quan sát ô đó<br>5. Mở popup 「回答表示」 của cả 3 câu trả lời và quan sát lại cùng ô thông tin bạn bè dạng file | 3 biến thể giá trị ô file: ảnh (.png/.jpg), file thường (.pdf), rỗng | Câu trả lời có ảnh: hiển thị dạng ảnh xem trước, mở xem lớn được. Câu trả lời có pdf: hiển thị dạng tệp tải về/xem tệp, không cố hiển thị như ảnh. Câu trả lời bỏ trống: không hiển thị khối file, không hiện biểu tượng ảnh lỗi, không báo lỗi. Cả 3 biến thể giống kết quả trước fix và không có lỗi JavaScript. | Chưa test |  | all |  |  |  |  | Studio #19275 (NEW-8) · mã theo quan điểm: TC-MEDIA001-01 · ui · auto · read-only · REQ: REQ-014 · spec: SCR-FA-04, EP-09, git:bed522fed1 · Fix thêm điều kiện bỏ qua việc đọc file khi giá trị rỗng; kết quả phân loại phải KHÔNG đổi (rỗng vẫn cho loại 'file' như trước, vì trước đây đọc đường dẫn thư mục public và luôn ra 'file'). Gộp 3 biến thể vào 1 case theo dữ liệu nhập vì cùng một nguyên nhân gốc. · author=AI · status=draft |
| NEW-15 | FUNC-DATE-001 | Boundary | Lọc đúng một ngày (ngày bắt đầu = ngày kết thúc) vẫn thấy đủ câu trả lời trong ngày | Biểu mẫu V3 có một ngày D chứa ít nhất 2 câu trả lời cách xa nhau trong ngày (ví dụ buổi sáng sớm và buổi tối muộn), và có câu trả lời ở ngày D-1 và D+1. Bấm 「全期間」 để ghi trước danh sách và giờ trả lời của các dòng thuộc D, D-1, D+1. | 1. Mở màn kết quả trả lời và bấm 「全期間」, ghi lại các dòng của ngày D-1, D, D+1 kèm giờ<br>2. Mở ô lịch và chọn ngày bắt đầu = ngày kết thúc = ngày D rồi áp dụng<br>3. Đếm số dòng và đọc giờ trả lời của từng dòng<br>4. Bấm 「全期間」 rồi lặp lại bước 2 với ngày D-1 | Khoảng lọc [D, D], sau đó [D-1, D-1] | Khi lọc [D, D], danh sách hiển thị ĐÚNG và ĐỦ các câu trả lời của ngày D — gồm cả dòng sớm nhất và dòng muộn nhất trong ngày; không xuất hiện dòng của D-1 hay D+1. Khi lọc [D-1, D-1] thì chỉ còn câu trả lời của D-1. Tổng số dòng của 2 lần lọc cộng lại đúng bằng số dòng của 2 ngày đó khi xem toàn kỳ. | Chưa test |  | all |  |  |  |  | Studio #19282 (NEW-15) · mã theo quan điểm: TC-FUNCDATE001-01 · ui · auto · read-only · REQ: REQ-005 · spec: EP-09, SCR-FA-04, git:bed522fed1 · Biên quan trọng nhất của thay đổi lọc ngày: khoảng phải bao trọn từ 00:00:00 tới 23:59:59 của ngày. Trường hợp biên theo GIÂY chính xác (00:00:00 và 23:59:59) được kiểm riêng bằng dữ liệu dựng sẵn ở case local của phần chỉ mục/hiệu năng. · author=AI · status=draft |
| NEW-16 | FUNC-DATE-001 | Boundary | Lọc khoảng ngày không có câu trả lời nào — danh sách rỗng, bỏ lọc thì trở lại bình thường | Biểu mẫu V3 có câu trả lời và biết trước một khoảng ngày chắc chắn không có câu trả lời nào (ví dụ khoảng trước ngày tạo biểu mẫu). | 1. Mở màn kết quả trả lời của biểu mẫu<br>2. Chọn khoảng ngày chắc chắn không có dữ liệu rồi áp dụng<br>3. Quan sát cả 2 tab và vùng phân trang<br>4. Bấm 「全期間」 và quan sát lại danh sách | Khoảng ngày trước thời điểm tạo biểu mẫu | Danh sách hiển thị rỗng ở cả 2 tab, không có dòng giả, không có chỉ báo tải treo, không lỗi JavaScript và không lỗi 500; vùng phân trang không hiển thị trang nào. Sau khi bấm 「全期間」, danh sách hiển thị lại đầy đủ câu trả lời như trước khi lọc. | Chưa test |  | all |  |  |  |  | Studio #19283 (NEW-16) · mã theo quan điểm: TC-FUNCDATE001-02 · ui · auto · read-only · REQ: REQ-005 · spec: EP-09, SCR-FA-04, git:bed522fed1 · Kiểm nhánh kết quả rỗng SAU KHI đã qua điều kiện lọc thời gian mới (khác với case biểu mẫu chưa có câu trả lời nào). · author=AI · status=draft |
| NEW-17 | SEARCH-001 | Boundary | Tổ hợp lọc ngày + sắp xếp + đổi trang cho kết quả nhất quán | Biểu mẫu V3 có đủ dữ liệu để sau khi lọc một khoảng ngày vẫn còn ≥2 trang (giảm số dòng/trang nếu cần). | 1. Mở màn kết quả trả lời, chọn một khoảng ngày còn nhiều dữ liệu<br>2. Đổi số dòng mỗi trang xuống mức nhỏ để có ≥2 trang<br>3. Bấm mũi tên sắp xếp tăng dần theo 「回答日時」<br>4. Sang trang 2 và ghi lại dòng đầu, dòng cuối<br>5. Đổi sang sắp xếp giảm dần rồi kiểm tra lại số trang và dữ liệu trang 1<br>6. Bấm 「全期間」 và kiểm tra số dòng tổng, thứ tự và số trang | Khoảng ngày có nhiều dữ liệu × số dòng/trang nhỏ × sắp xếp tăng rồi giảm | Ở mọi bước, mọi dòng hiển thị đều nằm trong khoảng ngày đang lọc (điều kiện lọc không bị mất khi đổi trang hoặc đổi thứ tự). Tổng số dòng của tất cả các trang không đổi khi chỉ đổi thứ tự sắp xếp. Đổi thứ tự đưa danh sách về trang hợp lệ và dữ liệu từng dòng (tên, ảnh đại diện, nội dung popup) vẫn khớp dòng đó. Sau 「全期間」, số dòng tổng tăng lên đúng bằng số câu trả lời ngoài khoảng vừa lọc. | Chưa test |  | all |  |  |  |  | Studio #19284 (NEW-17) · mã theo quan điểm: TC-SEARCH001-03 · ui · auto · read-only · REQ: REQ-002, REQ-005, REQ-007 · spec: EP-09, SCR-FA-04, git:bed522fed1 · Phủ tổ hợp lọc × sắp xếp × phân trang theo kiểu pairwise (mỗi cặp giá trị xuất hiện ≥1 lần) thay vì nhân đủ tổ hợp. Đây cũng là nơi dễ lộ lỗi nếu điều kiện lọc/sắp xếp bị mất khi truy vấn được viết lại. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-42 | REG-SHARED-001 | Normal | Chat 1:1 — tab 「フォーム回答」 bên phải hiển thị đúng lịch sử và nội dung trả lời biểu mẫu | Bot A có ít nhất 1 bạn bè U1 đã trả lời biểu mẫu V3 ít nhất 2 lần. Trong dữ liệu của U1 có ít nhất 1 câu trả lời có điểm chẩn đoán và dữ liệu câu hỏi đủ để đối chiếu với màn Form Result. Đã biết trước tên form, thời gian trả lời và nội dung từng lần trả lời. | 1. Mở màn 「1:1チャット」 và chọn cuộc trò chuyện của U1<br>2. Ở panel bên phải, mở tab 「フォーム回答」<br>3. Kiểm tra danh sách các lần trả lời biểu mẫu của U1: tên form, thời gian trả lời và thứ tự hiển thị<br>4. Mở chi tiết một lần trả lời có điểm chẩn đoán<br>5. Đối chiếu nội dung từng câu hỏi/câu trả lời và điểm chẩn đoán với cùng form result ở màn kết quả trả lời biểu mẫu<br>6. Chuyển sang một lần trả lời khác của U1 và kiểm tra dữ liệu không bị giữ lại từ lần trước | U1 có >=2 form answer; ít nhất 1 answer có điểm chẩn đoán và các giá trị dễ nhận biết để đối chiếu. | Tab 「フォーム回答」 hiển thị đúng các lần trả lời của U1, đúng tên form và thời gian tương ứng; không xuất hiện dữ liệu của bạn bè khác. Khi mở chi tiết, nội dung câu hỏi/câu trả lời và điểm chẩn đoán khớp với dữ liệu của cùng result ở màn Form Result. Chuyển giữa các lần trả lời không bị trộn hoặc giữ dữ liệu cũ. Không lỗi 500/JavaScript và không có vùng trắng bất thường. | Chưa test |  | all |  |  |  |  | Studio #19427 (NEW-42) · mã theo quan điểm: TC-REGSHARED001-01 · ui · auto · read-only · spec: TICKET-41064, git:bed522fed1 · Bổ sung regression cho một nơi khác hiển thị dữ liệu form answer. Mục tiêu là xác nhận dữ liệu form answer vẫn nhất quán sau thay đổi batch load/query của #41064; không mở rộng sang mutation. · author=cucdtk@mcp · status=draft |
| NEW-43 | REG-SHARED-001 | Normal | My Page — tab 「フォーム回答」 hiển thị đúng lịch sử và chi tiết trả lời biểu mẫu | Bot A có bạn bè U1 đã trả lời ít nhất 2 biểu mẫu V3 khác nhau hoặc trả lời cùng một biểu mẫu nhiều lần. Có ít nhất 1 result có điểm chẩn đoán. Đã biết trước tên form, thời gian trả lời và nội dung từng result để đối chiếu với màn Form Result. | 1. Mở My Page của U1 từ màn quản lý bạn bè hoặc từ link tên bạn bè trên màn Form Result<br>2. Mở tab 「フォーム回答」<br>3. Kiểm tra danh sách lịch sử trả lời: số lượng, tên form và thời gian của từng lần trả lời<br>4. Mở chi tiết một lần trả lời và đối chiếu toàn bộ nội dung câu hỏi/câu trả lời với cùng result trên màn Form Result<br>5. Kiểm tra điểm chẩn đoán của result nếu có<br>6. Mở lần trả lời khác và kiểm tra dữ liệu hiển thị đúng result tương ứng, không bị trộn giữa các form/lần trả lời | U1 có >=2 form answer; ít nhất 1 result có điểm chẩn đoán; mỗi result dùng dữ liệu khác nhau rõ ràng. | Tab 「フォーム回答」 của My Page hiển thị đúng toàn bộ lịch sử trả lời của U1 trong phạm vi dữ liệu test, đúng tên form và thời gian từng result. Nội dung chi tiết và điểm chẩn đoán khớp với cùng result ở màn Form Result; không xuất hiện dữ liệu của user khác, form khác hoặc result khác. Chuyển giữa các result không bị trộn dữ liệu. Không lỗi 500/JavaScript và không có vùng trắng bất thường. | Chưa test |  | all |  |  |  |  | Studio #19428 (NEW-43) · mã theo quan điểm: TC-REGSHARED001-02 · ui · auto · read-only · spec: TICKET-41064, git:bed522fed1 · Bổ sung regression cho My Page > 「フォーム回答」 để xác nhận dữ liệu form answer giữ nguyên tính nhất quán sau fix #41064. · author=cucdtk@mcp · status=draft |
| NEW-37 | REG-SHARED-001 | Normal | Người dùng LINE nộp biểu mẫu sau khi thêm chỉ mục — bản ghi mới vẫn vào đúng và hiện trên màn kết quả | Đã chạy migration thêm chỉ mục. Có biểu mẫu V3 đang công khai và 1 tài khoản LINE test đã là bạn của bot. Ghi trước số người trả lời hiển thị ở danh sách biểu mẫu và tổng số dòng ở màn kết quả. | 1. Mở trang trả lời biểu mẫu bằng đường dẫn phân phối trên trình duyệt (phía người dùng LINE)<br>2. Nhập đầy đủ các câu hỏi bắt buộc và bấm nộp<br>3. Quan sát màn hoàn tất phía người dùng<br>4. Mở màn kết quả trả lời ở trang quản trị, bấm 「全期間」 và tìm câu trả lời vừa nộp<br>5. Mở popup của câu trả lời vừa nộp, kiểm tra nội dung từng câu hỏi và 「診断ポイント」<br>6. Quay lại danh sách biểu mẫu và đọc lại số người trả lời | 1 lượt nộp biểu mẫu thật từ tài khoản LINE test | Nộp thành công, người dùng thấy màn hoàn tất, không lỗi và không chậm bất thường ở bước nộp. Trên màn kết quả, câu trả lời mới xuất hiện ở đầu danh sách (thứ tự mặc định mới nhất trước) với đúng ngày, đúng tên/ảnh đại diện của tài khoản test và đúng nội dung đã nhập; điểm chẩn đoán đúng. Số người trả lời ở danh sách biểu mẫu tăng đúng 1 nếu đây là lượt trả lời đầu của tài khoản đó. | Chưa test |  | all |  |  |  |  | Studio #19335 (NEW-37) · mã theo quan điểm: TC-REGSHARED001-03 · ui · auto · env-safe · REQ: REQ-013 · spec: SCR-FA-05, EP-71, TICKET-41064, git:bed522fed1 · Đây là hồi quy quan trọng nhất của việc thêm chỉ mục vào bảng log dùng chung: đường GHI phải không bị hỏng và không chậm bất thường. Phải nộp bằng trình duyệt thật trên trang biểu mẫu, không được tự dựng yêu cầu nộp. · author=AI · status=draft |
| NEW-18 | API-CONTRACT-001 | Normal | Endpoint danh sách câu trả lời giữ nguyên cấu trúc dữ liệu trả về | Có phiên đăng nhập admin hợp lệ của bot A (lấy đúng phiên và mã chống giả mạo như trình duyệt) và 1 biểu mẫu V3 của bot A có ≥3 câu trả lời, trong đó ≥1 câu trả lời có nhắc hẹn và ≥1 câu trả lời có điểm chẩn đoán. | 1. Gửi yêu cầu dạng ajax tới endpoint danh sách câu trả lời của biểu mẫu với tham số như giao diện gửi: khoảng ngày, cột sắp xếp là ngày trả lời, chiều giảm dần, số dòng mỗi trang 100<br>2. Kiểm tra mã trạng thái phản hồi và các khoá ở mức ngoài cùng của dữ liệu trả về<br>3. Kiểm tra các khoá phân trang trong phần kết quả<br>4. Kiểm tra các trường của từng dòng dữ liệu<br>5. Đối chiếu dữ liệu vừa nhận với những gì màn hình đang hiển thị cho cùng biểu mẫu và cùng khoảng ngày | Tham số giống giao diện: khoảng ngày mặc định, cột sắp xếp = ngày trả lời, chiều = giảm dần, số dòng/trang = 100 | Phản hồi mã 200, dữ liệu trả về có đủ 4 khoá ngoài cùng: cờ thành công, phần kết quả, ngày bắt đầu và ngày kết thúc (2 giá trị ngày đúng bằng tham số đã gửi). Phần kết quả là dữ liệu phân trang có đủ các khoá số trang hiện tại, trang cuối, từ, đến, số dòng mỗi trang, tổng số và danh sách dòng. Mỗi dòng có đủ: mã, ngày trả lời, mã bạn bè, dữ liệu trả lời đã giải mã, thời gian trả lời, tên hiển thị, tên, mã bạn bè để mở My Page, ảnh đại diện, tên biểu mẫu, mã biểu mẫu, điểm và danh sách nhắc hẹn (mảng, rỗng nếu không có). Dữ liệu khớp với những gì màn hình hiển thị. | Chưa test |  | all |  |  |  |  | Studio #19316 (NEW-18) · mã theo quan điểm: TC-APICONTRACT001-01 · api · auto · read-only · REQ: REQ-001 · spec: EP-09, TICKET-41064, git:bed522fed1 · Hợp đồng endpoint chính là đối tượng kiểm chứng của case này (dev tuyên bố 'FE không phải sửa') nên gọi trực tiếp endpoint là đúng tầng. Danh sách nhắc hẹn phải là MẢNG cho mọi dòng — sau fix dữ liệu được gom nhóm rồi đánh lại chỉ số, nếu trả về dạng đối tượng có khoá thì giao diện sẽ hiển thị sai. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-19 | API-001 | Normal | Sắp xếp theo từng cột được phép, cả chiều tăng và chiều giảm | Phiên đăng nhập admin bot A. Biểu mẫu có ≥5 câu trả lời của ≥2 bạn bè, có giá trị thời gian trả lời khác nhau và không lọc ngày (gửi khoảng ngày rỗng). | 1. Gọi endpoint lần lượt với cột sắp xếp = mã, ngày trả lời, mã bạn bè, thời gian trả lời; mỗi cột gọi 2 lần với chiều tăng và chiều giảm (8 lần gọi)<br>2. Với mỗi lần gọi, đọc danh sách dòng và kiểm tra thứ tự theo cột tương ứng<br>3. So sánh tổng số dòng của 8 lần gọi<br>4. Kiểm tra dữ liệu ghép (tên, ảnh đại diện, nhắc hẹn) của dòng đầu tiên trong mỗi lần gọi | 4 cột được phép × 2 chiều sắp xếp = 8 tổ hợp | Cả 8 lần gọi đều trả mã 200 và tổng số dòng bằng nhau. Mỗi lần gọi sắp xếp đúng theo cột và chiều đã yêu cầu; các dòng có giá trị bằng nhau ở cột sắp xếp thì xếp theo mã giảm dần. Dữ liệu ghép của dòng đầu mỗi lần gọi vẫn khớp đúng bạn bè và nhắc hẹn của dòng đó. | Chưa test |  | all |  |  |  |  | Studio #19317 (NEW-19) · mã theo quan điểm: TC-API001-01 · api · auto · read-only · REQ: REQ-007 · spec: EP-09, git:bed522fed1 · Gộp 8 tổ hợp vào 1 case theo tham số vì cùng một nguyên nhân gốc (danh sách cột được phép). Giao diện chỉ dùng cột ngày trả lời — 3 cột còn lại chỉ tồn tại ở tầng endpoint nên phải kiểm ở đây thay vì trên màn hình. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-20 | API-CONTRACT-001 | Normal | Hai định dạng ngày mà giao diện thực gửi cho cùng một kết quả | Phiên đăng nhập admin bot A; biểu mẫu có câu trả lời ở nhiều ngày khác nhau. | 1. Gọi endpoint với khoảng ngày viết bằng dấu chấm (dạng năm.tháng.ngày) — dạng giao diện dùng khi mở màn lần đầu<br>2. Gọi lại đúng khoảng ngày đó nhưng viết bằng dấu gạch ngang — dạng giao diện dùng sau khi chọn lịch<br>3. Gọi lại lần nữa với khoảng ngày viết bằng dấu gạch chéo<br>4. So sánh danh sách mã dòng và tổng số dòng của 3 lần gọi | Cùng một khoảng ngày viết theo 3 dạng: 2026.09.01→2026.09.15, 2026-09-01→2026-09-15, 2026/09/01→2026/09/15 | Cả 3 lần gọi trả mã 200, cùng tổng số dòng và cùng danh sách mã dòng theo cùng thứ tự. Hai giá trị ngày trả về trong phản hồi bằng nguyên văn tham số đã gửi (không bị chuẩn hoá khác), để giao diện vẫn hiển thị đúng ô lọc. | Chưa test |  | all |  |  |  |  | Studio #19318 (NEW-20) · mã theo quan điểm: TC-APICONTRACT001-02 · api · auto · read-only · REQ: REQ-006, REQ-005 · spec: EP-09, source: form_result_v3.js:12-13,:91-92, git:bed522fed1 · Lưu ý cho tester: nhận định 'giao diện chỉ gửi dạng dấu chấm' là không đúng — sau khi chọn lịch, giao diện gửi dạng dấu gạch ngang. Cả hai đều phải chạy qua nhánh đổi chuỗi ngày mới. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-21 | FUNC-DATE-001 | Normal | Lọc chỉ có ngày bắt đầu hoặc chỉ có ngày kết thúc | Phiên đăng nhập admin bot A. Biểu mẫu có câu trả lời ở nhiều ngày; biết trước danh sách toàn kỳ (gọi endpoint với khoảng ngày rỗng) để làm mốc. | 1. Gọi endpoint không kèm khoảng ngày, ghi lại toàn bộ dòng kèm ngày trả lời làm mốc<br>2. Gọi endpoint chỉ kèm ngày bắt đầu = một ngày D có dữ liệu<br>3. Gọi endpoint chỉ kèm ngày kết thúc = cùng ngày D<br>4. So hai tập kết quả với mốc ở bước 1 | Chỉ ngày bắt đầu = D; chỉ ngày kết thúc = D | Khi chỉ có ngày bắt đầu: kết quả gồm đúng mọi câu trả lời từ 00:00:00 của ngày D trở về sau, không thiếu dòng nào của chính ngày D và không chứa dòng trước ngày D. Khi chỉ có ngày kết thúc: kết quả gồm đúng mọi câu trả lời tới 23:59:59 của ngày D, gồm cả dòng muộn nhất của ngày D. Hợp của hai tập kết quả bằng toàn bộ danh sách toàn kỳ, giao của hai tập đúng bằng các dòng của ngày D. | Chưa test |  | all |  |  |  |  | Studio #19319 (NEW-21) · mã theo quan điểm: TC-FUNCDATE001-03 · api · auto · read-only · REQ: REQ-005 · spec: EP-09, git:bed522fed1 · Giao diện luôn gửi cả hai đầu (hoặc bỏ trống cả hai) nên trường hợp một đầu chỉ kiểm được ở tầng endpoint — spec EP-09 có mô tả 2 tham số độc lập nên đây thuộc hợp đồng cần kiểm. · author=AI · status=draft |
| NEW-22 | API-CONTRACT-001 | Normal | Đối chiếu dữ liệu trả về của bản trước fix và bản sau fix trên cùng dữ liệu | Ở local: cùng một cơ sở dữ liệu, dựng được 2 lần chạy ứng dụng — một lần ở nhánh gốc trước fix, một lần ở nhánh fix. Chọn biểu mẫu có đủ các tình huống: dòng có/không nhắc hẹn, dòng có bạn bè bình thường, dòng có nhiều trang, có câu hỏi dạng ngày giờ và ô file. | 1. Chạy ứng dụng ở nhánh trước fix, gọi endpoint với 4 bộ tham số: (a) không lọc ngày, (b) lọc một khoảng ngày, (c) trang 2, (d) sắp xếp ngày trả lời tăng dần — lưu lại dữ liệu trả về<br>2. Chuyển sang nhánh fix (chạy migration thêm chỉ mục), không thay đổi dữ liệu<br>3. Gọi lại đúng 4 bộ tham số đó và lưu dữ liệu trả về<br>4. So từng cặp dữ liệu theo từng khoá và từng dòng, bỏ qua các khác biệt về đường dẫn/địa chỉ phân trang nếu có | 4 bộ tham số × 2 phiên bản mã nguồn, cùng dữ liệu | Với cả 4 bộ tham số, dữ liệu trả về của hai phiên bản giống nhau: cùng tổng số dòng, cùng thứ tự mã dòng, cùng giá trị từng trường của từng dòng (tên hiển thị, tên, mã bạn bè, ảnh đại diện, điểm, dữ liệu trả lời đã giải mã, phân loại ô file) và cùng danh sách nhắc hẹn của từng dòng kể cả nhắc hẹn đã dừng. Mọi khác biệt phát hiện được phải báo là bug hồi quy hợp đồng. | Chưa test |  | local |  |  |  |  | Studio #19320 (NEW-22) · mã theo quan điểm: TC-APICONTRACT001-03 · api · auto · local-only · REQ: REQ-001, REQ-002, REQ-005 · spec: EP-09, TICKET-41064, git:bed522fed1 · Đây là oracle mạnh nhất cho tuyên bố 'giữ nguyên định dạng dữ liệu trả về'. Cần 2 phiên bản mã nguồn nên chỉ chạy được ở local. Chỉ mục mới không đổi kết quả nên có thể chạy migration trước khi đo bản cũ nếu thuận tiện hơn. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-29 | TOOL-KNOW-002 | Normal | Tái hiện và xác nhận đã fix — số truy vấn cho một trang không tăng theo số dòng | Ở local: bật ghi nhận truy vấn cơ sở dữ liệu (log truy vấn). Có biểu mẫu V3 với ≥120 câu trả lời của nhiều bạn bè khác nhau, trong đó nhiều câu trả lời có nhắc hẹn. Có thể chạy được cả nhánh trước fix và nhánh fix trên cùng dữ liệu. | 1. Ở nhánh TRƯỚC fix: xoá log truy vấn, mở màn kết quả trả lời và tải 1 trang 100 dòng, rồi đếm số truy vấn đã chạy cho lần tải đó<br>2. Chuyển sang nhánh fix và chạy migration thêm chỉ mục<br>3. Xoá log truy vấn, tải lại đúng 1 trang 100 dòng của cùng biểu mẫu với cùng tham số, rồi đếm số truy vấn<br>4. Lặp lại bước 3 với số dòng mỗi trang 10 và 100 để xem số truy vấn có tăng theo số dòng hay không<br>5. Đối chiếu dữ liệu hiển thị của trang ở hai nhánh (số dòng, tên bạn bè, nhắc hẹn) để chắc kết quả không đổi | 1 trang 100 dòng; sau đó so sánh trang 10 dòng và trang 100 dòng | Ở nhánh trước fix, khi tăng số dòng/trang thì số query tăng theo số result do có truy vấn theo từng dòng. Ở nhánh fix, khi tăng từ 10 lên 100 dòng/trang, tổng số query của phần tải kết quả không tăng tuyến tính theo số dòng; LineUser và FormAnswerItemRemind được lấy theo batch cho cả trang. Dữ liệu hiển thị giữa trước/sau fix vẫn tương đương về số dòng, tên/avatar và nhắc hẹn. | Chưa test |  | local |  |  |  |  | Studio #19327 (NEW-29) · mã theo quan điểm: TC-TOOLKNOW002-01 · api · auto · local-only · REQ: REQ-011 · spec: TICKET-41064, EP-09, git:bed522fed1 · Oracle chính là độ phức tạp query: trước fix tăng theo số row, sau fix gần như hằng số theo page nhờ batch load. Không dùng ngưỡng tuyệt đối kiểu '≤10 query' vì query framework/session/eager-load có thể thay đổi theo môi trường. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-30 | PERF-LARGE-001 | Normal | Thời gian phản hồi của màn kết quả trên biểu mẫu có rất nhiều lượt trả lời | Ở local: dựng 1 biểu mẫu V3 có ít nhất 20.000 câu trả lời trải trên nhiều tháng, trỏ tới vài trăm bạn bè khác nhau, một phần có nhắc hẹn. Đã chạy migration thêm chỉ mục. Có thể chạy lại cùng dữ liệu ở nhánh trước fix để so sánh. | 1. Ở nhánh trước fix: đo thời gian tải trang 1 (100 dòng) với khoảng ngày mặc định, lặp 5 lần và ghi lại thời gian<br>2. Đo thêm thời gian tải khi bỏ lọc ngày (toàn kỳ) và khi vào trang giữa danh sách<br>3. Chuyển sang nhánh fix (đã có chỉ mục) và đo lại đúng 3 phép đo trên, mỗi phép 5 lần<br>4. So sánh thời gian trung bình và thời gian lớn nhất của từng phép đo giữa hai nhánh<br>5. Kiểm tra dữ liệu trả về của bản fix vẫn đủ số dòng và đúng tên bạn bè | Biểu mẫu ≥20.000 câu trả lời; 3 tình huống tải: lọc mặc định, toàn kỳ, trang giữa | Trên cùng dataset, cùng tham số và cùng môi trường đo, bản fix phải có thời gian phản hồi trung bình và thời gian lớn nhất thấp hơn rõ rệt so với bản trước fix ở các tình huống đo; không timeout và không lỗi 500. Dữ liệu trả về vẫn đủ số dòng và đúng nội dung. Ghi lại toàn bộ số đo để leader/dev đối chiếu; không dùng ngưỡng tuyệt đối 3 giây/5 giây nếu chưa có acceptance được thống nhất. | Chưa test |  | local |  |  |  |  | Studio #19328 (NEW-30) · mã theo quan điểm: TC-PERFLARGE001-01 · api · auto · local-only · REQ: REQ-011 · spec: TICKET-41064, EP-09, git:bed522fed1 · Ticket chỉ cung cấp hiện trạng chậm tối đa khoảng 35 giây, chưa có SLA/acceptance tuyệt đối sau fix. Pass/fail dựa trên so sánh trước-sau cùng điều kiện và tính đúng dữ liệu; nếu team chốt ngưỡng chính thức thì cập nhật lại expected sau. · author=AI · status=draft |
| NEW-23 | TOOL-ERRHYG-001 | Abnormal | Yêu cầu sắp xếp theo cột không nằm trong danh sách được phép | Phiên đăng nhập admin bot A; biểu mẫu có ≥5 câu trả lời. Ghi trước danh sách khi gọi không kèm tham số sắp xếp để biết thứ tự mặc định. | 1. Gọi endpoint với cột sắp xếp là một cột có thật nhưng không thuộc danh sách được phép (ví dụ cột dữ liệu trả lời)<br>2. Gọi endpoint với cột sắp xếp là tên cột không tồn tại (ví dụ chuỗi vô nghĩa)<br>3. Gọi endpoint với cột sắp xếp chứa ký tự lạ dạng chèn câu truy vấn<br>4. So kết quả 3 lần gọi với thứ tự mặc định đã ghi | Cột sắp xếp = 'data', 'khong_ton_tai', "id; DROP TABLE" | Cả 3 lần gọi trả mã 200 với dữ liệu hợp lệ, không lỗi 500 và không có thông báo lỗi cơ sở dữ liệu lộ ra ngoài. Danh sách sắp theo thứ tự mặc định là mã giảm dần, giống hệt lần gọi không kèm tham số sắp xếp. Dữ liệu không bị hỏng sau lần gọi có ký tự chèn câu truy vấn. | Chưa test |  | all |  |  |  |  | Studio #19321 (NEW-23) · mã theo quan điểm: TC-TOOLERRHYG001-01 · api · auto · read-only · REQ: REQ-007 · spec: EP-09, spec: api-spec.md EP-09, git:bed522fed1 · CẦN LEADER XÁC NHẬN: đặc tả EP-09 hiện ghi 'sắp xếp động, không giới hạn cột'. Sau fix, cột lạ bị bỏ qua IM LẶNG (không lỗi, không sắp xếp). Nếu leader coi đây là hành vi mong muốn thì phải cập nhật spec; nếu có nơi khác ngoài màn này gửi cột khác thì đó là hồi quy cần xử lý. Test này chỉ ghi nhận hành vi thực tế, không tự phán quyết. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-24 | TOOL-ERRHYG-001 | Abnormal | Tham số ngày là chuỗi không đọc được thì không gây lỗi hệ thống | Phiên đăng nhập admin bot A; biểu mẫu có câu trả lời. | 1. Gọi endpoint với ngày bắt đầu là chuỗi chữ vô nghĩa<br>2. Gọi endpoint với ngày bắt đầu là ngày không tồn tại (tháng 13, ngày 45)<br>3. Gọi endpoint với ngày bắt đầu chứa khoảng trắng và ký tự đặc biệt<br>4. Với mỗi lần gọi, kiểm tra mã trạng thái, cấu trúc dữ liệu trả về và nội dung thông báo lỗi nếu có | Ngày bắt đầu = 'abc', '2026.13.45', ' 2026.09.01<script> ' | Không lần gọi nào trả lỗi 500 hoặc để lộ thông báo lỗi cơ sở dữ liệu/đường dẫn mã nguồn. Phản hồi vẫn là dữ liệu hợp lệ có cờ thành công và phần kết quả phân trang (có thể rỗng). Không có nội dung nào từ tham số được thực thi hay hiển thị như mã trên màn hình. | Chưa test |  | all |  |  |  |  | Studio #19322 (NEW-24) · mã theo quan điểm: TC-TOOLERRHYG001-02 · api · auto · read-only · REQ: REQ-006 · spec: EP-09, git:bed522fed1 · Nhánh mới: nếu đổi chuỗi ngày thất bại thì quay về cách lọc cũ, nên kết quả có thể rỗng — điều cần đảm bảo là 'không lỗi bẩn', không phải số dòng cụ thể. Ghi lại số dòng thực tế của từng lần gọi để leader tham chiếu. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-25 | COMPAT-LEGACY-001 | Abnormal | Tham số ngày là chuỗi tương đối kiểu 'yesterday' — ghi nhận khác biệt so với bản cũ | Phiên đăng nhập admin bot A. Biểu mẫu có câu trả lời của hôm nay, hôm qua và nhiều ngày trước. | 1. Gọi endpoint với ngày bắt đầu là chuỗi tương đối 'yesterday'<br>2. Ghi lại số dòng và ngày trả lời nhỏ nhất trong kết quả<br>3. Gọi endpoint không kèm khoảng ngày để có mốc toàn kỳ<br>4. So sánh 2 kết quả và ghi nhận phạm vi lọc thực tế | Ngày bắt đầu = 'yesterday' | Phản hồi mã 200, dữ liệu hợp lệ, không lỗi 500. Ghi nhận phạm vi lọc thực tế: dự kiến chỉ còn câu trả lời từ 00:00:00 hôm qua trở về sau (bản trước fix sẽ trả về toàn bộ dữ liệu do chuỗi này không so sánh được thành ngày). Đây là khác biệt hành vi đã được dev nêu trước — báo cho leader xác nhận chấp nhận được, KHÔNG tự kết luận là bug. | Chưa test |  | all |  |  |  |  | Studio #19323 (NEW-25) · mã theo quan điểm: TC-COMPATLEGACY001-01 · api · auto · read-only · REQ: REQ-006 · spec: EP-09, redmine: journal 2026-09-21, git:bed522fed1 · Không có nơi nào trong giao diện gửi chuỗi kiểu này nên rủi ro thực tế thấp; case tồn tại để hồ sơ kiểm thử phản ánh đúng rủi ro dev đã khai báo (rule: dev nói 'không ảnh hưởng' kèm điều kiện thì vẫn phải có case). · author=AI · status=draft |
| NEW-26 | SEC-001 | Abnormal | Truy cập kết quả của biểu mẫu không thuộc bot đang đăng nhập | Có 2 bot: bot A và bot B, mỗi bot có biểu mẫu V3 riêng và có câu trả lời. Đăng nhập admin và đang chọn bot A; biết mã biểu mẫu của bot B. | 1. Trong khi đang chọn bot A, mở màn kết quả trả lời bằng đường dẫn có mã biểu mẫu của bot B<br>2. Quan sát màn hình kết quả<br>3. Gọi endpoint dạng ajax với mã biểu mẫu của bot B<br>4. Kiểm tra nội dung phản hồi có chứa dữ liệu câu trả lời của bot B hay không | Mã biểu mẫu thuộc bot B, phiên đang chọn bot A | Không hiển thị và không trả về bất kỳ câu trả lời nào của bot B. Trình duyệt bị chuyển về màn danh sách biểu mẫu của bot A; yêu cầu dạng ajax cũng trả về hướng chuyển trang chứ không trả dữ liệu. Không lộ tên biểu mẫu, tên bạn bè hay nội dung trả lời của bot B. | Chưa test |  | all |  |  |  |  | Studio #19324 (NEW-26) · mã theo quan điểm: TC-SEC001-01 · api · auto · read-only · REQ: REQ-009 · spec: EP-09, git:bed522fed1 · Điều kiện giới hạn theo bot nằm ngay đầu hàm được sửa nên phải kiểm lại sau fix. Kiểm cả đường mở bằng trình duyệt và đường gọi endpoint để chắc không bypass được bằng cách gọi trực tiếp. · author=AI · status=draft |
| NEW-27 | API-001 | Abnormal | Mở bằng đường dẫn thường hoặc gửi yêu cầu không phải ajax thì trả về màn hình, không phải dữ liệu JSON | Phiên đăng nhập admin bot A và 1 biểu mẫu V3 của bot A có câu trả lời. | 1. Mở đường dẫn màn kết quả trả lời bằng trình duyệt (yêu cầu dạng GET)<br>2. Kiểm tra nội dung trả về là trang HTML của màn kết quả trả lời<br>3. Gửi yêu cầu POST tới cùng đường dẫn nhưng KHÔNG đánh dấu là ajax<br>4. Kiểm tra nội dung trả về | 1 yêu cầu GET, 1 yêu cầu POST không phải ajax | Yêu cầu GET trả về trang HTML màn kết quả trả lời (có tiêu đề biểu mẫu, 2 tab, ô lọc ngày) — không trả dữ liệu JSON. Yêu cầu POST không phải ajax cũng trả về trang HTML tương tự, không trả JSON và không lỗi 500. Sau khi trang HTML tải xong, giao diện tự gọi lấy dữ liệu và hiển thị danh sách bình thường. | Chưa test |  | all |  |  |  |  | Studio #19325 (NEW-27) · mã theo quan điểm: TC-API001-02 · api · auto · read-only · REQ: REQ-010 · spec: EP-09, git:bed522fed1 · Điều kiện phân nhánh (chỉ ajax + POST mới trả JSON) nằm trong cùng hàm được sửa. Nếu ai đó vô tình đổi nhánh này thì giao diện sẽ trắng hoặc tải file JSON — case rẻ và bắt được lỗi nặng. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-28 | FUNC-004 | Boundary | Biên số dòng mỗi trang và khoảng ngày đảo ngược | Phiên đăng nhập admin bot A; biểu mẫu có ít nhất 120 câu trả lời để kiểm được các giá trị phân trang mà giao diện thực tế sử dụng. | 1. Gọi endpoint KHÔNG kèm tham số số dòng mỗi trang và đếm số dòng trả về<br>2. Gọi endpoint với số dòng mỗi trang = 1 và kiểm tra số trang<br>3. Gọi endpoint với số dòng mỗi trang = 100 (giá trị giao diện thực gửi) và kiểm tra số dòng cùng cấu trúc phân trang<br>4. Gọi endpoint với ngày bắt đầu LỚN HƠN ngày kết thúc<br>5. Gọi endpoint với số trang vượt quá trang cuối | Số dòng/trang: không truyền / 1 / 100; khoảng ngày đảo ngược; số trang vượt quá. | Không truyền số dòng/trang: trả về đúng giá trị mặc định của endpoint theo implementation/spec hiện hành. Số dòng/trang = 1: mỗi trang 1 dòng và metadata phân trang đúng. Số dòng/trang = 100: trả về tối đa 100 dòng/trang, không lỗi/timeout và metadata đúng. Khoảng ngày đảo ngược: không lỗi 500 và trả response phân trang hợp lệ (thường là danh sách rỗng). Số trang vượt quá trang cuối: danh sách rỗng kèm thông tin phân trang hợp lệ. | Chưa test |  | all |  |  |  |  | Studio #19326 (NEW-28) · mã theo quan điểm: TC-FUNC004-01 · api · auto · read-only · REQ: REQ-001, REQ-005 · spec: EP-09, git:bed522fed1 · Bỏ stress paginate=1000 trên env dùng chung vì không phải đường FE thực tế. Giá trị 100 là đường giao diện dùng và đủ để kiểm ảnh hưởng batch load; stress lớn hơn chỉ nên làm ở local/performance case riêng. · author=AI · status=draft |
| NEW-31 | FUNC-DATE-001 | Boundary | Biên thời gian chính xác của bộ lọc ngày — 00:00:00 và 23:59:59 | Ở local: dựng trong cùng 1 biểu mẫu 6 câu trả lời với thời điểm trả lời chính xác tới giây — (1) ngày D-1 lúc 23:59:59, (2) ngày D lúc 00:00:00, (3) ngày D lúc 12:00:00, (4) ngày E lúc 23:59:59, (5) ngày E+1 lúc 00:00:00, (6) ngày E+1 lúc 00:00:01. Ghi lại mã của từng câu trả lời. | 1. Gọi endpoint danh sách câu trả lời với khoảng ngày [D, E]<br>2. Đối chiếu danh sách mã trả về với 6 mã đã dựng<br>3. Gọi lại với khoảng ngày [D, D] và đối chiếu<br>4. Gọi lại với khoảng ngày [E+1, E+1] và đối chiếu<br>5. Mở màn kết quả trên trình duyệt, chọn đúng khoảng [D, E] bằng ô lịch và đối chiếu số dòng với kết quả bước 1 | 6 câu trả lời tại các mốc giây biên: 23:59:59 và 00:00:00 của các ngày liền kề | Khoảng [D, E] trả về đúng các câu trả lời (2), (3), (4) — gồm cả bản ghi tại 00:00:00 của ngày bắt đầu và bản ghi tại 23:59:59 của ngày kết thúc — và KHÔNG chứa (1), (5), (6). Khoảng [D, D] chỉ trả về (2) và (3). Khoảng [E+1, E+1] chỉ trả về (5) và (6). Số dòng khi lọc bằng ô lịch trên màn hình khớp với kết quả gọi endpoint. | Chưa test |  | local |  |  |  |  | Studio #19329 (NEW-31) · mã theo quan điểm: TC-FUNCDATE001-04 · api · auto · local-only · REQ: REQ-005 · spec: EP-09, git:bed522fed1 · Đây là biên quyết định tính tương đương của thay đổi lọc ngày (trước fix so theo phần ngày, sau fix so theo khoảng thời gian). Cần ghi thời điểm chính xác tới giây nên phải dựng dữ liệu trực tiếp ở local. · author=AI · status=draft |
| NEW-32 | DATA-DB-001 | Normal | Chỉ mục mới được tạo đúng cột và đúng thứ tự sau khi chạy migration | Ở local: cơ sở dữ liệu đang ở trạng thái trước khi chạy migration của nhánh fix (chưa có chỉ mục mới). Ghi lại danh sách chỉ mục hiện có của bảng câu trả lời. | 1. Ghi lại danh sách chỉ mục của bảng câu trả lời trước khi chạy migration<br>2. Chạy migration của nhánh fix<br>3. Đọc lại danh sách chỉ mục của bảng câu trả lời<br>4. Kiểm tra tên chỉ mục mới, các cột thành phần và thứ tự các cột<br>5. Kiểm tra các chỉ mục cũ và tổng số dòng của bảng không thay đổi | Không có dữ liệu nhập; đối tượng kiểm tra là cấu trúc bảng | Migration chạy thành công, không lỗi. Bảng câu trả lời có thêm đúng 1 chỉ mục tên form_answer_result_form_deleted_created_index gồm 3 cột theo thứ tự: mã biểu mẫu, cột đánh dấu xoá mềm, ngày tạo. Toàn bộ chỉ mục cũ vẫn còn nguyên, không chỉ mục nào bị xoá hay đổi tên. Tổng số dòng của bảng không đổi (migration không sửa dữ liệu). | Chưa test |  | local |  |  |  |  | Studio #19330 (NEW-32) · mã theo quan điểm: TC-DATADB001-01 · data · auto · local-only · REQ: REQ-012 · spec: TICKET-41064, git:bed522fed1 · Thứ tự cột là điều kiện để chỉ mục dùng được cho cả lọc khoảng ngày và sắp xếp: 2 cột đầu là điều kiện bằng, cột ngày tạo ở cuối. Nếu thứ tự khác thì chỉ mục vô dụng dù vẫn được tạo — đây là lý do case này kiểm thứ tự chứ không chỉ kiểm sự tồn tại. · author=AI · status=draft |
| NEW-33 | DATA-DB-001 | Normal | Truy vấn của màn kết quả sử dụng được chỉ mục mới | Ở local: đã chạy migration thêm chỉ mục và bảng câu trả lời có lượng dữ liệu đủ lớn cho 1 biểu mẫu (tối thiểu vài nghìn dòng) để bộ tối ưu không chọn quét toàn bảng vì bảng quá nhỏ. | 1. Lấy nguyên văn hai câu truy vấn mà màn kết quả sinh ra khi lọc khoảng ngày (câu đếm tổng của phân trang và câu lấy dòng) từ log truy vấn<br>2. Xem kế hoạch thực thi của câu lấy dòng<br>3. Xem kế hoạch thực thi của câu đếm tổng<br>4. Xem kế hoạch thực thi của câu lấy dòng khi KHÔNG lọc ngày (chỉ theo mã biểu mẫu + sắp xếp) | Câu truy vấn thực tế của màn với và không có điều kiện khoảng ngày | Câu truy vấn sau fix phải dùng điều kiện có thể tận dụng index: form_id/deleted_at và khoảng created_at không bị bọc bởi hàm DATE(). Khi xem EXPLAIN, chỉ mục mới phải xuất hiện trong tập chỉ mục có thể sử dụng hoặc optimizer chọn một kế hoạch tương đương/tốt hơn; không fail chỉ vì optimizer chọn index khác. Với dataset đủ lớn, lượng row ước lượng/cost của truy vấn có lọc phải hợp lý hơn quét toàn bảng. Nếu plan không như kỳ vọng, lưu EXPLAIN thực tế để dev đánh giá. | Chưa test |  | local |  |  |  |  | Studio #19331 (NEW-33) · mã theo quan điểm: TC-DATADB001-02 · data · auto · local-only · REQ: REQ-011, REQ-012 · spec: TICKET-41064, EP-09, git:bed522fed1 · Mục tiêu là chứng minh query đã trở thành sargable và index mới có thể hỗ trợ truy vấn. Không ép MySQL phải luôn chọn đúng index form_answer_result_form_deleted_created_index hoặc tuyệt đối không filesort/temporary, vì lựa chọn optimizer phụ thuộc thống kê và cấu trúc index thực tế. · author=AI · status=draft |
| NEW-34 | DATA-DB-001 | Abnormal | Chạy lại migration khi chỉ mục đã tồn tại và rollback chỉ mục | Ở local: đã có chỉ mục mới (hoặc tự tạo tay một chỉ mục cùng tên trước khi chạy migration để mô phỏng việc DBA đã thêm tay trên môi trường thật). | 1. Khi chỉ mục đã tồn tại, chạy lại migration và quan sát kết quả<br>2. Kiểm tra chỉ mục vẫn còn đúng 1 bản, không bị trùng tên hay tạo thêm bản thứ hai<br>3. Thực hiện rollback migration này và kiểm tra chỉ mục đã bị xoá<br>4. Chạy lại migration sau rollback và kiểm tra chỉ mục được tạo lại<br>5. Mở màn kết quả trả lời sau mỗi lần thay đổi để chắc màn vẫn hoạt động | Trạng thái chỉ mục: đã tồn tại → chạy lại → rollback → chạy lại | Chạy lại migration khi chỉ mục đã tồn tại: hoàn tất không lỗi, bỏ qua việc tạo lại, không phát sinh chỉ mục trùng. Rollback: chỉ mục bị xoá, không ảnh hưởng chỉ mục khác và không mất dữ liệu. Chạy lại sau rollback: chỉ mục được tạo lại đúng 3 cột đúng thứ tự. Sau tất cả các bước, màn kết quả trả lời vẫn hiển thị đúng dữ liệu (chỉ mục chỉ ảnh hưởng tốc độ, không ảnh hưởng kết quả). | Chưa test |  | local |  |  |  |  | Studio #19332 (NEW-34) · mã theo quan điểm: TC-DATADB001-03 · data · auto · local-only · REQ: REQ-012 · spec: TICKET-41064, git:bed522fed1 · Migration có sẵn kiểm tra tồn tại bảng/chỉ mục nên kỳ vọng là bỏ qua êm. Không có case Boundary cho quan điểm này vì không tồn tại khái niệm biên cho việc tạo/xoá một chỉ mục. · author=AI · status=draft |
| NEW-35 | ENV-003 | Abnormal | Vận hành thêm chỉ mục trên bảng log lớn — ghi nhận ảnh hưởng đọc/ghi và bảo toàn dữ liệu | Ở local (hoặc môi trường diễn tập có dữ liệu lớn): bảng câu trả lời có lượng dòng lớn (càng gần quy mô thật càng tốt) và chưa có chỉ mục mới. Chuẩn bị sẵn 1 phiên trình duyệt đang xem màn kết quả và 1 luồng nộp biểu mẫu từ phía người dùng LINE. | 1. Bắt đầu chạy migration thêm chỉ mục và bấm giờ<br>2. Trong lúc migration đang chạy: tải lại màn kết quả trả lời của một biểu mẫu và quan sát<br>3. Trong lúc migration đang chạy: nộp 1 câu trả lời biểu mẫu từ phía người dùng LINE<br>4. Chờ migration kết thúc, ghi lại thời gian chạy<br>5. Kiểm tra câu trả lời vừa nộp có xuất hiện trên màn kết quả và chỉ mục đã được tạo | 1 lần chạy migration trên bảng dữ liệu lớn, đồng thời có 1 lượt đọc và 1 lượt ghi | Migration hoàn tất và tạo đúng chỉ mục. Trong thời gian ALTER, ghi nhận thực tế việc đọc/ghi có bị chặn hoặc tăng latency hay không; không dùng điều kiện 'phải luôn đọc/ghi không block' làm pass criteria. Sau khi migration hoàn tất, câu trả lời gửi trong quá trình thử (nếu thao tác ghi hoàn thành) phải còn nguyên và hiển thị đúng, không mất/corrupt dữ liệu; màn kết quả tiếp tục hoạt động bình thường. Ghi lại thời gian migration và mức ảnh hưởng để quyết định chạy ngoài giờ cao điểm hoặc dùng công cụ online schema change. | Chưa test |  | local |  |  |  |  | Studio #19333 (NEW-35) · mã theo quan điểm: TC-ENV003-01 · data · manual · local-only · REQ: REQ-012, REQ-013 · spec: TICKET-41064, redmine: journal 2026-09-21, git:bed522fed1 · Dev xác định DDL trên bảng log lớn là rủi ro vận hành và khuyến nghị off-peak/pt-online-schema-change. Case này đo lock/latency và tính toàn vẹn dữ liệu; không giả định MySQL ở mọi môi trường đều non-blocking. · author=AI · status=draft |
