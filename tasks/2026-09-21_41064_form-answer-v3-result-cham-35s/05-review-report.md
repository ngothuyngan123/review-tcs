# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | MCP LME TEST STUDIO — task #317 (ticket 41064, round 1, branch `ai_small_41064`) |
| Tổng số TC review | 39 |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `7/13 vùng ảnh hưởng đủ TC · 5 GAP · 2 RISK`

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | `T1 — Form Builder (FA-011): export CSV kết quả trả lời` (EP-19 `/ajax/download-answer/{id}`, `downloadViewer`) | `dev-impact` | `không có` | `GAP — 0 TC.` Studio `REQ-013` nêu đích danh "tải CSV kết quả ... vẫn đúng dữ liệu" nhưng không TC nào map. Đây là query khác cũng đọc `form_answer_result` — bảng vừa được thêm chỉ mục | `[BLOCKER]` |
| G2 | `T1 — đồng bộ Google Sheet của form` (BR-08, queue `form_answer_connect_googles`, cột `status_sync_sheet` trên chính bảng vừa thêm chỉ mục) | `diff code` | `không có` | `GAP — 0 TC.` Dev mục 3 + Studio `dev_impact` đều KHÔNG kê nhánh sync Google trong danh sách nơi dùng chung `form_answer_result` | `[MAJOR]` |
| G3 | `F6 — FormAnswerController::showFormResult` (màn kết quả **bản cũ**, `:4057`, endpoint khác, Dev ghi nhận **KHÔNG sửa**) | `diff code` | `không có` | `GAP — 0 TC.` Code không đổi nhưng **bảng đã đổi schema** → cần 1 TC hồi quy nhẹ xác nhận màn kết quả của form đời cũ (`using_old_version=1`) vẫn mở và hiển thị đúng sau migration | `[MAJOR]` |
| G4 | `D1 — form_answer_result: job nền / phiên đang mở trong lúc ALTER TABLE` | `diff code` | `NEW-35` | `GAP — NEW-35 chỉ mô phỏng 1 lượt đọc + 1 lượt ghi UI.` Không TC nào đặt **job nền đang chạy dở** (sync Google, job đếm lại số người trả lời) hoặc **màn đang mở từ trước khi deploy** vào lúc migration chạy | `[MAJOR]` |
| G5 | `D1 — chi phí GHI sau khi thêm chỉ mục` (bảng log dùng chung nhận INSERT mỗi lượt nộp form) | `diff code` | `NEW-37` | `GAP — NEW-37 chỉ verify bản ghi mới "vào đúng và không chậm bất thường" bằng cảm nhận.` Không TC nào **đo** độ trễ ghi trước/sau khi thêm chỉ mục — đây là chi phí cố hữu của việc thêm index lên bảng log lớn | `[MAJOR]` |
| G6 | `F4 + D2 — FormAnswerItemRemind::userDeleted` (gom theo lô `whereIn` + `withTrashed` + `groupBy` + đánh lại số thứ tự) | `dev-impact` | `NEW-3`, `NEW-4`, `NEW-13` | `RISK — 3 TC đều chỉ ĐỌC trạng thái có sẵn.` Không TC nào **thực hiện thao tác 「配信停止」** trên popup rồi tải lại để xác nhận: đúng nhắc hẹn đó bị dừng, nhắc hẹn khác của cùng câu trả lời giữ nguyên, và bản đã dừng vẫn về đúng dòng sau khi gom lô (vùng regression kho `TC-FORM-354`) | `[MAJOR]` |
| G7 | `BUG — request chậm max 35s trên production step.lme.jp` | `dev-impact` | `NEW-29`, `NEW-30`, `NEW-33` | `RISK — cả 3 TC đều chạy env local.` Mục tiêu của ticket là độ chậm **trên production**; đo trên local 20.000 dòng không kết luận được cho bảng log production. Xem Q1 | `[BLOCKER]` |

- **Vùng đã đủ TC, không ghi dòng**: `F1` showFormResultV3 · `F2` parseFormResultFilterDate · `F3` migration (tạo/chạy lại/rollback) · `F5` is_image · `D3` line_user (gom lô 4 cột, `NEW-13`/`NEW-5`) · `D4` bỏ query thừa lên `events` (bắt gián tiếp bởi `NEW-29` đếm query + `NEW-22` so hợp đồng trước/sau) · `T2` Friend Information FA-015.
- **4 câu hỏi adversarial chiều `diff code`** (nháp nội bộ — kết quả đã đổ vào bảng trên):
  1. *Mỗi điểm sửa có ≥1 TC đi qua?* — Có, cả 6 điểm sửa và cả 2 file.
  2. *Điều kiện mới có TC cho cả 2 nhánh?* — `parseFormResultFilterDate` parse OK: `NEW-20`/`NEW-21`/`NEW-31`. Parse fail → fallback: `NEW-24` **chỉ verify "không gây lỗi"**, chưa verify nhánh fallback trả về **đúng tập kết quả như cách lọc cũ** → đã ghi issue I7 ở §4 (bổ sung expected cho TC có sẵn, không đẻ TC mới). Không phải fix dạng generic catch → không áp rule ≥3 trigger.
  3. *Fix chạm thứ dùng chung → Dev có kê danh sách nơi ảnh hưởng?* — `form_answer_result` là **bảng log dùng chung toàn hệ thống** (Dev tự nhận trong mục 1). Danh sách Dev/Studio kê được **3 nơi** (LINE user nộp form, CSV, My Page) nhưng **thiếu** nhánh sync Google Sheet + job đếm lại số người trả lời → G2, G4, Q5.
  4. *Hành vi thay đổi so với trước → có TC cả hành vi mới lẫn chỗ cũ không hỏng?* — **Đủ**: whitelist cột sort có `NEW-19` (4 cột được phép vẫn chạy) + `NEW-23` (cột lạ bị bỏ qua). Chuỗi ngày tương đối đổi hành vi có `NEW-25`. Riêng việc whitelist là **thay đổi so với đặc tả cũ** → đã đưa §6.

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `27 quan điểm Trigger khớp task · 8 chưa cover đủ`

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `ENV-003` | `Cao` | `RISK — 0/39 TC chạy production.` Task là bug **hiệu năng trên production** + **DDL trên bảng log lớn**, đúng 2 hạng mục RULE-08 không được kết luận từ local/staging. `NEW-29`/`NEW-30`/`NEW-33`/`NEW-35` đều `Môi trường test = local`; `envAuto` của task cho thấy 1 run ở `local`, **0 run** ở dev/staging/prd | `[BLOCKER]` |
| Q2 | `OUT-EXPORT-001` | `Trung bình → nâng Cao` (màn có nút download CSV) | `GAP — 0 TC.` Không TC nào mở file CSV sau khi thêm chỉ mục; `REQ-013` yêu cầu nhưng không có TC map | `[BLOCKER]` |
| Q3 | `INTG-SHEET-001` | `Cao` | `GAP — 0 TC.` Câu trả lời form được ghi ra Google Spreadsheet (BR-08) từ chính bảng vừa đổi schema | `[MAJOR]` |
| Q4 | `REG-RUN-001` | `Cao` | `GAP — 0 TC.` Không TC nào đặt job/dữ liệu **đang chạy dở TỪ TRƯỚC** khi chạy migration rồi xác nhận tiếp tục đúng, không chạy lại từ đầu, không mất | `[MAJOR]` |
| Q5 | `JOB-001` | `Cao` | `GAP — 0 TC.` Kho `TC-FORM-357` cho thấy có **job đếm lại số người trả lời chạy 1 giờ/lần** đọc `form_answer_result`; checklist yêu cầu "job đã có retry sẵn (sync Google của Salon/Lesson/Form) → verify lại sau mỗi lần tính năng thay đổi" | `[MAJOR]` |
| Q6 | `COMPAT-LEGACY-001` | `Cao` | `RISK — TC gán mã này (NEW-25) thực chất test chuỗi ngày 'yesterday', KHÔNG test nhánh cũ.` Checklist yêu cầu test song song cặp **form cũ ⇄ form mới** và **bản ghi tạo trước migration ⇄ sau migration**; màn kết quả bản cũ (`showFormResult`, `using_old_version=1`) không có TC nào | `[MAJOR]` |
| Q7 | `DATA-COUNT-001` | `Cao` | `RISK — có TC chạm số đếm (NEW-11, NEW-36, NEW-37) nhưng không TC nào đối chiếu ĐỦ 4 NGUỒN.` Fix đã viết lại **câu đếm tổng của phân trang**; cần khớp `回答情報 = N人` ở màn list form ⇄ `total` của màn kết quả ⇄ CSV ⇄ API. Đây là "lỗi lặp nhiều nhất lịch sử bug" | `[MAJOR]` |
| Q8 | `DEPLOY-LIVE-001` | `Cao` | `GAP — 0 TC.` Release lên production **không bật maintain** kèm ALTER TABLE; không TC nào mở sẵn màn kết quả **trước** deploy rồi thao tác lọc/đổi trang **không reload** sau deploy. Rủi ro thấp hơn trigger điển hình vì hợp đồng API không đổi (`NEW-18`/`NEW-22`), nhưng migration chạy live thì chưa ai kiểm | `[MAJOR]` |

**Đã loại khỏi phạm vi** (trigger khớp nhưng không tìm được bằng chứng có ảnh hưởng — BƯỚC 5a):

- `DATA-BACKUP-001` — trigger "tính năng thêm/đổi bảng DB" khớp (migration thêm chỉ mục). Nhưng mục kiểm tra (6) là "tính năng **mới thêm bảng** DB → verify bảng nằm trong phạm vi backup và copy": migration này **không thêm bảng mới**, chỉ thêm chỉ mục. Đối chiếu kho `fa033-backup-データコピー.md` và `spec-features/admin/backup/feature-spec.md` §1.3: phạm vi copy bot liệt kê `form_answer`, `form_answer_page` v.v. (**định nghĩa form**); chuỗi `form_answer_result` **không xuất hiện** ở bất kỳ đâu trong kho FA-033 lẫn backup spec → câu trả lời của khách **không** nằm trong luồng copy bot. ⇒ **không đề xuất TC backup/copy**.
- `DATA-MIG-001` — bản ghi tạo trước migration đã được phủ bởi toàn bộ TC danh sách; bản ghi sau migration có `NEW-37`; migration idempotent + rollback có `NEW-34`. Đủ.

---

## 3. TC trùng lặp nội dung

**Đã rà 39 TC, không phát hiện trùng lặp.**

| Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| — | — | — | — | — | — |

- **Các cặp đã cân nhắc rồi loại** (khác ý định test, KHÔNG đề nghị gộp):
  - `NEW-18` vs `NEW-22` — cùng quan điểm `API-CONTRACT-001` nhưng khác oracle: `NEW-18` kiểm **sự tồn tại + kiểu** của từng khoá (đặc biệt: danh sách nhắc hẹn phải là **mảng** sau `groupBy` + đánh lại chỉ số) và chạy được ở mọi env; `NEW-22` so **từng giá trị** giữa 2 nhánh mã nguồn nên chỉ chạy được ở local. Bỏ `NEW-18` là mất chiều "chạy được ngoài local".
  - `NEW-29` vs `NEW-30` — cùng `REQ-011` nhưng `NEW-29` đo **số truy vấn**, `NEW-30` đo **thời gian phản hồi`. Hai oracle độc lập, bỏ cái nào cũng mất 1 chiều.
  - `NEW-12` (sort trên UI) vs `NEW-19` (sort qua endpoint, 4 cột × 2 chiều) — khác tầng; `NEW-19` ghi rõ 3 trong 4 cột **chỉ tồn tại ở tầng endpoint**, giao diện không gửi.
  - `NEW-9` / `NEW-11` / `NEW-17` (`SEARCH-001`) vs `NEW-15` / `NEW-16` / `NEW-21` / `NEW-31` (`FUNC-DATE-001`) — khác loại case và khác điểm biên.
- **Gate đã chạy**: không có TC nào được đề nghị xóa → coverage §1 + §2 không đổi.
- `DUP-CONFLICT`: không có.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | Toàn bộ 39 TC | **0/39 TC đã chạy** (`untested` 39, pass 0%). Mọi kết luận "đủ TC" ở §1 / §2 mới là đủ **trên giấy** — chưa có bất kỳ bằng chứng nào cho thấy fix hoạt động | Chạy bộ TC trước khi đưa Leader duyệt. Ưu tiên chạy trước: `NEW-22` (hợp đồng trước/sau fix), `NEW-29`, `NEW-13`, `NEW-3` |
| I2 | `[MAJOR]` | 15/39 TC | **8 mã quan điểm không có trong `framework/checklist-lme.md`**: `SEARCH-001`(3) · `API-CONTRACT-001`(3) · `DATA-RETENTION-001`(2) · `API-001`(2) · `TOOL-ERRHYG-001`(2) · `UI-TABLE-001`(1) · `TOOL-NEGCTRL-001`(1) · `TOOL-KNOW-002`(1). Theo BƯỚC 0.6 các TC này **không được tính là cover** quan điểm nào | Hoặc map lại sang mã chuẩn (`SEARCH-001`→`LIST-001`, `API-CONTRACT-001`→`REG-SHARED-001`, `TOOL-ERRHYG-001`→`FUNC-002`, `UI-TABLE-001`→`UI-001`, `TOOL-KNOW-002`→`PERF-LARGE-001`), hoặc bổ sung mã mới vào checklist theo RULE-10. Sửa trên Studio bằng `testcase_update` |
| I3 | `[MAJOR]` | `03-dev-impact.md` | `Auto-filled: 2026-09-21 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" chưa tick**. Thêm nữa: Dev **không kê theo format `F*`/`D*`/`T*`** — chỉ liệt kê "file thay đổi", bảng 4.1 trong file là do `/new-task` **suy ra**, chưa ai xác nhận | Tester đọc lại Journal #137306 trên Redmine, xác nhận bảng 4.1/4.2/4.3 không sót impact rồi tick checkbox |
| I4 | `[MAJOR]` | Fix (Redmine Journal #137306) | Dev **VERIFY chỉ ở mức `lint`**: `php -l` + in `toSql` + gọi hàm parse ngày qua Reflection. Dev tự ghi **"MySQL dev và web dev không kết nối được nên không EXPLAIN được trên dữ liệu thật"** → với một fix **hiệu năng**, kết luận "đã nhanh hơn" hiện **chưa có bất kỳ số đo nào** | Yêu cầu Dev bổ sung `EXPLAIN` trên dữ liệu thật, hoặc coi `NEW-33` + TC-ENV003-02 (§5) là gate bắt buộc trước khi release |
| I5 | `[MAJOR]` | `NEW-30`, `NEW-33`, `NEW-35` | **Expected không có tiêu chí pass/fail dứt khoát** (4a mục 1): `NEW-30` = "thấp hơn **rõ rệt**"; `NEW-33` = "**không fail chỉ vì** optimizer chọn index khác"; `NEW-35` = "**không dùng** điều kiện đọc/ghi không block làm pass criteria". Cả 3 đều là TC khảo sát, không phải TC quyết định Đạt/Không đạt | Chốt ngưỡng acceptance với Leader/Dev (xem §6 mục 2) rồi cập nhật expected. Trước khi chốt thì 3 TC này chỉ được dùng làm dữ liệu đầu vào, KHÔNG dùng để kết luận "đã fix" |
| I6 | `[MAJOR]` | `NEW-10` | Expected chỉ ghi "câu trả lời cũ hơn chỉ thấy khi bỏ lọc", **không ghi mốc mặc định cụ thể**. Kho `TC-FORM-345` đã chốt: khoảng mặc định = **từ ngày mồng 1 của 2 tháng trước tới ngày hiện tại**. Lọc ngày chính là thứ fix vừa viết lại → mốc mặc định phải là oracle | Bổ sung mốc mặc định vào expected của `NEW-10`, dẫn chiếu `TC-FORM-345` |
| I7 | `[MAJOR]` | `NEW-24` | Nhánh **fallback** khi `parseFormResultFilterDate` trả `null` mới chỉ được verify ở mức "không gây lỗi hệ thống". Chưa verify fallback **trả về đúng tập kết quả như cách lọc cũ** — mà đây chính là nhánh mất chỉ mục (quay về hàm cắt ngày) | Bổ sung expected cho `NEW-24`: so tập kết quả của nhánh fallback với nhánh lọc hợp lệ trên cùng dữ liệu; ghi nhận nhánh này **không dùng được chỉ mục** |
| I8 | `[MINOR]` | Studio task #317 | Task khai `feature: "shop-card"` trong khi ticket thuộc **Form Builder (FA-011)** — màn 「回答一覧」. Sai feature làm hỏng việc tra TC cũ / knowledge cùng tính năng trên Studio | Sửa `feature` của task #317 trên Studio |
| I9 | `[NIT]` | `NEW-19` | Gộp **8 tổ hợp** (4 cột × 2 chiều) vào 1 TC — vi phạm nguyên tắc atomic (4a mục 4). Ghi chú của TC có giải thích "cùng một nguyên nhân gốc" nên chấp nhận được | Giữ nguyên; nếu fail thì phải ghi rõ tổ hợp nào fail |

---

## 5. TCs đề xuất bổ sung (8)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa011-taobieumau-フォーム作成.md` (nhóm「Màn kết quả trả lời」16 TC + 「Export CSV」9 TC) · `kho-tcs/fa033-backup-データコピー.md` (kiểm phạm vi copy bot) |
| Vùng regression phát hiện từ kho | `TC-FORM-469` / `TC-FORM-470` / `TC-FORM-474` (Export CSV — lấy đủ toàn bộ form-result kể cả khi có phân trang, loại trừ câu trả lời đã xóa, >500 bản ghi) · `TC-FORM-357` (job đếm lại số người trả lời chạy 1 giờ/lần) · `TC-FORM-354` (「配信停止」 xóa remind đúng option) · `TC-FORM-355`/`356`/`358`/`359` (số người trả lời) · `TC-FORM-345` (khoảng lọc ngày mặc định) |
| Conflict expected vs kho | `Không` — không TC đề xuất nào mâu thuẫn expected của kho |
| GAP dùng lại TC kho (không viết mới) | `Không` — các TC kho ở trên viết cho hành vi gốc của tính năng, chưa có TC nào đặt trong bối cảnh "sau khi thêm chỉ mục vào bảng dùng chung", nên các TC dưới đây **dẫn chiếu** ID kho thay vì chép lại |
| Xác nhận chống trùng | Đã đối chiếu 39 TC ở BƯỚC 0 + kho FA-011 + kho FA-033 — **không TC đề xuất nào trùng** |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-ENV003-02 | UI | ENV-003 | Màn kết quả trả lời | Normal | manual | product | Đo thời gian mở màn kết quả trả lời trên production, trước và sau khi release fix | Trên production `step.lme.jp`: chọn ít nhất 3 biểu mẫu nằm trong danh sách ca chậm của ticket (ưu tiên form 12598 — ca 35s, 176252, 86320). Ghi lại số lượt trả lời của từng biểu mẫu. Mở sẵn tab Network của trình duyệt | 1. **Trước khi release**: mở màn kết quả trả lời của từng biểu mẫu với khoảng ngày mặc định, đo thời gian request `POST /basic/form-answer/v3/result/{id}` trên tab Network, lặp 3 lần mỗi biểu mẫu<br>2. Đo thêm 2 tình huống cho mỗi biểu mẫu: bấm 「全期間」 (bỏ lọc ngày) và vào trang giữa danh sách<br>3. Release fix + chạy migration lên production<br>4. Lặp lại đúng bộ đo ở bước 1 và 2, cùng biểu mẫu, cùng khoảng ngày, cùng số dòng/trang<br>5. Đối chiếu số đo trước/sau và đọc lại danh sách dòng để chắc dữ liệu không đổi<br>6. Theo dõi kênh cảnh báo Chatwork room 417532006 trong 48 giờ sau release | 3 biểu mẫu thật trên production × 3 tình huống tải × 3 lần đo | Sau release, thời gian request của cả 3 biểu mẫu ở cả 3 tình huống **giảm so với trước release** và **không còn lần nào vượt ngưỡng 5 giây** (mốc SLOWLV1 mà middleware `notifySlowRequestCommon()` dùng để bắn cảnh báo). Danh sách dòng, tên/ảnh đại diện bạn bè và tổng số bản ghi giống hệt trước release. Trong 48 giờ sau release, Chatwork room 417532006 **không** nhận thêm cảnh báo chậm cho endpoint này |  | Lấp `G7` + `Q1` · RULE-08: bug hiệu năng phát hiện trên production, không kết luận được từ local · `manual` vì bắt buộc chạy trên production với dữ liệu khách hàng thật · Đánh giá spec: Spec không ghi — ngưỡng 5s lấy từ mô tả middleware trong ticket, cần Leader chốt (§6 mục 2) · Evidence: bảng số đo trước/sau + screenshot tab Network + ảnh kênh Chatwork 48h sau release |
| TC-ENV003-03 | Data | ENV-003 | Bảng form_answer_result — chỉ mục và dữ liệu log | Abnormal | manual | product | Chạy migration thêm chỉ mục trên bảng log quy mô production — đo thời gian và ảnh hưởng đọc/ghi thật | Bản sao dữ liệu production của bảng `form_answer_result` (hoặc chính production, chạy trong khung off-peak đã thống nhất). Biết trước số dòng của bảng và dung lượng đĩa còn trống. Có quyền xem `SHOW PROCESSLIST` | 1. Ghi lại số dòng và dung lượng bảng trước khi chạy<br>2. Bắt đầu chạy migration, bấm giờ<br>3. Trong lúc chạy: mở màn kết quả trả lời của 1 biểu mẫu và ghi thời gian phản hồi<br>4. Trong lúc chạy: nộp 1 câu trả lời từ trang biểu mẫu phía LINE user<br>5. Quan sát `SHOW PROCESSLIST` xem có truy vấn nào bị chờ khoá<br>6. Migration kết thúc: ghi thời gian chạy, dung lượng bảng tăng thêm, và kiểm tra chỉ mục đã tạo đúng 3 cột đúng thứ tự<br>7. Kiểm tra câu trả lời nộp ở bước 4 còn nguyên và hiển thị đúng | 1 lần chạy migration trên bảng có quy mô bằng production | Migration hoàn tất, tạo đúng chỉ mục `(form_id, deleted_at, created_at)`. **Thời gian chạy và dung lượng tăng thêm được ghi lại thành số cụ thể** để quyết định khung giờ deploy. Không truy vấn nào bị chờ khoá quá ngưỡng team chấp nhận; nếu có thì phải kết luận rõ ràng là **bắt buộc dùng `pt-online-schema-change`**. Câu trả lời nộp trong lúc migration chạy còn nguyên, không mất, không sai dữ liệu. Số dòng của bảng sau migration bằng trước migration cộng số bản ghi phát sinh trong lúc chạy |  | Lấp `Q1` · RULE-08: DDL trên bảng log lớn không kết luận được từ local · Đây là bản "quy mô thật" của `NEW-35` (NEW-35 chạy local, giữ nguyên làm bước thử trước) · `manual` vì bắt buộc môi trường production/bản sao production · Đánh giá spec: Spec không ghi · Evidence: log thời gian chạy migration + `SHOW INDEX` sau khi chạy + `SHOW PROCESSLIST` lúc đang chạy |
| TC-OUTEXPORT001-01 | UI | OUT-EXPORT-001 | Export CSV | Normal | auto | Tất cả | Tải CSV kết quả trả lời sau khi thêm chỉ mục — đủ dòng, đúng dữ liệu, khớp màn hình | Biểu mẫu V3 có **>500 câu trả lời** trải trên nhiều tháng, trong đó có: ít nhất 1 câu trả lời **đã bị xóa**, 2 bạn bè khác ID cùng tên hiển thị, 1 bạn bè chưa có システム表示名, câu trả lời chứa dấu phẩy và emoji. Đã chạy migration thêm chỉ mục | 1. Mở màn kết quả trả lời, bấm 「全期間」, ghi lại tổng số bản ghi hiển thị<br>2. Bấm nút download CSV ở màn kết quả trả lời<br>3. Mở file CSV, đếm số dòng dữ liệu<br>4. Đối chiếu từng cột của 5 dòng bất kỳ với đúng dòng đó trên màn hình: ngày trả lời, LINE名, システム表示名, nội dung từng câu trả lời<br>5. Tìm câu trả lời đã bị xóa trong file CSV<br>6. Tải lại CSV từ nút ở màn danh sách biểu mẫu và so với file ở bước 2 | Biểu mẫu >500 câu trả lời, có 1 bản ghi đã xóa | File CSV tải về **đủ toàn bộ câu trả lời** (không dừng ở 100 dòng của trang hiện tại, không dừng ở 500 dòng), số dòng dữ liệu bằng đúng tổng số bản ghi ở bước 1. Từng cột khớp đúng giá trị trên màn hình; 2 cột LINE名 và システム表示名 tách riêng, bạn bè chưa có システム表示名 thì cột đó rỗng. Câu trả lời đã xóa **không** có trong file. Dữ liệu chứa dấu phẩy được bọc trong dấu nháy kép, mở bằng Excel không mojibake. File tải từ 2 nơi cho nội dung giống nhau |  | Lấp `G1` + `Q2` · regression — dẫn từ `TC-FORM-469`, `TC-FORM-470`, `TC-FORM-472`, `TC-FORM-474` · Studio `REQ-013` nêu đích danh nhánh CSV · Đánh giá spec: Spec ghi rõ (SCR-FA11-09 bước 5, EP-19) · Evidence: file CSV thật + screenshot màn hình tương ứng |
| TC-INTGSHEET001-01 | Data | INTG-SHEET-001 | Liên kết Google Sheet | Normal | manual | product | Đồng bộ câu trả lời sang Google Spreadsheet vẫn chạy đúng sau khi thêm chỉ mục | Bot đã liên kết Google OAuth và biểu mẫu V3 đã được cấp Spreadsheet riêng (`form_answer.google_sheet_id` có giá trị). Biểu mẫu đã có sẵn ít nhất 20 dòng trên Sheet. Đã chạy migration thêm chỉ mục | 1. Ghi lại số dòng hiện có trên Google Spreadsheet của biểu mẫu<br>2. Nộp 3 câu trả lời liên tiếp nhanh từ trang biểu mẫu phía LINE user, mỗi lần nội dung khác nhau<br>3. Chờ hết chu kỳ job đồng bộ rồi mở lại Google Spreadsheet<br>4. Đối chiếu 3 dòng mới trên Sheet với 3 câu trả lời trên màn kết quả: thứ tự, nội dung từng cột, tên người trả lời<br>5. Mở màn kết quả trả lời và kiểm tra 3 bản ghi đó cũng hiển thị đúng<br>6. Xóa 1 câu trả lời trên màn kết quả rồi kiểm tra lại Sheet | 3 câu trả lời nộp liên tiếp trên biểu mẫu đã liên kết Google Sheet | Cả 3 câu trả lời được ghi sang Google Spreadsheet, **không mất bản ghi nào, không ghi trùng**, thứ tự và nội dung từng cột khớp màn kết quả. Số dòng trên Sheet tăng đúng 3. Màn kết quả hiển thị đủ 3 bản ghi. Sau khi xóa 1 câu trả lời trên màn kết quả, Sheet giữ nguyên hành vi như trước fix (ghi rõ hành vi quan sát được nếu spec chưa định nghĩa) |  | Lấp `G2` + `Q3` · regression — nhánh GHI vào `form_answer_result` qua queue `form_answer_connect_googles` (BR-08), bảng vừa thêm chỉ mục · `manual` vì job nền chạy khác nhau giữa staging và production (ENV-003 Catalog D) + phải mở Google Sheet thật · Đánh giá spec: Spec không ghi — BR-08 ghi job `AddResultFormAnswerToGoogleSpreadSheet` đang bị comment out, cần Dev xác nhận cơ chế sync hiện hành (§6 mục 5) · Evidence: ảnh Google Sheet trước/sau + screenshot màn kết quả tương ứng |
| TC-COMPATLEGACY001-01 | UI | COMPAT-LEGACY-001 | Màn kết quả trả lời | Normal | auto | Tất cả | Màn kết quả trả lời bản CŨ của form đời cũ vẫn mở đúng sau khi thêm chỉ mục | Có 1 biểu mẫu **đời cũ** (`using_old_version = 1`, dùng endpoint `showFormResult`) đã có ít nhất 30 câu trả lời tạo **trước** khi chạy migration, và 1 biểu mẫu V3 để đối chứng. Đã chạy migration thêm chỉ mục | 1. Mở màn kết quả trả lời của biểu mẫu đời cũ<br>2. Kiểm tra danh sách hiển thị đủ số dòng, đúng ngày trả lời, đúng tên và ảnh đại diện bạn bè<br>3. Lọc theo một khoảng ngày có dữ liệu rồi đọc lại danh sách<br>4. Đổi trang và mở chi tiết một câu trả lời<br>5. Mở màn kết quả của biểu mẫu V3 để đối chứng<br>6. Với biểu mẫu đời cũ: sửa một câu trả lời hoặc thao tác nhắc hẹn rồi lưu lại | Biểu mẫu đời cũ có ≥30 câu trả lời tạo trước migration | Màn kết quả bản cũ mở bình thường, **không lỗi 500**, danh sách đủ dòng, ngày/tên/ảnh đại diện đúng. Lọc ngày và đổi trang của bản cũ cho kết quả đúng như trước khi chạy migration. Chi tiết câu trả lời mở được, nội dung đầy đủ. Bản ghi tạo trước migration mở → sửa → lưu lại không lỗi |  | Lấp `G3` + `Q6` · regression — endpoint `FormAnswerController::showFormResult` (`:4057`) Dev ghi nhận cùng pattern nhưng **KHÔNG sửa**; code không đổi nhưng bảng đã đổi schema · RULE-09 test song song cặp cũ ⇄ mới · Đánh giá spec: Spec ghi rõ (BR-09 `using_old_version`) · Evidence: screenshot màn kết quả bản cũ + bản V3 trên cùng dữ liệu |
| TC-JOB001-01 | Data | JOB-001 | Màn kết quả trả lời | Normal | auto | Tất cả | Job đếm lại số người trả lời vẫn khôi phục đúng con số sau khi thêm chỉ mục | Biểu mẫu V3 có số người trả lời đang bị lệch so với thực tế (cố tình làm lệch bằng cách thao tác trực tiếp hoặc xóa bạn bè). Đã chạy migration thêm chỉ mục. Biết trước số người trả lời đúng | 1. Ghi lại số 回答情報 đang hiển thị (đang lệch) và tổng số bản ghi thật trên màn kết quả<br>2. Chờ job đếm lại số người trả lời chạy (chu kỳ 1 giờ/lần)<br>3. Đọc lại số 回答情報 ở màn danh sách biểu mẫu<br>4. Đối chiếu với số đếm tay và với tổng số ở màn kết quả trả lời<br>5. Xóa 1 bạn bè đã trả lời nhiều biểu mẫu, chờ job chạy, kiểm tra số người trả lời ở **tất cả** biểu mẫu bạn bè đó đã trả lời | Biểu mẫu có số người trả lời lệch sẵn; 1 bạn bè đã trả lời ≥2 biểu mẫu | Sau khi job chạy, số 回答情報 được khôi phục về **đúng con số đếm tay** và khớp tổng số ở màn kết quả trả lời. Job không lỗi, không bỏ sót biểu mẫu. Khi xóa bạn bè đã trả lời nhiều biểu mẫu, số người trả lời giảm đúng ở **tất cả** biểu mẫu bạn bè đó đã trả lời |  | Lấp `Q5` · regression — dẫn từ `TC-FORM-357`, `TC-FORM-356` · Job đọc `form_answer_result` — bảng vừa thêm chỉ mục · Đánh giá spec: Spec không ghi — `feature-spec.md` chưa mô tả job đếm lại, nguồn là kho TCs (§6 mục 4) · Evidence: ảnh số 回答情報 trước/sau khi job chạy + phép đếm tay |
| TC-DATA001-01 | UI | DATA-001 | Màn kết quả trả lời | Normal | auto | Tất cả | Dừng nhắc hẹn trên popup rồi tải lại — đúng nhắc hẹn bị dừng, nhắc hẹn khác giữ nguyên | Biểu mẫu V3 có ít nhất 2 câu trả lời, mỗi câu trả lời có **≥2 nhắc hẹn** đang hoạt động thuộc các option khác nhau. Câu trả lời thứ hai nằm ở **trang 2** của danh sách | 1. Mở popup chi tiết của câu trả lời thứ nhất, sang tab 「リマインド」, ghi lại danh sách nhắc hẹn và thứ tự<br>2. Bấm 「配信停止」 trên đúng một nhắc hẹn<br>3. Tải lại màn kết quả trả lời và mở lại popup của câu trả lời đó<br>4. Sang trang 2, mở popup của câu trả lời thứ hai và kiểm tra tab 「リマインド」<br>5. Lặp bước 2 cho một nhắc hẹn của câu trả lời thứ hai rồi tải lại và kiểm tra cả 2 câu trả lời | 2 câu trả lời × ≥2 nhắc hẹn mỗi câu, nằm trên 2 trang khác nhau | Chỉ **đúng** nhắc hẹn vừa bấm 「配信停止」 chuyển sang trạng thái đã dừng, kèm **tên người thao tác và thời điểm**; các nhắc hẹn còn lại của cùng câu trả lời giữ nguyên trạng thái và **giữ nguyên thứ tự**. Nhắc hẹn của câu trả lời khác (kể cả ở trang khác) **không bị ảnh hưởng và không bị gán nhầm sang dòng khác**. Sau khi tải lại, danh sách nhắc hẹn của từng câu trả lời vẫn là **mảng** đúng thứ tự, câu trả lời không có nhắc hẹn vẫn hiện tab trống |  | Lấp `G6` · regression — dẫn từ `TC-FORM-354`, `TC-FORM-353` · Đây là nhánh GHI của `form_answer_item_remind`, bảng nay được nạp theo lô `whereIn` + `withTrashed` + `groupBy` + đánh lại số thứ tự · Bổ sung cho `NEW-3` (chỉ đọc trạng thái có sẵn) · Đánh giá spec: Spec ghi rõ (SCR-FA11-09 bước 4) · Evidence: screenshot tab 「リマインド」 trước/sau khi dừng, của cả 2 câu trả lời |
| TC-DATACOUNT001-01 | UI | DATA-COUNT-001 | Màn kết quả trả lời | Normal | auto | Tất cả | Số đếm câu trả lời khớp giữa 4 nguồn sau khi viết lại câu đếm tổng của phân trang | Biểu mẫu V3 có số câu trả lời **biết trước bằng phép đếm tay** (ví dụ đúng 137), trong đó có 1 câu trả lời đã xóa và 1 bạn bè đã bị block. Đã chạy migration thêm chỉ mục | 1. Đếm tay số câu trả lời hợp lệ của biểu mẫu<br>2. Đọc số 回答情報 = N人 ở màn danh sách biểu mẫu<br>3. Mở màn kết quả trả lời, bấm 「全期間」, đọc tổng số bản ghi và số trang<br>4. Lọc một khoảng ngày cụ thể, đọc lại tổng số bản ghi<br>5. Tải CSV và đếm số dòng dữ liệu<br>6. Gọi endpoint danh sách bằng ajax với cùng tham số ở bước 4, đọc khoá tổng số<br>7. Đối chiếu cả 4 nguồn với phép đếm tay | Biểu mẫu có đúng 137 câu trả lời hợp lệ, 1 bản ghi đã xóa, 1 bạn bè đã block | Cả 4 nguồn — số 回答情報 ở màn danh sách biểu mẫu, tổng số của màn kết quả, số dòng trong CSV, khoá tổng số của endpoint — **khớp nhau và khớp phép đếm tay**. Câu trả lời đã xóa không được tính vào bất kỳ nguồn nào. Khi lọc khoảng ngày, tổng số của màn kết quả và của endpoint bằng nhau và bằng số dòng đếm tay trong khoảng đó. Số trang khớp với tổng số chia cho số dòng mỗi trang |  | Lấp `Q7` · regression — dẫn từ `TC-FORM-355`, `TC-FORM-358` · Fix đã viết lại **câu đếm tổng của phân trang** (đổi điều kiện lọc ngày) nên số đếm là oracle trực tiếp · DATA-COUNT-001 là lỗi lặp nhiều nhất lịch sử bug · Đánh giá spec: Spec ghi rõ (SCR-FA11-09 bước 2, khoá `total`) · Evidence: bảng đối chiếu 4 nguồn + phép tính tay |

**GAP / quan điểm KHÔNG có TC đề xuất** (Leader chốt 2026-09-21 — loại 2 TC vì bắt buộc môi trường production + phải canh đúng thời điểm deploy, không đưa vào bộ chạy được):

| Mục | Vẫn hở gì | Phương án thay thế |
|---|---|---|
| `G4` + `Q4` `REG-RUN-001` + `Q8` `DEPLOY-LIVE-001` | Không ai kiểm **job nền đang chạy dở** và **tab mở sẵn từ trước deploy** tại thời điểm chạy migration | Không test bằng TC. Chuyển thành **việc vận hành khi release**: chạy migration trong khung off-peak đã chốt, và sau khi chạy xong đối chiếu hàng đợi sync Google Sheet + số 回答情報 (đã có `TC-JOB001-01` phủ phần số đếm sau khi job chạy). Rủi ro còn lại Leader chấp nhận |
| `G5` `PERF-LARGE-001` (chi phí GHI) | Không có số đo độ trễ **INSERT** trước/sau khi thêm chỉ mục trên bảng log lớn | Không test bằng TC. Thay bằng **giám sát sau release**: theo dõi cảnh báo chậm ở Chatwork room 417532006 cho các endpoint **nộp biểu mẫu** trong 48 giờ sau khi chạy migration. `TC-ENV003-02` đã có bước theo dõi kênh này cho endpoint đọc |

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | `spec-features/admin/form-answer/` — `api-spec.md` EP-09 + `feature-spec.md` SCR-FA11-09 | **Whitelist cột sắp xếp** (`id` / `created_at` / `line_id` / `duration_time_reply`) là **hành vi mới**: cột ngoài danh sách nay bị **bỏ qua im lặng** thay vì lỗi truy vấn. Spec hiện không có khái niệm whitelist | Studio `REQ-007` tự ghi: *"Đây là thay đổi hành vi so với đặc tả cũ, cần leader xác nhận"* · `source_refs: spec: api-spec.md EP-09 (không whitelist)` | Leader + Dev |
| 2 | Chưa có — cần tạo mục acceptance hiệu năng cho SCR-FA11-09 | **Chưa có ngưỡng acceptance** cho: (a) thời gian phản hồi màn kết quả sau fix; (b) mức tăng chấp nhận được của độ trễ GHI sau khi thêm chỉ mục; (c) mức lock/latency chấp nhận được khi chạy ALTER. Thiếu (a)(b)(c) thì `NEW-30`, `NEW-33`, `NEW-35` và TC-ENV003-02 / TC-PERFLARGE001-02 đều không có tiêu chí Đạt/Không đạt dứt khoát | `NEW-30` ghi chú: *"chưa có SLA/acceptance tuyệt đối sau fix ... nếu team chốt ngưỡng chính thức thì cập nhật lại expected sau"* · Issue I5 §4 | Leader + Dev |
| 3 | `spec-features/admin/form-answer/feature-spec.md` SCR-FA11-09 | **Chuỗi ngày tương đối** (ví dụ `yesterday`) nay cho phạm vi lọc **khác bản cũ**. `NEW-25` chỉ "ghi nhận khác biệt", không chốt đâu là hành vi đúng. Cần quyết: chấp nhận khác biệt (vì giao diện chỉ gửi `YYYY.MM.DD`) hay phải giữ tương thích | Dev tự nêu trong mục "Rủi ro / lưu ý khi test" · `NEW-25` không có expected dứt khoát | Leader + Dev |
| 4 | `spec-features/admin/form-answer/feature-spec.md` SCR-FA11-09 | Spec **thiếu 3 hành vi** mà fix vừa chạm: (a) khoảng lọc ngày **mặc định** = từ ngày mồng 1 của 2 tháng trước tới ngày hiện tại; (b) biên lọc là `00:00:00` → `23:59:59`; (c) **job đếm lại số người trả lời chạy 1 giờ/lần**. Cả 3 hiện chỉ tồn tại trong kho TCs, không có trong spec | Kho `TC-FORM-345` (khoảng mặc định) · `TC-FORM-357` (job đếm lại) · Studio `REQ-005` (biên `00:00:00`/`23:59:59`) vs `feature-spec.md` SCR-FA11-09 bước 3 chỉ ghi "Lọc theo `date_start`, `date_end`" | PM + Dev |
| 5 | `spec-features/admin/form-answer/feature-spec.md` BR-08 | BR-08 ghi *"Job `AddResultFormAnswerToGoogleSpreadSheet` hiện bị comment out — sync có thể đang dùng cơ chế khác"*. Không xác định được cơ chế sync hiện hành thì không viết được expected dứt khoát cho TC-INTGSHEET001-01 | `feature-spec.md` BR-08 tự đánh dấu là chưa chắc chắn | Dev |
| 6 | Tài liệu deploy (ngoài `spec-features/`) | Chưa có **quy trình chạy migration trên bảng log lớn**: khung giờ off-peak, điều kiện bắt buộc dùng `pt-online-schema-change`, cách rollback nếu ALTER kéo dài. Dev đã khuyến nghị trong migration nhưng chỉ là comment trong code | Journal #137306 mục "Rủi ro / lưu ý khi test" · Không có tài liệu vận hành tương ứng | Leader + Dev |
