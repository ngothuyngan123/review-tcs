# 05 — Review Report

> Draft cho Leader verify. Ticket #40044 「Webhook転送」 — Feature (tính năng mới), không phải bug fix.

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | `(1) MCP LME TEST STUDIO — task #269 (ticket 40044, round 1, branch ai-feature-40044-v2)` |
| Tổng số TC review | `137` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `7 GAP · 6 RISK · 1 orphan` — chiều `dev-impact` **không đánh giá được** (input thiếu).

> Chiều **(a) dev-impact**: `03-dev-impact.md` mục 4.1 `F*` / 4.2 `D*` / 4.3 `T*` đều là `<Input thiếu — Dev chưa cung cấp>`; Redmine #40044 không có section "Đánh giá ảnh hưởng".
> Chiều **(b) diff code**: lấy từ Studio `task_get_context(269, ["dev_impact","spec_delta"])`. `dev_impact` có đủ điểm sửa WEB + JOB; `spec_delta` **không có** `files[]` / `diffStat` (mode `sprint-spec`) → không đối chiếu được từng file, chỉ đối chiếu theo điểm sửa Studio kê.

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G0 | Toàn bộ `F*` / `D*` / `T*` | `dev-impact` | — | `Input thiếu: Dev chưa kê ảnh hưởng` → không đối chiếu được chiều (a). Mọi kết luận coverage dưới đây CHỈ dựa trên diff code do Studio suy ra. | `[MAJOR]` |
| G1 | Bảng `webhook_relay_queue` — web ghi nhưng **không có bộ tiêu thụ** (dead path, Studio ghi rõ "mismatch còn lại") | `diff code` | `không có` | `GAP` — 0 TC verify bảng này có phình vô hạn / có job dọn / có consumer. REQ-021 yêu cầu "phải có bộ tiêu thụ hoặc được gỡ" nhưng không TC nào chạm; REQ-010 khai báo phần này NGOÀI phạm vi. | `[MAJOR]` |
| G2 | `buildBody()` kênh LINE **bọc envelope** (thêm 3 trường: định danh LINE OA, id callback_event, loại event) — lệch `BR-01` 「加工せずそのまま」 | `diff code` | `NEW-138` | `RISK nặng` — TC có expected "không thêm trường, không bớt trường" nhưng exec = `pass`, trong khi chính note của TC và `dev_impact` đều ghi bản dựng ĐANG bọc envelope. Kết quả pass mâu thuẫn nội dung TC. | `[BLOCKER]` |
| G3 | 3 bảng nóng bị ALTER (`callback_event` · `tag_history` · `friend_info_history`) + `status_sync` — regression các luồng đọc/ghi 3 bảng này | `diff code` | `NEW-125` (skip) · `NEW-126` (chưa chạy) · `NEW-127` / `NEW-128` / `NEW-129` (pass) | `RISK` — nhận webhook LINE (NEW-125) skip, trả lời tự động + kịch bản (NEW-126) chưa chạy. **richmenu KHÔNG có TC nào** dù `dev_impact` liệt kê richmenu trong vùng regression → đã bổ sung `TC-REGSHARED001-08`. Release có bật maintain nên **không** cần case client cũ gọi server mới. | `[BLOCKER]` |
| G4 | Thứ tự phát hành P0: `migration → recover status_sync=4 → config → flag` | `diff code` | `NEW-119` (pass) · `NEW-120` · `NEW-121` (chưa chạy) · `NEW-122` (skip) | `RISK` — NEW-120 (bật cờ khi CHƯA chạy script đánh dấu → **toàn bộ lịch sử cũ bị gửi ra bên thứ 3**) là kịch bản rò rỉ dữ liệu, vẫn chưa chạy. 4/6 TC của REQ-019 không có kết luận. | `[BLOCKER]` |
| G5 | Luồng gửi ra ngoài của job: `TIMEOUT=10s` · `MAX_ATTEMPT=3` · backoff `{500,1000,2000}ms` · phân loại 2xx / ≥300 | `diff code` | `NEW-137` · `NEW-139` · `NEW-106` (chưa chạy) · `NEW-156` · `NEW-93` · `NEW-94` (skip) | `RISK` — đây là LÕI của tính năng, 6/7 TC không có kết luận. Chỉ NEW-136/138/140 (đường thành công) đã chạy. | `[BLOCKER]` |
| G6 | Vòng đời `status_sync` phía job: batch 500 · `MAX_QUEUE_SIZE=200` · 10 worker · `resumeStuckRow()` | `diff code` | `NEW-114` · `NEW-115` · `NEW-141` · `NEW-142` · `NEW-143` · `NEW-144` (tất cả chưa chạy) | `RISK` — 6/9 TC của REQ-018 chưa chạy: quét lô lớn không sót, đa luồng không gửi trùng, giải phóng bản ghi kẹt, quét đủ 2 giá trị trạng thái. | `[MAJOR]` |
| G7 | `WebhookNotifyEmitter` — nhánh **friend_info KHÔNG có caller** ở web (Studio ghi rõ), notify thực phát từ job | `diff code` | `NEW-73` · `NEW-118` (pass) · `NEW-134` (chưa chạy) | `GAP ma trận lối ghi` — REQ-012 chỉ 3 TC. Kho `fa015` đã liệt kê 17 lối ghi từ màn admin (`TC-FRI-199…215`) + 11 lối từ tính năng khác (`TC-FRI-216…226`: form, booking salon/lesson, mua item, import CSV) + nhóm action + nhóm job ngày tháng. Không TC nào của task đi qua ma trận này. | `[BLOCKER]` |
| G8 | `WebhookNotifyEmitter` — nhánh tag | `diff code` | `NEW-65` · `NEW-66` · `NEW-149` | `GAP ma trận lối ghi` — kho `fa012` đã liệt kê **24 điểm gắn tag** (`TC-TAG-180…203`, "Điểm gắn tag n/24"). Task chỉ test 2 điểm (màn quản trị) + 1 TC đối chiếu tổng (NEW-149, mã lạ). Bug Studio #857 (High) đã chứng minh có điểm sót thật: modal 「タグ編集」→「つける」 không sinh notify. | `[BLOCKER]` |
| G10 | `WebhookRelayStats` (Studio kê trong diff) | `diff code` | `không có` | `GAP` — 0 TC chạm. Cần Dev xác nhận có phải thành phần user-facing không; nếu chỉ là metric nội bộ thì đóng lại, không cần TC. | `[MINOR]` |
| G11 | 26/137 TC **không gắn `requirement_keys`** | `orphan` | `NEW-90 · 91 · 92 · 93 · 94 · 96 · 97 · 98 · 99 · 100 · 101 · 102 · 103 · 104 · 150 · 151 · 152 · 153 · 154 · 155 · 156 · 157 · 158 · 159 · 160 · 161` | Không map được về REQ nào → không tính vào coverage của Studio. Phần lớn là TC thật, chỉ thiếu nhãn. | `[MINOR]` |
| G12 | **Ma trận 15 loại event webhook LINE** phải forward (spec human cung cấp 2026-09-08) | `diff code` | `NEW-136` (forward end-to-end, **chỉ 4 loại**: message · follow · unfollow · postback) · `NEW-64` (8 loại nhưng chỉ ở **tầng ghi hàng đợi web**, webhook mô phỏng) · `NEW-31` (chỉ hiển thị cột 「イベント種別」) | `GAP` — **11/15 loại chưa được verify forward tới bên thứ 3**: `unsend` · `join` · `leave` · `videoPlayComplete` · `deactivated` · `activated` · `memberJoined` · `memberLeft` · `membership` · `beacon` · `accountLink`. Trong đó **6 loại không TC nào chạm ở bất kỳ tầng nào**: `deactivated` · `activated` · `memberLeft` · `membership` · `beacon` · `accountLink`. Riêng `follow` chưa phân biệt `isUnblocked` true/false. **Yêu cầu Leader 2026-09-08: mỗi loại callback tách riêng 1 TC** — `message` tách 8 kiểu tin (text · sticker · location · image · video · audio · file khác · text sinh từ tap button), `postback` tách 3 nguồn (richmenu · imagemap · button), `follow` tách 3 nhánh (kết bạn thường · quét QR · bỏ chặn), cộng 1 case **callback gộp chung** (1 request của LINE chứa nhiều event). Bộ TC hiện có **gộp 6 thao tác vào 1 TC duy nhất** (`NEW-136`) → không truy được loại nào hỏng khi TC fail. | `[BLOCKER]` |
| G13 | **Ma trận loại friend info** phải sinh notify kênh LME (phase 1) | `diff code` | `NEW-73` (text · select · point-cộng, ở tầng hàng đợi) · `NEW-118` (set/update/clear + "point operation được hỗ trợ" — mô tả mơ hồ) · `NEW-134` (chưa chạy) | `GAP` — thiếu hẳn 4 nhóm: **年月日** (cả 2 chế độ: đăng ký ngày hiện tại / ngày chỉ định — theo kho `fa015`, date đi qua **job**, không phải ghi trực tiếp) · **点数 đủ 3 phép** (chỉ có "cộng", thiếu ghi đè và trừ) · **画像** · **PDF**. Không TC nào chốt payload notify của info kiểu file (có kèm đường dẫn file hay chỉ định danh trường). | `[BLOCKER]` |
| G14 | **Phạm vi phase 1 của webhook LME** (chỉ tag + friend info) | `diff code` | `không có` | `GAP` — 0 TC negative control chứng minh các sự kiện LME **ngoài phase 1** (line user trả lời form, booking salon/lesson, mua item…) **KHÔNG** bị gửi ra bên thứ 3. Rủi ro: emitter bắt nhầm nguồn → đẩy PII của sự kiện chưa được duyệt phạm vi ra ngoài. | `[MAJOR]` |

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `27 quan điểm Trigger khớp task · 13 chưa cover đủ`.

> **Đã loại khỏi phạm vi** (RULE-03 — × có lý do), Leader xác nhận **2026-09-08**:
> - `DATA-BACKUP-001` — tính năng backup / copy bot **không hỗ trợ** 3 bảng của Webhook転送. Nếu sau này backup mở rộng phạm vi thì phải mở lại quan điểm.
> - `DEPLOY-LIVE-001` — release này **có bật maintain**, nên không có tình huống "client cũ gọi server mới". Phần regression của 3 bảng bị ALTER vẫn giữ ở `G3` và đã có TC riêng (`TC-REGSHARED001-08`) vì chiều đó không phụ thuộc maintain.

> Mã `Q*` / `G*` giữ nguyên số cũ sau khi gỡ dòng, không đánh lại — để cột `Ghi chú` của §5 vẫn trỏ đúng.

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q2 | `JOB-001` | Cao | `RISK` — không TC nào mang mã `JOB-001`; phần job chỉ được gắn mã lạ `JOB-002` (5 TC: 1 pass · 2 skip · 2 chưa chạy). Thiếu hẳn 2 chiều bắt buộc: (4) đối chiếu "số bản ghi vào = số thành công + số vào hàng đợi lỗi", và (2) dữ liệu vượt ngưỡng liên tục để chứng minh job không dừng. | `[BLOCKER]` |
| Q3 | `BULK-001` | Cao | `GAP` — 0 TC. Trigger: bộ lọc friend + gắn/gỡ tag hàng loạt → mỗi friend phải sinh đúng 1 event notify; kiểm tra bắt buộc là **đếm** số đối tượng thực sự bị tác động khớp 100% số đã lọc. NEW-67 (nhiều tag cho 1 friend) là chiều khác và đang fail (#40560). | `[BLOCKER]` |
| Q5 | `PERM-002` | Cao | `RISK` — 2 TC, TC quan trọng nhất (NEW-52: staff chưa cấp quyền gọi thẳng 5 đường dẫn dữ liệu → phải 403, không lộ secret) đang **blocked**. Bug liên quan #40536 đã bị **reject**. | `[BLOCKER]` |
| Q6 | `SEC-ISO-001` | Cao | `RISK` — TC duy nhất (NEW-56: user không thuộc LINE OA truy cập cấu hình + secret của OA đó) đang **blocked**, không có TC thay thế cho chiều cách ly đa LINE OA. | `[BLOCKER]` |
| Q7 | `SEC-001` | Cao | `RISK` — TC duy nhất (NEW-26, consent gửi PII ra bên thứ 3) đang **fail**, bug #40519 bị **reject** → quan điểm PII chưa có kết luận. Thiếu cả Normal + Boundary (RULE-01). | `[BLOCKER]` |
| Q8 | `ENV-003` | Cao | `RISK` — 3 TC nhưng **0/137 TC chạy trên production**; 137 TC khai `env_scope` không TC nào có `prd`. Tính năng chạm domain đích ngoài + job nền + loadbalance → RULE-08 cấm kết luận từ local/staging. | `[MAJOR]` |
| Q9 | `DATA-AUDIT-001` | Cao | `RISK` — 1 TC (NEW-48, lưu cấu hình). Thiếu audit cho **tái tạo signing secret** và **bật/tắt kênh** — đúng loại thao tác nhạy cảm quan điểm nhắm tới; thiếu chiều "cùng thao tác từ TỪNG nguồn" (màn web vs EP-02/EP-03 gọi thẳng). Thiếu Abnormal + Boundary (RULE-01). | `[MAJOR]` |
| Q10 | `REG-SPEC-001` | Cao | `GAP` — spec đổi SAU khi TC đã tồn tại (journal #133769 ngày 2026-09-03 "kênh LINE không có Signing secret", cộng 5 bug bị Dev reject = 5 quyết định spec mới) nhưng không có dấu vết rà lại từng TC theo 4 trạng thái `[Giữ nguyên]` / `[Cần sửa]` / `[Cần thêm mới]` / `[Hết hiệu lực]`. 137/137 TC vẫn `status=draft`, `spec_change=0`. | `[MAJOR]` |
| Q11 | `CONC-002` + `DATA-MIG-001` | Cao | `RISK` — script đánh dấu dữ liệu cũ (`status_sync=4`) chạy trên TOÀN BỘ lịch sử 3 bảng trong khi webhook mới vẫn đổ vào. NEW-121 (chạy lại script nhiều lần không hỏng dữ liệu đang chờ) **chưa chạy**; không TC nào chạy script ĐỒNG THỜI với traffic mới. | `[MAJOR]` |
| Q12 | `PERF-LARGE-001` + `DATA-COUNT-001` | Cao (nâng) | `RISK` — NEW-114 (quét lô lớn) chưa chạy, NEW-130 (index mới không làm chậm ghi khi tải cao) **skip**, NEW-144 (bộ đếm lỗi liên tiếp) chưa chạy. Chưa có số liệu quy mô thật (khách lớn nhất) cho `callback_event` — bảng nóng nhất hệ thống. | `[MAJOR]` |
| Q14 | `INTG-HOOK-001` | Cao | `GAP` — Trigger: nhận webhook từ bên ngoài (LINE). Chỉ 1 TC mang mã này (`NEW-64`, tầng hàng đợi). Chiều "nhận đủ **mọi loại** event mà bên gửi có thể phát" chưa được verify — 11/15 loại event LINE chưa đi hết đường forward (xem G12). Thiếu Normal + Abnormal cho nhóm event không do user thao tác trực tiếp (`beacon` · `accountLink` · `activated` · `deactivated` · `membership`). | `[BLOCKER]` |
| Q15 | `FRIEND-001` | Cao | `RISK` — 3 TC (N1 · A2), **thiếu Boundary** (RULE-01) và thiếu trục "loại info": 年月日 (2 chế độ) · 点数 (3 phép) · 画像 · PDF đều chưa có TC. TC bao quát nhất (`NEW-134`) vẫn **chưa chạy**. | `[BLOCKER]` |
| Q13 | `FUNC-DATE-001` | Cao | `GAP một phần` — 0 TC kiểm timezone / biên ngày của cột 「発生日時」 và mốc 「最終更新」 (JST vs UTC, 23:59 → 00:00, cuối tháng). 4 TC hiện có (NEW-30/150/34/151) chỉ kiểm thứ tự sort và thời điểm refresh. Trigger khớp một phần: màn chỉ hiển thị + sort theo thời gian, không có tính hạn. | `[MAJOR]` |

---

## 3. TC trùng lặp nội dung

**Đã rà 137 TC** (so theo 4 yếu tố: `mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`) — **không phát hiện trùng lặp cần xóa hay gộp**.

12 cặp có độ giống văn bản > 0.5 đã soi tay, tất cả **KHÔNG phải trùng**:

| Nhóm | Cặp TC | Vì sao không trùng |
|---|---|---|
| Cặp gương 2 kênh | `NEW-99↔100` · `NEW-97↔98` · `NEW-30↔150` · `NEW-34↔151` · `NEW-35↔152` · `NEW-158↔159` · `NEW-5↔20` · `NEW-103↔104` | Khác **đối tượng**: kênh LINE (không ký) vs kênh LME (ký HMAC) — 2 bản ghi cấu hình độc lập, 2 tab riêng. Bỏ 1 bên là mất cover đúng 1 kênh. |
| Negative control 2 chiều | `NEW-10↔23` | 2 chiều ngược nhau (lưu LINE không đụng LME / lưu LME không đụng LINE) — cần cả 2 để chứng minh cách ly. |
| Toggle 2 chiều | `NEW-16↔90` | Hiện secret ↔ che secret — 2 thao tác khác nhau. |
| Khác đối tượng dữ liệu | `NEW-127↔128` | `tag_history` vs `friend_info_history` — 2 bảng khác nhau. |
| Khác biên retry | `NEW-93↔94` | Thành công ở lần thử 2 (LINE) vs lần thử 3 (LME) — 2 giá trị biên khác nhau của `MAX_ATTEMPT`. |

| Sau khi bổ sung §5 | 6 TC lẻ mới: `TC-INTGLINE001-03` (message text) · `TC-INTGLINE001-11` (postback richmenu) · `TC-INTGLINE001-13` (postback button) · `TC-INTGLINE001-14` (follow thường) · `TC-INTGLINE001-16` (unfollow) · `TC-INTGLINE001-17` (unblock) | `NEW-136` | `DUP-SUBSET` (chiều ngược: TC cũ là **superset** của 6 TC mới) | Cùng `INTG-*`, cùng Normal, cùng 6 thao tác LINE, expected tương đương | `[MINOR]` |

- ⚠️ Nhóm trên **chỉ phát sinh sau khi thêm 28 TC lẻ ở §5**. Leader chọn 1 trong 2: (a) giữ TC lẻ + **retire `NEW-136`** (khuyến nghị — TC gộp 6 thao tác thì fail không truy được loại nào hỏng); (b) giữ `NEW-136` làm smoke và đánh dấu 6 TC lẻ là chi tiết hoá. **Không tự xóa** — TC read-only.
- **Gate đã chạy**: không đề nghị xóa TC nào → coverage §1 + §2 giữ nguyên.
- `DUP-INFLATE`: không có.
- `DUP-CONFLICT`: không có giữa 2 TC với nhau; nhưng có **mâu thuẫn TC ↔ code** (NEW-138 vs envelope) → đã đưa §6.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | `NEW-138` | **Kết quả `pass` mâu thuẫn chính nội dung TC.** Expected: "không thêm trường, không bớt trường"; note của TC: "⚠ bản dựng hiện tại KHÔNG gửi nguyên trạng mà bọc nội dung LINE vào lớp vỏ thêm 3 trường"; `dev_impact` Studio: "buildBody() BỌC envelope (lệch BR-01)". TC này phải là **fail**, hoặc expected phải đổi. Đang `pass` = false success che một lệch spec. | Re-triage NEW-138. Chốt BR-01 ở §6 trước, rồi sửa expected hoặc raise bug. |
| I2 | `[BLOCKER]` | `NEW-92` · bug Studio #857 · #861 · #859 | **TC fail / bug chưa raise ticket Redmine.** NEW-92 (chặn URL sai host) fail, `bug_tickets` rỗng, không bug Studio kèm. Bug Studio **#857 severity High** ("「つける」 ở modal 「タグ編集」 không sinh notify kênh 2") trạng thái `fixed` nhưng `redmine_id = NULL`. Bug #861, #859 trạng thái `stale` ("testcase đổi version, cần chạy lại") cũng chưa có ticket. | Raise ticket Redmine cho NEW-92 + #857; chạy lại 2 TC stale rồi quyết định log hay đóng. |
| I3 | `[BLOCKER]` | `NEW-52` · `NEW-56` | **2 TC bảo mật quan trọng nhất đang `blocked`**, snapshot không ghi lý do block. Đây là 2 nhánh chặn ở tầng server (gọi thẳng 5 route khi không có quyền / truy cập cấu hình của LINE OA khác) — chặn ở UI mà không chặn ở server thì chỉ cần biết địa chỉ là lấy được signing secret. | Gỡ block, chạy lại trên môi trường có 2 LINE OA + 2 tài khoản staff. Không đề xuất TC mới — TC đã có, chỉ thiếu kết luận. |
| I4 | `[MAJOR]` | Toàn bộ 137 TC | **Tỷ lệ pass thật 108/137 = 78,8% (< 80%)**: 3 fail · 9 skip · 2 blocked · 15 chưa chạy = **29 TC không có kết luận**. Phần chưa chạy tập trung đúng vào job nền + phát hành (REQ-017/018/019) — phần rủi ro nhất. | Chạy hết 29 TC trước khi chốt round 1. |
| I5 | `[MAJOR]` | Toàn bộ 9 run | **0 TC chạy trên production / staging / dev** — cả 9 run (`867 · 882 · 896 · 897 · 899 · 901 · 902 · 912 · 929`) đều `env = local`; `env_scope` khai của 137 TC cũng không TC nào có `prd`. Tính năng chạm **domain đích ngoài + job nền + loadbalance** → RULE-08 / `ENV-003` cấm kết luận từ local. | Bổ sung vòng chạy staging + production cho nhóm job / forward / URL (xem §5, các TC ghi `Phạm vi ENV = product`). |
| I6 | `[MAJOR]` | 5 bug bị reject: #40519 · #40534 · #40535 · #40536 · bug Studio #864 | **5 quyết định spec mới phát sinh sau khi TC đã viết**, nhưng TC gốc không được đánh dấu lại (`spec_change = 0`, `status = draft`). Ví dụ #40536 (staff chưa cấp quyền vẫn gọi được route dữ liệu) bị reject → expected của NEW-52 có thể đã sai chuẩn, mà NEW-52 vẫn `blocked`. | Rà lại từng TC thuộc 5 quyết định này theo `REG-SPEC-001` (4 trạng thái), ghi kết quả vào `spec_status`. Xem §6. |
| I7 | `[MAJOR]` | Nguồn spec | **Không tìm được spec cho tính năng.** `spec-features/` không có thư mục webhook/relay; `templates/LME-SYSTEM-SPEC.md` chỉ có `FA-038 LOA接続設定`, không có 「Webhook転送」. Chuẩn đánh giá Expected hiện là 21 `REQ-*` do Studio tự sinh + **phạm vi event do human cung cấp trực tiếp trong chat 2026-09-08** (15 loại webhook LINE; webhook LME phase 1 = tag + friend info với đủ loại info text · select · date 2 chế độ · point 3 phép · image · pdf) — phần này đã dùng làm nguồn cho `G12` / `G13` / `G14`. Tài liệu gốc (docs.missiona.co / prototype GitHub) vẫn chưa được đưa link trực tiếp vào lệnh review. | Leader dán link spec chốt vào vòng review sau, hoặc bổ sung `spec-features/admin/webhook-relay/feature-spec.md`. |
| I8 | `[MAJOR]` | `03-dev-impact.md` | Auto-fill `2026-09-08 by /new-task` nhưng checkbox "Tester verify auto-fill chính xác" **chưa tick**, và 4 mục đều `<Input thiếu>`. Chiều `dev-impact` của §1 vì vậy trống hoàn toàn. | Yêu cầu Dev bổ sung đánh giá 4 mục lên Redmine #40044 (đặc biệt 4.3 danh sách tính năng regression), rồi review lại §1 chiều (a). |
| I9 | `[MAJOR]` | 137/137 TC | **`spec_status` = null toàn bộ** (cột "Trạng thái đánh giá spec" trống) — trong khi ít nhất 2 TC ghi thẳng trong note "MÂU THUẪN CẦN LEADER QUYẾT" (NEW-26) và "CẦN LEADER CHỐT" (NEW-138), đúng loại case phải đánh `Spec không ghi` / `Đã hỏi leader`. | Điền `spec_status` cho các TC có note mâu thuẫn trước khi Leader duyệt. |
| I10 | `[MAJOR]` | Studio coverage | Studio tự báo **0/69 spec ref `covered`, 69/69 `partial`**; 20 ref chỉ có đúng 1 TC (`BR-04` · `BR-07` · `BR-23` · `BR-24` · `QA-spec-001/002/006/009/010/014/015/018/020` · `RR-03` · `C-07`…). Ref chỉ 1 TC thì mất TC đó là mất trắng chiều test. | Ưu tiên bổ sung TC cho các ref 1-TC thuộc nhóm `BR` (business rule). |
| I11 | `[MINOR]` | 16 mã quan điểm | Mã Studio **không có trong `framework/checklist-lme.md`**: `TOOL-NEGCTRL-001`(11) · `UI-TABLE-001`(7) · `SEC-INJECT-001`(5) · `JOB-002`(5) · `TOOL-ERRHYG-001`(4) · `API-001`(3) · `TOOL-AXIS-001`(2) · `TOOL-OLDREC-001`(2) · `PERF-LATENCY-001`(2) · `API-CONTRACT-001`(2) · `TOOL-SPECFIRST-001` · `TOOL-SCOPE-001` · `A11Y-001` · `TOOL-VAL2-001` · `AUTH-SESSION-001` · `TOOL-PAIRWISE-001`. **40/137 TC (29%)** không map được coverage về checklist LME. | Map lại về mã chuẩn (`JOB-002`→`JOB-001`, `SEC-INJECT-001`→`SEC-001`/`UI-INPUT-001`, `UI-TABLE-001`→`LIST-001`, `API-*`→`FUNC-*`), hoặc bổ sung mã mới vào checklist theo RULE-10. |
| I12 | `[MINOR]` | 137/137 TC | Tất cả TC `status = draft`, `reviewed = false`, `toolWritten.human = 0` (111 TC `author=AI` + 26 TC `haodtb@mcp`) — chưa có TC nào do QA người viết đối chứng, và bộ TC chưa qua duyệt. | Leader duyệt và chuyển trạng thái trước khi dùng làm bằng chứng release. |
| I13 | `[NIT]` | Evidence | `testcase_list` không trả cột Evidence nên snapshot `04-tc-list.md` để trống toàn bộ. Evidence **có tồn tại** trong Studio (bug items kèm `882/tc-15603/tc-15603-fail.png` + `trace.zip`) nhưng chỉ cho TC fail — chưa xác nhận được TC `pass` có evidence hay không (RULE-02). | Hỏi Studio/QA: run artifact có lưu screenshot cho case pass không; nếu không thì bổ sung cho nhóm TC bảo mật + job. |

---

## 5. TCs đề xuất bổ sung

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa012-quanlythe-タグ管理.md` (nhóm "Action gắn tag" 42 TC — **24 điểm gắn tag** `TC-TAG-180…203`) · `kho-tcs/fa015-quanlythongtinbanbe-友だち情報管理.md` (nhóm "Ghi giá trị từ màn admin" 17 TC `TC-FRI-199…215` · "Ghi giá trị từ tính năng khác" 11 TC `TC-FRI-216…226`). Nhóm "Backup & recover" `TC-FRI-310…319` **đã đọc nhưng loại khỏi phạm vi** theo xác nhận của Leader 2026-09-08. **Kho chưa có tính năng 「Webhook転送」** → phần màn mới không đối chiếu được. |
| Vùng regression phát hiện từ kho | (1) 24 điểm gắn tag `TC-TAG-180…203` → mỗi điểm phải phát notify kênh LME; (2) 28 lối ghi friend info `TC-FRI-199…226` + nhóm action + nhóm job ngày tháng → phải phát notify `friend_info`; (3) `kho-tcs/fa003` (自動応答) · `fa004` (リッチメニュー) · `fa009` (ステップ配信) là vùng đọc/ghi `callback_event` — mới dùng làm chỉ dấu vùng, **chưa soi từng TC**. |
| Conflict expected vs kho | `Không` — kho chưa có TC nào cho màn 「Webhook転送」. Mâu thuẫn hiện có là **TC vs code** (NEW-138 / BR-01) và **TC vs quyết định Dev** (5 bug reject) → đã đưa §6. |
| GAP dùng lại TC kho (không viết mới) | `G8 → TC-TAG-180…203` và `G7 → TC-FRI-199…226` dùng làm **danh sách lối ghi**, không copy nguyên TC: TC mới chỉ thêm chiều "có sinh event notify kênh LME hay không" lên đúng các lối đó. |
| Xác nhận chống trùng | Đã đối chiếu **137 TC ở BƯỚC 0** + 2 file kho — **không TC đề xuất nào trùng**. Riêng nhóm TC ma trận event / loại info: đã soi kỹ `NEW-136` (forward end-to-end, 4 loại event), `NEW-64` (8 loại nhưng ở tầng hàng đợi web), `NEW-31` (chỉ hiển thị cột 「イベント種別」), `NEW-125` (4 loại, tầng ghi callback_event), `NEW-73` / `NEW-118` / `NEW-134` (text · select · point-cộng) — TC đề xuất chỉ bổ sung **phần loại event / loại info chưa ai chạm**, không lặp lại loại đã có. `G5` (retry/timeout/2xx) và `G6` (batch, đa luồng, bản ghi kẹt) **KHÔNG viết TC mới**: NEW-137 · NEW-139 · NEW-106 · NEW-114 · NEW-115 · NEW-141 · NEW-142 · NEW-143 đã cover đúng chiều đó, chỉ chưa chạy → chuyển thành issue I4/I5. Tương tự `Q5`/`Q6` dùng lại NEW-52 / NEW-56 đang blocked. |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-BULK001-01 | UI | BULK-001 | Gắn tag hàng loạt sau bộ lọc | Normal | manual | Tất cả | Gắn tag hàng loạt cho đúng 10 friend đã lọc → sinh đúng 10 event notify kênh LME | - Kênh LME ON, URL trỏ về endpoint thu nhận đếm được request<br>- Bot có ≥ 50 friend; bộ lọc cho ra **đúng 10** friend chưa có tag T | 1. Vào 友だちリスト, đặt bộ lọc cho ra đúng 10 friend, ghi lại con số hiển thị<br>2. Chọn toàn bộ kết quả lọc → gắn tag T hàng loạt<br>3. Chờ xử lý xong, mở lại danh sách kiểm 10 friend đã có tag T<br>4. Đếm số request nhận được tại endpoint đích<br>5. Mở 「転送エラー」 tab LME kiểm không có dòng lỗi mới | Bộ lọc: friend chưa có tag T, còn theo dõi; tag T = `一括通知テスト` | Endpoint nhận **đúng 10** request loại tag/add, mỗi friend đúng 1 request, không thiếu không trùng. Số friend thật sự được gắn tag = 10, khớp 100% số đã lọc. 「転送エラー」 không phát sinh dòng mới | | Lấp `G8` · `Q3` · Đánh giá spec: Spec ghi rõ (BR-11/BR-14) · Evidence: ảnh số friend sau lọc + log endpoint đếm request · dẫn từ `TC-TAG-180…203` |
| TC-BULK001-02 | UI | BULK-001 | Gắn tag hàng loạt sau bộ lọc | Abnormal | manual | Tất cả | Gắn tag hàng loạt cho tập có friend ĐÃ CÓ sẵn tag → chỉ friend thật sự đổi mới sinh notify | - Kênh LME ON, endpoint đích đếm được request<br>- Bộ lọc cho ra 10 friend, trong đó **4 friend đã có sẵn** tag T | 1. Lọc ra 10 friend, ghi rõ 4 friend đã có tag T<br>2. Chọn tất cả → gắn tag T hàng loạt<br>3. Đếm request tại endpoint đích<br>4. Đối chiếu danh sách friend trong request với 6 friend chưa có tag | 10 friend (4 đã có tag T, 6 chưa) | Endpoint nhận **đúng 6** request, ứng đúng 6 friend chưa có tag. 4 friend đã có tag không sinh request nào (khớp hành vi NEW-68). Không request nào rơi về toàn bộ friend của bot | | Lấp `G8` · `Q3` · Đánh giá spec: Spec ghi rõ · Evidence: log endpoint + danh sách 10 friend trước thao tác |
| TC-BULK001-03 | UI | BULK-001 | Gắn tag hàng loạt sau bộ lọc | Boundary | manual | product | Gỡ tag hàng loạt trên tập lớn bằng quy mô khách lớn nhất → số notify khớp, job không dừng | - Kênh LME ON<br>- Bot production có tập friend gắn sẵn tag T ở quy mô bằng hoặc lớn hơn khách lớn nhất hiện tại (hỏi CS lấy số thật) | 1. Hỏi số friend lớn nhất một khách đang có, dựng tập tương đương gắn sẵn tag T<br>2. Lọc theo tag T, ghi lại tổng số<br>3. Gỡ tag T hàng loạt cho toàn bộ tập<br>4. Đo thời gian tới khi thao tác báo xong<br>5. Đếm request loại remove tại endpoint đích<br>6. Mở 「転送エラー」 tab LME đếm dòng lỗi mới | Số friend = quy mô khách lớn nhất (ghi con số thật vào kết quả) | Số request remove nhận được + số dòng 「転送エラー」 mới = **đúng** tổng số friend đã lọc, không bản ghi nào biến mất. Thao tác hoàn tất, job forward không dừng, không bản ghi nào kẹt ở trạng thái đang chạy | | Lấp `G8` · `Q3` · `Q12` · RULE-08 (job nền + tải lớn) · Đánh giá spec: Spec không ghi — ngưỡng do Leader chốt · Evidence: số liệu quy mô + thời gian chạy + bảng đối chiếu vào/ra |
| TC-FRIEND001-04 | UI | FRIEND-001 | Ma trận lối ghi friend info → notify | Normal | manual | Tất cả | Ghi giá trị friend info từ 5 lối phổ biến → mỗi lối sinh đúng 1 event notify `friend_info` | - Kênh LME ON, endpoint đích đếm được request<br>- Có sẵn: 1 form map friend info, 1 lịch salon/lesson map friend info, 1 file CSV import, 1 action gán friend info, màn chat 1:1 | 1. Ghi giá trị info F cho friend qua **màn chat 1:1**<br>2. Cho friend **trả lời form** có map info F<br>3. Cho friend **đặt lịch salon/lesson** có map info F<br>4. **Import CSV** ghi giá trị info F cho friend<br>5. Chạy **action gán friend info** từ kịch bản<br>6. Sau mỗi lối, đếm request tại endpoint và ghi loại event | Info F kiểu テキスト; giá trị khác nhau cho từng lối để truy vết | **Cả 5 lối** đều sinh đúng 1 request loại `friend_info` mang giá trị mới đúng như đã ghi. Lối nào không sinh request → ghi rõ tên lối và báo Leader (đúng dạng lỗi của bug Studio #857 ở nhánh tag) | | Lấp `G7` · Đánh giá spec: Spec ghi rõ (REQ-012/BR-11) · Evidence: log endpoint theo từng lối + ảnh giá trị info sau mỗi lối · dẫn từ `TC-FRI-199…226` |
| TC-FRIEND001-05 | UI | FRIEND-001 | Ma trận lối ghi friend info → notify | Abnormal | manual | Tất cả | Xoá giá trị friend info và thao tác điểm (cộng/trừ/set) → vẫn sinh notify đúng giá trị sau thao tác | - Như trên; thêm 1 info kiểu 点数 đã có giá trị | 1. Xoá giá trị info F của friend ở màn 友だち詳細<br>2. Cộng điểm cho info kiểu 点数 bằng multi action ở chat 1:1<br>3. Trừ điểm bằng action của kịch bản<br>4. Đặt điểm về 0<br>5. Đếm và đọc nội dung request tại endpoint sau từng thao tác | Info kiểu 点数 giá trị đầu = 100; cộng 50, trừ 30, set 0 | Mỗi thao tác sinh đúng 1 request `friend_info` mang giá trị SAU thao tác (150 → 120 → 0). Thao tác xoá giá trị cũng phát request, không im lặng. Không thao tác nào sinh 2 request | | Lấp `G7` · Đánh giá spec: Spec ghi rõ (QA-015) · Evidence: log endpoint từng thao tác · dẫn từ `TC-FRI-199…215` |
| TC-FRIEND001-06 | Data | FRIEND-001 | Ma trận lối ghi friend info → notify | Boundary | manual | product | Job action ngày tháng chạy đúng mốc → sinh notify `friend_info` từ tiến trình nền | - Kênh LME ON<br>- Info kiểu 年月日 có cấu hình lịch, đặt mốc chạy vào phút kế tiếp<br>- Chạy trên production vì job tách riêng theo môi trường | 1. Cấu hình info 年月日 để job chạy vào mốc T+2 phút cho 3 friend<br>2. Chờ qua mốc T<br>3. Kiểm giá trị info của 3 friend đã đổi trên màn<br>4. Đếm request `friend_info` tại endpoint đích<br>5. Kiểm 「転送エラー」 tab LME | 3 friend, mốc chạy sát ranh giới ngày (23:59 → 00:00 JST) | Job chạy đúng mốc, 3 friend đổi giá trị và endpoint nhận đúng 3 request `friend_info`. Không request nào lệch ngày do timezone. Không dòng lỗi mới ở 「転送エラー」 | | Lấp `G7` · `Q13` · RULE-08 (production tách 3 job) · Đánh giá spec: Spec không ghi · Evidence: log job + log endpoint + ảnh giá trị info · dẫn từ nhóm "Job action ngày tháng" của `fa015` |
| TC-REGSHARED001-08 | UI | REG-SHARED-001 | Regression 3 bảng nóng bị thêm cột | Normal | manual | Tất cả | Richmenu vẫn phản hồi đúng sau khi 3 bảng lịch sử được thêm cột | - Đã chạy migration ALTER `callback_event` · `tag_history` · `friend_info_history`<br>- OA đang áp dụng 1 richmenu có đủ 3 kiểu vùng: mở URL, gửi tin nhắn, postback<br>- Có richmenu thứ hai để kiểm luồng đổi richmenu theo action | 1. Từ app LINE, bấm lần lượt cả 3 vùng của richmenu đang áp dụng<br>2. Quan sát phản hồi thực tế trên app LINE sau từng lần bấm<br>3. Kích hoạt 1 action đổi richmenu (qua tag hoặc kịch bản), xác nhận richmenu trên app LINE đổi đúng<br>4. Mở màn quản trị kiểm lịch sử tương tác của friend vẫn ghi đủ<br>5. Kiểm 「転送エラー」 tab LINE không có dòng lỗi bất thường | 1 richmenu 3 vùng (URL · gửi tin · postback) + 1 richmenu thứ hai để đổi | Cả 3 vùng richmenu phản hồi đúng như trước migration: mở đúng URL, gửi đúng tin, chạy đúng action postback. Đổi richmenu bằng action vẫn đổi đúng trên app LINE. Lịch sử tương tác ghi đủ, không thiếu bản ghi nào. Cột mới thêm vào 3 bảng không làm đổi hành vi nghiệp vụ cũ |  | Lấp `G3` (richmenu là luồng duy nhất trong vùng regression Studio kê mà **không TC nào chạm**) · regression · Đánh giá spec: Spec ghi rõ — `dev_impact` Studio liệt kê richmenu trong vùng ALTER · Evidence: video thao tác 3 vùng richmenu trên app LINE + ảnh lịch sử tương tác |
| TC-JOB001-01 | Data | JOB-001 | Job forward — cân bằng vào/ra | Boundary | manual | product | Bắn liên tục vượt ngưỡng hàng đợi → số bản ghi vào = số forward thành công + số ghi lỗi | - Kênh LINE + LME đều ON, URL trỏ endpoint đếm được<br>- Nguồn phát event vượt `MAX_QUEUE_SIZE` (200) và vượt batch (500) trong 1 chu kỳ | 1. Ghi lại số bản ghi nguồn trước khi bắt đầu<br>2. Bắn liên tục > 500 event trong thời gian ngắn<br>3. Chờ job xử lý hết<br>4. Đếm: số request endpoint nhận, số dòng 「転送エラー」 mới của từng tab<br>5. Đếm lại số bản ghi nguồn đã chuyển sang hoàn tất / lỗi / bỏ qua | > 500 event trong 1 chu kỳ quét | **Số event vào = số forward thành công + số ghi lỗi + số bỏ qua**, không bản ghi nào biến mất hay kẹt ở trạng thái đang chạy. Job không dừng giữa chừng, không phải khởi động lại thủ công | | Lấp `Q2` · RULE-08 (job nền) · Đánh giá spec: Spec ghi rõ (extraInfo §2) · Evidence: log job + bảng đối chiếu vào/ra |
| TC-JOB001-02 | Data | JOB-001 | Job forward — endpoint đích chết kéo dài | Abnormal | manual | product | Endpoint đích chết liên tục nhiều chu kỳ → job vẫn sống, kênh không tự tắt, lỗi ghi đủ | - Kênh LME ON, URL trỏ endpoint chủ động trả 500 / không phản hồi<br>- Có ≥ 30 event chờ forward | 1. Đặt endpoint đích luôn trả 500<br>2. Bắn 30 event, chờ qua ít nhất 3 chu kỳ job<br>3. Quan sát job còn chạy không<br>4. Mở 「転送エラー」 tab LME đếm dòng lỗi và đọc cột lý do<br>5. Kiểm trạng thái switch của kênh LME trên màn<br>6. Sửa endpoint về 200 rồi bắn 1 event mới | 30 event, endpoint trả 500 | Job **không dừng**, tiếp tục quét chu kỳ sau. Mỗi event chỉ ghi **đúng 1** dòng lỗi (không nhân bản theo số lần retry). Switch kênh LME vẫn ON — hệ thống không tự tắt kênh. Event mới sau khi endpoint hồi phục được forward thành công ngay chu kỳ kế | | Lấp `Q2` · RULE-08 · Đánh giá spec: Spec ghi rõ (BR-15/BR-21) · Evidence: log job thấy retry + backoff + ảnh 「転送エラー」 |
| TC-DATA001-02 | Data | DATA-001 | Hàng đợi phía web (`webhook_relay_queue`) | Normal | manual | product | Sau nhiều ngày chạy thật, hàng đợi web không phình vô hạn / có nơi tiêu thụ | - Kênh LINE + LME ON trên bot production có traffic thật<br>- Đã bật `WEBHOOK_RELAY_ENABLED` | 1. Ghi lại số dòng hàng đợi web tại thời điểm T<br>2. Để hệ thống chạy với traffic thật ≥ 24h<br>3. Đọc lại số dòng tại T+24h<br>4. Hỏi Dev: có job nào tiêu thụ / dọn bảng này không<br>5. Kiểm dung lượng bảng và ảnh hưởng tới thời gian ghi webhook | Traffic thật 24h của 1 bot đang hoạt động | Một trong hai kết luận, phải được Leader chốt: (a) có bộ tiêu thụ → số dòng ổn định hoặc được dọn định kỳ; (b) không có bộ tiêu thụ → bảng phình tuyến tính theo traffic và chứa payload webhook (PII) không ai đọc → **phải gỡ trước khi release** | | Lấp `G1` · RULE-08 · Đánh giá spec: Spec không ghi — REQ-021 nêu "phải có bộ tiêu thụ hoặc được gỡ", chưa ai chốt · Evidence: 2 lần đếm cách 24h + xác nhận của Dev |
| TC-DATAAUDIT001-02 | UI | DATA-AUDIT-001 | Nhật ký thao tác nhạy cảm | Normal | manual | Tất cả | Tái tạo signing secret và bật/tắt kênh đều ghi nhật ký đủ 4 thông tin, không lộ secret | - Tài khoản chủ + 1 staff được cấp quyền màn<br>- Kênh LME đã có secret | 1. Bằng tài khoản chủ: bật kênh LINE → lưu<br>2. Tái tạo signing secret qua hộp xác nhận<br>3. Tắt kênh LME → lưu<br>4. Đăng nhập bằng staff, lặp lại bước 1 và 3<br>5. Mở nguồn nhật ký thao tác, đọc các bản ghi vừa sinh | 3 thao tác × 2 tài khoản | Mỗi thao tác sinh **đúng 1** bản ghi nhật ký đủ 4 thông tin: người thực hiện (phân biệt chủ vs staff) / thời gian / hành động / giá trị cũ→mới. Không bản ghi nào chứa giá trị secret (kể cả dạng che). Không ghi nhầm loại thao tác | | Lấp `Q9` · Đánh giá spec: Spec ghi rõ (technical-spec §7) · Evidence: ảnh nhật ký sau từng thao tác |
| TC-DATAAUDIT001-03 | API | DATA-AUDIT-001 | Nhật ký thao tác nhạy cảm | Abnormal | manual | Tất cả | Gọi thẳng EP-02 / EP-03 (không qua giao diện) vẫn ghi nhật ký đúng người thực hiện | - Phiên đăng nhập hợp lệ của staff có quyền<br>- Công cụ gọi HTTP giữ nguyên cookie phiên + mã chống giả mạo | 1. Gọi EP-02 lưu cấu hình kênh LME với URL mới<br>2. Gọi EP-03 tái tạo signing secret<br>3. Mở màn 「Webhook転送」 xác nhận cấu hình đã đổi<br>4. Đọc nhật ký thao tác của 2 lời gọi trên | URL mới `https://relay-api.example.com/lme` | 2 lời gọi sinh đủ 2 bản ghi nhật ký, ghi đúng staff đã gọi (không ghi là hệ thống / để rỗng), đủ giá trị cũ→mới. Không bản ghi nào chứa secret | | Lấp `Q9` · Đánh giá spec: Spec không ghi · Evidence: response 2 lời gọi + ảnh nhật ký |
| TC-FUNCDATE001-01 | UI | FUNC-DATE-001 | Card 「転送エラー」 — mốc thời gian | Boundary | manual | Tất cả | Cột 「発生日時」 và 「最終更新」 hiển thị đúng múi giờ, không nhảy ngày ở mốc 23:59 → 00:00 | - Kênh LINE ON, endpoint đích trả lỗi để sinh bản ghi lỗi<br>- Có thể tạo lỗi sát 23:59 và ngay sau 00:01 (JST) | 1. Sinh 1 lỗi lúc 23:59 (JST), 1 lỗi lúc 00:01 hôm sau<br>2. Mở 「転送エラー」 tab LINE<br>3. Đọc giá trị cột 「発生日時」 của 2 dòng<br>4. Bấm icon sort đổi chiều, đọc lại thứ tự<br>5. Bấm 「最新に更新」, đọc mốc 「最終更新」<br>6. So mốc hiển thị với đồng hồ JST thật lúc thao tác | 2 lỗi ở 2 phía mốc nửa đêm JST | 「発生日時」 hiển thị **giờ JST**, đúng ngày của từng lỗi (không lệch sang ngày hôm trước/sau do UTC). Sort xếp đúng thứ tự thời gian thật ở cả 2 chiều. 「最終更新」 khớp thời điểm bấm refresh theo JST | | Lấp `Q13` · Đánh giá spec: Spec không ghi · Evidence: ảnh 2 dòng lỗi + đồng hồ JST tại thời điểm chụp |

| TC-INTGLINE001-03 | Data | INTG-LINE-001 | Forward callback LINE — message | Normal | manual | Tất cả | message text — friend gửi tin nhắn chữ | - Kênh LINE ON, 「転送先URL」 trỏ endpoint thu nhận đọc được body từng request<br>- 1 tài khoản LINE test đã là bạn của OA<br>- Đã bật cờ forward và job callback đang chạy | 1. Từ app LINE, friend gửi cho OA 1 tin nhắn chữ có cả tiếng Nhật, emoji và xuống dòng<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | Nội dung: `テスト40044\n改行あり 🎉` | Endpoint nhận request loại `message` với kiểu tin là text và nội dung **nguyên văn** đúng như đã gửi (giữ emoji, giữ ký tự xuống dòng, không mojibake). Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · tách từ `NEW-136` · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-04 | Data | INTG-LINE-001 | Forward callback LINE — message | Normal | manual | Tất cả | message sticker — friend gửi sticker | - Kênh LINE ON, 「転送先URL」 trỏ endpoint thu nhận đọc được body từng request<br>- 1 tài khoản LINE test đã là bạn của OA<br>- Đã bật cờ forward và job callback đang chạy | 1. Từ app LINE, friend gửi cho OA 1 sticker thuộc bộ sticker cơ bản<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | 1 sticker bất kỳ, ghi lại tên bộ sticker đã chọn | Endpoint nhận request loại `message` với kiểu tin là sticker, kèm đủ mã bộ sticker và mã sticker để bên thứ 3 dựng lại được đúng sticker đã gửi. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-05 | Data | INTG-LINE-001 | Forward callback LINE — message | Normal | manual | Tất cả | message location — friend gửi vị trí | - Kênh LINE ON, 「転送先URL」 trỏ endpoint thu nhận đọc được body từng request<br>- 1 tài khoản LINE test đã là bạn của OA<br>- Đã bật cờ forward và job callback đang chạy | 1. Từ app LINE, friend gửi cho OA 1 vị trí bằng chức năng chia sẻ vị trí<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | 1 vị trí có tên địa điểm và địa chỉ rõ ràng (ví dụ 1 nhà ga) | Endpoint nhận request loại `message` với kiểu tin là location, kèm đủ tên địa điểm, địa chỉ, vĩ độ và kinh độ khớp vị trí đã gửi. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-06 | Data | INTG-LINE-001 | Forward callback LINE — message | Normal | manual | product | message image — friend gửi ảnh | - Kênh LINE ON, 「転送先URL」 trỏ endpoint thu nhận đọc được body từng request<br>- 1 tài khoản LINE test đã là bạn của OA<br>- Đã bật cờ forward và job callback đang chạy | 1. Từ app LINE, friend gửi cho OA 1 ảnh chụp từ máy<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | 1 ảnh JPG dung lượng thường (~1-3MB) | Endpoint nhận request loại `message` với kiểu tin là image kèm mã tin nhắn để bên thứ 3 tải nội dung ảnh từ LINE. Chức năng tải media của LME vẫn chạy như trước (ảnh xem được ở màn chat 1:1). Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · RULE-08 — media chạy job riêng trên production · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-07 | Data | INTG-LINE-001 | Forward callback LINE — message | Normal | manual | product | message video — friend gửi video | - Kênh LINE ON, 「転送先URL」 trỏ endpoint thu nhận đọc được body từng request<br>- 1 tài khoản LINE test đã là bạn của OA<br>- Đã bật cờ forward và job callback đang chạy | 1. Từ app LINE, friend gửi cho OA 1 video ngắn<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | 1 video ~10 giây | Endpoint nhận request loại `message` với kiểu tin là video kèm mã tin nhắn và độ dài video. Video vẫn xem được ở màn chat 1:1 của admin. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · RULE-08 — media · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-08 | Data | INTG-LINE-001 | Forward callback LINE — message | Normal | manual | product | message audio — friend gửi tin thoại | - Kênh LINE ON, 「転送先URL」 trỏ endpoint thu nhận đọc được body từng request<br>- 1 tài khoản LINE test đã là bạn của OA<br>- Đã bật cờ forward và job callback đang chạy | 1. Từ app LINE, friend ghi âm và gửi cho OA 1 tin thoại<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | 1 tin thoại ~5 giây | Endpoint nhận request loại `message` với kiểu tin là audio kèm mã tin nhắn và độ dài. Tin thoại vẫn nghe được ở màn chat 1:1. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · RULE-08 — media · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-09 | Data | INTG-LINE-001 | Forward callback LINE — message | Boundary | manual | product | message file — friend gửi file loại khác (không phải ảnh/video/audio) | - Kênh LINE ON, 「転送先URL」 trỏ endpoint thu nhận đọc được body từng request<br>- 1 tài khoản LINE test đã là bạn của OA<br>- Đã bật cờ forward và job callback đang chạy | 1. Từ app LINE, friend gửi cho OA 1 file tài liệu và 1 file **không có phần mở rộng**<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | File 1: PDF nhiều trang, tên file có tiếng Nhật; file 2: file không có phần mở rộng | Endpoint nhận **2 request** loại `message` kiểu file, mỗi request kèm tên file và dung lượng đúng. Tên file tiếng Nhật không mojibake; file không có phần mở rộng vẫn được nhận diện là file, không rơi vào nhánh lỗi. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · RULE-08 — Catalog D: file không phần mở rộng xử lý khác giữa staging và production · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-10 | Data | INTG-LINE-001 | Forward callback LINE — message | Normal | manual | Tất cả | message text sinh từ tap button (action kiểu gửi tin) | - Kênh LINE ON, 「転送先URL」 trỏ endpoint thu nhận đọc được body từng request<br>- 1 tài khoản LINE test đã là bạn của OA<br>- Đã bật cờ forward và job callback đang chạy<br>- Đã gửi cho friend 1 tin có button với action kiểu **gửi tin nhắn** (nội dung cố định) | 1. Từ app LINE, friend **bấm button** có action gửi tin nhắn<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | Button có nội dung gửi đi là `ボタン押下40044` | Endpoint nhận request loại `message` kiểu text, nội dung đúng chuỗi đã cấu hình cho button — **không** phải loại `postback`. Phân biệt được với message do friend tự gõ nhờ nội dung khớp cấu hình button. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-11 | Data | INTG-LINE-001 | Forward callback LINE — postback | Normal | manual | Tất cả | postback từ tap richmenu | - Kênh LINE ON, 「転送先URL」 trỏ endpoint thu nhận đọc được body từng request<br>- 1 tài khoản LINE test đã là bạn của OA<br>- Đã bật cờ forward và job callback đang chạy<br>- OA đang áp dụng 1 richmenu có vùng gắn action kiểu postback | 1. Từ app LINE, friend mở richmenu và **bấm vào vùng** có action postback<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | Vùng richmenu có dữ liệu postback `rm40044` | Endpoint nhận request loại `postback` với dữ liệu postback đúng chuỗi đã cấu hình cho vùng richmenu đó. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · tách từ `NEW-136` · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-12 | Data | INTG-LINE-001 | Forward callback LINE — postback | Normal | manual | Tất cả | postback từ tap imagemap | - Kênh LINE ON, 「転送先URL」 trỏ endpoint thu nhận đọc được body từng request<br>- 1 tài khoản LINE test đã là bạn của OA<br>- Đã bật cờ forward và job callback đang chạy<br>- Đã gửi cho friend 1 tin imagemap có vùng gắn action postback | 1. Từ app LINE, friend **bấm vào vùng** trên ảnh imagemap có action postback<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | Vùng imagemap có dữ liệu postback `im40044` | Endpoint nhận request loại `postback` với dữ liệu postback đúng chuỗi đã cấu hình cho vùng imagemap đó, phân biệt được với postback của richmenu. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-13 | Data | INTG-LINE-001 | Forward callback LINE — postback | Normal | manual | Tất cả | postback từ tap button trong tin nhắn | - Kênh LINE ON, 「転送先URL」 trỏ endpoint thu nhận đọc được body từng request<br>- 1 tài khoản LINE test đã là bạn của OA<br>- Đã bật cờ forward và job callback đang chạy<br>- Đã gửi cho friend 1 tin có button gắn action kiểu postback | 1. Từ app LINE, friend **bấm button** có action postback trong tin nhắn<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | Button có dữ liệu postback `btn40044` | Endpoint nhận request loại `postback` với dữ liệu postback đúng chuỗi của button. Nếu button là kiểu chọn ngày giờ thì phần giá trị ngày giờ friend chọn cũng phải có trong request. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · tách từ `NEW-136` · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-14 | Data | INTG-LINE-001 | Forward callback LINE — follow | Normal | manual | Tất cả | follow — kết bạn thường | - Kênh LINE ON, endpoint đọc được body<br>- 1 tài khoản LINE **chưa từng** kết bạn với OA<br>- Đã bật cờ forward | 1. Dùng tài khoản LINE chưa từng kết bạn, tìm OA và **add bạn** theo cách thông thường<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | 1 tài khoản LINE mới hoàn toàn với OA | Endpoint nhận request loại `follow`. Cờ đánh dấu bỏ chặn **không bật** (false hoặc không có trường) — đây là bạn mới, không phải bỏ chặn. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · tách từ `NEW-136` · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-15 | Data | INTG-LINE-001 | Forward callback LINE — follow | Normal | manual | Tất cả | follow — kết bạn bằng cách quét mã QR | - Kênh LINE ON, endpoint đọc được body<br>- 1 tài khoản LINE chưa từng kết bạn với OA<br>- Có mã QR add bạn của OA (nếu có QR kèm tham số nguồn thì dùng loại đó để đối chiếu) | 1. Dùng tài khoản LINE chưa kết bạn, **quét mã QR** của OA rồi add bạn<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | 1 tài khoản LINE mới + mã QR add bạn của OA | Endpoint nhận request loại `follow` giống trường hợp kết bạn thường. Mọi trường LINE gửi kèm khi add bằng QR đều được giữ **nguyên trạng**, không bị hệ thống lược bỏ. Nếu LINE có gửi thông tin nguồn add bạn thì thông tin đó phải có mặt trong request. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-16 | Data | INTG-LINE-001 | Forward callback LINE — block/unblock | Normal | manual | Tất cả | unfollow — friend block OA | - Kênh LINE ON, 「転送先URL」 trỏ endpoint thu nhận đọc được body từng request<br>- 1 tài khoản LINE test đã là bạn của OA<br>- Đã bật cờ forward và job callback đang chạy | 1. Từ app LINE, friend **chặn (block)** OA<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | 1 friend đang là bạn của OA | Endpoint nhận request loại `unfollow` với định danh đúng friend vừa chặn. Trạng thái friend trên màn quản trị cũng chuyển sang đã chặn như trước khi có tính năng. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · tách từ `NEW-136` · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-17 | Data | INTG-LINE-001 | Forward callback LINE — block/unblock | Normal | manual | Tất cả | follow (isUnblocked) — friend bỏ chặn OA | - Kênh LINE ON, endpoint đọc được body<br>- 1 tài khoản LINE **đang chặn** OA (dùng lại friend của TC unfollow) | 1. Từ app LINE, friend **bỏ chặn (unblock)** OA<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | 1 friend đang ở trạng thái đã chặn OA | Endpoint nhận request loại `follow` với **cờ bỏ chặn bật (true)** — phân biệt được với kết bạn mới ở TC-INTGLINE001-14. Định danh friend đúng friend vừa bỏ chặn. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · tách từ `NEW-136` · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-18 | Data | INTG-LINE-001 | Forward callback LINE — group/room | Normal | manual | Tất cả | join — OA được thêm vào group | - Kênh LINE ON, endpoint đọc được body<br>- Có 1 group LINE test, OA **chưa** nằm trong group | 1. Từ app LINE, mời OA vào group test<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | 1 group LINE có ≥ 2 thành viên | Endpoint nhận request loại `join` kèm định danh group đúng group vừa mời. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-19 | Data | INTG-LINE-001 | Forward callback LINE — group/room | Normal | manual | Tất cả | leave — OA bị kick hoặc rời group | - Kênh LINE ON, endpoint đọc được body<br>- OA **đang** nằm trong group test | 1. Từ app LINE, kick OA khỏi group (hoặc cho OA rời group)<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | Group test đang có OA | Endpoint nhận request loại `leave` kèm định danh group đúng group vừa rời. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-20 | Data | INTG-LINE-001 | Forward callback LINE — group/room | Normal | manual | Tất cả | memberJoined — thành viên khác vào group có OA | - Kênh LINE ON, endpoint đọc được body<br>- OA đang nằm trong group test<br>- Có 1 tài khoản LINE thứ hai chưa ở trong group | 1. Cho tài khoản LINE thứ hai **vào group** đang có OA<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | 1 tài khoản LINE thứ hai | Endpoint nhận request loại `memberJoined` kèm định danh group và định danh thành viên vừa vào. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-21 | Data | INTG-LINE-001 | Forward callback LINE — group/room | Normal | manual | Tất cả | memberLeft — thành viên khác rời group có OA | - Kênh LINE ON, endpoint đọc được body<br>- OA và tài khoản LINE thứ hai cùng ở trong group test | 1. Cho tài khoản LINE thứ hai **rời group** đang có OA<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | Tài khoản LINE thứ hai đang ở trong group | Endpoint nhận request loại `memberLeft` kèm định danh group và định danh thành viên vừa rời. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-22 | Data | INTG-LINE-001 | Forward callback LINE — khác | Normal | manual | Tất cả | unsend — friend thu hồi tin nhắn | - Kênh LINE ON, 「転送先URL」 trỏ endpoint thu nhận đọc được body từng request<br>- 1 tài khoản LINE test đã là bạn của OA<br>- Đã bật cờ forward và job callback đang chạy<br>- Friend vừa gửi cho OA 1 tin nhắn chữ | 1. Từ app LINE, friend **thu hồi** tin nhắn vừa gửi<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | 1 tin nhắn chữ vừa gửi, ghi lại nội dung | Endpoint nhận request loại `unsend` kèm mã tin nhắn đúng tin vừa bị thu hồi (đối chiếu với mã ở request `message` trước đó). Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-23 | Data | INTG-LINE-001 | Forward callback LINE — khác | Normal | manual | Tất cả | videoPlayComplete — friend xem hết video có mã theo dõi | - Kênh LINE ON, 「転送先URL」 trỏ endpoint thu nhận đọc được body từng request<br>- 1 tài khoản LINE test đã là bạn của OA<br>- Đã bật cờ forward và job callback đang chạy<br>- Đã gửi cho friend 1 video message có gắn mã theo dõi | 1. Từ app LINE, friend mở và **xem hết** video message có mã theo dõi<br>2. Xác nhận nghiệp vụ LINE gốc vẫn chạy đúng (tin trả lời tự động / hành vi màn hình như trước)<br>3. Chờ job callback xử lý xong bản ghi sự kiện vừa sinh<br>4. Đọc body request nhận tại endpoint bên thứ 3, ghi lại loại event và các trường đặc trưng<br>5. Mở tab 「LINE公式アカウントデータ転送」, bấm 「最新に更新」 kiểm 「転送エラー」 không có dòng mới | 1 video message gắn mã theo dõi truy vết được | Endpoint nhận request loại `videoPlayComplete` kèm **đúng mã theo dõi** đã gắn khi gửi video. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-24 | API | INTG-LINE-001 | Forward callback LINE — khác | Abnormal | auto | Tất cả | deactivated — OA bị tạm ngưng | - Kênh LINE ON, endpoint thu nhận đọc được body<br>- Không dựng được điều kiện thật → gửi webhook mô phỏng đúng payload chuẩn LINE kèm chữ ký hợp lệ vào endpoint nhận webhook của LME<br>- Payload lấy theo tài liệu Messaging API **mới nhất tại thời điểm test** (RULE-05) | 1. Gửi 1 webhook mô phỏng loại `deactivated` vào endpoint nhận webhook của LME<br>2. Xác nhận LME trả 200 về phía LINE<br>3. Chờ job callback xử lý xong<br>4. Đọc body request nhận tại endpoint bên thứ 3<br>5. Kiểm 「転送エラー」 tab LINE không có dòng mới | 1 payload mô phỏng `deactivated` theo tài liệu LINE mới nhất | Endpoint nhận request loại `deactivated`, chuỗi loại event giữ nguyên như LINE gửi — hệ thống **không** bỏ qua âm thầm chỉ vì loại này không có nghiệp vụ tương ứng trong LME. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · RULE-05 · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-25 | API | INTG-LINE-001 | Forward callback LINE — khác | Abnormal | auto | Tất cả | activated — OA được khôi phục | - Kênh LINE ON, endpoint thu nhận đọc được body<br>- Không dựng được điều kiện thật → gửi webhook mô phỏng đúng payload chuẩn LINE kèm chữ ký hợp lệ vào endpoint nhận webhook của LME<br>- Payload lấy theo tài liệu Messaging API **mới nhất tại thời điểm test** (RULE-05) | 1. Gửi 1 webhook mô phỏng loại `activated` vào endpoint nhận webhook của LME<br>2. Xác nhận LME trả 200 về phía LINE<br>3. Chờ job callback xử lý xong<br>4. Đọc body request nhận tại endpoint bên thứ 3<br>5. Kiểm 「転送エラー」 tab LINE không có dòng mới | 1 payload mô phỏng `activated` theo tài liệu LINE mới nhất | Endpoint nhận request loại `activated`, chuỗi loại event giữ nguyên như LINE gửi, không bị lọc bỏ. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · RULE-05 · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-26 | API | INTG-LINE-001 | Forward callback LINE — khác | Abnormal | auto | Tất cả | membership — friend tham gia / rời gói hội viên | - Kênh LINE ON, endpoint thu nhận đọc được body<br>- Không dựng được điều kiện thật → gửi webhook mô phỏng đúng payload chuẩn LINE kèm chữ ký hợp lệ vào endpoint nhận webhook của LME<br>- Payload lấy theo tài liệu Messaging API **mới nhất tại thời điểm test** (RULE-05) | 1. Gửi 1 webhook mô phỏng loại `membership` vào endpoint nhận webhook của LME<br>2. Xác nhận LME trả 200 về phía LINE<br>3. Chờ job callback xử lý xong<br>4. Đọc body request nhận tại endpoint bên thứ 3<br>5. Kiểm 「転送エラー」 tab LINE không có dòng mới | 1 payload mô phỏng `membership` (cả nhánh tham gia và nhánh rời, nếu tài liệu LINE tách 2 nhánh thì gửi đủ 2) | Endpoint nhận đủ request loại `membership` cho từng nhánh, giữ nguyên chuỗi loại event và các trường hội viên LINE gửi kèm. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · RULE-05 · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-27 | API | INTG-LINE-001 | Forward callback LINE — khác | Abnormal | auto | Tất cả | beacon — friend đi vào vùng beacon | - Kênh LINE ON, endpoint thu nhận đọc được body<br>- Không dựng được điều kiện thật → gửi webhook mô phỏng đúng payload chuẩn LINE kèm chữ ký hợp lệ vào endpoint nhận webhook của LME<br>- Payload lấy theo tài liệu Messaging API **mới nhất tại thời điểm test** (RULE-05) | 1. Gửi 1 webhook mô phỏng loại `beacon` vào endpoint nhận webhook của LME<br>2. Xác nhận LME trả 200 về phía LINE<br>3. Chờ job callback xử lý xong<br>4. Đọc body request nhận tại endpoint bên thứ 3<br>5. Kiểm 「転送エラー」 tab LINE không có dòng mới | 1 payload mô phỏng `beacon` có mã thiết bị và loại sự kiện beacon | Endpoint nhận request loại `beacon` kèm mã thiết bị nguyên trạng, không bị lọc bỏ. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · RULE-05 · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGLINE001-28 | API | INTG-LINE-001 | Forward callback LINE — khác | Abnormal | auto | Tất cả | accountLink — friend liên kết tài khoản | - Kênh LINE ON, endpoint thu nhận đọc được body<br>- Không dựng được điều kiện thật → gửi webhook mô phỏng đúng payload chuẩn LINE kèm chữ ký hợp lệ vào endpoint nhận webhook của LME<br>- Payload lấy theo tài liệu Messaging API **mới nhất tại thời điểm test** (RULE-05) | 1. Gửi 1 webhook mô phỏng loại `accountLink` vào endpoint nhận webhook của LME<br>2. Xác nhận LME trả 200 về phía LINE<br>3. Chờ job callback xử lý xong<br>4. Đọc body request nhận tại endpoint bên thứ 3<br>5. Kiểm 「転送エラー」 tab LINE không có dòng mới | 1 payload mô phỏng `accountLink` cho cả nhánh liên kết thành công và nhánh thất bại | Endpoint nhận đủ request loại `accountLink` cho từng nhánh, giữ nguyên kết quả liên kết và mã liên kết LINE gửi kèm. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · RULE-05 · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGHOOK001-02 | API | INTG-HOOK-001 | Forward callback LINE — 1 request nhiều event | Boundary | auto | product | Callback gộp chung — 1 request của LINE chứa nhiều event | - Kênh LINE ON, endpoint đọc được body và đếm được request<br>- Gửi 1 webhook mô phỏng có mảng sự kiện gồm **nhiều event khác loại** trong cùng 1 request, chữ ký hợp lệ | 1. Gửi 1 request webhook chứa 5 event trong cùng mảng sự kiện: `message` (text) · `follow` · `unfollow` · `postback` · `unsend`<br>2. Xác nhận LME trả 200 cho request gộp đó<br>3. Đếm số bản ghi sự kiện gọi lại được tạo<br>4. Chờ job xử lý xong, đếm và đọc body các request nhận tại endpoint bên thứ 3<br>5. Đối chiếu từng event đã gửi với từng request đã nhận<br>6. Kiểm 「転送エラー」 tab LINE | 1 request chứa 5 event khác loại trong 1 mảng sự kiện | **Không event nào bị mất**: 5 event trong 1 request được tách thành 5 bản ghi sự kiện và bên thứ 3 nhận đủ 5 event, đúng loại, đúng thứ tự phát sinh. Không gộp 5 event thành 1 request duy nhất tới bên thứ 3 (trừ khi contract chốt khác — phải ghi rõ), không nhân đôi event nào. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · Đây là hành vi thật của LINE: 1 webhook có thể chứa nhiều event · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-INTGHOOK001-03 | API | INTG-HOOK-001 | Forward callback LINE — đủ 15 loại 1 chu kỳ | Boundary | auto | product | Bắn đủ 15 loại event liên tiếp trong 1 chu kỳ job → nhận đúng 15 request | - Kênh LINE ON, endpoint đếm và đọc được body<br>- Chuẩn bị đủ 15 payload theo danh sách phase 1 | 1. Gửi liên tiếp 15 webhook (mỗi loại 1 lần) trong khoảng thời gian ngắn hơn 1 chu kỳ quét của job<br>2. Chờ job xử lý xong<br>3. Đếm số request tại endpoint, lập bảng đối chiếu 15 loại đã gửi ↔ 15 loại đã nhận<br>4. Đếm số dòng 「転送エラー」 mới<br>5. Kiểm không còn bản ghi nào kẹt ở trạng thái đang chạy | 15 loại: message · unsend · follow · unfollow · join · leave · postback · videoPlayComplete · deactivated · activated · memberJoined · memberLeft · membership · beacon · accountLink | Bên thứ 3 nhận **đúng 15 request**, ánh xạ 1-1 với 15 loại đã gửi — không thiếu, không gộp, không nhân đôi. Tổng số event vào = số forward thành công + số ghi lỗi. Không bản ghi nào kẹt trạng thái đang chạy sau khi job kết thúc. Mỗi event chỉ sinh **đúng 1 request** ra bên thứ 3, không thiếu không nhân đôi; request kênh LINE **không** có header `X-Lme-Signature`; bản ghi nguồn kết thúc ở trạng thái hoàn tất và 「転送エラー」 không phát sinh dòng mới. |  | Lấp `G12` · `Q14` · `Q2` · RULE-08 (job nền) · Đánh giá spec: Spec ghi rõ — phạm vi loại callback do human chốt 2026-09-08 · Evidence: body request nhận tại endpoint + ảnh thao tác trên app LINE / payload đã gửi |
| TC-FRIEND001-07 | UI | FRIEND-001 | Notify friend info — kiểu 年月日 | Normal | manual | product | Info kiểu 年月日 ở cả 2 chế độ (ngày hiện tại / ngày chỉ định) đều sinh notify với giá trị ngày đúng | - Kênh LME ON, endpoint đọc được body<br>- Có 2 trường info kiểu 年月日: trường A cấu hình đăng ký **ngày hiện tại**, trường B đăng ký **ngày chỉ định**<br>- Chạy trên production vì luồng ngày tháng đi qua job | 1. Kích hoạt action ghi trường A (đăng ký ngày hiện tại) cho friend<br>2. Đọc giá trị trường A trên màn 友だち詳細<br>3. Kích hoạt action ghi trường B với ngày chỉ định `2026/12/31`<br>4. Đọc giá trị trường B trên màn<br>5. Chờ job xử lý, đọc body 2 request nhận tại endpoint<br>6. Đối chiếu giá trị ngày trong body với giá trị hiển thị trên màn | Trường A: ngày hiện tại; trường B: `2026/12/31` | Endpoint nhận **đúng 2 request** loại `friend_info`, mỗi request có định danh trường thông tin đúng và giá trị ngày **khớp giá trị hiển thị trên màn** (A = ngày hôm nay theo JST, B = `2026/12/31`). Không request nào lệch 1 ngày do timezone | | Lấp `G13` · `Q15` · `Q13` (timezone) · RULE-08 (đi qua job) · Đánh giá spec: Spec ghi rõ — human chốt 2026-09-08 · Evidence: ảnh giá trị trên màn + body 2 request · dẫn từ nhóm "Job action ngày tháng" của `fa015` |
| TC-FRIEND001-08 | UI | FRIEND-001 | Notify friend info — kiểu 画像 và PDF | Normal | manual | product | Đăng ký và thay thế file (ảnh / PDF) đều sinh notify đúng loại, payload theo contract đã chốt | - Kênh LME ON, endpoint đọc được body<br>- Có 1 trường info kiểu 画像 và 1 trường kiểu PDF<br>- Chuẩn bị 2 ảnh và 2 file PDF khác nhau | 1. Đăng ký ảnh thứ nhất cho trường 画像 của friend<br>2. Thay bằng ảnh thứ hai<br>3. Đăng ký PDF thứ nhất cho trường PDF<br>4. Thay bằng PDF thứ hai<br>5. Đọc body 4 request nhận tại endpoint<br>6. Nếu body có đường dẫn file, mở thử đường dẫn đó từ ngoài hệ thống | Ảnh JPG + PNG; PDF 1 trang + PDF nhiều trang | Endpoint nhận **đúng 4 request** loại `friend_info` với định danh trường đúng và thao tác đúng (đăng ký / cập nhật). Payload khớp contract Leader chốt: nếu có kèm đường dẫn file thì đường dẫn **mở được** và trỏ đúng file vừa đăng ký; nếu chỉ gửi định danh trường thì **không** được lộ đường dẫn nội bộ. Không request nào chứa nội dung file | | Lấp `G13` · `Q15` · RULE-08 (media phải kiểm trên production — đường dẫn qua `p.lmes.jp` / B2) · Đánh giá spec: Spec không ghi — contract payload cho info kiểu file chưa ai chốt, đã đưa §6 · Evidence: body 4 request + kết quả mở đường dẫn file |
| TC-FRIEND001-09 | UI | FRIEND-001 | Notify friend info — xoá giá trị mọi loại | Abnormal | manual | Tất cả | Xoá giá trị của 年月日 · 点数 · 画像 · PDF đều sinh notify thao tác xoá, không kèm giá trị | - Kênh LME ON, endpoint đọc được body<br>- 4 trường info (年月日 · 点数 · 画像 · PDF) của friend đều **đang có giá trị** | 1. Xoá giá trị trường 年月日 → lưu<br>2. Xoá giá trị trường 点数 → lưu<br>3. Xoá file của trường 画像 → lưu<br>4. Xoá file của trường PDF → lưu<br>5. Sau mỗi thao tác, đọc body request nhận tại endpoint<br>6. Kiểm lại màn 友だち詳細 xác nhận 4 trường đã trống | 4 trường đang có giá trị trước khi xoá | Endpoint nhận **đúng 4 request** loại `friend_info` với thao tác xoá, mỗi request có định danh trường bị xoá và **không kèm giá trị** (khớp quy tắc đã ghi ở NEW-73). Màn hiển thị 4 trường đã trống. Không thao tác nào sinh 2 request, cũng không thao tác nào im lặng | | Lấp `G13` · `Q15` (bổ sung loại case còn thiếu của FRIEND-001) · Đánh giá spec: Spec ghi rõ · Evidence: body 4 request + ảnh màn sau khi xoá |
| TC-SEC001-02 | Data | SEC-001 | Phạm vi phase 1 của webhook LME | Normal | manual | Tất cả | Sự kiện ngoài phase 1 (form · booking salon/lesson · mua item) KHÔNG bị gửi ra bên thứ 3 | - Kênh LME ON, endpoint ghi lại **mọi** request nhận được<br>- Có 1 form đang phát hành, 1 lịch salon và 1 lịch lesson mở booking, 1 item đang bán<br>- Các đối tượng này **không** gắn action tag / friend info | 1. Ghi lại số request endpoint đã nhận tại thời điểm T<br>2. Cho friend trả lời form<br>3. Cho friend booking salon<br>4. Cho friend booking lesson<br>5. Cho friend mua item<br>6. Chờ qua ít nhất 2 chu kỳ job, đếm lại số request và đọc loại event của các request mới (nếu có) | 4 thao tác của LINE user, không thao tác nào kèm action gắn tag / ghi friend info | Endpoint **không nhận thêm request nào** từ 4 thao tác trên — phase 1 chỉ gửi `tag` và `friend_info`. Nếu có request phát sinh → ghi rõ loại event và báo Leader ngay: dữ liệu ngoài phạm vi đã duyệt đang bị đẩy ra bên thứ 3 | | Lấp `G14` · Đánh giá spec: Spec ghi rõ — human chốt phạm vi phase 1 ngày 2026-09-08 · Evidence: log endpoint trước/sau + danh sách 4 thao tác đã thực hiện |

**Dòng §1 / §2 không đề xuất TC mới** (đã có TC, chỉ thiếu kết luận — xử lý ở §4):
`G2` → re-triage NEW-138 (I1) · `G4` → chạy NEW-120/121/122 trước khi release (I4) · `G5` → chạy NEW-137/139/106/156/93/94 trên product (I4, I5) · `G6` → chạy NEW-114/115/141/142/143/144 (I4) · `Q5` / `Q6` → gỡ blocked NEW-52 / NEW-56 (I3) · `Q7` → chốt spec consent rồi mới sửa NEW-26 (§6) · `Q8` → chạy lại nhóm job/URL trên staging + production (I5) · `Q10` → rà lại TC theo REG-SPEC-001 (I6) · `Q11` → chạy NEW-121 đồng thời với traffic mới (I4) · `G10` / `G11` → không cần TC, cần Dev xác nhận + gắn nhãn `requirement_keys` (I11).

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | `BR-01` 「加工せずそのまま」 (forward nguyên trạng kênh LINE) | Bản dựng **bọc payload LINE vào envelope** thêm 3 trường (định danh LINE OA, id bản ghi callback_event, loại event). Chốt: giữ envelope và sửa BR-01 + expected của TC, hay bỏ envelope để đúng "nguyên trạng"? Bên nhận đã tích hợp theo dạng nào? | `TC vs code` — NEW-138 (exec `pass`, expected "không thêm bớt") vs note chính TC + `dev_impact` Studio | `Dev + PM` |
| 2 | `BR-24` / `QA-spec-018` / `RR-01` — bước đồng ý gửi dữ liệu cá nhân lần bật đầu | Spec nghiệp vụ yêu cầu hộp cảnh báo + ô tích đồng ý + lưu ai đồng ý lúc nào; bản dựng **không có**, bug #40519 đã bị **reject**. Chốt: bỏ hẳn requirement (cập nhật spec + gỡ REQ-016) hay implement. Đây là bước duy nhất chặn việc gửi PII của friend ra hệ thống bên thứ 3 | `TC vs quyết định Dev` — NEW-26 fail, bug #40519 rejected | `PM + Leader` |
| 3 | `BR-09` / `BR-10` — phân quyền màn ở tầng server | Bug #40536 (staff **chưa** được cấp quyền vẫn gọi được các đường dẫn dữ liệu của màn) bị **reject**. Nếu reject đúng chuẩn thì expected của NEW-52 sai và phải sửa; nếu reject nhầm thì đây là lỗ hổng lộ signing secret. Không được để nguyên trạng thái blocked | `TC vs quyết định Dev` — NEW-52 blocked, bug #40536 rejected | `Dev + Leader` |
| 4 | Hành vi khi thiếu quyền (403 vs 302) | Bug #40534 (middleware chặn bằng redirect 302 và **mất luôn LINE OA đang chọn**) bị **reject**. Chốt chuẩn chung: màn mới trả 403 + thông điệp, hay redirect. Ảnh hưởng expected của NEW-51 / 52 / 56 | `TC vs quyết định Dev` | `Dev + Leader` |
| 5 | `REQ-021` — `webhook_relay_queue` | Studio ghi rõ bảng hàng đợi phía web **không ai tiêu thụ** (dead path). Chốt: gỡ bảng khỏi release, hay bổ sung bộ tiêu thụ + chính sách dọn dữ liệu (bảng chứa payload webhook = PII) | `dev_impact Studio` — "mismatch còn lại" | `Dev + PM` |
| 6b | Contract payload notify của info kiểu **画像 / PDF** | Phase 1 có ghi/xoá info kiểu file nhưng chưa ai chốt notify gửi gì: kèm đường dẫn file (bên thứ 3 tải được — và tải được bao lâu, có cần ký URL không) hay chỉ gửi định danh trường. Ảnh hưởng trực tiếp tới rủi ro lộ file của khách | `Không có spec` — G13 / TC-FRIEND001-08 | `PM + Dev` |
| 6c | Danh sách loại event LINE được forward | Chốt rõ: forward **tất cả** loại event LINE nhận được, hay theo allow-list? Nếu allow-list thì 5 loại hiếm (`deactivated` · `activated` · `membership` · `beacon` · `accountLink`) có nằm trong không. Hiện 6/15 loại không TC nào chạm ở bất kỳ tầng nào | `Spec human 2026-09-08 vs bộ TC hiện có` — G12 / TC-INTGLINE001-24 … TC-INTGLINE001-28 | `PM + Dev` |
| 7 | Bộ mã lỗi job ↔ chuỗi hiển thị web | 3 `error_code` do job sinh không có trong bảng dịch tiếng Nhật của web → rơi về 「その他のエラー」. Chốt bổ sung 3 chuỗi hay chấp nhận gộp | `dev_impact Studio` | `Dev` |
| 8 | Phạm vi accessibility của màn | Bug #40535 (icon sort không thao tác được bằng bàn phím) bị **reject** → ghi rõ a11y có nằm trong chuẩn nghiệm thu màn mới hay không, để TC `A11Y-001` khỏi lặp lại tranh cãi | `TC vs quyết định Dev` | `Leader` |
