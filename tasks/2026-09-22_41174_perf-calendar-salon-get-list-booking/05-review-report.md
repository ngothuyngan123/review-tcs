# 05 — Review Report

> Draft cho Leader verify.

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | `(1) MCP LME TEST STUDIO — task #319 (ticket 41174), testcase_list` |
| Tổng số TC review | `24` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Trả lời**:

| Chiều | Kết quả |
|---|---|
| **(a) dev-impact** — Dev tự kê ở `03-dev-impact.md` mục 4 | `6/10 mục có TC` — **CHƯA ĐỦ** |
| **(b) diff code** — suy từ diff thật (Studio `dev_impact` + `spec_delta`) | `5/9 điểm có TC` — **CHƯA ĐỦ** |

**Kết luận**: `11/19 điểm đủ TC · 5 GAP · 3 RISK` — trong đó `G7` đã thu hẹp còn "xác nhận index tồn tại trên dev/staging/product" theo quyết định Leader 2026-09-22 (bỏ hướng test lúc migration đang chạy).

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | `BUG — thiếu index trên bảng dùng chung + DATE() bọc cột ⇒ full scan 2 lần/request + filesort` | `dev-impact` | `NEW-17`, `NEW-21`, `NEW-25` | `RISK — cả 3 TC khai env_scope=local, 0/3 đã chạy; Dev cũng CHƯA chạy được EXPLAIN (MySQL dev tắt). Ticket là perf trên production (85s, step.lme.jp) ⇒ RULE-08: không kết luận được từ local` | `[MAJOR]` |
| G2 | `F4 / T2 — xuất CSV danh sách booking (truy vấn khác trên cùng bảng, Dev tự kê)` | `dev-impact` | `không có` | `GAP — 0/24 TC chạm luồng xuất CSV` | `[MAJOR]` |
| G3 | `F4 / T2 — kiểm tra trùng giờ khi đặt lịch (truy vấn khác trên cùng bảng, Dev tự kê)` | `dev-impact` | `không có` | `GAP — 0/24 TC kiểm chặn đặt trùng slot sau khi thêm index` | `[MAJOR]` |
| G4 | `T1 — bộ lọc sidebar 「絞り込み設定」 (6 filter) trên CHÍNH endpoint bị sửa` | `dev-impact` | `không có` | `GAP — 0/24 TC gọi endpoint kèm filter. Spec SCR-SLN-02a ghi filter 「予約日」 = date_booking BETWEEN date_start AND date_end ⇒ điều kiện ngày THỨ BA trên cùng cột, cùng query builder vừa bị viết lại, nhưng không TC nào chạm` | `[BLOCKER]` |
| G5 | `Nhánh else của điều kiện ngày tab 「本日の予約」 (date_booking rỗng / format lạ → giữ nguyên value gốc)` | `diff code` | `NEW-11`, `NEW-12` | `RISK — expected version 2 đã hạ xuống "tuân theo cơ chế validation hiện tại", không nêu mã trạng thái / nội dung cụ thể ⇒ không kiểm chứng được nhánh nào, và trái REQ-005 (risk High). Xem §4 C4` | `[MAJOR]` |
| G6 | `Timezone của Carbon::now() khi tính mốc [now−7d 00:00:00, now 23:59:59]` | `diff code` | `không có` | `GAP — Studio dev_impact nêu thẳng "Nhạy biên GIÂY (endOfDay=23:59:59) và timezone của Carbon::now()". NEW-7 chỉ cover biên giây, 0 TC kiểm timezone app vs DB vs JST` | `[MAJOR]` |
| G7 | `Migration 2 index đã được deploy trên từng môi trường` | `diff code` | `NEW-18` (local) | `RISK — NEW-18 chỉ chạy migration ở local; 0 TC xác nhận index đã tồn tại trên dev / staging / production. Không có index thì mọi kết luận về hiệu năng đều vô nghĩa` | `[MAJOR]` |
| G8 | `Điều kiện date_booking >= hôm nay KHÔNG có giới hạn trên (đơn ngày hẹn xa hơn 7 ngày)` | `diff code` | `không có` | `GAP — nằm chung khối điều kiện vừa viết lại, 0 TC dùng đơn có ngày hẹn > +7 ngày. Spec KHÔNG có rule nào về giới hạn trên: db-mapping.md:641 bảng "Bộ lọc tương ứng" ghi tab new_booking CHỈ có created_at DESC (không điều kiện ngày), ui-spec.md:116 chỉ 1 dòng "Đặt lịch mới trong 7 ngày" không nói theo trường nào. Kho TC-SLN-45 + TC-SLN-82 cho thấy khoảng "hôm nay 〜 +7 ngày" là MẶC ĐỊNH CỦA BỘ LỌC 予約日, không phải điều kiện của tab ⇒ phải xác nhận bộ lọc mặc định có được gửi kèm mọi request của tab hay không (URL production trong ticket KHÔNG có date_start/date_end). Xem §4 C3` | `[MAJOR]` |

**Mục ĐỦ TC (không ghi dòng)** — chiều (a): `F1` `F2` `F3` `D1` `D2` `D3` · chiều (b): file Repository.php · file migration · biên giây tab 新着 (NEW-7) · index trên bảng dùng chung / rủi ro index trùng (NEW-20, NEW-23, NEW-24) · `paginate()` count query cùng WHERE (NEW-16, NEW-4) · repo method chỉ 1 caller (Studio đã xác nhận `service l.134`).

⚠️ **Toàn bộ 24 TC chưa chạy** (`exec.untested = 24`) — bảng trên đánh giá coverage theo **thiết kế TC**, không phải theo bằng chứng đã test. Xem §5 I1.

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `24 quan điểm Trigger khớp task · 14 chưa cover đủ · 3 loại khỏi phạm vi`

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `LIST-001` | `Trung bình` | `GAP — màn danh sách có search/filter/pagination, nhưng 0/24 TC áp bộ lọc 絞り込み設定 (予約日 / 予約開始時間 / コース / スタッフ / 予約ステータス / 決済ステータス) lên endpoint vừa sửa. Kho ĐÃ có 8 TC cho từng bộ lọc (TC-SLN-82…TC-SLN-89) ⇒ chạy lại bộ đó sau release, chỉ viết mới phần kho KHÔNG có: giao giữa bộ lọc 予約日 và điều kiện ngày vừa bị viết lại của tab` | `[MAJOR]` |
| Q2 | `ENV-003` ★ | `Cao` | `RISK — 9/24 TC khai env_scope=local, envAuto cho thấy 0 run ở prd; ticket là perf production. RULE-08: performance / job / loadbalance không được kết luận từ local-staging` | `[BLOCKER]` |
| Q3 | `PERF-LARGE-001` | `Trung bình → Cao (xử lý danh sách lớn)` | `RISK — có 3 TC (NEW-17, NEW-21, NEW-25) nhưng expected đều có nhánh thoát "GIỚI HẠN MÔI TRƯỜNG / BỎ QUA" ⇒ TC pass mà không chứng minh gì` | `[MAJOR]` |
| Q4 | `DATA-COUNT-001` | `Cao` | `RISK — có TC cho bộ đếm 「x - y / n行」 + total (NEW-1, NEW-5, NEW-6, NEW-16) nhưng 0 TC cho 2 con số 「予約数」/「キャンセル数」 của chính tab này (kho TC-SLN-44) — 2 số này đếm theo nhóm status trên cùng WHERE vừa bị viết lại` | `[MAJOR]` |
| Q5 | `BULK-001` | `Cao` | `GAP — tab có tổ hợp bộ lọc + 「リクエスト一括操作」 (kho TC-SLN-47) nhưng 0 TC kiểm phạm vi chọn sau khi nguồn danh sách đổi. Hạ từ BLOCKER xuống MAJOR: code thao tác hàng loạt KHÔNG nằm trong diff, rủi ro chỉ ở phạm vi danh sách` | `[MAJOR]` |
| Q7 | `JOB-001` ★ | `Cao` | `GAP — job remind salon đọc cùng bảng calendar_salon_line_booking, 0 TC xác nhận job vẫn gửi đúng sau khi thêm index. Hạ từ BLOCKER xuống MAJOR: index không đổi kết quả truy vấn, chỉ đổi execution plan — nhưng đây là đường gửi tin ra LINE nên vẫn cần 1 TC smoke` | `[MAJOR]` |
| Q9 | `FUNC-DATE-001` | `Cao` | `RISK — 4 TC (NEW-7, NEW-8, NEW-14, NEW-26) đều là Boundary, 0 Abnormal riêng cho tính toán ngày (timezone, thời điểm chạy vắt qua 00:00) ⇒ RULE-01` | `[MAJOR]` |
| Q10 | `FUNC-004` | `Cao` | `RISK — chỉ có Boundary (NEW-4, per_page 50/100); thiếu Abnormal cho tham số ngoài miền (per_page=9999, page=0, page=-1) ⇒ RULE-01` | `[MAJOR]` |
| Q11 | `FUNC-003` | `Trung bình → Cao (field ngày giờ)` | `RISK — NEW-11/NEW-12 là Abnormal nhưng expected mơ hồ; thiếu Normal cho các định dạng ngày hợp lệ KHÁC mà Carbon::parse vẫn nhận (2026/09/23, 20260923, 2026-9-3)` | `[MAJOR]` |
| Q12 | `SEC-001` | `Cao` | `RISK — NEW-1 chỉ kiểm cách ly giữa 2 lịch salon CÙNG bot; 0 TC kiểm WHERE scope trên 2 bot / 2 tài khoản sau khi điều kiện bị viết lại (RULE-07: "tạo bản ghi ở 2 tài khoản để kiểm chứng WHERE")` | `[MAJOR]` |
| Q13 | `CONC-003` | `Trung bình → Cao (phân trang + filter, 2 tab cùng gọi 1 API)` | `RISK — NEW-3 có chuyển tab + F5 nhưng expected không kiểm response về SAI THỨ TỰ khi bấm tab / đổi per_page liên tiếp (chính note của NEW-3 nêu ý định này nhưng expected không đo)` | `[MAJOR]` |
| Q14 | `CONC-001` | `Cao` | `GAP — bảng là giới hạn dùng chung (slot đặt lịch); Dev tự kê "kiểm tra trùng giờ" ở T2 nhưng 0 TC kiểm 2 khách đặt cùng slot sau khi thêm index` | `[BLOCKER]` |
| Q15 | `OUT-EXPORT-001` ★ | `Trung bình → BẮT BUỘC (nâng Cao)` | `GAP — Dev tự kê "xuất CSV" ở T2; 0 TC xuất CSV danh sách booking` | `[MAJOR]` |
| Q16 | `SYNC-APP-001` | `Trung bình → Cao (danh sách booking là flow critical, có mặt trên cả web và app)` | `GAP — 2 tab 「新着の予約」 / 「本日の予約」 có trên CẢ web lẫn app quản trị di động, nhưng 0 TC nào mở app thật để xem. NEW-24 chỉ GỌI ENDPOINT của app, không kiểm hiển thị; kho FA-020 có 5 TC "App mobile" (TC-SLN-474…478) nhưng không TC nào chạm 2 tab này. Rủi ro cụ thể: sau fix, web dùng điều kiện mới (mốc giây) còn app vẫn giữ nhánh query riêng bọc DATE() (note của NEW-24) ⇒ 2 nơi có thể hiện KHÁC tập đơn ở biên` | `[MAJOR]` |

<!-- Số Q nhảy cách (thiếu Q6, Q8) là do 2 quan điểm đã bị loại khỏi phạm vi — xem ghi chú ngay dưới. Giữ nguyên mã cũ để không gãy tham chiếu ở §7. -->

**Quan điểm đã loại khỏi phạm vi**:
- `REG-RUN-001` (Cao) và `DEPLOY-LIVE-001` ★ (Cao) — **loại theo quyết định Leader 2026-09-22**: task có migration thì TC **chỉ xác nhận index đã tồn tại trong DB ở dev / staging / product** (`TC-DATAMIG001-01`), **không** viết TC chạy migration trong lúc đang mở màn hình / đang có traffic, không đo thời gian `ALTER TABLE`. Lý do: ở 3 môi trường đó migration do **quy trình phát hành** chạy, tester không dựng lại được tình huống và cũng không phải việc của QA; chưa có index thì kết luận là **chưa deploy**, không phải lỗi sản phẩm.
- `DATA-BACKUP-001` ★ (Cao) — Trigger "tính năng thêm/đổi bảng DB" khớp (thêm 2 index), nhưng **loại**: (i) index phụ không đổi cột/dữ liệu nên logic copy bot (copy theo bảng + cột) không bị chạm; (ii) **đặt lịch salon KHÔNG nằm trong phạm vi copy bot** — `kho-tcs/fa033` MT-12 ("chưa có job backup của salon và lesson"), MT-28 ("Đang không support backup item / calendar"), `feature-spec.md` §7.5 liệt kê `booking_calendar` là bảng disabled `is_enable=0`. ⇒ GAP giả, không đề xuất TC.

---

## 3. TC trùng lặp nội dung

| Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| DUP-1 | `NEW-17` | `NEW-25` | `DUP-SUBSET` | Cùng `PERF-LATENCY-001` × `Normal` × "gọi endpoint get-list-booking 2 tab trên lịch nhiều đơn rồi so thời gian phản hồi trước/sau fix" × cùng tiền đề "DB dữ liệu lớn, migration đã chạy, env local" → cùng expected "thời gian sau fix nhỏ hơn trước fix, response không đổi". NEW-17 bao trọn NEW-25 và cụ thể hơn (5 lần/tab, so trung vị, đối chiếu danh sách trả về); phần riêng của NEW-25 ("không timeout", "response đúng cấu trúc") đã có ở `NEW-9` + `NEW-16` | `[MINOR]` |

- **Gate đã chạy**: giả định xóa `NEW-25` → chiều (a) `BUG` vẫn còn `NEW-17` + `NEW-21`; quan điểm `PERF-LATENCY-001` vẫn còn 2 TC. Coverage §1 + §2 **còn nguyên** ⇒ đề nghị xóa hợp lệ (hoặc gộp phần "không timeout" vào NEW-17).
- **Chồng lấp một phần, KHÔNG đề nghị xóa**: `NEW-4` (UI, FUNC-004) và `NEW-16` (API, API-CONTRACT-001) cùng kiểm "tổng số bằng nhau ở 50/100, không trùng không mất dòng", nhưng mỗi TC có phần riêng không thay thế được — NEW-4 có mũi tên vô hiệu + reset về trang 1, NEW-16 có trang vượt giới hạn. **Giữ cả 2**.
- `DUP-INFLATE`: không có — `PERF-LATENCY-001` vốn đã chỉ có Normal (0 Abnormal/Boundary) nên trùng không che được chiều nào; `BUG` đã ở trạng thái RISK ở §1 trước khi trừ TC trùng.
- Xóa thật do human thực hiện trên Studio (`testcase_delete`).

---

## 4. Mâu thuẫn trong TCs

| # | Loại | TC liên quan | Nội dung check trùng nhau | Expected A | Expected B / nguồn đối chiếu | Khả năng sai | Severity | Ai chốt |
|---|---|---|---|---|---|---|---|---|
| C1 | `CONF-SPEC` | `NEW-2`, `NEW-7` (cả 2 là `tc_group=ui`) | Thứ tự và giá trị của "thời điểm cập nhật" **quan sát trên màn** tab 「新着の予約」 | `NEW-2`: "toàn bộ danh sách xếp theo **thời điểm cập nhật** giảm dần" · `NEW-7`: "**Cột thời điểm cập nhật** của BIEN-4 hiển thị đúng ngày N kèm giờ 23:59" | `feature-spec.md:173`, `feature-spec.md:754`, `db-mapping.md:626` đều ghi cột 1 「操作が行われた日時」 = `created_at`. Đối chứng: response EP-07 (`api-spec.md:188`) **không có** field `created_at`, chỉ có `received_booking_date` + `userUpdateTime` | ✅ **ĐÃ CHỐT — Leader xác nhận 2026-09-22: cột 1 「操作が行われた日時」 = `user_update_time`.** ⇒ **spec SAI**, `NEW-2` và `NEW-7` **ĐÚNG** — cột đó có thật trên màn, giữ nguyên `tc_group = ui`, không phải sửa TC. Chỉ còn việc sửa spec (§8 dòng 1) | `[MINOR]` (hạ từ `[MAJOR]` sau khi chốt — chỉ còn là lỗi tài liệu) | `Đã chốt: Leader` |
| C2 | `CONF-SPEC` | Toàn bộ nhánh `new_booking` (`NEW-2`, `NEW-7`, `NEW-14`, `NEW-26`) + index `idx_cslb_salon_user_update` | Tab 「新着の予約」 sắp xếp theo trường nào | Cả bản cũ lẫn bản fix (SQL Dev dump ở `03-dev-impact.md` mục 2 + Studio `dev_impact`): lọc và sắp xếp theo **`user_update_time DESC`** — và index mới được tạo đúng theo cặp `(calendar_salon_id, user_update_time)` | `db-mapping.md:641` bảng "Bộ lọc tương ứng": `tab_query=new_booking` → **`created_at DESC`** — "新着 — sắp xếp theo thời gian nhận". Bảng này **không ghi bất kỳ điều kiện ngày nào** cho `new_booking` (không có cửa sổ 7 ngày, không có `date_booking >= today`) | ✅ **ĐÃ GIẢI QUYẾT theo C1** — cột 1 là `user_update_time` nên "sắp xếp theo thời gian nhận" ở `db-mapping.md:641` chính là sort theo cột 1, chỉ bị ghi nhầm tên trường thành `created_at`. ⇒ **spec sai, code đúng, index ĐÚNG cột** — bỏ khả năng "index đánh sai cột". Rủi ro root cause của bản fix không còn liên quan điểm này | `[MINOR]` (hạ từ `[MAJOR]`, **không còn khả năng lên BLOCKER**) | `Đã chốt: Leader` |
| C3 | `CONF-KHO` | `NEW-2` (và REQ-002) | Khoảng ngày hẹn mặc định hiển thị trên màn danh sách | Mọi đơn có `date_booking >= hôm nay` đều hiện, **không có giới hạn trên** (NEW-2 chỉ dùng đơn ngày mai / ngày kia nên không phát hiện được) | kho `TC-SLN-45`: "Mặc định hiển thị booking từ hôm nay đến **7 ngày sau**", màn hiện dòng 「表示期間：2024年7月2日(火) 〜 2024年7月9日(火)」 — và kho `TC-SLN-82`: "Filter booking theo khoảng ngày — **mặc định: từ ngày hiện tại đến 7 ngày sau**" | **Không hẳn là mâu thuẫn — là thiếu định nghĩa**: 2 TC kho khớp nhau cho thấy khoảng 7 ngày là **mặc định của bộ lọc 予約日** (`date_start`/`date_end`), không phải điều kiện của tab. Cần chốt: bộ lọc mặc định đó **có được gửi kèm mọi request** của tab 「新着の予約」 không? Nếu CÓ thì query thật có **3 điều kiện trên `date_booking`** (tab + filter) — vùng chưa TC nào chạm; nếu KHÔNG thì đơn ngày hẹn +30 ngày vẫn hiện và nhãn 表示期間 đang gây hiểu nhầm. URL production trong ticket **không có** `date_start`/`date_end` ⇒ nghiêng về "không gửi", nhưng chưa đủ để kết luận | `[MAJOR]` | `Leader` |
| C4 | `CONF-SPEC` | `NEW-11`, `NEW-12` (cả 2 đã sửa lên `version 2` ngày 2026-09-22) | Hành vi khi `date_booking` rỗng / thiếu / không phải chuỗi ngày, ở tab 「本日の予約」 | "Response phải **tuân theo cơ chế xử lý validation hiện tại của hệ thống**; không 500 và không trả sai dữ liệu" — không nêu mã trạng thái, không nêu nội dung trả về | Studio **REQ-005** (risk **High**, nguồn `kho: CS-162 (#39523)`): "endpoint phải **giữ hành vi bản cũ: trả HTTP 200 với danh sách rỗng**, không trả lỗi 500 và không trả nhầm bản ghi của ngày khác". `api-spec.md:164` khai `date_booking` là param **không bắt buộc**, không mô tả nhánh giá trị lỗi | `TC bị hạ expected quá mức` — mất bằng chứng cho REQ-005 và cho nhánh `else` của điều kiện ngày (G5) **HOẶC** `REQ-005 chặt hơn hành vi thật` — chưa ai xác nhận bản cũ thực sự trả 200 + rỗng | `[MAJOR]` | `Dev + Leader` |

**Đã rà**: `24` TC × `spec-features/admin/salon-booking/` — `feature-spec.md` (§SCR-SLN-02a, EP-07), `ui/ui-spec.md` (SCR-SLN-02a), `web/api-spec.md` (EP-07 params + response fields), `web/logic-spec.md` (§2.2 `getListBooking()`, §3.2 model), `db/db-mapping.md` (bảng cột màn + bảng "Bộ lọc tương ứng") + `kho-tcs/fa020-datlichsalon-サロン・面談予約.md` (nhóm "Tab 本日/新着の予約" `TC-SLN-42`…`TC-SLN-48` và nhóm "Modal filter booking & shift" `TC-SLN-82`…`TC-SLN-89`) → phát hiện **C1, C2, C3, C4**.
`CONF-TC` (2 TC trong bộ mâu thuẫn nhau): **không có** — đã so từng cặp TC cùng tab / cùng thao tác, không cặp nào có expected loại trừ nhau.
⚠️ **Spec KHÔNG mô tả điều kiện lọc của tab 「新着の予約」 ở bất kỳ file nào** — `db-mapping.md:641` chỉ ghi sort, `ui-spec.md:116` chỉ có 1 dòng "Đặt lịch mới trong 7 ngày" không nói theo trường nào. Đây là lý do `G6` (timezone) và `G8` (giới hạn trên) **không thể tự quyết từ tài liệu** mà phải hỏi Dev — xem §8 dòng 5.

**Trạng thái sau khi Leader chốt (2026-09-22)**: `C1` + `C2` **đã đóng** — cột 1 「操作が行われた日時」 = `user_update_time`, spec sai ở 4 chỗ (§8 dòng 1 + 2), TC `NEW-2`/`NEW-7` giữ nguyên. `C3` + `C4` **còn mở**, vẫn làm mất chuẩn đánh giá Đạt/Không đạt → giữ dòng `RISK` tương ứng ở §1 (`G5`, `G8`) và §2 (`Q9`, `Q11`).

📌 **Hệ quả cần lưu ý khi test** (phát sinh từ chốt C1, không phải mâu thuẫn): `user_update_time` cho phép **NULL** (`db-mapping.md:106`) — đơn do admin tạo hoặc khách chưa từng sửa thì cột 1 「操作が行われた日時」 sẽ **rỗng**. Ở tab 「新着の予約」 các đơn này bị loại khỏi danh sách (`NEW-26` đã cover), nhưng ở tab 「本日の予約」 chúng **vẫn hiện với cột 1 rỗng**. Hành vi hiển thị này **không nằm trong diff**, nên không đẻ TC mới — chỉ cần người chạy `NEW-1` biết trước để không báo nhầm là lỗi của bản fix.

---

## 5. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | Toàn bộ `24` TC | **0/24 TC đã chạy** — `exec.untested = 24`, `ranAt = null`, mọi TC `last_exec = null`, `status = draft`. Pass rate **0%**. `envAuto` ghi `staging: runs=1` nhưng `automated = 0` ⇒ không có kết quả nào. Task đang `tc-ready` + `reviewState = leader` nhưng **không có một bằng chứng nào** cho bản fix | Chạy tối thiểu: NEW-1, NEW-2, NEW-7, NEW-14, NEW-16 (tập kết quả + biên) và NEW-18 (index tồn tại) trước khi Leader duyệt. Không có TC fail nào để triage **vì chưa chạy**, không phải vì đã pass |
| I2 | `[MAJOR]` | `NEW-7`, `NEW-9`, `NEW-17`, `NEW-18`, `NEW-19`, `NEW-20`, `NEW-21`, `NEW-25`, `NEW-26` (9/24) | **RULE-08 / ENV-003** — toàn bộ TC chứng minh hiệu năng + EXPLAIN + migration chỉ khai `env_scope = local`. Ticket là **performance trên production** (p95 17s, đỉnh 85s, `step.lme.jp` / `s.lmes.jp`, 13 user). Không TC nào khai `product` ⇒ bộ TC này **không thể kết luận "đã hết chậm"** | Thêm `TC-ENV003-01` (§7) đo trên production sau release, đối chiếu report chậm 24h của chính AI check-performance |
| I3 | `[MAJOR]` | `NEW-21`, `NEW-17`, `NEW-25` | Expected tự cho phép trả **"BỎ QUA / GIỚI HẠN MÔI TRƯỜNG"** khi không lấy được EXPLAIN hoặc dữ liệu quá nhỏ. `NEW-21` là TC **duy nhất** chứng minh root cause (truy vấn thực sự dùng index mới) — mà Dev cũng **chưa từng chạy EXPLAIN** (MySQL dev Connection refused) ⇒ root cause có nguy cơ **không bao giờ được chứng minh** | Chỉ định 1 env có DB đủ dữ liệu để chạy NEW-21; coi "không lấy được EXPLAIN" là **blocked**, KHÔNG phải pass |
| I4 | `[MAJOR]` | `[AP-6]` `03-dev-impact.md` mục 4.1 + 4.3 | Mục 4.1 Dev ghi là **"File thay đổi"**, không phải list function. `F4`/`T2` chỉ nêu 4 nhóm chung chung ("xem theo tuần/tháng, danh sách đã xoá, xuất CSV, kiểm tra trùng giờ") — **không có tên method / endpoint**. Bằng chứng đã sót thật — **2 nhóm**: (a) `NEW-24` phát hiện endpoint danh sách booking của **app quản trị di động** (`app/Http/Controllers/Api/CalendarSalonController.php:539-558`) tự dựng query riêng cùng mẫu bọc `DATE()`; (b) đối chiếu bảng "Coverage theo màn hình/chức năng" của kho FA-020 còn 2 nhóm nữa đọc cùng bảng lọc theo lịch mà Dev không kê: **`Googleカレンダー連携`** (`CalendarSalonGoogleCalendarService`) và **`Googleスプレッドシート連携`** (`CalendarSalonGoogleSheetService`) | Yêu cầu Dev kê danh sách **method + endpoint** đọc `calendar_salon_line_booking` lọc theo `calendar_salon_id`, rồi rà lại §1 G2/G3. Không đề xuất TC cho 2 luồng Google sync vì index chỉ đổi execution plan, không đổi kết quả — nhưng Dev phải xác nhận 2 luồng đó không dùng method đã sửa |
| I5 | `[MAJOR]` | `[AP-3]` `NEW-23` | 1 TC duy nhất cover cả **4 màn** của `T2` (tuần / tháng / đã xoá / gán nhân viên) với tiền đề "có đơn ở tuần, ở tháng, 1 đơn đã xoá" — **không có edge state**: lịch nhiều nghìn đơn, đơn xoá quá 90 ngày (kho `MT-14`: booking xoá > 90 ngày không hiển thị), lịch không có đơn nào. Thêm nữa, `NEW-23` **bỏ hẳn màn 「Calendar theo ngày」** — nhóm lớn nhất của kho trong họ màn lịch (16 TC, `TC-SLN-49`…) và cũng đọc cùng bảng lọc theo lịch | Tách TC theo từng màn (**thêm màn lịch theo ngày**) + thêm tiền đề dữ liệu lớn và đơn xoá > 90 ngày |
| I6 | `[MAJOR]` | `24/24` TC | `spec_status = null` toàn bộ — **không TC nào ghi `Trạng thái đánh giá spec`**. Hệ quả đã hiện ra: 2 TC có expected mơ hồ (§4 C4) và 2 TC phải nhờ Leader chốt lại mới biết là đúng hay sai (§4 C1 — spec ghi sai trường của cột 1) đều là do không ai đối chiếu spec trước khi viết | Điền `Spec ghi rõ` / `Spec không ghi` / `Đã hỏi leader` cho mọi TC chạm điều kiện lọc + cột hiển thị |
| I7 | `[MAJOR]` | `NEW-24` (note) | Phát hiện quan trọng **chỉ nằm trong ghi chú TC**, không có ticket: nhánh app quản trị di động **vẫn giữ nguyên mẫu `DATE()` chậm** và không dùng method đã sửa ⇒ sau release này endpoint app mobile vẫn có thể chậm, trong khi ticket 41174 sẽ được đóng | Mở **ticket riêng** cho endpoint app mobile như chính TC đề nghị, trước khi đóng #41174 |
| I8 | `[MINOR]` | `[AP-4]` Studio `spec_delta` | `diffStrategy = "stat"`, `hasCode = false` → reviewer chỉ thấy 2 file + `55 insertions / 3 deletions`, **không đọc được nội dung diff**. Mọi khẳng định về nhánh `else`, timezone, và việc điều kiện `date_booking >= today` còn nguyên đều chỉ dựa vào lời Dev | Yêu cầu diff nội dung (hoặc link PR) để verify 3 điểm: nhánh `else`, timezone của `Carbon::now()`, giới hạn trên của `date_booking` |
| I9 | `[MINOR]` | `NEW-25`, `NEW-26`, `NEW-27` | `requirement_keys` **rỗng** (REQ chỉ ghi trong `spec_ids`) → Studio không map được 3 TC này vào requirement nào; coverage tự động trên tool sẽ báo thiếu REQ-007 / REQ-010 dù thực tế có TC | Điền `requirement_keys` cho 3 TC do `cucdtk@mcp` viết |
| I10 | `[NIT]` | `NEW-25`, `NEW-26`, `NEW-27` (3 TC `exec_mode = manual`) | Cả 3 đều **tự động hoá được**: NEW-26 chỉ cần set `user_update_time = NULL` rồi gọi endpoint; NEW-25 đo latency ở local; NEW-27 có bước "LINE user đặt lịch" — runner mô phỏng được callback, và theo quy tắc Studio "⛔ ĐỪNG mặc định manual" thì bước bạn bè thao tác trên LINE không phải lý do manual | Đổi sang `auto`, trừ khi có lý do thuộc 4 nhóm bắt buộc (production / thiết bị thật / mail thật / mắt người) |

---

## 6. TCs thừa / ngoài phạm vi task

**Đã rà `24` TC — không có TC nào ngoài phạm vi task.**

2 TC từng là ứng viên, đã **giữ lại** sau khi chạy gate:

| TC | Vì sao KHÔNG phải TC thừa |
|---|---|
| `NEW-27` (LINE user tạo booking salon) | Luồng ghi không nằm trong diff, nhưng map thẳng vào `D1`/`D2` — Dev tự kê "**chi phí ghi**: mỗi lần thêm/sửa đặt lịch salon phải cập nhật thêm 2 index". Đây là TC **duy nhất** cover chiều ghi của 2 index ⇒ gate chặn flag |
| `NEW-24` (API app quản trị di động) | Dẫn được từ `T2` + `REQ-008` (index trên bảng dùng chung) và là TC **duy nhất** chạm nhánh app mobile ⇒ TC regression hợp lệ, không phải over-coverage |

| # | TC | Vì sao ngoài phạm vi | Bằng chứng | Đề xuất | Severity |
|---|---|---|---|---|---|
| X1 | `NEW-8` — **chỉ nửa sau của expected** | Vế "Giờ kết thúc của đơn qua nửa đêm được hiển thị theo đúng cách màn vẫn hiển thị trước bản fix" test **logic hiển thị `nextDay`** — layer không bị chạm code | `spec_delta.files[]` chỉ có Repository + migration; logic `nextDay = (end_time < start_time)` nằm ở service (`feature-spec.md` §SCR-SLN-02a), không có trong diff | **Giữ TC** (điều kiện lọc theo ngày hẹn là trọng tâm hợp lệ), nhưng tách vế hiển thị `nextDay` sang bộ regression chung của kho | `[NIT]` |

---

## 7. TCs đề xuất bổ sung (16)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa020-datlichsalon-サロン・面談予約.md` — nhóm "Tab 本日/新着の予約" (`TC-SLN-42`…`TC-SLN-48`), nhóm **"Modal filter booking & shift"** (`TC-SLN-82`…`TC-SLN-89`), nhóm **"App mobile"** (`TC-SLN-474`…`TC-SLN-478`, tất cả `SYNC-APP-001` — **không TC nào chạm 2 tab 新着/本日**), nhóm "Booking đã xóa" + `MT-14` · `kho-tcs/fa033-backup-データコピー.md` (`MT-12`, `MT-28` — để loại GAP giả backup) |
| Vùng regression phát hiện từ kho | `TC-SLN-44` (bộ đếm 予約数 / キャンセル数) · `TC-SLN-47` (bộ lọc + 一括操作) · **`TC-SLN-82`…`TC-SLN-89`** (8 TC bộ lọc 予約日 / 予約開始時間 / コース / スタッフ / 予約ステータス / 決済ステータス / xóa filter) · `TC-SLN-45` + `TC-SLN-82` (khoảng ngày mặc định 7 ngày — sinh C3) · `TC-SLN-42` (8 cột + phân trang, đã có `NEW-4`/`NEW-16` cover) |
| Conflict expected vs kho | `NEW-2` vs `TC-SLN-45` + `TC-SLN-82` → **CONF-KHO**, đã đưa §4 C3 + §8 dòng 3. (Mâu thuẫn nặng hơn là `CONF-SPEC` C2 — trường sort `created_at` vs `user_update_time`, phát hiện ở `db-mapping.md:641`.) |
| GAP dùng lại TC kho (không viết mới) | `G4` **dùng lại một phần** — hành vi từng bộ lọc riêng lẻ đã có `TC-SLN-84` (コース/スタッフ), `TC-SLN-85` (予約ステータス), `TC-SLN-86`/`TC-SLN-87` (決済ステータス), `TC-SLN-83` (予約開始時間), `TC-SLN-88` (xóa filter): **không viết lại**, chỉ cần chạy lại sau release. Phần kho **KHÔNG có** và phải viết mới: tổ hợp bộ lọc **với điều kiện ngày vừa bị viết lại của tab** (`TC-LIST001-01`, `TC-LIST001-02`) — kho chỉ test bộ lọc đứng một mình, không test giao với `date_booking >= today` / cửa sổ 7 ngày |
| Căn cứ TC regression | `TC-OUTEXPORT001-01` / `TC-CONC001-01` ← Dev `T2` ("xuất CSV", "kiểm tra trùng giờ") · `TC-DATACOUNT001-01` ← kho `TC-SLN-44` · `TC-BULK001-01` ← kho `TC-SLN-47` · `TC-JOB001-01` ← Dev `T2` + `job-spec.md` (remind job đọc cùng bảng) · `TC-SEC001-01` ← RULE-07 + WHERE bị viết lại · `TC-DATAMIG001-01` ← Studio `dev_impact` dòng "ALTER TABLE thêm index trên bảng lớn chạy lâu" |
| Xác nhận chống trùng | Đã đối chiếu `24` TC ở BƯỚC 0 + kho FA-020 — **không TC đề xuất nào trùng** (kiểm theo 4 yếu tố; `TC-FUNCDATE001-02` khác `NEW-7` ở chiều timezone, `TC-FUNC004-01` khác `NEW-4` ở miền giá trị ngoài enum, `TC-ENV003-01` khác `NEW-17` ở môi trường product, **`TC-SYNCAPP001-01` khác `NEW-24`** — `NEW-24` gọi endpoint của app và chỉ so kết quả trước/sau migration, TC mới **mở app thật** và so **app ↔ web** ở cùng thời điểm; cũng khác kho `TC-SLN-475` vì kho so lưới ngày/tuần/tháng, không chạm 2 tab này) |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-LIST001-01 | UI | LIST-001 | Tab 本日/新着の予約 | Normal | auto | Tất cả | Áp bộ lọc 「絞り込み設定」 trên tab 「新着の予約」 vẫn trả đúng tập đơn | Đăng nhập admin, bot có lịch salon L1. Trên L1 có: 3 đơn status 予約確定 course C1 staff S1 (ngày hẹn ngày mai), 2 đơn status キャンセル course C2 staff S2 (ngày hẹn ngày kia), tất cả vừa cập nhật trong hôm nay | 1. Mở lịch L1 → tab 「新着の予約」, ghi lại tổng số dòng khi chưa lọc.<br>2. Mở 「絞り込み設定」, chọn 「予約ステータス」 = 予約確定, bấm 「絞り込み表示」.<br>3. Ghi lại danh sách + bộ đếm.<br>4. Mở lại bộ lọc, thêm 「表示するコース」 = C1 và 「表示するスタッフ」 = S1, bấm 「絞り込み表示」.<br>5. Bấm 「閉じる」 rồi bỏ hết điều kiện lọc, đối chiếu với bước 1 | Lọc lần 1: status = 予約確定. Lọc lần 2: status = 予約確定 + course C1 + staff S1 | B2: chỉ hiện 3 đơn 予約確定, bộ đếm 「1 - 3 / 3行」, không có đơn キャンセル.<br>B4: vẫn đúng 3 đơn của C1/S1, thứ tự theo thời điểm cập nhật giảm dần không đổi.<br>B5: danh sách và bộ đếm trở lại đúng như bước 1 — không dòng nào bị mất hay nhân đôi | | Lấp `G4` · `Q1` · Bộ lọc dùng CHUNG query builder vừa bị viết lại (`feature-spec.md` §SCR-SLN-02a bảng 「絞り込み設定」 + `api-spec.md:164` params `date_start`/`date_end`/`course_ids`/`staff_ids`/`booking_status`/`payment_status`) · Kho đã có TC cho **từng** bộ lọc đứng riêng (`TC-SLN-84`, `TC-SLN-85`, `TC-SLN-86`) — TC này chỉ thêm phần kho thiếu: bộ lọc **áp trên tab 新着** sau khi điều kiện ngày bị viết lại · Đánh giá spec: Spec ghi rõ · Evidence: screenshot danh sách + bộ đếm trước/sau mỗi lần lọc |
| TC-LIST001-02 | UI | LIST-001 | Tab 本日/新着の予約 | Boundary | auto | Tất cả | Lọc 「予約日」 chồng lên điều kiện ngày của tab — 2 điều kiện trên cùng cột date_booking | Lịch L1 có đơn ngày hẹn: hôm qua, hôm nay, ngày mai, +10 ngày. Tất cả vừa cập nhật trong hôm nay | 1. Mở tab 「新着の予約」, mở 「絞り込み設定」, đặt 「予約日」 = hôm qua → ngày mai, bấm 「絞り込み表示」.<br>2. Ghi lại danh sách.<br>3. Đặt 「予約日」 = hôm qua → hôm qua, lọc lại.<br>4. Chuyển sang tab 「本日の予約」 với cùng khoảng lọc 「予約日」 = ngày mai → ngày mai, lọc lại.<br>5. Ghi lại kết quả từng bước | Khoảng 「予約日」: (hôm qua → ngày mai), (hôm qua → hôm qua), (ngày mai → ngày mai) | B1: chỉ hiện đơn hôm nay + ngày mai — đơn hôm qua bị loại bởi điều kiện của tab (ngày hẹn >= hôm nay), đơn +10 ngày bị loại bởi bộ lọc.<br>B3: danh sách **rỗng** + thông báo rỗng của tab, không lỗi 500.<br>B4: danh sách **rỗng** (tab 本日 cố định ngày hôm nay, không lấy ngày mai), không lỗi.<br>Không bước nào trả đơn nằm ngoài giao của 2 điều kiện | | Lấp `G4` · `Q1` · Đây là chỗ 2 điều kiện ngày cùng cột gặp nhau — nếu bản fix viết lại sai thì 1 trong 2 điều kiện bị bỏ · Đánh giá spec: Đã hỏi leader (spec không ghi giao của bộ lọc và điều kiện tab) · Evidence: screenshot + URL request kèm query param |
| TC-FUNCDATE001-01 | UI | FUNC-DATE-001 | Tab 本日/新着の予約 | Boundary | auto | Tất cả | Đơn có ngày hẹn xa hơn 7 ngày vẫn hiện ở tab 「新着の予約」, và bộ lọc mặc định không âm thầm cắt bớt | Lịch L1 có 3 đơn vừa cập nhật trong hôm nay, ngày hẹn lần lượt: ngày mai, +7 ngày, +30 ngày. Chưa mở 「絞り込み設定」 lần nào trong phiên (để lấy đúng trạng thái mặc định) | 1. Mở lịch L1 → tab 「新着の予約」, **mở DevTools tab Network** trước khi tải.<br>2. Ghi lại URL request `get-list-booking` — có kèm `date_start`/`date_end` hay không.<br>3. Tìm từng đơn trong 3 đơn trên trong danh sách, đối chiếu với bộ đếm.<br>4. Đọc dòng 「表示期間」 nếu màn có hiển thị, ghi nguyên văn.<br>5. Mở 「絞り込み設定」, đọc khoảng 「予約日」 đang điền sẵn | Đơn ngày hẹn: +1, +7, +30 ngày; cả 3 cùng cập nhật hôm nay | Theo SQL Dev cung cấp (điều kiện chỉ có `date_booking >= hôm nay`, không giới hạn trên): **cả 3 đơn đều hiện**, kể cả đơn +30 ngày; bộ đếm bằng 3.<br>B2: request mặc định **không** kèm `date_start`/`date_end` (khớp URL production trong ticket).<br>B4–B5: nếu màn hiện 「表示期間：hôm nay 〜 +7 ngày」 hoặc bộ lọc điền sẵn khoảng 7 ngày **trong khi danh sách vẫn hiện đơn +30 ngày** → ghi nhận là **sai lệch nhãn/bộ lọc**, raise cho Leader (§4 C3), KHÔNG tự cho Không đạt cho bản fix.<br>Nếu đơn +30 ngày **KHÔNG** hiện → bản fix đã làm mất/ thêm điều kiện ngày ⇒ **Không đạt**, raise bug ngay | | Lấp `G8` · Liên quan §4 C3 · dẫn từ kho `TC-SLN-45` + `TC-SLN-82` (khoảng ngày mặc định 7 ngày là của **bộ lọc**, không phải của tab) · Bước 2 thêm vào để phân định dứt điểm 2 khả năng thay vì hỏi suông · Đánh giá spec: Spec không ghi (`db-mapping.md:641` không có điều kiện ngày cho `new_booking`) · Evidence: URL request + screenshot danh sách + dòng 表示期間 + khoảng điền sẵn trong bộ lọc |
| TC-FUNCDATE001-02 | UI | FUNC-DATE-001 | Tab 本日/新着の予約 | Boundary | auto | Tất cả | Mốc cửa sổ 7 ngày tính theo timezone của ứng dụng, không lệch ngày khi chạy sát 00:00 / 23:59 | Lịch L1 có 2 đơn ngày hẹn ngày mai; đặt `user_update_time` bằng cách ghi thẳng DB (màn không có ô nhập): đơn TZ-1 = (N−7) 00:00:00, đơn TZ-2 = (N−8) 23:59:59 theo **giờ ứng dụng**. Ghi lại timezone của ứng dụng và của DB trước khi test | 1. Ghi lại timezone ứng dụng (`config app.timezone`) và timezone DB, xác nhận có lệch nhau hay không.<br>2. Mở tab 「新着の予約」 lúc trong ngày, ghi nhận TZ-1 / TZ-2 có hiện hay không.<br>3. Lặp lại lần mở màn trong khoảng 23:50–00:10 (hoặc đổi giờ hệ thống về mốc đó).<br>4. Đối chiếu 2 lần mở | TZ-1 = (N−7) 00:00:00 · TZ-2 = (N−8) 23:59:59 · thời điểm mở màn: giữa ngày và sát 00:00 | TZ-1 hiện, TZ-2 không hiện ở **cả hai** thời điểm mở màn. Nếu timezone ứng dụng lệch DB, kết quả vẫn phải theo timezone ứng dụng — không có đơn nào đổi trạng thái hiện/ẩn chỉ vì đổi thời điểm mở màn trong cùng một ngày | | Lấp `G6` · `Q9` · dẫn từ Studio `dev_impact`: "Nhạy biên GIÂY (endOfDay=23:59:59) và **timezone của Carbon::now()**" · Đánh giá spec: Spec không ghi · Evidence: log timezone + screenshot 2 lần mở màn |
| TC-FUNC003-01 | API | FUNC-003 | EP-A55 — get-list-booking | Abnormal | auto | Tất cả | Tham số date_booking ở các định dạng khác và không hợp lệ — chốt mã trạng thái + nội dung trả về | Đăng nhập admin, phiên hợp lệ. Lịch L1 có 2 đơn ngày hẹn là hôm nay | 1. Gọi endpoint tab 「本日の予約」 với `date_booking` = định dạng chuẩn `YYYY-MM-DD` của hôm nay, lưu kết quả làm mốc.<br>2. Gọi lại lần lượt với `2026/09/22`, `20260922`, `2026-9-22`.<br>3. Gọi lại với `abc`, `2026-13-45`, `0000-00-00`, rỗng, và bỏ hẳn tham số.<br>4. Ghi mã trạng thái + số bản ghi từng lần.<br>5. Kiểm log lỗi ứng dụng | Định dạng hợp lệ khác: `2026/09/22`, `20260922`, `2026-9-22`. Không hợp lệ: `abc`, `2026-13-45`, `0000-00-00`, rỗng, thiếu | B2: cả 3 định dạng hợp lệ khác trả **đúng 2 đơn của hôm nay**, giống mốc ở bước 1.<br>B3: mọi giá trị không hợp lệ trả **HTTP 200 + danh sách rỗng**, `total = 0` (theo REQ-005), **không** HTTP 500, **không** trả đơn của ngày khác.<br>B5: không có exception chưa xử lý trong log | | Lấp `G5` · `Q11` · Chốt phần expected mà `NEW-11`/`NEW-12` đã hạ xuống mơ hồ — xem §4 C3, cần Dev xác nhận trước khi chạy · Đánh giá spec: Đã hỏi leader · Evidence: bảng mã trạng thái + body từng lần gọi + trích log |
| TC-FUNC004-01 | API | FUNC-004 | EP-A55 — get-list-booking | Abnormal | auto | Tất cả | Tham số per_page / page ngoài miền cho phép | Lịch L1 có hơn 100 đơn thỏa điều kiện tab đang kiểm | 1. Gọi endpoint với `per_page = 9999`.<br>2. Gọi với `per_page = 0` và `per_page = -1`.<br>3. Gọi với `page = 0` và `page = -1`.<br>4. Gọi với `per_page = abc`.<br>5. Ghi mã trạng thái, số bản ghi, thông tin phân trang từng lần | `per_page` = 9999 / 0 / −1 / `abc` · `page` = 0 / −1 | Không lần gọi nào trả HTTP 500. Với mỗi giá trị ngoài miền, endpoint hoặc rơi về mặc định (50) hoặc trả danh sách rỗng, nhưng **tổng số bản ghi trả về luôn khớp** số đơn thỏa điều kiện và **không vượt quá** số đơn thật; không lần nào trả đơn của lịch salon khác | | Lấp `Q10` · RULE-01 — bổ sung chiều Abnormal cho `FUNC-004` (hiện chỉ có Boundary ở `NEW-4`) · Ô 表示件数 trên UI chỉ có 50/100 nên chỉ gọi được qua endpoint · Đánh giá spec: Spec không ghi · Evidence: bảng mã trạng thái + metadata phân trang |
| TC-OUTEXPORT001-01 | UI | OUT-EXPORT-001 | Xuất CSV danh sách booking | Normal | auto | Tất cả | Xuất CSV danh sách đặt lịch sau khi thêm index — nội dung và số dòng không đổi | Migration đã chạy. Lịch L1 có tập đơn đa dạng: đơn hôm nay, đơn tương lai, đơn quá khứ, đơn đã xoá, đơn chưa từng được khách cập nhật. Đã xuất CSV **trước** khi chạy migration và lưu file làm mốc | 1. Mở lịch L1, thực hiện xuất CSV danh sách đặt lịch với cùng điều kiện đã dùng ở lần xuất mốc.<br>2. Mở file CSV vừa tải.<br>3. Đối chiếu với file mốc: số dòng, thứ tự dòng, tập cột, giá trị từng ô.<br>4. Kiểm tra dòng tiêu đề và mã hoá ký tự (tên khách tiếng Nhật) | Cùng lịch L1, cùng điều kiện xuất ở cả 2 lần | File CSV sau migration **giống hệt** file mốc: cùng số dòng, cùng thứ tự, cùng tập cột, cùng giá trị. Đơn đã xoá không xuất hiện. Tên khách tiếng Nhật không lỗi mã hoá. Không phát sinh lỗi khi tải file | | Lấp `G2` · `Q15` · regression · dẫn từ `03-dev-impact.md` mục 4.3 `T2` ("xuất CSV" — truy vấn khác trên cùng bảng lọc theo lịch salon) · Đánh giá spec: Spec không ghi · Evidence: 2 file CSV trước/sau + kết quả diff |
| TC-CONC001-01 | UI | CONC-001 | LINE user — chọn slot | Abnormal | auto | Tất cả | Hai khách đặt cùng một slot sau khi thêm index — vẫn chỉ 1 người đặt được | Migration đã chạy. Lịch L1 còn đúng **1 slot trống** vào ngày mai, 受付上限 = 1. Chuẩn bị 2 LINE user U1, U2 đều mở được link đặt lịch | 1. U1 và U2 cùng mở màn chọn slot, cùng chọn slot trống đó.<br>2. Cho U1 và U2 bấm xác nhận gần như đồng thời (trong cùng 1–2 giây).<br>3. Ghi nhận màn hình của từng user.<br>4. Mở màn quản lý lịch salon bằng admin, đếm số đơn trên slot đó.<br>5. Kiểm tra cả tab 「新着の予約」 và màn lịch theo ngày | 2 user đặt đồng thời vào 1 slot, 受付上限 = 1 | Đúng **1** user đặt thành công; user còn lại nhận thông báo slot đã được đặt / không đặt được (theo modal lý do không đặt được của màn). Trên màn admin chỉ có **1 đơn** ở slot đó — không có đơn trùng. Tab 「新着の予約」 hiện đúng 1 đơn mới đó | | Lấp `G3` · `Q14` · regression · dẫn từ `03-dev-impact.md` mục 4.3 `T2` ("kiểm tra trùng giờ" — truy vấn khác trên cùng bảng) · Đánh giá spec: Spec ghi rõ (受付上限) · Evidence: screenshot 2 màn user + đếm đơn trên màn admin |
| TC-DATAMIG001-01 | Data | DATA-MIG-001 | DB — Index bảng đặt lịch salon | Normal | manual | Tất cả | Migration đã được chạy — 2 index tồn tại đúng tên, đúng thứ tự cột trên dev, staging và production | Bản fix đã deploy lên từng môi trường cần kiểm. Có quyền chạy `SHOW INDEX` trên DB của dev / staging, và có quyền (hoặc nhờ DBA) xác nhận trên production | 1. Trên **dev**: liệt kê index của bảng `calendar_salon_line_booking`.<br>2. Lặp lại trên **staging**.<br>3. Lặp lại trên **production** (tự chạy hoặc nhờ DBA xác nhận).<br>4. Với mỗi môi trường, đối chiếu tên index và **thứ tự cột** với định nghĩa trong migration.<br>5. Ghi lại môi trường nào chưa có index | Không nhập liệu; chỉ đọc danh sách index trên 3 môi trường | Trên cả 3 môi trường, bảng có đúng 2 index: `idx_cslb_salon_user_update` gồm 2 cột theo thứ tự (`calendar_salon_id`, `user_update_time`) và `idx_cslb_salon_date_start` gồm 3 cột theo thứ tự (`calendar_salon_id`, `date_booking`, `start_time`). `PRIMARY` còn nguyên, không index nào khác bị xóa hay đổi. Môi trường nào **chưa có** index thì kết luận là **CHƯA DEPLOY** — không phải lỗi sản phẩm | | Lấp `G7` · `manual vì phải kiểm trên DB production` · Thứ tự cột là điểm phải soi, không chỉ tập cột — `calendar_salon_id` phải đứng đầu thì truy vấn lọc theo lịch mới dùng được index · Đánh giá spec: Spec ghi rõ (migration `2026_09_21_100000`) · Evidence: output `SHOW INDEX` của cả 3 môi trường |
| TC-DATACOUNT001-01 | UI | DATA-COUNT-001 | Tab 本日/新着の予約 | Normal | auto | Tất cả | Bộ đếm 「予約数」 / 「キャンセル数」 vẫn đếm đúng nhóm trạng thái sau khi viết lại điều kiện lọc | Lịch L1, trong phạm vi hiển thị của tab có: 2 đơn 予約確定, 1 đơn 予約リクエスト中, 1 đơn キャンセルリクエスト中, 1 đơn キャンセル, 1 đơn 否認. Ngoài phạm vi tab có thêm 1 đơn 予約確定 ngày hẹn đã qua | 1. Mở lịch L1 → tab cần kiểm.<br>2. Đọc 「予約数」 và 「キャンセル数」.<br>3. Đếm tay số dòng theo từng trạng thái trong danh sách.<br>4. Đối chiếu 2 con số với số dòng thật và với bộ đếm 「x - y / n行」.<br>5. Lặp lại cho tab còn lại | 6 đơn trong phạm vi tab trải đủ các trạng thái + 1 đơn nhiễu ngoài phạm vi | 「予約数」 và 「キャンセル数」 đếm đúng theo nhóm trạng thái và **chỉ đếm các đơn thuộc phạm vi của tab** — đơn ngày hẹn đã qua không được cộng vào. Tổng 2 bộ đếm cộng số đơn 否認 khớp với bộ đếm tổng số dòng. Kết quả giống nhau ở cả 2 tab về nguyên tắc đếm | | Lấp `Q4` · regression · dẫn từ kho `TC-SLN-44` "Bộ đếm 予約数 và キャンセル数 đếm đúng nhóm status" · 2 con số này dùng chung điều kiện WHERE vừa bị viết lại · Đánh giá spec: Spec không ghi · Evidence: screenshot bộ đếm + bảng đếm tay |
| TC-BULK001-01 | UI | BULK-001 | Tab 本日/新着の予約 | Normal | auto | Tất cả | Lọc rồi 「リクエスト一括操作」 chỉ tác động đúng các đơn đã tick | Lịch L1 có 5 đơn 予約リクエスト中 (3 của course C1, 2 của course C2) và 2 đơn 予約確定. Tất cả vừa cập nhật trong hôm nay | 1. Mở tab 「新着の予約」, áp bộ lọc 「表示するコース」 = C1.<br>2. Tick chọn toàn bộ đơn đang hiện, bấm 「アクションを選択する」 → chọn 新規予約リクエストを承認する → xác nhận.<br>3. Bỏ bộ lọc, mở lại danh sách đầy đủ.<br>4. Kiểm trạng thái của cả 7 đơn.<br>5. Kiểm màn lịch theo ngày và detail 1 đơn để xác nhận lịch sử thao tác | Lọc course C1, chọn tất cả đơn đang hiện, duyệt hàng loạt | Chỉ **3 đơn của C1** chuyển sang 予約確定. 2 đơn 予約リクエスト中 của C2 (bị bộ lọc ẩn) **giữ nguyên trạng thái** — không bị duyệt theo. 2 đơn 予約確定 sẵn có không đổi. Detail đơn hiện đúng lịch sử thao tác | | Lấp `Q5` · regression · dẫn từ kho `TC-SLN-47` (nút 一括操作 trên tab này) · Rủi ro: thao tác hàng loạt tác động theo **truy vấn danh sách** thay vì theo danh sách ID đã tick, mà truy vấn đó vừa bị viết lại · Đánh giá spec: Spec ghi rõ · Evidence: screenshot trạng thái 7 đơn trước/sau + lịch sử thao tác |
| TC-JOB001-01 | Data | JOB-001 | Job nền — remind đặt lịch salon | Normal | auto | Tất cả | Job remind đặt lịch salon vẫn chọn đúng tập booking sau khi thêm index | Migration đã chạy trên môi trường đang kiểm. Lịch L1 đã bật 「リマインド」 với 1 mốc gửi. Có 2 đơn 予約確定 đến hạn remind trong cửa sổ gửi kế tiếp, 1 đơn キャンセル cùng thời điểm (không được gửi), 1 đơn 予約確定 chưa tới hạn | 1. Ghi lại danh sách đơn đến hạn remind trước khi job chạy.<br>2. Chờ job remind chạy (hoặc dispatch job theo lịch của môi trường).<br>3. Đọc màn lịch sử gửi + log job, đối chiếu số tin gửi và đơn tương ứng.<br>4. Xác nhận đơn キャンセル và đơn chưa tới hạn không bị gửi.<br>5. Lặp lại trên môi trường còn lại (dev/staging trước, quan sát lại trên production sau release) | 2 đơn đến hạn + 1 đơn キャンセル cùng thời điểm + 1 đơn chưa tới hạn | Đúng **2** tin remind được gửi, ứng đúng 2 đơn đến hạn. Đơn キャンセル và đơn chưa tới hạn **không** được gửi. Không có tin gửi trùng, không đơn nào bị bỏ sót, log job không có lỗi. Kết quả giống nhau giữa các môi trường đã kiểm | | Lấp `Q7` · regression · dẫn từ `03-dev-impact.md` `T2` + `spec-features/admin/salon-booking/job/job-spec.md` (job remind đọc cùng bảng, hưởng index mới) · **Trọng tâm là job chọn đúng tập booking** — nội dung tin hiển thị trên LINE không bị index chạm nên không cần verify trên máy thật (bỏ RULE-06 khỏi TC này) · RULE-08 được thoả bằng `Phạm vi ENV = Tất cả`: chạy được ở dev/staging, và vẫn quan sát lại trên production sau release · Đánh giá spec: Spec ghi rõ · Evidence: lịch sử gửi + log job của từng môi trường |
| TC-SEC001-01 | API | SEC-001 | EP-A55 — get-list-booking | Abnormal | auto | Tất cả | Gọi endpoint với mã lịch salon của bot khác — không rò rỉ đơn đặt lịch | 2 bot A và B do 2 tài khoản admin khác nhau quản lý. Bot A có lịch salon LA với 3 đơn ngày hẹn hôm nay; bot B có lịch salon LB với 2 đơn ngày hẹn hôm nay. Đặt tên khách **giống nhau** ở cả 2 bot để dễ phát hiện lẫn dữ liệu | 1. Đăng nhập admin của bot A, gọi endpoint danh sách đặt lịch với mã lịch LA, lưu kết quả.<br>2. Giữ nguyên phiên bot A, gọi endpoint với mã lịch **LB** (của bot B).<br>3. Ghi mã trạng thái + nội dung trả về.<br>4. Đăng nhập tài khoản của bot B ở phiên đăng nhập riêng, gọi endpoint với mã lịch LA.<br>5. Đối chiếu kết quả bước 2 và bước 4 | Mã lịch salon chéo bot: LA ↔ LB, mỗi bot 1 phiên đăng nhập riêng | B1: trả đúng 3 đơn của LA.<br>B2 và B4: **không** trả bất kỳ đơn nào của lịch thuộc bot khác — bị chặn hoặc trả danh sách rỗng, không HTTP 500, không lộ tên khách của bot kia.<br>Không lần gọi nào trả đơn có tên khách trùng của bot đối diện | | Lấp `Q12` · RULE-07 ("tạo bản ghi trùng tên ở 2 tài khoản để kiểm chứng WHERE") · Điều kiện WHERE vừa bị viết lại nên phải kiểm lại phạm vi lọc theo lịch/bot · Bot context theo phiên nên bắt buộc dùng 2 phiên đăng nhập riêng · Đánh giá spec: Spec không ghi · Evidence: mã trạng thái + body từng lần gọi |
| TC-CONC003-01 | UI | CONC-003 | Tab 本日/新着の予約 | Abnormal | auto | Tất cả | Bấm đổi tab và đổi 表示件数 liên tiếp — danh sách hiện đúng lựa chọn cuối cùng | Lịch L1 có hơn 100 đơn ngày hẹn hôm nay và tập đơn 「新着」 **khác** tập đơn 「本日」 ít nhất 1 đơn. Mạng đặt ở chế độ chậm (throttle) để 2 request chồng nhau | 1. Mở lịch L1, bấm 「本日の予約」 rồi bấm ngay 「新着の予約」 trong vòng dưới 1 giây.<br>2. Chờ cả 2 response về, ghi lại danh sách đang hiện và tab đang active.<br>3. Đổi 表示件数 từ 50 sang 100 rồi bấm ngay về 50.<br>4. Chờ response, ghi lại số dòng và bộ đếm.<br>5. Lặp lại bước 1 và 3 thêm 2 lần | Không nhập liệu; chỉ thao tác liên tiếp trong khi mạng chậm | Danh sách hiển thị **luôn khớp với tab và 表示件数 đang được chọn**, không phải kết quả của request gửi trước. Không có dòng của tab kia lọt sang, không dòng nào bị nhân đôi, bộ đếm khớp số dòng thật. Lặp lại 3 lần đều cho cùng kết quả | | Lấp `Q13` · Mở rộng chiều mà `NEW-3` nêu trong note nhưng không đo (lỗi đua / nhầm response giữa 2 lần gọi liên tiếp) · Đánh giá spec: Spec không ghi · Evidence: video thao tác + log request/response theo thứ tự |
| TC-SYNCAPP001-01 | API | SYNC-APP-001 | App mobile | Normal | manual | Tất cả | 2 tab 「新着の予約」 / 「本日の予約」 trên app quản trị di động hiển thị cùng tập đơn với Web | Đăng nhập **cùng một tài khoản admin** của bot A trên CẢ Web và app quản trị di động. Lịch L1 có các đơn đặt ở mốc dễ lệch: 1 đơn cập nhật đúng (N−7) 00:00:00, 1 đơn (N−7) 23:59:59, 1 đơn (N−8) 23:59:59, 1 đơn ngày hẹn hôm nay, 1 đơn ngày hẹn hôm qua, 1 đơn ngày hẹn +30 ngày. Không thao tác gì trong lúc so sánh để 2 bên cùng một trạng thái dữ liệu | 1. Trên app, mở lịch L1 → tab 「新着の予約」, ghi lại danh sách mã đơn theo đúng thứ tự.<br>2. Ngay sau đó mở Web cùng lịch, cùng tab, ghi lại danh sách mã đơn.<br>3. Đối chiếu 2 danh sách: tập mã đơn và thứ tự.<br>4. Lặp lại bước 1–3 cho tab 「本日の予約」.<br>5. Soi riêng 3 đơn ở mốc biên (N−7) 00:00:00 / (N−7) 23:59:59 / (N−8) 23:59:59: đơn nào xuất hiện ở app, đơn nào xuất hiện ở Web | 6 đơn ở các mốc biên mô tả ở tiền điều kiện; so sánh app ↔ web cho từng tab | App và Web hiện **cùng tập mã đơn, cùng thứ tự** ở cả 2 tab. Ba đơn ở mốc biên xuất hiện/không xuất hiện **giống hệt nhau** ở 2 nơi. Nếu lệch bất kỳ đơn nào → app đang dùng nhánh truy vấn riêng chưa được sửa (xem note của `NEW-24`) ⇒ ghi lại đơn lệch + tab + mốc thời gian và raise cho Leader, **không** tự kết luận bên nào đúng | | Lấp `Q16` · regression · dẫn từ note của `NEW-24` ("endpoint app mobile tự dựng query riêng cùng mẫu bọc `DATE()`, KHÔNG dùng method đã sửa") + mẫu đối chiếu Web↔App của kho `TC-SLN-475` (kho chưa có cho 2 tab này) · `manual vì phải xem trên app admin mobile — thiết bị thật` · Đánh giá spec: Spec không ghi · Evidence: screenshot 2 tab trên app + 2 tab trên web, chụp cùng thời điểm |
| TC-ENV003-01 | API | ENV-003 | EP-A55 — get-list-booking | Normal | manual | product | Đo thời gian phản hồi endpoint trên production sau release, trên đúng các lịch salon đã ghi nhận chậm | Bản fix + migration **đã deploy lên production**. Có quyền quan sát màn quản lý lịch salon của các bot trong ticket và quyền đọc report request chậm (Chatwork room 417532006). Đã lưu số liệu 24h **trước** release làm mốc: 95 lần chậm, p95 17s, max 85s | 1. Xác nhận 2 index đã tồn tại trên production (`SHOW INDEX` hoặc nhờ DBA xác nhận).<br>2. Mở màn danh sách đặt lịch của các lịch đã ghi nhận chậm (5870, 8057, 11322, 8410, 6186, 6209), mỗi lịch mở cả 2 tab, ghi thời gian tải từng lần.<br>3. Theo dõi report request chậm trong 24h và 7 ngày sau release.<br>4. Đối chiếu số lần chậm / p95 / max với mốc trước release.<br>5. Kiểm nội dung danh sách hiển thị vẫn đúng (số dòng, thứ tự) | Các lịch salon: 5870, 8057, 11322, 8410, 6186, 6209 · per_page 50 · cả 2 tab | 2 index tồn tại trên production. Không lần mở màn nào vượt ngưỡng SLOWLV1 (5s) trên các lịch đã ghi nhận chậm. Sau 24h: số lần chậm của endpoint **giảm rõ rệt** so với mốc 95 lần/24h, không còn lần nào ở mức SUPPERSLOW (≥15s). Nội dung danh sách vẫn đúng như trước release. **Nếu vẫn còn lần chậm ≥15s** → ghi lại URL + lịch + thời điểm và raise ticket, không đóng #41174 | | Lấp `G1` · `Q2` · `Q3` · RULE-08 / ENV-003 — đây là TC **duy nhất** kết luận được ticket đã fix, vì mọi TC hiện có chỉ chạy local · `manual vì Phạm vi ENV = product (dữ liệu khách hàng thật)` · Đánh giá spec: Spec không ghi · Evidence: `SHOW INDEX` production + bảng thời gian tải từng lịch + ảnh report chậm 24h trước/sau |

---

## 8. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | ✅ **ĐÃ CHỐT** — `feature-spec.md:173` (bảng "Cột bảng đặt lịch") · `feature-spec.md:754` (Field Matrix #8) · `db/db-mapping.md:626` | **Leader chốt 2026-09-22: cột 1 「操作が行われた日時」 = `user_update_time`, KHÔNG phải `created_at`.** Sửa cả **3 chỗ** đang ghi `calendar_salon_line_booking.created_at` thành `user_update_time`. Đối chứng ủng hộ: response EP-07 (`api-spec.md:188`) không hề có field `created_at`. Ghi thêm 1 dòng cảnh báo: trường này **cho phép NULL** (`db-mapping.md:106`) nên cột 1 rỗng với đơn admin tạo / khách chưa sửa | `C1 CONF-SPEC` ở §4 | `Dev` (chỉ còn sửa tài liệu) |
| 2 | ✅ **ĐÃ CHỐT** — `db/db-mapping.md:641` bảng "Bộ lọc tương ứng" | Hệ quả của dòng 1: sửa `tab_query=new_booking` → `created_at DESC` thành **`user_update_time DESC`** (mô tả "sắp xếp theo thời gian nhận" giữ nguyên ý — đó chính là cột 1). **Code và index `idx_cslb_salon_user_update` là ĐÚNG**, không raise bug. Nhân tiện bổ sung điều kiện lọc còn thiếu của `new_booking` vào chính bảng này (xem dòng 5) | `C2 CONF-SPEC` ở §4 | `Dev` (chỉ còn sửa tài liệu) |
| 3 | `kho-tcs/fa020-datlichsalon-サロン・面談予約.md` `TC-SLN-45` + `TC-SLN-82` | Chốt khoảng "hôm nay 〜 +7 ngày" thuộc về đâu: **mặc định của bộ lọc 予約日** (như `TC-SLN-82` mô tả) hay điều kiện nội tại của tab 「新着の予約」. Nếu là mặc định bộ lọc thì làm rõ nó **có được gửi kèm request khi mới vào màn hay không** (URL production trong ticket không có `date_start`/`date_end`), và sửa expected của `TC-SLN-45` cho khỏi gây hiểu nhầm là điều kiện lọc | `C3 CONF-KHO` ở §4 | `Leader` |
| 4 | Studio **REQ-005** + `web/api-spec.md` EP-07 (dòng 164) | Chốt hành vi chuẩn khi `date_booking` rỗng / thiếu / không hợp lệ: HTTP status nào, body ra sao. Hiện REQ-005 (risk High) yêu cầu "HTTP 200 + danh sách rỗng", `api-spec.md` chỉ khai param "không bắt buộc" mà không mô tả nhánh giá trị lỗi, còn `NEW-11`/`NEW-12` đã hạ expected xuống "theo cơ chế validation hiện tại" ⇒ không bên nào còn là chuẩn kiểm chứng được | `C4 CONF-SPEC` ở §4 | `Dev + Leader` |
| 5 | `feature-spec.md` §SCR-SLN-02a + `ui/ui-spec.md` dòng 116 + `db/db-mapping.md:641` | **Spec không mô tả điều kiện lọc của tab 「新着の予約」 ở bất kỳ file nào**: `ui-spec.md:116` chỉ có 1 dòng "Đặt lịch mới trong **7 ngày**" (không nói 7 ngày theo trường nào), `db-mapping.md:641` chỉ ghi sort mà **không ghi điều kiện ngày nào**, `feature-spec.md` không nhắc. Cần bổ sung thành Business rule đầy đủ: 7 ngày tính theo `user_update_time`, mốc đầu/cuối tính tới **giây** (`00:00:00` → `23:59:59`), **timezone** dùng để tính mốc, và điều kiện `date_booking >= hôm nay` **có/không có giới hạn trên**. Đây là gốc của `G6` + `G8` — thiếu rule này thì không ai chấm được Đạt/Không đạt | `G6` + `G8` ở §1 (BƯỚC 1 — spec không đủ để đánh giá Expected) | `Dev` |
