<!-- sync-tcs: url=<chưa có — Leader điền URL Sheet TC human/master nếu cần push> | sheet=<tên tab> | anchor=Main Function -->
<!-- source: MCP LME TEST STUDIO — task_id=319, ticket 41174, testcase_list (24 TC), fetch lúc 2026-09-22. Redmine KHÔNG có Link TCs human. -->

# 04 — TC List (fetch từ MCP LME TEST STUDIO)

## ⚠️ Leader đọc trước khi review

| Mục | Giá trị |
|---|---|
| Nguồn | **MCP LME TEST STUDIO** — task `#319`, ticket `41174`, `testcase_list` 24 TC, fetch `2026-09-22` |
| Redmine Link TCs | **KHÔNG có** → file 04 lấy từ Studio (BƯỚC 6b của `/new-task`) |
| Task Studio | `status = tc-ready` · `pipelineStage = tc-ready` · `round = 1` · `branch = ai_small_41174` · `feature = calendar-salon` |
| Người tạo task | `tudv` (2026-09-21 09:05) · assignee `tudv` |
| Review state | `leader` · `reviewed = false` · `openBugs = 0` |
| **Tác giả TCs** | **21/24 TC do AI sinh** (`provenance.source = ai`, `created_job_id = 1084`) · **3/24 TC do người viết qua MCP** (`cucdtk@mcp`: NEW-25, NEW-26, NEW-27) · **0 TC human viết trực tiếp trên tool** |
| **Kết quả chạy** | **0 pass · 0 fail · 0 error · 24 CHƯA CHẠY** (`exec.untested = 24`, `ranAt = null`, mọi TC `last_exec = null`) |
| Môi trường đã chạy | `envAuto` ghi `staging: runs=1` nhưng **automated = 0 và cả 24 TC vẫn untested** → chưa có kết quả nào để triage. `local` / `dev` / `prd`: `runs = 0` |
| Trạng thái TC | Cả 24 TC `status = draft` |

### ⚠️ Cảnh báo bắt buộc nêu

1. **TCs do AI sinh, không phải member người viết** — 21/24 từ job AI `#1084`, 3/24 do `cucdtk` ghi qua MCP. Không có TC nào `provenance.source = human` viết trực tiếp trên tool.
2. **Chưa TC nào được chạy** → không có TC fail/error để triage, nhưng cũng **không có bằng chứng nào cho việc fix đã đúng**. Không thể kết luận gì về chất lượng bản fix từ bộ TC này.
3. **RULE-08 — 9/24 TC chỉ khai `env_scope = local`** (NEW-7, NEW-9, NEW-11 *(all)*, NEW-17, NEW-18, NEW-19, NEW-20, NEW-21, NEW-25, NEW-26): gồm toàn bộ TC đo performance + TC migration/EXPLAIN. Ticket là **performance trên production** (`step.lme.jp`, `s.lmes.jp`, đỉnh 85s) ⇒ **kết quả local KHÔNG kết luận được endpoint đã hết chậm ở production**. Chính TC NEW-17 / NEW-21 / NEW-25 đã tự ghi giới hạn này trong `note`.
4. **5 mã quan điểm Studio KHÔNG có trong [framework/checklist-lme.md](../../framework/checklist-lme.md)** (8/24 TC) → `/review-tc` không map được coverage cho các mã này:

   | Mã quan điểm Studio | Số TC | TC |
   |---|---|---|
   | `API-CONTRACT-001` | 2 | NEW-9, NEW-16 |
   | `PERF-LATENCY-001` | 3 | NEW-17, NEW-21, NEW-25 |
   | `API-001` | 1 | NEW-10 |
   | `TOOL-ERRHYG-001` | 1 | NEW-11 |
   | `RULE-TOOL-029` | 1 | NEW-20 |

   Mã **có** trong framework: `FUNC-001` (3 TC) · `FUNC-003` (1) · `FUNC-004` (1) · `FUNC-DATE-001` (4) · `FUNC-SEQ-001` (1) · `UI-003` (2) · `DATA-MIG-001` (2) · `REG-SHARED-001` (2).
5. **PHÁT HIỆN của AI cần Leader biết (note của NEW-24)**: endpoint danh sách booking cho **app quản trị di động** (`app/Http/Controllers/Api/CalendarSalonController.php:539-558`) **tự dựng query riêng với cùng mẫu bọc hàm `DATE()` trên cột thời điểm cập nhật và KHÔNG dùng method đã sửa** ⇒ nhánh app mobile **vẫn giữ nguyên mẫu chậm**, chỉ hưởng lợi gián tiếp nhờ index mới. AI đề nghị Leader cân nhắc **mở ticket riêng**. Điểm này **không có trong mục 4 của `03-dev-impact.md`**.

### Phân bố bộ TC

| Chiều | Phân bố |
|---|---|
| `case_type` | Normal 14 · Abnormal 5 · Boundary 5 |
| `tc_group` | `ui` 10 · `api` 10 · `data` 4 |
| `exec_mode` | `auto` 21 · `manual` 3 (đúng 3 TC do `cucdtk@mcp` viết: NEW-25, NEW-26, NEW-27) |
| `env_scope` | `all` 15 · `local` 9 |
| `screen` | SCR-03 Tab1 danh sách đặt lịch salon 8 · EP-A55 endpoint get-list-booking 8 · DB index/migration 4 · Regression các màn khác đọc cùng bảng 2 · Salon Booking LINE user tạo booking 1 · (NEW-27) 1 |
| `requirement_keys` | REQ-001 → REQ-010 (10 requirement khai trên Studio). ⚠️ 3 TC của `cucdtk@mcp` (NEW-25, NEW-26, NEW-27) có `requirement_keys` **rỗng** — chỉ ghi REQ trong `spec_ids` |

## Thông tin

| Trường | Giá trị |
|---|---|
| Tester viết TCs | AI job `#1084` (21 TC) + `cucdtk@mcp` (3 TC) — **không phải member người viết** |
| Ngày submit | 2026-09-21 (AI) · 2026-09-22 (3 TC của `cucdtk@mcp`) |
| Version TCs | Studio round 1 — 21 TC `version 1`, 3 TC `version 2` (NEW-9, NEW-11, NEW-12 đã được sửa lại ngày 2026-09-22) |
| Link TC gốc | MCP LME TEST STUDIO task `#319` (ticket 41174) |

---

## TC List

> **16 cột canonical.** ⚠️ Cột `TC No.` giữ **ID hiển thị trên Studio** (`temp_id` dạng `NEW-x`) theo quy ước repo cho TC fetch từ Studio — mã theo quan điểm đẩy xuống `Ghi chú`. Mọi tham chiếu TC trong `05-review-report.md` §1 → §6 dùng ID `NEW-x` này.

| TC No. | Mã quan điểm liên kết | Loại case | Tiêu đề test case | Điều kiện tiền đề | Các bước thực hiện | Dữ liệu test/input | Kết quả mong đợi | Kết quả thực thi | Evidence thực tế | Môi trường test | Người thực hiện | Ngày thực hiện | Số ticket bug | Trạng thái đánh giá spec | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| NEW-1 | FUNC-001 | Normal | Tab 「本日の予約」 chỉ hiện đơn của hôm nay, xếp theo giờ bắt đầu tăng dần | Đăng nhập admin và chọn bot có lịch salon L1. Trên lịch L1 chuẩn bị sẵn các đơn đặt lịch có tên khách mang tiền tố riêng của lượt test (ví dụ TC41174A-...) để không lẫn với dữ liệu các lượt chạy trước: 3 đơn ngày hôm nay với giờ bắt đầu 18:00, 09:00, 13:30 (tạo theo thứ tự đảo để chắc chắn thứ tự hiển thị không phải thứ tự tạo); 1 đơn ngày mai; 1 đơn ngày hôm qua; 1 đơn ngày hôm nay đã bị xóa. Ngoài ra trên lịch L2 (lịch salon khác) có 1 đơn ngày hôm nay. | 1. Mở màn quản lý lịch salon và vào lịch L1.<br>2. Ở tab đầu tiên của màn, bấm sang tab 「本日の予約」.<br>3. Quan sát danh sách hiển thị, thứ tự các dòng và bộ đếm số dòng ở góc phải.<br>4. Đối chiếu cột ngày nhận đơn và cột giờ bắt đầu của từng dòng. | Đơn hôm nay: 09:00, 13:30, 18:00 (tiền tố TC41174A). Đơn nhiễu: 1 đơn ngày mai, 1 đơn hôm qua, 1 đơn hôm nay đã xóa, 1 đơn hôm nay thuộc lịch L2. | Danh sách hiện đúng 3 đơn của ngày hôm nay thuộc lịch L1, theo thứ tự từ trên xuống 09:00 → 13:30 → 18:00. Không có đơn của ngày mai, ngày hôm qua, đơn đã xóa, cũng không có đơn của lịch L2. Bộ đếm hiển thị 「1 - 3 / 3行」. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #19507 (NEW-1) · `tc_group=ui` / `exec_mode=auto` / `env_tag=env-safe` · REQ-001 · spec: SCR-03, EP-A55, TICKET-41174, source CalendarSalonLineBookingRepository.php:40-44 · note: bản fix đổi `DATE(date_booking)=?` thành `date_booking=?` (cột DATE, NOT NULL) — bằng chứng tương đương cho nhánh `today_booking`. Màn KHÔNG có ô chọn ngày, tự gửi ngày hiện tại. Chạy sát 00:00 phải làm lại |
| NEW-2 | FUNC-001 | Normal | Tab 「新着の予約」 hiện đơn vừa cập nhật trong 7 ngày và còn hạn, mới nhất lên đầu | Đăng nhập admin, chọn bot có lịch salon L1. Trên L1 vừa tạo 2 đơn mới (tiền tố TC41174B) có ngày hẹn là ngày mai và ngày kia — tạo cách nhau vài phút để phân biệt được thứ tự thời điểm cập nhật. Ngoài ra L1 có sẵn 1 đơn vừa cập nhật nhưng NGÀY HẸN ĐÃ QUA (hôm qua) và 1 đơn có ngày hẹn tương lai nhưng lần cập nhật cuối cách đây hơn 7 ngày. | 1. Mở màn quản lý lịch salon và vào lịch L1.<br>2. Bấm sang tab 「新着の予約」 (tab này cũng là tab mở mặc định khi vào màn).<br>3. Quan sát danh sách, cột thời điểm cập nhật của từng dòng và thứ tự các dòng.<br>4. Tìm trong danh sách hai đơn nhiễu đã mô tả ở tiền điều kiện. | Đơn TC41174B-1 (tạo trước), TC41174B-2 (tạo sau, mới hơn). Đơn nhiễu: 1 đơn ngày hẹn hôm qua nhưng vừa cập nhật; 1 đơn ngày hẹn tương lai nhưng cập nhật cách đây hơn 7 ngày. | Hai đơn TC41174B có mặt trong danh sách, đơn TC41174B-2 (cập nhật mới hơn) đứng TRÊN đơn TC41174B-1; toàn bộ danh sách xếp theo thời điểm cập nhật giảm dần. Đơn có ngày hẹn là hôm qua KHÔNG xuất hiện dù vừa được cập nhật. Đơn cập nhật cách đây hơn 7 ngày KHÔNG xuất hiện. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #19508 (NEW-2) · `ui` / `auto` / `env-safe` · REQ-002 · spec: SCR-03, EP-A55, source Repository.php:45-50 · note: fix đổi `DATE(user_update_time) BETWEEN d1 AND d2` → `user_update_time >= (hôm nay−7) 00:00:00 AND <= hôm nay 23:59:59`, giữ `date_booking >= hôm nay` + sort giảm dần |
| NEW-3 | FUNC-SEQ-001 | Normal | Chuyển qua lại hai tab rồi tải lại trang, mỗi tab vẫn đúng dữ liệu của mình | Như tiền điều kiện của hai case Normal ở trên: lịch L1 có cả đơn hôm nay (TC41174A) lẫn đơn mới có ngày hẹn tương lai (TC41174B). Hai tập này phải KHÁC nhau ít nhất 1 đơn để phát hiện được việc lẫn dữ liệu. | 1. Mở lịch L1, để nguyên tab mặc định và ghi lại danh sách đang hiện.<br>2. Bấm sang tab còn lại, ghi lại danh sách.<br>3. Bấm quay lại tab ban đầu, đối chiếu với danh sách đã ghi ở bước 1.<br>4. Tải lại trang bằng F5 rồi lặp lại việc chuyển tab một lượt nữa. | Không nhập liệu; chỉ thao tác chuyển tab và tải lại trang. | Mỗi lần quay lại, tab 「本日の予約」 luôn hiện đúng tập đơn của hôm nay và tab 「新着の予約」 luôn hiện đúng tập đơn mới trong 7 ngày; không có dòng của tab này lọt sang tab kia, không có dòng bị nhân đôi. Số dòng và bộ đếm của mỗi tab giữ nguyên sau khi tải lại trang. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #19509 (NEW-3) · `ui` / `auto` / `read-only` · REQ-001, REQ-002 · note: mỗi lần đổi tab màn gọi lại endpoint với `tab_query` tương ứng, reset page=1 / per_page=50. Case chống lỗi đua/nhầm response giữa 2 lần gọi liên tiếp |
| NEW-5 | UI-003 | Abnormal | Tab 「本日の予約」 khi hôm nay không có đơn nào — hiện đúng thông báo rỗng | Tạo một lịch salon mới L3 chưa có đơn đặt lịch nào, hoặc dùng lịch không có đơn nào với ngày hẹn là hôm nay (vẫn có thể có đơn của ngày khác để chứng minh bộ lọc hoạt động). | 1. Mở màn quản lý lịch salon và vào lịch L3.<br>2. Bấm sang tab 「本日の予約」.<br>3. Quan sát vùng danh sách và bộ đếm số dòng. | Lịch không có đơn nào với ngày hẹn là ngày hôm nay. | Màn hiện thông báo 「本日の予約はありません」, không có dòng dữ liệu nào, bộ đếm hiển thị 「0 - 0 / 0行」. Không hiện màn trắng, không hiện vòng xoay tải mãi, không có hộp thoại lỗi. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #19511 (NEW-5) · `ui` / `auto` / `read-only` · REQ-001, REQ-009 · spec source: booking_list.blade.php:469 · note: trạng thái rỗng dễ lộ lỗi nhất nếu điều kiện lọc mới ném lỗi hoặc trả dữ liệu thừa |
| NEW-6 | UI-003 | Abnormal | Tab 「新着の予約」 khi 7 ngày qua không có đơn mới — hiện đúng thông báo rỗng | Lịch salon L3 không có đơn nào vừa được cập nhật trong 7 ngày gần nhất kèm ngày hẹn từ hôm nay trở đi (có thể có đơn cũ để chứng minh bộ lọc hoạt động). | 1. Mở màn quản lý lịch salon và vào lịch L3.<br>2. Bấm sang tab 「新着の予約」.<br>3. Quan sát vùng danh sách và bộ đếm số dòng. | Lịch không có đơn nào thỏa đồng thời hai điều kiện: cập nhật trong 7 ngày gần nhất và ngày hẹn từ hôm nay trở đi. | Màn hiện thông báo 「7日間以内の新着予約はありません」, không có dòng dữ liệu nào, bộ đếm hiển thị 「0 - 0 / 0行」, không có hộp thoại lỗi. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #19512 (NEW-6) · `ui` / `auto` / `read-only` · REQ-002, REQ-009 · spec source: booking_list.blade.php:304 · note: cùng gốc rủi ro với case rỗng tab 本日 nhưng đi qua nhánh điều kiện KHÁC (khoảng thời gian cập nhật) |
| NEW-4 | FUNC-004 | Boundary | Đổi 表示件数 và chuyển trang, không dòng nào bị trùng hay mất | Lịch salon L1 có hơn 100 đơn thỏa điều kiện tab đang kiểm (ví dụ hơn 100 đơn có ngày hẹn là hôm nay để kiểm tab 「本日の予約」), đủ để có ít nhất 3 trang khi hiển thị 50 dòng. | 1. Mở lịch L1, chọn tab cần kiểm.<br>2. Giữ 表示件数 = 50, ghi lại bộ đếm tổng số dòng và danh sách mã đơn ở trang 1.<br>3. Bấm mũi tên sang phải để sang trang 2, rồi trang 3; ghi lại danh sách mã đơn từng trang.<br>4. Đổi 表示件数 sang 100 và quan sát trang hiện tại cùng bộ đếm.<br>5. Gộp danh sách mã đơn của các trang ở mức 50 dòng và đối chiếu với danh sách ở mức 100 dòng. | 表示件数 = 50 rồi 100; trang 1, 2, 3. | Tổng số dòng trong bộ đếm giống nhau ở cả hai mức 50 và 100. Các trang không có mã đơn nào lặp lại giữa các trang và không thiếu đơn nào: tập hợp mã đơn gom từ các trang ở mức 50 trùng khớp với tập hợp ở mức 100. Khi đổi 表示件数, danh sách quay về trang 1. Mũi tên lùi bị vô hiệu ở trang 1 và mũi tên tiến bị vô hiệu ở trang cuối. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #19510 (NEW-4) · `ui` / `auto` / `env-safe` · REQ-009, REQ-002 · spec source: booking_list.blade.php:107-126 · note: phân trang dùng chung điều kiện WHERE với truy vấn đếm tổng ⇒ nếu viết lại điều kiện làm lệch một trong hai thì tổng số và các trang mâu thuẫn. Ô 表示件数 chỉ có 2 lựa chọn 50 và 100 |
| NEW-7 | FUNC-DATE-001 | Boundary | Đơn cập nhật đúng mốc đầu và cuối cửa sổ 7 ngày vẫn hiện trên tab 「新着の予約」 | Trên lịch L1 dựng 4 đơn có ngày hẹn là ngày mai (để luôn thỏa điều kiện ngày hẹn còn hạn), rồi đặt thời điểm cập nhật cuối của từng đơn bằng cách ghi thẳng vào cơ sở dữ liệu vì màn admin không có ô nhập trường này. Gọi ngày hôm nay là N. | 1. Ghi giá trị thời điểm cập nhật cho 4 đơn theo mô tả ở cột dữ liệu nhập.<br>2. Mở màn quản lý lịch salon, vào lịch L1.<br>3. Bấm sang tab 「新着の予約」.<br>4. Tìm từng đơn trong 4 đơn trên và ghi nhận đơn nào có, đơn nào không. | Đơn BIEN-1: thời điểm cập nhật = (N − 7 ngày) 00:00:00. Đơn BIEN-2: (N − 7 ngày) 23:59:59. Đơn BIEN-3: (N − 8 ngày) 23:59:59. Đơn BIEN-4: N 23:59:59. | BIEN-1, BIEN-2 và BIEN-4 xuất hiện trong danh sách; BIEN-3 KHÔNG xuất hiện. Cột thời điểm cập nhật của BIEN-4 hiển thị đúng ngày N kèm giờ 23:59, và BIEN-4 đứng ở vị trí trên cùng trong 4 đơn (mới nhất trước). | Chưa test | | LOCAL (dự kiến) | | | | | Studio #19513 (NEW-7) · `ui` / `auto` / `local-only` · REQ-004, REQ-002 · spec source: Repository.php:45-49 · ⚠️ **biên trọng tâm của bản fix**: điều kiện cũ `DATE(...) BETWEEN (N−7) AND N` bao trọn ngày N−7 và ngày N; mới dùng mốc giây. Lệch 1 giây ở hai đầu làm mất/thêm đơn. Phải ghi thẳng DB vì trường do hệ thống sinh |
| NEW-8 | FUNC-DATE-001 | Boundary | Đơn kéo dài qua nửa đêm vẫn nằm ở tab 「本日の予約」 của ngày bắt đầu | Trên lịch L1 có 1 đơn bắt đầu hôm nay lúc 23:00 và kết thúc 01:00 ngày hôm sau (đơn qua nửa đêm), và 1 đơn bắt đầu ngày mai lúc 01:00. | 1. Mở lịch L1 và bấm sang tab 「本日の予約」.<br>2. Tìm đơn qua nửa đêm trong danh sách.<br>3. Quan sát cách hiển thị giờ kết thúc của đơn đó. | Đơn QUA-DEM: ngày hẹn = hôm nay, giờ 23:00 – 01:00 (kết thúc sang ngày hôm sau). Đơn nhiễu: ngày hẹn = ngày mai, giờ 01:00. | Đơn QUA-DEM hiện ở tab 「本日の予約」 và nằm ở cuối danh sách theo thứ tự giờ bắt đầu; đơn có ngày hẹn là ngày mai KHÔNG xuất hiện. Giờ kết thúc của đơn qua nửa đêm được hiển thị theo đúng cách màn vẫn hiển thị trước bản fix (có dấu hiệu sang ngày hôm sau). | Chưa test | | Tất cả (dự kiến) | | | | | Studio #19514 (NEW-8) · `ui` / `auto` / `env-safe` · REQ-001 · note: bộ lọc tab chỉ dựa trên ngày hẹn bắt đầu ⇒ case chốt việc bỏ `DATE()` không vô tình kéo ngày kết thúc vào điều kiện. Nếu env không cho tạo đơn qua nửa đêm qua UI thì ghi nhận lý do, không đổi sang gọi endpoint |
| NEW-23 | REG-SHARED-001 | Normal | Các màn khác cùng đọc bảng đặt lịch salon vẫn hiển thị đúng sau khi thêm chỉ mục | Migration đã chạy trên cơ sở dữ liệu đang dùng. Lịch salon L1 có đơn ở tuần hiện tại, đơn ở tháng hiện tại và ít nhất 1 đơn đã bị xóa. Ghi lại ảnh chụp màn hình hoặc danh sách kết quả của các màn này TRƯỚC khi chạy migration để đối chiếu. | 1. Mở lịch L1, vào tab 「予約カレンダー」 ở chế độ xem theo tuần và ghi lại các đơn hiển thị theo từng ngày.<br>2. Chuyển sang chế độ xem theo tháng và ghi lại kết quả.<br>3. Mở màn danh sách đơn đã xóa của lịch L1 và ghi lại kết quả.<br>4. Mở màn quản lý đặt lịch có gán nhân viên và kiểm tra danh sách đơn theo ngày.<br>5. Đối chiếu toàn bộ với kết quả đã ghi trước khi chạy migration. | Không nhập liệu; chỉ xem các màn liệt kê ở trên trên cùng lịch L1. | Mọi màn đều hiển thị đúng tập đơn như trước khi chạy migration: không thiếu, không thừa, không đổi thứ tự, không xuất hiện lỗi tải. Đơn đã xóa vẫn chỉ hiện ở màn danh sách đơn đã xóa. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #19548 (NEW-23) · `ui` / `auto` / `read-only` · REQ-008 · spec: SRC-REGRESSION-007, SCR-03, EP-A56 · note: index nằm trên bảng dùng chung ⇒ mọi truy vấn lọc theo `calendar_salon_id` đổi execution plan. Các màn này dùng method truy vấn KHÁC method đã sửa ⇒ thuần kiểm tác động của index |
| NEW-27 | FUNC-001 | Normal | LINE user tạo booking salon bình thường sau khi thêm index vẫn lưu và hiển thị đúng | Có lịch salon đang mở booking và còn slot trống. Chuẩn bị một LINE user có thể truy cập link booking salon. Ghi nhận trước danh sách booking hiện có trên màn quản lý lịch salon để đối chiếu sau khi tạo mới. | 1. Mở link booking salon bằng LINE user.<br>2. Chọn lịch salon, ngày và thời gian còn trống.<br>3. Nhập thông tin booking theo yêu cầu của màn hình.<br>4. Xác nhận tạo booking.<br>5. Mở màn quản lý lịch salon bằng admin và kiểm tra booking vừa tạo. | LINE user booking mới với một slot còn trống. | LINE user tạo booking thành công. Booking mới được lưu và hiển thị đúng trên màn quản lý lịch salon của admin. Không ảnh hưởng tới các booking đã tồn tại trước đó. Nếu booking thỏa điều kiện của danh sách 「本日の予約」 hoặc 「新着の予約」 thì hiển thị đúng theo rule hiện tại. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #19613 (NEW-27) · `client_ref = TC41174-LINE-BOOKING-001` · `priority = Medium` · `ui` / **`exec_mode=manual`** / `env-safe` · **TC do `cucdtk@mcp` viết (human qua MCP)**, `requirement_keys` rỗng · spec: FA-020, calendar_salon_line_booking · note: regression cho phạm vi ảnh hưởng — bảng dùng chung, cần xác nhận luồng tạo booking của LINE user không bị ảnh hưởng |
| NEW-9 | API-CONTRACT-001 | Normal | Đối chiếu kết quả endpoint danh sách đặt lịch giữa bản cũ và bản fix trên cùng dữ liệu | Một cơ sở dữ liệu dùng chung cho cả hai lần đo, có lịch salon L1 với tập dữ liệu đa dạng: đơn của hôm nay, đơn ngày mai, đơn quá khứ, đơn đã xóa, đơn cũ có sẵn từ các lượt chạy trước, đơn chưa từng được khách cập nhật (trường thời điểm cập nhật để trống), và tổng số đơn đủ để có nhiều hơn một trang. Chuẩn bị được mã nguồn ở hai trạng thái: bản trước fix và bản sau fix. Chỉ số của bảng để nguyên như nhau ở cả hai lần đo. | 1. Chạy ứng dụng ở bản TRƯỚC fix, đăng nhập admin và chọn bot chứa lịch L1.<br>2. Lần lượt gọi endpoint danh sách đặt lịch của lịch L1 với các tổ hợp: tab 「本日の予約」 và tab 「新着の予約」, số dòng 50 và 100, trang 1 và trang cuối. Lưu lại toàn bộ nội dung trả về.<br>3. Chuyển mã nguồn sang bản SAU fix, giữ nguyên cơ sở dữ liệu, khởi động lại ứng dụng.<br>4. Gọi lại đúng những tổ hợp tham số ở bước 2 và lưu lại nội dung trả về.<br>5. So sánh từng cặp kết quả: danh sách mã đơn theo đúng thứ tự, tổng số bản ghi, số trang, số dòng mỗi trang, chỉ số dòng đầu và dòng cuối, và tập tên trường của mỗi dòng. | Tổ hợp tham số: tab 「本日の予約」 / 「新着の予約」 × số dòng 50 / 100 × trang đầu / trang cuối, cùng một mã lịch L1. | Với mọi tổ hợp, bản trước fix và bản sau fix trả về GIỐNG HỆT nhau: cùng tập mã đơn, cùng thứ tự, cùng tổng số bản ghi, cùng thông tin phân trang và cùng tập tên trường của mỗi dòng. Bất kỳ khác biệt nào, kể cả chỉ lệch thứ tự hoặc lệch một bản ghi, đều phải được ghi nhận là lỗi của bản fix. | Chưa test | | LOCAL (dự kiến) | | | | | Studio #19515 (NEW-9) · **version 2** (sửa 2026-09-22) · `api` / `auto` / `local-only` · REQ-003 · spec: SRC-REGRESSION-006, source git `ca8fe3c31f..2a894b8bc3` · ⚠️ mã quan điểm KHÔNG có trong framework/checklist-lme.md |
| NEW-10 | API-001 | Normal | Gọi endpoint với tab 「本日の予約」 cho một ngày khác hôm nay trả đúng đơn của ngày đó | Đăng nhập admin và giữ phiên hợp lệ. Lịch salon L1 có 2 đơn với ngày hẹn là ngày kia (giờ bắt đầu 10:00 và 08:00) và các đơn của những ngày khác. | 1. Gọi endpoint danh sách đặt lịch của lịch L1 với tab 「本日の予約」 và tham số ngày đặt bằng ngày kia theo định dạng năm-tháng-ngày.<br>2. Đối chiếu danh sách trả về với dữ liệu đã dựng.<br>3. Kiểm tra thứ tự các bản ghi theo giờ bắt đầu. | tab 「本日の予約」, ngày = ngày kia (định dạng năm-tháng-ngày, ví dụ 2026-09-23), số dòng 50, trang 1. | Chỉ trả về 2 đơn có ngày hẹn đúng bằng ngày truyền vào, theo thứ tự 08:00 rồi 10:00; tổng số bản ghi bằng 2. Không có đơn của ngày khác, không có đơn đã xóa, không có đơn của lịch salon khác. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #19516 (NEW-10) · `api` / `auto` / `read-only` · REQ-003, REQ-001 · source Repository.php:40-44 · note: chỉ gọi được bằng cách gọi endpoint trực tiếp vì UI luôn tự gửi ngày hôm nay, không có ô chọn ngày · ⚠️ mã quan điểm KHÔNG có trong framework |
| NEW-16 | API-CONTRACT-001 | Normal | Thông tin phân trang và tổng số bản ghi nhất quán với dữ liệu trả về | Lịch salon L1 có hơn 100 đơn thỏa điều kiện của tab đang kiểm, đủ để tạo nhiều trang ở cả hai mức số dòng. | 1. Gọi endpoint với số dòng 50, lần lượt từ trang 1 tới trang cuối; ghi lại tổng số bản ghi, số trang, chỉ số dòng đầu/cuối và danh sách mã đơn của từng trang.<br>2. Gọi lại với số dòng 100 theo cách tương tự.<br>3. Gộp danh sách mã đơn của tất cả các trang ở mỗi mức và đối chiếu hai tập hợp.<br>4. Gọi một trang vượt quá số trang cuối cùng. | Số dòng 50 và 100; trang 1 tới trang cuối; thêm một lần gọi với số trang vượt quá giới hạn. | Tổng số bản ghi bằng nhau ở cả hai mức số dòng và bằng đúng số đơn thỏa điều kiện. Số trang khớp với công thức tổng số chia cho số dòng, làm tròn lên. Gộp các trang lại cho đúng một tập đơn, không trùng và không thiếu, ở cả hai mức. Khi gọi trang vượt giới hạn, trả về danh sách rỗng kèm tổng số bản ghi vẫn đúng — giống hành vi bản cũ, không trả lỗi. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #19522 (NEW-16) · `api` / `auto` / `env-safe` · REQ-003, REQ-009 · spec SRC-REGRESSION-013 · note: truy vấn đếm tổng và truy vấn lấy dữ liệu dùng chung điều kiện lọc ⇒ đây là bất biến Dev nêu, phải kiểm chứ không tin lời · ⚠️ mã quan điểm KHÔNG có trong framework |
| NEW-17 | PERF-LATENCY-001 | Normal | So sánh thời gian phản hồi của endpoint trước và sau fix trên cùng bộ dữ liệu | Một cơ sở dữ liệu duy nhất dùng cho cả hai lần đo, trong đó bảng đặt lịch salon đã được nạp một lượng lớn bản ghi (ít nhất vài chục nghìn dòng trải trên nhiều lịch salon) để độ chênh lệch có thể quan sát được. Chuẩn bị được cả bản trước fix (chưa có chỉ mục mới) và bản sau fix (đã chạy migration thêm chỉ mục). | 1. Ở trạng thái TRƯỚC fix (mã cũ, chưa có chỉ mục mới), gọi endpoint cho lịch có nhiều đơn nhất, mỗi tab 5 lần; ghi lại thời gian phản hồi từng lần.<br>2. Chuyển sang trạng thái SAU fix và chạy migration thêm chỉ mục.<br>3. Gọi lại đúng các yêu cầu đó, mỗi tab 5 lần; ghi lại thời gian phản hồi.<br>4. Đối chiếu thời gian trung vị của từng tab giữa hai trạng thái và đối chiếu danh sách trả về của lần gọi đầu tiên ở mỗi trạng thái. | Mỗi tab 5 lần gọi, số dòng 50, trang 1, trên lịch salon có nhiều đơn nhất. | Thời gian phản hồi trung vị sau fix nhỏ hơn trước fix ở cả hai tab, và danh sách trả về của hai trạng thái vẫn giống hệt nhau. Nếu chênh lệch không quan sát được do khối lượng dữ liệu thử nghiệm quá nhỏ, ghi nhận là GIỚI HẠN MÔI TRƯỜNG kèm số đo thực tế, không kết luận bản fix hỏng. | Chưa test | | LOCAL (dự kiến) | | | | | Studio #19523 (NEW-17) · `api` / `auto` / `local-only` · REQ-010 · spec SRC-BUSINESS-001 · ⚠️ note: DB test là bản mô phỏng dữ liệu nhỏ, **KHÔNG tái hiện được mốc 85 giây của production** — kết quả case này KHÔNG phải bằng chứng endpoint đạt ngưỡng ở production, phải xác nhận bằng giám sát sau phát hành · mã quan điểm KHÔNG có trong framework |
| NEW-25 | PERF-LATENCY-001 | Normal | Kiểm tra thời gian phản hồi endpoint danh sách booking sau fix với dữ liệu lớn | Chuẩn bị dữ liệu lớn trong bảng calendar_salon_line_booking với nhiều calendar_salon_id, nhiều booking và nhiều ngày khác nhau. Migration index đã được chạy. | 1. Gọi endpoint GET /basic/calendar-salon/{id}/get-list-booking với tab 「本日の予約」 và 「新着の予約」.<br>2. Thực hiện nhiều lần gọi cùng điều kiện dữ liệu và ghi nhận thời gian phản hồi.<br>3. So sánh thời gian phản hồi với trước khi fix hoặc bộ dữ liệu đối chứng tương đương.<br>4. Kiểm tra response trả về sau fix. | calendar_id có số lượng booking lớn; per_page=50; page=1; kiểm tra cả tab 「本日の予約」 và 「新着の予約」. | Thời gian phản hồi sau fix được cải thiện so với trước fix trên cùng điều kiện dữ liệu. Endpoint không timeout và response trả về đúng cấu trúc, không thay đổi dữ liệu nghiệp vụ. | Chưa test | | LOCAL (dự kiến) | | | | | Studio #19609 (NEW-25) · `client_ref = TC41174-PERF-001` · `api` / **`exec_mode=manual`** / `local-only` · **TC do `cucdtk@mcp` viết**, `requirement_keys` rỗng (REQ-007/REQ-010 ghi ở `spec_ids`) · note: bổ sung vì mục tiêu chính của ticket là performance; không dùng để kết luận đạt SLA production nếu chỉ đo trên local · ⚠️ **có thể trùng nội dung với NEW-17** — Leader rà ở §3 report |
| NEW-11 | TOOL-ERRHYG-001 | Abnormal | Tham số ngày là chuỗi không phải ngày — endpoint vẫn trả danh sách rỗng, không lỗi máy chủ | Đăng nhập admin, phiên hợp lệ. Lịch salon L1 có sẵn một vài đơn của hôm nay để phát hiện trường hợp trả nhầm dữ liệu. | 1. Gọi endpoint danh sách đặt lịch của lịch L1 với tab 「本日の予約」 và tham số ngày là chuỗi vô nghĩa.<br>2. Ghi lại mã trạng thái trả về và nội dung trả về.<br>3. Lặp lại với vài chuỗi vô nghĩa khác nhau.<br>4. Kiểm tra bản ghi lỗi của ứng dụng xem có ngoại lệ chưa được xử lý nào không. | tab 「本日の予約」 với các giá trị ngày lần lượt: 'abc', '!!!', '2026-13-45', '0000-00-00'. | Khi truyền giá trị ngày không hợp lệ, endpoint không được crash hoặc trả dữ liệu sai. Response phải tuân theo cơ chế xử lý validation hiện tại của hệ thống; không phát sinh lỗi 500 ngoài kiểm soát và không trả về booking không thuộc điều kiện lọc. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #19517 (NEW-11) · **version 2** (sửa 2026-09-22) · `api` / `auto` / `read-only` · REQ-005 · spec: kho `CS-162 (#39523)`, source Repository.php:43-46 · note: đã hạ expected vì ticket không xác nhận bắt buộc giữ HTTP 200 + list rỗng cho input invalid · ⚠️ mã quan điểm KHÔNG có trong framework. ⚠️ Expected hiện **không kiểm chứng được** ("tuân theo cơ chế validation hiện tại" — không nêu rõ mã trạng thái mong đợi) |
| NEW-12 | FUNC-003 | Abnormal | Thiếu hoặc để trống tham số ngày ở tab 「本日の予約」 — không lỗi, không trả nhầm đơn | Đăng nhập admin, phiên hợp lệ. Lịch salon L1 có đơn của hôm nay và đơn của các ngày khác. | 1. Gọi endpoint với tab 「本日の予約」 nhưng để trống tham số ngày.<br>2. Ghi lại mã trạng thái và nội dung trả về.<br>3. Gọi lại với tab 「本日の予約」 nhưng BỎ HẲN tham số ngày khỏi yêu cầu.<br>4. So sánh hai kết quả với kết quả của cùng hai lần gọi trên bản trước fix. | Lần 1: tham số ngày = chuỗi rỗng. Lần 2: không gửi tham số ngày. | Khi thiếu hoặc để trống tham số ngày, endpoint xử lý theo hành vi validation hiện tại của hệ thống. Không được crash, không trả lỗi ngoài kiểm soát và không trả về booking sai phạm vi lọc. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #19518 (NEW-12) · **version 2** (sửa 2026-09-22) · `api` / `auto` / `read-only` · REQ-005 · source Repository.php:43-46 · note: tách rõ với NEW-11 — case này chỉ kiểm missing/empty parameter, không kiểm format invalid. ⚠️ Expected cùng kiểu mơ hồ như NEW-11 |
| NEW-14 | FUNC-DATE-001 | Boundary | Tab 「新着の予約」 — biên điều kiện ngày hẹn: hôm nay được giữ, hôm qua bị loại | Lịch salon L1 có 3 đơn vừa được cập nhật trong hôm nay, khác nhau ở ngày hẹn: một đơn có ngày hẹn là hôm nay, một đơn ngày hẹn là hôm qua, một đơn ngày hẹn là ngày mai. Thời điểm cập nhật của cả ba nằm trong cửa sổ 7 ngày. | 1. Gọi endpoint danh sách đặt lịch của lịch L1 với tab 「新着の予約」.<br>2. Tìm từng đơn trong ba đơn trên ở kết quả trả về.<br>3. Kiểm tra thứ tự các bản ghi theo thời điểm cập nhật. | tab 「新着の予約」, số dòng 50, trang 1. Ba đơn có ngày hẹn lần lượt là hôm qua, hôm nay, ngày mai; cả ba đều vừa cập nhật hôm nay. | Đơn có ngày hẹn là hôm nay và đơn có ngày hẹn là ngày mai đều CÓ trong kết quả; đơn có ngày hẹn là hôm qua KHÔNG có trong kết quả. Các bản ghi sắp theo thời điểm cập nhật giảm dần. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #19520 (NEW-14) · `api` / `auto` / `read-only` · REQ-004 · spec SRC-REGRESSION-011, source Repository.php:48 · note: điều kiện `ngày hẹn >= hôm nay` KHÔNG bị fix sửa nhưng nằm chung khối điều kiện vừa viết lại ⇒ phải kiểm lại biên |
| NEW-26 | FUNC-DATE-001 | Boundary | Đơn không có user_update_time không xuất hiện trong tab 新着の予約 | Có booking thuộc calendar salon với ngày hẹn từ hôm nay trở đi nhưng user_update_time không có giá trị. | 1. Gọi endpoint danh sách booking với tab 「新着の予約」.<br>2. Kiểm tra danh sách booking trả về.<br>3. Đối chiếu với booking có user_update_time không có giá trị. | Booking có date_booking >= hôm nay và user_update_time = NULL. | Booking không có user_update_time không xuất hiện trong kết quả tab 「新着の予約」. Các booking hợp lệ khác vẫn hiển thị bình thường. | Chưa test | | LOCAL (dự kiến) | | | | | Studio #19610 (NEW-26) · `client_ref = TC41174-DATA-001` · `api` / **`exec_mode=manual`** / `local-only` · **TC do `cucdtk@mcp` viết**, `requirement_keys` rỗng · note: bổ sung để kiểm boundary của thay đổi điều kiện lọc `user_update_time` (NULL vẫn bị loại ở cả 2 bản — Dev đã nêu trong mục 6 VERIFY) |
| NEW-24 | REG-SHARED-001 | Normal | Danh sách đặt lịch theo tab trên ứng dụng quản trị di động không đổi sau khi thêm chỉ mục | Migration đã chạy. Có tài khoản và phiên hợp lệ để gọi endpoint danh sách đặt lịch theo tab dành cho ứng dụng quản trị di động. Đã ghi lại kết quả của endpoint này TRƯỚC khi chạy migration trên cùng dữ liệu. | 1. Gọi endpoint danh sách đặt lịch theo tab dành cho ứng dụng quản trị di động cho lịch L1, lần lượt với từng giá trị tab.<br>2. Ghi lại số lượng đơn mới, số lượng đơn hôm nay và danh sách mã đơn trả về.<br>3. Đối chiếu với kết quả đã ghi trước khi chạy migration. | Cùng mã bot và mã lịch L1, lần lượt từng giá trị tab; các tham số mốc thời gian giữ nguyên giữa hai lần đo. | Kết quả sau khi thêm chỉ mục giống hệt kết quả trước đó: cùng số lượng, cùng tập mã đơn, cùng thứ tự; không phát sinh lỗi. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #19549 (NEW-24) · `api` / `auto` / `read-only` · REQ-008 · source `app/Http/Controllers/Api/CalendarSalonController.php:539-558` · 🔴 **PHÁT HIỆN CẦN LEADER BIẾT**: endpoint app mobile **tự dựng query riêng cùng mẫu bọc `DATE()` và KHÔNG dùng method đã sửa** ⇒ nhánh app mobile vẫn giữ mẫu chậm, chỉ hưởng lợi gián tiếp nhờ index. Case này chỉ kiểm KHÔNG ĐỔI KẾT QUẢ, không kết luận "đã hết chậm toàn hệ thống". AI đề nghị Leader mở **ticket riêng** |
| NEW-18 | DATA-MIG-001 | Normal | Chạy migration tạo đúng 2 chỉ mục mới, dữ liệu trong bảng không đổi | Cơ sở dữ liệu đang ở trạng thái TRƯỚC khi chạy migration của bản fix, bảng đặt lịch salon đã có sẵn dữ liệu của các lượt chạy trước. Mã nguồn đã ở nhánh chứa bản fix. | 1. Liệt kê toàn bộ chỉ mục hiện có của bảng đặt lịch salon và ghi lại.<br>2. Ghi lại tổng số bản ghi của bảng và mã kiểm tra nội dung bảng.<br>3. Chạy lệnh migrate của ứng dụng và ghi lại toàn bộ kết quả in ra.<br>4. Liệt kê lại chỉ mục của bảng và đối chiếu với danh sách ở bước 1.<br>5. Đếm lại số bản ghi và tính lại mã kiểm tra nội dung bảng. | Không nhập liệu; chỉ chạy migration của bản fix. | Migration chạy xong không báo lỗi. Sau khi chạy, bảng có thêm đúng 2 chỉ mục mang tên idx_cslb_salon_user_update gồm 2 cột theo thứ tự (mã lịch salon, thời điểm cập nhật) và idx_cslb_salon_date_start gồm 3 cột theo thứ tự (mã lịch salon, ngày hẹn, giờ bắt đầu). Khóa chính cũ vẫn còn nguyên, không chỉ mục nào khác bị xóa hay đổi. Tổng số bản ghi và mã kiểm tra nội dung bảng giống hệt trước khi chạy. | Chưa test | | LOCAL (dự kiến) | | | | | Studio #19543 (NEW-18) · `data` / `auto` / `local-only` · REQ-006 · spec SRC-REGRESSION-012, source migration `2026_09_21_100000:16-30` · note: thứ tự cột trong index là quan trọng — cột `calendar_salon_id` phải đứng đầu. Ở dev/staging/production migration do quy trình phát hành chạy ⇒ tester chỉ xác nhận index đã tồn tại; chưa có thì báo **chưa deploy**, không báo lỗi sản phẩm |
| NEW-19 | DATA-MIG-001 | Normal | Rollback migration gỡ sạch 2 chỉ mục rồi chạy lại vẫn tạo được như cũ | Migration của bản fix đã được chạy thành công (2 chỉ mục đang tồn tại), bảng có dữ liệu. | 1. Ghi lại tổng số bản ghi và mã kiểm tra nội dung bảng.<br>2. Chạy lệnh rollback cho đúng migration này.<br>3. Liệt kê chỉ mục của bảng.<br>4. Chạy lại lệnh migrate.<br>5. Liệt kê chỉ mục lần nữa, đếm lại số bản ghi và tính lại mã kiểm tra nội dung. | Không nhập liệu; chạy rollback rồi migrate lại. | Rollback chạy không lỗi và gỡ đúng 2 chỉ mục mới, khóa chính cùng mọi chỉ mục khác giữ nguyên. Chạy migrate lại tạo lại đúng 2 chỉ mục với cùng tên và cùng thứ tự cột. Sau cả chu trình, tổng số bản ghi và mã kiểm tra nội dung bảng không đổi so với ban đầu. | Chưa test | | LOCAL (dự kiến) | | | | | Studio #19544 (NEW-19) · `data` / `auto` / `local-only` · REQ-006 · source migration `2026_09_21_100000:32-40` · note: kiểm cả chiều lùi vì nếu phát hành gặp sự cố thì vận hành phải rollback; hàm rollback viết sai chỉ lộ ra khi chạy thật. Trên bảng lớn thao tác này chạy lâu ⇒ để ở local |
| NEW-21 | PERF-LATENCY-001 | Normal | Kế hoạch thực thi của hai truy vấn danh sách dùng đúng chỉ mục mới | Migration đã chạy xong (2 chỉ mục tồn tại). Bảng đặt lịch salon có đủ dữ liệu để bộ tối ưu có lý do dùng chỉ mục — nên nạp ít nhất vài nghìn bản ghi trải trên nhiều lịch salon; nếu bảng quá ít dữ liệu thì bộ tối ưu có quyền chọn quét toàn bảng. | 1. Lấy đúng hai câu truy vấn mà màn danh sách sinh ra cho tab 「本日の予約」 và tab 「新着の予約」 (bật ghi nhật ký truy vấn của ứng dụng rồi mở màn để lấy).<br>2. Cập nhật thống kê của bảng trước khi xem kế hoạch thực thi.<br>3. Xem kế hoạch thực thi của câu truy vấn tab 「本日の予約」 và ghi lại chỉ mục được chọn cùng phần ghi chú thêm.<br>4. Làm tương tự với câu truy vấn tab 「新着の予約」.<br>5. So sánh với kế hoạch thực thi của chính hai câu đó ở trạng thái chưa có chỉ mục mới. | Hai câu truy vấn thật do màn danh sách sinh ra, chạy trên lịch salon có nhiều đơn nhất. | Câu truy vấn tab 「本日の予約」 chọn chỉ mục idx_cslb_salon_date_start; câu truy vấn tab 「新着の予約」 chọn chỉ mục idx_cslb_salon_user_update và không còn phải sắp xếp thêm. Số dòng ước tính phải quét giảm rõ rệt so với trạng thái chưa có chỉ mục. Nếu bộ tối ưu vẫn chọn quét toàn bảng vì dữ liệu quá nhỏ, thì ít nhất hai chỉ mục mới phải nằm trong danh sách chỉ mục khả dụng, và kết quả phải ghi là GIỚI HẠN MÔI TRƯỜNG kèm số liệu thực tế. | Chưa test | | LOCAL (dự kiến) | | | | | Studio #19546 (NEW-21) · `data` / `auto` / `local-only` · REQ-007 · spec SRC-BUSINESS-002 · ⚠️ note: case chứng minh root cause nhưng dễ kết luận sai nhất. **Dev chưa từng chạy được bước này** (MySQL dev tắt). Không lấy được EXPLAIN ⇒ phải trả **BỎ QUA** kèm lý do môi trường, **tuyệt đối không suy kết luận từ đọc mã nguồn** · mã quan điểm KHÔNG có trong framework |
| NEW-20 | RULE-TOOL-029 | Abnormal | Trường hợp quản trị cơ sở dữ liệu đã tự thêm chỉ mục trùng trước khi phát hành | Cơ sở dữ liệu ở trạng thái chưa chạy migration của bản fix. Có quyền tạo/xóa chỉ mục trực tiếp để dựng tình huống. | 1. Tạo thủ công một chỉ mục trên bảng đặt lịch salon gồm đúng 2 cột (mã lịch salon, thời điểm cập nhật) nhưng đặt TÊN KHÁC với tên trong migration.<br>2. Chạy migration của bản fix và ghi lại kết quả in ra.<br>3. Liệt kê chỉ mục của bảng.<br>4. Xóa các chỉ mục vừa tạo để trả bảng về trạng thái ban đầu, rồi dựng lại tình huống thứ hai: tạo thủ công chỉ mục mang ĐÚNG TÊN trong migration.<br>5. Chạy lại migration và ghi lại kết quả. | Tình huống 1: chỉ mục trùng cột, khác tên. Tình huống 2: chỉ mục trùng tên. | Tình huống 1: migration chạy xong không lỗi, nhưng bảng tồn tại hai chỉ mục trùng cột — tester phải ghi nhận và báo lại để quản trị cơ sở dữ liệu gỡ chỉ mục thừa, vì chỉ mục dư làm chậm ghi và tốn dung lượng. Tình huống 2: migration BÁO LỖI trùng tên và dừng, không làm hỏng dữ liệu; tester ghi nhận đây là tình huống cần xử lý thủ công trước khi phát hành. Ở cả hai tình huống, dữ liệu trong bảng không bị thay đổi. | Chưa test | | LOCAL (dự kiến) | | | | | Studio #19545 (NEW-20) · `data` / `auto` / `local-only` · REQ-006 · spec SRC-REGRESSION-009, redmine journal cảnh báo index trùng · note: chính Dev đã cảnh báo rủi ro này và yêu cầu `SHOW INDEX` trước khi chạy migration. Kết luận của case là mô tả hành vi + khuyến nghị vận hành, không phải phán quyết lỗi sản phẩm · ⚠️ mã quan điểm KHÔNG có trong framework |

### Requirement khai trên Studio (đối chiếu coverage với `03-dev-impact.md`)

| REQ | Tiêu đề | Category | Risk | TC cover |
|---|---|---|---|---|
| REQ-001 | Tab 「本日の予約」 hiển thị đúng tập đơn của ngày hôm nay, sắp theo giờ bắt đầu tăng dần | ui | High | NEW-1, NEW-3, NEW-5, NEW-8, NEW-10 |
| REQ-002 | Tab 「新着の予約」 hiển thị đúng đơn cập nhật trong 7 ngày và ngày hẹn từ hôm nay trở đi, mới nhất trước | ui | High | NEW-2, NEW-3, NEW-4, NEW-6, NEW-7 |
| REQ-003 | Hợp đồng endpoint bất biến giữa nhánh cũ và nhánh fix | api | High | NEW-9, NEW-10, NEW-16 |
| REQ-004 | Biên cửa sổ 7 ngày và biên ngày hẹn tính đúng tới từng giây | api | High | NEW-7, NEW-14 |
| REQ-005 | Tham số ngày bất thường không làm endpoint lỗi máy chủ | validation | High | NEW-11, NEW-12 |
| REQ-006 | Migration 2 index chạy được cả chiều lên/lùi, không đổi dữ liệu | data | Medium | NEW-18, NEW-19, NEW-20 |
| REQ-007 | Hai truy vấn dùng được index mới thay vì full scan | data | Medium | NEW-21 (+ NEW-25 ghi REQ-007 ở `spec_ids`) |
| REQ-008 | Các màn / API khác đọc cùng bảng không đổi hành vi sau khi thêm index | data | Medium | NEW-23, NEW-24 |
| REQ-009 | Phân trang và bộ đếm giữ nguyên khi đổi số dòng hiển thị | ui | Medium | NEW-4, NEW-5, NEW-6, NEW-16 |
| REQ-010 | Thời gian phản hồi được cải thiện sau fix trên cùng bộ dữ liệu | api | Medium | NEW-17 (+ NEW-25 ghi ở `spec_ids`) |

> `test_viewpoint_selection` trên Studio = `null` — task **chưa chốt bộ quan điểm test** nào.

## Member tự check

`<member điền sau khi review>`

<!-- Source: fetched từ MCP LME TEST STUDIO — task_id=319, ticket 41174, testcase_list 24 TC (temp_id NEW-1…NEW-27, thiếu NEW-13/15/22 — đã bị xóa trên Studio), fetch lúc 2026-09-22. Redmine #41174 KHÔNG có Link TCs human. TCs là READ-ONLY: muốn sửa thì sửa trên Studio (testcase_update) rồi fetch lại. KHÔNG sửa title/precondition/steps/expected trong file này. -->
