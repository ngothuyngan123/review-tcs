<!-- sync-tcs: url=<chưa cấu hình> | sheet=<chưa cấu hình> | anchor=Main Function -->
<!-- source: MCP LME TEST STUDIO — task_id=199, ticket 28542, testcase_list (24 TC), fetch lúc 2026-08-26. Redmine KHÔNG có Link TCs human. -->
<!-- READ-ONLY: TCs chép nguyên văn từ Studio. Muốn sửa → sửa trên Studio (testcase_update) rồi fetch lại. KHÔNG sửa tay file này. -->

# 04 — TC List (snapshot từ MCP LME TEST STUDIO task #199)

> ⚠️ **Đây KHÔNG phải TC do member người viết.** 16/24 TC do **AI sinh** (job #583), 8/24 do QA `trangnq` thêm qua MCP. Xem §Cảnh báo bên dưới trước khi dùng.

## Thông tin

| Trường | Giá trị |
|---|---|
| Nguồn | MCP LME TEST STUDIO — `task_list(ticket_id=28542)` → `testcase_list(task_id=199)` |
| Studio task | `#199` · round `1` · status `done-ai` · feature `chat-1on1` · branch `ai_fixbug_28542` |
| Tạo bởi / Assignee | `ngannt` (2026-08-24 09:51) / `trangnq` |
| Review state | `leader` — **chưa reviewed** (`reviewed=false`, `reviewRound=1`) |
| Ngày fetch | `2026-08-26` |
| Tổng TC | `24` |
| Tác giả TC | AI (job #583): **16** · QA `trangnq@mcp`: **8** · human trực tiếp trên Studio: **0** |
| Kết quả thực thi | pass **0** · fail **0** · skip **6** · chưa chạy **18** |
| Run đã có | `run #459` — env `local`, status **fail**, 2026-08-24 12:02:46→12:02:52, by `pipeline` |
| Bug đang mở | `0` |

## ⚠️ Cảnh báo bắt buộc đọc trước khi review

1. **0/24 TC pass.** 6 TC `auto` chạy ở run #459 đều **skip** với cùng lý do hạ tầng, KHÔNG phải kết quả test:
   `[SOURCE_BLOCKED] web: SOURCE_CHECKOUT_ERROR: checkout "ai_fixbug_28542" trong worktree thất bại: error: unable to create file app/Helpers/PostbackActionBuilder.php: File exists`
   18 TC `manual` **chưa ai chạy lần nào** (`manual.results = []`).
2. **Môi trường**: run duy nhất ở `local`. `dev` / `staging` / `prd` đều `runs=0`. Chưa có bất kỳ kết quả nào trên production.
3. **Ai chạy**: 100% do `pipeline` (AI). **0 TC do QA người chạy.**
4. **3 mã quan điểm KHÔNG có trong `framework/checklist-lme.md`** → `/review-tc` không tính là cover:
   `TOOL-KNOW-002` (TC 12535 — chính là TC tái hiện bug gốc) · `API-CONTRACT-001` (12544) · `TOOL-ERRHYG-001` (12545, 13490).
5. **Toàn bộ 24 TC có `spec_status = null`** (cột `Trạng thái đánh giá spec` trống).
6. **2 cặp TC trùng nội dung**: `12541 (NEW-7)` ↔ `13491 (NEW-23)` · `13489 (NEW-21)` ↔ `13492 (NEW-24)`.
7. **Độ phủ spec theo Studio**: `0/93 covered` (27 partial, 66 none) — toàn bộ `EP-01…EP-49` và `BR-01…BR-17` của feature `chat-1on1` đều `none`.

### Phân bố

| Chiều | Phân bố |
|---|---|
| `Loại case` | Normal **13** · Abnormal **10** · Boundary **1** |
| `tc_group` | ui **20** · api **4** |
| `exec_mode` | manual **18** · auto **6** |
| `env_tag` | env-safe **13** · read-only **11** |
| `screen` | Leftbar realtime (đang mở) **8** · Leftbar realtime (get-badge) **7** · Kiểm tra tĩnh source **3** · Reload/cache asset **2** · API `/ajax/get-badge` **4** |
| `requirement_keys` | REQ-001 **3** · REQ-002 **2** · REQ-003 **3** · REQ-004 **2** · REQ-005 **1** · REQ-006 **1** · REQ-007 **1** · REQ-008 **2** · REQ-009 **1** · **rỗng 8** (toàn bộ TC do QA thêm) |

---

## TC List

| TC No. | Mã quan điểm liên kết | Loại case | Tiêu đề test case | Điều kiện tiền đề | Các bước thực hiện | Dữ liệu test/input | Kết quả mong đợi | Kết quả thực thi | Evidence thực tế | Môi trường test | Người thực hiện | Ngày thực hiện | Số ticket bug | Trạng thái đánh giá spec | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-TOOLKNOW002-01 | TOOL-KNOW-002 | Normal | Friend gửi stamp khi đang mở đúng hội thoại → leftbar hiển thị 【スタンプ】 (tái hiện & verify bug) | Đăng nhập admin/staff qua /login-v2, chọn bot có hội thoại 1:1 với 1 friend. Mở màn Chat 1:1 và đang xem đúng hội thoại của friend đó (hội thoại đang active). | 1. Mở màn Chat 1:1, chọn đúng hội thoại của friend đang test (để sk_conversation_id trùng hội thoại đang mở)<br>2. Từ tài khoản LINE của friend, gửi 1 stamp (nhãn dán) cho bot<br>3. Quan sát dòng hội thoại của friend ở cột trái (leftbar) ngay khi tin tới, KHÔNG reload trang | Friend gửi 1 stamp bất kỳ qua LINE app | Dòng leftbar của friend hiển thị đúng nhãn 「【スタンプ】」; KHÔNG hiển thị chuỗi JSON/nội dung thô (vd {"packageId":...,"stickerId":...}) hay mã code. Nội dung cập nhật realtime, không cần reload. | Chưa test | | DEV / STAGING / PRD (dự kiến) | | | | | Studio #12535 (NEW-1) · ui / manual / env-safe · REQ-001 · spec_ids SCR-CHT-01, TICKET-28542, diff:chat-v2.js:290-317 · tác giả **AI** job#583 · ⚠️ mã `TOOL-KNOW-002` KHÔNG có trong checklist-lme → không tính cover · note: TC tái hiện bug ticket + verify fix. Trigger BẮT BUỘC từ LINE thật → socket → browser; runtime chưa dựng được ở local (MySQL refused) nên để manual. Kỹ thuật: switch e.type dùng typeMessageV2.sticker (số 5) thay vì 'sticker'. |
| TC-OUTTRUTH001-01 | OUT-TRUTH-001 | Normal | Friend gửi ảnh khi đang mở đúng hội thoại → leftbar hiển thị 【画像】 | Đăng nhập admin/staff, chọn bot có hội thoại 1:1 với friend và đang mở đúng hội thoại của friend đó trên màn Chat 1:1. | 1. Mở màn Chat 1:1 và chọn đúng hội thoại của friend đang test<br>2. Từ tài khoản LINE của friend, gửi 1 ảnh cho bot<br>3. Quan sát dòng hội thoại của friend ở cột trái ngay khi tin tới, không reload trang | Friend gửi 1 ảnh bất kỳ qua LINE app | Dòng leftbar của friend hiển thị đúng nhãn 「【画像】」 ngay khi tin tới; không hiển thị nội dung thô/code của message và không cần reload trang. | Chưa test | | DEV / STAGING / PRD (dự kiến) | | | | | Studio #12536 (NEW-2) v2 · ui / manual / env-safe · REQ-001 · tác giả **AI** job#583 · note: Tách từ testcase cũ NEW-2 để mỗi message type có oracle riêng. Viewpoint đổi từ FRIEND-001 sang OUT-TRUTH-001 vì đối tượng kiểm chứng là nội dung hiển thị realtime ở leftbar, không phải lifecycle của Friend info. |
| TC-OUTTRUTH001-02 | OUT-TRUTH-001 | Normal | Friend gửi video khi đang mở đúng hội thoại → leftbar hiển thị 【動画】 | Đăng nhập admin/staff, chọn bot có hội thoại 1:1 với friend và đang mở đúng hội thoại của friend đó trên màn Chat 1:1. | 1. Mở màn Chat 1:1 và chọn đúng hội thoại của friend đang test<br>2. Từ tài khoản LINE của friend, gửi 1 video cho bot<br>3. Quan sát dòng hội thoại của friend ở cột trái ngay khi tin tới, không reload trang | Friend gửi 1 video bất kỳ qua LINE app | Dòng leftbar của friend hiển thị đúng nhãn 「【動画】」 ngay khi tin tới; không hiển thị nội dung thô/code của message và không cần reload trang. | Chưa test | | DEV / STAGING / PRD (dự kiến) | | | | | Studio #13485 (NEW-17, client_ref 28542-new2-video) · ui / manual / env-safe · requirement_keys **rỗng** · tác giả **QA trangnq@mcp** · note: Tách từ testcase cũ NEW-2 để kiểm riêng message type video. |
| TC-OUTTRUTH001-03 | OUT-TRUTH-001 | Normal | Friend gửi audio khi đang mở đúng hội thoại → leftbar hiển thị 【音声】 | Đăng nhập admin/staff, chọn bot có hội thoại 1:1 với friend và đang mở đúng hội thoại của friend đó trên màn Chat 1:1. | 1. Mở màn Chat 1:1 và chọn đúng hội thoại của friend đang test<br>2. Từ tài khoản LINE của friend, gửi 1 tin audio/voice cho bot<br>3. Quan sát dòng hội thoại của friend ở cột trái ngay khi tin tới, không reload trang | Friend gửi 1 audio/voice message qua LINE app | Dòng leftbar của friend hiển thị đúng nhãn 「【音声】」 ngay khi tin tới; không hiển thị nội dung thô/code của message và không cần reload trang. | Chưa test | | DEV / STAGING / PRD (dự kiến) | | | | | Studio #13486 (NEW-18, client_ref 28542-new2-audio) · ui / manual / env-safe · requirement_keys **rỗng** · tác giả **QA trangnq@mcp** |
| TC-OUTTRUTH001-04 | OUT-TRUTH-001 | Normal | Friend gửi vị trí khi đang mở đúng hội thoại → leftbar hiển thị 【位置】 | Đăng nhập admin/staff, chọn bot có hội thoại 1:1 với friend và đang mở đúng hội thoại của friend đó trên màn Chat 1:1. | 1. Mở màn Chat 1:1 và chọn đúng hội thoại của friend đang test<br>2. Từ tài khoản LINE của friend, chia sẻ 1 vị trí cho bot<br>3. Quan sát dòng hội thoại của friend ở cột trái ngay khi tin tới, không reload trang | Friend chia sẻ 1 location qua LINE app | Dòng leftbar của friend hiển thị đúng nhãn 「【位置】」 ngay khi tin tới; không hiển thị nội dung thô/code của message và không cần reload trang. | Chưa test | | DEV / STAGING / PRD (dự kiến) | | | | | Studio #13487 (NEW-19, client_ref 28542-new2-location) · ui / manual / env-safe · requirement_keys **rỗng** · tác giả **QA trangnq@mcp** |
| TC-OUTTRUTH001-05 | OUT-TRUTH-001 | Normal | Friend gửi file khi đang mở đúng hội thoại → leftbar hiển thị 【ファイル】 | Đăng nhập admin/staff, chọn bot có hội thoại 1:1 với friend và đang mở đúng hội thoại của friend đó trên màn Chat 1:1. | 1. Mở màn Chat 1:1 và chọn đúng hội thoại của friend đang test<br>2. Từ tài khoản LINE của friend, gửi 1 file cho bot<br>3. Quan sát dòng hội thoại của friend ở cột trái ngay khi tin tới, không reload trang | Friend gửi 1 file hợp lệ qua LINE app | Dòng leftbar của friend hiển thị đúng nhãn 「【ファイル】」 ngay khi tin tới; không hiển thị nội dung thô/code của message và không cần reload trang. | Chưa test | | DEV / STAGING / PRD (dự kiến) | | | | | Studio #13488 (NEW-20, client_ref 28542-new2-file) · ui / manual / env-safe · requirement_keys **rỗng** · tác giả **QA trangnq@mcp** |
| TC-OUTTRUTH001-06 | OUT-TRUTH-001 | Abnormal | Friend gửi loại tin không có nhánh riêng (image_map/pdf) → kiểm hiển thị nhánh mặc định, cảnh báo rủi ro image_map | Đang mở đúng hội thoại; chuẩn bị (nếu môi trường cho phép) tình huống friend gửi image_map hoặc pdf. | 1. Xác nhận với team LINE integration: friend có thể gửi loại image_map hoặc pdf từ phía LINE user không<br>2. Nếu có: cho friend gửi loại đó và quan sát leftbar realtime<br>3. So sánh với code mới (đã bỏ case 'image_map') | Loại tin: image_map, pdf (nếu friend gửi được) | Nếu friend KHÔNG gửi được image_map (đúng giả định) ⇒ không phát sinh vấn đề. Nếu friend CÓ THỂ gửi image_map/pdf ⇒ tin rơi nhánh default và có nguy cơ hiển thị nội dung thô thay vì nhãn 【画像】 → coi là bug tái xuất, báo dev. Kết quả phải được human/dev xác nhận trước khi kết luận. | Chưa test | | DEV / STAGING / PRD (dự kiến) | | | | | Studio #12538 (NEW-4) · ui / manual / env-safe · REQ-009 · tác giả **AI** job#583 · ⚠️ **oracle chưa chốt** — note: TC đánh dấu ASSUMPTION, code mới bỏ case image_map (config type_message_v2 không có key image_map). Không tự kết luận pass/fail — cần dev/LINE integration xác nhận. |
| TC-OUTTRUTH001-07 | OUT-TRUTH-001 | Normal | Friend gửi stamp tới hội thoại KHÔNG đang mở → leftbar cập nhật 【スタンプ】 từ get-badge, không flash nội dung thô | Bot có ít nhất 2 hội thoại: friend X (đang mở) và friend Y (không mở). conversation.last_message của Y đã được job linect chuẩn hoá cho stamp (【スタンプ】). Đang mở hội thoại X trên màn Chat 1:1. | 1. Mở màn Chat 1:1 và đang xem hội thoại của friend X<br>2. Cho friend Y (không phải hội thoại đang mở) gửi 1 stamp cho bot<br>3. Quan sát dòng leftbar của friend Y ngay khi tin tới (không reload) | Friend Y gửi 1 stamp | Dòng leftbar của Y cập nhật thành 「【スタンプ】」 (lấy từ a.data.last_message do /ajax/get-badge trả về). Không có hiện tượng nhấp nháy hiển thị nội dung thô/JSON (đã bỏ gán previewContentMessage). Dòng của X không bị đổi content. | Chưa test | | DEV / STAGING / PRD (dự kiến) | | | | | Studio #12539 (NEW-5) · ui / manual / env-safe · REQ-002 · tác giả **AI** job#583 · note: Nhánh else bỏ content=previewContentMessage, thêm content=a.data.last_message trong .done() get-badge. Precondition phụ thuộc last_message đã chuẩn hoá (job linect) — chỉ dùng làm tiền điều kiện, không test job. |
| TC-OUTTRUTH001-08 | OUT-TRUTH-001 | Normal | Kiểm tra tĩnh: switch leftbar dùng hằng số số typeMessageV2 khớp messages_v2s.type (root cause đã sửa) | Có source branch ai_fixbug_28542. | 1. Mở public/js/chats/chat-v2.js, đối chiếu các case của switch e.type trong handler socket message.to<br>2. Đối chiếu key trong config/sns-line.php (type_message_v2) và hằng số app/MessagesV2.php<br>3. Xác nhận resources/views/basic/chat/index.blade.php:100 inject var typeMessageV2 = json_encode(config('sns-line.type_message_v2')) | N/A (đọc source) | switch dùng case typeMessageV2.sticker/video/audio/image/location/file (giá trị SỐ: sticker=5, image=2, video=3, audio=4, file=12, location=13) — KHÔNG còn case chuỗi 'sticker'/'image'…; blade inject typeMessageV2 đúng map số. Root cause (int vs string) đã được sửa ở tầng nguồn. | Chưa test | | LOCAL (skip) | pipeline (AI) | 2026-08-24 | | | Studio #12546 (NEW-12) · ui / **auto** / read-only · REQ-001 · tác giả **AI** job#583 · `last_exec.status = skip` run#459 — `[SOURCE_BLOCKED] SOURCE_CHECKOUT_ERROR` · note: TC kiểm tra tĩnh (auto) bổ trợ vì runtime realtime chưa dựng được; KHÔNG thay thế TC đường người dùng thật. |
| TC-OUTTRUTH001-09 | OUT-TRUTH-001 | Normal | Kiểm tra tĩnh: nhánh hội thoại-khác đã bỏ previewContentMessage và dùng a.data.last_message | Có source branch ai_fixbug_28542. | 1. Mở public/js/chats/chat-v2.js nhánh else (hội thoại không mở)<br>2. Xác nhận dòng listFriend[i].content = t.data.previewContentMessage ĐÃ BỊ XOÁ<br>3. Xác nhận trong .done() của /ajax/get-badge có khối: if (listEmail.listFriend[i] && a.data.last_message) { listEmail.listFriend[i].content = a.data.last_message; } | N/A (đọc source) | Không còn gán content = previewContentMessage (nội dung thô); content chỉ được gán từ a.data.last_message và chỉ khi last_message truthy. Khớp REQ-002 và điều kiện phòng thủ null (REQ-005). | Chưa test | | LOCAL (skip) | pipeline (AI) | 2026-08-24 | | | Studio #12547 (NEW-13) · ui / **auto** / read-only · REQ-002 · tác giả **AI** job#583 · `last_exec.status = skip` run#459 — `[SOURCE_BLOCKED]` |
| TC-OUTTRUTH001-10 | OUT-TRUTH-001 | Normal | Reload màn Chat 1:1 sau khi friend gửi stamp → leftbar hiển thị đúng 【スタンプ】 (đường page-load) | Hội thoại của friend đã có tin cuối là stamp; conversation.last_message = 【スタンプ】 (do job linect ghi). | 1. Trên màn Chat 1:1, reload trang (F5)<br>2. Quan sát dòng leftbar của friend sau khi trang tải xong | N/A | Dòng leftbar hiển thị 「【スタンプ】」 (page-load đọc conversation.last_message as content). Đường tải trang vốn đã đúng, không bị fix realtime làm hồi quy — khớp báo cáo 'reload mới đúng'. | Chưa test | | DEV / STAGING / PRD (dự kiến) | | | | | Studio #12549 (NEW-15) · ui / manual / read-only · REQ-007 · spec_ids source:ConversationService.php:105-116, redmine journal Ngần 2026-08-19 · tác giả **AI** job#583 · note: Baseline regression cho page-load path (không đổi trong branch). Chỉ đọc dữ liệu. |
| TC-FUNC004-01 | FUNC-004 | Boundary | Friend gửi text khi đang mở hội thoại: hiển thị nguyên văn (<50) và cắt 50 ký tự + '...' (≥50) | Đang mở đúng hội thoại của friend trên màn Chat 1:1. | 1. Cho friend gửi 1 tin text ngắn 10 ký tự<br>2. Quan sát leftbar<br>3. Cho friend gửi tiếp 1 tin text đúng 49 ký tự, rồi 1 tin đúng 50 ký tự, rồi 1 tin 80 ký tự<br>4. Quan sát leftbar sau mỗi lần | 10 ký tự; 49 ký tự; 50 ký tự; 80 ký tự (gồm cả ký tự tiếng Nhật/emoji để kiểm độ dài) | Text <50 ký tự hiển thị nguyên văn; text ≥50 ký tự hiển thị 50 ký tự đầu + '...' (theo nhánh default: content.length>=50 ⇒ substring(0,50)+'...'). Nhánh text không đổi hành vi so với trước fix. | Chưa test | | DEV / STAGING / PRD (dự kiến) | | | | | Studio #12537 (NEW-3) · ui / manual / env-safe · REQ-006 · tác giả **AI** job#583 · note: Boundary tại 49/50. Nhánh default không thay đổi trong fix nhưng test để chốt không hồi quy; e.content.length tính theo ký tự JS (lưu ý emoji surrogate pair). |
| TC-UI003-01 | UI-003 | Abnormal | get-badge thành công nhưng last_message rỗng/null → row leftbar giữ nguyên nội dung cũ | Đang mở hội thoại X. Dàn cảnh hội thoại Y có last_message rỗng/null (vd conversation.last_message chưa được job chuẩn hoá, hoặc get-badge trả last_message=null). | 1. Đang xem hội thoại X<br>2. Kích hoạt tin realtime tới hội thoại Y trong tình huống /ajax/get-badge trả response thành công nhưng data.last_message = null hoặc chuỗi rỗng<br>3. Quan sát dòng leftbar của Y | data.last_message = null hoặc "" | Row Y giữ nguyên nội dung trước đó vì last_message không có giá trị để ghi đè. Không hiển thị JSON/nội dung thô của stamp và không ảnh hưởng các row khác. | Chưa test | | DEV / STAGING / PRD (dự kiến) | | | | | Studio #12540 (NEW-6) v2 · ui / manual / env-safe · REQ-005 · tác giả **AI** job#583 · note: Tách khỏi nhánh request failure. Chỉ kiểm trường hợp get-badge trả response thành công nhưng last_message là null/rỗng. |
| TC-UI003-02 | UI-003 | Abnormal | Request get-badge lỗi/timeout/5xx → row leftbar giữ nguyên nội dung cũ, không hiển thị raw code | Đang mở hội thoại X; Y là hội thoại khác và row Y đang có nội dung cũ trên leftbar. Có thể dùng Network throttling/proxy để làm request /ajax/get-badge của Y timeout, network error hoặc trả 5xx. | 1. Mở màn Chat 1:1 và đang xem hội thoại X<br>2. Cho friend Y gửi 1 stamp để kích hoạt realtime update cho hội thoại Y<br>3. Chủ động làm request /ajax/get-badge của Y bị timeout, network error hoặc trả 5xx<br>4. Quan sát row Y và các row khác trên leftbar | Request get-badge thất bại: timeout / network error / HTTP 5xx | Row Y giữ nguyên nội dung trước đó; không hiển thị JSON/code thô của stamp; không bị trắng màn hoặc crash; các row khác không bị ghi đè bởi nội dung của Y. | Chưa test | | DEV / STAGING / PRD (dự kiến) | | | | | Studio #13489 (NEW-21, client_ref 28542-getbadge-failure) · ui / manual / env-safe · requirement_keys **rỗng** (note ghi REQ-005) · tác giả **QA trangnq@mcp** · ⚠️ **trùng nội dung với #13492 (NEW-24)** |
| TC-UI003-03 | UI-003 | Abnormal | get-badge lỗi/timeout khi hội thoại không mở → leftbar giữ content cũ, không flash raw JSON và không trắng màn | Bot có ít nhất 2 hội thoại: X đang mở và Y không mở. Row Y đang có content cũ hợp lệ trên leftbar. Có thể chủ động chặn hoặc làm /ajax/get-badge của Y timeout/network error/HTTP 5xx. | 1. Mở màn Chat 1:1 và đang xem hội thoại X<br>2. Chủ động cấu hình để request /ajax/get-badge của hội thoại Y bị timeout, network error hoặc HTTP 5xx<br>3. Từ tài khoản LINE của friend Y, gửi 1 stamp cho bot<br>4. Quan sát row Y và toàn bộ màn Chat 1:1 sau khi request thất bại | Friend Y gửi 1 stamp; /ajax/get-badge của Y bị timeout/network error/HTTP 5xx | Row Y giữ nguyên content cũ đang hiển thị; không flash hoặc hiển thị raw JSON/code của stamp. Màn Chat 1:1 không trắng màn, không crash và các row khác không bị ghi đè. Khi reload lại trang sau đó, leftbar có thể lấy trạng thái server hiện tại theo page-load path bình thường. | Chưa test | | DEV / STAGING / PRD (dự kiến) | | | | | Studio #13492 (NEW-24, client_ref 28542-new-b-getbadge-failure) · ui / manual / env-safe · requirement_keys **rỗng** · tác giả **QA trangnq@mcp** · ⚠️ **TRÙNG #13489 (NEW-21)** — đề nghị archive 1 trong 2 trên Studio |
| TC-CONC003-01 | CONC-003 | Abnormal | Friend Y gửi stamp và friend Z gửi ảnh gần đồng thời → mỗi row leftbar giữ đúng nội dung dù get-badge trả về đảo thứ tự | Bot có ít nhất 3 hội thoại: X đang mở, Y và Z không mở. Y và Z là hai friend khác nhau. Có thể dùng Network throttling/proxy/breakpoint để cố tình làm response /ajax/get-badge của hai hội thoại trả về không cùng thứ tự với lúc request được gửi. | 1. Mở màn Chat 1:1 và đang xem hội thoại X<br>2. Cho friend Y gửi 1 stamp để kích hoạt cập nhật realtime cho row Y<br>3. Gần như đồng thời, cho friend Z gửi 1 ảnh để kích hoạt cập nhật realtime cho row Z<br>4. Chủ động làm chậm một request /ajax/get-badge để response của hai hội thoại trả về đảo thứ tự so với lúc gửi<br>5. Sau khi cả hai response hoàn tất, quan sát riêng row Y và row Z trên leftbar | Friend Y gửi stamp → expected 「【スタンプ】」; friend Z gửi image → expected 「【画像】」. Hai message gửi gần đồng thời và response get-badge được cố tình cho về đảo thứ tự. | Row Y phải hiển thị đúng 「【スタンプ】」 và row Z phải hiển thị đúng 「【画像】」 sau khi cả hai callback hoàn tất. Không được xảy ra hoán đổi/cross-update nội dung giữa Y và Z dù response get-badge về sai thứ tự. Row X đang mở không bị ghi đè bởi nội dung của Y hoặc Z. | Chưa test | | DEV / STAGING / PRD (dự kiến) | | | | | Studio #12541 (NEW-7) v2 · ui / manual / env-safe · REQ-004 · spec_ids diff:chat-v2.js:343-368, sourceCritique.conflicts[0] · tác giả **AI** job#583 · note: Viewpoint chính CONC-003; SEC-ISO-001 là coverage phụ · ⚠️ **trùng nội dung với #13491 (NEW-23)** |
| TC-CONC003-02 | CONC-003 | Abnormal | Chuyển hội thoại trước khi callback get-badge về → kiểm confirm_count/block_status không cập nhật nhầm hội thoại (rủi ro race còn tiềm ẩn) | Bot có hội thoại Y (không mở) và W (không mở). Đang xem hội thoại X. Có thể làm mạng chậm để kéo dài callback get-badge. | 1. Đang xem hội thoại X<br>2. Cho friend Y gửi tin (kích hoạt get-badge cho Y)<br>3. Ngay lập tức click chuyển sang mở hội thoại W trước khi callback get-badge của Y trả về (sk_conversation_id đổi sang W)<br>4. Sau khi callback về, kiểm số chưa đọc (confirm_count/total_confirm) và trạng thái block hiển thị ở các hàng | Thao tác chuyển hội thoại trong lúc callback get-badge của Y đang chờ | Confirm count và block status phát sinh từ tin mới của friend Y chỉ được cập nhật cho đúng hội thoại Y. Hội thoại W không được thay đổi confirm count hoặc block status do callback của Y, kể cả khi user đã chuyển sang W trước khi response get-badge của Y trả về. | Chưa test | | DEV / STAGING / PRD (dự kiến) | | | | | Studio #12542 (NEW-8) v2 · ui / manual / env-safe · REQ-004 · spec_ids diff:chat-v2.js:364-369, redmine journal AI LME 2026-08-24 mục 2 (1) · tác giả **AI** job#583 · ⭐ TC giá trị cao nhất: REQ-004 ghi rõ "phần confirm_count/block_status còn rủi ro dùng biến toàn cục cần xác nhận" |
| TC-CONC003-03 | CONC-003 | Abnormal | Friend Y gửi stamp, friend Z gửi ảnh gần đồng thời và callback get-badge trả ngược thứ tự → mỗi row leftbar hiển thị đúng nội dung | Bot có ít nhất 3 hội thoại: X đang mở, Y và Z không mở. Có thể dùng Network throttling/proxy/breakpoint để chủ động làm response /ajax/get-badge của hai hội thoại trả về ngược thứ tự so với request. | 1. Mở màn Chat 1:1 và đang xem hội thoại X<br>2. Cho friend Y gửi 1 stamp để kích hoạt cập nhật realtime cho row Y<br>3. Gần như đồng thời, cho friend Z gửi 1 ảnh để kích hoạt cập nhật realtime cho row Z<br>4. Chủ động làm chậm một request /ajax/get-badge để callback của hai hội thoại trả về ngược thứ tự<br>5. Sau khi cả hai callback hoàn tất, quan sát riêng row Y và row Z trên leftbar | Friend Y gửi stamp → expected 「【スタンプ】」; friend Z gửi image → expected 「【画像】」 | Row Y phải hiển thị đúng 「【スタンプ】」 và row Z phải hiển thị đúng 「【画像】」 sau khi cả hai callback hoàn tất. Không được swap/cross-update nội dung giữa Y và Z dù response get-badge trả về ngược thứ tự. Row X đang mở không bị ghi đè bởi nội dung của Y hoặc Z. | Chưa test | | DEV / STAGING / PRD (dự kiến) | | | | | Studio #13491 (NEW-23, client_ref 28542-new-a-race-distinct) · ui / manual / env-safe · requirement_keys **rỗng** · tác giả **QA trangnq@mcp** · ⚠️ **TRÙNG #12541 (NEW-7)** — đề nghị archive 1 trong 2 trên Studio |
| TC-DEPLOYASSET001-01 | DEPLOY-ASSET-001 | Normal | Kiểm tra tĩnh: version asset đã bump và blade nhúng chat-v2.js kèm query string ?v=version | Có source branch ai_fixbug_28542. | 1. Mở config/sns-line.php, xác nhận 'version' = 202608191100 (đổi từ 202608181516)<br>2. Mở resources/views/basic/chat/index.blade.php dòng ~185, xác nhận script src="/js/chats/chat-v2.js?v={{config('sns-line.version')}}" | N/A (đọc source) | version = 202608191100; blade nhúng chat-v2.js với query string ?v={{config('sns-line.version')}} ⇒ URL asset đổi ⇒ browser cache-bust, tải bản JS mới ở lần reload thường (không cần hard-reload). | Chưa test | | LOCAL (skip) | pipeline (AI) | 2026-08-24 | | | Studio #12548 (NEW-14) · ui / **auto** / read-only · REQ-008 · tác giả **AI** job#583 · `last_exec.status = skip` run#459 — `[SOURCE_BLOCKED]` · note: Xác nhận cơ chế cache-bust ở tầng nguồn; hành vi browser thật kiểm ở TC-DATACACHE001-01. |
| TC-DATACACHE001-01 | DATA-CACHE-001 | Abnormal | Browser đang giữ JS cũ: sau reload thường (không hard-reload) tải chat-v2.js mới và hết bug hiển thị stamp | Browser đã cache chat-v2.js phiên bản cũ (?v=202608181516). Deploy bản mới với version 202608191100. | 1. Với browser còn cache JS cũ, mở lại màn Chat 1:1 (reload thường, KHÔNG hard-reload/clear cache)<br>2. Kiểm tab Network: URL chat-v2.js có query ?v=202608191100 và được tải mới (không phải 304 từ bản cũ)<br>3. Cho friend gửi stamp và quan sát leftbar realtime | N/A | Browser tải chat-v2.js?v=202608191100 (bản mới) nhờ query string đổi; sau đó leftbar hiển thị 【スタンプ】 đúng khi friend gửi stamp — người dùng KHÔNG cần hard-reload để hết bug. | Chưa test | | DEV / STAGING / PRD (dự kiến) | | | | | Studio #12550 (NEW-16) · ui / manual / env-safe · REQ-008 · spec_ids diff:config/sns-line.php, source:index.blade.php:185, DATA-CACHE-001 · tác giả **AI** job#583 · note: Kiểm hành vi cache thật của browser (query-string cache-bust). Cần quan sát Network → để manual. |
| TC-APICONTRACT001-01 | API-CONTRACT-001 | Normal | get-badge với conversation_id thuộc bot đang đăng nhập → response trả last_message = conversation.last_message | Đăng nhập chọn bot A. Có 1 conversation của bot A với last_message đã biết (vd 【スタンプ】). | 1. Gửi POST /ajax/get-badge kèm CSRF, body conversation_id = id hội thoại của bot A<br>2. Đọc JSON response và đối chiếu field last_message với conversation.last_message trong DB (chỉ để đối chiếu, không sửa) | conversation_id = hội thoại của bot A, last_message hiện tại = 【スタンプ】 | Response data có đủ field mới last_message = giá trị conversation.last_message (vd '【スタンプ】'), kèm badge, confirm_count, msg_kind. Hợp đồng response bổ sung field last_message tương thích client mới. | Chưa test | | LOCAL (skip) | pipeline (AI) | 2026-08-24 | | | Studio #12544 (NEW-10) · api / **auto** / read-only · REQ-003 · tác giả **AI** job#583 · `last_exec.status = skip` run#459 — `[SOURCE_BLOCKED]` · ⚠️ mã `API-CONTRACT-001` KHÔNG có trong checklist-lme → không tính cover |
| TC-SEC001-01 | SEC-001 | Abnormal | get-badge với conversation_id của bot KHÁC → response last_message=null, confirm_count=0 (chặn lộ cross-bot) | Có 2 bot A và B thuộc hai phiên/khác chủ. Đăng nhập phiên đang chọn bot A. Biết 1 conversation_id hợp lệ THUỘC bot B (có last_message khác null). | 1. Với phiên đang đăng nhập chọn bot A, gửi POST /ajax/get-badge kèm CSRF token, body conversation_id = id hội thoại của bot B<br>2. Đọc JSON response | conversation_id = hội thoại thuộc bot B (khác bot đang đăng nhập) | Response: data.last_message = null và data.confirm_count = 0 (do query thêm ->where('bot_id', getBotId()) không tìm thấy conversation của bot khác). Không lộ nội dung tin cuối của bot B. HTTP 200, success=true nhưng không rò rỉ dữ liệu. | Chưa test | | LOCAL (skip) | pipeline (AI) | 2026-08-24 | | | Studio #12543 (NEW-9) · api / **auto** / env-safe · REQ-003 · spec_ids diff:ChatController.php:6969-6994, SEC-001 · tác giả **AI** job#583 · `last_exec.status = skip` run#459 — `[SOURCE_BLOCKED]` · note: Endpoint không có mã EP spec — expected suy từ diff code. |
| TC-TOOLERRHYG001-01 | TOOL-ERRHYG-001 | Abnormal | get-badge thiếu conversation_id → không crash server và không trả dữ liệu hội thoại | Đăng nhập chọn bot A. | 1. Gửi POST /ajax/get-badge với session/CSRF hợp lệ nhưng không truyền conversation_id<br>2. Đọc HTTP status và response body<br>3. Xác nhận response không chứa last_message/confirm_count của bất kỳ hội thoại nào khác | Không truyền conversation_id | Server không được crash hoặc trả HTTP 5xx. Response phải xử lý input thiếu conversation_id một cách an toàn và không được trả dữ liệu của bất kỳ hội thoại nào. HTTP status/body cụ thể cần đối chiếu contract endpoint khi được xác nhận, không cố định 200 như requirement chính thức. | Chưa test | | LOCAL (skip) | pipeline (AI) | 2026-08-24 | | | Studio #12545 (NEW-11) v2 · api / **auto** / read-only · REQ-003 · tác giả **AI** job#583 · `last_exec.status = skip` run#459 — `[SOURCE_BLOCKED]` · ⚠️ mã `TOOL-ERRHYG-001` KHÔNG có trong checklist-lme · ⚠️ **oracle chưa chốt** (endpoint chưa có contract/EP chính thức) |
| TC-TOOLERRHYG001-02 | TOOL-ERRHYG-001 | Abnormal | get-badge với conversation_id không tồn tại → không crash server và không trả dữ liệu hội thoại | Đăng nhập chọn bot A và có session/CSRF hợp lệ. | 1. Gửi POST /ajax/get-badge với conversation_id là một ID chắc chắn không tồn tại trong hệ thống<br>2. Đọc HTTP status và response body<br>3. Xác nhận response không chứa last_message/confirm_count của bất kỳ hội thoại hợp lệ nào | conversation_id = 999999999 hoặc một ID đã xác nhận không tồn tại | Server không được crash hoặc trả HTTP 5xx. Không được trả dữ liệu của hội thoại khác khi conversation_id không tồn tại. HTTP status/body cụ thể cần đối chiếu contract endpoint khi được xác nhận, không cố định 200 như requirement chính thức. | Chưa test | | ALL (dự kiến) | | | | | Studio #13490 (NEW-22, client_ref 28542-getbadge-notfound) · api / **auto** / read-only · requirement_keys **rỗng** · tác giả **QA trangnq@mcp** · ⚠️ mã `TOOL-ERRHYG-001` KHÔNG có trong checklist-lme · ⚠️ **oracle chưa chốt** |

---

## Requirements gốc trên Studio (task #199) — để đối chiếu coverage

| REQ | Tiêu đề | Category | Risk |
|---|---|---|---|
| REQ-001 | Realtime leftbar khi đang mở hội thoại: tin non-text hiển thị đúng nhãn 【…】 | ui | **High** |
| REQ-002 | Realtime leftbar khi ở hội thoại khác: lấy last_message đã chuẩn hoá từ get-badge | ui | **High** |
| REQ-003 | get-badge bảo mật cross-bot & hợp đồng response có last_message | api | **High** |
| REQ-004 | Không lẫn nội dung giữa các hội thoại khi nhiều tin realtime đến | ui | Medium |
| REQ-005 | get-badge không trả được last_message → không ghi đè content bằng nội dung thô | ui | Medium |
| REQ-006 | Tin text (nhánh mặc định) vẫn hiển thị đúng, cắt ≤50 ký tự | ui | Medium |
| REQ-007 | Tải lại trang: leftbar hiển thị đúng nhãn (page-load path không đổi) | ui | Low |
| REQ-008 | Version asset bump: browser tải chat-v2.js mới không cần hard-reload | data | Medium |
| REQ-009 | Loại tin không có nhánh riêng (image_map/pdf) rơi nhánh mặc định | validation | Low |

> ⚠️ REQ-004 ghi nguyên văn: *"phần confirm_count/block_status **còn rủi ro dùng biến toàn cục cần xác nhận**"* — tức chính Studio đã đánh dấu fix chưa chắc đóng hết race. Chỉ có TC-CONC003-02 (#12542) chạm điểm này và **chưa chạy**.
