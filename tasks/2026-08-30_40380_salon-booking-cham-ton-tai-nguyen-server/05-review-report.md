# 05 — Review Report

Ticket: **#40380** — Salon booking khi access vào màn hình booking đang chậm và bị tốn tài nguyên serve
Review: vòng 1 · 2026-08-30

---

## 0. Nguồn TC

| Mục | Giá trị |
|---|---|
| **Nguồn đã dùng** | **NGUỒN 1 — MCP LME TEST STUDIO** · `task_list(ticket_id=40380)` → task **#268** → `testcase_list(task_id=268)` |
| Định danh lấy từ | tên folder `..._40380_...` → ticket 40380 |
| Thời điểm fetch | 2026-08-30 (payload vượt token limit → parse bằng `scripts/parse_studio_tcs.py`, không đọc raw vào context) |
| Tổng TC lấy về | **30** (`totalMatched = 30`, `nextCursor = null`) |
| Nguồn 2 (Sheet human) | **KHÔNG dùng** — đã có TC ở nguồn 1, dừng theo thứ tự ưu tiên |
| Nguồn 3 (file 04) | **KHÔNG dùng làm nguồn** — đã có TC ở nguồn 1 |
| **Đối chiếu chéo nguồn** | **KHÔNG** — dừng ở nguồn đầu tiên có TC, theo đúng quy tắc BƯỚC 0 |
| Snapshot đã ghi | **Không ghi đè `04-tc-list.md`** — file này đã là snapshot Studio của **cùng task #268**, header `<!-- source: MCP LME TEST STUDIO — task_id=268 ... fetch lúc 2026-08-30 -->`, fetch cùng ngày. `task_list` gọi lại lúc review xác nhận task **không đổi** (`updated_at 2026-08-29 04:17:39` · `exec 30 untested` · `reviewed=false`) → snapshot không cũ, ghi lại là no-op |
| **Nguồn spec đã dùng** | [spec-features/admin/salon-booking/feature-spec.md](../../spec-features/admin/salon-booking/feature-spec.md) — §2.4.3 「前後の空き時間」 (`calendar_salon_setting_time_free.time_before_google` / `time_after_google`), §2.4.4 「店舗とスタッフの受付上限」, §2.6 Flow LINE User đặt lịch (LIFF), §3.1 `calendar_salon_booking_by_google`, **BR-09** (Google Calendar liên kết per-staff), **BR-10** (ẩn nhân viên → xóa booking sync Google), §7 Background Jobs. Không cần dùng tới lme.jp/manual |
| Spec sibling đã tra | [spec-features/admin/lesson-booking/feature-spec.md](../../spec-features/admin/lesson-booking/feature-spec.md) §1.2 — FA-019 **KHÔNG có** tích hợp Google Calendar (đã kiểm chứng 2 nguồn độc lập) → sibling Lesson **không** nằm trong vùng regression của nhánh Google |
| Kho TCs đã đối chiếu | [kho-tcs/fa020-datlichsalon-サロン・面談予約.md](../../kho-tcs/fa020-datlichsalon-サロン・面談予約.md) — 498 TC |

### 0.6 — Cảnh báo bắt buộc về chất lượng nguồn

| # | Nội dung | Kết quả |
|---|---|---|
| 1 | **Kết quả thực thi thật** | **0 / 30 TC pass (0%)**. `last_exec = null` ở cả 30 TC · `exec{total 30, pass 0, fail 0, other 0, untested 30}` → **[BLOCKER]** (ngưỡng < 50%). Toàn bộ bộ TC là **coverage trên giấy**, chưa có 1 kết luận test nào |
| 2 | **TC fail / error / gắn ticket bug** | 0 fail · 0 error · `bug_tickets` rỗng ở cả 30 TC · `openBugs = 0` → không có TC fail chưa raise ticket |
| 3 | **Môi trường đã chạy** | **Chưa chạy ở bất kỳ env nào.** `env_scope` khai báo: 23 TC `local+dev+staging`, 7 TC `local+staging` → **0 TC có `prd`**. Task chạm **job nền** (2 job tự gán nhân viên) + **loadbalance** + **hiệu năng** → **[MAJOR] RULE-08 / ENV-003** |
| 4 | **Ai chạy** | `last_exec.source` / `by` đều null. `submittedWithoutMcp = false`. Chưa xác định được sẽ do pipeline AI hay QA người chạy — nhưng `exec_mode = auto` ở **30/30 TC** cho thấy bộ này thiết kế cho runner tự động, không cho manual tester |
| 5 | **Tác giả TC** | **30/30 do AI sinh** (`author = AI` · `provenance.source = ai` · `created_job_id = 822` · `toolWritten: tool 30 / mcp 0 / human 0`), `reviewState = tester`, `reviewed = false` → **[MAJOR]** (100% ≥ ngưỡng 50%, review chưa `done`) |
| 6 | **Mã quan điểm không có trong `checklist-lme.md`** | **6/30 TC** không map được: `TOOL-KNOW-002`(2) · `TOOL-NEGCTRL-001`(2) · `API-CONTRACT-001`(2) · `TOOL-AXIS-001`(1) · `TOOL-ERRHYG-001`(1) · `RULE-07`(1 — là RULE, không phải quan điểm). **Không tính là cover** ở §3 / §3.6 |

---

## 1. Verdict

> ## ❌ **REJECTED**

Có **4 `[BLOCKER]`**. Không phải vì TC viết kém — nội dung TC khá tốt và bám đúng bản chất "bản vá tối ưu ⇒ kết quả phải KHÔNG ĐỔI" — mà vì **input của Dev thiếu**, **tiêu chí Đạt của 20/30 TC không dựng được**, và **chưa có TC nào được chạy**.

---

## 2. Tóm tắt cho member

**Điểm tốt**: bộ 30 TC nắm đúng bản chất bài toán — tiêu chí chính là *"slot mở/bị chặn không được đổi so với nhánh cũ"*, chứ không chỉ "chạy nhanh hơn". TC biên ngày (`NEW-9` ca qua nửa đêm, `NEW-10` ca từ hôm trước, `NEW-13`/`NEW-19` biên tháng) và `NEW-30` (so tập bản ghi truy vấn gộp = hợp các truy vấn cũ) là những TC đắt giá, đánh trúng chỗ prefetch + group-by-date dễ làm rơi bản ghi ở 2 đầu biên.

**Phải fix trước khi chạy**: (1) **20/30 TC** định nghĩa Đạt bằng *"khớp mốc đối chứng nhánh cũ"* nhưng **không TC nào mô tả cách dựng nhánh cũ** và không env nào chạy song song 2 nhánh — không có mốc thì 20 TC đó không kết luận được; (2) Dev **chưa cung cấp mục 3 + 4.1/4.2/4.3** nên không ai biết hàm dùng chung này còn được gọi ở đâu; (3) **thiếu hẳn** TC cho `受付上限` (Google block chặn slot nhưng không tính vào limit), cho **đặt trùng đồng thời**, và cho **production**; (4) quy mô test (8–10 nhân viên) **nhỏ hơn quy mô nêu trong chính mục 1 của ticket** (30 nhân viên).

---

## 3. Coverage Matrix

> ⚠️ **`03-dev-impact.md` mục 4.1 / 4.2 / 4.3 TRỐNG** → **không có tag `F*` / `D*` / `T*`** để lập ma trận theo đúng chuẩn.
> Ma trận dưới đây lập trên **BUG (3 tầng root cause ở mục 1)** + **FIX (mục 2)**. Trục `REQ-xxx` là **13 requirement do Studio/AI tự sinh từ source**, KHÔNG phải Dev khai — chỉ dùng tham chiếu.
> **Cột `Exec`**: 0 pass ở mọi dòng ⇒ theo quy tắc BƯỚC 2, **không dòng nào đạt `OK`** (có TC ≠ đã test).

| Impact | Loại | TC cover (suy luận) | # TC | Exec | Status |
|---|---|---|---|---|---|
| **BUG-1** — Tầng 1: N+1 ở nhánh Google (`getListBookingByGoogleConfirm` gọi trong `foreach` theo ca, không memo hoá) | BUG | NEW-1, NEW-2, NEW-3, NEW-4, NEW-5, NEW-8, NEW-14, NEW-15, NEW-20, NEW-30 | 10 | 0/10 | **RISK** |
| **BUG-2** — Tầng 2: `handleShowListBooking` `for i<=3` + fallback → nhân ~5 lần | BUG | NEW-6, NEW-12, NEW-18, NEW-20 (đếm query "nhân thêm do vòng dò nhiều tuần") | 4 | 0/4 | **RISK** |
| **BUG-3** — Tầng 3: full table scan · bảng chỉ có PK · `whereDate()` bọc cột `date` | BUG | NEW-29 (EXPLAIN + liệt kê index), NEW-26 (ghi chú nghi vấn index) | 2 | 0/2 | **RISK** — xem `[MAJOR] NEW-29` §4.2: expected cho phép kết luận "ngoài phạm vi bản vá" ⇒ TC không bao giờ Không đạt |
| **FIX-1** — Prefetch 1 query + group theo ngày | BUG | NEW-30 (tập bản ghi gộp = hợp tập cũ), NEW-9, NEW-10, NEW-13, NEW-19 (biên ngày sau khi gộp), NEW-23 (nhánh giới hạn lịch = 1) | 6 | 0/6 | **RISK** |
| **FIX-2 / FIX-3** — có xử lý tầng 2 và tầng 3 không? | BUG | — | — | — | **Input thiếu** — mục 2 dừng ở "Fix 1", xem §6 |
| **F\*** — function bị ảnh hưởng | Function | — | — | — | **Input thiếu (4.1 trống)** |
| **D\*** — data bị update | Data | — | — | — | **Input thiếu (4.2 trống)** — nếu bản vá thêm index thì đây là `MIGRATE` chưa khai |
| **T\*** — tính năng bị ảnh hưởng | Feature | — | — | — | **Input thiếu (4.3 trống)** |

### Vùng regression suy từ spec + kho TCs (không có trong 4.3 của Dev)

| Vùng | Căn cứ | TC cover | Status |
|---|---|---|---|
| Bảng chọn giờ tab 週 (LIFF khách) | spec §2.6 · `CalendarSalonController:747` | NEW-1…NEW-13 (13 TC) | **RISK** |
| Lịch tháng tab 月 | `CalendarSalonController:4095` | NEW-14, NEW-15, NEW-18, NEW-19, NEW-22 | **RISK** |
| Bước xác nhận / gửi đơn đặt lịch | `CalendarSalonController:2848 / 3105` | NEW-16, NEW-17 | **RISK** |
| 2 job tự gán nhân viên | spec §7 · `CalendarSalonStaffAssignment(.Admin)` | NEW-27, NEW-28 | **RISK** |
| Payload API trang đặt lịch (`booking.js` đọc trực tiếp) | `public/js/calendar_salon/booking.js` | NEW-21, NEW-22 | **RISK** (mã `API-CONTRACT-001` không có trong tầng 1 → không tính cover) |
| **`受付上限` — 店舗・スタッフ** | spec §2.4.4 · kho TC-SLN-284, TC-SLN-288 (Bug #26908) | **KHÔNG CÓ TC NÀO** | **GAP** |
| **Đặt trùng đồng thời (2 khách 1 slot cuối)** | kho TC-SLN-460 (Bug KH #38280) | NEW-17 **không phải** concurrency (xem §4.1) | **GAP** |
| **Google event vắt qua nửa đêm → tách 2 block** | kho TC-SLN-363 (Bug #26739) | **KHÔNG CÓ** | **GAP** |
| **2 nhân viên liên kết CHUNG 1 Google Calendar** | spec BR-09 · kho TC-SLN-366, TC-SLN-367 (Bug #26653) | **KHÔNG CÓ** | **GAP** |
| **所要時間 lẻ / 13 mức đơn vị nhận booking** | kho TC-SLN-433, TC-SLN-441 (Bug #31160) | Chỉ NEW-3 chạm "đơn vị chia giờ 30 phút"; toàn bộ 30 TC dùng khoá học 60 phút | **GAP** |
| **Multi-tenant 2 bot (WHERE scope của truy vấn gộp)** | kho TC-SLN-213 | NEW-30 chỉ tách theo *lịch salon khác*, không theo *bot khác* | **RISK** |
| **Production (job nền · loadbalance · quy mô thật)** | RULE-08 · Catalog `ENV-JOB`, `ENV-LB` | 0 TC có `prd` | **GAP** |
| **Release không maintain / job đang chạy dở** | kho TC-SLN-488, TC-SLN-494 | **KHÔNG CÓ** | **GAP** |

### ORPHAN TCs

| TC | Lý do |
|---|---|
| **NEW-25** (`TOOL-NEGCTRL-001` — endpoint xem lịch booking của app quản lý không đổi) | Là **đối chứng âm có chủ ý** (REQ-013 risk Low): endpoint này *không* đi qua engine tính slot. Theo **AP-5** đây là layer không bị chạm code → `[NIT]`, **giữ lại** vì chi phí thấp và giá trị chứng minh phạm vi bản vá; đề nghị ghi rõ "đối chứng âm" ở Ghi chú thay vì để lẫn như TC chức năng |
| **NEW-24** (`TOOL-ERRHYG-001` — tham số ngày sai định dạng) | Không nằm trên đường fix (validate input không bị chạm). Giữ ở mức `[NIT]` — hữu ích như smoke sau khi đổi cách dựng khoảng ngày |

---

## 3.5 Fix-shape analysis (adversarial)

**Fix shape nhận diện được từ mục 2**: `"Prefetch 1 query, group theo ngày (giết N+1)"` → shape **"fix N+1 / optimize query"**, cộng shape **"sửa hàm dùng chung"** (hàm bị sửa có ≥ 4 call site theo spec + Studio), và **có thể** shape **"migration / alter table"** (nếu bản vá thêm index cho tầng 3).

| Shape | Câu hỏi adversarial | Trả lời từ bộ TC hiện tại | Kết luận |
|---|---|---|---|
| fix N+1 / optimize query | Test với **quy mô khách hàng lớn nhất THỰC TẾ**? | NEW-6: 8 nhân viên / ~5.000 bản ghi · NEW-26: 10 nhân viên / ~20.000 bản ghi · NEW-20: 5 nhân viên. **Mục 1 của chính ticket nói "30 staff × 7 ngày ≈ 210 query"** → quy mô test **nhỏ hơn 3 lần** quy mô đã nêu là đang gây lỗi | **[MAJOR] FIX-SHAPE** |
| fix N+1 / optimize query | Có xét **rate limit API bên thứ 3**? | Không cần — bản vá nằm ở đường **đọc DB**, không gọi Google API. Job sync Google không bị chạm code | ✅ không áp dụng (ghi lý do theo RULE-03) |
| sửa hàm dùng chung | Có **danh sách nơi ảnh hưởng do DEV cung cấp**? | **KHÔNG.** `03-dev-impact.md` mục 3 và 4.1 **trống hoàn toàn**. Danh sách 4 call site hiện có là do **AI đọc source tự suy**, không phải Dev xác nhận | **[BLOCKER] FIX-SHAPE** |
| sửa hàm dùng chung | Chức năng **tương tự** (Salon ⇄ Lesson ⇄ Booking Event) đã rà? | Đã tra spec: **FA-019 Lesson KHÔNG có tích hợp Google Calendar** (kiểm chứng 2 nguồn) ⇒ nhánh Google **không** tồn tại ở Lesson. Nhưng **mẫu N+1 trong `foreach` theo ca** thì có thể lặp ở hàm dựng slot của Lesson / Booking Event — chưa ai xác nhận | **[MAJOR]** — cần Dev trả lời, không tự kết luận |
| migration / alter table (**nghi ngờ**) | Verify data cũ không mất? Migration **idempotent**? Rollback? Thời gian khoá bảng trên production? | NEW-29 **kỳ vọng bảng đã có index** ⇒ ngụ ý có migration. Nhưng mục 2 không khai, mục 4.2 trống, **0 TC** kiểm migration chạy lại / rollback / thời gian khoá bảng | **[BLOCKER] có điều kiện** — nếu Dev xác nhận có thêm index (xem §6 Q2) |

**Symptom-only KH report check**: **không áp dụng theo hướng thông thường.** Ticket là tracker `Bug tự detect`, **không có mô tả của khách hàng, không có Actual/Expected, 0 attachment, 0 journal**. Hệ quả ngược lại: **không có triệu chứng người dùng nào được ghi nhận** → không có mốc nghiệp vụ để nói "đã hết chậm". Toàn bộ tiêu chí hiệu năng đang tự định nghĩa trong TC. → ghi nhận ở `[MAJOR]` §4.2.

**Anti-patterns**:

| AP | Dính? | Ghi chú |
|---|---|---|
| **AP-6** — Mục 3 dev-impact trống | ✅ **DÍNH** | Mục 3 chỉ có heading. Có thể còn function khác cùng mẫu N+1 chưa được fix |
| **AP-4** — fix shape không verify được | ✅ **DÍNH** | Trường `Commit / Pull Request` = `<chưa có>` → không đọc được diff để biết bản vá thực sự làm gì (đặc biệt tầng 2 và tầng 3) |
| **AP-3** — Happy-path-only regression | ⚠️ **một phần** | Không kiểm được đúng chuẩn vì 4.3 trống. Nhưng các vùng regression suy ra từ kho (受付上限, đặt trùng, Google event qua nửa đêm) đều **0 TC** — đúng triệu chứng AP-3 |
| **AP-5** — Layer-downstream over-coverage | ⚠️ nhẹ | NEW-24, NEW-25 (xem ORPHAN) — mức `[NIT]`, không đề nghị xoá |
| AP-1 / AP-2 | ❌ | Không phải fix dạng generic catch; không phải KH report |

---

## 3.6 Bảng quan điểm đối chiếu

> Nguồn: [framework/checklist-lme.index.md](../../framework/checklist-lme.index.md) + [framework/catalog-lme.index.md](../../framework/catalog-lme.index.md).
> **Exec = 0 pass ở mọi dòng** — mọi kết luận "có TC" đều chỉ là trên giấy.

| Mã quan điểm | Ưu tiên | Trigger khớp task? | TC cover (suy luận) | Exec | Kết luận |
|---|---|---|---|---|---|
| `INTG-CAL-001` | Cao | ◯ — đồng bộ lịch với Google Calendar | NEW-2 | 0/1 | **[MAJOR] RULE-01** — chỉ 1 Normal. Kiểm tra yêu cầu **3 hướng** (LME→ngoài / ngoài→LME / thao tác gần đồng thời 2 phía); TC chỉ chạm hướng ngoài→LME. ⚠️ checklist ghi *"10 ticket Closed — điểm yếu lặp lại nhiều nhất của Salon"* |
| `REG-SHARED-001` | Cao | ◯ — sửa hàm dùng chung ≥ 4 call site | NEW-23, NEW-27, NEW-28 | 0/3 | **[BLOCKER]** — thiếu **danh sách nơi ảnh hưởng do Dev cung cấp** (bắt buộc theo Kiểm tra của quan điểm) |
| `FUNC-DATE-001` | Cao | ◯ — gộp truy vấn theo khoảng ngày, ca qua nửa đêm | NEW-9, NEW-10, NEW-13, NEW-19 | 0/4 | **RISK** — đủ 4 Boundary, **thiếu Normal + Abnormal** (RULE-01, không ghi lý do). Catalog `DI-09`: thiếu biên **`00:00` vs `24:00`** và `23:59→00:01`; `DI-10`: thiếu **xuyên năm** ở tab 月 |
| `DATA-DB-001` | Cao | ◯ — truy vấn gộp đổi `WHERE` scope | NEW-29, NEW-30 | 0/2 | **RISK** — NEW-30 tách được *lịch salon khác* + *nhân viên đã tắt*, nhưng **không có TC trên 2 bot** (Kiểm tra mục (1) của quan điểm). Không có UPDATE/DELETE nên chưa nâng BLOCKER |
| `CONC-001` | Cao | ◯ — slot là **giới hạn dùng chung**, có nút gửi đơn | NEW-17 (gắn nhãn CONC-001 nhưng **không phải** đồng thời) | 0/1 | **[BLOCKER]** — **0/4 kịch bản** bắt buộc (double-click · 2 tab cùng user · 2 user cùng bản ghi · batch đa luồng chạm giới hạn chung). Bộ TC gốc **không có TC nào chạm `受付上限`**, nên nhánh **staff limit = 1** cũng trống. → §5 TC-CONC001-01 → -07 |
| `ENV-003` ★ | Cao | ◯ — chạm **job nền** + loadbalance | 0 TC có `prd` | 0/0 | **[MAJOR] RULE-08** — Catalog `ENV-JOB` (prod tách 3 job độc lập), `ENV-LB` (prod 2 server) |
| `ENV-001` | Cao | ◯ — Trigger nêu đích danh **"validate slot chạy qua nhiều server"** | **KHÔNG CÓ** | — | **[BLOCKER]** — quan điểm Cao, 0 TC. Prefetch gom dữ liệu trong 1 request ⇒ hành vi khi 1 server rớt giữa chừng chưa ai kiểm |
| `JOB-001` ★ | Cao | ◯ — 2 job tự gán nhân viên gọi hàm bị sửa | NEW-27, NEW-28 | 0/2 | **[MAJOR]** — chỉ happy path 1 đơn. Thiếu Kiểm tra mục (4) *"số bản ghi vào = thành công + hàng đợi lỗi"* và mục (5) *"job đã có retry sẵn → verify lại sau mỗi lần tính năng thay đổi"* |
| `PERF-LARGE-001` | Cao (nâng — sync/hiệu năng) | ◯ | NEW-26 | 0/1 | **[MAJOR] RULE-01** — chỉ 1 Boundary, thiếu Normal + Abnormal; và quy mô nhỏ hơn quy mô trong mục 1 (xem §3.5) |
| `DATA-COUNT-001` | Cao | ◯ — `受付上限` đếm booking/slot; màn tháng đếm ngày còn chỗ | **KHÔNG CÓ** | — | **[BLOCKER]** — quan điểm Cao, 0 TC. checklist ghi *"lỗi lặp nhiều nhất lịch sử bug — 12 ticket Closed"* |
| `LIFF-ENTRY-001` ★ | Cao | ◯ — trang đặt lịch salon là link LINE user | 0 TC — **cả 30 TC mở bằng trình duyệt** (`/mobile/calendar-salon/<id>`) | — | **[MAJOR] RULE-06** — Catalog `MAP-LIFF-03`: thiếu điểm vào *app LINE (in-app browser)*; `MAP-LIFF-02`: thiếu case *chưa kết bạn*. Cơ chế LIFF không bị bản vá chạm, nên không nâng BLOCKER — nhưng **kết luận "nhanh hơn" đo trên desktop không đại diện cho thiết bị thật** |
| `OUT-TRUTH-001` | Cao | ◯ | NEW-1, NEW-3, NEW-14 | 0/3 | **RISK** — 3 Normal, thiếu Abnormal/Boundary; và cả 3 phụ thuộc "mốc đối chứng" chưa dựng được |
| `SEC-ISO-001` | Cao | ◯ — 1 màn tải slot của nhiều nhân viên đồng thời | NEW-4, NEW-8 | 0/2 | **RISK** — có Normal + Abnormal, thiếu Boundary; chưa có case *tải nhanh liên tiếp* (Kiểm tra của quan điểm) |
| `REG-RUN-001` | Cao | ◯ — job sync Google + job gán nhân viên chạy nền khi release | **KHÔNG CÓ** | — | **[MAJOR]** — Catalog `JOB-04`. Kho đã có TC-SLN-494 |
| `DEPLOY-LIVE-001` ★ | Cao | ◯ — release production không maintain | NEW-21/NEW-22 chỉ so payload **giữa 2 nhánh**, không phải *client cũ gọi server mới* | 0/2 | **[MAJOR]** — kho đã có TC-SLN-488 |
| `DATA-MIG-001` | Cao | ⚠️ **có điều kiện** — nếu bản vá thêm index | NEW-29 (chỉ EXPLAIN) | 0/1 | **[BLOCKER] có điều kiện** — xem §3.5 và §6 Q2 |
| `DATA-BACKUP-001` ★ | Cao | ⚠️ có điều kiện — "thêm/đổi bảng DB" | **KHÔNG CÓ** | — | Chỉ trigger nếu có ALTER TABLE. Kho có TC-SLN-496 (salon **không** nằm trong phạm vi データコピー) → nếu chỉ thêm index thì × với lý do này |
| `UI-003` | Trung bình → **Cao** (rủi ro false success: hiện slot trống mà thực tế không đặt được) | ◯ | NEW-12, NEW-18 | 0/2 | **RISK** — 2 Boundary. Thiếu Kiểm tra *mạng chậm / ngắt kết nối / loading vô hạn* — đáng chú ý vì đây là bug **hiệu năng** |
| `FUNC-SEQ-001` ★ | Trung bình | ◯ — chuyển tuần/tháng liên tiếp không reload | NEW-5, NEW-15, NEW-11 | 0/3 | **OK về chiều** (còn RISK vì chưa chạy). Thiếu bước **F5 sau chuỗi thao tác** |
| `PERM-001/002/003` | Cao | × | — | — | Bản vá không chạm phân quyền. Lý do RULE-03: không đổi route/middleware/điều kiện quyền |
| `MSG-002` / `STATE-DEP-001` | Cao | × | — | — | Gửi tin xác nhận + remind là **layer downstream không bị chạm code** — không đẻ TC (tránh AP-5) |
| `MEDIA-*` / `PAY-*` / `SEC-001/002` | Cao | × | — | — | Bản vá không chạm media, tiền, credential |
| `COMPAT-LEGACY-001` | Cao | × | — | — | Không có version-up đối tượng nào trong phạm vi bản vá |
| `DEPLOY-ASSET-001` | Cao | × | — | — | Không sửa JS/CSS — NEW-21/22 khẳng định payload không đổi nên `booking.js` không phải sửa. **Nếu Dev có sửa `booking.js` thì lật lại thành ◯** |

**Quan điểm ◯ mà KHÔNG có TC nào cover**: `ENV-001` · `DATA-COUNT-001` · `REG-RUN-001` · `ENV-003` (0 TC production) · `LIFF-ENTRY-001` (0 TC vào bằng LINE app).

---

## 4. Issues phát hiện

### 4.1 Blocker (phải fix trước khi merge)

**[BLOCKER] NGUỒN-0.6#1 — 0/30 TC đã chạy**: `exec{pass 0, fail 0, untested 30}`, `reviewed = false`. Coverage của toàn bộ report này là **trên giấy**. — *Đề xuất fix*: chạy bộ TC (ít nhất nhóm BUG-1/FIX-1) và cập nhật `last_exec` trên Studio trước khi xin duyệt vòng 2.

**[BLOCKER] TC-BASELINE — 20/30 TC định nghĩa Đạt bằng "mốc đối chứng nhánh cũ" nhưng không dựng được mốc**: NEW-1, 3, 6, 8, 11, 12, 13, 14, 17, 18, 20, 21, 22, 23, 24, 25, 26, 27, 28, 30 đều có tiêu chí dạng *"trùng khớp mốc đối chứng chạy trên nhánh cũ"* / *"giống hệt nhánh cũ"* / *"không xấu đi so với nhánh cũ"*. Nhưng: (a) **không TC nào có bước dựng/triển khai nhánh cũ `release_step_20260827`**, chỉ ghi ở tiền đề *"triển khai được cả hai nhánh"*; (b) `env_scope` là `local/dev/staging` — **staging chỉ có 1 phiên bản code**, không chạy song song 2 nhánh; (c) `exec_mode = auto` nhưng runner Studio không có cơ chế build 2 nhánh. ⇒ Nếu QA chạy mà không có mốc, 20 TC này sẽ được tick Đạt bằng cảm tính. — *Đề xuất fix*: chọn 1 trong 2 — **(A)** bổ sung 1 TC "chuẩn bị mốc đối chứng" chạy TRƯỚC toàn bộ bộ TC (deploy `release_step_20260827` lên local, chạy 6 kịch bản gốc, **export bảng slot ra file JSON/ảnh và đính kèm làm evidence dùng chung**), rồi mọi TC tham chiếu file đó; hoặc **(B)** đổi tiêu chí từ "so nhánh cũ" sang **giá trị tuyệt đối tính tay** (ô nào bị chặn, ô nào mở — đã có sẵn trong `Dữ liệu test` của từng TC). Khuyến nghị **(B)** cho TC chức năng, **(A)** cho TC hiệu năng.

**[BLOCKER] FIX-SHAPE + [AP-6] — Dev chưa cung cấp danh sách nơi ảnh hưởng**: `03-dev-impact.md` mục 3 và 4.1/4.2/4.3 **trống hoàn toàn**. `REG-SHARED-001` (Cao) bắt buộc *"yêu cầu dev cung cấp danh sách nơi ảnh hưởng → test lại từng mục"*. Danh sách 4 call site đang dùng là do **AI đọc source suy ra**, Dev chưa xác nhận, và **có thể còn caller thứ 5** cùng mẫu N+1 (ví dụ job monitor phát hiện booking chèn vào block time Google — kho TC-SLN-467). — *Đề xuất fix*: chặn merge tới khi Dev điền mục 3 + 4.1/4.2/4.3; sau đó lập lại §3 coverage matrix theo `F*/D*/T*` thật.

**[BLOCKER] GAP-1 `CONC-001` — không có TC đặt trùng đồng thời**: NEW-17 được gắn nhãn `CONC-001` nhưng kịch bản là **tuần tự** (mở màn → *rồi* dựng block Google → *rồi* gửi đơn), không phải 2 tiến trình chạy song song. **0/4 kịch bản** bắt buộc của `CONC-001`. Đây là rủi ro cao nhất của bản vá: bản vá thay đổi **chính tập dữ liệu quyết định slot còn trống**, mà kho đã có Bug KH **#38280** *"2 user đặt cùng slot cuối cùng gần như đồng thời"*. — *Đề xuất fix*: xem §5 TC-CONC001-01 → -07 (**-03/-04/-05** phủ nhánh **staff limit = 1**; **-06/-07** phủ **race với job sync Google** và **race với thao tác admin trên web**; **-08 → -11** và **TC-PAYSTATE001-01 → -04** lặp lại 4 kịch bản race đó trên 2 cấu hình **bill tiền tại chỗ** và **thanh toán trước**) + dùng lại kho TC-SLN-460.

**[BLOCKER] GAP-2 `DATA-COUNT-001` — không có TC nào cho `受付上限`**: spec §2.4.4 (`calendar_salon_setting_limit_booking.limited_quantity` + `setting_staffs_limit` JSON) và kho **TC-SLN-284** ghi rõ luật: *"Block time từ Google **KHÔNG** được tính vào limit **nhưng vẫn chặn slot**"* — và đây từng là bug thật (**#26908**, TC-SLN-288). Bản vá đổi đúng cái tập bản ghi Google nuôi luồng chặn slot ⇒ nếu tập gộp bị dùng nhầm cho bộ đếm limit, `受付上限` sai ngay. **0/30 TC** chạm tới `受付上限`. — *Đề xuất fix*: xem §5 TC-DATACOUNT001-01/-02 + dùng lại TC-SLN-284, TC-SLN-288.

**[BLOCKER] GAP-3 `ENV-001` — không có TC fail-safe khi sự cố hạ tầng**: Trigger của `ENV-001` nêu **đích danh** *"chức năng có bước validate quan trọng (**slot**, tồn kho, tiền) chạy qua nhiều server"*; Catalog `ENV-LB` ghi production có **2 server**. Prefetch gom toàn bộ dữ liệu chặn slot vào 1 lần đọc đầu request ⇒ nếu request rớt giữa chừng / server bị ngắt, bước kiểm slot phải **chặn an toàn**, không được bỏ qua. 0 TC. — *Đề xuất fix*: xem §5 TC-ENV001-01 (chạy `product`).

### 4.2 Major (nên fix)

**[MAJOR] NGUỒN-0.6#5 — 30/30 TC do AI sinh, `reviewState = tester`, `reviewed = false`**: chưa có người nào đọc lại. Kết hợp với `spec_status = null` ở cả 30 TC ⇒ không TC nào được đánh giá là `Spec ghi rõ` / `Spec không ghi` / `Đã hỏi leader`. — *Đề xuất fix*: member đọc lại 30 TC, điền `spec_status` cho từng TC (đối chiếu FA-020 BR-09/BR-10, §2.4.3, §2.4.4).

**[MAJOR] RULE-08 / `ENV-003` — 0 TC chạy production**: task chạm **job nền** (2 job) + **loadbalance** + **hiệu năng ở quy mô thật**. Catalog `ENV-JOB`: production tách **3 job độc lập**, dev/staging chạy 1 job all ⇒ tranh chấp giữa job chỉ lộ khi tách tiến trình. — *Đề xuất fix*: nâng `env_scope` của NEW-26, NEW-27, NEW-28, NEW-29 lên có `prd`; thêm TC-ENV003-01 (§5).

**[MAJOR] FIX-SHAPE NEW-26 / NEW-6 — quy mô test nhỏ hơn quy mô gây lỗi nêu trong ticket**: mục 1 nói *"30 staff × 7 ngày ≈ 210 query"*; NEW-6 dùng **8** nhân viên, NEW-26 dùng **10**, NEW-20 dùng **5**. `PERF-LARGE-001` yêu cầu *"hỏi/tra số liệu khách hàng lớn nhất hiện tại → tạo test data bằng hoặc lớn hơn"*. — *Đề xuất fix*: **sửa NEW-26 tại chỗ** (không đẻ TC mới): nâng tiền đề lên **≥ 30 nhân viên × 2 ca/ngày × 4 tuần**, và hỏi Dev/CS số nhân viên của khách lớn nhất thực tế.

**[MAJOR] NEW-6, NEW-12, NEW-18, NEW-26 — `Kết quả mong đợi` không đo lường được**: dùng *"giảm rõ rệt"*, *"nhanh hơn rõ rệt"*, *"thời gian chấp nhận được"* — không có ngưỡng số. Với **ticket hiệu năng** thì đây chính là tiêu chí nghiệm thu, không được để mơ hồ. — *Đề xuất fix*: chốt ngưỡng cụ thể với Dev/PO, ví dụ *"thời gian trung vị dựng bảng giờ ≤ 2s"* và *"số truy vấn đọc `calendar_salon_booking_by_google` ≤ 2 mỗi kỳ, không tăng theo số ca"*, rồi ghi thẳng số vào expected.

**[MAJOR] NEW-29 — expected cho phép TC không bao giờ "Không đạt"**: câu *"Nếu bảng vẫn KHÔNG có chỉ mục ... ghi kết quả là 'ngoài phạm vi bản vá' ... chuyển cho leader"* làm TC mất tiêu chí Đạt/Không đạt. Đây lại đúng là **tầng 3** của root cause (full table scan + `whereDate()` bọc cột). — *Đề xuất fix*: tách 2 TC — (1) *"truy vấn không còn bọc hàm lên cột `date`"* (kiểm được ngay từ query log, độc lập với việc có index hay không) → Đạt/Không đạt rõ ràng; (2) *"bảng có index phục vụ lọc theo `calendar_salon_id` + `date`"* → chỉ tick khi Dev xác nhận tầng 3 nằm trong phạm vi bản vá (§6 Q2).

**[MAJOR] NEW-11 — expected không quyết định được kết quả**: *"Nếu kết quả KHÁC mốc đối chứng ... báo cho leader/PO quyết định thay vì tự kết luận đạt hay không đạt"*. Nguyên nhân gốc là **spec chưa chốt** hành vi đúng cho ngày nhiều ca (REQ-004: ca sau ghi đè ca trước). — *Đề xuất fix*: đẩy lên §6 để chốt spec **trước**, rồi viết lại expected thành giá trị tuyệt đối.

**[MAJOR] `INTG-CAL-001` chỉ 1 TC / 1 chiều**: quan điểm Cao yêu cầu **3 hướng** (LME→Google, Google→LME, thao tác gần đồng thời 2 phía); NEW-2 chỉ có hướng Google→LME. checklist ghi rõ *"10 ticket Closed — điểm yếu lặp lại nhiều nhất của Salon"*. — *Đề xuất fix*: bổ sung theo §5 TC-INTGCAL001-01 và dùng lại kho TC-SLN-363, TC-SLN-366, TC-SLN-367.

**[MAJOR] `FUNC-DATE-001` — 4 TC nhưng đều là Boundary**: thiếu Normal + Abnormal, **không ghi lý do** (vi phạm RULE-01 cho quan điểm Cao). Catalog `DI-09` còn thiếu biên **`00:00` vs `24:00`** (kho TC-SLN-197 cho thấy đây là bug thật của salon) và `23:59 → 00:01`; `DI-10` thiếu **xuyên năm** ở tab 月 (NEW-13 có nhưng chỉ ở tab 週 và còn kèm điều kiện *"nếu môi trường cho phép"* — tức có thể bị bỏ qua).

**[MAJOR] `JOB-001` — NEW-27/NEW-28 chỉ happy path 1 đơn**: thiếu Kiểm tra (4) *"số bản ghi vào = số xử lý thành công + số vào hàng đợi lỗi"* và (5) *"job đã có retry sẵn (sync Google của Salon) → verify lại sau mỗi lần tính năng thay đổi"*. — *Đề xuất fix*: §5 TC-JOB001-01 (chạy `product`).

**[MAJOR] GAP-4 — Google event vắt qua nửa đêm**: kho TC-SLN-363 (Bug **#26739**) ghi *"event QUA NGÀY từ Google được tách thành 2 block time"*. Bản vá **group theo ngày** ⇒ đây chính xác là chỗ 1 trong 2 mảnh có thể rơi khỏi nhóm. NEW-9/NEW-10 test **ca làm** qua nửa đêm với block Google nằm gọn trong 1 ngày — **khác hẳn**. — *Đề xuất fix*: §5 TC-FUNCDATE001-01.

**[MAJOR] GAP-5 — 2 nhân viên liên kết CHUNG 1 Google Calendar**: spec BR-09 (per-staff OAuth) + kho TC-SLN-366/367 (Bug **#26653**). Truy vấn gộp gom bản ghi rồi phân phát lại theo ngày — nếu 2 nhân viên chung 1 calendar, cùng một `event_id_google_calendar` sinh ra bản ghi cho cả 2 `staff_id`; gộp/nhóm sai sẽ chặn nhầm hoặc mở nhầm. NEW-4/NEW-8 chỉ test 2 nhân viên có block **riêng biệt**. — *Đề xuất fix*: §5 TC-SECISO001-01.

**[MAJOR] GAP-6 — 所要時間 lẻ + 13 mức đơn vị nhận booking**: toàn bộ 30 TC dùng **khoá học 60 phút** và đơn vị 30 phút. Kho TC-SLN-433 (13 mức đơn vị) và TC-SLN-441 (Bug **#31160**: course 所要時間 lẻ → lịch hiển thị sai) cho thấy đây là vùng đã từng vỡ. Bản vá đổi cách dựng khoảng ⇒ phải test lại ít nhất 1 mức lẻ. — *Đề xuất fix*: §5 TC-OUTTRUTH001-01.

**[MAJOR] GAP-7 `REG-RUN-001` + `DEPLOY-LIVE-001`**: không TC nào kiểm (a) release khi job sync Google / job gán nhân viên **đang chạy dở**, (b) **client cũ gọi server mới** (khách đã mở màn chọn giờ trước release, bấm gửi sau release). Kho đã có TC-SLN-494 và TC-SLN-488 → dùng lại, không viết mới.

**[MAJOR] `LIFF-ENTRY-001` / RULE-06 — 0 TC vào bằng app LINE**: cả 30 TC mở `/mobile/calendar-salon/<mã lịch>` bằng **trình duyệt desktop**. Output cuối của tính năng này là **bảng giờ trong in-app browser của LINE trên điện thoại thật** (spec §2.6). Kết luận "nhanh hơn" đo trên desktop không đại diện. — *Đề xuất fix*: §5 TC-LIFFENTRY001-01 (1 TC, iOS + Android).

**[MAJOR] AP-4 — thiếu link Commit / Pull Request**: `03-dev-impact.md` ghi `<chưa có>` ⇒ không verify được bản vá thực sự chạm tầng nào, đặc biệt **tầng 2** (vòng `for i<=3`) và **tầng 3** (index + bỏ `whereDate`). — *Đề xuất fix*: yêu cầu Dev gắn PR link vào ticket.

**[MAJOR] Không có mốc nghiệp vụ cho "đã hết chậm"**: ticket là `Bug tự detect`, không có mô tả khách hàng, không Actual/Expected, 0 attachment. Toàn bộ ngưỡng hiệu năng đang do TC tự đặt. — *Đề xuất fix*: lấy số đo thực tế trên production của 1 lịch salon lớn (thời gian dựng bảng giờ hiện tại) làm mốc "trước", trước khi deploy.

### 4.3 Minor (có thể fix sau)

**[MINOR] NEW-28 phụ thuộc NEW-27**: tiền đề ghi *"Cùng lịch salon và cùng dữ liệu như ca trên"* → không chạy độc lập được. — *Đề xuất fix*: viết đủ tiền đề trong NEW-28.

**[MINOR] NEW-12, NEW-18 không atomic**: trộn khẳng định chức năng (*"tự nhảy tới tuần/tháng có ca làm"*) với khẳng định hiệu năng (*"nhanh hơn nhánh cũ"*) trong cùng 1 expected → fail một nửa thì không biết ghi Đạt hay Không đạt. — *Đề xuất fix*: tách phần hiệu năng sang NEW-6/NEW-26.

**[MINOR] RULE-02 — không TC nào ghi loại evidence bắt buộc**: `Ghi chú` của 30 TC không nêu loại bằng chứng phải đính kèm. Theo checklist: `FUNC-DATE-001` cần *bảng kết quả test các giá trị biên*; `PERF-LARGE-001` cần *số liệu quy mô test data + thời gian chạy*; `DATA-DB-001` cần *ảnh chụp kết quả query kèm câu query*; `JOB-001` cần *log job thấy retry + backoff*.

**[MINOR] `TC No.` không theo quy ước repo**: Studio dùng `temp_id` dạng `NEW-1`…`NEW-30`. Quy ước là `TC-<mã quan điểm bỏ gạch>-<nn>`. Snapshot `04-tc-list.md` đã sinh sẵn ID đúng chuẩn (`TC-OUTTRUTH001-01`…) và giữ `NEW-x` ở `Ghi chú` để trace ngược — không cần sửa trên Studio.

**[MINOR] `FUNC-SEQ-001` thiếu bước F5**: NEW-5/NEW-11/NEW-15 chuyển tuần/tháng qua lại nhưng không có bước **F5 sau chuỗi thao tác** rồi so lại (Kiểm tra bắt buộc của quan điểm).

### 4.4 Nit (gợi ý)

**[NIT] NEW-25, NEW-24 là ORPHAN nhẹ (AP-5)** — không nằm trên đường code bị sửa. **Giữ lại**, chỉ đề nghị ghi rõ *"đối chứng âm — endpoint không đi qua engine tính slot"* ở `Ghi chú` để không bị đếm nhầm là coverage chức năng.

**[NIT] 6/30 TC dùng mã quan điểm nội bộ Studio** (`TOOL-KNOW-002`, `TOOL-NEGCTRL-001`, `TOOL-AXIS-001`, `API-CONTRACT-001`, `TOOL-ERRHYG-001`) và 1 TC dùng `RULE-07` như mã quan điểm. Gợi ý map lại khi cập nhật trên Studio: `TOOL-KNOW-002` + `TOOL-AXIS-001` → `PERF-LARGE-001`; `API-CONTRACT-001` → `DEPLOY-LIVE-001` hoặc `REG-SHARED-001`; `TOOL-NEGCTRL-001` → `REG-SHARED-001`; `TOOL-ERRHYG-001` → `FUNC-003`; `RULE-07` → `OUT-TRUTH-001`.

**[NIT] `期間限定` (mùa vụ)** — kho TC-SLN-438 cho thấy setting này ảnh hưởng logic chọn tuần/tháng hiển thị, tức cùng vòng lặp dò tuần của tầng 2. Không bắt buộc vì bản vá không đổi setting, nhưng đáng chạy 1 lượt smoke.

---

## 4.5 TC trùng lặp nội dung

**Đã rà toàn bộ 30 TC** theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương` → `kết quả mong đợi` tương đương). Phát hiện **1 nhóm**:

| Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| Đo hiệu năng / đếm truy vấn của cùng một lời gọi dựng bảng giờ tuần | **NEW-26** (`PERF-LARGE-001`, Boundary, 10 nhân viên / 20.000 bản ghi, đo **cả** thời gian **và** số truy vấn) | **NEW-20** (`TOOL-KNOW-002`, Normal, 5 nhân viên / 70 ca, chỉ đếm truy vấn) → **GỘP, không xóa**<br>**NEW-6** (`TOOL-KNOW-002`, Normal, 8 nhân viên / 5.000 bản ghi, chỉ đo thời gian trên UI) → **giữ**, xem ghi chú | `DUP-SUBSET` (+ hệ quả `DUP-INFLATE`) | Cùng đối tượng đo (lời gọi lấy bảng giờ tuần, lần đầu) · cùng thao tác (chạy nhánh cũ → chạy nhánh vá → so) · tiền đề tương đương (lịch nhiều nhân viên, nhiều ca, dữ liệu Google có sẵn) · expected tương đương (*"số truy vấn giảm từ tỉ lệ theo số ca xuống hằng số"*). Tập đo của NEW-20 ⊂ tập đo của NEW-26 | `[MINOR]` cho phần trùng · `[MAJOR]` cho phần **INFLATE** |

**Vì sao là `GỘP` chứ không `XÓA`** — chạy gate bắt buộc: giả định xóa NEW-20 → chạy lại §3 + §3.6 trên 29 TC còn lại ⇒ `BUG-2` mất TC duy nhất định lượng được *"nhân thêm do vòng dò nhiều tuần"* ở quy mô kiểm soát được (70 ca, đếm chính xác). NEW-26 ở quy mô 20.000 bản ghi khó tách bạch phần tăng do vòng lặp. ⇒ **đổi đề xuất sang gộp**: giữ NEW-20 làm phép đo cơ sở, bổ sung vào `Ghi chú` rằng nó là bước 1 của cùng chuỗi đo với NEW-26.

**Hệ quả `DUP-INFLATE` (đây mới là phần quan trọng)**: 3 TC (NEW-6, NEW-20, NEW-26) khiến vùng hiệu năng **trông như được phủ dày**. Nhưng NEW-6 và NEW-20 mang mã `TOOL-KNOW-002` — **không có trong `checklist-lme.md`**, nên theo mục 0.6#6 **không được tính là cover**. ⇒ `PERF-LARGE-001` thực chất chỉ có **1 TC (NEW-26, Boundary)**, vi phạm RULE-01 cho quan điểm đã nâng lên Cao. Đã hạ `PERF-LARGE-001` xuống `[MAJOR]` ở §3.6.

**Không trùng dù trông giống** (đã kiểm, ghi lại để khỏi rà lại vòng sau): NEW-5 vs NEW-15 (tuần vs tháng — khác tab) · NEW-9 vs NEW-10 (qua nửa đêm về sau vs về trước — 2 đầu biên đối xứng, cần cả hai) · NEW-13 vs NEW-19 (biên tháng ở tab 週 vs tab 月) · NEW-21 vs NEW-22 (2 endpoint khác nhau) · NEW-1 vs NEW-2 vs NEW-3 (bao quát vs cơ chế chặn vs đệm trước/sau) · NEW-7 vs NEW-25 (2 đối chứng âm ở 2 tầng khác nhau).

---

## 5. TCs đề xuất bổ sung

> **Đã đối chiếu 30 TC ở BƯỚC 0 + `kho-tcs/fa020-datlichsalon-サロン・面談予約.md` (498 TC) — không TC đề xuất nào trùng.**
> Format **14 cột** (12 cột kho + `Chạy` + `Phạm vi ENV`). `Kết quả thực thi` để trống — người test tự điền.

### 5a — GAP dùng lại TC có sẵn trong kho (KHÔNG viết TC mới)

| GAP | Dùng lại | Tên case trong kho | Cần chỉnh gì cho bug này |
|---|---|---|---|
| GAP-1 (một phần) | **TC-SLN-460** | Bug KH #38280: 2 user đặt cùng slot cuối cùng gần như đồng thời | Thêm điều kiện: slot cuối bị **khung bận Google** kề sát (để đi qua đúng nhánh vừa sửa) |
| GAP-2 | **TC-SLN-284** | Block time từ Google KHÔNG được tính vào limit (nhưng chặn slot) | Chạy lại nguyên trạng làm **regression** sau bản vá — không cần sửa |
| GAP-2 | **TC-SLN-288** | Bug #26908: block time Google được coi như block time thường (không báo lý do "đã limit") | Chạy lại nguyên trạng làm regression |
| GAP-4 (nền) | **TC-SLN-363** | Bug #26739: event QUA NGÀY từ Google được tách thành 2 block time | Kho test ở **thời điểm sync**; bug này cần thêm ở **thời điểm dựng slot** → xem TC-FUNCDATE001-01 bên dưới |
| GAP-5 (nền) | **TC-SLN-366**, **TC-SLN-367** | Nhiều staff liên kết cùng/khác tài khoản Google · Bug #26653: 2 staff chung 1 Google Calendar | Kho test ở màn liên kết; cần thêm ở màn chọn giờ → xem TC-SECISO001-01 |
| GAP-6 (nền) | **TC-SLN-433**, **TC-SLN-441** | 13 mức đơn vị nhận booking · Bug #31160: course 所要時間 lẻ → lịch hiển thị sai | Chạy lại làm regression + xem TC-OUTTRUTH001-01 |
| GAP-7 | **TC-SLN-494** | Release trong lúc job remind / sync Google đang chạy dở | Đổi job theo dõi thành **job tự gán nhân viên** + job sync Google |
| GAP-7 | **TC-SLN-488** | UI load TRƯỚC release, submit SAU release | Áp cho màn **chọn ngày giờ** của trang đặt lịch |
| Regression chung | **TC-SLN-442**, **TC-SLN-439** | Bug KH #33013: xem theo tuần thấy ngày đặt được nhưng thực tế không · Bug KH #33003: giờ bắt đầu tuần sau bị lấy nhầm theo tuần trước | Hai bug cũ **đúng dạng lỗi mà bản vá này có thể tái tạo** (nhóm sai khoá ngày) — bắt buộc chạy lại |
| Multi-tenant | **TC-SLN-213** | Multi-tenant: thao tác ở bot A không ảnh hưởng bot B | Áp cho **truy vấn gộp** thay vì cho sửa ca |

### 5b — TC viết mới

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-CONC001-01 | API | CONC-001 | LINE user — xác nhận và gửi đơn đặt lịch | Normal | manual | product | Double-click nút gửi đơn ở slot bị Google chiếm kề bên — chỉ tạo đúng 1 booking | - Lịch salon 1 nhân viên, ca 09:00-18:00 ngày D<br>- Khung bận Google ngày D 11:00-12:00<br>- Slot 10:00 ngày D còn trống<br>- Chế độ duyệt tự động, không bật thanh toán | 1. Mở trang đặt lịch của khách, chọn khoá học rồi chọn slot 10:00 ngày D<br>2. Điền form thông tin bắt buộc<br>3. Ở màn xác nhận, **bấm nút gửi đơn 2 lần liên tiếp trong vòng dưới 1 giây**<br>4. Mở lại bảng chọn giờ ngày D<br>5. Mở màn quản lý booking của admin, đếm số đơn ngày D khung 10:00 | Slot đặt: ngày D 10:00-11:00; khung bận Google kề bên: 11:00-12:00 | Chỉ **đúng 1** đơn đặt lịch được tạo cho ngày D khung 10:00 (đếm ở màn quản lý admin và ở DB). Lần bấm thứ 2 bị chặn hoặc báo lỗi rõ ràng, **không** tạo đơn thứ 2, **không** trừ 2 lần vào 受付上限. Bảng chọn giờ sau đó hiện 10:00 không chọn được, còn 11:00-12:00 vẫn bị chặn như trước | | Lấp GAP-1 / cover BUG-1 · Đánh giá spec: Spec không ghi (đã hỏi Dev — chờ trả lời §6 Q5) · Evidence: ảnh chụp màn quản lý + kết quả query đếm đơn + log request 2 lần bấm · Kịch bản (1) của CONC-001 |
| TC-CONC001-02 | API | CONC-001 | LINE user — xác nhận và gửi đơn đặt lịch | Boundary | manual | product | Slot cuối cùng theo 受付上限 bị 2 tiến trình chạm đồng thời — tổng số đơn không vượt limit | - Lịch salon bật 受付上限 với `limited_quantity = 2` cho khung 14:00 ngày D<br>- Đã có sẵn 1 đơn ở khung 14:00 ngày D (còn đúng 1 chỗ)<br>- Có khung bận Google của **một nhân viên khác** cùng khung 14:00 (để đi qua nhánh gộp truy vấn)<br>- 2 tài khoản LINE khách khác nhau, mở sẵn 2 thiết bị | 1. Trên cả 2 thiết bị, đi tới màn xác nhận cho khung 14:00 ngày D, **chưa** bấm gửi<br>2. Bấm gửi trên 2 thiết bị **gần như cùng lúc** (trong vòng 1 giây)<br>3. Ghi lại màn hình kết quả của từng thiết bị<br>4. Đếm số đơn khung 14:00 ngày D ở màn quản lý và ở DB<br>5. Mở lại bảng chọn giờ, kiểm trạng thái ô 14:00 | Limit khung 14:00 = 2; đã dùng 1; 2 khách gửi đồng thời. Phép tính tay: 1 + 1 = 2 ⇒ **đúng 1** trong 2 khách được nhận | Tổng số đơn khung 14:00 ngày D = **đúng 2**, không phải 3. Một khách thành công, khách còn lại nhận thông báo khung giờ đã đầy và **không** có đơn nào được tạo cho khách đó. Sau đó ô 14:00 chuyển sang không chọn được. Khung bận Google của nhân viên khác vẫn chặn đúng như trước bản vá | | Lấp GAP-1 + GAP-2 / cover BUG-1, FIX-1 · Đánh giá spec: Spec ghi rõ (FA-020 §2.4.4) · Evidence: 2 ảnh màn kết quả + kết quả query đếm đơn · Kịch bản (4) của CONC-001 |
| TC-CONC001-03 | API | CONC-001 | LINE user — xác nhận và gửi đơn đặt lịch | Abnormal | manual | product | TẮT 決済 + staff limit = 1 — 2 khách cùng chọn đích danh nhân viên S1 rồi gửi đồng thời, chỉ 1 đơn được tạo | - Calendar **TẮT** chức năng thanh toán — 「決済機能の利用」 = không sử dụng (`is_use_payment = 0`)<br>- Lịch salon loại スタッフ, có nhân viên S1 và S2, khoá học C1<br>- Tab 「予約設定」 → 「スタッフごとの同時受付上限」: **S1 = 1**, S2 = 1<br>- 受付上限 của cửa hàng để **không giới hạn** (để chốt chặn duy nhất là staff limit)<br>- S1 và S2 đều có ca 09:00-18:00 ngày D<br>- Khung 10:00 ngày D chưa có đơn nào<br>- Có khung bận Google của S2 ngày D 10:00-11:00 (để request đi qua đúng nhánh gộp truy vấn vừa sửa)<br>- 2 tài khoản LINE khách U1, U2 đã kết bạn, mở sẵn trên 2 thiết bị | 1. Trên cả 2 thiết bị: mở trang đặt lịch, chọn khoá học C1, chọn **đích danh nhân viên S1**, chọn ô 10:00 ngày D, điền form, dừng ở màn xác nhận (**chưa** bấm gửi)<br>2. Bấm gửi trên 2 thiết bị **gần như cùng lúc** (trong vòng 1 giây)<br>3. Ghi lại màn hình kết quả của từng thiết bị<br>4. Mở màn quản lý booking của admin, đếm số đơn ngày D khung 10:00 của S1<br>5. Đếm lại trong DB số bản ghi đặt lịch ngày D khung 10:00 gắn S1<br>6. Mở lại bảng chọn giờ với nhân viên S1 và kiểm trạng thái ô 10:00 | S1 同時受付上限 = 1; 受付上限 cửa hàng = không giới hạn; khung 10:00 ngày D đang trống; U1 và U2 cùng gửi. Phép tính tay: 1 chỗ − 2 yêu cầu ⇒ **đúng 1** thành công, 1 bị từ chối | **Đúng 1** trong 2 khách đặt thành công. Khách còn lại nhận **thông báo lỗi rõ ràng** (khung giờ đã hết chỗ / đã đủ số nhận của nhân viên) và **KHÔNG** thấy màn thành công giả. Trong DB có **đúng 1** bản ghi đặt lịch ngày D khung 10:00 gắn S1 — không sinh bản ghi vượt limit. Sau đó ô 10:00 của S1 chuyển sang không chọn được, còn khung bận Google của S2 vẫn chặn đúng như trước bản vá |  | Lấp GAP-1 nhánh **staff limit** / cover BUG-1, FIX-1 · Đánh giá spec: Spec ghi rõ (FA-020 §2.4.4 + kho TC-SLN-281) · Evidence: 2 ảnh màn kết quả + kết quả query đếm bản ghi + log 2 request gửi đồng thời · Kịch bản (4) của CONC-001 · Khác TC-CONC001-02 ở chỗ chốt chặn nằm ở **staff limit**, không phải limit cửa hàng (2 nhánh tính riêng — xem TC-SLN-281) · dẫn từ TC-SLN-460, TC-SLN-281 |
| TC-CONC001-04 | API | CONC-001 | Job tự động gán nhân viên cho booking salon | Abnormal | manual | product | TẮT 決済 + staff limit = 1 — 2 khách cùng chọn 指定なし khi chỉ còn S1 khả dụng, job tự gán không gán vượt limit | - Calendar **TẮT** chức năng thanh toán — 「決済機能の利用」 = không sử dụng (`is_use_payment = 0`)<br>- Lịch salon loại スタッフ, nhân viên S1 và S2, khoá học C1, **bật tự gán nhân viên**<br>- 「スタッフごとの同時受付上限」: **S1 = 1**, S2 = 1; 受付上限 cửa hàng = không giới hạn<br>- Ngày D: S1 có ca 09:00-18:00; **S2 bị khung bận Google 10:00-11:00** ⇒ tại khung 10:00 chỉ còn S1 khả dụng<br>- Khung 10:00 ngày D chưa có đơn nào<br>- Tiến trình xử lý hàng đợi đang chạy<br>- 2 tài khoản LINE khách U1, U2 mở sẵn trên 2 thiết bị | 1. Trên cả 2 thiết bị: chọn khoá học C1, chọn 「指定なし」, chọn ô 10:00 ngày D, điền form, dừng ở màn xác nhận<br>2. Bấm gửi trên 2 thiết bị **gần như cùng lúc** (trong vòng 1 giây)<br>3. Ghi lại màn hình kết quả của từng thiết bị<br>4. Chờ hàng đợi xử lý xong công việc tự gán nhân viên<br>5. Đọc các bản ghi đặt lịch ngày D khung 10:00 và xem nhân viên được gán là ai<br>6. Đọc nhật ký của công việc nền tự gán nhân viên | S1 同時受付上限 = 1; S2 bị khung bận Google 10:00-11:00 ⇒ khung 10:00 chỉ còn S1 khả dụng. Phép tính tay: 1 nhân viên khả dụng × limit 1 = **1 chỗ**, 2 yêu cầu ⇒ 1 thành công | **Đúng 1** đơn được tạo và được gán cho **S1**. Khách còn lại nhận thông báo lỗi rõ ràng (không còn nhân viên nhận được khung giờ này), **KHÔNG** có màn thành công giả và **KHÔNG** để lại đơn treo ở trạng thái chưa gán nhân viên. Job chạy xong không lỗi; nhật ký không có lần gán thứ 2 cho S1 ở cùng khung. **S2 tuyệt đối không được gán** vì đang bận theo lịch Google |  | Lấp GAP-1 nhánh **指定なし + job tự gán** / cover BUG-1, FIX-1 vùng job · Đánh giá spec: Spec không ghi (đã hỏi Dev — §6 Q6) · Evidence: 2 ảnh màn kết quả + bản ghi đơn kèm nhân viên được gán + log job · Kịch bản (4) của CONC-001 · Đường đi qua `CalendarSalonStaffAssignment` — 1 trong 2 job gọi hàm bị sửa · dẫn từ TC-SLN-460 |
| TC-CONC001-05 | API | CONC-001 | LINE user — xác nhận và gửi đơn đặt lịch | Normal | manual | staging | Chống over-fix — 2 khách chọn 2 nhân viên khác nhau, mỗi nhân viên limit 1, cả hai đều phải đặt được | - Lịch salon loại スタッフ, nhân viên S1 và S2, khoá học C1<br>- 「スタッフごとの同時受付上限」: S1 = 1, S2 = 1; 受付上限 cửa hàng = không giới hạn<br>- Cả S1 và S2 đều có ca 09:00-18:00 ngày D<br>- Khung 10:00 ngày D chưa có đơn nào<br>- Có thêm nhân viên S3 bị khung bận Google ngày D 10:00-11:00 (giữ request trên nhánh gộp truy vấn)<br>- 2 tài khoản LINE khách U1, U2 mở sẵn trên 2 thiết bị | 1. U1 chọn **đích danh S1** ô 10:00 ngày D; U2 chọn **đích danh S2** cùng ô 10:00; cả hai dừng ở màn xác nhận<br>2. Bấm gửi trên 2 thiết bị **gần như cùng lúc** (trong vòng 1 giây)<br>3. Ghi lại màn hình kết quả của từng thiết bị<br>4. Đếm số đơn ngày D khung 10:00 theo từng nhân viên ở màn quản lý và ở DB<br>5. Mở lại bảng chọn giờ lần lượt với S1, S2 và 「指定なし」 | S1 limit 1, S2 limit 1; 2 khách chọn **2 nhân viên khác nhau** cùng khung 10:00. Phép tính tay: mỗi nhân viên còn 1 chỗ ⇒ **cả 2 phải thành công** | **CẢ HAI** khách đều đặt thành công. DB có **đúng 2** bản ghi ngày D khung 10:00 — 1 gắn S1, 1 gắn S2 — không đơn nào bị từ chối oan. Sau đó ô 10:00 của cả S1 và S2 đều chuyển sang không chọn được. Khoá chống đặt trùng **không được siết quá tay** thành khoá cả khung giờ của lịch |  | Đối chứng dương cho TC-CONC001-03 / -04 — **chống over-fix** · Đánh giá spec: Spec ghi rõ (FA-020 §2.4.4 + kho TC-SLN-281 bước 1) · Evidence: 2 ảnh màn kết quả + kết quả query 2 bản ghi · Lấp phần **Normal** của RULE-01 cho CONC-001 |
| TC-CONC001-06 | API | CONC-001 | LINE user — xác nhận và gửi đơn đặt lịch | Abnormal | manual | product | TẮT 決済 — lịch Google được sync về đúng lúc khách đang ở màn nhập form, submit phải bị từ chối, không dùng lại dữ liệu đã prefetch | - Calendar **TẮT** chức năng thanh toán — 「決済機能の利用」 = không sử dụng (`is_use_payment = 0`)<br>- Lịch salon 1 nhân viên S1 **đã liên kết Google Calendar thật** (BR-09), luồng sync đang hoạt động<br>- Ca 09:00-18:00 ngày D; khung 11:00 ngày D còn trống, chưa có khung bận Google nào chồng lên<br>- Có quyền đọc bảng `salon_google_calendar_callback` để theo dõi trạng thái bản ghi webhook<br>- Tiến trình job đồng bộ đang chạy<br>- 1 tài khoản LINE khách U1 đã kết bạn, mở sẵn trên thiết bị | 1. U1 mở trang đặt lịch, tới bước chọn ngày giờ ngày D, xác nhận ô 11:00 **đang chọn được**<br>2. U1 chọn ô 11:00, bấm tiếp sang bước nhập thông tin, điền đủ form, **dừng lại chưa bấm gửi** — giữ nguyên màn, **không reload**<br>3. Trên **Google Calendar thật** của S1, tạo event 11:00-12:00 ngày D<br>4. Theo dõi tới khi bản ghi trong `salon_google_calendar_callback` chuyển sang trạng thái xử lý xong **và** khung bận 11:00-12:00 ngày D đã xuất hiện trong dữ liệu<br>5. Quay lại thiết bị U1 (**không reload trang**) và bấm gửi đơn<br>6. Ghi lại thông báo hiển thị và đếm trong DB số bản ghi đặt lịch ngày D khung 11:00<br>7. Lặp lại bước 1-3, nhưng lần này bấm gửi **ngay khi bản ghi callback vừa xuất hiện và còn đang xử lý** — ghi lại kết quả thực tế kèm mốc thời gian | Event Google 11:00-12:00 ngày D tạo trên Google Calendar thật của S1 (đi qua webhook → callback → job), trong lúc U1 đã mở sẵn màn nhập form từ trước khi event tồn tại | **Bước 5-6 (tiêu chí Đạt/Không đạt):** đơn của U1 **bị từ chối** với thông báo 「指定の時間帯はブロックされています。予約時間を変更してください。」, và trong DB **không có** bản ghi đặt lịch ngày D khung 11:00. Điều này chứng minh bước kiểm slot lúc gửi đơn **đọc lại dữ liệu mới**, chứ không dùng lại tập dữ liệu đã lấy trước (prefetch) lúc dựng bảng giờ.<br>**Bước 7 (thăm dò, không quyết Đạt/Không đạt):** ghi lại đơn được nhận hay bị từ chối. Nếu đơn được tạo đè lên khung bận thì phải có cơ chế phát hiện sau đó (job monitor booking chèn vào block time Google) — không được im lặng bỏ qua. Kết quả bước 7 đưa vào §6 để leader chốt hành vi đúng |  | Lấp GAP-1 nhánh **race với job sync Google** / cover BUG-1, FIX-1 · Đánh giá spec: Spec không ghi (đã hỏi Dev — §6 Q6) · Evidence: ảnh màn kết quả của U1 + kết quả query đếm bản ghi + ảnh trạng thái bản ghi `salon_google_calendar_callback` kèm mốc thời gian · Kịch bản (3) của CONC-001 · **Khác NEW-17**: NEW-17 ghi thẳng khung bận vào bảng theo cách tuần tự, TC này đi qua **đường sync thật** (webhook → callback → job) và bấm gửi khi màn user vẫn giữ dữ liệu cũ · Catalog `ENV-HOOK` (dev/staging có thể không nhận được webhook) → **bắt buộc product** · dẫn từ TC-SLN-376, TC-SLN-469 |
| TC-CONC001-07 | API | CONC-001 | LINE user — xác nhận và gửi đơn đặt lịch | Abnormal | manual | product | TẮT 決済 — admin tạo booking chiếm nốt slot trên web đúng lúc khách đang ở màn nhập form, submit của khách phải bị từ chối | - Calendar **TẮT** chức năng thanh toán — 「決済機能の利用」 = không sử dụng (`is_use_payment = 0`)<br>- Lịch salon loại スタッフ, nhân viên S1, khoá học C1<br>- 「スタッフごとの同時受付上限」: **S1 = 1** (1 đơn của admin là đủ chiếm hết slot)<br>- Ca 09:00-18:00 ngày D; khung 14:00 ngày D chưa có đơn nào<br>- Có khung bận Google của một nhân viên khác trong ngày D (giữ request trên nhánh gộp truy vấn vừa sửa)<br>- 1 tài khoản LINE khách U1 mở sẵn trên thiết bị<br>- 1 phiên admin đăng nhập trang quản trị trên máy khác | 1. U1 mở trang đặt lịch, chọn khoá học C1, chọn **đích danh S1**, chọn ô 14:00 ngày D, bấm tiếp sang bước nhập thông tin, điền đủ form, **dừng chưa gửi** — **không reload**<br>2. Trên trang quản trị, admin mở lưới quản lý ngày D và **tạo 1 booking mới cho S1 vào đúng khung 14:00**<br>3. Xác nhận booking của admin đã lưu và hiện trên lưới<br>4. Quay lại thiết bị U1 (**không reload**) và bấm gửi đơn<br>5. Ghi lại thông báo hiển thị trên màn hình U1<br>6. Đếm trong DB số bản ghi đặt lịch ngày D khung 14:00 gắn S1<br>7. Mở lại bảng chọn giờ với S1 và kiểm trạng thái ô 14:00 | S1 同時受付上限 = 1; khung 14:00 ngày D ban đầu trống; admin tạo 1 booking chiếm khung đó trong lúc U1 đang nhập form. Phép tính tay: 1 chỗ − 1 đơn admin = 0 chỗ ⇒ đơn của U1 phải bị từ chối | Đơn của U1 **bị từ chối** với thông báo lỗi rõ ràng (khung giờ đã hết chỗ / đã đủ số nhận của nhân viên), **KHÔNG** hiện màn thành công giả. Trong DB có **ĐÚNG 1** bản ghi đặt lịch ngày D khung 14:00 gắn S1 — là booking do admin tạo; **không** có bản ghi thứ 2 của U1. Ô 14:00 của S1 sau đó hiện không chọn được. Không có lỗi máy chủ 500 và không có đơn lưu nửa vời |  | Lấp GAP-1 nhánh **race với thao tác admin trên web** / cover BUG-1, FIX-1 · Đánh giá spec: Spec ghi rõ (FA-020 §2.4.4 + kho TC-SLN-283) · Evidence: ảnh màn kết quả của U1 + ảnh lưới admin + kết quả query đếm bản ghi · Kịch bản (3) của CONC-001 · **Khác TC-SLN-462** (admin *xoá* booking → đơn vẫn thành công) và **khác TC-SLN-461** (admin đổi *cấu hình*, không phải tạo booking) · Production có 2 server (Catalog `ENV-LB`) → admin ghi ở server này, khách gửi ở server kia là đúng tình huống cần bắt → **product** · Thao tác admin *xoá ca làm* giữa chừng đã có ở kho TC-SLN-461, không lặp lại ở đây |
| TC-CONC001-08 | API | CONC-001 | LINE user — xác nhận và gửi đơn đặt lịch | Abnormal | manual | product | Bill tiền TẠI CHỖ + staff limit = 1 — 2 khách cùng chọn đích danh S1 rồi gửi đồng thời, chỉ 1 đơn được tạo | - Bot đã liên kết Stripe (hoặc UnivaPay); calendar **BẬT** 「決済機能の利用」 (`is_use_payment = 1`)<br>- 「予約時決済の選択」 = **選択可能** (`payment_time = 2`) → khách thấy 2 lựa chọn thanh toán<br>- Khoá học C1 có giá 5.000 円 để luồng thanh toán thực sự kích hoạt<br>- Form đặt lịch đã bật bắt buộc field tên và email (điều kiện để bật 決済)<br>- Khách chọn **現地決済 (thanh toán tại chỗ)** ở bước chọn phương thức<br>- 「スタッフごとの同時受付上限」: **S1 = 1**; 受付上限 cửa hàng = không giới hạn<br>- Ca 09:00-18:00 ngày D; khung 10:00 ngày D chưa có đơn nào<br>- Có khung bận Google của S2 ngày D 10:00-11:00 (giữ request trên nhánh gộp truy vấn vừa sửa)<br>- 2 tài khoản LINE khách U1, U2 mở sẵn trên 2 thiết bị | 1. Trên cả 2 thiết bị: chọn khoá học C1, chọn **đích danh S1**, chọn ô 10:00 ngày D, điền đủ form<br>2. Ở bước chọn phương thức thanh toán, cả 2 chọn **現地決済 (thanh toán tại chỗ)**, tới màn xác nhận, **dừng chưa gửi**<br>3. Bấm gửi trên 2 thiết bị **gần như cùng lúc** (trong vòng 1 giây)<br>4. Ghi lại màn hình kết quả của từng thiết bị<br>5. Đếm trong DB số bản ghi đặt lịch ngày D khung 10:00 gắn S1 và đọc trạng thái thanh toán của từng bản ghi<br>6. Mở màn quản lý booking của admin và kiểm trạng thái thanh toán hiển thị | S1 同時受付上限 = 1; C1 giá 5.000 円; 予約時決済の選択 = 選択可能; cả 2 khách chọn 現地決済. Phép tính tay: 1 chỗ − 2 yêu cầu ⇒ đúng 1 thành công | **Đúng 1** trong 2 khách đặt thành công; bản ghi đặt lịch mang trạng thái 「現地決済：未決済」 và số tiền đúng 5.000 円. Khách còn lại nhận thông báo lỗi rõ ràng, **KHÔNG** hiện màn thành công giả và **KHÔNG** sinh thêm bản ghi nào — kể cả bản ghi ở trạng thái 未決済. DB có **đúng 1** bản ghi ngày D khung 10:00 gắn S1. Màn quản lý admin hiển thị đúng trạng thái 現地決済：未決済 |  | Tách từ TC-CONC001-03 — biến thể **bill tiền TẠI CHỖ** · Đánh giá spec: Spec ghi rõ (FA-020 §2.5 + kho TC-SLN-417, TC-SLN-419) · Evidence: 2 ảnh màn kết quả + kết quả query bản ghi kèm trạng thái thanh toán + ảnh màn quản lý · Kịch bản (4) của CONC-001 · dẫn từ TC-SLN-419 |
| TC-CONC001-09 | API | CONC-001 | Job tự động gán nhân viên cho booking salon | Abnormal | manual | product | Bill tiền TẠI CHỖ + staff limit = 1 — 2 khách cùng chọn 指定なし khi chỉ còn S1 khả dụng, job tự gán không gán vượt limit | - Bot đã liên kết Stripe (hoặc UnivaPay); calendar **BẬT** 「決済機能の利用」 (`is_use_payment = 1`)<br>- 「予約時決済の選択」 = **選択可能** (`payment_time = 2`) → khách thấy 2 lựa chọn thanh toán<br>- Khoá học C1 có giá 5.000 円 để luồng thanh toán thực sự kích hoạt<br>- Form đặt lịch đã bật bắt buộc field tên và email (điều kiện để bật 決済)<br>- Khách chọn **現地決済 (thanh toán tại chỗ)** ở bước chọn phương thức<br>- 「スタッフごとの同時受付上限」: **S1 = 1**; bật tự gán nhân viên<br>- Ngày D: S1 có ca 09:00-18:00; **S2 bị khung bận Google 10:00-11:00** ⇒ khung 10:00 chỉ còn S1 khả dụng<br>- Khung 10:00 ngày D chưa có đơn nào; tiến trình hàng đợi đang chạy<br>- 2 tài khoản LINE khách U1, U2 mở sẵn trên 2 thiết bị | 1. Trên cả 2 thiết bị: chọn khoá học C1, chọn 「指定なし」, chọn ô 10:00 ngày D, điền đủ form<br>2. Cả 2 chọn **現地決済**, tới màn xác nhận, **dừng chưa gửi**<br>3. Bấm gửi trên 2 thiết bị **gần như cùng lúc**<br>4. Ghi lại màn hình kết quả của từng thiết bị<br>5. Chờ hàng đợi xử lý xong công việc tự gán nhân viên<br>6. Đọc các bản ghi đặt lịch ngày D khung 10:00: nhân viên được gán và trạng thái thanh toán<br>7. Đọc nhật ký công việc nền tự gán nhân viên | S1 同時受付上限 = 1; S2 bị Google chiếm 10:00-11:00; C1 giá 5.000 円; cả 2 khách chọn 現地決済. Phép tính tay: 1 nhân viên khả dụng × limit 1 = 1 chỗ | **Đúng 1** đơn được tạo, gán cho **S1**, trạng thái thanh toán 「現地決済：未決済」, số tiền 5.000 円. Khách còn lại nhận thông báo lỗi rõ ràng, **không** có đơn treo ở trạng thái chưa gán nhân viên. Job chạy xong không lỗi; **S2 không được gán** vì đang bận theo lịch Google. Việc job gán nhân viên **không làm đổi** trạng thái thanh toán của đơn |  | Tách từ TC-CONC001-04 — biến thể **bill tiền TẠI CHỖ** · Đánh giá spec: Spec ghi rõ (FA-020 §2.5 + kho TC-SLN-421) · Evidence: 2 ảnh màn kết quả + bản ghi đơn kèm nhân viên và trạng thái thanh toán + log job · Đường đi qua `CalendarSalonStaffAssignment` — 1 trong 2 job gọi hàm bị sửa · dẫn từ TC-SLN-421, TC-SLN-422 |
| TC-CONC001-10 | API | CONC-001 | LINE user — xác nhận và gửi đơn đặt lịch | Abnormal | manual | product | Bill tiền TẠI CHỖ — lịch Google sync về đúng lúc khách đang ở màn xác nhận, submit phải bị từ chối | - Bot đã liên kết Stripe (hoặc UnivaPay); calendar **BẬT** 「決済機能の利用」 (`is_use_payment = 1`)<br>- 「予約時決済の選択」 = **選択可能** (`payment_time = 2`) → khách thấy 2 lựa chọn thanh toán<br>- Khoá học C1 có giá 5.000 円 để luồng thanh toán thực sự kích hoạt<br>- Form đặt lịch đã bật bắt buộc field tên và email (điều kiện để bật 決済)<br>- Khách chọn **現地決済 (thanh toán tại chỗ)** ở bước chọn phương thức<br>- Nhân viên S1 **đã liên kết Google Calendar thật** (BR-09), luồng sync đang hoạt động<br>- Ca 09:00-18:00 ngày D; khung 11:00 ngày D còn trống, chưa có khung bận Google nào chồng lên<br>- Có quyền đọc bảng `salon_google_calendar_callback` để theo dõi trạng thái bản ghi webhook<br>- 1 tài khoản LINE khách U1 mở sẵn trên thiết bị | 1. U1 mở trang đặt lịch, tới bước chọn ngày giờ ngày D, xác nhận ô 11:00 **đang chọn được**<br>2. U1 chọn ô 11:00, điền form, chọn **現地決済**, tới màn xác nhận, **dừng chưa gửi** — không reload<br>3. Trên **Google Calendar thật** của S1, tạo event 11:00-12:00 ngày D<br>4. Chờ tới khi bản ghi trong `salon_google_calendar_callback` xử lý xong **và** khung bận đã xuất hiện trong dữ liệu<br>5. Quay lại thiết bị U1 (**không reload**) và bấm gửi đơn<br>6. Ghi lại thông báo hiển thị và đếm trong DB số bản ghi đặt lịch ngày D khung 11:00 | Event Google 11:00-12:00 ngày D tạo trên Google Calendar thật của S1; C1 giá 5.000 円; U1 chọn 現地決済 và giữ nguyên màn xác nhận từ trước khi event tồn tại | Đơn của U1 **bị từ chối** với thông báo 「指定の時間帯はブロックされています。予約時間を変更してください。」; trong DB **không có** bản ghi đặt lịch ngày D khung 11:00, kể cả bản ghi ở trạng thái 「現地決済：未決済」. Chứng minh bước kiểm slot lúc gửi đơn **đọc lại dữ liệu mới** chứ không dùng lại tập đã prefetch, và nhánh có bật 決済 cũng đi qua đúng bước kiểm đó |  | Tách từ TC-CONC001-06 — biến thể **bill tiền TẠI CHỖ** · Đánh giá spec: Spec không ghi (đã hỏi Dev — §6 Q6) · Evidence: ảnh màn kết quả + kết quả query + ảnh trạng thái bản ghi callback kèm mốc thời gian · Catalog `ENV-HOOK` (dev/staging có thể không nhận được webhook) → bắt buộc product · dẫn từ TC-SLN-376 |
| TC-CONC001-11 | API | CONC-001 | LINE user — xác nhận và gửi đơn đặt lịch | Abnormal | manual | product | Bill tiền TẠI CHỖ — admin tạo booking chiếm nốt slot đúng lúc khách đang ở màn xác nhận | - Bot đã liên kết Stripe (hoặc UnivaPay); calendar **BẬT** 「決済機能の利用」 (`is_use_payment = 1`)<br>- 「予約時決済の選択」 = **選択可能** (`payment_time = 2`) → khách thấy 2 lựa chọn thanh toán<br>- Khoá học C1 có giá 5.000 円 để luồng thanh toán thực sự kích hoạt<br>- Form đặt lịch đã bật bắt buộc field tên và email (điều kiện để bật 決済)<br>- Khách chọn **現地決済 (thanh toán tại chỗ)** ở bước chọn phương thức<br>- 「スタッフごとの同時受付上限」: **S1 = 1**<br>- Ca 09:00-18:00 ngày D; khung 14:00 ngày D chưa có đơn nào<br>- Có khung bận Google của một nhân viên khác trong ngày D (giữ request trên nhánh gộp truy vấn)<br>- 1 tài khoản LINE khách U1 trên thiết bị; 1 phiên admin đăng nhập trang quản trị trên máy khác | 1. U1 mở trang đặt lịch, chọn C1, chọn **đích danh S1**, chọn ô 14:00 ngày D, điền form, chọn **現地決済**, tới màn xác nhận, **dừng chưa gửi** — không reload<br>2. Trên trang quản trị, admin mở lưới quản lý ngày D và **tạo 1 booking mới cho S1 vào đúng khung 14:00**<br>3. Xác nhận booking của admin đã lưu và hiện trên lưới, ghi lại cảnh báo về việc không tính phí (nếu có)<br>4. Quay lại thiết bị U1 (**không reload**) và bấm gửi đơn<br>5. Ghi lại thông báo hiển thị trên màn hình U1<br>6. Đếm trong DB số bản ghi đặt lịch ngày D khung 14:00 gắn S1 và trạng thái thanh toán từng bản ghi | S1 同時受付上限 = 1; C1 giá 5.000 円; U1 chọn 現地決済; admin tạo 1 booking chiếm khung 14:00 trong lúc U1 đang ở màn xác nhận | Đơn của U1 **bị từ chối** với thông báo lỗi rõ ràng, **KHÔNG** hiện màn thành công giả. Trong DB có **ĐÚNG 1** bản ghi ngày D khung 14:00 gắn S1 — là booking do admin tạo; không có bản ghi thứ 2 của U1 ở bất kỳ trạng thái thanh toán nào. Không có lỗi máy chủ 500 và không có đơn lưu nửa vời |  | Tách từ TC-CONC001-07 — biến thể **bill tiền TẠI CHỖ** · Đánh giá spec: Spec ghi rõ (FA-020 §2.5 + kho TC-SLN-423) · Evidence: ảnh màn kết quả của U1 + ảnh lưới admin + kết quả query · Production có 2 server (Catalog `ENV-LB`) → admin ghi ở server này, khách gửi ở server kia · dẫn từ TC-SLN-423 |
| TC-PAYSTATE001-01 | API | PAY-STATE-001 | LINE user — xác nhận và gửi đơn đặt lịch | Abnormal | manual | product | Thanh toán TRƯỚC + staff limit = 1 — 2 khách cùng thanh toán cho slot cuối, chỉ 1 đơn và chỉ 1 giao dịch được ghi nhận | - Bot đã liên kết **Stripe**; calendar **BẬT** 「決済機能の利用」 (`is_use_payment = 1`)<br>- 「販売環境設定」 = **テスト**, dùng **thẻ test** của Stripe — chạy trên production nhưng không phát sinh tiền thật<br>- 「予約時決済の選択」 = **のみ** (`payment_time = 1`) → bắt buộc thanh toán trước khi đặt<br>- Khoá học C1 có giá 5.000 円; đã bật **3D Secure** để kéo dài cửa sổ giữa lúc chọn slot và lúc chốt đơn<br>- Có quyền xem dashboard Stripe để đối soát số giao dịch<br>- 「スタッフごとの同時受付上限」: **S1 = 1**; 受付上限 cửa hàng = không giới hạn<br>- Ca 09:00-18:00 ngày D; khung 10:00 ngày D chưa có đơn nào<br>- Có khung bận Google của S2 ngày D 10:00-11:00 (giữ request trên nhánh gộp truy vấn vừa sửa)<br>- 2 tài khoản LINE khách U1, U2 mở sẵn trên 2 thiết bị | 1. Trên cả 2 thiết bị: chọn khoá học C1, chọn **đích danh S1**, chọn ô 10:00 ngày D, điền đủ form<br>2. Cả 2 vào màn nhập thẻ, nhập **thẻ test**, hoàn tất bước xác thực 3D Secure, dừng ở màn xác nhận cuối, **chưa bấm thanh toán**<br>3. Bấm thanh toán trên 2 thiết bị **gần như cùng lúc** (trong vòng 1 giây)<br>4. Ghi lại màn hình kết quả của từng thiết bị<br>5. Đếm trong DB số bản ghi đặt lịch ngày D khung 10:00 gắn S1 và trạng thái thanh toán từng bản ghi<br>6. Mở **dashboard Stripe**, đếm số giao dịch phát sinh cho khung giờ này và trạng thái từng giao dịch | S1 同時受付上限 = 1; C1 giá 5.000 円; thẻ test Stripe; 3D Secure bật. Phép tính tay: 1 chỗ ⇒ đúng 1 booking và đúng 1 giao dịch thu 5.000 円 | **Đúng 1** khách đặt thành công: 1 bản ghi đặt lịch **và đúng 1** giao dịch thu tiền 5.000 円 trên dashboard Stripe. Khách còn lại nhận thông báo lỗi rõ ràng và **KHÔNG bị trừ tiền** — trên dashboard Stripe không có giao dịch thành công thứ 2. Nếu hệ thống đã tạo giao dịch trước khi phát hiện hết chỗ thì giao dịch đó phải được **huỷ hoặc hoàn tự động** và có bản ghi lịch sử tương ứng. **TUYỆT ĐỐI không** có trường hợp đã thu tiền mà không có booking |  | Tách từ TC-CONC001-03 — biến thể **bill tiền thanh toán TRƯỚC** · Đánh giá spec: Spec không ghi (đã hỏi Leader — hành vi khi charge xong mới phát hiện hết chỗ) → §6 · Evidence: 2 ảnh màn kết quả + kết quả query bản ghi + **ảnh dashboard Stripe đếm giao dịch** (RULE-02) · Cửa sổ race rộng hơn hẳn biến thể TẮT 決済 vì có bước nhập thẻ + 3D Secure · **RULE-08 bill tiền → product** · dẫn từ TC-SLN-402, TC-SLN-407 |
| TC-PAYSTATE001-02 | API | PAY-STATE-001 | Job tự động gán nhân viên cho booking salon | Abnormal | manual | product | Thanh toán TRƯỚC + staff limit = 1 — 2 khách cùng chọn 指定なし, không được tồn tại đơn đã thu tiền mà không gán được nhân viên | - Bot đã liên kết **Stripe**; calendar **BẬT** 「決済機能の利用」 (`is_use_payment = 1`)<br>- 「販売環境設定」 = **テスト**, dùng **thẻ test** của Stripe — chạy trên production nhưng không phát sinh tiền thật<br>- 「予約時決済の選択」 = **のみ** (`payment_time = 1`) → bắt buộc thanh toán trước khi đặt<br>- Khoá học C1 có giá 5.000 円; đã bật **3D Secure** để kéo dài cửa sổ giữa lúc chọn slot và lúc chốt đơn<br>- Có quyền xem dashboard Stripe để đối soát số giao dịch<br>- 「スタッフごとの同時受付上限」: **S1 = 1**; bật tự gán nhân viên<br>- Ngày D: S1 có ca 09:00-18:00; **S2 bị khung bận Google 10:00-11:00** ⇒ khung 10:00 chỉ còn S1 khả dụng<br>- Khung 10:00 ngày D chưa có đơn nào; tiến trình hàng đợi đang chạy<br>- 2 tài khoản LINE khách U1, U2 mở sẵn trên 2 thiết bị | 1. Trên cả 2 thiết bị: chọn C1, chọn 「指定なし」, chọn ô 10:00 ngày D, điền đủ form<br>2. Cả 2 vào màn nhập thẻ, nhập **thẻ test**, hoàn tất 3D Secure, dừng ở màn xác nhận cuối<br>3. Bấm thanh toán trên 2 thiết bị **gần như cùng lúc**<br>4. Ghi lại màn hình kết quả của từng thiết bị<br>5. Chờ hàng đợi xử lý xong công việc tự gán nhân viên<br>6. Đọc các bản ghi đặt lịch ngày D khung 10:00: nhân viên được gán, trạng thái thanh toán<br>7. Đối soát dashboard Stripe: số giao dịch và trạng thái từng giao dịch<br>8. Đọc nhật ký công việc nền tự gán nhân viên | S1 同時受付上限 = 1; S2 bị Google chiếm 10:00-11:00; C1 giá 5.000 円; thẻ test. Phép tính tay: 1 nhân viên khả dụng × limit 1 = 1 chỗ ⇒ 1 booking + 1 giao dịch | **Đúng 1** đơn được tạo, được gán cho **S1**, kèm **đúng 1** giao dịch thu 5.000 円. Khách còn lại nhận lỗi rõ ràng và **không bị trừ tiền**. **KHÔNG** được tồn tại đơn ở trạng thái đã thu tiền nhưng chưa gán được nhân viên; nếu job không gán được thì tiền phải được hoàn và có bản ghi lịch sử. **S2 không được gán** vì đang bận theo lịch Google. Job chạy xong không lỗi |  | Tách từ TC-CONC001-04 — biến thể **bill tiền thanh toán TRƯỚC** · Đánh giá spec: Spec không ghi (đã hỏi Leader — đơn đã thu tiền mà job không gán được nhân viên) → §6 · Evidence: 2 ảnh màn kết quả + bản ghi đơn + ảnh dashboard Stripe + log job · Giao của 2 rủi ro: race slot và tiền · **RULE-08 → product** · dẫn từ TC-SLN-408 |
| TC-PAYSTATE001-03 | API | PAY-STATE-001 | LINE user — xác nhận và gửi đơn đặt lịch | Abnormal | manual | product | Thanh toán TRƯỚC — lịch Google sync về trong lúc khách đang ở bước xác thực 3D Secure, quay lại phải bị từ chối và không giữ tiền | - Bot đã liên kết **Stripe**; calendar **BẬT** 「決済機能の利用」 (`is_use_payment = 1`)<br>- 「販売環境設定」 = **テスト**, dùng **thẻ test** của Stripe — chạy trên production nhưng không phát sinh tiền thật<br>- 「予約時決済の選択」 = **のみ** (`payment_time = 1`) → bắt buộc thanh toán trước khi đặt<br>- Khoá học C1 có giá 5.000 円; đã bật **3D Secure** để kéo dài cửa sổ giữa lúc chọn slot và lúc chốt đơn<br>- Có quyền xem dashboard Stripe để đối soát số giao dịch<br>- Nhân viên S1 **đã liên kết Google Calendar thật** (BR-09), luồng sync đang hoạt động<br>- Ca 09:00-18:00 ngày D; khung 11:00 ngày D còn trống, chưa có khung bận Google nào chồng lên<br>- Có quyền đọc bảng `salon_google_calendar_callback` để theo dõi trạng thái bản ghi webhook<br>- 1 tài khoản LINE khách U1 mở sẵn trên thiết bị | 1. U1 chọn ô 11:00 ngày D, điền form, vào màn nhập thẻ, nhập **thẻ test** và bấm thanh toán để chuyển sang trang xác thực 3D Secure — **dừng lại ở trang xác thực, chưa hoàn tất**<br>2. Trên **Google Calendar thật** của S1, tạo event 11:00-12:00 ngày D<br>3. Chờ tới khi bản ghi callback xử lý xong **và** khung bận đã xuất hiện trong dữ liệu<br>4. Quay lại trang 3D Secure của U1 và **hoàn tất xác thực**<br>5. Ghi lại màn kết quả sau khi quay về từ 3D Secure<br>6. Đếm trong DB số bản ghi đặt lịch ngày D khung 11:00<br>7. Đối soát **dashboard Stripe**: có giao dịch nào được tạo không, ở trạng thái gì | Event Google 11:00-12:00 ngày D tạo trên Google Calendar thật của S1 trong lúc U1 đang ở trang 3D Secure; C1 giá 5.000 円; thẻ test | U1 nhận thông báo khung giờ đã bị chặn 「指定の時間帯はブロックされています。予約時間を変更してください。」 và **không** có bản ghi đặt lịch ngày D khung 11:00. Trên dashboard Stripe: hoặc **không có** giao dịch thành công nào, hoặc giao dịch đã được **huỷ / hoàn tự động** — **không được để trạng thái đã thu tiền của khách mà không có booking**. Nếu tiền bị giữ, phải có bản ghi hoàn tiền và thông báo cho khách |  | Tách từ TC-CONC001-06 — biến thể **bill tiền thanh toán TRƯỚC** · Đánh giá spec: Spec không ghi (đã hỏi Leader) → §6 · Evidence: ảnh màn kết quả + kết quả query + **ảnh dashboard Stripe** + ảnh trạng thái bản ghi callback kèm mốc thời gian · **Đây là cửa sổ race rộng nhất của cả tính năng** — khách ở trang ngân hàng có thể mất hàng chục giây tới vài phút · Catalog `ENV-HOOK` + `ENV-PAY` → **bắt buộc product** · dẫn từ TC-SLN-407 |
| TC-PAYSTATE001-04 | API | PAY-STATE-001 | LINE user — xác nhận và gửi đơn đặt lịch | Abnormal | manual | product | Thanh toán TRƯỚC — admin tạo booking chiếm nốt slot trong lúc khách đang ở bước xác thực 3D Secure | - Bot đã liên kết **Stripe**; calendar **BẬT** 「決済機能の利用」 (`is_use_payment = 1`)<br>- 「販売環境設定」 = **テスト**, dùng **thẻ test** của Stripe — chạy trên production nhưng không phát sinh tiền thật<br>- 「予約時決済の選択」 = **のみ** (`payment_time = 1`) → bắt buộc thanh toán trước khi đặt<br>- Khoá học C1 có giá 5.000 円; đã bật **3D Secure** để kéo dài cửa sổ giữa lúc chọn slot và lúc chốt đơn<br>- Có quyền xem dashboard Stripe để đối soát số giao dịch<br>- 「スタッフごとの同時受付上限」: **S1 = 1**<br>- Ca 09:00-18:00 ngày D; khung 14:00 ngày D chưa có đơn nào<br>- Có khung bận Google của một nhân viên khác trong ngày D (giữ request trên nhánh gộp truy vấn)<br>- 1 tài khoản LINE khách U1 trên thiết bị; 1 phiên admin đăng nhập trang quản trị trên máy khác | 1. U1 chọn C1, chọn **đích danh S1**, chọn ô 14:00 ngày D, điền form, vào màn nhập thẻ, nhập **thẻ test** và bấm thanh toán để chuyển sang trang 3D Secure — **dừng lại, chưa hoàn tất**<br>2. Trên trang quản trị, admin mở lưới quản lý ngày D và **tạo 1 booking mới cho S1 vào đúng khung 14:00**<br>3. Xác nhận booking của admin đã lưu và hiện trên lưới<br>4. Quay lại trang 3D Secure của U1 và **hoàn tất xác thực**<br>5. Ghi lại màn kết quả trên màn hình U1<br>6. Đếm trong DB số bản ghi đặt lịch ngày D khung 14:00 gắn S1<br>7. Đối soát **dashboard Stripe**: số giao dịch và trạng thái từng giao dịch | S1 同時受付上限 = 1; C1 giá 5.000 円; thẻ test; admin tạo booking chiếm khung 14:00 trong lúc U1 đang ở trang 3D Secure | Đơn của U1 **bị từ chối** với thông báo lỗi rõ ràng. Trong DB có **ĐÚNG 1** bản ghi ngày D khung 14:00 gắn S1 — là booking do admin tạo. Trên dashboard Stripe **không có** giao dịch thành công của U1; nếu đã tạo thì phải huỷ / hoàn tự động kèm bản ghi lịch sử. Không có lỗi máy chủ 500, không có đơn lưu nửa vời và không có tiền treo |  | Tách từ TC-CONC001-07 — biến thể **bill tiền thanh toán TRƯỚC** · Đánh giá spec: Spec không ghi (đã hỏi Leader) → §6 · Evidence: ảnh màn kết quả của U1 + ảnh lưới admin + kết quả query + **ảnh dashboard Stripe** · Production có 2 server (Catalog `ENV-LB`) · **RULE-08 bill tiền → product** · dẫn từ TC-SLN-423 |
| TC-DATACOUNT001-01 | Data | DATA-COUNT-001 | 受付上限 — 店舗・スタッフ | Boundary | manual | staging | Khung bận Google chặn slot nhưng KHÔNG bị đếm vào 受付上限 sau khi gộp truy vấn | - Lịch salon 2 nhân viên A, B; ca 09:00-18:00 ngày D<br>- 受付上限 của cửa hàng đặt `limited_quantity = 2` cho mỗi khung<br>- Khung bận Google của nhân viên A: ngày D 13:00-14:00<br>- Chưa có đơn đặt nội bộ nào ngày D | 1. Mở trang đặt lịch của khách, chọn 「指定なし」, tới bước chọn ngày giờ ngày D<br>2. Ghi lại trạng thái ô 13:00 và ô 15:00<br>3. Đặt 1 đơn vào khung 15:00 ngày D<br>4. Mở lại bảng chọn giờ, ghi lại trạng thái ô 15:00<br>5. Đặt tiếp 1 đơn nữa vào khung 15:00<br>6. Mở lại bảng chọn giờ, ghi lại trạng thái ô 15:00 và ô 13:00<br>7. Ở màn admin, mở modal lý do không đặt được của ô 13:00 | Limit = 2/khung; khung 13:00 chỉ có block Google (0 đơn); khung 15:00 lần lượt có 1 rồi 2 đơn. Phép tính tay: khung 13:00 số đơn đếm vào limit = **0**, khung 15:00 = 1 → 2 | Ô 13:00 **bị chặn** (do block Google) nhưng bộ đếm limit của khung đó vẫn là **0/2** — khách vẫn đặt được với nhân viên B nếu B rảnh. Ô 15:00: sau đơn thứ 1 vẫn chọn được (1/2), sau đơn thứ 2 mới bị chặn (2/2). Modal lý do của ô 13:00 hiển thị lý do **block time**, KHÔNG phải "đã đạt 受付上限". Toàn bộ khớp với hành vi trước bản vá | | Lấp GAP-2 / cover BUG-1, FIX-1 · Đánh giá spec: Spec ghi rõ (FA-020 §2.4.4 + kho TC-SLN-284) · Evidence: ảnh bảng giờ 3 lần + ảnh modal lý do + kết quả query đếm đơn · regression · dẫn từ TC-SLN-284, TC-SLN-288 |
| TC-FUNCDATE001-01 | UI | FUNC-DATE-001 | LINE user — chọn slot | Boundary | manual | staging | Event Google vắt qua nửa đêm (tách thành 2 block) vẫn chặn đủ cả 2 phía sau khi gộp theo ngày | - Lịch salon 1 nhân viên, có ca ngày D 18:00-23:59 và ca ngày D+1 00:00-06:00<br>- Trên Google Calendar của nhân viên đó, tạo 1 event **23:00 ngày D → 01:00 ngày D+1**, chờ sync xong<br>- Xác nhận trong dữ liệu đã sinh **2 bản ghi block** (phần ngày D và phần ngày D+1) | 1. Mở trang đặt lịch của khách, tới bước chọn ngày giờ, chuyển tới tuần chứa ngày D<br>2. Ghi lại trạng thái các ô 22:30, 23:00, 23:30 của ngày D<br>3. Ghi lại trạng thái các ô 00:00, 00:30, 01:00, 01:30 của ngày D+1<br>4. Chuyển sang tuần sau rồi quay lại tuần chứa ngày D, ghi lại lần nữa cả 2 nhóm ô<br>5. Lặp lại bước 1-3 ở tab 「月」 rồi bấm vào ngày D và ngày D+1 | Event Google 23:00 ngày D → 01:00 ngày D+1 (vắt nửa đêm, sinh 2 block); ca ngày D 18:00-23:59 và ca ngày D+1 00:00-06:00 | Ô 23:00 và 23:30 của ngày D **bị chặn**; ô 00:00 và 00:30 của ngày D+1 **bị chặn**; ô 22:30 ngày D và ô 01:00, 01:30 ngày D+1 **vẫn chọn được**. **Cả 2 mảnh** của event đều có tác dụng — không mảnh nào bị rơi khi gộp truy vấn rồi nhóm theo ngày. Kết quả lần xem thứ 2 (sau khi chuyển tuần đi rồi về) giống hệt lần đầu. Tab 月 đánh dấu cả 2 ngày đúng trạng thái | | Lấp GAP-4 / cover FIX-1 · Đánh giá spec: Spec ghi rõ (kho TC-SLN-363, Bug #26739) · Evidence: bảng kết quả biên thời gian đã chạy + ảnh 2 lần xem · regression · dẫn từ TC-SLN-363 |
| TC-FUNCDATE001-02 | UI | FUNC-DATE-001 | LINE user — chọn slot | Abnormal | manual | staging | Ca kết thúc 24:00 và ca kết thúc 00:00 cho ra cùng tập slot bị chặn | - Lịch salon 1 nhân viên<br>- Ngày D: ca nhập giờ kết thúc là **24:00**<br>- Ngày E (khác tuần, cùng cấu hình còn lại): ca nhập giờ kết thúc là **00:00**<br>- Mỗi ngày có 1 khung bận Google 23:00-23:30 | 1. Ở màn admin, xác nhận 2 ca đã lưu và xem giá trị hiển thị của giờ kết thúc từng ca<br>2. Mở trang đặt lịch của khách, tới ngày D, ghi lại trạng thái các ô từ 22:30 tới ô cuối cùng của ca<br>3. Chuyển tới ngày E, ghi lại tương tự<br>4. So 2 tập ô | Ca ngày D kết thúc 24:00; ca ngày E kết thúc 00:00; khung bận Google 23:00-23:30 mỗi ngày | Hai ngày cho ra **cùng một tập ô** (cùng ô cuối cùng, cùng ô bị chặn 23:00). Không ngày nào sinh thêm ô thuộc ngày hôm sau. Hành vi giống hệt trước bản vá | | Lấp GAP FUNC-DATE-001 thiếu Abnormal (RULE-01) · Đánh giá spec: Spec không ghi (đã hỏi Leader — quy đổi 24:00 ⇔ 00:00) · Evidence: bảng kết quả biên thời gian · Catalog DI-09 · dẫn từ TC-SLN-197 |
| TC-SECISO001-01 | API | SEC-ISO-001 | Googleカレンダー連携 | Abnormal | manual | staging | Hai nhân viên liên kết CHUNG 1 Google Calendar — truy vấn gộp không chặn nhầm chéo | - Lịch salon nhiều nhân viên với nhân viên A và nhân viên B<br>- **Cả A và B cùng liên kết một tài khoản / một Google Calendar duy nhất** (BR-09), chờ sync xong<br>- Trên calendar đó có 1 event ngày D 16:00-17:00<br>- Nhân viên C liên kết 1 Google Calendar riêng, không có event ngày D | 1. Mở trang đặt lịch của khách, chọn khoá học, tới bước chọn nhân viên<br>2. Chọn nhân viên A, ghi lại trạng thái ô 16:00 và 16:30 ngày D<br>3. Quay lại, chọn nhân viên B, ghi lại lần nữa<br>4. Quay lại, chọn nhân viên C, ghi lại lần nữa<br>5. Quay lại, chọn 「指定なし」, ghi lại lần nữa<br>6. Thực hiện bước 2→3→4 liên tiếp nhanh (không chờ tải xong hẳn) rồi ghi lại kết quả cuối | 1 Google Calendar dùng chung cho A và B; event ngày D 16:00-17:00; nhân viên C có calendar riêng, trống | Khi chọn A: ô 16:00, 16:30 bị chặn. Khi chọn B: **cũng bị chặn** (cùng calendar). Khi chọn C: **vẫn chọn được**. Khi chọn 「指定なし」: vẫn chọn được (vì còn C rảnh). Kết quả ở bước 6 (thao tác nhanh liên tiếp) giống hệt kết quả từng lần chọn chậm — không dính dữ liệu chéo giữa các lần tải | | Lấp GAP-5 / cover BUG-1, FIX-1 · Đánh giá spec: Spec ghi rõ (FA-020 BR-09) · Evidence: bằng chứng dữ liệu từng nhân viên độc lập khi tải nhanh liên tiếp · dẫn từ TC-SLN-366, TC-SLN-367 |
| TC-OUTTRUTH001-01 | API | OUT-TRUTH-001 | LINE user — chọn slot | Boundary | manual | staging | Khoá học 所要時間 lẻ + đơn vị nhận booking khác 30 phút — slot bị chặn đúng sau khi gộp | - Lịch salon 1 nhân viên, ca 09:00-18:00 ngày D<br>- Tạo 2 khoá học: khoá X **45 phút**, khoá Y **90 phút**<br>- Đơn vị nhận booking đặt là **15 phút**<br>- Khung bận Google ngày D 13:00-13:30<br>- Đệm trước/sau cho lịch Google = 0 | 1. Mở trang đặt lịch, chọn khoá X (45 phút), tới bước chọn ngày giờ ngày D<br>2. Ghi lại toàn bộ ô trong khoảng 12:00-14:30<br>3. Quay lại, chọn khoá Y (90 phút), ghi lại toàn bộ ô trong cùng khoảng<br>4. Đặt thử 1 đơn khoá Y vào ô ngay trước khung bận và xác nhận đặt được<br>5. So cả 2 tập ô với kết quả tính tay | Khoá X 45 phút, khoá Y 90 phút, đơn vị 15 phút, khung bận Google 13:00-13:30. Phép tính tay khoá X: ô bắt đầu bị chặn = từ 12:15 tới 13:15. Khoá Y: từ 11:30 tới 13:15 | Tập ô bị chặn khớp **đúng phép tính tay** cho từng khoá học (khoá dài hơn thì bị chặn sớm hơn). Không có ô nào bị chặn thừa hoặc thiếu ở hai đầu khung bận. Đơn ở bước 4 đặt thành công và sau đó ô đó chuyển sang không chọn được | | Lấp GAP-6 / cover FIX-1 · Đánh giá spec: Spec ghi rõ (FA-020 §2.4.3) · Evidence: bảng đối chiếu tập ô với phép tính tay · regression · dẫn từ TC-SLN-433, TC-SLN-441 |
| TC-DATADB001-01 | Data | DATA-DB-001 | Googleカレンダー連携 | Boundary | auto | staging | Truy vấn gộp giữ đủ điều kiện lọc theo bot — 2 bot có lịch salon trùng tên không lẫn dữ liệu | - 2 bot khác nhau (bot A, bot B), mỗi bot có 1 lịch salon **đặt trùng tên** và trùng cấu hình ca làm ngày D<br>- Mỗi lịch có khung bận Google ngày D nhưng **khác giờ**: bot A 10:00-11:00, bot B 15:00-16:00<br>- Có quyền đọc nhật ký truy vấn | 1. Mở trang đặt lịch của lịch thuộc **bot A**, tới bước chọn ngày giờ ngày D, ghi lại trạng thái ô 10:00 và ô 15:00<br>2. Trích câu truy vấn gộp đọc bảng khung bận Google từ nhật ký, ghi lại đủ mệnh đề lọc<br>3. Lặp lại bước 1-2 với lịch thuộc **bot B**<br>4. Chạy tay câu truy vấn của bot A và đếm số bản ghi trả về | Bot A: block 10:00-11:00; bot B: block 15:00-16:00; 2 lịch trùng tên | Lịch bot A: ô 10:00 bị chặn, ô 15:00 **chọn được**. Lịch bot B: ngược lại. Câu truy vấn gộp có đủ mệnh đề giới hạn theo lịch salon (và theo bot nếu schema có), và số bản ghi trả về **chỉ** gồm bản ghi của lịch đang xem — không có bản ghi của lịch bot còn lại | | Lấp RISK multi-tenant / cover FIX-1 · Đánh giá spec: Spec ghi rõ · Evidence: ảnh chụp kết quả query kèm câu query trên cả 2 bot (RULE-07) · dẫn từ TC-SLN-213 |
| TC-ENV001-01 | API | ENV-001 | Phân quyền & môi trường | Abnormal | manual | product | Ngắt 1 server giữa lúc khách gửi đơn — bước kiểm slot không bị bỏ qua | - Môi trường production có **2 server sau cân bằng tải**<br>- Lịch salon 1 nhân viên, ca 09:00-18:00 ngày D<br>- Khung bận Google ngày D 11:00-12:00<br>- Có quyền lấy router id và ngắt 1 server (phối hợp với hạ tầng)<br>- Đã ghi lại router id phục vụ request đầu | 1. Mở trang đặt lịch, tới màn chọn giờ, ghi lại router id đang phục vụ<br>2. Chọn ô 11:00 (đang bị chặn) bằng cách gọi thẳng lời gọi gửi đơn với khung 11:00<br>3. Xác nhận đơn bị từ chối<br>4. Lặp lại luồng đặt bình thường cho ô 10:00 tới màn xác nhận, **chưa** gửi<br>5. Ngắt server đang phục vụ, rồi bấm gửi đơn<br>6. Kiểm màn kết quả, kiểm DB, và chạy lại bộ 3 case cơ bản trên **từng router** | Ô 11:00 bị Google chiếm; ô 10:00 trống; ngắt 1 trong 2 server giữa chừng | Bước 3: đơn vào khung bị chặn **bị từ chối**, không tạo bản ghi. Bước 5-6: hoặc đơn được tạo đúng 1 lần với đầy đủ kiểm slot, hoặc bị từ chối/đưa vào hàng đợi retry với thông báo rõ ràng — **tuyệt đối không** có trường hợp đơn được tạo mà bỏ qua bước kiểm slot. Bộ 3 case cơ bản cho kết quả giống nhau trên cả 2 router | | Lấp GAP-3 / cover FIX-1 · Đánh giá spec: Spec không ghi (đã hỏi Dev — §6 Q6) · Evidence: hành vi khi giả lập đứt 1 server + router id của từng lượt chạy · Catalog ENV-LB · **RULE-08 → bắt buộc product** |
| TC-JOB001-01 | API | JOB-001 | Job nền & monitor | Boundary | manual | product | Job tự gán nhân viên chạy lô lớn sau bản vá — số bản ghi vào = thành công + hàng đợi lỗi | - Production, lịch salon nhiều nhân viên (≥ 10 nhân viên), bật tự gán nhân viên<br>- Có ≥ 50 đơn 「指定なし」 chờ job xử lý trong cùng khoảng thời gian<br>- Một phần nhân viên có khung bận Google chồng lên giờ của các đơn đó<br>- Có quyền đọc log của **cả 3 job production** (callback / broadcast / scenario) và hàng đợi lỗi | 1. Ghi lại **số đơn đầu vào** đang chờ gán<br>2. Cho hàng đợi chạy, theo dõi log job tự gán nhân viên<br>3. Sau khi job xong, đếm số đơn đã gán thành công<br>4. Đếm số đơn vào hàng đợi lỗi<br>5. Với 5 đơn ngẫu nhiên, đối chiếu nhân viên được gán có bị bận theo lịch Google trong đúng khung đó không<br>6. Đo thời gian chạy job và so với lần chạy tương đương trước bản vá | ≥ 50 đơn 「指定なし」; ≥ 10 nhân viên; một phần có khung bận Google trùng giờ | **Số đơn vào = số gán thành công + số vào hàng đợi lỗi** — không đơn nào biến mất. Không đơn nào bị gán cho nhân viên đang bận theo lịch Google. Job không dừng giữa chừng; nếu có lỗi thì log ghi rõ lần retry và backoff. Thời gian chạy job **không tăng** so với trước bản vá | | Lấp GAP JOB-001 / cover BUG-1 vùng job · Đánh giá spec: Spec ghi rõ (FA-020 §7) · Evidence: log job thấy retry + backoff, bảng đối chiếu số bản ghi vào/ra · Catalog ENV-JOB (prod tách 3 job) · **RULE-08 → bắt buộc product** |
| TC-LIFFENTRY001-01 | UI | LIFF-ENTRY-001 | LINE user — mở link & entry | Normal | manual | product | Bảng chọn giờ mở từ app LINE trên máy thật — hiển thị đúng và đo được thời gian thật | - Lịch salon quy mô thật (≥ 10 nhân viên, có khung bận Google)<br>- Có LIFF URL đặt lịch của lịch đó<br>- 1 máy iOS và 1 máy Android, mỗi máy 2 tài khoản LINE: **1 đã là bạn** của OA, **1 chưa là bạn**<br>- Mạng 4G thật, không dùng Wi-Fi văn phòng | 1. Trên iOS, tài khoản **đã là bạn**: mở LIFF URL từ tin nhắn LINE, đi tới bước chọn ngày giờ, đo thời gian từ lúc bấm tới lúc bảng giờ hiển thị xong<br>2. Đối chiếu tập ô bị chặn với kết quả chạy trên trình duyệt desktop cùng dữ liệu<br>3. Chuyển tuần và chuyển sang tab 「月」, kiểm hiển thị<br>4. Lặp lại bước 1-3 trên Android<br>5. Trên tài khoản **chưa là bạn**, mở cùng LIFF URL và quan sát màn hiển thị<br>6. Mở lại cùng URL bằng trình duyệt ngoài trên điện thoại | Lịch salon quy mô thật; 2 hệ điều hành; 2 trạng thái kết bạn; mạng 4G | Trên cả iOS và Android: bảng giờ hiển thị đầy đủ, **không tràn ngang, không cắt cột ngày**, tập ô bị chặn **trùng khớp** với kết quả trên desktop. Thời gian hiển thị xong được ghi lại thành số cụ thể cho từng máy (đây là con số nghiệm thu thật của ticket). Chuyển tuần và tab 月 hoạt động bình thường. Tài khoản **chưa là bạn** được chuyển sang màn kết bạn. Mở bằng trình duyệt ngoài vẫn vào được | | Lấp GAP LIFF-ENTRY-001 / cover BUG-2 (đo thời gian thật) · Đánh giá spec: Spec ghi rõ (FA-020 §2.6) · Evidence: 1 ảnh in-app LINE iOS + 1 ảnh in-app LINE Android + 1 ảnh trình duyệt ngoài + 1 ảnh case chưa kết bạn (RULE-02) · Catalog MAP-LIFF-02, MAP-LIFF-03 · **RULE-06 output cuối trên thiết bị thật** |
| TC-DATAMIG001-01 | Data | DATA-MIG-001 | Dữ liệu cũ & hồi quy | Abnormal | manual | product | Migration thêm chỉ mục chạy lại được và không khoá bảng quá lâu trên dữ liệu thật | ⚠️ **Chỉ chạy nếu Dev xác nhận bản vá có thêm index (§6 Q2)**<br>- Bản sao dữ liệu production của bảng khung bận đồng bộ Google ở quy mô thật<br>- Có quyền chạy migration và đo thời gian<br>- Ghi lại số bản ghi của bảng trước khi chạy | 1. Đếm số bản ghi của bảng và ghi lại<br>2. Chạy migration của bản vá, **đo thời gian** và theo dõi bảng có bị khoá ghi không<br>3. Trong lúc migration chạy, thử đặt 1 đơn từ trang đặt lịch<br>4. Sau khi xong, đếm lại số bản ghi và so với bước 1<br>5. **Chạy lại migration lần 2** và quan sát<br>6. Chạy kịch bản rollback theo hướng dẫn của Dev rồi mở lại trang đặt lịch | Bảng ở quy mô production thật; migration chạy 2 lần liên tiếp | Số bản ghi trước và sau migration **bằng nhau** — không mất dữ liệu. Thời gian chạy được ghi thành số cụ thể và nằm trong cửa sổ release cho phép. Trong lúc migration chạy, đơn ở bước 3 hoặc thành công hoặc báo lỗi rõ ràng, **không** treo vô hạn và **không** lưu nửa vời. Lần chạy thứ 2 **không lỗi** (idempotent). Sau rollback, trang đặt lịch vẫn hoạt động bình thường | | Lấp GAP DATA-MIG-001 (có điều kiện) / cover BUG-3 · Đánh giá spec: Spec không ghi (đã hỏi Dev — §6 Q2) · Evidence: kết quả đếm bản ghi trước/sau + thời gian chạy + log migration lần 2 · **RULE-08 → bắt buộc product** · dẫn từ TC-SLN-486 |

**Xác nhận chống trùng**: đã đối chiếu **25 TC đề xuất** (24 mới + phần chỉnh TC-SLN-460) với **30 TC ở BƯỚC 0** và **498 TC trong kho fa020** theo đủ 4 yếu tố — không TC nào trùng. Các trường hợp gần giống đã được chuyển sang mục **5a (dùng lại kho)** thay vì viết mới. Hai GAP cùng dẫn tới một TC đã được gộp: GAP-1 + GAP-2 → `TC-CONC001-02`.

---

## 6. Spec update needed

| # | Câu hỏi / điểm cần chốt | Vì sao chặn | Người quyết |
|---|---|---|---|
| **Q1** | **Mục 2 "Cách fix" mới có "Fix 1" — còn Fix 2 / Fix 3 không?** Cụ thể: bản vá có xử lý **tầng 2** (vòng `for i<=3` ở `handleShowListBooking`) và **tầng 3** (bỏ `whereDate()` bọc cột + thêm index) không? | Quyết định BUG-2 và BUG-3 có nằm trong phạm vi nghiệm thu hay không. Hiện NEW-29 đang tự cho phép kết luận "ngoài phạm vi bản vá" | Dev |
| **Q2** | **Bản vá có thêm index cho bảng `calendar_salon_booking_by_google` không?** Nếu có: migration nào, chạy trên production mất bao lâu, có khoá bảng không, rollback thế nào? | Nếu có → `DATA-MIG-001` (Cao) trở thành **BLOCKER**, mục 4.2 phải khai `MIGRATE`, và TC-DATAMIG001-01 bắt buộc chạy | Dev |
| **Q3** | **Mục 3 + 4.1 / 4.2 / 4.3 trống** — Dev đã check những caller nào của `getListBookingByGoogleConfirm` / `getListBookingByShift`? Có gồm **2 job tự gán nhân viên**, **bước xác nhận gửi đơn**, và **job monitor phát hiện booking chèn vào block time Google** (kho TC-SLN-467) không? | `REG-SHARED-001` (Cao) bắt buộc có danh sách của Dev. Không có → coverage matrix không lập được theo `F*/D*/T*` | Dev |
| **Q4** | **Ngày có NHIỀU CA — hành vi ĐÚNG là gì?** REQ-004 của Studio ghi *"hiện tại kết quả của ca sau ghi đè ca trước trên cùng một ngày"*, và NEW-11 kết thúc bằng *"báo cho leader/PO quyết định thay vì tự kết luận đạt hay không đạt"*. Spec FA-020 không định nghĩa hành vi này | TC không có tiêu chí Đạt/Không đạt. Nếu hành vi cũ là **sai** thì bản vá "giữ nguyên kết quả" đang bảo tồn một bug | PO + Dev |
| **Q5** | **Ngưỡng nghiệm thu hiệu năng là bao nhiêu?** Cần con số cho: (a) thời gian dựng bảng giờ tuần lần đầu; (b) số truy vấn tối đa đọc bảng khung bận Google mỗi lần dựng. Và **số nhân viên của khách hàng lớn nhất hiện tại** | 4 TC đang dùng "giảm rõ rệt" — không nghiệm thu được. Quy mô test hiện tại (8-10 nhân viên) nhỏ hơn quy mô nêu trong mục 1 (30 nhân viên) | PO + Dev + CS |
| **Q6** | **Bước kiểm slot lúc gửi đơn có chạy lại truy vấn hay dùng dữ liệu đã prefetch?** Nếu dùng dữ liệu prefetch từ đầu request thì cửa sổ đặt trùng rộng ra bao nhiêu? | Quyết định mức độ nghiêm trọng của GAP-1 (đặt trùng đồng thời) và thiết kế của TC-ENV001-01 | Dev |
| **Q7** | **Branch `ai_fixbug_40378` có đúng là branch của ticket #40380 không?** (cả Redmine lẫn Studio đều ghi số 40378) | Nếu là branch fix chung nhiều ticket thì phạm vi regression rộng hơn phạm vi ticket này | Dev |
| **Q8** | **Mẫu N+1 này có tồn tại ở Booking Event (FA-021) không?** (Đã xác nhận **FA-019 Lesson không có** tích hợp Google Calendar nên không áp dụng) | `REG-SHARED-001` — cặp Salon ⇄ Lesson ⇄ Booking Event là điểm lặp lại đã ghi trong checklist | Dev |

---

<!-- Review vòng 1 · nguồn TC: MCP LME TEST STUDIO task #268 (30 TC, 0 đã chạy) · spec: spec-features/admin/salon-booking/feature-spec.md · kho: kho-tcs/fa020 (498 TC) -->
<!-- Draft cho Leader verify — chưa phải final. -->
