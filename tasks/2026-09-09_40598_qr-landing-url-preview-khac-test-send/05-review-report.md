# 05 — Review Report

## 0. Nguồn TC

- **Nguồn đã dùng**: MCP LME TEST STUDIO — task `#285` (round 1, branch `ai_fixbug_40598`, `status=tc-ready`, `reviewState=leader`, `exec: 0 pass / 0 fail / 15 untested`).
- **Tổng số TC review**: 15.

---

## 1. Coverage — 2 chiều `dev-impact` + `diff code`

**Kết luận — đối chiếu thẳng 16 mục Dev kê ở [`03-dev-impact.md`](03-dev-impact.md) §4.3:**
> ## ✅ 3 đủ · ⚠️ 3 thiếu một phần · ⛔ **10 TRỐNG HOÀN TOÀN**

Quy ra vùng ảnh hưởng chi tiết (F/D/T + điểm diff): **7/29 đủ · 14 GAP · 8 RISK**.

⚠️ Toàn bộ 15 TC **chưa chạy** (§4-1) → mọi mục đánh ✅ cũng mới là "có TC", **chưa phải "đã verify"**.

### 1a. Truy vết 1:1 với danh sách 4.3 của Dev

| # | Mục Dev kê ở 4.3 | TC Studio phủ | Kết luận | Dòng chi tiết |
|---|---|---|---|---|
| 1 | **Message Template (FA-010)** — gửi thử ở màn mẫu tin (`/ajax/template-v2/send-test-template-v3`, qua `TemplateService`) | NEW-1, 2, 3, 4, 5, 6 | ✅ **Đủ** (6 TC) | — |
| 2 | **Step Delivery / Scenario (FA-009)** — nút gửi thử ở màn kịch bản (`/ajax/template-v2/send-test-template`, qua `ChatHelper`) | — | ⛔ **TRỐNG** | G3 |
| 3 | **Broadcast (FA-008)** — nút gửi thử ở màn gửi tin hàng loạt (cùng route trên, qua `ChatHelper`) | — | ⛔ **TRỐNG** | G4 |
| 4 | **1-on-1 Chat (FA-001)** web — gửi mẫu tin *(gồm nhánh **sửa nội dung ngay trước khi gửi**)* và gửi tin văn bản | NEW-17, 18, 23 | ⚠️ **Một phần** — expected NEW-17/18 **SAI** (§4-5); nhánh **sửa nội dung trước khi gửi = 0 TC** | G15 |
| 5 | **1-on-1 Chat trên app (FA-001)** — gửi mẫu tin từ app (`Api ChatController::sendTemplate`) | NEW-20 | ✅ Đủ (1 TC) | — |
| 6 | **Action Settings (SC-004) / Action Schedule (FA-016)** — `TemplateService::sendMessageAction`, `HelperService::sendAction`, `executeActionOpenMobile` / `...Normal` | NEW-26 | ⚠️ **Một phần** — chỉ phủ open-mobile, lại **1 trong 2 hàm**; `sendMessageAction`, `sendAction`, gửi theo lịch **= 0 TC** | G16 · G14 |
| 7 | **QR Code Action / Landing (FA-017)** — trọng tâm ticket: `[LANDING_INTRO_xxx]` ra link đầy đủ, giống link do rich menu gửi | NEW-1, 14, 15 | ✅ **Đủ** | — |
| 8 | **Form Builder (FA-011) + Point** — tin trả lời sau khi gửi biểu mẫu và tin tặng điểm (`FormAnswerService`) | — | ⛔ **TRỐNG** | G7 |
| 9 | **Lesson / Calendar Booking (FA-019)** — `CalendarCourseBookingService` | — | ⛔ **TRỐNG** | G7 |
| 10 | **Salon Booking (FA-020)** — `CalendarSalonLineBookingService` | — | ⛔ **TRỐNG** | G7 |
| 11 | **Event Booking (FA-021)** — `EventBookingService` | — | ⛔ **TRỐNG** | G7 |
| 12 | **Single Product / Sales (FA-026)** — `SalesService` | — | ⛔ **TRỐNG** | G7 |
| 13 | **Quick reply** — 2 bản riêng (`TemplateService::detectContent` và `QuickReplyService::detectContent`) | — | ⛔ **TRỐNG** | G8 |
| 14 | **Rich message / image map** — `makeImageMapVer2` và `ChatHelper::makeImageMap` | — | ⛔ **TRỐNG** | G9 |
| 15 | **LIFF** — tin gửi theo hành động khi khách mở link (`LiffController::sendMessageAction`) | — | ⛔ **TRỐNG** | G10 |
| 16 | **URL Analytics (FA-023)** — URL ngoài VẪN rút gọn **và vẫn đếm lượt bấm**; URL miền hệ thống thôi rút gọn (trừ `/landing-qr/`) | NEW-2, 3, 4 | ⚠️ **Một phần** — 3 chiều dạng URL có TC, nhưng **"vẫn đếm lượt bấm" = 0 TC** | G2 · G6 |

> 🔴 **10 mục trống KHÔNG ngẫu nhiên** — chúng đúng bằng nhóm **"gửi tin THẬT"** (biểu mẫu, tặng điểm, 3 loại đặt lịch, bán hàng, quick reply, image map, LIFF). Toàn bộ đi qua `replaceMessageText` — chính chỗ Dev tự cảnh báo: *"nay **phụ thuộc ngầm** vào cờ `$bot->is_shorten_url`… nơi gọi MỚI không theo nếp này sẽ **gửi đi nguyên mã**"*. Bộ TC hiện tại chỉ chứng minh **test send** và **chat** đúng; **không** chứng minh được đường khách hàng thật nhận tin.
>
> 🔴 **Mục 2 và 3 là phạm vi PHÁT SINH THÊM** khi Dev chỉ đạo sửa `ChatMessages.php` ở bản fix cuối (#135377). Bộ TC Studio sinh theo bản 2 file (#135335) nên **không biết 2 mục này tồn tại**.
>
> ⚠️ **Phân bố lệch**: 6/15 TC (40%) dồn vào **một** mục (FA-010 gửi thử), trong khi 10 mục khác trắng.

### 1b. Chi tiết GAP / RISK

| Mã | Chiều | Vùng ảnh hưởng | Vấn đề | Status | Severity |
|---|---|---|---|---|---|
| **G1** | `diff code` | `BUG` · F1 — `makeLinkLandingPageIntro` nhánh **catch** nay `return $message` (trước rơi xuống trả **rỗng**) | Studio `dev_impact` nêu đích danh; **REQ-007 risk High**; **0 TC** verify tin vẫn còn nội dung khi dựng link **ném lỗi**. Fix sửa **cả 2 bản sao** (TemplateService + ChatMessages) → cần **≥ 2 trigger**, hiện có **0**. ⚠️ **Chưa ai biết cách ép hàm ném lỗi** — Leader đã xác nhận mã xóa / mã sai định dạng chỉ dẫn tới **"bỏ mã"** (đường xử lý thường), nên `TC-DATAREF001-01` và `TC-FUNC002-01` ở §5 **KHÔNG** lấp được G1. Guard này do **chính bản fix thêm vào** (đảo thứ tự làm nó thành lời gọi cuối) → hỏng là tin gửi đi **rỗng** | **GAP** | `[BLOCKER]` |
| **G2** | `dev-impact` | T16 · D2 — URL Analytics (FA-023) **đếm lượt bấm** | Dev tự nêu ma trận test (c) "URL ngoài VẪN được rút gọn **và đếm lượt bấm**". **Không TC nào mở màn 「URL分析」 kiểm số đếm.** D2 nói mọi URL cùng miền hệ thống thôi rút gọn → **mất số liệu**, không TC nào đo mức mất | **GAP** | `[BLOCKER]` |
| **G3** | `dev-impact` | T3 — Step Delivery / Scenario (FA-009), nút gửi thử qua `ChatHelper` | Đây là **phạm vi MỚI** phát sinh do DEV CHỈ ĐẠO sửa thêm `ChatMessages.php` (bản #135377). **0 TC**. Nguy cơ regression High | **GAP** | `[BLOCKER]` |
| **G4** | `dev-impact` | T4 — Broadcast (FA-008), nút gửi thử qua `ChatHelper` (cùng route với T3) | Cùng lý do G3. **0 TC**. Nguy cơ regression High | **GAP** | `[BLOCKER]` |
| **G5** | `diff code` | F1 · F2 — **gửi template THẬT** (không phải test send) | Studio `dev_impact` ghi rõ: "builder tin template — ảnh hưởng **CẢ test send lẫn gửi template thật**". 15 TC chỉ phủ **test send · chat · open-mobile**; không TC nào gửi template thật qua action / scenario / broadcast tới friend | **GAP** | `[BLOCKER]` |
| **G6** | `dev-impact` | D2 — URL **có setting action** mà thôi được rút gọn | Kho **TC-TMT-138** (rule "action-thắng-checkbox"): URL có setting action **phải bị shorten để hệ thống bắt action**; **TC-TMT-158/159/160/161** cho thấy action click chạy qua `url_shorten.action`. Sau fix, link dựng từ mã + URL cùng miền **không còn bản ghi rút gọn** → **action click và số đếm có thể không chạy**. **0 TC** verify | **GAP** | `[BLOCKER]` |
| **G7** | `dev-impact` | F11 · T8–T12 — 15 nơi gọi `replaceMessageText`: Form+Point, Lesson (FA-019), Salon (FA-020), Event (FA-021), Sales (FA-026) | Dev đã kê danh sách caller (mục 3 file 03) nhưng **0 TC** test bất kỳ nơi nào. `replaceMessageText` nay **phụ thuộc ngầm** cờ `$bot->is_shorten_url` → sai nếp là gửi đi nguyên mã | **GAP** | `[MAJOR]` |
| **G8** | `dev-impact` | F7 · T13 — Quick reply (2 bản `detectContent`: TemplateService + QuickReplyService) | **0 TC**. Dev tự nêu rủi ro: nhánh quick reply đổi từ `$line_user_info['name']` sang `$lineUser->name` — **"cần xác nhận hai giá trị luôn như nhau"**, chưa ai xác nhận | **GAP** | `[MAJOR]` |
| **G9** | `dev-impact` | F8 · T14 — Rich message / image map (`makeImageMapVer2` + `ChatHelper::makeImageMap`) | URL gắn trong vùng bấm của ảnh và nút đi qua đúng logic vừa đổi thứ tự. **0 TC** | **GAP** | `[MAJOR]` |
| **G10** | `dev-impact` | F9 · T15 — `sendMessageAction` · `HelperService::sendAction` · `LiffController::sendMessageAction` | NEW-26 chỉ phủ `executeActionOpenMobile`. 3 đường gửi theo hành động còn lại **0 TC** | **GAP** | `[MAJOR]` |
| **G11** | `diff code` | F5 · T2 — cờ **thứ 3**: checkbox 「URL gốc」 (per-URL) | Ma trận NEW-6 chỉ có **2 chiều** (cờ bot × cờ template). Kho **TC-TMT-136/137/138/139** cho thấy còn cờ per-URL 「URL gốc」 quyết định rút gọn. **0 TC** phủ chiều thứ 3 | **GAP** | `[MAJOR]` |
| **G12** | `dev-impact` | F10 · D3 · `BUG` (c) — preview / history **phía web** (`generateContentPreview` / `generateCode` :2072/:2184, `ChatController` :6042-6110) | Root cause (c): web **có ghi** `replace_content` nhưng ghi **bản đầy đủ, khác link rút gọn đã gửi**. Chỉ NEW-14 (bước 6–7) chạm preview và **chỉ phía job**. Đây là **nửa còn lại của câu hỏi khách** (preview text khác nhau) | **RISK** | `[MAJOR]` |
| **G13** | `diff code` | F3 — 3 nhánh trong `getTemplate` | Studio `dev_impact`: "`template_url_redirect` + `is_shorten_url==1` vs chỉ `is_shorten_url==1` vs tắt rút gọn — **3 nhánh**". NEW-6 bước 3 chỉ chạy nhánh redirect **"nếu môi trường hỗ trợ"** → điều kiện tuỳ nghi, không đảm bảo phủ | **RISK** | `[MAJOR]` |
| **G14** | `diff code` | F6 — `executeActionOpenMobile` **vs** `executeActionOpenMobileNormal`, **2 loop rút gọn** | Studio `dev_impact` nêu rõ 2 hàm + 2 loop; 4 biểu thức sửa nằm ở `:3289, :3292, :3616, :3619`. NEW-26 chỉ chạy **1 đường**, lại còn cho phép thay bằng gọi endpoint | **RISK** | `[MAJOR]` |
| **G15** | `dev-impact` | T5 · F4 — Chat 1:1 web (gửi mẫu tin + gửi text) | **Leader đã chốt 2026-09-09**: LINE hiện URL **rút gọn**, chat 1:1 hiện **URL gốc**. Expected của **NEW-17** và **NEW-18** ("chat admin khớp LINE") **SAI** → coverage 2 TC này khai cho T5/REQ-005 **không hợp lệ** cho tới khi sửa trên Studio (§4-5). Còn lại NEW-23 mang **mã Studio lạ** nên cũng không tính | **GAP** | `[BLOCKER]` |
| **G17** | `diff code` | F5 · D1 · T16 — **phân loại link theo DOMAIN**: nhánh chặn rút gọn chỉ so với **`BASE_URL`** (1 domain) + `domain_url_shorten` của bot | Leader xác nhận (2026-09-09) hệ thống LME có **≥ 4 domain nội bộ phải KHÔNG shorten**: `step.lme.jp` · `s.lmes.jp` · `sl.lmes.jp` · `form.lmes.jp` (+ `step3.lmes.jp` form cũ theo `MAP-LIFF-04`/RULE-09). **Toàn bộ 15 TC Studio dùng placeholder `<miền hệ thống>` SỐ ÍT — không TC nào liệt kê từng domain.** Nếu guard chỉ khớp `BASE_URL` thì **3 domain còn lại vẫn bị shorten** → bug chỉ fix được một phần. Ngoài ra biểu thức đã sửa dùng `stripos` (**so khớp chuỗi con**) → nghi ngờ nhánh so domain cũng vậy, dễ **phân loại nhầm** URL ngoài có chứa tên domain nội bộ trong tham số | **GAP** | `[BLOCKER]` |
| **G16** | `dev-impact` | T7 — Action Settings (SC-004) / Action Schedule (FA-016) | Chỉ NEW-26 (open-mobile). Tin gửi khi **chạy hành động theo lịch** và `TemplateService::sendMessageAction` chưa có TC | **RISK** | `[MAJOR]` |

> **Adversarial Q1** (mỗi file trong diff có TC đi qua?) — ✅ cả 3 file (`TemplateService.php`, `ChatMessages.php`, `UserController.php`) đều có ≥ 1 TC chạm.
> **Adversarial Q2** (nhánh mới, generic-fix ≥ 3 trigger) — ❌ nhánh `catch` mới có **0 trigger** → **G1 `[BLOCKER]`**.
> **Adversarial Q3** (hàm dùng chung có danh sách caller + TC từng nơi?) — Dev **CÓ** kê danh sách, nhưng TC chỉ phủ 4/19 nơi → **G5, G7, G8, G9, G10**.
> **Adversarial Q4** (hành vi đổi: verify cả mới lẫn cũ không hỏng?) — hành vi MỚI có TC (NEW-4), chiều "cũ không hỏng" **thiếu vế đo lường** (đếm click + action click) → **G2, G6**.

---

## 2. Quan điểm test còn thiếu

**Kết luận: 6/19 quan điểm khớp trigger đã cover đủ · 13 thiếu.**

| Mã | Quan điểm | Ưu tiên | Vì sao thiếu | Status | Severity |
|---|---|---|---|---|---|
| **Q1** | `OUT-PREVIEW-001` | **Cao** (BẮT BUỘC khi có preview / test send) | **Không TC nào mang mã này**, dù ticket là 100% về **test send + preview**. Preview phía web (`replace_content` ghi bản đầy đủ ≠ link đã gửi) không TC nào verify | **GAP** | `[BLOCKER]` |
| **Q2** | `DATA-COUNT-001` | **Cao** (BẮT BUỘC khi màn hình có con số đếm / tổng hợp) | Màn 「URL分析」 có số đếm lượt bấm; D2 nói số này **giảm** sau fix. **0 TC** | **GAP** | `[BLOCKER]` |
| **Q3** | `LIFF-ENTRY-001` ★ | **Cao** (BẮT BUỘC khi phát sinh URL cho LINE user access) | NEW-6 chỉ kiểm `[FORM_x]`/`[CONVERSION_x]`/`[ITEM_x]` **"ra URL thật, không còn ngoặc vuông"** — **không mở link**, không xác nhận form/conversion/item load đúng và ghi nhận đúng người | **GAP** | `[BLOCKER]` |
| **Q4** | `FRIEND-001` | **Cao** (BẮT BUỘC khi dùng friend info làm biến) | Chỉ NEW-23 chạm, mà NEW-23 mang **mã Studio lạ `TOOL-AXIS-001`** → theo quy tắc **không tính là cover**. **REQ-009** (giá trị friend info **chứa URL** → bị rút gọn ngoài dự kiến) có **0 TC** | **GAP** | `[BLOCKER]` |
| **Q5** | `FUNC-003` | **Cao** (nâng — field liên quan **URL**) | Fix đổi chính vùng **nhận dạng URL** + thứ tự xử lý, nhưng **0 TC Boundary** với dạng URL biên (URL cuối câu có dấu chấm, URL trong 「」, 2 URL liền nhau, URL rất dài, URL nhúng biến) | **GAP** | `[MAJOR]` |
| **Q6** | `REG-SHARED-001` | **Cao** | Có 5 TC nhưng **cả 5 đều `Normal`** — thiếu `Abnormal` + `Boundary` (**RULE-01**, không TC nào ghi lý do). 5 TC lại tập trung vào **cùng 1 biểu thức `/landing-qr/`** ở 3 file, bỏ trống 15 caller (§1 G7) | **RISK** | `[MAJOR]` |
| **Q7** | `ENV-003` ★ | **Cao** (BẮT BUỘC khi chạm URL/domain, job nền) | Có 2 TC nhưng **0 TC chạy production**; `envAuto` cho thấy `runs = 0` ở cả 4 env. **RULE-08**: domain / job / URL **không được kết luận từ local/staging** | **RISK** | `[MAJOR]` |
| **Q8** | `MSG-004` | **Cao** | Chỉ 1 TC (NEW-17), `Normal` — thiếu `Abnormal` + `Boundary` (RULE-01) | **RISK** | `[MAJOR]` |
| **Q9** | `OUT-TRUTH-001` | **Cao** | 3 TC (NEW-2, 14, 15) **đều `Normal`** — thiếu `Abnormal` + `Boundary` (RULE-01) | **RISK** | `[MAJOR]` |
| **Q10** | `COMPAT-LEGACY-001` ★ | **Cao** | Chỉ NEW-30 (`Normal`). Thiếu nhánh template **đời cũ chứa URL đã bị xóa khỏi bảng `url`** — kho đã có **TC-TMT-163/164** cho đúng vùng này (RULE-09) | **RISK** | `[MAJOR]` |
| **Q11** | `DATA-CACHE-001` | **Cao** (nâng — output user-facing) | Trigger nêu đích danh "chức năng dùng **short link** / preview". Fix đổi cả 2. Không TC nào mang mã này; NEW-30 chỉ phủ redirect link cũ | **RISK** | `[MAJOR]` |
| **Q12** | `JOB-001` ★ | **Cao** | Job Java (`MessageBuilder.replaceFriendInfo` — nơi **DUY NHẤT** ghi `mapReplaceContent`, **không có LANDING_INTRO**) là gốc của root cause (c) và **không được fix**. Chỉ NEW-14 chạm, và chỉ ở mức "ghi nhận nếu còn raw code" | **RISK** | `[MAJOR]` |
| **Q13** | `DATA-REF-001` | **Cao** (BẮT BUỘC khi đối tượng có thao tác **xóa** VÀ được nơi khác tham chiếu) | QR action / biểu mẫu / sản phẩm bị **xóa** trong khi template vẫn tham chiếu mã của chúng — **0 TC**. Leader xác nhận hành vi đúng là **bỏ mã lúc gửi tin**, nhưng không TC nào verify, cũng không TC nào kiểm **một mã hỏng có làm hỏng các mã khác trong cùng tin hay không** | **GAP** | `[BLOCKER]` |

> Quan điểm đã cover đủ (không ghi dòng): `FUNC-001` · `FUNC-MULTI-001` · `DATA-TEXT-001` · `INTG-LINE-001` · `MSG-USER-001` · `SYNC-APP-001`.

---

## 3. TC trùng lặp

**Đã rà đủ 15 TC theo 4 yếu tố** (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`) — **không phát hiện trùng lặp nội bộ** (`DUP-EXACT` / `DUP-SUBSET` / `DUP-INFLATE` / `DUP-CONFLICT` trong bộ = 0).

| Nhóm | TC giữ lại | TC đề nghị xóa/gộp | Loại | 4 yếu tố | Severity |
|---|---|---|---|---|---|
| — | — | **Không đề nghị xóa/gộp TC nào** | — | — | — |

**Ghi nhận chồng lấn một phần (KHÔNG đề nghị xóa):**

| Cặp | Chồng lấn | Vì sao vẫn giữ cả hai |
|---|---|---|
| NEW-1 ↔ NEW-6 (lần chạy `bot BẬT` + `template BẬT`) | Cùng kiểm link giới thiệu giữ dạng đầy đủ khi 2 cờ đều BẬT | NEW-1 là **TC duy nhất mở link thật** và verify trang giới thiệu + `u_code`; xóa sẽ mất cover `LIFF-ENTRY-001` |
| NEW-3 ↔ NEW-26(A) · NEW-4 ↔ NEW-26(B) | Cùng kiểm hai chiều của biểu thức `/landing-qr/` | **Khác đối tượng + thao tác** (test send qua `TemplateService` vs open-mobile action qua `UserController`) → không phải trùng; đây là 2 file khác nhau trong diff |

⚠️ **Cảnh báo phân bố, không phải trùng lặp**: 5/15 TC (NEW-3, 4, 5, 18, 26) dồn vào **một biểu thức 1 dòng** (`stripos(..., '/landing-qr/') === false`), khiến `REG-SHARED-001` **trông như đã phủ dày**, trong khi **19 nơi gọi hàm dùng chung** chỉ được phủ 4. Hệ quả đã ghi ở **§1 G5/G7/G8/G9/G10** và **§2 Q6** — không hạ số TC, mà **bổ sung** ở §5.

**Trùng/mâu thuẫn LIÊN NGUỒN (Studio vs kho-tcs)** — không xóa, đẩy lên §6:

| Nhóm | Studio | kho-tcs | Loại | Severity |
|---|---|---|---|---|
| Hiển thị URL ở màn chat admin | **NEW-17**, **NEW-18** — expected: "khung chat admin **KHỚP** tin thực nhận trên LINE" → ❌ **SAI** | **TC-CHT-183**, **TC-CHT-186** (`Đã hỏi leader`) — "chat 1:1 và màn preview hiện **URL GỐC**, app LINE hiện **URL RÚT GỌN**" → ✅ **ĐÚNG, Leader xác nhận lại 2026-09-09** | **DUP-CONFLICT — đã phân xử**: kho đúng, Studio sai | `[BLOCKER]` |
| URL có setting action | Không TC nào | **TC-TMT-138** (`Abnormal`) — "URL **CÓ setting action** → **vẫn bị shorten** để hệ thống bắt action" | **CONFLICT với hệ quả fix** (D1/D2) | `[MAJOR]` |

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | Toàn bộ 15 TC | **0 TC đã chạy** — `exec: 0 pass / 0 fail / 15 untested`, `last_exec = null` ở cả 15 TC; `ranAt = null`. Tỷ lệ `pass` = **0% (< 50%)** | Chạy bộ TC trước khi Leader duyệt. Cho tới lúc đó **mọi vùng `OK` ở §1 chỉ là "có TC", không phải "đã verify"** |
| 2 | `[MAJOR]` | Toàn bộ 15 TC | **RULE-08 / ENV-003** — task chạm **URL/domain + job nền**, nhưng `envAuto` cho thấy `runs = 0` ở **cả 4 env** (local/dev/staging/prd). Ngoài ra 14/15 TC gắn `env_tag = local-only` trong khi NEW-14/15/16 khai `env_scope = dev, staging` → **mâu thuẫn `env_tag` ↔ `env_scope`** | Chốt lại `env_scope`; bắt buộc chạy nhóm domain / job / URL analytics trên **production** (xem TC-ENV003-03 ở §5) |
| 3 | `[MAJOR]` | NEW-1, NEW-23 | **2 mã quan điểm không có trong `framework/checklist-lme.md`**: `TOOL-KNOW-002` (NEW-1), `TOOL-AXIS-001` (NEW-23) — mã nội bộ Studio. Hệ quả: `FRIEND-001` bị tính **chưa cover** (§2 Q4) | Map lại: NEW-1 → `OUT-PREVIEW-001` hoặc `LIFF-ENTRY-001`; NEW-23 → `FRIEND-001`. Sửa trên Studio bằng `testcase_update` |
| 4 | `[MAJOR]` | `03-dev-impact.md` | Có `Auto-filled: 2026-09-09 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" CHƯA tick** — F/D/T có thể thiếu hoặc map sai | Tester đọc lại journal #135377 trên Redmine và tick checkbox trước khi review có giá trị |
| 5 | `[BLOCKER]` | **NEW-17**, **NEW-18** | **EXPECTED SAI — đã chốt với Leader 2026-09-09**: hành vi đúng là **LINE hiện URL rút gọn, chat 1:1 của LME hiện URL GỐC** (khớp kho `TC-CHT-183` / `TC-CHT-186`). Hai TC này lại đặt expected **"khung chat admin KHỚP tin thực nhận trên LINE"** → chạy sẽ **báo fail trên hành vi đúng**, và nếu Dev "sửa" theo expected sai thì **tạo bug mới**. Coverage của REQ-005 / T5 mà 2 TC này khai là **không hợp lệ** cho tới khi sửa | **Sửa expected trên Studio** (`testcase_update` NEW-17 + NEW-18): tách rõ 2 vế — link giới thiệu **giống nhau** ở 2 phía; URL ngoài **cố ý khác nhau** (LINE rút gọn / chat gốc). Xem `TC-OUTPREVIEW001-01` §5 làm mẫu |
| 6 | `[MAJOR]` | Hệ quả D1/D2 | **SPEC-CONFLICT**: kho `TC-TMT-138` ghi rule **"action-thắng-checkbox"** (URL có setting action **phải** bị shorten để hệ thống bắt action). Sau fix, link dựng từ mã + URL cùng miền **không còn bản ghi rút gọn** → action click / đếm click có thể **không chạy** | Chốt với Dev/PM (§6-2). Chưa chốt thì **không kết luận pass** cho nhóm URL analytics |
| 7 | `[MINOR]` | Toàn bộ 15 TC | **RULE-02** — cột `Evidence thực tế` rỗng và `Ghi chú` **không ghi loại evidence bắt buộc** phải đính kèm (ảnh tin trên LINE / ảnh màn URL分析 / bản ghi DB) | Bổ sung loại evidence vào `Ghi chú` từng TC trước khi giao chạy |

### Chất lượng từng TC

| # | Severity | TC | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 8 | `[MAJOR]` | **NEW-6** | **Không atomic** — 1 TC gộp 3 lần chạy × 4 loại mã × external URL × nhánh `template_url_redirect`. Bước 3 lại có điều kiện **"nếu template có bản ghi chuyển hướng URL"** và tiền đề ghi **"nếu có thể tạo thì chuẩn bị"** → nhánh 3 của `getTemplate` (§1 G13) **có thể không bao giờ chạy** mà TC vẫn pass | Tách nhánh `template_url_redirect` thành TC riêng với tiền đề **bắt buộc**, không để tuỳ nghi |
| 9 | `[MAJOR]` | **NEW-26** | **Không tái lập nhất quán** — bước 2 cho phép "kích hoạt bằng đường thật trên LINE **nếu có thể**; nếu không thì gọi đúng endpoint". Hai đường đi qua code khác nhau; người chạy khác nhau sẽ ra kết luận khác nhau. Ngoài ra chỉ phủ 1 trong 2 hàm `executeActionOpenMobile` / `...Normal` (§1 G14) | Chốt **một** đường thực thi; tách TC cho hàm còn lại |
| 10 | `[MAJOR]` | **NEW-16** | `Kết quả mong đợi` **không đo lường được** — nội dung là "kỳ vọng ... nhưng CẢNH BÁO ... nếu khác thì KHÔNG kết luận pass, chuyển Dev xác nhận". TC không có tiêu chí pass/fail | Chốt hành vi đúng trước (§6-3), rồi viết lại expected thành giá trị cụ thể |
| 11 | `[MAJOR]` | **NEW-30** | Tiền đề phụ thuộc may rủi — "nếu không có đường dẫn đời cũ nào thì **trả skip**". `COMPAT-LEGACY-001` là quan điểm **Cao**; skip = không có kết luận (RULE-09) | Chuẩn bị sẵn dữ liệu link đời cũ như **điều kiện bắt buộc** của vòng test, không cho skip |
| 12 | `[MINOR]` | NEW-1, 3, 4, 5, 26 | `Dữ liệu test` dùng placeholder cho field nghiệp vụ: `abc123`, `DUMMYUCODE`, `example.com`, `<mã liff>` | Thay bằng mã QR / `u_code` / domain thật của env test để người chạy không phải tự suy |
| 13 | `[AP-3]` `[MAJOR]` | Toàn bộ bộ TC | **Happy-path-only regression** — 13/15 `Normal`, 2 `Abnormal`, **0 `Boundary`**; 5 quan điểm ưu tiên **Cao** đều thiếu loại case mà **không TC nào ghi lý do** (RULE-01) | Bổ sung `Abnormal` + `Boundary` theo §5 |

---

## 5. TCs đề xuất bổ sung (20)

> **Đã đối chiếu 15 TC ở BƯỚC 0 + `kho-tcs/fa010-mautinnhan-テンプレート.md` + `kho-tcs/fa001-chat11-11チャット.md` — không TC đề xuất nào trùng.**

**Dùng lại TC kho (KHÔNG viết mới):**

| GAP | Dùng lại | Cần chỉnh gì cho bug này |
|---|---|---|
| G11 | `TC-TMT-136` · `TC-TMT-137` · `TC-TMT-139` | Chạy lại nguyên trạng làm **regression cờ 「URL gốc」**. `TC-TMT-139` ("URL trong hệ thống → giữ nguyên link") chính là hành vi **sau fix** — trước fix TC này lẽ ra fail; xác nhận nay pass |
| G12 · Q1 | `TC-CHT-213` · `TC-TMT-171` | Bổ sung nội dung có `[LANDING_INTRO_x]` vào template test; `TC-TMT-171` đã có sẵn khung "preview ở **cả 4 đường gửi**" |
| Q10 | `TC-TMT-163` · `TC-TMT-164` | Chạy lại nguyên trạng — template đời cũ chứa URL (còn / đã bị xóa khỏi bảng `url`) |
| G7 | `TC-CHT-185` | Đã phủ "friend info + URL rút gọn trong cùng 1 tin"; chạy lại sau fix vì **thứ tự thay thế đã đổi** |
| Q4 | `TC-CHT-176` | Đã phủ "friend info chưa có giá trị → để trống, không hiện mã thô"; chạy lại sau fix |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-DATAREF001-01 | UI | DATA-REF-001 | Mẫu tin nhắn — gửi thử | Abnormal | manual | Tất cả | Mã giới thiệu trỏ tới QR **đã bị xóa** → **bỏ mã** khi gửi, phần còn lại nguyên vẹn | Bot **BẬT** rút gọn URL **và** template **BẬT** rút gọn URL (ghim cả 2 cờ — dạng URL ngoài trong expected đổi theo cờ). Có 1 QR action đang hoạt động, đã lấy mã. Có 1 biểu mẫu đang hoạt động, đã lấy mã. Có 1 template text chứa **cả 2 mã** + 1 câu chữ + 1 URL ngoài. Có 1 tester là bạn của bot | 1. Gửi thử template, xác nhận tin bình thường và **cả 2 mã đều thành link**<br>2. Vào màn 「QRコードアクション」 xóa QR action đang được template tham chiếu<br>3. Gửi thử lại chính template đó cho tester<br>4. Chép **nguyên văn** tin nhận được trên LINE<br>5. Lặp lại bước 3–4 nhưng gửi từ **màn chat 1:1** (đường đi qua bản sao thứ hai `ChatMessages.php`) | Template: 「ご案内です。詳しくはこちら [FORM_&lt;mã biểu mẫu&gt;] / [LANDING_INTRO_&lt;mã QR sẽ bị xóa&gt;] 参考: https://example.com/guide」 | Tin gửi đi: **(1)** `[LANDING_INTRO_...]` **BỊ BỎ** — không còn chuỗi mã trong tin, cũng không thành link; **(2)** `[FORM_...]` **vẫn thành link đầy đủ** và mở đúng biểu mẫu — một mã hỏng **không được** làm hỏng mã khác; **(3)** chữ 「ご案内です。詳しくはこちら」 và 「参考:」 **còn nguyên**; **(4)** URL ngoài **đã rút gọn** thành `<miền rút gọn>/l/<khoá>`; **(5)** tin **KHÔNG rỗng**. Kết quả **giống nhau ở cả 2 đường** (gửi thử và chat 1:1) | | Lấp Q13 · Evidence: ảnh tin trên LINE của cả 2 đường + ảnh trang biểu mẫu mở từ link · Môi trường: staging · Đánh giá spec: **Đã hỏi leader — "mã đã xóa thì bỏ mã lúc gửi tin"** · ⚠️ TC này **KHÔNG lấp G1**: "bỏ mã" là đường xử lý **thường** (giải mã không ra → thay bằng rỗng), không đi qua nhánh `catch` của REQ-007 |
| TC-FUNC002-01 | UI | FUNC-002 | Chat 1:1 — gửi tin văn bản | Abnormal | manual | Tất cả | Mã **sai định dạng / không tồn tại** → **bỏ mã** khi gửi, tin vẫn còn nội dung | Bot **BẬT** rút gọn URL. Có cuộc chat 1:1 với 1 friend đã có mã định danh | 1. Trong ô soạn tin, gõ nội dung chứa mã giới thiệu với chuỗi mã **không tồn tại** trong bot → gửi<br>2. Chép nguyên văn tin trên LINE **và** trong khung chat<br>3. Lặp lại với mã chứa **ký tự lạ**<br>4. Lặp lại với mã **thiếu dấu ngoặc đóng**<br>5. Đối chiếu 3 lần gửi | (1) 「テストです [LANDING_INTRO_ZZZZZZ] 終わり」<br>(2) 「テストです [LANDING_INTRO_!!!] 終わり」<br>(3) 「テストです [LANDING_INTRO_abc 終わり」 | Với **(1)** và **(2)**: **mã BỊ BỎ** khỏi tin, chữ 「テストです」 và 「終わり」 **còn nguyên**, tin **không rỗng**, **không** hiện lỗi hệ thống. Với **(3)** — chuỗi thiếu ngoặc đóng nên không phải mã hợp lệ: **giữ nguyên như người dùng gõ**, tuyệt đối **không** bỏ nhầm phần chữ phía sau. Cả 3 lần gửi đều không làm tin rỗng | | Lấp Q13 · Q5 · Evidence: ảnh 3 tin trên LINE · Môi trường: staging · Đánh giá spec: **Đã hỏi leader — "mã sai định dạng thì bỏ mã lúc gửi tin"**; riêng nhánh **(3) thiếu ngoặc đóng chưa được chốt** — kết quả khác thì ghi lại và hỏi Leader, không tự kết luận · ⚠️ TC này **KHÔNG lấp G1** |
| TC-DATACOUNT001-01 | Data | DATA-COUNT-001 | URL分析 (FA-023) — đếm lượt bấm | Normal | manual | **product** | URL ngoài hệ thống vẫn rút gọn và **vẫn đếm đúng lượt bấm** sau fix | Bot BẬT rút gọn URL, template BẬT rút gọn URL. Có 1 template text chứa 1 URL ngoài hệ thống chưa từng dùng. Ghi lại số lượt bấm hiện tại của URL đó ở màn 「URL分析」 | 1. Gửi thử template cho tester<br>2. Trên LINE, xác nhận URL ngoài ở **dạng rút gọn**<br>3. Bấm vào URL rút gọn 3 lần từ 3 thiết bị/tài khoản khác nhau<br>4. Mở màn 「URL分析」, tìm URL vừa gửi<br>5. Đối chiếu số lượt bấm trước và sau | 1 URL ngoài: `https://example.com/campaign-40598`; bấm 3 lần | URL ngoài **bị rút gọn** như trước fix. Màn 「URL分析」 hiện đúng URL và số lượt bấm **tăng đúng 3**. Bấm link mở đúng trang đích | | Lấp G2 · Q2 · Evidence: ảnh màn 「URL分析」 trước/sau + ảnh tin trên LINE · **RULE-08** → bắt buộc product · Đánh giá spec: Đã hỏi leader — Dev ma trận test (c) |
| TC-REGURL001-01 | UI | REG-URL-001 | Mẫu tin nhắn — URL表示期限・アクション設定 | Abnormal | manual | **product** | URL **có setting action** sau fix — action khi bấm còn chạy không | Bot BẬT rút gọn URL. Có 1 template text chứa 2 URL: (A) URL ngoài **đã setting action** khi click = gắn tag T; (B) URL trỏ về miền hệ thống **đã setting action** = gắn tag T2. Friend tester chưa có tag T và T2 | 1. Gửi thử template cho tester<br>2. Trên LINE, ghi lại dạng của A và B (rút gọn hay đầy đủ)<br>3. Bấm A → kiểm tra tag của tester<br>4. Bấm B → kiểm tra tag của tester<br>5. Mở màn friend detail đối chiếu tag đã gắn | A = URL ngoài + action gắn tag T; B = URL miền hệ thống + action gắn tag T2 | A vẫn **bị rút gọn** và bấm A **gắn được tag T** (giữ nguyên rule kho `TC-TMT-138`). Với B: ghi lại **chính xác** B ở dạng nào và tag T2 **có được gắn hay không**. Nếu B ở dạng đầy đủ mà **tag T2 không gắn** → đây là **regression do fix**, ghi ticket và **KHÔNG kết luận pass** | | Lấp G6 · Evidence: ảnh tin trên LINE + ảnh tag ở màn friend · **RULE-08** → product · Đánh giá spec: **Spec không ghi — mâu thuẫn kho TC-TMT-138, xem §6-2** |
| TC-OUTPREVIEW001-01 | UI | OUT-PREVIEW-001 | Chat 1:1 / lịch sử tin — preview nội dung đã gửi | Normal | manual | Tất cả | LINE hiện URL ngoài **rút gọn**, chat 1:1 hiện **URL gốc** — riêng link giới thiệu **giống nhau** ở 2 phía | Bot **BẬT** rút gọn URL **và** template **BẬT** rút gọn URL. Có template text chứa `[LANDING_INTRO_x]` + 1 URL ngoài. Có tester là bạn của bot, đã có mã định danh | 1. Gửi thử template cho tester<br>2. Chép **nguyên văn** link giới thiệu và URL ngoài trong tin nhận trên **LINE**<br>3. Mở chat 1:1 của tester trên tool, tìm tin vừa gửi, chép nguyên văn 2 URL<br>4. Mở màn xem lại **lịch sử tin** của tester, chép lần thứ ba<br>5. Đối chiếu 3 bản theo bảng expected bên dưới<br>6. Bấm URL rút gọn trên LINE, xác nhận mở đúng trang đích | Template: 「紹介: [LANDING_INTRO_&lt;mã QR&gt;]\n資料: https://example.com/guide」 | **Link giới thiệu** — giống nhau ở **cả 3 nơi**: `<miền>/landing/page-intro/<mã QR>/<mã định danh tester>`, dạng **đầy đủ**, không nơi nào còn chuỗi `[LANDING_INTRO_...]` nguyên trạng.<br>**URL ngoài** — **CỐ Ý KHÁC NHAU**: LINE hiện **rút gọn** `<miền rút gọn>/l/<khoá>`; chat 1:1 và lịch sử tin hiện **URL GỐC** `https://example.com/guide`. Đây là hành vi **ĐÚNG**, **KHÔNG** được báo fail.<br>Bấm URL rút gọn trên LINE mở đúng `https://example.com/guide` | | Lấp G12 · Q1 · Evidence: ảnh 3 nơi (LINE, chat 1:1, lịch sử tin) · Môi trường: staging · Đánh giá spec: **Đã hỏi leader (2026-09-09)** — khớp kho `TC-CHT-183` / `TC-CHT-186`; spec `BR-15` chưa ghi, xem §6-1 |
| TC-OUTPREVIEW001-02 | UI | OUT-PREVIEW-001 | Lịch sử tin — tin sinh từ job (chạm rich menu) | Abnormal | manual | **product** | Tin do job gửi: preview/lịch sử **còn hiện nguyên chuỗi mã giới thiệu** hay không | Môi trường có chạy tiến trình job. Có rich menu gắn action gửi template chứa `[LANDING_INTRO_x]`. Dùng **cùng 1 tester** cho cả bài | 1. Trên LINE của tester, chạm nút rich menu gắn action gửi template<br>2. Chép nguyên văn tin nhận được trên LINE<br>3. Mở chat 1:1 và màn lịch sử tin của chính tester trên tool<br>4. Tìm tin sinh từ rich menu, chép nguyên văn nội dung đang hiển thị<br>5. So sánh với bản ở bước 2 | Template: 「紹介: [LANDING_INTRO_&lt;mã QR&gt;]」 | Ghi nhận rõ: preview/lịch sử của tin do **job** gửi **có còn chuỗi `[LANDING_INTRO_...]` nguyên trạng hay không**. Theo root cause (c) và phạm vi fix (chỉ web/PHP), **dự kiến VẪN còn raw code** → nếu đúng vậy thì đây là **phần chưa fix của yêu cầu khách** (khách hỏi cả về preview), phải ghi ticket riêng, **không đóng ticket 40598 là hoàn tất** | | Lấp G12 · Q1 · Q12 · BUG(c) · Evidence: ảnh LINE + ảnh lịch sử tin · **RULE-08** → product (job nền) · Đánh giá spec: **Spec không ghi — xem §6-4** |
| TC-OUTPREVIEW001-03 | UI | OUT-PREVIEW-001 | Mẫu tin nhắn — preview trước khi gửi | Boundary | manual | Tất cả | Tin chứa **đủ 4 loại mã + nhiều URL** → preview và tin thật khớp nhau | Có sẵn 1 QR action, 1 biểu mẫu, 1 mã chuyển đổi, 1 sản phẩm của bot. Bot BẬT rút gọn URL | 1. Tạo template text chứa đủ 4 mã + 2 URL ngoài + 1 URL miền hệ thống, đặt các mã **liền nhau không có dấu cách** và 1 mã ở **cuối tin không có ký tự sau**<br>2. Mở màn preview của template, chép nguyên văn<br>3. Gửi thử cho tester, chép nguyên văn tin trên LINE<br>4. So sánh từng URL giữa preview và tin thật | 「[FORM_x][CONVERSION_x] [ITEM_x] https://example.com/a https://example.com/b &lt;miền hệ thống&gt;/basic/landing 紹介:[LANDING_INTRO_x]」 | **Không mã nào còn nguyên dạng ngoặc vuông** ở cả preview lẫn tin thật, kể cả mã nằm liền nhau và mã ở cuối tin. Số URL trong preview bằng số URL trong tin thật. Không URL nào bị ghép dính hoặc mất ký tự | | Lấp Q1 · Q5 (RULE-01 Boundary) · Evidence: ảnh preview + ảnh tin trên LINE · Môi trường: staging |
| TC-LIFFENTRY001-01 | UI | LIFF-ENTRY-001 | Mẫu tin nhắn — mã sinh URL cho LINE user | Normal | manual | **product** | 4 loại mã → **mở link thật**, trang đích load đúng và ghi đúng người | Có 1 QR action, 1 biểu mẫu, 1 mã chuyển đổi, 1 sản phẩm của bot. Bot + template BẬT rút gọn URL. Tester là bạn của bot và đã có mã định danh | 1. Gửi thử template chứa đủ 4 mã cho tester<br>2. Trên LINE, bấm lần lượt từng link<br>3. Với `[FORM_x]`: điền và gửi biểu mẫu → kiểm tra câu trả lời gắn đúng tester<br>4. Với `[CONVERSION_x]`: mở link → kiểm tra màn thống kê chuyển đổi ghi nhận<br>5. Với `[ITEM_x]`: mở link → trang sản phẩm hiện đúng<br>6. Với `[LANDING_INTRO_x]`: mở link → trang giới thiệu hiện đúng QR và ghi đúng người giới thiệu | Template chứa `[FORM_x] [CONVERSION_x] [ITEM_x] [LANDING_INTRO_x]` của các đối tượng đã chuẩn bị | Cả 4 link **mở được** trang đích đúng. Câu trả lời biểu mẫu gắn đúng tester; lượt chuyển đổi được ghi nhận; trang sản phẩm đúng; trang giới thiệu đúng QR và đúng người giới thiệu. **Không link nào dẫn tới màn đăng nhập hoặc trang lỗi** | | Lấp Q3 · Evidence: ảnh 4 trang đích + ảnh màn thống kê tương ứng · **RULE-08** → product · Đánh giá spec: Đã hỏi leader — Dev ma trận test (a)(b) |
| TC-LIFFENTRY001-02 | UI | LIFF-ENTRY-001 | Đặt lịch / bán hàng — mã hủy & đổi lịch | Abnormal | manual | **product** | Mã `[CANCEL_x]` / `[CHANGE_x]` vẫn ra link đúng và thao tác được | Có 1 booking (lesson hoặc salon) đã đặt của tester. Template tin xác nhận có chứa mã hủy và mã đổi lịch. Bot BẬT rút gọn URL | 1. Tạo 1 booking cho tester để hệ thống gửi tin xác nhận<br>2. Đọc tin xác nhận trên LINE, xác nhận 2 link hủy/đổi ở dạng đầy đủ hay rút gọn<br>3. Bấm link đổi lịch → đổi sang khung giờ khác → xác nhận<br>4. Bấm link hủy → hủy booking<br>5. Kiểm tra màn quản lý booking trên tool | Booking của tester; tin xác nhận chứa `[CANCEL_x]` và `[CHANGE_x]` | Hai link **không còn ở dạng ngoặc vuông** và **mở đúng** màn đổi lịch / hủy của **chính booking đó**. Đổi lịch và hủy thực hiện được, màn quản lý booking trên tool cập nhật đúng trạng thái | | Lấp Q3 · G7 · Evidence: ảnh tin trên LINE + ảnh màn booking trước/sau · **RULE-08** → product · Đánh giá spec: Đã hỏi leader — Dev ma trận test (b) |
| TC-FRIEND001-01 | UI | FRIEND-001 | Mẫu tin nhắn — biến trong URL | Normal | manual | Tất cả | `{name}` và friend info **nhúng trong URL** → thay giá trị thật rồi mới rút gọn | Bot BẬT rút gọn URL, template BẬT rút gọn URL. Tester có tên hiển thị xác định và đã điền 1 trường friend info dạng ký tự có giá trị xác định (không chứa URL) | 1. Tạo template text chứa 1 URL ngoài có **tham số mang biến**: `https://example.com/x?u={name}&m=[FRIEND_INFO_x]`<br>2. Gửi thử cho tester<br>3. Trên LINE, xác nhận URL ở dạng rút gọn<br>4. Bấm URL, quan sát thanh địa chỉ trang đích sau khi chuyển hướng<br>5. Đối chiếu tham số với giá trị thật của tester | `https://example.com/x?u={name}&m=[FRIEND_INFO_&lt;mã trường&gt;]`; tester tên 「テスト太郎」, trường ký tự = 「A123」 | URL sau khi chuyển hướng mang **giá trị thật** (`u=テスト太郎`, `m=A123`), **không** còn `{name}` hay mã friend info nguyên trạng. Đây là **thay đổi hành vi** so với trước fix (trước rút gọn khi còn nguyên mã) — ghi nhận rõ để đối chiếu | | Lấp Q4 · REQ-009 · Evidence: ảnh tin trên LINE + ảnh thanh địa chỉ trang đích · Môi trường: staging · Đánh giá spec: **Spec không ghi — Dev tự nêu là "thay đổi quan sát được"** |
| TC-FUNC003-01 | UI | FUNC-003 | Mẫu tin nhắn — dạng URL biên | Boundary | manual | Tất cả | Các dạng URL biên vẫn được nhận dạng và xử lý đúng sau khi đổi thứ tự | Bot BẬT rút gọn URL, template BẬT rút gọn URL. Tester là bạn của bot | 1. Tạo template text chứa 6 dạng URL biên, mỗi dạng 1 dòng<br>2. Gửi thử cho tester<br>3. Trên LINE, kiểm tra từng dòng: URL có bị cắt / dính ký tự / bỏ sót không<br>4. Bấm thử từng URL xem có mở đúng trang đích<br>5. Đếm số bản ghi rút gọn sinh ra, so với số URL ngoài trong tin | (1) URL cuối câu có dấu chấm `https://example.com/a.`<br>(2) URL trong 「」<br>(3) 2 URL liền nhau cách 1 dấu cách<br>(4) URL rất dài (> 200 ký tự)<br>(5) URL có `#anchor` và nhiều tham số<br>(6) URL ngay sát mã `[LANDING_INTRO_x]` không có dấu cách | Mỗi URL ngoài được nhận dạng **trọn vẹn**, không nuốt dấu chấm cuối câu vào URL, không dính ký tự 」, không gộp 2 URL liền nhau thành 1. Bấm từng URL mở đúng trang đích. Số bản ghi rút gọn sinh ra **bằng đúng số URL ngoài**. Mã `[LANDING_INTRO_x]` sát URL vẫn được dựng thành link đầy đủ | | Lấp Q5 (RULE-01 Boundary) · Evidence: ảnh tin trên LINE + ảnh 「URL分析」 · Môi trường: staging |
| TC-REGSHARED001-06 | UI | REG-SHARED-001 | Kịch bản (FA-009) — nút gửi thử | Normal | manual | Tất cả | Gửi thử từ màn kịch bản (qua `ChatHelper`) cho kết quả giống màn mẫu tin | Bot BẬT rút gọn URL. Có 1 kịch bản chứa 1 bước gắn template text có `[LANDING_INTRO_x]` + 1 URL ngoài + 1 URL `/landing-qr/`. Đã chạy trước TC gửi thử ở màn mẫu tin với **cùng template** và ghi lại kết quả | 1. Mở màn 「ステップ配信」, vào kịch bản đã chuẩn bị<br>2. Bấm nút gửi thử ở bước chứa template<br>3. Chọn tester và gửi<br>4. Chép nguyên văn tin nhận trên LINE<br>5. So sánh từng ký tự với tin từ màn mẫu tin | Cùng template với TC gửi thử ở màn mẫu tin, cùng tester | Link giới thiệu ở **dạng đầy đủ**; URL ngoài **bị rút gọn**; URL `/landing-qr/` **bị rút gọn**. Nội dung **trùng khớp** với tin gửi thử từ màn mẫu tin — hai bản sao hàm rút gọn cho **cùng kết quả** | | Lấp G3 · G5 · Q6 · Evidence: ảnh 2 tin trên LINE để đối chiếu · Môi trường: staging · Phạm vi MỚI do sửa `ChatMessages.php` |
| TC-REGSHARED001-07 | UI | REG-SHARED-001 | Gửi tin hàng loạt (FA-008) — nút gửi thử | Normal | manual | Tất cả | Gửi thử từ màn broadcast (cùng route với kịch bản) cho kết quả giống | Như TC trên, nhưng dùng 1 broadcast nháp chứa cùng template | 1. Mở màn 「メッセージ配信」, tạo/mở 1 broadcast nháp gắn template đã chuẩn bị<br>2. Bấm nút gửi thử<br>3. Chọn tester và gửi<br>4. Chép nguyên văn tin nhận trên LINE<br>5. So sánh với tin từ màn mẫu tin và màn kịch bản | Cùng template, cùng tester | Kết quả **giống hệt** 2 màn kia: link giới thiệu đầy đủ, URL ngoài rút gọn, URL `/landing-qr/` rút gọn. Ba đường gửi thử cho **cùng một chuỗi** | | Lấp G4 · G5 · Q6 · Evidence: ảnh 3 tin đối chiếu · Môi trường: staging · Phạm vi MỚI do sửa `ChatMessages.php` |
| TC-REGSHARED001-08 | UI | REG-SHARED-001 | Hành động — gửi template THẬT (không phải gửi thử) | Normal | manual | **product** | Template gửi thật qua action rich menu / nút bấm cho kết quả giống gửi thử | Bot BẬT rút gọn URL. Có rich menu gắn action gửi template đã chuẩn bị. Dùng **cùng 1 tester** đã dùng cho TC gửi thử | 1. Trên LINE của tester, chạm nút rich menu gắn action gửi template<br>2. Chép nguyên văn tin nhận<br>3. Gửi thử **cùng template** cho **chính tester đó** từ màn mẫu tin<br>4. Chép nguyên văn tin thứ hai<br>5. So sánh từng ký tự: miền, đường dẫn, mã QR, mã định danh, dạng URL ngoài | Cùng template, cùng tester, 2 đường gửi | Hai tin **trùng khớp**: cùng miền, cùng `/landing/page-intro/`, cùng mã QR, **cùng mã định danh** (vì cùng người nhận), URL ngoài xử lý cùng kiểu. Đây là điều khách hỏi — hai đường phải ra **cùng một kết quả** | | Lấp G5 · G16 · Evidence: ảnh 2 tin đặt cạnh nhau · **RULE-08** → product (job nền) · Bổ sung cho NEW-14: NEW-14 chỉ so **gửi thử ↔ rich menu**, TC này xác nhận **gửi thật ≡ gửi thử** |
| TC-REGSHARED001-09 | UI | REG-SHARED-001 | Trả lời nhanh (quick reply) | Abnormal | manual | Tất cả | Nút trả lời nhanh chứa mã và URL → nội dung và tên hiển thị đúng | Bot BẬT rút gọn URL. Có 1 template gắn quick reply, trong đó 1 nút có nội dung chứa `{name}`, 1 mã friend info và 1 URL ngoài. Tester có tên hiển thị xác định | 1. Gửi thử template có quick reply cho tester<br>2. Trên LINE, quan sát nhãn các nút trả lời nhanh<br>3. Bấm nút có chứa biến<br>4. Đọc tin phản hồi hệ thống gửi lại<br>5. Đối chiếu tên hiển thị trong tin phản hồi với tên thật của tester | Nút quick reply: 「[NAME]さん確認 https://example.com/qr」 | Tên hiển thị trong nội dung quick reply và tin phản hồi **đúng tên tester**, không còn mã nguyên trạng. URL trong nội dung xử lý đúng theo cờ rút gọn. **Đặc biệt xác nhận**: giá trị tên giống với tên hiển thị ở các đường gửi khác (Dev nêu rủi ro `$line_user_info['name']` → `$lineUser->name` chưa được xác nhận là luôn bằng nhau) | | Lấp G8 · Evidence: ảnh nút quick reply + ảnh tin phản hồi · Môi trường: staging · Đánh giá spec: **Spec không ghi — Dev tự nêu "cần xác nhận hai giá trị luôn như nhau"** |
| TC-REGSHARED001-10 | UI | REG-SHARED-001 | Rich message / image map | Normal | manual | Tất cả | URL gắn trong vùng bấm của ảnh và nút vẫn dựng đúng | Bot BẬT rút gọn URL. Có 1 template dạng image map với 2 vùng bấm: vùng A gắn URL ngoài, vùng B gắn mã `[LANDING_INTRO_x]`. Có 1 template dạng panel-button với nút gắn URL ngoài | 1. Gửi thử template image map cho tester<br>2. Trên LINE, bấm vùng A → quan sát trang mở ra và thanh địa chỉ<br>3. Bấm vùng B → quan sát trang giới thiệu và mã định danh trên URL<br>4. Gửi thử template panel-button, bấm nút → quan sát trang đích<br>5. Lặp lại toàn bộ khi bot **TẮT** rút gọn URL | Image map 2 vùng (A = URL ngoài, B = mã giới thiệu); panel-button 1 nút URL ngoài | Vùng A và nút mở đúng trang đích. Vùng B mở đúng **trang giới thiệu** với đúng mã QR và **đúng mã định danh của tester**, không phải trang lỗi. Khi bot TẮT rút gọn: các URL giữ dạng đầy đủ nhưng mã vẫn được dựng thành link đúng | | Lấp G9 · Evidence: ảnh trang đích của từng vùng bấm · Môi trường: staging |
| TC-REGSHARED001-11 | UI | REG-SHARED-001 | Biểu mẫu · Đặt lịch · Bán hàng — tin tự động | Normal | manual | **product** | 5 luồng dùng chung `replaceMessageText` vẫn dựng mã và URL đúng | Bot BẬT rút gọn URL. Chuẩn bị 5 luồng, mỗi luồng có tin trả lời chứa 1 mã (`[LANDING_INTRO_x]` hoặc `[FORM_x]`) + 1 URL ngoài: (1) biểu mẫu + tặng điểm, (2) đặt lịch bài học, (3) đặt lịch salon, (4) đặt lịch sự kiện, (5) mua sản phẩm | 1. Với từng luồng, thực hiện thao tác kích hoạt bằng tài khoản tester (gửi biểu mẫu / đặt lịch / mua)<br>2. Đọc tin tự động nhận được trên LINE<br>3. Kiểm tra mã đã thành link đầy đủ chưa và URL ngoài đã rút gọn chưa<br>4. Bấm link trong tin, xác nhận mở đúng trang<br>5. Ghi kết quả từng luồng vào bảng đối chiếu | 5 luồng × (1 mã + 1 URL ngoài) | **Cả 5 luồng**: không tin nào còn mã nguyên dạng ngoặc vuông; link dựng từ mã ở **dạng đầy đủ** và mở đúng trang; URL ngoài **bị rút gọn**. Nếu **bất kỳ luồng nào** gửi đi còn nguyên mã → đúng rủi ro Dev cảnh báo về phụ thuộc ngầm cờ `is_shorten_url`, ghi ticket ngay | | Lấp G7 · Q6 · Evidence: ảnh tin của cả 5 luồng · **RULE-08** → product (job nền + bill tiền ở luồng bán hàng) · Data-driven 5 luồng, ghi kết quả riêng từng luồng |
| TC-REGSHARED001-12 | UI | REG-SHARED-001 | Hành động mở trên mobile · LIFF | Boundary | manual | Tất cả | Cả **2 hàm** open-mobile và LIFF cùng xử lý URL đúng | Bot BẬT rút gọn URL. Chuẩn bị **2 đường vào công khai khác nhau** mà tester bấm được thật: **(I)** trang trung gian 「LINEアプリを開く」 — `/open-mobile/{type}/{id}` (`SCR-QRL-38`, mở LIFF) và **(II)** trang trung gian 「LINEアプリで続行」 — `/open-external-browser/{type}/{id}` (`SCR-QRL-39`, mở trình duyệt ngoài). Mỗi đường gắn 1 action trả về nội dung chứa 3 URL: **(A)** miền hệ thống có `/landing-qr/`, **(B)** miền hệ thống không có `/landing-qr/`, **(C)** URL ngoài. Chuẩn bị thêm 1 action LIFF gửi tin khi khách mở link. ⚠️ **Trước khi chạy phải hỏi Dev**: 2 đường này có thật sự đi qua 2 hàm `executeActionOpenMobile` và `executeActionOpenMobileNormal` không — nếu cả hai cùng đi 1 hàm thì tiền đề chưa phủ đủ 2 nhánh, xem §6-9 | 1. Trên LINE, mở đường **(I)** 「LINEアプリを開く」 để kích hoạt action<br>2. Ghi lại dạng của A, B, C trong tin nhận<br>3. Trên LINE, mở đường **(II)** 「LINEアプリで続行」 để kích hoạt action<br>4. Ghi lại dạng của A, B, C<br>5. Mở link LIFF để kích hoạt action gửi tin, ghi lại dạng A, B, C<br>6. Đối chiếu 3 đường | A/B/C như mô tả; 2 nhánh open-mobile + 1 nhánh LIFF | **Cả 3 nhánh cho cùng kết quả**: A **bị rút gọn** và mở đúng trang QR; B **giữ dạng đầy đủ**, không sinh bản ghi rút gọn; C **bị rút gọn**. Không nhánh nào để mã hoặc URL sai dạng | | Lấp G10 · G14 · Q6 (RULE-01 Boundary) · Evidence: ảnh tin của cả 3 đường · Môi trường: staging · Bổ sung cho NEW-26 (NEW-26 chỉ chạy 1 đường và cho phép thay bằng gọi endpoint) · Đánh giá spec: 2 trang trung gian lấy từ `spec-features/admin/qr-landing/feature-spec.md` `SCR-QRL-38` / `SCR-QRL-39` (EP-67 / EP-68, cùng dùng EP-69) · ⚠️ **Ánh xạ sang 2 hàm Dev nêu chưa xác nhận — xem §6-9** |
| TC-ENV003-04 | UI | ENV-003 | Mẫu tin nhắn — ma trận **phân loại link theo domain** | Normal | manual | **product** | **Từng domain nội bộ** đều KHÔNG shorten, link ngoài LUÔN shorten | Bot **BẬT** rút gọn URL, template **BẬT** rút gọn URL. Bot **KHÔNG** cấu hình miền rút gọn riêng (tách riêng nhánh đó ở NEW-5). Xác nhận môi trường có khai báo `BASE_URL` và **ghi lại giá trị cụ thể** của nó. Ghi lại số bản ghi rút gọn hiện có | 1. Tạo 1 template text chứa **mỗi domain 1 dòng**, gồm đủ 4 domain nội bộ + 1 link cũ + 2 link ngoài<br>2. Gửi thử cho tester<br>3. Trên LINE, ghi lại **dạng của TỪNG dòng** (đầy đủ hay `<miền rút gọn>/l/<khoá>`)<br>4. Đối chiếu với `BASE_URL` đã ghi ở tiền đề: domain nào **trùng** `BASE_URL`, domain nào **không**<br>5. Mở 「URL分析」 đếm số bản ghi rút gọn mới sinh<br>6. Bấm từng link, xác nhận mở đúng trang | (1) `https://step.lme.jp/basic/overview`<br>(2) `https://s.lmes.jp/<khoá form mới>`<br>(3) `https://sl.lmes.jp/<khoá>`<br>(4) `https://form.lmes.jp/<khoá>`<br>(5) `https://step3.lmes.jp/<khoá form cũ>`<br>(6) `https://example.com/a`<br>(7) `https://example.com/b?ref=abc` | Dòng **(1)–(5)**: **TẤT CẢ giữ dạng đầy đủ, KHÔNG bị shorten**, **không** sinh bản ghi rút gọn — **kể cả domain KHÔNG trùng `BASE_URL`**. Dòng **(6)–(7)**: **cả hai bị shorten**, sinh đúng **2** bản ghi mới. Mọi link bấm vào đều mở đúng trang. 🔴 Nếu **bất kỳ** dòng nào trong (1)–(5) bị shorten → guard chỉ phủ 1 domain, **bug chưa fix xong**, ghi ticket ngay | | Lấp G17 · Q7 · Evidence: ảnh tin trên LINE (thấy rõ 7 dòng) + ảnh 「URL分析」 trước/sau + giá trị `BASE_URL` · **RULE-08** → product (domain) · Đánh giá spec: **Đã hỏi leader (2026-09-09)** — danh sách 4 domain nội bộ · ⚠️ Xác nhận lại tên domain dòng (1) trước khi chạy, xem §6-8 |
| TC-ENV003-05 | UI | ENV-003 | Mẫu tin nhắn — domain gần giống / domain lồng trong tham số | Abnormal | manual | **product** | URL ngoài **có chứa tên domain nội bộ** không được phân loại nhầm thành link nội bộ | Như TC trên. Ghi lại số bản ghi rút gọn hiện có | 1. Tạo template text chứa 4 dòng URL bẫy<br>2. Gửi thử cho tester<br>3. Ghi lại dạng của từng dòng trên LINE<br>4. Đếm số bản ghi rút gọn mới sinh<br>5. Bấm từng link xác nhận đích đến | (1) `https://example.com/go?to=https://s.lmes.jp/abc` — URL **ngoài**, chứa domain nội bộ trong tham số<br>(2) `https://s.lmes.jp.example.com/x` — domain **ngoài** giả dạng<br>(3) `https://xs.lmes.jp/x` — domain **khác** nhưng chứa chuỗi con<br>(4) `https://s.lmes.jp/that` — domain nội bộ **thật** (đối chứng) | Dòng **(1)(2)(3) là URL NGOÀI → phải BỊ shorten** và sinh 3 bản ghi. Dòng **(4) là nội bộ → giữ đầy đủ**, không sinh bản ghi. 🔴 Nếu (1)(2)(3) **không** bị shorten → nhánh so domain đang dùng **so khớp chuỗi con** thay vì so đúng host → **mất số liệu lượt bấm** của link khách và có rủi ro phân loại sai; ghi ticket, chuyển Dev | | Lấp G17 · Evidence: ảnh tin trên LINE + ảnh 「URL分析」 · **RULE-08** → product · Đánh giá spec: **Spec không ghi — Dev cần xác nhận nhánh so domain dùng `stripos` hay parse host**, xem §6-8 |

---

### 5a. Truy vết — mọi `G<x>` / `Q<x>` phải có TC hoặc lý do

**22/30 mã có TC · 3 chỉ dùng lại TC kho · 2 không viết được TC (có lý do) · 3 TRỐNG do Leader loại TC.**

> 🔴 **Leader loại 10 TC ngày 2026-09-09**: `TC-DATACOUNT001-02` · `-03` · `TC-FRIEND001-02` · `TC-COMPATLEGACY001-02` · `-03` · `TC-ENV003-03` · `TC-MSG004-02` · `TC-OUTTRUTH001-04` · `TC-DATACACHE001-01` · `TC-REGSHARED001-13` · `TC-MSG004-03`. Hệ quả ghi ở các dòng bên dưới — **Q8, Q9 và Q11 nay không còn TC nào**.

| Mã | Nội dung rút gọn | Lấp bằng |
|---|---|---|
| G1 | Nhánh `catch` `makeLinkLandingPageIntro` | ❌ **KHÔNG viết được TC** — Leader đã chốt mã xóa / mã sai định dạng chỉ dẫn tới "bỏ mã" (đường thường), không chạm nhánh catch. **Chờ Dev trả lời §6-6** |
| G2 | URL Analytics — đếm lượt bấm | TC-DATACOUNT001-01 (chỉ còn vế **link ngoài vẫn đếm đúng**). ⚠️ Vế **D2 — đo mức mất số liệu của URL cùng miền** và **REQ-011 — bản ghi rút gọn trùng** đã bị Leader loại |
| G3 | Scenario (FA-009) gửi thử | TC-REGSHARED001-06 |
| G4 | Broadcast (FA-008) gửi thử | TC-REGSHARED001-07 |
| G5 | Gửi template THẬT | TC-REGSHARED001-08 |
| G6 | URL có setting action | TC-REGURL001-01 |
| G7 | 15 caller `replaceMessageText` | TC-REGSHARED001-11 |
| G8 | Quick reply | TC-REGSHARED001-09 |
| G9 | Rich message / image map | TC-REGSHARED001-10 |
| G10 | `sendMessageAction` · `sendAction` · LIFF | TC-REGSHARED001-12 |
| G11 | Cờ thứ 3 「URL gốc」 | Dùng lại kho `TC-TMT-136` · `-137` · `-139` |
| G12 | Preview / history phía web | TC-OUTPREVIEW001-01 · -02 |
| G13 | 3 nhánh `getTemplate` | ⚠️ **Chỉ còn dùng lại kho** `TC-TMT-151` · `-172` · `-173` — TC ép đủ 3 nhánh đã bị Leader loại; bước 3 tuỳ nghi của NEW-6 (§4-8) **vẫn chưa được thay** |
| G14 | `executeActionOpenMobile` vs `...Normal` | TC-REGSHARED001-12 |
| G15 | Chat 1:1 web — expected NEW-17/18 sai | ❌ **KHÔNG viết TC mới** — cách sửa là **`testcase_update` NEW-17 + NEW-18 trên Studio** (§4-5), dùng `TC-OUTPREVIEW001-01` làm mẫu expected. ⚠️ Riêng **nhánh "sửa nội dung ngay trước khi gửi"** (mục 4 của §1a) **vẫn trống** — chờ Leader mô tả thao tác thật |
| G16 | Action Settings / Schedule | TC-REGSHARED001-08 |
| **G17** | **Phân loại link theo domain** (4 domain nội bộ) | TC-ENV003-04 · TC-ENV003-05. ⚠️ Vế **form cũ `step3.lmes.jp`** đã bị Leader loại |
| Q1 | `OUT-PREVIEW-001` | TC-OUTPREVIEW001-01 · -02 · -03 |
| Q2 | `DATA-COUNT-001` | TC-DATACOUNT001-01 |
| Q3 | `LIFF-ENTRY-001` | TC-LIFFENTRY001-01 · -02 |
| Q4 | `FRIEND-001` | TC-FRIEND001-01 + dùng lại kho `TC-CHT-176` (Abnormal) · `TC-CHT-185`. ⚠️ **REQ-009 — friend info chứa URL** đã bị Leader loại |
| Q5 | `FUNC-003` — URL biên | TC-FUNC003-01 · TC-FUNC002-01 |
| Q6 | `REG-SHARED-001` RULE-01 | TC-REGSHARED001-06 → -12 (Normal + Abnormal `-09` + Boundary `-12`) |
| Q7 | `ENV-003` RULE-08 | TC-ENV003-04 · TC-ENV003-05 (cả hai `product`). ⚠️ TC **chạy lại bộ core trên production** đã bị Leader loại |
| **Q8** | `MSG-004` RULE-01 | ⛔ **TRỐNG — Leader loại TC ngày 2026-09-09** — cả `TC-MSG004-02` (Abnormal) lẫn `TC-MSG004-03` (Boundary) đều bị loại. Quan điểm ưu tiên **Cao**, `[MAJOR]` ở §2 **giữ nguyên chưa xử lý**. Rủi ro **tin vượt giới hạn ký tự LINE do link giới thiệu dài ra sau fix** hiện **không có TC nào** |
| **Q9** | `OUT-TRUTH-001` RULE-01 | ⛔ **TRỐNG — Leader loại TC ngày 2026-09-09** — cả `TC-OUTTRUTH001-04` lẫn `TC-MSG004-02` đều bị loại. Quan điểm ưu tiên **Cao**, `[MAJOR]` ở §2 **giữ nguyên chưa xử lý** |
| Q10 | `COMPAT-LEGACY-001` RULE-09 | ⚠️ **Chỉ còn dùng lại kho** `TC-TMT-163` · `-164` — cả 2 TC mới đều bị Leader loại |
| **Q11** | `DATA-CACHE-001` | ⛔ **TRỐNG — Leader loại TC ngày 2026-09-09** — quan điểm nâng lên **Cao** (output user-facing), `[MAJOR]` ở §2 **giữ nguyên chưa xử lý** |
| Q12 | `JOB-001` | TC-OUTPREVIEW001-02 |
| Q13 | `DATA-REF-001` | TC-DATAREF001-01 · TC-FUNC002-01 |

**RULE-01 — quan điểm ưu tiên Cao thiếu loại case, ghi lý do:**

| Quan điểm | Thiếu | Lý do |
|---|---|---|
| `DATA-REF-001` | Normal · Boundary | Nhánh Normal (mã trỏ tới QR **còn sống**) đã được NEW-1 phủ; quan điểm này **không có khái niệm biên** — đối tượng chỉ có 2 trạng thái còn/đã xóa |
| `LIFF-ENTRY-001` | Boundary | Không có khái niệm biên cho "URL mở được / không mở được"; chiều biên về **độ dài URL** đã chuyển sang TC-MSG004-03 |
| `ENV-003` | Boundary | Nhánh biên của môi trường là **có/không khai báo BASE_URL** — đã nằm trong tiền đề của TC-ENV003-04 và NEW-4, không tách TC riêng |
| `MSG-004` | Abnormal · Boundary | ⚠️ **Không phải lý do kỹ thuật** — `TC-MSG004-02` và `TC-MSG004-03` bị Leader loại ngày 2026-09-09 |
| `OUT-TRUTH-001` | Normal · Abnormal · Boundary | ⚠️ **Không phải lý do kỹ thuật** — toàn bộ TC bị Leader loại ngày 2026-09-09 |
| `COMPAT-LEGACY-001` | Abnormal · Boundary | ⚠️ **Không phải lý do kỹ thuật** — `TC-COMPATLEGACY001-02` và `-03` bị Leader loại ngày 2026-09-09 |

---

## 6. Spec update needed

| # | Điểm cần chốt | Hiện trạng | Ai chốt |
|---|---|---|---|
| **1** | ✅ **ĐÃ CHỐT (Leader, 2026-09-09)** — chat 1:1 của LME hiện **URL GỐC**, app LINE hiện **URL RÚT GỌN** | Khớp kho `TC-CHT-183` / `TC-CHT-186` (`Đã hỏi leader`, ghi chú MT-13). Nhưng **spec vẫn chưa ghi**: `BR-15` (`feature-spec.md:477`) chỉ nói URL được chuyển thành short URL, **không nói** chat 1:1 giữ link gốc → người viết TC sau vẫn sẽ hiểu sai đúng như Studio đã hiểu sai. **Việc còn lại: (a) bổ sung rule này vào spec; (b) sửa expected NEW-17 + NEW-18 trên Studio** (§4-5) | **(a) Leader** cập nhật spec · **(b) Người viết TC** sửa Studio bằng `testcase_update` |
| **2** | **URL có setting action mà thôi được rút gọn thì action còn chạy không?** | Kho `TC-TMT-138` ghi rule **"action-thắng-checkbox"**: URL có setting action **phải bị shorten để hệ thống bắt action** (ghi chú: rule này **KHÔNG có trong spec** → MT-08). `TC-TMT-158/159/160/161` cho thấy action click chạy qua `url_shorten.action`. Sau fix, link dựng từ mã và URL cùng miền hệ thống **không còn bản ghi rút gọn** (D1/D2) → action click và đếm click có nguy cơ **ngừng chạy**. Dev **không nêu** rủi ro này trong đánh giá ảnh hưởng | **Dev + PM** — trước khi release |
| **3** | **Miền của link giới thiệu: miền hệ thống hay miền rút gọn riêng của bot?** | Spec `qr-landing` **BR-02** (`feature-spec.md:503`, `logic-spec.md:791`) nói link QR chuẩn **KHÔNG dùng `domain_url_shorten``, nhưng **có** dùng ở `pageIntro()` (EP-65) — `logic-spec.md:365` và `api-spec.md:798` ghi rõ link chia sẻ trong EP-65 ưu tiên `bots.domain_url_shorten` → `env(URL_OUTSIDE_STEP)` → `landing.link_qr_code`. **Spec có tiền lệ cho cả hai chiều** nên **không tự chốt được**. Hiện web dựng bằng miền hệ thống, job dựng bằng miền riêng của bot; **fix không chạm phần này** (NEW-16 để ngỏ expected) | **Dev + PM** — chốt rồi viết lại expected NEW-16 |
| **4** | **Phần "preview text khác nhau" của khách đã được fix chưa?** | Khách hỏi **2 việc**: (a) URL chuyển đổi khác nhau, (b) **text preview khác nhau**. Root cause (c) chỉ ra job Java **không ghi** cặp thay thế của mã giới thiệu vào `mapReplaceContent` (`MessageBuilder.replaceFriendInfo:4081` là nơi **DUY NHẤT** ghi). Fix chỉ chạm **web/PHP (3 file)**, **không chạm linect-service** → phần (b) **nhiều khả năng chưa được fix** | **Dev + PM** — nếu đúng thì tách ticket riêng, **không đóng #40598 là hoàn tất** |
| **5** | **Giá trị friend info chứa URL có nên bị rút gọn không?** | Sau khi đảo thứ tự, tên và friend info được ghép **trước** bước rút gọn (REQ-009). Nếu giá trị friend info của khách là một URL thì URL đó **sẽ đi qua bước rút gọn** → sinh bản ghi rút gọn **riêng cho từng friend**, nguy cơ phình dữ liệu theo số lượng friend. **Không TC nào** và **không mục nào trong đánh giá ảnh hưởng** đề cập | **Dev** — xác nhận hành vi mong muốn |
| **6** | **`makeLinkLandingPageIntro` ném lỗi trong điều kiện nào?** | Bản fix thêm `return $message;` vào nhánh `catch` **ở cả 2 file**, vì sau khi đảo thứ tự đây là **lời gọi cuối** — guard hỏng thì tin gửi đi **rỗng** (REQ-007, risk High). Leader đã xác nhận **mã đã xóa** và **mã sai định dạng** chỉ dẫn tới **"bỏ mã"** = đường xử lý thường, **không** ném lỗi → hiện **không có cách nào chạm được nhánh catch**, G1 là GAP trắng và không TC nào lấp được | **Dev** — cho biết điều kiện ném lỗi (QR xóa mềm? `u_code` null? bot mismatch? DB timeout?) để viết TC; nếu **không thể ép lỗi từ UI** thì ghi nhận là vùng **không test được bằng manual** và yêu cầu unit test |
| **9** | **`executeActionOpenMobile` và `executeActionOpenMobileNormal` khác nhau ở đâu?** | Dev kê 2 hàm này là nơi sửa 4 biểu thức (`UserController.php:3289, 3292, 3616, 3619`) nhưng **không mô tả điều kiện nào dẫn vào hàm nào**. Spec `qr-landing` chỉ có 2 trang trung gian công khai — `SCR-QRL-38` 「LINEアプリを開く」 (`/open-mobile/{type}/{id}`, EP-67, mở LIFF) và `SCR-QRL-39` 「LINEアプリで続行」 (`/open-external-browser/{type}/{id}`, EP-68, mở trình duyệt ngoài) — nhưng cả hai thuộc **`QRCodeController`**, **không phải** `Basic/UserController`, nên **không ánh xạ được**. Không có câu trả lời thì tester **không dựng được tiền đề** phủ đủ 2 nhánh, và `TC-REGSHARED001-12` chỉ chắc chắn phủ được 1 | **Dev** — nêu điều kiện rẽ nhánh + đường thao tác thật tương ứng từng hàm |
| **8** | **Danh sách ĐẦY ĐỦ domain nội bộ không được shorten + cách so khớp domain** | Leader cho biết (2026-09-09): `step.lme.jp` · `s.lmes.jp` · `sl.lmes.jp` · `form.lmes.jp`. Nhưng **(a)** `framework/catalog-lme.md:313` (ENV-DOMAIN) ghi là **`step.lmes.jp`** — lệch đúng 1 chữ so với `step.lme.jp`, mà đây là chỗ **so khớp chuỗi domain**; **(b)** `MAP-LIFF-04` + RULE-09 còn nêu **`step3.lmes.jp`** (form cũ) phải access được — không có trong danh sách 4 domain; **(c)** guard hiện chỉ so `BASE_URL` (**1** domain) + `domain_url_shorten` của bot → **chưa rõ 3 domain còn lại được chặn bằng cách nào**; **(d)** biểu thức đã sửa dùng `stripos` (so khớp **chuỗi con**) — nếu nhánh so domain cũng vậy thì URL ngoài chứa tên domain nội bộ trong tham số sẽ bị **phân loại nhầm** | **Leader** chốt danh sách domain chuẩn (kể cả legacy) · **Dev** xác nhận guard so khớp thế nào và phủ được mấy domain |
| **7** | **Mất số liệu lượt bấm của URL cùng miền hệ thống có chấp nhận được không?** | Dev tự nêu: khi BASE_URL có giá trị (môi trường thật có), **mọi URL trỏ về miền hệ thống thôi được rút gọn** → mất số liệu lượt bấm; hệ thống đã chạy nhiều năm với chốt chặn không hoạt động. Ngược lại kho `TC-TMT-139` đã ghi expected **"URL trong hệ thống → giữ nguyên link"**, tức hành vi mới **đúng với TC kho** — nhưng hệ quả mất số liệu vẫn là thật | **PM** — xác nhận chấp nhận đánh đổi |
