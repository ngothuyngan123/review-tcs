# 05 — Review Report

## 0. Nguồn TC

- **Nguồn đã dùng**: MCP LME TEST STUDIO — task `#318` (ticket 41075, round 1, branch `ai_small_41075`, status `tc-ready`, reviewState `leader`).
- **Tổng số TC review**: 25.

---

## 1. Coverage GAP / RISK — 2 chiều `dev-impact` + `diff code`

**Kết luận: 9/18 vùng ảnh hưởng đủ TC · 5 GAP · 4 RISK.**

| # | Chiều | Vùng ảnh hưởng | TC hiện có | Status | Vấn đề | Severity |
|---|---|---|---|---|---|---|
| **G1** | diff code | **Migration online DDL trên 2 bảng nhật ký khổng lồ, chạy khi production đang ghi liên tục** — Dev tự nêu "chạy giờ thấp điểm, theo dõi thời gian chạy và dung lượng đĩa" | Không có | **GAP** | NEW-27/28/29/32 chỉ chạy migration trên `local` (bảng rỗng/nhỏ). **Không TC nào** verify: ghi vào bảng có bị chặn trong lúc ALTER không · thời gian chạy thực tế · dung lượng đĩa index mới · hành vi khi migration bị ngắt giữa chừng. Đây là rủi ro vận hành số 1 do chính Dev nêu mà không ai kiểm. `REG-RUN-001` + `DEPLOY-LIVE-001` (đều ưu tiên Cao) → 0 TC | `[BLOCKER]` |
| **G2** | dev-impact (D2) + diff code | **Đường GHI vào `detail_landing_click`** — Dev D2: "mỗi lần chèn bản ghi click/thêm bạn từ landing tốn thêm 1 index" | Không có | **GAP** | NEW-30 chỉ verify đường ghi `detail_action_popup` (popup nhúng → EP-17). Đường ghi `detail_landing_click` là **Laravel `LiffController.php:1222-1237`** (INSERT `action=1` khi mở LIFF/kết bạn qua QR) — **0 TC**. NEW-9/16/19 chỉ ĐỌC bảng này | `[MAJOR]` |
| **G3** | diff code | **Danh sách nơi dùng chung `detail_landing_click` của Dev THIẾU** | NEW-16, NEW-19 (chỉ 2 màn) | **GAP** | Dev kê đủ caller của **function**, nhưng với **bảng dùng chung** chỉ nêu "QR Code Action / Landing (FA-017)". Theo `spec-features/admin/qr-landing/db/db-mapping.md:68`, `detail_landing_click` còn được đọc bởi: **cron A-1 `statistic:landing_action`** (02:05 hằng ngày, tổng hợp `landing_histories` cho TOÀN hệ thống) · **`HandleExportCsvTask`** (export CSV) · **EP-37 / EP-38 / EP-39 / EP-41** (tab 「分岐詳細」 SCR-QRL-29, panel chi tiết ngày SCR-QRL-30). **0 TC** cho cả 4 nhóm. Câu hỏi adversarial #3 — hàm/bảng dùng chung phải có danh sách nơi ảnh hưởng + TC từng nơi | `[MAJOR]` |
| **G4** | dev-impact (F6) | `BotController:8008` + `FlowDeleteBot:481` + job change-bot — **DELETE `detail_action_popup` theo `popup_id`** | Không có | **GAP** | Dev ghi "hưởng lợi từ index mới, không sửa" nhưng không có TC nào xác nhận DELETE vẫn đúng sau khi thêm index. Ngoài 2 nơi Dev kê, `spec-features/admin/change-bot/db/db-mapping.md:177` còn 1 nơi thứ 3: job đổi LOA step2 `DELETE ... JOIN popup ON popup.id = detail_action_popup.popup_id WHERE popup.bot_id = ?`. → **kho đã có TC-CB-166**, dùng lại thay vì viết mới (xem §5) | `[MAJOR]` |
| **G5** | dev-impact (T3) | Popup Ads Setting (FS-006) | Không có | **GAP** | Dev đánh Low risk, "không đụng logic, chỉ hưởng lợi gián tiếp từ index". **Không đề xuất TC** — không tìm được bằng chứng FS-006 đọc/ghi 2 bảng nhật ký này trong `spec-features/`; trigger suông không đủ để đẻ TC (xem ghi chú §5) | `[NIT]` |
| **R1** | diff code | **Biên gộp ngày `DATE(created_at)` vs `startOfDay/endOfDay` PHP — rủi ro timezone** | NEW-12 | **RISK** | Đây là **điểm rủi ro cao nhất** theo cả Dev lẫn Studio (`assumptions`: "nếu session timezone MySQL lệch với giả định PHP, bản ghi gần nửa đêm có thể rơi khác ngày. Cần verify trên data thật"). NEW-12 khai `env_scope = local` và dựng dữ liệu bằng ghi thẳng DB → **timezone phiên MySQL local KHÔNG đại diện cho production**. NEW-3 (`env_scope = all`) là đường duy nhất kiểm được biên ngày ngoài local nhưng chưa chạy | `[MAJOR]` |
| **R2** | dev-impact (F4) | Migration index — tên, thứ tự cột, idempotent, rollback | NEW-27, NEW-28, NEW-29, NEW-32 | **RISK** | Đủ 3 loại case nhưng **100% `env_scope = local`**. `DATA-MIG-001` ưu tiên Cao. Kết luận từ local không thay được production (RULE-08) | `[MAJOR]` |
| **R3** | diff code | **Hiệu năng — mục tiêu chính của ticket** | NEW-15, NEW-21, NEW-22, NEW-31 | **RISK** | Ticket là bug perf đo trên **production `step.lme.jp`** (p95 135s). NEW-21/22/31 đều `env_scope = local` với dữ liệu tự dựng; NEW-15 đo trên UI nhưng **expected không có ngưỡng pass/fail**. **Không TC nào đóng vòng lặp trên production** — trong khi Dev tự nêu "chưa đo được thời gian thực tế… cần theo dõi lại số liệu giám sát sau khi release". RULE-08: performance/loadbalance không kết luận từ local | `[MAJOR]` |
| **R4** | dev-impact (T2) | QR Code Action / Landing (FA-017) | NEW-19 | **RISK** | Chỉ cover 1 màn (Chi tiết dữ liệu, 2 tab). Các nhánh còn lại của FA-017 nằm ở G3 | `[MAJOR]` |

**4 câu hỏi adversarial chiều `diff code`** (Studio `spec_delta.diffAvailable = true`, 3 file / 152+ / 17−):

| # | Câu hỏi | Kết quả |
|---|---|---|
| 1 | Mỗi file trong diff có ≥1 TC đi qua? | ✅ Cả 3 file đều có TC |
| 2 | Điều kiện mới có TC cả 2 nhánh? Generic-fix? | ✅ Không phải generic-fix. Nhánh "ngày trống fill 0" → NEW-11; nhánh "khoảng rỗng `start>end` bỏ qua query" → NEW-10 |
| 3 | Hàm/bảng dùng chung có danh sách nơi ảnh hưởng + TC từng nơi? | ❌ **Function**: Dev kê đủ. **Bảng dùng chung**: thiếu 4 nhóm consumer → **G3** |
| 4 | Hành vi đổi so với trước → TC verify cả mới lẫn cũ không hỏng? | ✅ `dev_impact` khẳng định output BẤT BIẾN; NEW-1 + NEW-3 verify tính bất biến |

---

## 2. Quan điểm test còn thiếu

**Kết luận: 11/14 quan điểm Trigger khớp đã cover đủ · 3 thiếu.**

| # | Mã quan điểm | Ưu tiên | Trigger khớp vì | Thiếu gì | Severity |
|---|---|---|---|---|---|
| **Q1** | `REG-RUN-001` | Cao | "release khi hệ thống có job/dữ liệu đang chạy dở" — migration INPLACE trên bảng nhật ký đang được ghi liên tục, cron A-1 chạy 02:05 | **0 TC.** Không có Normal / Abnormal / Boundary nào. Trùng gốc với G1 | `[BLOCKER]` |
| **Q2** | `PERF-LARGE-001` | Trung bình → **Cao** (task là bug hiệu năng, xử lý khối lượng lớn) | Chính là mục tiêu ticket | Có Normal (NEW-21) + Boundary (NEW-22, NEW-31) nhưng **thiếu Abnormal**, không ghi lý do → vi phạm **RULE-01**. Abnormal còn thiếu: nhiều phiên cùng mở khoảng 90 ngày, khoảng vượt 90 ngày | `[MAJOR]` |
| **Q3** | `ENV-003` ★ | Cao | "BẮT BUỘC khi tính năng chạm **job nền**" — cron A-1 đọc bảng vừa thêm index; + performance. **Không được đánh × với lý do staging/local đã pass** (RULE-08) | **0 TC chạy ở `prd`.** `envAuto[].runs = 0` ở cả 4 env; 12/25 TC khai `local`, 13/25 khai `all` nhưng chưa chạy ở đâu. Gộp hệ quả của R1+R2+R3 | `[MAJOR]` |

**Đã kiểm và loại khỏi phạm vi** (trigger khớp chữ nhưng không có bằng chứng ảnh hưởng):

- `DATA-BACKUP-001` ★ — trigger "tính năng thêm/đổi bảng DB". **Loại**: popup **không** nằm trong phạm vi copy bot (FA-033 `db-mapping.md:415` `popup.is_enable = 0`; kho `kho-tcs/fa033-backup-データコピー.md` MT-22 + TC-BK-328 xác nhận popup không được copy). Không đẻ TC backup.
- `DATA-DB-001` ★ — trigger "mọi chức năng có UPDATE/DELETE". **Loại**: luồng sửa là **CHỈ ĐỌC**; migration là DDL, không phải UPDATE/DELETE dữ liệu. DELETE theo `popup_id` là code có sẵn không bị chạm → đã xử lý ở G4 bằng cách dùng lại TC kho.
- `COMPAT-LEGACY-001` ★ — không chạm template / form / remind / richmenu / google spread.
- `DEPLOY-ASSET-001` ★ — không sửa JS/CSS/font/icon.

---

## 3. TC trùng lặp

Đã rà **25/25 TC** theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`) — phát hiện **2 nhóm trùng**.

| Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| Đếm query / thời gian theo số ngày | **NEW-22** | **NEW-31** → **GỘP**, không xóa | `DUP-SUBSET` | Cùng `PERF-LARGE-001` · cùng `Boundary` · cùng thao tác (gọi EP-16 với 3 mốc **1 / 30 / 90 ngày** trên cùng popup) · cùng tiền đề (popup nhiều log, `local`) · cùng expected ("số query / thời gian không tăng theo số ngày"). NEW-22 là **superset** — còn đo thời gian phản hồi và đếm dòng nhật ký. NEW-31 chỉ còn bước 4 "kiểm số lượng query" | `[MINOR]` |
| Kiểm index sau migration | **NEW-27** | **NEW-32** → **GỘP một phần** (rút gọn còn EXPLAIN) | `DUP-SUBSET` | Cùng `DATA-MIG-001` · cùng `Normal` · bước 1–2 của NEW-32 ("kiểm index hiện có trên 2 bảng") **trùng hoàn toàn** bước 3 của NEW-27, mà NEW-27 còn kiểm **thứ tự cột** + "index cũ không bị xoá". Giá trị riêng của NEW-32 là **bước 3 EXPLAIN** — đúng chỗ Dev chưa chạy được (`Connection refused`) | `[MINOR]` |

**Gate trước khi đề nghị xóa** — đã chạy lại BƯỚC 2 + 3 trên tập giả định:

- Bỏ NEW-31 → `PERF-LARGE-001` vẫn còn NEW-22 (Boundary) + NEW-21 (Normal) ⇒ không mất cover. **Nhưng** NEW-31 do human thêm sau (`cucdtk@mcp`) nên đề xuất **gộp, không xóa** — chuyển bước "đếm số query" vào NEW-22 rồi mới bỏ.
- Bỏ hẳn NEW-32 → **mất** chiều EXPLAIN (không TC nào khác có) ⇒ **đổi từ "xóa" sang "gộp một phần"**: giữ NEW-32 nhưng cắt bước 1–2, chỉ còn EXPLAIN.

⚠️ **Không tự xóa** — TC Studio read-only. Human thực hiện qua `testcase_update` / `testcase_delete` trên Studio.

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | Toàn bộ 25 TC | **0 pass / 0 fail / 25 chưa chạy** (`exec.untested = 25`, mọi `last_exec = null`, `ranAt = null`, `envAuto[].runs = 0` ở cả 4 env). Tỷ lệ pass = 0% < 50% (BƯỚC 0.6 #1) | Đây là **bộ TC draft chưa thực thi**, không phải kết quả test. Mọi kết luận "đủ TC" ở §1 đã hạ xuống `RISK`. Phải chạy trước khi dùng làm căn cứ đóng ticket |
| 2 | `[MAJOR]` | Spec | **Không có spec cho Popup (FA-018) trong `spec-features/`** — chỉ có `spec-features/admin/qr-landing/` (FA-017). Studio `info_pack.specRefs` có dẫn `popup/feature-spec.md:165` (EP-16), `:53` (SCR-PU-03), `ui-spec.md:235-262` nhưng đó là spec repo của Studio, không có trong repo này | Không có chuẩn độc lập để đánh giá Expected của tab1 — hiện đang tin hoàn toàn vào mô tả của Dev + Studio. Đề nghị bổ sung `spec-features/admin/popup/` hoặc Leader xác nhận dùng spec Studio làm nguồn chính thức |
| 3 | `[MAJOR]` | `03-dev-impact.md` | File có `Auto-filled: 2026-09-21 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" CHƯA tick** | Auto-fill từ Redmine chưa được tester verify — F/D/T có thể thiếu hoặc map sai (thực tế §1 G3 + G4 đã cho thấy mục 4.2/4.3 thiếu consumer). Yêu cầu tester đọc lại Redmine #41075 và tick trước khi review có giá trị |
| 4 | `[MAJOR]` | 8/18 mã quan điểm | Mã **không có** trong `framework/checklist-lme.md`: `TOOL-AXIS-001` (NEW-3) · `TOOL-NEGCTRL-001` (NEW-5) · `TOOL-OLDREC-001` (NEW-6) · `DATA-RETENTION-001` (NEW-9) · `TOOL-KNOW-002` (NEW-15) · `RULE-TOOL-029` (NEW-30) · `API-CONTRACT-001` (NEW-20) · `PERF-LATENCY-001` (NEW-21) | ⚠️ **Ghi chú cách xử lý**: theo BƯỚC 0.6 #5 thì mã lạ "không tính là cover". Tôi **đã đánh giá coverage theo NỘI DUNG TC** thay vì theo nhãn, vì 8 TC này đều có giá trị thật (equivalence, negative control, soft-delete, API contract) — máy móc gạch đi sẽ đẻ ra GAP giả. Leader chọn 1: (a) bổ sung 8 mã vào `framework/checklist-lme.md` theo RULE-10, hoặc (b) remap sang mã framework có sẵn (`TOOL-AXIS-001`→`DATA-COUNT-001`, `DATA-RETENTION-001`→`DATA-001`, `API-CONTRACT-001`→`INTG-*`, `PERF-LATENCY-001`→`PERF-LARGE-001`) |
| 5 | `[MINOR]` | NEW-31, NEW-32 | 2 TC do human thêm sau (`cucdtk@mcp`, 2026-09-21 09:16) **không gắn `requirement_keys`** | Không map được vào REQ nào → lọt khỏi ma trận coverage của Studio. Gắn `REQ-003` cho NEW-31, `REQ-008` cho NEW-32 |
| 6 | `[NIT]` | Task Studio | `info_pack.critique.conflicts`: human gắn `taskType = sprint-task` nhưng Studio phân loại `fix-bug` (có git diff, không tạo tính năng mới) | Leader chốt lại loại task trên Studio |

### Chất lượng từng TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 7 | `[MAJOR]` | **NEW-15** | `Kết quả mong đợi` **không đo lường được**: "tải thành công, không timeout hoặc treo màn. Thời gian cần được ghi nhận để đối chiếu". **Không có ngưỡng pass/fail** — tester không biết bao nhiêu giây là Không đạt. Ngưỡng 10 giây chỉ nằm trong `note`, không nằm trong expected (4a #1) | Đưa ngưỡng vào expected: "lớp phủ tải dữ liệu tắt trong **≤ 10 giây**; quá 10 giây → Không đạt". Ghi rõ khối lượng log thực tế của popup đã chọn vào kết quả |
| 8 | `[MAJOR]` | **NEW-19** | (a) Expected "chức năng xuất dữ liệu **(nếu có trên màn)** vẫn chạy" — điều kiện mơ hồ, tester tự quyết (4a #1). (b) Precondition "**nếu có thể**, ghi lại trước số liệu khi chưa thêm index để đối chiếu" nhưng expected lại đòi "số liệu thống kê **không đổi**" → không có baseline thì không chạy được (4a #2) | (a) Chốt hẳn: màn SCR-QRL có nút export hay không, viết thành bước bắt buộc hoặc bỏ. (b) Đổi precondition thành **bắt buộc** ghi baseline, hoặc đổi expected sang oracle độc lập (đối chiếu với số đếm bản ghi) |
| 9 | `[MAJOR]` | **NEW-21** | Precondition đòi "dựng ít nhất **200.000** bản ghi hành vi và **50.000** landing click trải đều trong 120 ngày" nhưng **không nói dựng bằng cách nào**, cũng không nói cấu hình máy tối thiểu. Hai người chạy sẽ ra 2 kết quả khác nhau (4a #2) | Bổ sung script/cách seed dữ liệu + ghi cấu hình máy vào `Ghi chú`. **Quan trọng hơn**: ngưỡng 4s chỉ có nghĩa nếu đo trên phần cứng tương đương production (xem R3) |
| 10 | `[MINOR]` | NEW-6 vs NEW-16 / NEW-19 / NEW-30 | **Phụ thuộc thứ tự chạy** không được khai ở đâu: NEW-6 đòi môi trường **CHƯA** chạy migration; NEW-16/19/30 đòi **ĐÃ** chạy; NEW-27 đòi chưa; NEW-29 gỡ migration rồi phải chạy lại | Thêm cột/ghi chú thứ tự chạy bắt buộc: NEW-6 → NEW-27 → (migration) → NEW-32/16/19/30 → NEW-28 → NEW-29 → (chạy lại migration) |
| 11 | `[MINOR]` | NEW-27 | Expected "các index sẵn có trước đó vẫn còn nguyên" nhưng **không liệt kê index nào đang có** → tester không biết đối chiếu với cái gì. Thêm nữa, Dev ghi "`detail_landing_click` chỉ index `landing_id`" trong khi `spec-features/admin/qr-landing/db/db-mapping.md:311-312` ghi có **cả 2**: `detail_landing_click_landing_id_index` **và** `detail_landing_click_post_code_index` | Liệt kê tường minh index kỳ vọng trước migration. Mâu thuẫn Dev↔spec đã đưa vào §6 |

---

## 5. TCs đề xuất bổ sung (9)

### Đối chiếu kho TCs + chống trùng

| Mục | Kết quả |
|---|---|
| Kho FA-018 (Popup) | ❌ **Chưa có** `kho-tcs/fa018-*` — không đối chiếu được vùng regression của chính tính năng popup |
| Kho FA-017 (QR Code Action) | ❌ **Chưa có** `kho-tcs/fa017-*` — TC cho G3 phải viết mới, dựa `spec-features/admin/qr-landing/db/db-mapping.md` |
| Kho FA-033 (Backup) | ✅ Đã đọc — popup **không** thuộc phạm vi copy (`is_enable=0`, MT-22, TC-BK-328) ⇒ **loại** `DATA-BACKUP-001` khỏi phạm vi, không đẻ TC |
| Kho FA-039 (Change bot) | ✅ Đã đọc — **TC-CB-166** (Data · `DATA-COUNT-001` · PRODUCTION) đã cover đúng G4: query `detail_action_popup` trước/sau đổi LOA, xác nhận bản ghi bị xoá + cấu hình popup còn nguyên ⇒ **dùng lại, không viết TC mới** |
| Chống trùng | ✅ Đã đối chiếu **25 TC** ở BƯỚC 0 + `kho-tcs/fa033` + `kho-tcs/fa039` — không TC đề xuất nào trùng. TC-PERFLARGE001-01 khác NEW-21/22/31 ở chiều **đồng thời** (concurrent), không phải lặp lại phép đo đơn luồng |

**Dùng lại TC kho (không viết mới):**

- **G4** → dùng lại **`TC-CB-166`** (`kho-tcs/fa039-changebot-LINE公式アカウント入れ替え機能.md`) — "Popup — dữ liệu thống kê lượt click popup của friend LOA cũ bị xóa". **Cần chỉnh cho bug này**: thêm 1 bước ghi lại **thời gian chạy** của job dọn dữ liệu trước/sau khi có index `idx_dap_popup_id_created_at`, vì DELETE giờ đi qua index mới.
- **G5** (Popup Ads Setting FS-006) → **không đề xuất TC**. Đã loại khỏi phạm vi: không tìm được câu khẳng định nào trong `spec-features/` rằng FS-006 đọc/ghi `detail_action_popup` hoặc `detail_landing_click`. Dev cũng chỉ ghi "hưởng lợi gián tiếp". Trigger suông không đủ bằng chứng để viết TC.
- **Q2 / R1** — không đẻ TC mới cho biên ngày ở production: NEW-3 (`env_scope = all`) đã là oracle đúng cho việc này. Thay vào đó xem issue §4 và **bắt buộc chạy NEW-3 ở `staging` + `prd`**, không để nằm ở local.

### Bảng TC

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-REGRUN001-01 | Data | REG-RUN-001 | Migration index — 2 bảng nhật ký `detail_action_popup` / `detail_landing_click` | Normal | manual | product | Chạy migration thêm index trong lúc hệ thống vẫn đang ghi log popup/landing | - Đã lên lịch cửa sổ release vào giờ thấp điểm, có DBA/Dev trực<br>- Đã ghi lại số bản ghi hiện tại của 2 bảng và dung lượng đĩa còn trống của máy chủ DB<br>- Chuẩn bị sẵn 1 popup đang được nhúng thật trên trang khách và 1 QR Code Action đang hoạt động, để phát sinh ghi trong lúc chạy | 1. Bấm giờ, bắt đầu chạy migration thêm index<br>2. Trong khi migration đang chạy: mở trang khách có nhúng popup, để popup hiện rồi bấm vào popup (sinh lượt hiển thị + lượt click)<br>3. Trong khi migration đang chạy: kết bạn qua QR Code Action bằng 1 máy LINE thật (sinh bản ghi landing click)<br>4. Chờ migration kết thúc, ghi lại tổng thời gian chạy<br>5. Ghi lại dung lượng đĩa đã dùng thêm<br>6. Mở màn 「詳細データ」 của popup vừa thao tác, chọn khoảng có hôm nay, đọc dòng hôm nay<br>7. Mở màn Chi tiết dữ liệu của QR Code Action, kiểm bạn vừa kết có trong danh sách không | 1 lượt hiển thị + 1 lượt click popup, 1 lượt kết bạn qua QR, phát sinh **trong lúc** migration chạy | - Migration kết thúc thành công, không có lỗi khoá bảng<br>- Các thao tác ở bước 2 và 3 **không bị chặn, không lỗi, không treo** trong lúc migration chạy<br>- Bước 6: dòng hôm nay tăng đúng 1 lượt hiển thị và 1 lượt click — **không mất bản ghi nào phát sinh trong lúc chạy migration**<br>- Bước 7: bạn vừa kết bạn có trong danh sách<br>- Thời gian chạy và dung lượng đĩa tăng thêm được ghi lại thành số cụ thể trong kết quả | | Lấp G1 · Q1 · manual vì bắt buộc production (khối lượng bảng nhật ký thật + thiết bị LINE thật) · Môi trường: PRODUCTION (RULE-08 — job/DDL không kết luận từ local) · Đánh giá spec: Đã hỏi leader · Evidence: log chạy migration có mốc thời gian đầu/cuối · ảnh màn thống kê trước/sau · số liệu dung lượng đĩa · Dev nêu rủi ro này ở `03-dev-impact.md` mục 7.1 nhưng không có TC nào kiểm |
| TC-REGRUN001-02 | Data | REG-RUN-001 | Migration index — xung đột với cron A-1 `statistic:landing_action` | Abnormal | manual | product | Migration chạy đè lên khung giờ cron tổng hợp số liệu ngày (02:05) | - Biết lịch cron A-1 `statistic:landing_action` chạy 02:05 hằng ngày (`app/Console/Kernel.php:196`) và cron A-2 chạy 02:10<br>- Có quyền đọc log `job_crontab`<br>- Đã ghi lại số liệu tab 「日別」 của ≥2 QR Code Action cho ngày hôm trước | 1. Hẹn chạy migration thêm index vào đúng khoảng 02:00–02:15<br>2. Theo dõi log `job_crontab` trong lúc cron A-1 chạy<br>3. Chờ cả migration và cron kết thúc<br>4. Mở màn Chi tiết dữ liệu của các QR Code Action đã ghi, xem tab 「日別」 của ngày hôm trước<br>5. So từng con số với giá trị đã ghi trước đó | Migration + cron A-1 chạy chồng khung giờ | - Cron A-1 chạy tới cùng, **không dừng giữa chừng** và không bỏ sót landing nào (log `job_crontab` có đủ cặp dòng đầu/cuối cho từng landing)<br>- Số liệu tab 「日別」 ngày hôm trước **không đổi** so với giá trị đã ghi<br>- Không có bản ghi `landing_histories` bị trùng hoặc bị thiếu<br>- Nếu cron bị lỗi: ghi lại nguyên văn log và raise ticket ngay | | Lấp G1 · Q1 · Q3 · manual vì bắt buộc production · Môi trường: PRODUCTION · Đánh giá spec: Đã hỏi leader · ⚠️ `spec-features/admin/qr-landing/job/job-spec.md:813` ghi rõ cron A-1 **không có try/catch** — 1 landing lỗi làm hỏng cả lần chạy, mất số liệu hàng loạt. Đây là lý do case này bắt buộc chạy · Evidence: log `job_crontab` + ảnh tab 「日別」 trước/sau |
| TC-REGRUN001-03 | Data | REG-RUN-001 | Migration index — bị ngắt giữa chừng | Boundary | manual | product | Migration bị ngắt giữa chừng trên bảng khối lượng thật rồi chạy lại | - Môi trường có bản sao khối lượng tương đương production của 2 bảng nhật ký (bản restore, không phải production sống)<br>- Đã ghi lại danh sách index hiện có của 2 bảng | 1. Bắt đầu chạy migration<br>2. Khi migration đang chạy dở, ngắt phiên kết nối (đóng terminal / kill phiên)<br>3. Đọc lại danh sách index của 2 bảng, ghi lại trạng thái<br>4. Chạy lại migration từ đầu<br>5. Đọc lại danh sách index lần cuối | Ngắt migration ở giữa, sau đó chạy lại | - Sau bước 3: bảng **không** ở trạng thái hỏng — hoặc index chưa được tạo, hoặc đã tạo xong; không có index nửa vời làm bảng không truy vấn được<br>- Bước 4 chạy lại **không lỗi trùng tên index** (migration idempotent như Dev cam kết)<br>- Sau bước 5: đúng 1 index `idx_dap_popup_id_created_at` và đúng 1 `idx_dlc_popup_id_created_at`, không phát sinh index thứ hai<br>- Màn 「詳細データ」 của popup bất kỳ vẫn đọc được số liệu bình thường | | Lấp G1 · Q1 · manual vì phải thao tác trên bản sao khối lượng production · Môi trường: PRODUCTION (bản restore) · Đánh giá spec: Đã hỏi leader · Khác NEW-28: NEW-28 chỉ chạy lại migration **đã hoàn tất** trên local bảng nhỏ; case này kiểm trạng thái **dở dang** trên bảng lớn — tình huống thật khi ALTER chạy hàng chục phút · Evidence: 3 lần chụp danh sách index |
| TC-REGSHARED001-01 | Data | REG-SHARED-001 | Cron A-1 `statistic:landing_action` → bảng `landing_histories` → tab 「日別」 của QR Code Action | Normal | manual | product | Job tổng hợp số liệu ngày vẫn đúng sau khi thêm index vào bảng nguồn | - Đã chạy migration thêm index trên production<br>- Chọn ≥3 QR Code Action có phát sinh dữ liệu trong ngày hôm nay (1 cái nhiều lượt, 1 cái ít lượt, 1 cái không có lượt nào)<br>- Đã ghi lại số lượt click / kết bạn / mở trang hiện đang hiển thị realtime của ngày hôm nay trên tab 「日別」<br>- Có quyền đọc log `job_crontab` | 1. Ghi lại số liệu realtime của ngày hôm nay trên tab 「日別」 của cả 3 QR Code Action (trước 02:05)<br>2. Chờ qua 02:05 hôm sau để cron A-1 chạy<br>3. Đọc log `job_crontab` xác nhận cron chạy hết<br>4. Mở lại tab 「日別」 của cả 3 QR Code Action, xem dòng của ngày hôm qua<br>5. So từng con số với giá trị đã ghi ở bước 1 | 3 QR Code Action: nhiều lượt / ít lượt / không có lượt | - Cron A-1 chạy hết, log có đủ cặp dòng đầu/cuối cho mọi landing<br>- Số liệu ngày hôm qua trên tab 「日別」 **trùng khít** với số realtime đã ghi ở bước 1, cho cả 3 QR Code Action<br>- QR Code Action không có lượt nào vẫn hiện dòng với số 0, không biến mất<br>- Không có dòng `landing_histories` trùng cho cùng bộ (bot, landing, ngày) | | Lấp G3 · Q3 · manual vì bắt buộc production + phải chờ job nền chạy theo lịch thật · Môi trường: PRODUCTION (RULE-08 — job nền) · Đánh giá spec: Đã hỏi leader · Nguồn xác định consumer: `spec-features/admin/qr-landing/db/db-mapping.md:68` + `job/job-spec.md:64` (A-1 `dailyAt('02:05')`) · **Dev KHÔNG kê cron A-1 trong caller list lẫn mục 4.3** · Evidence: log `job_crontab` + ảnh tab 「日別」 trước/sau |
| TC-REGSHARED001-02 | UI | REG-SHARED-001 | Export CSV danh sách bạn bè của QR Code Action (`HandleExportCsvTask`) | Normal | manual | product | Xuất CSV danh sách bạn bè qua QR vẫn đủ dòng và đúng nội dung sau khi thêm index | - Đã chạy migration thêm index trên production<br>- Chọn 1 QR Code Action có ≥20 bạn thêm mới trong khoảng ngày sẽ chọn, gồm cả bạn mới, bạn cũ và bạn gỡ block<br>- Đã ghi lại tổng số bạn hiển thị trên màn Chi tiết dữ liệu của QR Code Action đó | 1. Mở màn Chi tiết dữ liệu của QR Code Action đã chọn<br>2. Chọn khoảng ngày có dữ liệu, ghi lại tổng số dòng hiển thị trên màn<br>3. Bấm chức năng xuất CSV<br>4. Chờ file được tạo và tải về<br>5. Mở file CSV, đếm số dòng và đối chiếu nội dung vài dòng đầu/cuối với màn hình | 1 QR Code Action, khoảng ngày có ≥20 bạn gồm đủ 3 loại bạn | - File CSV tạo thành công, tải về được, không lỗi timeout<br>- **Số dòng trong CSV bằng đúng số dòng hiển thị trên màn** ở bước 2<br>- Cột thời điểm kết bạn và tên bạn ở các dòng đối chiếu khớp với màn hình<br>- Nhãn loại bạn (「新規友だち」/「既存友だち」/「ブロックを解除した友だち」) đúng như trên màn | | Lấp G3 · manual vì bắt buộc production (khối lượng thật) + phải mở file tải về · Môi trường: PRODUCTION · Đánh giá spec: Đã hỏi leader · Nguồn: `db-mapping.md:68` liệt kê `HandleExportCsvTask` là consumer của `detail_landing_click` — **Dev không kê** · RULE-06: output cuối là file CSV, không dừng ở màn admin · Evidence: file CSV + ảnh màn danh sách |
| TC-REGSHARED001-03 | UI | REG-SHARED-001 | QR Code Action — tab 「分岐詳細」 (SCR-QRL-29, EP-37/EP-38) và panel chi tiết theo ngày (SCR-QRL-30, EP-39) | Normal | auto | Tất cả | Các tab thống kê khác của QR Code Action vẫn hiển thị đúng sau khi thêm index | - Đã chạy migration thêm index<br>- Chọn 1 QR Code Action có dữ liệu ở cả 4 nhóm chỉ số của sơ đồ 「分岐詳細」<br>- Đã ghi lại 10 chỉ số của sơ đồ 「分岐詳細」 và số liệu 1 ngày bất kỳ ở tab 1 **trước** khi chạy migration (hoặc lấy từ môi trường chưa migrate để đối chiếu) | 1. Mở màn Chi tiết dữ liệu của QR Code Action đã chọn<br>2. Bấm tab 「分岐詳細」, chọn khoảng ngày đã ghi số liệu<br>3. Đọc đủ 10 chỉ số của sơ đồ, so với giá trị đã ghi<br>4. Bấm vào 1 chỉ số bất kỳ để mở panel chi tiết theo nhóm, đếm số dòng<br>5. Quay lại tab 1, bấm 「詳細表示」 của 1 ngày có dữ liệu để mở panel chi tiết theo ngày<br>6. Đổi 「表示単位」 giữa 「人数」 và 「回数」, đọc lại số liệu | 1 QR Code Action có dữ liệu ở cả 4 nhóm chỉ số; 2 chế độ 「人数」/「回数」 | - Cả 10 chỉ số của 「分岐詳細」 **trùng khít** giá trị đã ghi trước migration<br>- Panel chi tiết theo nhóm mở được, số dòng khớp chỉ số vừa bấm<br>- Panel chi tiết theo ngày mở được, số dòng khớp số liệu tab 1 của ngày đó<br>- Đổi 「表示単位」 cho ra 2 bộ số khác nhau đúng như trước, không lỗi truy vấn | | Lấp G3 · Môi trường: Tất cả (chỉ đọc, không cần khối lượng production) · Đánh giá spec: Spec ghi rõ — `db-mapping.md` mục 5.29 (EP-37/38) + 5.30 (EP-39) · Nguồn: `db-mapping.md:68` liệt kê EP-37/38/39/41 là consumer — **Dev không kê** · Evidence: ảnh sơ đồ 「分岐詳細」 trước/sau + ảnh 2 panel |
| TC-DATACOUNT001-01 | UI | DATA-COUNT-001 | Đường GHI `detail_landing_click` — kết bạn qua QR đích của popup (LIFF) | Normal | manual | product | Kết bạn qua popup vẫn ghi đủ bản ghi và số bạn thêm mới tăng đúng sau khi thêm index | - Đã chạy migration thêm index trên production<br>- Có 1 popup đã nhúng trên trang test, cấu hình đích là QR code action (có `landing_id_end_point`)<br>- Có 2 tài khoản LINE thật **chưa từng** kết bạn với bot này<br>- Đã ghi lại số liệu dòng hôm nay ở tab 「数値情報」 và số dòng ở tab 「友だち一覧」 của popup đó | 1. Mở trang test có nhúng popup bằng máy A, để popup hiện, bấm vào popup<br>2. Trên máy LINE thật A, hoàn tất kết bạn qua QR đích<br>3. Lặp bước 1–2 với máy B<br>4. Mở màn 「詳細データ」 của popup ở cửa sổ quản trị, chọn khoảng có hôm nay<br>5. Đọc dòng hôm nay ở tab 「数値情報」<br>6. Chuyển sang tab 「友だち一覧」, đếm số dòng và kiểm 2 bạn vừa kết có trong danh sách không | 2 lượt kết bạn mới qua QR đích của popup, bằng 2 tài khoản LINE thật chưa từng kết bạn | - Bước 5: cột 「友だち追加数」 của hôm nay **tăng đúng 2 người**, tỉ lệ thêm bạn được tính lại tương ứng; 「表示回数」 và 「クリック数」 cũng tăng đúng theo số lần thao tác<br>- Bước 6: tab 「友だち一覧」 có thêm đúng 2 dòng, tên 2 bạn đúng, bấm vào mở đúng trang thông tin bạn<br>- Không lượt nào bị mất, không ghi trùng, thao tác kết bạn trên LINE không bị chậm bất thường | | Lấp G2 · manual vì bắt buộc production + thiết bị LINE thật · Môi trường: PRODUCTION (RULE-08) · Đánh giá spec: Đã hỏi leader · **Đây là GAP Dev tự tạo ra**: `03-dev-impact.md` D2 ghi "mỗi lần chèn bản ghi click/thêm bạn từ landing tốn thêm 1 index" nhưng NEW-30 chỉ test đường ghi `detail_action_popup`. Đường ghi `detail_landing_click` là `LiffController.php:1222-1237` (`db-mapping.md:1115`) · RULE-06: output cuối ở app LINE thật · Evidence: video thao tác trên máy LINE + ảnh 2 tab trước/sau |
| TC-PERFLARGE001-01 | UI | PERF-LARGE-001 | EP-16 — nhiều phiên cùng mở khoảng ngày lớn | Abnormal | manual | product | Nhiều người cùng mở màn Chi tiết dữ liệu với khoảng 90 ngày tại cùng thời điểm | - Đã chạy migration thêm index trên production<br>- Chọn 3 popup có khối lượng log lớn nhất trên hệ thống, thuộc ≥2 bot khác nhau (ví dụ các bot 179609 / 160621 / 46203 nêu trong ticket)<br>- Có 3 phiên đăng nhập quản trị riêng biệt (3 trình duyệt / 3 tài khoản), vì đổi bot ở 1 tab sẽ đổi cả trình duyệt<br>- Có quyền đọc cảnh báo request chậm bắn lên Chatwork room 417532006 | 1. Chuẩn bị 3 phiên, mỗi phiên mở sẵn màn 「詳細データ」 của 1 popup khác nhau<br>2. Đếm 3-2-1 rồi cả 3 phiên **cùng lúc** chọn khoảng 90 ngày gần nhất<br>3. Bấm giờ từng phiên, ghi thời gian tới khi bảng hiện xong<br>4. Lặp lại bước 2–3 thêm 2 lần nữa, lấy giá trị lớn nhất<br>5. Kiểm tra có cảnh báo request chậm nào cho `/ajax/popup/init-data-detail-click` được bắn lên Chatwork không | 3 phiên đồng thời × khoảng 90 ngày × 3 popup nhiều log nhất, lặp 3 lần | - Cả 3 phiên đều trả kết quả, **không phiên nào timeout hay treo**<br>- Thời gian lớn nhất của cả 3 phiên qua 3 lần đo **dưới 4 giây** (ngưỡng middleware `NotifyChatworkRequestTimeSlow`)<br>- **Không có cảnh báo request chậm nào** được bắn lên Chatwork cho đường dẫn này trong suốt bài đo<br>- Số liệu 3 phiên đọc ra không lẫn sang nhau (mỗi phiên đúng popup của nó) | | Lấp Q2 (RULE-01 — `PERF-LARGE-001` đang thiếu Abnormal) · R3 · manual vì bắt buộc production · Môi trường: PRODUCTION (RULE-08 — performance/loadbalance) · Đánh giá spec: Đã hỏi leader · Khác NEW-21/22/31 ở chiều **đồng thời**: 3 TC kia đều đo đơn luồng trên local. Bug gốc ghi nhận **2 user cùng bị chậm** trong 1 giờ · ⚠️ Dùng 3 phiên đăng nhập riêng vì đổi bot ở 1 tab sẽ đổi cả trình duyệt · Evidence: 9 mốc thời gian đo được + ảnh Chatwork room 417532006 |
| TC-ENV003-01 | API | ENV-003 | EP-16 `POST /ajax/popup/init-data-detail-click` — đóng vòng lặp giám sát sau release | Normal | manual | product | Sau release, endpoint không còn xuất hiện trong báo cáo request chậm | - Đã release branch `ai_small_41075` lên production và đã chạy migration<br>- Biết mốc số liệu trước fix: max 135s · p95 135s · trung bình 84s · 4 lần chậm trong 3 ngày (2026-09-14 → 2026-09-17)<br>- Có quyền xem báo cáo request chậm bắn lên Chatwork room 417532006 | 1. Ghi lại ngày giờ hoàn tất release và hoàn tất migration<br>2. Theo dõi liên tục **7 ngày** kể từ mốc đó<br>3. Mỗi ngày kiểm báo cáo request chậm xem có dòng nào cho `/ajax/popup/init-data-detail-click` không<br>4. Cuối kỳ, tổng hợp số lần chậm và thời gian max / p95 / trung bình của endpoint này<br>5. So với mốc trước fix | Cửa sổ quan sát 7 ngày sau release, trên đúng server `step.lme.jp` | - Trong 7 ngày, endpoint **không xuất hiện** ở mức SUPPERSLOW (≥15s) và VERYSLOW (≥10s) lần nào<br>- Nếu còn xuất hiện ở mức SLOWLV1 (≥5s): ghi lại số lần, bot id và khoảng ngày người dùng đã chọn, so với 1 lần / 3 ngày trước fix<br>- Thời gian max trong kỳ **giảm ít nhất 1 bậc độ lớn** so với 135s<br>- Nếu vẫn còn SUPPERSLOW → fix chưa đạt, raise lại ticket kèm bot id và khoảng ngày | | Lấp Q3 · R3 · manual vì bắt buộc production + theo dõi giám sát thật · Môi trường: PRODUCTION · Đánh giá spec: Đã hỏi leader · **Đây là TC đóng ticket** — Dev tự nêu "chưa đo được thời gian thực tế… cần theo dõi lại số liệu giám sát sau khi release" (`03-dev-impact.md` mục 7.4). Không có TC này thì không ai kết luận được bug gốc đã hết · Evidence: ảnh báo cáo Chatwork 7 ngày + bảng tổng hợp số liệu trước/sau |

---

## 6. Spec update needed

| # | Nội dung | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|
| 1 | **Index hiện có của `detail_landing_click`** — Dev ghi "chỉ index `landing_id`" và "migration `2025_06_13_164227` định thêm index `post_code` nhưng dòng đó bị comment". Nhưng `spec-features/admin/qr-landing/db/db-mapping.md:311-312` ghi schema production có **cả 2**: `detail_landing_click_landing_id_index` **và** `detail_landing_click_post_code_index` | `03-dev-impact.md` mục 6 (Bằng chứng) ↔ `spec-features/admin/qr-landing/db/db-mapping.md:309-312` | Dev + DBA — cần `SHOW INDEX` thật trên production trước khi chạy migration. Ảnh hưởng trực tiếp tới expected của **NEW-27** ("index sẵn có vẫn còn nguyên") |
| 2 | **Spec cho Popup (FA-018) chưa có trong `spec-features/`** — chỉ tồn tại trong spec repo của Studio (`popup/feature-spec.md:165` EP-16, `:53` SCR-PU-03, `ui-spec.md:235-262`) | `spec-features/admin/` không có thư mục `popup` | Leader — chốt bổ sung `spec-features/admin/popup/` hay công nhận spec Studio là nguồn chính thức cho FA-018 |
| 3 | **Danh sách nơi dùng chung 2 bảng nhật ký chưa được Dev kê đủ** — thiếu cron A-1, `HandleExportCsvTask`, EP-37/38/39/41 (`detail_landing_click`) và job đổi LOA step2 (`detail_action_popup`) | `03-dev-impact.md` mục 3 + 4.3 ↔ `spec-features/admin/qr-landing/db/db-mapping.md:68` + `spec-features/admin/change-bot/db/db-mapping.md:177` | Dev — bổ sung vào mục 3 và 4.3 của đánh giá ảnh hưởng, rồi tester tick lại checkbox verify |
| 4 | **`detail_landing_click` thiếu 2 cột trong dump schema** (`action_multi_capture_id`, `message_id` — có thật trên production, `db-mapping.md:293-302` ghi "dump LỖI THỜI"). Không ảnh hưởng ticket này nhưng ai dựng lại DB để test migration sẽ thiếu cột và Spring Boot crash | `spec-features/admin/qr-landing/db/db-mapping.md:302` | Dev/DBA — export lại schema trước khi ai đó dựng môi trường test cho TC-REGRUN001-03 |
