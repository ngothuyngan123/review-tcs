# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | `(1) MCP LME TEST STUDIO — task #216 (ticket 37845, round 1, branch ai_fixbug_37845)` |
| Tổng số TC review | `30` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `8/12 vùng ảnh hưởng đủ TC · 2 GAP · 3 RISK`

> Chiều (b) lấy từ Studio `task_get_context(216, ["dev_impact","spec_delta"])` — `diffAvailable = true`, `diffStat` = 1 file / 29 insertions / 3 deletions (`app/Http/Controllers/Admin/UserController.php`), `diffStrategy = stat`.

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | Route group `/admin` **không gắn `basic_access`** — root cause tầng route (Dev: `routes/web.php:547` nằm trong group `['admin_access','https_protocol','check_remember_token']`). Fix chỉ vá **3 endpoint của riêng màn này** bằng guard trong controller, **không** đóng lỗ ở tầng route → các màn/endpoint **sibling cùng group** vẫn có thể bypass y hệt | `diff code` | `không có` | `GAP — 0 TC rà endpoint sibling cùng group thiếu basic_access. REG-SHARED-001 yêu cầu "fix bug ở chức năng A → rà chức năng B có logic tương tự"; Dev mục 3 chỉ liệt kê caller của hàm quyền, KHÔNG liệt kê route đồng cảnh` | `[BLOCKER]` |
| G2 | Endpoint **lấy danh sách nhân viên theo tài khoản** (`list-staff-by-account`) — chính màn lịch sử gọi để dựng checkbox staff ở side panel lọc | `diff code` | `không có` | `GAP — Studio dev_impact ghi rõ "Nhánh còn hở đã biết (ngoài scope): endpoint list-staff-by-account chưa kiểm scope"; staff đã bị chặn khỏi màn vẫn gọi thẳng được → lộ **tên nhân viên (PII)**. 0 TC ghi nhận hiện trạng` | `[MAJOR]` |
| G3 | `D1 user_access_bot` + `D2 user_staff_bots` — khẳng định "chỉ SELECT, không đổi dữ liệu" | `dev-impact` | `TC-TOOLNEGCTRL001-01` | `RISK — chỉ được cover bởi TC mang mã quan điểm lạ TOOL-NEGCTRL-001 (không có trong checklist-lme) → theo BƯỚC 0.6 không tính là cover` | `[MINOR]` |
| G4 | `T2 — màn cha 「スタッフ管理」` (dùng chung `getListBotIdStaffManagement`; Studio dev_impact: *"nếu logic quyền màn cha thay đổi sẽ ảnh hưởng cả màn lịch sử"*) | `dev-impact` | `TC-REGSHARED001-04` | `RISK — [AP-3] chỉ 1 TC happy-path (bot có sẵn dữ liệu, staff đã accept). Không có TC regression ở edge state: bot 0 nhân viên · staff 「招待中」 · role không có quyền → dropdown/danh sách/nút 「ログイン履歴」` | `[MAJOR]` |
| G5 | `TC-DATAHIST001-01` — verify bản ghi lịch sử **sinh ra** khi staff truy cập bot | `orphan` | — | `[AP-5] Layer-downstream: đường GHI bản ghi nằm ở AdminAccess::handle / setBotInvite — KHÔNG nằm trong diff (diff chỉ có UserController.php). Fix chỉ chạm đường ĐỌC` | `[NIT]` |

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `14 quan điểm Trigger khớp task · 9 chưa cover đủ`

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `PERM-001` | `Cao` (BẮT BUỘC khi chức năng có phân biệt quyền) | `GAP — 0 TC. Fix đổi hẳn tập người xem được màn nhưng KHÔNG có TC đăng nhập từng role đối chiếu ma trận menu/nút. Câu hỏi chưa ai trả lời: staff KHÔNG có quyền có còn NHÌN THẤY nút 「ログイン履歴」 trên màn cha không? Nếu còn → nút dẫn thẳng tới màn bị chặn (false affordance)` | `[BLOCKER]` |
| Q2 | `ENV-003` | `Cao` (BẮT BUỘC — tính năng sinh **file CSV**) | `GAP — 0/30 TC khai product. Rủi ro chỉ lộ trên production (Catalog D): production chạy **2 server loadbalance**, file CSV ghi ở `public/msg_template/access-histories-bot/{timestamp}.csv` trên server A rồi request tải có thể rơi vào server B → 404. Không kết luận được từ local` | `[MAJOR]` |
| Q3 | `DATA-TEXT-001` | `Trung bình → **Cao**` (text được export CSV) | `GAP — 0 TC. `staff_name` là text tự do, file CSV encode **Shift-JIS** (`mb_convert_encoding`). Ký tự ngoài bảng SJIS (emoji, ①②③, ㈱, ký tự kiểu cũ) bị nuốt/mojibake. TC-OUTEXPORT001-01 chỉ kiểm "tên tiếng Nhật không lỗi font" — chưa chạm ký tự đặc biệt` | `[MAJOR]` |
| Q4 | `OUT-EXPORT-001` | `Trung bình → **Cao**` (BẮT BUỘC với mọi chức năng export CSV) | `RISK — 2 TC đều `Normal`, thiếu Abnormal + Boundary (RULE-01, không ghi lý do). Trong 8 điểm kiểm của quan điểm còn trống: dữ liệu chứa **dấu phẩy / xuống dòng** phải bọc `"` · export **vượt ngưỡng** (>500 bản ghi, quy mô khách lớn nhất) · tên file` | `[MAJOR]` |
| Q5 | `LIST-001` | `Trung bình` (BẮT BUỘC — màn danh sách có search/filter/pagination) | `RISK — chỉ được cover bởi TC mã lạ `SEARCH-001` → không tính cover. Fix làm `bot_id` thành **tham số bắt buộc**; chưa có TC chạy filter ngày/keyword/staff **kèm phân trang trang 2+** để chứng minh FE vẫn gửi kèm `bot_id` (thiếu → 403 giữa chừng)` | `[MAJOR]` |
| Q6 | `PERM-003` | `Cao` (BẮT BUỘC — tổ chức nhiều LINE OA / có change bot) | `RISK — 1 TC duy nhất, loại `Normal`; thiếu Abnormal + Boundary (RULE-01). Chưa có TC **đổi bot giữa chừng** sang bot ngoài quyền, và chưa có TC staff chỉ có quyền đúng **1 bot** (dropdown 1 phần tử — biên dưới)` | `[MAJOR]` |
| Q7 | `PERM-004` | `Trung bình → **Cao**` (quyền liên quan PII) | `RISK — 2 TC đều `Abnormal`, đều là chiều **THU HỒI** quyền. Thiếu chiều ngược lại: **CẤP** quyền cho staff đang đăng nhập → có vào được ngay không hay phải login lại` | `[MAJOR]` |
| Q8 | `UI-003` | `Trung bình → **Cao**` (rủi ro false success) | `RISK — khi API trả **403 giữa phiên** (quyền bị thu hồi lúc màn đang mở), chưa có TC xác nhận màn hiển thị **thông báo lỗi rõ ràng**. Nguy cơ: `access_histories.js` nuốt 403 → bảng rỗng trông y hệt "không có dữ liệu", hoặc spinner quay vô hạn` | `[MAJOR]` |
| Q9 | `FUNC-001` · `PERM-002` · `SEC-001` | `Cao` | `RISK — RULE-01 thiếu loại case, không TC nào ghi lý do: FUNC-001 chỉ `Normal`(1) · PERM-002 cả 4 TC đều `Abnormal` · SEC-001 cả 3 TC đều `Abnormal`. **Giảm nhẹ**: chiều còn lại thực tế có TC nhưng nằm dưới mã lạ (`API-001`) — xử lý bằng cách gán lại mã chuẩn (§4 #5), không cần viết TC mới` | `[MINOR]` |

---

## 3. TC trùng lặp nội dung

Đã rà **30/30** TC. Phát hiện **1 nhóm trùng**.

| Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| DUP-1 | `TC-REGSHARED001-03` (Studio #16665 / NEW-46) | `TC-REGSHARED001-02` (Studio #13993 / NEW-29) — **KHÔNG xóa** | `DUP-CONFLICT` | ① mã quan điểm `REG-SHARED-001` ② loại case `Abnormal` ③ đối tượng + thao tác: **nhân viên được mời có quyền 「スタッフ管理」 bấm nút 「CSV」 xuất lịch sử** ④ tiền đề tương đương (staff đã accept, role có quyền, bot có sẵn lịch sử) | `[MAJOR]` |

**Expected mâu thuẫn trực tiếp:**
- `TC-REGSHARED001-02`: *"ghi lại chính xác một trong hai kết cục (a) tải được / (b) bị đá về /admin/home … **tuyệt đối không kết luận #37845 Không đạt vì case này**"* → chấm **Đạt**.
- `TC-REGSHARED001-03`: *"phải tải được file CSV … không bị chuyển hướng về /admin/home"* → chấm **Không đạt**, đã raise ticket **40714**.

⇒ Cùng một kịch bản, hai chuẩn chấm ngược nhau, và **cả hai đều được tính vào thống kê 28/30 pass**.

- **Gate đã chạy**: giả định xóa `TC-REGSHARED001-02` → coverage §1 + §2 **còn nguyên** (REQ-011/REQ-012 vẫn còn `TC-REGSHARED001-03` + 3 TC khác cover). Tuy nhiên đây là `DUP-CONFLICT` → **không đề nghị xóa**, chờ Leader chốt chuẩn.
- `DUP-INFLATE` che GAP: `không có`.
- `DUP-CONFLICT` → đã đưa vào **§6** để Dev/Leader chốt.

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[MAJOR]` | Toàn bộ 30 TC | **RULE-02 — cột `Evidence thực tế` rỗng 30/30** dù 28 TC đã chấm `Đạt`. 26/30 kết quả do `pipeline` AI tự chạy (`last_exec.source = ai`), 4 TC do `vinth`. Kết quả hiện là **tự khai, không có bằng chứng đính kèm** | Bổ sung evidence đúng loại theo từng quan điểm trước khi chốt Đạt: PERM-002/SEC-001 → mã lỗi trả về khi gọi API; OUT-EXPORT-001 → **file CSV thật** + screenshot màn tương ứng; PERM-001 → screenshot từng role |
| 2 | `[MAJOR]` | Toàn bộ 30 TC | **RULE-08 / ENV-003 — 30/30 TC chạy ở `env = local`** (7 run), 0 TC ở dev/staging/prd. Task **sinh file** (CSV) → thuộc nhóm bắt buộc không được kết luận từ local | Chạy lại tối thiểu nhóm CSV (`TC-OUTEXPORT001-01/-02`, `TC-REGSHARED001-02/-03`) trên **production**; xem TC bổ sung `TC-ENV003-01` ở §5 |
| 3 | `[MAJOR]` | 29/30 TC | **Cột `Trạng thái đánh giá spec` rỗng**. TC duy nhất có giá trị là `TC-REGSHARED001-02` ghi `deferred-non-blocking` — **không thuộc enum** (`Spec ghi rõ` / `Spec không ghi` / `Đã hỏi leader`) | Điền enum hợp lệ cho từng TC; case dựa trên hành vi chưa có trong spec phải ghi `Spec không ghi` + nêu **đã hỏi ai** (đặc biệt REQ-009 「招待中」 và REQ-011/012) |
| 4 | `[MAJOR]` | 7/30 TC (23%) | **5 mã quan điểm không có trong `framework/checklist-lme.md`**: `TOOL-ERRHYG-001`(3) · `SEARCH-001` · `DATA-HIST-001` · `API-001` · `TOOL-NEGCTRL-001`. `/review-tc` không map được coverage → 7 TC này **không tính là cover** bất kỳ quan điểm nào | Gán lại mã chuẩn trên Studio (`testcase_update`): `SEARCH-001`→`LIST-001` · `API-001`→`SEC-001`/`FUNC-001` · `TOOL-NEGCTRL-001`→`DATA-001` · `TOOL-ERRHYG-001`→`FUNC-003`/`FUNC-004`. Việc này lấp luôn phần lớn Q5 + Q9 mà **không cần viết TC mới** |
| 5 | `[MINOR]` | `03-dev-impact.md` | Checkbox **"Tester verify auto-fill chính xác" chưa tick**, file `Auto-filled: 2026-09-08 by /new-task`. Thêm nữa: mục 4.1 Redmine **chỉ liệt kê FILE**, không liệt kê function → bảng `F1–F6` là do `/new-task` map lại từ mục 3, chưa ai xác nhận | Tester đọc lại Journal #133046 trên Redmine, xác nhận F/D/T rồi tick checkbox |
| 6 | `[NIT]` | Nguồn diff | Studio trả `diffStrategy = "stat"`, `hasCode = false` → chỉ có **số dòng thay đổi**, không có nội dung hunk. Không verify được fix-shape ở mức dòng lệnh (AP-4) | Nếu Leader cần chốt fix-shape: yêu cầu Dev đính link PR / diff đầy đủ (hiện chỉ có commit `7fc394d0a1`) |

### Chất lượng từng TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 7 | `[MAJOR]` | `TC-REGSHARED001-02` | **`Kết quả mong đợi` không đo lường được** — chấp nhận *cả hai* kết cục (a) và (b) rồi vẫn chấm `Đạt`. TC **pass trong mọi trường hợp** ⇒ không có giá trị kiểm chứng, nhưng vẫn được đếm vào 28/30 pass | Sau khi Leader chốt chuẩn ở §6: đổi thành 1 expected duy nhất, hoặc chuyển hẳn sang trạng thái "ghi nhận hiện trạng" và **loại khỏi mẫu số pass rate** |
| 8 | `[MINOR]` | `TC-FUNC004-04` | **Không atomic** — gộp 2 input khác nhau trong 1 TC ("tài khoản **để trống hoặc bằng 0**"), trong khi phía màn hình cùng 2 input đó lại tách thành 2 TC riêng (`TC-FUNC004-02`, `-03`) | Tách thành 2 TC để đối xứng với phía màn hình, hoặc ghi rõ ở `Ghi chú` vì sao gộp |

> Không phát hiện AP-1 (không phải generic-fix), AP-2 (bug report có steps cụ thể), AP-6 (Dev đã list 9 caller ở mục 3).

---

## 5. TCs đề xuất bổ sung (15)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/` **chưa có FA-035 (スタッフ管理)** — không đối chiếu được. 18 file kho hiện có đều thuộc feature khác |
| Vùng regression phát hiện từ kho | `Không có` (kho chưa cover feature này) |
| Conflict expected vs kho | `Không` |
| GAP dùng lại TC kho (không viết mới) | `Không` |
| Xác nhận chống trùng | Đã đối chiếu **30** TC ở BƯỚC 0 + kho-tcs — **không TC đề xuất nào trùng**. `G3` và `Q9` **không đề xuất TC mới** (xử lý bằng gán lại mã quan điểm — §4 #4); `G5` không đề xuất TC mới (layer ngoài diff, AP-5) |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-REGSHARED001-06 | API | REG-SHARED-001 | Rà bypass sibling cùng route group | Abnormal | manual | Tất cả | Staff không quyền gõ thẳng URL các màn khác cùng nhóm route `/admin` thiếu cổng kiểm quyền | - Chủ tài khoản A sở hữu BOT-A1<br>- Staff S2 được mời vào BOT-A1, role **không** tick quyền nào<br>- Đã xin Dev danh sách các route trong group `/admin` **không** gắn `basic_access` | 1. Xin Dev danh sách route trong group `/admin` không có `basic_access` (cùng nhóm với `access-histories`)<br>2. Đăng nhập bằng Staff S2, chọn BOT-A1<br>3. Với **từng** route trong danh sách: gõ thẳng URL kèm `?bot_id=<BOT-A1>`<br>4. Ghi lại với mỗi route: URL cuối cùng, thông báo hiển thị, và màn có render dữ liệu không<br>5. Lặp lại bước 3 với `bot_id` của một bot Staff S2 hoàn toàn không liên quan | Danh sách route do Dev cung cấp; `bot_id` = BOT-A1 và bot ngoài quyền | Mọi route trong danh sách đều bị chặn: chuyển về `/admin/home` kèm 「この権限は許可されていません。」, không màn nào render dữ liệu nghiệp vụ. Route nào **vẫn vào được** phải được ghi lại kèm URL + ảnh chụp và mở ticket riêng — đây là cùng một lỗ hổng với #37845 ở màn khác | | Lấp `G1` · regression · Đánh giá spec: Spec không ghi — cần hỏi Dev danh sách route · Evidence: ảnh chụp URL cuối + thông báo của **từng** route đã thử |
| TC-PERM002-05 | API | PERM-002 | Endpoint lấy danh sách nhân viên theo tài khoản | Abnormal | manual | Tất cả | Gọi thẳng endpoint lấy danh sách nhân viên bằng staff không có quyền — kiểm có lộ tên nhân viên không | - Chủ tài khoản A sở hữu BOT-A1, BOT-A1 có ≥ 3 nhân viên tên khác nhau<br>- Staff S2 được mời vào BOT-A1, role **không** tick quyền 「スタッフ管理」<br>- Đã đăng nhập thật bằng S2 và giữ được phiên làm việc | 1. Đăng nhập bằng Staff S2, xác nhận mở màn lịch sử đăng nhập của BOT-A1 **bị chặn**<br>2. Dùng chính phiên đó gọi thẳng endpoint lấy danh sách nhân viên theo tài khoản với `bot_id` = BOT-A1<br>3. Ghi lại mã trả về và toàn bộ nội dung trả về<br>4. Đối chiếu xem nội dung có chứa tên nhân viên của BOT-A1 không | `bot_id` = BOT-A1 (bot Staff S2 không có quyền quản lý nhân viên) | Endpoint phải từ chối (403) và **không trả về tên nhân viên nào**. Nếu vẫn trả về danh sách tên → **ghi nhận là lỗ hổng lộ PII còn tồn đọng**, mở ticket riêng; **KHÔNG** dùng kết quả này để chấm #37845 Không đạt (Dev đã ghi rõ ngoài phạm vi fix) | | Lấp `G2` · Đánh giá spec: Spec không ghi — Dev đã ghi nhận ở mục quét ngang · Evidence: mã trả về + nội dung response (che bớt PII) |
| TC-REGSHARED001-07 | UI | REG-SHARED-001 | Màn cha 「スタッフ管理」 — regression edge state | Abnormal | manual | Tất cả | Màn Quản lý nhân viên vẫn đúng ở trạng thái biên sau khi màn lịch sử đổi nguồn quyền | - Chủ tài khoản A sở hữu BOT-A1 (**0 nhân viên**) và BOT-A2 (có nhân viên, trong đó 1 người ở trạng thái 「招待中」)<br>- Staff S1 được mời vào BOT-A2, role có quyền 「スタッフ管理」 | 1. Đăng nhập bằng chủ tài khoản A, mở màn 「スタッフ管理」 với BOT-A1 (bot chưa có nhân viên nào)<br>2. Kiểm tra dropdown 「対象のアカウント」, danh sách nhân viên và nút 「ログイン履歴」<br>3. Chuyển dropdown sang BOT-A2, xác nhận nhân viên 「招待中」 vẫn hiển thị đúng trạng thái<br>4. Đăng nhập lại bằng Staff S1, mở màn 「スタッフ管理」 và so **từng phần tử** của dropdown với dropdown trên màn lịch sử đăng nhập | BOT-A1 = 0 nhân viên; BOT-A2 = có nhân viên 「招待中」 | Bước 1–3: màn cha giữ nguyên hành vi cũ — bot 0 nhân viên hiện danh sách rỗng đúng cách (không trắng màn, không lỗi), dropdown và nút 「ログイン履歴」 vẫn đủ, trạng thái 「招待中」 hiển thị đúng. Bước 4: dropdown màn cha và dropdown màn lịch sử của Staff S1 **liệt kê đúng cùng một tập bot, cùng thứ tự** | | Lấp `G4` · regression · [AP-3] bổ sung edge state · Đánh giá spec: Spec ghi rõ (BR-004) · Evidence: ảnh chụp 2 dropdown đặt cạnh nhau |
| TC-PERM001-01 | UI | PERM-001 | Ma trận quyền — nút 「ログイン履歴」 | Normal | manual | Tất cả | Chủ tài khoản và staff có quyền đều thấy và vào được nút 「ログイン履歴」 | - Chủ tài khoản A sở hữu BOT-A1<br>- Staff S1 được mời vào BOT-A1, **đã accept**, role R-YES có tick quyền 「スタッフ管理」<br>- Đã xin Leader/Dev **ma trận quyền** của FA-035 | 1. Đăng nhập bằng chủ tài khoản A, mở màn 「スタッフ管理」 của BOT-A1<br>2. Ghi lại nút 「ログイン履歴」 hiện hay ẩn; bấm vào và ghi lại màn đích<br>3. Đăng xuất, đăng nhập bằng Staff S1, chọn BOT-A1, lặp lại bước 1–2<br>4. Đối chiếu 2 kết quả với ma trận quyền đã xin | 2 role: chủ tài khoản A, Staff S1 (role R-YES) | Cả 2 role đều **thấy** nút 「ログイン履歴」 và bấm vào mở được màn lịch sử của đúng BOT-A1, bảng lịch sử render dữ liệu, không hiện thông báo thiếu quyền. Kết quả khớp ma trận quyền | | Lấp `Q1` · RULE-01 (Normal) · Đánh giá spec: Spec ghi rõ (BR-004, SCR-EMP-01) · Evidence: ma trận quyền đã tick + ảnh chụp từng role |
| TC-PERM001-02 | UI | PERM-001 | Ma trận quyền — nút 「ログイン履歴」 | Abnormal | manual | Tất cả | Staff KHÔNG có quyền 「スタッフ管理」 — nút 「ログイン履歴」 phải nhất quán với việc màn bị chặn | - Chủ tài khoản A sở hữu BOT-A1<br>- Staff S2 được mời vào BOT-A1, đã accept, role R-NO **không** tick quyền 「スタッフ管理」 | 1. Đăng nhập bằng Staff S2, chọn BOT-A1<br>2. Mở màn 「スタッフ管理」 (nếu vào được) và ghi lại nút 「ログイン履歴」 hiện hay ẩn<br>3. Nếu nút còn hiện → bấm vào, ghi lại URL cuối + thông báo<br>4. Ghi lại toàn bộ menu/nút hiển thị cho role R-NO, đối chiếu ma trận quyền | Staff S2, role R-NO (không có quyền 「スタッフ管理」) | Nút 「ログイン履歴」 **không được** dẫn tới màn xem được dữ liệu. Kết quả đúng: nút bị ẩn cùng với màn 「スタッフ管理」. Nếu nút vẫn hiện mà bấm vào bị đá về `/admin/home` kèm 「この権限は許可されていません。」 → ghi nhận là **lỗi nhất quán UI** (nút mời gọi thao tác không được phép) và báo Leader | | Lấp `Q1` · RULE-01 (Abnormal) · Đánh giá spec: Spec không ghi — cần hỏi Leader nút nên ẩn hay chỉ chặn ở server · Evidence: ảnh chụp màn cha của role R-NO + URL cuối sau khi bấm |
| TC-PERM001-03 | UI | PERM-001 | Ma trận quyền — nút 「ログイン履歴」 | Boundary | manual | Tất cả | Staff có quyền trên đúng 1 trong 2 bot — nút và màn phải đổi theo bot đang chọn | - Chủ tài khoản A sở hữu BOT-A1 và BOT-A2<br>- Staff S3 được mời vào **cả hai**, đã accept; role trên BOT-A1 **có** quyền 「スタッフ管理」, role trên BOT-A2 **không** có | 1. Đăng nhập bằng Staff S3, chọn BOT-A1<br>2. Ghi lại nút 「ログイン履歴」 và mở màn lịch sử, ghi lại danh sách bot trong dropdown 「対象のアカウント」<br>3. Chuyển tài khoản đang làm việc sang BOT-A2<br>4. Lặp lại bước 2 với BOT-A2<br>5. Trong lúc đang ở ngữ cảnh BOT-A2, gõ thẳng URL màn lịch sử kèm `bot_id` của BOT-A1 | Staff S3 có quyền đúng 1/2 bot | Ở BOT-A1: vào được màn, dropdown chứa **đúng 1 phần tử là BOT-A1** (không có BOT-A2). Ở BOT-A2: bị chặn về `/admin/home` kèm 「この権限は許可されていません。」. Bước 5: vẫn vào được màn lịch sử của BOT-A1 (đúng quyền), dữ liệu hiển thị là của BOT-A1 chứ không phải BOT-A2 | | Lấp `Q1` + `Q6` · RULE-01 (Boundary) · Đánh giá spec: Spec ghi rõ (REQ-002) · Evidence: ảnh chụp dropdown ở cả 2 ngữ cảnh |
| TC-PERM003-02 | UI | PERM-003 | Cách ly dữ liệu khi đổi bot trên màn lịch sử | Abnormal | manual | Tất cả | Đổi dropdown sang bot ngoài quyền bằng cách sửa URL — dữ liệu không được lẫn | - Chủ tài khoản A sở hữu BOT-A1; chủ tài khoản B sở hữu BOT-B1<br>- Staff S1 có quyền 「スタッフ管理」 trên BOT-A1, không liên quan BOT-B1<br>- Cả 2 bot đều có bản ghi lịch sử, cố ý tạo **2 bản ghi trùng tên nhân viên** ở 2 bot | 1. Đăng nhập Staff S1, mở màn lịch sử của BOT-A1, ghi lại toàn bộ dòng đang hiển thị<br>2. Sửa `bot_id` trên thanh URL thành `bot_id` của BOT-B1, tải lại trang<br>3. Ghi lại URL cuối, thông báo và nội dung bảng<br>4. Quay lại BOT-A1, xác nhận bảng lịch sử vẫn đúng như bước 1 | `bot_id` lần lượt: BOT-A1 (trong quyền) → BOT-B1 (chủ khác) | Bước 2–3: bị chuyển về `/admin/home` kèm 「この権限は許可されていません。」, **không dòng nào của BOT-B1 hiển thị**, kể cả dòng trùng tên nhân viên. Bước 4: dữ liệu BOT-A1 nguyên vẹn, không lẫn bản ghi lạ | | Lấp `Q6` · RULE-01 (Abnormal) · Đánh giá spec: Spec ghi rõ (REQ-001) · Evidence: ảnh chụp bảng trước/sau + URL cuối |
| TC-PERM004-03 | UI | PERM-004 | Cấp quyền có hiệu lực ngay | Normal | manual | Tất cả | Vừa được CẤP quyền 「スタッフ管理」 thì vào được màn lịch sử ngay, không cần đăng nhập lại | - Chủ tài khoản A sở hữu BOT-A1<br>- Staff S2 đã accept lời mời vào BOT-A1, role **chưa** tick quyền 「スタッフ管理」<br>- Staff S2 **đang đăng nhập** ở trình duyệt khác, đã xác nhận bị chặn khỏi màn lịch sử | 1. Trình duyệt 1: Staff S2 mở màn lịch sử của BOT-A1 → xác nhận bị chặn<br>2. Trình duyệt 2: chủ tài khoản A vào 「操作権限を変更する」, tick quyền 「スタッフ管理」 cho role của S2, lưu<br>3. Trình duyệt 1 (**không** đăng xuất, không đăng nhập lại): tải lại màn lịch sử của BOT-A1<br>4. Ghi lại dropdown 「対象のアカウント」 và nội dung bảng | Role của Staff S2: từ không có quyền → có quyền 「スタッフ管理」 | Ngay ở bước 3, phiên cũ của Staff S2 **vào được** màn lịch sử của BOT-A1: không còn thông báo thiếu quyền, dropdown xuất hiện BOT-A1, bảng lịch sử hiển thị đúng bản ghi của BOT-A1 | | Lấp `Q7` · RULE-01 (Normal, chiều ngược của TC-PERM004-01) · Đánh giá spec: Spec không ghi — cần hỏi Leader · Evidence: ảnh chụp trước/sau khi cấp quyền trên **cùng một phiên** |
| TC-LIST001-01 | UI | LIST-001 | Lọc + phân trang màn lịch sử | Normal | manual | Tất cả | Lọc ngày/nhân viên/từ khoá kết hợp phân trang vẫn giữ đúng tài khoản sau khi bot_id thành bắt buộc | - Chủ tài khoản A sở hữu BOT-A1<br>- BOT-A1 có **> 150 bản ghi** lịch sử trải trên ≥ 3 ngày, ≥ 3 nhân viên khác nhau (để có ít nhất 2 trang, mặc định 100 dòng/trang) | 1. Mở màn lịch sử của BOT-A1<br>2. Đặt bộ lọc khoảng ngày phủ toàn bộ dữ liệu, bấm 「表示」<br>3. Chuyển sang **trang 2**, ghi lại URL/`bot_id` gửi kèm và nội dung bảng<br>4. Đang ở trang 2, nhập từ khoá tên 1 nhân viên rồi bấm 「検索」<br>5. Mở side panel 「絞り込み」, tick 1 nhân viên cụ thể, bấm 「絞り込み検索」<br>6. Sau mỗi bước 3–5, kiểm tra không xuất hiện thông báo thiếu quyền và không bị đá về màn Home | > 150 bản ghi; lọc theo khoảng ngày + từ khoá tên nhân viên + checkbox 1 nhân viên | Mọi thao tác lọc/chuyển trang đều trả dữ liệu bình thường, **không lần nào bị 403 hay bị chuyển về `/admin/home`**. Trang 2 chỉ chứa bản ghi của BOT-A1. Sau khi lọc, bộ lọc được giữ khi chuyển trang; số dòng và nội dung khớp điều kiện lọc cuối cùng | | Lấp `Q5` · regression · Đánh giá spec: Spec ghi rõ (EP-12) · Evidence: ảnh chụp trang 2 + tab Network cho thấy `bot_id` được gửi kèm ở mọi request |
| TC-UI003-01 | UI | UI-003 | Trạng thái lỗi của bảng lịch sử | Abnormal | manual | Tất cả | Quyền bị thu hồi giữa phiên — màn phải báo lỗi rõ, không hiện bảng rỗng như "không có dữ liệu" | - Chủ tài khoản A sở hữu BOT-A1<br>- Staff S1 có quyền 「スタッフ管理」 trên BOT-A1, đang mở sẵn màn lịch sử với bảng **đang có dữ liệu**<br>- Có trình duyệt thứ 2 đăng nhập chủ tài khoản A | 1. Trình duyệt 1 (Staff S1): màn lịch sử BOT-A1 đang hiển thị dữ liệu, mở sẵn công cụ nhà phát triển ở thẻ mạng<br>2. Trình duyệt 2 (chủ tài khoản A): bỏ tick quyền 「スタッフ管理」 khỏi role của S1, lưu<br>3. Trình duyệt 1: **không tải lại trang**, bấm 「表示」 hoặc chuyển trang để phát sinh request mới<br>4. Ghi lại mã trả về của request và **những gì người dùng nhìn thấy trên màn**<br>5. Tải lại trang và ghi lại URL cuối + thông báo | Thu hồi quyền trong lúc màn đang mở | Request trả **403**. Trên màn người dùng phải thấy **thông báo lỗi quyền rõ ràng** — không được chỉ hiện bảng trống (dễ hiểu nhầm thành "không có dữ liệu"), không được quay spinner vô hạn, không được trắng màn. Bước 5: chuyển về `/admin/home` kèm 「この権限は許可されていません。」 | | Lấp `Q8` · Đánh giá spec: Spec không ghi — cần hỏi Leader thông báo mong đợi trên UI · Evidence: video thao tác + ảnh chụp tab Network (403) kèm màn hình tại thời điểm đó |
| TC-OUTEXPORT001-03 | UI | OUT-EXPORT-001 | Xuất CSV lịch sử — dữ liệu có ký tự phân tách | Abnormal | manual | Tất cả | Tên nhân viên chứa dấu phẩy / dấu nháy kép / xuống dòng — CSV không được vỡ cột | - Chủ tài khoản A sở hữu BOT-A1<br>- Tạo 3 nhân viên có tên lần lượt chứa: dấu phẩy (`山田, 太郎`), dấu nháy kép (`"テスト"スタッフ`), ký tự xuống dòng<br>- Mỗi nhân viên có ≥ 1 bản ghi lịch sử trên BOT-A1 | 1. Mở màn lịch sử của BOT-A1, ghi lại 3 dòng của 3 nhân viên trên<br>2. Bấm nút 「CSV」 và tải file về<br>3. Mở file bằng Excel và đối chiếu số cột của từng dòng<br>4. Mở lại file bằng trình soạn thảo văn bản, kiểm tra cách các giá trị đặc biệt được bọc | Tên nhân viên chứa `,` · `"` · ký tự xuống dòng | File CSV giữ đúng **3 cột** 「アクセス日時」「スタッフ名」「IPアドレス」 ở **mọi dòng** — dòng có dấu phẩy không bị tách thành 4 cột, dấu nháy kép không làm lệch cột, tên có xuống dòng vẫn nằm trong 1 ô. Giá trị đọc được trên Excel khớp đúng tên hiển thị trên màn | | Lấp `Q4` · RULE-01 (Abnormal) · Đánh giá spec: Spec không ghi — cần hỏi Leader quy tắc escape · Evidence: file CSV thật + ảnh chụp mở bằng Excel |
| TC-OUTEXPORT001-04 | UI | OUT-EXPORT-001 | Xuất CSV lịch sử — khối lượng lớn | Boundary | manual | product | Xuất CSV ở quy mô khách hàng lớn nhất — không cắt dòng, không treo | - Chủ tài khoản có bot với lượng bản ghi lịch sử **bằng hoặc lớn hơn khách hàng lớn nhất hiện tại** (xin số liệu từ Leader/Dev; tối thiểu > 500 bản ghi)<br>- Đã đếm chính xác số bản ghi trong khoảng thời gian sẽ export | 1. Mở màn lịch sử, đặt khoảng thời gian phủ toàn bộ dữ liệu, ghi lại **tổng số bản ghi** hiển thị<br>2. Bấm 「CSV」, bấm đồng hồ đo thời gian từ lúc bấm tới lúc file tải xong<br>3. Đếm số dòng dữ liệu trong file (không tính dòng tiêu đề)<br>4. So số dòng file với tổng số bản ghi ghi ở bước 1 | Số bản ghi lấy theo quy mô khách hàng lớn nhất thực tế (ghi rõ nguồn số liệu) | Số dòng trong file **bằng đúng** tổng số bản ghi ở bước 1 — không bị cắt ở 100 (giới hạn phân trang của màn) hay 500. File tải về được, không lỗi hết thời gian chờ, không lỗi máy chủ. Ghi lại thời gian chạy thực tế | | Lấp `Q4` · RULE-01 (Boundary) · RULE-08 (khối lượng lớn → product) · Đánh giá spec: Spec không ghi — cần hỏi Leader ngưỡng chấp nhận · Evidence: file CSV + số liệu quy mô (nguồn) + thời gian chạy |
| TC-DATATEXT001-01 | UI | DATA-TEXT-001 | Xuất CSV lịch sử — ký tự đặc biệt qua Shift-JIS | Abnormal | manual | product | Tên nhân viên có emoji / ①②③ / ㈱ — CSV mã hoá Shift-JIS không được nuốt ký tự | - Chủ tài khoản A sở hữu BOT-A1<br>- Tạo các nhân viên có tên chứa lần lượt: emoji (😀), ký tự vòng tròn (①②③), ㈱, và ký tự Hán tự kiểu cũ<br>- Mỗi người có ≥ 1 bản ghi lịch sử trên BOT-A1 | 1. Mở màn lịch sử của BOT-A1, chụp màn hình cột 「スタッフ名」 của các dòng trên<br>2. Bấm 「CSV」 và tải file<br>3. Mở file bằng Excel (mã hoá Shift-JIS) và đối chiếu từng tên với ảnh ở bước 1<br>4. Mở lại file bằng trình soạn thảo hỗ trợ chọn mã hoá, xác nhận từng ký tự đặc biệt còn nguyên hay đã bị thay/mất | Tên nhân viên chứa 😀 · ①②③ · ㈱ · Hán tự kiểu cũ | Tên trên màn quản trị và tên trong file CSV **khớp nhau hoàn toàn**, mở bằng Excel không mojibake. Ký tự nào không biểu diễn được bằng Shift-JIS thì phải có quy tắc xử lý nhất quán và **được ghi nhận rõ** (thay bằng ký tự thay thế) — **không được nuốt âm thầm** làm lệch tên nhân viên | | Lấp `Q3` · RULE-06 (đi tới file export thật) · RULE-08 (file/encoding → product) · Đánh giá spec: Spec không ghi — cần hỏi Leader cách xử lý ký tự ngoài Shift-JIS · Evidence: ảnh chụp màn + file CSV thật mở bằng Excel |
| TC-ENV003-01 | UI | ENV-003 | Xuất CSV lịch sử trên production (loadbalance) | Normal | manual | product | Tải file CSV trên production có 2 server — link tải phải luôn lấy được file | - Tài khoản thật trên production (`step.lme.jp`) có quyền xem lịch sử đăng nhập của 1 bot<br>- Bot có sẵn bản ghi lịch sử<br>- Đã đối chiếu [Catalog D](../../framework/catalog-lme.md) mục môi trường production | 1. Đăng nhập production, mở màn lịch sử của bot<br>2. Bấm 「CSV」, ghi lại đường dẫn file trả về và mã trả về của request tải<br>3. Mở file để xác nhận tải được và nội dung đúng<br>4. **Lặp lại bước 2–3 ít nhất 5 lần liên tiếp** (để request rơi vào cả 2 server cân bằng tải), ghi lại router/server id nếu lấy được<br>5. Sau ~1 ngày, thử tải lại bằng đúng đường dẫn file đã sinh ở bước 2 | Tài khoản thật trên production; lặp ≥ 5 lần | **Cả 5 lần** đều tải được file, nội dung đầy đủ và đúng bot — không lần nào trả về không tìm thấy file. Nếu có lần nào không tải được thì phải ghi lại server/router id của lần đó (dấu hiệu file ghi ở server này nhưng đọc ở server kia). Bước 5: ghi nhận hành vi thực tế của file cũ (còn tải được hay đã bị dọn) | | Lấp `Q2` · RULE-08 / ENV-003 (file + loadbalance, không kết luận được từ local) · Đánh giá spec: Spec không ghi — cần hỏi Dev vòng đời file trong `public/msg_template/` · Evidence: log/ảnh chụp **lấy trên production** cho từng lần thử |
| TC-PERM003-03 | UI | PERM-003 | Cách ly dữ liệu — biên dưới của dropdown | Boundary | manual | Tất cả | Staff chỉ có quyền đúng 1 bot — dropdown 1 phần tử, không mở được bot khác | - Chủ tài khoản A sở hữu BOT-A1, BOT-A2, BOT-A3<br>- Staff S4 được mời vào **cả 3**, đã accept, nhưng chỉ role trên BOT-A2 có quyền 「スタッフ管理」<br>- Cả 3 bot đều có bản ghi lịch sử | 1. Đăng nhập Staff S4, chọn BOT-A2, mở màn lịch sử<br>2. Đếm và ghi lại **toàn bộ** phần tử trong dropdown 「対象のアカウント」<br>3. Thử mở dropdown và chọn bot khác (nếu chọn được)<br>4. Gõ thẳng URL màn lịch sử kèm `bot_id` của BOT-A1, rồi của BOT-A3<br>5. Ghi lại URL cuối và nội dung màn ở mỗi lần | Staff S4 có quyền đúng 1/3 bot được mời | Dropdown chứa **đúng 1 phần tử là BOT-A2** — BOT-A1 và BOT-A3 không xuất hiện dù staff được mời vào cả hai. Bước 4: cả 2 lần đều bị chuyển về `/admin/home` kèm 「この権限は許可されていません。」, không hiện bản ghi nào của BOT-A1/BOT-A3 | | Lấp `Q6` · RULE-01 (Boundary) · Đánh giá spec: Spec ghi rõ (REQ-002) · Evidence: ảnh chụp dropdown + URL cuối của 2 lần thử |

---

## 6. Spec update needed

| # | Vấn đề | Nguồn | Cần chốt |
|---|---|---|---|
| 1 | **`DUP-CONFLICT` (§3)** — `TC-REGSHARED001-02` và `TC-REGSHARED001-03` mô tả cùng kịch bản (nhân viên được mời có quyền 「スタッフ管理」 xuất CSV) nhưng expected ngược nhau: một bên coi việc bị chặn là "ghi nhận, không phải lỗi", một bên coi là **Không đạt** và đã raise ticket **40714** | BƯỚC 4b | Leader/PO chốt: nhân viên được mời có quyền 「スタッフ管理」 **có được phép** xuất CSV lịch sử không? Chốt xong → sửa 1 trong 2 TC trên Studio và quyết định ticket 40714 giữ hay đóng |
| 2 | **REQ-009 「招待中」 [CHỜ CHỐT]** — Studio ghi nhận nguồn quyền không lọc trạng thái lời mời; Dev ghi ở mục rủi ro rằng nhân viên chưa accept **vẫn** được tính có quyền (kế thừa hành vi màn cha). Nhưng `TC-PERM004-02` đã ghi "Human đã chốt: Staff 「招待中」 chưa có quyền" và chấm **Không đạt** + ticket **40709** | BƯỚC 1 + Studio `requirements` | Chốt chính thức và ghi vào spec: nhân viên 「招待中」 có quyền xem lịch sử không. Nếu chốt là **không** → đây là thay đổi hành vi của **hàm dùng chung với màn cha** ⇒ phải đánh giá lại ảnh hưởng lên 「スタッフ管理」 trước khi sửa |
| 3 | **Nút 「ログイン履歴」 khi staff không có quyền** — spec [feature-spec.md](../../spec-features/admin/staff-management/feature-spec.md) SCR-EMP-01 mô tả nút này trong toolbar nhưng **không quy định** nút phải ẩn hay chỉ bị chặn ở server đối với role không có quyền | BƯỚC 1 (spec) + `TC-PERM001-02` | Bổ sung vào spec quy tắc hiển thị nút theo role (liên quan trực tiếp `PERM-001` — hiện là GAP `[BLOCKER]` ở §2) |
| 4 | **Vòng đời file CSV trong `public/msg_template/access-histories-bot/`** — spec mô tả đường sinh file nhưng không nói file tồn tại bao lâu, có dọn định kỳ không, và trên production (2 server) file ghi ở server nào thì đọc ở server nào | BƯỚC 5a + `TC-ENV003-01` | Dev xác nhận cơ chế lưu/dọn file export trên production để chốt expected cho `TC-ENV003-01` |
| 5 | **Xử lý ký tự ngoài bảng Shift-JIS trong CSV** — spec ghi rõ encode Shift-JIS nhưng không quy định xử lý emoji / ký tự không biểu diễn được | BƯỚC 5a + `TC-DATATEXT001-01` | Chốt quy tắc: nuốt, thay bằng ký tự thay thế, hay chặn từ lúc nhập tên nhân viên |

<!-- Nguồn spec đã dùng: spec-features/admin/staff-management/feature-spec.md — SCR-EMP-04 (dòng 352-418), Field Traceability SCR-EMP-04 (724-735), BR-004 (780), BR-012/BR-013 (859-880), EP-12 (953-983). -->
