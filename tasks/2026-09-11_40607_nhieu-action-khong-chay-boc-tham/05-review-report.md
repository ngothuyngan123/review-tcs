# 05 — Review Report

## 0. Nguồn TC

- **Nguồn đã dùng**: MCP LME TEST STUDIO — task `#291` (ticket 40607), `testcase_list` + `task_get_report`, fetch lần đầu 2026-09-11 (15 TC) và **fetch lại 2026-09-14 (19 TC, 10 run)**.
- **Tổng số TC review**: **19** — run mới nhất `#1390` staging: **19/19 pass** (`aiResult = pass`, `openBugs = 0`); toàn bộ lịch sử auto: pass 42 · fail 4 · skip 9 · error 8, manual: pass 4 · fail 1.

---

## 1. Coverage GAP / RISK — 2 chiều `dev-impact` + `diff code`

**Kết luận (cập nhật 2026-09-14 sau khi fetch lại 19 TC): 16/21 vùng ảnh hưởng đủ TC · 2 GAP · 3 RISK · 1 orphan.** Đã đóng `G6`; `G1` và `G4` thu hẹp còn phần chưa phủ; `G8` hạ mức.

| Mã | Chiều | Vùng ảnh hưởng | TC hiện có | Status | Vấn đề |
|---|---|---|---|---|---|
| **G1** | diff code | **Nhánh thứ 3 của `deleteFriendInfo`** — 6 trường mặc định `d_1..d_6` (view_name · phone · email · birthday · age · province) đi nhánh `SET line_user.<col> = NULL`, **KHÔNG ghi lịch sử dòng nào** | **0** | **GAP** | Cùng nút 「一括削除」, cùng màn, cùng hàm vừa sửa. Dev tự kê đây là 1 trong 3 lỗi còn lại ("không ghi lịch sử cho sáu trường mặc định") nhưng **không TC nào chạm**. Tester chạy repro trên 電話番号/メールアドレス sẽ thấy **không có dòng lịch sử nào** — nặng hơn chính bug đang fix. Nguồn: `feature-spec.md` luồng EP-11 + BR-10.<br>✅ **Leader đã chốt (2026-09-14)**: trường mặc định **cũng phải** hiển thị lịch sử kèm tên người thao tác, **cùng chuẩn** với trường tuỳ chỉnh ⇒ hành vi hiện tại là **BUG**, không phải hành vi cố ý. ✅ **ĐÃ XỬ LÝ (cập nhật 2026-09-14)**: Studio đã thêm `#17662 DATA-HIST-001` phủ folder 「基本情報」 → TC này **fail**, sinh bug **`#1047` High** → Redmine **40906** → đã fix, **closed** (verified run `#1390`). Ô trống còn lại: **都道府県名 `d_6`** → `TC-DATAAUDIT001-02` (§5) |
| **G2** | diff code | Boundary **`Auth::id()` = null** (session hết hạn / mất phiên giữa thao tác) → lịch sử ghi `手動（）` trống tên | **0** | **GAP** | `dev_impact` của Studio nêu **đích danh** "Cần kiểm boundary: Auth::id() null". TC-DATADB001-01 chỉ *nhắc* trạng thái `手動（）` ở `note` như điểm quan sát, **không có TC nào ép trạng thái này xảy ra** |
| **G3** | diff code | Boundary **username có ký tự đặc biệt / emoji / độ dài max** khi render `手動（<tên>）` | **0** | **GAP** | `dev_impact` nêu đích danh. Tên người thao tác là snapshot ghi thẳng vào DB rồi in ra UI — chưa có TC kiểm escape/độ dài |
| **G4** | dev-impact | **F2 `recordFriendInfoHistory`** — hàm ghi lịch sử **dùng chung**; `dev_impact` yêu cầu "cần smoke các nguồn ghi lịch sử khác (HelperService, CalendarSalon, EventBooking...)" | 4 (`TC-REGSHARED001-01` + `#17703` Form · `#17704` Salon · `#17705` Lesson) | **RISK (thu hẹp)** | ✅ **Cập nhật 2026-09-14**: Studio đã thêm 3 TC cho Form / Salon Booking / Lesson Booking (pass@staging) — và `#17703` đã tìm ra bug **`#1050`** → Redmine **40917** → **closed**. Còn lại **event booking** + **import CSV** chưa nguồn nào chạm → `TC-REGSHARED001-02` (§5) |
| **G5** | dev-impact | **D1** — dòng lịch sử hiển thị ở **right bar chat 1:1** (bề mặt đọc thứ 3 của friend info, kho-tcs xác nhận tồn tại) | **0** | **RISK** | TC chỉ đối chiếu 2 bề mặt: bảng 「友だち情報追加履歴」 ở 友だち詳細 + modal 「操作履歴」. Kho `TC-FRI-169/170/172` cho thấy right bar là nơi thứ 3 phải khớp |
| **G6** | diff code | **7/15 TC `error` ở run `#1247`** — Chromium chết giữa chừng (`page.goto: Target page... has been closed`) | — | ✅ **ĐÃ ĐÓNG** | Triage của Studio: "không phải lỗi sản phẩm" (Chromium chết). ✅ **Cập nhật 2026-09-14**: đã chạy lại nhiều lượt trên **local + staging** (10 run), `last_exec` của **cả 19/19 TC đều `pass`**, `aiResult = pass`. Vấn đề hạ tầng đã hết, không còn TC nào ở trạng thái không kết luận |
| **G7** | orphan | **3 triệu chứng gốc của ticket** (① nút 抽選スタート không chạy action · ② template không hiển thị · ③ sau khi cộng điểm template trúng thưởng không hiện) | **0** | **orphan — KHÔNG phải GAP của task này** | Branch `ai_fixbug_40607` **không còn bản sửa nào** cho 3 triệu chứng (Dev tự ghi: "phiếu này hiện KHÔNG còn bản sửa nào cho triệu chứng gốc"). Cả 3 đã điều tra và kết luận **đúng thiết kế** (tag đạt giới hạn gán · action tự gán chính tag đó), **không có code nào bị sửa**. ⇒ Đây là **vấn đề phạm vi ticket**, không phải thiếu TC cho bản fix: hành vi liên quan thuộc **FA-012 tag management / action settings**, khác feature với `friend-info` đang review. **Không đề xuất TC ở §5** — xử lý ở **§6 mục 5** (chốt hướng đóng ticket) |
| **G8** | dev-impact | `friend_information_setting.total_user_has_value` — xóa N bạn chỉ giảm **1** (hiển thị 4 thay vì 2) | 1 (`#17215`, nay **pass@staging**) | **RISK (hạ mức)** | ✅ **Cập nhật 2026-09-14**: bug **`#1022`** đã được raise lên Redmine **40866** rồi **`rejected`** — vì `#17215` **pass trên staging** với đúng expected theo spec (5−3=2); kết quả sai `4` **chỉ xảy ra ở `local`** run `#1247` ⇒ không phải lỗi sản phẩm. Còn thiếu biên **"xóa TẤT CẢ bạn → 0人, không âm"** (kho có `TC-FRI-165`) và loại case Normal → `TC-DATACOUNT001-02/03` |

---

## 2. Quan điểm test còn thiếu

**Kết luận (cập nhật 2026-09-14): 16 quan điểm Trigger khớp task · 8 chưa cover đủ** (`Q1` và `Q5` đã thu hẹp đáng kể sau khi Studio bổ sung TC + chạy staging).

| Mã | Quan điểm | Ưu tiên | Tình trạng | Severity |
|---|---|---|---|---|
| **Q1** | `DATA-AUDIT-001` — Audit log / Lịch sử thao tác | **Cao** | **RISK (hạ từ BLOCKER).** ✅ **Cập nhật 2026-09-14**: Studio đã bổ sung `#17662` (folder 基本情報) + `#17703/17704/17705` (Form · Salon · Lesson) ⇒ nguồn **web** và **3 luồng tự động** nay đã có TC, và chính chúng tìm ra 2 bug thật (`#1047`→40906, `#1050`→40917, đều closed). **Còn thiếu**: nguồn **app mobile** (Dev xác nhận "bản di động không ghi lịch sử" — 0 TC) và **xoá** giá trị qua action (event booking · import CSV). ⚠️ Vẫn **không TC nào mang mã `DATA-AUDIT-001`** — 6 mã Studio gánh vai trò này (`TOOL-KNOW-002`, `TOOL-OLDREC-001`, `SELECT-SCOPE-001`, `TOOL-NEGCTRL-001`, `API-CONTRACT-001`, **`DATA-HIST-001`** mới thêm) đều **không có trong `framework/checklist-lme.md`** → coverage không map được (§4 mục 5) | **[MAJOR]** |
| **Q2** | `DATA-DB-001` ★ — Xác minh tầng DB (`WHERE` scope) | **Cao** | **RISK → BLOCKER.** ⚠️ **Không đổi sau 2026-09-14**: `#17207` vẫn ở `v1`, nay `pass@local` nhưng **vẫn không làm** chiều bắt buộc của quan điểm: *"tạo bản ghi trùng ở 2 tài khoản (2 bot, 2 staff) → xóa ở A → query xác nhận B không đổi"*. Đặc biệt nguy hiểm ở nhánh **setting_id âm (-7..-10)**: các ID này là **hằng số config dùng chung mọi bot** (`config/sns-line.php:875`), nếu `WHERE` thiếu `bot_id` thì xóa 郵便番号 ở bot A có thể xóa của bot khác. Đây đúng là nhánh dòng 457 vừa bị sửa | **[BLOCKER]** |
| **Q3** | `DATA-COUNT-001` — Số đếm & tổng hợp | **Cao** | **RISK.** Vẫn chỉ **1 TC** (`#17215`, Boundary) — nay `pass@staging`, bug `#1022` đã `rejected` (Redmine 40866). Thiếu **Normal + Abnormal** → vi phạm **RULE-01** (quan điểm Cao phải đủ 3 loại case), không có dòng ghi lý do. Thiếu biên "xóa TẤT CẢ → 0人 / không âm" | **[MAJOR]** |
| **Q4** | `BULK-001` — Phạm vi thao tác hàng loạt **sau lọc** | **Cao** | **RISK.** Yêu cầu của quan điểm là *"áp bộ lọc thu hẹp → thao tác hàng loạt → đếm lại số đối tượng thực sự bị tác động, khớp 100% số đã lọc"*. Mọi TC hiện có **tick checkbox thủ công, chưa TC nào áp filter/search** ở màn 情報一覧 trước khi 一括削除. Chỉ có loại case Normal (RULE-01) | **[MAJOR]** |
| **Q5** | `ENV-003` ★ + `JOB-001` ★ — Môi trường & job nền | **Cao** | **RISK (thu hẹp).** ✅ **Cập nhật 2026-09-14**: `envAuto` nay là `local` 5 run (17 automated) · **`staging` 5 run (16 automated, 84,2%)** · `dev` 0 · **`prd` vẫn 0**. Phần staging đã xong. Nhưng ENV-003 ghi rõ *"Không được đánh × với lý do **staging đã pass**"*, mà task chạm **job nhắc lịch** (`#17213`) và **bug gốc phát hiện trên production** (bot_id 155192) ⇒ vẫn cần vòng xác nhận trên **product**. Vi phạm **RULE-08** | **[MAJOR]** |
| **Q6** | `REG-SHARED-001` — Regression code dùng chung | **Cao** | **RISK (thu hẹp).** ✅ **Cập nhật 2026-09-14**: nay có **4 TC** `REG-SHARED-001` (`#17208` + `#17703` Form · `#17704` Salon · `#17705` Lesson). Lập luận cũ của TC ("1 TC đại diện là đủ") đã bị **bằng chứng bác bỏ**: mở rộng sang Form lập tức lộ bug `#1050` → Redmine 40917 ⇒ càng phải phủ nốt **event booking** + **import CSV**. Liên kết **G4** | **[MAJOR]** |
| **Q7** | `CONC-001` — Một hành động chỉ xử lý 1 lần | **Cao** | **GAP.** Nút 「一括削除」 là nút thực thi hành động quan trọng + nay **sinh thêm bản ghi audit**. Không TC nào double-click / mở 2 tab / 2 user thao tác song song. `TC-BULK001-01` có assert "không bạn bè nào có 2 dòng trùng" nhưng **trong 1 lần bấm**, không phải khi bấm trùng | **[MAJOR]** |
| **Q8** | `DATA-001` — Phản ánh đủ ở mọi màn liên quan | **Cao** | **RISK.** Quan điểm liệt kê nơi phải đối chiếu gồm **lịch sử thao tác** và **mobile app**. Hiện đối chiếu 2 bề mặt (bảng lịch sử + modal), thiếu **right bar chat 1:1** và **app**. Liên kết **G5** | **[MAJOR]** |
| **Q9** | `PERM-002` + `SEC-001` — Bypass quyền qua API / PII | **Cao** | **GAP trong bộ TC này.** `EP-13 POST /basic/deleteFriendInfo` xóa **PII của khách** (điện thoại, email, ngày sinh). `TC-APICONTRACT001-01` gọi endpoint nhưng **chỉ bằng phiên admin hợp lệ**, không thử role thiếu quyền / id của bot khác. ⚠️ Fix **không chạm authz** nên đây là lỗ hổng có sẵn, không phải regression → **dùng lại TC kho**, không đẻ TC mới (§5) | **[MAJOR]** |
| **Q10** | `FRIEND-001` — Đọc/ghi friend info | **Cao** | **RISK.** Chỉ phủ trường địa chỉ `-7 (郵便番号)`. `都道府県名` (`d_6`/`-6`) **lưu khác** 4 trường địa chỉ còn lại (kho `TC-FRI-308` xác nhận) và đi nhánh `line_user`, cùng 4 trường cơ bản `d_1..d_4` → chưa phủ. Liên kết **G1** | **[MAJOR]** |

---

## 3. TC trùng lặp

Đã rà **15/15 TC** theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`). Phát hiện **2 nhóm**, **không đề nghị xóa TC nào** (cả 2 đều là TC duy nhất mang mã quan điểm của mình → gate BƯỚC 4b chuyển sang "gộp").

| Nhóm | TC giữ lại | TC đề nghị gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| N1 | `TC-TOOLKNOW002-01` (Studio #17190) — TC tái hiện repro chính thức, gắn `TICKET-40607` + dòng 441 | `TC-DATADB001-01` (#17207) | **DUP-SUBSET** | Cùng `Normal` · cùng đối tượng + thao tác (trường `TC291_情報A`, **1 bạn bè**, 一括削除) · tiền đề tương đương · cùng cho ra dòng lịch sử `手動（qa_admin_291）`. Bằng chứng run `#1247` cho thấy #17190 **đã tự kiểm cả 6 cột DB** (`trigger_type_start` · `trigger_from_id` · `trigger_from_name` · `action` · `old_value` · `new_value`) — tức toàn bộ phần DB của #17207 | `[MINOR]` |
| | **Đề xuất**: KHÔNG xóa #17207. **Đổi kịch bản #17207 sang chiều còn thiếu của `DATA-DB-001`** — dựng cùng trường ở **2 bot**, xóa ở bot A, query xác nhận bot B không đổi (lấp **Q2**). Như vậy 2 TC hết trùng và quan điểm `DATA-DB-001` mới thật sự được cover | | | | |
| N2 | `TC-TOOLKNOW002-01` (#17190) | `TC-OUTTRUTH001-01` (#17206) | **DUP-SUBSET** | Toàn bộ nội dung của #17206 (mở modal 「操作履歴」 → đọc cột 「操作者」) **đã là assertion con** bên trong **6 TC khác** (#17190, #17205, #17209, #17210, #17211, #17216), tất cả đều pass ở run `#1247` với `✓ modal 詳細 「操作者」: "qa_admin_291"`. Bản thân #17206 **chưa pass lần nào** (error ở #1247, không có ở #1238) | `[MINOR]` |
| | **Đề xuất**: KHÔNG xóa — `REQ-004` vẫn cần 1 TC mang nhãn riêng. **Gộp phần trùng**: thu #17206 lại chỉ còn chiều **chưa ai kiểm** — modal liệt kê **cả dòng cũ lẫn dòng mới của cùng trường** (note của chính TC nêu "modal lấy dữ liệu theo trường nên sẽ liệt kê cả lịch sử cũ"), tức thứ tự + phân biệt 自動/tên trong 1 modal nhiều dòng | | | | |

**Không phát hiện `DUP-EXACT`, `DUP-INFLATE`, `DUP-CONFLICT`.** Đối chiếu kho `fa015` cũng không thấy `DUP-CONFLICT`: kho **không có TC nào** nhắc tới 「操作者」/「手動」 (grep = 0) → vùng lịch sử-người-thao-tác là coverage hoàn toàn mới, không mâu thuẫn expected với kho.

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | ~~[BLOCKER]~~ → ✅ **ĐÃ XỬ LÝ** | Toàn bộ run | Tại thời điểm review (2026-09-11): pass 7/15 = 46,7% (< 50%), `aiResult = fail`. ✅ **Cập nhật 2026-09-14**: đã chạy lại 10 run trên local + staging, **19/19 TC `pass`**, `aiResult = pass`, `openBugs = 0` | Không còn việc phải làm. `reviewed` vẫn `false` → chờ Leader duyệt (mục 9) |
| 2 | ~~[BLOCKER]~~ → ✅ **ĐÃ XỬ LÝ** | `#17215` · `#17662` · `#17703` | Tại thời điểm review: bug `#1022` `open` với `redmine_id = null`. ✅ **Cập nhật 2026-09-14**: cả **3 bug đều đã có số Redmine** — `#1022` → **40866** (`rejected`, vì `#17215` pass@staging đúng spec 5−3=2; sai lệch chỉ có ở `local`) · `#1047` **High** "xoá value folder 基本情報 không ghi dòng lịch sử nào" → **40906** (`closed`, verified run `#1390`) · `#1050` "Form trả lời Friend Info không tạo lịch sử đúng" → **40917** (`closed`, verified run `#1414`) | Không còn việc phải làm. ⚠️ Lưu ý cho Leader: `#1047` chính là **G1** — lỗ hổng nặng nhất của vòng review này đã được xác nhận là bug thật và đã fix |
| 3 | **[MAJOR]** | 14/15 TC | **RULE-02 — Evidence rỗng.** Chỉ TC `fail` (#17215) có `artifacts` (screenshot + trace). 7 TC `pass` có `actual` dạng text tự khai, `artifacts: []` → kết quả Đạt **không có bằng chứng đính kèm** | Bật lưu screenshot/trace cho cả case pass, ít nhất với TC gánh `REQ` risk High |
| 4 | **[MAJOR]** | Run `#1238` vs `#1247`, TC #17197 | **Tính toàn vẹn của kết quả pass.** Cùng 1 assertion "modal 詳細 – dòng CŨ 「操作者」" cho **2 kết quả trái ngược**: run `#1238` đọc ra `"qa_admin_291"` (≠ expected `自動`) nhưng **vẫn chấm ✓** kèm lời tự hạ chuẩn *"theo testcase KHÔNG dùng làm blocker"*; run `#1247` đọc ra `自動` và chấm ✓. Một runner tự quyết định bỏ qua mismatch làm **mọi dấu ✓ khác mất độ tin cậy** | Yêu cầu runner **không tự hạ chuẩn**: mismatch → `fail` hoặc `blocked`, không phải ✓ kèm chú thích. Rà lại các ✓ khác của run #1238 |
| 5 | **[MAJOR]** | 7/15 TC | **5 mã quan điểm không có trong `framework/checklist-lme.md`**: `TOOL-KNOW-002` · `TOOL-OLDREC-001` · `SELECT-SCOPE-001` · `TOOL-NEGCTRL-001` · `API-CONTRACT-001` (nhóm `TOOL-*` là mã nội bộ Studio) → 7 TC **không map được coverage** sang bộ 80 quan điểm LME | Map lại sang mã framework (`DATA-AUDIT-001`, `COMPAT-LEGACY-001`, `BULK-001`, `REG-SHARED-001`, `DATA-DB-001`) hoặc bổ sung mã mới vào framework theo **RULE-10** |
| 6 | **[MAJOR]** | Spec | **Không có chuẩn spec để đánh giá Expected của toàn bộ fix.** `spec-features/admin/friend-information/feature-spec.md` (750 dòng) **không có một dòng nào** về `friend_info_history` / cột 「トリガー」 / 「操作者」 / nhãn `手動（tên）` (grep = 0). Toàn bộ expected của 15 TC dựa vào đọc code, không dựa spec. Ngoài ra Studio tham chiếu `spec: admin/friend-info/...` — **khác đường dẫn và khác số dòng** với `spec-features/admin/friend-information/` của repo (BR-03 hai bên nghĩa khác nhau) → 2 bản spec không đồng bộ | Xem §6 mục 1. Trước mắt mọi TC ở vùng này phải để `Trạng thái đánh giá spec = Spec không ghi` + nêu đã hỏi ai |
| 7 | **[MAJOR]** | `03-dev-impact.md` | `Auto-filled: 2026-09-11 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" CHƯA tick** → F/D/T có thể thiếu hoặc map sai. Cụ thể mục **4.1 chỉ có 1 dòng** (file thay đổi), **4.3 chỉ có 1 dòng** (FA-015) trong khi `dev_impact` của Studio nêu thêm rủi ro ở các caller dùng chung | Tester đọc lại Redmine #40607 journal #135742 và tick checkbox trước khi review được coi là có hiệu lực |
| 8 | **[MAJOR]** | `[AP-2]` Symptom-only KH report | 3 triệu chứng KH (①②③) **không có error message / error code / log** trong file 01; Dev đi tới root cause bằng đọc log riêng, rồi **fix một thứ hoàn toàn khác**. TCs bám 100% theo repro của QA (journal #135750), **0 TC cho 3 triệu chứng gốc** | Xem **G7** + §6 mục 5. Chốt với Dev/Leader: đóng #40607 theo hướng nào |
| 9 | **[MINOR]** | 15/15 TC | `status = draft` — **chưa TC nào được duyệt**; `reviewed = false`, `reviewState = leader` (đang chờ chính vòng review này) | Duyệt/chuyển trạng thái sau khi xử lý §1–§5 |

### Chất lượng từng TC

| # | Severity | TC | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 10 | `[NIT]` | `TC-SELECTSCOPE001-02` | `Kết quả mong đợi` **không đơn trị**: thân TC ghi "nút ở trạng thái không dùng được" nhưng `note` lại bỏ ngỏ 2 khả năng (disabled **hoặc** bấm được nhưng thoát sớm) → tester khó chấm. Thực tế run `#1238` (#17200) **đã chứng minh** `class="btn btn-secondary disabled"` | Chốt expected theo bằng chứng run #1238, bỏ nhánh "nếu bấm được" khỏi note |
| 11 | `[NIT]` | `TC-TOOLOLDREC001-02` | `Điều kiện tiền đề` cho phép **chèn thẳng 1 bản ghi lịch sử vào DB** để dựng data cũ — chấp nhận được với case legacy, nhưng data dựng tay không phản ánh đúng hình dạng row do code cũ sinh ra | Ghi rõ ở `Ghi chú` là data dựng tay; ưu tiên dùng row cũ có thật trên staging nếu có |
| 13 | `[MINOR]` | `#17209` (`OUT-TRUTH-001`, `v2`) | TC đã được mở rộng để cover username chứa **tiếng Nhật / emoji / ký tự đặc biệt** 「QA_日本😀（Staff）」 — tốt, và đã thay thế đề xuất `TC-DATAAUDIT001-04` của report. Còn thiếu **2 chiều nhỏ**: (a) ký tự có ý nghĩa HTML `<>&"'` (kiểm escape, tránh vỡ bảng / inject) · (b) tên dài **tối đa** hệ thống cho phép (kiểm tràn cột / cắt ngắn) | **Bổ sung 2 biến thể dữ liệu vào `#17209`** (không cần đẻ TC mới) — thêm 1 tài khoản tên `qa_<>&"'_291` và 1 tài khoản tên dài tối đa vào cột `Dữ liệu test` |
| 12 | `[NIT]` | `[AP-5]` `TC-DATACOUNT001-01` | TC test đoạn code **không nằm trong diff** (`434-437`, bug có sẵn) → theo AP-5 là ORPHAN so với fix | **KHÔNG xóa** — TC này tìm ra bug thật (#1022). Chỉ **đổi nhãn thành `regression`** ở `Ghi chú` để coverage matrix không tính nhầm là cover cho fix #40607 |

---

## 5. TCs đề xuất bổ sung (9)

### Đối chiếu trước khi viết

| Kiểm | Kết quả |
|---|---|
| Kho TCs của tính năng | `kho-tcs/fa015-quanlythongtinbanbe-友だち情報管理.md` — **có**, 27 nhóm chức năng. Đã đọc nhóm 17 (Xóa info) · 18 (Màn danh sách câu trả lời) · 19 (Bộ đếm 回答人数, 30 TC) · 26 (Thông tin mặc định & địa chỉ, 20 TC) |
| Conflict expected với kho | **Không có.** Kho **0 TC** nhắc 「操作者」/「手動」 → vùng lịch sử-người-thao-tác là coverage mới |
| TC kho dùng lại được (KHÔNG viết mới) | **Q9** → `TC-FRI-168` (mở 情報一覧 bằng id trường của bot khác) · `TC-FRI-157` (API xóa trường id bot khác) · `TC-FRI-38` (API xóa folder id bot khác) — chỉnh lại cho endpoint `EP-13 deleteFriendInfo`. **G8/Q3 biên** → `TC-FRI-165` (一括削除 TẤT CẢ → 0人, không âm) và `TC-FRI-187` (count=0 còn giá trị sót → không âm), chỉ cần đổi lối vào |
| Chống trùng | Đã đối chiếu **15 TC ở BƯỚC 0** + kho `fa015`. G5 đã gộp vào Q8; G6 không sinh TC (là việc chạy lại); G8/Q3 tái dùng kho + thêm 1 đối chứng |

#### ⚠️ Rà trùng lần 2 — fetch lại Studio ngày **2026-09-14** (bộ TC đã tăng 15 → **19**)

Bộ TC trên Studio đã được bổ sung 4 TC mới (`#17662`, `#17703`, `#17704`, `#17705`) và `#17209` được sửa lên `v2`. Đối chiếu lại 14 TC đề xuất → **bỏ 3, gộp 1, thu hẹp 2, giữ 9** ⇒ còn **11 TC**.

| TC đề xuất cũ | Đối chiếu với Studio hiện tại | Xử lý |
|---|---|---|
| ~~`TC-DATAAUDIT001-01`~~ (xoá 電話番号 folder 基本情報) | **`#17662` `DATA-HIST-001` Normal** — trùng **cả 4 yếu tố**: cùng folder 「基本情報」, cùng ưu tiên field 「電話番号」, cùng tick A/giữ B đối chứng, cùng expected 「手動（tên）」 + modal 「操作者」 + đối chiếu DB. Note của `#17662` ghi đúng quyết định của Leader ("xoá value ở MỌI folder đều phải tạo lịch sử"). TC này **đã chạy pass@staging** và chính nó đã phát hiện bug **`#1047` High** → Redmine **40906** → **closed** | **BỎ — DUP-EXACT** (đã có TC, đã chạy, đã ra bug, đã fix) |
| ~~`TC-DATAAUDIT001-04`~~ (username emoji / ký tự đặc biệt) | **`#17209` `OUT-TRUTH-001` Boundary `v2`** — đã được sửa để gộp đúng chiều này: tài khoản Staff tên 「QA_日本😀（Staff）」, expected "hiển thị **nguyên văn**, không mất ký tự, lỗi encoding, escape sai, cắt ngắn". Note ghi rõ "cover boundary Dev nêu về username chứa ký tự đặc biệt/emoji" | **BỎ — DUP**. Chiều còn thiếu (`<>&"'` kiểu HTML + độ dài **tối đa**) chuyển thành **issue §4 mục 13**, đề nghị bổ sung vào `#17209` thay vì đẻ TC mới |
| ~~`TC-DATAAUDIT001-06`~~ (action job ghi nhãn nguồn tự động) | **`#17703` Form · `#17704` Salon Booking · `#17705` Lesson Booking** (`REG-SHARED-001`, pass@staging) đã phủ ý định "nguồn tự động phải giữ nhãn riêng, không thành 手動" | **GỘP** vào `TC-REGSHARED001-02` (xem dưới) — 2 đề xuất cũ dẫn tới cùng 1 TC |
| `TC-REGSHARED001-02` (smoke salon · event booking · CSV) | Trùng **một phần**: Salon Booking đã có `#17704`, Form đã có `#17703`. **Event Booking** và **import CSV** vẫn **chưa nguồn nào chạm** | **THU HẸP** còn 2 nguồn chưa phủ + gộp thêm chiều *xoá* giá trị của `TC-DATAAUDIT001-06` |
| `TC-ENV003-01` (chạy staging + production) | Trùng **một phần**: `envAuto` nay cho thấy **staging 5 run, 16/19 TC automated (84,2%)**. **`prd` vẫn 0 run** | **THU HẸP** còn **production** |
| `TC-DATAAUDIT001-02` (都道府県名) | `#17662` phủ folder 「基本情報」, `#17192` phủ 郵便番号 `-7`. **都道府県名 `d_6`** — nằm folder địa chỉ nhưng lưu ở `line_user` — vẫn **không TC nào** | **GIỮ** (đã chỉnh lại lý do cho đúng ô trống thật) |
| `TC-DATAAUDIT001-03` · `TC-DATADB001-02` · `TC-DATACOUNT001-02` · `TC-DATACOUNT001-03` · `TC-BULK001-02` · `TC-CONC001-01` · `TC-DATA001-01` | Không TC nào trong 19 TC chạm tới: `Auth::id()` null · 2 bot với `setting_id` âm dùng chung · xoá TẤT CẢ → 0人 · xoá đúng 1 → giảm 1 · áp filter rồi 一括削除 · double-click / 2 tab · right bar chat 1:1 | **GIỮ nguyên 7 TC** |

### Bảng TC (14 cột)

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-DATAAUDIT001-02 | UI | DATA-AUDIT-001 | 友だち情報管理（情報一覧）→ 友だち詳細 | Normal | manual | Tất cả | Xoá giá trị 都道府県名 — trường DUY NHẤT trong folder địa chỉ đi nhánh `line_user`, chưa nguồn nào phủ | Đã đăng nhập màn quản trị bằng tài khoản test biết trước tên hiển thị (ví dụ 「qa_admin_291」) và đã chọn đúng bot test. Ở folder 「住所情報」 có trường 都道府県名 với ít nhất 2 bạn bè đã chọn tỉnh/thành, và trường 郵便番号 cũng có ít nhất 2 bạn bè có giá trị để làm đối chứng. Ghi lại tên các bạn bè trước khi thao tác | 1. Mở 「友だち情報管理（一覧）」 → folder 「住所情報」<br>2. Bấm 「回答人数」 của 都道府県名<br>3. Tick 2 bạn → 「一括削除」 → OK<br>4. Mở 「友だち詳細」 từng bạn xem bảng lịch sử<br>5. Làm lại y hệt với 郵便番号 (đối chứng) và so sánh 2 kết quả | 都道府県名 (`d_6`/`-6`) 2 bạn · đối chứng 郵便番号 (`-7`) 2 bạn | **Cả hai** trường cho kết quả **giống nhau**: mỗi bạn bè có đúng 1 dòng 「削除」 với 「トリガー」 = 「手動（qa_admin_291）」 và modal 「操作者」 = 「qa_admin_291」. Cụ thể 郵便番号 (`-7`) đã pass ở run #1238; **都道府県名 (`d_6`) phải cho kết quả y hệt** dù lưu ở bảng khác. Hai trường cùng nằm trong folder 「住所情報」 mà hành vi audit khác nhau là **KHÔNG ĐẠT**. ⚠️ Dự kiến 都道府県名 hiện KHÔNG ghi dòng nào ⇒ bug, gộp cùng ticket với bug `#1047` (Redmine **40906**) vì cùng bản chất | | Lấp **G1** (phần còn lại) + **Q10** · ⚠️ Sau rà trùng 2026-09-14: folder 「基本情報」 **đã được** `#17662` phủ (và đã tìm ra bug `#1047` → Redmine **40906**, closed); 郵便番号 `-7` đã được `#17192` phủ. **都道府県名 (`d_6`) là ô trống còn lại**: nằm trong folder địa chỉ như `-7..-10` nhưng **lưu ở `line_user` như `d_1..d_4`** (kho `TC-FRI-308` xác nhận lưu khác 4 trường địa chỉ còn lại) · Đánh giá spec: **Đã hỏi leader** (§6 mục 2) · Evidence: 2 screenshot đặt cạnh nhau + query |
| TC-DATAAUDIT001-05 | UI | DATA-AUDIT-001 | App mobile — my page bạn bè | Normal | manual | product | Xóa giá trị friend info từ **app mobile** → lịch sử ghi đủ người thao tác (nguồn thứ 2 của DATA-AUDIT-001) | App mobile đăng nhập cùng bot test. Bạn bè U1 đang có giá trị ở trường `TC291_情報A` | 1. Trên app mobile mở my page của U1<br>2. Xóa trắng giá trị trường `TC291_情報A` → lưu<br>3. Trên web mở 「友だち詳細」 của U1 → xem bảng 「友だち情報追加履歴」<br>4. Mở modal 「詳細」 của dòng mới nhất<br>5. Đối chiếu với cùng thao tác làm từ web | Xóa từ app mobile · 1 bạn bè | Bảng 「友だち情報追加履歴」 trên web có **đúng 1 dòng MỚI** cho lần xoá từ app: 「操作」 = 「削除」 và người thao tác **xác định được** — hiển thị tên tài khoản admin đã thao tác trên app, KHÔNG để trống, KHÔNG là 「自動」, KHÔNG là 「手動（）」 rỗng. Kết quả phải **tương đương** khi làm cùng thao tác từ web. ⚠️ **Dự kiến KHÔNG ĐẠT**: Dev tự kê "bản di động không ghi lịch sử" ⇒ cùng 1 hành vi nghiệp vụ cho ra audit trail khác nhau tuỳ nguồn, vi phạm `DATA-AUDIT-001` ("cùng 1 thao tác từ TỪNG nguồn → mỗi lần đúng 1 bản ghi") ⇒ raise ticket riêng | | Lấp **Q1** · Đánh giá spec: **Đã hỏi leader** — cùng chuẩn với `#17662` / bug `#1047` (§6 mục 2) · **RULE-08**: app mobile chỉ kết luận được trên **product** · Kho `TC-FRI-181` cũng ghi môi trường PRODUCTION cho path mobile · ⚠️ DỰ KIẾN KHÔNG ĐẠT |
| TC-DATADB001-02 | UI | DATA-DB-001 | 友だち情報管理（情報一覧）— 2 bot | Abnormal | manual | Tất cả | Xóa 郵便番号 ở bot A không được đụng dữ liệu 郵便番号 của bot B (setting_id âm dùng chung mọi bot) | Có 2 bot test A và B cùng truy cập được bằng `qa_admin_291`. Mỗi bot có ≥ 2 bạn bè đã điền 郵便番号; ghi lại giá trị và 回答人数 của **cả 2 bot** | 1. Ghi 回答人数 của 郵便番号 ở bot A và bot B<br>2. Chọn bot A → mở 「回答人数」 của 郵便番号 → tick tất cả → 「一括削除」 → OK<br>3. Đọc lại 回答人数 của bot A<br>4. **Chuyển sang bot B** → mở 「回答人数」 của 郵便番号<br>5. Mở 「友だち詳細」 1 bạn bè của bot B kiểm tra giá trị và bảng lịch sử<br>6. Lặp lại với 1 trường tùy chỉnh cùng tên tạo ở cả 2 bot | Trường `郵便番号` (`setting_id = -7`, hằng số dùng chung) ở 2 bot · trường tùy chỉnh trùng tên ở 2 bot | Bot A: giá trị bị xóa, có dòng lịch sử 「手動（qa_admin_291）」. **Bot B: giá trị 郵便番号 còn NGUYÊN VẸN**, 回答人数 không đổi, **không phát sinh dòng lịch sử 「削除」 nào**. Trường tùy chỉnh trùng tên ở bot B cũng không bị đụng | | Lấp **Q2** · Thay cho kịch bản đang trùng của `TC-DATADB001-01` (§3 nhóm N1) · Rủi ro cụ thể: `-7..-10` là hằng số `config/sns-line.php:875` dùng chung mọi bot, `WHERE` thiếu `bot_id` là xóa nhầm · Evidence: kết quả query `friend_information_value` lọc theo `bot_id` trước/sau, chụp cả 2 bot |
| TC-DATACOUNT001-02 | UI | DATA-COUNT-001 | 友だち情報管理（一覧）+（情報一覧） | Boundary | manual | Tất cả | 一括削除 TẤT CẢ bạn bè → 回答人数 về 0人, không âm, màn rỗng không lỗi | Trường `TC291_情報D` có đúng 4 bạn bè có giá trị, 回答人数 hiển thị 4 | 1. Ghi 回答人数 = 4人<br>2. Mở 情報一覧 → tick **toàn bộ** 4 bạn → 「一括削除」 → OK<br>3. Quay lại màn list, tải lại trang, đọc 回答人数<br>4. Mở lại màn 情報一覧<br>5. Thử bấm 「一括削除」 lần nữa khi bảng đã rỗng | Xóa 4/4 bạn trong 1 lần | 回答人数 hiển thị **「0人」**, tuyệt đối **không hiển thị số âm**. Màn 情報一覧 rỗng, không văng lỗi. Bấm xóa khi rỗng không gây lỗi. Nếu kết quả ra **3人** (dấu hiệu số đếm chỉ giảm 1 cho cả lô) thì đối chiếu ngay với Studio `#17215` và Redmine **40866** trước khi mở phiếu mới — bug đó đã từng bị `rejected` vì staging cho kết quả đúng, nên phải ghi rõ môi trường đã chạy | | Lấp **Q3** + **G8** · **Dùng lại kho `TC-FRI-165`**, chỉ đổi dữ liệu cho khớp task · Evidence: screenshot 回答人数 + `SELECT total_user_has_value` |
| TC-DATACOUNT001-03 | UI | DATA-COUNT-001 | 友だち情報管理（一覧）+（情報一覧） | Normal | manual | Tất cả | Xóa đúng **1** bạn bằng 一括削除 → 回答人数 giảm đúng 1 | Trường `TC291_情報D` có đúng 5 bạn bè có giá trị, 回答人数 = 5 | 1. Ghi 回答人数 = 5人<br>2. Tick **đúng 1 bạn** → 「一括削除」 → OK<br>3. Tải lại màn list, đọc 回答人数<br>4. Lặp lại: tick đúng 1 bạn nữa → xóa → đọc 回答人数<br>5. So sánh với kết quả xóa 3 bạn cùng lúc ở TC 「Số 回答人数 trên màn danh sách cập nhật đúng sau khi xoá nhiều bạn bè」 (Studio #17215) | Xóa 1 bạn/lần, làm 2 lần liên tiếp | Sau lần 1: 回答人数 = 4人. Sau lần 2: = 3人. Mỗi lần giảm **đúng 1** và luôn khớp số dòng thực tế ở màn 情報一覧. Đặt cạnh TC xoá 3 bạn cùng lúc (Studio #17215): nếu 1 bạn giảm đúng 1 mà N bạn cũng chỉ giảm 1 thì lỗi nằm ở vòng lặp nhiều bản ghi, không phải ở phép trừ — đây là phép kiểm chứng để phân loại đúng, không phải để kết luận ngay | | Lấp **Q3** — loại case **Normal** còn thiếu của `DATA-COUNT-001` theo **RULE-01** (hiện chỉ có 1 Boundary `#17215`) · ⚠️ Cập nhật 2026-09-14: bug `#1022` đã **rejected** (Redmine 40866) vì `#17215` **pass@staging** với đúng expected 5−3=2; kết quả `4` chỉ xảy ra ở `local` run#1247 ⇒ TC này **không còn** vai trò khoanh vùng bug, nhưng vẫn cần để đủ 3 loại case · Evidence: 3 screenshot 回答人数 theo từng bước |
| TC-BULK001-02 | UI | BULK-001 | 友だち情報管理（情報一覧） | Normal | manual | Tất cả | Áp bộ lọc/tìm kiếm rồi 一括削除 → số bạn bè thực sự bị tác động khớp 100% số đã lọc | Trường `TC291_情報A` có 12 bạn bè có giá trị, trong đó 4 bạn có tên chứa chuỗi `QATEST`. Ghi lại danh sách 12 bạn | 1. Mở 情報一覧 của `TC291_情報A`, ghi tổng số dòng = 12<br>2. Áp bộ lọc/tìm kiếm theo `QATEST` → đếm số dòng hiển thị (kỳ vọng 4)<br>3. Tick **chọn tất cả** trong trạng thái đang lọc<br>4. Bấm 「一括削除」 → OK<br>5. Bỏ bộ lọc, đếm lại tổng số dòng<br>6. Mở 「友だち詳細」 của 1 bạn **không** thuộc kết quả lọc | 12 bạn bè, lọc còn 4 (`QATEST`) | Đúng **4** bạn bè bị xóa giá trị — khớp **100%** số đã lọc. Sau khi bỏ lọc còn đúng **8** dòng. 8 bạn ngoài bộ lọc **giữ nguyên giá trị** và **không có** dòng lịch sử 「削除」 mới. Tuyệt đối không rơi về xóa toàn bộ 12 | | Lấp **Q4** — chiều "bộ lọc + thao tác hàng loạt" mà `BULK-001` bắt buộc, hiện 0 TC có · Evidence: số bản ghi thật bị tác động đếm từ DB khớp số đã lọc |
| TC-REGSHARED001-02 | UI | REG-SHARED-001 | Các nguồn ghi lịch sử dùng chung | Normal | manual | product | Xoá giá trị friend info qua **action của các nguồn CHƯA được phủ** (Event Booking · import CSV) → lịch sử giữ đúng nhãn nguồn tự động, không thành 手動 | Bạn bè U1 có sẵn giá trị ở 2 friend info tuỳ chỉnh `TC291_EVENT_INFO` và `TC291_CSV_INFO` (folder thường). Có Event Booking cấu hình action **xoá** giá trị `TC291_EVENT_INFO`. Có file CSV import ghi giá trị **rỗng** cho `TC291_CSV_INFO`. Ghi lại nhãn 「トリガー」 của các dòng lịch sử cũ của U1 | 1. Cho U1 đi qua luồng Event Booking khiến action xoá giá trị chạy<br>2. Mở 「友だち詳細」 của U1 → bảng 「友だち情報追加履歴」, đọc nhãn 「トリガー」 và modal 「操作者」 của dòng mới<br>3. Import CSV với giá trị rỗng cho `TC291_CSV_INFO` của U1<br>4. Đọc lại bảng lịch sử, kiểm dòng mới của lần import<br>5. Đối chiếu các dòng lịch sử cũ xem có bị đổi nhãn không | 2 nguồn: Event Booking (action xoá giá trị) · import CSV (giá trị rỗng) · 1 bạn bè U1 | Mỗi nguồn sinh **đúng 1** dòng lịch sử mang **nhãn 「トリガー」 riêng của nguồn đó** (nguồn tự động), **KHÔNG** phải 「手動」 và **KHÔNG** phải 「手動（）」 rỗng; modal 「操作者」 hiển thị 「自動」, không gán nhầm tên admin đang đăng nhập. Các dòng lịch sử cũ giữ nguyên nhãn. Không thiếu dòng, không sinh trùng dòng, không ghi nhầm sang field/friend khác | | Lấp **G4** + **Q6** (phần còn lại) · **Gộp** từ 2 đề xuất cũ (`TC-DATAAUDIT001-06` + bản `TC-REGSHARED001-02` đầu) sau khi rà trùng 2026-09-14 · ⚠️ Form / Salon Booking / Lesson Booking **đã được Studio phủ** bằng `#17703` · `#17704` · `#17705` (pass@staging) ⇒ TC này **chỉ còn** 2 nguồn chưa ai chạm · **RULE-08**: job nền ⇒ chạy trên product · Kho `TC-FRI-178` xác nhận action salon / import CSV / form booking đều xoá được giá trị |
| TC-CONC001-01 | UI | CONC-001 | 友だち情報管理（情報一覧） | Abnormal | manual | Tất cả | Bấm 一括削除 trùng (double-click / 2 tab) → chỉ sinh đúng 1 dòng lịch sử, không ghi trùng | Trường `TC291_情報A` có 3 bạn bè có giá trị. Mở **2 tab** cùng màn 情報一覧 của trường đó | 1. Tab 1: tick 1 bạn → **double-click nhanh** nút 「一括削除」 → OK<br>2. Mở 「友だち詳細」 của bạn đó, đếm số dòng 「削除」 mới<br>3. Tab 1 và tab 2: cùng tick **cùng 1 bạn bè thứ 2**<br>4. Bấm 「一括削除」 ở tab 1 rồi bấm ngay ở tab 2 → OK cả hai<br>5. Mở 「友だち詳細」 bạn thứ 2, đếm số dòng 「削除」 mới và đọc 回答人数 | Double-click · 2 tab thao tác song song trên cùng 1 bạn bè | Mỗi trường hợp chỉ sinh **đúng 1** dòng lịch sử 「削除」, không có 2 dòng trùng cùng thời điểm. Lần bấm thứ 2 phải bị **chặn hoặc báo không còn dữ liệu**, không âm thầm ghi thêm dòng. 回答人数 **không bị trừ 2 lần** cho cùng 1 bản ghi | | Lấp **Q7** · Nút 一括削除 nay sinh thêm bản ghi audit → ghi trùng làm hỏng sổ audit · Evidence: số dòng lịch sử thật đếm từ DB sau khi cố tình bấm trùng |
| TC-DATA001-01 | UI | DATA-001 | 友だち詳細 + right bar chat 1:1 | Normal | manual | Tất cả | Sau khi xóa giá trị, 3 bề mặt hiển thị friend info đều khớp nhau | Bạn bè U1 có giá trị ở trường `TC291_情報A`. Có quyền vào màn chat 1:1 | 1. Xóa giá trị `TC291_情報A` của U1 qua 「一括削除」 bằng `qa_admin_291`<br>2. Mở 「友だち詳細」 của U1 → đọc bảng 「友だち情報追加履歴」 và khu vực giá trị<br>3. Mở màn chat 1:1 của U1 → mở right bar tab 友だち情報 → đọc giá trị và lịch sử nếu có<br>4. Mở lại màn 情報一覧 của trường đó<br>5. Đối chiếu 3 nơi | 1 bạn bè, 1 trường tùy chỉnh | Cả 3 nơi khớp nhau: giá trị đã bị xóa ở **cả** 友だち詳細 **và** right bar chat 1:1; U1 không còn ở màn 情報一覧. Dòng lịch sử 「手動（qa_admin_291）」 hiển thị nhất quán ở mọi bề mặt có hiện lịch sử; không bề mặt nào còn cache giá trị cũ | | Lấp **Q8** + **G5** · Kho `TC-FRI-169/170/172` xác nhận right bar chat 1:1 là bề mặt thứ 3 bắt buộc đối chiếu · Evidence: bảng đối chiếu 3 nơi kèm screenshot |

### `G<x>` / `Q<x>` không sinh TC mới

| Mã | Lý do |
|---|---|
| **G6** | Không phải thiếu TC mà là **thiếu kết quả** — 7 TC `error` do Chromium chết. Việc cần làm là **chạy lại run**, không viết TC mới |
| **G7** | **KHÔNG viết TC.** 3 triệu chứng gốc đã kết luận **đúng thiết kế**, **không code nào bị sửa** → viết TC ở đây là test vùng ngoài diff (đúng thứ `[AP-5]` đang bị flag ở §4 mục 12). Hành vi tag (giới hạn số người gán · action tự tham chiếu) thuộc **FA-012 tag management**, khác feature — nếu muốn khóa lại thì bổ sung vào `kho-tcs/fa012-quanlythe-タグ管理.md` bằng `/collect-tcs`, **không nhét vào report của task friend-info**. Theo **RULE-10**, cái cần sinh ra là **1 dòng quan điểm/catalog**, không phải TC trong task này. Quyết định phạm vi ticket ở **§6 mục 5** |
| **G8** | Đã lấp bằng `TC-DATACOUNT001-02` + `TC-DATACOUNT001-03`; phần raise ticket cho bug #1022 nằm ở §4 mục 2 |
| **G5** | Đã gộp vào `TC-DATA001-01` (cùng kịch bản, tránh đẻ TC trùng) |
| **G2** | **Leader loại trừ (2026-09-14)** — `TC-DATAAUDIT001-03` (boundary `Auth::id()` null) **không sync**. `dev_impact` có nêu boundary này nhưng Leader quyết định không đưa vào đợt test; nếu muốn phủ lại thì mở TC riêng sau |
| **Q5** | **Leader loại trừ (2026-09-14)** — `TC-ENV003-01` (chạy bộ TC lõi trên production) **không sync**. ⚠️ Hệ quả: **RULE-08 / ENV-003 vẫn chưa được thỏa** — `prd` còn **0 run** trong khi task chạm job nhắc lịch và bug gốc phát hiện trên production; Leader cần tự quyết vòng xác nhận product ngoài Studio |
| **Q9** | **Dùng lại TC kho**, không viết mới: `TC-FRI-168` · `TC-FRI-157` · `TC-FRI-38` — chỉ cần đổi endpoint đích sang `EP-13 POST /basic/deleteFriendInfo` và thêm 1 lượt gọi bằng role staff không có quyền màn 友だち情報管理 |
| **Q10** | Đã lấp bằng `TC-DATAAUDIT001-02` (都道府県名) + **`#17662`** của Studio (trường cơ bản folder 基本情報) |

---

## 6. Spec update needed

| # | Nội dung | Nguồn |
|---|---|---|
| 1 | **Bổ sung mục "Lịch sử thay đổi thông tin bạn bè" vào `spec-features/admin/friend-information/feature-spec.md`.** Spec hiện **không có dòng nào** về `friend_info_history`, cột 「トリガー」, cột 「操作者」 hay nhãn `手動（tên）` → toàn bộ 15 TC đang lấy expected từ đọc code. Phải ghi rõ **2 quy tắc hiển thị KHÁC nhau**: cột 「トリガー」 ở bảng 「友だち情報追加履歴」 in kèm tên khi có `trigger_from_name`; modal 「操作履歴」 cột 「操作者」 chỉ in tên khi có **đủ** tên **và** nguồn trigger thủ công, ngược lại rơi về 「自動」 | §4 mục 6 |
| 2 | ✅ **ĐÃ CHỐT (Leader, 2026-09-14)** — **cả 3 nhánh của `deleteFriendInfo` đều phải ghi lịch sử kèm tên người thao tác.** Cùng nút 「一括削除」 trên cùng màn nhưng hiện 3 nhánh hành xử khác nhau: (a) trường tuỳ chỉnh `id > 0` → ghi lịch sử ✓ · (b) địa chỉ `-7..-10` → ghi lịch sử ✓ · (c) **mặc định `d_1..d_6`** (tên · điện thoại · email · ngày sinh · tuổi · 都道府県名) → `SET line_user.<col> = NULL`, **không ghi dòng nào**. **Quyết định: nhánh (c) phải ghi lịch sử với 「トリガー」 = 「手動（tên）」, cùng chuẩn với (a) và (b)** ⇒ hành vi hiện tại là **bug cần fix**, không phải hành vi cố ý.<br>✅ **ĐÃ THỰC HIỆN MỘT PHẦN (2026-09-14)**: Studio thêm `#17662` → bug `#1047` **High** → Redmine **40906** → đã fix, `closed`. **Việc còn lại**: ① bổ sung mục "Lịch sử thay đổi thông tin bạn bè" vào `feature-spec.md` ghi rõ **cả 3 nhánh** đều ghi lịch sử (gộp với mục 1) · ② phủ nốt **都道府県名 `d_6`** — trường duy nhất trong folder địa chỉ đi nhánh `line_user`, chưa nằm trong phạm vi 40906 · ③ áp cùng chuẩn cho **app mobile** (mục 2b) | **G1** · Studio `#17662` → bug `#1047` → Redmine **40906** (closed) · `TC-DATAAUDIT001-02` · `feature-spec.md` luồng EP-11 + BR-10 · Dev tự kê ở mục "3 lỗi còn lại" |
| 2b | **Hệ quả của quyết định trên — app mobile.** `DATA-AUDIT-001` yêu cầu *"cùng 1 thao tác từ TỪNG nguồn → mỗi lần đúng 1 bản ghi lịch sử"*. Đã chốt trường mặc định phải ghi lịch sử thì **path app mobile cũng phải theo cùng chuẩn** — Dev tự kê "bản di động không ghi lịch sử" ⇒ cũng là bug. Gộp vào cùng ticket với nhánh (c) hoặc tách riêng tuỳ khối lượng fix | **Q1** · `TC-DATAAUDIT001-05` · Kho `TC-FRI-181` (path mobile, môi trường PRODUCTION) |
| 3 | **Chốt `total_user_has_value`: spec đúng hay code đúng.** Spec nói mỗi bản ghi bị xóa giảm số đếm đi 1 (chặn dưới 0); code giảm đúng 1 lần cho cả lô. Bug Studio **#1022** đang `open` với `redmine_id = null`. ⚠️ Kho `TC-FRI-164` **đã có sẵn** TC này với đúng expected "回答人数 = 2人" → đây là vi phạm spec **đã có TC từ trước, chưa từng được raise** | **G8** · §4 mục 2 |
| 4 | **Gộp quyết định với `MT-03` đang treo ở kho-tcs** (mức CAO, trạng thái ⏳ CHỜ QUYẾT ĐỊNH): *"cùng 1 hành vi nghiệp vụ đi qua ≥ 7 lối ghi khác nhau, fix chỉ đụng 1 lối → cần chốt scope để TC ở các lối còn lại không pass oan/fail oan"*. Ticket #40607 **lặp lại đúng pattern đó** (fix 2 lời gọi trong 1 lối, còn app mobile / action job / import CSV chưa đụng) → nên chốt **một lần** danh sách lối ghi phải tuân quy tắc audit, thay vì quyết định lẻ theo từng ticket | Kho `fa015` mục "Điểm mâu thuẫn với spec" MT-03 |
| 5 | **Chốt hướng đóng ticket #40607.** Tiêu đề và nội dung khách là 3 triệu chứng bốc thăm; branch hiện chỉ còn bản sửa lịch sử thao tác — **không còn bản sửa nào cho triệu chứng gốc**. Thêm nữa, phần sửa **điểm nhận webhook LINE** (bỏ sót sự kiện thứ 3 trở đi, lưu ngược thứ tự) đã bị **gỡ khỏi branch** và theo Dev **đã được báo lại 6 lần ở các ticket trước mà chưa lần nào được push**. Cần quyết định: đóng #40607 theo scope lịch sử thao tác và **tách ticket mới** cho 3 triệu chứng + webhook, hay giữ #40607 mở | **G7** · §4 mục 8 · Redmine journal #135742 |
| 6 | **Bổ sung mã quan điểm còn thiếu vào framework** theo **RULE-10**: 5 mã Studio (`TOOL-KNOW-002`, `TOOL-OLDREC-001`, `SELECT-SCOPE-001`, `TOOL-NEGCTRL-001`, `API-CONTRACT-001`) đang gánh coverage thật nhưng không có trong `framework/checklist-lme.md` → hoặc map về mã có sẵn, hoặc thêm mới kèm ID + ngày + nguồn bug | §4 mục 5 |
