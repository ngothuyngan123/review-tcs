# 05 — Review Report

## 0. Nguồn TC

- **Nguồn đã dùng**: MCP LME TEST STUDIO — task #308 (ticket 40051, round 1, branch `ai_fixbug_40051`, status `tc-ready`, `aiResult=fail`), snapshot ghi vào `04-tc-list.md`.
- **Tổng số TC review**: 94 TC (71 pass · 9 fail · 10 skip · 4 chưa chạy).

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: 6/17 vùng ảnh hưởng đủ TC · **6 GAP** · **5 RISK**.

> **Đối chiếu tồn kho endpoint** (`spec-features/admin/chat-11/feature-spec.md` §6 — 49 endpoint): **33/49 có TC `pass`** · **4/49 chỉ có TC `skip`/chưa chạy** (EP-10, EP-11, EP-18, EP-49) · **12/49 không có TC nào nhắm tới** (EP-16, EP-29, EP-30, EP-33, EP-38, EP-42→EP-48). Chi tiết ở **G10** + **G11**.

| # | Chiều | Vùng ảnh hưởng | TC hiện có | Status | Vấn đề | Severity |
|---|---|---|---|---|---|---|
| **G1** | dev-impact | `F2` · `T1` — 4 endpoint GỬI TIN dùng chung `validateSendRequest` (`sendMessage`/`sendMedia`/`sendTemplate`/`sendSticker`) | NEW-56 (text 1-1) `skip` · NEW-45 (text nhóm) `skip` · NEW-47 (template + sticker nhóm) `skip` · NEW-46 (ảnh nhóm) `pass` | **RISK** | Đây là **điểm sửa nặng nhất** của vòng 2 (`to_user` từ `integer` → `string`, journal #136625 mục (4) — trước đó KHÔNG gửi được tin vào nhóm). 3/4 TC chưa chạy ⇒ chỉ có đúng 1 kết luận thật (ảnh vào nhóm). Chức năng lõi của màn chat không có bằng chứng còn chạy. | `[BLOCKER]` |
| **G2** | dev-impact | `D` (Dev kê "không có") · `T3` Tag Management — `saveTagLine` cộng/trừ `Tags.count_user_tag` | NEW-40 (chỉ kiểm **bot khác** không bị sai số đếm) | **GAP** | Fix thêm `continue` khi tag không thuộc bot. Không TC nào kiểm: 1 request gắn **nhiều nhãn trong đó có nhãn không thuộc bot** thì các nhãn hợp lệ còn lại có được gắn đủ và `count_user_tag` của **chính bot mình** có đúng không. Nhánh `continue` rất dễ làm lệch số đếm hiển thị. | `[BLOCKER]` |
| **G3** | dev-impact | `T2` Chat Settings — `ajaxDeleteItemStatus` (xóa trạng thái hội thoại) | NEW-83 (thêm/sửa/đổi thứ tự/xóa, `pass`) | **GAP** | `Kết quả mong đợi` của NEW-83 dừng ở "danh sách trên màn + dữ liệu" — **không** verify cascade BR-06: mọi hội thoại đang dùng trạng thái đó phải reset `id_status = NULL` và được đồng bộ lại. Đây là output cuối của thao tác xóa (RULE-06/07). | `[MAJOR]` |
| **G4** | dev-impact | `T1` — `ajaxSaveHideFriend` (ẩn bạn bè) | NEW-60 (ghim/bỏ ghim/ẩn, `pass`) | **RISK** | NEW-60 chỉ verify `is_hide` + `datetime_hide` + vị trí trên danh sách. Không verify **tác dụng phụ BR-05**: ẩn bạn bè phải tự động xác nhận toàn bộ tin chưa đọc (badge về 0) và sinh tin hệ thống 「非表示しました」. Chính nhánh ghi tin hệ thống này đang lỗi (bug Studio #1150 — vẫn chèn tin vào hội thoại của bot khác). | `[MAJOR]` |
| **G5** | dev-impact | `T5` Rich Menu (`chatEditRichMenu`, `ajaxGetDataForEditRichMenu`) · `T6` Step Delivery (`ajaxGetDataForEditStep`, `sendActionForUser`) | NEW-53 `skip` · NEW-87 `skip` · NEW-89 chưa chạy · NEW-90 chưa chạy | **RISK** | **4/4 TC của cả 2 tính năng đều không có kết luận.** Toàn bộ vùng rich menu + kịch bản bước thao tác từ màn chat đang là vùng trắng, dù Dev kê là `T5`/`T6` và 2 endpoint nằm trong 17 điểm siết bot. | `[BLOCKER]` |
| **G6** | dev-impact | `T7` (Leader bổ sung) — endpoint dùng chung `confirm` (`/basic/rec_message`) ở màn chat cũ (`chat_basic`/`chat_group`), màn quản lý hội thoại, app mobile admin | NEW-81 **`fail`** · NEW-80 `skip` · NEW-82 chưa chạy · NEW-84 `pass` | **RISK** | Dev **không kê** vùng này ở 4.3. `ajax-error.js` cố ý KHÔNG nạp ở 2 màn chat cũ ⇒ lỗi ở đó vẫn hỏng im lặng. NEW-81 fail kéo theo bug Studio #1155 (`/admin/list_message` trả 500). Không có TC pass nào cho app mobile admin. | `[MAJOR]` |
| **G7** | diff code | Danh sách file thay đổi — đối chiếu `spec_delta.files[]` (9 file) với `03-dev-impact.md` mục 4.1 (6 file) | NEW-75/76 (màn QR) `pass` · NEW-77 (tab QR) `pass` · NEW-79 (header) `pass` · NEW-78 (setting-add-friend) **`skip`** | **GAP** | Diff thật **không có** `config/sns-line.php` (Dev kê ở 4.1) và **có thêm 4 file Dev không kê**: `public/js/layout_v2_header.js`, `public/js/qr_code/v2/index.js`, `public/js/qr_code/v2/mixins/qr_display_tab.js`, `.../setting-add-friend/setting-add-friend-new.js`. 3/4 file có TC pass; riêng `setting-add-friend-new.js` chỉ có NEW-78 và đang `skip` ⇒ 1 file trong diff **không có TC nào chạy**. | `[MAJOR]` |
| **G8** | diff code | Hàm dùng chung mới `showChatAjaxError` (`public/js/chats/ajax-error.js`) — nối vào **~25 nhánh error** của `chat-v2.js` | NEW-67/68/69/70/71/72/73 | **GAP** | **BƯỚC 2 câu hỏi 3**: fix chạm hàm dùng chung nhưng Dev **KHÔNG kê danh sách 25 nhánh đã nối** ⇒ không cách nào biết nhánh nào còn sót. 2 bug Studio đã chứng minh có sót thật: #1152 (`changeStatus` chỉ có nhánh `success`, 403 không hiện gì) và #1156 (sắp xếp / xóa ghi chú thất bại không hiện lý do). TC hiện có chỉ chạm được vài nhánh, không phải toàn bộ. | `[BLOCKER]` |
| **G9** | diff code | `ChatController.php` (+742 dòng) — endpoint `saveSettingChat` (EP-34) của màn Cài đặt chat | NEW-83 (chỉ verify lưu thành công) | **RISK** | `spec-features/admin/chat-11/feature-spec.md` §9.3 cảnh báo endpoint này dùng `$request->all()` để update thẳng bảng `bots` (mass assignment). Ticket yêu cầu "validate required đầu vào" cho **mọi** api/ajax chat11 nhưng `saveSettingChat` **không có** trong danh sách 61 method Dev kê ở mục 3. Không TC nào kiểm field ngoài danh sách cho phép bị bỏ qua. | `[MAJOR]` |
| **G10** | diff code | **Endpoint "anh em" cùng màn nhưng KHÔNG nằm trong 61 method Dev rà** — `EP-16` `/ajax/remove-hide-friend` (bỏ ẩn bạn bè) · `EP-29` `/ajax/save-item-status-v2` · `EP-30` `/ajax/save-all-status` · `EP-32` `/ajax/sort-status-chat` · `EP-33` `/ajax/initial/get-data-setting` · `EP-38` `/ajax/get-bot-data` | **0 TC** nhắm tới (EP-32 chỉ được NEW-83 chạm gián tiếp qua thao tác đổi thứ tự; EP-33/EP-38 chỉ chạy khi tải trang, không TC nào assert) | **GAP** | Đây là **lỗ hổng ở tầng tồn kho endpoint**, không phải ở tầng TC: mandate của ticket là "**mọi** query PHẢI ràng buộc theo bot đang đăng nhập", nhưng Dev đã siết `ajaxSaveHideFriend`/`ajaxSaveHideFriends` (EP-14/15) mà **bỏ qua endpoint anh em `remove-hide-friend`** (EP-16); siết `ajaxSaveItemStatus`/`ajaxDeleteItemStatus` (EP-28/31) mà **bỏ qua `save-item-status-v2`, `save-all-status`, `sort-status-chat`** (EP-29/30/32) — cả ba đều ghi vào cùng bảng `StatusChat`. Đúng pattern "UI/endpoint anh em cùng lưới phân quyền bị bỏ sót ở tầng API". Vì **không TC nào chạm**, lỗ này sẽ không bao giờ bị bắt ở vòng test hiện tại. **Bằng chứng gián tiếp**: `feature-spec.md` BR-12 ghi giới hạn tên trạng thái **10 ký tự** tại `ajaxSaveAllStatus` (EP-30, **không được sửa**) trong khi EP-28 (**đã sửa**) nay cho 20 ký tự — NEW-26 đã quan sát được sự lệch này nhưng quy cho "khác nhau giữa 2 màn". | `[BLOCKER]` |
| **G11** | diff code | Nhóm **Hẹn gửi tin nhắn (「送信予約」)** — `EP-42`→`EP-48` (7 endpoint: tạo/sửa lịch gửi, lấy dữ liệu lịch, xoá template khỏi lịch, sắp xếp template, đổi delay, huỷ lịch, kiểm tra lịch đang chờ) | **0 TC** — không TC nào nhắc tới lịch gửi / 送信予約 | **GAP** | 7/49 endpoint (14% bề mặt API của màn) **hoàn toàn không có TC**. `ScheduleSendChatController` không nằm trong 61 method Dev rà và không có trong 9 file của diff ⇒ **backend không đổi**. NHƯNG nút 「送信予約」 nằm ngay trong màn chat và `public/js/chats/chat-v2.js` **đã bị sửa (+79 dòng, nối ~25 nhánh error)** ⇒ cần Dev xác nhận các lời gọi ajax của luồng hẹn gửi tin có nằm trong 25 nhánh bị đụng không. Nếu có mà không test ⇒ hỏng luồng đặt lịch gửi tin mà không ai biết. Kho đã có sẵn TC cho luồng này (`TC-CHT-315`, `TC-CHT-318`). | `[MAJOR]` |

---

## 2. Thiếu so với quan điểm test

**Kết luận**: 37 quan điểm Trigger khớp task · 26 đủ · **11 thiếu** (5 `[BLOCKER]` · 6 `[MAJOR]`).

| # | Mã quan điểm | Ưu tiên | Trigger khớp vì | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|---|
| **Q1** | `MSG-005` | Cao (BẮT BUỘC khi tiêu thụ quota tin nhắn) | Gửi tin từ chat 1:1 tăng `free_send_count`; BR-07: gói miễn phí giới hạn 1.000 tin/tháng, vượt thì lỗi 「配信数上限に達しています」 | **0 TC** | Lỗi vượt quota là **lỗi nghiệp vụ** ⇒ theo REQ-001 nay phải trả **400**, trước đây trả 200 kèm cờ `success=false`. Không TC nào chạm. Cũng không TC nào kiểm bộ đếm không bị cộng nhầm khi request bị 422/403 chặn ở tầng mới. | `[BLOCKER]` |
| **Q2** | `CONC-001` | Cao (nút thực thi hành động quan trọng) | Màn chat có nút gửi tin thật + thao tác hàng loạt (`updateStatusConfirm`, `quickAction`) | **0 TC** | Rủi ro **mới sinh ra từ chính bản fix**: nhánh error nay hiện thông báo ở ~25 chỗ, trong đó có 500 xảy ra **sau khi đã ghi dữ liệu** (bug #1147: bulk status trả 500 dù vòng lặp đã chạy). Người dùng thấy lỗi → bấm lại → tin trùng / trạng thái ghi 2 lần / quota trừ 2 lần. Không TC nào test bấm 2 lần hoặc gửi lại sau lỗi. | `[BLOCKER]` |
| **Q3** | `REG-RUN-001` | Cao (BẮT BUỘC khi có job/dữ liệu đang chạy dở) | BR-03 (tìm kiếm dùng read replica `ConversationReplicate`) · BR-06 (xóa status → sync Elasticsearch từng conversation) · job `SyncEsTask` | **0 TC** | Các write vừa bị thêm guard bot (ẩn friend, xóa status, đổi trạng thái hàng loạt) đều kéo theo bước đồng bộ ES. Guard trả sớm / `continue` có thể bỏ qua bước sync ⇒ ô **tìm kiếm** và **bộ lọc** bạn bè trả kết quả lệch với danh sách. Không TC nào verify danh sách sau khi ghi còn khớp khi tìm kiếm. | `[BLOCKER]` |
| **Q9** | `MSG-004` | Cao (BẮT BUỘC khi output là nội dung user cuối nhìn thấy trên LINE) | 4 endpoint gửi tin thật ra LINE | NEW-56 `skip`, và `Kết quả mong đợi` của nó dừng ở **khung hội thoại + danh sách + dữ liệu** | **RULE-06**: không TC nào verify tin thực sự đến **app LINE của bạn bè**. Cả bộ 94 TC dừng ở màn admin. Với ticket đổi mã HTTP + siết validate cho đúng 4 endpoint gửi tin, đây là output cuối bắt buộc phải kiểm. | `[BLOCKER]` |
| **Q4** | `ENV-003` ★ | Cao (BẮT BUỘC khi chạm media/file, URL/domain, job nền) | Gửi ảnh, tải tệp đính kèm từ kho lưu trữ (nhánh 502), tải ảnh mã QR ở 4 điểm gọi | 90/94 TC chạy `env = local`, 4 TC chưa chạy — **0 TC production** | **RULE-08**: không được kết luận media / domain / job từ local. Nhánh 502 "kho lưu trữ không trả được nội dung" (NEW-5) và toàn bộ luồng tải ảnh QR chỉ có bằng chứng trên local. | `[MAJOR]` |
| **Q5** | `MEDIA-001` | Trung bình → **Cao** (ảnh hiển thị cho khách cuối trên LINE) | `sendMedia` nằm trong nhóm 4 endpoint đổi validate | NEW-46 (Normal, `pass`) — **chỉ 1 TC** | Thiếu `Abnormal` + `Boundary`: tệp sai định dạng, tệp vượt dung lượng, tải lên hỏng giữa chừng → phải ra mã lỗi đúng nghĩa (422 / 400 / 502) và hiện thông báo, không phải 200 im lặng. Ngưỡng dung lượng chưa được chốt ở spec (xem §6). | `[MAJOR]` |
| **Q6** | `DEPLOY-LIVE-001` ★ | Cao (release lên production không bật maintain, đặc biệt khi **đổi payload/hợp đồng API**) | Ticket đổi mã HTTP của hàng loạt endpoint + thêm rule validate; `dev_impact` của Studio ghi rõ: *"rủi ro FE cũ đang đọc theo cờ success bị vỡ luồng nếu chưa nạp JS mới"* | NEW-74 (`DEPLOY-ASSET-001`, Boundary) — **chưa chạy** | Kịch bản thật khi deploy: người dùng đang mở sẵn màn chat với JS cũ, backend đã đổi sang mã 4xx/5xx ⇒ JS cũ không có `showChatAjaxError`, thao tác hỏng im lặng hoặc văng lỗi JavaScript. TC duy nhất chạm kịch bản này chưa có kết luận. | `[MAJOR]` |
| **Q7** | `SEC-ISO-001` | Cao (UI tải/hiển thị nhiều đối tượng dữ liệu đồng thời — nhiều hội thoại, nhiều tab) | Màn chat 1:1 mở nhiều tab / nhiều hội thoại; `botIdCurrent` do trang render theo session | #18384 `pass` · #18385 **`fail`** (bug #1154) | Chưa có TC `pass` nào cho tình huống nhiều phiên/nhiều tab. Lưu ý: đổi bot ở 1 tab đổi cả trình duyệt ⇒ TC đa bot **phải dùng 2 phiên đăng nhập riêng**, không phải 2 tab cùng phiên như #18385. | `[MAJOR]` |
| **Q8** | `DATA-REF-001` | Cao (BẮT BUỘC khi đối tượng có thao tác xóa VÀ được nơi khác tham chiếu) | `deleteProfilesBots` bị siết bot (lỗ (b)); BR-14: `messages_v2s.profile_send` là FK trỏ tới `bots_profiles` | NEW-62 (CRUD hồ sơ) **`skip`** · #18386 chỉ kiểm ảnh đại diện của hồ sơ **mặc định** | Không TC nào kiểm: xóa hồ sơ người gửi **đang được tin nhắn cũ tham chiếu** thì lịch sử tin hiển thị ra sao (tên/ảnh người gửi của tin cũ). TC duy nhất chạm CRUD hồ sơ lại đang `skip`. | `[MAJOR]` |
| **Q10** | `OUT-TRUTH-001` | Cao (mọi thao tác lưu/gửi có thông báo kết quả) | Chính là root cause của ticket — "báo thành công khi thất bại" | NEW-7 (Abnormal, `pass`) là TC **duy nhất** mang mã này; TC chiều ngược lại (NEW-11 — "dữ liệu hợp lệ vẫn trả thành công và hình dạng phản hồi không đổi") mang **mã lạ** `API-CONTRACT-001` **và đang `skip`** | Thiếu bằng chứng cho chiều "thành công vẫn là thành công": sau khi đổi hàng loạt mã trả về, không có kết luận nào chứng minh payload của luồng hợp lệ **không đổi hình dạng** so với bản trước. Đây là rủi ro vỡ FE lớn nhất của ticket. | `[MAJOR]` |
| **Q11** | `SYNC-APP-001` | Trung bình → **Cao** (flow critical user-facing) | Tính năng có mặt trên **cả web và app mobile quản trị**; journal #136646 nêu rõ endpoint `confirm` (`/basic/rec_message`) được gọi từ `public/js/mobile/common.js` và `public/js/mobile/events.js` | **NEW-82 là TC DUY NHẤT** chạm app mobile — **chưa chạy**, `exec_mode = manual` | 2 file JS của app (`mobile/common.js`, `mobile/events.js`) **không nằm trong 9 file của diff** ⇒ phía app **không** được nối vào hàm hiển thị lỗi mới, trong khi backend đã đổi từ 200 sang 4xx/5xx. Ngoài ra NEW-82 chỉ cover **1 thao tác** (xác nhận đã đọc); toàn bộ thao tác GUI còn lại trên app (mở hội thoại, đọc lịch sử tin, gửi tin) **không có TC nào**. Kho có `TC-CHT-430` (smoke web + app sau phát hành) để dẫn chiếu. | `[BLOCKER]` |

> Đã loại khỏi phạm vi sau khi kiểm chứng, **không đề xuất TC**: `INTG-HOOK-*`, `PAY-*`, `LIFF-ENTRY-001`, `MSG-002` (đặt lịch gửi tin) — `ScheduleSendChatController` và các job gửi tin **không nằm trong 9 file của diff** cũng không nằm trong 61 method Dev kê. `DATA-AUDIT-001` (lịch sử memo giới hạn 10 bản ghi, BR-13) — logic xoay vòng lịch sử **không bị chạm**, chỉ thêm điều kiện bot ở tầng truy vấn.

---

## 3. TC trùng lặp nội dung

Đã rà **94 TC** theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`) — phát hiện **1 nhóm** trùng.

| Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| Đổi rich menu + đổi/dừng kịch bản bước **từ màn chat** | **NEW-87** (kiểm thêm ở màn quản lý rich menu / kịch bản) | **NEW-53** → **GỘP, không xóa** | `DUP-SUBSET` | Cùng quan điểm nhóm regression · cùng `Normal` · cùng đối tượng + thao tác (đổi rich menu, đổi kịch bản, dừng kịch bản từ khối thông tin bên phải) · cùng tiền đề (bot có ≥1 rich menu + ≥1 kịch bản) · cùng expected "trả 200 + hiện đúng trên khối thông tin và trong dữ liệu". Phần 1-1 của NEW-53 nằm trọn trong NEW-87, NEW-87 còn đi xa hơn (đối chiếu màn quản lý). | `[MINOR]` |

**Gate xóa đã chạy**: NEW-53 là TC **duy nhất** cover chiều **hội thoại nhóm** (định danh dạng chuỗi) cho rich menu / kịch bản bước — xóa sẽ mất cover `COMPAT-LEGACY-001` ở vùng này ⇒ đổi đề xuất từ *xóa* sang **gộp**: giữ NEW-87, bổ sung vào NEW-87 bước lặp lại toàn bộ trên một hội thoại nhóm, rồi mới xóa NEW-53.

⚠️ Cả 2 TC đang `skip` — xem **G5**: gộp xong vẫn phải **chạy thật**, nếu không thì vùng rich menu / step vẫn là vùng trắng.

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | 9 TC `fail` + 10 bug Studio | **10 bug đang `open` trên Studio, tất cả đều có `redmine_id = null`** — chưa bug nào được raise thành ticket Redmine, nên Dev không nhìn thấy. 5 bug `severity = High`, trong đó **3 lỗ IDOR vẫn chưa được vá** dù đó là mandate chính của ticket: #1148 `/basic/chat/get-friends` trả danh sách bạn bè của bot khác khi đổi `botIdCurrent`; #1151 `/ajax/edit-system-name` sửa được tên bạn bè của bot khác; #1150 ẩn bạn bè vẫn chèn tin hệ thống 「非表示しました」 vào hội thoại bot khác. Cộng thêm #1147 (bulk status → 500 `Undefined variable: last_msg`) và #1149 (lưu cấu hình trỏ `id_setting` không tồn tại làm tab 友だち情報 **luôn** trả 500 — hỏng dữ liệu vĩnh viễn). | Raise đủ 10 bug lên Redmine #40051 (hoặc ticket con) trước khi chuyển test vòng 2. **Không kết luận ticket đạt** khi 3 lỗ IDOR gốc còn mở. |
| 2 | `[BLOCKER]` | `[AP-6]` — mục 3 `03-dev-impact.md` | Dev kê đủ 18 dòng caller ở tầng PHP nhưng **không kê danh sách ~25 nhánh error của `chat-v2.js` đã nối vào `showChatAjaxError`** — chính là hàm dùng chung mới. 2 bug (#1152, #1156) chứng minh có nhánh bị sót. Xem **G8**. | Yêu cầu Dev xuất danh sách đầy đủ `<thao tác> → <đã nối / chưa nối>` cho toàn bộ lời gọi `$.ajax` trong `chat-v2.js`, rồi đối chiếu 1-1 với TC. |
| 3 | `[MAJOR]` | Toàn bộ 94 TC | Tỷ lệ Đạt thật = **71/94 = 75,5% < 80%**. 23 TC (9 `fail` + 10 `skip` + 4 chưa chạy) **không có kết luận** — trong đó nằm đúng các vùng lõi: gửi tin 1-1, gửi tin nhóm, rich menu, kịch bản bước, hồ sơ người gửi, app mobile. | Chạy lại đủ 23 TC trước khi đánh giá coverage. |
| 4 | `[MAJOR]` | Toàn bộ 94 TC — `RULE-08` / `ENV-003` | 90 TC chạy ở `env = local`, **0 TC ở `prd`/`staging`/`dev`** (run #1754). Task chạm media (gửi ảnh), file storage (tải tệp đính kèm — nhánh 502), tải ảnh mã QR ở 4 điểm gọi. | Chạy lại nhóm media / tải tệp / tải ảnh QR trên **production**. Xem `TC-ENV003-01` §5. |
| 5 | `[MAJOR]` | 10 bug Studio + 94 TC — `RULE-02` | Cả 10 bug đều có `evidence = []`; cột `Evidence thực tế` của 94 TC trống (payload `testcase_list` không trả về trường này). Kết quả `Đạt` hiện là **tự khai**, không có bằng chứng đính kèm. | Bổ sung evidence theo đúng loại của từng quan điểm (ảnh chụp request/response, log ứng dụng, truy vấn dữ liệu trước/sau) trước khi chốt. |
| 6 | `[MAJOR]` | 33/94 TC | **14 mã quan điểm không có trong `framework/checklist-lme.md`**: `API-CONTRACT-001` (5) · `RULE-12` (4) · `SELECT-SCOPE-001` (4) · `TOOL-ERRHYG-001` (4) · `TOOL-KNOW-002` (3) · `RULE-TOOL-028` (3) · `RULE-TOOL-029` (2) · `API-001` (2) · `TOOL-OLDREC-001` · `TOOL-VAL2-001` · `RULE-09` · `SEARCH-001` · `I18N-001` · `TOOL-NEGCTRL-001`. Các TC này **không được tính là cover** quan điểm nào ở §2 (đã ảnh hưởng trực tiếp tới Q10). | Map lại sang mã của `checklist-lme.md`, hoặc bổ sung các mã này vào checklist nếu đó là quan điểm mới thật sự (RULE-10). |
| 7 | `[MAJOR]` | 94/94 TC | Cột `Trạng thái đánh giá spec` (`spec_status`) **trống toàn bộ**. Có ít nhất 1 chỗ TC đang chạy theo hành vi **khác spec** mà không ai đánh dấu: giới hạn tên trạng thái hội thoại 20 ký tự (NEW-18, NEW-26 đều pass) trong khi `feature-spec.md` BR-12 ghi **10 ký tự**. | Điền `Spec ghi rõ` / `Spec không ghi` / `Đã hỏi leader` cho từng TC; mọi TC `Spec không ghi` phải ghi đã hỏi ai. Xem §6. |
| 8 | `[MAJOR]` | `03-dev-impact.md` mục 4.1 + 4.2 | (a) Mục **4.2 kê "Không có data ảnh hưởng"** nhưng mục 2 TẦNG 2 thêm điều kiện `bot_id` vào các câu UPDATE/DELETE thật (`BotsProfiles`, `Tags.count_user_tag`, memo, `Conversation`, `SettingDisplayInfo*`, `StatusChat`) — không đổi câu lệnh nhưng **đổi phạm vi bản ghi bị ghi**. (b) Mục **4.1 kê `config/sns-line.php`** nhưng file này **không có trong diff**; ngược lại diff có 4 file JS Dev không kê (xem **G7**). | Yêu cầu Dev cập nhật lại 4.1 + 4.2 theo `spec_delta.files[]` của Studio. |
| 9 | `[MAJOR]` | Checkbox verify của `03-dev-impact.md` | File có `Auto-filled: 2026-09-21 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" chưa tick** — `F*`/`D*`/`T*` chưa được người verify, mà thực tế đã phát hiện 2 chỗ Dev kê sai (issue #8) và 1 vùng Dev kê thiếu (`T7`, xem G6). | Tester đọc lại 4 journal Redmine + `spec_delta` của Studio rồi tick. |
| 10 | `[MAJOR]` | Luồng GUI "chạy đúng bình thường" của màn chat | Bộ TC nghiêng về **kiểm mã lỗi** (42 TC `api` + 8 TC riêng về hiển thị lỗi). Các luồng GUI happy-path của chính màn chat lại mỏng hoặc chưa chạy: **gửi text 1-1** (NEW-56 `skip`), **gửi mẫu tin + nhãn dán** (NEW-47 `skip`), **hồ sơ người gửi** (NEW-62 `skip`), **rich menu + kịch bản bước** (NEW-53/NEW-87 `skip`), **gửi ảnh cho bạn bè 1-1** (chỉ có NEW-46 cho hội thoại **nhóm**), **gắn/gỡ nhãn cho bạn bè 1-1 từ màn chat** (chỉ NEW-51 cho nhóm), **xem câu trả lời biểu mẫu ở tab 4** (chỉ có NEW-25 ở tầng API, không có thao tác GUI), **bỏ ẩn bạn bè** (không có TC — xem G10), **hẹn gửi tin** (không có TC — xem G11). | Trước khi kết luận ticket đạt, phải có **một lượt GUI đi hết màn chat** với kết quả `pass`: mở màn → tải danh sách → tìm kiếm/lọc → mở hội thoại → cuộn tải lịch sử → gửi đủ 4 loại nội dung → đánh dấu đã đọc → đổi trạng thái → ghim/ẩn/bỏ ẩn → ghi chú → nhãn → thông tin bạn bè → rich menu → kịch bản bước → hồ sơ người gửi → hẹn gửi tin. Hiện bộ TC có đủ mảnh ghép nhưng **1/3 số mảnh chưa chạy**. |
| 11 | `[MINOR]` | 29/94 TC | Không gắn mã `EP-xx` nào trong `spec_ids` ⇒ không tra ngược được TC đó cover endpoint nào, phải suy từ nội dung. Chính vì vậy bảng tồn kho endpoint ở §1 phải đối chiếu **hai lần** (theo tag rồi theo nội dung) mới ra con số đúng. | Bổ sung `EP-xx` vào `spec_ids` cho 29 TC còn thiếu để `/review-tc` các vòng sau map được tự động. |

### Chất lượng từng TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 12 | `[MAJOR]` | `[AP-3]` — NEW-53, NEW-87, NEW-62, NEW-56 | **Happy-path-only regression**: các tính năng regression Dev kê (`T5` rich menu, `T6` step, hồ sơ người gửi) chỉ có đúng TC `Normal` với tiền đề "dữ liệu sạch", **và tất cả đều `skip`**. Không TC nào chạy regression với trạng thái biên (bạn bè đã ẩn / đã chặn / hội thoại nhóm / bản ghi cũ thiếu cột bot). | Chạy lại 4 TC này; bổ sung nhánh tiền đề biên cho NEW-87 sau khi gộp NEW-53. |
| 13 | `[MAJOR]` | NEW-65, NEW-61, NEW-83, NEW-60, NEW-66 | **Không atomic** — 1 TC gộp nhiều mục đích với các `Kết quả mong đợi` không liên quan nhau. Nặng nhất là **NEW-65**: gộp (a) mở lịch sử hội thoại đã ẩn, (b) mở lịch sử bạn bè đã chặn bot, (c) *gửi tin* cho bạn bè đã chặn phải hiện thông báo lý do — (c) là một quan điểm hoàn toàn khác (BR-09) bị nhét vào expected. Tương tự NEW-61 / NEW-83 gộp 4 thao tác CRUD, NEW-60 gộp 6 thao tác. | Tách (c) của NEW-65 thành TC riêng (kho đã có `TC-CHT-171` cho đúng tình huống này). Với NEW-61/NEW-83/NEW-60: giữ nguyên nhưng bổ sung expected cho **từng** thao tác thay vì 1 câu chung — hiện đang che mất G3 và G4. |
| 14 | `[MINOR]` | `FUNC-004` (6 TC) · `DEPLOY-ASSET-001` (2 TC) — `RULE-01` | `FUNC-004` là quan điểm ưu tiên **Cao** nhưng **cả 6 TC đều gắn `Boundary`**, không có dòng nào `Normal`/`Abnormal` và không ghi lý do ở `Ghi chú`. (Nội dung thực tế có kiểm cả trong ngưỡng lẫn vượt ngưỡng nên rủi ro thấp — đây là vấn đề gắn nhãn.) `DEPLOY-ASSET-001` có `Normal` + `Boundary`, thiếu `Abnormal`. | Gắn lại `Loại case` cho đúng, hoặc ghi lý do ở `Ghi chú` theo RULE-01. |
| 15 | `[NIT]` | Task #308 | Đang có **run #1803 ở trạng thái `queued`** — các con số pass/fail trong report này chụp từ run #1754 (2026-09-20 22:25 → 2026-09-21 02:28) và có thể đổi sau khi run mới chạy xong. | Đối chiếu lại §4 mục 3 sau khi run #1803 kết thúc. |

> **Anti-pattern đã rà, KHÔNG dính**: `AP-1` (single-trigger generic-fix) — bộ TC có ≥ 5 trigger khác nhau cho nhánh xử lý lỗi chung (422 NEW-68 · 403/404 NEW-69 · 500 NEW-7 · 502 NEW-5/NEW-6 · phản hồi không đọc được NEW-70) **cộng** nhánh fallback cho mã chưa biết ⇒ đạt yêu cầu. `AP-2` (symptom-only) — ticket không phải báo cáo triệu chứng của khách, root cause đã xác định bằng code. `AP-4`, `AP-5` — không dính.

---

## 5. TCs đề xuất bổ sung (17)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa001-chat11-11チャット.md` (FA-001, 558 dòng — đọc §"Điểm mâu thuẫn với spec" + các nhóm 対応ステータス CRUD · Quick action · Message hệ thống · Gửi text · Giới hạn plan & token · Phân quyền staff & môi trường) |
| Vùng regression phát hiện từ kho | `TC-CHT-158` (cascade xóa status, khớp BR-06) · `TC-CHT-100` + `TC-CHT-273` (ẩn bạn bè + tin hệ thống 「非表示しました」, khớp BR-05) · `TC-CHT-418` (giới hạn 1.000 tin/tháng gói miễn phí, khớp BR-07, môi trường PRODUCTION) · `TC-CHT-171` (gửi tin cho bạn bè đã block, khớp BR-09) · `TC-CHT-37`/`TC-CHT-38`/`TC-CHT-48` (bulk + tìm kiếm qua read replica) · `TC-CHT-430` (smoke sau phát hành, web + app) |
| Conflict expected vs kho | **Có 3 điểm CHƯA CHỐT ở kho, đã đưa sang §6, KHÔNG tự chọn bên**: `MT-08` (ẩn bạn bè có tự động xác nhận tin không) chặn expected của **G4** · `MT-10` (ngưỡng dung lượng/định dạng media chat 1:1) chặn expected của **Q5** · `MT-14` (spec không có quy tắc nào cho hội thoại nhóm) chặn expected của toàn bộ nhánh nhóm ở **G5**. Không có conflict trực tiếp giữa TC đề xuất và TC kho. |
| GAP dùng lại TC kho (không viết mới) | `G3` → `TC-CHT-158` "Xoá status — có xác nhận, bạn bè đang dùng status đó về なし" · `G4` → `TC-CHT-100` "非表示にする — bạn bè bị ẩn khỏi danh sách và có message hệ thống" + `TC-CHT-273` "Ẩn bạn bè — có lưu message hệ thống; bỏ ẩn thì KHÔNG lưu" · `Q1` → `TC-CHT-418` "Bot miễn phí — giới hạn 1.000 tin gửi mỗi tháng" |
| Xác nhận chống trùng | Đã đối chiếu **94 TC** ở BƯỚC 0 + kho FA-001 — **không TC đề xuất nào trùng**. 5 vùng thiếu (`G1`, `G5`, `G6`, `G7`, `Q10`) **KHÔNG sinh TC mới** vì đã có TC trong bộ gốc, chỉ cần chạy lại (xem bảng giải thích cuối §5). |
| Đối chiếu tồn kho endpoint | Đã map 94 TC với **49 endpoint** ở `feature-spec.md` §6 theo **2 lần** (theo tag `spec_ids` rồi theo nội dung TC): 33 `OK` · 4 chỉ có TC chưa chạy · **12 không có TC** → sinh `TC-PERM002-01`, `TC-FUNC004-01`, `TC-REGSHARED001-01`. |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-DATACOUNT001-01 | API | DATA-COUNT-001 | Nhãn (タグ) trong chat | Abnormal | auto | Tất cả | Gắn một lượt nhiều nhãn trong đó có nhãn không thuộc bot — các nhãn hợp lệ vẫn gắn đủ và số đếm không lệch | Đăng nhập tài khoản có quyền trên bot A. Bot A có 3 nhãn T1, T2, T3; ghi lại số người dùng đang gắn của từng nhãn. Bot X (ngoài quyền) có nhãn TX, ghi lại số người dùng của TX. Chọn sẵn 1 bạn bè của bot A chưa gắn nhãn nào | 1. Mở màn chat 1:1 của bot A, chọn bạn bè kiểm thử, mở khối gắn nhãn và gắn T1 + T2 rồi lưu (thao tác bình thường trên màn hình)<br>2. Ghi lại số người dùng của T1, T2 hiển thị ở màn quản lý nhãn<br>3. Bắt request gắn nhãn vừa phát ra, sửa danh sách nhãn thành T1 + T3 + TX rồi gửi lại<br>4. Ghi lại mã trạng thái và toàn văn phản hồi<br>5. Mở lại khối nhãn của bạn bè trên màn chat, đối chiếu nhãn đang gắn<br>6. Mở màn quản lý nhãn của bot A đọc số người dùng của T1, T2, T3; đăng nhập tài khoản của bot X đọc số người dùng của TX<br>7. Lặp lại bước 3-6 cho thao tác **gỡ** nhãn (danh sách gỡ trộn TX) | Danh sách gắn: `T1, T3, TX` · Danh sách gỡ: `T2, TX` | Bạn bè được gắn đúng T1 + T3 (nhãn hợp lệ trong lô **không bị bỏ sót** vì có phần tử lạ), TX không được gắn. Số người dùng của T1, T2, T3 khớp đúng số bạn bè thực tế đang gắn — không cộng/trừ thừa, không âm. Số người dùng của TX **giữ nguyên** giá trị ghi ở tiền điều kiện. Thao tác gỡ cho kết quả tương ứng. Không lượt nào trả 200 kèm cờ thất bại. |  | Lấp `G2` · Đánh giá spec: Spec không ghi (hành vi của lô trộn nhãn lạ chưa có trong spec — đã đưa vào §6) · Evidence: ảnh chụp request/response + số người dùng của 4 nhãn trước/sau |
| TC-UI003-01 | UI | UI-003 | Hiển thị lỗi trên màn hình | Abnormal | auto | Tất cả | Mọi thao tác ghi và xoá trên màn chat đều hiện lý do khi máy chủ từ chối | Đăng nhập bot A, mở màn chat 1:1 với đầy đủ dữ liệu (có bạn bè, ghi chú, nhãn, trạng thái hội thoại, hồ sơ người gửi, rich menu, kịch bản bước). Có cách buộc máy chủ trả mã lỗi cho từng thao tác (chặn/sửa phản hồi ở tầng trình duyệt) | 1. Lập danh sách **toàn bộ** thao tác ghi/xoá bấm được trên màn chat: gửi tin, gửi ảnh, gửi mẫu tin, gửi nhãn dán, đánh dấu đã đọc, đổi trạng thái đối ứng (đơn + hàng loạt), ghim, ẩn, thêm/sửa/xoá/sắp xếp ghi chú, gắn/gỡ nhãn, sửa tên hệ thống, đồng bộ thông tin từ LINE, lưu cấu hình hiển thị, thêm/chọn/xoá hồ sơ người gửi, đổi rich menu, đổi/dừng kịch bản bước, tải tệp đính kèm<br>2. Với **từng** thao tác: thực hiện bình thường, buộc phản hồi trả 403, ghi lại màn hình có hiện thông báo không<br>3. Lặp lại với mã 422 và 500<br>4. Với mỗi lượt, ghi thêm: lớp phủ đang tải có tắt không, danh sách trên màn có bị đổi giả không<br>5. Lập bảng `thao tác × mã lỗi → có/không hiện thông báo` | 3 mã lỗi (403, 422, 500) × toàn bộ thao tác ghi/xoá ở bước 1 | **Không ô nào trong bảng là "không hiện gì"**: mọi thao tác, với cả 3 mã lỗi, đều hiện thông báo lấy từ phản hồi máy chủ (422 hiện lỗi từng trường, 403/500 hiện câu thông báo). Lớp phủ đang tải luôn được tắt. Danh sách trên màn không đổi khi máy chủ từ chối. Mọi ô "không hiện gì" là một nhánh error chưa được nối vào hàm hiển thị lỗi chung — ghi rõ tên thao tác để Dev vá. |  | Lấp `G8` · Đây là TC **bao quát** thay cho danh sách ~25 nhánh mà Dev chưa kê · Đã biết trước 2 ô sẽ đỏ: đổi trạng thái đối ứng (bug Studio #1152) và sắp xếp/xoá ghi chú (bug #1156) · Đánh giá spec: Spec ghi rõ (REQ-007) · Evidence: bảng đối chiếu đầy đủ + ảnh chụp từng ô đỏ |
| TC-SEC001-01 | API | SEC-001 | Cài đặt chat | Abnormal | auto | Tất cả | Lưu cài đặt chat bỏ qua những trường không thuộc màn cài đặt | Đăng nhập bot A. Ghi lại giá trị hiện tại của các trường nhạy cảm của bot A không hiển thị trên màn cài đặt chat (ví dụ gói cước, cờ rút gọn URL, cờ tự động xác nhận khi trả lời) | 1. Mở màn cài đặt chat, đổi một tuỳ chọn bất kỳ rồi bấm lưu — bắt lại request lưu cài đặt<br>2. Thêm vào request các trường nhạy cảm ghi ở tiền điều kiện với giá trị khác giá trị hiện tại, giữ nguyên các trường hợp lệ, gửi lại<br>3. Ghi lại mã trạng thái và phản hồi<br>4. Tải lại màn cài đặt chat, đối chiếu tuỳ chọn hợp lệ đã đổi đúng chưa<br>5. Đối chiếu lại toàn bộ trường nhạy cảm ở tiền điều kiện | Request lưu cài đặt chat hợp lệ + các trường nhạy cảm bị chèn thêm | Tuỳ chọn hợp lệ của màn cài đặt chat được lưu đúng. **Toàn bộ trường nhạy cảm giữ nguyên giá trị ban đầu** — request không ghi đè được trường nằm ngoài phạm vi màn hình, dù client có gửi lên. |  | Lấp `G9` · Bắt nguồn từ cảnh báo `feature-spec.md` §9.3 (EP-34 dùng `$request->all()`) · Đánh giá spec: Spec không ghi — cần chốt endpoint này có thuộc phạm vi ticket không (§6) · Evidence: ảnh chụp request + giá trị các trường nhạy cảm trước/sau |
| TC-CONC001-01 | UI | CONC-001 | Gửi tin nhắn | Abnormal | auto | Tất cả | Gửi tin bấm liên tiếp hai lần và gửi lại sau khi hiện lỗi đều không tạo tin trùng | Đăng nhập bot A, mở hội thoại với bạn bè kiểm thử. Ghi lại số tin hiện có trong hội thoại và số tin đã gửi trong tháng của bot | 1. Nhập nội dung tin rồi bấm nút gửi **hai lần liên tiếp thật nhanh**<br>2. Đếm số tin xuất hiện trong khung hội thoại và trong dữ liệu; đếm số tin đã gửi trong tháng của bot<br>3. Nhập tin mới, buộc phản hồi của thao tác gửi trả 500 **sau khi máy chủ đã ghi tin**<br>4. Quan sát màn hình: có hiện thông báo lỗi không<br>5. Bấm gửi lại đúng nội dung đó một lần nữa<br>6. Đếm lại số tin trong khung hội thoại, trong dữ liệu, số tin đã gửi trong tháng và số tin bạn bè thực nhận | Cùng một nội dung tin, gửi 2 lần · Kịch bản 500 sau khi ghi | Bấm 2 lần chỉ sinh **1 tin**: khung hội thoại, dữ liệu và số tin đã gửi trong tháng đều chỉ tăng 1. Với kịch bản lỗi sau khi ghi: màn hình hiện thông báo lỗi, và sau khi người dùng gửi lại thì **không tồn tại 2 tin giống nhau** — hoặc hệ thống chặn tin trùng, hoặc màn hình phải cho biết tin trước đã được gửi. Số tin đã gửi trong tháng không tăng nhiều hơn số tin bạn bè thực nhận. |  | Lấp `Q2` · RULE-01: loại case `Normal` của luồng gửi tin đã có ở NEW-56 (cần chạy lại), `Boundary` không áp dụng · Đánh giá spec: Spec không ghi (§6) · Evidence: ảnh chụp khung hội thoại + truy vấn tin nhắn + bộ đếm tin đã gửi |
| TC-CONC001-02 | UI | CONC-001 | Đánh dấu đã đọc và trạng thái hội thoại | Abnormal | auto | Tất cả | Đổi trạng thái hàng loạt bấm hai lần hoặc bấm lại sau lỗi không ghi đè sai dữ liệu | Đăng nhập bot A. Chọn sẵn 5 hội thoại với các trạng thái đối ứng khác nhau, ghi lại trạng thái từng hội thoại | 1. Tích chọn 5 hội thoại, chọn một trạng thái mới và bấm thực thi **hai lần liên tiếp**<br>2. Ghi lại số request phát sinh và mã trạng thái từng request<br>3. Đối chiếu trạng thái của 5 hội thoại trên danh sách và trong dữ liệu<br>4. Lặp lại thao tác nhưng chọn trạng thái 「なし」 (bỏ chọn trạng thái) và buộc phản hồi trả 500<br>5. Quan sát màn hình rồi bấm thực thi lại một lần nữa<br>6. Đối chiếu lại trạng thái 5 hội thoại và số huy hiệu chưa đọc | 5 hội thoại · Trạng thái mới, rồi trạng thái 「なし」 | Bấm 2 lần cho kết quả giống hệt bấm 1 lần: cả 5 hội thoại về đúng trạng thái đã chọn, không hội thoại nào bị ghi sai, số huy hiệu chưa đọc không lệch. Với kịch bản 500: màn hình hiện lý do, và sau khi bấm lại thì dữ liệu vẫn về đúng trạng thái cuối cùng đã chọn, không sinh bản ghi thừa. |  | Lấp `Q2` · Liên quan bug Studio #1147 (đổi hàng loạt sang 「なし」 trả 500) — chạy TC này sau khi #1147 được fix · Đánh giá spec: Spec không ghi (§6) · Evidence: ảnh chụp danh sách + truy vấn trạng thái 5 hội thoại trước/sau |
| TC-REGRUN001-01 | UI | REG-RUN-001 | Danh sách bạn bè và lịch sử tin | Normal | auto | product | Tìm kiếm và lọc bạn bè vẫn khớp danh sách sau khi ẩn bạn bè, đổi trạng thái và xoá trạng thái | Đăng nhập bot A trên môi trường production với bot có nhiều bạn bè (đủ để danh sách phải phân trang). Chọn sẵn bạn bè F1 (tên dễ tìm), F2, F3. Có một trạng thái đối ứng S1 đang gắn cho F2 và F3 | 1. Gõ tên F1 vào ô tìm kiếm, xác nhận tìm ra F1<br>2. Ẩn F1 từ màn chat<br>3. Tìm lại F1 bằng bộ lọc mặc định và bằng bộ lọc 「非表示中」, ghi lại kết quả<br>4. Đổi trạng thái đối ứng của F2 sang một trạng thái khác<br>5. Lọc danh sách theo trạng thái cũ và trạng thái mới, đối chiếu F2 xuất hiện ở đúng nhóm nào<br>6. Xoá trạng thái S1 ở màn cài đặt chat<br>7. Lọc danh sách theo trạng thái 「なし」 và kiểm tra F2, F3<br>8. Với mỗi bước, đợi tối đa 1 phút rồi tìm/lọc lại lần nữa để loại trừ độ trễ đồng bộ | F1, F2, F3 · trạng thái S1 | Sau mỗi thao tác ghi, kết quả tìm kiếm và bộ lọc **khớp ngay** với danh sách hiển thị và với dữ liệu: F1 biến khỏi bộ lọc mặc định và hiện ở 「非表示中」; F2 chỉ hiện ở nhóm trạng thái mới; sau khi xoá S1 thì F2, F3 hiện ở nhóm 「なし」. Không trường hợp nào phải đợi lần tìm thứ hai mới đúng — nếu có, ghi rõ thao tác nào bị lệch để truy bước đồng bộ bị bỏ qua. |  | Lấp `Q3` · RULE-08: tìm kiếm dùng read replica/đồng bộ nền nên **không kết luận từ local** · Dẫn từ `TC-CHT-37`, `TC-CHT-38`, `TC-CHT-48` của kho (regression) · Đánh giá spec: Spec ghi rõ (BR-03, BR-06) · Evidence: ảnh chụp kết quả tìm/lọc + truy vấn dữ liệu sau mỗi bước |
| TC-ENV003-01 | UI | ENV-003 | API/ajax chung màn chat | Normal | manual | product | Nhóm thao tác media và tải tệp chạy đúng trên production | Tài khoản có quyền trên một bot thật ở production. Hội thoại có tin chứa tệp đính kèm (ảnh, video, tệp PDF nếu có). Màn mã QR của bot đã có ảnh QR | 1. Mở màn chat 1:1 trên production, gửi một ảnh cho bạn bè kiểm thử<br>2. Tải một tệp đính kèm từ khung hội thoại<br>3. Mở màn quản lý mã QR, tải ảnh mã QR<br>4. Mở tab hiển thị mã QR, tải ảnh<br>5. Mở màn cài đặt thêm bạn, tải ảnh<br>6. Bấm nút tải mã QR trên đầu trang quản trị<br>7. Với mỗi bước, ghi lại mã trạng thái, tệp tải về có mở được không và kích thước tệp | Ảnh gửi đi · 4 điểm tải ảnh mã QR · 1 tệp đính kèm | Cả 6 thao tác thành công trên production: ảnh gửi được và hiện đúng, tệp tải về mở được và không rỗng, 4 điểm tải ảnh mã QR đều trả tệp hợp lệ. Không thao tác nào trả mã lỗi và không thao tác nào im lặng. |  | Lấp `Q4` · RULE-08 / RULE-06 · `manual` vì bắt buộc chạy trên production với bot thật · Đánh giá spec: Spec ghi rõ (REQ-008) · Evidence: tệp tải về + ảnh chụp request/response trên production |
| TC-MEDIA001-01 | UI | MEDIA-001 | Gửi tin nhắn | Abnormal | auto | product | Gửi media sai định dạng hoặc vượt dung lượng bị từ chối với thông báo rõ ràng | Đăng nhập bot A, mở hội thoại với bạn bè kiểm thử. Chuẩn bị sẵn: 1 tệp sai định dạng (ví dụ `.txt` đổi đuôi thành `.png`), 1 tệp ảnh vượt ngưỡng cho phép, 1 tệp video vượt ngưỡng | 1. Bấm 「メディア送信」, chọn tệp sai định dạng, bấm gửi<br>2. Ghi lại mã trạng thái, thông báo hiện trên màn và nội dung phản hồi<br>3. Lặp lại với tệp ảnh vượt ngưỡng<br>4. Lặp lại với tệp video vượt ngưỡng<br>5. Ngắt mạng giữa lúc tải lên rồi nối lại, ghi lại màn hình hiện gì<br>6. Sau mỗi lượt, kiểm tra khung hội thoại và dữ liệu xem có tin rác nào được tạo không<br>7. Kiểm tra bạn bè trên app LINE có nhận được gì không | Tệp sai định dạng · ảnh vượt ngưỡng · video vượt ngưỡng · ngắt mạng giữa chừng | Không lượt nào trả 200 kèm cờ thất bại: sai định dạng và vượt dung lượng trả mã lỗi dữ liệu đầu vào, lỗi tầng lưu trữ trả mã lỗi dịch vụ ngoài. Màn hình hiện thông báo lý do bằng tiếng Nhật ở **mọi** lượt. Không tin rác nào được tạo trong khung hội thoại lẫn trong dữ liệu, bạn bè không nhận được gì. |  | Lấp `Q5` · RULE-08 (media) → chạy production · Ngưỡng dung lượng chưa được chốt ở spec (kho `MT-10`) → xem `TC-MEDIA001-02` và §6 · Đánh giá spec: Spec không ghi · Evidence: ảnh chụp thông báo + phản hồi + truy vấn tin nhắn sau mỗi lượt |
| TC-MEDIA001-02 | UI | MEDIA-001 | Gửi tin nhắn | Boundary | auto | product | Ghi nhận ngưỡng dung lượng thực tế của từng loại media ở chat 1:1 | Đăng nhập bot A, mở hội thoại với bạn bè kiểm thử. Chuẩn bị bộ tệp quanh các mốc đang được nhắc trong tài liệu cũ: ảnh 9,9 MB / 10 MB / 10,1 MB; video 199 MB / 200 MB / 201 MB; âm thanh 24,9 MB / 25 MB / 25,1 MB | 1. Gửi lần lượt từng tệp trong bộ ảnh, ghi lại mã trạng thái và thông báo<br>2. Lặp lại với bộ video<br>3. Lặp lại với bộ âm thanh<br>4. Với mỗi lượt thành công, kiểm tra bạn bè nhận được tệp trên app LINE và tệp mở được<br>5. Lập bảng `loại media × dung lượng → chấp nhận / từ chối + thông báo` | 3 loại media × 3 mốc dung lượng | Ghi nhận **ngưỡng thực tế** của từng loại media thành bảng, và với mọi mốc bị từ chối thì màn hình phải hiện thông báo lý do (không im lặng, không trả 200 kèm cờ thất bại). Bảng này là đầu vào để Leader chốt ngưỡng chính thức và bổ sung vào spec — **TC không tự khẳng định ngưỡng đúng là bao nhiêu**. |  | Lấp `Q5` · RULE-08 (media) → chạy production · Kho `MT-10` đang ⏳ CHỜ QUYẾT ĐỊNH, đã đưa vào §6 — KHÔNG tự chọn bên · Đánh giá spec: Spec không ghi · Evidence: bảng ngưỡng thực tế + ảnh chụp từng lượt |
| TC-DEPLOYLIVE001-01 | UI | DEPLOY-LIVE-001 | API/ajax chung màn chat | Abnormal | manual | product | Tab chat mở sẵn từ trước khi phát hành vẫn thao tác được hoặc báo lỗi rõ ràng | Môi trường production, chuẩn bị ngay trước thời điểm phát hành bản sửa. Mở sẵn màn chat 1:1 ở 2 tab và để nguyên, không tải lại trang. Mở sẵn bảng điều khiển trình duyệt ở cả 2 tab | 1. Trước khi phát hành: ở tab 1 thực hiện gửi tin, đánh dấu đã đọc, thêm ghi chú — xác nhận đều chạy<br>2. Tiến hành phát hành bản sửa<br>3. **Không tải lại trang**, ở tab 1 lặp lại đúng 3 thao tác trên<br>4. Ghi lại: thao tác có thành công không, màn hình hiện gì, bảng điều khiển có lỗi JavaScript nào không<br>5. Ở tab 1 thực hiện thêm một thao tác bị máy chủ từ chối (ví dụ đánh dấu đã đọc lần 2 khi không còn tin chưa đọc) và ghi lại màn hình hiện gì<br>6. Tải lại trang ở tab 2 rồi lặp lại toàn bộ bước 3-5 để so sánh<br>7. Đối chiếu dữ liệu: các thao tác ở bước 3 có thực sự ghi đúng không | 3 thao tác lõi · tab chưa tải lại vs tab đã tải lại | Thao tác ở tab chưa tải lại vẫn ghi dữ liệu **đúng** như tab đã tải lại. Không có lỗi JavaScript nào trong bảng điều khiển làm hỏng thao tác tiếp theo. Khi máy chủ từ chối: tab đã tải lại hiện thông báo lý do; tab chưa tải lại — nếu không hiện được thông báo thì **tối thiểu không được báo thành công giả** và không được làm treo màn hình. Ghi rõ mọi khác biệt giữa 2 tab để quyết định có cần ép tải lại khi phát hành hay không. |  | Lấp `Q6` · `manual` vì phải bám đúng thời điểm phát hành trên production · Dẫn từ `TC-CHT-430` của kho (smoke sau phát hành) · Đánh giá spec: Spec ghi rõ (REQ-014) · Evidence: ảnh chụp 2 tab + bảng điều khiển + truy vấn dữ liệu sau mỗi thao tác |
| TC-SECISO001-01 | UI | SEC-ISO-001 | API/ajax chung màn chat | Abnormal | auto | Tất cả | Hai phiên đăng nhập mở hai tài khoản LINE khác nhau cùng lúc không lẫn dữ liệu | Có 2 tài khoản quản trị U1 (quyền trên bot A) và U2 (quyền trên bot B), **đăng nhập ở 2 trình duyệt hoặc 2 cửa sổ ẩn danh riêng** — không dùng 2 tab cùng một phiên vì đổi bot ở 1 tab sẽ đổi cả trình duyệt. Mỗi bot có bạn bè và hội thoại riêng, tên dễ phân biệt | 1. Phiên U1 mở màn chat bot A, phiên U2 mở màn chat bot B<br>2. Ở cả 2 phiên, tải danh sách bạn bè và mở một hội thoại<br>3. Đối chiếu danh sách và nội dung tin của từng phiên với bot tương ứng<br>4. Ở phiên U1 gửi tin, thêm ghi chú và đổi trạng thái hội thoại; ở phiên U2 làm tương tự trên bot B — thực hiện xen kẽ<br>5. Đọc số huy hiệu chưa đọc ở cả 2 phiên<br>6. Sau mỗi lượt, đối chiếu dữ liệu của bot A và bot B<br>7. Làm mới cả 2 phiên rồi đối chiếu lần nữa | Bot A và bot B, thao tác xen kẽ giữa 2 phiên | Mỗi phiên **chỉ** thấy và chỉ ghi dữ liệu của bot của mình: không bạn bè, tin nhắn, ghi chú, trạng thái hay số huy hiệu nào của bot kia lọt sang. Dữ liệu của bot A và bot B sau toàn bộ thao tác khớp đúng với thao tác thực hiện trên chính phiên đó. |  | Lấp `Q7` · Thay thế cách dựng env sai của #18385 (2 tab cùng phiên — bug Studio #1154) · Đánh giá spec: Spec ghi rõ (REQ-003) · Evidence: ảnh chụp 2 phiên song song + truy vấn dữ liệu 2 bot trước/sau |
| TC-DATAREF001-01 | UI | DATA-REF-001 | Hồ sơ người gửi | Abnormal | auto | Tất cả | Xoá hồ sơ người gửi đang được tin nhắn cũ tham chiếu không làm hỏng lịch sử tin | Đăng nhập bot A. Tạo hồ sơ người gửi P1 với tên và ảnh riêng biệt, chọn P1 rồi gửi 2 tin cho bạn bè kiểm thử. Xác nhận 2 tin đó hiển thị tên và ảnh của P1. Ghi lại nội dung 2 tin | 1. Mở modal hồ sơ người gửi, chọn lại hồ sơ mặc định<br>2. Xoá hồ sơ P1, ghi lại mã trạng thái<br>3. Tải lại màn chat và mở hội thoại vừa gửi<br>4. Quan sát 2 tin đã gửi bằng P1: tên người gửi, ảnh đại diện, nội dung tin<br>5. Cuộn tải thêm lịch sử tin để chắc chắn không lỗi<br>6. Gửi thêm 1 tin mới và quan sát tên/ảnh người gửi<br>7. Kiểm tra dữ liệu tin nhắn: trường hồ sơ người gửi của 2 tin cũ còn trỏ tới bản ghi đã xoá không | Hồ sơ P1 bị xoá sau khi đã gửi tin | Xoá hồ sơ trả 200. **Lịch sử tin vẫn mở được bình thường** — 2 tin cũ không làm vỡ khung hội thoại, không trả lỗi, không mất nội dung; tên/ảnh người gửi của tin cũ hiển thị theo quy tắc rõ ràng (giữ nguyên giá trị cũ hoặc rơi về hồ sơ mặc định) và **nhất quán** giữa các lần tải lại. Tin mới hiển thị đúng hồ sơ đang chọn. |  | Lấp `Q8` · RULE-01: loại case `Normal` của CRUD hồ sơ đã có ở NEW-62 (đang `skip`, cần chạy lại) · Đánh giá spec: Spec không ghi (BR-14 không mô tả hành vi khi hồ sơ bị xoá) — đã đưa §6 · Evidence: ảnh chụp lịch sử tin trước/sau khi xoá + truy vấn trường hồ sơ của 2 tin cũ |
| TC-PERM002-01 | API | PERM-002 | API/ajax chung màn chat | Abnormal | auto | Tất cả | Năm endpoint anh em chưa được siết phạm vi bot vẫn phải từ chối dữ liệu của tài khoản LINE khác | Đăng nhập tài khoản U1 có quyền trên bot A, **không** có quyền trên bot X. Bot X có: 1 bạn bè đang ẩn, 2 trạng thái hội thoại (ghi lại tên, màu, thứ tự). Ghi lại toàn bộ giá trị này trước khi test | 1. Từ màn chat bot A, thực hiện thao tác **bỏ ẩn** một bạn bè của bot A để lấy request mẫu; sửa mã hội thoại thành hội thoại đang ẩn của bot X rồi gửi lại<br>2. Lấy request mẫu của thao tác lưu trạng thái ở màn cài đặt chat, đổi sang biến thể lưu nhiều trạng thái cùng lúc và biến thể lưu hàng loạt, mỗi lượt gắn mã trạng thái thuộc bot X rồi gửi lại<br>3. Lấy request mẫu của thao tác đổi thứ tự trạng thái, chèn mã trạng thái của bot X vào danh sách rồi gửi lại<br>4. Lấy request mẫu của thao tác đọc thông tin bot hiện tại, đổi mã bot thành bot X rồi gửi lại<br>5. Với mỗi lượt: ghi lại mã trạng thái HTTP và toàn văn phản hồi<br>6. Đăng nhập tài khoản của bot X, đối chiếu lại bạn bè đang ẩn và 2 trạng thái hội thoại (tên, màu, thứ tự) | Mã hội thoại / mã trạng thái / mã bot đều thuộc **bot X**; tài khoản thao tác chỉ có quyền trên bot A | Không lượt nào đọc được dữ liệu của bot X và **không lượt nào thay đổi dữ liệu của bot X**: bạn bè của bot X vẫn đang ẩn, 2 trạng thái hội thoại giữ nguyên tên/màu/thứ tự, thông tin bot X không bị trả về. Phản hồi phải là mã từ chối quyền (403) hoặc không tìm thấy (404), **không** phải 200. Mọi lượt trả 200 kèm dữ liệu bot X là lỗ IDOR còn sót ⇒ ghi rõ endpoint nào để Dev bổ sung vào phạm vi fix. |  | Lấp `G10` · Endpoint kiểm: bỏ ẩn bạn bè · lưu nhiều trạng thái · lưu hàng loạt trạng thái · sắp xếp trạng thái · đọc thông tin bot · Đánh giá spec: Spec ghi rõ (REQ-003 — mandate "mọi query ràng buộc theo bot") · Evidence: ảnh chụp 5 request/response + truy vấn dữ liệu bot X trước/sau |
| TC-FUNC004-01 | API | FUNC-004 | Cài đặt chat — Trạng thái hội thoại | Boundary | auto | Tất cả | Giới hạn độ dài tên trạng thái phải giống nhau ở mọi đường lưu trạng thái | Đăng nhập bot A. Mở màn cài đặt chat, xác định đủ các đường lưu trạng thái hội thoại thao tác được từ giao diện: thêm/sửa 1 trạng thái, lưu nhiều trạng thái cùng lúc, lưu hàng loạt | 1. Với **từng** đường lưu, nhập tên trạng thái dài đúng 20 ký tự rồi lưu; ghi lại mã trạng thái, thông báo và giá trị ghi vào dữ liệu<br>2. Lặp lại với tên dài 21 ký tự<br>3. Lặp lại với tên dài 11 ký tự (mốc mà màn chat đang chặn tại chỗ)<br>4. Lặp lại toàn bộ với tên gồm ký tự tiếng Nhật toàn giác và emoji<br>5. Lập bảng `đường lưu × độ dài → chấp nhận / từ chối + thông báo` | 3 đường lưu × các mốc 11, 20, 21 ký tự × (nửa giác · toàn giác · emoji) | **Mọi đường lưu cho cùng một kết quả** ở cùng một độ dài: cùng chấp nhận 20 ký tự (kể cả toàn giác và emoji, đếm theo ký tự không theo byte) và cùng từ chối 21 ký tự với cùng câu thông báo tiếng Nhật. Bất kỳ đường nào chặn ở mốc khác (ví dụ 10 ký tự) là bằng chứng rule validate chưa được áp dụng đồng bộ ⇒ ghi rõ đường nào. |  | Lấp `G10` · Bắt nguồn từ chênh lệch BR-12 (10 ký tự, `ajaxSaveAllStatus` — **không được sửa**) vs hành vi mới 20 ký tự · Bổ sung chiều còn thiếu của NEW-18 / NEW-26 (mới chỉ kiểm 1 đường lưu) · Đánh giá spec: Đã hỏi leader (xem §6 mục 1) · Evidence: bảng đối chiếu + truy vấn bản ghi trạng thái sau mỗi lượt |
| TC-REGSHARED001-01 | UI | REG-SHARED-001 | Hẹn gửi tin (送信予約) | Normal | auto | Tất cả | Luồng hẹn gửi tin từ màn chat vẫn chạy đủ sau khi sửa mã JavaScript của màn | Đăng nhập bot A, mở màn chat 1:1 và chọn bạn bè kiểm thử. Bot có ≥2 mẫu tin dùng được. Mở sẵn bảng điều khiển trình duyệt để bắt lỗi JavaScript | 1. Bấm icon 「送信予約」 để mở màn đặt lịch gửi của bạn bè<br>2. Thêm 2 mẫu tin vào lịch gửi, chọn ngày giờ gửi rồi lưu; ghi lại mã trạng thái<br>3. Mở lại màn đặt lịch, xác nhận dữ liệu lịch vừa lưu được tải đúng<br>4. Đổi thứ tự 2 mẫu tin rồi lưu<br>5. Đổi khoảng giãn cách giữa 2 mẫu tin rồi lưu<br>6. Xoá 1 mẫu tin khỏi lịch gửi<br>7. Huỷ toàn bộ lịch gửi<br>8. Quay lại khung hội thoại, kiểm tra dấu hiệu "đang có lịch gửi chờ" hiển thị đúng ở từng bước trên<br>9. Sau mỗi bước: ghi mã trạng thái, kiểm tra bảng điều khiển có lỗi JavaScript không và đối chiếu bản ghi lịch gửi trong dữ liệu | 2 mẫu tin · 1 mốc ngày giờ trong tương lai | Cả 7 thao tác trả mã 200 và có hiệu lực đúng trên màn lẫn trong dữ liệu; dấu hiệu "đang có lịch gửi chờ" bật/tắt đúng. **Không có lỗi JavaScript nào** trong bảng điều khiển. Không thao tác nào báo thành công mà dữ liệu không đổi, và không thao tác nào thất bại im lặng. |  | Lấp `G11` · Dẫn từ `TC-CHT-315`, `TC-CHT-318` của kho (regression) · Backend luồng này **không** nằm trong diff — TC nhằm bắt hồi quy do `chat-v2.js` bị sửa · Đánh giá spec: Spec ghi rõ (`feature-spec.md` §2 Luồng 4, BR-11) · Evidence: ảnh chụp màn đặt lịch từng bước + bảng điều khiển + truy vấn bản ghi lịch gửi |
| TC-SYNCAPP001-01 | UI | SYNC-APP-001 | API/ajax chung màn chat | Normal | manual | product | Luồng chính trên app quản trị di động không hồi quy sau khi đổi mã HTTP | Thiết bị di động thật đã cài app quản trị và đăng nhập cùng tài khoản, cùng bot với phiên web. Có hội thoại còn tin chưa đọc và một bạn bè nhận được tin. **Chạy sau khi bản sửa đã lên môi trường tương ứng** | 1. Trên app: mở danh sách hội thoại, ghi lại số huy hiệu chưa đọc<br>2. Mở một hội thoại và cuộn xem lịch sử tin<br>3. Thực hiện xác nhận đã đọc<br>4. Gửi một tin văn bản từ app<br>5. Thử một thao tác chắc chắn bị từ chối (xác nhận đã đọc lần 2 khi không còn tin chưa đọc) và quan sát app hiện gì<br>6. Sau mỗi bước: quan sát app có treo, có màn trắng, có thông báo lỗi lạ không<br>7. Mở màn chat 1:1 trên trình duyệt đối chiếu cùng hội thoại: nội dung tin, số huy hiệu, trạng thái đã đọc<br>8. Đối chiếu bản ghi trong dữ liệu | 4 thao tác lõi trên app · 1 thao tác bị từ chối | Cả 4 thao tác lõi trên app **chạy đúng như trước bản sửa**: mở được danh sách, đọc được lịch sử tin, xác nhận đã đọc làm số huy hiệu giảm đúng, gửi được tin. Kết quả khớp giữa app, màn chat trên trình duyệt và dữ liệu. Với thao tác bị từ chối: app **không treo, không màn trắng, không báo thành công giả** — nếu app không hiện được lý do thì ghi rõ để quyết định có phải cập nhật app theo mã HTTP mới hay không. |  | Lấp `Q11` · `manual` vì bắt buộc thiết bị thật chạy app quản trị · 2 file JS của app (`mobile/common.js`, `mobile/events.js`) **không nằm trong diff** ⇒ đây là vùng rủi ro cao nhất chưa ai chạm · Bổ sung chiều còn thiếu của NEW-82 (mới chỉ cover thao tác xác nhận đã đọc) · Dẫn từ `TC-CHT-430` của kho · Đánh giá spec: Spec ghi rõ (REQ-012) · Evidence: video/ảnh chụp màn hình app từng bước + ảnh chụp màn web đối chiếu |
| TC-MSG004-01 | UI | MSG-004 | Gửi tin nhắn | Normal | manual | product | Tin gửi từ màn chat đến đúng trên app LINE của bạn bè — đủ 4 loại nội dung | Bot thật trên production, có một tài khoản LINE thật đang là bạn bè của bot và có thiết bị đăng nhập sẵn app LINE. Bot có sẵn 1 mẫu tin dùng được ở màn chat | 1. Từ màn chat 1:1 gửi tin văn bản cho bạn bè kiểm thử<br>2. Mở app LINE trên thiết bị thật, xác nhận tin đến, đối chiếu nội dung và tên/ảnh người gửi<br>3. Lặp lại với ảnh<br>4. Lặp lại với mẫu tin<br>5. Lặp lại với nhãn dán<br>6. Với mỗi loại, ghi lại mã trạng thái của request, thời điểm gửi và thời điểm nhận trên app<br>7. Đối chiếu khung hội thoại trên màn chat, dòng tin cuối ở danh sách bạn bè và bản ghi tin nhắn trong dữ liệu | 4 loại nội dung: văn bản · ảnh · mẫu tin · nhãn dán | Cả 4 loại đều **đến đúng trên app LINE thật** với nội dung, tên và ảnh người gửi khớp với những gì màn chat hiển thị. Request trả 200. Khớp đủ 4 nơi: app LINE của bạn bè, khung hội thoại, dòng tin cuối ở danh sách, bản ghi trong dữ liệu. |  | Lấp `Q9` · RULE-06 (output cuối) · `manual` vì phải quan sát trên thiết bị thật chạy app LINE, ở môi trường production · Đánh giá spec: Spec ghi rõ (REQ-009) · Evidence: ảnh chụp màn hình app LINE + ảnh chụp màn chat + truy vấn tin nhắn |

### Vùng thiếu KHÔNG sinh TC mới (đã có TC trong bộ gốc hoặc trong kho — chỉ cần chạy lại / chỉnh)

| Mã | Xử lý |
|---|---|
| **G1** | TC đã có đủ: **NEW-56** (text 1-1), **NEW-45** (text nhóm), **NEW-47** (mẫu tin + nhãn dán nhóm). Cả 3 đang `skip` ⇒ **chạy lại**, không viết TC mới. Riêng NEW-56 cần bổ sung bước verify trên app LINE (đã tách thành `TC-MSG004-01`). |
| **G5** | TC đã có: **NEW-53** + **NEW-87** (gộp theo §3), **NEW-89**, **NEW-90**. Cả 4 chưa có kết luận ⇒ **chạy lại** sau khi gộp. |
| **G6** | TC đã có: **NEW-81** (`fail` — đã sinh bug #1155), **NEW-80** (`skip`), **NEW-82** (chưa chạy). ⇒ fix bug #1155, rồi chạy lại NEW-80 + NEW-82. Kho có `TC-CHT-430` (smoke web + app sau phát hành) dùng làm đối chiếu. |
| **G7** | TC đã có: **NEW-78** cho `setting-add-friend-new.js` (`skip`) ⇒ **chạy lại**. Phần Dev kê sai file là issue tài liệu, đã ghi ở §4 mục 8 — không phải việc của TC. |
| **G3** | Dùng lại kho **`TC-CHT-158`** "Xoá status — có xác nhận, bạn bè đang dùng status đó về なし". Cần chỉnh: bổ sung assert **mã HTTP của request xoá** (theo REQ-001) và bước kiểm bạn bè bị reset còn tìm/lọc đúng ở nhóm 「なし」 (gối với `TC-REGRUN001-01`). |
| **G4** | Dùng lại kho **`TC-CHT-100`** "非表示にする — bạn bè bị ẩn khỏi danh sách và có message hệ thống" + **`TC-CHT-273`** "Ẩn bạn bè — có lưu message hệ thống; bỏ ẩn thì KHÔNG lưu". Cần chỉnh: bổ sung assert **số tin chưa đọc về 0 và badge cập nhật** (BR-05), và assert mã HTTP. ⚠️ Expected của nhánh "chọn đồng thời 未確認 + 非表示" **chưa chốt được** — kho `MT-08` đang chờ quyết định (§6). |
| **Q1** | Dùng lại kho **`TC-CHT-418`** "Bot miễn phí — giới hạn 1.000 tin gửi mỗi tháng" (đã ghi môi trường PRODUCTION). Cần chỉnh: bổ sung assert **mã HTTP = lỗi nghiệp vụ (400), không phải 200 kèm cờ thất bại** theo REQ-001, và assert màn chat hiện thông báo 「配信数上限に達しています」 qua hàm hiển thị lỗi mới. |
| **Q10** | TC đã có: **NEW-11** "Dữ liệu hợp lệ vẫn trả thành công và hình dạng phản hồi không đổi so với bản trước" — đang `skip` ⇒ **chạy lại**, và đổi `Mã quan điểm` từ `API-CONTRACT-001` sang `OUT-TRUTH-001` để map được coverage (§4 mục 6). Không viết TC mới. |

---

## 6. Spec update needed

| # | Nội dung cần chốt | Nguồn mâu thuẫn | Ai chốt | Chặn việc gì |
|---|---|---|---|---|
| 1 | **Giới hạn độ dài tên trạng thái hội thoại**: `spec-features/admin/chat-11/feature-spec.md` §5 **BR-12** ghi `name_status` tối đa **10 ký tự** — và trích dẫn đúng hàm `ChatController@ajaxSaveAllStatus` (**EP-30, KHÔNG nằm trong 61 method Dev sửa**). Trong khi đó EP-28 (`ajaxSaveItemStatus`, **đã sửa**) nay cho 20 ký tự và NEW-18 + NEW-26 đều `pass` ở mốc 20. ⇒ Nhiều khả năng đây **không phải "khác nhau giữa 2 màn"** như NEW-26 kết luận, mà là **rule validate mới chỉ được áp cho 1 trong 3 đường lưu trạng thái**. | `feature-spec.md` BR-12 (EP-30) vs REQ-010 + NEW-18 / NEW-26 (EP-28) — xem **G10** và `TC-FUNC004-01` | Leader + Dev | Không có chuẩn để phán TC biên tên trạng thái; và chưa rõ BR-12 sai so với code hay code áp rule không đồng bộ. |
| 2 | **`saveSettingChat` (EP-34) có nằm trong phạm vi ticket không**: `feature-spec.md` §9.3 cảnh báo endpoint này dùng `$request->all()` update thẳng bảng `bots` (mass assignment), trong khi ticket yêu cầu "validate required đầu vào" cho **mọi** api/ajax chat11 — nhưng endpoint này **không** có trong danh sách 61 method Dev kê. | Yêu cầu ticket vs mục 3 `03-dev-impact.md` vs `feature-spec.md` §9.3 | Dev + Leader | Chặn expected của **G9** / `TC-SEC001-01`: chưa rõ nên kỳ vọng "bỏ qua field lạ" hay "ticket này không đụng tới". |
| 3 | **Ẩn bạn bè có tự động xác nhận tin chưa đọc không, và khi người dùng chọn đồng thời 「未確認」 + 「非表示」 thì ưu tiên lựa chọn nào** — kho `MT-08` đang ⏳ CHỜ QUYẾT ĐỊNH; BR-05 có mô tả tự động xác nhận nhưng không nói gì về tổ hợp mâu thuẫn. | `kho-tcs/fa001-chat11-11チャット.md` `MT-08` vs `feature-spec.md` BR-05 | Leader | Chặn expected của **G4** (không thể chốt badge về 0 hay giữ nguyên). |
| 4 | **Ngưỡng dung lượng và định dạng file khi gửi media ở chat 1:1** — spec không ghi; kho `MT-10` nhắc các mốc 10 MB (ảnh) / 200 MB (video) / 25 MB (âm thanh) nhưng chưa ai xác nhận. Kho `MT-11` còn ghi 2 chỗ upload ảnh trong cùng màn chat có quy tắc định dạng khác nhau. | `kho-tcs` `MT-10`, `MT-11` | Leader + Dev | Chặn expected của **Q5** / `TC-MEDIA001-01`, `TC-MEDIA001-02` — hiện chỉ ghi nhận được ngưỡng thực tế, không phán đúng/sai. |
| 5 | **Quy tắc nghiệp vụ cho hội thoại NHÓM** — kho `MT-14` ghi "spec không có quy tắc nào cho hội thoại nhóm", trong khi chính ticket này nới 17 rule `line_user_id`/`lineId`/`line_id`/`to_user` từ `integer` sang `string` để hỗ trợ nhóm (`olioa_group_<id>`). | `kho-tcs` `MT-14` vs journal #136625 mục (3)(4) | Leader + Dev | Không có chuẩn để phán expected cho NEW-45/47/50/51/52/53 (nhóm) — cả 4 TC nhóm quan trọng đang `skip`. |
| 6 | **Hành vi khi hồ sơ người gửi bị xoá nhưng tin nhắn cũ còn tham chiếu** — BR-14 mô tả `messages_v2s.profile_send` trỏ tới `bots_profiles` nhưng không nói tin cũ hiển thị gì sau khi hồ sơ bị xoá. | `feature-spec.md` BR-14 | Dev | Chặn expected của **Q8** / `TC-DATAREF001-01` (giữ giá trị cũ hay rơi về hồ sơ mặc định). |
| 7 | **Hành vi khi một lô gắn/gỡ nhãn chứa nhãn không thuộc bot** — fix dùng `continue` (bỏ qua phần tử lạ, xử lý tiếp phần còn lại) nhưng không có spec nào nói nên bỏ qua hay từ chối cả lô. | journal #136589 mục 2 TẦNG 2 (i) | Leader + Dev | Chặn expected của **G2** / `TC-DATACOUNT001-01`. |
| 8 | **Hành vi chống gửi trùng khi thao tác bị lỗi sau khi đã ghi dữ liệu** — spec không có mô tả nào về idempotency cho các endpoint gửi tin / đổi trạng thái hàng loạt, trong khi bản fix làm lỗi trở nên **hiển thị** nên người dùng sẽ bấm lại. | Không có nguồn spec | Leader + Dev | Chặn expected của **Q2** / `TC-CONC001-01`, `TC-CONC001-02`. |
