# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | (1) MCP LME TEST STUDIO — task `#289`, ticket `40222`, round 1, branch `ai_small_40222` |
| Tổng số TC review | `20` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `16/18 vùng ảnh hưởng đủ TC · 1 GAP · 3 RISK` — ⚠️ **mọi kết luận "đủ TC" ở đây chỉ đủ TRÊN GIẤY**: 0/20 TC đã chạy (§4 #1), nên toàn bộ vùng `OK` thực chất chưa có bằng chứng.

> Chiều (b) `diff code` lấy từ Studio `task_get_context` → `dev_impact` + `spec_delta` (`diffAvailable=true`, 4 file / 56 dòng thêm).

| # | Chiều | Vùng ảnh hưởng | TC hiện có | Status | Vấn đề |
|---|---|---|---|---|---|
| **G1** | diff code | `public/js/my_page/my_page.js` (F5) — **1 trong 4 file của diff, nghi là code chết** | `NEW-2` (xác nhận route My Page render ra màn nào) | **RISK** | Studio `dev_impact` ghi *"API dùng CHUNG cho 3 màn (Chat 1:1, My Page FA-036, Friend Detail) ⇒ cần test cả 3 surface"*. **Leader đã xác nhận (2026-09-17): "My Page" mà Dev nói chính là màn chi tiết bạn bè** → thực chất chỉ có **2 surface**, cả 2 đều đã có TC: Chat 1:1 (`NEW-4`/`NEW-6`) và màn chi tiết bạn bè (`NEW-1`/`NEW-3`). **Không còn GAP về surface.** Phần còn lại là rủi ro khác: `my_page.js` được nạp bởi blade `basic/friendlist/mypage_v2.blade.php` — **khác** blade của màn chi tiết bạn bè (`basic/friend_detail/index.blade.php`, cũng nằm trong diff). Nếu không màn nào còn nạp `mypage_v2.blade.php` thì **1/4 file trong diff là sửa code chết** — không cần TC, nhưng cần Dev xác nhận để chốt phạm vi và cân nhắc dọn code (§6 #4). `NEW-2` là TC lo đúng việc này. |
| **G2** | diff code | `ChatController::isBlockedByFriend` (F3) — **nhánh fail-open** | Không có | **GAP** | Studio `dev_impact`: *"trả false khi thiếu botId/lineUserId"*; journal Dev: *"Không tìm thấy bản ghi hội thoại thì coi như chưa chặn, giữ nguyên hành vi cũ"*. Guard có **3 nhánh** (chặn / không chặn / không xác định được → cho qua) nhưng TC chỉ cover 2 nhánh đầu. Nhánh thứ 3 là **fail-open có chủ ý** — chưa TC nào chứng minh nó không bị kích hoạt nhầm với friend đã chặn thật (ví dụ conversation tồn tại nhưng `tb_line_user_id` lệch). `NEW-17` test thiếu tham số `rich_menu_id`, **không phải** thiếu định danh friend/bot. |
| **G3** | dev-impact (BUG) + spec | **Admin block friend** (`blocked_by=1`) — ma trận action sau block | `NEW-9`, `NEW-10` | **RISK** | 2 TC có chạm nhưng **expected đã bị làm mờ** (v2, sửa 2026-09-16): *"Không kết luận success/fail… ghi nhận actual để đối chiếu nghiệp vụ"* → chạy xong vẫn **không ai chốt được đúng/sai**. Trong khi spec + kho đã có chuẩn ngược lại: `chat-11` **BR-09** chặn gửi tin với **mọi** `is_blocked=1` (không phân biệt `blocked_by`), và kho `TC-CHT-104` ghi nhận với friend bị **bot block** thì 確認状況を変更 + タグ編集 **đã bị chặn sẵn**. Sau fix, cùng một friend bị admin block sẽ: gửi tin ✗ · sửa tag ✗ · **bắt đầu step ✓ · gán rich menu ✓** → lên lịch gửi tin cho friend mà chính hệ thống cấm gửi tin. Cần chốt nghiệp vụ (§6 #1). |
| **G4** | diff code | FE alert — nhánh **success không bị phá** | `NEW-1` (màn chi tiết bạn bè) · `NEW-4` (Chat 1:1) | **RISK** | Studio `dev_impact` yêu cầu verify *"vẫn getBasicInfo/reload đúng khi không bị chặn"*. Sau xác nhận của Leader (chỉ 2 surface), 2 TC này **đã phủ đủ cả 2 surface** — RISK còn lại chỉ là **chưa TC nào được chạy** (§4 #1), không phải thiếu TC. |
| **G5** | dev-impact (D1/F3) | Phạm vi lọc `bot_id` + `tb_line_user_id` | `NEW-14` (trục **bot**) | **RISK** | `NEW-14` chứng minh friend bị chặn ở bot A không chặn nhầm ở bot B — **chỉ trục bot**. Thiếu trục **friend**: friend A bị chặn có làm hỏng thao tác của friend B **cùng bot** không (RULE-07 kiểm `WHERE` scope trên 2 bản ghi). Xem Q3 §2. |

**Không GAP** (đã đủ TC, không ghi chi tiết): F1 `actionScenarioMyPage` · F2 `chatEditRichMenu` · F4 `chat-v2.js` · F6 `friend_detail blade` · D1–D6 (`conversation` read · `scenario_lineuser` · `scenario_step_time` · `bot_line_user.rich_menu_id` · `rich_menu_history` · `scenario.count_follow`) · T1 Chat 1:1 · T2 Scenario · T3 Rich Menu · T5 Friend Detail · `BUG` root cause (tái hiện bởi `NEW-6` + `NEW-8`).

**Không có TC orphan** — cả 20 TC đều map được về ít nhất 1 vùng ảnh hưởng.

---

## 2. Thiếu so với quan điểm test LME

**Kết luận**: duyệt 31 quan điểm có Trigger khớp task · **4 quan điểm chưa cover đủ**.

| # | Mã quan điểm | Ưu tiên | Trigger khớp vì | Tình trạng TC | Severity |
|---|---|---|---|---|---|
| **Q1** | `DEPLOY-ASSET-001` | Cao (**BẮT BUỘC** khi release sửa file JS/CSS) | Diff sửa **3 file FE**: `chat-v2.js`, `my_page.js`, `friend_detail/index.blade.php` | **GAP — 0 TC** | `[BLOCKER]` |
| **Q2** | `CONC-001` | Cao | Nút thực thi hành động quan trọng (bắt đầu step = kích hoạt loạt tin); trạng thái block có thể đổi **giữa lúc mở dialog và lúc submit** | **GAP — 0 TC** | `[MAJOR]` |
| **Q3** | `DATA-DB-001` | Cao (**BẮT BUỘC** với mọi chức năng UPDATE/DELETE; RULE-07) | Thao tác ghi `scenario_lineuser` / `bot_line_user.rich_menu_id`; guard lọc theo `bot_id` + `tb_line_user_id` | **RISK** — `NEW-14` chỉ kiểm `WHERE` scope trên trục **bot**, thiếu trục **friend cùng bot** | `[MAJOR]` |
| **Q4** | `OUT-TRUTH-001` | Cao (**BẮT BUỘC** với mọi thao tác có thông báo kết quả) | Lõi bug chính là **false success** (báo thành công khi thực tế không tác dụng) | **RISK** — `NEW-12`/`NEW-15` **tự nêu** false-success vẫn còn ở luồng `cancel` (endpoint trả `success=true` trong khi `ChatMessages::changeScenario` được cho là trả thất bại) nhưng expected **cố ý né** phần dữ liệu → chạy xong vẫn không kết luận được | `[MAJOR]` |

**Chi tiết Q1 (`[BLOCKER]`)** — rủi ro thực tế: fix **phụ thuộc hoàn toàn vào JS mới** để hiện thông báo. Admin đang mở sẵn tab Chat 1:1 từ trước lúc deploy sẽ giữ `chat-v2.js` **bản cũ** trong cache; server trả `success=false` + `msg` nhưng JS cũ **im lặng bỏ qua đúng như trước fix** → **bug gốc vẫn tái hiện y nguyên** với mọi admin chưa reload. Đây là màn admin ngồi cả ngày không tải lại. Không TC nào chạm tới vùng này.

**Đã loại khỏi phạm vi** (Trigger khớp trên chữ nhưng không tìm được bằng chứng có ảnh hưởng — không đề xuất TC):
- `DATA-BACKUP-001` — fix **không** thêm/đổi bảng DB, chỉ đọc 2 cột `conversation` sẵn có; không chạm copy bot / restore.
- `BULK-001` + `REG-SHARED-001` (nhánh thao tác hàng loạt) — lối đổi rich menu / gắn action hàng loạt (kho `TC-CHT-237`, `TC-CHT-105`) đi **endpoint khác**, không nằm trong 4 file của diff. Dev đã **tự loại yokoten** (callback LINE, action sau quét QR, action theo tag). Đây là lựa chọn phạm vi cần Leader biết (§6 #5), không phải GAP của bộ TC.
- `REG-RUN-001` (job step vẫn gửi cho friend đã chặn) — kho `TC-SCE-383`/`TC-SCE-386` đã khẳng định job **không** gửi step cho friend đang block; không phải rủi ro mới do fix.
- `DATA-COUNT-001` (số 「表示中」 màn Rich Menu) — kho đã có `TC-RM-346` đếm `bot_line_user WHERE rich_menu_id AND is_blocked=0`; fix **không** đổi cách tính, chỉ ngăn phát sinh bản ghi mới. Màn list rich menu không nằm trong diff.
- `ENV-003` / RULE-08 — task **không** chạm media/domain/job nền/loadbalance/bill tiền, nên việc 0 TC chạy production **không** bị tính là vi phạm.

---

## 3. TC trùng lặp nội dung

**Đã rà toàn bộ 20 TC theo 4 yếu tố (mã quan điểm × loại case × đối tượng+thao tác × tiền đề tương đương) — không phát hiện cặp trùng cần xóa hoặc gộp.**

| Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| Chặn step: UI vs API | `NEW-6` | — (giữ `NEW-13`) | Không trùng | Trùng 3/4 yếu tố nhưng **khác lối vào** (UI có JS/dialog; API chứng minh guard ở tầng server không bypass được). Cả 2 đều cần. | — |
| Chặn rich menu: UI vs API | `NEW-8` | — (giữ `NEW-16`) | Không trùng | Như trên. | — |
| Biên "cho qua": UI vs API | `NEW-11`, `NEW-12` | — (giữ `NEW-18`, `NEW-15`) | Không trùng | UI có thêm bước confirm dialog trình duyệt; API kiểm trực tiếp giá trị biên (`rich_menu_id=0`, `action=cancel`). | — |
| Gán rich menu cho friend KHÔNG bị chặn | `NEW-5` (staging, mắt người trên LINE) | — (giữ `NEW-10` lượt 2) | Chồng lấn **có chủ ý** | `NEW-10` note tự khai: *"Phần kiểm tra 'không bị chặn nhầm' chạy được ở local đã gộp vào lượt 2"*. Lượt 2 là đối chứng **trong cùng TC**, không tách rời được; `NEW-5` phủ thêm output cuối trên LINE app (RULE-06). | — |

⚠️ **Không đề nghị xóa TC nào** — mọi TC trong bộ đều là TC duy nhất cover ít nhất một lối vào hoặc một chiều kiểm chứng.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | **Toàn bộ 20 TC** | **0/20 TC đã chạy** — pass 0% (`pass 0 / fail 0 / error 0 / untested 20`). `task_get_report` cho thấy auto-run `#1544` (env `local`) ở trạng thái `running` từ `2026-09-16 08:09` và **chưa trả kết quả nào**. Bộ TC đang ở `reviewState = leader` nhưng chưa có bằng chứng thực thi nào để review kết quả. | Chạy hết bộ TC (tối thiểu nhóm `local-only` = 16 TC) trước khi đưa Leader duyệt. Kiểm tra vì sao run `#1544` treo. |
| 2 | `[MAJOR]` | 9 TC: `NEW-2`, `NEW-6`, `NEW-7`, `NEW-8`, `NEW-9`, `NEW-10`, `NEW-13`, `NEW-16`, `NEW-17` | **7/13 mã quan điểm không tồn tại trong `framework/checklist-lme.md`**: `TOOL-SCOPE-001`, `TOOL-KNOW-002`, `TOOL-OLDREC-001`, `TOOL-NEGCTRL-001`, `TOOL-VAL2-001`, `TOOL-ERRHYG-001`, `API-CONTRACT-001`. Không map được coverage tự động về bộ 80 quan điểm LME. *(Review này đã map thủ công theo **nội dung** TC nên §1/§2 vẫn phản ánh đúng thực chất — nhưng lần sau sẽ không tự động hoá được.)* | Gán lại mã chuẩn trên Studio (`testcase_update`), hoặc bổ sung các mã `TOOL-*` vào `framework/checklist-lme.md` nếu đây là bộ mã nội bộ được chấp nhận chính thức. |
| 3 | `[MAJOR]` | **20/20 TC** | Cột `Trạng thái đánh giá spec` (`spec_status`) **trống toàn bộ**. Task này có ít nhất 4 điểm spec chưa chốt (§6) → không phân biệt được TC nào dựa trên spec ghi rõ, TC nào đang tự suy diễn. | Điền `Spec ghi rõ` / `Spec không ghi` / `Đã hỏi leader` cho từng TC; TC nào `Spec không ghi` phải ghi rõ đã hỏi ai. |
| 4 | `[MAJOR]` | `NEW-9`, `NEW-10` | **Expected không có oracle quyết định**: *"Không kết luận success/fail dựa trên việc thay đổi dữ liệu khi spec chưa xác nhận"*. TC chạy xong vẫn không cho ra Đạt/Không đạt cho phần quan trọng nhất (admin block có được thao tác không). | Chốt nghiệp vụ §6 #1 **trước**, rồi sửa expected thành khẳng định kiểm chứng được. |
| 5 | `[MAJOR]` | `NEW-12`, `NEW-15` | Tương tự #4 cho luồng **dừng step**: oracle chính (không hiện message chặn) thì rõ, nhưng phần *"thao tác dừng có thực sự ghi dữ liệu không"* bị đẩy sang **"tester GHI NHẬN, không dùng làm căn cứ đạt/không đạt"**. Đây chính là chỗ nghi có **false-success thứ hai** — tức có thể là bug chưa được raise. | Chốt §6 #2, rồi đặt expected khẳng định. Nếu xác nhận là lỗi → raise ticket riêng, không để trôi trong ghi chú TC. |
| 6 | `[MAJOR]` | `03-dev-impact.md` | File có `Auto-filled: 2026-09-16 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" CHƯA tick** → F/D/T có thể thiếu hoặc map sai mà chưa ai đối chiếu lại Redmine. | Tester đọc lại journal #133130 trên Redmine, đối chiếu đủ 4 mục rồi tick. |
| 7 | `[MINOR]` | `03-dev-impact.md` mục 4.1 | Dev ghi mục 4.1 là **"File thay đổi"** (4 file) chứ không phải list function bị ảnh hưởng theo template → phải suy ngược từ mục 3 mới gán được tag `F*`. | Yêu cầu Dev kê đúng mục 4.1 theo template ở lần sau. |
| 8 | `[MINOR]` | `NEW-17` | Oracle yếu — điểm quyết định chỉ là *"không có 5xx"*, phần còn lại là ghi nhận. Chấp nhận được vì đang thăm dò hành vi, nhưng không nên để nguyên sau khi §6 #3 chốt. | Sau khi chốt §6 #3 → đổi thành expected khẳng định. |
| 9 | `[NIT]` | `NEW-3`, `NEW-6` | Ghi nhận UX (đã nằm trong note TC): hộp thoại chọn kịch bản **đóng trước** khi alert lỗi hiện ra → lựa chọn vừa nhập bị mất, admin phải thao tác lại từ đầu. | Leader quyết định có mở ticket cải thiện UX riêng không. |

**Anti-patterns (AP-1…AP-6)**: rà xong, **không dính AP nào**. Cụ thể: fix không phải generic catch (AP-1/AP-4 không áp dụng) · ticket có root cause cụ thể chứ không chỉ triệu chứng (AP-2) · bộ TC có đủ nhánh regression `NEW-1`/`NEW-4`/`NEW-19`/`NEW-20` chứ không chỉ happy path (AP-3) · không có TC phủ thừa xuống layer không bị chạm (AP-5) · mục 3 của `03-dev-impact.md` có list caller đầy đủ 8 dòng (AP-6).

**Spec đã tìm được** — không flag thiếu spec: `spec-features/admin/chat-11/feature-spec.md` (BR-09 block check trước khi gửi, dòng 471) · `spec-features/admin/detail-friend/feature-spec.md` (BR-01 block side effects + luồng admin block dòng 200-210) · `spec-features/admin/rich-menu/feature-spec.md` (số 「表示中」 dòng 52-63).

---

## 5. TCs đề xuất bổ sung (8)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa001-chat11-11チャット.md` · `kho-tcs/fa004-richmenu-リッチメニュー.md` · `kho-tcs/fa009-phathanhtheobuoc-ステップ配信.md` |
| Vùng regression phát hiện từ kho | `TC-CHT-104` (ma trận action nào bị chặn sau block) · `TC-CHT-171` (gửi tin cho friend đã block bị chặn, BR-09) · `TC-CHT-271` (friend block bot — message hệ thống) · `TC-SCE-383`/`TC-SCE-386` (job step không gửi khi đang block) · `TC-SCE-327` (friend bị chặn giữa lúc scenario chạy) · `TC-RM-346` (số 「表示中」) |
| Conflict expected vs kho | **Không có conflict trực tiếp.** Nhưng `TC-CHT-104` + BR-09 cho thấy chuẩn hiện hành chặn theo `is_blocked` **không phân biệt** `blocked_by`, còn fix mới chỉ chặn `blocked_by=0` → **bất nhất nghiệp vụ**, đã đưa §6 #1 và sinh `TC-REGSHARED001-03` để phơi bày ma trận (không tự chọn bên). |
| GAP dùng lại TC kho (không viết mới) | `DATA-COUNT-001` (số 「表示中」) → dùng lại `TC-RM-346`, không viết mới · `REG-RUN-001` (job step khi block) → dùng lại `TC-SCE-386` |
| Xác nhận chống trùng | Đã đối chiếu `20` TC ở BƯỚC 0 + 3 file kho — **không TC đề xuất nào trùng**. ⚠️ **Cập nhật 2026-09-17**: 2 TC bản nháp (`TC-REGSHARED001-01/02` cũ — thao tác trên "màn nạp `my_page.js`") **đã bị rút** sau khi Leader xác nhận My Page chính là màn chi tiết bạn bè → chúng trùng `NEW-3` và `NEW-1`, nếu `/sync-review-tc` đẩy lên Studio sẽ tạo TC trùng. §5 còn **8 TC**. |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-REGSHARED001-01 | UI | REG-SHARED-001 | Chat 1:1 — ma trận thao tác sau khi bị chặn | Abnormal | manual | Tất cả | Admin chặn friend — đối chiếu thao tác nào bị chặn, thao tác nào còn chạy trên cùng một màn | - Đăng nhập quản trị, chọn bot A<br>- Friend T3 bị **admin chặn** qua nút 「ブロック」 ở màn chi tiết bạn bè (không dựng bằng sửa dữ liệu, để đúng side effect của BR-01)<br>- Bot A có ≥ 1 kịch bản bước và ≥ 1 rich menu đang bật | 1. Mở Chat 1:1 với T3<br>2. Thử gõ và gửi một tin nhắn — ghi nguyên văn thông báo<br>3. Thử sửa tag của T3 — ghi kết quả<br>4. Ở tab 「基本情報」 thử bắt đầu một kịch bản bước — ghi nguyên văn thông báo và kết quả<br>5. Ở tab 「基本情報」 thử gán một rich menu — ghi nguyên văn thông báo và kết quả<br>6. Lập bảng 4 thao tác: bị chặn / còn chạy | Friend T3 bị admin chặn (`blocked_by=1`) | Ghi nhận đầy đủ 4 thao tác. Chuẩn đối chiếu hiện hành: gửi tin **bị chặn** với thông báo 「ブロックしていますので、メッセージが送信できません。」 (BR-09, kho `TC-CHT-171`) và sửa tag **bị chặn** (kho `TC-CHT-104`). Nếu bắt đầu kịch bản + gán rich menu **vẫn chạy được** → xác nhận bất nhất, chuyển Leader/BA chốt theo §6 #1. TC này **không tự kết luận Đạt/Không đạt cho phần nghiệp vụ**, mục tiêu là lập bảng đối chứng. | | Lấp `G3` · Đánh giá spec: Spec không ghi — đã đưa §6 #1 · Evidence: ảnh 4 thao tác + bảng tổng hợp · dẫn từ `TC-CHT-104`, `TC-CHT-171` |
| TC-FUNC001-01 | UI | FUNC-001 | Chat 1:1 — chốt chặn khi không tra được trạng thái | Boundary | manual | Tất cả | Friend không có bản ghi hội thoại — thao tác bắt đầu kịch bản vẫn chạy như trước (fail-open đúng chủ ý) | - Đăng nhập quản trị, chọn bot A<br>- Chuẩn bị friend T4 **chưa từng phát sinh hội thoại** với bot A (chưa có bản ghi conversation) nhưng vẫn mở được ở màn danh sách<br>- Bot A có ≥ 1 kịch bản bước dùng được | 1. Mở Chat 1:1 (hoặc màn chi tiết bạn bè) với T4<br>2. Ở mục 「ステップ配信」 bắt đầu một kịch bản bước<br>3. Đọc thông báo hiện ra (nếu có)<br>4. Tải lại màn, đọc mục 「ステップ配信」<br>5. Kiểm tra dữ liệu kịch bản của T4 | Friend T4 không có bản ghi hội thoại | Không hiện thông báo chặn. Kịch bản bắt đầu thành công và hiển thị đúng ở mục 「ステップ配信」 — xác nhận nhánh "không tra được trạng thái ⇒ coi như chưa chặn" giữ đúng hành vi cũ, không gây hồi quy cho friend bình thường. | | Lấp `G2` · Đánh giá spec: Spec không ghi — hành vi do Dev tự khai ở journal · Evidence: ảnh màn + ảnh dữ liệu kịch bản |
| TC-FUNC001-02 | API | FUNC-001 | API đổi kịch bản — thiếu định danh friend | Abnormal | auto | Tất cả | Gọi API đổi kịch bản thiếu định danh friend khi friend đang chặn bot — không lỗi máy chủ, không ghi dữ liệu | - Đăng nhập quản trị bằng phiên hợp lệ, đã chọn bot A trong phiên<br>- Friend T5 đang ở trạng thái friend chặn bot, đang chạy kịch bản A<br>- Ghi lại trước bản ghi kịch bản của T5 | 1. Gửi yêu cầu tới endpoint đổi kịch bản với thao tác bắt đầu nhưng **không kèm định danh friend**<br>2. Đọc mã trạng thái HTTP và toàn bộ nội dung phản hồi<br>3. Lặp lại với định danh friend để rỗng<br>4. Đối chiếu bản ghi kịch bản của T5 với giá trị đã ghi ở tiền điều kiện | Lượt 1: thiếu định danh friend. Lượt 2: định danh friend rỗng | Cả 2 lượt trả HTTP 200, **không** lỗi máy chủ (không 5xx, không trang lỗi). Không phát sinh và không thay đổi bản ghi kịch bản của bất kỳ friend nào. Ghi lại nguyên văn phản hồi để Leader đối chiếu — vì guard trả "không chặn" khi thiếu định danh, cần khẳng định không có đường ghi dữ liệu nào đi lọt. | | Lấp `G2` · Đánh giá spec: Spec không ghi · Evidence: log request/response 2 lượt + ảnh dữ liệu kịch bản trước/sau |
| TC-DATADB001-01 | Data | DATA-DB-001 | Chat 1:1 — phạm vi chốt chặn theo friend | Normal | manual | Tất cả | Hai friend cùng một bot — friend bị chặn không làm hỏng thao tác của friend còn lại | - Đăng nhập quản trị, chọn bot A<br>- Friend T6 ở trạng thái friend chặn bot; friend T7 cùng bot A **không** bị chặn<br>- Cả hai chưa chạy kịch bản nào<br>- Ghi lại trước bản ghi kịch bản của cả T6 và T7 | 1. Mở Chat 1:1 với T6, bắt đầu một kịch bản bước → ghi nhận bị chặn<br>2. Chuyển sang friend T7 trong cùng phiên, cùng bot<br>3. Bắt đầu đúng kịch bản bước đó cho T7<br>4. Đọc thông báo hiện ra (nếu có) và mục 「ステップ配信」 sau khi tải lại<br>5. Đối chiếu bản ghi kịch bản của **cả hai** friend với giá trị ghi ở tiền điều kiện | T6 đã chặn bot, T7 không chặn, cùng bot A, cùng kịch bản | Thao tác cho T7 **thành công**, không hiện thông báo chặn, dữ liệu kịch bản của T7 được tạo đúng. Dữ liệu của T6 **không đổi** (vẫn không có bản ghi kịch bản). Xác nhận điều kiện lọc bám đúng từng friend, không chặn lan theo bot (RULE-07 — kiểm phạm vi trên 2 bản ghi). | | Lấp `Q3` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh 2 màn friend + ảnh dữ liệu kịch bản của cả 2 |
| TC-DEPLOYASSET001-01 | UI | DEPLOY-ASSET-001 | Deploy — cache tệp JS màn Chat 1:1 | Abnormal | manual | product | Tab admin mở TRƯỚC khi deploy — thao tác với friend đã chặn có còn im lặng như bug cũ không | - Môi trường sẽ được deploy bản fix (chuẩn bị trước thời điểm deploy)<br>- **Mở sẵn** màn Chat 1:1 trong trình duyệt và **giữ nguyên tab, không tải lại** suốt quá trình deploy<br>- Friend T8 ở trạng thái friend chặn bot<br>- Bot có ≥ 1 kịch bản bước dùng được | 1. Trước deploy: mở Chat 1:1, chọn T8, mở tab 「基本情報」, để nguyên tab<br>2. Tiến hành deploy bản fix<br>3. Quay lại **đúng tab cũ** (không F5, không đóng mở lại)<br>4. Ở mục 「ステップ配信」 bắt đầu một kịch bản bước<br>5. Quan sát có thông báo hiện ra không<br>6. Kiểm tra dữ liệu kịch bản của T8 | Tab giữ tệp JS bản trước fix; friend T8 đã chặn bot | Dữ liệu **không** bị ghi (chốt chặn ở máy chủ luôn có hiệu lực) — đây là điểm quyết định đạt/không đạt. Phần thông báo: ghi nhận có hiện 「ブロックされましたため、ステップを開始できません。」 hay **im lặng như bug cũ**. Nếu im lặng → xác nhận admin chưa tải lại trang vẫn gặp lại đúng triệu chứng ticket, cần chốt cách xử lý (§6 #5). | | Lấp `Q1` · RULE-08 (quan sát trên môi trường deploy thật) · Đánh giá spec: Spec không ghi — đã đưa §6 #5 · Evidence: video thao tác liền mạch từ trước deploy đến sau deploy |
| TC-DEPLOYASSET001-02 | UI | DEPLOY-ASSET-001 | Deploy — cache tệp JS màn Chat 1:1 | Normal | manual | product | Sau deploy + tải lại cứng — thông báo chặn hiện đúng trên cả 2 màn dùng chung API | - Đã deploy bản fix<br>- Đã tải lại cứng trình duyệt (xoá cache tệp tĩnh)<br>- Friend T9 ở trạng thái friend chặn bot | 1. Tải lại cứng trình duyệt<br>2. Trên Chat 1:1: bắt đầu kịch bản bước cho T9 → ghi thông báo<br>3. Trên màn chi tiết bạn bè của T9 (mở qua đường dẫn My Page `/basic/friendlist/my_page/{line_user_id}`): đổi kịch bản → ghi thông báo | Friend T9 đã chặn bot | Cả 2 màn đều hiện đúng 「ブロックされましたため、ステップを開始できません。」. Xác nhận sau khi trình duyệt lấy đúng tệp JS mới thì hành vi thống nhất trên mọi màn dùng chung API. | | Lấp `Q1` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh thông báo ở từng màn |
| TC-DEPLOYASSET001-03 | UI | DEPLOY-ASSET-001 | Deploy — cache tệp JS màn Chat 1:1 | Boundary | manual | product | Đường dẫn tệp JS sau deploy có đổi dấu hiệu phiên bản để trình duyệt tự lấy bản mới không | - Đã deploy bản fix<br>- Ghi lại trước đường dẫn đầy đủ của tệp JS màn Chat 1:1 (kèm tham số phiên bản nếu có) ở bản trước deploy | 1. Trước deploy: mở công cụ dành cho nhà phát triển, ghi lại đường dẫn tệp JS màn Chat 1:1 và các tiêu đề cache của nó<br>2. Sau deploy: tải lại trang **bình thường** (không tải lại cứng)<br>3. Ghi lại đường dẫn tệp JS lần này và so sánh với lần trước<br>4. Kiểm tra nội dung tệp tải về có chứa nhánh hiển thị thông báo mới không | Không nhập dữ liệu — chỉ quan sát tệp tĩnh | Nếu đường dẫn/tham số phiên bản **có đổi** sau deploy → trình duyệt tự lấy bản mới, rủi ro ở TC-DEPLOYASSET001-01 được khử. Nếu **không đổi** và tệp vẫn lấy từ cache → xác nhận cần bổ sung cơ chế đổi phiên bản tệp tĩnh khi release (§6 #5). | | Lấp `Q1` · Đánh giá spec: Spec không ghi · Evidence: ảnh tab mạng của công cụ nhà phát triển trước/sau deploy |
| TC-CONC001-01 | UI | CONC-001 | Chat 1:1 — trạng thái chặn đổi giữa chừng | Abnormal | manual | product | Friend chặn bot TRONG LÚC hộp thoại chọn kịch bản đang mở — thao tác submit sau đó bị từ chối | - Đăng nhập quản trị, chọn bot A<br>- Friend T10 **chưa** chặn bot, đang mở phòng chat với bot trên điện thoại thật<br>- Bot A có ≥ 1 kịch bản bước dùng được | 1. Mở Chat 1:1 với T10, tab 「基本情報」<br>2. Mở hộp thoại 「ステップ配信 操作」 và chọn một kịch bản bước — **dừng lại, chưa bấm nút bắt đầu**<br>3. Trên điện thoại, cho T10 chặn bot<br>4. Quay lại trình duyệt, bấm nút 「ステップを開始する」<br>5. Đọc nguyên văn thông báo hiện ra<br>6. Kiểm tra dữ liệu kịch bản và các tin bước đã lên lịch của T10 | T10 chuyển từ chưa chặn sang đã chặn giữa 2 thao tác | Thao tác **bị từ chối** với thông báo 「ブロックされましたため、ステップを開始できません。」 — chốt chặn đọc trạng thái tại thời điểm submit chứ không phải lúc mở màn. Không phát sinh bản ghi kịch bản và không phát sinh tin bước đã lên lịch cho T10. | | Lấp `Q2` · RULE-08 (race, cần LINE thật để chặn) · Đánh giá spec: Spec không ghi · Evidence: video liền mạch 2 màn (trình duyệt + điện thoại) |

**Các dòng §1/§2 KHÔNG đề xuất TC mới — kèm lý do**:
- **G4** (FE alert không phá luồng success) — sau xác nhận của Leader chỉ còn **2 surface**, `NEW-1` + `NEW-4` đã phủ đủ. Không đẻ thêm TC, chỉ cần **chạy** 2 TC sẵn có.
- **G1** (`my_page.js`) — Leader đã xác nhận My Page chính là màn chi tiết bạn bè, surface này đã có `NEW-1`/`NEW-3`. Việc còn lại là Dev xác nhận blade `mypage_v2.blade.php` còn được dùng không (§6 #4) — là câu hỏi về **phạm vi code**, không phải thiếu TC. `NEW-2` đã lo.
- **G5** (phạm vi lọc theo bot) — `NEW-14` đã cover trục bot; trục friend đã có `TC-DATADB001-01`. Không viết TC thứ ba.
- **Q4** (`OUT-TRUTH-001` — false-success ở luồng dừng step) — **cố ý không đề xuất TC mới**: `NEW-12` và `NEW-15` đã thực hiện đúng thao tác cần thiết, thứ còn thiếu là **quyết định nghiệp vụ về expected**, không phải thiếu TC. Viết thêm TC khi chưa có chuẩn đúng/sai chỉ tạo thêm TC không kết luận được. Đã đẩy §6 #2; sau khi Leader/Dev chốt thì **sửa expected của `NEW-12`/`NEW-15`** trên Studio.

---

## 6. Spec update needed

| # | Điểm cần chốt | Căn cứ | Ai chốt |
|---|---|---|---|
| 1 | **Ma trận thao tác theo loại block.** Fix chỉ chặn `blocked_by=0` (friend chủ động chặn bot). Với **admin chặn friend** (`blocked_by=1`): gửi tin **bị chặn** (BR-09 chặn theo `is_blocked` không phân biệt chiều) và sửa tag **bị chặn** (kho `TC-CHT-104`), nhưng bắt đầu kịch bản + gán rich menu **vẫn chạy** → cho phép lên lịch gửi tin cho friend mà hệ thống cấm gửi tin. Cần chốt: có chặn luôn `blocked_by=1` không, và nếu có thì **câu thông báo tiếng Nhật riêng** là gì (câu hiện tại 「ブロックされました」 ở thể bị động, không hợp trường hợp admin chủ động chặn). Chốt xong phải bổ sung vào `spec-features/admin/chat-11/feature-spec.md` một BR mô tả **ma trận action × loại block**, vì hiện BR-09 chỉ nói về gửi tin. | `chat-11/feature-spec.md:471` (BR-09) · `detail-friend/feature-spec.md:200-210,399-404` (BR-01) · kho `TC-CHT-104`, `TC-CHT-171` · journal Dev tự nêu là điểm cần BA chốt | BA + Leader |
| 2 | **Luồng dừng kịch bản cho friend đã chặn — có phải false-success thứ hai không.** Guard mới cố ý cho `action=cancel` đi qua, nhưng `ChatMessages::changeScenario` được cho là trả thất bại với **mọi** hội thoại đang bị chặn trong khi endpoint vẫn trả `success=true` → màn báo thành công mà dữ liệu có thể không đổi. Nếu đúng thì đây là **bug cùng loại với ticket gốc, chưa được raise**. | `NEW-12` + `NEW-15` (note TC tự nêu) · Studio `dev_impact` | Dev xác nhận code → Leader quyết raise ticket riêng hay không |
| 3 | **Tham số rich menu thiếu / rỗng.** Điều kiện chốt chặn dùng so sánh lỏng nên thiếu hoặc rỗng đều bị coi như `0` → đi vào **nhánh gỡ rich menu** thay vì bị chặn. Giao diện luôn gửi giá trị cụ thể nên rủi ro thực tế thấp, nhưng cần chốt có siết điều kiện không. | `NEW-17` · `ChatController.php:2620` | Dev + Leader |
| 4 | **Màn My Page cũ còn được dùng không.** `public/js/my_page/my_page.js` là 1 trong 4 file của diff, nhưng theo `NEW-2` thì route `friendlistMypage` render `basic.friend_detail.index` và có thể **không màn nào còn nạp file này**. Nếu đúng → sửa file đó là sửa code chết, cần ghi nhận để loại khỏi phạm vi test (và cân nhắc dọn code). Nếu sai → đang thiếu hẳn 1 surface chưa test. | `NEW-2` (REQ-009) · `routes/web.php:1026` · `FriendlistController.php:1762` | Dev |
| 5 | **Cách xử lý tệp JS cũ còn trong cache sau deploy.** Hiệu lực của thông báo lỗi phụ thuộc hoàn toàn vào JS mới; admin giữ tab mở từ trước deploy vẫn gặp lại đúng triệu chứng ticket. Cần chốt: có đổi dấu hiệu phiên bản tệp tĩnh khi release không, hay chấp nhận và hướng dẫn người dùng tải lại cứng. | Diff sửa 3 file FE · `DEPLOY-ASSET-001` · `TC-DEPLOYASSET001-01/03` | Dev + Leader |
