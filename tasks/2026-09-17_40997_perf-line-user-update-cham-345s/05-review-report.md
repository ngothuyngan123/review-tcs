# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | MCP LME TEST STUDIO — task #311 (ticket 40997, branch `ai_small_40997`, round 1, `reviewState=leader`) |
| Tổng số TC review | 35 |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `9/18 vùng ảnh hưởng đủ TC · 5 GAP · 4 RISK`

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | `T3 — QR Code / Landing statistics` — màn 「データ詳細」 (EP-16 tab1/2/3 · EP-37 · EP-38 · EP-39) + CSV `detail_landing_click_*.csv` | `dev-impact` | `không có` | `GAP — 0 TC`. Dev kê T3 là "chỉ hưởng lợi từ chỉ mục mới, không sửa code" nhưng theo `qr-landing` BR-27 số liệu **HÔM NAY của màn này tính realtime từ chính `detail_landing_click` + `collect_open_landings`** — xóa bạn hôm nay là đổi số ngay. Toàn bộ 35 TC chỉ đọc số ở màn danh sách QR 「ランディングページ一覧」 và tầng DB | `[BLOCKER]` |
| G2 | Luồng **GHI** 3 bảng vừa thêm chỉ mục — `LiffController:1221-1237` (tạo `detail_landing_click`), `QRCodeController@countScan` EP-64 + `LiffController:1207` (tạo `collect_open_landings`) | `diff code` | `không có` | `GAP — 0 TC`. "Quét ngang" của Dev chỉ kê nơi **ĐỌC** ("các chỗ khác đều lọc theo mã trang đích"). 3 bảng là **log ghi nóng dùng chung toàn hệ thống** (chính lời Dev); mỗi INSERT nay phải bảo trì thêm 1 chỉ mục. Bug gốc là **performance** → phải chứng minh chi phí không bị đẩy sang đường ghi | `[BLOCKER]` |
| G3 | Tầng **Spring Boot** ghi/đọc cùng 3 bảng — `HandlePostbackTask` (cập nhật `detail_landing_click.action=2`, `is_action_web`, `bot_line_user_id`) · `MappingDeviceTask` (ghi `collect_id`, đọc `collect_open_landings` trong vòng lặp `while(true)`) | `diff code` | `không có` | `GAP — 0 TC`. Danh sách caller ở mục 3 của file 03 **chỉ có PHP**. Xem thêm §6 mục 6 — chỉ mục mới `(bot_id, landing_id)` **không dùng được** cho câu truy vấn của `MappingDeviceTask` | `[BLOCKER]` |
| G4 | `collectLandingDeviceIdsOfLineUsers` — `device_id` **không distinct**, trùng giữa nhiều trang đích | `diff code` | `NEW-3` | `GAP`. Studio `dev_impact` ghi rõ phải "verify device_id (không distinct, khớp pluck cũ)". NEW-3 dựng "mỗi lượt gắn **một mã thiết bị riêng**" → chưa có ca 1 `device_id` xuất hiện ở ≥ 2 trang đích (thực tế rất phổ biến: cookie `device_scan_landing` TTL 365 ngày) | `[MAJOR]` |
| G5 | `BUG` root cause — **mục tiêu chính của ticket: thao tác xóa bạn hết chậm** | `dev-impact` | `NEW-25`, `NEW-27` | `RISK — cả 2 TC đều CHƯA CHẠY`. TC đã chạy (`NEW-22` đếm câu truy vấn, `NEW-23` kế hoạch thực thi) chỉ đo **riêng khối landing**, không đo tổng thời gian endpoint. Ticket đóng được hay không phụ thuộc đúng 2 TC này | `[BLOCKER]` |
| G6 | `F1 — removeHistoryLineUser` (nằm trong diff: `functions.php` +102 dòng) | `dev-impact` | `NEW-26` | `RISK — TC duy nhất, CHƯA CHẠY, expected không đo được` ("dữ liệu lịch sử được xử lý đúng theo behavior hiện tại") — không nêu bảng nào, giá trị nào | `[MAJOR]` |
| G7 | `D4 — landing_histories` (số liệu theo ngày) | `dev-impact` | `NEW-3` | `RISK`. NEW-3 chỉ kiểm **2 ngày QUÁ KHỨ** (2026/09/10, 09/11). Theo BR-27 đó là nhánh đọc bảng tổng hợp; nhánh **hôm nay** tính realtime là code path khác, chưa TC nào chạm | `[MAJOR]` |
| G8 | Đỉnh bộ nhớ khi xóa lô lớn — **rủi ro MỚI do chính bản fix tạo ra** (gom toàn bộ cặp trang đích/thiết bị trong 1 `get()`) | `diff code` | `NEW-13` | `RISK — TC duy nhất, exec = error, chưa raise ticket bug`. Chính NEW-13 ghi "không nằm trong danh sách vùng ảnh hưởng của dev". Xem thêm `G9` — TC này còn đang đặt sai màn | `[BLOCKER]` |
| G9 | `deleteUserBlockAction` — nhánh mảng `line_id` **lớn** (xóa hàng loạt trên màn **không phân trang**) | `diff code` | `NEW-13` (đặt ở màn có phân trang, exec = `error`) | `GAP — nhánh này chưa từng chạy`. Theo **BR-04**: 「非表示中の友だち」 và 「ブロックした友だち」 **không phân trang (get all)**, còn 「ブロックされた友だち」 = **50 dòng/trang**. 全選択 ở 2 màn không phân trang gom **toàn bộ** bạn của màn vào 1 mảng `line_id` rồi đẩy thẳng vào 2 hàm gom mới — đúng nhánh rủi ro nhất. `NEW-13` dựng 200 bạn nhưng lại đặt ở màn 50 dòng/trang nên không tick nổi 200 trong 1 thao tác | `[BLOCKER]` |

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `17 quan điểm Trigger khớp task · 9 chưa cover đủ`

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `DATA-COUNT-001` | `Cao` | `GAP — 0 TC mang mã này`. Quan điểm yêu cầu cùng 1 số liệu khớp giữa **4 nguồn**; bộ TC mới đối chiếu 2 (màn danh sách QR + tầng DB). Thiếu: màn chi tiết 「データ詳細」 · **CSV export** (BR-31, Shift_JIS). Chưa TC nào chạm `type_count = 1` (đếm **unique** `COUNT(DISTINCT line_id/device)`) vs `= 2` (đếm tất cả lượt) — **BR-28**: cùng 1 lần xóa bạn cho 2 kết quả trừ khác nhau tuỳ chế độ đếm | `[BLOCKER]` |
| Q2 | `BULK-001` | `Cao` | `GAP — 0 TC kết hợp bộ lọc + 全選択 + xóa hàng loạt`. Màn 「非表示中の友だち」 có bộ lọc 「表示期間」 (spec `SCR-FRL-04`, kho `TC-FRL-123`). `NEW-11` tick tay 2/3 không qua lọc; `#18649` dùng 全選択 nhưng không lọc trước. Đây chính là đường mảng `line_id` chảy vào 2 hàm gom mới — lọc sai là trừ oan số liệu trang đích | `[BLOCKER]` |
| Q3 | `REG-RUN-001` | `Cao` | `GAP — 0 TC`. 3 câu `ALTER TABLE` chạy trên bảng log lớn **đang có traffic ghi** và job `MappingDeviceTask` chạy `while(true)` liên tục. Dev tự cảnh báo "nên chạy ngoài giờ cao điểm hoặc bằng `pt-online-schema-change`" nhưng không TC nào kiểm hành vi trong lúc ALTER chạy. `NEW-18/19/21` chạy migration trên DB test không có traffic | `[BLOCKER]` |
| Q4 | `CONC-001` | `Cao` | `RISK — chỉ NEW-14 (Abnormal)`. Thiếu `Normal` + `Boundary`, không ghi lý do (**RULE-01**). Expected của NEW-14 còn để mở ("nếu ra 90/36 thì chạy lại nhánh trước fix rồi để leader quyết") → chưa phải oracle chốt được | `[MAJOR]` |
| Q5 | `DATA-REF-001` | `Cao` | `RISK`. Xóa bạn cascade ~40 bảng nhưng TC chỉ verify trang đích + `action_limit_tags` + `NEW-26` (chưa chạy). Kho đã có `TC-FRL-136/137/138/139` verify cascade **đầy đủ** (url rút gọn · form · event · lesson · salon · item · tag · friend info · memo · scenario · richmenu) ở PRODUCTION — Studio không có TC tương đương | `[MAJOR]` |
| Q6 | `SYNC-APP-001` | `Cao` (flow critical user-facing) | `GAP — 0 TC cho đồng bộ Elasticsearch khi xóa bạn`. Luồng xóa ghi `sync_elasticsearch`, và `removeHistoryLineUser` nằm trong diff. Không verify được thì bạn đã xóa vẫn còn tìm thấy ở ô tìm kiếm (**RULE-06** — output cuối). Kho có `TC-FRL-143` | `[MAJOR]` |
| Q7 | `COMPAT-LEGACY-001` | `Cao` | `GAP — 0 TC dùng dòng log tạo TRƯỚC các migration bổ sung cột`. Bằng chứng của chính Dev: `collect_open_landings` tạo 2025-06-19 **+ 2 migration bổ sung cột**, `detail_landing_click` từ 2020. Mọi TC đều dựng dữ liệu mới → nhánh dữ liệu đời cũ (`bot_id` / `device_id` / `collect_id` khuyết) chưa được chạm. **RULE-09** | `[MAJOR]` |
| Q8 | `DATA-MIG-001` | `Cao` | `RISK — có Normal (NEW-18/19/21) + Abnormal (NEW-20), thiếu Boundary`, không ghi lý do cho cả quan điểm (**RULE-01**; ghi chú của NEW-19 chỉ giải thích cho riêng ca chạy lại). Biên chưa chạm: `line_id` **dài hơn tiền tố 64 ký tự** của chỉ mục | `[MAJOR]` |
| Q9 | `UI-003` | `Cao` (rủi ro **false success**) | `RISK — chỉ NEW-13 (error)` chạm trạng thái lô lớn / timeout. Kho ghi bug tester **#35742** "nhấn xóa nhiều friend xong không reload được màn hình" — không TC nào verify màn tải lại được sau xóa hàng loạt | `[MAJOR]` |

> **Đã loại khỏi phạm vi** — `DATA-BACKUP-001` (Cao, Trigger "thêm/đổi bảng DB" có khớp vì migration đổi cấu trúc 3 bảng). Nguồn kiểm chứng: FA-033 chỉ copy **13 loại dữ liệu** và QR/landing **không** nằm trong danh sách (kho `TC-BK-329` / `TC-BK-333`); migration chỉ thêm chỉ mục, **không thêm bảng mới**. Không tìm được câu khẳng định nào rằng 3 bảng log này nằm trong luồng backup/copy bot → **không đề xuất TC**, tránh GAP giả.

---

## 3. TC trùng lặp nội dung

| Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| DUP-1 | `NEW-25` | `NEW-27` → **gộp** | `DUP-SUBSET` | Quan điểm: cả 2 là mã lạ nhóm đo thời gian · Loại case: `Normal` · Đối tượng + thao tác: màn 「友だち詳細」 → nút 「削除」 → đo thời gian request · Tiền đề: "môi trường có dữ liệu landing đủ lớn" · Expected: "không còn kéo dài hàng trăm giây". `NEW-25` bao trọn `NEW-27` và có thêm bước đối chiếu nhật ký máy chủ + quy định cách ghi nhận khi không dựng được dữ liệu | `[MINOR]` |

- **Gate đã chạy**: giả định gộp `NEW-27` vào `NEW-25` → coverage §1 (G5) và §2 còn nguyên (cả 2 TC cùng lấp G5; `PERF-LATENCY-001` là mã lạ, không tính cover, và vẫn còn `NEW-22`/`NEW-23` mang mã đó). `Có` — gộp an toàn.
- **Cụm gần trùng — ĐÃ RÀ, KHÔNG đề nghị xóa**: `#18645` (màn 「ブロックされた友だち」) · `#18647` (màn 「ブロックした友だち」) · `#18648` (màn 「非表示中の友だち」) trùng 3/4 yếu tố (cùng `Normal`, cùng thao tác "bấm 削除 trên dòng → `action_limit_tags` = 0", tiền đề tương đương). **Giữ cả 3**: 3 màn dùng 3 file JS khác nhau (`user_block.js:214` · `block.js:116` · `hiden.js:101`) nên là 3 lối vào FE riêng; mỗi TC là TC **duy nhất** cover lối vào của màn đó.
- `DUP-INFLATE`: không có.
- `DUP-CONFLICT`: không có giữa các TC; xung đột expected vs kho-tcs xem §6 mục 3.
- Xóa/gộp thật do human thực hiện trên Studio (`testcase_update` / `testcase_delete`).

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | `NEW-13` | Exec = `error`, `bug_tickets` **rỗng** — TC lỗi mà chưa raise ticket bug. Đây lại đúng là TC duy nhất cover rủi ro mới do bản fix tạo ra (đỉnh bộ nhớ lô lớn) | Raise ticket bug cho `NEW-13` hoặc ghi rõ `error` là lỗi môi trường kèm bằng chứng, rồi chạy lại |
| I2 | `[MAJOR]` | Toàn bộ 35 TC | **RULE-08 / ENV-003** — 31/35 TC chạy ở env `local`, `staging`/`dev`/`prd` = **0 lần chạy**. Task là bug **performance đo trên production** (`step.lme.jp`) + **ALTER TABLE hạ tầng DB** + chạm **job nền**. Chính Dev ghi "không EXPLAIN được trên dữ liệu thật... mức cải thiện thực tế cần đo lại" | Chạy tối thiểu `NEW-25` · `NEW-23` · `NEW-13` trên môi trường có khối lượng log thật; không kết luận "hết chậm" từ `local` |
| I3 | `[MAJOR]` | Toàn bộ 30 TC `pass` | **RULE-02** — cột `Evidence thực tế` rỗng 100%. 30 TC được tick Đạt mà không có bằng chứng đính kèm; riêng `DATA-DB-001` / `DATA-MIG-001` / `BULK-001` bắt buộc evidence là ảnh chụp kết quả query trước/sau kèm câu query | Bổ sung evidence cho ít nhất nhóm TC đối chiếu số liệu (`NEW-1`, `NEW-2`, `NEW-10`, `NEW-11`, `NEW-15`, `NEW-18`) trước khi Leader ký |
| I4 | `[MAJOR]` | `03-dev-impact.md` | Có `Auto-filled: 2026-09-17 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" CHƯA tick** — F/D/T có thể thiếu hoặc map sai; toàn bộ chiều `dev-impact` của §1 đang dựa trên bản auto-fill chưa ai đọc lại | Tester đọc lại Journal #136862 trên Redmine, đối chiếu 4 mục rồi tick checkbox |
| I5 | `[MAJOR]` | 20/35 TC | **12 mã quan điểm không có trong `framework/checklist-lme.md`**: `PERF-LATENCY-001`(3) · `SELECT-SCOPE-001`(3) · `RULE-TOOL-029`(2) · `TOOL-NEGCTRL-001`(2) · `TOOL-OLDREC-001`(2) · `TOOL-AXIS-001` · `TOOL-KNOW-002` · `DATA-HIST-001` · `TOOL-PAIRWISE-001` · `TOOL-SCOPE-001` · `TOOL-ERRHYG-001` · `API-001`. Hơn nửa bộ TC **không map được coverage** theo bộ quan điểm chuẩn | Map lại về mã chuẩn (vd `SELECT-SCOPE-001` → `BULK-001`, `PERF-LATENCY-001` → `PERF-LARGE-001`, `API-001` → `FUNC-002`), hoặc Leader bổ sung mã mới theo **RULE-10** |
| I6 | `[MAJOR]` | `[AP-2]` `01-bug-task.md` | **Symptom-only** — ticket do AI detect tự sinh, chỉ có hiện tượng (345s), không có error message/steps. Dev fix **1 root cause duy nhất** (khối dọn số liệu trang đích) trong khi cùng endpoint đã từng chậm vì nguyên nhân khác (**#38390** `url_shorten`, **#40708**). `NEW-22` chỉ đếm câu truy vấn của **riêng khối landing** | Hỏi Dev còn nút cổ chai nào khác trong cascade ~40 bảng không; TC phải đo **tổng thời gian endpoint**, không chỉ 1 khối |
| I7 | `[MAJOR]` | `NEW-13` | **Không dựng lại được như viết** — TC yêu cầu "tick chọn toàn bộ **200 bạn**" trên màn 「ブロックされた友だち」, nhưng spec friend-list **BR-04** ghi màn này phân trang **50 items/page**. Đây nhiều khả năng là nguyên nhân `error` | Viết lại steps: nêu rõ cách chọn qua 4 trang, hoặc đổi sang màn không phân trang (「非表示中の友だち」 / 「ブロックした友だち」 — BR-04: không phân trang) |
| I8 | `[MAJOR]` | `NEW-26` | Expected **không đo lường được**: "dữ liệu lịch sử được xử lý đúng theo behavior hiện tại của hệ thống, không phát sinh lỗi hoặc dữ liệu mồ côi" — không nêu bảng nào, cột nào, giá trị bao nhiêu. Tiền đề cũng chỉ ghi "có dữ liệu lịch sử liên quan" | Liệt kê đích danh bảng/màn cần kiểm sau khi xóa (lịch sử tag · lịch sử richmenu · lịch sử scenario) kèm số dòng trước/sau |
| I9 | `[MAJOR]` | `NEW-28` | Expected mơ hồ ("API trả lỗi phù hợp") **và không atomic** — 1 TC gộp 2 mục đích khác nhau (token không hợp lệ · thiếu/sai định danh bạn bè) với 2 expected khác nhau | Tách 2 TC; ghi rõ mã trạng thái + nội dung phản hồi mong đợi cho từng ca |
| I10 | `[MINOR]` | `NEW-27` | Không có mốc đo — "không còn kéo dài hàng trăm giây" trong khi `NEW-25` có mốc tham chiếu 345s | Gộp vào `NEW-25` theo §3 DUP-1 |
| I11 | `[MINOR]` | `NEW-22`, `NEW-23`, `requirement_keys` REQ-001/003/007 | **Mã endpoint lệch spec**: cột `screen` ghi "EP-17 POST /basic/line_user/update" nhưng spec friend-list `EP-17` = `POST /basic/friendlist/send-action` (Bulk action); `source_refs` dùng `friend-list/EP-25` · `EP-26` là 2 endpoint **import CSV**. Endpoint xóa 1 bạn đúng là **`EP-14` POST /ajax/friendlist/delete-user-block** | Sửa mã endpoint trên Studio để tester tra ngược được về spec |
| I12 | `[MINOR]` | `#18649`, `#18650` | **Lệch mã màn còn tồn từ kho** (chính TC tự ghi ⚠): Studio gắn `SCR-FL-06` / `SCR-FL-04` trong khi spec friend-list ghi 「非表示中の友だち」 = `SCR-FRL-04`, 「ブロックした友だち」 = `SCR-FRL-06` | Đưa §6 — Leader chốt bộ mã rồi sửa đồng loạt |
| I13 | `[MINOR]` | `NEW-6`, `NEW-16` | Dẫn chiếu "case kho **FL-019**" để loại phân quyền khỏi phạm vi, nhưng `kho-tcs/fa013-friendlist-友だちリスト.md` đánh ID dạng `TC-FRL-nnn` — **không tra được `FL-019`** | Sửa thành ID kho thật, hoặc ghi rõ đây là ID nội bộ Studio |
| I14 | `[MINOR]` | `03-dev-impact.md` mục 4.3 | Dev ghi "**Landing Page (FA-011)**" — `FA-011` là 「フォーム作成」. Màn đúng là **`FA-017` QR Code Action** 「QRコードアクション」 (`/basic/landing`) | Sửa mã feature trong đánh giá ảnh hưởng |

---

## 5. TCs đề xuất bổ sung (17)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa013-friendlist-友だちリスト.md` (FA-013 — tính năng chính của task). **`kho-tcs` chưa có `FA-017` QR Code Action** → phần số liệu trang đích không đối chiếu được với kho |
| Vùng regression phát hiện từ kho | `TC-FRL-136/137/138/139` (xóa bạn & cascade đầy đủ, PRODUCTION) · `TC-FRL-142` (`PERF-LARGE-001`, bug #38390 cùng endpoint) · `TC-FRL-143` (`SYNC-APP-001`, đồng bộ Elasticsearch khi xóa bạn) · `TC-FRL-123`/`128`/`131` (khung 3 màn trạng thái + bộ lọc 「表示期間」) |
| Conflict expected vs kho | `Không` có conflict giữa TC đề xuất và kho. Riêng **MT-13** của kho (`sync_elasticsearch.type` khi xóa bạn = `2` theo TC cũ vs `11` `TYPE_DELETE_LINE_USER` theo spec §7.2) là xung đột **có sẵn trong kho**, chạm đúng Q6 → đã đưa §6 mục 3 |
| GAP dùng lại TC kho (không viết mới) | `G5 → TC-FRL-142` "Xoá friend có RẤT NHIỀU dữ liệu không bị treo màn (Bug #38390)" — chỉnh: đổi trọng tâm dữ liệu từ `url_shorten_detail` sang lượt bấm trang đích, và ghi lại con số thời gian để đối chiếu mốc 345s · `G6 + Q5 → TC-FRL-136` / `TC-FRL-137` / `TC-FRL-139` — chỉnh: chèn thêm bước đọc 4 số liệu trang đích trước/sau · `Q5 (phạm vi chọn) → TC-FRL-138` "Tích chọn toàn bộ rồi chỉ xoá 1 friend" — pattern không có ở Studio, dùng nguyên · `Q6 → TC-FRL-143` — dùng nguyên, ghi lại giá trị `type` thật để chốt MT-13 · `Q9 → TC-FRL-136` (đã có vế "màn hidden RELOAD được ngay sau khi xoá", nguồn bug #35742) |
| Xác nhận chống trùng | Đã đối chiếu **35** TC ở BƯỚC 0 + `kho-tcs/fa013` — **không TC đề xuất nào trùng** |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-DATACOUNT001-01 | UI | DATA-COUNT-001 | Màn 「データ詳細」 của QR action | Normal | manual | Tất cả | Xóa bạn hôm nay — số liệu HÔM NAY trên màn データ詳細 giảm ngay theo | - Đăng nhập quản trị bot B1, đã chọn bot B1<br>- Trang đích 「LP_RV_D1」 có số liệu của **hôm nay**: 12 lượt đọc URL, 5 lượt kết bạn, 4 lượt chạy action<br>- Bạn D1 có 4 lượt bấm ở trang đích này **đều phát sinh trong hôm nay** (2 thao tác web, 1 lượt kết bạn, 1 chỉ truy cập)<br>- Bạn D2 (đối chứng) có 3 lượt bấm hôm nay ở cùng trang đích | 1. Mở màn danh sách QR action → dòng 「LP_RV_D1」 → bấm 「データ詳細」<br>2. Ở tab 1, chọn khoảng ngày chứa **hôm nay**; ghi lại số của cột ngày hôm nay và số tổng trên box-count<br>3. Mở màn danh sách bạn bè → màn chi tiết bạn D1 → bấm 「削除」 → xác nhận<br>4. Quay lại màn 「データ詳細」, tải lại trang và đọc lại đúng các số ở bước 2<br>5. Chọn thêm 1 ngày quá khứ bất kỳ và đọc số của ngày đó | Bạn D1: 4 lượt bấm phát sinh trong hôm nay. Bạn D2 đối chứng: 3 lượt bấm hôm nay | - Số của **ngày hôm nay** giảm đúng 4 lượt đọc URL và 1 lượt kết bạn — khớp phép đếm tay, không phải chờ job chạy đêm<br>- Số tổng trên box-count giảm tương ứng<br>- Số của **ngày quá khứ** giữ nguyên không đổi<br>- Phần lượt bấm của bạn D2 vẫn còn đủ 3 |  | Lấp `G1` · `Q1` · Đánh giá spec: Spec ghi rõ (qr-landing BR-27 — hôm nay tính realtime từ `detail_landing_click` + `collect_open_landings`) · Evidence: screenshot màn データ詳細 trước/sau + phép tính tay |
| TC-DATACOUNT001-02 | UI | DATA-COUNT-001 | Màn 「データ詳細」 của QR action | Normal | manual | Tất cả | Xóa bạn — file CSV tải từ màn データ詳細 không còn dòng của bạn đã xóa | - Như TC-DATACOUNT001-01<br>- Máy test mở được file CSV mã hóa Shift_JIS | 1. Mở màn 「データ詳細」 của trang đích 「LP_RV_D1」<br>2. Bấm nút tải CSV; mở file và đếm số dòng của bạn D1 và của bạn D2 đối chứng<br>3. Xóa bạn D1 từ màn chi tiết bạn bè<br>4. Quay lại màn 「データ詳細」, tải CSV lần 2<br>5. Mở file lần 2: đếm lại số dòng của D1 và D2, đối chiếu tổng số dòng với lần 1 | Không nhập gì — chỉ tải và đối chiếu 2 file CSV | - File CSV lần 2 **không còn dòng nào** của bạn D1<br>- Các dòng của bạn D2 còn **nguyên vẹn**, nội dung từng cột không đổi<br>- Tổng số dòng giảm đúng bằng số dòng của D1<br>- File mở được bằng Excel, tiếng Nhật không bị lỗi font (Shift_JIS), tên file theo dạng `detail_landing_click_<thời điểm>.csv` |  | Lấp `Q1` (nguồn thứ 3 của DATA-COUNT-001) · Đánh giá spec: Spec ghi rõ (qr-landing BR-31) · Evidence: 2 file CSV trước/sau |
| TC-DATACOUNT001-03 | UI | DATA-COUNT-001 | Màn 「データ詳細」 của QR action | Boundary | manual | Tất cả | Xóa bạn có NHIỀU lượt bấm — số ở chế độ đếm unique và chế độ đếm tất cả lượt đều đúng | - Đăng nhập quản trị bot B1<br>- Trang đích 「LP_RV_D2」 có số liệu hôm nay: 20 lượt đọc URL (tính tất cả lượt) từ 8 người khác nhau<br>- Bạn D3 **một mình** có 6 lượt bấm ở trang đích này trong hôm nay (cùng 1 định danh LINE, 1 mã thiết bị)<br>- Ghi lại số ở CẢ HAI chế độ đếm trước khi xóa | 1. Mở màn 「データ詳細」 của 「LP_RV_D2」<br>2. Đọc số ở chế độ **đếm số người** (unique) và ghi lại<br>3. Chuyển sang chế độ **đếm tất cả lượt** và ghi lại<br>4. Xóa bạn D3 từ màn chi tiết bạn bè<br>5. Quay lại màn 「データ詳細」, đọc lại số ở **cả hai** chế độ đếm | Bạn D3: 6 lượt bấm nhưng chỉ là **1 người** | - Chế độ **đếm số người**: giảm đúng **1** (từ 8 → 7)<br>- Chế độ **đếm tất cả lượt**: giảm đúng **6** (từ 20 → 14)<br>- Hai chế độ không lẫn số của nhau; không chế độ nào giảm 6 khi lẽ ra phải giảm 1 và ngược lại |  | Lấp `Q1` · regression · Đánh giá spec: Spec ghi rõ (qr-landing BR-28 — `type_count`=1 đếm unique, =2 đếm tất cả lượt) · Evidence: screenshot 2 chế độ trước/sau + phép tính tay |
| TC-REGSHARED001-01 | UI | REG-SHARED-001 | Ghi log khi khách quét QR / mở trang đích | Normal | manual | product | Sau khi thêm 3 chỉ mục — khách quét QR vẫn ghi log đủ và không chậm đi | - Đã chạy migration thêm 3 chỉ mục trên môi trường có khối lượng bảng log thật<br>- Bot B1 có trang đích 「LP_RV_W1」 đang bật, có cấu hình action khi mở URL<br>- Có điện thoại thật chưa từng quét trang đích này (cookie thiết bị sạch)<br>- Ghi lại số dòng hiện có của 3 bảng log và thời gian phản hồi trung bình của đường mở trang đích **trước** khi chạy migration | 1. Trước migration: quét QR bằng điện thoại thật, mở trang đích, kết bạn; ghi lại thời gian phản hồi và số dòng log mới sinh ở 3 bảng<br>2. Chạy migration thêm 3 chỉ mục<br>3. Lặp lại đúng thao tác ở bước 1 bằng 1 thiết bị sạch khác<br>4. Đối chiếu: số dòng sinh ra ở từng bảng, đủ cột hay không, thời gian phản hồi<br>5. Lặp bước 3 thêm 5 lần liên tiếp để lấy thời gian trung bình | 2 thiết bị thật, cookie sạch; quét cùng 1 trang đích trước và sau migration | - Cả 2 lần đều sinh đủ log: 1 dòng lượt mở trang đích, 1 dòng lượt bấm, số liệu trang đích tăng đúng<br>- Các cột của dòng log sau migration đầy đủ y như trước migration<br>- Thời gian phản hồi của đường mở trang đích **không tăng đáng kể** so với trước migration — ghi lại con số cụ thể để Leader chốt ngưỡng<br>- Action cấu hình trên trang đích vẫn chạy và tin nhắn vẫn tới LINE |  | Lấp `G2` · **RULE-08** (performance trên bảng ghi nóng) · **RULE-06** (verify tới tin nhắn trên LINE) · Đánh giá spec: Spec ghi rõ (qr-landing LUỒNG 2) · Evidence: bảng đối chiếu thời gian trước/sau + ảnh chụp dòng log |
| TC-REGSHARED001-02 | API | REG-SHARED-001 | Job ghép mã thiết bị của trang đích | Normal | manual | product | Sau khi thêm chỉ mục — job ghép lượt mở trang đích với lượt bấm vẫn chạy đúng | - Môi trường có job nền đang chạy<br>- Đã chạy migration thêm 3 chỉ mục<br>- Chuẩn bị ≥ 10 lượt quét trang đích mới (chưa được job ghép) bằng thiết bị thật<br>- Ghi lại số bản ghi đang chờ ghép trước khi quan sát | 1. Ghi lại số lượt bấm đang ở trạng thái **chờ ghép** và số đã ghép thành công<br>2. Tạo thêm 10 lượt quét trang đích mới bằng thiết bị thật<br>3. Chờ job nền chạy; ghi lại thời điểm bắt đầu và kết thúc<br>4. Đếm lại số bản ghi đã ghép, số vẫn chờ ghép và số đánh dấu không ghép được<br>5. Mở màn 「データ詳細」 tab thống kê theo thiết bị và kiểm tra dữ liệu hiển thị<br>6. Đọc nhật ký của job xem có lỗi hay cảnh báo chậm không | 10 lượt quét mới từ thiết bị thật | - Job vẫn ghép được các bản ghi mới, số chờ ghép giảm về đúng như quy luật trước khi thêm chỉ mục<br>- Tab thống kê theo thiết bị hiển thị đủ dữ liệu, không trống bất thường<br>- Nhật ký job **không** có lỗi mới và **không** chậm hơn rõ rệt so với trước migration<br>- Không bản ghi nào bị đánh dấu không ghép được một cách bất thường |  | Lấp `G3` · **RULE-08** (job nền) · Đánh giá spec: Spec ghi rõ (qr-landing §6.4 — `MappingDeviceTask` đọc `collect_open_landings`, ghi `collect_id`) · Evidence: số bản ghi trước/sau + trích nhật ký job |
| TC-DATADB001-01 | Data | DATA-DB-001 | Xóa bạn bè — dọn lượt mở trang đích | Abnormal | auto | Tất cả | Xóa bạn dùng CHUNG một mã thiết bị ở nhiều trang đích — dọn đúng, không xóa thừa | - Đăng nhập quản trị bot B1<br>- Bot B1 có 3 trang đích 「LP_RV_V1」, 「LP_RV_V2」, 「LP_RV_V3」<br>- Bạn V1 có lượt bấm ở cả 3 trang đích nhưng **dùng CHUNG đúng 1 mã thiết bị** cho cả 3 (mô phỏng cùng 1 điện thoại quét nhiều QR — cookie thiết bị giữ 365 ngày)<br>- Mỗi trang đích có thêm bản ghi lượt mở trang đích của mã thiết bị đó<br>- Bạn V2 (đối chứng) cũng có lượt mở ở 「LP_RV_V3」 nhưng bằng mã thiết bị KHÁC | 1. Ghi lại ở tầng dữ liệu: số bản ghi lượt mở trang đích theo mã thiết bị chung, ở từng trang đích<br>2. Ghi lại bản ghi của bạn V2<br>3. Mở màn chi tiết bạn bè của V1 → bấm 「削除」 → xác nhận<br>4. Đếm lại bản ghi lượt mở trang đích của mã thiết bị chung ở cả 3 trang đích<br>5. Đếm lại bản ghi của bạn V2<br>6. Kiểm tra số liệu của cả 3 trang đích | Bạn V1: 1 mã thiết bị dùng chung cho 3 trang đích; bạn V2: mã thiết bị riêng | - Thao tác xóa hoàn tất không lỗi<br>- Bản ghi lượt mở trang đích của mã thiết bị chung được dọn **đủ ở cả 3 trang đích**, không sót trang nào<br>- Không bản ghi nào bị xóa **2 lần** hoặc gây lỗi trùng<br>- Bản ghi của bạn V2 còn nguyên<br>- Số liệu 3 trang đích trừ đúng phần của V1, không trang nào nhận nhầm phần của trang khác |  | Lấp `G4` · Đánh giá spec: Spec ghi rõ (Studio `dev_impact` — "verify device_id không distinct, khớp pluck cũ") · Evidence: kết quả đếm trước/sau kèm câu truy vấn |
| TC-BULK001-01 | UI | BULK-001 | Xóa hàng loạt sau khi lọc | Normal | manual | Tất cả | Lọc 「表示期間」 thu hẹp rồi 全選択 → chỉ bạn trong tập đã lọc bị xóa | - Đăng nhập quản trị bot B1<br>- Màn 「非表示中の友だち」 có 10 bạn đang ẩn, ngày ẩn trải trên 3 khoảng thời gian khác nhau<br>- Chọn bộ lọc sao cho chỉ còn **đúng 3 bạn** (E1, E2, E3), cả 3 đều có lượt bấm ở trang đích 「LP_RV_B1」<br>- 7 bạn nằm ngoài khoảng lọc cũng có lượt bấm ở **chính** trang đích đó<br>- Ghi lại số liệu 「LP_RV_B1」 và số lượt bấm của từng nhóm | 1. Mở màn 「非表示中の友だち」, đọc tổng số bạn đang hiển thị<br>2. Nhập bộ lọc 「表示期間」 để danh sách còn đúng 3 bạn; đếm lại số dòng trên màn<br>3. Tích 「全選択」; xác nhận số hiển thị trên panel đúng bằng 3<br>4. Bấm 「削除」 ở panel xóa hàng loạt và xác nhận trên hộp thoại<br>5. Chờ màn tải lại; bỏ bộ lọc và đếm lại tổng số bạn còn trên màn<br>6. Đọc lại số liệu trang đích 「LP_RV_B1」 và đếm lượt bấm còn lại của 7 bạn ngoài khoảng lọc | 10 bạn đang ẩn, lọc còn 3; cả 10 cùng có lượt bấm ở 1 trang đích | - Sau khi lọc, panel hiển thị đúng **3** người được chọn<br>- Chỉ **3** bạn E1, E2, E3 bị xóa; bỏ lọc ra vẫn còn đúng **7** bạn<br>- Số liệu trang đích 「LP_RV_B1」 chỉ trừ đúng tổng lượt bấm của 3 bạn đã lọc, **không** trừ phần của 7 bạn còn lại<br>- Lượt bấm của 7 bạn còn lại vẫn nguyên |  | Lấp `Q2` · Đánh giá spec: Spec ghi rõ (friend-list `SCR-FRL-04` — bộ lọc 「表示期間」 + panel 一括友だち操作) · Evidence: số bản ghi thật bị tác động khớp 100% số đã lọc |
| TC-BULK001-02 | UI | BULK-001 | Xóa hàng loạt sau khi lọc | Abnormal | manual | Tất cả | Lọc ra tập KHÔNG có ai bấm trang đích → xóa hàng loạt, mọi số liệu trang đích giữ nguyên | - Như TC-BULK001-01 nhưng chọn bộ lọc sao cho 3 bạn còn lại (F1, F2, F3) **đều không có lượt bấm trang đích nào**<br>- Các bạn nằm ngoài khoảng lọc vẫn có lượt bấm ở trang đích 「LP_RV_B2」<br>- Ghi lại số liệu của **toàn bộ** trang đích của bot B1 | 1. Mở màn 「非表示中の友だち」, nhập bộ lọc để còn đúng 3 bạn không có lượt bấm<br>2. Tích 「全選択」 và bấm 「削除」, xác nhận trên hộp thoại<br>3. Chờ màn tải lại; xác nhận 3 bạn đã biến mất và màn **không** báo lỗi<br>4. Mở màn danh sách QR action, đối chiếu số liệu của toàn bộ trang đích với số đã ghi<br>5. Kiểm tra nhật ký ứng dụng ở luồng xóa | 3 bạn được lọc, cả 3 có 0 lượt bấm trang đích | - Cả 3 bạn bị xóa, màn tải lại được, không hiện thông báo lỗi<br>- Số liệu của **MỌI** trang đích trong bot B1 giữ nguyên, không đổi một đơn vị nào<br>- Nhật ký ứng dụng không ghi lỗi ở luồng xóa<br>- Lượt bấm của các bạn ngoài khoảng lọc vẫn nguyên |  | Lấp `Q2` · Đánh giá spec: Spec ghi rõ · Evidence: bảng đối chiếu số liệu toàn bộ trang đích trước/sau |
| TC-BULK001-03 | UI | BULK-001 | Xóa hàng loạt sau khi lọc | Boundary | manual | Tất cả | Màn phân trang 50 dòng — 全選択 ở trang 1 rồi chuyển trang: phạm vi xóa đúng những gì đang tick | - Đăng nhập quản trị bot B1<br>- Màn 「ブロックされた友だち」 có **120 bạn** bị chặn OA (phân trang 50 dòng/trang → 3 trang)<br>- Trang đích 「LP_RV_B3」 có lượt bấm của bạn ở **cả 3 trang** danh sách<br>- Ghi lại số liệu trang đích và số lượt bấm theo từng nhóm 50 | 1. Mở màn 「ブロックされた友だち」, xác nhận có 3 trang<br>2. Ở trang 1, tích 「全選択」; ghi lại số hiển thị trên panel<br>3. Chuyển sang trang 2 **mà không** bấm xóa; quan sát trạng thái tick và số trên panel<br>4. Quay lại trang 1, kiểm tra trạng thái tick còn giữ hay đã mất<br>5. Bấm 「削除」 ở panel và xác nhận<br>6. Đếm số bạn còn lại trên cả 3 trang và đọc lại số liệu trang đích 「LP_RV_B3」 | 120 bạn chia 3 trang 50 dòng; 全選択 chỉ ở trang 1 | - Số người được chọn hiển thị trên panel **khớp đúng** với số dòng thật sự đang tick ở mọi thời điểm (không hiện 120 khi chỉ tick 1 trang)<br>- Sau khi xóa: số bạn bị xóa **khớp 100%** với số đang tick, không rơi về toàn bộ 120<br>- Số liệu trang đích 「LP_RV_B3」 chỉ trừ phần của đúng nhóm bạn đã bị xóa<br>- Lượt bấm của các bạn ở trang chưa tick còn nguyên |  | Lấp `Q2` · Đánh giá spec: Spec ghi rõ (friend-list **BR-04** — 「ブロックされた友だち」 50 items/page) · Evidence: số bản ghi thật bị tác động khớp 100% số đã tick |
| TC-BULK001-04 | UI | BULK-001 | Xóa hàng loạt sau khi lọc | Boundary | manual | product | Màn KHÔNG phân trang — 全選択 lô lớn, trừ đúng tổng và không timeout / hết bộ nhớ | - Đăng nhập quản trị bot B1<br>- Màn 「非表示中の友だち」 (không phân trang) có **200 bạn** đang ẩn<br>- Trang đích 「LP_RV_B4」 có 6000 lượt đọc URL và 1200 lượt kết bạn<br>- Mỗi bạn có 25 lượt bấm ở trang đích này (tổng 5000 dòng), trong đó mỗi bạn 5 lượt kết bạn (tổng 1000); **mỗi dòng gắn một mã thiết bị riêng**<br>- Đã chạy migration thêm 3 chỉ mục | 1. Mở màn danh sách QR action, ghi lại số liệu 「LP_RV_B4」<br>2. Mở màn 「非表示中の友だち」, xác nhận cả 200 bạn hiển thị trên **một trang** (màn này không phân trang)<br>3. Tích 「全選択」; xác nhận panel hiển thị đúng 200<br>4. Bấm 「削除」 ở panel, xác nhận trên hộp thoại và bấm giờ từ lúc gửi yêu cầu<br>5. Chờ màn tải lại xong; ghi thời gian hoàn tất, xác nhận không lỗi timeout/lỗi máy chủ và màn **reload được**<br>6. Kiểm tra nhật ký ứng dụng có lỗi hết bộ nhớ hoặc vượt thời gian thực thi không<br>7. Đếm số bạn còn lại và đọc lại số liệu trang đích<br>8. Lặp lại đúng kịch bản trên nhánh trước fix với dữ liệu giống hệt và ghi lại thời gian + đỉnh bộ nhớ | 200 bạn × 25 lượt bấm = 5000 dòng ở cùng 1 trang đích; 1000 lượt kết bạn; 5000 mã thiết bị khác nhau | - Yêu cầu hoàn tất, **không** timeout máy chủ/trình duyệt, nhật ký **không** có lỗi hết bộ nhớ<br>- Cả 200 bạn biến mất, màn 「非表示中の友だち」 **tải lại được ngay** (không đứng màn — bug #35742)<br>- Trang đích 「LP_RV_B4」: 6000 → **1000** lượt đọc URL, 1200 → **200** lượt kết bạn — trừ đúng **một lần** tổng của cả lô<br>- Đỉnh bộ nhớ và thời gian của nhánh fix ghi lại để đối chiếu nhánh trước fix; **cao hơn rõ rệt** → hồi quy thật, báo lỗi ngay |  | Lấp `G9` · `G8` · `Q9` · **RULE-08** (performance + lô lớn) · Thay chỗ cho `NEW-13` vốn đặt ở màn phân trang 50 dòng/trang nên không dựng lại được · Đánh giá spec: Spec ghi rõ (friend-list **BR-04** — hidden/admin-block không phân trang) · Evidence: thời gian + đỉnh bộ nhớ 2 nhánh + số liệu trang đích trước/sau |
| TC-REGRUN001-01 | API | REG-RUN-001 | Chạy migration thêm chỉ mục khi hệ thống đang chạy | Normal | manual | product | Chạy 3 câu thêm chỉ mục trong lúc vẫn có lượt quét trang đích — không mất log, không lỗi | - Môi trường có khối lượng bảng log tương đương thật và đang có traffic ghi<br>- Job nền đang chạy bình thường<br>- Chuẩn bị kịch bản tạo lượt quét trang đích liên tục (thiết bị thật hoặc luồng quét lặp) trong suốt thời gian chạy migration<br>- Ghi lại số dòng của 3 bảng log tại thời điểm bắt đầu | 1. Bắt đầu tạo lượt quét trang đích liên tục và giữ suốt quá trình<br>2. Chạy migration thêm 3 chỉ mục; bấm giờ từ lúc bắt đầu<br>3. Trong lúc migration chạy: mở màn danh sách QR action và màn danh sách bạn bè, quan sát có lỗi hay treo không<br>4. Ghi lại thời gian hoàn tất của từng câu thêm chỉ mục<br>5. Dừng tạo lượt quét; đếm số lượt đã tạo và đối chiếu với số dòng log thực tế sinh ra<br>6. Kiểm tra nhật ký ứng dụng và nhật ký cơ sở dữ liệu trong khoảng thời gian đó | Traffic ghi liên tục trong suốt thời gian chạy 3 câu thêm chỉ mục | - Migration hoàn tất, ghi lại thời gian thật của từng câu<br>- **Không mất lượt log nào**: số dòng sinh ra khớp đúng số lượt quét đã tạo<br>- Màn danh sách QR action và danh sách bạn bè vẫn mở được trong lúc migration chạy, không treo, không lỗi<br>- Nhật ký không có lỗi khóa bảng hay hết thời gian chờ<br>- Job nền không bị dừng |  | Lấp `Q3` · **RULE-08** (hạ tầng DB + job nền) · Đánh giá spec: Đã hỏi leader (Dev khuyến nghị chạy ngoài giờ cao điểm hoặc bằng công cụ đổi cấu trúc trực tuyến) · Evidence: nhật ký migration + đối chiếu số lượt quét ↔ số dòng log |
| TC-REGRUN001-02 | API | REG-RUN-001 | Chạy migration thêm chỉ mục khi hệ thống đang chạy | Abnormal | manual | product | Xóa bạn NGAY TRONG LÚC migration đang chạy — hoàn tất đúng hoặc báo lỗi rõ ràng | - Như TC-REGRUN001-01<br>- Chuẩn bị sẵn bạn G1 có lượt bấm ở trang đích 「LP_RV_R1」, ghi lại số liệu trang đích và số lượt bấm của G1<br>- Có người thứ hai trực để bấm xóa đúng lúc | 1. Bắt đầu chạy migration thêm 3 chỉ mục<br>2. **Trong lúc** câu thêm chỉ mục đang chạy, mở màn chi tiết bạn bè của G1 và bấm 「削除」, xác nhận<br>3. Ghi lại thời gian phản hồi và thông báo hiện trên màn<br>4. Chờ migration kết thúc<br>5. Kiểm tra bạn G1 còn trong danh sách bạn bè không và số liệu trang đích 「LP_RV_R1」 hiện bao nhiêu<br>6. Đếm số dòng lượt bấm còn lại của G1 | 1 thao tác xóa bạn phát sinh đúng lúc migration đang chạy | - Thao tác xóa **hoặc** hoàn tất bình thường (bạn biến mất, số liệu trang đích trừ đúng, không còn dòng lượt bấm của G1), **hoặc** báo lỗi rõ ràng cho người dùng và **không để lại trạng thái nửa vời**<br>- Tuyệt đối không có ca: bạn đã biến mất khỏi danh sách nhưng số liệu trang đích chưa trừ, hoặc dòng lượt bấm còn sót<br>- Ghi lại kết quả quan sát được để Leader chốt có cần chặn thao tác trong lúc migration hay không |  | Lấp `Q3` · **RULE-08** · Đánh giá spec: Spec không ghi — cần Leader chốt hành vi mong đợi (đã đưa §6 mục 7) · Evidence: ảnh chụp màn + kết quả đếm dòng lượt bấm sau khi migration xong |
| TC-REGRUN001-03 | API | REG-RUN-001 | Chạy migration thêm chỉ mục khi hệ thống đang chạy | Boundary | manual | product | Migration bị ngắt giữa chừng — chạy lại vẫn ra đúng 3 chỉ mục, dữ liệu nguyên vẹn | - Môi trường có khối lượng bảng log lớn (câu thêm chỉ mục chạy đủ lâu để ngắt được)<br>- Ghi lại số dòng của 3 bảng và danh sách chỉ mục hiện có<br>- Có quyền dừng tiến trình đang chạy | 1. Chạy migration thêm 3 chỉ mục<br>2. Trong lúc đang chạy (sau khi câu thứ nhất xong, câu thứ hai chưa xong), **ngắt** tiến trình<br>3. Đọc lại danh sách chỉ mục của cả 3 bảng và đếm số dòng<br>4. Chạy lại migration từ đầu<br>5. Đọc lại danh sách chỉ mục và đếm số dòng lần nữa<br>6. Thực hiện 1 lượt xóa bạn có lượt bấm trang đích và kiểm tra số liệu | Ngắt migration sau câu thêm chỉ mục thứ nhất | - Sau khi ngắt: dữ liệu của 3 bảng **không mất dòng nào**<br>- Chạy lại: bỏ qua chỉ mục đã tạo, tạo nốt 2 chỉ mục còn lại, kết thúc không lỗi trùng tên<br>- Mỗi bảng có **đúng 1** chỉ mục mang tên tương ứng, không bị tạo bản sao<br>- Xóa bạn sau đó vẫn cho số liệu trang đích đúng |  | Lấp `Q3` (RULE-01 đủ 3 loại case) · **RULE-08** · Đánh giá spec: Spec ghi rõ (migration có kiểm tra chỉ mục đã tồn tại trước khi tạo) · Evidence: danh sách chỉ mục + số dòng qua từng bước |
| TC-CONC001-01 | UI | CONC-001 | Xóa bạn bè đồng thời | Normal | manual | product | Hai lượt xóa song song chạm HAI trang đích khác nhau — cả hai đều trừ đủ | - Đăng nhập bot B1 trên **hai cửa sổ trình duyệt riêng**<br>- Trang đích 「LP_RV_C1」 (số liệu 60 lượt đọc URL, 20 lượt kết bạn) và 「LP_RV_C2」 (cùng số liệu)<br>- Bạn H1 có 8 lượt bấm **chỉ ở** 「LP_RV_C1」 (3 lượt kết bạn); bạn H2 có 8 lượt bấm **chỉ ở** 「LP_RV_C2」 (3 lượt kết bạn)<br>- Mỗi bạn có thêm dữ liệu ở 20 trang đích khác để kéo dài thời gian xử lý | 1. Ghi lại số liệu của cả hai trang đích<br>2. Cửa sổ 1: mở màn chi tiết bạn H1, chuẩn bị bấm 「削除」<br>3. Cửa sổ 2: mở màn chi tiết bạn H2, chuẩn bị bấm 「削除」<br>4. Bấm xác nhận xóa ở cả hai cửa sổ trong cùng vài giây<br>5. Chờ cả hai hoàn tất, xác nhận cả hai bạn đã biến mất<br>6. Đọc lại số liệu của cả hai trang đích | H1 → 「LP_RV_C1」 8 lượt bấm · H2 → 「LP_RV_C2」 8 lượt bấm; hai trang đích **không** giao nhau | - Cả hai yêu cầu hoàn tất thành công, không lỗi máy chủ, không lỗi khóa bảng<br>- 「LP_RV_C1」: 60 → 52 lượt đọc URL, 20 → 17 lượt kết bạn<br>- 「LP_RV_C2」: 60 → 52 lượt đọc URL, 20 → 17 lượt kết bạn<br>- **Không** trang đích nào bị trừ thiếu — đây là ca không tranh chấp nên phải đúng tuyệt đối |  | Lấp `Q4` (RULE-01 — bổ sung `Normal` cho CONC-001) · **RULE-08** (race) · Đánh giá spec: Spec ghi rõ · Evidence: số liệu 2 trang đích trước/sau + thời điểm 2 request |
| TC-CONC001-02 | UI | CONC-001 | Xóa bạn bè đồng thời | Boundary | manual | product | Năm lượt xóa song song cùng chạm MỘT trang đích — ghi nhận mức mất mát phần trừ | - Đăng nhập bot B1 trên **năm cửa sổ/thiết bị riêng**<br>- Trang đích 「LP_RV_C3」 có 200 lượt đọc URL và 80 lượt kết bạn<br>- Năm bạn I1..I5, mỗi bạn có 10 lượt bấm ở **cùng** 「LP_RV_C3」 (mỗi bạn 4 lượt kết bạn)<br>- Mỗi bạn có thêm dữ liệu ở 20 trang đích khác để kéo dài thời gian xử lý<br>- Đã chuẩn bị môi trường nhánh trước fix với dữ liệu giống hệt | 1. Ghi lại số liệu của 「LP_RV_C3」<br>2. Mở 5 cửa sổ, mỗi cửa sổ vào màn chi tiết của 1 bạn I1..I5<br>3. Bấm xác nhận xóa ở cả 5 cửa sổ trong cùng vài giây<br>4. Chờ tất cả hoàn tất, xác nhận cả 5 bạn đã biến mất<br>5. Đọc lại số liệu 「LP_RV_C3」 và ghi con số quan sát được<br>6. Chạy lại **đúng kịch bản này** trên nhánh trước fix với dữ liệu giống hệt và ghi con số | 5 lượt xóa song song, tổng 50 lượt bấm và 20 lượt kết bạn ở cùng 1 trang đích | - Cả 5 yêu cầu hoàn tất, không yêu cầu nào lỗi máy chủ<br>- Kỳ vọng số liệu: 200 → 150 lượt đọc URL và 80 → 60 lượt kết bạn<br>- Nếu số quan sát được **lớn hơn** kỳ vọng (mất một phần trừ): ghi rõ **mất bao nhiêu trên 5 luồng**, rồi đối chiếu với nhánh trước fix. Nhánh trước fix mất **tương đương** → hành vi có sẵn, chuyển Leader tách ticket riêng, **không** báo là hồi quy của #40997. Nhánh trước fix **không** mất mà nhánh fix mất → hồi quy thật, báo lỗi ngay<br>- Số liệu tuyệt đối **không** được âm và không trang đích nào khác bị đổi |  | Lấp `Q4` (RULE-01 — bổ sung `Boundary`, nâng số luồng từ 2 lên 5 so với `NEW-14`) · **RULE-08** (race) · Đánh giá spec: Spec không ghi — hành vi ghi counter bằng giá trị tuyệt đối đã đưa §6 mục 1 · Evidence: bảng đối chiếu 2 nhánh |
| TC-COMPATLEGACY001-01 | Data | COMPAT-LEGACY-001 | Xóa bạn bè — dữ liệu log đời cũ | Abnormal | manual | product | Xóa bạn có lượt bấm tạo TRƯỚC các migration bổ sung cột — vẫn dọn đúng | - Môi trường có dữ liệu log thật, đủ cũ<br>- Tìm 1 bạn J1 có **cả hai** loại dòng: (a) dòng lượt bấm tạo **trước** các migration bổ sung cột cho bảng lượt mở trang đích (các cột thêm sau còn khuyết/để trống), (b) dòng lượt bấm tạo **gần đây** với đầy đủ cột<br>- Ghi lại số dòng từng loại, số liệu của các trang đích liên quan<br>- Chuẩn bị 1 bạn J2 đối chứng chỉ có dòng đời mới | 1. Ghi lại ở tầng dữ liệu: số dòng đời cũ và số dòng đời mới của bạn J1, kèm giá trị các cột thêm sau<br>2. Ghi lại số liệu của mọi trang đích mà J1 từng bấm<br>3. Mở màn chi tiết bạn bè của J1 → bấm 「削除」 → xác nhận<br>4. Xác nhận màn chuyển về danh sách bạn bè, không hiện thông báo lỗi và nhật ký ứng dụng không ghi lỗi<br>5. Đếm lại dòng lượt bấm còn sót của J1 (cả 2 đời) và đọc lại số liệu các trang đích<br>6. Lặp lại toàn bộ với bạn J2 và đối chiếu hai kết quả | Bạn J1: dòng log đời cũ (cột thêm sau khuyết) + dòng log đời mới. Bạn J2: chỉ dòng đời mới | - Thao tác xóa hoàn tất không lỗi với **cả hai** loại dữ liệu<br>- **Không sót** dòng lượt bấm nào của J1, kể cả dòng đời cũ có cột khuyết<br>- Số liệu các trang đích trừ đúng **tổng của cả hai đời dòng**, không bỏ qua dòng đời cũ<br>- Kết quả của J1 và J2 nhất quán về cách trừ<br>- Bản ghi lượt mở trang đích đời cũ cũng được dọn theo phạm vi bot |  | Lấp `Q7` · **RULE-09** · regression · Đánh giá spec: Spec ghi rõ (bằng chứng của Dev: bảng lượt mở trang đích tạo 2025-06-19 **+ 2 migration bổ sung cột**; bảng lượt bấm từ 2020) · Evidence: ảnh chụp kết quả query cả 2 đời dòng trước/sau |
| TC-DATAMIG001-01 | Data | DATA-MIG-001 | Chỉ mục CSDL cho khối dọn số liệu trang đích | Boundary | auto | Tất cả | Định danh LINE dài hơn tiền tố 64 ký tự của chỉ mục — vẫn lọc và trừ đúng | - Đã chạy migration thêm 3 chỉ mục<br>- Dựng 2 bạn có định danh LINE **dài hơn 64 ký tự** và **trùng nhau đúng 64 ký tự đầu**, chỉ khác từ ký tự thứ 65 trở đi (cột này là chuỗi 255 ký tự)<br>- Cả 2 bạn cùng có lượt bấm ở trang đích 「LP_RV_M1」: bạn K1 có 5 lượt bấm, bạn K2 có 3 lượt bấm<br>- Ghi lại số liệu trang đích | 1. Xác nhận ở tầng dữ liệu rằng 2 định danh LINE có 64 ký tự đầu giống hệt nhau<br>2. Ghi lại số liệu trang đích 「LP_RV_M1」 và số dòng lượt bấm của từng bạn<br>3. Mở màn chi tiết bạn bè của K1 → bấm 「削除」 → xác nhận<br>4. Đọc lại số liệu trang đích và đếm dòng lượt bấm còn lại của K2<br>5. Xem kế hoạch thực thi của câu gom số liệu để xác nhận có dùng chỉ mục mới và **có lọc lại chính xác** phần sau tiền tố hay không | 2 định danh LINE > 64 ký tự, trùng nhau 64 ký tự đầu | - Trang đích 「LP_RV_M1」 chỉ trừ đúng **5** lượt bấm của K1, **không** trừ 8 (tức không gom nhầm cả K2 do trùng tiền tố chỉ mục)<br>- Bạn K2 vẫn còn đủ **3** dòng lượt bấm và vẫn nằm trong danh sách bạn bè<br>- Kế hoạch thực thi cho thấy chỉ mục mới được dùng để thu hẹp, nhưng điều kiện lọc vẫn so khớp **toàn bộ** định danh LINE |  | Lấp `Q8` (RULE-01 — bổ sung `Boundary` cho DATA-MIG-001) · Đánh giá spec: Spec ghi rõ (migration dùng tiền tố 64 ký tự cho cột định danh LINE) · Evidence: ảnh chụp kết quả query trước/sau + kế hoạch thực thi |

**GAP/RISK không đề xuất TC mới** (dùng lại TC kho hoặc yêu cầu chạy TC sẵn có):

| # | Xử lý |
|---|---|
| `G5` | **Không viết TC mới** — `NEW-25` đã đúng ý định, vấn đề là **chưa chạy**. Việc phải làm: chạy `NEW-25` trên môi trường có khối lượng log thật (§4 I2), và dùng lại kho `TC-FRL-142` "Xoá friend có RẤT NHIỀU dữ liệu không bị treo màn (Bug #38390)" — chỉnh: đổi trọng tâm dữ liệu từ `url_shorten_detail` sang lượt bấm trang đích, ghi con số thời gian để đối chiếu mốc 345s của ticket |
| `G6` · `Q5` | **Không viết TC mới** — dùng lại kho `TC-FRL-136` / `TC-FRL-137` / `TC-FRL-139` (cascade đầy đủ, PRODUCTION), chỉnh: chèn thêm bước đọc 4 số liệu trang đích trước/sau. Riêng pattern "tích 全選択 rồi chỉ bấm xóa 1 dòng" dùng nguyên `TC-FRL-138` — Studio không có TC tương đương. Song song đó, `NEW-26` phải được viết lại expected theo §4 I8 rồi chạy |
| `Q6` | **Không viết TC mới** — dùng lại kho `TC-FRL-143` (đồng bộ Elasticsearch khi xóa bạn). Khi chạy phải ghi lại giá trị `type` thật để chốt xung đột **MT-13** (§6 mục 3) |
| `Q9` | **Không viết TC mới** — vế "màn tải lại được ngay sau khi xóa hàng loạt" đã nằm trong kho `TC-FRL-136` (nguồn bug tester #35742). Việc phải làm: sửa `NEW-13` cho dựng lại được (§4 I7) rồi chạy |
| `G7` | Đã cover bởi `TC-DATACOUNT001-01` (bước 5 đọc lại số của ngày quá khứ trong cùng lượt xóa) |
| `G8` · `G9` | Đã cover bởi `TC-BULK001-04` — TC này **thay chỗ** cho `NEW-13` (chuyển sang màn không phân trang để dựng lại được, thêm vế đối chiếu đỉnh bộ nhớ 2 nhánh). Song song: raise ticket cho lần `error` của `NEW-13` theo §4 I1 |
| — | **KHÔNG phải GAP**: việc không lặp lại đối chiếu số liệu trang đích ở cả 3 màn tab. Cả 3 màn gọi chung `deleteUserBlock` / `deleteUserBlockAction`, và bản fix **không sửa file JS nào** (diff đúng 5 file PHP). `#18647` / `#18648` đã chứng minh nút 「削除」 của từng màn gọi tới được handler; nhân bản phép đối chiếu số liệu ra 3 màn là dư thừa. Cái **thật sự** khác nhau giữa 3 màn là **phân trang** → đã tách thành `G9` |

---

## 6. Spec update needed

| # | Điểm cần chốt | Nguồn mâu thuẫn / thiếu | Ai chốt |
|---|---|---|---|
| 1 | **Hành vi đúng khi nhiều lượt xóa bạn chạy song song chạm cùng 1 trang đích** — 4 counter của bảng trang đích được ghi bằng **giá trị tuyệt đối** (đọc bản ghi rồi ghi đè) trong khi số liệu theo ngày lại trừ nguyên tử, nên một phần trừ có thể bị mất. Cách ghi này **giống nhau ở cả nhánh trước và sau fix** → không phải hồi quy của #40997 nhưng đang để `NEW-14` không có oracle chốt được | `NEW-14` ghi chú tự nêu là "câu hỏi mở đã ghi trong plan để leader chốt"; `TC-CONC001-02` đề xuất ở §5 cũng dừng ở mức ghi nhận | Leader + Dev — chốt rồi tách ticket riêng nếu cần |
| 2 | **Bộ mã màn hình của 3 màn trạng thái bạn bè** — Studio dùng `SCR-FL-04/05/06`, spec `friend-list` dùng `SCR-FRL-04` (非表示中の友だち) / `SCR-FRL-05` (ブロックされた友だち) / `SCR-FRL-06` (ブロックした友だち). Hai bộ mã gán **ngược nhau** cho cùng màn | `#18649`, `#18650` tự ghi cảnh báo lệch; `spec-features/admin/friend-list/feature-spec.md` §SCR-FRL-04..06 | Leader — chốt 1 bộ rồi sửa đồng loạt trên Studio và kho |
| 3 | **Giá trị `type` của bản ghi đồng bộ Elasticsearch khi xóa bạn bè: `2` hay `11`?** | Kho `MT-13`: TC cũ ghi `type=2`, spec `friend-list` §7.2 ghi `TYPE_DELETE_LINE_USER = 11` (còn `2` = `TYPE_CONVERSATION`, dành cho thao tác KHÁC). Chạm trực tiếp `Q6` | Dev — xác nhận enum hiện hành |
| 4 | **Mã feature của màn số liệu trang đích** — `03-dev-impact.md` mục 4.3 ghi "Landing Page (FA-011)" nhưng `FA-011` = 「フォーム作成」; màn đúng là `FA-017` QR Code Action 「QRコードアクション」 (`/basic/landing`) | `templates/LME-SYSTEM-SPEC.md` bảng feature (dòng 689 vs 692) | Dev — sửa đánh giá ảnh hưởng |
| 5 | **Mã endpoint của luồng xóa bạn** — `requirement_keys` của Studio dẫn `friend-list/EP-17` · `EP-25` · `EP-26`, nhưng spec ghi `EP-17` = `send-action` (bulk action), `EP-25`/`EP-26` = import CSV. Mã đúng: màn **chi tiết bạn bè** = `detail-friend/EP-08` `POST /basic/line_user/update` (`type=deleteLineUser`) → `FriendlistController@updateLineUser:2064-2466`; **3 màn tab** = `friend-list/EP-14` `POST /ajax/friendlist/delete-user-block`. ⚠️ Kèm theo: `friend-list/feature-spec.md` §SCR-FRL-03 ghi 「削除」 → `EP-14` là **sai** — spec riêng của màn chi tiết (`detail-friend`) mới đúng | `spec-features/admin/detail-friend/feature-spec.md` §BR-03 + bảng endpoint EP-07/EP-08 · `spec-features/admin/friend-list/feature-spec.md` §SCR-FRL-03 | Leader — sửa `source_refs` trên Studio **và** sửa dòng SCR-FRL-03 của spec `friend-list` |
| 6 | **Chỉ mục mới trên bảng lượt mở trang đích có thật sự dùng được cho job nền không?** Chỉ mục thêm là `(bot_id, landing_id)` trong khi job ghép thiết bị truy vấn `WHERE device = ? AND landing_id = ?` (không có `bot_id`) → **không khớp tiền tố trái nên không dùng được chỉ mục này**, job vẫn quét toàn bảng trong vòng lặp liên tục, đồng thời bảng lại gánh thêm chi phí ghi. Spec đã đánh giá đây là rủi ro hiệu năng **Nghiêm trọng** từ trước | `spec-features/admin/qr-landing/db/db-mapping.md` §6.4 + mục rủi ro (`collect_open_landings` chỉ có PK; `MappingDeviceTask` full-scan trong `while(true)`) | Dev + DBA — cân nhắc bổ sung chỉ mục `(device, landing_id)`, hoặc xác nhận chỉ mục hiện tại là đủ và ghi lý do |
| 7 | **Hành vi mong đợi khi thao tác xóa bạn phát sinh đúng lúc migration đang chạy** — chặn thao tác, cho chạy bình thường, hay báo lỗi có kiểm soát? Hiện không có spec để viết Expected cho `TC-REGRUN001-02` | Dev chỉ khuyến nghị "chạy ngoài giờ cao điểm hoặc bằng `pt-online-schema-change`", không định nghĩa hành vi | Leader + Dev |

---

**Nguồn spec đã dùng** (in ra chat theo BƯỚC 1, ghi lại ở đây để Leader tra):
`spec-features/admin/friend-list/feature-spec.md` — §SCR-FRL-04/05/06 (3 màn trạng thái + bộ lọc 「表示期間」 + bulk action), bảng endpoint (EP-14), **BR-04** (page size 200/50/không phân trang), **BR-05** (ngưỡng 200 user → background job) ·
`spec-features/admin/qr-landing/feature-spec.md` — LUỒNG 2 (ghi log khi quét QR), §Thống kê **BR-27** (hôm nay realtime) / **BR-28** (`type_count` unique vs tất cả lượt) / **BR-30** / **BR-31** (CSV Shift_JIS), §6.4 `MappingDeviceTask` ·
`spec-features/admin/qr-landing/db/db-mapping.md` — P-2 / P-3 (ai ghi 3 bảng log), §6.5 hàng đợi `collect_id`, mục rủi ro hiệu năng `collect_open_landings` chỉ có PK ·
`templates/LME-SYSTEM-SPEC.md` — bảng feature (FA-011 vs FA-017).
