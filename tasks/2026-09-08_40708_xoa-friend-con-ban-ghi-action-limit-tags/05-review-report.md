# 05 — Review Report

## 0. Nguồn TC

- **Nguồn đã dùng**: MCP LME TEST STUDIO — task #283 (ticket 40708, round 1, branch `ai_small_40708`, `reviewState=leader`, `reviewed=false`)
- **Tổng số TC review**: 22

---

## 1. Coverage — `dev-impact` + `diff code`

**Kết luận: 10/20 vùng ảnh hưởng đủ TC · 10 GAP · 5 RISK.**

> Chiều `diff code` lấy từ Studio tab Thông tin (`diffAvailable=true`, `diffStat` = 2 file / +134 dòng: `RecoverActionLimitTagOrphan.php` +121, `app/Helpers/functions.php` +13) + `dev_impact` Studio.
> ⚠️ Toàn bộ dòng dưới đây ở mức **RISK tối thiểu** kể cả khi có TC, vì **0/22 TC pass** (xem §4) — có TC ≠ đã test.

| Mã | Chiều | Vùng ảnh hưởng | TC hiện có | Status | Vấn đề |
|---|---|---|---|---|---|
| **G1** | `diff code` | Hiệu năng luồng xóa friend sau khi thêm DELETE `action_limit_tags` | — | **GAP** `[BLOCKER]` | Dev tự nêu bảng **KHÔNG có index trên (`bot_id`,`line_user_id`)`** → mỗi lần xóa friend giờ quét thêm 1 bảng lớn **không index**, ở **cả 4 đường xóa** và trong **vòng lặp xóa hàng loạt**. Kho đã có bug thật cùng luồng: `TC-FRL-142` / Bug **#38390** "xóa friend có nhiều data → chậm load mãi ở màn xóa". **0 TC đo thời gian xóa sau fix** → không ai biết fix có làm bug #38390 nặng thêm không |
| **G2** | `dev-impact` T1 + `diff code` | Regression cascade xóa friend (spec `friend-list/BR-06`: cascade **~20+ bảng**, hàm ~400 dòng) | `TC-REGSHARED001-01` | **GAP** `[BLOCKER]` | Bước DELETE mới nằm **cùng khối try/catch** với các bước dọn cũ (`functions.php:11940-11953` — chính TC-REGSHARED001-01 ghi nhận). Nhưng TC chỉ kiểm **4/20+ bảng** (`tag_history`, `rich_menu_history`, `friend_information_history`, `line_user_add_friend_history`) và **chỉ ở 1/5 đường xóa** (màn chi tiết). 4 đường còn lại không có TC nào xác nhận cascade cũ còn nguyên |
| **G3** | `diff code` | Xóa hàng loạt quy mô thật | `TC-SELECTSCOPE001-01` (3 friend) · `TC-TOOLNEGCTRL001-03` (1/3) | **GAP** `[MAJOR]` | Hàm dọn được gọi **trong vòng lặp từng friend** (Dev tự nêu). TC lớn nhất chỉ **3 friend**, trong khi màn 「ブロックされた友だち」 hiển thị **50/trang** (`friend-list/BR-04`) và có nút 「全選択」. Không có TC nào cho lô đầy trang / lô lớn → không phủ rủi ro timeout + hỏng giữa chừng |
| **G10** | `dev-impact` F5 + `diff code` | **Nút xóa hàng loạt ở 2/3 màn danh sách phụ** — `deleteUserBlockAction` (`:4026-4133`) | Chỉ `TC-SELECTSCOPE001-01` · `TC-TOOLNEGCTRL001-03`, **cả 2 đều ở màn 「ブロックされた友だち」** | **GAP** `[BLOCKER]` | Grep 22 TC: chỉ **2 TC** chứa thao tác `一括友だち削除`, đều ở **1 màn duy nhất**. Màn 「非表示中の友だち」 và 「ブロックした友だち」 **đều có** nút xóa hàng loạt (kho `TC-FRL-136`, `TC-FRL-137`; spec `EP-15 POST /ajax/friendlist/delete-multiple-user-block` = "Xoá hàng loạt (cascade)"), nhưng mỗi màn chỉ có TC **xóa 1 friend**. Bulk đi qua **handler riêng** `deleteUserBlockAction`, **khác** handler xóa đơn `deleteUserBlock` (`:3635-4023`) → **không suy ra được** từ TC xóa 1 friend. `REG-SHARED-001` yêu cầu test **TỪNG lối vào** của hàm dùng chung |
| **G4** | `dev-impact` T2 | `count_user_tag` / cột 人数制限 của tag sau khi xóa friend | — | **GAP** `[MAJOR]` | Bug là về **cơ chế giới hạn tag**, nhưng không TC nào kiểm con số giới hạn. Kho FA-012 đã chuẩn hóa 3 trạng thái cột này (`TC-TAG-146/147/148`: `人数制限なし` / `{count}/{limit}人` / `{n}/{n}人（上限到達）` màu đỏ). Xóa friend đang giữ tag đầy → tag phải **rời trạng thái 上限到達**. Đây cũng là **root cause hợp lý thứ 2** của triệu chứng (xem AP-2 ở §4) |
| **G5** | `diff code` | Race: xóa friend **trong lúc** action giới hạn của chính friend đó đang chạy | — | **GAP** `[MAJOR]` | Fix xóa `action_limit_tags` rồi mới/đồng thời xóa `bot_line_user`. Nếu web (`HelperService::addTags`) hoặc job nền ghi bản ghi tracking **xen vào giữa**, hệ thống **sinh ra bản ghi mồ côi MỚI ngay sau fix** — đúng loại dữ liệu mà command recover phải đi dọn. Không TC nào chạm kịch bản này |
| **G6** | `dev-impact` F6 (job nền) | `LineUserModel.checkLimitActionModeAddTag` — phía job đọc cùng bảng | `TC-REGSHARED001-05` | **RISK** `[MAJOR]` | TC **duy nhất** cho job nền và kết quả là **`skip`** (`runId=1083`, env `local`, by `pipeline-manual`). REQ-009 hiện có **0 bằng chứng**. Chính note của TC cấm thay bằng thao tác web rồi kết luận pass |
| **G7** | `dev-impact` D2 | Command recover chạy khi bot đang **backup / データコピー** | — | **GAP** `[MAJOR]` | Spec `tag-management/BR-02`: **cấm mọi CUD khi bot đang backup** (`BackupHistory status IN (0,1)`). Command chạy bằng CLI, **nằm ngoài lớp khóa đó** → xóa vĩnh viễn trong lúc đang copy bot có thể làm bản copy sai lệch. Không TC nào chạm |
| **G8** | `dev-impact` F2 | Log của command recover | — | **GAP** `[MAJOR]` | Dev khai "ghi log start/end + số bản ghi xóa mỗi lô theo chuẩn `logInfo`". Đây là **bằng chứng duy nhất** cho một thao tác xóa vĩnh viễn không hoàn tác được, nhưng **0 TC** verify log có ghi và có khớp số thực xóa |
| **G9** | `orphan` | Đường xóa thứ **5** — màn 「非表示中の友だち」 | `TC-TOOLSCOPE001-01` | **RISK** `[MAJOR]` | TC này **không map được vào danh sách caller Dev kê** (Dev chỉ kê 4 đường). Studio tự phát hiện qua `public/js/friendlist/hiden.js:83,101`. → **danh sách caller ở `03-dev-impact.md` mục 3 chưa đầy đủ** → phải hỏi Dev còn đường xóa nào khác chưa kê (REG-SHARED-001 yêu cầu danh sách do Dev cung cấp) |
| — | `dev-impact` BUG/F1/F3/F4/F5/D1 | Root cause + 4 đường xóa + đọc/ghi bản ghi tracking | 13 TC | RISK | Có TC đủ chiều, **nhưng 0 TC pass** → chưa có kết luận |

---

## 2. Thiếu so với quan điểm test

**Kết luận: 20 quan điểm Trigger khớp · 13 đã cover đủ về nội dung · 7 chưa cover.**

| Mã | Quan điểm | Ưu tiên | Vấn đề | Severity |
|---|---|---|---|---|
| **Q1** | `PERM-002` — không bypass quyền bằng API / URL trực tiếp | Cao | Xóa friend = thao tác nhạy cảm (xóa + PII) → Trigger khớp tuyệt đối. Dev tự nêu endpoint app **"nhận mã bot từ tham số request (không lấy từ phiên web)"**. `TC-DATADB001-02` cố tình dùng "hai bot **do cùng một tài khoản** quản trị" → **né đúng ca rủi ro**. **0 TC** gọi API với `bot_id` KHÔNG thuộc quyền. Kho FA-013 đã ghi nhận đây là vùng mù (`MT-12`) và có sẵn `TC-FRL-151` chờ Leader duyệt | `[BLOCKER]` |
| **Q2** | `ENV-003` — khác biệt dev/staging/production (RULE-08) | Cao | Task chạm **job nền** + **command xóa dữ liệu vĩnh viễn** → RULE-08 cấm kết luận từ staging. `envAuto` Studio: `local` **1 run**, `dev`/`staging`/`prd` đều **0 run**. Production tách **3 job độc lập** (callback/broadcast/scenario) trong khi dev/staging chạy 1 job all → hành vi job nền ở G6 **không thể suy từ local** | `[BLOCKER]` |
| **Q3** | `CONC-001` — một hành động logic chỉ xử lý 1 lần | Cao | Trigger "**giới hạn dùng chung**" khớp trực tiếp (制限人数 của tag). **0/4 kịch bản** CONC-001 có TC: double-click nút 削除 · 2 tab song song · 2 user cùng sửa · batch đa luồng cùng chạm 1 giới hạn. Kho FA-012 có `TC-TAG-213` (race limit lúc gắn tag) nhưng **không phủ race xóa × limit action** (= G5) | `[BLOCKER]` |
| **Q4** | `DATA-COUNT-001` — số đếm & tổng hợp | Cao | = **G4**. Không TC nào đối chiếu con số 人数制限 bằng phép tính tay trước/sau khi xóa friend. Checklist ghi rõ đây là **"lỗi lặp nhiều nhất lịch sử bug — 12 ticket Closed trên 5 tính năng"** | `[MAJOR]` |
| **Q5** | `DATA-AUDIT-001` — audit log / lịch sử thao tác | Cao | Trigger khớp: xóa trên **dữ liệu nhạy cảm (khách hàng + tag)**. Yêu cầu: cùng 1 thao tác từ **TỪNG nguồn** (web / app / job / bulk) → mỗi lần đúng 1 bản ghi lịch sử đủ 4 thông tin. **0 TC**. Cộng với G8 (log command) → thao tác xóa vĩnh viễn hiện **không có vết** | `[MAJOR]` |
| **Q6** | `JOB-001` — job nền / batch theo lô | Cao | `TC-FUNC004-01` mới phủ ý (2) tạo dữ liệu vượt ngưỡng ở quy mô đồ chơi (5 bản ghi, chunk=2). Thiếu ý **(3)** job không dừng / retry, **(4)** "số bản ghi vào = số xử lý thành công + số vào hàng đợi lỗi — không bản ghi nào biến mất". Với lệnh xóa vĩnh viễn, ý (4) là bắt buộc | `[MAJOR]` |
| **Q7** | `REG-RUN-001` — dữ liệu / job đang chạy dở khi release | Cao | Spec `friend-list/BR-05`: bulk action **> 200 user → ActionSchedule chạy nền**. Xóa friend đang có ActionSchedule / step message / remind chờ chạy → chưa TC nào kiểm job còn lại chạy đúng, không lỗi vì friend đã biến mất | `[MAJOR]` |

> Quan điểm **đã cover đủ nội dung**, chỉ đếm không ghi chi tiết: `FUNC-001` · `FUNC-004` · `DATA-001` · `DATA-REF-001` · `DATA-DB-001` (WHERE scope 2 bot — `TC-TOOLNEGCTRL001-02`/`TC-DATADB001-02`) · `PERM-003` · `REG-SHARED-001` (4 đường) · `STATE-CLEAN-001` · `SEC-001` · `FRIEND-001` · `SYNC-APP-001` · `UI-003` · `LIST-001`.
> ⚠️ **Cảnh báo map**: 12/22 TC gắn mã quan điểm **không tồn tại** trong `framework/checklist-lme.md` (xem §4 #4) → phần cover ở trên là kết quả **đọc nội dung TC**, không phải đọc nhãn. Nếu chấm máy móc theo nhãn thì các quan điểm trên đều thành "chưa cover".

---

## 3. TC trùng lặp

Đã rà **22/22 TC** theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`) — phát hiện **3 nhóm**, **không nhóm nào được phép xóa** (gate BƯỚC 4b chặn cả 3).

| Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| Xóa friend từ màn chi tiết | `TC-TOOLKNOW002-01` | `TC-DATADB001-01` → **GỘP, KHÔNG XÓA** | `DUP-SUBSET` | Bước 1–4 của `TC-TOOLKNOW002-01` lặp **nguyên vẹn** toàn bộ `TC-DATADB001-01` (gắn tag A → tag B được gắn → đếm 1 dòng → xóa → đếm 0 dòng); cùng tiền đề, cùng đối tượng+thao tác, expected tương đương | `[MINOR]` |
| Kiểm kê orphan trước khi chạy lệnh | `TC-DATARETENTION001-02` | `TC-DATADB001-04` → **GỘP, KHÔNG XÓA** | `DUP-SUBSET` | Bước 1 của `TC-DATARETENTION001-02` lặp đúng bước 1–3 của `TC-DATADB001-04` (đếm tổng / đếm mồ côi / tách nhóm bot còn–bot đã xóa); cùng mục đích "biết phạm vi trước khi xóa vĩnh viễn" | `[MINOR]` |
| Xóa 1 friend từ màn danh sách phụ | `TC-REGSHARED001-02` (màn 「ブロックされた友だち」) | `TC-TOOLSCOPE001-01` (màn 「非表示中の友だち」) → **GIỮ CẢ 2** | `DUP-SUBSET` ở tầng code | Chính note của `TC-TOOLSCOPE001-01` xác nhận màn 非表示中 **gọi đúng 2 endpoint xóa của màn đã chặn** → cùng code path, cùng loại case, expected tương đương. Khác nhau **chỉ ở màn vào** | `[MINOR]` |

⚠️ **Gate đã chặn xóa ở cả 3 nhóm** — lý do:
- Xóa `TC-DATADB001-01` → mất **TC duy nhất** map vào mã hợp lệ `DATA-DB-001` cho kịch bản lõi (TC giữ lại mang mã `TOOL-KNOW-002` **không có trong checklist**) → coverage lõi mất chỗ bám.
- Xóa `TC-DATADB001-04` → mất **oracle thủ công** để đối chiếu số mà command tự báo (đúng yêu cầu RULE-04 "xác nhận phạm vi khi recovery").
- Xóa `TC-TOOLSCOPE001-01` → mất **màn 非表示中**, tức mất luôn G9 (đường xóa thứ 5 Dev chưa kê).

**Không có `DUP-INFLATE`** (không nhóm trùng nào đang che GAP) và **không có `DUP-CONFLICT`** giữa 22 TC.

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | Toàn bộ 22 TC | **0/22 pass (0%)** — 0 pass · 0 fail · 1 `skip` · 21 chưa chạy. Ngưỡng `pass < 50%` → BLOCKER. Bộ TC hiện **không cung cấp bằng chứng nào** về chất lượng bản fix, dù ticket đã ở trạng thái "Fix done - Đợi test" | Chạy bộ TC trước khi đưa ra bất kỳ kết luận nào. Mọi dòng "đủ TC" ở §1 phải hiểu là **RISK**, không phải OK |
| 2 | `[BLOCKER]` | Toàn task | **Dev CHƯA verify trên dữ liệu thật** — MySQL dev `Connection refused` lúc implement; mức verify chỉ là `php -l` + `php artisan list` + in câu SQL. Nghĩa là **chưa ai chạy command recover một lần nào**, kể cả Dev | Bắt buộc chạy `--dry-run` trên staging rồi production trước khi chạy thật (đúng đề nghị của chính Dev) |
| 3 | `[MAJOR]` | Toàn bộ 22 TC | **RULE-08 / ENV-003**: `env_scope` khai `["all"]` nhưng `env_tag` thực tế `local-only` ở 21/22 TC; `envAuto` = local 1 run, dev/staging/prd **0 run**. Task có job nền + command destructive | Khai lại `env_scope` đúng thực tế; bổ sung run trên staging + production (xem `TC-ENV003-01` §5) |
| 4 | `[MAJOR]` | 12/22 TC | **7 mã quan điểm không tồn tại** trong `framework/checklist-lme.md`: `TOOL-KNOW-002` (2 TC) · `TOOL-NEGCTRL-001` (3) · `TOOL-AXIS-001` (1) · `TOOL-SCOPE-001` (1) · `SELECT-SCOPE-001` (1) · `JOB-002` (2 — checklist chỉ có `JOB-001`) · `DATA-RETENTION-001` (2). Coverage **không map tự động được** | Map lại sang mã tầng 1, hoặc bổ sung chính thức các mã này vào checklist (RULE-10) rồi chạy `python scripts/build_indexes.py` |
| 5 | `[MAJOR]` | 22/22 TC | `spec_status = null` toàn bộ → cột **Trạng thái đánh giá spec trống 100%**. Có TC đang đặt expected cho hành vi **spec chưa định nghĩa** (phạm vi recover) mà không đánh dấu | Điền `Spec ghi rõ` / `Spec không ghi` / `Đã hỏi leader`; case `Spec không ghi` phải nêu đã hỏi ai |
| 6 | `[MAJOR]` | `TC-REGSHARED001-05` | TC duy nhất có kết quả (`skip`) nhưng **Evidence rỗng và không ghi lý do skip** — RULE-02. `skip` không nêu lý do thì không phân biệt được "môi trường không dựng được job" với "bỏ qua cho nhanh" | Ghi lý do skip cụ thể + bằng chứng đã thử dựng job |
| 7 | `[MAJOR]` | `TC-JOB002-01` · `TC-FUNC004-01` · `TC-DATARETENTION001-02` | **Expected dựng trên tên lệnh / option chưa đọc source** — chính note TC thừa nhận: tên `recover:actionLimitTagOrphan`, `--dry-run`, `--chunk` lấy từ report Dev, "chưa đọc trực tiếp được vì nhánh fix chưa checkout" | Checkout `ai_small_40708`, chạy `php artisan list` + `--help`, đối chiếu rồi chốt expected |
| 8 | `[MAJOR]` | `03-dev-impact.md` | Checkbox **"Tester verify auto-fill chính xác" CHƯA tick** (file có `Auto-filled: 2026-09-08 by /new-task`) → F/D/T có thể thiếu hoặc map sai. G9 đã chứng minh **mục 3 của Dev thiếu ít nhất 1 caller** | Tester đọc lại Redmine #40708 journal #135225, đối chiếu, rồi tick |
| 9 | `[MINOR]` | `TC-DATADB001-03` · `TC-TOOLKNOW002-02` | 2 TC do người viết (`cucdtk@mcp`) có `requirement_keys` **rỗng** → không map được vào REQ nào, dù note ghi "liên quan REQ-005/008/011" | Gắn `requirement_keys` thật |
| 10 | `[MINOR]` | 22/22 TC | `exec_mode = auto` toàn bộ nhưng tỷ lệ automated = **0%** ở mọi env. Khai `auto` không đồng nghĩa đã tự động hóa | Đổi sang `manual` cho TC thực tế chạy tay |

### Chất lượng từng TC + anti-pattern

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 11 | `[MAJOR]` | `TC-DATARETENTION001-02` | **Expected không đo lường được** — viết dạng điều kiện: "**Nếu kết quả = 4**, xác nhận...". TC không chốt được Đạt/Không đạt, thực chất là một bước điều tra | Tách: 1 TC đo phạm vi (số dứt khoát) + 1 quyết định nghiệp vụ đưa lên §6 |
| 12 | `[MAJOR]` | `TC-TOOLKNOW002-02` | **Expected mơ hồ**: "`action_limit_tags` phải có trạng thái tracking **phù hợp**". Không nêu là 0 dòng, 1 dòng mới, hay giữ dòng cũ | Chốt con số cụ thể sau khi §6 quyết định phạm vi recover |
| 13 | `[MAJOR]` | `[AP-3]` Happy-path-only regression | TC regression duy nhất (`TC-REGSHARED001-01`) chỉ chạy **luồng thuận** ("xóa thành công, 4 bảng lịch sử được dọn"). Không TC nào **ép lỗi** ở bước DELETE mới để chứng minh các bước sau nó vẫn chạy — đúng rủi ro mà chính note của TC nêu ra | Bổ sung `TC-OUTTRUTH001-01` (§5) |
| 14 | `[MAJOR]` | `[AP-2]` Symptom-only KH report | Ticket chỉ mô tả triệu chứng ("không được gắn tag B nữa"); Dev fix **1 root cause**. **Root cause hợp lý thứ 2 chưa loại trừ**: trạng thái đầy của tag (`count_user_tag` / cột 人数制限) cũng chặn được việc gắn tag A — không TC nào tách 2 nguyên nhân | Bổ sung `TC-DATACOUNT001-01/02` (§5) |
| 15 | `[MINOR]` | `[AP-6]` mục 3 dev-impact | Mục 3 **có** liệt kê 9 function (không trống), nhưng **thiếu ít nhất 1 caller** (màn 非表示中 — G9); mục 4.1 chỉ ghi 2 file, **không đánh tag `F*`**; mục 4.3 **bỏ trống mức nguy cơ regression** ở cả 3 tính năng | Hỏi Dev bổ sung caller còn thiếu + chấm mức risk |
| 16 | `[NIT]` | Bộ 22 TC | Nhiều TC dùng oracle **`id` / `number_action` của `action_limit_tags`** (đọc DB trực tiếp) làm bước chính. Đúng về kỹ thuật nhưng manual tester khó chạy; cột 人数制限 trên màn tag là oracle nhìn được bằng mắt | Bổ sung bước quan sát UI song song với query DB |

---

## 5. TCs đề xuất bổ sung (16)

### Đã đối chiếu kho TCs + chống trùng

| Kiểm | Kết quả |
|---|---|
| Kho đã đọc | [kho-tcs/fa013-friendlist-友だちリスト.md](../../kho-tcs/fa013-friendlist-友だちリスト.md) (FA-013) · [kho-tcs/fa012-quanlythe-タグ管理.md](../../kho-tcs/fa012-quanlythe-タグ管理.md) (FA-012) |
| Vùng regression phát hiện từ kho | `TC-FRL-136/137/138/139/140` (cascade xóa friend ở 5 lối vào) · `TC-FRL-141` ⭐ (xóa → kết bạn lại, 13 mục lịch sử) · `TC-FRL-142` (Bug #38390 xóa friend chậm) · `TC-FRL-143` (ES sync khi xóa) · `TC-TAG-208/209/210/211` (bảng chân trị limit action) · `TC-TAG-213` (race limit) · `TC-TAG-146/147/148` (3 trạng thái cột 人数制限) · `TC-TAG-107/113` (xóa tag → dọn `action_limit_tags`) |
| **TC kho DÙNG LẠI, không viết mới** | **G2** → chạy lại `TC-FRL-136`, `TC-FRL-137`, `TC-FRL-139`, `TC-FRL-140` (cascade đủ ở 4 lối vào; chỉ cần **thêm 1 dòng kiểm `action_limit_tags`** vào cột Kết quả mong đợi). **G2/BUG** → chạy lại `TC-FRL-141` ⭐ — kho tự ghi *"Không test — phụ thuộc LINE re-follow"* nên **chưa từng có bằng chứng chạy thật**, mà đây đúng là kịch bản của ticket #40708. **Q1** → `TC-FRL-151` ⭐ (staff gọi thẳng API `delete-user-block`) đang **chờ Leader duyệt** — duyệt và chạy luôn ở vòng này |
| Chống trùng vs 22 TC BƯỚC 0 | Đã đối chiếu **14/14** TC đề xuất theo 4 yếu tố — **không TC nào trùng**. Vùng chồng lấn gần nhất: `TC-FUNC004-01` (chunk) vs `TC-JOB001-01` — khác ở **dừng giữa chừng rồi chạy lại**, không phải chia lô |
| Chống trùng trong nội bộ §5 | `G1`+`Q6` gộp vào `TC-PERFLARGE001-01/02`; `G4`+`Q4` gộp vào `TC-DATACOUNT001-01/02`; `G5`+`Q3` gộp vào `TC-CONC001-01`; `G7`+`Q2` tách riêng vì khác môi trường |
| **Đã push lên Studio** (2026-09-09) | **6/16** TC đã tạo trên Studio task #283, `status=draft`: `TC-PERFLARGE001-02` → Studio **#16785** (NEW-24) · `TC-DATACOUNT001-01` → **#16786** (NEW-25) · `TC-CONC001-02` → **#16787** (NEW-26) · `TC-PERM002-01` → **#16788** (NEW-27) · `TC-REGSHARED001-06` → **#16789** (NEW-28) · `TC-REGSHARED001-07` → **#16790** (NEW-29). `client_ref` = TC No. nên push lại không tạo trùng. **10 TC còn lại chưa push** |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-PERFLARGE001-01 | UI | PERF-LARGE-001 | Xóa bạn bè & cascade — màn chi tiết bạn bè | Boundary | manual | product | Xóa 1 friend nhiều dữ liệu trên bot quy mô lớn — thời gian xóa không tệ hơn trước fix | - Bot PRODUCTION có lượng friend lớn nhất hiện có của khách (hỏi CS lấy con số thật)<br>- Chọn 1 friend có nhiều dữ liệu: đã quét QR có URL rút gọn, đã trả lời form, đã đặt lịch, có nhiều tag, có friend info<br>- Ghi lại số bản ghi từng loại của friend đó<br>- Chuẩn bị 1 friend tương đương trên bot **chưa deploy fix** để đối chứng | 1. Trên bot CHƯA có fix, mở màn chi tiết friend đối chứng, bấm 「削除」 → 「削除する」, bấm giờ từ lúc xác nhận đến khi màn hình phản hồi xong. Ghi lại số giây<br>2. Trên bot ĐÃ deploy fix, mở màn chi tiết friend đã chuẩn bị, bấm 「削除」 → 「削除する」, bấm giờ tương tự<br>3. Quan sát màn hình trong lúc chờ: có đứng ở trạng thái loading vô hạn không<br>4. So sánh 2 con số thời gian<br>5. Kiểm tra friend đã biến mất khỏi danh sách và dữ liệu liên quan đã được dọn | Friend có khối lượng dữ liệu lớn (ghi rõ số bản ghi URL rút gọn / form / đặt lịch); quy mô bot = số friend lớn nhất thực tế của khách | - Màn hình KHÔNG đứng loading vô hạn<br>- Thời gian xóa sau fix **không tăng quá 20%** so với bot chưa fix (ghi lại cả 2 con số thực tế để Leader chốt ngưỡng)<br>- Friend biến mất khỏi danh sách, dữ liệu liên quan được dọn đủ | | **Lấp G1 + Q6.** RULE-08: hiệu năng → PRODUCTION · Evidence: 2 con số thời gian đo thật + ảnh màn hình sau khi xóa · Đánh giá spec: Spec không ghi — đã hỏi Leader chốt ngưỡng · Bối cảnh: `action_limit_tags` **không có index (bot_id, line_user_id)** (Dev tự nêu) nên DELETE mới có thể quét toàn bảng · Nối tiếp kho `TC-FRL-142` / Bug #38390 · regression |
| TC-PERFLARGE001-02 | UI | PERF-LARGE-001 | Xóa bạn bè hàng loạt — màn 「ブロックされた友だち」 | Boundary | manual | product | Xóa hàng loạt đầy 1 trang (50 friend) — hoàn tất, không sót, màn reload được | - Bot PRODUCTION có ≥ 50 friend đang chặn OA hiển thị ở màn 「ブロックされた友だち」 (màn này 50 friend/trang)<br>- Trong đó ít nhất 10 friend đã từng kích hoạt action khi tag đầy<br>- Ghi lại danh sách tên 50 friend trước khi xóa | 1. Mở màn 「ブロックされた友だち」<br>2. Tích 「全選択」 để chọn toàn bộ 50 friend trên trang<br>3. Ở panel 「一括友だち削除」 bấm 「削除」, xác nhận<br>4. Bấm giờ từ lúc xác nhận đến khi màn hình tải lại xong<br>5. Đếm số friend còn lại trên màn, đối chiếu danh sách đã ghi<br>6. Mở màn quản lý tag, kiểm tra cột 人数制限 của các tag liên quan | 50 friend (đầy 1 trang), ≥ 10 friend có bản ghi giới hạn tag | - Cả 50 friend biến mất khỏi danh sách, **không sót friend nào**<br>- Màn hình **tải lại được ngay**, không đứng loading, không lỗi 500/timeout<br>- Ghi lại thời gian thực tế để Leader chốt ngưỡng<br>- Cột 人数制限 của các tag liên quan giảm đúng số friend vừa xóa | | **Lấp G3.** RULE-08: khối lượng lớn + xóa cascade → PRODUCTION · Evidence: thời gian chạy + ảnh danh sách trước/sau + số đếm · Bối cảnh: hàm dọn được gọi **trong vòng lặp từng friend** → 50 lần DELETE trên bảng không index · Nối tiếp kho `TC-FRL-137` · regression |
| TC-DATACOUNT001-01 | UI | DATA-COUNT-001 | Quản lý tag — cột 人数制限 | Normal | manual | staging | Xóa friend đang giữ tag đã đạt giới hạn → cột 人数制限 rời trạng thái 上限到達 | - Bot B1 có tag A đặt 「人数制限」=「制限する」, 「制限人数」=3<br>- Đúng 3 friend đang giữ tag A → màn danh sách tag hiển thị 「3/3 人（上限到達）」 màu đỏ<br>- Ghi lại con số hiển thị trước khi thao tác | 1. Mở màn quản lý tag 「タグ管理」, tìm dòng tag A, chụp lại cột 人数制限 (đang là 3/3, màu đỏ)<br>2. Mở màn danh sách bạn bè, vào chi tiết 1 trong 3 friend đang giữ tag A<br>3. Bấm 「削除」 → 「削除する」<br>4. Quay lại màn quản lý tag, đọc lại cột 人数制限 của tag A<br>5. Mở màn danh sách friend có tag A, đếm số friend thực tế | Tag A: 制限人数 = 3, đang có đúng 3 friend. Xóa 1 friend | - Trước khi xóa: cột hiển thị 「3/3 人（上限到達）」 màu đỏ<br>- Sau khi xóa: cột hiển thị **「2/3 人」**, **không còn** chữ 上限到達 và **không còn** màu đỏ<br>- Phép tính tay: 3 − 1 = 2, khớp số friend đếm được ở màn danh sách friend có tag A | | **Lấp G4 + Q4.** Evidence: ảnh cột 人数制限 trước/sau + số đếm ở màn friend có tag · Đánh giá spec: Spec ghi rõ — `tag-management/BR-05` · Nối tiếp kho `TC-TAG-147/148` (3 trạng thái cột này) · Oracle nhìn bằng mắt, không cần query DB |
| TC-DATACOUNT001-02 | UI | DATA-COUNT-001 | Quản lý tag — giới hạn tag sau khi xóa friend | Boundary | manual | staging | Tag hết trạng thái đầy sau khi xóa friend → friend mới gắn tag A vào bình thường, KHÔNG chạy action giới hạn | - Bot B1 có tag A 「制限する」/「制限人数」=3, đang đủ 3 friend (đầy)<br>- Tag A có 「制限到達後のタグ追加時アクション」 gắn tag B<br>- Friend M chưa có tag A và chưa từng kích hoạt action giới hạn | 1. Khi tag A đang đầy 3/3, vào chi tiết friend M, tab 「タグ」, tick tag A rồi lưu<br>2. Quan sát tag của friend M: xác nhận tag A KHÔNG được gắn, tag B ĐƯỢC gắn (action giới hạn đã chạy)<br>3. Gỡ tag B khỏi friend M để làm sạch<br>4. Xóa 1 friend khác đang giữ tag A → cột 人数制限 về 「2/3 人」<br>5. Vào lại chi tiết friend M, tab 「タグ」, tick tag A rồi lưu<br>6. Quan sát tag của friend M và cột 人数制限 của tag A | Tag A: 制限人数 = 3. Bước 1 chạy khi đầy, bước 5 chạy khi còn chỗ | - Bước 2 (tag đầy): friend M **không** có tag A, **có** tag B<br>- Bước 6 (tag còn chỗ): friend M **ĐƯỢC gắn tag A** bình thường, **KHÔNG** được gắn thêm tag B (action giới hạn **không** chạy)<br>- Cột 人数制限 chuyển từ 「2/3 人」 sang 「3/3 人（上限到達）」 sau bước 6 | | **Lấp G4 + Q4 + AP-2 (root cause thứ 2).** Tách bạch 2 nguyên nhân cùng gây triệu chứng "không được gắn tag": (a) bản ghi tracking còn sót — đã có TC; (b) tag vẫn đang đầy — chưa có TC nào · Evidence: ảnh tab タグ của friend M ở bước 2 và 6 · Đánh giá spec: Spec ghi rõ — `tag-management/BR-05` + `BR-06` |
| TC-CONC001-01 | UI | CONC-001 | Xóa bạn bè × action giới hạn tag đang chạy | Abnormal | manual | product | Xóa friend ngay lúc action giới hạn của friend đó đang chạy → không sinh bản ghi mồ côi mới | - Bot B1 có tag A đang đầy với action giới hạn gắn tag B<br>- Friend N là bạn của B1, chưa có tag A<br>- Chuẩn bị 2 cửa sổ trình duyệt cùng đăng nhập: cửa sổ 1 mở tab 「タグ」 của friend N, cửa sổ 2 mở màn chi tiết friend N<br>- Ghi lại số bản ghi giới hạn tag của friend N (= 0) | 1. Ở cửa sổ 1, tick tag A cho friend N và bấm lưu<br>2. **Ngay lập tức** (trong lúc cửa sổ 1 còn đang xử lý), ở cửa sổ 2 bấm 「削除」 → 「削除する」 để xóa friend N<br>3. Chờ cả 2 thao tác kết thúc, ghi lại thông báo của từng cửa sổ<br>4. Kiểm tra friend N còn trong danh sách bạn bè không<br>5. Đếm bản ghi giới hạn tag còn lại của cặp bot B1 + friend N<br>6. Lặp lại 5 lần để bắt được ca chèn nhau | Thao tác gắn tag và thao tác xóa friend phát sinh gần như đồng thời trên cùng 1 friend | - Hệ thống **khóa hoặc báo xung đột**, không âm thầm xử lý cả hai<br>- Sau khi friend N bị xóa: **0 bản ghi** giới hạn tag còn lại cho cặp bot B1 + friend N<br>- ⚠️ Nếu còn ≥ 1 bản ghi → fix vừa **sinh ra bản ghi mồ côi MỚI**, đúng loại dữ liệu mà lệnh recover phải đi dọn → raise bug ngay | | **Lấp G5 + Q3.** RULE-08: race → PRODUCTION · Evidence: log/DB số bản ghi thực tế sau mỗi lần lặp (5 lần) · RULE-01: quan điểm Cao mới có 2 case Abnormal — **thiếu Normal/Boundary vì kịch bản race không có nhánh thuận và không có khái niệm biên** · Nối tiếp kho `TC-TAG-213` (race ở lúc gắn tag, không phủ race với xóa) |
| TC-CONC001-02 | UI | CONC-001 | Xóa bạn bè — thao tác trùng | Abnormal | manual | staging | Double-click nút 削除 và xóa cùng friend trên 2 tab → chỉ xử lý 1 lần, không lỗi | - Bot B1 có friend P đang có đúng 1 bản ghi giới hạn tag<br>- Mở màn chi tiết friend P trên 2 tab trình duyệt cùng lúc | 1. Ở tab 1, bấm 「削除」, rồi **double-click nhanh** nút 「削除する」 trong hộp thoại<br>2. Ghi lại thông báo hiển thị<br>3. Mở tab 2 (vẫn đang ở màn chi tiết friend P đã bị xóa), bấm 「削除」 → 「削除する」<br>4. Ghi lại thông báo của tab 2<br>5. Kiểm tra danh sách bạn bè và đếm bản ghi giới hạn tag của friend P | Double-click 1 nút; sau đó xóa lại chính friend đã bị xóa từ tab thứ 2 | - Tab 1: friend P bị xóa **đúng 1 lần**, không phát sinh lỗi hệ thống<br>- Tab 2: hiển thị **thông báo lỗi đúng nguyên nhân** (friend không còn tồn tại), **không** báo "xóa thành công", **không** trả lỗi 500<br>- Bản ghi giới hạn tag của friend P = 0, không có bản ghi lạ phát sinh | | **Lấp Q3** (kịch bản 1 + 2 của CONC-001) · Evidence: ảnh thông báo 2 tab + số bản ghi DB sau thao tác · Đánh giá spec: Spec không ghi — cần hỏi Leader hành vi mong đợi của tab 2 |
| TC-PERM002-01 | API | PERM-002 | API xóa bạn từ ứng dụng di động 「/api/mobile/delete-friend」 | Abnormal | manual | staging | Gọi API xóa friend với mã bot KHÔNG thuộc quyền tài khoản → bị từ chối, dữ liệu bot kia nguyên vẹn | - Tài khoản quản trị X chỉ quản lý bot B1<br>- Tài khoản quản trị Y quản lý bot B2 (X **không** có quyền trên B2)<br>- Bot B2 có friend K đang có 1 bản ghi giới hạn tag<br>- Có token đăng nhập app hợp lệ của tài khoản X<br>- Ghi lại số friend và số bản ghi giới hạn tag của bot B2 trước khi thử | 1. Đăng nhập app bằng tài khoản X, xác nhận app chỉ hiển thị bot B1<br>2. Dùng công cụ gọi API (DevTools/Postman) với token của X, gọi endpoint xóa friend nhưng truyền **mã bot = B2** và **mã friend = K**<br>3. Ghi lại mã trạng thái và nội dung phản hồi<br>4. Đăng nhập tài khoản Y, mở danh sách bạn bè bot B2, tìm friend K<br>5. Đếm lại bản ghi giới hạn tag của friend K ở bot B2 | Token của tài khoản X + tham số `bot_id` = B2 (bot không thuộc quyền), `friend_id` = K | - Endpoint **TỪ CHỐI** (trả lỗi phân quyền, không trả 'ok')<br>- Friend K **vẫn còn** trong danh sách bạn bè của bot B2<br>- Bản ghi giới hạn tag của K ở bot B2 **vẫn còn nguyên** (đếm không đổi)<br>- ⚠️ Nếu xóa được → lỗ hổng phân quyền nghiêm trọng: API nhận `bot_id` từ tham số request nên bất kỳ tài khoản nào cũng xóa được friend của bot người khác | | **Lấp Q1.** Evidence: mã lỗi trả về + ảnh danh sách bạn bè bot B2 sau khi thử · Đánh giá spec: Spec không ghi — `friend-list` §9 Gaps #7 tự nhận controller không có middleware phân quyền riêng; đã hỏi Leader · Căn cứ: Dev tự nêu "endpoint nhận mã bot từ tham số request (không lấy từ phiên web)" · Chạy kèm kho `TC-FRL-151` |
| TC-ENV003-01 | UI | ENV-003 | Smoke luồng ticket #40708 trên PRODUCTION | Normal | manual | product | Kịch bản gốc của ticket chạy đúng trên production sau deploy | - Bot PRODUCTION đã deploy nhánh có fix<br>- Tag A 「制限する」/「制限人数」 nhỏ và đang đầy, có action giới hạn gắn tag B, chế độ 「1度のみアクション稼働」<br>- Có 1 tài khoản LINE thật để kết bạn / hủy kết bạn được<br>- Đã thống nhất với Leader về việc thao tác trên bot thật | 1. Cho tài khoản LINE thật kết bạn với bot, xác nhận xuất hiện ở danh sách bạn bè<br>2. Vào chi tiết friend, tab 「タグ」, tick tag A rồi lưu → xác nhận tag B được gắn<br>3. Bấm 「削除」 → 「削除する」 để xóa friend<br>4. Từ app LINE, kết bạn lại với bot<br>5. Vào chi tiết friend vừa kết bạn lại, tab 「タグ」, tick tag A rồi lưu<br>6. Quan sát tag của friend trên màn hình | 1 tài khoản LINE thật, tag A đang đầy, chế độ 「1度のみアクション稼働」 | - Bước 2: tag A không được gắn, **tag B ĐƯỢC gắn**<br>- Bước 5–6 (sau khi xóa và kết bạn lại): **tag B ĐƯỢC gắn lại** đúng như lần đầu<br>- Không lỗi trên màn hình ở mọi bước | | **Lấp Q2 + G6.** RULE-08: job nền + luồng gốc bắt buộc kết luận trên PRODUCTION; production tách **3 job độc lập** (callback/broadcast/scenario) khác hẳn dev/staging chạy 1 job all · Evidence: ảnh tab タグ ở bước 2 và bước 6 chụp trên production · ⚠️ Thao tác trên bot thật — thống nhất phạm vi với Leader trước |
| TC-JOB001-01 | Data | JOB-001 | Lệnh dọn bản ghi mồ côi 「recover:actionLimitTagOrphan」 | Abnormal | manual | product | Dừng lệnh giữa chừng rồi chạy lại → không sót, không xóa nhầm, tổng số khớp | - Môi trường đã deploy nhánh có fix<br>- Đã dựng 20 bản ghi mồ côi và 5 bản ghi hợp lệ (trong đó 1 của friend đang bị chặn)<br>- Đã backup bảng `action_limit_tags`<br>- Ghi lại danh sách khóa chính của cả 25 bản ghi | 1. Ghi nhận 20 mồ côi + 5 hợp lệ<br>2. Chạy lệnh với kích thước lô nhỏ (ví dụ 5) ở chế độ xóa thật<br>3. Khi lệnh đang chạy giữa chừng, **dừng lệnh** (Ctrl+C)<br>4. Đọc log, ghi lại số bản ghi đã xóa tính tới lúc dừng<br>5. Chạy lại lệnh từ đầu ở chế độ xóa thật cho tới khi kết thúc<br>6. Cộng số đã xóa của 2 lần chạy, đối chiếu với 20<br>7. Đối chiếu 5 bản ghi hợp lệ theo danh sách khóa chính | 20 mồ côi + 5 hợp lệ; lô = 5; dừng giữa chừng rồi chạy lại | - Tổng số bản ghi xóa của 2 lần chạy = **đúng 20**, không sót, không xóa trùng<br>- **5 bản ghi hợp lệ còn nguyên**, kể cả bản ghi của friend đang bị chặn<br>- Lần chạy thứ 2 không báo lỗi vì dữ liệu đã bị xóa dở<br>- Log ghi rõ điểm dừng và điểm tiếp tục | | **Lấp Q6** (ý (4) JOB-001: số vào = số xử lý + số lỗi, không bản ghi nào biến mất) · RULE-08: job/command → PRODUCTION · Evidence: log 2 lần chạy + bảng đối chiếu khóa chính trước/sau · ⚠️ XÓA VĨNH VIỄN — backup trước khi chạy |
| TC-JOB001-02 | Data | JOB-001 | Lệnh dọn bản ghi mồ côi — log | Normal | manual | product | Log của lệnh ghi đủ start/end + số bản ghi mỗi lô và khớp số thực xóa | - Môi trường đã deploy nhánh có fix<br>- Đã dựng 12 bản ghi mồ côi<br>- Đã backup bảng `action_limit_tags`<br>- Biết đường dẫn file log ứng dụng | 1. Ghi nhận 12 bản ghi mồ côi<br>2. Ghi lại thời điểm bắt đầu<br>3. Chạy lệnh với kích thước lô = 5 ở chế độ xóa thật<br>4. Mở file log ứng dụng, tìm các dòng của lệnh này<br>5. Đối chiếu: có dòng bắt đầu, có dòng kết thúc, có số bản ghi xóa của **từng lô**<br>6. Cộng số của các lô, so với 12 và so với số thực tế biến mất khỏi bảng | 12 bản ghi mồ côi, lô = 5 (chia thành 3 lô: 5 + 5 + 2) | - Log có **dòng bắt đầu** và **dòng kết thúc** kèm thời điểm<br>- Log ghi số bản ghi xóa của **từng lô**: 5, 5, 2<br>- Tổng số trong log = **12** = số bản ghi thực tế biến mất khỏi bảng<br>- Không có dòng lỗi bất thường | | **Lấp G8 + Q5.** RULE-08: job/command → PRODUCTION · Evidence: trích đoạn log + bảng đếm trước/sau · Lý do bắt buộc: xóa vĩnh viễn không hoàn tác được → log là **bằng chứng duy nhất** về phạm vi đã xóa (RULE-04) |
| TC-DATABACKUP001-01 | Data | DATA-BACKUP-001 | Lệnh recover × データコピー (backup bot) | Abnormal | manual | product | Chạy lệnh recover trong lúc bot đang backup → không làm hỏng bản copy | - Bot B1 có ≥ 10 bản ghi mồ côi và ≥ 5 bản ghi hợp lệ<br>- Đã backup bảng `action_limit_tags`<br>- Biết cách khởi động chức năng データコピー / backup của bot B1 và biết nó chạy đủ lâu | 1. Ghi lại số bản ghi mồ côi và hợp lệ của bot B1<br>2. Khởi động chức năng backup / データコピー cho bot B1<br>3. Xác nhận bot đang ở trạng thái backup (thử sửa 1 tag → hệ thống chặn với thông báo 「データコピー中は…編集不可能です」)<br>4. **Trong lúc backup đang chạy**, chạy lệnh recover ở chế độ `--dry-run`, ghi lại số<br>5. Chạy lệnh recover ở chế độ xóa thật<br>6. Chờ backup kết thúc, mở bot đích/bản copy, kiểm tra dữ liệu tag và friend<br>7. Đếm lại bản ghi hợp lệ của bot B1 | 10 mồ côi + 5 hợp lệ; lệnh recover chạy xen giữa lúc backup đang chạy | - Bản copy sau khi backup xong **không bị thiếu / lệch** dữ liệu tag và friend<br>- 5 bản ghi hợp lệ của bot B1 **còn nguyên**<br>- ⚠️ Nếu bản copy bị lệch → lệnh recover phải được bổ sung kiểm tra khóa backup như mọi thao tác CUD khác, hoặc phải ghi rõ trong hướng dẫn vận hành là **cấm chạy khi có bot đang backup** | | **Lấp G7.** Căn cứ spec: `tag-management/BR-02` — cấm mọi CUD khi bot đang backup (`BackupHistory status IN (0,1)`); lệnh chạy bằng CLI nên **nằm ngoài lớp khóa này** · RULE-08: job/command → PRODUCTION · Evidence: ảnh trạng thái backup + đối chiếu dữ liệu bản copy · Đánh giá spec: Spec không ghi — cần hỏi Leader/BA |
| TC-DATAAUDIT001-01 | UI | DATA-AUDIT-001 | Lịch sử thao tác xóa bạn bè | Normal | manual | staging | Xóa friend từ từng nguồn (web / app / hàng loạt) → mỗi lần đúng 1 bản ghi lịch sử đủ thông tin | - Bot B1 có 3 friend tương đương nhau, mỗi friend có 1 bản ghi giới hạn tag<br>- Có tài khoản đăng nhập web và app cùng quyền<br>- Biết vị trí màn lịch sử thao tác 「操作履歴」 của hệ thống | 1. Mở màn lịch sử thao tác, ghi lại số bản ghi hiện có<br>2. Xóa friend 1 từ **màn chi tiết trên web**<br>3. Xóa friend 2 từ **app di động**<br>4. Xóa friend 3 bằng **xóa hàng loạt** trên màn 「ブロックされた友だち」<br>5. Mở lại màn lịch sử thao tác<br>6. Với mỗi bản ghi mới, kiểm tra đủ 4 thông tin: người thực hiện / thời gian / hành động / đối tượng bị tác động | 3 friend, 3 nguồn thao tác khác nhau | - Sinh **đúng 3 bản ghi lịch sử mới**, mỗi thao tác 1 bản ghi, không thiếu không trùng<br>- Mỗi bản ghi có đủ **người thực hiện · thời gian · hành động (ghi đúng là "xóa") · friend bị xóa**<br>- Không bản ghi nào ghi nhầm loại thao tác (ví dụ xóa ghi thành cập nhật) | | **Lấp Q5.** Evidence: ảnh/export bản ghi lịch sử sau **mỗi** thao tác, cho **từng nguồn** · Đánh giá spec: Spec không ghi — cần Leader xác nhận hệ thống có màn 操作履歴 cho thao tác xóa friend không; nếu **không có** thì đây là phát hiện thiếu audit cho thao tác xóa PII, đưa lên §6 · Checklist ghi DATA-AUDIT-001 là lỗ hổng thật với **10/10 ticket Closed** |
| TC-REGRUN001-01 | UI | REG-RUN-001 | Xóa bạn bè × job đang chạy dở | Abnormal | manual | product | Xóa friend đang có action hàng loạt chờ chạy → job còn lại chạy đúng, không lỗi vì friend đã biến mất | - Bot PRODUCTION có > 200 friend (ngưỡng đẩy sang job nền theo `friend-list/BR-05`)<br>- Chọn sẵn 3 friend mẫu trong nhóm sẽ chạy, trong đó 1 friend có bản ghi giới hạn tag<br>- Ghi lại danh sách friend trong nhóm | 1. Ở màn danh sách bạn bè, tích 「全選択」 chọn > 200 friend<br>2. Chạy action hàng loạt (gắn tag + gửi text) để hệ thống tạo lịch chạy nền<br>3. **Trong lúc job đang chạy dở**, mở màn chi tiết 1 friend mẫu thuộc nhóm đó và bấm 「削除」 → 「削除する」<br>4. Chờ job chạy xong<br>5. Kiểm tra màn 配信エラー có bản ghi lỗi bất thường không<br>6. Đếm số friend còn lại được gắn tag, đối chiếu với số friend còn lại trong nhóm<br>7. Kiểm tra bản ghi giới hạn tag của friend đã xóa | > 200 friend trong 1 action hàng loạt; xóa 1 friend giữa chừng | - Job **chạy tiếp tới khi hoàn tất**, không dừng, không lặp vô hạn<br>- Số friend được gắn tag = số friend còn lại trong nhóm (nhóm ban đầu trừ friend đã xóa)<br>- Màn 配信エラー **không** phát sinh lỗi hệ thống do friend biến mất giữa chừng<br>- Bản ghi giới hạn tag của friend đã xóa = 0, **không có bản ghi mồ côi mới** do job ghi lại sau khi friend đã bị xóa | | **Lấp Q7** (và bổ trợ G5 ở phía job) · RULE-08: job nền → PRODUCTION · Evidence: log job + số đếm friend được gắn tag + ảnh màn 配信エラー · Nối tiếp kho `TC-FRL-119/120` (job action schedule) |
| TC-REGSHARED001-06 | UI | REG-SHARED-001 | Màn bạn bè đang ẩn 「非表示中の友だち」 — `/basic/friendlist/hidden` (spec SCR-FRL-04) | Normal | manual | staging | Xóa hàng loạt trên màn 「非表示中の友だち」 → dọn bản ghi giới hạn tag của mọi friend được chọn | - Bot B1 có tag A 「制限する」 đang đầy, action giới hạn gắn tag B, chế độ 「1度のみアクション稼働」<br>- 3 friend H1, H2, H3 đều đã kích hoạt action → mỗi friend có đúng 1 dòng `action_limit_tags`<br>- Cả 3 đang ở trạng thái ẩn nên hiển thị trên màn 「非表示中の友だち」<br>- Bot B1 còn ≥ 1 friend khác (H4) **KHÔNG** ở trạng thái ẩn và có 1 dòng `action_limit_tags` — dùng làm đối chứng<br>- Ghi lại số dòng của từng friend H1–H4 | 1. Mở màn 「非表示中の友だち」 từ menu danh sách bạn bè (`/basic/friendlist/hidden`)<br>2. Xác nhận cả 3 friend H1, H2, H3 đang hiển thị; ghi nhận `action_limit_tags` có đúng 1 dòng cho mỗi friend<br>3. Tích 「全選択」 để chọn cả 3 friend<br>4. Ở panel xóa hàng loạt, bấm 「削除」 và xác nhận trên hộp thoại<br>5. Chờ màn hình tải lại, kiểm tra danh sách còn friend nào không<br>6. Đếm lại `action_limit_tags` cho cặp bot B1 + từng friend H1, H2, H3<br>7. Đếm lại của friend đối chứng H4 và xác nhận H4 vẫn nằm trong danh sách bạn bè | 3 friend đang ẩn, mỗi friend 1 bản ghi giới hạn tag; 1 friend đối chứng H4 không ẩn, cũng có 1 bản ghi | - Cả 3 friend biến mất khỏi màn 「非表示中の友だち」, màn hình tải lại được, không hiện lỗi<br>- `action_limit_tags` **không còn dòng nào** cho cả 3 friend (đếm = 0 từng friend)<br>- Friend đối chứng **H4 VẪN còn** trong danh sách và bản ghi của H4 **VẪN nguyên** (đếm = 1) — xóa hàng loạt không lan ra ngoài danh sách được tick | | **Lấp G10.** Căn cứ màn này có nhánh bulk: note `TC-TOOLSCOPE001-01` ghi `hiden.js:83,101` gọi đúng **hai** endpoint xóa bạn đã chặn; spec `EP-15 POST /ajax/friendlist/delete-multiple-user-block`; kho `TC-FRL-136` xác nhận có 「全選択」+「削除」 ở panel · Bulk đi qua handler riêng `deleteUserBlockAction` (`:4026-4133`), **khác** handler xóa đơn `deleteUserBlock` (`:3635-4023`) · Evidence: ảnh danh sách trước/sau + query đếm từng friend · ⚠️ Lệch mã màn: Studio gắn SCR-FL-06, spec ghi 非表示中 = **SCR-FRL-04** · regression |
| TC-REGSHARED001-07 | UI | REG-SHARED-001 | Màn bạn bè bị bot chặn 「ブロックした友だち」 — `/basic/friendlist/block` (spec SCR-FRL-06) | Boundary | manual | staging | Xóa hàng loạt lô **HỖN HỢP** trên màn 「ブロックした友だち」 → vòng lặp không dừng khi gặp friend không có bản ghi | - Bot B1 có tag A đang đầy với action giới hạn gắn tag B<br>- 4 friend Q1–Q4 đều bị quản trị viên chặn nên hiển thị trên màn 「ブロックした友だち」 (`blocked_by=1`)<br>- **Q1 và Q3 ĐÃ** kích hoạt action → mỗi friend 1 dòng `action_limit_tags`<br>- **Q2 và Q4 CHƯA** từng kích hoạt → 0 dòng<br>- Cả 4 friend đều đã có dữ liệu lịch sử: từng được gắn/gỡ 1 tag thường và từng đổi rich menu<br>- Ghi lại: `action_limit_tags` đang có đúng 2 dòng (Q1, Q3) + dữ liệu lịch sử của cả 4 | 1. Mở màn 「ブロックした友だち」 từ menu danh sách bạn bè (`/basic/friendlist/block`)<br>2. Xác nhận cả 4 friend Q1–Q4 đang hiển thị; ghi nhận `action_limit_tags` đang có đúng 2 dòng (Q1, Q3)<br>3. Tích 「全選択」 chọn cả 4 friend — lô hỗn hợp: 2 friend **CÓ** bản ghi, 2 friend **KHÔNG** có<br>4. Ở panel xóa hàng loạt, bấm 「削除」 và xác nhận trên hộp thoại<br>5. Chờ màn hình tải lại, đếm số friend còn lại và đối chiếu danh sách đã ghi<br>6. Đếm lại `action_limit_tags` cho từng friend Q1, Q2, Q3, Q4<br>7. Kiểm tra dữ liệu lịch sử (lịch sử tag, lịch sử rich menu) của cả 4 friend đã được dọn chưa | Lô 4 friend bị quản trị viên chặn, tick toàn bộ: Q1 và Q3 có bản ghi giới hạn tag, Q2 và Q4 không có | - Cả 4 friend biến mất khỏi màn 「ブロックした友だち」 — **KHÔNG SÓT** friend nào, đặc biệt **Q4** (friend cuối lô và không có bản ghi giới hạn tag) vẫn phải bị xóa<br>- `action_limit_tags` = 0 dòng cho cả 4 friend<br>- Dữ liệu lịch sử (tag, rich menu) của **CẢ 4** friend đều được dọn như trước khi có fix<br>- Màn hình tải lại được, không báo lỗi, không có lỗi trong log ứng dụng của luồng xóa<br>- ⚠️ Nếu chỉ một phần lô bị xóa (VD Q1, Q2 mất nhưng Q3, Q4 còn) → vòng lặp xóa đã bị ngắt giữa chừng → raise bug ngay | | **Lấp G10.** Kho `TC-FRL-137` xác nhận màn `/block` có 「全選択」+「削除」 · Biên chọn là **lô hỗn hợp** vì hàm dọn được gọi **trong vòng lặp từng friend** và bước xóa `action_limit_tags` mới thêm nằm **cùng khối try/catch** với các bước dọn cũ (`functions.php:11940-11953`) → lỗi ở friend không có bản ghi có thể chặn các friend sau trong lô. `TC-REGSHARED001-01` đã phủ nhánh "không có bản ghi" nhưng **chỉ ở đường xóa ĐƠN**, không phủ vòng lặp · Evidence: ảnh danh sách trước/sau + query đếm từng friend + log luồng xóa · ⚠️ Lệch mã màn: Studio gắn SCR-FL-04, spec ghi ブロックした = **SCR-FRL-06** · regression |
| TC-OUTTRUTH001-01 | UI | OUT-TRUTH-001 | Xóa bạn bè — thông báo kết quả | Abnormal | manual | staging | Bước dọn bản ghi giới hạn tag lỗi → hệ thống KHÔNG báo xóa thành công sai sự thật | - Bot B1 có friend R với đầy đủ dữ liệu lịch sử và 1 bản ghi giới hạn tag<br>- Phối hợp Dev để ép bước xóa `action_limit_tags` ném lỗi (ví dụ tạm khóa quyền ghi trên bảng đó ở môi trường test)<br>- Ghi lại toàn bộ dữ liệu lịch sử của friend R trước khi thao tác | 1. Xác nhận friend R còn trong danh sách và có đủ dữ liệu lịch sử<br>2. Nhờ Dev bật điều kiện gây lỗi ở bước xóa bản ghi giới hạn tag<br>3. Mở màn chi tiết friend R, bấm 「削除」 → 「削除する」<br>4. Ghi lại **nguyên văn thông báo** hiển thị trên màn hình<br>5. Kiểm tra friend R còn trong danh sách bạn bè không<br>6. Kiểm tra các bảng lịch sử của friend R đã bị dọn hay còn nguyên<br>7. Gỡ điều kiện gây lỗi | 1 friend đầy đủ dữ liệu; ép lỗi ở đúng bước DELETE mới thêm | - Thông báo trên màn hình **khớp trạng thái thật**: nếu dữ liệu chưa được dọn xong thì **KHÔNG** được hiện "xóa thành công"<br>- Trạng thái dữ liệu **nhất quán**: hoặc xóa trọn vẹn, hoặc giữ nguyên hoàn toàn — **không** để friend biến mất khỏi danh sách mà dữ liệu lịch sử còn sót lại<br>- Có bản ghi lỗi trong log ứng dụng nêu đúng nguyên nhân | | **Lấp AP-3 (§4 #13) + bổ trợ G2.** Đây là rủi ro do chính note của `TC-REGSHARED001-01` nêu: bước mới nằm **cùng khối try/catch** với 4 lệnh dọn cũ (`functions.php:11940-11953`) → lỗi ở bước mới có thể làm các bước sau không chạy · Evidence: ảnh thông báo + đối chiếu dữ liệu trước/sau + log lỗi · Đánh giá spec: Spec không ghi — cần Leader chốt hành vi mong đợi |

> **RULE-01** — lý do thiếu loại case ở các quan điểm Cao mới đề xuất: `PERF-LARGE-001` chỉ có Boundary (quan điểm này **chỉ có ý nghĩa ở biên quy mô**, nhánh thuận đã nằm trong TC chức năng) · `CONC-001` chỉ có Abnormal (race **không có nhánh thuận** và **không có khái niệm biên**) · `PERM-002` chỉ có Abnormal (nhánh thuận = tài khoản đủ quyền, đã có `TC-REGSHARED001-04`) · `DATA-AUDIT-001` / `OUT-TRUTH-001` / `REG-RUN-001` / `DATA-BACKUP-001` — đề xuất tối thiểu, mở rộng sau khi Leader duyệt hướng.

---

## 6. Spec update needed

| # | Điểm cần chốt | Nguồn phát hiện | Ai chốt |
|---|---|---|---|
| 1 | **Phạm vi lệnh recover — orphan do nguyên nhân KHÁC**: lệnh dọn mọi dòng mồ côi kể cả khi **bot đã bị xóa**, rộng hơn phạm vi bug #40708. Dev tự cảnh báo, `TC-DATARETENTION001-02` phải dừng ở dry-run chờ quyết định | `TC-DATARETENTION001-02` + mục 7 `03-dev-impact.md` | Leader + BA |
| 2 | **Friend kết bạn lại TRƯỚC khi chạy recover**: khi đó `bot_line_user` đã tồn tại trở lại nên **không còn là orphan** → lệnh bỏ qua, bản ghi cũ **vẫn chặn nghiệp vụ**. `TC-TOOLKNOW002-02` ghi rõ đây là FAIL nếu lệnh bỏ qua nhóm này | `TC-TOOLKNOW002-02` (do `cucdtk@mcp` bổ sung) | Leader + BA |
| 3 | **Block / bỏ theo dõi / gỡ tag ở màn quản lý tag vẫn giữ bản ghi tracking** — Dev ghi "chờ BA xác nhận". Hiện `TC-TOOLNEGCTRL001-01` đang **mặc định hành vi này là ĐÚNG** và lấy làm expected, trong khi chưa ai chốt | Mục 7 `03-dev-impact.md` + `TC-TOOLNEGCTRL001-01` | BA |
| 4 | **Spec `friend-list/BR-06` chưa liệt kê `action_limit_tags`** trong danh sách cascade khi xóa friend (chỉ ghi chung "cascade ~20+ bảng"). Sau fix cần ghi rõ bảng này thuộc phạm vi cascade | `spec-features/admin/friend-list/feature-spec.md` §5 BR-06 | Dev + BA |
| 5 | **Spec `tag-management/BR-06` chưa mô tả tương tác với vòng đời friend**: BR-06 chỉ nói "`limit_action_mode=0` → trigger 1 lần/user, tracking qua `ActionLimitTag`", không nói điều gì xảy ra khi user bị xóa rồi kết bạn lại — đúng chỗ sinh ra bug | `spec-features/admin/tag-management/feature-spec.md` §5 BR-06 | Dev + BA |
| 6 | **Phân quyền tầng API của màn friendlist** — kho FA-013 đã ghi nhận là vùng mù (`MT-12`), spec §9 Gaps #7 tự nhận "controller KHÔNG có middleware phân quyền riêng". Ticket này thêm 1 thao tác xóa dữ liệu nữa đi qua chính các endpoint đó | Kho `MT-12` + `TC-FRL-151` + Q1 | Leader + Dev |
| 7 | **Ngưỡng thời gian chấp nhận được của thao tác xóa friend** — chưa có chuẩn; `TC-PERFLARGE001-01` và kho `TC-FRL-142` đều phải để Leader chốt ngưỡng | G1 + kho `TC-FRL-142` (Bug #38390) | Leader |
| 8 | **Có audit log cho thao tác xóa friend hay không** — nếu hệ thống không ghi lịch sử cho thao tác xóa PII thì đây là thiếu sót cần bổ sung spec, không chỉ là thiếu TC | Q5 / `TC-DATAAUDIT001-01` | Leader + BA |
