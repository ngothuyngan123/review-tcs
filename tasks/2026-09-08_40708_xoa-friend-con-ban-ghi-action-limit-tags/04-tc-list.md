<!-- sync-tcs: url=<chưa có — Redmine #40708 KHÔNG có Link TCs human> | sheet=<chưa có> | anchor=Main Function -->
<!-- source: MCP LME TEST STUDIO — task_id=283, ticket 40708, testcase_list (22 TC), fetch lúc 2026-09-08. Redmine KHÔNG có Link TCs human. -->

# 04 — TC List (fetch từ MCP LME TEST STUDIO)

> ⚠️ **ĐÂY KHÔNG PHẢI TC DO MEMBER NGƯỜI VIẾT.** Redmine #40708 không có section "Link TCs" nên bộ TC dưới đây được fetch từ **MCP LME TEST STUDIO** (task #283, ticket 40708).
> **TCs là read-only** — chép nguyên văn từ Studio. Muốn sửa thì sửa trên Studio (`testcase_update`) rồi fetch lại, KHÔNG sửa tay ở file này.

## ⚠️ Cảnh báo bắt buộc đọc trước khi review

### 1. Nguồn & tác giả TC

| Mục | Giá trị |
|---|---|
| Studio task | `#283` — "Khi xóa friend chưa xóa bản ghi khỏi bảng action_limit_tags" |
| Type / Feature | `fix-bug` / `friend-list` |
| Status / Pipeline | `running` — `pipelineStage=running`, `round=1`, `aiResult=null` |
| Branch | `ai_small_40708` |
| Review state | `leader` — `reviewed=false` (**chưa ai duyệt**) |
| Task tạo bởi | `cucdtk` lúc `2026-09-08 09:33:38` |
| Tổng số TC | **22** |
| **Tác giả TC** | **20 TC do AI sinh** (`provenance.source=ai`, `created_job_id=880`) + **2 TC do người viết qua MCP** (`provenance.source=human`, actor `cucdtk@mcp`: `TC-DATADB001-03`, `TC-TOOLKNOW002-02` — bổ sung sau review) |

### 2. Tình trạng chạy — **thực tế CHƯA CÓ TC NÀO ĐƯỢC CHẠY**

| Kết quả | Số TC |
|---|---|
| `pass` (Đạt) | **0** |
| `fail` (Không đạt) | **0** |
| `skip` / other | **1** |
| Chưa chạy | **21** |

- **0 TC pass, 0 TC fail** → bộ TC này **chưa cung cấp bằng chứng gì** về chất lượng bản fix. Không được coi ticket là đã verify.
- TC duy nhất có `last_exec` là `TC-REGSHARED001-05` (Studio #16714, job nền) — kết quả **`skip`**, chạy bởi `pipeline-manual` (nguồn `ai`, không phải QA người chạy) trên `env=local` lúc `2026-09-08 11:15:53`. Chính `note` của TC đã ghi: nếu không dựng được job nền thì **trả skip kèm lý do**, TUYỆT ĐỐI không thay bằng thao tác trên web rồi kết luận pass.
- **Không có TC nào raise bug ticket** (`bug_tickets` rỗng ở cả 22 TC) — nhất quán với việc chưa chạy.

### 3. Môi trường — ⚠️ RULE-08

- `env_scope` của **cả 22 TC** đều khai `["all"]` (Tất cả), nhưng `env_tag` thực tế là **`local-only`** ở 21 TC và `read-only` ở 1 TC (`TC-DATADB001-04`).
- Thống kê `envAuto` của task: **`local` có 1 run**, `dev` / `staging` / `prd` đều **0 run**; tỷ lệ automated = **0%** ở mọi env.
- ⚠️ **RULE-08**: task này chạm **job nền** (`TC-REGSHARED001-05`) và **command xóa dữ liệu vĩnh viễn** (`recover:actionLimitTagOrphan`) → **không được kết luận từ local/staging**. Hiện chưa có bất kỳ run nào ở `staging` hay `prd`.

### 4. ⚠️ Mã quan điểm Studio KHÔNG có trong `framework/checklist-lme.md`

`/review-tc` sẽ **không map được coverage** cho các mã dưới đây (đã grep đối chiếu):

| Mã quan điểm Studio | Có trong checklist-lme.md? | Số TC dùng |
|---|---|---|
| `DATA-DB-001` | ✅ CÓ | 4 |
| `REG-SHARED-001` | ✅ CÓ | 5 |
| `FUNC-004` | ✅ CÓ | 1 |
| `TOOL-KNOW-002` | ❌ **KHÔNG** | 2 |
| `TOOL-NEGCTRL-001` | ❌ **KHÔNG** | 3 |
| `TOOL-AXIS-001` | ❌ **KHÔNG** | 1 |
| `TOOL-SCOPE-001` | ❌ **KHÔNG** | 1 |
| `SELECT-SCOPE-001` | ❌ **KHÔNG** | 1 |
| `JOB-002` | ❌ **KHÔNG** (checklist chỉ có `JOB-001`) | 2 |
| `DATA-RETENTION-001` | ❌ **KHÔNG** | 2 |

→ **7/10 mã (12/22 TC)** dùng bộ mã riêng của Studio, không thuộc 80 quan điểm LME. Giữ nguyên theo quy tắc `/new-task` (KHÔNG tự map sang mã checklist).

### 5. Phân bố

| Chiều | Phân bố |
|---|---|
| `case_type` | `Normal` **14** · `Boundary` **5** · `Abnormal` **3** |
| `tc_group` | `ui` **11** · `job` **8** *(gồm 2 TC do người viết)* · `api` **2** · `data` **1** |
| `exec_mode` | `auto` **22/22** (không TC nào `manual`) |
| `screen` | Chi tiết bạn bè SCR-FL-07 (6) · Màn đã chặn OA SCR-FL-05 (3) · Màn bot chặn SCR-FL-04 (1) · Màn đang ẩn SCR-FL-06 (1) · API app EP-23 (2) · Command recover (5) · Recovery+gắn tag (2) · Job nền linect (1) · Bảng `action_limit_tags` (1) |
| `requirement_keys` | REQ-001 (3) · REQ-002 (3) · REQ-003 (2) · REQ-004 (1) · REQ-005 (1) · REQ-006 (3) · REQ-007 (1) · REQ-008 (5) · REQ-009 (1) · REQ-010 (1) · REQ-011 (3) · **2 TC human KHÔNG gắn requirement nào (rỗng)** |
| `spec_status` | `null` ở **cả 22 TC** → cột "Trạng thái đánh giá spec" để trống toàn bộ |
| `status` | `draft` ở **cả 22 TC** |

### 6. Điểm Studio tự đánh dấu là CHƯA CHỐT (cần Leader/BA quyết)

- `TC-DATARETENTION001-02` (#16713): command recover dọn **cả orphan do bot đã bị xóa** — rộng hơn phạm vi bug gốc. TC yêu cầu **DỪNG ở dry-run**, không chạy xóa thật, chờ Leader/BA xác nhận phạm vi.
- `TC-TOOLKNOW002-02` (#16719, human bổ sung): friend đã **kết bạn lại TRƯỚC** khi chạy recover thì `bot_line_user` đã tồn tại lại → điều kiện orphan **không bắt được**, record cũ vẫn chặn nghiệp vụ. TC ghi rõ: nếu command bỏ qua nhóm này thì **FAIL** và phải chốt lại phạm vi recovery.
- `TC-JOB002-01` / `TC-FUNC004-01`: tên lệnh `recover:actionLimitTagOrphan` và option `--dry-run` / `--chunk` **lấy từ report của Dev, chưa đọc trực tiếp source** — khi chạy phải đối chiếu `php artisan list` thật, lệch thì báo Leader chứ không tự đổi expected.
- `TC-TOOLSCOPE001-01` (#16701): màn 「非表示中の友だち」 **KHÔNG nằm trong danh sách vùng ảnh hưởng** Dev kê, là phát hiện bổ sung của bước viết TC — cần Leader ghi nhận.

---

## Thông tin

| Trường | Giá trị |
|---|---|
| Tester viết TCs | AI Studio (job #880) — 20 TC · `cucdtk@mcp` — 2 TC |
| Ngày submit | 2026-09-08 (TC tạo `09:57`–`11:11`) |
| Version TCs | Studio round 1 — `status=draft`, `reviewed=false` |
| Link TC gốc (nếu có) | MCP LME TEST STUDIO — `task_id=283` |

---

## TC List

> Fetch nguyên văn từ Studio. Cột `Kết quả thực thi` map: `pass`→Đạt · `fail`→Không đạt · `skip`/`error`/null→Chưa test (raw status ghi ở Ghi chú).

| TC No. | Mã quan điểm liên kết | Loại case | Tiêu đề test case | Điều kiện tiền đề | Các bước thực hiện | Dữ liệu test/input | Kết quả mong đợi | Kết quả thực thi | Evidence thực tế | Môi trường test | Người thực hiện | Ngày thực hiện | Số ticket bug | Trạng thái đánh giá spec | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-DATADB001-01 | DATA-DB-001 | Normal | Xóa bạn từ màn chi tiết thì bản ghi giới hạn tag của bạn đó bị dọn sạch | Đã đăng nhập tool và đã chọn bot B1. Trong bot B1 có tag A tên 「TAG_A_40708_<mã run>」 đặt 「人数制限」=「制限する」, 「制限人数」=1 và đã có 1 bạn khác giữ tag A nên tag A đang đầy; phần 「制限到達後のタグ追加時アクション」 cấu hình một hành động gắn tag B 「TAG_B_40708_<mã run>」 với 「アクションの稼働回数」=「1度のみアクション稼働」. Bạn X 「FRIEND_X_40708_<mã run>」 đang là bạn hoạt động của bot B1 và chưa có tag A. | 1. Mở màn danh sách bạn bè 「友だちリスト」, bấm vào tên bạn X để vào màn chi tiết bạn bè.<br>2. Chuyển sang tab 「タグ」, tick chọn tag A rồi lưu lại.<br>3. Quan sát danh sách tag của bạn X trên màn hình: tag A không được gắn, tag B xuất hiện (hành động khi tag đầy đã chạy).<br>4. Kiểm tra bảng action_limit_tags: ghi nhận đang có đúng 1 dòng cho cặp bot B1 + bạn X.<br>5. Ở phần đầu màn chi tiết bạn bè, bấm nút 「削除」.<br>6. Trong hộp thoại 「友だちを削除してよろしいですか？」, bấm nút 「削除する」.<br>7. Chờ màn hình quay về danh sách bạn bè, kiểm tra lại bảng action_limit_tags cho cặp bot B1 + bạn X. | Tag A: 「制限する」/「制限人数」=1/「1度のみアクション稼働」/hành động gắn tag B. Bạn X: 1 bạn hoạt động chưa có tag A. | Bước gắn tag: tag A không được gắn cho bạn X, tag B được gắn (thấy trên tab 「タグ」) và bảng action_limit_tags có 1 dòng của bạn X trong bot B1. Sau khi bấm 「削除する」: bạn X biến mất khỏi danh sách bạn bè và bảng action_limit_tags KHÔNG còn dòng nào cho cặp bot B1 + bạn X. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16694 (NEW-1) · ui / auto / local-only · REQ-001 · TICKET-40708, friend-list/EP-17, friend-list/SCR-FL-07, friend-list/BR-07, tag-management/BR-05 · **Note Studio:** Đây là TC lõi của ticket. Oracle kỹ thuật: đếm SELECT COUNT(*) FROM action_limit_tags WHERE bot_id=<B1> AND line_user_id=<X> trước (=1) và sau (=0). Trước fix TC này fail ở bước cuối (vẫn còn 1 dòng). Hành động xóa BẮT BUỘC phát sinh từ nút 「削除」 trên browser (POST /basic/line_user/update type=deleteLineUser do FE gửi), không được gọi thẳng endpoint. |
| TC-TOOLKNOW002-01 | TOOL-KNOW-002 | Normal | Xóa bạn rồi kết bạn lại thì hành động khi tag đầy chạy lại được | Giống TC xóa bạn cơ bản: bot B1, tag A 「TAG_A_40708_<mã run>」 đang đầy (「制限人数」=1) với hành động gắn tag B 「TAG_B_40708_<mã run>」 ở chế độ 「1度のみアクション稼働」; bạn X 「FRIEND_X_40708_<mã run>」 là bạn hoạt động chưa có tag A. | 1. Mở màn chi tiết bạn bè của bạn X, vào tab 「タグ」, tick tag A rồi lưu — xác nhận tag B được gắn và action_limit_tags có đúng 1 dòng cho bot B1 + bạn X. Ghi lại id và number_action của dòng này.<br>2. Vào lại tab 「タグ」, tick tag A và lưu lần nữa.<br>3. Kiểm tra action_limit_tags: dòng của bot B1 + bạn X vẫn là cùng id và number_action không tăng — xác nhận hành động không chạy lại khi chưa xóa bạn.<br>4. Bấm 「削除」 rồi 「削除する」 để xóa bạn X và xác nhận action_limit_tags không còn dòng nào cho bot B1 + bạn X.<br>5. Dựng lại quan hệ kết bạn cho đúng người LINE của bạn X với bot B1, sao cho bạn X hiện lại trong danh sách bạn bè.<br>6. Mở màn chi tiết bạn bè của bạn X vừa kết bạn lại, vào tab 「タグ」, tick tag A rồi lưu.<br>7. Kiểm tra tag và action_limit_tags của bạn X. | Tag A đang đầy, chế độ 「1度のみアクション稼働」, hành động gắn tag B. Bạn X bị xóa rồi kết bạn lại (cùng người LINE). | Lần gắn tag thứ nhất: tag B được gắn và action_limit_tags tạo đúng 1 dòng. Lần gắn thứ hai khi chưa xóa bạn: dòng action_limit_tags giữ nguyên cùng id, number_action không tăng và hành động không chạy lại. Sau khi xóa bạn: dòng cũ bị xóa. Sau khi kết bạn lại và gắn tag A: tag B ĐƯỢC gắn lại và action_limit_tags tạo một dòng mới cho bot B1 + bạn X, khác id so với dòng đã ghi trước khi xóa. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16695 (NEW-2, version 2) · ui / auto / local-only · REQ-005, REQ-001 · TICKET-40708, friend-list/EP-17, friend-list/SCR-FL-07, tag-management/BR-05, tag-management/BR-06 · ⚠️ Mã quan điểm KHÔNG có trong checklist-lme.md · **Note Studio:** TC tái hiện bug theo đúng report gốc và xác nhận đã fix. Oracle chính dùng action_limit_tags.id/number_action để tránh false-pass khi tag B đã tồn tại: chỉ nhìn UI tag B không đủ chứng minh hành động có chạy lại hay không. Trước fix, sau delete→re-add dòng cũ còn sót nên handleLimitActionTag có thể chặn lần chạy mới. |
| TC-REGSHARED001-01 | REG-SHARED-001 | Normal | Xóa bạn không có bản ghi giới hạn tag vẫn thành công và vẫn dọn đủ các bảng lịch sử cũ | Bot B1 có bạn Y 「FRIEND_Y_40708_<mã run>」 chưa từng kích hoạt hành động khi tag đầy (không có dòng nào trong action_limit_tags) nhưng đã có lịch sử: đã từng được gắn/gỡ 1 tag thường, đã từng đổi rich menu, đã từng cập nhật một mục thông tin bạn bè và đã có lịch sử kết bạn. | 1. Mở màn chi tiết bạn bè của bạn Y và ghi nhận các bảng lịch sử của bạn Y đang có dữ liệu (lịch sử tag, lịch sử rich menu, lịch sử thông tin bạn bè, lịch sử kết bạn).<br>2. Bấm nút 「削除」.<br>3. Trong hộp thoại 「友だちを削除してよろしいですか？」, bấm 「削除する」.<br>4. Quan sát màn hình sau khi xóa và kiểm tra lại các bảng lịch sử của bạn Y. | Bạn Y: không có bản ghi action_limit_tags, có dữ liệu ở 4 bảng lịch sử. | Xóa thành công: không hiện lỗi trên màn hình, màn hình quay về danh sách bạn bè và bạn Y không còn trong danh sách. Toàn bộ dữ liệu ở 4 bảng lịch sử của bạn Y bị dọn như trước khi có fix; không có lỗi ghi trong log ứng dụng của luồng xóa. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16697 (NEW-4) · ui / auto / local-only · REQ-010 · TICKET-40708, friend-list/EP-17, friend-list/SCR-FL-07, friend-list/BR-07 · **Note Studio:** Hồi quy cho hàm dùng chung: bước xóa action_limit_tags được thêm vào cùng khối try/catch với 4 lệnh xóa lịch sử cũ (app/Helpers/functions.php:11940-11953). Nếu bước mới ném lỗi thì các bảng sau nó có thể không được dọn — TC này bắt đúng rủi ro đó, đồng thời phủ nhánh 'không có bản ghi nào để xóa'. Bảng liên quan: tag_history, rich_menu_history, friend_information_history, line_user_add_friend_history. |
| TC-TOOLNEGCTRL001-01 | TOOL-NEGCTRL-001 | Abnormal | Chặn bạn không làm mất bản ghi giới hạn tag và hành động vẫn không chạy lại | Bot B1, tag A đang đầy với hành động gắn tag B ở chế độ 「1度のみアクション稼働」. Bạn Z 「FRIEND_Z_40708_<mã run>」 đã kích hoạt hành động một lần nên đang có đúng 1 dòng trong action_limit_tags và đang ở trạng thái bạn bè bình thường (chưa bị chặn). | 1. Mở màn chi tiết bạn bè của bạn Z, ghi nhận action_limit_tags đang có đúng 1 dòng cho bot B1 + bạn Z; lưu lại id và number_action.<br>2. Bấm nút 「ブロック」 và xác nhận bằng 「ブロックする」.<br>3. Kiểm tra lại action_limit_tags: dòng của bot B1 + bạn Z vẫn còn, cùng id và number_action không đổi.<br>4. Bấm 「ブロック解除」 để bỏ chặn bạn Z.<br>5. Vào tab 「タグ」, tick tag A rồi lưu.<br>6. Kiểm tra lại action_limit_tags và trạng thái tag của bạn Z. | Bạn Z đang có 1 bản ghi giới hạn tag; thao tác chặn rồi bỏ chặn. | Sau khi chặn: bạn Z chuyển sang trạng thái bị chặn nhưng action_limit_tags vẫn giữ nguyên đúng cùng id và number_action. Sau khi bỏ chặn và gắn lại tag A: action_limit_tags vẫn không tạo dòng mới, id/number_action không đổi và hành động giới hạn không chạy lại. Đây là đối chứng xác nhận block/unblock không được coi như delete. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16698 (NEW-5, version 2) · ui / auto / local-only · REQ-007 · TICKET-40708, friend-list/BR-05, friend-list/SCR-FL-07, tag-management/BR-06 · ⚠️ Mã quan điểm KHÔNG có trong checklist-lme.md · **Note Studio:** Oracle dùng id/number_action của action_limit_tags để tránh false-pass khi tag B đã tồn tại từ lần chạy trước. Trọng tâm của TC là chứng minh block không cleanup record; nếu môi trường không bỏ chặn được do phụ thuộc LINE API thì vẫn phải xác nhận được record còn nguyên ngay sau block và ghi rõ phần còn lại bị chặn bởi môi trường. |
| TC-TOOLAXIS001-01 | TOOL-AXIS-001 | Boundary | Xóa bạn đang có nhiều bản ghi giới hạn tag thì dọn hết mọi bản ghi | Bot B1 có 2 tag đang đầy: tag A1 「TAG_A1_40708_<mã run>」 (「1度のみアクション稼働」) và tag A2 「TAG_A2_40708_<mã run>」 (「何度でもアクション稼働」), mỗi tag có một hành động gắn tag kết quả riêng. Bạn X 「FRIEND_X_40708_<mã run>」 là bạn hoạt động, chưa có tag A1/A2. | 1. Mở màn chi tiết bạn bè của bạn X, vào tab 「タグ」, tick tag A1 rồi lưu.<br>2. Vào lại tab 「タグ」, tick tag A2 rồi lưu.<br>3. Vào lại tab 「タグ」, tick tag A2 và lưu thêm 1 lần nữa (chế độ 「何度でもアクション稼働」 nên hành động chạy lại và số lần chạy tăng).<br>4. Kiểm tra bảng action_limit_tags: ghi nhận đang có 2 dòng cho cặp bot B1 + bạn X (một dòng theo tag A1, một dòng theo tag A2 với số lần chạy > 1).<br>5. Bấm 「削除」 rồi 「削除する」 để xóa bạn X.<br>6. Kiểm tra lại bảng action_limit_tags cho cặp bot B1 + bạn X. | 2 tag đã đầy với 2 chế độ chạy hành động khác nhau (「1度のみ」 và 「何度でも」), cùng gắn cho 1 bạn. | Trước khi xóa: có đúng 2 dòng action_limit_tags của bạn X trong bot B1, trong đó dòng của tag A2 có số lần chạy lớn hơn 1. Sau khi xóa bạn: cả 2 dòng đều biến mất (đếm = 0), không sót dòng nào theo tag hay theo chế độ nào. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16696 (NEW-3) · ui / auto / local-only · REQ-001 · TICKET-40708, friend-list/EP-17, friend-list/SCR-FL-07, tag-management/BR-06 · ⚠️ Mã quan điểm KHÔNG có trong checklist-lme.md · **Note Studio:** Biên của trục 'số bản ghi mỗi bạn' và trục 'chế độ chạy hành động' — lệnh xóa mới chỉ lọc theo bot_id + line_user_id nên phải xóa hết bất kể tag_id/action_id/number_action. Oracle: COUNT(*) WHERE bot_id=<B1> AND line_user_id=<X> đi từ 2 về 0. |
| TC-TOOLNEGCTRL001-02 | TOOL-NEGCTRL-001 | Boundary | Cùng một người LINE ở hai bot thì xóa bạn ở bot này không làm mất bản ghi ở bot kia | Cùng một người dùng LINE U là bạn của bot B1 và bot B2 (hồ sơ người dùng LINE dùng chung giữa các bot). Ở mỗi bot đều có 1 tag đang đầy với hành động riêng, và U đã kích hoạt hành động ở cả hai bot nên có 1 dòng action_limit_tags ở B1 và 1 dòng ở B2. | 1. Ghi nhận bảng action_limit_tags: U có 1 dòng ở bot B1 và 1 dòng ở bot B2.<br>2. Chọn bot B1, mở màn chi tiết bạn bè của U.<br>3. Bấm 「削除」 rồi 「削除する」 để xóa U khỏi bot B1.<br>4. Kiểm tra lại số dòng action_limit_tags của U ở bot B1 và ở bot B2.<br>5. Chuyển sang bot B2 và mở danh sách bạn bè để xác nhận U vẫn là bạn của bot B2. | 1 người dùng LINE là bạn của 2 bot, mỗi bot 1 bản ghi giới hạn tag. | Sau khi xóa ở bot B1: dòng của U ở bot B1 biến mất (đếm = 0), dòng của U ở bot B2 vẫn còn nguyên (đếm = 1), và U vẫn hiển thị trong danh sách bạn bè của bot B2. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16706 (NEW-13) · ui / auto / local-only · REQ-006 · TICKET-40708, friend-list/EP-17, friend-list/SCR-FL-07, friend-list/BR-01 · ⚠️ Mã quan điểm KHÔNG có trong checklist-lme.md · **Note Studio:** Biên quan trọng nhất của điều kiện lọc mới: nếu lệnh xóa thiếu điều kiện theo bot thì dữ liệu của bot khác sẽ mất — hậu quả không khôi phục được. Cần chuyển bot bằng chức năng chọn bot trên giao diện (các màn quản lý bạn bè phụ thuộc bot đang chọn). |
| TC-REGSHARED001-02 | REG-SHARED-001 | Normal | Xóa 1 bạn trên màn bạn bè đã chặn OA thì bản ghi giới hạn tag bị dọn | Bot B1 có tag A đang đầy với hành động gắn tag B ở chế độ 「1度のみアクション稼働」. Bạn P 「FRIEND_P_40708_<mã run>」 đã kích hoạt hành động nên có đúng 1 dòng trong action_limit_tags, và đang ở trạng thái tự chặn OA nên xuất hiện trên màn 「ブロックされた友だち」. | 1. Mở màn 「ブロックされた友だち」 từ menu danh sách bạn bè.<br>2. Tìm dòng của bạn P và ghi nhận bảng action_limit_tags đang có 1 dòng cho cặp bot B1 + bạn P.<br>3. Ở cột 「エルメ上から削除」 của dòng bạn P, bấm 「削除」.<br>4. Trong hộp thoại xác nhận 「削除しますか？」, chọn đồng ý.<br>5. Chờ màn hình tải lại danh sách và kiểm tra lại bảng action_limit_tags cho cặp bot B1 + bạn P. | Bạn P đang chặn OA, có 1 bản ghi giới hạn tag. | Bạn P biến mất khỏi danh sách 「ブロックされた友だち」 sau khi màn hình tải lại, và bảng action_limit_tags không còn dòng nào cho cặp bot B1 + bạn P. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16699 (NEW-6) · ui / auto / local-only · REQ-002 · TICKET-40708, friend-list/EP-25, friend-list/SCR-FL-05, friend-list/BR-07 · **Note Studio:** Đường vào thứ 2 của hàm dọn dữ liệu chung (FriendlistController::deleteUserBlock, :4019). Thao tác phải phát sinh từ link 「削除」 trên browser (FE gọi POST /ajax/friendlist/delete-user-block với tham số id). |
| TC-SELECTSCOPE001-01 | SELECT-SCOPE-001 | Normal | Xóa nhiều bạn cùng lúc thì dọn bản ghi giới hạn tag của tất cả bạn được chọn | Bot B1 có tag A đang đầy với hành động gắn tag B. Ba bạn P1, P2, P3 (「FRIEND_P1/P2/P3_40708_<mã run>」) đều đã kích hoạt hành động nên mỗi bạn có 1 dòng trong action_limit_tags và cả ba đang chặn OA nên đều hiển thị trên màn 「ブロックされた友だち」. | 1. Mở màn 「ブロックされた友だち」.<br>2. Ghi nhận bảng action_limit_tags đang có 3 dòng, mỗi bạn 1 dòng.<br>3. Tick chọn cả ba bạn P1, P2, P3 trong danh sách.<br>4. Ở panel 「一括友だち削除」, bấm nút 「削除」.<br>5. Chờ màn hình tải lại danh sách và kiểm tra lại bảng action_limit_tags của ba bạn. | 3 bạn được tick, mỗi bạn 1 bản ghi giới hạn tag. | Cả ba bạn biến mất khỏi danh sách và không còn dòng action_limit_tags nào của P1, P2, P3 trong bot B1 (đếm = 0 cho cả ba). | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16702 (NEW-9) · ui / auto / local-only · REQ-003 · TICKET-40708, friend-list/EP-26, friend-list/SCR-FL-05, friend-list/BR-07 · ⚠️ Mã quan điểm KHÔNG có trong checklist-lme.md · **Note Studio:** Đường vào thứ 3 của hàm dọn dữ liệu chung (FriendlistController::deleteUserBlockAction, :4130) — hàm được gọi trong vòng lặp từng bạn nên phải xác nhận không sót bạn nào. FE gửi danh sách id qua POST /ajax/friendlist/delete-multiple-user-block; phải bấm trên màn hình chứ không tự dựng danh sách id. |
| TC-TOOLNEGCTRL001-03 | TOOL-NEGCTRL-001 | Boundary | Xóa nhiều bạn nhưng chỉ chọn 1 bạn thì bản ghi của các bạn không được chọn phải còn nguyên | Giống TC xóa nhiều bạn: ba bạn P1, P2, P3 cùng chặn OA và mỗi bạn có 1 dòng trong action_limit_tags của bot B1. | 1. Mở màn 「ブロックされた友だち」.<br>2. Ghi nhận bảng action_limit_tags đang có 3 dòng (P1, P2, P3).<br>3. Chỉ tick chọn duy nhất bạn P2, để trống ô chọn của P1 và P3.<br>4. Ở panel 「一括友だち削除」, bấm nút 「削除」.<br>5. Chờ màn hình tải lại và kiểm tra bảng action_limit_tags cho cả ba bạn. | Tick 1/3 bạn (chỉ P2). | Chỉ bạn P2 biến mất khỏi danh sách và chỉ dòng action_limit_tags của P2 bị xóa. Dòng của P1 và P3 còn nguyên (đếm mỗi bạn vẫn = 1) và hai bạn này vẫn hiển thị trong danh sách. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16703 (NEW-10) · ui / auto / local-only · REQ-003, REQ-006 · TICKET-40708, friend-list/EP-26, friend-list/SCR-FL-05, friend-list/BR-01 · ⚠️ Mã quan điểm KHÔNG có trong checklist-lme.md · **Note Studio:** Đối chứng âm cho phạm vi chọn: xóa theo danh sách tick phải khớp tuyệt đối với payload FE gửi lên. Nếu dòng của P1/P3 bị mất thì lệnh xóa mới đã thiếu điều kiện lọc theo bạn bè và phải báo bug ngay. |
| TC-REGSHARED001-03 | REG-SHARED-001 | Normal | Xóa 1 bạn trên màn bạn bè bị bot chặn thì bản ghi giới hạn tag bị dọn | Bot B1 có tag A đang đầy với hành động gắn tag B. Bạn Q 「FRIEND_Q_40708_<mã run>」 đã kích hoạt hành động nên có 1 dòng trong action_limit_tags, sau đó bị quản trị viên chặn nên xuất hiện trên màn 「ブロックした友だち」. | 1. Mở màn 「ブロックした友だち」 từ menu danh sách bạn bè.<br>2. Ghi nhận bảng action_limit_tags đang có 1 dòng cho cặp bot B1 + bạn Q (bản ghi vẫn còn dù bạn đã bị chặn).<br>3. Ở cột 「エルメ上から削除」 của dòng bạn Q, bấm 「削除」 và xác nhận 「削除しますか？」.<br>4. Chờ màn hình tải lại và kiểm tra lại bảng action_limit_tags cho cặp bot B1 + bạn Q. | Bạn Q bị bot chặn, có 1 bản ghi giới hạn tag. | Trước khi xóa, bản ghi vẫn còn (chặn không xóa dữ liệu). Sau khi xóa: bạn Q biến mất khỏi danh sách 「ブロックした友だち」 và bảng action_limit_tags không còn dòng nào cho cặp bot B1 + bạn Q. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16700 (NEW-7, version 2) · ui / auto / local-only · REQ-002 · TICKET-40708, friend-list/EP-25, friend-list/SCR-FL-04, friend-list/BR-05 · **Note Studio:** Phủ một caller khác của hàm cleanup dùng chung: trạng thái 'bị bot chặn rồi xóa'. Trước khi xóa phải xác nhận action_limit_tags vẫn còn; sau khi bấm 「削除」 trên browser, record của đúng bot + bạn phải được dọn. Dùng REG-SHARED-001 vì mục tiêu chính là regression của shared cleanup, không phải thông báo/người dùng. |
| TC-TOOLSCOPE001-01 | TOOL-SCOPE-001 | Normal | Xóa 1 bạn trên màn bạn bè đang ẩn thì bản ghi giới hạn tag bị dọn | Bot B1 có tag A đang đầy với hành động gắn tag B. Bạn R 「FRIEND_R_40708_<mã run>」 đã kích hoạt hành động nên có 1 dòng trong action_limit_tags và đang ở trạng thái ẩn nên xuất hiện trên màn 「非表示中の友だち」. | 1. Mở màn 「非表示中の友だち」 từ menu danh sách bạn bè.<br>2. Ghi nhận bảng action_limit_tags đang có 1 dòng cho cặp bot B1 + bạn R.<br>3. Bấm 「削除」 ở dòng của bạn R và xác nhận 「削除しますか？」.<br>4. Chờ màn hình tải lại và kiểm tra lại bảng action_limit_tags cho cặp bot B1 + bạn R. | Bạn R đang bị ẩn, có 1 bản ghi giới hạn tag. | Bạn R biến mất khỏi danh sách 「非表示中の友だち」 và bảng action_limit_tags không còn dòng nào cho cặp bot B1 + bạn R. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16701 (NEW-8) · ui / auto / local-only · REQ-002 · TICKET-40708, friend-list/EP-25, friend-list/SCR-FL-06, friend-list/BR-09 · ⚠️ Mã quan điểm KHÔNG có trong checklist-lme.md · **Note Studio:** Màn này KHÔNG có trong danh sách vùng ảnh hưởng của bước điều tra nhưng mã FE (public/js/friendlist/hiden.js:83, 101) cho thấy nó gọi đúng hai endpoint xóa bạn đã chặn, tức là cũng đi qua hàm dọn dữ liệu đã sửa. Đây là phát hiện bổ sung của bước viết testcase, cần leader ghi nhận. |
| TC-REGSHARED001-04 | REG-SHARED-001 | Normal | Xóa bạn từ ứng dụng di động thì bản ghi giới hạn tag bị dọn | Có token đăng nhập ứng dụng di động hợp lệ cho tài khoản quản trị bot B1 (endpoint nằm sau lớp xác thực của app và lớp kiểm tra hạn hợp đồng). Bot B1 có tag A đang đầy với hành động gắn tag B; bạn S 「FRIEND_S_40708_<mã run>」 đã kích hoạt hành động nên có đúng 1 dòng trong action_limit_tags. | 1. Ghi nhận bảng action_limit_tags đang có 1 dòng cho cặp bot B1 + bạn S.<br>2. Gọi endpoint xóa bạn của ứng dụng di động với mã bot B1 và mã người bạn S, kèm token xác thực của app.<br>3. Ghi nhận nội dung phản hồi của endpoint.<br>4. Kiểm tra lại bảng action_limit_tags cho cặp bot B1 + bạn S và kiểm tra bạn S không còn trong danh sách bạn bè của bot B1. | Tham số: mã bot = B1, mã người bạn = S. Xác thực bằng token app. | Endpoint trả về phản hồi thành công (kết quả 'ok'), bạn S không còn là bạn bè của bot B1, và bảng action_limit_tags không còn dòng nào cho cặp bot B1 + bạn S. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16704 (NEW-11) · **api** / auto / local-only · REQ-004 · TICKET-40708, friend-info/EP-23, friend-info/BR-11, friend-list/BR-07 · **Note Studio:** Đây là đường vào thứ 4 của hàm dọn dữ liệu chung (FriendInformationController::deleteFriend, :1336) và là hợp đồng API của ứng dụng di động nên viết ở tầng api chứ không mượn màn web. Endpoint nhận mã bot từ tham số request (không lấy từ phiên web), cần lưu ý khi dựng dữ liệu. |
| TC-DATADB001-02 | DATA-DB-001 | Abnormal | Xóa bạn ở bot B1 không làm mất bản ghi giới hạn tag của cùng người LINE ở bot B2 | Có hai bot B1 và B2 do cùng một tài khoản quản trị (để thao tác hợp lệ về quyền). Người dùng LINE U là bạn của cả hai bot và đang có 1 dòng action_limit_tags ở B1 và 1 dòng ở B2. Ngoài ra bot B1 còn bạn T 「FRIEND_T_40708_<mã run>」 có 1 dòng action_limit_tags. | 1. Ghi nhận số dòng action_limit_tags trước khi xóa: người dùng U ở bot B1 = 1, U ở bot B2 = 1, bạn T ở bot B1 = 1.<br>2. Gọi endpoint xóa bạn của ứng dụng di động với mã bot = B1 và mã người bạn = U.<br>3. Ghi nhận phản hồi của endpoint.<br>4. Kiểm tra lại action_limit_tags của U ở B1, U ở B2 và T ở B1. | Bot = B1, bạn = U. U đồng thời là bạn của B1 và B2; T là một bạn khác của B1. | Endpoint xóa thành công U khỏi bot B1. Chỉ dòng action_limit_tags của U ở B1 bị xóa; dòng của U ở B2 vẫn còn nguyên và dòng của T ở B1 vẫn còn nguyên. Không có bản ghi ngoài đúng cặp bot B1 + U bị tác động. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16705 (NEW-12, version 2) · **api** / auto / local-only · REQ-006 · TICKET-40708, friend-info/EP-23, friend-list/BR-01 · **Note Studio:** TC tập trung đúng rủi ro của thay đổi mới: điều kiện xóa phải có đủ bot_id + line_user_id. Đã bỏ nhánh gọi friend ID không tồn tại vì đó là validation của endpoint cũ, không có requirement trực tiếp từ ticket #40708. |
| TC-JOB002-01 | JOB-002 | Normal | Chạy lệnh dọn dữ liệu ở chế độ chỉ đếm thì báo đúng số bản ghi mồ côi và không xóa gì | Môi trường đã triển khai nhánh có fix. Dữ liệu chuẩn bị trong bot B1: 3 dòng action_limit_tags mồ côi (không còn quan hệ bạn bè tương ứng, mô phỏng dữ liệu sinh trước fix) và 2 dòng hợp lệ của hai người bạn còn tồn tại, trong đó 1 người đang bị chặn. | 1. Ghi nhận số dòng của bảng action_limit_tags: 3 dòng mồ côi và 2 dòng hợp lệ.<br>2. Chạy lệnh dọn bản ghi mồ côi ở chế độ chỉ đếm (tùy chọn chạy thử, không xóa).<br>3. Đọc kết quả lệnh in ra màn hình dòng lệnh.<br>4. Đếm lại toàn bộ dòng của bảng action_limit_tags. | 3 dòng mồ côi + 2 dòng hợp lệ; chạy lệnh với tùy chọn chỉ đếm. | Lệnh kết thúc thành công và báo số bản ghi mồ côi = 3 (khớp với số đếm thủ công), đồng thời nêu rõ đây là chế độ chạy thử. Bảng action_limit_tags KHÔNG thay đổi: vẫn đủ 5 dòng như trước khi chạy. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16708 (NEW-15) · **job** / auto / local-only · REQ-008, REQ-011 · TICKET-40708, redmine: journal 2026-09-08 mục 5, source: RecoverActionLimitTagOrphan.php · ⚠️ Mã quan điểm KHÔNG có trong checklist-lme.md (chỉ có JOB-001) · ⚠️ RULE-08: TC job, không kết luận từ local · **Note Studio:** Tên lệnh 'recover:actionLimitTagOrphan' và tùy chọn '--dry-run' lấy từ report của Dev, chưa đọc trực tiếp được vì nhánh fix chưa checkout ở cây mã nguồn local. Khi chạy phải liệt kê danh sách lệnh của ứng dụng để xác nhận tên và tùy chọn thật; nếu lệch thì báo lại cho leader chứ không tự đổi kết quả mong đợi. Dựng dòng mồ côi bằng cách tạo dữ liệu rồi xóa quan hệ bạn bè tương ứng trực tiếp ở tầng dữ liệu (mô phỏng dữ liệu cũ trước fix) — đây là tiền điều kiện, không phải hành vi cần kiểm chứng. |
| TC-DATARETENTION001-01 | DATA-RETENTION-001 | Normal | Chạy lệnh dọn dữ liệu thật thì xóa hết bản ghi mồ côi và giữ nguyên bản ghi của bạn còn tồn tại | Giống TC chế độ chỉ đếm: 3 dòng mồ côi và 2 dòng hợp lệ (1 trong 2 người bạn đang bị chặn) trong bot B1. | 1. Ghi lại danh sách khóa chính của 5 dòng action_limit_tags trước khi chạy.<br>2. Chạy lệnh dọn bản ghi mồ côi ở chế độ xóa thật.<br>3. Đọc kết quả lệnh in ra màn hình.<br>4. Đối chiếu lại từng dòng theo danh sách khóa chính đã ghi ở bước 1.<br>5. Kiểm tra hai người bạn còn tồn tại vẫn hiển thị trong danh sách bạn bè của bot B1 (người bị chặn vẫn nằm ở màn 「ブロックした友だち」). | 3 dòng mồ côi + 2 dòng hợp lệ; chạy lệnh ở chế độ xóa thật. | Lệnh báo đã xóa 3 bản ghi. Ba dòng mồ côi biến mất khỏi bảng action_limit_tags; hai dòng hợp lệ còn nguyên, kể cả dòng của người bạn đang bị chặn. Hai người bạn vẫn còn trong danh sách bạn bè, các tag và hành động của họ không bị thay đổi. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16709 (NEW-16) · **job** / auto / local-only · REQ-008 · TICKET-40708, redmine: journal 2026-09-08 mục 5, source: RecoverActionLimitTagOrphan.php · ⚠️ Mã quan điểm KHÔNG có trong checklist-lme.md · ⚠️ **XÓA VĨNH VIỄN — backup trước khi chạy** · **Note Studio:** Xóa ở đây là xóa vĩnh viễn, không có bước khôi phục — nên đối chứng 'cái phải CÒN' quan trọng ngang với 'cái phải MẤT'. Bản ghi của người bạn đang bị chặn là đối chứng bắt buộc vì bạn bị chặn vẫn tồn tại quan hệ bạn bè. |
| TC-JOB002-02 | JOB-002 | Normal | Chạy lại lệnh dọn dữ liệu lần thứ hai thì không còn gì để xóa và không phát sinh lỗi | Vừa chạy xong lệnh dọn dữ liệu ở chế độ xóa thật và không còn bản ghi mồ côi; trong bảng vẫn còn 2 dòng hợp lệ của bạn bè đang tồn tại. | 1. Ghi nhận số dòng hiện tại của bảng action_limit_tags (2 dòng hợp lệ, 0 dòng mồ côi).<br>2. Chạy lại lệnh dọn bản ghi mồ côi ở chế độ xóa thật lần thứ hai.<br>3. Đọc kết quả lệnh in ra màn hình và mã kết thúc của lệnh.<br>4. Đếm lại số dòng của bảng action_limit_tags. | 0 dòng mồ côi, 2 dòng hợp lệ; chạy lệnh lần thứ hai. | Lệnh kết thúc thành công, báo số bản ghi đã xóa = 0, không sinh lỗi hay cảnh báo bất thường. Bảng action_limit_tags vẫn giữ nguyên 2 dòng hợp lệ. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16711 (NEW-18) · **job** / auto / local-only · REQ-008 · TICKET-40708, redmine: journal 2026-09-08 mục 5 · ⚠️ Mã quan điểm KHÔNG có trong checklist-lme.md · **Note Studio:** Đây là nhánh 'chạy lại' của quan điểm kiểm thử job, diễn giải cho lệnh chạy tay: lệnh phải chạy được nhiều lần mà không gây tác dụng phụ, vì thực tế vận hành có thể chạy lặp sau khi release. |
| TC-DATARETENTION001-02 | DATA-RETENTION-001 | Abnormal | Dry-run phát hiện command bao gồm orphan do bot đã xóa — dừng chờ leader xác nhận phạm vi | Trong bảng action_limit_tags có 2 nhóm dòng mồ côi khác nguyên nhân: nhóm 1 gồm 2 dòng mồ côi do bạn bè bị xóa (bot vẫn tồn tại — đúng phạm vi bug), nhóm 2 gồm 2 dòng mồ côi mà bot tương ứng đã không còn tồn tại (nguyên nhân khác). Ngoài ra có 1 dòng hợp lệ. | 1. Ghi nhận số dòng của từng nhóm: nhóm 1 = 2 dòng mồ côi do xóa bạn nhưng bot vẫn tồn tại; nhóm 2 = 2 dòng mồ côi thuộc bot đã bị xóa; ngoài ra có 1 dòng hợp lệ.<br>2. Chạy 「recover:actionLimitTagOrphan --dry-run」.<br>3. Ghi lại tổng số record mà command dự kiến xóa và đối chiếu với hai nhóm ở bước 1.<br>4. Nếu dry-run bao gồm cả nhóm orphan do bot đã bị xóa, DỪNG tại đây, không chạy chế độ xóa thật và báo số liệu cho Leader/BA xác nhận phạm vi recovery. | 2 dòng mồ côi do xóa bạn + 2 dòng mồ côi do bot đã bị xóa + 1 dòng hợp lệ. | Dry-run phải cho biết chính xác phạm vi mà command hiện tại sẽ tác động. Nếu kết quả = 4, xác nhận command đang bao gồm cả 2 dòng orphan do xóa bạn và 2 dòng orphan do bot đã bị xóa; đây là phạm vi rộng hơn bug gốc nên testcase chỉ ghi nhận evidence và phải chờ Leader/BA xác nhận trước khi chạy xóa thật. Không được coi việc xóa cả hai nhóm là PASS nghiệp vụ khi chưa có quyết định phạm vi. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16713 (NEW-20, version 2) · **job** / auto / local-only · REQ-008, REQ-011 · TICKET-40708, redmine: journal 2026-09-08 mục 5 · ⚠️ Mã quan điểm KHÔNG có trong checklist-lme.md · ⚠️ **TC DỪNG chờ Leader/BA chốt phạm vi — không chạy xóa thật** · **Note Studio:** TC này xử lý conflict đã nêu trong task context: code hiện tại dọn mọi orphan theo bot_id + line_user_id, kể cả orphan do nguyên nhân khác; Dev cũng đã cảnh báo điều này. Vì hành vi mong muốn chưa được human chốt, không dùng code hiện tại làm expected chính thức và không chạy destructive delete ở testcase này. |
| TC-FUNC004-01 | FUNC-004 | Boundary | Số bản ghi mồ côi lớn hơn kích thước lô thì lệnh vẫn dọn hết | Đã dựng 5 dòng action_limit_tags mồ côi và 1 dòng hợp lệ trong bot B1. | 1. Ghi nhận có 5 dòng mồ côi và 1 dòng hợp lệ.<br>2. Chạy lệnh dọn bản ghi mồ côi với kích thước lô đặt bằng 2 (nhỏ hơn số bản ghi mồ côi).<br>3. Đọc kết quả lệnh in ra màn hình và ghi nhận lệnh có kết thúc hay không.<br>4. Đếm lại số dòng mồ côi và số dòng hợp lệ. | 5 dòng mồ côi, 1 dòng hợp lệ, kích thước lô = 2. | Lệnh chạy hết nhiều lô rồi kết thúc bình thường (không treo, không lặp vô hạn), báo tổng số đã xóa = 5. Không còn dòng mồ côi nào và dòng hợp lệ vẫn còn. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16710 (NEW-17) · **job** / auto / local-only · REQ-008 · TICKET-40708, redmine: journal 2026-09-08 mục 5 · **Note Studio:** Biên của cơ chế chia lô theo con trỏ mã tăng dần mà report mô tả: nếu con trỏ bị đặt lại sai sau mỗi lô thì hoặc sót bản ghi, hoặc lặp vô hạn. Tên tùy chọn kích thước lô ('--chunk' theo report) phải đối chiếu với phần trợ giúp thật của lệnh khi chạy. |
| TC-REGSHARED001-05 | REG-SHARED-001 | Normal | Job nền gắn tag đạt giới hạn chạy lại hành động sau khi bạn bị xóa rồi kết bạn lại | Job nền của dịch vụ xử lý sự kiện đang chạy và dùng chung cơ sở dữ liệu với web. Bot B1 có tag A đang đầy (「制限する」, đủ số người) với 「制限到達後のタグ追加時アクション」 gắn tag B ở chế độ 「1度のみアクション稼働」. Đã cấu hình sẵn một hành động chạy nền có bước gắn tag A cho người dùng (ví dụ hành động của kịch bản/trả lời tự động). Bạn W 「FRIEND_W_40708_<mã run>」 là bạn của bot B1. | 1. Kích hoạt hành động nền gắn tag A cho bạn W lần thứ nhất và chờ job xử lý xong.<br>2. Kiểm tra bạn W được gắn tag B (hành động khi tag đầy đã chạy) và bảng action_limit_tags có 1 dòng cho cặp bot B1 + bạn W.<br>3. Xóa bạn W bằng nút 「削除」 trên màn chi tiết bạn bè và xác nhận 「削除する」.<br>4. Dựng lại quan hệ kết bạn của bạn W với bot B1.<br>5. Kích hoạt lại hành động nền gắn tag A cho bạn W và chờ job xử lý xong.<br>6. Kiểm tra tag của bạn W và bảng action_limit_tags. | Tag A đã đầy, chế độ 「1度のみアクション稼働」, hành động nền gắn tag A cho bạn W. | Lần chạy đầu: tag B được gắn cho bạn W và có 1 dòng action_limit_tags. Sau khi xóa bạn: không còn dòng nào của bạn W. Sau khi kết bạn lại và chạy lại hành động nền: tag B ĐƯỢC gắn lại cho bạn W và bảng action_limit_tags có dòng mới — chứng tỏ phía job cũng hưởng lợi từ fix dù không sửa mã job. | Chưa test | | LOCAL | pipeline-manual | 2026-09-08 | | | Studio #16714 (NEW-21) · **job** / auto / local-only · REQ-009 · TICKET-40708, tag-management/BR-05, tag-management/BR-06, source: linect-service LineUserModel.java:2424 · ⚠️ **`last_exec.status = skip`** (runId 1083, source `ai`, by `pipeline-manual`, env `local`, lúc 2026-09-08 11:15:53) — TC DUY NHẤT có run, và là **skip** chứ không phải pass · ⚠️ RULE-08: TC job nền, không kết luận từ local · **Note Studio:** Phía job đọc cùng bảng theo bộ khóa (bot, tag, bạn, hành động) và cùng quy ước chế độ với web (mã nguồn: LineUserModel.checkLimitActionModeAddTag :2424-2460, TagConstants LIMIT_ACTION_MODE_HAVE_LIMIT = 0). Hành vi phải phát sinh từ job thật — nếu không dựng được job nền hoặc không kích hoạt được hành động nền trong môi trường test thì trả skip kèm lý do môi trường cụ thể, TUYỆT ĐỐI không thay bằng thao tác gắn tag trên web rồi kết luận pass, vì như vậy chỉ lặp lại TC của phía web. |
| TC-DATADB001-03 | DATA-DB-001 | Normal | Dữ liệu orphan trước fix sau khi recover và kết bạn lại phải chạy được action giới hạn như lần đầu | Bot B1 có tag A đang đầy, action khi đạt giới hạn gắn tag B ở chế độ 「1度のみアクション稼働」. Có friend Y từng kích hoạt action trước fix nên action_limit_tags có 1 dòng; sau đó Y đã bị xóa trước fix, bot_line_user không còn nhưng dòng action_limit_tags vẫn còn mồ côi. Y chưa kết bạn lại. | 1. Kiểm tra DB trước recover: bot_line_user của Y không tồn tại, action_limit_tags vẫn còn đúng 1 dòng mồ côi; ghi lại id của dòng này.<br>2. Chạy 「recover:actionLimitTagOrphan --dry-run」 và xác nhận số lượng dự kiến xóa có bao gồm dòng của Y.<br>3. Chạy command recover ở chế độ xóa thật.<br>4. Kiểm tra action_limit_tags: dòng mồ côi cũ của Y đã bị xóa.<br>5. Cho đúng người LINE Y kết bạn lại với bot B1 và xác nhận Y xuất hiện lại trong danh sách bạn bè.<br>6. Mở màn chi tiết Y, vào tab 「タグ」 và gắn tag A đang đầy.<br>7. Kiểm tra tag B và action_limit_tags của Y. | 1 legacy orphan action_limit_tags của friend Y đã bị xóa trước fix; Y chỉ re-add sau khi command recover hoàn tất. | Dry-run bao gồm dòng mồ côi của Y; sau chạy thật dòng cũ bị xóa. Khi Y kết bạn lại rồi gắn tag A đang đầy, action gắn tag B ĐƯỢC chạy như lần đầu và action_limit_tags tạo tracking mới cho Y. Kết quả này chứng minh command recovery không chỉ xóa DB đúng mà còn khôi phục nghiệp vụ của ticket #40708. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16720 (NEW-23) · client_ref `review283-recover-e2e` · **job** / auto / local-only · **priority High** · requirement_keys **RỖNG** · TICKET-40708, redmine: journal 2026-09-08 mục 5, tag-management/BR-05, tag-management/BR-06 · 👤 **TC do người viết** (`provenance.source=human`, actor `cucdtk@mcp`, 2026-09-08 11:11) · **Note Studio:** E2E recovery cho dữ liệu đã lỗi trước fix. Requirement liên quan: REQ-005/REQ-008. Đây là oracle nghiệp vụ còn thiếu ở NEW-15/NEW-16, vốn mới kiểm việc đếm/xóa record. |
| TC-TOOLKNOW002-02 | TOOL-KNOW-002 | Boundary | Friend đã kết bạn lại trước khi chạy recover vẫn phải được phục hồi để action giới hạn chạy lại | Mô phỏng dữ liệu đã lỗi trước fix: bot B1 có tag A đang đầy, action khi đạt giới hạn gắn tag B ở chế độ 「1度のみアクション稼働」. Friend X đã từng kích hoạt action nên có 1 dòng action_limit_tags; sau đó X bị xóa theo logic cũ nhưng dòng action_limit_tags còn sót. Trước khi chạy command recover, cùng người LINE X đã kết bạn lại với bot B1 nên bot_line_user của X đã tồn tại trở lại, trong khi dòng action_limit_tags cũ vẫn còn. | 1. Kiểm tra DB: bot_line_user của X trong bot B1 đang tồn tại và action_limit_tags vẫn còn dòng cũ của X; ghi lại id/number_action của dòng này.<br>2. Chạy 「recover:actionLimitTagOrphan --dry-run」 và ghi nhận X có nằm trong phạm vi dự kiến recover hay không.<br>3. Chạy command recover ở chế độ xóa thật theo phạm vi đã được phê duyệt.<br>4. Kiểm tra lại action_limit_tags của bot B1 + X.<br>5. Mở màn chi tiết friend X, vào tab 「タグ」, gắn lại tag A đang đầy.<br>6. Kiểm tra tag B và action_limit_tags sau thao tác gắn tag. | Dữ liệu legacy: X đã bị xóa trước fix nhưng đã kết bạn lại trước thời điểm chạy recovery; vẫn còn action_limit_tags cũ. | Recovery phải xử lý được trạng thái legacy đã gây bug: dòng tracking cũ không được tiếp tục chặn nghiệp vụ. Sau recovery, khi gắn lại tag A đang đầy cho X thì action gắn tag B phải chạy lại và action_limit_tags phải có trạng thái tracking mới phù hợp. Nếu dry-run/command bỏ qua X chỉ vì bot_line_user đã tồn tại lại và action vẫn bị chặn, testcase FAIL và cần báo lại phạm vi recovery. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16719 (NEW-22) · client_ref `review283-recover-readded` · **job** / auto / local-only · **priority High** · requirement_keys **RỖNG** · TICKET-40708, redmine: journal 2026-09-08 mục 5, tag-management/BR-05, tag-management/BR-06 · 👤 **TC do người viết** (`provenance.source=human`, actor `cucdtk@mcp`, 2026-09-08 11:11) · ⚠️ Mã quan điểm KHÔNG có trong checklist-lme.md · ⚠️ **Lỗ hổng phạm vi recover — cần BA/Leader chốt** · **Note Studio:** Case High-risk bổ sung từ review. Query hiện tại của command dùng điều kiện orphan (không còn bot_line_user), nên friend đã re-add trước recover có nguy cơ không được xử lý dù vẫn mang record cũ gây bug. Requirement liên quan: REQ-005/REQ-008/REQ-011. Nếu BA/Leader chủ đích không recover nhóm này thì cần chốt rõ ngoại lệ trước khi đóng ticket. |
| TC-DATADB001-04 | DATA-DB-001 | Normal | Kiểm kê phạm vi bản ghi mồ côi trước khi chạy lệnh dọn dữ liệu | Có quyền truy vấn chỉ đọc cơ sở dữ liệu của môi trường cần đánh giá. Chưa chạy lệnh dọn dữ liệu mồ côi. | 1. Đếm tổng số dòng trong bảng action_limit_tags.<br>2. Đếm số dòng mồ côi: dòng không còn quan hệ bạn bè tương ứng theo cặp bot và người dùng LINE.<br>3. Trong số dòng mồ côi, tách riêng nhóm mà bot vẫn còn tồn tại (mồ côi do xóa bạn — đúng phạm vi bug) và nhóm mà bot đã không còn tồn tại (mồ côi do nguyên nhân khác).<br>4. Ghi lại ba con số trên kèm thời điểm đo để làm căn cứ đối chiếu với kết quả đếm của lệnh dọn dữ liệu. | Không nhập liệu — chỉ truy vấn đếm. | Thu được ba con số: tổng số dòng, số dòng mồ côi, và phân tách mồ côi do xóa bạn / do bot không còn tồn tại. Ba con số này phải đối chiếu được với số mà lệnh dọn dữ liệu báo ở chế độ chỉ đếm; nếu số của lệnh lớn hơn số dòng mồ côi đo được thì dừng lại và báo cho leader trước khi chạy xóa thật. | Chưa test | | Tất cả (dự kiến) | | | | | Studio #16707 (NEW-14) · **data** / auto / **read-only** · REQ-011 · TICKET-40708, redmine: journal 2026-09-08 mục 5 · **Note Studio:** TC thuần dữ liệu, không chạy hành vi ứng dụng, chỉ đọc. Oracle: đếm bằng phép nối trái action_limit_tags với bot_line_user theo (line_user_id, bot_id) lấy phần không khớp, rồi nối tiếp với bảng bot để tách nhóm bot đã bị xóa. Đây là bước bắt buộc trước khi cho phép chạy lệnh xóa vĩnh viễn, đồng thời cung cấp số liệu cho leader duyệt điểm mâu thuẫn 'lệnh dọn cả mồ côi do nguyên nhân khác'. |

---

## Requirements của Studio task #283 (để đối chiếu coverage)

| REQ | Tiêu đề | Category | Risk | TC cover |
|---|---|---|---|---|
| REQ-001 | Xóa bạn từ màn chi tiết bạn bè phải dọn sạch bản ghi giới hạn tag của bạn đó | ui | High | TC-DATADB001-01, TC-TOOLKNOW002-01, TC-TOOLAXIS001-01 |
| REQ-002 | Xóa bạn từ các màn bạn bị chặn / đã chặn / đang ẩn cũng phải dọn bản ghi giới hạn tag | ui | High | TC-REGSHARED001-02, TC-REGSHARED001-03, TC-TOOLSCOPE001-01 |
| REQ-003 | Xóa nhiều bạn cùng lúc chỉ dọn bản ghi của các bạn được chọn | ui | High | TC-SELECTSCOPE001-01, TC-TOOLNEGCTRL001-03 |
| REQ-004 | Xóa bạn từ ứng dụng di động cũng phải dọn bản ghi giới hạn tag | api | High | TC-REGSHARED001-04 |
| REQ-005 | Sau khi xóa bạn rồi kết bạn lại, hành động khi tag đầy phải chạy lại | ui | High | TC-TOOLKNOW002-01 |
| REQ-006 | Phạm vi xóa phải giới hạn đúng trong bot đang chọn và đúng người bạn bị xóa | data | High | TC-TOOLNEGCTRL001-02, TC-TOOLNEGCTRL001-03, TC-DATADB001-02 |
| REQ-007 | Chặn bạn không được xóa bản ghi giới hạn tag | ui | Medium | TC-TOOLNEGCTRL001-01 |
| REQ-008 | Lệnh dọn dữ liệu mồ côi phải đếm được trước khi xóa, xóa đúng phạm vi và chạy lại được | job | High | TC-JOB002-01, TC-DATARETENTION001-01, TC-JOB002-02, TC-DATARETENTION001-02, TC-FUNC004-01 |
| REQ-009 | Job nền gắn tag cũng phải chạy lại hành động khi tag đầy sau khi bạn kết bạn lại | job | Medium | TC-REGSHARED001-05 *(skip)* |
| REQ-010 | Thêm bước dọn bảng mới không được làm hỏng phần dọn dữ liệu sẵn có của luồng xóa bạn | ui | Medium | TC-REGSHARED001-01 |
| REQ-011 | Phải kiểm kê được phạm vi dữ liệu mồ côi trước khi quyết định chạy lệnh dọn | data | Medium | TC-DATADB001-04, TC-JOB002-01, TC-DATARETENTION001-02 |

> `test_viewpoint_selection` của task = `null` — Studio **KHÔNG** lưu bảng chọn quan điểm test cho task này.

---

## Member tự check trước khi submit

> ⚠️ Bộ TC này **không do member người viết** — checklist dưới đây để **Leader** đối chiếu khi review, không phải member tự tick.

### Coverage check
- [ ] Đã đọc kỹ `01-bug-task.md`
- [ ] Đã đọc spec của tính năng (`spec-features/<feature>/feature-spec.md`, hoặc link spec leader đưa trực tiếp)
- [ ] Đã đọc kỹ `03-dev-impact.md`, hiểu 4 mục
- [ ] **Mỗi impact** trong 4.1 / 4.2 / 4.3 có **ít nhất 1 TC** verify
- [ ] Có **ít nhất 1 TC** verify trực tiếp bug fix (reproduce flow KH)
- [ ] Có **ít nhất 1 TC** verify tính năng cũ không hỏng cho mỗi tính năng trong 4.3 (ghi "regression" ở Ghi chú)
- [ ] Có **ít nhất 1 Abnormal + 1 Boundary** cho mỗi data quan trọng trong 4.2
- [ ] Mọi TC có `Mã quan điểm liên kết`, steps rõ ràng, `Kết quả mong đợi` đo lường được
- [ ] `Tiêu đề test case` chứa **keyword** giúp Leader nhận ra impact TC đó cover

### Base quan điểm test LME (2 tầng)

**Tầng 1 — quan điểm**: [framework/checklist-lme.md](../../framework/checklist-lme.md) (80 quan điểm + 12 RULE).
**Tầng 2 — catalog**: [framework/catalog-lme.md](../../framework/catalog-lme.md).

| Mã quan điểm | Ưu tiên | ◯/× | Catalog đã tra | TC cover | Lý do nếu × |
|---|---|---|---|---|---|
| `<Studio không lưu bảng duyệt quan điểm — test_viewpoint_selection = null. Leader tự duyệt khi chạy /review-tc>` | | | | | |
