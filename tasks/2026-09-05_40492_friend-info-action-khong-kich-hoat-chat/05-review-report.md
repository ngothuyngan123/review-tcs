# 05 — Review Report

> Draft cho Leader verify. Bug `#40492` · folder `tasks/2026-09-05_40492_friend-info-action-khong-kich-hoat-chat/` · **Round 1**.

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | **(1) MCP LME TEST STUDIO — task #275** |
| Vì sao không dùng nguồn ưu tiên cao hơn | `N.A. — đã dùng nguồn 1 (ưu tiên cao nhất)` |
| Ticket · task_id · round · branch | `40492` · `#275` · round `1` · `ai_fixbug_40492` |
| Thời điểm fetch | `2026-09-05` |
| Tổng số TC review | **27** |
| Snapshot đã ghi | `04-tc-list.md` — snapshot Studio đã ghi ở phiên `/new-task` cùng ngày (header `<!-- source: MCP LME TEST STUDIO — task_id=275 ... -->`), dữ liệu Studio **không đổi** giữa 2 lần gọi (`exec` y hệt) → **không refresh lại** |
| Đối chiếu chéo nguồn | `KHÔNG — đã dừng ở nguồn 1` |
| Tester được review | TC: `AI pipeline` 21 TC + `thanhntp` 6 TC · Chạy: auto runner `run #881` + manual `thanhntp` |
| Vòng review | `Round 1` · Studio `reviewState = leader`, `reviewed = false`, `aiResult = fail` |
| **Nguồn spec đã dùng** | [spec-features/admin/friend-information/feature-spec.md](../../spec-features/admin/friend-information/feature-spec.md) — **BR-04** (Action mode và trigger, dòng 463-465), luồng lưu setting (dòng 146, 181), bảng field `action_mode` (dòng 394). Đối chiếu thêm: [kho-tcs/fa015-quanlythongtinbanbe-友だち情報管理.md](../../kho-tcs/fa015-quanlythongtinbanbe-友だち情報管理.md) (TC-FRI-68, TC-FRI-69, MT-03, MT-12) |

### ⚠️ Đính chính so với summary của `/new-task`

`/new-task` báo *"chỉ 12/27 TC (44%) đạt"* — con số đó lấy từ `last_exec` của `testcase_list`, **chỉ phản ánh lần chạy CUỐI**. `task_get_report` cho thấy Studio có **2 luồng kết quả song song**:

| Luồng | Thời điểm | Kết quả |
|---|---|---|
| **Manual** (`thanhntp`) | local 09:21–10:21 · staging 13:52–14:31 | pass **38** · fail 0 · skip 16 |
| **Auto runner** (`run #881`, staging) | 14:34–15:28 | pass **11** · fail **2** · skip **13** |

Auto run chạy **SAU** manual nên ghi đè `last_exec` → nhiều TC manual đã pass bị hiển thị là `skip`. Bức tranh thật:

| Nhóm | Số TC | TC |
|---|---|---|
| ✅ Auto pass — **có screenshot + `actual` chi tiết** | 11 | NEW-53, 61, 62, 63, 64, 65, 66, 67, 68, 69, 70 |
| ⚠️ Chỉ manual pass — **`actual = null`, `evidence = []`** | 8 | NEW-55, 56, 57, 59, 60, 76, 78, 79 |
| ⛔ **Skip ở CẢ 2 luồng — chưa từng chạy** | 6 | NEW-54, 58, 71, 74, 75, 77 |
| ⛔ **FAIL** | 2 | NEW-72, NEW-73 |

> 2 lần chạy trước đó đều lỗi hạ tầng: `run #858` (local) `error`, `run #871` (staging) `error`.

**Cảnh báo bắt buộc về chất lượng nguồn — Nguồn 1 (Studio)**

| # | Chiều | Kết quả | Flag |
|---|---|---|---|
| 1 | Kết quả thực thi thật | **19/27 pass (70%)** nếu tính cả manual · **11/27 (41%)** nếu chỉ tính kết quả có bằng chứng · **8/27 (30%)** không có kết luận (6 skip + 2 fail) | `[MAJOR]` (tính cả manual) → `[BLOCKER]` nếu áp tiêu chí có-evidence |
| 2 | TC `fail` + ticket bug | `NEW-72` → bug Studio #856 → **Redmine #40527** (`status: logged`, **đang mở**). `NEW-73` **không có số ticket riêng** — nhưng bug #40527 ghi rõ *"Ảnh hưởng testcase: 15906, 15907"* → NEW-73 **đã được gộp vào cùng ticket**, không phải fail bỏ quên | `[BLOCKER]` (nội dung bug, không phải vì thiếu ticket) |
| 3 | Môi trường đã chạy | `PRODUCTION 0` · `STAGING 27` · `LOCAL` (manual, 1 phần) | `[MAJOR] RULE-08 / ENV-003` |
| 4 | Ai chạy | 26/27 `source = ai` (runner), 1 `manual`; toàn bộ luồng manual do `thanhntp` tick tay, **0 evidence** | `[MAJOR]` |
| 5 | Tác giả TC | `AI` 21 (78%) · `thanhntp` 6 (22%); `toolWritten.human = 0` (không TC nào nhập trên UI Studio) | `[MAJOR]` — ≥50% TC do AI sinh mà `reviewState` chưa `done` |
| 6 | Mã quan điểm KHÔNG có trong `checklist-lme.md` | **6 mã / 8 lượt TC**: `TOOL-NEGCTRL-001`(3) · `TOOL-SCOPE-001` · `RULE-12` · `TOOL-KNOW-002` · `TOOL-ERRHYG-001` · `TOOL-OLDREC-001` | `[MAJOR]` — 8 TC không tính là cover |

> ⚠️ Đặc biệt: **TC tái hiện bug chính `NEW-61` mang mã `TOOL-KNOW-002`** — mã nội bộ Studio, không phải quan điểm LME → theo BƯỚC 0.6 #6, TC quan trọng nhất của ticket **không map được vào coverage matrix**.

> ⚠️ Coverage engine của chính Studio: `total 49 · covered 0 · partial 47 · none 2`. Hai mục `none` là **BR-05** và **BR-08 — "Action theo giá trị (point/select)", ghi chú của Studio: *"điểm bug chính bị chạm"*, `tcIds: []`**. Tức rule nghiệp vụ lõi của ticket **không có TC nào link tới**.

---

## 1. Verdict

- [ ] **APPROVED**
- [ ] **APPROVED WITH CHANGES**
- [x] **REJECTED** — còn `[BLOCKER]`

**Lý do REJECTED**: 3/7 tệp Dev sửa **chưa từng được test** (6 TC skip cả 2 luồng), 1/7 tệp **test FAIL và bug vẫn đang mở** (#40527 — chứng minh bản vá ở tệp đó là **dead code**), và quan điểm `CONC-001` (Cao) **không có TC nào** trong khi fix đổi trực tiếp logic đọc/ghi cờ "chỉ chạy một lần".

---

## 2. Tóm tắt cho member

Bộ TC này **chất lượng viết rất tốt**: tiền điều kiện dựng được, expected đo lường được, 3 đối chứng âm tách đúng 3 chốt code, và phần chống trùng đã được làm chủ động (`NEW-53` ghi rõ *"GỘP từ 4 TC cũ trùng nhau"*, `NEW-54` gộp từ 2 TC) — rà 27 TC **không phát hiện trùng lặp nội dung**.

Vấn đề **không nằm ở chất lượng TC mà ở độ phủ thực thi**: fix quét ngang 11 điểm trên 7 tệp, nhưng chỉ **2/7 tệp** có kết quả pass kèm bằng chứng (chat 1:1 đường từng trường + màn 友だち詳細). 3 tệp API (app mobile v1/v2, API đặt lịch sự kiện) bị chặn vì không lấy được phiên xác thực mobile — và tester manual cũng tick `skip` với lý do ghi vỏn vẹn *"API"*. Tệp thứ 4 (đường lưu cụm đời cũ ở chat) FAIL với lỗi nặng hơn cả bug gốc.

Phải fix trước khi merge: (1) dựng được đường test cho 3 endpoint mobile/API, (2) đóng #40527 rồi chạy lại NEW-72/73, (3) bổ sung TC đồng thời `CONC-001`, (4) 38 kết quả manual pass phải có evidence — hiện toàn bộ `actual = null`.

**Lỗ hổng lớn nhất nằm ở trục KIỂU TRƯỜNG, không phải trục lối ghi.** Cả 27 TC Studio lẫn 11 TC ở §5 gần như chỉ test kiểu **選択肢**: kiểu **ポイント** chỉ có đúng 1 TC (`NEW-64`, ở 1 lối ghi, 1 chế độ), kiểu **年月日** có **0 TC**. Trong khi điều kiện Dev sửa là `if (type == point || type == select)` — tức **nửa phạm vi bản vá (kiểu điểm) gần như chưa được test**. §5.1 dựng ma trận đầy đủ 17 lối ghi × 5 tổ hợp (kiểu × chế độ) và bổ sung **60 TC regression** cho các ô trống.

---

## 3. Coverage Matrix

| Impact | Loại | TC cover (suy luận) | # TC | Exec | Status |
|---|---|---|---|---|---|
| `BUG` — sửa tay ở chat 1:1, mode 何度でも, cờ đã set → action phải chạy | Root cause | NEW-61 · NEW-79 (E2E đúng chuỗi ticket) | 2 | 1 auto-pass (evidence) · 1 manual-pass (no evidence) | `RISK` |
| `F1` `saveSettingDisplayInfoItem` (chat 1:1 từng trường) | Direct | NEW-61, 62, 63, 64, 65, 66, 67, 68, 53 | 9 | 9/9 auto-pass + screenshot | **`OK`** |
| `F2` `saveSettingDisplayInfo` (chat, lưu cụm đời cũ) | Direct | NEW-72, NEW-73 | 2 | **0/2 — FAIL cả 2** | **`GAP`** |
| `F3` `Api\FriendInformationController::saveCustomInfo` (v1) | Direct | NEW-74, NEW-54 | 2 | **0/2 — skip cả 2 luồng** | **`GAP`** |
| `F4` `saveCustomInfo2` (v2) | Direct | NEW-71, NEW-75 | 2 | **0/2 — skip cả 2 luồng** | **`GAP`** |
| `F5` `Admin\BotController::saveCustomInfo` (màn 友だち詳細) | Direct | NEW-69, NEW-70 | 2 | 2/2 auto-pass + screenshot | **`OK`** |
| `F6` `Basic\BookingEventController` (**2 nhánh**: 1473, 2008) | Direct | NEW-55, NEW-56 | 2 | manual-pass, no evidence · **chỉ nhánh 1473** | `RISK` |
| `F7` `Basic\BookingEventDayController` (**2 nhánh**: 2855, 3510) | Direct | NEW-57, NEW-76 | 2 | manual-pass, no evidence · **2 nhánh gộp 1 TC** | `RISK` |
| `F8` `Api\BookingEventController` (đặt lịch phía app) | Direct | NEW-77, NEW-58 | 2 | **0/2 — skip cả 2 luồng** | **`GAP`** |
| `F9` `setValueFriendInfo` → `LiffController` (QR landing) | Direct | NEW-59, NEW-78 | 2 | manual-pass, no evidence | `RISK` |
| `F10` `sendAction` / `HelperService` / `FriendDetailFriendInfoService` (bản chuẩn, **không sửa**) | Indirect | NEW-60 (smoke form) | 1 | manual-pass, no evidence | `RISK` |
| `F11` `saveSettingDisplayInfoV2` (Dev loại khỏi phạm vi) | Indirect | — | 0 | — | **`GAP`** |
| `F12` Nhóm khuôn khác **không sửa** (15+ điểm: form · bán hàng · salon · lesson · sự kiện bản dịch vụ) | Indirect | NEW-60 (1 TC đại diện cho toàn nhóm) | 1 | manual-pass, no evidence | `RISK` |
| `D1` Cột cờ "đã chạy" trên bảng giá trị friend info | UPDATE | NEW-53, 70, 56, 76, 78, 54, 75, 58, 68, 73 | 10 | 3 auto-pass · 3 manual-pass · 3 skip · 1 fail | `RISK` |
| `D3` Đọc bản ghi giá trị theo `friend + field` (thay cho `id` sót vòng lặp) | READ | NEW-73 | 1 | **FAIL** | **`GAP`** |
| `T1` 1-on-1 Chat (FA-001) | High | F1 + F2 TCs | 11 | 9 pass · 2 fail | `RISK` |
| `T2` Friend Information (FA-015) | High | toàn bộ 27 TC | 27 | xem trên | `RISK` |
| `T3` Action Settings (SC-004) — action đổi 対応ステータス | High | NEW-61, 62, 69, 53, 68 … | ~15 | phần lớn pass | `RISK` |
| `T4` Step Delivery / Scenario (FA-009) | High | NEW-61, 62, 79 | 3 | chỉ **NEW-79** verify tin **đến LINE thật** — và không có evidence | **`RISK`** |
| `T5` Event Booking (FA-021) | Medium | NEW-55, 56, 57, 76, 77, 58 | 6 | 4 manual-pass no-evidence · 2 skip | `RISK` |
| `T6` LIFF / trang mini (FA-011) | Medium | NEW-59, NEW-78 | 2 | manual-pass, no evidence | `RISK` |

**Tổng kết**: `OK` 2 · `RISK` 14 · `GAP` 5.

### ORPHAN TCs

Không có. 27/27 TC đều map được về ít nhất 1 impact trong `03-dev-impact.md`.

---

## 3.5 Fix-shape analysis (adversarial)

Fix shape đọc từ mục 2 `03-dev-impact.md`: **"sửa hàm dùng chung / quét ngang"** (11 điểm / 7 tệp, cùng một khuôn sửa) **+ "thêm điều kiện if"** (xét chế độ vận hành trước khi xét cờ) **+ "đổi trạng thái trên dữ liệu khách hàng"** (対応ステータス, tag, step delivery).

| Fix shape | Câu hỏi adversarial | Trả lời từ bộ TC | Kết luận |
|---|---|---|---|
| **Sửa hàm dùng chung / shared** (`REG-SHARED-001`) | Có danh sách nơi ảnh hưởng do Dev cung cấp? TC test **từng nơi**? Chức năng tương tự đã rà? | Danh sách **CÓ** (11 điểm / 7 tệp + nhóm "không sửa"). Nhưng chỉ **2/7 tệp** có kết quả pass kèm bằng chứng; 3/7 tệp **0 TC chạy được**; 1/7 **fail**. Chức năng tương tự (salon · lesson · bán hàng) chỉ có **1 TC smoke** (`NEW-60`) đại diện cho **15+ điểm** | **`[BLOCKER] FIX-SHAPE`** |
| **Thêm điều kiện if / validation** (`FUNC-002/003/004`) | Bao nhiêu biến thể input? Test **server-side** hay chỉ frontend? Đủ pattern biên? | Biên: 6 TC `Boundary` (vòng đời 一度のみ). Server-side: **có TC gọi thẳng endpoint** (`NEW-54, 71, 74, 75, 58, 77, 72, 73`) nhưng **6 skip + 2 fail** → thực tế **0 TC server-side chạy thành công** | **`[MAJOR] FIX-SHAPE`** |
| **Đổi trạng thái dữ liệu khách hàng** (`DATA-AUDIT-001`) | Có TC verify **lịch sử thao tác** đủ 4 thông tin, từ **TỪNG nguồn**? | Chỉ `NEW-61` kiểm bản ghi lịch sử (giá trị cũ → mới + tài khoản đăng nhập), và **chỉ ở lối chat 1:1**. 6 lối ghi còn lại (my page · API v1/v2 · booking × 3 · LIFF) **không TC nào kiểm lịch sử** | `[MAJOR] FIX-SHAPE` |
| **Gửi tin** (`MSG-001/004`, `BULK-001`) | Đếm tay số người khớp rồi đối chiếu số nhận thật? **Nhận tin THẬT trên LINE app**? | Cách ly đối tượng: `NEW-68` (X vs Y) — **tốt, auto-pass**. Nhận tin thật trên LINE: **chỉ `NEW-79`**, và TC này manual-pass **không evidence**. 26 TC còn lại dừng ở *"kịch bản bước được khởi động"* = quan sát màn admin | `[MAJOR] FIX-SHAPE` — RULE-06 |
| **Race / cờ chạy-một-lần** (`CONC-001`) | Đủ 4 kịch bản CONC-001? Evidence **đếm số lần xử lý thực tế**? | **KHÔNG có TC nào**. Fix đổi trực tiếp điều kiện đọc cờ + câu lệnh ghi cờ — đây chính là guard "chỉ chạy một lần", target kinh điển của race | **`[BLOCKER] FIX-SHAPE`** |

### Symptom-only KH report check

`01-bug-task.md` — KH mô tả **thuần triệu chứng**: *"Dù đã refresh màn hình, 対応ステータス・ステップ配信 vẫn không kích hoạt"*. Không có error code / log / message.

Nghiêm trọng hơn: **Dev tự thừa nhận chưa xác nhận cấu hình thật của account khách**:

> *"Nếu trường của khách đang để chế độ chạy một lần thì việc không kích hoạt lại là **đúng cài đặt**; khi đó cần hướng dẫn khách chuyển sang chế độ chạy nhiều lần"* — mục TỰ REVIEW, `03-dev-impact.md`

`NEW-79` cũng ghi ở note: *"Cần xác nhận cấu hình `action_mode` thực tế của account khách; case này áp dụng khi field đặt 「何度でも稼働」"*.

→ **`[MAJOR] SYMPTOM-ONLY`**: nếu trường 【来院状態】 của khách đang để 「一度のみ」 thì bản vá này **không giải quyết ca khách báo** — hiện tượng vẫn còn và câu trả lời đúng là hướng dẫn đổi cấu hình. Phải lấy cấu hình thật của bot `GCT Tokyoアイクリニック` trước khi kết luận fix xong.

### Anti-patterns

| AP | Dính? | Ghi chú |
|---|---|---|
| `[AP-1]` Single-trigger generic-fix | Không | Fix không phải generic catch; là đổi điều kiện, áp cùng khuôn ở 11 điểm |
| `[AP-2]` **Symptom-only KH report** | **Có** | Xem trên — cấu hình `action_mode` của account khách chưa xác nhận |
| `[AP-3]` **Happy-path-only regression** | **Có** | 4 impact `T*` risk High nhưng TC regression (`REG-SHARED-001`) **4/4 đều `Normal`**, tiền điều kiện toàn "data sạch". Không TC nào chạy regression ở edge state (friend đã block, step đang chạy dở, trường vừa đổi 稼働設定) |
| `[AP-4]` Specific code-check nguỵ trang generic catch | Một phần | Mục "Commit / Pull Request" **không có link PR** — chỉ có commit hash `f50ab9bd1b`. Không verify được fix shape thực tế từ diff |
| `[AP-5]` Layer-downstream over-coverage | Không | TC bám đúng layer bị sửa |
| `[AP-6]` Mục 3 dev-impact trống | Không | Mục 3 rất đầy đủ (11 điểm + nhóm không sửa) |

---

## 3.6 Bảng quan điểm đối chiếu

| Mã quan điểm | Ưu tiên | Trigger khớp task? | TC cover (suy luận) | Exec | Kết luận |
|---|---|---|---|---|---|
| `FUNC-001` | Cao | ◯ mọi chức năng | NEW-61, 69, 62, 63 … | pass | `OK` |
| `DATA-DB-001` ★ | Cao | ◯ UPDATE cột cờ + giá trị | NEW-70, 56, 76, 78, 54, 75, 58 | 1 auto-pass · 3 manual · 3 skip | **`RISK`** — thiếu `Abnormal` (RULE-01); verify cờ **bằng suy luận UI**, không query DB (RULE-07) |
| `REG-SHARED-001` | Cao | ◯ quét ngang 11 điểm | NEW-69, 55, 77, 60 | 1 auto-pass · 2 manual · 1 skip | **`RISK`** — 4/4 đều `Normal`, thiếu Abnormal + Boundary (RULE-01 + AP-3) |
| `COMPAT-LEGACY-001` ★ | Cao | ◯ đường lưu cụm **đời cũ** | NEW-72 | **FAIL** | **`GAP`** |
| `SYNC-APP-001` | TB → **Cao** (flow critical user-facing) | ◯ web ⇔ app mobile | NEW-71, NEW-74 | **skip cả 2** | **`GAP`** |
| `STATE-DEP-001` | Cao | ◯ step delivery đã lên lịch, gốc đổi | NEW-62 | auto-pass | `RISK` — 1 TC Normal, thiếu Abnormal/Boundary |
| `FRIEND-001` | Cao | ◯ ghi friend info + dùng làm điều kiện | NEW-64 | auto-pass | `RISK` — 1 TC (kiểu điểm); Catalog C `MAP-FI-*` có 11 nơi update, TC chỉ chạm 6 |
| `LIFF-ENTRY-001` ★ | Cao | ◯ QR landing sinh URL cho LINE user | NEW-59 | manual-pass no-evidence | `RISK` |
| `OUT-TRUTH-001` | Cao | ◯ thao tác lưu có thông báo kết quả | NEW-79 | manual-pass no-evidence | `RISK` |
| `SEC-ISO-001` | Cao | ◯ cách ly bạn bè X / Y | NEW-68 | auto-pass + screenshot | `OK` |
| `DATA-ID-001` | TB → **Cao** | ◯ ghi cờ nhầm bản ghi trường khác | NEW-73 | **FAIL** | **`GAP`** |
| `CONC-001` | **Cao** | ◯ cờ "chỉ chạy một lần" = guard idempotency; chat 1:1 nhiều nhân viên; auto-save khi đổi lựa chọn | **KHÔNG CÓ** | — | **`GAP` → `[BLOCKER]`** |
| `MSG-002` | Cao | ◯ action khởi động ステップ配信 | NEW-61, 62, 79 | chỉ NEW-79 đi tới LINE | **`RISK`** |
| `MSG-004` | Cao | ◯ output cuối = tin trên LINE app | NEW-79 | manual-pass no-evidence, không nêu iOS/Android | **`RISK`** — RULE-06 |
| `MSG-005` | **Cao** | ◯ Dev tự cảnh báo mode 何度でも → **số tin gửi tăng theo số lần nhân viên sửa** | **KHÔNG CÓ** | — | **`GAP` → `[MAJOR]`** |
| `MSG-USER-001` | Cao | ◯ tương tác friend LINE | NEW-79 | manual-pass | `RISK` |
| `DATA-AUDIT-001` | Cao | ◯ sửa dữ liệu khách hàng, có màn lịch sử thay đổi | NEW-61 (chỉ lối chat) | auto-pass | **`RISK`** — 6/7 lối ghi không kiểm lịch sử |
| `UI-003` | TB → **Cao** (rủi ro false success) | ◯ bug #40527: endpoint trả `success:false` — màn chat có báo lỗi hay im lặng? | **KHÔNG CÓ** | — | **`GAP` → `[MAJOR]`** |
| `ENV-003` ★ | Cao | ◯ job nền: production tách **3 job độc lập** (callback/broadcast/scenario), dev-staging chạy **1 job all** → hành vi ステップ配信 khác nhau | **0 TC production** | — | **`GAP` → `[MAJOR]` RULE-08** |
| `JOB-001` ★ | Cao | ◯ action khởi động kịch bản bước chạy nền | **KHÔNG CÓ** TC kiểm job/log | — | **`GAP`** |
| `PERM-003` | Cao | ◯ root cause dùng nhầm **`getBotId()`** cho cột định danh bạn bè → rủi ro nhiễm chéo bot | **KHÔNG CÓ** | — | `GAP` → `[MAJOR]` |
| `REG-RUN-001` | Cao | ◯ deploy khi có step delivery đang chạy dở | **KHÔNG CÓ** | — | `GAP` → `[MAJOR]` |
| `DATA-MIG-001` | Cao | × | — | — | Không đổi schema, không migration (`03` mục 4.2) |
| `DATA-BACKUP-001` | Cao | × | — | — | Không thêm/đổi bảng DB |
| `PAY-*` (7 mã) | Cao | × | — | — | Không chạm tiền / gói cước |
| `MEDIA-*` (3 mã) | — | × | — | — | Không chạm media/file |
| `INTG-HOOK-001/002` | Cao | × | — | — | Không nhận webhook ngoài; action là luồng nội bộ |
| `PERM-001/002/004` | Cao | × | — | — | Fix không chạm tầng phân quyền (theo nguyên tắc không mở rộng sang layer không bị sửa). ⚠️ Nhưng kho-tcs **MT-12** ghi FA-015 **chưa xác định** kiểm quyền ở đâu — xem §6 |
| `BULK-001` | Cao | × | — | — | Không có tổ hợp filter + thao tác hàng loạt trong phạm vi fix |
| `DEPLOY-ASSET-001` | Cao | × | — | — | Không sửa JS/CSS/asset (7 tệp đều là PHP backend) |

**Quan điểm ưu tiên Cao thiếu TC hoàn toàn**: `CONC-001` · `MSG-005` · `UI-003`(nâng Cao) · `ENV-003` · `JOB-001` · `PERM-003` · `REG-RUN-001` — **7 quan điểm**.

---

## 4. Issues phát hiện

### 4.1 Blocker (phải fix trước khi merge)

**`[BLOCKER]` NEW-72 / NEW-73 — bản vá ở `ChatController::saveSettingDisplayInfo` là dead code, chưa từng chạy được**
Bug #40527 (Studio bug #856, `status: logged`) chứng minh: endpoint `/ajax/saveSettingDisplayInfo` trả `HTTP 200 {"success":false}` **bất cứ khi nào payload có một trường kiểu 選択肢**. Nguyên nhân ghi trong bug: hàm duyệt `json_decode(setting_value)` ở mức ngoài cùng rồi lấy `$settingValue['value']` → ném lỗi với trường lựa chọn, bị catch chung, **kéo theo đoạn vá #40492 nằm cùng khối không bao giờ chạy tới**.
→ **Đề xuất**: (1) đóng #40527 trước; (2) chạy lại NEW-72 + NEW-73; (3) yêu cầu Dev xác nhận bản vá ở tệp này có ý nghĩa gì khi route đã hỏng sẵn — nếu route đời cũ thực sự không còn UI nào gọi, cân nhắc **gỡ route** thay vì vá.

**`[BLOCKER]` GAP-A — 3/7 tệp Dev sửa chưa từng được test (6 TC skip cả 2 luồng)**
`F3` `saveCustomInfo` (v1) · `F4` `saveCustomInfo2` (v2) · `F8` `Api\BookingEventController` — TC `NEW-54, 71, 74, 75, 58, 77`. Auto runner ghi lý do: *"Không có cách lấy phiên xác thực của ứng dụng di động … `/api/mobile/login` đòi email+mật khẩu"*. Tester manual tick `skip` với `actual` chỉ ghi **`"API"`**, không nêu lý do.
Đây là **API app mobile đang chạy production** và là điểm human trực tiếp chỉ định đưa vào phạm vi vòng 2.
→ **Đề xuất**: cấp tài khoản mobile test (email + mật khẩu) hoặc JWT dài hạn cho staging vào kho tài nguyên Studio; **không được** kết luận fix xong khi 3 điểm sửa chưa chạy dòng nào. Nếu không cấp được, chuyển 6 TC sang `manual` + tester đăng nhập app thật.

**`[BLOCKER]` GAP-B — `CONC-001` (Cao) không có TC nào**
Fix đổi trực tiếp (a) điều kiện đọc cờ "đã chạy" và (b) câu lệnh ghi cờ. Cờ này **chính là guard idempotency** của chế độ 「一度のみ」. Không TC nào thử: double-click nút lưu · 2 nhân viên mở 2 tab cùng sửa 1 bạn bè · gọi endpoint 2 lần song song. Hậu quả nếu lọt: action chạy 2 lần → step delivery khởi động 2 lần → **bạn bè nhận tin trùng**, và chế độ "chỉ chạy một lần" mất ý nghĩa.
→ **Đề xuất**: bổ sung `TC-CONC001-01/02/03` ở §5.

### 4.2 Major (nên fix)

**`[MAJOR]` RULE-02 — 38 kết quả manual `pass` nhưng `actual = null` và `evidence = []` toàn bộ**
Trong đó có chính `NEW-79` (E2E tái hiện ticket) và 4 TC đặt lịch sự kiện. Đối chiếu: 11 TC auto-pass đều có `actual` mô tả từng bước quan sát + screenshot. Kết quả manual hiện **không phân biệt được với "tick cho xong"**.
→ Yêu cầu `thanhntp` bổ sung `actual` + evidence cho **8 TC chỉ có manual pass** (NEW-55, 56, 57, 59, 60, 76, 78, 79) trước khi tính là coverage.

**`[MAJOR]` RULE-08 / `ENV-003` — 0 TC chạy production**
Catalog D: production tách **3 job độc lập** (callback / broadcast / scenario), dev-staging chạy **1 job all**. Action của ticket này khởi động **ステップ配信** = job scenario → hành vi có thể khác hẳn. Bug gốc phát sinh trên tài khoản production thật.
→ Bổ sung `TC-ENV003-01` (§5) chạy riêng trên `step.lme.jp`, **và** đặt `Phạm vi ENV = Tất cả` cho 10/11 TC còn lại ở §5 để nhánh `prd` được chạy — xem rào an toàn ở cuối §5.
→ Đồng thời: **27 TC hiện có ở Studio đã khai `env_scope = all`** (24/27) nhưng **chưa TC nào thực sự chạy trên `prd`** (`envAuto.prd.runs = 0`). Khai phạm vi ≠ đã chạy; yêu cầu chạy lại bộ TC ưu tiên cao trên production sau khi deploy.

**`[MAJOR]` RULE-06 / `MSG-004` — 26/27 TC dừng ở màn admin**
Expected của hầu hết TC là *"kịch bản bước được khởi động"* — quan sát trên màn quản trị. Chỉ `NEW-79` đi tới *"tài khoản LINE test thực sự nhận tin của bước đầu tiên"*, và TC đó không có evidence, cũng không nêu iOS/Android.
→ **Không đẻ TC mới** (NEW-79 đã cover đúng chiều): bổ sung vào `NEW-79` yêu cầu chụp màn hình LINE app **iOS và Android**, và chạy lại có evidence.

**`[MAJOR]` RULE-07 / `DATA-DB-001` — verify cờ bằng suy luận UI, không query DB**
Auto run tự khai ở `NEW-53`: *"Cờ đã-chạy **không đọc trực tiếp được trên env ngoài** (không có quyền vào DB app) nên được kiểm **gián tiếp**: lần 2/lần 3 không kích hoạt action nữa nghĩa là cờ đã được ghi đúng bản ghi"*. Quan điểm `DATA-DB-001` ghi rõ *"Không thay thế được bằng kiểm tra trên UI"*, Evidence bắt buộc = *"ảnh chụp kết quả query trước/sau (kèm câu query) trên 2 tài khoản"*.
Suy luận gián tiếp **không phân biệt được** 2 trường hợp: cờ ghi đúng bản ghi, hay action bị chặn vì lý do khác.
→ Cấp quyền đọc DB staging (read-only) cho runner, hoặc thêm endpoint debug trả về giá trị cờ.

**`[MAJOR]` `MSG-005` — không TC nào kiểm quota tin nhắn**
Dev tự cảnh báo: *"chế độ chạy nhiều lần: từ nay **mỗi lần đổi tay ở chat đều chạy action** — nếu action có gửi tin thì lượng tin gửi ra **tăng theo số lần nhân viên sửa**"*. Đây là thay đổi làm **tăng tiêu thụ quota LINE** — ảnh hưởng tiền của khách.
→ Bổ sung `TC-MSG005-01/02` (§5).

**`[MAJOR]` `UI-003` — không TC nào kiểm false success**
Bug #40527 cho thấy endpoint trả `success:false` mà giá trị không lưu. Không TC nào kiểm màn chat **có báo lỗi cho nhân viên** hay im lặng như không có gì. Triệu chứng khách báo ban đầu chính là "im lặng không có gì xảy ra".
→ Bổ sung `TC-UI003-01` (§5).

**`[MAJOR]` RULE-01 — 8 quan điểm ưu tiên Cao thiếu loại case, không ghi lý do**

| Quan điểm | Có | Thiếu |
|---|---|---|
| `DATA-DB-001` | Normal 2 · Boundary 5 | **Abnormal** |
| `REG-SHARED-001` | Normal 4 | **Abnormal + Boundary** |
| `COMPAT-LEGACY-001` | Normal 1 | **Abnormal + Boundary** |
| `LIFF-ENTRY-001` · `OUT-TRUTH-001` · `STATE-DEP-001` · `FRIEND-001` | Normal 1 | **Abnormal + Boundary** |
| `SEC-ISO-001` | Abnormal 1 | Normal + Boundary |

→ Ưu tiên bù `DATA-DB-001` (Abnormal) và `REG-SHARED-001` (Abnormal + Boundary) — xem §5. Các mã còn lại: ghi lý do vào `Ghi chú` nếu Leader chấp nhận.

**`[MAJOR]` `[AP-2]` SYMPTOM-ONLY — cấu hình `action_mode` thật của account khách chưa xác nhận**
Xem §3.5. Nếu trường 【来院状態】 của bot `GCT Tokyoアイクリニック` đang để 「一度のみ」 thì bản vá **không giải quyết ca khách báo**.
→ Lấy cấu hình thật trước khi trả lời khách; nếu đúng là 一度のみ → đây là **hướng dẫn sử dụng**, không phải bug, và phải nêu rõ trong ticket.

**`[MAJOR]` `[AP-3]` Happy-path-only regression** — 4 impact `T*` risk High nhưng toàn bộ TC regression đều `Normal` với tiền điều kiện "data sạch". Không có regression ở edge state.
→ Bổ sung `TC-REGSHARED001-02` (§5).

**`[MAJOR]` Nhánh code chưa được phủ hết trong cùng một tệp**
`NEW-57` note: *"Trong tệp này có **hai nhánh sửa (dòng 2855 và 3510)** cùng khối code và cùng root cause; chỉ viế[t]…"* (bị cắt) — tức 1 TC cho 2 nhánh.
`NEW-55` note: *"Điểm sửa `Basic\BookingEventController` **dòng 1473** (một trong hai nhánh của tệp này) … **Nhánh thứ hai (dòng 2008)** cùng kh[ối]…"* — nhánh 2008 **không có TC**.
`NEW-76` note tự đặt điều kiện: *"nếu hai nhánh ứng với hai thao tác UI khác nhau thì **phải chạy cùng kịch bản trên cả hai thao tác**"* — điều kiện này **chưa được thực hiện**.
→ Bổ sung `TC-REGSHARED001-01` (§5) cho nhánh 2008; xác nhận với Dev 2 nhánh của `BookingEventDayController` ứng với thao tác UI nào.

**`[MAJOR]` 8 TC mang mã quan điểm nội bộ Studio → không tính là cover**
`TOOL-NEGCTRL-001`(3) · `TOOL-SCOPE-001` · `TOOL-KNOW-002` · `TOOL-ERRHYG-001` · `TOOL-OLDREC-001` · `RULE-12`. Nghiêm trọng nhất: **`NEW-61` — TC tái hiện bug chính — mang `TOOL-KNOW-002`**.
→ Map lại sang mã tầng 1: `NEW-61` → `FRIEND-001` hoặc `STATE-DEP-001`; `NEW-65/66/67` → `FUNC-001`; `NEW-63` → `FUNC-002`; `NEW-53` → `DATA-DB-001`; `NEW-57` → `REG-SHARED-001`; `NEW-60` → `REG-SHARED-001`. Sửa trên Studio bằng `testcase_update`.

**`[MAJOR]` `[AP-4]` Không có link PR** — mục "Commit / Pull Request" chỉ có hash `f50ab9bd1b`, không có URL. Không verify được fix shape thực tế từ diff (Dev khai `git diff --stat`: 7 tệp, 100 thêm / 31 bớt).
→ Yêu cầu Dev gắn link PR/compare.

**`[MAJOR]` File `01` và `03` chưa được tester verify**
Cả hai đều có `Auto-filled: 2026-09-05 by /new-task` và checkbox **"Tester verify auto-fill chính xác" chưa tick**. Bảng `F*`/`D*`/`T*` ở `03` là do `/new-task` suy ra từ mục 3 + 4.1 của Dev (Dev ghi 4.1 là **"File thay đổi"**, không phải list function) → có thể lệch.
→ Tester đọc lại Redmine #40492 journal #134074 rồi tick 2 checkbox.

### 4.3 Minor (có thể fix sau)

**`[MINOR]` `Loại case` không nhất quán cho cùng một hình dạng kịch bản** — vòng đời 「一度のみ」 (lần 1 chạy → lần 2 không chạy) được gán `Boundary` ở `NEW-70, 56, 76, 54, 75` nhưng `Normal` ở `NEW-78, 58`. Chọn 1 quy ước.

**`[MINOR]` Note của `NEW-71` đã lỗi thời** — ghi *"chỉ cần một TC chế độ chạy nhiều lần là phủ đủ hai điểm sửa"*, nhưng sau đó `NEW-74` được thêm đúng cho v1 何度でも. Note nên sửa để không dẫn người sau bỏ bớt TC.

**`[MINOR]` RULE-02 — `Ghi chú` của 27 TC không ghi loại evidence bắt buộc** theo cột Evidence của quan điểm (VD `DATA-DB-001` → ảnh query trước/sau kèm câu query; `MSG-004` → screenshot LINE iOS + Android).

**`[MINOR]` `TC No.` dùng `temp_id` Studio (`NEW-53` … `NEW-79`)**, không theo format repo `TC-<mã quan điểm bỏ gạch>-<nn>`. Chấp nhận được vì nguồn là Studio, nhưng khi đồng bộ về sheet human cần ánh xạ lại.

### 4.4 Nit (gợi ý)

**`[NIT]`** `NEW-64` note: *"Chỉ cần 1 TC cho kiểu điểm vì cùng khối code; các kiể[u]…"* — hợp lý, nhưng Catalog C `MAP-FI-*` liệt kê **11 nơi update friend info**; bộ TC chạm 6. Ghi rõ 5 nơi còn lại được loại vì lý do gì.

**`[NIT]`** Studio `openBugs: 0` trong khi bug #856/#40527 `status: logged` và chưa `verified_at` — số liệu dashboard có thể gây hiểu nhầm là "không còn bug mở".

---

## 4.5 TC trùng lặp nội dung

**Đã rà toàn bộ 27 TC theo 4 yếu tố** (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương` → `kết quả mong đợi`) — **không phát hiện trùng lặp**.

Các cặp *trông giống nhau* đã được kiểm và kết luận **KHÔNG trùng**:

| Cặp nghi ngờ | Vì sao KHÔNG trùng |
|---|---|
| `NEW-55` ⟷ `NEW-57` (đặt lịch sự kiện vs theo ngày, cùng 何度でも) | **Tệp khác nhau** — `BookingEventController` vs `BookingEventDayController`. Note của `NEW-57` đã tự nêu đúng lý do |
| `NEW-53` ⟷ `NEW-68` (cùng dùng trường 一度のみ) | Trục khác nhau: `NEW-53` = trục **THỜI GIAN** (lần 1 vs lần 2 trên cùng bạn bè), `NEW-68` = trục **PHẠM VI** (bạn bè X vs Y trong cùng một lần) |
| `NEW-61` ⟷ `NEW-62` (cùng chat 1:1, 何度でも) | `NEW-61` = 1 lần đổi + kiểm lịch sử thay đổi; `NEW-62` = 3 lần liên tiếp + trục quay-về-giá-trị-cũ (A→B→A) |
| `NEW-65` ⟷ `NEW-66` ⟷ `NEW-67` (3 TC `TOOL-NEGCTRL-001` Abnormal, cùng màn) | Mỗi TC chặn ở **một chốt code khác nhau**: giá trị không đổi · lựa chọn không gắn action · trường kiểu văn bản. Gộp lại sẽ mất khả năng định vị chốt nào hỏng |
| `NEW-71` ⟷ `NEW-74` (API v2 vs v1, cùng 何度でも) | **Endpoint khác nhau** (dòng 837 vs 380), là 2 điểm sửa riêng |
| `NEW-61` ⟷ `NEW-79` | `NEW-79` là E2E đi hết chuỗi QR → Form → Chat và **verify tin đến LINE thật**; `NEW-61` chỉ đoạn chat + màn admin. `NEW-79` là superset nhưng bổ sung chiều output cuối → giữ cả hai |

> Bộ TC này đã được chống trùng **chủ động ngay khi viết**: `NEW-53` ghi *"GỘP từ 4 TC cũ trùng nhau"*, `NEW-54` ghi *"GỘP từ 2 TC cũ"*, và phần lớn TC có note giải thích trục khác biệt. Đây là điểm mạnh rõ rệt, nên giữ làm chuẩn cho các task sau.

---

## 5. TCs đề xuất bổ sung

> **Đã đối chiếu 27 TC ở BƯỚC 0 + [kho-tcs/fa015-quanlythongtinbanbe-友だち情報管理.md](../../kho-tcs/fa015-quanlythongtinbanbe-友だち情報管理.md) (443 dòng, nhóm「Ghi giá trị từ màn admin」·「Ghi giá trị từ tính năng khác」·「Info kiểu Lựa chọn」·「Action gán friend info」) — không TC đề xuất nào trùng.**
>
> **Đã dùng lại từ kho, KHÔNG viết mới**:
> - Regression multi-action ở chat 1:1 → dùng lại **`TC-FRI-202`** / **`TC-FRI-203`** (set/cộng/trừ/xóa/random điểm cho bạn CHƯA / ĐÃ có giá trị). Cần chỉnh: thêm bước xác nhận `action_mode` của trường và kiểm cờ sau thao tác.
> - Action chạy tới LINE user → **`TC-FRI-67`** (bạn bè chọn option có action → nhận tin trên LINE app). Cần chỉnh: đổi lối ghi từ 友だち詳細 sang chat 1:1.
> - 一度のみ gán lại lần 2 không chạy → **`TC-FRI-68`**; đổi 稼働設定 sang 何度でも → chạy lại → **`TC-FRI-69`**. Hai TC này là **oracle của spec BR-04** — dùng để đối chiếu, không viết lại.
>
> **Không viết TC mới cho RULE-06** vì `NEW-79` đã cover đúng chiều — chỉ ghi issue bổ sung steps/evidence ở §4.2.

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-CONC001-01 | UI | CONC-001 | Ghi giá trị từ màn admin | Normal | manual | Tất cả | Bấm lưu liên tiếp 2 lần thật nhanh trên cùng một lựa chọn ở chat 1:1 — action chỉ chạy đúng 1 lần | - Trường lựa chọn `TC40492_A`, 稼働設定 =「一度のみ」<br>- Lựa chọn `DaDenKham` gắn action đổi 対応ステータス sang `St2` + khởi động kịch bản bước `Step1`<br>- Bạn bè test đang có `ChuaDenKham`, cờ đã-chạy trống, 対応ステータス = `St0`<br>- Ghi lại số tin đã gửi cho bạn bè trước khi thao tác | 1. Mở chat 1:1, chọn bạn bè test<br>2. Ở thanh 友だち情報 bên phải bấm sửa trường `TC40492_A`<br>3. Chọn `DaDenKham` rồi bấm lưu **2 lần liên tiếp trong dưới 1 giây** (double-click)<br>4. Chờ lớp phủ tải biến mất, tải lại màn chat<br>5. Đếm số tin bạn bè nhận được và số lần kịch bản bước được khởi động | Double-click nút lưu với giá trị `DaDenKham` | - Giá trị lưu đúng `DaDenKham`<br>- 対応ステータス = `St2`<br>- Kịch bản bước `Step1` được khởi động **đúng 1 lần** — không có 2 phiên chạy song song<br>- Bạn bè nhận **đúng 1 tin** của bước đầu (số tin tăng đúng 1) | | Lấp GAP-B / cover impact `F1`, `D1` · Đánh giá spec: Spec không ghi (đã hỏi Leader — BR-04 không mô tả hành vi đồng thời) · Evidence: log/DB số lần action chạy + screenshot LINE app đếm tin (RULE-02 của CONC-001) |
| TC-CONC001-02 | UI | CONC-001 | Ghi giá trị từ màn admin | Abnormal | manual | Tất cả | Hai nhân viên mở 2 tab cùng sửa một bạn bè ở chế độ 一度のみ — action không được chạy 2 lần | - Như TC-CONC001-01<br>- Hai phiên đăng nhập khác nhau (nhân viên A và B) cùng bot, cùng mở chat 1:1 của **cùng một bạn bè test**<br>- Cả hai tab đều đã tải xong, trường đang hiển thị `ChuaDenKham` | 1. Tab A: bấm sửa trường `TC40492_A`, chọn `DaDenKham`, **chưa lưu**<br>2. Tab B: bấm sửa cùng trường, chọn `DaDenKham`, **chưa lưu**<br>3. Bấm lưu ở tab A, ngay sau đó (dưới 1 giây) bấm lưu ở tab B<br>4. Tải lại cả hai tab<br>5. Đếm số lần action chạy và số tin bạn bè nhận | Hai phiên cùng ghi `DaDenKham` gần như đồng thời | - Giá trị cuối cùng là `DaDenKham` ở cả 2 tab<br>- Action chạy **đúng 1 lần**: 対応ステータス = `St2`, kịch bản bước chỉ có **1 phiên** đang chạy<br>- Bạn bè nhận **đúng 1 tin**, không nhận tin trùng<br>- Cờ đã-chạy được ghi đúng 1 lần trên bản ghi của bạn bè test | | Lấp GAP-B / cover impact `F1`, `D1` · Đánh giá spec: Spec không ghi (đã hỏi Leader) · Evidence: log số lần action + screenshot 2 tab + screenshot LINE app |
| TC-CONC001-03 | API | CONC-001 | Ghi giá trị từ màn admin | Boundary | auto | Tất cả | Gọi endpoint lưu giá trị 5 lần song song ở chế độ 一度のみ — đúng 1 lần chạy action | - Như TC-CONC001-01, cờ đã-chạy trống<br>- Có phiên web hợp lệ để gọi thẳng endpoint lưu từng trường của chat 1:1 | 1. Ghi lại 対応ステータス và số tin đã gửi<br>2. Bắn **5 request song song** cùng lúc tới endpoint lưu giá trị, tất cả cùng đổi `TC40492_A` sang `DaDenKham`<br>3. Ghi nhận mã trạng thái của cả 5 phản hồi<br>4. Mở chat 1:1, tải lại và đọc 対応ステータス<br>5. Đếm số phiên kịch bản bước và số tin bạn bè nhận | 5 request đồng thời, cùng giá trị `DaDenKham` | - Giá trị lưu `DaDenKham`<br>- 対応ステータス đổi **đúng 1 lần** sang `St2`<br>- Chỉ **1 phiên** kịch bản bước được khởi động<br>- Bạn bè nhận **đúng 1 tin**<br>- Không phản hồi nào trả lỗi 500 | | Lấp GAP-B / cover impact `F1`, `D1` · Đánh giá spec: Spec không ghi (đã hỏi Leader) · Evidence: log 5 response + query đếm bản ghi kịch bản bước |
| TC-ENV003-01 | UI | ENV-003 | Action gán friend info | Normal | manual | product | Trên production, đổi giá trị ở chat 1:1 chế độ 何度でも — kịch bản bước khởi động qua job scenario riêng | - **Môi trường production** (`step.lme.jp`), bot nội bộ dùng để smoke (KHÔNG dùng bot khách)<br>- Trường lựa chọn 稼働設定 =「何度でも稼働」, lựa chọn gắn action khởi động kịch bản bước có bước đầu gửi text<br>- Bạn bè test là tài khoản LINE nội bộ đã kết bạn<br>- Xác nhận trước: production tách 3 job độc lập (callback / broadcast / scenario) | 1. Ghi lại 対応ステータス và giờ hệ thống<br>2. Ở chat 1:1 đổi giá trị trường sang lựa chọn có gắn action<br>3. Tải lại màn chat, ghi nhận 対応ステータス<br>4. Mở LINE app của bạn bè test, chờ tới mốc gửi của bước đầu<br>5. Đối chiếu giờ nhận tin thực tế với giờ cấu hình của bước<br>6. Đọc log job scenario trên production | Đổi 1 lần trên bot smoke production | - 対応ステータス đổi đúng action<br>- Kịch bản bước được khởi động và **job scenario của production** xử lý được (log có bản ghi tương ứng)<br>- Bạn bè **nhận tin bước đầu trên LINE app thật**, sai số giờ nhận so với cấu hình nằm trong ngưỡng cho phép<br>- Không có bản ghi lỗi trong log job | | Lấp GAP `ENV-003` + `JOB-001` / cover impact `T4` · RULE-08: job nền → bắt buộc production · Đánh giá spec: Spec ghi rõ (Catalog D — production tách 3 job) · Evidence: screenshot LINE app + log job scenario production · ⚠️ Không tạo/xoá data thật |
| TC-MSG005-01 | UI | MSG-005 | Action gán friend info | Normal | manual | Tất cả | Chế độ 何度でも — sửa tay 5 lần thì tiêu thụ đúng 5 tin, không nhiều hơn | - Trường lựa chọn 稼働設定 =「何度でも稼働」, **cả hai lựa chọn** gắn action gửi 1 tin text phân biệt được<br>- Bạn bè test đã kết bạn, đọc được số tin còn lại trong tháng của bot trước khi test<br>- Phép tính tay: 5 lần đổi giá trị × 1 tin/action = **5 tin** | 1. Ghi lại số tin đã dùng trong tháng của bot ở màn thống kê<br>2. Ở chat 1:1 đổi giá trị qua lại 5 lần (A→B→A→B→A), mỗi lần chờ lưu xong<br>3. Tải lại màn thống kê số tin<br>4. Mở LINE app của bạn bè, đếm số tin thực nhận<br>5. Đối chiếu 3 con số: số lần thao tác · số tin trừ vào quota · số tin nhận thật | 5 lần đổi giá trị · phép tính tay: 5 × 1 = 5 tin | - Số tin đã dùng tăng **đúng 5**<br>- Bạn bè nhận **đúng 5 tin** trên LINE app<br>- Ba con số khớp nhau: 5 thao tác = 5 tin trừ quota = 5 tin nhận | | Lấp GAP `MSG-005` / cover rủi ro Dev tự nêu ở mục TỰ REVIEW `03` · Đánh giá spec: Spec không ghi (đã hỏi Leader) · Evidence: screenshot màn thống kê trước/sau + screenshot LINE app đếm tin |
| TC-MSG005-02 | UI | MSG-005 | Action gán friend info | Boundary | manual | Tất cả | Sửa tay khi quota tin nhắn còn 1 / hết — hành vi tại limit−1 và limit | - Như TC-MSG005-01<br>- Bot được đặt về trạng thái quota **chỉ còn 1 tin** trong tháng (dùng bot test riêng, không dùng bot khách)<br>- Trường 稼働設定 =「何度でも稼働」, action gửi 1 tin | 1. Xác nhận số tin còn lại = 1<br>2. Đổi giá trị lần 1 → chờ lưu xong, kiểm tra bạn bè có nhận tin không<br>3. Xác nhận số tin còn lại = 0<br>4. Đổi giá trị lần 2 → chờ lưu xong<br>5. Quan sát: giá trị có lưu không, action có chạy không, màn hình có cảnh báo gì, bạn bè có nhận tin không | Quota: 1 → 0 → thử vượt | - Lần 1 (limit−1): tin gửi được, quota về 0<br>- Lần 2 (vượt limit): **giá trị vẫn được lưu** và 対応ステータス vẫn đổi, nhưng tin **không gửi được**; hệ thống hiển thị cảnh báo hết quota đúng nội dung theo plan, **không văng lỗi 500**<br>- Bạn bè không nhận tin thứ 2 | | Lấp GAP `MSG-005` (RULE-01 — Boundary) · Đánh giá spec: Spec không ghi (đã hỏi Leader — cần chốt: hết quota thì action có được coi là đã chạy và ghi cờ không?) · Evidence: screenshot cảnh báo + màn thống kê quota + LINE app |
| TC-UI003-01 | UI | UI-003 | Ghi giá trị từ màn admin | Abnormal | manual | Tất cả | Khi lưu giá trị thất bại ở chat 1:1, màn hình phải báo lỗi rõ — không được im lặng như đã lưu | - Trường lựa chọn có gắn action, bạn bè test đang có `ChuaDenKham`<br>- Dựng được điều kiện lưu thất bại (VD: dùng đường lưu cụm đời cũ đang lỗi theo bug #40527, hoặc ngắt mạng giữa chừng, hoặc bot đang bị khoá do backup) | 1. Ghi lại giá trị hiện tại và 対応ステータス<br>2. Ở chat 1:1 đổi giá trị trường sang lựa chọn có gắn action<br>3. Kích hoạt điều kiện thất bại ở bước 2<br>4. Quan sát màn hình ngay sau thao tác: có thông báo lỗi không, trường hiển thị giá trị nào<br>5. Tải lại màn chat và đọc lại giá trị thật đã lưu | Thao tác lưu bị thất bại phía server | - Màn hình **hiển thị thông báo lỗi rõ ràng**, không im lặng<br>- Trường **không hiển thị giá trị mới** như thể đã lưu thành công<br>- Sau khi tải lại, giá trị vẫn là `ChuaDenKham` (đúng dữ liệu thật)<br>- 対応ステータス không đổi | | Lấp GAP `UI-003` (nâng Cao — rủi ro false success) · Liên quan trực tiếp bug #40527 và triệu chứng gốc khách báo ("không có gì xảy ra") · Đánh giá spec: Spec không ghi (đã hỏi Leader) · Evidence: screenshot 3 trạng thái + đối chiếu dữ liệu thật sau reload |
| TC-DATADB001-01 | Data | DATA-DB-001 | Ghi giá trị từ màn admin | Abnormal | auto | Tất cả | Gọi lưu giá trị với định danh bạn bè **không thuộc bot đang đăng nhập** — không ghi giá trị, không ghi cờ, không chạy action | - Hai bot A và B do cùng tài khoản quản trị<br>- Cả hai bot đều có trường lựa chọn **trùng tên** `TC40492_A` (RULE-07 — kiểm `WHERE` scope trên 2 tài khoản), 稼働設定 =「一度のみ」, lựa chọn gắn action<br>- Bạn bè `FA` thuộc bot A, bạn bè `FB` thuộc bot B; cả hai cờ đã-chạy trống<br>- Đang đăng nhập và chọn **bot A** | 1. Ghi lại giá trị, cờ đã-chạy và 対応ステータス của cả `FA` và `FB`<br>2. Đang ở bot A, gọi endpoint lưu giá trị nhưng truyền định danh của bạn bè `FB` (thuộc bot B)<br>3. Ghi nhận phản hồi<br>4. Kiểm tra giá trị + cờ đã-chạy + 対応ステータス của `FB`<br>5. Kiểm tra lại `FA` xem có bị ghi nhầm không | Bot đăng nhập = A · định danh bạn bè truyền vào = `FB` (bot B) | - Phản hồi từ chối thao tác (không thành công), **không văng lỗi 500**<br>- `FB`: giá trị **không đổi**, cờ đã-chạy **vẫn trống**, 対応ステータス **không đổi**, không nhận tin nào<br>- `FA`: không bị ghi nhầm giá trị hay cờ<br>- Không có bản ghi lịch sử thay đổi nào được tạo | | Lấp RULE-01 (`DATA-DB-001` thiếu Abnormal) + GAP `PERM-003` / cover impact `D1` · Bám đúng root cause: câu ghi cờ trước fix so **cột định danh bạn bè với `getBotId()`** · Đánh giá spec: Spec không ghi (đã hỏi Leader) · Evidence: ảnh chụp query cờ trước/sau kèm câu query, trên **cả 2 bot** |
| TC-REGSHARED001-01 | UI | REG-SHARED-001 | Ghi giá trị từ tính năng khác | Normal | manual | Tất cả | Nhánh thứ hai của đặt lịch sự kiện (điểm sửa dòng 2008) ghi friend info — action chạy đúng chế độ | - Xác nhận với Dev thao tác UI nào đi vào nhánh **dòng 2008** của `Basic\BookingEventController` (khác thao tác đã test ở `NEW-55` — nhánh 1473)<br>- Sự kiện test có 予約枠 còn chỗ và có mục biểu mẫu ánh xạ tới trường `TC40492_A` (稼働設定 =「何度でも稼働」, lựa chọn gắn action)<br>- Bạn bè test đang có `ChuaDenKham`, cờ đã-chạy **khác rỗng** | 1. Ghi lại 対応ステータス của bạn bè test<br>2. Thực hiện đúng thao tác đi vào nhánh 2008 để tạo lượt đặt lịch ghi `DaDenKham`<br>3. Chờ lưu xong<br>4. Mở chat 1:1 của bạn bè test, tải lại màn<br>5. Đọc giá trị trường, 対応ステータス và trạng thái kịch bản bước | Lượt đặt lịch qua nhánh 2008, ghi `TC40492_A = DaDenKham` | - Lượt đặt lịch tạo thành công<br>- Giá trị trường đổi thành `DaDenKham`<br>- Action **CHẠY** dù cờ đã-chạy khác rỗng: 対応ステータス đổi đúng, kịch bản bước được khởi động | | Lấp GAP nhánh code chưa phủ (§4.2) / cover impact `F6` · Đánh giá spec: Spec không ghi (đã hỏi Dev để xác định thao tác UI của nhánh 2008) · Evidence: screenshot màn đặt lịch + màn chat sau reload |
| TC-REGSHARED001-02 | UI | REG-SHARED-001 | Action gán friend info | Abnormal | manual | Tất cả | Đổi giá trị khi bạn bè đang có kịch bản bước chạy dở và đã chặn bot — action xử lý đúng, không lỗi | - Trường lựa chọn 稼働設定 =「何度でも稼働」, lựa chọn gắn action khởi động kịch bản bước<br>- Bạn bè `F1`: **đang có một kịch bản bước chạy dở** (chưa tới bước cuối) từ lần đổi giá trị trước<br>- Bạn bè `F2`: **đã chặn bot** (block) nhưng vẫn còn trong danh sách<br>- Ghi lại trạng thái kịch bản của `F1` và 対応ステータス của cả hai | 1. Với `F1`: ở chat 1:1 đổi giá trị sang lựa chọn khác có gắn action<br>2. Tải lại màn, quan sát kịch bản bước cũ và kịch bản mới<br>3. Với `F2` (đã chặn bot): đổi giá trị sang lựa chọn có gắn action<br>4. Tải lại màn, quan sát 対応ステータス và trạng thái gửi tin<br>5. Kiểm tra log xem có lỗi hệ thống nào phát sinh không | `F1` có kịch bản chạy dở · `F2` đã block bot | - `F1`: kịch bản cũ được xử lý đúng theo cấu hình (dừng hoặc chạy tiếp — theo chốt của Leader), kịch bản mới khởi động, **không tồn tại 2 kịch bản trùng chạy song song ngoài ý muốn**<br>- `F2`: giá trị vẫn lưu và 対応ステータス vẫn đổi; việc gửi tin thất bại do bị chặn được ghi nhận đúng, **không văng lỗi hệ thống**<br>- Log không có exception | | Lấp `[AP-3]` happy-path-only regression + RULE-01 (`REG-SHARED-001` thiếu Abnormal) / cover impact `T4` · regression · Đánh giá spec: Spec không ghi (đã hỏi Leader — cần chốt hành vi kịch bản chạy dở khi giá trị đổi lại) · Evidence: screenshot trạng thái kịch bản trước/sau + log |
| TC-DATAAUDIT001-01 | UI | DATA-AUDIT-001 | Ghi giá trị từ màn admin | Normal | manual | Tất cả | Lịch sử thay đổi friend info được ghi đủ 4 thông tin ở **mọi lối ghi**, không chỉ chat 1:1 | - Trường lựa chọn `TC40492_A` có gắn action, 稼働設定 =「何度でも稼働」<br>- Bạn bè test đang có `ChuaDenKham`<br>- Có sẵn: phiên web admin · phiên app mobile · 1 sự kiện đặt lịch có ánh xạ trường này | 1. Đổi giá trị qua **màn 友だち詳細** (my page) → mở phần lịch sử thay đổi, ghi nhận bản ghi<br>2. Đổi giá trị qua **app mobile** (API lưu thông tin bạn bè) → mở lịch sử, ghi nhận<br>3. Đổi giá trị qua **đặt lịch sự kiện** → mở lịch sử, ghi nhận<br>4. Đổi giá trị qua **chat 1:1** (đối chứng, đã có ở `NEW-61`) → mở lịch sử<br>5. Đối chiếu 4 bản ghi lịch sử | 4 lối ghi khác nhau, mỗi lối đổi 1 lần | - Mỗi lối ghi tạo **đúng 1 bản ghi lịch sử** cho đúng bạn bè và đúng trường<br>- Mỗi bản ghi có đủ 4 thông tin: **ai** thao tác (tài khoản/nguồn) · **khi nào** · **hành động** · **giá trị cũ → mới**<br>- Lối ghi từ app mobile và từ đặt lịch ghi rõ nguồn, không để trống người thao tác | | Lấp GAP `DATA-AUDIT-001` (6/7 lối ghi chưa kiểm lịch sử) / cover impact `F3`, `F5`, `F6` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` không mô tả lịch sử cho lối app mobile / booking) · Evidence: screenshot 4 bản ghi lịch sử đặt cạnh nhau |

**Tổng: 11 TC bổ sung** — `CONC-001` 3 (RULE-01 đủ 3 loại case) · `ENV-003`/`JOB-001` 1 · `MSG-005` 2 · `UI-003` 1 · `DATA-DB-001` 1 · `REG-SHARED-001` 2 · `DATA-AUDIT-001` 1.

> ⚠️ **Cột `Nhóm` ở bảng trên đã ghi đè `GROUP_MAP`** — không suy máy móc từ tiền tố mã quan điểm. Quy tắc áp dụng: **`API` chỉ khi tester phải tự dựng request ngoài** (không có màn hình nào làm được thao tác đó) · **`UI` khi human thao tác được trên màn hình** · **`Data` khi tầng kiểm chứng là dữ liệu lưu trữ**.
>
> | TC | `GROUP_MAP` suy ra | Đã ghi đè thành | Lý do |
> |---|---|---|---|
> | `TC-CONC001-01` · `TC-CONC001-02` | `API` (`CONC-` → API) | **`UI`** | Double-click nút lưu · 2 nhân viên mở 2 tab — đều là thao tác tay trên màn chat 1:1 |
> | `TC-CONC001-03` | `API` | `API` (giữ) | Bắn 5 request song song — không có UI nào làm được |
> | `TC-ENV003-01` | `API` (`ENV-` → API) | **`UI`** | Human đổi giá trị ở chat 1:1 trên production rồi quan sát LINE app; log job chỉ là bằng chứng phụ |
> | `TC-MSG005-01` · `TC-MSG005-02` | `API` (`MSG-` → API) | **`UI`** | Đổi giá trị ở chat 1:1, đọc màn thống kê quota, đếm tin trên LINE app — toàn thao tác màn hình |
> | `TC-DATADB001-01` | `Data` (`DATA-` → Data) | `Data` (giữ) | Tầng kiểm chứng là query cờ đã-chạy trên DB (RULE-07) |
> | `TC-DATAAUDIT001-01` | `Data` | **`UI`** | Đọc bản ghi lịch sử thay đổi **trên màn hình**; 4 lối ghi đều có giao diện thật (kể cả app mobile) |
>
> Khi member copy sang `04-tc-list.md` hoặc khi `/sync-review-tc` đẩy lên Studio, **giữ nguyên giá trị `Nhóm` ở bảng này**, không để `GROUP_MAP` tự gán lại.

> ⚠️ **Cột `Phạm vi ENV` — 10/11 TC để `Tất cả`** (Studio `env_scope = ["dev","local","prd","staging"]`), không dùng default `staging` của quy trình. Lý do:
>
> 1. **Bộ TC nguồn đã là `all`**: 24/27 TC Studio khai `env_scope = all`; chỉ `NEW-59` · `NEW-78` · `NEW-79` để `dev, staging` (vì cần tài khoản LINE thật). §5 không có cớ gì hẹp hơn nguồn.
> 2. **Bản vá này chạm 11 điểm trên 7 tệp** — trong đó có API app mobile, LIFF, job scenario. Hành vi **khác nhau theo môi trường**: production tách 3 job độc lập còn dev/staging chạy 1 job all (Catalog D); phiên xác thực mobile khác nhau; LIFF/QR landing cần domain public.
> 3. **Bug gốc phát sinh trên production thật** — chốt "đã fix" mà chưa chạm production là đúng thứ RULE-08 cấm.
>
> `TC-ENV003-01` giữ **`product`** vì bản thân nó là TC chỉ có ý nghĩa trên production (đối chiếu job scenario tách riêng).
>
> **Rào an toàn khi chạy nhánh `prd`** — 3 TC cần Leader duyệt trước, không để member tự chạy:
>
> | TC | Rủi ro trên production | Cách chạy an toàn |
> |---|---|---|
> | `TC-MSG005-02` | Hạ quota bot xuống còn 1 tin = **đụng tiền thật** của hợp đồng | Dùng bot nội bộ có plan riêng, không dùng bot khách; hoặc chọn bot đã gần hết quota tự nhiên cuối tháng thay vì ép quota |
> | `TC-CONC001-03` | Bắn 5 request song song lên production = tạo tải + ghi dữ liệu thật | Giới hạn 1 bạn bè test nội bộ, chạy ngoài giờ cao điểm |
> | `TC-DATADB001-01` | Cần 2 bot có trường trùng tên + truyền định danh chéo bot | Chỉ dùng 2 bot nội bộ; **tuyệt đối không** truyền định danh bạn bè của bot khách |
>
> Mọi TC chạy trên production đều **không tạo/xoá data thật của khách** — chỉ dùng bot và bạn bè test nội bộ.

---

## 5.1 Regression — ma trận lối ghi friend info × loại info × chế độ action

> **Nguồn danh sách lối ghi**: Test Leader cung cấp (2026-09-05), đối chiếu khớp với Catalog C.2 [`MAP-FI-06`..`MAP-FI-09`](../../framework/catalog-lme.md) — Web cập nhật (chat 1:1 · my page · import CSV · multi action) · Job cập nhật (QR landing có param) · LINE user cập nhật (form nhập trong booking event/lesson/salon/item/form) · App cập nhật (my page).

> **Đã xác minh bằng live MCP `STAGING_LME` (`list_friend_info_fields`, bot `BOT_staging`)** — không suy đoán:
>
> - Kiểu trường thực có: `input` (text) · `select` · `calendar` (年月日) · `point` · `image` · `file`.
> - **`calendar` luôn có `action_mode: "1"`** ở 11/11 trường trên bot, **không trường nào là `"2"`** → xác nhận kiểu 年月日 **không có** 稼働設定 一度のみ/何度でも như Leader nêu. Trục tương đương của 年月日 là `option_compare1`: 「月日」(lặp hàng năm) / 「年月日」(1 lần), và action đi qua **lịch gửi + job nền** (`event_step_time` → `NewEventRemindTask`), KHÔNG qua khối code đã sửa.
> - `select` và `point` đều có thật cả `action_mode` `"1"` và `"2"` trên bot staging.
>
> ⚠️ Hệ quả quan trọng: điều kiện Dev sửa là `if ($dataType == point || $dataType == select)` (note của `NEW-64`) → **kiểu 年月日 không đi qua nhánh vừa sửa**. TC cột 年月日 dưới đây là **regression thuần** (chứng minh bản vá không làm hỏng lịch gửi), không phải verify fix.

### Ma trận phủ

| # | Lối ghi friend info | Catalog | 選択肢 × 一度のみ | 選択肢 × 何度でも | ポイント × 一度のみ | ポイント × 何度でも | 年月日 |
|---|---|---|---|---|---|---|---|
| 1 | Chat 1:1 — thanh 友だち情報 bên phải | `MAP-FI-06` | ✅ NEW-53 · NEW-68 | ✅ NEW-61/62/63/65/66 | **TC-FRIEND001-01** | ✅ NEW-64 | **TC-FRIEND001-02** |
| 2 | My page (màn 友だち詳細 web admin) | `MAP-FI-06` | ✅ NEW-70 | ✅ NEW-69 | **TC-FRIEND001-03** | **TC-FRIEND001-04** | **TC-FRIEND001-05** |
| 3 | Quản lý CSV — import (modal chọn CÓ chạy action) | `MAP-FI-06` | **TC-FRIEND001-06** | **TC-FRIEND001-07** | **TC-FRIEND001-08** | **TC-FRIEND001-09** | ✅ TC-FRI-243 (kho) |
| 4 | Quản lý CSV — import (modal chọn KHÔNG chạy action) | `MAP-FI-06` | **TC-FRIEND001-10** | **TC-FRIEND001-10** | **TC-FRIEND001-11** | **TC-FRIEND001-11** | **TC-FRIEND001-12** |
| 5 | Multi action phía job | `MAP-FI-06` | **TC-FRIEND001-13** | **TC-FRIEND001-14** | **TC-FRIEND001-15** | **TC-FRIEND001-16** | **TC-FRIEND001-17** |
| 6 | QR landing có set import param | `MAP-FI-07` | ✅ NEW-78 | ✅ NEW-59 | **TC-FRIEND001-18** | **TC-FRIEND001-19** | **TC-FRIEND001-20** |
| 7 | Event booking — form nhập thông tin khi booking | `MAP-FI-08` | ✅ NEW-56 · NEW-76 · NEW-58 | ✅ NEW-55 · NEW-57 · NEW-77 | — N/A | — N/A | **TC-FRIEND001-21** |
| 8 | Lesson booking — form nhập thông tin khi booking | `MAP-FI-08` | **TC-FRIEND001-22** | **TC-FRIEND001-23** | — N/A | — N/A | **TC-FRIEND001-24** |
| 9 | Salon booking — form nhập thông tin khi booking | `MAP-FI-08` | **TC-FRIEND001-25** | **TC-FRIEND001-26** | — N/A | — N/A | **TC-FRIEND001-27** |
| 10 | Item (商品販売) — form nhập thông tin khi mua item | `MAP-FI-08` | **TC-FRIEND001-28** | **TC-FRIEND001-29** | — N/A | — N/A | **TC-FRIEND001-30** |
| 11 | Form — form nhập thông tin (user trả lời form) | `MAP-FI-08` | **TC-FRIEND001-31** | ✅ NEW-60 | — N/A | — N/A | **TC-FRIEND001-32** |
| 12 | Event booking — multi action lúc booking / change / cancel | `MAP-FI-06` | **TC-FRIEND001-33** | **TC-FRIEND001-34** | **TC-FRIEND001-35** | **TC-FRIEND001-36** | **TC-FRIEND001-37** |
| 13 | Lesson booking — multi action lúc booking / cancel | `MAP-FI-06` | **TC-FRIEND001-38** | **TC-FRIEND001-39** | **TC-FRIEND001-40** | **TC-FRIEND001-41** | **TC-FRIEND001-42** |
| 14 | Salon booking — multi action lúc booking / cancel | `MAP-FI-06` | **TC-FRIEND001-43** | **TC-FRIEND001-44** | **TC-FRIEND001-45** | **TC-FRIEND001-46** | **TC-FRIEND001-47** |
| 15 | Item — multi action lúc open item / mua item thành công | `MAP-FI-06` | **TC-FRIEND001-48** | **TC-FRIEND001-49** | **TC-FRIEND001-50** | **TC-FRIEND001-51** | **TC-FRIEND001-52** |
| 16 | Form — multi action lúc mở form / lúc trả lời form | `MAP-FI-06` | **TC-FRIEND001-53** | **TC-FRIEND001-54** | **TC-FRIEND001-55** | **TC-FRIEND001-56** | **TC-FRIEND001-57** |
| 17 | App mobile — màn My page | `MAP-FI-09` | ✅ NEW-54 · NEW-75 ⚠skip | ✅ NEW-71 · NEW-74 ⚠skip | **TC-FRIEND001-58** | **TC-FRIEND001-59** | **TC-FRIEND001-60** |

**Tổng kết** — 85 ô (17 lối ghi × 5 tổ hợp): **13 ô đã có TC** (2 ô app mobile ⚠️ chưa từng chạy) · **10 ô N/A** (form nhập × ポイント) · **62 ô trống → sinh 60 TC regression mới** (lối import CSV «KHÔNG chạy action» dùng 2 TC phủ 4 ô, vì kết quả mong đợi ở cả 2 chế độ giống nhau: action không chạy).

> Ô `✅` dẫn TC hiện có ở Studio (`NEW-xx`) hoặc kho (`TC-FRI-xxx`) — **không viết lại** (BƯỚC 5b chống trùng). Riêng `app mobile × 選択肢` tuy có TC nhưng **skip cả 2 luồng** → vẫn là rủi ro, đã ghi ở `[BLOCKER] GAP-A` §4.1, không đẻ TC mới.

### TC regression bổ sung (14 cột)

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-FRIEND001-01 | UI | FRIEND-001 | Chat 1:1 — thanh 友だち情報 bên phải | Boundary | manual | Tất cả | Chat 1:1 ghi trường ポイント — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Đăng nhập web admin, chọn đúng bot<br>- Trường đã được bật hiển thị ở thanh 友だち情報 của chat 1:1<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Mở màn chat 1:1, chọn bạn bè test<br>2. Ở thanh 友だち情報 bên phải bấm biểu tượng sửa của trường TC40492_REG_Point<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Lần 1: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-06 — lối ghi «Chat 1:1» × ポイント (điểm) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-02 | UI | FRIEND-001 | Chat 1:1 — thanh 友だち情報 bên phải | Normal | manual | Tất cả | Chat 1:1 ghi trường 年月日 — lịch gửi sinh đúng và job gửi đúng mốc | - Đăng nhập web admin, chọn đúng bot<br>- Trường đã được bật hiển thị ở thanh 友だち情報 của chat 1:1<br>- Trường `TC40492_REG_Date` kiểu 年月日 với cấu hình lịch: 登録 =「月日」, `từ 0 ngày`, `後`, giờ gửi trong ngày còn tới được<br>- Action của lịch: gửi 1 tin text phân biệt được<br>- ⚠️ Kiểu 年月日 **KHÔNG có** 稼働設定 一度のみ/何度でも — trục tương ứng là 登録 「月日」(lặp hàng năm) / 「年月日」(1 lần)<br>- Bạn bè test chưa có giá trị cho trường này | 1. Mở màn chat 1:1, chọn bạn bè test<br>2. Ở thanh 友だち情報 bên phải bấm biểu tượng sửa của trường TC40492_REG_Date<br>3. Kiểm tra giá trị đã lưu và lịch gửi vừa sinh (mốc gửi, trạng thái)<br>4. Chờ tới mốc gửi, mở LINE app của bạn bè test và xác nhận tin nhận được<br>5. Đọc log job và trạng thái lịch sau khi gửi | Ghi giá trị ngày = hôm nay | Giá trị `TC40492_REG_Date` được ghi đúng cho đúng bạn bè.<br>Lịch gửi của trường được **sinh mới hoặc cập nhật** đúng mốc tính từ giá trị vừa ghi.<br>Tới mốc gửi, job chạy và bạn bè **nhận đúng 1 tin trên LINE app**; lịch chuyển sang trạng thái đã gửi.<br>Không phát sinh lịch trùng và không có lỗi trong log job. |  | Regression ma trận MAP-FI-06 — lối ghi «Chat 1:1» × 年月日 (ngày tháng) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · liên quan `JOB-001` — lịch gửi qua job nền; nhánh code này **KHÔNG** nằm trong điều kiện đã sửa (`if type == point hoặc type == select`) nên là regression thuần · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + trạng thái lịch gửi · regression |
| TC-FRIEND001-03 | UI | FRIEND-001 | My page (màn 友だち詳細 web admin) | Boundary | manual | Tất cả | My page (màn 友だち詳細 web admin) ghi trường ポイント — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Đăng nhập web admin, chọn đúng bot<br>- Mở được màn 友だち詳細 của bạn bè test<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Từ danh sách bạn bè mở màn 友だち詳細 của bạn bè test<br>2. Vào khu vực thông tin bạn bè, bật chế độ chỉnh sửa cho trường TC40492_REG_Point<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Lần 1: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-06 — lối ghi «My page (màn 友だち詳細 web admin)» × ポイント (điểm) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-04 | UI | FRIEND-001 | My page (màn 友だち詳細 web admin) | Normal | manual | Tất cả | My page (màn 友だち詳細 web admin) ghi trường ポイント — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Đăng nhập web admin, chọn đúng bot<br>- Mở được màn 友だち詳細 của bạn bè test<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Từ danh sách bạn bè mở màn 友だち詳細 của bạn bè test<br>2. Vào khu vực thông tin bạn bè, bật chế độ chỉnh sửa cho trường TC40492_REG_Point<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Cả hai lần: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-06 — lối ghi «My page (màn 友だち詳細 web admin)» × ポイント (điểm) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-05 | UI | FRIEND-001 | My page (màn 友だち詳細 web admin) | Normal | manual | Tất cả | My page (màn 友だち詳細 web admin) ghi trường 年月日 — lịch gửi sinh đúng và job gửi đúng mốc | - Đăng nhập web admin, chọn đúng bot<br>- Mở được màn 友だち詳細 của bạn bè test<br>- Trường `TC40492_REG_Date` kiểu 年月日 với cấu hình lịch: 登録 =「月日」, `từ 0 ngày`, `後`, giờ gửi trong ngày còn tới được<br>- Action của lịch: gửi 1 tin text phân biệt được<br>- ⚠️ Kiểu 年月日 **KHÔNG có** 稼働設定 一度のみ/何度でも — trục tương ứng là 登録 「月日」(lặp hàng năm) / 「年月日」(1 lần)<br>- Bạn bè test chưa có giá trị cho trường này | 1. Từ danh sách bạn bè mở màn 友だち詳細 của bạn bè test<br>2. Vào khu vực thông tin bạn bè, bật chế độ chỉnh sửa cho trường TC40492_REG_Date<br>3. Kiểm tra giá trị đã lưu và lịch gửi vừa sinh (mốc gửi, trạng thái)<br>4. Chờ tới mốc gửi, mở LINE app của bạn bè test và xác nhận tin nhận được<br>5. Đọc log job và trạng thái lịch sau khi gửi | Ghi giá trị ngày = hôm nay | Giá trị `TC40492_REG_Date` được ghi đúng cho đúng bạn bè.<br>Lịch gửi của trường được **sinh mới hoặc cập nhật** đúng mốc tính từ giá trị vừa ghi.<br>Tới mốc gửi, job chạy và bạn bè **nhận đúng 1 tin trên LINE app**; lịch chuyển sang trạng thái đã gửi.<br>Không phát sinh lịch trùng và không có lỗi trong log job. |  | Regression ma trận MAP-FI-06 — lối ghi «My page (màn 友だち詳細 web admin)» × 年月日 (ngày tháng) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · liên quan `JOB-001` — lịch gửi qua job nền; nhánh code này **KHÔNG** nằm trong điều kiện đã sửa (`if type == point hoặc type == select`) nên là regression thuần · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + trạng thái lịch gửi · regression |
| TC-FRIEND001-06 | UI | FRIEND-001 | Quản lý CSV — import (modal chọn CÓ chạy action) | Boundary | manual | Tất cả | Quản lý CSV ghi trường 選択肢 — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Đăng nhập web admin, chọn đúng bot<br>- Có file CSV chứa cột định danh bạn bè và cột giá trị của trường TC40492_REG_Select<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Mở màn quản lý CSV, chọn import file CSV đã chuẩn bị<br>2. Ở modal lựa chọn khi import, chọn **CÓ chạy action**<br>3. Xác nhận import và chờ xử lý xong<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Lần 1: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-06 — lối ghi «Quản lý CSV» × 選択肢 (lựa chọn) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · ⚠️ Modal lựa chọn chạy/không chạy action khi import CSV — đối chiếu với `TC-FRI-243` của kho (ghi 年月日 qua CSV KHÔNG kích hoạt action) và mâu thuẫn corpus nêu ở kho MT-16 · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-07 | UI | FRIEND-001 | Quản lý CSV — import (modal chọn CÓ chạy action) | Normal | manual | Tất cả | Quản lý CSV ghi trường 選択肢 — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Đăng nhập web admin, chọn đúng bot<br>- Có file CSV chứa cột định danh bạn bè và cột giá trị của trường TC40492_REG_Select<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Mở màn quản lý CSV, chọn import file CSV đã chuẩn bị<br>2. Ở modal lựa chọn khi import, chọn **CÓ chạy action**<br>3. Xác nhận import và chờ xử lý xong<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Cả hai lần: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-06 — lối ghi «Quản lý CSV» × 選択肢 (lựa chọn) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · ⚠️ Modal lựa chọn chạy/không chạy action khi import CSV — đối chiếu với `TC-FRI-243` của kho (ghi 年月日 qua CSV KHÔNG kích hoạt action) và mâu thuẫn corpus nêu ở kho MT-16 · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-08 | UI | FRIEND-001 | Quản lý CSV — import (modal chọn CÓ chạy action) | Boundary | manual | Tất cả | Quản lý CSV ghi trường ポイント — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Đăng nhập web admin, chọn đúng bot<br>- Có file CSV chứa cột định danh bạn bè và cột giá trị của trường TC40492_REG_Point<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Mở màn quản lý CSV, chọn import file CSV đã chuẩn bị<br>2. Ở modal lựa chọn khi import, chọn **CÓ chạy action**<br>3. Xác nhận import và chờ xử lý xong<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Lần 1: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-06 — lối ghi «Quản lý CSV» × ポイント (điểm) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · ⚠️ Modal lựa chọn chạy/không chạy action khi import CSV — đối chiếu với `TC-FRI-243` của kho (ghi 年月日 qua CSV KHÔNG kích hoạt action) và mâu thuẫn corpus nêu ở kho MT-16 · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-09 | UI | FRIEND-001 | Quản lý CSV — import (modal chọn CÓ chạy action) | Normal | manual | Tất cả | Quản lý CSV ghi trường ポイント — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Đăng nhập web admin, chọn đúng bot<br>- Có file CSV chứa cột định danh bạn bè và cột giá trị của trường TC40492_REG_Point<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Mở màn quản lý CSV, chọn import file CSV đã chuẩn bị<br>2. Ở modal lựa chọn khi import, chọn **CÓ chạy action**<br>3. Xác nhận import và chờ xử lý xong<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Cả hai lần: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-06 — lối ghi «Quản lý CSV» × ポイント (điểm) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · ⚠️ Modal lựa chọn chạy/không chạy action khi import CSV — đối chiếu với `TC-FRI-243` của kho (ghi 年月日 qua CSV KHÔNG kích hoạt action) và mâu thuẫn corpus nêu ở kho MT-16 · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-10 | UI | FRIEND-001 | Quản lý CSV — import (modal chọn KHÔNG chạy action) | Abnormal | manual | Tất cả | Quản lý CSV — import KHÔNG chạy action với trường 選択肢: giá trị vẫn ghi, action không chạy | - Đăng nhập web admin, chọn đúng bot<br>- Có file CSV chứa cột định danh bạn bè và cột giá trị của trường TC40492_REG_Select<br>- Trường `TC40492_REG_Select` kiểu 選択肢, chạy lần lượt CẢ HAI 稼働設定「一度のみ」và「何度でも稼働」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Mở màn quản lý CSV, chọn import file CSV đã chuẩn bị<br>2. Ở modal lựa chọn khi import, chọn **KHÔNG chạy action**<br>3. Xác nhận import và chờ xử lý xong<br>4. Kiểm tra giá trị đã lưu, 対応ステータス và số tin bạn bè nhận<br>5. Lặp lại toàn bộ kịch bản với 稼働設定 còn lại và đối chiếu kết quả | Đổi `ChuaDenKham` → `DaDenKham` | Import thành công, giá trị `TC40492_REG_Select` của bạn bè được cập nhật đúng.<br>**KHÔNG action nào chạy** ở CẢ hai 稼働設定: 対応ステータス giữ nguyên `St0`, không kịch bản bước nào khởi động, bạn bè không nhận tin nào.<br>Cờ đã-chạy trên bản ghi giá trị **không bị đổi** bởi lần import này. |  | Regression ma trận MAP-FI-06 — lối ghi «Quản lý CSV» × 選択肢 (lựa chọn) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · ⚠️ Modal lựa chọn chạy/không chạy action khi import CSV — đối chiếu với `TC-FRI-243` của kho (ghi 年月日 qua CSV KHÔNG kích hoạt action) và mâu thuẫn corpus nêu ở kho MT-16 · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-11 | UI | FRIEND-001 | Quản lý CSV — import (modal chọn KHÔNG chạy action) | Abnormal | manual | Tất cả | Quản lý CSV — import KHÔNG chạy action với trường ポイント: giá trị vẫn ghi, action không chạy | - Đăng nhập web admin, chọn đúng bot<br>- Có file CSV chứa cột định danh bạn bè và cột giá trị của trường TC40492_REG_Point<br>- Trường `TC40492_REG_Point` kiểu ポイント, chạy lần lượt CẢ HAI 稼働設定「一度のみ」và「何度でも稼働」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Mở màn quản lý CSV, chọn import file CSV đã chuẩn bị<br>2. Ở modal lựa chọn khi import, chọn **KHÔNG chạy action**<br>3. Xác nhận import và chờ xử lý xong<br>4. Kiểm tra giá trị đã lưu, 対応ステータス và số tin bạn bè nhận<br>5. Lặp lại toàn bộ kịch bản với 稼働設定 còn lại và đối chiếu kết quả | Ghi điểm `5` → `10` (đạt ngưỡng) | Import thành công, giá trị `TC40492_REG_Point` của bạn bè được cập nhật đúng.<br>**KHÔNG action nào chạy** ở CẢ hai 稼働設定: 対応ステータス giữ nguyên `St0`, không kịch bản bước nào khởi động, bạn bè không nhận tin nào.<br>Cờ đã-chạy trên bản ghi giá trị **không bị đổi** bởi lần import này. |  | Regression ma trận MAP-FI-06 — lối ghi «Quản lý CSV» × ポイント (điểm) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · ⚠️ Modal lựa chọn chạy/không chạy action khi import CSV — đối chiếu với `TC-FRI-243` của kho (ghi 年月日 qua CSV KHÔNG kích hoạt action) và mâu thuẫn corpus nêu ở kho MT-16 · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-12 | UI | FRIEND-001 | Quản lý CSV — import (modal chọn KHÔNG chạy action) | Abnormal | manual | Tất cả | Quản lý CSV — import KHÔNG chạy action với trường 年月日: giá trị vẫn ghi, action không chạy | - Đăng nhập web admin, chọn đúng bot<br>- Có file CSV chứa cột định danh bạn bè và cột giá trị của trường TC40492_REG_Date<br>- Trường `TC40492_REG_Date` kiểu 年月日 với cấu hình lịch: 登録 =「月日」, `từ 0 ngày`, `後`, giờ gửi trong ngày còn tới được<br>- Action của lịch: gửi 1 tin text phân biệt được<br>- ⚠️ Kiểu 年月日 **KHÔNG có** 稼働設定 一度のみ/何度でも — trục tương ứng là 登録 「月日」(lặp hàng năm) / 「年月日」(1 lần)<br>- Bạn bè test chưa có giá trị cho trường này | 1. Mở màn quản lý CSV, chọn import file CSV đã chuẩn bị<br>2. Ở modal lựa chọn khi import, chọn **KHÔNG chạy action**<br>3. Xác nhận import và chờ xử lý xong<br>3. Kiểm tra giá trị đã lưu và lịch gửi vừa sinh (mốc gửi, trạng thái)<br>4. Chờ tới mốc gửi, mở LINE app của bạn bè test và xác nhận tin nhận được<br>5. Đọc log job và trạng thái lịch sau khi gửi | Ghi giá trị ngày = hôm nay | Import thành công, giá trị `TC40492_REG_Date` của bạn bè được cập nhật đúng.<br>**KHÔNG action nào chạy** ở CẢ hai 稼働設定: 対応ステータス giữ nguyên `St0`, không kịch bản bước nào khởi động, bạn bè không nhận tin nào.<br>Với trường kiểu 年月日: **không** sinh/cập nhật lịch gửi. |  | Regression ma trận MAP-FI-06 — lối ghi «Quản lý CSV» × 年月日 (ngày tháng) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · liên quan `JOB-001` — lịch gửi qua job nền; nhánh code này **KHÔNG** nằm trong điều kiện đã sửa (`if type == point hoặc type == select`) nên là regression thuần · ⚠️ Modal lựa chọn chạy/không chạy action khi import CSV — đối chiếu với `TC-FRI-243` của kho (ghi 年月日 qua CSV KHÔNG kích hoạt action) và mâu thuẫn corpus nêu ở kho MT-16 · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + trạng thái lịch gửi · regression |
| TC-FRIEND001-13 | UI | FRIEND-001 | Multi action phía job | Boundary | manual | Tất cả | Multi action phía job ghi trường 選択肢 — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Đã tạo một multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Select<br>- Multi action đó được gắn vào một trigger chạy nền (VD tự động trả lời theo từ khoá)<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Kích hoạt trigger để job chạy multi action ghi giá trị TC40492_REG_Select cho bạn bè test<br>3. Chờ job xử lý xong<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Lần 1: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-06 — lối ghi «Multi action phía job» × 選択肢 (lựa chọn) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-14 | UI | FRIEND-001 | Multi action phía job | Normal | manual | Tất cả | Multi action phía job ghi trường 選択肢 — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Đã tạo một multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Select<br>- Multi action đó được gắn vào một trigger chạy nền (VD tự động trả lời theo từ khoá)<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Kích hoạt trigger để job chạy multi action ghi giá trị TC40492_REG_Select cho bạn bè test<br>3. Chờ job xử lý xong<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Cả hai lần: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-06 — lối ghi «Multi action phía job» × 選択肢 (lựa chọn) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-15 | UI | FRIEND-001 | Multi action phía job | Boundary | manual | Tất cả | Multi action phía job ghi trường ポイント — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Đã tạo một multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Point<br>- Multi action đó được gắn vào một trigger chạy nền (VD tự động trả lời theo từ khoá)<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Kích hoạt trigger để job chạy multi action ghi giá trị TC40492_REG_Point cho bạn bè test<br>3. Chờ job xử lý xong<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Lần 1: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-06 — lối ghi «Multi action phía job» × ポイント (điểm) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-16 | UI | FRIEND-001 | Multi action phía job | Normal | manual | Tất cả | Multi action phía job ghi trường ポイント — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Đã tạo một multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Point<br>- Multi action đó được gắn vào một trigger chạy nền (VD tự động trả lời theo từ khoá)<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Kích hoạt trigger để job chạy multi action ghi giá trị TC40492_REG_Point cho bạn bè test<br>3. Chờ job xử lý xong<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Cả hai lần: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-06 — lối ghi «Multi action phía job» × ポイント (điểm) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-17 | UI | FRIEND-001 | Multi action phía job | Normal | manual | Tất cả | Multi action phía job ghi trường 年月日 — lịch gửi sinh đúng và job gửi đúng mốc | - Đã tạo một multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Date<br>- Multi action đó được gắn vào một trigger chạy nền (VD tự động trả lời theo từ khoá)<br>- Trường `TC40492_REG_Date` kiểu 年月日 với cấu hình lịch: 登録 =「月日」, `từ 0 ngày`, `後`, giờ gửi trong ngày còn tới được<br>- Action của lịch: gửi 1 tin text phân biệt được<br>- ⚠️ Kiểu 年月日 **KHÔNG có** 稼働設定 一度のみ/何度でも — trục tương ứng là 登録 「月日」(lặp hàng năm) / 「年月日」(1 lần)<br>- Bạn bè test chưa có giá trị cho trường này | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Kích hoạt trigger để job chạy multi action ghi giá trị TC40492_REG_Date cho bạn bè test<br>3. Chờ job xử lý xong<br>3. Kiểm tra giá trị đã lưu và lịch gửi vừa sinh (mốc gửi, trạng thái)<br>4. Chờ tới mốc gửi, mở LINE app của bạn bè test và xác nhận tin nhận được<br>5. Đọc log job và trạng thái lịch sau khi gửi | Ghi giá trị ngày = hôm nay | Giá trị `TC40492_REG_Date` được ghi đúng cho đúng bạn bè.<br>Lịch gửi của trường được **sinh mới hoặc cập nhật** đúng mốc tính từ giá trị vừa ghi.<br>Tới mốc gửi, job chạy và bạn bè **nhận đúng 1 tin trên LINE app**; lịch chuyển sang trạng thái đã gửi.<br>Không phát sinh lịch trùng và không có lỗi trong log job. |  | Regression ma trận MAP-FI-06 — lối ghi «Multi action phía job» × 年月日 (ngày tháng) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · liên quan `JOB-001` — lịch gửi qua job nền; nhánh code này **KHÔNG** nằm trong điều kiện đã sửa (`if type == point hoặc type == select`) nên là regression thuần · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + trạng thái lịch gửi · regression |
| TC-FRIEND001-18 | UI | FRIEND-001 | QR landing có set import param | Boundary | manual | Tất cả | QR landing có set import param ghi trường ポイント — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Trang landing test đã bật nhận tham số, khai báo param trỏ tới trường TC40492_REG_Point<br>- Có tài khoản LINE test CHƯA kết bạn với bot<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Mở landing kèm tham số ghi giá trị vào TC40492_REG_Point bằng tài khoản LINE test chưa kết bạn<br>2. Hoàn tất luồng kết bạn qua landing<br>3. Trên web admin mở bạn bè vừa tạo<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Lần 1: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-07 — lối ghi «QR landing có set import param» × ポイント (điểm) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-07` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-19 | UI | FRIEND-001 | QR landing có set import param | Normal | manual | Tất cả | QR landing có set import param ghi trường ポイント — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Trang landing test đã bật nhận tham số, khai báo param trỏ tới trường TC40492_REG_Point<br>- Có tài khoản LINE test CHƯA kết bạn với bot<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Mở landing kèm tham số ghi giá trị vào TC40492_REG_Point bằng tài khoản LINE test chưa kết bạn<br>2. Hoàn tất luồng kết bạn qua landing<br>3. Trên web admin mở bạn bè vừa tạo<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Cả hai lần: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-07 — lối ghi «QR landing có set import param» × ポイント (điểm) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-07` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-20 | UI | FRIEND-001 | QR landing có set import param | Normal | manual | Tất cả | QR landing có set import param ghi trường 年月日 — lịch gửi sinh đúng và job gửi đúng mốc | - Trang landing test đã bật nhận tham số, khai báo param trỏ tới trường TC40492_REG_Date<br>- Có tài khoản LINE test CHƯA kết bạn với bot<br>- Trường `TC40492_REG_Date` kiểu 年月日 với cấu hình lịch: 登録 =「月日」, `từ 0 ngày`, `後`, giờ gửi trong ngày còn tới được<br>- Action của lịch: gửi 1 tin text phân biệt được<br>- ⚠️ Kiểu 年月日 **KHÔNG có** 稼働設定 一度のみ/何度でも — trục tương ứng là 登録 「月日」(lặp hàng năm) / 「年月日」(1 lần)<br>- Bạn bè test chưa có giá trị cho trường này | 1. Mở landing kèm tham số ghi giá trị vào TC40492_REG_Date bằng tài khoản LINE test chưa kết bạn<br>2. Hoàn tất luồng kết bạn qua landing<br>3. Trên web admin mở bạn bè vừa tạo<br>3. Kiểm tra giá trị đã lưu và lịch gửi vừa sinh (mốc gửi, trạng thái)<br>4. Chờ tới mốc gửi, mở LINE app của bạn bè test và xác nhận tin nhận được<br>5. Đọc log job và trạng thái lịch sau khi gửi | Ghi giá trị ngày = hôm nay | Giá trị `TC40492_REG_Date` được ghi đúng cho đúng bạn bè.<br>Lịch gửi của trường được **sinh mới hoặc cập nhật** đúng mốc tính từ giá trị vừa ghi.<br>Tới mốc gửi, job chạy và bạn bè **nhận đúng 1 tin trên LINE app**; lịch chuyển sang trạng thái đã gửi.<br>Không phát sinh lịch trùng và không có lỗi trong log job. |  | Regression ma trận MAP-FI-07 — lối ghi «QR landing có set import param» × 年月日 (ngày tháng) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-07` · liên quan `JOB-001` — lịch gửi qua job nền; nhánh code này **KHÔNG** nằm trong điều kiện đã sửa (`if type == point hoặc type == select`) nên là regression thuần · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + trạng thái lịch gửi · regression |
| TC-FRIEND001-21 | UI | FRIEND-001 | Event booking — form nhập thông tin khi booking | Normal | manual | Tất cả | Event booking ghi trường 年月日 — lịch gửi sinh đúng và job gửi đúng mốc | - Sự kiện test còn 予約枠 đặt được<br>- Tab 予約ページ設定 có mục nhập với 紐づけ友だち情報 = trường TC40492_REG_Date<br>- Trường `TC40492_REG_Date` kiểu 年月日 với cấu hình lịch: 登録 =「月日」, `từ 0 ngày`, `後`, giờ gửi trong ngày còn tới được<br>- Action của lịch: gửi 1 tin text phân biệt được<br>- ⚠️ Kiểu 年月日 **KHÔNG có** 稼働設定 一度のみ/何度でも — trục tương ứng là 登録 「月日」(lặp hàng năm) / 「年月日」(1 lần)<br>- Bạn bè test chưa có giá trị cho trường này | 1. Bằng tài khoản LINE test, mở trang đặt lịch sự kiện<br>2. Điền mục form có ánh xạ tới TC40492_REG_Date với giá trị mới<br>3. Hoàn tất đặt lịch và chờ xử lý xong<br>3. Kiểm tra giá trị đã lưu và lịch gửi vừa sinh (mốc gửi, trạng thái)<br>4. Chờ tới mốc gửi, mở LINE app của bạn bè test và xác nhận tin nhận được<br>5. Đọc log job và trạng thái lịch sau khi gửi | Ghi giá trị ngày = hôm nay | Giá trị `TC40492_REG_Date` được ghi đúng cho đúng bạn bè.<br>Lịch gửi của trường được **sinh mới hoặc cập nhật** đúng mốc tính từ giá trị vừa ghi.<br>Tới mốc gửi, job chạy và bạn bè **nhận đúng 1 tin trên LINE app**; lịch chuyển sang trạng thái đã gửi.<br>Không phát sinh lịch trùng và không có lỗi trong log job. |  | Regression ma trận MAP-FI-08 — lối ghi «Event booking» × 年月日 (ngày tháng) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-08` · liên quan `JOB-001` — lịch gửi qua job nền; nhánh code này **KHÔNG** nằm trong điều kiện đã sửa (`if type == point hoặc type == select`) nên là regression thuần · ⚠️ Xác nhận mục form nhập chọn được 紐づけ友だち情報 kiểu 年月日 (kiểu ポイント đã xác nhận là KHÔNG map được → đánh N/A) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + trạng thái lịch gửi · regression |
| TC-FRIEND001-22 | UI | FRIEND-001 | Lesson booking — form nhập thông tin khi booking | Boundary | manual | Tất cả | Lesson booking ghi trường 選択肢 — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Lịch レッスン予約 test còn khung giờ đặt được<br>- Form nhập của lesson có mục ánh xạ tới trường TC40492_REG_Select<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Bằng tài khoản LINE test, mở trang đặt lịch lesson<br>2. Điền mục form có ánh xạ tới TC40492_REG_Select với giá trị mới<br>3. Hoàn tất đặt lịch và chờ xử lý xong<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Lần 1: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-08 — lối ghi «Lesson booking» × 選択肢 (lựa chọn) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-08` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-23 | UI | FRIEND-001 | Lesson booking — form nhập thông tin khi booking | Normal | manual | Tất cả | Lesson booking ghi trường 選択肢 — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Lịch レッスン予約 test còn khung giờ đặt được<br>- Form nhập của lesson có mục ánh xạ tới trường TC40492_REG_Select<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Bằng tài khoản LINE test, mở trang đặt lịch lesson<br>2. Điền mục form có ánh xạ tới TC40492_REG_Select với giá trị mới<br>3. Hoàn tất đặt lịch và chờ xử lý xong<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Cả hai lần: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-08 — lối ghi «Lesson booking» × 選択肢 (lựa chọn) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-08` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-24 | UI | FRIEND-001 | Lesson booking — form nhập thông tin khi booking | Normal | manual | Tất cả | Lesson booking ghi trường 年月日 — lịch gửi sinh đúng và job gửi đúng mốc | - Lịch レッスン予約 test còn khung giờ đặt được<br>- Form nhập của lesson có mục ánh xạ tới trường TC40492_REG_Date<br>- Trường `TC40492_REG_Date` kiểu 年月日 với cấu hình lịch: 登録 =「月日」, `từ 0 ngày`, `後`, giờ gửi trong ngày còn tới được<br>- Action của lịch: gửi 1 tin text phân biệt được<br>- ⚠️ Kiểu 年月日 **KHÔNG có** 稼働設定 一度のみ/何度でも — trục tương ứng là 登録 「月日」(lặp hàng năm) / 「年月日」(1 lần)<br>- Bạn bè test chưa có giá trị cho trường này | 1. Bằng tài khoản LINE test, mở trang đặt lịch lesson<br>2. Điền mục form có ánh xạ tới TC40492_REG_Date với giá trị mới<br>3. Hoàn tất đặt lịch và chờ xử lý xong<br>3. Kiểm tra giá trị đã lưu và lịch gửi vừa sinh (mốc gửi, trạng thái)<br>4. Chờ tới mốc gửi, mở LINE app của bạn bè test và xác nhận tin nhận được<br>5. Đọc log job và trạng thái lịch sau khi gửi | Ghi giá trị ngày = hôm nay | Giá trị `TC40492_REG_Date` được ghi đúng cho đúng bạn bè.<br>Lịch gửi của trường được **sinh mới hoặc cập nhật** đúng mốc tính từ giá trị vừa ghi.<br>Tới mốc gửi, job chạy và bạn bè **nhận đúng 1 tin trên LINE app**; lịch chuyển sang trạng thái đã gửi.<br>Không phát sinh lịch trùng và không có lỗi trong log job. |  | Regression ma trận MAP-FI-08 — lối ghi «Lesson booking» × 年月日 (ngày tháng) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-08` · liên quan `JOB-001` — lịch gửi qua job nền; nhánh code này **KHÔNG** nằm trong điều kiện đã sửa (`if type == point hoặc type == select`) nên là regression thuần · ⚠️ Xác nhận mục form nhập chọn được 紐づけ友だち情報 kiểu 年月日 (kiểu ポイント đã xác nhận là KHÔNG map được → đánh N/A) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + trạng thái lịch gửi · regression |
| TC-FRIEND001-25 | UI | FRIEND-001 | Salon booking — form nhập thông tin khi booking | Boundary | manual | Tất cả | Salon booking ghi trường 選択肢 — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Lịch サロン・面談予約 test còn khung giờ đặt được<br>- Form nhập của salon có mục ánh xạ tới trường TC40492_REG_Select<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Bằng tài khoản LINE test, mở trang đặt lịch salon<br>2. Điền mục form có ánh xạ tới TC40492_REG_Select với giá trị mới<br>3. Hoàn tất đặt lịch và chờ xử lý xong<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Lần 1: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-08 — lối ghi «Salon booking» × 選択肢 (lựa chọn) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-08` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-26 | UI | FRIEND-001 | Salon booking — form nhập thông tin khi booking | Normal | manual | Tất cả | Salon booking ghi trường 選択肢 — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Lịch サロン・面談予約 test còn khung giờ đặt được<br>- Form nhập của salon có mục ánh xạ tới trường TC40492_REG_Select<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Bằng tài khoản LINE test, mở trang đặt lịch salon<br>2. Điền mục form có ánh xạ tới TC40492_REG_Select với giá trị mới<br>3. Hoàn tất đặt lịch và chờ xử lý xong<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Cả hai lần: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-08 — lối ghi «Salon booking» × 選択肢 (lựa chọn) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-08` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-27 | UI | FRIEND-001 | Salon booking — form nhập thông tin khi booking | Normal | manual | Tất cả | Salon booking ghi trường 年月日 — lịch gửi sinh đúng và job gửi đúng mốc | - Lịch サロン・面談予約 test còn khung giờ đặt được<br>- Form nhập của salon có mục ánh xạ tới trường TC40492_REG_Date<br>- Trường `TC40492_REG_Date` kiểu 年月日 với cấu hình lịch: 登録 =「月日」, `từ 0 ngày`, `後`, giờ gửi trong ngày còn tới được<br>- Action của lịch: gửi 1 tin text phân biệt được<br>- ⚠️ Kiểu 年月日 **KHÔNG có** 稼働設定 一度のみ/何度でも — trục tương ứng là 登録 「月日」(lặp hàng năm) / 「年月日」(1 lần)<br>- Bạn bè test chưa có giá trị cho trường này | 1. Bằng tài khoản LINE test, mở trang đặt lịch salon<br>2. Điền mục form có ánh xạ tới TC40492_REG_Date với giá trị mới<br>3. Hoàn tất đặt lịch và chờ xử lý xong<br>3. Kiểm tra giá trị đã lưu và lịch gửi vừa sinh (mốc gửi, trạng thái)<br>4. Chờ tới mốc gửi, mở LINE app của bạn bè test và xác nhận tin nhận được<br>5. Đọc log job và trạng thái lịch sau khi gửi | Ghi giá trị ngày = hôm nay | Giá trị `TC40492_REG_Date` được ghi đúng cho đúng bạn bè.<br>Lịch gửi của trường được **sinh mới hoặc cập nhật** đúng mốc tính từ giá trị vừa ghi.<br>Tới mốc gửi, job chạy và bạn bè **nhận đúng 1 tin trên LINE app**; lịch chuyển sang trạng thái đã gửi.<br>Không phát sinh lịch trùng và không có lỗi trong log job. |  | Regression ma trận MAP-FI-08 — lối ghi «Salon booking» × 年月日 (ngày tháng) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-08` · liên quan `JOB-001` — lịch gửi qua job nền; nhánh code này **KHÔNG** nằm trong điều kiện đã sửa (`if type == point hoặc type == select`) nên là regression thuần · ⚠️ Xác nhận mục form nhập chọn được 紐づけ友だち情報 kiểu 年月日 (kiểu ポイント đã xác nhận là KHÔNG map được → đánh N/A) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + trạng thái lịch gửi · regression |
| TC-FRIEND001-28 | UI | FRIEND-001 | Item (商品販売) — form nhập thông tin khi mua item | Boundary | manual | Tất cả | Item (商品販売) ghi trường 選択肢 — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Item test đang bán, còn mua được<br>- Form nhập của item có mục ánh xạ tới trường TC40492_REG_Select<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Bằng tài khoản LINE test, mở trang mua item<br>2. Điền mục form có ánh xạ tới TC40492_REG_Select với giá trị mới<br>3. Hoàn tất mua item (thanh toán thành công) và chờ xử lý xong<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Lần 1: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-08 — lối ghi «Item (商品販売)» × 選択肢 (lựa chọn) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-08` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-29 | UI | FRIEND-001 | Item (商品販売) — form nhập thông tin khi mua item | Normal | manual | Tất cả | Item (商品販売) ghi trường 選択肢 — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Item test đang bán, còn mua được<br>- Form nhập của item có mục ánh xạ tới trường TC40492_REG_Select<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Bằng tài khoản LINE test, mở trang mua item<br>2. Điền mục form có ánh xạ tới TC40492_REG_Select với giá trị mới<br>3. Hoàn tất mua item (thanh toán thành công) và chờ xử lý xong<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Cả hai lần: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-08 — lối ghi «Item (商品販売)» × 選択肢 (lựa chọn) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-08` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-30 | UI | FRIEND-001 | Item (商品販売) — form nhập thông tin khi mua item | Normal | manual | Tất cả | Item (商品販売) ghi trường 年月日 — lịch gửi sinh đúng và job gửi đúng mốc | - Item test đang bán, còn mua được<br>- Form nhập của item có mục ánh xạ tới trường TC40492_REG_Date<br>- Trường `TC40492_REG_Date` kiểu 年月日 với cấu hình lịch: 登録 =「月日」, `từ 0 ngày`, `後`, giờ gửi trong ngày còn tới được<br>- Action của lịch: gửi 1 tin text phân biệt được<br>- ⚠️ Kiểu 年月日 **KHÔNG có** 稼働設定 一度のみ/何度でも — trục tương ứng là 登録 「月日」(lặp hàng năm) / 「年月日」(1 lần)<br>- Bạn bè test chưa có giá trị cho trường này | 1. Bằng tài khoản LINE test, mở trang mua item<br>2. Điền mục form có ánh xạ tới TC40492_REG_Date với giá trị mới<br>3. Hoàn tất mua item (thanh toán thành công) và chờ xử lý xong<br>3. Kiểm tra giá trị đã lưu và lịch gửi vừa sinh (mốc gửi, trạng thái)<br>4. Chờ tới mốc gửi, mở LINE app của bạn bè test và xác nhận tin nhận được<br>5. Đọc log job và trạng thái lịch sau khi gửi | Ghi giá trị ngày = hôm nay | Giá trị `TC40492_REG_Date` được ghi đúng cho đúng bạn bè.<br>Lịch gửi của trường được **sinh mới hoặc cập nhật** đúng mốc tính từ giá trị vừa ghi.<br>Tới mốc gửi, job chạy và bạn bè **nhận đúng 1 tin trên LINE app**; lịch chuyển sang trạng thái đã gửi.<br>Không phát sinh lịch trùng và không có lỗi trong log job. |  | Regression ma trận MAP-FI-08 — lối ghi «Item (商品販売)» × 年月日 (ngày tháng) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-08` · liên quan `JOB-001` — lịch gửi qua job nền; nhánh code này **KHÔNG** nằm trong điều kiện đã sửa (`if type == point hoặc type == select`) nên là regression thuần · ⚠️ Xác nhận mục form nhập chọn được 紐づけ友だち情報 kiểu 年月日 (kiểu ポイント đã xác nhận là KHÔNG map được → đánh N/A) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + trạng thái lịch gửi · regression |
| TC-FRIEND001-31 | UI | FRIEND-001 | Form — form nhập thông tin (user trả lời form) | Boundary | manual | Tất cả | Form ghi trường 選択肢 — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Form test có mục nhập ánh xạ tới trường TC40492_REG_Select<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Bằng tài khoản LINE test, mở đường dẫn form test<br>2. Trả lời mục có ánh xạ tới TC40492_REG_Select với giá trị mới<br>3. Bấm gửi form và chờ màn hoàn tất hiển thị<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Lần 1: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-08 — lối ghi «Form» × 選択肢 (lựa chọn) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-08` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-32 | UI | FRIEND-001 | Form — form nhập thông tin (user trả lời form) | Normal | manual | Tất cả | Form ghi trường 年月日 — lịch gửi sinh đúng và job gửi đúng mốc | - Form test có mục nhập ánh xạ tới trường TC40492_REG_Date<br>- Trường `TC40492_REG_Date` kiểu 年月日 với cấu hình lịch: 登録 =「月日」, `từ 0 ngày`, `後`, giờ gửi trong ngày còn tới được<br>- Action của lịch: gửi 1 tin text phân biệt được<br>- ⚠️ Kiểu 年月日 **KHÔNG có** 稼働設定 一度のみ/何度でも — trục tương ứng là 登録 「月日」(lặp hàng năm) / 「年月日」(1 lần)<br>- Bạn bè test chưa có giá trị cho trường này | 1. Bằng tài khoản LINE test, mở đường dẫn form test<br>2. Trả lời mục có ánh xạ tới TC40492_REG_Date với giá trị mới<br>3. Bấm gửi form và chờ màn hoàn tất hiển thị<br>3. Kiểm tra giá trị đã lưu và lịch gửi vừa sinh (mốc gửi, trạng thái)<br>4. Chờ tới mốc gửi, mở LINE app của bạn bè test và xác nhận tin nhận được<br>5. Đọc log job và trạng thái lịch sau khi gửi | Ghi giá trị ngày = hôm nay | Giá trị `TC40492_REG_Date` được ghi đúng cho đúng bạn bè.<br>Lịch gửi của trường được **sinh mới hoặc cập nhật** đúng mốc tính từ giá trị vừa ghi.<br>Tới mốc gửi, job chạy và bạn bè **nhận đúng 1 tin trên LINE app**; lịch chuyển sang trạng thái đã gửi.<br>Không phát sinh lịch trùng và không có lỗi trong log job. |  | Regression ma trận MAP-FI-08 — lối ghi «Form» × 年月日 (ngày tháng) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-08` · liên quan `JOB-001` — lịch gửi qua job nền; nhánh code này **KHÔNG** nằm trong điều kiện đã sửa (`if type == point hoặc type == select`) nên là regression thuần · ⚠️ Xác nhận mục form nhập chọn được 紐づけ友だち情報 kiểu 年月日 (kiểu ポイント đã xác nhận là KHÔNG map được → đánh N/A) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + trạng thái lịch gửi · regression |
| TC-FRIEND001-33 | UI | FRIEND-001 | Event booking — multi action lúc booking / change / cancel | Boundary | manual | Tất cả | Event booking ghi trường 選択肢 — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Sự kiện test đã cấu hình multi action ở CẢ ba mốc: lúc đặt lịch, lúc đổi lịch, lúc huỷ lịch<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Select (giá trị khác nhau ở 3 mốc để phân biệt)<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Đặt lịch → quan sát giá trị TC40492_REG_Select và hiệu ứng action<br>3. Đổi lịch đặt đó → quan sát lại<br>4. Huỷ lịch → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Lần 1: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-06 — lối ghi «Event booking» × 選択肢 (lựa chọn) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-34 | UI | FRIEND-001 | Event booking — multi action lúc booking / change / cancel | Normal | manual | Tất cả | Event booking ghi trường 選択肢 — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Sự kiện test đã cấu hình multi action ở CẢ ba mốc: lúc đặt lịch, lúc đổi lịch, lúc huỷ lịch<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Select (giá trị khác nhau ở 3 mốc để phân biệt)<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Đặt lịch → quan sát giá trị TC40492_REG_Select và hiệu ứng action<br>3. Đổi lịch đặt đó → quan sát lại<br>4. Huỷ lịch → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Cả hai lần: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-06 — lối ghi «Event booking» × 選択肢 (lựa chọn) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-35 | UI | FRIEND-001 | Event booking — multi action lúc booking / change / cancel | Boundary | manual | Tất cả | Event booking ghi trường ポイント — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Sự kiện test đã cấu hình multi action ở CẢ ba mốc: lúc đặt lịch, lúc đổi lịch, lúc huỷ lịch<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Point (giá trị khác nhau ở 3 mốc để phân biệt)<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Đặt lịch → quan sát giá trị TC40492_REG_Point và hiệu ứng action<br>3. Đổi lịch đặt đó → quan sát lại<br>4. Huỷ lịch → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Lần 1: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-06 — lối ghi «Event booking» × ポイント (điểm) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-36 | UI | FRIEND-001 | Event booking — multi action lúc booking / change / cancel | Normal | manual | Tất cả | Event booking ghi trường ポイント — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Sự kiện test đã cấu hình multi action ở CẢ ba mốc: lúc đặt lịch, lúc đổi lịch, lúc huỷ lịch<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Point (giá trị khác nhau ở 3 mốc để phân biệt)<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Đặt lịch → quan sát giá trị TC40492_REG_Point và hiệu ứng action<br>3. Đổi lịch đặt đó → quan sát lại<br>4. Huỷ lịch → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Cả hai lần: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-06 — lối ghi «Event booking» × ポイント (điểm) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-37 | UI | FRIEND-001 | Event booking — multi action lúc booking / change / cancel | Normal | manual | Tất cả | Event booking ghi trường 年月日 — lịch gửi sinh đúng và job gửi đúng mốc | - Sự kiện test đã cấu hình multi action ở CẢ ba mốc: lúc đặt lịch, lúc đổi lịch, lúc huỷ lịch<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Date (giá trị khác nhau ở 3 mốc để phân biệt)<br>- Trường `TC40492_REG_Date` kiểu 年月日 với cấu hình lịch: 登録 =「月日」, `từ 0 ngày`, `後`, giờ gửi trong ngày còn tới được<br>- Action của lịch: gửi 1 tin text phân biệt được<br>- ⚠️ Kiểu 年月日 **KHÔNG có** 稼働設定 一度のみ/何度でも — trục tương ứng là 登録 「月日」(lặp hàng năm) / 「年月日」(1 lần)<br>- Bạn bè test chưa có giá trị cho trường này | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Đặt lịch → quan sát giá trị TC40492_REG_Date và hiệu ứng action<br>3. Đổi lịch đặt đó → quan sát lại<br>4. Huỷ lịch → quan sát lại<br>3. Kiểm tra giá trị đã lưu và lịch gửi vừa sinh (mốc gửi, trạng thái)<br>4. Chờ tới mốc gửi, mở LINE app của bạn bè test và xác nhận tin nhận được<br>5. Đọc log job và trạng thái lịch sau khi gửi | Ghi giá trị ngày = hôm nay | Giá trị `TC40492_REG_Date` được ghi đúng cho đúng bạn bè.<br>Lịch gửi của trường được **sinh mới hoặc cập nhật** đúng mốc tính từ giá trị vừa ghi.<br>Tới mốc gửi, job chạy và bạn bè **nhận đúng 1 tin trên LINE app**; lịch chuyển sang trạng thái đã gửi.<br>Không phát sinh lịch trùng và không có lỗi trong log job. |  | Regression ma trận MAP-FI-06 — lối ghi «Event booking» × 年月日 (ngày tháng) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · liên quan `JOB-001` — lịch gửi qua job nền; nhánh code này **KHÔNG** nằm trong điều kiện đã sửa (`if type == point hoặc type == select`) nên là regression thuần · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + trạng thái lịch gửi · regression |
| TC-FRIEND001-38 | UI | FRIEND-001 | Lesson booking — multi action lúc booking / cancel | Boundary | manual | Tất cả | Lesson booking ghi trường 選択肢 — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Lịch lesson test đã cấu hình multi action ở cả hai mốc: lúc đặt lịch và lúc huỷ lịch<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Select (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Đặt lịch lesson → quan sát giá trị TC40492_REG_Select và hiệu ứng action<br>3. Huỷ lịch đó → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Lần 1: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-06 — lối ghi «Lesson booking» × 選択肢 (lựa chọn) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-39 | UI | FRIEND-001 | Lesson booking — multi action lúc booking / cancel | Normal | manual | Tất cả | Lesson booking ghi trường 選択肢 — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Lịch lesson test đã cấu hình multi action ở cả hai mốc: lúc đặt lịch và lúc huỷ lịch<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Select (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Đặt lịch lesson → quan sát giá trị TC40492_REG_Select và hiệu ứng action<br>3. Huỷ lịch đó → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Cả hai lần: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-06 — lối ghi «Lesson booking» × 選択肢 (lựa chọn) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-40 | UI | FRIEND-001 | Lesson booking — multi action lúc booking / cancel | Boundary | manual | Tất cả | Lesson booking ghi trường ポイント — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Lịch lesson test đã cấu hình multi action ở cả hai mốc: lúc đặt lịch và lúc huỷ lịch<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Point (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Đặt lịch lesson → quan sát giá trị TC40492_REG_Point và hiệu ứng action<br>3. Huỷ lịch đó → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Lần 1: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-06 — lối ghi «Lesson booking» × ポイント (điểm) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-41 | UI | FRIEND-001 | Lesson booking — multi action lúc booking / cancel | Normal | manual | Tất cả | Lesson booking ghi trường ポイント — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Lịch lesson test đã cấu hình multi action ở cả hai mốc: lúc đặt lịch và lúc huỷ lịch<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Point (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Đặt lịch lesson → quan sát giá trị TC40492_REG_Point và hiệu ứng action<br>3. Huỷ lịch đó → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Cả hai lần: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-06 — lối ghi «Lesson booking» × ポイント (điểm) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-42 | UI | FRIEND-001 | Lesson booking — multi action lúc booking / cancel | Normal | manual | Tất cả | Lesson booking ghi trường 年月日 — lịch gửi sinh đúng và job gửi đúng mốc | - Lịch lesson test đã cấu hình multi action ở cả hai mốc: lúc đặt lịch và lúc huỷ lịch<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Date (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Date` kiểu 年月日 với cấu hình lịch: 登録 =「月日」, `từ 0 ngày`, `後`, giờ gửi trong ngày còn tới được<br>- Action của lịch: gửi 1 tin text phân biệt được<br>- ⚠️ Kiểu 年月日 **KHÔNG có** 稼働設定 一度のみ/何度でも — trục tương ứng là 登録 「月日」(lặp hàng năm) / 「年月日」(1 lần)<br>- Bạn bè test chưa có giá trị cho trường này | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Đặt lịch lesson → quan sát giá trị TC40492_REG_Date và hiệu ứng action<br>3. Huỷ lịch đó → quan sát lại<br>3. Kiểm tra giá trị đã lưu và lịch gửi vừa sinh (mốc gửi, trạng thái)<br>4. Chờ tới mốc gửi, mở LINE app của bạn bè test và xác nhận tin nhận được<br>5. Đọc log job và trạng thái lịch sau khi gửi | Ghi giá trị ngày = hôm nay | Giá trị `TC40492_REG_Date` được ghi đúng cho đúng bạn bè.<br>Lịch gửi của trường được **sinh mới hoặc cập nhật** đúng mốc tính từ giá trị vừa ghi.<br>Tới mốc gửi, job chạy và bạn bè **nhận đúng 1 tin trên LINE app**; lịch chuyển sang trạng thái đã gửi.<br>Không phát sinh lịch trùng và không có lỗi trong log job. |  | Regression ma trận MAP-FI-06 — lối ghi «Lesson booking» × 年月日 (ngày tháng) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · liên quan `JOB-001` — lịch gửi qua job nền; nhánh code này **KHÔNG** nằm trong điều kiện đã sửa (`if type == point hoặc type == select`) nên là regression thuần · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + trạng thái lịch gửi · regression |
| TC-FRIEND001-43 | UI | FRIEND-001 | Salon booking — multi action lúc booking / cancel | Boundary | manual | Tất cả | Salon booking ghi trường 選択肢 — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Lịch salon test đã cấu hình multi action ở cả hai mốc: lúc đặt lịch và lúc huỷ lịch<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Select (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Đặt lịch salon → quan sát giá trị TC40492_REG_Select và hiệu ứng action<br>3. Huỷ lịch đó → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Lần 1: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-06 — lối ghi «Salon booking» × 選択肢 (lựa chọn) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-44 | UI | FRIEND-001 | Salon booking — multi action lúc booking / cancel | Normal | manual | Tất cả | Salon booking ghi trường 選択肢 — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Lịch salon test đã cấu hình multi action ở cả hai mốc: lúc đặt lịch và lúc huỷ lịch<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Select (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Đặt lịch salon → quan sát giá trị TC40492_REG_Select và hiệu ứng action<br>3. Huỷ lịch đó → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Cả hai lần: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-06 — lối ghi «Salon booking» × 選択肢 (lựa chọn) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-45 | UI | FRIEND-001 | Salon booking — multi action lúc booking / cancel | Boundary | manual | Tất cả | Salon booking ghi trường ポイント — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Lịch salon test đã cấu hình multi action ở cả hai mốc: lúc đặt lịch và lúc huỷ lịch<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Point (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Đặt lịch salon → quan sát giá trị TC40492_REG_Point và hiệu ứng action<br>3. Huỷ lịch đó → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Lần 1: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-06 — lối ghi «Salon booking» × ポイント (điểm) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-46 | UI | FRIEND-001 | Salon booking — multi action lúc booking / cancel | Normal | manual | Tất cả | Salon booking ghi trường ポイント — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Lịch salon test đã cấu hình multi action ở cả hai mốc: lúc đặt lịch và lúc huỷ lịch<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Point (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Đặt lịch salon → quan sát giá trị TC40492_REG_Point và hiệu ứng action<br>3. Huỷ lịch đó → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Cả hai lần: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-06 — lối ghi «Salon booking» × ポイント (điểm) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-47 | UI | FRIEND-001 | Salon booking — multi action lúc booking / cancel | Normal | manual | Tất cả | Salon booking ghi trường 年月日 — lịch gửi sinh đúng và job gửi đúng mốc | - Lịch salon test đã cấu hình multi action ở cả hai mốc: lúc đặt lịch và lúc huỷ lịch<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Date (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Date` kiểu 年月日 với cấu hình lịch: 登録 =「月日」, `từ 0 ngày`, `後`, giờ gửi trong ngày còn tới được<br>- Action của lịch: gửi 1 tin text phân biệt được<br>- ⚠️ Kiểu 年月日 **KHÔNG có** 稼働設定 一度のみ/何度でも — trục tương ứng là 登録 「月日」(lặp hàng năm) / 「年月日」(1 lần)<br>- Bạn bè test chưa có giá trị cho trường này | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Đặt lịch salon → quan sát giá trị TC40492_REG_Date và hiệu ứng action<br>3. Huỷ lịch đó → quan sát lại<br>3. Kiểm tra giá trị đã lưu và lịch gửi vừa sinh (mốc gửi, trạng thái)<br>4. Chờ tới mốc gửi, mở LINE app của bạn bè test và xác nhận tin nhận được<br>5. Đọc log job và trạng thái lịch sau khi gửi | Ghi giá trị ngày = hôm nay | Giá trị `TC40492_REG_Date` được ghi đúng cho đúng bạn bè.<br>Lịch gửi của trường được **sinh mới hoặc cập nhật** đúng mốc tính từ giá trị vừa ghi.<br>Tới mốc gửi, job chạy và bạn bè **nhận đúng 1 tin trên LINE app**; lịch chuyển sang trạng thái đã gửi.<br>Không phát sinh lịch trùng và không có lỗi trong log job. |  | Regression ma trận MAP-FI-06 — lối ghi «Salon booking» × 年月日 (ngày tháng) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · liên quan `JOB-001` — lịch gửi qua job nền; nhánh code này **KHÔNG** nằm trong điều kiện đã sửa (`if type == point hoặc type == select`) nên là regression thuần · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + trạng thái lịch gửi · regression |
| TC-FRIEND001-48 | UI | FRIEND-001 | Item — multi action lúc open item / mua item thành công | Boundary | manual | Tất cả | Item ghi trường 選択肢 — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Item test đã cấu hình multi action ở cả hai mốc: lúc mở trang item và lúc mua thành công<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Select (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Bằng tài khoản LINE test mở trang item → quan sát giá trị TC40492_REG_Select và hiệu ứng action<br>3. Hoàn tất mua item → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Lần 1: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-06 — lối ghi «Item» × 選択肢 (lựa chọn) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-49 | UI | FRIEND-001 | Item — multi action lúc open item / mua item thành công | Normal | manual | Tất cả | Item ghi trường 選択肢 — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Item test đã cấu hình multi action ở cả hai mốc: lúc mở trang item và lúc mua thành công<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Select (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Bằng tài khoản LINE test mở trang item → quan sát giá trị TC40492_REG_Select và hiệu ứng action<br>3. Hoàn tất mua item → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Cả hai lần: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-06 — lối ghi «Item» × 選択肢 (lựa chọn) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-50 | UI | FRIEND-001 | Item — multi action lúc open item / mua item thành công | Boundary | manual | Tất cả | Item ghi trường ポイント — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Item test đã cấu hình multi action ở cả hai mốc: lúc mở trang item và lúc mua thành công<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Point (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Bằng tài khoản LINE test mở trang item → quan sát giá trị TC40492_REG_Point và hiệu ứng action<br>3. Hoàn tất mua item → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Lần 1: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-06 — lối ghi «Item» × ポイント (điểm) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-51 | UI | FRIEND-001 | Item — multi action lúc open item / mua item thành công | Normal | manual | Tất cả | Item ghi trường ポイント — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Item test đã cấu hình multi action ở cả hai mốc: lúc mở trang item và lúc mua thành công<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Point (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Bằng tài khoản LINE test mở trang item → quan sát giá trị TC40492_REG_Point và hiệu ứng action<br>3. Hoàn tất mua item → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Cả hai lần: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-06 — lối ghi «Item» × ポイント (điểm) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-52 | UI | FRIEND-001 | Item — multi action lúc open item / mua item thành công | Normal | manual | Tất cả | Item ghi trường 年月日 — lịch gửi sinh đúng và job gửi đúng mốc | - Item test đã cấu hình multi action ở cả hai mốc: lúc mở trang item và lúc mua thành công<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Date (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Date` kiểu 年月日 với cấu hình lịch: 登録 =「月日」, `từ 0 ngày`, `後`, giờ gửi trong ngày còn tới được<br>- Action của lịch: gửi 1 tin text phân biệt được<br>- ⚠️ Kiểu 年月日 **KHÔNG có** 稼働設定 一度のみ/何度でも — trục tương ứng là 登録 「月日」(lặp hàng năm) / 「年月日」(1 lần)<br>- Bạn bè test chưa có giá trị cho trường này | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Bằng tài khoản LINE test mở trang item → quan sát giá trị TC40492_REG_Date và hiệu ứng action<br>3. Hoàn tất mua item → quan sát lại<br>3. Kiểm tra giá trị đã lưu và lịch gửi vừa sinh (mốc gửi, trạng thái)<br>4. Chờ tới mốc gửi, mở LINE app của bạn bè test và xác nhận tin nhận được<br>5. Đọc log job và trạng thái lịch sau khi gửi | Ghi giá trị ngày = hôm nay | Giá trị `TC40492_REG_Date` được ghi đúng cho đúng bạn bè.<br>Lịch gửi của trường được **sinh mới hoặc cập nhật** đúng mốc tính từ giá trị vừa ghi.<br>Tới mốc gửi, job chạy và bạn bè **nhận đúng 1 tin trên LINE app**; lịch chuyển sang trạng thái đã gửi.<br>Không phát sinh lịch trùng và không có lỗi trong log job. |  | Regression ma trận MAP-FI-06 — lối ghi «Item» × 年月日 (ngày tháng) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · liên quan `JOB-001` — lịch gửi qua job nền; nhánh code này **KHÔNG** nằm trong điều kiện đã sửa (`if type == point hoặc type == select`) nên là regression thuần · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + trạng thái lịch gửi · regression |
| TC-FRIEND001-53 | UI | FRIEND-001 | Form — multi action lúc mở form / lúc trả lời form | Boundary | manual | Tất cả | Form ghi trường 選択肢 — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Form test đã cấu hình multi action ở cả hai mốc: lúc mở form và lúc trả lời form<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Select (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Bằng tài khoản LINE test mở form → quan sát giá trị TC40492_REG_Select và hiệu ứng action<br>3. Trả lời và gửi form → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Lần 1: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-06 — lối ghi «Form» × 選択肢 (lựa chọn) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-54 | UI | FRIEND-001 | Form — multi action lúc mở form / lúc trả lời form | Normal | manual | Tất cả | Form ghi trường 選択肢 — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Form test đã cấu hình multi action ở cả hai mốc: lúc mở form và lúc trả lời form<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Select (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Select` kiểu 選択肢, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Hai lựa chọn `ChuaDenKham` / `DaDenKham`, mỗi lựa chọn gắn một action phân biệt được (đổi 対応ステータス sang `St1` / `St2` + khởi động kịch bản bước riêng)<br>- Bạn bè test đang có `ChuaDenKham`, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Bằng tài khoản LINE test mở form → quan sát giá trị TC40492_REG_Select và hiệu ứng action<br>3. Trả lời và gửi form → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Đổi `ChuaDenKham` → `DaDenKham`, rồi đổi ngược lại `DaDenKham` → `ChuaDenKham` (lần 2) | Cả hai lần: giá trị `TC40492_REG_Select` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-06 — lối ghi «Form» × 選択肢 (lựa chọn) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-55 | UI | FRIEND-001 | Form — multi action lúc mở form / lúc trả lời form | Boundary | manual | Tất cả | Form ghi trường ポイント — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Form test đã cấu hình multi action ở cả hai mốc: lúc mở form và lúc trả lời form<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Point (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Bằng tài khoản LINE test mở form → quan sát giá trị TC40492_REG_Point và hiệu ứng action<br>3. Trả lời và gửi form → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Lần 1: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-06 — lối ghi «Form» × ポイント (điểm) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-56 | UI | FRIEND-001 | Form — multi action lúc mở form / lúc trả lời form | Normal | manual | Tất cả | Form ghi trường ポイント — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Form test đã cấu hình multi action ở cả hai mốc: lúc mở form và lúc trả lời form<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Point (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Bằng tài khoản LINE test mở form → quan sát giá trị TC40492_REG_Point và hiệu ứng action<br>3. Trả lời và gửi form → quan sát lại<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Cả hai lần: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-06 — lối ghi «Form» × ポイント (điểm) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-57 | UI | FRIEND-001 | Form — multi action lúc mở form / lúc trả lời form | Normal | manual | Tất cả | Form ghi trường 年月日 — lịch gửi sinh đúng và job gửi đúng mốc | - Form test đã cấu hình multi action ở cả hai mốc: lúc mở form và lúc trả lời form<br>- Mỗi multi action chứa action 友だち情報 ghi giá trị vào trường TC40492_REG_Date (giá trị khác nhau để phân biệt)<br>- Trường `TC40492_REG_Date` kiểu 年月日 với cấu hình lịch: 登録 =「月日」, `từ 0 ngày`, `後`, giờ gửi trong ngày còn tới được<br>- Action của lịch: gửi 1 tin text phân biệt được<br>- ⚠️ Kiểu 年月日 **KHÔNG có** 稼働設定 一度のみ/何度でも — trục tương ứng là 登録 「月日」(lặp hàng năm) / 「年月日」(1 lần)<br>- Bạn bè test chưa có giá trị cho trường này | 1. Ghi lại trạng thái ban đầu của bạn bè test<br>2. Bằng tài khoản LINE test mở form → quan sát giá trị TC40492_REG_Date và hiệu ứng action<br>3. Trả lời và gửi form → quan sát lại<br>3. Kiểm tra giá trị đã lưu và lịch gửi vừa sinh (mốc gửi, trạng thái)<br>4. Chờ tới mốc gửi, mở LINE app của bạn bè test và xác nhận tin nhận được<br>5. Đọc log job và trạng thái lịch sau khi gửi | Ghi giá trị ngày = hôm nay | Giá trị `TC40492_REG_Date` được ghi đúng cho đúng bạn bè.<br>Lịch gửi của trường được **sinh mới hoặc cập nhật** đúng mốc tính từ giá trị vừa ghi.<br>Tới mốc gửi, job chạy và bạn bè **nhận đúng 1 tin trên LINE app**; lịch chuyển sang trạng thái đã gửi.<br>Không phát sinh lịch trùng và không có lỗi trong log job. |  | Regression ma trận MAP-FI-06 — lối ghi «Form» × 年月日 (ngày tháng) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-06` · liên quan `JOB-001` — lịch gửi qua job nền; nhánh code này **KHÔNG** nằm trong điều kiện đã sửa (`if type == point hoặc type == select`) nên là regression thuần · Multi action đi qua `sendAction` — Dev khai là **BẢN CHUẨN không sửa**; TC này chứng minh bản vá không làm lệch nhóm đó (RULE-12 vùng ảnh hưởng) · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + trạng thái lịch gửi · regression |
| TC-FRIEND001-58 | UI | FRIEND-001 | App mobile — màn My page | Boundary | manual | Tất cả | App mobile ghi trường ポイント — chế độ 一度のみ: chạy action lần đầu rồi khoá, lần sau không chạy lại | - Có tài khoản đăng nhập được app mobile cho đúng bot<br>- Trường TC40492_REG_Point đang hiển thị trên màn My page của app<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「一度のみ (chỉ chạy 1 lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Trên app mobile mở màn My page của bạn bè test<br>2. Sửa trường TC40492_REG_Point sang giá trị mới và lưu<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Lần 1: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY** — 対応ステータス đổi sang `St2`, kịch bản bước được khởi động, cờ đã-chạy được ghi bằng `1` trên bản ghi giá trị của **đúng bạn bè và đúng trường**.<br>Lần 2: giá trị vẫn lưu và hiển thị đúng, nhưng **KHÔNG chạy action** — 対応ステータス giữ nguyên `St2`, không kịch bản bước nào khởi động thêm, số tin bạn bè nhận không tăng. |  | Regression ma trận MAP-FI-09 — lối ghi «App mobile» × ポイント (điểm) · chế độ 一度のみ (chỉ chạy 1 lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-09` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-59 | UI | FRIEND-001 | App mobile — màn My page | Normal | manual | Tất cả | App mobile ghi trường ポイント — chế độ 何度でも: mỗi lần đổi giá trị đều chạy lại action | - Có tài khoản đăng nhập được app mobile cho đúng bot<br>- Trường TC40492_REG_Point đang hiển thị trên màn My page của app<br>- Trường `TC40492_REG_Point` kiểu ポイント, 稼働設定 = 「何度でも稼働 (chạy nhiều lần)」<br>- Cấu hình ngưỡng: đạt `10` điểm → action đổi 対応ステータス sang `St2` + khởi động kịch bản bước<br>- Bạn bè test đang có `5` điểm, 対応ステータス = `St0` | 1. Trên app mobile mở màn My page của bạn bè test<br>2. Sửa trường TC40492_REG_Point sang giá trị mới và lưu<br>3. Tải lại màn quản trị và đọc giá trị đã lưu, 対応ステータス, trạng thái kịch bản bước<br>4. Thực hiện **lần thứ hai** theo cột Dữ liệu nhập<br>5. Tải lại và đối chiếu lại 対応ステータス, kịch bản bước, số tin bạn bè nhận trên LINE app | Ghi điểm `5` → `10` (đạt ngưỡng), rồi hạ `10` → `5` và ghi lại `10` (lần 2 đạt ngưỡng) | Cả hai lần: giá trị `TC40492_REG_Point` lưu đúng và action **CHẠY LẠI mỗi lần** — 対応ステータス đổi theo action tương ứng (`St2` rồi `St1`), kịch bản bước được khởi động lại ở mỗi lần.<br>**Không lần nào bị bỏ qua vì cờ đã-chạy**, kể cả khi cờ đang khác rỗng từ trước.<br>Chế độ chạy nhiều lần không dùng cờ để chặn action. |  | Regression ma trận MAP-FI-09 — lối ghi «App mobile» × ポイント (điểm) · chế độ 何度でも稼働 (chạy nhiều lần) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-09` · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + query cờ đã-chạy · regression |
| TC-FRIEND001-60 | UI | FRIEND-001 | App mobile — màn My page | Normal | manual | Tất cả | App mobile ghi trường 年月日 — lịch gửi sinh đúng và job gửi đúng mốc | - Có tài khoản đăng nhập được app mobile cho đúng bot<br>- Trường TC40492_REG_Date đang hiển thị trên màn My page của app<br>- Trường `TC40492_REG_Date` kiểu 年月日 với cấu hình lịch: 登録 =「月日」, `từ 0 ngày`, `後`, giờ gửi trong ngày còn tới được<br>- Action của lịch: gửi 1 tin text phân biệt được<br>- ⚠️ Kiểu 年月日 **KHÔNG có** 稼働設定 一度のみ/何度でも — trục tương ứng là 登録 「月日」(lặp hàng năm) / 「年月日」(1 lần)<br>- Bạn bè test chưa có giá trị cho trường này | 1. Trên app mobile mở màn My page của bạn bè test<br>2. Sửa trường TC40492_REG_Date sang giá trị mới và lưu<br>3. Kiểm tra giá trị đã lưu và lịch gửi vừa sinh (mốc gửi, trạng thái)<br>4. Chờ tới mốc gửi, mở LINE app của bạn bè test và xác nhận tin nhận được<br>5. Đọc log job và trạng thái lịch sau khi gửi | Ghi giá trị ngày = hôm nay | Giá trị `TC40492_REG_Date` được ghi đúng cho đúng bạn bè.<br>Lịch gửi của trường được **sinh mới hoặc cập nhật** đúng mốc tính từ giá trị vừa ghi.<br>Tới mốc gửi, job chạy và bạn bè **nhận đúng 1 tin trên LINE app**; lịch chuyển sang trạng thái đã gửi.<br>Không phát sinh lịch trùng và không có lỗi trong log job. |  | Regression ma trận MAP-FI-09 — lối ghi «App mobile» × 年月日 (ngày tháng) · cover impact `T2` (FA-015) · quan điểm `FRIEND-001` + Catalog `MAP-FI-02`/`MAP-FI-09` · liên quan `JOB-001` — lịch gửi qua job nền; nhánh code này **KHÔNG** nằm trong điều kiện đã sửa (`if type == point hoặc type == select`) nên là regression thuần · Đánh giá spec: Spec không ghi (đã hỏi Leader — `feature-spec.md` chỉ mô tả BR-04 ở mức trường, không định nghĩa theo lối ghi) · Evidence: screenshot màn quản trị trước/sau + screenshot LINE app đếm tin + trạng thái lịch gửi · regression |

> **Đã chống trùng** (BƯỚC 5b): đối chiếu từng TC với 27 TC Studio · 11 TC ở §5 · kho `fa015` (nhóm「Action gán friend info」`TC-FRI-227`→`TC-FRI-244`, nhóm「Job action ngày tháng」`TC-FRI-245`→`TC-FRI-266`, `TC-FRI-202/203` multi action điểm, `TC-FRI-221/222/243` import CSV) — không TC nào trùng **4 yếu tố**.
> Kho phủ **hành vi của lịch gửi 年月日 và của action điểm ở mức cấu hình**, còn ma trận này phủ **trục lối ghi** — chiều mà cả kho lẫn Studio đều bỏ trống.

> **10 ô đánh N/A** (form nhập của Event/Lesson/Salon/Item/Form × ポイント × 2 chế độ): Mục item của form nhập **không có** ánh xạ 紐づけ友だち情報 tới trường kiểu ポイント (Test Leader xác nhận 2026-09-05). mục form nhập có chọn 紐づけ友だち情報 kiểu ポイント hay không — `get_form_detail` không trả về ánh xạ này. **Không tự suy diễn**: hỏi BA/Dev, nếu UI không hỗ trợ thì đánh N/A + ghi lý do thay vì để TC treo.

---
## 6. Spec update needed

| # | Vấn đề | Hiện trạng spec | Đề nghị |
|---|---|---|---|
| 1 | **BR-04 không mô tả hành vi runtime của `action_mode = 2`** | [feature-spec.md](../../spec-features/admin/friend-information/feature-spec.md) BR-04 (dòng 463-465) chỉ ghi: *"`action_mode = 1`: action chỉ trigger khi `friend_information_value.action` = NULL"* và *"`action_mode = 2`: **khi lưu setting** → reset tất cả `friend_information_value.action = NULL` để action trigger lại"*. Tức spec mô tả mode 2 bằng cơ chế **reset một lần lúc lưu cấu hình** — hàm ý runtime **vẫn xét cờ**. Bản vá #40492 lại đặt luật runtime: **mode 2 KHÔNG bao giờ xét cờ** | Bổ sung vào BR-04 vế runtime: *"Khi ghi giá trị, mode 2 luôn chạy action không phụ thuộc cờ; cờ chỉ có ý nghĩa với mode 1"*. Làm rõ luôn: mode 2 **có ghi cờ không**? (`NEW-69`/`NEW-61` khẳng định "không ghi thêm", auto run `15903` xác nhận gián tiếp; nhưng note của `NEW-62` lại từ chối khẳng định điều này) |
| 2 | **Tiêu chí chặn của nhóm "KHÔNG sửa" khác tiêu chí của nhóm đã sửa** | Dev tự ghi ở mục ■ 6 VERIFY: *"Nhóm KHÔNG sửa dùng khuôn khác: chế độ chạy một lần thì chỉ chạy khi **chưa từng có bản ghi giá trị**, chế độ chạy nhiều lần luôn chạy. Tiêu chí 'chưa từng có giá trị' **khác** với cờ đã-chạy, **ghi nhận để BA xem lại sau, không đụng trong ticket này**"* | Hai nhóm code đang hiểu 「一度のみ」 theo **2 nghĩa khác nhau**: "chưa từng có giá trị" vs "chưa từng chạy action". Với bạn bè đã có giá trị nhưng chưa chạy action, hai nhóm cho kết quả ngược nhau. Yêu cầu BA chốt định nghĩa duy nhất rồi thống nhất code; hiện `NEW-60` chỉ smoke 1 điểm đại diện cho 15+ điểm của nhóm này |
| 3 | **Spec không định nghĩa danh sách lối ghi giá trị phải chạy action** | kho-tcs FA-015 **MT-03** (⏳ CHỜ QUYẾT ĐỊNH, mức CAO) đã nêu đúng vấn đề này cho ticket #38591: *"Cùng 1 hành vi nghiệp vụ đi qua ≥ 7 lối ghi khác nhau; fix của #38591 chỉ đụng 1 lối"*, và **RV-07**: *"Path mobile (`Api/FriendInformationController`) KHÔNG dùng service được fix (yokoten)"*. `feature-spec.md` §2 chỉ mô tả luồng xoá từ màn 情報一覧 | Ticket #40492 lặp lại **đúng bài học của #38591**. Chốt và ghi vào `feature-spec.md` danh sách lối ghi giá trị + lối nào phải chạy action: 友だち詳細 · right bar chat 1:1 (2 đường: từng trường / cả cụm) · app mobile v1/v2 · form · QR param/LIFF · CSV import · booking event (3 biến thể) · booking salon/lesson · multi action · scenario action |
| 4 | **Import CSV: có chạy friend info action không?** | kho-tcs `TC-FRI-243`: *"Import CSV cập nhật giá trị 年月日 → **KHÔNG kích hoạt action**"*. Nhưng **MT-16** ghi corpus **tự mâu thuẫn** ở chính tab này (r124 vs r279-r281), và `feature-spec.md` §7 ghi *"CSV import → scheduling"*. Import CSV **không nằm trong cả danh sách ĐÃ SỬA lẫn danh sách KHÔNG SỬA** của Dev | Xác nhận với Dev: đường import CSV có đi qua khối chạy action đã sửa không? Nếu có mà chưa sửa → là **điểm sót của lần quét ngang vòng 2**. Nếu đúng là CSV không chạy action → ghi rõ vào `feature-spec.md` để lần sau không phải hỏi lại |
| 5 | **Phân quyền của FA-015 chưa xác định** | kho-tcs **MT-12** (⏳ CHỜ QUYẾT ĐỊNH): *"không phát hiện Policy/Gate riêng trong controller"*, `feature-spec.md` §9 Gap #2 + §10 Open Question 2. Fix này chạm **2 endpoint API mobile** và **1 endpoint web đời cũ** | Ngoài phạm vi ticket nên đã đánh × ở §3.6, nhưng cần ghi nhận: nếu quyền chỉ kiểm ở tầng ẩn menu, các endpoint vừa sửa có thể gọi thẳng được. Đề nghị chạy `TC-FRI-332` (gõ thẳng URL) và `TC-FRI-333` (gọi thẳng API) của kho trong đợt regression kế tiếp |
| 6 | **Hành vi kịch bản bước đang chạy dở khi giá trị đổi lại** | Không nguồn nào (spec, kho, Studio requirement) định nghĩa: bạn bè đang chạy kịch bản bước từ action cũ, nay giá trị đổi sang lựa chọn có action khác → kịch bản cũ dừng hay chạy tiếp? `NEW-62` quan sát được *"lần 2: ステップ配信 **đã dừng**; lần 3: **đang chạy**"* nhưng đây là ghi nhận hành vi thực tế, không phải đối chiếu chuẩn | Chốt hành vi rồi bổ sung vào `feature-spec.md`; là oracle cho `TC-REGSHARED001-02` ở §5 |
| 7 | **Vai trò của route `/ajax/saveSettingDisplayInfo` (đời cũ)** | Dev ghi: *"đường dẫn còn đăng ký, giao diện cũ không còn gọi"*. Bug #40527 cho thấy route này **hỏng hoàn toàn với trường kiểu 選択肢** — nghĩa là đã hỏng từ trước ticket này, không ai phát hiện | Chốt: route này còn cần thiết không? Nếu không còn UI nào gọi và không phải hợp đồng API công khai → **gỡ route** thay vì vá. Nếu còn dùng (app cũ, tích hợp ngoài) → phải fix #40527 và đưa vào bộ smoke regression cố định (RULE-12) |

---

> **Đọc tiếp**: bộ TC đầy đủ 27 dòng ở [04-tc-list.md](04-tc-list.md) · input bug ở [01-bug-task.md](01-bug-task.md) · đánh giá Dev ở [03-dev-impact.md](03-dev-impact.md).
> Muốn đẩy 11 TC ở §5 về Studio task #275 → chạy `/sync-review-tc tasks/2026-09-05_40492_friend-info-action-khong-kich-hoat-chat/`.
