# 05 — Review Report

> Draft cho Leader verify. Ticket #39639 — `[Header/Menu] Update text của button mua slot`.

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | `(1) MCP LME TEST STUDIO task #112` (ticket 39639, round 1, branch `ai-feature-39639`) |
| Tổng số TC review | `15` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: chiều `dev-impact` **không kiểm chứng được** (file 03 không có 4.1/4.2/4.3 thật) · chiều `diff code` 2/2 file trong diff đều có TC đi qua · **2 GAP · 4 RISK**.

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | `03-dev-impact.md` mục 3 + 4.1 / 4.2 / 4.3 — Dev không kê `F*` / `D*` / `T*` | `dev-impact` | — | `Input thiếu` — chiều (a) không đối chiếu được; toàn bộ coverage dưới đây chỉ dựa trên diff của Studio | `[MAJOR]` |
| G2 | Điều kiện rẽ nhánh `$isAddBotFree` với **admin đăng ký trước 01/07/2021** (nhóm được phép có **nhiều** bot free) | `diff code` | `không có` | `GAP` — 0/15 TC. Cờ chỉ đếm `plan_type=2, is_deleted=0` (oracle Studio `UserController@index:363-367`), **không xét `flagChangeFreePlan`** → nhóm legacy sẽ thấy nhãn 「有料プランを契約して…」 + ghi chú 「ご選択いただけません」 trong khi họ **vẫn thêm được** bot free (bot-add-v2 `BR-004`, bot-edit `BR-35`, kho `TC-BLP-56`) | `[BLOCKER]` |
| G3 | Nhánh hiển thị lật lại khi **bot free duy nhất được upgrade lên plan trả phí** (không xóa bot) | `diff code` | `không có` | `GAP` — TC chỉ cover free **bị xóa** (`TC-OUTTRUTH001-04`) và free **chưa từng có**; kho `TC-BLP-58` cho thấy sau upgrade nhánh phải quay về "chưa dùng free" | `[MAJOR]` |
| G4 | `public/css/admin/index_v3.css` — asset version sau release | `diff code` | `TC-DEPLOYASSET001-01` (**Không đạt**) | `RISK` — TC đúng chỗ nhưng **fail** và chỉ chạy `local`. `spec_delta.files[]` chỉ có 2 file, **không có `config/sns-line.php`**, trong khi `dev_impact` ghi rõ "cần bump `config('sns-line.version')` để CSS mới có hiệu lực" | `[MAJOR]` |
| G5 | Màn khác cũng render nút phụ thuộc cờ bot free: 「契約情報・領収書」 `/basic/point-settings` (nút 「フリープランで接続する」 — kho `TC-BLP-09` / `TC-BLP-48`) | `diff code` | `TC-REGSHARED001-01` (chỉ cover `/admin/pre-select-bot`) | `RISK` — regression "không lan sang màn khác" mới cover 1/2 màn dùng chung cờ | `[MINOR]` |
| G6 | Bot free ở trạng thái **tạm** `is_deleted=2` (đang giữa flow thêm bot free, bot-add-v2 `BR-003`) | `diff code` | `không có` | `RISK` — trong cửa sổ ~180s nhãn vẫn hiện nhánh "chưa dùng free". **Không đề xuất TC** — cửa sổ hẹp, hiển thị-only, giá trị thấp so với chi phí dựng; Leader muốn thì thêm 1 Boundary | `[MINOR]` |

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `14 quan điểm Trigger khớp task · 11 chưa cover đủ` (7 thiếu nội dung + 4 chỉ thiếu loại case, gộp ở Q8).

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `PAY-LIMIT-001` | `Cao` | `GAP` — 0 TC mang mã này. Quan điểm yêu cầu test **cả tài khoản cũ (tạo trước khi đổi spec) lẫn mới**; toàn bộ 15 TC chỉ giả định luật "1 bot free/admin". Xem G2 | `[BLOCKER]` |
| Q2 | `UI-002` | `Trung bình` | `GAP` — 0 TC. Ghi chú 30 ký tự JP bị ép 1 dòng bằng `white-space: nowrap`; metric font khác nhau giữa **Mac Safari / Windows Chrome** → rủi ro tràn chỉ lộ trên browser khác. 15/15 TC chạy `auto` trên 1 runner local | `[MAJOR]` |
| Q3 | `PERM-001` | `Cao` | `RISK` — `TC-PERM001-01` là TC duy nhất, expected tự khai "Không kết luận nhánh Free cụ thể" → **không đo lường được**. Quan điểm bắt buộc **có role matrix trong spec trước khi test**; matrix cho màn 「アカウント一覧」 chưa tồn tại. Thiếu cả Abnormal + Boundary (RULE-01) | `[MAJOR]` |
| Q4 | `LIST-001` | `Trung bình` | `RISK` — màn có search / 並べ替え / 100件表示; TC duy nhất chạm là `TC-RULE12-01` mang **mã không hợp lệ** (`RULE-12` là RULE, không phải quan điểm) → không tính cover; expected "hoạt động như trước" không đo được, không thực sự chạy search / đổi trang sau khi layout bị đẩy xuống | `[MAJOR]` |
| Q5 | `ENV-003` | `Cao` | `RISK` — 15/15 TC khai `env_scope` không có `prd` và **0 run** ngoài `local`. Production có **2 server loadbalance** + cache CDN → version asset phải verify trên production (RULE-08). `TC-DEPLOYASSET001-01` khai `env_scope = all` nhưng vẫn chỉ chạy local | `[MAJOR]` |
| Q6 | `FUNC-001` | `Cao` | `RISK` — 2 TC đều `Normal`. Thiếu `Boundary` (nhánh lật lại sau upgrade — G3) và `Abnormal` (trạng thái cờ bất thường — G6), không ghi lý do (RULE-01) | `[MAJOR]` |
| Q7 | `OUT-TRUTH-001` | `Cao` | `RISK` — 4 TC: 2 `Normal` + 2 `Boundary`, **thiếu `Abnormal`**, không ghi lý do (RULE-01). Abnormal đáng có: cờ đếm ra trạng thái mà nhãn nói sai sự thật (G2 / G3) | `[MAJOR]` |
| Q8 | `COMPAT-LEGACY-001` · `SEC-ISO-001` · `REG-SHARED-001` · `DEPLOY-ASSET-001` | `Cao` | `RISK` — mỗi quan điểm chỉ 1 loại case, không ghi lý do (RULE-01). **Không đề xuất TC mới** — 4 quan điểm này không có biên/bất thường có ý nghĩa riêng ngoài phần đã nêu ở Q1/Q6/Q7; đề nghị ghi lý do vào `Ghi chú` của TC thay vì đẻ TC | `[MINOR]` |

---

## 3. TC trùng lặp nội dung

Đã rà **15/15** TC theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`). **Không có `DUP-EXACT` / `DUP-INFLATE` / `DUP-CONFLICT`.** Phát hiện 1 nhóm **chồng lấn assertion**:

| Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| DUP-1 | `TC-OUTTRUTH001-01` · `TC-COMPATLEGACY001-01` · `TC-OUTTRUTH001-02` | `TC-UI003-01` → **GIỮ NGUYÊN, không xóa** | `DUP-SUBSET` (một phần) | Cùng đối tượng (nhãn nút + dòng ghi chú), cùng 2 tiền đề (đã / chưa dùng free), phần expected "chuỗi JP đúng nguyên văn" nằm gọn trong 3 TC kia. **Khác**: mã quan điểm (`UI-003` vs `OUT-TRUTH-001`/`COMPAT-LEGACY-001`), loại case (`Abnormal` vs `Normal`), và 2 assertion riêng (chuỗi không rỗng, không lộ cú pháp mẫu sau khi `v-cloak` gỡ) | `[MINOR]` |

- **Gate đã chạy**: giả định xóa `TC-UI003-01` → quan điểm `UI-003` mất **toàn bộ** coverage (là TC duy nhất) và rủi ro R-01 (Vue nuốt chữ JP) không còn ai kiểm → **KHÔNG xóa**. Đề xuất: giữ nguyên, chỉ trỏ chéo trong `Ghi chú` để người chạy biết phần string-check đã có ở 3 TC kia.
- Ghi nhận thêm (không tính là trùng): `TC-SECISO001-01` lặp lại assertion nhãn + ghi chú của `TC-OUTTRUTH001-01/-02` + `TC-COMPATLEGACY001-01`, nhưng mục đích test là **cách ly giữa 2 tài khoản + cache trang giữa 2 phiên** → khác mã quan điểm, giữ nguyên.
- `DUP-INFLATE` che GAP: `không có`.
- `DUP-CONFLICT`: `không có` (mâu thuẫn expected **vs kho TCs** đã đưa vào §6, không phải trùng nội bộ).

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | `TC-DEPLOYASSET001-01` (Studio #14644) | TC **Không đạt** ở run 855 nhưng `bug_tickets` **rỗng** — fail chưa raise ticket. Fail nhiều khả năng là **thật**: `dev_impact` yêu cầu bump `config('sns-line.version')`, mà `spec_delta.files[]` của branch chỉ có 2 file (`index_v3.css`, `index_v3.blade.php`) — **không có `config/sns-line.php`** | Raise ticket bug cho TC fail. Hỏi Dev: version asset được bump ở branch này hay ở bước release riêng? Chưa trả lời được thì **không kết luận task pass** |
| I2 | `[MAJOR]` | Toàn bộ 15 TC | **0 run ngoài `local`** (`envAuto`: local 2 runs; dev / staging / prd đều 0). Task sửa **CSS asset** → cache + loadbalance chỉ tồn tại trên production (RULE-08) | Chạy lại tối thiểu `TC-DEPLOYASSET001-01` + `TC-UI001-02` trên staging và production sau khi bump version (xem `TC-ENV003-01` §5) |
| I3 | `[MAJOR]` | Toàn bộ 15 TC | Cột `Evidence thực tế` **rỗng 15/15** dù 14 TC đã tick Đạt → kết quả **tự khai**, vi phạm RULE-02. Quan điểm `UI-001` / `DEPLOY-ASSET-001` yêu cầu evidence là screenshot từng surface + screenshot DevTools Network | Đính screenshot cho ít nhất các TC assert giá trị đo được (12px / `#FF4D4F` / nowrap / version query) trước khi Leader duyệt |
| I4 | `[MAJOR]` | `TC-OUTTRUTH001-02` · `TC-FUNC001-02` · toàn bộ 15 TC | Cột `Trạng thái đánh giá spec` **rỗng 15/15**, trong khi chính `note` của TC thừa nhận **2 điểm spec chưa chốt**: (a) QA-004 — ghi chú 12px hay 14px, TC đang khẳng định 12px; (b) QA-001 — nút có bị disable khi đã dùng free hay không, TC đang khẳng định KHÔNG disable | Điền `Đã hỏi leader` + link quyết định, hoặc chốt spec trước (xem §6). TC khẳng định 1 phía khi spec chưa chốt = nguy cơ báo Đạt sai |
| I5 | `[MAJOR]` `[AP-6]` | `03-dev-impact.md` | Mục 3 "Đã check và sửa các function sử dụng đến function/data vừa sửa" **trống** (`<Input thiếu>`); mục 4.1/4.2/4.3 chỉ có placeholder; field `Auto-filled: 2026-09-09 by /new-task` nhưng checkbox "Tester verify auto-fill chính xác" **chưa tick** | Yêu cầu Dev điền đủ 4 mục (đặc biệt: nơi nào khác đọc cờ `$isAddBotFree`), tester verify rồi tick checkbox. Hiện tại chiều `dev-impact` của §1 không kiểm chứng được |
| I6 | `[MAJOR]` `[AP-3]` | 15/15 TC | Toàn bộ precondition là trạng thái sạch ("tài khoản riêng cho lượt test", "1 LINE công thức"). Không có TC nào chạy ở **edge state** của chính màn: admin nhiều LOA, LOA hết hạn / bị 強制解約, admin có slot hợp đồng chưa kết nối (`TC-FUNC001-02` **cố ý loại trừ**) | Bổ sung ít nhất 1 tiền đề "tài khoản thật nhiều trạng thái" cho nhóm TC hiển thị, hoặc ghi rõ lý do giới hạn phạm vi |
| I7 | `[MAJOR]` | `TC-PERM001-01` | `Kết quả mong đợi` không đo lường được: "hiển thị theo logic trạng thái Free của tài khoản đăng nhập thực tế… Không kết luận nhánh Free cụ thể". Người chạy không thể phán Đạt / Không đạt | Chốt role matrix (§6 #4) rồi viết lại expected thành giá trị cụ thể — xem `TC-PERM001-02` §5 |
| I8 | `[MAJOR]` | `TC-RULE12-01` (Studio #14640) | Cột `Mã quan điểm liên kết` ghi `RULE-12` — đây là **RULE** (`framework/checklist-lme.md:73`), không nằm trong 80 quan điểm → TC này **không map được coverage**; expected "hoạt động như trước thay đổi" cũng không đo được (chỉ quan sát sự tồn tại, không thao tác) | Đổi mã sang `LIST-001` (hoặc `REG-SHARED-001`) và viết expected thành thao tác cụ thể — xem `TC-LIST001-01` §5 |
| I9 | `[NIT]` | Bộ TC Studio | Studio lưu `temp_id` dạng `NEW-1` … `NEW-15`, không có `client_ref`; `TC No.` canonical (`TC-OUTTRUTH001-01`…) là do snapshot `04-tc-list.md` sinh ra. Trace ngược đang phải dựa vào cột `Ghi chú` | Nếu muốn ID ổn định 2 chiều thì set `client_ref` trên Studio bằng `TC No.` canonical |

---

## 5. TCs đề xuất bổ sung (9)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa031-billtientool-契約プラン・決済情報.md` — nhóm 『Rule slot trống & bot free』 (`TC-BLP-56` → `TC-BLP-62`) + `TC-BLP-09` / `TC-BLP-48` / `TC-BLP-51`. ⚠️ **kho chưa có tính năng cho màn 「アカウント一覧」 `/admin/home`** (không có mã FA tương ứng trong `LME-SYSTEM-SPEC.md`) → chỉ đối chiếu được kho lân cận dùng chung rule bot free |
| Vùng regression phát hiện từ kho | (1) `TC-BLP-56` — user tạo **trước 01/07/2021** tạo được **không giới hạn** bot free → G2/Q1; (2) `TC-BLP-58` — bot free **upgrade** lên plan khác thì nhánh hiển thị lật lại → G3; (3) `TC-BLP-09` / `TC-BLP-48` — màn 「契約情報・領収書」 cũng có nút phụ thuộc cờ bot free → G5 |
| Conflict expected vs kho | **Có** — `TC-COMPATLEGACY001-01` + `TC-OUTTRUTH001-02` (Studio) khẳng định *mọi* admin đã có 1 bot free đều thấy nhãn trả phí + ghi chú 「ご選択いただけません」, **vs** kho `TC-BLP-56` (admin tạo trước 01/07/2021 vẫn add được bot free thứ 2) → `[MAJOR] SPEC-CONFLICT`, đã đưa §6 #1. **Không tự chọn bên** |
| GAP dùng lại TC kho (không viết mới) | `Không` — TC kho nằm ở màn 「プラン選択」 / 「契約情報」, không dùng lại nguyên si cho màn 「アカウント一覧」; chỉ dẫn chiếu làm nguồn expected |
| Xác nhận chống trùng | Đã đối chiếu **15** TC ở BƯỚC 0 + kho `fa031` — **không TC đề xuất nào trùng** (4 yếu tố khác nhau ở tiền đề trạng thái tài khoản / browser / màn hình đích) |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-PAYLIMIT001-01 | UI | PAY-LIMIT-001 | Nút thêm LOA — 「アカウント一覧」 | Normal | manual | Tất cả | Tài khoản đăng ký trước 01/07/2021 đang có 1 bot free vẫn thấy nhãn gói Free | Đăng nhập bằng **tài khoản cũ có ngày đăng ký trước 01/07/2021** (nhóm không bị giới hạn 1 bot free), tài khoản này đang có đúng 1 LINE công thức gói Free còn hiệu lực | 1. Đăng nhập trang quản trị bằng tài khoản cũ đã chuẩn bị<br>2. Mở màn 「アカウント一覧」, chờ danh sách hiện xong<br>3. Đọc nhãn trên nút thêm LOA<br>4. Quan sát vùng ngay dưới nút<br>5. Bấm nút và quan sát màn mở ra | Tài khoản cũ (đăng ký trước 01/07/2021) · đang có 1 LOA gói Free | Nút hiển thị 「フリープランでLINE公式アカウント追加」 và **KHÔNG** có dòng ghi chú đỏ; bấm nút mở luồng thêm LOA gói Free (`/admin/bot-add-v2`) — đúng với quyền thêm bot free của nhóm tài khoản cũ | | Lấp `G2` / `Q1` · Đánh giá spec: **Đã hỏi leader — chờ chốt §6 #1**; expected viết theo bot-add-v2 `BR-004` + bot-edit `BR-35` + kho `TC-BLP-56`. Nếu BA chốt ngược (áp giới hạn cho mọi tài khoản) thì sửa expected thành nhãn trả phí + ghi chú · Evidence: screenshot nút + ghi chú + màn đích · dẫn từ `TC-BLP-56` |
| TC-PAYLIMIT001-02 | UI | PAY-LIMIT-001 | Nút thêm LOA — 「アカウント一覧」 | Boundary | manual | Tất cả | Tài khoản đăng ký đúng ngày 01/07/2021 rơi vào nhánh nào | Đăng nhập bằng tài khoản có **ngày đăng ký đúng 01/07/2021**, đang có 1 LINE công thức gói Free còn hiệu lực | 1. Đăng nhập bằng tài khoản mốc 01/07/2021<br>2. Mở màn 「アカウント一覧」<br>3. Đọc nhãn nút thêm LOA và vùng dưới nút<br>4. Ghi lại nhãn + có/không có ghi chú | Ngày đăng ký = 01/07/2021 · 1 LOA gói Free | Nhãn + ghi chú khớp đúng **một** nhánh đã chốt cho mốc ngày (bao gồm hay loại trừ chính ngày 01/07/2021), và khớp với hành vi thật khi bấm nút thêm LOA — không có trạng thái nửa vời (nhãn nói không chọn được free nhưng vẫn thêm được free, hoặc ngược lại) | | Lấp `G2` / `Q1` · Đánh giá spec: **Spec không ghi — đã hỏi leader (§6 #1)**; spec hiện chỉ ghi "sau ngày 2021-07-01", không nói mốc đóng hay mở · Evidence: screenshot nút + kết quả sau khi bấm |
| TC-PAYLIMIT001-03 | UI | PAY-LIMIT-001 | Nút thêm LOA — 「アカウント一覧」 | Abnormal | manual | Tất cả | Tài khoản cũ đang có 2 bot free — ghi chú "không chọn được" có nói sai sự thật không | Đăng nhập bằng tài khoản cũ (đăng ký trước 01/07/2021) đang có **≥ 2** LINE công thức gói Free còn hiệu lực cùng lúc | 1. Đăng nhập bằng tài khoản cũ đã có 2 LOA gói Free<br>2. Mở màn 「アカウント一覧」, xác nhận cột plan có 2 dòng 「フリー」<br>3. Đọc nhãn nút và dòng ghi chú<br>4. Bấm nút, đi hết luồng đến bước chọn gói và ghi lại gói Free còn chọn được hay không | Tài khoản cũ · 2 LOA gói Free đang hoạt động | Chữ trên màn phải khớp với quyền thật: nếu tài khoản vẫn thêm được bot free thứ 3 thì **không được** hiển thị ghi chú 「※フリープランはすでにご利用中のため、ご選択いただけません。」; nếu hệ thống thật sự chặn thì bước chọn gói phải chặn tương ứng | | Lấp `G2` / `Q1` · Đánh giá spec: **Đã hỏi leader (§6 #1)** · Evidence: screenshot nút + ghi chú + màn chọn gói · phát hiện từ kho `TC-BLP-56` |
| TC-FUNC001-03 | UI | FUNC-001 | Nút thêm LOA — 「アカウント一覧」 | Boundary | manual | Tất cả | Bot free duy nhất được nâng lên gói trả phí thì nhãn quay lại nhánh gói Free | Đăng nhập bằng tài khoản chỉ có **1** LINE công thức đang ở gói Free; LOA này **đã được nâng lên gói trả phí** (standard/pro) và hợp đồng mới đang ở trạng thái 正常 — không xóa LOA nào | 1. Mở màn 「アカウント一覧」 trước khi nâng gói, ghi lại nhãn nút + có dòng ghi chú<br>2. Nâng LOA gói Free lên gói trả phí, chờ hợp đồng chuyển 正常<br>3. Tải lại màn 「アカウント一覧」<br>4. Đọc lại nhãn nút và vùng dưới nút | 1 LOA · trước: gói Free · sau: gói trả phí | Sau khi nâng gói, tài khoản không còn LOA gói Free nào → nút quay lại nhãn 「フリープランでLINE公式アカウント追加」 và dòng ghi chú đỏ **biến mất khỏi màn** (phần tử không còn tồn tại, không phải bị ẩn) | | Lấp `G3` / `Q6` · Đánh giá spec: Spec ghi rõ (đếm `plan_type=2`) · Evidence: screenshot trước/sau khi nâng gói · regression — dẫn từ kho `TC-BLP-58` |
| TC-PERM001-02 | UI | PERM-001 | Nút thêm LOA — 「アカウント一覧」 | Normal | manual | Tất cả | Nhân viên được mời thấy nhãn theo trạng thái gói Free của chủ tài khoản | Chuẩn bị 1 chủ tài khoản **đã có** 1 LOA gói Free và 1 chủ tài khoản **chưa có** LOA gói Free; mỗi bên mời 1 nhân viên vào LINE công thức của mình; có sẵn tài khoản đăng nhập của 2 nhân viên | 1. Đăng nhập bằng nhân viên của chủ tài khoản **đã dùng** gói Free, mở 「アカウント一覧」, ghi lại nhãn nút + dòng ghi chú<br>2. Đăng xuất, đăng nhập bằng nhân viên của chủ tài khoản **chưa dùng** gói Free, mở lại màn, ghi lại nhãn nút + dòng ghi chú<br>3. Đối chiếu 2 kết quả với nhãn mà chính 2 chủ tài khoản nhìn thấy | 2 nhân viên thuộc 2 chủ tài khoản khác trạng thái gói Free | Nhân viên thấy **đúng nhãn + ghi chú của chủ tài khoản mình thuộc về**: nhân viên bên đã dùng free thấy 「有料プランを契約してLINE公式アカウント追加」 + ghi chú đỏ; nhân viên bên chưa dùng free thấy 「フリープランでLINE公式アカウント追加」, không ghi chú. Không nhân viên nào thấy nhãn của bên còn lại | | Lấp `Q3` · Đánh giá spec: **Spec không ghi — đã hỏi leader (§6 #4, cần role matrix)**; expected viết theo cách tính hiện tại là đếm theo `admin_id` chủ sở hữu · Evidence: screenshot từng role + ma trận role đã test · thay thế expected mơ hồ của `TC-PERM001-01` (I7) |
| TC-UI002-01 | UI | UI-002 | Dòng ghi chú dưới nút thêm LOA | Normal | manual | Tất cả | Ghi chú một dòng hiển thị đủ trên Mac Safari và Windows Chrome | Tài khoản đang có 1 LINE công thức gói Free; có sẵn máy Mac (Safari + Chrome) và máy Windows (Chrome), cửa sổ đặt về 1366×768 | 1. Trên Mac Safari, đăng nhập và mở 「アカウント一覧」<br>2. Đọc dòng ghi chú dưới nút: có bị cắt chữ, xuống dòng hay tràn khỏi vùng nội dung không<br>3. Lặp lại trên Mac Chrome<br>4. Lặp lại trên Windows Chrome<br>5. Đối chiếu 3 kết quả với nhau | 3 tổ hợp: Mac Safari · Mac Chrome · Windows Chrome, cùng 1366×768 | Cả 3 trình duyệt đều hiện đủ 「※フリープランはすでにご利用中のため、ご選択いただけません。」 trên **một dòng**, không cắt chữ, không tràn khỏi vùng nội dung, không đè lên tiêu đề hay bảng danh sách và không sinh thanh cuộn ngang mới | | Lấp `Q2` · Đánh giá spec: Spec ghi rõ (thiết kế yêu cầu 1 dòng) · Evidence: screenshot từng browser · rủi ro R-02: `white-space: nowrap` + font JP khác metric giữa Safari/Chrome |
| TC-LIST001-01 | UI | LIST-001 | Danh sách LINE công thức — 「アカウント一覧」 | Normal | manual | Tất cả | Search / sắp xếp / đổi trang vẫn chạy đúng khi bảng bị ghi chú đẩy xuống | Tài khoản đang có 1 LINE công thức gói Free (để dòng ghi chú hiển thị) và **≥ 101** LINE công thức trong danh sách để có nhiều trang; biết trước tên LINE và tên quản lý của 1 LOA để tìm kiếm | 1. Mở 「アカウント一覧」, xác nhận dòng ghi chú đỏ đang hiển thị và bảng bị đẩy xuống<br>2. Gõ **tên LINE** của 1 LOA vào ô tìm kiếm → xem kết quả<br>3. Gõ **tên quản lý** của LOA đó → xem kết quả<br>4. Gõ một chuỗi chắc chắn không tồn tại → xem trạng thái rỗng<br>5. Bấm 「並べ替え」 đổi thứ tự rồi chuyển sang trang 2 bằng 「100件表示」/phân trang<br>6. Kiểm tra dòng ghi chú và nút thêm LOA sau mỗi thao tác | Danh sách ≥ 101 LOA · từ khóa: tên LINE / tên quản lý / chuỗi không tồn tại | Cả 3 kiểu tìm kiếm trả đúng kết quả (khớp đúng LOA, chuỗi không tồn tại thì hiện trạng thái rỗng rõ ràng, không trắng màn); đổi thứ tự và chuyển trang chạy đúng; sau mọi thao tác, nhãn nút và dòng ghi chú vẫn nguyên nội dung, không bị bảng đè lên và không biến mất | | Lấp `Q4` · Đánh giá spec: Spec ghi rõ · Evidence: screenshot 4 góc của LIST-001 · thay `TC-RULE12-01` vốn chỉ quan sát sự tồn tại (I8) |
| TC-ENV003-01 | UI | ENV-003 | Asset CSS của khối nút thêm LOA | Abnormal | manual | product | CSS mới có hiệu lực sau release trên production, kiểm trên cả 2 server | Đã có bản release chứa thay đổi này lên production; tài khoản dùng để kiểm đang có 1 LINE công thức gói Free; trình duyệt **đã mở màn 「アカウント一覧」 bản cũ trước khi release** để còn giữ CSS cũ trong bộ nhớ đệm | 1. Trước release: mở 「アカウント一覧」 trên production để browser cache CSS cũ<br>2. Sau khi release xong, quay lại tab đó và **chỉ F5 thường** (không Ctrl+F5, không xoá cache)<br>3. Quan sát dòng ghi chú: màu, cỡ chữ, có nằm 1 dòng không<br>4. Mở DevTools > Network, lọc file CSS của màn: kiểm mã trả về và tham số phiên bản trên đường dẫn<br>5. Lặp lại bước 2-4 vài lần để rơi vào **cả 2 server loadbalance** (ghi lại router id mỗi lần)<br>6. Kiểm lại trên staging để so sánh | Production sau release · browser còn cache CSS bản cũ | Ngay sau F5 thường, dòng ghi chú đã ăn kiểu chữ mới (đỏ `#FF4D4F`, 12px, 1 dòng, canh mép phải nút); file CSS trả **200** kèm tham số phiên bản **mới**, không 404; kết quả giống nhau ở **mọi lần tải, trên cả 2 server** — không có lần có kiểu mới, lần rơi về kiểu mặc định | | Lấp `G4` / `Q5` · **RULE-08** — chỉ kết luận được trên production · Đánh giá spec: **Đã hỏi leader (§6 #2 — version asset bump ở đâu)** · Evidence: screenshot DevTools Network (version + 200) + screenshot màn sau F5 thường + router id từng lần · liên quan `TC-DEPLOYASSET001-01` đang fail (I1) |
| TC-REGSHARED001-02 | UI | REG-SHARED-001 | Nút thêm LOA ở màn 「契約情報・領収書」 `/basic/point-settings` | Normal | manual | Tất cả | Màn hợp đồng dùng chung cờ bot free không bị đổi chữ theo thay đổi lần này | Chuẩn bị 2 tài khoản: 1 đang có LOA gói Free, 1 không có LOA gói Free nào; cả hai mở được màn 「契約情報・領収書」 | 1. Đăng nhập tài khoản **chưa dùng** gói Free, mở `/basic/point-settings`<br>2. Ghi lại nhãn nút thêm LOA / 「フリープランで接続する」 và vùng dưới nút<br>3. Đăng nhập tài khoản **đã dùng** gói Free, mở lại màn đó<br>4. Ghi lại nhãn nút và vùng dưới nút<br>5. Đối chiếu với trạng thái trước thay đổi | 2 tài khoản khác trạng thái gói Free | Ở cả 2 trạng thái, màn 「契約情報・領収書」 giữ nguyên nhãn nút như trước thay đổi và **không** xuất hiện dòng ghi chú 「※フリープランはすでにご利用中のため、ご選択いただけません。」; bố cục màn không đổi | | Lấp `G5` · Đánh giá spec: Spec ghi rõ (diff không chạm màn này) · Evidence: screenshot 2 trạng thái · regression — dẫn từ kho `TC-BLP-09` / `TC-BLP-48` · nếu wording 3 màn cần thống nhất thì xem §6 #5 |

**Dòng `GAP`/`RISK` không đề xuất TC**: `G1` (input thiếu — cần Dev điền file 03, không phải việc của TC) · `G6` (cửa sổ `is_deleted=2` ~180s, hiển thị-only, giá trị thấp — Leader quyết) · `Q7` (Abnormal của `OUT-TRUTH-001` đã nằm trong `TC-PAYLIMIT001-03`) · `Q8` (4 quan điểm chỉ cần ghi lý do thiếu loại case theo RULE-01).

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | `spec-features/admin/billing-plan/feature-spec.md` (chưa ghi) + `bot-add-v2` BR-004 + `bot-edit` BR-35 | Chốt nhãn + ghi chú mới áp dụng thế nào cho **admin đăng ký trước 01/07/2021** (nhóm được có nhiều bot free): giữ nhãn gói Free, hay vẫn hiện 「ご選択いただけません」 dù họ vẫn thêm được? Đồng thời bổ sung luật mốc 01/07/2021 vào spec `billing-plan` (hiện chỉ nằm rải ở `bot-add-v2` / `bot-edit`) | TC Studio (`TC-COMPATLEGACY001-01`, `TC-OUTTRUTH001-02` — đếm thuần) **vs** kho `TC-BLP-56` + `BR-004` / `BR-35`. Kho `fa031` cũng đang ghi `feature-spec.md` không mô tả quy tắc mốc 01/07/2021 | `BA / PM` + `Dev` |
| 2 | Quy trình release — version asset | Bump `config('sns-line.version')` nằm ở branch tính năng hay ở bước release chung? `dev_impact` yêu cầu bump nhưng diff branch không có file config → `TC-DEPLOYASSET001-01` đang fail | `dev_impact` Studio **vs** `spec_delta.files[]` (2 file) | `Dev` + `Leader` |
| 3 | Thiết kế khối ghi chú (QA-004 / QA-001) | (a) Cỡ chữ dòng ghi chú: **12px** (đang assert trong TC) hay **14px** cho đồng bộ với chú thích đỏ cạnh tiêu đề? (b) Nút có bị **disable** khi đã dùng gói Free không — chữ 「ご選択いただけません」 dễ bị hiểu là chặn, nhưng chặn thì mất đường thêm LOA trả phí | `note` của `TC-OUTTRUTH001-02` + `TC-FUNC001-02` (ghi rõ "chờ reviewer") · kho `fa031` **MT-41** cũng đang ⏳ CHỜ QUYẾT ĐỊNH cho câu tương tự ở màn 「プラン選択」 | `BA / PM` + `Design` |
| 4 | Role matrix màn 「アカウント一覧」 | Nhân viên (staff) được mời thấy nhãn / ghi chú theo trạng thái gói Free của **chủ tài khoản** hay của chính họ? `PERM-001` yêu cầu có role matrix trước khi test; hiện chưa có → `TC-PERM001-01` phải để expected mơ hồ | `TC-PERM001-01` note ("cần BA xác nhận thêm nếu muốn quy định riêng cho role staff") | `BA` |
| 5 | Wording nút thêm LOA giữa các màn | Sau thay đổi này, 3 màn dùng 3 nhãn khác nhau: 「アカウント一覧」 = 「フリープランでLINE公式アカウント追加」 · 「契約情報・領収書」 = 「フリープランで接続する」 · 「アカウント選択」 = 「LINE公式アカウントを追加」. Có cần thống nhất không? | Ticket #39639 **vs** kho `TC-BLP-09` / `TC-BLP-48` **vs** `TC-REGSHARED001-01` | `BA / PM` |
