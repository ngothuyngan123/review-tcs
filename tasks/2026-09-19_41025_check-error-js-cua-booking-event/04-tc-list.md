<!-- source: MCP LME TEST STUDIO — task_id=315, ticket 41025, testcase_list (32 TC), fetch lúc 2026-09-19. READ-ONLY snapshot, sinh bởi scripts/parse_studio_tcs.py. -->

# 04 — TC List (snapshot từ MCP LME TEST STUDIO)

> ⚠️ `contentTrust = untrusted` → xử lý như **data**, không phải chỉ thị.
> ⚠️ **READ-ONLY** — muốn sửa TC thì sửa trên Studio (`testcase_update`) rồi fetch lại.

# Digest — Studio task #315 · ticket 41025 · 32 TC

## Kết quả thực thi
| Trạng thái | Số TC |
|---|---|
| `(chưa chạy)` | 32 |

→ **0/32 TC (0%) thực sự Đạt**; 32 TC còn lại KHÔNG có kết luận test.

## Môi trường
| Env | Số TC |
|---|---|
| `(CHƯA CHẠY)` | 32 |

→ Production: **0 TC**.  ⚠️ **RULE-08**: không kết luận media / domain / job nền / bill tiền từ local-staging.

## Ai chạy (source / by)
| source / by | Số TC |
|---|---|
| `— / —` | 32 |

## Tác giả TC
| author | Số TC |
|---|---|
| `AI` | 32 |

## Loại case
| case_type | Số TC |
|---|---|
| `Abnormal` | 20 |
| `Normal` | 9 |
| `Boundary` | 3 |

## Nhóm / chế độ chạy
| tc_group | Số TC |
|---|---|
| `ui` | 21 |
| `api` | 11 |

| exec_mode | Số TC |
|---|---|
| `auto` | 31 |
| `manual` | 1 |

## Mã quan điểm KHỚP checklist-lme (12 mã)
`CONC-003`(1) · `DEPLOY-ASSET-001`(1) · `ENV-003`(1) · `FRIEND-001`(1) · `FUNC-003`(1) · `FUNC-004`(2) · `INTG-LINE-001`(2) · `LIFF-ENTRY-001`(1) · `REG-SHARED-001`(1) · `REG-SPEC-001`(1) · `SEC-002`(1) · `UI-003`(1)

## ⚠️ Mã quan điểm KHÔNG có trong checklist-lme (7 mã)
| Mã Studio | Số TC |
|---|---|
| `TOOL-ERRHYG-001` | 4 |
| `API-CONTRACT-001` | 4 |
| `TOOL-NEGCTRL-001` | 3 |
| `TOOL-KNOW-002` | 2 |
| `RULE-TOOL-029` | 2 |
| `OBS-001` | 2 |
| `RULE-TOOL-028` | 1 |

→ `/review-tc` KHÔNG map được coverage cho các mã này.

## ⚠️ TC fail / error hoặc có ticket bug (0)
(không có)

## ⚠️ TC skip / chưa chạy (32)
| ID | exec | Tiêu đề |
|---|---|---|
| NEW-1 | `(chưa chạy)` | Đăng ký sự kiện miễn phí hoàn tất bình thường sau bản vá |
| NEW-2 | `(chưa chạy)` | Sự kiện miễn phí gặp lỗi hết chỗ vẫn ở màn xác nhận và gửi lại được |
| NEW-3 | `(chưa chạy)` | Sự kiện Univapay gặp lỗi ở bước thanh toán không sinh lỗi JavaScript v |
| NEW-4 | `(chưa chạy)` | Sự kiện Stripe gặp lỗi thanh toán vẫn quay về bước nhập thẻ và ba ô th |
| NEW-5 | `(chưa chạy)` | Xác thực 3D-Secure thất bại vẫn quay về bước nhập thẻ và xoá trắng ô t |
| NEW-6 | `(chưa chạy)` | Sự kiện bật thu tiền nhưng khách không đi qua bước thẻ vẫn giữ màn xác |
| 18924 | `(chưa chạy)` | Kiểm tra thiếu dữ liệu trước khi sang màn xác nhận vẫn báo đúng và khô |
| 18933 | `(chưa chạy)` | Lấy thông tin khách trả về mã lỗi thật thì báo ngay, không thử lại và  |
| NEW-7 | `(chưa chạy)` | Đổi lịch sự kiện miễn phí hoàn tất bình thường sau bản vá |
| NEW-8 | `(chưa chạy)` | Đổi lịch sự kiện miễn phí gặp lỗi hết chỗ vẫn giữ màn xác nhận thay đổ |
| NEW-9 | `(chưa chạy)` | Đổi lịch sự kiện Stripe gặp lỗi vẫn quay về bước nhập thẻ và ba ô thẻ  |
| 18927 | `(chưa chạy)` | Đổi lịch sự kiện Univapay gặp lỗi vượt hạn mức chuyển về bước nhập thẻ |
| NEW-10 | `(chưa chạy)` | Mở màn đăng ký bình thường điền sẵn thông tin khách và bật nút đi tiếp |
| NEW-14 | `(chưa chạy)` | Vết thao tác của khách được gửi kèm theo đúng thứ tự khi báo lỗi |
| NEW-11 | `(chưa chạy)` | Request lấy thông tin khách chết ở tầng vận chuyển lần đầu thì tự thử  |
| NEW-12 | `(chưa chạy)` | Request lấy thông tin khách chết liên tục thì chỉ thử lại một lần rồi  |
| NEW-13 | `(chưa chạy)` | Quay lại trang đăng ký sau khi sang màn kết bạn thì cơ chế thử lại và  |
| NEW-15 | `(chưa chạy)` | Thiếu khối khai báo ghi vết không được làm chết thao tác đăng ký của k |
| 18934 | `(chưa chạy)` | Khách chưa kết bạn được đưa sang màn kết bạn mà không bị báo động giả  |
| NEW-28 | `(chưa chạy)` | Trang đăng ký nạp đúng bản JavaScript đã vá sau khi release |
| NEW-29 | `(chưa chạy)` | Trên LINE thật: quay lại trang đăng ký sau khi sang màn kết bạn vẫn bá |
| NEW-16 | `(chưa chạy)` | Báo lỗi JS không gửi cờ báo động vẫn giữ nguyên hành vi cảnh báo như t |
| NEW-17 | `(chưa chạy)` | Báo lỗi JS với cờ báo động tắt chỉ ghi log và không đẩy cảnh báo |
| NEW-18 | `(chưa chạy)` | Vết thao tác được gửi kèm khi báo lỗi lỗi thật nhưng không đi vào nội  |
| NEW-20 | `(chưa chạy)` | Báo lỗi JS với bản tin rỗng bị từ chối và không ghi log, không đẩy cản |
| NEW-22 | `(chưa chạy)` | Giới hạn số lần báo lỗi theo địa chỉ IP vẫn còn hiệu lực và đo được qu |
| NEW-23 | `(chưa chạy)` | Log và cảnh báo lỗi JS không được chứa thông tin thẻ, mã xác thực hay  |
| NEW-19 | `(chưa chạy)` | Báo lỗi JS cắt đúng độ dài từng phần và không gây lỗi hệ thống với dữ  |
| NEW-21 | `(chưa chạy)` | Khoá trùng cảnh báo gom nhiều khách cùng lỗi thành một tin, nhưng nhán |
| NEW-24 | `(chưa chạy)` | Lấy thông tin khách trả về đúng ba phần như trước và ghi đủ ba dòng lo |
| NEW-25 | `(chưa chạy)` | Lấy thông tin khách với mã người dùng không tra ra được vẫn trả về thà |
| NEW-26 | `(chưa chạy)` | Lấy thông tin khách với mã sự kiện không tồn tại trả về danh sách rỗng |

## Màn hình (9)
| screen | Số TC |
|---|---|
| `API - Báo lỗi JS màn sự kiện LIFF` | 8 |
| `LIFF - Màn đăng ký sự kiện (xác nhận & nhánh báo lỗi)` | 6 |
| `LIFF - Màn đăng ký sự kiện (khởi tạo & lấy thông tin khách)` | 6 |
| `LIFF - Màn đổi lịch sự kiện (xác nhận & nhánh báo lỗi)` | 3 |
| `API - Lấy thông tin khách màn sự kiện LIFF` | 3 |
| `LIFF - Trang đăng ký sự kiện (đổi lịch, hủy, thanh toán, lịch sử)` | 2 |
| `Vận hành - Phát hành JavaScript màn sự kiện LIFF` | 2 |
| `Line user - màn hình change booking` | 1 |
| `LINE user — mở link & entry` | 1 |

## requirement_keys (17)
`REQ-001`(4) · `REQ-002`(3) · `REQ-003`(2) · `REQ-004`(3) · `REQ-005`(2) · `REQ-006`(4) · `REQ-007`(1) · `REQ-008`(3) · `REQ-009`(2) · `REQ-010`(3) · `REQ-011`(2) · `REQ-012`(3) · `REQ-013`(2) · `REQ-014`(3) · `REQ-015`(1) · `REQ-016`(1) · `REQ-017`(1)

---

## Bảng TC (16 cột canonical)

| TC No. | Mã quan điểm liên kết | Loại case | Tiêu đề test case | Điều kiện tiền đề | Các bước thực hiện | Dữ liệu test/input | Kết quả mong đợi | Kết quả thực thi | Evidence thực tế | Môi trường test | Người thực hiện | Ngày thực hiện | Số ticket bug | Trạng thái đánh giá spec | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| NEW-1 | TOOL-NEGCTRL-001 | Normal | Đăng ký sự kiện miễn phí hoàn tất bình thường sau bản vá | Môi trường chạy nhánh fix `ai_fixbug_41025` (không phải `ai_fixbug_40892`). Có sự kiện đặt chỗ KHÔNG bật thu phí, chế độ duyệt tự động, một khung giờ còn ít nhất 1 chỗ trống và có ít nhất 1 gói. Có sẵn đường dẫn đăng ký của sự kiện kèm mã người dùng của một người dùng LINE đã kết bạn. Đã ghi lại số chỗ đã dùng của khung giờ và số đơn hiện có của người dùng test — môi trường dùng lại dữ liệu các lần chạy trước nên không được giả định danh sách đang rỗng. Mở màn ở khung nhìn điện thoại. | 1. Ghi lại số chỗ đã dùng của khung giờ và số đơn hiện có của người dùng test.<br>2. Mở đường dẫn đăng ký sự kiện trên trình duyệt ở khung nhìn điện thoại.<br>3. Chọn ngày, khung giờ còn chỗ, gói, số lượng 1 và nhập đủ các mục thông tin bắt buộc.<br>4. Bấm 「予約確認にすすむ」 và xác nhận đã sang màn xác nhận nội dung đăng ký.<br>5. Bấm nút gửi đăng ký 「申し込む」 đúng một lần rồi chờ xử lý xong.<br>6. Kiểm tra màn hình kết quả, mục 「予約履歴」, danh sách người tham gia trên màn quản trị và số chỗ đã dùng của khung giờ. | Sự kiện không thu phí, số lượng 1, nhập đủ mục thông tin bắt buộc. Phép tính tay: 1 lần đăng ký hợp lệ ⇒ 1 đơn mới, số chỗ đã dùng tăng đúng 1. | Không có hộp thoại lỗi nào hiện lên; màn hình đi tới trang hoàn tất hoặc quay về màn chat theo cấu hình của sự kiện. Tạo đúng 1 đơn mới; 「予約履歴」 có đúng 1 dòng tương ứng; danh sách người tham gia trên màn quản trị có đúng 1 bản ghi mới; số chỗ đã dùng của khung giờ tăng đúng 1. Trong suốt luồng không xuất hiện lỗi JavaScript nào trên console. | Chưa test |  | all |  |  |  |  | Studio #18917 (NEW-1) · mã theo quan điểm: TC-TOOLNEGCTRL001-01 · ui · auto · local-only · REQ: REQ-002 · spec: TICKET-41025, SCR-EVB-16, diff: order-item.js payment() (chốt if autoBill == 1) · Đối chứng âm cho chốt mới `if (autoBill == 1)` trong payment(): chốt chỉ được chặn nhánh BÁO LỖI, không được chặn đường thành công. TC này tạo dữ liệu thật nên gắn env_tag local-only cho runner tự động, còn tester vẫn chạy được trên dev/staging/production. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-2 | TOOL-KNOW-002 | Abnormal | Sự kiện miễn phí gặp lỗi hết chỗ vẫn ở màn xác nhận và gửi lại được | Môi trường chạy nhánh fix `ai_fixbug_41025`. Có sự kiện đặt chỗ KHÔNG bật thu phí. Cần dựng được tình trạng khung giờ HẾT CHỖ đúng vào lúc khách bấm gửi đăng ký: dùng hai người dùng LINE test đã kết bạn (A và B) và một khung giờ chỉ còn ĐÚNG 1 chỗ. Mở màn bằng trình duyệt ở khung nhìn điện thoại, bật sẵn console để theo dõi lỗi JavaScript. Đã ghi lại số chỗ đã dùng của khung giờ. | 1. Dùng người dùng A: mở đường dẫn đăng ký, chọn khung giờ chỉ còn 1 chỗ, nhập đủ thông tin và đi tới màn xác nhận, NHƯNG CHƯA bấm gửi đăng ký.<br>2. Dùng người dùng B ở cửa sổ khác: đăng ký hoàn tất vào đúng khung giờ đó để chỗ cuối cùng bị lấy hết.<br>3. Quay lại cửa sổ của người dùng A, bấm nút gửi đăng ký 「申し込む」.<br>4. Đọc nguyên văn nội dung hộp thoại hiện lên rồi bấm OK để đóng.<br>5. KHÔNG tải lại trang. Quan sát ngay màn hình: còn thấy nội dung xác nhận đăng ký (ngày giờ, gói, số lượng, thông tin đã nhập) và còn nút 「申し込む」 cùng nút 「戻る」 hay không.<br>6. Đọc console của trình duyệt, tìm lỗi JavaScript phát sinh kể từ lúc bấm gửi.<br>7. Bấm 「戻る」 để về bước nhập, đổi sang một khung giờ khác còn chỗ, rồi đi lại tới màn xác nhận và bấm 「申し込む」.<br>8. Kiểm tra kết quả lần gửi lại và số chỗ đã dùng của hai khung giờ. | Sự kiện không thu phí; khung giờ mục tiêu chỉ còn đúng 1 chỗ và bị người dùng B lấy trước; người dùng A giữ nguyên dữ liệu đã nhập, số lượng 1. | Hộp thoại hiện đúng thông báo hết chỗ 「予約がいっぱいです」 (nếu môi trường trả nguyên văn khác thì ghi lại nguyên văn nhận được, không fail chỉ vì khác chữ). Sau khi đóng hộp thoại, màn hình VẪN LÀ màn xác nhận nội dung đăng ký: vẫn thấy ngày giờ, gói, số lượng và các mục thông tin đã nhập, nút 「申し込む」 đã được bật lại và nút 「戻る」 bấm được — KHÔNG được chỉ còn lại ảnh/tiêu đề sự kiện, KHÔNG được thành màn trắng, KHÔNG được nhảy sang bước nhập thẻ. Console KHÔNG có lỗi nào về việc đọc thuộc tính của giá trị rỗng ở đoạn xoá trắng ô thẻ. Sau khi bấm 「戻る」 và đổi khung giờ, khách gửi lại được ngay trong cùng phiên và đăng ký thành công; số chỗ đã dùng của khung giờ đầu không tăng thêm vì lần gửi thất bại. | Chưa test |  | all |  |  |  |  | Studio #18918 (NEW-2) · mã theo quan điểm: TC-TOOLKNOW002-01 · ui · auto · local-only · REQ: REQ-001, REQ-002 · spec: TICKET-41025, SCR-EVB-16, diff: order-item.js resetElement() (kiểm tra null từng ô thẻ), diff: order-item.js payment() (chốt if autoBill == 1), source: index.blade.php dòng 371 (v-show confirm-order) + 647 (v-show khối thẻ) · TC TÁI HIỆN CHÍNH của ticket cho luồng đăng ký, gộp cả hai lỗi: (1) TypeError do `this.cardNumber.clear()` gọi vô điều kiện khi sự kiện không có ô thẻ Stripe; (2) màn trắng do `self.page = 'payment'` chạy vô điều kiện, mà khối thẻ chỉ render khi `eventDetail.is_auto_bill == 1`. Lưu ý kỹ thuật: ở payment(), lệnh bật lại nút nằm TRƯỚC lệnh xoá ô thẻ nên trước bản vá nút vẫn được bật — dấu hiệu phân biệt duy nhất là nội dung màn hình, nên bước 5 phải quan sát nội dung chứ không chỉ trạng thái nút. Nếu không dựng được ca hết chỗ bằng hai người dùng, có thể làm khung giờ hết chỗ từ màn quản trị; ghi rõ cách dựng vào kết quả chạy. CẤM gọi thẳng endpoint đăng ký rồi kết luận đạt — hành vi cần kiểm nằm ở JavaScript phía trình duyệt. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-3 | REG-SHARED-001 | Abnormal | Sự kiện Univapay gặp lỗi ở bước thanh toán không sinh lỗi JavaScript và dùng lại được form thẻ | Môi trường chạy nhánh fix `ai_fixbug_41025`. Bot test đã cấu hình cổng thanh toán Univapay môi trường thử; sự kiện BẬT thu phí, chọn cổng Univapay, gói có giá lớn hơn 0 và bot KHÔNG ở gói không được dùng thu tiền (để nút thanh toán hiện ra). Có thẻ thử hợp lệ của Univapay. Dựng được tình trạng khung giờ hết chỗ đúng vào lúc khách bấm thanh toán (dùng hai người dùng LINE test và khung giờ còn đúng 1 chỗ). Bật sẵn console theo dõi lỗi JavaScript. | 1. Dùng người dùng A: mở đường dẫn đăng ký, chọn khung giờ còn đúng 1 chỗ và gói có giá, nhập đủ thông tin, bấm 「予約確認にすすむ」 rồi bấm nút 「申し込む」 để sang bước nhập thẻ.<br>2. Nhập thẻ thử hợp lệ của Univapay, đi tới màn xác nhận thanh toán nhưng CHƯA bấm 「決済する」.<br>3. Dùng người dùng B ở cửa sổ khác: đăng ký hoàn tất vào đúng khung giờ đó để lấy hết chỗ cuối cùng.<br>4. Quay lại cửa sổ của người dùng A, bấm 「決済する」.<br>5. Đọc nguyên văn nội dung hộp thoại hiện lên rồi đóng.<br>6. Đọc console, tìm lỗi JavaScript phát sinh kể từ lúc bấm 「決済する」.<br>7. Quan sát màn hình sau khi đóng hộp thoại: đang ở bước nào, có ô nhập thẻ Univapay dùng được không, có nút nào bấm được không.<br>8. Thử nhập lại thẻ và tiếp tục thao tác để xác nhận khách không bị kẹt; kiểm tra đơn của người dùng A và số chỗ đã dùng của khung giờ. | Sự kiện Univapay, gói có giá lớn hơn 0, số lượng 1, thẻ thử hợp lệ của Univapay; khung giờ bị lấy hết chỗ trước khi bấm 「決済する」. | Hộp thoại hiện thông báo lỗi CÓ NỘI DUNG (không phải hộp thoại trắng). Console KHÔNG có lỗi nào về việc đọc thuộc tính của giá trị rỗng ở đoạn xoá trắng ô thẻ — đây là điểm chính của bản vá, vì sự kiện Univapay không có ô thẻ Stripe. Sau khi đóng hộp thoại, màn hình đưa khách về bước nhập thẻ với khối nhập thẻ Univapay vẫn hiển thị và còn nhập được; khách vẫn còn nút để đi tiếp, KHÔNG bị trắng màn và KHÔNG phải đóng mở lại trang. Không tạo đơn thành công cho người dùng A và số chỗ đã dùng của khung giờ chỉ phản ánh đơn của người dùng B. | Chưa test |  | all |  |  |  |  | Studio #18919 (NEW-3) · mã theo quan điểm: TC-REGSHARED001-01 · ui · auto · local-only · REQ: REQ-001, REQ-005 · spec: TICKET-41025, SCR-EVB-16, diff: order-item.js resetElement(), source: index.blade.php dòng 696 (khối thẻ Univapay type_system_bill == 2) · Điểm cần chú ý: sự kiện Univapay là `is_auto_bill = 1` nhưng `type_system_bill = 2`, mà khối <script> Stripe chỉ được nạp khi `type_system_bill == 1` ⇒ ba biến ô thẻ vẫn rỗng và trước bản vá vẫn ném TypeError dù sự kiện CÓ thu tiền. Khác với sự kiện miễn phí, ở đây `autoBill == 1` nên bước thẻ VẪN được hiển thị đúng như trước — bản vá chỉ bỏ lỗi JavaScript, không đổi màn. Khối thẻ dùng v-show nên form Univapay đã dựng trước đó không bị huỷ; đây chính là phần Dev đề nghị QA bấm thử tay. Nếu môi trường không có thông tin xác thực Univapay thử hợp lệ thì báo skip kèm lý do môi trường, KHÔNG thay bằng gọi thẳng endpoint rồi kết luận đạt. · author=AI · status=draft |
| NEW-4 | RULE-TOOL-029 | Abnormal | Sự kiện Stripe gặp lỗi thanh toán vẫn quay về bước nhập thẻ và ba ô thẻ được xoá trắng | Môi trường chạy nhánh fix `ai_fixbug_41025`. Bot test đã cấu hình cổng Stripe môi trường thử; sự kiện BẬT thu phí, chọn cổng Stripe, gói có giá lớn hơn 0, bot không ở gói bị chặn thu tiền. Có thẻ thử hợp lệ của Stripe. Dựng được tình trạng khung giờ hết chỗ đúng lúc khách bấm thanh toán. Bật sẵn console. | 1. Mở đường dẫn đăng ký, chọn khung giờ còn đúng 1 chỗ và gói có giá, nhập đủ thông tin rồi bấm 「予約確認にすすむ」 và 「申し込む」 để sang bước nhập thẻ.<br>2. Nhập đầy đủ số thẻ, hạn dùng và mã bảo mật của thẻ thử hợp lệ; ghi lại rằng ba ô đã có dữ liệu.<br>3. Đi tới màn xác nhận thanh toán, để người dùng khác lấy hết chỗ cuối cùng của khung giờ, rồi bấm 「決済する」.<br>4. Đọc nội dung hộp thoại hiện lên rồi đóng.<br>5. Quan sát màn hình: có quay về bước nhập thẻ 「カード情報入力」 hay không.<br>6. Kiểm tra ba ô số thẻ, hạn dùng, mã bảo mật: còn giữ dữ liệu cũ hay đã bị xoá trắng.<br>7. Nhập lại thẻ và xác nhận vẫn gửi lại được; đọc console tìm lỗi JavaScript. | Sự kiện Stripe, gói có giá lớn hơn 0, số lượng 1, thẻ thử hợp lệ của Stripe đã nhập đủ ba ô; khung giờ bị lấy hết chỗ trước khi bấm 「決済する」. | Hộp thoại hiện thông báo lỗi có nội dung. Sau khi đóng, màn hình quay về bước nhập thẻ 「カード情報入力」 đúng như hành vi trước bản vá. Cả BA ô số thẻ, hạn dùng và mã bảo mật đều đã được xoá trắng — bản vá thêm kiểm tra rỗng nhưng KHÔNG được làm mất tác dụng xoá trắng khi ô thẻ thực sự tồn tại. Nút thanh toán được bật lại, khách nhập thẻ lại và gửi lại được. Console không có lỗi JavaScript. | Chưa test |  | all |  |  |  |  | Studio #18920 (NEW-4) · mã theo quan điểm: TC-RULETOOL029-01 · ui · auto · local-only · REQ: REQ-004 · spec: TICKET-41025, SCR-EVB-16, diff: order-item.js resetElement(), diff: order-item.js payment() (chốt if autoBill == 1) · Hồi quy theo RULE-TOOL-029: Dev khẳng định nhánh thẻ Stripe 'giữ nguyên hành vi', nhưng bản vá sửa đúng hàm dùng chung nên vẫn phải đo. Trọng tâm khác với case kho EB-023 (case đó kiểm NỘI DUNG hộp thoại của ticket #39507): ở đây kiểm bước hiển thị sau lỗi và việc ba ô thẻ có thật sự bị xoá trắng. Kỹ thuật: `autoBill == 1` nên `self.page = 'payment'` vẫn chạy, và ba biến ô thẻ tồn tại nên cả ba lệnh xoá đều phải thực thi. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-5 | RULE-TOOL-029 | Abnormal | Xác thực 3D-Secure thất bại vẫn quay về bước nhập thẻ và xoá trắng ô thẻ | Môi trường chạy nhánh fix `ai_fixbug_41025`. Sự kiện bật thu phí qua cổng Stripe môi trường thử, gói có giá lớn hơn 0, khung giờ còn chỗ. Có thẻ thử của Stripe bắt buộc xác thực bổ sung và xác thực đó có thể cho THẤT BẠI (thẻ thử 3D-Secure loại từ chối xác thực). Bật sẵn console. | 1. Mở đường dẫn đăng ký, chọn khung giờ còn chỗ và gói có giá, nhập đủ thông tin rồi đi tới bước nhập thẻ.<br>2. Nhập thẻ thử bắt buộc xác thực bổ sung, đi tới màn xác nhận thanh toán và bấm 「決済する」.<br>3. Khi màn xác thực bổ sung của ngân hàng hiện lên, chọn phương án làm xác thực THẤT BẠI.<br>4. Đọc nội dung hộp thoại hiện lên rồi đóng.<br>5. Quan sát bước hiện tại của màn hình và trạng thái ba ô thẻ.<br>6. Đọc console tìm lỗi JavaScript; kiểm tra không có đơn nào được tạo thành công và số chỗ đã dùng không tăng. | Thẻ thử Stripe bắt buộc xác thực bổ sung, chọn kết quả xác thực thất bại; gói có giá lớn hơn 0, số lượng 1. | Hộp thoại hiện đúng nội dung lỗi do cổng trả về (không phải hộp thoại trắng). Màn hình quay về bước nhập thẻ 「カード情報入力」 và ba ô thẻ được xoá trắng; nút thanh toán bật lại để khách nhập thẻ khác. Console không có lỗi JavaScript. Không có đơn thành công và số chỗ đã dùng của khung giờ không tăng. | Chưa test |  | all |  |  |  |  | Studio #18921 (NEW-5) · mã theo quan điểm: TC-RULETOOL029-02 · ui · auto · local-only · REQ: REQ-004 · spec: TICKET-41025, SCR-EVB-16, source: order-item.js nhánh xác nhận thẻ 3D-Secure (4 điểm xoá ô thẻ không đổi), redmine: journal ■ TỰ REVIEW (4 điểm reset 3D-Secure không dùng tới) · Đây là 'phần Dev nói không dùng tới': bốn điểm xoá ô thẻ trong nhánh xác nhận thẻ 3D-Secure vẫn để VÔ ĐIỀU KIỆN (`eventBooking.resetElement(); self.page = 'payment';`), không có chốt autoBill. Về lý thuyết chúng chỉ chạy khi cổng là Stripe nên ô thẻ đã tồn tại, nhưng theo RULE-TOOL-029 vẫn phải có case đo thay vì tin lời khẳng định. Nếu môi trường thử không có thẻ 3D-Secure loại từ chối được, báo skip kèm lý do môi trường cụ thể. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-6 | FUNC-004 | Boundary | Sự kiện bật thu tiền nhưng khách không đi qua bước thẻ vẫn giữ màn xác nhận khi lỗi | Môi trường chạy nhánh fix `ai_fixbug_41025`. Cần dựng được hai biến thể: biến thể A là sự kiện BẬT thu phí qua cổng thẻ nhưng gói được chọn có giá 0 đồng; biến thể B là sự kiện BẬT thu phí qua cổng thẻ trong khi bot đang ở gói hợp đồng không được dùng tính năng thu tiền. Mỗi biến thể có một khung giờ dựng được tình trạng hết chỗ đúng lúc khách bấm gửi. Bật sẵn console. Đã ghi lại số chỗ đã dùng của từng khung giờ. | 1. Biến thể A: cấu hình sự kiện bật thu phí qua cổng thẻ, đặt giá gói của khung giờ cần test là 0 đồng rồi lưu.<br>2. Mở đường dẫn đăng ký, chọn khung giờ đó và gói 0 đồng, nhập đủ thông tin rồi bấm 「予約確認にすすむ」.<br>3. Ghi lại nút gửi đang hiện trên màn xác nhận là nút đi thẳng tới gửi đăng ký hay nút mở bước nhập thẻ.<br>4. Làm khung giờ hết chỗ, rồi bấm nút gửi trên màn xác nhận.<br>5. Đóng hộp thoại lỗi và quan sát: màn hình còn là màn xác nhận với nội dung và nút bấm được, hay đã chuyển sang bước nhập thẻ.<br>6. Đọc console tìm lỗi JavaScript rồi thử bấm gửi lại sau khi đổi sang khung giờ còn chỗ.<br>7. Biến thể B: đưa bot test về gói hợp đồng không được dùng thu tiền, giữ sự kiện bật thu phí với gói có giá lớn hơn 0, rồi lặp lại toàn bộ các bước trên.<br>8. Đối chiếu kết quả hai biến thể và khôi phục lại cấu hình gói/giá ban đầu. | Biến thể A: sự kiện bật thu phí + gói giá 0 đồng. Biến thể B: sự kiện bật thu phí + gói giá lớn hơn 0 + bot ở gói không được dùng thu tiền. Cả hai biến thể: số lượng 1, khung giờ bị lấy hết chỗ trước khi bấm gửi. | Ở CẢ HAI biến thể, màn xác nhận hiện nút gửi đăng ký trực tiếp (không phải nút mở bước nhập thẻ), nghĩa là khách chưa từng đi qua bước nhập thẻ. Sau khi gặp lỗi và đóng hộp thoại, màn hình VẪN LÀ màn xác nhận với đầy đủ nội dung đã nhập, nút gửi được bật lại và nút 「戻る」 bấm được — KHÔNG được nhảy sang bước nhập thẻ. Console không có lỗi JavaScript. Sau khi đổi sang khung giờ còn chỗ, khách gửi lại được ngay trong cùng phiên. | Chưa test |  | all |  |  |  |  | Studio #18922 (NEW-6) · mã theo quan điểm: TC-FUNC004-01 · ui · auto · local-only · REQ: REQ-016, REQ-002 · spec: TICKET-41025, SCR-EVB-16, source: index.blade.php dòng 444-451 (điều kiện order.plan_price > 0 && contractType != 'free'), diff: order-item.js payment() (dùng tham số autoBill thay cho cờ is_auto_bill), knowledge: LIM-021 · Đây là ca biên mà chính Dev nêu ra khi giải thích vì sao dùng tham số `autoBill` của nút vừa bấm thay vì cờ `is_auto_bill` của sự kiện: hai biến thể này có `is_auto_bill == 1` nhưng điều kiện hiển thị nút thẻ (`order.plan_price > 0 && contractType != 'free'`) không thoả nên khách rơi vào nhánh `autoBill == 0`. Biến thể B là hệ quả của LIM-021 (gói Free không được dùng tính năng bill tiền) — dùng làm dữ liệu dựng ca biên, KHÔNG phải case kiểm đếm giới hạn gói. Gộp hai biến thể trong một TC vì cùng một failure mode và cùng một dòng mã. · author=AI · status=draft |
| #18924 | FUNC-003 | Abnormal | Kiểm tra thiếu dữ liệu trước khi sang màn xác nhận vẫn báo đúng và không bị chặn bởi ghi vết mới | Sự kiện bật 利用規約 và có nhiều ngày/khung giờ/gói để tạo các thiếu sót độc lập.<br>Trang đủ dài để quan sát vị trí tự cuộn tới lỗi. | 1. Biến thể A: nhập đủ mọi dữ liệu nhưng không tick 利用規約; bấm 「予約確認にすすむ」.<br>2. Biến thể B: thiếu ngày/giờ; bấm lại.<br>3. Biến thể C: chọn ngày/giờ nhưng thiếu gói; bấm lại.<br>4. Biến thể D: nhập đầy đủ tất cả dữ liệu và tick điều khoản; bấm 「予約確認にすすむ」.<br>5. Ở mỗi biến thể ghi lại nội dung cảnh báo, vị trí cuộn và page hiện tại. | A: thiếu tick điều khoản.<br>B: thiếu ngày/giờ.<br>C: thiếu gói.<br>D: đầy đủ hợp lệ. | A: hiện 「利用規約に同意をしてください」 và tự cuộn tới vị trí lỗi, vẫn ở bước nhập.<br>B/C: hiện cảnh báo 「予約日時を選択してください。」 hoặc cảnh báo tương ứng với mục còn thiếu, cuộn tới đúng vùng và không chuyển bước.<br>D: chuyển sang page confirm-order và cuộn về đầu, không còn cảnh báo thừa 「予約日時を選択してください。」 sau khi chuyển bước thành công. | Chưa test |  | all |  |  |  |  | Studio #18924 · mã theo quan điểm: TC-FUNC003-01 · ui · auto · env-safe · REQ: REQ-013 · spec: TICKET-40806, TICKET-41025, source: order-item.js:565-580 (validation đầu hàm confirmOrder), diff: order-item.js confirmOrder (thêm điểm ghi vết tap:confirm-order ở đầu hàm) · TÁI DÙNG NGUYÊN case kho. Lý do đưa vào phạm vi #41025: bản vá chèn một lệnh ghi vết ngay DÒNG ĐẦU của hàm xác nhận này; nếu lệnh đó ném lỗi thì toàn bộ phần kiểm tra thiếu dữ liệu và việc chuyển bước sẽ chết. Chạy nguyên case kho là đủ để bắt hồi quy đó, không cần viết case mới. · author=AI · status=draft |
| #18933 | UI-003 | Abnormal | Lấy thông tin khách trả về mã lỗi thật thì báo ngay, không thử lại và bật lại nút đi tiếp | Có cách làm cho yêu cầu lấy thông tin khách hàng thất bại từ phía trình duyệt: ép địa chỉ lấy thông tin friend trả về mã lỗi 500 (có phản hồi HTTP thật, khác với ca bị huỷ ở tầng vận chuyển). Ghi rõ cách mô phỏng đã dùng vào kết quả chạy. Bật sẵn bảng theo dõi request mạng để đếm số lần gọi. | 1. Bật cách ép yêu cầu lấy thông tin khách hàng của trang đăng ký trả về mã lỗi 500.<br>2. Mở đường dẫn đăng ký sự kiện trên trình duyệt thật.<br>3. Chờ trang nạp xong và đếm số lần trang gọi lấy thông tin khách trên bảng request mạng.<br>4. Quan sát hộp thoại thông báo hiện lên và ghi lại thời điểm xuất hiện.<br>5. Đóng hộp thoại rồi kiểm tra nút 「予約確認にすすむ」 có còn bị mờ và không bấm được hay không.<br>6. Bỏ chặn, tải lại trang và kiểm tra luồng đăng ký hoạt động bình thường trở lại. | Yêu cầu lấy thông tin khách hàng trả về mã lỗi 500 (có phản hồi HTTP thật). | Trang gọi lấy thông tin khách ĐÚNG MỘT LẦN — KHÔNG được thử lại, vì cơ chế thử lại chỉ dành cho ca không nhận được phản hồi nào; lỗi có mã phản hồi thật là lỗi phía server nên gọi lại không giúp gì và chỉ làm khách chờ thêm.<br>Hộp thoại hiện NGAY (không chờ thêm khoảng nghỉ) với nội dung 「情報の取得に失敗しました。通信環境をご確認のうえ、もう一度お試しください。」.<br>Nút 「予約確認にすすむ」 phải được bật lại, không bị disabled vĩnh viễn.<br>Sau khi bỏ điều kiện lỗi và tải lại, luồng đăng ký hoạt động bình thường. | Chưa test |  | all |  |  |  |  | Studio #18933 · mã theo quan điểm: TC-UI003-01 · ui · auto · env-safe · REQ: REQ-007 · spec: TICKET-40806, TICKET-41025, diff: order-item.js (nhánh thất bại của getFriendInfo — điều kiện thử lại chỉ với status === 0), source: EP-F4 POST /mobile/booking-event/get-friend-info · CHỈNH CASE KHO THEO SPEC MỚI (giữ khoOrigin để leader duyệt sync ngược đúng dòng gốc). Lý do chỉnh: bản vá #41025 thêm cơ chế thử lại vào chính nhánh thất bại này, nên case cũ (chỉ kiểm thông báo và nút được bật lại) không còn phân biệt được hai ca hoàn toàn khác nhau. Đã bổ sung: cố định cách mô phỏng là mã lỗi 500 (loại bỏ phương án 'chặn đường gọi' của case cũ vì nó thuộc ca status 0 và có hành vi khác), thêm oracle đếm ĐÚNG MỘT lần gọi, và thêm oracle về thời điểm hiện hộp thoại. Ca status 0 được phủ bằng hai case riêng trong cùng màn này. · author=AI · status=draft |
| NEW-7 | TOOL-NEGCTRL-001 | Normal | Đổi lịch sự kiện miễn phí hoàn tất bình thường sau bản vá | Môi trường chạy nhánh fix `ai_fixbug_41025`. Có sự kiện đặt chỗ KHÔNG bật thu phí, cho phép khách tự đổi lịch. Người dùng LINE test đã kết bạn và ĐANG CÓ một đơn đặt chỗ hợp lệ ở khung giờ S1. Có khung giờ S2 khác còn ít nhất 1 chỗ. Đã ghi lại số chỗ đã dùng của cả S1 và S2. Mở màn ở khung nhìn điện thoại. | 1. Ghi lại số chỗ đã dùng của khung giờ S1 và S2.<br>2. Mở đường dẫn đăng ký sự kiện, vào mục 「予約履歴」 và mở đơn hiện có.<br>3. Chọn chức năng đổi lịch, chọn khung giờ S2 còn chỗ, giữ nguyên số lượng rồi đi tới màn xác nhận thay đổi.<br>4. Bấm nút 「変更内容を送信」 đúng một lần rồi chờ xử lý xong.<br>5. Kiểm tra màn hình kết quả, đơn của người dùng test trong 「予約履歴」 và trên màn quản trị.<br>6. Kiểm tra số chỗ đã dùng của S1 và S2. | Đơn hiện có ở khung giờ S1, đổi sang khung giờ S2 còn chỗ, giữ nguyên số lượng. Phép tính tay: S1 giảm đúng theo số lượng của đơn, S2 tăng đúng theo số lượng đó. | Không có hộp thoại lỗi nào hiện lên; màn hình đi tới trạng thái hoàn tất theo cấu hình của sự kiện. Đơn của người dùng test được cập nhật sang khung giờ S2, không sinh thêm đơn mới. Số chỗ đã dùng của S1 giảm và của S2 tăng đúng theo số lượng của đơn. Console không có lỗi JavaScript. | Chưa test |  | all |  |  |  |  | Studio #18925 (NEW-7) · mã theo quan điểm: TC-TOOLNEGCTRL001-02 · ui · auto · local-only · REQ: REQ-003 · spec: TICKET-41025, SCR-EVB-16, diff: order-item.js changeBooking() (chốt if autoBill == 1) · Đối chứng âm cho chốt mới `if (autoBill == 1)` trong changeBooking(): chốt chỉ được chặn nhánh BÁO LỖI, không được chặn đường đổi lịch thành công. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-8 | TOOL-KNOW-002 | Abnormal | Đổi lịch sự kiện miễn phí gặp lỗi hết chỗ vẫn giữ màn xác nhận thay đổi và gửi lại ngay được | Môi trường chạy nhánh fix `ai_fixbug_41025`. Có sự kiện đặt chỗ KHÔNG bật thu phí, cho phép khách tự đổi lịch. Người dùng LINE test A đã kết bạn và ĐANG CÓ một đơn hợp lệ ở khung giờ S1. Có khung giờ S2 chỉ còn ĐÚNG 1 chỗ và một người dùng test B khác để lấy nốt chỗ đó. Có thêm khung giờ S3 còn chỗ để thử gửi lại. Bật sẵn console theo dõi lỗi JavaScript. Đã ghi lại số chỗ đã dùng của S1, S2, S3. | 1. Dùng người dùng A: mở 「予約履歴」, mở đơn hiện có ở S1, chọn đổi lịch sang khung giờ S2 rồi đi tới màn xác nhận thay đổi 「変更内容の最終確認をしてください」, NHƯNG CHƯA bấm gửi.<br>2. Dùng người dùng B ở cửa sổ khác: đặt chỗ hoàn tất vào S2 để lấy nốt chỗ cuối cùng.<br>3. Quay lại cửa sổ của người dùng A, bấm 「変更内容を送信」.<br>4. Đọc nguyên văn nội dung hộp thoại hiện lên rồi đóng.<br>5. KHÔNG tải lại trang và KHÔNG đóng mở lại đường dẫn. Quan sát ngay màn hình: còn thấy khối xác nhận thay đổi (dòng 「まだ変更は完了していません」, nội dung thay đổi, nút 「変更内容を送信」 và nút 「変更内容を修正」) hay không.<br>6. Đọc console tìm lỗi JavaScript phát sinh kể từ lúc bấm gửi.<br>7. Bấm 「変更内容を修正」 để về bước chọn, đổi sang khung giờ S3 còn chỗ rồi gửi lại 「変更内容を送信」.<br>8. Kiểm tra kết quả lần gửi lại, đơn của người dùng A và số chỗ đã dùng của S1, S2, S3. | Sự kiện không thu phí; đơn của A đang ở S1; S2 chỉ còn 1 chỗ và bị B lấy trước khi A bấm gửi; S3 còn chỗ dùng cho lần gửi lại. | Hộp thoại hiện thông báo hết chỗ 「予約がいっぱいです」 (nếu môi trường trả nguyên văn khác thì ghi lại nguyên văn nhận được). Sau khi đóng hộp thoại, khối xác nhận thay đổi VẪN CÒN TRÊN MÀN HÌNH với đầy đủ nội dung thay đổi; nút 「変更内容を送信」 đã được bật lại và nút 「変更内容を修正」 bấm được — KHÔNG được biến thành màn trắng, KHÔNG được nhảy sang bước nhập thẻ, và khách KHÔNG phải đóng rồi mở lại đường dẫn mới thao tác tiếp được. Console KHÔNG có lỗi nào về việc đọc thuộc tính của giá trị rỗng ở đoạn xoá trắng ô thẻ. Sau khi bấm 「変更内容を修正」 và đổi sang S3, khách gửi lại được ngay trong cùng phiên và đổi lịch thành công. Đơn của A chỉ bị đổi đúng một lần sang S3; số chỗ đã dùng của S2 chỉ phản ánh đơn của B. | Chưa test |  | all |  |  |  |  | Studio #18926 (NEW-8) · mã theo quan điểm: TC-TOOLKNOW002-02 · ui · auto · local-only · REQ: REQ-001, REQ-003 · spec: TICKET-41025, SCR-EVB-16, diff: order-item.js resetElement() (kiểm tra null từng ô thẻ), diff: order-item.js changeBooking() (chốt if autoBill == 1), source: index.blade.php dòng 454 (v-if confirm-change-order) + 647 (v-show khối thẻ) · TC TÁI HIỆN CHÍNH cho HỒI QUY của vòng fix trước, và là nhánh NẶNG NHẤT của ticket. Khác biệt cốt lõi so với luồng đăng ký: khối xác nhận đổi lịch dùng `v-if="page == 'confirm-change-order'"` nên khi đổi page nó bị HUỶ khỏi DOM (luồng đăng ký dùng v-show, chỉ bị ẩn) ⇒ mất hẳn, phải mở lại trang. Trong changeBooking(), lệnh xoá ô thẻ đứng TRƯỚC lệnh đổi bước nên trước vòng fix 1 dòng đổi bước chưa từng chạy; vá lỗi xoá ô thẻ đã 'mở khoá' nó ⇒ đây đúng là hồi quy do vòng fix 1 tạo ra. Bước 5 và 7 là oracle bắt buộc: chỉ kiểm nút được bật lại là KHÔNG đủ, vì lệnh bật nút nằm trước lệnh xoá ô thẻ nên vẫn luôn chạy. CẤM gọi thẳng endpoint đổi lịch rồi kết luận đạt. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-9 | RULE-TOOL-028 | Abnormal | Đổi lịch sự kiện Stripe gặp lỗi vẫn quay về bước nhập thẻ và ba ô thẻ được xoá trắng | Môi trường chạy nhánh fix `ai_fixbug_41025`. Bot test cấu hình cổng Stripe môi trường thử; sự kiện BẬT thu phí qua Stripe, gói có giá lớn hơn 0, bot không ở gói bị chặn thu tiền, cho phép khách tự đổi lịch. Người dùng LINE test đang có một đơn hợp lệ. Có thẻ thử hợp lệ của Stripe và thẻ thử bắt buộc xác thực bổ sung có thể cho thất bại. Dựng được tình trạng khung giờ đích hết chỗ đúng lúc khách bấm gửi. Bật sẵn console. | 1. Biến thể A — lỗi nghiệp vụ: mở đơn hiện có, chọn đổi sang khung giờ đích, đi tới bước nhập thẻ, nhập đủ ba ô của thẻ thử hợp lệ rồi sang màn xác nhận thanh toán.<br>2. Làm khung giờ đích hết chỗ, rồi bấm 「決済する」.<br>3. Đóng hộp thoại lỗi, quan sát màn hình có quay về bước nhập thẻ 「カード情報入力」 và kiểm tra ba ô số thẻ, hạn dùng, mã bảo mật đã bị xoá trắng chưa.<br>4. Nhập lại thẻ, xác nhận gửi lại được; đọc console tìm lỗi JavaScript.<br>5. Biến thể B — xác thực bổ sung thất bại: lặp lại luồng đổi lịch với thẻ thử bắt buộc xác thực bổ sung và chọn phương án làm xác thực THẤT BẠI.<br>6. Đóng hộp thoại, quan sát bước hiện tại và trạng thái ba ô thẻ; đọc console.<br>7. Kiểm tra ở cả hai biến thể: đơn không bị đổi và số chỗ đã dùng của các khung giờ không thay đổi. | Biến thể A: thẻ thử Stripe hợp lệ, khung giờ đích bị lấy hết chỗ trước khi bấm gửi. Biến thể B: thẻ thử Stripe bắt buộc xác thực bổ sung, chọn kết quả xác thực thất bại. | Ở cả hai biến thể: hộp thoại hiện thông báo lỗi CÓ NỘI DUNG; sau khi đóng, màn hình quay về bước nhập thẻ 「カード情報入力」 đúng như hành vi trước bản vá; cả BA ô số thẻ, hạn dùng và mã bảo mật đều được xoá trắng; nút gửi được bật lại và khách nhập thẻ lại rồi gửi lại được. Console không có lỗi JavaScript. Đơn không bị đổi và số chỗ đã dùng của các khung giờ giữ nguyên. | Chưa test |  | all |  |  |  |  | Studio #18928 (NEW-9) · mã theo quan điểm: TC-RULETOOL028-01 · ui · auto · local-only · REQ: REQ-004 · spec: TICKET-41025, SCR-EVB-16, diff: order-item.js changeBooking() (chốt if autoBill == 1), source: order-item.js nhánh xác nhận thẻ 3D-Secure của luồng đổi lịch (2 điểm xoá ô thẻ không đổi) · Phủ đều hai màn theo RULE-TOOL-028: cùng hành vi 'sau lỗi quay về bước thẻ và xoá trắng ô thẻ' đã kiểm ở màn đăng ký thì phải kiểm cả ở màn đổi lịch, vì resetElement() là hàm dùng chung. Biến thể B là hai điểm xoá ô thẻ của nhánh xác nhận thẻ 3D-Secure trong luồng đổi lịch — vẫn để VÔ ĐIỀU KIỆN, không có chốt autoBill; Dev nói 'không dùng tới' nhưng theo RULE-TOOL-029 vẫn đo. Nếu môi trường thử không có thẻ 3D-Secure loại từ chối được thì chạy biến thể A và báo skip riêng biến thể B kèm lý do môi trường. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| #18927 | REG-SPEC-001 | Abnormal | Đổi lịch sự kiện Univapay gặp lỗi vượt hạn mức chuyển về bước nhập thẻ và dùng lại được | Event đặt chỗ bật cổng univapay, bật chế độ chờ webhook. Môi trường chạy nhánh fix `ai_fixbug_41025`. Bật sẵn console để theo dõi lỗi JavaScript. | 1. Mở màn change booking của một đơn đang có.<br>2. Đi tới bước nhập thẻ, nhập test card hợp lệ rồi sang màn xác nhận thanh toán.<br>3. Bấm 「決済する」 để gửi thay đổi.<br>4. Đọc nguyên văn nội dung hộp thoại hiện lên rồi đóng.<br>5. Đọc console, tìm lỗi JavaScript phát sinh kể từ lúc bấm 「決済する」.<br>6. Quan sát màn hình sau khi đóng hộp thoại: đang ở bước nhập thẻ 「カード情報入力」 hay còn ở màn xác nhận thanh toán với nút 「決済する」.<br>7. Thử nhập lại thẻ và tiếp tục thao tác để xác nhận khách không bị kẹt; kiểm tra đơn có bị đổi hay không. | Giá plan > hạn mức univapay môi trường thử; test card hợp lệ của Univapay. | Hộp thoại hiện đúng nội dung 「課金金額が課金最大額より超過しています。」 — KHÔNG phải hộp thoại trắng, không phải chuỗi rỗng, không phải câu chung chung. Console KHÔNG có lỗi nào về việc đọc thuộc tính của giá trị rỗng ở đoạn xoá trắng ô thẻ (sự kiện Univapay không có ô thẻ Stripe nên trước bản vá luôn ném lỗi tại đây). Sau khi đóng hộp thoại, màn hình chuyển về BƯỚC NHẬP THẺ 「カード情報入力」 với khối thẻ Univapay vẫn dùng được — lưu ý đây là điểm ĐỔI so với hành vi khách đang thấy trước bản vá (trước đây lỗi JavaScript giữ khách lại ở màn xác nhận thanh toán với nút 「決済する」). Khách nhập lại thẻ và gửi lại được, không bị trắng màn, không phải đóng mở lại trang. Bản ghi đặt chỗ không bị đổi. | Chưa test |  | all |  |  |  |  | Studio #18927 · mã theo quan điểm: TC-REGSPEC001-01 · ui · auto · local-only · REQ: REQ-001, REQ-005 · spec: TICKET-41025, TICKET-39507, SCR-EVB-16, diff: order-item.js changeBooking() (resetElement đứng trước chốt if autoBill == 1), source: index.blade.php dòng 647 + 670-760 (khối nhập thẻ và khối xác nhận thanh toán) · CHỈNH CASE KHO THEO SPEC MỚI (giữ khoOrigin để leader duyệt sync ngược đúng dòng gốc). Lý do chỉnh: kết quả mong đợi cũ ghi 'màn hình quay lại bước thanh toán và nút 「決済する」 bấm lại được' — sau bản vá #41025 câu này KHÔNG còn đúng. Univapay là `is_auto_bill = 1` + `type_system_bill = 2` nên ba ô thẻ Stripe rỗng; trong changeBooking() lệnh xoá ô thẻ đứng TRƯỚC `self.page = 'payment'` nên trước bản vá lỗi JavaScript giữ khách ở màn 'confirm-payment' (nơi có nút 「決済する」). Sau bản vá lệnh xoá chạy qua và `autoBill == 1` nên page đổi sang 'payment' = màn nhập thẻ, còn nút 「決済する」 thuộc màn đã bị ẩn. Đã bổ sung thêm bước đọc console (oracle cho lỗi JavaScript) mà case cũ không có. Nếu môi trường không có thông tin xác thực Univapay thử hợp lệ thì báo skip kèm lý do môi trường, KHÔNG thay bằng gọi thẳng endpoint. · author=AI · status=draft |
| NEW-10 | TOOL-NEGCTRL-001 | Normal | Mở màn đăng ký bình thường điền sẵn thông tin khách và bật nút đi tiếp | Môi trường chạy nhánh fix `ai_fixbug_41025`. Có sự kiện đặt chỗ đang mở với ít nhất 3 mục thông tin khách được bật hiển thị, trong đó có ít nhất 1 mục mà người dùng test ĐÃ có dữ liệu sẵn (ví dụ tên hoặc email). Người dùng LINE test đã kết bạn, chưa bị chặn, và có đường dẫn đăng ký kèm mã người dùng. Mở màn bằng trình duyệt ở khung nhìn điện thoại, bật sẵn console và bảng theo dõi request mạng. | 1. Mở đường dẫn đăng ký sự kiện trên trình duyệt ở khung nhìn điện thoại.<br>2. Chờ trang nạp xong, quan sát bảng theo dõi request mạng và đếm số lần trang gọi lấy thông tin khách.<br>3. Quan sát các mục thông tin khách trên form: mục nào đã có dữ liệu sẵn thì đã được điền chưa.<br>4. Kiểm tra nút 「予約確認にすすむ」 có bật và bấm được không.<br>5. Đọc console tìm lỗi JavaScript và kiểm tra không có hộp thoại lỗi nào hiện lên.<br>6. Chọn ngày, khung giờ, gói rồi bấm 「予約確認にすすむ」 để xác nhận đi tiếp được. | Người dùng LINE test đã kết bạn, có sẵn dữ liệu ở ít nhất 1 mục thông tin khách; sự kiện đang mở và còn khung giờ. | Trang gọi lấy thông tin khách ĐÚNG MỘT LẦN (không có lần gọi lại nào vì lần đầu đã thành công). Các mục thông tin khách mà người dùng đã có dữ liệu được điền sẵn đúng giá trị; danh sách khung giờ hiển thị. Nút 「予約確認にすすむ」 được bật và bấm được; không có hộp thoại lỗi nào và console không có lỗi JavaScript. Bấm nút đi tiếp được sang màn xác nhận. | Chưa test |  | all |  |  |  |  | Studio #18930 (NEW-10) · mã theo quan điểm: TC-TOOLNEGCTRL001-03 · ui · auto · env-safe · REQ: REQ-006 · spec: TICKET-41025, SCR-EVB-16, EP-F4, diff: order-item.js getFriendInfo (ghi vết friend-info:send / friend-info:ok) · Đối chứng âm cho cơ chế thử lại: chỉ được gọi lại khi request THẤT BẠI ở tầng vận chuyển, không được gọi lại ở đường bình thường (nếu không sẽ tăng gấp đôi tải endpoint và nhân đôi số dòng log mới). Kỹ thuật: điểm ghi vết friend-info:send phải xuất hiện đúng 1 lần với retry=0. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-14 | OBS-001 | Normal | Vết thao tác của khách được gửi kèm theo đúng thứ tự khi báo lỗi | Môi trường chạy nhánh fix `ai_fixbug_41025`. Sự kiện đang mở, người dùng test đã kết bạn. Có cách bắt và đọc nội dung request mà trang gửi tới địa chỉ báo lỗi JavaScript. Có cách làm phát sinh một lỗi được báo về (ví dụ chặn request lấy thông tin khách ở tầng vận chuyển) SAU KHI khách đã thực hiện một chuỗi thao tác. | 1. Mở đường dẫn đăng ký sự kiện và để trang nạp xong bình thường.<br>2. Thực hiện một chuỗi thao tác xác định: chọn khung giờ, nhập đủ thông tin, bấm 「予約確認にすすむ」 để sang màn xác nhận, rồi bấm 「戻る」 về bước nhập.<br>3. Bật cách chặn để lần gọi lấy thông tin khách tiếp theo chết ở tầng vận chuyển.<br>4. Kích hoạt lại việc lấy thông tin khách để trang phát sinh báo lỗi.<br>5. Bắt nội dung request gửi tới địa chỉ báo lỗi JavaScript và đọc phần vết thao tác trong đó.<br>6. Đối chiếu dãy thao tác ghi được với chuỗi thao tác đã thực hiện ở bước 2 về nội dung và thứ tự.<br>7. Thực hiện thêm hơn 40 thao tác bấm/chọn liên tiếp rồi lặp lại bước phát sinh lỗi, và đếm số bước trong vết thao tác gửi kèm. | Chuỗi thao tác xác định trước: chọn khung giờ → sang màn xác nhận → quay lại bước nhập. Lần thử thứ hai: hơn 40 thao tác liên tiếp trước khi phát sinh lỗi. | Request báo lỗi mang kèm phần vết thao tác, trong đó có các mốc tương ứng chuỗi thao tác đã thực hiện (chọn khung giờ, xác nhận nội dung, gửi/khởi tạo lấy thông tin khách) theo ĐÚNG THỨ TỰ thời gian, mỗi mốc kèm mốc thời gian. Ở lần thử thứ hai, vết thao tác chỉ giữ TỐI ĐA 40 bước và là 40 bước GẦN NHẤT (các thao tác cũ nhất bị loại bỏ, không phải các thao tác mới nhất). Việc ghi vết không làm phát sinh thêm request nào tới địa chỉ báo lỗi ngoài các request báo lỗi thật, và không gây lỗi JavaScript. | Chưa test |  | all |  |  |  |  | Studio #18936 (NEW-14) · mã theo quan điểm: TC-OBS001-01 · ui · auto · env-safe · REQ: REQ-012 · spec: TICKET-41025, SCR-EVB-16, diff: index.blade.php (bộ đệm vết thao tác, giữ 40 bước gần nhất, gửi kèm khi báo lỗi), diff: order-item.js (10 điểm ghi vết trên các hàm thao tác chính) · Điểm quan trọng về thiết kế cần bảo vệ: vết thao tác được giữ trong bộ nhớ trang rồi gửi KÈM lúc báo lỗi, KHÔNG bắn từng bước một — vì endpoint báo lỗi bị giới hạn 10 request mỗi phút theo IP, bắn từng thao tác thì khách thao tác hơi nhanh là bị chặn và mất luôn báo lỗi thật. Bước 7 kiểm đúng chiều 'giữ bước GẦN NHẤT' của bộ đệm: giữ sai chiều thì lúc lỗi lại chỉ còn các bước đầu phiên, vô dụng cho việc truy nguyên. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-11 | INTG-LINE-001 | Abnormal | Request lấy thông tin khách chết ở tầng vận chuyển lần đầu thì tự thử lại và khách không thấy lỗi | Môi trường chạy nhánh fix `ai_fixbug_41025`. Cùng sự kiện và người dùng test như case mở màn bình thường. Có cách làm request lấy thông tin khách CHẾT Ở TẦNG VẬN CHUYỂN (không nhận được phản hồi nào, không có mã lỗi HTTP) — ví dụ huỷ/chặn kết nối của request đó ở tầng trình duyệt — và chỉ áp dụng cho LẦN GỌI ĐẦU TIÊN, các lần sau cho đi qua bình thường. Ghi rõ cách mô phỏng đã dùng vào kết quả chạy. Bật sẵn console và bảng theo dõi request mạng. | 1. Bật cách chặn để LẦN GỌI ĐẦU TIÊN tới địa chỉ lấy thông tin khách bị huỷ ở tầng vận chuyển; các lần gọi sau cho đi qua.<br>2. Mở đường dẫn đăng ký sự kiện trên trình duyệt.<br>3. Theo dõi bảng request mạng: đếm số lần trang gọi lấy thông tin khách và ghi lại khoảng thời gian giữa lần gọi thứ nhất và lần gọi thứ hai.<br>4. Quan sát màn hình trong suốt quá trình: có hộp thoại lỗi nào hiện lên hay không.<br>5. Sau khi trang ổn định, kiểm tra các mục thông tin khách đã được điền sẵn và nút 「予約確認にすすむ」 có bật không.<br>6. Đọc console tìm lỗi JavaScript rồi bấm 「予約確認にすすむ」 để xác nhận luồng đăng ký đi tiếp được bình thường. | Chặn đúng LẦN GỌI ĐẦU TIÊN ở tầng vận chuyển (không phản hồi, không mã lỗi HTTP); lần gọi thứ hai cho đi qua bình thường. | Trang tự gọi lại ĐÚNG MỘT LẦN, cách lần đầu khoảng 800ms (chấp nhận sai số của môi trường, ghi lại số đo được). Lần gọi lại thành công nên KHÔNG có hộp thoại lỗi nào hiện lên — khách không nhìn thấy bất thường gì. Các mục thông tin khách được điền sẵn đúng và nút 「予約確認にすすむ」 được bật, khách bấm đi tiếp được. Console không có lỗi JavaScript chặn luồng. | Chưa test |  | all |  |  |  |  | Studio #18931 (NEW-11) · mã theo quan điểm: TC-INTGLINE001-01 · ui · auto · env-safe · REQ: REQ-006 · spec: TICKET-41025, SCR-EVB-16, EP-F4, diff: order-item.js getFriendInfo (MAX_GET_FRIEND_INFO_RETRY = 1, chờ 800ms, điều kiện status === 0) · Đây là mục tiêu chính của phần thử lại: sự cố thoáng qua phải được tự khắc phục êm, khách không bị doạ 'kiểm tra đường truyền'. Quan trọng: phải mô phỏng lỗi TẦNG VẬN CHUYỂN (status 0), không phải mã lỗi HTTP — vì điều kiện thử lại chỉ nhận status 0. Trong trình duyệt tự động có thể dùng chức năng huỷ/abort request theo đường dẫn. Đúng một lần thử lại là chủ ý của Dev (MAX_GET_FRIEND_INFO_RETRY = 1): nhiều lần chỉ kéo dài thời gian khách chờ. · author=AI · status=draft |
| NEW-12 | INTG-LINE-001 | Abnormal | Request lấy thông tin khách chết liên tục thì chỉ thử lại một lần rồi mới báo lỗi | Môi trường chạy nhánh fix `ai_fixbug_41025`. Cùng sự kiện và người dùng test như case trên. Có cách làm MỌI lần gọi lấy thông tin khách đều chết ở tầng vận chuyển (không phản hồi, không mã lỗi HTTP). Bật sẵn console và bảng theo dõi request mạng để đếm cả request lấy thông tin khách và request báo lỗi. | 1. Đánh dấu mốc log backend trước khi bắt đầu và bật cách chặn để MỌI lần gọi tới địa chỉ lấy thông tin khách đều bị huỷ ở tầng vận chuyển.<br>2. Mở đường dẫn đăng ký sự kiện trên trình duyệt.<br>3. Theo dõi bảng request mạng: đếm tổng số lần trang gọi lấy thông tin khách.<br>4. Đếm số request trang gửi tới địa chỉ báo lỗi JavaScript và ghi lại giá trị cờ báo động của từng request.<br>5. Đọc nguyên văn nội dung hộp thoại hiện lên và ghi lại thời điểm nó xuất hiện so với lúc mở trang.<br>6. Đọc hai bản ghi log backend tương ứng lần lỗi thoáng qua và lần lỗi cuối; kiểm tra khối xhr của từng bản ghi.<br>7. Đóng hộp thoại, kiểm tra nút 「予約確認にすすむ」 có được bật lại không.<br>8. Bỏ chặn, tải lại trang và kiểm tra luồng đăng ký hoạt động bình thường trở lại. | Chặn TẤT CẢ các lần gọi lấy thông tin khách ở tầng vận chuyển. | Trang gọi lấy thông tin khách ĐÚNG HAI LẦN (lần đầu + đúng một lần thử lại), không thử lại lần thứ ba. Hộp thoại chỉ hiện SAU khi lần thử lại cũng thất bại, với nội dung 「情報の取得に失敗しました。通信環境をご確認のうえ、もう一度お試しください。」. Trang gửi ĐÚNG HAI request báo lỗi: lần thử lại có notify TẮT và lần thất bại cuối có notify BẬT; hai request không bị khoá trùng lẫn nhau. Mỗi bản ghi log chứa đủ các thuộc tính chẩn đoán được collectXhrInfo thu thập: status, statusText, readyState, state của deferred, response headers, Content-Type, responseText tối đa 500 ký tự, responseJSON và readError. Với lỗi vận chuyển status=0, headers/Content-Type phải rỗng và log không được tự dựng thuộc tính không tồn tại. Nút 「予約確認にすすむ」 được bật lại. Sau khi bỏ chặn và tải lại, luồng đăng ký hoạt động bình thường. | Chưa test |  | all |  |  |  |  | Studio #18932 (NEW-12) · mã theo quan điểm: TC-INTGLINE001-02 · ui · auto · env-safe · REQ: REQ-006, REQ-012 · spec: TICKET-41025, SCR-EVB-16, EP-F4, diff: order-item.js getFriendInfo (báo lỗi notify=false trước khi thử lại, notify mặc định khi thất bại cuối), diff: index.blade.php (MAX_REPORT = 5 có khoá trùng, MAX_LOG_ONLY = 10 không khoá trùng) · Khóa số lần retry, thời điểm báo khách, việc tách notify và tính đầy đủ của collectXhrInfo. Cần đối chiếu cả request phía trình duyệt lẫn log backend; chỉ nhìn network request chưa đủ chứng minh dữ liệu chẩn đoán mới được ghi đúng. · author=AI · status=draft |
| NEW-13 | CONC-003 | Abnormal | Quay lại trang đăng ký sau khi sang màn kết bạn thì cơ chế thử lại và báo lỗi hoạt động lại | Môi trường chạy nhánh fix `ai_fixbug_41025`. Có tài khoản LINE test CHƯA kết bạn với bot để lần mở đầu tiên bị đẩy sang trang kết bạn. Trình duyệt phải giữ trang trong bộ nhớ đệm khi quay lại (dùng nút Back của trình duyệt, không tải lại trang). Có cách làm request lấy thông tin khách chết ở tầng vận chuyển theo yêu cầu. Bật sẵn console và bảng theo dõi request mạng. | 1. Mở đường dẫn đăng ký sự kiện bằng tài khoản chưa kết bạn; trang tự chuyển sang trang kết bạn.<br>2. Bấm nút Back của trình duyệt để quay lại trang đăng ký (KHÔNG tải lại trang, để trang được phục hồi từ bộ nhớ đệm).<br>3. Sau khi trang hiện lại, bật cách chặn để mọi lần gọi lấy thông tin khách chết ở tầng vận chuyển.<br>4. Kích hoạt lại việc lấy thông tin khách (ví dụ thao tác lại bước chọn để trang gọi lại, hoặc dùng nút thao tác có gọi lại hàm này).<br>5. Đếm số lần trang gọi lấy thông tin khách và số request báo lỗi gửi đi sau khi quay lại.<br>6. Quan sát hộp thoại thông báo lỗi có hiện lên hay không.<br>7. Đọc console tìm lỗi JavaScript. | Tài khoản chưa kết bạn cho lần mở đầu; quay lại bằng nút Back của trình duyệt; sau khi quay lại thì chặn mọi lần gọi lấy thông tin khách ở tầng vận chuyển. | Sau khi trang được phục hồi từ bộ nhớ đệm, cơ chế xử lý lỗi phải hoạt động BÌNH THƯỜNG TRỞ LẠI: trang vẫn tự thử lại đúng một lần và vẫn hiện hộp thoại 「情報の取得に失敗しました。通信環境をご確認のうえ、もう一度お試しください。」 khi thử lại thất bại, và vẫn gửi request báo lỗi với cờ báo động BẬT. KHÔNG được im lặng nuốt cả lần thử lại lẫn thông báo. Console không có lỗi JavaScript. | Chưa test |  | all |  |  |  |  | Studio #18935 (NEW-13) · mã theo quan điểm: TC-CONC003-01 · ui · auto · env-safe · REQ: REQ-008, REQ-006 · spec: TICKET-41025, SCR-EVB-16, diff: order-item.js (gỡ cờ rời trang ở sự kiện trang hiện lại), source: git log commit 36501465fa (ai-selfreview #41025) · Đây chính là khuyết điểm mà vòng tự review của Dev phát hiện và sửa ở commit cuối: sự kiện 'trang bị ẩn đi' cũng bắn khi trang chỉ được cất vào bộ nhớ đệm back-forward, lúc đó biến JavaScript được giữ nguyên nên cờ 'đang rời trang' còn bật; nếu không gỡ cờ ở sự kiện 'trang hiện lại' thì MỌI lỗi sau đó bị nuốt — đúng vào ca hay hỏng nhất là vừa quay lại từ nền. Nếu trình duyệt tự động không giữ được trang trong bộ nhớ đệm back-forward, người chạy phải ghi rõ và chuyển ca này sang thiết bị/trình duyệt giữ được bộ nhớ đệm; KHÔNG được thay bằng tải lại trang (tải lại làm mất ý nghĩa của case vì biến JavaScript được khởi tạo lại). · author=AI · status=draft |
| NEW-15 | TOOL-ERRHYG-001 | Abnormal | Thiếu khối khai báo ghi vết không được làm chết thao tác đăng ký của khách | Môi trường chạy nhánh fix `ai_fixbug_41025`. Sự kiện đang mở, người dùng test đã kết bạn, khung giờ còn chỗ. Cần mô phỏng được tình huống khối khai báo hàm ghi vết/báo lỗi ở đầu trang KHÔNG chạy (ví dụ chặn hoặc làm lỗi khối script đó) trước khi tệp JavaScript của màn đăng ký được nạp. Bật sẵn console. | 1. Mở đường dẫn đăng ký sự kiện với khối khai báo hàm ghi vết/báo lỗi ở đầu trang bị vô hiệu hoá.<br>2. Đọc console ngay sau khi trang nạp xong, tìm lỗi về việc gọi một hàm không tồn tại.<br>3. Chọn ngày, khung giờ, gói và nhập đủ thông tin bắt buộc.<br>4. Bấm 「予約確認にすすむ」 và kiểm tra có sang được màn xác nhận không.<br>5. Bấm nút gửi đăng ký và kiểm tra đăng ký có hoàn tất không.<br>6. Ghi lại toàn bộ lỗi console phát sinh trong luồng. | Khối khai báo hàm ghi vết/báo lỗi ở đầu trang bị vô hiệu hoá; phần còn lại của luồng đăng ký nhập dữ liệu hợp lệ như bình thường. | Console KHÔNG có lỗi về việc gọi hàm ghi vết/báo lỗi không tồn tại. Toàn bộ luồng đăng ký vẫn chạy trọn: chọn được khung giờ, sang được màn xác nhận và đăng ký hoàn tất được. Nói cách khác, công cụ chẩn đoán vắng mặt thì chỉ mất dữ liệu chẩn đoán, KHÔNG được làm chết chức năng nó đang theo dõi. | Chưa test |  | all |  |  |  |  | Studio #18937 (NEW-15) · mã theo quan điểm: TC-TOOLERRHYG001-01 · ui · auto · env-safe · REQ: REQ-013 · spec: TICKET-41025, SCR-EVB-16, diff: order-item.js (khai hàm rỗng thay thế khi hàm ghi vết/báo lỗi chưa tồn tại), diff: index.blade.php (bộ đệm vết thao tác bọc try/catch) · Bản vá chèn lệnh ghi vết vào DÒNG ĐẦU của 10 hàm thao tác chính (chuyển bước, huỷ, lấy khung giờ, chọn khung giờ, xác nhận, mở form thẻ, gửi đăng ký, gửi thay đổi, gửi huỷ, lấy thông tin khách) nên nếu hàm đó không tồn tại thì một lỗi tham chiếu sẽ chết ngay ở thao tác đầu tiên của khách — đây là rủi ro do chính công cụ chẩn đoán mang lại. Bản vá chặn bằng cách khai hàm rỗng thay thế nếu chưa có. Nếu không mô phỏng được việc vô hiệu hoá khối script trong trình duyệt tự động, ĐƯỢC PHÉP thay bằng KIỂM TRA TĨNH trên mã nguồn: xác nhận khối khai hàm rỗng thay thế tồn tại trong tệp JavaScript của màn đăng ký và nằm TRƯỚC nơi khởi tạo giao diện; ghi rõ đã dùng cách kiểm tĩnh vào kết quả chạy. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| #18934 | LIFF-ENTRY-001 | Abnormal | Khách chưa kết bạn được đưa sang màn kết bạn mà không bị báo động giả lỗi đường truyền | Có 1 tài khoản LINE chưa kết bạn với bot.<br>Có đường dẫn đăng ký của sự kiện (bản đã vá `ai_fixbug_41025`).<br>Có máy tính để mở cùng đường dẫn.<br>Bật sẵn console và bảng theo dõi request mạng ở điểm vào trình duyệt để quan sát request lấy thông tin khách và request báo lỗi. | 1. Tài khoản chưa kết bạn mở đường dẫn từ LINE → quan sát có được đưa sang màn kết bạn không, và trong lúc chuyển trang có hộp thoại lỗi nào hiện lên không.<br>2. Lặp lại bằng trình duyệt có bảng theo dõi request: mở đường dẫn với tài khoản chưa kết bạn, ghi lại các request đã gửi đi và trạng thái của chúng ngay trước khi trang chuyển đi.<br>3. Kết bạn xong, mở lại đường dẫn → đi trọn luồng tới khi đặt xong.<br>4. Mở cùng đường dẫn bằng trình duyệt ngoài trên điện thoại → lặp chuyển bước >= 2 lần, quan sát vị trí cuộn.<br>5. Mở cùng đường dẫn trên máy tính → lặp chuyển bước >= 2 lần, quan sát.<br>6. Với mỗi điểm vào, ghi lại có đặt chỗ hoàn tất được không. | 3 điểm vào: trong LINE (chưa kết bạn → đã kết bạn) · trình duyệt ngoài · máy tính. | Chưa kết bạn: được đưa sang màn kết bạn đúng như trước, việc bỏ neo trang không làm hỏng bước chuyển hướng này.<br>Trong lúc chuyển sang màn kết bạn, KHÔNG được hiện hộp thoại 「情報の取得に失敗しました。通信環境をご確認のうえ、もう一度お試しください。」 dù request lấy thông tin khách đang bay bị trình duyệt huỷ theo — đây là báo động giả mà bản vá phải loại bỏ.<br>Cũng KHÔNG được gửi request báo lỗi có cờ báo động BẬT cho lần bị huỷ đó, và không được phát sinh lần thử lại nào.<br>Cả 3 điểm vào đều cuộn về đầu ở mọi lần đổi bước và đặt chỗ hoàn tất được.<br>Không điểm vào nào bị trắng màn hoặc kẹt ở bước xác nhận. | Chưa test |  | all |  |  |  |  | Studio #18934 · mã theo quan điểm: TC-LIFFENTRY001-01 · ui · auto · env-safe · REQ: REQ-008 · spec: TICKET-40806, TICKET-41025, SCR-EVB-16, diff: order-item.js getSlots (đánh dấu rời trang trước khi chuyển sang trang kết bạn), diff: order-item.js getFriendInfo (bỏ qua nhánh báo lỗi khi đang rời trang và không có phản hồi) · CHỈNH CASE KHO THEO SPEC MỚI (giữ khoOrigin để leader duyệt sync ngược đúng dòng gốc). Lý do chỉnh: bản vá #41025 chạm trực tiếp đúng nhánh này — khi khách chưa kết bạn, hàm lấy khung giờ đánh dấu 'đang rời trang' NGAY TRƯỚC khi đổi địa chỉ trang, để nhánh thất bại của hàm lấy thông tin khách (được gọi ngay trước đó và thường vẫn đang bay) không báo nhầm lỗi mạng. Đã bổ sung 2 điều kiện mong đợi mới (không hộp thoại lỗi mạng, không cảnh báo lỗi thật) và một bước quan sát request. Phần kiểm cuộn trang giữ nguyên từ ticket #40806 để không làm mất coverage cũ. Kỹ thuật: jQuery báo ca bị huỷ do chuyển trang y hệt ca mất mạng (status 0) nên đây là chỗ duy nhất phân biệt được bằng cờ đánh dấu. · author=AI · status=draft |
| NEW-28 | DEPLOY-ASSET-001 | Normal | Trang đăng ký nạp đúng bản JavaScript đã vá sau khi release | Môi trường cần kiểm đã triển khai nhánh fix `ai_fixbug_41025`. Có đường dẫn đăng ký của một sự kiện test. Biết giá trị tham số phiên bản mà môi trường ĐỐI CHIẾU (bản release chưa vá) đang dùng, để so sánh. Có thể xem mã nguồn trang trả về và tải trực tiếp tệp JavaScript của màn đăng ký. | 1. Mở đường dẫn đăng ký sự kiện và xem mã nguồn trang trả về.<br>2. Tìm thẻ nạp tệp JavaScript của màn đăng ký sự kiện và ghi lại giá trị tham số phiên bản đi kèm.<br>3. So sánh giá trị tham số phiên bản này với giá trị của môi trường đối chiếu chưa vá.<br>4. Tải trực tiếp tệp JavaScript đó theo đúng đường dẫn kèm tham số phiên bản.<br>5. Kiểm tra trong nội dung tệp có đủ ba thay đổi của bản vá: kiểm tra rỗng trước khi xoá từng ô nhập thẻ; chốt chỉ quay về bước nhập thẻ khi khách đi qua bước thẻ (ở CẢ hàm gửi đăng ký và hàm gửi thay đổi); cơ chế thử lại một lần cho việc lấy thông tin khách kèm cờ đánh dấu rời trang và điểm gỡ cờ khi trang hiện lại.<br>6. Mở lại trang bằng trình duyệt ĐÃ có bộ nhớ đệm của bản JavaScript cũ (không xoá bộ nhớ đệm, không tải lại cưỡng bức) và kiểm tra trang vẫn nhận được bản mới. | Đường dẫn đăng ký của sự kiện test trên môi trường đã triển khai bản vá; giá trị tham số phiên bản của môi trường đối chiếu chưa vá. | Thẻ nạp tệp JavaScript của màn đăng ký có tham số phiên bản và giá trị đó KHÁC giá trị của môi trường chưa vá — nếu giống nhau thì khách đang dùng bộ nhớ đệm cũ sẽ không nhận được bản vá, phải báo lại cho người release trước khi test tiếp. Nội dung tệp JavaScript tải về có đủ cả ba thay đổi liệt kê ở bước 5. Trình duyệt đã có bộ nhớ đệm của bản cũ vẫn nhận được bản mới mà khách không cần tải lại cưỡng bức. | Chưa test |  | all |  |  |  |  | Studio #18961 (NEW-28) · mã theo quan điểm: TC-DEPLOYASSET001-01 · ui · auto · read-only · REQ: REQ-017 · spec: TICKET-41025, SCR-EVB-16, source: index.blade.php dòng 887 (tệp JavaScript màn đăng ký gắn tham số phiên bản), knowledge: DEPLOY-ASSET-001 · TC TIỀN ĐỀ, phải chạy TRƯỚC mọi TC khác của ticket: toàn bộ bản vá nằm trong một tệp JavaScript duy nhất, nên nếu tham số phiên bản không được nâng lúc release thì mọi TC còn lại sẽ 'đạt' một cách giả trên chính bản chưa vá. Tham số phiên bản lấy từ cấu hình phiên bản chung của ứng dụng nên nó phụ thuộc quy trình release, không phải thay đổi trong ticket này. Bước 5 là KIỂM TRA TĨNH trên nội dung tệp được trả về (hợp lệ với exec_mode auto); có thể đối chiếu thêm với mã nguồn của nhánh fix. · author=AI · status=draft |
| NEW-29 | ENV-003 | Abnormal | Trên LINE thật: quay lại trang đăng ký sau khi sang màn kết bạn vẫn báo lỗi và thử lại bình thường | Có iPhone hoặc Android thật đã cài LINE và mở được đường dẫn đăng ký sự kiện của bản đã vá `ai_fixbug_41025`. Có tài khoản LINE CHƯA kết bạn với bot để lần mở đầu tiên bị đẩy sang trang kết bạn. Trước khi chạy phải xác nhận thiết bị đang tải bản JavaScript đã vá (chạy TC tiền đề về phiên bản tệp JavaScript trước). Cần cách làm mất kết nối của thiết bị trong thời gian ngắn (ví dụ bật chế độ máy bay) để tạo lỗi đường truyền thật. | 1. Từ cửa sổ chat với bot trên LINE, mở đường dẫn đăng ký sự kiện bằng tài khoản chưa kết bạn; quan sát có được đưa sang màn kết bạn không và trong lúc chuyển có hộp thoại lỗi nào hiện lên không.<br>2. Dùng thao tác quay lại của LINE hoặc của WebView để trở về trang đăng ký, KHÔNG mở lại đường dẫn từ đầu.<br>3. Sau khi trang hiện lại, đưa thiết bị vào trạng thái mất kết nối trong thời gian ngắn rồi thao tác để trang phải lấy lại thông tin khách.<br>4. Quan sát có hộp thoại thông báo lỗi hiện lên hay không, và sau bao lâu kể từ thao tác.<br>5. Bỏ trạng thái mất kết nối, thao tác lại và kiểm tra luồng đăng ký chạy được bình thường trở lại.<br>6. Đóng LINE để đẩy ứng dụng xuống nền, mở lại LINE để trang được khôi phục, rồi lặp lại bước 3 và 4.<br>7. Nếu có cả hai loại thiết bị thì lặp toàn bộ trên iPhone và Android rồi đối chiếu kết quả. | Tài khoản LINE chưa kết bạn cho lần mở đầu; quay lại bằng thao tác quay lại của LINE/WebView; mất kết nối thiết bị trong thời gian ngắn để tạo lỗi đường truyền thật. Lặp trên iPhone và Android nếu có. | Lần mở đầu: tài khoản chưa kết bạn được đưa sang màn kết bạn và trong lúc chuyển KHÔNG hiện hộp thoại 「情報の取得に失敗しました。通信環境をご確認のうえ、もう一度お試しください。」. Sau khi quay lại trang đăng ký (kể cả trường hợp trang được khôi phục sau khi LINE bị đẩy xuống nền), cơ chế xử lý lỗi phải hoạt động BÌNH THƯỜNG TRỞ LẠI: khi mất kết nối thật thì trang vẫn hiện hộp thoại thông báo lỗi sau khi đã tự thử lại — KHÔNG được im lặng, không có phản hồi gì. Sau khi có kết nối lại, luồng đăng ký chạy được bình thường. Nếu iPhone và Android khác nhau về cơ chế khôi phục trang thì ghi nhận chi tiết, nhưng không được có trường hợp nào bị nuốt hoàn toàn thông báo lỗi. | Chưa test |  | all |  |  |  |  | Studio #18962 (NEW-29) · mã theo quan điểm: TC-ENV003-01 · ui · manual · env-safe · REQ: REQ-008 · spec: TICKET-41025, SCR-EVB-16, diff: order-item.js (gỡ cờ rời trang ở sự kiện trang hiện lại), knowledge: ENV-003, knowledge: SYNC-APP-001 · LÝ DO PHẢI MANUAL: phần này cần WebView LINE thật và hành vi khôi phục trang khi ứng dụng bị đẩy xuống nền — đúng chỗ mà trình duyệt tự động không tái hiện được, và đúng chỗ Dev sửa ở commit tự review cuối (gỡ cờ 'đang rời trang' khi trang hiện lại). Lưu ý: KHÔNG suy diễn rằng cả bộ TC của ticket phải manual — phần lớn màn đăng ký chạy được trên trình duyệt thường vì mã người dùng được cấp từ phía server qua mã người dùng trong đường dẫn, chỉ khi thiếu mã đó trang mới cần gọi bộ công cụ LIFF. TC này chỉ giữ lại đúng khác biệt môi trường THẬT có. · author=AI · status=draft |
| NEW-16 | API-CONTRACT-001 | Normal | Báo lỗi JS không gửi cờ báo động vẫn giữ nguyên hành vi cảnh báo như trước | Môi trường local chạy nhánh fix `ai_fixbug_41025`. Đọc được file log ứng dụng và xem được danh sách công việc đang chờ trong kết nối hàng đợi của ứng dụng. Có mã bảo vệ biểu mẫu lấy từ chính trang đăng ký sự kiện để gọi được endpoint. Đã đánh dấu mốc hiện tại trong file log và đếm số công việc gửi cảnh báo đang chờ trong hàng đợi. Nếu môi trường có khoá trùng cảnh báo còn hiệu lực từ lần chạy trước thì dùng nội dung bản tin mới, chưa từng gửi. | 1. Đánh dấu mốc hiện tại trong file log ứng dụng và đếm số công việc gửi cảnh báo đang chờ trong hàng đợi.<br>2. Trên một trang/bundle mô phỏng client cũ, gọi hàm báo lỗi JavaScript bằng ĐÚNG HAI đối số cũ: message và detail; không truyền notify và steps. Ghi lại request thực tế mà helper gửi tới endpoint.<br>3. Đọc nội dung phản hồi và mã trạng thái HTTP.<br>4. Mở file log ứng dụng từ mốc đã đánh dấu, tìm dòng ghi bản tin vừa gửi và ghi lại MỨC của dòng log đó.<br>5. Đếm lại số công việc gửi cảnh báo đang chờ trong hàng đợi và so với bước 1.<br>6. Gọi phiên bản hàm mới với cùng loại lỗi nhưng notify='1', dùng một bản tin khác, rồi đối chiếu kết quả. | Lần 1: bốn tham số của bản trang cũ, bản tin 「TC41025 legacy client no notify」, không có cờ báo động, không có vết thao tác. Lần 2: thêm cờ báo động = 「1」, bản tin 「TC41025 explicit notify 1」. | Lời gọi client cũ chỉ có hai đối số vẫn gửi request hợp lệ; việc thiếu notify và steps không gây lỗi JavaScript hoặc lỗi server. Cả hai lần đều trả HTTP 200 với phản hồi thành công. Khi notify bị thiếu, server áp dụng mặc định như lỗi thật: ghi log mức LỖI và đẩy đúng 1 công việc cảnh báo; kết quả tương đương lần notify='1'. Nội dung steps thiếu được xử lý an toàn, không tạo chuỗi lỗi hoặc exception. | Chưa test |  | local |  |  |  |  | Studio #18938 (NEW-16) · mã theo quan điểm: TC-APICONTRACT001-01 · api · auto · local-only · REQ: REQ-009 · spec: TICKET-41025, source: POST /ajax/booking-event/report-js-error, diff: MobileEventBookingController@reportJsError (mặc định coi như lỗi thật khi thiếu tham số) · Kiểm tương thích ngược theo đúng giao diện client cũ hai đối số, không giả lập bằng cách tự gọi endpoint với bốn trường. Sau release, tab đã mở trước đó vẫn có thể chạy bundle cũ; oracle trọng tâm là giá trị mặc định của notify/steps và việc cảnh báo thật không bị im lặng. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-17 | API-CONTRACT-001 | Normal | Báo lỗi JS với cờ báo động tắt chỉ ghi log và không đẩy cảnh báo | Môi trường local chạy nhánh fix `ai_fixbug_41025`. Đọc được file log ứng dụng và xem được danh sách công việc đang chờ trong hàng đợi. Có mã bảo vệ biểu mẫu lấy từ trang đăng ký sự kiện. Đã đánh dấu mốc trong file log và đếm số công việc gửi cảnh báo đang chờ. | 1. Đánh dấu mốc trong file log và đếm số công việc gửi cảnh báo đang chờ trong hàng đợi.<br>2. Gọi endpoint báo lỗi JS với cờ báo động đặt bằng chuỗi 「0」, kèm bản tin lỗi, địa chỉ trang, thông tin trình duyệt, chi tiết chẩn đoán và vết thao tác.<br>3. Đọc nguyên văn nội dung phản hồi và mã trạng thái HTTP.<br>4. Mở file log từ mốc đã đánh dấu, tìm dòng ghi bản tin vừa gửi và ghi lại MỨC của dòng log đó; kiểm tra dòng log có chứa giá trị cờ báo động, địa chỉ IP và vết thao tác hay không.<br>5. Đếm lại số công việc gửi cảnh báo đang chờ trong hàng đợi và so với bước 1.<br>6. Gọi lại endpoint với cờ báo động đặt bằng chuỗi 「false」 (một giá trị KHÔNG phải chuỗi 「0」), dùng bản tin khác, rồi đối chiếu mức log và số công việc trong hàng đợi. | Lần 1: cờ báo động = 「0」, bản tin 「TC41025 transient notify 0」, vết thao tác là một dãy thao tác mẫu. Lần 2: cờ báo động = 「false」, bản tin 「TC41025 notify false literal」. | Lần 1: trả mã HTTP 200 với phản hồi báo thành công VÀ có dấu hiệu đã ghi nhận log; dòng log được ghi ở MỨC THÔNG TIN (không phải mức lỗi) để không lẫn vào cảnh báo thật; dòng log có ghi giá trị cờ báo động, địa chỉ IP và vết thao tác; số công việc gửi cảnh báo trong hàng đợi KHÔNG TĂNG. Lần 2: vì giá trị không đúng chuỗi 「0」 nên hệ thống coi là lỗi thật — dòng log ở MỨC LỖI và số công việc gửi cảnh báo TĂNG 1. Ghi lại kết quả lần 2 để leader biết chỉ đúng chuỗi 「0」 mới tắt cảnh báo. | Chưa test |  | local |  |  |  |  | Studio #18939 (NEW-17) · mã theo quan điểm: TC-APICONTRACT001-02 · api · auto · local-only · REQ: REQ-009 · spec: TICKET-41025, source: POST /ajax/booking-event/report-js-error, diff: MobileEventBookingController@reportJsError (nhánh cờ tắt: ghi log mức thông tin rồi trả về trước phần cảnh báo) · Nhánh cờ tắt phục vụ sự cố thoáng qua đã tự khắc phục (request hỏng nhưng gọi lại là qua): khách không thấy gì nên không được làm phiền cảnh báo, nhưng vẫn phải ghi lại để biết sự cố xảy ra bao nhiêu lần. Lần gọi thứ hai là ca biên của cách so sánh chuỗi: giá trị 「false」 KHÔNG tắt được cảnh báo — không phải lỗi, nhưng là điều tester cần biết để không hiểu sai hợp đồng. Toàn bộ trang hiện tại chỉ gửi 「0」 hoặc 「1」 nên không ảnh hưởng thực tế. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-18 | OBS-001 | Normal | Vết thao tác được gửi kèm khi báo lỗi lỗi thật nhưng không đi vào nội dung cảnh báo | Môi trường local chạy nhánh fix `ai_fixbug_41025`. Đọc được file log ứng dụng và đọc được NỘI DUNG của công việc gửi cảnh báo trong hàng đợi (không cần gửi thật ra ngoài). Có mã bảo vệ biểu mẫu. Đã đánh dấu mốc trong file log. | 1. Đánh dấu mốc trong file log ứng dụng.<br>2. Gọi endpoint báo lỗi JS với cờ báo động BẬT, kèm chi tiết chẩn đoán và một vết thao tác có nội dung nhận dạng được (ví dụ chứa chuỗi đánh dấu riêng).<br>3. Đọc dòng log vừa sinh ra và kiểm tra vết thao tác có được ghi vào log hay không.<br>4. Đọc nội dung công việc gửi cảnh báo vừa được đẩy vào hàng đợi.<br>5. Kiểm tra nội dung cảnh báo gồm những phần nào: bản tin, địa chỉ trang, thông tin trình duyệt, chi tiết chẩn đoán — và có chứa chuỗi đánh dấu của vết thao tác không. | Cờ báo động BẬT; vết thao tác chứa chuỗi đánh dấu 「TC41025-STEPS-MARKER」; chi tiết chẩn đoán chứa chuỗi đánh dấu 「TC41025-DETAIL-MARKER」. | Dòng log CÓ chứa cả chuỗi đánh dấu của vết thao tác và của chi tiết chẩn đoán. Nội dung công việc gửi cảnh báo CÓ chứa bản tin, địa chỉ trang, thông tin trình duyệt và chuỗi đánh dấu của chi tiết chẩn đoán, nhưng KHÔNG chứa chuỗi đánh dấu của vết thao tác — vết thao tác dài và chỉ dùng khi ngồi truy nguyên nên không được làm phình tin cảnh báo. | Chưa test |  | local |  |  |  |  | Studio #18940 (NEW-18) · mã theo quan điểm: TC-OBS001-02 · api · auto · local-only · REQ: REQ-010, REQ-012 · spec: TICKET-41025, diff: MobileEventBookingController@reportJsError (vết thao tác chỉ ghi log, không gửi cảnh báo) · Phân tách đích đến của hai loại dữ liệu chẩn đoán là chủ ý của bản vá; nếu vết thao tác lọt vào tin cảnh báo thì tin sẽ dài gấp nhiều lần và người nhận cảnh báo khó đọc. Có thể đọc nội dung công việc trong hàng đợi thay vì gửi thật ra ngoài để không làm ồn kênh cảnh báo của đội. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-20 | TOOL-ERRHYG-001 | Abnormal | Báo lỗi JS với bản tin rỗng bị từ chối và không ghi log, không đẩy cảnh báo | Môi trường local chạy nhánh fix `ai_fixbug_41025`. Đọc được file log ứng dụng và hàng đợi công việc. Có mã bảo vệ biểu mẫu. Đã đánh dấu mốc trong file log và đếm số công việc gửi cảnh báo đang chờ. | 1. Đánh dấu mốc trong file log và đếm số công việc gửi cảnh báo đang chờ.<br>2. Biến thể A: gọi endpoint báo lỗi JS với bản tin là chuỗi rỗng, các tham số khác điền đầy đủ.<br>3. Biến thể B: gọi lại với bản tin chỉ gồm khoảng trắng và ký tự xuống dòng.<br>4. Biến thể C: gọi lại mà KHÔNG gửi tham số bản tin nào.<br>5. Với mỗi biến thể: đọc mã trạng thái HTTP và nội dung phản hồi.<br>6. Sau cả ba biến thể: đọc file log từ mốc đã đánh dấu và đếm lại số công việc gửi cảnh báo đang chờ. | A: bản tin là chuỗi rỗng. B: bản tin chỉ gồm khoảng trắng và ký tự xuống dòng. C: không gửi tham số bản tin. Cả ba biến thể vẫn gửi đầy đủ địa chỉ trang, thông tin trình duyệt, chi tiết chẩn đoán và cờ báo động BẬT. | Cả ba biến thể đều trả mã HTTP 200 với phản hồi báo KHÔNG thành công — lỗi được từ chối sạch sẽ, KHÔNG phát sinh lỗi hệ thống 5xx. Không có dòng log nào được ghi cho ba lần gọi này và số công việc gửi cảnh báo đang chờ KHÔNG TĂNG. Biến thể B chứng tỏ khoảng trắng được loại bỏ trước khi kiểm tra rỗng. | Chưa test |  | local |  |  |  |  | Studio #18942 (NEW-20) · mã theo quan điểm: TC-TOOLERRHYG001-02 · api · auto · local-only · REQ: REQ-010 · spec: TICKET-41025, source: MobileEventBookingController@reportJsError (bản tin rỗng trả về thất bại) · Gộp ba biến thể cùng một failure mode vào một TC thay vì ba TC. Hành vi này có trước bản vá nhưng nằm ngay đầu hàm đã bị sửa nên chạy như hồi quy: nếu bị bỏ qua thì cảnh báo sẽ nhận được tin rỗng vô nghĩa. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-22 | TOOL-ERRHYG-001 | Abnormal | Giới hạn số lần báo lỗi theo địa chỉ IP vẫn còn hiệu lực và đo được quan hệ với hạn mức phía trình duyệt | Môi trường local chạy nhánh fix `ai_fixbug_41025`. Bộ đếm giới hạn theo IP đã được đặt lại hoặc đã hết hiệu lực (chờ qua 1 phút). Có mã bảo vệ biểu mẫu. Đọc được file log. | 1. Đảm bảo bộ đếm giới hạn theo IP đang ở trạng thái sạch (đặt lại hoặc chờ qua một phút).<br>2. Gọi endpoint báo lỗi JS 15 lần liên tiếp từ cùng một địa chỉ IP trong vòng dưới một phút, mỗi lần dùng một bản tin khác nhau; trong đó 10 lần đầu đặt cờ báo động TẮT và 5 lần sau đặt cờ báo động BẬT.<br>3. Ghi lại mã trạng thái HTTP của TỪNG lần gọi và số thứ tự lần gọi đầu tiên bị chặn.<br>4. Đọc file log và đếm số dòng log thật sự được ghi.<br>5. Ghi rõ vào kết quả chạy: có bao nhiêu lần gọi có cờ báo động BẬT bị chặn mất.<br>6. Chờ qua một phút rồi gọi lại một lần và kiểm tra request được chấp nhận trở lại. | 15 lần gọi liên tiếp từ cùng một IP trong dưới một phút, theo thứ tự đúng như trang thật có thể phát ra: 10 lần cờ báo động TẮT trước, rồi 5 lần cờ báo động BẬT. Phép tính tay theo giới hạn hiện tại (10 request mỗi phút mỗi IP): 10 lần đầu được chấp nhận, 5 lần sau bị chặn. | Endpoint chấp nhận 10 request đầu và TỪ REQUEST THỨ 11 trở đi trả mã chặn vì quá số lần cho phép (mã 429). Số dòng log được ghi đúng bằng số request được chấp nhận. Sau khi chờ qua một phút, request mới lại được chấp nhận. Người chạy phải GHI RÕ con số quan sát được, đặc biệt là số request cờ báo động BẬT bị chặn mất — theo phép tính trên thì toàn bộ 5 cảnh báo lỗi thật bị mất suất do 10 request chỉ-ghi-log đi trước. ĐÂY LÀ SỐ ĐO ĐỂ BÁO CÁO, KHÔNG tự kết luận đạt hay không đạt: mức chấp nhận cần leader/Dev chốt (xem câu hỏi trong kết quả). | Chưa test |  | local |  |  |  | needs-human-review | Studio #18944 (NEW-22) · mã theo quan điểm: TC-TOOLERRHYG001-03 · api · auto · local-only · REQ: REQ-011 · spec: TICKET-41025, source: routes/web.php (giới hạn 10 request mỗi phút theo IP trên endpoint báo lỗi), diff: index.blade.php (hạn mức 5 lỗi thật + 10 chỉ-ghi-log trong một lượt mở trang) · TC đo mâu thuẫn có thật: phía trình duyệt cho phép tối đa 5 request lỗi thật + 10 request chỉ-ghi-log, trong khi endpoint chỉ nhận 10 request/phút/IP. Thứ tự gọi mô phỏng trường hợp xấu nhất. Chưa có oracle pass/fail trong requirement; cần Leader/Dev chốt mức chấp nhận trước khi dùng TC làm tiêu chí nghiệm thu, vì hạn mức server hiện tại có thể chặn toàn bộ 5 cảnh báo thật. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-23 | SEC-002 | Abnormal | Log và cảnh báo lỗi JS không được chứa thông tin thẻ, mã xác thực hay mã bảo vệ biểu mẫu | Môi trường local chạy nhánh fix `ai_fixbug_41025`. Bot test cấu hình cổng thanh toán thẻ môi trường thử; sự kiện bật thu phí. Đọc được file log ứng dụng và nội dung công việc gửi cảnh báo. Cần dựng được một lỗi được báo về XẢY RA SAU KHI khách đã nhập thẻ (ví dụ chặn request lấy thông tin khách ở tầng vận chuyển khi khách đang ở bước thẻ). Đã đánh dấu mốc trong file log. | 1. Đánh dấu mốc trong file log ứng dụng.<br>2. Mở trang đăng ký sự kiện trên trình duyệt, đi tới bước nhập thẻ và nhập đầy đủ thẻ thử của môi trường thử; ghi lại số thẻ đã nhập.<br>3. Làm phát sinh lỗi được báo về ở đúng thời điểm đó (chặn request lấy thông tin khách ở tầng vận chuyển rồi kích hoạt lại).<br>4. Đọc các dòng log mới sinh ra kể từ mốc đã đánh dấu.<br>5. Tìm trong các dòng log đó: số thẻ vừa nhập (đủ hoặc một phần dài hơn 4 chữ số cuối), mã xác thực của cổng thanh toán, mã bảo vệ biểu mẫu của trang, và mã phiên.<br>6. Đọc nội dung công việc gửi cảnh báo và tìm cùng các giá trị trên.<br>7. Ghi lại các loại dữ liệu cá nhân THẬT SỰ có trong log: mã người dùng LINE, địa chỉ IP, thông tin trình duyệt, trang giới thiệu, trích đoạn nội dung phản hồi. | Thẻ thử của môi trường thử đã nhập đủ ở bước thẻ; lỗi được báo về phát sinh ngay sau đó. | Tiêu chí PASS/FAIL: log và nội dung cảnh báo KHÔNG chứa số thẻ (kể cả phần dài hơn 4 chữ số cuối), token/mã xác thực thanh toán, mã bảo vệ biểu mẫu CSRF hoặc mã phiên/session. Việc xuất hiện mã người dùng LINE, địa chỉ IP, user-agent, referrer và trích đoạn response phải được ghi nhận riêng như bằng chứng PII vận hành, không tự động làm testcase fail khi requirement chưa cấm. Người chạy liệt kê đúng dữ liệu thực tế quan sát được; quyết định mốc gỡ log, thời hạn lưu và owner xử lý phải được chốt ở thông tin task, không dùng làm oracle ngầm của testcase. | Chưa test |  | local |  |  |  |  | Studio #18945 (NEW-23) · mã theo quan điểm: TC-SEC002-01 · api · auto · local-only · REQ: REQ-015 · spec: TICKET-41025, diff: order-item.js (thu thập thông tin phản hồi của request hỏng: nội dung phản hồi, tiêu đề, phản hồi dạng JSON), diff: MobileEventBookingController@reportJsError (ghi thêm địa chỉ IP, nới chi tiết lên 4000), redmine: journal ■4.2 (dữ liệu cá nhân mới trong log) · Bản vá nới chi tiết chẩn đoán lên 4000 ký tự và thu thập thêm responseText/responseJSON. Testcase chỉ kết luận bảo mật đối với payment secret, CSRF và session. Danh sách PII còn lại là dữ liệu để Leader/Dev quyết định retention/removal trong extra_info; không trộn quyết định quản trị chưa có requirement vào kết quả pass/fail. · author=AI · status=draft |
| NEW-19 | FUNC-004 | Boundary | Báo lỗi JS cắt đúng độ dài từng phần và không gây lỗi hệ thống với dữ liệu quá dài | Môi trường local chạy nhánh fix `ai_fixbug_41025`. Đọc được file log ứng dụng và nội dung công việc gửi cảnh báo trong hàng đợi. Có mã bảo vệ biểu mẫu. Đã đánh dấu mốc trong file log. Chuẩn bị chuỗi dữ liệu dài có ĐÁNH SỐ THỨ TỰ để xác định chính xác điểm bị cắt, và một bộ dữ liệu dài gồm ký tự tiếng Nhật nhiều byte. | 1. Đánh dấu mốc trong file log ứng dụng.<br>2. Gọi endpoint báo lỗi JS với cờ báo động BẬT, chi tiết chẩn đoán dài 4500 ký tự có đánh số thứ tự, vết thao tác dài 2500 ký tự có đánh số thứ tự, bản tin dài 600 ký tự.<br>3. Đọc mã trạng thái HTTP và nội dung phản hồi.<br>4. Đọc dòng log vừa sinh ra, đếm độ dài phần chi tiết chẩn đoán và phần vết thao tác đã được ghi, xác định vị trí bị cắt theo số thứ tự.<br>5. Đọc nội dung công việc gửi cảnh báo và đếm độ dài phần chi tiết chẩn đoán trong đó.<br>6. Lặp lại bước 2 với bộ dữ liệu toàn ký tự tiếng Nhật nhiều byte (chi tiết chẩn đoán 4500 ký tự tiếng Nhật), rồi đếm lại độ dài đã ghi và kiểm tra không có ký tự nào bị cắt dở thành ký tự lỗi.<br>7. Kiểm tra dòng log vẫn đọc được bình thường, các phần trong dòng log không bị lẫn vào nhau. | Chi tiết chẩn đoán 4500 ký tự (lần 2: 4500 ký tự tiếng Nhật), vết thao tác 2500 ký tự, bản tin 600 ký tự, cờ báo động BẬT. Phép tính tay: log giữ 4000 ký tự chi tiết, cảnh báo giữ 1000 ký tự chi tiết, log giữ 2000 ký tự vết thao tác, bản tin giữ 500 ký tự. | Endpoint trả mã HTTP 200, KHÔNG phát sinh lỗi hệ thống 5xx dù dữ liệu vượt hạn mức. Trong log: phần chi tiết chẩn đoán được giữ ĐÚNG 4000 ký tự đầu, phần vết thao tác giữ ĐÚNG 2000 ký tự đầu, bản tin giữ ĐÚNG 500 ký tự đầu — xác định được bằng số thứ tự trong chuỗi. Trong nội dung cảnh báo: phần chi tiết chẩn đoán chỉ còn ĐÚNG 1000 ký tự đầu (bản rút gọn), không phải 4000. Với bộ dữ liệu tiếng Nhật, số ký tự giữ lại vẫn là 4000 KÝ TỰ (cắt theo ký tự, không theo byte) và không có ký tự nào bị cắt dở thành ký tự lỗi. Dòng log vẫn đọc được, các phần không bị lẫn vào nhau. | Chưa test |  | local |  |  |  |  | Studio #18941 (NEW-19) · mã theo quan điểm: TC-FUNC004-02 · api · auto · local-only · REQ: REQ-010 · spec: TICKET-41025, diff: MobileEventBookingController@reportJsError (chi tiết 4000 cho log / 1000 cho cảnh báo, vết thao tác 2000, bản tin 500) · Ba hạn mức khác nhau trong cùng một endpoint nên phải đo tách bạch: 4000 cho log là phần được NỚI trong bản vá (từ 1000) vì chi tiết chẩn đoán nay kèm cả thông tin phản hồi của request hỏng; 1000 cho cảnh báo được giữ nguyên có chủ ý. Biến thể tiếng Nhật kiểm việc cắt theo ký tự nhiều byte (catalog DI-01/DATA-TEXT-001) — cắt theo byte sẽ sinh ký tự lỗi ở cuối chuỗi log. Đồng thời TC này là ca vệ sinh lỗi: dữ liệu quá hạn mức phải bị cắt êm, không được thành lỗi hệ thống. · author=AI · status=draft |
| NEW-21 | API-CONTRACT-001 | Boundary | Khoá trùng cảnh báo gom nhiều khách cùng lỗi thành một tin, nhưng nhánh chỉ-ghi-log không bị gom | Môi trường local chạy nhánh fix `ai_fixbug_41025`. Đọc được file log và hàng đợi công việc; xoá được bộ đệm khoá trùng hoặc chờ hết hiệu lực. Có mã bảo vệ biểu mẫu. Chuẩn bị hai địa chỉ trang đăng ký chỉ KHÁC NHAU ở phần mã người dùng ở cuối, và một địa chỉ trang KHÁC hẳn. Đã đánh dấu mốc trong file log và đếm số công việc gửi cảnh báo đang chờ. Dùng bản tin chưa từng gửi trong 10 phút gần nhất. | 1. Đánh dấu mốc trong file log và đếm số công việc gửi cảnh báo đang chờ.<br>2. Gọi endpoint với cờ báo động BẬT, bản tin X, địa chỉ trang có mã người dùng thứ nhất. Ghi lại phản hồi.<br>3. Gọi lại ngay với cùng cờ BẬT, cùng bản tin X, nhưng địa chỉ trang có mã người dùng THỨ HAI. Ghi lại phản hồi.<br>4. Đếm số công việc gửi cảnh báo đã tăng thêm sau hai lần gọi.<br>5. Gọi lại với cờ BẬT, cùng bản tin X, nhưng địa chỉ trang KHÁC hẳn (không cùng phần đầu đường dẫn). Ghi lại phản hồi và số công việc tăng thêm.<br>6. Gọi endpoint BA LẦN liên tiếp với cờ báo động TẮT, cùng một bản tin Y và cùng một địa chỉ trang.<br>7. Đọc file log và đếm số dòng log tương ứng bản tin Y. | Bản tin X dùng cho ba lần gọi có cờ BẬT: lần 1 và lần 2 cùng phần đầu đường dẫn nhưng khác mã người dùng, lần 3 đường dẫn khác hẳn. Bản tin Y dùng cho ba lần gọi có cờ TẮT, cùng địa chỉ trang. Phép tính tay: cờ BẬT sinh 1 cảnh báo cho cặp lần 1+2 và thêm 1 cảnh báo cho lần 3 (tổng 2); cờ TẮT sinh 3 dòng log. | Lần gọi thứ nhất trả phản hồi thành công và đẩy 1 công việc gửi cảnh báo. Lần gọi thứ hai trả phản hồi thành công CÓ dấu hiệu bị khoá trùng và KHÔNG đẩy thêm công việc nào — chứng tỏ mã người dùng ở cuối đường dẫn đã được loại khỏi khoá, nên 100 khách gặp cùng lỗi trên cùng trang chỉ sinh 1 tin cảnh báo. Lần gọi thứ ba (đường dẫn khác hẳn) đẩy thêm 1 công việc, tổng cộng 2 công việc cho cả ba lần. Ba lần gọi có cờ báo động TẮT đều ghi log, cho ĐÚNG BA dòng log riêng biệt — nhánh chỉ-ghi-log KHÔNG được khoá trùng vì cần biết sự cố lặp mấy lần. | Chưa test |  | local |  |  |  |  | Studio #18943 (NEW-21) · mã theo quan điểm: TC-APICONTRACT001-03 · api · auto · local-only · REQ: REQ-011 · spec: TICKET-41025, source: MobileEventBookingController@reportJsError (khoá trùng theo bản tin + đường dẫn trang đã bỏ mã người dùng, hiệu lực 10 phút), diff: index.blade.php (nhánh chỉ-ghi-log không khoá trùng) · Hành vi khoá trùng có trước bản vá nhưng bản vá thêm nhánh rẽ NGAY TRƯỚC nó, nên phải kiểm lại là nhánh cờ BẬT vẫn đi qua khoá trùng và nhánh cờ TẮT thì không (nhánh cờ TẮT trả về trước khi tới đoạn khoá trùng). Khoá trùng có hiệu lực 10 phút nên phải dùng bản tin mới hoặc xoá bộ đệm trước khi chạy, nếu không kết quả lần 1 sẽ sai. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-24 | API-CONTRACT-001 | Normal | Lấy thông tin khách trả về đúng ba phần như trước và ghi đủ ba dòng log chẩn đoán | Môi trường local chạy nhánh fix `ai_fixbug_41025`. Đọc được file log ứng dụng. Có sự kiện đặt chỗ với ít nhất 3 mục thông tin khách và ít nhất 2 khung giờ; biết trước số mục thông tin và số khung giờ của sự kiện đó. Có một người dùng LINE test đã kết bạn và có ít nhất 1 mục thông tin đã có dữ liệu. Có mã bảo vệ biểu mẫu lấy từ trang đăng ký. Đã đánh dấu mốc trong file log. | 1. Ghi lại số mục thông tin khách và số khung giờ của sự kiện test, và đếm số mục mà người dùng test đã có dữ liệu sẵn.<br>2. Đánh dấu mốc trong file log ứng dụng.<br>3. Gọi endpoint lấy thông tin khách của màn sự kiện với mã sự kiện và mã người dùng LINE của người dùng test.<br>4. Đọc mã trạng thái HTTP và liệt kê ĐẦY ĐỦ các khoá có trong phản hồi.<br>5. Đối chiếu số mục thông tin và số khung giờ trong phản hồi với số đã ghi ở bước 1.<br>6. Đọc file log từ mốc đã đánh dấu, đếm số dòng log mới do lần gọi này sinh ra và ghi lại từng dòng.<br>7. Kiểm tra trong ba dòng log có các trường: mã sự kiện, mã người dùng LINE, cờ mã người dùng rỗng, địa chỉ IP, thông tin trình duyệt, trang giới thiệu, cờ tìm thấy người dùng, số mục thông tin, số khung giờ, số mục đã điền sẵn, thời lượng xử lý. | Mã sự kiện của sự kiện test và mã người dùng LINE của người dùng test đã kết bạn. | Trả mã HTTP 200. Phản hồi có ĐÚNG BA khoá như trước bản vá: trạng thái thành công, danh sách mục thông tin khách, danh sách khung giờ — KHÔNG thêm, KHÔNG bớt khoá nào (bản vá chỉ thêm log, không đổi hợp đồng). Số mục thông tin và số khung giờ trong phản hồi khớp số đã ghi ở bước 1; mục đã có dữ liệu được trả về kèm giá trị. Log sinh ra ĐÚNG BA dòng mức thông tin cho một lần gọi (dòng vào, dòng tra cứu, dòng ra) — không nhiều hơn, để không làm phình log vận hành. Ba dòng đó chứa đủ các trường đã liệt kê ở bước 7; cờ tìm thấy người dùng có giá trị 'tìm thấy', số mục đã điền sẵn khớp số đếm ở bước 1, và thời lượng xử lý là một con số hợp lý (không âm, không rỗng). | Chưa test |  | local |  |  |  |  | Studio #18957 (NEW-24) · mã theo quan điểm: TC-APICONTRACT001-04 · api · auto · local-only · REQ: REQ-014 · spec: TICKET-41025, EP-F4, diff: BookingEventDayController@getFriendInfoEvent (thêm log vào / tra cứu / ra) · Đây là endpoint mà trang gọi ngay khi mở màn đăng ký, và cũng là endpoint bị nghi trong ticket. Log mới dùng để đối chiếu với log truy cập của web server: KHÔNG có dòng vào ⇒ request chưa từng tới server (lỗi tầng vận chuyển phía khách); CÓ dòng vào mà KHÔNG có dòng ra ⇒ request tới được nhưng chết giữa chừng. Vì vậy đếm đúng 3 dòng là yêu cầu vận hành có ý nghĩa, không phải hình thức — vừa để truy vết được, vừa để biết mức tăng log mỗi lượt mở màn. Endpoint thuần đọc nên gọi lại an toàn, không nhân đôi dữ liệu. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| NEW-25 | FRIEND-001 | Abnormal | Lấy thông tin khách với mã người dùng không tra ra được vẫn trả về thành công và ghi rõ trong log | Môi trường local chạy nhánh fix `ai_fixbug_41025`. Đọc được file log ứng dụng. Có sự kiện đặt chỗ TỒN TẠI với ít nhất 3 mục thông tin khách. Có mã bảo vệ biểu mẫu. Đã đánh dấu mốc trong file log. Chuẩn bị một mã người dùng LINE chắc chắn KHÔNG tồn tại trong hệ thống. | 1. Đánh dấu mốc trong file log ứng dụng.<br>2. Biến thể A: gọi endpoint lấy thông tin khách với mã sự kiện hợp lệ và một mã người dùng LINE KHÔNG tồn tại.<br>3. Đọc mã trạng thái HTTP, liệt kê các khoá trong phản hồi và kiểm tra giá trị của từng mục thông tin khách.<br>4. Đọc ba dòng log mới sinh ra và ghi lại giá trị cờ tìm thấy người dùng và cờ mã người dùng rỗng.<br>5. Biến thể B: gọi lại endpoint với mã sự kiện hợp lệ nhưng KHÔNG gửi mã người dùng LINE (hoặc gửi chuỗi rỗng).<br>6. Lặp lại việc đọc phản hồi và ba dòng log, ghi lại giá trị hai cờ nói trên.<br>7. Đối chiếu hai biến thể để xác nhận log phân biệt được hai tình huống. | Biến thể A: mã sự kiện hợp lệ + mã người dùng LINE không tồn tại. Biến thể B: mã sự kiện hợp lệ + không gửi mã người dùng LINE. | Cả hai biến thể trả mã HTTP 200 với trạng thái THÀNH CÔNG (đây là hành vi có từ trước, endpoint không coi đây là lỗi) và vẫn đủ ba khoá như phản hồi bình thường; danh sách mục thông tin khách vẫn có đủ các mục nhưng phần giá trị của chúng RỖNG — nghĩa là khách sẽ thấy form trắng chứ không được điền sẵn. Log phân biệt được hai tình huống: biến thể A có cờ mã người dùng rỗng = không rỗng và cờ tìm thấy người dùng = không tìm thấy; biến thể B có cờ mã người dùng rỗng = rỗng. Số mục đã điền sẵn trong dòng log cuối bằng 0 ở cả hai biến thể. | Chưa test |  | local |  |  |  |  | Studio #18958 (NEW-25) · mã theo quan điểm: TC-FRIEND001-01 · api · auto · local-only · REQ: REQ-014 · spec: TICKET-41025, EP-F4, diff: BookingEventDayController@getFriendInfoEvent (cờ tìm thấy người dùng, cờ mã người dùng rỗng) · Gộp hai biến thể cùng một failure mode (không tra ra người dùng) vào một TC. Đây chính là lý do Dev thêm cờ vào log: ca 'form trắng vì tra không ra người dùng' trước đây không phân biệt được với ca 'khách thật sự chưa có dữ liệu', dẫn tới chẩn đoán sai. Với biến thể B, phía trình duyệt cũng ghi cờ mã người dùng rỗng trong vết thao tác nên hai bên đối chiếu được. Endpoint không trả lỗi nghiệp vụ nào — đó là căn cứ để cơ chế thử lại ở trang được coi là an toàn tuyệt đối. · author=AI · status=draft |
| NEW-26 | TOOL-ERRHYG-001 | Abnormal | Lấy thông tin khách với mã sự kiện không tồn tại trả về danh sách rỗng và ghi log kết thúc sớm | Môi trường local chạy nhánh fix `ai_fixbug_41025`. Đọc được file log ứng dụng. Có mã bảo vệ biểu mẫu. Đã đánh dấu mốc trong file log. Chuẩn bị một mã sự kiện chắc chắn KHÔNG tồn tại. | 1. Đánh dấu mốc trong file log ứng dụng.<br>2. Gọi endpoint lấy thông tin khách với một mã sự kiện KHÔNG tồn tại và một mã người dùng LINE hợp lệ.<br>3. Đọc mã trạng thái HTTP, liệt kê các khoá trong phản hồi và giá trị của từng khoá.<br>4. Đọc file log từ mốc đã đánh dấu, đếm số dòng log mới và ghi lại nội dung từng dòng.<br>5. Kiểm tra có dòng log báo không tìm thấy sự kiện hay không, và có dòng log tra cứu/dòng ra đầy đủ hay không.<br>6. Gọi thêm một lần với mã sự kiện là chuỗi rỗng và ghi lại kết quả tương tự. | Lần 1: mã sự kiện không tồn tại + mã người dùng LINE hợp lệ. Lần 2: mã sự kiện là chuỗi rỗng. | Trả mã HTTP 200 với trạng thái THÀNH CÔNG, danh sách mục thông tin khách RỖNG và danh sách khung giờ RỖNG; vẫn đủ ba khoá như phản hồi bình thường và KHÔNG phát sinh lỗi hệ thống 5xx. Log chỉ có hai dòng: dòng vào và một dòng báo kết thúc vì không tìm thấy sự kiện — KHÔNG có dòng tra cứu và không có dòng ra đầy đủ, vì hàm đã trả về sớm. Lần gọi với mã sự kiện rỗng cho kết quả tương tự. | Chưa test |  | local |  |  |  |  | Studio #18959 (NEW-26) · mã theo quan điểm: TC-TOOLERRHYG001-04 · api · auto · local-only · REQ: REQ-014 · spec: TICKET-41025, EP-F4, diff: BookingEventDayController@getFriendInfoEvent (nhánh không tìm thấy sự kiện ghi log kết thúc) · Nhánh này quan trọng cho việc đọc log: có dòng vào mà không có dòng ra đầy đủ có thể là hai nguyên nhân hoàn toàn khác nhau — sự kiện không tồn tại (có dòng báo kết thúc riêng) hoặc request chết giữa chừng vì lỗi hệ thống/hết thời gian (không có dòng nào sau dòng vào). TC này chốt dấu hiệu phân biệt để người đọc log không kết luận sai. Bản vá thay dòng log mức gỡ lỗi cũ bằng dòng mức thông tin nên dòng này nay thật sự xuất hiện trong log vận hành. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
