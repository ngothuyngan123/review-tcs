# 05 — Review Report

> Draft cho Leader verify. Chỉ ghi phần THIẾU + việc phải làm.

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | (1) Studio task #315 (ticket 41025, round 1, branch `ai_fixbug_41025`) |
| Tổng số TC review | 32 |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: 0/23 vùng ảnh hưởng đạt OK vì **0/32 TC đã chạy** (xem I1). Nếu bỏ qua lý do "chưa chạy", bộ TC chạm được 21/23 vùng; còn **3 GAP · 3 RISK** như dưới.

> Vùng rà: BUG (1)(2)(3) · F1–F11 · D1–D4 · T1–T5 (file 03) + 4 file / 7 điểm rủi ro trong `dev_impact` Studio (diffStat: 4 file, +441 / −29).

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | BUG (2) — nhánh `res.success=='error'` của `payment()` / `changeBooking()` khi `autoBill = 0` (giữ màn xác nhận) | `diff code` | NEW-2, NEW-8, NEW-6 | `RISK` — đây là nhánh xử lý **mọi** lỗi nghiệp vụ server trả về, nhưng cả 3 TC chỉ dùng **1 trigger duy nhất** (khung giờ hết chỗ 予約がいっぱいです). Chưa có trigger thứ 2, thứ 3, cũng chưa có lỗi chưa biết trước → AP-1 | `[BLOCKER]` |
| G2 | T3 — thanh toán Stripe / UnivaPay đi **đường thành công** sau bản vá (đăng ký + đổi lịch) | `dev-impact` | không có | `GAP` — mọi TC có thẻ (NEW-3/4/5/9, #18927) đều là nhánh lỗi. Bản vá chèn lệnh ghi vết vào đầu các hàm "mở form thẻ / gửi đăng ký / gửi thay đổi", nhưng không TC nào chứng minh khách vẫn trả tiền thành công được → AP-3 | `[MAJOR]` |
| G3 | F6 — `reportBookingEventJsError` đổi chữ ký; caller `window.onerror` (:115) và `unhandledrejection` (:126) | `diff code` | NEW-16 (gọi helper 2 đối số) | `GAP` — không TC nào phát sinh **lỗi JS thật chưa bắt** để chứng minh 2 caller này vẫn báo lỗi với cờ báo động bật và tin cảnh báo vẫn đến Chatwork (RULE-06) — **Leader loại TC đề xuất (2026-09-19), chấp nhận rủi ro** | `[MAJOR]` |
| G4 | BUG (3) — `getFriendInfo request failed` status 0: Dev ghi "CHƯA chứng minh được vì sao kết nối hỏng" | `diff code` | NEW-11, NEW-12 (mô phỏng abort trên trình duyệt) | `RISK` — fix chỉ là giảm nhẹ (retry 1 lần), root cause vẫn mở → AP-2. TC chỉ chứng minh trên lỗi mô phỏng, không có bước đối chiếu trên production xem cảnh báo thật có giảm và log chẩn đoán có thu được dữ liệu không — **Leader loại TC đề xuất (2026-09-19), chấp nhận rủi ro** | `[MAJOR]` |
| G5 | BUG (1)(2) trên **LINE WebView thật** (bug gốc: iOS 18.7 / iOS 26.6.1 / Android 16, LINE 15.9–26.14) | `dev-impact` | NEW-29 (chỉ phần quay lại từ màn kết bạn) | `RISK` — mọi TC tái hiện chính (NEW-2, NEW-8, NEW-3, #18927) chạy bằng trình duyệt ở khung nhìn điện thoại. Chưa có lần nào kiểm nhánh lỗi đăng ký / đổi lịch trong app LINE thật (RULE-06) | `[MAJOR]` |
| G6 | T3 — UnivaPay theo **2 chế độ bill**: kiểu cũ (polling, không webhook) · kiểu mới (callback / webhook) | `dev-impact` | NEW-3 (booking mới, **không chốt chế độ**) · #18927 (đổi lịch, kiểu webhook, trigger vượt hạn mức) | `GAP` — mỗi chế độ trả lỗi về trình duyệt theo đường khác nhau: lỗi ngay khi bấm, hoặc lỗi đến sau qua poll fail / callback fail (kho TC-EBK-236: "quay lại màn nhập thẻ"). Chưa có: đổi lịch kiểu cũ (cả thành công lẫn hết chỗ) · đổi lịch kiểu mới hết chỗ · bill fail đến sau ở cả 2 chế độ. Dev không nói nhánh chờ kết quả UnivaPay có gọi `resetElement` / đổi bước về `payment` không (§6 #5) | `[MAJOR]` |

---

## 2. Thiếu so với quan điểm test

**Kết luận**: 22 quan điểm có Trigger khớp task · **6 chưa cover đủ**. Ngoài ra 18/32 TC mang mã Studio lạ (I3), nên không được tính là cover.

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `FUNC-001` (+ `OUT-TRUTH-001`) | Cao | `GAP` về mã — không TC nào mang mã này. **Nội dung đã có đủ**: NEW-2 / NEW-8 (TC tái hiện chính) mang `TOOL-KNOW-002`, còn NEW-1 / NEW-7 (đối chứng) mang `TOOL-NEGCTRL-001`. Chỉ cần **đổi mã trên Studio**, không cần TC mới | `[BLOCKER]` |
| Q2 | `CONC-001` | Cao | `GAP` — bản vá **mở lại** nút 「申し込む」 / 「変更内容を送信」 ngay trên màn xác nhận sau khi lỗi (trước đây đăng ký bị màn trắng, đổi lịch bị TypeError chặn). Khách gửi lại được, nhưng chưa có TC bấm liên tiếp khi gửi lại. Riêng đổi lịch tạo **cặp bản ghi** (BR-16), nên rủi ro trùng cao hơn | `[BLOCKER]` |
| Q3 | `SEC-001` | Cao | `GAP` — PII mới được ghi vào log: `line_id`, IP, UA, Referer mỗi lượt mở màn, cộng `responseText` / `responseJSON` (có thể chứa tên / SĐT / email). NEW-23 (`SEC-002`) chỉ kết luận về thẻ / CSRF / session, còn PII thì "chỉ ghi nhận". **Không đề xuất TC**, vì chưa có requirement để làm oracle. Chặn bằng §6 #3 | `[BLOCKER]` |
| Q4 | `PAY-STATE-001` | Cao | `RISK` — nhánh lỗi thanh toán có nội dung ở NEW-4 / NEW-5 / NEW-9 nhưng mang mã lạ `RULE-TOOL-028/029`. Đường thành công đang GAP (G2) | `[MAJOR]` |
| Q5 | `REG-SHARED-001` | Cao | `RISK` — hàm dùng chung `resetElement` có 6 caller. Mới chỉ NEW-3 (Abnormal) mang mã đúng; các caller Stripe (NEW-4/5/9) mang mã lạ; thiếu Normal (RULE-01) | `[MAJOR]` |
| Q6 | `INTG-LINE-001` · `SEC-002` · `DEPLOY-ASSET-001` · `LIFF-ENTRY-001` · `FRIEND-001` · `ENV-003` · `REG-SPEC-001` | Cao | `RISK` RULE-01 — mỗi quan điểm chỉ có **1 loại case**: INTG-LINE-001 chỉ Abnormal (NEW-11/12), vì Normal NEW-10 mang mã lạ · SEC-002 chỉ Abnormal (NEW-23) · DEPLOY-ASSET-001 chỉ Normal (NEW-28), dù file bị sửa nằm trong luồng thanh toán → Cao tuyệt đối · LIFF-ENTRY-001 chỉ Abnormal (#18934) · FRIEND-001 chỉ Abnormal (NEW-25) · ENV-003 chỉ Abnormal (NEW-29) · REG-SPEC-001 chỉ Abnormal (#18927). Không TC nào ghi lý do thiếu | `[MAJOR]` |

---

## 3. TC trùng lặp nội dung

Đã rà 32 TC, **không phát hiện trùng lặp**.

- Đã cân nhắc 2 cặp và kết luận **không trùng**:
  - NEW-13 ↔ NEW-29: cùng ý "quay lại từ màn kết bạn thì báo lỗi hoạt động lại". Nhưng NEW-13 dùng bfcache của trình duyệt, còn NEW-29 dùng LINE WebView thật và app bị đẩy xuống nền. Khác mã quan điểm, khác môi trường.
  - NEW-3 ↔ #18927: Univapay, nhưng một bên là đăng ký, một bên là đổi lịch.
- Gate: không đề xuất xóa, nên không ảnh hưởng coverage.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | Cả 32 TC | Task `tc-ready`, `exec` = 0 pass / 0 fail / 32 untested. Chưa TC nào có kết luận, kể cả TC tiền đề NEW-28 (xác nhận trang nạp đúng bản JS đã vá) | Chạy NEW-28 **trước**, rồi chạy toàn bộ bộ TC. Report này chỉ đánh giá thiết kế TC |
| I2 | `[MAJOR]` | NEW-3, NEW-4, NEW-5, NEW-9, #18927 · 11 TC API (NEW-16 → NEW-26) | **RULE-08 / ENV-003**: bug phát hiện ở **production** (`s.lmes.jp`), task chạm **thanh toán** Stripe / UnivaPay, nhưng 0 TC có kế hoạch chạy product. Riêng 11 TC API khai `env_scope = local`: mức log, driver hàng đợi và room Chatwork ở local có thể khác staging / production | Thêm ít nhất 1 lượt smoke product cho nhánh lỗi thanh toán (TC-ENV003-01 đã bị Leader loại 2026-09-19). Mở `env_scope` của TC API sang staging |
| I3 | `[MAJOR]` | 18 TC mang 7 mã lạ | Các mã này không có trong `checklist-lme.md`, nên không được tính là cover: `TOOL-ERRHYG-001` (NEW-15/20/22/26) · `API-CONTRACT-001` (NEW-16/17/21/24) · `TOOL-NEGCTRL-001` (NEW-1/7/10) · `TOOL-KNOW-002` (NEW-2/8) · `RULE-TOOL-029` (NEW-4/5) · `RULE-TOOL-028` (NEW-9) · `OBS-001` (NEW-14/18) | Đổi mã trên Studio (`testcase_update`). Gợi ý: NEW-1/2/7/8 → `FUNC-001` · NEW-4/5/9 → `PAY-STATE-001` hoặc `REG-SHARED-001` · NEW-10 → `INTG-LINE-001` · NEW-16 → `DEPLOY-LIVE-001` · NEW-17/21/24 → `OUT-TRUTH-001` · NEW-15 → `DEPLOY-ASSET-001` · NEW-20/22/26 → `FUNC-004` / `UI-003` · NEW-14/18 → `FUNC-001` |
| I4 | `[MAJOR]` | `03-dev-impact.md` | (a) Auto-fill `by /new-task` nhưng checkbox "Tester verify auto-fill chính xác" **chưa tick**. (b) Đánh giá do **AI auto-fixbug** viết, Dev ghi verify ở mức **lint** và "chưa chạy được trên LINE thật". (c) 2 journal lệch nhau: trần `detail` phía server 1000→4000 (#137108) hay 2000→4000 (#137091); số điểm ghi vết là 13 hay 14 (Studio REQ-012 lại ghi 10) | Tester đọc lại Redmine, tick checkbox. Hỏi Dev để chốt 2 con số, xem §6 #4 |
| I5 | `[MAJOR]` | Cả 32 TC | `spec_status` rỗng hoặc `needs-human-review`. Không TC nào ghi đã đối chiếu spec hay đã hỏi ai, trong khi nhiều expected tự suy từ code (vd NEW-19 "bản tin giữ 500 ký tự" không có trong mục 2 / 4.2 của Dev) | Điền `Trạng thái đánh giá spec` cho từng TC. Riêng các con số giới hạn phải hỏi Dev |
| I6 | `[MAJOR]` | NEW-22 | Expected ghi rõ "KHÔNG tự kết luận đạt hay không đạt", tức **không có oracle**. TC cũng đo ra mâu thuẫn thật: client cho phép 5 lỗi thật + 10 chỉ-ghi-log, trong khi server `throttle:10,1` → 5 cảnh báo lỗi thật có thể mất hết | Dev / Leader chốt mức chấp nhận (§6 #2) rồi mới dùng TC làm tiêu chí nghiệm thu |
| I7 | `[MAJOR]` | #18934, NEW-12 | Không atomic. #18934 gộp 3 mục đích: không báo động giả khi sang màn kết bạn (#41025) + cuộn trang ở 3 điểm vào (#40806) + đặt chỗ hoàn tất. NEW-12 gộp: số lần retry + thời điểm hiện hộp thoại + tách `notify` + đủ 9 thuộc tính `collectXhrInfo` | Tách phần #41025 của #18934 thành TC riêng. Tách phần `collectXhrInfo` của NEW-12 ra TC riêng |
| I8 | `[MAJOR]` | NEW-19, NEW-14 | Boundary không nằm đúng biên: NEW-19 chỉ gửi giá trị vượt (4500 / 2500 / 600), NEW-14 chỉ kiểm "> 40 bước". Nếu off-by-one ở 4000/4001 thì không bắt được | Thêm cặp giá trị đúng biên: detail 4000 / 4001, bản tin cảnh báo 1000 / 1001, steps 2000 / 2001, vết thao tác 40 / 41 bước |
| I9 | `[MAJOR]` | NEW-16, NEW-17, NEW-18, NEW-21 | **RULE-06**: T2 (cảnh báo Chatwork) là output ra ngoài, nhưng expected dừng ở "đếm job trong hàng đợi" | Ít nhất 1 TC kiểm tin đến room Chatwork test. Leader đã loại TC đề xuất TC-REGSHARED001-01 (2026-09-19) — chấp nhận rủi ro |
| I10 | `[MAJOR]` | [AP-1] G1 | Nhánh lỗi chung chỉ test 1 trigger | Xem TC-FUNC001-01/02/03 ở §5 |
| I11 | `[MAJOR]` | [AP-2] G4 | Root cause của lỗi (3) chưa chứng minh; TC chỉ phủ phần giảm nhẹ | Leader đã loại TC-ENV003-01 (2026-09-19). Còn lại: hỏi Dev mốc đánh giá lại root cause khi có log chẩn đoán |
| I12 | `[MAJOR]` | [AP-3] G2 | Regression T3 chỉ có nhánh lỗi, không có đường thành công | Dùng lại kho TC-EBK-210 / 218 / 230 (§5) |
| I13 | `[MAJOR]` | NEW-3 | Tiền đề chỉ ghi "đã cấu hình cổng Univapay môi trường thử", không chốt bot chạy bill kiểu cũ (polling) hay kiểu mới (webhook). Chạy xong cũng không biết đã phủ chế độ nào | Sửa tiền đề NEW-3 trên Studio: chốt **kiểu mới (webhook)**. Kiểu cũ đã có TC-PAYSTATE001-01 ở §5 |
| I14 | `[MINOR]` | #18924 | Map `FUNC-003` (field có định dạng), nhưng nội dung là kiểm **thiếu dữ liệu bắt buộc** | Đổi sang `FUNC-002` |
| I15 | `[MINOR]` | NEW-15 | Cho phép thay bằng "kiểm tra tĩnh trên mã nguồn" nếu không vô hiệu hoá được khối script. Như vậy là đọc code, không còn là test hành vi | Nếu dùng fallback thì ghi `Kết quả thực thi` = Chưa test, kèm lý do; không tính là Đạt |

---

## 5. TCs đề xuất bổ sung (12)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | [kho-tcs/fa021-eventbooking-イベント予約.md](../../kho-tcs/fa021-eventbooking-イベント予約.md) (nhóm "LINE user — mở link & entry", "Thanh toán — Stripe / UnivaPay / 3D Secure") |
| Vùng regression phát hiện từ kho | Thanh toán thành công: TC-EBK-210 (Stripe đăng ký), TC-EBK-218 (Stripe đổi lịch 全承認 có tiền), TC-EBK-230 (UnivaPay). Double-click: TC-EBK-155 (đặt chỗ), TC-EBK-223 (màn thanh toán) |
| Conflict expected vs kho | #18927 sửa expected của case kho Studio **EB-019**; #18933 sửa **EB-032**; #18934 sửa **EB-038** → §6 #1 |
| GAP dùng lại TC kho (không viết mới) | **G2 / Q4** → TC-EBK-210 "Bill Stripe THÀNH CÔNG → cập nhật status_payment = 1…" · TC-EBK-218 "Đổi lịch 全承認 CÓ tiền — bill thành công…" · TC-EBK-230 "Bill kiểu CŨ (polling)… bill thành công". Chạy nguyên văn trên branch `ai_fixbug_41025`, thêm bước đọc console (không có lỗi JS). **Q2 (đăng ký)** → TC-EBK-155 "Double-click nút đặt chỗ → KHÔNG tạo booking trùng": đổi tiền đề thành "vừa gặp lỗi hết chỗ, đã quay lại chọn khung giờ khác". Nhánh đổi lịch kho chưa có nên viết mới (TC-CONC001-01). **G6 (UnivaPay đường thành công + timeout)** → chạy lại TC-EBK-230 "Bill kiểu CŨ… bill thành công" · TC-EBK-235 "Bill kiểu MỚI — callback success trong 2 phút" · TC-EBK-239 "Đổi lịch 全承認 kiểu mới…" · TC-EBK-237 "Quá 2 phút chưa có callback → màn chờ" (Boundary). Chạy trên branch `ai_fixbug_41025`, thêm bước đọc console: không TypeError `clear` |
| Xác nhận chống trùng | Đã đối chiếu 32 TC ở BƯỚC 0 + kho FA-021: không TC đề xuất nào trùng. TC-FUNC001-01/02/03 dùng trigger khác "hết chỗ khung giờ" của NEW-2/8/6. TC-INTGLINE001-01 khác NEW-29 (NEW-29 chỉ kiểm quay lại từ màn kết bạn). Nhóm UnivaPay: TC-PAYSTATE001-01 khác NEW-3 ở chế độ (NEW-3 sẽ chốt kiểu mới, I13). TC-PAYSTATE001-03 khác #18927 ở trigger (hết chỗ vs vượt hạn mức). TC-INTGHOOK001-01/02 và TC-PAYSTATE001-05 dẫn từ kho TC-EBK-236 / 240 / 231 nhưng thêm oracle của bản vá (console, form thẻ dùng lại được, gửi lại được) mà kho không có |
| Input thiếu | Kho và spec **không ghi cách chuyển bot giữa kiểu cũ và kiểu mới** (setting nào, ở màn nào). Tiền đề 7 TC UnivaPay dưới đây ghi theo cách gọi của kho; cách dựng cụ thể cần hỏi Dev (§6 #5) |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-FUNC001-01 | UI | FUNC-001 | LINE user — đăng ký sự kiện (màn xác nhận & nhánh báo lỗi) | Abnormal | auto | Tất cả | Đăng ký sự kiện miễn phí: コース hết chỗ trong khi khung giờ còn chỗ → vẫn giữ màn xác nhận và gửi lại được | - Môi trường chạy branch `ai_fixbug_41025`<br>- Sự kiện KHÔNG bật 決済, slot S1 全承認, 定員 slot = 10<br>- S1 có 2 コース: P1 定員 = 1, P2 còn chỗ<br>- 2 người dùng LINE test A, B đã kết bạn<br>- Mở trang ở khung nhìn điện thoại, bật console | 1. A mở link đăng ký, chọn S1 + コース P1, số lượng 1, nhập đủ thông tin, bấm 「予約確認にすすむ」, dừng ở màn xác nhận (chưa bấm gửi)<br>2. B đăng ký hoàn tất S1 + P1 để lấy chỗ cuối của コース<br>3. A bấm 「申し込む」, đọc nguyên văn hộp thoại rồi bấm OK<br>4. Không tải lại trang, quan sát màn hình<br>5. Đọc console<br>6. A bấm 「戻る」, đổi sang P2, đi lại tới màn xác nhận, bấm 「申し込む」<br>7. Xem 「予約履歴」 của A và số chỗ đã dùng của P1 / P2 trên màn admin | コース P1 định員 1, bị B lấy trước; slot S1 vẫn còn chỗ | - Bước 3: hộp thoại có nội dung lỗi (ghi nguyên văn), không phải hộp thoại trắng<br>- Bước 4: vẫn là màn 「予約内容確認」 với đủ ngày giờ / コース / số lượng / thông tin đã nhập; nút 「申し込む」 bấm được; 「戻る」 bấm được. Không màn trắng, không nhảy sang 「カード情報入力」<br>- Bước 5: không có TypeError đọc thuộc tính `clear` của null<br>- Bước 6–7: A đặt thành công P2 trong cùng phiên; P1 chỉ có đơn của B, P2 +1 | | Lấp G1 (trigger 2 — định員 コース, BR-07) · Đánh giá spec: Spec ghi rõ (BR-07 định員 コース) · Evidence: video màn hình + ảnh console + ảnh 予約履歴 |
| TC-FUNC001-02 | UI | FUNC-001 | LINE user — đổi lịch sự kiện (màn xác nhận thay đổi & nhánh báo lỗi) | Abnormal | auto | Tất cả | Đổi lịch sự kiện miễn phí: slot đích bị admin tắt / xoá khi khách đang ở màn xác nhận thay đổi → khối xác nhận vẫn còn, sửa được | - Branch `ai_fixbug_41025`<br>- Sự kiện KHÔNG bật 決済, 予約変更 = 全承認, còn trong 変更受付期限<br>- Người dùng A có đơn ở slot S1; slot S2, S3 còn chỗ<br>- Có tài khoản admin song song ở trình duyệt khác<br>- Bật console | 1. A mở 「予約履歴」 → đơn S1 → 「予約内容変更」 → chọn S2, đi tới màn 「変更内容の最終確認をしてください」 (chưa gửi)<br>2. Admin xoá slot S2 (hoặc tắt hiển thị nếu slot không xoá được) ở màn 予約枠<br>3. A bấm 「変更内容を送信」, đọc nguyên văn hộp thoại rồi đóng<br>4. Không tải lại trang, quan sát khối xác nhận thay đổi<br>5. Đọc console<br>6. A bấm 「変更内容を修正」 → chọn S3 → 「変更内容を送信」<br>7. Xem đơn của A và số chỗ đã dùng của S1 / S3 | Slot đích S2 bị admin xoá / tắt sau khi A đã ở màn xác nhận | - Bước 3: có hộp thoại mang nội dung lỗi (ghi nguyên văn). **Nếu server nhận đơn vào S2 đã xoá mà không báo lỗi → ghi Không đạt và báo Dev**<br>- Bước 4: khối xác nhận thay đổi vẫn còn (dòng 「まだ変更は完了していません」, nội dung thay đổi, 2 nút 「変更内容を送信」 / 「変更内容を修正」 bấm được). Không màn trắng, không phải đóng / mở lại link<br>- Bước 5: không có TypeError<br>- Bước 6–7: đổi sang S3 thành công; S1 −1, S3 +1; không sinh bản ghi thừa | | Lấp G1 (trigger 3 — lỗi **chưa biết trước**, nhánh đổi lịch là hồi quy nặng nhất do `v-if` huỷ khối khỏi DOM) · Đánh giá spec: Spec không ghi (message khi slot bị xoá) — cần hỏi Dev · Evidence: video + console |
| TC-FUNC001-03 | UI | FUNC-001 | LINE user — đăng ký sự kiện (màn xác nhận & nhánh báo lỗi) | Abnormal | auto | Tất cả | Đăng ký sự kiện miễn phí chỉ cho đặt 1 lần: gửi lần 2 từ tab thứ hai → báo lỗi và vẫn giữ màn xác nhận | - Branch `ai_fixbug_41025`<br>- Sự kiện KHÔNG bật 決済, 「予約可能回数」 = chỉ cho đặt 1 lần (BR-11), slot S1 còn ≥ 2 chỗ<br>- Người dùng A đã kết bạn, chưa có đơn<br>- Mở link đăng ký ở **2 tab** cùng phiên đăng nhập, bật console | 1. Tab 1 và tab 2: chọn S1, nhập đủ thông tin, dừng ở màn xác nhận<br>2. Tab 1: bấm 「申し込む」 → hoàn tất<br>3. Tab 2: bấm 「申し込む」, đọc nguyên văn hộp thoại rồi đóng<br>4. Quan sát tab 2: còn màn xác nhận và nút bấm được không<br>5. Đọc console tab 2<br>6. Đếm đơn của A trong 「予約履歴」 và số chỗ đã dùng của S1 | 「予約可能回数」 = 1 lần; 2 tab cùng người dùng | - Bước 3: hộp thoại có nội dung lỗi (ghi nguyên văn)<br>- Bước 4: vẫn là màn xác nhận với nội dung + nút 「戻る」 bấm được; không màn trắng<br>- Bước 5: không có TypeError<br>- Bước 6: A chỉ có **1** đơn; S1 chỉ +1<br>- Nếu tab 2 đặt thành công (A có 2 đơn) → ghi Không đạt, báo Leader (BR-11 bị bypass, ngoài phạm vi #41025) | | Lấp G1 (trigger 4 — BR-11 phía server) · Đánh giá spec: Spec không ghi (spec BR-11 chỉ nói FE ẩn slot, chưa rõ server có chặn không) — cần hỏi Dev · Evidence: video 2 tab + console + 予約履歴 |
| TC-CONC001-01 | UI | CONC-001 | LINE user — đổi lịch sự kiện (màn xác nhận thay đổi) | Abnormal | auto | Tất cả | Đổi lịch sau khi gặp lỗi hết chỗ: bấm liên tiếp 「変更内容を送信」 khi gửi lại → chỉ đổi lịch 1 lần | - Branch `ai_fixbug_41025`<br>- Sự kiện KHÔNG bật 決済, 予約変更 = 全承認<br>- A có đơn ở S1; S2 còn đúng 1 chỗ; S3 còn ≥ 2 chỗ<br>- Người dùng B để lấy chỗ S2<br>- Đã ghi số chỗ đã dùng của S1 / S2 / S3 | 1. A đổi lịch S1 → S2, dừng ở màn xác nhận thay đổi<br>2. B đặt hết chỗ S2<br>3. A bấm 「変更内容を送信」, đóng hộp thoại lỗi<br>4. A bấm 「変更内容を修正」 → chọn S3 → tới màn xác nhận<br>5. Bấm 「変更内容を送信」 2 lần liên tiếp thật nhanh (< 300ms)<br>6. Xem 「予約履歴」 của A, danh sách người tham gia S1 / S3 trên màn admin, số chỗ đã dùng | Double-tap < 300ms ở lần gửi lại | - Chỉ có **1** thay đổi được ghi nhận: đơn của A nằm ở S3, không có 2 đơn S3 hay đơn S1 còn sót<br>- S1 −1, S3 **+1** (không +2), S2 chỉ có đơn của B<br>- Không có hộp thoại lỗi thứ 2 làm mất khối xác nhận; không TypeError | | Lấp Q2 · dẫn từ TC-EBK-155 (kho chỉ có double-click lần đặt đầu) · Đánh giá spec: Spec ghi rõ (BR-16 cặp booking `update_to`) · Evidence: video + ảnh danh sách người tham gia |
| TC-INTGLINE001-01 | UI | INTG-LINE-001 | LINE user — đăng ký / đổi lịch sự kiện trên LINE app | Abnormal | manual | Tất cả | Trên LINE app thật (iPhone + Android): sự kiện miễn phí gặp lỗi hết chỗ khi đăng ký và khi đổi lịch → giữ màn xác nhận, không kẹt | - Môi trường đã deploy `ai_fixbug_41025`, đã chạy NEW-28<br>- 1 iPhone (iOS 18+) và 1 Android (Android 16 nếu có), cài LINE bản hiện hành<br>- Sự kiện miễn phí, slot S2 còn đúng 1 chỗ, S3 còn chỗ; người dùng A (thiết bị thật) có sẵn 1 đơn ở S1 để đổi lịch<br>- Người dùng B (trình duyệt) để lấy chỗ | 1. Trên LINE của A: mở link sự kiện từ chat bot, chọn S2, đi tới màn xác nhận đăng ký (chưa gửi)<br>2. B đặt hết S2<br>3. A bấm 「申し込む」 → đóng hộp thoại → quan sát màn hình<br>4. A bấm 「戻る」, chọn S3, gửi lại<br>5. Trả S2 về còn 1 chỗ; A mở 「予約履歴」 → đơn S1 → 「予約内容変更」 → chọn S2 → màn xác nhận thay đổi<br>6. B đặt hết S2; A bấm 「変更内容を送信」 → đóng hộp thoại → quan sát<br>7. A bấm 「変更内容を修正」 → S3 → gửi lại<br>8. Lặp toàn bộ trên thiết bị còn lại | Sự kiện miễn phí; S2 hết chỗ trước khi A gửi | - Cả 2 thiết bị, cả đăng ký và đổi lịch: sau khi đóng hộp thoại vẫn là màn xác nhận (đăng ký) / khối xác nhận thay đổi (đổi lịch), nút bấm được<br>- Không màn trắng chỉ còn ảnh sự kiện; không phải đóng / mở lại LINE<br>- Gửi lại S3 thành công ngay trong cùng phiên | | Lấp G5 · manual vì thiết bị thật (LINE WebView iOS / Android, RULE-06) · Đánh giá spec: Spec ghi rõ (SCR-EBD-20 page `confirm`) · Evidence: quay màn hình thiết bị |
| TC-PAYSTATE001-01 | UI | PAY-STATE-001 | Thanh toán — UnivaPay (booking mới, kiểu cũ) | Abnormal | auto | Tất cả | UnivaPay kiểu cũ (không webhook) — booking mới gặp hết chỗ khi bấm 「決済する」 → về bước nhập thẻ, form UnivaPay dùng lại được, không trừ tiền | - Branch `ai_fixbug_41025`<br>- Bot liên kết UnivaPay môi trường thử, dùng cơ chế bill **kiểu cũ (polling, không callback)**<br>- Sự kiện bật 決済 = UnivaPay, slot S1 còn đúng 1 chỗ, コース P1 料金 > 0, S2 còn chỗ<br>- Bot không ở gói Free (nút thanh toán hiện ra)<br>- 2 người dùng test A, B đã kết bạn<br>- Bật console; ghi số chỗ đã dùng của S1 | 1. A mở link, chọn S1 + P1, nhập đủ thông tin → 「予約確認にすすむ」 → 「申し込む」 → nhập thẻ thử hợp lệ của UnivaPay → tới màn xác nhận thanh toán (chưa bấm 「決済する」)<br>2. B đặt hết chỗ S1<br>3. A bấm 「決済する」, đọc nguyên văn hộp thoại rồi đóng<br>4. Quan sát màn hình: đang ở bước nào, form thẻ UnivaPay nhập được không<br>5. Đọc console<br>6. A chọn lại S2, nhập lại thẻ, bấm 「決済する」<br>7. Xem 「予約履歴」 của A, số chỗ S1 / S2, giao dịch trên dashboard UnivaPay môi trường thử | Thẻ thử hợp lệ của UnivaPay (tra tài liệu UnivaPay mới nhất — RULE-05); S1 bị B lấy chỗ cuối trước khi A bấm | - Bước 3: hộp thoại có nội dung lỗi (ghi nguyên văn), không phải hộp thoại trắng<br>- Bước 4: về 「カード情報入力」, form UnivaPay hiển thị và nhập được; khách còn nút đi tiếp; không màn trắng<br>- Bước 5: không có TypeError đọc `clear` của null<br>- Bước 6–7: A đặt S2 thành công; **không có đơn S1 của A**; S1 chỉ có đơn của B; dashboard UnivaPay **không có giao dịch thành công** cho lần bấm ở bước 3, chỉ có giao dịch S2 | | Lấp G6 (booking mới × kiểu cũ) · Đánh giá spec: Spec không ghi (hành vi FE sau lỗi theo mô tả fix Dev) · Evidence: video + console + ảnh dashboard UnivaPay |
| TC-PAYSTATE001-02 | UI | PAY-STATE-001 | Thanh toán — UnivaPay (đổi lịch, kiểu cũ) | Abnormal | auto | Tất cả | UnivaPay kiểu cũ (không webhook) — đổi lịch gặp hết chỗ khi bấm 「決済する」 → về bước nhập thẻ, đơn cũ giữ nguyên, không trừ tiền | - Branch `ai_fixbug_41025`<br>- Bot dùng UnivaPay **kiểu cũ**; sự kiện bật 決済 = UnivaPay, 予約変更 = 全承認, còn trong 変更受付期限<br>- A có đơn đã thanh toán ở S1 (コース P_a); slot đích S2 còn đúng 1 chỗ với コース P_b 料金 cao hơn P_a (có phát sinh tiền); S3 còn chỗ<br>- Người dùng B để lấy chỗ S2; bật console | 1. A mở 「予約履歴」 → đơn S1 → 「予約内容変更」 → chọn S2 + P_b → nhập thẻ thử hợp lệ → tới màn xác nhận thanh toán (chưa bấm)<br>2. B đặt hết S2<br>3. A bấm 「決済する」, đọc nguyên văn hộp thoại rồi đóng<br>4. Quan sát màn hình và form thẻ UnivaPay<br>5. Đọc console<br>6. A chọn S3, nhập lại thẻ, gửi lại<br>7. Xem đơn của A, số chỗ S1 / S2 / S3, dashboard UnivaPay | Đổi sang コース có phát sinh tiền; S2 bị B lấy chỗ cuối | - Bước 3: hộp thoại có nội dung lỗi<br>- Bước 4: về 「カード情報入力」, form UnivaPay nhập được; không màn trắng, không phải đóng / mở lại link<br>- Bước 5: không TypeError<br>- Sau bước 3: đơn của A **vẫn ở S1** với thông tin cũ; S2 chỉ có đơn B; dashboard **không có giao dịch thành công** cho lần bấm ở bước 3<br>- Bước 6–7: đổi sang S3 thành công; S1 −1, S3 +1 | | Lấp G6 (đổi lịch × kiểu cũ) · Đánh giá spec: Spec ghi rõ (BR-16 cặp booking) · Evidence: video + console + ảnh dashboard |
| TC-PAYSTATE001-03 | UI | PAY-STATE-001 | Thanh toán — UnivaPay (đổi lịch, kiểu mới) | Abnormal | auto | Tất cả | UnivaPay kiểu mới (webhook) — đổi lịch gặp hết chỗ khi bấm 「決済する」 → về bước nhập thẻ, không treo ở màn chờ callback, không trừ tiền | - Branch `ai_fixbug_41025`<br>- Bot dùng UnivaPay **kiểu mới (callback / webhook)**; môi trường nhận được webhook UnivaPay (local thường không nhận được → chạy dev / staging)<br>- Còn lại giống TC-PAYSTATE001-02 | 1–7. Giống TC-PAYSTATE001-02<br>8. Sau bước 3, chờ thêm 2 phút: quan sát có hiện màn chờ 「決済処理を行っています」 hoặc có tin 「決済に失敗しました…」 gửi tới LINE của A không | Đổi sang コース có phát sinh tiền; S2 bị B lấy chỗ cuối | - Giống TC-PAYSTATE001-02 (hộp thoại có nội dung · về 「カード情報入力」 · không TypeError · đơn cũ giữ nguyên · không giao dịch thành công · gửi lại S3 được)<br>- Bước 8: **không** hiện màn chờ callback và **không** có tin thanh toán thất bại, vì lần bấm bị chặn trước khi thu tiền. Nếu có → ghi lại nguyên văn, báo Dev | | Lấp G6 (đổi lịch × kiểu mới × hết chỗ; #18927 chỉ phủ trigger vượt hạn mức) · Đánh giá spec: Spec không ghi (bước 8) — cần hỏi Dev · Evidence: video + console + ảnh LINE + dashboard |
| TC-PAYSTATE001-04 | UI | PAY-STATE-001 | Thanh toán — UnivaPay (đổi lịch, kiểu cũ) | Normal | manual | product | UnivaPay kiểu cũ (không webhook) — đổi lịch có phát sinh tiền thành công sau bản vá | - Production đã release bản vá, đã chạy NEW-28<br>- Bot dùng UnivaPay **kiểu cũ**; sự kiện bật 決済 = UnivaPay, 予約変更 = 全承認<br>- A có đơn đã thanh toán ở S1 (P_a); S2 còn chỗ, コース P_b 料金 cao hơn P_a<br>- Bật console (trình duyệt) hoặc quay màn hình (thiết bị) | 1. A 「予約履歴」 → đơn S1 → 「予約内容変更」 → chọn S2 + P_b → nhập thẻ thật / thẻ test của môi trường → bấm 「決済する」<br>2. Chờ kết quả (< 5 phút)<br>3. Quan sát màn hình phía A<br>4. Xem đơn của A trên 「予約履歴」 và màn admin 予約詳細<br>5. Xem giao dịch trên dashboard UnivaPay + tin nhắn LINE của A | Đổi lịch có phát sinh tiền, bill thành công | - Không hộp thoại lỗi, không TypeError<br>- Đơn của A chuyển sang S2 + P_b; S1 −1, S2 +1<br>- Dashboard có **đúng 1** giao dịch thành công với số tiền của P_b theo quy tắc đổi lịch<br>- A nhận action / remind theo cấu hình (nếu có) | | Lấp G6 (đổi lịch × kiểu cũ × thành công — kho chưa có) · regression · RULE-08 bill tiền · manual vì môi trường production · Đánh giá spec: Spec ghi rõ (BR-16) · Evidence: video + ảnh dashboard + ảnh 予約詳細 |
| TC-PAYSTATE001-05 | UI | PAY-STATE-001 | Thanh toán — UnivaPay (booking mới, kiểu cũ) | Abnormal | manual | product | UnivaPay kiểu cũ — bill thất bại được poll trả về → hiện lỗi, khách nhập lại thẻ được, không TypeError, booking bị xoá và hoàn chỗ | - Production đã release bản vá<br>- Bot dùng UnivaPay **kiểu cũ**; sự kiện bật 決済 = UnivaPay, slot S1 全承認, コース P1 料金 > 0<br>- Có thẻ sẽ bị UnivaPay từ chối (RULE-05: tra tài liệu UnivaPay mới nhất)<br>- Ghi số chỗ đã dùng của S1 / P1; bật console | 1. A đặt S1 + P1, nhập thẻ bị từ chối → 「決済する」<br>2. Chờ kết quả (< 5 phút)<br>3. Đọc nguyên văn thông báo phía A rồi đóng<br>4. Quan sát màn hình: ở bước nào, form UnivaPay nhập được không<br>5. Đọc console<br>6. Nhập thẻ hợp lệ, gửi lại<br>7. Xem 「予約履歴」, số chỗ S1 / P1 | Thẻ bị từ chối, rồi thẻ hợp lệ | - Bước 3: thông báo lỗi có nội dung<br>- Bước 4: khách không kẹt: về 「カード情報入力」 (hoặc màn có nút để thử lại), form UnivaPay nhập được<br>- Bước 5: không TypeError đọc `clear` của null<br>- Sau bước 2: booking lỗi **bị xoá**, số chỗ S1 / P1 quay về như trước (theo kho TC-EBK-231)<br>- Bước 6–7: đặt thành công đúng **1** đơn; số chỗ +1 | | Lấp G6 (bill fail đến sau × kiểu cũ) · dẫn từ TC-EBK-231 · RULE-08 · manual vì môi trường production · Đánh giá spec: Spec không ghi (màn phía khách sau poll fail) — cần hỏi Dev · Evidence: video + console + 予約履歴 |
| TC-INTGHOOK001-01 | UI | INTG-HOOK-001 | Thanh toán — UnivaPay (booking mới, kiểu mới) | Abnormal | manual | product | UnivaPay kiểu mới (webhook) — callback FAIL khi booking mới → quay lại màn nhập thẻ, form UnivaPay dùng lại được, không TypeError | - Production đã release bản vá<br>- Bot dùng UnivaPay **kiểu mới (callback)**; sự kiện bật 決済 = UnivaPay, slot S1 全承認, コース P1 料金 > 0<br>- Có thẻ sẽ bị từ chối (RULE-05); bật console | 1. A đặt S1 + P1, nhập thẻ bị từ chối → 「決済する」<br>2. Chờ callback fail (< 2 phút)<br>3. Đọc nguyên văn thông báo phía A rồi đóng<br>4. Quan sát màn hình + form UnivaPay<br>5. Đọc console<br>6. Nhập thẻ hợp lệ, gửi lại; chờ callback success<br>7. Đọc tin LINE của A; xem 「予約履歴」 | Thẻ bị từ chối, rồi thẻ hợp lệ | - Bước 3: hiện thông báo lỗi có nội dung<br>- Bước 4: **quay lại 「カード情報入力」**, form UnivaPay nhập được, còn nút đi tiếp (theo kho TC-EBK-236)<br>- Bước 5: không TypeError<br>- Bước 6: thanh toán lại thành công trong cùng phiên<br>- Bước 7: có tin 「決済に失敗しました。カードのご利用枠や有効期限などをご確認いただき再度、購入手続きを行なってください。」 cho lần 1; chỉ **1** đơn thành công | | Lấp G6 (callback fail × booking mới) · dẫn từ TC-EBK-236 · RULE-08 · manual vì môi trường production · Đánh giá spec: Spec ghi rõ (kho TC-EBK-236) · Evidence: video + console + ảnh tin LINE |
| TC-INTGHOOK001-02 | UI | INTG-HOOK-001 | Thanh toán — UnivaPay (đổi lịch, kiểu mới) | Abnormal | manual | product | UnivaPay kiểu mới (webhook) — callback FAIL khi đổi lịch → đơn cũ giữ nguyên, khách thử lại được, không TypeError, khối đổi lịch không mất | - Production đã release bản vá<br>- Bot dùng UnivaPay **kiểu mới**; sự kiện bật 決済 = UnivaPay, 予約変更 = 全承認<br>- A có đơn đã thanh toán ở S1 (P_a); S2 còn chỗ, コース P_b 料金 cao hơn P_a<br>- Có thẻ sẽ bị từ chối (RULE-05); bật console | 1. A đổi lịch S1 → S2 + P_b, nhập thẻ bị từ chối → 「決済する」<br>2. Chờ callback fail<br>3. Đọc nguyên văn thông báo rồi đóng<br>4. Quan sát màn hình: còn thao tác đổi lịch tiếp được không (form thẻ / khối xác nhận), có phải đóng / mở lại link không<br>5. Đọc console<br>6. Nhập thẻ hợp lệ, gửi lại; chờ callback success<br>7. Xem đơn của A, số chỗ S1 / S2, tin LINE | Thẻ bị từ chối, rồi thẻ hợp lệ | - Bước 3: thông báo lỗi có nội dung<br>- Bước 4: khách thao tác tiếp được ngay trong cùng phiên; không màn trắng, không phải mở lại link<br>- Bước 5: không TypeError<br>- Sau bước 2: đơn A **giữ nguyên S1 + P_a**, không gửi action (theo kho TC-EBK-240); có tin 「決済に失敗しました…」<br>- Bước 6–7: đổi sang S2 thành công; S1 −1, S2 +1 | | Lấp G6 (callback fail × đổi lịch — nhánh đổi lịch là hồi quy nặng nhất, khối xác nhận dùng `v-if`) · dẫn từ TC-EBK-240 · RULE-08 · manual vì môi trường production · Đánh giá spec: Spec không ghi (màn phía khách sau callback fail khi đổi lịch) — cần hỏi Dev · Evidence: video + console + ảnh tin LINE |

> G3 (TC-REGSHARED001-01) và G4 (TC-ENV003-01): Leader đã loại TC đề xuất ngày 2026-09-19, không push.
> Q1, Q4, Q5 (phần mã lạ) và Q6 (RULE-01) **không đề xuất TC mới**: nội dung đã có trong bộ TC, chỉ cần đổi mã trên Studio (I3) hoặc ghi lý do thiếu loại case ở `Ghi chú`. Q3 không đề xuất TC vì chưa có requirement về lưu / gỡ PII trong log (§6 #3).

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | Kho TCs Studio EB-019 / EB-032 / EB-038 (FA-021 — đổi lịch UnivaPay · getFriendInfo lỗi · kết bạn từ màn đăng ký) | Sau bản vá, expected đổi: EB-019 "quay lại bước thanh toán, nút 「決済する」 bấm lại được" → nay là về 「カード情報入力」 (#18927). EB-032 thêm "không retry khi có mã HTTP thật, retry 1 lần khi status 0" (#18933). EB-038 thêm "không báo động giả khi chuyển sang màn kết bạn" (#18934) | TC vs kho (REG-SPEC-001) | Leader duyệt rồi sync ngược kho |
| 2 | Endpoint `/ajax/booking-event/report-js-error` (throttle `10,1`) vs hạn mức phía client (5 lỗi thật + 10 chỉ-ghi-log) | Chốt thứ tự ưu tiên: sự cố thoáng qua có được phép ăn mất suất của lỗi thật không? NEW-22 tính ra trường hợp xấu nhất mất cả 5 cảnh báo thật. Hiện chưa có oracle | NEW-22 vs mô tả fix (5) "không ăn mất suất của lỗi thật" | Dev + Leader |
| 3 | Chính sách log PII mới (`line_id`, IP, UA, Referer, `responseText` / `responseJSON`) | Thời hạn lưu, mốc gỡ log chẩn đoán, người chịu trách nhiệm. Dev đã tự ghi "cần chốt mốc gỡ log" | File 03 mục 4.2 D4 | PM / Leader |
| 4 | Giới hạn độ dài của endpoint báo lỗi | Chốt con số: trần `detail` cũ phía server là 1000 hay 2000; giới hạn `message` 500 (NEW-19 dùng nhưng Dev không kê); số điểm ghi vết 10 / 13 / 14 | Journal #137091 vs #137108 vs Studio REQ-012 | Dev |
| 5 | UnivaPay — 2 chế độ bill (kiểu cũ polling / kiểu mới callback) trên màn đặt chỗ LIFF (SCR-EBD-20) | (a) Setting nào quyết định bot chạy kiểu cũ hay kiểu mới, bật / tắt ở đâu — spec và kho không ghi. (b) Nhánh FE xử lý kết quả **đến sau** (poll fail / callback fail) có gọi `resetElement` và đổi bước về `payment` không; nếu có thì có đi qua điều kiện `autoBill == 1` mới không. (c) Màn phía khách sau poll fail (kiểu cũ) và sau callback fail khi **đổi lịch** — spec chưa mô tả | G6 · TC-PAYSTATE001-05 · TC-INTGHOOK001-02 | Dev |
