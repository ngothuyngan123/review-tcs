# 05 — Review Report

## 0. Nguồn TC

- **Nguồn đã dùng**: MCP LME TEST STUDIO — task #284 (ticket 40153, round 1, branch `ai-feature-40153`, `reviewState=leader`), `testcase_list` + `task_get_report` + `task_get_context`.
- **Tổng số TC review**: 68.

---

## 1. Coverage — `dev-impact` + `diff code`

**Kết luận**: 20/30 vùng ảnh hưởng đủ TC · **6 GAP** · **3 RISK**.

> ⚠️ **Cập nhật 2026-09-14 — Leader chốt lại tập trigger**: 4 thao tác **PHẢI xóa data init**: (1) tắt browser · (2) reload browser · (3) bấm「スキップして設定に進む」ở **bất kỳ step 0→5** · (4) **mở 1 tab khác rồi thao tác bất kỳ**. Kèm 2 yêu cầu kết quả: **không sót bản ghi xóa mềm** và **gỡ rich menu khỏi LINE friend đã trải nghiệm**. `G1` · `G8` · `G9` bên dưới đã viết lại theo chuẩn này — **`REQ-004` của Studio và `NEW-80` đang ngược chuẩn**.

| Mã | Chiều | Vùng ảnh hưởng | Trạng thái | Vấn đề | Severity |
|---|---|---|---|---|---|
| **G1** | dev-impact + diff code | `F7` trigger **"mở tab khác → thao tác bất kỳ"** (Leader: **PHẢI xóa**) | **GAP** | **`NEW-80` có `Kết quả mong đợi` NGƯỢC chuẩn Leader**: *"Chỉ chuyển sang tab khác/minimize rồi quay lại thì **KHÔNG được xóa** data init"* — và TC này **chưa chạy**. `NEW-86` còn ghi thẳng *"Chỉ chuyển app/tab mà không đóng **không thuộc oracle** của TC này"*. `REQ-004` của Studio cũng xếp nhóm này vào "không kích hoạt xóa". → **Không TC nào verify đúng chuẩn**, và nếu chạy `NEW-80` nguyên trạng thì sẽ **raise bug nhầm** vào hành vi đúng. Về code: FE dùng `visibilitychange → hidden` nên chuyển tab **có** bắn tín hiệu → **implementation nhiều khả năng đã đúng chuẩn Leader**; thứ phải sửa là **TC + REQ-004**, không phải code. Còn thiếu chiều "thao tác bất kỳ ở tab khác" (tab 体験 ẩn **lâu**, có POST ở tab kia) — chưa TC nào chạm. | `[BLOCKER]` |
| **G1b** | dev-impact | `F7` trigger **đăng xuất** — Leader **chưa chốt** | **RISK** | Danh sách 4 trigger của Leader **không nhắc đăng xuất**, trong khi `REQ-004` xếp đăng xuất vào "không kích hoạt xóa" và `NEW-116` **error cả 2 lượt**. Run #1295 ghi rõ: *"FE vẫn phát tín hiệu xoá khi rời trang để đăng xuất … dữ liệu còn nguyên CHỈ VÌ phiên đã bị huỷ trước khi beacon tới server (HTTP 419)"* → hiện data sống sót **nhờ CSRF chặn tình cờ**, không phải do thiết kế. Nếu chuẩn là "chuyển tab cũng xóa" thì đăng xuất **rất có thể cũng phải xóa** → cần Leader chốt (đã đưa §6). | `[MAJOR]` |
| **G2** | diff code | `F8` `add_bot_v5.js` (+1488 dòng) — file JS **dùng chung toàn wizard thêm bot**; `T2` toàn bộ wizard | **GAP** | Dev **không kê danh sách caller** (file 03 mục 3 là suy từ diff, không phải Dev xác nhận) → vi phạm câu hỏi adversarial #3 của BƯỚC 2. TC chạm wizard chỉ có `NEW-85` (fail, đã triage là lỗi có trước #40153) và `NEW-130` (thêm bot mới). Studio coverage xác nhận: **0/65 mục spec `bot-add` được phủ đủ, 33 mục hoàn toàn trống** — gồm `EP-01`…`EP-24` và `BR-01`…`BR-11` (bot slot, free plan limit, channel ID unique, QR timeout 180s, tutorial 7 ngày…). Các bước còn lại của wizard **không TC nào đi qua**. | `[BLOCKER]` |
| **G3** | dev-impact | `F9` · `D7` · `T3` **gỡ rich menu khỏi LINE friend đã trải nghiệm** (Leader nêu là kết quả bắt buộc) | **RISK** | **Chưa TC nào PASS chứng minh được điều này.** `NEW-107` đúng scenario (friend đang thấy rich menu → sau dọn phải biến mất khỏi khung chat) nhưng **chưa chạy** (`manual`, `env_scope` chỉ `dev/staging`). `NEW-113` (friend mất rich menu + trả lời tự động) **chưa chạy**. `NEW-124` có kiểm "rich menu vừa đăng ký được gỡ khỏi LINE" nhưng tiền đề ghi rõ **"chưa có LINE user nào quét QR"** → **không có friend nào để verify phía LINE**, lại chạy `auto` ở local nên không chạm LINE thật. `NEW-66` (gỡ thất bại → ghi log) pass nhưng **chỉ ở local**. Output cuối nằm trên LINE app → **RULE-06**; gọi LINE API thật → **RULE-08 / ENV-003**. | `[BLOCKER]` |
| **G4** | dev-impact | `T5` trải nghiệm phía LINE user (REQ-002, risk High) | **RISK** | Cả **2 TC** của REQ-002 đều **chưa chạy**: `NEW-112` (LINE user đã quét QR kết bạn nhưng vẫn xóa), `NEW-113` (mất trả lời tự động + rich menu, không thông báo). Đây là phần người dùng cuối nhìn thấy, không kết luận được từ admin screen (**RULE-06**). | `[MAJOR]` |
| **G5** | diff code | `D9` · `T8` log phiên + `tag_line_user` **không bị xóa** trong khi `tags` bị **xóa cứng** (GAP `G1` của file 03 / BR-C05) | **GAP** | `NEW-100` ghi nhận "bảng lịch sử và liên kết tag–người dùng vẫn còn bản ghi", `NEW-103` (phải về 0) **chưa chạy** → nhưng **không TC nào verify HỆ QUẢ**: `tag_line_user` trỏ tới tag đã bị xóa cứng thì màn 友だちリスト, lọc theo tag, chi tiết friend, màn `削除済みタグ` hoạt động ra sao. Kho TC cho thấy tag LME vốn dùng **soft delete** (`deleted_at`, màn `削除済みタグ`, `TC-TAG-36` / `TC-TAG-63` / `TC-TAG-117`) — cleanup này **xóa cứng**, khác hẳn cơ chế cũ. | `[BLOCKER]` |
| **G6** | dev-impact | `D6` xóa file QR / ảnh trên server | **GAP** | `NEW-77` chỉ verify **file seed bị xóa**. **Thiếu đối chứng âm**: file QR + ảnh của **bot thật** (`landing.link_qr_code`, QR PNG 300×300 sinh ở `EP-04` bước 11c/11d của wizard) phải **còn nguyên và còn mở được** sau cleanup. `NEW-65` chỉ verify **dòng DB** landing「テスト」, không verify **file**. | `[MAJOR]` |
| **G7** | diff code | `D8` bảng `onboarding_sessions` (+ `webhook_url`) · `OnboardingSessionCleanupCommand.php` (+42, file mới) | **GAP** | Không TC nào chạm 2 điểm sửa này. Có **2 cơ chế dọn song song** (`TutorialInitDataCleanupJob` và `OnboardingSessionCleanupCommand`) mà quan hệ giữa chúng chưa được test. | `[MAJOR]` |
| **G8** | dev-impact | `F7` trigger **「スキップして設定に進む」ở step 2 · 3 · 4 · 5** | **GAP** | Leader yêu cầu nút skip xóa data init **ở bất kỳ step 0→5**. TC hiện có chỉ phủ **2/6 step**: `NEW-124` = **step 0** (màn QR đầu tiên) · `NEW-123` = **step 1**. `NEW-125` là step 5 nhưng bấm **nút khác**. → **step 2, 3, 4, 5 không TC nào bấm nút skip**. ⚠️ **Leader xác nhận 2026-09-14: step 5 có CẢ HAI nút**「スキップして設定に進む」và「エルメの設定に進む」 → step 5 có **2 lối thoát**, hiện mới phủ 1 (`NEW-125` =「エルメの設定に進む」), còn nút skip ở step 5 **chưa TC nào bấm**. Hai nút gửi `reason` khác nhau (`skip` vs `navigate_away`) nên không suy ra nhau được. Mỗi step có trạng thái seed khác nhau (đã có friend, đã trả lời form, đã bấm rich menu) nên không suy ra từ step 0/1 được. | `[BLOCKER]` |
| **G9** | dev-impact | `F7` trigger **tắt hẳn browser** (khác đóng tab) | **RISK** | 4 TC pass đều là **đóng tab** (`NEW-109`, `NEW-126`) hoặc reload (`NEW-110`), chạy `auto` ở local. TC duy nhất chạm **đóng cửa sổ / tắt trình duyệt** là `NEW-86` — **chưa chạy**. Quit browser khác đóng tab về kỹ thuật: tiến trình bị teardown, `sendBeacon` có thể **không kịp flush**. Thêm nữa `NEW-121` (tab bị kill đột ngột) lại expect **KHÔNG xóa** → **ranh giới giữa "tắt browser bình thường = phải xóa" và "tab bị kill = chấp nhận không xóa" chưa được định nghĩa ở đâu cả**. | `[MAJOR]` |

> Không phát hiện TC **orphan** — 68/68 TC đều trace được về ít nhất 1 vùng ảnh hưởng hoặc 1 requirement.

---

## 2. Thiếu so với quan điểm test

**Kết luận**: 40 quan điểm có Trigger khớp task — **31 đủ TC · 9 thiếu**.

| Mã | Quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| **Q1** | `DATA-BACKUP-001` | Cao (★ BẮT BUỘC khi **thêm/đổi bảng DB** hoặc chạm backup / copy bot) | Release thêm **2 bảng mới** (`bots_tutorial_cleanup_queue`, `bots_tutorial_seed_records`) và chuyển sang **xóa cứng** (bỏ soft-delete). Không TC nào rà tương tác với **データコピー (FA-033) / copy bot / restore**. Rủi ro cụ thể: copy bot nhân bản dòng `bots_tutorial_seed_records` → cleanup của bot copy xóa nhầm bản ghi của bot gốc (hoặc ngược lại). | `[BLOCKER]` |
| **Q2** | `DATA-REF-001` | Cao (★ BẮT BUỘC khi đối tượng bị xóa **được nơi khác tham chiếu**) | Xóa cứng 17 bảng seed nhưng chỉ verify tham chiếu **trong phạm vi purge** (`NEW-106` orphan con–cha) và `notify_setting` (`NEW-76`). Chưa có TC cho tham chiếu **từ ngoài phạm vi vào**: scenario / auto-reply do Admin tạo trỏ tới template seed, bộ lọc broadcast trỏ tới tag seed, form answer map tag seed (kho `TC-TAG-117` cho thấy vùng này có lịch sử bug). | `[BLOCKER]` |
| **Q3** | `DEPLOY-ASSET-001` | Cao (★ BẮT BUỘC khi release sửa JS/CSS) | Release sửa `add_bot_v5.js` **+1488**, thêm `tutorial_cleanup_signal.js` **+138**, CSS `add_bot_v4.css` **+1100** / `connect_direct.css` **+1165**. Không TC nào cho: tab đang mở **trước** deploy giữ **JS cũ** (không có module tín hiệu → không dọn, hoặc gọi endpoint chưa tồn tại), cache asset, `DEPLOY-LIVE-001` (deploy không bật maintain khi queue đang có việc chờ). | `[BLOCKER]` |
| **Q4** | `LIFF-ENTRY-001` | Cao (★ BẮT BUỘC khi tính năng phát sinh URL cho LINE user access — form, **QR code**, popup) | Seed sinh landing + QR + form「アンケート」 cho LINE user truy cập. Sau cleanup các URL này **chết**. Không TC nào mở URL đó **từ phía LINE user** sau khi xóa (404 / trang lỗi / màn trắng / crash LIFF?). `NEW-76` chỉ kiểm "không lỗi do tham chiếu chết" **ở phía admin**. | `[BLOCKER]` |
| **Q5** | `INTG-LINE-001` | Cao (★ BẮT BUỘC khi gọi LINE API) | Gỡ rich menu = gọi LINE API thật. Chỉ có `NEW-66` (một nhánh lỗi chung, chạy local). Thiếu: **token hết hạn**, **429 rate limit**, rich menu **đã bị gỡ tay trước đó** (404 từ LINE), LINE API timeout. Mã `INTG-LINE-001` **không xuất hiện** trong bộ 68 TC. | `[MAJOR]` |
| **Q6** | `FRIEND-001` | Cao (★ BẮT BUỘC khi chức năng đọc/ghi friend info hoặc dùng làm điều kiện) | LINE user đã kết bạn qua QR seed **vẫn là friend** sau cleanup, nhưng tag / lịch sử / friend info của họ thì mất một phần (xem `G5`). Không TC nào mở màn **友だちリスト** / **chi tiết friend** sau cleanup để xác nhận không lỗi và dữ liệu friend thật không hụt. | `[MAJOR]` |
| **Q7** | `JOB-001` | Cao (★ BẮT BUỘC khi thêm/sửa job nền) | **Nội dung đã được cover** bởi `NEW-101` / `NEW-66` / `NEW-67` / `NEW-68` / `NEW-71` / `NEW-73`, nhưng các TC này mang mã **`JOB-002`** — mã **không có** trong `framework/checklist-lme.md` → `/review-tc` không map được coverage. **Không cần viết TC mới**, chỉ cần **re-map mã** trên Studio. | `[MAJOR]` |
| **Q8** | `REG-SPEC-001` | Cao (★ BẮT BUỘC khi spec/requirement đổi **SAU** khi đã có TC) | Bộ TC bị **thay mới hoàn toàn** giữa run #1295 và #1314 (`tcId` 16xxx → 17xxx, 0 id trùng nhau), và `Kết quả mong đợi` của `NEW-70` / `NEW-100` / `NEW-108` đã được viết lại thành "**ghi nhận thực tế**" để khớp code thay vì khớp spec (xem §4 #7). Không có TC nào chạy lại để chốt vùng expected đã đổi. **Không viết TC mới** — cần quyết định spec ở §6 trước. | `[MAJOR]` |
| **Q9** | `MEDIA-IMG-001` | Trung bình → **BẮT BUỘC (nâng Cao)** — ảnh dùng cho richmenu / imagemap gửi ra LINE user | Cleanup **xóa file ảnh** nhưng gỡ rich menu là **best-effort** (lỗi chỉ ghi log). Chưa có TC cho tổ hợp xấu nhất: **gỡ rich menu thất bại + file ảnh đã bị xóa** → LINE user còn thấy rich menu nhưng ảnh vỡ / tap ra lỗi. | `[MAJOR]` |

---

## 3. TC trùng lặp

Đã rà **68/68 TC** theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`).

| Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| Lịch sử phiên trải nghiệm sau cleanup | **Giữ CẢ HAI — không xóa** | — | `DUP-CONFLICT` | Cùng đối tượng (bảng lịch sử + `tag_line_user`), cùng thao tác (chạy cleanup), tiền đề tương đương → nhưng **`Kết quả mong đợi` mâu thuẫn**: `NEW-100` = *"bảng lịch sử và bảng liên kết tag–người dùng **vẫn còn** bản ghi — ghi nhận theo gap đã biết"* vs `NEW-103` = *"Theo spec: toàn bộ bản ghi lịch sử … **phải về 0**"*. `NEW-100` pass, `NEW-103` chưa chạy → nếu chạy `NEW-103` sẽ fail. Một trong hai sai chuẩn → **đã đưa §6** để Dev/PM chốt. | `[MAJOR]` |
| Trigger "mở tab khác" | **`NEW-80` phải sửa expected** (không xóa → **phải xóa**); `NEW-86` phải bỏ câu loại trừ "chuyển app/tab không thuộc oracle" | — | `DUP-CONFLICT` (TC vs **chuẩn Leader**) | `NEW-80` + `NEW-86` + `REQ-004` cùng khẳng định chuyển tab **không** kích hoạt xóa, trong khi Leader chốt 2026-09-14 là **phải xóa**. Không phải 2 TC mâu thuẫn nhau mà là **TC mâu thuẫn với chuẩn** → nếu chạy nguyên trạng sẽ raise bug nhầm vào hành vi đúng. **Không xóa TC** — sửa expected trên Studio (`testcase_update`). | `[BLOCKER]` |
| Hạn trải nghiệm 7 ngày không chặn cleanup | `NEW-127` (UI, rời trang) + `NEW-95` (API, gọi endpoint) | **Giữ cả 2, không gộp** | `DUP-SUBSET` (nhẹ) | Cùng `FUNC-DATE-001` · cùng `Boundary` · cùng REQ-006 · tiền đề tương đương (bot quá hạn 7 ngày). Khác nhau ở **tầng kiểm chứng**: `NEW-127` là end-to-end qua UI, `NEW-95` là contract của EP-C01. **Gate xóa**: bỏ `NEW-95` sẽ mất cover contract tầng API của REQ-006 → **không đề nghị xóa**. | `[NIT]` |

> Không phát hiện `DUP-EXACT` và không có `DUP-INFLATE` che GAP. Các cụm idempotency (`NEW-118` / `NEW-88` / `NEW-89` / `NEW-69` / `NEW-122`) và cụm đối chứng âm (`NEW-65` / `NEW-84` / `NEW-104` / `NEW-105`) **đã kiểm và KHÔNG trùng** — khác tầng (UI/API/job) hoặc khác đối tượng dữ liệu.

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | `NEW-116` (đăng xuất không kích hoạt xóa) | `error` ở run #1314 (`spawnSync mysql EAGAIN` — lỗi hạ tầng runner) và **fail ở run #1295** với bằng chứng app thật: *"FE vẫn phát tín hiệu xoá khi rời trang để đăng xuất"*. **Chưa raise ticket bug nào** (`bug_tickets` rỗng, bug duy nhất của task là #1031 thuộc `NEW-85`). Lỗi hạ tầng đang **che một defect thật** của REQ-004. | Chạy lại `NEW-116` trên môi trường DB ổn định; nếu tái hiện → **raise ticket** với evidence run #1295. Không được đóng task khi TC này còn `error`. |
| 2 | `[MAJOR]` | `NEW-97` (staff có quyền gửi yêu cầu dọn) | `error` do `spawnSync mysql EAGAIN` ở bước 5/5. Triage ghi "ba nhánh đã xảy ra đúng contract trước khi phép đo chết" nhưng **không có kết luận chính thức** → `PERM-001` (Cao) thực tế **chưa được chứng minh**. | Chạy lại. Nếu không dựng được staff trên runner → chuyển `exec_mode=manual`. |
| 3 | `[MAJOR]` | Toàn bộ 68 TC | **0/68 TC có `env_scope` gồm `prd`**; 60 TC đã chạy đều ở **`local`**, 0 run trên dev / staging / production. Task chạm **media (file QR/ảnh), domain (LINE API), job nền** → **RULE-08 / ENV-003**: không được kết luận từ local. | Khai `env_scope` có `prd` cho nhóm media / LINE API / job, và chạy tối thiểu 1 vòng trên staging + production trước khi release. |
| 4 | `[MAJOR]` | 8 TC chưa chạy | 8 TC `exec_mode=manual` chưa chạy **đúng là 8 TC cover các vùng rủi ro nhất**: `NEW-80` (chuyển tab), `NEW-112` / `NEW-113` (phía LINE user), `NEW-107` (rich menu trên LINE), `NEW-103` (lịch sử), `NEW-121` (mất tín hiệu), `NEW-122` (2 tab), `NEW-86` (ma trận trình duyệt). Tỷ lệ pass 57/68 = **83,8%** nhìn thì đạt, nhưng **11 TC không có kết luận** và phần chưa chạy lệch hẳn về vùng nguy hiểm. | Giao người chạy tay 8 TC này trước khi chốt round 1. |
| 5 | `[MAJOR]` | 20/68 TC | **10 mã quan điểm không có trong `framework/checklist-lme.md`**: `TOOL-ERRHYG-001` (4) · `JOB-002` (4) · `TOOL-NEGCTRL-001` (3) · `AUTH-SESSION-001` (2) · `API-001` (2) · `TOOL-VAL2-001` · `OBS-001` · `DATA-RETENTION-001` · `DATA-HIST-001` · `TOOL-SELFAUDIT-001`. Theo BƯỚC 0.6 #5 các TC này **không được tính là cover** → kéo theo `Q7`. | Re-map sang mã chuẩn (`JOB-002`→`JOB-001`, `API-001`→`FUNC-001`/`SEC-001`, `AUTH-SESSION-001`→`SEC-002`, `DATA-HIST-001`→`DATA-AUDIT-001`…) bằng `testcase_update` trên Studio, hoặc bổ sung mã mới vào checklist theo **RULE-10**. |
| 6 | `[MAJOR]` | Toàn bộ 68 TC | `Trạng thái đánh giá spec` (`spec_status`) **null 68/68** — trong khi task có **3 điểm spec↔code lệch đã biết** (dry_run, xóa log, `notify_setting`). Không phân biệt được TC nào dựa spec, TC nào tự suy diễn. | Điền `spec_status` cho ít nhất các TC chạm 3 vùng lệch: `Spec ghi rõ` / `Spec không ghi` / `Đã hỏi leader`. |
| 7 | `[MAJOR]` | `NEW-70` · `NEW-108` · `NEW-100` | **`Kết quả mong đợi` được viết lại để khớp code thay vì khớp spec**, rồi chấm `pass`. `NEW-70`: *"Ghi nhận thực tế: không tồn tại công tắc nào chặn việc xóa"* — trong khi technical spec §7 coi feature flag `tutorial_init_data_cleanup` là **hàng rào BẮT BUỘC** (run #1295 từng **fail 8 TC** vì thiếu cờ này). `NEW-108`: chấp nhận "không có cơ chế dọn bù". `NEW-100`: chấp nhận log không bị xóa dù BR-C05 yêu cầu. → Leader nhìn "57 pass" sẽ hiểu nhầm là **không còn vi phạm spec**. | Giữ TC nhưng đổi kết luận thành **`Không đạt` + link quyết định spec**, hoặc tách ra khỏi tỷ lệ pass. Chốt §6 trước khi release. |
| 8 | `[MAJOR]` | Spec nguồn | `spec-features/admin/bot-add-v2/feature-spec.md` có, nhưng §1 Phạm vi ghi rõ **"Không bao gồm: chi tiết tính năng tutorial (`/manual/tutorial`)"** → **không có spec trong repo cho chính màn 体験** đang sửa. Business rule (`BR-C01`…`BR-C05`, `QA-spec-*`, `EP-C01`) chỉ tồn tại trong `requirements` của Studio (`contentTrust=untrusted`). | Bổ sung spec màn 体験 vào `spec-features/`, hoặc Leader xác nhận bộ 17 REQ của Studio là chuẩn chính thức. |

### Chất lượng từng TC + anti-pattern

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 9 | `[MAJOR]` | `[AP-6]` file 03 mục 3 | Dev **không cung cấp danh sách caller** của `add_bot_v5.js` — file JS dùng chung toàn wizard. Mục 3 hiện tại là suy từ diff, không phải Dev xác nhận. | Yêu cầu Dev liệt kê các bước/màn dùng chung file này → làm cơ sở cho `G2`. |
| 10 | `[MAJOR]` | `[AP-3]` `T2` · `T4` · `T6` | Regression happy-path-only: mỗi tính năng liền kề chỉ có 1 TC với tiền đề "bot vừa thêm mới, data sạch". Không có TC regression ở **edge state** (bot free plan, bot đã hết hạn hợp đồng, bot nhiều friend, `notify_setting` đã có nhiều người nhận). | Thêm tiền đề edge cho `TC-REGSHARED001-02` (§5). |
| 11 | `[MAJOR]` | `[AP-1]` biến thể — tập trigger | Không phải generic catch, nhưng **1 sự kiện chung `visibilitychange → hidden` đại diện cho nhiều hành vi người dùng**. Chỉ **3 trigger** được verify (đóng tab, reload, điều hướng). **3 trigger chưa có kết luận**: chuyển tab, minimize, đăng xuất. Theo AP-1 (< 3 trigger cho nhánh phủ định + không có trigger "chưa biết") → cùng dạng bỏ lọt. | Xem `G1` + 2 TC ở §5. |
| 12 | `[MINOR]` | `[AP-4]` file 03 mục Thông tin | `Commit / Pull Request` trống. **Giảm nhẹ**: Studio có `spec_delta` (`diffAvailable=true`, 524 file) + `dev_impact` nên vẫn verify được hình dạng fix — nhưng không review được ở mức dòng code. | Xin link PR để đối chiếu điều kiện `currentStep 22-27` và danh sách 17 bảng seed. |
| 13 | `[MINOR]` | `NEW-83` / EP-C01 | Run #1295 ghi nhận: request không session trả **HTTP 419 TokenMismatch** (CSRF middleware) thay vì **401** — EP-C01 nằm sau CSRF, chỉ `/ajax/*` được miễn. TC hiện tại pass vì chỉ kiểm "bị từ chối". | Chốt mã lỗi đúng với Dev, ghi rõ vào `Kết quả mong đợi` (419 hay 401) — ảnh hưởng tới cách FE xử lý beacon thất bại. |
| 14 | `[MINOR]` | Bug Studio #1031 (`NEW-85`) | Bị **rejected** với lý do *"khi reload, hiển thị màn resume"* — lý do này chỉ trả lời case **F5/reload**, **không trả lời case đóng tab** (mất dữ liệu credential đang nhập dở, không có màn resume). Evidence trong bug xác nhận lỗi có **trước** #40153 (commit `16fc69e617` của #36409). | Tách ticket riêng cho case đóng tab ở bước nhập kênh — **không gộp vào #40153**. |
| 15 | `[MINOR]` | Toàn bộ 68 TC | `TC No.` dùng `NEW-xx` (temp id Studio), không theo format repo `TC-<mã quan điểm bỏ gạch>-<nn>`. | Không bắt buộc sửa với TC do Studio sinh; chỉ áp format cho TC bổ sung ở §5. |

> **Evidence (RULE-02) — ĐẠT**: 58/60 kết quả run #1314 có `artifacts` + 60/60 có `actual` mô tả đo được. 2 trường hợp thiếu là 2 TC `error`. Không flag.

---

## 5. TCs đề xuất bổ sung (21)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs` **chưa có `FA-032` 新規アカウント追加 / màn 体験** → không đối chiếu trực tiếp được. Đã đọc **vùng lân cận bị cleanup chạm**: `kho-tcs/fa012-quanlythe-タグ管理.md` · `kho-tcs/fa004-richmenu-リッチメニュー.md` · `kho-tcs/fa011-taobieumau-フォーム作成.md` · `kho-tcs/fa013-friendlist-友だちリスト.md` |
| Vùng regression phát hiện từ kho | **Tag**: LME dùng **soft delete** (`deleted_at`, màn `削除済みタグ` 18 TC, `TC-TAG-36` / `TC-TAG-63` unique chỉ xét tag active, `TC-TAG-117` action gắn tag đã xoá → không gắn, không lỗi). Cleanup này **xóa cứng** + để lại `tag_line_user` → cơ chế khác hẳn giả định của toàn bộ nhóm TC kho. **Richmenu**: `TC-RM-11` (xóa folder có richmenu → chuyển 未分類) — cần xác nhận folder seed bị xóa cứng không cuốn richmenu Admin. |
| Conflict expected vs kho | **Có** — `TC-TAG-117` (kho) giả định tag bị xoá là **soft delete** và `tag_line_user` **không được tạo**; TC đề xuất `TC-DATAREF001-01` xét tag bị **xoá cứng** mà `tag_line_user` **vẫn còn**. Hai giả định không thể cùng đúng → `[MAJOR] SPEC-CONFLICT`, **đã đưa §6**. |
| GAP dùng lại TC kho (không viết mới) | `G3` → chạy lại `NEW-107` trên **production** (không viết mới) · `G4` → chạy `NEW-112` + `NEW-113` trên staging/production (không viết mới) · `Q7` → **re-map mã**, không viết mới · `Q8` → chờ quyết định §6, không viết mới |
| Xác nhận chống trùng | Đã đối chiếu **68 TC** ở BƯỚC 0 + 4 file kho — **không TC đề xuất nào trùng**. |

**Đã push lên Studio task #284** (2026-09-14, `testcase_create`, `client_ref` = `ID` → idempotent):

| ID (§5) | Studio | `temp_id` |
|---|---|---|
| `TC-STATECLEAN001-01` | #17708 | `NEW-132` |
| `TC-FUNC001-01` | #17709 | `NEW-133` |
| `TC-FUNC001-02` | #17710 | `NEW-134` |
| `TC-REGSHARED001-01` | #17711 | `NEW-135` |
| `TC-REGSHARED001-02` | #17712 | `NEW-136` |
| `TC-FRIEND001-01` | #17713 | `NEW-137` |
| `TC-MEDIACLEAN001-01` | #17714 | `NEW-138` |

**14 TC còn lại của §5 chưa push** — Leader chưa chọn. Riêng `TC-STATECLEAN001-02` (đăng xuất) **không nên push trước khi chốt §6 #9**, vì `Kết quả mong đợi` phụ thuộc quyết định đó.

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-STATECLEAN001-01 | UI | STATE-CLEAN-001 | Màn 体験 — tập trigger dọn | Normal | manual | Tất cả | Mở tab khác rồi thao tác ở tab đó: data init **bị xóa** và rich menu bị gỡ | - Admin đăng nhập, phiên còn hiệu lực<br>- Đang ở màn 体験 bước 1, data init đã sinh<br>- Đã có 1 LINE user quét QR kết bạn và đang thấy rich menu「リッチメニュー」<br>- Ghi lại số bản ghi cấu hình 体験 trước khi thao tác | 1. Ở màn 体験, mở tab mới trong **cùng trình duyệt**<br>2. Ở tab mới, thao tác bình thường ≥ 2 phút (mở màn danh sách bạn bè, tạo 1 tag, lưu lại)<br>3. **Không** quay lại tab 体験<br>4. Đếm lại số bản ghi cấu hình 体験 của bot<br>5. Mở khung chat LINE của friend đã kết bạn | Tab 体験 bị ẩn, tab khác có thao tác ghi dữ liệu | - Tín hiệu dọn được gửi ngay khi tab 体験 bị ẩn (đúng 1 yêu cầu vào hàng đợi)<br>- Toàn bộ cấu hình 体験 của bot bị xóa, **kể cả bản ghi xóa mềm**<br>- Rich menu biến mất khỏi khung chat của LINE friend<br>- Tag Admin vừa tạo ở tab mới **không bị xóa** | | Lấp `G1` · Đánh giá spec: **Spec ghi rõ (Leader chốt 2026-09-14)** — ⚠️ **ngược với `NEW-80` và `REQ-004`**, đã đưa §6 · Evidence: video 2 tab + nhật ký request + ảnh khung chat LINE |
| TC-STATECLEAN001-02 | UI | STATE-CLEAN-001 | Màn 体験 — tập trigger dọn | Abnormal | manual | Tất cả | Đăng xuất từ màn 体験 khi phiên vẫn hợp lệ: hành vi phải khớp chuẩn đã chốt | - Admin đang ở màn 体験, data init đã sinh<br>- Tiến trình xử lý hàng đợi đang chạy<br>- Ghi lại số bản ghi cấu hình 体験 của bot trước khi thao tác | 1. Từ màn 体験 bấm đăng xuất (hoặc mở trực tiếp đường dẫn đăng xuất)<br>2. Theo dõi request mạng xem có tín hiệu dọn nào được gửi không và server trả mã gì<br>3. Chờ 2 phút, đăng nhập lại<br>4. Mở lại bot đó, đếm số bản ghi cấu hình 体験<br>5. Đối chiếu nhật ký hàng đợi | Thao tác đăng xuất bình thường | - Kết quả khớp **đúng quyết định spec ở §6 #9** (xóa hay không xóa)<br>- Dù chọn hướng nào: hành vi phải **có chủ đích** — nếu không xóa thì phải do đăng xuất nằm ngoài tập trigger, **không phải** vì beacon bị chặn tình cờ bởi lỗi token 419 | | Lấp `G1b` · Đánh giá spec: **Đã hỏi leader — chờ chốt §6 #9** · Evidence: nhật ký request + mã HTTP + số bản ghi trước/sau · ⚠️ Run #1295: FE **vẫn bắn** tín hiệu khi đăng xuất, chỉ bị chặn nhờ HTTP 419 |
| TC-FUNC001-01 | UI | FUNC-001 | Màn 体験 — nút「スキップして設定に進む」 | Normal | manual | Tất cả | Bấm nút bỏ qua ở **từng step 2 → 5** đều xóa data init và gỡ rich menu | - Chuẩn bị **4 bot riêng**, mỗi bot đi tới đúng 1 step: step 2, 3, 4, 5<br>- Mỗi bot đã có LINE user quét QR kết bạn, đã gửi「エルメ体験」, đã trả lời form「アンケート」tùy theo step<br>- Ghi lại số bản ghi cấu hình 体験 của từng bot | 1. Với bot ở step 2: bấm「スキップして設定に進む」, theo dõi request dọn<br>2. Chờ 30 giây, đếm lại bản ghi cấu hình 体験 (tính cả bản ghi xóa mềm)<br>3. Mở khung chat LINE của friend tương ứng<br>4. Lặp lại y hệt cho bot ở step 3, step 4, step 5 | 4 bot × 4 step, mỗi bot 1 LINE friend thật | - Cả 4 step: tín hiệu dọn gửi **ngay tại thời điểm bấm**, `reason` = **bỏ qua** (`skip`), màn chuyển sang 活用設定 không hỏi lại<br>- Cấu hình 体験 của cả 4 bot về 0, **không sót bản ghi xóa mềm**<br>- Rich menu biến mất khỏi khung chat của cả 4 friend<br>- Riêng step 5: bấm **nút skip** cho kết quả xóa **giống hệt** khi bấm「エルメの設定に進む」(`NEW-125`), chỉ khác `reason` ghi trong hàng đợi<br>- Không step nào bị bỏ sót hoặc xử lý khác các step còn lại | | Lấp `G8` · Đánh giá spec: **Spec ghi rõ (Leader chốt 2026-09-14: step 0→5)** · Evidence: nhật ký request 4 lượt + ảnh khung chat 4 friend · Step 0 = `NEW-124`, step 1 = `NEW-123` đã có · **Step 5 có CẢ 2 nút** (Leader xác nhận 2026-09-14): nút skip = TC này, 「エルメの設定に進む」= `NEW-125` |
| TC-FUNC001-02 | UI | FUNC-001 | Màn 体験 — tắt hẳn trình duyệt | Boundary | manual | Tất cả | Tắt hẳn trình duyệt (không phải đóng tab) vẫn xóa data init | - Bot đang ở màn 体験, data init đã sinh, đã có LINE friend thấy rich menu<br>- Chuẩn bị 3 bot cho 3 trình duyệt: Chrome, Edge, Safari<br>- Tiến trình xử lý hàng đợi đang chạy | 1. Với từng trình duyệt: mở màn 体験 của bot tương ứng<br>2. **Thoát hẳn trình duyệt** bằng lệnh thoát của ứng dụng (không đóng từng tab trước)<br>3. Chờ 1 phút<br>4. Đếm bản ghi cấu hình 体験 của từng bot và kiểm tra hàng đợi<br>5. Mở khung chat LINE của friend tương ứng | 3 trình duyệt × 1 bot | - Cả 3 trình duyệt đều phát sinh **đúng 1** yêu cầu dọn<br>- Cấu hình 体験 về 0 ở cả 3 bot, không sót bản ghi xóa mềm<br>- Rich menu biến mất khỏi khung chat cả 3 friend<br>- Nếu có trình duyệt nào tín hiệu không kịp gửi → ghi nhận rõ trình duyệt nào, để chốt với Dev (khác với case tab bị kill đột ngột ở `NEW-121`) | | Lấp `G9` · Đánh giá spec: **Spec ghi rõ (Leader chốt 2026-09-14)** · Evidence: nhật ký request từng trình duyệt + ảnh khung chat · ⚠️ Ranh giới với `NEW-121` (kill đột ngột → chấp nhận không xóa) cần chốt ở §6 #10 |
| TC-REGSHARED001-01 | UI | REG-SHARED-001 | Wizard thêm bot — smoke toàn luồng | Normal | manual | Tất cả | Thêm bot mới chạy trọn vẹn từ đầu đến cuối sau khi thêm cơ chế dọn | - Tài khoản admin còn slot trống<br>- Có sẵn LINE OA chưa kết nối (channel ID/secret hợp lệ) | 1. Vào wizard thêm bot, đi lần lượt từng bước: chọn phương thức kết nối → nhập thông tin kênh → xác nhận webhook → quét QR kết bạn → hoàn tất<br>2. Ở mỗi bước, bấm quay lại 1 bước rồi tiến tiếp<br>3. Đi tới màn 体験, đi hết các bước ①〜⑤<br>4. Bấm「エルメの設定に進む」<br>5. Kiểm tra bot vừa tạo ở màn danh sách bot và màn 活用設定 | 1 LINE OA thật chưa từng kết nối | - Mọi bước của wizard hoạt động đúng như trước: không kẹt bước, không lỗi JS, nút quay lại/tiến tiếp bình thường<br>- Bot được tạo đủ dữ liệu khởi tạo (hồ sơ bot, cấu hình kết bạn, trạng thái chat, landing + QR)<br>- Chỉ data init của phần 体験 bị dọn, dữ liệu khởi tạo bot **còn nguyên** | | Lấp `G2` · regression · Đánh giá spec: Spec ghi rõ (`spec-features/admin/bot-add-v2`) · Evidence: video toàn luồng + ảnh DB bot mới · Studio coverage hiện **0/65** mục spec `bot-add` |
| TC-REGSHARED001-02 | UI | REG-SHARED-001 | Wizard thêm bot — trạng thái edge | Abnormal | manual | Tất cả | Wizard vẫn đúng ở các trạng thái edge sau khi thêm watcher bước 22-27 | - Chuẩn bị 3 tình huống: (a) tài khoản **hết slot**, (b) tài khoản **free plan đã có 1 bot**, (c) phiên thêm bot **dở dang** (đã nhập kênh, thoát giữa chừng rồi vào lại) | 1. Với mỗi tình huống, vào wizard thêm bot<br>2. Đi tới bước xa nhất có thể<br>3. Quan sát thông báo chặn / màn resume<br>4. Kiểm tra hàng đợi dọn có phát sinh bản ghi nào không<br>5. Với (c): tiếp tục tới màn 体験 rồi đóng tab | (a) hết slot · (b) free plan đã có bot · (c) phiên dở dang | - Thông báo chặn của từng tình huống hiển thị đúng như trước khi có #40153<br>- Màn resume của phiên dở dang hoạt động bình thường<br>- **Không** phát sinh yêu cầu dọn ở bất kỳ bước nào trước màn 体験<br>- Tình huống (c) khi vào 体験 rồi đóng tab thì dọn đúng 1 lần | | Lấp `G2` · `[AP-3]` regression edge state · Đánh giá spec: Spec ghi rõ (BR-001, BR-004) · Evidence: ảnh từng thông báo + nhật ký hàng đợi |
| TC-DATAREF001-01 | UI | DATA-REF-001 | Tag — sau khi tag seed bị xóa cứng | Abnormal | manual | Tất cả | Friend đã được gắn tag seed: màn danh sách bạn bè và lọc theo tag không lỗi sau cleanup | - LINE user đã quét QR seed, kết bạn và **được gắn tag seed** trong lúc trải nghiệm<br>- Sau đó Admin rời màn 体験 → cleanup đã chạy xong | 1. Mở màn 友だちリスト của bot<br>2. Tìm friend vừa kết bạn, mở chi tiết friend<br>3. Xem mục tag của friend đó<br>4. Mở màn quản lý tag và màn 削除済みタグ<br>5. Tạo bộ lọc theo tag rồi áp dụng trên màn danh sách bạn bè | 1 friend thật đã gắn tag seed | - Màn danh sách bạn bè mở được, **không lỗi**, friend vẫn hiển thị<br>- Chi tiết friend mở được; mục tag không hiển thị tag rác / không văng lỗi<br>- Màn 削除済みタグ không hiển thị tag seed (vì bị xóa cứng, không phải xóa mềm) — hoặc hiển thị theo đúng quyết định spec ở §6<br>- Bộ lọc theo tag chạy ra kết quả, không lỗi truy vấn | | Lấp `G5` · `Q2` · `Q6` · Đánh giá spec: **Spec không ghi — đã đưa §6** · Evidence: ảnh 3 màn + ảnh bản ghi liên kết tag–user · ⚠️ SPEC-CONFLICT với kho `TC-TAG-117` (giả định tag xoá mềm) |
| TC-FRIEND001-01 | UI | FRIEND-001 | Chi tiết friend — sau cleanup | Abnormal | manual | Tất cả | Friend kết bạn trong phiên trải nghiệm vẫn dùng được bình thường sau khi data init bị xóa | - LINE user đã kết bạn qua QR seed, đã trả lời form「アンケート」, đã bấm rich menu<br>- Cleanup đã chạy xong | 1. Mở chi tiết friend đó<br>2. Xem các tab: thông tin bạn bè, lịch sử hành động, tab rich menu<br>3. Từ màn chat 1:1 gửi 1 tin nhắn cho friend này<br>4. Gắn 1 tag mới (do Admin tạo) cho friend<br>5. Quan sát phía LINE user | 1 friend thật có đủ tương tác | - Mọi tab của chi tiết friend mở được, không lỗi, không mất khung<br>- Gửi tin 1:1 thành công, LINE user nhận được<br>- Gắn tag mới thành công<br>- Không có tham chiếu chết nào làm hỏng màn | | Lấp `Q6` · `G5` · Đánh giá spec: Spec không ghi · Evidence: ảnh từng tab + ảnh khung chat LINE · RULE-06 (verify tại LINE app) |
| TC-DATAREF001-02 | UI | DATA-REF-001 | Template / kịch bản do Admin tạo trỏ tới đối tượng seed | Abnormal | manual | Tất cả | Kịch bản và trả lời tự động của Admin tham chiếu template seed không bị hỏng sau cleanup | - Bot đang ở màn 体験 (data init đã sinh)<br>- Admin mở tab khác tạo 1 auto-reply và 1 kịch bản **trỏ tới template seed** đang có | 1. Ghi lại auto-reply + kịch bản vừa tạo<br>2. Quay lại tab 体験, đóng tab để kích hoạt dọn<br>3. Chờ cleanup xong<br>4. Mở lại màn trả lời tự động và màn kịch bản<br>5. Mở chi tiết từng cái, bấm lưu lại | 1 auto-reply + 1 kịch bản trỏ template seed | - Hai màn mở được, không lỗi<br>- Mục trỏ tới template đã xóa hiển thị trạng thái rõ ràng (trống / báo đã xóa), **không** hiển thị lỗi hệ thống<br>- Lưu lại được, không văng exception<br>- LINE user kích hoạt auto-reply đó không bị lỗi im lặng | | Lấp `Q2` · Đánh giá spec: **Spec không ghi — đã đưa §6** · Evidence: ảnh 2 màn + log lỗi (nếu có) |
| TC-DATAREF001-03 | UI | DATA-REF-001 | Bộ lọc gửi tin trỏ tới tag seed | Abnormal | manual | Tất cả | Bộ lọc broadcast có điều kiện tag seed không làm hỏng luồng gửi tin sau cleanup | - Đang ở màn 体験, tag seed đã tồn tại<br>- Admin tạo 1 broadcast nháp với điều kiện lọc **theo tag seed** | 1. Lưu broadcast nháp, ghi lại số bạn bè dự kiến<br>2. Kích hoạt cleanup (đóng tab 体験)<br>3. Mở lại broadcast nháp<br>4. Bấm tính lại số bạn bè dự kiến<br>5. Thử gửi test cho 1 friend | 1 broadcast nháp lọc theo tag seed | - Màn broadcast mở được, không lỗi<br>- Điều kiện lọc trỏ tag đã xóa hiển thị rõ ràng hoặc bị gỡ có kiểm soát<br>- Tính số bạn bè dự kiến chạy được, **không** trả số sai lệch ngẫu nhiên<br>- Gửi test không văng lỗi | | Lấp `Q2` · Đánh giá spec: Spec không ghi · Evidence: ảnh màn broadcast + số dự kiến trước/sau |
| TC-MEDIACLEAN001-01 | UI | MEDIA-CLEAN-001 | File QR / ảnh trên máy chủ | Abnormal | manual | product | Đối chứng âm: file QR và ảnh của **bot thật** vẫn còn sau khi dọn data init | - Bot vừa thêm mới: đã sinh landing + **file QR kết bạn của bot** ở bước tạo bot<br>- Admin tự upload thêm 1 ảnh vào thư viện ảnh của bot<br>- Sau đó vào màn 体験 rồi rời trang để kích hoạt dọn | 1. Trước khi dọn: mở và lưu lại đường dẫn file QR kết bạn của bot + ảnh Admin tự upload<br>2. Kích hoạt dọn, chờ hoàn tất<br>3. Mở lại 2 đường dẫn đó trên trình duyệt<br>4. Mở màn kết bạn của bot xem mã QR có hiển thị không<br>5. Mở thư viện ảnh xem ảnh Admin upload còn không | 1 QR bot thật + 1 ảnh Admin upload | - File QR kết bạn của bot **vẫn mở được**, ảnh hiển thị đúng<br>- Màn kết bạn vẫn hiện mã QR bình thường, quét vẫn kết bạn được<br>- Ảnh Admin tự upload vẫn còn trong thư viện<br>- Chỉ file QR/ảnh **thuộc seed 体験** bị xóa | | Lấp `G6` · **RULE-08** (media → bắt buộc product) · Đánh giá spec: Spec ghi rõ (REQ-011) · Evidence: ảnh chụp 2 URL trước/sau + ảnh màn kết bạn |
| TC-DATADB001-01 | Data | DATA-DB-001 | Phiên onboarding & lệnh dọn phiên | Normal | manual | Tất cả | Hai cơ chế dọn (job dọn data init và lệnh dọn phiên onboarding) không giẫm chân nhau | - Có bot đang ở giữa luồng onboarding (phiên onboarding đang mở)<br>- Có bot khác đã kích hoạt dọn data init, việc dọn đang chờ trong hàng đợi | 1. Ghi lại trạng thái phiên onboarding của cả 2 bot<br>2. Chạy lệnh dọn phiên onboarding<br>3. Chạy tiến trình xử lý hàng đợi dọn data init<br>4. Kiểm tra lại trạng thái phiên onboarding + bản ghi hàng đợi + dữ liệu 体験 của cả 2 bot<br>5. Đảo thứ tự 2 bước trên rồi lặp lại | 2 bot ở 2 trạng thái khác nhau | - Phiên onboarding đang mở của bot đang dùng **không bị đóng nhầm**<br>- Việc dọn data init vẫn hoàn tất đúng cho bot còn lại<br>- Đảo thứ tự cho kết quả giống nhau (không phụ thuộc thứ tự chạy)<br>- Không bản ghi nào bị xóa 2 lần hoặc bị bỏ sót | | Lấp `G7` · Đánh giá spec: **Spec không ghi — đã đưa §6** · Evidence: bảng đối chiếu trạng thái trước/sau của 2 bot |
| TC-DATABACKUP001-01 | Data | DATA-BACKUP-001 | データコピー (FA-033) × bảng ánh xạ seed | Abnormal | manual | Tất cả | Copy bot không nhân bản bảng ánh xạ seed khiến dọn nhầm bot nguồn | - Bot A đang có data init 体験 (bảng ánh xạ seed đã ghi đủ)<br>- Dùng chức năng データコピー copy cấu hình bot A sang bot B | 1. Thực hiện copy bot A → bot B<br>2. Kiểm tra bot B có bản ghi ánh xạ seed nào không, nếu có thì trỏ tới bản ghi của bot nào<br>3. Kích hoạt dọn cho **bot B**<br>4. Kiểm tra dữ liệu của **bot A** sau khi dọn bot B<br>5. Lặp lại theo chiều ngược: kích hoạt dọn cho bot A, kiểm tra bot B | 2 bot cùng tài khoản, 1 lần copy | - Dọn bot B **không xóa bất kỳ bản ghi nào của bot A** và ngược lại<br>- Nếu bản ghi ánh xạ bị copy sang bot B thì phải trỏ đúng bản ghi của bot B, hoặc bị bỏ qua với lý do rõ ràng<br>- Không bản ghi nào bị xóa 2 lần | | Lấp `Q1` · Đánh giá spec: **Spec không ghi — đã đưa §6** · Evidence: số bản ghi từng bảng của A và B trước/sau · ⚠️ Rủi ro mất dữ liệu bot thật |
| TC-DATABACKUP001-02 | Data | DATA-BACKUP-001 | Xóa mềm / khôi phục bot × dọn data init | Boundary | manual | Tất cả | Bot bị xóa mềm rồi khôi phục thì việc dọn vẫn đúng phạm vi | - Bot A có data init 体験 và đã có yêu cầu dọn nằm chờ trong hàng đợi<br>- Tiến trình xử lý hàng đợi đang **tạm dừng** | 1. Xóa mềm bot A (theo luồng hủy/ngắt kết nối hiện có)<br>2. Bật lại tiến trình xử lý hàng đợi<br>3. Kiểm tra việc dọn kết thúc ở trạng thái nào<br>4. Khôi phục bot A (nếu hệ thống cho phép)<br>5. Kiểm tra dữ liệu 体験 và dữ liệu thật của bot A | 1 bot bị xóa mềm giữa chừng | - Việc dọn kết thúc ở trạng thái rõ ràng (hoàn tất hoặc bỏ qua có lý do), **không treo vô hạn**, không lỗi không bắt được<br>- Không xóa nhầm dữ liệu của bot khác<br>- Sau khi khôi phục, dữ liệu thật của bot A còn nguyên | | Lấp `Q1` · Đánh giá spec: Spec không ghi · Evidence: trạng thái bản ghi hàng đợi + nhật ký · Xóa cứng **không hoàn tác được** nên phải chốt hành vi trước khi lên product |
| TC-DEPLOYASSET001-01 | UI | DEPLOY-ASSET-001 | Deploy — tab mở sẵn giữ JS cũ | Abnormal | manual | product | Tab 体験 mở trước khi deploy (giữ JS cũ) không làm hỏng dữ liệu và không báo thành công giả | - Mở màn 体験 trên môi trường **trước** khi deploy bản có cơ chế dọn, giữ nguyên tab (không refresh)<br>- Deploy bản mới lên trong lúc tab vẫn mở | 1. Sau khi deploy xong, quay lại tab cũ (vẫn chạy JS cũ)<br>2. Bấm qua lại vài bước 体験<br>3. Đóng tab<br>4. Kiểm tra hàng đợi dọn và dữ liệu 体験 của bot<br>5. Mở tab mới (JS mới) vào lại màn 体験 của bot đó rồi đóng tab | 1 tab JS cũ + 1 tab JS mới | - Tab JS cũ: hoặc không gửi tín hiệu (data init còn nguyên), hoặc gửi và được xử lý đúng — **không** rơi vào trạng thái lỡ dở (xóa một phần)<br>- Không lỗi JS hiển thị cho người dùng<br>- Tab JS mới sau đó dọn được bình thường, không bị chặn bởi phiên cũ | | Lấp `Q3` · **RULE-08** (deploy/domain → product) · Đánh giá spec: Spec không ghi · Evidence: nhật ký request + số bản ghi trước/sau |
| TC-DEPLOYASSET001-02 | Data | DEPLOY-LIVE-001 | Deploy khi hàng đợi còn việc chờ | Boundary | manual | product | Deploy giữa lúc hàng đợi còn việc dọn chưa chạy thì việc dọn không mất và không chạy 2 lần | - Tạo 3 yêu cầu dọn nằm chờ trong hàng đợi<br>- Tiến trình xử lý hàng đợi đang chạy | 1. Ghi lại 3 bản ghi hàng đợi đang chờ<br>2. Thực hiện deploy (tiến trình xử lý hàng đợi bị khởi động lại)<br>3. Chờ tiến trình chạy lại<br>4. Kiểm tra trạng thái 3 bản ghi và dữ liệu 体験 của 3 bot<br>5. Kiểm tra nhật ký xem có bản ghi nào bị xử lý 2 lần không | 3 yêu cầu dọn chờ sẵn | - Cả 3 việc dọn đều kết thúc ở trạng thái cuối (hoàn tất hoặc thất bại có lý do), **không** còn treo ở trạng thái chờ vô hạn<br>- Mỗi bot chỉ bị dọn **đúng 1 lần**<br>- Không việc dọn nào biến mất khỏi hàng đợi mà không có kết luận | | Lấp `Q3` · **RULE-08** (job nền → product) · Đánh giá spec: Spec không ghi · Evidence: ảnh bảng hàng đợi trước/sau + nhật ký |
| TC-LIFFENTRY001-01 | UI | LIFF-ENTRY-001 | Landing / QR seed — phía LINE user | Abnormal | manual | product | LINE user mở lại đường dẫn landing seed sau khi data init bị xóa | - LINE user đã lưu / còn mở đường dẫn landing seed (hoặc còn ảnh QR seed đã chụp)<br>- Cleanup đã chạy xong | 1. Trên điện thoại, mở lại đường dẫn landing seed bằng trình duyệt trong LINE<br>2. Quét lại ảnh QR seed đã chụp trước đó<br>3. Quan sát màn hình LINE user nhìn thấy<br>4. Kiểm tra phía admin có phát sinh bản ghi lượt truy cập / kết bạn mới không | 1 đường dẫn landing + 1 ảnh QR seed | - LINE user thấy trang báo lỗi **có kiểm soát** (không tìm thấy / đã kết thúc), **không** màn trắng, không lỗi hệ thống lộ thông tin<br>- Không phát sinh bản ghi rác phía admin<br>- Không tự động kết bạn / gắn tag vào bot | | Lấp `Q4` · **RULE-06** (output cuối trên LINE) + **RULE-08** (domain → product) · Đánh giá spec: Spec không ghi · Evidence: ảnh màn hình điện thoại |
| TC-LIFFENTRY001-02 | UI | LIFF-ENTRY-001 | Form「アンケート」seed — phía LINE user | Abnormal | manual | product | LINE user đang mở form seed dở dang thì form bị xóa | - LINE user đã mở form「アンケート」seed trên điện thoại và **đang điền dở**, chưa bấm gửi<br>- Admin rời màn 体験 → cleanup chạy trong lúc đó | 1. Để form đang mở dở trên điện thoại<br>2. Kích hoạt cleanup từ phía admin, chờ hoàn tất<br>3. Trên điện thoại bấm gửi form<br>4. Quan sát thông báo LINE user nhận được<br>5. Kiểm tra phía admin có bản ghi câu trả lời nào được tạo không | 1 form seed đang điền dở | - LINE user nhận thông báo lỗi rõ ràng (form không còn tồn tại), **không** màn trắng / không treo vô hạn<br>- **Không** tạo bản ghi câu trả lời mồ côi phía admin<br>- Không văng lỗi hệ thống lộ thông tin kỹ thuật | | Lấp `Q4` · **RULE-06** · Đánh giá spec: Spec không ghi · Evidence: ảnh màn hình điện thoại + màn quản lý form |
| TC-INTGLINE001-01 | API | INTG-LINE-001 | Gỡ rich menu qua LINE API — token hỏng | Abnormal | manual | product | Token LINE hết hạn khi gỡ rich menu: dữ liệu vẫn xóa xong, lỗi chỉ ghi nhật ký | - Bot có data init với rich menu đã đăng ký lên LINE<br>- Làm token LINE của bot hết hạn / không hợp lệ ngay trước khi việc dọn chạy | 1. Kích hoạt dọn<br>2. Chờ việc dọn chạy xong<br>3. Kiểm tra trạng thái bản ghi hàng đợi<br>4. Kiểm tra 17 bảng seed đã về 0 chưa<br>5. Đọc nhật ký phần gọi LINE<br>6. Kiểm tra phía LINE user rich menu còn hiển thị không | Token LINE không hợp lệ | - Việc dọn kết thúc **hoàn tất** (không phải thất bại), dữ liệu seed đã xóa hết<br>- Nhật ký ghi rõ lỗi gọi LINE kèm mã lỗi, **không nuốt lỗi im lặng**<br>- Trạng thái "rich menu chưa gỡ được" nhìn thấy được để vận hành xử lý tay | | Lấp `Q5` · **RULE-05** (tra tài liệu LINE mới nhất) + **RULE-08** · Đánh giá spec: Spec ghi rõ (REQ-012) · Evidence: nhật ký + ảnh khung chat LINE |
| TC-INTGLINE001-02 | API | INTG-LINE-001 | Gỡ rich menu qua LINE API — đã gỡ tay / bị giới hạn tần suất | Boundary | manual | product | Rich menu đã bị gỡ thủ công trước đó, hoặc LINE trả giới hạn tần suất | - Chuẩn bị 2 bot: (a) rich menu seed **đã bị gỡ tay** trên LINE Official Account Manager, (b) bot chạy dọn lặp nhiều lần liên tiếp để chạm giới hạn tần suất của LINE | 1. Với bot (a): kích hoạt dọn, xem hệ thống xử lý phản hồi "không tìm thấy" từ LINE thế nào<br>2. Với bot (b): kích hoạt nhiều việc dọn liên tiếp<br>3. Đọc nhật ký phần gọi LINE của cả 2<br>4. Kiểm tra trạng thái việc dọn và dữ liệu seed | (a) rich menu đã gỡ tay · (b) nhiều việc dọn liên tiếp | - Bot (a): việc dọn **vẫn hoàn tất**, không coi "không tìm thấy" là lỗi nghiêm trọng<br>- Bot (b): bị giới hạn tần suất thì có thử lại hoặc ghi nhật ký rõ ràng, **không** làm hỏng phần dữ liệu đã xóa<br>- Không việc dọn nào kẹt ở trạng thái thất bại vì nguyên nhân bên ngoài | | Lấp `Q5` · **RULE-05** + **RULE-08** · Đánh giá spec: Spec không ghi · Evidence: nhật ký gọi LINE của 2 bot |
| TC-MEDIAIMG001-01 | UI | MEDIA-IMG-001 | Rich menu trên LINE khi gỡ thất bại + ảnh đã xóa | Abnormal | manual | product | Gỡ rich menu thất bại nhưng file ảnh đã bị xóa: LINE user thấy gì | - Bot có rich menu seed đã đăng ký lên LINE và LINE user đang thấy trong khung chat<br>- Làm bước gỡ rich menu thất bại (token hỏng / chặn mạng tới LINE) nhưng **vẫn để bước xóa file ảnh chạy** | 1. Kích hoạt dọn, chờ hoàn tất<br>2. Trên điện thoại LINE user, mở khung chat với bot<br>3. Quan sát rich menu: còn hiện không, ảnh có vỡ không<br>4. Bấm vào từng vùng của rich menu<br>5. Kiểm tra nhật ký phía server | 1 LINE user thật đang thấy rich menu | - Ghi nhận chính xác LINE user nhìn thấy gì (rich menu còn / ảnh vỡ / bấm ra lỗi)<br>- Hệ thống **không** trả lỗi kỹ thuật về phía LINE user<br>- Trạng thái không nhất quán này phải nhìn thấy được trong nhật ký để vận hành dọn tay | | Lấp `Q9` · **RULE-06** + **RULE-08** · Đánh giá spec: **Spec không ghi — đã đưa §6** · Evidence: ảnh/video khung chat LINE + nhật ký |

**GAP/RISK không đề xuất TC mới** (đã có TC, chỉ cần chạy hoặc sửa mã):

- `G3` → chạy `NEW-107` trên **production** (hiện `env_scope` chỉ `dev/staging`, chưa chạy). Không viết TC mới.
- `G4` → chạy `NEW-112` + `NEW-113` trên staging/production với LINE user thật. Không viết TC mới.
- `Q7` → `JOB-001` đã được cover về nội dung bởi `NEW-101` / `NEW-66` / `NEW-67` / `NEW-68` / `NEW-71` / `NEW-73`; chỉ cần **đổi mã quan điểm** từ `JOB-002` sang `JOB-001` trên Studio.
- `Q8` → `REG-SPEC-001` phụ thuộc quyết định spec ở §6; viết TC trước khi chốt spec sẽ phải viết lại. Sau khi §6 chốt, chạy lại toàn bộ TC thuộc 3 vùng lệch (`NEW-70`, `NEW-100`, `NEW-103`, `NEW-108`).

---

## 6. Spec update needed

| # | Vấn đề | Hai phương án | Ai chốt |
|---|---|---|---|
| 1 | **Feature flag `dry_run` (technical spec §7)** — spec coi công tắc `tutorial_init_data_cleanup` (off → dry_run → on) là **hàng rào BẮT BUỘC** và là biện pháp giảm thiểu duy nhất còn lại sau khi bỏ soft-delete. Code **không có** key này (`config/sns-line.php`). Run #1295 từng fail 8 TC vì thiếu; sau đó `NEW-70` được viết lại thành "ghi nhận thực tế: không có công tắc nào" và **pass**. | (a) Bổ sung feature flag theo spec §7 → viết lại `NEW-70` theo chuẩn spec · (b) Bỏ yêu cầu flag khỏi spec → ghi rõ đường lùi vận hành thay thế (hiện chỉ là "dừng tiến trình xử lý hàng đợi") | Dev + PM + Leader — **trước khi lên production** |
| 2 | **BR-C05 — xóa log phiên** — spec yêu cầu xóa `tag_history`, `detail_landing_click`, `landing_histories`, `auto_reply_history`, `detail_click_richmenu` + `tag_line_user`. Code **không xóa**. Hai TC mâu thuẫn trực tiếp: `NEW-100` (vẫn còn — pass) vs `NEW-103` (phải về 0 — chưa chạy). | (a) Implement xóa log theo BR-C05 → `NEW-100` phải sửa expected · (b) Sửa BR-C05 thành "giữ log" → `NEW-103` phải sửa expected + bổ sung TC cho hệ quả dữ liệu mồ côi (`TC-DATAREF001-01`) | Dev + PM chốt, Leader duyệt |
| 3 | **`notify_setting` bị chạm dù spec liệt "không xóa"** — implement có detach mã seed khỏi CSV (`NEW-76` / `NEW-65` đang verify theo hành vi code). | (a) Sửa spec: ghi rõ `notify_setting` **được phép** gỡ đúng mã seed · (b) Bỏ bước detach khỏi code | Dev + PM |
| 4 | **Cơ chế xóa tag: cứng hay mềm?** — kho TC `FA-012` cho thấy tag LME dùng **soft delete** (`deleted_at`, màn `削除済みタグ`, `TC-TAG-36` / `TC-TAG-63` unique chỉ xét tag active, `TC-TAG-117` action gắn tag đã xoá → không tạo liên kết). Cleanup này **xóa cứng** tag seed và **để lại** `tag_line_user`. Hai giả định không thể cùng đúng. | (a) Cleanup dùng soft delete cho tag như phần còn lại của hệ thống · (b) Giữ xóa cứng nhưng phải xóa kèm `tag_line_user` và ghi rõ ngoại lệ này vào spec `FA-012` | Dev + PM + Leader (ảnh hưởng cả `FA-012`) |
| 5 | **Ticket nói "ĐỀU xóa" nhưng implement là best-effort** — Redmine #40153 ghi *"user close/nhảy trang khác thì mình **đều** xóa data init"*; thực tế tín hiệu là best-effort: mất mạng / tab bị kill / phiên hết hạn → data init **nằm lại vĩnh viễn**, không có tiến trình dọn bù, không có chỉ số phát hiện (REQ-014, `NEW-108` / `NEW-121`). | (a) Bổ sung tiến trình quét dọn bù định kỳ → đúng cam kết "đều xóa" · (b) Sửa mô tả ticket + spec thành "best-effort", nêu rõ giới hạn cho CS/PM | PM + Leader |
| 6 | **Hai cơ chế dọn song song** — `TutorialInitDataCleanupJob` và `OnboardingSessionCleanupCommand` (file mới, +42) chưa có tài liệu mô tả quan hệ/thứ tự. | Bổ sung mô tả vào spec: cái nào chạy trước, có phụ thuộc nhau không, chạy đồng thời thì sao | Dev |
| 7 | **Mã lỗi của EP-C01 khi thiếu phiên** — trả **419 TokenMismatch** (CSRF) thay vì **401**, vì endpoint nằm sau CSRF middleware (chỉ `/ajax/*` được miễn). FE gửi bằng beacon nên rất dễ rơi vào trường hợp này. | Chốt mã lỗi chuẩn cho contract EP-C01 và ghi vào spec — quyết định này ảnh hưởng trực tiếp tới `TC-STATECLEAN001-02` (phân biệt "bị chặn có chủ đích" vs "bị chặn tình cờ") | Dev + Leader |
| 8 | **Spec màn 体験 không có trong repo** — `spec-features/admin/bot-add-v2/feature-spec.md` §1 ghi rõ *"Không bao gồm: chi tiết tính năng tutorial (/manual/tutorial)"*. Toàn bộ business rule (`BR-C01`…`BR-C05`, `QA-spec-*`, `EP-C01`) chỉ tồn tại trong `requirements` của Studio. | Bổ sung spec màn 体験 vào `spec-features/`, hoặc Leader xác nhận chính thức bộ 17 REQ của Studio là nguồn spec chuẩn | Leader |
| **9** | ⚠️ **`REQ-004` NGƯỢC chuẩn Leader** (phát hiện 2026-09-14) — Leader chốt **"mở 1 tab khác → thao tác bất kỳ" PHẢI xóa data init**, trong khi `REQ-004` của Studio + `NEW-80` + `NEW-86` đều khẳng định chuyển tab **không** kích hoạt xóa. Kèm theo: **đăng xuất chưa được Leader nhắc tới** nhưng `REQ-004` xếp vào nhóm "không xóa", còn code thì **vẫn bắn tín hiệu** (run #1295). | (a) **Sửa `REQ-004` + `NEW-80` + `NEW-86` trên Studio** theo chuẩn Leader (code nhiều khả năng đã đúng) — và chốt luôn **đăng xuất**: xóa hay không · (b) Nếu Leader đổi ý giữ `REQ-004` thì phải **sửa code FE** (không dùng `visibilitychange`, chỉ dùng `pagehide` + điều hướng) | **Leader chốt trước**, Dev + QA thực thi — **chặn round 2** |
| **10** | **Ranh giới "tắt browser" vs "tab bị kill đột ngột"** — Leader nói tắt browser **phải xóa**; `NEW-121` (tab bị kill / mất mạng) lại expect **không xóa** và được chấp nhận (`REQ-014` best-effort). Hai case chỉ khác nhau ở cách tiến trình kết thúc — hiện **không có tài liệu nào định nghĩa ranh giới**. | Ghi rõ vào spec: thoát trình duyệt bằng lệnh thoát bình thường = **phải xóa**; tiến trình bị kill / mất mạng = **chấp nhận không xóa**. Kèm danh sách trình duyệt được cam kết hỗ trợ | PM + Dev + Leader |
