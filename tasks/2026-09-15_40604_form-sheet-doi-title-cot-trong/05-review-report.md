# 05 — Review Report

> Draft cho Leader verify. Chỉ ghi phần THIẾU + việc phải làm.

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | `(1) MCP LME TEST STUDIO — task #302 (round 1, tc-ready, reviewState=leader, branch m_202609_form_googlesheet_change_title_40604)` |
| Tổng số TC review | `29` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `19 vùng ảnh hưởng rà 2 chiều · 10 đủ TC · 7 GAP · 2 RISK` (+ 1 TC orphan).

> ⚠️ `spec_delta.diffAvailable = false` — Studio **không chạy được `git diff`** (không tìm thấy ref `m_202609_form_googlesheet_change_title_40604`). Chiều (b) dựa trên `dev_impact` của Studio (có số dòng cụ thể, suy từ đọc source) + mục 1/2/3 của `03-dev-impact.md`, **không có `diffStat` để đối chiếu file thật**.

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | `F4 — DataFormAnswerResult.getFormDetailIdCustom`: nhánh **có `form_detail_id` nhưng không parse được số** (rỗng / chuỗi chữ) | `dev-impact` | không có | `GAP` — hàm mới có 3 nhánh (id hợp lệ / thiếu field / không parse được), TC chỉ cover 2 | `[MAJOR]` |
| G2 | `F3 + T3 — parseValueCell` ở **nhánh ghi lại toàn bộ** (select / radio / checkbox / dropdown sau khi đổi title) | `dev-impact` | `NEW-2` — có chạm 1 câu 単一選択 nhưng expected chỉ đòi "có giá trị", không đòi **nhãn thay vì giá trị thô** | `RISK` — Dev kê "test cả 2 nhánh"; chỉ nhánh ghi thêm dòng mới (`NEW-15`) có TC đúng chiều | `[MAJOR]` |
| G3 | `getTitleRow` / `getArrayHeaderTitle` — sheet **còn dữ liệu nhưng header hỏng** (header bị sửa/xoá, `回答ID` không ở A1/B1), hoặc **ô cột A trống giữa vùng data** | `diff code` | không có | `GAP` — bản vá đổi cách dựng header nhưng không TC nào chạm 2 ca này | `[MAJOR]` |
| G4 | **Cách kích hoạt nhánh ghi lại toàn bộ**: đổi tên tab / xoá tab sheet (đúng thao tác "đã tạo lại sheet" khách mô tả) | `diff code` | không có — 12/12 TC nhánh này đều kích hoạt bằng "xoá sạch nội dung sheet" | `GAP` — chưa chứng minh ca thật của khách đi vào đúng nhánh được vá | `[MAJOR]` |
| G5 | Ghi lại toàn bộ ở **khối lượng thật**: ghi theo lô 500 record, nhiều luồng, lock theo `form_id`, hạn mức Google API | `diff code` | `NEW-9` — 300 bản ghi nhưng mục đích là race, không đo lô / hạn mức | `GAP` | `[MAJOR]` |
| G6 | Bản ghi đang xử lý dở / chờ thử lại **tại thời điểm deploy bản vá** (job vắt qua release) | `diff code` | không có | `GAP` — header cũ (dựng bởi code cũ) và vòng ghi mới chạy lẫn nhau | `[MAJOR]` |
| G7 | **Phạm vi tác động**: 2 form cùng bot (2 spreadsheet) / 2 bot đồng bộ song song — không ghi nhầm sheet, không đụng trạng thái form khác | `dev-impact` + `diff code` | không có | `GAP` — RULE-07 (`WHERE` scope). Hạ từ `[BLOCKER]` xuống `[MAJOR]` vì bản vá không chạm truy vấn/scope, chỉ chạm map cột | `[MAJOR]` |
| G8 | `BUG` — **ca lỗi thật của ticket**: dữ liệu câu trả lời cũ của bot `195346` / form `235311` **có** `form_detail_id` hay không | `dev-impact` | không có | `GAP` — nếu data của khách thuộc nhóm "đời cũ không có id" thì bản vá **không lấp được ticket**; không TC nào kiểm chứng trên data thật | `[BLOCKER]` |
| G9 | `NEW-10` (data cũ + title đã đổi) và `NEW-11` (LINE user đã xoá) | `dev-impact` | có TC | `RISK` — expected ghi nguyên văn "CHƯA CHỐT" / "CẦN XÁC NHẬN" nhưng vẫn được đánh **Đạt** → không kết luận được gì | `[BLOCKER]` |
| G10 | `NEW-31` — xoá câu hỏi trong lúc user đang điền form | `orphan` | — | Không thuộc `BUG` / `F*` / `D*` / `T*` nào của bản vá (chính TC cũng ghi "chỉ đổi tiêu đề sẽ KHÔNG bị chặn") | `[NIT]` |

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `20 quan điểm Trigger khớp task · 9 chưa cover đủ`.

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `OUT-EXPORT-001` | `Trung bình → BẮT BUỘC (nâng Cao)` — ghi Google Spreadsheet | `GAP` — 0 TC mang mã. Thiếu: giá trị `0` vs bỏ trống + **mất số 0 đầu** (090…, 〒166-…), giá trị bắt đầu bằng `=` / `+` / `-` / `@`, test **từng giá trị enum riêng biệt**, export vượt ngưỡng > 500 bản ghi | `[BLOCKER]` |
| Q2 | `INTG-SHEET-001` | `Cao` | `RISK` — chỉ 1 TC Abnormal mang mã (`NEW-24`). Thiếu Normal + Boundary (RULE-01) và 3 kịch bản phá của quan điểm: **đổi tên / di chuyển file**, **quyền truy cập bị thu hồi**, **rate limit API** | `[MAJOR]` |
| Q3 | `REG-RUN-001` | `Cao` | `GAP` — không TC nào có dữ liệu/job **đang chạy dở từ TRƯỚC release** rồi tiếp tục sau khi deploy bản vá | `[BLOCKER]` |
| Q4 | `JOB-001` | `Cao` | `RISK` — 4 TC nội dung đúng nhưng mang mã `JOB-002` (không có trong checklist → không trace được). Thiếu: tra rate limit Google mới nhất (RULE-05), dựng tải **vượt ngưỡng**, log thấy retry + **backoff**, đối chiếu **số bản ghi vào = thành công + hàng đợi lỗi** ở quy mô lớn | `[MAJOR]` |
| Q5 | `PERF-LARGE-001` | `Trung bình → Cao` (sync) | `GAP` — không TC nào lấy **số liệu khách lớn nhất** làm mốc test data; TC lớn nhất mới 300 bản ghi | `[MAJOR]` |
| Q6 | `DATA-MIG-001` | `Cao` | `RISK` — có Abnormal (`NEW-10`) nhưng expected chưa chốt; thiếu Boundary (`form_detail_id` không parse được số — xem G1) | `[MAJOR]` |
| Q7 | `DATA-DB-001` | `Cao` | `GAP` — không TC nào kiểm `WHERE` scope trên **2 form / 2 tài khoản** (RULE-07). TC duy nhất mang mã (`NEW-30`) chỉ đọc cấu trúc dữ liệu | `[MAJOR]` |
| Q8 | `DATA-TEXT-001` | `Trung bình → Cao` (text ghi ra Google) | `RISK` — `NEW-12` chỉ test **title câu hỏi**; chưa test **giá trị câu trả lời** nhiều dòng / emoji / dài / chứa dấu phẩy ở nhánh ghi lại toàn bộ | `[MAJOR]` |
| Q9 | `COMPAT-LEGACY-001` | `Cao` | `RISK` — cặp "header spread cũ ⇄ mới" và "form cũ ⇄ mới" **có TC nội dung** nhưng 5 TC gắn mã `RULE-09` (là số hiệu RULE, không phải mã quan điểm) → coverage không trace được về quan điểm | `[MINOR]` |

---

## 3. TC trùng lặp nội dung

Đã rà **29** TC — **không có `DUP-EXACT`, không `DUP-INFLATE`, không `DUP-CONFLICT` nội bộ**; 2 nhóm cận trùng dạng `SUBSET`:

| Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| DUP-1 | `NEW-2` (đổi title 3 câu hỏi) | `NEW-1` — **GỘP, KHÔNG xoá** | `DUP-SUBSET` | Cùng nhánh ghi lại toàn bộ · cùng `Normal` · cùng thao tác (đổi title → xoá sheet → ghi lại) · tiền đề tương đương (form 1 page + câu trả lời cũ) → expected tương đương "cột đổi title có đủ giá trị cũ" | `[MINOR]` |
| DUP-2 | `NEW-10` (data cũ + title đã đổi) | `NEW-6` phần dữ liệu đời cũ — **GỘP expected, KHÔNG xoá** | `DUP-SUBSET` | Cùng đối tượng (dữ liệu đời cũ × title đã đổi) · cùng thao tác ghi lại toàn bộ · tiền đề tương đương · `NEW-6` tự khai expected của nhóm này là "đối chiếu với kết luận ở TC dành riêng" → chính là `NEW-10` | `[MAJOR]` |

- **Gate đã chạy**: giả định xoá `NEW-1` → mất TC duy nhất verify **header 6 cột mặc định** và mất ca gốc của ticket (câu hỏi ngày giờ 参加希望日時) → **đổi sang GỘP** (giữ cả 2; nếu sau này muốn rút gọn thì bổ sung mục kiểm header 6 cột vào `NEW-2` trước). Giả định bỏ phần dữ liệu cũ của `NEW-6` → `NEW-10` vẫn cover, nhưng `NEW-6` còn cover "trộn cũ + mới trong **cùng một lần** ghi lại" nên giữ, chỉ sửa expected.
- `DUP-CONFLICT` với **kho TCs** (không phải nội bộ): `NEW-12` (title 「 参加人数 」 giữ nguyên khoảng trắng đầu/cuối) vs `TC-FORM-386` của kho ("tiêu đề item có khoảng trắng đầu-cuối → **tự trim**") → đã đưa §6, không tự chọn bên.
- Xoá/gộp thật do human thực hiện trên Studio (`testcase_update` / `testcase_delete`).

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | `NEW-10`, `NEW-11` (2/29 TC) | Expected ghi nguyên văn **"CHƯA CHỐT — cần Dev/PO xác nhận"** / **"CẦN XÁC NHẬN"** nhưng `last_exec.status = pass` → đang tính Đạt cho TC **không có chuẩn đúng/sai**. `NEW-10` lại chính là nhánh có thể trùng ca khách (dữ liệu đời cũ + title đã đổi) | Chốt expected với Dev/PO **trước**, `testcase_update` rồi chạy lại; trong lúc chưa chốt phải để `Chưa test`, không để `Đạt` |
| I2 | `[BLOCKER]` | Toàn bộ 29 TC | **0 TC chạy staging/production** — `envAuto` cho thấy 29/29 chạy `local` (2 run). Task là **job nền gọi Google Sheets API thật** → RULE-08 / `ENV-003` cấm kết luận từ local | Chạy lại tối thiểu bộ lõi trên production/staging với tài khoản Google thật: `NEW-1`, `NEW-2`, `NEW-14`, `NEW-15`, `NEW-20`, `NEW-24` + TC mới `TC-ENV003-01` |
| I3 | `[MAJOR]` | Toàn bộ 29 TC | Cột `Evidence thực tế` **rỗng 29/29** dù tất cả đánh Đạt → RULE-02 (kết quả tự khai, không kiểm chứng lại được) | Đính ảnh sheet + ảnh màn danh sách câu trả lời + log job cho từng TC trước khi Leader duyệt |
| I4 | `[MAJOR]` | 13/29 TC | Mã quan điểm **không có trong `framework/checklist-lme.md`**: `RULE-09` ×5, `JOB-002` ×4, `TOOL-NEGCTRL-001` ×2, `RULE-TOOL-027` ×1, `I18N-001` ×1 → không map được coverage cho 45% bộ TC | Map lại: `RULE-09` → `COMPAT-LEGACY-001` · `JOB-002` → `JOB-001` · `I18N-001` → `OUT-EXPORT-001` (format datetime) · `RULE-TOOL-027` / `TOOL-NEGCTRL-001` là mã nội bộ Studio → ghép thêm 1 mã chuẩn |
| I5 | `[MAJOR]` | Toàn bộ 29 TC + spec FA-011 | `Trạng thái đánh giá spec` trống 29/29, trong khi spec gốc [feature-spec.md](../../spec-features/admin/form-answer/feature-spec.md) BR-08 tự nhận "job `AddResultFormAnswerToGoogleSpreadSheet` **bị comment out**, sync có thể đang dùng cơ chế khác" (Gap #11) → mọi expected suy từ code, không có chuẩn nghiệp vụ độc lập | Điền `Spec không ghi` + ghi rõ đã hỏi ai; đồng thời update BR-08 (§6) |
| I6 | `[MAJOR]` | `NEW-6` | Expected dẫn chiếu sang TC khác ("ghi nhận kết quả thực tế và đối chiếu với kết luận của Dev/PO ở TC dành riêng") → không đo lường được, tester không biết pass hay fail | Viết expected cứng cho **cả 2 nhóm dữ liệu** sau khi chốt I1 |
| I7 | `[MAJOR]` | `03-dev-impact.md` | Auto-fill `2026-09-15 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" chưa tick**. `dev_impact` của Studio nêu **4 điểm Dev không kê ở mục 4.3**: sheet 4 cột vs 6 cột, form 1 page vs nhiều page, `lineUser == null`, đường phục hồi `RetryErrorGoogleSheetTask` | Tester đọc lại Redmine journal #136470 + tick checkbox trước khi Leader duyệt |
| I8 | `[MAJOR]` | `NEW-12` vs kho `TC-FORM-386` | `SPEC-CONFLICT`: Studio kỳ vọng title 「 参加人数 」 **giữ nguyên** khoảng trắng đầu/cuối; kho kỳ vọng hệ thống **tự trim**. Nếu hệ thống trim khi lưu thì phép so title (đường fallback của bản vá) có thể lệch với data cũ | Không tự chọn bên → §6, Dev chốt |
| I9 | `[MINOR]` | `NEW-26` | Không atomic — 1 TC gộp 2 kịch bản biên (số lần đã thử = 4 và = 5) với 2 expected riêng | Tách thành 2 TC |
| I10 | `[NIT]` | Nguồn Studio | `test_viewpoint_selection = null` (không có bảng quan điểm do Studio chốt để đối chiếu); `spec_delta.diffAvailable = false` vì branch không tồn tại trên repo Studio | Push branch lên remote để lần review sau có diff thật |

---

## 5. TCs đề xuất bổ sung (10)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa011-taobieumau-フォーム作成.md` — nhóm **"Sync Google Sheet & job" (32 TC)** + "Liên kết Google Sheet" (14 TC) |
| Vùng regression phát hiện từ kho | `TC-FORM-378` (đổi tên/xoá sheet → tạo lại + fill toàn bộ) · `TC-FORM-390` (sheet hỏng cấu trúc → dựng lại header) · `TC-FORM-394` (ô cột A trống → không đè dòng) · `TC-FORM-391` + `TC-FORM-396` (backoff 1p→5p→10p→30p→60p, ghi theo lô 500) · `TC-FORM-398` (restart ứng dụng khi job đang chạy) · `TC-FORM-399` (Google trả 429 → backoff) · `TC-FORM-397` + `TC-FORM-402` (không ghi nhầm sheet form khác) · `TC-FORM-404` (sheet hàng nghìn dòng) · `TC-FORM-405` (Unicode/emoji) — **tất cả nằm trong vùng bản vá chạm** (`getArrayTitle` / `getTitleRow` / `parseValueCell`) nên là regression bắt buộc |
| Conflict expected vs kho | `NEW-12` vs `TC-FORM-386` (khoảng trắng đầu/cuối của title: giữ nguyên hay trim) → `[MAJOR] SPEC-CONFLICT`, đã đưa §6 |
| GAP dùng lại TC kho (không viết mới) | `G3 → TC-FORM-390` + `TC-FORM-394` · `G6 / Q3 → TC-FORM-398` · `Q4 → TC-FORM-399` + `TC-FORM-391` — chỉ cần **chạy lại trên branch fix** |
| Xác nhận chống trùng | Đã đối chiếu **29** TC ở BƯỚC 0 + kho FA-011 — **không TC đề xuất nào trùng** |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-DATAMIG001-02 | Data | DATA-MIG-001 | Sync Google Sheet & job | Boundary | manual | Tất cả | Câu trả lời cũ có mã câu hỏi ở dạng không hợp lệ (rỗng / chuỗi chữ) — job vẫn chạy hết và ghép cột theo tên câu hỏi | Form đời mới 1 page đã liên kết Google Sheet, có 3 câu trả lời cũ điền đủ. Trong DB test: bản ghi 2 có mã câu hỏi của 1 câu là chuỗi rỗng, bản ghi 3 có mã là chuỗi chữ `abc`, bản ghi 1 giữ mã hợp lệ | 1. Mở màn danh sách câu trả lời, ghi lại giá trị của cả 3 bản ghi<br>2. Chưa đổi title: xoá sạch nội dung sheet đích, trả lời form 1 lần để kích hoạt ghi lại toàn bộ, chờ job xong rồi đối chiếu 3 dòng cũ<br>3. Mở màn chỉnh sửa form, đổi title câu hỏi đó rồi lưu<br>4. Xoá sạch sheet lần nữa, trả lời form 1 lần, chờ job xong<br>5. Đối chiếu ô của 3 dòng cũ và đọc log job | Mã câu hỏi: bản ghi 1 = số hợp lệ · bản ghi 2 = rỗng · bản ghi 3 = `abc` | Bước 2: cả 3 dòng có đủ giá trị (bản ghi mã rỗng/chữ tự đi đường so tên câu hỏi). Bước 4-5: bản ghi 1 có giá trị ở cột title mới; bản ghi 2 và 3 cho kết quả **giống hệt nhóm dữ liệu đời cũ** đã chốt ở `NEW-10`, không được khác. Job chạy hết, không văng lỗi parse, không dừng giữa chừng, các cột khác không lệch |  | Lấp `G1` · `Q6` · Đánh giá spec: Đã hỏi leader (chốt cùng `NEW-10`) · Evidence: ảnh sheet + log job |
| TC-REGSHARED001-03 | Data | REG-SHARED-001 | Sync Google Sheet & job | Normal | manual | Tất cả | Ghi lại toàn bộ sau khi đổi title — 4 kiểu câu hỏi lựa chọn vẫn ra nhãn, không ra giá trị thô | Form đời mới đã liên kết Google Sheet, có đủ 4 kiểu câu hỏi lựa chọn (chọn một đáp án, nút chọn, hộp chọn nhiều đáp án, danh sách xổ xuống), mỗi lựa chọn có nhãn khác giá trị lưu. Đã có 3 câu trả lời cũ chọn các nhãn khác nhau, trong đó 1 câu chọn từ 2 nhãn trở lên | 1. Mở màn chỉnh sửa form, ghi lại nhãn của từng lựa chọn<br>2. Mở màn danh sách câu trả lời, ghi lại giá trị của 3 câu trả lời cũ<br>3. Đổi title của cả 4 câu hỏi lựa chọn rồi lưu form<br>4. Xoá sạch nội dung sheet đích<br>5. Trả lời form 1 lần để kích hoạt ghi lại toàn bộ<br>6. Chờ job xong, đối chiếu 4 cột lựa chọn ở cả 3 dòng cũ | Nhãn 「午前の部」 ứng giá trị lưu 1, 「午後の部」 ứng 2; câu nhiều lựa chọn chọn 2 nhãn | Ở cả 3 dòng câu trả lời cũ, 4 cột lựa chọn hiển thị **nhãn tiếng Nhật** đúng như trên form (vd 「午後の部」), không phải số/giá trị thô và không trống. Câu nhiều lựa chọn hiện đủ các nhãn đã chọn, cách ngăn giống màn danh sách câu trả lời |  | Lấp `G2` · `Q1` (enum từng giá trị) · regression — `parseValueCell` dùng chung 2 nhánh · Evidence: ảnh sheet + ảnh màn danh sách câu trả lời |
| TC-INTGSHEET001-02 | API | INTG-SHEET-001 | Sync Google Sheet & job | Normal | manual | product | Đổi tên tab rồi xoá hẳn tab sheet sau khi đã đổi title — tab dựng lại chứa đủ câu trả lời cũ | Form 1 page đã liên kết Google Sheet bằng tài khoản Google thật, có ít nhất 3 câu trả lời cũ. Đã đổi title câu hỏi ngày giờ và lưu form | 1. Mở màn danh sách câu trả lời, ghi lại giá trị câu hỏi vừa đổi title của 3 bản ghi cũ<br>2. Trên spreadsheet, đổi tên tab 「シート1」 thành tên khác, KHÔNG xoá nội dung<br>3. Trả lời form 1 lần từ trình duyệt, chờ job chạy xong<br>4. Mở spreadsheet kiểm tra tab nào được ghi và nội dung tab bị đổi tên<br>5. Lặp lại kịch bản với thao tác xoá hẳn tab thay vì đổi tên | Tab 「シート1」 → đổi tên thành 「回答_old」; lần 2 xoá hẳn tab | Hệ thống dựng lại tab đích và ghi lại toàn bộ câu trả lời: cột title mới có đủ giá trị ở cả 3 dòng cũ, bằng đúng giá trị ghi ở bước 1. Tab bị đổi tên giữ nguyên nội dung cũ, không bị xoá hay ghi đè. Không có dòng trùng, mỗi 回答ID xuất hiện đúng 1 lần |  | Lấp `G4` · `Q2` · regression — dẫn từ `TC-FORM-378` · RULE-08 (Google API thật) · Evidence: ảnh spreadsheet trước/sau + log job |
| TC-INTGSHEET001-03 | API | INTG-SHEET-001 | Sync Google Sheet & job | Abnormal | manual | product | Quyền truy cập spreadsheet bị thu hồi giữa lúc đang ghi lại toàn bộ — job dừng có kiểm soát, retry xong không mất dòng | Form đã liên kết Google Sheet bằng tài khoản Google thật, có ít nhất 50 câu trả lời cũ, đã đổi title 1 câu hỏi. Có quyền gỡ chia sẻ file trên Google Drive | 1. Ghi lại tổng số câu trả lời trên màn danh sách câu trả lời<br>2. Xoá sạch nội dung sheet, trả lời form 1 lần để kích hoạt ghi lại toàn bộ<br>3. Ngay khi job bắt đầu ghi, gỡ quyền truy cập file của tài khoản đang liên kết<br>4. Chờ job kết thúc, đọc trạng thái bản ghi kết quả, hàng đợi lỗi Google và log job<br>5. Cấp lại quyền, để tiến trình thử lại chạy, rồi đối chiếu lại sheet | Gỡ quyền truy cập file của tài khoản liên kết trong lúc job đang ghi | Job không treo, không lặp vô hạn; lỗi quyền được ghi vào hàng đợi lỗi kèm số lần thử và mốc thử lại kế tiếp. Dữ liệu câu trả lời trong LME nguyên vẹn, vẫn xem được trên màn danh sách. Sau khi cấp lại quyền và retry: số dòng trên sheet bằng đúng tổng số câu trả lời, không dòng trùng, cột đổi title có đủ giá trị |  | Lấp `Q2` · RULE-08 · Evidence: log job (thấy retry) + ảnh sheet sau retry |
| TC-OUTEXPORT001-01 | API | OUT-EXPORT-001 | Sync Google Sheet & job | Abnormal | manual | Tất cả | Câu trả lời bắt đầu bằng `=` `+` `-` `@` — ô trên sheet giữ nguyên chuỗi, không bị hiểu thành công thức | Form đã liên kết Google Sheet, có câu hỏi nhập chữ tự do và sheet đích đã có sẵn dữ liệu | 1. Trả lời form với từng giá trị ở cột dữ liệu nhập (mỗi giá trị 1 lần trả lời)<br>2. Chờ job ghi các dòng mới, mở sheet kiểm từng ô và thanh công thức<br>3. Đổi title câu hỏi đó rồi lưu form<br>4. Xoá sạch nội dung sheet, trả lời form 1 lần để kích hoạt ghi lại toàn bộ<br>5. Chờ job xong, kiểm lại chính các ô đó ở nhánh ghi lại toàn bộ | `=1+1` · `+81 90-1234-5678` · `-100` · `@yamada` · `=HYPERLINK("http://example.com","x")` | Ở **cả hai nhánh**: ô hiển thị đúng nguyên chuỗi đã nhập kể cả dấu `=` ở đầu; không bị Google Sheets tính thành công thức, không ra `#ERROR!` / `#NAME?` / link tự sinh; giá trị khớp màn danh sách câu trả lời |  | Lấp `Q1` · Evidence: ảnh ô sheet kèm thanh công thức + ảnh màn danh sách câu trả lời |
| TC-OUTEXPORT001-02 | API | OUT-EXPORT-001 | Sync Google Sheet & job | Boundary | manual | Tất cả | Giá trị có số 0 ở đầu, giá trị `0` và câu bỏ trống — không mất số 0, phân biệt được rỗng với 0 | Form đã liên kết Google Sheet, có câu hỏi nhập chữ (số điện thoại, mã bưu điện), 1 câu hỏi số và 1 câu hỏi KHÔNG bắt buộc | 1. Trả lời form lần 1 với các giá trị ở cột dữ liệu nhập, câu số nhập `0`, bỏ trống câu không bắt buộc<br>2. Chờ job ghi dòng mới, kiểm từng ô trên sheet<br>3. Đổi title 3 câu hỏi đó rồi lưu form<br>4. Xoá sạch nội dung sheet, trả lời form 1 lần để ghi lại toàn bộ<br>5. Chờ job xong, kiểm lại chính các ô đó | `09012345678` · `007` · `166-0003` · câu số = `0` · câu không bắt buộc để trống | Ở cả hai nhánh: ô số điện thoại giữ đủ `09012345678` (không mất số 0 đầu, không thành dạng số khoa học); `007` giữ nguyên; ô câu số hiển thị `0`; ô câu bỏ trống để trống, KHÔNG hiển thị `0` hay `null`; các cột phía sau không lệch |  | Lấp `Q1` · Evidence: ảnh ô sheet kèm thanh công thức |
| TC-PERFLARGE001-01 | API | PERF-LARGE-001 | Sync Google Sheet & job | Boundary | manual | product | Ghi lại toàn bộ ở quy mô khách lớn nhất sau khi đổi title — đủ dòng, đúng cột, không vượt hạn mức Google | Form đã liên kết Google Sheet, có số câu trả lời **bằng hoặc lớn hơn form nhiều câu trả lời nhất hiện có của khách** (tối thiểu 500 — tra số thật trước khi dựng data). Đã đổi title 1 câu hỏi và lưu form | 1. Ghi lại tổng số câu trả lời trên màn danh sách câu trả lời<br>2. Xoá sạch nội dung sheet đích<br>3. Trả lời form 1 lần để kích hoạt ghi lại toàn bộ, bấm giờ<br>4. Theo dõi log job: số lần gọi ghi, kích thước mỗi lô, số luồng, lỗi hạn mức<br>5. Đếm số dòng trên sheet và kiểm 20 dòng ngẫu nhiên ở cột vừa đổi title | Số câu trả lời ≥ 500 — ghi rõ con số thật đã dùng và nguồn số liệu khách lớn nhất | Số dòng dữ liệu trên sheet bằng tổng số câu trả lời; mỗi 回答ID xuất hiện đúng 1 lần; 20 dòng kiểm ngẫu nhiên đều có giá trị đúng ở cột đổi title. Job hoàn tất, không lỗi vượt hạn mức, log cho thấy chia lô khi ghi. Ghi lại thời gian chạy làm mốc so sánh cho lần sau |  | Lấp `G5` · `Q5` · RULE-08 · dẫn từ `TC-FORM-396`, `TC-FORM-404` · Evidence: log job + số đo thời gian + ảnh sheet |
| TC-DATADB001-02 | Data | DATA-DB-001 | Sync Google Sheet & job | Normal | manual | Tất cả | Hai form cùng bot và hai bot đồng bộ song song — không ghi nhầm spreadsheet, không đụng trạng thái form khác | Bot A có form F1 và F2, mỗi form liên kết 1 spreadsheet riêng và đều đã có câu trả lời cũ. Bot B có form F3 liên kết tài khoản Google khác. Đã đổi title 1 câu hỏi **chỉ ở F1** | 1. Ghi lại số dòng, nội dung sheet của F2 và F3 cùng trạng thái đồng bộ các bản ghi của chúng<br>2. Xoá sạch nội dung sheet của F1<br>3. Trả lời F1, rồi trong vòng vài giây trả lời tiếp F2 và F3<br>4. Chờ job chạy xong<br>5. Đối chiếu cả 3 sheet và trạng thái đồng bộ của bản ghi cũ ở F2, F3 | 3 form thuộc 2 bot, 3 spreadsheet khác nhau, trả lời gần như đồng thời | Sheet F1 được ghi lại từ đầu, đủ dòng và đúng cột với title mới. Sheet F2 và F3 **chỉ thêm đúng dòng mới của chính mình**, không bị ghi lại từ đầu, không xuất hiện câu trả lời của form khác, header không đổi. Trạng thái đồng bộ của các bản ghi cũ ở F2, F3 giữ nguyên. Dữ liệu 2 bot không lẫn nhau |  | Lấp `G7` · `Q7` · `SEC-001` (dữ liệu cá nhân khách) · dẫn từ `TC-FORM-397`, `TC-FORM-402` · Evidence: ảnh 3 sheet + bảng trạng thái bản ghi |
| TC-DATATEXT001-02 | Data | DATA-TEXT-001 | Sync Google Sheet & job | Boundary | manual | Tất cả | Giá trị câu trả lời nhiều dòng / emoji / dấu phẩy / rất dài — ghi gọn trong 1 ô ở nhánh ghi lại toàn bộ | Form đã liên kết Google Sheet, có câu hỏi text nhiều dòng. Đã có 2 câu trả lời cũ chứa các giá trị ở cột dữ liệu nhập. Đã đổi title câu hỏi đó và lưu form | 1. Mở màn danh sách câu trả lời, ghi lại nguyên văn nội dung 2 câu trả lời cũ<br>2. Xoá sạch nội dung sheet đích<br>3. Trả lời form 1 lần để kích hoạt ghi lại toàn bộ<br>4. Chờ job xong, mở từng ô tương ứng trên sheet, xem đầy đủ nội dung và các cột kế bên | Giá trị 1: 3 dòng có xuống dòng + emoji 🎉 + dấu `"` và dấu phẩy. Giá trị 2: chuỗi khoảng 2.000 ký tự tiếng Nhật | Mỗi câu trả lời nằm trong **đúng 1 ô**: xuống dòng giữ trong ô (không tách thành dòng hay ô mới), emoji không mojibake, dấu phẩy và dấu `"` giữ nguyên, chuỗi dài không bị cắt. Các cột phía sau không lệch; nội dung khớp màn danh sách câu trả lời |  | Lấp `Q8` · dẫn từ `TC-FORM-405` · Evidence: ảnh ô sheet (mở rộng) + ảnh màn danh sách câu trả lời |
| TC-ENV003-01 | API | ENV-003 | Sync Google Sheet & job | Normal | manual | product | Kiểm chứng ca lỗi thật của ticket trên production sau khi deploy bản vá | Bản vá đã deploy production. Có quyền truy cập bot `195346` / form `235311` (form 「オンライン授業申込」), hoặc bản sao cùng cấu hình câu hỏi và có câu trả lời tạo cùng giai đoạn. Tài khoản Google của khách đang liên kết | 1. Trên màn danh sách câu trả lời, ghi lại các bản ghi khách báo trống cột 参加希望日時 (nhóm ở giữa) kèm giá trị đang hiển thị<br>2. Kiểm tra dữ liệu đã lưu của đúng các bản ghi đó **có kèm mã câu hỏi hay không**, ghi lại tỷ lệ có/không<br>3. Cho form đồng bộ lại từ đầu<br>4. Chờ job xong, mở spreadsheet và đối chiếu cột 参加希望日時 của đúng các dòng ở bước 1 | Ca lỗi thật: bot `195346`, form `235311`, cột K 参加希望日時, thời điểm khách báo 2026/09/06 | Với bản ghi **có** mã câu hỏi: ô có giá trị, bằng đúng giá trị trên màn danh sách câu trả lời → ticket được giải quyết. Với bản ghi **không có** mã câu hỏi: ô vẫn trống → **KHÔNG kết luận Đạt**, ghi nhận tỷ lệ và báo Dev/PO vì bản vá không lấp được ca của khách, cần phương án bổ sung |  | Lấp `G8` · `I1` · RULE-08 · Đánh giá spec: Đã hỏi leader · Evidence: ảnh sheet production + ảnh màn danh sách câu trả lời + kết quả kiểm tra dữ liệu bản ghi |

**GAP/RISK không viết TC mới** (đã có TC trong kho, hoặc đã xử lý ở §4):

- `G3` → chạy lại `TC-FORM-390` (sheet hỏng cấu trúc → dựng lại header) + `TC-FORM-394` (ô cột A trống → không đè dòng) **trên branch fix** — bản vá đổi cách dựng header nên 2 TC này là regression bắt buộc.
- `G6` / `Q3` → chạy lại `TC-FORM-398` (restart ứng dụng khi job đang xử lý), thực hiện **bắc qua mốc deploy** (dựng bản ghi chờ xử lý trước khi deploy, kiểm sau khi deploy) để phủ `REG-RUN-001`.
- `Q4` → chạy lại `TC-FORM-399` (Google trả 429 → backoff, retry tới khi đủ) + `TC-FORM-391` (chuỗi 1p→5p→10p→30p→60p) trên branch fix.
- `G9` / `I1` → không viết TC mới: expected của `NEW-10` và `NEW-11` phải được Dev/PO chốt rồi `testcase_update` chính 2 TC đó (§6 mục 2, 3).
- `G10` (orphan `NEW-31`) → giữ nguyên, không cần TC.
- `Q9` → không thiếu TC, chỉ cần đổi mã quan điểm (I4).

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | `spec-features/admin/form-answer/feature-spec.md` — BR-08 + Gap #11 + §9 | Viết lại cơ chế sync thật: 2 nhánh (**ghi lại toàn bộ** khi sheet rỗng/chưa có cột 回答ID ⇄ **ghi thêm dòng mới**), header 4 cột đời cũ ⇄ 6 cột đời mới, map đáp án theo `form_detail_id` rồi fallback so title, ghi theo lô 500 record, retry 1p→5p→10p→30p→60p và dừng sau 5 lần, lock theo `form_id`, số luồng mặc định 5. Bỏ câu "job `AddResultFormAnswerToGoogleSpreadSheet` bị comment out" | `dev_impact` Studio task #302 vs BR-08 hiện tại · kho-tcs `MT-22` | `Dev + PM` |
| 2 | Hành vi với **câu trả lời đời cũ không có `form_detail_id` + title đã đổi** | Chốt: chấp nhận ô trống vĩnh viễn, hay cần bổ sung `form_detail_id` cho data cũ / đường map khác. **Quyết định này xác định ticket #40604 có được giải quyết cho khách hay không** | `NEW-10` expected ghi "CHƯA CHỐT" · `NEW-6` dẫn chiếu sang `NEW-10` (§3 DUP-2) | `Dev + PO` |
| 3 | Hành vi khi **LINE user của câu trả lời cũ đã bị xoá** | Chốt bản ghi nào bị đánh dấu bỏ qua (bản ghi cũ bị lỗi hay bản ghi đang được đồng bộ), và dòng bị bỏ có được ghi nhận ở đâu không | `NEW-11` expected ghi "CẦN XÁC NHẬN" | `Dev + PO` |
| 4 | Hành vi **header ở nhánh ghi thêm dòng mới** | `NEW-14` kỳ vọng header **giữ title cũ**, không cập nhật khi đổi title → khách đổi title xong vẫn thấy tên cột cũ trên sheet cho tới khi sheet được ghi lại từ đầu. Cần chốt đây là hành vi mong muốn và ghi vào spec để CS trả lời khách | `NEW-14` vs mong đợi của khách trong ticket · kho `TC-FORM-380` | `PM + CS` |
| 5 | Chuẩn hoá **title câu hỏi có khoảng trắng đầu/cuối** | Chốt hệ thống **trim** hay **giữ nguyên** khi lưu title — ảnh hưởng trực tiếp đường fallback so title của bản vá với dữ liệu cũ | `NEW-12` (giữ nguyên) vs kho `TC-FORM-386` (tự trim) — `[MAJOR] SPEC-CONFLICT` ở §4 I8 | `Dev + Leader` |
