<!-- sync-tcs: url=<chưa có — nguồn TC là MCP LME TEST STUDIO, không phải Google Sheet> | sheet=<chưa có> | anchor=Main Function -->
<!-- source: MCP LME TEST STUDIO — task_id=287, ticket 40539, testcase_list (15 TC), fetch lúc 2026-09-11 (refresh kết quả exec cùng ngày qua task_get_report: 13 pass / 2 fail). Redmine KHÔNG có Link TCs human. -->

# 04 — TC List (fetch từ MCP LME TEST STUDIO — task #287)

> ⚠️ **ĐỌC TRƯỚC KHI REVIEW — bộ TC này KHÔNG do member người viết.**

## 0. Nguồn TC + cảnh báo bắt buộc

| Mục | Giá trị |
|---|---|
| Nguồn | **MCP LME TEST STUDIO** — `task_id=287`, ticket `40539`, `testcase_list` (15 TC), fetch `2026-09-11` |
| Lý do fallback | Redmine #40539 **KHÔNG có Section "Link TCs"** → không có Sheet TC human |
| Task Studio | `#287` · type `fix-bug` · feature `chat-1on1` · status `done-ai` · round `1` · branch `ai_fixbug_40539` · `archived=false` |
| Review state | `reviewState = leader` · `reviewed = false` · `openBugs = 0` |
| Tạo / chạy | `addedBy=ngannt` (2026-09-10 08:07) · `assignee=vinth` · `runBy=vinth` (2026-09-10 11:29) |

### 0.1. Tác giả TC — TC do **AI sinh**, không phải member người viết

| Tác giả | Số TC | Chi tiết |
|---|---|---|
| **AI** (`provenance.source = ai`, `createdJobId = 914`) | **14** | NEW-1 → NEW-14, tạo `2026-09-10 08:38` |
| **Human qua kênh MCP** (`provenance.source = human`, actor `vinth@mcp`) | **1** | NEW-15 (Studio #17331) — Sticker, tạo `2026-09-11 02:42` |

> `toolWritten` của Studio: `total=15 · tool=14 · mcp=1 · human=0 · rate=93.3%`. Field `toolWritten.mcp` chỉ là **kênh ghi**, tác giả thật đọc ở `provenance.source` (bảng trên).
> Toàn bộ 15 TC còn ở `status = draft`.

### 0.2. Kết quả thực thi

> 🔄 **Refresh 2026-09-11 (lúc `/review-tc`)** — kết quả đã đổi so với bản fetch đầu ngày: `aiResult` chuyển từ `null` → **`fail`**, exec từ `14 pass / 0 fail / 1 untested` → **`13 pass / 2 fail / 0 untested`**.

| Trạng thái | Số TC | TC No. |
|---|---|---|
| `pass` (Đạt) | **13** | tất cả trừ 2 TC dưới |
| `fail` (Không đạt) | **2** | `TC-TOOLAXIS001-01` (#17102 → Redmine **#40854**) · `TC-TOOLAXIS001-02` (#17331 → Redmine **#40855**) |
| `error` | **0** | — |
| Chưa chạy | **0** | — |

- **2 TC fail ĐÃ raise ticket** (Redmine #40854, #40855 — bug Studio #1020, #1019, cùng `severity = Medium`, `status = logged`, `source = human`, by `vinth`, run 1257, env `local`). **Nhưng cả 2 bug đều có `actual = null` và `evidence = []`** → Dev không có hiện tượng thật để điều tra.
- 🔴 **TC-TOOLAXIS001-01 có 2 kết quả trái ngược**: auto (pipeline, 2026-09-10) = `pass` kèm bằng chứng đo được `types=["text/uri-list","text/plain","text/html"]`; manual (vinth, 2026-09-11) = `fail` không bằng chứng. Chính note của TC đã dặn trước: *"tester phải ghi lại nguyên trạng kèm tên và phiên bản trình duyệt, KHÔNG tự kết luận là lỗi hay là đạt, để leader chốt kỳ vọng"*.
- **2 lần auto run** (`task_get_report`): run **1214** (2026-09-10 08:43→09:47) = 7 pass / **6 skip**; run **1234** (2026-09-10 10:35→11:29) = 6 pass. 6 TC `skip` ở run 1214 là do env local **không gửi được tin/media** (bot fixture không có `channel_access_token` thật; PHP local thiếu extension `imagick`). Run 1234 chạy lại 6 TC đó và `pass` — nhưng **`api.line.me` được thay bằng TEST DOUBLE cục bộ**, actual ghi rõ: *"KHÔNG chứng minh được hiển thị thực tế trên app LINE của người nhận"*.
- Manual: `pass 1` (TC-UI002-01) · `fail 2` · cả 3 kết quả manual đều **`evidence = []`**.

### 0.3. ⚠️ Môi trường đã chạy — RULE-08 KHÔNG được thỏa

| Env | total | automated | rate | **runs** |
|---|---|---|---|---|
| `local` | 15 | 13 | 86.7% | **2** |
| `dev` | 15 | 0 | 0% | **0** |
| `staging` | 15 | 0 | 0% | **0** |
| `prd` | 15 | 0 | 0% | **0** |

> 🔴 **Toàn bộ 14 kết quả `pass` đều lấy ở `env = local`.** Task này chạm **media** (kéo-thả ảnh/video/audio, gửi qua `EP-09 /basic/send-media-v2`, biên 5 tệp · 10MB) → theo **RULE-08**, media **không được kết luận từ local/staging**. Hiện chưa có run nào ở `staging` hay `prd`.
> Lưu ý: `env_scope` khai báo ở từng TC (`all` / `local,dev,staging`) là **phạm vi dự kiến**, KHÔNG phải env đã chạy — env đã chạy đọc ở `envAuto[].runs` (bảng trên).

### 0.4. ⚠️ Mã quan điểm Studio KHÔNG có trong `framework/checklist-lme.md`

| Mã quan điểm Studio | Có trong framework? | TC dùng |
|---|---|---|
| `TOOL-KNOW-002` | ❌ **KHÔNG** | TC-TOOLKNOW002-01 |
| `TOOL-AXIS-001` | ❌ **KHÔNG** | TC-TOOLAXIS001-01, TC-TOOLAXIS001-02 |
| `TOOL-NEGCTRL-001` | ❌ **KHÔNG** | TC-TOOLNEGCTRL001-01 |
| `UI-INPUT-001` | ✅ có | TC-UIINPUT001-01 |
| `OUT-TRUTH-001` | ✅ có | TC-OUTTRUTH001-01, -02 |
| `REG-SHARED-001` | ✅ có | TC-REGSHARED001-01, -02 |
| `UI-002` | ✅ có | TC-UI002-01 |
| `UI-003` | ✅ có | TC-UI003-01 |
| `MEDIA-001` | ✅ có | TC-MEDIA001-01 |
| `FUNC-004` | ✅ có | TC-FUNC004-01, -02 |
| `DEPLOY-ASSET-001` | ✅ có | TC-DEPLOYASSET001-01 |

> **3 mã** (`TOOL-*`) là mã nội bộ của Studio → `/review-tc` **không map được coverage** cho 4 TC dùng chúng. Leader cần quy chiếu tay sang mã tầng 1.

### 0.5. Phân bố

| Chiều | Phân bố |
|---|---|
| `case_type` | `Normal` **7** · `Abnormal` **6** · `Boundary` **2** |
| `tc_group` | `ui` **15** (không có TC nhóm API / Data) |
| `exec_mode` | `auto` **14** · `manual` **1** (TC-UI002-01 — đa trình duyệt) |
| `env_tag` | `env-safe` **8** · `local-only` **6** · `read-only` **1** |
| `env_scope` | `all` **9** · `local,dev,staging` **6** (loại `prd` vì gửi tin/media thật) |
| `screen` | Chat 1:1 (SCR-CHT-01) — kéo & thả trong khung hội thoại **13** · gửi media bằng nút 「メディア送信」 **1** · nạp asset JS sau phát hành **1** |
| `requirement_keys` | REQ-001 **7** · REQ-002 **5** · REQ-003 **6** · REQ-004 **1** · REQ-005 **1** · REQ-006 **1** |

### 0.6. Requirements Studio (`task_get_context`) — để đối chiếu coverage với file 03

| REQ | Risk | Tiêu đề |
|---|---|---|
| REQ-001 | **High** | Kéo-thả văn bản trong khung hội thoại Chat 1:1 không được bật màn hình upload |
| REQ-002 | **High** | Kéo tệp thật vào khung hội thoại vẫn mở màn upload và gửi được media |
| REQ-003 | Medium | Luồng xử lý tệp sau khi thả giữ nguyên kiểm tra định dạng, dung lượng và số lượng |
| REQ-004 | **High** | Nhận diện tệp phải đúng trên các trình duyệt quản trị viên đang dùng (Chrome / Edge / Firefox) |
| REQ-005 | Medium | Điều kiện chặn mới không làm đổi hành vi ngoài khung hội thoại |
| REQ-006 | Medium | Sau khi phát hành, trình duyệt nhận đúng bản JavaScript có điều kiện chặn mà không cần xóa cache |

> `test_viewpoint_selection` của task Studio = `null` (chưa có bảng duyệt quan điểm tầng 1).

---

## TC List

> **16 cột canonical.** TC chép **nguyên văn** từ Studio — **READ-ONLY**, không sửa title / precondition / steps / expected ở file này. Muốn sửa thì sửa trên Studio (`testcase_update`) rồi fetch lại.
> `TC No.` sinh theo quy ước repo (`TC-<mã quan điểm bỏ gạch>-<nn>`); `temp_id` + `id` Studio giữ ở cột **Ghi chú** để trace ngược.

| TC No. | Mã quan điểm liên kết | Loại case | Tiêu đề test case | Điều kiện tiền đề | Các bước thực hiện | Dữ liệu test/input | Kết quả mong đợi | Kết quả thực thi | Evidence thực tế | Môi trường test | Người thực hiện | Ngày thực hiện | Số ticket bug | Trạng thái đánh giá spec | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-TOOLKNOW002-01 | TOOL-KNOW-002 | Normal | Kéo & thả chữ bôi đen trong ô soạn tin — không hiện màn hình upload | Đã đăng nhập trang quản trị và đã chọn bot test. Đang mở màn Chat 1:1 tại /basic/chat-v3 bằng Chrome bản mới, đã chọn một bạn bè test có sẵn lịch sử hội thoại. Môi trường đang chạy bản có bản sửa của ticket #40539. Mở sẵn bảng điều khiển của trình duyệt để theo dõi lỗi JavaScript. | 1. Mở màn Chat 1:1 và chọn bạn bè test ở cột danh sách bên trái<br>2. Gõ vào ô soạn tin 「メッセージを入力してください」 chuỗi: kiem tra keo tha 40539<br>3. Bôi đen cụm chữ 'keo tha' trong ô soạn tin<br>4. Nhấn giữ chuột trái trên phần chữ đã bôi đen, kéo rê sang vị trí khác trong chính ô soạn tin, giữ khoảng 1 giây rồi nhả chuột<br>5. Quan sát toàn màn hình trong lúc kéo, tại lúc thả và sau khi thả<br>6. Đọc bảng điều khiển của trình duyệt để kiểm tra có lỗi JavaScript nào không | Nội dung ô soạn tin: 'kiem tra keo tha 40539'; phần bôi đen được kéo: 'keo tha' | Trong suốt thao tác kéo, tại lúc thả và sau khi thả đều KHÔNG xuất hiện lớp phủ tối toàn màn hình 「送信するデータをアップロード」. Thao tác kéo diễn ra bình thường theo mặc định của trình duyệt: con trỏ đổi thành biểu tượng kéo, cụm chữ 'keo tha' được di chuyển tới đúng vị trí thả và nội dung ô soạn tin đổi theo vị trí mới. Khung hội thoại vẫn thao tác được bình thường và bảng điều khiển không có lỗi JavaScript. | Đạt | | LOCAL | pipeline-manual | 2026-09-10 | | | Studio #17096 (NEW-1) · tc_group=ui · exec_mode=auto · env_tag=env-safe · env_scope=all · sort=10 · req: REQ-001 · spec_ids: SCR-CHT-01, TICKET-40539, source: public/js/chats/chat-v2.js:888-918 · last_exec: pass / source=ai / by=pipeline-manual / runId=1214<br>**Note Studio**: Đây là testcase tái hiện đúng kịch bản lỗi gốc của ticket. Trước khi sửa, bộ xử lý kéo-thả gắn cho khung hội thoại (.content-chat) bật cờ mở lớp phủ upload vô điều kiện và gọi preventDefault ngay từ sự kiện bắt đầu kéo, nên vừa hiện lớp phủ vừa chặn luôn thao tác kéo chữ. Khi tự động hóa: phải kéo bằng chuột thật trên trang thật (nhấn giữ - di chuyển - nhả) để trình duyệt tự sinh dữ liệu kéo-thả; chỉ khi môi trường không sinh được thao tác kéo thật thì mới phát sự kiện kéo-thả lên đúng phần tử với đối tượng dữ liệu kéo thật, và phải ghi rõ cách đã dùng vào bằng chứng. CẤM can thiệp vào hàm nhận diện tệp, cấm tự đặt cờ mở lớp phủ và cấm kết luận đạt bằng cách đọc mã nguồn. Nếu chạy đúng các bước mà không tái hiện được biểu hiện lỗi trên bản chưa sửa thì vẫn phải ghi lại nguyên trạng kèm môi trường, trình duyệt và dữ liệu đã dùng. |
| TC-UIINPUT001-01 | UI-INPUT-001 | Normal | Kéo chữ từ bong bóng tin nhắn thả vào ô soạn tin rồi gửi tin | Đã đăng nhập và chọn bot test. Mở màn Chat 1:1 với một bạn bè test đã có ít nhất một tin nhắn văn bản trong lịch sử hội thoại. Chuẩn bị sẵn một cửa sổ soạn thảo văn bản ngoài trình duyệt (ví dụ Notepad) có chứa chuỗi test. Không dùng bạn bè là khách hàng thật. | 1. Mở màn Chat 1:1 và chọn bạn bè test<br>2. Bôi đen một đoạn chữ trong bong bóng tin nhắn cũ ở khung hội thoại<br>3. Nhấn giữ chuột trên đoạn chữ đã bôi đen, kéo xuống ô soạn tin 「メッセージを入力してください」 rồi nhả chuột<br>4. Quan sát có lớp phủ upload hiện lên không và kiểm tra nội dung đã vào ô soạn tin chưa<br>5. Chuyển sang cửa sổ soạn thảo văn bản ngoài, bôi đen chuỗi test tiếng Nhật kèm emoji, kéo chuỗi đó thả tiếp vào ô soạn tin<br>6. Kéo thả tiếp chuỗi có chứa thẻ HTML vào ô soạn tin<br>7. Bấm nút gửi trên thanh công cụ soạn tin<br>8. Nhấn F5 tải lại màn Chat 1:1, mở lại đúng hội thoại đó và đối chiếu nội dung tin vừa gửi | Chuỗi kéo từ bong bóng tin nhắn: đoạn chữ bất kỳ có sẵn · Chuỗi kéo từ ngoài trình duyệt: 'テスト全角ドラッグ😀' · Chuỗi có thẻ HTML: `<b>drag</b>` | Không lần kéo-thả nào làm hiện lớp phủ 「送信するデータをアップロード」. Cả ba chuỗi đều được chèn vào ô soạn tin đúng nguyên văn, kể cả chữ Nhật full-width và emoji; chuỗi `<b>drag</b>` hiển thị nguyên văn dạng chữ, không bị hiểu thành định dạng đậm. Nút gửi hoạt động và sau khi bấm gửi, tin nhắn xuất hiện trong khung hội thoại đúng nội dung đã thả. Sau khi F5 và mở lại hội thoại, tin nhắn vẫn còn và nội dung khớp từng ký tự với nội dung đã gửi. | Đạt | | LOCAL | vinth | 2026-09-10 | | | Studio #17097 (NEW-2) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=local,dev,staging · sort=20 · req: REQ-001 · spec_ids: SCR-CHT-01, TICKET-40539, source: public/js/chats/chat-v2.js:913-918, spec: admin/chat-1on1/ui/ui-spec.md:386-393 · last_exec: pass / source=ai / by=vinth / runId=1234<br>**Note Studio**: Điểm cần bắt lỗi ở đây không chỉ là lớp phủ mà là chữ thả vào ô soạn tin có thật sự trở thành nội dung tin hay không — thao tác thả không phát sinh sự kiện bàn phím nên nếu khung soạn tin không cập nhật thì tin gửi đi sẽ rỗng hoặc thiếu chữ. Bước kiểm sau F5 là để không kết luận đạt chỉ bằng những gì đang hiện trên màn. Loại prd khỏi phạm vi chạy vì testcase gửi tin thật tới bạn bè LINE. |
| TC-OUTTRUTH001-01 | OUT-TRUTH-001 | Normal | Kéo tệp ảnh PNG vào khung hội thoại — mở màn upload và gửi ảnh thành công | Đã đăng nhập và chọn bot test, mở màn Chat 1:1 với một bạn bè test (không phải khách hàng thật). Máy chạy test có sẵn tệp ảnh PNG hợp lệ dưới 10MB. Nếu có sẵn thiết bị đã kết bạn với bot test thì mở LINE trên thiết bị đó để đối chiếu. | 1. Mở màn Chat 1:1 và chọn bạn bè test<br>2. Kéo tệp ảnh PNG từ trình quản lý tệp của máy vào giữa khung hội thoại và giữ chuột ở đó<br>3. Quan sát màn hình ngay khi con trỏ vào vùng khung hội thoại<br>4. Nhả chuột để thả tệp lên lớp phủ đang hiện<br>5. Quan sát khung xem trước phía trên ô soạn tin<br>6. Bấm nút gửi<br>7. Đối chiếu ảnh hiển thị trong khung hội thoại với ảnh gốc đã kéo<br>8. Nhấn F5 và mở lại hội thoại để xác nhận ảnh vẫn nằm trong lịch sử<br>9. Nếu có thiết bị mở sẵn LINE: kiểm tra bạn bè test nhận được đúng tấm ảnh đó | Tệp ảnh PNG hợp lệ, dung lượng khoảng 1-2MB, có nội dung dễ nhận biết bằng mắt | Ngay khi kéo tệp vào vùng khung hội thoại, lớp phủ tối 「送信するデータをアップロード」 kèm dòng 「最大5つまで同時にアップロードできます」 và 「対応データ：画像・動画・音声」 hiện ra. Sau khi thả, lớp phủ đóng lại và ảnh vừa thả xuất hiện trong khung xem trước phía trên ô soạn tin. Bấm gửi thì ảnh được gửi đi và hiển thị trong khung hội thoại; sau khi F5 ảnh vẫn còn trong lịch sử. Ảnh nhận được trên LINE giống ảnh gốc đã kéo (khi có thiết bị đối chiếu). | Đạt | | LOCAL | vinth | 2026-09-10 | | | Studio #17098 (NEW-3) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=local,dev,staging · sort=30 · req: REQ-002, REQ-003 · spec_ids: SCR-CHT-01, EP-09, TICKET-40539, source: public/js/chats/chat-v2.js:905-922, spec: admin/chat-1on1/web/api-spec.md:281 · last_exec: pass / source=ai / by=vinth / runId=1234<br>**Note Studio**: Trình tự thật của luồng này: lớp phủ upload bật ngay từ sự kiện kéo vào vùng khung hội thoại, và vì lớp phủ nằm đè toàn màn hình nên cú thả cuối cùng rơi vào chính lớp phủ chứ không phải khung hội thoại — người chạy đừng tưởng là thiếu bước. Tệp chỉ được đính kèm vào khung xem trước, tin chỉ thật sự đi khi bấm nút gửi (request POST /basic/send-media-v2, tức EP-09, với dữ liệu dạng media[i][file] và media[i][type]). Khi tự động hóa: bắt buộc thao tác trên trình duyệt thật với tệp thật; nếu có bắt request để kiểm thì chỉ được kiểm những trường mà chính giao diện gửi lên, CẤM tự thêm hay sửa trường trong dữ liệu gửi. Loại prd khỏi phạm vi chạy vì testcase gửi media thật. |
| TC-REGSHARED001-01 | REG-SHARED-001 | Normal | Kéo tệp video MP4 vào khung hội thoại — đính kèm và gửi thành công | Đã đăng nhập và chọn bot test, mở màn Chat 1:1 với bạn bè test. Máy chạy test có sẵn tệp video MP4 ngắn (vài giây), dung lượng dưới 200MB. | 1. Mở màn Chat 1:1 và chọn bạn bè test<br>2. Kéo tệp video MP4 từ máy vào giữa khung hội thoại<br>3. Quan sát lớp phủ upload có hiện ra không<br>4. Nhả chuột để thả tệp<br>5. Kiểm tra khung xem trước phía trên ô soạn tin<br>6. Bấm nút gửi và quan sát khung hội thoại | Tệp video MP4 dài khoảng 3-5 giây, dung lượng vài MB | Lớp phủ 「送信するデータをアップロード」 hiện ra giống như khi kéo ảnh, chứng tỏ việc phân biệt kéo tệp với kéo văn bản không phụ thuộc vào loại tệp. Sau khi thả, video được đính kèm vào khung xem trước, không có thông báo lỗi định dạng. Bấm gửi thì video xuất hiện trong khung hội thoại và phát được. | Đạt | | LOCAL | vinth | 2026-09-10 | | | Studio #17099 (NEW-4) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=local,dev,staging · sort=40 · req: REQ-002, REQ-003 · spec_ids: SCR-CHT-01, EP-09, TICKET-40539, source: public/js/chats/chat-v2.js:3690-3701 · last_exec: pass / source=ai / by=vinth / runId=1234<br>**Note Studio**: Testcase này giữ nguyên đường thao tác của người dùng, mục đích là rà nhánh còn lại của hàm nhận tệp dùng chung (ánh xạ loại media theo đuôi tệp) sau khi thêm điều kiện chặn ở phía trước. Biến thể tệp âm thanh .m4a chạy được cùng bộ bước, tester chạy thêm khi có tệp mẫu; không tách thành testcase riêng vì cùng một nguyên nhân gốc. Loại prd khỏi phạm vi chạy vì testcase gửi media thật. |
| TC-UI002-01 | UI-002 | Normal | Kéo & thả chữ và tệp trên Firefox và Edge — nhận diện đúng trên từng trình duyệt | Máy test cài sẵn Firefox bản mới và Microsoft Edge bản mới, đều đăng nhập được trang quản trị với bot test. Có sẵn tệp ảnh PNG hợp lệ trên máy. Bạn bè dùng để thử là bạn bè test. | 1. Mở màn Chat 1:1 trên Firefox và chọn bạn bè test<br>2. Bôi đen chữ trong ô soạn tin rồi kéo-thả sang vị trí khác trong ô soạn tin<br>3. Ghi nhận lớp phủ upload có hiện không và chữ có kéo được không<br>4. Vẫn trên Firefox, kéo tệp ảnh PNG thật từ trình quản lý tệp của hệ điều hành vào khung hội thoại rồi thả<br>5. Ghi nhận lớp phủ có hiện không, ảnh có vào khung xem trước không; bấm gửi và ghi nhận kết quả<br>6. Lặp lại toàn bộ các bước trên với Microsoft Edge | Chuỗi kéo trong ô soạn tin: 'kiem tra trinh duyet' · Tệp kéo: ảnh PNG hợp lệ dưới 10MB | Trên cả Firefox và Edge: kéo chữ KHÔNG làm hiện lớp phủ 「送信するデータをアップロード」 và chữ kéo-thả được bình thường; kéo tệp ảnh thật thì lớp phủ VẪN hiện, thả xong ảnh vào khung xem trước và gửi được. Không trình duyệt nào cho kết quả khác hai trình duyệt còn lại. | Đạt | | LOCAL | vinth | 2026-09-10 | | | Studio #17100 (NEW-5) · tc_group=ui · **exec_mode=manual** · env_tag=local-only · env_scope=local,dev,staging · sort=50 · req: REQ-004, REQ-001, REQ-002 · spec_ids: SCR-CHT-01, TICKET-40539, source: public/js/chats/chat-v2.js:893-901 · last_exec: pass / **source=manual** / by=vinth / at 2026-09-10 11:33:49 (không có runId)<br>**Note Studio**: Chạy tay vì đây chính là trục đang có giả định rủi ro: việc nhận diện tệp dựa vào danh sách kiểu dữ liệu do trình duyệt cung cấp, dòng Chromium báo 'Files' còn Firefox báo 'application/x-moz-file'; ở giai đoạn kéo qua vùng thì trình duyệt chưa cho đọc danh sách tệp nên nhánh dự phòng không cứu được. Nếu một trình duyệt không phát ra hai chuỗi đó thì luồng gửi tệp bằng kéo-thả mất hoàn toàn. Bắt buộc kéo tệp THẬT từ hệ điều hành; sự kiện kéo-thả dựng bằng script KHÔNG được tính là bằng chứng đạt cho riêng testcase này. Safari có thể chạy thêm nếu có máy Mac nhưng không tính vào phán quyết đạt/không đạt vì màn quản trị này dùng trên web PC và ticket không có bằng chứng người dùng Safari. |
| TC-OUTTRUTH001-02 | OUT-TRUTH-001 | Abnormal | Kéo liên kết vào khung hội thoại — không hiện màn hình upload | Đã đăng nhập và chọn bot test, mở màn Chat 1:1 với bạn bè test. Trong lịch sử hội thoại có sẵn ít nhất một tin nhắn chứa đường dẫn hiển thị thành liên kết bấm được; nếu chưa có thì dùng đường dẫn trên thanh địa chỉ của trình duyệt. | 1. Mở màn Chat 1:1 và chọn bạn bè test<br>2. Nhấn giữ chuột trên liên kết trong bong bóng tin nhắn, kéo vào giữa khung hội thoại rồi thả<br>3. Quan sát màn hình trong lúc kéo và sau khi thả<br>4. Lặp lại bằng cách kéo biểu tượng đường dẫn trên thanh địa chỉ của trình duyệt thả vào khung hội thoại<br>5. Đọc bảng điều khiển của trình duyệt | Liên kết trong tin nhắn có sẵn · Đường dẫn trên thanh địa chỉ: https://example.com/test-40539 | Cả hai lần kéo liên kết đều KHÔNG làm hiện lớp phủ 「送信するデータをアップロード」. Trang giữ hành vi mặc định của trình duyệt, khung hội thoại không bị khóa thao tác, không có lỗi JavaScript trên bảng điều khiển và không có tệp nào bị đính kèm vào khung xem trước. | Đạt | | LOCAL | pipeline-manual | 2026-09-10 | | | Studio #17101 (NEW-6) · tc_group=ui · exec_mode=auto · env_tag=env-safe · env_scope=all · sort=60 · req: REQ-001 · spec_ids: SCR-CHT-01, TICKET-40539, source: public/js/chats/chat-v2.js:893-901 · last_exec: pass / source=ai / by=pipeline-manual / runId=1214<br>**Note Studio**: Liên kết được kéo mang kiểu dữ liệu dạng đường dẫn/văn bản chứ không phải tệp, nên điều kiện chặn mới phải cho qua. Đây là một giá trị riêng của trục 'thứ được kéo', tách khỏi testcase kéo chữ vì trình duyệt xử lý hai loại này bằng kiểu dữ liệu khác nhau. Testcase không phát sinh tin nhắn nên chạy được ở mọi môi trường. |
| TC-TOOLAXIS001-01 | TOOL-AXIS-001 | Abnormal | Kéo ảnh có sẵn trong lịch sử hội thoại — không hiện màn hình upload | Đã đăng nhập và chọn bot test, mở màn Chat 1:1 với bạn bè test đã có sẵn ít nhất một tin nhắn dạng ảnh trong lịch sử hội thoại. | 1. Mở màn Chat 1:1 và chọn bạn bè test có tin nhắn ảnh<br>2. Nhấn giữ chuột trên tấm ảnh đang hiển thị trong khung hội thoại, kéo sang vị trí khác trong khung hội thoại rồi thả<br>3. Quan sát màn hình trong lúc kéo và sau khi thả<br>4. Kiểm tra khung xem trước phía trên ô soạn tin | Ảnh có sẵn trong lịch sử hội thoại (không phải tệp trên máy) | Không hiện lớp phủ 「送信するデータをアップロード」 vì thứ được kéo là phần tử có sẵn trên trang chứ không phải tệp từ máy. Không có tệp nào bị đính kèm lại vào khung xem trước, thao tác kéo không bị chặn và không sinh lỗi JavaScript. | **Không đạt** | ❌ rỗng | LOCAL | vinth | 2026-09-11 | **#40854** | | ⚠️ **KẾT QUẢ MÂU THUẪN**: auto run 1214 (2026-09-10, pipeline) = **pass** với bằng chứng `types=["text/uri-list","text/plain","text/html"]` → không bung lớp phủ; manual run 1257 (2026-09-11, vinth) = **fail**, `actual = null`, `evidence = []` → đã raise Redmine #40854 (bug Studio #1020, severity Medium, status logged). Studio #17102 (NEW-7) · tc_group=ui · exec_mode=auto · env_tag=env-safe · env_scope=all · sort=70 · req: REQ-001 · spec_ids: SCR-CHT-01, TICKET-40539, source: public/js/chats/chat-v2.js:893-901 · last_exec: pass / source=ai / by=pipeline-manual / runId=1214<br>🔺 **Note Studio — ĐIỂM CẦN LEADER XÁC NHẬN**: kết quả mong đợi ở trên suy ra từ hợp đồng của điều kiện chặn mới (chỉ nhận là tệp khi danh sách kiểu dữ liệu chứa 'Files' hoặc 'application/x-moz-file'), còn thao tác kéo ảnh trong trang thì các trình duyệt phổ biến báo kiểu văn bản/HTML/đường dẫn. Ticket không có quan sát thực tế cho kịch bản này. Nếu khi chạy thật mà trình duyệt vẫn báo kiểu tệp và lớp phủ mở ra, tester phải ghi lại nguyên trạng kèm tên và phiên bản trình duyệt, KHÔNG tự kết luận là lỗi hay là đạt, để leader chốt kỳ vọng. |
| TC-TOOLAXIS001-02 | TOOL-AXIS-001 | Abnormal | Kéo Sticker đã hiển thị trong lịch sử chat — không hiện màn hình upload | Đã đăng nhập và chọn bot test, mở màn Chat 1:1 với một bạn bè test có sẵn ít nhất 1 tin nhắn dạng Sticker trong lịch sử hội thoại. Sticker phải đang hiển thị trực tiếp trong khung chat và không phải tệp được kéo từ máy. | 1. Mở màn Chat 1:1 và chọn bạn bè test có tin nhắn Sticker trong lịch sử<br>2. Nhấn giữ chuột trên Sticker đang hiển thị trong khung hội thoại<br>3. Kéo Sticker sang một vị trí khác trong vùng khung hội thoại rồi thả<br>4. Quan sát màn hình trong lúc kéo, tại lúc thả và sau khi thả<br>5. Kiểm tra khung xem trước phía trên ô soạn tin và trạng thái thao tác của màn chat | Sticker đã có sẵn trong lịch sử Chat 1:1 của bạn bè test | Trong suốt thao tác kéo và sau khi thả, KHÔNG xuất hiện lớp phủ 「送信するデータをアップロード」 vì Sticker đang hiển thị trong lịch sử chat không phải tệp local được kéo từ máy. Không có Sticker nào bị đính kèm lại vào khung xem trước dưới dạng file/media mới. Màn Chat 1:1 vẫn thao tác bình thường, không bị khóa hoặc kẹt sau thao tác kéo-thả. | **Không đạt** | ❌ rỗng | LOCAL | vinth | 2026-09-11 | **#40855** | | ⚠️ Manual run 1257 (2026-09-11, vinth) = **fail**, `actual = null`, `evidence = []` → đã raise Redmine #40855 (bug Studio #1019, severity Medium, status logged). Chưa từng có auto run cho TC này. Studio #17331 (NEW-15) · client_ref=task287-sticker-drag-history · tc_group=ui · exec_mode=auto · env_tag=env-safe · env_scope=all · sort=80 · req: REQ-001 · spec_ids: SCR-CHT-01, TICKET-40539, source: public/js/chats/chat-v2.js:893-901 · **author=vinth@mcp, provenance.source=human** (TC duy nhất do người viết), tạo 2026-09-11 02:42 · **last_exec = null → CHƯA CHẠY LẦN NÀO**<br>**Note Studio**: Bổ sung coverage cho trục loại dữ liệu đang hiển thị trong message history. Sticker phải được phân biệt với file ảnh local: chỉ drag event mang loại file thật mới được mở overlay upload. Nếu browser cụ thể serialize Sticker DOM thành kiểu dữ liệu khác thường, cần ghi lại DataTransfer types và hiện tượng thực tế để đối chiếu; không tự biến Sticker thành file local trong automation. |
| TC-MEDIA001-01 | MEDIA-001 | Abnormal | Kéo tệp sai định dạng (PDF, TXT) — màn upload vẫn mở nhưng báo sai định dạng | Đã đăng nhập và chọn bot test, mở màn Chat 1:1 với bạn bè test. Máy chạy test có sẵn một tệp PDF và một tệp TXT. | 1. Mở màn Chat 1:1 và chọn bạn bè test<br>2. Kéo tệp PDF từ máy vào giữa khung hội thoại và giữ chuột ở đó<br>3. Ghi nhận lớp phủ upload có hiện ra không<br>4. Nhả chuột để thả tệp PDF lên lớp phủ<br>5. Đọc thông báo bật ra và kiểm tra khung xem trước phía trên ô soạn tin<br>6. Lặp lại các bước trên với tệp TXT<br>7. Sau khi đóng thông báo, gõ một tin nhắn text và bấm gửi để xác nhận khung chat vẫn dùng được | Tệp PDF bất kỳ dưới 10MB · Tệp TXT bất kỳ | Với cả hai tệp: lớp phủ 「送信するデータをアップロード」 VẪN hiện ra khi kéo vào khung hội thoại (điều kiện chặn mới chỉ phân biệt tệp với văn bản, không phân biệt loại tệp). Sau khi thả, hệ thống bật thông báo 「ファイルの形式が正しくありません。」, lớp phủ đóng lại và KHÔNG có tệp nào được đính kèm vào khung xem trước. Sau khi đóng thông báo, khung chat vẫn gõ và gửi được tin nhắn text bình thường. | Đạt | | LOCAL | vinth | 2026-09-10 | | | Studio #17103 (NEW-8) · tc_group=ui · exec_mode=auto · env_tag=env-safe · env_scope=all · sort=90 · req: REQ-002, REQ-003 · spec_ids: SCR-CHT-01, TICKET-40539, source: public/js/chats/chat-v2.js:3762-3790, source: resources/views/basic/chat/modal/upload_file.blade.php:17 · last_exec: pass / source=ai / by=vinth / runId=1234<br>🔺 **Note Studio — CẢNH BÁO NGUỒN MÂU THUẪN**: tài liệu bước điều tra ghi 'kéo-thả tệp PDF vào vùng chat thì gửi thành công', nhưng mã nguồn chỉ chấp nhận .png/.jpg/.jpeg/.mp4/.m4a ở luồng kéo-thả và chính lớp phủ cũng ghi 「対応データ：画像・動画・音声」. PDF có đường gửi riêng bằng nút 「PDF送信」 trên thanh công cụ (đặc tả giao diện mục thanh công cụ gửi tin nhắn). Kết quả mong đợi ở trên viết theo mã nguồn thực tế; nếu leader muốn kéo-thả PDF gửi được thì đó là yêu cầu mới, phải mở ticket riêng chứ không tính là lỗi của ticket này. Bước gửi tin text ở cuối chỉ để chứng minh màn hình không bị kẹt, tester dùng bạn bè test. |
| TC-UI003-01 | UI-003 | Abnormal | Kéo tệp vào khung hội thoại rồi rời đi không thả — đóng màn upload và chat dùng lại bình thường | Đã đăng nhập và chọn bot test, mở màn Chat 1:1 với bạn bè test. Máy chạy test có sẵn tệp ảnh PNG hợp lệ. | 1. Mở màn Chat 1:1 và chọn bạn bè test<br>2. Kéo tệp ảnh PNG vào giữa khung hội thoại cho tới khi lớp phủ upload hiện ra<br>3. Không nhả chuột trong vùng chat, kéo con trỏ ra ngoài cửa sổ trình duyệt rồi mới nhả chuột<br>4. Quan sát trạng thái lớp phủ upload<br>5. Bấm nút ✕ ở góc trên bên phải của lớp phủ<br>6. Gõ một tin nhắn text vào ô soạn tin và bấm gửi<br>7. Kéo lại tệp ảnh PNG vào khung hội thoại và thả để xác nhận luồng upload vẫn dùng được sau đó | Tệp ảnh PNG hợp lệ · Tin nhắn text: 'kiem tra sau khi dong man upload' | Lớp phủ upload không tự đóng khi kéo ra ngoài mà không thả — đây là hành vi sẵn có, không tính là lỗi của bản sửa. Bấm nút ✕ thì lớp phủ đóng hẳn, khung hội thoại hiện lại đầy đủ, không bị trắng màn và không kẹt biểu tượng đang tải. Sau đó gõ và gửi được tin nhắn text bình thường, và kéo-thả tệp lần nữa vẫn mở được lớp phủ và đính kèm được ảnh. | Đạt | | LOCAL | vinth | 2026-09-10 | | | Studio #17104 (NEW-9) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=local,dev,staging · sort=100 · req: REQ-002 · spec_ids: SCR-CHT-01, TICKET-40539, source: public/js/chats/chat-v2.js:913-922, source: resources/views/basic/chat/modal/upload_file.blade.php:6-11 · last_exec: pass / source=ai / by=vinth / runId=1234<br>**Note Studio**: Bộ xử lý đăng ký cả sự kiện rời vùng nhưng không có nhánh nào tắt cờ mở lớp phủ, nên lớp phủ ở lại là hành vi có sẵn từ trước bản sửa; testcase này chốt mốc rằng bản sửa không làm tệ đi và người dùng luôn có đường thoát. Nếu leader muốn lớp phủ tự đóng khi kéo ra ngoài thì mở ticket cải tiến riêng. Loại prd khỏi phạm vi chạy vì có bước gửi tin thật. |
| TC-TOOLNEGCTRL001-01 | TOOL-NEGCTRL-001 | Abnormal | Kéo chữ và tệp lên cột danh sách bạn bè và cột thông tin — không hiện màn hình upload | Đã đăng nhập và chọn bot test, mở màn Chat 1:1 với bạn bè test; cột danh sách bạn bè bên trái và cột thông tin bạn bè bên phải đều đang hiển thị. Máy chạy test có sẵn tệp ảnh PNG. | 1. Mở màn Chat 1:1 và chọn bạn bè test<br>2. Bôi đen tên một bạn bè ở cột trái rồi kéo-thả sang chỗ khác trong chính cột trái<br>3. Kéo tệp ảnh PNG từ máy thả lên cột danh sách bạn bè bên trái<br>4. Kéo tệp ảnh PNG thả lên cột thông tin bạn bè bên phải<br>5. Sau mỗi lần, quan sát lớp phủ upload, trạng thái danh sách bạn bè và bảng điều khiển của trình duyệt | Tên bạn bè hiển thị ở cột trái · Tệp ảnh PNG hợp lệ | Không thao tác nào trong ba thao tác trên làm hiện lớp phủ 「送信するデータをアップロード」. Danh sách bạn bè giữ nguyên thứ tự và bạn bè đang chọn không bị đổi; cột thông tin bạn bè không đổi nội dung; không có tệp nào được đính kèm; bảng điều khiển không có lỗi JavaScript. | Đạt | | LOCAL | pipeline-manual | 2026-09-10 | | | Studio #17105 (NEW-10) · tc_group=ui · exec_mode=auto · env_tag=env-safe · env_scope=all · sort=110 · req: REQ-005 · spec_ids: SCR-CHT-01, TICKET-40539, source: resources/views/basic/chat/content_chat.blade.php:4 · last_exec: pass / source=ai / by=pipeline-manual / runId=1214<br>**Note Studio**: Đây là đối chứng âm cho phạm vi của điều kiện chặn mới: nó chỉ được thu hẹp hành vi bên trong khung hội thoại, tuyệt đối không nới rộng vùng nhận kéo-thả sang các cột khác. Đã rà toàn bộ view: chuỗi lớp của khung hội thoại chỉ tồn tại ở màn Chat 1:1 nên không cần trải testcase sang màn khác. |
| TC-FUNC004-01 | FUNC-004 | Boundary | Kéo 5 tệp và 6 tệp cùng lúc — biên số lượng tệp đính kèm | Đã đăng nhập và chọn bot test, mở màn Chat 1:1 với bạn bè test. Máy chạy test có sẵn 6 tệp ảnh PNG hợp lệ, mỗi tệp dưới 10MB, đặt tên phân biệt được. | 1. Mở màn Chat 1:1 và chọn bạn bè test<br>2. Chọn cùng lúc 5 tệp ảnh trong trình quản lý tệp, kéo cả nhóm vào khung hội thoại và thả lên lớp phủ<br>3. Đếm số ảnh trong khung xem trước phía trên ô soạn tin<br>4. Tải lại màn hình để xóa khung xem trước, rồi chọn cùng lúc 6 tệp ảnh và kéo-thả theo cách trên<br>5. Đọc thông báo bật ra và đếm lại số ảnh trong khung xem trước | Lần 1: 5 tệp PNG · Lần 2: 6 tệp PNG (đều hợp lệ, dưới 10MB) | Với 5 tệp: lớp phủ hiện ra, thả xong đủ 5 ảnh vào khung xem trước, không có thông báo lỗi. Với 6 tệp: hệ thống bật thông báo 「同時にアップロードできるのは最大5つだけです。」, lớp phủ đóng lại và KHÔNG có ảnh nào được đính kèm thêm, khớp với dòng hướng dẫn 「最大5つまで同時にアップロードできます」 in trên lớp phủ. | Đạt | | LOCAL | pipeline-manual | 2026-09-10 | | | Studio #17106 (NEW-11) · tc_group=ui · exec_mode=auto · env_tag=env-safe · env_scope=all · sort=120 · req: REQ-003 · spec_ids: SCR-CHT-01, TICKET-40539, source: public/js/chats/chat-v2.js:3682-3689, source: resources/views/basic/chat/modal/upload_file.blade.php:16 · last_exec: pass / source=ai / by=pipeline-manual / runId=1214<br>**Note Studio**: Trục 'chữ hay tệp' của bản sửa không có khái niệm biên số học, nên biên được lấy ở luồng upload nằm ngay sau điều kiện chặn — đây cũng là nhánh dùng chung với nút 「メディア送信」. Testcase chỉ đính kèm, không bấm gửi, nên không phát sinh tin nhắn thật và chạy được ở mọi môi trường; tester nhớ tải lại màn hình để dọn khung xem trước sau khi chạy. |
| TC-FUNC004-02 | FUNC-004 | Boundary | Kéo ảnh sát và vượt 10MB — biên dung lượng tệp đính kèm | Đã đăng nhập và chọn bot test, mở màn Chat 1:1 với bạn bè test. Máy chạy test có sẵn hai tệp ảnh PNG: một tệp khoảng 9,5MB và một tệp khoảng 11MB. | 1. Mở màn Chat 1:1 và chọn bạn bè test<br>2. Kéo tệp ảnh khoảng 9,5MB vào khung hội thoại và thả lên lớp phủ<br>3. Kiểm tra khung xem trước phía trên ô soạn tin<br>4. Tải lại màn hình để xóa khung xem trước<br>5. Kéo tệp ảnh khoảng 11MB vào khung hội thoại và thả lên lớp phủ<br>6. Đọc thông báo bật ra và kiểm tra lại khung xem trước | Ảnh PNG ~9,5MB (dưới ngưỡng) · Ảnh PNG ~11MB (vượt ngưỡng 10MB) | Ảnh 9,5MB: lớp phủ hiện ra, thả xong ảnh vào khung xem trước bình thường, không có thông báo lỗi. Ảnh 11MB: lớp phủ vẫn hiện khi kéo vào, nhưng sau khi thả hệ thống bật thông báo 「10MBまでのファイルをアップロードできます」, lớp phủ đóng lại và không có ảnh nào được đính kèm. | Đạt | | LOCAL | pipeline-manual | 2026-09-10 | | | Studio #17107 (NEW-12) · tc_group=ui · exec_mode=auto · env_tag=env-safe · env_scope=all · sort=130 · req: REQ-003 · spec_ids: SCR-CHT-01, TICKET-40539, source: public/js/chats/chat-v2.js:3742-3760, catalog: MED-L01 · last_exec: pass / source=ai / by=pipeline-manual / runId=1214<br>**Note Studio**: Ngưỡng 10MB cho ảnh ở màn Chat 1:1 khớp bảng tra media của dự án. Chỉ đính kèm chứ không bấm gửi nên không phát sinh tin nhắn thật. Nếu cần kiểm thêm biên của video (200MB) và âm thanh (25MB) thì chạy cùng bộ bước với tệp tương ứng; không tách testcase riêng vì cùng một nhánh kiểm dung lượng dùng chung. |
| TC-REGSHARED001-02 | REG-SHARED-001 | Normal | Gửi ảnh bằng nút 「メディア送信」 — luồng chọn tệp không kéo-thả vẫn nguyên | Đã đăng nhập và chọn bot test, mở màn Chat 1:1 với bạn bè test. Máy chạy test có sẵn tệp ảnh PNG hợp lệ dưới 10MB. | 1. Mở màn Chat 1:1 và chọn bạn bè test<br>2. Bấm biểu tượng 「メディア送信」 trên thanh công cụ phía dưới khung soạn tin<br>3. Trong hộp thoại chọn tệp của hệ điều hành, chọn tệp ảnh PNG rồi xác nhận<br>4. Kiểm tra khung xem trước phía trên ô soạn tin<br>5. Bấm nút gửi và quan sát khung hội thoại | Tệp ảnh PNG hợp lệ, dung lượng khoảng 1-2MB | Bấm 「メディア送信」 mở thẳng hộp thoại chọn tệp của hệ điều hành, KHÔNG hiện lớp phủ 「送信するデータをアップロード」. Chọn ảnh xong thì ảnh vào khung xem trước; bấm gửi thì ảnh được gửi và hiển thị trong khung hội thoại như trước khi sửa. | Đạt | | LOCAL | vinth | 2026-09-10 | | | Studio #17108 (NEW-13) · tc_group=ui · exec_mode=auto · env_tag=local-only · env_scope=local,dev,staging · sort=140 · req: REQ-003 · spec_ids: SCR-CHT-01, EP-09, TICKET-40539, source: public/js/chats/chat-v2.js:3676-3682, 3811-3814, spec: admin/chat-1on1/ui/ui-spec.md:146 · last_exec: pass / source=ai / by=vinth / runId=1234<br>**Note Studio**: Testcase hồi quy cho nhánh dùng chung: nút này và luồng kéo-thả cùng gọi một hàm nhận tệp, còn bản sửa chỉ thêm điều kiện ở phía kéo-thả nên nhánh chọn tệp phải nguyên vẹn. Đây cũng là đường thay thế mà quản trị viên dùng khi kéo-thả gặp sự cố, nên phải chắc chắn còn chạy. Loại prd khỏi phạm vi chạy vì testcase gửi media thật. |
| TC-DEPLOYASSET001-01 | DEPLOY-ASSET-001 | Normal | Sau khi phát hành, F5 thường nhận đúng bản JavaScript có phân biệt kéo tệp | Trình duyệt đã mở màn Chat 1:1 TRƯỚC khi phát hành nên đang giữ bản JavaScript cũ trong bộ nhớ đệm. Bản sửa đã được phát hành lên môi trường cần kiểm. Mở sẵn tab Network của công cụ dành cho nhà phát triển và KHÔNG bật tùy chọn tắt bộ nhớ đệm. | 1. Trên đúng trình duyệt đang giữ bản cũ, mở lại màn Chat 1:1 và nhấn F5 THƯỜNG (không dùng Ctrl+F5, không xóa bộ nhớ đệm)<br>2. Trong tab Network, tìm request tới tệp JavaScript của màn chat và đọc tham số phiên bản trên đường dẫn cùng mã trạng thái trả về<br>3. Mở nội dung tệp được trả về và tìm phần xử lý phân biệt sự kiện kéo tệp với kéo văn bản<br>4. Bôi đen chữ trong ô soạn tin rồi kéo-thả sang vị trí khác trong ô soạn tin<br>5. Quan sát lớp phủ upload và bảng điều khiển của trình duyệt | Không nhập dữ liệu nghiệp vụ; chuỗi dùng để kéo trong ô soạn tin: 'kiem tra sau deploy' | Request tới tệp JavaScript của màn chat trả mã 200 với tham số phiên bản hiện hành của bản phát hành, khác đường dẫn đã lưu trong bộ nhớ đệm cũ, không có request nào trả 404. Nội dung tệp trả về có chứa phần xử lý chỉ mở màn upload khi thứ được kéo là tệp. Chỉ F5 thường là kéo chữ đã không còn bật lớp phủ 「送信するデータをアップロード」, không cần hard-reload hay xóa bộ nhớ đệm, và không có lỗi JavaScript. | Đạt | | LOCAL | pipeline-manual | 2026-09-10 | | | Studio #17109 (NEW-14) · tc_group=ui · exec_mode=auto · env_tag=read-only · env_scope=all · sort=150 · req: REQ-006, REQ-001 · spec_ids: SCR-CHT-01, TICKET-40539, source: resources/views/basic/chat/index.blade.php:185, source: public/js/chats/chat-v2.js:886-918 · last_exec: pass / source=ai / by=pipeline-manual / runId=1214<br>**Note Studio**: Không ghi cứng số phiên bản vào kết quả mong đợi để testcase không lạc hậu ở bản phát hành sau; điểm kiểm là tham số phiên bản phải đổi so với bản đang nằm trong bộ nhớ đệm và nội dung tải về phải là bản có điều kiện chặn. Thẻ nạp tệp này đã dùng phiên bản chung của cấu hình chứ không ghi cứng như sự cố asset ở ticket #33117, nên chỉ cần một testcase thay vì cả cụm rà version. Testcase chỉ quan sát, không phát sinh tin nhắn nên chạy được ở mọi môi trường kể cả prd. |

---

## Member tự check trước khi submit

> ⚠️ **KHÔNG áp dụng** — bộ TC này fetch từ Studio (14 TC do AI sinh + 1 TC do `vinth@mcp` thêm), không phải member người viết. Bảng duyệt quan điểm tầng 1 (`test_viewpoint_selection`) trên Studio đang **null**.
>
> Leader chạy `/review-tc tasks/2026-09-11_40539_chat-1-1-keo-tha-text-hien-man-upload/ 40539` để review coverage theo file 03 + 80 quan điểm LME.

<!-- Source: fetched từ MCP LME TEST STUDIO task_id=287 (ticket 40539), testcase_list 15 TC, lúc 2026-09-11. TCs là READ-ONLY — KHÔNG sửa ở file này, sửa trên Studio (testcase_update) rồi fetch lại. -->
