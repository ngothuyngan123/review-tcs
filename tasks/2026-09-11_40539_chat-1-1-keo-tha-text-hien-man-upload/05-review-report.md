# 05 — Review Report (draft cho Leader verify)

## 0. Nguồn TC

- **Nguồn đã dùng**: MCP LME TEST STUDIO — task **#287** (ticket 40539, `round 1`, branch `ai_fixbug_40539`, `reviewState = leader`, `reviewed = false`); snapshot ghi vào `04-tc-list.md`.
- **Tổng số TC review**: **15** — exec `13 pass / 2 fail / 0 untested` (`aiResult = fail`), 14 TC do AI sinh + 1 TC do `vinth@mcp` viết.

---

## 1. Coverage — chiều `dev-impact` + chiều `diff code`

**Kết luận: 7 vùng ảnh hưởng · 2 đủ TC · 3 GAP · 2 RISK.**

| Mã | Chiều | Vùng ảnh hưởng | Trạng thái | Vấn đề | Severity |
|---|---|---|---|---|---|
| **G1** | dev-impact | `T1` 1-on-1 Chat — 4.3 Dev ghi rõ *"kéo-thả tệp ảnh/**video**/**âm thanh** để đính kèm vẫn giữ nguyên"* | `GAP` | **Không TC nào kéo-thả tệp âm thanh** (`.m4a`). TC-REGSHARED001-01 chỉ ghi ở `Ghi chú`: *"biến thể tệp âm thanh .m4a chạy được cùng bộ bước, tester chạy thêm khi có tệp mẫu"* — ghi chú không phải TC, và không có bằng chứng đã chạy. `.m4a` đi qua đúng nhánh map loại media của `getFileUpload` như `.mp4` nhưng là nhánh riêng. Kho `TC-CHT-188` đã liệt kê audio M4A trong bộ dữ liệu test chuẩn của màn này. | `[MAJOR]` |
| **G2** | diff code | `F2` `isDragFileEvent` — **nhánh dự phòng `dataTransfer.files`** | `GAP` | Hàm mới có **3 nhánh nhận diện**: `types` chứa `Files` (Chromium) · `types` chứa `application/x-moz-file` (Firefox) · **fallback đọc `dataTransfer.files`** khi trình duyệt không cấp `types`. Hai nhánh đầu có TC (TC-TOOLKNOW002-01, TC-UI002-01); **nhánh fallback không TC nào**. Chính Dev nêu rủi ro này ở file 03 mục *Rủi ro/lưu ý khi test #2*: trường hợp đó *"màn upload không bật lúc rê chuột, cần thả tệp"* — tức **hành vi khác hẳn**, chưa ai xác nhận. | `[MAJOR]` |
| **G3** | diff code | `F1` handler `.content-chat` — **bỏ `preventDefault` cho drag không phải tệp** | `GAP` | Hành vi mới trả drag non-file về mặc định trình duyệt. Dev tự cảnh báo: *"thả một liên kết kéo từ nơi khác vào vùng không nhập liệu **có thể khiến trình duyệt điều hướng theo liên kết**"*. Hệ quả thật với admin: **đang soạn tin dở → trình duyệt rời trang → mất nội dung**. TC-OUTTRUTH001-02 có test kéo link nhưng **chỉ kéo link có sẵn trong chính trang**, và auto-actual ghi *"trình duyệt không nhận cú thả"* — **không** cover kéo link từ **tab/ứng dụng khác** thả vào vùng không nhập liệu. | `[MAJOR]` |
| **G4** | diff code | `F2` — **tương thích trình duyệt**, trục rủi ro số 1 do chính Studio `dev_impact` xếp hạng | `RISK` | `dev_impact` Studio: *"Rủi ro tương thích trình duyệt: phụ thuộc `dataTransfer.types` ('Files' vs 'application/x-moz-file') — cần verify Chrome/Firefox/Edge"*. TC-UI002-01 có tồn tại và `pass`, nhưng: chạy **manual, `evidence = []`, `actual = null`**, chỉ ở env `local`, và **Safari bị TC tự loại khỏi phán quyết**. Trục rủi ro cao nhất đang không có bằng chứng kiểm chứng được. | `[MAJOR]` |
| **G5** | diff code | `F3`/`F4`/`F5` — luồng gửi media `EP-09 POST /basic/send-media-v2` tới LINE | `RISK` | Studio `dev_impact` xếp đây là *"Rủi ro hồi quy CAO nhất: nếu `isDragFileEvent` nhận diện SAI tệp thật → mất đường gửi tệp bằng kéo-thả"*. Có TC (TC-OUTTRUTH001-01, TC-REGSHARED001-01/02) nhưng **mọi kết quả `pass` của bước GỬI đều lấy từ TEST DOUBLE cục bộ thay `api.line.me`** — actual ghi nguyên văn *"KHÔNG chứng minh được hiển thị thực tế trên app LINE của người nhận"*. Run 1214 trước đó **`skip` đúng 6 TC này** vì local thiếu `imagick` + bot fixture không có `channel_access_token`. Vi phạm **RULE-06** (output cuối chuỗi). | `[MAJOR]` |
| — | dev-impact | `F1` handler `.content-chat` (đủ) · `BUG` root cause (đủ) | `OK` | TC-TOOLKNOW002-01 reproduce đúng steps file 01; cả 2 nhánh true/false của điều kiện mới đều có TC. Không ghi ra. | — |
| — | dev-impact | `D*` data | — | File 03 mục 4.2 = "Không có" — fix thuần FE, không đụng DB. Không cần TC data. | — |

**Ghi chú chiều (b)**: `spec_delta` Studio có diff thật (`diffAvailable = true`, `public/js/chats/chat-v2.js`, **1 file / +25 dòng**) → mọi kết luận trên dựa trên diff, không phải suy từ mô tả.

**Không có TC orphan** — cả 15 TC đều map được về `F*`/`T1`/`BUG`.

---

## 2. Thiếu so với quan điểm test LME

**Kết luận: 21 quan điểm Trigger khớp task · 15 đã cover đủ · 6 thiếu.**

| Mã | Quan điểm | Ưu tiên | Vấn đề | Severity |
|---|---|---|---|---|
| **Q1** | `ENV-003` ★ | **Cao** | Trigger: *"**BẮT BUỘC** khi tính năng chạm **media/file**, URL/domain, job nền, thanh toán"* — task này chạm media/file toàn bộ. **`envAuto` cho thấy `staging` 0 run · `prd` 0 run**, 100% kết quả lấy ở `local` (2 run). Quan điểm này ghi thẳng: *"Không được đánh × với lý do 'staging đã pass'"* — ở đây còn chưa tới staging. Vi phạm **RULE-08**. | `[BLOCKER]` |
| **Q2** | `MEDIA-IMG-001` ★ | Trung bình → **nâng Cao** (ảnh gửi ra phía LINE user) | Không TC nào verify **ảnh sau khi hệ thống resize** hiển thị ra sao trên LINE người nhận. Luồng `ChatService::setMessage` dựng ảnh bằng `ImageManager(driver=imagick)` — chính extension mà env local **không có** (bằng chứng: run 1214 `skip` TC 17098/17108 với lỗi `ImageMagick module not available`). Nghĩa là **nhánh xử lý ảnh thật chưa từng chạy** trong bộ TC này. Kho có `TC-CHT-195` cover đúng điểm này. | `[BLOCKER]` |
| **Q3** | `MEDIA-CLEAN-001` ★ | Trung bình → **nâng Cao** | Trigger: *"khi chức năng cho phép **xóa hoặc thay thế** file/ảnh đã upload"*. Sau khi kéo-thả, file nằm ở khung xem trước và admin **xoá/sắp xếp lại được** trước khi gửi (kho `TC-CHT-193`). Không TC nào của task chạm thao tác này — TC-FUNC004-01 còn phải **tải lại màn hình** để dọn khung xem trước thay vì xoá. | `[MAJOR]` |
| **Q4** | `FUNC-DRAFT-001` | Trung bình | Trigger: *"**chỉ** editor/soạn thảo (chat, template, form builder)"* — đúng màn này. Không TC nào verify **nội dung đang soạn dở còn nguyên** sau các thao tác kéo-thả (thả file khi ô soạn tin đang có chữ, thả nhầm ra ngoài, đóng lớp phủ bằng ✕). Liên kết trực tiếp với **G3**. | `[MAJOR]` |
| **Q5** | `MSG-004` | **Cao** | Trigger: *"**BẮT BUỘC** khi output là nội dung user cuối nhìn thấy trên LINE"*. Text/ảnh/video kéo-thả đều đi ra LINE. Mọi verify dừng ở DB `messages_v2s` + khung hội thoại admin, LINE API là test double. Đây là mặt quan điểm của **G5** (RULE-06). | `[MAJOR]` |
| **Q6** | `COMPAT-LEGACY-001` ★ | **Cao** | Trigger: *"**BẮT BUỘC** khi tính năng chạm ... **bất kỳ đối tượng nào đã từng version-up**"*. Màn này đã version-up rõ: route `/basic/chat-v3`, asset `chat-v2.js`, endpoint `send-media-v2` / `send-message-v2`, và `logic-spec.md:62` ghi còn **`sendMedia()` legacy V1**. Không TC nào chạm nhánh cũ / tin nhắn dữ liệu cũ. Kho có `TC-CHT-260`. **Không được đánh × chỉ vì "dữ liệu mới chạy ổn"** (RULE-09). | `[MAJOR]` |

**Đã cover đủ, không ghi chi tiết** (15): `FUNC-001` · `FUNC-004` · `FUNC-MULTI-001` · `OUT-PREVIEW-001` · `OUT-TRUTH-001` · `UI-001` · `UI-002` · `UI-003` · `UI-INPUT-001` · `MEDIA-001` (trừ audio → G1) · `REG-SHARED-001` · `DEPLOY-ASSET-001` · `DATA-TEXT-001` · `DEPLOY-LIVE-001` · `INTG-LINE-001` (phần payload).

**Đánh × có lý do** — `SYNC-APP-001` (chat 1:1 có bản APP MOBILE, kho có `TC-CHT-201/207/263`): fix nằm trọn trong `public/js/chats/chat-v2.js` — asset **chỉ web PC** dùng; app mobile không nạp file này nên không có đường hồi quy. Không đề xuất TC. `PERM-*` · `DATA-DB-001` · `JOB-001` · `CONC-001` · `SEC-*` × vì fix không chạm phân quyền / DB / job / luồng gửi đồng thời / dữ liệu nhạy cảm.

---

## 3. TC trùng lặp nội dung

Đã rà **15/15 TC** theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`) → phát hiện **1 nhóm nghi trùng**.

| Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| Kéo phần tử có sẵn trong lịch sử hội thoại | `TC-TOOLAXIS001-01` (#17102 — kéo **ảnh**) | `TC-TOOLAXIS001-02` (#17331 — kéo **Sticker**) — **GIỮ CẢ 2, KHÔNG xoá** | `DUP-SUBSET` (một phần) | Trùng: mã quan điểm `TOOL-AXIS-001` · loại case `Abnormal` · thao tác "nhấn giữ → kéo trong khung hội thoại → thả" · tiền đề tương đương · expected tương đương ("không bung lớp phủ"). **Khác**: đối tượng kéo (ảnh `<img>` vs Sticker) — Sticker có thể được trình duyệt serialize thành bộ `types` khác, nên vẫn là giá trị trục riêng. | `[MINOR]` (TC) |

**Đề xuất**: **không xoá TC nào** — gate BƯỚC 4b: bỏ `TC-TOOLAXIS001-02` sẽ mất cover trục "Sticker", bỏ `TC-TOOLAXIS001-01` mất cover trục "ảnh trong trang".

⚠️ **Nhưng 2 ticket bug thì cần hợp nhất**: cả 2 TC này cùng `fail` ngày 2026-09-11 và sinh **2 Redmine ticket riêng (#40854, #40855)** với cùng một mô tả hiện tượng, cùng severity, cùng người, cách nhau 30 giây. Nếu điều tra cho thấy cùng root cause (cùng `dataTransfer.types` của phần tử DOM trong trang) thì đây là **duplicate ticket** → xem `[BLOCKER] #2` ở §4.

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | `TC-TOOLAXIS001-01` (#17102) | **Cùng 1 TC cho 2 kết quả ngược nhau**: auto run 1214 (2026-09-10, pipeline) = `pass` **kèm bằng chứng đo được** `types=["text/uri-list","text/plain","text/html"]` + screenshot; manual run 1257 (2026-09-11, vinth) = `fail` với `actual = null`, `evidence = []`. Không thể kết luận bản fix đúng hay sai. | Yêu cầu tester chạy lại và ghi **`actual` + tên/phiên bản trình duyệt + `dataTransfer.types` quan sát được** — đúng như note của chính TC đã dặn. Trước khi có dữ liệu đó, **không đóng và cũng không bác** #40854. |
| 2 | `[BLOCKER]` | Redmine #40854, #40855 (bug Studio #1020, #1019) | **2 bug ticket raise với `actual = null` và `evidence = []`**. Dev nhận ticket không có hiện tượng thật, không ảnh, không log → không điều tra được; vi phạm **RULE-02**. Thêm nữa 2 ticket tạo cách nhau 30 giây với mô tả cùng dạng → nghi **duplicate** (§3). | Bổ sung `actual` + ảnh/video + môi trường vào cả 2 ticket; đối chiếu root cause rồi hợp nhất nếu trùng. |
| 3 | `[MAJOR]` | Toàn bộ 3 kết quả **manual** (TC-UI002-01, TC-TOOLAXIS001-01, TC-TOOLAXIS001-02) | `evidence = []` ở cả 3 — trong đó TC-UI002-01 là TC `manual` **duy nhất** và đang gánh trục rủi ro số 1 (đa trình duyệt). "Đạt" ở đây là **tự khai**. Vi phạm **RULE-02**. | Chạy lại kèm ảnh/video từng trình duyệt; TC-UI002-01 phải có evidence riêng cho Chrome / Firefox / Edge. |
| 4 | `[MAJOR]` | 15/15 TC | Cột `Trạng thái đánh giá spec` **rỗng toàn bộ** (`spec_status = null`), trong khi **spec `spec-features/admin/chat-11/` KHÔNG mô tả hành vi kéo-thả ở bất kỳ đâu** (đã grep `drag` / `ドラッグ` / `drop` — không có kết quả nào về hành vi này; `ui-spec.md` chỉ có toolbar 「メディア送信」「PDF送信」「スタンプ送信」). Mọi `Kết quả mong đợi` đang suy từ **code**, không có chuẩn spec → đúng cái bẫy RULE-11 (tự suy diễn rồi cho Đạt). | Điền `Spec không ghi` + ghi rõ **đã hỏi ai** cho toàn bộ TC; xem §6. |
| 5 | `[MAJOR]` | 6 TC từng `skip` ở run 1214 (#17097, #17098, #17099, #17103, #17104, #17108) | Bước GỬI **không chạy được trên local** (bot fixture không có `channel_access_token` thật; PHP local thiếu `imagick`). Run 1234 sau đó cho `pass` nhưng **`api.line.me` bị thay bằng TEST DOUBLE cục bộ**. Nghĩa là nhánh xử lý ảnh thật (`imagick` resize) và đường ra LINE thật **chưa từng thực thi**. | Chạy lại nhóm này ở `staging`/`prd` (xem TC đề xuất `TC-ENV003-01`, `TC-MSG004-01`). |
| 6 | `[MAJOR]` | `03-dev-impact.md` | Checkbox **"Tester verify auto-fill chính xác" chưa tick**, trong khi file có `Auto-filled: 2026-09-11 by /new-task`. F/D/T có thể thiếu hoặc map sai — và thực tế mục 4.1 của Dev **chỉ ghi tên file**, không kê function theo dạng `F*`. | Tester đọc lại journal #135432 trên Redmine, xác nhận đủ 4 mục rồi tick. |
| 7 | `[MINOR]` | 4 TC (#17096, #17102, #17105, #17331) | Mã quan điểm **`TOOL-KNOW-002`, `TOOL-AXIS-001`, `TOOL-NEGCTRL-001` không tồn tại** trong `framework/checklist-lme.md` (mã nội bộ Studio) → không map coverage tự động được. Nội dung 4 TC vẫn hợp lệ và đã được quy chiếu tay trong §2. | Quy chiếu về tầng 1: `TOOL-KNOW-002`→`UI-INPUT-001`, `TOOL-AXIS-001`→`UI-001`/`FUNC-001`, `TOOL-NEGCTRL-001`→`REG-SHARED-001`. |
| 8 | `[MINOR]` | Coverage Studio | `task_get_report` → `coverage.summary = {total: 23, covered: 0, partial: 23, none: 0}` — **0/23 ref được đánh dấu `covered`**, kể cả `SCR-CHT-01` và `EP-09` dù có 15 và 3 TC trỏ vào. | Cập nhật trạng thái coverage trên Studio sau khi chốt review, để con số phản ánh đúng. |
| 9 | `[NIT]` | `spec_ids` của #17097, #17108 | Trỏ `spec: admin/chat-1on1/ui/ui-spec.md:386-393` và `:146`, nhưng ở bản `spec-features/` trong repo hai vùng dòng này là "Luồng 10/11 — trạng thái đối ứng / ẩn bạn bè" và "Tab 2 friend info", **không phải** ô soạn tin / toolbar media. Có thể do lệch version giữa spec Studio và sub-repo `spec-features/`. | Verify lại số dòng khi đối chiếu spec; không chặn review. |

### Chất lượng từng TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 10 | `[MAJOR]` | `TC-UIINPUT001-01` (#17097) | **Không atomic** — 1 TC gộp: kéo chữ trong trang + kéo chữ từ ứng dụng ngoài + chuỗi chứa thẻ HTML + **bấm gửi** + **F5 đối chiếu**. Hệ quả đã xảy ra thật: run 1214 đánh **cả TC là `skip`** chỉ vì bước GỬI không chạy được, **chôn luôn** phần kéo-thả vốn đã verify xong và đạt. | Tách: (a) kéo-thả chữ vào ô soạn tin giữ nguyên văn (chạy được mọi env); (b) gửi tin từ nội dung đã thả + F5 (cần env gửi được). |
| 11 | `[MAJOR]` | `TC-MEDIA001-01` (#17103) | `Kết quả mong đợi` **mâu thuẫn với tài liệu điều tra** — chính note TC ghi: *"tài liệu bước điều tra ghi 'kéo-thả tệp PDF vào vùng chat thì gửi thành công', nhưng mã nguồn chỉ chấp nhận .png/.jpg/.jpeg/.mp4/.m4a"*. TC đã tự chọn bên **code**. Đây là quyết định spec, không phải quyết định của TC. | Đẩy lên Leader/Dev chốt (§6). Trước khi chốt, không dùng TC này làm căn cứ đóng ticket. |
| 12 | `[MINOR]` | `TC-UI002-01` (#17100) | Gộp 2 trình duyệt (Firefox + Edge) trong 1 TC, và tự loại Safari khỏi phán quyết. Với trục rủi ro số 1 thì mỗi trình duyệt cần 1 kết luận + 1 evidence riêng. | Tách theo trình duyệt — xem `TC-UI002-02`. |

**Anti-pattern**: đã rà AP-1…AP-6 — không phát hiện anti-pattern nào cần ghi.

---

## 5. TCs đề xuất bổ sung (7)

> **Đã đối chiếu 15 TC ở BƯỚC 0 + `kho-tcs/fa001-chat11-11チャット.md` (430 TC) — không TC đề xuất nào trùng.**
> Vùng đã đối chiếu trong kho: `TC-CHT-187…197` (Gửi media) · `TC-CHT-198…203` (Gửi PDF) · `TC-CHT-204…211` (Gửi sticker) · `TC-CHT-256…265` (Hiển thị message — media & định dạng) · `TC-CHT-193` (xem trước/xoá/sắp xếp file).

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-MEDIA001-02 | UI | MEDIA-001 | Chat 1:1 — kéo & thả trong khung hội thoại | Normal | manual | Tất cả | Kéo tệp âm thanh .m4a thẳng vào khung hội thoại — đính kèm và gửi được | - Đăng nhập admin, đã chọn bot test<br>- Mở /basic/chat-v3, chọn bạn bè test<br>- Máy có sẵn 1 tệp .m4a ngắn (vài giây), dung lượng dưới 25MB | 1. Kéo tệp .m4a từ máy vào giữa khung hội thoại, giữ chuột lại<br>2. Quan sát lớp phủ có hiện không<br>3. Nhả chuột để thả tệp<br>4. Quan sát khung xem trước phía trên ô soạn tin<br>5. Bấm nút gửi<br>6. Quan sát khung hội thoại, bấm phát thử tệp âm thanh | Tệp âm thanh .m4a khoảng 5 giây, vài trăm KB | - Lớp phủ 「送信するデータをアップロード」 hiện ra giống hệt khi kéo ảnh và video<br>- Thả xong tệp âm thanh vào khung xem trước, KHÔNG có thông báo 「ファイルの形式が正しくありません。」<br>- Bấm gửi thì tin âm thanh xuất hiện trong khung hội thoại và **phát được tiếng** | | Lấp G1 · Đánh giá spec: Spec không ghi — cần hỏi Leader · Evidence: video quay màn hình lúc kéo + ảnh khung hội thoại sau gửi · Dẫn chiếu kho `TC-CHT-188` (audio M4A nằm trong bộ dữ liệu chuẩn) · regression |
| TC-UI002-02 | UI | UI-002 | Chat 1:1 — kéo & thả trong khung hội thoại | Normal | manual | Tất cả | Kéo chữ và kéo tệp trên Chrome · Edge · Firefox · Safari — mỗi trình duyệt một kết luận riêng | - Máy test cài Chrome, Edge, Firefox bản mới; nếu có máy Mac thì thêm Safari<br>- Đều đăng nhập được trang quản trị với bot test<br>- Có sẵn 1 ảnh PNG hợp lệ trên máy | 1. Trên trình duyệt thứ nhất: mở Chat 1:1, chọn bạn bè test<br>2. Bôi đen chữ trong ô soạn tin, kéo sang vị trí khác trong ô soạn tin, thả<br>3. Chụp màn hình kết quả<br>4. Kéo tệp ảnh PNG **thật từ trình quản lý tệp của hệ điều hành** vào khung hội thoại, thả<br>5. Chụp màn hình lớp phủ + khung xem trước, bấm gửi, chụp kết quả<br>6. Lặp lại toàn bộ bước 1-5 trên từng trình duyệt còn lại, ghi kết luận riêng cho mỗi trình duyệt | Chuỗi kéo: 'kiem tra trinh duyet 40539' · Ảnh PNG dưới 10MB | Với **từng** trình duyệt, ghi riêng 1 kết luận Đạt/Không đạt:<br>- Kéo chữ: KHÔNG hiện lớp phủ 「送信するデータをアップロード」, chữ di chuyển được bình thường<br>- Kéo tệp thật: lớp phủ VẪN hiện, thả xong ảnh vào khung xem trước, bấm gửi thì ảnh vào khung hội thoại<br>- Safari: ghi nhận nguyên trạng kèm phiên bản; nếu khác 3 trình duyệt kia thì báo Leader, không tự kết luận | | Lấp G4 · Thay thế `TC-UI002-01` vốn gộp Firefox+Edge và chạy không evidence · Evidence: **bắt buộc** 2 ảnh/trình duyệt (lúc kéo chữ, lúc kéo tệp) · Đánh giá spec: Spec không ghi — cần hỏi Leader · Kéo tệp dựng bằng script KHÔNG tính là Đạt |
| TC-FUNCDRAFT001-01 | UI | FUNC-DRAFT-001 | Chat 1:1 — ô soạn tin khi kéo & thả | Abnormal | manual | Tất cả | Kéo liên kết từ tab khác thả vào vùng không nhập liệu — không rời trang, không mất tin đang soạn | - Đăng nhập admin, mở /basic/chat-v3, chọn bạn bè test<br>- Mở sẵn **1 tab trình duyệt khác** có 1 trang bất kỳ<br>- Trong ô soạn tin đã gõ sẵn một đoạn chữ **chưa gửi** | 1. Gõ vào ô soạn tin: 'tin dang soan chua gui 40539', KHÔNG bấm gửi<br>2. Sang tab khác, nhấn giữ biểu tượng đường dẫn trên thanh địa chỉ<br>3. Kéo sang tab Chat 1:1, thả vào **vùng lịch sử hội thoại** (không phải ô soạn tin)<br>4. Quan sát: trang có bị chuyển sang địa chỉ vừa thả không<br>5. Kiểm tra nội dung ô soạn tin<br>6. Lặp lại bước 2-5 nhưng thả vào **cột danh sách bạn bè** bên trái<br>7. Lặp lại với một đoạn chữ bôi đen kéo từ ứng dụng ngoài (Notepad) thả vào vùng lịch sử hội thoại | Tin đang soạn: 'tin dang soan chua gui 40539' · Đường dẫn kéo từ tab khác · Đoạn chữ từ Notepad | - Trang Chat 1:1 **KHÔNG điều hướng** sang địa chỉ vừa thả, vẫn ở /basic/chat-v3 đúng hội thoại đang mở<br>- Nội dung ô soạn tin vẫn **nguyên vẹn** 'tin dang soan chua gui 40539', không bị xoá và không bị chèn thêm<br>- Không hiện lớp phủ 「送信するデータをアップロード」, không tệp nào bị đính kèm<br>- Bấm gửi sau đó vẫn gửi đúng nội dung đang soạn | | Lấp G3 + Q4 · ⚠️ Dev **tự nêu rủi ro này** ở file 03 (bỏ `preventDefault` → trình duyệt có thể điều hướng theo liên kết) và coi là "hành vi mặc định giống mọi màn khác" → **nếu trang thật sự điều hướng và mất tin đang soạn, cần Leader chốt có chấp nhận không** (xem §6) · Evidence: video quay từ lúc thả tới lúc kiểm ô soạn tin |
| TC-MSG004-01 | UI | MSG-004 | Chat 1:1 — gửi media bằng kéo & thả | Normal | manual | **product** | Gửi ảnh và video bằng kéo-thả — kiểm trên máy LINE thật của người nhận | - Môi trường **production** (`step.lme.jp`), bản đã deploy fix<br>- Bot test + **thiết bị thật đã kết bạn với bot** (không dùng khách hàng thật)<br>- Máy test có 1 ảnh PNG ~2MB (nội dung dễ nhận biết) và 1 video MP4 ~5 giây | 1. Mở Chat 1:1, chọn đúng bạn bè test đang cầm thiết bị<br>2. Kéo tệp ảnh PNG vào khung hội thoại, thả lên lớp phủ, bấm gửi<br>3. Trên **thiết bị LINE thật**: mở hội thoại với bot, quan sát ảnh nhận được<br>4. Phóng to ảnh trên LINE, đối chiếu với ảnh gốc trên máy (nội dung, tỷ lệ, độ nét)<br>5. Quay lại admin, kéo tệp video MP4 vào khung hội thoại, thả, bấm gửi<br>6. Trên thiết bị LINE: mở và **bấm phát** video nhận được | Ảnh PNG ~2MB · Video MP4 ~5 giây | - Người nhận **thấy ảnh trên LINE**, nội dung khớp ảnh gốc, không méo tỷ lệ, không mất nét bất thường sau khi hệ thống resize<br>- Người nhận **phát được video** trên LINE, có hình và tiếng<br>- Khung hội thoại bên admin hiển thị đúng 2 tin đã gửi sau khi F5 | | Lấp G5 + Q5 + Q2 · **RULE-06** — mọi kết quả hiện tại chỉ tới TEST DOUBLE cục bộ, chưa tới LINE thật · **RULE-08** → bắt buộc `product` vì chạm media · Nhánh resize dùng `imagick` **chưa từng chạy** (local thiếu extension) · Dẫn chiếu kho `TC-CHT-195` · Evidence: ảnh chụp màn hình LINE trên thiết bị thật · Đánh giá spec: Spec không ghi — cần hỏi Leader |
| TC-ENV003-01 | UI | ENV-003 | Chat 1:1 — smoke kéo & thả sau deploy | Normal | manual | **product** | Smoke kéo-thả chữ và tệp trên production ngay sau deploy | - Bản fix đã deploy lên **production** (`step.lme.jp`)<br>- Đăng nhập bằng tài khoản test trên production, chọn bot test<br>- Có 1 ảnh PNG nhỏ trên máy | 1. Mở Chat 1:1 trên production, chọn bạn bè test<br>2. Nhấn **F5 thường** (không Ctrl+F5)<br>3. Gõ chữ vào ô soạn tin, bôi đen, kéo sang vị trí khác trong ô soạn tin, thả<br>4. Quan sát lớp phủ upload<br>5. Kéo tệp ảnh PNG từ máy vào khung hội thoại, thả<br>6. Quan sát lớp phủ + khung xem trước, rồi đóng bằng ✕ (**không bấm gửi** để tránh phát sinh tin thật nếu dùng bot có khách) | Chuỗi: 'smoke 40539' · Ảnh PNG dưới 10MB | - Chỉ F5 thường là hành vi đã đúng: kéo chữ **không** bung lớp phủ 「送信するデータをアップロード」<br>- Kéo tệp thật **vẫn** bung lớp phủ và đính kèm được vào khung xem trước<br>- Bấm ✕ đóng lớp phủ, màn chat dùng lại bình thường<br>- Không lỗi JavaScript mới trên bảng điều khiển | | Lấp Q1 · **RULE-08 / ENV-003** — hiện `staging` 0 run và `prd` 0 run, 100% kết quả ở `local` · RULE-01: quan điểm Cao nhưng chỉ đề xuất 1 Normal — biên và bất thường đã cover ở `TC-FUNC004-01/02`, `TC-MEDIA001-01`, không lặp lại trên production để tránh tạo dữ liệu thật · Evidence: ảnh màn hình production + tab Network cho thấy `chat-v2.js?v=` bản mới |
| TC-MEDIACLEAN001-01 | UI | MEDIA-CLEAN-001 | Chat 1:1 — khung xem trước tệp đã đính kèm | Abnormal | manual | staging | Xoá bớt tệp khỏi khung xem trước sau khi kéo-thả nhiều tệp — gửi đúng phần còn lại | - Đăng nhập admin, mở Chat 1:1 với bạn bè test<br>- Máy có 3 tệp ảnh PNG đặt tên phân biệt được (A, B, C) | 1. Chọn cùng lúc 3 tệp A, B, C, kéo cả nhóm vào khung hội thoại, thả lên lớp phủ<br>2. Đếm và đọc tên 3 ảnh trong khung xem trước<br>3. Xoá ảnh B khỏi khung xem trước<br>4. Kiểm tra khung xem trước còn lại những ảnh nào<br>5. Kéo thả thêm tệp C lần nữa vào khung hội thoại<br>6. Bấm gửi<br>7. Đối chiếu số tin và nội dung trong khung hội thoại | 3 ảnh PNG: A.png, B.png, C.png (mỗi tệp dưới 10MB) | - Sau bước 1: đủ 3 ảnh A, B, C trong khung xem trước, đúng tên<br>- Sau khi xoá B: khung xem trước còn **đúng A và C**, không mất nhầm ảnh khác, không còn vết của B<br>- Thả thêm C: khung xem trước xử lý theo đúng quy tắc của màn (nếu chặn trùng thì báo rõ, nếu cho phép thì hiện 2 mục C) — ghi nhận nguyên trạng<br>- Bấm gửi: **chỉ** những ảnh còn trong khung xem trước được gửi, khung hội thoại hiện đúng số ảnh đó | | Lấp Q3 · Dẫn chiếu kho `TC-CHT-193` (xem trước / xoá / sắp xếp file) — TC kho đi từ nút 「メディア送信」, TC này đi từ **đường kéo-thả** là đường vừa bị sửa · Đánh giá spec: Spec không ghi — cần hỏi Leader · Evidence: ảnh khung xem trước trước và sau khi xoá |
| TC-COMPATLEGACY001-01 | UI | COMPAT-LEGACY-001 | Chat 1:1 — hiển thị tin nhắn dữ liệu cũ | Normal | manual | staging | Hội thoại có tin nhắn dữ liệu cũ — vẫn hiển thị đủ và kéo-thả không bung lớp phủ | - Đăng nhập admin, chọn bot test<br>- Chọn một bạn bè có lịch sử hội thoại **cũ** (tin gửi từ bản trước khi màn chat version-up: có tin ảnh, video, sticker, template cũ) | 1. Mở Chat 1:1 với bạn bè có lịch sử cũ<br>2. Cuộn lên xem hết các tin cũ, ghi nhận loại tin hiển thị được<br>3. Bôi đen chữ trong một tin cũ, kéo sang vị trí khác trong khung hội thoại, thả<br>4. Quan sát lớp phủ và giao diện các tin cũ<br>5. Kéo một tệp ảnh PNG từ máy vào khung hội thoại, thả rồi đóng bằng ✕<br>6. Quan sát lại các tin cũ | Hội thoại có tin cũ (ảnh, video, sticker, template) · Ảnh PNG dưới 10MB | - Các tin cũ hiển thị đủ, không vỡ giao diện, không mất phần nào so với trước bản sửa<br>- Kéo chữ trong tin cũ: KHÔNG bung lớp phủ 「送信するデータをアップロード」<br>- Kéo tệp thật: lớp phủ vẫn bung bình thường<br>- Sau khi đóng lớp phủ, các tin cũ vẫn hiển thị nguyên như bước 2 | | Lấp Q6 · **RULE-09** — màn đã version-up (`/basic/chat-v3`, `chat-v2.js`, `send-media-v2`; `logic-spec.md:62` còn `sendMedia()` legacy V1) · Dẫn chiếu kho `TC-CHT-260` (tin nhắn dữ liệu CŨ — hiển thị đủ loại) · Đánh giá spec: Spec không ghi — cần hỏi Leader |

### GAP / quan điểm không đề xuất TC — lý do

| Mã | Lý do không đề xuất TC |
|---|---|
| **G2** (nhánh dự phòng `dataTransfer.files`) | **Không dựng được bằng thao tác UI thuần** — nhánh này chỉ chạy trên trình duyệt không cấp `dataTransfer.types`, mà theo chính Dev thì *"các trình duyệt hiện hành đều có types"*. Manual tester không có cách tạo điều kiện đó. **Đề nghị**: hỏi Dev xem nhánh fallback có còn cần thiết không — nếu không có trình duyệt nào rơi vào thì đây là code chết và nên bỏ; nếu có thì Dev phải chỉ ra trình duyệt/phiên bản cụ thể để QA dựng. `TC-UI002-02` (thêm Safari) đã phủ được phần rủi ro thực tế còn lại. |
| **Q2** `MEDIA-IMG-001` | Đã gộp vào `TC-MSG004-01` (bước 3-4 đối chiếu ảnh sau resize trên LINE thật) thay vì đẻ TC riêng. |
| `SYNC-APP-001` | Fix nằm trọn trong asset web `public/js/chats/chat-v2.js`; app mobile không nạp file này → không có đường hồi quy. |

---

## 6. Spec update needed

| # | Điểm cần chốt | Hiện trạng 2 nguồn | Ai chốt |
|---|---|---|---|
| 1 | **Kéo-thả tệp PDF vào khung hội thoại: chặn hay gửi được?** | Tài liệu bước điều tra ghi *"kéo-thả tệp PDF vào vùng chat thì gửi thành công"*; mã nguồn chỉ nhận `.png/.jpg/.jpeg/.mp4/.m4a` và lớp phủ ghi 「対応データ：画像・動画・音声」; PDF có đường riêng qua nút 「PDF送信」 (`ui-spec.md` toolbar #3). `TC-MEDIA001-01` đã tự chọn bên **code**. | Leader + Dev — nếu chốt "PDF phải gửi được bằng kéo-thả" thì đó là **yêu cầu mới, mở ticket riêng**, không tính là lỗi của #40539. |
| 2 | **Kéo phần tử có sẵn trong trang (ảnh / Sticker trong lịch sử chat): kỳ vọng là gì?** | Expected của `TC-TOOLAXIS001-01/02` *"không bung lớp phủ"* **suy từ code**, spec không ghi. Auto đo được `types=["text/uri-list","text/plain","text/html"]` → pass; manual báo fail không bằng chứng → đã raise #40854, #40855. | Leader chốt kỳ vọng **trước**, rồi mới kết luận 2 ticket kia là bug thật hay expected sai. |
| 3 | **Bỏ `preventDefault` cho drag non-file — chấp nhận việc trình duyệt điều hướng theo liên kết?** | Dev coi đây là *"hành vi mặc định giống mọi màn khác của hệ thống"*. Nhưng ở màn chat, điều hướng đi giữa chừng đồng nghĩa **mất tin đang soạn dở**. Spec không ghi. | Leader — nếu không chấp nhận thì cần fix bổ sung (chặn điều hướng nhưng vẫn cho kéo chữ). Kết quả `TC-FUNCDRAFT001-01` là đầu vào để chốt. |
| 4 | **Spec `spec-features/admin/chat-11/` không mô tả hành vi kéo & thả** | Grep `drag` / `ドラッグ` / `drop` trong toàn bộ thư mục spec: không có mô tả hành vi nào. Cả 15 TC đang lấy chuẩn từ code. | Bổ sung mục "Kéo & thả tệp / văn bản trong khung hội thoại" vào `ui-spec.md`, ghi rõ: loại dữ liệu nào mở lớp phủ, loại nào trả về mặc định trình duyệt. |
| 5 | **Giới hạn dung lượng / định dạng media ở Chat 1:1 (MT-10 tồn đọng của kho)** | Kho `fa001-chat11` đã ghi `MT-10 ⏳ CHỜ QUYẾT ĐỊNH`: spec chat-11 **không** ghi giới hạn dung lượng/định dạng nào, trong khi code + TC đang dùng 10MB ảnh / 200MB video / 25MB audio / 5 tệp. `TC-FUNC004-01/02` của task này đang verify các con số đó. | Leader — chốt và ghi vào spec để TC của cả kho lẫn task này có căn cứ. |

---

> Draft do `/review-tc` sinh ngày 2026-09-11 từ Studio task #287 — **Leader verify trước khi trả về member/Dev.**
