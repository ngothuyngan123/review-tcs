# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | `(1) MCP LME TEST STUDIO — task #316, round 1, branch m_202608_bug_detect_delete_scenario_step_time` |
| Tổng số TC review | `40` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `15/22 vùng ảnh hưởng đủ TC · 4 GAP · 3 RISK`

> Chiều **(a) `dev-impact`** (`BUG` + `F1`–`F8` + `D1` + `T1`–`T5`, tổng 15 vùng): **đủ TC, không GAP** — cả 5 caller Dev kê ở mục 3 đều có TC (`ScenarioModel:448/481/510` → NEW-1/3/7/8/28 · `BotLineUserModel:108` → NEW-14/15 · `HandlePostbackTask:2211` → NEW-13). Không ghi dòng nào.
> Chiều **(b) `diff code`**: `Input thiếu — Studio spec_delta.diffAvailable = false` (git diff không chạy được: không tìm thấy ref `m_202608_bug_detect_delete_scenario_step_time`). Chiều (b) dưới đây suy từ `dev_impact` của Studio + mục 1/2/3 file 03 + `spec-features/admin/scenario/job/job-spec.md` (State Machine + Business Rules) + `feature-spec.md §7`. **Không suy tên file/hàm ngoài những gì 2 nguồn này ghi.**

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | Caller thứ **6** của `startScenarioWithCreatedSentNow`: **auto-next scenario** (`afterScenarioId1..5` → `ActionLaterService`, job-spec BR-05 + feature-spec BR-12) — stop scenario cũ rồi start scenario kế tiếp **liên tiếp trong cùng luồng job**, đúng hình dạng bug | `diff code` | `không có` | `GAP — 0 TC`. Dev **không kê** caller này ở mục 3/4.1; NEW-12 còn cố ý loại trừ (「không gắn kịch bản kế tiếp」) | `[BLOCKER]` |
| G2 | `SentMessageHelper.actionStepSend` — **nhánh gửi LINE thất bại** (job-spec BR-08: `STATUS_SEND_FAILURE` → DELETE row khỏi `scenario_step_time` + ghi `step_message_history` status=3) | `diff code` | `không có` | `GAP — 0 TC`. Cùng lệnh xoá đã sửa nhưng ở nhánh lỗi; nếu xoá ở nhánh này ném exception thì vẫn mất phần ghi lịch sử + chạy action | `[BLOCKER]` |
| G3 | Retry `a83d475` **chỉ bắt đúng 1 error code** — deadlock MySQL `1213` (specific code-check, không phải catch chung) | `diff code` | `NEW-20`, `NEW-21` | `RISK — 1 trigger duy nhất`. Không TC nào trigger **lỗi DB khác** (lock wait timeout `1205`, mất kết nối `2006`/`2013`) và **không** TC nào trigger lỗi **chưa biết trước**. Các lỗi đó vẫn ném ra ngoài → tái lập đúng hậu quả gốc "bỏ dở phần việc còn lại" (AP-1 + AP-4) | `[BLOCKER]` |
| G4 | `actionStepSend` — **nhánh skip**: `STATUS_SKIPPED_BY_FILTER` (friend không thỏa filter branch) và `STATUS_SKIPPED_BY_BOT_EXPIRED_PLAN` (bot hết hạn plan > 7 ngày) — job-spec State Machine, cả 2 đều kết thúc bằng DELETE row | `diff code` | `không có` | `GAP — 0 TC`. Toàn bộ 40 TC **không TC nào** chạm filter phân nhánh (feature-spec BR-06) | `[MAJOR]` |
| G5 | Luồng **テスト配信 / send test** (`is_last_step = 2` / `3`, job-spec BR-04) — cũng đi qua `actionStepSend` + xoá row; `TEST_STEP_MESSAGE_ALL` còn tự chuyển sang step tiếp theo | `diff code` | `không có` | `GAP — 0 TC` | `[MAJOR]` |
| G6 | Đường dọn `scenario_step_time` **phía web (Laravel)**: cascade khi xoá step / xoá `filter_manager` / xoá scenario (feature-spec BR-06 + BR-07) — đây là **phía gây ra race**, chạy song song với job Java | `diff code` | `NEW-4` | `RISK — NEW-4 chỉ cover phía job khi scenario đã bị xoá`; không TC nào **ép đồng thời** thao tác xoá trên màn admin với lúc job đang gửi cho friend của scenario đó | `[MAJOR]` |
| G7 | Throughput / độ trễ worker sau khi thêm `retry + sleepQuietly` vào `actionStepSend` — Studio `dev_impact` nêu thẳng: *"retry+sleep chạy trong worker gửi hàng loạt (job-spec §2.2, 200 worker) — kiểm tra throughput/độ trễ khi nhiều friend cùng mốc giờ"* | `diff code` | `NEW-22` | `RISK — NEW-22 có 30 friend cùng mốc phút nhưng expected không có tiêu chí thời gian/độ trễ nào`, chỉ đếm đủ tin | `[MAJOR]` |

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `22 quan điểm Trigger khớp task · 6 chưa cover đủ`

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `JOB-001` | `Cao` | `RISK — không map được coverage`. 6 TC cốt lõi của task (NEW-3, NEW-8, NEW-15, NEW-19, NEW-20, NEW-21) gắn mã **`JOB-002`** — mã này **không có** trong `framework/checklist-lme.md`. Nội dung TC thực tế có cover JOB-001; đây là lỗi **nhãn**, không phải thiếu test (vì vậy để `[MAJOR]`, không nâng `[BLOCKER]`) | `[MAJOR]` |
| Q2 | `MSG-005` | `Cao` | `GAP — 0 TC`. Scenario tiêu thụ quota tin nhắn; job-spec BR-06: hết quota → `isAvailableToSent = false` → **ảnh hưởng việc thực thi action kèm theo** — chính là phần mà fix `a83d475` bảo vệ khỏi bị mất | `[BLOCKER]` |
| Q3 | `PERF-LARGE-001` | `Trung bình → Cao (gửi tin)` | `RISK — xem G7`. Có TC quy mô (NEW-22, 30 friend) nhưng không đo thời gian/độ trễ, trong khi fix vừa **thêm sleep vào đường nóng** của 200 worker | `[MAJOR]` |
| Q4 | `CONC-001` | `Cao` | `RISK — RULE-01`. 6 TC (NEW-9/11/16/22/23/30) đủ `Abnormal` + `Boundary` nhưng **0 `Normal`** và không ghi lý do. Thiếu case đối chứng "2 thao tác đồng thời **không** xung đột thì cả hai đều hoàn tất" | `[MAJOR]` |
| Q5 | `RULE-04` (phạm vi recovery) | — | `GAP — 0 TC / 0 kế hoạch`. Bug **đã làm lệch `count_follow` / `count_unfinish` trên production** (log prod 18/09). NEW-39 chỉ *khoanh vùng trên bot test* và xác nhận "bản vá không tự sửa dữ liệu cũ" — **không** có thống kê toàn hệ thống xem bao nhiêu scenario / friend đang lệch và kế hoạch khôi phục | `[MAJOR]` |
| Q6 | `DATA-COUNT-001` | `Cao` | `RISK — RULE-01`. Chỉ NEW-6 (`Abnormal`) mang mã này. Chiều `Normal` (NEW-26) và chiều đối chứng cộng/trừ một lần (NEW-23) **có nội dung nhưng gắn mã khác** (`OUT-TRUTH-001` / `CONC-001`) → bảng coverage theo mã trông như thiếu | `[MINOR]` |

> Đã xét và **loại khỏi phạm vi** (không đề xuất TC): `DATA-BACKUP-001` — fix không thêm/đổi bảng DB và không chạm copy bot (NEW-38 đã xác nhận cấu trúc 5 bảng không đổi); `DATA-MIG-001` — không có migration (file 03 mục 4.2 = "Không có", NEW-38 xác nhận); `COMPAT-LEGACY-001` — `scenario_step_time` không có nhánh cũ/mới song song trong `job-spec.md`.

---

## 3. TC trùng lặp nội dung

**Đã rà đủ 40 TC theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương` → `expected`) — không phát hiện trùng lặp cần xóa hoặc gộp.**

4 nhóm ứng viên đã xét kỹ và **kết luận KHÔNG trùng**:

| Nhóm đã xét | TC | Vì sao không trùng |
|---|---|---|
| Đổi kịch bản | `NEW-1` vs `NEW-27` | Khác **nguồn kích hoạt**: NEW-1 đổi bằng 2 trả lời tự động (đường job), NEW-27 đổi bằng nút 「手動変更」 trên màn chi tiết bạn bè (đường web). Expected chồng nhau ở 3 cột đếm nhưng trọng tâm khác (hàng đợi lịch gửi vs bảng lịch sử + 次回配信予定) |
| Hủy kết bạn | `NEW-13` vs `NEW-14` | Đúng **2 caller khác nhau** Dev kê ở mục 3: `HandlePostbackTask:2211` (event từ LINE) vs `BotLineUserModel:108` (job tự phát hiện khi gửi thất bại) |
| Race lúc đang gửi | `NEW-30` vs `NEW-16` vs `NEW-23` | Cùng pattern nhưng khác **thao tác đối kháng**: 強制停止 tay / friend chặn bot / mốc cuối hoàn tất |
| Dọn lịch trên tập rỗng | `NEW-3` vs `NEW-8` vs `NEW-15` | Khác thao tác chủ thể: dừng / bắt đầu / hủy kết bạn — mỗi cái đi qua một caller riêng |

- **Gate đã chạy**: không đề nghị xóa TC nào → coverage §1 + §2 giữ nguyên.
- `DUP-INFLATE`: không có.
- `DUP-CONFLICT`: không có giữa 2 TC trong bộ. Tuy nhiên có **conflict giữa TC và kho-tcs** (NEW-13/14/18 vs `MT-02`) → đã đưa vào §6.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | Toàn bộ 40 TC | **Chưa TC nào được chạy**: Studio `exec.untested = 40`, `pass = 0` (0%), `ranAt = null`. Mọi kết luận "đủ coverage" ở §1/§2 hiện chỉ là **trên giấy** — với bug race condition, TC chưa chạy không chứng minh được gì | Chạy hết bộ TC trước khi Leader ký. Ưu tiên nhóm race (NEW-9/11/16/23/30) và nhóm retry (NEW-20/21) |
| I2 | `[MAJOR]` | Nguồn — môi trường | **RULE-08 / ENV-003**: `env_scope` = 31 TC `local`, 9 TC `all`, **0 TC `prd`**. Task chạm **job nền + race + throughput**, và bug phát sinh trên **production** (log prod 18/09), counter drift cũng trên prod → không được kết luận từ local | Bổ sung TC chạy production (xem §5 `TC-ENV003-01`, `TC-PERFLARGE001-01`, `TC-DATACOUNT001-01`) |
| I3 | `[MAJOR]` | Nguồn — diff | `spec_delta.diffAvailable = false` — Studio không đọc được branch `m_202608_bug_detect_delete_scenario_step_time`. Không verify được **fix shape thực tế** (AP-4): retry là catch chung hay `if (errorCode == 1213)`; bulk DELETE có đúng 3 method hay còn method thứ 4 | Yêu cầu Dev push branch lên remote Studio đọc được, hoặc dán diff 2 commit `938c171` + `a83d475` vào tab Thông tin |
| I4 | `[MAJOR]` | 14 TC | **6 mã quan điểm không có trong `checklist-lme.md`**: `JOB-002`(6 TC) · `DATA-HIST-001`(3) · `TOOL-NEGCTRL-001`(2) · `TOOL-KNOW-002`(1) · `RULE-TOOL-029`(1) · `TOOL-OLDREC-001`(1). Cột `Mã quan điểm liên kết` là cột map coverage → 14/40 TC không map được | Đổi mã trên Studio về mã chuẩn: `JOB-002`→`JOB-001` · `DATA-HIST-001`→`DATA-001`/`LIST-001` · `TOOL-NEGCTRL-001`→`DATA-DB-001` · `TOOL-KNOW-002`→`CONC-001` · `RULE-TOOL-029`→`DATA-MIG-001` · `TOOL-OLDREC-001`→`DATA-COUNT-001`. Hoặc bổ sung các mã này vào checklist nếu Leader muốn giữ |
| I5 | `[MAJOR]` | `NEW-20`, `NEW-21` | Steps yêu cầu "cấu hình fault injection để `deleteById` trả MySQL 1213" nhưng **không nêu cơ chế nào** (công cụ, flag, cách bật). Người khác không dựng lại được env → vi phạm yêu cầu tái lập | Dev/QA bổ sung 1 dòng cụ thể vào `Điều kiện tiền đề`: tên flag / biến môi trường / cách chèn lỗi, hoặc cách thay thế (giữ lock trên chính row bằng phiên MySQL thứ 2) |
| I6 | `[MAJOR]` | `NEW-13`, `NEW-14`, `NEW-18` | Expected chốt cứng "「途中で終了した友だち」 tăng 1" khi friend **chặn bot / hủy kết bạn**. Nhưng kho `kho-tcs/fa009` đánh dấu đây là **`MT-02` — CAO, ⏳ CHỜ QUYẾT ĐỊNH** (vào cột 読了済 hay 途中で終了, không spec nào mô tả). Chạy theo expected này rồi tick Đạt = **tự suy diễn** | Không chạy 3 TC này cho tới khi Leader chốt `MT-02`. Đã đưa vào §6 |
| I7 | `[MINOR]` | 40/40 TC | Cột `Trạng thái đánh giá spec` **rỗng toàn bộ** (Studio trả `spec_status = null` cho mọi TC). Đây là giới hạn của tool, không phải lỗi người viết — nhưng kết hợp với I6 thì mất đúng cái chốt chặn "đã hỏi ai chưa" | Đề nghị Studio bổ sung field `spec_status`; tạm thời QA ghi vào `note` của TC |
| I8 | `[MINOR]` | `NEW-10` | Không atomic — 1 TC test **2 nguồn kích hoạt** khác nhau trên 2 friend (P1 thêm bạn mới, P2 gắn tag). Fail ở nhánh nào cũng làm cả TC fail, khó triage | Tách thành 2 TC (`NEW-10a` thêm bạn mới, `NEW-10b` gắn tag). Mức thấp vì 2 nhánh cùng một mục đích test |
| I9 | `[NIT]` | `NEW-39` | "Khoanh vùng dữ liệu đã lệch từ trước khi triển khai" thực chất là **việc khảo sát dữ liệu**, không phải TC có kết quả Đạt/Không đạt rõ ràng | Giữ nguyên nhưng gắn nhãn khảo sát, và bổ sung TC thống kê toàn hệ thống ở §5 (`TC-DATACOUNT001-01`) |

---

## 5. TCs đề xuất bổ sung (14)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa009-phathanhtheobuoc-ステップ配信.md` (545 dòng — nhóm 「Job gửi step & is_last_step」 19 TC, 「Job khi edit step đang chạy」 13 TC, 「Xóa scenario」 9 TC) |
| Vùng regression phát hiện từ kho | `TC-SCE-342` (step cuối + có next scenario) → **G1** · `TC-SCE-343` (step có filter, friend không thỏa) → **G4** · `TC-SCE-358` (job send test hàng loạt, 3 option) → **G5** · `TC-SCE-370` / `TC-SCE-371` (xóa step / xóa filter chứa last message khi scenario đang chạy) → **G6** · `TC-SCE-349` / `TC-SCE-350` (RECOVER khi `is_following=1` mà không còn `scenario_step_time`) → hệ quả trực tiếp của hành vi mới "xóa 0 row im lặng" |
| Conflict expected vs kho | `NEW-13`/`NEW-14`/`NEW-18` vs `MT-02` (kho FA-009) → `[MAJOR] SPEC-CONFLICT`, **đã đưa §6**. Không tự chọn bên → không viết TC mới cho vùng counter khi block/unfollow |
| GAP dùng lại TC kho (không viết mới) | `TC-SCE-349` + `TC-SCE-350` — chạy lại nguyên trạng để bắt hệ quả "xóa 0 row im lặng" sinh thêm friend kẹt `is_following=1`; không viết TC mới vì kho đã cover đúng ý định |
| Xác nhận chống trùng | Đã đối chiếu `40` TC ở BƯỚC 0 + `kho-tcs/fa009` — **không TC đề xuất nào trùng** (mọi TC dưới đây có `đối tượng + thao tác` hoặc `tiền đề` khác hẳn 40 TC gốc) |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-JOB001-01 | UI | JOB-001 | Job gửi step & is_last_step | Normal | auto | Tất cả | Mốc cuối gửi xong → kịch bản kế tiếp được bắt đầu liền, không mất tin của kịch bản mới | Bot test. Kịch bản A riêng của lần chạy có 2 mốc, mốc thứ 2 là mốc cuối đặt tới hạn ngay. Kịch bản B riêng của lần chạy có 1 mốc 「ステップ開始直後」 + 1 mốc 「経過時間」5分後. Trên cấu hình của A đặt B làm kịch bản kế tiếp, khoảng ngày hiệu lực bao trùm hôm nay. 1 bạn bè test đang nhận A. Job chạy ở nhánh đã vá | 1. Ghi lại 3 cột đếm của A và B trên màn 「ステップ配信」.<br>2. Chờ mốc cuối của A tới hạn để job gửi.<br>3. Mở chat 1:1 của bạn bè, đếm tin của mốc cuối A và tin hệ thống báo bắt đầu kịch bản.<br>4. Mở màn chi tiết bạn bè → tab 「ステップ配信」, đọc 「配信中のステップ」, 「次回配信予定」 và bảng lịch sử.<br>5. Đọc lại 3 cột đếm của A và B.<br>6. Chờ tới mốc 5 phút của B, kiểm tin của B trong chat 1:1.<br>7. Đọc log job cả khoảng thời gian trên | A: 経過時間 0時間0分後 + 0時間2分後 (mốc cuối) · B: ステップ開始直後 + 経過時間 0時間5分後 · 次のステップ配信 của A = B, hiệu lực từ hôm nay | Tin mốc cuối của A xuất hiện đúng MỘT lần. Ngay sau đó chat 1:1 có tin hệ thống báo bắt đầu kịch bản B kèm tin của mốc 「ステップ開始直後」 của B. 「配信中のステップ」 = B, 「次回配信予定」 trỏ mốc 5 phút của B. Bảng lịch sử: dòng A ở trạng thái hoàn tất, dòng B ở 「配信中」, không dòng A nào kẹt 「配信中」. A: 「読了済の友だち」 tăng đúng 1 và 「購読中の友だち」 giảm đúng 1; B: 「購読中の友だち」 tăng đúng 1 — mỗi cột đổi đúng một lần. Tới mốc 5 phút bạn bè nhận đúng tin của B. Log job không có lỗi trạng thái cũ và không có cảnh báo | | Lấp `G1` · regression — dẫn từ `TC-SCE-342` (kho FA-009) · Đánh giá spec: Spec ghi rõ (job-spec BR-05, feature-spec BR-12) · Evidence: ảnh chat 1:1 + ảnh 3 cột đếm trước/sau + log job |
| TC-JOB001-02 | UI | JOB-001 | Job gửi step & is_last_step | Abnormal | auto | Tất cả | Mốc cuối chuyển sang kịch bản kế tiếp đúng lúc admin bấm 「強制停止」 cho chính bạn bè đó | Giống `TC-JOB001-01` nhưng chuẩn bị sẵn màn chi tiết bạn bè đang mở nút 「強制停止」 ở tab khác. Job chạy ở nhánh đã vá | 1. Ghi lại 3 cột đếm của A và B.<br>2. Chờ mốc cuối của A tới hạn; ngay trong cửa sổ job đang xử lý bạn bè đó, bấm 「強制停止」 ở tab đã mở sẵn.<br>3. Đọc log job cả hai luồng.<br>4. Mở chat 1:1 đếm tin của mốc cuối A và tin báo bắt đầu B.<br>5. Mở màn chi tiết bạn bè đọc 「配信中のステップ」, 「次回配信予定」 và bảng lịch sử.<br>6. Đọc lại 3 cột đếm của A và B.<br>7. Chờ qua mốc 5 phút của B, kiểm có nhận thêm tin nào không | Như `TC-JOB001-01`; thao tác 「強制停止」 thực hiện trong vòng 2 giây sau khi mốc cuối tới hạn | Log job không có lỗi trạng thái cũ thoát ra ngoài. Kết quả cuối nhất quán ở đúng MỘT dạng, không lẫn lộn: hoặc bạn bè đã vào B rồi bị dừng ngay (dòng lịch sử B ở trạng thái dừng, không còn lịch gửi chờ, không nhận tin ở mốc 5 phút), hoặc bạn bè không vào B (không có dòng lịch sử B, 「配信中のステップ」 = 「停止中」). Tin mốc cuối của A xuất hiện tối đa MỘT lần. 3 cột đếm của A và B mỗi cột đổi tối đa một lần, không cột nào bị trừ hai lần và không cột nào âm | | Lấp `G1` · Đánh giá spec: Spec không ghi — hành vi khi stop tay chen vào auto-next chưa có trong job-spec, đã nêu ở §6 · Evidence: log job theo timeline + ảnh bảng lịch sử + ảnh 3 cột đếm trước/sau |
| TC-JOB001-03 | API | JOB-001 | Job gửi step & is_last_step | Abnormal | auto | Tất cả | Lỗi DB KHÁC 1213 khi xóa lịch sau gửi step — phần chạy action và ghi kết quả gửi không được mất | Kịch bản G riêng của lần chạy có 1 mốc tới hạn ngay, mốc này gắn action gắn tag AFTER_SEND. 1 bạn bè test đang nhận G và có lịch gửi của mốc đó. Có cách ép lệnh xóa lịch trả về lỗi MySQL **lock wait timeout 1205** (giữ lock trên chính dòng đó bằng một phiên MySQL thứ hai, hoặc fault injection tương đương). Job chạy ở nhánh đã vá | 1. Ghi lại trạng thái tag AFTER_SEND và số bản ghi lịch sử gửi hiện tại của bạn bè.<br>2. Bật cơ chế ép lỗi 1205 cho lệnh xóa lịch của đúng dòng sắp gửi.<br>3. Để job lấy mốc tới hạn và gửi step.<br>4. Đọc log job theo timeline: ghi lại lỗi nhận được, có retry hay không, số lần thử.<br>5. Mở chat 1:1 đếm số lần tin của mốc xuất hiện.<br>6. Kiểm bảng lịch sử gửi step của mốc đó.<br>7. Kiểm tag AFTER_SEND trên màn chi tiết bạn bè.<br>8. Kiểm hàng đợi lịch gửi của bạn bè còn dòng nào kẹt không.<br>9. Kiểm job có tiếp tục xử lý được mốc tới hạn tiếp theo không | Lỗi ép: MySQL error **1205** (lock wait timeout), khác error 1213 mà bản vá bắt | Tin của mốc xuất hiện đúng MỘT lần. **Tag AFTER_SEND được gắn** và **bảng lịch sử gửi có đúng MỘT bản ghi cho mốc đó** — tức phần việc sau lệnh xóa KHÔNG bị bỏ dở. Log job ghi rõ lỗi 1205 và cách xử lý; nếu bản vá không retry cho mã lỗi này thì log phải cho thấy lỗi được nuốt lại chứ không ném ra ngoài làm chết worker. Worker vẫn xử lý được mốc tới hạn tiếp theo. Không có dòng lịch gửi nào của bạn bè kẹt ở trạng thái đang gửi quá 30 phút | | Lấp `G3` · AP-1/AP-4 — trigger thứ 2 ngoài 1213 · Đánh giá spec: Spec không ghi — chưa có tài liệu nào liệt kê error code nào được retry, đã nêu ở §6 · Evidence: log job theo timeline + ảnh tag AFTER_SEND + ảnh bảng lịch sử gửi |
| TC-JOB001-04 | API | JOB-001 | Job gửi step & is_last_step | Abnormal | auto | Tất cả | Lỗi CHƯA BIẾT TRƯỚC khi xóa lịch sau gửi step — job vẫn chạy tiếp, không mất bạn bè phía sau | Giống `TC-JOB001-03`. Có cách ép lệnh xóa lịch ném một lỗi **không nằm trong danh sách bản vá xử lý** (vd mất kết nối DB 2006/2013, hoặc một RuntimeException bất kỳ). Có 3 bạn bè test cùng tới hạn cùng mốc, ép lỗi chỉ cho bạn bè thứ nhất | 1. Ghi lại danh sách mốc tới hạn của cả 3 bạn bè.<br>2. Bật ép lỗi không xác định cho lệnh xóa lịch của bạn bè thứ nhất.<br>3. Để job xử lý cả 3 mốc.<br>4. Đọc log job: lỗi được ghi ra ở dạng nào, có notify/cảnh báo không, có ném ra ngoài không.<br>5. Kiểm bạn bè thứ hai và thứ ba có nhận tin và được dọn lịch bình thường không.<br>6. Kiểm chat 1:1 của bạn bè thứ nhất đếm số tin của mốc.<br>7. Kiểm job có nhận và xử lý được mốc tới hạn phát sinh sau đó không | Lỗi ép: mất kết nối DB (2006/2013) hoặc RuntimeException, ép đúng 1 lần cho bạn bè thứ nhất | Job **không** chết: bạn bè thứ hai và thứ ba vẫn nhận đủ tin và lịch gửi của họ được dọn sạch. Bạn bè thứ nhất nhận tin của mốc tối đa MỘT lần (không gửi lặp ở vòng job sau). Lỗi được ghi log kèm đủ định danh (bot, bạn bè, dòng lịch) và có cảnh báo theo cơ chế đã cấu hình; **không** có exception thoát ra ngoài làm dừng worker. Job tiếp tục xử lý được mốc tới hạn kế tiếp | | Lấp `G3` · AP-1 — trigger "lỗi chưa biết trước", bắt buộc cho fix dạng chặn lỗi · Đánh giá spec: Spec không ghi, đã nêu ở §6 · Evidence: log job theo timeline + ảnh chat 1:1 của cả 3 bạn bè |
| TC-INTGLINE001-01 | API | INTG-LINE-001 | Job gửi step & is_last_step | Abnormal | auto | Tất cả | Gửi step thất bại phía LINE — dòng lịch gửi vẫn bị dọn, lịch sử ghi trạng thái thất bại, hàng đợi không kẹt | Kịch bản G riêng của lần chạy có 1 mốc tới hạn ngay. 1 bạn bè test đang nhận G và có lịch gửi của mốc đó. Có cách làm lệnh gửi tới LINE thất bại cho đúng bạn bè này (token sai cho bot test, hoặc bạn bè đã chặn bot nhưng hệ thống chưa cập nhật). Job chạy ở nhánh đã vá | 1. Ghi lại số dòng lịch gửi đang chờ của bạn bè và số bản ghi lịch sử gửi hiện có.<br>2. Bật điều kiện làm gửi LINE thất bại cho bạn bè này.<br>3. Để job lấy mốc tới hạn và gửi.<br>4. Đọc log job xem lỗi gửi được ghi thế nào và sau đó lệnh xóa lịch có chạy không.<br>5. Kiểm hàng đợi lịch gửi của bạn bè còn dòng nào của mốc đó không.<br>6. Kiểm bảng lịch sử gửi step trên màn chi tiết bạn bè.<br>7. Kiểm job có tiếp tục xử lý mốc tới hạn tiếp theo không | Điều kiện gây lỗi gửi: token bot sai hoặc bạn bè đã chặn bot | Dòng lịch gửi của mốc đó **biến mất khỏi hàng đợi** (không còn ở bất kỳ trạng thái nào, không kẹt ở đang gửi). Bảng lịch sử gửi có đúng MỘT bản ghi cho mốc đó ở **trạng thái thất bại**. Log job ghi rõ lỗi gửi; không có lỗi trạng thái cũ và không có exception thoát ra ngoài. Bạn bè không nhận tin. Job tiếp tục xử lý được mốc tới hạn kế tiếp | | Lấp `G2` · Đánh giá spec: Spec ghi rõ (job-spec BR-08 — gửi thất bại thì DELETE row + ghi lịch sử status=3) · Evidence: ảnh bảng lịch sử gửi + log job + ảnh hàng đợi trước/sau |
| TC-MSG001-01 | UI | MSG-001 | Job gửi step & is_last_step | Abnormal | auto | Tất cả | Bạn bè không thỏa điều kiện lọc của mốc — dòng lịch gửi được dọn, bạn bè khác cùng lô vẫn nhận đủ | Kịch bản F riêng của lần chạy có 2 nhánh 「配信対象」: nhánh 1 lọc theo tag T-IN, nhánh 2 không điều kiện. Mốc của nhánh 1 đặt tới hạn ngay. Có 2 bạn bè test đang nhận F: bạn bè X **có** tag T-IN, bạn bè Y **không** có tag T-IN. Cả hai đều có dòng lịch gửi của mốc đó. Job chạy ở nhánh đã vá | 1. Ghi lại số dòng lịch gửi đang chờ của X và Y.<br>2. Chờ mốc tới hạn để job xử lý cả hai.<br>3. Mở chat 1:1 của X và của Y, kiểm tin của mốc.<br>4. Kiểm hàng đợi lịch gửi của X và Y.<br>5. Kiểm bảng lịch sử gửi step của cả hai trên màn chi tiết bạn bè.<br>6. Kiểm 3 cột đếm của kịch bản F.<br>7. Đọc log job | Tag T-IN chỉ gắn cho bạn bè X | Bạn bè X nhận tin của mốc đúng MỘT lần. Bạn bè Y **không** nhận tin, và dòng lịch gửi của Y cho mốc đó **biến mất khỏi hàng đợi** chứ không kẹt lại. Không có lỗi trạng thái cũ trong log job và không có cảnh báo. 3 cột đếm của F không cột nào bị trừ hai lần. Các mốc sau của X và Y vẫn còn nguyên trong hàng đợi với đúng thời điểm hẹn | | Lấp `G4` · regression — dẫn từ `TC-SCE-343` (kho FA-009) · Đánh giá spec: Spec ghi rõ (job-spec State Machine — `STATUS_SKIPPED_BY_FILTER`, feature-spec BR-06) · Evidence: ảnh chat 1:1 của cả X và Y + ảnh hàng đợi trước/sau + log job |
| TC-MSG001-02 | API | MSG-001 | Job gửi step & is_last_step | Boundary | auto | Tất cả | Bot hết hạn gói quá 7 ngày — dòng lịch gửi được bỏ qua và dọn sạch, không kẹt hàng đợi | Bot test có thể đưa về trạng thái hết hạn gói quá 7 ngày. Kịch bản H riêng của lần chạy có 1 mốc tới hạn ngay, 1 bạn bè test đang nhận H và có dòng lịch gửi của mốc đó. Job chạy ở nhánh đã vá | 1. Ghi lại số dòng lịch gửi đang chờ của bạn bè và trạng thái gói của bot.<br>2. Đưa bot về trạng thái hết hạn gói quá 7 ngày.<br>3. Chờ mốc tới hạn để job xử lý.<br>4. Mở chat 1:1 kiểm tin của mốc.<br>5. Kiểm hàng đợi lịch gửi của bạn bè.<br>6. Đọc log job.<br>7. Khôi phục gói bot về trạng thái ban đầu và kiểm mốc kế tiếp có được xử lý bình thường không | Trạng thái gói bot: hết hạn > 7 ngày | Bạn bè **không** nhận tin. Dòng lịch gửi của mốc đó **biến mất khỏi hàng đợi**, không kẹt ở trạng thái đang gửi. Log job ghi rõ lý do bỏ qua vì hết hạn gói, không có lỗi trạng thái cũ và không có exception. Sau khi khôi phục gói, mốc kế tiếp được gửi bình thường | | Lấp `G4` · Đánh giá spec: Spec ghi rõ (job-spec State Machine — `STATUS_SKIPPED_BY_BOT_EXPIRED_PLAN`) · Evidence: ảnh hàng đợi trước/sau + log job + ảnh trạng thái gói bot |
| TC-OUTPREVIEW001-01 | UI | OUT-PREVIEW-001 | Job gửi step & is_last_step | Abnormal | auto | Tất cả | テスト配信 đang chạy đúng lúc dừng kịch bản cho chính bạn bè đó — không kẹt lịch, không mất phần dọn | Kịch bản T riêng của lần chạy có 3 mốc. 1 bạn bè test đang nhận kịch bản T và có lịch gửi chờ. Màn soạn kịch bản mở sẵn chức năng 「テスト配信」 với tùy chọn gửi toàn bộ tin nhắn. Mở sẵn màn chi tiết bạn bè ở tab khác để bấm 「強制停止」. Job chạy ở nhánh đã vá | 1. Ghi lại số dòng lịch gửi đang chờ của bạn bè và 3 cột đếm của T.<br>2. Chạy 「テスト配信」 gửi toàn bộ tin nhắn cho chính bạn bè test.<br>3. Trong lúc テスト配信 đang chạy, bấm 「強制停止」 ở tab đã mở sẵn.<br>4. Đọc log job cả hai luồng.<br>5. Mở chat 1:1 đếm tin nhận được.<br>6. Kiểm hàng đợi lịch gửi của bạn bè.<br>7. Kiểm bảng lịch sử gửi step và bảng lịch sử kịch bản.<br>8. Đọc lại 3 cột đếm của T | Tùy chọn テスト配信: 「全てのメッセージを同時に送信」 | Log job không có lỗi trạng thái cũ ở cả luồng テスト配信 lẫn luồng dừng kịch bản. Sau khi cả hai xử lý xong, bạn bè **không còn dòng lịch gửi nào ở bất kỳ trạng thái nào** (không có dòng kẹt ở đang gửi). Bảng lịch sử kịch bản có dòng T ở trạng thái dừng, không kẹt 「配信中」. 3 cột đếm của T mỗi cột đổi tối đa một lần. Tin テスト配信 không bị nhân đôi trong chat 1:1 | | Lấp `G5` · regression — dẫn từ `TC-SCE-358` (kho FA-009) · Đánh giá spec: Spec ghi rõ (job-spec BR-04 — `TEST_STEP_MESSAGE_ALL` không ghi lịch sử và tự chuyển step tiếp); ⚠️ kho FA-009 `MT-31` còn để ngỏ cách tạo bản ghi của option này · Evidence: log job theo timeline + ảnh chat 1:1 + ảnh hàng đợi sau khi xong |
| TC-DATAREF001-01 | UI | DATA-REF-001 | Job khi edit step đang chạy | Abnormal | auto | Tất cả | Xóa step / xóa 配信対象 trên màn admin đúng lúc job đang gửi mốc đó cho bạn bè | Kịch bản E riêng của lần chạy có 2 nhánh 「配信対象」, nhánh 1 có 2 mốc trong đó 1 mốc đặt tới hạn ngay. Có 3 bạn bè test đang nhận E, cả 3 đều có lịch gửi của mốc tới hạn. Mở sẵn màn soạn kịch bản E ở tab khác. Job chạy ở nhánh đã vá | 1. Ghi lại số dòng lịch gửi đang chờ của cả 3 bạn bè và 3 cột đếm của E.<br>2. Chờ mốc tới hạn để job bắt đầu gửi.<br>3. Ngay trong cửa sổ job đang gửi, ở tab đã mở sẵn thực hiện xóa nhánh 「配信対象」 số 1 (kéo theo xóa các mốc của nhánh đó).<br>4. Đọc log job.<br>5. Mở chat 1:1 của cả 3 bạn bè đếm tin của mốc.<br>6. Kiểm hàng đợi lịch gửi của cả 3 bạn bè.<br>7. Kiểm bảng lịch sử kịch bản trên màn chi tiết từng bạn bè.<br>8. Đọc lại 3 cột đếm của E | Thao tác xóa nhánh 「配信対象」 thực hiện trong vòng 5 giây kể từ khi mốc tới hạn | Log job **không** có lỗi trạng thái cũ và không có exception thoát ra ngoài. Mỗi bạn bè nhận tin của mốc đó tối đa MỘT lần, không ai nhận hai lần. Sau khi cả hai bên xong, **không còn dòng lịch gửi nào của nhánh đã xóa** ở bất kỳ trạng thái nào, và **không có dòng nào kẹt ở trạng thái đang gửi**. Các mốc thuộc nhánh 2 vẫn còn nguyên với đúng thời điểm hẹn. 3 cột đếm của E không cột nào bị trừ hai lần và không cột nào âm | | Lấp `G6` · regression — dẫn từ `TC-SCE-370`/`TC-SCE-371` (kho FA-009) · ⚠️ expected cuối phụ thuộc `MT-30` của kho (xóa step khi đang chạy thì dòng lịch bị XÓA hay CÒN) — đã nêu ở §6 · Evidence: log job theo timeline + ảnh hàng đợi trước/sau + ảnh chat 1:1 của 3 bạn bè |
| TC-CONC001-01 | UI | CONC-001 | Job gửi step & is_last_step | Normal | auto | Tất cả | Hai thao tác đồng thời trên HAI bạn bè khác nhau — cả hai đều hoàn tất trọn vẹn | Kịch bản A và kịch bản B riêng của lần chạy, mỗi cái có 2 mốc. Bạn bè M1 đang nhận A và có lịch gửi chờ; bạn bè M2 chưa vào kịch bản nào. Chuẩn bị 2 phiên thao tác để dừng A cho M1 và bắt đầu B cho M2 **cùng thời điểm**. Job chạy ở nhánh đã vá | 1. Ghi lại 3 cột đếm của A và B.<br>2. Thực hiện đồng thời: dừng kịch bản A cho M1 và bắt đầu kịch bản B cho M2.<br>3. Đọc log job cả hai luồng.<br>4. Mở chat 1:1 của M1 và M2.<br>5. Kiểm hàng đợi lịch gửi của M1 và M2.<br>6. Mở màn chi tiết của M1 và M2 đọc 「配信中のステップ」, 「次回配信予定」 và bảng lịch sử.<br>7. Đọc lại 3 cột đếm của A và B | Hai thao tác cách nhau không quá 1 giây | **Cả hai thao tác đều hoàn tất**, không thao tác nào bị bỏ dở: M1 không còn dòng lịch gửi nào của A, 「配信中のステップ」 = 「停止中」, dòng lịch sử A ở trạng thái dừng; M2 có lịch gửi mới của B, 「配信中のステップ」 = B, dòng lịch sử B ở 「配信中」 và chat 1:1 có tin báo bắt đầu B. A: 「購読中の友だち」 giảm đúng 1 và 「途中で終了した友だち」 tăng đúng 1; B: 「購読中の友だち」 tăng đúng 1. Log job sạch, không có lỗi trạng thái cũ | | Lấp `Q4` — bổ sung loại case `Normal` còn thiếu của CONC-001 (RULE-01); đây là **case đối chứng dương**: 2 luồng đồng thời nhưng không đụng cùng bạn bè thì cả hai phải chạy trọn · Đánh giá spec: Spec ghi rõ · Evidence: ảnh 3 cột đếm trước/sau + ảnh màn chi tiết của cả M1 và M2 + log job |
| TC-MSG005-01 | API | MSG-005 | Job gửi step & is_last_step | Abnormal | auto | Tất cả | Bot hết quota tin nhắn khi tới mốc gửi — action kèm theo và phần dọn lịch vẫn chạy | Bot test có thể đưa về trạng thái hết quota gửi tin. Kịch bản Q riêng của lần chạy có 1 mốc tới hạn ngay, mốc này gắn action gắn tag AFTER_SEND. 1 bạn bè test đang nhận Q và có lịch gửi của mốc đó. Job chạy ở nhánh đã vá | 1. Ghi lại quota còn lại của bot, trạng thái tag AFTER_SEND và số dòng lịch gửi đang chờ.<br>2. Đưa bot về trạng thái hết quota.<br>3. Chờ mốc tới hạn để job xử lý.<br>4. Đọc log job xem quota được xử lý thế nào và lệnh xóa lịch có chạy không.<br>5. Kiểm tag AFTER_SEND trên màn chi tiết bạn bè.<br>6. Kiểm hàng đợi lịch gửi của bạn bè.<br>7. Kiểm bảng lịch sử gửi step.<br>8. Khôi phục quota và kiểm mốc kế tiếp chạy bình thường | Quota gửi của bot = 0 | Dòng lịch gửi của mốc đó **biến mất khỏi hàng đợi**, không kẹt ở trạng thái đang gửi. Log job ghi rõ tình trạng hết quota và **không** có lỗi trạng thái cũ, không có exception thoát ra ngoài. Việc tag AFTER_SEND có được gắn hay không phải **nhất quán với quy tắc quota đã chốt** và được ghi rõ trong log — không được ở trạng thái lửng (log báo đã chạy action nhưng tag không có). Sau khi khôi phục quota, mốc kế tiếp gửi bình thường | | Lấp `Q2` · ⚠️ job-spec BR-06 ghi mức tin cậy **Trung bình** cho quy tắc quota ↔ action kèm theo → hỏi Dev chốt expected trước khi chạy, đã nêu ở §6 · Đánh giá spec: Đã hỏi leader · Evidence: log job + ảnh tag AFTER_SEND + ảnh quota trước/sau |
| TC-PERFLARGE001-01 | API | PERF-LARGE-001 | Job gửi step & is_last_step | Boundary | manual | product | Gửi hàng loạt cùng một mốc phút sau khi thêm retry — độ trễ và thông lượng không xấu đi so với trước bản vá | Môi trường **production** sau khi deploy bản vá. Có số liệu đối chứng của một lần gửi hàng loạt **trước** bản vá ở cùng quy mô (thời điểm bắt đầu/kết thúc lô, số bạn bè, số tin/phút) lấy từ log job. Chọn một kịch bản thật có lô gửi đủ lớn ở cùng khung giờ | 1. Ghi lại mốc đối chứng trước bản vá: số bạn bè trong lô, thời điểm tin đầu và tin cuối được gửi.<br>2. Theo dõi lô gửi tương đương sau bản vá tại cùng khung giờ.<br>3. Đọc log job ghi lại thời điểm tin đầu và tin cuối của lô, đếm số bạn bè đã gửi.<br>4. Đếm số lần retry xóa lịch xuất hiện trong log của lô đó.<br>5. Kiểm hàng đợi còn dòng nào của lô kẹt ở trạng thái đang gửi không.<br>6. Đối chiếu số bạn bè nhận tin với số bạn bè trong lô.<br>7. So thời gian hoàn tất lô sau bản vá với mốc đối chứng | Lô gửi quy mô tương đương mốc đối chứng, cùng khung giờ trong ngày | Toàn bộ bạn bè trong lô nhận đủ tin, không ai thiếu và không ai nhận hai lần. **Thời gian hoàn tất lô sau bản vá không dài hơn mốc đối chứng quá 20%**; nếu dài hơn, log phải cho thấy nguyên nhân là số lần retry thực tế chứ không phải sleep chạy trên đường không có lỗi. Số lần retry ghi nhận được phải tương ứng với số lỗi thật, không có retry thừa. Sau khi lô xong, không còn dòng nào kẹt ở trạng thái đang gửi | | Lấp `G7` + `Q3` · **RULE-08** — hiệu năng job nền không kết luận được từ local · manual vì môi trường production với dữ liệu khách hàng thật · Đánh giá spec: Spec không ghi — chưa có ngưỡng thông lượng nào trong job-spec, đã nêu ở §6 · Evidence: log job 2 lô (trước/sau) + bảng so thời gian |
| TC-ENV003-01 | UI | ENV-003 | Job gửi step & is_last_step | Normal | manual | product | Smoke trên production sau deploy — bật, dừng, đổi kịch bản và hủy kết bạn đều chạy trọn vẹn | Môi trường **production** sau khi deploy bản vá. Dùng bot nội bộ trên production (không dùng bot khách hàng). Có 2 kịch bản nội bộ riêng của lần smoke, mỗi cái 2 mốc. Có 1 tài khoản LINE thật đã kết bạn với bot nội bộ | 1. Ghi lại 3 cột đếm của cả 2 kịch bản.<br>2. Bật kịch bản 1 cho bạn bè test, kiểm chat 1:1 có tin báo bắt đầu và tin của mốc đầu.<br>3. Đổi sang kịch bản 2, kiểm 「配信中のステップ」 và 「次回配信予定」 trên màn chi tiết bạn bè.<br>4. Cho bạn bè hủy kết bạn từ ứng dụng LINE, kiểm hàng đợi lịch gửi và bảng lịch sử.<br>5. Kết bạn lại, kiểm thiết lập 「友だち追加時」 chạy đúng.<br>6. Đọc log job production trong khoảng thời gian smoke.<br>7. Đọc lại 3 cột đếm của cả 2 kịch bản | Tài khoản LINE thật, bot nội bộ trên production | Không có lỗi trạng thái cũ (`StaleStateException`) trong log job production suốt khoảng smoke — đây là tiêu chí chính. Mỗi bước bật/đổi/hủy đều hoàn tất trọn vẹn: có tin báo bắt đầu đúng kịch bản, 「配信中のステップ」 và bảng lịch sử khớp thao tác vừa làm, sau khi hủy kết bạn không còn dòng lịch gửi chờ. 3 cột đếm của cả 2 kịch bản đổi đúng một lần cho mỗi thao tác. Kết bạn lại chạy đúng thiết lập 「友だち追加時」 | | Lấp `Q2`(§4 I2) · **RULE-08 + RULE-06** — bug phát sinh trên production, 0/40 TC gốc chạy prd · manual vì môi trường production và phải quan sát trên ứng dụng LINE thật · Đánh giá spec: Spec ghi rõ · Evidence: log job production + ảnh chat 1:1 trên LINE thật + ảnh 3 cột đếm trước/sau |
| TC-DATACOUNT001-01 | Data | DATA-COUNT-001 | Màn list scenario | Normal | manual | product | Thống kê toàn hệ thống các kịch bản có số đếm lệch do bug cũ và chốt phạm vi khôi phục | Môi trường **production**. Có quyền chạy thống kê đối chiếu 3 cột đếm của kịch bản với số bạn bè đếm thực tế theo trạng thái. Đã biết mốc thời gian deploy bản vá và mốc các lần lỗi trên log prod (18/09) | 1. Chạy thống kê toàn hệ thống liệt kê mọi kịch bản có 3 cột đếm lệch so với số bạn bè đếm thực tế, kèm bot và mức lệch.<br>2. Chia danh sách theo mốc deploy bản vá: lệch phát sinh **trước** và **sau** mốc đó.<br>3. Với nhóm sau deploy, mở từng kịch bản trên màn 「ステップ配信」 và đối chiếu số đếm với danh sách bạn bè mở ra từ chính số đó.<br>4. Liệt kê các bạn bè kẹt trạng thái đang nhận nhưng không còn lịch gửi chờ.<br>5. Lập danh sách bot/kịch bản cần khôi phục kèm mức lệch, gửi Leader chốt.<br>6. Lặp lại thống kê sau 7 ngày kể từ deploy | Mốc so sánh: thời điểm deploy bản vá; khoảng quét: toàn bộ bot đang hoạt động | Có **danh sách đầy đủ** kịch bản/bot đang lệch kèm mức lệch và phân loại trước/sau deploy. **Nhóm lệch phát sinh SAU deploy phải bằng 0** — nếu khác 0 thì bản vá chưa chặn hết đường sinh lệch, phải báo lại Dev. Nhóm lệch trước deploy được chốt thành danh sách khôi phục riêng có người phụ trách. Lần quét lại sau 7 ngày không phát sinh thêm kịch bản lệch mới | | Lấp `Q5` (RULE-04) · manual vì chạy trên dữ liệu khách hàng thật ở production · Đánh giá spec: Spec ghi rõ (feature-spec BR-02 — counter là cached, không JOIN realtime) · Evidence: file danh sách kịch bản lệch (2 lần quét) + ảnh màn 「ステップ配信」 của vài case đối chứng |

**Dòng `GAP`/`RISK` không viết TC mới:**

- `Q1` (JOB-001 mã lạ) — không phải thiếu test mà là sai nhãn; xử lý bằng `testcase_update` trên Studio, xem §4 I4.
- `Q6` (DATA-COUNT-001 thiếu loại case) — **không viết TC mới**: nội dung `Normal` đã nằm ở `NEW-26` và chiều đối chứng cộng/trừ một lần ở `NEW-23`. Chỉ cần đổi `Mã quan điểm liên kết` của 2 TC này về `DATA-COUNT-001` (hoặc ghi mã phụ ở `note`) là đủ 3 loại case.
- Hệ quả "xóa 0 row im lặng" sinh thêm bạn bè kẹt `is_following = 1` mà không còn lịch gửi — **dùng lại `TC-SCE-349` + `TC-SCE-350`** của kho FA-009, chạy nguyên trạng, không viết TC mới.

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | `spec-features/admin/scenario/job/job-spec.md` §Error Handling | Bổ sung hành vi MỚI sau bản vá: (a) xóa `scenario_step_time` **0 row = thành công**, không ném exception; (b) chính sách retry của `deleteById` — **error code nào được retry** (hiện chỉ 1213), số lần tối đa, công thức backoff, hành vi khi hết retry. Không có mục này thì `TC-JOB001-03` / `TC-JOB001-04` không có chuẩn để chấm Đạt | Journal Dev #137276 mục 2 vs job-spec §Error Handling hiện tại (không đề cập) | `Dev` |
| 2 | `feature-spec.md §6 scenario_lineuser.is_following` + `§7 BR-02` | **`MT-02` của kho FA-009 (CAO, ⏳ CHỜ QUYẾT ĐỊNH)**: bạn bè chặn bot / hủy kết bạn giữa chừng kịch bản thì vào cột 「読了済」 hay 「途中で終了」. `NEW-13`, `NEW-14`, `NEW-18` đang chốt cứng "「途中で終了した友だち」 tăng 1" trong khi không spec nào mô tả | `NEW-13`/`NEW-14`/`NEW-18` vs `kho-tcs/fa009` `MT-02` | `Leader` + `PM` |
| 3 | `feature-spec.md §6 scenario_lineuser.is_following` | **`MT-01` của kho FA-009 (CAO, ⏳ CHỜ QUYẾT ĐỊNH)**: `is_following = 0` và `= 2` ứng với counter nào (`count_stop` hay `count_unfinish`). Toàn bộ expected 3 cột đếm của `NEW-1`/`NEW-6`/`NEW-23`/`NEW-27`/`NEW-28` phụ thuộc câu trả lời này | Kho FA-009 `MT-01` | `Leader` |
| 4 | `feature-spec.md §7 BR-02` (counter cached) | **`MT-37` của kho FA-009 (CAO)**: counter phía JOB lệch phía WEB ở ít nhất 3 điểm. Đây đúng là vùng bug này gây lệch trên prod → cần chốt nguồn sự thật của 3 cột đếm và quy tắc khôi phục khi lệch (**RULE-04**, xem `TC-DATACOUNT001-01`) | Kho FA-009 `MT-37` + journal Dev (count bị trừ 2 lần) | `Leader` + `Dev` |
| 5 | `feature-spec.md §7 BR-06` (filter phân nhánh) / `job-spec.md` | **`MT-30` của kho FA-009 (TRUNG BÌNH)**: xóa step hoặc 一括消去 khi kịch bản đang chạy thì `scenario_step_time` bị XÓA hay CÒN nhận. Expected của `TC-DATAREF001-01` (§5) phụ thuộc câu trả lời này | Kho FA-009 `MT-30` | `Dev` |
| 6 | `job-spec.md §Business Rules BR-06` (quota) | BR-06 tự ghi mức tin cậy **Trung bình** — cần chốt: hết quota thì action kèm theo của step **có chạy hay không**. Đây chính là phần mà fix `a83d475` bảo vệ khỏi bị mất, nên expected của `TC-MSG005-01` không thể tự suy | job-spec BR-06 (tin cậy Trung bình) vs mục 2 journal Dev | `Dev` |
| 7 | `job-spec.md §2.2` (200 worker) | Chưa có **ngưỡng thông lượng / độ trễ** nào cho lô gửi cùng mốc phút → không có chuẩn để chấm `TC-PERFLARGE001-01`. Tạm dùng mốc đối chứng trước/sau bản vá; cần Leader chốt ngưỡng chính thức | Studio `dev_impact` rủi ro hồi quy 2 vs job-spec (không có ngưỡng) | `Leader` + `Dev` |
