# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | **MCP LME TEST STUDIO** — task #265 (ticket 39467), `round 1`, `status done-ai`, `reviewState leader`, branch `ai_fixbug_39467` |
| Tổng số TC review | **16** |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận: 13 vùng ảnh hưởng đã rà · 5 đủ TC · 6 GAP · 2 RISK.**

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | **F7 — nhóm route ĐỌC cấu hình bị cố ý giữ nguyên**: EP-02 `GET /ajax/basic/notify-setting` (trả về **toàn bộ** `notify_setting`, spec §9 ghi rõ *"EP-12 API token trả lại trong response body — rủi ro bảo mật"*) và EP-10 `GET /basic/notify-setting-chatwork` (render trang ChatWork) | dev-impact | **0 TC** | Bug là "quyền màn 通知設定 không được enforce", nhưng fix chỉ chặn **GHI**. Không TC nào kiểm staff KHÔNG quyền có **ĐỌC** được URL phòng + **API token ChatWork** của bot qua EP-02 / EP-10 hay không. TC-PERM002-02 (NEW-6) còn dùng chính đường `/basic/notify-setting-chatwork` để mở màn mà **không assert gì** về việc màn này có được phép mở | `[BLOCKER]` |
| G2 | **F4 — EP-14 `test-send-chatwork`, nhánh CÓ quyền (gửi thật)** | dev-impact | TC-OUTTRUTH001-01 (NEW-3), TC-OUTTRUTH001-02 (NEW-22) | Cả 2 TC đều **`Chưa test`** (`exec_mode = manual`, `last_exec = null`). Nhánh 403 (TC-OUTTRUTH001-03) chỉ chứng minh **không** gọi ChatWork; **không có bằng chứng nào** cho việc người có quyền vẫn gửi thật được sau khi thêm guard ⇒ rủi ro over-block EP-14 chưa được loại trừ | `[MAJOR]` |
| G3 | **F6 — nhánh `admin_id` override của `userHasScreenAccess`** | dev-impact | **0 TC** | Dev khẳng định *"Chủ bot (admin_id) luôn qua guard nên luồng bình thường không đổi"*. Mọi TC "chủ bot" hiện có đều dùng chủ bot **không gắn nhóm quyền hạn chế** ⇒ chưa TC nào ép đúng nhánh biên: tài khoản **là `admin_id` của bot nhưng nhóm quyền đang gán KHÔNG có màn 通知設定** | `[MAJOR]` |
| G4 | **D2 — oracle tầng DB dựa trên giả định sai mô hình dữ liệu** | dev-impact | 13/16 TC | 13 TC dùng oracle *"bản ghi cấu hình thông báo của **cặp (bot, staff)**"*. Spec **BR-11** ghi: *"Mỗi bot chỉ có 1 record trong `notify_setting`. Query luôn dùng `WHERE bot_id = ? LIMIT 1`"*, và `db-mapping.md:49` ghi `user_id` = *"ID user **tạo record**"*, không phải khóa phân tách. Nếu BR-11 đúng ⇒ staff không quyền ghi đè **chính cấu hình ChatWork của chủ bot** (mức nghiêm trọng cao hơn hẳn ticket mô tả), và oracle *"bản ghi của cặp (bot, staff) không đổi"* đang kiểm **sai hàng dữ liệu**. **KHÔNG tự chốt bên** → §6 | `[BLOCKER]` |
| G5 | **Nhánh Abnormal của chính 4 hàm vừa sửa** — người **CÓ** quyền nhập URL phòng / API token **sai định dạng** | diff code | **0 TC** | Log của ticket cho thấy EP-12 có nhánh validate (`200` + 「ChatworkAPIトークンが正しくありません。」) và TC-PERM002-03 (NEW-13) ghi chú EP-11 có nhánh 「ChatworkルームURLが正しくありません。」. Guard mới chèn **trước** nhánh validate ⇒ phải verify nhánh validate cũ không hỏng. Spec §9 còn ghi *"EP-11 Không validate URL room ChatWork (format, domain)"* ⇒ hành vi này chưa được chốt | `[BLOCKER]` |
| G6 | **Pattern gốc "route `/ajax/*` nằm ngoài nhóm middleware kiểm quyền theo màn" ở các màn KHÁC** | diff code | **0 TC** | Dev nguyên văn: *"Quét ngang **trong cùng màn**"* — mới rà bên trong `NotifySettingController`. `spec_delta` xác nhận diff **chỉ 1 file / +16 dòng**. Nguyên nhân gốc nằm ở `routes/web.php:3937-3940` (nhóm route ajax không qua middleware theo màn), là **lỗi cấu hình route**, không riêng màn này. Chưa có danh sách các màn khác dùng cùng pattern, cũng chưa TC nào rà ngang | `[MAJOR]` |
| G7 | **REQ-008 — log truy vết lần thử ghi trái phép** | dev-impact | **0 TC** | Studio khai REQ-008 (*"guard đặt SAU `addLogUserAction` ⇒ mỗi lần staff không quyền thử ghi phải để lại dòng log kèm danh tính + dòng từ chối quyền"*) nhưng **không TC nào tham chiếu REQ-008**. Thêm rủi ro kèm theo: vì log ghi **trước** guard, payload có thể chứa **API token ChatWork** ⇒ token vào server log | `[MAJOR]` |
| G8 | **REQ-007 — dữ liệu cấu hình đã tồn tại không bị bản fix sửa/xoá** | dev-impact | **0 TC** | Dev ghi *"Cấu hình đã bị nhân viên không có quyền ghi trước đó (nếu có) vẫn giữ nguyên, chủ bot tự chỉnh lại trên màn"*. Không TC nào dựng dữ liệu **trước** deploy rồi verify **sau** deploy | `[MAJOR]` |

> Vùng đã đủ TC (không liệt kê chi tiết): **F1** EP-11 · **F2** EP-12 · **F3** EP-13 · **F5** 3 route lưu cài đặt đã có guard từ trước · **BUG** tái hiện ticket gốc (TC-TOOLKNOW002-01).
> **Không có TC orphan** — 16/16 TC đều map được về ít nhất 1 vùng ảnh hưởng.

---

## 2. Thiếu so với quan điểm test

**Kết luận: 13 quan điểm Trigger khớp · 2 đủ · 5 GAP · 5 RISK.**

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `SEC-002` — Không lộ token / mật khẩu | **Cao** | **GAP.** Trigger khớp rõ: chức năng xử lý **API token ChatWork** = credential. **0 TC mang mã này.** TC-PERM002-04 (NEW-14) chỉ kiểm *response* không chứa token — thiếu 3 chiều bắt buộc của quan điểm: (1) quét **server log** (liên quan G7 — guard đặt sau `addLogUserAction`); (2) quét **console DevTools / network** khi màn ChatWork load (EP-02 trả token — spec §9); (3) chủ động **tạo lỗi** rồi quét màn lỗi | `[BLOCKER]` |
| Q2 | `PERM-001` — Menu/nút hiện đúng theo role | **Cao** | **GAP.** Trigger: *"BẮT BUỘC khi chức năng có phân biệt quyền"*. Quan điểm yêu cầu **phải có role matrix trong spec trước khi test** — spec FA-006 §1 Actors ghi Staff: *"Tùy role — **chưa xác minh** rõ Staff có được phép truy cập 通知設定 không (Confidence: **Thấp**)"*, §9 G-07 lặp lại. **0 TC** kiểm menu/nút hiện-ẩn theo role, cũng không có ma trận role. Studio cố ý bỏ PERM-001 (ghi chú NEW-2) nhưng lý do đó chỉ hợp lệ cho *từng TC*, không xoá được trigger của quan điểm | `[BLOCKER]` |
| Q3 | `DATA-AUDIT-001` — Audit log / Lịch sử thao tác | **Cao** | **GAP.** Trigger: thao tác trên dữ liệu nhạy cảm nhóm **phân quyền**. Trùng khớp REQ-008 của chính Studio. **0 TC.** Chưa verify: mỗi thao tác bị từ chối để lại **đúng 1** bản ghi lịch sử, đủ 4 thông tin (người / thời gian / hành động / giá trị cũ→mới), và **không** bị ghi nhầm thành thao tác thành công | `[BLOCKER]` |
| Q4 | `FUNC-003` — Nhập dữ liệu sai định dạng | **Cao** (nâng từ Trung bình: field là **URL** + **token**) | **GAP.** **0 TC** cho nhánh sai định dạng ở phía người **CÓ** quyền sau fix. Quan điểm nêu đúng lỗi kinh điển: *"Frontend chặn được nhưng gọi thẳng API vẫn lưu được"* — nhóm `/ajax/*` này lại **miễn CSRF** (ghi chú NEW-13) nên rất dễ dính. Catalog `DI-05` (URL) còn yêu cầu: thiếu scheme, `javascript:`, khoảng trắng, có/không dấu `/` cuối, URL dài sát giới hạn | `[BLOCKER]` |
| Q5 | `DATA-DB-001` ★ — Xác minh tầng DB (WHERE scope) | **Cao** | **RISK → BLOCKER.** Quan điểm bắt buộc *"tạo bản ghi trùng ở **2 tài khoản** (2 bot, **2 staff**) → update ở A, xác nhận B không đổi"*. TC-PERM003-01 (NEW-18) chỉ làm **2 bot**; **không TC nào** làm **2 người dùng trên CÙNG 1 bot** — đúng chỗ mâu thuẫn BR-11 ở G4. TC-DATADB001-01 (NEW-19) chỉ có Boundary, thiếu Normal + Abnormal (RULE-01, không ghi lý do) | `[BLOCKER]` |
| Q6 | `ENV-003` ★ / RULE-08 — Khác biệt dev / staging / production | **Cao** | **GAP.** Trigger: tính năng chạm **URL/domain** bên ngoài (`api.chatwork.com`, URL phòng `chatwork.com/#!rid…`). `envAuto` của task: **local 1 run · dev 0 · staging 0 · prd 0** ⇒ **16/16 TC chỉ chạy local**. 10/16 TC còn cố tình đặt `env_scope` loại `prd`. Quan điểm ghi thẳng: *"Không được đánh × với lý do 'staging đã pass'"* — ở đây thậm chí chưa từng chạy staging. Chưa rà `ENV-LB` (production 2 server) | `[MAJOR]` |
| Q7 | `PERM-002` — Không bypass quyền bằng API / URL trực tiếp | **Cao** | **RISK.** Có 4 TC (1 Normal + 3 Abnormal) nhưng **thiếu Boundary** và không ghi lý do → RULE-01. Catalog `MAP-PERM-02` liệt kê 4 đường truy cập khi staff không có quyền: menu chính · **menu favourite** · gõ thẳng URL · **hyperlink dẫn tới màn đó** — TC hiện chỉ phủ "gõ thẳng URL" + gọi thẳng API | `[MAJOR]` |
| Q8 | `PERM-003` — Cách ly dữ liệu đa tài khoản LINE OA | **Cao** | **RISK.** Chỉ 1 TC (NEW-18, Abnormal) — thiếu Normal + Boundary. Catalog `MAP-PERM-01` yêu cầu 4 kiểu tài khoản; TC mới phủ "staff của nhiều bot", **thiếu** kiểu *"vừa là owner bot A vừa là staff bot B"* — đúng kiểu tài khoản dễ làm guard tính nhầm quyền theo tài khoản thay vì theo bot đang chọn | `[MAJOR]` |
| Q9 | `OUT-TRUTH-001` + `OUT-PREVIEW-001` — UI/message khớp trạng thái thật · test-send vs output thật | **Cao** | **RISK.** (1) 2/3 TC của nhóm này **chưa chạy** (NEW-3, NEW-22) ⇒ nhánh "gửi thật tới phòng ChatWork" hoàn toàn chưa có bằng chứng — vi phạm đúng tinh thần OUT-PREVIEW-001. (2) Dev tự khai rủi ro *"staff bị thu hồi quyền sẽ thấy thao tác lưu ChatWork **im lặng không có thông báo**"* và **cố ý không sửa FE**. TC-PERM004-01 (NEW-8) chỉ assert *"không hiện 保存しました。"* — **không** assert người dùng có được báo lỗi hay không ⇒ hành vi vi phạm OUT-TRUTH-001 đang được TC ngầm chấp nhận | `[MAJOR]` |
| Q10 | `REG-SHARED-001` — Shared code / logic | **Cao** | **RISK.** Có NEW-20 (Normal) + NEW-21 (Abnormal) nhưng phạm vi chỉ trong **cùng controller**. Quan điểm yêu cầu *"**dev cung cấp danh sách** nơi ảnh hưởng → test lại từng mục"* và *"fix bug ở chức năng A → rà chức năng B có logic tương tự"* — mục 3 file 03 chỉ liệt kê trong cùng màn, **không có danh sách màn khác** (xem G6). Thiếu Boundary | `[MAJOR]` |

> Quan điểm Trigger khớp và **đã đủ**: `FUNC-001` · `PERM-004`.

---

## 3. TC trùng lặp nội dung

**Đã rà toàn bộ 16 TC theo 4 yếu tố (mã quan điểm × loại case × đối tượng + thao tác × tiền đề tương đương) — KHÔNG phát hiện TC trùng lặp cần xoá hoặc gộp.**

Hai cụm **chồng lấn có chủ đích**, đã kiểm gate "xoá đi có mất cover không" → **giữ nguyên cả hai**:

| Cụm chồng lấn | Vì sao KHÔNG phải trùng |
|---|---|
| TC-PERM002-02 (NEW-6) ↔ TC-TOOLKNOW002-01 (NEW-15) — cùng EP-13, cùng staff không quyền, cùng oracle 403 + DB không đổi | Khác **tầng kiểm chứng**: NEW-6 đi qua UI (radio tự lưu) và phủ **cả 3 lựa chọn** kiểu nhận thông báo + verify màn revert sau reload; NEW-15 gọi thẳng API và có bước chứng minh role thực sự không có quyền — là TC **tái hiện đúng bug ticket**. Xoá bên nào cũng mất cover |
| TC-PERM002-03/-04 + TC-TOOLKNOW002-01 (từng route riêng) ↔ TC-DATADB001-01 (NEW-19, gọi gộp 3 route) | NEW-19 có oracle **riêng biệt**: "không **tạo mới** bản ghi khi trước đó chưa có" (nhánh biên auto-create theo BR-02), không phải lặp lại oracle 403 |

⚠️ Không có `DUP-CONFLICT` **giữa các TC** (16 TC nhất quán với nhau). Mâu thuẫn tồn tại là **TC ↔ spec** (BR-11) → đã đưa vào **G4** và **§6**.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | Toàn bộ 14 TC có `Kết quả thực thi = Đạt` | **Cột `Evidence thực tế` rỗng 16/16 TC** — vi phạm RULE-02 ("chỉ tick Đạt khi đã đính kèm đúng loại bằng chứng"). Kết quả hiện là **tự khai bởi pipeline AI** (`last_exec.source = ai`, `by = pipeline-manual`, run #805), chưa QA người nào xác nhận. Với các quan điểm liên quan, evidence bắt buộc là *"kết quả gọi API trực tiếp bằng role không đủ quyền (mã lỗi trả về)"* (PERM-002) và *"ảnh chụp kết quả query trước/sau kèm câu query trên 2 tài khoản"* (DATA-DB-001) | Đính evidence cho từng TC `Đạt`, hoặc hạ toàn bộ về `Chưa test` cho tới khi có. Trước khi có evidence, **mọi kết luận "đủ TC" ở §1/§2 chỉ ở mức `RISK`** |
| 2 | `[MAJOR]` | Nguồn TC — môi trường chạy | **16/16 TC chạy `env = local`**; `envAuto` cho thấy dev / staging / prd đều **0 run**. Task chạm domain ngoài (`api.chatwork.com`) ⇒ RULE-08 / ENV-003 | Chạy lại tối thiểu trên staging cho nhóm EP-11/12/14, và trên production (bot QA riêng) cho nhánh outbound ChatWork — xem TC-ENV003-01/-02 ở §5 |
| 3 | `[MAJOR]` | `03-dev-impact.md` | Checkbox **"Tester verify auto-fill chính xác" chưa tick** trong khi file có `Auto-filled: 2026-09-08 by /new-task` ⇒ bảng `F*`/`D*`/`T*` chưa được người verify. Riêng mục **4.1 Dev chỉ ghi *file* thay đổi**, không kê function ⇒ F1–F7 hiện là suy luận | Tester đọc lại Journal #133412 trên Redmine, xác nhận rồi tick. Nếu F7 (nhóm route đọc) không đúng ý Dev → hỏi lại trước khi đóng G1 |
| 4 | `[MAJOR]` | Toàn bộ 16 TC | Cột **`Trạng thái đánh giá spec` rỗng 16/16**. Đây không phải lỗi hình thức: spec FA-006 ghi thẳng quyền Staff *"chưa xác minh"* (§1 Actors) và §9 G-07 ⇒ giá trị đúng phải là **`Spec không ghi`** kèm tên người đã hỏi. Để rỗng = TC đang **tự suy diễn Expected rồi cho Đạt** | Điền `Spec không ghi` + ghi rõ đã hỏi ai cho mọi TC liên quan tới hành vi quyền của staff |
| 5 | `[MAJOR]` | TC-PERM004-01 (NEW-8) · TC-DATADB001-01 (NEW-19) · TC-REGSHARED001-02 (NEW-21) · TC-PERM002-02 (NEW-6) | **Không atomic** — mỗi TC gộp nhiều nhánh vào 1 kết quả: NEW-8 chạy **2 biến thể mất quyền** (bỏ tick role / xoá staff khỏi bot); NEW-19 và NEW-21 mỗi cái gọi **3 route**; NEW-6 chạy **3 giá trị radio**. Một chữ `Đạt` duy nhất **không truy được** nhánh nào thực sự đã chạy — đặc biệt rủi ro khi evidence đang rỗng (issue #1) | Tách theo nhánh, hoặc bắt buộc ghi evidence **riêng cho từng nhánh** trong `Evidence thực tế` |
| 6 | `[MAJOR]` | TC-DATADB001-01 (NEW-19) | Tiền đề *"CHƯA từng có bản ghi cấu hình thông báo cho cặp (bot test, staff)"* **có thể không dựng được**: spec BR-02 + BR-11 nói record được auto-create **theo bot** ngay lần đầu truy cập màn. Nếu vậy tiền đề không bao giờ đúng và kết quả `Đạt` là **vacuous** (không chứng minh gì) | Chốt mô hình dữ liệu ở §6 trước; sau đó viết lại tiền đề + oracle của NEW-19 |
| 7 | `[MINOR]` | 3 TC: TC-AUTHSESSION001-01 · TC-APICONTRACT001-01 · TC-TOOLKNOW002-01 | **Mã quan điểm không tồn tại** trong `framework/checklist-lme.md`: `AUTH-SESSION-001`, `API-CONTRACT-001`, `TOOL-KNOW-002` ⇒ theo quy tắc review, các quan điểm này **không được tính là cover**. Đáng chú ý: TC tái hiện bug gốc lại mang mã lạ `TOOL-KNOW-002` | Map lại: `AUTH-SESSION-001` → `PERM-004`; `API-CONTRACT-001` → `REG-SHARED-001`; `TOOL-KNOW-002` → `PERM-002`. Sửa trên Studio bằng `testcase_update`, không sửa trong repo |
| 8 | `[MINOR]` | TC-REGSHARED001-01 (NEW-20) · TC-REGSHARED001-02 (NEW-21) | **`spec_ids` lệch với spec repo**: 2 TC gắn `EP-07/EP-08/EP-09` cho 3 route lưu cài đặt, nhưng trong [feature-spec.md §6](../../spec-features/admin/notify-setting/feature-spec.md) đó là `EP-03` (`/basic/notify-setting-receive`), `EP-04` (`-receive-page`), `EP-05` (`/basic/notify-setting` legacy) — còn `EP-07/08/09` là 3 route **GET** lấy danh sách. (Nhóm EP-11→EP-14, EP-16, EP-18 thì khớp.) Nội dung steps của TC ghi **đúng URL** nên không sai test, chỉ sai truy vết | Đối chiếu bảng EP giữa info-pack Studio và `spec-features/`, chỉnh bên nào lệch |
| 9 | `[NIT]` | Ticket Redmine #39467 | Mô tả ghi `Task: #15` · `Test case: NEW-65`, không khớp task Studio hiện hành (**#265**, `temp_id` `NEW-1..NEW-22`) | Cập nhật mô tả ticket hoặc bỏ 2 dòng số hiệu cũ để tránh QA tra nhầm |

> Anti-patterns AP-1…AP-6: **không dính cái nào** — fix không phải generic catch (AP-1/AP-4), ticket có root cause rõ không phải symptom-only (AP-2), regression có cả nhánh 403 lẫn happy path (AP-3), không over-cover layer downstream (AP-5), mục 3 file 03 có đủ 9 dòng caller (AP-6).

---

## 5. TCs đề xuất bổ sung (21)

**Xác nhận chống trùng:**

| Kiểm | Kết quả |
|---|---|
| Đối chiếu kho TCs | ⚠️ [kho-tcs/](../../kho-tcs/) **chưa có file cho `FA-006` (通知設定)** — 18 file hiện có là FA-001/003/004/007/008/009/010/011/012/013/015/019/020/021/026/031/033. Không đối chiếu được vùng regression từ kho; không phát hiện được conflict expected với TC cũ |
| Chống trùng vs 16 TC ở BƯỚC 0 | Đã đối chiếu **16 TC** Studio task #265 theo 4 yếu tố — **không TC đề xuất nào trùng**. Các chỗ TC cũ chỉ *thiếu chiều* (không thiếu hẳn) đã ghi thành issue ở §4 thay vì đẻ TC mới (issue #5, #6) |
| Chống trùng nội bộ §5 | Đã gộp: Boundary của `PERM-001` dùng chung `TC-PERM003-02` (kiểu tài khoản "vừa owner bot A vừa staff bot B"); G7 dùng chung `TC-SEC002-03` + `TC-DATAAUDIT001-01` |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-PERM001-01 | UI | PERM-001 | 通知設定 — menu trái + nút 「連携設定」 | Normal | manual | Tất cả | Chủ bot và staff CÓ quyền đều thấy menu 通知設定 và mở được màn liên kết ChatWork | - Bot test có 1 staff đã nhận lời mời, nhóm quyền CÓ tick màn 「通知設定」<br>- Có sẵn tài khoản chủ bot của cùng bot đó<br>- Đã lập bảng ma trận role (chủ bot / staff có quyền / staff không quyền) trên giấy để đối chiếu | 1. Đăng nhập bằng chủ bot, chọn bot test, chụp menu trái<br>2. Mở màn 「通知設定」, chụp hàng 「ChatWork（チャットワーク）」 và nút 「連携設定」<br>3. Bấm 「連携設定」, chụp màn 「通知設定 ChatWork連携」<br>4. Đăng xuất, đăng nhập bằng staff CÓ quyền, chọn cùng bot<br>5. Lặp lại bước 1–3 và chụp lại | Không nhập liệu — chỉ quan sát menu và màn hình | Cả 2 tài khoản: menu trái **hiện** mục 「通知設定」; màn 通知設定 mở được; nút 「連携設定」 hiện và bấm được; màn 「通知設定 ChatWork連携」 mở đầy đủ 3 ô (URL phòng / API token / mục 9 chọn kiểu nhận). Không xuất hiện 「この権限は許可されていません。」 ở bất kỳ bước nào | | Lấp Q2 · ENV: Tất cả · Evidence: screenshot menu + màn ChatWork của **từng role**, kèm bảng ma trận role đã tick · Spec: `Spec không ghi` — FA-006 §1 Actors ghi quyền Staff "chưa xác minh" |
| TC-PERM001-02 | UI | PERM-001 | 通知設定 — menu trái + truy cập trực tiếp URL | Abnormal | manual | Tất cả | Staff KHÔNG quyền màn 通知設定 không thấy menu và không mở được màn liên kết ChatWork | - Bot test có 1 staff đã nhận lời mời, nhóm quyền **KHÔNG** tick màn 「通知設定」<br>- Đã đăng nhập bằng staff đó và chọn đúng bot test | 1. Chụp toàn bộ menu trái, tìm mục 「通知設定」<br>2. Gõ thẳng URL màn chính `/basic/notify-setting` vào thanh địa chỉ<br>3. Gõ thẳng URL màn liên kết ChatWork `/basic/notify-setting-chatwork`<br>4. Với mỗi lần, chụp màn hình kết quả và mở DevTools tab Network xem mã trạng thái của trang | Không nhập liệu | Menu trái **không** có mục 「通知設定」. Hai URL gõ thẳng đều **không mở được** màn cấu hình: hoặc trả 403 kèm 「この権限は許可されていません。」, hoặc bị đưa về màn khác. **Không** có trường hợp màn ChatWork mở ra với dữ liệu cấu hình thật | | Lấp Q2 + G1 · ⚠️ **Expected chưa được spec chốt** — Dev cố ý không thêm guard cho route render màn (EP-10). Chạy TC này **sau** khi Leader/Dev chốt ở §6 · Evidence: screenshot menu + screenshot 2 lần gõ URL kèm mã trạng thái |
| TC-SEC002-01 | UI | SEC-002 | 通知設定 ChatWork連携 — ô API token | Normal | manual | Tất cả | Chủ bot lưu API token ChatWork thành công mà token không lộ ra màn hình, console hay log | - Đăng nhập bằng chủ bot của bot test, đã chọn đúng bot<br>- Có API token ChatWork hợp lệ của QA<br>- Đã nhờ Dev/DevOps mở sẵn log ứng dụng của môi trường test để tra theo thời điểm | 1. Mở màn 「通知設定 ChatWork連携」, mở DevTools tab Network + Console<br>2. Nhập API token vào ô token, bấm 「保存」<br>3. Đọc response của lời gọi lưu token trong tab Network<br>4. Tải lại màn, quan sát ô token hiển thị thế nào<br>5. Xem lại response của lời gọi lấy cấu hình khi tải màn<br>6. Nhờ Dev tra log ứng dụng của khoảng thời gian vừa thao tác, tìm chuỗi token | API token ChatWork hợp lệ của QA (lấy từ kho tài khoản test, không ghi vào TC) | Lưu thành công, hiện 「保存しました。」. Ô token sau khi tải lại **hiển thị dạng che** (không phải chuỗi token thô). Console **không** in token. Log ứng dụng **không** chứa chuỗi token thô | | Lấp Q1 · Evidence: screenshot màn + ảnh response Network + kết quả tra log (che bớt token khi chụp) · ⚠️ Spec §9 ghi *"EP-12 API token trả lại trong response body — rủi ro bảo mật"* → nếu response có token thô thì đây là phát hiện thật, raise ticket riêng |
| TC-SEC002-02 | UI | SEC-002 | 通知設定 ChatWork連携 — đọc cấu hình bằng tài khoản không quyền | Abnormal | manual | Tất cả | Staff KHÔNG quyền không đọc được URL phòng và API token ChatWork của bot | - Bot test đã có URL phòng + API token ChatWork được chủ bot lưu sẵn<br>- Có staff đã nhận lời mời, nhóm quyền **KHÔNG** tick màn 「通知設定」, đã đăng nhập và chọn đúng bot test | 1. Mở DevTools tab Network<br>2. Gõ thẳng URL `/basic/notify-setting-chatwork`<br>3. Nếu màn mở ra: quan sát ô URL phòng và ô API token trên giao diện<br>4. Trong tab Network, mở response của lời gọi lấy cấu hình thông báo và tìm giá trị URL phòng / token<br>5. Chụp lại toàn bộ | Không nhập liệu — chỉ đọc | Staff không quyền **không** đọc được cấu hình: màn bị chặn, hoặc nếu màn mở thì 2 ô rỗng **và** response không chứa URL phòng lẫn API token thật của bot | | Lấp G1 + Q1 · ⚠️ Cùng điểm chờ chốt với TC-PERM001-02 (route đọc không được thêm guard) → §6 · Evidence: screenshot màn + ảnh response Network |
| TC-SEC002-03 | Data | SEC-002 | Route AJAX cấu hình ChatWork — log ứng dụng | Abnormal | manual | Tất cả | Lần ghi trái quyền bị chặn vẫn vào log nhưng log không chứa API token thô | - Staff KHÔNG quyền màn 「通知設定」, đã đăng nhập và chọn đúng bot test<br>- Đã hẹn trước với Dev/DevOps để tra log ứng dụng theo mốc thời gian<br>- Ghi lại thời điểm chính xác trước khi thao tác | 1. Ghi lại giờ hệ thống<br>2. Dùng DevTools của phiên staff, gọi lần lượt 2 route: lưu URL phòng và lưu API token<br>3. Ghi lại mã trạng thái trả về của cả 2<br>4. Nhờ Dev mở log ứng dụng đúng khoảng thời gian đó<br>5. Trong log, tìm: dòng ghi nhận thao tác (kèm danh tính staff), dòng từ chối quyền, và chuỗi token vừa gửi | URL phòng và chuỗi token do tester tự đặt riêng cho lần chạy (đặt chuỗi dễ tra, VD `QA-TOKEN-39467`) | Cả 2 lời gọi trả 403 kèm 「この権限は許可されていません。」. Log **có** dòng ghi nhận thao tác kèm danh tính staff **và** dòng từ chối quyền (REQ-008). Log **không** chứa chuỗi token vừa gửi ở dạng thô | | Lấp Q1 + G7 (REQ-008) · Guard đặt SAU `addLogUserAction` (dev_impact) nên payload chắc chắn đi qua tầng log — đây là chỗ token dễ rò · Evidence: kết quả tra log (che token) + ảnh 2 response 403 |
| TC-SEC002-04 | UI | SEC-002 | 通知設定 ChatWork連携 — nút 「テスト送信」 | Boundary | manual | product | Gửi thử ChatWork thất bại do token sai — màn lỗi và log không lộ token | - Chạy trên **production** bằng **bot QA riêng** (tuyệt đối không dùng bot khách) và **phòng ChatWork của QA**<br>- Đăng nhập bằng chủ bot của bot QA đó, kênh ChatWork đang BẬT<br>- Đã lưu một API token **sai** để ép nhánh lỗi | 1. Mở màn 「通知設定 ChatWork連携」, mở DevTools tab Network + Console<br>2. Bấm 「テスト送信」<br>3. Chụp thông báo lỗi hiện trên trang quản trị<br>4. Đọc response của lời gọi gửi thử<br>5. Nhờ Dev tra log ứng dụng của khoảng thời gian đó, tìm chuỗi token | Token ChatWork sai (chuỗi tự đặt, VD `QA-BAD-TOKEN-39467`) | Trang quản trị hiện thông báo lỗi gửi thử (không phải 「エルメ 通知テスト送信完了」). Thông báo lỗi, response và log ứng dụng **đều không** chứa chuỗi token; phòng ChatWork của QA **không** nhận tin nào | | Lấp Q1 + Q6 · **ENV product** theo RULE-08 (chạm domain ngoài `api.chatwork.com`) — dùng bot QA riêng để không đụng dữ liệu khách · Evidence: screenshot màn lỗi + ảnh phòng ChatWork không có tin mới + kết quả tra log |
| TC-DATAAUDIT001-01 | Data | DATA-AUDIT-001 | Route AJAX cấu hình ChatWork — lịch sử thao tác | Normal | manual | Tất cả | Mỗi lần thử ghi cấu hình ChatWork trái quyền để lại đúng 1 bản ghi lịch sử với đủ 4 thông tin | - Staff KHÔNG quyền màn 「通知設定」, đã đăng nhập và chọn đúng bot test<br>- Đã hẹn trước với Dev để mở nguồn lịch sử thao tác của hệ thống<br>- Ghi lại mốc thời gian trước khi bắt đầu | 1. Ghi lại giờ hệ thống<br>2. Dùng phiên staff, gọi **lần lượt từng route một**, mỗi route đúng **1 lần**: lưu URL phòng → lưu API token → đổi kiểu nhận thông báo → gửi thử<br>3. Ghi lại mã trạng thái của từng lời gọi<br>4. Nhờ Dev mở lịch sử thao tác của khoảng thời gian đó<br>5. Đếm số bản ghi và đọc nội dung từng bản ghi | Mỗi route 1 lần gọi, dữ liệu đặt riêng cho lần chạy để dễ tra | Cả 4 lời gọi trả 403. Lịch sử thao tác có **đúng 4 bản ghi** (1 bản ghi / 1 lần thao tác, không nhân đôi, không thiếu), mỗi bản ghi đủ 4 thông tin: **người thực hiện** (đúng staff) / **thời gian** / **hành động** (đúng loại thao tác) / **kết quả bị từ chối**. Không bản ghi nào ghi nhầm thành thao tác thành công | | Lấp Q3 + G7 (REQ-008) · Evidence: export/screenshot 4 bản ghi lịch sử + 4 response 403 |
| TC-FUNC003-01 | UI | FUNC-003 | 通知設定 ChatWork連携 — ô URL phòng ChatWork | Abnormal | manual | Tất cả | Chủ bot nhập URL phòng ChatWork sai định dạng thì bị báo lỗi và cấu hình cũ không đổi | - Đăng nhập bằng chủ bot của bot test, đã chọn đúng bot<br>- Đã lưu sẵn 1 URL phòng ChatWork hợp lệ và ghi lại giá trị đó để đối chiếu | 1. Mở màn 「通知設定 ChatWork連携」<br>2. Xoá ô URL phòng, nhập giá trị sai định dạng đầu tiên, bấm 「保存」, chụp thông báo<br>3. Tải lại màn, đối chiếu ô URL phòng<br>4. Lặp lại bước 2–3 cho từng giá trị sai còn lại trong cột Dữ liệu nhập | Chạy lần lượt 5 giá trị: (1) chuỗi không phải URL `abc xyz`; (2) thiếu scheme `www.chatwork.com/#!rid123`; (3) `javascript:alert(1)`; (4) URL có khoảng trắng ở giữa; (5) URL ChatWork đúng dạng nhưng thiếu phần `#!rid` | Mỗi lần đều hiện thông báo lỗi 「ChatworkルームURLが正しくありません。」 (hoặc thông báo lỗi tương đương), **không** hiện 「保存しました。」. Sau khi tải lại, ô URL phòng vẫn là **URL hợp lệ cũ**, không bị ghi đè bằng giá trị sai. Giá trị `javascript:` không được lưu | | Lấp Q4 + G5 · Catalog `DI-05` (URL) · Evidence: screenshot lỗi cho **từng** giá trị + screenshot ô URL sau khi tải lại · Spec: `Spec không ghi` — feature-spec §9 ghi *"EP-11 Không validate URL room ChatWork"* → cần Dev xác nhận hành vi đúng |
| TC-FUNC003-02 | UI | FUNC-003 | 通知設定 ChatWork連携 — ô API token | Abnormal | manual | Tất cả | Chủ bot nhập API token ChatWork sai thì bị báo lỗi và token cũ giữ nguyên | - Đăng nhập bằng chủ bot của bot test, đã chọn đúng bot<br>- Đã lưu sẵn 1 API token ChatWork hợp lệ<br>- Môi trường ra được Internet tới dịch vụ ChatWork (hệ thống kiểm token trước khi lưu) | 1. Mở màn 「通知設定 ChatWork連携」<br>2. Nhập token sai vào ô token, bấm 「保存」, chụp thông báo<br>3. Tải lại màn, quan sát ô token<br>4. Bấm 「テスト送信」 để xác nhận cấu hình vẫn dùng token cũ<br>5. Lặp lại bước 2–3 với các giá trị sai còn lại | Chạy lần lượt: (1) chuỗi ngẫu nhiên không phải token; (2) ô token để trống; (3) token toàn khoảng trắng; (4) token của tài khoản ChatWork **không có quyền** vào phòng đã cấu hình | Mỗi lần hiện 「ChatworkAPIトークンが正しくありません。」 (hoặc lỗi tương đương), **không** hiện 「保存しました。」. Sau khi tải lại, token cũ vẫn còn hiệu lực — bấm 「テスト送信」 vẫn gửi được tin vào phòng ChatWork của QA | | Lấp Q4 + G5 · Đối chiếu log ticket: EP-12 từng trả **200** kèm nội dung báo lỗi token — cần xác nhận sau fix mã trạng thái/nội dung của nhánh này **không đổi** · Evidence: screenshot lỗi từng giá trị + ảnh phòng ChatWork nhận tin ở bước 4 |
| TC-FUNC003-03 | API | FUNC-003 | Route AJAX `/ajax/notify/save-url-chat-work` | Boundary | manual | Tất cả | Gọi thẳng route lưu URL bằng phiên CÓ quyền vẫn bị chặn ở tầng server với giá trị biên | - Có phiên đăng nhập của chủ bot đã chọn đúng bot test<br>- Đã ghi lại URL phòng hợp lệ hiện tại<br>- Gọi route bằng DevTools của chính phiên đó (nhóm `/ajax/*` không yêu cầu token chống giả mạo biểu mẫu) | 1. Với từng giá trị ở cột Dữ liệu nhập, gọi thẳng route lưu URL phòng<br>2. Ghi lại mã trạng thái + nội dung trả về của từng lần<br>3. Sau mỗi lần, tải lại màn 「通知設定 ChatWork連携」 và đối chiếu ô URL phòng | Chạy lần lượt: (1) URL đúng dạng **có** dấu `/` ở cuối; (2) URL đúng dạng **không có** dấu `/` ở cuối; (3) URL rỗng; (4) URL dài ~1000 ký tự; (5) `javascript:alert(1)` | Hai giá trị (1) và (2) **đều** lưu được và hiển thị lại đúng. Ba giá trị (3)(4)(5) bị từ chối ở **tầng server** (không chỉ chặn ở giao diện) và URL phòng cũ không bị ghi đè. Không có giá trị nào làm màn ChatWork lỗi khi mở lại | | Lấp Q4 + G5 · Catalog `DI-05` — quan điểm FUNC-003 nêu rõ *"frontend chặn được nhưng gọi thẳng API vẫn lưu được = lỗi kinh điển"*, nhóm route này lại miễn CSRF · Evidence: ảnh response từng lần + ảnh màn sau khi tải lại |
| TC-DATADB001-02 | UI | DATA-DB-001 | 通知設定 ChatWork連携 — 2 người dùng trên cùng 1 bot | Normal | manual | Tất cả | Chủ bot và staff CÓ quyền cùng lưu cấu hình ChatWork trên cùng 1 bot — xác định rõ cấu hình dùng chung hay tách theo người | - Cùng 1 bot test; 1 tài khoản chủ bot và 1 staff đã nhận lời mời với nhóm quyền CÓ tick màn 「通知設定」<br>- Dùng 2 trình duyệt/2 hồ sơ riêng để giữ 2 phiên song song<br>- Ghi lại URL phòng hiện tại trước khi bắt đầu | 1. Ở phiên chủ bot: lưu URL phòng giá trị **A**, chụp màn sau khi lưu<br>2. Ở phiên staff: tải lại màn 「通知設定 ChatWork連携」, quan sát ô URL phòng đang hiện giá trị nào<br>3. Ở phiên staff: lưu URL phòng giá trị **B**<br>4. Ở phiên chủ bot: tải lại màn, quan sát ô URL phòng<br>5. Ghi lại kết quả quan sát của cả 2 phiên | URL phòng A và B là 2 phòng ChatWork khác nhau của QA, đặt tên dễ phân biệt | Ghi nhận **rõ ràng** một trong hai hành vi: **(a) dùng chung 1 cấu hình / bot** — bước 2 staff thấy A, bước 4 chủ bot thấy B; hoặc **(b) tách theo người** — bước 2 staff thấy giá trị cũ của staff, bước 4 chủ bot vẫn thấy A. Kết quả quan sát được phải khớp với mô hình dữ liệu mà Dev chốt ở §6 | | Lấp Q5 + G4 · **TC quyết định** cho mâu thuẫn BR-11 (spec: 1 record/bot) vs oracle của 13 TC Studio (theo cặp bot+user). Chạy TC này TRƯỚC khi kết luận các TC còn lại · Evidence: 4 screenshot theo đúng thứ tự bước |
| TC-DATADB001-03 | UI | DATA-DB-001 | 通知設定 ChatWork連携 — góc nhìn của chủ bot | Abnormal | manual | Tất cả | Staff KHÔNG quyền bị chặn ghi thì cấu hình ChatWork mà CHỦ BOT nhìn thấy không đổi | - Cùng 1 bot test; chủ bot đã lưu URL phòng + API token + kiểu nhận thông báo, đã chụp lại màn làm mốc<br>- 1 staff nhóm quyền **KHÔNG** tick màn 「通知設定」, đang đăng nhập ở trình duyệt khác và đã chọn đúng bot test | 1. Ở phiên chủ bot: chụp màn 「通知設定 ChatWork連携」 làm mốc trước<br>2. Ở phiên staff: gọi lần lượt 3 route ghi cấu hình ChatWork (lưu URL phòng, lưu API token, đổi kiểu nhận thông báo) với giá trị mới<br>3. Ghi lại mã trạng thái của từng lời gọi<br>4. Ở phiên chủ bot: tải lại màn, chụp lại và so từng ô với ảnh mốc<br>5. Ở phiên chủ bot: bấm 「テスト送信」 và kiểm phòng ChatWork nhận tin | URL phòng / token / kiểu nhận thông báo đều đặt giá trị mới, khác hẳn giá trị của chủ bot để dễ nhận ra nếu bị ghi đè | Cả 3 lời gọi của staff trả 403. Màn của **chủ bot** sau khi tải lại **giống hệt ảnh mốc** ở cả 3 ô. Bấm 「テスト送信」 vẫn gửi tin vào **đúng phòng ChatWork cũ** của chủ bot, không phải phòng mà staff cố ghi vào | | Lấp Q5 + G4 · Đây là oracle **đúng cho cả 2 mô hình dữ liệu**, dùng để chốt tác động thật của bug từ góc nhìn người dùng cuối · Evidence: cặp ảnh trước/sau của màn chủ bot + ảnh phòng ChatWork nhận tin |
| TC-DATADB001-04 | UI | DATA-DB-001 | 通知設定 ChatWork連携 — dữ liệu ghi trái quyền có từ trước bản fix | Normal | manual | product | Cấu hình do staff không quyền ghi TRƯỚC khi deploy vẫn còn nguyên sau khi deploy và chủ bot chỉnh lại được | - Chuẩn bị **trước khi deploy bản fix** trên môi trường sẽ deploy: dùng staff không quyền ghi 1 URL phòng dễ nhận biết vào cấu hình ChatWork của bot QA<br>- Chụp lại màn + ghi rõ giá trị đã ghi và thời điểm<br>- Sau đó mới deploy branch `ai_fixbug_39467` | 1. Trước deploy: ghi giá trị bằng staff không quyền, chụp màn của chủ bot xác nhận giá trị đã vào<br>2. Deploy bản fix<br>3. Sau deploy: đăng nhập chủ bot, mở màn 「通知設定 ChatWork連携」, đối chiếu với ảnh trước deploy<br>4. Chủ bot sửa URL phòng về giá trị đúng, bấm 「保存」<br>5. Tải lại màn và bấm 「テスト送信」 | URL phòng "bẩn" đặt tên dễ nhận biết, VD phòng QA phụ; URL phòng đúng là phòng QA chính | Sau deploy, giá trị "bẩn" **vẫn còn nguyên** (bản fix không migrate, không dọn dữ liệu). Chủ bot sửa lại được bằng thao tác bình thường, hiện 「保存しました。」, và 「テスト送信」 gửi tin vào **phòng đúng** sau khi sửa | | Lấp G8 (REQ-007) · **ENV product** vì chỉ có ở môi trường deploy thật mới đo được "trước/sau deploy"; dùng bot QA riêng · Phải chuẩn bị dữ liệu **trước** deploy — nếu bỏ lỡ thì TC không chạy lại được ở vòng này |
| TC-PERM002-05 | UI | PERM-002 | Route AJAX cấu hình ChatWork — tài khoản chủ bot bị gán role hạn chế | Boundary | manual | Tất cả | Chủ bot có nhóm quyền KHÔNG chứa màn 通知設定 vẫn thao tác được (guard không chặn nhầm chủ bot) | - Bot test có chủ bot là người tạo bot<br>- Gán cho chính tài khoản chủ bot một nhóm quyền **KHÔNG** tick màn 「通知設定」 (nếu hệ thống cho phép gán role cho chủ bot)<br>- Đăng nhập bằng chủ bot đó và chọn đúng bot test | 1. Kiểm tra menu trái xem mục 「通知設定」 còn hiện không<br>2. Mở màn 「通知設定 ChatWork連携」<br>3. Lưu URL phòng ChatWork, chụp thông báo<br>4. Đổi kiểu nhận thông báo ở mục 「9.通知受け取り方法を選択」<br>5. Bấm 「テスト送信」<br>6. Tải lại màn và đối chiếu cả 3 thay đổi | URL phòng ChatWork của QA; kiểu nhận thông báo đổi sang 「複数人のグループでエルメからの通知を受け取る」 | Cả 3 thao tác **thành công** (hiện 「保存しました。」 / 「エルメ 通知テスト送信完了」), **không** xuất hiện 「この権限は許可されていません。」. Sau khi tải lại, cả 3 thay đổi được giữ. Nếu hệ thống **không cho** gán role cho chủ bot → ghi nhận điều đó và đánh dấu TC là không áp dụng, kèm ảnh chứng minh | | Lấp Q7 + G3 · Nhánh `admin_id` luôn qua guard là **giả định trung tâm** của bản fix (dev_impact) nhưng chưa TC nào ép đúng nhánh biên này · Evidence: screenshot menu + 3 thao tác + màn sau khi tải lại |
| TC-PERM002-06 | UI | PERM-002 | 通知設定 — các đường truy cập khác vào màn ChatWork | Abnormal | manual | Tất cả | Staff không quyền bị chặn ở mọi đường vào màn ChatWork, không chỉ khi gõ thẳng URL | - Staff KHÔNG quyền màn 「通知設定」, đã đăng nhập và chọn đúng bot test<br>- Trước đó (bằng tài khoản có quyền) đã thêm màn 「通知設定」 vào **menu yêu thích** nếu hệ thống có chức năng này<br>- Có sẵn 1 màn khác trong hệ thống chứa hyperlink dẫn tới 通知設定 (nếu có) | 1. Mở menu yêu thích, tìm và bấm mục 「通知設定」 (nếu còn hiện)<br>2. Quay lại, tìm hyperlink dẫn tới 通知設定 ở màn khác và bấm vào<br>3. Dùng nút Back/Forward của trình duyệt quay lại màn ChatWork đã từng mở bằng tài khoản khác<br>4. Với mỗi đường vào, chụp kết quả và mã trạng thái ở tab Network | Không nhập liệu | Cả 3 đường vào đều **không** mở được màn cấu hình ChatWork với dữ liệu thật: mục trong menu yêu thích không hiện (hoặc bấm vào bị chặn), hyperlink bị chặn, Back/Forward không làm lộ dữ liệu cấu hình đã tải trước đó | | Lấp Q7 · Catalog `MAP-PERM-02` — 4 đường truy cập; TC Studio mới phủ "gõ thẳng URL" · Đường nào hệ thống không có thì ghi "không áp dụng" kèm lý do, không bỏ trống · Evidence: screenshot từng đường vào |
| TC-PERM003-02 | UI | PERM-003 | Route AJAX cấu hình ChatWork — tài khoản vừa là chủ bot vừa là staff | Normal | manual | Tất cả | Tài khoản vừa là chủ bot A vừa là staff không quyền ở bot B — quyền tính đúng theo bot đang chọn | - 1 tài khoản duy nhất: là **chủ bot** của bot A, đồng thời là **staff** ở bot B với nhóm quyền **KHÔNG** tick màn 「通知設定」<br>- Ghi lại URL phòng ChatWork hiện có của cả bot A và bot B | 1. Đăng nhập, chọn **bot A**, chụp menu trái<br>2. Mở màn 「通知設定 ChatWork連携」 của bot A, lưu URL phòng giá trị **A1**<br>3. Dùng chức năng đổi bot sang **bot B**, chụp menu trái<br>4. Thử mở màn 「通知設定 ChatWork連携」 của bot B và thử lưu URL phòng<br>5. Đổi ngược lại bot A, tải lại màn và đối chiếu URL phòng<br>6. Kiểm tra URL phòng của bot B | URL phòng A1 (phòng QA 1) cho bot A; URL phòng B1 (phòng QA 2) thử ghi ở bot B | Ở **bot A**: menu 「通知設定」 hiện, lưu thành công, URL phòng của bot A = A1. Ở **bot B**: menu 「通知設定」 **không** hiện và thao tác lưu bị từ chối 403 kèm 「この権限は許可されていません。」. URL phòng của bot B **không đổi**. Quay lại bot A, URL phòng vẫn là A1 — không bị lẫn sang bot B | | Lấp Q8 + dùng làm **Boundary của Q2 (`PERM-001`)** · Catalog `MAP-PERM-01` kiểu tài khoản *"vừa là owner vừa là staff của bot khác"* — kiểu dễ làm guard tính quyền theo tài khoản thay vì theo bot đang chọn · Evidence: screenshot menu + màn ở từng bot |
| TC-OUTTRUTH001-04 | UI | OUT-TRUTH-001 | 通知設定 ChatWork連携 — thông báo khi thao tác bị từ chối | Abnormal | manual | Tất cả | Staff bị thu hồi quyền khi đang mở màn phải được BÁO LỖI, không im lặng | - Hai trình duyệt/hai hồ sơ: 1 phiên chủ bot, 1 phiên staff của cùng bot test<br>- Ban đầu nhóm quyền của staff CÓ tick màn 「通知設定」<br>- Staff đang mở sẵn màn 「通知設定 ChatWork連携」, **chưa** tải lại trang | 1. Ở phiên chủ bot: bỏ tick màn 「通知設定」 khỏi nhóm quyền của staff, lưu<br>2. Ở phiên staff (trang cũ, không tải lại): nhập URL phòng mới, bấm 「保存」<br>3. **Quay video hoặc chụp liên tiếp** màn hình staff trong 5 giây sau khi bấm<br>4. Quan sát: có overlay loading không, có thông báo nào hiện lên không, ô nhập giữ nguyên giá trị vừa gõ hay bị xoá<br>5. Lặp lại bước 2 với thao tác đổi kiểu nhận thông báo ở mục 9 | URL phòng ChatWork của QA (giá trị mới, khác giá trị đang lưu) | Sau khi bấm 「保存」, màn hình staff **hiện thông báo lỗi rõ ràng** cho biết thao tác thất bại (VD 「この権限は許可されていません。」). **Không** được rơi vào trạng thái im lặng: overlay tắt mà không có bất kỳ thông báo nào, khiến người dùng tưởng đã lưu thành công | | Lấp Q9 · ⚠️ Dev **tự khai** hành vi hiện tại là im lặng và **cố ý không sửa FE** để giữ fix tối giản → TC này nhiều khả năng **Không đạt**; đó chính là mục đích: bắt Leader chốt ở §6 chứ không để lọt âm thầm · Evidence: **video** thao tác (ảnh tĩnh không chứng minh được "không có thông báo") |
| TC-REGSHARED001-03 | API | REG-SHARED-001 | Các màn khác có route `/ajax/*` ghi cấu hình | Abnormal | manual | Tất cả | Rà ngang: các màn khác dùng cùng pattern route ajax có bị thiếu kiểm quyền như 通知設定 không | - **Chặn chạy nếu chưa có**: danh sách do Dev cung cấp gồm các màn khác có route `/ajax/*` **ghi** dữ liệu và nằm ngoài nhóm middleware kiểm quyền theo màn (đối chiếu `routes/web.php`)<br>- Với mỗi màn trong danh sách: chuẩn bị 1 staff có role **KHÔNG** chứa màn đó, đã đăng nhập và chọn đúng bot test<br>- Ghi lại giá trị cấu hình hiện tại của từng màn | 1. Yêu cầu Dev gửi danh sách màn + route `/ajax/*` ghi dữ liệu tương ứng<br>2. Chọn tối thiểu **3 màn** trong danh sách<br>3. Với từng màn: xác nhận staff không thấy menu của màn đó<br>4. Gọi thẳng route `/ajax/*` ghi dữ liệu của màn đó bằng phiên staff<br>5. Ghi lại mã trạng thái<br>6. Mở lại màn đó bằng tài khoản có quyền và đối chiếu giá trị cấu hình | Với mỗi màn: 1 giá trị hợp lệ, đặt riêng cho lần chạy để dễ tra | Với **mọi** màn đã chọn: lời gọi trả **403** kèm 「この権限は許可されていません。」 và cấu hình của màn đó **không đổi**. Bất kỳ màn nào trả 200 và ghi được dữ liệu ⇒ **raise ticket bug riêng** — cùng root cause với #39467 | | Lấp Q10 + G6 · Quan điểm REG-SHARED-001 yêu cầu *"dev cung cấp danh sách nơi ảnh hưởng → test lại từng mục"*; mục 3 của file 03 mới chỉ quét **trong cùng màn** · Chưa có danh sách Dev thì **không tự đoán màn** — báo lại Leader |
| TC-REGSHARED001-04 | UI | REG-SHARED-001 | 通知設定 — bot mới chưa từng có cấu hình thông báo | Boundary | manual | Tất cả | Bot vừa tạo, chưa có cấu hình thông báo — chủ bot mở màn lần đầu và mọi thao tác lưu vẫn hoạt động | - Tạo **bot test mới hoàn toàn**, chưa ai mở màn 「通知設定」 lần nào<br>- Đăng nhập bằng chủ bot của bot mới đó và chọn đúng bot | 1. Mở màn 「通知設定」 lần đầu tiên, chụp toàn bộ giá trị mặc định của 3 kênh và 3 ô tần suất<br>2. Bật kênh 「ChatWork（チャットワーク）」 và đổi tần suất<br>3. Bấm 「連携設定」 mở màn ChatWork, lưu URL phòng và API token<br>4. Đổi kiểu nhận thông báo ở mục 9<br>5. Bấm 「テスト送信」<br>6. Tải lại cả 2 màn và đối chiếu toàn bộ thay đổi | URL phòng + API token ChatWork của QA; tần suất đổi từ 「リアルタイム」 sang 「15分ごと」 | Màn 通知設定 mở được ở lần đầu với bộ giá trị mặc định (không lỗi, không trắng màn). Cả 4 thao tác lưu đều thành công, **không** xuất hiện 「この権限は許可されていません。」. Sau khi tải lại, mọi thay đổi được giữ đúng. 「テスト送信」 gửi được tin vào phòng ChatWork của QA | | Lấp Q10 (Boundary còn thiếu của REG-SHARED-001) · Phủ nhánh **auto-create** cấu hình (spec BR-02) ở phía người CÓ quyền — nhánh mà TC-DATADB001-01 (NEW-19) chỉ chạm từ phía không quyền · Evidence: screenshot màn lần đầu + màn sau khi tải lại + ảnh phòng ChatWork |
| TC-ENV003-01 | UI | ENV-003 | 通知設定 ChatWork連携 — chạy lại trên production | Normal | manual | product | Chạy lại bộ 403 + happy path ChatWork trên production bằng bot QA | - Chạy trên **production** với **bot QA riêng** (không dùng bot khách hàng) và **phòng ChatWork của QA**<br>- Trên bot QA đó: 1 tài khoản chủ bot, 1 staff CÓ quyền màn 「通知設定」, 1 staff KHÔNG quyền<br>- Đã ghi lại cấu hình ChatWork hiện tại của bot QA để khôi phục sau khi test | 1. Bằng staff KHÔNG quyền: gọi lần lượt 4 route ghi cấu hình ChatWork, ghi lại mã trạng thái<br>2. Bằng chủ bot: lưu URL phòng + API token, đổi kiểu nhận thông báo, bấm 「テスト送信」<br>3. Mở phòng ChatWork của QA kiểm tra tin nhắn thử<br>4. Bằng staff CÓ quyền: lặp lại bước 2<br>5. Khôi phục cấu hình ChatWork của bot QA về giá trị ban đầu | URL phòng + API token ChatWork của QA (dùng phòng riêng cho production, tách khỏi phòng dùng ở local) | Staff KHÔNG quyền: **cả 4** lời gọi trả 403, cấu hình không đổi. Chủ bot và staff CÓ quyền: lưu thành công, 「テスト送信」 gửi tin thật và phòng ChatWork của QA **nhận đúng 2 tin** 「エルメ 通知テスト送信完了」 (1 của chủ bot, 1 của staff) | | Lấp Q6 · RULE-08 / ENV-003: task chạm **domain ngoài** `api.chatwork.com`, hiện **16/16 TC chỉ chạy local** ⇒ không được kết luận từ local · Evidence: ảnh 4 response 403 + ảnh phòng ChatWork có 2 tin |
| TC-ENV003-02 | UI | ENV-003 | 通知設定 ChatWork連携 — production cân bằng tải nhiều server | Boundary | manual | product | Thao tác lưu và gửi thử ChatWork cho kết quả nhất quán qua từng server của production | - Chạy trên **production** với bot QA riêng<br>- Đã hỏi DevOps cách nhận biết đang đi qua server nào (router id / header) và cách ghim phiên vào từng server<br>- Có 1 staff KHÔNG quyền và 1 chủ bot trên bot QA | 1. Ghim phiên vào **server thứ nhất**, ghi lại cách nhận biết<br>2. Bằng staff không quyền: gọi 4 route ghi cấu hình ChatWork, ghi mã trạng thái<br>3. Bằng chủ bot: lưu URL phòng và bấm 「テスト送信」<br>4. Chuyển sang **server thứ hai**, lặp lại bước 2–3<br>5. So sánh kết quả 2 lượt<br>6. Khôi phục cấu hình ban đầu | Cùng một bộ URL phòng + token cho cả 2 lượt để so sánh trực tiếp | Kết quả **giống hệt nhau** ở cả 2 server: staff không quyền luôn nhận 403 ở cả 4 route; chủ bot luôn lưu được và phòng ChatWork của QA nhận đủ tin thử của cả 2 lượt. Không có server nào cho staff không quyền ghi được | | Lấp Q6 · Catalog `ENV-LB` — production có **2 server**, quan điểm ENV-003 yêu cầu chạy bộ case cơ bản theo **từng router** · Nếu DevOps không cung cấp được cách ghim server → ghi "không verify được" + phương án giám sát, không tự bỏ qua |

**Đối chiếu G/Q ↔ TC đề xuất — mọi dòng §1/§2 đều có TC hoặc lý do:**

| Dòng | TC đề xuất tương ứng |
|---|---|
| G1 | TC-PERM001-02, TC-SEC002-02 |
| G2 | **Không viết TC mới** — TC đã có sẵn: chỉ cần **chạy nốt** TC-OUTTRUTH001-01 (NEW-3) và TC-OUTTRUTH001-02 (NEW-22) đang ở trạng thái `Chưa test` |
| G3 | TC-PERM002-05 |
| G4 | TC-DATADB001-02, TC-DATADB001-03 (+ chốt spec ở §6) |
| G5 | TC-FUNC003-01, TC-FUNC003-02, TC-FUNC003-03 |
| G6 | TC-REGSHARED001-03 |
| G7 | TC-SEC002-03, TC-DATAAUDIT001-01 |
| G8 | TC-DATADB001-04 |
| Q1 | TC-SEC002-01, TC-SEC002-02, TC-SEC002-03, TC-SEC002-04 |
| Q2 | TC-PERM001-01, TC-PERM001-02 (+ Boundary dùng TC-PERM003-02) |
| Q3 | TC-DATAAUDIT001-01 |
| Q4 | TC-FUNC003-01, TC-FUNC003-02, TC-FUNC003-03 (Normal đã có TC-FUNC001-01 / NEW-1) |
| Q5 | TC-DATADB001-02, TC-DATADB001-03, TC-DATADB001-04 |
| Q6 | TC-ENV003-01, TC-ENV003-02, TC-SEC002-04 |
| Q7 | TC-PERM002-05, TC-PERM002-06 |
| Q8 | TC-PERM003-02 |
| Q9 | TC-OUTTRUTH001-04 + chạy nốt NEW-3 / NEW-22 (xem G2) |
| Q10 | TC-REGSHARED001-03, TC-REGSHARED001-04 |

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | [feature-spec.md](../../spec-features/admin/notify-setting/feature-spec.md) §5 Business Rules — **BR-11** (+ §3 Entities, `db/db-mapping.md:49`) | Chốt mô hình dữ liệu của `notify_setting`: **1 bản ghi / bot** (spec hiện tại: *"Query luôn dùng `WHERE bot_id = ? LIMIT 1`"*, `user_id` = "ID user tạo record") hay **1 bản ghi / (bot, user)** như 13/16 TC Studio giả định. Nếu là 1/bot ⇒ mức nghiêm trọng của #39467 phải nâng lên: staff không quyền đang ghi đè **chính cấu hình ChatWork của chủ bot** (có thể chuyển hướng thông báo sang phòng ChatWork của người khác), và oracle DB của 13 TC đang kiểm sai hàng dữ liệu | Spec BR-11 vs oracle của TC-FUNC001-01/PERM002-01/…/DATADB001-01 (Studio) và bước 5 của Redmine #39467 (*"notify_setting (bot_id, user_id)"*) | **Dev** xác nhận, chạy `TC-DATADB001-02` để kiểm chứng, rồi cập nhật spec + oracle TC trên Studio |
| 2 | [feature-spec.md](../../spec-features/admin/notify-setting/feature-spec.md) §1 Actors + §9 Gaps **G-07** | Ghi chính thức ma trận quyền của màn 通知設定 (hiện spec ghi *"chưa xác minh"*, Confidence **Thấp**), nêu rõ: (a) 4 route ghi cấu hình ChatWork **EP-11→EP-14** yêu cầu quyền màn `notifySetting` — đây là hành vi bản fix vừa thêm; (b) các route **ĐỌC** (EP-02 `/ajax/basic/notify-setting`, EP-10 `/basic/notify-setting-chatwork`) có yêu cầu quyền hay không — Dev **cố ý giữ nguyên**, cần quyết định chính thức vì EP-02 trả về cả **API token ChatWork** (§9 đã ghi đây là rủi ro bảo mật) | Bản fix #39467 vs spec chưa định nghĩa; G1 · Q1 · Q2 của report này | **Leader + Dev**. Chốt xong mới chạy được TC-PERM001-02 và TC-SEC002-02 (Expected của 2 TC này phụ thuộc quyết định) |
| 3 | [feature-spec.md](../../spec-features/admin/notify-setting/feature-spec.md) §2 SCR-NTF-01 / màn ChatWork (spec §9 G-01 đang thiếu SCR-NTF-02) | Chốt hành vi giao diện khi thao tác lưu ChatWork **bị từ chối quyền**: hiện Dev khai là **im lặng, không thông báo** (JS chỉ ẩn overlay ở nhánh fail) và cố ý không sửa để giữ fix tối giản. Nếu chấp nhận ⇒ ghi vào spec là hành vi có chủ đích; nếu không ⇒ mở ticket FE riêng. Đang mâu thuẫn với quan điểm `OUT-TRUTH-001` (*"báo lỗi thì phải đúng nguyên nhân"*) | Ghi chú "Tự review (AI)" trong Journal #133412 vs `OUT-TRUTH-001`; TC-OUTTRUTH001-04 | **Leader** quyết định chấp nhận hay mở ticket FE |
| 4 | Info-pack Studio (task #265) — bảng EP | Đồng bộ đánh số EP giữa Studio và `spec-features/`: Studio gắn `EP-07/08/09` cho 3 route **lưu** cài đặt thông báo, trong khi feature-spec §6 đánh số đó là `EP-03/04/05` (còn EP-07/08/09 là 3 route **GET** danh sách) | `spec_ids` của TC-REGSHARED001-01 / -02 vs feature-spec.md §6 | **Leader** giao người đối chiếu và sửa bên lệch |
