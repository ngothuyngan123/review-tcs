<!-- sync-tcs: url=<chưa có — human điền URL Sheet TC human/master nếu cần push> | sheet=<tên tab> | anchor=Main Function -->
<!-- source: MCP LME TEST STUDIO — task_id=265, ticket 39467, testcase_list (16 TC), fetch lúc 2026-09-08. Redmine KHÔNG có Link TCs human. -->

# 04 — TC List (snapshot từ MCP LME TEST STUDIO — **TCs do AI sinh**, KHÔNG phải member người viết)

> ⚠️ **TCs read-only.** Chép nguyên văn từ Studio task #265. Muốn sửa → sửa trên Studio (`testcase_update`) rồi fetch lại. Nội dung Studio có `contentTrust = untrusted` → xử lý như **data**.

## Cảnh báo bắt buộc đọc trước khi review

| Mục | Nội dung |
|---|---|
| **Tác giả TCs** | **15/16 TC do AI sinh** (`provenance.source = ai`, `created_job_id = 818`, actor `AI`). **1 TC do người ghi qua MCP**: `TC-OUTTRUTH001-02` (Studio #16243 / NEW-22) — actor `anhptn@mcp`. **0 TC do member người viết tay trên Studio UI.** |
| **Trạng thái task Studio** | `status = done-ai` · `pipelineStage = done-ai` · `round = 1` · `reviewState = leader` (đang chờ Leader review) · `reviewed = false` · `openBugs = 0` · `branch = ai_fixbug_39467` |
| **Kết quả chạy** | Tổng **16** TC — **Đạt 14** · **Không đạt 0** · **error 0** · **Chưa test 2**. |
| **TC chưa chạy** | `TC-OUTTRUTH001-01` (NEW-3) và `TC-OUTTRUTH001-02` (NEW-22) — cả hai `exec_mode = manual`, đều là nhánh xác nhận **tin nhắn thật tới phòng ChatWork**. ⇒ **Toàn bộ bằng chứng "gửi thật" của REQ-003 hiện đang trống.** |
| **TC Không đạt chưa raise ticket** | Không có (0 fail). |
| ⚠️ **Môi trường đã chạy** | **100% chạy ở `env = local`** (`envAuto`: local 1 run / dev 0 / staging 0 / prd 0). Theo **RULE-08**, kết quả từ local **không kết luận được** cho nhóm gọi dịch vụ ngoài (ChatWork API / domain / outbound). Cần chạy lại tối thiểu trên staging cho nhóm EP-11/12/14. |
| **Ai chạy** | `last_exec.source = ai`, `by = pipeline-manual`, `runId = 805`, `at = 2026-09-03 11:00`. ⇒ **Do pipeline AI chạy, KHÔNG phải QA người chạy.** |
| ⚠️ **Mã quan điểm không có trong checklist LME** | 3 mã — xem bảng bên dưới. `/review-tc` **không map được coverage** cho các mã này. |
| **Ghi chú số hiệu** | Mô tả Redmine ghi `Task: #15` · `Test case: NEW-65`, nhưng task Studio hiện hành gắn ticket 39467 là **#265** với `temp_id` `NEW-1..NEW-22`. Số trong mô tả là của lần chạy phát hiện bug, không khớp task hiện tại. |

### Mã quan điểm Studio KHÔNG có trong `framework/checklist-lme.md`

| Mã quan điểm Studio | TC dùng mã này | Ghi chú |
|---|---|---|
| `AUTH-SESSION-001` | `TC-AUTHSESSION001-01` | Không tồn tại trong 80 quan điểm tầng 1. Gần nhất trong checklist: nhóm `PERM-*` / `STATE-*` — Leader tự quyết map. |
| `API-CONTRACT-001` | `TC-APICONTRACT001-01` | Không tồn tại trong 80 quan điểm tầng 1. |
| `TOOL-KNOW-002` | `TC-TOOLKNOW002-01` | Không tồn tại trong 80 quan điểm tầng 1. Đây lại là **TC tái hiện bug gốc** → mã quan trọng nhất nhưng không map được. |

*Các mã có trong checklist: `FUNC-001` · `PERM-002` · `PERM-003` · `PERM-004` · `OUT-TRUTH-001` · `REG-SHARED-001` · `DATA-DB-001`.*

### Phân bố

| Chiều | Phân bố |
|---|---|
| `case_type` | Normal **6** · Abnormal **7** · Boundary **3** |
| `tc_group` | ui **8** · api **8** |
| `exec_mode` | auto **14** · manual **2** |
| `screen` | 通知設定 ChatWork連携 (màn liên kết ChatWork) **7** · Route AJAX cấu hình ChatWork **7** · 通知設定 (trang chính) **2** |
| `requirement_keys` | REQ-003 **6** · REQ-001 **5** · REQ-004 **2** · REQ-006 **2** · REQ-002 **1** · REQ-005 **1** |
| `priority` (Studio) | High **10** · Medium **6** |

⚠️ **Requirement Studio khai 8 REQ nhưng TC chỉ phủ 6** — `REQ-007` (dữ liệu cấu hình đã tồn tại không bị fix sửa/xoá) và `REQ-008` (lần thử ghi trái phép vẫn để lại log truy vết) **không có TC nào tham chiếu**. Để `/review-tc` đánh giá.

## Thông tin

| Trường | Giá trị |
|---|---|
| Tester viết TCs | `AI (pipeline Studio, job #818)` + 1 TC do `anhptn@mcp` |
| Ngày submit | `2026-08-28` (tạo) → `2026-09-05` (cập nhật lần cuối) |
| Version TCs | `v2` (15 TC ở version 2, TC `TC-OUTTRUTH001-02` ở version 1) |
| Link TC gốc (nếu có) | MCP LME TEST STUDIO — `task_id = 265`, ticket 39467 |

---

## TC List

| TC No. | Mã quan điểm liên kết | Loại case | Tiêu đề test case | Điều kiện tiền đề | Các bước thực hiện | Dữ liệu test/input | Kết quả mong đợi | Kết quả thực thi | Evidence thực tế | Môi trường test | Người thực hiện | Ngày thực hiện | Số ticket bug | Trạng thái đánh giá spec | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-FUNC001-01 | FUNC-001 | Normal | Chủ bot lưu URL phòng ChatWork và API token trên màn liên kết ChatWork | Đã đăng nhập trang quản trị bằng tài khoản chủ bot (người tạo bot) của bot dùng để test và đã chọn đúng bot đó. Chuẩn bị sẵn một phòng ChatWork của QA cùng một API token ChatWork hợp lệ có quyền vào phòng đó. Môi trường chạy được ra Internet tới dịch vụ ChatWork. | 1. Mở màn 「通知設定」 từ menu trái<br>2. Ở khối kênh nhận thông báo, hàng 「ChatWork（チャットワーク）」, bấm nút 「連携設定」 để mở màn 「通知設定 ChatWork連携」<br>3. Ở mục 「9.通知受け取り方法を選択」 chọn 「1つのLINE公式アカウントの通知を、自分1人だけで受け取る」<br>4. Nhập URL phòng ChatWork vào ô có gợi ý 「https://www.chatwork.com/#!rid******」 rồi bấm nút 「保存」 ngay bên cạnh ô đó<br>5. Nhập API token ChatWork vào ô token rồi bấm nút 「保存」 ngay bên cạnh ô token<br>6. Tải lại màn 「通知設定 ChatWork連携」 và đối chiếu lại nội dung hai ô vừa nhập<br>7. Đối chiếu bản ghi cấu hình thông báo của cặp bot đang chọn và tài khoản chủ bot trong cơ sở dữ liệu | URL phòng: đường dẫn phòng ChatWork của QA dạng https://www.chatwork.com/#!rid<mã phòng>. API token: token ChatWork của tài khoản QA (không ghi giá trị token vào testcase, tester lấy từ kho tài khoản test). | Sau mỗi lần bấm 「保存」 hiện thông báo nổi màu xanh 「保存しました。」, không có hộp thoại lỗi nào. Sau khi tải lại màn, ô URL phòng và ô API token vẫn giữ đúng giá trị vừa lưu. Trong cơ sở dữ liệu, bản ghi cấu hình thông báo của cặp (bot đang chọn, chủ bot) có URL phòng và API token đúng giá trị vừa nhập. Hai lời gọi lưu trả mã 200 với nội dung báo thành công kèm chính giá trị vừa lưu. | Đạt |  | LOCAL | pipeline-manual | 2026-09-03 |  |  | Studio #15211 (NEW-1) · tc_group=ui · exec_mode=auto · env_tag=local-only · author=AI · req: REQ-003 · spec: SCR-NS-02, EP-11, EP-12, TICKET-39467 · Hồi quy chống chặn nhầm (over-block) cho chủ bot: hàm kiểm quyền luôn cho qua khi tài khoản đăng nhập là admin_id của bot. Kỹ thuật: EP-11 /ajax/notify/save-url-chat-work, EP-12 /ajax/notify/save-api-token-chat-work. Không đặt env prd vì thao tác ghi cấu hình thật của khách. Trước khi lưu, hệ thống còn gọi ra ChatWork để kiểm token và quyền vào phòng, nên bắt buộc dùng URL/token thật và môi trường có Internet; nếu thiếu, testcase fail vì lý do khác chứ không phải phân quyền. |
| TC-PERM002-01 | PERM-002 | Normal | Staff có quyền màn 通知設定 lưu được URL phòng và API token trên màn liên kết ChatWork | Trong bot test đã có một nhân viên (staff) được mời và đã nhận lời mời, đang gắn với một nhóm quyền CÓ tick màn 「通知設定」. Đăng nhập bằng tài khoản staff đó và đã chọn đúng bot test. Chuẩn bị sẵn phòng ChatWork và API token ChatWork hợp lệ của QA; môi trường ra được Internet tới dịch vụ ChatWork. | 1. Mở màn 「通知設定」 từ menu trái (mục này phải hiển thị vì nhóm quyền có tick màn này)<br>2. Bấm nút 「連携設定」 ở hàng 「ChatWork（チャットワーク）」 để mở màn 「通知設定 ChatWork連携」<br>3. Nhập URL phòng ChatWork rồi bấm nút 「保存」 bên cạnh ô URL<br>4. Nhập API token ChatWork rồi bấm nút 「保存」 bên cạnh ô token<br>5. Tải lại màn và đối chiếu hai ô vừa nhập<br>6. Đối chiếu bản ghi cấu hình thông báo của cặp bot test và tài khoản staff trong cơ sở dữ liệu | Nhóm quyền của staff: tạo riêng cho lần chạy, có tick màn 「通知設定」. URL phòng và API token ChatWork của QA. | Cả hai lần bấm 「保存」 đều hiện thông báo nổi 「保存しました。」. Sau khi tải lại, hai ô giữ đúng giá trị vừa lưu. Bản ghi cấu hình thông báo của cặp (bot test, staff) lưu đúng URL phòng và API token. Không xuất hiện thông báo từ chối quyền 「この権限は許可されていません。」 ở bất kỳ bước nào. | Đạt |  | LOCAL | pipeline-manual | 2026-09-03 |  |  | Studio #15212 (NEW-2) · tc_group=ui · exec_mode=auto · env_tag=local-only · author=AI · req: REQ-003 · spec: SCR-NS-02, EP-11, EP-12, TICKET-39467 · Nhánh đối chứng chống over-block: staff có quyền màn 「通知設定」 phải tiếp tục lưu được URL/token sau khi thêm guard cho 4 route ChatWork. Kỹ thuật endpoint để trong note, không dùng PERM-001 vì trọng tâm testcase là server-side permission guard chứ không chỉ hiển thị menu. |
| TC-OUTTRUTH001-01 | OUT-TRUTH-001 | Normal | Chủ bot bấm gửi thử ChatWork và nhận được tin nhắn thử trong phòng đã cấu hình | Đăng nhập bằng tài khoản chủ bot của bot test, đã chọn đúng bot. Trên màn 「通知設定」 kênh 「ChatWork（チャットワーク）」 đang ở trạng thái BẬT. Đã lưu thành công URL phòng ChatWork và API token của QA (chạy sau testcase lưu URL/token của chủ bot). Mở sẵn phòng ChatWork đó để quan sát. | 1. Mở màn 「通知設定 ChatWork連携」<br>2. Bấm nút 「テスト送信」<br>3. Quan sát thông báo trên trang quản trị<br>4. Chuyển sang phòng ChatWork đã cấu hình và quan sát tin nhắn mới | Không nhập liệu; dùng URL phòng và API token đã lưu ở bước chuẩn bị. | Trang quản trị hiện thông báo nổi 「エルメ 通知テスト送信完了」. Trong phòng ChatWork đã cấu hình xuất hiện một tin nhắn mới có nội dung 「エルメ 通知テスト送信完了」. Không có hộp thoại lỗi 「エルメ 通知テスト送信失敗」. | Chưa test |  | local, dev, staging (dự kiến) |  |  |  |  | Studio #15213 (NEW-3) · tc_group=ui · exec_mode=manual · env_tag=local-only · author=AI · req: REQ-003 · spec: SCR-NS-02, EP-14, TICKET-39467 · Lý do manual: cần mắt người mở ứng dụng ChatWork thật để xác nhận tin nhắn đã tới đúng phòng — đây là bằng chứng duy nhất cho nhánh 'gửi thật', không thay thế được bằng mã trạng thái. Kỹ thuật: EP-14 /ajax/notify/test-send-chatwork, hệ thống gọi ra api.chatwork.com. Không chạy trên prd để tránh phát tin vào phòng ChatWork của khách hàng. |
| TC-OUTTRUTH001-02 | OUT-TRUTH-001 | Normal | Staff có quyền màn 通知設定 gửi thử ChatWork thành công | Trong bot test có một staff đã nhận lời mời và role CÓ màn 「通知設定」. Đăng nhập bằng staff đó, chọn đúng bot test. Kênh ChatWork đang bật và staff đã lưu URL phòng + API token ChatWork hợp lệ của QA. Mở sẵn đúng phòng ChatWork để quan sát tin nhận thật. | 1. Mở màn 「通知設定」 từ menu trái<br>2. Ở hàng 「ChatWork（チャットワーク）」 bấm 「連携設定」 để mở màn liên kết ChatWork<br>3. Bấm nút 「テスト送信」<br>4. Quan sát thông báo trên trang quản trị<br>5. Mở phòng ChatWork đã cấu hình và kiểm tra tin nhắn mới | Không nhập thêm dữ liệu; sử dụng URL phòng và API token hợp lệ đã cấu hình cho staff. | Thao tác không bị từ chối quyền. Trang quản trị hiện 「エルメ 通知テスト送信完了」 và phòng ChatWork đã cấu hình nhận đúng một tin nhắn thử tương ứng. Không xuất hiện HTTP 403 hoặc thông báo 「この権限は許可されていません。」. | Chưa test |  | local, dev, staging (dự kiến) |  |  |  |  | Studio #16243 (NEW-22) · tc_group=ui · exec_mode=manual · env_tag=local-only · author=anhptn@mcp (human ghi qua MCP) · req: REQ-003 · spec: SCR-NS-02, EP-14, TICKET-39467 · client_ref=task265-staff-chatwork-test-send. Regression chống over-block cho EP-14 với staff có quyền. Manual vì cần xác nhận output thật ở dịch vụ ChatWork; đây là nhánh đối ứng với case staff không quyền bị chặn trước outbound call. |
| TC-PERM002-02 | PERM-002 | Abnormal | Staff không có quyền màn 通知設定 đổi kiểu nhận thông báo ChatWork thì lựa chọn không được ghi | Staff đã nhận lời mời trong bot test, nhóm quyền KHÔNG tick màn 「通知設定」, đã đăng nhập và chọn đúng bot. Bản ghi cấu hình thông báo của cặp (bot test, staff) đang có kiểu nhận thông báo là 「1つのLINE公式アカウントの通知を、自分1人だけで受け取る」 (giá trị 0); ghi lại giá trị này để đối chiếu. | 1. Mở trực tiếp đường dẫn màn liên kết ChatWork /basic/notify-setting-chatwork<br>2. Mở bảng theo dõi mạng của trình duyệt<br>3. Ở mục 「9.通知受け取り方法を選択」 bấm chọn 「複数人のグループでエルメからの通知を受け取る」<br>4. Quan sát lời gọi phát sinh ngay khi đổi lựa chọn<br>5. Tải lại màn và xem lựa chọn nào đang được chọn<br>6. Đối chiếu kiểu nhận thông báo trong bản ghi cấu hình của cặp (bot test, staff) | Đổi từ lựa chọn 「1つのLINE公式アカウントの通知を、自分1人だけで受け取る」 sang 「複数人のグループでエルメからの通知を受け取る」. Chạy thêm một lượt nữa với lựa chọn 「複数のLINE公式アカウントの通知を分けて受け取る」 để phủ cả ba lựa chọn. | Mỗi lần đổi lựa chọn, lời gọi lưu trả mã 403 kèm thông báo 「この権限は許可されていません。」. Sau khi tải lại màn, lựa chọn quay về 「1つのLINE公式アカウントの通知を、自分1人だけで受け取る」 như trước khi thao tác. Kiểu nhận thông báo trong cơ sở dữ liệu vẫn là giá trị cũ (0 giữ nguyên 0). | Đạt |  | LOCAL | pipeline-manual | 2026-09-03 |  |  | Studio #15216 (NEW-6) · tc_group=ui · exec_mode=auto · env_tag=local-only · author=AI · req: REQ-001 · spec: SCR-NS-02, EP-13, TICKET-39467 · Thao tác này không có nút lưu riêng: chọn radio là tự động gọi lưu ngay. Giao diện không hiện thông báo thành công cũng không hiện lỗi ở nhánh này, nên oracle bắt buộc là mã 403 + giá trị sau khi tải lại + giá trị trong dữ liệu. Kỹ thuật: EP-13 /ajax/notify/save-type_notify_send_chatwork; nhánh này không gọi ra ChatWork nên chạy được ở mọi môi trường. env_scope=all |
| TC-PERM004-01 | PERM-004 | Boundary | Thu hồi màn 通知設定 khỏi nhóm quyền khi staff đang mở màn liên kết ChatWork thì thao tác lưu tiếp theo bị chặn ngay | Chuẩn bị hai trình duyệt/hai hồ sơ riêng: một đăng nhập bằng chủ bot, một đăng nhập bằng staff của cùng bot test. Ban đầu nhóm quyền của staff CÓ tick màn 「通知設定」. Staff đang mở sẵn màn 「通知設定 ChatWork連携」 và chưa tải lại trang. | 1. Ở cửa sổ staff: mở màn 「通知設定 ChatWork連携」 và để nguyên, không tải lại<br>2. Ở cửa sổ chủ bot: vào màn quản lý nhân viên, bỏ tick màn 「通知設定」 khỏi nhóm quyền đang gán cho staff rồi lưu<br>3. Quay lại cửa sổ staff (vẫn là trang đã mở từ trước), nhập một URL phòng ChatWork đúng định dạng vào ô URL<br>4. Bấm nút 「保存」<br>5. Quan sát lời gọi trong bảng theo dõi mạng của trình duyệt<br>6. Đối chiếu URL phòng trong bản ghi cấu hình thông báo của cặp (bot test, staff) | Chạy hai biến thể của cùng tình huống mất quyền: (1) bỏ tick màn 「通知設定」 khỏi nhóm quyền của staff; (2) xoá hẳn staff khỏi danh sách nhân viên của bot (lời mời không còn hiệu lực). Cả hai đều thao tác từ cửa sổ chủ bot khi cửa sổ staff đang mở màn. | Ở cả hai biến thể, thao tác 「保存」 của staff trả mã 403 kèm thông báo 「この権限は許可されていません。」 dù staff chưa tải lại trang. Không hiện thông báo nổi 「保存しました。」. URL phòng trong cơ sở dữ liệu không đổi. Ở biến thể xoá staff khỏi bot, các thao tác tiếp theo của staff bị đưa về màn chọn bot vì bot đang chọn đã bị gỡ khỏi phiên. | Đạt |  | LOCAL | pipeline-manual | 2026-09-03 |  |  | Studio #15218 (NEW-8) · tc_group=ui · exec_mode=auto · env_tag=local-only · author=AI · req: REQ-004 · spec: SCR-NS-02, EP-11, TICKET-39467 · Regression bổ sung cho việc thu hồi quyền khi session đang mở. Giữ ở mức Medium vì ticket không nêu trực tiếp yêu cầu đổi quyền tức thời; dùng để bắt rủi ro session cũ tiếp tục ghi sau khi quyền bị thu hồi. Priority Studio=Medium. env_scope=all |
| TC-AUTHSESSION001-01 | AUTH-SESSION-001 | Boundary | Cấp lại màn 通知設定 cho nhóm quyền thì staff lưu được ngay trong cùng phiên đăng nhập | Chạy tiếp ngay sau testcase thu hồi quyền, biến thể bỏ tick màn (staff vẫn còn trong bot). Staff vẫn đang đăng nhập, chưa đăng xuất và chưa tải lại trang. | 1. Ở cửa sổ chủ bot: tick lại màn 「通知設定」 cho nhóm quyền đang gán cho staff rồi lưu<br>2. Quay lại cửa sổ staff, không đăng xuất, tải lại màn 「通知設定 ChatWork連携」<br>3. Ở mục 「9.通知受け取り方法を選択」 đổi sang một lựa chọn khác lựa chọn hiện tại<br>4. Quan sát lời gọi phát sinh<br>5. Tải lại màn và đối chiếu lựa chọn đang được chọn<br>6. Đối chiếu kiểu nhận thông báo trong bản ghi cấu hình của cặp (bot test, staff) | Đổi kiểu nhận thông báo sang 「複数人のグループでエルメからの通知を受け取る」. | Lời gọi lưu trả mã 200 báo thành công kèm giá trị kiểu nhận thông báo vừa chọn, không còn mã 403. Sau khi tải lại màn, lựa chọn mới được giữ. Kiểu nhận thông báo trong cơ sở dữ liệu đã đổi sang giá trị mới. Staff không phải đăng xuất/đăng nhập lại để quyền có hiệu lực. | Đạt |  | LOCAL | pipeline-manual | 2026-09-03 |  |  | Studio #15219 (NEW-9) · tc_group=ui · exec_mode=auto · env_tag=local-only · author=AI · req: REQ-004, REQ-003 · spec: SCR-NS-02, EP-11, EP-13, TICKET-39467 · ⚠️ Mã quan điểm AUTH-SESSION-001 KHÔNG có trong framework/checklist-lme.md. Regression bổ sung cho việc cấp lại quyền trong cùng phiên. Giữ Medium vì không phải acceptance criterion trực tiếp của bug; mục tiêu là bảo đảm guard không giữ trạng thái quyền cũ sau khi quyền được cấp lại. Priority Studio=Medium. env_scope=all |
| TC-REGSHARED001-01 | REG-SHARED-001 | Normal | Chủ bot bật kênh ChatWork và đổi tần suất gom thông báo trên màn 通知設定 vẫn lưu bình thường | Đăng nhập bằng tài khoản chủ bot của bot test và đã chọn đúng bot. Ghi lại trạng thái hiện tại của ba kênh nhận thông báo và ba ô tần suất trên màn 「通知設定」. | 1. Mở màn 「通知設定」 từ menu trái<br>2. Bật công tắc kênh 「ChatWork（チャットワーク）」<br>3. Đổi ô tần suất của hàng ChatWork sang một giá trị khác giá trị hiện tại<br>4. Chuyển sang tab 「1:1チャット」 ở menu bên trái và tick thêm một mục trong danh sách<br>5. Tải lại màn 「通知設定」 và đối chiếu ba thay đổi vừa thực hiện<br>6. Đối chiếu bản ghi cấu hình thông báo của cặp (bot test, chủ bot) trong cơ sở dữ liệu | Tần suất ChatWork: đổi từ 「リアルタイム」 sang 「15分ごと」. Tab 「1:1チャット」: tick thêm một mục đang bỏ trống. | Các thao tác lưu tự động đều trả kết quả thành công, không xuất hiện thông báo 「この権限は許可されていません。」. Sau khi tải lại màn, công tắc kênh ChatWork, ô tần suất và mục vừa tick đều giữ đúng trạng thái mới. Bản ghi cấu hình thông báo của chủ bot phản ánh đúng ba thay đổi này. | Đạt |  | LOCAL | pipeline-manual | 2026-09-03 |  |  | Studio #15230 (NEW-20) · tc_group=ui · exec_mode=auto · env_tag=local-only · author=AI · req: REQ-006 · spec: SCR-NS-01, EP-07, TICKET-39467 · regression. Smoke regression cho các route lưu cài đặt nhận thông báo đã có guard từ trước trong cùng controller. Không dùng RULE-12 làm viewpoint testcase; RULE-12 chỉ định cách chọn phạm vi regression. Priority Studio=Medium. env_scope=all |
| TC-APICONTRACT001-01 | API-CONTRACT-001 | Normal | Chủ bot gọi thẳng bốn route cấu hình ChatWork thì kết quả trả về giữ nguyên như trước khi sửa | Có phiên đăng nhập hợp lệ của tài khoản chủ bot và phiên đó đã chọn đúng bot test (mở màn quản trị của bot một lần để phiên ghi nhận bot đang chọn). Chuẩn bị URL phòng ChatWork và API token hợp lệ của QA; môi trường ra được Internet tới dịch vụ ChatWork. | 1. Dùng phiên đăng nhập của chủ bot, gọi lần lượt bốn route: lưu URL phòng (/ajax/notify/save-url-chat-work), lưu API token (/ajax/notify/save-api-token-chat-work), đổi kiểu nhận thông báo (/ajax/notify/save-type_notify_send_chatwork), gửi thử (/ajax/notify/test-send-chatwork)<br>2. Ghi lại mã trạng thái và nội dung trả về của từng lời gọi<br>3. Đối chiếu bản ghi cấu hình thông báo của cặp (bot test, chủ bot) sau mỗi lời gọi ghi dữ liệu | Lưu URL phòng: URL phòng ChatWork của QA. Lưu API token: token ChatWork hợp lệ của QA. Đổi kiểu nhận thông báo: giá trị 1. Gửi thử: không tham số. | Cả bốn lời gọi đều trả mã 200 và không xuất hiện thông báo 「この権限は許可されていません。」. Ba lời gọi ghi dữ liệu trả nội dung báo thành công kèm đúng trường vừa lưu (URL phòng / API token / kiểu nhận thông báo). Bản ghi cấu hình thông báo của cặp (bot test, chủ bot) chứa đúng ba giá trị vừa gửi. Lời gọi gửi thử trả kết quả thành công. | Đạt |  | LOCAL | pipeline-manual | 2026-09-03 |  |  | Studio #15220 (NEW-10) · tc_group=api · exec_mode=auto · env_tag=local-only · author=AI · req: REQ-003 · spec: EP-11, EP-12, EP-13, EP-14, TICKET-39467 · ⚠️ Mã quan điểm API-CONTRACT-001 KHÔNG có trong framework/checklist-lme.md. Bảo vệ hợp đồng response cho phía giao diện: lớp kiểm quyền mới chỉ được thêm nhánh từ chối, không được đổi dạng dữ liệu trả về của nhánh thành công. Nhóm route /ajax/* được miễn kiểm tra token chống giả mạo biểu mẫu nên chỉ cần cookie phiên là gọi được. Không chạy trên prd; hai route lưu URL và lưu token cần Internet tới ChatWork vì hệ thống kiểm tra token và quyền vào phòng trước khi lưu. |
| TC-PERM002-03 | PERM-002 | Abnormal | Gọi thẳng route lưu URL phòng ChatWork bằng phiên staff không quyền thì bị từ chối và dữ liệu không đổi | Phiên đăng nhập của staff KHÔNG có màn 「通知設定」 trong nhóm quyền, đã chọn đúng bot test. Bản ghi cấu hình thông báo của cặp (bot test, staff) đã tồn tại và đã ghi lại giá trị URL phòng hiện tại. | 1. Dùng phiên của staff, gọi thẳng route lưu URL phòng ChatWork (/ajax/notify/save-url-chat-work) mà không đi qua màn hình<br>2. Ghi lại mã trạng thái và nội dung trả về<br>3. Đọc lại URL phòng trong bản ghi cấu hình thông báo của cặp (bot test, staff)<br>4. Đếm số bản ghi cấu hình thông báo của bot test trước và sau khi gọi | URL phòng đúng định dạng, đặt riêng cho lần chạy để dễ tra trong dữ liệu. | Lời gọi trả mã 403, nội dung báo không thành công kèm thông báo 「この権限は許可されていません。」. URL phòng trong bản ghi của cặp (bot test, staff) giữ nguyên giá trị cũ, không chứa giá trị vừa gửi. Số bản ghi cấu hình thông báo của bot test không tăng. | Đạt |  | LOCAL | pipeline-manual | 2026-09-03 |  |  | Studio #15223 (NEW-13) · tc_group=api · exec_mode=auto · env_tag=local-only · author=AI · req: REQ-001 · spec: EP-11, TICKET-39467 · Chứng minh không bypass được bằng cách bỏ qua giao diện — đúng kiểu tấn công mà ticket mô tả. Nhóm route /ajax/* miễn kiểm tra token chống giả mạo biểu mẫu nên chỉ cần cookie phiên. Phân biệt: mã 200 kèm 「ChatworkルームURLが正しくありません。」 là lớp kiểm tra định dạng, không phải chặn quyền. env_scope=all |
| TC-PERM002-04 | PERM-002 | Abnormal | Gọi thẳng route lưu API token ChatWork bằng phiên staff không quyền thì bị từ chối và token không đổi | Phiên đăng nhập của staff KHÔNG có màn 「通知設定」 trong nhóm quyền, đã chọn đúng bot test. Đã ghi lại API token hiện có trong bản ghi cấu hình của cặp (bot test, staff). | 1. Dùng phiên của staff, gọi thẳng route lưu API token ChatWork (/ajax/notify/save-api-token-chat-work)<br>2. Ghi lại mã trạng thái và toàn bộ nội dung trả về<br>3. Đọc lại API token trong bản ghi cấu hình của cặp (bot test, staff) | Chuỗi token do tester tự đặt riêng cho lần chạy (không phải token thật). | Lời gọi trả mã 403 kèm thông báo 「この権限は許可されていません。」. API token trong dữ liệu giữ nguyên giá trị cũ. Nội dung trả về không chứa token cũ, không chứa URL phòng hay bất kỳ thông tin cấu hình nào của bot. | Đạt |  | LOCAL | pipeline-manual | 2026-09-03 |  |  | Studio #15224 (NEW-14) · tc_group=api · exec_mode=auto · env_tag=local-only · author=AI · req: REQ-001 · spec: EP-12, TICKET-39467 · Core permission case cho EP-12: staff không có quyền màn 「通知設定」 phải bị server chặn trước bước kiểm token với ChatWork và token trong DB không đổi. Không dùng SEC-001 vì ticket không phải IDOR/PII; trọng tâm là bypass quyền endpoint. env_scope=all |
| TC-TOOLKNOW002-01 | TOOL-KNOW-002 | Abnormal | Tái hiện bug gốc: staff không có quyền đổi type_notify_send_chatwork thì bị 403 và giá trị DB không đổi | Có một staff đã được mời vào bot test và role KHÔNG có màn 「通知設定」. Đăng nhập bằng staff đó, chọn đúng bot test. Xác nhận bản ghi cấu hình thông báo của cặp (bot test, staff) đang tồn tại với type_notify_send_chatwork = 0. | 1. Dùng đúng phiên staff không quyền, gọi /basic/notify-setting-receive và xác nhận request bị từ chối để chứng minh role hiện tại thực sự không có quyền màn 「通知設定」<br>2. Không đổi phiên đăng nhập, gọi thẳng /ajax/notify/save-type_notify_send_chatwork với giá trị 1<br>3. Ghi lại HTTP status và response của request đổi kiểu nhận ChatWork<br>4. Query lại bản ghi cấu hình thông báo của đúng cặp (bot test, staff) và đọc type_notify_send_chatwork | type_notify_send_chatwork: 1. Giá trị trước khi chạy: 0. | Request /basic/notify-setting-receive bị từ chối theo quyền hiện tại. Request /ajax/notify/save-type_notify_send_chatwork trả HTTP 403 kèm thông báo 「この権限は許可されていません。」. Sau request, type_notify_send_chatwork vẫn bằng 0; không có thay đổi 0→1. | Đạt |  | LOCAL | pipeline-manual | 2026-09-03 |  |  | Studio #15225 (NEW-15) · tc_group=api · exec_mode=auto · env_tag=local-only · author=AI · req: REQ-001 · spec: EP-13, TICKET-39467 · ⚠️ Mã quan điểm TOOL-KNOW-002 KHÔNG có trong framework/checklist-lme.md. Đây là testcase bắt buộc tái hiện đúng bug report Redmine #39467: staff không quyền, route chuẩn của màn bị 403 nhưng route ChatWork từng ghi được type_notify_send_chatwork 0→1. Sau fix phải chặn đúng route này và DB không đổi. Không trộn thêm input 2/99 vào case reproduce; validation giá trị không phải mục tiêu ticket. env_scope=all |
| TC-OUTTRUTH001-03 | OUT-TRUTH-001 | Abnormal | Gọi thẳng route gửi thử ChatWork bằng phiên staff không quyền thì hệ thống không gọi ra dịch vụ ChatWork | Phiên đăng nhập của staff KHÔNG có màn 「通知設定」 trong nhóm quyền, đã chọn đúng bot test. Cấu hình của cặp (bot test, staff) đủ điều kiện gửi thử nếu không bị chặn. Chuẩn bị cách quan sát outbound ChatWork ở tầng server (mock/stub/proxy hoặc log request ra ngoài) để tự động xác nhận không phát sinh call. | 1. Dùng phiên của staff, gọi thẳng /ajax/notify/test-send-chatwork<br>2. Ghi lại HTTP status và response<br>3. Kiểm tra bộ quan sát outbound phía server cho request vừa chạy | Không tham số. | Request trả HTTP 403 kèm thông báo 「この権限は許可されていません。」, không phải 200 với kết quả gửi thử. Bộ quan sát outbound xác nhận không có request nào được gửi tới dịch vụ ChatWork cho lần thao tác này. | Đạt |  | LOCAL | pipeline-manual | 2026-09-03 |  |  | Studio #15226 (NEW-16) · tc_group=api · exec_mode=auto · env_tag=local-only · author=AI · req: REQ-002 · spec: EP-14, TICKET-39467 · Giữ auto bằng cách kiểm outbound ở ranh giới server; không yêu cầu mở phòng ChatWork bằng mắt. Mục tiêu là chứng minh guard chặn trước khi gọi dịch vụ ngoài. Việc gửi/nhận thật đã có testcase manual riêng cho nhánh có quyền. env_scope=all |
| TC-PERM003-01 | PERM-003 | Abnormal | Staff có quyền ở bot khác nhưng đang chọn bot không được cấp quyền thì bị từ chối | Một tài khoản staff được mời vào hai bot: ở bot A nhóm quyền CÓ màn 「通知設定」, ở bot B nhóm quyền KHÔNG có màn này. Đăng nhập bằng tài khoản đó rồi chọn bot B ở màn chọn bot. Ghi lại URL phòng hiện có trong bản ghi cấu hình của cặp (bot B, staff). | 1. Với phiên đang chọn bot B, gọi route lưu URL phòng ChatWork<br>2. Ghi lại mã trạng thái và nội dung trả về<br>3. Đọc lại URL phòng trong bản ghi cấu hình của cặp (bot B, staff)<br>4. Chuyển phiên sang chọn bot A rồi gọi lại đúng route đó<br>5. Đọc URL phòng trong bản ghi cấu hình của cặp (bot A, staff) | URL phòng ChatWork của QA, đặt riêng cho lần chạy; dùng cùng một giá trị cho cả hai lượt gọi để dễ đối chiếu. | Lượt gọi khi đang chọn bot B trả mã 403 kèm thông báo 「この権限は許可されていません。」 và URL phòng của cặp (bot B, staff) không đổi. Lượt gọi khi đang chọn bot A trả kết quả thành công và URL phòng của cặp (bot A, staff) được cập nhật. Cấu hình của bot B vẫn không bị ảnh hưởng sau cả hai lượt. | Đạt |  | LOCAL | pipeline-manual | 2026-09-03 |  |  | Studio #15228 (NEW-18) · tc_group=api · exec_mode=auto · env_tag=local-only · author=AI · req: REQ-005 · spec: EP-11, TICKET-39467 · Kiểm phạm vi của lớp kiểm quyền: quyền được tính theo bot đang chọn trong phiên, không phải theo tài khoản. Đây cũng là đối chứng âm dạng cách ly nhiều bot. Lượt gọi thành công cần Internet tới ChatWork (hệ thống kiểm quyền vào phòng trước khi lưu) nên không xếp env prd và cần môi trường ra được ngoài; riêng lượt bị từ chối thì chạy được ở mọi môi trường. Priority Studio=Medium. |
| TC-DATADB001-01 | DATA-DB-001 | Boundary | Staff không quyền thao tác khi chưa có bản ghi cấu hình thông báo thì không bản ghi nào được tạo mới | Staff mới được mời vào bot test với nhóm quyền KHÔNG có màn 「通知設定」 và CHƯA từng có bản ghi cấu hình thông báo cho cặp (bot test, staff) — xác nhận trước khi chạy là chưa tồn tại bản ghi nào cho cặp này. Lưu ý không mở màn 「通知設定」 bằng tài khoản staff trước khi chạy vì thao tác nạp dữ liệu cài đặt sẽ tự sinh bản ghi. | 1. Xác nhận trong dữ liệu chưa có bản ghi cấu hình thông báo cho cặp (bot test, staff)<br>2. Dùng phiên của staff, gọi lần lượt ba route ghi cấu hình ChatWork: lưu URL phòng, lưu API token, đổi kiểu nhận thông báo<br>3. Ghi lại mã trạng thái của từng lời gọi<br>4. Kiểm tra lại trong dữ liệu xem có bản ghi cấu hình thông báo nào được tạo cho cặp (bot test, staff) không | URL phòng đúng định dạng, chuỗi token tự đặt và kiểu nhận thông báo giá trị 1 — cả ba đều đặt giá trị riêng cho lần chạy để dễ tra. | Cả ba lời gọi trả mã 403 kèm thông báo 「この権限は許可されていません。」. Sau khi chạy xong vẫn KHÔNG tồn tại bản ghi cấu hình thông báo nào cho cặp (bot test, staff). Tổng số bản ghi cấu hình của bot test không đổi. | Đạt |  | LOCAL | pipeline-manual | 2026-09-03 |  |  | Studio #15229 (NEW-19) · tc_group=api · exec_mode=auto · env_tag=local-only · author=AI · req: REQ-001 · spec: EP-11, EP-12, EP-13, TICKET-39467 · Đây là nhánh biên dễ bị bỏ sót: ba endpoint này tự tạo bản ghi mới khi chưa có, nên nếu lớp kiểm quyền đặt sai chỗ thì staff không quyền vẫn sinh được bản ghi mới dù không sửa được bản ghi cũ. Cảnh báo tiền điều kiện: route nạp dữ liệu cài đặt của màn tự tạo bản ghi mặc định, nên chỉ cần staff mở màn 「通知設定」 một lần là mất trạng thái 'chưa có bản ghi'. Priority Studio=Medium. env_scope=all |
| TC-REGSHARED001-02 | REG-SHARED-001 | Abnormal | Ba thao tác lưu cài đặt nhận thông báo đã có kiểm quyền từ trước vẫn từ chối staff không quyền | Phiên đăng nhập của staff KHÔNG có màn 「通知設定」 trong nhóm quyền, đã chọn đúng bot test. Đã ghi lại các giá trị hiện tại trong bản ghi cấu hình của cặp (bot test, staff): ba cờ kênh, ba tần suất và danh sách lựa chọn của nhóm 1:1チャット. | 1. Dùng phiên của staff, gọi route lưu kênh nhận thông báo và tần suất (/basic/notify-setting-receive)<br>2. Gọi route lưu lựa chọn theo từng nhóm (/basic/notify-setting-receive-page)<br>3. Gọi route lưu cài đặt bản cũ (/basic/notify-setting)<br>4. Ghi lại mã trạng thái và nội dung trả về của từng lời gọi<br>5. Đọc lại toàn bộ các giá trị đã ghi ở bước chuẩn bị trong bản ghi cấu hình của cặp (bot test, staff) | Lưu kênh + tần suất: bật cả ba kênh, tần suất đặt giá trị 3. Lưu theo nhóm: nhóm 「chat_11」 kèm danh sách lựa chọn khác danh sách hiện tại. Lưu bản cũ: bộ tham số tối thiểu của biểu mẫu cũ. | Cả ba lời gọi đều trả mã 403 kèm thông báo 「この権限は許可されていません。」. Toàn bộ giá trị trong bản ghi cấu hình của cặp (bot test, staff) giữ nguyên như trước khi gọi. Số bản ghi cấu hình của bot test không tăng. | Đạt |  | LOCAL | pipeline-manual | 2026-09-03 |  |  | Studio #15231 (NEW-21) · tc_group=api · exec_mode=auto · env_tag=local-only · author=AI · req: REQ-006 · spec: EP-07, EP-08, EP-09, TICKET-39467 · regression. Hồi quy phần KHÔNG bị sửa nhưng nằm cùng tệp điều khiển: ba route này đã có lớp kiểm quyền từ trước và là mẫu mà bản fix sao chép. Gộp ba route vào một testcase vì mục tiêu chỉ là chứng minh không hồi quy, khác với bốn route ChatWork phải tách riêng do đó là bề mặt vừa thay đổi. Ghi chú: route lưu bản cũ khi thành công sẽ chuyển trang chứ không trả dữ liệu, nhưng nhánh từ chối quyền vẫn trả mã 403 dạng dữ liệu. Priority Studio=Medium. env_scope=all |

---

## Member tự check

`<member điền sau khi review>` — TCs trong file này **do AI sinh trên Studio**, chưa có member nào tự check.

<!-- Source: MCP LME TEST STUDIO task_id=265 (ticket Redmine #39467), testcase_list — 16 TC, fetch lúc 2026-09-08. Redmine KHÔNG có Section "Link TCs" nên dùng fallback Studio theo /new-task BƯỚC 6b. TCs read-only — KHÔNG sửa ở file này. -->
