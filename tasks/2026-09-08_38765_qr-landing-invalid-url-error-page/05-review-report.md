# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | `(1) MCP LME TEST STUDIO — task #279, ticket 38765, branch ai_small_38765` |
| Tổng số TC review | `26` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `8/13 vùng ảnh hưởng đủ TC · 6 GAP · 2 RISK`

> Chiều `diff code` lấy từ Studio `task_get_context` — `spec_delta` (`diffAvailable = true`, 2 file, `+122/-0`: `app/Exceptions/Handler.php` +73, `resources/views/errors/invalid_link.blade.php` +49) và `dev_impact` (7 điểm rủi ro hồi quy do Studio suy từ diff).

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | `BUG` root cause — ca lỗi thực tế KH báo: URL **đúng route nhưng mã sai / đối tượng đã bị xoá** (`/s/<mã sai>`, `/l/<mã sai>`, `/landing-qr/<mã sai>`, `/basic/form-answer/form-render/<key sai>`, và **toàn bộ link phân phối cho friend của form · QR · salon · lesson · event · item · shorten sau khi đối tượng bị xoá**) | `diff code` | `TC-TOOLSCOPE001-01` (Studio #16560) — **skip** | `GAP — không TC nào chấm được`. Log run #1039 + #1010 chứng minh các đường vào này **khớp route** nên không ném `NotFoundHttpException`; `CollectController@getInfo` / `QRCodeController@qrLanding` / `FormAnswerController` tự `redirect()->route('404')` ⇒ **guard #38765 không bao giờ chạy**, user vẫn ra `/error/404` CŨ **có nút 「TOPページへ戻る」 trỏ `/admin/home`** = đúng triệu chứng KH phàn nàn. Fix hiện tại **có thể chưa giải quyết ca lỗi mà ticket mô tả**. ⚠️ **Leader đã chốt expected** = phải ra trang 「お探しのページが見つかりませんでした。」 → đã bổ sung **8 TC** (`TC-COMPATLEGACY001-02`, `TC-LIFFENTRY001-04…-11`) phủ đủ 7 nhóm link ở §5; theo bằng chứng hiện có, nhóm TC này **dự kiến FAIL** trên code hiện tại | `[BLOCKER]` |
| G2 | `T3 — form.lmes.jp` + danh sách domain mặc định | `dev-impact` + `diff code` | `không có` | `GAP — 0 TC`. Danh sách hard-code chỉ 3 host `s.lmes.jp` / `sl.lmes.jp` / `form.lmes.jp`. **Domain form thế hệ CŨ `step3.lmes.jp` KHÔNG nằm trong danh sách** ⇒ link hỏng trên domain cũ vẫn redirect về màn đăng nhập, bug gốc còn nguyên với link đã phát tán. Kho TC `TC-FORM-457` (COMPAT-LEGACY-001, PRODUCTION, bug #29719) xác nhận `step3.lmes.jp` **vẫn đang dùng thật** | `[BLOCKER]` |
| G3 | `F1 — Handler::isNotFoundLikeException()` nhánh `ModelNotFoundException` | `diff code` | `không có` | `GAP — 0 TC`. `dev_impact` Studio nêu đích danh: *"isNotFoundLikeException bắt cả ModelNotFoundException & MethodNotAllowedHttpException ⇒ cần verify không nuốt nhầm lỗi đáng lẽ hiển thị khác"*. TC hiện có mới trigger 3/4 nhóm: route-not-found (#16540), `MethodNotAllowed` (#16545), `abort(404)` chủ động (#16546). **Chưa TC nào trigger `findOrFail` trên domain public** | `[MAJOR]` |
| G4 | `T3 — form.lmes.jp` / trang lỗi nghiệp vụ khi **bot hết hạn** | `diff code` | `không có` | `GAP — 0 TC`. Kho `TC-FORM-415` (ENV-001, PRODUCTION): bot hết hạn quá mốc **18 giờ** ⇒ mở form phải hiện **màn thông báo lỗi** ở cả 6 đường mở. Nếu màn đó phát sinh qua nhóm not-found trên `form.lmes.jp`, guard mới sẽ thay bằng 「お探しのページが見つかりませんでした。」 ⇒ **mất thông điệp nghiệp vụ**. ⚠️ Leader yêu cầu phủ **đủ loại domain** cho ca này → đã mở thành **ma trận domain** ở §5: `TC-REGSHARED001-11` (`form.lmes.jp`) + `-13` (`s.lmes.jp`) + `-14` (link rút gọn 2 thế hệ) + `-15` (domain riêng của bot) + `-16` (`step3.lmes.jp`) + `-17` (`step.lme.jp` quản trị) + `-18` (mọi cấu hình danh sách domain) | `[MAJOR]` |
| G5 | `F1 — Handler::render` áp cho **mọi** route trên domain public | `diff code` | `không có` | `GAP — 0 TC`. Spec FA-017 §1.2 liệt kê 4 trang public; TC mới chạm `/landing-qr` + `/s/` + `/l/` + `/form-render`. **Chưa TC nào chạm `/open-mobile/{type}/{id}`, `/open-external-browser/{type}/{id}`, `/landing/page-intro/{code}/{u_code}`** — trong đó spec §9.2 ghi rõ Public Redirect Page phục vụ **8 tính năng** (booking calendar/salon/event, product, form_answer) | `[MAJOR]` |
| G6 | `F1` — kết luận trên hạ tầng thật | `dev-impact` | `TC-REGSHARED001-07` (#16582), `TC-REGSHARED001-03` (#16619) | `RISK — cả 2 TC production đều skip`. **0/26 TC chạy hạ tầng thật**: auto chạy `php -S` cổng 18400, 3 domain public + `step.lme.jp` đều map bằng `/etc/hosts` về chính server test. Chính runner ghi nhận `public/test` tồn tại nên `php -S` trả 404 tĩnh, request **không vào tới Laravel** ⇒ nginx/CDN production có thể chặn trước, kết quả local không suy ra được (RULE-08) | `[MAJOR]` |
| G7 | `F2 — invalid_link.blade.php` | `diff code` | `TC-OUTTRUTH001-01` (#16541) | `RISK — REQ-002 yêu cầu meta robots noindex,nofollow nhưng TC không assert`. Log actual của #16541 chỉ ghi text / số thẻ `<a>` / 375px, không có dòng nào về `noindex` | `[MINOR]` |
| G8 | `D2 — danh sách domain` / **domain rút gọn riêng của từng bot** (`bots.domain_url_shorten`) | `diff code` | `không có` | `GAP — 0 TC`. Danh sách domain của bản fix chỉ gồm **3 host hard-code** + host suy từ `URL_OUTSIDE_STEP`/`URL_OUTSIDE_STEP_V2` + `INVALID_LINK_DOMAINS` — **không đọc `bots.domain_url_shorten`**. Spec [bot-edit](../../spec-features/admin/bot-edit/web/api-spec.md) xác nhận màn 「アカウント設定」 có trường **domain rút gọn URL tùy chỉnh** (validate `url` + kiểm DNS), spec [qr-landing](../../spec-features/admin/qr-landing/web/logic-spec.md) xác nhận link chia sẻ QR giới thiệu dựng theo thứ tự `bots.domain_url_shorten` → `env(URL_OUTSIDE_STEP)` → link lưu sẵn ⇒ **link đã phát cho friend có thể nằm trên domain riêng của khách**; link hỏng trên domain đó vẫn đẩy friend về màn đăng nhập = **bug gốc còn nguyên**. Cần Dev đếm số bot có `domain_url_shorten` khác rỗng trên production để chốt mức độ | `[MAJOR]` |

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `11 quan điểm Trigger khớp task · 4 chưa cover đủ`

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `COMPAT-LEGACY-001` | `Cao` | `GAP — 0 TC`. RULE-09 nêu đích danh cặp version-up `form s.lmes.jp vs step3.lmes.jp`; fix còn đọc 2 thế hệ cấu hình link rút gọn (`URL_OUTSIDE_STEP` / `URL_OUTSIDE_STEP_V2`) nhưng không TC nào test **nhánh cũ**. Lấp bởi `G2` | `[BLOCKER]` |
| Q2 | `ENV-003` | `Cao` | `RISK — có 4 TC (#16555/#16556/#16557/#16558) nhưng 100% chạy env local, 0 TC chạy production`; 2 TC scope production đều `skip` ("Không test được dưới dev"). Quan điểm này **cấm** đánh × với lý do "staging đã pass" (RULE-08) — ở đây còn chưa tới staging. Cùng gốc với `G6`; kéo theo `DEPLOY-LIVE-001` chưa có smoke sau deploy | `[MAJOR]` |
| Q3 | `LIFF-ENTRY-001` | `Cao` | `RISK — TC duy nhất (#16548) không dùng được làm bằng chứng`: yêu cầu iOS + Android thật trên domain public đã deploy, chính `Kết quả mong đợi` ghi *"thiếu một trong hai thiết bị thì phải đánh blocked, không được pass một phần"*, nhưng thực tế chấm `pass` ở `env=local`, `evidence = []`, actual 1 câu. Ngoài ra chỉ có `Normal`, **thiếu `Abnormal` + `Boundary`** (RULE-01). Trigger của quan điểm này liệt kê đúng 7 nhóm đối tượng Leader yêu cầu phủ (form · item · event · salon · lesson · QR code · conversion) — hiện **chỉ QR** có TC. Lấp bởi `TC-LIFFENTRY001-02…-11` ở §5 | `[MAJOR]` |
| Q4 | `DATA-CACHE-001` | `Trung bình → Cao (output user-facing)` | `GAP — 0 TC`. Trigger khớp: chức năng dùng **short link** + phát sinh response HTML mới trên domain public. Chưa TC nào kiểm header cache của trang 404 mới, và nguy cơ CDN/proxy cache trang lỗi cho URL sau đó trở thành hợp lệ | `[MAJOR]` |

---

## 3. TC trùng lặp nội dung

Đã rà **26** TC. Phát hiện **3 nhóm chồng lấn**, tất cả đều **KHÔNG đề nghị xóa** — chỉ gộp / thu hẹp, vì xóa sẽ mất cover.

| Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| DUP-1 | `TC-TOOLKNOW002-01` (#16540) | `TC-OUTTRUTH001-01` (#16541) — **thu hẹp**, không xóa | `DUP-SUBSET` | Cùng đối tượng + thao tác (mở `/test` trên `s.lmes.jp` chưa đăng nhập) · cùng tiền đề · expected trùng ở 3 assert: có 「お探しの…」 / không 「TOPページへ戻る」 / không dòng hướng dẫn phụ. #16541 chỉ **thêm** h1 + số thẻ `<a>`=0 + 375px | `[MINOR]` |
| DUP-2 | `TC-REGSHARED001-07` (#16582) | `TC-REGSHARED001-03` (#16619) — **gộp** | `DUP-SUBSET` | #16619 = hợp của #16550 (step.lme.jp giữ hành vi cũ) + #16540 (s.lmes.jp ra 404), chỉ khác ở chỗ dùng **cùng 1 path** trên 2 domain. Cả #16619 và #16582 đều là smoke trên domain thật và đều đang `skip` cùng lý do thiếu env | `[MINOR]` |
| DUP-3 | `TC-REGURL001-01` (#16559) | `TC-REGSHARED001-04` (#16616) — **gộp** | `DUP-SUBSET` | Cùng quan điểm regression link hợp lệ · cùng `Normal` · cùng đối tượng (URL hợp lệ trên `s.lmes.jp`) · expected tương đương "mở đúng đích, không ra trang link lỗi". #16559 chạy auto có 3 screenshot; #16616 chấm `pass` thủ công **không actual, không evidence** | `[MINOR]` |

- **Gate đã chạy**: giả định xóa `#16541` / `#16619` / `#16616` → coverage §1 + quan điểm §2 **KHÔNG** còn nguyên (mất REQ-002 responsive, mất TC production duy nhất cho `step.lme.jp`, mất ref `landing/SCR-01`) → **đổi toàn bộ sang GỘP**, không xóa TC nào.
- `DUP-INFLATE` che GAP: `không có` — nhưng lưu ý #16616/#16617/#16618 đều `pass` mà không actual/evidence, khiến `T1 QR Landing` **trông như đủ**; đã hạ về vấn đề chất lượng nguồn ở §4 `I1`.
- `DUP-CONFLICT`: `không có giữa 2 TC` — mâu thuẫn thực tế nằm giữa **TC và journal Dev**, đã đưa vào §6.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | `#16548`, `#16616`, `#16617`, `#16618` (4 TC manual) | **Kết quả `pass` không có giá trị kết luận**: cả 4 chấm ở `env = local` dù `env_scope = staging,prd`; `evidence = []`; 3/4 có `actual = null` (#16616/#16617/#16618). #16548 còn yêu cầu **iOS + Android thật** trên domain public đã deploy và tự ghi trong expected là thiếu thiết bị phải `blocked`. Vi phạm RULE-02 (evidence) + RULE-06 (output cuối trên thiết bị thật) + RULE-08 | Huỷ 4 kết quả `pass`, đưa về `Chưa test`; chạy lại trên staging/production với thiết bị thật, đính screenshot/log |
| I2 | `[MAJOR]` | Toàn bộ 26 TC | **0 TC chạy hạ tầng thật** (RULE-08). Auto chạy trên `php -S` cổng 18400; 3 domain public và `step.lme.jp` đều map `/etc/hosts` về chính server test. Runner tự ghi nhận: *"public/test tồn tại nên php -S trả 404 tĩnh cho /test, request không vào tới Laravel"* ⇒ tầng web server production (nginx/CDN) chưa được kiểm | Chạy `#16582` + `#16619` trên production sau deploy; bổ sung TC ở §5 cho hạ tầng thật |
| I3 | `[MAJOR]` | `03-dev-impact.md` | `Auto-filled: 2026-09-08 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" chưa tick** → F/D/T chưa ai xác nhận. File 03 còn 5 mâu thuẫn tự thân đã ghi sẵn (commit "chưa push" vs "[đã push]"; mục 3 vẫn kê `isQrLandingDomain` đã bị thay; 4.3 chỉ kê 1 domain trong khi fix áp 3 domain) | Tester đọc lại journal #130769 trên Redmine, tick checkbox; hỏi Dev 3 điểm mâu thuẫn trước khi kết luận review |
| I4 | `[MAJOR]` | `03-dev-impact.md` mục 3 + 4.3 — **AP-6** | Fix chạm `Handler::render` = **exception handler dùng chung TOÀN APP**, nhưng mục 3 chỉ liệt kê đúng 4 dòng của chính 2 file vừa sửa, **không có danh sách nơi bị ảnh hưởng**; mục 4.3 kê đúng 1 tính năng (`s.lmes.jp`) trong khi fix áp cho 3 domain. Studio `dev_impact` có bù phần này (*"handler dùng chung toàn app ⇒ phải verify domain KHÁC"*) nhưng file 03 vẫn thiếu | Yêu cầu Dev bổ sung mục 3 + 4.3: liệt kê các route/màn hình chạy trên 3 domain public bị guard đi qua |
| I5 | `[MAJOR]` | 9 TC | **6/13 mã quan điểm không tồn tại trong `framework/checklist-lme.md`**: `TOOL-KNOW-002` (1 TC), `TOOL-SCOPE-001` (1), `TOOL-ERRHYG-001` (3), `TOOL-NEGCTRL-001` (2), `AUTH-SESSION-001` (1), `API-CONTRACT-001` (1). Đây là mã nội bộ Studio ⇒ **không tính là cover** khi map coverage | Map lại sang mã tầng 1 (gợi ý: `TOOL-KNOW-002`/`TOOL-ERRHYG-001` → `REG-SHARED-001`; `AUTH-SESSION-001` → `PERM-001`; `API-CONTRACT-001` → `REG-SHARED-001`), hoặc bổ sung mã mới vào checklist theo RULE-10 |
| I6 | `[MAJOR]` | `#16619`, `#16616`, `#16617`, `#16618`, `#16582` | **5 TC không gắn `requirement_keys`** → không trace được về REQ nào; đúng 4/5 TC này lại là nhóm có vấn đề kết quả ở `I1` | Gắn `requirement_keys` trên Studio (`testcase_update`) |
| I7 | `[MINOR]` | `TC-OUTTRUTH001-01` (#16541) | Expected thiếu assert `meta robots noindex,nofollow` dù REQ-002 yêu cầu (xem `G7`) | Bổ sung 1 dòng expected vào TC sẵn có, **không** đẻ TC mới |
| I8 | `[MINOR]` | `TC-APICONTRACT001-01` (#16552) | Baseline để so sánh **không lấy từ nhánh release** mà lấy bằng cách **tắt guard qua env `INVALID_LINK_DOMAINS`** (runner tự ghi: *"không có cây source nhánh release trong lượt chạy"*). Kết luận "không khác biệt" chỉ đúng dưới giả định guard nằm sau nhánh JSON — tức đang lấy chính thứ cần chứng minh làm tiền đề | Chạy lại với cây source nhánh `release_step_20260623` thật, hoặc ghi rõ giới hạn này vào `Ghi chú` TC |
| I9 | `[MINOR]` | `TC-FUNC004-01` (#16547) | Gán mã `FUNC-004` (trigger = *giới hạn số lượng / ký tự / dung lượng*) nhưng nội dung test là **so khớp tên miền không phân biệt hoa thường** → mã quan điểm sai, làm nhiễu coverage | Đổi sang `REG-SHARED-001` hoặc `FUNC-003` (định dạng) |
| I10 | `[MINOR]` | Cấu hình deploy | Run #16556 ghi nhận: `INVALID_LINK_DOMAINS` có nhiều domain kèm khoảng trắng **phải bọc nháy** trong `.env`; để trần làm dotenv nổ ngay lúc bootstrap → **HTTP 500 trắng toàn site**, không phải lỗi sản phẩm nhưng là bẫy khi deploy | Ghi vào hướng dẫn deploy / release note |
| I11 | `[NIT]` | Studio task #279 | Run #1039 có `status = "fail"` ở cấp run dù 0 TC fail (pass 18 / skip 2) — dễ đọc nhầm trên dashboard. Ngoài ra `coverage.summary` = `0/34 covered`, 33 `partial`, 1 `none` (`SCR-01` màn 「QRコードアクション」 — 0 TC) | Hỏi Studio về cách tính `run.status`; cân nhắc bổ sung TC cho `SCR-01` nếu Leader thấy cần |

---

## 5. TCs đề xuất bổ sung (24)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs` **chưa có FA-017** (QR Code Action) → đọc thay bằng [kho-tcs/fa011-taobieumau-フォーム作成.md](../../kho-tcs/fa011-taobieumau-フォーム作成.md) vì fix chạm `form.lmes.jp` |
| Vùng regression phát hiện từ kho | `TC-FORM-457` (COMPAT-LEGACY-001, PRODUCTION) — form cũ redirect **`step3.lmes.jp`** và `s.lmes.jp` ⇒ xác nhận domain form thế hệ cũ còn dùng thật → sinh `G2` / `Q1` · `TC-FORM-415` (ENV-001, PRODUCTION) — bot hết hạn quá mốc 18 giờ phải hiện **màn thông báo lỗi** ở 6 đường mở form → sinh `G4` |
| Conflict expected vs kho | `Không` — không TC đề xuất nào mâu thuẫn expected của kho |
| GAP dùng lại TC kho (không viết mới) | `Không` — kho không có TC nào cover đúng các GAP này (kho test link **hợp lệ** trên domain cũ, ta cần link **hỏng**) |
| Xác nhận chống trùng | Đã đối chiếu **26** TC ở BƯỚC 0 + kho FA-011 — **không TC đề xuất nào trùng**. Nhóm **8 TC ma trận domain** (`TC-REGSHARED001-13…-18`, `TC-ENV003-03`, `TC-ENV003-04`) đối chiếu riêng với `TC-REGSHARED001-08` (`sl.lmes.jp`), `-09` (`form.lmes.jp`), `TC-ENV002-01`, `TC-ENV003-02`, `TC-FUNC004-01`, `TC-TOOLNEGCTRL001-02`: nhóm Studio test **URL hỏng** trên 3 domain mặc định, nhóm mới test **lỗi nghiệp vụ (bot hết hạn)** trên từng loại domain + **domain ngoài danh sách** ⇒ khác `thao tác + expected`, không trùng. Nhóm 9 TC "link đã xoá" (`TC-COMPATLEGACY001-02`, `TC-LIFFENTRY001-04…-11`) đối chiếu riêng với `TC-TOOLSCOPE001-01` (#16560): #16560 test **mã sai nhập tay** trên 4 route, nhóm mới test **đối tượng bị xoá thật từ màn quản trị** trên 7 nhóm link ⇒ khác `đối tượng + thao tác`, không trùng |
| Route public đã tra để viết TC | `spec-features` — form v1 `/basic/form-answer/form-render/{slug}` (EP-22) · form v3 `/form-render-v3/{slug}` (EP-23) · QR `/landing-qr/{liffId}` (EP-63) · QR giới thiệu `/landing/page-intro/{code}/{u_code}` · lesson `/mobile/calendar/{hash}/{uCode?}` (EP-P01) · event `/mobile/event-booking/index/{hash_event_id}/{u_code?}` (EP-70) · salon LIFF `?calendar_salon_id={id}` · item `type=product-detail / product-change / product-cancel` · shorten `/s/{bot}/{code}` + `/l/{code}` |

**Các GAP/RISK không đề xuất TC mới** (giải thích thay vì đẻ TC):

- `G1` — **đã viết 8 TC mới** (`TC-COMPATLEGACY001-02`, `TC-LIFFENTRY001-04…-11`). Leader chốt expected: **mọi link LME đã phát cho friend, sau khi đối tượng bị xoá, phải ra trang 「お探しのページが見つかりませんでした。」**. `TC-TOOLSCOPE001-01` (#16560) giữ nguyên làm TC gốc mô tả phạm vi. ⚠️ Theo log run #16560, code hiện tại đưa các link này về `/error/404` **cũ** ⇒ nhóm TC mới **dự kiến FAIL** — đó chính là mục đích: bắt đúng chỗ fix còn hụt. Người test **không được đổi expected theo code**; fail thì raise ticket và đối chiếu §6 mục 1.
- `G6` + `Q2` — **không viết TC mới**: `TC-REGSHARED001-07` (#16582) đã là đúng TC cần; chỉ cần **chạy trên production sau deploy** thay vì skip.
- `G7` — **không viết TC mới**: bổ sung 1 dòng expected vào `TC-OUTTRUTH001-01`, xem §4 `I7`.

**Ma trận domain — ô nào đã có TC, ô nào vừa bổ sung** (Leader yêu cầu phủ đủ loại domain cho ca `bot hết hạn` của `TC-REGSHARED001-11`):

| # | Loại domain | Ví dụ | Trong danh sách của bản fix? | TC `URL hỏng` | TC `lỗi nghiệp vụ (bot hết hạn)` |
|---|---|---|---|---|---|
| D1 | Public mặc định — QR / rút gọn | `s.lmes.jp` | ✔ hard-code | `TC-TOOLKNOW002-01` (#16540) | **`TC-REGSHARED001-13`** ⭐ |
| D2 | Public mặc định — short link | `sl.lmes.jp` | ✔ hard-code | `TC-REGSHARED001-08` | **`TC-REGSHARED001-14`** ⭐ |
| D3 | Public mặc định — form | `form.lmes.jp` | ✔ hard-code | `TC-REGSHARED001-09` | `TC-REGSHARED001-11` (đã có ở §5) |
| D4 | **Domain rút gọn riêng của bot** | `bots.domain_url_shorten` | ✘ **không đọc tới** | **`TC-ENV003-03`** ⭐ (`G8`) | **`TC-REGSHARED001-15`** ⭐ |
| D5 | Form thế hệ cũ | `step3.lmes.jp` | ✘ (`G2`) | `TC-COMPATLEGACY001-01` | **`TC-REGSHARED001-16`** ⭐ |
| D6 | Quản trị | `step.lme.jp` | ✘ — phải giữ hành vi cũ | `TC-REGSHARED001-01/-02/-03` | **`TC-REGSHARED001-17`** ⭐ |
| D7 | Suy từ cấu hình môi trường | `URL_OUTSIDE_STEP` / `_V2` | ✔ khi env có khai | `TC-ENV003-02`, `TC-ENV002-02` | **`TC-REGSHARED001-18`** ⭐ (gộp cả D8) |
| D8 | Khai đè thủ công | `INVALID_LINK_DOMAINS` | ✔ thay **toàn bộ** danh sách | `TC-ENV002-01` | **`TC-REGSHARED001-18`** ⭐ |
| D9 | Biên tên miền (hoa/thường · gần giống) | `S.LMES.JP` · `xs.lmes.jp` | biên | `TC-FUNC004-01`, `TC-TOOLNEGCTRL001-02` | *không viết thêm* — biên tên miền đã chốt ở nhóm URL hỏng, lặp lại cho lỗi nghiệp vụ là trùng vô ích |
| D10 | **Kiểm kê toàn bộ domain thật** | — | — | **`TC-ENV003-04`** ⭐ | — |

> ⭐ = TC bổ sung ở đợt này. Ô D4 lộ ra `G8` (§1) — domain rút gọn riêng của bot **không** nằm trong danh sách nào của bản fix.

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-COMPATLEGACY001-01 | Data | COMPAT-LEGACY-001 | Trang báo link lỗi — domain thế hệ cũ | Normal | manual | product | URL hỏng trên domain form thế hệ cũ `step3.lmes.jp` cũng phải ra trang báo link lỗi, không về màn đăng nhập | - Bản fix #38765 đã deploy lên production, chế độ gỡ lỗi tắt<br>- Có sẵn 1 form cũ đang redirect qua `step3.lmes.jp` còn hoạt động (theo kho `TC-FORM-457`)<br>- Trình duyệt ở cửa sổ ẩn danh, không có phiên đăng nhập | 1. Mở form cũ qua `step3.lmes.jp` để xác nhận link **hợp lệ** vẫn mở được form (đối chứng).<br>2. Bật tab Network của công cụ nhà phát triển.<br>3. Nhập `https://step3.lmes.jp/khong-ton-tai-38765` rồi Enter.<br>4. Đọc nội dung trang + nhìn thanh địa chỉ.<br>5. Xem mã trạng thái và chuỗi chuyển hướng của request Document đầu tiên.<br>6. Lặp bước 3-5 với `https://s.lmes.jp/khong-ton-tai-38765` để đối chứng. | Đường dẫn không khớp route: `/khong-ton-tai-38765` trên `step3.lmes.jp` và `s.lmes.jp` | Trên `step3.lmes.jp`: thanh địa chỉ **giữ nguyên** URL hỏng, request Document trả **HTTP 404**, trang hiển thị 「お探しのページが見つかりませんでした。」, **không** có nút 「TOPページへ戻る」, **không** có chuyển hướng 302 về `/` và **không** hiện màn đăng nhập LME — giống hệt `s.lmes.jp` ở bước 6.<br>Nếu `step3.lmes.jp` vẫn 302 về `/` ⇒ **fail**: link QR/form thế hệ cũ đã phát tán vẫn dính đúng bug #38765. |  | Lấp `G2` · `Q1` · regression · dẫn từ kho `TC-FORM-457` · RULE-08 (domain) → bắt buộc production · Đánh giá spec: **Spec không ghi** — danh sách domain mặc định chỉ có 3 host, chưa ai chốt domain cũ có thuộc phạm vi không → hỏi BA cùng lúc với §6 mục 1 · Evidence: screenshot trang + ảnh tab Network có mã trạng thái & chuỗi chuyển hướng |
| TC-REGSHARED001-10 | UI | REG-SHARED-001 | Trang báo link lỗi — nhóm lỗi không tìm thấy | Abnormal | auto | Tất cả | Lỗi `ModelNotFound` (bản ghi không tồn tại) trên domain public cũng ra trang báo link lỗi, không nuốt nhầm sang lỗi khác | - Chế độ gỡ lỗi tắt, không khai `INVALID_LINK_DOMAINS`<br>- Có phiên đăng nhập hợp lệ và đã chọn bot (dùng lại cách lấy cookie phiên của `#16546`)<br>- Đã hỏi Dev để lấy **1 màn dùng `findOrFail`** (khác `abort(404)` của `#16546`) truy cập được qua domain public | 1. Đăng nhập, chọn bot, xác nhận vào được 「basic/overview」.<br>2. Gọi màn dùng `findOrFail` qua host `s.lmes.jp` với id chắc chắn không tồn tại.<br>3. Ghi lại mã trạng thái + nội dung trang trả về.<br>4. Lặp lại đúng request đó trên host `localhost` (ngoài danh sách) để đối chứng. | Host `s.lmes.jp` + id không tồn tại (`99999999`); host đối chứng `localhost` | Trên `s.lmes.jp`: **HTTP 404** kèm trang báo link lỗi 1 dòng 「お探しのページが見つかりませんでした。」, không có 「TOPページへ戻る」, không chuyển hướng.<br>Trên `localhost`: **giữ nguyên hành vi cũ** (302 về `/` hoặc trang `/error/404` cũ), **không** ra trang mới.<br>Không request nào trả 500 hay trang lỗi hệ thống. |  | Lấp `G3` · Bổ sung trigger thứ 4 cho nhóm không tìm thấy (đã có: route-not-found `#16540`, MethodNotAllowed `#16545`, abort(404) `#16546`) · Đánh giá spec: **Spec ghi rõ** (REQ-003) · Evidence: log request/response 2 host |
| TC-REGSHARED001-11 | UI | REG-SHARED-001 | Form public — bot hết hạn | Abnormal | manual | product | Bot hết hạn quá mốc 18 giờ: mở form trên `form.lmes.jp` vẫn hiện **màn thông báo lỗi nghiệp vụ**, không bị thay bằng trang báo link lỗi | - Bản fix #38765 đã deploy, chế độ gỡ lỗi tắt<br>- Bot A ở trạng thái hết hạn, điều chỉnh được mốc thời gian hết hạn (dựng theo kho `TC-FORM-415`)<br>- Có form F1 của bot A còn công khai, URL trên `form.lmes.jp`<br>- LINE user U1 đã kết bạn với bot A | 1. Đặt (thời điểm hiện tại − thời điểm hết hạn) = **17 giờ 59 phút** → U1 mở form F1 → quan sát.<br>2. Đặt = **18 giờ 01 phút** → U1 mở lại form F1 từ LINE app → quan sát nội dung trang.<br>3. Mở lại đúng URL đó bằng trình duyệt ngoài LINE → quan sát.<br>4. Ghi mã trạng thái HTTP của request Document ở bước 2 và 3. | Bot hết hạn ở 2 mốc: `17h59` và `18h01`; form F1 trên `form.lmes.jp` | Bước 1 (17h59): form mở bình thường.<br>Bước 2-3 (18h01): hiện **đúng màn thông báo lỗi của nghiệp vụ hết hạn** như trước khi có bản fix — **KHÔNG** hiện trang 「お探しのページが見つかりませんでした。」.<br>Nếu ra trang báo link lỗi mới ⇒ **fail**: guard #38765 đã nuốt thông điệp nghiệp vụ, khách không biết bot hết hạn. |  | Lấp `G4` · regression · dẫn từ kho `TC-FORM-415` · RULE-08 (bill tiền / hợp đồng) → production · Đánh giá spec: **Spec không ghi** — chưa ai chốt lỗi nghiệp vụ trên domain public có được guard bỏ qua không → hỏi cùng §6 mục 1 · Evidence: screenshot 2 mốc + log mã trạng thái |
| TC-REGSHARED001-12 | UI | REG-SHARED-001 | Các trang public khác của FA-017 | Abnormal | auto | Tất cả | 3 nhóm trang public còn lại (`open-mobile`, `open-external-browser`, `page-intro`) với tham số sai không rơi vào màn đăng nhập | - Chế độ gỡ lỗi tắt, không khai `INVALID_LINK_DOMAINS`<br>- Trình duyệt không có phiên đăng nhập<br>- Host `s.lmes.jp` trỏ về môi trường test | 1. Mở `s.lmes.jp/open-mobile/<type sai>/<id sai>` → ghi mã trạng thái + trang hiển thị + URL cuối.<br>2. Mở `s.lmes.jp/open-external-browser/<type sai>/<id sai>` → ghi tương tự.<br>3. Mở `s.lmes.jp/landing/page-intro/<code sai>/<u_code sai>` → ghi tương tự.<br>4. Với mỗi trang, kiểm có nút/liên kết nào dẫn về `/admin/home` hay màn đăng nhập không. | 3 dataset: `type` + `id` không tồn tại cho `open-mobile` và `open-external-browser`; `code` + `u_code` không tồn tại cho `page-intro` | Cả 3 dataset: **không** trang nào dẫn user về màn đăng nhập LME và **không** trang nào có liên kết tới `/admin/home`.<br>Ghi rõ từng dataset ra trang nào (trang báo link lỗi mới / `/error/404` cũ / khác) — nếu ra `/error/404` cũ **có** nút 「TOPページへ戻る」 thì đánh **fail** và gộp vào quyết định phạm vi ở §6 mục 1. |  | Lấp `G5` · Spec FA-017 §9.2: `open-mobile` / `open-external-browser` phục vụ **8 tính năng** (booking calendar/salon/event, product, form_answer) · Đánh giá spec: **Spec không ghi** → hỏi BA · Evidence: log 3 request + screenshot trang cuối |
| TC-LIFFENTRY001-02 | UI | LIFF-ENTRY-001 | Trang báo link lỗi — mở từ LINE in-app browser | Abnormal | manual | product | Link QR hỏng dạng `/landing-qr/<liffId sai>` mở trong LINE in-app browser không đưa friend về màn đăng nhập | - Bản fix đã deploy lên môi trường có domain public thật<br>- 1 thiết bị **iOS** và 1 thiết bị **Android**, mỗi máy đã cài LINE và kết bạn với bot test<br>- Chuẩn bị `liffId` sai định dạng và `liffId` đúng định dạng nhưng không tồn tại | 1. Gửi cho friend U1 link `https://s.lmes.jp/landing-qr/<liffId sai định dạng>`.<br>2. U1 tap link trên **iOS** → quan sát trang hiển thị trong in-app browser.<br>3. Lặp bước 2 trên **Android**.<br>4. Lặp bước 1-3 với `liffId` đúng định dạng nhưng không tồn tại.<br>5. Với mỗi lần, chụp lại toàn màn hình gồm cả thanh địa chỉ. | `liffId` sai định dạng: `%%%38765`; `liffId` đúng định dạng nhưng không tồn tại: `1234567890-abcdefgh` | Trên **cả iOS và Android**, cả 2 dataset: **không** hiện màn đăng nhập LME và **không** có nút/liên kết dẫn về trang quản trị. Ghi rõ mỗi dataset ra trang nào.<br>Thiếu 1 trong 2 thiết bị ⇒ đánh **blocked**, **không** được pass một phần. |  | Lấp `Q3` (Abnormal còn thiếu theo RULE-01) · RULE-06 output cuối trên thiết bị thật · RULE-08 domain → production · Đánh giá spec: **Spec không ghi** → hỏi BA · Evidence: screenshot toàn màn hình từ **cả 2** thiết bị |
| TC-LIFFENTRY001-03 | UI | LIFF-ENTRY-001 | Trang báo link lỗi — biên độ dài & ký tự URL | Boundary | manual | product | URL hỏng rất dài / chứa ký tự đa byte đã mã hoá vẫn ra trang báo link lỗi, không tràn chữ trên màn điện thoại | - Bản fix đã deploy lên domain public thật, chế độ gỡ lỗi tắt<br>- 1 thiết bị điện thoại mở được LINE in-app browser<br>- Trình duyệt không có phiên đăng nhập | 1. Mở `https://s.lmes.jp/<đường dẫn 2000 ký tự>` → quan sát trang + mã trạng thái.<br>2. Mở `https://s.lmes.jp/テスト-38765-無効なリンク` (ký tự tiếng Nhật, trình duyệt tự mã hoá) → quan sát.<br>3. Mở `https://s.lmes.jp/` + đường dẫn chỉ gồm ký tự đặc biệt `..%2F..%2Ftest` → quan sát.<br>4. Ở bước 2, xem trang trên bề rộng điện thoại và kiểm chữ có bị tràn ngang không. | 3 dataset: đường dẫn 2000 ký tự · đường dẫn ký tự tiếng Nhật · đường dẫn ký tự đặc biệt đã mã hoá | Cả 3 dataset: trả **HTTP 404** kèm trang 「お探しのページが見つかりませんでした。」, **không** 302 về `/`, **không** lỗi 500 / trang trắng, **không** lộ đường dẫn hệ thống trên trang.<br>Ở bề rộng điện thoại, dòng chữ hiển thị đủ, không tràn ngang. |  | Lấp `Q3` (Boundary còn thiếu theo RULE-01) · RULE-08 domain → production · Đánh giá spec: **Spec không ghi** → hỏi BA · Evidence: screenshot 3 dataset + log mã trạng thái |
| TC-DATACACHE001-01 | Data | DATA-CACHE-001 | Trang báo link lỗi — cache tầng ngoài | Boundary | manual | product | Trang báo link lỗi không bị CDN/proxy cache đè lên link rút gọn sau đó trở thành hợp lệ | - Bản fix đã deploy lên production (có CDN/proxy thật trước ứng dụng)<br>- Có quyền tạo link rút gọn mới trên `s.lmes.jp`<br>- Ghi lại được header phản hồi (dùng công cụ nhà phát triển hoặc lệnh gọi HTTP) | 1. Chọn 1 mã link rút gọn **chưa tồn tại**, mở `https://s.lmes.jp/s/<bot>/<mã>` → ghi mã trạng thái + toàn bộ header `Cache-Control` / `Expires` / `Age` / header cache của CDN.<br>2. Tạo link rút gọn **thật** dùng đúng mã đó, trỏ tới 1 form hợp lệ.<br>3. Mở lại đúng URL ở bước 1 (không xoá cache trình duyệt) → quan sát.<br>4. Mở lại URL đó ở thiết bị/mạng khác → quan sát.<br>5. Mở 1 URL hỏng bất kỳ 2 lần liên tiếp, so header `Age` để xem CDN có phục vụ từ cache không. | Mã link rút gọn dùng lại: `tc38765cache` · 2 thiết bị/mạng khác nhau | Bước 1: phản hồi 404 mang header **không cho cache** (`Cache-Control: no-store` hoặc tương đương), CDN không lưu.<br>Bước 3-4: sau khi link được tạo thật, mở lại **đi đúng đích form**, **không** còn trả trang 「お探しのページが見つかりませんでした。」 ở bất kỳ thiết bị/mạng nào.<br>Bước 5: nếu header cho thấy CDN phục vụ trang 404 từ cache ⇒ **fail** và ghi rõ TTL đang áp. |  | Lấp `Q4` · RULE-08 (domain + loadbalance/CDN) → production · Đánh giá spec: **Spec không ghi** — bản fix không nói gì về header cache của trang mới → hỏi Dev · Evidence: log header phản hồi đủ 5 bước |
| TC-COMPATLEGACY001-02 | Data | COMPAT-LEGACY-001 | Link form đã xoá — form cũ (v1) & form mới (v3) | Abnormal | manual | product | Xoá form rồi friend mở lại link: cả form cũ và form mới đều ra trang báo link lỗi, không về màn đăng nhập | - Bản fix #38765 đã deploy production, chế độ gỡ lỗi tắt<br>- Bot test có **1 form cũ (v1)** và **1 form mới (v3)** đang công khai<br>- Đã bấm nút copy URL công khai của cả 2 form ở màn 「フォーム作成」 và lưu lại<br>- LINE user U1 đã kết bạn với bot test | 1. Mở lần lượt 2 URL đã copy, xác nhận cả 2 mở đúng màn form (chụp ảnh đối chứng trước khi xoá).<br>2. Vào màn 「フォーム作成」, xoá form v1 bằng thao tác xoá chuẩn.<br>3. Xoá form v3 theo cùng cách.<br>4. U1 mở lại URL form v1 từ LINE app, chụp toàn màn hình gồm cả thanh địa chỉ.<br>5. Mở lại URL form v1 bằng trình duyệt ngoài LINE, ghi mã trạng thái HTTP của request Document đầu tiên.<br>6. Lặp bước 4-5 cho URL form v3. | URL form cũ dạng `https://form.lmes.jp/basic/form-answer/form-render/<slug>` · URL form mới dạng `https://form.lmes.jp/basic/form-answer/form-render-v3/<slug>` — dùng đúng URL nút copy trên màn admin sinh ra | Cả **4 lượt mở** (2 form × 2 đường mở) đều hiển thị trang chỉ có 1 dòng 「お探しのページが見つかりませんでした。」, **không** có nút hay liên kết 「TOPページへ戻る」, **không** có liên kết tới `/admin/home`, **không** hiện màn đăng nhập LME.<br>Ra trang `/error/404` cũ (có nút 「TOPページへ戻る」) hoặc màn đăng nhập ⇒ **fail**, raise ticket. |  | Lấp `G1` · RULE-09 cũ & mới song song (form v1 vs v3) · RULE-08 domain → production · ⚠️ **Dự kiến FAIL** trên code hiện tại (log #16560: `/form-render/<key sai>` đang `redirect()->route('404')`) — **không** đổi expected theo code · Đánh giá spec: **Đã hỏi leader** — Leader chốt expected là trang mới, BA xác nhận ở §6 mục 1 · Evidence: screenshot 4 lượt mở + log mã trạng thái |
| TC-LIFFENTRY001-04 | UI | LIFF-ENTRY-001 | Link QR đã xoá — QR thường & QR giới thiệu | Abnormal | manual | product | Xoá QR rồi friend mở lại link QR thường và link QR giới thiệu đều ra trang báo link lỗi | - Bản fix đã deploy production, chế độ gỡ lỗi tắt<br>- Màn 「QRコードアクション」 có 1 QR đang công khai, đã bật chức năng giới thiệu bạn bè<br>- Đã copy sẵn **link QR thường** và **link QR giới thiệu** (link có `u_code` của friend giới thiệu)<br>- LINE user U1 đã kết bạn với bot | 1. Mở cả 2 link, xác nhận vào đúng Landing (ảnh đối chứng).<br>2. Xoá QR ở màn 「QRコードアクション」.<br>3. U1 mở lại link QR thường từ LINE app, chụp toàn màn hình.<br>4. Mở lại link đó bằng trình duyệt ngoài LINE, ghi mã trạng thái HTTP.<br>5. Lặp bước 3-4 với link QR giới thiệu.<br>6. Kiểm trên mỗi trang có liên kết nào dẫn về `/admin/home` không. | Link QR thường dạng `https://s.lmes.jp/landing-qr/<liffId>` · Link QR giới thiệu dạng `https://s.lmes.jp/landing/page-intro/<code>/<u_code>` — dùng đúng URL nút copy sinh ra | Cả **4 lượt mở** đều ra trang 1 dòng 「お探しのページが見つかりませんでした。」, **không** nút 「TOPページへ戻る」, **không** liên kết `/admin/home`, **không** hiện màn đăng nhập LME.<br>Riêng link giới thiệu: **không** được tạo thêm bản ghi giới thiệu nào cho `u_code` trong link (QR đã xoá thì không tính giới thiệu). |  | Lấp `G1` + `Q3` (Abnormal) · RULE-08 domain → production · ⚠️ **Dự kiến FAIL** (log #16560: `/landing-qr/<mã sai>` ra `/error/404` cũ **có** link `/admin/home`) · Đánh giá spec: **Đã hỏi leader** · Evidence: screenshot 4 lượt mở + ảnh màn thống kê giới thiệu trước/sau |
| TC-LIFFENTRY001-05 | UI | LIFF-ENTRY-001 | Link salon đã xoá — link đặt chỗ & link huỷ | Abnormal | manual | product | Xoá lịch salon rồi friend mở lại link đặt chỗ và link huỷ đều ra trang báo link lỗi | - Bản fix đã deploy production, chế độ gỡ lỗi tắt<br>- Màn 「サロン・面談予約」 có 1 lịch salon đang hoạt động<br>- Đã copy 「予約ページURL」 của lịch đó<br>- U1 đã đặt chỗ thành công 1 lần và **đã nhận được tin nhắn chứa link huỷ đặt chỗ** trong LINE | 1. Mở link đặt chỗ và link huỷ, xác nhận cả 2 dùng được (ảnh đối chứng).<br>2. Xoá lịch salon ở màn quản trị.<br>3. U1 mở lại link đặt chỗ từ LINE app, chụp toàn màn hình.<br>4. U1 mở lại link huỷ trong tin nhắn đã nhận, chụp toàn màn hình.<br>5. Mở lại cả 2 link bằng trình duyệt ngoài LINE, ghi mã trạng thái HTTP. | Link đặt chỗ = 「予約ページURL」 dạng LIFF `https://liff.line.me/<liff_id>?calendar_salon_id=<id>` · Link huỷ = link trong tin nhắn xác nhận đặt chỗ gửi cho U1 | Cả **4 lượt mở** (2 link × 2 đường mở) đều ra trang 1 dòng 「お探しのページが見つかりませんでした。」, **không** nút 「TOPページへ戻る」, **không** màn đăng nhập LME, **không** trang trắng hay lỗi 500.<br>Không lượt nào tạo thêm đặt chỗ mới hay đổi trạng thái đặt chỗ cũ. |  | Lấp `G1` + `Q3` · RULE-08 domain → production · Link đi qua LIFF nên phải kiểm **cả trong LINE in-app browser và ngoài LINE** (RULE-06) · Đánh giá spec: **Đã hỏi leader** · Evidence: screenshot 4 lượt mở + ảnh danh sách đặt chỗ trước/sau |
| TC-LIFFENTRY001-06 | UI | LIFF-ENTRY-001 | Link lesson đã xoá — link đặt chỗ & link huỷ | Abnormal | manual | product | Xoá lịch lesson rồi friend mở lại link đặt chỗ và link huỷ đều ra trang báo link lỗi | - Bản fix đã deploy production, chế độ gỡ lỗi tắt<br>- Màn 「レッスン予約」 có 1 lịch lesson đang hoạt động, đã copy URL trang đặt chỗ<br>- U1 đã đặt chỗ thành công và **đã nhận tin nhắn chứa link huỷ / link xem lịch sử đặt chỗ** | 1. Mở link đặt chỗ và link huỷ, xác nhận cả 2 dùng được (ảnh đối chứng).<br>2. Xoá lịch lesson ở màn quản trị.<br>3. U1 mở lại link đặt chỗ từ LINE app, chụp toàn màn hình.<br>4. U1 mở lại link huỷ, chụp toàn màn hình.<br>5. Mở lại cả 2 link bằng trình duyệt ngoài LINE, ghi mã trạng thái HTTP. | Link đặt chỗ dạng LIFF `https://liff.line.me/<liffId>?calendar_id=<id>` (đích `/mobile/calendar/<hash>`) · Link huỷ = link trong tin nhắn xác nhận đặt chỗ gửi cho U1 | Cả **4 lượt mở** đều ra trang 1 dòng 「お探しのページが見つかりませんでした。」, **không** nút 「TOPページへ戻る」, **không** màn đăng nhập LME.<br>Không lượt nào đổi trạng thái đặt chỗ cũ trong DB/màn quản trị. |  | Lấp `G1` + `Q3` · RULE-08 domain → production · ⚠️ Lesson còn có trang riêng `/mobile/calendar-lesson/<hash>/close-booking` (受付停止) — nếu môi trường có thì kiểm thêm như dataset thứ 3 · Đánh giá spec: **Đã hỏi leader** · Evidence: screenshot 4 lượt mở |
| TC-LIFFENTRY001-07 | UI | LIFF-ENTRY-001 | Link event đã xoá — link đặt chỗ | Abnormal | manual | product | Xoá sự kiện rồi friend mở lại link đặt chỗ ra trang báo link lỗi | - Bản fix đã deploy production, chế độ gỡ lỗi tắt<br>- Màn 「イベント予約」 có 1 sự kiện đang công khai, đã copy URL trang đặt chỗ<br>- U1 đã kết bạn với bot và đã mở được trang đặt chỗ 1 lần | 1. Mở link đặt chỗ, xác nhận vào đúng trang sự kiện (ảnh đối chứng).<br>2. Xoá sự kiện ở màn quản trị.<br>3. U1 mở lại link từ LINE app, chụp toàn màn hình.<br>4. Mở lại link bằng trình duyệt ngoài LINE, ghi mã trạng thái HTTP của request Document.<br>5. Kiểm trang có liên kết nào dẫn về màn quản trị không. | Link đặt chỗ dạng LIFF `https://liff.line.me/<liffId>?booking_event_id=<id>` (đích `/mobile/event-booking/index/<hash_event_id>`) | Cả 2 đường mở đều ra trang 1 dòng 「お探しのページが見つかりませんでした。」, **không** nút 「TOPページへ戻る」, **không** liên kết về màn quản trị, **không** hiện màn đăng nhập LME. |  | Lấp `G1` + `Q3` · RULE-08 domain → production · Đánh giá spec: **Đã hỏi leader** · Evidence: screenshot 2 lượt mở + log mã trạng thái |
| TC-LIFFENTRY001-08 | UI | LIFF-ENTRY-001 | Link item đơn đã xoá — link mua | Abnormal | manual | product | Xoá sản phẩm bán 1 lần rồi friend mở lại link mua ra trang báo link lỗi, không vào được trang thanh toán | - Bản fix đã deploy production, chế độ gỡ lỗi tắt<br>- Màn 「商品販売」 có 1 sản phẩm **bán 1 lần** đang công khai, đã copy URL phân phối (`product-detail`)<br>- U1 đã kết bạn với bot, chưa mua sản phẩm này | 1. Mở link mua, xác nhận vào đúng trang sản phẩm (ảnh đối chứng).<br>2. Xoá sản phẩm ở màn 「商品販売」.<br>3. U1 mở lại link mua từ LINE app, chụp toàn màn hình.<br>4. Mở lại link bằng trình duyệt ngoài LINE, ghi mã trạng thái HTTP.<br>5. Kiểm màn lịch sử bán hàng xem có phát sinh đơn mới nào không. | URL phân phối `type=product-detail` của sản phẩm bán 1 lần — dùng đúng URL nút copy trên màn admin sinh ra | Cả 2 đường mở đều ra trang 1 dòng 「お探しのページが見つかりませんでした。」, **không** vào được bất kỳ bước nào của luồng mua (không hiện form nhập thông tin, không hiện ô nhập thẻ), **không** nút 「TOPページへ戻る」, **không** màn đăng nhập LME.<br>Lịch sử bán hàng **không** phát sinh đơn mới. |  | Lấp `G1` + `Q3` · RULE-08 bill tiền + domain → production · ⚠️ **Tuyệt đối không** nhập thẻ thật; nếu trang thanh toán vẫn mở được thì dừng ngay và raise ticket mức cao · Đánh giá spec: **Đã hỏi leader** · Evidence: screenshot 2 lượt mở + ảnh màn lịch sử bán hàng trước/sau |
| TC-LIFFENTRY001-09 | UI | LIFF-ENTRY-001 | Link item chu kỳ đã xoá — link mua, đổi thẻ, huỷ | Abnormal | manual | product | Xoá sản phẩm thanh toán định kỳ rồi friend mở lại cả 3 link (mua, đổi thẻ, huỷ) đều ra trang báo link lỗi | - Bản fix đã deploy production, chế độ gỡ lỗi tắt<br>- Màn 「商品販売」 có 1 sản phẩm **thanh toán định kỳ** đang công khai<br>- Đã copy đủ **3 URL phân phối**: mua (`product-detail`), đổi thẻ (`product-change`), huỷ (`product-cancel`)<br>- U1 đã kết bạn với bot | 1. Mở lần lượt 3 link, xác nhận cả 3 vào đúng trang tương ứng (ảnh đối chứng).<br>2. Xoá sản phẩm định kỳ ở màn 「商品販売」.<br>3. U1 mở lại **link mua** từ LINE app rồi từ trình duyệt ngoài LINE, ghi kết quả và mã trạng thái.<br>4. Lặp bước 3 cho **link đổi thẻ**.<br>5. Lặp bước 3 cho **link huỷ**.<br>6. Kiểm màn hợp đồng định kỳ xem trạng thái đơn cũ có bị đổi không. | 3 URL trên cùng 1 `product_id`: `type=product-detail` · `type=product-change` · `type=product-cancel` | Cả **6 lượt mở** (3 link × 2 đường mở) đều ra trang 1 dòng 「お探しのページが見つかりませんでした。」, **không** nút 「TOPページへ戻る」, **không** màn đăng nhập LME.<br>**Không** lượt nào thực hiện được thao tác nghiệp vụ: không tạo đơn mới, không đổi được thẻ, không huỷ được hợp đồng định kỳ đang chạy. |  | Lấp `G1` + `Q3` · RULE-08 bill tiền + domain → production · ⚠️ Link huỷ và link đổi thẻ tác động **trực tiếp lên tiền** — nếu 2 link này vẫn chạy được sau khi xoá sản phẩm thì raise ticket riêng, nghiêm trọng hơn #38765 · Đánh giá spec: **Đã hỏi leader** · Evidence: screenshot 6 lượt mở + ảnh màn hợp đồng định kỳ trước/sau |
| TC-LIFFENTRY001-10 | UI | LIFF-ENTRY-001 | Link rút gọn đã xoá — short link của 1 LP bất kỳ | Abnormal | manual | product | Xoá link rút gọn (hoặc LP đích) rồi friend mở lại short link ra trang báo link lỗi | - Bản fix đã deploy production, chế độ gỡ lỗi tắt<br>- Đã tạo 1 link rút gọn trỏ tới 1 LP bất kỳ, lấy được **cả 2 thế hệ** short link nếu môi trường có: `s.lmes.jp/s/<bot>/<code>` và `sl.lmes.jp/l/<code>`<br>- U1 đã kết bạn với bot | 1. Mở short link, xác nhận chuyển đúng tới LP đích (ảnh đối chứng, ghi lại chuỗi chuyển hướng).<br>2. Xoá link rút gọn ở màn quản lý URL.<br>3. U1 mở lại short link từ LINE app, chụp toàn màn hình.<br>4. Mở lại short link bằng trình duyệt ngoài LINE, ghi mã trạng thái và chuỗi chuyển hướng.<br>5. Lặp bước 3-4 cho short link thế hệ còn lại (nếu có).<br>6. Kiểm màn thống kê lượt click xem có tăng thêm không. | Short link thế hệ 1 `https://s.lmes.jp/s/<bot>/<code>` · thế hệ 2 `https://sl.lmes.jp/l/<code>` | Mọi lượt mở đều ra trang 1 dòng 「お探しのページが見つかりませんでした。」, **không** nút 「TOPページへ戻る」, **không** màn đăng nhập LME, **không** chuyển hướng tới LP đích cũ.<br>Số lượt click **không** tăng cho link đã xoá. |  | Lấp `G1` + `Q3` · RULE-09 short link 2 thế hệ (`/s/` vs `/l/`) · RULE-08 domain → production · Bổ sung cho `TC-REGURL001-01` (#16559) vốn chỉ test link **hợp lệ** · Đánh giá spec: **Đã hỏi leader** · Evidence: screenshot + log chuỗi chuyển hướng + ảnh màn thống kê click trước/sau |
| TC-LIFFENTRY001-11 | UI | LIFF-ENTRY-001 | Link đã xoá — xoá mềm, xoá vĩnh viễn, khôi phục | Boundary | manual | product | Phân biệt 3 trạng thái xoá: vào thùng rác, xoá vĩnh viễn khỏi thùng rác, và khôi phục lại thì link phải sống lại | - Bản fix đã deploy production, chế độ gỡ lỗi tắt<br>- Màn 「QRコードアクション」 có 1 QR đang công khai và có **thùng rác** dùng được<br>- Đã copy link QR, xác nhận link mở được trước khi bắt đầu | 1. Xoá QR (đưa vào thùng rác), mở lại link, ghi kết quả và mã trạng thái.<br>2. Vào thùng rác, **khôi phục** QR, mở lại đúng link đó, ghi kết quả.<br>3. Xoá QR lần nữa, rồi **xoá vĩnh viễn** khỏi thùng rác.<br>4. Mở lại link, ghi kết quả và mã trạng thái.<br>5. So sánh trang hiển thị ở bước 1 và bước 4. | 1 link QR duy nhất, đo ở 3 trạng thái: trong thùng rác · đã khôi phục · đã xoá vĩnh viễn | Bước 1 (trong thùng rác): trang 1 dòng 「お探しのページが見つかりませんでした。」, HTTP 404.<br>Bước 2 (đã khôi phục): link **mở lại được Landing bình thường** — xoá mềm không làm hỏng vĩnh viễn link.<br>Bước 4 (đã xoá vĩnh viễn): trang 1 dòng 「お探しのページが見つかりませんでした。」, HTTP 404.<br>Bước 1 và bước 4 phải cho **cùng một trang**; không bước nào hiện màn đăng nhập LME hay nút 「TOPページへ戻る」. |  | Lấp `G1` + `Q3` (Boundary theo RULE-01) · RULE-08 domain → production · Bắt trường hợp xoá mềm và xoá cứng đi 2 nhánh code khác nhau · Đánh giá spec: **Spec không ghi** — chưa nơi nào định nghĩa hành vi link khi đối tượng nằm trong thùng rác → hỏi BA cùng §6 mục 1 · Evidence: screenshot 3 trạng thái + log mã trạng thái |
| TC-REGSHARED001-13 | UI | REG-SHARED-001 | Trang public `s.lmes.jp` — bot hết hạn | Abnormal | manual | product | Bot hết hạn quá mốc 18 giờ: cả 3 đường public trên `s.lmes.jp` (QR landing · trang giới thiệu · trang mở nội dung) vẫn hiện **màn thông báo lỗi nghiệp vụ**, không bị thay bằng trang báo link lỗi | - Bản fix #38765 đã deploy production, chế độ gỡ lỗi tắt<br>- Bot A ở trạng thái hết hạn, điều chỉnh được mốc thời gian hết hạn (dựng theo kho `TC-FORM-415`)<br>- Bot A còn 1 QR công khai (link `/landing-qr/…`), 1 link QR giới thiệu (`/landing/page-intro/…`) và 1 lịch salon hoặc lesson mở được qua `/open-mobile/…`<br>- LINE user U1 đã kết bạn với bot A<br>- Đã chụp màn hình hành vi của cả 3 đường **trước khi** bot hết hạn để đối chứng | 1. Đặt (thời điểm hiện tại − mốc hết hạn) = **17 giờ 59 phút** → U1 mở lần lượt 3 link → ghi trang hiển thị.<br>2. Đặt = **18 giờ 01 phút** → U1 mở lại lần lượt 3 link từ LINE app → ghi nội dung trang + mã trạng thái HTTP.<br>3. Mở lại đúng 3 URL đó bằng trình duyệt ngoài LINE → ghi tương tự.<br>4. Đối chiếu với ảnh chụp trước khi bot hết hạn. | 2 mốc (`17h59`, `18h01`) × 3 đường public trên `s.lmes.jp` × 2 cách mở (trong LINE / ngoài LINE) | Bước 1 (17h59): cả 3 đường mở bình thường.<br>Bước 2-3 (18h01): cả 3 đường hiện **đúng màn thông báo lỗi nghiệp vụ hết hạn** như trước bản fix — **KHÔNG** hiện 「お探しのページが見つかりませんでした。」.<br>Đường nào ra trang báo link lỗi mới ⇒ **fail**: guard #38765 nuốt thông điệp nghiệp vụ trên `s.lmes.jp`, khách không biết bot hết hạn. |  | Lấp `G4` — ô **D1** ma trận domain · regression · dẫn từ kho `TC-FORM-415` · RULE-08 (domain + hợp đồng) → production · Đánh giá spec: **Spec không ghi** → hỏi cùng §6 mục 1 · Evidence: screenshot 2 mốc × 3 đường + log mã trạng thái |
| TC-REGSHARED001-14 | UI | REG-SHARED-001 | Link rút gọn 2 thế hệ — bot hết hạn | Abnormal | manual | product | Bot hết hạn: mở link rút gọn **cả 2 thế hệ** (`s.lmes.jp/s/{bot}/{code}` và `sl.lmes.jp/l/{code}`) cho ra đúng hành vi như trước bản fix | - Bản fix đã deploy production, chế độ gỡ lỗi tắt<br>- Bot A ở trạng thái hết hạn, điều chỉnh được mốc<br>- Có 1 link rút gọn **thế hệ 1** (`s.lmes.jp/s/<bot>/<code>`) và 1 link **thế hệ 2** (`sl.lmes.jp/l/<code>`) của bot A, cả 2 còn trỏ tới đích hợp lệ<br>- Đã chụp hành vi của 2 link này **trước khi** bot hết hạn (và nếu còn môi trường chưa deploy fix thì chụp thêm ở đó) làm ảnh đối chứng | 1. Mốc **17h59**: mở 2 link → ghi đích đến.<br>2. Mốc **18h01**: mở lại 2 link trong LINE app → ghi trang cuối + URL cuối + mã trạng thái.<br>3. Mở lại 2 link bằng trình duyệt ngoài LINE → ghi tương tự.<br>4. So từng bước với ảnh đối chứng. | 2 mốc × 2 thế hệ link rút gọn × 2 cách mở | Ở mốc 18h01, hành vi phải **giống hệt trước bản fix** — ghi rõ thực tế là chuyển tới đích rồi hiện màn lỗi nghiệp vụ của đích, hay hiện màn lỗi nghiệp vụ ngay ở bước rút gọn.<br>**KHÔNG** được thay bằng 「お探しのページが見つかりませんでした。」 và **không** về màn đăng nhập.<br>Khác biệt bất kỳ so với ảnh đối chứng ⇒ **fail**, ghi rõ link nào / thế hệ nào. |  | Lấp `G4` — ô **D2** · regression · Đánh giá spec: **Spec không ghi** — chưa ai chốt hành vi link rút gọn khi bot hết hạn ⇒ oracle dùng **đối chứng trước/sau fix**, người test **không tự đặt expected mới**, hỏi BA cùng §6 mục 1 · RULE-08 → production · Evidence: screenshot + log request 2 link |
| TC-REGSHARED001-15 | UI | REG-SHARED-001 | Domain rút gọn riêng của bot — bot hết hạn | Abnormal | manual | product | Bot hết hạn: link đã phát cho friend trên **domain rút gọn riêng của bot** vẫn hiện màn thông báo lỗi nghiệp vụ, không rơi về màn đăng nhập | - Bản fix đã deploy production, chế độ gỡ lỗi tắt<br>- Bot A đã khai **domain rút gọn URL tùy chỉnh** ở màn 「アカウント設定」 (khai được nghĩa là DNS đã trỏ đúng)<br>- Có 1 link QR giới thiệu (`page-intro`) của bot A **đang dựng trên domain riêng đó** — kiểm bằng cách đọc URL hiển thị ở màn QR<br>- Bot A điều chỉnh được mốc hết hạn; U1 đã kết bạn với bot A | 1. Ở màn QR, copy link chia sẻ và **xác nhận domain trong link là domain riêng của bot**, không phải `s.lmes.jp`.<br>2. Mốc **17h59**: U1 mở link → ghi trang.<br>3. Mốc **18h01**: mở lại trong LINE app và ngoài LINE → ghi trang + mã trạng thái + URL cuối.<br>4. Ghi lại domain riêng này có nằm trong danh sách domain đang chạy của bản fix hay không (hỏi Dev / đọc cấu hình môi trường). | 2 mốc × 1 domain riêng của bot × 2 cách mở | Bước 3 (18h01): hiện **màn thông báo lỗi nghiệp vụ hết hạn**, **không** phải trang báo link lỗi và **không** bị đẩy về màn đăng nhập LME.<br>Nếu ra màn đăng nhập ⇒ **fail** — domain riêng của bot chưa được bản fix xử lý (xem `G8`, chạy tiếp `TC-ENV003-03`). |  | Lấp `G4` — ô **D4** · liên quan `G8` · Nguồn: spec [bot-edit](../../spec-features/admin/bot-edit/web/api-spec.md) (trường domain rút gọn tùy chỉnh, có kiểm DNS) + spec [qr-landing](../../spec-features/admin/qr-landing/web/logic-spec.md) (link chia sẻ ưu tiên domain riêng của bot) · RULE-08 → production · Đánh giá spec: **Spec không ghi** → §6 mục 3 · Evidence: screenshot + URL đầy đủ |
| TC-REGSHARED001-16 | Data | COMPAT-LEGACY-001 | Domain form thế hệ cũ — bot hết hạn | Abnormal | manual | product | Bot hết hạn: mở form trên domain form thế hệ cũ `step3.lmes.jp` vẫn hiện màn thông báo lỗi nghiệp vụ (đối chứng âm — domain đang **ngoài** danh sách) | - Bản fix đã deploy production, chế độ gỡ lỗi tắt<br>- Có form cũ của bot A còn redirect qua `step3.lmes.jp` và mở được (theo kho `TC-FORM-457`)<br>- Bot A điều chỉnh được mốc hết hạn; U1 đã kết bạn | 1. Mốc **17h59**: U1 mở form qua `step3.lmes.jp` → ghi trang.<br>2. Mốc **18h01**: mở lại trong LINE app và ngoài LINE → ghi trang + mã trạng thái.<br>3. Lưu kết quả làm **mốc đối chứng**.<br>4. **Chạy lại toàn bộ TC này sau khi Dev bổ sung `step3.lmes.jp` vào danh sách domain** (theo `G2`) → so 2 lần chạy. | 2 mốc × 1 domain thế hệ cũ × 2 cách mở × 2 lần chạy (trước / sau khi thêm domain cũ vào danh sách) | Cả 2 lần chạy, mốc 18h01 đều phải hiện **màn thông báo lỗi nghiệp vụ hết hạn** — không bị thay bằng trang báo link lỗi, không về màn đăng nhập.<br>Nếu lần chạy **sau** (đã thêm domain cũ vào danh sách) đổi thành 「お探しの…」 ⇒ **fail**: việc lấp `G2` đã kéo theo nuốt thông điệp nghiệp vụ trên domain cũ. |  | Lấp `G4` — ô **D5** · gắn với `G2`/`Q1`, **chạy 2 lần** trước/sau khi Dev thêm domain cũ · regression · dẫn từ kho `TC-FORM-457` + `TC-FORM-415` · RULE-08 → production · Đánh giá spec: **Spec không ghi** → §6 mục 3 · Evidence: screenshot 2 lần chạy |
| TC-REGSHARED001-17 | UI | REG-SHARED-001 | Domain quản trị `step.lme.jp` — bot hết hạn | Abnormal | manual | product | Bot hết hạn: màn quản trị trên `step.lme.jp` vẫn hiện đúng thông báo hợp đồng hết hạn cho admin (đối chứng âm — domain quản trị phải giữ nguyên hành vi cũ) | - Bản fix đã deploy production, chế độ gỡ lỗi tắt<br>- Có tài khoản admin của bot A đăng nhập được `https://step.lme.jp/`<br>- Bot A ở trạng thái hết hạn quá 18 giờ | 1. Đăng nhập `step.lme.jp` bằng tài khoản của bot A → ghi màn hình ngay sau đăng nhập.<br>2. Mở 1 màn chức năng bị chặn khi hết hạn (màn hợp đồng / thanh toán) → ghi trang.<br>3. Mở 1 URL không tồn tại trên `step.lme.jp` → ghi trang (đối chiếu `TC-REGSHARED001-02`).<br>4. Kiểm cả 3 bước xem có trang nào hiện 「お探しのページが見つかりませんでした。」 không. | 1 domain quản trị × 3 loại thao tác (đăng nhập / màn bị chặn / URL không tồn tại) | Bước 1-2: hiện **đúng thông báo hợp đồng hết hạn / màn hướng dẫn gia hạn** như trước bản fix.<br>Bước 3: giữ nguyên hành vi cũ (về `basic/overview` hoặc màn đăng nhập).<br>Bất kỳ bước nào ra trang báo link lỗi mới ⇒ **fail**: guard đã lan sang domain quản trị, trái mục 2 journal Dev. |  | Lấp `G4` — ô **D6** · đối chứng âm cho `T4` · regression · Đánh giá spec: **Spec ghi rõ** — journal Dev mục 2 vòng 3: 「mọi domain khác kể cả domain quản trị giữ nguyên hành vi cũ」 · Evidence: screenshot 3 bước |
| TC-REGSHARED001-18 | UI | ENV-002 | Cấu hình danh sách domain — bot hết hạn | Boundary | manual | Tất cả | Trang lỗi nghiệp vụ không bị nuốt ở **mọi cấu hình danh sách domain**: mặc định · khai `INVALID_LINK_DOMAINS` (kể cả khai **thừa** domain đang phục vụ trang nghiệp vụ) · chỉ dùng domain suy từ cấu hình link rút gọn | - Môi trường test sửa được tệp cấu hình môi trường; **ghi lại giá trị gốc** để khôi phục<br>- Xoá cache cấu hình sau **mỗi** lần đổi<br>- Bot A hết hạn quá 18 giờ, có form F1 công khai mở được<br>- Chế độ gỡ lỗi tắt ở cả 4 cấu hình | 1. **Cấu hình 1** — không khai `INVALID_LINK_DOMAINS`: mở form F1 → ghi trang.<br>2. **Cấu hình 2** — khai `INVALID_LINK_DOMAINS` **có** domain đang mở form: mở lại → ghi trang.<br>3. **Cấu hình 3** — khai `INVALID_LINK_DOMAINS` **không** có domain đó: mở lại → ghi trang.<br>4. **Cấu hình 4** — bỏ trống `INVALID_LINK_DOMAINS`, chỉ để domain suy từ `URL_OUTSIDE_STEP` rồi `URL_OUTSIDE_STEP_V2`: mở lại → ghi trang.<br>5. Khôi phục cấu hình gốc, xoá cache, mở lại lần cuối để xác nhận môi trường sạch. | 4 cấu hình danh sách domain × 1 kịch bản bot hết hạn ở mốc 18h01 | Cả **4 cấu hình** đều hiện **màn thông báo lỗi nghiệp vụ hết hạn** — danh sách domain **không được** đổi kết cục của trang lỗi nghiệp vụ.<br>Cấu hình nào ra 「お探しのページが見つかりませんでした。」 ⇒ **fail** + ghi rõ cấu hình đó: đây là bằng chứng guard **phân loại lỗi sai**, không phải lỗi cấu hình.<br>Bước 5 phải trở lại đúng kết quả bước 1. |  | Lấp `G4` — ô **D7 + D8** · **khác** `TC-ENV002-01` / `TC-ENV002-02` / `TC-ENV003-02`: các TC đó test **URL hỏng**, TC này test **lỗi nghiệp vụ** ở cùng 4 cấu hình · Đánh giá spec: **Spec không ghi** → §6 mục 2 · Evidence: screenshot 4 cấu hình + bản ghi giá trị cấu hình trước/sau |
| TC-ENV003-03 | UI | ENV-003 | Danh sách domain — domain rút gọn riêng của bot | Abnormal | manual | product | URL hỏng trên **domain rút gọn riêng của bot** phải ra trang báo link lỗi, không đẩy friend về màn đăng nhập | - Bản fix đã deploy production, chế độ gỡ lỗi tắt<br>- Bot A đã khai **domain rút gọn URL tùy chỉnh** ở màn 「アカウント設定」 và domain đó mở được link thật<br>- **Không** khai `INVALID_LINK_DOMAINS` (để hệ thống dùng danh sách mặc định)<br>- Trình duyệt ở cửa sổ ẩn danh, không có phiên đăng nhập | 1. Mở 1 URL không khớp route trên domain riêng: `https://<domain riêng>/test` → ghi trang + mã trạng thái + URL cuối.<br>2. Mở 1 link **đúng route nhưng mã không tồn tại** trên domain riêng → ghi tương tự.<br>3. Mở đúng 2 dạng URL đó trên `s.lmes.jp` để đối chiếu.<br>4. Ở cả 4 lần mở, kiểm có nút hay liên kết nào dẫn về `/admin/home` / màn đăng nhập không. | 2 dạng URL hỏng × 2 domain (domain riêng của bot vs `s.lmes.jp`) | Theo expected Leader đã chốt: **cả 2 domain** đều ra trang 「お探しのページが見つかりませんでした。」, không có nút 「TOPページへ戻る」, không về màn đăng nhập.<br>⚠️ Danh sách hiện tại chỉ hard-code 3 host nên domain riêng của bot **không** nằm trong đó ⇒ TC này **dự kiến FAIL** — đúng mục đích: đo phần phạm vi còn hụt. Người test **không được** đổi expected để cho pass. |  | Lấp `G8` · Nguồn: spec [bot-edit](../../spec-features/admin/bot-edit/web/api-spec.md) + [qr-landing](../../spec-features/admin/qr-landing/web/logic-spec.md) · RULE-08 → production · Đánh giá spec: **Spec không ghi** → §6 mục 3 · Evidence: screenshot + URL đầy đủ + mã trạng thái 4 lần mở |
| TC-ENV003-04 | UI | ENV-003 | Danh sách domain — kiểm kê toàn bộ domain phát link cho friend | Boundary | manual | product | Kiểm kê **đủ mọi loại domain đang phát link cho friend** trên môi trường thật rồi mở URL hỏng trên từng domain — lập bảng domain nào ra trang báo link lỗi, domain nào còn về màn đăng nhập | - Bản fix đã deploy production, chế độ gỡ lỗi tắt<br>- Đã hỏi Dev / vận hành lấy **danh sách đầy đủ** domain đang phục vụ link cho friend: 3 domain mặc định (`s.lmes.jp` · `sl.lmes.jp` · `form.lmes.jp`), domain form thế hệ cũ (`step3.lmes.jp`), **mọi domain rút gọn riêng của bot** đang khai khác rỗng, và domain suy từ cấu hình link rút gọn của môi trường<br>- Trình duyệt ở cửa sổ ẩn danh | 1. Lập danh sách domain theo tiền đề, ghi rõ **nguồn** của từng domain (hard-code / cấu hình môi trường / khai theo bot).<br>2. Với **mỗi** domain: mở `https://<domain>/test` → ghi mã trạng thái + trang hiển thị + URL cuối.<br>3. Với **mỗi** domain: mở thêm 1 URL **đúng route nhưng mã không tồn tại** → ghi tương tự.<br>4. Lập bảng 4 cột: `domain` · `nguồn` · `có trong danh sách của bản fix` · `kết cục thực tế`. | Toàn bộ domain thu thập được ở bước 1 × 2 dạng URL hỏng | Bảng kết quả phải cho thấy **mọi domain phát link cho friend** đều ra trang 「お探しのページが見つかりませんでした。」.<br>Domain nào còn về màn đăng nhập hoặc `/error/404` cũ (có nút 「TOPページへ戻る」) ⇒ **fail**, ghi tên domain vào báo cáo — đó chính là danh sách domain còn thiếu trong bản fix, đưa về §6 mục 3. |  | Lấp `G8` + `G2` · **khác** `TC-REGSHARED001-07` (#16582): TC đó chỉ chạy đúng 3 domain mặc định, TC này quét **toàn bộ** domain thật gồm domain **ngoài** danh sách · RULE-08 → production · Đánh giá spec: **Spec không ghi** → §6 mục 3 · Evidence: bảng kết quả + screenshot từng domain |

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | Phạm vi #38765 — định nghĩa "URL không hợp lệ" | **Test Leader đã chốt expected**: mọi link LME đã phát cho friend mà **đối tượng bị xoá** thì phải ra trang 「お探しのページが見つかりませんでした。」 — gồm 7 nhóm: form (cũ + mới) · QR (thường + giới thiệu) · salon (đặt chỗ + huỷ) · lesson (đặt chỗ + huỷ) · event (đặt chỗ) · item (đơn: mua; chu kỳ: mua + đổi thẻ + huỷ) · link rút gọn của LP. **Cần BA xác nhận để đưa vào spec**, vì hiện các controller tự `redirect()->route('404')` ⇒ ra trang `/error/404` **CŨ có nút 「TOPページへ戻る」 trỏ `/admin/home`** — tức **vẫn đưa friend về màn đăng nhập**, đúng thứ KH phàn nàn. Đây nhiều khả năng là **ca lỗi phổ biến nhất ngoài thực tế** (link hỏng thường do đối tượng bị xoá / sai mã, không phải sai route). Đã có `TC-COMPATLEGACY001-02` + `TC-LIFFENTRY001-04…-11` ở §5 phủ đủ 7 nhóm — **dự kiến FAIL**, fail thì raise ticket mở rộng phạm vi fix | `TC-TOOLSCOPE001-01` (#16560) `skip / needs-human-review` + log run #1039 · yêu cầu của Test Leader (2026-09-08) · mâu thuẫn với design BA (journal 2026-08-10 yêu cầu **bỏ** nút dẫn về TOP) | `BA` xác nhận — `Leader` đã chốt expected |
| 5 | Hành vi link khi đối tượng nằm trong **thùng rác** | Chưa nơi nào định nghĩa: đối tượng xoá mềm (còn trong thùng rác) thì link phải ra trang báo link lỗi hay vẫn mở được, và khôi phục từ thùng rác thì link có sống lại không. Ảnh hưởng trực tiếp cách chấm `TC-LIFFENTRY001-11` | `TC-LIFFENTRY001-11` §5 · FA-017 Cụm G 「ゴミ箱」 | `BA` |
| 2 | Điều kiện kích hoạt theo cấu hình môi trường | Chốt: khi `URL_OUTSIDE_STEP` **và** `URL_OUTSIDE_STEP_V2` đều rỗng thì 3 domain mặc định còn hiệu lực hay không. Journal Dev 20/08 ghi *"URL_OUTSIDE_STEP rỗng → trả false → giữ hành vi cũ"*, nhưng code vòng 3 **hard-code 3 domain** trong `Handler::invalidLinkDomains()` nên vẫn ra trang mới (đã đo bằng run: 3 domain vẫn trả 404 + trang mới khi 2 env rỗng) | `TC-ENV002-02` (#16558) `skip / needs-human-review` — 2 oracle chọi nhau | `Dev` + `BA` |
| 3 | Danh sách domain áp trang báo link lỗi | Bổ sung vào spec: (a) domain thế hệ **cũ** `step3.lmes.jp` có nằm trong phạm vi không — kho `TC-FORM-457` xác nhận domain này còn dùng thật trên production; (b) `INVALID_LINK_DOMAINS` **thay thế toàn bộ** danh sách mặc định ⇒ khai thiếu ở môi trường thật là **mất tính năng**, phải ghi thành mục cấu hình bắt buộc khi deploy; (c) **domain rút gọn riêng của từng bot** (`bots.domain_url_shorten`, khai ở màn 「アカウント設定」, có kiểm DNS) có thuộc phạm vi không — spec qr-landing ghi link chia sẻ QR giới thiệu dựng theo thứ tự `domain_url_shorten` → `URL_OUTSIDE_STEP` → link lưu sẵn, nghĩa là **link đã phát cho friend có thể nằm trên domain của khách**, trong khi danh sách của bản fix không đọc tới trường này | `G2` / `Q1` + `TC-ENV002-01` (#16556) · **`G8`** + `TC-ENV003-03` / `TC-ENV003-04` | `Dev` + `Leader` |
| 4 | Nội dung trang báo link lỗi (ticket + spec FA-017) | Description Redmine hiện vẫn ghi text đề xuất ban đầu 「このリンクは無効です」, trong khi bản chốt là 「お探しのページが見つかりませんでした。」 + **bỏ** dòng phụ và nút 「TOPページへ戻る」 (design BA 2026-08-10, code vòng 3). Cần cập nhật mô tả ticket/spec để lần review sau không lấy nhầm text cũ làm expected | Journal Dev vòng 1 (2026-07-16) vs journal BA 2026-08-10 vs journal Dev vòng 3 (2026-08-20) | `BA` |
