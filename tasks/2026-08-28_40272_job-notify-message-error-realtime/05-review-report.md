# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| **Nguồn đã dùng** | **NGUỒN 1 — MCP LME TEST STUDIO**, task `#260` (ticket Redmine `40272`), `testcase_list` → **28 TC** |
| Vì sao không dùng nguồn 2/3 | Nguồn 1 có TC → **dừng ngay**, KHÔNG fetch Sheet human, KHÔNG đọc file 04 làm nguồn, **KHÔNG đối chiếu chéo** (đúng quy tắc BƯỚC 0) |
| Thời điểm fetch | 2026-08-28 (snapshot ghi vào `04-tc-list.md`, header `<!-- source: MCP LME TEST STUDIO ... -->` → refresh hợp lệ) |
| Studio task | `#260` · round 1 · `status = done-ai` · `aiResult = pass` · `reviewState = leader` · `reviewed = false` · `submittedWithoutMcp = true` |
| Branch | `m_202608_notify-pc-msg-error_39543_improve` |
| **Nguồn spec đã dùng** | [spec-features/admin/notify-setting/feature-spec.md](../../spec-features/admin/notify-setting/feature-spec.md) — bảng Business rules **BR-01 → BR-15** (dòng 596–610). Đủ trả lời "hành vi đúng là gì" → **không** cần lên https://lme.jp/manual/. |
| kho-tcs | **Chưa có** file kho cho `notify-setting` (`ls kho-tcs/*.md` → 18 file, không có mã màn hình của tính năng này) → không đối chiếu được TC cũ ở kho. |

### 0.6 — Cảnh báo chất lượng nguồn

> ⚠️ **ĐÍNH CHÍNH quan trọng**: `testcase_list` chỉ trả `last_exec` (lần chạy **cuối** của mỗi TC). Đọc riêng nó sẽ ra kết luận sai "10 TC skip / 18 pass". Đã gọi thêm **`task_get_report`** → bức tranh thật khác hẳn.

| # | Nội dung | Kết quả thực tế | Flag |
|---|---|---|---|
| 1 | **Kết quả thực thi thật** | **Auto**: pass 12 · fail 0 · skip 9 (2 run). **Manual**: pass 34 · fail 0 · skip 1. **Cả 28/28 TC đều đã pass ≥ 1 lần.** 9 `skip` của run #760 là `[SOURCE_BLOCKED] web: SOURCE_BRANCH_MISSING: project web chưa có branch run` → **hạ tầng, không phải verdict test**. | — (không flag pass-rate) |
| 2 | **TC fail / có ticket bug** | `bugs.total = 0`, không TC nào gắn ticket. **NHƯNG** evidence của `15119` ghi thẳng *"ĐÂY LÀ HỒI QUY do thay đổi lần này"* và `15117` ghi *"thông báo mất vĩnh viễn, không có cơ chế thử lại"* — **cả 2 vẫn để `pass`, KHÔNG raise ticket**. | **`[BLOCKER]`** — xem B1, B2 |
| 3 | **Môi trường đã chạy** | Auto run `#739` = **local**, run `#760` = **staging**. Manual = local + staging. **PRODUCTION = 0 TC.** Task chạm **job nền + ALTER TABLE**. | **`[MAJOR]` RULE-08 / ENV-003** |
| 4 | **Ai chạy** | 12 kết quả auto (`ai/haodtb`) có log + evidence.json + screenshot. **34 kết quả manual (`haodtb`) có `evidence = []` và `actual = null` ở 33/34.** `submittedWithoutMcp = true`. | **`[BLOCKER]` RULE-02** — xem B6 |
| 5 | **Tác giả TC** | `provenance.source`: **21 TC = `haodtb@mcp`** (người viết, ghi qua kênh MCP) · **7 TC = `AI`**. `toolWritten.mcp = 21` chỉ là **kênh ghi**. → **KHÔNG** phải "≥50% do AI sinh"; `reviewState = leader`, `reviewed = false`. | — |
| 6 | **Mã quan điểm không có trong `checklist-lme.md`** | `RULE-09` (8 TC — **là mã RULE, không phải mã quan điểm**) · `TOOL-NEGCTRL-001` (7 TC — mã nội bộ Studio) · `JOB-002` (1 TC). → **16/28 TC KHÔNG được tính là cover** ở BƯỚC 2/3.6. | **`[MAJOR]`** — xem M3 |

**Bổ sung ngoài checklist 0.6 — 2 điểm Leader phải biết:**

- **Run staging `#760` có `status = "fail"`** ở cấp run (5 pass / 9 skip) trong khi task-level `aiResult = "pass"`. Hai con số này mâu thuẫn → đừng đọc `aiResult` như "đã test xong trên staging".
- **Bộ TC này là phần BỔ SUNG, không phải bộ đầy đủ.** Note của nhiều TC dẫn *"bộ 32 TC"*, `existing-tc:NEW-16`, `NEW-29`, `NEW-31`. Studio có **task `#155` cho ticket cha `#39543` với 40 TC** (`released`). **Tôi KHÔNG gộp 2 nguồn** (đúng quy tắc), nhưng `temp_id` **trùng nhau** giữa 2 bộ (cả 2 đều có `NEW-1`, `NEW-16`…) → Leader phải chốt phạm vi review. Xem M12.
- **Studio tự chấm coverage spec: `0/25 covered · 24 partial · 1 none`.** Mục `none` = **BR-09** kèm note *"badge = bots.count_app_notify — lệch code job đã chuyển mobile_badge_noitfy theo bot+user (#38562)"*.

---

## 1. Verdict

# ❌ REJECTED

**7 `[BLOCKER]`.** Hai loại rủi ro tách biệt:

1. **2 hành vi lỗi ĐÃ ĐƯỢC CHỨNG MINH bằng evidence lại bị chấm `pass` và không raise ticket** (B1 hồi quy đổi chu kỳ, B2 mất notify khi web lỗi). Đây không phải "thiếu TC" — TC đã bắt được lỗi nhưng verdict ghi sai.
2. **4 impact trực tiếp của cách fix không có TC nào** (B3 mốc `NULL` khi deploy lần đầu, B4 job thứ 4 dùng chung `job_config_daily`, B5 bỏ `markChatworkDone`, B7 job đang chạy dở khi deploy) + **B6** toàn bộ 34 verdict manual không có evidence.

---

## 2. Tóm tắt cho member

Bộ TC **có chiều sâu kỹ thuật rất tốt** ở phần AI sinh: `NEW-1/2/3/4/5/6/7` dựng được kịch bản khó (bot đã xóa, bảng lỗi rỗng, chèn 1800 bản ghi để bắt race, tiêm lỗi 502 vào web quản trị, đổi chu kỳ trên màn hình thật) và evidence auto rất chi tiết — đây là chuẩn nên giữ.

Phải fix 3 nhóm: **(1)** `NEW-7` và `NEW-5` đã chứng minh được lỗi thật (hồi quy + mất notify vĩnh viễn) nhưng vẫn để `pass` và không raise ticket — verdict phải sửa và phải raise bug; **(2)** cách fix có 4 vùng chưa hề có TC: mốc `NULL` ở lần deploy đầu sau `ALTER TABLE`, `MonitorCalendarBookingTask` (job thứ 4 ghi cùng `job_config_daily`), phần "Fix kèm" bỏ `markChatworkDone` cho notify quá 24h, và job đang chạy dở lúc deploy; **(3)** 34/34 kết quả manual không đính kèm evidence nào — theo RULE-02 những verdict đó chưa có giá trị.

---

## 3. Coverage Matrix

> `Exec` = `<số TC đã pass ≥1 lần>/<số TC>` (tính theo **toàn bộ lịch sử run**, không theo `last_exec`).
> ⚠️ TC mang mã `RULE-09` / `TOOL-NEGCTRL-001` / `JOB-002` **vẫn được tính cover impact** (map theo nội dung), nhưng **không** được tính cover **quan điểm** ở §3.6.

| Impact | Loại | TC cover (suy luận) | # TC | Exec | Status |
|---|---|---|---|---|---|
| BUG — mốc `message_error.id` thay cho `countMsgError` ở nhánh realtime | Fix | NEW-1, NEW-4, NEW-6, NEW-8, NEW-10 | 5 | 5/5 | **OK** |
| F1 — `startJobNotify`/`doPushNotify`/`createMsgErrorNotify`/`buildMsgErrorNotify` (app + PC) | Function | NEW-1, NEW-2, NEW-3, NEW-4, NEW-5 | 5 | 5/5 | **OK** |
| F2 — `HandlePushNotifyChatwork.run`/`pushPendingNotify`/`createMsgErrorNotify`/`buildContentNotify`/`updateNextSchedule`/`pushNextScheduleDefault` | Function | NEW-10, NEW-11, NEW-12…18 | 9 | 9/9 | **RISK** — chỉ happy path; `updateNextSchedule`/`pushNextScheduleDefault`/`pushPendingNotify` không có TC riêng (xem B5, M7) |
| F3 — `HandlePushNotifyPc.*` (Dev ghi trùng nửa PC của F1) | Function | NEW-4, NEW-5 | 2 | 2/2 | **RISK** — `NEW-5` verdict sai (B2) |
| F4 — `JobConfigDailyRepository.updateLastIdMessageErrorNotify{App,Pc,Chatwork}` + **`updateBookingLastId`** | Function | NEW-9 (3 cột mốc); **`updateBookingLastId` = 0 TC** | 1 | 1/1 | **GAP** (phần `updateBookingLastId`) — xem **B4** |
| F5 — `MessageErrorRepository.findMaxId` / `findDistinctBotIdByIdBetween` + entity `JobConfigDaily` | Function | NEW-3, NEW-6 | 2 | 2/2 | **OK** |
| D1 — `job_config_daily` + 3 cột mốc (**MIGRATE / ALTER TABLE**) | Data | NEW-3 (chỉ chạy với mốc = `1000`, bảng lỗi **rỗng**) | 1 | 1/1 | **GAP** — nhánh mốc `NULL` + bảng lỗi **đầy dữ liệu cũ** chưa từng chạy. Xem **B3** |
| D2 — ghi lại 3 cột mốc sau mỗi vòng job | Data | NEW-3, NEW-6, NEW-8, NEW-9 | 4 | 4/4 | **OK** |
| D3 — `job_config_daily` ghi bằng `updateBookingLastId()` thay `save()` full-column (bản ghi `id=1` dùng chung 4 job) | Data | NEW-9 (chỉ app ⇄ PC) | 1 | 1/1 | **RISK/GAP** — không TC nào chạm job thứ 4 (`MonitorCalendarBookingTask`) hay cột `lesson_booking_last_id`. Xem **B4** |
| D4 — `notify_setting`: nhánh realtime ChatWork bỏ UPDATE khi không có gì để gửi | Data | NEW-10 (gián tiếp) | 1 | 1/1 | **RISK** — không TC nào verify "KHÔNG có UPDATE" |
| D5 — bỏ `markChatworkDone` giữa vòng lặp cho notify quá 24h (chatwork 429) | Data | **0** | **0** | — | **GAP** — xem **B5** |
| T1 — Notify 配信エラー realtime **app** | Feature | NEW-1, NEW-2, NEW-3, NEW-8, NEW-26/27/28 | 7 | 7/7 | **OK** |
| T2 — Notify 配信エラー realtime **PC** | Feature | NEW-3, NEW-4, NEW-5, NEW-9, NEW-26/27/28 | 7 | 7/7 | **RISK** — `NEW-5` verdict sai (B2) |
| T3 — Notify 配信エラー realtime **ChatWork** | Feature | NEW-10, NEW-11, NEW-26/27/28 | 5 | 5/5 | **RISK** — không có case abnormal (room bị xóa / token hết hạn / 429) |
| T4 — Notify 配信エラー **theo schedule** 3 kênh (15p→24h) + badge + counter | Feature | NEW-7, NEW-19…25 | 8 | 8/8 | **RISK** — 7 TC gần trùng nhau, steps 2 dòng, không đo counter/badge; `NEW-7` verdict sai (B1). Xem §4.5 |
| T5 — Notify ChatWork loại khác (add friend, QR, chat 1-1, メッセージ配信, action schedule, ASP, conversion) | Feature | NEW-12…18 | 7 | 7/7 | **RISK** — happy path thuần (AP-3); thiếu nhánh "notify quá 24h vẫn bị bỏ qua" + "chưa liên kết room / bot hết hạn → đẩy lịch 15 phút" mà Dev nêu ở T5 |
| T6 — **Deploy lần đầu (mốc = 0)**: vòng đầu chỉ ghi `MAX(id)`, KHÔNG bắn notify cho toàn bộ lỗi cũ | Feature | NEW-3 (điều kiện **ngược lại**: bảng lỗi rỗng, mốc = 1000) | 1 | 1/1 | **GAP** — xem **B3** |

### ORPHAN TCs

**Không có ORPHAN.** Cả 28 TC đều trace về `BUG` / `F*` / `D*` / `T*` trong `03-dev-impact.md`.
Cụ thể `NEW-12…18` (7 loại action ChatWork khác) **không** phải AP-5 over-coverage — Dev **liệt kê đích danh** 7 loại này ở mục 4.3 T5, nên đây là vùng regression hợp lệ do Dev chỉ định (RULE-12 phần 2).

---

## 3.5 Fix-shape analysis (adversarial)

Mục 2 `03-dev-impact.md` khớp **4 fix shape**:

| Fix shape | Câu hỏi adversarial | Trả lời từ bộ TC | Kết luận |
|---|---|---|---|
| **"migration" / "alter table"** — thêm 3 cột `bigint NULL` vào `job_config_daily` | Data cũ có mất không? Migration chạy lại được không? Nhánh cũ chạy song song nhánh mới? Mốc `NULL` ở vòng đầu xử lý ra sao? | `NEW-3` là TC duy nhất chạm nhánh này, nhưng evidence run `#739` ghi rõ **`mốc ban đầu app=1000, pc=1000`** và **`số bản ghi bảng lỗi gửi tin = 0`** → **đúng điều kiện NGƯỢC LẠI** với thực tế deploy (mốc `NULL`, bảng `message_error` có hàng chục triệu bản ghi cũ). Nhánh `findDistinctBotIdByIdBetween(NULL, maxId)` **chưa bao giờ chạy**. | **`[BLOCKER]` B3** — DATA-MIG-001 |
| **"sửa hàm dùng chung"** — `job_config_daily` (1 bản ghi `id = 1`) dùng chung **4 job** | Dev có cung cấp danh sách nơi ảnh hưởng không? TC test **từng nơi** trong danh sách? | Dev **CÓ** cung cấp (mục 3: "đã rà toàn bộ 4 callsite"). Nhưng TC chỉ chạm **3/4 job**. `MonitorCalendarBookingTask.monitorSalonCalendar` — chính là chỗ Dev **đổi code** từ `save()` sang `updateBookingLastId()` — **0 TC**. | **`[BLOCKER]` B4** — REG-SHARED-001 |
| **"fix race condition" / batch đa luồng chạm giới hạn dùng chung** | Đủ kịch bản đồng thời? Evidence **đếm số lần xử lý thực tế** từ log/DB? | `NEW-6` xuất sắc: chèn 1800 bản ghi, evidence đếm được **"số khe hở giữa các khoảng quét = 0"**, **"số bản ghi nằm NGOÀI mọi khoảng quét = 0"**. `NEW-9` phủ độc lập cột app ⇄ PC (pass local). **Còn thiếu**: 4 job ghi **đồng thời** cùng bản ghi `id=1` (job thứ 4 không có mặt), và job **chạy 2 instance** / restart giữa vòng. | `NEW-6`/`NEW-9` đạt → **không** BLOCKER cho CONC-001. Phần thiếu gộp vào **B4 + B7** |
| **"fix N+1 / performance"** — mục 1 nêu lý do chính là *"count message_error … mỗi vòng 60s ở cả 3 job → query nặng"* | Có đo tải query trước/sau? Test ở quy mô khách hàng lớn nhất thực tế? | **Không TC nào đo mục tiêu chính của ticket.** `NEW-6` chèn 1800 bản ghi nhưng để bắt race, không đo số query / thời gian vòng. `NEW-5` biến thể 2 có đo (187s / 5 bot) nhưng đó là đo **điểm nghẽn**, không phải cải thiện. | **`[MAJOR]` M8** — PERF-LARGE-001 |

**Symptom-only KH report check**: **Không áp dụng.** Redmine `#40272` tracker = `Improve nội bộ`, do Dev tự tạo, `01-bug-task.md` không có Steps/Expected/Actual. Không có triệu chứng KH để suy ngược ≥2 root cause.

**Anti-patterns:**

| AP | Dính? | Ghi chú |
|---|---|---|
| AP-1 Single-trigger generic-fix | Không | Fix không phải generic catch. *(Ghi nhận: `NEW-5` phát hiện `RequestHelper.executeSyncRequest` nuốt Exception chỉ ghi log — là code **có sẵn**, không phải fix lần này.)* |
| AP-2 Symptom-only KH report | Không | Không có report KH |
| **AP-3 Happy-path-only regression** | **CÓ** | T5 (NEW-12…18) và T4 (NEW-19…25) — mỗi Tx chỉ có precondition "data sạch", 2–3 bước, không có edge state (room bị xóa, token hết hạn, 429, notify quá 24h, bot hết hạn). → **`[MAJOR]` M7** |
| AP-4 Specific code-check disguised as generic catch | Không | Mục "Commit / Pull Request" **có** đủ (`06cd8fb6`, `ad10a531`, `be829f69`) |
| AP-5 Layer-downstream over-coverage | Không | NEW-12…18 nằm trong T5 do Dev chỉ định |
| AP-6 Mục 3 dev-impact trống | Không | Dev điền đủ mục 3 (3 dòng caller + 4 callsite) |

---

## 3.6 Bảng quan điểm đối chiếu

> TC mang mã `RULE-09` / `TOOL-NEGCTRL-001` / `JOB-002` **không được tính cover** (quy tắc 0.6 #6).

| Mã quan điểm | Ưu tiên | Trigger khớp task? | TC cover (suy luận) | Exec | Kết luận |
|---|---|---|---|---|---|
| `JOB-001` ★ | **Cao** | ◯ **BẮT BUỘC** — task thêm/sửa job nền (3 job + 1 job dùng chung bảng config) | **0** (NEW-5 mang mã `JOB-002` — không có trong checklist) | — | **`[BLOCKER]` GAP** → B7 |
| `ENV-003` ★ | **Cao** | ◯ **BẮT BUỘC** — chạm job nền | **0** TC chạy production | 0/28 | **`[MAJOR]` RULE-08** → M1 |
| `DATA-MIG-001` | **Cao** | ◯ **BẮT BUỘC** — `ALTER TABLE` + tồn tại dữ liệu cũ | **0** (NEW-3 chạy điều kiện ngược) | — | **`[BLOCKER]` GAP** → B3 |
| `REG-SHARED-001` | **Cao** | ◯ **BẮT BUỘC** — `job_config_daily` dùng chung 4 job | Thiếu job thứ 4 | — | **`[BLOCKER]` GAP một phần** → B4 |
| `REG-RUN-001` | **Cao** | ◯ **BẮT BUỘC** — có job/dữ liệu đang chạy dở khi release | **0** | — | **`[BLOCKER]` GAP** → B7 |
| `DEPLOY-LIVE-001` ★ | **Cao** | ◯ — deploy production không bật maintain, đổi schema | **0** | — | **`[MAJOR]`** → gộp vào B3 |
| `CONC-001` | **Cao** | ◯ **BẮT BUỘC** — batch đa luồng + giới hạn/bản ghi dùng chung | NEW-6 (Boundary), NEW-9 (Boundary) | 2/2 | **OK về nội dung**, nhưng **thiếu Normal + Abnormal** → **`[MAJOR]` RULE-01** |
| `DATA-DB-001` ★ | **Cao** | ◯ **BẮT BUỘC** — có UPDATE (`job_config_daily`, `notify_setting`, `mobile_notify`) | NEW-1 (WHERE theo bot), NEW-9 (WHERE theo cột), NEW-11 (WHERE theo room) | 3/3 | **RISK** — `markChatworkDone` update theo `(bot_id, user_id)` (D5) không có TC → B5 |
| `SEC-ISO-001` | **Cao** | ◯ — job xử lý **nhiều bot / nhiều người nhận** đồng thời | NEW-1, NEW-4 | 2/2 | **RISK** — 2 TC đều `Normal`, thiếu Abnormal + Boundary → **`[MAJOR]` RULE-01**; thêm **xung đột BR-11** (M5) |
| `OUT-TRUTH-001` | **Cao** | ◯ **BẮT BUỘC** — mọi thao tác gửi có thông báo kết quả | NEW-26, NEW-27, NEW-28 | 3/3 | **RISK** — cả 3 đều `Normal`, thiếu Abnormal/Boundary → **`[MAJOR]` RULE-01**. Ngoài ra Expected dừng ở "3 nền tảng nhận notify" nhưng steps không nêu thiết bị thật → RULE-06 chưa chắc chắn |
| `DATA-COUNT-001` | **Cao** | ◯ **BẮT BUỘC** — có counter `*_total_msg_error` + badge `count_app_notify` | NEW-19…25 (nhắc "counter/badge" ở Expected, không đo) | 7/7 | **RISK/GAP** → **`[MAJOR]` M9**; Studio tự chấm **BR-09 = `none`** |
| `DATA-BACKUP-001` ★ | **Cao** | ◯ (trigger "thêm/đổi bảng DB") — **nhưng** `job_config_daily` là config hệ thống (1 bản ghi `id=1`), không nằm trong scope copy bot | **0** | — | **`[MAJOR]`** → M10: đề nghị Leader duyệt đánh **×** kèm lý do (RULE-03), không tự bỏ |
| `STATE-CLEAN-001` | **Cao** | ◯ — bot bị xóa / hết hạn quá 7 ngày | NEW-2 (Abnormal) | 1/1 | **RISK** — 1 TC, thiếu Normal/Boundary; còn câu hỏi mở cho Dev (M6) |
| `FUNC-004` | **Cao** | ◯ — giới hạn/biên (bảng lỗi rỗng, mốc biên) | NEW-3 (Boundary) | 1/1 | **RISK** — thiếu Normal + Abnormal → RULE-01 |
| `DATA-ID-001` | Trung bình (→ Cao) | ◯ — phân biệt bot/room/người nhận trùng | NEW-8 (Normal), NEW-11 (Boundary) | 2/2 | **OK** |
| `SYNC-APP-001` | Trung bình (→ Cao) | ◯ — notify hiện trên cả web (PC) và app | NEW-10 | 1/1 | **OK** |
| `PERF-LARGE-001` | Trung bình (→ Cao) | ◯ — mục tiêu chính của ticket là giảm tải query | **0** (không TC nào đo) | — | **`[MAJOR]`** → M8 |
| `COMPAT-LEGACY-001` ★ | **Cao** | ◯ — hành vi cũ (counter) vs mới (mốc id) chạy song song sau deploy | NEW-7 (đã đối chiếu bản cũ `64626ebd`) | 1/1 | **OK về ý định** — nhưng verdict sai → B1 |
| `DEPLOY-ASSET-001` ★ | Cao | × | — | — | Không sửa JS/CSS/font/icon — chỉ job backend |
| `MSG-*` / `PAY-*` / `MEDIA-*` / `LIFF-*` / `PERM-*` | — | × | — | — | Không chạm gửi tin theo filter, tiền, media, LIFF, phân quyền |

**Quan điểm ưu tiên Cao thiếu TC hoàn toàn**: `JOB-001`, `DATA-MIG-001`, `REG-RUN-001`, `DEPLOY-LIVE-001`, `PERF-LARGE-001`, `DATA-BACKUP-001`.

---

## 4. Issues phát hiện

### 4.1 Blocker (phải fix trước khi merge)

**`[BLOCKER]` B1 — NEW-7 (Studio `15119`): evidence khẳng định HỒI QUY, verdict vẫn `pass`, không raise ticket.**
Evidence của cả 2 run ghi nguyên văn: *"ĐỐI CHIẾU BẢN CŨ (64626ebd): nhánh realtime bản CŨ CÓ cập nhật thời điểm thông báo gần nhất và mốc đếm lỗi ⇒ sau khi đổi chu kỳ người dùng phải chờ đủ chu kỳ. Bản mới KHÔNG cập nhật ⇒ **ĐÂY LÀ HỒI QUY do thay đổi lần này**."* Và đo được: `thời điểm gửi kế tiếp rơi vào QUÁ KHỨ`, `bot A nhận thông báo … ~8s sau khi đổi chu kỳ, thay vì phải chờ 1 giờ`. Chính Expected của TC đã viết sẵn *"nếu đối chiếu bản cũ cho kết quả khác bản mới thì đây là HỒI QUY … và phải báo bug"* — điều kiện đó **đã thoả**. Nhưng `bugs.total = 0`.
→ **Fix**: đổi verdict `15119` thành **Không đạt**, raise ticket Redmine con của `#40272`, gắn vào cột `Số ticket bug`. Nếu PO xác nhận đây là hành vi chấp nhận được → phải cập nhật spec (BR-05/BR-06) và ghi quyết định vào §6, **không** để `pass` im lặng. Ảnh hưởng KH: người dùng đổi từ リアルタイム sang 15分/1時間… sẽ bị bắn notify ngay lập tức thay vì chờ hết chu kỳ.

**`[BLOCKER]` B2 — NEW-5 (Studio `15117`): mất notify vĩnh viễn đã được chứng minh, nhưng staging chấm `skip "ngoài phạm vi test"` và không raise ticket.**
Run auto `#739` (local) **pass** với evidence đo được: `OK bản ghi thông báo VẪN được tạo dù đẩy thất bại = 1` · `OK trạng thái kênh PC (đã đẩy) — dù U1 chưa nhận được gì = "1"` · `OK mốc kênh PC VẪN tiến tới bản ghi lỗi vừa tạo` · `OK số yêu cầu đẩy lại sau khi web hồi phục (không có cơ chế thử lại ⇒ thông báo mất vĩnh viễn) = 0` · và biến thể chậm: `OK một vòng quét kéo dài 187s ≈ 5 bot × 25s`. Đây là **REQ-001, `risk = High`**. Sau đó kết quả manual `#6242` trên staging lại ghi `skip — "ngoài phạm vi test"`, khiến `last_exec` thành `skip` và lỗi biến mất khỏi báo cáo.
→ **Fix**: raise ticket cho 2 vấn đề (ghi `status_pc = 1` khi đẩy thất bại → trạng thái không khớp sự thật, không có retry; và lệnh đẩy đồng bộ trong vòng lặp làm 1 vòng kéo dài tuyến tính theo số bot). Gỡ verdict `skip "ngoài phạm vi test"` — TC này **thuộc phạm vi** vì `HandlePushNotifyPc.createMsgErrorNotify` nằm đúng trong F1/F3.

**`[BLOCKER]` B3 — GAP-1: nhánh mốc `NULL` ở lần deploy đầu sau `ALTER TABLE` chưa từng được chạy (T6 + D1).**
Dev ghi ở 4.3 T6: *"Deploy lần đầu (mốc = 0): vòng chạy đầu của cả 3 job chỉ ghi nhận MAX(id), check KHÔNG bắn notify cho toàn bộ message_error cũ"*. TC duy nhất chạm vùng này là `NEW-3`, nhưng evidence run `#739` cho thấy nó chạy với **`mốc ban đầu app=1000, pc=1000`** và **`số bản ghi bảng lỗi gửi tin = 0`** (đã xoá 4041 bản ghi lịch sử) — tức **ngược hoàn toàn** với hiện trường deploy thật: 3 cột mới `BIGINT NULL DEFAULT NULL` = `NULL`, còn `message_error` giữ nguyên toàn bộ dữ liệu cũ.
→ **Rủi ro cụ thể**: nếu `findDistinctBotIdByIdBetween(NULL, maxId)` không được guard đúng, vòng chạy đầu sẽ lấy **toàn bộ bot từng có lỗi trong lịch sử** và bắn notify hàng loạt cho cả 3 kênh (app + web push + ChatWork) ngay sau khi deploy.
→ **Fix**: bổ sung `TC-DATAMIG001-01/-02/-03` ở §5, chạy trên **production-like** dữ liệu (RULE-08).

**`[BLOCKER]` B4 — GAP-2: `MonitorCalendarBookingTask` — job thứ 4 ghi cùng `job_config_daily` — không có TC nào (F4 + D3).**
Dev **đổi code** ở đây (`save()` full-column → `updateBookingLastId()`) chính vì nó sẽ ghi đè 3 cột mốc mới. Đây là fix shape **REG-SHARED-001** với danh sách 4 callsite do Dev cung cấp, nhưng TC chỉ phủ 3 job notify. Không TC nào verify **(a)** đặt lịch salon/lesson vẫn chạy đúng sau khi đổi sang UPDATE theo cột, **(b)** `lesson_booking_last_id` vẫn được ghi đúng, **(c)** `MonitorCalendarBookingTask` không xoá 3 cột mốc mới khi chạy đồng thời.
→ **Rủi ro**: nếu `updateBookingLastId()` sai, hoặc job này vẫn ghi đè, hệ quả là **notify lỗi gửi tin bị bắn lặp vô hạn** (mốc bị reset về giá trị cũ) hoặc **giám sát đặt lịch salon/lesson ngừng hoạt động**.
→ **Fix**: `TC-REGSHARED001-01/-02` ở §5.

**`[BLOCKER]` B5 — GAP-3: phần "Fix kèm" của ChatWork (bỏ `markChatworkDone` cho notify quá 24h) không có TC nào (D5).**
Mục 2 `03-dev-impact.md` nêu rõ: *"notify quá 24h chỉ bỏ qua chứ không markChatworkDone giữa vòng lặp nữa (hàm này update theo cả (bot_id, user_id) nên đánh dấu luôn notify mới chưa gửi, chatwork trả 429 là mất notify)"*. Đây là **thay đổi hành vi trực tiếp**, có `WHERE` scope sai đã được Dev thừa nhận — đúng trọng tâm `DATA-DB-001` (RULE-07). Trong 28 TC **không TC nào** nhắc tới 24h, `markChatworkDone`, hay 429.
→ Thêm nữa, thay đổi này **mâu thuẫn spec `BR-14`** (*"Thông báo ChatWork quá 24h … sẽ bị auto-expire (mark `status_chat_work=1`), không gửi"*). Nếu không còn mark, notify quá 24h sẽ **tồn đọng mãi** và bị quét lại mỗi vòng.
→ **Fix**: `TC-DATADB001-01/-02` ở §5 + cập nhật BR-14 (§6).

**`[BLOCKER]` B6 — RULE-02: 34/34 kết quả manual chấm `pass` với `evidence = []` và `actual = null`.**
`task_get_report` → `manual.results`: cả 34 bản ghi đều `"evidence":[]`; 33/34 có `"actual":null` (duy nhất `15248` có 1 dòng chữ, vẫn không kèm ảnh/log). Trong khi 12 kết quả auto đều có `tc-*-job.log`, `tc-*-evidence.json`, và `15119` còn có 2 screenshot.
→ Theo RULE-02, **verdict `Đạt` không có evidence đúng loại thì chưa có giá trị**. Điều này chạm phần lớn coverage của T3/T4/T5 — tức phần lớn "pass" của bộ TC này đang là tự khai.
→ **Fix**: chạy lại và đính kèm evidence cho ít nhất toàn bộ TC thuộc T4 + T5 (14 TC), hoặc hạ verdict về `Chưa test`.

**`[BLOCKER]` B7 — GAP-4: `JOB-001` + `REG-RUN-001` không có TC nào — job restart / deploy khi job đang chạy dở.**
`JOB-001` là quan điểm **BẮT BUỘC** cho mọi task thêm/sửa job nền, `REG-RUN-001` **BẮT BUỘC** cho mọi release khi hệ thống có job đang chạy dở. Bộ TC không có case nào cho: job bị kill/restart **giữa** lúc đã đọc `MAX(id)` nhưng **chưa** ghi mốc (mất hay lặp?); 2 instance job chạy song song; deploy code mới khi vòng cũ đang chạy dở; ChatWork còn notify pending lúc deploy. `NEW-5` có mã `JOB-002` — mã này **không có** trong `checklist-lme.md`.
→ **Fix**: `TC-JOB001-01/-02/-03` ở §5.

### 4.2 Major (nên fix)

- **`[MAJOR]` M1 — RULE-08 / ENV-003: 0/28 TC chạy production.** Task chạm **job nền** + **ALTER TABLE** — theo Catalog D không được kết luận từ local/staging. Toàn bộ evidence hiện có đến từ `local` (run #739) và `staging` (run #760). → Bổ sung `TC-ENV003-01` (§5) chạy smoke sau deploy trên `step.lme.jp`.
- **`[MAJOR]` M2 — `aiResult = pass` ở cấp task gây hiểu nhầm.** Run staging `#760` có `status = "fail"` (5 pass / 9 skip vì `SOURCE_BRANCH_MISSING: project web chưa có branch run`). Nghĩa là **branch web chưa được deploy lên staging**, nên toàn bộ nhánh phụ thuộc web quản trị không chạy được ở staging. → Deploy branch web lên staging rồi chạy lại run auto, hoặc ghi rõ giới hạn này vào kết luận.
- **`[MAJOR]` M3 — 16/28 TC dùng mã quan điểm không có trong `framework/checklist-lme.md`.** `RULE-09` (8 TC) — **`RULE-09` là mã RULE, không phải mã quan điểm**, đang bị điền nhầm vào cột quan điểm; `TOOL-NEGCTRL-001` (7 TC) — mã nội bộ Studio; `JOB-002` (1 TC) — không tồn tại. → Map lại: `NEW-7` → `COMPAT-LEGACY-001`; `NEW-19…25` → `REG-SHARED-001` hoặc `DATA-COUNT-001`; `NEW-12…18` → `REG-SHARED-001`; `NEW-5` → `JOB-001`.
- **`[MAJOR]` M4 — RULE-01: 5 quan điểm ưu tiên **Cao** thiếu loại case, không ghi lý do.** `CONC-001` (2 Boundary, thiếu Normal + Abnormal) · `SEC-ISO-001` (2 Normal, thiếu Abnormal + Boundary) · `OUT-TRUTH-001` (3 Normal, thiếu Abnormal + Boundary) · `STATE-CLEAN-001` (1 Abnormal) · `FUNC-004` (1 Boundary). Tổng thể phân bố **21 Normal / 3 Abnormal / 4 Boundary** — quá lệch về Normal cho một task job + migration.
- **`[MAJOR]` M5 — Xung đột spec BR-11 chưa được chốt nhưng TC vẫn `pass`.** Evidence `15113`/`15116`: *"môi trường DỰNG ĐƯỢC nhiều notify_setting cho cùng 1 bot (4 bản ghi cho bot 990430113) và job xử lý từng bản ghi ⇒ hành vi code khác BR-01, cần leader chốt"*. Spec `feature-spec.md` **BR-11** ghi *"Mỗi bot chỉ có 1 record trong `notify_setting`. Query luôn dùng `WHERE bot_id = ? LIMIT 1`"*. Code job lại duyệt **từng cặp (bot, user)**. → Chưa chốt mà đã `pass` = tự suy diễn. Đưa vào §6.
- **`[MAJOR]` M6 — NEW-2 (`15114`) để ngỏ câu hỏi cho Dev nhưng vẫn `pass`.** Evidence đo được: bot đã xoá (J1) và bot hết hạn >7 ngày (K1) **vẫn xuất hiện trong danh sách xử lý mỗi 60 giây** và `next_notify_time` **bị ghi lại ở MỖI vòng** (`true,true,true`). TC tự ghi *"CẦN DEV XÁC NHẬN: ghi DB lặp mỗi phút cho bot đã chết có được chấp nhận không"*. Đây đi ngược mục tiêu "giảm tải query" của chính ticket. → Đưa vào §6.
- **`[MAJOR]` M7 — AP-3 Happy-path-only regression ở T4 và T5.** 14 TC regression đều precondition "data sạch", 2–3 bước, không có edge state. Thiếu hẳn: ChatWork room bị xoá / token hết hạn / API trả 429 · notify quá 24h · bot hết hạn → đẩy lịch 15 phút (chính Dev nêu ở T5) · schedule khi `next_notify_time` ở quá khứ.
- **`[MAJOR]` M8 — Mục tiêu chính của ticket (giảm tải query) không có TC nào đo.** Mục 1 nêu *"query nặng"* là lý do làm ticket. Không có phép đo số query / thời gian vòng trước–sau. → `TC-PERFLARGE001-01` (§5).
- **`[MAJOR]` M9 — DATA-COUNT-001 / BR-09: counter + badge không được đo.** Dev yêu cầu ở T4 *"counter `*_total_msg_error` giữ nguyên như trước"* và *"badge"*. `NEW-19…25` chỉ ghi "badge/pending/counter hoạt động như trước" ở Expected — **không đo giá trị cụ thể**, không có phép tính tay. Studio tự chấm **BR-09 = `none`** kèm note *"badge = bots.count_app_notify — lệch code job đã chuyển mobile_badge_noitfy theo bot+user (#38562)"* → đã biết có lệch mà vẫn không có TC.
- **`[MAJOR]` M10 — DATA-BACKUP-001 chưa được xử lý.** Trigger "thêm/đổi bảng DB" đã khớp (3 cột mới). Đánh giá của tôi: `job_config_daily` là **config hệ thống 1 bản ghi `id=1`**, gần như chắc chắn **không** nằm trong scope copy bot / backup theo bot → hợp lý để đánh **×**. Nhưng theo **RULE-03**, quan điểm **Cao** đánh × phải có **Leader approve + lý do ghi lại**. → Leader xác nhận × kèm lý do, không bỏ trống.
- **`[MAJOR]` M11 — Checkbox "Tester verify auto-fill chính xác" ở cả `01-bug-task.md` và `03-dev-impact.md` CHƯA tick.** Cả 2 file đều `Auto-filled: 2026-08-28 by /new-task`. F/D/T có thể thiếu hoặc map sai (đặc biệt: Dev không ghi mức risk cho T1–T6, và F3 trùng F1). → Tester đọc lại Redmine `#40272` + journal `#133203` rồi tick.
- **`[MAJOR]` M12 — Phạm vi review chưa được chốt: bộ 28 TC này là phần BỔ SUNG.** Note của `NEW-1`/`NEW-2` dẫn *"bộ 32 TC"*, `existing-tc:NEW-16`, `NEW-29`, `NEW-31`. Studio có task **`#155`** (ticket cha `#39543`, **40 TC**, `released`). **`temp_id` trùng nhau giữa 2 bộ** — cả 2 đều có `NEW-1`, `NEW-16`… → rất dễ dẫn nhầm TC khi trao đổi. Leader chốt: review `#260` độc lập, hay `#155 + #260` là 1 bộ? *(Tôi KHÔNG gộp 2 nguồn theo đúng quy tắc BƯỚC 0.)*
- **`[MAJOR]` M13 — NEW-12…18: Expected khẳng định "cả 3 nền tảng App, PC và ChatWork" nhưng Steps chỉ có "Kiểm tra ChatWork room".** 2 nơi không khớp → người chạy sau sẽ ra kết quả khác. Cộng thêm B6 (không evidence) thì không thể biết thực tế đã kiểm mấy nền tảng. → Sửa Steps cho khớp Expected, hoặc thu Expected về đúng ChatWork.
- **`[MAJOR]` M14 — Toàn bộ 28 TC có `spec_status = null`.** Không TC nào ghi `Spec ghi rõ` / `Spec không ghi` / `Đã hỏi leader`. Với 3 TC đang treo câu hỏi cho Dev/PO (`NEW-2`, `NEW-5`, `NEW-7`) thì đây đúng là chỗ phải ghi `Spec không ghi (đã hỏi <ai>)`.

### 4.3 Minor (có thể fix sau)

- **`[MINOR]` m1 — Số hiệu BR trong `spec_ids` không khớp `feature-spec.md`.** `NEW-1`/`NEW-4` ghi `spec:feature-spec BR-01` và mô tả *"mỗi bot tối đa 1 bản ghi cài đặt"* — nội dung đó là **BR-11** (BR-01 = "Toggle đảo ngược"). `NEW-7` ghi `BR-06` cho `next_notify_time` (thực tế **BR-05**) và `BR-08` cho mốc đếm lỗi (thực tế **BR-06**). Lệch đều 1 bậc → có thể spec đã đánh số lại sau khi viết TC. → Rà lại `spec_ids`, nếu không Leader không trace được TC ↔ spec.
- **`[MINOR]` m2 — `NEW-20…25`: Steps chỉ 2 dòng ("Chạy job schedule / Kiểm tra notify"), `Dữ liệu nhập` dạng "Schedule 3 giờ + message_error".** Không đủ để người khác dựng lại env (không nêu kênh nào, mốc counter bao nhiêu, bao nhiêu lỗi chưa xác nhận). So với `NEW-19` (3 bước) đã sơ sài, `NEW-20…25` còn kém hơn.
- **`[MINOR]` m3 — `NEW-12…18`: `Dữ liệu nhập` dạng "Event add friend" / "Event ASP"** — không phải dữ liệu test cụ thể.
- **`[MINOR]` m4 — `TC No.` dùng `temp_id` Studio (`NEW-1`…`NEW-28`)** thay vì format repo `TC-<mã quan điểm bỏ gạch>-<nn>`. Chấp nhận được vì đây là convention của Studio, nhưng khi đồng bộ về file 04/Sheet cần map lại (snapshot `04-tc-list.md` đã tự sinh `TC No.` chuẩn và giữ `Studio #<id> (<temp_id>)` ở `Ghi chú`).

### 4.4 Nit (gợi ý)

- **`[NIT]` n1** — `NEW-19` có ghi chú manual đáng giá: *"khi chọn 15p nội dung chatwork sẽ vẫn hiển thị nội dung riêng lẻ và app và pc thì hiển thị msg chung"*. Quan sát này nên được đưa lên thành TC riêng (khác biệt format nội dung giữa ChatWork và app/PC), đừng để chìm trong trường `actual`.
- **`[NIT]` n2** — `NEW-6` evidence tự ghi giới hạn rất trung thực: *"không thể chứng minh trực tiếp có bản ghi rơi đúng vào khe giữa 2 truy vấn … cái kiểm được là bất biến hệ quả"*. Cách viết này nên nhân rộng cho các TC khác.
- **`[NIT]` n3** — `NEW-5` evidence ghi rõ vì sao lệch mô tả TC: *"độ trễ dùng 25s thay vì 30s … readTimeout của OkHttp trong job đúng bằng 30s"*. Nên cập nhật lại `Dữ liệu nhập` của TC thành 25s để lần chạy sau không lặp lại việc phải giải thích.

---

## 4.5 TC trùng lặp nội dung

**Đã rà toàn bộ 28 TC theo 4 yếu tố** (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương` → `expected` tương đương). Phát hiện **2 nhóm**:

| Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| **A — Regression schedule 配信エラー** | `NEW-19` (15p, có 3 bước + có ghi chú thực tế) | **Gộp** `NEW-20`…`NEW-25` (30p/1h/3h/6h/12h/24h) vào 1 TC tham số hoá | `DUP-SUBSET` + `DUP-INFLATE` | Cùng `RULE-09` · cùng `Normal` · cùng đối tượng+thao tác ("bot A setting 配信エラー schedule → chạy job → kiểm tra notify") · tiền đề chỉ khác **giá trị chu kỳ** · Expected gần như y hệt ("notify tạo đúng, push đúng channel, badge/pending/counter giữ nguyên") | **`[MAJOR]`** — 7 TC làm T4 **trông như** phủ dày, thực tế 0 TC đo counter/badge và 0 TC nêu rõ kênh nào |
| **B — Regression ChatWork action khác** | `NEW-12` (chat 1-1) | **Gộp** `NEW-13`…`NEW-18` (add friend / QR / メッセージ配信 / schedule / ASP / conversion) vào 1 TC có bảng `Dữ liệu nhập` 7 loại event | `DUP-SUBSET` | Cùng `TOOL-NEGCTRL-001` · cùng `Normal` · cùng thao tác ("trigger event → chờ job → kiểm tra ChatWork room") · Expected **copy-paste nguyên văn**, chỉ thay tên action | **`[MINOR]`** — 7 loại event **do Dev chỉ định ở T5** nên vẫn là coverage hợp lệ; vấn đề là hình thức, không phải thừa |

**Gate trước khi đề nghị xóa** — đã chạy lại BƯỚC 2 + 3.6 trên tập giả định:

- Nhóm A: xoá hẳn `NEW-20…25` sẽ làm **T4 mất chiều "đủ 7 chu kỳ"** → **đổi từ "xoá" sang "GỘP"**: giữ 1 TC với `Dữ liệu nhập` liệt kê 7 chu kỳ + **thêm 2 TC biên** (15 phút = biên nhỏ nhất, 24 giờ = biên lớn nhất) và **tách theo 3 kênh** như Dev yêu cầu ở T4.
- Nhóm B: xoá hẳn sẽ làm **T5 mất 6/7 loại event Dev liệt kê** → **GỘP**, không xoá.
- **Không TC nào bị đề nghị xoá hẳn.** Không có `DUP-EXACT`, không có `DUP-CONFLICT` giữa các TC trong bộ.

**Không phải trùng** (đã kiểm, ghi lại để khỏi rà lại): `NEW-1` vs `NEW-4` (khác kênh app/PC) · `NEW-6` vs `NEW-9` (khác ý định: race chèn xen giữa 2 query vs độc lập cột mốc) · `NEW-26`/`27`/`28` (khác nguồn phát sinh lỗi: web / job / app — là bộ tham số hợp lệ của `OUT-TRUTH-001`).

⚠️ **KHÔNG tự xóa/sửa TC.** TC Studio là read-only — human thực hiện bằng `testcase_update` / `testcase_delete` trên Studio.

---

## 5. TCs đề xuất bổ sung

> **Đã đối chiếu 28 TC ở BƯỚC 0 + kho-tcs — không TC đề xuất nào trùng.**
> ⚠️ `kho-tcs/` **chưa có** file cho tính năng `notify-setting` (18 file hiện có không chứa mã màn hình này) → **không đối chiếu được TC cũ ở kho**; không phát hiện được conflict expected với kho. Khi `/collect-tcs` gom xong tính năng này thì nên rà lại.
> 14 cột = 12 cột format kho + `Chạy` + `Phạm vi ENV`.

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-DATAMIG001-01 | Data | DATA-MIG-001 | Deploy lần đầu — mốc lỗi gửi tin | Normal | manual | product | Deploy lần đầu với 3 cột mốc = rỗng và bảng lỗi gửi tin còn nguyên dữ liệu cũ thì KHÔNG bắn notify hàng loạt | - Đã chạy `ALTER TABLE` thêm 3 cột, cả 3 đang là **rỗng (NULL)** — đúng trạng thái sau migration<br>- Bảng lỗi gửi tin **giữ nguyên toàn bộ dữ liệu cũ** (≥ 100.000 bản ghi, thuộc ≥ 50 bot khác nhau)<br>- ≥ 3 bot đang bật chu kỳ リアルタイム cho cả 3 kênh app / PC / ChatWork, đã bật hạng mục 配信エラー<br>- Chưa khởi động 3 job notify | 1. Ghi lại số thông báo hiện có của từng bot trong 3 bot mẫu và tổng số bản ghi trong bảng lỗi gửi tin<br>2. Ghi lại số thứ tự lớn nhất hiện có của bảng lỗi gửi tin<br>3. Khởi động cả 3 job notify (app / PC / ChatWork), chờ đúng **1 vòng** (tối đa 60 giây)<br>4. Đếm số thông báo mới sinh ra của **toàn bộ hệ thống**, không chỉ 3 bot mẫu<br>5. Đọc lại giá trị 3 cột mốc<br>6. Chờ thêm 2 vòng nữa mà không tạo lỗi mới, đếm lại thông báo | - 3 cột mốc = rỗng<br>- Bảng lỗi gửi tin có sẵn ≥ 100.000 bản ghi cũ, số thứ tự lớn nhất = `MAX_CŨ`<br>- Phép tính tay: số thông báo mới **phải = 0**, không phải 50 bot × 1 | - Vòng đầu: **KHÔNG có thông báo lỗi gửi tin nào được sinh ra trên toàn hệ thống** (0, không phải 50)<br>- Cả 3 cột mốc được ghi đúng bằng `MAX_CŨ`<br>- Điện thoại thật của người nhận (app) **không** nhận push, trình duyệt **không** hiện web push, room ChatWork **không** nhận message<br>- 2 vòng sau: vẫn 0 thông báo mới, 3 cột mốc giữ nguyên `MAX_CŨ` | | Lấp **GAP-1 / B3** · cover impact **T6, D1, D2, F5** · Đánh giá spec: Spec không ghi (cần hỏi Dev Văn Dũng Đinh về nhánh mốc `NULL`) · Evidence: ảnh chụp 3 cột mốc trước/sau + log job 3 vòng + đếm bản ghi thông báo + ảnh thiết bị thật (RULE-06) · **RULE-08 → bắt buộc `product`** |
| TC-DATAMIG001-02 | Data | DATA-MIG-001 | Deploy lần đầu — mốc lỗi gửi tin | Boundary | manual | product | Mốc = rỗng nhưng bảng lỗi gửi tin cũng rỗng thì job không ghi mốc và không lỗi | - 3 cột mốc = rỗng<br>- Bảng lỗi gửi tin **rỗng hoàn toàn**<br>- ≥ 1 bot bật リアルタイム mỗi kênh | 1. Xác nhận bảng lỗi gửi tin không có bản ghi nào và 3 cột mốc đang rỗng<br>2. Chạy cả 3 job 3 vòng liên tiếp<br>3. Đọc log 3 job<br>4. Đọc lại 3 cột mốc và đếm thông báo mới | - Bảng lỗi gửi tin: 0 bản ghi<br>- 3 cột mốc: rỗng | - Cả 3 job chạy hết vòng, **không** ném ngoại lệ, **không** rơi vào trạng thái ngủ 30 giây<br>- 3 cột mốc **vẫn rỗng** — không bị ghi `0`, không bị ghi chuỗi rỗng<br>- 0 thông báo mới | | Lấp **GAP-1 / B3** · cover **D1, F5** · mở rộng `NEW-3` sang **kênh ChatWork** và sang trạng thái mốc **rỗng** (`NEW-3` chạy với mốc = 1000) · Đánh giá spec: Spec không ghi · Evidence: log 3 job + ảnh 3 cột mốc |
| TC-DATAMIG001-03 | Data | DATA-MIG-001 | Deploy lần đầu — mốc lỗi gửi tin | Abnormal | manual | product | Chạy lại migration lần 2 và rollback code không làm hỏng mốc đã có | - Đã deploy xong, 3 cột mốc đang có giá trị > 0<br>- Có bản `ALTER TABLE` của mục 4.2 để chạy lại<br>- Có bản code cũ (trước `06cd8fb6`) để rollback | 1. Ghi lại giá trị 3 cột mốc và số thứ tự lớn nhất bảng lỗi gửi tin<br>2. Chạy lại đúng câu `ALTER TABLE` ở mục 4.2 lần thứ 2, ghi nhận thông báo trả về<br>3. Đọc lại 3 cột mốc<br>4. Rollback code về bản cũ, để 3 job chạy 2 vòng<br>5. Deploy lại bản mới, để 3 job chạy 2 vòng, đọc lại 3 cột mốc và đếm thông báo | - Câu `ALTER TABLE` nguyên văn ở mục 4.2 của `03-dev-impact.md` | - Lần chạy `ALTER TABLE` thứ 2: báo lỗi cột đã tồn tại (hoặc bị bỏ qua nếu dùng `IF NOT EXISTS`) — **giá trị 3 cột mốc KHÔNG bị reset về rỗng**<br>- Trong lúc chạy code cũ: 3 cột mốc **không bị ghi đè** bởi bất kỳ job nào<br>- Sau khi deploy lại bản mới: mốc tiếp tục từ giá trị cũ, **không** bắn lại notify cho các lỗi đã xử lý | | Lấp **GAP-1 / B3** (nhánh idempotent + rollback) · cover **D1** · Đánh giá spec: Spec không ghi (cần hỏi Dev về thứ tự migration vs deploy) · Evidence: log SQL + ảnh 3 cột mốc ở 5 mốc thời gian · **RULE-08 → `product`** |
| TC-REGSHARED001-01 | UI | REG-SHARED-001 | Giám sát đặt lịch salon (`MonitorCalendarBookingTask`) | Normal | manual | staging | Job giám sát đặt lịch salon vẫn ghi đúng mốc riêng và KHÔNG xoá 3 cột mốc lỗi gửi tin | - Cả 4 job nền đang chạy (3 job notify + job giám sát đặt lịch salon)<br>- 3 cột mốc lỗi gửi tin đang có giá trị > 0, ghi lại giá trị chính xác<br>- Có ≥ 1 lịch salon mới để job giám sát xử lý | 1. Ghi lại giá trị của **cả 4 cột mốc** trên bản ghi cấu hình job (3 cột mốc lỗi gửi tin + mốc đặt lịch)<br>2. Tạo 1 lịch đặt salon mới để job giám sát có việc<br>3. Chờ job giám sát đặt lịch chạy hết 1 vòng<br>4. Đọc lại **cả 4 cột mốc**<br>5. Tạo 1 bản ghi lỗi gửi tin mới, chờ 3 job notify chạy 1 vòng<br>6. Đọc lại cả 4 cột mốc lần nữa | - 1 lịch salon mới<br>- 1 bản ghi lỗi gửi tin mới<br>- Phép tính tay: sau bước 4, **3 cột mốc lỗi gửi tin phải giữ nguyên giá trị bước 1** | - Sau bước 4: mốc đặt lịch tiến đúng theo lịch mới; **3 cột mốc lỗi gửi tin giữ NGUYÊN giá trị ghi ở bước 1** (không bị `save()` full-column ghi đè về giá trị cũ/rỗng)<br>- Sau bước 6: 3 cột mốc lỗi gửi tin tiến đúng, **mốc đặt lịch giữ nguyên**<br>- Màn quản lý đặt lịch salon hiển thị lịch mới đúng như trước khi sửa; thông báo đặt lịch vẫn tới người nhận | | Lấp **GAP-2 / B4** · cover impact **F4 (`updateBookingLastId`), D3** · regression · Đánh giá spec: Spec không ghi (Dev nêu ở mục 3) · Evidence: ảnh bản ghi cấu hình job ở 3 thời điểm + log 4 job + ảnh màn đặt lịch |
| TC-REGSHARED001-02 | UI | REG-SHARED-001 | Giám sát đặt lịch salon (`MonitorCalendarBookingTask`) | Abnormal | manual | staging | Bốn job ghi đồng thời vào cùng bản ghi cấu hình thì không job nào mất mốc của mình | - Cả 4 job đang chạy, chu kỳ trùng nhau (cùng ~60 giây)<br>- 4 cột mốc có 4 giá trị **khác nhau**, ghi lại đầy đủ<br>- Có công cụ tạo đồng thời: lịch salon mới + lỗi gửi tin mới | 1. Ghi lại 4 giá trị mốc ban đầu<br>2. Tạo **đồng thời** 1 lịch salon mới và 3 bản ghi lỗi gửi tin mới (trong cùng 1 giây)<br>3. Để 4 job cùng chạy qua ít nhất 3 vòng<br>4. Sau mỗi vòng, đọc và ghi lại cả 4 cột mốc<br>5. Đối chiếu: có cột nào bị lùi về giá trị cũ hoặc về rỗng không | - 1 lịch salon + 3 bản ghi lỗi gửi tin tạo đồng thời<br>- Phép tính tay: 4 cột mốc phải **đơn điệu tăng**, không cột nào lùi | - Qua cả 3 vòng, **cả 4 cột mốc chỉ tăng, không cột nào lùi về giá trị cũ hoặc rỗng**<br>- Không có notify lỗi gửi tin bị bắn lặp (mỗi vòng có lỗi mới → đúng 1 thông báo/bot/kênh)<br>- Lịch salon không bị xử lý lại nhiều lần | | Lấp **GAP-2 / B4** + phần thiếu của `CONC-001` · cover **D3, F4** · Đánh giá spec: Spec không ghi · Evidence: bảng 4 cột mốc × 3 vòng + log 4 job |
| TC-DATADB001-01 | Data | DATA-DB-001 | Notify ChatWork — bỏ `markChatworkDone` cho notify quá 24h | Abnormal | manual | staging | Notify ChatWork quá 24h bị bỏ qua nhưng KHÔNG đánh dấu nhầm notify mới chưa gửi của cùng bot và người nhận | - Bot A có liên kết ChatWork room hợp lệ, đã bật 配信エラー<br>- Trong bảng thông báo, cùng cặp (bot A, người nhận U1) có: **1 notify tạo cách đây > 24 giờ** chưa gửi ChatWork và **1 notify vừa tạo trong vòng 5 phút** chưa gửi ChatWork<br>- Ghi lại id của cả 2 notify | 1. Ghi lại trạng thái kênh ChatWork của cả 2 notify (cũ > 24h và mới)<br>2. Chờ job notify ChatWork chạy hết 1 vòng<br>3. Đọc lại trạng thái kênh ChatWork của **cả 2** notify<br>4. Kiểm tra room ChatWork nhận được message nào<br>5. Chờ thêm 2 vòng, đọc lại trạng thái notify cũ | - notify cũ: tạo lúc `now - 25h`<br>- notify mới: tạo lúc `now - 5 phút`<br>- Cả 2 cùng `bot_id = A`, cùng `user_id = U1` | - Notify **mới** được gửi lên room ChatWork **đúng 1 lần** và trạng thái chuyển sang đã gửi<br>- Notify **cũ > 24h** KHÔNG được gửi lên room<br>- **Notify mới KHÔNG bị đánh dấu nhầm** trạng thái đã xử lý trước khi được gửi<br>- Sau 2 vòng nữa: notify cũ vẫn ở trạng thái chưa gửi và **vẫn bị quét lại mỗi vòng** → ghi rõ số lần bị quét lại làm bằng chứng cho câu hỏi ở §6 | | Lấp **GAP-3 / B5** · cover impact **D5, F2** · Đánh giá spec: **Spec không ghi — mâu thuẫn BR-14**, đã đưa vào §6 · Evidence: ảnh 2 bản ghi notify trước/sau + log job + ảnh room ChatWork thật (RULE-06) |
| TC-DATADB001-02 | Data | DATA-DB-001 | Notify ChatWork — 429 từ ChatWork | Abnormal | manual | staging | ChatWork trả 429 thì notify không bị mất và được gửi lại ở vòng sau | - Bot A liên kết room ChatWork hợp lệ<br>- Có cách làm ChatWork trả mã 429 (giới hạn tần suất) cho các yêu cầu gửi<br>- Có ≥ 2 notify ChatWork đang chờ gửi của bot A, tạo trong vòng 24 giờ | 1. Ghi lại id và trạng thái kênh ChatWork của các notify đang chờ<br>2. Bật điều kiện làm ChatWork trả 429<br>3. Chờ job notify ChatWork chạy 1 vòng, đọc log<br>4. Đọc lại trạng thái kênh ChatWork của các notify<br>5. Tắt điều kiện 429, chờ job chạy thêm 2 vòng<br>6. Kiểm tra room ChatWork và trạng thái các notify | - ≥ 2 notify chờ gửi, tạo trong 24 giờ<br>- ChatWork trả 429 cho mọi yêu cầu ở bước 2–3 | - Khi bị 429: job **không dừng**, chỉ ghi log; các notify **giữ nguyên trạng thái chưa gửi** (không bị đánh dấu đã xử lý)<br>- Sau khi hết 429: **toàn bộ** notify chờ được gửi lên room ChatWork, mỗi notify đúng 1 lần, **không mất notify nào**<br>- Room ChatWork thật nhận đủ số message đúng bằng số notify chờ | | Lấp **GAP-3 / B5** · cover **D5, F2** · chính là kịch bản Dev nêu *"chatwork trả 429 là mất notify"* · Đánh giá spec: Spec không ghi · Evidence: log job + ảnh room ChatWork + bảng trạng thái notify trước/sau |
| TC-JOB001-01 | API | JOB-001 | Job notify lỗi gửi tin — khởi động lại giữa vòng | Abnormal | manual | product | Job bị dừng sau khi đọc số thứ tự lớn nhất nhưng trước khi ghi mốc thì không mất và không bắn lặp | - Bot A bật リアルタイム kênh app, đã bật 配信エラー<br>- Mốc kênh app > 0, ghi lại giá trị<br>- Có cách dừng tiến trình job giữa vòng (kill) và khởi động lại | 1. Ghi lại mốc kênh app và số thông báo hiện có của bot A<br>2. Tạo 3 bản ghi lỗi gửi tin mới cho bot A<br>3. Dừng job **ngay khi log cho thấy đã đọc xong số thứ tự lớn nhất nhưng chưa ghi mốc**<br>4. Đọc mốc kênh app (kỳ vọng chưa đổi)<br>5. Khởi động lại job, chờ 2 vòng<br>6. Đếm tổng số thông báo mới của bot A và đọc mốc | - 3 bản ghi lỗi gửi tin mới<br>- Phép tính tay: bot A nhận **đúng 1** thông báo cho cả 3 lỗi (không phải 3, không phải 0) | - Sau bước 4: mốc **chưa** tiến (giao dịch chưa hoàn tất thì không ghi mốc)<br>- Sau bước 6: bot A có **đúng 1** thông báo mới cho cả 3 lỗi — **không mất** và **không bắn lặp**<br>- Mốc tiến tới số thứ tự lớn nhất trong 3 bản ghi<br>- Thiết bị thật nhận đúng 1 push | | Lấp **GAP-4 / B7** · cover **F1, F5, D2** · quan điểm `JOB-001` (BẮT BUỘC, hiện 0 TC) + `REG-RUN-001` · Đánh giá spec: Spec không ghi · Evidence: log job có mốc thời gian kill + bảng đếm thông báo + ảnh push thiết bị thật · **RULE-08 → `product`** |
| TC-JOB001-02 | API | JOB-001 | Job notify lỗi gửi tin — hai tiến trình chạy song song | Abnormal | manual | product | Hai instance của cùng job chạy song song không bắn notify trùng | - Bot A bật リアルタイム kênh app<br>- Có thể khởi động **2 tiến trình** cùng job notify app cùng lúc<br>- Mốc kênh app > 0 | 1. Ghi lại mốc kênh app và số thông báo của bot A<br>2. Khởi động 2 tiến trình job notify app cùng lúc<br>3. Tạo 2 bản ghi lỗi gửi tin mới cho bot A<br>4. Chờ 2 vòng<br>5. Đếm số thông báo mới của bot A và số message thực nhận trên thiết bị<br>6. Đọc mốc kênh app | - 2 tiến trình job · 2 bản ghi lỗi mới<br>- Phép tính tay: bot A nhận **đúng 1** thông báo, không phải 2 | - Bot A có **đúng 1** thông báo mới; thiết bị thật nhận **đúng 1** push (không nhân đôi theo số tiến trình)<br>- Mốc kênh app tiến đúng 1 lần tới số thứ tự lớn nhất, không bị lùi<br>- Log không có ngoại lệ khoá/deadlock làm job dừng | | Lấp **GAP-4 / B7** + phần thiếu `CONC-001` (Abnormal) · cover **F1, D2** · Đánh giá spec: Spec không ghi (hỏi Dev job có cơ chế khoá 1 instance không) · Evidence: log 2 tiến trình + ảnh thiết bị · **RULE-08 → `product`** |
| TC-JOB001-03 | API | JOB-001 | Job notify ChatWork — deploy khi còn notify chờ gửi | Normal | manual | product | Deploy bản mới khi ChatWork còn notify chờ gửi thì các notify đó vẫn được gửi hết | - Bot A liên kết room ChatWork, có ≥ 5 notify ChatWork **đang chờ gửi**, tạo trong 24 giờ<br>- Đang chạy bản code cũ<br>- Có bản mới (`be829f69`) để deploy | 1. Ghi lại id và trạng thái của 5 notify đang chờ<br>2. Deploy bản mới **trong lúc** job ChatWork đang ở giữa vòng chạy<br>3. Chờ job khởi động lại và chạy 3 vòng<br>4. Kiểm tra room ChatWork và trạng thái 5 notify | - 5 notify chờ gửi tạo trong 24 giờ<br>- Phép tính tay: room nhận **đúng 5** message | - **Cả 5** notify được gửi lên room ChatWork, mỗi cái đúng 1 lần — không mất, không lặp<br>- Không notify nào bị đánh dấu đã xử lý mà chưa thực sự gửi<br>- Job không dừng vì ngoại lệ khi gặp dữ liệu tạo bởi bản cũ | | Lấp **GAP-4 / B7** · cover **F2, D4, T3** · quan điểm `REG-RUN-001` + `DEPLOY-LIVE-001` · Đánh giá spec: Spec không ghi · Evidence: ảnh room ChatWork + bảng trạng thái 5 notify + log deploy · **RULE-08 → `product`** |
| TC-ENV003-01 | API | ENV-003 | Smoke sau deploy — 3 kênh notify 配信エラー | Normal | manual | product | Smoke production sau deploy: 3 kênh realtime bắn đúng 1 notify tới output cuối trên thiết bị thật | - Đã deploy bản mới lên production và đã chạy `ALTER TABLE`<br>- 1 bot test trên production bật リアルタイム cho cả 3 kênh, đã bật 配信エラー, đã liên kết room ChatWork<br>- Có điện thoại thật đã đăng nhập app + trình duyệt đã bật web push | 1. Ghi lại 3 cột mốc và số thông báo hiện có của bot test<br>2. Tạo **đúng 1** lỗi gửi tin cho bot test (gửi 1 tin tới friend đã block)<br>3. Chờ tối đa 60 giây<br>4. Kiểm tra: thông báo trên **điện thoại thật**, web push trên **trình duyệt thật**, message trong **room ChatWork thật**<br>5. Đọc lại 3 cột mốc<br>6. Chờ thêm 3 vòng không tạo lỗi mới, kiểm tra có bị bắn lặp không | - 1 lỗi gửi tin duy nhất<br>- Phép tính tay: mỗi kênh nhận **đúng 1**, tổng 3 output, không phải 3 lần/kênh | - Điện thoại thật hiện **đúng 1** thông báo, nội dung `[配信エラーが発生しました] PCでエルメにログインして「配信エラー」ページをご確認ください。`<br>- Trình duyệt thật hiện **đúng 1** web push với tiền tố `【tên bot】`<br>- Room ChatWork thật nhận **đúng 1** message<br>- Cả 3 cột mốc tiến tới số thứ tự bản ghi lỗi vừa tạo<br>- 3 vòng sau: **không** có thêm output nào ở cả 3 kênh | | Lấp **GAP-5 / M1** · cover **T1, T2, T3, D2** · **RULE-08** (job nền + migration → bắt buộc production) + **RULE-06** (đi tới output cuối trên thiết bị thật) · Đánh giá spec: Spec ghi rõ (BR-13, BR-14) · Evidence: ảnh màn hình điện thoại + ảnh web push + ảnh room ChatWork + ảnh 3 cột mốc · ⚠️ dùng bot test, **tránh tạo/xoá data thật** |
| TC-PERFLARGE001-01 | API | PERF-LARGE-001 | Tải query của 3 job notify realtime | Normal | manual | product | Số truy vấn mỗi vòng của nhánh realtime giảm so với bản cũ ở quy mô khách hàng thật | - Môi trường có quy mô gần production: ≥ 500 bản ghi cài đặt đang bật リアルタイム trên cả 3 kênh<br>- Bật ghi log truy vấn chậm / đếm truy vấn cho 3 job<br>- Có sẵn số đo của **bản cũ** (trước `06cd8fb6`) làm mốc so sánh | 1. Với **bản cũ**: chạy 3 job 5 vòng, ghi lại tổng số truy vấn đếm lỗi gửi tin và thời gian trung bình mỗi vòng<br>2. Deploy **bản mới**, chạy 3 job 5 vòng trong cùng điều kiện dữ liệu<br>3. Ghi lại tổng số truy vấn và thời gian trung bình mỗi vòng<br>4. Lặp lại bước 2–3 trong tình huống **không có lỗi mới nào** suốt 5 vòng<br>5. Đối chiếu 2 bộ số đo | - ≥ 500 bản ghi cài đặt リアルタイム<br>- Phép tính tay bản cũ: ~500 truy vấn đếm/vòng/job × 3 job = ~1500/vòng<br>- Bản mới kỳ vọng: 2 truy vấn/vòng/job (`MAX(id)` + `DISTINCT bot_id`) = 6/vòng | - Bản mới: mỗi vòng mỗi job chỉ phát sinh **2 truy vấn** lên bảng lỗi gửi tin, **không** phụ thuộc số bản ghi cài đặt<br>- Khi không có lỗi mới: **0** truy vấn đếm lỗi theo từng cài đặt<br>- Thời gian trung bình mỗi vòng của bản mới **nhỏ hơn** bản cũ, ghi lại con số cụ thể cả 2 bên<br>- Không vòng nào vượt 60 giây | | Lấp **GAP-6 / M8** — đây là **mục tiêu chính** của ticket mà chưa TC nào đo · cover impact **BUG, F1, F2, F5** · Đánh giá spec: Spec không ghi (mục 1 dev-impact) · Evidence: log đếm truy vấn 2 bản + bảng so sánh thời gian vòng · **RULE-08 → `product`** |
| TC-DATACOUNT001-01 | Data | DATA-COUNT-001 | Notify 配信エラー theo lịch — counter và badge | Normal | manual | staging | Nhánh theo lịch giữ nguyên counter lỗi và badge sau khi nhánh realtime thôi cập nhật | - Bot A bật kênh app chu kỳ **15分**, đã bật 配信エラー<br>- Ghi lại `app_total_msg_error` hiện tại và badge trên màn hình<br>- Có **5** lỗi gửi tin chưa xác nhận của bot A | 1. Ghi lại: counter lỗi của kênh app, badge hiển thị trên màn hình quản trị, số lỗi chưa xác nhận (đếm tay)<br>2. Tạo thêm **3** lỗi gửi tin mới cho bot A<br>3. Chờ đủ 1 chu kỳ 15 phút cho nhánh theo lịch chạy<br>4. Đọc lại counter, badge, số thông báo mới, và số lỗi chưa xác nhận<br>5. Lặp lại bước 2–4 cho kênh **PC** và kênh **ChatWork** | - Trước: 5 lỗi chưa xác nhận, counter = 5<br>- Thêm 3 lỗi → tổng 8<br>- **Phép tính tay**: counter sau = **8**, badge tăng đúng **1** (1 thông báo hệ thống mới), không phải 3 | - Counter lỗi của kênh app sau khi nhánh theo lịch chạy = **8** (đúng số lỗi chưa xác nhận đếm tay)<br>- Badge trên màn hình quản trị tăng đúng **1**, khớp với số bản ghi thông báo mới đếm được<br>- Bot A nhận **đúng 1** thông báo cho cả 3 lỗi mới<br>- Kênh PC và ChatWork cho kết quả tương đương với counter riêng của từng kênh | | Lấp **GAP-7 / M9** · cover impact **T4** · thay phần "counter/badge" mà `NEW-19…25` chỉ nêu chung chung · Đánh giá spec: **Spec ghi rõ BR-06, BR-07** — nhưng **BR-09 đang là điểm lệch đã biết** (`mobile_badge_noitfy` theo bot+user, #38562), ghi rõ kết quả quan sát để §6 chốt · Evidence: ảnh badge trước/sau + ảnh counter + phép tính tay |

**Ghi chú về RULE-01 cho các quan điểm Cao đề xuất mới**: `DATA-MIG-001` đã đủ 3 loại case (`-01` Normal, `-02` Boundary, `-03` Abnormal). `JOB-001` có Normal (`-03`) + 2 Abnormal (`-01`, `-02`), **thiếu Boundary** — lý do: mốc số thứ tự không có khái niệm biên riêng ngoài trường hợp rỗng đã được `TC-DATAMIG001-02` phủ. `REG-SHARED-001` có Normal + Abnormal, thiếu Boundary vì bản ghi cấu hình job chỉ có đúng 1 dòng (`id = 1`), không có biên số lượng.

---

## 6. Spec update needed

| # | Vấn đề | Spec hiện tại | Hành vi code quan sát được (có evidence) | Cần ai chốt |
|---|---|---|---|---|
| S1 | **Số bản ghi `notify_setting` trên mỗi bot** | **BR-11**: *"Mỗi bot chỉ có 1 record trong `notify_setting`. Query luôn dùng `WHERE bot_id = ? LIMIT 1`"* | Evidence `15113`/`15116`: *"môi trường DỰNG ĐƯỢC nhiều notify_setting cho cùng 1 bot (4 bản ghi cho bot 990430113) và job xử lý từng bản ghi"*. Job duyệt theo cặp **(bot, người dùng)**, mỗi bản ghi sinh 1 thông báo riêng. | **Leader + Dev** — spec sai hay code sai? Nếu code đúng thì BR-11 phải viết lại, và `NEW-1`/`NEW-4` mới được coi là `pass` hợp lệ. |
| S2 | **Notify ChatWork quá 24h** | **BR-14**: *"Thông báo ChatWork quá 24h kể từ thời điểm tạo sẽ bị auto-expire (mark `status_chat_work=1`), không gửi"* | Mục 2 `03-dev-impact.md`: *"notify quá 24h chỉ bỏ qua chứ không markChatworkDone giữa vòng lặp nữa"*. → Notify quá 24h **không còn được đánh dấu**, sẽ tồn đọng và bị quét lại mỗi vòng vô thời hạn. | **Dev + PO** — BR-14 phải cập nhật theo hành vi mới, và phải định nghĩa cơ chế dọn notify quá hạn (nếu không sẽ tích tụ). Chưa có TC nào (→ B5). |
| S3 | **Đổi chu kỳ từ リアルタイム sang theo lịch** | **BR-05** (`next_notify_time = last_notify_time + interval`) + **BR-06** (chụp mốc đếm lỗi khi đổi phương tiện) | Evidence `15119` (cả 2 run): nhánh realtime **không còn** cập nhật `last_notify_time` và mốc đếm lỗi → `next_notify_time` rơi vào quá khứ (`2026-08-25 22:10:48`), người dùng nhận notify **~8 giây** sau khi đổi chu kỳ thay vì chờ 1 giờ. Evidence kết luận: *"ĐÂY LÀ HỒI QUY do thay đổi lần này"*. | **PO + Dev** — chấp nhận hành vi mới (thì sửa BR-05/BR-06) hay coi là bug phải fix. Đây là **B1**, hiện đang bị chấm `pass` và chưa raise ticket. |
| S4 | **Bot đã xoá / hết hạn >7 ngày ở nhánh realtime** | Spec không mô tả | Evidence `15114`: bản ghi cài đặt của bot đã xoá (J1) và bot hết hạn (K1) **vẫn xuất hiện trong danh sách xử lý mỗi 60 giây** và `next_notify_time` **bị ghi lại ở MỖI vòng**, vì câu lọc nhánh realtime không xét `next_notify_time`. | **Dev** — mâu thuẫn với chính mục tiêu "giảm tải query" của ticket. Cần chốt: chấp nhận, hay bổ sung điều kiện lọc. |
| S5 | **Badge count** | **BR-07**: badge = `getTotalNotifyExceptSystemAndBillCycle(botId)` → `bots.count_app_notify` | Studio tự chấm coverage **BR-09 = `none`** kèm note: *"badge = bots.count_app_notify — lệch code job đã chuyển `mobile_badge_noitfy` theo bot+user (#38562), cần đối chiếu khi tính expected badge"*. | **Leader + Dev** — spec badge đang lệch code kể từ `#38562`, cần cập nhật trước khi viết expected badge (liên quan `TC-DATACOUNT001-01`). |
| S6 | **Trạng thái `status_pc = 1` khi đẩy web thất bại** | Spec không mô tả; **BR-15** chỉ nói về dọn subscription hỏng | Evidence `15117`: `createMsgErrorNotify` lưu `status_pc = 1` **trước** khi gọi `pushNotifyWeb`; khi web trả 502 thì trạng thái vẫn là "đã đẩy" dù người nhận chưa nhận gì, mốc vẫn tiến → **mất notify vĩnh viễn, không retry**. | **Dev + PO** — đây là **B2**. Cần chốt: giữ trạng thái chờ để retry, hay chấp nhận mất. |
| S7 | **Số hiệu BR trong `spec_ids` của TC lệch với `feature-spec.md`** | — | `NEW-1`/`NEW-4` ghi `BR-01` nhưng mô tả nội dung của **BR-11**; `NEW-7` ghi `BR-06`/`BR-08` nhưng nội dung là **BR-05**/**BR-06**. Lệch đều 1 bậc. | **Leader** — xác nhận `feature-spec.md` có được đánh số lại sau khi viết TC không; nếu có thì rà lại toàn bộ `spec_ids` (→ m1). |
