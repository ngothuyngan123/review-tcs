# 05 — Review Report

## 0. Nguồn TC

- **Nguồn đã dùng**: MCP LME TEST STUDIO — task `#286` (ticket 40378, feature `calendar-salon`, round 1, branch `ai_fixbug_40378`), `testcase_list` 18/18 TC.
- **Tổng số TC review**: **18** (15 `pass` · 0 `fail` · 2 `skip` · 1 `error` · 0 chưa chạy — toàn bộ chỉ chạy ở `staging`).

---

## 1. Coverage — `dev-impact` + `diff code`

**Kết luận: 15 vùng/điểm ảnh hưởng · 5 đủ TC · 6 GAP · 4 RISK.**

> ⚠️ Chiều `diff code` lấy từ **Studio tab Thông tin** (`spec_delta` + `dev_impact`, `computedAt = 2026-09-10T05:10:17Z`). **Bản diff này KHÔNG phải bản fix hiệu lực** — xem `G10`.

| Mã | Chiều | Vùng ảnh hưởng | TC cover | Status | Severity | Ghi chú |
|---|---|---|---|---|---|---|
| **G1** | dev-impact | **F5 — `copyBooking` → tách hàm `copyBookingStepTime`** (luồng 「Đặt lại」 từ lịch sử đặt) | **0 TC** | **GAP** | `[BLOCKER]` | Đây là **chỗ DUY NHẤT đổi cấu trúc code** ở bản fix hiệu lực. Dev ghi rõ *"PHẢI test kỹ luồng 'Đặt lại' vì đây là chỗ duy nhất đổi cấu trúc code"*. Rà cả 18 TC: không TC nào mở màn lịch sử đặt → bấm 同じ内容で予約. Kho có sẵn `TC-SLN-454` cover luồng này. |
| **G2** | dev-impact | **F4 — `getListCourseByCalendar`** (nạp danh sách khoá học, bỏ `async: false` + trả `jqXHR`) | **0 TC** | **GAP** | `[BLOCKER]` | Theo Dev, hàm này có **đúng 1 nơi gọi = `copyBooking`**, và là **nơi DUY NHẤT đọc kết quả ngay sau lời gọi** → chính là chỗ rủi ro cao nhất. Không TC nào chạm. |
| **G3** | dev-impact | **F6 — nhánh request THẤT BẠI** (`status = 0` không rơi vào `$.ajaxSetup`; nhánh `error` chỉ tắt overlay, để lịch rỗng) | **0 TC** | **GAP** | `[BLOCKER]` | Fix **không** sửa hành vi này — chỉ làm request ít bị huỷ hơn. `REQ-004` yêu cầu *"khi request thất bại thì lớp phủ phải tắt và tester phải ghi lại màn hình hiện gì"* nhưng **không TC nào ép request lỗi thật** (offline / 500 / timeout). `TC-UI003-01` chỉ test **mạng chậm** — request vẫn thành công. Đây đúng là cơ chế sinh ra bug gốc (rỗng giả, im lặng). |
| **G4** | diff code | **Hành vi mới của `.always()`** khi request khoá học lỗi (giữ `index_course_selected` giá trị cũ, luồng chạy tiếp) | **0 TC** | **GAP** | `[MAJOR]` | Câu hỏi adversarial #4: `dev_impact` nêu hành vi thay đổi → phải có TC verify hành vi MỚI **và** chỗ cũ không hỏng. Dev tự khai dùng `.always()` (không phải `.done()`) *"để giữ đúng hành vi cũ khi request lỗi"* — **chưa ai kiểm chứng**. |
| **G5** | dev-impact | **F3 — `getListStaffByCalendar`** (nạp danh sách nhân viên, 3 nơi gọi) | 1 phần — `TC-REGSHARED001-01` | **GAP** | `[MAJOR]` | `TC-REGSHARED001-01` test **chọn nhân viên → bảng khung giờ**, tức verify đường nạp *khung giờ*, KHÔNG verify việc **nạp chính danh sách nhân viên** bất đồng bộ (thứ tự, staff OFF bị ẩn, modal chi tiết). 3 nơi gọi → 0 TC nhắm thẳng. |
| **G6** | diff code | **3/4 điểm bỏ `async: false` không có TC nhắm thẳng** (`initDataBooking`, `getListStaffByCalendar`, `getListCourseByCalendar`) | rải rác | **GAP** | `[MAJOR]` | Câu hỏi adversarial #1: mỗi điểm sửa có ≥1 TC đi qua? Chỉ `getListTimeBooking` được cover dày. Nguyên nhân gốc: bộ TC thiết kế khi mới có **1** điểm sửa (xem `G10`). |
| **G7** | dev-impact | **F2 — `initDataBooking`** (nạp ngày trống, lịch tháng — server tính tới **3 tháng**/request) | `TC-REGSHARED001-04` | **RISK** | `[MAJOR]` | TC có tồn tại nhưng **note của chính TC ghi sai giả định**: *"nhánh này dùng đường nạp dữ liệu KHÁC … và **KHÔNG nằm trong phạm vi sửa lần này**"* → TC được thiết kế ở mức **smoke**, không phải verify code vừa đổi. Thực tế `initDataBooking` **CÓ** bị bỏ `async: false`. Lịch tháng dò tới 3 tháng → thời gian request **dài hơn** nhánh tuần (4 tuần) ⇒ rủi ro huỷ request **cao hơn**, nhưng lại được test nhẹ hơn. |
| **G8** | dev-impact | **F8 — thứ tự thực thi bị đảo** (lối vào DUY NHẤT gọi nạp khung giờ **TRƯỚC** `getListDay`) | `TC-STATEDEP001-01` — **`skip`** | **RISK** | `[MAJOR]` | Có TC đúng chỗ nhưng **không chạy** ⇒ không có kết luận. Đây là rủi ro trực tiếp nhất của việc chuyển sync→async (cột ngày dựng trước/sau dữ liệu). "Có TC ≠ đã test". |
| **G9** | dev-impact | **F9 — endpoint/contract phía server không đổi** | `TC-APICONTRACT001-01` — **`error`** | **RISK** | `[BLOCKER]` | TC chạy **lỗi** và **chưa raise ticket bug** (`bug_tickets = []`). Không có bằng chứng contract giữ nguyên. |
| **G10** | diff code | **Diff Studio dùng để thiết kế TC ≠ bản fix hiệu lực** | — | **GAP** | `[BLOCKER]` | Studio `spec_delta` (tính lúc `05:10:17`): `public/js/calendar_salon/booking.js \| 7 ++++++-` → **1 file, 6 thêm / 1 xoá**; `dev_impact` Studio ghi *"bỏ `async: false` trong request nạp bảng giờ trống (**getListTimeBooking**)"* — **số ít**. Bản fix hiệu lực (Redmine Journal #135581, `05:25:31`, commit `c9889e70a1`): **1 file, 28 thêm / 10 xoá**, bỏ **4** lời gọi + tách hàm `copyBookingStepTime`. ⇒ **Toàn bộ thiết kế TC dựa trên một diff đã lỗi thời**; `G1`/`G2`/`G4`/`G6`/`G7` đều là hệ quả của điểm này. |
| **G11** | diff code | **AP-2 — Symptom-only**: nguyên nhân thứ 2 (server nặng) chưa có TC | — | **RISK** | `[MAJOR]` | Khách báo 2 triệu chứng: (a) lịch lúc hiện lúc mất → đã fix; (b) *「màn hình đặt lịch hoạt động nặng」* (Journal #135014, **07/09 — vẫn tái diễn**). Dev tự khai *"Phần tính toán phía server không đổi: màn hình vẫn nặng như cũ"*. Không TC nào **đo** thời gian nạp ⇒ nếu khách vẫn báo nặng sau release thì không có số liệu đối chứng. |
| **G12** | dev-impact | **T7 — redirect `/lme/timeout`** (handler global `$.ajaxSetup` nay áp cho request async) | 0 TC | **RISK** | `[MINOR]` | Không TC nào ép 401/403/500 để xem redirect còn đúng sau khi chuyển async. Rủi ro thấp (handler không đổi) nhưng chưa có bằng chứng. |
| — | dev-impact | F1 `getListTimeBooking` (8 nơi gọi) | `TC-FUNC001-01/02`, `TC-REGSHARED001-01/02/03`, `TC-CONC003-01`, `TC-FUNC004-01` | OK | — | — |
| — | dev-impact | F7 `LoadingOverlay` (bộ đếm show/hide) | `TC-UI003-01` | OK | — | — |
| — | dev-impact | BUG — tái hiện iOS in-app LINE | `TC-TOOLKNOW002-01`, `TC-ENV003-01/02`, `TC-SYNCAPP001-01` | OK | — | Nhưng xem `Q7` (chưa chạy production) |

> **D\* (data impact)**: `03-dev-impact.md` mục 4.2 ghi **"Không có"** — chỉ sửa JS, không đổi schema/API. Đối chiếu `spec_delta.files[] = ["public/js/calendar_salon/booking.js"]` → **khớp**, không phát sinh vùng data cần TC. RULE-07 (verify DB) **không áp dụng** cho task này.

---

## 2. Thiếu so với quan điểm test

**Kết luận: 16 quan điểm Trigger khớp task · 5 đủ TC · 11 chưa cover đủ.**

| Mã | Quan điểm | Ưu tiên | Trigger khớp vì | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|---|
| **Q1** | `LIFF-ENTRY-001` | **Cao** | Trigger nêu đích danh **salon**: *"phát sinh URL cho LINE user access (form, item, event, **salon**, lesson…)"*. Toàn bộ fix nằm trên trang LIFF hội viên. | **0 TC** | Không TC nào mang mã này. Thiếu hẳn: mở LIFF từ tin nhắn LINE (iOS/Android), chưa lấy được LINE ID → hành vi, mở LIFF ngoài app LINE, trang mở sẵn từ lâu rồi mới thao tác. Kho có 9 TC `LIFF-ENTRY-001` cho FA-020. | `[BLOCKER]` |
| **Q2** | `REG-SPEC-001` | **Cao** | *"BẮT BUỘC với mọi thay đổi spec/requirement xảy ra **SAU** khi đã có test case (kể cả thay đổi nhỏ)"*. Bản fix đổi từ 1 → 4 điểm sửa **sau** khi 11 TC đầu đã sinh (xem `G10`). | **0 TC** | Không có TC/bước xác nhận môi trường test đang chạy **đúng commit `c9889e70a1`**. 15 kết quả `pass` hiện tại **không chứng minh được** là chạy trên bản hiệu lực. | `[BLOCKER]` |
| **Q3** | `UI-003` | Trung bình **→ Cao** (rủi ro **false success**) | Trigger: *"mọi màn danh sách / **xử lý bất đồng bộ**"*. Nâng Cao vì bug gốc **chính là false success**: lịch rỗng trông như hết khung. | `TC-UI003-01` (Abn) · `-02` (Bnd) · `-03` (Bnd) | Cả 3 TC đều test nhánh **thành công**. Thiếu nhánh **request LỖI THẬT** — xem `G3`. Thiếu `Normal` (chấp nhận được: `TC-FUNC001-01` đã cover nhánh Normal cùng màn — **cần ghi lý do vào `Ghi chú` theo RULE-01**). | `[MAJOR]` |
| **Q4** | `CONC-003` | Trung bình **→ Cao** (màn có nhiều tab 週/月 + filter khoá/nhân viên cùng gọi API) | Race **tầng client** — đúng rủi ro hồi quy MỚI mà `dev_impact` Studio tự nêu: *"chuyển từ sync sang async ⇒ có thể race khi bấm chuyển tuần nhanh liên tiếp"*. | `TC-CONC003-01` (Abn) | Chỉ **1 TC, chỉ Abnormal**. Thiếu `Normal` (đối chứng tuần tự) + `Boundary` (chuyển **tháng** nhanh — request dài hơn tuần nên dễ chồng hơn) → RULE-01. | `[MAJOR]` |
| **Q5** | `REG-SHARED-001` | **Cao** | *"BẮT BUỘC với mọi release sửa code dùng chung"*. `getListTimeBooking` 8 nơi gọi, `initDataBooking` 8, `getListStaffByCalendar` 3. | `-01` `-02` `-03` `-04` — **cả 4 đều `Normal`** | **AP-3 Happy-path-only regression**: 4 TC cùng một hình dạng (nạp lại thành công ở các lối vào khác nhau). Thiếu `Abnormal` + `Boundary` → RULE-01. Không TC nào cho lối vào `copyBooking` (`G1`) và danh sách nhân viên (`G5`). | `[MAJOR]` |
| **Q6** | `STATE-DEP-001` | **Cao** | Studio gán cho lối vào quay lại từ booking đã có — lối vào **duy nhất** đảo thứ tự thực thi. | `TC-STATEDEP001-01` — **`skip`** | TC duy nhất **không chạy** ⇒ quan điểm coi như **chưa cover**. | `[MAJOR]` |
| **Q7** | `ENV-003` | **Cao** | *"Không được đánh × với lý do 'staging đã pass'"* (RULE-08). Bug phụ thuộc **thiết bị iOS + độ trễ mạng + tải server thật**, phát sinh trên **production** (salon `22739`, vài chục nhân viên, payload ~500-550 phần tử/ngày). | `TC-ENV003-01` (Abn) · `-02` (Bnd) | **RULE-08 vi phạm**: 6 TC khai `env_scope` có `prd` nhưng `prd` = **0 run**; toàn bộ 18 TC chỉ chạy `staging`. Không có bằng chứng salon staging dựng đủ 20–30 nhân viên. Thiếu `Normal`. | `[MAJOR]` |
| **Q8** | `DEPLOY-ASSET-001` | **Cao** | *"BẮT BUỘC khi release có sửa file JS/CSS"* — đúng 1 file JS. | `TC-DEPLOYASSET001-01` (Abn) | TC **pass trên staging**, nhưng Dev xác nhận **chưa bump `config/sns-line.php`**. `Evidence thực tế` rỗng ⇒ **không biết cảnh báo cache-bust đã được ghi lại chưa**. Chưa verify trên production — nơi user thật giữ cache. Thiếu `Normal` + `Boundary`. | `[MAJOR]` |
| **Q9** | `PERF-LARGE-001` | Trung bình **→ Cao** (sync/xử lý khối lượng lớn) | *"mọi chức năng xử lý danh sách/khối lượng dữ liệu"* — server dò 4 tuần (tuần) / 3 tháng (tháng) × ngày × khung giờ × nhân viên. | 0 TC mang mã này | Không TC nào **đo** thời gian nạp / kích thước payload. Xem `G11`. Kho có `TC-SLN-450` (`PERF-LARGE-001`, salon nhiều staff). | `[MAJOR]` |
| **Q10** | `DATA-REF-001` | **Cao** | *"BẮT BUỘC khi đối tượng có thao tác **xóa**/copy/đổi tên VÀ được nơi khác tham chiếu"* — luồng 「Đặt lại」 **copy** booking cũ và tham chiếu khoá học có thể **đã bị xoá**. | **0 TC** | Dev nêu đích danh case cần test: *"đặt lại booking có khoá học đã bị xoá (phải vẫn hiện 「コースが存在していません」)"*. Không TC nào cover. Kho có `TC-SLN-453`. | `[MAJOR]` |
| **Q11** | `DATA-CACHE-001` | Trung bình **→ Cao** (output user-facing) | *"release có đổi JS/asset"*. | 0 TC mang mã này | Bổ trợ `Q8` ở góc **người dùng đang mở trang bản cũ lúc deploy**. Kho có `TC-SLN-428`. | `[MINOR]` |
| — | `FUNC-001` | Cao | mọi chức năng | `TC-FUNC001-01` (pass) · `-02` (`skip`) | Cả 2 đều `Normal`, 1 `skip` → RULE-01 thiếu Abnormal/Boundary. **Đã tính vào `Q3`/`Q4`/`Q5`** (Abnormal/Boundary của cùng màn nằm ở các quan điểm đó) — không đếm trùng. | — |
| — | `FUNC-004` | Cao | giới hạn số lượng (biên dò 4 tuần) | `TC-FUNC004-01` (Bnd, pass) | Đủ cho phạm vi fix. | — |
| — | `SYNC-APP-001` | TB → Cao | có mặt trên web và app | `TC-SYNCAPP001-01` (Normal, pass) | Đủ — có đối chứng Android. | — |
| — | `UI-002` | Trung bình | UI user-facing | `TC-SYNCAPP001-01` (expected có kiểm tràn/mất cột trên điện thoại) | Đủ. | — |
| — | `INTG-CAL-001`, `CONC-001`, `DATA-DB-001` | Cao | — | — | **KHÔNG flag** — fix nằm hoàn toàn ở tầng JS trình duyệt, không chạm Google Calendar sync / logic tranh slot / UPDATE-DELETE. Đề xuất TC ở các tầng này là **AP-5 (over-coverage tầng downstream)**. | — |

---

## 3. TC trùng lặp

Đã rà **18/18** TC theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`). **Không có `DUP-EXACT`, không có `DUP-INFLATE`.** Phát hiện **2 nhóm chồng lấn** mức nhẹ:

| Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| Lịch tháng → bấm ngày → sang lịch tuần | `TC-REGSHARED001-04` (TC **duy nhất** cover điều hướng lịch tháng) | **KHÔNG xóa** `TC-REGSHARED001-03` (TC **duy nhất** cover nút 「今日」) — chỉ **làm rõ ranh giới**: bỏ nhánh 4 của `-03` (bấm ngày trên lịch tháng), để nhánh đó thuộc riêng `-04` | `DUP-SUBSET` (chỉ 1/3 nhánh) | Cùng `REG-SHARED-001` · cùng `Normal` · cùng thao tác "bấm ngày trên lịch tháng → mở lịch tuần chứa ngày đó" · cùng tiền đề K1 có ca ở tuần khác trong tháng | `[MINOR]` |
| Tái hiện trên iPhone LINE in-app | `TC-ENV003-02` (20–30 staff — gần production nhất) | **KHÔNG xóa** `TC-TOOLKNOW002-01` và `TC-ENV003-01` | chồng lấn, **không đủ điều kiện DUP** | Cùng đối tượng (iPhone LINE in-app, reload lặp), nhưng **khác mã quan điểm** (`TOOL-KNOW-002` / `ENV-003`) và **khác loại case** (Normal / Abnormal / Boundary); `-01` có thêm mạng chậm + background × 5 lần, `TOOLKNOW002-01` có thêm đối chứng bản trước fix | — (ghi nhận, không hành động) |

> **Gate đã chạy**: giả định xóa từng TC ở trên rồi chạy lại BƯỚC 2 + 3 → cả hai đều làm **mất cover** (`-03` mất nút 「今日」; `TOOLKNOW002-01` mất đối chứng bản trước fix; `ENV003-01` mất kịch bản background × mạng chậm × 5 lần). ⇒ **Không đề nghị xóa TC nào**, chỉ đề nghị làm rõ ranh giới nhánh.

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | Toàn bộ task Studio #286 | **Diff Studio dùng để thiết kế TC không phải bản fix hiệu lực.** `spec_delta` tính lúc `05:10:17` → 1 file, **6 thêm / 1 xoá**; `dev_impact` chỉ nêu `getListTimeBooking` (số ít). Bản hiệu lực (Journal #135581, `05:25:31`, commit `c9889e70a1`) → **28 thêm / 10 xoá**, 4 lời gọi + tách hàm. | Cập nhật lại `spec_delta`/`dev_impact` trên Studio theo commit `c9889e70a1`, **rà lại toàn bộ 18 TC**, rồi mới kết luận. Trước khi làm việc này, 15 kết quả `pass` **không có giá trị kết luận**. |
| 2 | `[BLOCKER]` | `REQ-007` (risk **High**) | Nội dung *"Danh sách nhân viên, danh sách khóa và dữ liệu lịch tháng **vẫn dùng gọi đồng bộ sau fix**"* — **mâu thuẫn trực tiếp** với bản fix hiệu lực (đã bỏ cả 4). Đồng thời **0 TC** map tới `REQ-007`. | Viết lại `REQ-007` theo commit `c9889e70a1`, hoặc đóng và thay bằng requirement mới cho 3 điểm sửa còn lại. |
| 3 | `[BLOCKER]` | `TC-APICONTRACT001-01` (#16993) | Kết quả `error` mà **chưa raise ticket bug** (`bug_tickets = []`). | Chạy lại; còn `error` → raise ticket và gắn vào TC. Không được để `error` trôi. |
| 4 | `[MAJOR]` | **18/18 TC** | **RULE-02 vi phạm**: `Evidence thực tế` **rỗng toàn bộ** trong khi 15 TC ghi `Đạt`. Kết quả hoàn toàn **tự khai** — đặc biệt nghiêm trọng với bug **ngẫu nhiên** cần bằng chứng nhiều lần lặp. | Bắt buộc đính kèm: video/ảnh màn hình iPhone cho các TC iOS, ảnh tab Network (DevTools) cho TC nạp asset và TC race, log số lần lặp cho `TC-TOOLKNOW002-01` (10 lần) và `TC-ENV003-01` (5 lần). |
| 5 | `[MAJOR]` | **18/18 TC** | **RULE-08 vi phạm**: `prd` = **0 run**. 6 TC khai `env_scope = ["staging","prd"]` nhưng chưa chạy production, dù bug gốc chỉ phát sinh ở production với dữ liệu thật. | Chạy tối thiểu `TC-TOOLKNOW002-01`, `TC-ENV003-01`, `TC-ENV003-02`, `TC-SYNCAPP001-01`, `TC-DEPLOYASSET001-01` trên **production** sau khi release. |
| 6 | `[MAJOR]` | `TC-STATEDEP001-01` (High), `TC-FUNC001-02` (High) | 2 TC **priority High** ở trạng thái `skip` — không có kết luận. `TC-STATEDEP001-01` là TC **duy nhất** cover lối vào đảo thứ tự thực thi. | Chạy lại cả 2. `TC-FUNC001-02` là `local-only` (tạo booking thật + phát sinh tin LINE) → chạy trên `local`/`dev`, không skip. |
| 7 | `[MAJOR]` | `03-dev-impact.md` | Có `Auto-filled: 2026-09-10 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" CHƯA tick** → F/D/T có thể thiếu hoặc map sai; review dựa trên input chưa được người xác nhận. | Tester đọc lại Journal #135581 trên Redmine, đối chiếu 4 mục, rồi tick checkbox. |
| 8 | `[MAJOR]` | `TC-TOOLKNOW002-01`, `TC-APICONTRACT001-01` | **2 mã quan điểm không có trong `framework/checklist-lme.md`** (`TOOL-KNOW-002`, `API-CONTRACT-001` — mã nội bộ Studio) → theo BƯỚC 0.6 **không tính là cover** cho bất kỳ quan điểm LME nào. | Map lại sang mã LME (`TOOL-KNOW-002` → `LIFF-ENTRY-001`/`ENV-003`; `API-CONTRACT-001` → `REG-SHARED-001`/`DEPLOY-LIVE-001`), hoặc bổ sung 2 mã này vào checklist theo RULE-10. |
| 9 | `[MINOR]` | **18/18 TC** | Cột `Trạng thái đánh giá spec` **rỗng toàn bộ** (`spec_status = null`) — không TC nào ghi `Spec ghi rõ` / `Spec không ghi` / `Đã hỏi leader`. | Điền cho các TC có expected là chuỗi cố định (「予約できる日程がありません」, 「この週には予約可能な日程がありません」, 「コースが存在していません」). |
| 10 | `[MINOR]` | `TC-ENV003-02` (#17032) | Còn `status = draft` (17 TC còn lại `ready`) nhưng đã được chạy và ghi `pass`. | Chuyển sang `ready` để kết quả có hiệu lực trong báo cáo task. |

### Chất lượng từng TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 11 | `[MAJOR]` | `TC-TOOLKNOW002-01` | **`Kết quả mong đợi` không đo lường được / không thể FAIL**: *"Nếu sau khi thực hiện đúng flow mà không tái hiện được bản cũ, vẫn ghi rõ số lần… **không coi đó là lý do bỏ verify**"* → mọi kết cục đều thoả mãn expected. TC này đang ghi `Đạt` nhưng `Đạt` không mang thông tin gì. | Tách 2 TC: (a) tái hiện trên **bản trước fix** — expected đo được (≥1/10 lần rỗng giả); (b) verify **bản đã fix** — expected: 10/10 lần hiển thị đủ khung. Nếu (a) không dựng được môi trường cũ → ghi `Chưa test` + lý do, **không** ghi `Đạt`. |
| 12 | `[MAJOR]` | `TC-REGSHARED001-03` | **Không atomic** — gộp 3 nhánh không liên quan vào 1 TC (nút 「今日」 · chuyển tab 週/月 · bấm ngày trên lịch tháng). Studio note tự thừa nhận *"Gộp ba lối vào ít rủi ro vào một TC nhiều nhánh"*. 1 nhánh fail → cả TC fail, không biết nhánh nào. | Tách thành 3 TC, hoặc tối thiểu tách nhánh 「今日」 (lối vào riêng) khỏi 2 nhánh lịch tháng — xem thêm §3. |
| 13 | `[MINOR]` | `TC-CONC003-01`, `TC-APICONTRACT001-01` | Mỗi TC gộp 2 biến thể (A/B). Chấp nhận được vì cùng `failure mode`, nhưng khi fail sẽ không biết biến thể nào. | Ghi rõ kết quả **từng biến thể** vào `Evidence thực tế` thay vì 1 dòng `Đạt` chung. |
| 14 | `[MINOR]` | `TC-ENV003-02` | `Tiền điều kiện` yêu cầu *"20–30 nhân viên active, nhiều nhân viên có ca trùng nhau"* nhưng không ghi **cách dựng** (seed script / salon mẫu có sẵn / import CSV ca làm). Người khác khó dựng lại đúng env. | Bổ sung tên salon mẫu trên staging + cách nạp ca làm hàng loạt (kho FA-020 mục 19 「Ca làm việc — CSV」). |

### Anti-pattern

| # | Severity | Phạm vi | Vấn đề |
|---|---|---|---|
| 15 | `[MAJOR]` | `[AP-3]` Happy-path-only regression | 4/4 TC `REG-SHARED-001` đều `Normal` — chỉ verify các lối vào **nạp thành công**, không có lối vào nào bị ép lỗi/chậm/chồng request. Xem `Q5`. |
| 16 | `[MAJOR]` | `[AP-2]` Symptom-only KH report | Ticket mô tả 2 triệu chứng, Dev fix 1 root cause (sync XHR) và **tự khai không xử lý** triệu chứng 「màn hình hoạt động nặng」 (vẫn tái diễn 07/09). Không TC nào phủ nguyên nhân thứ 2. Xem `G11` / `Q9`. |

> `[AP-1]` (single-trigger generic-fix) và `[AP-4]` **không áp dụng** — fix không phải dạng generic catch. `[AP-6]` **không áp dụng** — Dev đã kê đầy đủ danh sách nơi gọi (8 / 8 / 3 / 1). `[AP-5]` — đã chủ động tránh, xem dòng cuối §2.

> **Nguồn spec đã dùng**: [spec-features/admin/salon-booking/feature-spec.md](../../spec-features/admin/salon-booking/feature-spec.md) — §2.6 *Flow LINE User đặt lịch (qua LIFF)*, §5 Business Rules **BR-09** (Google Calendar per-staff), **BR-10** (ẩn nhân viên khỏi trang booking). Spec **có** cho FA-020 → không phát sinh issue thiếu spec.

---

## 5. TCs đề xuất bổ sung (15)

**Xác nhận chống trùng** — đã đối chiếu **18 TC** ở BƯỚC 0 + [kho-tcs/fa020-datlichsalon-サロン・面談予約.md](../../kho-tcs/fa020-datlichsalon-サロン・面談予約.md) (498 TC, mục 44–54 phía LINE user): **không TC đề xuất nào trùng** với bộ TC BƯỚC 0, và không TC nào trùng nhau trong chính §5.

**Dùng lại TC kho (không viết mới)** — 4 dòng, không tính vào số 15:

| Lấp | Dùng lại từ kho | Cần chỉnh gì cho bug này |
|---|---|---|
| `Q11` | `TC-SLN-428` — *Trang LIFF mở sẵn từ trước, admin đổi cài đặt rồi user mới thao tác* (`DATA-CACHE-001`) | Đổi trục: thay vì "admin đổi cài đặt", dùng "**deploy bản `booking.js` mới**" trong lúc trang đang mở. |
| `Q9` | `TC-SLN-450` — *salon nhiều staff gây lỗi `Undefined index: start_time`* (`PERF-LARGE-001`) | Chạy lại như **regression** trên bản fix; bổ sung ghi **thời gian phản hồi** của endpoint nạp khung giờ. |
| `Q3` | `TC-SLN-449` — *mất mạng / server chậm ở bước tải form → không mất dữ liệu* (`LIFF-ENTRY-001`) | Áp cùng kịch bản mất mạng cho **bước tải bảng khung giờ** thay vì bước tải form. |
| `Q1` | `TC-SLN-433`, `TC-SLN-434`, `TC-SLN-435`, `TC-SLN-436`, `TC-SLN-437` — nhóm *LINE user — chọn slot* | Chạy lại nguyên trạng làm **regression RULE-12(2)** cho vùng đường nạp khung giờ vừa đổi. |

**TC viết mới:**

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-REGSHARED001-05 | UI | REG-SHARED-001 | LINE user — lịch sử & 「Đặt lại」 | Normal | manual | Tất cả | Đặt lại 同じ内容で予約 từ booking cũ vẫn giữ đúng khoá học và mở đúng bước chọn ngày giờ | Người dùng LINE đã có **1 booking cũ** trên lịch salon K1, khoá học của booking đó **vẫn đang bật**. Lịch salon còn ca trống ở ít nhất 2 tuần. Môi trường đã deploy commit `c9889e70a1`. | 1. Mở màn đặt lịch bằng đúng người dùng LINE có booking cũ.<br>2. Vào màn lịch sử đặt lịch, mở booking cũ.<br>3. Bấm 「同じ内容で予約」 (Đặt lại).<br>4. Quan sát khoá học được chọn sẵn ở bước tiếp theo.<br>5. Chờ bảng khung giờ nạp xong, ghi lại các ô đặt được.<br>6. Chọn 1 ô và đi tiếp tới bước xác nhận. | Booking cũ trên khoá K1; lịch còn ≥ 3 khung trống trong tuần hiện tại. | Khoá học được chọn sẵn **đúng bằng** khoá học của booking cũ (không rỗng, không lệch sang khoá khác). Bảng khung giờ hiện đủ các ô đặt được thực tế, không rỗng giả. Ngày giờ mang sang bước xác nhận đúng ô đã chọn. Không có lỗi JS trên console. | | Lấp G1 · Chỗ DUY NHẤT đổi cấu trúc code (`copyBooking` → `copyBookingStepTime`) · Evidence: video thao tác + ảnh console · Môi trường: staging + product · Đánh giá spec: Spec ghi rõ (feature-spec §2.6) · Dẫn chiếu kho `TC-SLN-454` |
| TC-DATAREF001-01 | UI | DATA-REF-001 | LINE user — lịch sử & 「Đặt lại」 | Abnormal | manual | Tất cả | Đặt lại booking có khoá học ĐÃ BỊ XOÁ vẫn hiện đúng thông báo コースが存在していません | Người dùng LINE có 1 booking cũ. **Sau khi booking được tạo, admin XOÁ khoá học đó** khỏi tab コース・スタッフ. Môi trường đã deploy commit `c9889e70a1`. | 1. Admin xoá khoá học đang được booking cũ tham chiếu.<br>2. Người dùng LINE mở màn lịch sử đặt lịch và mở booking cũ đó.<br>3. Bấm 「同じ内容で予約」.<br>4. Quan sát thông báo hiển thị và trạng thái màn hình sau đó. | Booking cũ trỏ tới khoá học đã bị xoá. | Hiện đúng thông báo 「**コースが存在していません**」. Màn hình **không** treo ở vòng quay chờ, **không** nhảy sang bước chọn ngày giờ với khoá học rỗng, **không** hiện bảng khung giờ rỗng không lời giải thích. | | Lấp G1 + Q10 · Dev nêu đích danh case này phải test · Rủi ro: sau khi tách `copyBookingStepTime` chạy trong callback, thông báo có thể bị bỏ qua hoặc hiện sai thời điểm · Evidence: ảnh thông báo · Đánh giá spec: Đã hỏi leader · Dẫn chiếu kho `TC-SLN-453` |
| TC-DATAREF001-02 | UI | DATA-REF-001 | LINE user — lịch sử & 「Đặt lại」 | Abnormal | manual | Tất cả | Request nạp danh sách khoá học LỖI trong luồng Đặt lại vẫn chạy tiếp đúng như trước fix | Người dùng LINE có booking cũ. Dùng DevTools (hoặc proxy) **chặn / trả lỗi 500** cho request nạp danh sách khoá học của màn đặt lịch. | 1. Mở DevTools, đặt chặn request nạp danh sách khoá học.<br>2. Người dùng LINE mở lịch sử đặt lịch, mở booking cũ.<br>3. Bấm 「同じ内容で予約」.<br>4. Quan sát: vòng quay chờ, thông báo, bước màn hình dừng lại ở đâu.<br>5. Bỏ chặn, lặp lại thao tác và so sánh. | Request nạp khoá học trả HTTP 500 (hoặc bị chặn hoàn toàn). | Vòng quay chờ **tắt** (không treo vĩnh viễn). Luồng **vẫn chạy tiếp** đúng như hành vi trước fix (Dev dùng `.always()` chính là để giữ điều này) — không đứng im không phản hồi. Ghi lại chính xác màn hình hiện gì để PO chốt có cần thông báo lỗi riêng hay không. | | Lấp G2 + G4 · Verify trực tiếp lựa chọn `.always()` thay `.done()` mà Dev tự khai nhưng chưa ai kiểm chứng · Evidence: video + ảnh tab Network · Đánh giá spec: Spec không ghi — đã hỏi leader |
| TC-REGSHARED001-06 | UI | REG-SHARED-001 | LINE user — chọn コース/スタッフ | Normal | auto | Tất cả | Danh sách nhân viên nạp bất đồng bộ vẫn đủ, đúng thứ tự và ẩn nhân viên OFF | Lịch salon K1 bật bước chọn nhân viên, có **≥ 5 nhân viên**: 3 nhân viên đang bật, 1 nhân viên để **OFF**, 1 nhân viên bật cờ **ẩn khỏi trang booking**. | 1. Mở màn đặt lịch salon, chọn khoá K1.<br>2. Chờ bước chọn nhân viên nạp xong, ghi lại danh sách nhân viên hiển thị và thứ tự.<br>3. Mở modal chi tiết của 1 nhân viên, đóng lại.<br>4. Quay lại bước chọn khoá rồi vào lại bước chọn nhân viên, ghi lại danh sách lần 2.<br>5. Đối chiếu 2 lần ghi với cấu hình ở tiền điều kiện. | 3 nhân viên bật · 1 OFF · 1 ẩn khỏi trang booking. | Cả 2 lần đều hiện **đúng 3 nhân viên** đang bật, đúng thứ tự cấu hình. Nhân viên OFF và nhân viên ẩn **không** xuất hiện. Danh sách **không** rỗng ở bất kỳ lần nào. Modal chi tiết mở đúng nhân viên. | | Lấp G5 + G6 · `getListStaffByCalendar` bỏ `async: false`, 3 nơi gọi, hiện chỉ được cover gián tiếp · Liên quan BR-10 (feature-spec §5) · Evidence: ảnh 2 lần danh sách · Đánh giá spec: Spec ghi rõ (BR-10) · Dẫn chiếu kho `TC-SLN-432` |
| TC-REGSHARED001-07 | UI | REG-SHARED-001 | LINE user — chọn slot (lịch tháng) | Boundary | auto | Tất cả | Lịch tháng: các tháng đầu không có ca thì tự nhảy tới tháng gần nhất còn khung | Khoá K1 **KHÔNG** có ca làm ở tháng hiện tại và tháng kế tiếp, chỉ có ca ở **tháng thứ ba**. Lịch salon bật hiển thị dạng tháng. Thời hạn nhận đặt lịch bao trọn tháng thứ ba. | 1. Mở màn đặt lịch, chọn khoá K1 và vào bước chọn ngày giờ dạng **tháng**.<br>2. Chờ nạp xong (lần này lâu nhất vì server dò tới 3 tháng).<br>3. Đọc tiêu đề tháng đang hiển thị và các ngày được đánh dấu còn khung.<br>4. Lặp lại toàn bộ 5 lần, ghi kết quả từng lần. | Chỉ tháng thứ ba có ca làm; lặp 5 lần. | Cả 5 lần đều mở thẳng vào **tháng thứ ba** và đánh dấu đúng các ngày còn khung. Không lần nào lịch tháng rỗng trong khi ca làm vẫn còn. | | Lấp G6 + G7 · `initDataBooking` bỏ `async: false` — server dò tới **3 tháng** nên request **dài hơn** nhánh tuần (4 tuần) ⇒ rủi ro huỷ cao hơn nhưng đang được test nhẹ hơn · Evidence: ảnh 5 lần · Đánh giá spec: Spec ghi rõ · Dẫn chiếu kho `TC-SLN-436` |
| TC-CONC003-02 | UI | CONC-003 | LINE user — chọn slot (lịch tháng) | Boundary | auto | Tất cả | Bấm chuyển tháng liên tiếp thật nhanh khi request chưa về — màn hình khớp tháng cuối cùng | Bật giả lập mạng chậm 3G. Khoá K1 có ca làm **khác nhau rõ rệt** ở 4 tháng liên tiếp. Lịch salon bật hiển thị dạng tháng. | 1. Mở lịch dạng tháng của khoá K1.<br>2. Bấm mũi tên chuyển tháng **4 lần liên tiếp thật nhanh**, không chờ request trước về.<br>3. Chờ tất cả request kết thúc.<br>4. Đối chiếu tiêu đề tháng và các ngày còn khung với **tháng của thao tác cuối cùng**.<br>5. Lặp lại 3 lần. | 4 tháng có ca làm khác nhau; mạng Slow 3G; lặp 3 lần. | Sau khi mọi request kết thúc, tiêu đề tháng và các ngày đánh dấu **luôn khớp thao tác cuối cùng**. Response cũ về sau **không** ghi đè dữ liệu tháng mới. | | Lấp Q4 (Boundary còn thiếu của CONC-003) · Nhánh tháng chưa được test race, dù request tháng dài hơn tuần nên dễ chồng hơn · Rủi ro Dev tự nêu: *"bấm nhanh liên tiếp prev/next nay có thể để 2 request chồng nhau"* · Evidence: video + ảnh tab Network · Đánh giá spec: Spec không ghi — đã hỏi leader |
| TC-CONC003-03 | UI | CONC-003 | LINE user — chọn slot (lịch tuần) | Normal | auto | Tất cả | Chuyển tuần TUẦN TỰ (chờ từng request) luôn hiển thị đúng — đối chứng của kịch bản race | Khoá K1 có ca làm khác nhau ở 5 tuần liên tiếp. Mạng bình thường. | 1. Mở lịch tuần của khoá K1.<br>2. Bấm tiến tuần, **chờ bảng nạp xong hoàn toàn** rồi mới bấm tiếp; lặp đủ 5 tuần.<br>3. Ghi lại tiêu đề khoảng ngày và ô đặt được của từng tuần.<br>4. Lùi ngược về tuần đầu theo cùng cách, ghi lại lần nữa.<br>5. Đối chiếu 2 chiều với ca làm đã dựng. | 5 tuần có ca làm khác nhau; chờ hoàn tất giữa mỗi lần bấm. | Cả 10 lượt (5 tiến + 5 lùi) đều hiện đúng tuần và đúng ô đặt được theo ca làm đã dựng. Không lượt nào rỗng hoặc giữ dữ liệu tuần trước. | | Lấp Q4 (Normal còn thiếu của CONC-003) · **Đối chứng bắt buộc**: nếu TC này cũng sai thì lỗi không phải do race mà do dữ liệu/logic → tránh chẩn đoán nhầm khi `TC-CONC003-01` fail · Evidence: bảng ghi 10 lượt · Đánh giá spec: Spec ghi rõ |
| TC-UI003-04 | UI | UI-003 | LINE user — chọn slot (lịch tuần) | Abnormal | manual | product | Ngắt mạng giữa lúc nạp khung giờ — màn hình KHÔNG được rỗng im lặng | iPhone thật mở màn đặt lịch qua ứng dụng LINE, môi trường đã deploy commit `c9889e70a1`. Khoá K1 còn nhiều khung trống. Chuẩn bị bật/tắt chế độ máy bay nhanh. | 1. Mở màn đặt lịch trên iPhone qua LINE, chọn khoá K1.<br>2. **Ngay khi vòng quay chờ xuất hiện, bật chế độ máy bay** để cắt mạng.<br>3. Chờ 10 giây, quan sát toàn bộ màn hình: vòng quay chờ, bảng khung giờ, thông báo (nếu có).<br>4. Tắt chế độ máy bay, tải lại trang và xác nhận lịch hiển thị lại đủ khung.<br>5. Lặp lại 5 lần, ghi rõ mỗi lần màn hình hiện gì. | Cắt mạng ngay khi request đang chạy; lặp 5 lần. | **Ghi lại chính xác** màn hình hiện gì ở bước 3. Yêu cầu tối thiểu: vòng quay chờ **phải tắt** (không treo). ⚠️ Nếu màn hiện **lịch rỗng không kèm bất kỳ thông báo lỗi nào** → đây **chính là hành vi false success của bug gốc, chưa được fix** → ghi `Không đạt` và raise ticket, **không** được coi là hành vi chấp nhận được. Sau khi có mạng lại và tải lại, lịch phải hiển thị đủ khung. | | Lấp G3 + Q3 · **RULE-08 → product**: cần độ trễ mạng và tải server thật · Fix chỉ làm request ít bị huỷ hơn, **không** sửa nhánh error im lặng (`status = 0` không rơi vào `$.ajaxSetup`) · `REQ-004` yêu cầu ghi lại màn hình hiện gì · Evidence: video 5 lần · Đánh giá spec: Spec không ghi — đã hỏi leader |
| TC-UI003-05 | UI | UI-003 | LINE user — chọn slot (lịch tuần) | Abnormal | auto | Tất cả | Server trả 500 cho endpoint nạp khung giờ — phân biệt được LỖI với HẾT KHUNG | Mở màn đặt lịch trên trình duyệt máy tính có DevTools. Khoá K1 **còn nhiều khung trống**. Dùng DevTools chặn / trả 500 cho endpoint nạp khung giờ tuần. | 1. Mở DevTools, đặt chặn endpoint nạp khung giờ tuần trả HTTP 500.<br>2. Mở màn đặt lịch, chọn khoá K1.<br>3. Quan sát màn hình và ghi lại nguyên văn nội dung hiển thị.<br>4. Đối chiếu với màn hình của `TC-UI003-02` (khoá **thật sự** hết khung → 「予約できる日程がありません」).<br>5. Lặp lại với mã 502 và với timeout (không bao giờ trả về). | Endpoint trả 500 / 502 / treo không trả; khoá K1 vẫn còn khung thật. | Màn hình khi **lỗi** phải **phân biệt được** với màn hình khi **thật sự hết khung**. Không được hiện 「予約できる日程がありません」 hoặc 「この週には予約可能な日程がありません」 trong khi nguyên nhân thật là lỗi request — đó là **thông báo sai sự thật**, đúng bản chất bug KH đã báo. Nếu hiện giống nhau → ghi `Không đạt` và raise ticket đề nghị bổ sung thông báo lỗi riêng. | | Lấp G3 + G12 + Q3 · Với mã 500/502 request rơi vào `$.ajaxSetup` (redirect `/lme/timeout`) — cần xác nhận redirect còn đúng sau khi chuyển async · Evidence: ảnh 3 kịch bản + ảnh đối chứng TC-UI003-02 · Đánh giá spec: Spec không ghi — đã hỏi leader |
| TC-LIFFENTRY001-01 | UI | LIFF-ENTRY-001 | LINE user — mở link & entry | Normal | manual | product | Mở LIFF từ tin nhắn LINE trên iOS và Android đều nạp đủ khung giờ | Bot có lịch salon K1 công khai, còn khung trống. Chuẩn bị **iPhone thật** và **máy Android thật**, cả hai đã kết bạn với bot. Có tin nhắn/Step配信 chứa link đặt lịch như flow khách. | 1. Trên iPhone, mở tin nhắn LINE chứa link đặt lịch, bấm link để mở LIFF trong ứng dụng LINE.<br>2. Đi qua trang TOP → chọn khoá K1 → bước chọn ngày giờ; ghi lại các ô đặt được.<br>3. Lặp lại đủ **10 lần** trên iPhone, ghi rõ từng lần.<br>4. Lặp lại toàn bộ trên máy Android, 10 lần.<br>5. Đối chiếu 2 thiết bị với ca làm đã dựng. | Link đặt lịch trong tin nhắn LINE; 10 lần / thiết bị. | Cả 20 lượt đều hiển thị **đúng và đủ** các ô đặt được theo ca làm; **không lượt nào rỗng giả**. Trang TOP hiển thị đúng nội dung đã cài. Hai thiết bị cho cùng một tập ô đặt được. | | Lấp Q1 · `LIFF-ENTRY-001` là quan điểm **Cao** nêu đích danh salon nhưng **0/18 TC** dùng · **RULE-08 → product** (ca bug gốc) · Evidence: video + bảng ghi 20 lượt · Đánh giá spec: Spec ghi rõ (feature-spec §2.6) · Dẫn chiếu kho `TC-SLN-425` |
| TC-LIFFENTRY001-02 | UI | LIFF-ENTRY-001 | LINE user — mở link & entry | Abnormal | manual | Tất cả | Mở link đặt lịch NGOÀI ứng dụng LINE / chưa lấy được LINE ID | Lịch salon K1 công khai, còn khung trống. Lấy được URL đặt lịch dạng mở trực tiếp (không qua LIFF). | 1. Copy link đặt lịch, mở bằng **trình duyệt thường trên máy tính** (không qua LINE).<br>2. Quan sát: trang có nạp được bảng khung giờ không, nút xác nhận có bị vô hiệu hoá không.<br>3. Mở link bằng trình duyệt thường trên **điện thoại**.<br>4. Ghi lại hành vi ở cả 2 trường hợp. | URL đặt lịch mở ngoài ứng dụng LINE. | Hành vi phải **nhất quán và rõ ràng**: hoặc chặn với thông báo cần mở trong LINE, hoặc cho xem nhưng **vô hiệu hoá nút xác nhận** (chống booking thiếu định danh). Trong mọi trường hợp **không** được hiện lịch rỗng không lời giải thích trong khi lịch còn khung. | | Lấp Q1 · Sau khi 4 request chuyển async, thứ tự nạp `getDataFriendInfo` ↔ bảng khung giờ có thể đổi ⇒ cần xác nhận điều kiện chặn vẫn đúng · Evidence: ảnh 2 trường hợp · Đánh giá spec: Spec ghi rõ · Dẫn chiếu kho `TC-SLN-426` |
| TC-LIFFENTRY001-03 | UI | LIFF-ENTRY-001 | LINE user — mở link & entry | Boundary | manual | Tất cả | Trang LIFF mở sẵn từ lâu rồi mới thao tác — dữ liệu khung giờ phải được nạp lại đúng | iPhone thật. Mở màn đặt lịch salon K1 qua LINE, để nguyên **không thao tác trong ≥ 30 phút** (chuyển sang app khác, khoá màn hình). Trong thời gian đó admin **thêm 1 ca làm mới**. | 1. Mở màn đặt lịch trên iPhone qua LINE, dừng ở bước chọn khoá.<br>2. Khoá màn hình / chuyển app khác, để yên ≥ 30 phút.<br>3. Admin thêm 1 ca làm mới cho khoá K1 trong khoảng thời gian đó.<br>4. Quay lại ứng dụng LINE và tiếp tục thao tác: chọn khoá K1 → xem bảng khung giờ.<br>5. Ghi lại các ô đặt được và đối chiếu với ca làm **sau khi đã thêm**. | Trang mở sẵn ≥ 30 phút; 1 ca làm mới được thêm giữa chừng. | Bảng khung giờ hiện **đủ cả ca làm mới thêm** (dữ liệu nạp tại thời điểm thao tác, không phải lúc mở trang). Màn hình **không** rỗng, **không** treo ở vòng quay chờ sau khi app quay lại từ nền. | | Lấp Q1 + Q11 · Ghép 2 trục: trang cũ trong bộ nhớ + WKWebView bị đưa xuống nền lâu — chính điều kiện huỷ request mà fix nhắm tới · Evidence: video · Đánh giá spec: Spec không ghi — đã hỏi leader · Dẫn chiếu kho `TC-SLN-428` |
| TC-REGSPEC001-01 | UI | REG-SPEC-001 | LINE user — nạp mã lệnh trang đặt lịch | Normal | auto | Tất cả | Môi trường test đang chạy ĐÚNG bản fix hiệu lực (commit c9889e70a1), không phải bản đã rollback | Môi trường test đã deploy nhánh `ai_fixbug_40378`. Xem được nội dung tệp `booking.js` do máy chủ trả về. | 1. Mở màn đặt lịch salon với chế độ bỏ qua bộ nhớ đệm, bật tab Network.<br>2. Mở nội dung `public/js/calendar_salon/booking.js` do máy chủ trả về.<br>3. Tìm chuỗi `async: false` trong toàn bộ tệp và đếm số lần xuất hiện.<br>4. Tìm hàm `copyBookingStepTime` trong tệp.<br>5. Đối chiếu với `git diff --stat` của commit `c9889e70a1` (1 file, 28 thêm / 10 xoá). | Tệp `booking.js` do máy chủ trả về trên môi trường test. | Số lần xuất hiện `async: false` = **0** (trước fix có 4). Hàm `copyBookingStepTime` **tồn tại** trong tệp. ⚠️ Nếu `async: false` còn > 0 hoặc **không** có `copyBookingStepTime` → môi trường đang chạy **bản cũ / bản đã rollback**, **toàn bộ kết quả test khác đều vô hiệu** → dừng test, báo release. | | Lấp Q2 + G10 · **Chạy TC này TRƯỚC mọi TC khác** · Bộ 18 TC vòng 1 được thiết kế trên diff `6 thêm / 1 xoá` trong khi bản hiệu lực là `28 thêm / 10 xoá` ⇒ phải chốt được đang test bản nào · Evidence: ảnh nội dung tệp + kết quả đếm · Đánh giá spec: Spec ghi rõ (Journal #135581) |
| TC-PERFLARGE001-01 | UI | PERF-LARGE-001 | LINE user — chọn slot (lịch tuần) | Boundary | manual | product | Đo thời gian nạp khung giờ với 20–30 nhân viên trên production | Lịch salon **production** có **20–30 nhân viên active**, nhiều ca trùng nhau (tương đương salon `22739` của khách). Mở màn đặt lịch trên iPhone qua LINE, có công cụ đo thời gian phản hồi (DevTools remote / proxy). | 1. Mở màn đặt lịch, chọn khoá và 「指定なし」.<br>2. Ghi lại **thời gian phản hồi** của request nạp khung giờ tuần và **kích thước payload** trả về.<br>3. Chuyển sang lịch **tháng**, ghi lại thời gian và kích thước tương ứng.<br>4. Lặp lại toàn bộ 5 lần vào **giờ cao điểm** và 5 lần vào giờ thấp điểm.<br>5. Lập bảng so sánh 20 số đo. | 20–30 nhân viên active; 5 lần giờ cao điểm + 5 lần giờ thấp điểm × (tuần + tháng). | Ghi được **bảng số liệu cụ thể** (không phải nhận định "nhanh/chậm"): thời gian phản hồi và kích thước payload từng lượt. Mọi lượt phải nạp **thành công** và hiển thị đủ khung trống. ⚠️ Nếu có lượt > 10 giây hoặc payload > 1 MB → ghi vào kết quả làm **bằng chứng cho ticket tối ưu riêng** mà Dev đã đề xuất. | | Lấp Q9 + G11 · **RULE-08 → product** · Dev tự khai *"Phần tính toán phía server không đổi: màn hình vẫn nặng như cũ"*; khách vẫn báo 「màn hình đặt lịch hoạt động nặng」 ngày 07/09 ⇒ cần số liệu đối chứng trước khi đóng ticket · Evidence: bảng 20 số đo + ảnh tab Network · Đánh giá spec: Spec không ghi — đã hỏi leader · Dẫn chiếu kho `TC-SLN-450` |
| TC-DATACACHE001-01 | UI | DATA-CACHE-001 | LINE user — nạp mã lệnh trang đặt lịch | Abnormal | manual | product | Người dùng đang giữ cache booking.js bản cũ có nhận được bản mới sau release không | Có thiết bị **đã từng mở** màn đặt lịch salon **trước khi release** (đang giữ `booking.js` bản cũ trong bộ nhớ đệm). Sau đó release bản fix lên môi trường. Dev xác nhận **chưa bump `config/sns-line.php`**. | 1. Trước release: mở màn đặt lịch trên iPhone qua LINE để trình duyệt cache `booking.js`.<br>2. Release bản fix lên môi trường.<br>3. Trên **cùng thiết bị đó** (không xoá cache, không cài lại LINE), mở lại màn đặt lịch qua LINE.<br>4. Kiểm tra tệp `booking.js` mà trang đang chạy còn `async: false` hay không.<br>5. Xoá cache / cài lại LINE rồi mở lại, kiểm tra lần nữa và so sánh. | Thiết bị có cache bản cũ; release không đổi tham số phá cache. | Bước 4 phải cho thấy thiết bị **đã nhận `booking.js` bản mới** (`async: false` = 0). ⚠️ Nếu bước 4 vẫn là bản cũ mà bước 5 mới ra bản mới → **người dùng thật sẽ tiếp tục gặp bug dù đã release** → ghi `Không đạt`, báo đội release bump `VERSION_ASSET` trong `config/sns-line.php` trước khi đóng ticket. | | Lấp Q11 + bổ trợ Q8 · **RULE-08 → product** (hành vi cache của WKWebView trên thiết bị người dùng thật) · Đây là rủi ro Dev **tự nêu nhưng chưa xử lý** · Evidence: ảnh nội dung tệp trước/sau xoá cache · Đánh giá spec: Spec không ghi — đã hỏi leader · Dẫn chiếu kho `TC-SLN-428` |

**Đối ứng cho GAP/RISK không đề xuất TC mới:**

| Mã | Vì sao không viết TC mới |
|---|---|
| `G8` | Đã có `TC-STATEDEP001-01` đúng chỗ, chỉ đang `skip` → **chạy lại TC cũ**, không đẻ TC mới (§4 mục 6). |
| `G9` | Đã có `TC-APICONTRACT001-01`, đang `error` → **chạy lại + raise ticket** (§4 mục 3). |
| `Q6` | Trùng `G8`. |
| `Q7` | Các TC iOS đã khai `env_scope` có `prd` → **chạy lại trên production**, không đẻ TC mới (§4 mục 5). |
| `Q8` | `TC-DEPLOYASSET001-01` đã cover phần kiểm tệp; phần còn thiếu (cache thiết bị thật) đã có `TC-DATACACHE001-01`. |
| `G12` | Đã gộp vào `TC-UI003-05` (kịch bản 500/502 kiểm luôn redirect `/lme/timeout`). |

---

## 6. Spec update needed

| # | Vấn đề | Cần chốt gì | Ai chốt |
|---|---|---|---|
| 1 | **`REQ-007` (Studio, risk High) mâu thuẫn bản fix hiệu lực** — ghi *"danh sách nhân viên, danh sách khoá và dữ liệu lịch tháng vẫn dùng gọi đồng bộ sau fix"*, trong khi commit `c9889e70a1` đã bỏ cả 4 lời gọi đồng bộ. | Viết lại `REQ-007` theo commit hiệu lực, hoặc đóng và thay bằng requirement mới. | Dev + Leader |
| 2 | **Note của `TC-REGSHARED001-04` ghi lịch tháng "KHÔNG nằm trong phạm vi sửa lần này"** — sai so với bản fix hiệu lực (`initDataBooking` **có** bị sửa). | Cập nhật note + nâng mức test cho nhánh lịch tháng từ *smoke* lên *verify code vừa đổi*. | Dev |
| 3 | **Hành vi khi request nạp khung giờ THẤT BẠI chưa có chuẩn** — `REQ-004` chỉ yêu cầu *"tester ghi lại màn hình hiện gì để PO chốt"*, tức **spec chưa quy định**. Hiện tại lịch rỗng im lặng, không phân biệt với "hết khung" — đúng cơ chế gây ra bug KH. | Chốt: khi request lỗi/bị huỷ thì hiển thị gì? Có cần banner lỗi + nút thử lại + timeout/retry không? (Dev đã nêu *"chưa làm, chờ quyết định"*.) | PO + Dev |
| 4 | **Quy trình cache-bust asset chưa rõ** — Dev không bump `config/sns-line.php` theo quy ước và tự ghi *"nhờ đội release xử lý"*. Chưa có nguồn nào khẳng định quy trình release **tự động** tăng phiên bản asset. | Chốt: release có tự bump `VERSION_ASSET` không? Nếu không → bổ sung vào checklist release cho mọi thay đổi JS/CSS. | Dev + đội release |
| 5 | **2 mã quan điểm Studio chưa có trong bộ chuẩn** (`TOOL-KNOW-002`, `API-CONTRACT-001`). | Theo **RULE-10**: bổ sung vào `framework/checklist-lme.md` (kèm ID + ngày + nguồn bug), hoặc map sang mã LME sẵn có. | Leader |
| 6 | **feature-spec FA-020 không mô tả tầng LIFF phía hội viên** — §2.6 chỉ có sơ đồ luồng, không có business rule cho *cách tính và hiển thị khung trống* (dò 4 tuần / 3 tháng, điều kiện ẩn ô, thông báo rỗng). Reviewer không có chuẩn để đánh giá Expected của nhóm TC hiển thị slot. | Bổ sung mục business rule cho màn đặt lịch LIFF vào `spec-features/admin/salon-booking/feature-spec.md`. | Leader + Dev |
