# 05 — Review Report

## 0. Nguồn TC

- **Nguồn đã dùng**: MCP LME TEST STUDIO — task #223 (`ticket 38727`, round 1, branch `ai_fixbug_38727`, `reviewState=leader`, `reviewed=false`)
- **Tổng số TC review**: 22

---

## 1. Coverage — `dev-impact` + `diff code`

**Kết luận**: 10/15 vùng ảnh hưởng đủ TC · **3 GAP** · **2 RISK**

| Mã | Chiều | Vùng ảnh hưởng | TC hiện có | Status | Vấn đề | Severity |
|---|---|---|---|---|---|---|
| **G1** | diff code | **Kiểu trường friend info** đi qua `storeFriendInfo` — 6 kiểu (`type_data` 1-6) | 記述 ×10 · 選択肢 ×4 (+2 tự sinh) · ポイント ×1 | **GAP** | Guard mới nằm **đầu `storeFriendInfo`, chạy cho mọi kiểu** → mỗi kiểu là 1 nhánh dữ liệu qua đúng điểm sửa. Mới phủ **3/6 kiểu**. Thiếu hẳn **年月日 (type 3)** — kiểu **duy nhất sinh lịch gửi** (`logic-spec.md:224`: type 3 tạo `event_step` + `event_step_time` cho **Spring Boot background job**; BR-06 bước 6 ghi "*Implicit*: xoá event_step"). Thiếu **画像 (4)** và **PDF (5)**. ⚠️ NEW-4 **không** phủ 年月日 custom — nó chỉ dùng 生年月日 là **trường mặc định id âm**, đi nhánh nhãn cố định, không qua nhánh `id>0` mà guard nhắm tới. | `[BLOCKER]` |
| **G2** | dev-impact | `F3` — caller của `recordFriendInfoHistory` (hàm dùng chung) | — | **GAP** | File 03 mục 3 chỉ kê đúng 2 function của chính bản fix, **không có danh sách caller**. Dev tự viết ở mục 2 "*các luồng khác (đặt lịch/booking) cũng không còn tạo lịch sử*", Studio `dev_impact` (suy từ diff) nêu 4 luồng: **form · chat · booking · lifecycle** — nhưng không nguồn nào là **danh sách caller do Dev cam kết**. Không có danh sách thì không chốt được bộ TC đã đủ. | `[BLOCKER]` |
| **G3** | diff code | Luồng **chat 1:1** — tab 2 「友だち情報」 ghi `friend_information_value` (spec `chat-11` §4.4 + BR-17, EP-19) | — | **GAP** | Studio `dev_impact` liệt kê `chat` / "chat staff edit" trong danh sách regression rủi ro CAO. **0/22 TC** chạm màn chat 1:1. ⚠️ **Cần Dev xác nhận trước**: đây là suy luận từ Studio + spec chat-11, chưa đối chiếu code xem đường lưu của chat có thật sự gọi `recordFriendInfoHistory` không. Xác nhận rồi mới chạy 2 TC ở §5. | `[MAJOR]` |
| **R1** | dev-impact | `T3` — luồng **đặt lịch sự kiện (booking)** | NEW-10 · NEW-11 | **RISK** | Có đủ cặp dương/âm nhưng **cả 2 đều `skip`** — chưa chạy lần nào. `dev_impact` xếp booking vào "rủi ro hồi quy CAO". Có TC ≠ đã test. | `[MAJOR]` |
| **R2** | dev-impact | `D2` — `friend_information_value` trên đường **form bản cũ** | NEW-13 · NEW-18 | **RISK** | Guard chỉ đặt ở `storeFriendInfo` (form bản mới) + `recordFriendInfoHistory`. Đường form bản cũ (`FormAnswerController`) **vẫn chèn dòng giá trị** trỏ setting đã xóa — `REQ-008` và Expected của NEW-13/NEW-18 đều **để ngỏ chờ leader/dev quyết**. TC không có chuẩn để phán Đạt/Không đạt → §6. | `[MAJOR]` |

> Chiều `diff code` lấy từ Studio `task_get_context.dev_impact` + `spec_delta` (`diffAvailable=true`; 2 file / 18 thêm / 3 bớt: `app/Helpers/functions.php`, `app/Services/FormAnswer/FormAnswerService.php`).
> 4 câu hỏi adversarial: **Q1** mỗi file sửa đều có TC đi qua ✓ nhưng **chưa đủ nhánh kiểu dữ liệu** → G1 · **Q2** không phải generic-fix (guard điều kiện hẹp `id>0` + setting rỗng), 2 nhánh true/false đều có TC ✓ · **Q3** hàm dùng chung **không có danh sách caller** → G2 · **Q4** hành vi siết lại có cả TC chiều mới lẫn chiều cũ ✓ (trừ booking đang `skip` → R1).

> **Đã cân nhắc và LOẠI khỏi phạm vi review này** (ghi lại để Leader không phải hỏi lại): xóa **folder** chứa trường · **一括削除** nhiều trường · kiểm `WHERE` scope trên **2 bot**. Guard mới là **điều kiện theo trạng thái** (`setting không tồn tại`) — không quan tâm trường biến mất bằng đường nào, nên xóa folder / xóa hàng loạt cho ra **cùng trạng thái, cùng code path** với xóa 1 trường, không thêm thông tin. Còn `WHERE` scope thuộc luồng **DELETE** (BR-06 cascade) — **bản fix không chạm code luồng này**; nó thuộc bộ regression riêng của tính năng friend info (kho `TC-FRI-34`, `TC-FRI-44`, `TC-FRI-157`), không thuộc ticket #38727.

---

## 2. Thiếu so với quan điểm test

**Kết luận**: 8/14 quan điểm khớp trigger đã cover đủ · **1 GAP** · **4 RISK**

| Mã | Quan điểm | Ưu tiên | Trigger khớp vì | TC hiện có | Status | Severity |
|---|---|---|---|---|---|---|
| **Q1** | `FRIEND-001` | Cao | Chức năng ghi friend info theo **6 kiểu dữ liệu** khác nhau; guard chạy chung cho mọi kiểu. | 4 TC (`記述`/`選択肢`/`ポイント`) | **GAP** | Thiếu Boundary theo **kiểu trường** — không TC nào dùng custom 年月日 / 画像 / PDF (chi tiết ở G1). Đây là chiều biên tự nhiên của quan điểm này, và 年月日 kéo theo cả side-effect lịch gửi. | `[BLOCKER]` |
| **Q2** | `JOB-001` | Cao | Fix chạm hàm được **job nền** gọi: job nhập CSV (`HandleImportCsv`) + **Spring Boot job đọc `event_step`** của trường 年月日. | NEW-12 (`pass`) · NEW-21 (`skip`) — cả 2 mang mã `REG-SHARED-001` | **RISK** | Nhánh CSV có TC nhưng NEW-21 (job ôm dữ liệu đã queue trước khi xóa) **chưa chạy**. Nhánh **job lịch gửi của 年月日** không có TC nào (trùng G1). | `[MAJOR]` |
| **Q3** | `ENV-003` | Cao | Task chạm **job nền**; nếu 画像/PDF gắn được vào form thì chạm thêm **media/file** → RULE-08 cấm kết luận từ staging. | — | **RISK** | `env_scope` của cả 22 TC khai `all`, nhưng `envAuto` cho thấy **prd: 0 run** (local 7 run, staging 2 run). Khai phạm vi ≠ đã chạy. | `[MAJOR]` |
| **Q4** | `DATA-COUNT-001` | Cao | `dev_impact` ghi rõ guard bỏ qua cả bước **tăng `total_user_has_value`** (回答人数). Màn quản lý friend info có cột đếm này. | NEW-19 (`error`) | **RISK** | TC duy nhất chạm bộ đếm đang `error` → không có kết luận. Chưa có TC nào kiểm 回答人数 với **≥ 2 friend** (chiều cộng dồn). Kho FA-015 có hẳn nhóm 30 TC cho bộ đếm này. | `[MAJOR]` |
| **Q5** | `MSG-USER-001` | Cao | Câu hỏi lựa chọn gắn **action gửi tin theo giá trị friend info**; `dev_impact` nêu `$friendInfoHistoryId` truyền vào `sendAction(...,'6004',...)` nay trả `null`. | NEW-6 (chiều âm, `pass`) | **RISK** | Chỉ verify **không gửi** khi trường đã xóa. **Không TC nào** verify chiều dương: trường còn tồn tại → action vẫn gửi đúng tin, kiểm **trên LINE app thật** (RULE-06). Đây là biến số nằm ngay trên dòng dữ liệu bị sửa. | `[MAJOR]` |
| **Q6** | `OUT-TRUTH-001` · `LIST-001` | Cao · Trung bình | Khối 友だち情報追加履歴 có lọc theo trường + phân trang + dialog 詳細. | NEW-16 (`error`) · NEW-15 (`skip`) | **RISK** | Cả 2 TC của màn lịch sử đều **không có kết quả**. Đây chính là màn khách hàng nhìn thấy triệu chứng bug (cột 管理名 trống) → không có kết luận là rủi ro trực tiếp. | `[MAJOR]` |

> **`DATA-DB-001` — cố ý KHÔNG flag GAP.** Rule cơ học "task có UPDATE/DELETE mà không có TC kiểm `WHERE` scope trên 2 tài khoản → `[BLOCKER]`" có khớp về mặt chữ, nhưng **luồng DELETE không nằm trong diff** của bản fix này (chỉ 2 file: `functions.php`, `FormAnswerService.php`). Kiểm `WHERE` scope thuộc bộ regression của chính tính năng xóa friend info (kho `TC-FRI-157`), không thuộc ticket #38727. NEW-18 đã cover chiều rà soát dữ liệu ở mức cần cho ticket này.

---

## 3. TC trùng lặp

Đã rà **22/22 TC** theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`).

| Nhóm trùng | TC giữ lại | TC đề nghị xóa/gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| Trả lời form khi trường đã xóa — form bản mới | **NEW-3** (form 3 câu: 1 câu trỏ trường đã xóa + 1 câu trường còn sống) | NEW-1 — **KHÔNG xóa, giữ cả 2** | `DUP-SUBSET` | Cùng đối tượng (form bản mới) + cùng thao tác (trả lời form khi trường đã xóa) + tiền đề tương đương → cùng expected "không sinh dòng 管理名 trống". NEW-3 bao trọn steps + expected của NEW-1 và cover thêm chiều dương. | `[MINOR]` |

**Lý do giữ cả 2**: NEW-1 là **kịch bản tái hiện đúng nguyên văn** 6 bước của ticket #38727 (`01-bug-task.md`) — xóa đi thì mất TC repro chuẩn để đối chứng với báo cáo ban đầu. Ngoài ra NEW-1 hiện là TC duy nhất mang mã `TOOL-KNOW-002`; chỉ sau khi map lại mã quan điểm hợp lệ (§4 #6) mới cân nhắc gộp.

Không phát hiện `DUP-EXACT`, `DUP-INFLATE`, `DUP-CONFLICT`.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | `[BLOCKER]` | NEW-19 · NEW-16 | `exec = error` nhưng **chưa raise ticket bug** (`bug_tickets` rỗng). NEW-8 cùng trạng thái đã có `#40663` → 2 TC này bị bỏ quên chứ không phải đã triage. | Raise ticket cho từng TC; nếu `error` do hạ tầng thì chạy lại và ghi rõ nguyên nhân vào `note`. |
| 2 | `[BLOCKER]` | `03-dev-impact.md` mục 3 | **`[AP-6]` Caller chưa list** — fix đặt guard tại gốc hàm dùng chung `recordFriendInfoHistory` nhưng Dev chỉ kê 2 function của chính bản fix. Danh sách 4 luồng (`form/chat/booking/lifecycle`) là do **Studio suy từ diff**, không phải Dev cam kết. | Yêu cầu Dev bổ sung danh sách caller đầy đủ của `recordFriendInfoHistory` trước khi chốt bộ TC (liên quan G2 · G3). |
| 3 | `[MAJOR]` | Toàn bộ 22 TC | **Chỉ 15/22 (68%) thực sự Đạt** — dưới ngưỡng 80%. 4 `skip` (NEW-10, NEW-11, NEW-15, NEW-21) + 3 `error` (NEW-8, NEW-16, NEW-19). 7 TC không có kết luận, rơi đúng vào vùng regression rủi ro cao. | Chạy hết 7 TC còn lại trước khi Leader duyệt round 1. |
| 4 | `[MAJOR]` | NEW-18 | TC "rà soát tổng thể" có tiền đề **"đã chạy xong toàn bộ các kịch bản có thao tác ghi"** — nhưng 7/22 TC chưa chạy. Kết luận `pass` của nó ("không phát sinh dòng lịch sử thiếu tên quản lý nào") **không có giá trị** ở trạng thái hiện tại. | Chạy lại NEW-18 **sau cùng**, chỉ khi 21 TC còn lại đã có kết quả. |
| 5 | `[MAJOR]` | Toàn bộ 22 TC | **RULE-08 / ENV-003** — task chạm job nền (job nhập CSV, Spring Boot job đọc `event_step`) nhưng **prd: 0 run**. `env_scope` khai `all` không thay được lịch sử chạy thật. | Chạy tối thiểu NEW-12 · NEW-21 · NEW-6 trên production sau khi deploy; ghi nhận `runs` vào Studio. |
| 6 | `[MAJOR]` | NEW-1 · NEW-15 · NEW-17 | **3 mã quan điểm không có trong `framework/checklist-lme.md`**: `TOOL-KNOW-002` (NEW-1), `TOOL-OLDREC-001` (NEW-15), `DATA-HIST-001` (NEW-17). Nghiêm trọng nhất là **NEW-1 — TC tái hiện bug gốc** — không map được vào coverage. | Map lại: NEW-1 → `FRIEND-001` hoặc `DATA-REF-001`; NEW-15 → `COMPAT-LEGACY-001`; NEW-17 → `DATA-REF-001`. Sửa trên Studio (`testcase_update`) rồi fetch lại. |
| 7 | `[MAJOR]` | Nguồn spec | Khối 友だち情報追加履歴 (bảng `friend_info_history`) **không được mô tả** trong `spec-features/admin/friend-information/feature-spec.md` — grep toàn sub-repo chỉ ra **1 dòng gián tiếp** ở `event-booking/feature-spec.md` BR-20 (trigger `8001`). Không có chuẩn cho cột トリガー / 操作 / quy tắc ghi. | Bổ sung BR cho `friend_info_history` vào spec friend-information (§6). |
| 8 | `[MAJOR]` | `03-dev-impact.md` | File có `Auto-filled: 2026-09-09 by /new-task` nhưng checkbox **"Tester verify auto-fill chính xác" chưa tick** → F/D/T có thể thiếu hoặc map sai. Mục 4.1 gốc trên Redmine thực chất là "File thay đổi", không phải list function. | Tester đọc lại Journal #133033 trên Redmine, xác nhận đủ 4 mục rồi tick checkbox. |
| 9 | `[MINOR]` | NEW-6 · NEW-4 · NEW-18 · NEW-3 · NEW-13 | **RULE-01** — 5 quan điểm ưu tiên Cao thiếu loại case mà không ghi lý do: `DATA-REF-001` (1 Abnormal) · `FUNC-001` (1 Normal) · `DATA-DB-001` (1 Normal) · `CONC-001` (1 Abnormal) · `COMPAT-LEGACY-001` (thiếu Boundary). Chỉ `REG-SHARED-001` đủ 3 loại. | Bổ sung TC hoặc ghi lý do vào `note` từng TC. |
| 10 | `[NIT]` | Toàn bộ 22 TC | `testcase_list` **không trả về trường evidence** → không kiểm chứng được RULE-02 từ phía review. 22/22 TC do pipeline AI chạy (`last_exec.source = ai`, `by = vinth`), `human = 0`. | Leader mở Studio đối chiếu evidence của 15 TC `pass` trước khi duyệt. |

---

## 5. TCs đề xuất bổ sung (7)

**Đã đối chiếu trước khi viết**

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa015-quanlythongtinbanbe-友だち情報管理.md` (nhóm 17 「Xóa info」 10 TC · nhóm 19 「Bộ đếm 回答人数」 30 TC · `MT-01`, `MT-09`, `MT-14`) |
| Vùng regression phát hiện từ kho | `TC-FRI-150` (xóa trường → giá trị bị dọn ở 友だち詳細 **và right bar chat** → G3) · `TC-FRI-153` (xóa trường 年月日 đang có lịch gửi → G1 nhánh date) · `TC-FRI-154` (xóa rồi tạo lại cùng 管理名) — đã được NEW-20 cover, **không viết mới** · `TC-FRI-34`, `TC-FRI-44`, `TC-FRI-157` (xóa folder / 一括削除 / API xóa bot khác) — **cố ý không dùng**, xem khối "Đã cân nhắc và LOẠI" ở §1 |
| Conflict expected vs kho | `TC-FRIEND001-02` vs kho `MT-09` (⏳ CHỜ QUYẾT ĐỊNH: xóa trường 年月日 → lịch gửi đã lên **có được dọn không**) → `[MAJOR] SPEC-CONFLICT`, đã đưa §6. **Không tự chọn bên** — expected viết theo cả 2 nhánh. · `TC-FRIEND001-03` vs kho `MT-01` (⏳ 画像/PDF có hiện ở danh sách theo folder không) → §6; TC có bước xác minh tiền đề trước. |
| GAP dùng lại TC kho (không viết mới) | Nhánh "lịch gửi bị dọn khi xóa trường 年月日" → dùng lại `TC-FRI-153`; `TC-FRIEND001-02` chỉ bổ sung phần **lịch sử mồ côi** mà kho chưa có. |
| Xác nhận chống trùng | Đã đối chiếu **22 TC** ở BƯỚC 0 + kho FA-015 — **không TC đề xuất nào trùng**. Các GAP/RISK đã có TC sẵn (`G2` input Dev · `R1` NEW-10/11 · `R2` NEW-13/18 · `Q2` nhánh CSV NEW-21 · `Q3` env · `Q4` NEW-19 · `Q6` NEW-15/16) **không đẻ TC mới** — xem bảng cuối §5. |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-FRIEND001-01 | UI | FRIEND-001 | Form trả lời (LIFF) — câu hỏi gắn trường 年月日 | Normal | manual | Tất cả | Câu hỏi form gắn trường 年月日 CÒN tồn tại — ghi giá trị, lịch sử đủ tên quản lý và lịch gửi được tạo | Đã tạo trường friend info tùy chỉnh kiểu 「年月日」 tên 「TC38727_日付_X1」 thuộc thư mục 「TC38727_フォルダ」, có cấu hình action gửi tin theo lịch (± N ngày sau ngày đăng ký). Đã tạo form bản mới có 1 câu hỏi gắn trường này theo kiểu 「すでに作成済みの友だち情報に回答を記録」; form đã lưu và mở công khai. **KHÔNG xóa trường.** Đã ghi lại mốc số dòng khối 「友だち情報追加履歴」 của friend test. | 1. Dùng friend test mở link form, nhập ngày và nộp form.<br>2. Mở chi tiết friend → tab 「友だち情報」 → bảng thông tin bạn bè.<br>3. Xem khối 「友だち情報追加履歴」, so với mốc ở tiền đề.<br>4. Mở dialog 「詳細」 của dòng vừa sinh.<br>5. Chờ tới thời điểm lịch gửi đã cấu hình, mở LINE app của friend test. | Ngày nhập: ngày hiện tại + 1 | Form nộp thành công. Bảng thông tin bạn bè hiển thị đúng ngày đã nhập cho trường 「TC38727_日付_X1」. Khối 「友だち情報追加履歴」 có đúng 1 dòng mới: 「フォルダ」 = 「TC38727_フォルダ」, 「管理名」 = 「TC38727_日付_X1」 (KHÔNG trống), 「登録情報」 = ngày đã nhập. Dialog 「詳細」 mở được. Tới thời điểm lịch gửi, friend **nhận được** tin nhắn action trên LINE app. |  | Lấp `G1` (nhánh 年月日) · đối chứng dương cho TC-FRIEND001-02 · RULE-06 verify tại LINE app thật · Đánh giá spec: Spec ghi rõ (BR-08 + `logic-spec.md:224`) · Evidence: ảnh khối 履歴 + ảnh LINE app |
| TC-FRIEND001-02 | UI | FRIEND-001 | Form trả lời (LIFF) — câu hỏi gắn trường 年月日 | Abnormal | manual | product | Trường 年月日 gắn ở câu hỏi form bị xóa — không sinh lịch sử mồ côi và không có action lịch mồ côi | Như TC-FRIEND001-01 nhưng dùng trường 「TC38727_日付_X2」. Friend test đã trả lời form **1 lần** khi trường còn tồn tại (đã có giá trị + lịch gửi đã được tạo). Sau đó **xóa trường** 「TC38727_日付_X2」 tại màn quản lý friend info. Đã ghi lại mốc thời gian, số dòng khối 「友だち情報追加履歴」 và nội dung hộp thoại LINE của friend test. | 1. Dùng friend test (hoặc friend test thứ 2) mở link form, nhập ngày và nộp form **sau khi trường đã bị xóa**.<br>2. Mở chi tiết friend → tab 「友だち情報」 → khối 「友だち情報追加履歴」, so với mốc ở tiền đề.<br>3. Mở màn kết quả form và tab 「フォーム回答」 của friend.<br>4. Chờ qua thời điểm lịch gửi đã cấu hình ở lần trả lời trước.<br>5. Mở LINE app của friend test, kiểm tra tin nhắn. | Ngày nhập: ngày hiện tại + 1 | Form nộp thành công, không lỗi 500 / màn trắng. Khối 「友だち情報追加履歴」 **không có dòng mới nào** và không xuất hiện dòng 「管理名」 trống. Màn kết quả form và tab 「フォーム回答」 vẫn hiển thị đủ câu trả lời. Friend **không nhận** tin nhắn action nào của trường đã xóa trên LINE app. ⚠️ Hai nhánh chờ chốt theo `MT-09` (§6): nếu lịch gửi ĐƯỢC dọn khi xóa trường → job không chạy; nếu KHÔNG được dọn → job có chạy nhưng phải bị guard chặn. **Cả 2 nhánh phải cho cùng kết quả quan sát ở trên** — ghi rõ nhánh thực tế vào evidence. |  | Lấp `G1` (nhánh 年月日), `Q1`, `Q2` · RULE-08 job nền → chạy product · SPEC-CONFLICT với kho `MT-09` → §6 · dùng kèm `TC-FRI-153` · Đánh giá spec: Đã hỏi leader · Evidence: ảnh khối 履歴 + ảnh LINE app + log job |
| TC-FRIEND001-03 | UI | FRIEND-001 | Form trả lời (LIFF) — câu hỏi gắn trường 画像 / PDF | Abnormal | manual | product | Trường 画像 (và PDF) gắn ở câu hỏi form bị xóa — không sinh lịch sử mồ côi, file tải lên không để lại rác | **Bước xác minh tiền đề trước** (liên quan `MT-01`, §6): kiểm tra ở màn tạo câu hỏi form, mục 「友だち情報の選択」 → 「すでに作成済みの友だち情報に回答を記録」 **có liệt kê** trường kiểu 画像 / PDF không. **Nếu KHÔNG liệt kê** → dừng TC, ghi kết quả "không áp dụng" kèm ảnh chụp danh sách (đó chính là bằng chứng chốt `MT-01`). **Nếu CÓ** → tạo trường 「TC38727_画像_Y」 kiểu 画像, gắn vào 1 câu hỏi form bản mới, lưu và mở công khai, rồi xóa trường đó. Đã ghi lại mốc số dòng khối 「友だち情報追加履歴」. | 1. Dùng friend test mở link form, tải lên 1 ảnh hợp lệ và nộp form.<br>2. Mở chi tiết friend → tab 「友だち情報」 → khối 「友だち情報追加履歴」, so với mốc.<br>3. Mở màn kết quả form và tab 「フォーム回答」 kiểm tra ảnh đã tải.<br>4. Lặp lại toàn bộ với trường kiểu PDF 「TC38727_PDF_Y2」. | Ảnh JPG hợp lệ < giới hạn cho phép · file PDF hợp lệ | Form nộp thành công. Khối 「友だち情報追加履歴」 **không có dòng mới nào**, không có dòng 「管理名」 trống. Bảng thông tin bạn bè không hiện trường đã xóa. Màn kết quả form / tab 「フォーム回答」 vẫn xem được file đã tải lên (nếu spec quy định lưu kèm câu trả lời) — ghi rõ hành vi thực tế vào evidence để đối chiếu §6. |  | Lấp `G1` (nhánh 画像/PDF) · RULE-08 media → chạy product · phụ thuộc `MT-01` → §6 · Đánh giá spec: Đã hỏi leader · Evidence: ảnh danh sách chọn trường + ảnh khối 履歴 + ảnh màn kết quả form |
| TC-REGSHARED001-08 | UI | REG-SHARED-001 | Chat 1:1 — cột phải tab 友だち情報 | Normal | manual | Tất cả | Staff sửa friend info từ màn chat 1:1 cho trường CÒN tồn tại — lịch sử ghi đủ tên quản lý | ⚠️ **Chỉ chạy sau khi Dev xác nhận** đường lưu của chat 1:1 có gọi `recordFriendInfoHistory` (§4 #2). Đăng nhập admin bằng tài khoản có tên hiển thị xác định, chọn bot A. Đã tạo trường friend info tùy chỉnh kiểu 記述 tên 「TC38727_CHAT_S」 thuộc thư mục 「TC38727_フォルダ」 và đã bật hiển thị trường này ở cột phải màn chat. Có 1 friend test đã nhắn tin để xuất hiện trong danh sách chat. | 1. Mở màn chat 1:1, chọn friend test.<br>2. Ở cột phải chọn tab 「友だち情報」.<br>3. Nhập giá trị vào trường 「TC38727_CHAT_S」 rồi lưu.<br>4. Mở màn chi tiết friend đó → tab 「友だち情報」 → khối 「友だち情報追加履歴」. | Giá trị: `チャット入力38727` | Cột phải màn chat hiển thị giá trị `チャット入力38727` ngay sau khi lưu. Khối 「友だち情報追加履歴」 có đúng 1 dòng mới: 「フォルダ」 = 「TC38727_フォルダ」, 「管理名」 = 「TC38727_CHAT_S」 (KHÔNG trống), 「登録情報」 = `チャット入力38727`, cột 「トリガー」 hiển thị tên tài khoản admin đang thao tác. Dialog 「詳細」 mở được và hiện đúng dòng vừa ghi. |  | Lấp `G3` · regression · dẫn từ `TC-FRI-150` · Đánh giá spec: Spec không ghi (khối lịch sử chưa có BR — §6) · Evidence: ảnh cột phải chat + ảnh khối 履歴 |
| TC-REGSHARED001-09 | UI | REG-SHARED-001 | Chat 1:1 — cột phải tab 友だち情報 | Abnormal | manual | Tất cả | Trường friend info bị xóa trong lúc màn chat đang mở — lưu từ chat không sinh lịch sử thiếu tên quản lý | ⚠️ Điều kiện chạy như TC-REGSHARED001-08. Dùng trường 「TC38727_CHAT_T」. Chuẩn bị 2 cửa sổ trình duyệt cùng đăng nhập bot A: cửa sổ 1 mở màn chat 1:1 của friend test (tab 友だち情報 đã tải xong), cửa sổ 2 mở màn quản lý friend info. Đã ghi lại mốc thời gian và số dòng hiện có của khối 「友だち情報追加履歴」. | 1. Ở cửa sổ 2, xóa trường 「TC38727_CHAT_T」 và xác nhận xóa thành công.<br>2. KHÔNG tải lại cửa sổ 1 — nhập giá trị vào trường 「TC38727_CHAT_T」 ở cột phải rồi bấm lưu.<br>3. Mở màn chi tiết friend → tab 「友だち情報」 → khối 「友だち情報追加履歴」, so với mốc ở tiền đề.<br>4. Mở dialog lọc của khối lịch sử và dialog 「詳細」 để kiểm tra. | Giá trị: `チャット入力38727T` | Thao tác lưu không gây lỗi 500 / màn trắng. Khối 「友だち情報追加履歴」 **KHÔNG có dòng mới nào** so với mốc ở tiền đề; đặc biệt không xuất hiện dòng có cột 「管理名」 trống. Bảng thông tin bạn bè không còn hiển thị trường đã xóa. Màn chat vẫn dùng được bình thường sau thao tác. |  | Lấp `G3` · Đánh giá spec: Đã hỏi leader · Evidence: ảnh khối 履歴 trước/sau + ảnh màn chat sau khi lưu |
| TC-MSGUSER001-01 | UI | MSG-USER-001 | Form trả lời (LIFF) — action theo giá trị friend info | Normal | manual | product | Trường friend info CÒN tồn tại có option gắn action — friend nhận đúng tin nhắn trên LINE app | Đã tạo trường tùy chỉnh kiểu 「選択肢」 tên 「TC38727_選択_Y」 với option 「はい」 gắn 1 action gửi tin nhắn (chế độ 「何度でも稼働」), nội dung tin nhắn nhận biết được. Đã gắn trường này vào 1 câu hỏi của form bản mới theo kiểu 「すでに作成済みの友だち情報に回答を記録」; form đã lưu và mở công khai. **KHÔNG xóa trường**. Đã ghi lại mốc hộp thoại LINE của friend test. | 1. Dùng friend test mở link form, chọn 「はい」 và nộp form.<br>2. Mở LINE app của friend test, kiểm tra tin nhắn mới.<br>3. Mở chi tiết friend → tab 「友だち情報」 → khối 「友だち情報追加履歴」.<br>4. Mở dialog 「詳細」 của dòng lịch sử vừa sinh. | Chọn option: `はい` | Friend **nhận đúng** tin nhắn của action trên LINE app, nội dung khớp cấu hình. Khối 「友だち情報追加履歴」 có đúng 1 dòng mới với 「管理名」 = 「TC38727_選択_Y」 (không trống), 「登録情報」 = 「はい」, 「トリガー」 = 「フォーム回答完了時アクション」. Dialog 「詳細」 mở được và hiện đúng dòng đó. |  | Lấp `Q5` · đối chứng dương của NEW-6 — `$friendInfoHistoryId` nằm ngay trên dòng dữ liệu bị sửa · RULE-06 verify tại LINE app thật + RULE-08 → chạy product · Đánh giá spec: Spec không ghi (§6) · Evidence: ảnh màn hình LINE app + ảnh khối 履歴 |
| TC-DATACOUNT001-01 | Data | DATA-COUNT-001 | Quản lý friend info — bộ đếm 回答人数 | Boundary | manual | Tất cả | Bộ đếm 回答人数 với nhiều friend và trả lời lặp — cộng đúng số người, không cộng trùng | Đã tạo trường tùy chỉnh 「TC38727_記述_Z」, cột 「回答人数」 đang là 0. Đã gắn trường vào 1 câu hỏi form bản mới; form đã lưu và mở công khai. Có **2 friend test** khác nhau (friend 1, friend 2), cả hai đều chưa có giá trị cho trường này. | 1. Friend 1 trả lời form → kiểm tra cột 「回答人数」 của trường tại màn quản lý friend info.<br>2. Friend 2 trả lời form → kiểm tra lại cột 「回答人数」.<br>3. Friend 1 trả lời form **lần thứ 2** với giá trị khác → kiểm tra lại cột 「回答人数」.<br>4. Mở chi tiết friend 1 → khối 「友だち情報追加履歴」. | Friend 1 lần 1: `旧値Z38727` · Friend 2: `回答Z38727_2` · Friend 1 lần 2: `新値Z38727` | Sau bước 1: 「回答人数」 = 1. Sau bước 2: 「回答人数」 = 2. Sau bước 3: 「回答人数」 **vẫn = 2** (không tăng thành 3 vì friend 1 đã có giá trị từ trước). Giá trị của friend 1 được cập nhật thành `新値Z38727`. Khối 「友だち情報追加履歴」 của friend 1 có 2 dòng, cả 2 đều có 「管理名」 = 「TC38727_記述_Z」 (không trống). |  | Lấp `Q4` · `total_user_has_value` nằm ngay trong đoạn code bị guard bỏ qua · mở rộng chiều nhiều friend mà NEW-19 chưa có · dẫn từ kho nhóm 「Bộ đếm 回答人数」 · Đánh giá spec: Spec không ghi · Evidence: ảnh cột 回答人数 sau từng bước |

**GAP / RISK không viết TC mới** (đã có TC ở BƯỚC 0, hoặc thiếu input chứ không thiếu TC):

| Mã | Lý do không viết TC mới | Việc phải làm |
|---|---|---|
| `G2` | Không phải thiếu TC mà thiếu **input từ Dev** — chưa có danh sách caller thì không chốt được bộ TC đã đủ hay chưa. | Yêu cầu Dev bổ sung danh sách caller của `recordFriendInfoHistory` (§4 #2). Kết quả có thể làm G3 mất hiệu lực hoặc phát sinh luồng mới. |
| `R1` | NEW-10 · NEW-11 đã cover đủ cặp dương/âm cho booking. | **Chạy** 2 TC đang `skip`. |
| `R2` | NEW-13 · NEW-18 đã cover, nhưng expected đang để ngỏ. | Chốt hành vi ở §6 rồi cập nhật expected của NEW-13 trên Studio. |
| `Q2` (nhánh CSV) | NEW-12 (`pass`) + NEW-21 (`skip`) đã cover đúng kịch bản job ôm dữ liệu cũ. | **Chạy** NEW-21; chạy lại NEW-12 trên production (RULE-08). |
| `Q3` | Không phải thiếu TC mà thiếu **lần chạy trên production**. | Chạy NEW-6 · NEW-12 · NEW-21 ở env `prd`. |
| `Q6` | NEW-15 · NEW-16 đã cover lọc / phân trang / dữ liệu cũ của màn lịch sử. | **Chạy lại** NEW-16 (`error`) và NEW-15 (`skip`); raise ticket nếu vẫn lỗi. |

---

## 6. Spec update needed

| # | Chủ đề | Hiện trạng | Cần chốt |
|---|---|---|---|
| 1 | **Form bản cũ vẫn chèn `friend_information_value` mồ côi** | Guard chỉ đặt ở `storeFriendInfo` (form bản mới) và `recordFriendInfoHistory`. Đường form bản cũ (`FormAnswerController`) vẫn thêm dòng giá trị trỏ setting đã xóa — chỉ lịch sử bị chặn. Expected của NEW-13 và NEW-18 đều ghi rõ "để leader/dev quyết". | Chốt: đường form bản cũ **có** phải chặn chèn giá trị không? Nếu có → fix chưa đủ, mở lại ticket. Nếu không → ghi thành business rule để TC có chuẩn phán Đạt. |
| 2 | **Khối 友だち情報追加履歴 chưa có business rule** | `spec-features/admin/friend-information/feature-spec.md` không có mục nào cho `friend_info_history`. Chỉ `event-booking/feature-spec.md` BR-20 nhắc gián tiếp (trigger `8001`). Không có chuẩn cho cột トリガー / 操作, cũng không định nghĩa hành vi với bản ghi mồ côi cũ. | Bổ sung BR vào spec friend-information: điều kiện ghi lịch sử, bảng giá trị cột トリガー theo từng luồng (form `6004` / booking `8001` / CSV / thao tác tay), và cách hiển thị dòng mồ côi ghi trước bản fix. |
| 3 | **`MT-09` (kho FA-015) — xóa trường 年月日 có dọn lịch gửi không** | Kho đánh dấu ⏳ CHỜ QUYẾT ĐỊNH. `feature-spec.md` BR-06 bước 6 ghi "**Implicit**: xoá `event_step` + `event_step_time` (type=3)" — chữ "implicit" cho thấy chưa được xác nhận. Quyết định này đổi hẳn nhánh expected của `TC-FRIEND001-02`. | Chốt BR-06 có thực sự dọn lịch gửi khi xóa trường 年月日 hay không, rồi cập nhật expected `TC-FRIEND001-02` theo đúng nhánh. |
| 4 | **`MT-01` (kho FA-015) — 画像 / PDF có gắn được vào câu hỏi form không** | `web/logic-spec.md:62` — `getFriendInfoSettingByFolderId()` chỉ `whereIn('type_data', [1,2,3,6])`, **loại trừ type 4 (画像) và 5 (PDF)**; BR-02 xác nhận. Nhưng corpus TC cũ trong kho coi 2 kiểu này là dùng được bình thường (có `回答人数`, gán value được). Kho để ⏳ CHỜ QUYẾT ĐỊNH mức CAO. Quyết định này xác định `TC-FRIEND001-03` có áp dụng được hay không. | Chốt: 画像 / PDF có xuất hiện ở danh sách chọn trường của câu hỏi form không. Nếu **không** → ghi rõ vào spec form-answer + friend-information rằng 2 kiểu này ngoài phạm vi gắn form (và `TC-FRIEND001-03` ghi "không áp dụng"). Nếu **có** → spec đang sai ở 3 chỗ, và cần bổ sung TC cho nhánh media (RULE-08 → production). |
| 5 | **Mâu thuẫn mapping id âm giữa 2 file spec** | `friend-information` BR-10 ghi `d_4 = birthday`; `event-booking` BR-20 ghi `-4 = age`. NEW-4 (Boundary) test cả 「生年月日」 và 「年齢」 như 2 trường riêng. Guard `id>0` nên bản fix không bị ảnh hưởng, nhưng bảng mapping đang sai ở ít nhất 1 trong 2 file. | Chốt mapping chuẩn cho `-4` (và `-5` hiện không xuất hiện ở cả 2 bảng), sửa file spec sai. |
