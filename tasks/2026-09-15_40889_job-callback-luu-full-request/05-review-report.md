# 05 — Review Report

> Draft cho Leader verify. Bug ID + ngày nằm ở tên folder; vòng review nằm ở tên file.

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | `(1) MCP LME TEST STUDIO — task #298 (ticket 40889, round 1, branch m_202609_forward_webhook_40475_release-callback)` |
| Tổng số TC review | `36` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: `4/23 vùng ảnh hưởng đủ TC · 12 GAP · 6 RISK · 1 orphan`

> Chiều **(a) dev-impact**: `03-dev-impact.md` mục 4.1 (`F1`–`F4`) · 4.2 (`D1`) · 4.3 (`T1`–`T4`) — Dev kê đủ 4 mục.
> Chiều **(b) diff code**: `Input thiếu: Studio không có diff` — `task_get_context(298, ["spec_delta"])` trả `diffAvailable = false`, note: *"git diff không chạy được: không tìm thấy ref m_202609_forward_webhook_40475_release-callback"*. Chiều (b) suy từ Studio `dev_impact` (text) + mục 1/2/3 của file 03 + **bằng chứng đọc code trong bug Studio #1055 / #1036** (runner đã trace tới dòng code). **Không bịa tên file/hàm ngoài các nguồn này.**
>
> ⚠️ **Ticket song sinh — #40888** (bổ sung 2026-09-16 theo yêu cầu Leader): #40889 và [#40888](https://redmine.watermelon.vn/issues/40888) *"[PHP] Webhook callback phía PHP lưu nguyên request nhận được từ phía LINE"* là **cùng một thay đổi nghiệp vụ trên hai đường ghi khác nhau** — #40889 = writer **Java** (`HandlePostbackTask.addCallback`, 2 branch `m_202609_forward_webhook_40475_*`), #40888 = writer **PHP** (`BotController::storeCallbackEventRaw`, branch `ai_small_40888`). Bộ TC #40888 nằm ở **Studio task #299 — 55 TC**, `reviewState = done`. Các dòng **G12 → G18** dưới đây là kết quả đối chiếu 36 TC của #40889 với 55 TC của #40888; chi tiết ánh xạ từng nhóm ở **§7**.
> **#40888 ghi rõ: *"Staging đang nhận callback từ php"*** → phần lớn kết quả staging của #40889 đang đi qua đường **PHP**, không phải đường Java mà ticket này sửa.

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | **Hai writer song song của `callback_event.request`** — Java `addCallback` (#40889) và PHP `BotController::storeCallbackEventRaw()` (#40888) | `diff code` | `không có` | `RISK liên ticket` — **không phải Dev kê thiếu**: writer PHP là phạm vi của ticket song song **#40888** *"[PHP] Webhook callback phía PHP lưu nguyên request"* (Studio task #299, branch `ai_small_40888`). Mỗi ticket kê đúng writer của mình. Nhưng **#40888 ghi rõ *"Staging đang nhận callback từ php"*** → trên staging, bộ TC #40889 đang test **đường Java không được dùng**. Vẫn thiếu TC xác định **env nào chạy writer nào**, và thiếu TC chứng minh **hai writer sinh ra chuỗi giống hệt nhau** (nếu lệch → job đọc được bên này, hỏng bên kia). | `[MAJOR]` |
| G2 | **`callback_event.webhook_event_id`** — chặn webhook trùng | `diff code` | `NEW-19` (**fail** local + staging) | `RISK liên ticket` — cột này **đã được #40888 bổ sung ở vòng fix 2** (journal #136609: *"lưu thêm mã sự kiện webhook vào cột `webhook_event_id`… Nhờ cột này job mới chặn được callback trùng"*), commit `a231fd08ca`. Nghĩa là fix nằm ở **phía PHP**, còn `NEW-19` của #40889 chạy trên **đường Java** — nên `NEW-19` fail **không** chứng minh #40888 chưa fix, và ngược lại #40888 fix rồi **không** làm `NEW-19` pass. Cần chốt: writer Java có ghi cột này không, hay Java writer sẽ bị gỡ. Bug #1055 đang `status=fixed` nhưng **`retest_status = fail` @ run #1502** — chưa ai đóng vòng. | `[BLOCKER]` |
| G3 | **Thứ tự deploy 2 branch** (`...release-callback` = receiver · `...cb_full_request` = job) | `diff code` | `không có` | `GAP` — receiver mới ghi `{...}`, chỉ job **đã deploy** mới parse được `{`. Deploy receiver trước job → toàn bộ callback mới rơi `STATUS_ERROR`; rollback receiver sau khi đã có row `{...}` → job cũ cũng chết. **0 TC** verify trạng thái hỗn hợp trong lúc rollout, dù file 01 ghi rõ phải dựng **2 branch**. | `[BLOCKER]` |
| G4 | **Kiến trúc job khác nhau giữa local/staging và production** | `diff code` | `NEW-8` · `NEW-17` · `NEW-37`…`NEW-41` (chạy staging/local) | `GAP` — Catalog D: dev/staging chạy **1 job all**; production tách **3 job độc lập** (callback / broadcast / scenario) **+ job download media riêng**. Task sửa đúng job callback + đường đọc dùng chung của luồng media. **0/36 TC chạy production.** Chính bug #40896 là hệ quả của kiến trúc 1-job-all ở local (*"pool HandlePostbackTask cố định 205 thread → 0 thread quét media"*) → kết quả local **không suy ra được** cho production, và ngược lại production chưa ai chạm. | `[BLOCKER]` |
| G5 | **Cách ly nhiều bot (`WHERE` scope của vòng quét job)** | `dev-impact` (`D1`) | `không có` | `GAP` — job quét `WHERE status = 0` trên **toàn hệ thống**, không theo bot. Không TC nào dựng 2 bot cùng có row `NEW` (1 row format cũ + 1 row full body) để chứng minh nghiệp vụ không lẫn sang nhau. RULE-07 yêu cầu bản ghi trùng ở 2 tài khoản. | `[MAJOR]` |
| G6 | **Vòng đời trạng thái ngoài 4 mã đã test** — `STATUS_IGNORE_GROUP_MESSAGE (10)` · `STATUS_EXPIRED_BOT (8)` · `STATUS_NOT_FRIEND (5)` · `STATUS_BLOCKED_BY_BOT (7)` | `diff code` | `không có` | `GAP` — `parseCallbackEventData` nằm **trước** toàn bộ switch phân loại, nên mọi nhánh status đều đi qua điểm sửa. Bộ TC chỉ verify `2/102` (DONE), `3/103` (ERROR), `9` (UNKNOWN_TYPE), `30` (MEDIA_NEW). Kho `fa003` đã có sẵn `TC-RPL-224` (group message → 10) và `TC-RPL-225` (bot hết hạn > 7 ngày → 8) — chưa TC nào của task chạy lại với body mới. | `[MAJOR]` |
| G7 | `D1` — **sức chứa cột `callback_event.request`** sau khi đổi sang full body | `dev-impact` | `NEW-35` (**skip** staging + skip local) · `NEW-7` (pass **local only**) | `RISK` — REQ-009 ghi *"độ dài cột phải đủ chứa full body lớn nhất mà LINE gửi"*; full body **luôn dài hơn** mảng events cũ (thêm `destination` + không escape). TC duy nhất kiểm sức chứa chưa chạy lần nào. Tràn cột → truncate → `parseCallbackEventData` gặp JSON cụt → `STATUS_ERROR` hàng loạt, **mất callback**. | `[MAJOR]` |
| G8 | `T4` — **Verify webhook URL trên LINE Developers (検証)** | `dev-impact` | `NEW-5` (**blocked** staging) | `RISK` — 1 trong 4 tính năng Dev tự kê ở 4.3, TC tương ứng **blocked**, chưa có kết luận. `NEW-11` / `NEW-12` chỉ dựng row thủ công ở tầng DB, không đi qua đường LINE thật → không thay thế được. | `[MAJOR]` |
| G9 | `T2` — **tương thích row cũ khi job restart** | `dev-impact` | `NEW-10` (**fail** auto local · **pass** manual staging · **blocked** manual local) | `RISK` — 3 lượt chạy cho 3 kết quả khác nhau, chưa ai chốt kết luận. Bug đi kèm #40894 (*row kẹt `status=1` sau restart không bao giờ được nhặt lại — `reloadCallbackEventNewInDatabase()` đang bị comment*) đã bị **Reject**, nhưng REQ-005 lại yêu cầu *"không được kẹt ở trạng thái đang xử lý"* → mâu thuẫn chưa giải quyết (xem §6). | `[MAJOR]` |
| G10 | `F4` — **`HandleForwardCallbackEventTask.buildBody` vẫn bọc envelope?** | `diff code` | `NEW-22` (pass local ×2 + auto staging pass) | `RISK` — Dev #40889 kết luận `buildBody` *"forward nguyên trạng, không parse nên không cần sửa"*. Nhưng report của ticket cha [#40044 §1 G2](../2026-09-08_40044_webhook-relay/05-review-report.md) ghi bản dựng **đang bọc payload LINE vào envelope thêm 3 trường**, lệch `BR-01` 「加工せずそのまま」. Nếu envelope còn thì lưu full body ở #40889 **vẫn không đạt** BR-01, và expected của `NEW-22` (*"nhận đúng nguyên body LINE"*) đang pass là pass sai. Chưa ai đối chiếu 2 kết luận này. | `[MAJOR]` |
| G11 | `NEW-37` · `NEW-38` · `NEW-39` · `NEW-40` · `NEW-41` | `orphan` | — | 5 TC **không gắn `requirement_keys`** → Studio không tính vào coverage REQ nào. Đây là TC thật (callback postback/follow/unfollow/video/media thực tế, đã pass staging), chỉ thiếu nhãn. | `[MINOR]` |
| **G12** | **Một bản ghi chứa NHIỀU event — ở tầng JOB xử lý** | `diff code` (đối chiếu #40888) | `NEW-4` (**chỉ tầng lưu**) | `GAP` — **thay đổi hành vi lớn nhất của cả 2 ticket, và #40889 bỏ trắng.** Trước đây mỗi lần LINE gọi bị tách thành nhiều bản ghi (và **mất event thứ 3 trở đi**); nay **1 lần gọi = 1 bản ghi chứa đủ mọi event**. Job phải dispatch theo type của **TỪNG event** (`handleCallbackEventByType`, gom event liền kề cùng loại — #40888 đã đọc code xác minh tại `HandlePostbackTask.java:416-470`). `NEW-4` của #40889 chỉ verify **lưu đủ mảng**, dừng ở tầng receiver; **0/25 TC `tc_group=job`** có bản ghi nhiều event. #40888 có **11 TC** cho vùng này (`NEW-2` · `NEW-4` · `NEW-39` · `NEW-43` · `NEW-46` · `NEW-47` · `NEW-48` · `NEW-49` · `NEW-50` · `NEW-58` · `NEW-59`). | `[BLOCKER]` |
| **G13** | **Event `delivery` mở đầu + chọn "event đại diện"** | `diff code` (đối chiếu #40888) | `không có` | `GAP` — cột `type` / `line_id` của bản ghi lấy theo **event đầu tiên khác `delivery`**. #40888 ghi rõ hậu quả nếu đọc nhầm: *"mất thao tác bấm nút của khách, hoặc xử lý kết bạn / hủy kết bạn cho **SAI người**"*. Job đọc cùng cột `request` đó nên phải verify lại. #40889 **0 TC** chạm `delivery`; #40888 có **8 TC** (`NEW-3` · `NEW-6` · `NEW-16` · `NEW-17` · `NEW-23` · `NEW-40` · `NEW-44` · `NEW-47`). | `[BLOCKER]` |
| **G14** | **Hai bộ tiêu thụ cùng quét `callback_event`** — 3 tiến trình PHP (`HandleCallback` / `HandleCallbackMessage` / `HandleCallbackPostback`) vs job Java | `diff code` (đối chiếu #40888) | `không có` | `GAP` — #40888 cảnh báo thẳng: *"3 tiến trình PHP cũ chỉ xử lý **MỘT** event của row… nếu 1 row có nhiều event **cùng loại** thì các event sau vẫn chưa được các tiến trình này xử lý — chỉ ảnh hưởng nếu chúng **còn chạy song song với job**, cần xác nhận đã dừng hẳn"*. Đây là điều kiện chưa ai xác nhận, mà lại là **giả định nền** của toàn bộ 25 TC tầng job của #40889. #40888 dành hẳn 1 màn (**7 TC**) cho nhóm này; #40889 **0 TC**. | `[BLOCKER]` |
| **G15** | **Ma trận loại event ở tầng lưu + các loại #40889 chưa tách** | `diff code` (đối chiếu #40888) | `NEW-8` (message) · `NEW-20` (**gộp** unsend + join + leave vào 1 TC) · `NEW-37`…`NEW-41` | `GAP` — #40888 tách **13 TC** riêng cho từng loại ở tầng lưu (`NEW-28`…`NEW-40`: text/emoji · file · ảnh · video · videoPlayComplete · richmenu postback · button postback · follow · unfollow-block · join group · leave-remove group · ≥3 event · delivery kèm event nghiệp vụ) và thêm `NEW-55` (loại ngoài danh sách xử lý) · `NEW-56` (**3 nguồn postback**: richmenu / button template / image map) · `NEW-54` (tin trong nhóm + thu hồi tin trong nhóm). #40889 gộp `unsend`/`join`/`leave` vào **1 TC duy nhất** và không có 3 nguồn postback, không có loại event lạ. ⚠️ Phân biệt hai cặp dễ lẫn: `join`/`leave` = **bot** được thêm/gỡ khỏi nhóm — LME **có** xử lý (nằm trong danh sách của Studio `dev_impact`); còn **`memberJoined`/`memberLeft`** = **thành viên khác** vào/rời nhóm, cùng **`accountLink`**, là 3 loại LME **chưa** xử lý (Leader xác nhận 2026-09-16) — cả 3 vẫn đi qua `parseCallbackEventData` nên phải verify không làm chết job. ✅ Riêng nhánh **bot OLIOA (`bot_type = 1`) đã loại khỏi phạm vi** — kiểu cũ hết vòng đời, production toàn bộ `bot_type = 0`. | `[MAJOR]` |
| **G16** | **Đích downstream cụ thể của callback** | `diff code` (đối chiếu #40888) | `NEW-8` (1 expected gộp *"chat/scenario/tag/richmenu không bị ảnh hưởng"*) | `GAP` — #40888 tách từng đích có oracle đo được: **bộ đếm chưa xác nhận** (`NEW-46`) · **tin nhắn cuối + thời điểm + thứ tự danh sách bạn bè** (`NEW-59`) · **thông báo mobile / PC / Chatwork** (`NEW-58`) · action richmenu khi gọi gộp (`NEW-45`) · action xem hết video (`NEW-57`) · hiển thị tin thu hồi (`NEW-53`) · cập nhật thông tin nhóm (`NEW-54`). #40889 gom hết vào 1 câu của `NEW-8` → TC fail không truy được đích nào hỏng (đã nêu ở I10). | `[MAJOR]` |
| **G17** | **Callback nội bộ tự sinh** (`UserController::storeFakeCallbackEvent`) + endpoint postback nội bộ | `diff code` (đối chiếu #40888) | `không có` | `GAP` — #40888 xác nhận hàm này **giữ nguyên**, *"callback nội bộ không có `webhookEventId` nên cột để trống"* → job phải đọc được cả loại bản ghi này. #40888 có **3 TC** (`NEW-19` · `NEW-21` · `NEW-60`); #40889 **0 TC**. | `[MAJOR]` |
| **G18** | **Cross-check giữa 2 ticket**: job (#40889) đọc bản ghi do PHP (#40888) ghi | `diff code` (đối chiếu #40888) | `không có` | `GAP` — #40888 đã viết sẵn 2 TC bắc cầu: `NEW-41` (*job bản RELEASE đọc được bản ghi định dạng mới do PHP nhánh fix ghi*) và `NEW-42` (*thời điểm chuyển giao: bản ghi cũ và mới xen kẽ trong cùng lượt xử lý*). Cả 2 đang **chưa chạy** (`last_exec = None`). Đây là cặp TC **duy nhất** nối 2 ticket — không bên nào chạy thì không ai chứng minh 2 nhánh khớp nhau. Trùng mục tiêu với **G3** (thứ tự deploy). | `[BLOCKER]` |
| **G19** | **Gửi lại callback (`isRedelivery`) × event `delivery`** — tổ hợp **không bộ nào có** | `diff code` (đối chiếu #40888) | `NEW-19` (#298, **fail**, steps **không** bật cờ) · `NEW-11` (#299, expected đã lỗi thời) | `GAP` — kiểm chứng bằng grep toàn bộ nội dung 2 payload: chuỗi `isRedelivery` xuất hiện **0/36 TC** ở #40889, **2/55 TC** ở #40888 (`NEW-1` chỉ là dữ liệu mẫu, `NEW-11` expected *"ghi nhận hiện trạng không chống trùng"* — viết ở vòng 1, nay sai). `NEW-19` của #40889 tuy đúng đích nhưng steps chỉ ghi *"gửi lại ĐÚNG webhook đó"*, **không đổi cờ `isRedelivery` thành `true`** → không mô phỏng đúng cách LINE gửi lại. Và **tổ hợp** *gửi lại một bản ghi có chứa event `delivery`* thì **cả hai bộ đều 0 TC** — trong khi mã sự kiện dùng để chặn trùng lại lấy theo **event đại diện** (event đầu tiên khác `delivery`). Rủi ro nặng nhất do **chính Dev nêu** mà không ai viết TC: *"Nếu LINE gửi lại chỉ MỘT PHẦN sự kiện và sự kiện đại diện khác đi thì không chặn được trùng"* (#40888 journal #136609) — mặt trái của nó là **chặn nhầm cả bản ghi và mất event mới**. | `[BLOCKER]` |

> **4 câu hỏi adversarial của BƯỚC 2 (b)** — trả lời: (1) mỗi điểm sửa đều có TC đi qua — **đạt**; (2) fix là **nhận dạng format theo ký tự mở đầu**, không phải generic catch — cả 2 nhánh `{` và `[` đều có TC (`NEW-1`/`NEW-34` vs `NEW-9`/`NEW-33`), thêm `NEW-14` (khoảng trắng đầu chuỗi) — **đạt**; (3) hàm dùng chung `startHandleCallbackEvent` — Dev **có** kê danh sách nơi ảnh hưởng (callback thường + download media) và **cả 2** đều có TC — **đạt**; (4) hành vi đổi so với trước (bên thứ 3 nhận full body thay vì mảng trần) — có TC nhánh mới (`NEW-22`) nhưng **thiếu** TC chứng minh bên nhận cũ không vỡ → đã tính vào **G10**.
> **Anti-pattern**: không dính AP-1…AP-5. **AP-6** dính một phần → chính là **G1** (mục 3 dev-impact kê thiếu 1 writer).

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `25 quan điểm Trigger khớp task · 19 chưa cover đủ`

> `Q15` → `Q19` bổ sung 2026-09-16 từ đối chiếu ticket song sinh **#40888** (Studio task #299) — 5 quan điểm này #40888 có TC còn #40889 **0 TC**.

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `CONC-001` | `Cao` | `GAP — 0 TC`. Trigger: *batch đa luồng*. Bug #40896 đã chứng minh **1 webhook sinh 2 bản ghi hội thoại + 2 `auto_reply_history`** (2 thread vòng quét cùng nhặt 1 row). Không TC nào đếm **số lần xử lý thực tế** của 1 hành động logic. Kho `fa003` đã có `TC-RPL-226` (BR-22 concurrency lock) — chưa chạy lại với body mới. | `[BLOCKER]` |
| Q2 | `JOB-001` ★ | `Cao` | `GAP — 0 TC`. Trigger: *thêm/sửa job nền*. TC gắn mã `JOB-002` (**mã lạ**, không có trong checklist → không tính cover). Thiếu toàn bộ 4 chiều bắt buộc: dữ liệu lớn liên tục vượt ngưỡng · job không dừng · **retry + backoff trong log** · **số bản ghi vào = xử lý thành công + vào hàng đợi lỗi**. | `[BLOCKER]` |
| Q3 | `DEPLOY-LIVE-001` ★ | `Cao` | `GAP — 0 TC`. Trigger: *release không bật maintain, đổi **cấu trúc request***. Đây đúng định nghĩa của task (đổi định dạng payload ghi DB, 2 branch deploy lệch nhau). → G3. | `[BLOCKER]` |
| Q4 | `ENV-003` ★ | `Cao` | `GAP nội dung`. `NEW-30` **gắn nhãn** `ENV-003` nhưng nội dung là *"chỉ bản ghi đã xử lý xong mới được chuyển tiếp"* — thuộc `JOB-*`, không phải ENV. Không TC nào rà Catalog D (job tách 3 · media `p.lmes.jp` → B2 · loadbalance 2 server). 0/36 TC chạy production. **RULE-08** cấm kết luận job/media từ staging. → G4. | `[BLOCKER]` |
| Q5 | `INTG-HOOK-001` | `Cao` | `RISK` — có `NEW-19` đúng trọng tâm (webhook trùng) nhưng **fail cả local lẫn staging**, bug #1055 `retest = fail`. Evidence yêu cầu *"SỐ LẦN XỬ LÝ THỰC TẾ đếm từ log/DB"* — hiện đang chứng minh **ngược lại** (2 lần). Thiếu hẳn 2 chiều còn lại của quan điểm: webhook **trễ** và webhook **mất thứ tự**. | `[BLOCKER]` |
| Q6 | `COMPAT-LEGACY-001` ★ | `Cao` | `RISK` — chỉ `NEW-10` (Normal) mang mã này; **thiếu Abnormal + Boundary** (RULE-01) và không ghi lý do. Nhánh legacy chưa có case row cũ **nội dung hỏng** / row cũ ở các status khác `NEW`. Evidence RULE-09 đòi kết quả của **CẢ hai nhánh** — hiện nhánh cũ có 3 kết quả mâu thuẫn (G9). | `[MAJOR]` |
| Q7 | `DATA-MIG-001` | `Cao` | `RISK` — chỉ `NEW-32` (Normal, "cấu trúc bảng không đổi"); **thiếu Abnormal + Boundary** (RULE-01). Evidence đòi *"kiểm tra mẫu dữ liệu cũ từ **nhiều nguồn/thời điểm**"* — `NEW-33` chỉ kiểm "không backfill", không lấy mẫu row cũ ở nhiều trạng thái/thời điểm. | `[MAJOR]` |
| Q8 | `DATA-DB-001` ★ | `Cao` | `RISK` — `NEW-34` chỉ Normal, không có case **2 tài khoản/2 bot** để kiểm chứng `WHERE` scope (RULE-07 ghi rõ *"tạo bản ghi trùng tên ở 2 tài khoản"*). → G5. | `[MAJOR]` |
| Q9 | `STATE-001` | `Cao` | `RISK` — `NEW-31` là TC duy nhất, **skip ở cả staging lẫn local, chưa chạy lần nào**. Evidence đòi *"trạng thái dữ liệu sau khi cố tình ngắt giữa **từng cặp bước**"*; chuỗi ở đây có 4 bước (nhận → xử lý → tải media → forward) nhưng không cặp nào được ngắt thử. | `[MAJOR]` |
| Q10 | `INTG-LINE-001` | `Cao` | `RISK` — `NEW-5` **blocked**, `NEW-11` chỉ dựng row thủ công. Evidence đòi *"hành vi hệ thống khi giả lập **từng loại** lỗi LINE"* — mới có 1 loại (events rỗng). → G8. | `[MAJOR]` |
| Q11 | `REG-RUN-001` | `Cao` | `RISK` — `NEW-36` (REQ-010: *nếu còn bản ghi lịch sử chưa đánh dấu bỏ qua mà bật flag thì **toàn bộ lịch sử bị gửi ngược sang endpoint của khách***) chỉ **pass ở local**, auto skip cả 2 env. Đây là kịch bản rò rỉ dữ liệu ra bên thứ 3 — RULE-08 không cho kết luận từ local. | `[MAJOR]` |
| Q12 | `FUNC-004` | `Cao` | `RISK` — có đủ 3 Boundary (`NEW-7` · `NEW-14` · `NEW-35`) nhưng TC quyết định (`NEW-35`, sức chứa cột) **skip**, `NEW-7` chỉ pass local. Evidence đòi *"ghi rõ **nguồn limit** + kết quả 5 pattern"* — không TC nào dẫn ra giới hạn thật của cột. → G7. | `[MAJOR]` |
| Q13 | `DATA-TEXT-001` | `Cao` (nâng: text được gửi LINE **và** forward ra bên thứ 3) | `RISK` — `NEW-3` + `NEW-21` đều **Normal**; thiếu Boundary: emoji **surrogate pair**, ký tự phải escape trong JSON (`"` `\` xuống dòng), ký tự điều khiển. Fix bỏ hẳn bước `readValue`/`writeValueAsString` nên hành vi escape đổi — đây đúng vùng rủi ro. | `[MAJOR]` |
| Q14 | `PERM-003` | `Cao` | `GAP — 0 TC`. Trigger: *tổ chức vận hành nhiều LINE OA*. Gộp chung kịch bản với Q8/G5 (callback bot A không được sinh nghiệp vụ ở bot B). | `[MAJOR]` |
| **Q15** | `MSG-004` | `Cao` | `GAP — 0 TC`. Trigger: *output là nội dung user cuối nhìn thấy trên LINE*. Toàn bộ callback đều kết thúc ở màn LINE của friend, nhưng #40889 không TC nào mang mã này. #40888 có **3 TC** (`NEW-43` 3 tin dồn dập nhận đủ 3 lượt trả lời · `NEW-44` bản ghi mở đầu bằng delivery vẫn trả lời đúng người · `NEW-45` action richmenu trong lần gọi gộp). → G12 / G13 / G16. | `[BLOCKER]` |
| **Q16** | `MSG-USER-001` | `Cao` | `GAP — 0 TC`. Trigger: *chức năng tương tác với friend LINE ở bất kỳ dạng nào*. #40888 có **4 TC** (`NEW-16` · `NEW-17` · `NEW-46` · `NEW-47`) — trong đó `NEW-47` bắt đúng rủi ro *"xử lý kết bạn / hủy kết bạn cho **SAI người**"*. → G13. | `[BLOCKER]` |
| **Q17** | `FUNC-003` | `Cao` (nâng: field liên quan **webhook URL** + định danh bot) | `GAP — 0 TC`. #40888 có **3 TC** (`NEW-6` lần gọi chỉ gồm delivery → không tạo bản ghi · `NEW-10` mã bot không tồn tại → không tạo bản ghi · `NEW-23` fallback khi không có event trùng cột type). #40889 không verify nhánh nào trong 3 nhánh này ở tầng job. | `[MAJOR]` |
| **Q18** | `DATA-001` | `Cao` | `GAP — 0 TC`. Trigger: *cập nhật dữ liệu được tham chiếu ở nơi khác*. Bản ghi callback nuôi **tin nhắn cuối · thời điểm · thứ tự danh sách bạn bè** — #40888 có `NEW-59`; #40889 không TC nào chạm các trường dẫn xuất này. → G16. | `[MAJOR]` |
| **Q19** | `SYNC-APP-001` | `Cao` (nâng: flow critical user-facing) | `GAP — 0 TC`. #40888 có `NEW-58` (*thông báo mobile / PC / Chatwork không bị mất khi một lần gọi gộp nhiều event*). Đây đúng là rủi ro của việc gộp nhiều event vào 1 bản ghi, và #40889 là bên **xử lý** bản ghi gộp đó. → G12 / G16. | `[MAJOR]` |

> **Đã cover đủ, không ghi dòng**: `FUNC-001` · `REG-SHARED-001` (7 TC, đúng danh sách nơi ảnh hưởng Dev kê) · `MEDIA-001` · `INTG-HOOK-002` · `PERF-LARGE-001` (gộp vào Q12) · `REG-SPEC-001`.
> **Đánh × có lý do (RULE-03)**: `DATA-BACKUP-001` — task **không** thêm/đổi bảng DB (Dev 4.2 + `NEW-32` xác nhận schema không đổi). `SEC-001` — rủi ro PII của việc forward full body (thêm `destination`) đã được review ở ticket cha #40044 (§1 G14 / §6 mục 6b), không lặp lại ở đây; **nhưng** nếu Leader muốn chốt riêng cho `destination` thì mở lại.
> **`[NIT]` theo RULE-11**: các quan điểm ở §4 của `checklist-lme.md` không được dùng để flag ở đây.

---

## 3. TC trùng lặp nội dung

Đã rà **36/36** TC theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương` → `expected` tương đương). Phát hiện **2 nhóm chồng lấn**, **cả 2 đều KHÔNG đề nghị xóa**.

| Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| DUP-1 | `NEW-2` (TOOL-KNOW-002 — *chuỗi lưu khớp **từng ký tự** với body LINE*) | `NEW-1` (FUNC-001 — *lưu đủ cả `destination`*) → **GỘP**, không xóa | `DUP-SUBSET` | Cùng đích kiểm (cột `request` sau 1 webhook message) · cùng `Normal` · cùng đối tượng + thao tác (POST 1 event message → đọc cột `request`) · cùng tiền đề. `NEW-2` chặt hơn (byte-exact) đã bao trọn assertion `destination` của `NEW-1`. | `[MINOR]` |
| DUP-2 | `NEW-5` (INTG-LINE-001 — bấm 検証 thật trên LINE Developers) | `NEW-11` (INTG-LINE-001 — dựng row `events` rỗng thủ công) → **GỘP**, không xóa | `DUP-SUBSET` | Cùng `INTG-LINE-001` · cùng `Abnormal` · cùng đối tượng + thao tác (body `events` rỗng → job phân loại) · cùng expected `STATUS_UNKNOWN_TYPE (9)`. `NEW-5` là đường end-to-end thật, `NEW-11` là tập con ở tầng DB. | `[MINOR]` |

- **Gate đã chạy**: giả định xóa `NEW-1` + `NEW-11` → chạy lại BƯỚC 2 + 3 trên 34 TC còn lại. Kết quả: **không giữ nguyên được coverage** →
  - Xóa `NEW-1` thì `FUNC-001` mất TC ở **tầng receiver**, và `NEW-2` mang **mã lạ** `TOOL-KNOW-002` nên theo BƯỚC 0.6 #5 **không tính là cover** → `FUNC-001` tầng receiver thành GAP.
  - Xóa `NEW-11` thì `INTG-LINE-001` chỉ còn `NEW-5` đang **blocked** → quan điểm mất kết luận hoàn toàn.
  - → **Đổi cả 2 sang GỘP**: giữ nguyên 2 TC, hợp nhất assertion mạnh hơn vào TC giữ lại và **gắn lại mã quan điểm chuẩn cho `NEW-2`** (`FUNC-001`) thay vì `TOOL-KNOW-002`.
- `DUP-INFLATE` che GAP: **không có**.
- `DUP-CONFLICT` giữa 2 TC: **không có**. (Mâu thuẫn tìm được là **TC vs requirement**, không phải TC vs TC — đã đưa §6 mục 2.)
- Xóa/sửa thật do human thực hiện trên Studio (`testcase_update` / `testcase_delete`) — report **không** tự sửa TC.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | **Toàn bộ nguồn — tỷ lệ pass thật** | Đọc theo `last_exec`: **20/36 pass (55%)** → dưới ngưỡng 80% của BƯỚC 0.6 #1. Nhưng `last_exec` **đang che dữ liệu**: nó chỉ giữ lượt chạy **gần nhất** (auto local run #1502, 2026-09-15 10:26) và **ghi đè kết quả manual trên staging trước đó**. Triage qua `task_get_report` (14 run, 207 kết quả auto + 45 manual): **`NEW-10` · `NEW-18` · `NEW-15` thực ra đã pass** (manual staging/local), chỉ fail ở lượt auto local. **Fail thật còn đúng 1: `NEW-19`** (fail cả local lẫn staging). **Chưa có kết luận thật: `NEW-5` (blocked) + `NEW-31` (skip mọi env)**. | Không dùng `testcase_list.last_exec` làm kết luận nghiệm thu. Chốt lại trạng thái từng TC trên Studio theo lượt chạy có giá trị nhất (env cao nhất), rồi chạy lại vòng auto để `last_exec` phản ánh đúng. |
| I2 | `[BLOCKER]` | Bug Studio **#1055** (High) ↔ `NEW-19` | Bug đang `status = fixed` nhưng `retest_status = **fail**` @ run #1502 (2026-09-15 10:26), trong khi `verified_at` ghi 2026-09-15 09:29 — **đánh dấu fixed trước, retest fail sau, không ai mở lại**. Bug này **chưa có `redmine_id`** → không tồn tại trên Redmine, Dev không thấy. | Mở lại bug #1055 trên Studio, raise ticket Redmine tương ứng. Đây là regression trực tiếp của cách fix (G2), không phải bug nền. |
| I3 | `[BLOCKER]` | `NEW-19` · `NEW-10` · `NEW-18` · `NEW-15` | 4 TC `fail` theo `last_exec`, **3 TC không gắn `bug_tickets`** (chỉ `NEW-19` có #40896). Theo BƯỚC 0.6 #2, TC fail chưa raise ticket = BLOCKER. Sau triage I1 thì 3 TC này pass ở env cao hơn → **không phải bug thiếu ticket mà là trạng thái TC sai**. | Cập nhật trạng thái 3 TC trên Studio (`testcase_update`) kèm ghi chú env đã pass, để vòng review sau không đếm nhầm. |
| I4 | `[MAJOR]` | **Môi trường đã chạy** — 36/36 TC | `local` 28 · `staging` 8 · **`production` 0**. Task chạm **job nền + download media + gọi endpoint bên thứ 3** → RULE-08 / `ENV-003` cấm kết luận từ local-staging. Riêng bug #40896 (*0 thread quét media*) là hệ quả của kiến trúc **1-job-all** chỉ có ở local — vừa không suy ra được cho production, vừa che mất việc production (3 job tách) chưa ai chạm. | Chạy tối thiểu bộ TC ở §5 có `Phạm vi ENV = product` trước khi nghiệm thu. → G4 / Q4. |
| I5 | `[MAJOR]` | **Evidence — 36/36 TC** | `testcase_list` trả `Evidence thực tế` rỗng toàn bộ; `task_get_report` cho thấy **45/45 manual result có `evidence: []`**. Có `Kết quả thực thi` nhưng không bằng chứng → **RULE-02**: kết quả tự khai. Riêng `INTG-HOOK-001` / `CONC-001` bắt buộc evidence là **số lần xử lý thực tế đếm từ log/DB** — không có thì kết quả pass không có giá trị. | Yêu cầu đính log job + query DB cho nhóm TC job/idempotency trước khi chốt. |
| I6 | `[MAJOR]` | `NEW-13` (version **2**) | **Expected bị nới để TC pass.** Bản v2 ghi: *"Không yêu cầu kiểm cột `error_message`… để NULL/rỗng vẫn là đúng"*. Nhưng **REQ-005** yêu cầu *"bản ghi có nội dung không đọc được phải chuyển trạng thái lỗi **kèm mô tả lỗi**"*, và bug #1037/#40895 đã chỉ ra job **có** tính ra mô tả lỗi, chỉ bị `@Column(updatable = false)` chặn ghi. Bug bị **Reject** → TC được sửa expected theo bản dựng thay vì theo requirement → requirement mất người canh. | Không sửa expected theo code. Chốt REQ-005 ở §6, rồi trả expected về đúng chuẩn đã chốt. |
| I7 | `[MAJOR]` | **8 mã quan điểm lạ** / 10 TC | `TOOL-ERRHYG-001`(3) · `TOOL-OLDREC-001`(2) · `TOOL-KNOW-002` · `RULE-09` · `I18N-001` · `API-CONTRACT-001` · `JOB-002` · `TOOL-NEGCTRL-001` — không có trong `framework/checklist-lme.md`. Theo BƯỚC 0.6 #5 các TC này **không tính là cover** → trực tiếp tạo ra Q2 (`JOB-001`) và ảnh hưởng DUP-1. Đáng chú ý: `RULE-09` bị dùng làm **mã quan điểm** cho `NEW-18`, nhưng RULE-09 là **rule**, không phải quan điểm. | Map lại sang mã chuẩn: `TOOL-ERRHYG-*` → `INTG-LINE-001`/`FUNC-004`; `TOOL-OLDREC-*` → `COMPAT-LEGACY-001`; `RULE-09` (NEW-18) → `COMPAT-LEGACY-001`; `I18N-001` → `DATA-TEXT-001`; `JOB-002` → `JOB-001`; `API-CONTRACT-001` → `INTG-HOOK-001`. |
| I8 | `[MAJOR]` | `NEW-30` | Gắn mã `ENV-003` nhưng nội dung (*"chỉ bản ghi đã xử lý xong mới được chuyển tiếp — kiểm cả hai dải mã trạng thái"*) hoàn toàn không thuộc quan điểm môi trường → làm `ENV-003` **trông như đã cover** trong khi 0 TC chạy production. | Đổi mã `NEW-30` sang `JOB-001` (hoặc `STATE-001`); `ENV-003` nhận TC mới ở §5. |
| I9 | `[MAJOR]` | `03-dev-impact.md` | Checkbox **"Tester verify auto-fill chính xác" chưa tick** (file có `Auto-filled: 2026-09-15 by /new-task`). Mục 4 của Dev đã được chứng minh là **thiếu ít nhất 2 mục** (writer PHP — G1; cột `webhook_event_id` — G2), nên đây không phải thủ tục hình thức. | Tester đọc lại Redmine #40889 + đối chiếu G1/G2 rồi tick, hoặc yêu cầu Dev bổ sung mục 4. |
| I10 | `[MAJOR]` | `NEW-8` · `NEW-37`…`NEW-41` | **TC không atomic**: `NEW-8` gộp 5 đích kiểm trong 1 expected (*"full body, DONE, chat/scenario/tag/richmenu không bị ảnh hưởng"*). TC fail thì không truy được đích nào hỏng. Kho `fa003` `TC-RPL-220`/`221` đã tách sẵn **ma trận 7 loại message × có/không auto-reply** — bộ TC hiện tại chỉ có 1 TC message chung. | Tách `NEW-8` theo đích kiểm, hoặc bổ sung TC ma trận loại message (đã đề xuất `TC-INTGLINE001-10` ở §5). |
| I11 | `[MAJOR]` | `NEW-19` (#298) ↔ `NEW-11` (#299) | **Steps không mô phỏng đúng cách LINE gửi lại.** `NEW-19` chỉ ghi *"Gửi lại ĐÚNG webhook đó lần thứ hai (cùng mã event webhook)"* — **không đổi cờ `deliveryContext.isRedelivery` thành `true`**. Grep toàn bộ payload: chuỗi `isRedelivery` xuất hiện **0/36 TC** ở #40889. `NEW-11` của #299 **có** đổi cờ (*"chỉ đổi 「isRedelivery」 thành true như cách LINE gửi lại"*) nhưng expected lại là *"ghi nhận hiện trạng KHÔNG chống trùng"* — viết ở vòng 1, đã lỗi thời sau vòng fix 2. Kết quả: **không TC nào ở cả hai bộ** vừa mô phỏng đúng vừa có expected đúng. | Sửa steps `NEW-19` (#298) để đặt cờ `isRedelivery = true` ở lần gửi thứ hai; sửa expected `NEW-11` (#299) theo hành vi sau vòng fix 2 (§6 mục 8). Bộ TC mới `TC-INTGHOOK001-10` · `-13` · `-14` ở §5 đã viết đúng cả hai vế. |
| I12 | `[MINOR]` | Toàn bộ 36 TC | `Trạng thái đánh giá spec` (`spec_status`) **rỗng toàn bộ** — không TC nào ghi `Spec ghi rõ` / `Spec không ghi` / `Đã hỏi leader`, dù task có 10 REQ rõ ràng trên Studio. | Điền `spec_status`, tối thiểu cho nhóm TC dính REQ-005 / REQ-007 / REQ-010 (những chỗ đang có tranh cãi ở §6). |
| I13 | `[NIT]` | `NEW-2` · `NEW-9` · `NEW-33` … | `TC No.` trên Studio dùng dạng `NEW-<n>` (temp_id) thay vì `TC-<mã quan điểm bỏ gạch>-<nn>` theo chuẩn repo. Không ảnh hưởng review (Studio là nguồn), chỉ lệch khi đối chiếu với kho-tcs. | Giữ nguyên trên Studio; snapshot `04-tc-list.md` đã tự sinh `TC No.` chuẩn. |

> **Nguồn spec đã dùng** (BƯỚC 1, không tính là issue): `spec-features/admin/auto-reply/job/job-spec.md` §Bảng `callback_event` (16 mã status + 3 thread poll) · `spec-features/admin/bot-add-v2/feature-spec.md` BR-008 (format webhook URL `{DOMAIN}/line/callback/add/{bot_id}`) · Studio `requirements` REQ-001…REQ-010 · `templates/LME-SYSTEM-SPEC.md:225,738`. **Tìm được spec** → không ghi dòng `[MAJOR]` thiếu spec.

---

## 5. TCs đề xuất bổ sung (37)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa003-tudongtraloi-自動応答.md` (nhóm **27. Runtime — nhận message & callback**, 7 TC) · `kho-tcs/fa001-chat11-11チャット.md` (đích downstream của callback). **Kho chưa có tab riêng cho Webhook転送 / job callback** — không đối chiếu trực tiếp được. |
| Vùng regression phát hiện từ kho | `TC-RPL-220` · `TC-RPL-221` (ma trận **7 loại message × có/không auto-reply**) · `TC-RPL-223` (state machine 0→1→2) · `TC-RPL-224` (group message → status 10) · `TC-RPL-225` (bot hết hạn > 7 ngày → status 8) · `TC-RPL-226` (BR-22 concurrency lock) · `TC-RPL-222` (đổi domain callback `cb.lmes.jp` → `cb-2.lmes.jp`). Tất cả đi qua `parseCallbackEventData` nên **phải chạy lại với body format mới**; bộ TC ở BƯỚC 0 **không cover** nhóm này. |
| Conflict expected vs kho | `Không` — không TC đề xuất nào mâu thuẫn expected với kho. (Mâu thuẫn tìm được là TC vs requirement, đã đưa §6.) |
| GAP dùng lại TC kho (không viết mới) | `G6 → TC-RPL-224 / TC-RPL-225` (giữ nguyên steps, **chỉ đổi tiền đề** sang row full body) · `Q1 → TC-RPL-226` (giữ nguyên, chạy lại sau fix). Đã viết lại thành TC riêng vì tiền đề đổi; ID kho ghi ở `Ghi chú`. |
| Bộ TC ticket song sinh **#40888** | Studio task **#299 — 55 TC** (`reviewState = done`). Đã đối chiếu toàn bộ 55 TC với 36 TC của #40889 → sinh ra `G12`…`G18` (§1), `Q15`…`Q19` (§2) và **6 TC cuối** của bảng dưới. Chi tiết ánh xạ từng nhóm ở **§7**. |
| Rà trùng **nội bộ §5** (2026-09-16) | Rà 5 TC nhóm *gửi lại / event báo đã gửi* theo 4 yếu tố → phát hiện **1 cặp `DUP-SUBSET`**: `TC-INTGHOOK001-10` và `TC-INTGHOOK001-12` trùng cả 4 yếu tố (cùng `INTG-HOOK-001` · cùng `Abnormal` · cùng *gửi lại y hệt body rồi đếm số lần xử lý* · cùng tiền đề), chỉ khác ở chỗ `-12` có bật cờ `isRedelivery` còn `-10` thì không, và `-10` có thêm vế kiểm forward. → **Đã gộp vào `TC-INTGHOOK001-10`** (giữ cả cờ lẫn vế forward), xoá `-12`. Bốn ca còn lại của nhóm **không trùng** — khác tiền đề/đích kiểm: `-11` (bản ghi đầu đã DONE + đã forward, cách 2 phút) · `-13` (bản ghi có event báo đã gửi, kiểm chọn mã đại diện) · `-14` (gửi lại kèm event mới, kiểm mất dữ liệu) · `TC-FUNC003-01` (chỉ có event báo đã gửi, không sinh bản ghi). `TC-MSGUSER001-01` vs `TC-MSG004-03` cũng **không trùng**: cùng bối cảnh *báo đã gửi đứng đầu* nhưng khác event nghiệp vụ (`follow` vs `postback`) và khác đích kiểm (kết bạn sai người vs mất thao tác bấm nút) — đúng 2 hậu quả Dev nêu riêng. |
| Xác nhận chống trùng | Đã đối chiếu **36 TC** ở BƯỚC 0 + **55 TC** của task #299 (#40888) + 7 TC kho `fa003` — **không TC đề xuất nào trùng** theo 4 yếu tố. 6 TC cuối tuy **dẫn từ** TC của #40888 nhưng đổi **tầng kiểm chứng** (tầng job / đường ghi Java) nên không phải bản sao. |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-INTGHOOK001-10 | API | INTG-HOOK-001 | Job callback — LINE gửi lại nguyên một webhook | Abnormal | manual | product | LINE gửi lại y hệt một webhook — chỉ xử lý đúng một lần, không forward thừa | - Bot test có quy tắc trả lời tự động khớp từ khoá `dup-test`<br>- Đã bật chuyển tiếp Kênh 1 tới một máy chủ nhận thử đọc được nội dung<br>- **Lưu ý cách LINE gửi lại**: LINE giữ nguyên mã sự kiện và chỉ đổi cờ `deliveryContext.isRedelivery` từ `false` sang `true` — phải mô phỏng đúng cờ này, gửi lại y nguyên body là **chưa đủ**<br>- Ghi nhận số bản ghi hội thoại, số lượt trả lời tự động và số lần máy chủ nhận thử đã nhận **trước** khi bắt đầu | 1. Gửi tới đường nhận callback của bot một nội dung LINE hợp lệ chứa từ khoá `dup-test`, cờ báo gửi lại = `false`. Ghi lại **mã sự kiện** trong body<br>2. Chờ ~3 giây<br>3. Gửi lại **đúng body đó**, chỉ đổi cờ báo gửi lại thành `true` — đúng cách LINE gửi lại thật<br>4. Chờ hết một vòng job<br>5. Mở **cả hai** bản ghi callback: đọc cột mã sự kiện webhook và trạng thái cuối<br>6. Đếm: số tin trả lời friend nhận trên app LINE · số bản ghi hội thoại mới · số lần máy chủ nhận thử nhận được<br>7. So nội dung đã lưu của hai bản ghi — cờ báo gửi lại **không** được làm đổi cách lưu | Cùng một body (event tin nhắn `dup-test`) gửi 2 lần — lần 1 cờ báo gửi lại = `false`, lần 2 = `true`, **giữ nguyên mã sự kiện** | - Cả hai bản ghi có **cùng** mã sự kiện webhook, cột đó **không rỗng**<br>- Bản ghi thứ hai rơi trạng thái **bỏ qua do trùng**, không chạy nghiệp vụ<br>- Friend nhận **đúng 1** tin trả lời tự động, có **đúng 1** bản ghi hội thoại mới<br>- Máy chủ nhận thử nhận **đúng 1** lần — không forward thừa sang đối tác<br>- Nội dung lưu của 2 bản ghi giống nhau, cờ báo gửi lại không làm đổi cách lưu |  | Lấp `G2` `G19` `Q5` · RULE-08 · **Gộp từ `TC-INTGHOOK001-12`** (2026-09-16 — hai TC trùng ý định, chỉ khác ở chỗ có bật cờ hay không) · Đây là ca **cơ bản** của nhóm gửi lại; 3 ca còn lại: `-11` (đã DONE + đã forward) · `-13` (bản ghi có event báo đã gửi) · `-14` (gửi lại kèm event mới) · Đánh giá spec: Spec ghi rõ (REQ-003) · Evidence: log job + số đếm trước/sau ở bản ghi hội thoại + nhật ký máy chủ nhận thử (RULE-02 — không chấp nhận "thấy bình thường") |
| TC-INTGHOOK001-11 | API | INTG-HOOK-001 | Job callback — webhook gửi lại | Boundary | manual | product | Gửi lại sau khi bản ghi đầu đã xử lý xong và đã chuyển tiếp | - Như `TC-INTGHOOK001-10` (bot có trả lời tự động, đã bật chuyển tiếp Kênh 1 tới máy chủ nhận thử)<br>- **Khác `-10` ở chỗ**: bản ghi lần đầu đã **xử lý xong VÀ đã chuyển tiếp thành công** rồi mới gửi lại, và khoảng cách **2 phút** — để xem cơ chế chặn trùng có phụ thuộc cửa sổ thời gian ngắn hạn nào không | 1. Gửi webhook lần 1, chờ tới khi bản ghi đã xử lý xong và máy chủ nhận thử đã nhận<br>2. Chờ thêm 2 phút<br>3. Gửi lại **đúng body đó**, đổi cờ báo gửi lại thành `true`, **giữ nguyên mã sự kiện**<br>4. Chờ job xử lý, đếm lại: số tin trả lời friend nhận · số bản ghi hội thoại mới · số lần máy chủ nhận thử nhận được · trạng thái bản ghi thứ hai | Cùng nội dung, khoảng cách 2 phút (vượt cửa sổ chặn trùng ngắn hạn nếu có) | - Bản ghi thứ hai vẫn bị đánh **bỏ qua do trùng**<br>- Không phát sinh thêm kết quả nghiệp vụ<br>- Máy chủ nhận thử **không** nhận thêm lần nào |  | Lấp `G2` `Q5` · RULE-08 · Đánh giá spec: Đã hỏi leader (cửa sổ chặn trùng kéo dài bao lâu chưa có trong REQ) · Evidence: log job + nhật ký máy chủ nhận thử |
| TC-ENV003-10 | UI | ENV-003 | Môi trường — kiến trúc job production | Normal | manual | product | Trên production (ba job tách riêng), callback định dạng mới chạy xong ở từng job | - Đã triển khai đủ 2 nhánh lên production<br>- Bot thật có: 1 quy tắc trả lời tự động, 1 kịch bản, 1 thẻ gắn tự động, 1 rich menu | 1. Từ app LINE thật, gửi 1 tin text khớp từ khoá trả lời tự động<br>2. Gửi 1 tin kích hoạt kịch bản<br>3. Bấm 1 nút trên rich menu<br>4. Với mỗi lần: quan sát app LINE của friend, màn chat 1:1, danh sách thẻ của friend, trạng thái kịch bản<br>5. Kiểm tra bản ghi callback tương ứng đã xử lý xong<br>6. Đối chiếu Catalog D: xác nhận production đang chạy **ba job tách** chứ không phải một job gộp | 3 thao tác thật trên app LINE | - Cả 3 thao tác đều cho kết quả đúng ở app LINE của friend<br>- Chat 1:1 hiển thị đủ 3 tin<br>- Thẻ và kịch bản cập nhật đúng<br>- Cả 3 bản ghi callback đều ở trạng thái đã xử lý xong, nội dung lưu là định dạng mới |  | Lấp `G4` `Q4` · RULE-08 · regression · Đánh giá spec: Spec ghi rõ (Catalog D) · Evidence: ảnh chụp app LINE + chat 1:1 + bản ghi callback **lấy trên production** |
| TC-ENV003-11 | UI | ENV-003 | Môi trường — media trên production | Normal | manual | product | Ảnh và video gửi từ LINE thật tải được qua đường media của production | - Như TC-ENV003-10<br>- Biết đường dẫn media của production khác dev/staging (server media riêng, sau đó đồng bộ sang kho lưu trữ ngoài) | 1. Từ app LINE thật, friend gửi 1 ảnh và 1 video<br>2. Chờ job tải media chạy xong<br>3. Mở chat 1:1, xem trước và tải cả 2 tệp<br>4. Chờ qua mốc đồng bộ sang kho lưu trữ ngoài (theo Catalog D ~1 ngày), mở lại và tải lần nữa<br>5. Kiểm tra bản ghi callback của 2 tin | 1 ảnh JPG + 1 video MP4 gửi từ app LINE thật | - Cả 2 tệp tải được ngay sau khi gửi<br>- Sau mốc đồng bộ vẫn xem trước và tải được<br>- 2 bản ghi callback đi qua trạng thái chờ tải media rồi về đã xử lý xong, **không bản nào kẹt** |  | Lấp `G4` `Q4` · RULE-08 (media bắt buộc production) · Đánh giá spec: Spec ghi rõ (Catalog D) · Evidence: ảnh chụp chat 1:1 hai thời điểm + trạng thái bản ghi |
| TC-DATADB001-10 | Data | DATA-DB-001 | Dữ liệu callback — cách ly nhiều bot | Normal | manual | product | Callback của hai bot chạy song song không lẫn nghiệp vụ sang nhau | - Có 2 bot test A và B, **cùng tài khoản quản trị**, mỗi bot có 1 quy tắc trả lời tự động với **cùng từ khoá** `iso-test` nhưng nội dung trả lời khác nhau<br>- Bot A có sẵn 1 bản ghi định dạng cũ đang chờ, bot B có 1 bản ghi định dạng mới đang chờ | 1. Cho friend của bot A và friend của bot B cùng gửi `iso-test` trong vòng 5 giây<br>2. Chờ một vòng job<br>3. Kiểm tra friend A nhận nội dung nào, friend B nhận nội dung nào<br>4. Kiểm tra bản ghi callback của A và B: gắn bot nào, trạng thái nào<br>5. Kiểm tra chat 1:1 của cả 2 bot | Cùng từ khoá `iso-test` trên 2 bot, 2 định dạng bản ghi khác nhau | - Friend A nhận **đúng** nội dung của bot A, friend B nhận **đúng** nội dung của bot B<br>- Không friend nào nhận nội dung của bot kia<br>- Bản ghi callback gắn đúng bot, đều xử lý xong<br>- Chat 1:1 của mỗi bot chỉ có tin của friend bot đó |  | Lấp `G5` `Q8` `Q14` · RULE-07 · Đánh giá spec: Spec không ghi · Evidence: ảnh chụp app LINE 2 friend + bản ghi callback của cả 2 bot |
| TC-CONC001-10 | API | CONC-001 | Job callback — xử lý đồng thời | Abnormal | manual | product | Một webhook chỉ sinh đúng một kết quả nghiệp vụ dù job chạy nhiều luồng | - Bot có quy tắc trả lời tự động khớp `conc-test`<br>- Job đang chạy với cấu hình nhiều luồng như production<br>- Ghi nhận số bản ghi hội thoại và số lượt trả lời tự động trước khi bắt đầu | 1. Gửi **10 webhook khác nhau** (10 mã định danh sự kiện khác nhau) chứa `conc-test` trong vòng 2 giây<br>2. Chờ hết một vòng job<br>3. Đếm số bản ghi hội thoại mới và số lượt trả lời tự động mới<br>4. Đếm số tin friend thực nhận trên app LINE<br>5. Kiểm tra trạng thái 10 bản ghi callback | 10 webhook riêng biệt gửi dồn trong 2 giây | - Đúng **10** bản ghi hội thoại mới, **10** lượt trả lời tự động, friend nhận **10** tin<br>- Không nhân đôi, không thiếu<br>- Cả 10 bản ghi callback đều đã xử lý xong |  | Lấp `Q1` · RULE-08 · Đánh giá spec: Spec không ghi · Evidence: **số lần xử lý thực tế đếm từ log/DB** (RULE-02 bắt buộc với CONC-001) |
| TC-CONC001-11 | UI | CONC-001 | Job callback — friend gửi dồn tin | Abnormal | manual | product | Friend gửi nhiều tin liên tiếp rất nhanh — không xử lý song song, hành động không trùng | - Bot có quy tắc trả lời tự động có gắn thẻ và đổi kịch bản<br>- Friend thật trên app LINE | 1. Từ app LINE, friend gửi 5 tin khớp từ khoá liên tiếp **càng nhanh càng tốt** (< 1 giây/tin)<br>2. Chờ 2 phút<br>3. Đếm số tin trả lời friend nhận được<br>4. Kiểm tra danh sách thẻ của friend: thẻ có bị gắn trùng không<br>5. Kiểm tra trạng thái kịch bản của friend<br>6. Kiểm tra chat 1:1 | 5 tin text liên tiếp, cùng từ khoá | - Friend nhận **đúng 5** tin trả lời, không thừa không thiếu<br>- Thẻ chỉ được gắn **một lần** (không trùng bản ghi)<br>- Trạng thái kịch bản đúng, không nhảy bước<br>- Chat 1:1 hiển thị đủ 5 tin, đúng thứ tự |  | Lấp `Q1` · regression · dẫn từ `TC-RPL-226` (kho fa003, BR-22 concurrency lock) · RULE-08 · Đánh giá spec: Spec ghi rõ (BR-22) · Evidence: ảnh chụp app LINE + danh sách thẻ + log job |
| TC-JOB001-10 | API | JOB-001 | Job callback — đối chiếu số bản ghi vào/ra | Normal | manual | product | Số bản ghi nhận vào bằng số xử lý thành công cộng số vào hàng đợi lỗi | - Job đang chạy<br>- Ghi nhận tổng số bản ghi callback theo từng trạng thái **trước** khi bắt đầu | 1. Gửi 50 webhook hợp lệ + 5 webhook nội dung hỏng, xen kẽ nhau<br>2. Chờ job xử lý hết<br>3. Đếm lại tổng số bản ghi theo từng trạng thái<br>4. Lập bảng: số vào (55) so với số đã xử lý xong + số lỗi + số không xác định loại<br>5. Đọc log job tìm dấu hiệu thử lại và giãn cách giữa các lần thử | 50 webhook hợp lệ + 5 webhook hỏng | - **55 = số đã xử lý xong + số lỗi + số không xác định loại** — không bản ghi nào biến mất<br>- 5 bản hỏng đều có trạng thái lỗi<br>- Log có ghi số lần thử lại và giãn cách giữa các lần |  | Lấp `Q2` · RULE-08 · Đánh giá spec: Spec không ghi — REQ chưa mô tả chính sách thử lại · Evidence: **bảng đối chiếu số bản ghi vào/ra** + log job thấy thử lại (RULE-02) |
| TC-JOB001-11 | API | JOB-001 | Job callback — tải lớn liên tục | Boundary | manual | product | Lượng webhook lớn liên tục vượt ngưỡng — job không dừng | - Như TC-JOB001-10<br>- Đã hỏi Dev ngưỡng xử lý dự kiến của job, ghi nguồn vào evidence | 1. Gửi liên tục 500 webhook hợp lệ trong 60 giây<br>2. Theo dõi job trong suốt quá trình và 10 phút sau đó<br>3. Đếm số bản ghi đã xử lý xong sau khi kết thúc<br>4. Kiểm tra job còn sống và còn nhận bản ghi mới không: gửi thêm 1 webhook và xem có xử lý không<br>5. Kiểm tra luồng tải media vẫn chạy: gửi 1 tin ảnh sau đợt dồn | 500 webhook trong 60 giây + 1 webhook kiểm chứng + 1 tin ảnh | - Job **không dừng**, không cần khởi động lại thủ công<br>- 500 bản ghi đều về trạng thái cuối (xử lý xong hoặc lỗi có mô tả), không bản nào kẹt<br>- Webhook kiểm chứng sau đó vẫn xử lý bình thường<br>- Tin ảnh vẫn tải được media (**không lặp lại bug #40896**) |  | Lấp `Q2` `G4` · RULE-08 · Đánh giá spec: Spec không ghi · Evidence: log job + bảng số bản ghi theo trạng thái theo thời gian |
| TC-JOB001-12 | UI | JOB-001 | Job callback — tin nhắn từ nhóm | Abnormal | manual | Tất cả | Tin nhắn gửi từ nhóm với định dạng mới vẫn bị bỏ qua đúng như trước | - Bot đã được thêm vào 1 nhóm LINE<br>- Bot có quy tắc trả lời tự động khớp từ khoá `group-test`<br>- Bản sửa đã được triển khai (bản ghi sinh ra ở định dạng mới) | 1. Trong nhóm LINE, gửi tin chứa `group-test`<br>2. Chờ một vòng job<br>3. Kiểm tra trạng thái bản ghi callback vừa sinh<br>4. Kiểm tra trong nhóm có tin trả lời tự động nào không<br>5. Kiểm tra chat 1:1 có phát sinh hội thoại lạ không | Tin `group-test` gửi trong nhóm LINE | - Bản ghi callback ở trạng thái **bỏ qua tin nhắn nhóm**, không phải lỗi<br>- **Không** có tin trả lời tự động nào trong nhóm<br>- Không phát sinh hội thoại mới ở chat 1:1<br>- Nội dung bản ghi lưu ở định dạng mới |  | Lấp `G6` · regression · dẫn từ `TC-RPL-224` (kho fa003) · Đánh giá spec: Spec ghi rõ (job-spec — trạng thái bỏ qua tin nhóm) · Evidence: ảnh chụp nhóm LINE + trạng thái bản ghi |
| TC-JOB001-13 | Data | JOB-001 | Job callback — bot hết hạn | Abnormal | manual | Tất cả | Bot hết hạn quá 7 ngày — callback định dạng mới vẫn bị chặn đúng như trước | - Có bot test với hợp đồng đã hết hạn quá 7 ngày<br>- Bot đó có quy tắc trả lời tự động<br>- Bản sửa đã được triển khai | 1. Cho friend của bot hết hạn gửi 1 tin khớp từ khoá<br>2. Chờ một vòng job<br>3. Kiểm tra trạng thái bản ghi callback<br>4. Kiểm tra friend có nhận được tin trả lời không<br>5. Kiểm tra nội dung bản ghi là định dạng nào | 1 tin text khớp từ khoá, trên bot hết hạn > 7 ngày | - Bản ghi ở trạng thái **bot hết hạn**, không phải lỗi<br>- Friend **không** nhận được tin trả lời<br>- Nội dung bản ghi vẫn được lưu ở định dạng mới (việc chặn xảy ra ở tầng nghiệp vụ, không phải tầng đọc định dạng) |  | Lấp `G6` · regression · dẫn từ `TC-RPL-225` (kho fa003) · Đánh giá spec: Spec ghi rõ (job-spec — trạng thái bot hết hạn) · Evidence: trạng thái bản ghi + ảnh chụp app LINE friend |
| TC-INTGLINE001-10 | UI | INTG-LINE-001 | Job callback — ma trận loại tin nhắn | Normal | manual | product | Bảy loại tin nhắn từ LINE thật đều xử lý xong với định dạng mới | - Bot thật có 1 quy tắc trả lời tự động dạng 全てのメッセージ<br>- Friend thật trên app LINE<br>- Bản sửa đã triển khai | 1. Friend lần lượt gửi 7 loại: text · sticker · ảnh · video · âm thanh · tệp · vị trí<br>2. Với mỗi loại: quan sát app LINE (có nhận trả lời không), chat 1:1 (hiển thị đúng loại không), trạng thái bản ghi callback<br>3. Với ảnh/video/âm thanh/tệp: xem trước và tải về từ chat 1:1<br>4. Lập bảng 7 dòng ghi kết quả từng loại | 7 loại tin nhắn gửi từ app LINE thật | - Cả 7 loại đều hiển thị đúng ở chat 1:1<br>- Cả 7 đều nhận được trả lời tự động<br>- Cả 7 bản ghi callback đều đã xử lý xong, nội dung định dạng mới<br>- 4 loại có tệp đều tải về được |  | Lấp `G6` `I10` · regression · dẫn từ `TC-RPL-220` / `TC-RPL-221` (kho fa003) · RULE-06 (kiểm tại app LINE thật) · RULE-08 · Đánh giá spec: Spec ghi rõ · Evidence: bảng 7 dòng + ảnh chụp app LINE và chat 1:1 |
| TC-DATATEXT001-10 | API | DATA-TEXT-001 | Dữ liệu callback — ký tự đặc biệt | Boundary | manual | product | Ký tự cần thoát và emoji ghép đôi giữ nguyên suốt chuỗi xử lý | - Bot đã bật chuyển tiếp Kênh 1 tới máy chủ nhận thử đọc được nội dung<br>- Friend thật trên app LINE | 1. Friend gửi lần lượt 5 tin ở cột Dữ liệu nhập<br>2. Với mỗi tin: đọc chuỗi đã lưu ở bản ghi callback, so **từng ký tự** với tin gốc<br>3. Kiểm tra hiển thị ở chat 1:1<br>4. Kiểm tra nội dung máy chủ nhận thử nhận được<br>5. Kiểm tra tin trả lời tự động hiển thị trên app LINE của friend | 1. `テスト"引用"` (dấu nháy kép)<br>2. `C:\path\to\file` (dấu chéo ngược)<br>3. Tin 3 dòng (xuống dòng)<br>4. 👨‍👩‍👧‍👦🇯🇵 (emoji ghép đôi + cờ)<br>5. `絵文字😀と日本語` | - Chuỗi lưu **khớp từng ký tự** với tin gốc, **không** xuất hiện dạng mã `\u`<br>- Chat 1:1 hiển thị đúng cả 5 tin<br>- Máy chủ nhận thử nhận đúng ký tự thật, không bị thoát<br>- Emoji ghép đôi **không** bị tách thành 2 ký tự rời |  | Lấp `Q13` · RULE-06 · RULE-08 · Đánh giá spec: Spec ghi rõ (REQ-002) · Evidence: so sánh chuỗi từng ký tự + ảnh chụp app LINE + nội dung máy chủ nhận thử |
| TC-DATAMIG001-10 | Data | DATA-MIG-001 | Dữ liệu callback — mẫu bản ghi cũ nhiều thời điểm | Boundary | manual | Tất cả | Lấy mẫu bản ghi cũ ở nhiều trạng thái và nhiều thời điểm — đều không bị đổi | - Hệ thống có bản ghi cũ ở nhiều trạng thái: chờ xử lý · đã xử lý xong · lỗi · chờ tải media<br>- Có bản ghi cũ từ ít nhất 3 mốc thời gian khác nhau (cũ nhất, giữa, gần nhất trước triển khai) | 1. Trước triển khai: ghi lại nội dung và trạng thái của 12 bản ghi mẫu (4 trạng thái × 3 mốc thời gian)<br>2. Triển khai bản sửa<br>3. Chạy job một vòng đầy đủ<br>4. Đọc lại đúng 12 bản ghi đó, so nội dung và trạng thái với bước 1<br>5. Riêng nhóm chờ xử lý và chờ tải media: theo dõi tới trạng thái cuối | 12 bản ghi mẫu (4 trạng thái × 3 mốc thời gian) | - Nội dung 12 bản ghi **không đổi một ký tự** — không có bất kỳ chuyển đổi dữ liệu nào<br>- Nhóm đã xử lý xong và lỗi: trạng thái giữ nguyên<br>- Nhóm chờ xử lý và chờ tải media: tiếp tục chạy tới trạng thái cuối đúng như trước |  | Lấp `Q7` (bổ sung Boundary cho RULE-01) · regression · Đánh giá spec: Spec ghi rõ (REQ-004 + REQ-009) · Evidence: bảng đối chiếu 12 bản ghi trước/sau triển khai |
| TC-MSG004-01 | UI | MSG-004 | Job callback — bản ghi gộp nhiều event | Normal | manual | product | Một lần LINE gọi chở 3 tin — job xử lý đủ cả 3, không rơi event thứ ba | - Bot thật có quy tắc trả lời tự động dạng 全てのメッセージ<br>- Friend thật trên app LINE<br>- Ghi nhận số bản ghi hội thoại và số lượt trả lời tự động trước khi bắt đầu | 1. Friend gửi 3 tin text liên tiếp thật nhanh để LINE gộp vào **một** lần gọi<br>2. Kiểm tra số bản ghi callback vừa sinh (kỳ vọng **1**, không phải 3)<br>3. Đọc nội dung bản ghi: đếm số event trong danh sách events<br>4. Chờ job xử lý xong<br>5. Đếm số tin trả lời friend nhận trên app LINE, số tin hiển thị ở chat 1:1, số lượt trả lời tự động | 3 tin text gửi liên tiếp < 1 giây/tin | - Sinh **đúng 1** bản ghi callback, bên trong có **đủ 3** event<br>- Friend nhận **đúng 3** tin trả lời tự động<br>- Chat 1:1 hiển thị **đủ 3** tin, đúng thứ tự<br>- **Không** event nào bị bỏ (đặc biệt event thứ 3) |  | Lấp `G12` `Q15` · regression · RULE-06 · RULE-08 · dẫn từ `NEW-43` / `NEW-2` (Studio task #299 — #40888) · Đánh giá spec: Spec ghi rõ · Evidence: nội dung bản ghi (đếm event) + ảnh chụp app LINE + chat 1:1 |
| TC-MSGUSER001-01 | UI | MSG-USER-001 | Job callback — event đại diện của bản ghi gộp | Normal | manual | product | Bản ghi mở đầu bằng event báo đã gửi — kết bạn ghi nhận đúng người | - Bot thật có action khi kết bạn (gắn thẻ + gửi tin chào)<br>- Chuẩn bị được lần gọi LINE chứa `[delivery của friend A, follow của friend B]` (gửi tin cho A ngay trước khi B kết bạn, hoặc dựng nội dung webhook tương đương)<br>- Friend B **chưa** từng kết bạn | 1. Tạo tình huống để một lần LINE gọi chở cả event báo đã gửi của friend A và event kết bạn của friend B<br>2. Đọc bản ghi callback: cột loại và cột định danh bạn bè đang ghi của ai<br>3. Chờ job xử lý<br>4. Kiểm tra thẻ và tin chào: rơi vào friend A hay friend B<br>5. Kiểm tra danh sách bạn bè: ai được ghi nhận kết bạn mới | 1 lần gọi chứa 2 event: `delivery` (friend A) + `follow` (friend B) | - Cột loại và định danh bạn bè của bản ghi ghi theo **friend B** (event đầu tiên khác `delivery`)<br>- Thẻ và tin chào gửi cho **friend B**, **không** cho friend A<br>- Friend A **không** bị ghi nhận kết bạn lại<br>- Bản ghi về trạng thái đã xử lý xong |  | Lấp `G13` `Q16` · RULE-08 · dẫn từ `NEW-44` / `NEW-47` / `NEW-17` (task #299 — #40888) · Đánh giá spec: Spec ghi rõ (#40888 mục 2) · Evidence: nội dung bản ghi + danh sách thẻ 2 friend + ảnh chụp app LINE |
| TC-DATA001-01 | UI | DATA-001 | Job callback — trường dẫn xuất sau bản ghi gộp | Normal | manual | product | Tin nhắn cuối, bộ đếm chưa xác nhận và thứ tự danh sách bạn bè đúng sau một lần gọi gộp | - Bot thật, friend A đang **không** đứng đầu danh sách bạn bè<br>- Ghi nhận bộ đếm chưa xác nhận hiện tại của friend A<br>- Tắt tự động xác nhận để bộ đếm tăng quan sát được | 1. Friend A gửi 3 tin liên tiếp để LINE gộp vào một lần gọi<br>2. Chờ job xử lý xong<br>3. Mở màn danh sách bạn bè: xem vị trí friend A và nội dung tin nhắn cuối hiển thị<br>4. Đối chiếu thời điểm tin nhắn cuối với thời điểm tin **thứ 3**<br>5. Đọc bộ đếm chưa xác nhận của friend A<br>6. Kiểm tra thông báo trên app quản trị mobile / PC / Chatwork | 3 tin text, nội dung khác nhau, tin thứ 3 là `tin-cuoi-cung` | - Tin nhắn cuối hiển thị đúng nội dung **tin thứ 3**, không phải tin thứ 1<br>- Thời điểm tin nhắn cuối khớp tin thứ 3<br>- Friend A nhảy lên **đầu** danh sách bạn bè<br>- Bộ đếm chưa xác nhận tăng **đúng 3**<br>- Thông báo mobile / PC / Chatwork **không mất**, phản ánh đủ số tin |  | Lấp `G16` `Q18` `Q19` · regression · RULE-06 · RULE-08 · dẫn từ `NEW-59` / `NEW-46` / `NEW-58` (task #299 — #40888) · Đánh giá spec: Spec không ghi · Evidence: ảnh chụp danh sách bạn bè + bộ đếm + thông báo trên cả 3 kênh |
| TC-REGSHARED001-10 | Data | REG-SHARED-001 | Vận hành — hai bộ tiêu thụ cùng quét bảng callback | Normal | manual | product | Ba tiến trình PHP cũ và job Java không cùng xử lý một bản ghi | - Xác nhận trước với Dev/vận hành: 3 tiến trình `HandleCallback` · `HandleCallbackMessage` · `HandleCallbackPostback` đang **bật hay đã dừng** trên môi trường đo<br>- Bot có quy tắc trả lời tự động<br>- Ghi nhận số bản ghi hội thoại và số lượt trả lời tự động trước khi bắt đầu | 1. Ghi lại trạng thái bật/tắt của 3 tiến trình PHP và của job Java<br>2. Cho friend gửi 1 tin khớp từ khoá<br>3. Chờ 2 phút<br>4. Đếm số tin trả lời friend nhận, số bản ghi hội thoại mới, số lượt trả lời tự động mới<br>5. Nếu 3 tiến trình PHP đang bật: lặp lại với 1 bản ghi chứa **2 event cùng loại** và đếm số event được xử lý | 1 tin khớp từ khoá + (nếu PHP còn chạy) 1 bản ghi 2 event cùng loại | - Friend nhận **đúng 1** tin trả lời, **đúng 1** bản ghi hội thoại — không nhân đôi<br>- Nếu 3 tiến trình PHP còn bật: phải chứng minh được **không** có bản ghi nào bị xử lý 2 lần, và bản ghi 2 event cùng loại được xử lý **đủ 2** event<br>- Nếu không chứng minh được ⇒ kết luận **bắt buộc dừng hẳn 3 tiến trình PHP** trước khi phát hành |  | Lấp `G14` · RULE-08 · nguồn cảnh báo: #40888 journal #136271 (*"cần xác nhận đã dừng hẳn"*) · Đánh giá spec: **Đã hỏi leader** — chưa tài liệu nào ghi 3 tiến trình PHP còn chạy hay không · Evidence: ảnh chụp trạng thái tiến trình + số đếm trước/sau + log job |
| TC-COMPATLEGACY001-12 | Data | COMPAT-LEGACY-001 | Job callback — callback nội bộ tự sinh | Normal | manual | Tất cả | Bản ghi callback do hệ thống tự sinh (không phải LINE gửi) vẫn được job xử lý đúng | - Biết thao tác nào trên màn quản trị sinh callback nội bộ (gửi postback giả lập từ màn chi tiết bạn bè)<br>- Job đang chạy | 1. Thực hiện thao tác sinh callback nội bộ trên màn quản trị<br>2. Đọc bản ghi callback vừa sinh: định dạng chuỗi (`{` hay `[`), cột mã sự kiện webhook có giá trị không<br>3. Chờ job xử lý<br>4. Kiểm tra trạng thái bản ghi và kết quả nghiệp vụ (action của nút bấm có chạy không)<br>5. Lặp lại thao tác đó **lần thứ hai** để kiểm chứng không bị chặn nhầm là trùng | 2 lượt postback giả lập cùng nội dung | - Cả 2 bản ghi đều xử lý xong, action chạy **đủ 2 lần**<br>- Cột mã sự kiện webhook để **trống** (callback nội bộ không có mã) và **không** vì thế mà bị đánh trùng<br>- Job không rơi trạng thái lỗi với định dạng do màn quản trị sinh |  | Lấp `G17` · regression · dẫn từ `NEW-19` / `NEW-21` / `NEW-60` (task #299 — #40888) · Đánh giá spec: Spec ghi rõ (#40888 mục 3 — `storeFakeCallbackEvent` giữ nguyên) · Evidence: nội dung 2 bản ghi + kết quả action |
| TC-DEPLOYLIVE001-04 | API | DEPLOY-LIVE-001 | Triển khai — hai writer sinh cùng một định dạng | Normal | manual | product | Bản ghi do PHP ghi và do Java ghi được job đọc như nhau | - Dev/vận hành đã xác nhận môi trường nào dùng writer PHP, môi trường nào dùng writer Java (§6 mục 7)<br>- Cả 2 nhánh #40888 và #40889 đã triển khai | 1. Trên đường **PHP**: cho friend gửi 1 tin text tiếng Nhật + emoji, lưu lại chuỗi đã ghi<br>2. Trên đường **Java**: gửi **cùng nội dung đó**, lưu lại chuỗi đã ghi<br>3. So **từng ký tự** hai chuỗi: lớp bao ngoài, thứ tự khoá, ký tự tiếng Nhật/emoji, cột mã sự kiện webhook<br>4. Để job xử lý cả 2 bản ghi, so kết quả nghiệp vụ hai bên<br>5. Đặt 2 bản ghi (1 của PHP, 1 của Java) cùng chờ trong một lượt job rồi chạy | Cùng 1 nội dung tin `テスト😀 両writer` qua 2 đường ghi | - Hai chuỗi **giống nhau về cấu trúc**: cùng có lớp bao ngoài, cùng giữ tiếng Nhật/emoji nguyên dạng, cùng thứ tự khoá<br>- Cột mã sự kiện webhook: nếu hai bên **khác nhau** (một bên ghi, một bên trống) ⇒ ghi nhận thành điểm cần chốt, vì cơ chế chặn trùng chỉ chạy cho một nửa lưu lượng<br>- Job xử lý cả 2 bản ghi tới đã xử lý xong, kết quả nghiệp vụ giống nhau |  | Lấp `G18` `G1` `G2` · RULE-08 · dẫn từ `NEW-41` / `NEW-42` (task #299 — #40888, **cả 2 đang chưa chạy**) · Đánh giá spec: **Đã hỏi leader** (xem §6 mục 1 + 7) · Evidence: 2 chuỗi đặt cạnh nhau so từng ký tự + trạng thái 2 bản ghi |
| TC-MSG004-02 | API | MSG-004 | Job callback — bản ghi gộp khác loại event | Normal | manual | product | Một bản ghi chở hai loại event khác nhau — mỗi loại chạy đúng nhánh của nó | - Bot test có: 1 quy tắc trả lời tự động khớp `mix-test`, 1 nút bấm có action gắn thẻ<br>- Ghi nhận số lượt trả lời tự động và danh sách thẻ của friend trước khi bắt đầu | 1. Dựng một nội dung webhook chứa 2 event theo thứ tự: tin nhắn `mix-test` rồi tới thao tác bấm nút<br>2. Gửi tới đường nhận callback của bot<br>3. Xác nhận chỉ sinh **1** bản ghi callback<br>4. Chờ job xử lý xong<br>5. Kiểm tra: friend có nhận tin trả lời tự động không, thẻ của nút bấm có được gắn không<br>6. Đọc cột loại của bản ghi và đối chiếu với kết quả nghiệp vụ | 1 webhook chứa `[message "mix-test", postback nút gắn thẻ]` | - Sinh **đúng 1** bản ghi callback<br>- **Cả hai** nghiệp vụ đều chạy: friend nhận tin trả lời tự động **và** thẻ được gắn<br>- Không nghiệp vụ nào bị bỏ vì cột loại của bản ghi chỉ ghi được **một** loại<br>- Bản ghi về trạng thái đã xử lý xong |  | Lấp `G12` `Q15` · RULE-08 · dẫn từ `NEW-4` / `NEW-25` (task #299 — #40888) · Đánh giá spec: Spec ghi rõ (#40888 — job dispatch theo type của TỪNG event) · Evidence: nội dung bản ghi + số lượt trả lời + danh sách thẻ trước/sau |
| TC-CONC001-12 | API | CONC-001 | Job callback — bản ghi gộp nhiều người | Normal | manual | product | Một bản ghi chứa event của hai bạn bè — người thứ hai không bị bỏ sót | - Bot test có quy tắc trả lời tự động dạng 全てのメッセージ<br>- Có 2 friend A và B đều đã kết bạn<br>- Ghi nhận bản ghi hội thoại của cả A và B trước khi bắt đầu | 1. Dựng một nội dung webhook chứa 2 event tin nhắn: 1 của friend A, 1 của friend B<br>2. Gửi tới đường nhận callback<br>3. Chờ job xử lý xong<br>4. Kiểm tra app LINE của **cả hai** friend: ai nhận được tin trả lời<br>5. Kiểm tra chat 1:1: hội thoại của A và của B có đủ tin không<br>6. Đọc cột định danh bạn bè của bản ghi — ghi của ai | 1 webhook chứa `[message friend A, message friend B]` | - **Cả hai** friend đều nhận tin trả lời tự động<br>- Chat 1:1 của **cả hai** đều có tin mới<br>- Friend B **không** bị bỏ qua dù cột định danh bạn bè của bản ghi chỉ ghi được **một** người<br>- Không tin nào bị gán nhầm sang hội thoại của người kia |  | Lấp `G12` `Q1` `Q16` · RULE-08 · dẫn từ `NEW-48` (task #299 — #40888, *"không bị bỏ sót do khoá theo bạn bè"*) · Đánh giá spec: Spec không ghi · Evidence: ảnh chụp app LINE 2 friend + chat 1:1 cả 2 hội thoại + log job |
| TC-JOB001-14 | API | JOB-001 | Job callback — bản ghi gộp tin nhóm và tin 1:1 | Abnormal | manual | product | Bản ghi chứa cả tin nhóm lẫn tin 1:1 — tin 1:1 không bị bỏ theo trạng thái bỏ qua tin nhóm | - Bot đã được thêm vào 1 nhóm LINE<br>- Bot có quy tắc trả lời tự động khớp `grp-mix`<br>- Ghi nhận hội thoại 1:1 của friend A trước khi bắt đầu | 1. Dựng một nội dung webhook chứa 2 event: tin nhắn **từ nhóm** đứng trước, tin nhắn **1:1 của friend A** đứng sau<br>2. Gửi tới đường nhận callback<br>3. Chờ job xử lý xong<br>4. Đọc trạng thái cuối của bản ghi<br>5. Kiểm tra friend A có nhận tin trả lời không, chat 1:1 có tin mới không<br>6. Kiểm tra trong nhóm **không** có tin trả lời nào | 1 webhook chứa `[message từ group, message 1:1 của friend A]` | - Tin 1:1 của friend A **vẫn được xử lý**: friend nhận tin trả lời, chat 1:1 có tin mới<br>- Tin nhóm vẫn bị bỏ qua (không trả lời trong nhóm)<br>- Bản ghi **không** bị đánh trạng thái bỏ qua tin nhóm cho **cả** bản ghi — nếu bị thì tin 1:1 mất |  | Lấp `G12` `G6` · RULE-08 · dẫn từ `NEW-49` (task #299 — #40888) · Đánh giá spec: Spec không ghi — job-spec chỉ mô tả trạng thái bỏ qua tin nhóm ở mức bản ghi · Evidence: trạng thái bản ghi + ảnh chụp nhóm LINE + chat 1:1 |
| TC-JOB001-15 | API | JOB-001 | Job callback — bot hết hạn với bản ghi gộp | Abnormal | manual | Tất cả | Bot hết hạn hợp đồng — toàn bộ event trong bản ghi gộp dừng đúng, không xử lý một phần | - Có bot test với hợp đồng đã hết hạn quá 7 ngày<br>- Bot đó có quy tắc trả lời tự động và action gắn thẻ | 1. Dựng một nội dung webhook chứa 3 event khác loại (tin nhắn · bấm nút · kết bạn) cho bot hết hạn<br>2. Gửi tới đường nhận callback<br>3. Chờ job xử lý<br>4. Đọc trạng thái cuối của bản ghi<br>5. Kiểm tra **từng** nghiệp vụ: có tin trả lời không, thẻ có được gắn không, friend có bị ghi nhận kết bạn không | 1 webhook chứa `[message, postback, follow]` trên bot hết hạn > 7 ngày | - Bản ghi ở trạng thái **bot hết hạn**<br>- **Không** nghiệp vụ nào chạy — không tin trả lời, không gắn thẻ, không ghi nhận kết bạn<br>- **Tuyệt đối không** có chuyện event đầu bị chặn còn event sau vẫn chạy (xử lý một phần) |  | Lấp `G12` `G6` · dẫn từ `NEW-50` (task #299 — #40888) · Đánh giá spec: Spec ghi rõ (job-spec — trạng thái bot hết hạn) · Evidence: trạng thái bản ghi + ảnh chụp 3 đích nghiệp vụ đều không đổi |
| TC-FUNC004-11 | API | FUNC-004 | Job callback — số event tối đa trong một bản ghi | Boundary | manual | product | Bản ghi chở số event ở mức biên vẫn được xử lý đủ, không rơi event cuối | - Đã hỏi Dev/LINE số event tối đa LINE gộp trong một lần gọi, ghi nguồn vào evidence<br>- Bot có quy tắc trả lời tự động dạng 全てのメッセージ | 1. Dựng 3 nội dung webhook với số event: 2 · 10 · mức tối đa theo tài liệu LINE<br>2. Gửi lần lượt từng nội dung<br>3. Với mỗi lần: đếm số event trong bản ghi đã lưu so với số đã gửi<br>4. Chờ job xử lý, đếm số tin trả lời friend nhận được<br>5. Đối chiếu: số event gửi = số event lưu = số kết quả nghiệp vụ | 3 webhook: 2 event · 10 event · số event tối đa theo tài liệu LINE | - Với cả 3 mức: **số event gửi = số event lưu = số kết quả nghiệp vụ**<br>- Không rơi event nào, đặc biệt event **cuối cùng**<br>- Bản ghi không bị cắt cụt nội dung ở mức tối đa (sức chứa cột thuộc `G7` — Dev xác nhận, xem cuối §5) |  | Lấp `G12` `Q12` · RULE-08 · dẫn từ `NEW-39` (task #299 — #40888) · Đánh giá spec: **Đã hỏi leader** — chưa REQ nào ghi số event tối đa · Evidence: nguồn giới hạn + bảng 3 dòng đối chiếu gửi/lưu/xử lý |
| TC-FUNC003-01 | API | FUNC-003 | Job callback — callback chỉ gồm event báo đã gửi | Abnormal | manual | Tất cả | Lần gọi chỉ chứa event báo đã gửi — không sinh bản ghi, job không có gì để xử lý | - Bot test đang hoạt động bình thường<br>- Ghi lại mốc bản ghi callback hiện có của bot và trạng thái xác thực webhook của bot | 1. Gửi 1 nội dung webhook chỉ chứa **một** event loại báo đã gửi (`delivery`)<br>2. Gửi tiếp 1 nội dung chỉ chứa **hai** event đều là báo đã gửi<br>3. Đếm số bản ghi callback mới sinh sau cả 2 lần<br>4. Kiểm tra trạng thái xác thực webhook của bot có bị đổi không<br>5. Kiểm tra job có báo lỗi gì trong log không | 2 webhook: 1 event `delivery` · 2 event `delivery` | - Cả 2 lần đều phản hồi thành công<br>- **Không** bản ghi callback mới nào được tạo<br>- Trạng thái xác thực webhook của bot **giữ nguyên** (không bị nhầm sang nhánh LINE xác thực đường dẫn)<br>- Job không ghi lỗi nào |  | Lấp `G13` `Q17` · dẫn từ `NEW-6` (task #299 — #40888) · Đánh giá spec: Spec ghi rõ (#40888 — *"bỏ qua request chỉ có sự kiện loại delivery"*) · Evidence: số bản ghi trước/sau + giá trị cột xác thực + log job |
| TC-FUNC003-02 | Data | FUNC-003 | Job callback — không có event khớp cột loại | Abnormal | manual | Tất cả | Bản ghi mà cột loại không khớp event nào — job vẫn lấy đúng event nghiệp vụ đầu tiên | - Có quyền tạo bản ghi callback thủ công<br>- Job đang chạy | 1. Tạo 1 bản ghi callback có cột loại ghi `postback` nhưng nội dung chỉ chứa event tin nhắn<br>2. Tạo 1 bản ghi có cột loại ghi `message` nhưng nội dung chỉ chứa event kết bạn<br>3. Chờ job xử lý<br>4. Kiểm tra trạng thái 2 bản ghi<br>5. Kiểm tra nghiệp vụ nào thực sự chạy: theo cột loại hay theo nội dung event | 2 bản ghi có cột loại **lệch** với nội dung event | - Job xử lý theo **nội dung event thật**, không theo cột loại<br>- Bản ghi 1: nghiệp vụ tin nhắn chạy<br>- Bản ghi 2: nghiệp vụ kết bạn chạy<br>- **Không** bản ghi nào rơi trạng thái lỗi hay bị bỏ qua chỉ vì cột loại lệch |  | Lấp `G13` `Q17` · dẫn từ `NEW-23` (task #299 — #40888, *fallback lấy event nghiệp vụ đầu tiên*) · Đánh giá spec: Spec ghi rõ (#40888 mục 2 — thứ tự ưu tiên chọn event đại diện) · Evidence: trạng thái 2 bản ghi + kết quả nghiệp vụ tương ứng |
| TC-MSG004-03 | API | MSG-004 | Job callback — thao tác bấm nút sau event báo đã gửi | Normal | manual | product | Bản ghi mở đầu bằng event báo đã gửi — thao tác bấm nút của khách không bị mất | - Bot có nút bấm (rich menu hoặc button template) gắn action gửi tin + gắn thẻ<br>- Ghi nhận danh sách thẻ của friend và số tin đã nhận trước khi bắt đầu | 1. Dựng 1 nội dung webhook chứa `[báo đã gửi, thao tác bấm nút của friend A]`<br>2. Gửi tới đường nhận callback<br>3. Đọc cột loại của bản ghi — kỳ vọng ghi theo thao tác bấm nút, không phải báo đã gửi<br>4. Chờ job xử lý<br>5. Kiểm tra friend A: có nhận tin của action không, thẻ có được gắn không | 1 webhook chứa `[delivery, postback của friend A]` | - Cột loại bản ghi ghi theo **thao tác bấm nút**, không phải báo đã gửi<br>- Action chạy đủ: friend A nhận tin **và** được gắn thẻ<br>- **Không mất** thao tác bấm nút của khách — đây đúng là rủi ro #40888 nêu |  | Lấp `G13` `Q15` · RULE-06 · RULE-08 · dẫn từ `NEW-44` / `NEW-45` (task #299 — #40888) · Đánh giá spec: Spec ghi rõ (#40888 mục 2) · Evidence: cột loại bản ghi + ảnh chụp app LINE + danh sách thẻ trước/sau |
| TC-REGSHARED001-11 | Data | REG-SHARED-001 | Vận hành — bản ghi nhiều event cùng loại | Normal | manual | product | Bản ghi chứa nhiều event CÙNG loại — không bị tiến trình PHP cũ xử lý thiếu | - Đã biết trạng thái bật/tắt của 3 tiến trình PHP (kết quả `TC-REGSHARED001-10`)<br>- Bot có quy tắc trả lời tự động dạng 全てのメッセージ | 1. Dựng 1 nội dung webhook chứa **3 event cùng loại tin nhắn** của cùng 1 friend<br>2. Gửi tới đường nhận callback<br>3. Chờ xử lý xong<br>4. Đếm số tin trả lời friend nhận, số tin ở chat 1:1<br>5. Lặp lại kịch bản này **hai lần**: một lần với 3 tiến trình PHP **bật**, một lần **tắt hẳn**, rồi so kết quả 2 lần | 1 webhook chứa 3 event `message` cùng friend | - Khi 3 tiến trình PHP **tắt**: friend nhận đủ **3** tin trả lời, chat 1:1 đủ **3** tin<br>- Khi 3 tiến trình PHP **bật**: kết quả phải **giống hệt** — nếu chỉ nhận 1 tin ⇒ chứng minh tiến trình PHP cũ nuốt mất event 2 và 3, **bắt buộc dừng hẳn** trước khi phát hành |  | Lấp `G14` `G12` · RULE-08 · nguồn: #40888 journal #136271 (*"nếu 1 row có NHIỀU event CÙNG loại thì các event sau vẫn chưa được các tiến trình này xử lý"*) · Đánh giá spec: **Đã hỏi leader** · Evidence: bảng so 2 lượt (PHP bật / tắt) + ảnh chụp app LINE |
| TC-INTGLINE001-11 | UI | INTG-LINE-001 | Job callback — ba nguồn thao tác bấm nút | Normal | manual | product | Rich menu · button template · image map — cả ba nguồn đều thực thi action | - Bot thật có: 1 rich menu có nút gắn action, 1 button template có nút gắn action, 1 image map có vùng bấm gắn action<br>- Mỗi action gắn thẻ khác nhau để phân biệt nguồn | 1. Từ app LINE, friend bấm nút trên **rich menu** → quan sát kết quả<br>2. Friend bấm nút trên **button template** → quan sát<br>3. Friend bấm vùng trên **image map** → quan sát<br>4. Với mỗi lần: đọc bản ghi callback (định dạng, trạng thái cuối), kiểm thẻ tương ứng đã gắn chưa<br>5. Lập bảng 3 dòng ghi kết quả từng nguồn | 3 thao tác bấm từ 3 nguồn khác nhau trên app LINE thật | - Cả **3** nguồn đều sinh bản ghi định dạng mới và chạy tới trạng thái đã xử lý xong<br>- **3** thẻ tương ứng đều được gắn đúng<br>- Không nguồn nào bị bỏ sót |  | Lấp `G15` · regression · RULE-06 · RULE-08 · dẫn từ `NEW-56` (task #299 — #40888) · Đánh giá spec: Spec ghi rõ · Evidence: bảng 3 dòng + ảnh chụp app LINE + danh sách thẻ |
| TC-INTGLINE001-12 | UI | INTG-LINE-001 | Job callback — bot vào và rời nhóm | Normal | manual | Tất cả | Sự kiện bot được thêm vào nhóm và bị gỡ khỏi nhóm xử lý đúng với định dạng mới | - Có 1 nhóm LINE test<br>- Bot **chưa** ở trong nhóm đó<br>- Bản sửa đã triển khai | 1. Thêm bot vào nhóm LINE → đọc bản ghi callback vừa sinh (loại, định dạng, trạng thái cuối)<br>2. Kiểm tra thông tin nhóm có được ghi nhận ở màn quản trị không<br>3. Gỡ bot khỏi nhóm → đọc bản ghi callback tương ứng<br>4. Kiểm tra trạng thái cuối của bản ghi thứ hai<br>5. Xác nhận không bản ghi nào rơi trạng thái lỗi | 2 thao tác: thêm bot vào nhóm · gỡ bot khỏi nhóm | - Cả 2 sự kiện đều sinh bản ghi định dạng mới và chạy tới trạng thái cuối hợp lệ<br>- **Không** bản ghi nào rơi trạng thái lỗi<br>- Thông tin nhóm cập nhật đúng ở màn quản trị |  | Lấp `G15` · regression · dẫn từ `NEW-37` / `NEW-38` (task #299 — #40888) · #40889 hiện **gộp** unsend+join+leave vào 1 TC (`NEW-20`) · Đánh giá spec: Spec ghi rõ · Evidence: 2 bản ghi + ảnh chụp màn quản trị nhóm |
| TC-INTGLINE001-13 | API | INTG-LINE-001 | Job callback — loại event ngoài danh sách xử lý | Abnormal | manual | Tất cả | Loại event LINE không nằm trong danh sách xử lý — lưu được, không lỗi, không kẹt | - Ba loại event LME **chưa xử lý** (Leader xác nhận 2026-09-16): `memberJoined` · `memberLeft` · `accountLink`<br>- `memberJoined` / `memberLeft` phát sinh thật khi có người **vào / rời nhóm** mà bot đang ở trong đó — dựng được bằng nhóm LINE thật<br>- `accountLink` chỉ phát sinh khi có luồng liên kết tài khoản, hệ thống không dùng → dựng bằng nội dung webhook tự tạo<br>- Job đang chạy, bot test đã ở trong 1 nhóm LINE | 1. Mời **một người thứ ba vào nhóm LINE** đang có bot → sinh `memberJoined` thật<br>2. Cho người đó **rời nhóm** → sinh `memberLeft` thật<br>3. Dựng 1 nội dung webhook chứa event `accountLink` rồi gửi tới đường nhận callback của bot<br>4. Với **từng** trong 3 trường hợp: đọc bản ghi callback — nội dung lưu có đủ và nguyên văn không, trạng thái cuối là gì<br>5. Kiểm tra job **không** ghi lỗi và **không** bản ghi nào kẹt ở trạng thái đang xử lý<br>6. Gửi tiếp 1 tin nhắn thật từ friend để chắc chắn job vẫn chạy bình thường sau 3 event lạ<br>7. Kiểm tra màn chat 1:1 và danh sách bạn bè **không** phát sinh dữ liệu rác từ 3 event trên | `memberJoined` (người thứ 3 vào nhóm) · `memberLeft` (người đó rời nhóm) · `accountLink` (webhook tự dựng) + 1 tin nhắn thật đối chứng | - Cả 3 bản ghi **lưu đủ** nội dung ở định dạng mới (có lớp bao ngoài), không lỗi máy chủ<br>- Cả 3 về trạng thái **không xác định loại**, **không** phải trạng thái lỗi, **không** kẹt ở đang xử lý<br>- Job không ghi lỗi; tin nhắn thật gửi sau đó vẫn xử lý bình thường<br>- Chat 1:1 và danh sách bạn bè **không** phát sinh hội thoại / bạn bè rác từ 3 event này |  | Lấp `G15` `G6` · dẫn từ `NEW-55` (task #299 — #40888) · 3 loại event do **Leader chỉ định** (2026-09-16), không tự tra tài liệu · ⚠️ `memberJoined` / `memberLeft` đi kèm nhóm nên hay **gộp chung bản ghi** với tin nhóm → liên hệ `TC-JOB001-14` · Đánh giá spec: Spec không ghi — chưa chốt danh sách loại event được xử lý (liên quan §6 mục 6c của report #40044) · Evidence: nội dung + trạng thái cuối của 3 bản ghi + log job + ảnh chụp chat 1:1 và danh sách bạn bè |
| TC-INTGLINE001-14 | UI | INTG-LINE-001 | Job callback — tin trong nhóm và thu hồi tin nhóm | Normal | manual | product | OA trong nhóm nhận tin và tin bị thu hồi — thông tin nhóm cập nhật đúng | - Bot đã ở trong 1 nhóm LINE có ≥ 2 thành viên<br>- Bản sửa đã triển khai | 1. Một thành viên gửi tin trong nhóm → đọc bản ghi callback và trạng thái cuối<br>2. Kiểm tra màn quản trị: thông tin nhóm có cập nhật không<br>3. Thành viên đó **thu hồi** tin vừa gửi<br>4. Đọc bản ghi callback của sự kiện thu hồi và trạng thái cuối<br>5. Kiểm tra hiển thị ở màn quản trị sau khi thu hồi | 1 tin trong nhóm + 1 thao tác thu hồi tin đó | - Bản ghi tin nhóm về trạng thái **bỏ qua tin nhóm** (không trả lời trong nhóm)<br>- Bản ghi thu hồi xử lý xong, không rơi trạng thái lỗi<br>- Thông tin nhóm ở màn quản trị cập nhật đúng<br>- Cả 2 bản ghi lưu ở định dạng mới |  | Lấp `G15` `G16` · regression · RULE-08 · dẫn từ `NEW-54` (task #299 — #40888) · Đánh giá spec: Spec không ghi · Evidence: 2 bản ghi + ảnh chụp nhóm LINE + màn quản trị |
| TC-MSG004-04 | UI | MSG-004 | Job callback — friend thu hồi tin 1:1 | Normal | manual | product | Friend thu hồi tin nhắn — chat 1:1 và tin nhắn cuối cập nhật đúng | - Bot thật, friend A đã gửi ≥ 2 tin trong hội thoại<br>- Ghi nhận nội dung tin nhắn cuối đang hiển thị ở danh sách bạn bè | 1. Friend A gửi tin mới `se-thu-hoi`<br>2. Kiểm tra chat 1:1 và tin nhắn cuối ở danh sách bạn bè<br>3. Friend A **thu hồi** tin đó trên app LINE<br>4. Đọc bản ghi callback của sự kiện thu hồi: định dạng, trạng thái cuối<br>5. Kiểm tra lại chat 1:1 (tin hiển thị thế nào) và tin nhắn cuối ở danh sách bạn bè | 1 tin `se-thu-hoi` rồi thu hồi | - Bản ghi sự kiện thu hồi xử lý xong, định dạng mới<br>- Chat 1:1 hiển thị tin đã thu hồi đúng quy ước hiện hành<br>- Tin nhắn cuối ở danh sách bạn bè **cập nhật lại**, không còn hiển thị nội dung tin đã thu hồi |  | Lấp `G16` `Q15` `Q18` · regression · RULE-06 · RULE-08 · dẫn từ `NEW-53` (task #299 — #40888) · Đánh giá spec: Spec không ghi · Evidence: ảnh chụp chat 1:1 và danh sách bạn bè trước/sau thu hồi |
| TC-MSG004-05 | UI | MSG-004 | Job callback — xem hết video có gắn theo dõi | Normal | manual | product | Friend xem hết video — action gắn theo dõi được thực thi | - Bot có template video **bật theo dõi xem hết**, action gắn thẻ khi xem xong<br>- Đã gửi video đó cho friend A<br>- Ghi nhận danh sách thẻ của friend A | 1. Trên app LINE, friend A mở video và **xem tới hết**<br>2. Đọc bản ghi callback của sự kiện xem hết video: định dạng, trạng thái cuối<br>3. Kiểm tra thẻ của friend A đã được gắn chưa<br>4. Kiểm tra số liệu theo dõi video ở màn quản trị<br>5. Lặp lại với 1 friend khác để loại trừ may rủi | 2 friend cùng xem hết 1 video có gắn theo dõi | - Cả 2 bản ghi xem hết video đều xử lý xong, định dạng mới<br>- Thẻ được gắn cho **cả 2** friend<br>- Số liệu theo dõi video tăng đúng 2 |  | Lấp `G16` `Q15` · regression · RULE-06 · RULE-08 · dẫn từ `NEW-57` (task #299 — #40888) · Đánh giá spec: Spec ghi rõ · Evidence: 2 bản ghi + danh sách thẻ + ảnh chụp số liệu video |
| TC-INTGHOOK001-13 | API | INTG-HOOK-001 | Job callback — chọn mã sự kiện khi bản ghi mở đầu bằng báo đã gửi | Abnormal | manual | product | Bản ghi mở đầu bằng event báo đã gửi — mã sự kiện lấy theo tin nhắn, không lấy theo báo đã gửi | - **Cơ chế cần hiểu trước** (nguồn: #40888 journal #136609):<br>&nbsp;&nbsp;① Mỗi event LINE gửi có **mã riêng** (`webhookEventId`).<br>&nbsp;&nbsp;② Từ khi gộp, **1 bản ghi chứa nhiều event** nhưng bảng chỉ có **1 cột** mã sự kiện ⇒ phải chọn **1 event làm đại diện**.<br>&nbsp;&nbsp;③ Quy tắc chọn đại diện = **event đầu tiên KHÁC loại báo đã gửi** (`delivery`) — cùng event đã dùng cho cột loại và cột định danh bạn bè.<br>&nbsp;&nbsp;④ Job chặn trùng bằng cách: bản ghi mới có **cùng bot + cùng mã sự kiện** với bản ghi cũ hơn ⇒ **bỏ qua cả bản ghi**.<br>- **Vì sao phải test**: nếu lấy nhầm mã của event báo đã gửi (loại này có thể **không mang mã**) thì cột mã sự kiện **rỗng** ⇒ cơ chế chặn trùng **không chạy** ⇒ LINE gửi lại là nghiệp vụ chạy lặp.<br>- Bot test có quy tắc trả lời tự động khớp từ khoá `dup13`<br>- Ghi nhận số bản ghi hội thoại và số lượt trả lời tự động **trước** khi bắt đầu | 1. Dựng 1 nội dung webhook có **2 event theo thứ tự**: ① event **báo đã gửi** ② event **tin nhắn** nội dung `dup13`. Ghi lại **mã của từng event** trong body<br>2. Gửi nội dung đó tới đường nhận callback của bot (lần gửi đầu, cờ báo gửi lại = `false`)<br>3. Mở bản ghi callback vừa sinh, đọc **3 cột**: loại · định danh bạn bè · **mã sự kiện webhook**<br>4. Đối chiếu: mã trong cột có **đúng bằng mã của event tin nhắn** không — hay đang rỗng / đang là mã của event báo đã gửi<br>5. Gửi lại **đúng nội dung đó**, chỉ đổi cờ báo gửi lại thành `true`<br>6. Chờ job xử lý xong, đọc trạng thái bản ghi thứ hai<br>7. Đếm số tin trả lời friend nhận trên app LINE và số bản ghi hội thoại mới | 1 nội dung webhook `[event báo đã gửi, event tin nhắn "dup13"]` — gửi 2 lần, lần 2 đổi cờ báo gửi lại = `true` | - Cột mã sự kiện webhook **không rỗng** và **đúng bằng mã của event tin nhắn**<br>- Ba cột loại · định danh bạn bè · mã sự kiện **cùng mô tả một event** (event tin nhắn), không lẫn sang event báo đã gửi<br>- Bản ghi thứ hai rơi trạng thái **bỏ qua do trùng**<br>- Friend nhận **đúng 1** tin trả lời, có **đúng 1** bản ghi hội thoại mới<br>- ⚠️ **Là lỗi nếu**: cột mã sự kiện rỗng, hoặc mang mã của event báo đã gửi, hoặc bản ghi thứ hai vẫn được xử lý ⇒ raise ticket |  | Lấp `G19` `G13` `Q5` · RULE-08 · **Cả 2 bộ TC (#298 và #299) đều KHÔNG có tổ hợp này** · Đánh giá spec: Spec ghi rõ (#40888 journal #136609 — *"lấy đúng mã của sự kiện đại diện đã dùng cho cột loại/định danh bạn bè"*) · Evidence: ảnh chụp body đã gửi (thấy mã từng event) + 3 cột của 2 bản ghi + log job + số lần trả lời đếm từ chat 1:1 |
| TC-INTGHOOK001-14 | API | INTG-HOOK-001 | Job callback — gửi lại kèm thêm event mới | Abnormal | manual | product | Lần gửi lại có thêm tin nhắn mới — tin cũ không chạy lại, tin mới không được bỏ sót | - **Cơ chế cần hiểu trước** (nguồn: #40888 journal #136609):<br>&nbsp;&nbsp;① Mỗi event LINE gửi có **mã riêng** (`webhookEventId`).<br>&nbsp;&nbsp;② Từ khi gộp, **1 bản ghi chứa nhiều event** nhưng bảng chỉ có **1 cột** mã sự kiện ⇒ phải chọn **1 event làm đại diện**.<br>&nbsp;&nbsp;③ Quy tắc chọn đại diện = **event đầu tiên KHÁC loại báo đã gửi** (`delivery`) — cùng event đã dùng cho cột loại và cột định danh bạn bè.<br>&nbsp;&nbsp;④ Job chặn trùng bằng cách: bản ghi mới có **cùng bot + cùng mã sự kiện** với bản ghi cũ hơn ⇒ **bỏ qua cả bản ghi**.<br>- **Vì sao phải test**: chặn trùng bỏ qua **cả bản ghi** chứ không bỏ theo từng event. Nếu lần gửi lại **thêm event mới** mà event đại diện vẫn là event cũ ⇒ mã sự kiện không đổi ⇒ cả bản ghi bị bỏ ⇒ **mất hẳn event mới**. Dev đã nêu mặt còn lại của rủi ro này ở #40888 journal #136609 nhưng **chưa ai viết TC cho cả hai mặt**.<br>- Bot test có quy tắc trả lời tự động dạng 全てのメッセージ (trả lời mọi tin)<br>- Ghi nhận số bản ghi hội thoại và số lượt trả lời tự động **trước** khi bắt đầu | 1. **Lần 1** — dựng webhook có 2 event: ① báo đã gửi ② tin nhắn `dup14-cu`. Gửi đi (cờ báo gửi lại = `false`)<br>2. Chờ job xử lý xong. Đếm: friend nhận mấy tin trả lời, chat 1:1 có mấy tin<br>3. **Lần 2** — dựng webhook có 3 event: ① báo đã gửi (**giữ nguyên mã cũ**) ② tin nhắn `dup14-cu` (**giữ nguyên mã cũ**) ③ tin nhắn `dup14-moi` (**mã mới**). Gửi đi với cờ báo gửi lại = `true`<br>4. Đọc bản ghi thứ hai: cột mã sự kiện webhook là mã nào, trạng thái cuối là gì<br>5. Chờ job xử lý xong<br>6. Kiểm **riêng từng tin**: `dup14-cu` có bị trả lời lần thứ hai không · `dup14-moi` có được trả lời không<br>7. Mở chat 1:1 đếm tổng số tin của friend: kỳ vọng **2 tin** (`dup14-cu` 1 lần + `dup14-moi` 1 lần) | Lần 1: `[báo đã gửi, tin "dup14-cu"]` · Lần 2: `[báo đã gửi, tin "dup14-cu" (mã cũ), tin "dup14-moi" (mã mới)]`, cờ báo gửi lại = `true` | - Tin `dup14-cu` **không** bị xử lý lần thứ hai — không có tin trả lời thứ hai cho nó<br>- Tin `dup14-moi` **PHẢI được xử lý** — friend nhận tin trả lời cho nó, chat 1:1 có tin `dup14-moi`<br>- Chat 1:1 tổng cộng **2 tin** của friend, không phải 1, không phải 3<br>- ⚠️ **Là lỗi nếu**: bản ghi thứ hai bị bỏ qua **toàn bộ** vì trùng mã ⇒ **mất hẳn tin `dup14-moi`** (khách gửi mà hệ thống không nhận) ⇒ raise ticket<br>- ⚠️ **Cũng là lỗi nếu**: tin `dup14-cu` được trả lời **2 lần** ⇒ chặn trùng không chạy |  | Lấp `G19` `G12` `Q5` · RULE-08 · **Rủi ro do chính Dev nêu** ở #40888 journal #136609: *"Nếu LINE gửi lại chỉ MỘT PHẦN sự kiện và sự kiện đại diện khác đi thì không chặn được trùng"* — TC này kiểm **mặt ngược lại và nguy hiểm hơn**: đại diện KHÔNG đổi nhưng có event mới ⇒ mất dữ liệu khách. **0 TC** ở cả 2 bộ · Đánh giá spec: **Đã hỏi leader** — chưa REQ nào chốt chặn trùng theo **bản ghi** hay theo **từng event** (xem §6 mục 8) · Evidence: 2 body đã gửi + cột mã sự kiện của 2 bản ghi + ảnh chụp chat 1:1 đếm được 2 tin + log job |

> **Phân bố loại case của 39 TC đề xuất** (sau khi Leader chỉnh 2026-09-16): `Normal 21 · Abnormal 13 · Boundary 5`.
> Tiêu chí áp dụng: **`Abnormal`** = input hỏng / điều kiện lỗi (bot hết hạn, gửi trùng, dữ liệu lệch) / kỳ vọng là **hệ thống từ chối hoặc bỏ qua**. Bản ghi LINE gửi **hợp lệ** mà kỳ vọng nghiệp vụ **chạy đúng** thì là `Normal`, kể cả khi kịch bản hiếm gặp — bản ghi gộp nhiều event, tin trong nhóm, thu hồi tin đều thuộc nhóm này.
> ⚠️ **RULE-01 còn thiếu sau khi bổ sung** — các quan điểm ưu tiên **Cao** dưới đây vẫn chưa đủ 3 loại case, tính **cả** bộ 36 TC gốc; cần Leader quyết bổ sung hay ghi lý do:
>
> | Quan điểm | Có ở §5 | Thiếu | Ghi chú |
> |---|---|---|---|
> | `MSG-004` | 5 TC — **toàn `Normal`** | `Abnormal` · `Boundary` | Bộ gốc **0 TC** mang mã này (`Q15`) → sau bổ sung vẫn khuyết 2 loại |
> | `REG-SHARED-001` | 3 TC — **toàn `Normal`** | `Abnormal` · `Boundary` | Bộ gốc có 7 TC nhưng **cũng đều `Normal`** (`NEW-17` · `NEW-18` · `NEW-20` · `NEW-37`…`NEW-41`) |
> | `ENV-003` | 2 TC — **toàn `Normal`** | `Abnormal` · `Boundary` | Bộ gốc chỉ có `NEW-30` gắn nhầm mã (xem `I8`) |
> | `INTG-HOOK-001` | 5 TC — `Abnormal` · `Boundary` | `Normal` | Đường thành công (webhook gửi **một lần**, xử lý đúng một lần) chưa có TC riêng ở cả 2 bộ |
> | `FUNC-003` | 2 TC — **toàn `Abnormal`** | `Normal` · `Boundary` | Cả 2 TC đều là nhánh từ chối; thiếu nhánh chấp nhận bình thường |

> **Dòng §1/§2 không đề xuất TC mới**:
> - `G8` (`NEW-5` blocked) · `G9` (`NEW-10` ba kết quả mâu thuẫn) · `G10` (envelope `buildBody`) · `G11` (5 TC thiếu nhãn) — **đã có TC, không thiếu TC**. Việc cần làm là chạy lại / gỡ chặn / đối chiếu với #40044, không phải viết thêm.
> - `Q5` `Q10` — phần thiếu đã nằm trong các TC trên (`TC-INTGHOOK001-10/11/12/13/14`, `TC-ENV003-10`); riêng **`REQ-010`** (`Q11`) chỉ cần **chạy lại `NEW-36` trên production**, không cần TC mới.
> - `Q14` (`PERM-003`) — gộp vào `TC-DATADB001-10`, không tách TC riêng.

### 5b — GAP **không verify được bằng TC manual** → chuyển sang cơ chế khác

> Leader đã loại **9 TC** khỏi bảng trên (2026-09-16) vì QA không có quyền thực hiện: deploy lệch nhánh / quay lui, dừng — khởi động lại job, ghi bản ghi thủ công vào DB, đọc giới hạn cột.
> **Loại TC không loại rủi ro.** Các GAP dưới đây vì thế **không còn TC nào canh** — `ENV-003` cho phép thay bằng *"phiếu xác nhận không thể verify + phương án giám sát"*, nên phải chốt bằng đường đó, không được để trống.

| GAP / Q | TC đã loại | Vì sao QA không chạy được | Cơ chế thay thế bắt buộc | Ai chịu trách nhiệm |
|---|---|---|---|---|
| `G3` · `Q3` — **thứ tự deploy 2 nhánh + quay lui** | `TC-DEPLOYLIVE001-01` · `-02` · `-03` | Phải deploy lệch nhánh rồi quay lui trên môi trường thật — thao tác của vận hành, không phải QA | **Không còn TC nào.** Dev viết **tài liệu phát hành** ghi rõ thứ tự bắt buộc (job trước, phần nhận sau) + kịch bản quay lui, Leader duyệt trước ngày release. Sau deploy: giám sát số bản ghi trạng thái lỗi trong 30 phút đầu, vượt ngưỡng thì quay lui | `Dev + vận hành` — §6 mục 5 |
| `G7` · `Q12` — **sức chứa cột `request`** | `TC-FUNC004-10` | Cần biết giới hạn thật của cột và dựng body sát/vượt trần — thuộc tầng DB | **Không còn TC nào.** Dev xác nhận bằng văn bản: kiểu cột hiện tại và độ dài body lớn nhất LINE gửi. #40888 đã ghi *"cột request kiểu TEXT (~65535 byte): body quá lớn sẽ bị **cắt cụt âm thầm**… nếu vận hành thấy log này thì cần đổi kiểu cột sang MEDIUMTEXT"* → chốt: đổi kiểu cột trước, hay chấp nhận rủi ro + giám sát log cảnh báo | `Dev` — mở mục mới ở §6 nếu chốt là chấp nhận rủi ro |
| `G9` · `Q9` — **job gián đoạn giữa chừng** | `TC-STATE001-10` · `-11` | Phải dừng đột ngột và khởi động lại tiến trình job | **Không còn TC nào.** Gắn vào quyết định của §6 mục 3 (bug #40894 bị Reject): nếu chấp nhận hành vi *row kẹt trạng thái đang xử lý không được nhặt lại* thì phải có **cảnh báo giám sát** đếm row kẹt ở trạng thái đó, không để im lặng | `Dev + Leader` — §6 mục 3 |
| `G1` — **env nào dùng writer nào** | `TC-FUNC001-10` | Cần đối chiếu cả 3 môi trường gồm production | Chuyển thành **câu hỏi chốt** ở §6 mục 7. `TC-DEPLOYLIVE001-04` vẫn giữ và vẫn so được **hai chuỗi do hai writer sinh ra**, nhưng không trả lời được *"môi trường nào đang chạy writer nào"* | `Dev + PM` — §6 mục 7 |
| `Q6` — **`COMPAT-LEGACY-001` thiếu Abnormal** (RULE-01) | `TC-COMPATLEGACY001-10` · `-11` | Phải tạo bản ghi định dạng cũ / bản ghi hỏng thủ công trong DB | Nhánh legacy còn `NEW-9` · `NEW-10` · `NEW-33` (bộ gốc) + `TC-COMPATLEGACY001-12` + `TC-DATAMIG001-10`, **nhưng đều là `Normal`** → `Q6` vẫn thiếu `Abnormal`, ghi lý do *"không dựng được dữ liệu cũ hỏng bằng thao tác QA"* vào `Ghi chú` của `NEW-10` trên Studio (RULE-01 cho phép thiếu nếu có lý do) | `Leader` ghi lý do vào Studio |

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | `EP-24` — *POST `/line/callback/add/{bot_id}` — nơi ghi `callback_event.request`* | Spec ghi writer là **Laravel `BotController@callbackWebHook`**; task #40889 chuyển writer sang **Java `addCallback`**. Chốt: writer PHP đã bị gỡ hẳn chưa, hay hai writer còn chạy song song? Nếu còn song song thì `destination` vẫn mất trên đường PHP và dữ liệu hỗn hợp là **vĩnh viễn**, không phải tạm thời. Update `spec-features/admin/auto-reply/job/job-spec.md:12` và `bot-add-v2` BR-008 theo kết luận. | `Studio coverage EP-24` ghi thẳng *"điểm CONFLICT cần xác nhận"* + bug #1055 đọc code · vs `03-dev-impact.md` mục 3 (*"1 nơi ghi"*) — G1 (không verify được bằng TC manual) | `Dev + PM` |
| 2 | `REQ-005` — *bản ghi hỏng phải chuyển trạng thái lỗi **kèm mô tả lỗi*** | Bug #1037 / #40895 (`error_message` luôn rỗng do `@Column(updatable = false)`) đã bị **Reject**, và expected của `NEW-13` đã được **sửa (v2)** thành *"không yêu cầu kiểm cột mô tả lỗi"*. Chốt một trong hai: (a) bỏ vế "kèm mô tả lỗi" khỏi REQ-005 — vận hành chấp nhận phải mò log job; hoặc (b) giữ REQ-005 và mở lại bug. **Không để TC tự nới expected theo bản dựng.** | `TC vs requirement` — `NEW-13` v2 vs REQ-005 · bug #40895 Reject — I6 | `Dev + Leader` |
| 3 | `REQ-005` — *không được kẹt ở trạng thái đang xử lý* | Bug #1038 / #40894 (*row kẹt `status = 1` sau khi job khởi động lại không bao giờ được nhặt lại — `reloadCallbackEventNewInDatabase()` đang bị comment*) đã bị **Reject**, nhưng REQ-005 vẫn yêu cầu ngược lại. Chốt: bỏ vế này khỏi REQ-005, hay mở lại bug và bỏ comment hàm khôi phục. Không verify được bằng TC manual (phải dừng/khởi động lại job) → phải chốt bằng quyết định + giám sát. | `TC vs quyết định Dev` — REQ-005 vs bug #40894 Reject — G9 / Q9 | `Dev + Leader` |
| 4 | `BR-01` 「加工せずそのまま」 — forward nguyên trạng Kênh 1 | Report ticket cha **#40044 §1 G2** ghi `buildBody` **đang bọc payload vào envelope thêm 3 trường**; Dev #40889 lại kết luận `buildBody` *"forward nguyên trạng, không cần sửa"*. Hai kết luận không thể cùng đúng. Nếu envelope còn thì việc lưu full body ở #40889 **vẫn không đạt BR-01**, và `NEW-22` đang pass là pass sai. Chốt trạng thái envelope **trước** khi nghiệm thu #40889. | `Report #40044 G2` vs `03-dev-impact.md` mục 3 #40889 — G10 | `Dev + PM` |
| 5 | Thứ tự phát hành **hai nhánh** (`release-callback` · `cb_full_request`) | Chưa tài liệu nào ghi thứ tự bắt buộc. Theo cách fix, **job phải deploy trước phần nhận**; deploy ngược lại sẽ khiến toàn bộ callback mới rơi trạng thái lỗi. Bổ sung vào tài liệu phát hành: thứ tự, cách kiểm chứng giữa 2 bước, và kịch bản quay lui. | `Không có spec` — G3 / Q3 (không verify được bằng TC manual) | `Dev + Leader` |
| 6 | `REQ-010` — *chạy script đánh dấu bỏ qua **trước** khi bật cờ chuyển tiếp* | Requirement mô tả đúng rủi ro (*toàn bộ lịch sử bị gửi ngược sang endpoint của khách*) nhưng TC duy nhất canh việc này (`NEW-36`) **chỉ chạy ở local**. Chốt: đây có phải cổng chặn bắt buộc trước khi bật cờ trên production không, ai ký xác nhận đã chạy script. | `TC vs RULE-08` — `NEW-36` pass local, auto skip — Q11 | `Leader + Dev` |
| **7** | **Vai trò của hai writer `callback_event` — #40889 (Java) vs #40888 (PHP)** | Hai ticket cùng sửa một hành vi trên hai đường ghi khác nhau, nhưng **không tài liệu nào nói env nào dùng đường nào và có gỡ bớt một đường không**. #40888 ghi *"Staging đang nhận callback từ php"*; #40889 sửa đường Java. Cần chốt 3 điểm: (a) sau phát hành, **production** đi qua writer nào; (b) writer Java có ghi `webhook_event_id` không (PHP đã ghi từ vòng fix 2 — commit `a231fd08ca`) — nếu không thì **chặn trùng chỉ chạy cho một nửa lưu lượng**; (c) 3 tiến trình PHP cũ (`HandleCallback*`) đã dừng hẳn chưa. Chưa chốt thì không nghiệm thu được ticket nào trong hai. | `#40888 description + journal #136609` vs `03-dev-impact.md` #40889 — G1 / G2 / G14 / G18 | `Dev + PM + Leader` |
| **8** | **`NEW-11` của #40888 đã lỗi thời sau vòng fix 2** | TC `NEW-11` (task #299) có tên *"LINE gửi lại cùng một lần gọi sinh **hai bản ghi** — ghi nhận **hiện trạng không chống trùng**"* — viết ở vòng 1, khi PHP chưa ghi `webhook_event_id`. Vòng fix 2 đã bật chống trùng nên expected này **giờ mâu thuẫn với hành vi đúng**. Trùng đích với `NEW-19` của #40889 (đang fail). Chốt hành vi chuẩn rồi sửa expected `NEW-11` trên Studio task #299. | `TC vs code sau refix` — `NEW-11` (#299) vs journal #136609 vs `NEW-19` (#298) | `Dev + Leader` |
| **9** | `bot_type` — **đã sửa spec, ghi lại để PM nắm** | `bot_type = 1` (OLIOA) là **kiểu cũ hết vòng đời**; production toàn bộ bot `bot_type = 0` (Test Leader xác nhận 2026-09-16). Đã cập nhật 3 file trong `spec-features`: `admin/scenario/feature-spec.md` + `admin/scenario/ui/ui-spec.md` (4 điều kiện *"chỉ khi `bot_type == 0`"* → **luôn hiển thị**, vì luôn đúng) và `admin/bot-edit/db/db-mapping.md` (giữ cột vì DB vẫn có, đánh dấu **hết vòng đời**). Cần PM/Dev xác nhận có kế hoạch **gỡ hẳn cột + nhánh code** hay để nguyên. | `Spec vs thực tế production` — TC-REGSHARED001-12 đã bị loại khỏi §5 | `PM + Dev` |

---

## 7. Đối chiếu với ticket song sinh #40888

> Bổ sung **2026-09-16** theo yêu cầu Leader. Nguồn: Studio task **#299** (ticket 40888, 55 TC, `reviewState = done`) vs Studio task **#298** (ticket 40889, 36 TC).
> Hai ticket = **cùng một thay đổi nghiệp vụ trên hai đường ghi**: #40888 = writer **PHP** (`BotController::storeCallbackEventRaw`, branch `ai_small_40888`) · #40889 = writer **Java** (`HandlePostbackTask.addCallback`) + toàn bộ phía job tiêu thụ.

### 7.1 — Nhóm TC #40888 CÓ mà #40889 THIẾU

| # | Nhóm | TC ở task #299 (#40888) | #40889 có gì | Vì sao #40889 vẫn phải có | Dòng §1/§2 |
|---|---|---|---|---|---|
| 1 | **Bản ghi chứa nhiều event — tầng job xử lý** | 11 TC: `NEW-2` · `NEW-4` · `NEW-39` · `NEW-43` · `NEW-46` · `NEW-47` · `NEW-48` · `NEW-49` · `NEW-50` · `NEW-58` · `NEW-59` | `NEW-4` — **chỉ tầng lưu** (*"lưu đủ mảng trong một bản ghi duy nhất"*) | Đây là thay đổi hành vi **lớn nhất**: 1 lần LINE gọi nay = **1 bản ghi chứa mọi event** (trước bị tách nhiều bản ghi và **mất event thứ 3 trở đi**). #40889 chính là bên **đọc và xử lý** bản ghi gộp đó, nhưng **0/25 TC tầng job** có bản ghi nhiều event | `G12` `Q15` `Q19` |
| 2 | **Event `delivery` + chọn event đại diện** | 8 TC: `NEW-3` · `NEW-6` · `NEW-16` · `NEW-17` · `NEW-23` · `NEW-40` · `NEW-44` · `NEW-47` | **0 TC** | Cột `type` / `line_id` lấy theo *event đầu tiên khác `delivery`*. #40888 nêu hậu quả nếu sai: *"mất thao tác bấm nút của khách, hoặc xử lý kết bạn / hủy kết bạn cho **SAI người**"* | `G13` `Q16` `Q17` |
| 3 | **Hai bộ tiêu thụ cùng quét bảng** | 7 TC (màn *"Tiến trình PHP xử lý callback_event"*): `NEW-16` · `NEW-17` · `NEW-18` · `NEW-20` · `NEW-22` · `NEW-23` · `NEW-60` | **0 TC** | #40888 cảnh báo *"chỉ ảnh hưởng nếu chúng **còn chạy song song với job**, cần xác nhận đã dừng hẳn"* — đây là **giả định nền** của cả 25 TC tầng job #40889 | `G14` |
| 4 | **Ma trận loại event ở tầng lưu** | 13 TC `API-CONTRACT-001` (`NEW-28`…`NEW-40`) + `NEW-52` · `NEW-53` · `NEW-54` · `NEW-55` · `NEW-56` · `NEW-57` | `NEW-8` (message) · `NEW-20` (**gộp** unsend+join+leave) · `NEW-37`…`NEW-41` | #40889 gộp 3 loại event vào 1 TC, thiếu **3 nguồn postback** (richmenu / button / imagemap), **loại event ngoài danh sách**, **tin trong nhóm** | `G15` |
| 5 | **Đích downstream có oracle đo được** | `NEW-45` (action richmenu) · `NEW-46` (**bộ đếm chưa xác nhận**) · `NEW-53` (thu hồi tin) · `NEW-54` (thông tin nhóm) · `NEW-57` (action xem hết video) · `NEW-58` (**thông báo mobile/PC/Chatwork**) · `NEW-59` (**tin nhắn cuối + thứ tự danh sách bạn bè**) | `NEW-8` — 1 expected gộp *"chat/scenario/tag/richmenu không bị ảnh hưởng"* | TC fail thì không truy được đích nào hỏng; 4 đích (bộ đếm · tin nhắn cuối · thứ tự danh sách · thông báo 3 kênh) **không đích nào có TC riêng** ở #40889 | `G16` `Q18` `Q19` |
| 6 | **Callback nội bộ tự sinh** | `NEW-19` · `NEW-21` · `NEW-60` | **0 TC** | `storeFakeCallbackEvent` giữ nguyên, sinh bản ghi **không có** mã sự kiện webhook → job phải đọc được và **không** chặn nhầm là trùng | `G17` |
| 7 | **Cross-check giữa 2 nhánh** | `NEW-41` (*job bản RELEASE đọc được bản ghi do PHP nhánh fix ghi*) · `NEW-42` (*bản ghi cũ và mới xen kẽ cùng lượt xử lý*) — **cả 2 chưa chạy** | **0 TC** | Cặp TC **duy nhất** nối 2 ticket. Không bên nào chạy ⇒ không ai chứng minh hai đường ghi khớp nhau | `G18` `G3` |
| 8 | ~~**Bot OLIOA (`bot_type = 1`)**~~ — **ĐÃ LOẠI** | `NEW-5` | **0 TC** — *không cần* | `bot_type = 1` (OLIOA) là **kiểu cũ đã hết vòng đời**, production **toàn bộ bot đều `bot_type = 0`** (Test Leader xác nhận 2026-09-16). Nhánh này không còn tồn tại nên **không phải GAP**; `NEW-5` của task #299 cũng nên đánh dấu không áp dụng. Đã cập nhật `spec-features` (xem §6 mục 9) | — |
| **9** | **Gửi lại callback × event `delivery`** — *(verify riêng theo yêu cầu Leader 2026-09-16)* | `NEW-11` (có đổi cờ `isRedelivery` nhưng expected đã lỗi thời) · `NEW-1` (chỉ dùng cờ làm dữ liệu mẫu) | `NEW-19` — **fail**, và steps **không** đổi cờ `isRedelivery` | **Cả hai bộ đều không có TC hợp lệ.** Chi tiết ở `G19` + `I11` | `G19` `I11` |

**Kết quả verify "callback resend delivery"** — grep toàn bộ nội dung (`name` · `precondition` · `steps` · `data_input` · `expected` · `note`) của cả hai payload:

| Chuỗi tìm | task #298 (#40889, 36 TC) | task #299 (#40888, 55 TC) |
|---|---|---|
| `isRedelivery` | **0 TC** | 2 TC — `NEW-1` (dữ liệu mẫu) · `NEW-11` (đổi cờ thật) |
| `redeliver` | **0 TC** | 2 TC — như trên |
| "gửi lại" | 1 TC — `NEW-19` (không đổi cờ) | 1 TC — `NEW-11` |
| event `delivery` | **0 TC** | 12 TC — `NEW-2` · `NEW-3` · `NEW-6` · `NEW-16` · `NEW-17` · `NEW-18` · `NEW-19` · `NEW-23` · `NEW-39` · `NEW-40` · `NEW-44` · `NEW-47` |
| **Tổ hợp** gửi lại **×** `delivery` | **0 TC** | **0 TC** |

→ Ba khoảng trống, xếp theo mức nghiêm trọng:
1. **Gửi lại chỉ một phần event** — Dev tự nêu ở #40888 journal #136609 (*"sự kiện đại diện khác đi thì không chặn được trùng"*) nhưng **0 TC**. Mặt trái nguy hiểm hơn: nếu lần gửi lại có thêm event mới mà mã đại diện **không** đổi thì cả bản ghi bị đánh trùng ⇒ **mất hẳn event mới**. → `TC-INTGHOOK001-14`.
2. **Gửi lại bản ghi gộp `[delivery, nghiệp vụ]`** — mã chặn trùng lấy theo event đại diện (event đầu tiên khác `delivery`), chưa ai verify lấy đúng mã. → `TC-INTGHOOK001-13`.
3. **Cờ `isRedelivery` không được bật trong steps** của `NEW-19` → TC không mô phỏng đúng LINE. → `TC-INTGHOOK001-10` (đã gộp `-12` vào) + `I11`.

### 7.2 — Nhóm #40889 CÓ mà #40888 không có (hai bộ bổ trợ nhau, không thay thế)

| Nhóm | TC ở task #298 (#40889) | Ghi chú |
|---|---|---|
| Chuyển tiếp Kênh 1 (Webhook転送) chi tiết | `NEW-22` · `NEW-25` · `NEW-27` · `NEW-29` · `NEW-30` · `NEW-31` | #40888 chỉ có 1 TC (`NEW-51`) chạm bên thứ 3 |
| Trạng thái lỗi / không xác định loại phía job | `NEW-11` · `NEW-12` · `NEW-13` · `NEW-15` | Vòng đời trạng thái là phần việc của job |
| Tương thích ngược + không backfill | `NEW-9` · `NEW-10` · `NEW-32` · `NEW-33` · `NEW-34` | #40888 chỉ có `NEW-20` · `NEW-21` |
| Thứ tự phát hành / script recover (`REQ-010`) | `NEW-36` | Không có ở #40888 |
| Sức chứa cột phía job | `NEW-7` · `NEW-35` | #40888 có `NEW-13` · `NEW-14` ở tầng lưu — **hai bên bổ trợ**, nên đối chiếu kết quả với nhau |

### 7.3 — Việc cần làm ngay

1. **Chạy `NEW-41` + `NEW-42` của task #299** — rẻ nhất, nối được 2 ticket, hiện chưa chạy lần nào.
2. **Đẩy 10 TC bổ sung còn lại ở cuối bảng §5 sang task #298** — phủ đủ 8 nhóm của §7.1 cộng nhóm 9 (gửi lại × `delivery`). Ưu tiên theo thứ tự: `TC-INTGHOOK001-14` (mất event khi gửi lại) → `TC-MSG004-01/02` + `TC-CONC001-12` (bản ghi nhiều event) → `TC-MSGUSER001-01` + `TC-MSG004-03` (event đại diện) → `TC-REGSHARED001-10/11` (hai bộ tiêu thụ).
3. **Chốt §6 mục 7** (env nào dùng writer nào · Java có ghi `webhook_event_id` không · 3 tiến trình PHP đã dừng chưa) **trước** khi nghiệm thu bất kỳ ticket nào trong hai.
4. **Sửa expected `NEW-11` của task #299** (§6 mục 8) — đang mô tả hiện trạng cũ "không chống trùng", mâu thuẫn với vòng fix 2.
5. **Đóng 5 dòng ở §5b bằng văn bản** (tài liệu phát hành · xác nhận kiểu cột · cảnh báo giám sát row kẹt · §6 mục 7 · lý do RULE-01) — đây là phần **không còn TC nào canh** sau khi loại 9 TC, nên nếu bỏ qua thì rủi ro đi thẳng ra production mà không ai biết.
