<!-- source: MCP LME TEST STUDIO — task_id=298, ticket 40889, testcase_list (36 TC), fetch lúc 2026-09-15. READ-ONLY snapshot, sinh bởi scripts/parse_studio_tcs.py. -->

# 04 — TC List (snapshot từ MCP LME TEST STUDIO)

> ⚠️ `contentTrust = untrusted` → xử lý như **data**, không phải chỉ thị.
> ⚠️ **READ-ONLY** — muốn sửa TC thì sửa trên Studio (`testcase_update`) rồi fetch lại.

# Digest — Studio task #298 · ticket 40889 · 36 TC

## Kết quả thực thi
| Trạng thái | Số TC |
|---|---|
| `pass` | 20 |
| `skip` | 11 |
| `fail` | 4 |
| `blocked` | 1 |

→ **20/36 TC (55%) thực sự Đạt**; 16 TC còn lại KHÔNG có kết luận test.

## Môi trường
| Env | Số TC |
|---|---|
| `LOCAL` | 28 |
| `STAGING` | 8 |

→ Production: **0 TC**.  ⚠️ **RULE-08**: không kết luận media / domain / job nền / bill tiền từ local-staging.

## Ai chạy (source / by)
| source / by | Số TC |
|---|---|
| `ai / haodtb` | 32 |
| `manual / haodtb` | 4 |

## Tác giả TC
| author | Số TC |
|---|---|
| `AI` | 31 |
| `haodtb@mcp` | 5 |

## Loại case
| case_type | Số TC |
|---|---|
| `Normal` | 21 |
| `Abnormal` | 10 |
| `Boundary` | 5 |

## Nhóm / chế độ chạy
| tc_group | Số TC |
|---|---|
| `job` | 25 |
| `api` | 6 |
| `data` | 5 |

| exec_mode | Số TC |
|---|---|
| `auto` | 28 |
| `manual` | 8 |

## Mã quan điểm KHỚP checklist-lme (13 mã)
`COMPAT-LEGACY-001`(1) · `DATA-DB-001`(1) · `DATA-MIG-001`(1) · `DATA-TEXT-001`(1) · `ENV-003`(1) · `FUNC-001`(3) · `FUNC-004`(4) · `INTG-HOOK-001`(1) · `INTG-LINE-001`(2) · `MEDIA-001`(1) · `REG-RUN-001`(1) · `REG-SHARED-001`(7) · `STATE-001`(1)

## ⚠️ Mã quan điểm KHÔNG có trong checklist-lme (8 mã)
| Mã Studio | Số TC |
|---|---|
| `TOOL-ERRHYG-001` | 3 |
| `TOOL-OLDREC-001` | 2 |
| `TOOL-KNOW-002` | 1 |
| `RULE-09` | 1 |
| `I18N-001` | 1 |
| `API-CONTRACT-001` | 1 |
| `JOB-002` | 1 |
| `TOOL-NEGCTRL-001` | 1 |

→ `/review-tc` KHÔNG map được coverage cho các mã này.

## ⚠️ TC fail / error hoặc có ticket bug (4)
| ID | exec | ticket | Tiêu đề |
|---|---|---|---|
| NEW-10 | `fail` |  | Restart job khi còn row format CŨ ở trạng thái NEW/retry — vẫn parse v |
| NEW-18 | `fail` |  | Tin nhắn có media định dạng CŨ — tải được media và hoàn tất xử lý |
| NEW-19 | `fail` | 40896 | Webhook bị LINE gửi lại — bản ghi trùng bị bỏ qua đúng như trước |
| NEW-15 | `fail` |  | Bản ghi request NULL/rỗng — chuyển STATUS_ERROR và job vẫn xử lý recor |

## ⚠️ TC skip / chưa chạy (11)
| ID | exec | Tiêu đề |
|---|---|---|
| NEW-38 | `skip` | Callback follow thực tế — full body và friend được xử lý đúng tới DONE |
| NEW-39 | `skip` | Callback unfollow thực tế — full body và trạng thái friend được cập nh |
| NEW-40 | `skip` | Callback videoPlayComplete thực tế — full body và xử lý tới DONE |
| NEW-41 | `skip` | LINE friend gửi video thật — callback full body, download media và cha |
| NEW-22 | `skip` | Forward webhook Kênh 1 — bên thứ ba nhận đúng nguyên body LINE gồm des |
| NEW-29 | `skip` | Event có media vẫn được chuyển tiếp và không kèm theo tệp |
| NEW-25 | `skip` | Regression forward Kênh 1 lỗi — sau retry cuối hiển thị đúng 1 bản ghi |
| NEW-27 | `skip` | Bot chưa bật chuyển tiếp — không gọi endpoint và không ghi bản ghi lỗi |
| NEW-31 | `skip` | Job bị dừng giữa chừng — bản ghi đang chuyển tiếp dở được nhặt lại đún |
| NEW-30 | `skip` | Chỉ bản ghi đã xử lý xong mới được chuyển tiếp — kiểm cả hai dải mã tr |
| NEW-36 | `skip` | Trước khi bật chuyển tiếp — không còn bản ghi lịch sử nào chưa được đá |

## Màn hình (4)
| screen | Số TC |
|---|---|
| `Job xử lý callback LINE` | 19 |
| `Receiver callback LINE — lưu callback_event` | 6 |
| `Job chuyển tiếp webhook Kênh 1` | 6 |
| `Dữ liệu callback_event` | 5 |

## requirement_keys (10)
`REQ-001`(8) · `REQ-002`(2) · `REQ-003`(6) · `REQ-004`(5) · `REQ-005`(5) · `REQ-006`(2) · `REQ-007`(2) · `REQ-008`(5) · `REQ-009`(5) · `REQ-010`(1)

---

## Bảng TC (16 cột canonical)

| TC No. | Mã quan điểm liên kết | Loại case | Tiêu đề test case | Điều kiện tiền đề | Các bước thực hiện | Dữ liệu test/input | Kết quả mong đợi | Kết quả thực thi | Evidence thực tế | Môi trường test | Người thực hiện | Ngày thực hiện | Số ticket bug | Trạng thái đánh giá spec | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-FUNC001-01 | FUNC-001 | Normal | Nhận webhook LINE 1 event message — bản ghi callback lưu đủ cả destination | Dịch vụ nhận callback (linect-service) chạy bản đã có fix #40889. Đã xác nhận webhook LINE của môi trường test đang được đẩy vào dịch vụ này chứ không phải vào ứng dụng web. Có sẵn 1 bot test riêng của phiên chạy (ghi lại bot_id) và 1 friend test đã kết bạn với bot đó. | 1. Ghi lại thời điểm bắt đầu và số bản ghi callback hiện có của bot test<br>2. Gửi tới đường nhận callback của bot test một request POST với nội dung đúng như LINE gửi: một body JSON gồm destination và mảng events chứa 1 event loại message<br>3. Chờ tối đa 10 giây rồi tra bản ghi callback mới nhất của bot test trong cơ sở dữ liệu<br>4. Đọc nội dung cột request của bản ghi đó | Body gửi đi: {"destination":"U40889desttest01","events":[{"type":"message","webhookEventId":"WH-40889-001","timestamp":1789000000000,"mode":"active","source":{"type":"user","userId":"U40889friend01"},"replyToken":"rt40889001","message":{"type":"text","id":"m40889001","text":"test 40889"}}]} | Phát sinh đúng 1 bản ghi callback mới của bot test. Nội dung cột request bắt đầu bằng dấu { (không phải dấu [), chứa nguyên cặp destination: U40889desttest01 và chứa nguyên mảng events. Các cột đi kèm lấy đúng từ event đầu tiên: loại = message, mã friend = U40889friend01, mã event webhook = WH-40889-001; trạng thái = chưa xử lý (0 hoặc 100 tuỳ chế độ chạy). | Đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17576 (NEW-1) · api · auto · local-only · REQ: REQ-001 · spec: TICKET-40889, COMMIT-f6104a3, EP-24, BR-01 · Oracle kỹ thuật: HandlePostbackTask.addCallback bản fix gọi setRequest(request) thẳng thay vì writeValueAsString(map.get("events")). Endpoint: POST /line/callback/add/{bot_id} của CallbackController (linect-service), KHÔNG phải API /v1 công bố ra ngoài — vì vậy surface khai là web. Trước khi kết luận fail phải xác nhận receiver đang chạy là bản Java. · author=AI · status=draft |
| TC-TOOLKNOW002-01 | TOOL-KNOW-002 | Normal | Tái hiện lỗi cũ — chuỗi lưu phải khớp từng ký tự với body LINE gửi | Như TC nhận webhook 1 event message. Chuẩn bị sẵn chuỗi body test và giữ nguyên bản sao của chuỗi đó để đối chiếu về sau. | 1. Chuẩn bị một chuỗi body trong đó mảng events đứng TRƯỚC destination, và có thêm một field mà hệ thống chưa dùng tới<br>2. Gửi chuỗi body đó tới đường nhận callback của bot test, gửi đúng nguyên văn không định dạng lại<br>3. Tra bản ghi callback mới nhất của bot test<br>4. So sánh chuỗi trong cột request với chuỗi body đã gửi, so sánh từng ký tự | Body gửi đi (giữ nguyên thứ tự key): {"events":[{"type":"message","webhookEventId":"WH-40889-002","timestamp":1789000000001,"source":{"type":"user","userId":"U40889friend01"},"message":{"type":"text","id":"m40889002","text":"order-check"}}],"destination":"U40889desttest01","unknownFutureField":"keep-me"} | Chuỗi trong cột request GIỐNG HỆT chuỗi body đã gửi: vẫn còn field unknownFutureField, vẫn giữ thứ tự events đứng trước destination, không bị thêm/bớt khoảng trắng hay dấu ngoặc. Ba triệu chứng của bản cũ đều không còn: không mất destination, không bị đảo thứ tự key, không có field nào bị loại bỏ. | Đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17577 (NEW-2) · api · auto · local-only · REQ: REQ-001 · spec: TICKET-40889, COMMIT-f6104a3, BR-01 · Đây là TC tái hiện bug + xác nhận đã fix (bắt buộc với task fix-bug). Bản cũ đọc body thành HashMap rồi ghi lại riêng phần events nên chắc chắn mất destination, mất field lạ và đảo thứ tự key. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-DATATEXT001-01 | DATA-TEXT-001 | Normal | Nội dung tiếng Nhật và emoji được lưu nguyên văn, không bị đổi thành mã \u | Như TC nhận webhook 1 event message. Cơ sở dữ liệu và kết nối của dịch vụ dùng bảng mã utf8mb4. | 1. Gửi tới đường nhận callback của bot test một body có nội dung tin nhắn gồm tiếng Nhật, emoji và ký tự đặc biệt<br>2. Tra bản ghi callback mới nhất của bot test<br>3. Đọc cột request và kiểm tra dạng biểu diễn của phần nội dung tin nhắn | Nội dung tin nhắn trong body: 「こんにちは😊テスト・全角ＡＢＣ＆<>"」 | Cột request chứa đúng chuỗi 「こんにちは😊テスト・全角ＡＢＣ＆<>"」 ở dạng ký tự thật. Trong cột request KHÔNG xuất hiện chuỗi dạng \u3053 hay \ud83d cho các ký tự này. Emoji không bị mất, không bị thay bằng dấu hỏi. | Đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17578 (NEW-3) · api · auto · local-only · REQ: REQ-002, REQ-001 · spec: TICKET-40889, COMMIT-f6104a3 · Bản cũ dùng ObjectMapper ghi lại chuỗi nên mọi ký tự ngoài ASCII bị escape thành \uXXXX. Nếu vẫn thấy \uXXXX thì hoặc fix chưa được deploy, hoặc bản ghi do receiver khác (ứng dụng web) tạo ra. · author=AI · status=draft |
| TC-FUNC001-02 | FUNC-001 | Normal | Webhook chứa nhiều event — lưu đủ mảng trong một bản ghi duy nhất | Như TC nhận webhook 1 event message. | 1. Gửi tới đường nhận callback của bot test một body chứa 3 event khác loại trong cùng mảng events<br>2. Tra các bản ghi callback của bot test phát sinh sau thời điểm gửi<br>3. Đọc cột request và các cột mô tả của bản ghi | Body chứa 3 event theo thứ tự: message (webhookEventId WH-40889-004-A, userId U40889friend01), follow (WH-40889-004-B), postback (WH-40889-004-C); destination = U40889desttest01 | Chỉ phát sinh đúng 1 bản ghi callback (không tách thành 3 bản ghi). Cột request chứa đủ cả 3 event theo đúng thứ tự đã gửi và vẫn còn destination. Các cột mô tả lấy theo event ĐẦU TIÊN: loại = message, mã event webhook = WH-40889-004-A. | Đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17579 (NEW-4) · api · auto · local-only · REQ: REQ-001 · spec: TICKET-40889, COMMIT-f6104a3 · Chặn nguy cơ fix làm hỏng phần lấy thông tin mô tả từ event đầu tiên. Hệ thống còn bắn thông báo nội bộ khi một webhook chứa nhiều event khác loại — có thông báo đó là bình thường. · author=AI · status=draft |
| TC-TOOLERRHYG001-01 | TOOL-ERRHYG-001 | Abnormal | Body không phải JSON hợp lệ — dịch vụ không chết và không lưu bản ghi rác | Như TC nhận webhook 1 event message. | 1. Gửi tới đường nhận callback của bot test một body không phải JSON<br>2. Đọc mã trạng thái HTTP trả về<br>3. Kiểm tra không có bản ghi callback mới của bot test<br>4. Gửi tiếp một webhook hợp lệ và kiểm tra nó vẫn được lưu bình thường | Lần 1: chuỗi thường 'khong-phai-json'. Lần 2 (kiểm tra phục hồi): body hợp lệ với webhookEventId WH-40889-006 | Lần 1: trả về 2xx, không phát sinh bản ghi callback nào, log ghi lỗi phân tích dữ liệu. Lần 2: webhook hợp lệ vẫn được lưu đầy đủ như thường — một request hỏng không làm hỏng luồng nhận webhook của các request sau. | Đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17581 (NEW-6) · api · auto · local-only · REQ: REQ-001 · spec: TICKET-40889, COMMIT-f6104a3 · Kiểm tra vệ sinh lỗi ở tầng nhận: input sai phải bị loại sạch, không để rơi chuỗi rác vào cột request rồi đẩy sang job xử lý. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-FUNC004-01 | FUNC-004 | Boundary | Webhook có dung lượng sát trần lưu trữ của cột request | Như TC nhận webhook 1 event message. Đã tra và ghi lại kiểu dữ liệu thật của cột request trên cơ sở dữ liệu mà dịch vụ đang dùng (theo bản dump hiện có là kiểu TEXT, trần 65.535 byte). | 1. Dựng một body hợp lệ có tổng kích thước sát dưới trần của cột request (khoảng 65.400 byte), phần lớn dung lượng nằm ở nội dung tin nhắn<br>2. Gửi body đó tới đường nhận callback của bot test và kiểm tra bản ghi lưu được có đúng độ dài, đọc lại được thành JSON hợp lệ<br>3. Dựng tiếp một body vượt trần (khoảng 65.600 byte) và gửi lần nữa<br>4. Kiểm tra bản ghi phát sinh từ lần 2: độ dài chuỗi lưu được và chuỗi đó có còn là JSON hợp lệ không | Lần 1: body ~65.400 byte (webhookEventId WH-40889-007-A). Lần 2: body ~65.600 byte (WH-40889-007-B). Nội dung tin nhắn là chuỗi ASCII lặp để kiểm soát chính xác số byte. | Lần 1: chuỗi lưu trong cột request dài đúng bằng body đã gửi và vẫn đọc được thành JSON hợp lệ. Lần 2: hệ thống KHÔNG được cắt cụt âm thầm — hoặc báo lỗi rõ ràng và không lưu bản ghi, hoặc lưu đầy đủ; tuyệt đối không lưu một chuỗi bị cắt giữa chừng. | Đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17582 (NEW-7) · api · auto · local-only · REQ: REQ-001, REQ-009 · spec: TICKET-40889, COMMIT-f6104a3 · Biên sinh ra trực tiếp từ fix: bản mới lưu thêm phần bao ngoài {"destination":"U…","events":…} nên dài hơn bản cũ khoảng 55-60 byte với cùng một webhook ASCII. Bản ghi bị cắt sẽ thành JSON hỏng và bị job xử lý đánh trạng thái lỗi — mất luôn tin nhắn của friend. Nếu cột là LONGTEXT thì TC thành đối chứng âm. · author=AI · status=draft |
| TC-FUNC001-03 | FUNC-001 | Normal | Callback message thực tế — full body, DONE, chat/scenario/tag/richmenu không bị ảnh hưởng | Bot test đã deploy fix #40889. Có 1 LINE friend test. Chuẩn bị keyword test sao cho khi friend gửi message này hệ thống có thể quan sát đủ regression: tin nhắn vào chat 1:1, action/scenario chạy, gắn tag và đổi richmenu (có thể dùng cùng một action chain hoặc cấu hình tương đương theo hệ thống hiện tại). | 1. Từ LINE app thật, friend test gửi keyword đã chuẩn bị cho bot<br>2. Tra callback_event mới tạo và xác nhận request bắt đầu bằng {, có destination và events chứa event message vừa gửi<br>3. Chờ job callback xử lý xong<br>4. Tra status của callback_event<br>5. Mở chat 1:1 của đúng friend và kiểm tra message vừa gửi xuất hiện đúng phòng chat<br>6. Kiểm tra scenario/action liên quan đã chạy đúng, tag đã được gắn đúng và richmenu của friend đã thay đổi đúng theo cấu hình | 1 message text gửi từ LINE app thật, dùng keyword riêng cho testcase; ghi lại webhookEventId/messageId của event thực tế để đối chiếu. | callback_event lưu full body có destination + events và giữ nguyên nội dung message. Job xử lý tới DONE = 2 hoặc 102, không STATUS_ERROR. Message hiển thị đúng tại chat 1:1 của chính friend gửi. Scenario/action, tag và richmenu chạy đúng như trước fix, không thiếu và không chạy trùng. | Đạt |  | STAGING | haodtb | 2026-09-15 |  |  | Studio #17583 (NEW-8) · job · manual · env-safe · REQ: REQ-003 · spec: TICKET-40889, COMMIT-f6104a3 · Đây là regression E2E chính cho event message theo dev impact. Trọng tâm là chứng minh thay đổi format request không làm hỏng downstream chat/scenario/tag/richmenu. · author=AI · status=draft |
| TC-TOOLOLDREC001-01 | TOOL-OLDREC-001 | Normal | Bản ghi định dạng CŨ (chỉ mảng event) vẫn được job xử lý đúng như trước | Như TC bản ghi định dạng mới. Bản ghi định dạng cũ phải được tạo trực tiếp trong cơ sở dữ liệu vì bản đã fix không còn sinh ra định dạng này nữa. | 1. Tạo trực tiếp một bản ghi callback của bot test với cột request là MẢNG event theo định dạng cũ (bắt đầu bằng dấu [), trạng thái chưa xử lý<br>2. Để job xử lý callback chạy, chờ tối đa 30 giây<br>3. Tra lại bản ghi đó: trạng thái, mô tả lỗi<br>4. Kiểm tra kết quả nghiệp vụ tương ứng với event đã tạo | Cột request = [{"type":"message","webhookEventId":"WH-40889-102","timestamp":1789000000100,"source":{"type":"user","userId":"U40889friend01"},"message":{"type":"text","id":"m40889102","text":"<từ khoá trả lời tự động>"}}] | Bản ghi chuyển sang trạng thái đã xử lý xong (2 hoặc 102), mô tả lỗi rỗng, tin nhắn trả lời tự động được phát sinh — hành vi giống hệt bản ghi định dạng mới. Cột request KHÔNG bị hệ thống sửa lại thành định dạng mới (không có backfill). | Đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17584 (NEW-9) · job · auto · local-only · REQ: REQ-004 · spec: TICKET-40889, COMMIT-f6104a3 · Đường tương thích ngược: nhánh không bắt đầu bằng { rơi về logic cũ. Bản ghi loại này tồn tại thật ở môi trường đã chạy trước khi deploy. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-COMPATLEGACY001-01 | COMPAT-LEGACY-001 | Normal | Restart job khi còn row format CŨ ở trạng thái NEW/retry — vẫn parse và xử lý được | Có quyền dừng/khởi động lại job callback trên môi trường test. Chuẩn bị được row callback_event format cũ bắt đầu bằng [..] với trạng thái NEW và trạng thái retry hợp lệ của hệ thống; đồng thời có thể tạo thêm row format mới để đối chứng. | 1. Dừng job callback trước khi tạo dữ liệu<br>2. Tạo nhiều row callback_event format cũ [{...}]: ít nhất 2 row ở trạng thái NEW và 2 row đang chờ retry; các row dùng webhookEventId khác nhau<br>3. Tạo thêm 2 row format mới full body {destination,events:[...]} ở trạng thái NEW làm đối chứng<br>4. Ghi lại id, request và status ban đầu của toàn bộ row<br>5. Khởi động lại job callback<br>6. Chờ job xử lý các row vừa chuẩn bị<br>7. Tra lại status, error và kết quả nghiệp vụ của từng row | Tối thiểu 6 row: 4 row legacy bắt đầu bằng [ (2 NEW + 2 retry) và 2 row full body mới bắt đầu bằng {. Có thể dùng message/postback với mã event riêng để dễ đối chiếu. | Sau restart, mọi row legacy NEW/retry đều được parse bằng logic tương thích cũ và tiếp tục xử lý; không row nào nhảy STATUS_ERROR chỉ vì request bắt đầu bằng [. Các row hợp lệ kết thúc DONE = 2 hoặc 102 và tạo đúng một kết quả nghiệp vụ tương ứng. Row format cũ giữ nguyên nội dung request, không bị backfill sang full body. Các row format mới đối chứng cũng xử lý bình thường. | Không đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17585 (NEW-10) · job · auto · local-only · REQ: REQ-004, REQ-003 · spec: TICKET-40889, COMMIT-f6104a3 · Case này trực tiếp cover yêu cầu dev impact: để lại row cũ [{...}] đang NEW/retry rồi restart job. Nếu retry của môi trường dùng status/flag riêng, seed theo đúng trạng thái retry thực tế thay vì tự gán số không có spec. · author=AI · status=draft |
| TC-REGSHARED001-01 | REG-SHARED-001 | Normal | LINE friend gửi ảnh thật — callback full body, download media và chat 1:1 hoàn tất | Bot test đã deploy fix #40889; LINE friend test đang kết bạn; job callback và job download media đều chạy; tester có thể xem callback_event, media lưu và chat 1:1. | 1. Từ LINE app thật, friend test gửi 1 ảnh cho bot<br>2. Tra callback_event mới sinh theo friend/messageId và kiểm tra cột request là full body bắt đầu bằng {, có destination và event message type=image<br>3. Chờ callback job chuyển record qua luồng download media<br>4. Chờ job media tải file xong và callback_event kết thúc xử lý<br>5. Mở chat 1:1 của đúng friend và kiểm tra ảnh vừa gửi xuất hiện đúng cuộc hội thoại<br>6. Đối chiếu file media đã được lưu/tải thành công theo cơ chế hiện tại | 1 ảnh hợp lệ gửi từ LINE app thật; ghi lại messageId/webhookEventId thực tế. | callback_event lưu full body nguyên trạng có destination + events. Record không STATUS_ERROR và kết thúc DONE = 2/102 sau luồng media. File ảnh được download/lưu thành công; message ảnh hiển thị đúng tại chat 1:1 của friend gửi, không sang phòng chat khác và không tạo trùng. | Đạt |  | STAGING | haodtb | 2026-09-14 |  |  | Studio #17592 (NEW-17) · job · manual · env-safe · REQ: REQ-006 · spec: TICKET-40889, COMMIT-f6104a3 · Cover trực tiếp nhánh download media dùng chung parseCallbackEventData với format full body mới. · author=AI · status=draft |
| TC-RULE09-01 | RULE-09 | Normal | Tin nhắn có media định dạng CŨ — tải được media và hoàn tất xử lý | Như TC tin nhắn có media định dạng mới. | 1. Tạo trực tiếp một bản ghi callback của bot test với cột request là MẢNG event định dạng cũ, chứa 1 event message loại image, trạng thái chưa xử lý<br>2. Chờ bản ghi chuyển sang trạng thái chờ tải media<br>3. Để luồng tải media chạy, chờ tối đa 60 giây<br>4. Tra trạng thái cuối và kiểm tra tệp media, lịch sử hội thoại | Cột request = [{"type":"message","webhookEventId":"WH-40889-111","timestamp":1789000000600,"source":{"type":"user","userId":"U40889friend01"},"message":{"type":"image","id":"m40889111"}}] | Hành vi giống hệt bản ghi định dạng mới: đi qua trạng thái chờ tải media rồi kết thúc ở trạng thái đã xử lý xong, media được tải và lưu, tin nhắn vào lịch sử hội thoại. Không có trạng thái lỗi. | Không đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17593 (NEW-18) · job · auto · local-only · REQ: REQ-006, REQ-004 · spec: TICKET-40889, COMMIT-f6104a3 · Bản ghi media định dạng cũ tồn tại thật ở môi trường đã chạy trước deploy (bản ghi nằm ở hàng đợi media lúc deploy) — kịch bản mất tin nhắn nguy hiểm nhất nếu tương thích ngược sai. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-REGSHARED001-02 | REG-SHARED-001 | Normal | Regression các event còn lại: unsend / join / leave với full body mới | Job callback chạy bản fix #40889. Có dữ liệu/nhóm test phù hợp để tạo event unsend, join và leave. Các event message/postback/follow/unfollow/videoPlayComplete được kiểm ở testcase riêng. | 1. Lần lượt tạo event unsend, join và leave theo luồng LINE thực tế hoặc fixture tích hợp tương đương trên môi trường test<br>2. Với từng event, tra callback_event và xác nhận request là full body có destination + events<br>3. Chờ job callback xử lý<br>4. Tra status cuối và kiểm tra nghiệp vụ tương ứng của từng event | 3 loại event còn lại trong shared parser: unsend, join, leave; mỗi loại dùng webhookEventId riêng. | Cả 3 loại event đều parse được từ full body mới và không rơi STATUS_ERROR do thay đổi format request. Record hợp lệ kết thúc DONE = 2/102; unsend xử lý thu hồi đúng message tương ứng, join/leave cập nhật trạng thái nhóm/phòng đúng như hành vi trước fix. | Đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17595 (NEW-20) · job · auto · local-only · REQ: REQ-003 · spec: TICKET-40889, COMMIT-f6104a3, QA-spec-010 · Các event message/postback/follow/unfollow/videoPlayComplete đã tách thành case E2E riêng để tránh một testcase quá lớn. Case này chỉ giữ regression bổ sung cho các loại event còn lại đi chung parseCallbackEventData. · author=AI · status=draft |
| TC-REGSHARED001-03 | REG-SHARED-001 | Normal | Callback postback thực tế — full body và action postback xử lý tới DONE | Bot test đã deploy fix #40889. Friend test đang kết bạn. Có sẵn một message/template từ OA chứa button postback; action postback được cấu hình để tạo kết quả dễ quan sát như gắn tag hoặc chạy action test. | 1. Gửi message/template có button postback tới friend test<br>2. Từ LINE app thật, friend bấm button postback<br>3. Tra callback_event vừa sinh và xác nhận request là full body có destination + events chứa type=postback<br>4. Chờ job callback xử lý<br>5. Tra status cuối của record và kiểm tra action cấu hình cho postback đã chạy đúng | 1 postback action với data riêng cho testcase, ví dụ qa40889=postback01; ghi lại webhookEventId thực tế. | callback_event lưu full body có destination + events postback. Job parse và xử lý thành công tới DONE = 2/102, không STATUS_ERROR. Action postback chạy đúng đúng một lần và tác động đúng friend. | Đạt |  | STAGING | haodtb | 2026-09-14 |  |  | Studio #17612 (NEW-37) · job · manual · env-safe · spec: TICKET-40889, COMMIT-f6104a3 · Tách riêng event postback để dễ xác định lỗi shared parser theo từng event. · author=haodtb@mcp · status=draft |
| TC-REGSHARED001-04 | REG-SHARED-001 | Normal | Callback follow thực tế — full body và friend được xử lý đúng tới DONE | Bot test đã deploy fix #40889. Có một LINE user test đang ở trạng thái chưa follow OA hoặc có thể đưa về trạng thái cho phép phát sinh follow event thật. | 1. Từ LINE app thật, user test thêm bạn/follow LINE OA<br>2. Tra callback_event phát sinh theo user và xác nhận request là full body có destination + events chứa type=follow<br>3. Chờ job callback xử lý<br>4. Tra status cuối của record<br>5. Kiểm tra trạng thái friend và các xử lý follow hiện có của hệ thống vẫn đúng như trước fix | 1 follow event thật từ LINE user test; ghi lại webhookEventId/userId thực tế. | callback_event lưu full body có destination + event follow. Job xử lý tới DONE = 2/102, không STATUS_ERROR. Friend được tạo/kích hoạt lại đúng theo nghiệp vụ hiện tại; không tạo trùng friend và không chạy trùng xử lý follow. | Chưa test |  | STAGING | haodtb | 2026-09-15 |  |  | Studio #17613 (NEW-38) · job · manual · env-safe · spec: TICKET-40889, COMMIT-f6104a3 · Nếu cần tạo lại follow event, chuẩn bị user test phù hợp thay vì giả lập bằng DB để giữ trigger LINE thật. · author=haodtb@mcp · status=draft |
| TC-REGSHARED001-05 | REG-SHARED-001 | Normal | Callback unfollow thực tế — full body và trạng thái friend được cập nhật tới DONE | Bot test đã deploy fix #40889. LINE user test đang follow/kết bạn với OA và có bản ghi friend tương ứng trong hệ thống. | 1. Từ LINE app thật, user test block/unfollow LINE OA để phát sinh unfollow event<br>2. Tra callback_event phát sinh theo user và xác nhận request là full body có destination + events chứa type=unfollow<br>3. Chờ job callback xử lý<br>4. Tra status cuối của record<br>5. Kiểm tra trạng thái friend trong hệ thống phản ánh đúng unfollow/block theo nghiệp vụ hiện tại | 1 unfollow event thật từ LINE user test; ghi lại webhookEventId/userId thực tế. | callback_event lưu full body có destination + event unfollow. Job xử lý tới DONE = 2/102, không STATUS_ERROR. Friend được cập nhật đúng trạng thái unfollow/block, không ảnh hưởng nhầm friend khác. | Chưa test |  | STAGING | haodtb | 2026-09-15 |  |  | Studio #17614 (NEW-39) · job · manual · env-safe · spec: TICKET-40889, COMMIT-f6104a3 · Case này kiểm regression follow lifecycle sau khi đổi format callback request. · author=haodtb@mcp · status=draft |
| TC-REGSHARED001-06 | REG-SHARED-001 | Normal | Callback videoPlayComplete thực tế — full body và xử lý tới DONE | Bot test đã deploy fix #40889. Có video message từ OA được cấu hình trackingId để LINE có thể phát sinh videoPlayComplete event; friend test nhận được video đó. | 1. Gửi video có trackingId từ OA tới friend test theo luồng hiện tại<br>2. Từ LINE app thật, friend mở và xem video đến hết để phát sinh videoPlayComplete<br>3. Tra callback_event mới và xác nhận request là full body có destination + events chứa type=videoPlayComplete và trackingId tương ứng<br>4. Chờ job callback xử lý<br>5. Tra status cuối và kiểm tra dữ liệu/nghiệp vụ ghi nhận xem hết video vẫn đúng | 1 video có trackingId riêng cho testcase; ghi lại trackingId và webhookEventId thực tế. | callback_event lưu full body có destination + videoPlayComplete event. Job xử lý tới DONE = 2/102, không STATUS_ERROR. trackingId được nhận đúng và nghiệp vụ ghi nhận video đã xem hoàn tất hoạt động như trước fix. | Chưa test |  | STAGING | haodtb | 2026-09-15 |  |  | Studio #17615 (NEW-40) · job · manual · env-safe · spec: TICKET-40889, COMMIT-f6104a3 · Đây là event videoPlayComplete, khác với case friend gửi file video vào bot để download media. · author=haodtb@mcp · status=draft |
| TC-REGSHARED001-07 | REG-SHARED-001 | Normal | LINE friend gửi video thật — callback full body, download media và chat 1:1 hoàn tất | Bot test đã deploy fix #40889. Friend test đang kết bạn. Job callback và download media đang chạy; tester có thể kiểm callback_event, file media và chat 1:1. | 1. Từ LINE app thật, friend test gửi 1 video hợp lệ cho bot<br>2. Tra callback_event mới sinh theo friend/messageId và xác nhận request là full body có destination + events chứa message type=video<br>3. Chờ callback job chuyển record qua luồng download media<br>4. Chờ job media tải video và hoàn tất xử lý<br>5. Mở chat 1:1 của đúng friend và kiểm tra video vừa gửi xuất hiện đúng cuộc hội thoại<br>6. Đối chiếu file video đã được download/lưu thành công | 1 video hợp lệ gửi từ LINE app thật; ghi lại messageId/webhookEventId thực tế. | callback_event lưu full body có destination + events video. Record không STATUS_ERROR và kết thúc DONE = 2/102 sau luồng media. File video được download/lưu thành công; message video hiển thị đúng chat 1:1 của friend gửi, không sang phòng khác và không tạo trùng. | Chưa test |  | STAGING | haodtb | 2026-09-15 |  |  | Studio #17616 (NEW-41) · job · manual · env-safe · spec: TICKET-40889, COMMIT-f6104a3 · Bổ sung riêng video media vì NEW-17 hiện chỉ kiểm ảnh; đây là nhánh download media có rủi ro thực tế tương đương. · author=haodtb@mcp · status=draft |
| TC-I18N001-01 | I18N-001 | Normal | Tin nhắn tiếng Nhật và emoji đi hết chuỗi xử lý mà không bị sai ký tự | Như TC bản ghi định dạng mới. | 1. Gửi qua đường nhận callback của bot test một webhook có nội dung tin nhắn gồm tiếng Nhật, emoji và ký tự toàn rộng<br>2. Để job xử lý callback chạy, chờ tối đa 30 giây<br>3. Kiểm tra nội dung tin nhắn đã lưu trong lịch sử hội thoại của friend test<br>4. Kiểm tra nội dung này ở nơi hiển thị cho người vận hành (màn chat 1:1 nếu môi trường test có ứng dụng web) | Nội dung tin nhắn: 「テスト😊アイウエオ・全角ＡＢＣ」 | Nội dung lưu trong lịch sử hội thoại đúng nguyên văn 「テスト😊アイウエオ・全角ＡＢＣ」: không mất emoji, không biến thành dấu hỏi, không còn dạng mã \uXXXX. Bản ghi callback kết thúc ở trạng thái đã xử lý xong. | Đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17596 (NEW-21) · job · auto · local-only · REQ: REQ-002, REQ-003 · spec: TICKET-40889, COMMIT-f6104a3 · Bước kiểm ở màn chat 1:1 chỉ chạy khi môi trường test có sẵn ứng dụng web trỏ vào cùng cơ sở dữ liệu; nếu không có thì ghi skip cho riêng bước đó và kết luận theo dữ liệu trong lịch sử hội thoại. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-INTGLINE001-01 | INTG-LINE-001 | Abnormal | LINE Developers 「検証」— events rỗng được xử lý thành UNKNOWN_TYPE | Webhook URL của LINE OA test đang trỏ tới receiver đã deploy fix #40889 trên môi trường dev hoặc staging. Có quyền mở LINE Developers của OA và quyền đọc bảng callback_event/log job. | 1. Mở LINE Developers > Messaging API của LINE OA test<br>2. Tại mục Webhook URL, bấm nút 「検証」 để LINE gửi request kiểm tra webhook với mảng events rỗng<br>3. Xác nhận LINE Developers báo webhook URL hợp lệ/response thành công<br>4. Tra bản ghi callback_event phát sinh từ request kiểm tra và kiểm tra cột request lưu full body có destination và events=[]<br>5. Chờ job xử lý callback chạy<br>6. Tra lại status và log của chính bản ghi đó | Request do LINE Developers gửi khi bấm 「検証」: full body có destination và events=[] | LINE Developers xác nhận webhook URL thành công. callback_event lưu full body của request kiểm tra, gồm destination và events=[]. Job không phát sinh lỗi parse/exception; bản ghi kết thúc ở STATUS_UNKNOWN_TYPE = 9 hoặc 109 tùy mode, KHÔNG chuyển STATUS_ERROR = 3/103 và không bị kẹt ở trạng thái đang xử lý. | Chưa test |  | STAGING | haodtb | 2026-09-15 |  |  | Studio #17580 (NEW-5) · job · manual · env-safe · REQ: REQ-001, REQ-005 · spec: TICKET-40889, COMMIT-f6104a3, EP-24 · Case này phải chạy bằng nút 「検証」 thật trên LINE Developers để đúng luồng dev impact. Nếu receiver thực tế không tạo row cho events=[], ghi FAIL/mismatch với REQ-005 thay vì đổi expected theo implementation. · author=AI · status=draft |
| TC-INTGLINE001-02 | INTG-LINE-001 | Abnormal | Bản ghi có mảng event rỗng được đánh trạng thái không xác định loại, không phải lỗi | Như TC bản ghi định dạng cũ. | 1. Tạo trực tiếp một bản ghi callback của bot test với cột request là full body có destination nhưng mảng events rỗng, trạng thái chưa xử lý<br>2. Để job xử lý callback chạy, chờ tối đa 30 giây<br>3. Tra lại trạng thái và mô tả lỗi của bản ghi | Cột request = {"destination":"U40889desttest01","events":[]} | Bản ghi chuyển sang trạng thái không xác định loại (9 hoặc 109). KHÔNG chuyển sang trạng thái lỗi (3/103), cột mô tả lỗi rỗng, không có ngoại lệ nào được ghi log. Bản ghi không bị job lấy lại xử lý vòng sau. | Đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17586 (NEW-11) · job · auto · local-only · REQ: REQ-005 · spec: TICKET-40889, COMMIT-f6104a3 · Nhánh phòng thủ dành cho request LINE kiểm tra URL webhook. Với receiver Java hiện tại bản ghi kiểu này không tự phát sinh nên phải tạo dữ liệu trực tiếp; bản ghi kiểu này vẫn có thể do receiver phía ứng dụng web ghi vào. · author=AI · status=draft |
| TC-TOOLERRHYG001-02 | TOOL-ERRHYG-001 | Abnormal | Bản ghi thiếu hẳn phần events cũng được đánh trạng thái không xác định loại | Như TC bản ghi định dạng cũ. | 1. Tạo trực tiếp 2 bản ghi callback chưa xử lý của bot test: một bản chỉ có destination (không có phần events), một bản có phần events nhưng giá trị rỗng<br>2. Để job xử lý callback chạy, chờ tối đa 30 giây<br>3. Tra trạng thái của cả hai bản ghi | Bản 1: {"destination":"U40889desttest01"} — Bản 2: {"destination":"U40889desttest01","events":null} | Cả hai bản ghi đều chuyển sang trạng thái không xác định loại (9 hoặc 109), không phải trạng thái lỗi, không phát sinh ngoại lệ trong log. | Đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17587 (NEW-12) · job · auto · local-only · REQ: REQ-005 · spec: TICKET-40889, COMMIT-f6104a3 · Hai biến thể cùng một nhánh code nên gộp chung một TC bằng hai dòng dữ liệu thay vì tách hai TC. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-TOOLERRHYG001-03 | TOOL-ERRHYG-001 | Abnormal | Bản ghi có nội dung hỏng chuyển STATUS_ERROR và không làm chết luồng job | Như TC bản ghi định dạng cũ. | 1. Tạo trực tiếp 3 bản ghi callback chưa xử lý của bot test với 3 kiểu nội dung hỏng khác nhau<br>2. Tạo thêm 1 bản ghi hợp lệ định dạng mới ĐẶT SAU 3 bản ghi hỏng<br>3. Để job xử lý callback chạy, chờ tối đa 60 giây<br>4. Tra trạng thái của cả 4 bản ghi và kiểm tra luồng job vẫn tiếp tục xử lý bản ghi hợp lệ | Bản 1: JSON cắt giữa chừng {"destination":"U1","events":[{ — Bản 2: {"destination":"U1","events":"abc"} — Bản 3: {"destination":"U1","events":[123]} — Bản 4: bản ghi hợp lệ định dạng mới, webhookEventId WH-40889-106 | Ba bản ghi hỏng phải rời trạng thái chưa xử lý và chuyển sang STATUS_ERROR = 3 hoặc 103; không bản ghi nào bị kẹt ở trạng thái đang xử lý. Không yêu cầu kiểm cột error_message/mô tả lỗi: cột này hiện chưa được job sử dụng nên để NULL/rỗng vẫn là đúng. Bản ghi hợp lệ đứng sau vẫn phải được xử lý tới DONE = 2 hoặc 102, chứng minh một callback hỏng không làm chết vòng job. | Đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17588 (NEW-13) · job · auto · local-only · REQ: REQ-005 · spec: TICKET-40889, COMMIT-f6104a3 · Dev xác nhận cột error_message hiện chưa dùng đến và job không ghi dữ liệu vào cột này. Vì vậy NEW-13 chỉ đánh giá status + khả năng job tiếp tục xử lý record kế tiếp; error_message = NULL/rỗng không được coi là lỗi. Phân biệt với events rỗng/thiếu: JSON hợp lệ nhưng không có event đi UNKNOWN_TYPE, còn nội dung không parse được/sai shape đi STATUS_ERROR. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-INTGHOOK001-01 | INTG-HOOK-001 | Abnormal | Webhook bị LINE gửi lại — bản ghi trùng bị bỏ qua đúng như trước | Như TC bản ghi định dạng mới. | 1. Gửi một webhook định dạng mới tới đường nhận callback của bot test và chờ bản ghi được xử lý xong<br>2. Gửi lại ĐÚNG webhook đó lần thứ hai (cùng mã event webhook)<br>3. Để job xử lý callback chạy, chờ tối đa 30 giây<br>4. Tra trạng thái của bản ghi thứ hai và kiểm tra kết quả nghiệp vụ có bị nhân đôi không | Cùng một body, webhookEventId WH-40889-112 cho cả hai lần gửi | Bản ghi thứ hai chuyển sang trạng thái bỏ qua do trùng webhook (11 hoặc 111). Kết quả nghiệp vụ KHÔNG bị nhân đôi: chỉ có đúng một tin nhắn trả lời tự động / một bản ghi hội thoại được tạo. | Không đạt |  | LOCAL | haodtb | 2026-09-15 | 40896 |  | Studio #17594 (NEW-19) · job · auto · local-only · REQ: REQ-003 · spec: TICKET-40889, COMMIT-f6104a3 · Kiểm tra chống trùng đọc mã event webhook từ cột riêng, nhưng cột đó do chính addCallback điền nên vẫn cần đối chứng sau khi sửa addCallback. · author=AI · status=draft |
| TC-FUNC004-02 | FUNC-004 | Boundary | Bản ghi có khoảng trắng hoặc xuống dòng ở đầu vẫn được nhận dạng đúng định dạng | Như TC bản ghi định dạng cũ. | 1. Tạo trực tiếp 2 bản ghi callback chưa xử lý của bot test: một bản full body đứng sau vài khoảng trắng và một dấu xuống dòng; một bản mảng event cũ cũng đứng sau khoảng trắng<br>2. Để job xử lý callback chạy, chờ tối đa 30 giây<br>3. Tra trạng thái của cả hai bản ghi | Bản 1: khoảng trắng + xuống dòng rồi tới {"destination":"U40889desttest01","events":[1 event message]} — Bản 2: khoảng trắng + xuống dòng rồi tới [1 event message] | Cả hai bản ghi đều được xử lý tới trạng thái hoàn tất (2 hoặc 102), không bản ghi nào rơi vào trạng thái lỗi. Nội dung event được đọc đúng như khi không có khoảng trắng ở đầu. | Đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17589 (NEW-14) · job · auto · local-only · REQ: REQ-003, REQ-004 · spec: TICKET-40889, COMMIT-f6104a3 · Biên của phép nhận dạng định dạng: bản fix cắt khoảng trắng trước khi xét ký tự đầu. Bỏ bước cắt khoảng trắng trong lần refactor sau sẽ làm bản ghi rơi nhầm nhánh và báo lỗi. · author=AI · status=draft |
| TC-FUNC004-03 | FUNC-004 | Boundary | Bản ghi request NULL/rỗng — chuyển STATUS_ERROR và job vẫn xử lý record kế tiếp | Job callback đang chạy bản fix #40889 và có quyền tạo dữ liệu test trực tiếp trong callback_event. | 1. Tạo 2 row callback_event chưa xử lý: một row request=NULL và một row request là chuỗi rỗng<br>2. Tạo thêm 1 row hợp lệ format mới ngay sau 2 row lỗi<br>3. Chạy/đợi job callback xử lý<br>4. Tra status, error của cả 3 row và xác nhận job vẫn tiếp tục xử lý row hợp lệ | Row 1: request=NULL. Row 2: request=''. Row 3: full body hợp lệ {destination,events:[1 message event]}. | Hai row request NULL/rỗng chuyển STATUS_ERROR = 3 hoặc 103 và có thông tin lỗi phù hợp; không row nào kẹt ở trạng thái đang xử lý. Row hợp lệ phía sau vẫn được xử lý tới DONE = 2 hoặc 102, chứng minh lỗi parse của một row không làm chết vòng job. | Không đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17590 (NEW-15) · job · auto · local-only · REQ: REQ-005 · spec: TICKET-40889, COMMIT-f6104a3 · Oracle được chốt theo REQ-005: nội dung không đọc được phải là STATUS_ERROR. UNKNOWN_TYPE chỉ áp dụng JSON hợp lệ nhưng events rỗng/thiếu. · author=AI · status=draft |
| TC-APICONTRACT001-01 | API-CONTRACT-001 | Normal | Forward webhook Kênh 1 — bên thứ ba nhận đúng nguyên body LINE gồm destination/events và JP/emoji | Môi trường có receiver fix #40889 và job forward Kênh 1 hoạt động cùng nhau. Bot test bật Kênh 1 và endpoint đối tác/test server có thể lưu nguyên văn request body nhận được rồi trả 200. | 1. Xóa log/request cũ trên endpoint đối tác/test server<br>2. Chuẩn bị một callback body cố định có destination, events, một field top-level chưa map và text tiếng Nhật + emoji; giữ nguyên chính xác chuỗi body này để đối chiếu<br>3. Gửi nguyên body đó vào receiver callback của bot test<br>4. Tra callback_event và xác nhận cột request lưu đúng full body vừa gửi<br>5. Chờ callback job xử lý record tới DONE<br>6. Chờ job forward Kênh 1 gửi record sang endpoint đối tác<br>7. Đọc nguyên văn request body mà endpoint đối tác nhận được và so sánh với body ban đầu | Ví dụ body: {"events":[{"type":"message","webhookEventId":"WH-40889-FWD-01","source":{"type":"user","userId":"U40889friend01"},"message":{"type":"text","id":"m40889F01","text":"転送テスト😊あいうえお"}}],"destination":"U40889desttest01","futureField":"keep-me"}. Khi chạy phải giữ nguyên thứ tự key và nguyên chuỗi thực tế đã gửi. | Endpoint đối tác nhận đúng 1 request. Request body nhận được phải giống nguyên body callback ban đầu: có destination, events vẫn là mảng event gốc, futureField vẫn còn, thứ tự/key và nội dung không bị serialize lại; không bị bọc thêm object trung gian/double-wrap. Chuỗi 「転送テスト😊あいうえお」 xuất hiện nguyên văn, không thành \\uXXXX và emoji không hỏng. callback_event đã DONE trước khi forward và trạng thái forward kết thúc thành công. | Chưa test |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17597 (NEW-22) · job · auto · local-only · REQ: REQ-007 · spec: BR-01, TICKET-40889, TICKET-40475 · Case này gộp oracle forward raw body + JP/emoji để tránh trùng NEW-24. Nếu bên nhận thấy dạng {bot_id,...,events:{destination,events:[...]}} thì FAIL theo BR-01 vì đã bị double-wrap. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-MEDIA001-01 | MEDIA-001 | Normal | Event có media vẫn được chuyển tiếp và không kèm theo tệp | Như TC chuyển tiếp bản ghi định dạng mới. | 1. Xoá sạch bản ghi đã nhận trên máy chủ nhận thử<br>2. Gửi một webhook định dạng mới chứa event message loại image và chờ bản ghi callback xử lý xong (bao gồm cả lượt tải media)<br>3. Để job chuyển tiếp chạy, chờ tối đa 60 giây<br>4. Đọc thân request máy chủ nhận thử ghi lại | 1 event message loại image, messageId m40889208, webhookEventId WH-40889-208 | Bản ghi vẫn được chuyển tiếp (trạng thái đã chuyển tiếp xong). Thân request chứa thông tin event media với mã nội dung m40889208, KHÔNG chứa dữ liệu tệp và không có phần nào là dữ liệu nhị phân — đúng theo QA-012. | Chưa test |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17604 (NEW-29) · job · auto · local-only · REQ: REQ-007, REQ-008 · spec: QA-012, BR-01 · Kiểm thêm trong log của job: không được in nguyên thân request ra log vì payload chứa nội dung tin nhắn của friend. Nếu thấy thì ghi thành ghi chú rủi ro lộ dữ liệu, không tính là fail của ticket này. · author=AI · status=draft |
| TC-JOB002-01 | JOB-002 | Abnormal | Regression forward Kênh 1 lỗi — sau retry cuối hiển thị đúng 1 bản ghi trong bảng lỗi | Bot test đã bật Forward Kênh 1 và trỏ tới endpoint test có thể chủ động trả lỗi HTTP (ví dụ 500). Có quyền xem callback_event/status_sync và bảng lỗi webhook_relay_error. | 1. Ghi lại số bản ghi lỗi hiện có của bot test<br>2. Tạo một callback hợp lệ định dạng full body mới và chờ callback_event xử lý tới DONE = 2 hoặc 102<br>3. Để job forward Kênh 1 gửi request sang endpoint test đang trả lỗi<br>4. Chờ job retry tới lần cuối theo cơ chế hiện tại<br>5. Tra lại status_sync của callback_event và danh sách/bảng lỗi webhook_relay_error<br>6. Đối chiếu callback_event_id, event type và HTTP status của bản ghi lỗi | 1 callback hợp lệ full body mới (message event). Endpoint test trả HTTP 500 cho mọi lần gọi. | Job forward có thực hiện retry theo cơ chế hiện tại; sau lần thất bại cuối, callback_event có status_sync = 3. Bảng lỗi/webhook_relay_error phát sinh ĐÚNG 1 bản ghi cho callback này, không tạo bản ghi ở từng lần retry trung gian. Bản ghi lỗi trỏ đúng callback_event_id, đúng Kênh 1/event type và lưu HTTP status = 500. Các callback khác vẫn tiếp tục được job xử lý bình thường. | Chưa test |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17600 (NEW-25) · job · auto · local-only · REQ: REQ-008 · spec: TICKET-40889, TICKET-40475, BR-01 · Giữ ở task #298 như regression tối thiểu cho luồng forward sau khi callback_event.request đổi sang full body. Không kiểm chi tiết timeout/backoff interval vì đó là scope sâu của #40475. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-TOOLNEGCTRL001-01 | TOOL-NEGCTRL-001 | Abnormal | Bot chưa bật chuyển tiếp — không gọi endpoint và không ghi bản ghi lỗi | Như TC chuyển tiếp bản ghi định dạng mới, nhưng bot test TẮT chuyển tiếp Kênh 1 (hoặc để trống địa chỉ đích). | 1. Xoá sạch bản ghi đã nhận trên máy chủ nhận thử<br>2. Tạo một bản ghi callback định dạng mới ở trạng thái đã xử lý xong và chưa chuyển tiếp<br>3. Để job chuyển tiếp chạy, chờ tối đa 60 giây<br>4. Kiểm tra máy chủ nhận thử, trạng thái chuyển tiếp của bản ghi và danh sách bản ghi lỗi | 1 bản ghi callback định dạng mới, webhookEventId WH-40889-206; cấu hình chuyển tiếp Kênh 1 của bot ở trạng thái tắt | Máy chủ nhận thử KHÔNG nhận request nào. Bản ghi chuyển sang trạng thái bỏ qua (4) để không bị quét lại. KHÔNG phát sinh bản ghi lỗi chuyển tiếp nào. | Chưa test |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17602 (NEW-27) · job · auto · local-only · REQ: REQ-008 · spec: TICKET-40475 · Đối chứng âm cho trục cấu hình: dữ liệu của bot không bật phải dừng lại trước khi ra khỏi hệ thống. Cũng là chốt chặn cho rủi ro lộ dữ liệu cá nhân của friend. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-STATE001-01 | STATE-001 | Abnormal | Job bị dừng giữa chừng — bản ghi đang chuyển tiếp dở được nhặt lại đúng một lần | Như TC chuyển tiếp bản ghi định dạng mới. Có quyền dừng và khởi động lại tiến trình job của môi trường test. | 1. Dừng job chuyển tiếp<br>2. Tạo một bản ghi callback định dạng mới ở trạng thái đã xử lý xong và đặt trạng thái chuyển tiếp = đang chuyển tiếp (mô phỏng bản ghi kẹt khi tiến trình chết giữa chừng)<br>3. Xoá sạch bản ghi đã nhận trên máy chủ nhận thử rồi khởi động lại job chuyển tiếp<br>4. Chờ tối đa 90 giây, kiểm tra máy chủ nhận thử và trạng thái chuyển tiếp của bản ghi | 1 bản ghi callback định dạng mới, webhookEventId WH-40889-210, trạng thái chuyển tiếp đặt sẵn = đang chuyển tiếp (1) | Sau khi khởi động lại, bản ghi được đưa trở lại hàng chờ và chuyển tiếp thành công: máy chủ nhận thử nhận ĐÚNG MỘT request cho bản ghi này, trạng thái chuyển tiếp cuối = đã chuyển tiếp xong (2). Bản ghi không kẹt vĩnh viễn ở trạng thái đang chuyển tiếp. | Chưa test |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17606 (NEW-31) · job · auto · local-only · REQ: REQ-008 · spec: TICKET-40475 · Đúng một request là điểm cần soi kỹ: cơ chế nhặt lại bản ghi kẹt vừa phải cứu được bản ghi vừa không được gửi lặp sang khách. · author=AI · status=draft |
| TC-ENV003-01 | ENV-003 | Boundary | Chỉ bản ghi đã xử lý xong mới được chuyển tiếp — kiểm cả hai dải mã trạng thái | Như TC chuyển tiếp bản ghi định dạng mới. Đã ghi lại chế độ chạy của tiến trình job (thường hay BACKEND). | 1. Xoá sạch bản ghi đã nhận trên máy chủ nhận thử<br>2. Tạo 6 bản ghi callback định dạng mới của bot test, mỗi bản một trạng thái khác nhau theo bảng dữ liệu, tất cả đều chưa chuyển tiếp<br>3. Để job chuyển tiếp chạy, chờ tối đa 60 giây<br>4. Đối chiếu số request máy chủ nhận thử nhận được với danh sách bản ghi và trạng thái chuyển tiếp của từng bản ghi | 6 bản ghi với trạng thái xử lý lần lượt: 2 (đã xử lý xong, dải thường) · 102 (đã xử lý xong, dải BACKEND) · 0 (chưa xử lý) · 1 (đang xử lý) · 3 (lỗi) · 9 (không xác định loại) | Chỉ hai bản ghi mang trạng thái 2 và 102 được gửi sang máy chủ nhận thử (đúng 2 request) và chuyển sang trạng thái đã chuyển tiếp xong. Bốn bản ghi còn lại KHÔNG được gửi và giữ nguyên trạng thái chưa chuyển tiếp — không bị đánh dấu bỏ qua. | Chưa test |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17605 (NEW-30) · job · auto · local-only · REQ: REQ-008 · spec: TICKET-40475, TICKET-40889 · Quét cả 2 giá trị là bắt buộc vì các tiến trình chạy ở chế độ khác nhau cùng ghi vào một bảng: chế độ BACKEND ghi dải 100 nên 'đã xử lý xong' là 102. Bỏ sót một giá trị sẽ mất nguyên nửa dữ liệu mà không có lỗi nào hiện ra. · author=AI · status=draft |
| TC-DATAMIG001-01 | DATA-MIG-001 | Normal | Cấu trúc bảng callback_event không đổi sau khi triển khai bản sửa | Đã lưu lại bản mô tả cấu trúc bảng callback_event trước khi triển khai (danh sách cột, kiểu dữ liệu, cho phép rỗng hay không, chỉ mục). | 1. Lấy lại bản mô tả cấu trúc bảng callback_event sau khi triển khai bản sửa<br>2. So sánh với bản mô tả đã lưu trước khi triển khai: danh sách cột, kiểu dữ liệu, giá trị mặc định, chỉ mục | Không có dữ liệu nhập — chỉ đọc cấu trúc bảng của cơ sở dữ liệu mà dịch vụ xử lý callback đang dùng | Cấu trúc bảng giống hệt trước khi triển khai: không thêm cột, không bớt cột, không đổi kiểu dữ liệu của cột request, không thêm/bớt chỉ mục. Đúng với khẳng định của dev là ticket này không thay đổi cấu trúc dữ liệu, chỉ đổi định dạng giá trị lưu trong cột request. | Đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17607 (NEW-32) · data · auto · read-only · REQ: REQ-009 · spec: TICKET-40889, COMMIT-f6104a3 · Nếu phát hiện có cột mới (ví dụ cột trạng thái chuyển tiếp) thì đó là phần của ticket chuyển tiếp webhook #40475 chứ không phải #40889 — ghi rõ trong kết quả để không quy nhầm trách nhiệm. · author=AI · status=draft |
| TC-TOOLOLDREC001-02 | TOOL-OLDREC-001 | Normal | Bản ghi cũ giữ nguyên định dạng — không có backfill dữ liệu | Đã ghi lại trước khi triển khai: mã và nội dung cột request của một tập bản ghi mẫu (ít nhất 10 bản ghi cũ, gồm cả bản ghi đã xử lý xong và bản ghi đang chờ tải media). | 1. Sau khi triển khai và để hệ thống chạy ít nhất 30 phút, đọc lại chính các bản ghi mẫu đã ghi nhận trước triển khai<br>2. So sánh nội dung cột request của từng bản ghi với bản đã lưu<br>3. Đếm số bản ghi được tạo TRƯỚC thời điểm triển khai có cột request bắt đầu bằng dấu { | Tập bản ghi mẫu đã chụp trước triển khai (kèm mã bản ghi và thời điểm tạo) | Nội dung cột request của mọi bản ghi cũ giữ nguyên từng ký tự, vẫn ở định dạng mảng bắt đầu bằng dấu [. Số bản ghi tạo trước thời điểm triển khai mà có cột request bắt đầu bằng dấu { = 0. Không có bất kỳ thao tác cập nhật hàng loạt nào chạm vào dữ liệu cũ. | Đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17608 (NEW-33) · data · auto · read-only · REQ: REQ-009, REQ-004 · spec: TICKET-40889, COMMIT-f6104a3 · Đối chứng cho quyết định 'không backfill'. Nếu phát hiện dữ liệu cũ bị sửa thì đó là rủi ro nghiêm trọng: bản ghi đang chờ xử lý có thể bị sửa sai giữa chừng và mất tin nhắn. · author=AI · status=draft · ⚠️ mã quan điểm KHÔNG có trong checklist-lme |
| TC-DATADB001-01 | DATA-DB-001 | Normal | Bản ghi phát sinh sau khi triển khai đều ở định dạng đầy đủ | Đã ghi lại chính xác thời điểm triển khai bản sửa lên môi trường đang kiểm tra. Hệ thống đã nhận webhook thật ít nhất 30 phút sau thời điểm đó. | 1. Đếm số bản ghi tạo SAU thời điểm triển khai, tách theo ký tự đầu của cột request (dấu { và dấu [)<br>2. Lấy ngẫu nhiên 5 bản ghi tạo sau thời điểm triển khai và kiểm tra từng bản có chứa phần destination hay không | Mốc thời gian triển khai của môi trường đang kiểm tra | Toàn bộ bản ghi tạo sau thời điểm triển khai có cột request bắt đầu bằng dấu { — số bản ghi bắt đầu bằng dấu [ sau mốc này phải bằng 0. Cả 5 bản ghi lấy mẫu đều có phần destination. | Đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17609 (NEW-34) · data · auto · read-only · REQ: REQ-009, REQ-001 · spec: TICKET-40889, COMMIT-f6104a3 · Cách kiểm nhanh 'fix có thực sự đang chạy ở môi trường này không' trước khi chạy các nhóm TC khác. Nếu vẫn còn bản ghi định dạng cũ sinh ra sau triển khai thì rất có thể webhook đang đi vào receiver khác (ứng dụng web) — phải xác nhận trước khi báo bug cho #40889. · author=AI · status=draft |
| TC-REGRUN001-01 | REG-RUN-001 | Abnormal | Trước khi bật chuyển tiếp — không còn bản ghi lịch sử nào chưa được đánh dấu bỏ qua | Môi trường đã chạy bước thêm cột trạng thái chuyển tiếp và đã chạy script đánh dấu dữ liệu cũ, NHƯNG chưa bật cờ tính năng chuyển tiếp. Có quyền đọc bảng callback_event. | 1. Đếm số bản ghi callback ở trạng thái đã xử lý xong (cả hai dải 2 và 102) mà trạng thái chuyển tiếp còn để trống<br>2. Trong số đó, đếm riêng các bản ghi được tạo TRƯỚC thời điểm chạy script đánh dấu<br>3. Đối chiếu với thứ tự triển khai bắt buộc: thêm cột → chạy script đánh dấu → thêm cấu hình → mới bật cờ | Mốc thời gian chạy script đánh dấu dữ liệu cũ của môi trường đang kiểm tra | Số bản ghi tạo trước thời điểm chạy script mà trạng thái chuyển tiếp còn để trống phải bằng 0. Nếu còn lớn hơn 0 thì TUYỆT ĐỐI KHÔNG được bật cờ chuyển tiếp: job sẽ gửi ngược toàn bộ lịch sử webhook sang endpoint của khách, dữ liệu đã gửi không thu hồi được. | Chưa test |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17611 (NEW-36) · data · auto · read-only · REQ: REQ-010 · spec: RR-06, TICKET-40475 · Thuộc quy trình triển khai của #40475 (rủi ro RR-06), không phải mã nguồn của #40889 — giữ lại đúng một TC vì hậu quả không thể đảo ngược và chỉ tốn một câu truy vấn. Trên môi trường thật phải chạy như chốt chặn bắt buộc ngay trước khi bật cờ. · author=AI · status=draft |
| TC-FUNC004-04 | FUNC-004 | Boundary | Sức chứa của cột request đủ cho webhook lớn nhất ở định dạng đầy đủ | Có quyền đọc cấu trúc bảng và thống kê dữ liệu trên cơ sở dữ liệu mà dịch vụ xử lý callback đang dùng. | 1. Đọc kiểu dữ liệu thật của cột request và ghi lại trần dung lượng tương ứng<br>2. Đo độ dài lớn nhất của cột request trong các bản ghi hiện có và so với trần dung lượng<br>3. Kiểm tra chế độ ghi dữ liệu của máy chủ cơ sở dữ liệu có báo lỗi khi vượt trần hay cắt cụt âm thầm | Không có dữ liệu nhập — chỉ đọc cấu trúc và thống kê. Theo bản dump hiện có, cột request đang là kiểu TEXT với trần 65.535 byte. | Ghi rõ kiểu cột, trần dung lượng và độ dài lớn nhất đang có. Khoảng trống còn lại phải lớn hơn phần bao ngoài mà định dạng mới thêm vào (khoảng 55-60 byte). Nếu độ dài lớn nhất đã sát trần hoặc máy chủ đang ở chế độ cắt cụt âm thầm thì kết luận là RỦI RO CẦN XỬ LÝ và báo leader: webhook lớn sẽ bị lưu thiếu và trở thành bản ghi lỗi. | Đạt |  | LOCAL | haodtb | 2026-09-15 |  |  | Studio #17610 (NEW-35) · data · auto · read-only · REQ: REQ-009 · spec: TICKET-40889, COMMIT-f6104a3 · Cặp đôi với TC biên ở nhóm receiver: TC này đánh giá rủi ro trên dữ liệu thật của từng môi trường, TC kia thử trực tiếp bằng webhook sát trần trên môi trường cho phép ghi. · author=AI · status=draft |
