# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | `MCP LME TEST STUDIO — task #280 (ticket 40050, round 1, branch ai_fixbug_40050)` |
| Tổng số TC review | `80` |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Trả lời:**

| Chiều | Kết quả |
|---|---|
| **(a) dev-impact** — Dev tự kê ở `03-dev-impact.md` mục 4 | `11/18 mục có TC đủ chiều` — **CHƯA ĐỦ** |
| **(b) diff code** — suy từ diff thật (Studio `dev_impact` + `spec_delta`, 17 file / +1018 −202) + **danh mục 114 endpoint** (`feature-spec.md` §9.1) | `19/26 điểm có TC` — **CHƯA ĐỦ** |

**Kết luận**: `30/44 vùng ảnh hưởng đủ TC · 9 GAP · 5 RISK` (cập nhật 2026-09-22: `G10` sau quyết định C4; `G11`–`G14` sau lượt rà **danh mục endpoint + màn hình**).

> 📐 **Cách đo `G11`**: danh mục 114 endpoint lấy từ `feature-spec.md` §9.1 (A 65 · B 17 · LIFF 17 · app mobile 15). ⚠️ **Không dùng `spec_ids` của TC làm chỉ số coverage** — 29/31 TC dẫn sai mã (§5 `I12`), nên 43 endpoint "thiếu" theo `spec_ids` thực ra **33 cái đã có TC** chạm tới qua hành vi (vd `EP-15` xoá khoá học ← `NEW-42`/`NEW-44`; `EP-63` xoá nhiều khung giờ ← `NEW-12`). Con số `10` ở `G11` là kết quả sau khi map lại **theo hành vi** của cả 95 TC.

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | `resources/views/basic/setting.blade.php` — view **dùng chung nằm ngoài** thư mục `calendar_management/`, được thêm dòng nhúng `ajax-error.js` | `diff code` (+ `F7`) | `không có` | `GAP — 0/80 TC chạm màn nào khác dùng view này. Hàm toàn cục mới `window.showLessonAjaxError` nạp trên các màn ngoài Lesson mà không ai verify` | `[MAJOR]` |
| G2 | `public/js/calendar_management/setting-payment.js` — màn 「決済連携」 của lịch bài học | `diff code` | `không có` | `GAP — danh sách 15 màn hình của bộ TC không có màn 決済連携; NEW-55 chỉ chạm nhánh hoàn tiền, không chạm luồng LƯU cấu hình thanh toán` | `[MAJOR]` |
| G3 | IDOR: `changeStatusBooking` / `updateBooking` / `checkBookingCancel` — 3 endpoint Studio `dev_impact` **liệt kê đích danh** là đã vá | `diff code` | `không có` | `GAP — nhóm truy cập chéo (NEW-23…36) không có ca nào đổi trạng thái / huỷ đơn của bot khác. Đây là nhóm ghi đổi trạng thái đơn + tiền` | `[BLOCKER]` |
| G4 | `settingBookingDisplay` / `getBookingSettingDisplay` — 「予約ページの表示設定」 **cấp lịch** (Studio `dev_impact` nêu riêng) | `diff code` | `NEW-32` (chỉ bật/tắt hiển thị **khoá học**) | `GAP — cấu hình cấp lịch chưa có ca truy cập chéo nào` | `[MAJOR]` |
| G5 | `T2 — Friend Information`: `saveInfoFormBooking` ghi hồ sơ bạn bè; `D2` đổi scope truy vấn bảng bạn bè qua bảng trung gian | `dev-impact` | `NEW-31` (chỉ nhánh **chặn** bot khác) | `GAP — 0 TC cho luồng THUẬN: sửa câu trả lời form của đơn thuộc chính bot → giá trị có còn ghi đúng vào hồ sơ bạn bè + lịch sử thay đổi không` | `[BLOCKER]` |
| G6 | `D5 — bổ sung ghi log ở 8 chỗ vốn không có` + nhánh lỗi hệ thống trả câu tiếng Nhật chung | `dev-impact` | `NEW-19` | `RISK — fix dạng xử lý lỗi chung nhưng TC chỉ trigger 1 nhánh (tràn cột số tiền). Cần ≥ 3 trigger khác nhau + 1 trigger chưa biết để test fallback` | `[MAJOR]` |
| G7 | `T3 / T4` + rủi ro hồi quy (a) của Studio — job nền `NewEventRemindTask`, API v1, app quản trị mobile sau khi siết phạm vi bot | `dev-impact` + `diff code` | `NEW-66` (ghi hàng đợi), `NEW-49` (fail), `NEW-67` (**chưa chạy**) | `RISK — dừng ở tầng DB, không TC nào verify tin nhắn nhắc thật sự tới LINE của khách (RULE-06); TC app mobile duy nhất chưa chạy` | `[MAJOR]` |
| G8 | `T4` — luồng khách LINE đặt lịch (LIFF) sau khi siết phạm vi | `dev-impact` | `NEW-64` (**fail**), `NEW-65`, `#18284`, `#18285` | `RISK — ca hồi quy quan trọng nhất của phát biểu "cố ý không ràng buộc bot ở luồng dùng chung" đang fail (bug #1190)` | `[MAJOR]` |
| G9 | `D4` — dữ liệu con mồ côi để lại từ bản dựng `adad1a4191` | `dev-impact` | `NEW-41`, `NEW-43` (**đều fail**) | `RISK — 2 ca quét mồ côi đều fail nên chưa có bằng chứng DB sạch; chưa có thủ tục dọn dữ liệu cũ trước khi test lại (RULE-04)` | `[MAJOR]` |
| G10 | Server có chặn **xoá khoá học khi còn đơn 予約確定 / リクエスト** không — `BR-30` ghi rule này **chỉ kiểm ở FRONT-END** | `diff code` | `NEW-44` (nhóm `ui`, chỉ bấm nút trên màn hình) | `GAP — 0 TC gọi thẳng endpoint xoá khoá học để kiểm server. Leader đã chốt đây là **business rule** (2026-09-22), mà ticket này chính là đợt siết tầng API; kho `TC-LSN-613` cũng dự kiến FAIL đúng điểm này` | `[BLOCKER]` |
| G11 | **10 endpoint nhóm A+B chưa TC nào chạm** — `EP-39` đơn trong 1 khung giờ · `EP-40` lọc đơn tuỳ biến · `EP-62` đơn gần nhất của 1 LINE user · `EP-22` danh sách trường hồ sơ bạn bè · `EP-64` kiểm tra có thể xoá khung giờ · `EP-65` tắt tooltip · `EP-59/60/61` 3 màn preview · `EP-01` callback OAuth Google Sheets | `diff code` | `không có` | `GAP — yêu cầu (3) của ticket áp cho TOÀN BỘ màn quản trị (82 endpoint nhóm A+B); 10 endpoint này nằm trong đó mà chưa ai xác nhận đã siết đúng` | `[MAJOR]` |
| G12 | **Đối chứng dương quá hẹp** — `NEW-21` chỉ gọi 6 endpoint hợp lệ trên tổng 82 | `diff code` | `NEW-21`, `NEW-76`, `NEW-1` | `RISK — rủi ro số 1 do chính Dev nêu là "validate required chặn nhầm luồng thật" và "QA nên bấm lại toàn màn"; phân bố loại case đang lệch nặng 54 Abnormal / 20 Normal / 6 Boundary ⇒ bộ TC chứng minh "chặn đúng" nhiều hơn "chạy vẫn đúng"` | `[BLOCKER]` |
| G13 | **9/26 màn admin web chưa có TC** — `SCR-LSN-03` khoá học đầu tiên · `SCR-LSN-05` tab 本日／新着の予約 · `SCR-LSN-11/12/13` 3 màn cấu hình 全体設定 (メッセージ・締切 / 承認方式 / キャンセル時) · `SCR-LSN-18` トップ画面設定 · `SCR-LSN-24/25/26` 3 màn preview | `dev-impact` (`T1`) | `không có` | `GAP — T1 khai "toàn bộ màn quản trị lịch bài học" nhưng 1/3 số màn không có TC nào` | `[MAJOR]` |
| G14 | **App mobile admin: 0/15 endpoint có kết luận** | `dev-impact` (`T4`) | `NEW-67` (**skip** qua cả 4 run), `NEW-85` (vừa tạo, chưa chạy) | `GAP — bảng `mobile_notify` nằm TRONG chuỗi xoá dây chuyền mà ticket sửa lại thứ tự, nhưng không có kết luận test nào từ phía app` | `[BLOCKER]` |

> ⚠️ Các vùng **đã đủ TC** (không ghi dòng): `BUG` root cause · `F1` validateInput 24 endpoint · `F2` 113 nhánh httpCode · `F3` 96 điểm ràng buộc bot · `F4`+`D3` thứ tự chuỗi xoá · `F5` service httpCode · `F6` 78 nhánh JS · `F8` thông báo tiếng Nhật · `D1` · `D2` · `T1` · và các điểm diff còn lại (cache asset, validate chặn nhầm bản ghi cũ, hợp đồng 200→4xx).

---

## 2. Thiếu so với quan điểm test

**Kết luận**: `26 quan điểm Trigger khớp task · 9 chưa cover đủ`.

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `PERM-001` | `Cao` | `GAP — 0/80 TC dùng tài khoản Staff. Fix siết theo BOT nhưng không chạm lưới phân quyền theo ROLE; spec ghi nhóm route /ajax/calendar/* (17 EP) nằm NGOÀI basic_access (A-01) và 0/82 endpoint có checkHasPermission` | `[BLOCKER]` |
| Q2 | `INTG-SHEET-001` | `Cao` | `GAP — addNewBooking / changeStatusBooking / saveInfoFormBooking đều nằm trong diff và đều đẩy dữ liệu lên Google Sheet, nhưng 0 TC verify Sheet sau fix` | `[BLOCKER]` |
| Q3 | `DATA-AUDIT-001` | `Cao` | `GAP — Studio dev_impact nêu bảng "lịch sử thao tác đơn" nay bị chặn qua truy vấn con. 0 TC verify lịch sử thao tác đơn + lịch sử hoàn tiền còn ghi/đọc đủ` | `[BLOCKER]` |
| Q4 | `PAY-STATE-001` | `Cao` | `RISK — có NEW-28 (chéo bot) + NEW-72 (gateway lỗi, fail); thiếu nhánh hoàn tiền LẦN HAI trên đơn đã hoàn và nhánh refundType không nằm trong danh sách cho phép` | `[MAJOR]` |
| Q5 | `ENV-003` ★ | `Cao` | `RISK — 0/80 TC chạy product (3 run đều env local) trong khi task chạm bill tiền, CSV import, asset version, job nền (RULE-08)` | `[MAJOR]` |
| Q6 | `CONC-001` | `Cao` | `RISK — chỉ #18287 (bấm liên tục tạo item). Sau khi lỗi nghiệp vụ đổi 200→404, lần bấm thứ HAI của nút xoá / duyệt sẽ rơi vào nhánh lỗi và bật thông báo ngay sau thao tác đã thành công — đúng khuôn lỗi Dev đã phải sửa ở vòng refix 1` | `[MAJOR]` |
| Q7 | `BULK-001` | `Cao` | `RISK — NEW-61 cover tập đơn hỗn hợp; thiếu nhánh "thao tác hàng loạt thất bại giữa chừng" sau khi fix bổ sung 5 chỗ trước đây nuốt kết quả thất bại` | `[MAJOR]` |
| Q8 | `NOTI-MAIL-001` ★ | `Trung bình → nâng Cao (mail chứa mã xác thực)` | `RISK — sendMailCodeAuthDeleteCalendar nằm trong diff; NEW-5 chỉ test nhánh mã SAI, 0 TC verify mail thật vẫn gửi được và mã đúng vẫn dùng được` | `[MAJOR]` |
| Q9 | `MSG-002` / `JOB-001` | `Cao` | `RISK — xem G7: dừng ở hàng đợi event_step_time, không verify output cuối trên LINE thật (RULE-06)` | `[MAJOR]` |

> **Đã loại khỏi phạm vi (Trigger khớp nhưng có bằng chứng không bị ảnh hưởng)**:
> - `DATA-BACKUP-001` — fix không thêm/đổi bảng DB, không chạm copy bot. Kho `fa033-backup` + spec FA-033 không liệt kê lịch bài học trong 13 loại dữ liệu được copy ⇒ không đẻ TC backup.
> - `INTG-CAL-001` — `feature-spec.md` §1.2 xác nhận **FA-019 không có bất kỳ tích hợp Google Calendar nào** (2 nguồn độc lập: `job-spec.md` §12 + `db-mapping.md` §0 — 174/174 bản ghi `google_calendar_id` đều NULL).
> - `DATA-MIG-001` — `03-dev-impact.md` mục 4.2: không đổi cấu trúc bảng, không chạy migration.

---

## 3. TC trùng lặp nội dung

Đã rà **80** TC. Phát hiện **3 nhóm trùng**, cả 3 đều là TC `vinth@mcp` (`status = draft`) chồng lên TC pipeline đã có.

| Nhóm trùng | TC giữ lại | TC đề nghị xóa / gộp | Loại trùng | 4 yếu tố trùng nhau | Severity |
|---|---|---|---|---|---|
| DUP-1 | `NEW-42` | `NEW-48` **và** `NEW-71` | `DUP-SUBSET` | `DATA-HIST-001` × `Normal` × `xoá khoá học → đọc lại đơn liên quan` × `khoá học có đơn active + đơn xoá mềm`. Ban đầu chỉ `NEW-71` trùng `NEW-48`; **sau quyết định C3** (*không cần snapshot*), phần còn lại của `NEW-48` là *"đơn vẫn xem được ở 「削除済み予約」 + lịch sử ghi lý do xoá"* — đã nằm trọn trong `NEW-42` | `[MINOR]` |
| DUP-2 | `NEW-42` + `NEW-43` | `NEW-70` | `DUP-SUBSET` | `DATA-DB-001` × `Normal` × `xoá khoá học → quét dữ liệu con` × `khoá học đầy đủ dữ liệu con` → cùng expected "không còn reception / booking / history / lịch gửi nhắc mồ côi". `NEW-42` (thao tác + DB) và `NEW-43` (quét mồ côi độc lập) đã bao trọn | `[MINOR]` |
| DUP-3 | `NEW-23…36` + `NEW-38` | `NEW-73` | `DUP-SUBSET` | `PERM-002/003` × `Abnormal` × `gọi endpoint với ID của bot khác` × `bot A đăng nhập, dữ liệu bot B` → cùng expected "không trả / không đổi dữ liệu bot B". `NEW-73` không nêu endpoint nào nên không thêm chiều nào so với 14 ca riêng + oracle tổng `NEW-38` | `[MINOR]` |

- **Gate đã chạy (chạy lại sau quyết định C3/C4)**: giả định xoá `NEW-48`, `NEW-70`, `NEW-71`, `NEW-73` → chạy lại BƯỚC 2 + 3 trên 76 TC còn lại: `DATA-DB-001` còn `NEW-40/41/42/43`; `PERM-003` còn `NEW-24/32/36/37`. Riêng `DATA-HIST-001` còn **0 TC** — chấp nhận được vì (a) đó là mã nội bộ của Studio, không có trong `checklist-lme.md` nên không tính coverage quan điểm, và (b) Leader đã chốt snapshot **ngoài phạm vi** ticket này (C3). → **Điều kiện kèm theo**: trước khi xoá `NEW-48`, phải chuyển vế *"đơn vẫn xem được ở 「削除済み予約」"* sang `NEW-42` (đây là **gộp**, không phải xoá trắng).
- Không có `DUP-INFLATE` — không nhóm trùng nào đang che GAP ở §1 / §2.
- Xoá thật do human thực hiện trên Studio (`testcase_delete`). ⚠️ `NEW-70` và `NEW-73` đang ở trạng thái **fail chưa có bug** (xem §5 I2) — xử lý bug trước rồi mới xoá, đừng xoá mất dấu vết.

---

## 4. Mâu thuẫn trong TCs

> BƯỚC 4c. `CONF-TC` = 2 TC cùng đối tượng + thao tác + tiền đề nhưng expected loại trừ nhau · `CONF-SPEC` = expected trái Business rule ở `spec-features/` · `CONF-KHO` = expected trái TC ở `kho-tcs/`.
> **KHÔNG tự chọn bên** — mỗi mâu thuẫn nêu đủ 2 khả năng. Chi tiết từng dòng ở các khối **C1 … C7** bên dưới bảng.

| # | Loại | TC liên quan | Vấn đề | Severity | Trạng thái |
|---|---|---|---|---|---|
| C1 | `CONF-SPEC` | `NEW-22` | Xem lịch của bot khác: TC đòi **404**, spec ghi middleware **redirect im lặng** | `[MAJOR]` | ✅ Leader chốt **404** |
| C2 | `CONF-KHO` | `NEW-18` | `updateReception` với id không tồn tại: TC đòi **404**, kho `MT-18` ghi **luôn trả `success:true`** | `[MAJOR]` | ✅ Leader chốt **404** |
| C3 | `CONF-SPEC` | `NEW-48` · `NEW-42` · `NEW-71` | Ảnh chụp khoá học trong đơn: TC ghi *"tại thời điểm **đặt**"*, `BR-31` ghi snapshot **lúc xoá** | `[MAJOR]` | ✅ Leader chốt **không cần snapshot** → bỏ vế này khỏi TC |
| C4 | `CONF-TC` | `NEW-42` vs `NEW-48` | Cùng thao tác xoá khoá học: một TC nói đơn **đã bị dọn**, TC kia nói đơn **vẫn hiển thị** | `[MAJOR]` | ✅ Leader chốt **xoá mềm + 削除済み予約** |
| C5 | `CONF-SPEC` | `NEW-40` · `NEW-41` | TC đòi **0 mồ côi tuyệt đối**, `RA-08` nói history + `mobile_notify` **vẫn mồ côi theo thiết kế** | `[MAJOR]` | ✅ Leader chốt **0 mồ côi** → `RA-08` phải fix |
| C6 | `CONF-KHO` | `NEW-40` | Bảng `events`: TC đòi xoá sạch, kho `TC-LSN-457` nói **`type = 2` phải còn** | `[MAJOR]` | ✅ Leader chốt **xoá remind của đơn** |
| C7 | `CONF-KHO` | `NEW-55` · `NEW-72` | Lỗi cổng thanh toán: TC đòi **tiếng Nhật**, kho `TC-LSN-242` đòi **nguyên văn tiếng Anh** | `[BLOCKER]` | ✅ Leader chốt **giữ msg của cổng** → **TC sai** |

### C1 · `CONF-SPEC` — `NEW-22` · ✅ đã chốt

| Mục | Nội dung |
|---|---|
| Nội dung check | Mở màn chi tiết lịch bằng `{id}` thuộc bot khác |
| Expected của TC | Trả **HTTP 404**, trang báo không tìm thấy, không lộ dữ liệu bot B |
| Nguồn đối chiếu | `feature-spec.md` §11.3 — middleware `checkLessonCalendarInBot`: *"thất bại ⇒ **redirect im lặng** về `calendar.index` (các nhánh trả JSON 500 đã bị comment)"* |
| ✅ Quyết định | Leader chốt 2026-09-22: **kỳ vọng đúng là 404** ⇒ `NEW-22` giữ nguyên expected, **không sửa TC** |
| Hệ quả | (a) spec §11.3 mô tả hành vi cũ → phải update (§8 dòng 1) · (b) `NEW-22` đang **fail** ⇒ bug Studio **#1145** là **defect thật** · (c) bug *"Ajax của lịch đã bị xoá bị chuyển hướng **302** về danh sách nên màn hình im lặng"* cũng là defect thật — nhánh ajax còn sót redirect |
| Lưu ý | Vẫn phải chạy lại trên bản build đúng (§5 **I1**) mới kết luận được đã fix hay chưa |

### C2 · `CONF-KHO` — `NEW-18` · ✅ đã chốt

| Mục | Nội dung |
|---|---|
| Nội dung check | Gọi `updateReception` với `reception_id` không tồn tại |
| Expected của TC | Trả **404** kèm 「受付枠が見つかりません。」 |
| Nguồn đối chiếu | kho `fa019` §Điểm mâu thuẫn **`MT-18`** (mức CAO): *"`updateReception` (EP-46) LUÔN trả `success: true` kể cả khi reception không tồn tại"* |
| ✅ Quyết định | Leader chốt 2026-09-22: **kỳ vọng đúng là 404** ⇒ `NEW-18` giữ nguyên expected (và đang **pass** — khớp hành vi sau fix) |
| Hệ quả | **Kho cũ/sai**: đóng `MT-18`, sửa expected `TC-LSN-286` sang **404 + 「受付枠が見つかりません。」** (§8 dòng 2) |

### C3 · `CONF-SPEC` — `NEW-48`, `NEW-42`, `NEW-71`

| Mục | Nội dung |
|---|---|
| Nội dung check | Giá trị ảnh chụp khoá học hiển thị trong đơn **sau khi xoá khoá học** |
| Expected của TC | `NEW-48`: *"các đơn liên quan vẫn hiển thị đúng tên khoá học, giá và tên hệ thống **tại thời điểm đặt**"* · `NEW-71`: *"snapshot … tại thời điểm **đặt lịch**"* |
| Nguồn đối chiếu | `feature-spec.md` §8 **`BR-31`**: *"Xoá khoá học ⇒ **snapshot** `course_name`/`course_amount`/`system_name`/`course_image` vào **mọi** booking (kể cả đã soft-delete) **trước khi xoá**"* — tức lấy giá trị **lúc xoá**. Thêm **`BR-29`**: `booking.payment_amount` mới là cột snapshot **lúc tạo đơn** |
| ✅ Quyết định | Leader 2026-09-22: **không cần snapshot** |
| Cách hiểu đã áp dụng | **Bỏ phần KIỂM snapshot khỏi test case**, vì ticket #40050 không chạm logic ghi snapshot — **không phải** bỏ snapshot khỏi sản phẩm. Xem ô dưới về lý do chọn cách hiểu này; nếu ý khác xin nói lại một câu |
| Vì sao hiểu như vậy | Nếu **sản phẩm** bỏ snapshot thì bảng `calendar_course` bị xoá **cứng** (không có SoftDeletes) ⇒ màn 「削除済み予約」 vừa chốt ở **C4** sẽ mất cột 予約していたコース (kho `TC-LSN-236`, `TC-LSN-238`) và trái `BR-31` + kho `TC-LSN-77`. Đó là thay đổi sản phẩm nằm **ngoài** phạm vi ticket này, phải mở ticket riêng |
| Việc phải làm | `testcase_update` trên Studio: (1) `NEW-42` — bỏ câu *"Theo BR-15, mọi đơn (kể cả đơn đã xoá mềm) vẫn giữ ảnh chụp tên khoá học, giá, tên hệ thống và ảnh"*; (2) `NEW-48` và `NEW-71` — xem hệ quả ở §3 |
| ⚠️ Hệ quả sang §3 | Bỏ vế snapshot thì `NEW-48` chỉ còn *"đơn vẫn xem được ở 「削除済み予約」 + lịch sử thao tác đơn ghi lý do xoá"* — **đã nằm trọn trong `NEW-42`**. Đề nghị: gộp vế 「削除済み予約」 vào `NEW-42` rồi **xoá cả `NEW-48` lẫn `NEW-71`** (§3 `DUP-1` đã cập nhật) |
| Không phải sửa | `BR-31` và kho `TC-LSN-77` **giữ nguyên** — chúng mô tả hành vi sản phẩm, không mâu thuẫn với việc TC của ticket này không kiểm phần đó |
| Ai chốt | ✅ `Leader` — đã chốt (chờ xác nhận cách hiểu ở ô thứ 2) |

### C4 · `CONF-TC` — `NEW-42` vs `NEW-48`

| Mục | Nội dung |
|---|---|
| Nội dung check | Xoá khoá học có đơn đã huỷ (+ 1 đơn đã xoá mềm) → số phận của các đơn đó |
| Expected A | `NEW-42`: *"toàn bộ đơn đặt chỗ của khoá học đó **đã bị dọn**"* |
| Expected B | `NEW-48`: *"Mở lại danh sách đơn đặt chỗ … các đơn liên quan **vẫn hiển thị** đúng tên khoá học, giá và tên hệ thống; không hiển thị trống hoặc lỗi"* |
| ✅ Quyết định | Leader 2026-09-22 chốt đầy đủ quy tắc xoá khoá học: **(1)** còn đơn đang 予約確定 / リクエスト ⇒ **KHÔNG cho xoá**; **(2)** toàn bộ đơn đã ở trạng thái huỷ ⇒ **cho xoá**; **(3)** sau khi xoá, toàn bộ đơn của khoá học **bị xoá mềm** và **xuất hiện ở màn 「削除済み予約」** |
| Kết luận | **Cả 2 TC đều phải sửa lời** — bản chất kiểm không đổi. `NEW-42`: *"đã bị dọn"* → **"bị xoá mềm (ghi `deleted_at`), biến khỏi danh sách đơn thường"**, KHÔNG được hiểu là xoá cứng. `NEW-48`: *"vẫn hiển thị"* → **"vẫn xem được ở màn 「削除済み予約」"** |
| Xác nhận thêm | Quy tắc (1) và (2) khớp đúng `NEW-44` (pass), kho `TC-LSN-71`/`TC-LSN-72`/`TC-LSN-75`/`TC-LSN-76` và spec `BR-30` ⇒ không phát sinh mâu thuẫn mới |
| ⚠️ Hệ quả mới | `BR-30` ghi rõ quy tắc (1) **chỉ kiểm ở FRONT-END, server không kiểm lại**; `NEW-44` là TC nhóm `ui` (bấm nút trên màn hình). Vì Leader nay xác nhận đây là **business rule**, việc server không chặn là lỗ bỏ qua rule bằng cách gọi thẳng endpoint ⇒ thêm **`G10`** ở §1 + TC `TC-DATAREF001-01` ở §7 (kho `TC-LSN-613` cũng đang dự kiến FAIL đúng điểm này) |
| Ai chốt | ✅ `Leader` — đã chốt → §8 dòng 7 |

### C5 · `CONF-SPEC` — `NEW-40`, `NEW-41` (kéo theo `NEW-47`)

| Mục | Nội dung |
|---|---|
| Nội dung check | Sau khi xoá lịch, DB còn sót bản ghi con nào không |
| Expected của TC | `NEW-40`: *"**Không bảng nào** còn dòng trỏ tới mã lịch/mã khoá học/mã đơn đã bị xoá"* · `NEW-41`: *"**Tất cả** truy vấn trả về 0 dòng"* |
| Nguồn đối chiếu | `feature-spec.md` §11 **`RA-08`**: *"`deleteCalendar()` bước 4 **chỉ duyệt booking chưa soft-delete** để dọn history/notify, nhưng bước 5 `forceDelete()` **mọi** booking ⇒ **mồ côi**"*. Vòng refix (4) chỉ đổi **thứ tự** xoá bản ghi cha, **không** nói sửa `withTrashed()` ở bước 4 |
| ✅ Quyết định | Leader 2026-09-22: **xoá calendar phải xoá toàn bộ bản ghi liên quan, KHÔNG để lại dữ liệu mồ côi trong DB** |
| Kết luận | **TC đúng — `NEW-40` và `NEW-41` giữ nguyên expected, không sửa TC.** Phần sai nằm ở **code**: `RA-08` nay thuộc phạm vi phải fix của ticket này ⇒ `deleteCalendar()` bước 4 phải duyệt booking bằng `withTrashed()` để dọn `history` + `mobile_notify` của cả đơn **đã xoá mềm**, trước khi bước 5 `forceDelete()` |
| ⚠️ Hai nguyên nhân khác nhau trong cùng 1 bug | Bug Studio *"Xoá lịch bài học để lại dữ liệu con mồ côi: **courses=2, receptions=4, bookings=6**, **history=10, mobileNotify=6**"* gộp 2 lỗi: (a) **courses/receptions/bookings** = lỗi **thứ tự xoá cha trước con**, đã được vòng refix (4) sửa ở commit `f1c9a55319` — nhưng commit này có dấu hiệu **chưa push** (§5 **I1**); (b) **history/mobileNotify** = `RA-08`, **chưa ai sửa**. ⇒ Deploy đúng build chỉ làm hết nhóm (a); nhóm (b) vẫn còn nếu không fix thêm |
| ⚠️ Rủi ro kỹ thuật kèm theo | Kỳ vọng *"0 mồ côi"* là tuyệt đối, trong khi `RA-01` / `A-14` ghi chuỗi xoá 10 nhóm **không bọc transaction** (và transaction từng có rồi bị comment). Đứt giữa chừng ⇒ vẫn để lại mồ côi. `NEW-47` (xoá lịch khối lượng lớn, đang **fail**) chính là ca bộc lộ điều này ⇒ Dev cân nhắc bọc transaction hoặc bổ sung bước dọn bù |
| Quan hệ với C4 | Quyết định C4 khiến xoá khoá học **sinh ra booking đã xoá mềm theo đúng thiết kế**, nên một lịch bình thường sẽ có sẵn nhiều đơn `deleted_at != null` — đúng nhóm mà `RA-08` bỏ sót. Càng dùng chức năng xoá khoá học thì mồ côi khi xoá lịch càng nhiều ⇒ fix `RA-08` là bắt buộc, không phải tuỳ chọn |
| Ai chốt | ✅ `Leader` — đã chốt → §8 dòng 9 |

### C6 · `CONF-KHO` — `NEW-40`

| Mục | Nội dung |
|---|---|
| Nội dung check | Bảng `events` sau khi xoá lịch |
| Expected của TC | *"không còn … **sự kiện nhắc lịch và bước nhắc** … Không bảng nào còn dòng trỏ tới mã lịch"* — không phân biệt `type` |
| Nguồn đối chiếu | kho **`TC-LSN-457`**: *"Với bảng `events`: **CHỈ** xoá bản ghi có `booking_calendar_id` = id booking đã xoá **VÀ `type` = 4**; **KHÔNG xoá** bản ghi `type` = 2 của booking calendar"* |
| ✅ Quyết định | Leader 2026-09-22: **remind của các đơn đó phải bị xoá — friend không còn nhận remind nữa** |
| Kết luận | **TC diễn đạt quá rộng → sửa lời `NEW-40`**: xoá `events` / `event_step` / `event_step_time` **thuộc `type = 4` của lịch bài học**; giữ nguyên `events type = 2` của booking calendar. Phần `type = 2` suy từ kho `TC-LSN-457` + `NEW-69` (pass, đã kiểm đúng điểm *"bước gửi tin thuộc loại khác không bị xoá"*), **không** phải do Leader nói trực tiếp — nếu hiểu khác xin chỉnh lại |
| ⚠️ Nâng phạm vi kiểm | *"Friend sẽ không được nhận remind nữa"* là **output cuối trên LINE** (RULE-06), trong khi `NEW-49` chỉ dừng ở DB (*"không còn bản ghi ở trạng thái chờ gửi"*). Đã bổ sung nhánh đối chứng âm vào TC `TC-MSG002-10` ở §7 |
| Đối chiếu kho — **không mâu thuẫn** | Leader ghi chú *"khi cancel booking thì đã phải xoá list remind phía line friend rồi"*. Kho `TC-LSN-408` có 2/6 nhánh **KHÔNG** xoá remind, nhưng đó là nhánh **2** (vừa bấm xin huỷ, chưa được duyệt) và **5** (admin **từ chối** yêu cầu huỷ) — booking vẫn còn hiệu lực và vẫn chiếm chỗ (`BR-20`) nên giữ remind là đúng. Hai phát biểu khớp nhau |
| Ai chốt | ✅ `Leader` — đã chốt → §8 dòng 10 |

### C7 · `CONF-KHO` — `NEW-55`, `NEW-72` · `[BLOCKER]`

| Mục | Nội dung |
|---|---|
| Nội dung check | Ngôn ngữ của thông báo khi **cổng thanh toán từ chối hoàn tiền** |
| Expected của TC | `NEW-72`: *"Hiển thị lỗi **tiếng Nhật**"* · `NEW-55`: *"hiện thông báo lỗi **tiếng Nhật** do máy chủ trả về (nội dung từ cổng thanh toán hoặc thông báo không tìm thấy)"* |
| Nguồn đối chiếu | kho **`TC-LSN-242`**: *"Hiện lỗi **do Stripe trả về**: 「Charge ch_xxx has already been refunded.」"* — nguyên văn tiếng Anh; `TC-LSN-241` tương tự với UnivaPay |
| ✅ Quyết định | Leader 2026-09-22: **lỗi hoàn tiền do hệ thống thanh toán trả về thì hiển thị đúng message của hệ thống thanh toán** ⇒ **kho đúng**, `NEW-55` và `NEW-72` **sai expected** |
| Việc phải làm | Sửa expected trên Studio (`testcase_update`): `NEW-72` *"Hiển thị lỗi **tiếng Nhật**"* → **"hiển thị đúng message do cổng thanh toán trả về"**; `NEW-55` bỏ chữ *"tiếng Nhật"* ở vế nội dung đến từ cổng (vế *"thông báo không tìm thấy"* do hệ thống sinh thì vẫn là tiếng Nhật) |
| Hệ quả triage | Bug Studio **#1168** (*"Lỗi hoàn tiền … hiện nguyên văn tiếng Anh"*) → **không còn là bug về mặt ngôn ngữ**; `NEW-55`/`NEW-72` fail là do expected sai, phải sửa TC rồi chạy lại |
| ⚠️ Điểm còn lại của #1168 | Payload cụ thể của bug là **`Invalid API Key provided: sk_test_***`** — đây là **lỗi cấu hình/credential**, không phải cổng từ chối giao dịch, và nội dung có mảnh khoá bí mật. `SEC-002` (và `NEW-19`) yêu cầu không lộ credential ra màn hình. Đề nghị Leader xác nhận: nhóm lỗi **credential/cấu hình** có nằm ngoài quy tắc *"hiển thị nguyên văn"* không? Nếu có thì #1168 vẫn là bug ở phần lộ khoá |
| ⚠️ Kéo theo | Yêu cầu (2) của ticket (*"toàn bộ thông báo lỗi sang tiếng Nhật"*) nay có **ngoại lệ** — phải ghi ngoại lệ đó vào spec, và `NEW-62` (*"không còn chuỗi tiếng Anh nội bộ"*) nên nói rõ message từ cổng thanh toán không thuộc phạm vi kiểm |
| Ai chốt | ✅ `Leader` — đã chốt (còn 1 câu hỏi về credential) → §8 dòng 11 |

> ✅ **Quyết định của Leader — 2026-09-22**
>
> | # | Chốt | TC phải sửa | Nơi khác phải sửa |
> |---|---|---|---|
> | C1 | Expect **404** | không | spec §11.3 + nhánh ajax còn 302 (Dev) |
> | C2 | Expect **404** | không | kho: đóng `MT-18`, sửa `TC-LSN-286` |
> | C3 | **Không cần snapshot** — bỏ vế kiểm snapshot khỏi TC | `NEW-42` (bỏ câu snapshot) · `NEW-48` + `NEW-71` → đề nghị xoá (§3) | không — `BR-31` và kho `TC-LSN-77` giữ nguyên |
> | C4 | Chặn xoá khi còn 予約確定/リクエスト · xoá được khi toàn bộ đã huỷ · sau xoá → **xoá mềm + 「削除済み予約」** | `NEW-42` **và** `NEW-48` (sửa lời) | — · sinh thêm `G10` ở §1 |
> | C5 | Xoá lịch → **0 dữ liệu mồ côi** | không | **code**: Dev fix `RA-08` (`withTrashed()` ở bước 4) |
> | C6 | **Xoá remind của đơn**, friend không nhận remind nữa | `NEW-40` (giới hạn `type = 4`) · `NEW-49` (nêu rõ phạm vi) | — |
> | C7 | Hiển thị **message của hệ thống thanh toán** | `NEW-55` **và** `NEW-72` (expected sai) | spec ghi ngoại lệ cho yêu cầu (2) |
>
> ⏳ **Còn 2 việc cần 1 câu trả lời**: (1) xác nhận cách hiểu **C3** — *"không cần snapshot"* = bỏ khỏi **TC**, không phải bỏ khỏi **sản phẩm**; (2) lỗi **credential/cấu hình** của cổng thanh toán có nằm ngoài quy tắc *"hiển thị nguyên văn"* không (**C7**).

**Đã rà**: 80 TC × `spec-features/admin/lesson-booking/feature-spec.md` (§4 luồng end-to-end · §8 bảng 93 business rule · §8.10 BR-53…56 · §11.1 TOP-12 · §11.2 bảng IDOR · §11.3 bảng nhóm route) + `web/logic-spec.md` (bảng BR-01…BR-13) + `kho-tcs/fa019-datlichbaihoc-レッスン予約.md` (632 TC · 65 điểm mâu thuẫn MT-01…MT-65) → phát hiện **C1 … C7**. Lượt rà **BƯỚC 4c vòng 2** (2026-09-22) cụm TC theo `đối tượng + thao tác` thay vì theo mã quan điểm, in cột `Kết quả mong đợi` của cả 2 phía, và tra lại từng mã BR mà TC tự dẫn: **xoá lịch** (`NEW-40/41/46/47`) · **xoá khoá học** (`NEW-42/43/44/48/70/71`) · **hoàn tiền** (`NEW-28/55/72`) · **đổi trạng thái + xoá đơn** (`NEW-49/50/61/66`) · **xoá khung giờ 受付枠** (`NEW-11/12`).

**`CONF-TC`**: 1 cặp — `NEW-42` vs `NEW-48` (**C4**). Riêng cặp `NEW-15` (thiếu **mã khoá học** khi xoá item action → 422) và `NEW-59` (thiếu **mã item action** khi xoá item chưa lưu → 200, không báo lỗi) **không** phải mâu thuẫn: hai TC ràng buộc hai trường khác nhau, đúng theo phần (1) của vòng refix 1.

---

## 5. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[BLOCKER]` | Toàn bộ 3 run (`#1753`, `#1792`, `#1808`, 2026-09-20→21) | **Không xác định được bản build đang test, nhiều khả năng KHÔNG phải bản fix cuối.** 3 bằng chứng cùng chiều: (1) Studio `task_get_context.dev_impact` (tính 2026-09-16) ghi *"Nhánh `ai_fixbug_40050` đã tiến tới commit `adad1a41`"* và *"Journal nói đã chuyển xoá cha xuống cuối nhưng **chưa thấy trong commit**"*; (2) journal #136541 (`f1c9a55319`) là journal **duy nhất KHÔNG có dấu `[đã push]`**, trong khi #134734 và #136534 đều có; (3) 11/20 TC fail rơi **đúng** vào 2 lỗi mà phần (4) của bản `f1c9a55319` được viết ra để sửa (`NEW-40/41/42/43/70/47` cascade mồ côi · `NEW-33/34/35/36/38` cụm endpoint khoá học tra theo id thô) | **Chốt commit đang chạy trước khi triage 18 bug.** Nếu runner đang ở `adad1a41` → 11 bug này là "fail do chưa deploy", phải chạy lại trên `f1c9a55319` rồi mới kết luận |
| I2 | `[BLOCKER]` | `NEW-55`, `NEW-70`, `NEW-73` | **3/20 TC fail chưa có bug nào trên Studio** (17/20 còn lại đã có). `NEW-55` là ca duy nhất kiểm nhánh lỗi nhắc lịch + hoàn tiền đọc khoá phản hồi khác nhau — fail mà không ai ghi nhận | Raise bug cho `NEW-55`; `NEW-70`/`NEW-73` xử theo §3 (trùng) nhưng vẫn phải đối chiếu với bug của TC gốc trước khi xoá |
| I3 | `[MAJOR]` | Toàn bộ bộ TC | **Tỷ lệ Đạt thật = 59/80 = 73.75% (< 80%)**: 20 fail + 1 skip. 21 TC **không có kết luận test** | Chạy lại sau khi chốt I1; `NEW-67` (app mobile) chưa chạy lần nào |
| I4 | `[MAJOR]` | Toàn bộ 3 run | **RULE-08 / ENV-003 — 0/80 TC chạy production**, cả 3 run đều `env = local`. Task chạm **bill tiền** (`orderRefund` → Stripe/UnivaPay), **media** (`importCsv`), **domain/asset** (`DEPLOY-ASSET-001`), **job nền** (`NewEventRemindTask`) | Không được kết luận "đã test xong" cho 4 hạng mục trên từ local. Tối thiểu chạy `TC-ENV003-10` (§7) trên product |
| I5 | `[MAJOR]` | 18 bug Studio | **Cả 18 bug đều `client_ref = null`** — chưa bug nào được gắn ticket Redmine; cột `Số ticket bug` của cả 80 TC đều rỗng. 5 bug mức `High` (xoá được khoá học của bot khác · đọc được cấu hình khoá học của bot khác · mồ côi khi xoá lịch · mồ côi khi xoá khoá học · đơn đã xoá còn lịch gửi nhắc chờ) đang nằm ngoài Redmine | Sau khi chốt I1: bug nào còn lại thật thì raise ticket Redmine và gắn ngược vào TC |
| I6 | `[MAJOR]` | `NEW-14`, `NEW-15`, `NEW-16`, `NEW-18`, `NEW-21` | **TC gộp lô — mỗi ca chạy 8–12 endpoint trong 1 lần** (vi phạm atomic). `NEW-15` fail nhưng kết quả không cho biết endpoint nào hỏng; bug tương ứng chỉ bắt được 1 nhánh (`calendar id` chưa dịch nhãn) trong khi ca còn 11 nhánh khác | Giữ cách gộp (lý do gộp hợp lý: dùng chung 1 hàm kiểm tra) nhưng **bắt buộc ghi kết quả theo từng endpoint** ở evidence, hoặc tách ca khi có nhánh fail |
| I7 | `[MAJOR]` | `NEW-61` | `Kết quả mong đợi` **để ngỏ**: *"nếu chỉ hiện thành công chung chung mà không nêu đơn nào bị bỏ qua thì ghi nhận lại để leader chốt kỳ vọng"* → không đo lường được, không phán Đạt/Không đạt | Leader chốt kỳ vọng cho thông báo khi có đơn bị bỏ qua, rồi sửa expected thành 1 giá trị cụ thể |
| I8 | `[MAJOR]` | `NEW-73` | Tiền đề + steps quá chung (*"Gọi API với ID của bot B"*) — không nêu endpoint, không nêu dữ liệu ⇒ người khác không dựng lại được, và đang **fail** | Đã đề nghị xoá ở §3 (trùng `NEW-23…36`); nếu giữ thì phải liệt kê endpoint cụ thể |
| I12 | `[MAJOR]` | **29/31 TC có dẫn mã quy tắc** | **Mã Business rule dẫn trong `Ghi chú` / `Kết quả mong đợi` gần như toàn bộ trỏ sang rule của chức năng khác** — đã tra từng mã trong `spec-features/admin/lesson-booking/` (cả `feature-spec.md` lẫn `web/logic-spec.md`). Chỉ `NEW-2` (`BR-01`) và `NEW-8` (`BR-02`) dẫn đúng; thêm **3 mã không tồn tại** ở bất kỳ file spec nào. Bảng đối chiếu đầy đủ ở khối **I12 — chi tiết** ngay dưới bảng này | Sửa mã dẫn chiếu theo bảng đối chiếu. **Hệ quả nếu không sửa**: TC không truy vết được về spec, và người review vòng sau sẽ tin mã dẫn là đúng rồi bỏ qua mâu thuẫn thật — đúng cách mà **C3**, **C5**, **C6** đã lọt ở vòng 1 |
| I9 | `[MINOR]` | `NEW-70` … `NEW-76` (7 TC `vinth@mcp`, `status = draft`) | Steps 3–4 dòng, tiền đề 1 câu — ngắn hơn hẳn TC do pipeline sinh; 3/7 đang fail. `NEW-72` tiền đề *"Gateway refund trả lỗi"* không nói cách dựng trạng thái đó | Bổ sung tiền đề dựng được (khoá API sai / thẻ test bị từ chối) và steps đủ chi tiết, rồi chuyển khỏi `draft` |
| I10 | `[NIT]` | `NEW-4`, `NEW-13` | Biên 10/11 ký tự của 管理名 và biên ngày của 受付枠 **không thấy nêu** trong điểm sửa (journal chỉ nói thêm rule `required`, không đổi `max` / rule ngày) | Hỏi member: đây là regression chủ đích quanh `validateInput` hay lọt từ bộ TC khác? Nếu là regression chủ đích thì ghi rõ ở `Ghi chú` |
| I11 | `[NIT]` | `spec-features/admin/lesson-booking/feature-spec.md` | §11.1 / §11.2 / §11.3 đang mô tả **hiện trạng trước fix** (A-02 mass assignment, A-04 `deleteEventStep` không kiểm `bot_id`, A-06 không kiểm id con, A-09 refund không kiểm `bot_id`) | Đã đưa xuống §8 |
| I13 | `[NIT]` | `NEW-49` | Expected ghi *"Không còn bản ghi lịch gửi nhắc nào ở trạng thái chờ gửi trỏ tới đơn đã bị xoá"* mà không nói rõ chỉ áp cho đường **xoá đơn**. Kho `TC-LSN-408` (SpecChange #32367) ghi khi **huỷ** đơn thì có **2/6 nhánh CỐ Ý KHÔNG xoá remind** | Thêm 1 câu giới hạn phạm vi vào expected của `NEW-49` để không bị đọc lẫn sang đường huỷ đơn |

> Nguồn spec đã dùng: `spec-features/admin/lesson-booking/feature-spec.md` (§1.2 · §4.1/4.4/4.6/4.7 · §8.10 BR-53…56 · §11.1 · §11.2 · §11.3) — **tìm được spec**, không có issue thiếu chuẩn đánh giá.


### I12 — chi tiết: bảng đối chiếu mã quy tắc bị dẫn sai

| Mã TC dẫn | Rule đó **thật sự** nói gì | Bị dẫn cho | Rule **đúng** phải là |
|---|---|---|---|
| `BR-04` | Khoá học tối đa 2 (gói free) / trần cứng 200 mỗi lịch | biên ngày khung giờ (`NEW-13`) | `BR-12` / `BR-13` |
| `BR-05` | Câu hỏi form tối đa **100**/lịch | bỏ qua đơn không khớp (`NEW-61`), khách huỷ đơn (`NEW-65`) | `BR-P14` / `BR-P16` |
| `BR-07` | Tạo lịch ⇒ lazy-create **2** cấu hình tin + **2** form mặc định | chặn xoá khung giờ còn 予約確定 (`NEW-11`, `NEW-12`), chặn xoá khoá học (`NEW-44`) | `BR-17` (khung giờ) · `BR-30` (khoá học) |
| `BR-09` | Khoá học mới `booking_page_display = 1`, **tắt** gửi tin cấp khoá học | hoàn tiền (`NEW-28`, `NEW-72`) | `BR-P27` / `BR-P28` + `RA-07` / `A-09` |
| `BR-11` | Mỗi khách chỉ **1** đăng ký chờ chỗ / slot | câu hỏi form thứ 101 bị chặn (`NEW-10`) | `BR-05` |
| `BR-12` | `repeatDueDate` **không được ở quá khứ** (`web/logic-spec.md`) | chuỗi xoá dây chuyền (`NEW-40`, `NEW-41`, `NEW-46`, `NEW-47`, `NEW-70`) · mã xác thực xoá lịch (`NEW-5`, `NEW-6`, `NEW-26`) | §4.7 + `RA-08` / `RA-01` / `A-14` · `BR-53` |
| `BR-14` | Bắt buộc `startTime` + `endTime` khi thêm khung giờ | lịch gửi nhắc (`NEW-49`, `NEW-66`), khách chọn câu hỏi (`#18284`) | `BR-38` / `BR-39` / `BR-40` |
| `BR-15` | Chống trùng `(course_id, ngày, giờ)` khi thêm khung giờ | snapshot khoá học trong đơn (`NEW-42`, `NEW-48`, `NEW-71`) | `BR-31` |
| `BR-16` | Chế độ chọn ngày cụ thể nhận **timestamp mili-giây** | snapshot khoá học (`NEW-48`, `NEW-71`), khách đặt lịch (`NEW-64`) | `BR-31` · `BR-P07`…`BR-P13` |
| `CC-01` | ⛔ **không tồn tại** ở bất kỳ file spec nào của FA-019 | `NEW-4` | bỏ, hoặc chỉ rõ tài liệu nguồn |
| `SR-2` | ⛔ **không tồn tại** | `NEW-23`, `NEW-24` | bỏ, hoặc chỉ rõ tài liệu nguồn |
| `SR-3` | ⛔ **không tồn tại** | `NEW-28` | bỏ, hoặc chỉ rõ tài liệu nguồn |

---

## 6. TCs thừa / ngoài phạm vi task

Đã rà **80** TC — **không có TC nào ngoài phạm vi task**.

Chi tiết 3 TC từng bị nghi rồi loại khỏi bảng này:

| TC | Vì sao **KHÔNG** phải TC thừa |
|---|---|
| `NEW-68` "Màn lịch salon không bị ảnh hưởng…" | Dẫn được từ vùng regression của kho: `kho-tcs` **MT-65** — FA-019 dùng **chung bảng** `calendar_salon_setting_send_messages` với FA-020; và `TC-LSN-628` đã coi lesson↔salon là vùng hồi quy cố định |
| `NEW-47` "Xoá lịch khối lượng lớn…" | Map thẳng `F4` + `D3` (đổi thứ tự chuỗi xoá) và `spec-features` RA-01 (chuỗi xoá 10 nhóm **không transaction**) |
| `NEW-4`, `NEW-13` (biên ký tự / biên ngày) | Không chắc ý định ⇒ theo quy tắc **không flag**, đã chuyển thành 1 dòng `[NIT] I10` ở §5 để hỏi member |

---

## 7. TCs đề xuất bổ sung (28)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa019-datlichbaihoc-レッスン予約.md` (632 TC · 47 nhóm chức năng · 65 điểm mâu thuẫn MT-01…MT-65) |
| Vùng regression phát hiện từ kho | `TC-LSN-459…483` Googleスプレッドシート連携 (sync theo thao tác đơn) · `TC-LSN-240…248` Hoàn tiền 返金 · `TC-LSN-188…200` リクエスト一括操作 · `TC-LSN-454…458` 予約システムの削除 · `TC-LSN-611/612/623/624` Phân quyền & verify API · `TC-LSN-615…617` App mobile · `TC-LSN-294` double-click xoá nhiều · `MT-65` bảng dùng chung lesson↔salon |
| Conflict expected vs kho | `NEW-18` vs `MT-18` (`updateReception` luôn trả `success:true`) → **C2 `CONF-KHO`**, đã đưa §4 + §8 |
| GAP dùng lại TC kho (không viết mới) | `Q1 → TC-LSN-624 "Staff bị cấm route レッスン予約 vẫn thao tác được qua nhóm /ajax/calendar/*"` + `TC-LSN-611` (verify API thiếu kiểm quyền staff + CSRF) + `TC-LSN-623` (staff thao tác các màn) — **chỉ cần chỉnh 1 điểm**: chạy lại trên branch fix và bổ sung bước xác nhận staff **cùng bot** vẫn qua được (fix siết theo bot, không siết theo role). `G7 (phần app mobile) → TC-LSN-615 / TC-LSN-616` |
| Căn cứ TC regression `R<x>` | Không có TC mã `R` — mọi TC dưới đây đều sinh từ dòng `G<x>` (§1) hoặc `Q<x>` (§2) |
| Xác nhận chống trùng | Đã đối chiếu **80** TC ở BƯỚC 0 + 632 TC kho — **không TC đề xuất nào trùng** (4 yếu tố: mã quan điểm × loại case × đối tượng+thao tác × tiền đề) |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-REGSHARED001-10 | UI | REG-SHARED-001 | Phân quyền & môi trường | Normal | auto | Tất cả | Các màn cài đặt khác dùng chung view 設定 vẫn chạy đúng sau khi nhúng tệp xử lý lỗi của lịch bài học | Đăng nhập bot A trên bản có bản fix. Biết danh sách các màn dựng từ `resources/views/basic/setting.blade.php` (các mục trong 設定 ngoài 予約管理) | 1. Mở lần lượt từng màn cài đặt dựng từ view dùng chung này, ngoài lịch bài học<br>2. Mở bảng điều khiển trình duyệt, ghi lại mọi lỗi JavaScript và mọi tệp bị tải hỏng<br>3. Trên mỗi màn, thực hiện 1 thao tác lưu hợp lệ và 1 thao tác chắc chắn bị từ chối<br>4. Quan sát thông báo hiện ra ở cả 2 lần<br>5. So sánh với hành vi của cùng màn đó trên bản trước khi sửa | Thao tác lưu hợp lệ + thao tác bị từ chối có sẵn của từng màn | Không màn nào phát sinh lỗi JavaScript hay tải hỏng tệp. Thao tác lưu hợp lệ vẫn thành công. Thao tác bị từ chối vẫn hiện đúng thông báo **như bản trước khi sửa** — không bị hàm xử lý lỗi mới của lịch bài học ghi đè hoặc nuốt mất thông báo | | Lấp `G1` · Đánh giá spec: Spec không ghi — cần Dev xác nhận danh sách màn dùng chung view này · Evidence: ảnh chụp bảng điều khiển trình duyệt + ảnh thông báo trước/sau |
| TC-UI003-10 | UI | UI-003 | 決済連携 — cài đặt | Abnormal | auto | staging | Lỗi khi lưu cấu hình 決済連携 của lịch bài học hiện thông báo thay vì im lặng | Bot A có lịch bài học và đã liên kết cổng thanh toán ở chế độ thử nghiệm. Mở tab 「決済連携」 của lịch đó trên 2 tab trình duyệt | 1. Ở tab thứ hai, xoá lịch đó (hoặc đổi lịch sang trạng thái làm cho yêu cầu bị từ chối)<br>2. Quay lại tab thứ nhất, bấm lưu cấu hình thanh toán<br>3. Quan sát thông báo và lớp phủ đang tải<br>4. Lặp lại với trường hợp bỏ trống 1 trường bắt buộc của cấu hình thanh toán<br>5. Kiểm tra bảng điều khiển trình duyệt có lỗi JavaScript không | Trường hợp 1: lịch đã bị xoá · Trường hợp 2: bỏ trống 1 trường bắt buộc của cấu hình thanh toán | Cả 2 lần đều hiện thông báo lỗi **tiếng Nhật** do máy chủ trả về, lớp phủ đang tải được tắt, không có mục nào im lặng hay hiện thông báo lưu thành công giả. Không có lỗi JavaScript. Cấu hình thanh toán trong DB không bị ghi một phần | | Lấp `G2` · Đánh giá spec: Spec ghi rõ — `feature-spec.md` §4.1 bước 6 BR-50/BR-51/BR-52 · Evidence: ảnh thông báo + bản ghi mạng của trình duyệt (mã HTTP) |
| TC-PERM002-10 | API | PERM-002 | Modal danh sách booking | Abnormal | auto | staging | Không đổi trạng thái và không huỷ được đơn đặt chỗ của tài khoản khác | Bot B có 1 đơn đang chờ duyệt và 1 đơn đã xác nhận. Đang đăng nhập với bot A đang được chọn. Đã ghi lại trạng thái, lịch sử và số dòng lịch gửi nhắc của 2 đơn đó | 1. Gọi endpoint đổi trạng thái đơn với mã đơn của bot B, thao tác duyệt<br>2. Gọi lại với thao tác từ chối, rồi với thao tác duyệt yêu cầu huỷ<br>3. Gọi endpoint kiểm tra điều kiện huỷ đơn với mã đơn của bot B<br>4. Ghi lại mã HTTP và thông báo của từng lần gọi<br>5. Đọc lại trạng thái, lịch sử thao tác và lịch gửi nhắc của 2 đơn trong DB<br>6. Mở màn quản trị của bot B, đối chiếu 2 đơn hiển thị như cũ | Mã đơn: của bot B · Thao tác: duyệt / từ chối / duyệt yêu cầu huỷ | Mọi lần gọi trả **404** kèm 「予約が見つかりません。」, không lần nào trả 200. Trạng thái 2 đơn của bot B không đổi, không phát sinh bản ghi lịch sử thao tác mới, không thêm/xoá bản ghi lịch gửi nhắc nào, và không có tin nhắn nào được gửi cho khách của bot B | | Lấp `G3` · Đánh giá spec: Spec ghi rõ — `feature-spec.md` §11.2 A-06 (id con không được kiểm) · Studio `dev_impact` liệt kê `checkBookingCancel` + `updateBooking` trong nhóm đã vá · Evidence: mã HTTP + ảnh chụp DB trước/sau |
| TC-PERM003-10 | API | PERM-003 | 予約ページの表示設定 | Abnormal | auto | staging | Không đọc và không lưu được cấu hình trang đặt chỗ của tài khoản khác | Bot B có lịch đã cấu hình 「予約ページの表示設定」 (đã ghi lại toàn bộ giá trị hiện tại). Đang đăng nhập với bot A đang được chọn | 1. Gọi endpoint đọc cấu hình trang đặt chỗ với mã lịch của bot B, ghi lại mã HTTP và nội dung trả về<br>2. Gọi endpoint lưu cấu hình trang đặt chỗ với mã lịch của bot B kèm giá trị mới<br>3. Ghi lại mã HTTP và thông báo<br>4. Đọc lại cấu hình của lịch bot B trong DB<br>5. Mở trang đặt chỗ phía LINE của bot B, đối chiếu hiển thị không đổi | Mã lịch: của bot B · Giá trị mới: đổi mục hiển thị bất kỳ sang giá trị khác | Lần đọc không trả nội dung thật của bot B (trả 404 hoặc dữ liệu rỗng). Lần lưu trả **404** kèm thông báo tiếng Nhật không tìm thấy lịch. Cấu hình trong DB của bot B giữ nguyên toàn bộ giá trị, trang đặt chỗ phía LINE của bot B hiển thị không đổi | | Lấp `G4` · Đánh giá spec: Spec ghi rõ — Studio `dev_impact` nêu `CalendarManagementService.getBookingSettingDisplay` nay chặn theo bot · Evidence: mã HTTP + ảnh chụp DB trước/sau |
| TC-FRIEND001-01 | UI | FRIEND-001 | Detail booking & lịch sử | Normal | auto | staging | Sửa câu trả lời form của đơn trên màn quản trị vẫn ghi đúng hồ sơ bạn bè của chính tài khoản | Bot A có 1 đơn đặt chỗ của 1 người bạn đã có hồ sơ (họ tên, số điện thoại, email, ngày sinh). Đã ghi lại giá trị hồ sơ hiện tại và số dòng lịch sử thay đổi thông tin bạn bè | 1. Mở chi tiết đơn đó trên màn quản trị, vào mục câu trả lời form<br>2. Sửa họ tên, số điện thoại và email sang giá trị mới rồi lưu<br>3. Quan sát thông báo và giá trị hiển thị lại trên màn chi tiết đơn<br>4. Mở màn 「友だちリスト」 → chi tiết người bạn đó, đối chiếu 3 trường vừa sửa<br>5. Mở lịch sử thay đổi thông tin bạn bè, đối chiếu có bản ghi mới đúng giá trị<br>6. Lặp lại bước 1–2 với 1 người bạn **chưa có** hồ sơ nào | Họ tên mới · Số điện thoại mới · Email mới | Lưu thành công (200). Câu trả lời form của đơn hiển thị giá trị mới. Màn chi tiết người bạn hiển thị đúng 3 giá trị mới. Lịch sử thay đổi thông tin bạn bè có bản ghi mới đúng giá trị và đúng người thực hiện. Trường hợp bạn chưa có hồ sơ: hồ sơ được tạo mới đầy đủ, **không bị lọc mất do truy vấn bảng bạn bè nay đi qua bảng trung gian theo bot** | | Lấp `G5` · Đánh giá spec: Spec ghi rõ — `feature-spec.md` §4.2 BR-P36 (ghi đè hồ sơ bạn bè) · Evidence: ảnh màn chi tiết bạn bè + ảnh lịch sử thay đổi thông tin bạn bè |
| TC-SEC002-10 | API | SEC-002 | Đồng thời & verify API | Abnormal | auto | staging | Bốn nhánh lỗi hệ thống khác nhau đều trả một câu tiếng Nhật chung và ghi đủ log | Đăng nhập bot A, có quyền đọc tệp log ứng dụng. Đã xoá/đánh dấu mốc tệp log trước khi chạy | 1. Gây lỗi hệ thống ở nhánh **sửa khoá học** (giá trị vượt sức chứa cột)<br>2. Gây lỗi hệ thống ở nhánh **nhập CSV** (tệp đúng định dạng nhưng nội dung làm vỡ xử lý)<br>3. Gây lỗi hệ thống ở nhánh **hoàn tiền** (cấu hình khoá cổng thanh toán sai)<br>4. Gây lỗi ở **1 nhánh chưa biết trước**: chọn ngẫu nhiên 1 endpoint trong nhóm nhắc lịch và làm hỏng dữ liệu phụ thuộc của nó<br>5. Với mỗi lần: ghi lại mã HTTP + toàn văn thông báo trả về<br>6. Mở tệp log, đếm số bản ghi lỗi mới và kiểm nội dung có đủ để truy vết không | 4 trigger như mô tả ở các bước | Cả **4** lần đều trả **500** kèm đúng một câu tiếng Nhật chung 「システムエラーが発生しました。」. Không lần nào lộ nội dung ngoại lệ, câu lệnh SQL, tên bảng hay đường dẫn tệp mã nguồn — **kể cả nhánh chưa biết trước**. Tệp log có đủ **4** bản ghi lỗi, mỗi bản ghi có thông tin truy vết (endpoint, tham số, nội dung ngoại lệ gốc) | | Lấp `G6` · fix dạng xử lý lỗi chung ⇒ bắt buộc ≥ 3 trigger + 1 trigger chưa biết · Đánh giá spec: Spec ghi rõ — journal mục 2(2)(c) "bổ sung ghi log ở 8 chỗ vốn không có" · Evidence: toàn văn phản hồi 4 lần + trích tệp log |
| TC-MSG002-10 | API | MSG-002 | リマインド — job gửi & recover | Normal | manual | Tất cả | Tin nhắn nhắc lịch vẫn tới được LINE của khách sau khi siết phạm vi theo tài khoản | Bot A có lịch đã bật nhắc lịch với mốc gần (ví dụ 5 phút trước giờ học). Có tài khoản LINE thật là bạn của bot A. Job gửi tin đang chạy | 1. Trên màn quản trị bot A, tạo 1 đơn đặt chỗ thủ công cho người bạn đó vào khung giờ sắp tới<br>2. Truy vấn bảng lịch gửi nhắc, xác nhận bản ghi được tạo với thời điểm gửi đúng<br>3. Chờ tới mốc gửi<br>4. **Mở LINE thật của người bạn**, kiểm tra tin nhắn nhắc đã nhận<br>5. Đối chiếu nội dung tin với mẫu cấu hình<br>6. Truy vấn lại bảng lịch gửi nhắc, xác nhận bản ghi đã chuyển khỏi trạng thái chờ gửi<br>7. Lặp lại với 1 đơn do **khách tự đặt qua LINE** | Mốc nhắc: 5 phút trước giờ bắt đầu | Cả 2 trường hợp (đơn admin tạo và đơn khách tự đặt): tin nhắn nhắc **tới được LINE thật** đúng mốc, nội dung đúng mẫu, và bản ghi lịch gửi nhắc chuyển khỏi trạng thái chờ gửi. Job không bỏ sót bản ghi nào do bị lọc nhầm theo phạm vi tài khoản. **Đối chứng âm (theo chốt C6)**: với đơn đã bị xoá (và đơn đã huỷ có hiệu lực), tới mốc nhắc thì LINE của khách **KHÔNG** nhận được tin nào | | Lấp `G7` + `Q9` · manual vì phải nhìn kết quả trên app LINE thật (RULE-06) · Đánh giá spec: Spec ghi rõ — `feature-spec.md` §4.5 BR-38/39/40 · Evidence: ảnh chụp màn hình LINE + truy vấn bảng lịch gửi nhắc trước/sau |
| TC-REGSHARED001-11 | UI | REG-SHARED-001 | App mobile | Normal | manual | Tất cả | Ứng dụng quản trị mobile và API v1 vẫn đọc được dữ liệu lịch bài học sau khi siết phạm vi theo tài khoản | Có app quản trị mobile đã đăng nhập bot A. Bot A có đơn mới chưa xử lý và thông báo chưa đọc. Có thể gọi được nhóm API v1 của lịch bài học | 1. Mở app mobile, vào mục thông báo đặt lịch bài học, ghi lại danh sách<br>2. Mở 1 đơn mới và duyệt đơn trên app<br>3. Đối chiếu trạng thái đơn và số thông báo chưa đọc giữa app và web<br>4. Gọi các endpoint API v1 của lịch bài học bằng thông tin xác thực của bot A, ghi lại mã HTTP và số bản ghi trả về<br>5. So sánh số bản ghi API v1 trả về với số bản ghi trên màn quản trị web | — | App mobile hiển thị đủ danh sách đơn và thông báo của bot A, duyệt đơn thành công, trạng thái khớp với web. API v1 trả 200 với **đủ** số bản ghi như màn web — không bị rỗng do hàm lấy bot đọc từ phiên đăng nhập trả về rỗng ở luồng này | | Lấp `G7` (phần app + API v1) · dẫn từ `TC-LSN-615` / `TC-LSN-616` của kho · manual vì cần thiết bị thật chạy app admin mobile · Đánh giá spec: Spec ghi rõ — `feature-spec.md` §11.3 nhóm route app (EP-M01…M12) · Evidence: ảnh chụp app + bản ghi phản hồi API |
| TC-INTGSHEET001-01 | API | INTG-SHEET-001 | Googleスプレッドシート連携 | Normal | auto | staging | Thao tác đơn đặt chỗ trên màn quản trị vẫn đẩy đủ dòng lên Google Sheet | Bot A có lịch đã liên kết Google Sheet thành công và đang sync bình thường. Đã ghi lại số dòng và nội dung dòng cuối trên Sheet | 1. Trên màn quản trị, thêm 1 đơn đặt chỗ thủ công<br>2. Mở Google Sheet, đối chiếu dòng mới và các cột họ tên / khoá học / trạng thái<br>3. Đổi trạng thái đơn đó sang đã xác nhận<br>4. Đối chiếu cột ステータス trên Sheet<br>5. Sửa câu trả lời form của đơn đó<br>6. Đối chiếu các cột thông tin bạn bè trên Sheet<br>7. Xoá đơn đó và đối chiếu Sheet | Đơn thủ công với đầy đủ câu trả lời form | Mỗi thao tác đều phản ánh lên Google Sheet trong thời gian sync cho phép: dòng mới xuất hiện đủ cột, cột ステータス đổi theo, cột thông tin bạn bè cập nhật theo lần sửa form, đơn đã xoá không còn hiển thị. **Không thao tác nào bị mất sync do truy vấn nay bị ràng buộc theo bot** | | Lấp `Q2` · dẫn từ `TC-LSN-461` / `TC-LSN-463` của kho · Đánh giá spec: Spec ghi rõ — `feature-spec.md` §4.2 (ghi Google Sheet trong luồng đặt chỗ) · Evidence: ảnh chụp Google Sheet trước/sau từng bước |
| TC-DATAAUDIT001-01 | Data | DATA-AUDIT-001 | Detail booking & lịch sử | Normal | auto | staging | Lịch sử thao tác đơn và lịch sử hoàn tiền vẫn ghi và đọc đủ sau khi siết phạm vi theo tài khoản | Bot A có 1 đơn đã thanh toán (cổng thanh toán chế độ thử nghiệm). Đã ghi lại số dòng lịch sử thao tác của đơn và số dòng lịch sử hoàn tiền | 1. Đổi trạng thái đơn (duyệt), mở modal lịch sử thao tác đơn và đối chiếu bản ghi mới<br>2. Thực hiện hoàn tiền loại đánh dấu thủ công<br>3. Mở modal lịch sử hoàn tiền, đối chiếu bản ghi mới (số tiền, người thực hiện, loại hoàn tiền)<br>4. Truy vấn DB bảng lịch sử thao tác đơn và bảng lịch sử thay đổi thanh toán<br>5. Tải lại màn hình và mở lại 2 modal lịch sử, đối chiếu vẫn đọc ra đủ bản ghi cũ | Loại hoàn tiền: đánh dấu thủ công | Mỗi thao tác sinh đúng 1 bản ghi lịch sử với đủ nội dung (thao tác, số tiền, người thực hiện, thời điểm). Sau khi tải lại, 2 modal lịch sử vẫn đọc ra **đủ** bản ghi cũ lẫn mới — không bản ghi nào biến mất do truy vấn lịch sử nay đi qua truy vấn con theo bot | | Lấp `Q3` · dẫn từ `TC-LSN-246` của kho · Đánh giá spec: Spec ghi rõ — `feature-spec.md` §4.6 (INSERT history `status = 12`) · Evidence: ảnh 2 modal lịch sử + truy vấn DB |
| TC-PAYSTATE001-10 | API | PAY-STATE-001 | Hoàn tiền 返金 | Abnormal | auto | staging | Hoàn tiền lần hai và loại hoàn tiền lạ không tạo giao dịch mới | Bot A có 1 đơn **đã hoàn tiền** (trạng thái thanh toán là đã hoàn tiền) và 1 đơn đã thanh toán chưa hoàn. Cổng thanh toán ở chế độ thử nghiệm. Đã ghi lại số giao dịch trên cổng | 1. Gọi endpoint hoàn tiền lần hai trên đơn đã hoàn tiền, loại hoàn ngay<br>2. Ghi lại mã HTTP, thông báo, và kiểm trên cổng thanh toán có phát sinh giao dịch mới không<br>3. Gọi endpoint hoàn tiền trên đơn chưa hoàn với loại hoàn tiền là một chuỗi không nằm trong danh sách cho phép<br>4. Ghi lại mã HTTP và giá trị được ghi vào cột loại hoàn tiền trong DB<br>5. Đối chiếu trạng thái thanh toán và lịch sử của cả 2 đơn | Lần 1: đơn đã hoàn tiền, loại `hoàn ngay` · Lần 2: loại hoàn tiền = chuỗi lạ | Lần hoàn tiền thứ hai bị từ chối bằng mã 4xx kèm thông báo **do cổng thanh toán trả về** (theo chốt **C7** — không bắt buộc tiếng Nhật; đối chiếu kho `TC-LSN-241`/`TC-LSN-242`), **không** phát sinh giao dịch mới trên cổng thanh toán và không ghi thêm lịch sử. Loại hoàn tiền lạ bị chặn bằng **422** kèm thông báo **tiếng Nhật** (lỗi kiểm tra dữ liệu do hệ thống sinh), không được ghi thẳng vào cột loại hoàn tiền | | Lấp `Q4` · dẫn từ `TC-LSN-241` / `TC-LSN-242` của kho + `feature-spec.md` §11.1 RA-07/A-09 (refund thiếu 4 chốt) · Đánh giá spec: Đã hỏi leader — spec mô tả hiện trạng thiếu chốt, cần Dev xác nhận bản fix có bổ sung không · Evidence: bảng giao dịch trên cổng thanh toán + truy vấn DB |
| TC-ENV003-10 | API | ENV-003 | Hoàn tiền 返金 | Normal | manual | product | Hoàn tiền thật trên môi trường product trả đúng mã và đúng thông báo tiếng Nhật | Trên **production**: 1 đơn đặt chỗ đã thanh toán thật với số tiền nhỏ nhất cho phép, đã được nghiệp vụ duyệt cho phép hoàn. Đã ghi lại trạng thái thanh toán và số dư/giao dịch trên cổng | 1. Thực hiện hoàn tiền loại hoàn ngay trên màn quản trị<br>2. Quan sát thông báo trên màn hình và mã HTTP trong bản ghi mạng<br>3. Đối chiếu giao dịch hoàn tiền trên trang quản trị của cổng thanh toán<br>4. Đối chiếu trạng thái thanh toán của đơn và lịch sử hoàn tiền<br>5. Thử 1 nhánh bị từ chối (hoàn tiền lần hai) và quan sát thông báo | Số tiền nhỏ nhất cho phép hoàn | Hoàn tiền thành công trả 200, giao dịch hoàn tiền xuất hiện đúng số tiền trên cổng thanh toán thật, trạng thái đơn chuyển sang đã hoàn tiền và có bản ghi lịch sử. Nhánh bị từ chối trả 4xx kèm thông báo **do cổng thanh toán trả về**, hiển thị nguyên vẹn để admin biết lý do (theo chốt **C7**). Riêng thông tin nhạy cảm: thông báo **KHÔNG** được chứa khoá API hay mảnh credential nào (`SEC-002`) | | Lấp `Q5` · RULE-08 (bill tiền không kết luận từ local/staging) · manual vì môi trường production + dữ liệu tiền thật · Đánh giá spec: Spec ghi rõ — `feature-spec.md` §4.6 BR-P27/BR-P28 · Evidence: ảnh giao dịch trên cổng thanh toán + ảnh thông báo |
| TC-CONC001-10 | API | CONC-001 | 受付枠 — xóa nhiều | Abnormal | auto | staging | Bấm hai lần liên tiếp nút xoá không làm hiện thông báo lỗi sau thao tác đã thành công | Bot A có lịch với khoá học, nhiều khung giờ tiếp nhận và 1 đơn đang chờ duyệt. Mạng đặt ở chế độ chậm để bấm kịp lần thứ hai | 1. Chọn nhiều khung giờ rồi bấm nút xoá hàng loạt **hai lần liên tiếp**<br>2. Quan sát thông báo hiện ra và trạng thái nút<br>3. Lặp lại với nút xoá đơn đặt chỗ<br>4. Lặp lại với nút duyệt đơn<br>5. Lặp lại với nút xoá khoá học<br>6. Với mỗi lần: ghi lại mã HTTP của cả 2 yêu cầu và kiểm DB xem có xoá/đổi nhiều hơn dự kiến không | 4 nút: xoá nhiều khung giờ · xoá đơn · duyệt đơn · xoá khoá học | Với cả 4 nút: thao tác chỉ được tính **một lần** (không xoá/đổi nhiều hơn dự kiến trong DB), và **không** hiện thông báo lỗi nào sau thao tác đã thành công — yêu cầu thứ hai phải bị chặn ở màn hình (nút bị khoá / bỏ qua) chứ không để rơi vào nhánh 404 rồi bật thông báo 「…が見つかりません。」 | | Lấp `Q6` · dẫn từ `TC-LSN-294` của kho + khuôn lỗi Dev đã sửa ở vòng refix 1 (ép bắt buộc làm hiện alert ngay sau thao tác đã thành công) · Đánh giá spec: Spec không ghi — cần Leader chốt kỳ vọng · Evidence: bản ghi mạng 2 yêu cầu + ảnh thông báo + truy vấn DB |
| TC-BULK001-10 | API | BULK-001 | リクエスト一括操作 | Abnormal | auto | staging | Duyệt hàng loạt có một đơn thất bại giữa chừng báo đúng số đơn đã xử lý | Bot A có 5 đơn đang chờ duyệt, trong đó 1 đơn được dựng để chắc chắn thất bại (ví dụ đơn có thanh toán mà cấu hình cổng đã bị gỡ). Đã ghi lại trạng thái 5 đơn | 1. Chọn cả 5 đơn, chọn thao tác duyệt rồi thực hiện<br>2. Ghi lại mã HTTP và toàn văn thông báo trên màn hình<br>3. Tải lại danh sách, kiểm trạng thái từng đơn trong 5 đơn<br>4. Truy vấn DB: trạng thái, lịch sử thao tác và lịch gửi nhắc của từng đơn<br>5. Kiểm số tin nhắn đã gửi cho khách so với số đơn thực sự được duyệt | 5 đơn chờ duyệt, 1 đơn dựng để thất bại | Thông báo trên màn hình **nêu rõ** số đơn đã xử lý và số đơn thất bại, không hiện thành công chung chung. Các đơn xử lý được chuyển đúng trạng thái và có lịch sử + lịch gửi nhắc; đơn thất bại giữ nguyên trạng thái, không sinh lịch sử sai và không gửi tin cho khách | | Lấp `Q7` · dẫn từ `TC-LSN-197` / `TC-LSN-199` của kho + journal mục 2(1) "bổ sung 5 chỗ trước đây bỏ qua kết quả thất bại của service mà vẫn báo thành công" · Đánh giá spec: Đã hỏi leader — liên quan `MT-23` của kho (chưa chốt) · Evidence: ảnh thông báo + truy vấn DB 5 đơn |
| TC-DATAREF001-01 | Data | DATA-REF-001 | コース — tạo/sửa/xóa | Abnormal | auto | staging | Gọi thẳng endpoint xoá khoá học khi còn đơn 予約確定 vẫn bị server từ chối | Bot A có 1 khoá học còn ít nhất 1 đơn ở trạng thái 「予約確定」 trong khung giờ tương lai, và 1 đơn ở trạng thái 「リクエスト」. Đã ghi lại mã khoá học, mã khung giờ, mã các đơn và số dòng của từng bảng | 1. Trên màn hình, bấm xoá khoá học đó và xác nhận bị chặn (đối chứng dương)<br>2. Gọi **trực tiếp** endpoint xoá khoá học với mã khoá học đó, **bỏ qua màn hình**<br>3. Ghi lại mã HTTP và thông báo trả về<br>4. Lặp lại bước 2 với khoá học chỉ còn đơn ở trạng thái 「リクエスト」<br>5. Truy vấn DB: khoá học, khung giờ tiếp nhận, đơn đặt chỗ, lịch sử thao tác đơn, lịch gửi nhắc<br>6. Mở lại tab 「コース設定」 đối chiếu khoá học vẫn hiển thị | Mã khoá học: của bot A, còn đơn 予約確定 · lần 2: còn đơn リクエスト | Cả 2 lần gọi thẳng endpoint đều bị **server từ chối** bằng mã 4xx kèm thông báo tiếng Nhật về việc còn đơn 予約確定 / リクエスト — không lần nào trả 200. Khoá học, khung giờ tiếp nhận và toàn bộ đơn vẫn còn nguyên trong DB, **không bảng con nào bị dọn dở dang**, không có đơn nào bị xoá mềm. Khoá học vẫn hiển thị trên tab 「コース設定」 | | Lấp `G10` · dẫn từ `BR-30` (*rule chỉ kiểm ở FRONT-END, server không kiểm lại*) + kho `TC-LSN-613` (dự kiến FAIL) + quyết định Leader C4 ngày 2026-09-22 · Đánh giá spec: Đã hỏi leader — Leader chốt đây là business rule ⇒ server phải chặn · Evidence: mã HTTP + ảnh chụp DB trước/sau |
| TC-NOTIMAIL001-01 | API | NOTI-MAIL-001 | 予約システムの削除 | Normal | manual | Tất cả | Mã xác thực xoá lịch vẫn gửi tới hộp thư thật và dùng được sau khi chuẩn hoá mã lỗi | Bot A có 1 lịch thử nghiệm không còn dùng. Tài khoản quản trị dùng hộp thư thật truy cập được | 1. Mở tab 「全体設定」 → mục xoá hệ thống đặt lịch, bấm gửi mã xác thực<br>2. Quan sát thông báo và mã HTTP trả về<br>3. **Mở hộp thư thật**, xác nhận đã nhận mail và ghi lại mã<br>4. Nhập đúng mã vừa nhận, xác nhận<br>5. Quan sát thông báo và mã HTTP ở bước kiểm mã<br>6. Hoàn tất xoá và đối chiếu lịch biến mất khỏi danh sách | Mã xác thực lấy từ mail thật | Yêu cầu gửi mã trả 200 và **mail tới hộp thư thật** với nội dung chứa mã. Nhập đúng mã trả 200 và mở khoá được bước xoá — không bị nhánh kiểm tra dữ liệu bắt buộc mới chặn nhầm. Lịch bị xoá khỏi danh sách | | Lấp `Q8` · dẫn từ `TC-LSN-454` / `TC-LSN-455` của kho · manual vì cần mail thật trong hộp thư · Đánh giá spec: Spec ghi rõ — `feature-spec.md` §8.10 BR-53 · Evidence: ảnh chụp mail + ảnh thông báo từng bước |
| TC-PERM003-11 | API | PERM-003 | Modal danh sách booking | Abnormal | auto | staging | Ba lối đọc danh sách đơn không trả dữ liệu của tài khoản khác | Bot A và bot B mỗi bên có lịch, khung giờ tiếp nhận và ít nhất 2 đơn đặt chỗ. Đã ghi lại số đơn của từng bot. Có `line_user_id` là bạn của bot B. Đang đăng nhập với bot A đang được chọn | 1. Gọi endpoint lấy danh sách đơn trong 1 khung giờ với mã khung giờ của bot B<br>2. Gọi endpoint lọc đơn tuỳ biến với mã lịch của bot B<br>3. Gọi endpoint lấy đơn gần nhất của 1 LINE user với `line_user_id` thuộc bot B<br>4. Ghi lại mã HTTP và số dòng trả về của từng lần<br>5. Lặp lại cả 3 lần gọi với dữ liệu của chính bot A (đối chứng dương)<br>6. So số dòng của bước 5 với số đơn hiển thị trên màn quản trị bot A | Mã khung giờ / mã lịch / line_user_id: của bot B · rồi của bot A | Ba lần gọi với dữ liệu bot B: trả 404 hoặc danh sách rỗng, **không lộ** tên khách, tên khoá học hay giờ đặt của bot B. Ba lần gọi với dữ liệu bot A: trả 200 và **đủ số dòng** đúng bằng số đơn trên màn quản trị — không bị lọc mất dòng nào |  | Lấp `G11` (`EP-39` · `EP-40` · `EP-62`) · Đánh giá spec: Spec ghi rõ — `api-spec.md` §2.2 · Evidence: mã HTTP + số dòng từng lần gọi + ảnh màn quản trị bot A |
| TC-FUNC001-10 | UI | FUNC-001 | Chi tiết lịch — endpoint phụ trợ | Normal | auto | staging | Ba endpoint phụ trợ của form, khung giờ và tooltip vẫn chạy đúng sau khi siết phạm vi | Bot A có lịch với khung giờ tiếp nhận (1 khung còn đơn 予約確定, 1 khung trống) và đã khai vài trường hồ sơ bạn bè. Tooltip hướng dẫn đang bật cho tài khoản đăng nhập | 1. Mở mục cấu hình câu hỏi form, quan sát danh sách trường hồ sơ bạn bè hiện ra để chọn<br>2. Đối chiếu số trường hiện ra với số trường khai ở màn 友だち情報管理<br>3. Bấm xoá khung giờ còn đơn 予約確定 — quan sát kết quả bước kiểm tra trước khi xoá<br>4. Bấm xoá khung giờ trống — quan sát kết quả bước kiểm tra<br>5. Tắt tooltip hướng dẫn, tải lại trang và kiểm tooltip không hiện lại | Trường hồ sơ bạn bè có sẵn của bot A · 1 khung giờ còn đơn 予約確定 · 1 khung giờ trống | Danh sách trường hồ sơ bạn bè trả đúng và đủ các trường của bot A, không lẫn trường của bot khác. Bước kiểm tra trước khi xoá trả **chặn** với khung còn đơn 予約確定 và **cho phép** với khung trống. Tắt tooltip có hiệu lực và không hiện lại sau khi tải lại trang |  | Lấp `G11` (`EP-22` · `EP-64` · `EP-65`) · Đánh giá spec: Spec ghi rõ — `BR-17` (chặn xoá khung còn 予約確定) · Evidence: ảnh màn hình + bản ghi mạng 3 endpoint |
| TC-UI002-10 | UI | UI-002 | 全体設定 — preview | Normal | auto | staging | Ba màn preview mở được và chỉ hiện nội dung của tài khoản đang đăng nhập | Bot A có lịch đã điền 「トップ画面設定」, 「店舗・ビジネス情報」 và ít nhất 2 câu hỏi form. Bot B có lịch với nội dung khác hẳn. Đã ghi lại nội dung đã lưu của bot A | 1. Mở lần lượt 3 màn preview của lịch bot A: preview trang top, preview thông tin cửa hàng, preview form câu hỏi<br>2. Đối chiếu nội dung từng preview với nội dung đã lưu ở màn cấu hình tương ứng<br>3. Đổi mã lịch trên đường dẫn của cả 3 màn preview sang mã lịch của bot B<br>4. Quan sát mã HTTP và nội dung hiển thị<br>5. Kiểm bảng điều khiển trình duyệt có lỗi JavaScript không | Mã lịch: của bot A · rồi của bot B | Ba màn preview của bot A mở được, không trắng trang, không lỗi JavaScript, và hiện đúng nội dung đã lưu. Với mã lịch của bot B: không hiện bất kỳ nội dung nào của bot B (trả 404 hoặc chuyển hướng). ⚠️ Nếu preview trang top hiện nội dung của trường khác với trường đã lưu ở màn cấu hình thì **ghi nhận lại** — `feature-spec.md` §3.1 có đánh dấu điểm bất thường ở `SCR-LSN-24` |  | Lấp `G11` (`EP-59` · `EP-60` · `EP-61`) + `G13` (`SCR-LSN-24/25/26`) · Đánh giá spec: Đã hỏi leader — spec đánh dấu bất thường ở `SCR-LSN-24`, cần Dev xác nhận · Evidence: ảnh 3 màn preview + ảnh màn cấu hình tương ứng |
| TC-INTGSHEET001-02 | API | INTG-SHEET-001 | Googleスプレッドシート連携 | Normal | manual | Tất cả | Callback liên kết Google Sheets lưu token đúng tài khoản đang đăng nhập | Bot A chưa liên kết Google Sheets. Có tài khoản Google thật dùng để cấp quyền. Đã ghi lại trạng thái liên kết hiện tại của bot A và bot B | 1. Ở màn 「Googleスプレッドシート連携」 của bot A, bấm liên kết Google<br>2. Hoàn tất cấp quyền trên trang Google, chờ quay về hệ thống<br>3. Quan sát thông báo và trạng thái liên kết trên màn hình<br>4. Kiểm trong DB: token và email tài khoản Google được ghi vào đúng bản ghi lịch của bot A<br>5. Mở màn tương ứng của bot B, đối chiếu trạng thái liên kết của bot B **không** bị thay đổi<br>6. Tạo 1 đơn đặt chỗ và đối chiếu dòng mới xuất hiện trên Google Sheet | Tài khoản Google thật có quyền tạo spreadsheet | Liên kết thành công, màn hình hiện trạng thái đã liên kết. Trong DB, token và email Google ghi vào **đúng** bản ghi lịch của bot A; trạng thái liên kết của bot B không đổi. Đơn đặt chỗ mới đẩy được lên Google Sheet của bot A |  | Lấp `G11` (`EP-01`) · manual vì phải đăng nhập tài khoản Google thật và cấp quyền OAuth · Đánh giá spec: Spec ghi rõ — `BR-55` · Evidence: ảnh màn liên kết + ảnh Google Sheet + truy vấn DB |
| TC-REGSHARED001-12 | UI | REG-SHARED-001 | Wizard tạo calendar | Normal | auto | staging | Đối chứng dương — nhóm endpoint lịch và khoá học vẫn trả 200 với dữ liệu hợp lệ | Bot A ở gói cho phép tạo thêm lịch và thêm khoá học. Đã ghi lại số lịch, số khoá học hiện có | 1. Gọi lần lượt từng endpoint trong bảng dữ liệu bằng dữ liệu hợp lệ đầy đủ<br>2. Ghi lại mã HTTP và cấu trúc thân phản hồi của **từng** lần gọi vào bảng kết quả theo dòng<br>3. Sau mỗi lần gọi, đối chiếu dữ liệu tương ứng trong DB đã thay đổi đúng như yêu cầu<br>4. Mở lại màn hình tương ứng, xác nhận thay đổi hiển thị đúng | Tạo lịch · Sửa thông tin lịch · Sắp xếp lịch · Lưu điều khoản · Lưu thông tin cửa hàng · Tạo khoá học · Sửa khoá học · Xoá ảnh khoá học · Bật/tắt hiển thị khoá học · Sắp xếp khoá học · Đọc chi tiết khoá học · Đọc tin nhắn thông báo của khoá học | **Mọi** endpoint trong bảng trả **200** với `success=true` và giữ nguyên cấu trúc phản hồi cũ. Dữ liệu trong DB đổi đúng và màn hình hiển thị đúng. **Không endpoint hợp lệ nào bị trả 4xx** do lớp kiểm tra dữ liệu bắt buộc mới. Bảng kết quả phải ghi đủ **từng dòng** endpoint — không gộp thành một kết luận chung |  | Lấp `G12` · mở rộng đối chứng dương của `NEW-21` (hiện chỉ 6/82 endpoint) · Đánh giá spec: Spec ghi rõ — `api-spec.md` §1.1 · Evidence: bảng `endpoint · HTTP · thân phản hồi · Đạt/Không` theo từng dòng |
| TC-REGSHARED001-13 | UI | REG-SHARED-001 | 受付枠 — thêm khung giờ | Normal | auto | staging | Đối chứng dương — nhóm endpoint khung giờ, đơn đặt chỗ và CSV vẫn trả 200 với dữ liệu hợp lệ | Bot A có lịch, khoá học và ít nhất 1 khung giờ còn chỗ. Có tệp CSV đúng định dạng. Đã ghi lại số khung giờ và số đơn hiện có | 1. Gọi lần lượt từng endpoint trong bảng dữ liệu bằng dữ liệu hợp lệ đầy đủ<br>2. Ghi lại mã HTTP và thân phản hồi của **từng** lần gọi vào bảng kết quả theo dòng<br>3. Đối chiếu DB sau mỗi lần gọi<br>4. Mở lại tab 「予約カレンダー」 và modal đơn, xác nhận hiển thị đúng | Thêm khung giờ · Sửa khung giờ · Xoá 1 khung giờ · Xoá nhiều khung giờ · Thêm đơn thủ công · Đổi trạng thái đơn · Xoá đơn · Lưu câu trả lời form của đơn · Nhập CSV khung giờ · Xuất CSV đơn của 1 khung giờ · Đọc danh sách đơn trong khung giờ · Đọc lịch sử thao tác đơn | **Mọi** endpoint trong bảng trả **200** với `success=true`. Dữ liệu DB đổi đúng, số khung giờ và số đơn khớp với màn hình. **Không endpoint hợp lệ nào bị trả 4xx** do lớp kiểm tra dữ liệu bắt buộc mới, và **không thao tác nào im lặng** (mỗi thao tác đều có phản hồi rõ trên màn hình). Bảng kết quả ghi đủ từng dòng endpoint |  | Lấp `G12` · Đánh giá spec: Spec ghi rõ — `api-spec.md` §2.2 · Evidence: bảng kết quả theo dòng + ảnh tab 予約カレンダー trước/sau |
| TC-REGSHARED001-14 | UI | REG-SHARED-001 | 全体設定 — メッセージ・締切・承認方式 | Normal | auto | staging | Đối chứng dương — nhóm endpoint cấu hình 全体設定 vẫn trả 200 với dữ liệu hợp lệ | Bot A có lịch đã bật nhắc lịch, có ít nhất 1 bước nhắc và vài câu hỏi form. Đã ghi lại toàn bộ cấu hình hiện tại của 全体設定 | 1. Gọi lần lượt từng endpoint trong bảng dữ liệu bằng dữ liệu hợp lệ đầy đủ<br>2. Ghi lại mã HTTP và thân phản hồi của **từng** lần gọi vào bảng kết quả theo dòng<br>3. Tải lại từng mục cấu hình trên màn hình, đối chiếu giá trị vừa lưu hiện đúng<br>4. Đối chiếu DB của từng mục | Lưu cấu hình tin nhắn đặt/huỷ · Đọc cấu hình tin nhắn · Lưu 承認方式 và hạn nhận đặt · Tạo cấu hình nhắc lịch · Lưu tin nhắn bước nhắc · Đọc chi tiết bước nhắc · Xoá bước nhắc · Tạo/sửa/xoá/sắp xếp câu hỏi form · Lưu toàn bộ form câu hỏi · Lưu cấu hình thông báo còn chỗ · Đọc lịch sử thông báo còn chỗ · Lưu cấu hình trang đặt chỗ | **Mọi** endpoint trong bảng trả **200** với `success=true`, giá trị vừa lưu hiển thị đúng khi tải lại màn hình và khớp DB. **Không endpoint hợp lệ nào bị trả 4xx**. Riêng `type_remind` của bước nhắc **đời cũ để trống** vẫn lưu được (cố ý không khai bắt buộc). Bảng kết quả ghi đủ từng dòng endpoint |  | Lấp `G12` · nhóm `/ajax/calendar/*` là nhóm Dev sửa nhiều nhất · Đánh giá spec: Spec ghi rõ — `api-spec.md` §2.3 · Evidence: bảng kết quả theo dòng + ảnh từng mục cấu hình sau khi tải lại |
| TC-UI001-10 | UI | UI-001 | Tab 本日/新着の予約 | Normal | auto | staging | Tab 本日／新着の予約 hiển thị đúng và duyệt / từ chối đơn ngay trên tab vẫn chạy | Bot A có lịch với ít nhất 1 đơn đặt chỗ hôm nay, 1 đơn mới đến đang chờ duyệt và 1 đơn đã huỷ. Bot B cũng có đơn hôm nay. Đã ghi lại trạng thái từng đơn | 1. Mở chi tiết lịch của bot A — tab 本日／新着の予約 là tab mặc định<br>2. Đối chiếu danh sách đơn hôm nay và đơn mới đến với dữ liệu thật của bot A<br>3. Xác nhận **không** có đơn nào của bot B xuất hiện<br>4. Duyệt đơn đang chờ ngay trên tab, quan sát thông báo và trạng thái đổi trên danh sách<br>5. Từ chối 1 đơn khác, quan sát thông báo<br>6. Tải lại trang và đối chiếu trạng thái 2 đơn vừa thao tác trong DB | Đơn hôm nay · đơn mới đến chờ duyệt · đơn đã huỷ | Tab mở đúng là tab mặc định, hiển thị đủ đơn hôm nay và đơn mới đến của **riêng** bot A, không lẫn đơn bot B. Duyệt và từ chối đều có thông báo rõ ràng (không im lặng), trạng thái đổi ngay trên danh sách và khớp DB sau khi tải lại |  | Lấp `G13` (`SCR-LSN-05`) · đây là tab mặc định khi mở chi tiết lịch nên mọi người dùng đều đi qua · Đánh giá spec: Spec ghi rõ — `feature-spec.md` §3.1 · Evidence: ảnh tab trước/sau + truy vấn DB |
| TC-FUNC002-10 | UI | FUNC-002 | 全体設定 — メッセージ・締切・承認方式 | Normal | auto | staging | Ba màn cấu hình 全体設定 lưu và hiển thị lại đúng sau khi siết phạm vi | Bot A có lịch đã điền sẵn cấu hình 3 màn: 「予約・キャンセルのメッセージ・リクエストと締切」, 「予約時の各種設定」, 「予約キャンセル時の各種設定」. Đã ghi lại toàn bộ giá trị hiện tại | 1. Mở màn 「予約・キャンセルのメッセージ・リクエストと締切」, đổi nội dung tin nhắn rồi lưu<br>2. Mở màn 「予約時の各種設定」, đổi 承認方式 và hạn nhận đặt rồi lưu<br>3. Mở màn 「予約キャンセル時の各種設定」, đổi 承認方式 huỷ rồi lưu<br>4. Với mỗi lần lưu: quan sát thông báo, tải lại màn và đối chiếu giá trị hiển thị<br>5. Đối chiếu DB của cả 3 màn<br>6. Đặt thử 1 đơn từ phía khách LINE và kiểm tin nhắn nhận được khớp cấu hình vừa đổi | Nội dung tin nhắn mới · 承認方式 đổi giữa tự động duyệt và chờ duyệt · hạn nhận đặt mới | Cả 3 màn lưu thành công với thông báo rõ ràng, giá trị hiển thị lại đúng sau khi tải lại và khớp DB. Đơn đặt thử đi đúng nhánh 承認方式 vừa cấu hình và khách nhận đúng nội dung tin nhắn vừa đổi |  | Lấp `G13` (`SCR-LSN-11` · `SCR-LSN-12` · `SCR-LSN-13`) · 3 màn cấu hình lõi, `saveSettingSendMessageEventStep` nằm trong diff · Đánh giá spec: Spec ghi rõ — `feature-spec.md` §3.1 · Evidence: ảnh 3 màn sau khi tải lại + ảnh tin nhắn trên LINE |
| TC-FUNC001-11 | UI | FUNC-001 | Wizard tạo calendar | Normal | auto | staging | Luồng tạo lịch mới đi hết tới màn khoá học đầu tiên và màn トップ画面設定 lưu được | Bot A ở gói còn cho phép tạo thêm lịch. Đã ghi lại số lịch hiện có | 1. Từ màn danh sách lịch, bấm 新規作成 và đặt tên lịch mới rồi lưu<br>2. Quan sát hệ thống chuyển sang màn đăng ký khoá học đầu tiên<br>3. Điền thông tin khoá học đầu tiên rồi lưu<br>4. Quan sát chuyển sang màn chi tiết lịch<br>5. Kiểm DB: lịch mới có đủ 2 cấu hình tin nhắn mặc định và 2 câu hỏi mặc định được tạo kèm<br>6. Mở màn 「トップ画面設定」, đổi ảnh và nội dung mô tả rồi lưu, tải lại và đối chiếu | Tên lịch mới · thông tin khoá học đầu tiên · ảnh + mô tả cho màn top | Luồng đi trọn từ tạo lịch → khoá học đầu tiên → chi tiết lịch, không màn nào đứng im hay báo lỗi. DB có lịch mới kèm **2** cấu hình tin nhắn mặc định và **2** câu hỏi mặc định. Màn 「トップ画面設定」 lưu thành công và hiển thị lại đúng sau khi tải lại |  | Lấp `G13` (`SCR-LSN-03` · `SCR-LSN-18`) · `EP-32` lưu khoá học đầu tiên là endpoint spec đánh dấu `A-12` bỏ qua toàn bộ validation · Đánh giá spec: Spec ghi rõ — `BR-07` (lazy-create 2+2) · Evidence: ảnh từng bước + truy vấn DB lịch mới |
| TC-SYNCAPP001-10 | UI | SYNC-APP-001 | App mobile | Normal | manual | Tất cả | App quản trị mobile đọc đủ dữ liệu lịch bài học của tài khoản đang đăng nhập | App quản trị mobile đã đăng nhập bot A. Bot A có ít nhất 2 lịch, mỗi lịch có khoá học, khung giờ và đơn đặt chỗ ở nhiều trạng thái, có thông báo chưa đọc. Đã mở sẵn màn quản trị web của bot A để đối chiếu | 1. Mở app, vào danh sách lịch bài học — đối chiếu số lịch với web<br>2. Mở chi tiết 1 lịch — đối chiếu tên, khoá học với web<br>3. Mở danh sách đơn theo tab 新規 và tab 本日 — đối chiếu số đơn với web<br>4. Mở chi tiết 1 đơn, xem lịch sử thao tác và lịch sử thanh toán — đối chiếu với web<br>5. Mở danh sách khoá học (gồm cả khoá đang ẩn) — đối chiếu với web<br>6. Mở lịch theo tháng và theo ngày — đối chiếu khung giờ với web | Không có dữ liệu nhập — dùng dữ liệu sẵn có của bot A | Toàn bộ 6 màn trên app hiển thị **đủ và đúng** dữ liệu của bot A, khớp từng con số với màn quản trị web: số lịch, số khoá học (kể cả khoá đang ẩn), số đơn từng tab, nội dung lịch sử thao tác và lịch sử thanh toán. **Không màn nào rỗng** do hàm lấy bot đọc từ phiên đăng nhập trả về rỗng ở luồng app |  | Lấp `G14` — nhóm ĐỌC (`EP-M02` · `EP-M03` · `EP-M04` · `EP-M05` · `EP-M06` · `EP-M07` · `EP-M09` · `EP-M10`) · manual vì cần thiết bị thật chạy app admin mobile · Đánh giá spec: Spec ghi rõ — `feature-spec.md` §9.5 · Evidence: ảnh chụp từng màn app đặt cạnh màn web tương ứng |
| TC-SYNCAPP001-11 | UI | SYNC-APP-001 | App mobile | Abnormal | manual | Tất cả | App quản trị mobile ghi dữ liệu vẫn chạy đúng và khớp ngược lại với web | App quản trị mobile đã đăng nhập bot A. Bot A có 1 đơn đang chờ duyệt, 1 đơn đã thanh toán (cổng ở chế độ thử nghiệm), 1 khung giờ còn chỗ và 1 khung giờ trống. Đã ghi lại trạng thái tất cả | 1. Trên app, duyệt đơn đang chờ — đối chiếu trạng thái trên web và bản ghi lịch gửi nhắc trong DB<br>2. Trên app, hoàn tiền đơn đã thanh toán — đối chiếu trạng thái thanh toán trên web và giao dịch trên cổng<br>3. Trên app, sửa sức chứa 1 khung giờ — đối chiếu trên web<br>4. Trên app, thêm rồi xoá 1 khung giờ — đối chiếu trên web<br>5. Trên app, admin đặt chỗ hộ khách — đối chiếu đơn mới trên web<br>6. Trên app, đổi cấu hình form お客様情報 — đối chiếu trên web<br>7. Sau mỗi thao tác: kiểm thông báo trên app có rõ ràng không, có im lặng không | Đơn chờ duyệt · đơn đã thanh toán · khung giờ còn chỗ · khung giờ trống | Cả 6 thao tác ghi đều thành công trên app và **phản ánh đúng sang web** ngay sau khi tải lại: trạng thái đơn, trạng thái thanh toán, sức chứa khung giờ, khung giờ mới/đã xoá, đơn admin đặt hộ, cấu hình form. Lịch gửi nhắc và thông báo ứng dụng được ghi/dọn đúng. **Không thao tác nào im lặng** — mỗi thao tác đều có phản hồi rõ trên app |  | Lấp `G14` — nhóm GHI (`EP-M01` · `EP-M08` · `EP-M11` · `EP-M12` · `EP-M13` · `EP-M14` · `EP-M15`) · manual vì cần thiết bị thật + cổng thanh toán · Đánh giá spec: Đã hỏi leader — spec ghi `0/15` endpoint app kiểm `bot_id` và `EP-M11`/`EP-M15` không kiểm sức chứa (`S-20`/`S-22`), đã đưa §8 · Evidence: ảnh app + ảnh web đặt cạnh nhau cho từng thao tác |

---

## 8. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | `spec-features/admin/lesson-booking/feature-spec.md` §11.3 — middleware `checkLessonCalendarInBot` | ✅ **Leader đã chốt: hành vi đúng = HTTP 404.** Việc phải làm: (1) sửa §11.3 — bỏ mô tả *"redirect im lặng về `calendar.index`"*, ghi rõ middleware `checkLessonCalendarInBot` **trả 404** khi `{id}` không thuộc bot đang đăng nhập; (2) Dev rà **nhánh ajax** của nhóm route lịch bài học — Studio ghi nhận còn **302** về danh sách khiến màn hình im lặng, phải đưa về 404 giống `detailCalendar` | `C1 CONF-SPEC` (§4) — **đã chốt** | `Dev` (thực thi) |
| 2 | `kho-tcs/fa019-datlichbaihoc-レッスン予約.md` §Điểm mâu thuẫn **MT-18** | ✅ **Leader đã chốt: hành vi đúng = HTTP 404 kèm 「受付枠が見つかりません。」.** Việc phải làm: (1) **đóng `MT-18`** với kết luận *"đã fix ở #40050 — `updateReception` trả 404 khi không tìm thấy khung giờ"*; (2) sửa expected `TC-LSN-286` (mở 2 tab, tab 1 xoá slot, tab 2 edit slot đó) thành **404 + 「受付枠が見つかりません。」**, bỏ vế *"HOẶC tự reload"*; (3) build lại kho (`python kho-tcs/build.py md` + `sheet`) sau khi sửa `kho-tcs/data/*.py` | `C2 CONF-KHO` (§4) — **đã chốt** | `Leader` (thực thi) |
| 3 | `feature-spec.md` §11.1 TOP-12 + §11.2 bảng IDOR — `A-02`, `A-04`, `A-06`, `A-09` | 4 lỗ IDOR này nay được tuyên bố đã vá bằng ràng buộc `getBotId()`. Spec vẫn mô tả hiện trạng trước fix ⇒ người đọc spec sau này sẽ hiểu sai. Update sau khi chốt I1 (bản build) và sau khi 18 bug được triage | Fix phần (3) — `03-dev-impact.md` mục 2 | `Dev` + `Leader` |
| 4 | `feature-spec.md` §11.3 — cột *"Kiểm sở hữu `bot_id`"* của nhóm route A | Đang ghi *"chỉ ở cấp `{id}` calendar, **không kiểm id con**"*. Bản fix bổ sung 4 hàm dựng truy vấn con cho các bảng không có cột bot ⇒ cần mô tả lại cơ chế mới | Fix phần (3) | `Dev` |
| 5 | `feature-spec.md` §11.1 `A-02` / kho `MT-16` / `TC-LSN-612` — mass assignment `POST /{id}/edit` | Fix vá **IDOR** (chặn theo bot) nhưng journal chỉ nói thêm kiểm tra **bắt buộc**, không nói thêm danh sách trắng trường. Nếu vẫn `update($request->all())` thì admin bot A vẫn tự ghi đè `bot_id` / `is_use_payment` / `code_delete` trên **lịch của chính mình**. Cần Dev xác nhận, nếu chưa vá thì raise ticket riêng | `feature-spec.md` §11.1 mục 3 + kho `MT-16` | `Dev` |
| 6 | `NEW-42` / `NEW-48` / `NEW-71` trên Studio (spec + kho **không** đổi) | ✅ **Leader đã chốt (C3)**: *không cần snapshot* ⇒ bỏ vế kiểm snapshot khỏi TC, **không** đụng `BR-31` hay kho `TC-LSN-77`. Việc phải làm: (1) `testcase_update` `NEW-42` bỏ câu về ảnh chụp; (2) gộp vế 「削除済み予約」 vào `NEW-42` rồi `testcase_delete` `NEW-48` + `NEW-71` (§3 `DUP-1`). ⏳ **Chờ xác nhận 1 câu**: *"không cần snapshot"* hiểu là bỏ khỏi **TC** (đã áp dụng) chứ không phải bỏ khỏi **sản phẩm** — nếu là sản phẩm thì màn 「削除済み予約」 mất cột 予約していたコース và phải mở ticket riêng | `C3 CONF-SPEC` (§4) — **đã chốt** | `Leader` (thực thi trên Studio) |
| 7 | `NEW-42` / `NEW-48` trên Studio + kho `TC-LSN-72` / `TC-LSN-73` / `TC-LSN-77` | ✅ **Leader đã chốt (C4)**: chặn xoá khi còn đơn 予約確定/リクエスト · cho xoá khi toàn bộ đơn đã huỷ · sau khi xoá → đơn **xoá mềm** + hiện ở 「削除済み予約」. Việc phải làm: (1) `testcase_update` `NEW-42` — *"đã bị dọn"* → **"bị xoá mềm (`deleted_at`), biến khỏi danh sách đơn thường"**; (2) `testcase_update` `NEW-48` — *"vẫn hiển thị"* → **"vẫn xem được ở màn 「削除済み予約」"**; (3) kho đã đúng, không phải sửa | `C4 CONF-TC` (§4) — **đã chốt** | `Leader` (thực thi trên Studio) |
| 8 | Phạm vi phân quyền **Staff** cho FA-019 — `feature-spec.md` `A-01` / `BR-56` + kho `MT-43`, `TC-LSN-611`, `TC-LSN-624` | Ticket #40050 siết theo **bot** nhưng **không** chạm lưới phân quyền theo **role**: nhóm route `/ajax/calendar/*` (17 EP) vẫn ngoài `basic_access`, vẫn miễn CSRF, và 0/82 endpoint có `checkHasPermission`. Cần chốt: phần này thuộc phạm vi ticket khác hay bỏ ngỏ? | `Q1` (§2) | `Leader` + `PM` |
| 9 | `feature-spec.md` §11 `RA-08` + `RA-01` / `A-14` + §4.7 chuỗi xoá 10 nhóm | ✅ **Leader đã chốt (C5)**: xoá calendar phải xoá toàn bộ bản ghi liên quan, **không để lại mồ côi**. ⇒ `NEW-40` / `NEW-41` **đúng, không sửa TC**; bug mồ côi là **defect thật phải fix**. Việc phải làm: (1) Dev sửa `deleteCalendar()` bước 4 → duyệt booking bằng `withTrashed()` để dọn `history` + `mobile_notify` của cả đơn đã xoá mềm; (2) cân nhắc bọc **transaction** cho chuỗi 10 nhóm (`RA-01`/`A-14` — transaction từng có rồi bị comment), vì kỳ vọng *"0 mồ côi"* không đạt được nếu chuỗi đứt giữa chừng — `NEW-47` đang fail đúng điểm này; (3) update spec: `RA-08` chuyển sang trạng thái đã xử lý sau khi fix | `C5 CONF-SPEC` (§4) — **đã chốt** | `Dev` (thực thi) |
| 10 | `NEW-40` / `NEW-49` trên Studio (kho `TC-LSN-457` giữ nguyên) | ✅ **Leader đã chốt (C6)**: remind của các đơn đó bị xoá, friend không còn nhận remind. Việc phải làm: (1) `testcase_update` `NEW-40` — giới hạn phạm vi xoá `events`/`event_step`/`event_step_time` về **`type = 4` của lịch bài học**, ghi rõ `events type = 2` của booking calendar **phải còn**; (2) `NEW-49` nêu rõ chỉ áp cho đường **xoá đơn** — kho `TC-LSN-408` nhánh 2 (chờ duyệt huỷ) và 5 (bị từ chối huỷ) **cố ý giữ** remind vì đơn vẫn còn hiệu lực; (3) bổ sung kiểm output cuối trên LINE → đã đưa vào `TC-MSG002-10` (§7) | `C6 CONF-KHO` (§4) — **đã chốt** | `Leader` (thực thi trên Studio) |
| 11 | `NEW-55` / `NEW-72` / `NEW-62` trên Studio + `feature-spec.md` §4.6 | ✅ **Leader đã chốt (C7)**: lỗi hoàn tiền do hệ thống thanh toán trả về thì **hiển thị đúng message của hệ thống thanh toán** ⇒ **kho đúng, TC sai**. Việc phải làm: (1) `testcase_update` `NEW-72` + `NEW-55` bỏ kỳ vọng *"tiếng Nhật"* cho nội dung đến từ cổng; (2) `NEW-62` ghi rõ message từ cổng thanh toán **ngoài phạm vi** kiểm *"không còn chuỗi tiếng Anh"*; (3) spec ghi **ngoại lệ** cho yêu cầu (2) của ticket; (4) triage lại bug **#1168** — không còn là bug về ngôn ngữ. ⏳ **Còn 1 câu hỏi**: lỗi **credential/cấu hình** (`Invalid API Key provided: sk_test_***`) có nằm ngoài quy tắc *"hiển thị nguyên văn"* không, vì `SEC-002` cấm lộ credential | `C7 CONF-KHO` (§4) — **đã chốt**, còn 1 câu hỏi | `Leader` + `Dev` |
| 12 | kho `MT-23` + `TC-LSN-197` vs `NEW-61` | `MT-23` đang để ngỏ *"thao tác hàng loạt với booking không khớp action: im lặng nhưng API vẫn trả `success: true`"*; `NEW-61` cũng để ngỏ expected (§5 I7). Chốt **một lần** kỳ vọng cho thông báo khi có đơn bị bỏ qua, rồi sửa cả 2 phía | `Q7` (§2) + `I7` (§5) | `Leader` |
| 13 | `feature-spec.md` §9.5 — app di động Admin (15 endpoint) | ⚠️ **Phát hiện khi rà coverage 2026-09-22**: spec ghi **0/15** endpoint app kiểm `bot_id`, chỉ **1/15** (`EP-M02`) gọi `checkHasPermission`. Ticket #40050 yêu cầu *"MỌI query phải ràng buộc theo bot"* nhưng Dev **cố ý loại trừ** app / API / job (hàm lấy bot đọc từ session). Thêm `EP-M11` sửa sức chứa **không chặn hạ dưới số đã đặt** (`S-22`) và `EP-M15` admin đặt hộ **không kiểm sức chứa** (`S-20`). Cần chốt: phần app có thuộc phạm vi ticket này không, hay mở ticket riêng | Rà coverage endpoint (§1 `G14`) | `Leader` + `PM` |
