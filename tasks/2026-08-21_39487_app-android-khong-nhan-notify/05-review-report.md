# 05 — Review Report

## Thông tin

| Trường | Giá trị |
|---|---|
| Bug ID | `#39487 — [Step][Notify setting][App][Android] Phía app android không nhận được notify (nhận notify bị chập chờn lúc được lúc không)` |
| Reviewer (Leader) | `<điền tên>` |
| Tester được review | **AI** (32/42 TC, `created_job_id=341`) + `haodtb` (10/42 TC) — bộ TC fetch từ MCP LME TEST STUDIO `task_id=73` |
| Ngày review | `2026-08-21` |
| Version TCs | round 1 · 42/42 TC ở `version=1`, `status=draft`, `reviewed=false` |
| Vòng review | `Round 1` |
| Nguồn file 04 | MCP LME TEST STUDIO (Redmine KHÔNG có "Link TCs") |

> **Spec reference**: folder không có `02-spec-reference.md` → dùng [templates/LME-SYSTEM-SPEC.md](../../templates/LME-SYSTEM-SPEC.md) §3.6 **FA-006 「通知設定」** + spec gốc [spec-features/admin/notify-setting/feature-spec.md](../../spec-features/admin/notify-setting/feature-spec.md).

---

## 1. Verdict

- [ ] **APPROVED** — TCs đạt, không cần chỉnh sửa
- [ ] **APPROVED WITH CHANGES** — Approve sau khi fix các issue MINOR (không cần review lại)
- [x] **REJECTED** — Có issue BLOCKER/MAJOR, cần fix và review lại

**Lý do ngắn gọn**: Studio báo `aiResult=pass` nhưng **28/42 TC (66,7%) ở trạng thái `skip`** — trong đó **20/20 TC nhóm `api` và 21/21 TC `exec_mode=auto`**; **2 TC duy nhất verify trực tiếp root cause của #39487 (app đổi `device_id` sang `ANDROID_ID`) đều `skip`** → ticket hiện **không có một bằng chứng nào** chứng minh fix của chính nó hoạt động. Cộng thêm `03-dev-impact.md` **trống mục 3 + 4.1/4.2/4.3**, không thể lập coverage matrix có giá trị.

---

## 2. Tóm tắt cho member

Bộ TC này **viết rất tốt về mặt kỹ thuật** — precondition nêu rõ seed data (`TOK_A_39487`, `OLDREC.39487`), expected result đo lường được tới từng field DB và HTTP body, và đặc biệt `TC-TOOLKNOW002-02` (đối chứng chạy trên nhánh **trước fix** để chứng minh bộ TC thật sự bắt được bug) là một ý tưởng rất đáng khen, nên nhân rộng sang task khác.

Vấn đề không nằm ở chất lượng viết mà ở **chỗ dừng lại**: 2/3 bộ TC chưa hề chạy, và phần chưa chạy đúng là phần lõi (toàn bộ `api` + toàn bộ `auto`). Quan trọng hơn, **trọng tâm bị lệch ticket** — 20 TC `api` đều có tiền đề *"Backend chạy nhánh chứa fix #39559"*, tức đang verify fix của **ticket #39559** (đã có folder review riêng, đã bị REJECTED), trong khi deliverable của **#39487 là bản app Android** (`ANDROID_ID` qua MethodChannel) thì chỉ có 2 TC và cả 2 đều `skip`.

Việc cần làm theo thứ tự: (1) chạy `TC-DATAID001-01/02` để có bằng chứng cho chính #39487; (2) bổ sung 15 TC ở §5 cho 6 GAP (đường nâng cấp app, đồng thời, trigger space thông báo, nhánh scheduled, production, job lô lớn); (3) xin Dev điền mục 3 + 4.1/4.2/4.3 của `03-dev-impact.md`.

---

## 3. Coverage Matrix

> ⚠️ **Input thiếu — matrix này là SUY LUẬN, không phải đối chiếu.** `03-dev-impact.md` mục 3 và mục 4.1/4.2/4.3 **trống** (Dev chưa cung cấp).
> - Impact ký hiệu **`BUG`, `Fn`, `Dn`, `Tn`** dưới đây do Leader/Claude **tự dựng** từ: (a) journal Redmine #129164 mục 3+4 — nguồn Redmine hợp lệ; (b) **Phụ lục A** của file 03 — nguồn **MCP Studio, `contentTrust=untrusted`, chưa Dev xác nhận trên Redmine**.
> - Impact đánh dấu **`[A]`** = chỉ có ở Phụ lục A → **bắt buộc hỏi Dev xác nhận** trước khi dùng matrix này làm căn cứ approve.
> - Cột `Status` tính theo **kết quả thực thi thật**, không tính TC `Chưa test`.

| Impact | Loại | Priority | TCs map | # TC | # Đạt | Status |
|---|---|---|---|---|---|---|
| **BUG** — app Android gửi `device_id` = build ID (`BP4A.251205.006`) thay vì `ANDROID_ID`; fix = MethodChannel `Settings.Secure.ANDROID_ID` (`MainActivity.kt`, commit `6daa49c`) | Fix | — | `TC-DATAID001-01` (NEW-24), `TC-DATAID001-02` (NEW-38) | 2 | **0** | **GAP thực tế** → `[BLOCKER] GAP-1` |
| **BUG-b** — đường nâng cấp app 2.8.6 (build ID) → bản fix (`ANDROID_ID`) trên máy **đã có bản ghi cũ**; Dev khẳng định *"bản ghi cũ trên server tự khớp lại"* | Fix | — | — | **0** | 0 | **GAP** → `[BLOCKER] GAP-2` |
| **F1** `[A]` — `MobileController@updateFirebaseToken` (`POST /api/mobile/update-firebase_token`) | Function | Direct | `TC-TOOLKNOW002-01/02`, `TC-FUNC001-01`, `TC-TOOLAXIS001-01`, `TC-TOOLERRHYG001-01`, `TC-TOOLOLDREC001-01`, `TC-SYNCAPP001-02` | 7 | **0** | **RISK** — 7/7 `skip` (thuộc phạm vi #39559) |
| **F2** `[A]` — `MobileController@getFirebaseToken` (`POST /api/mobile/get-token-firebase`) — bỏ HẲN fallback theo `device_id`, bỏ lọc `os` | Function | Direct | `TC-FUNC001-02`, `TC-APICONTRACT001-01`, `TC-SECISO001-01`, `TC-SEC001-01`, `TC-TOOLNEGCTRL001-01`, `TC-TOOLOLDREC001-02` | 6 | **0** | **RISK** — 6/6 `skip` |
| **F3** `[A]` — `MobileController@deleteFirebaseToken` (`POST /api/mobile/delete-firebase_token`) | Function | Direct | `TC-TOOLNEGCTRL001-02`, `TC-TOOLERRHYG001-02`, `TC-TOOLOLDREC001-03` | 3 | **0** | **RISK** — 3/3 `skip` |
| **F4** `[A]` — `AuthMobileController@logout` — `orderByDesc('id')` trước khi đánh dấu huỷ subscribe topic | Function | Direct | `TC-STATECLEAN001-01`, `TC-AUTHSESSION001-01`, `TC-TOOLOLDREC001-04`, `TC-STATECLEAN001-03` | 4 | **1** | **RISK** — chỉ `TC-STATECLEAN001-03` (NEW-40) Đạt |
| **F5** `[A]` — job `firebase:subscribe_topic` (`HandleSubscribeTopic`) — đọc `status_subscribe_topic IN (0,2)` | Function | Indirect (dây chuyền) | `TC-JOB002-01/02`, `TC-STATECLEAN001-02`, `TC-TOOLOLDREC001-05` | 4 | **3** | **RISK** — không có TC rate limit / retry-backoff / vào=ra (`JOB-001`) → `[MAJOR]` |
| **F6** — `MainActivity.kt` MethodChannel đọc `ANDROID_ID` (app Flutter, repo `lme-fluter-app`) | Function | **Direct — đây là fix của #39487** | `TC-DATAID001-01/02` | 2 | **0** | **GAP thực tế** → `[BLOCKER] GAP-1` |
| **D1** `[A]` — `user_firebase_token` (`user_id`, `device_id`, `os`, `firebase_token`, `status_subscribe_topic`) — CREATE / UPDATE / DELETE | Data | — | `TC-TOOLOLDREC001-01..05`, `TC-TOOLKNOW002-01`, `TC-SYNCAPP001-05`, `TC-STATECLEAN001-03`, `TC-DATAMIG001-01` | 9 | **2** | **RISK** — có kiểm `WHERE` scope 2 tài khoản (RULE-07 ✓) nhưng 7/9 `skip` |
| **D2** `[A]` — `is_notify_mobile_maintain` (popup bảo trì) | Data | — | `TC-REGSHARED001-05` (NEW-19) | 1 | **0** | **RISK** — 1 TC, `skip` |
| **D3** — `notify_setting.app_notification_settings` / `notification_schedule` (tiền đề gửi push) | Data | — | `TC-OUTTRUTH001-01` (NEW-30) | 1 | **0** | **RISK** — 1 TC, `skip`, chỉ test `schedule = 0` |
| **T1** — Nhận push notification trên app Android (FA-006 kênh App) | Feature | **High** | `TC-TOOLKNOW002-03`, `TC-FUNCSEQ001-01`, `TC-TOOLNEGCTRL001-03`, `TC-COMPATLEGACY001-01`, `TC-SECISO001-02`, `TC-SYNCAPP001-04` | 6 | **4** | **RISK** — trigger space chỉ 1-2 loại / **13 hạng mục** → `[BLOCKER] GAP-5`; nhánh `schedule ≠ 0` 0 TC → `[BLOCKER] GAP-6` |
| **T2** — Badge số chưa đọc trên app | Feature | Medium (Dev) — nhưng `DATA-COUNT-001` là **Cao** | `TC-FUNC001-02`, `TC-SECISO001-01`, `TC-SYNCAPP001-03` | 3 | **1** | **RISK** — chưa đối chiếu đủ 4 nguồn + phép tính tay → `[MAJOR]` |
| **T3** — Đăng xuất / huỷ đăng ký topic | Feature | High | `TC-STATECLEAN001-01/02/03`, `TC-TOOLNEGCTRL001-03`, `TC-TOOLOLDREC001-04` | 5 | **3** | **OK** — chiều tốt nhất của bộ TC |
| **T4** — Regression các màn app dùng chung `device_id` (Booking Salon / Lesson / Chat 1:1 / Khách hàng / Chuyển bot) | Feature | High | `TC-REGSHARED001-01..04`, `TC-SYNCAPP001-01` | 5 | **5** | **RISK** — 5/5 Đạt nhưng **không có danh sách nơi ảnh hưởng từ Dev** → `[BLOCKER] GAP-4` `[AP-6]`; precondition đều "data sạch" → `[AP-3]` |
| **T5** — Đa thiết bị / đa tài khoản cùng `device_id` | Feature | High | `TC-SYNCAPP001-02/04/05`, `TC-SECISO001-02`, `TC-COMPATLEGACY001-01` | 5 | **2** | **RISK** |

### ORPHAN TCs

| TC No. | Title (rút gọn) | Lý do orphan | Hành động đề xuất |
|---|---|---|---|
| `TC-TOOLKNOW002-01/02`, `TC-FUNC001-01/02`, `TC-TOOLAXIS001-01`, `TC-TOOLERRHYG001-01/02`, `TC-TOOLOLDREC001-01/02/03/04`, `TC-SYNCAPP001-02`, `TC-APICONTRACT001-01`, `TC-SECISO001-01`, `TC-SEC001-01`, `TC-TOOLNEGCTRL001-01/02`, `TC-STATECLEAN001-01`, `TC-AUTHSESSION001-01`, `TC-REGSHARED001-05` — **20 TC nhóm `api`** | Toàn bộ API `update` / `get` / `delete` firebase token + `logout` + `hide-popup-maintain` | Tiền đề của **cả 20 TC** ghi *"Backend chạy nhánh chứa fix **#39559**"* → đang verify fix của **ticket khác**. Deliverable của #39487 là **bản app Android** (`ANDROID_ID`). Folder [tasks/2026-08-13_39559_.../](../2026-08-13_39559_app-smartphone-khong-nhan-thong-bao/) đã review cùng phạm vi này và **REJECTED**. | **KHÔNG remove.** Giữ làm regression backend nhưng: (1) Leader xác nhận với Dev ranh giới #39487 ⇄ #39559 để **không review 2 lần**; (2) nếu #39559 đã chạy xong 20 TC này thì **tái sử dụng kết quả**, đừng chạy lại; (3) đánh dấu `regression` ở `Ghi chú` — xem `[MAJOR] AP-5`. |

> Không có TC nào lạc chủ đề hoàn toàn (mọi TC đều thuộc scope `notify-setting` / `user_firebase_token`).

---

## 3.5 Fix-shape analysis (adversarial)

| Mục | Giá trị |
|---|---|
| **Fix shape** (đọc mục 2 dev-impact) | **Hỗn hợp 3 shape**: (1) **Migration / version-up** — đổi **nguồn sinh `device_id`** ở app (build ID → `ANDROID_ID`), tồn tại dữ liệu cũ trên server ⇒ `DATA-MIG-001` + `COMPAT-LEGACY-001`; (2) **Race-condition / dedupe** `[A]` — backend `orderByDesc('id')` → update bản mới nhất → **xoá bản cũ hơn** = read-modify-write ⇒ `CONC-001`; (3) **Sửa hàm dùng chung** — `MobileController` + native `MainActivity.kt` phục vụ mọi luồng app ⇒ `REG-SHARED-001` |
| **Trigger space cần cover** | **(a) Đường nâng cấp app** — 4 tổ hợp: máy sạch·app mới / máy **đã có bản ghi build ID**·app mới / máy app cũ / **máy app cũ + máy app mới cùng model chạy song song**.<br>**(b) Hạng mục thông báo** — FA-006 có **13 hạng mục** (chat 1:1, bạn bè mới, broadcast, QR code, form, salon/lesson/event booking, bán hàng, action schedule, conversion, ASP, delivery count alert) + `TYPE_SYSTEM`.<br>**(c) Chế độ gửi** — `notification_schedule` **enum 0–7** (0 = リアルタイム gửi ngay; 1–7 = qua job poll 60s theo `next_notify_time`).<br>**(d) Đồng thời** — 4 kịch bản `CONC-001`. |
| **Số trigger TCs hiện cover** | **(a) 2/4** — thiếu "máy đã có bản ghi build ID rồi nâng cấp" và "cũ + mới chạy song song" → `GAP-2`.<br>**(b) ~1/13** — TC chỉ ghi *"sinh sự kiện notify"* chung chung; `TC-TOOLKNOW002-03` nêu ví dụ **chat 1:1** → **trái spec** (xem GAP-5).<br>**(c) 1/8** — chỉ `schedule = 0` → `GAP-6`.<br>**(d) 0/4** → `GAP-3`. |
| **KH report dạng** | **Symptom-only** — file 01 chỉ ghi *"Không nhận được notify"*, **không** error code / log / screenshot (`attachments = []`). Dev tự tái hiện **1 root cause** (device_id trùng) trên Galaxy A55. |
| **Alternative root causes cần verify** | (1) **Quyền `POST_NOTIFICATIONS` bị từ chối** (bắt buộc runtime từ Android 13) — máy Galaxy A55 chạy **Android 16**; (2) **Doze / tối ưu pin** giết FCM khi app ở background — khớp mô tả *"mở lại app thì hết"*; (3) **notification channel bị tắt ở tầng OS**; (4) **FCM token bị `NotRegistered`/xoay vòng** mà app không gửi lại token; (5) **kênh 「スマートフォンアプリ」 tắt hoặc `notification_schedule ≠ 0`** ở màn 通知設定 → push đi qua job poll, trễ tới 24h = "lúc được lúc không". → **0 TC** cho (1)(2)(3)(4); (5) chỉ có `TC-OUTTRUTH001-01` và đang `skip`. |
| **Anti-patterns dính** | **AP-2** (symptom-only, 0 TC cho alternative root cause) · **AP-3** (5 TC regression đều precondition "data sạch", không edge state) · **AP-4** (backend ghi *"làm trước, tạm thời"*, **không mô tả, không PR link trong Redmine** → không verify được fix shape backend) · **AP-5** (20 TC `api` test layer backend thuộc #39559) · **AP-6** (mục 3 dev-impact **trống**). **Không dính AP-1** (fix không phải generic catch-all). |

> Trigger space cover **(a) 2/4 · (b) 1/13 · (c) 1/8 · (d) 0/4** → flag `[BLOCKER] FIX-SHAPE` ở §4.1.

---

## 4. Issues phát hiện

### 4.1 Blocker (phải fix trước khi merge)

- `[BLOCKER] FIX-SHAPE-1` — **`aiResult=pass` không phải bằng chứng: 28/42 TC (66,7%) ở trạng thái `skip`.** Phân bố skip **không ngẫu nhiên**: **20/20 TC nhóm `api`** và **21/21 TC `exec_mode=auto`** đều skip; chỉ `job` (8/12 Đạt) và `ui` (5/6 Đạt) có kết quả. Nghĩa là phần **automation của task này chưa từng chạy được** và Studio **không lưu lý do skip** cho từng TC. `aiResult=pass` chỉ phản ánh "không TC nào fail" — Leader đọc lướt rất dễ hiểu nhầm là "đã test xong". — **Fix**: (1) `haodtb` ghi rõ **lý do skip** cho 28 TC (thiếu 2 máy thật? thiếu account? automation lỗi? thiếu quyền DB?) vào `Ghi chú` từng TC trên Studio; (2) chạy lại toàn bộ nhóm `api` + `auto`; (3) đề nghị Studio **không đặt `aiResult=pass` khi tỷ lệ `skip` > 20%** — nên có state riêng (`partial`).

- `[BLOCKER] GAP-1` — **Root cause của chính #39487 có 0 bằng chứng.** Fix của ticket này là **app Android đổi nguồn `device_id` sang `Settings.Secure.ANDROID_ID`** (commit `6daa49c`, `MainActivity.kt`). Đúng **2 TC** verify trực tiếp thay đổi đó — `TC-DATAID001-01` (NEW-24: 2 máy cùng model gửi `device_id` khác nhau) và `TC-DATAID001-02` (NEW-38: login sinh bản ghi với `device_id` = `ANDROID_ID`, không còn build ID) — và **cả 2 đều `skip`**. Trong khi đó 20 TC được viết cho backend (#39559). ⇒ Ticket đang ở trạng thái *"Fix done - Đợi test", done_ratio 100%* nhưng **chưa ai chứng minh app đã gửi đúng `ANDROID_ID`**. — **Fix**: chạy `TC-DATAID001-01` + `TC-DATAID001-02` trên **2 máy Android thật cùng model + cùng firmware**, evidence bắt buộc: ảnh chụp `adb shell settings get secure android_id` của **từng máy** đặt cạnh kết quả query `SELECT user_id, device_id FROM user_firebase_token`. Không chạy được 2 máy thật → **ghi phiếu chặn**, không được để `skip` im lặng.

- `[BLOCKER] GAP-2` — **Đường nâng cấp app (2.8.6 → bản fix) trên máy ĐÃ có bản ghi build ID: 0 TC.** Dev khẳng định trong journal: *"Chọn ANDROID_ID vì đúng bằng giá trị app dùng tới bản 2.8.5 → **bản ghi cũ trên server tự khớp lại**"*. **Không TC nào verify claim này**: `TC-COMPATLEGACY001-01` (NEW-27) cho **cả 2 máy cài app CŨ**; `TC-DATAID001-02` (NEW-38) yêu cầu tiền đề *"không còn bản ghi hiện hành của UserA trên máy này"* = **máy sạch**; `TC-DATAID001-01` (NEW-24) cho **cả 2 máy đã cài bản fix**. ⇒ Tổ hợp thật của khách hàng — **máy đang chạy 2.8.6 với bản ghi mang build ID, rồi cập nhật app** — nằm ngoài mọi TC. Rủi ro: bản ghi build ID cũ **không khớp lại**, trở thành bản ghi mồ côi vẫn `status_subscribe_topic = 1` (vẫn subscribe topic) → máy nhận **trùng** hoặc bản ghi mới bị dedupe xoá nhầm. `DATA-MIG-001` + `COMPAT-LEGACY-001` đều là quan điểm **Cao**. — **Fix**: thêm `TC-COMPATLEGACY001-02/03/04` (§5).

- `[BLOCKER] GAP-3` — **`CONC-001` (Cao): 0/4 kịch bản.** Fix backend `[A]` là **read-modify-write**: tra bản ghi theo `user_id` + `orderByDesc('id')` → update bản mới nhất → **xoá các bản cũ hơn**. Hai request đồng thời trên cùng `(user_id, device_id, os)` → cả hai cùng đọc, cùng chọn bản "mới nhất", cùng xoá bản còn lại ⇒ **mất sạch bản ghi token** = mất thông báo (đúng triệu chứng ticket). Kịch bản có thật, không phải giả định: app gọi `update-firebase_token` khi khởi động **và** khi FCM xoay token; user login gần đồng thời trên 2 máy; job `firebase:subscribe_topic` đọc `status_subscribe_topic IN (0,2)` **song song** với API đang ghi cùng cột đó. Không TC nào chạm tới. — **Fix**: thêm `TC-CONC001-01/02/03` (§5); evidence bắt buộc theo `CONC-001` = **số lần xử lý thực tế đếm từ log/DB**, không chấp nhận "đã thử, thấy bình thường".

- `[BLOCKER] GAP-4` `[AP-6]` — **`REG-SHARED-001` (Cao): mục 3 `03-dev-impact.md` TRỐNG — không có danh sách nơi ảnh hưởng từ Dev.** Fix chạm **2 tầng dùng chung**: native `MainActivity.kt` (mọi luồng app đọc `device_id`) và `MobileController` (mọi API app mobile). 5 TC regression (`TC-REGSHARED001-01..04`, `TC-SYNCAPP001-01`) do **member tự chọn màn** (Booking Salon / Lesson / Chat 1:1 / Khách hàng / Chuyển bot) — **không có căn cứ Dev**, nên không thể kết luận là đủ. Bảng fix-shape quy định rõ: *"sửa hàm dùng chung + không có danh sách nơi ảnh hưởng → BLOCKER"*. Câu hỏi bắt buộc hỏi Dev: **ngoài FCM token và badge, còn chỗ nào trong app đọc `device_id` không** (session / thiết bị tin cậy / analytics / log / popup bảo trì)? và `MobileController` có **dùng chung cho brand khác** (Lwaka / Saruwaka / Lgram) không? — **Fix**: Dev điền mục 3 + 4.1/4.2/4.3; sau khi có danh sách, đối chiếu lại 5 TC regression và bổ sung cho mục còn thiếu.

- `[BLOCKER] GAP-5` — **Trigger space thông báo 1/13, và `TC-TOOLKNOW002-03` dùng trigger TRÁI SPEC.** FA-006 có **13 hạng mục thông báo**; toàn bộ TC nhận push chỉ ghi *"sinh sự kiện notify"* / *"sinh sự kiện có thông báo cho BotA"* — **không chỉ định hạng mục nào**, nên 2 người chạy sẽ ra 2 kết quả khác nhau. Nghiêm trọng hơn: `TC-TOOLKNOW002-03` (NEW-25) nêu ví dụ trigger *"khách LINE **gửi tin nhắn 1:1**"* trong khi màn đã set 「リアルタイム」 (`notification_schedule = 0`), nhưng spec ghi rõ ở Luồng 8: **`Chat 1:1 (TYPE_CHAT_11): KHÔNG gửi push App/PC ngay (tránh spam), xử lý ở nơi khác`** và điều kiện realtime là `type != CHAT_11`. ⇒ Chạy đúng theo step này thì **máy sẽ không nhận push — và đó là hành vi ĐÚNG**, nhưng tester sẽ ghi "Không đạt" và raise bug giả; hoặc tệ hơn, đổi sang trigger khác rồi ghi "Đạt" mà không sửa step. — **Fix**: sửa `TC-TOOLKNOW002-03` bỏ ví dụ chat 1:1, chỉ định **đích danh hạng mục**; thêm `TC-FUNC001-03` (≥3 hạng mục khác nhau) + `TC-FUNC001-04` (chat 1:1 realtime → **KHÔNG** push ngay, đúng spec) (§5).

- `[BLOCKER] GAP-6` — **Nhánh `notification_schedule ≠ 0` (gửi theo lịch): 0 TC — trong khi đây là nhánh khớp triệu chứng "chập chờn" nhất.** `notification_schedule` là **enum 0–7**; chỉ giá trị `0` = gửi ngay qua Firebase, còn `1–7` đi qua `HandlePushMessageNotifyService` **poll mỗi 60 giây** với điều kiện `app_notification_settings=0 AND schedule!=0 AND next_notify_time<=now()`. Toàn bộ TC chỉ set `schedule = 0` (`TC-OUTTRUTH001-01`, và TC này cũng đang `skip`). Nếu bot của khách đang để chế độ theo lịch thì **fix `device_id` có thể không giải thích được triệu chứng của họ**, và ticket sẽ bị mở lại. — **Fix**: thêm `TC-FUNC004-01/02` (§5) + xác nhận với KH/PM giá trị `notification_schedule` thực tế của bot đã báo lỗi.

### 4.2 Major (nên fix)

- `[MAJOR] AP-2 SYMPTOM-ONLY` — **KH chỉ báo triệu chứng, 0 TC cho alternative root cause.** File 01 không có error code / log / screenshot. Máy tái hiện là **Galaxy A55 chạy Android 16** — từ **Android 13** quyền `POST_NOTIFICATIONS` là **runtime permission**, user từ chối là mất notify hoàn toàn; Doze / tối ưu pin khớp chính xác mô tả *"mở lại app thì hết"*. Bộ TC **giả định 100%** root cause là `device_id`. — **Fix**: hỏi Dev/KH có alternative root cause không; thêm `TC-SYNCAPP001-06/07` (§5).

- `[MAJOR] RULE-08 / `ENV-003`` — **0/42 TC chạy PRODUCTION** (42/42 `last_exec.env = staging`). Task chạm **job nền** (`firebase:subscribe_topic`, `HandlePushMessageNotifyService`) và **push FCM thật tới app bản store** → đúng nhóm RULE-08 cấm kết luận từ staging. Catalog D: production **tách 3 job độc lập** + **2 server loadbalance**, khác hẳn staging chạy 1 job all; Firebase project staging ≠ production. Thêm nữa, deliverable của #39487 là **APK phát hành qua store** — staging không validate được bản build đó. ⚠️ Ghi chú cho Leader: report của **#39559 đã flag cùng vấn đề ở mức `[BLOCKER] GAP-4`**; ở đây flag `[MAJOR]` theo đúng quy tắc `/review-tc` (task chỉ ghi env Staging → MAJOR) — **Leader cân nhắc nâng lên BLOCKER cho thống nhất giữa 2 folder**. — **Fix**: thêm `TC-ENV003-01/02` (§5).

- `[MAJOR] AP-5 / ORPHAN` — **20/42 TC (48%) verify fix của ticket #39559, không phải #39487.** Tiền đề cả 20 TC ghi *"Backend chạy nhánh chứa fix #39559"*. Folder [2026-08-13_39559_...](../2026-08-13_39559_app-smartphone-khong-nhan-thong-bao/) đã review cùng phạm vi và **REJECTED** với 9 BLOCKER. Rủi ro **review + chạy trùng 2 lần**, và rủi ro ngược lại: BLOCKER của #39559 (VD `recover:user_firebase_token` còn lỗi, `getFirebaseToken` bỏ điều kiện `os`) **không được nhắc lại ở đây** nên có thể rơi giữa 2 ticket. — **Fix**: Leader chốt ranh giới #39487 (app) ⇄ #39559 (backend) bằng văn bản; khai báo **relation trên Redmine** (hiện `relations = []`); nếu #39559 đã chạy 20 TC này thì tái sử dụng kết quả.

- `[MAJOR] AP-4` — **Fix backend không mô tả được, không PR link trong Redmine.** Journal chỉ ghi đúng một dòng *"Backend (làm trước, tạm thời)"*. 5 commit backend (`1cba79c51d`, `ca27fe1abe`, `caa3c277b7`, `49f1bc49a6`, `3233088f19`) **chỉ có ở Phụ lục A (Studio)**, không commit nào trên Redmine. Từ *"tạm thời"* suy ra backend sẽ **còn sửa tiếp** — nghĩa là 20 TC `api` có thể lạc hậu ngay sau khi chạy. — **Fix**: yêu cầu Dev bổ sung PR link + trả lời **"tạm thời" nghĩa là gì, khi nào có bản chính thức**, có ticket theo dõi chưa.

- `[MAJOR] AP-3` — **Regression chỉ happy path.** 5 TC regression (`TC-REGSHARED001-01..04`, `TC-SYNCAPP001-01`) đều precondition dạng *"đăng nhập thành công, BotA có dữ liệu"* — không có edge state (bot hết hợp đồng, tài khoản thiếu quyền, danh sách rỗng, mạng chậm). — **Fix**: thêm ≥1 biến thể edge state cho mỗi màn sau khi Dev cung cấp danh sách ở GAP-4.

- `[MAJOR] RULE-01` — **5 quan điểm ưu tiên Cao chỉ có 1 loại case, không ghi lý do**: `OUT-TRUTH-001` (1 TC Normal) · `COMPAT-LEGACY-001` (1 Normal) · `DATA-MIG-001` (1 Normal) · `FUNC-SEQ-001` (1 Normal) · `SEC-001` (1 Abnormal — thiếu cả Normal lẫn Boundary). — **Fix**: bổ sung đủ Normal + Abnormal + Boundary, hoặc ghi lý do ở `Ghi chú` (VD "quan điểm không có khái niệm biên").

- `[MAJOR] RULE-02` — **Evidence trống 100% (42/42), trong đó 14 TC đang `Đạt`.** `testcase_list` không trả field evidence, và **không TC nào ghi loại evidence bắt buộc** ở `Ghi chú`. Theo RULE-02, `Đạt` không kèm đúng loại bằng chứng thì **không được nghiệm thu**. Với task này evidence bắt buộc là: ảnh `adb shell settings get secure android_id` từng máy · ảnh thanh thông báo trên **máy thật** · log job `firebase:subscribe_topic` · kết quả query `user_firebase_token` trước/sau. — **Fix**: lấy evidence từ Studio UI hoặc chạy lại; bổ sung loại evidence vào `Ghi chú` từng TC.

- `[MAJOR]` **Trạng thái đánh giá spec trống 42/42.** `spec_status = null` ở **toàn bộ** TC → không TC nào khai báo hành vi mình verify là `Spec ghi rõ` / `Spec không ghi` / `Đã hỏi leader`. Đây chính là chỗ đẻ ra `GAP-5`: `TC-TOOLKNOW002-03` mâu thuẫn spec mà không ai phát hiện vì không ai đối chiếu spec. — **Fix**: điền `spec_status` cho 42 TC; case `Spec không ghi` phải ghi rõ **đã hỏi ai**.

- `[MAJOR]` **`JOB-001` (Cao) thiếu chiều** — job `firebase:subscribe_topic` gọi **`iid.googleapis.com` (API bên thứ 3) theo lô**, nhưng 4 TC hiện có (`TC-JOB002-01/02`, `TC-STATECLEAN001-02`, `TC-TOOLOLDREC001-05`) chỉ verify "đăng ký đúng chủ" + 1 case lỗi. Thiếu toàn bộ: **rate limit hiện hành tra từ tài liệu chính thức (RULE-05)** · **retry + backoff** · **số bản ghi vào = số thành công + số vào hàng đợi lỗi**. Rủi ro riêng của ticket này: sau khi app đổi sang `ANDROID_ID`, **số bản ghi token tăng** (mỗi máy 1 bản ghi thay vì nhiều máy dùng chung 1) → **tải job tăng theo**. Thêm nữa `TC-JOB002-02` chỉ test **1 trigger lỗi** (token rác) trong khi Firebase trả nhiều loại (`NotRegistered`, `InvalidRegistration`, `MismatchSenderId`, quota exceeded, 5xx). — **Fix**: thêm `TC-JOB001-01` (§5); phần rate limit/retry tham chiếu §5 của report #39559 để **không viết trùng**.

- `[MAJOR]` **`DATA-COUNT-001` (Cao) — badge chưa đối chiếu đủ 4 nguồn.** `get-token-firebase` trả `badgeNotSeen`; `TC-FUNC001-02` có phép tính tay (2 + 3 = 5 ✓) nhưng **`skip`**; `TC-SECISO001-01` cũng `skip`; chỉ `TC-SYNCAPP001-03` Đạt (đối chiếu app ⇄ màn web). Chưa TC nào đối chiếu đủ **4 nguồn** (màn tóm tắt / chi tiết / CSV / API). `DATA-COUNT-001` là **lỗi lặp nhiều nhất lịch sử bug — 12 ticket Closed**. — **Fix**: chạy `TC-FUNC001-02` + `TC-SECISO001-01`; phần đối chiếu 4 nguồn tham chiếu `TC-DATACOUNT001-01/02/03` đã đề xuất ở report #39559.

- `[MAJOR]` **8 mã quan điểm KHÔNG tồn tại trong `framework/checklist-lme.md`** — phủ **18/42 TC (42,9%)**: `TOOL-OLDREC-001` (5) · `TOOL-KNOW-002` (3) · `TOOL-NEGCTRL-001` (3) · `TOOL-ERRHYG-001` (2) · `JOB-002` (2) · `TOOL-AXIS-001` (1) · `API-CONTRACT-001` (1) · `AUTH-SESSION-001` (1). Hệ quả trực tiếp: **không map được coverage theo tầng 1**, và `JOB-002` "che" mất `JOB-001` khiến GAP rate-limit/retry ở trên không lộ ra. — **Fix**: Leader quyết quy đổi (gợi ý: `TOOL-OLDREC-001` → `DATA-ID-001`/`DATA-DB-001`; `TOOL-ERRHYG-001` → `FUNC-002`; `TOOL-NEGCTRL-001` → `SEC-001`/`DATA-DB-001`; `API-CONTRACT-001` → `FUNC-003`; `AUTH-SESSION-001` → `PERM-002`; `JOB-002` → `JOB-001`), rồi đồng bộ mã trên Studio.

- `[MAJOR]` **Auto-fill `01-bug-task.md` chưa được tester verify** — file có `Auto-filled: 2026-08-21 by /new-task` nhưng checkbox *"Tester verify auto-fill chính xác"* **chưa tick**. Steps trong Redmine chỉ 2 dòng, không nêu model máy / app version / hạng mục notify / bot. — **Fix**: tester đọc lại Redmine #39487 và tick checkbox trước khi review này có giá trị.

- `[MAJOR]` **Auto-fill `03-dev-impact.md` chưa được tester verify** — tương tự, checkbox chưa tick, **và** file đang thiếu mục 3 + 4.1/4.2/4.3 nên F/D/T trong §3 là suy luận. — **Fix**: tester verify + yêu cầu Dev điền đủ 4 mục.

### 4.3 Minor (có thể fix sau)

- `[MINOR]` **`env_tag` mâu thuẫn nơi chạy thật** — 32/42 TC gắn `env_tag = local-only` nhưng `last_exec.env = staging` cho cả 42. Một trong hai sai; nếu nhãn đúng thì kết quả 8 TC `local-only` đang `Đạt` cần xem lại tính hợp lệ. — **Fix**: `haodtb` xác nhận và sửa nhãn trên Studio.

- `[MINOR]` **Tiêu đề + steps quá chung ở nhóm nhận push** — *"Sinh sự kiện notify"*, *"Sinh một sự kiện có thông báo cho BotA"* (`TC-TOOLKNOW002-03`, `TC-FUNCSEQ001-01`, `TC-TOOLNEGCTRL001-03`, `TC-TOOLOLDREC001-05`). Không nêu hạng mục cụ thể → không tái lập được. — **Fix**: chỉ định đích danh hạng mục + cấu hình 通知設定 tương ứng.

- `[MINOR]` **`priority` và `spec_status` = `null` toàn bộ 42 TC**; `origin`, `kho_id`, `client_ref` cũng null. — **Fix**: điền khi cập nhật Studio.

- `[MINOR]` **`TC-TOOLOLDREC001-03` (NEW-15) có expected không xác định** — *"nếu vẫn còn sót bản trùng của UserA thì **ghi nhận chính xác bản nào còn lại**"*. Expected phải **đo lường được**, không được để tester tự quyết. — **Fix**: hỏi Dev hành vi đúng (xoá hết bản cũ hay giữ lại?) rồi viết expected cố định.

- `[MINOR]` **`TC-DATAMIG001-01` (NEW-31) là bước khảo sát, không phải test case** — chỉ đếm số liệu và rà thư mục migration, không có pass/fail rõ ràng. — **Fix**: chuyển thành bước chuẩn bị dữ liệu (precondition dùng chung), hoặc bổ sung ngưỡng để kết luận Đạt/Không đạt.

### 4.4 Nit (gợi ý)

- `[NIT]` **Nhân rộng `TC-TOOLKNOW002-02` (NEW-37)** — TC đối chứng chạy trên **nhánh trước fix** để chứng minh bộ TC thật sự bắt được bug. Đây là practice tốt, nên đưa thành mẫu bắt buộc cho mọi task `fix-bug`. Tiếc là TC này đang `skip` — nên ưu tiên chạy sớm.

- `[NIT]` **Một người làm hết vòng đời** — `haodtb` vừa là author ticket Redmine, vừa `addedBy` task Studio, vừa `runBy` toàn bộ 42 lần chạy. Không có cross-check. Gợi ý: người chạy TC khác người viết TC cho nhóm `job` / `ui`.

- `[NIT]` **2 kênh còn lại của FA-006 — đánh × có lý do (RULE-03)**: ChatWork (`HandlePushNotifyChatwork`) và PC Web Push (`HandlePushNotifyPc`) **không** đọc `user_firebase_token` và **không bị fix chạm code** → không đề xuất TC (theo nguyên tắc không test layer downstream không bị chạm). Ghi × + lý do này vào bảng quan điểm để lần sau không bị hỏi lại.

- `[NIT]` **`REG-URL-001` / `DEPLOY-ASSET-001` đánh ×** — task không chạm URL đo lường conversion, không chạm JS/CSS/font web.

---

## 5. TCs đề xuất bổ sung

> Member copy thẳng vào `04-tc-list.md` ở round tiếp theo — **16 cột canonical**, `TC No.` đã tránh trùng toàn bộ TC hiện có ở file 04.
> **Không viết trùng** report #39559: phần `JOB-001` rate-limit/retry và `DATA-COUNT-001` 4 nguồn đã đề xuất ở folder đó — ở đây chỉ thêm phần **riêng của #39487**.

| TC No. | Mã quan điểm liên kết | Loại case | Tiêu đề test case | Điều kiện tiền đề | Các bước thực hiện | Dữ liệu test/input | Kết quả mong đợi | Kết quả thực thi | Evidence thực tế | Môi trường test | Người thực hiện | Ngày thực hiện | Số ticket bug | Trạng thái đánh giá spec | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-COMPATLEGACY001-02 | COMPAT-LEGACY-001 | Normal | Nâng cấp app 2.8.6 → bản fix trên máy ĐÃ có bản ghi token theo build ID — bản ghi cũ khớp lại theo ANDROID_ID, không sinh bản ghi mồ côi | - Máy Android M1 (Galaxy A55, Android 16) đang cài app **2.8.6 (367)**<br>- UserA đã login M1, chọn BotA, đã bật 「スマートフォンアプリ」 + 「リアルタイム」 ở 通知設定<br>- DB có sẵn 1 bản ghi `user_firebase_token` của UserA với `device_id` = build ID `BP4A.251205.006`, `status_subscribe_topic = 1`<br>- Đã ghi lại `adb shell settings get secure android_id` của M1 | 1. Query `SELECT id, user_id, device_id, os, firebase_token, status_subscribe_topic FROM user_firebase_token WHERE user_id = <UserA>` — chụp kết quả làm mốc<br>2. Cập nhật app M1 lên bản fix (commit `6daa49c`) **không gỡ app, không logout**<br>3. Mở app, chờ app gửi lại token<br>4. Query lại đúng câu lệnh bước 1<br>5. Chờ job `firebase:subscribe_topic` xử lý rồi query lại `status_subscribe_topic`<br>6. Sinh 1 sự kiện thông báo hạng mục 「新しい友だち」 của BotA, quan sát thanh thông báo M1 | `device_id` cũ = `BP4A.251205.006`; `ANDROID_ID` M1 = `<giá trị đọc bằng adb>` | Sau bước 4: bản ghi của UserA có `device_id` = đúng `ANDROID_ID` của M1 (16 ký tự hex), **KHÔNG** còn giá trị `BP4A.251205.006`. **Tổng số bản ghi của UserA không tăng** — bản ghi build ID cũ được cập nhật hoặc bị dọn, không tồn tại song song 2 bản ghi. Sau bước 5: `status_subscribe_topic = 1`. Bước 6: M1 nhận thông báo 「【BotA】新しい通知があります。アプリからご確認ください。」 | Chưa test | | PRODUCTION | | | | Spec không ghi | Lấp `[BLOCKER] GAP-2` · cover **BUG-b**, `D1` · **Evidence bắt buộc**: ảnh `adb ... android_id` + ảnh query DB trước/sau + ảnh thanh thông báo máy thật · Verify claim của Dev *"bản ghi cũ tự khớp lại"* — nếu KHÔNG khớp thì đây là bug mới, raise ticket · RULE-08: chạy PRODUCTION vì dùng bản app store |
| TC-COMPATLEGACY001-03 | COMPAT-LEGACY-001 | Abnormal | Hai máy cùng model đang DÙNG CHUNG 1 bản ghi build ID — sau khi cả hai nâng cấp, tách thành 2 bản ghi và cả hai đều nhận thông báo | - 2 máy M1, M2 **cùng model + cùng firmware**, đều đang cài app 2.8.6<br>- UserA login M1 (BotA), UserB login M2 (BotB)<br>- DB đang ở trạng thái lỗi cũ: chỉ **1** bản ghi mang `device_id = BP4A.251205.006`<br>- Đã ghi lại `ANDROID_ID` của cả M1 và M2 (phải khác nhau) | 1. Query toàn bộ bản ghi có `device_id = BP4A.251205.006`, chụp làm mốc<br>2. Nâng cấp app M1 lên bản fix, mở app, chờ gửi token<br>3. Query lại — ghi nhận trạng thái **trung gian** (M1 đã nâng cấp, M2 chưa)<br>4. Sinh sự kiện thông báo cho BotA và cho BotB, quan sát **cả 2 máy**<br>5. Nâng cấp app M2 lên bản fix, mở app, chờ gửi token<br>6. Query lại toàn bộ bản ghi của UserA và UserB<br>7. Sinh lại sự kiện thông báo cho BotA rồi BotB, quan sát cả 2 máy | `ANDROID_ID` M1 ≠ `ANDROID_ID` M2; build ID chung = `BP4A.251205.006` | Bước 3: tồn tại 2 bản ghi tách biệt — UserA mang `ANDROID_ID` của M1, UserB vẫn mang build ID. Bước 4: **cả M1 và M2 đều nhận đúng thông báo của bot mình**, không máy nào mất (đây là giai đoạn rủi ro nhất — ghi rõ nếu có máy không nhận). Bước 6: 2 bản ghi, `device_id` = `ANDROID_ID` riêng của từng máy, `user_id` không bị hoán đổi. Bước 7: M1 nhận đúng thông báo BotA và **không** nhận của BotB; M2 ngược lại | Chưa test | | PRODUCTION | | | | Spec không ghi | Lấp `[BLOCKER] GAP-2` · cover **BUG-b**, `T5` · **Evidence**: query DB ở cả 3 mốc + ảnh thông báo 2 máy · Đây là kịch bản thật khi khách cập nhật app **lệch thời điểm** — bắt buộc hỏi Dev xác nhận hành vi mong đợi ở bước 3-4 |
| TC-COMPATLEGACY001-04 | COMPAT-LEGACY-001 | Boundary | Máy đã nâng cấp chạy song song máy CHƯA nâng cấp cùng model, kéo dài qua nhiều lần gửi — không máy nào bị mất thông báo | - Như TC trên nhưng **dừng ở trạng thái lệch**: M1 đã cài bản fix, M2 vẫn giữ app 2.8.6<br>- UserA (M1, BotA) và UserB (M2, BotB) đều đã nhận được ít nhất 1 thông báo | 1. Sinh sự kiện thông báo BotA — quan sát M1, M2<br>2. Sinh sự kiện thông báo BotB — quan sát M1, M2<br>3. Lặp lại bước 1-2 thêm **2 vòng nữa**, mỗi vòng cách nhau ≥ 5 phút<br>4. Trên M2 thoát app rồi mở lại, sinh tiếp 1 sự kiện BotB<br>5. Query `user_firebase_token` của cả UserA và UserB sau toàn bộ 3 vòng | 3 vòng gửi liên tiếp; 1 lần restart app trên máy chưa nâng cấp | Cả **3/3 vòng**: M1 nhận đủ thông báo BotA, M2 nhận đủ thông báo BotB, **không vòng nào bị mất** (đây chính là triệu chứng "lúc được lúc không" trong tiêu đề ticket). Bước 4: M2 vẫn nhận bình thường sau restart. Bước 5: 2 bản ghi độc lập, `firebase_token` của từng máy không bị máy kia ghi đè, `status_subscribe_topic = 1` ở cả hai | Chưa test | | PRODUCTION | | | | Spec không ghi | Lấp `[BLOCKER] GAP-2` + RULE-01 (đủ 3 loại case cho `COMPAT-LEGACY-001` ưu tiên Cao) · regression · **Evidence**: bảng 3 vòng × 2 máy ghi rõ nhận/không nhận |
| TC-CONC001-01 | CONC-001 | Normal | Hai request update-firebase_token đồng thời cùng (user, device, os) — chỉ còn 1 bản ghi hợp lệ, không mất token | - Backend chạy nhánh chứa fix (5 commit backend)<br>- UserA có `user_token = TOK_A_39487`, đã có 1 bản ghi `device_id = CONC.39487`, `os = android`, `firebase_token = FCM_A_OLD`<br>- Có công cụ bắn request song song (curl `&` / JMeter / script) | 1. Bắn **đồng thời** 2 request `POST /api/mobile/update-firebase_token` cùng `user_token = TOK_A_39487`, `device_id = CONC.39487`, `os = android`, với `firebase_token` lần lượt `FCM_A_N1` và `FCM_A_N2` (lệch nhau < 50ms)<br>2. Query toàn bộ bản ghi có `device_id = CONC.39487`<br>3. Đếm số bản ghi và ghi lại `firebase_token`, `status_subscribe_topic`<br>4. Đọc log ứng dụng, đếm **số lần** truy vấn/xoá được thực thi<br>5. Lặp lại toàn bộ 5 lần để loại trừ may rủi | 2 request lệch < 50ms, lặp 5 lần | **Sau cả 5 lần**: luôn còn **đúng 1** bản ghi của UserA trên `device_id` đó, `firebase_token` bằng 1 trong 2 giá trị vừa gửi (không rỗng, không null), `status_subscribe_topic = 0`. **Tuyệt đối không** xảy ra trạng thái 0 bản ghi. Log cho thấy hệ thống khoá hoặc báo xung đột, không âm thầm xử lý trùng | Chưa test | | STAGING | | | | Spec không ghi | Lấp `[BLOCKER] GAP-3` · cover `F1`, `D1` · **Evidence bắt buộc (CONC-001)**: **số lần xử lý thực tế đếm từ log/DB** cho cả 5 lượt — không chấp nhận "đã thử, thấy bình thường" · Hỏi Dev: `updateFirebaseToken` có transaction/lock không? |
| TC-CONC001-02 | CONC-001 | Abnormal | API update-firebase_token chạy đồng thời với job firebase:subscribe_topic trên cùng bản ghi — trạng thái không bị job ghi đè ngược | - Backend + job `firebase:subscribe_topic` cùng bật trên staging<br>- UserA có 1 bản ghi `device_id = CONCJOB.39487`, `status_subscribe_topic = 0` (đang chờ job đăng ký)<br>- Biết chu kỳ chạy của job để canh thời điểm | 1. Canh đúng lúc job bắt đầu xử lý bản ghi của UserA (theo dõi log job)<br>2. **Ngay trong lúc job đang chạy**, gửi `POST /api/mobile/update-firebase_token` với `firebase_token = FCM_A_RACE`<br>3. Chờ job kết thúc chu kỳ<br>4. Query `firebase_token` + `status_subscribe_topic` của bản ghi<br>5. Đọc log job xem topic được đăng ký cho token nào<br>6. Sinh 1 sự kiện thông báo BotA, quan sát máy đang giữ `FCM_A_RACE` | Token mới gửi lúc job đang xử lý token cũ | `firebase_token` cuối cùng = `FCM_A_RACE` (token mới, **không bị job ghi đè về token cũ**). `status_subscribe_topic` phản ánh đúng token mới: hoặc `= 0` (chờ job đăng ký lại) hoặc `= 1` **kèm log đăng ký topic cho `FCM_A_RACE`** — **không được** `= 1` mà log chỉ đăng ký token cũ (trạng thái này = máy không nhận thông báo dù DB trông "đã đăng ký"). Bước 6: máy nhận được thông báo | Chưa test | | STAGING | | | | Spec không ghi | Lấp `[BLOCKER] GAP-3` · cover `F1`, `F5`, `D1` · Đây là cơ chế "chập chờn" đáng ngờ nhất còn lại sau fix `device_id` · **Evidence**: log job + query DB + ảnh thông báo |
| TC-CONC001-03 | CONC-001 | Boundary | Hai tài khoản login đồng thời trên hai máy cùng build ID (app cũ) — dedupe không xoá nhầm bản ghi của tài khoản kia | - Backend chạy nhánh chứa fix<br>- UserA và UserB đều **chưa** có bản ghi trên `device_id = CONCDUP.39487`<br>- Công cụ bắn request song song | 1. Bắn **đồng thời** 2 request `update-firebase_token`: một của `TOK_A_39487` (`firebase_token = FCM_A`), một của `TOK_B_39487` (`firebase_token = FCM_B`), **cùng** `device_id = CONCDUP.39487`, `os = android`<br>2. Query toàn bộ bản ghi có `device_id = CONCDUP.39487`<br>3. Lặp lại 5 lần (dọn dữ liệu giữa các lần)<br>4. Ở lần cuối, chờ job xử lý rồi sinh sự kiện thông báo cho bot của UserA và bot của UserB | 2 tài khoản, 1 `device_id` chung, request lệch < 50ms, lặp 5 lần | **Cả 5/5 lần**: tồn tại **đúng 2** bản ghi — `user_id` = UserA giữ `FCM_A`, `user_id` = UserB giữ `FCM_B`. Không lần nào chỉ còn 1 bản ghi, không lần nào bản ghi bị đổi chủ. Bước 4: cả 2 máy nhận đúng thông báo của bot mình | Chưa test | | STAGING | | | | Spec không ghi | Lấp `[BLOCKER] GAP-3` + RULE-01 (đủ 3 loại case cho `CONC-001` ưu tiên Cao) · cover `F1`, `D1` · Đây là phiên bản **đồng thời** của `TC-TOOLKNOW002-01` (hiện chỉ test tuần tự) |
| TC-FUNC001-03 | FUNC-001 | Normal | Nhận push thật với ba hạng mục thông báo khác nhau ở chế độ リアルタイム — không hạng mục nào bị rơi | - 2 máy Android cài bản fix, UserA/M1 chọn BotA, UserB/M2 chọn BotB<br>- 通知設定 của BotA bật 「スマートフォンアプリ」 + 「リアルタイム」, và bật **cả 3 hạng mục**: 「新しい友だち」, 「フォーム回答」, 「サロン予約」<br>- BotA có sẵn 1 form đang publish và 1 salon còn slot trống | 1. Dùng tài khoản LINE test **kết bạn mới** với BotA → quan sát M1<br>2. Gửi 1 câu trả lời form của BotA → quan sát M1<br>3. Đặt 1 booking Salon của BotA → quan sát M1<br>4. Sau mỗi bước, query `SELECT type, status FROM mobile_notify WHERE bot_id = <BotA> ORDER BY id DESC LIMIT 5`<br>5. Quan sát M2 trong suốt 3 bước | 3 hạng mục: `新しい友だち` / `フォーム回答` / `サロン予約`; `notification_schedule = 0` | **Cả 3/3 hạng mục** đều đẩy push tới M1 với nội dung 「【BotA】新しい通知があります。アプリからご確認ください。」; mỗi lần `mobile_notify` sinh **đúng 1** bản ghi với `type` tương ứng và `status` chuyển `0 → 1`. **M2 không nhận bất kỳ thông báo nào của BotA** trong cả 3 bước | Chưa test | | PRODUCTION | | | | Spec ghi rõ | Lấp `[BLOCKER] GAP-5` · cover `T1` · Spec: [feature-spec.md](../../spec-features/admin/notify-setting/feature-spec.md) Luồng 8 · **Evidence**: ảnh thanh thông báo M1 cho **từng** hạng mục + query `mobile_notify` · Nếu chỉ chạy được STAGING thì ghi rõ và vẫn phải chạy lại PRODUCTION (RULE-08) |
| TC-FUNC001-04 | FUNC-001 | Abnormal | Chat 1:1 ở chế độ リアルタイム — KHÔNG được đẩy push App ngay theo spec, chỉ ghi vào hàng đợi | - Như TC trên, thêm: 通知設定 của BotA bật hạng mục 「1:1チャット」<br>- `notification_schedule = 0` (リアルタイム)<br>- M1 đang ở background | 1. Query `mobile_notify` của BotA, ghi lại `id` lớn nhất làm mốc<br>2. Dùng tài khoản LINE test gửi 1 tin nhắn 1:1 tới BotA<br>3. Quan sát thanh thông báo M1 trong **3 phút**<br>4. Query `SELECT id, type, status FROM mobile_notify WHERE bot_id = <BotA> AND id > <mốc>`<br>5. Đối chiếu với hành vi mô tả ở Luồng 8 của spec | Tin nhắn 1:1 từ LINE user; `notification_schedule = 0` | Bản ghi `mobile_notify` **được tạo** với `type = TYPE_CHAT_11`, nhưng **KHÔNG có push App đẩy ngay** tới M1 trong 3 phút — đúng spec *"Chat 1:1 (TYPE_CHAT_11): KHÔNG gửi push App/PC ngay (tránh spam), xử lý ở nơi khác"* và điều kiện realtime `type != CHAT_11`. **Không nhận được thông báo ở TC này là ĐẠT, không phải bug** | Chưa test | | STAGING | | | | Spec ghi rõ | Lấp `[BLOCKER] GAP-5` · **TC này tồn tại để chặn bug giả**: `TC-TOOLKNOW002-03` (NEW-25) đang nêu chat 1:1 làm ví dụ trigger → tester dễ ghi "Không đạt" nhầm · Spec: Luồng 8 · **Evidence**: query `mobile_notify` + video 3 phút màn hình M1 |
| TC-FUNC004-01 | FUNC-004 | Normal | Chế độ gửi theo lịch (notification_schedule ≠ 0) — thông báo đến qua job poll đúng mốc next_notify_time | - Máy M1 cài bản fix, UserA/BotA<br>- 通知設定 BotA: bật 「スマートフォンアプリ」, đổi 通知タイミング sang giá trị **≠ リアルタイム** (VD 「1時間ごと」)<br>- Job `HandlePushMessageNotifyService` đang chạy trên môi trường test | 1. Query `SELECT app_notification_settings, notification_schedule, last_notify_time, next_notify_time FROM notify_setting WHERE bot_id = <BotA>` — ghi lại `next_notify_time`<br>2. Sinh 1 sự kiện hạng mục 「新しい友だち」 của BotA<br>3. Quan sát M1 **ngay lập tức** (kỳ vọng chưa có push)<br>4. Chờ tới mốc `next_notify_time`, quan sát M1<br>5. Query lại `mobile_notify.status` và `notify_setting.last/next_notify_time` | `notification_schedule` = giá trị ≠ 0 (VD `2`) | Bước 3: **chưa** có push (đúng — chế độ theo lịch). Bước 4: push đến **trong vòng 60 giây sau `next_notify_time`** (job poll mỗi 60s). Bước 5: `mobile_notify.status` chuyển `0 → 1`; `last_notify_time` cập nhật, `next_notify_time` dời đúng theo enum đã chọn | Chưa test | | PRODUCTION | | | | Spec ghi rõ | Lấp `[BLOCKER] GAP-6` · cover `D3`, `T1` · Spec: [feature-spec.md](../../spec-features/admin/notify-setting/feature-spec.md) Luồng 7 · **Nhánh này khớp triệu chứng "chập chờn" nhất** — bắt buộc xác nhận với KH/PM giá trị `notification_schedule` thật của bot đã báo lỗi · RULE-08: job nền → PRODUCTION |
| TC-FUNC004-02 | FUNC-004 | Boundary | Biên enum notification_schedule — 0 / giá trị lớn nhất / giá trị ngoài enum gửi thẳng API | - Như TC trên<br>- Có thể gọi trực tiếp endpoint lưu 通知設定 (không qua UI) | 1. Qua UI đặt `notification_schedule = 0`, F5, query DB xác nhận<br>2. Qua UI đặt **giá trị lớn nhất** trong dropdown (VD 「24時間ごと」), F5, query DB<br>3. Gọi **thẳng API** lưu 通知設定 với `notification_schedule = 8` (ngoài enum 0–7)<br>4. Gọi thẳng API với `notification_schedule = -1`<br>5. Query DB sau bước 3 và 4<br>6. Sinh 1 sự kiện thông báo và quan sát hành vi job | `0` / giá trị max dropdown / `8` / `-1` | Bước 1-2: DB lưu đúng giá trị chọn, F5 giữ nguyên. Bước 3-4: API **từ chối** giá trị ngoài enum (báo lỗi validation) **hoặc** ghi log và giữ nguyên giá trị cũ — **tuyệt đối không** lưu `8` / `-1` vào `notify_setting.notification_schedule`. Bước 6: job không rơi vào vòng lặp lỗi, không dừng | Chưa test | | STAGING | | | | Spec ghi rõ | Lấp `[BLOCKER] GAP-6` + `[MAJOR] RULE-01` (Boundary cho `FUNC-004` ưu tiên Cao) · cover `D3` · Spec: enum `notification_schedule` 0–7 · **FUNC-004 yêu cầu test server-side**, không chỉ UI — frontend chặn nhưng gọi thẳng API vẫn lưu được là lỗi kinh điển |
| TC-SYNCAPP001-06 | SYNC-APP-001 | Abnormal | Android 13+ từ chối quyền POST_NOTIFICATIONS — app phải phản ánh rõ, phân biệt được với lỗi device_id | - Máy M1 (Android 13 trở lên, lý tưởng là Galaxy A55 / Android 16) cài **mới** bản app đã fix<br>- UserA chưa từng cấp quyền thông báo cho app trên máy này<br>- BotA đã bật 「スマートフォンアプリ」 + 「リアルタイム」 | 1. Cài app, mở lần đầu, khi hệ thống hỏi quyền thông báo → **chọn Từ chối**<br>2. Login UserA, chọn BotA<br>3. Query `user_firebase_token` của UserA — xác nhận bản ghi có được tạo không và `firebase_token` có giá trị không<br>4. Sinh 1 sự kiện thông báo BotA, quan sát M1<br>5. Vào Cài đặt hệ thống → bật lại quyền thông báo cho app<br>6. Mở lại app, chờ đồng bộ, sinh lại sự kiện thông báo BotA, quan sát M1 | Quyền `POST_NOTIFICATIONS` = Từ chối → Cho phép | Bước 3-4: ghi nhận **chính xác** hành vi — bản ghi token có được tạo không, `firebase_token` rỗng hay có giá trị, máy có nhận thông báo không. Bước 6: sau khi bật lại quyền, M1 **nhận được thông báo bình thường** và `user_firebase_token` của UserA có `firebase_token` hợp lệ, `status_subscribe_topic = 1`. Nếu app **không** có bất kỳ chỉ dẫn nào cho user khi quyền bị tắt → ghi nhận là **điểm cần cải thiện** và báo Leader | Chưa test | | PRODUCTION | | | | Spec không ghi | Lấp `[MAJOR] AP-2` — alternative root cause #1 · cover `T1` · Máy tái hiện của Dev là **Android 16** nên đây là nhánh rất đáng ngờ · **Evidence**: ảnh màn hình quyền + query DB + video quan sát · **Hỏi Dev**: app có kiểm tra và nhắc user bật quyền thông báo không? |
| TC-SYNCAPP001-07 | SYNC-APP-001 | Abnormal | Máy bật tối ưu pin / Doze — xác định thông báo trễ hay mất, phân biệt với lỗi device_id | - Máy M1 cài bản fix, UserA/BotA, đang nhận thông báo bình thường<br>- Biết cách bật "Tối ưu hoá pin" (最適化) cho app trong Cài đặt hệ thống | 1. Xác nhận M1 đang nhận thông báo bình thường (sinh 1 sự kiện, quan sát)<br>2. Bật "Tối ưu hoá pin"/hạn chế nền cho app<br>3. Đưa app xuống background, **không chạm máy 30 phút** (để vào Doze)<br>4. Sinh 1 sự kiện thông báo BotA, quan sát M1 — ghi lại **thời điểm** thông báo đến (nếu có)<br>5. Mở lại app, quan sát có thông báo dồn về không<br>6. Đặt app vào danh sách **không tối ưu pin**, lặp lại bước 3-4 | Doze mode ~30 phút; so sánh có/không tối ưu pin | Ghi nhận và **so sánh 2 nhánh**: có tối ưu pin (bước 4) vs không tối ưu pin (bước 6) — độ trễ thông báo bao nhiêu, có mất hẳn không. Bước 5: nếu thông báo chỉ hiện khi mở lại app thì **đây chính là triệu chứng khách mô tả** (*"mở lại app thì hết"*) và nguyên nhân là Doze **chứ không phải `device_id`** → phải báo Leader/Dev ngay. Bản ghi `user_firebase_token` **không đổi** trong suốt TC | Chưa test | | PRODUCTION | | | | Spec không ghi | Lấp `[MAJOR] AP-2` — alternative root cause #2 · cover `T1` · Khớp trực tiếp mô tả *"thỉnh thoảng mất thông báo, mở lại app thì hết"* trong journal Dev · **Evidence**: bảng thời điểm gửi vs thời điểm nhận cho cả 2 nhánh · Kết quả TC này quyết định ticket có đóng được hay không |
| TC-ENV003-01 | ENV-003 | Normal | PRODUCTION — hai máy thật cùng model với app bản store, hai tài khoản đều nhận thông báo qua ba vòng | - **Môi trường PRODUCTION** (`step.lme.jp`), Firebase project production<br>- 2 máy Android cùng model + cùng firmware, cài app **tải từ store** (bản đã chứa fix `6daa49c`)<br>- 2 tài khoản thật UserA/BotA (M1), UserB/BotB (M2), đã bật 「スマートフォンアプリ」 + 「リアルタイム」<br>- Có quyền đọc DB production (chỉ đọc) | 1. Query `user_firebase_token` của UserA và UserB trên production — xác nhận `device_id` khác nhau và đúng `ANDROID_ID` từng máy<br>2. Sinh sự kiện thông báo hạng mục 「新しい友だち」 cho BotA → quan sát M1 và M2<br>3. Sinh sự kiện tương ứng cho BotB → quan sát cả 2 máy<br>4. Lặp lại bước 2-3 thêm 2 vòng, cách nhau ≥ 5 phút<br>5. Ghi lại router id/server đã phục vụ từng request (production có 2 server loadbalance)<br>6. Đối chiếu log job push app trên production | 2 máy thật + app store + tài khoản thật, 3 vòng gửi | Bước 1: 2 bản ghi độc lập, `device_id` = `ANDROID_ID` riêng từng máy. Bước 2-4: **3/3 vòng** M1 nhận đúng thông báo BotA và không nhận của BotB; M2 ngược lại; **không vòng nào mất**. Bước 5: kết quả **giống nhau trên cả 2 router** của loadbalance. Bước 6: log job production khớp số thông báo đã gửi | Chưa test | | PRODUCTION | | | | Spec ghi rõ | Lấp `[MAJOR] RULE-08 / ENV-003` · cover `BUG`, `T1`, `T5` · Catalog D: production **tách 3 job độc lập** + **2 server loadbalance** + Firebase project khác staging · ⚠️ **Tránh tạo/xoá data thật** — chỉ dùng bot test trên production · **Evidence**: ảnh thông báo 2 máy × 3 vòng + query DB production + log job |
| TC-ENV003-02 | ENV-003 | Abnormal | PRODUCTION — đăng xuất một máy rồi đăng nhập lại, xác nhận job huỷ/đăng ký topic chạy đúng trên kiến trúc job production | - Như TC trên, cả 2 máy đang nhận thông báo bình thường trên production | 1. Trên M1 bấm Đăng xuất trong app<br>2. Query `status_subscribe_topic` của bản ghi UserA và UserB trên production<br>3. Chờ job huỷ đăng ký topic xử lý (theo dõi log job production), query lại<br>4. Sinh sự kiện thông báo BotA rồi BotB → quan sát cả 2 máy<br>5. Đăng nhập lại UserA trên M1, chờ app gửi token<br>6. Query lại bản ghi UserA (`device_id`, `firebase_token`, `status_subscribe_topic`)<br>7. Sinh lại sự kiện thông báo BotA → quan sát M1 | Logout → chờ job → login lại, trên production | Bước 2-3: bản ghi UserA chuyển `1 → 2 → 3`; bản ghi UserB **giữ nguyên `= 1`**, không có lệnh huỷ đăng ký nào cho token của UserB trong log. Bước 4: M1 **không** nhận thông báo BotA; M2 **vẫn** nhận đúng thông báo BotB. Bước 6: bản ghi UserA có `device_id` = đúng `ANDROID_ID` của M1 (không đổi so với trước logout), `firebase_token` mới, `status_subscribe_topic` về `0` rồi lên `1`. Bước 7: M1 nhận lại thông báo bình thường | Chưa test | | PRODUCTION | | | | Spec ghi rõ | Lấp `[MAJOR] RULE-08 / ENV-003` · cover `F4`, `F5`, `T3` · Phiên bản production của `TC-TOOLNEGCTRL001-03` (hiện chỉ Đạt ở staging) · **Evidence**: log job production + query DB 3 mốc + ảnh 2 máy |
| TC-JOB001-01 | JOB-001 | Boundary | Job subscribe_topic xử lý lô lớn sau khi số bản ghi token tăng — vào = ra, có retry, job không dừng | - Môi trường có job `firebase:subscribe_topic` bật và kết nối được `iid.googleapis.com`<br>- **Đã tra tài liệu chính thức Firebase mới nhất** để lấy rate limit hiện hành của Instance ID / topic subscription (RULE-05) — ghi số vào `Dữ liệu test`<br>- Seed một lô bản ghi `user_firebase_token` ở `status_subscribe_topic = 0` với số lượng **≥ ngưỡng rate limit**, trong đó cố ý xen **5 bản ghi có `firebase_token` không hợp lệ** | 1. Ghi lại **tổng số bản ghi vào** ở `status_subscribe_topic = 0`<br>2. Kích hoạt job, theo dõi log liên tục tới khi job xử lý hết<br>3. Đếm trong log: số lần gọi Firebase thành công, số lần bị rate limit, **số lần retry và khoảng cách giữa các lần retry (backoff)**<br>4. Query phân bố cuối: số bản ghi `= 1` (đăng ký OK), `= -1` (lỗi), còn `= 0` (chưa xử lý)<br>5. Đối chiếu: **số vào = số `1` + số `-1` + số còn `0`**<br>6. Xác nhận job **không dừng giữa chừng** và không có bản ghi nào biến mất | Số bản ghi = `<ngưỡng rate limit Firebase>` + 10%, trong đó 5 token rác. **Ghi rõ nguồn ngưỡng**: tài liệu Firebase phiên bản `<ngày tra>` | **Không bản ghi nào biến mất**: số vào = số `1` + số `-1` + số còn `0`. 5 bản ghi token rác chuyển `= -1` kèm log `job subscribe_topic error token id: <id>` + cảnh báo Chatwork. Khi chạm rate limit, job **retry có backoff** (khoảng cách giữa các lần retry tăng dần, thấy rõ trong log) và **không dừng**. Log **không in `firebase_token` đầy đủ** | Chưa test | | PRODUCTION | | | | Spec không ghi | Lấp `[MAJOR] JOB-001` · cover `F5` · **Rủi ro riêng của #39487**: sau fix, mỗi máy có 1 bản ghi riêng thay vì nhiều máy dùng chung → **số bản ghi token tăng** → tải job tăng theo · RULE-05: bắt buộc tra tài liệu Firebase mới nhất, không dựa trí nhớ · Phần rate-limit/retry tổng quát xem thêm §5 report **#39559** để không viết trùng · **Evidence**: log job (thấy retry + backoff) + bảng đối chiếu số bản ghi vào/ra |

---

## 6. Spec update needed

- [ ] Không cần update spec
- [x] **Cần update spec** — chi tiết:

**(1) Spec hành vi khi đổi nguồn `device_id`** — *chưa có ở bất kỳ đâu*
  - **Section**: [spec-features/admin/notify-setting/feature-spec.md](../../spec-features/admin/notify-setting/feature-spec.md) — thêm mục về vòng đời `user_firebase_token`.
  - **Nội dung cần update**: Dev khẳng định *"bản ghi cũ trên server tự khớp lại"* khi app đổi từ build ID sang `ANDROID_ID`. Đây là **hành vi nghiệp vụ quan trọng nhưng không có trong spec** → tester không có chuẩn để kết luận Đạt/Không đạt (đúng lý do `spec_status` trống 42/42). Cần ghi rõ: bản ghi cũ mang build ID được xử lý thế nào (khớp lại / để mồ côi / dọn); có/không có script dọn dữ liệu; hành vi trong giai đoạn **lệch phiên bản app** giữa các máy.
  - **Người chịu trách nhiệm**: Dev `Thinh Nguyen` + PM.

**(2) Bổ sung `03-dev-impact.md` mục 3 + 4.1 / 4.2 / 4.3** — *bắt buộc trước round 2*
  - Không phải "spec" nhưng là **input chặn**: thiếu 3 mục này thì coverage matrix ở §3 mãi là suy luận, và `[BLOCKER] GAP-4` không đóng được.
  - **Người chịu trách nhiệm**: Dev `Thinh Nguyen`.

**(3) Khai báo relation Redmine #39487 ⇄ #39559**
  - Hiện `relations = []`. Hai ticket cùng feature `notify-setting`, cùng bảng `user_firebase_token`, TC chồng lấn 20/42. Không khai báo → nguy cơ review trùng hoặc bỏ lọt giữa 2 ticket.
  - **Người chịu trách nhiệm**: Leader / `haodtb`.

---

## 7. Checklist đã chạy

- [x] **A. Coverage** — ⚠️ chạy được **một phần**: A.1 (root cause) **FAIL** → GAP-1; A.2/A.3/A.4 **không đối chiếu được** vì mục 4.1/4.2/4.3 trống, phải suy luận; A.5 phát hiện 20 TC lệch ticket; A.6 fix-shape **FAIL** (trigger space 1/13, 1/8, 0/4).
- [x] **B. Chất lượng từng TC** — **PASS phần lớn**. B.1 tốt (precondition có seed data cụ thể, expected tới từng field DB/HTTP body); B.2/B.3 tốt; B.4 tốt. Trừ: `TC-TOOLOLDREC001-03` expected không xác định, nhóm nhận push steps quá chung.
- [x] **C. Chất lượng bộ TC tổng thể** — **RISK**. Tỷ lệ `Normal 26 / Abnormal 10 / Boundary 6` = 62/24/14, **lệch so với gợi ý 40/35/25** — thiếu Abnormal và Boundary cho một task bản chất là xử lý trùng + phân tách dữ liệu. Không có TC trùng lặp. Phân bố quan điểm dồn vào `TOOL-*` (mã không tồn tại).
- [x] **D. Spec alignment** — **FAIL**. `TC-TOOLKNOW002-03` mâu thuẫn Luồng 8 của spec (chat 1:1 không push realtime); `spec_status` trống 42/42; spec chưa mô tả hành vi migration `device_id` (§6).
- [x] **E. Hành chính** — **PASS**. TC No. đã chuẩn hoá về `TC-<mã quan điểm>-<nn>` khi fetch; file đúng folder review. Trừ: toàn bộ 42 TC còn `status = draft`, `reviewed = false`.
- [x] **F. Base quan điểm test LME**
  - [x] F.1 Quan điểm (tầng 1) — bảng dưới
  - [x] F.2 Catalog (tầng 2) — **A** × (không có màn nhập liệu mới) · **B** ◯ một phần (màn 通知設定 — chỉ 1 TC) · **C** ◯ (bản đồ đường thông báo — **13 hạng mục, mới cover ~1**) · **D/D2** ◯ **FAIL** (0 TC production dù chạm job nền — RULE-08) · **E** × (task không chạm media/file)
  - [x] F.3 RULE quy trình — **RULE-01 FAIL** (5 quan điểm Cao thiếu loại case) · **RULE-02 FAIL** (evidence trống 100%) · RULE-03 ⚠️ (không có bảng duyệt quan điểm để kiểm ×) · RULE-05 ⚠️ (chưa tra tài liệu Firebase cho rate limit) · **RULE-06 PASS** (nhóm `job`/`ui` verify tới thông báo trên máy thật) · **RULE-07 PASS** (có kiểm `WHERE` scope trên 2 tài khoản — điểm mạnh của bộ TC) · **RULE-08 FAIL** (0 TC production) · **RULE-09 FAIL** (chỉ 1 TC nhánh cũ, đang `skip`) · **RULE-12 FAIL** (không có danh sách vùng ảnh hưởng từ Dev)

### F.1 — Bảng quan điểm đối chiếu

| Mã quan điểm | Ưu tiên | Trigger khớp task? | TC cover (suy luận) | Kết luận |
|---|---|---|---|---|
| `FUNC-001` | Cao | ◯ luôn bắt buộc | `TC-FUNC001-01/02` (cả 2 `skip`) | **RISK** — 0 kết quả + trigger space 1/13 → `[BLOCKER] GAP-5` |
| `FUNC-004` | Cao | ◯ `notification_schedule` enum 0–7; giới hạn số bản ghi token / máy | — | **GAP** → `[BLOCKER] GAP-6` + `[MAJOR] RULE-01` |
| `FUNC-SEQ-001` | Trung bình | ◯ nhận thông báo nhiều lần liên tiếp | `TC-FUNCSEQ001-01` (Đạt) | **RISK** — 1 TC Normal, không có Abnormal |
| `CONC-001` | **Cao** | ◯ **BẮT BUỘC** — backend read-modify-write + dedupe; job chạy song song API | — | **GAP** → `[BLOCKER] GAP-3` |
| `DATA-DB-001` | Cao | ◯ **BẮT BUỘC** — fix có UPDATE + DELETE trên `user_firebase_token` | `TC-TOOLOLDREC001-01..05`, `TC-TOOLNEGCTRL001-02`, `TC-STATECLEAN001-03` | **RISK** — có kiểm `WHERE` scope 2 tài khoản (RULE-07 ✓) nhưng 6/7 `skip` |
| `DATA-ID-001` | Cao (màn chọn đối tượng thao tác) | ◯ **BẮT BUỘC** — cả bug là định danh máy sai | `TC-DATAID001-01/02` (cả 2 `skip`) | **GAP thực tế** → `[BLOCKER] GAP-1` |
| `DATA-MIG-001` | **Cao** | ◯ **BẮT BUỘC** — đổi nguồn sinh `device_id`, tồn tại dữ liệu cũ | `TC-DATAMIG001-01` (`skip`, chỉ khảo sát) | **GAP** → `[BLOCKER] GAP-2` + `[MAJOR] RULE-01` |
| `COMPAT-LEGACY-001` | **Cao** | ◯ **BẮT BUỘC** — app 2.8.5 / 2.8.6 / bản fix chạy song song (RULE-09) | `TC-COMPATLEGACY001-01` (`skip`) | **GAP** → `[BLOCKER] GAP-2` + `[MAJOR] RULE-01` |
| `DATA-COUNT-001` | **Cao** | ◯ **BẮT BUỘC** — API trả `badgeNotSeen` | `TC-FUNC001-02` (`skip`), `TC-SECISO001-01` (`skip`), `TC-SYNCAPP001-03` (Đạt) | **RISK** — chưa đối chiếu 4 nguồn → `[MAJOR]` |
| `DATA-REF-001` | Cao | ◯ bản ghi token cũ có thể thành mồ côi sau đổi `device_id` | `TC-DATAMIG001-01` (`skip`) | **RISK** → gộp vào `[BLOCKER] GAP-2` |
| `SYNC-APP-001` | Trung bình → **Cao** (flow critical user-facing) | ◯ **BẮT BUỘC** — output nằm trên mobile app | `TC-SYNCAPP001-01..05` | **RISK** — 3/5 Đạt; thiếu nhánh quyền OS + Doze → `[MAJOR] AP-2` |
| `STATE-CLEAN-001` | Cao | ◯ logout → huỷ đăng ký topic | `TC-STATECLEAN001-01/02/03` | **OK** — 2/3 Đạt, chiều tốt nhất của bộ TC |
| `SEC-ISO-001` | Cao | ◯ nhiều tài khoản trên cùng `device_id` | `TC-SECISO001-01` (`skip`), `TC-SECISO001-02` (Đạt) | **RISK** |
| `SEC-001` | Cao | ◯ badge + token là dữ liệu gắn tài khoản | `TC-SEC001-01` (`skip`) | **RISK** — 1 TC Abnormal, thiếu Normal + Boundary → `[MAJOR] RULE-01` |
| `SEC-002` | Cao | ◯ log không được in `firebase_token` | `TC-TOOLERRHYG001-01`, `TC-JOB002-02` (kỳ vọng phụ, cả 2 `skip`) | **RISK** — không có TC chuyên quét log/màn lỗi |
| `PERM-003` | Cao | ◯ chuyển bot (`app_selected_bot`) quyết định badge + topic | `TC-SYNCAPP001-01` (Đạt), `TC-SECISO001-01` (`skip`) | **RISK** |
| `PERM-002` | Cao | ◯ `logout` / `hide-popup-maintain` yêu cầu JWT | `TC-AUTHSESSION001-01` (`skip`) | **RISK** — mã Studio `AUTH-SESSION-001` không tồn tại → `[MAJOR]` |
| `REG-SHARED-001` | **Cao** | ◯ **BẮT BUỘC** — sửa native dùng chung + `MobileController` | `TC-REGSHARED001-01..05`, `TC-SYNCAPP001-01` | **RISK** — 5/6 Đạt **nhưng không có danh sách nơi ảnh hưởng từ Dev** → `[BLOCKER] GAP-4` |
| `REG-RUN-001` | Cao | ◯ bản ghi token + job đang chạy dở khi release app | — | **GAP** → gộp `[BLOCKER] GAP-2` (giai đoạn lệch phiên bản app) |
| `OUT-TRUTH-001` | Cao | ◯ màn 通知設定 auto-save, không có nút Lưu | `TC-OUTTRUTH001-01` (`skip`) | **RISK** — 1 TC Normal → `[MAJOR] RULE-01` |
| `ENV-003` | **Cao** | ◯ **BẮT BUỘC** — job nền + push FCM + app bản store | — (42/42 chạy `staging`) | **GAP** → `[MAJOR] RULE-08` (Leader cân nhắc nâng BLOCKER cho khớp #39559) |
| `JOB-001` | **Cao** | ◯ **BẮT BUỘC** — job `firebase:subscribe_topic` gọi `iid.googleapis.com` theo lô | `TC-JOB002-01/02` (mã sai), `TC-STATECLEAN001-02`, `TC-TOOLOLDREC001-05` | **RISK** — 0 TC rate limit / retry-backoff / vào=ra → `[MAJOR]` |
| `PERF-LARGE-001` | Trung bình → Cao | ◯ số bản ghi token **tăng** sau fix (mỗi máy 1 bản ghi) | — | **GAP** → gộp `[MAJOR] JOB-001` (`TC-JOB001-01` §5) |
| `UI-002` | Trung bình | ◯ nhiều model / nhiều bản Android | `TC-DATAID001-01` (`skip`) | **RISK** — chỉ 1 model (Galaxy A55) |
| `DATA-AUDIT-001` | Cao | × | — | `user_firebase_token` **không** thuộc nhóm dữ liệu nhạy cảm liệt kê (khách hàng / thanh toán / phân quyền / tag); không có màn 操作履歴 cho thao tác này |
| `MEDIA-*`, `MSG-*`, `PAY-*`, `LIFF-ENTRY-001`, `OUT-EXPORT-001`, `NOTI-MAIL-001`, `BULK-001`, `LIST-001`, `INTG-SHEET-001`, `INTG-CAL-001`, `FRIEND-001` | — | × | — | Task **không** chạm media/file, không gửi tin LINE, không tiền/gói cước, không sinh URL cho LINE user, không export, không email, không thao tác hàng loạt, không màn danh sách có filter, không đồng bộ Google, không friend info |
| `INTG-HOOK-001/002` | Cao / Trung bình | × | — | FCM là **push đi ra**, LME không nhận webhook trong luồng này (lỗi khi gọi Firebase đã cover ở `TC-JOB002-02`) |
| `DEPLOY-ASSET-001`, `REG-URL-001` | Cao / Trung bình | × | — | Không sửa JS/CSS/font web, không chạm URL đo lường conversion |
| Kênh **ChatWork** (`HandlePushNotifyChatwork`) + **PC Web Push** (`HandlePushNotifyPc`) của FA-006 | — | × | — | Hai kênh này **không đọc `user_firebase_token`** và **không bị fix chạm code** → không đề xuất TC cho layer downstream không bị chạm (`[NIT]` §4.4) |
| §4 checklist-lme (`FORM-01`, `CHAT-01`, `ADM-01/03/04`, `TPL-01`) | — | — | — | **Không dùng để flag** — RULE-11, các mục này chưa đủ bằng chứng |

---

## 8. Ký duyệt

| Người | Tên | Ngày |
|---|---|---|
| Reviewer (Leader) | | |
| Tester | (đã đọc & hiểu feedback) | |
