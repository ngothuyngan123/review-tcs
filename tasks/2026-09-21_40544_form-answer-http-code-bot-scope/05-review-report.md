# 05 — Review Report

## 0. Nguồn TC

| Mục | Giá trị |
|---|---|
| Nguồn đã dùng | **MCP LME TEST STUDIO** — task #307 (`ticket_id=40544`, round 1, branch `ai_fixbug_40544`, chưa archived) |
| Tổng số TC review | **112** |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận: 9/21 vùng ảnh hưởng đủ TC · 8 GAP · 4 RISK.**

> Chiều `dev-impact` = `03-dev-impact.md` mục 4 (Dev tự kê: F1–F10, D1 "không có", T1).
> Chiều `diff code` = Studio tab Thông tin (`dev_impact` + `spec_delta`, `diffAvailable=true`, 10 file / +714 −171 dòng) + 18 `requirements` REQ-001…REQ-018.

| # | Chiều | Vùng ảnh hưởng | TC hiện có | Status | Vấn đề |
|---|---|---|---|---|---|
| **G1** | `diff code` | `storeRenderForm` — **nhánh thành công ghi thông tin bạn bè** (spec BR-05: `friend_info_id` −1/−2/−3/−6 → `view_name`/`phone`/`email`/`province`, cập nhật `view_name` trigger `SyncElasticsearch`) | **0** | **GAP** | Hàm bị sửa nặng (500 → 400/404/409/403, thêm early-return). Không TC nào kiểm sau khi người dùng submit thành công thì friend info **vẫn được ghi**. Early-return đặt sai chỗ sẽ làm mất ghi friend info mà màn admin vẫn báo thành công. Chạm cả **FA-015 友だち情報管理**. |
| **G2** | `diff code` | `storeRenderForm` + `saveV3` — **đồng bộ Google Spreadsheet** (spec BR-08 queue `form_answer_connect_googles`; BR-14 `saveV3` tự tạo spreadsheet lần lưu đầu) | **0** | **GAP** | Cả 2 hàm đều trong diff và `saveV3` còn được thêm ràng buộc bot. Không TC nào chạm Google Sheet. Kho FA-011 có hẳn 2 nhóm cho vùng này (「Liên kết Google Sheet」14 TC + 「Sync Google Sheet & job」32 TC) ⇒ vùng có thật, không phải suy diễn. |
| **G3** | `diff code` | `deleteItem` / `deleteItemV3` / `deleteListFormanswer` — **hệ quả xoá mềm** (spec BR-07: giữ 90 ngày, reset `count_user_reply` về 0) | **0** | **GAP** | REQ-002 (9 TC) chỉ kiểm **phạm vi bot** của xoá lan, **không** kiểm hệ quả nghiệp vụ của xoá mềm. Đây là nhóm Dev tự đánh giá "rủi ro cao nhất" (xoá lan 7 bảng con). |
| **G4** | `diff code` | `store` (form v1) — **thiếu tham số bắt buộc** | **0** | **GAP** | REQ-008 liệt kê đúng **8 endpoint** được thêm `validateRequestOrFail`, **`store` KHÔNG nằm trong đó**. Bằng chứng thực tế: NEW-22 gửi payload thiếu trường → `POST /ajax/form-answer/store` trả **HTTP 500** `「保存に失敗しました」`. Trái thẳng yêu cầu ticket "validate required đầu vào". |
| **G5** | `diff code` | `uploadImageFormAnswer` (màn quản trị) — **chặn phần mở rộng tệp** | **0** | **GAP** | Kết quả NEW-90: nhánh 422 chỉ có ở `uploadImageFormAnswerRender` (màn trả lời); bản quản trị nhận tệp **không có đuôi** và trả **200**. REQ-010 yêu cầu "phần mở rộng không hợp lệ → 422" — 2 endpoint upload đang lệch chuẩn nhau. |
| **G6** | `dev-impact` | `03-dev-impact.md` mục **4.2 ghi "Không có data ảnh hưởng"** | — | **RISK** | Mâu thuẫn diff: Studio `dev_impact` nêu `deleteItem/deleteItemV3/deleteListFormanswer` xoá lan `FormAnswerDetails/Result/Page/Setting/SettingCommon/PointSetting/UserAccept`, và `saveFormAnswerPointSetting` ghi `form_answer.using_old_version`. Mục 4.2 sai ⇒ mọi TC suy từ `D*` đều trống, dễ bỏ lọt vùng dữ liệu. |
| **G7** | `diff code` | REQ-016 — **bản JS cũ trong cache + phiên mở trước release** | 3 (NEW-94 `error` · NEW-95 `skip` · NEW-96 `skip`) | **RISK** | **0 TC pass.** Đây là rủi ro Dev tự nêu và là rủi ro phát hành lớn nhất (7 file JS đổi, nhiều nhánh 200 → 4xx, Dev không được tự bump `config/sns-line.php`). Hiện chưa có kết luận nào. |
| **G8** | `diff code` | REQ-015 — **trang cũ có `bot_id` trống/lệch vẫn sửa được** | 1 (NEW-36 `error`) | **RISK** | Giả định Dev tự ghi là "CHƯA đối chiếu dữ liệu thật". TC `error` do **assert sai** (`form_answer_page.bot_id` là NOT NULL nên lưu `0`, package assert `=== null`) — quan sát thực tế trong log đúng mong đợi. Cần sửa assert + chạy lại để **chốt chính thức**; và mới chỉ dựng được trên local bằng ghi thẳng DB. |
| **G9** | `diff code` | REQ-011 — **service nuốt HTTP status** (`FormAnswerService::checkingNextPage` trả `JsonResponse` bị controller bọc lại) | 3 (1 `pass`, 2 `error`) | **RISK** | Đây là **root cause nặng nhất** Dev tự xếp hạng ("client luôn nhận 200"). 2/3 TC `error` do CSRF của harness (NEW-84, NEW-85) — nhánh lỗi của chính hàm gốc **chưa lần nào chạy tới controller**. |
| **G10** | `diff code` | **EP-19** `GET /ajax/download-answer/{id}` — **xuất CSV toàn bộ câu trả lời** | **0** | **GAP** | Endpoint **xuất PII ra file** (tên hiển thị, số điện thoại, email, địa chỉ — spec BR-05), nhận `{id}` thô từ URL. **Dev không đụng tới**, nên không rõ có `where bot_id` hay không. Nếu thiếu thì chỉ cần đổi số trong URL là tải được dữ liệu cá nhân của bot khác — nặng hơn `apiDetailFormanswerResult` mà Dev đã vá. |
| **G11** | `diff code` | **6 endpoint GHI dữ liệu nằm ngoài phạm vi Dev sửa**: EP-08 `update-settings-style` · EP-09 `save-setting-common` · EP-33 `v3/save-setting-remind` · EP-38 `PUT v3/change-action-open` · EP-40 `{id}/sort-page` · EP-41 `quickSendForm` | **0** | **GAP** | Ticket yêu cầu áp cho **"các api, ajax"** của tính năng, không giới hạn endpoint. 6 endpoint này đều nhận `{id}`/`{form}` từ client **y hệt nhóm đã bị vá** nhưng không nằm trong danh sách Dev kê ⇒ lỗ hổng IDOR ticket muốn đóng có thể **vẫn mở ở cửa khác**. `quickSendForm` còn **gửi tin thật ra LINE**. |
| **G12** | `diff code` | **8 endpoint ĐỌC + 3 trang render ngoài phạm vi**: EP-10 `{id}` · EP-11 `get-form-page` · EP-12 `get-form-of-page` · EP-13 `get-setting-common` · EP-34 `get-list-step-remind` · EP-37 `getSettingAction` · EP-20 `google-sheet-active` · EP-17 `link-google` · EP-02 `edit/{id}` · EP-03 `common-design-setting/{id}` | **0** | **GAP** | Đều nhận `{id}` từ client và trả nội dung form/trang/item/action. Rủi ro thấp hơn nhóm ghi nhưng vẫn thuộc diện "mọi query phải ràng buộc bot". EP-11 có 2 TC trông như khớp nhưng thực ra nhắm `saveV3`/`addPage`; EP-33 có 4 TC nhưng đó là `stopItemRemind` — **endpoint khác**. |

---

## 2. Thiếu so với quan điểm test

**Kết luận: 13/24 quan điểm Trigger khớp đã cover đủ · 7 GAP · 4 RISK.**

| # | Mã quan điểm | Ưu tiên | Trigger khớp vì | TC hiện có | Kết luận |
|---|---|---|---|---|---|
| **Q1** | `INTG-SHEET-001` | **Cao** | "BẮT BUỘC khi ghi dữ liệu khách hàng (câu trả lời form…) ra spreadsheet ngoài" — `storeRenderForm` + `saveV3` đều trong diff; spec BR-08/BR-14 xác nhận | **0** | **[BLOCKER] GAP** — lấp bằng G2 |
| **Q2** | `DATA-DB-001` | **Cao** | "BẮT BUỘC với mọi chức năng có UPDATE hoặc DELETE… tạo bản ghi trùng tên ở 2 tài khoản để kiểm chứng `WHERE`" — đúng bản chất ticket | 2 — **cả 2 đều `fail`** (NEW-69 → #41170, NEW-53 → #41171); `Normal=0`, `Boundary=0` | **[BLOCKER]** — chưa TC nào **pass** verify được `WHERE bot_id`, lại vi phạm RULE-01 (thiếu Normal + Boundary) |
| **Q3** | `COMPAT-LEGACY-001` | **Cao** | "BẮT BUỘC khi tính năng chạm **form**, google spread, hoặc bất kỳ đối tượng nào đã **version-up**" — spec BR-09 `using_old_version` v1/v3; diff sửa cả `create.js`/`index.js` (v1) lẫn `index_v3.js`/`form_render_v3.js`/`v3/*`; Studio `dev_impact` ghi thẳng "Cần test song song v1 và v3" | 2, nhưng mang mã **`RULE-09`** — mã không có trong `checklist-lme.md` (NEW-45, NEW-76) | **[MAJOR] RISK** — cover về nội dung nhưng chỉ 2 TC cho cả 2 phiên bản; theo mục 0.6 mã lạ không tính là cover |
| **Q4** | `ENV-003` + **RULE-08** | **Cao** | "BẮT BUỘC khi tính năng chạm media/file, URL/domain, job nền" — upload ảnh (2 endpoint), Google Spreadsheet, queue sync | **0 TC chạy production** (106/112 chạy `local`, `dev`/`staging`/`prd` đều 0 run) | **[MAJOR] GAP** |
| **Q5** | `PERM-003` | **Cao** | "BẮT BUỘC khi tổ chức vận hành nhiều LINE OA hoặc có chức năng change bot" — toàn bộ ticket là phân tách dữ liệu theo bot | 10 TC mang mã **`MAP-PERM-03`** (mã **catalog**, không phải mã quan điểm) | **RISK** — thiếu riêng ca **2 phiên đăng nhập độc lập**: phiên bot dùng chung cả trình duyệt nên đổi bot ở tab khác làm tab đang mở lệch phiên (NEW-63 đúng ca này nhưng `error`) |
| **Q6** | `RULE-01` (toàn bộ quan điểm Cao) | **Cao** | — | `Boundary` chỉ **5/112**, trong đó **4** dồn vào riêng hạn mức gói Free (NEW-57/58/59/60); `PERM-002` `SEC-001` `OUT-TRUTH-001` `DATA-DB-001` `CONC-001` `DEPLOY-*` đều **Normal=0 và Boundary=0**; `FUNC-001` 23 TC **toàn Normal** | **[MAJOR]** — chiều Boundary trên thực tế chỉ phủ đúng 1 quy tắc nghiệp vụ |
| **Q7** | `MEDIA-CLEAN-001` | Trung bình → **nâng Cao** (chức năng cho phép xoá/thay ảnh đã upload) | "Đặt và xoá ảnh nền trang v3" nằm thẳng trong phạm vi ticket (Dev yêu cầu test tay) | 1 (NEW-32, `error` — harness không click được `.remove-img`) | **RISK** — chưa có kết luận cho nhánh **xoá** ảnh nền |
| **Q8** | `FRIEND-001` | **Cao** | "BẮT BUỘC khi chức năng đọc/ghi friend info hoặc dùng nó làm điều kiện/biến" — `storeRenderForm` (trong diff) ghi `view_name`/`phone`/`email`/`province`/friend info tuỳ chỉnh khi người dùng submit, spec BR-05 xác nhận | **0** | **[BLOCKER] GAP** — lấp bằng G1; đây còn là regression chéo sang **FA-015 友だち情報管理** |
| **Q9** | `SEC-001` + `PERM-002` | **Cao** | "BẮT BUỘC khi chức năng chạm dữ liệu cá nhân khách hàng" / "khi có thao tác xóa / thanh toán / **export** / PII" — EP-19 xuất CSV toàn bộ câu trả lời | 5 TC `SEC-001` + 5 TC `PERM-002`, nhưng **không TC nào chạm đường xuất CSV** (EP-19) | **[BLOCKER] RISK** — thống kê nhìn như đã cover, thực tế đường lộ PII nguy hiểm nhất lại trống; lấp bằng G10 |
| **Q10** | `FUNC-001` · `UI-001` · `STATE-001` · `MEDIA-001` | **Cao** | **REQ-014** ("luồng chính không được hồi quy") + **REQ-012** ("giao diện xử lý nhánh lỗi mới đúng như luồng 200 cũ") — diff sửa **7 file JS** chạm cả nhánh `done` lẫn `fail` của editor v3, danh sách v3/v1, màn trả lời v1/v3, item chẩn đoán, upload ảnh | 28 TC `ui`+`Normal` nhưng **chỉ 14 `pass`**; ruột form builder trắng: thêm/sửa/xoá **item câu hỏi** = 0 TC · tab **共通デザイン設定** = 0 · **preview** = 0 · màn **回答一覧** làm đối tượng test = 0 · **xuất CSV từ GUI** = 0 | **[MAJOR] GAP** — lấp bằng 25 TC regression chọn từ kho FA-011 (§5) |
| **Q11** | `SYNC-APP-001` | Trung bình → **nâng Cao** (flow critical user-facing) | "tính năng có mặt trên cả web và app" — REQ-013 ghi rõ `apiGetFormAnswerList` **CỐ Ý giữ 200 + cờ quyền vì app Flutter đang đọc cờ này** | 10 TC mobile đều là `tc_group=api` (gọi thẳng endpoint); **0 TC mở app** | **[MAJOR] GAP** — lý do duy nhất để không đổi contract lại không ai kiểm chứng ở tầng app; lấp bằng `TC-SYNCAPP001-01` |

**Đã loại khỏi phạm vi sau khi kiểm chứng** (Trigger có thể khớp nhưng không tìm được bằng chứng đối tượng nằm trong luồng — không đẻ GAP giả):
- `JOB-001` — spec BR-08 ghi rõ job `AddResultFormAnswerToGoogleSpreadSheet` **hiện bị comment out**, "sync có thể đang dùng cơ chế khác" ⇒ không khẳng định được có job nền; gộp vào Q1 thay vì tách GAP riêng.
- `CONC-002`, `SEC-ISO-001`, `PERF-001`, `DATA-COUNT-001`, `REG-RUN-001` — không có điểm sửa nào trong `spec_delta` chạm re-sync toàn bộ / đa đối tượng đồng thời / hiệu năng / bộ đếm; không đề xuất TC.
- `PERM-001` — đã cover bởi NEW-99 (cờ quyền của `apiGetFormAnswerList`, `pass`).

---

## 3. TC trùng lặp nội dung

**Đã rà toàn bộ 112 TC theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`) — KHÔNG phát hiện TC trùng cần xoá hoặc gộp.**

24 cặp có từ vựng giống nhau ở mức cao đã được soi tay và **kết luận là KHÔNG trùng** — chúng test **endpoint khác nhau** trên cùng một helper dùng chung, đúng yêu cầu `REG-SHARED-001` ("code dùng chung phải test **từng nơi** gọi"):

| Nhóm nghi trùng | Vì sao KHÔNG trùng |
|---|---|
| NEW-58 / NEW-59 / NEW-60 (hạn mức gói Free 3 biểu mẫu) | 3 endpoint khác nhau: `store` · `copyFormanswer` · `restoreFormAnswer` — cùng đi qua `PlanLimitGuard` nhưng là 3 cửa vào riêng |
| NEW-15 / NEW-18 / NEW-27 / NEW-33 / NEW-39 (`MAP-PERM-03`) | 5 endpoint khác nhau: `saveMessageReply` · `updateDiagnosticContentSettings` · `addPage` · `updatePage` · `createFolder` |
| NEW-49 / NEW-68 / NEW-71 / NEW-21 (`TOOL-ERRHYG-001`) | 4 endpoint khác nhau cùng dùng `validateRequestOrFail` |
| NEW-26 / NEW-30 (`FUNC-UNIQ-001`) | `addPage` (thêm trang trùng tên) vs `updatePage` (đổi tên thành tên đã có) |
| NEW-28 / NEW-37 / NEW-50 | Đổi tên **trang** vs đổi tên **thư mục** vs **di chuyển** biểu mẫu vào thư mục |

⚠️ Một lưu ý **không phải trùng lặp nhưng gây ảo giác đủ**: 4/5 TC `Boundary` của cả bộ dồn vào riêng hạn mức gói Free ⇒ nhìn vào thống kê `case_type` tưởng có chiều Boundary, thực tế mọi quy tắc nghiệp vụ khác đều **không có Boundary** → đã ghi thành **Q6** ở §2.

---

## 4. Issues khác

### Chất lượng nguồn TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 1 | **[BLOCKER]** | NEW-69 (bug #41170) · NEW-53 (bug #41171) | **Yêu cầu cốt lõi của ticket CHƯA ĐẠT.** Ticket ghi 「Mọi query đều PHẢI ràng buộc theo bot đang đăng nhập」 nhưng 2 điểm còn sót đã được xác nhận bằng TC `fail`: `saveFormAnswerPointSetting` cập nhật `form_answer.using_old_version` theo **id thô**, và `moveFolderForItem` **không kiểm `folder_move_id`** thuộc bot nào (⇒ gán được biểu mẫu của mình vào thư mục bot khác). Redmine vẫn đang ở trạng thái `Fix done - Đợi test`. | Trả ticket về Dev xử lý 2 điểm này **trước khi** test tiếp; hoặc Leader chốt là chấp nhận được và ghi vào spec (§6). |
| 2 | **[BLOCKER]** | Toàn bộ bộ TC | **Chỉ 73/112 = 65% TC thực sự Đạt** (< 80% theo ngưỡng 0.6#1). 39 TC còn lại không có kết luận: 27 `error` · 3 `skip` · 6 chưa chạy · 3 `fail`. | Sửa test package + chạy lại; chưa đạt ≥ 80% pass thì chưa kết luận được "fix xong". |
| 3 | **[MAJOR]** | 27 TC `error` | Đã triage: **toàn bộ là lỗi harness/test-bug, KHÔNG phải lỗi sản phẩm** — 13 ca Playwright headless (menu ⋮ không bung, element không visible, timeout), 14 ca test-bug (CSRF thiếu token ở route `/basic/*`, fixture dựng payload thiếu trường, assert sai kiểu, tên thư mục fixture không gắn `runId` nên trùng lần chạy trước). Hệ quả: **24% bộ TC không cho kết luận nào**, trong đó có cả REQ-011 (root cause gốc), REQ-015, REQ-016. | Sửa package theo từng nguyên nhân đã ghi ở `actual`, chạy lại run mới. Không tick Đạt thủ công. |
| 4 | **[MAJOR]** | Toàn bộ bộ TC | **RULE-08 / ENV-003**: 106/112 TC chạy ở `env = local`, đúng **1 run** (`runId=1755`, `source=ai`, `by=ngannt`); `dev`/`staging`/`prd` đều **0 run**. Task chạm media (2 endpoint upload) + Google Spreadsheet + domain form công khai ⇒ không được kết luận từ local. | Chạy lại tối thiểu nhóm media + Google + form công khai trên `staging`, riêng Q1/Q4 trên `product`. |
| 5 | **[MAJOR]** | 41 TC / 11 mã | **11 mã quan điểm không có trong `framework/checklist-lme.md`**: `TOOL-ERRHYG-001`(15) · `MAP-PERM-03`(10) · `TOOL-NEGCTRL-001`(5) · `RULE-09`(2) · `TOOL-KNOW-002`(2) · `API-CONTRACT-001`(2) · `TOOL-OLDREC-001` · `AUTH-SESSION-001` · `SELECT-SCOPE-001` · `DATA-RETENTION-001` · `TOOL-SCOPE-001`. Không map được coverage; riêng `RULE-09` và `MAP-PERM-03` đang **thay thế ngầm** cho `COMPAT-LEGACY-001` và `PERM-003` (→ Q3, Q5). | Leader chốt bảng ánh xạ mã Studio → mã `checklist-lme`, hoặc bổ sung mã mới theo RULE-10. |
| 6 | **[MAJOR]** | Toàn bộ 112 TC | **`status = draft` 100%**, `reviewed = false`, `reviewState = leader` — chưa TC nào được duyệt, nhưng kết quả run đã được dùng để chuyển ticket sang "Đợi test". | Duyệt TC trước, rồi mới lấy kết quả run làm căn cứ. |
| 7 | **[MINOR]** | Toàn bộ bộ TC | Cột `Evidence thực tế` trống toàn bộ (RULE-02). Thực tế run **có** artifact (ví dụ `1755/tc-19191/tc-19191-fail.png`) nhưng không gắn vào TC. | Đổ đường dẫn artifact của run về cột Evidence khi export. |

### Chất lượng từng TC

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| 8 | **[BLOCKER]** | NEW-90 | **Phát hiện sản phẩm thật đang bị gán nhãn "test-bug"** — `actual` ghi rõ: `uploadImageFormAnswer` (màn quản trị) **không có nhánh chặn phần mở rộng rỗng**, tệp không đuôi được nhận và trả **200**; nhánh 422 chỉ tồn tại ở `uploadImageFormAnswerRender`. Nhãn `[triage] test-bug/TC-data` sẽ khiến phát hiện này bị đóng nhầm. | Tách thành ticket riêng hoặc đưa vào phạm vi #40544; đổi nhãn triage. → TC lấp ở §5 (G5). |
| 9 | **[MAJOR]** | NEW-22 | Cùng vấn đề nhãn: `actual` cho thấy `POST /ajax/form-answer/store` trả **500** khi payload thiếu trường. Được gán `[triage] test-bug` vì package dựng payload thiếu — nhưng chính ticket yêu cầu "thiếu tham số → 400, không được 500". `store` không nằm trong 8 endpoint được thêm validate (REQ-008). | Leader chốt `store` (v1) có thuộc phạm vi "validate required" không (§6). → TC lấp ở §5 (G4). |
| 10 | **[MAJOR]** | NEW-36 | TC bị tính `error` do **assert sai của package**, không phải do sản phẩm: `form_answer_page.bot_id` là `NOT NULL` nên `UPDATE ... SET bot_id = NULL` lưu thành `0`, package lại assert `=== null`; log quan sát thực tế **đúng mong đợi**. Đang che mất một kết luận tích cực về REQ-015. | Sửa assert thành `0 hoặc null`, chạy lại để chốt chính thức. |
| 11 | **[MINOR]** | NEW-97 | `error` do fixture INSERT cột `required` không tồn tại trong `form_answer_details` (`SQLSTATE[42S22]`). Là lỗi fixture, nhưng đang chặn TC kiểm "mọi nhánh lỗi giữ nguyên cấu trúc thân phản hồi" — một TC hợp đồng quan trọng với 7 file JS. | Sửa fixture theo schema thật rồi chạy lại. |
| 12 | **[MINOR]** | `FUNC-001` (23 TC) | 23/23 là `Normal` — nhóm quan điểm lớn nhất bộ TC không có một ca Abnormal/Boundary nào của riêng nó. | Gắn lại mã quan điểm cho sát, hoặc bổ sung chiều case. |

---

## 5. TCs đề xuất bổ sung (51)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa011-taobieumau-フォーム作成.md` (481 TC, 37 nhóm chức năng) |
| Vùng regression phát hiện từ kho | 「Sync Google Sheet & job」(32 TC — `TC-FORM-374/375/376/377`) · 「Liên kết Google Sheet」(14 TC — `TC-FORM-361/370`) · 「Xóa & khôi phục form」(`TC-FORM-73/74/76`, `DATA-CASCADE-001` "xóa data phía LINE user") · 「Folder form」(`TC-FORM-18` xoá folder → xoá mềm form + liên kết Google Sheet KHÔNG bị xoá) · 「Liên kết friend info & tag」(16 TC — `TC-FORM-147`) |
| Conflict expected vs kho | **Không** (kho không có TC nào khẳng định mã HTTP của các nhánh này) — nhưng có **conflict với spec BR-01**, đã đưa §6 |
| GAP dùng lại TC kho (không viết mới) | **Không** — TC kho mô tả hành vi nghiệp vụ gốc, không kiểm chiều "sau khi thêm ràng buộc bot + đổi mã HTTP", nên phải viết mới có dẫn chiếu |
| Xác nhận chống trùng | Đã đối chiếu **112** TC ở BƯỚC 0 + kho FA-011 — **không TC đề xuất nào trùng** |
| GAP dùng lại TC kho (regression GUI) | **Q10 → 25 TC lấy thẳng từ kho FA-011**, giữ nguyên nội dung, chỉ đánh lại `ID` theo mã quan điểm (kho `build.py` đánh số lại mỗi lần build). Phạm vi hẹp = đúng các màn/tab mà 7 file JS đã sửa chạm tới: `setting_form_items.js` (5) · `diagnostic_content.js` (4) · `index_v3.js`/`index.js` (6) · `form_render.js`/`form_render_v3.js` (6) · `uploadImage*` (4) |
| Đối chiếu bề mặt endpoint | Đã map **43 endpoint** của [feature-spec.md §6](../../spec-features/admin/form-answer/feature-spec.md) với 112 TC: **26 có TC · 17 trống**. 13 TC cuối bảng (`TC-REGSHARED001-01` → `TC-INTGSHEET001-04`) lấp đủ cả 17 endpoint đó |
| ⚠️ Spec §6 không đủ làm danh sách kiểm | Spec khai 43 endpoint nhưng TC đang test **~18 endpoint không có trong bảng đó** (`updatePage`, `deleteItem`/`deleteItemV3`, `stopItemRemind`, `deleteFormAnswerPointSetting`, `uploadImageFormAnswer`(+`Render`), `checkingNextPage`, `userOpenFormanswer`, `userAcceptFormAnswer`, `initDetailForm`/`V3`, `store`(v1), `copyFormanswer`, `ajaxGetFormAnswerList`(v1), `save`(v1), 4 API mobile) ⇒ bề mặt thật **~61 endpoint**. Muốn kết luận chắc chắn phải đối chiếu `routes/web.php` + `routes/api.php` trên nhánh `ai_fixbug_40544` — đó là việc của `TC-REGSHARED001-01` |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-INTGSHEET001-01 | API | INTG-SHEET-001 | Form answer — Màn trả lời biểu mẫu (enduser v3) | Normal | auto | product | Người dùng trả lời biểu mẫu thành công thì câu trả lời vẫn được đẩy sang Google Spreadsheet của biểu mẫu | Bot A đã liên kết Google account · biểu mẫu F1 (v3) đang công khai và đã có file Google Spreadsheet · mở sẵn file Spreadsheet đó ở tab riêng và ghi lại số dòng hiện tại | 1. Dùng tài khoản LINE đã kết bạn mở URL công khai của F1<br>2. Điền đủ các câu hỏi bắt buộc và bấm gửi<br>3. Quan sát màn hoàn tất phía người trả lời<br>4. Mở màn 回答一覧 của F1 trên admin, đếm số kết quả<br>5. Tải lại file Google Spreadsheet của F1, đếm lại số dòng và đọc dòng mới nhất | 1 lượt trả lời đầy đủ các câu hỏi bắt buộc | Màn người trả lời hiện hoàn tất · màn 回答一覧 tăng đúng 1 kết quả · file Google Spreadsheet **tăng đúng 1 dòng** và nội dung dòng mới khớp từng câu trả lời vừa nhập (không thiếu cột, không lệch cột) | | Lấp `G2` · `Q1` · Đánh giá spec: Spec ghi rõ (BR-08) · Evidence: ảnh chụp file Spreadsheet trước/sau + ảnh màn 回答一覧 · regression — dẫn từ `TC-FORM-376` · product vì RULE-08 (Google API ngoài) |
| TC-INTGSHEET001-02 | API | INTG-SHEET-001 | Form answer — Màn tạo/sửa biểu mẫu (v3) | Normal | auto | Tất cả | Lưu biểu mẫu mới lần đầu vẫn tự tạo được file Google Spreadsheet sau khi thêm ràng buộc bot | Bot A đã liên kết Google account · chưa có biểu mẫu nào tên 「TC40544-GS」 · mở sẵn Google Drive của tài khoản đã liên kết | 1. Tạo biểu mẫu v3 mới tên 「TC40544-GS」, thêm 1 câu hỏi<br>2. Bấm lưu (đây là lần lưu đầu tiên)<br>3. Đợi hệ thống xử lý xong<br>4. Mở lại biểu mẫu, kiểm tra biểu tượng liên kết Google Spreadsheet<br>5. Mở Google Drive tìm file mới theo tên biểu mẫu | Tên biểu mẫu 「TC40544-GS」 | Lưu thành công (mã 200) · biểu mẫu hiện biểu tượng đã liên kết Spreadsheet · Google Drive có **đúng 1 file mới** mang tên 「TC40544-GS」 · file thuộc tài khoản Google mà **bot A** đã liên kết, không phải bot khác |  | Lấp `G2` · `Q1` · Đánh giá spec: Spec ghi rõ (BR-14) · Evidence: ảnh Google Drive + ảnh màn biểu mẫu · regression — dẫn từ `TC-FORM-375` · product vì RULE-08 |
| TC-INTGSHEET001-03 | API | INTG-SHEET-001 | Form answer — Màn trả lời biểu mẫu (enduser v3) | Abnormal | auto | product | Biểu mẫu chưa liên kết Google thì người dùng vẫn trả lời bình thường và không phát sinh lỗi | Bot A có biểu mẫu F2 (v3) đang công khai và **chưa** liên kết Google account | 1. Dùng tài khoản LINE đã kết bạn mở URL công khai của F2<br>2. Điền đủ câu hỏi bắt buộc và bấm gửi<br>3. Quan sát màn phía người trả lời<br>4. Mở màn 回答一覧 của F2 trên admin | 1 lượt trả lời đầy đủ | Gửi thành công, màn người trả lời hiện hoàn tất bình thường · 回答一覧 tăng đúng 1 kết quả · **không** hiện thông báo lỗi nào liên quan Google · không có màn trắng hay lỗi hệ thống | | Lấp `G2` · `Q1` · Đánh giá spec: Spec ghi rõ (BR-08) · Evidence: ảnh màn hoàn tất + ảnh 回答一覧 · regression — dẫn từ `TC-FORM-374` · product vì RULE-08 |
| TC-FRIEND001-01 | Data | FRIEND-001 | Form answer — Màn trả lời biểu mẫu (enduser v3) | Normal | auto | product | Trả lời biểu mẫu thành công vẫn ghi đúng thông tin bạn bè đã liên kết | Bot A có biểu mẫu F3 (v3) công khai, có 3 câu hỏi liên kết friend info: 1 câu nối tên hiển thị, 1 câu nối số điện thoại, 1 câu nối friend info tuỳ chỉnh dạng text · tài khoản LINE test đã kết bạn, mở sẵn màn 友だち詳細 của tài khoản đó và ghi lại 3 giá trị hiện tại | 1. Mở màn 友だち詳細 của tài khoản LINE test, chụp lại 3 giá trị trước khi trả lời<br>2. Dùng tài khoản LINE đó mở URL công khai của F3<br>3. Nhập 3 giá trị mới khác hẳn giá trị cũ rồi gửi<br>4. Quay lại màn 友だち詳細, tải lại trang<br>5. Mở màn 友だちリスト và tìm bạn bè theo tên hiển thị mới | Tên hiển thị 「TC40544テスト」 · số điện thoại 09012345678 · friend info tuỳ chỉnh 「フォーム更新値」 | Gửi thành công · màn 友だち詳細 hiển thị **đủ cả 3 giá trị mới** · màn 友だちリスト tìm được bạn bè theo tên hiển thị mới (xác nhận chỉ mục tìm kiếm đã cập nhật) |  | Lấp `G1` · `Q8` · Đánh giá spec: Spec ghi rõ (BR-05) · Evidence: ảnh 友だち詳細 trước/sau + ảnh kết quả tìm kiếm · regression sang **FA-015** — dẫn từ `TC-FORM-147` |
| TC-FRIEND001-02 | Data | FRIEND-001 | Form answer — Màn trả lời biểu mẫu (enduser v3) | Abnormal | auto | product | Lượt trả lời bị từ chối thì KHÔNG được ghi thông tin bạn bè | Như TC-FRIEND001-01 nhưng F3 đặt giới hạn số lần trả lời = 1 và tài khoản LINE test **đã trả lời 1 lần** | 1. Ghi lại 3 giá trị friend info hiện tại ở màn 友だち詳細<br>2. Dùng tài khoản LINE đó mở lại URL công khai của F3<br>3. Nhập 3 giá trị khác hẳn rồi bấm gửi<br>4. Quan sát thông báo phía người trả lời<br>5. Quay lại 友だち詳細, tải lại trang và đối chiếu 3 giá trị | 3 giá trị khác hẳn lần trả lời trước | Yêu cầu bị từ chối vì hết lượt trả lời, phía người trả lời hiện đúng thông báo hết lượt (không phải màn trắng, không phải báo thành công) · **cả 3 giá trị friend info giữ nguyên như bước 1** · 回答一覧 không tăng thêm kết quả |  | Lấp `G1` · `Q8` · Đánh giá spec: Spec ghi rõ (BR-03 + BR-05) · Evidence: ảnh thông báo + ảnh 友だち詳細 trước/sau · bắt ca early-return mới ghi nhầm friend info |
| TC-DATADB001-01 | Data | DATA-DB-001 | Form answer — Danh sách biểu mẫu (v3) | Normal | auto | dev/local/staging | Xoá biểu mẫu của chính bot vẫn xoá đúng dữ liệu con và đặt lại số người trả lời | Bot A có biểu mẫu F4 đã có **3 lượt trả lời**, có đặt nhắc lại đang chạy, có cài đặt khoảng điểm · ghi lại số người trả lời hiển thị ở màn danh sách | 1. Ở màn danh sách, ghi lại số người trả lời của F4<br>2. Xoá F4 qua menu thao tác, xác nhận xoá<br>3. Mở màn 削除したフォーム và tìm F4<br>4. Khôi phục F4<br>5. Mở lại F4, kiểm tra cài đặt chung, trang, câu hỏi, nhắc lại, khoảng điểm<br>6. Mở màn 回答一覧 của F4 và đếm số kết quả | Biểu mẫu F4 có 3 lượt trả lời | F4 chuyển sang màn 削除したフォーム (không biến mất hẳn) · khôi phục được và **giữ nguyên toàn bộ cài đặt, trang, câu hỏi** · số người trả lời hiển thị **về 0** và 回答一覧 trống (dữ liệu trả lời đã bị xoá theo spec) · nhắc lại đã đặt của F4 bị huỷ |  | Lấp `G3` · `Q2` (bù chiều Normal còn thiếu) · Đánh giá spec: Spec ghi rõ (BR-07) · Evidence: ảnh màn danh sách trước/sau + ảnh 回答一覧 · regression — dẫn từ `TC-FORM-73`, `TC-FORM-76` |
| TC-DATADB001-02 | Data | DATA-DB-001 | Form answer — Danh sách biểu mẫu (v3) | Boundary | auto | dev/local/staging | Xoá hàng loạt danh sách lẫn id của bot khác chỉ xoá phần của mình và giữ nguyên phần bot kia | Bot A có F5, F6 (đều có lượt trả lời) · Bot B có F7 (có lượt trả lời) · ghi lại số người trả lời của cả 3 · đang đăng nhập phiên bot A | 1. Ở màn danh sách bot A, chọn F5 và F6 rồi thực hiện xoá hàng loạt, đồng thời cho id của F7 (bot B) lẫn vào danh sách gửi đi<br>2. Ghi lại mã trạng thái và thông báo hiển thị<br>3. Kiểm tra màn danh sách và 削除したフォーム của bot A<br>4. Chuyển sang phiên đăng nhập của bot B, mở danh sách và 回答一覧 của F7 | Danh sách id gửi đi gồm: F5, F6 (bot A) và F7 (bot B) | F5, F6 chuyển sang 削除したフォーム và số người trả lời về 0 · **F7 của bot B còn nguyên**: vẫn ở danh sách đang hoạt động, số người trả lời **không đổi**, 回答一覧 vẫn đủ kết quả cũ · không có bản ghi con nào của F7 bị xoá |  | Lấp `G3` · `Q2` (bù chiều Boundary còn thiếu — danh sách lẫn id 2 bot) · Đánh giá spec: Đã hỏi leader (REQ-002) · Evidence: ảnh 2 phiên đăng nhập + ảnh 回答一覧 của F7 · dùng **2 phiên đăng nhập riêng** vì phiên bot dùng chung cả trình duyệt |
| TC-FUNC001-01 | API | FUNC-001 | Form answer — Màn tạo/sửa biểu mẫu (v1) | Abnormal | auto | Tất cả | Tạo biểu mẫu bản v1 thiếu tham số bắt buộc trả mã lỗi yêu cầu chứ không phải lỗi hệ thống | Đang đăng nhập phiên bot A · có quyền tạo biểu mẫu · bot A chưa chạm hạn mức gói | 1. Mở màn tạo biểu mẫu bản v1<br>2. Gửi yêu cầu tạo biểu mẫu nhưng bỏ trống tên biểu mẫu<br>3. Ghi lại mã trạng thái và toàn văn thân phản hồi<br>4. Lặp lại, lần này bỏ trống một tham số bắt buộc khác của biểu mẫu<br>5. Mở màn danh sách biểu mẫu của bot A và đếm số biểu mẫu | Lần 1: thiếu tên biểu mẫu. Lần 2: thiếu tham số bắt buộc khác | Cả 2 lần trả **mã 400** kèm thông báo tiếng Nhật cho người dùng, **KHÔNG** trả 500 và thân phản hồi **không** chứa nội dung lỗi nội bộ (không có tên tệp, tên bảng, câu SQL) · không có biểu mẫu nào được tạo · số biểu mẫu của bot A không đổi | | Lấp `G4` · Đánh giá spec: **Spec không ghi** — REQ-008 chỉ liệt kê 8 endpoint, `store` không nằm trong đó; đã đưa câu hỏi lên §6 · Evidence: toàn văn phản hồi 2 lần gọi · bằng chứng hiện trạng: NEW-22 ghi nhận trả 500 |
| TC-MEDIA001-01 | UI | MEDIA-001 | Form answer — Màn tạo/sửa biểu mẫu (v3) | Abnormal | auto | Tất cả | Tải ảnh từ màn quản trị với tệp sai định dạng bị chặn và hiện lý do | Đang đăng nhập phiên bot A · mở màn tạo/sửa biểu mẫu v3 · chuẩn bị 3 tệp: 1 tệp ảnh hợp lệ, 1 tệp **không có phần mở rộng**, 1 tệp `.txt` đổi tên thành `.png` | 1. Tải tệp ảnh hợp lệ lên → xác nhận thành công<br>2. Tải tệp không có phần mở rộng lên<br>3. Ghi lại mã trạng thái và thông báo hiển thị trên màn<br>4. Tải tệp `.txt` đổi tên thành `.png` lên<br>5. Ghi lại mã trạng thái và thông báo<br>6. Tải lại trang, kiểm tra vùng ảnh xem có tệp lỗi nào được gắn vào không | Tệp hợp lệ `ok.png` · tệp không đuôi `noext` · tệp giả đuôi `fake.png` (nội dung text) | Bước 1 thành công. Bước 2 và 4 **bị từ chối** với mã **422** và hộp thoại nêu rõ lý do tệp không hợp lệ — **không được im lặng trả 200** · sau khi tải lại trang, không tệp lỗi nào được gắn vào biểu mẫu |  | Lấp `G5` · `Q4` · Đánh giá spec: **Đã hỏi leader** — hiện `uploadImageFormAnswer` (quản trị) không có nhánh chặn, chỉ bản màn trả lời có; xem §6 · Evidence: ảnh hộp thoại + toàn văn phản hồi · product vì RULE-08 (media) · bằng chứng hiện trạng: NEW-90 |
| TC-MEDIACLEAN001-01 | UI | MEDIA-CLEAN-001 | Form answer — Màn tạo/sửa biểu mẫu (v3) | Normal | auto | Tất cả | Xoá ảnh nền trang v3 đã đặt vẫn chạy đúng sau khi thêm ràng buộc bot | Đang đăng nhập phiên bot A · biểu mẫu F8 (v3) có 1 trang đã đặt ảnh nền · ghi lại ảnh nền hiện tại | 1. Mở màn cài đặt trang của F8, xác nhận ảnh nền đang hiển thị<br>2. Bấm xoá ảnh nền<br>3. Ghi lại mã trạng thái và thông báo<br>4. Lưu và tải lại màn cài đặt trang<br>5. Mở URL công khai của F8 bằng tài khoản LINE đã kết bạn, xem trang đó | Ảnh nền đang đặt sẵn ở trang 1 của F8 | Xoá thành công (mã 200) · sau khi tải lại, vùng ảnh nền trống, không còn ảnh cũ · phía người trả lời cũng **không còn** ảnh nền ở trang đó · yêu cầu xoá **không** bắt buộc gửi kèm tên trang (bản fix đã gỡ ràng buộc đó) |  | Lấp `Q7` · Đánh giá spec: Spec ghi rõ (REQ-014) · Evidence: ảnh màn cài đặt + ảnh phía người trả lời · product vì RULE-08 (media) · bổ sung cho NEW-32 đang `error` |
| TC-COMPATLEGACY001-01 | UI | COMPAT-LEGACY-001 | Form answer — Màn trả lời biểu mẫu (enduser v1) | Normal | auto | Tất cả | Biểu mẫu bản cũ v1 vẫn mở và trả lời trọn vẹn sau khi đổi mã HTTP | Bot A có biểu mẫu F9 với cờ dùng phiên bản cũ đang bật (v1), đang công khai, có ít nhất 2 trang · tài khoản LINE test đã kết bạn · **đã xoá cache trình duyệt / hard-reload** trước khi chạy | 1. Dùng tài khoản LINE đã kết bạn mở URL công khai của F9<br>2. Điền trang 1, bấm sang trang tiếp<br>3. Điền trang 2 rồi bấm gửi<br>4. Quan sát màn hoàn tất<br>5. Mở màn 回答一覧 của F9 trên admin và đối chiếu nội dung | 1 lượt trả lời đầy đủ qua 2 trang | Mở được biểu mẫu bản v1 · chuyển trang thành công (không dừng giữa chừng, không báo lỗi) · gửi thành công và hiện màn hoàn tất · 回答一覧 tăng đúng 1 kết quả, nội dung khớp từng câu đã nhập | | Lấp `Q3` · Đánh giá spec: Spec ghi rõ (BR-09) · Evidence: video luồng trả lời + ảnh 回答一覧 · **RULE-09** — nhánh cũ song song với v3 · Studio `dev_impact` yêu cầu "test song song v1 và v3" |
| TC-DEPLOYASSET001-01 | UI | DEPLOY-ASSET-001 | Form answer — Danh sách biểu mẫu (v3) | Abnormal | manual | product | Trình duyệt còn giữ bản JavaScript cũ thì nhánh lỗi mới vẫn hiện thông báo, không im lặng | Đã có bản cũ của trang nằm trong cache trình duyệt (mở trang trước khi phát hành) · sau đó phát hành bản mới đã nâng số hiệu phiên bản tệp giao diện | 1. Trước khi phát hành: mở màn danh sách biểu mẫu v3 để trình duyệt lưu cache bản JS cũ<br>2. Phát hành bản mới (đội phát hành đã nâng số hiệu phiên bản)<br>3. Không xoá cache, chỉ bấm tải lại trang thường (F5)<br>4. Kiểm tra đường dẫn tệp JavaScript đang được nạp có mang số hiệu phiên bản mới không<br>5. Thực hiện một thao tác chắc chắn bị từ chối (ví dụ xoá biểu mẫu của bot khác)<br>6. Quan sát màn hình | Thao tác gây lỗi: xoá biểu mẫu bằng id của bot khác | Sau F5 thường, trang nạp **tệp JavaScript mang số hiệu phiên bản mới** · thao tác bị từ chối hiện **thông báo lý do rõ ràng trên màn hình**, không im lặng và không hiện lỗi hệ thống chung chung · không phải xoá cache thủ công mới dùng được |  | Lấp `G7` · Đánh giá spec: Spec ghi rõ (REQ-016) · Evidence: ảnh tab Network cho thấy tham số phiên bản + ảnh thông báo · **manual vì** phải phối hợp đúng thời điểm đội phát hành bump version trên production · product vì RULE-08 (domain/deploy) |
| TC-REGSHARED001-01 | Data | REG-SHARED-001 | Form answer — toàn bộ api/ajax của tính năng | Normal | auto | Tất cả | Rà soát toàn bộ route của Form answer, liệt kê endpoint còn thiếu ràng buộc bot hoặc thiếu kiểm tham số | Có mã nguồn nhánh `ai_fixbug_40544` trên máy chạy kiểm thử | 1. Liệt kê **mọi** route của tính năng Form answer từ tệp khai báo route (cả nhóm quản trị, nhóm người trả lời và nhóm API di động)<br>2. Với từng route, mở hàm xử lý tương ứng<br>3. Đánh dấu hàm nào nhận mã đối tượng từ client mà truy vấn **không kèm điều kiện bot**<br>4. Đánh dấu hàm nào ghi dữ liệu mà **không kiểm tham số bắt buộc**<br>5. Lập bảng kết quả: tên route · tên hàm · thiếu ràng buộc bot (có/không) · thiếu kiểm tham số (có/không)<br>6. Đối chiếu bảng này với danh sách 16 endpoint Dev đã kê trong ticket | Không có dữ liệu nhập | Có bảng liệt kê **đầy đủ** mọi route của tính năng · mỗi route có kết luận rõ ràng về 2 tiêu chí · **mọi route nằm ngoài danh sách Dev kê mà bị đánh dấu thiếu đều được liệt kê riêng** để leader quyết định đưa vào ticket này hay tách ticket mới · đặc biệt phải có kết luận cho: xuất CSV, lưu thiết kế chung, lưu cài đặt giao diện, lưu cài đặt nhắc lại, đổi action khi mở form, sắp xếp trang, gửi thử biểu mẫu | | Lấp `G11` · `G12` · Đánh giá spec: **Đã hỏi leader** — ticket ghi "các api, ajax" nhưng Dev chỉ kê 16 endpoint; xem §6 · Evidence: bảng rà soát đính kèm · **Chạy TC này TRƯỚC 12 TC bên dưới** — kết quả quyết định endpoint nào thật sự cần TC, rẻ hơn viết mò 17 TC |
| TC-SEC001-01 | API | SEC-001 | Form answer — Màn kết quả trả lời | Abnormal | auto | product | Tải CSV kết quả trả lời bằng mã biểu mẫu của bot khác không lấy được dữ liệu | Bot A và bot B đều có biểu mẫu · biểu mẫu F_B của bot B đã có ít nhất 2 lượt trả lời chứa tên hiển thị, số điện thoại và email thật · đang đăng nhập phiên bot A · ghi lại mã biểu mẫu F_B | 1. Ở phiên bot A, mở màn kết quả trả lời của một biểu mẫu thuộc bot A và bấm tải CSV → xác nhận tải được<br>2. Ghi lại đường dẫn tải xuống vừa dùng<br>3. Gọi lại đường dẫn đó nhưng thay mã biểu mẫu bằng mã của F_B (bot B)<br>4. Ghi lại mã trạng thái trả về<br>5. Nếu có tệp tải về thì mở tệp và tìm tên hiển thị, số điện thoại, email của người đã trả lời F_B | Mã biểu mẫu F_B thuộc bot B | Bước 3 **không trả về dữ liệu của bot B**: hoặc trả 404 「フォームが見つかりません。」, hoặc trả tệp rỗng không chứa bản ghi nào · **tuyệt đối không có** tên hiển thị, số điện thoại, email hay bất kỳ câu trả lời nào của người dùng thuộc bot B trong tệp tải về | | Lấp `G10` · `Q9` · Đánh giá spec: **Spec không ghi** — EP-19 không nằm trong 16 endpoint Dev kê, chưa ai xác nhận có ràng buộc bot; xem §6 · Evidence: tệp CSV tải về + ảnh mã trạng thái · product vì RULE-08 (xuất tệp) · **ưu tiên chạy cao nhất trong nhóm bổ sung** |
| TC-SEC001-02 | API | SEC-001 | Form answer — Màn kết quả trả lời | Abnormal | auto | Tất cả | Tải CSV với mã biểu mẫu không tồn tại hoặc sai kiểu trả mã lỗi đúng nghĩa | Đang đăng nhập phiên bot A · có quyền xem kết quả trả lời | 1. Gọi đường dẫn tải CSV với mã biểu mẫu là một số rất lớn không tồn tại<br>2. Ghi lại mã trạng thái và nội dung trả về<br>3. Gọi lại với mã biểu mẫu là chuỗi chữ<br>4. Ghi lại mã trạng thái và nội dung trả về<br>5. Gọi lại với mã biểu mẫu của một biểu mẫu đã bị xoá mềm của chính bot A | Mã không tồn tại `999999999` · mã sai kiểu `"abc"` · mã của biểu mẫu đã xoá | Mã không tồn tại → **404**; mã sai kiểu → **400**; cả 2 đều kèm thông báo tiếng Nhật cho người dùng và **không** trả 500, **không** lộ nội dung lỗi nội bộ (không có tên tệp, tên bảng, câu SQL) · biểu mẫu đã xoá mềm → ghi lại mã trạng thái thực tế và báo leader nếu vẫn tải được dữ liệu |  | Lấp `G10` · Đánh giá spec: **Spec không ghi** · Evidence: toàn văn phản hồi 3 lần gọi · product vì RULE-08 (xuất tệp) |
| TC-PERM002-01 | UI | PERM-002 | Form answer — Màn tạo/sửa biểu mẫu (v3) | Abnormal | auto | Tất cả | Lưu thiết kế chung cho biểu mẫu thuộc bot khác bị từ chối | Bot B có biểu mẫu F_B với thiết kế chung đã cấu hình (màu nền, phông chữ) · đang đăng nhập phiên bot A · ghi lại giá trị thiết kế chung hiện tại của F_B | 1. Ở phiên bot A, mở màn thiết kế chung của một biểu mẫu thuộc bot A<br>2. Ghi lại yêu cầu lưu mà màn hình gửi đi<br>3. Gửi lại yêu cầu đó nhưng thay mã biểu mẫu bằng mã của F_B, đổi màu nền sang giá trị khác hẳn<br>4. Ghi lại mã trạng thái<br>5. Chuyển sang phiên đăng nhập bot B, mở thiết kế chung của F_B và đối chiếu | Mã biểu mẫu F_B · màu nền mới khác hẳn giá trị cũ | Yêu cầu bị từ chối với **404** 「フォームが見つかりません。」 · thiết kế chung của F_B **giữ nguyên toàn bộ** như bước 1, không bị đổi màu nền hay bất kỳ thuộc tính nào | | Lấp `G11` (EP-09) · Đánh giá spec: **Đã hỏi leader** — endpoint ngoài danh sách Dev kê · Evidence: ảnh 2 phiên đăng nhập · dùng **2 phiên đăng nhập riêng** vì phiên bot dùng chung cả trình duyệt |
| TC-PERM002-02 | UI | PERM-002 | Form answer — Màn tạo/sửa biểu mẫu (v3) | Abnormal | auto | Tất cả | Lưu cài đặt giao diện cho biểu mẫu thuộc bot khác bị từ chối | Bot B có biểu mẫu F_B đã cấu hình giao diện · đang đăng nhập phiên bot A · ghi lại cài đặt giao diện hiện tại của F_B | 1. Ở phiên bot A, mở màn cài đặt giao diện của biểu mẫu thuộc bot A<br>2. Ghi lại yêu cầu lưu mà màn hình gửi đi<br>3. Gửi lại yêu cầu đó với mã biểu mẫu của F_B, đổi một thuộc tính giao diện<br>4. Ghi lại mã trạng thái<br>5. Sang phiên bot B, mở F_B và đối chiếu cài đặt giao diện | Mã biểu mẫu F_B · 1 thuộc tính giao diện đổi khác | Bị từ chối với **404** · cài đặt giao diện của F_B giữ nguyên | | Lấp `G11` (EP-08) · Đánh giá spec: **Đã hỏi leader** · Evidence: ảnh 2 phiên đăng nhập |
| TC-PERM002-03 | UI | PERM-002 | Form answer — Cài đặt điểm & tin nhắn trả lời | Abnormal | auto | dev/local/staging | Lưu cài đặt nhắc lại cho biểu mẫu thuộc bot khác bị từ chối | Bot B có biểu mẫu F_B đã đặt nhắc lại đang chạy · đang đăng nhập phiên bot A · ghi lại nội dung và lịch nhắc hiện tại của F_B | 1. Ở phiên bot A, mở màn cài đặt nhắc lại của biểu mẫu thuộc bot A<br>2. Ghi lại yêu cầu lưu mà màn hình gửi đi<br>3. Gửi lại yêu cầu đó với mã biểu mẫu của F_B, đổi nội dung tin nhắn nhắc<br>4. Ghi lại mã trạng thái<br>5. Sang phiên bot B, mở cài đặt nhắc lại của F_B và đối chiếu nội dung + lịch | Mã biểu mẫu F_B · nội dung tin nhắn nhắc mới | Bị từ chối với **404** · nội dung và lịch nhắc của F_B giữ nguyên · **không** phát sinh tin nhắn nhắc nào gửi ra LINE cho bạn bè của bot B |  | Lấp `G11` (EP-33) · Đánh giá spec: **Đã hỏi leader** · Evidence: ảnh 2 phiên + kiểm hộp thoại LINE của bạn bè bot B · lưu ý: 4 TC `stopItemRemind` hiện có **không** cover endpoint này |
| TC-PERM002-04 | UI | PERM-002 | Form answer — Màn tạo/sửa biểu mẫu (v3) | Abnormal | auto | Tất cả | Đổi action khi mở biểu mẫu thuộc bot khác bị từ chối | Bot B có biểu mẫu F_B đã cấu hình action khi mở form · đang đăng nhập phiên bot A · ghi lại action hiện tại của F_B | 1. Ở phiên bot A, mở màn cài đặt action khi mở form của biểu mẫu thuộc bot A<br>2. Ghi lại yêu cầu cập nhật mà màn hình gửi đi<br>3. Gửi lại yêu cầu đó với mã biểu mẫu của F_B, đổi sang một action khác (ví dụ gắn thẻ khác)<br>4. Ghi lại mã trạng thái<br>5. Sang phiên bot B, mở F_B và đối chiếu action | Mã biểu mẫu F_B · action mới khác hẳn | Bị từ chối với **404** · action của F_B giữ nguyên · không có thẻ nào bị gắn thêm cho bạn bè của bot B | | Lấp `G11` (EP-38) · Đánh giá spec: **Đã hỏi leader** · Evidence: ảnh 2 phiên đăng nhập |
| TC-PERM002-05 | UI | PERM-002 | Form answer — Màn tạo/sửa biểu mẫu (v3) | Abnormal | auto | Tất cả | Sắp xếp trang của biểu mẫu thuộc bot khác bị từ chối | Bot B có biểu mẫu F_B (dạng phân nhánh) với ít nhất 3 trang theo thứ tự đã biết · đang đăng nhập phiên bot A · ghi lại thứ tự trang hiện tại của F_B | 1. Ở phiên bot A, mở biểu mẫu thuộc bot A và kéo thả đổi thứ tự trang<br>2. Ghi lại yêu cầu sắp xếp mà màn hình gửi đi<br>3. Gửi lại yêu cầu đó với mã biểu mẫu của F_B và danh sách trang của F_B theo thứ tự đảo ngược<br>4. Ghi lại mã trạng thái<br>5. Sang phiên bot B, mở F_B và đối chiếu thứ tự trang<br>6. Mở URL công khai của F_B bằng tài khoản LINE đã kết bạn, kiểm tra thứ tự trang phía người trả lời | Mã biểu mẫu F_B · danh sách trang đảo ngược | Bị từ chối với **404** · thứ tự trang của F_B **giữ nguyên** cả ở màn quản trị lẫn phía người trả lời · luồng phân nhánh của F_B không bị hỏng | | Lấp `G11` (EP-40) · Đánh giá spec: **Đã hỏi leader** · Evidence: ảnh 2 phiên + ảnh phía người trả lời |
| TC-PERM002-06 | UI | PERM-002 | Form answer — Danh sách biểu mẫu (v3) | Abnormal | auto | Tất cả | Gửi thử biểu mẫu thuộc bot khác bị từ chối và không gửi tin ra LINE | Bot B có biểu mẫu F_B và có tài khoản tester đã đăng ký · đang đăng nhập phiên bot A · mở sẵn ứng dụng LINE của tài khoản tester bên bot B | 1. Ở phiên bot A, thực hiện gửi thử một biểu mẫu thuộc bot A → xác nhận nhận được tin trên LINE<br>2. Ghi lại yêu cầu gửi thử mà màn hình gửi đi<br>3. Gửi lại yêu cầu đó với mã biểu mẫu của F_B<br>4. Ghi lại mã trạng thái<br>5. Kiểm tra hộp thoại LINE của tài khoản tester bên bot B trong 5 phút | Mã biểu mẫu F_B | Bị từ chối với **404** · tài khoản tester của bot B **KHÔNG nhận được tin nhắn nào** · không có bản ghi gửi tin nào phát sinh cho bot B |  | Lấp `G11` (EP-41) · Đánh giá spec: **Đã hỏi leader** · Evidence: ảnh hộp thoại LINE của tester bot B + ảnh mã trạng thái · product vì RULE-08 + RULE-06 (output cuối trên LINE thật) |
| TC-SEC001-03 | API | SEC-001 | Form answer — Màn tạo/sửa biểu mẫu (v3) | Abnormal | auto | Tất cả | Các endpoint đọc nội dung biểu mẫu không trả dữ liệu của bot khác | Bot B có biểu mẫu F_B gồm ít nhất 2 trang, mỗi trang có câu hỏi với nhãn dễ nhận biết, đã cấu hình thiết kế chung · đang đăng nhập phiên bot A · ghi lại nhãn câu hỏi và tên trang của F_B | 1. Ở phiên bot A, mở biểu mẫu của bot A và ghi lại 4 yêu cầu đọc mà màn hình gửi đi: lấy chi tiết biểu mẫu, lấy danh sách trang, lấy nội dung 1 trang, lấy thiết kế chung<br>2. Gọi lại lần lượt cả 4 yêu cầu nhưng thay mã biểu mẫu bằng mã của F_B<br>3. Ghi lại mã trạng thái và toàn văn thân phản hồi từng lần<br>4. Tìm trong phản hồi các nhãn câu hỏi và tên trang đã ghi ở bước chuẩn bị | Mã biểu mẫu F_B cho cả 4 yêu cầu | Cả 4 lần **không trả về nội dung của F_B**: trả 404 hoặc dữ liệu rỗng · **không** phản hồi nào chứa nhãn câu hỏi, tên trang hay thiết kế chung của bot B | | Lấp `G12` (EP-10, EP-11, EP-12, EP-13) · Đánh giá spec: **Đã hỏi leader** — 4 endpoint đọc ngoài danh sách Dev kê · Evidence: toàn văn 4 phản hồi · gộp 4 endpoint vì cùng một dạng truy vấn theo mã biểu mẫu, nhưng **phải gọi đủ cả 4** (REG-SHARED-001) |
| TC-SEC001-04 | API | SEC-001 | Form answer — Cài đặt điểm & tin nhắn trả lời | Abnormal | auto | Tất cả | Đọc cài đặt nhắc lại và cài đặt action của biểu mẫu bot khác không trả dữ liệu | Bot B có biểu mẫu F_B đã cấu hình các bước nhắc lại và action sau trả lời với nội dung dễ nhận biết · đang đăng nhập phiên bot A | 1. Ở phiên bot A, mở màn cài đặt nhắc lại và màn cài đặt action của biểu mẫu thuộc bot A<br>2. Ghi lại 2 yêu cầu đọc mà màn hình gửi đi<br>3. Gọi lại cả 2 yêu cầu với mã biểu mẫu của F_B<br>4. Ghi lại mã trạng thái và toàn văn phản hồi<br>5. Tìm trong phản hồi nội dung các bước nhắc và action đã cấu hình của F_B | Mã biểu mẫu F_B cho cả 2 yêu cầu | Cả 2 lần không trả về dữ liệu của F_B (404 hoặc rỗng) · không lộ nội dung tin nhắn nhắc, lịch nhắc hay cấu hình action của bot B | | Lấp `G12` (EP-34, EP-37) · Đánh giá spec: **Đã hỏi leader** · Evidence: toàn văn 2 phản hồi |
| TC-PERM002-07 | UI | PERM-002 | Form answer — Màn tạo/sửa biểu mẫu (v3) | Abnormal | auto | Tất cả | Mở trực tiếp trang chỉnh sửa và trang thiết kế chung của biểu mẫu bot khác bị chặn | Bot B có biểu mẫu F_B với tên và nội dung dễ nhận biết · đang đăng nhập phiên bot A · ghi lại mã biểu mẫu F_B | 1. Ở phiên bot A, dán thẳng đường dẫn trang chỉnh sửa kèm mã biểu mẫu F_B vào thanh địa chỉ và mở<br>2. Quan sát màn hình hiển thị<br>3. Lặp lại với đường dẫn trang thiết kế chung kèm mã F_B<br>4. Quan sát màn hình<br>5. Kiểm tra trên 2 màn có hiển thị tên biểu mẫu, tên trang hay câu hỏi nào của F_B không | Mã biểu mẫu F_B | Cả 2 lần **không mở được nội dung của F_B**: chuyển về danh sách biểu mẫu của bot A, hoặc hiện trang báo không tìm thấy · **không** màn nào hiển thị tên biểu mẫu, tên trang hay câu hỏi của bot B · không hiện trang lỗi hệ thống thô | | Lấp `G12` (EP-02, EP-03) · Đánh giá spec: **Đã hỏi leader** · Evidence: ảnh chụp 2 màn |
| TC-INTGSHEET001-04 | API | INTG-SHEET-001 | Form answer — Danh sách biểu mẫu (v3) | Abnormal | auto | Tất cả | Hai endpoint liên quan Google không trả trạng thái kết nối của bot khác | Bot B **đã** liên kết Google account · bot A **chưa** liên kết · đang đăng nhập phiên bot A | 1. Ở phiên bot A, gọi endpoint kiểm tra trạng thái kết nối Google với mã bot của bot A → ghi lại kết quả (kỳ vọng: chưa liên kết)<br>2. Gọi lại endpoint đó nhưng khai mã bot của bot B<br>3. Ghi lại mã trạng thái và toàn văn phản hồi<br>4. Mở trang kết nối Google ở phiên bot A, kiểm tra trạng thái hiển thị<br>5. Tìm trong phản hồi bước 3 địa chỉ email Google hoặc mã tài khoản của bot B | Mã bot của bot B | Bước 2 **không trả về trạng thái đã liên kết của bot B** và **không lộ** địa chỉ email Google hay mã tài khoản Google của bot B · trang kết nối Google ở phiên bot A vẫn hiển thị đúng trạng thái của bot A (chưa liên kết) |  | Lấp `G12` (EP-17, EP-20) · `Q1` · Đánh giá spec: **Đã hỏi leader** · Evidence: toàn văn phản hồi + ảnh trang kết nối · product vì RULE-08 (tích hợp Google) |
| TC-UI001-01 | UI | UI-001 | Màn edit form | Normal | auto | Tất cả | Modal 項目を追加: hiển thị 2 nhóm 質問項目 và 装飾, item được tick đổi màu nền | Đăng nhập admin bot A<br>Đang ở màn edit form F1 (loại basic), tab フォーム編集 | 1. Click nút ＋ 項目・装飾を追加<br>2. Quan sát modal<br>3. Tick chọn 1 item, quan sát màu | — | Modal căn giữa, nền tối · hiển thị 2 nhóm: 質問項目 (câu hỏi) và 装飾 (trang trí) · item đã tick có text #FFFFFF trên nền #5799DB |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-91` (kho FA-011) · JS đã sửa: `setting_form_items.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-FUNC001-02 | UI | FUNC-001 | Màn edit form | Normal | auto | Tất cả | Tick 1 item ở modal → item được thêm vào cuối list (trên nút bấm) và tự focus vào item đó | Đăng nhập admin bot A<br>Đang ở màn edit form F1 (loại basic), tab フォーム編集<br>F1 hiện có 3 item | 1. Mở modal 項目を追加<br>2. Tick 1 item loại 短文回答<br>3. Click この項目を追加<br>4. Quan sát list item và vùng setting bên phải | 1 item 短文回答 | Item mới nằm cuối list, ngay trên vùng setting nút bấm · vùng setting bên phải tự focus vào item vừa thêm với data trống · item chỉ được thêm tạm ở FE, phải nhấn lưu form mới ghi nhận |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-93` (kho FA-011) · JS đã sửa: `setting_form_items.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-FUNC001-03 | UI | FUNC-001 | Màn edit form | Normal | auto | Tất cả | Hover giữa 2 item hiện icon + → thêm item vào ĐÚNG vị trí giữa 2 item đó | Đăng nhập admin bot A<br>Đang ở màn edit form F1 (loại basic), tab フォーム編集<br>F1 có 3 item theo thứ tự I1, I2, I3 | 1. Rê chuột vào khoảng giữa I1 và I2<br>2. Click icon + hiện ra<br>3. Chọn 1 item mới ở modal → thêm<br>4. Quan sát thứ tự list item<br>5. Lưu form, reload | Thêm item mới giữa I1 và I2 | Item mới nằm ở vị trí thứ 2 (giữa I1 và I2), KHÔNG bị đẩy xuống cuối · sau lưu + reload thứ tự giữ nguyên |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-97` (kho FA-011) · JS đã sửa: `setting_form_items.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-FUNCSORT001-01 | UI | FUNC-SORT-001 | Màn edit form | Normal | auto | Tất cả | Move item ở form 1 page: đổi chỗ item đầu danh sách và item quanh mốc 127 đều lưu được sau reload | Đăng nhập admin bot A<br>Đang ở màn edit form F1 (loại basic), tab フォーム編集<br>F1 (basic) có 130 item | 1. Move item 1 và 2 → lưu → reload → kiểm tra vị trí<br>2. Move item 126 và 127 → lưu → reload → kiểm tra<br>3. Move item 127 và 128 → lưu → reload → kiểm tra<br>4. Move các item cuối danh sách → lưu → reload | Form 130 item, các mốc 1-2, 126-127, 127-128, cuối danh sách | Cả 4 mốc: move thành công, sau reload item hiển thị ĐÚNG vị trí mới, không văng về chỗ cũ |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-99` (kho FA-011) · JS đã sửa: `setting_form_items.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-FUNC001-04 | UI | FUNC-001 | Màn edit form | Normal | auto | Tất cả | Sửa 管理名 và フォルダ trực tiếp trên header màn edit → lưu và phản ánh ở màn list | Đăng nhập admin bot A<br>Đang ở màn edit form F1 (loại basic), tab フォーム編集<br>F1 đang ở folder A, 管理名 =「アンケート」 | 1. Sửa 管理名 thành「アンケート改」<br>2. Đổi フォルダ sang folder B<br>3. Lưu<br>4. Về màn list, mở folder B | 管理名 mới + folder B | F1 hiển thị ở folder B với tên「アンケート改」· folder A không còn F1 |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-90` (kho FA-011) · JS đã sửa: `setting_form_items.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-STATE001-01 | UI | STATE-001 | Item chẩn đoán | Normal | auto | Tất cả | Enable/disable setting chẩn đoán → màn kết quả hiện/ẩn phần điểm tương ứng | Form F1 có item chẩn đoán, đã có 3 người trả lời với điểm khác nhau | 1. Bật setting chẩn đoán (作成する) → mở màn kết quả trả lời → quan sát phần điểm<br>2. Tắt setting chẩn đoán (作成しない) → mở lại màn kết quả → quan sát | 3 người trả lời có điểm | Bật: hiển thị số điểm cộng dồn của từng user · tắt: ẨN các câu trả lời item point mà friend đã trả lời trước đó |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-183` (kho FA-011) · JS đã sửa: `diagnostic_content.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-UIFIELD001-01 | UI | UI-FIELD-001 | Setting chẩn đoán | Normal | auto | Tất cả | Bật 診断コンテンツを作成する → mở ra 2 vùng: chọn friend info ghi điểm và hướng dẫn tạo nội dung | Màn edit form F1, tab 診断コンテンツ | 1. Quan sát lựa chọn mặc định<br>2. Chọn 作成する → quan sát vùng mở ra | — | Mặc định 作成しない · chọn 作成する mở ra: ポイントを記録（合算）する友だち情報を選択 và 診断コンテンツの作成方法 |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-299` (kho FA-011) · JS đã sửa: `diagnostic_content.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-FUNC002-01 | UI | FUNC-002 | Setting chẩn đoán | Abnormal | auto | Tất cả | Bật 作成する nhưng không chọn friend info point → báo lỗi 友だち情報を選択してください。 | Màn edit form F1, tab 診断コンテンツ | 1. Chọn 作成する<br>2. Không chọn friend info → lưu | Không chọn friend info point | Báo lỗi「友だち情報を選択してください。」· không lưu được setting chẩn đoán |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-300` (kho FA-011) · JS đã sửa: `diagnostic_content.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-STATE001-02 | UI | STATE-001 | Setting chẩn đoán | Abnormal | auto | Tất cả | Nhập khoảng điểm chưa lưu rồi chuyển sang khoảng khác / chuyển sang 基本設定 → hành vi khác nhau | Màn edit form F1, tab 診断コンテンツ có nhiều khoảng điểm | 1. Nhập đầy đủ 1 khoảng điểm, KHÔNG lưu → chuyển sang khoảng điểm khác → quay lại → quan sát<br>2. Nhập đầy đủ, KHÔNG lưu → chuyển sang tab 基本設定 → quay lại → quan sát | Chuyển sang khoảng khác / sang 基本設定 | Chuyển sang khoảng khác: khoảng chưa lưu VẪN GIỮ thông tin đã nhập · chuyển sang 基本設定: khoảng chưa lưu bị RESET và đóng lại |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-307` (kho FA-011) · JS đã sửa: `diagnostic_content.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-UI001-02 | UI | UI-001 | Xóa & khôi phục form | Normal | auto | Tất cả | Xóa 1 form từ menu 3 chấm → hiện modal confirm, nhấn キャンセル hoặc X thì không xóa | Đăng nhập admin, đang chọn bot A (plan Standard)<br>Đang ở màn /basic/form-answer<br>Form F1 tồn tại | 1. Menu 3 chấm của F1 → chọn xóa<br>2. Quan sát modal confirm<br>3. Nhấn キャンセル → kiểm tra list<br>4. Lặp bước 1, nhấn X → kiểm tra list | F1 | Hiện modal confirm (cùng kiểu với màn tag) · nhấn キャンセル: đóng modal, F1 vẫn ở list · nhấn X: đóng modal, F1 vẫn ở list |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-72` (kho FA-011) · JS đã sửa: `index_v3.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-FUNC001-05 | UI | FUNC-001 | Xóa & khôi phục form | Normal | auto | Tất cả | Khôi phục form đã xóa → form trở về ĐÚNG folder cũ với đầy đủ setting và item | Đăng nhập admin, đang chọn bot A (plan Standard)<br>Đang ở màn /basic/form-answer<br>F1 thuộc folder A, đã bị xóa<br>F1 có setting đầy đủ và 10 item | 1. Vào màn 削除したフォーム<br>2. Click nút 復元する của F1<br>3. Về màn list form, mở folder A<br>4. Mở F1 kiểm tra các setting và list item | F1 ở folder A với 10 item, trước khi xóa có 3 người trả lời | F1 hiển thị trở lại trong folder A (đúng folder lúc tạo, không rơi về 未分類) · các setting hiển thị đầy đủ như trước khi xóa · đủ 10 item · số người trả lời = 0人 và màn kết quả trả lời TRỐNG (form-result đã bị xóa lúc xóa form, KHÔNG khôi phục lại) |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-76` (kho FA-011) · JS đã sửa: `index_v3.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-FUNC001-06 | UI | FUNC-001 | Màn list form | Normal | auto | Tất cả | 一括フォルダ変更: di chuyển form giữa các folder theo 3 chiều đều thành công | Đăng nhập admin, đang chọn bot A (plan Standard)<br>Đang ở màn /basic/form-answer<br>Có folder A, folder B và folder mặc định 未分類<br>未分類 có form F1, folder A có form F2, F3 | 1. Tick chọn F1 → click 一括フォルダ変更 → chọn folder A → OK<br>2. Tick F2 → chuyển về 未分類<br>3. Tick F3 → chuyển sang folder B<br>4. Reload, mở từng folder kiểm tra | F1: 未分類→A · F2: A→未分類 · F3: A→B | Cả 3 chiều di chuyển thành công · sau reload mỗi form nằm đúng folder đích · form không bị nhân bản hoặc mất |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-07` (kho FA-011) · JS đã sửa: `index_v3.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-FUNCDEL001-01 | UI | FUNC-DEL-001 | Folder form | Normal | auto | Tất cả | Xóa folder rỗng → folder biến mất khỏi sidebar, vẫn mất sau reload | Đăng nhập admin, đang chọn bot A (plan Standard)<br>Đang ở màn /basic/form-answer<br>Folder「空フォルダ」chưa có form nào | 1. Xóa folder「空フォルダ」→ xác nhận 削除する<br>2. Quan sát sidebar<br>3. Reload trang | Folder rỗng | Folder không còn hiển thị ở sidebar · sau reload vẫn không hiển thị |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-17` (kho FA-011) · JS đã sửa: `index_v3.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-FUNC001-07 | UI | FUNC-001 | Folder form | Normal | auto | Tất cả | Tạo folder form với tên hợp lệ (latinh / JP full-width / half-width) → tạo thành công | Đăng nhập admin, đang chọn bot A (plan Standard)<br>Đang ở màn /basic/form-answer<br>Chưa tồn tại folder tên「見込み客」,「VIP-Kyaku」,「ｷｬﾝﾍﾟｰﾝ」 | 1. Click nút tạo folder<br>2. Nhập tên folder<br>3. Lưu<br>4. Reload màn /basic/form-answer<br>5. Lặp cho 3 loại ký tự | Lần 1: 「見込み客」· Lần 2: 「VIP-Kyaku」· Lần 3: 「ｷｬﾝﾍﾟｰﾝ」 | Cả 3 folder hiển thị ở sidebar · sau reload vẫn còn · tên lưu verbatim, không mojibake · form_answer_folder có 3 bản ghi mới của bot A |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-10` (kho FA-011) · JS đã sửa: `index_v3.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-FUNCSORT001-02 | UI | FUNC-SORT-001 | Folder form | Normal | auto | Tất cả | Sort folder bằng drag&drop rồi Save → thứ tự mới giữ sau reload | Đăng nhập admin, đang chọn bot A (plan Standard)<br>Đang ở màn /basic/form-answer<br>Bot A có 4 folder theo thứ tự F1,F2,F3,F4 | 1. Click nút sort folder<br>2. So sánh list trong popup với sidebar<br>3. Kéo F4 lên đầu<br>4. Click Save<br>5. Reload trang | Kéo F4 từ vị trí 4 lên vị trí 1 | Bước 2: list trong popup khớp sidebar · sau Save sidebar hiển thị F4,F1,F2,F3 · sau reload thứ tự vẫn giữ |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-19` (kho FA-011) · JS đã sửa: `index_v3.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-LIFFENTRY001-01 | UI | LIFF-ENTRY-001 | LINE user — mở form | Normal | auto | Tất cả | Form ở trạng thái ON → user mở link thấy màn nhập form | LINE user U1 đã kết bạn với bot A<br>Form F1 đang public | 1. U1 mở link form trong LINE app<br>2. Quan sát màn hình | Form ON | Hiện màn nhập thông tin form đầy đủ item |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-406` (kho FA-011) · JS đã sửa: `form_render*.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-FUNC001-08 | UI | FUNC-001 | LINE user — nhập & submit | Normal | auto | Tất cả | Form BASIC không bật màn xác nhận → nhấn nút submit là lưu form-result ngay | LINE user U1 đã kết bạn với bot A<br>Form F1 đang public<br>Form F1 basic, KHÔNG bật page confirm | 1. U1 điền form → nhấn nút submit<br>2. Quan sát màn hình<br>3. Kiểm tra màn kết quả trả lời | Form basic, không bật confirm | Nhấn submit là lưu form-result ngay, chuyển sang màn sau khi trả lời (không qua màn xác nhận) |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-425` (kho FA-011) · JS đã sửa: `form_render*.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-FUNC001-09 | UI | FUNC-001 | LINE user — nhập & submit | Normal | auto | Tất cả | Màn xác nhận: nhấn nút gửi thì lưu form-result, nhấn back thì về màn nhập giữ nguyên câu trả lời | LINE user U1 đã kết bạn với bot A<br>Form F1 đang public<br>Form F1 (200 item) đã bật page confirm | 1. U1 điền đủ 200 item → nhấn submit → mở màn xác nhận<br>2. Nhấn nút back → quan sát màn nhập<br>3. Kiểm tra 200 câu trả lời còn nguyên không (đặc biệt checkbox nhiều option và ngày)<br>4. Nhấn submit lại → ở màn xác nhận nhấn nút gửi<br>5. Kiểm tra form-result | Form 200 item, back rồi gửi | Nhấn back: về đúng màn nhập, TOÀN BỘ 200 câu trả lời còn nguyên (kể cả checkbox nhiều option và ngày) · nhấn nút gửi: lưu form-result đầy đủ 200 câu |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-428` (kho FA-011) · JS đã sửa: `form_render*.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-FUNC001-10 | UI | FUNC-001 | LINE user — nhập & submit | Normal | auto | Tất cả | Form RẼ NHÁNH: nút ở page setting kết thúc thì submit, setting next thì mở page tiếp | LINE user U1 đã kết bạn với bot A<br>Form F1 đang public<br>Form F2 rẽ nhánh 3 page: P1 next → P2, P2 next → P3, P3 kết thúc form | 1. U1 mở form → quan sát page hiển thị đầu tiên<br>2. Ở P1 nhấn nút → quan sát<br>3. Ở P2 nhấn nút → quan sát<br>4. Ở P3 nhấn nút → quan sát<br>5. Kiểm tra form-result | Form 3 page | Mở form: hiện page 1 · P1 và P2 nhấn nút: mở sang page tiếp theo · P3 (kết thúc form): submit và lưu form-result · form-result chứa ĐỦ câu trả lời của cả 3 page |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-433` (kho FA-011) · JS đã sửa: `form_render*.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-FUNC002-02 | UI | FUNC-002 | LINE user — nhập & submit | Abnormal | auto | Tất cả | Item required bỏ trống → chặn submit và báo lỗi; item không required bỏ trống → submit được | LINE user U1 đã kết bạn với bot A<br>Form F1 đang public<br>Form F1 có item required và item không required | 1. U1 bỏ trống item required → submit → quan sát<br>2. Điền item required, bỏ trống item không required → submit → quan sát<br>3. Quan sát nhãn 必須 / 任意 hiển thị cạnh từng item | 1 item required, 1 item không required | Bỏ trống item required: báo lỗi, chặn submit · bỏ trống item không required: submit thành công · item required hiện nhãn 必須, item còn lại hiện 任意 |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-418` (kho FA-011) · JS đã sửa: `form_render*.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-STATE001-03 | UI | STATE-001 | LINE user — nhập & submit | Abnormal | auto | Tất cả | Mạng chậm hoặc ngắt giữa chừng khi submit → hiển thị lỗi mạng đúng, không tạo bản ghi nửa vời | LINE user U1 đã kết bạn với bot A<br>Form F1 đang public<br>Form F1 nhiều item | 1. U1 điền form<br>2. Bật chế độ mạng chậm hoặc ngắt mạng ngay lúc nhấn submit<br>3. Quan sát màn hình<br>4. Bật lại mạng, kiểm tra màn kết quả trả lời | Ngắt mạng lúc submit | Khi THẬT SỰ mất mạng: hiển thị thông báo lỗi mạng là ĐÚNG · KHÔNG tạo bản ghi câu trả lời nửa vời/thiếu · sau khi có mạng lại, user submit lại được |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-444` (kho FA-011) · JS đã sửa: `form_render*.js` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-MEDIA001-02 | UI | MEDIA-001 | Item upload file | Normal | auto | Tất cả | LINE user upload file ĐÚNG định dạng theo từng loại setting → upload thành công | Form F1 có 5 item file, mỗi item set 1 loại: 画像 / 動画 / 音声 / PDF / 指定無し<br>LINE user U1 đã kết bạn | 1. U1 mở form<br>2. Item 画像: upload .jpg, .png, .jpeg và chọn chụp ảnh trực tiếp<br>3. Item 動画: upload .mp4, .mov và chọn quay video<br>4. Item 音声: upload .m4a<br>5. Item PDF: upload .pdf<br>6. Item 指定無し: upload lần lượt ảnh, mp4, m4a, pdf, doc, txt, csv<br>7. Submit form | Các định dạng hợp lệ theo từng loại | Tất cả các định dạng hợp lệ đều upload thành công · item 指定無し nhận mọi định dạng · submit form thành công, file hiển thị được ở màn kết quả |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-163` (kho FA-011) · JS đã sửa: `uploadImage*` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-MEDIA001-03 | UI | MEDIA-001 | Item upload file | Abnormal | auto | Tất cả | LINE user upload file SAI định dạng → báo lỗi ファイルの形式が正しくありません。 | Form F1 có 4 item file (画像 / 動画 / 音声 / PDF)<br>LINE user U1 đã kết bạn | 1. Item 画像: upload video, audio, pdf, và ảnh gif/webp/avif<br>2. Item 動画: upload ảnh, audio, pdf<br>3. Item 音声: upload ảnh, video, pdf và file .mp3<br>4. Item PDF: upload ảnh, audio, video, doc, excel, csv<br>5. Mọi item: upload file KHÔNG có đuôi | Các định dạng sai theo từng loại · file không đuôi · mp3 vào item audio | Tất cả đều báo lỗi「ファイルの形式が正しくありません。」· KHÔNG upload lên được · lưu ý: item 音声 chỉ nhận .m4a, .mp3 bị TỪ CHỐI |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-164` (kho FA-011) · JS đã sửa: `uploadImage*` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-STATE001-04 | UI | STATE-001 | Item upload file | Normal | auto | Tất cả | LINE user thay/xóa file đã upload trước khi submit → file cuối cùng là file được lưu | Form F1 có item file, có bật màn xác nhận<br>LINE user U1 đã kết bạn | 1. Upload ảnh A → submit → kiểm tra kết quả<br>2. Upload ảnh A → change sang ảnh B → submit → kiểm tra<br>3. Upload ảnh A → xóa ảnh → submit → kiểm tra<br>4. Upload A → xóa → upload lại C → submit → kiểm tra<br>5. Ở màn xác nhận back về, change ảnh → mở lại màn xác nhận | Ảnh A, B, C | Bước 1: lưu ảnh A · bước 2: lưu ảnh B · bước 3: không có file trong câu trả lời · bước 4: lưu ảnh C · bước 5: màn xác nhận hiển thị ảnh mới nhất sau khi change |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-167` (kho FA-011) · JS đã sửa: `uploadImage*` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác |
| TC-MEDIA001-04 | UI | MEDIA-001 | Item upload file | Normal | auto | product | Ảnh user upload hiển thị được ở đủ 4 nơi: form result tab 1, tab 2, chat 1:1, my-page | Form F1 có item file loại 画像, U1 đã submit kèm ảnh | 1. Mở màn kết quả trả lời tab 回答一覧 → xem ảnh<br>2. Mở tab list người trả lời → click detail → xem ảnh<br>3. Mở chat 1:1 với U1 → xem ảnh<br>4. Mở my-page của U1 → xem ảnh<br>5. Kiểm tra thêm ở màn friend-information | 1 ảnh đã upload | Ảnh hiển thị được ở CẢ 4 nơi (2 tab form result, chat 1:1, my-page) và ở màn friend-information nếu có map info · không nơi nào bị vỡ ảnh |  | Lấp `Q10` · regression GUI — dẫn từ `TC-FORM-168` (kho FA-011) · JS đã sửa: `uploadImage*` · Đánh giá spec: Spec ghi rõ · Evidence: ảnh/video màn thao tác · product vì hiển thị nội dung khách hàng tải lên (RULE-08 media) |
| TC-SYNCAPP001-01 | UI | SYNC-APP-001 | Form answer — App mobile admin | Normal | manual | Tất cả | App mobile admin hiển thị đúng danh sách biểu mẫu theo cờ quyền, không vỡ sau khi đổi mã HTTP | Có thiết bị thật đã cài app mobile admin (bản Flutter đang phát hành) · có 2 tài khoản nhân viên của bot A: TK1 **được** cấp quyền xem biểu mẫu, TK2 **không** được cấp quyền · bot A có ít nhất 3 biểu mẫu · đã đối chiếu trước danh sách biểu mẫu trên web quản trị | 1. Đăng nhập app mobile admin bằng TK1, chọn bot A<br>2. Mở màn danh sách biểu mẫu, đối chiếu số lượng và tên biểu mẫu với màn web quản trị<br>3. Mở chi tiết 1 biểu mẫu và xem kết quả trả lời<br>4. Đăng xuất, đăng nhập lại bằng TK2 (không có quyền), chọn bot A<br>5. Mở màn danh sách biểu mẫu, quan sát màn hình hiển thị<br>6. Ghi lại toàn bộ thông báo / trạng thái màn hình ở bước 5 | TK1 có quyền xem biểu mẫu · TK2 không có quyền | TK1: danh sách biểu mẫu hiển thị **đủ và đúng** như màn web, mở được chi tiết và kết quả trả lời · TK2: app hiển thị **đúng trạng thái không có quyền** theo cờ quyền mà API trả về — **không** màn trắng, **không** thông báo lỗi hệ thống, **không** văng app · cả 2 tài khoản đều không thấy biểu mẫu của bot khác |  | Lấp `Q11` · Đánh giá spec: Spec ghi rõ (REQ-013 — `apiGetFormAnswerList` CỐ Ý giữ 200 + cờ quyền vì app Flutter đọc cờ này) · Evidence: ảnh chụp màn app ở cả 2 tài khoản · **manual vì** cần thiết bị thật chạy app mobile admin · ⚠️ Đây là TC **duy nhất** kiểm chứng lý do Dev viện ra để không đổi contract endpoint này |

---

## 6. Spec update needed

| # | Vấn đề | Nguồn mâu thuẫn | Cần ai chốt |
|---|---|---|---|
| 1 | **Mã HTTP khi vượt hạn mức gói Free.** Spec ghi hạn mức 3 biểu mẫu, vi phạm → **HTTP 500**. Bản fix đổi thành **403** (REQ-009) và các TC NEW-58/59/60 đã lấy 403 làm chuẩn. | `spec-features/admin/form-answer/feature-spec.md` §5 **BR-01** (nguồn `FormAnswerController.php:6928`, `:7328`) vs Redmine #40544 mục 2 + Studio REQ-009 | Leader xác nhận 403 là chuẩn mới → cập nhật BR-01 |
| 2 | **Hai điểm query chưa ràng buộc bot: là GAP hay chấp nhận được?** `saveFormAnswerPointSetting` cập nhật `form_answer.using_old_version` theo id thô; `moveFolderForItem` không kiểm `folder_move_id` thuộc bot nào. | Ticket ghi 「Mọi query đều PHẢI ràng buộc theo bot đang đăng nhập」 vs code hiện tại; TC NEW-69 (#41170) và NEW-53 (#41171) đều `fail` | **Dev + Leader** — đây là §4 issue #1, quyết định ảnh hưởng thẳng việc ticket có được đóng hay không |
| 3 | **`store` (form v1) có thuộc phạm vi "validate required đầu vào" không?** REQ-008 chỉ liệt kê 8 endpoint và `store` không nằm trong đó, nên thiếu tham số vẫn trả 500. | Description ticket ("validate required đầu vào" cho **các api, ajax**) vs REQ-008 (8 endpoint) | Leader chốt phạm vi → nếu có thì trả Dev bổ sung |
| 4 | **Hai endpoint upload ảnh đang lệch chuẩn.** `uploadImageFormAnswerRender` (màn trả lời) chặn phần mở rộng → 422; `uploadImageFormAnswer` (màn quản trị) **không chặn**, nhận tệp không đuôi và trả 200. | REQ-010 ("phần mở rộng không hợp lệ → 422") vs kết quả NEW-90 | Leader chốt có siết màn quản trị theo cùng chuẩn không |
| 5 | **Khối bắt lỗi của endpoint danh sách biểu mẫu bản v1 vẫn trả thông điệp ngoại lệ kèm 500.** TC NEW-65 tự ghi nhận đây là endpoint quản trị nên tài liệu quy ước "vẫn chấp nhận được", cần xác nhận. | Studio REQ-017 + `.claude/docs/api-error-code-rules.md` mục 2 vs hiện trạng code | Leader chốt có siết luôn nhóm endpoint quản trị không |
| 7 | **Phạm vi "các api, ajax" của ticket rộng tới đâu?** Dev chỉ kê **16 endpoint**, nhưng bề mặt thật của tính năng khoảng **61 endpoint**. Riêng **EP-19 xuất CSV** (PII: tên hiển thị, số điện thoại, email, địa chỉ) và **6 endpoint ghi** (EP-08/09/33/38/40/41) đều nhận mã đối tượng từ client mà **không nằm trong danh sách Dev kê** — chưa ai xác nhận chúng có ràng buộc bot hay không. | Description ticket (「Mọi query đều PHẢI ràng buộc theo bot đang đăng nhập」 + "validate required đầu vào" cho **các api, ajax**) vs 16 endpoint Dev kê ở journal #136752 | **Dev + Leader** — chốt: siết luôn trong #40544, hay tách ticket mới. Chạy `TC-REGSHARED001-01` trước để có danh sách chính xác. |
| 6 | **Ánh xạ 11 mã quan điểm riêng của Studio sang `framework/checklist-lme.md`** — đặc biệt `RULE-09` → `COMPAT-LEGACY-001` và `MAP-PERM-03` → `PERM-003`. | 41/112 TC đang dùng mã ngoài checklist | Leader (theo RULE-10: bổ sung mã mới kèm ID + ngày + nguồn) |
