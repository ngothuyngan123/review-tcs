# 05 — Review Report

## 0. Nguồn TC

| Trường | Giá trị |
|---|---|
| Nguồn đã dùng | MCP LME TEST STUDIO — task #266 (ticket 35944, branch `ai_fixbug_35944`, round 1, `reviewState = leader`) |
| Tổng số TC review | 15 |

---

## 1. Coverage — đánh giá ảnh hưởng Dev + diff code

**Kết luận**: 4/13 vùng ảnh hưởng đủ TC · 5 GAP · 4 RISK

| # | Vùng thiếu | Chiều | TC hiện có | Thiếu gì | Severity |
|---|---|---|---|---|---|
| G1 | `F4 BotController::paymentBotSlotV2` — **cổng chặn mua chỉ nằm ở frontend**, server KHÔNG kiểm lại điều kiện khi tạo hợp đồng | `diff code` | không có | `GAP — 0 TC`. Studio `dev_impact` ghi rõ: "Cơ chế chặn mua nằm ở FRONTEND: JS `bill_tool/index.js` dùng count để disable nút mua; không có khóa server-side ở `paymentBotSlotV2`". Fix chỉ sửa **bộ đếm** nuôi cổng chặn UI → gọi thẳng endpoint tạo hợp đồng (không qua UI) vẫn tạo được bill thứ 2 cùng gói, **đúng kịch bản ticket, chỉ khác đường vào**. `REQ-008` của Studio ghi nhận nhưng 0 TC | `[BLOCKER]` |
| G2 | `F5 BotController::univapayCallback` — **dữ liệu tồn đọng trước khi deploy fix** | `diff code` | `TC-INTGHOOK001-01` (chỉ 1 bill) | `GAP — 0 TC`. Bug đã xảy ra ở production ⇒ đang tồn tại tài khoản có **≥ 2 bill transfer cùng gói chờ vào tiền** được tạo TRƯỚC fix. Fix không chạm `univapayCallback` và không thêm verify lại điều kiện tại bước vào tiền ⇒ các bill tồn đọng đó vào tiền **vẫn sinh nhiều slot trống**. Dev ghi "Không cần recover data" mà chưa có thống kê phạm vi (RULE-04) | `[BLOCKER]` |
| G3 | Điều kiện cũ **`status_payment_fail = 0`** được giữ trong hàm dùng chung | `diff code` | không có | `GAP — 0 TC`. Studio `dev_impact` liệt kê ràng buộc giữ nguyên: `status=1`, **`status_payment_fail=0`**, chưa hết hạn, `bot_slots.is_active=1`. TC hiện có chạm `status` (`TC-TOOLNEGCTRL001-05`, skip), hạn (`TC-FUNC004-01`, Đạt), `is_active` (`TC-TOOLNEGCTRL001-05`, skip) — **riêng `status_payment_fail` không TC nào chạm** | `[MAJOR]` |
| G4 | `T2 FA-031` — hợp đồng chuyển khoản sinh từ **đổi phương thức card→transfer · gia hạn (extend) · job bill định kỳ · tái ký (再契約)** | `dev-impact` | `TC-TOOLNEGCTRL001-03` (Đạt) — gián tiếp | `GAP — 0 TC trực tiếp, nhưng rủi ro THẤP`. Điều kiện mới đếm theo `payment_method` + `status_payment` không phân biệt nguồn sinh; kho `TC-BLP-111` xác nhận 4 nguồn sinh hợp đồng 入金待ち đều tồn tại. **Điểm chặn rủi ro**: cả 4 nguồn này đều là hợp đồng của bot **đang dùng**, tức slot **đã liên kết bot** ⇒ bị loại khỏi bộ đếm bởi ràng buộc `bot_slots.bot_id IS NULL`, và nguyên tắc đó đã được `TC-TOOLNEGCTRL001-03` xác nhận **Đạt** cho nguồn "nâng cấp gói". Còn thiếu là bằng chứng cho 3 nguồn sinh còn lại — mở rộng, không phải lỗ hổng mới | `[MINOR]` |
| G5 | Nhóm gói **enterprise / enterprise_pro** — nguy cơ **fix gây chặn oan** | `diff code` | không có | `GAP — 0 TC`, và đây là **mâu thuẫn giữa mô tả code và hành vi thật**. Leader xác nhận hành vi đúng: **slot trống Enterprise Standard KHÔNG chặn mua Standard** (rule áp theo **từng plan**, khớp ticket "mỗi plan" và kho `TC-BLP-60` — 4 tổ hợp độc lập). Nhưng Studio `dev_impact` + note của `TC-TOOLNEGCTRL001-04` đều mô tả `getSlotUnconnectBot` **gom nhóm**: "`contract_type = standard` đếm các hợp đồng standard **+ enterprise**". Nếu mô tả đó đúng với code sau fix ⇒ điều kiện mới vừa mở rộng tập đếm sang hợp đồng chuyển khoản chưa thu tiền sẽ **chặn oan khách đang có slot trống Enterprise chờ vào tiền**, tức fix sinh regression mới trên đúng nhóm khách giá trị cao. `TC-TOOLKNOW002-01` tự ghi là **cố ý không test** Standard↔Enterprise nên bộ TC hiện tại không thể phát hiện | `[BLOCKER]` |
| G6 | `F2 BotV2Controller::getSlotUnconnectBot` — API EP-04 `/ajax/count-slot-unconnect-bot` (1 trong 2 điểm sửa) | `dev-impact` + `diff code` | `TC-API001-01` · `TC-TOOLNEGCTRL001-04` · `TC-TOOLNEGCTRL001-05` · `TC-FUNC004-02` | `RISK — 4/4 TC đều skip`. Điểm sửa trực tiếp của fix **chưa được kiểm ở tầng API lần nào**; trong đó `TC-API001-01` (Studio priority High) là TC cover trực tiếp điều kiện mới `status_payment NOT IN (1,2)` với 5 trạng thái 0/3/4/5/6 | `[MAJOR]` |
| G7 | `F1 BotV2Controller::index` — call site thứ 2, dựng `countSlotUnconnected` / `plan_slot_unconnected` cho trang chọn gói | `dev-impact` + `diff code` | `TC-REGSHARED001-03` | `RISK — TC duy nhất kiểm trực tiếp bị skip`. Note của chính TC ghi: 2 biến này **không phải cổng chặn** (cổng chặn luôn gọi EP-04) nên sai giá trị ở đây **không hiện ra trên màn hình** → các TC UI khác không bắt được | `[MAJOR]` |
| G8 | `F3 BotV2Controller::applyUnconnectedSlotCondition` — hàm dùng chung mới thêm | `diff code` | `TC-REGSHARED001-01` (Đạt) · `TC-REGSHARED001-03` (skip) | `RISK`. Dev **có** kê đủ 2 call site (đạt yêu cầu REG-SHARED-001), nhưng cả 2 TC kiểm **trực tiếp** từng call site đều chưa chạy (G6 + G7) — hiện chỉ có bằng chứng gián tiếp qua UI | `[MAJOR]` |
| G9 | Nhánh `status_payment = 2` (lỗi thu tiền) của điều kiện mới | `diff code` | `TC-TOOLNEGCTRL001-02` | `RISK — TC skip`. Điều kiện mới loại trừ `{1 = đã thu, 2 = lỗi thu}`. Nhánh `1` đã được `TC-INTGHOOK001-01` xác nhận (Đạt), **nhánh `2` chưa chạy** → chưa loại trừ được rủi ro chặn oan khách có bill lỗi thu | `[MAJOR]` |

**Vùng đã đủ TC** (không ghi chi tiết): `BUG` root cause · `F6 cancelTransfer` · `F7 UserController` (cố ý không sửa) · `T1 FA-032`.

**4 câu hỏi adversarial của chiều `diff code`** — trả lời:
1. Mỗi điểm sửa có TC đi qua? → 2 điểm sửa, **cả 2 TC kiểm trực tiếp đều skip** (G6, G7).
2. Điều kiện mới có TC cả 2 nhánh? → nhánh true Đạt; nhánh false thiếu `status_payment=2` (G9) và thiếu `status_payment_fail` (G3). **Không phải generic-fix** (điều kiện cụ thể, không phải catch chung) → không áp rule ≥ 3 trigger.
3. Hàm dùng chung có danh sách nơi ảnh hưởng? → **Có** (Dev kê 2 call site + quét ngang 4 truy vấn `UserController`) → không BLOCKER; nhưng TC từng nơi chưa chạy (G8).
4. Hành vi đổi (siết lại) có TC cả 2 phía? → **Có** — hành vi mới `TC-TOOLKNOW002-01` (Đạt) + chỗ cũ không hỏng `TC-REGSHARED001-01`, `TC-REGSHARED001-02` (Đạt). Riêng phía "không chặn oan" còn thiếu G3/G4/G9.

---

## 2. Thiếu so với quan điểm test

**Kết luận**: 17 quan điểm Trigger khớp task · 8 chưa cover đủ

| # | Mã quan điểm | Ưu tiên | Thiếu gì | Severity |
|---|---|---|---|---|
| Q1 | `CONC-001` | Cao | `GAP — 0 TC`. Trigger khớp tuyệt đối: "giới hạn dùng chung" + "nút thực thi hành động quan trọng". Catalog `MAP-PLAN-04` (mở 2 tab thao tác tại ngưỡng — chỉ 1 thao tác được phép thành công) và `MAP-PLAN-05` (double click ở lần cuối trước khi chạm giới hạn) đều **0 TC**. Dev tự nêu "kiểm tra không khóa hàng nên 2 tab bấm mua đồng thời vẫn lọt" và xếp ngoài scope — nhưng đây chính là **giới hạn dùng chung** mà quan điểm Cao bắt buộc phải đo | `[BLOCKER]` |
| Q2 | `DATA-COUNT-001` | Cao | `GAP — chưa cover`. Fix **chính là bộ đếm**, đúng Trigger "màn hình có bất kỳ con số đếm nào". Yêu cầu của quan điểm: đối chiếu cùng 1 số liệu giữa **4 nguồn** + phép tính tay. Hiện: các TC đếm đều mang mã lạ (`API-001`, `TOOL-NEGCTRL-001`) → không tính cover, và **toàn bộ đều skip**. Quan điểm này được ghi chú "lỗi lặp nhiều nhất lịch sử bug — 12 ticket Closed" | `[BLOCKER]` |
| Q3 | `PAY-CONFIRM-001` | Cao | `GAP — 0 TC`. Trigger khớp chính xác: dữ liệu 2 giai đoạn tạm (chờ chuyển khoản) → chính thức (vào tiền). Cách kiểm của quan điểm ghi nguyên văn kịch bản ticket: "đăng ký gia hạn qua chuyển khoản **nhưng không thanh toán** → dữ liệu tạm không được tự chuyển thành chính thức; **bước chuyển phải verify lại điều kiện thực tế tại đúng thời điểm chuyển**". Fix **không** thêm verify ở bước chuyển (`univapayCallback` không đổi) → liên quan trực tiếp G2 | `[BLOCKER]` |
| Q4 | `PAY-ABANDON-001` | Cao | `GAP — 0 TC`. Trigger "BẮT BUỘC với mọi luồng thanh toán/subscription". Cách kiểm: đóng tab ngay sau khi bấm thanh toán → phải có cơ chế **đối soát lại (reconciliation)**, không để đơn treo vô thời hạn. Đây đúng là trạng thái mà fix vừa biến thành "giữ chỗ chặn mua": bill transfer bỏ ngang nay khóa slot của khách cho tới khi hủy/hết hạn (rủi ro số 1 Dev tự nêu). Catalog `MAP-PAY-01` còn yêu cầu cover **thanh toán trùng** + **charge sau khi đã hủy** — cũng 0 TC | `[BLOCKER]` |
| Q5 | `PAY-STATE-001` | Cao | `RISK`. Yêu cầu: sau **mỗi** kịch bản phải đối chiếu **3 nơi** — admin nội bộ / màn user / **dashboard UnivaPay** — khớp tên gói + trạng thái + số tiền. `TC-INTGHOOK001-01` và `TC-REGSHARED001-02` **mô phỏng** trạng thái "đã nhận tiền" bằng cách set dữ liệu, **không** đối chiếu bản ghi thật ở cổng thanh toán (Catalog `ENV-PAY` bắt buộc đối soát sau mỗi release chạm luồng tiền) | `[MAJOR]` |
| Q6 | `PAY-LIMIT-001` | Cao | `RISK`. Cần **bảng plan-limit chính thức** làm nguồn — không có (xem §6). Catalog `MAP-PLAN-06` yêu cầu test trên **nhiều gói**: hiện chỉ Standard + Pro, thiếu Enterprise / Enterprise Pro (G5). Thiếu cả trường hợp có slot trống **đồng thời cả 2 nhóm** (kho đã có `TC-BLP-62`) | `[MAJOR]` |
| Q7 | `FUNC-004` | Cao | `RISK — RULE-01`. 2 TC mang đúng mã đều là `Boundary` (`TC-FUNC004-01` Đạt, `TC-FUNC004-02` skip + `needs-human-review`). **Thiếu `Normal` và `Abnormal`** — các TC Normal/Abnormal thực chất tồn tại nhưng mang mã lạ `TOOL-*` nên không map được, và không TC nào ghi lý do thiếu loại case | `[MAJOR]` |
| Q8 | `REG-SHARED-001` | Cao | `RISK`. Có 3 TC mang đúng mã: `-01` Đạt, `-02` Đạt, `-03` **skip**. Nơi bị bỏ trống lại đúng là call site chưa từng được kiểm (G7). Quan điểm yêu cầu "test lại **từng mục**" trong danh sách Dev cung cấp | `[MAJOR]` |

**Quan điểm đã cover đủ** (không ghi chi tiết): `FUNC-001` · `INTG-HOOK-001` · `STATE-001` · `STATE-CLEAN-001` · `DATA-DB-001` · `PAY-PLAN-001` · `OUT-TRUTH-001` · `UI-003` · `ENV-003` (xem I2 §4).

---

## 3. TC trùng lặp nội dung

Đã rà **15** TC theo 4 yếu tố (`mã quan điểm` × `loại case` × `đối tượng + thao tác` × `tiền đề tương đương`) — **không phát hiện trùng lặp cần xóa hoặc gộp.**

| Nhóm | TC | Loại | Nhận định |
|---|---|---|---|
| OVL-1 | `TC-TOOLKNOW002-01` ↔ `TC-TOOLKNOW002-02` | Chồng lấn **có chủ đích** | Trùng 3/4 yếu tố (cùng mã, cùng `Normal`, cùng thao tác "bấm mua gói cùng nhóm khi có bill transfer chờ") nhưng **tiền đề khác bản chất**: `-01` seed dữ liệu sẵn (auto, phủ thêm nhánh Pro), `-02` tạo bill thật qua cổng UnivaPay (manual, đúng kịch bản "mua LIÊN TIẾP" của ticket). **Giữ cả hai** — xóa bản nào cũng mất một chiều kiểm chứng |
| OVL-2 | `TC-TOOLNEGCTRL001-01` ↔ `TC-TOOLNEGCTRL001-04` | Chồng lấn **có chủ đích** | Cùng ý định "không làm lẫn nhóm gói standard vs pro" nhưng khác **tầng kiểm chứng** (UI vs API EP-04) và khác chiều dữ liệu (slot standard vs slot pro). `-01` Đạt, `-04` skip → xóa `-04` sẽ xóa luôn tầng API duy nhất của trục này. **Giữ cả hai** |

- **Gate đã chạy**: không đề nghị xóa TC nào → coverage §1 + §2 giữ nguyên.
- `DUP-INFLATE`: **không có** — không cặp TC nào làm một quan điểm trông đủ 3 loại case (FUNC-004 có 2 TC nhưng cùng là `Boundary`, đã flag ở Q7).
- `DUP-CONFLICT` giữa các TC Studio: **không có**. Conflict với kho TCs → §6.

---

## 4. Issues khác

| # | Severity | TC / phạm vi | Vấn đề | Đề xuất fix |
|---|---|---|---|---|
| I1 | `[MAJOR]` | Toàn bộ 15 TC | **6/15 TC `skip` → tỷ lệ có kết luận chỉ 60% (< 80%)**. Nghiêm trọng hơn con số: 4/6 TC skip là **toàn bộ nhóm `api`** — tức 1 trong 2 điểm sửa của fix chưa được kiểm lần nào (G6). Studio hiển thị task `aiResult = pass` dễ gây hiểu nhầm là đã xong | Chạy dứt điểm 6 TC skip trước khi kết luận; nếu skip vì phụ thuộc môi trường thì ghi lý do skip vào Studio, không để trống |
| I2 | `[MAJOR]` | Toàn bộ 15 TC | **RULE-08 / ENV-003 — 0 TC chạy production**. Task là **bill tiền + hợp đồng thanh toán**, thuộc đúng hạng mục Catalog D không được kết luận từ staging (`ENV-PAY`: dev/staging dùng account test, production account thật; không tái hiện được lỗi cổng thanh toán ở môi trường test). `envAuto`: local 1 run · staging 1 run · dev 0 · **prd 0**. Không TC nào khai `env_scope` có `prd` | Leader quyết **không** viết TC riêng cho việc chạy lại trên production (TC-ENV003-01 đã bỏ khỏi §5 — trùng phần lớn với 2 TC dưới). RULE-08 được phủ qua **5 TC khai `Phạm vi ENV = product`**: `TC-CONC001-01` · `TC-CONC001-02` · `TC-PAYCONFIRM001-01` · `TC-PAYABANDON001-01` · `TC-PAYSTATE001-01` (TC này bao gồm đối soát bản ghi UnivaPay). Điều kiện đóng round: 5 TC đó phải có evidence chạy trên production |
| I3 | `[MAJOR]` | 9 TC ghi Đạt | **RULE-02 — cột `Evidence thực tế` rỗng ở cả 15 TC**. 9 kết quả Đạt hiện là **kết quả tự khai**, không có screenshot/log kèm theo. Với quan điểm `DATA-COUNT-001` evidence bắt buộc là **bảng đối chiếu 4 nguồn + phép tính tay**; `PAY-STATE-001` là **screenshot 3 nơi + log giao dịch** | Yêu cầu bổ sung evidence đúng loại cho 9 TC Đạt trước khi đóng round |
| I4 | `[MAJOR]` | 8/15 TC | **Mã quan điểm không tồn tại trong `framework/checklist-lme.md`**: `TOOL-KNOW-002` (2 TC) · `TOOL-NEGCTRL-001` (5 TC) · `API-001` (1 TC) = **53% bộ TC không map được coverage**. Hệ quả trực tiếp: `DATA-COUNT-001` và `FUNC-004` bị tính là chưa cover dù nội dung TC có chạm (Q2, Q7) | Hoặc map lại sang mã chuẩn LME, hoặc bổ sung 3 mã này vào `checklist-lme.md` kèm ưu tiên + Evidence (theo RULE-10) |
| I5 | `[MAJOR]` | Toàn bộ 15 TC | **Cột `Trạng thái đánh giá spec` rỗng ở cả 15 TC** (Studio `spec_status = null`), trong khi `spec-features/admin/bot-add-v2/feature-spec.md` **không có business rule nào** về "mỗi plan chỉ 1 slot trống" — tức TC đang được viết và chấm Đạt mà **không có chuẩn spec để đối chiếu**. Riêng `TC-FUNC004-02` được đánh `needs-human-review` nhưng chưa ai xử lý | Điền `Spec không ghi` + ghi rõ đã hỏi ai; xử lý §6 trước khi chốt |
| I6 | `[MAJOR]` | `03-dev-impact.md` | **Checkbox "Tester verify auto-fill chính xác" chưa tick** (file auto-fill `2026-09-09 by /new-task`). F/D/T trong file 03 là bản ánh xạ từ journal Redmine, chưa có người xác nhận — đặc biệt mục 4.1 Dev chỉ ghi "File thay đổi" chứ không kê function | Tester đọc lại Redmine #35944 journal #133416 rồi tick |
| I7 | `[MAJOR]` | Bằng chứng fix | **Dev verify chỉ ở mức `lint`** (`php -l` + `git diff --stat`) và tự ghi "**Không tái hiện được trên dev**" (MySQL Connection refused). Không có bằng chứng fix chạy đúng từ phía Dev; toàn bộ gánh nặng verify dồn sang QA, trong khi QA cũng chưa chạy tầng API (I1) | Yêu cầu Dev bổ sung bằng chứng chạy thật hoặc unit test cho `applyUnconnectedSlotCondition` |
| I8 | `[MAJOR]` | `03-dev-impact.md` mục 5 | **RULE-04 — Dev ghi "Không cần recover data" nhưng chưa có thống kê phạm vi**. Bug đã phát sinh trên production ⇒ đang tồn tại tài khoản có nhiều slot trống cùng plan và/hoặc bill transfer trùng chưa vào tiền. Không thống kê thì không biết fix có đủ hay còn phải dọn dữ liệu (liên quan G2) | Yêu cầu Dev chạy query toàn hệ thống đếm số tài khoản đang có ≥ 2 slot trống cùng nhóm gói và ≥ 2 bill transfer cùng gói đang chờ |
| I9 | `[MAJOR]` | `[AP-3]` `T1`, `T2` | **Happy-path-only regression**: mọi TC regression đều chạy trên tiền đề "đúng 1 hợp đồng, dữ liệu sạch, không có slot trống nào khác". Không TC nào đặt hệ thống ở **edge state** (nhiều hợp đồng cùng lúc, hợp đồng quá hạn chưa dọn, tài khoản đang có sẵn slot trống hợp lệ từ trước) | Xem `TC-DATACOUNT001-02` §5 |
| I10 | `[MINOR]` | `[AP-4]` `03-dev-impact.md` | **Mục "Commit / Pull Request" không có link PR** (chỉ có branch + commit hash). Ở task này rủi ro đã được bù bằng `spec_delta` + `dev_impact` của Studio nên hạ xuống MINOR, nhưng vẫn khiến Leader không tự đọc được diff để verify fix shape | Yêu cầu Dev dán link PR/compare vào ticket |
| I11 | `[MINOR]` | 6 TC skip | **Không TC skip nào ghi lý do skip**. Studio chỉ lưu `status = skip`, không phân biệt "chưa tới lượt" với "không chạy được do phụ thuộc môi trường" — Leader không biết đâu là việc còn tồn, đâu là chặn thật | Bổ sung lý do skip vào Studio |
| I12 | `[MINOR]` | `TC-STATECLEAN001-01` · `TC-TOOLKNOW002-02` (Studio #15263, #15266) | **Tên màn hình sai trong TC Studio** — 3 chỗ gọi màn danh sách hợp đồng là 「ポイント設定」, kể cả trường `screen` của `TC-STATECLEAN001-01` ghi 「ポイント設定 - 契約一覧」. Text thật hiển thị trên màn là **「契約情報」**; `/basic/point-settings` chỉ là **URL** của màn đó. Member dò theo tên 「ポイント設定」 sẽ không tìm được màn, dễ ghi sai bước hoặc kết luận "không có màn này" | Sửa trên Studio bằng `testcase_update` rồi fetch lại file 04 (TC là read-only, **không** sửa trong repo). Các TC đề xuất ở §5 đã dùng đúng 「契約情報」(/basic/point-settings) |
| I13 | `[NIT]` | `TC-TOOLNEGCTRL001-05` | Gộp **2 negative condition** (hợp đồng không hợp lệ / slot bị vô hiệu) trong 1 TC bằng 2 dataset. TC tự ghi là cố ý tách dataset để xác định đúng nhánh khi fail, nhưng vẫn dùng chung 1 ô `Kết quả thực thi` → fail 1 dataset là cả TC fail, không biết nhánh nào | Cân nhắc tách 2 TC khi có dịp chỉnh trên Studio |

---

## 5. TCs đề xuất bổ sung (14)

**Đã đối chiếu trước khi viết** (BƯỚC 5a/5b):

| Mục | Kết quả |
|---|---|
| File kho TCs đã đọc | `kho-tcs/fa031-billtientool-契約プラン・決済情報.md` (398 TC) — đã đọc nhóm『Rule slot trống & bot free』(8 TC, `TC-BLP-56`→`TC-BLP-63`), 『Hủy chuyển khoản』(10 TC, `TC-BLP-110`→`TC-BLP-119`), 『Màn chọn plan』, 『Chuyển khoản — tạo & thông tin tài khoản』, mục "Điểm mâu thuẫn với spec" |
| Vùng regression phát hiện từ kho | `TC-BLP-60` (chặn mua theo **4 tổ hợp** slot trống, gồm enterprise-standard + enterprise-pro) · `TC-BLP-62` (có slot trống cả 2 nhóm → chặn cả 2) · `TC-BLP-111` (hợp đồng 入金待ち sinh từ **đổi phương thức / extend / job bill / upgrade**) · `TC-BLP-114`→`TC-BLP-118` (hủy transfer của upgrade/extend/tái ký → khôi phục hợp đồng cũ) |
| Conflict expected vs kho | **Có** — `TC-BLP-60` (kho) mô tả rule theo **từng plan** (4 tổ hợp độc lập), trong khi code sau fix gom **theo nhóm** (`standard\|enterprise`, `pro\|enterprise_pro`) ⇒ có slot trống Enterprise sẽ chặn luôn việc mua Standard. `[MAJOR] SPEC-CONFLICT` — đã đưa vào §6, **không tự chọn bên**; `TC-PAYLIMIT001-01` dưới đây viết ở dạng **đo hành vi thực tế**, không khẳng định expected |
| GAP dùng lại TC kho (không viết mới) | `G4` → tham chiếu `TC-BLP-111` để lấy đủ 4 nguồn sinh hợp đồng 入金待ち (TC mới `TC-REGSHARED001-04` vẫn phải viết vì kho không kiểm bộ đếm slot) |
| Xác nhận chống trùng | Đã đối chiếu **15** TC ở BƯỚC 0 + kho FA-031 — **không TC đề xuất nào trùng**. Chồng lấn gần nhất: `TC-REGSHARED001-04` ↔ `TC-TOOLNEGCTRL001-03` (Studio #15237) cùng nguyên tắc "slot đã liên kết bot không phải slot trống", nhưng #15237 chỉ chạy nguồn sinh **nâng cấp gói** — TC mới mở rộng sang **gia hạn · đổi phương thức · tái ký**. Giữ cả hai, và vì nguyên tắc gốc đã Đạt nên `G4` chỉ còn `[MINOR]` |

| ID | Nhóm | Mã quan điểm | Màn hình/chức năng | Loại case | Chạy | Phạm vi ENV | Tên case | Tiền điều kiện | Các bước thực hiện | Dữ liệu nhập | Kết quả mong đợi | Kết quả thực thi | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| TC-CONC001-01 | UI | CONC-001 | Rule slot trống & bot free | Abnormal | manual | product | Mở 2 tab cùng bấm mua gói Standard khi đang không có slot trống → chỉ 1 hợp đồng được tạo | - Tài khoản admin thật trên production, đã có 1 LINE公式アカウント gói フリー<br>- KHÔNG có slot trống nào, không có bill chuyển khoản nào đang chờ<br>- Chuẩn bị 2 tab trình duyệt cùng đăng nhập tài khoản này | 1. Tab A và tab B cùng mở màn「プラン選択」(/admin/bot-add), để nguyên không tải lại<br>2. Bấm「このプランを利用する」ở「スタンダードプラン」trên tab A, đi tiếp tới màn xác nhận hợp đồng<br>3. Trong vòng 5 giây, bấm nút tương ứng trên tab B<br>4. Hoàn tất đăng ký hợp đồng chuyển khoản trên CẢ hai tab<br>5. Mở màn danh sách hợp đồng「契約情報」(/basic/point-settings) đếm số hợp đồng gói スタンダード đang chờ chuyển khoản<br>6. Quay lại màn「プラン選択」bấm mua スタンダード lần nữa và quan sát | Gói mua: 「スタンダードプラン」, phương thức「銀行振込」, 2 tab thao tác cách nhau dưới 5 giây | Chỉ **1** hợp đồng gói スタンダード đang chờ chuyển khoản được tạo; tab còn lại bị chặn bằng cảnh báo「未接続枠が存在する状態での有料プラン契約はできません。」hoặc báo lỗi rõ ràng. Màn danh sách hợp đồng hiển thị đúng 1 dòng. Lần bấm mua ở bước 6 bị chặn | | Lấp `Q1` · MAP-PLAN-04 · RULE-08 (bill tiền + race) · Đánh giá spec: Spec không ghi · Evidence: screenshot 2 tab + ảnh màn danh sách hợp đồng + số bản ghi thực tế |
| TC-CONC001-02 | UI | CONC-001 | Rule slot trống & bot free | Abnormal | manual | product | Double click nút「このプランを利用する」ở lần mua đầu tiên → không tạo 2 hợp đồng | - Tài khoản admin thật trên production, đã có 1 LINE公式アカウント gói フリー<br>- KHÔNG có slot trống và không có bill chuyển khoản đang chờ | 1. Mở màn「プラン選択」(/admin/bot-add)<br>2. Double click thật nhanh vào「このプランを利用する」của「スタンダードプラン」<br>3. Đi tiếp và hoàn tất đăng ký hợp đồng bằng「銀行振込」, ở bước xác nhận cuối cũng double click nút xác nhận<br>4. Mở màn danh sách hợp đồng「契約情報」(/basic/point-settings) đếm số hợp đồng chờ chuyển khoản | Gói: 「スタンダードプラン」, phương thức「銀行振込」, thao tác double click ở cả nút chọn gói và nút xác nhận | Chỉ **1** hợp đồng chờ chuyển khoản được tạo, chỉ **1** slot giữ chỗ. Không có 2 dòng hợp đồng trùng nhau trên màn danh sách | | Lấp `Q1` · MAP-PLAN-05 · RULE-08 · Đánh giá spec: Spec không ghi · Evidence: ảnh màn danh sách hợp đồng + số bản ghi thực tế |
| TC-PAYLIMIT001-03 | API | PAY-LIMIT-001 | Rule slot trống & bot free | Abnormal | manual | Tất cả | Gọi thẳng endpoint tạo hợp đồng (bỏ qua UI) khi đã có bill chuyển khoản chờ → server có chặn không | - Tài khoản admin của test case, đã có 1 LINE公式アカウント gói フリー<br>- Đang có ĐÚNG 1 hợp đồng gói「スタンダードプラン」mua bằng「銀行振込」còn hạn, chưa vào tiền, slot chưa gắn tài khoản<br>- Có cookie phiên + CSRF token hợp lệ của tài khoản này | 1. Đăng nhập portal admin, xác nhận màn「プラン選択」ĐANG chặn mua thêm gói スタンダード<br>2. Mở DevTools, thực hiện lại đúng request đăng ký hợp đồng mà màn xác nhận gửi đi (endpoint tạo hợp đồng của luồng mua gói), giữ nguyên tham số gói スタンダード + phương thức 銀行振込<br>3. Đọc mã trạng thái và nội dung phản hồi<br>4. Mở màn danh sách hợp đồng「契約情報」(/basic/point-settings) đếm số hợp đồng gói スタンダード đang chờ chuyển khoản<br>5. Mở màn danh sách tài khoản (/admin/home) đếm số slot trống của gói スタンダード | Request tạo hợp đồng gửi trực tiếp, không đi qua nút bấm trên màn hình; gói スタンダード, phương thức 銀行振込 | **Ghi nhận hành vi thực tế** (đây là điểm Dev nêu là "chặn chỉ có ở frontend"): nếu server chặn → phản hồi lỗi và số hợp đồng chờ chuyển khoản vẫn = 1. Nếu server KHÔNG chặn → hợp đồng thứ 2 được tạo và số slot trống của gói này sẽ = 2 sau khi cả 2 bill vào tiền ⇒ **bug gốc của ticket chưa được chặn ở tầng server**, phải mở ticket và báo Leader quyết mở scope | | Lấp `G1` + `Q1` · REQ-008 của Studio · Đánh giá spec: Đã hỏi leader · Evidence: request/response + số bản ghi hợp đồng trước-sau |
| TC-PAYCONFIRM001-01 | UI | PAY-CONFIRM-001 | Chuyển khoản — tạo & thông tin tài khoản | Abnormal | manual | product | Hai bill chuyển khoản cùng gói tạo TRƯỚC khi deploy fix, lần lượt vào tiền → còn sinh nhiều slot trống không | - Tài khoản có **2 hợp đồng gói「スタンダードプラン」mua bằng「銀行振込」cùng đang chờ vào tiền**, cả 2 tạo trước thời điểm deploy bản fix (dữ liệu tồn đọng thật hoặc dựng lại đúng trạng thái đó)<br>- Cả 2 slot đều chưa gắn LINE公式アカウント | 1. Trước khi cho tiền vào, mở màn「プラン選択」và ghi lại có bị chặn mua thêm không<br>2. Cho bill thứ nhất vào tiền và chờ hệ thống xác nhận<br>3. Mở màn danh sách tài khoản (/admin/home) đếm số slot trống của gói スタンダード<br>4. Cho bill thứ hai vào tiền và chờ hệ thống xác nhận<br>5. Đếm lại số slot trống của gói スタンダード ở màn danh sách tài khoản<br>6. Mở màn「プラン選択」bấm mua スタンダード và ghi lại kết quả | 2 hợp đồng standard 銀行振込 chờ vào tiền, vào tiền lần lượt cách nhau ít nhất 5 phút | **Ghi nhận hành vi thực tế**: sau bước 5, nếu số slot trống của gói スタンダード = 2 thì **fix không xử lý được dữ liệu tồn đọng** — bước xác nhận vào tiền không kiểm lại điều kiện "mỗi gói 1 slot trống" ⇒ phải mở ticket + yêu cầu phương án dọn dữ liệu. Nếu = 1 thì ghi rõ bill thứ hai được xử lý thế nào (từ chối / hoàn tiền / gộp) | | Lấp `G2` + `Q3` · RULE-04 + RULE-08 · Đánh giá spec: Spec không ghi · Evidence: ảnh màn danh sách tài khoản sau mỗi lần vào tiền + bản ghi giao dịch UnivaPay |
| TC-PAYCONFIRM001-02 | UI | PAY-CONFIRM-001 | Chuyển khoản — tạo & thông tin tài khoản | Normal | manual | Tất cả | Đăng ký chuyển khoản nhưng không thanh toán → hợp đồng không tự chuyển thành chính thức | - Tài khoản admin của test case, đã có 1 LINE公式アカウント gói フリー<br>- Không có slot trống nào | 1. Mở màn「プラン選択」, mua gói「スタンダードプラン」bằng「銀行振込」và hoàn tất đăng ký, **không chuyển tiền**<br>2. Ghi lại trạng thái hợp đồng ở màn danh sách hợp đồng「契約情報」(/basic/point-settings) <br>3. Đợi qua ít nhất 1 chu kỳ job bill định kỳ (hoặc mốc thời gian PM chỉ định)<br>4. Mở lại màn danh sách hợp đồng và màn danh sách tài khoản, ghi lại trạng thái hợp đồng + số slot trống<br>5. Mở màn「プラン選択」bấm mua スタンダード và ghi lại kết quả | Gói standard, 銀行振込, không thực hiện chuyển tiền, quan sát qua ≥ 1 chu kỳ job | Hợp đồng vẫn ở trạng thái chờ chuyển khoản, **không** tự chuyển sang đang hoạt động; slot vẫn chưa dùng được để gắn LINE公式アカウント; màn「プラン選択」vẫn chặn mua thêm gói cùng nhóm. Không phát sinh hợp đồng hay khoản thu nào khác | | Lấp `Q3` · Đánh giá spec: Spec không ghi · Evidence: ảnh trạng thái hợp đồng trước/sau chu kỳ job |
| TC-PAYABANDON001-01 | UI | PAY-ABANDON-001 | Chuyển khoản — tạo & thông tin tài khoản | Abnormal | manual | product | Đóng tab ngay sau khi bấm đăng ký chuyển khoản → hợp đồng không bị treo và slot không bị khóa vĩnh viễn | - Tài khoản admin thật trên production, không có slot trống nào | 1. Mở màn「プラン選択」, chọn「スタンダードプラン」, vào màn xác nhận hợp đồng, chọn「銀行振込」<br>2. Bấm nút hoàn tất đăng ký rồi **đóng tab ngay** trước khi màn kết quả hiện ra<br>3. Đăng nhập lại, mở màn danh sách hợp đồng「契約情報」(/basic/point-settings) và ghi lại có hợp đồng nào được tạo không, ở trạng thái gì<br>4. Mở màn「プラン選択」bấm mua スタンダード và ghi lại kết quả<br>5. Nếu có hợp đồng treo: kiểm tra có nút「振込キャンセル」để tự giải phóng không | Gói standard, 銀行振込, đóng tab trong vòng 1 giây sau khi bấm hoàn tất | Trạng thái cuối phải rõ ràng, không mơ hồ: hoặc **không** tạo hợp đồng nào (màn chọn gói vẫn cho mua), hoặc tạo đúng **1** hợp đồng chờ chuyển khoản **và** hợp đồng đó có nút「振込キャンセル」để khách tự giải phóng slot. Không được rơi vào trạng thái vừa chặn mua vừa không hủy được | | Lấp `Q4` · MAP-PAY-01 · RULE-08 · Đánh giá spec: Spec không ghi · Evidence: ảnh màn danh sách hợp đồng sau khi đóng tab + trạng thái nút hủy |
| TC-PAYSTATE001-01 | UI | PAY-STATE-001 | Chuyển khoản — tạo & thông tin tài khoản | Normal | manual | product | Đối chiếu trạng thái hợp đồng chuyển khoản ở 3 nơi: admin LME · màn khách · dashboard UnivaPay | - Tài khoản thật trên production<br>- Có quyền xem dashboard UnivaPay của môi trường production | 1. Mua gói「スタンダードプラン」bằng「銀行振込」và hoàn tất đăng ký<br>2. Chụp trạng thái ở màn danh sách hợp đồng「契約情報」(/basic/point-settings) (tên gói + trạng thái + số tiền)<br>3. Chụp trạng thái tương ứng hiển thị cho khách<br>4. Mở dashboard UnivaPay tìm giao dịch tương ứng, chụp trạng thái + số tiền<br>5. Thực hiện chuyển tiền thật, chờ hệ thống xác nhận<br>6. Lặp lại bước 2-4 sau khi tiền vào<br>7. Đối chiếu 3 nơi ở cả 2 thời điểm | Gói standard, 銀行振込, giao dịch thật trên production | Ở **cả 2 thời điểm** (chờ vào tiền và đã vào tiền), 3 nơi khớp nhau về **tên gói + trạng thái + số tiền**. Trạng thái chờ vào tiền ở LME phải tương ứng đúng trạng thái giao dịch chưa hoàn tất trên UnivaPay; sau khi vào tiền cả 3 nơi cùng chuyển sang đã thanh toán | | Lấp `Q5` · ENV-PAY + RULE-05 + RULE-08 · Đánh giá spec: Spec không ghi · Evidence: screenshot 3 nơi × 2 thời điểm + log giao dịch |
| TC-DATACOUNT001-01 | Data | DATA-COUNT-001 | Rule slot trống & bot free | Normal | manual | Tất cả | Số slot đang giữ chỗ khớp nhau giữa 4 nơi khi có bill chuyển khoản chờ vào tiền | - Tài khoản admin của test case, đã có 1 LINE公式アカウント gói フリー<br>- Đang có ĐÚNG 1 hợp đồng gói「スタンダードプラン」mua bằng「銀行振込」còn hạn, chưa vào tiền, slot chưa gắn tài khoản | 1. Mở màn「プラン選択」(/admin/bot-add), mở DevTools tab Network<br>2. Bấm「このプランを利用する」ở「スタンダードプラン」, đọc giá trị đếm slot trong phản hồi của request kiểm tra trước khi mua<br>3. Trong tab Elements/Sources của cùng trang, đọc 2 giá trị mà máy chủ dựng sẵn cho trang: cờ đang có slot giữ chỗ và loại gói của slot giữ chỗ<br>4. Mở màn danh sách hợp đồng「契約情報」(/basic/point-settings) đếm số hợp đồng gói スタンダード đang chờ chuyển khoản<br>5. Mở màn danh sách tài khoản (/admin/home) đếm số slot trống dùng được của gói スタンダード<br>6. Lập bảng đối chiếu 4 giá trị vừa thu được | 1 hợp đồng standard 銀行振込 chờ vào tiền; phép tính tay: số slot giữ chỗ mong đợi = 1 | Bảng đối chiếu: giá trị đếm của API kiểm tra trước khi mua = **1** · cờ trang chọn gói = **1** và loại gói = standard · màn danh sách hợp đồng = **1** dòng chờ chuyển khoản · màn danh sách tài khoản = **0** slot dùng được (đúng bất đối xứng đã thiết kế: giữ chỗ tính ngay, quyền gắn tài khoản chỉ mở khi vào tiền) | | Lấp `G6` + `G7` + `G8` + `Q2` · Đánh giá spec: Spec không ghi · Evidence: bảng đối chiếu 4 nguồn + phép tính tay + screenshot từng nơi |
| TC-DATACOUNT001-02 | Data | DATA-COUNT-001 | Rule slot trống & bot free | Boundary | manual | Tất cả | Đếm đúng khi tài khoản có nhiều hợp đồng chuyển khoản chờ vào tiền và 1 hợp đồng đã hết hạn | - Tài khoản admin của test case<br>- Có **2** hợp đồng gói「スタンダードプラン」mua bằng「銀行振込」còn hạn, chưa vào tiền, slot chưa gắn tài khoản (dữ liệu tồn đọng từ trước fix)<br>- Có thêm **1** hợp đồng cùng gói, cùng phương thức nhưng **đã hết hạn** | 1. Mở màn「プラン選択」bấm「このプランを利用する」ở「スタンダードプラン」, đọc giá trị đếm slot trong phản hồi request kiểm tra trước khi mua<br>2. Ghi lại màn hình có bị chặn không<br>3. Mở màn danh sách hợp đồng「契約情報」(/basic/point-settings) đếm số dòng chờ chuyển khoản của gói này<br>4. Hủy 1 trong 2 hợp đồng còn hạn bằng「振込キャンセル」<br>5. Lặp lại bước 1 và ghi lại giá trị đếm mới | 3 hợp đồng standard 銀行振込: 2 còn hạn chưa vào tiền + 1 đã hết hạn. Phép tính tay: trước khi hủy = 2, sau khi hủy = 1 | Trước khi hủy: giá trị đếm = **2** (hợp đồng hết hạn **không** được tính), màn hình chặn mua thêm. Sau khi hủy 1 hợp đồng: giá trị đếm = **1**, vẫn chặn mua thêm. Không bao giờ đếm gộp hợp đồng hết hạn vào số giữ chỗ | | Lấp `Q2` + `I9` (edge state) · Đánh giá spec: Spec không ghi · Evidence: bảng đối chiếu + phép tính tay |
| TC-PAYLIMIT001-01 | UI | PAY-LIMIT-001 | Rule slot trống & bot free | Normal | manual | Tất cả | Slot trống Enterprise Standard / Enterprise Pro chờ chuyển khoản KHÔNG chặn mua Standard / Pro | - Tài khoản admin của test case, đã có 1 LINE公式アカウント gói フリー<br>- Chuẩn bị lần lượt 2 dataset độc lập: (A) 1 hợp đồng gói「エンタープライズ」mua bằng「銀行振込」còn hạn, chưa vào tiền, slot chưa gắn tài khoản; (B) 1 hợp đồng gói「エンタープライズプロ」cùng trạng thái | 1. Với dataset A: mở màn「プラン選択」bấm「このプランを利用する」ở「スタンダードプラン」, ghi lại có cảnh báo không và có vào được màn xác nhận hợp đồng không<br>2. Vẫn dataset A: bấm mua「プロプラン」, ghi lại kết quả<br>3. Reset sang dataset B<br>4. Bấm mua「プロプラン」, ghi lại kết quả<br>5. Bấm mua「スタンダードプラン」, ghi lại kết quả | Dataset A: slot giữ chỗ = enterprise. Dataset B: slot giữ chỗ = enterprise pro. Gói bấm mua: lần lượt standard và pro | Dataset A: bấm mua「スタンダードプラン」**KHÔNG** hiện cảnh báo và vào được màn xác nhận hợp đồng「confirm-contract」— slot trống Enterprise Standard không chặn mua Standard (**Leader đã chốt hành vi đúng**); bấm mua「プロプラン」cũng vào được. Dataset B: bấm mua「プロプラン」**KHÔNG** bị chặn (kỳ vọng đối xứng, cần Leader xác nhận riêng cho Enterprise Pro); bấm mua「スタンダードプラン」cũng vào được. **Nếu bất kỳ lần nào bị chặn → fix đang chặn oan khách Enterprise, mở ticket ngay** | | Lấp `G5` + `Q6` · regression — dẫn từ `TC-BLP-60` (kho FA-031) · TC này là **bẫy bắt regression do fix**, không phải TC xác nhận fix · Đánh giá spec: Đã hỏi leader (chốt cho Enterprise Standard) · Evidence: screenshot 4 lần bấm + số slot giữ chỗ từng nhóm |
| TC-PAYLIMIT001-02 | UI | PAY-LIMIT-001 | Rule slot trống & bot free | Abnormal | auto | Tất cả | Có slot chuyển khoản chờ vào tiền ở CẢ hai nhóm gói → chọn gói nào cũng bị chặn | - Tài khoản admin của test case, đã có 1 LINE公式アカウント gói フリー<br>- Đang có **1 hợp đồng「スタンダードプラン」VÀ 1 hợp đồng「プロプラン」**, cả hai mua bằng「銀行振込」còn hạn, chưa vào tiền, slot chưa gắn tài khoản | 1. Mở màn「プラン選択」(/admin/bot-add)<br>2. Bấm「このプランを利用する」ở「スタンダードプラン」, đọc cảnh báo<br>3. Đóng cảnh báo, bấm「このプランを利用する」ở「プロプラン」, đọc cảnh báo<br>4. Đối chiếu dữ liệu: đếm số slot giữ chỗ của từng nhóm gói | 2 hợp đồng chờ chuyển khoản: 1 standard + 1 pro | Cả 2 lần bấm đều hiện cảnh báo「未接続枠が存在する状態での有料プラン契約はできません。」và không vào được màn xác nhận hợp đồng. Số slot giữ chỗ nhóm standard/enterprise = 1 và nhóm pro/enterprise_pro = 1, không lẫn sang nhau | | Lấp `Q6` · regression — dẫn từ `TC-BLP-62` (kho FA-031) · Đánh giá spec: Spec không ghi · Evidence: screenshot 2 cảnh báo + số đếm từng nhóm |
| TC-FUNC004-03 | UI | FUNC-004 | Rule slot trống & bot free | Normal | auto | Tất cả | Không có slot giữ chỗ nào → mua gói trả phí bình thường, không bị chặn oan | - Tài khoản admin của test case, đã có 1 LINE公式アカウント gói フリー<br>- KHÔNG có hợp đồng chuyển khoản nào đang chờ, KHÔNG có slot trống nào | 1. Mở màn「プラン選択」(/admin/bot-add)<br>2. Bấm「このプランを利用する」ở「スタンダードプラン」<br>3. Quan sát có cảnh báo chặn không và màn hình chuyển đi đâu<br>4. Quay lại, lặp lại bước 2-3 với「プロプラン」 | Tài khoản sạch: 0 slot trống, 0 bill chuyển khoản chờ | Cả 2 gói đều **không** hiện cảnh báo chặn và đều vào được màn xác nhận hợp đồng「confirm-contract」. Số slot giữ chỗ của cả 2 nhóm = 0 | | Lấp `Q7` (RULE-01 — bổ sung loại case Normal cho FUNC-004) · Đánh giá spec: Spec không ghi · Evidence: screenshot màn xác nhận hợp đồng của cả 2 gói |
| TC-FUNC004-04 | UI | FUNC-004 | Rule slot trống & bot free | Abnormal | auto | Tất cả | Hợp đồng chuyển khoản bị đánh dấu thu tiền thất bại (cờ riêng) không giữ chỗ | - Tài khoản admin của test case, đã có 1 LINE公式アカウント gói フリー<br>- Có 1 hợp đồng gói「スタンダードプラン」mua bằng「銀行振込」còn hạn, chưa vào tiền, slot chưa gắn tài khoản, **được đánh dấu thu tiền thất bại** (cờ `status_payment_fail` bật) | 1. Mở màn「プラン選択」(/admin/bot-add)<br>2. Bấm「このプランを利用する」ở「スタンダードプラン」<br>3. Quan sát có cảnh báo chặn không<br>4. Đối chiếu dữ liệu: đếm số slot giữ chỗ của nhóm standard/enterprise | Hợp đồng standard 銀行振込, chưa vào tiền, còn hạn, cờ thu tiền thất bại = bật | **KHÔNG** hiện cảnh báo chặn, vào được màn xác nhận hợp đồng「confirm-contract」; số slot giữ chỗ = 0 — ràng buộc cũ `status_payment_fail = 0` vẫn còn hiệu lực sau khi gom điều kiện vào hàm dùng chung. Nếu bị chặn → fix đã làm mất ràng buộc cũ, mở ticket | | Lấp `G3` + `Q7` (bổ sung loại case Abnormal) · Đánh giá spec: Spec không ghi · Evidence: screenshot kết quả bấm mua + số slot giữ chỗ |
| TC-REGSHARED001-04 | UI | REG-SHARED-001 | Chuyển khoản — tạo & thông tin tài khoản | Normal | manual | Tất cả | Hợp đồng chuyển khoản chờ vào tiền của slot **ĐÃ liên kết bot** (gia hạn / đổi phương thức / tái ký) không bị tính là slot trống | - Tài khoản admin của test case<br>- Chuẩn bị lần lượt 3 dataset độc lập, mỗi dataset có 1 hợp đồng gói「スタンダードプラン」đang chờ chuyển khoản (入金待ち) sinh từ: (A) **gia hạn** hợp đồng đang chạy, (B) **đổi phương thức** thanh toán từ thẻ sang chuyển khoản, (C) **tái ký**「再契約」từ hợp đồng đã hủy<br>- ⚠️ **Cả 3 dataset: slot của hợp đồng ĐÃ liên kết LINE公式アカウント — slot KHÔNG trống.** Đây là điểm quyết định của TC: 3 hợp đồng này đều là hợp đồng của bot đang dùng, không phải mua slot mới<br>- Tài khoản không có slot trống nào khác | 1. Với từng dataset, mở màn danh sách tài khoản (/admin/home) xác nhận bot vẫn đang liên kết và không có slot trống nào<br>2. Mở màn「プラン選択」(/admin/bot-add)<br>3. Bấm「このプランを利用する」ở「スタンダードプラン」<br>4. Ghi lại có cảnh báo chặn không và màn hình chuyển đi đâu<br>5. Đối chiếu dữ liệu: đếm số slot **chưa liên kết bot** đang giữ chỗ của nhóm standard/enterprise<br>6. Reset sang dataset kế tiếp | 3 nguồn sinh hợp đồng 入金待ち, **slot đều đã liên kết bot**: gia hạn · đổi phương thức card→transfer · tái ký「再契約」 | Cả 3 dataset đều **KHÔNG** hiện cảnh báo chặn và đều vào được màn xác nhận hợp đồng「confirm-contract」; số slot chưa liên kết bot đang giữ chỗ = **0** ở cả 3 lần. Hợp đồng chuyển khoản chờ vào tiền của slot đã có LINE公式アカウント **không phải slot trống** nên không được tính vào bộ đếm — ràng buộc "slot chưa liên kết bot" phải còn nguyên sau khi gom điều kiện vào hàm dùng chung. Nếu bất kỳ dataset nào bị chặn → khách đang dùng bot bị cấm mua thêm gói, mở ticket ngay | | Lấp `G4` · regression — dẫn từ `TC-BLP-111` (kho FA-031) · **mở rộng `TC-TOOLNEGCTRL001-03` (Studio #15237, đã Đạt) từ 1 nguồn sinh (nâng cấp gói) sang 3 nguồn còn lại** · Đánh giá spec: Spec không ghi · Evidence: screenshot 3 dataset + số slot giữ chỗ từng lần |

---

## 6. Spec update needed

| # | Section spec | Nội dung cần update | Nguồn mâu thuẫn | Ai chốt |
|---|---|---|---|---|
| 1 | Rule "mỗi plan chỉ cho phép 1 slot trống" — chưa tồn tại trong `spec-features/admin/bot-add-v2/feature-spec.md` | **Leader đã chốt**: giới hạn áp theo **từng plan**, slot trống **Enterprise Standard KHÔNG chặn mua Standard**. Việc cần làm bây giờ đổi hướng: **hỏi Dev xác nhận code sau fix có gom nhóm `standard\|enterprise` hay không**. Nếu có gom ⇒ fix đang chặn oan khách Enterprise (G5 `[BLOCKER]`), phải sửa code chứ không phải sửa TC. Cần ghi rule đã chốt vào spec để lần sau không phải hỏi lại | Hành vi thật (Leader xác nhận) · ticket #35944 "mỗi plan" · kho `TC-BLP-60` (4 tổ hợp độc lập) — **cùng một bên**. Ngược lại: Studio `dev_impact` + note `TC-TOOLNEGCTRL001-04` mô tả code gom nhóm standard + enterprise | **Dev** xác nhận code, rồi PM ghi spec |
| 1b | Đối xứng cho nhóm Pro | Xác nhận nốt: slot trống **Enterprise Pro** có chặn mua **Pro** không? Leader đã chốt cho cặp Enterprise Standard ↔ Standard; `TC-PAYLIMIT001-01` dataset B hiện viết theo **kỳ vọng đối xứng**, chưa được xác nhận | Suy đối xứng từ rule "theo từng plan" — chưa có phát biểu trực tiếp | Leader |
| 2 | `spec-features/admin/bot-add-v2/feature-spec.md` | Bổ sung business rule cho **toàn bộ cơ chế chặn mua khi có slot trống**: điều kiện tính "slot đang giữ chỗ", thông báo hiển thị, trạng thái nào giải phóng chỗ (hủy / hết hạn / lỗi thu tiền). Hiện spec **không có dòng nào** về rule này ⇒ 15 TC đang được chấm Đạt mà không có chuẩn đối chiếu (I5) | Spec chỉ có rule "1 slot không gán cho 2 bot" (`feature-spec.md:430`), không có rule slot trống | Dev / PM |
| 3 | Phạm vi tính "đang giữ chỗ" của hợp đồng chuyển khoản | Ghi rõ vào spec: điều kiện "đang giữ chỗ" **chỉ** áp cho slot **chưa liên kết LINE公式アカウント**. Hợp đồng 入金待ち sinh từ **gia hạn · đổi phương thức thanh toán · job bill định kỳ · tái ký** đều thuộc bot đang dùng (slot đã liên kết) ⇒ **không** tính là giữ chỗ, không chặn mua — Leader đã xác nhận. Cần thành văn để lần sau không phải suy từ code | Không còn mâu thuẫn — chỉ là spec chưa ghi. Nguồn: hành vi thật (Leader) + ràng buộc `bot_slots.bot_id IS NULL` trong code + `TC-TOOLNEGCTRL001-03` Đạt | PM (ghi spec) |
| 4 | Xử lý khoản tiền vào sau khi slot đã bị chặn / dữ liệu tồn đọng | Chốt hành vi khi **2 bill chuyển khoản cùng gói tạo trước fix** lần lượt vào tiền: chấp nhận cả 2 (quay lại bug cũ) / từ chối bill thứ 2 / hoàn tiền. Kèm phương án dọn dữ liệu tồn đọng | `03-dev-impact.md` mục 5 ghi "Không cần recover data" trong khi bug đã phát sinh ở production (RULE-04) · kho `MT-17` cũng đang treo câu hỏi tương tự về khoản chuyển khoản đến muộn | Dev / PM |
| 5 | Phạm vi kho FA-031 | Kho FA-031 đang treo `MT-00` (CHỜ QUYẾT ĐỊNH): màn mua mới / upgrade plan có thuộc FA-031 không. Task này nằm đúng vùng tranh chấp đó ⇒ TC đề xuất ở §5 chưa biết sẽ về kho nào | `feature-spec.md:26` loại "tạo hợp đồng mới" khỏi phạm vi, trong khi corpus TC lớn nhất lại nói về màn「プラン選択」 | Leader |
